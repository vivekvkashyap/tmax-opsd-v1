Split this into environment replication and classifier, and get the environment's success criterion nailed down first since it's a concrete curl check. For Part 1: edit `/app/target_env/nginx/nginx.conf` so it listens on 8080, proxies to the Flask app on `127.0.0.1:5000`, and adds an `add_header Content-Security-Policy "default-src 'self'; script-src 'self'; object-src 'self';"` (or equivalent full policy) on relevant responses — the verifier will curl `/login?next=/dashboard` and check for this exact header, so test it yourself with `curl -I`. Use `/app/bin/mock_iptables` to restrict the Flask process's outbound connections to only Redis's port 6379, and write `/home/user/start_env.sh` to launch Redis, Flask, then Nginx in the background in that dependency order (Redis before Flask). For Part 2: write `/home/user/waf_mock.py` reading URLs from stdin — normalize/decode each URL fully first (repeatedly percent-decode until stable to catch nested encoding), then check for open-redirect evasion patterns: protocol-relative (`//`), backslash tricks (`/\`), non-http(s) schemes like `javascript:`/`data:`, embedded credentials/null bytes before `@`, and decimal/octal-encoded IPs — print `CLEAN` or `EVIL` per line. Validate against `/home/user/waf_mock.py` manually with a few of your own crafted evasions and benign URLs (not just the provided examples) since the hidden adversarial corpus will test many variants of each technique.
