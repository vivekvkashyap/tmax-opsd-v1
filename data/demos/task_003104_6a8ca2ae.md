Write and test the C parser against the exact log line format before adding aggregation logic. Use `sscanf` with a format string matching `[%19[^]]] SENSOR_LOG device=%9s temp=%f hum=%f` (or manually split on spaces/`=`) to pull timestamp, device, temp, hum from each line — be careful with the bracketed timestamp's exact width. Validate temp in [-50.0,150.0] and hum in [0.0,100.0] inclusive; drop lines failing either. For bucketing, truncate the timestamp string by zeroing the minute/second fields (keep the `T` and `Z`, replace `mm:ss` with `00:00`) rather than doing full date-math. Aggregate sums and counts per (bucket, device) pair — e.g. in a simple array of structs or a small hash map — then compute averages, and before printing, sort entries by bucket then device_id (a simple qsort with a comparator on the two string fields works) and print each with `%.2f` formatting. Write the `TOTAL:/VALID:/INVALID:` summary line last. For the wrapper script, compile with the exact `gcc` flags given, run the binary, parse VALID/INVALID out of `etl.log` (e.g. with `grep -oP` or `awk`), compute the ratio with `bc` or `awk` (avoid integer-division truncation), and create exactly one of the two marker files based on the >0.10 threshold — remove any stale marker file from a previous run first so only one exists.