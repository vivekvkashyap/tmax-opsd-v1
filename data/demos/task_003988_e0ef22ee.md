Write the Rust program to sum file sizes using `std::fs::read_dir` on `/home/user/app_data`, filtering to entries where `metadata().is_file()` is true (skip subdirectories, since only the flat directory contents count), summing `metadata().len()`, then `std::process::exit(1)` if the total is strictly greater than 5000, else `exit(0)` — produce zero stdout. In the bash script, compile with `rustc monitor.rs -o monitor` (or matching binary name), then export `TZ=Asia/Tokyo` before generating the timestamp so `date` reflects Tokyo local time (e.g. `TZ='Asia/Tokyo' date '+%Y-%m-%d %H:%M:%S'`) rather than relying on the system's default timezone. Capture `./monitor`'s exit code immediately after running it (`$?`) before any other command overwrites it, branch on 0 vs 1, and append exactly one `Status: PASS - <timestamp>` or `Status: FAIL - <timestamp>` line to `/home/user/build.log` using `>>`. Make the script executable with `chmod +x` and run it once so `build.log` is actually produced — don't just write the script and stop.