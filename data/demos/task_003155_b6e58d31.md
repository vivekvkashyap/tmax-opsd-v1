Since the binary is stripped, don't guess the FFI signature — inspect it directly: run `objdump -d /app/libcustom_crc.so` or `nm -D` (symbol table for exported names should survive strip on a shared lib) to confirm `calculate_crc` is exported, then read the disassembly of that function's prologue to see how many parameter registers it consumes on entry (System V x86-64: rdi/rsi/rdx/...) and how it uses each — a length parameter that's clearly used across a full 64-bit register (not just the 32-bit sub-register) implies `u64`, and a third integer parameter reveals the initial value the current broken binding is missing entirely. Rewrite `/home/user/crc_service/src/crc_ffi.rs`'s `extern "C"` declaration to match: `fn calculate_crc(data: *const u8, len: u64, init_val: u32) -> u32`. Then write `proto/crc.proto` defining `CrcService`/`ComputeCrc` with the specified request/response fields, implement the tonic server on `127.0.0.1:50051` calling into the fixed FFI, `cargo build --release` it and run it backgrounded. Finally write `/home/user/proxy/nginx.conf` as an HTTP/2 (`listen 8080 http2;`, or use `grpc_pass`) reverse proxy forwarding to `grpc://127.0.0.1:50051`, and start nginx with that config file explicitly (`nginx -c`). Test end-to-end with a manual gRPC call against port 8080 before finishing.