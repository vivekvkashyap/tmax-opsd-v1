Write a Python script that walks /home/user/sys_configs recursively (e.g. `os.walk` or `pathlib.Path.rglob("*.ccb")`) collecting every .ccb file regardless of nesting depth. For each file, open it in binary mode and parse the fixed header with `struct.unpack("<4sII", header_bytes)` — verify the magic equals b"CCB\x01" before trusting the rest, then read exactly `payload_length` bytes for the payload and decompress with `zlib.decompress()`, and `json.loads()` the result. Pull `timestamp`, `service`, `version` directly from the JSON, and compute `param_count` as `len(json_obj["parameters"])` rather than assuming a fixed count. Collect all rows into a list of tuples, then sort with a key of `(timestamp, service)` so ties on timestamp break alphabetically by service — Python's stable sort on a tuple key does this correctly in one call. Write the CSV manually with `csv.writer` (or plain string joins) making sure there are no spaces after commas and the header line matches exactly `timestamp,service,version,param_count`, then diff your output against the exact expected content once done to catch trailing-newline or type-formatting mismatches (integers, not floats, for timestamp and param_count).
