Replace the naive `bufio.Scanner`+comma-split with Go's `encoding/csv` reader, which correctly handles quoted fields containing embedded newlines and commas — this is the core fix, don't try to patch the scanner logic. After parsing, build a map from sensor_id to a slice of records, sort each slice by integer timestamp, then do a single forward pass per sensor: for interpolation, when you hit an empty temperature, look back to the last valid value and forward to the next valid value in the sorted sequence and take their midpoint (since the task guarantees no leading/trailing gaps and no consecutive gaps, a simple one-pass fill works). Compute the moving average only after imputation is complete, using a sliding window of up to the current + 2 previous readings for that sensor. Marshal each record as its own JSON object (one per line, not a JSON array) with fields in the given schema, rounding floats to 2 decimals before serializing. Finally write the crontab line exactly as specified (`*/15 * * * *` schedule invoking `go run /home/user/process.go`) to `/home/user/crontab.txt` — this is a file artifact, it does not need to be installed via `crontab -e`.
