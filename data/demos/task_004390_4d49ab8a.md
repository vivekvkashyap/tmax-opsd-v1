Structure the Go program as a single streaming pass using `encoding/csv`'s `Reader.Read()` in a loop (avoid loading the whole file into memory at once, even though the sample is small — the requirement is about the approach). For each row: lowercase `lang_code` for validation/output but preserve the row's original text unchanged when writing it to the invalid-rows output; validate `timestamp` with `time.Parse(time.RFC3339, ...)`, check the lowercased `lang_code` against the fixed allow-list, validate `translator_id` with `strconv.Atoi`, and reject empty `translation_key`/`translated_text` — write any row failing any single check, verbatim, to `invalid_rows.csv` in the order encountered (no header). For valid rows, keep an in-memory map keyed by `(lang_code, translation_key)` and replace the stored entry only when a new row's timestamp is strictly later than the stored one (ties keep the first-seen row) — parse timestamps to `time.Time` for correct chronological comparison rather than comparing strings lexically. Finally, collect the map's values, sort them with `sort.Slice` first by `lang_code` then by `translation_key`, and marshal each as one JSON object per line (translator_id as an integer, not a string) to `latest_translations.jsonl`.
