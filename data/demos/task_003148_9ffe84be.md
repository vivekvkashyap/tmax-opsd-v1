Load the 5 HDF5 document files in numeric order (`docs_0.h5` ... `docs_4.h5`) and `np.vstack`/`np.concatenate` them to preserve global index order 0..9999 — getting this order wrong will silently corrupt your top-5 indices. Implement `naive_search` with explicit nested Python `for` loops over queries and documents computing dot-product/(norm*norm) per pair (this is intentionally slow — that's the point, to contrast with vectorization), and `optimized_search` using pure matrix ops: normalize both matrices by their row-wise L2 norms (`np.linalg.norm(..., axis=1, keepdims=True)`) then a single matrix multiply for the full similarity matrix — no Python-level loops over queries/docs. Time each with `time.perf_counter()` around just the computation, not I/O. Compute `max_abs_error` as `np.max(np.abs(naive_sim - optimized_sim))` — expect this near machine epsilon, so if it's large, suspect a norm/broadcasting bug. Get top-5 indices per query via `np.argsort(-sim, axis=1)[:, :5]` and convert numpy types to native Python ints/lists before JSON serialization (`json.dump` chokes on numpy scalars). Finally write `run_experiment.sh` to install deps, run the python script, print the success message, and `chmod +x` it.