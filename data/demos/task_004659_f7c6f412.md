Build `/home/user/query_tool` as a script (Python with numpy is simplest and fast enough) that: loads `/app/embeddings.csv` once per invocation into a dict/array keyed by integer frame_id; if F isn't a key, print exactly `{"error": "invalid F"}` and exit 0 before doing anything else. Compute cosine similarity of F's vector against every row (including itself) via `dot / (norm_f * norm_other)`, guarding division by zero. Sort with a key that rounds similarity to 6 decimals descending, then frame_id ascending — implement this precisely (e.g. `sorted(rows, key=lambda r: (-round(sim,6), frame_id))`) since ties are common with random-looking vectors. Take the top K, compute Pearson correlation between frame_id and similarity arrays using sample (ddof=1) std, returning 0.0 if either std is 0 or K<2. Compute the 95% CI with the exact formula given (1.96 * sample_std/sqrt(K)) — use ddof=1 for the std here too. For brightness, extract each of the K frames from `/app/experiment.mp4` with ffmpeg (`select='eq(n,fid)'`, `-vframes 1`, `-pix_fmt rgb24`/PNG piped to memory) and average all R,G,B pixel values across all K frames combined — do not convert to grayscale. Round all floats to 4 decimals and print one JSON line with the exact key order/names shown. Test against several manually-computed (F,K) pairs, including out-of-range F, before considering it done — exactness matters since it's checked bit-for-bit against a hidden oracle.