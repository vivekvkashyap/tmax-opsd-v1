Work phase by phase and verify each service independently before wiring them together. Phase 1: `npm install` fails because package.json declares a peerDependencies entry for express that conflicts with the direct dependency version — either delete/relax the bad peerDependencies entry in package.json, or install with `npm install --legacy-peer-deps` (document why). Then implement evaluateExpression in server.js with a real algorithm: tokenize the string into numbers/operators/parens, then either build a recursive-descent parser (term/factor/expr functions respecting precedence and parens) or convert to postfix via shunting-yard and evaluate the RPN stack — never call eval() or a math library. Test it directly with curl against port 3001 before moving on. Phase 2: write nginx.conf with `listen 8080`, `location /api/calc { proxy_pass http://127.0.0.1:3001/... }` and `location /api/status { proxy_pass http://127.0.0.1:3002/... }`, and set `pid`, `error_log`, `access_log`, and `client_body_temp_path` all under nginx_run/ so nginx can run unprivileged (start it with `nginx -c`/`-p` pointing at that config). Phase 3: start both backends and nginx in the background, then write run_e2e.py to hit the proxy (not the backends directly), parse both JSON responses, and dump them into the exact e2e_results.json structure requested — validate by hand-computing the expression's expected value from operator precedence before trusting your parser's output.