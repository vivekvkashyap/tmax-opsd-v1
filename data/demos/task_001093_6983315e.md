Work in order since each step feeds the next. First transcribe `/app/voicemail.wav` (e.g. `whisper`/`speech_recognition` if available, or another installed ASR tool) to recover the three key/value updates spoken in the recording — don't assume the values, extract them from the actual transcript. Extract `/home/user/config_archive.tar.gz`, edit the `system.conf` key=value lines in place to reflect the three transcribed changes (leave other keys untouched), then repackage the archive so `system.conf` sits at the archive root as before. For the server, write a small Python script that starts two listeners concurrently (e.g. `threading` or `asyncio`, or `http.server` plus a raw `socket` server in a second thread): the HTTP listener on port 9000 parses `?key=` from the query string and returns the value as plain text; the raw TCP listener on port 9001 reads a newline-terminated key and writes back `value\n`. Both handlers must open `system.conf`, take an `flock`/`fcntl` shared lock, read the current value, and release the lock before responding — implement this as a shared helper function used by both protocols. Launch the server in the background and leave it running, and leave the final `system.conf` at `/home/user/`.
