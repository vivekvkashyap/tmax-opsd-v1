Work through this as three independent shell pipeline stages, verifying each before moving to the next. First: `mkdir -p /home/user/extracted_docs && unzip docs_archive.zip -d /home/user/extracted_docs`, then locate `drafts.tar.gz` inside and `tar -xzf` it with `-C /home/user/extracted_docs` so it lands correctly as `drafts/`. Second: for bulk renaming, loop over `*.txt` in the drafts directory and build the new name via shell/`sed`/`tr`: lowercase (`tr 'A-Z' 'a-z'`), replace spaces with underscores, and swap the `.txt` extension for `.md` — use `mv` per file rather than a fragile one-shot regex, and quote filenames since they contain spaces. Third: parse `changelog.log` by splitting on lines that are exactly `---` (e.g. `awk`/Python splitting on `^---$`), then for each block extract `Commit:`, `Author:`, `Date:`, and the indented lines following `Message:` (strip the leading two-space indentation but keep internal newlines joined with `\n`), filter to blocks where Author is exactly `Alice Writer`, and emit a JSON array with keys `commit`, `date`, `message` in commit order. Use Python's `json.dump` to guarantee valid escaping rather than hand-building the JSON string.
