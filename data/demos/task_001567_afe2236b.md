Build the filter first and validate it standalone against the given corpora before wiring it into the server — this isolates parsing bugs from integration bugs. In Go, read the file bytes: verify the first 4 bytes equal `BKP1`, read 1 byte for encoding-name length, read that many ASCII bytes as the encoding name, read a little-endian uint32 payload size, then read that many payload bytes. Use `golang.org/x/text/encoding/...` (e.g. `htmlindex.Get(name)` or explicit charmap/UTF-16LE decoders keyed off the parsed encoding string) to transcode the payload to UTF-8 — don't assume UTF-8 input, since the whole point is that evil payloads hide inside other encodings. After decoding, do a plain substring check for `EXEC_BASH` and `/dev/tcp/` and exit 0/1 accordingly. Loop the tool over every file in `/app/corpus/clean/` and `/app/corpus/evil/` and confirm exit codes match expectations before touching `server.go`. Only then modify the upload handler to write the request body to a temp file, `exec.Command("/home/user/filter", tmpPath).Run()`, check the exit code (via `exitErr.ExitCode()`), and branch to either the existing MinIO/Redis save + 200, or delete-temp + 403. Restart services via `/app/start.sh` and re-test end-to-end with curl.