Follow the pipeline stage-by-stage and validate each before moving to the next, since a strict fuzz-comparison verifier means every step must match bit-for-bit. First extract exactly N frames as raw grayscale with `ffmpeg -i <video> -vframes N -f rawvideo -pix_fmt gray -s 64x64 pipe:1`, capturing stdout as bytes and confirming you read exactly `N*4096` bytes (use `subprocess.run` with `stdout=PIPE`, not a shell pipeline that might truncate). Convert bytes to float32 via `np.frombuffer(..., dtype=np.uint8).reshape(N,4096).astype(np.float32)/255.0` — use the same dtype precision (float32) as a reference implementation would, since float64 vs float32 differences compound through matmul and could shift similarity scores at the 4th decimal. Load `W`/`b` from `/app/model_params.npz`, compute `embeddings = np.maximum(frames @ W + b, 0.0)` (Linear + ReLU), then cosine similarity against the parsed target vector, explicitly handling the zero-norm edge case (embedding or target norm 0 -> similarity 0.0) rather than letting it divide-by-zero to NaN. Pick the argmax with lowest-index tiebreak (iterate in order and use strict `>` comparison, never `>=`), and print only `idx,score` with score formatted to 4 decimals via `f"{score:.4f}"`. Verify no extra output (e.g. stderr noise, print statements, or ffmpeg logs) leaks to stdout.
