Break this into three independently verifiable stages before writing Rust. (1) Frame extraction: use `ffmpeg` to dump /app/experiment.mp4 frames to a temp directory (e.g. PNG/PGM), then for each frame compute mean grayscale intensity (convert RGB to luminance if needed) and mark frame indices (0-based, in ffmpeg output order) where intensity is strictly > 100.0 as "active" — verify your frame numbering against ffmpeg's actual naming/ordering convention, since an off-by-one here silently breaks everything downstream. (2) Query: connect to /app/graph.db with the `rusqlite` crate (or shell out to `sqlite3`), and since it has "fragmented/unoptimized indexes," add an index on `source`/`target` (or just build an in-memory HashSet of active IDs and do one filtered SELECT with an `IN` clause / temp table join) rather than looping per-node with individual queries — that's the naive slow path. Pull only edges where both endpoints are in the active set. (3) Clustering coefficient: build an adjacency structure (treat edges appropriately — check if the graph should be treated as undirected for neighbor counting), for each active node with degree >= 2 count actual edges among its neighbor set divided by C(deg,2), then average only over nodes meeting the degree>=2 threshold — don't include others in the denominator. Write the float to /home/user/result.txt; the checker allows tolerance 0.005 so avoid catastrophic rounding in intermediate division.