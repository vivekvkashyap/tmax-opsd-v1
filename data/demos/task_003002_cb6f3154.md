Read `/home/user/build_dataset.sh` closely before touching it: the parallel extraction stage (`xargs -P 4`) appends lines to `/home/user/singular_values.txt` in whatever order workers finish, and the final `awk` command uses `NR` (line-number-dependent) in its formula, so the result depends on file order. The fix is narrow and surgical — do not touch the extraction logic, the `awk` formula, or the parallelism; only insert a deterministic `sort -n` (numeric ascending) on `singular_values.txt` between the extraction step and the `awk` reduction, e.g. pipe through `sort -n` or rewrite the file with `sort -n -o singular_values.txt singular_values.txt` before invoking `awk`. Keep `LC_ALL=C` set so sort behaves consistently. After editing, run the script multiple times in a row and confirm `/home/user/training_label.txt` produces byte-identical output each time — that repeatability check is your own verification that the ordering is truly fixed, not the graded value itself.
