First transcribe `/app/memo.wav` (install `ffmpeg`/a speech-to-text tool as needed) to extract the exact filtering rule — listen carefully for the window size, how boundaries are handled (fewer elements available at the edges vs. wrapping or padding), what kind of division is used (integer vs floating point), and any baseline-subtraction or clipping step, since getting any one of these wrong will pass simple cases but fail edge-of-array or negative-result inputs under fuzzing. Once you have the rule, implement it in Bash entry-point `/home/user/spectral_filter.sh`: iterate over `"$@"` by index, for each index sum only the in-bounds neighbors (don't read past index 0 or the last index), divide by however many elements were actually summed using integer arithmetic (bash arithmetic `$(( ))` truncates toward zero, matching typical integer-division semantics — verify this matches what the memo describes), apply the described baseline shift, then clip to zero if negative, and finally echo all results space-separated on one line. Test manually against a few hand-computed short sequences (length 1, 2, and an even/odd mix) before trusting bit-exact equivalence against the hidden reference across many random-length arrays.