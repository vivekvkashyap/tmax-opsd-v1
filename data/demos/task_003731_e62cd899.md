Follow the filtering pipeline in the exact order specified, since each step gates the next. First list files in `/home/user/experiments` and keep only those with `os.path.getsize(f) >= 50*1024` bytes (50KB, not 50000). For each surviving file, load it (e.g. with pandas) and check that every value in the `confidence` column satisfies `0.0 <= v <= 1.0`; if even one row violates this, drop the entire file rather than just that row — this "any bad value voids the file" rule is the easy part to get wrong. Concatenate the `prediction` columns of the files that pass both filters into one combined Series/DataFrame (preserve simple row order, e.g. by iterating files in a consistent sorted order). Then draw the bootstrap sample with `combined['prediction'].sample(n=1000, replace=True, random_state=42)` exactly as specified — using this exact pandas call (not a manual `np.random.choice`) matters because the specific RNG algorithm and seed determine which rows get picked, and any deviation changes the result. Compute the mean of those 1000 sampled predictions, round to 4 decimals with `round(x, 4)`, and write only that number as text into `/home/user/bootstrap_mean.txt`.