Write the CSV parser as a real character-by-character state machine, not line-based reading — track whether you're inside an open double-quoted field, and only treat `\n` as a record separator when you are NOT inside quotes; within quotes, `\n` and `,` are literal content. Build each record as a list of fields, splitting on unquoted commas, then check if the sensor_id field equals `S-100` before touching the value column. Maintain a fixed-size rolling buffer (array of 5 doubles + index counter) that you overwrite circularly as new S-100 rows arrive, so at EOF it holds exactly the last 5 values in correct chronological order (oldest first) — be careful not to reverse the order when the buffer wraps. Compute population mean/variance (divide by N=5, not N-1), take sqrt for stddev, guard the stddev==0 case explicitly, then compute Z-scores and the Euclidean distance to the fixed reference vector using plain double arithmetic. Print with `printf` using `%.5f` for every float. Compile with gcc, test process.c against the given CSV directly before wiring the full pipeline, then background `python3 -m http.server 8080 --directory /home/user/public_html &` and confirm with curl that report.json is reachable.
