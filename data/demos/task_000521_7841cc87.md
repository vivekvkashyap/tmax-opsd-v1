Work through the steps in order and verify each before moving on. First `tar -czf /home/user/migration/backup/src_backup.tar.gz -C /home/user/migration src` to snapshot the original code. Then read server.c and locate the hardcoded socket path string (search for the `AF_UNIX`/`sun_path` assignment or a literal `.sock` string) and change it to exactly `/home/user/migration/socket/backend.sock`. Compile with `gcc server.c -o /home/user/migration/bin/backend_daemon`, launch it in the background (`... &`), and capture its PID with `$!` into `/home/user/migration/daemon.pid` — don't guess the PID from `ps` since that's error-prone; use the shell's own `$!` right after backgrounding. Give the daemon a moment to bind before curling, since a socket connection issued too early will fail even with correct code. Run the specified curl command and redirect its stdout into `/home/user/migration/test_response.txt`. Finally write `monitor.sh` to check `[ -s /home/user/migration/logs/app.log> ]`-style file-size test (using `stat -c%s` or `-s` test operator) and echo `Log Active` into `/home/user/migration/status.txt` when true, then execute it once as instructed.
