Build the tool incrementally: load the CSV with `csv.DictReader`-equivalent (Python `csv.reader`), create an in-memory `sqlite3.connect(":memory:")` DB, create a table, and bulk-insert rows with `executemany` using `?` placeholders. Use `argparse` to collect `--category`, `--min-amount`, `--sort`, `--limit`, `--offset`. Build the SQL as a template string with placeholders for the WHERE clause values (`category = ?`, `amount >= ?`) and bind those via the parameter tuple passed to `cursor.execute(sql, params)` — never string-format the category or amount into the SQL text. For `--sort`, split on `:` to get column and direction, validate both against an allow-list (known columns and `asc`/`desc`), then safely interpolate only those validated tokens directly into the `ORDER BY` clause (since placeholders can't be used there). Bind `--limit` and `--offset` as parameters too. Print only `tx_id` values, one per line, with no extra formatting. After the script works, run it twice with the two exact argument sets described in the task and redirect each run's stdout to the two specified report files — verify by eye that filtering/sorting/limiting logic matches the requested query semantics before trusting the output.