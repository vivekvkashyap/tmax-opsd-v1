Build this as a straightforward streaming C++ pipeline, implementing the embedding spec literally rather than approximating it: for each line, zero a `float vec[16]`, then for each dimension i (0..15) sum `(int)c * (i+1)` over every character in the line (excluding the trailing newline but including spaces), take `% 100`, then L2-normalize (guard divide-by-zero when the norm is 0). Keep a `std::vector<std::array<float,16>>` of accepted embeddings; for each new line's embedding compute the dot product against every accepted embedding, track the max, and accept (push to output + accepted list) only if max similarity < 0.90, otherwise discard — process strictly in file order, no reordering or parallelism. Time only the phase from opening the input file to closing the output file using `std::chrono::high_resolution_clock`, and write total_lines/kept_lines/time_seconds/throughput to benchmark.json manually (throughput = total_lines/time_seconds). Write the Makefile with `-O3`, and the run_pipeline.sh should call `make`, run `./cleaner`, then `sha256sum data/cleaned_dataset.txt | awk '{print $1}' > data/reproducibility_hash.txt`. Test end-to-end by running run_pipeline.sh and inspecting cleaned_dataset.txt for plausibility before finishing.