Write a short numpy script that loads the three input files with `np.loadtxt(..., delimiter=',')`, reshaping as needed (embeddings is 100x3, targets is length-100, artifact weights is length-3 — watch out if it loads as a 1x3 2D array and flatten it). Compute OLS via the closed form directly: `W = np.linalg.inv(X.T @ X) @ X.T @ y` (do not add a bias column — the spec explicitly says no intercept). Compute MSE for both weight vectors as `np.mean((y - X @ w)**2)`. The reproducibility check compares the two MSE values (not the weight vectors) against a `1e-5` threshold — get this comparison direction right (ols_mse vs artifact_mse, not vs weights). Round every float to exactly 4 decimal places with Python's `round()` before dumping to JSON (careful: `round()` on a numpy float should be cast to a plain `float` first so `json.dumps` doesn't choke or emit numpy repr). Match the exact key names and structure shown in the spec, then write to `/home/user/audit_report.json`.