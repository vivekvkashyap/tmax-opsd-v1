Build and verify each artifact incrementally rather than all at once. Compile the C source with the macro actually defined: `gcc -DENABLE_REVERSE=1 /home/user/src/processor.c -o /home/user/artifacts/c_linux_amd64` — test immediately with `./c_linux_amd64 abc` and confirm it prints "cba" (the code has a silent non-reversing fallback path if the macro is missing, so don't skip this check). For Go, build natively with `go build -o /home/user/artifacts/go_linux_amd64 processor.go`, and cross-compile for Windows via `GOOS=windows GOARCH=amd64 go build -o /home/user/artifacts/go_windows_amd64.exe processor.go`; confirm the Windows binary's format with `file` (expect PE32+). For the property test, fill in `test_processors_match` to call `subprocess.run([binary, input_str], capture_output=True)` for both Linux binaries and assert `.stdout` are equal — remember both binaries take the string as `argv[1]`, and hypothesis will fuzz many `input_str` values, so an off-by-one in whitespace/encoding handling will surface as a failure. Run the script as `__main__` so it exercises the hypothesis test and, on success, writes the report file exactly as already coded.