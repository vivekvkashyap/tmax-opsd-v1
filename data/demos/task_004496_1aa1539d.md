Do the graph traversal inside SQL itself with a recursive CTE, not in application C code, since the task requires a single SQL query. Anchor the recursion on tables with no row in `deps` (backup_level 0, path = table name), then recursively join `deps` to walk from each table to its dependencies, accumulating `backup_level = MAX(child_level) + 1` per table and building `path` by concatenating the current table name with `->` plus the dependency's path — since a table can have multiple dependencies at different depths, take the path through whichever dependency yields the *shortest* overall path (aggregate with something like `MIN` on path length or track depth explicitly and pick the minimal-depth branch per table, e.g. via `GROUP BY table_id HAVING level = MIN(level)` semantics) rather than just any arbitrary dependency edge. In the C program, use `sqlite3_open`, `sqlite3_prepare_v2` with your CTE query, `sqlite3_step` to iterate rows, and write each as `table_name,backup_level,path\n` to `/home/user/backup_plan.csv`, having your query itself `ORDER BY backup_level ASC, table_name ASC` so no extra sorting logic is needed. Compile with `gcc -o /home/user/planner /home/user/planner.c -lsqlite3` and run it.
