Run `tesseract /app/data/instructions.png stdout` (or via pytesseract) to read the tuning instructions image, and note the exact grid-search strategy, model type, and random_state it specifies — do not guess these. Rewrite `/app/model_pipeline.py` so all preprocessing (e.g. scaling) lives inside a single `sklearn.pipeline.Pipeline` fit only on the training split — never call `fit`/`fit_transform` on the scaler before `train_test_split`, since that leaks test-set statistics into training. Split with the `test_size=0.2` and `random_state` from the OCR'd instructions. Wrap the pipeline in the specified search (e.g. GridSearchCV) with `scoring='roc_auc'` over the given parameter grid and CV folds, refit on the full training data. For the confidence interval, resample the test set with replacement 1000 times, compute ROC AUC on each bootstrap sample, take the 2.5th/97.5th percentiles, and write `lower,upper` to `/app/ci.txt`. Finally `joblib.dump` the fitted best pipeline (the whole Pipeline object, including the scaler step) to `/app/best_model.pkl` so it can score raw, unscaled hidden inputs directly via `decision_function`.