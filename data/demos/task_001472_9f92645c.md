Build this as a pipeline of `curl`/`jq`/`awk` per input line, read with a `while read -r qid threshold; do ... done` loop so it consumes stdin until EOF. For each query: `curl -s "http://127.0.0.1:5001/features?id=$qid"` and `curl -s "http://127.0.0.1:5002/candidates"`, then for every candidate fetch its own feature vector the same way. Use `jq` to convert `null` entries to `0` right at parse time (e.g. `jq -c 'map(if . == null then 0 else . end)'`) since that's the pandas-artifact fix the task calls out — do this uniformly for both the query and candidate vectors before any math. Compute population covariance with `awk`: pass both vectors in, compute each mean, then `sum((x_i-mean_x)*(y_i-mean_y))/N` using floating point (awk's native doubles, not `bc` integer pitfalls). Filter candidates with covariance `< threshold`, then sort remaining results by covariance descending and candidate_id ascending as the tiebreak (`sort -t: -k2 -rn` won't handle the tiebreak alone — sort by a composite key or do it in awk/jq). Format the covariance with `printf "%.3f"` exactly, and match the oracle's exact text format `<query_dataset_id> -> <candidate_id>: <covariance>` byte-for-byte, including no trailing whitespace.
