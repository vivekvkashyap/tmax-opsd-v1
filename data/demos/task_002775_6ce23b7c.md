Diagnose the two failures separately with direct evidence before patching. First, run the C++ binary manually without the wrapper script and observe that it silently fails when `DATASET_PATH` isn't set — confirm by checking `run_pipeline.sh` for a missing `export DATASET_PATH=...` (pointing at the real CSV path) and for the `2>/dev/null` that's hiding the actual error; fix the script to export the variable before invoking the binary and stop swallowing stderr while debugging. Second, inspect the bootstrap-mean computation in `cleaner.cpp`: look for an accumulator declared as `int` that sums `double` feature values across many samples — that causes truncation/precision loss (and possible overflow) so the resulting mean is wrong, causing the outlier filter (deviation > 10.0 from mean) to reject everything. Fix the accumulator's type to `double` (and make sure the division that computes the mean is floating-point, not integer division). After both fixes, recompile and rerun the script, then check that `model_params.txt` actually contains sane, non-empty `m=...,c=...` values consistent with a linear fit on the surviving (non-outlier) rows.
