This is a group-by-and-sort transform, best done with `awk` + `sort` + `jq` rather than hand-rolled parsing. Read the headerless CSV columns (source_ip, target_ip, port, timestamp) and aggregate by the triple (source_ip, target_ip, port), counting occurrences into `weight` while discarding timestamp entirely. A clean approach: use `awk -F, '{print $1","$2","$3}' file.csv | sort | uniq -c` to get counts per unique triple, then reformat each line into the required JSON object with `jq -n` or a small awk/jq pipeline, making sure `port` and `weight` are emitted as JSON numbers, not strings (watch for `jq` implicitly quoting values pulled from split text — cast with `tonumber`). Apply the three-level sort exactly as specified: primary lexicographic by source, secondary lexicographic by target, tertiary numeric by port — plain `sort` on text will sort port lexicographically by default, so sort port as `-k3,3n` or coerce sort keys explicitly rather than relying on default string ordering. Write the final lines to `/home/user/graph_edges.jsonl` and diff your output structurally (e.g. via `jq -c .` on each line) against your own hand-computed expectation for a couple of rows to catch integer/string type mistakes before finishing.