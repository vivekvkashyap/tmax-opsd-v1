Work through the recon steps before writing any exploit code. OCR `/app/diagram.png` with tesseract to recover the master validation salt exactly as written (don't guess the hex value). Disassemble `/app/legacy_auth` (e.g. `objdump -d` or a disassembler) to find the offset within the payload buffer where the legacy backdoor role flag is checked, and what value it must be set to — read the actual instructions comparing/loading at that offset rather than assuming a round number. Separately, read `/app/sec_broker.c` to find where a fixed-size stack buffer is filled by an oversized `fread`/similar call — that's your CWE-121 overflow: since you can write more bytes than the buffer holds, you can control what lands at the offset you found in the legacy binary. Design the payload layout as: salt bytes at the offset the parser expects, arbitrary filler for the middle region, and the backdoor flag's required 4-byte value placed exactly at the offset you found via disassembly relative to the buffer start — keep total size minimal to satisfy the <=48 byte constraint (don't pad beyond what's structurally necessary). Write `exploit_gen.c` to emit this exact byte layout to `payload.bin`, compile it, run it, then directly test `/app/sec_broker /home/user/payload.bin` yourself and confirm the flag is printed before finishing.
