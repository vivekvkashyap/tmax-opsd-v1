Three independent deliverables — build and test each in isolation before combining. For video: `ffmpeg -i /app/rack_monitor.mp4 -vf fps=1 frame_%d.jpg` extracts one frame per second (check ffmpeg's frame-numbering convention against the video's duration so your timestamps line up correctly); then scan each frame's pixels (Python/PIL or a small C++ program) counting where R>200, G<50, B<50, flagging a frame anomalous only when that count is significant (not 1-2 stray pixels), and write matching timestamps to the output file. For the sanitizer, write a C++ program that reads the target file fully and rejects (`exit(1)`) if it contains `../`, `..%2F`, a null byte, or any line/field over 1000 characters — otherwise `exit(0)`; test against both malicious and benign payloads you construct. For the filesystem part, use `dd if=/dev/zero of=vault.img bs=1M count=50 && mkfs.ext4 vault.img`, create the proxy_cache subdirectories, symlink `alerts` into the not-yet-mounted `vault_mount` path with `ln -s`, and write the fstab line with the `loop` option in standard six-field fstab format.
