Break this into independent, testable pieces before wiring them together. First transcribe `/app/measurements.wav` using a real speech-recognition tool (e.g. Python's `SpeechRecognition` with Google/Sphinx backend, or `openai-whisper`) and parse the five spoken number-words into integers in spoken order — don't hardcode guessed numbers, actually run recognition and convert words like "eighty two" to 82 (a small word-to-number mapping or a library like `word2number` helps). Load `/app/reference_data.csv` sorted by `id` to get Variable B in matching order. Compute Pearson correlation and the sample (N-1) covariance matrix with `numpy` (`np.corrcoef`, `np.cov`), rounding each output number to exactly 4 decimal places as specified. For the server, use Python's built-in `http.server`/`BaseHTTPRequestHandler` (or Flask) bound explicitly to `127.0.0.1:9090`; in the handler for `GET /api/analysis`, check `self.headers.get('X-Researcher-Token') == 'alpha-bravo-charlie'` and return 401/403 with no body if missing/wrong, else return 200 with the exact JSON structure shown. Run the server in the background (nohup/&, or a screen/tmux session) and verify it yourself with `curl` including and excluding the header before leaving it running for the grader.