Part 1: extract frames with `ffmpeg -i /app/electrophoresis.mp4 frame_%04d.png`, then for each frame compute average grayscale intensity (e.g. `convert frame.png -colorspace Gray -format "%[fx:mean*255]" info:` or an `awk`-based pixel sum), track the frame with the maximum average using a loop that keeps a running max and its 1-indexed frame number, and write only that integer to `/home/user/max_frame.txt`. Part 2: in `/home/user/sanitizer.sh`, parse the header line's `<value>` after `# SUM:`, then extract all remaining numeric lines, sort them numerically in strictly descending order (`sort -t. -k1,1nr` or `sort -g -r` as appropriate — pure numeric sort, not string sort), and accumulate a running sum sequentially using `bc` with `scale=4` at each addition step (accumulate one-by-one rather than one big `paste+bc` sum, since order and truncation-per-step matter for reproducing "sequential" floating precision). Compare the final accumulated sum to the header value with exact string/decimal match; `exit 0` if equal, `exit 1` otherwise (also `exit 1` on malformed files, e.g. missing header, non-numeric lines, wrong format). Validate your script against both `/app/corpus/clean/` and `/app/corpus/evil/` to confirm perfect separation before finishing.
