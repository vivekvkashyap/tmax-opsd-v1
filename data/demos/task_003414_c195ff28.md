Implement each policy as an independent pass over the right file, then concatenate results in the required order (SQLi, then XSS, then brute-force) — don't try to interleave them in one scan. For Policy 1, read `source_code.txt` line by line, track the 1-based line number, and flag lines matching both `SELECT ... WHERE` (regex or substring search, case as given) and a `+` used for concatenation on that same line — a plain parameterized query (`?` placeholder, no `+`) must NOT be flagged, so test your logic against the clean example too. For Policy 2, similarly require both an HTML tag pattern (`<[A-Za-z0-9]+>`-style) and a `+` concatenation on the same line. For Policy 3, parse `auth.log` lines as `IP - STATUS`, maintain a `std::map<std::string,int>` of failed-login counts per IP, and after the full scan emit one alert per IP with count strictly greater than 3 (not `>=`), preserving first-seen or encountered order. Compile with the exact `g++ -std=c++17` command given, run it, and diff `scan_results.txt` against your own hand-derived expectation from re-reading the two input files before considering it done.
