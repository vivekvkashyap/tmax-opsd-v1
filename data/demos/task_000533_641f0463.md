Write the Go scanner as two clearly separated passes so a corrupted stream can't derail offset detection. Pass 1: read the whole binary file into memory (or use a buffered reader) and do a raw byte scan for every occurrence of `0x1F 0x8B`, including overlapping matches, recording each offset in ascending order — this must find ALL magic-byte occurrences regardless of whether they lead to valid gzip data, since offsets and recovered content are graded separately. Write these offsets, one integer per line, to gzip_offsets.txt. Pass 2: for each recorded offset, construct an `io.Reader` over the file starting at that offset (e.g. `bytes.NewReader(data[offset:])` or a `io.SectionReader`) and attempt `gzip.NewReader` followed by fully reading with `io.ReadAll` until you get a clean `io.EOF` with no other error — any error (corrupt header, CRC mismatch, unexpected EOF, truncated payload) means skip that stream entirely, do not write anything for it. On success, append the decompressed text to recovered_data.txt followed by a newline. Do not assume a fixed number of streams — let the magic-byte scan drive it. Test your program manually against the actual file and re-check the two output files' exact formatting (no extra whitespace, correct line ordering) before finishing.