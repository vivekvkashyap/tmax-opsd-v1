First OCR the policy image with `tesseract /app/operator_policy.png stdout` and read the recovered text carefully to get the exact field names and condition — don't assume standard Kubernetes semantics, use precisely what the image states (which fields must match which values, and which field to remove). Implement `/home/user/manifest_filter.py` to read stdin, `json.loads()` it defensively (catch parse errors and pass raw input through unmodified on failure), check the specific `kind`/annotation condition using safe nested `.get()` calls so missing fields don't raise, and if the condition matches, delete the specified nested field (again via safe traversal, no KeyError) while leaving every other field byte-identical — re-serialize with `json.dumps` matching key order/spacing conventions likely used by the reference (test round-tripping a manifest that doesn't match the condition to confirm it comes out unchanged). Since verification is bit-exact fuzz equivalence against a hidden reference across 1000 random manifests, be exhaustive about edge cases: missing `metadata`, missing `annotations`, non-dict `data`, and non-string annotation values. Make the script executable with the correct shebang and `chmod +x`.
