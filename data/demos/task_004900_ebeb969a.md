Follow the full debugging workflow rather than jumping straight to a fix: first make the flaky test deterministic (e.g. patch in a fixed `random.seed(N)` via a wrapper invocation, or run the suite in a loop/with `pytest -p no:randomly --count` if available) so you can reliably reproduce the AssertionError before bisecting. Use `git bisect start`, mark current `HEAD` bad and tag `v1.0` good, then in `git bisect run` execute your deterministic reproduction command each step so bisection is automatic and accurate; once it converges, capture the full 40-char SHA with `git rev-parse <bad-ref>` and write only that hash (no trailing newline issues, no extra text) to `/home/user/bad_commit.txt`. Inspect the flagged commit's diff to `src/math_utils.py`'s `apply_calibration` to understand exactly what precision-losing operation was introduced (look for string-formatting/truncation/rounding patterns applied to the float before it's returned) and revert only that logic, keeping the original calibration multiplier intact. Write `/home/user/mre.py` as a minimal script that imports `apply_calibration` and asserts its output on input `100.0` matches the multiplier's true product exactly (no rounding) — derive the expected value from the calibration constant itself, don't guess a number. Finally re-run `git bisect reset`, confirm `pytest tests/test_pipeline.py` passes across repeated runs, and confirm `python3 /home/user/mre.py` exits 0.