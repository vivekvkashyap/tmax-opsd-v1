Treat /app/graph_engine as a black-box oracle you query, not something to disassemble: run it once per node ID (`for i in $(seq 0 9999); do ./graph_engine $i; done`, capturing stdout as the list of outbound-edge targets for that node) and redirect/tag each result with its source ID to build a plain-text edge list (e.g. "src dst" per line) — this brute-force sweep across all 10000 IDs is expected and fine even if slow, since it only needs to run once. Pipe/append results into a single edges.txt. Then in C++, parse that edge list into an adjacency list (`vector<vector<int>>` sized N=10000), track out-degree per node, and implement standard PageRank: initialize all ranks to 1/N, then for 20 iterations compute `new_rank[v] = (1-d)/N + d * (sum of rank[u]/outdeg[u] for edges u->v + dangling_mass/N)`, where dangling_mass is the summed rank of all nodes with zero outbound edges redistributed evenly to every node each iteration — don't forget this term or your damping sum will be wrong for nodes with no outgoing edges. After 20 iterations, write NodeID,PageRank sorted ascending by NodeID with exactly 6 decimal places (use `std::fixed << std::setprecision(6)`). Compile with `g++ -O2` and rerun until output stabilizes.