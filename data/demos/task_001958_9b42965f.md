Start by inspecting each of the three input files' actual structure (json.load, csv.DictReader, ElementTree.parse) to build an explicit field-name mapping per format to the canonical (source, target_lang, translation, timestamp) shape — don't assume field names match across formats, the task deliberately varies them (src/source_text/source, lang/target_lang/lang, trans/translation/translated, ts/timestamp/time). Normalize source text with `unicodedata.normalize('NFKC', s).lower().strip()` — apply this consistently to every record before dedup-keying. Convert every timestamp to Unix epoch: JSON's int stays as-is, CSV's ISO8601 string needs `datetime.fromisoformat` with 'Z' handled (e.g. replace 'Z' with '+00:00') and `.timestamp()`, XML's numeric string just needs numeric parsing. Merge all records into one list, group by (normalized_source, target_lang), and within each group keep only the entry with the max timestamp. Sort the deduplicated list by target_lang then normalized_source before dumping to cleaned_translations.json with `indent=4` and the exact key names given, and separately count per target_lang for loc_stats.txt sorted alphabetically. Print intermediate normalized/timestamp values while developing to catch mapping mistakes early.