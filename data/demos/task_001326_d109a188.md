Reverse-engineer the legacy binary methodically rather than guessing parameters: run `/app/legacy_cleaner` on `/app/raw_spectra.csv`, then compare each output spectrum against the input in the frequency domain — take the FFT of both, and look at which frequency bins differ (the filtered ones will be exactly zeroed in the output's spectrum, revealing the low-pass cutoff band boundaries) rather than assuming a cutoff frequency. Separately isolate the baseline-correction step by checking whether the output's endpoints trend to zero — test the hypothesis that it's a linear baseline anchored at the mean of the first and last several points of the smoothed (not raw) signal, and confirm by checking how many edge points are averaged. Implement your Python pipeline in this exact order — FFT, zero the identified frequency band, inverse FFT, compute the two anchor means from the smoothed signal, subtract the linear interpolation between them — since order changes (e.g. baseline before smoothing) will produce a different but plausible-looking result that fails the MSE check. For the confidence interval, take the max of each cleaned spectrum, then bootstrap-resample (with replacement, 1000 iterations) that array of maxima and take the 2.5/97.5 percentiles.