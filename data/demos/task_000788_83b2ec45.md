Work the pipeline stage by stage and validate each output before proceeding. First, inspect `/app/audit_recording.mp4` for embedded subtitle/metadata streams with `ffprobe -v error -show_streams /app/audit_recording.mp4`, then extract the subtitle track text (e.g. `ffmpeg -i /app/audit_recording.mp4 -map 0:s:0 out.srt` or dump metadata) to recover the admin hash string exactly as it appears — do not guess it. Second, crack that MD5 hash by hashing each line of `/app/wordlist.txt` (`md5sum`, or a loop comparing computed hashes) until you find the matching plaintext; use the recovered password, not any assumed value. Third, decrypt with `openssl enc -d -aes-256-cbc -salt -pbkdf2 -in /app/policy.enc -out /home/user/policy.txt -pass pass:<recovered-password>`, matching the exact cipher/salt/kdf flags given. Fourth, write `secure_checker.sh`: iterate the directory with `find <dir> -maxdepth 1 -type f -print0 | while IFS= read -r -d '' f; do ...; done` (null-delimited, never word-splitting or `ls`/unquoted globs) to be immune to spaces/quotes/newlines in filenames; compute each file's SHA256 with `sha256sum`, check membership in `policy.txt` via `grep -Fxq`, then sort filenames with `sort -z`/collect-then-sort before printing `[OK]`/`[FAIL] <filename>` lines in alphabetical order. Test against filenames containing spaces, quotes, `$()`, and semicolons yourself before finishing.
