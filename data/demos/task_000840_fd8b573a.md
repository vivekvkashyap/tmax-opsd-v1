Build the pipeline as a chain of small, testable pieces rather than one monolithic script, since a naive full OLS or a wrong FFT convention will silently produce plausible-looking but incorrect numbers. Steps: (1) load the CSV matrices with `numpy.loadtxt`; use `numpy.linalg.pinv(X, rcond=1e-2)` exactly (not `lstsq` or a manual SVD with a different cutoff — the rcond value truncates small singular values and materially changes the estimate) then multiply by y to get p. (2) Implement Forward Euler literally as specified: z += dt*(-p0*z + p1*cos(p2*t)) each step, t += dt, looping `int(100/dt)` times starting from z(0)=1.0 — run it separately for dt=1.0, 0.1, 0.01 and don't reuse a coarser trajectory to approximate a finer one. (3) For the FFT, use the full dt=0.01 trajectory (10001 points), get frequencies via `np.fft.fftfreq(len(traj), d=0.01)`, take magnitude of `np.fft.fft`, explicitly zero out or exclude the freq==0 (DC) bin before argmax, and take the absolute value of the peak frequency. Round every reported value to exactly 4 decimals and write the 5 lines in the required order to `/home/user/results.txt`; make `/home/user/analyze.sh` executable and have it actually invoke the Python logic (embedded or standalone) end-to-end when run.