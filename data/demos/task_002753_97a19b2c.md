Build this as a streaming pipeline rather than loading everything into memory at once, since the emitter produces ~500k lines. Pipe `/app/log_emitter`'s stdout directly into your parser (e.g. `subprocess.Popen(..., stdout=PIPE)` or shell pipe) and process line by line. Use a regex anchored to the exact log shape `\[ISO_TIMESTAMP\] USER_ID ACTION ... bytes=NUMBER` to extract timestamp/user/action/bytes, discarding any line that doesn't match (count both total lines read and valid parsed lines for the metrics file). For dedup, compute `hashlib.md5((timestamp+user_id+action).encode()).hexdigest()` per parsed line and keep a seen-hash set, dropping repeats. For the rolling window, maintain a per-user deque/list of (timestamp, bytes) pairs; for each new event, evict entries older than `current_time - 5 minutes` from that user's window, append the current entry, then average the bytes in the window — do this incrementally, not by rescanning full history each time, for performance. For stratified sampling, keep a per-user running counter of valid deduplicated entries and emit every 10th one (1st, 11th, 21st...) in chronological order per user. Write `/home/user/rolling_averages.csv` with the exact header and 2-decimal rolling averages, and `/home/user/pipeline_metrics.json` with the three counts. Since grading uses MSE against a reference, get the window boundary condition (inclusive of both endpoints) and timestamp parsing exactly right.