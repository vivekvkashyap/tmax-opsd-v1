Build this as a genuine two-stage pipeline, not a single script. In `build_and_run.sh`, first filter `/home/user/lock_events.csv` to rows where the `Status` field is exactly `WAITING` using `awk -F, '$2=="WAITING"{print $3, $4}'` (adjust field indexing to the actual header order), then pipe the resulting `WaitingTxID HoldingTxID` pairs into your compiled `detect_cycles` binary's stdin. In `detect_cycles.c`, read pairs into a directed graph (adjacency list keyed by transaction ID, edge Waiter->Holder), then find all nodes on any cycle — a straightforward approach is DFS-based cycle detection tracking the recursion stack, marking every node currently on the stack when a back-edge to an ancestor is found as part of a cycle (run this from every unvisited node, and only mark nodes that are truly on a cycle, not just reachable from one). Print each deadlocked ID once. Back in bash, pipe the C program's output through `sort -rn | head -n 4` to get the top 4 IDs descending, and write them to `/home/user/audit_report.txt` one per line. Make the script executable and actually run it end-to-end to produce the output file.
