Run the transcription first and treat its output as ground truth for filenames — do not guess filenames from the task description itself. Write a small Python script using `whisper.load_model(...)` (a reasonably accurate model size, e.g. "base" or larger, to hit the similarity threshold) to transcribe /app/voice_memo.wav, then write the raw transcript text verbatim to /home/user/memo.txt exactly as returned (avoid over-editing/paraphrasing, since the verifier compares string similarity against the true transcript). Read that transcript back and extract the three filenames mentioned (they'll appear as distinct tokens with extensions) — cross-check each against what actually exists in /home/user/project_raw/ so a mis-transcribed token doesn't derail the rest. For each of the three located files, convert from ISO-8859-1 to UTF-8 (read bytes with `encoding='iso-8859-1'`, write out `encoding='utf-8'`) in place or to new files, leaving other files in that directory untouched. Create /home/user/staging/ and inside it make symlinks (`ln -s` or `os.symlink`) pointing at the converted files, then tar the staging directory into /home/user/delivery.tar.gz with `tar -czf ... -C /home/user staging` (or equivalent), preserving the three filenames as tar members.