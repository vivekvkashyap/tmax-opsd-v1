Write `/home/user/build.sh` as a single orchestration script, and test each step manually first before wiring it into the script. For the Makefile: read `Makefile.broken`, and since `make` requires literal tab characters before recipe lines (not spaces), use something like `sed` or `expand`/`unexpand` logic (or a `sed` substitution matching the leading 4-space run and replacing it with `\t`) to convert leading 4-space indents to real tabs, writing the result to `c_src/Makefile` — verify with `cat -A` that tabs (`^I`) are present, not spaces. Run `make` inside `c_src/` to produce `c_server`. For the Go side, apply the provided patch with `patch < proxy_fix.patch` (or `git apply`) against `proxy.go` inside `go_src/`, which corrects the backend URL the proxy forwards to, then `go build -o proxy_server proxy.go`. In the script, background-launch `c_server` then `proxy_server`, `sleep 2` to let them bind their ports, then `curl` the proxy's `/api/status` endpoint and write only the raw response body (not headers) to `/home/user/build_result.log` — use `curl -s` and be careful not to capture the injected `X-Proxy-Version` header in the body. Make the script executable but do not run it yourself; let the verifier invoke it.