Treat `/app/acl_checker` as an opaque black box you only ever invoke, never read/decompile — build the wrapper as a small Flask/FastAPI app on `127.0.0.1:9000`. Implement the security pipeline strictly in the specified order, since each stage's early-exit response depends on earlier checks not running later logic: (1) reject `file_path` containing `..` or `//` with 403 before touching the filesystem; (2) load `/home/user/secure_ledger.txt` (format: `<sha256>  <path>`, two spaces) into a dict, compute the actual file's SHA256 with `hashlib.sha256(open(path,'rb').read()).hexdigest()`, and 403 if the path isn't in the ledger or hashes differ; (3) redact SSN patterns in `justification` with a regex like `\d{3}-\d{2}-\d{4}` replaced by `***-**-****`, applied before any logging; (4) only now call the legacy binary via `subprocess.run(["/app/acl_checker", file_path, auth_token])` and use its exit code (0=granted, 1=denied) to decide GRANTED/DENIED; (5) append the audit log line in the exact specified format; (6) return the JSON response. Keep the server running in the background (e.g. `nohup ... &` or `uvicorn` daemonized) so it's listening when you finish. Test each of the four request patterns manually with curl before declaring done.