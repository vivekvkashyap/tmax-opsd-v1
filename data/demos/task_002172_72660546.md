Build and test the hook logic locally (running it as a plain script against sample files) before wiring it into git, since debugging inside a `post-receive` hook triggered by `push` is painful. Create the bare repo with `git init --bare /home/user/git-server/deploy.git`, and write `hooks/post-receive` (any language, must be executable) that reads stdin for the ref update, then does `git --work-tree=/home/user/deploy/src --git-dir=<repo> checkout -f <branch>` to materialize the pushed files. From the checked-out `virtual_fstab`, split each non-empty line on whitespace, keep rows where field 3 equals `xfs` exactly, collect field 2, sort alphabetically, and write one per line to `xfs_volumes.txt`. From `network_links.txt`, split each line into `source target port`, group by source into a dict of lists of `"target:port"` strings preserving the order links appear per source, and serialize as strict JSON (e.g. `json.dumps` with real dict, not manually concatenated strings) to `mesh.json`. After the hook works standalone, clone the bare repo to `/home/user/workspace`, add the two files, commit to `master`, and `git push` to trigger it — then inspect the generated output files.
