First generate the keypair with `ssh-keygen -t ed25519 -f /home/user/.ssh/id_ed25519 -N ""`. Then carefully read `/home/user/server.py` to identify the exact order of checks: it requires a `Cookie: session_role=admin` header, rejects requests missing `X-File-Name`, and — critically — runs its path-traversal regex check (blocking the literal substring `../`) against the *raw* header value *before* URL-unquoting it, then builds the actual filesystem path from the *unquoted* result. This is a classic check-then-decode bypass: percent-encode the traversal sequence (e.g. encode the dots or slash so the literal string `../` never appears in the header the IDS inspects) so the regex sees something benign, but `urllib.parse.unquote` later reconstructs the real `../` needed to escape `uploads/` and land at `/tmp/victim/.ssh/authorized_keys`. Write `/home/user/exploit.py` using `requests` (or raw `http.client`) to POST the public key bytes as the body with the required cookie and header, start `server.py` in the background (e.g. `nohup python3 server.py &`), then run the exploit and confirm the target file's contents match the `.pub` file byte-for-byte.
