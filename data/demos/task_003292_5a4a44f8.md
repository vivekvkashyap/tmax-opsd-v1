Run `tesseract /app/lab_notes.png stdout` (or similar) to OCR the handwritten note and read off the growth rate `r` and carrying capacity `K` values printed there — don't hardcode guessed numbers, extract them from the OCR text yourself, cleaning up any OCR misreads by cross-checking the text makes sense as decimal numbers. Then implement `/home/user/simulate_growth.sh` as a pure Euler-method integrator of the logistic ODE `dP/dt = r*P*(1-P/K)`: loop N times, each iteration compute `dP = r*P*(1-P/K)` then `P = P + dP*dt`, using `awk` (or `bc -l`) for floating point since bash arithmetic is integer-only. Read `P0`, `dt`, `N` as `$1 $2 $3` positional args, hardcode the `r` and `K` you extracted from the image as constants in the script (they are fixed simulation parameters, not command-line inputs), and print only the final `P` rounded to exactly 4 decimal places with a trailing newline. Since your script will be fuzz-tested against a reference binary across many random `(P0, dt, N)` combinations, be precise about iteration order (update P after computing the full derivative, not mid-loop) and rounding (printf `%.4f`) to match bit-for-bit.