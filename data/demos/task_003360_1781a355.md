Work through this as a chained investigation, verifying each artifact before using it in the next step. Extract the hash first: run `strings /home/user/vulnerable_service.bin | grep AUTH_SIG:` (or `grep -a -o 'AUTH_SIG:[0-9a-f]*'`) to pull the hex string following the exact prefix, stripping the prefix itself. Since it's stated to be a SHA-256 of a 4-character lowercase English word, brute-force it in Python by iterating combinations of `string.ascii_lowercase` of length 4, hashing each with `hashlib.sha256(word.encode()).hexdigest()`, and comparing against the extracted hash — this is a tiny (26^4) search space, so it completes instantly; don't try anything fancier like a dictionary unless brute force is too slow. Once cracked, generate the ed25519 key non-interactively with `ssh-keygen -t ed25519 -f /home/user/compliance_key -N "<cracked_password>"`, append the `.pub` contents to `/home/user/.ssh/authorized_keys`, then `chmod 700 /home/user/.ssh` and `chmod 600` the key files/authorized_keys. Finally write `generate_audit.py` to assemble the JSON exactly as specified (payload = 64 `A`s concatenated directly with the cracked password, no separator) and run it to produce `audit_report.json`.
