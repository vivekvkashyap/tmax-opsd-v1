Build this as three testable stages. (1) Load `/home/user/data/nodes.csv` and `/home/user/data/edges.csv` into SQLite (e.g. `.mode csv` + `.import` in a `sqlite3` script, or a small loader script) at `/home/user/graph.db`. (2) Construct `two_hop` via a self-join of `edges` on `e1.target = e2.source` requiring `e1.source != e2.target`, computing `weight = MIN(e1.weight, e2.weight)` per path, then collapse duplicate (source,target) pairs by keeping only the row with `MAX(weight)` — a `GROUP BY source, target` with `MAX(weight)` subquery or window function does this; a naive join without the final max-aggregation will leave multiple rows per pair and break downstream queries. (3) Stand up a Bash TCP listener on `localhost:9000` using `socat TCP-LISTEN:9000,fork,reuseaddr EXEC:handler.sh` (or an `nc -l` loop): on each connection read one line as `node_id`, query `two_hop` for rows where `source = node_id` (use parameter binding via `sqlite3` with `.param` or careful shell-escaping — never string-concatenate untrusted input into SQL), write matching rows to a fresh temp CSV (`source,target,weight`, no header), invoke `/app/score_oracle <tmpfile> <node_id>`, stream its stdout back to the client followed by a newline, then delete the temp file. Keep the listener backgrounded (e.g. `nohup ... &`) so it survives after your task ends.