Start by fixing the vendored dependency: open the Makefile at `/app/cJSON-1.7.15/`, find the line where the compiler variable is set to something invalid, and correct it to a real compiler (e.g. `gcc`); then run `make` there to produce the static/shared library. In your `filter_pipelines.c`, read stdin line by line, split each line on the first two commas (leaving the rest as the JSON pipeline string, since it may itself contain commas), and parse that JSON with cJSON's `cJSON_Parse`. Walk the resulting array: for each object, check if it has a `$graphLookup` key; if so, require it to also have an integer `maxDepth` field within `0..=5` inclusive — if that key is missing or out of range, drop the whole row (skip printing it) but still process remaining lines. Rows with valid JSON and no `$graphLookup`, or a compliant `maxDepth`, should be echoed unmodified to stdout. Compile with the include path and library from your fixed cJSON build (`-I... -L... -lcjson`), then self-test by piping small crafted CSVs (valid JSON with in-range/out-of-range/missing maxDepth, and non-JSON garbage) through the binary to confirm it passes clean rows through byte-for-byte and silently drops invalid ones.