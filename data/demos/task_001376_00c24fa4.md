This is a black-box reverse-engineering task: do not try to disassemble `/app/sql_generator` first — instead run it repeatedly with varied arguments (different DIRECTION values, table names, and IDs, including edge cases like empty/invalid direction) and capture the raw stdout each time with something like `/app/sql_generator ANCESTOR foo 1 | cat -A` to see exact whitespace, newlines, and indentation (spaces vs tabs matter for an exact match). Compare outputs for ANCESTOR vs DESCENDANT to isolate exactly which JOIN condition or line differs between the two recursive CTE branches. Once you've characterized the full template (including any error behavior for invalid direction, if the verifier might test that), write `/home/user/generate_query.py` using an f-string or `.format()` template that reproduces the captured structure character-for-character — pay special attention to the literal characters printed by C's `printf` (e.g. no trailing space differences) versus what Python's print naturally adds. Before finishing, run several fresh comparisons: `diff <(/app/sql_generator ANCESTOR test 5) <(python3 /home/user/generate_query.py ANCESTOR test 5)` across a handful of random inputs to sanity-check equivalence, since the real evaluation will fuzz with many random table names and IDs.
