Split the work into a video-detection stage and a CSV-processing stage, then merge. For the video: pipe `ffmpeg` frames of the video's top-left 10x10 region as raw RGB (e.g. `ffmpeg -i /app/ui_test.mp4 -vf "crop=10:10:0:0" -pix_fmt rgb24 -f rawvideo -`) into your C++ program, and for each frame check if all 100 pixels are exactly (255,255,255); count frames matching this as screen loads — do not just eyeball brightness thresholds, require an exact match to avoid noise/compression artifacts. For the CSV: parse with a proper CSV reader, drop rows where `translation` is empty or literally "MISSING", then deduplicate using a `std::set` or `std::unordered_set` keyed on the tuple (screen_id, loc_key, lang_target), keeping only the first occurrence in file order. Build a per-screen map from valid, deduplicated rows: count distinct loc_keys, and collect the sorted set of languages that had at least one valid translation. Assemble the final JSON exactly matching the required schema (including the video load count field) and write it to /home/user/loc_summary.json — field names, nesting, and array sort order must match exactly since verification is exact-match, not fuzzy.
