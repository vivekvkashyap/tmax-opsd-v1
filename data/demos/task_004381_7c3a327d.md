Build this as a strict Unix pipeline (no Python/Perl/Ruby) in clear stages: dedupe+sort, forward-fill, normalize, then rolling anomaly detection. Use `sort` on the timestamp field piped into `uniq` to remove exact duplicates and get chronological order, reattaching the header after. For `MISSING` values, process rows in order with `awk`, keeping a running "last valid value" that updates on real floats and substitutes in on `MISSING`. For normalization, lowercase, replace any non-`[a-z0-9]` character with a space via `sed`/`tr`, then squeeze repeated spaces and trim ends. For the rolling flag, maintain the last 3 imputed values in an `awk` array as you iterate row by row, compute their average only once at least 3 prior values exist, and flag `1` only when the current value strictly exceeds that average by more than 15.0. Write the four columns with the exact header to `/home/user/clean_alerts.csv`, and diff against a manual trace of the sample data before finishing.
