Implement this as three clearly separated phases in the C++ program, testing each with print statements before moving on. Phase 1: build the 10 fixed coarse bins over [0,100), read `reads.csv`, sum each read's count into whichever bin contains its location (last bin inclusive of 100); then do exactly one pass — for any bin whose total count exceeds 500, split it into two equal-width sub-bins and re-distribute the original reads into whichever child bin each read's exact location falls into (don't re-check the child bins against the threshold again, only one pass). Write the final bin count to `mesh_stats.txt`. Phase 2: for each final bin compute its midpoint as x_i and use its aggregated count as c_i; implement the log-likelihood sum given in the prompt (this avoids underflow that a naive product-of-probabilities would hit). Run Metropolis-Hastings for 10,000 iterations starting mu=50, using C++'s `<random>` with `std::mt19937` seeded with 42 and `std::normal_distribution`/`std::uniform_real_distribution` for the proposal and acceptance draws, in the exact order specified (propose, then accept/reject) so the sequence is reproducible. Discard the first 1000 samples, average the rest, and write `Mean mu: <value>` rounded to 2 decimals. Compile with the exact flags given and actually run the binary to produce both output files.