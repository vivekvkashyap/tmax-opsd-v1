Rebuild the metrics from a clean relational model instead of patching the broken cross-join query. Load nodes.csv and edges.csv into SQLite (or pandas) and compute out_degree as `COUNT(DISTINCT dst)` grouped by src (LEFT JOIN from nodes so zero-degree nodes still appear with 0), and total_bytes as `SUM(bytes)` grouped by src (also LEFT JOIN, coalesce NULL to 0). For rank, use a proper window function `DENSE_RANK() OVER (ORDER BY out_degree DESC, total_bytes DESC, node_id ASC)` — do not hand-roll tie-breaking with sorts alone, since dense rank must give equal ranks to true ties only per the ordering keys. For cumulative_bytes, use `SUM(total_bytes) OVER (ORDER BY rank ROWS UNBOUNDED PRECEDING)`, i.e., a running total in rank order, not in arbitrary row order. Join the department column from nodes.csv onto the final result. Write output ordered by rank ascending to /home/user/graph_stats.csv with the exact header `node_id,department,out_degree,total_bytes,rank,cumulative_bytes`. Verify by inspecting intermediate query results for a couple of nodes by hand before trusting the final CSV.