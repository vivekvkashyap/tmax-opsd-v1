Build the script method-first: for each N in {10,50,100,200}, use h=1/(N+1) and grid points x_i=i*h for i=1..N (interior points only — do not include the boundary nodes 0 and 1 in the N x N system). Assemble the standard tridiagonal negative-Laplacian matrix A with 2/h^2 on the diagonal and -1/h^2 on the sub/super-diagonals (e.g. via numpy.diag three times, or scipy.sparse then densify). Get the condition number specifically via SVD (scipy.linalg.svd or numpy.linalg.svd), not via numpy.linalg.cond, since the task requires computing it as max singular value / min singular value explicitly. Build b_i = sin(pi*x_i), factor A once with scipy.linalg.cho_factor, and solve with cho_solve — reuse the same factorization object for both the clean solve and the perturbed solve (don't refactor A twice, and don't accidentally use a different h or "solve" like lstsq). Compute clean_error as max|u - u_exact| with u_exact = sin(pi*x)/pi^2. For the perturbation, build p_i = 1e-6*(-1)^i indexed i=1..N (matching the same indexing convention you used for x), add to b, solve with the cached factors, and take max|u_pert - u|. Dump a nested dict keyed by string N to /home/user/sim_results.json with the three named float fields per N.
