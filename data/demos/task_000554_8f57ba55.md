Split this into a two-stage pipeline: use a scripting tool (e.g. Python+OpenCV, or ffmpeg to dump frames plus a small script) purely to extract every frame from `/app/reaction.mp4` and compute per-frame mean R, G, B, saving that as an intermediate CSV/text file — this stage does not need to be C++. The core deliverable, `/home/user/pipeline.cpp`, must read that intermediate data and implement everything else from scratch in C++ with no external math/ML libraries: (1) standardize each of R, G, B using sample mean and sample stddev with N-1 denominator; (2) compute the 3x3 covariance matrix of the standardized features; (3) run power iteration (repeated matrix-vector multiply plus normalization, >=100 iterations) starting from an arbitrary non-zero vector to converge on the dominant eigenvector — watch for sign ambiguity, which doesn't matter for regression since it's linear; (4) project each standardized RGB triple onto that eigenvector to get scalar `p`; (5) fit ordinary least-squares `y = m*p + c` against frame index using the closed-form slope/intercept formulas. Write `model_artifacts.json` (eigenvector, m, c, MSE) and `predictions.csv` with header `true_index,predicted_index` starting true_index at 0. Compile with g++, run, and sanity-check the MSE yourself before considering it done — the verifier requires MSE <= 5.0, so a bug in standardization or eigenvector convergence will be visible as high error.
