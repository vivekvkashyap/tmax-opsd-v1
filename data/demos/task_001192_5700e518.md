Run OCR on `/app/transformation_rules.png` with `tesseract /app/transformation_rules.png stdout` (or via Python `pytesseract`) and read the output carefully to recover the exact 2x3 weight matrix, the 1x2 bias vector, and the min/max clamp values under their respective section headers — double check ambiguous OCR characters (e.g. negative signs, decimal points, digit confusions like 0/O or 1/l) by re-cropping/zooming the image region if needed, since these numbers directly determine correctness. Implement the C++ program to read three doubles from stdin with `std::cin >> x1 >> x2 >> x3`, compute `y1 = W[0][0]*x1 + W[0][1]*x2 + W[0][2]*x3 + B[0]` and `y2` analogously with row 2 of W and B[1], then clamp each with `std::max(min_val, std::min(max_val, y))`. Print both values space-separated with `std::fixed << std::setprecision(4)` and a trailing newline, matching the exact format shown in the task's example. Compile with `g++ -O2 -o /home/user/transform transform.cpp` and sanity-test with a few hand-computed inputs before considering it done, since the grader fuzzes against a hidden oracle expecting bit-exact formatted output.
