In `CMakeLists.txt`, the bug is `find_package(PythonLibs 2 REQUIRED)` — change the major version to 3 (or better, switch to the modern `find_package(Python3 COMPONENTS Development REQUIRED)` and link against `Python3::Python`/use `Python3_INCLUDE_DIRS`), then re-run `cmake .` and `make` to confirm it configures and links against the Python 3 shared library. For the leak: build first, run `valgrind --leak-check=full python3 test.py` to see the leak report — it will point at the allocation inside `reverse_string`. The fix is simply to `free()` the heap buffer (`strdup`'d copy) after constructing the returned `PyObject*` and before the `return`, since `PyUnicode_FromString` copies the data rather than taking ownership. Rebuild the extension, rerun the same valgrind command, and redirect full stdout+stderr into `/home/user/migration/success.log` exactly as specified (`> success.log 2>&1`) so both the program's printed output and valgrind's summary land in the same file.
