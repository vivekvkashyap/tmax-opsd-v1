Write detect_slip.py to open the input file through gzip (e.g. `gzip.open(path, 'rb')`), then loop: read 4 bytes and verify they equal b"IBK1" (stop cleanly at EOF), read 2 bytes as little-endian unsigned short for path length, read that many bytes and decode as UTF-8 for the path, read 4 bytes as little-endian unsigned int for data length, then instead of reading the whole payload into memory just skip forward that many bytes — on a gzip stream you can't `seek` past unread data cheaply, so use `f.read(data_len)` and discard it (still avoids buffering the full file at once) or wrap in `io.BufferedReader` and `.seek(data_len, 1)` if supported, then read the 1-byte incremental flag and continue the loop. For each path collected, check unsafe patterns: starts with `/`, contains `../` or `..\\`, or has a path component equal to `..` at the end — if any path matches, print MALICIOUS and exit(1) immediately; only after scanning every record with no matches, print SAFE and exit(0). Validate against both /app/corpus/clean and /app/corpus/evil before finishing.
