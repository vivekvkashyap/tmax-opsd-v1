This is a data-cleaning-before-math bug, analogous to a plotting pipeline producing blank output from unhandled sentinel values — locate the TODO markers in `/home/user/embed_pipeline.cpp` and fill them in the right order: cleaning must happen strictly before the projection loop uses the data. For each column, do two passes: first compute mean/std over only the values that are not exactly `-999.0` (compare doubles for exact equality is fine here since the sentinel is written verbatim), then a second pass to replace `-999.0` entries with that mean and clip any value outside `[mean - 3*std, mean + 3*std]` to the nearest bound — do this in-place on the loaded matrix before it feeds the existing projection matrix multiplication. After projection, compute Euclidean distance from embedding 0 to every other embedding, sort indices by distance ascending (excluding index 0 itself), and take the first three, writing them as a comma-separated line to `/home/user/top3_similar.txt`. Since the verifier does an exact diff against a golden neighbor ordering, be careful with std computation (population vs sample) and floating point precision consistency, and test your compiled binary's output makes intuitive sense (e.g. no NaNs) before finishing.