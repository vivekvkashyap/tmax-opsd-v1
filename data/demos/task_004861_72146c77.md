Build the analysis as a clean join-then-bootstrap pipeline, and pay close attention to the exact join semantics and RNG order since the grader checks numeric output to 4 decimal places. Load the three CSVs with pandas, filter `users.csv` to region=='North', inner-join with `exposures.csv` on `user_id` to get each North user's variant, then left-join total purchase amounts per user from `purchases.csv` (group by user_id, sum amount), filling missing purchase totals with 0 — this "keep all exposed North users, defaulting no-purchase users to 0" step is the easiest place to introduce silent errors (e.g. accidentally dropping users with no purchases via an inner join). Compute `Mean(Treatment) - Mean(Control)` from the two full-precision arrays. For the bootstrap, call `numpy.random.seed(42)` immediately before the loop (not earlier), then run exactly 10000 iterations resampling Control and Treatment arrays independently, each with replacement at their original sizes, computing the mean difference per iteration; use `numpy.percentile` with `[2.5, 97.5]` on the resulting array for the CI. Round all three outputs to exactly 4 decimals and write them to `/home/user/ab_test_results.json` with keys `diff`, `ci_lower`, `ci_upper`.