Read the raw CSV bytes yourself (not via a UTF-8-only string reader) so you can detect and replace invalid byte sequences with U+FFFD instead of erroring out or silently dropping bytes — in Rust, use `String::from_utf8_lossy` (or equivalent byte-level decoding) on each field before further processing. For the reshape, iterate the three `review_*` columns per row, emit one output row `(id, month_suffix, review)` per non-empty cell, and skip cells that are exactly empty (zero-length) entirely — do not emit blank rows. Apply Unicode normalization to NFC using the `unicode-normalization` crate's `.nfc().collect::<String>()` on each review string; this matters because some input cells are already decomposed (NFD) and must be recomposed, not left as base+combining-mark sequences. Use the `csv` crate for both reading (configure it to tolerate irregular fields/handle raw bytes as needed) and writing (with header `id,month,review`) so quoting/escaping is handled correctly rather than hand-formatting CSV lines. Compile and run the program to actually produce `cleaned_reviews.csv`, then spot-check a decomposed-accent row and an invalid-byte row in the output.
