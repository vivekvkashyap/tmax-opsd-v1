Implement this as a straightforward fixed-step RK4 integrator in C, being careful about loop bounds and formatting since the verifier compares your output numerically against an independently computed reference trajectory. Define `f(t,x) = -x*x*x + cos(t)` and step with the standard four-stage RK4 update using `dt = 0.01`, iterating for exactly the right number of steps to cover `t=0` through `t=5` inclusive (that's 500 steps beyond the initial point, so the CSV should have 501 data rows plus a header). Print `t` and `x` to 4 decimal places each step, appending to `/home/user/trajectory.csv` with a `t,x` header first. While stepping, track the previous and current `x` values; the first time their product is `<= 0` (a genuine sign change or exact zero), linearly interpolate between the two `(t,x)` points to estimate the precise crossing time, and write that single float (4 decimal places) to `/home/user/root.txt`. Compile with `gcc simulate.c -o simulate -lm`, run it, and spot-check a few early rows of the CSV by hand against the ODE to catch step-count or formatting mistakes before finishing.
