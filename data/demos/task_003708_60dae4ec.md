Start by running `ffprobe -show_frames -select_streams v:0 -of json /app/video.mp4` to dump each frame's `coded_picture_number`, `pict_type`, and `pkt_size` — persist this as your graph's node data (e.g. embed it in the C++ server or load it from a generated file at startup), sorted by `coded_picture_number`. Build dependency edges per the stated rules: I-frames have no deps; a P-frame's single dep is the nearest preceding I/P frame; a B-frame has two deps — nearest preceding I/P and nearest following I/P — found by simple backward/forward scans over the sorted frame list. For the query engine, implement `dependency_size(n)` as a DFS/BFS from node n following dep edges, accumulating a *set* of visited frame indices (to avoid double-counting shared ancestors when a node is reachable via multiple paths), then sum `pkt_size` over that visited set including n itself. Expose this via a minimal HTTP server (raw sockets or a header-only lib) parsing the `frame` query parameter from `GET /dependency_size?frame=<N>` and returning the integer as a plain-text 200 body. Compile with C++17, launch it backgrounded on `127.0.0.1:8080`, and sanity check a couple of frames with `curl` before finishing — the recursion must dedupe visited nodes or answers will be inflated.