Before writing any of your own logic, get the vendored library importable: set `PYTHONPATH` to include `/app/networkx-3.1/`, try importing it, and when it errors, open `networkx/algorithms/dag.py` and look near the `lexicographical_topological_sort` function for an inserted statement that unconditionally raises an exception — a stray sabotage line unrelated to real logic — and remove/comment it out rather than patching around it in your own code. For the SQL, notice the "starter query" bug is a classic implicit cross join (selecting from two tables without a proper join condition, e.g. comma-joining `foreign_keys, tables` without matching both parent and child ids), which produces a cartesian product of duplicated/wrong rows; replace it with two explicit `JOIN`s — one mapping `child_table_id` to its `table_name`, one mapping `parent_table_id` to its `table_name` — so each edge row is exactly (parent_name, child_name). Build a `networkx.DiGraph`, add all table nodes (including isolated ones) plus the parent→child edges, then print `",".join(nx.lexicographical_topological_sort(G))`. Test against several hand-built SQLite fixtures covering ties, isolated tables, and multi-level chains before trusting it against the fuzzer.
