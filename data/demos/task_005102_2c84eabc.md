Locate the log-likelihood accumulation code in `/app/bio_mcmc-1.2.3/bio_mcmc/likelihood.py`. Look specifically for a `ThreadPoolExecutor` whose results are combined via `concurrent.futures.as_completed` — that pattern accumulates floats in whatever order threads happen to finish, and because floating-point addition is not associative, summing many small negative log-likelihood terms in a nondeterministic order produces run-to-run drift that compounds through MCMC acceptance decisions. Fix it by forcing a deterministic, fixed accumulation order: either collect `future.result()` in the original submission order and sum sequentially, or use `executor.map(...)` (which preserves input order) feeding into `math.fsum` for extra numerical stability. After editing, reinstall the package in-place with `pip install -e /app/bio_mcmc-1.2.3` so your fix is actually picked up (don't forget this step — otherwise the old installed copy still runs). Then write `/home/user/run_estimation.py` exactly per the spec: parse the FASTA sequences, build `bio_mcmc.Sampler(sequences, seed=42)`, run `sampler.run(10000)`, drop the first 2000 samples as burn-in, and write only the posterior mean float to `/home/user/posterior_mean.txt`. Re-run the script twice to confirm you now get bit-identical results, which is your own signal the fix is correct before relying on the external accuracy check.
