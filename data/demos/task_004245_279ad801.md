Don't guess the algorithms — determine them empirically by feeding `/app/dataset_analyzer` small, hand-constructed graphs via stdin (per the documented input format) and comparing outputs against metrics you compute by hand, isolating one query type at a time. For `CENTRALITY u`, try graphs where you vary only in-edges or only out-edges of `u` to see if the output tracks in-degree, out-degree, or a product/sum of both. For `CO_CITE u v`, test with vertices that share zero, some, and all out-neighbors to see whether it's measuring outgoing-edge overlap (common targets) versus shared incoming edges. For `PATTERN u`, construct small directed triangle configurations (`v->u`, `u->w`, `v->w` present or absent in various combinations) to isolate exactly which triangle orientation is being counted. Once confirmed, implement `solution.cpp` with adjacency lists (`vector<vector<int>>` for out-edges, and a separate in-edges list or reverse adjacency for in-degree/co-citation) plus `unordered_set` or boolean membership arrays for O(1) edge existence checks, since brute-force O(V^3) triangle counting will time out on graphs with up to 1000 vertices and 10000 edges. Compile with the exact specified command and test against fresh random graphs, not just your probing examples, before finalizing.