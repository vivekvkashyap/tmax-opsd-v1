Transcribe `/app/incident_report.wav` (e.g. with a local speech-to-text tool such as `whisper`) to recover the precise definition of an "evil" query in the incident report — listen for exactly which Cypher pattern is called out as causing the implicit cross join. Build `query_filter.py` to read the query file, normalize whitespace/case, and use a regex/parser that flags a `MATCH` clause containing two node patterns separated by a comma with no relationship between them (an implicit Cartesian product), while explicitly NOT flagging queries that use multiple separate `MATCH` clauses or that link nodes with an explicit relationship (`-[...]->`) — validate both directions against representative examples before finalizing, since the hidden test requires 100% accuracy on both evil and clean corpora. Exit 1 for evil, 0 for clean. For centrality.sql, compute each job's degree as the count of edges where it appears as source plus the count where it appears as target (e.g. `UNION ALL` of source_job/target_job grouped and counted, or correlated subqueries), then apply `RANK() OVER (ORDER BY centrality DESC)` as `centrality_rank`, and order the final SELECT by `centrality_rank`, then `job_id`.
