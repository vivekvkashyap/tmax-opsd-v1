Two independent, straightforward pieces — solve them separately and verify each in isolation. Part 1: run `soxi -D /app/fan_profile.wav` (or `sox --i -D`) to get the exact duration in seconds and write that raw value to `/home/user/audio_duration.txt` — don't round or reformat it beyond what `soxi -D` already produces, since the verifier compares against that same tool's output. Part 2: write `/home/user/tv_distance.sh` to parse `$1` and `$2` as comma-separated integer lists (use `IFS=','` with `read -ra` or `awk`'s `split`), sum each list, and if either sum is 0 print `Invalid input` to stdout and `exit 1`. Otherwise normalize both lists into probability vectors by dividing by their respective sums, compute TV distance as half the sum of absolute per-index differences, and print the result formatted to exactly 4 decimal places via `awk`'s `printf "%.4f\n"` (do the floating-point math in `awk` rather than raw bash arithmetic, since bash can't do non-integer division). Make the script executable with `chmod +x`. Test it against the two worked examples in the task description, plus a couple of hand-picked edge cases (equal lists, a zero-sum list, lists of length 2 and 10) before finishing.