First transcribe `/app/voicemail.wav` — try `pocketsphinx`, or install `ffmpeg` plus Python's `SpeechRecognition` (which can call Google's free web API or a local recognizer) to get the spoken instructions; if automated transcription is imperfect, listen for the general structure (a join condition, a projection, a window-function aggregate, and an exact output key/ordering spec) and cross-check by testing your script's output shape against what an SQL query with that logic would naturally produce. Build `/home/user/fixed_analyze.sh` as: load the input CSV and `/app/regions.csv` into `sqlite3 :memory:` via `.mode csv` / `.import`, fix the cross-join by adding a proper `JOIN ... ON` condition, add a window function (`SUM(...) OVER (PARTITION BY ... ORDER BY ...)`) per the transcribed instructions, then switch to `.mode json` and `SELECT` only the exact column names/order specified, with a final `ORDER BY` matching the transcript exactly (source then time, or whatever was said) — because the verifier does bit-for-bit stdout diffing against a hidden oracle across 100 fuzzed CSVs, so even one extra whitespace, wrong sort key, or wrong output key name will fail every case. Make the script executable (`chmod +x`) and test it against several manually crafted CSVs before trusting it.
