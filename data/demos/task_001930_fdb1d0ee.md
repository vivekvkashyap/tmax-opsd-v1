Diagnose and fix the SQL bug before writing any graph logic. Inspect the query in get_links.sh: it selects from `servers s1, servers s2, network_links nl` with only `s1.id = nl.source_id` in the WHERE clause, which is a classic implicit cross join — it must also join `s2.id = nl.dest_id` so each link maps to exactly one source/dest hostname pair rather than being multiplied by every server row. Fix it to output `source_hostname|destination_hostname|latency` with the correct row count (one row per network_links entry), and sanity-check by counting output rows against `SELECT count(*) FROM network_links`. Separately, query backup_meta.db (join backups→servers) to find which server currently hosts the `main_prod` backup — don't assume a hostname, derive it from the data. For calc_shortest_path.sh, read `source|dest|latency` triples from stdin, build an adjacency structure (associative arrays keyed by hostname work well in bash), and implement Dijkstra or BFS-with-relaxation entirely with bash/awk — track a `previous` map to reconstruct the path once the target is settled, not just the total latency. Write the result to restore_plan.txt in the exact literal format `Path: <source> -> ... -> db-target-01 | Total Latency: <n>`, matching spacing and arrow syntax precisely since the check is an exact string match. Make both scripts executable.