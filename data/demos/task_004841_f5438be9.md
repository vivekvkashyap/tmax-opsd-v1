First diagnose the ABI mismatch: run `ldd /app/legacy_proc/bin/data_engine` and `LD_LIBRARY_PATH=/app/legacy_proc/lib/v1 ldd ...` vs `.../v2 ldd ...` (and/or `strace -f` on the run attempt) to see which version resolves symbols/loads without crashing — don't assume, verify empirically which of v1/v2 satisfies the binary. For the memo, OCR `/app/legacy_proc/docs/handwritten_memo.png` with `tesseract` and read the extracted text carefully to find the exact env var name and hex value the author specifies — don't guess or hardcode a plausible-looking one. In `start_service.sh`, export `LD_LIBRARY_PATH` to the correct lib dir, export the OCR'd env var, then wrap the binary with `socat TCP-LISTEN:9099,fork,reuseaddr,bind=0.0.0.0 EXEC:/app/legacy_proc/bin/data_engine` so each connection spawns a fresh process handling newline-terminated input/output. For `benchmark.sh`, time 100 sequential (or looped) requests against `127.0.0.1:9099` with `time`/`date +%s%N` deltas and log the total elapsed time to `bench_results.log`.
