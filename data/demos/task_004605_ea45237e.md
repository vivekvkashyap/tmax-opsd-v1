Read the file line by line, wrapping each `json.loads` in try/except. On `JSONDecodeError`, don't give up — sanitize the raw line first with a regex that finds `\u` escapes not followed by exactly 4 valid hex digits and either strips the backslash or replaces the whole malformed sequence with a placeholder, then retry parsing; only skip the line if it still fails. For normalization, apply the three rules in one careful pass rather than three sequential blind re.sub calls: strip whitespace, then substitute runs of letters with `VAR` and runs of digits (with an optional single `.`) with `NUM` — do this via one combined regex with alternation (e.g. `[a-zA-Z]+|\d+\.?\d*`) so you don't accidentally re-scan already-substituted `VAR`/`NUM` tokens for stray letters/digits. Aggregate with a `Counter`, then sort by `(-count, template)` so ties break alphabetically ascending. Write only the top 100 rows to `/home/user/top_templates.csv` with header `template,count`, using the `csv` module to get correct quoting/escaping for symbols like commas inside templates.
