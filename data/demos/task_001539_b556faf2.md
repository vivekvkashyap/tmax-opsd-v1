Extract the square's x-position per frame from `/app/oscillator.mp4` using OpenCV (or ffmpeg + a simple thresholding script): for each frame, threshold to find the bright/white region and take its centroid or bounding-box center x-coordinate, then convert to displacement by subtracting the known equilibrium x=320, giving you a displacement-vs-time series at 1/30s intervals — don't hardcode displacement, actually measure it per frame. Fit that series to the damped-oscillator model x(t) = e^(-ζω_n t)(C1 cos(ω_d t) + C2 sin(ω_d t)) using a nonlinear least-squares fit (e.g. `scipy.optimize.curve_fit`) to recover ζ and ω_n — plot or numerically sanity-check the fit against the raw data before trusting the parameters. Write `/home/user/ode_solver.c` implementing exactly this closed-form analytical solution (not a numerical integrator, which risks drift) parameterized by the fitted ζ/ω_n but taking x0, v0, t as CLI args, derive C1/C2 from x0/v0 analytically, compile with `gcc`, and confirm `./ode_solver <x0> <v0> <t>` prints a single float. Then build the Flask/http.server-based API on 0.0.0.0:8080 that checks the exact Authorization header, parses the JSON body, shells out to your compiled solver (or reimplements the same formula in Python) with the fitted constants, and returns `{"x": ...}`; run it persistently in the background and test with `curl` before finishing.