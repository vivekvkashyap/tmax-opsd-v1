First recover the secret multiplier from the audio: transcribe `/app/voicemail.wav` (with a tool like `whisper` or by careful listening) and extract the spoken number — this value is required by query.sh but must be discovered, not assumed. Fix process.c so `encode_string` truncates any input over 10 characters before use (e.g. copy at most 10 chars plus a null terminator into a fixed or heap buffer sized to fit, rather than trusting the caller's length) and audit/fix any allocation so the returned buffer is properly sized and freed by the caller contract; compile with `gcc -shared -fPIC -o libprocess.so process.c`. For server.go, the race is almost certainly a shared package-level `C.CString`/buffer reused across goroutines handling `/compute` concurrently — fix it by allocating a fresh CString (and freeing it with `C.free`) inside each request handler rather than at package scope, so concurrent requests don't stomp each other's memory; test this by hammering the endpoint with concurrent curl requests (e.g. `xargs -P` or a small load script) and checking outputs are internally consistent before trusting it. Since your final grading is fuzz-equivalence against a hidden oracle over random alphanumeric strings, write query.sh to just build the URL with the input and the multiplier you extracted, curl it, and extract the `result` field from the JSON with a tool like `jq -r .result` — test query.sh against many random strings yourself and sanity check the encoding logic matches your understanding of process.c's truncate+multiply behavior end-to-end.
