Work through the three sub-problems independently, then wire them together. First, fix the shared library build: compile `checksum.cpp` with `-fPIC -shared` into `libchecksum.so`, and since `main.cpp` uses `dlopen`/`dlsym` to find `calculate_checksum` by its plain C name, wrap the function declaration in `checksum.h` (or its definition) with `extern "C"` so the compiler doesn't mangle the symbol name — this is the classic C++/dlsym ABI pitfall. Also link `main.cpp` with `-ldl` for `dlopen`/`dlsym`/`dlclose`. Second, in the `/diff` handler, remember `std::set_difference` requires both input ranges to already be sorted — sort `list_a` and `list_b` (e.g. with `std::sort`) before calling it, otherwise the diff will be wrong or incomplete. Third, iterate: run `make`, fix compiler/linker errors as they appear, then start `./safehashed &`, confirm it's listening on port 8080 (e.g. with a quick curl or checking the process), and only then issue the real curl request, redirecting stdout to `/home/user/api_test.log`. Verify the log's contents look like a sorted, comma-separated JSON array before finishing.