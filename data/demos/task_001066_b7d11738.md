Implement this as a strict, deterministic pipeline where the grader recomputes the same steps independently, so match the spec's exact algorithm rather than a "reasonable" variant. Load chemical_data.csv, drop is_toxic, compute the absolute Pearson correlation matrix, then do a single left-to-right pass over columns: always keep the first column; for each subsequent column, check its max absolute correlation against only the columns already kept so far (not all columns) — drop it if that max exceeds 0.85, otherwise keep it and add it to the kept set (this greedy "keep vs already-kept" order matters, don't compare against all original columns). Standardize the kept features with StandardScaler, fit PCA with exactly `n_components=5` and no random_state. For each of the 5 components, split by is_toxic label and run `scipy.stats.ttest_ind(group1, group0, equal_var=False)`, keeping components with p < 0.05, sorted ascending. Train `LogisticRegression(random_state=42)` on only those significant components against the full dataset, score training accuracy, round to 4 decimals, and write dropped_features (in evaluation order), significant_components, and model_accuracy to results.json.
