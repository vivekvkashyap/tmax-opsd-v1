Work in order: patch first, then test, then run. Apply the patch with `patch /home/user/migrate.py < /home/user/fix.patch` (or `patch -p1`/`-p0` depending on the header format — inspect the patch's `---`/`+++` lines to pick the right strip level, and verify afterward by reading the file rather than assuming it applied). Then write `test_migration.py`: import `hypothesis`'s `given`, `strategies as st`, `settings`; build a strategy for a v1 record using `st.fixed_dictionaries` with `"id": st.integers()` and `"data": st.lists(st.floats(min_value=-100.0, max_value=100.0, allow_nan=False, allow_infinity=False), min_size=1, max_size=100)` — excluding NaN/inf is essential or the mean comparison will fail unpredictably. Decorate `test_migration_invariants(v1)` with `@given(...)`, call `migrate_v1_to_v2(v1)`, then assert `record_id == id` exactly and use `math.isclose(v2["mean"], statistics.mean(v1["data"]), rel_tol=1e-5, abs_tol=1e-8)` for the mean — never use `==` on floats. Finally run `pytest /home/user/test_migration.py > /home/user/test_results.log 2>&1` (capture both streams) and confirm the log shows a pass before leaving it in place.
