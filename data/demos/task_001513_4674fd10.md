Fix the vendored library before building anything on top of it. In `/app/vendored/libjsonparse-1.2/src/decoder.cpp`, locate the code path that throws when it sees a `\u` unicode escape and remove/replace that throw so parsing continues instead of aborting (either properly decode the escape or safely skip it without crashing) — verify by writing a tiny test program that parses a string containing `é` and confirms no exception. In the `Makefile`, check the compilation rule for the `$(CXX)` step for indentation issues — Make requires literal tab characters before recipe commands, and a line using spaces instead will fail with "missing separator"; retype that line with a tab. Rebuild to produce `libjsonparse.a`. For the HTTP server, use a lightweight single-header library (e.g. cpp-httplib) and implement `/ingest` (parse each JSON-line with your fixed library, maintain a rolling window of the last 10 `value` fields), `/rolling_stats` (return `{"rolling_avg": <avg>}` as JSON), and `/export` (bulk-insert accumulated records into a `events` table in `/home/user/analytics.db` via SQLite's C API using a single transaction/prepared statement for efficiency, not one INSERT per row). Start the server in the background and test each endpoint with `curl` before finishing, paying attention to the UTF-8/unicode-escaped event names surviving the round trip into SQLite.