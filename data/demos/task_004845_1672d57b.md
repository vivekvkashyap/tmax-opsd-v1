Use `git bisect start`, mark `HEAD` bad and `v1.0` good, then drive it with `git bisect run <script>` instead of manually stepping through ~200 commits. Write the test script to: (1) attempt `go build` (or `go run`) — if compilation fails, `exit 125` so git bisect skips that commit entirely rather than treating it as good/bad, which is essential since some commits in the range are known to be build-broken and would otherwise poison the bisection; (2) if the build succeeds, run the resulting binary, parse its printed value, and compare it against the known-good precision from `v1.0`'s output — exit 1 (bad) if precision has visibly degraded (e.g. differs at an early decimal place, not just noise), exit 0 (good) otherwise. Let git bisect converge automatically, then read the reported first-bad commit SHA with `git rev-parse` and write the full 40-character SHA (no extra whitespace) to `/home/user/bad_commit.txt`. Do not alter history or attempt a fix — only identify and record the SHA.
