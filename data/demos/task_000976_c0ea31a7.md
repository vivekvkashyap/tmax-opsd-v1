Write a C++17 program using only `<vector>`, `<fstream>`, `<sstream>`, `<cmath>` etc.: for each CSV, parse lines by splitting on commas into doubles, building a Nx3 matrix. Compute the sample mean per column, then the 3x3 sample covariance with the standard formula Σ[i][j] = sum((x_i - mean_i)*(x_j - mean_j)) / (N-1) — remember covariance is symmetric, so only compute the upper triangle and mirror it. Add λ=0.1 to the diagonal only (Σ' = Σ + 0.1·I). For the 3x3 determinant, use the direct cofactor-expansion formula (a(ei−fh) − b(di−fg) + c(dh−eg)) rather than a generic LU routine — it's exact and simple for this fixed size. Round to 4 decimals using proper half-up rounding (e.g. `std::round(det * 10000.0) / 10000.0`, being careful this matches Python's rounding behavior for the values you'll get — verify against a couple of hand-computed cases). Finally compare the three rounded determinants to find the minimum and write the exact JSON shape requested (key order and quoting matter) with a small helper — either hand-roll the JSON text or use simple string formatting, no need for a JSON library.