Build this as three independent, single-purpose bash scripts and wire them together rather than one monolith. `monitor.sh`: an infinite `while true; do ... sleep 1; done` loop that runs `du -sb /home/user/workloads/db /home/user/workloads/app`, parses each line's size and derives just the basename (`db`/`app`) from the path (e.g. with `basename`), then appends `$(date +%s),name,size` to the CSV — get the field order and exact comma-separated format right since the verifier greps it directly. `supervisor.sh`: check liveness with `pgrep -f monitor.sh` (not just checking a PID file, since the daemon isn't tracked by PID here); if not found, launch `nohup /home/user/scripts/monitor.sh >/dev/null 2>&1 &` and append the exact `[<epoch>] Restarted monitor daemon` line to the log. Install the cron line with `(crontab -l 2>/dev/null; echo "* * * * * /home/user/scripts/supervisor.sh") | crontab -`. After running supervisor once, use plain `sleep 3` around the file-creation event, then in `analyze.sh` compute max-minus-min size per directory from the CSV with `awk` (group by column 2, track min/max) and write `db_growth_bytes:`/`app_growth_bytes:` lines exactly as specified.
