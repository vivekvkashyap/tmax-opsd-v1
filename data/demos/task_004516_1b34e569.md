Treat the git history as the primary evidence source, not just the working tree. Run `git log -p -- build.py` (or `git log --all -p`) in `/home/user/project` to find the commit where the CI token was still hardcoded before it was switched to `os.environ.get("CI_TOKEN", ...)` — the old literal value in that earlier diff is the token you need to recover; don't guess it. Separately, diff the commits around the directory rename (`git log --stat` / `git show` on the commit that reorganized tests) to see the old vs. new test directory name, then compare that to what `build.py` currently references — the bug is a stale path string left over from before the rename, so update it to match the actual current directory on disk (check with `ls`). Once both fixes are identified, export the recovered token as the `CI_TOKEN` environment variable in the same shell invocation (`CI_TOKEN=<recovered-token> python3 build.py`) after fixing the path in build.py, and confirm `/home/user/success.txt` gets created with the expected marker content.
