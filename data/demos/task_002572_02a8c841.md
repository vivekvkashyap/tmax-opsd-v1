Start by locating and reading the exact bottleneck: `build_edge_list` in the vendored package does a nested-loop join comparing every source to every target (O(N^2)). Replace it with a hash-based join — build a dictionary keyed by `node_id` from `targets` once, then do a single pass over `sources` doing O(1) dictionary lookups to emit edges; this is the standard technique for turning a nested-loop join into a hash join and is what will deliver the required speedup. For `analyze.py`, make sure the vendored package is importable (add its directory to `sys.path` or set `PYTHONPATH` before importing), ingest the JSONL, filter by year >= 2010, then build undirected co-authorship edges: for each paper's author list, generate all unique author pairs and increment an edge-weight counter per pair (careful to treat (A,B) and (B,A) as the same edge). Degree centrality per author is the sum of weights of all edges touching that author — compute it directly from your edge-weight map rather than re-deriving it from the graph library if that's simpler and correct. Write the result as a flat JSON object of author->integer centrality to the exact output path, and time your script against the original unmodified package on a synthetic large dataset yourself to sanity-check the speedup before submitting.