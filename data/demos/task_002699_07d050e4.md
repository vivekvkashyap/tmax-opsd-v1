Model this as graph search over the `citations` edge list where an edge `(citer_id, cited_id)` means citer -> cited. For the shortest path from 100 to 1, run BFS from node 100 following `citer_id -> cited_id` edges, tracking parent pointers so you can reconstruct the path once you reach 1, then join the path with commas. For "all papers that transitively cite paper 1", do a reverse BFS/DFS starting at 1 but walking edges backwards (i.e., from `cited_id=1` find all `citer_id`s, then recurse on those as the new `cited_id` targets) — equivalently, build a reverse adjacency map first. You can implement either approach in plain Python with `sqlite3.connect(...).execute(...)` to pull all rows into memory and build adjacency dicts, or push the traversal into SQL via `WITH RECURSIVE`; the recursive-SQL approach is less error-prone for cycle safety but make sure to dedupe visited nodes either way to avoid infinite loops if the citation graph has cycles. Sort the influence-analysis result numerically (cast to int, not lexicographic string sort) before writing one ID per line, and write the shortest path only once fully resolved.
