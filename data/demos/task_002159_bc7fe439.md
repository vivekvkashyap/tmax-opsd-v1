Locate the numerical integrator in `/app/spectra_fitter-1.0/src/` (an adaptive step-size loop) and find where it responds to "error exceeds tolerance" by scaling `dt` up instead of down — flip that multiplicative factor to a divide/halve so the integrator converges instead of diverging. Separately check `CMakeLists.txt` for the shared library target and add the missing math-library link (`target_link_libraries(spectra_fitter m)` or `-lm`) so linking against `sin`/`cos`/etc. succeeds. Build with CMake out-of-tree, setting `-DCMAKE_INSTALL_PREFIX=/app/installed`, then `make install`. Write `/home/user/service.cpp` as a minimal POSIX TCP server: `socket()`/`bind()`/`listen()` on 127.0.0.1:8181, loop `accept()`-ing connections, read until `\n`, split the line on commas into a `std::vector<double>` (use `std::stod` per token), call `SpectraFitter::fit_peak(data)` from the installed header, format the result with `std::fixed << std::setprecision(4)` prefixed by `"RESULT: "` and suffixed with `\n`, write it back, then close that connection and continue accepting. Compile linking against `-I/app/installed/include -L/app/installed/lib -lspectra_fitter` (set `LD_LIBRARY_PATH` or use `-Wl,-rpath` so the shared lib is found at runtime), then launch it backgrounded and confirm with a quick manual `nc`/socket test that it's listening and responds correctly before considering it done.
