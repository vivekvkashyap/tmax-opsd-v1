Start by transcribing /app/compliance_memo.wav (whisper or similar) to extract the precise two rules — a hierarchical/graph constraint and a numeric threshold on a rolling aggregate — rather than assuming generic validation logic. Then load nodes.csv and edges.csv into SQLite (or an in-memory structure) per batch. For the hierarchy rule, use a recursive CTE (`WITH RECURSIVE`) walking parent_id -> child_id and detect if any node revisits an ancestor already in its own path (a true cycle check, not just duplicate edges). For the numeric rule, use a window function `SUM(cost) OVER (PARTITION BY department ORDER BY timestamp ROWS BETWEEN 2 PRECEDING AND CURRENT ROW)` and check the threshold from the memo. Build /home/user/validate_batch as an executable (script with shebang + chmod +x, or a wrapper) taking one directory argument, printing exactly ACCEPT or REJECT with exit 0. Validate against /app/corpus/clean and /app/corpus/evil yourself before submitting, iterating until every clean batch passes and every evil batch is rejected — the hidden test set follows the same two rules.