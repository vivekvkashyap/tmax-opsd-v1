Scaffold with `cargo new latency_monitor` in `/home/user/latency_monitor`, then write `main.rs` to open a `TcpStream::connect_timeout` (2s timeout) against each of `8.8.8.8:53` and `1.1.1.1:53`, timing the connection attempt with `std::time::Instant`, and appending one JSON line per endpoint to `/home/user/latency.log` matching the exact key set `endpoint`, `latency_ms` (integer), `success` (bool) — handle the timeout/connection-refused case by still logging `success: false` rather than crashing, and exit 0 regardless of outcome. Build with `cargo build --release` and confirm the binary at `target/release/latency_monitor` runs standalone and appends valid JSON lines you can `jq` parse. For scheduling, edit the user crontab with `crontab -e` (or `crontab -l | { cat; echo "..."; } | crontab -`) adding a `* * * * *` line pointing at the absolute release-binary path — don't schedule via `cargo run`, which would rebuild each run. For log rotation, write `/home/user/logrotate.conf` targeting the exact log path with `size 10k`, `rotate 5`, `compress`, `missingok`, and `notifempty` directives; do not invoke `logrotate` or the cron daemon yourself, just leave the config and crontab installed.