Load the CSV into SQLite first (`sqlite3 sensors.db` with `.mode csv` and `.import`, or an explicit `CREATE TABLE` + `.import --skip 1`) and manually inspect a few rows to confirm columns/types imported correctly, especially that timestamp comparisons will work lexicographically (ISO-8601 strings sort correctly as text, which is convenient here). In the bash script, accept the sensor_id as `$1` and build a query that finds the max timestamp for that sensor, then among ties on that timestamp picks the row with the highest temperature — this is naturally expressed as `ORDER BY timestamp DESC, temperature DESC LIMIT 1` rather than a nested MAX() subquery, which avoids double-counting ties incorrectly. Pass the sensor_id into sqlite3 safely using a parameter binding mechanism (e.g. `sqlite3 db ".parameter set :sid '$1'"` style, or careful quoting since it's a CLI value not raw SQL) to avoid injection via string concatenation. Use `sqlite3 -json` or manually format the query result into the exact nested JSON schema requested (numbers unquoted, not strings) — test by running your script against the known sample data and diffing against the expected shape before finishing, and remember to `chmod +x` it.