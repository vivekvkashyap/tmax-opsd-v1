Start the dependency server first (`python3 ws_server.py &`) and inspect what it actually sends before writing builder.py — connect with a quick throwaway `websockets` client and print the received JSON so you know the real key/value shape rather than assuming. Implement a standard Kahn's-algorithm or DFS-based topological sort over that graph (don't hardcode an order, since the grader may vary the graph). Compile each `.c` file separately with `gcc -shared -fPIC -o modX.so src/modX.c` — critically, do NOT pass `-l` flags linking them to each other, since the whole point is that they only resolve symbols at load time. In run_test.py, load the `.so` files with `ctypes.CDLL(path, mode=ctypes.RTLD_GLOBAL)` in exactly the topological order computed by builder.py (dependencies before dependents) — loading out of order or without `RTLD_GLOBAL` will produce an undefined-symbol error at dlopen time, which is the exact CI bug you're asked to fix. Then call `getD()` via ctypes (set `.restype = ctypes.c_int` if needed) and write the returned integer to result.txt. Verify by tracing each function's dependency chain manually to sanity-check the final number before trusting the program's output.