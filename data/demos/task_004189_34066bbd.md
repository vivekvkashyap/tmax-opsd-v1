Fix things bottom-up: build, then memory-safety, then automation. For the Makefile, remember `make` requires recipe lines to start with a literal TAB, not spaces — if `make` reports "missing separator," that's almost certainly the cause; also add `-g` to the compile flags for debug symbols. For the C fix, trace every `malloc` to its corresponding `free` on every code path (including early returns) — a common bug is allocating a line buffer and returning before freeing it; add the missing `free()` calls so `valgrind --leak-check=full` reports zero leaks. Verify by running valgrind directly against a sample stdin before wiring it into the pipeline script. For `ci_pipeline.sh`: chain `make -C ... clean all`, then the valgrind check with `--error-exitcode=1` so a leak aborts the script immediately (`set -e` or explicit `$?` checks help), then loop over `*.txt` in the artifacts directory, run the filter with max size 1000 feeding each file's contents via stdin, capture PASS/FAIL, sleep 0.5s after each invocation (rate limiting), and finally sort matching basenames alphabetically into the approved log. Make the script executable and run it to produce real output, then eyeball the log against the artifacts' actual size values.
