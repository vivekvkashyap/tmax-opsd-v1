Build `/home/user/validate_pipeline.sh` as a straightforward sequential Bash script, testing each piece interactively first. For step 1, run `fft.py < signal.dat` to get the baseline frequency, then compare it against the number in `legacy_fft.out` using `awk` or `bc` for the floating-point absolute-difference check against the 0.01 threshold, recording PASS/FAIL based on that comparison. For step 2, loop 100 times; each iteration use `awk '{print $1, $2 + (rand()*1 - 0.5)}' signal.dat` (or similar, mind that awk's `rand()` needs seeding per iteration — e.g. `awk -v seed=$RANDOM 'BEGIN{srand(seed)} ...'` since a fixed default seed would give identical "noise" every loop) to perturb only column 2 within [-0.5, 0.5], pipe the noisy data through `fft.py`, and collect the printed frequency into an array/file; after 100 runs, `sort -n` the results and pick the 5th and 95th lines (1-indexed) as the CI bounds. For step 3, actually run `analytical_model.py` at `1.0` and `1.00000001` and compute the absolute difference of the two printed values yourself — don't assume which way the PASS/FAIL comparison against the 1.0 threshold falls, just implement the comparison faithfully and let the real output decide it. Assemble the three results into `/home/user/report.txt` matching the exact three-line format given, then `chmod +x` and run the script to produce the real file.