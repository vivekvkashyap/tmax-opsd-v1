Handle the encoding conversion explicitly: read raw_sensors.csv as bytes (or with Go's `golang.org/x/text/encoding/charmap.ISO8859_1` reader) and decode each `sensor_name` field from ISO-8859-1 to UTF-8 — reading the file as plain UTF-8 will corrupt non-ASCII bytes silently. Parse timestamp, sensor_name, temperature per row, skip the header, and drop rows where temperature < -50.0 or > 150.0 (strict inequalities, so boundary values like exactly -50.0 or 150.0 are kept). For hourly sampling, truncate each timestamp to its hour (e.g. parse with `time.Parse` and zero out minutes/seconds) and keep only the first valid row seen per distinct hour, preserving file order — process rows top-to-bottom and skip once an hour key is already recorded. Append (not overwrite) the surviving rows in UTF-8 to processed_sensors.csv, then append the exact `SUCCESS: Extracted <N> samples.` log line with the true count written. After confirming the script runs correctly and produces the two files, install the cron entry with `crontab -l | { cat; echo "*/15 * * * * /usr/local/go/bin/go run /home/user/etl.go"; } | crontab -` for the `user` account, then verify with `crontab -l`.