Two independent fixes, both must be exact. First, edit `/app/vendor/github.com/acmecorp/logsync/sync.go` in place so `AdjustTimestamp` implements `return raw + int64(float64(raw)*drift)` (the current bug applies drift as a flat millisecond offset instead of scaling by the raw value). Second, in `/home/user/`, run `go mod init` for your reconstructor and add a `replace github.com/acmecorp/logsync => /app/vendor/github.com/acmecorp/logsync` directive in `go.mod` so your code imports the fixed local package rather than fetching one. In `log_reconstructor.go`: read stdin line by line, split strictly on `|` and require exactly 4 fields (drop malformed lines silently), parse `RAW_TIMESTAMP`/`DRIFT_FACTOR` and drop lines where timestamp `<= 0` or drift is outside `[-0.5, 0.5]` — do the bounds check with `<` / `>` (strict) as specified, not `<=`/`>=`. Call the fixed `AdjustTimestamp` for valid lines, then sort with `sort.SliceStable` using a comparator that orders primarily by corrected timestamp, then alphabetically by `SERVICE_ID`, relying on stability to preserve original order for full ties. Print each as `[CORRECTED_TIMESTAMP] SERVICE_ID: MESSAGE` and compile with `go build -o /home/user/log_reconstructor`.
