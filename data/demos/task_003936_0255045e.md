Fix the build first, isolated from everything else: `cd /app/net-sniff-processor-v1.2.0 && make build` and read the actual error — the Makefile passes a `go build` flag (a `-mod=` variant) that requires either vendoring or a `go.sum` neither of which exist in this minimal module, so the fix is to remove/correct that flag in the Makefile (e.g. drop `-mod=readonly`/`-mod=vendor` or run `go mod tidy`/`go mod init` first) so `go build -o net-sniff main.go` succeeds and produces the `net-sniff` binary — confirm with `./net-sniff` before moving on. Then write `/home/user/redactor.go` as a strict pattern-based sanitizer over `os.Args[1:]`: for each argument, regex-replace any substring matching `AKIA[A-Za-z0-9]{16}` and any substring matching `PASSWORD=[^ ]+` (careful: the pattern can appear mid-argument, not just as a whole arg) with `[REDACTED]`, marshal the resulting slice of strings to a JSON array, then exec the program named by the `TARGET_SCRIPT` env var and write that JSON to its stdin (don't pass secrets as argv to the child — that's the exact vulnerability being fixed). Validate against both corpora manually: `TARGET_SCRIPT=/bin/cat /home/user/redactor $(cat /app/tests/corpus/evil/1.txt)` etc., checking clean files pass through untouched as JSON and evil files have every secret substring replaced, before considering it done. Clean up any leftover background processes when finished.
