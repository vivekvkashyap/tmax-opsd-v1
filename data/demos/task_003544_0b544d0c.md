First recover the secret project name by listening to `/app/voicemail.wav` (play it or run it through a speech-to-text tool such as `whisper` or any available STT) — the name is a single capitalized word spoken in the recording; do not guess it, transcribe it. Then write redactor.py to process stdin line-by-line (iterate `for line in sys.stdin:` so it streams rather than buffering everything, and write with `sys.stdout.write` immediately per line so it terminates cleanly on EOF). Apply the three transforms in order on each line's content (excluding its trailing newline, which you should preserve/reattach exactly rather than let `print` add a second one): (1) regex-replace exactly-16-digit runs bounded by non-digit boundaries (use negative lookaround like `(?<!\d)\d{16}(?!\d)` so 15/17-digit sequences are left untouched) with `[REDACTED_CC]`; (2) exact case-sensitive substring replace of the recovered project name with `[REDACTED_PROJECT]`; (3) if the exact literal `' OR 1=1 --` appears anywhere in the line, append ` [CWE-89_DETECTED]` right before the newline. Since correctness is checked by bit-exact fuzz comparison against a hidden oracle, test with many synthetic lines mixing these patterns and edge cases (empty lines, lines without trailing newline, adjacent 15/16/17-digit runs) to catch off-by-one boundary bugs in your CC regex.
