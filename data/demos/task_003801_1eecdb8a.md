Write this as a pure bash script — the instructions explicitly forbid a Python helper for the dataset-length step, so lean on `h5dump` output parsing. First ensure `h5dump` is installed (`apt-get install -y hdf5-tools` if missing, using sudo). To get the length of `/spectroscopy/signal`, run `h5dump -d /spectroscopy/signal <file>` and parse the `DATASPACE` line's dimension value with `grep`/`awk`/`sed` (e.g. grep for the `SIMPLE { ( N )` pattern and extract N) rather than hardcoding it, since it must be derived programmatically. Write the CSV header first with `echo`. Then loop `for level in 2 4 8 16`: invoke `/usr/bin/time -f "%e" -o /tmp/time.txt python3 /home/user/analyze_mesh.py /home/user/data/spectra.h5 $level` (redirecting/capturing stdout separately for the density peak value, since `/usr/bin/time`'s timing output and the program's stdout must not be conflated), read the elapsed seconds from the time output, compute `rate = length / time` with `awk` or `bc -l` rounded to 2 decimals, and append a CSV row. Make the script executable with `chmod +x`, run it once, and verify the CSV has exactly 5 lines with the length constant across all rows.
