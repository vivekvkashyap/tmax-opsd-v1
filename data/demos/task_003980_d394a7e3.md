Install `mpi4py`, `scipy`, and `matplotlib` first, and confirm `mpirun -n 4 python3 -c "from mpi4py import MPI; print(MPI.COMM_WORLD.Get_rank())"` works before writing analysis code. In `/home/user/analyze.py`, loop seeds 1-50, and for each seed use `subprocess.run(["mpirun","-n","1","python3","/home/user/reduce_sim.py",str(seed)], capture_output=True)` and the same with `-n 4`, parsing the printed float from stdout (only rank 0 prints, so no special handling needed beyond reading stdout). Collect the 50 one-process results and 50 four-process results into two arrays, compute `scipy.stats.wasserstein_distance(arr1, arr4)` and write just that number to `/home/user/metric.txt`, and plot overlaid histograms/KDEs of both arrays to `/home/user/visualization.png`. For `/home/user/fix_sim.py`, keep the same scatter/local-sum structure but replace `comm.reduce(..., op=MPI.SUM)` with gathering all local sums (or the full array) to root via `comm.gather`, then compute the final total on rank 0 with a deterministic, order-independent summation (`math.fsum` over the concatenated/sorted data) so the printed result is bit-identical across different process counts — floating-point `+` is not associative, so any tree/pairwise reduction across ranks will vary unless you funnel everything through one fixed-order summation.
