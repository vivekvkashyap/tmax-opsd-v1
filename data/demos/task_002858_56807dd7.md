Work in three phases. First, transcribe `/app/voicenote.wav` with a speech-to-text tool (e.g. `openai-whisper`, `vosk`, or `speech_recognition` with a local engine) to recover the exact passphrase text, then normalize it (lowercase, strip spaces/punctuation as spoken) into the string the archive was encrypted with, and use it to open `/app/traffic_dump.zip` (`unzip -P '<recovered>' ...` or `7z x -p'<recovered>' ...` if unzip's crypto is unsupported). Second, diff the `evil/` vs `clean/` payload files byte-by-byte and pattern-by-pattern: look specifically for SQL-injection keywords (SELECT/UNION) combined with hex-encoded literals (`0x[0-9a-fA-F]+`) followed by padded whitespace before a comment marker — evil samples share this structural chain while clean samples use normal syntax or lack the whitespace-padded hex+comment combo. Derive a regex/heuristic from that structural difference rather than memorizing filenames. Third, implement `scan_payload(filepath)` in `/home/user/waf.py` reading the file content and returning True/False based on that pattern, then sanity-check it against both provided corpora before trusting it on the hidden eval set — a naive attempt that hardcodes sample strings or ignores whitespace/case variants will fail generalization.