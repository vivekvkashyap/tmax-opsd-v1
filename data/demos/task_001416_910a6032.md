Treat this as three coupled fixes plus a build script, and validate against the two corpora rather than assuming your fix is right. In `c_src/filter.c`, the loop `for (i = 0; i <= len; i++)` reads one byte past the buffer (should be `i < len`) — fix that off-by-one, and add real strict UTF-8 validation (reject overlong encodings, invalid continuation bytes, and out-of-range code points) before/while scanning for the `<script>`/`exec(` substrings, since the current code has none. In `rust_src/src/main.rs`, the bug is that `CString::new(...).unwrap().as_ptr()` drops the temporary `CString` immediately, leaving a dangling pointer passed to the FFI call — bind the `CString` to a named variable first so it stays alive for the duration of the `unsafe` call, and prefer passing the raw byte slice's pointer/length directly (since content can contain interior nulls, `CString::new` will fail on those) rather than going through a NUL-terminated `CString` at all. Write `build.sh` to compile `filter.c` into `libfilter.so`, set linker rpath so the Rust binary finds it at runtime, `cargo build --release`, then copy the binary to `/home/user/polyglot-sanitizer/bin/sanitizer_cli`. Finally, loop your compiled `sanitizer_cli` over every file in `/app/corpus/clean/` and `/app/corpus/evil/`, comparing exit codes and (for clean files) stdout byte-for-byte against what the reference oracle at `/app/sanitizer_oracle` produces for the same input, to confirm behavior matches before declaring done.