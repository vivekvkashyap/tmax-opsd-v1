Start by fixing the vendored package: open `/app/custom_b64-1.0/setup.py`, find the `Extension(...)` call referencing the wrong source filename, and correct it to point at the actual `.c` file present in that directory, then build/install with `pip install .` (or `python setup.py install`) from inside that directory so `import custom_b64` succeeds — verify with a quick `python3 -c "import custom_b64; print(custom_b64.decode(...))"` before moving on. For the checker script, read all of stdin as raw bytes (don't assume line-buffered input), parse it as an HTTP request to locate the `X-Secure-Auth` header case-insensitively, and wrap each risky step (missing header, `custom_b64.decode` failure, cert parsing) in its own try/except so you can emit the precise required string. Compute the server certificate fingerprint yourself: load `/app/server.crt` with a crypto/x509 library, get its DER bytes, and SHA-256 hash them to lowercase hex — do not trust any string embedded in the token as the source of truth. Match the decoded `username|role|cert_sha256_hex` fields exactly against this computed fingerprint before deciding GRANT/DENY, and print only the exact literal strings specified, nothing extra (no trailing debug output) since the oracle comparison is exact-match over stdout.