Parse `/home/user/search_logs.txt` by splitting each line on `|` into timestamp, user_id, and raw_query (trim whitespace from each field). Normalize each query with a pipeline like: lowercase it, then substitute every character that isn't `[a-z0-9 ]` with a space (e.g. `sed -E 's/[^a-z0-9]/ /g'` or an `awk`/`gsub` equivalent), collapse repeated spaces into one, and trim leading/trailing spaces. Count tokens by splitting the normalized string on whitespace (empty string = 0 tokens, be careful not to count a phantom empty token). Aggregate per user_id with an associative array (awk's native support is convenient here) tracking query count, running token sum, and running max. When writing `/home/user/user_stats.csv`, emit the exact header line first, then one row per user with `user_id,total_queries,total_tokens,max_tokens`, sorted by total_queries descending and user_id ascending as a tiebreak (`sort -t, -k2,2nr -k1,1` on the data rows, keeping the header separate). Test against a few hand-crafted lines including empty/punctuation-only queries to confirm the 0-token edge case works.
