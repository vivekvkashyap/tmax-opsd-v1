Split this into a measurement phase and a detection phase, and lock down the measurement number before writing the detector. Extract frames with `ffmpeg -i /app/network_capture.mp4 -vf fps=<rate> frame_%03d.png` (or extract every frame), run each through `/app/extract_edges.py` to get its edge CSV, build an adjacency/degree count per frame, compute each frame's degree centrality (e.g. average node degree, matching whatever definition the extractor implies), and average across all frames — this average is the threshold anchor your C++ detector needs, so compute it once in Python/shell and hardcode or pass it into the C++ program rather than recomputing video analysis at classification time. In `detector.cpp`, parse the CSV (Source,Target rows) into an adjacency count map, find the maximum single-node degree, and classify `EVIL` if that max exceeds `1.5 * average_centrality_from_video`, else `CLEAN`. Compile with `g++ -O2 -o /home/user/detector detector.cpp` (standard library only, no external deps) and validate against every file in both `/app/corpora/clean/` and `/app/corpora/evil/` before finishing, since the pass criterion is 100% correct classification on held-out samples using the same threshold logic.
