Parse the custom concatenation format with a small script rather than ad-hoc shell text munging: read `/home/user/legacy_docs.txt` line by line (or with a regex over the whole text), splitting on `===FILE:<name>===` headers and `===EOF===` footers, writing each captured body to `/home/user/extracted/<name>` (create the dir first). For the rename step, strip the `Draft_v<number>_` prefix with a regex substitution (e.g. `re.sub(r'^Draft_v\d+_', '', name)` or `rename 's/^Draft_v[0-9]+_//'` / a `for f in ...; mv` loop) — don't hardcode the specific filenames, derive the pattern generically so it works regardless of version number. When building the tarball, `cd` into the extracted directory (or use `tar -C`) so the archive contains only bare filenames, not the `extracted/` path prefix — a common mistake is tarring the directory itself, which the verifier's flat extraction would then fail to find. Finally satisfy the atomicity requirement: `tar czf` into a `mktemp` path in the same filesystem as the destination, then `mv` it into place at `/home/user/final_docs.tar.gz` so no partial file is ever visible at the final path.