Work top-down through the three parts. First fix the 502: compare the socket path Nginx's `proxy_pass` expects (`unix:/home/user/run/app.sock`) against what the app process is actually bound to in the supervisor config (likely `gunicorn --bind` to a TCP host:port) — change the supervisor program's command to bind gunicorn to that same unix socket path (e.g. `--bind unix:/home/user/run/app.sock`) rather than editing Nginx, then reload/restart both services with the exact commands given and confirm `curl localhost:8080/api/costs` no longer 502s. Second, write the `post-receive` hook in Python reading `oldrev newrev refname` from stdin (there can be multiple ref lines — handle at least one), use `git archive <newrev> | tar -x -C <target_dir>` (via subprocess) to materialize `/home/user/app_data/releases/<newrev>`, verify `cost.json` exists there before proceeding (exit nonzero with a message otherwise), then atomically retarget the `current` symlink (e.g. build a new symlink at a temp path and `os.rename` over the old one, avoiding a window where `current` is missing) and finally restart the app via supervisorctl. Third, actually exercise the pipeline: clone, add `cost.json`, commit, push, sleep, then curl and save the body — use this end-to-end run to catch any path or permission mistakes in the hook.
