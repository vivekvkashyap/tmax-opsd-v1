Start by inspecting the schema directly rather than assuming a shape: run `sqlite3 /home/user/graph.db ".schema"` and sample rows from each table to see how nodes/edges are actually modeled (a generic property-graph layout typically has one table for nodes keyed by id/label and one table for edges with source_id/target_id/rel_type columns). Once you understand the model, express "mutual follows" as a self-join on the edges table: join the target-users that `<target_user_id>` follows against the edges table again to find other users who follow those same targets, excluding the target user itself, then `GROUP BY` the other user and `COUNT` distinct shared targets. Apply `ORDER BY mutual_count DESC, similar_user_id ASC` and `LIMIT <limit> OFFSET <offset>` directly in SQL rather than post-processing in bash, so pagination and tie-breaking are correct. Add indexes on the edge table's source/target columns (and filter on rel_type) to speed up the self-join, and capture `EXPLAIN QUERY PLAN` for the exact parameterized query (substituting U1, 10, 0) into `/home/user/query_plan.txt`, checking it shows index usage rather than a full table scan. Rewrite `find_mutual.sh` to build and execute this SQL via `sqlite3`, output as `similar_user_id,mutual_count` CSV, and make it executable.