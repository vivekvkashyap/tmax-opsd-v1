Compute degree with a single pass: use `awk -F'|'` to print `$2` and `$3` (molecule_A and molecule_B) each on their own line for every event, one pair per row, then pipe through `sort | uniq -c` to get counts per molecule — that count is the degree. For the ranking file, sort the `count name` pairs numerically descending on count and, for ties, alphabetically ascending on name (e.g. `sort -k1,1nr -k2,2`), then reformat to `Name Degree` order (the count column comes first from `uniq -c`, so swap fields with `awk '{print $2, $1}'` after sorting, or sort on the right columns before reformatting — be careful about which column you sort on before vs. after the swap). For the bar chart, take the top 5 from that same sorted list and generate `#` repeated `Degree` times using `awk` (e.g. build a string in a loop or use `printf` with a repeated pattern), formatted as `Name: ###...` with exactly one space after the colon. Avoid any Python/Perl — do it purely with awk/sort/uniq/grep pipelines, and double check the tie-break sort key ordering matches degree-desc-then-alpha-asc exactly.