Work through the four sub-problems in dependency order since later steps need earlier ones built. In `parser.c`, implement the `%XX` branch of the state machine: after seeing `%`, read the next two hex characters, convert each to its nibble value (handle both cases 0-9/a-f/A-F), combine into one byte, write it to `dest`, and advance `src` by 3 total — get the pointer arithmetic right so you don't read past a truncated `%` at the end of string. For the build/link failure, the Rust build likely needs a `build.rs` (or `cargo:rustc-link-search`/`-lib` directives) pointing at the compiled `libparser.so`, and at runtime the dynamic linker needs to find it — export `LD_LIBRARY_PATH` (or use an rpath) when invoking the binary. In `main.rs`, the classic bug is calling `.as_ptr()` on a temporary `CString` that's dropped at the end of the statement, leaving a dangling pointer — fix by binding the `CString` to a named variable that outlives the FFI call. For the schema migration, use `ALTER TABLE rules ADD COLUMN action TEXT DEFAULT 'block';` via the `sqlite3` CLI in a script. Finally run the built binary with the literal payload argument and redirect stdout to `result.log`.