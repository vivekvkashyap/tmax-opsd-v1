Decompose into a clear awk/join pipeline and validate each intermediate against the sample data by hand. For Output 1: parse `config_logs.txt` with `awk`/`grep -oP` to extract `date,host` per line (one row per change event, param is irrelevant here), join against `inventory.csv` on `host_id` (sort both by host and use `join -t, -1 <col> -2 1`, or an awk associative array keyed by host loaded from inventory first) to attach `region`, then aggregate counts with `awk '{a[date,region]++} END{...}'`. For the rolling average, after producing sorted per-region daily totals, iterate chronologically per region tracking the previous day's total in an awk array keyed by region, defaulting to the current value when there's no prior day — output with `printf "%.1f"`. For Output 2: filter to `us-east` hosts via the inventory join, build a host x param count matrix (awk associative array `cnt[host,param]`), then for every unique pair of hosts (nested loop over the sorted host list, i<j) sum `abs(cnt[h1,p]-cnt[h2,p])` over the union of params either host touched. Sort final CSVs with `sort -t, -k1,1 -k2,2` (dates) and similarly for host pairs, and diff your output against the worked example in the spec before finishing.