Fix bottom-up: C library, then Rust build, then Python. First, in `parser.c`, replace the unbounded `strcpy(output, input)` with a bounded copy that truncates safely to fit a 16-byte buffer, e.g. `strncpy(output, input, 15); output[15] = '\0';` — verify by testing with an input longer than 16 chars and confirming no overflow/crash. Generate the patch with `cd /home/user/project && git diff` (init a git repo there if needed, or use `diff -u` against a saved copy of the original file) and save it to `parser_fix.patch`. Second, edit `build.rs` to use the `cc` crate: `cc::Build::new().file("c_ext/parser.c").compile("parser");` — this statically links it; run `cargo build` and iterate on any linker errors (e.g., missing include paths). Third, in `gateway.py`: parse the POST body with `json.loads`, wrap in try/except returning 400 on failure or missing/wrong-typed `data`/`user_id` keys; maintain a module-level `dict` counting requests per `user_id`, incrementing on each valid request and returning 429 once the count exceeds 3, before invoking the subprocess. Test incrementally: first confirm `cargo build` succeeds and the binary truncates correctly, then test the HTTP validation/rate-limit logic with manual curl requests.