Read `embeddings` from `/home/user/seq_data.h5` with `h5py`, then compute the full N×N dot-product matrix `D = X @ X.T` once (broadcastable / cheap enough to compute on rank 0 or redundantly on every rank, since N is small — the expensive part conceptually is the per-row softmax). For the log-softmax step, never compute `exp(D)` directly on raw values — subtract each row's max before exponentiating: `S[i,:] = D[i,:] - max(D[i,:]) - log(sum(exp(D[i,:] - max(D[i,:]))))`, equivalently use `scipy.special.logsumexp(D[i,:])` per row, since dot products here exceed 700 and unshifted `exp()` will overflow to `inf`/`nan`. Parallelize with `mpi4py`: get `comm.rank`/`comm.size`, have each rank take a contiguous or strided slice of row indices (e.g. `range(rank, N, size)`), compute stable log-softmax only for its assigned rows, then gather all rows back to rank 0 with `comm.gather` (or write directly with parallel HDF5 if available) before rank 0 writes the assembled `log_scores` dataset to `/home/user/results.h5`. Launch with the exact required `mpiexec -n 4 python /home/user/compute.py` command.
