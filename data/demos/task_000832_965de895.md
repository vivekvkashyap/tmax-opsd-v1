Don't brute-force by shelling out to the binary — reverse-engineer the algorithm first so you can crack it natively in C. Run `strings /app/token_gen` and `objdump -d /app/token_gen` (or `ltrace`) to identify the hashing library calls it makes (look for symbols/strings hinting at a known hash function name); confirm your hypothesis by running the real binary on a couple of known inputs, e.g. `/app/token_gen A1B2C3D4E5 1234`, and reproducing the exact same output independently (e.g. via `openssl` or a quick script) using your guessed formula (such as hashing `thumbprint:pin` or `thumbprint+pin` — test both concatenation styles since the separator matters) until it matches exactly. Once verified, write a C program that links against the matching hash library (e.g. `-lcrypto` for OpenSSL), reads `/home/user/redirect_logs.txt` line by line, and for each `thumbprint,token` pair brute-forces all 10,000 four-digit PINs (`0000`-`9999`, zero-padded) computing the hash of your confirmed formula per candidate and comparing to the target token — this is fast enough in compiled C. Write recovered `thumbprint,pin` pairs to `/home/user/recovered_pins.txt` in the same order as the input, without touching the original log.