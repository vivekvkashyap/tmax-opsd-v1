Start by querying a few rows from `raw_events` and pretty-printing the JSON to discover the actual nested field paths (sender/receiver/amount/status/type may be nested under several levels, e.g. inside a "payload" wrapper) — don't assume flat field names. For the SQL side, write `CREATE INDEX` statements in `optimize.sql` using SQLite's `json_extract(document, '$.path.to.field')` as the indexed expression on the sender and receiver paths you discovered, so future queries filtering/joining on those extracted values can use the index. For the Python analysis: query only rows where the extracted status is "completed" and type is "transfer", build a directed graph with networkx (edges = sender->receiver), run `networkx.pagerank` with default parameters, then filter candidate suspects to nodes that lie on at least one directed cycle (use `networkx.simple_cycles` or check strongly-connected-components of size >1 / self-loops) — a node with high PageRank but no cycle membership must be excluded even if it has the single highest overall score. Among the cycle-participating nodes, pick the one with max PageRank, round the score to 4 decimals, and write the required JSON shape to `suspect.json`.
