Build this as two small, testable pieces rather than one monolithic script. In the C validator, read stdin line-by-line (e.g. `fgets`/`getline`), and for each line split on commas carefully — don't assume `strtok` handles trailing/embedded whitespace or empty fields gracefully; validate each field explicitly: use `strtol`/`sscanf` and check the *entire* field was consumed (no trailing garbage) to confirm ID and Age are pure integers, then check ID > 0 and 18 <= Age <= 120, and separately scan the Email field byte-by-byte (or `strchr`) for at least one `@`. Preserve the original line text exactly (don't reconstruct it from parsed fields) when writing to stdout/stderr so formatting/whitespace is untouched. Compile with `gcc -o /home/user/validator /home/user/validator.c` and check the exit status. For the bash orchestrator, redirect stdout and stderr of the validator to the two target files separately (`validator < raw_export.csv > clean_import.csv 2> rejected.csv`), and make both files executable with `chmod +x`. Test against the actual raw_export.csv, inspect line order is preserved, and check no stray newlines/headers were introduced.