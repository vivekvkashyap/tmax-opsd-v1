Start by checking out the PR branch (`git checkout pr-104-fast-delta`) and reading each file before editing, since the bugs are localized and specific. In `delta_calc.c`, the inline assembly uses `addl` where it must compute `curr - prev`; change that single instruction to `subl` (keep it as inline asm, don't rewrite in plain C) — mind operand order, since AT&T syntax `subl %2, %eax` computes `eax = eax - %2`, so load `curr` into eax first then subtract `prev`. In `buffer.c`, implement `push_buffer`/`pop_buffer` as a standard ring buffer using the existing `head`/`tail`/`max`/`full` fields: push checks `full` first, writes at `tail`, advances `tail` mod `max`, and sets `full` if `tail == head`; pop checks emptiness (`!full && head==tail`), reads at `head`, advances `head` mod `max`, clears `full`. In `config.c`, parse `KEY=VALUE` lines with `fgets`+`strchr('=')` or `sscanf`, skipping blank lines, and set `cfg->max_buffer_size` via `atoi` when the key is `MAX_BUFFER_SIZE`. In the test fixture, replace the file-writing `default_sink` usage by defining a mock function that `strcat`/`snprintf`s into `mock_output` and assigning it to `write_sink` before `main()` runs its tests — don't touch `/etc/telemetry.log`. Build with `make` and `make test`, redirecting to the log file, and re-run it yourself to confirm the exact pass string appears.