Start by reverse-engineering the schema: run `sqlite3 /home/user/production.db ".schema"` to list all tables and their `FOREIGN KEY` constraints, then build a directed graph where an edge points from a dependent table to the table it references. Write `/home/user/generate_plan.py` to do a phase-based topological sort of this graph (Kahn's algorithm): repeatedly collect all tables whose dependencies are already satisfied by prior phases into the next phase, so each phase is a list of tables safely backed up together, and every table appears exactly once, only after all its FK targets. Since `/app/backup_scheduler` is a black-box stripped binary, don't try to disassemble it — instead treat it as a scoring function: run it against your generated `/home/user/backup_plan.json`, read the printed latency, and iterate. If latency is too high, experiment with phase balance — since cost per phase likely grows worse for very large phases, try splitting independent tables that don't need to share a phase into separate phases, or grouping them differently, and re-run the binary after each change until the reported "Total Latency" is under 1500ms. Keep `generate_plan.py` as the actual code that produced your final `backup_plan.json`.