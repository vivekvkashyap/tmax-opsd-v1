Work through this as separable subtasks. First, diff-read `filter_valid.sh` carefully line by line — the bug is a mismatched `-v` variable name passed to awk (the flag defines one name but the awk program body references a different one), so awk silently treats the referenced variable as empty/undefined and the score filter never actually constrains anything; fix the name to match. Second, to process the 5 CSVs in parallel from Bash, launch each `filter_valid.sh` invocation backgrounded with `&` (or via `xargs -P`), redirect each to its own temp output file, then `wait` before aggregating — don't `cat` files that are still being written. Sum the `score` field (column 4, skip header) of the filtered rows across all outputs with `awk` and write the numeric total to `/home/user/sum.txt`. Third, for the `nc` HTTP server, remember plain `nc` exits after one connection — wrap it in `while true; do { printf 'HTTP/1.1 200 OK\r\n\r\n%s' "$(cat /home/user/sum.txt)"; } | nc -l -p 8080 -q1; done &` (or equivalent) so it keeps accepting new connections, and verify with `curl` that the body matches the sum exactly with no extra whitespace.