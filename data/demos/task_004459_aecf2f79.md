Get the three services actually talking before touching detection logic, since the corpus test exercises the full pipeline end to end. Read `/home/user/app/config/broker.env` to find the real Redis host/port and fix `gateway.py` to load them via `os.environ.get('REDIS_HOST'...)` instead of its hardcoded (wrong) values, and fix `enforcer.js` to `BLPOP`/`RPOP` from the actual `log_queue` key the gateway pushes to — start each service and manually POST one clean payload to `/submit` to confirm it reaches the archive before writing detection logic. For Phase 2, in the worker: base64-decode the payload, recompute SHA-256 of the decoded `log` field and compare against the checksum in the JSON wrapper (reject on mismatch), then implement `classifyPayload` to scan for injected markers such as `<script>` tags, SQLi strings, or disallowed CSP directives/external domains — inspect a few files in `/home/user/corpora/evil/` vs `/home/user/corpora/clean/` to see what actually differs before hardcoding checks. Build `cli_test.js` as a thin wrapper that reads a file, calls `classifyPayload`, and exits 0/1 accordingly, then batch-test it against both corpora directories to confirm 100% separation. Finally `chmod 700` the archive directory.
