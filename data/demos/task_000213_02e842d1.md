First transcribe `/app/legacy_note.wav` — use a local speech-to-text tool (e.g. `whisper` if available, or another installed STT utility) to get the exact wording of which Python 2 patterns must be banned; don't assume the two classic patterns (unparenthesized `print` statements, `urllib2` imports) without confirming against the actual audio content, since the specific wording (e.g. exactly which modules/statements) is what the hidden corpus tests against. Once you have the patterns, write `/home/user/check_py3_ready.sh` using `grep -E` with care for false positives: match `urllib2` imports only via `import`/`from` statements (not the string "urllib2" appearing inside a variable name or value), and match bare `print` statements followed by a string/expression without an opening paren, while excluding lines that are comments (`#...`) or that use `print(...)` with parentheses. Test it directly against every file in `/app/corpora/clean/` (must all exit 0) and `/app/corpora/evil/` (must all exit 1) before finishing — iterate on the regex until 100% of both corpora classify correctly, since near-miss patterns (spacing variations, print inside comments) are exactly what trips up a naive first attempt.