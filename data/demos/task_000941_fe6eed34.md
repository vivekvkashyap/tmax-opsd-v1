Build each script independently and test it against small self-made inputs before wiring the master pipeline. Make `setup_pipeline.sh` idempotent using `mkdir -p` for every directory so repeated runs never fail. For `filter_logs.py`, iterate `.json` files in the input dir, wrap `json.load` in try/except to reject invalid JSON, verify all three required keys are present, confirm `cpu_usage` is numeric (`int`/`float`, not a string) and in `[0, 100]`, and check `service_name` contains none of the forbidden characters via a simple substring/regex check — copy only files passing every check, using something robust like `shutil.copy2` rather than shell interpolation (avoid injecting the filename into a shell command). For `analyze_video.py`, extract frames from `/app/dashboard_recording.mp4` (via `ffmpeg -i ... frame_%04d.png` or `cv2.VideoCapture`), for each frame crop the top-right 100x100 pixel region precisely (mind the coordinate origin and image width), compute mean R, G, B over that crop, and count frames where R > 150 and G < 50 and B < 50 strictly — print only the integer count. Finally have `run_pipeline.sh` call all three in order, redirecting `analyze_video.py`'s stdout into `video_alert_count.txt`, and `chmod +x` every script.