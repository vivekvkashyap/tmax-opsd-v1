Split this into recovery, plumbing, and logic, verifying each independently. First, recover the rate-card numbers by running OCR on `/app/storage_rates.png` (e.g. `tesseract /app/storage_rates.png -`), then manually inspect the raw OCR text against expected formatting (dollar amounts per GB and per request for three tiers) and correct any obvious OCR errors before hardcoding them into your script — never guess the numbers, read them from the image output. Second, set up environment/logging: append an `export FINOPS_LOG_PATH=/home/user/finops/optimizer.log` line to `/home/user/.bashrc`, create `/home/user/finops/`, and in `optimizer.py` build a `logging.handlers.RotatingFileHandler(path, maxBytes=10240, backupCount=3)` attached to a logger reading the path from `os.environ.get("FINOPS_LOG_PATH", ...)` so it works even if bashrc isn't sourced in the test harness. Third, implement the core loop: read lines from stdin until EOF, parse two space-separated ints, log `INFO` `Processing record: <GB> GB, <reqs> requests` for each, compute the three tiers' costs as `gb*price_per_gb + reqs*price_per_request` using your OCR'd rates, and print `min(...)` formatted to exactly 4 decimals with `print(f"{cost:.4f}")`. Test manually with a few hand-fed lines (including 0 requests) and confirm output formatting before considering it done.
