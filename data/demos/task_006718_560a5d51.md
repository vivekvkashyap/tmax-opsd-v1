Parse the CAZ format as a strict binary reader, not by shelling out to string tools: open the file, verify the 4-byte `CAZ1` magic, then read a little-endian `uint32` file count and loop that many times reading a little-endian `uint16` path length, that many raw path bytes (not null-terminated — read exactly N bytes into a buffer and NUL-terminate yourself), a little-endian `uint32` file size, and that many data bytes — use `fread`/`ntohl`/manual byte-shifting since the host may be big-endian and the spec requires little-endian. Before writing anything to disk, check the path for the two attack patterns: starts with `/`, or contains `../` as a substring anywhere — reject if either matches, log `REJECTED: <path>`, and skip past the file data without extracting it (still advance the file pointer by File Size so subsequent entries parse correctly). For accepted paths, create any needed subdirectories under the destination (e.g. `mkdir -p` equivalent via `strdup`+`strtok` on `/`), write the file, and log `EXTRACTED: <path>`. Keep the log lines in the exact order entries appear in the archive, opening `extraction.log` once and appending as you go.
