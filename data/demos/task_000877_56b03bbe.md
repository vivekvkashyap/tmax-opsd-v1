Reverse-engineer the oracle's behavior empirically rather than trying to infer every edge case from the prose alone — feed /app/log_oracle a variety of hand-crafted CSV inputs (rows with embedded quoted newlines, duplicate messages, sparse/irregular timestamps, single-row inputs, empty input) and diff its stdout against your Go program's output as you build it, iterating until they match exactly. Implement the pipeline as ordered stages: (1) a proper CSV reader that supports quoted fields containing literal newlines (Go's encoding/csv handles this) — after parsing, detect and drop any record whose raw fields contained a `\n` character (the quirk is about the *content*, not CSV syntax, so check post-parse field values); (2) MD5-hash the message field and skip rows whose hash was already seen, using a map; (3) bucket remaining rows into floor(timestamp/10)*10 buckets and sum metrics; (4) walk every 10-second bucket from min to max bucket inclusive, filling gaps with sum 0; (5) compute the anomaly flag by comparing each bucket's sum to the previous bucket's sum+50, with the first bucket always 0. Match CSV formatting (headers, line endings, no trailing blank line) exactly against the oracle's raw bytes.