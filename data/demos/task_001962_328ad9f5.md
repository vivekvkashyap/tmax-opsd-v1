Build the pipeline bottom-up, verifying each layer before moving on. In `/app/wav_parser`, run `cargo build` and read the lifetime error — it likely returns a pointer to a local that doesn't outlive the function; fix by transferring heap ownership across FFI (e.g. `Box::into_raw` with a matching free fn), keeping the C ABI signature intact, then produce `libwav_parser.so`. In `/app/c_filter`, `make` will show the missing `-fPIC` and a bad linker rule — fix both so `libfilter.so` builds as a proper shared object. For the C++ core, don't port the naive O(N*K) loop — compute the moving average with a running sum that adds the incoming sample and subtracts the one leaving the window (O(N) total), calling into the Rust parser and C filter via FFI, and build `libcore.so`. In the Python API, add per-IP rate limiting (track recent request timestamps per IP, evict entries older than 60s, return 429 once count hits 5) around the `ctypes` call into `libcore.so`, then start it on port 8000 via `/app/run_server.sh`. Validate correctness against a brute-force Python moving average and measure latency directly.
