Work through the four phases strictly in order since each depends on the previous output. Phase 1: `gcc -o /home/user/bin/seq_cov /home/user/src/seq_cov.c -lm` and `chmod +x` it — check the source first to know its exact CLI/output format before writing downstream code. Phase 2: run it on `/home/user/data/seqs.txt` and save stdout verbatim to `/home/user/output/cov.txt`. Phase 3: write `cholesky.awk` implementing the classic Cholesky algorithm from scratch — read the N×N matrix into a 2D awk array, then for each row i and column j<=i compute L[i][j] via the standard formula (subtract the dot product of already-computed row entries, divide by L[j][j] for off-diagonal, use sqrt of the remainder for the diagonal), filling the upper triangle with 0.0000, and print each row with `printf "%.4f "` per element (careful with trailing space/newline formatting matching expected output). Phase 4: write `integrate.sh` to map each sequence's characters (A=1,C=2,G=3,T=4) to numeric samples, then apply the trapezoidal rule (`0.5 * (first + 2*sum(middle) + last)` with Δx=1) per line, printing each result to 1 decimal place into `integrals.txt`. Validate each phase's output against hand-computed expectations for the small 4-sequence test set before considering it done.