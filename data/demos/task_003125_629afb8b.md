Work top-down through the pipeline and validate each stage's intermediate output. First, inspect `raw_data.json`'s actual structure with a quick `python -c "import json; print(json.load(open(...))[0])"` to find exactly where the author handle and mentioned-user handles are nested — don't assume field names, read them from the real file. Build RDF triples with `rdflib`: mint a `URIRef` per user under the `http://example.org/user/` namespace, and for each document add one `mentions` triple per mentioned handle from the author's URI to the mentioned user's URI. Run a SPARQL `SELECT` query against the graph to pull back (mentioner, mentioned) pairs — this decouples the RDF layer from the graph-analytics layer and satisfies the stated requirement to query via SPARQL rather than iterating the raw JSON again. Feed those pairs as directed edges into a `networkx.DiGraph`, then call `networkx.pagerank(G, alpha=0.85)` with otherwise-default parameters. When writing output, strip the namespace prefix off each URI so the JSON keys are bare handles (e.g. "alice"), and dump the float scores directly — verify the resulting graph's edge list matches your manual reading of the source data before trusting the PageRank numbers.