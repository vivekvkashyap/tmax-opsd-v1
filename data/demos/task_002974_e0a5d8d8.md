Write the C program to read the input file line by line, and for each line use OpenSSL's `EVP_DecodeBlock` (or `BIO_f_base64` chained with a memory BIO) to decode the Base64 payload — be careful that `EVP_DecodeBlock` pads output to a multiple of 3 bytes and can include trailing NUL bytes, so compute the true decoded length yourself (e.g. subtract padding based on trailing `=` characters in the input, or use the BIO approach which handles length correctly) rather than trusting the raw decode buffer length. Strip any trailing `\n`/`\r` from each line before decoding so it isn't treated as data. Hash the decoded bytes with `EVP_Digest`/`SHA256()`, convert the digest to lowercase hex manually (loop over bytes with `%02x`), and write `<hex>:<decoded_text>` lines to the output file. After the C program runs and produces `/home/user/secure_audit.log`, use `chmod 400` on it to set owner-read-only permissions — do this as a separate shell step, not inside the C program.