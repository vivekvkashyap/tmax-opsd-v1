Work through the four steps in order, since later steps depend on artifacts from earlier ones. Extract frames with `ffmpeg -i /app/experiment.mp4 -vframes 100 /home/user/dataset/frames/frame_%03d.jpg` (create the directory first). Parse `/app/metadata.bin` in C using `fread` against the documented layout — 16-byte header (4-byte magic "DATA", little-endian uint32 version, little-endian uint64 record count) followed by 100 fixed 16-byte records (little-endian uint32 frame number, uint32 event code, little-endian double sensor value); read each record with a packed struct or manual byte offsets (mind alignment — don't just `fread` into a naive C struct without `#pragma pack` since padding will misalign fields), then `rename()` each `frame_%03d.jpg` to `evt<code>_val<int(sensor)>_frm<frame>.jpg`, truncating (not rounding) the sensor value's integer part. For backups, `tar -cf base_backup.tar` the first 50 renamed frames explicitly (don't rely on lexicographic globbing matching frame identity — match by the original frame-number field), then use `tar --listed-incremental=/home/user/dataset/snapshot.snar -cf diff_backup.tar` after adding frames 51-100 so it captures only the incremental set. For the C HTTP server, parse just enough of the request line to detect `GET /backups/base` vs `GET /backups/diff`, respond with correct headers (`Content-Type: application/x-tar`, `Content-Length`) followed by the raw tar bytes, and keep it looping/accepting connections in the background on `127.0.0.1:8080`.