Treat the stripped binary as a black box you must characterize by input/output experimentation, not disassembly. Generate several small, varied edge-list CSVs by hand (isolated nodes, chains, cycles, disconnected components, ties in degree) and run `/app/graph_oracle` on each to observe its exact output. Infer the algorithm structure: connected components (likely via union-find), per-node degree counts, and a dense rank of nodes within each component ordered by degree descending, with ties sharing a rank. Pay special attention to tie-breaking behavior in ranking (dense rank vs. standard competition rank) and what value is used as `component_id` (verify it's the minimum node id in the component, not an arbitrary label) — these are the two places naive reimplementations diverge from the oracle. Once your hypothesis matches the oracle on all your hand-built test cases, implement it in C++ using an efficient union-find with path compression for components, an O(1) array for degree counts, and a per-component sort (or bucket sort by degree) for ranking — avoid O(n^2) approaches so it scales to 5M edges within the 2-second budget. Compile with `g++ -O3` and diff your output against the oracle's on the same inputs before considering it correct.
