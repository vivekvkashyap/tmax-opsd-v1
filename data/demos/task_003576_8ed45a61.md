Handle the two fixture problems before writing any server code. For the "corrupted index," don't try to hand-repair index internals — just drop and recreate it (or run `REINDEX`) via the sqlite3 CLI or a short script, then sanity-check citation counts with a couple of manual `SELECT COUNT(*)` queries against `citations` to confirm results look consistent before trusting them in your API. For the passphrase, transcribe `/app/instructions.wav` — use a speech-to-text tool available in the environment (e.g. an installed whisper/vosk model, or `ffmpeg`+a STT library you install) rather than guessing; the exact recovered phrase is what the `Authorization: Bearer <phrase>` header must equal, so extract it precisely including spacing/case. For the h-index endpoint, compute per-paper citation counts with a `GROUP BY` on `citations.target_id`, restrict to the requested author's papers, sort counts descending, and find the largest h where at least h papers have >= h citations (a single SQL window-function query with `ROW_NUMBER()` compared against citation count works, or do this reduction in Python after one query — either is fine as long as the definition is followed exactly). For top_papers, `GROUP BY target_id ORDER BY COUNT(*) DESC LIMIT 5` joined back to titles. Wrap everything in a Flask/FastAPI app with header-checking middleware returning 401 on mismatch, and start it bound to 127.0.0.1:8080 in the background so it stays up for verification.