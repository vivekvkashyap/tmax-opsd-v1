Split this into an OCR step and a server step, and test each independently before combining. First run `tesseract /app/secret_receipt.png stdout` (or similar) to see the raw extracted text, then write a redaction function using a regex that matches 16 digits optionally separated by dashes (e.g. `\d{4}[-]?\d{4}[-]?\d{4}[-]?\d{4}`) and replaces the match with the literal string `XXXX-XXXX-XXXX-XXXX`; test it against the OCR output to confirm the card number disappears and the literal text is exactly right (OCR may introduce stray whitespace/newlines — normalize before matching). Second, build the server on `http.server`'s `BaseHTTPRequestHandler`: check the `Authorization` header equals exactly `Bearer AUDITOR-TOKEN-992` before doing anything else and return 401 otherwise; always set the `Content-Security-Policy: default-src 'self'` header on successful responses; for POST bodies, read `Content-Length` bytes and check for the exact substrings `<script>` or `UNION SELECT` (case-sensitive) before other processing, returning 403 if found — do the WAF check before the redaction logic. Wire `GET /receipt` to return the pre-extracted, redacted text, and `POST /redact` to apply the same redaction function to the request body. Launch the server backgrounded (`&`) bound to 127.0.0.1:8080 and leave it running.