Parse `/home/user/proteins.fasta` preserving original order: each `>` line starts a new node, the following line is its sequence. Build the graph by comparing every pair of sequences' first-3-characters prefix (or longer, but the rule triggers at prefix length >= 3, so exact-match on the first 3 characters is the deciding test) and add an undirected edge when they match — this naturally forms disjoint cliques per distinct 3-letter prefix group. Implement the LCG exactly as specified character-for-character (same seed, same multiplier/increment/modulus, same call order — one `get_rand()` call per node per iteration, iterating nodes 0..N-1 in file order) since any deviation in call order will desync the reproducible sequence from the checker. For each of the 10,000 iterations, mark a node knocked out if `get_rand() < 0.25`, then compute the largest connected component among surviving nodes using a simple union-find or BFS/DFS over the adjacency structure restricted to active nodes — sum sizes per component and take the max (0 if none active). Accumulate the LCC size across iterations, divide by 10000 at the end, and format with exactly 4 decimal places into `/home/user/simulation_results.txt` as `Mean LCC: <value>`. Double-check by hand-tracing the first few LCG outputs against the formula to confirm your generator implementation is bit-exact before trusting the final average.