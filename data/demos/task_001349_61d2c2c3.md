Start by installing the SQLite C dev headers (e.g. `apt-get install -y libsqlite3-dev`) so you can compile with `-lsqlite3`. In the C++ program, open `/home/user/audit.db` read-only via `sqlite3_open`, then build the suspicious-transaction query using a window function: compute each department's average transaction amount with `AVG(amount) OVER (PARTITION BY department)` joined from `employees` to `transactions` on `emp_id`, and filter rows where `amount > 3.0 * dept_avg`. For each suspicious row, run a second query using a `WITH RECURSIVE` CTE that starts at the employee's `manager_id` and repeatedly joins to `employees` until `manager_id IS NULL`, taking the final row's `name` as `top_manager_name`. Use `sqlite3_prepare_v2`/`sqlite3_step`/`sqlite3_finalize` and check every return code for errors. When writing the CSV, format `amount` with exactly one decimal place (e.g. via `std::fixed << std::setprecision(1)`), order output rows by `tx_id` ascending, and write the exact header `tx_id,emp_name,amount,top_manager_name`. Compile to `/home/user/auditor` and run it to produce `/home/user/audit_report.csv`, then double-check the CSV content and ordering manually.