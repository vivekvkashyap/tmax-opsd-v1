Design `pipeline.c` to take `k` as argv[1], read `sensor_data.csv` (200 rows x 120 features + 1 label), and reduce each row by averaging consecutive non-overlapping blocks of `k` features. For LOOCV with 1-NN: for each row i, compute Euclidean distance in the reduced feature space to every other row j != i, take the nearest neighbor's label as the prediction, and tally correct predictions across all 200 folds to get accuracy — recompute this fresh for each candidate k since the feature space changes. Give the C program a way to both report accuracy and write the transformed dataset (e.g. a mode flag) so `run.sh` can drive the tuning loop. In `run.sh`, compile once with `gcc`, loop over `k` in {2,3,4,5,6,8,10,12,15,20}, capture each accuracy, and track the best — for ties, explicitly prefer the *larger* k (compare with strict `>` for new-better and `>=` combined with a k-comparison for ties, not first-found-wins). Write the winning integer k to `/home/user/best_k.txt` and the reduced dataset to `/home/user/cleaned_data.csv`, features at exactly 4 decimal places, preserving original row order.