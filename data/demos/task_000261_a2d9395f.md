Write ci_runner.sh incrementally and test each requirement in isolation. Argument validation: check `$#` for exactly one arg and `[ -x "$1" ]`, printing an error and `exit 1` on either failure, before doing anything else — test this path first with zero args and with a non-executable file. Build: `cd`/`make -C /home/user/project` (or `(cd /home/user/project && make)`) so the build works regardless of caller's cwd. Linking: rather than installing the .so system-wide, export `LD_LIBRARY_PATH=/home/user/project:$LD_LIBRARY_PATH` (or the directory containing the target executable) just before invoking it, and confirm the binary runs standalone with that env var set before wrapping it in valgrind. Profiling: run `valgrind --leak-check=full "$1"` and redirect/capture stderr (valgrind writes its report there) into a variable or temp file. Parse the leak total by grepping the captured output for the line containing "definitely lost" and extracting the numeric byte count with `grep -oE` (don't hardcode an expected number — compute it from whatever valgrind actually reports for that run). Write the two-line report with `printf` to get exact formatting, and `chmod +x` the script at the end.
