Build this as three independent, individually-testable pieces. For `process_billing.py`: iterate every CSV in `/home/user/billing_logs/` (glob, sorted by filename for deterministic ordering) with `csv.DictReader`, parse `cost` as a float, and for rows strictly greater than 500.00 append (not overwrite — open in append mode or accumulate then write once) a line to `/home/user/finops_output/anomalies.log` (creating the output dir with `os.makedirs(..., exist_ok=True)` first) formatted exactly `[<date>] WARNING: High cost anomaly - Service: <service>, Resource: <resource_id>, Cost: $<cost>` with cost forced to two decimals via an f-string like `f"{cost:.2f}"` — get the exact punctuation/spacing right since this will likely be diffed literally. For `organize_links.sh`: parse each anomaly log line with `grep`/`sed`/awk to pull out date and resource_id (e.g. a regex on `\[(.*)\].*Resource: ([^,]+)`), `mkdir -p` a per-resource directory under `finops_output/high_cost/`, and `ln -s` (absolute source path) the matching `billing_logs/<date>.csv` into it named `<date>.csv` — dedupe so re-running doesn't error on existing symlinks. For `logrotate.conf`, write a standard logrotate stanza targeting the anomalies.log path with `size 1k`, `rotate 3`, `compress`, and `missingok` directives inside the braces. Finally actually execute both scripts in order and inspect the resulting log and symlink tree before finishing.