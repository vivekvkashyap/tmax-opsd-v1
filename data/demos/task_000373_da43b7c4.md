Use `git bisect` (or a manual binary search with `git log`/`git checkout` + running `python3 simulate.py`) between the known-good initial commit and HEAD to pinpoint the exact commit that introduced the crash — don't assume it's the most recent commit; test intermediate commits and mark each good/bad based on whether the script exits cleanly or crashes with a dump. Once located, record the full commit hash (`git rev-parse`). Next, run the buggy code to actually produce sim_memory.dmp, then extract the embedded error string by scanning the binary dump for a pattern matching `ERR_CODE_[A-Z_0-9]+` (e.g. `strings sim_memory.dmp | grep -oE 'ERR_CODE_[A-Z_0-9]+'` or a Python regex over the raw bytes) rather than hardcoding a guess. For the fix, apply the rationalization trick: `sqrt(x**2+1) - x` equals `1/(sqrt(x**2+1) + x)` algebraically — rewrite `calculate_energy` in the current HEAD (not the bad commit) using this form to avoid cancellation for large x. Finally write the two-line resolution file with the discovered hash and error code, and rerun simulate.py from HEAD to confirm it now succeeds with no dump/error.
