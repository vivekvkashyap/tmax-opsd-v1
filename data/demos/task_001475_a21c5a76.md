Start by creating the index with `CREATE INDEX idx_dep_source ON dependencies(source_id);` since traversal always looks up targets by source. For the traversal script, write a single `sqlite3` invocation using a recursive CTE: seed the base case by joining `tasks` on `name = ?` to get the starting id, then recursively join `dependencies` on `source_id` to walk downstream, collecting `target_id`s and joining back to `tasks` for names — exclude the starting task itself from final output, dedupe (recursive CTEs on a DAG can revisit nodes if there are diamond dependencies, so use DISTINCT or a UNION rather than UNION ALL to avoid duplicate output rows), and sort alphabetically before printing. Pass the task-name argument safely into the query using sqlite3's parameter binding (e.g. `.param` or constructing with `printf`/here-doc plus `-cmd` bind, or simplest: use a parameterized query via `sqlite3 db ".param set :name '$1'"` or embed via a bound placeholder) rather than naive string concatenation. Finally, run `EXPLAIN QUERY PLAN` on the same CTE hardcoded to 'extract_users' and redirect the raw output to the plan file, checking it mentions the new index by name.