Start by recovering the filter type: run OCR (e.g. `tesseract /app/rule.png stdout`) on the image and parse out the value after `FILTER_TYPE=` — don't hardcode a guessed value, extract it programmatically since your C program needs to embed or read this at runtime (you can OCR once during build/startup and bake the result into the server, or shell out to tesseract at startup). Then write a C TCP server using standard `socket`/`bind`/`listen`/`accept` on `127.0.0.1:7070` that, per connection, reads until EOF (loop on `read`/`recv` accumulating into a growable buffer, since the client signals end of input by half-closing the write side), then parses the buffered CSV line by line (`source,target,edge_type,weight`), filters to rows matching the extracted edge_type exactly, and accumulates per-source weight totals in a small hash map or linear array of (name, sum) pairs. Find the source with the maximum total (careful with tie-breaking and floating vs integer weight parsing — check whether weights should be summed as integers or floats based on the sample data), then write back exactly one line in the specified `MAX_SOURCE: <name>, AMOUNT: <total>\n` format, using the `NONE`/`0` fallback when no rows matched. Compile with `gcc`, launch it in the background (e.g. `./server &` or `nohup`), and verify with a local test client that a full CSV payload round-trips to the correct response before considering it done.
