Write `process.c` to read `/home/user/sensor_data.csv` line by line with `fscanf`/`strtod`, filtering each row where X or Y falls strictly outside `[-50.0, 50.0]` (this naturally also excludes the `-999.0` sentinel for missing values, so you don't need a separate special case for it). Accumulate valid points in an array (or do two passes over the file) so you can compute exact means first, then use those means to compute the unbiased sample covariance terms (`Cov(X,X)`, `Cov(Y,Y)`, `Cov(X,Y)`, each divided by N-1) — a single-pass sum-of-products formula is more error-prone here, so prefer the two-pass mean-centered approach for accuracy. Derive the regression slope from `Cov(X,Y)/Cov(X,X)` and the intercept from `meanY - slope*meanX`. Write the eight output lines to `/home/user/clean_stats.txt` using `%.4f` for every float and matching the exact label text and order given in the task, then compile with a command like `gcc -O2 process.c -o process -lm`, run it, and diff your output file's formatting against the required template before finishing.
