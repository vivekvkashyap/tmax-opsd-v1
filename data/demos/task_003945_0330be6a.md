First recover the 16-character watermark string: run OCR (e.g. `pytesseract`/`tesseract` CLI) over `/app/watermark.png`, print/inspect the recovered text, and hardcode exactly that string as the check constant in your script (don't guess it). Parse the `.artx` binary format precisely: read 4 bytes and compare to `b'ARTX'`, read the next 16 bytes and compare to the OCR'd watermark, read 4 bytes as `struct.unpack('<I', ...)` for the declared uncompressed size, then RLE-decode the remainder as `[count][char]` pairs — reject if the byte stream ends mid-pair (odd trailing byte) rather than crashing. After decoding, check `len(decoded) == declared_size` exactly, then `json.loads()` the bytes in a try/except, and confirm the parsed object has key `"artifact_id"`. Wrap every validation step in a single top-level try/except so any malformed input falls through to `sys.exit(1)` without partial writes. On success, write JSON to a sibling temp file (e.g. `output_path + '.tmp'`) and use `os.replace()`/`os.rename()` to atomically publish it, only after which exit 0 — never create the output file on the reject path. Test against small handmade clean and corrupted artifacts before trusting it on the hidden corpus.