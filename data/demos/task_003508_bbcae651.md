Write `deploy_update.sh` to parse `$1` (the manifest) line by line, splitting on `|` and trimming whitespace from each field (path, octal perms, acl user, acl perms) — skip blank lines. For each entry: `mkdir -p` the destination's parent dir under `/home/user/production`, `cp` the file from `/home/user/release_v2`, then `chmod <perms>` and `setfacl -m u:<acl_user>:<acl_perms>` on the deployed copy. The critical correctness point is the audit log: don't just echo back the manifest values — after deployment, re-derive the actual applied state from the filesystem itself (e.g. `stat -c '%a'` for octal perms, and parse `getfacl` output with `awk` to extract the specific named-user ACL entry) so the log reflects reality, not intent. Collect one `FILE: <path> - PERMS: <perms> - ACL: <user>:<acl>` line per deployed file, sort all lines alphabetically by file path (e.g. pipe through `sort`), and write them to `/home/user/audit.log`. Make the script executable with `chmod +x`, then actually run it against the manifest to produce the log.