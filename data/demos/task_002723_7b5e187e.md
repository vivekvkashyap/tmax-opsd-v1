Structure `/home/user/mcmc.go` in clear stages matching the spec exactly, since the verifier checks precise numeric fields. Parse `/home/user/data.csv` with `encoding/csv`, skipping the header row. Compute OLS closed-form: beta = covariance(X,Y)/variance(X), alpha = mean(Y) - beta*mean(X) — this is your accuracy baseline, independent of MCMC. For the MCMC sampler, use `rand.New(rand.NewSource(42))` (a single seeded generator, not the global `rand` funcs, so the sequence is deterministic and reproducible) and draw proposals from `rand.NormFloat64()*0.1 + current`; compute the log-likelihood under the fixed-variance Gaussian model plus the uniform prior bounds check ([-100,100], reject outside instantly), and accept/reject via the standard Metropolis ratio using `math.Log(rand.Float64())`. Run exactly 100,000 iterations, discard the first 10,000 as burn-in, and average the rest for posterior means. Time only the sampling loop with `time.Now()`/`time.Since()`, excluding I/O and OLS. Compare `|mcmc_alpha-ols_alpha|<=0.05` and same for beta to set `accuracy_pass`, then marshal all six fields to `/home/user/results.json` matching the schema exactly.
