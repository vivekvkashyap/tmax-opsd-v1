Work through the stack bottom-up: fix config, start backend, start proxy, then verify.
1. Read `/home/user/backend/app.py` to find the two hidden requirements it enforces before serving: the required `TZ` environment variable value and the exact pin code it expects on stdin (both are checked explicitly in the source — read the comparisons directly rather than guessing).
2. Edit `/home/user/nginx/nginx.conf` so `proxy_pass` points at `127.0.0.1:9001` (matching where app.py actually binds), leaving the `listen 8080` directive untouched.
3. Write `/home/user/start_backend.exp` using Expect to set the `TZ` env var you discovered, `spawn` the backend, expect its pin prompt, `send` the pin code you discovered followed by a newline, then detach/background it so it keeps running (e.g. `disown`/nohup pattern within expect, or spawn with `-noecho` and background via `&` in a wrapping shell).
4. Start nginx with the given `-p`/`-c` flags.
5. Write `/home/user/monitor.py` to GET `http://127.0.0.1:8080/`, catching connection/timeout/HTTP errors explicitly (e.g. `requests.exceptions.RequestException`), appending `SUCCESS` on a 200 response or `ERROR` otherwise to `/home/user/health.log`. Run it once at the end and confirm via `curl` that the full proxy chain actually returns the backend's JSON before finishing.
