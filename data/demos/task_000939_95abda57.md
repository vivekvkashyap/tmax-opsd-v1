Work through this as three sequential fixes plus a validation binary, testing each in isolation. First get the Rust project to build: check `Cargo.toml`/build scripts for missing C library links (look for `build.rs` or `links =` entries referencing system libs) and install/point to the correct dev packages or fix `pkg-config`/env vars — run `cargo build` repeatedly until clean. Second, find the panics: look for direct array indexing on frame-index fields from deserialized payloads (e.g. `frames[idx]` without bounds checks) and fix with `.get(idx)` style checked access or explicit range validation against the actual frame count; also check `serde` deserialization for fields that should be `Option` or validated rather than unwrapped. Third, find `refine_motion` and add a hard iteration cap plus a convergence-delta check so it can never loop forever — trace why the current condition can fail to shrink toward convergence. For `payload_filter`, use `ffprobe -v error -count_frames -show_entries stream=nb_read_frames /app/video_fixture.mp4` (or equivalent) to get the true frame count dynamically rather than hardcoding it, since the pass/fail boundary in the hidden corpora depends on exactly that number; wrap all parsing/processing in error handling so malformed JSON or bad indices produce a clean `exit(1)` rather than a panic.