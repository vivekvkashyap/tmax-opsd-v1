Since your `process_feed.py` will be fuzz-tested for bit-exact stdout equivalence against a hidden oracle, focus on matching the spec exactly rather than approximating it. Read all of stdin, extract every line matching a strict regex for `[INTEGER] METRIC_VAL: FLOAT` (anchor it so noise text doesn't partially match), discarding anything that doesn't fit that exact shape. Collect the valid (timestamp, value) pairs, determine the min and max timestamp among them, and build a dense integer range with step 1 across that span. For timestamps present in your extracted data, use the extracted value; for missing ones, linearly interpolate strictly between the nearest lower and higher known timestamps (not global regression, not extrapolation past the ends). Round every output value to exactly 2 decimal places using a consistent rounding rule (Python's `round`/format `.2f` banker's-rounding behavior is a safe default to match a Python oracle) and print `timestamp,value` CSV rows with a header, being careful about edge cases: empty input, a single valid reading, or duplicate timestamps. Separately, write `run_pipeline.sh` to pull the SRT subtitle stream out of the mp4 with `ffmpeg -i input.mp4 -map 0:s:0 out.srt` (or pipe to stdout), feed that text into your script, and bulk-load the resulting CSV into a SQLite `readings` table with a schema matching the CSV columns.
