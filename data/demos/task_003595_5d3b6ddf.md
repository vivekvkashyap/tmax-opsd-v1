Port the legacy TLV parsing logic (magic header check, then repeated [1-byte type][2-byte little-endian length][value] records) into a Python 3 C-extension, but harden every unsafe spot rather than translating it as-is: reject (raise ValueError) any record whose Length exceeds the fixed value buffer size instead of memcpy'ing past it; when Type is the special underflow-triggering value, validate the length is non-zero *before* subtracting from it (don't let an unsigned length wrap around) and reject if it would; and treat a zero-length record as valid only if the stream correctly advances past it, never dereferencing/advancing based on unvalidated pointers. Build with a standard `setup.py` (`Extension` + `build_ext`) so the module is importable as `fast_tlv`. In sanitizer.py, call the module's parse function inside try/except ValueError: on success, copy the exact input bytes to the output path and exit 0; on the exception, don't write the output file and exit 1. Before trusting it, craft your own small valid and malformed TLV byte strings that exercise each of the three vulnerability classes to confirm accept/reject behavior, and separately confirm the benchmark script's timing target is met.
