Handle the three parts independently, in the order they naturally depend: OCR first, then performance fix, then final run. Run `tesseract /app/dashboard.png stdout` to extract the `RELEASE_VERSION: <version_string>` line, and parse out just the version substring programmatically (don't hardcode a guessed version) — double check OCR didn't mangle punctuation like hyphens/dots in the version string, since it becomes part of an output filename. For the performance fix, read `/home/user/merge_builds.py` and `/app/routes.json` to find the O(N^2) pattern — typically a nested loop re-scanning the full list to find "the latest build per module" for every item; replace it with a single pass using a dict keyed by module name where you compare/overwrite only when a later `build_id` is seen, giving O(N) behavior while preserving identical output semantics (order and tie-breaking) to the original slow version — verify this by diffing old vs. new output on a small sample before trusting it on the full dataset. Preserve the script's CLI contract (`python3 merge_builds.py <output_file>`). Finally, run the optimized script with the output path built from the OCR'd version string, and separately time it (e.g. `time python3 merge_builds.py ...`) to confirm it's comfortably under the 1.0s threshold, not just barely passing.
