Start with the audio: use `ffmpeg` to inspect/convert `/app/alert.wav` if needed, then run a speech-to-text tool (e.g. install and run `whisper` or another available STT) on it and read the transcript text file it produces rather than guessing — the offline node names are spoken in the recording, so extract them from the actual transcription output. Then write `/home/user/route_calculator.py` to open `/app/routing_backup.db` read-only, and explicitly bypass the corrupted index with `SELECT ... FROM edges NOT INDEXED` (or `DROP INDEX idx_edges_source` and rebuild a fresh one) so no phantom rows leak in. Build an adjacency list excluding any edge touching the transcribed offline node names, then run Dijkstra's algorithm (a `heapq`-based priority queue) from the start node name to the end node name, printing the integer total weight to stdout, or `-1` if unreachable. For the 100ms latency requirement, create an index once (e.g. via a startup check that creates it if missing) and keep the DB connection/query efficient — avoid recomputing the whole graph unnecessarily on every invocation if that's what's slow. Test with several random start/end pairs before finishing.