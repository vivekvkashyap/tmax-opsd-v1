Transcribe `/app/voicemail.wav` first using the local whisper binary, e.g. `/opt/whisper/main -m /opt/whisper/models/ggml-base.en.bin -f /app/voicemail.wav -otxt` (check `--help` for exact flags/output naming), and read the resulting transcript carefully rather than guessing at the content — the directory layout, symlink target, and the precise transformation steps (order of operations and which characters map to which digits) all come only from what's said. Recreate the described directories/symlink under `/home/user/` exactly as named (use `ln -s` for the symlink, pointing at the correct relative/absolute target). For the processor script, implement the transformation steps in the exact order dictated by the audio, reading one line from stdin (strip the trailing newline) and printing the result to stdout; make it executable with `chmod +x` and include a `#!/usr/bin/env python3` shebang. Before finishing, manually test your script against a few hand-picked strings to confirm each transformation step behaves as you expect, since the grader will fuzz it with hundreds of random strings and require byte-for-bit matching output.