For the C flattener, detect symlink loops safely: don't just call `realpath()` blindly since it can hang or error unpredictably — instead manually follow symlinks with `readlink()` in a bounded loop (e.g. max ~40 iterations or track visited paths in a small array/set) and treat exceeding the bound as a loop to skip with a stdout warning; use `lstat()` first to distinguish symlinks from regular files, and `stat()` (which follows links) to confirm the final target is a regular file, treating ENOENT as broken. For atomic writes, open/write to `<dest>/<name>.md.tmp` (or use `mkstemp` in the dest dir), flush/close it, then call `rename()` to the final name — this guarantees no partial file is ever visible under the final name. For Phase 2, use `grep -oP 'DOC_ID: \K[0-9]+'` or `sed`/`awk` to extract the ID from each `.md` file, then `mv` to prepend `${id}_` only when an ID was found, leaving other files untouched; append the archive marker line with `echo >>` or `sed -i '$a...'` to every file. For Phase 3, build the tarball with `tar -czf final_docs.tar.gz -C /home/user/docs_flat .` so paths inside the archive are relative to that directory, not prefixed with `docs_flat/`.