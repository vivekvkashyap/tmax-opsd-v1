Tackle this in the stated order — spec, then correctness, then speed. First OCR `/app/whiteboard.png` (e.g. `tesseract`/`pytesseract`) to recover the actual `DELIM` and `OFFSET` values the previous developer used, and read the recovered text carefully rather than assuming defaults; update the corresponding constants in `/home/user/parser/parser.cpp`. Next inspect the existing parsing loop for the segfault: after matching the delimiter and skipping the offset bytes, the code reads a length byte and then blindly allocates/reads that many bytes without checking it against remaining file size — add a bounds check (compare against bytes remaining in the buffer/file) and skip or safely truncate malformed entries instead of reading out-of-bounds or over-allocating. For performance, the current implementation does per-byte `read()` syscalls in a loop, which is extremely slow on large files — replace this with a single `mmap()` of the whole file (or one large buffered read into memory) and scan/search within that buffer in-process, eliminating per-byte syscall overhead; this is the change needed to clear the 0.2s threshold on a multi-MB dump. Compile to `/home/user/parser/parser`, run it against `/app/mem_dump.bin` writing to `/home/user/extracted_strings.txt`, and time it yourself before considering the task done.