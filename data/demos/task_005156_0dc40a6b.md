Implement the C++ program as three clearly separated stages so each rule is easy to get exactly right: (1) parse each line on ` | ` into timestamp/user_id/message, then clean the message by keeping only chars with ASCII code in [32,126] (drop everything else, including the file's non-UTF8 noise bytes — read the file as raw bytes/latin1-safe, not strict UTF-8, or you'll throw on the injected high-byte noise); (2) for global dedup, maintain a `std::set<std::string>` (or unordered_set) keyed on `user_id + "\x1f" + cleaned_message` (or similar unambiguous join) and skip any line whose key was already seen, checked in original line order; (3) resample into 16 fixed one-minute bins from 10:00:00 to 10:15:00 inclusive — for each bin keep only the *earliest* surviving (non-deduped) entry, and for empty bins synthesize `SYSTEM | NO_ACTIVITY`. Be careful with boundary lines exactly at :00 seconds and anything outside the 10:00–10:15 window (must be excluded even if only one second over). Output timestamps with seconds forced to `:00`. Compile with `g++` and run it to actually generate `/home/user/processed_logs.txt`, then spot-check a few lines against your own trace of the rules.