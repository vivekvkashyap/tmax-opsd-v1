Start by extracting the coefficients from the image at `/app/scoring_params.png` using OCR (`tesseract` CLI, installing it first if missing) — read the raw OCR text output yourself and parse the `alpha=`, `beta=`, `gamma=` values from it rather than guessing; if OCR output is noisy, crop/preprocess (grayscale, threshold, upscale) to improve accuracy, and double check the parsed numbers look sane (e.g., are they in a plausible small range). Then write the C++ program to parse the FASTA file (lines starting with `>` are headers, following line(s) are sequence), take exactly the first 20 bases of each sequence as the primer, and compute GC_content, Tm, and the homopolymer run length precisely per the given formulas — be careful with the homopolymer scan to track the longest run correctly (reset counter on mismatch, track running max) and use `double` precision throughout to avoid rounding drift, since the grader checks MSE against a reference to a tight tolerance. Format each output score to exactly 6 decimal places. Compile with g++ and run it, then spot-check a few rows of `/home/user/output/primer_scores.csv` by hand-computing one sequence's score to confirm your formula implementation matches the spec exactly.