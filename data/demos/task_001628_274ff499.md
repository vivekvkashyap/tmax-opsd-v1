Since the verifier will fuzz your Go program against the real binary on arbitrary inputs, focus first on discovering the oracle's exact algorithm rather than guessing: run `strings /app/user_token_bin` to look for hardcoded salts, format strings, or hash-function symbols, and use `objdump -d` / `ltrace` / running the binary with a few known inputs to see how output length and character set behave (e.g. hex-digest length strongly suggests a specific hash like SHA-256 over some concatenation of the arguments and any discovered salt). Once you have a hypothesis, test it against the oracle on several varied inputs (different lengths, edge-case uid values) before writing your Go implementation, so you're not just matching one example. Implement `/home/user/token_gen.go` to reproduce that exact construction and hashing, compile with `go build -o /home/user/token_gen`, and re-verify byte-for-byte output equality against the oracle on many random samples yourself. For the Git hook, create the bare repo with `git init --bare`, write an executable `hooks/pre-receive` that calls your compiled `token_gen` with the specified test arguments and redirects output to the log file. Keep the user-setup script idempotent by checking `id <user>`/`getent group` before creating.
