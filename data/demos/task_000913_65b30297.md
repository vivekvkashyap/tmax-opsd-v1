Treat the binary as a reverse-engineering target: run `strings /app/upload_server_bin` to look for hardcoded key material, hash-related strings, or header names, then use `objdump -d` (or gdb with breakpoints on read/recv/write) to trace how the `X-Custom-Auth` header value is checked and how the payload bytes are transformed before being written to disk — confirm your hypothesis by testing the real binary as a black-box oracle (send it crafted requests and observe accept/reject and output bytes) rather than assuming an algorithm. Once you've derived the auth check (likely a hash of some time-based or fixed string plus a salt) and the cipher (likely a simple XOR or similar reversible transform with a fixed key), reimplement both in your Python replacement — recompute the auth value the same way the binary does at request time (e.g., if it's date-based, compute today's date server-side, don't hardcode a token). Build the firewall path check by normalizing/decoding the `X-File-Name` header (handle both `../` and percent-encoded `..%2F`) and rejecting anything that resolves outside `/home/user/safe_uploads/` via `os.path.realpath` comparison, not a naive substring check. Log every request as JSON lines with status/filename/reason as specified.
