Build the frame extraction exactly like a careful ffmpeg pipeline: put `-ss T` *before* `-i /app/dashboard_vnc.mp4` for fast/accurate seeking, request `-vframes 1 -f image2pipe -vcodec rawvideo -pix_fmt rgb24 -` and redirect ffmpeg's own stderr to `/dev/null` inside the command string so no log noise leaks onto stdout (your popen'd process's stdout is what you're reading, and any stray ffmpeg banner text would corrupt the raw pixel stream if not sent to stderr already, but be sure your own program never writes anything except the final number). Run this via `popen(cmd, "r")` and `fread` exactly `width*height*3` bytes (1920*1080*3) into a buffer — verify the fread count to catch truncated pipes. The frame is RGB24, so for each pixel `(x,y)` with `x,y` in `[0,100)`, the red byte is at `buffer[(y*1920 + x)*3]`; sum those and divide by `10000.0` (100*100 pixels) for the average. Print with `std::fixed << std::setprecision(2)` and nothing else — no extra `std::endl`-preceding debug text, no trailing prompts.
