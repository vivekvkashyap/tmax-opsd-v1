First recover the port from the image: open /app/network_diagram.png (or OCR it, e.g. with `tesseract` if available, or view it directly) to read the labeled port number embedded in the diagram — do not guess or hardcode a plausible-looking port. Then design finder.c around the standard /proc credential-leak technique: parse /proc/net/tcp line by line, match the local address field's port (hex, after the colon) against the target port converted to hex, filter for connection state `0A` (LISTEN), and extract that row's inode number. Then iterate over /proc/*/fd/*, following each fd symlink with readlink and matching against the pattern `socket:[<inode>]` to find the owning PID (skip PIDs you can't access due to permissions). Read that PID's /proc/[pid]/cmdline (note: entries are NUL-separated, not space-separated — split on '\0'), scan the arguments for one starting with `--passphrase=`, and print only the substring after the `=` with no trailing newline. Compile with gcc and test manually by starting a dummy listener process with a `--passphrase=` arg on some port and running your finder against it. Finally write firewall_rule.sh to echo the iptables DROP rule for the port you found in the image.