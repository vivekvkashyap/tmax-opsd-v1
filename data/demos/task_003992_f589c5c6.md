Work through this as three sequential, independently-testable fixes before wiring the server. First, `cd /app/libmathops-1.2.0` and inspect the Makefile's `CFLAGS` — building a shared object (.so) that will later be linked into another shared object requires position-independent code, so add `-fPIC` to CFLAGS, then confirm `make` actually produces `libmathops.so` cleanly. Second, `cd /app/pr-104` and read `fibfast.c`: find where it calls the library function that returns a heap-allocated (malloc'd) string, uses it (e.g. via printf), but never calls `free()` on it — add the missing `free()` call right after the string is no longer needed, then `make` to rebuild `fibfast.so`, verifying it links against the newly-fixed libmathops. Third, write `server.sh`: `enable -f /app/pr-104/fibfast.so fibfast`, export `LD_LIBRARY_PATH=/app/libmathops-1.2.0` so the runtime linker finds libmathops.so, then use `socat TCP-LISTEN:9090,reuseaddr,fork EXEC:...` (or a bash read-loop with coproc) to accept lines, parse the JSON manually (bash has no JSON parser — use simple pattern extraction like `grep -oP` for the n and mod integers), call the builtin, and echo back `{"result": <value>}\n`. Run it backgrounded and test with a raw TCP client before finishing, including a burst of repeated calls to confirm memory stays flat after your free() fix.
