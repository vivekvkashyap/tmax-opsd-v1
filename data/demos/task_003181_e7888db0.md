This is a bug-for-bug behavioral clone, so precisely replicate the described quirks rather than writing a "clean" CSV parser. Read stdin line-by-line using Python's `csv` module set up to detect logical rows, but explicitly special-case and drop any row whose raw source or translated field contains an embedded `\n` — even properly quoted multi-line fields, which a naive `csv.reader` would otherwise correctly join, must still be discarded per the legacy bug. For each surviving valid row: compute `bucket = timestamp - (timestamp % 300)`; compute `distance = abs(len(source_string) - len(translated_string))`; maintain a rolling window of at most the last 3 distances *per bucket*, resetting the window completely the moment a row's bucket differs from the previous row's bucket (not per-service, purely time-window-based); print `bucket,rolling_avg` with the average formatted to exactly 2 decimal places for every valid row processed, in order. Since correctness will be checked by exact output diffing against a legacy binary across many randomized fuzz inputs, test against tricky cases (rows with embedded newlines inside quotes, bucket boundary timestamps, empty strings) before trusting your logic. Also create `/home/user/sync_cron` with a crontab line running the script every 15 minutes with the exact stdin/stdout redirection specified.