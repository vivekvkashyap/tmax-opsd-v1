Implement this as a strict sequential pipeline matching the spec's exact operations, since the checker does an exact frame/value comparison. Load both CSVs, fill missing `price` per-dataset with that dataset's median and missing `rating` with that dataset's mean (compute fill values independently for A and B, not combined). Fit one `TfidfVectorizer` on the concatenation of both `desc` columns (default params), transform A and B separately, then compute the full cosine similarity matrix between A and B rows. For each row in A, find the max similarity value, gather all B indices tied at that max (use `np.isclose` for float comparisons), and break ties by picking the lexicographically smallest B `id`. Build the joined table with A's price, B's price, and B's rating as features X and A's rating as target y, fit one `LinearRegression`, and predict rating for A on that same X (no train/test split). Round `similarity_score` and `predicted_rating_A` to 4 decimals, order columns exactly as specified, sort rows by `id_A`, and write to `/home/user/predictions.csv` with a header.