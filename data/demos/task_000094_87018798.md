In your Python script, connect with `sqlite3`, and craft the query to explicitly append `NOT INDEXED` right after the table name in the `FROM backups NOT INDEXED` clause so SQLite is forced to full-scan instead of using the corrupted `idx_status_time` index — don't touch the schema or drop the index. Build a `WITH RECURSIVE` CTE: the base case selects rows where `parent_id IS NULL` (chain roots) carrying along the root's own timestamp for later sorting, and the recursive case joins children to their parents via `parent_id = id`, accumulating each chain's member ids (e.g. as a comma-separated path or via post-processing) plus a flag for whether any member has `status = 'FAILED'`. After computing all chains, group by root, keep only those with at least one FAILED descendant (including the root itself), sort by root timestamp descending, then apply pagination logic yourself in Python (skip the first `page_size * (page-1)` chains, take the next `page_size`) rather than relying on SQL `LIMIT/OFFSET` inside the recursive query, since ordering full chains post-recursion is simpler and less error-prone. Finally serialize each surviving chain's ids root-to-leaf as a JSON list of lists to `/home/user/failed_chains_page2.json`.
