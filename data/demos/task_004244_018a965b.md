Parse `edges.csv` first to determine `N` (max node id + 1 across both columns), then build a dense `N x N` matrix (e.g. `std::vector<std::vector<double>>`) and fill weights from each `source,target,weight` line, remembering this graph may be directed so only set `A[source][target]`, not symmetrize unless edges appear both ways in the file. Implement power iteration precisely as specified: start with a vector of all 1.0s, each iteration compute `A*v`, normalize by its L2 norm, compute the Rayleigh quotient `v^T A v` on the *normalized* vector as the eigenvalue estimate, and stop on `|lambda_new - lambda_old| < 1e-6` or 1000 iterations — implement the mat-vec, dot product, and norm manually with loops over `std::vector`, no external linear-algebra library. Format the final eigenvalue with exactly 4 decimal places (e.g. via `printf("%.4f", lambda)` or `std::fixed << std::setprecision(4)`) into `eigenvalue.log`. In the Makefile, compile with both `-O3` and `-pg` together (they can coexist) targeting the `analyze_network` binary. In `run_pipeline.sh`, call `make`, then `./analyze_network` (which must run in the same directory so `gmon.out` is produced there), then `gprof ./analyze_network gmon.out > /home/user/profile_results.txt`.