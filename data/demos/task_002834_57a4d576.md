Design the covering index to match the query shape: since queries filter on `rel_type` and `deleted` and need to read `source`/`target` without hitting the table, put the filter columns first in the index definition and include the projected columns after, e.g. `CREATE INDEX idx_edges_extract ON edges(rel_type, deleted, source, target);` — create it with `sqlite3` and also save the exact DDL you ran to `/home/user/index.sql`. For the extraction, write a SQL query joining `edges` to `nodes` twice (once for source as user, once for target as group), filtering `rel_type='member_of' AND deleted=0`, then group by user to build a sorted group list and count (centrality); exclude users with zero active memberships (they simply won't appear if you use an inner join and group-by). Use `sqlite3 -json` or manual formatting plus `jq`/`python3` to emit one JSON object per line with keys in the exact order `username, centrality, groups`, sort the groups array alphabetically per user, and sort the output lines alphabetically by username before writing to `/home/user/users_graph.jsonl`. Double check the "deleted" edges are excluded and that no trailing whitespace or extra blank lines end up in the file.
