First recover the binary format spec by running OCR against the image (e.g. `tesseract /app/format_spec.png -`), then re-read the raw output carefully — pay close attention to stated byte offsets, field widths, and declared endianness, since a common failure mode is misreading a multi-byte integer's byte order or off-by-one on a length-prefixed field. Cross-check your parsed field layout by writing a tiny synthetic DSAR file yourself (matching what you believe the header/record structure is) and confirming your parser reads it back sensibly before trusting the spec fully. Implement `/home/user/parser.py` using `mmap.mmap()` over the opened file descriptor (not plain `read()`), walk records sequentially updating a cursor, and for each record decode the path per the spec's exact byte layout. Enforce the security rule precisely: skip (print nothing for) any record whose path starts with `/` or contains the literal substring `../` anywhere — do this check on the decoded path string, not on raw bytes, and do it before printing. Match the required output line format character-for-character, and make the script executable with `chmod +x`. Handle truncated/malformed input defensively (e.g. a record's declared length running past EOF) without crashing.