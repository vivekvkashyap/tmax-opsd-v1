Work through the three phases independently and verify each before moving on. Phase 1: `mkdir -p /home/user/backups` then `tar -czf /home/user/backups/legacy_data.tar.gz -C /home/user legacy_data` (tar from the parent dir so the archive preserves the `legacy_data/...` prefix, which the verifier checks for). Phase 2: write `service_manager.sh` with a `case "$1" in start|stop|status)` structure — `start` should launch the app with `nohup`/background (`&`) and capture `$!` into `app.pid` immediately, `stop` should read that PID, `kill` it, then remove the pid file, and `status` should check both that the pid file exists and that `kill -0 $(cat pid)` succeeds (treat a stale pid file as "stopped"); after writing it, `chmod +x` and actually run `start`, then confirm with `curl localhost:8080`. Phase 3: in `migration_builder.py`, parse the CSV with the standard `csv` module, filter strictly on `role == "admin"`, and write shell lines in the exact literal format `useradd -m -G admin <username>` — don't add extra flags or reorder arguments, since the verifier greps for that exact string. Run the script, then `chmod +x create_admins.sh`.
