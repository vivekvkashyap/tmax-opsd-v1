Model this as a directed graph problem and solve it in well-separated stages, checking intermediate output at each stage rather than writing one monolithic pass. First parse `backup_metadata.json` and build edges as B→A whenever job A's `depends_on` list contains B (the edge direction is reversed from the list itself — get this backwards and everything downstream is wrong). Stage 1: run a standard cycle-detection algorithm (e.g. Tarjan's or Kosaraju's SCC, or simple DFS with recursion-stack tracking for smaller graphs) to find any SCC with more than one node; collect and alphabetically sort those job IDs as `cyclical_jobs`. Stage 2: build a filtered graph that drops those nodes and any edge touching them — print/log the remaining edge list to sanity-check by hand. Stage 3: compute out-degree per node on the filtered graph only, and pick the top 3 by (out-degree desc, job_id asc) — implement the tie-break explicitly, don't rely on map iteration order. Stage 4: find source nodes (in-degree 0) in the filtered DAG and run a longest-path DP (topological order, accumulate durations) from each source to find the maximum-duration path to any sink. Since the graph is confirmed acyclic after filtering, longest-path DP is safe. Write the final JSON with exact key names and array orderings as specified.