Build and validate the bucketer script in isolation before touching the video. Read stdin as raw bytes (sys.stdin.buffer.read()), split on b'\n', and for each line split on the *first* '|' only (e.g. `line.split(b'|', 1)`); drop the record if there's no '|' or the left part isn't a valid base-10 integer (wrap the int() parse in a try/except). Decode the right side with `.decode('utf-8', errors='replace')` so malformed bytes become U+FFFD, then count characters with `ord(c) > 127`. Bucket by `ts - (ts % 3600)` and accumulate counts into a dict, then print `json.dumps(dict, sort_keys=True)` — test this script directly with a few crafted stdin lines covering the drop cases (no pipe, bad int) and the replacement-char case before moving on. For the video, use `ffmpeg -i /app/telemetry.mp4 -map 0:s:0 /home/user/raw_logs.txt` to pull the subtitle stream (check exact syntax needed for the container's subtitle codec, and if raw extraction doesn't work directly try extracting as .srt first, then stripping SRT numbering/timestamp lines to leave only the payload text). Pipe the resulting log through your script into video_summary.json. Finally, install the cron job for user "user" with the exact literal command given (`crontab -e` or `crontab -l | { cat; echo "..."; } | crontab -`), matching the schedule and command string verbatim.
