Diagnose the 502 first: `curl -v http://127.0.0.1:8080/` and check Nginx's `error.log`, then open `/home/user/nginx/nginx.conf` and find the `proxy_pass` directive — it's pointing at the wrong upstream port, so change it to `http://127.0.0.1:8000;` (the port the app is supposed to run on per the task). For `/home/user/deploy.py`, parse `/home/user/fstab_config` line by line, skip comments/blank lines, split on whitespace, and for any line whose options field contains `bind`, take field 1 (source) and field 2 (dest), `os.makedirs(os.path.dirname(dest), exist_ok=True)`, then `os.symlink(source, dest)` — guard against the symlink already existing (remove/skip) so reruns don't crash. Then shell out to `pip install -r requirements.txt` inside `/home/user/app`, and launch `gunicorn --bind 127.0.0.1:8000 main:app` from that directory as a detached background subprocess (e.g. `subprocess.Popen` with its own process group, not a blocking call). Restart Nginx with the exact given command line (kill any existing instance using that pid file first, or use `-s reload`/`-s stop` beforehand). Finally run `deploy.py`, then `curl -s http://127.0.0.1:8080/data` and redirect its output into `/home/user/verification.log`, confirming both the symlink and the live curl path work end-to-end without sudo.