Build `get_network.sh` around two separate recursive CTEs run via `sqlite3 /home/user/company.db` inside the script, taking `$1` (employee_id), `$2` (limit), `$3` (offset) as positional args. First create indexes if missing: `CREATE INDEX IF NOT EXISTS idx_manager_emp ON manager(emp_id); CREATE INDEX IF NOT EXISTS idx_manager_mgr ON manager(manager_id);`. For the "up" chain, write a recursive CTE that starts at the given employee and walks up via `manager_id` until it hits a row with no manager (the CEO), then reverse the order (e.g. collect rows and print top-down, or use `ORDER BY` on recursion depth descending) and join `employees` for name/department, formatting as `Name (Dept) -> Name (Dept) -> ...`. For the "down" list, write a recursive CTE that starts at the employee and walks down via `emp_id` matching `manager_id` to collect all descendants, then in SQL apply `ORDER BY id ASC LIMIT $2 OFFSET $3`. Print `Chain: ...`, then a literal `Subordinates:` line, then the paginated rows as `ID | Name | Department`. Use `sqlite3 -separator` or manual formatting to avoid stray whitespace, and `chmod +x` the script.
