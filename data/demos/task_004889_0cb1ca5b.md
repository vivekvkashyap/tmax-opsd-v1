This is a two-part fix: derive the closed-form antiderivative analytically, then improve the numerical scheme — don't just crank up N on the existing broken Riemann sum without also fixing its structure. For $\int 1/(x^2+\alpha^2)\,dx$, recall the standard result $\frac{1}{\alpha}\arctan(x/\alpha) + C$; evaluate at the bounds $x=\pm1$ and combine (note the function is even, so this simplifies nicely) to get the analytical constant — compute it in your C code with `atan()` from `<math.h>`, not a hardcoded literal, so it's derived from `alpha` at runtime. For the numerical part, switch from the left-Riemann-sum to the trapezoidal rule (average the two endpoint values of each interval, or equivalently weight interior points by 1 and endpoints by 0.5) with `N=10000000` steps and `double` precision throughout — a naive Riemann sum systematically misses the sharp peak near x=0 even with huge N because of how sample points align, so getting the trapezoidal weighting right matters more than just increasing N. Compute the absolute error as `fabs(analytical - numerical)`. Print all three values with 6 decimal places (`%.6f`) into validation.log matching the exact three-line label format given, then compile with a standard C compiler and run it to actually produce the file.