Phase 1: use shell text tools (grep/awk/sed) to filter `/home/user/raw_latency.log` to lines matching both `Status: 200` and `Endpoint: /api/v1/data`, extract just the numeric value preceding `ms` in the `Latency:` field (e.g. a regex like `Latency: ([0-9.]+)ms`), and write one number per line to `/home/user/clean_latency.csv` — verify the line count looks sane before proceeding, since a later assertion checks it. Phase 2: implement Metropolis-Hastings from scratch in `/home/user/fit_mcmc.py` exactly per spec — init (mu1,mu2)=(25,100), propose each independently from N(current, 2.0^2), compute log-likelihood as the log of the mixture density `0.7*N(x|mu1,5^2)+0.3*N(x|mu2,10^2)` summed over data, accept/reject via the standard MH ratio (respecting the uniform priors' support bounds — reject immediately if a proposal falls outside [0,50] or [50,150]), run 5000 iterations, discard the first 1000 as burn-in, and average the remaining 4000 samples per parameter. Phase 3: draw 10,000 GMM samples from your fitted mu1/mu2 with the known weights/sigmas, and compute `scipy.stats.wasserstein_distance` against the cleaned empirical data. Write all four required keys to `/home/user/profiling_results.json` and run the script.
