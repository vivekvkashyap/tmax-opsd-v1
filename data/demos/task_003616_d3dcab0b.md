Do black-box and static analysis together: run `/app/graph_oracle` on a few small hand-crafted graph files to learn the exact input/output format (node/edge line syntax, the `MAX_PATH: <int>` output line), and read `/app/parser_fix.patch` carefully — it reveals that lines starting with `#` must be skipped as comments, and likely other whitespace/blank-line edge cases, which is easy to miss if you only look at the oracle's happy-path behavior. Implement the core algorithm yourself in C/C++: parse `N <id> <weight>` and `E <src> <dst>` lines (ignoring `#` comments and blank lines), build an adjacency list plus in-degree counts, run a topological sort (Kahn's algorithm), and compute `dp[node] = weight[node] + max(dp[predecessor] for predecessor in incoming edges, default 0)`, taking the max `dp` value over all nodes as the critical path weight. Expose it as `extern "C" long calculate_critical_path(const char* graph_data)` compiled into `/home/user/libgraph.so` (`-shared -fPIC`), then write `/home/user/cli.py` using `ctypes.CDLL` to load it, read the input file's full contents, call the function, and print `MAX_PATH: <value>` matching the oracle's exact formatting. Before trusting it, diff your CLI's output against the oracle's on several test files (including ones with comments and edge cases) to catch format mismatches early, then run `/app/bench.py` to confirm performance is acceptable.
