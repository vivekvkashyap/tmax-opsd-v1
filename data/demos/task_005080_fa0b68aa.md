Treat this as a producer/consumer streaming problem, not a batch script: start `/app/start_services.sh` first, then run concurrent workers (threads, asyncio tasks, or separate processes) so slow parts don't stall fast ones. For the active file, follow it like `tail -f` (seek to end, poll for new bytes, only treat a line as complete once it ends in `\n` to avoid ever chunking a partial line), buffering exactly 500 complete lines before flushing a `chunk_NNNN.md` file. For the legacy archiver, poll `/home/user/legacy_docs/` for new `.tar.gz` files, and validate each with `tarfile.is_tarfile()` / a wrapped `tarfile.open().getmembers()` try/except before extracting, silently skipping ones that raise (the corrupted ones) rather than crashing the whole pipeline. Keep the chunk counter global/shared across both phases so numbering continues sequentially. As soon as a chunk file is finalized, compute its SHA256, append `hash  filename` to the manifest, and immediately open a TCP socket to localhost:9000 and send its lines (reconnect per chunk or keep a persistent connection, but always flush) — do this incrementally throughout the 3-minute window rather than waiting until the end, since the goal is maximizing lines successfully forwarded before generators stop.
