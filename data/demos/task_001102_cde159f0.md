Build this as three independently-runnable processes wired through Redis, not one monolith. Start with `npm init`/`npm install express redis` and `pip install scipy redis` in `/app/`, then bring up `redis-server`, `node api.js`, and `python3 worker.py` as background processes (verify each is actually listening/connected before moving on). In `worker.py`, loop popping from the `jobs` list, count amino acids to get k1/k2, and integrate the two-compartment ODE with `scipy.integrate.solve_ivp` over `t=[0,50]` with `np.linspace(0,50,1000)` as `t_eval`, then take `max(B)` as peak_B and push the JSON result to `results`. In `api.js`, add middleware that rejects requests unless the exact `Authorization: Bearer bio-secret-2024` header is present (401 otherwise). For reproducibility, don't sum results as they arrive — collect all of them into a list first, `sort` that list alphabetically by the `sequence` field, then do a single sequential `reduce`/for-loop sum over `peak_B` in that fixed order so floating-point rounding is deterministic. Compute simple linear regression (least squares) of `peak_B` on `length` for slope/intercept, write the (length, peak_B, fitted line) data out and invoke `plot.py` via `child_process` before responding, and confirm it writes `/home/user/plot.png`. Leave all three processes running in the background at the end.