Work top-down through the pipeline: build, then config, then runtime crash, then test. Run `make` in `/home/user/telemetryd`, read the linker error message carefully (undefined reference to a pthread/math symbol is typical), and add the missing library flag (e.g. `-lpthread`) to the Makefile's link line rather than restructuring the sources. For configuration, don't guess the port/token — run OCR against the note image, e.g. `tesseract /app/server_notes.png -`, and read off the exact `PORT` and `AUTH_TOKEN` strings it prints (try `--psm` variants if the first pass mangles characters), then write them verbatim into `config.ini`. For the crash, start the server, send one request, and let it core-dump or attach live with `gdb ./telemetryd`; run `bt` (backtrace) at the fault to find the exact function and line, which will point to a pointer that's read before being validated/initialized in the request-parsing path — add a null/bounds check or proper initialization there, don't just silence the symptom, then rebuild. Write `test_regression.sh` using `curl -s -o /dev/null -w '%{http_code}'` against `/health` with the `Authorization: Bearer <token>` header, checking for `200`. Finally leave the server running in the background on the recovered port.
