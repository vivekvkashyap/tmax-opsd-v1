Write the parser as a sequence of independent regexes rather than one giant pattern, and test each against every sample line in the log before combining. Match the two timestamp formats separately (bracketed `DD/MMM/YYYY:HH:MM:SS +0000` vs ISO `YYYY-MM-DDTHH:MM:SSZ`) and convert both to the same normalized ISO output using `datetime.strptime`/`strftime`, not string surgery. Extract the quoted request line with a regex like `"(\w+) (\S+)"`, split off any `?query` from the path yourself, then lowercase the path. Pull status/bytes as separate integer groups and the IP after `IP: `. For PII, search the whole line (not just designated fields) for email/CC/SSN patterns since they float anywhere in the text; apply masking exactly as specified — first char + five literal asterisks for email, keep hyphens for CC/SSN masks, and only mask, never drop, the surrounding digits/hyphens structure. Process line-by-line, build one dict per line matching the required key set, and write each as a JSON object using `json.dumps` followed by a newline (JSON Lines, not a JSON array) to the output path.
