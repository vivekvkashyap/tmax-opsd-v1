Write a single Go program that performs four independent, precisely-scoped checks — don't over-generalize the matching logic, since exact substring/regex matches are what's graded. (1) Parse `rbac.json` with `encoding/json` into a struct with `name` and `can_sudo` fields, then filter for `can_sudo == true && name != "admin"` (exact string comparison, not case-insensitive) and write matching names one per line. (2) Walk the app directory recursively (`filepath.Walk` or `filepath.WalkDir`), and for each `.go` file check if its raw contents contain the literal substring `fmt.Sprintf("SELECT` (not a regex, just `strings.Contains`), and for each `.html` file check for the literal substring `{{.`; write full absolute paths for matches to the respective log files. (3) For secrets redaction, tokenize each line's whitespace-separated fields (or use a regex like `AKIA\S+`) and replace any token starting exactly with `AKIA` with the literal string `[REDACTED]`, preserving everything else in the file exactly, then write to `secrets_clean.txt`. (4) Read threat IPs line by line and emit a bash script starting with `#!/bin/bash` followed by one `iptables -A INPUT -s <IP> -j DROP` line per IP, then `os.Chmod` it to 0755. Compile and run the Go binary yourself to actually produce all four output artifacts before finishing.