Treat this as a build-config bug plus an algorithmic-complexity bug, both required to meet the SLA. First inspect the `Makefile` in `/app/graph_backup_validator-1.0.0/` for compiler flags — an unoptimized/debug flag set will dominate runtime even before the algorithm fix, so switch it to a proper optimization level. Second, open `src/query.c`'s `find_edges_from_node` (or equivalent) and confirm it does a full linear scan over the edge array per lookup; since the caller/loader sorts edges by `src_id`, replace the scan with `bsearch` (or a hand-written binary search) to locate any matching edge, then walk backward and forward from that hit to capture the full contiguous run sharing that `src_id` (don't assume `bsearch` lands exactly on the first match). Recompile with `make` after both fixes, then run the exact validator command specified with the given roots and depth, redirecting stdout to the report CSV — check the output format matches the required header/columns exactly. Finally time the run yourself (e.g. `time ./validator ...`) before trusting the automated check, since the SLA threshold is strict.
