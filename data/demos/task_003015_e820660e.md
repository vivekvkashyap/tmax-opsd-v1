Work through the four defects independently, verifying each in isolation before combining into daemon_fixed.sh. (1) Makefile: run `make` in the helper directory, read the actual linker error, and add the missing math-library link flag (`-lm`) to the compile/link line — linker order matters, `-lm` must come after the object/source file on the command line. Rebuild and confirm `process_data` exists and runs. (2) Binary auth: don't guess the env var — use `strings legacy_submitter` to spot candidate variable/value literals, and/or `ltrace`/`strace` while running it with various env vars set to observe which `getenv()` call it makes and what value it compares against; once you find the exact variable name and expected value, export it in the script immediately before the call to legacy_submitter. (3) Memory leak: search daemon.sh for a variable inside the `while` loop that uses `+=` (string/array append) — change that single append to a plain assignment `=` so it holds only the latest entry instead of growing forever. (4) Arithmetic: Bash `$(( ))` follows normal operator precedence, so `VAL1 + VAL2 / 2` divides before adding — wrap the sum in parentheses: `(VAL1 + VAL2) / 2`. Save the corrected script as daemon_fixed.sh and `chmod +x` it.