Treat this as four independent bugs to fix in order, testing each stage's output before moving to the next. (1) Recovery: mount/inspect fs.img read-only with debugfs — use `debugfs -R "ls -d" /home/user/fs.img` to find the deleted process_log.awk's inode number (deleted entries still show, often with inode 0 in the listing but recoverable via a deleted-inode scan), then `debugfs -R "dump <inode> /home/user/pipeline/process_log.awk" fs.img` to extract its bytes; verify the recovered file looks like valid awk before trusting it. (2) parse_timestamps.sh: reproduce the crash by feeding it real ffprobe output containing timestamps like `1.2` and `1.234` (differing decimal precision) and missing tags — the bug is brittle positional/column-count parsing that assumes fixed width; fix by parsing fields by key (e.g. splitting on `=`) with an explicit default when a tag is absent, not by hardcoded character positions. (3) calibrate.sh: this is a binary-search convergence bug — when `mid=(low+high)/2` and both `low=mid` and `high=mid` are used without `+1`/`-1` adjustments, the loop can get stuck oscillating between two adjacent values forever; fix the bound updates (`low=mid+1` or `high=mid-1` depending on branch) and add a `low<high` (or similar) termination guard. (4) aggregate_queries.sh: the JOIN condition only bounds by timestamp range without matching stream_id, producing a cross-join across streams — add the missing `f.stream_id = e.stream_id` predicate. Run run_all.sh only after all four are individually verified, and sanity-check final_output.csv's row count and value ranges look reasonable before finishing.