Do the symlink audit first, since deleting bad links changes what later steps see. To detect broken vs cyclic symlinks without hanging, walk the tree yourself (don't blindly follow links): for each symlink found via `find /home/user/project_root -type l`, test with `test -e "$link"` (which follows the chain and fails/times out on cycles) — for suspected cycles, resolve with a bounded-iteration loop using `readlink` rather than an unbounded `os.path.realpath`/`readlink -f`, which can loop forever on a real cycle; alternatively catch cycles by tracking visited paths in your own resolution loop. Log absolute paths (sorted) to `/home/user/bad_links.txt`, then `rm` those links. Second, find `*.conf` files under the tree and use `sed -i 's/^PASSWORD=.*/PASSWORD=REDACTED/' file` (or equivalent Python) on each. Third, fix `backup_script.py`: change `os.walk(..., followlinks=True)` to `followlinks=False` (the default), and inside the walk explicitly separate `files` into regular files (add to tar) versus valid symlinks (record path to a list, don't add to tar) — check `os.path.islink()` and `os.path.exists()` per entry. Write the recorded symlink paths (absolute, sorted) to `/home/user/skipped_symlinks.log`. Finally execute the fixed script.
