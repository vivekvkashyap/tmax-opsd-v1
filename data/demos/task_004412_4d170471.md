Build this as a classic extract-dedupe-compile shell pipeline: (1) use `grep`/`sed` or `awk` with a regex to pull `TIMESTAMP`, `Key`, `Lang`, `Value` from every line matching the "Extracted translation" pattern across all `*.log` files in `/home/user/raw_logs/`, ignoring non-matching lines; (2) for conflict resolution, sort all extracted records by `Key`, `Lang`, then `TIMESTAMP` ascending, and for each `Key|Lang` group keep only the last (most recent timestamp) row — `sort -t'|' -k1,1 -k2,2 -k3,3` followed by `awk` tracking the previous key/lang and overwriting on match, or grouping with `awk` arrays keyed by `Key_Lang` that only update when a newer timestamp is seen, works well; (3) emit the final CSV sorted by Key then Lang with a `Key,Lang,Value` header, watching out for commas/values that need no special escaping given the fixture; (4) separately count occurrences per `Key_Lang`, and for every combo appearing more than once, compute `printf '%s' "<Key>_<Lang>" | md5sum` (careful: no trailing newline in the hashed string) and write the sorted hash list to `duplicate_report.txt`. Use `mkdir -p /home/user/locales` before writing outputs, then actually run the script to produce both files.