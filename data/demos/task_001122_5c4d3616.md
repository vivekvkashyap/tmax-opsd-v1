Work through the four stages in order since later stages depend on earlier ones building cleanly. First, fix the Makefile: shared objects require position-independent code, so the compile step needs `-fPIC` and the link step needs `-shared -o libtextverify.so`; run `make` and confirm the .so is produced before moving on. Next inspect verify.c for unbounded copies into fixed-size stack buffers (a classic strcpy-into-char[N] overflow) — replace it with a length-checked copy (e.g. strncpy with an explicit bound and null-termination, or an strlen check that rejects oversized input) so it degrades to a safe rejection instead of corrupting the stack; test with a >128-byte input to confirm no crash. Then write the .proto with the TextVerifier service/Verify RPC and VerifyRequest/VerifyResponse messages, and generate Go bindings with protoc + protoc-gen-go/protoc-gen-go-grpc. Implement the Go server using cgo (`#cgo LDFLAGS`, `import "C"`) to call `is_valid_text`, listening on 127.0.0.1:50051. Finally build the scanner CLI that walks a directory, calls Verify per file, and prints ACCEPTED/REJECTED per filename. Start the server in the background before testing the scanner.