Treat this as building an adversarial input filter that must generalize beyond the samples you're given, not just pass the local files. Start by probing the black-box oracle binary with small, deliberately crafted inputs (valid UTF-8, invalid continuation bytes, embedded NUL, stray control bytes) to confirm exactly which byte patterns crash it — use a subprocess wrapper or shell loop and check the exit status/signal each time. Then write `/home/user/sanitizer.cpp` that reads the target file as raw bytes (not as a C string, since NUL bytes must be detectable) and performs two checks: (1) a proper UTF-8 validator that walks multi-byte sequences and rejects truncated/invalid continuation bytes — don't just check `isascii`; and (2) a scan rejecting any byte in 0x00-0x1F except \t \n \r. Print exactly `CLEAN`/exit 0 or `REJECT`/exit 1 per the spec. Compile to `/home/user/sanitizer` and validate against both your own probes and the provided `/home/user/samples/` files before considering it done, since the hidden test corpus will include many more variants of the same failure classes.
