Work through each broken layer in order and test it before moving to the next, since later steps depend on earlier ones. First fix the symlink: `rm` the existing broken link and `ln -s /home/user/app/releases/v1 /home/user/app/current` (verify with `readlink -f`). Second, in nginx.conf change `proxy_pass http://localhost:8080;` to `proxy_pass http://127.0.0.1:8080;` — this is the classic IPv6-vs-IPv4 loopback resolution bug when `localhost` resolves to `::1` first but the backend only binds IPv4. Third, generate the cert with `openssl req -x509 -newkey rsa:2048 -nodes -keyout /home/user/key.pem -out /home/user/cert.pem -days 365 -subj "/CN=localhost"`, then uncomment and set `ssl_certificate /home/user/cert.pem;` and `ssl_certificate_key /home/user/key.pem;` in the server block exactly. Start the Python backend in the background first, confirm it's listening (e.g. `curl 127.0.0.1:8080`) before starting nginx with `nginx -c /home/user/nginx.conf`, then confirm nginx itself answers before writing the health monitor. In `health_monitor.py`, use `requests.get(..., verify=False)` (or `urllib` with an unverified SSL context) to avoid self-signed cert errors, check `status_code == 200`, and append exactly `STATUS: UP\n` or `STATUS: DOWN\n` — run it once at the end.
