Write the C program using the SQLite C API (`sqlite3_open`, `sqlite3_prepare_v2`, `sqlite3_step`) with a query that avoids the index — e.g. `SELECT id, source, destination, power, timestamp FROM signals` with no ORDER BY, then do all filtering/joining/sorting in C rather than relying on SQL to use the index. Load all rows into memory, then for relay-path detection do a nested-loop join: for each row A->B with power > 50.0, scan for a row B->C (where the first row's destination equals the second row's source) also with power > 50.0, and check `0 < timestamp2 - timestamp1 <= 100` (get the strict-inequality boundaries right — the difference must be positive and at most 100, not less-than-or-equal on the lower bound). path_power is the min of the two powers. Emit CSV rows with the header `start,relay,end,path_power`, formatting path_power with exactly one decimal via `%.1f`. Sort the collected results by path_power descending, breaking ties by `start` ascending (stable sort or explicit comparator, not insertion order) before writing the file. Compile with `gcc /home/user/extractor.c -o /home/user/extractor -lsqlite3` and run it, then spot-check the CSV against the raw signals table to sanity check the relay logic.