Use an in-memory/embedded SQLite (e.g. `github.com/mattn/go-sqlite3` with a `:memory:` DSN, requiring `CGO_ENABLED=1`) so you can express the ranking logic declaratively rather than hand-rolling grouping in Go: create `nodes(id, category)` and `edges(src, dst, weight)` tables, `LOAD DATA` (bulk insert) from the parsed CSVs using `encoding/csv`, then run a single SQL query that first aggregates `SUM(weight) AS in_weight` grouped by `dst` joined to `nodes` for `category`, and wraps that in a `RANK() OVER (PARTITION BY category ORDER BY in_weight DESC) AS rnk` window function, filtering the outer query to `rnk <= 2`. Double check tie-breaking behavior of `RANK()` vs `ROW_NUMBER()` matches what's expected for nodes with equal in_weight. Scan the query results into Go structs, sort by `(category ASC, rank ASC)` explicitly before marshaling (don't rely on SQL ORDER BY alone surviving into JSON iteration), and marshal to the exact `{"top_nodes":[...]}` schema with `encoding/json`, writing to `/home/user/output.json`. Run it with `go run /home/user/etl.go` to confirm it executes end-to-end and produces valid JSON.