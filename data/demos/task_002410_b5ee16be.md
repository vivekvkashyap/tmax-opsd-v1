First filter the noisy log with a precise shell pipeline: use `grep '^VALIDATE: '` (or `awk`/`sed` anchored to the exact prefix) on `/home/user/artifact_log.txt` and strip the prefix to get just filenames, one per line, into `/home/user/target_files.txt` — an unanchored match would wrongly catch lines that merely mention a filename (e.g. an ERROR line referencing a different, invalid file), so anchor strictly on lines that start with `VALIDATE: `. Then `cargo new /home/user/elf_inspector`, add the `goblin` crate, and write a small CLI that opens the ELF file given as argv[1], parses its header via `goblin::elf::Elf::parse`, and prints one JSON line: `{"file": "<basename>", "machine": <e_machine as integer>, "entry": <e_entry as base-10 integer>}` — remember e_entry from goblin is already numeric, don't accidentally format it in hex. Finally loop over `/home/user/target_files.txt` in bash, run your Rust binary (or `cargo run --release --quiet --`) against each corresponding file in `/home/user/artifacts/`, and append each JSON line to `/home/user/final_report.jsonl`, preserving log order.