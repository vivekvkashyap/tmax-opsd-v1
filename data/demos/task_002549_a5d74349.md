The bug is that the query has no join conditions at all (a Cartesian product), so fix it as a proper graph-cycle self-join rather than patching around the symptom. Alias the transactions table three times (t1, t2, t3) representing consecutive edges in the 3-cycle, and add explicit join predicates chaining them: t1.receiver_id = t2.sender_id, t2.receiver_id = t3.sender_id, and t3.receiver_id = t1.sender_id (closing the loop back to the start), plus an amount filter `> ?` on all three legs using a parameterized placeholder (never string-format the threshold into the SQL). Select `DISTINCT t1.sender_id`. Also guard against degenerate "cycles" that reuse the same entity twice if the schema/data could produce them — add inequality checks between the three sender ids as needed to keep true distinct triangles. For indexing, run `CREATE INDEX idx_tx_sender ON transactions(sender_id);` and `CREATE INDEX idx_tx_receiver ON transactions(receiver_id);` before running the query so the join can use index lookups instead of full scans. Execute the fixed script with threshold 10000, and write the resulting distinct sender ids to the CSV one per line, sorted ascending.
