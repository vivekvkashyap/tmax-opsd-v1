This is a three-condition AND filter — implement and check each condition independently before combining them. (1) Certificate validity: for each `client_cert_file`, verify it against the CA chain using `openssl verify -CAfile /home/user/ca-chain.pem <cert>` via `subprocess`, checking the exit code / "OK" output rather than assuming validity; a cert signed by a different (rogue) CA must fail this check even though it looks well-formed. (2) Malicious payload: search the `payload` string for the exact substrings `<script>` or `' OR ` (note the trailing space in the injection substring — get this exactly right, since a too-loose or too-strict substring match will misclassify borderline entries). (3) Successful auth: `response_code == 200`. A request is "compromised" only if all three hold simultaneously — walk through each log entry by hand against these three checks to sanity-check your script's classification logic before running it broadly. Write only the qualifying `req_id`s, one per line, sorted ascending, to `/home/user/compromised_sessions.txt`, with no extra text.