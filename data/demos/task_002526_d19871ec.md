Break this into independent, testable stages. First extract frames from the video (ffmpeg or OpenCV/Python) and compute each frame's mean grayscale intensity in [0,1], multiply by 1000 and round to get the raw integer per frame — verify frame ordering starts at 1 and matches file/temporal order. Second, build a Bash/awk cleaning pipeline: any raw value < 10 becomes missing, then impute by averaging the nearest valid frame before and after (not simply forward/backward fill), falling back to the single nearest valid frame if missing at the very start or end — write this as clear, testable awk/bash logic rather than eyeballing it, and print the cleaned CSV to confirm imputed values by hand against the surrounding valid points. Third, write the bootstrap script in `gawk` (not mawk/awk) with `srand(42)` called once before the resampling loop, since implementation and seed placement change every subsequent `rand()` value — do exactly 10,000 resamples of size n with replacement, collect the 10,000 resample means, sort them, and take mean/2.5th/97.5th percentile with `%.2f` formatting. Finally, wire up the TCP server (socat/nc + bash) on 127.0.0.1:9090 that dispatches on the literal request strings (`SCHEMA\n`, `DATA <n>\n`, `STATS\n`), re-running or caching the cleaned data and bootstrap result rather than recomputing per connection if that risks non-determinism, and test each command manually with `nc` before finishing.
