Reverse engineer the salt first, since everything downstream depends on it: use `strings /home/user/legacy_auth | less` (or grep for plausible salt-looking tokens) to find the hardcoded salt string embedded in the binary's data section — don't guess, confirm it's a string literal near SHA256-related symbols. Once you have the salt, write rotate_cred.cpp to compute SHA256 of `password + salt` using OpenSSL's `SHA256_Init/Update/Final` (or the EVP API), hex-encode the digest, and overwrite /home/user/auth_config.txt with it. For process isolation, use `pipe()` twice (stdin and stdout pipes to the child), `fork()`, and in the child `dup2` the pipe ends onto fd 0/1, then call `execve(path, argv, envp)` where `envp` is an array containing only a single `NULL` element (not `environ`, not partially cleared) — this is the detail naive attempts get wrong, since even inheriting the process's own environment or forgetting to null-terminate correctly will make the anti-tamper check reject the login. In the parent, write the new password to the child's stdin, close it, then read the child's stdout fully before waiting on the child. Log SALT, HASH, and RESULT (the exact captured stdout) to rotation_log.txt in the specified format, and actually run rotate_cred to confirm "Access Granted" appears rather than assuming the pipe/exec plumbing is correct.
