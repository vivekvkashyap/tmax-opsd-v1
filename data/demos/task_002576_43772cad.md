Work in order: OCR the image first, then wire the compile/run chain. Run `tesseract /app/system_spec.png stdout` (or output to a file) and read the raw text to find the two labeled values; do not guess or hardcode them. Insert the extracted `MATH_MAGIC_CONSTANT` into `src/constants.rs` as `pub const MATH_MAGIC_CONSTANT: f64 = <value>;` so `main.rs`'s `constants::MATH_MAGIC_CONSTANT` reference resolves, then `cargo build --release` inside `/home/user/math_engine`. For the Go server, use `net/http`, parse the JSON body into a struct with a `value int` field, check the `Authorization` header equals `Bearer <extracted-token>` before doing any work (return 401 immediately if not), then in a goroutine invoke `exec.Command` on the compiled Rust binary with the value as a string argument, capture stdout, parse it as float64, and marshal `{"computed": <float>}`. Start the server with `go run` or a built binary backgrounded (e.g. `nohup ... &`), then verify with `curl` and `ss -ltnp` that port 8080 is listening before finishing. Watch for stale/incorrect OCR text (verify by eye) and for forgetting the trailing newline handling when parsing the Rust binary's stdout.