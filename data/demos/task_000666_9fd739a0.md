Build this as a single streaming pipeline, not a script that writes intermediate files. Use `find /home/user/docs_raw -type f \( -name '*.md' -o -name '*.txt' \) | sort` to get a deterministic, alphabetically-sorted-by-full-path file list, then loop over that list (e.g. via `while IFS= read -r f`) and for each file emit the header line `===FILE: $(basename "$f")===` followed by the file's content through a stream editor that performs the exact string substitution (`sed 's/Copyright 2018 Acme Corp/Copyright 2024 Acme Global/'`, careful to preserve any trailing text on that line like "All rights reserved." — only replace the matched substring, not the whole line). Ensure each file's output ends with a newline before the next header (e.g. append one unconditionally with `printf`, or check with `tail -c1`) so headers don't get glued onto content. Pipe this entire concatenated stream directly into `gzip > /home/user/archive/docs_custom.gz` inside one shell pipeline — never stage the combined text into a temp file. Make the script executable with `chmod +x`, run it once, and self-check by `zcat`-ing the archive and diffing it against what you expect from the header/substitution rules, confirming order matches the sorted full-path order.