Handle this as two independent fixes plus a pipeline. First, diagnose the vendored `annoy` build failure by reading `/app/annoy-1.17.3/setup.py` directly — check whether `ext_modules` is empty or the C-extension entry has been stripped/commented out; restore the proper `ext_modules=[Extension(...)]` definition (compare against what a normal annoy 1.17.3 setup.py declares) so the compiled C++ extension actually gets built, then `pip install -e .` from that directory and confirm with `python3 -c "from annoy import AnnoyIndex; AnnoyIndex(128,'angular')"` before moving on — don't assume install succeeded just because pip printed no error. Second, build the test-set Annoy index (`metric='angular'`, load each row's 128-d vector by ID, `build()`, then `save('/home/user/test_index.ann')`). Third, in detector.py, for each row in each input CSV, load the index once (not per-row) and use `get_nns_by_vector(vec, k, include_distances=True)` with a small k, then flag a leak if any returned angular distance is `< 0.05`; mark a whole file `false` the moment any row leaks, else `true`, and write the full path→bool JSON at the end.
