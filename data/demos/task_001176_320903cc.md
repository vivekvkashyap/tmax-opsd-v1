Build this as a short, testable pipeline rather than one big script. Parse the FASTA by reading header/sequence line pairs (or use Biopython's SeqIO if installed) and bucket each record into GroupA or GroupB based on whether the header contains that substring. For each sequence, map every residue through the given 1-based alphabetical index table (build the dict once: sorted the 20 standard amino acid letters, enumerate from 1) — do not hardcode any other ordering. Compute the length-3 "valid" moving average manually as `[(x[i]+x[i+1]+x[i+2])/3 for i in range(len(x)-2)]` (equivalent to `numpy.convolve(x, [1/3,1/3,1/3], mode='valid')`), take the max of that smoothed array per sequence, and collect those maxima into two lists, one per group. Compute the 1st Wasserstein distance between the two empirical distributions with `scipy.stats.wasserstein_distance(group_a_maxima, group_b_maxima)` (do not implement your own EMD unless scipy is unavailable — order of arguments doesn't matter since it's symmetric). Format the result with exactly 4 decimal places (`f"{d:.4f}"`) and write only that string (no extra whitespace/newline issues that would break a strict-equality check) to /home/user/wasserstein.txt. Double check by re-deriving the value with an independent second script before finishing.
