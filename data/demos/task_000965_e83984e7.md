The failure is that `build.rs` never compiles the C source, so the FFI symbol is unresolved at link time. Fix it with the `cc` crate: in `build.rs`'s `main`, call `cc::Build::new().file("src/fast_chk.c").compile("fast_chk");`. Confirm `cc` is listed under `[build-dependencies]` in `Cargo.toml` — if the crate isn't declared, the build script can't `use` it. The library name you pass to `.compile()` should be `fast_chk` as specified. Do not touch the C algorithm or the Rust reference impl in the proptest; the point is only to make them link and agree.

Then create `.github/workflows/ci.yml` with `on: push`, a job `runs-on: ubuntu-latest`, a checkout step, and a step running `cargo test`.

Finally verify locally: `cd /home/user/sec-payload-verifier && cargo test 2>&1 | tee /home/user/test_results.log`. Redirect BOTH stdout and stderr so the log captures the real result line. Confirm the log shows a passing run before finishing.
