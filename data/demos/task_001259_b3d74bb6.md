Read `time`/`signal` from the HDF5 file with h5py, compute `np.fft.rfft(signal)` and `np.fft.rfftfreq(len(signal), d=dt)` (derive dt from the time array's spacing), take the magnitude to get the amplitude spectrum, then mask to keep only 1.0<=f<=4.0 Hz. Define the model B - A*exp(-(f-f0)^2/(2*sigma^2)) and a Gaussian log-likelihood (sum of squared residuals over an assumed noise sigma, plus flat log-prior returning -inf outside the given uniform bounds). Use `emcee.EnsembleSampler` with >=4 walkers and pass `pool=` a `multiprocessing.Pool` (or `concurrent.futures.ProcessPoolExecutor`) as the `pool` argument so chains truly run in parallel — a common mistake is running emcee without a pool, which is not real parallelism. Initialize walkers as small Gaussian perturbations around a reasonable guess (e.g. baseline near max amplitude, f0 near the visible dip). Run enough steps, discard an initial burn-in fraction, then average the flattened posterior samples for f0 and sigma. Create `/home/user/results/` if missing and dump `{"f_0": ..., "sigma": ...}` to the exact output path.