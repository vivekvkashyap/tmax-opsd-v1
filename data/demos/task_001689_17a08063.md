For IP extraction, filter lines containing "Failed password" and pull the IPv4 token out with a targeted regex (e.g. `grep 'Failed password' syslog.txt | grep -oE '([0-9]{1,3}\.){3}[0-9]{1,3}'`), then `sort -u` before writing to `/home/user/blocked_ips.txt` — don't accidentally also match port numbers or other digit sequences. For redaction, use a range-based stream edit rather than trying to match a multi-line regex in one pass: `sed '/-----BEGIN OPENSSH PRIVATE KEY-----/,/-----END OPENSSH PRIVATE KEY-----/c\[REDACTED]'` collapses each full block (both marker lines and everything between) into a single `[REDACTED]` line while leaving all other lines untouched — verify this against the actual file since there may be multiple separate key blocks and surrounding context (like trailing `" > /tmp/key` on the same logical entry) that must be preserved outside the block. Redirect output to `/home/user/syslog_redacted.txt`, never modify the original file, and diff your outputs against your own manual trace of the log before finishing.
