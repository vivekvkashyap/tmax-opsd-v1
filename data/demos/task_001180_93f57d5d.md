Treat this as two unrelated sub-tasks and solve/test them independently. Part 1: fix `start_services.sh` step by step — start `redis-server` bound to port 6379 (verify with `redis-cli -p 6379 ping`), start the Flask API with its listen port set to 5050 via whatever env var / `app.run(port=...)` the code expects, and launch the MPI worker (`mpirun`/`mpiexec`) with its Redis connection string pointed at `localhost:6379` — grep `api.py`/`worker.py` for how they currently read connection config (hardcoded vs env var) since that's likely also broken, not just the shell script. Test each service starts cleanly on its own before chaining them, then confirm end-to-end with the exact curl command in the task and tail `worker.log` for the success line. Part 2 is unrelated: build the classifier by loading each CSV's `dt` column, computing a density estimate (`scipy.stats.gaussian_kde` or `numpy.histogram`), and thresholding on the presence of significant density mass below a small cutoff (the task states values below ~1e-6 indicate divergence) — pick your threshold by actually inspecting the clean vs evil corpora's dt distributions empirically (e.g. min/percentiles per file) rather than hardcoding a number blind, since real separability must be verified for 100% accuracy on the given corpus. Save the KDE plot with matplotlib for every invocation.