Extract the config from the image before writing any C++: run `tesseract /app/policy_config.png stdout` and read off the three labeled values (`REQUIRED_COOKIE`, `REDACT_WORD`, `AES_KEY`) directly from the OCR text — don't guess formatting, and re-run/crop/preprocess (e.g. `convert` to grayscale or upscale) if the OCR output looks garbled, checking it visually against what a cookie/codename/hex-key should look like. Build the C++ server as a minimal raw-socket HTTP listener (`socket`/`bind`/`listen`/`accept` on 127.0.0.1:8080, or a small embedded library) that reads until it sees `\r\n\r\n` to separate headers from body, then uses `Content-Length` to read exactly that many further bytes for the POST body — a common bug is stopping at the header boundary and dropping/truncating the body. Parse the `Cookie:` header by exact substring/equality match against the OCR'd cookie string, returning `403` on mismatch. For redaction use straightforward string replace-all of the extracted codename with `[REDACTED]`. For encryption, use OpenSSL's EVP AES-256-CBC API with the OCR'd 32-hex-char key (convert hex to 16 raw bytes — note AES-256 needs a 32-byte key, so double check whether the hex string decodes to 16 or 32 bytes and handle accordingly) and a 16-null-byte IV, then base64-encode the ciphertext with `EVP_EncodeBlock` or similar before writing it as the response body. Compile with `-lcrypto`, run the binary in the background, and manually curl it with a POST to sanity-check both the 403 and 200 paths before trusting it.
