Before writing any Go code, fix the vendored dependency: inspect `/app/vendor/github.com/buger/jsonparser/Makefile` for cross-compilation overrides (e.g. `GOOS=windows GOARCH=386`) or spurious env vars/build tags that break native Linux amd64 builds, and remove/correct them so `make build` succeeds natively in that directory — check for stray files (like a broken test file gated by a bogus build tag) that also need to be excluded or fixed. Then implement `main.go`: read the log file argument line by line, and for each line use the vendored `jsonparser` package's `Get`/`GetFloat`/`GetString` functions to check for the `metric_value` key's presence and type (must exist and be numeric, not a string) and check whether `metric_name` contains `DROP_ME` as a substring — drop the line silently (no error printed) if any check fails, otherwise print the original line unmodified. Test against both provided corpora directly (`go run main.go <file>` on each clean and evil sample) and diff your stdout against expectations before considering it done, since the verifier runs exactly that command per file.
