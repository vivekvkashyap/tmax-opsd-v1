Break this into exploit-then-defend phases and test each half against the running legacy service directly before building the proxy. Phase 1: start `/app/legacy_profile_svc`, then hand-craft a JWT with `alg: none` — base64url-encode a header JSON like `{"alg":"none","typ":"JWT"}` and a payload like `{"user":"admin"}`, join them with `.`, and per the `none` algorithm spec leave the signature segment empty (trailing `.`) since the service skips verification when it sees `none`; confirm it works by curling `127.0.0.1:9000/profile` with that bearer token before saving it, and write the exact string to `/home/user/admin_exploit.jwt` with `chmod 600`. Phase 2: build the Python proxy as a simple `http.server`-based or socket-level reverse proxy on `127.0.0.1:8080` — decode the base64url header segment of the incoming Authorization JWT (no signature verification needed on your side, just inspect the claimed `alg`), reject with 403 if it's `none` (case-insensitively) and log via `open(path, 'a')` after `os.chmod(path, 0o600)` (create the file with correct permissions before or immediately after first write, since verification checks the mode), otherwise forward the request to port 9000, parse the JSON response, and if an `ssn` key exists, overwrite its value with the literal masked string before returning it to the client. Run the proxy as a background process and re-test both flows via curl before finishing.
