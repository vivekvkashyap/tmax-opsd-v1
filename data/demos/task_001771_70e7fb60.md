Start by running the pipeline as-is and reading the assertion failure message plus the line it fires on — this tells you which variable goes negative/NaN. For the variance bug: naive single-pass variance (`sum_sq - sum*sum/n`) computed in `float` suffers catastrophic cancellation when values share a large common offset (e.g., all near 100000), producing a tiny difference of two huge nearly-equal numbers; replace it with Welford's online algorithm (track `mean` and `M2`, update per new value with `delta = x - mean; mean += delta/n; delta2 = x - mean; M2 += delta*delta2; variance = M2/(n-1)`) using `double` throughout — this is numerically stable regardless of offset. For the moving-average bug: carefully re-derive the window loop bounds; look for an off-by-one such as `<=` where it should be `<`, which reads one extra (garbage/uninitialized) slot into the window sum. Fix both, then compile with `gcc -O2 processor.c -o processor -lm`, run against `sensor_data.txt`, and redirect stdout to `processed_output.csv` with the exact header and formatting (`%.2f` for Value/MovingAvg, `%.6f` for RunningVar). Verify output line-by-line against your own independent recomputation before finishing.
