Build this as a watch-parse-minify-archive-notify loop, and pay special attention to what actually shrinks the archive, since the grading is a strict size threshold. Use a filesystem-watch mechanism (e.g. Python's `watchdog`, or a polling loop over `os.stat` mtimes if watchdog isn't available) on `/home/user/config_mount/` for `.json` create/modify events. On each event: `json.load` the file, recursively strip out any keys whose value is `null`/`None` (walk dicts and lists), then re-serialize with `json.dumps(data, separators=(",", ":"))` to eliminate all whitespace — this compact separator choice is what actually removes the extra bytes, not just skipping `indent`. Archive into `/home/user/archives/config_history.tar.gz` by adding/overwriting each file's minified bytes as a `tarfile` member (open in `"w:gz"` mode with `compresslevel=9` — the default compresslevel leaves meaningful bytes on the table against a tight 15000-byte ceiling for 50 files); since tarfile doesn't support in-place update inside a gzip stream, either rebuild the whole tar.gz from an in-memory dict of latest minified contents on every event, or add members incrementally to a fresh `.tar.gz` you regenerate each time. After writing, get the archive's real size with `os.path.getsize` and POST the exact JSON payload shape specified to `http://127.0.0.1:5000/webhook`. Provide `/home/user/run.sh` to launch the script in the background.
