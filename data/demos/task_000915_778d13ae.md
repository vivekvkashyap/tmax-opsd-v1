For the video ETL, use ffmpeg's `-vf fps=1` combined with `-frames:v 20 -pix_fmt gray` to extract exactly 20 grayscale frames at 1 FPS, naming outputs with `frame_%02d.pgm` (verify numbering matches the required frame_01.pgm...frame_20.pgm pattern, adjusting the start number if ffmpeg's default 1-based numbering doesn't line up). To measure per-frame block intensities, read each pgm with a lightweight parser (PIL/numpy `imageio`, or manually parse the P5 PGM header) and average pixel values over the top-left 50x50 slice and the bottom-right 50x50 slice (`img[-50:, -50:]`), writing frame,sensor_x,sensor_y rows (21 lines total with header) to sensor_data.csv. For the Bash covariance script, mirror the reference formula precisely: split argv in half by count (`n=$#/2`), sum first n as A and last n as B using awk for floating math (avoid bash integer-only arithmetic), compute means, sum of `(a_i-mean_a)*(b_i-mean_b)`, divide by `(n-1)` for sample covariance, multiply by 1000, and truncate toward zero with `int()` in awk (not round) before printing only the integer — since it will be fuzz-tested against a reference for bit-exact matches, get the truncation and mean/covariance order of operations exactly right.
