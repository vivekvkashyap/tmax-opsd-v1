Diagnose before you patch: start the stack, then read `/home/user/incident.pcap` (e.g. with `tshark`/`tcpdump -r` filtering on HTTP) to find the exact request URL+query that appears right before the 504s begin — record it verbatim, don't guess the query string. Cross-reference that request against `/app/logs/api.log` and `nginx-error.log` to see which backend call it triggers and why it stalls; in a Flask+Redis stack behind Nginx, a classic cause is a blocking, O(n)-over-the-keyspace Redis command (e.g. `KEYS pattern*`) invoked with a wildcard derived from user input — that blocks the single-threaded Redis event loop for everyone, cascading into Nginx read-timeouts. Fix `app.py` by replacing the blocking full-keyspace scan with a non-blocking, cursor-based iteration (and/or bound the pattern/result size), keeping the search results correct. Separately bump `proxy_read_timeout` to 10s in nginx.conf and restart the stack. Validate empirically: hit the previously-problematic endpoint many times in a loop and confirm it stays fast and returns 200 before writing the regression-test script and resolution.json — put the function/command name you actually changed into the report, not a guess.