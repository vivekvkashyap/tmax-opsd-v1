Fix the four layers bottom-up and test each in isolation. First, get the C extension building: check `setup.py`'s Extension() call for missing linker flags — a `math.h` function like `sqrt` typically needs `libraries=['m']` on the linker line, so add that and rerun `python3 setup.py build_ext --inplace` until it succeeds cleanly. Second, in the C source, find the loop that walks a Python list by index — an off-by-one bound (using `<=` instead of `<` against the list size) causes an out-of-bounds `PyList_GetItem` and eventual segfault; fix the loop bound. Third, fix the actual RMS math: it must square each sample, sum the squares, divide by count, then take the square root (`sqrt(sum_of_squares / n)`) — check whether the current code is only summing raw values without squaring or without the final sqrt. Fourth, in `processor.py`, the query joins events against a metadata table with multiple rows per event without deduplication, causing each interval to be processed multiple times — rewrite the query to select distinct (event_id, start_sample, end_sample) or drop the redundant join entirely. After fixing all four, rerun `python3 processor.py` and compute the MSE against a hand-computed reference RMS (using the same interval boundaries) to confirm you're under the 0.001 threshold before finishing.