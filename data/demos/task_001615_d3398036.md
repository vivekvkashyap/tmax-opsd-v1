Implement this as a clear sequence of pure functions over the parsed CSV rather than one tangled loop. Read `/home/user/pipeline_logs.csv` with `encoding/csv`, filter out rows where `RowsProcessed == 0`, then group remaining rows by `PipelineID` into a `map[string][]Task`. Within each group, sort by `DurationSec` descending, breaking ties by `TaskID` ascending (use `sort.Slice` with a comparator that checks duration first, then falls back to string comparison on TaskID) — this tie-break is easy to miss and is exactly where PipeB's T04/T06 tie needs to resolve correctly. Take at most the first 2 entries per group as top_tasks with rank 1/2, sum their RowsProcessed for `top_2_rows_processed`. Skip any pipeline that ends up with zero tasks after the RowsProcessed==0 filter (don't emit an empty entry). Sort the final list of pipeline objects by PipelineID ascending. Marshal with `json.MarshalIndent(..., "", "  ")` to get 2-space pretty printing and write to `/home/user/etl_summary.json`. Run the program (not just compile it) so the output file actually exists, and diff your output's structure against the schema in the task description field-by-field.
