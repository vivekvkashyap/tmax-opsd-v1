Break this into three parts in order, verifying output at each step. For the join, since both CSVs are sorted by `id` with matching headers, use coreutils `join`: strip headers, `join -t, -1 1 -2 1` the two data bodies on `id`, then prepend the combined header line once — don't hand-roll a merge loop, and handle the header explicitly rather than piping it through `join`. For the C program, parse the joined CSV skipping its header, apply the logistic formula directly (`1.0/(1.0+exp(-z))` where `z = W[0]*t + W[1]*v + b`), and write predictions with `%.4f` for probability — check the class threshold is `p > 0.5` not `>=`. For timing, call `clock_gettime(CLOCK_MONOTONIC, &t1)` immediately before the inference loop (after data is already in memory) and again right after, computing microseconds as `(t2.tv_sec-t1.tv_sec)*1e6 + (t2.tv_nsec-t1.tv_nsec)/1e3`. Compile with `gcc infer.c -o infer_model -lm`, then run the bash script followed by the compiled binary.