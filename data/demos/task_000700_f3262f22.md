Treat this as three independent recovery/optimization steps that each need verification before moving to the next. First, OCR the screenshot (`tesseract /app/creds_screenshot.png stdout`, trying preprocessing like upscaling/thresholding with `convert` if the raw OCR is noisy) to recover the readable partial password prefix — don't assume any particular string, read what's actually there. Second, grep `/app/auth_logs.txt` for `admin` failed-login lines and take the hash from the *most recent* one specifically (sort/inspect timestamps, don't just take the last grep match blindly if ordering isn't guaranteed). Third, study `legacy_hash.cpp` to understand its exact bit operations, then reimplement equivalent logic in your brute-forcer but algorithmically simplified (remove redundant loops/sleeps, precompute invariant work outside the innermost loop, use tight integer/vectorized ops) — brute force only the unknown 4 lowercase suffix characters (26^4 space) appended to the OCR'd prefix, hashing each candidate and comparing to the target hash from the log, timing your candidate loop to confirm it's comfortably under 1.5s before finalizing. Once cracked, write the full password to the required file, then call `/app/vault_cli` with `--pass <recovered> --newpass <your-choice>` and capture its stdout to the rotation log.