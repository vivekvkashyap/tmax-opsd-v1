Debug this layer by layer rather than guessing: first make `make` succeed, then make the C logic correct, then make `go test` pass. Inspect the Makefile's compile and link rules for a cgo shared library — a shared object built without position-independent code (missing `-fPIC` on the `.o` compile step) or with a malformed link command (e.g. `gcc -shared vm.o libmathvm.so` treats the output name as another input instead of using `-o libmathvm.so`) will fail to build correctly; fix both the compile and link recipes. Next, look closely at the VM's subtraction opcode handling: on a stack machine, operands are typically pushed in evaluation order and popped in reverse, so if `a` is popped first (the more recently pushed value) and `b` second, the correct subtraction result is `b - a`, not `a - b` — trace through a concrete example like push(5), push(3), sub to see which order is right, and compare against what Go's property test expects (`a - b` where a,b are pushed as OpPush, OpPush, OpSub in that order). Finally, export `LD_LIBRARY_PATH` to include the build directory so the dynamically-linked test binary can find `libmathvm.so` at runtime, run `go test -v` until it passes, then write the exact success string to the log file.