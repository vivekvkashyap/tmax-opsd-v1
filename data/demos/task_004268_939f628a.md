Work through this in strict dependency order and validate each stage before moving to the next. First, open `/app/scipack-1.0.0/scipack/ode.py` and inspect the adaptive step-size update line: when the local error is large relative to tolerance, the step should shrink, and when error is small, it should grow — check whether the exponentiated ratio has error and tolerance in the correct numerator/denominator positions, and fix accordingly; install the package in editable/dev mode or set PYTHONPATH so your later scripts pick up the fix rather than any cached/installed copy. Second, load `state_0` from the HDF5 file with h5py, integrate the given ODE system with `solve_ivp_adaptive`, then interpolate the resulting trajectory (e.g. cubic spline on t vs y) and use `scipy.optimize.brentq` on the y-component (with the additional sign condition on vy) to bracket and solve for the crossing time precisely — a coarse root grid search is not precise enough. Third, build the bootstrap pipeline exactly as specified: seed 42 before generating the 100 noisy initial states, re-seed 42 immediately before the 1000-resample bootstrap, and compute percentiles of the resampled means. Finally, stand up an HTTP server on 127.0.0.1:8080 with a `/metrics` GET handler that checks the `X-Auth-Token` header equals the given secret before returning JSON, else 401; keep the process running in the background (e.g. nohup + &) so the grader can query it after your turn ends.