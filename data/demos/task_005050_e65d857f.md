Read X and y from data.h5 with `h5py`. Compute the OLS estimate via SVD explicitly (don't just call `np.linalg.lstsq`): decompose X = U S Vt with `np.linalg.svd(X, full_matrices=False)`, form the pseudoinverse from U, 1/S, and Vt, and multiply by y to get w_ols — this is the required initialization point for MCMC. Write a from-scratch Metropolis-Hastings sampler: define the log-posterior as the sum of the Gaussian log-likelihood of y given Xw (sigma=0.5) and the standard-normal log-prior over each weight component. Critically, call `np.random.seed(42)` immediately before the MCMC loop starts (not earlier, and not before the OLS/data-loading steps) so the accept/reject draws are reproducible against the reference. Each iteration: propose w' = w + N(0, 0.05^2 per dimension), accept if `log(uniform()) < logpost(w') - logpost(w)`, otherwise keep the current w, and record the current w every iteration (10,000 total). Discard the first 2,000 as burn-in, then compute the fraction of the remaining samples where w[0] > 1.0. Have analyze.py print that single float, and have run_analysis.sh (chmod +x) invoke it and redirect stdout into hypothesis_test.txt.