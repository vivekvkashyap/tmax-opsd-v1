Build this as a streaming state machine, not a full-file load: open `/home/user/events.jsonl` and process one line at a time so memory stays flat regardless of file size. For each line, extract the `username` field's raw string value (simple bracket/quote scanning is fine since the schema is fixed) and decode any `\uXXXX` escapes yourself by parsing the 4 hex digits and casting to `char` — remember only ASCII (0x00-0x7F) escapes are guaranteed, so no UTF-8 multi-byte handling is needed. Write the masked line by re-emitting the original JSON text with the username value replaced by `***`, preserving exact original formatting/spacing elsewhere. For bucketing, parse the ISO 8601 timestamp fields manually (year/month/day/hour/min/sec) and convert to Unix epoch using UTC-only arithmetic (no local timezone, no library that assumes local time) — a common bug is using `mktime` which applies local timezone; instead compute days-since-epoch via a manual civil-to-days algorithm or use `timegm`. Floor the epoch to the start of its hour by zeroing minutes/seconds. Accumulate counts in a map keyed by (bucket_epoch, action), then dump sorted first by bucket_epoch ascending, then by action alphabetically, into the CSV with the exact header `bucket_epoch,action,count`. Compile and run the binary as specified so both output files actually exist on disk.