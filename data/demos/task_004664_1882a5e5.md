Work in three independent, verifiable stages rather than trying to do everything by hand. First, read `policy.json` in Python, build a dict mapping dependency name to its minimum version (strip the `>=` prefix), then read `Cargo.toml` line by line (or with a regex per dependency line like `^name = "version"`) and rewrite each matched line's version string to the policy minimum — write this logic into `/home/user/project/fix_deps.py` and run it so `Cargo.toml` is actually updated on disk, not just printed. Second, write `/home/user/project/migrate.py` that loads `schema_v1.json`, and for each record builds a new object with `id` and `full_name = first_name + " " + last_name` (single space), then dumps the list to `/home/user/project/data.json`; run this script too. Third, write `/home/user/project/ci_pipeline.sh` starting with `#!/bin/bash`, `cd /home/user/project`, run `cargo check`, capture its exit code (`$?`), and `exit` with that exact code — then `chmod +x` the script. Verify each output file exists and matches the expected structure before finishing; don't just write the scripts, execute them so the target files are produced.
