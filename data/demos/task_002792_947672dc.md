Split this into the two independent halves and verify each end-to-end before moving on. TLS/proxy half: generate the cert with `openssl req -x509 -newkey rsa:2048 -sha256 -nodes -keyout /home/user/key.pem -out /home/user/cert.pem -days 365 -subj ...`; write an nginx.conf that sets `pid`, `access_log`, `error_log`, and every `*_temp_path` (client_body, proxy, fastcgi, uwsgi, scgi) into `/home/user/nginx_data/` (create that dir first) since nginx defaults to root-owned system paths that a non-root user can't write; listen on `127.0.0.1:8443 ssl` with the generated cert/key, and `proxy_pass http://127.0.0.1:8080;`. Start the backend python script and `nginx -c /home/user/nginx.conf`, both backgrounded, then confirm with `curl -sk https://127.0.0.1:8443/` that it reaches the backend. Filter half: in filter.sh, first reject on presence of shell metacharacters (`` ` $() ; | < > ``) and privesc keywords (`sudo`, `/bin/bash`, `etc/shadow`, `/root`) anywhere in the argument, then reject if any query value looks like an absolute/protocol-relative external URL (`http://`, `https://`, `//`) — order matters, check both categories independently since a payload can trigger either. Validate exhaustively against both corpus files, not just a few examples, before declaring done.