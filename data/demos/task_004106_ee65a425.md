Split this into getting the pipeline healthy and reverse-engineering the token algorithm — they don't depend on each other. For the pipeline: fix `/app/config.json` so its Redis fields point to `127.0.0.1:6379` and its auth-socket field points to `/tmp/auth.sock`, then launch `redis-server`, `auth_daemon`, and `api_gateway.py` in that order (each backgrounded), and confirm the chain with a manual `curl -X POST http://127.0.0.1:5000/auth -d '{"username":"testuser"}'`. For the algorithm, don't read raw disassembly blindly — use dynamic tracing (`ltrace`/`strace` while sending a request over the Unix socket, or a Python script talking to the socket directly) to observe input/output byte pairs for several known usernames, then look for a simple per-character transform (common patterns: XOR with a constant, add/subtract a constant, or both) by comparing input bytes against output hex byte-pairs mathematically. Once derived from evidence, implement it in `/home/user/backdoor_token.py` taking the username as `sys.argv[1]` and printing only the resulting lowercase hex token; validate against the real daemon for several test usernames, including edge cases, before finishing.