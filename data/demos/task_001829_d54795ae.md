Load the CSV and build an in-memory tree by mapping each emp_id to its record, then grouping employees by manager_id to build a children lookup (empty/blank manager_id marks a root). Compute total_team_cost with a recursive post-order traversal: a node's total is its own cost plus the sum of its children's totals, so process leaves before parents (recursion or an explicit stack that visits children first). Build the nested JSON by recursively constructing each employee object with its `subordinates` list from the children lookup, sorting both the subordinates list and the top-level roots array by emp_id ascending at every level. Use an actual JSON library to serialize (not string concatenation) so property order/types and the empty-array case for leaf nodes are handled correctly, and validate the result against `/home/user/schema.json` (e.g. with a jsonschema validator) before finishing — this catches missing/extra keys like accidental floats where integers are required. Write the final structure to `/home/user/org_chart.json`.