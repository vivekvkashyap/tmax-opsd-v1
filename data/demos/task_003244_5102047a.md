Work bottom-up: first get the vendored package importable, then compute, then validate statistically. Read `/app/fastvecsearch/fastvecsearch/distance.py` carefully line by line — a broken vendored package usually has one small typo (e.g. a misspelled numpy attribute call) rather than a logic error, so diff each function call against the real numpy API before assuming anything structural is wrong. After fixing, `pip install -e /app/fastvecsearch` and confirm `import fastvecsearch` and its cosine_sim function work in a throwaway script before touching the real data. For the top-5 hard negatives, compute cosine similarity of every anchor against every candidate (since embeddings are L2-normalized, cosine similarity reduces to a dot product) and take argsort descending per anchor — do this with matrix multiplication (anchors @ candidates.T) rather than a nested Python loop for speed and correctness. Call `numpy.random.seed(42)` immediately before the random-negative sampling step (and nowhere else) so your random draws match the expected reproducible sequence. Compute per-anchor means of hard vs random negative similarities, run `scipy.stats.ttest_rel` on the two paired arrays of 500 means, and get the 95% CI via the result's `.confidence_interval(0.95)` method rather than hand-rolling the CI formula, to avoid subtle errors.