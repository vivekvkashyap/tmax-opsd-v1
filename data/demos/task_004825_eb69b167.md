First fix the vendored package: open `/app/brotli-1.1.0/setup.py` and look for a misspelled build-argument keyword (compare where it's defined versus where it's actually passed to the compiler/extension config) — a subtle typo there silently drops compile args and breaks the build. Fix the spelling, then `pip install /app/brotli-1.1.0` and confirm `python3 -c "import brotli"` works before writing any application code. Then implement `/home/user/archive.py` as a true streaming reader over `sys.stdin`: read line by line (not `sys.stdin.read()` all at once), skip blank lines, `json.loads` each line, pull the `message` field, reverse it with slicing (`msg[::-1]`), encode UTF-8, compress with `brotli.compress(data, quality=4)`, then write to `sys.stdout.buffer` a 4-byte little-endian unsigned length prefix (`struct.pack('<I', len(compressed))`) followed by the raw compressed bytes — flush stdout after each line or at minimum before exit so downstream readers relying on incremental output aren't stalled. Since this will be checked against a reference implementation across many random inputs, be exact about byte order, quality level, and not adding extraneous newlines/whitespace to the binary output.