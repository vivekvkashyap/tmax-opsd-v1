Write `/home/user/analyze_backup.c` to parse both CSVs (skip header lines) and accumulate a per-node degree count: for `backup_edges.csv`, each `source,target,type` row increments the out-degree of `source` and the in-degree of `target` — total degree per node is the sum of both. Use a simple array or hash map keyed by numeric node ID sized to cover the IDs seen in `backup_nodes.csv`. After tallying, scan all nodes to find the maximum total degree; on ties, prefer the numerically smaller ID (sort by degree descending, then ID ascending, and take the first) — don't just take the first node encountered in file order, since that only works if IDs happen to be pre-sorted. Write the winner to `/home/user/critical_node.txt` as exactly `NodeID,TotalDegree` with no extra whitespace or trailing content. Compile with `gcc -o /home/user/analyze_backup /home/user/analyze_backup.c` and run it to produce the output file. Finally write `/home/user/validation_query.cypher` as the Cypher equivalent: match each node's undirected relationships, count them per node, order by count descending then by `id` ascending, and `LIMIT 1` to return the same tie-breaking semantics as your C program.