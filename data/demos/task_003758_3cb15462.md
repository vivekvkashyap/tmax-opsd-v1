Recover the salt by inspecting the binary's readable strings rather than guessing: run `strings /home/user/upload_auth.bin | grep -A0 -B0 SALT` (or grep for the literal marker) to locate the string that begins with `SALT:` and take the 16 characters immediately following that prefix as the salt. Write `keygen.cpp` to concatenate the fixed password `StrictPassword2024!` directly with the recovered salt (password first, then salt, no separator unless the format says otherwise), hash the concatenation with OpenSSL's `SHA256()` from `<openssl/sha.h>`, hex-encode the 32-byte digest in lowercase (e.g. via a loop with `snprintf(..., "%02x", digest[i])`), and write `AUTH-TOKEN-<hex>` to `new_token.txt` using a stream that does not append a trailing newline (e.g. write raw bytes or strip the newline after `<<`). Compile with `g++ keygen.cpp -o keygen -lcrypto` (link order matters — put `-lcrypto` after the source/object), run `./keygen`, and confirm the file has no trailing whitespace or newline before finishing.
