Build this as two layered artifacts: a bash wrapper (`run_pipeline.sh`) that creates `/home/user/venv`, installs numpy inside it, then invokes the python script with that venv's interpreter — remember to `chmod +x` the wrapper and source/activate the venv (or call `venv/bin/python3` directly) rather than relying on system python. In the python script, follow the spec literally and in order: generate data with `np.random.RandomState(42).normal(loc=1e7, scale=2.5, size=100000).astype(np.float32)` — the naive variance implementation must operate on the float32 array as-is (E[X^2] - E[X]^2) so it naturally suffers catastrophic cancellation since 1e7^2 already exceeds float32's ~7 significant digits of precision; don't accidentally upcast it. Implement Welford's algorithm as a single running-mean/M2 update loop (vectorized or per-element) for population variance (divide M2 by count, not count-1). For timing, call `time.perf_counter()` around 100 separate invocations of `welford_variance(arr)` to collect 100 durations, then bootstrap: resample the 100 durations with replacement 1000 times (seeded `RandomState(42)`), take the mean of each resample, and read off the 2.5/97.5 percentiles for the CI bounds. Write results with exact JSON keys as specified.
