Build the script as a clear pipeline of parse -> detect -> validate -> extract, each as a separate function you can test independently. Parse each JSON line, and check both `path` and `headers.get("X-Upload-Path", "")` against traversal patterns using a case-insensitive regex that matches literal `../`, `..\`, and their percent-encoded equivalents `%2e%2e%2f`/`%2e%2e%5c` (compile with `re.IGNORECASE`, and remember `%2e` may appear mixed-case like `%2E`). Extract the `session=` value from the `Cookie` header by splitting on `;` and `=` rather than a broad regex, since other cookie fields can follow. To validate: split the token on the last `.` into payload and signature, re-pad each base64url segment to a multiple of 4 with `+= '=' * (-len(s) % 4)` before `base64.urlsafe_b64decode`, then recompute `hmac.new(secret_bytes, payload_b64_bytes, hashlib.sha256).digest()` and compare its base64url encoding (no padding) to the signature using `hmac.compare_digest` — sign over the base64-encoded payload string, not the decoded JSON. Only decode the JSON payload for `username` after signature verification passes, then append `IP,username` per matching request to the output file.
