Build this as a graph unification + BFS problem, not four separate parsers glued ad hoc. Steps: (1) parse `users.csv` and `user_roles.csv` with basic `split(',')` line handling (skip the header row); (2) parse `roles.json` by hand with simple string scanning since it's a flat map of string keys to string arrays — no need for a full JSON parser, just locate each `"R_id": [...]` block and split the bracketed contents on commas, stripping quotes/whitespace; (3) parse `role_resources.csv` similarly. Unify everything into one adjacency structure (e.g. `HashMap<String, Vec<String>>`) where edges are: user -> its direct role(s), role -> roles it inherits from, role -> resources it grants. Then run BFS from `U73` over this combined graph to find the shortest path to `RES-999`, reconstructing the path via a predecessor map. Write the path as comma-joined IDs to `/home/user/violation_path.txt`. Compile with `rustc /home/user/audit.rs` and run the resulting binary to actually produce the output file — don't just leave it compiled.
