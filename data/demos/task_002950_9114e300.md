Inspect the schema first with `.schema` to confirm the `reporting(subordinate_id, manager_id)` and `access_events` column names. Build the top-manager lookup as a top-down recursive CTE, not a per-employee bottom-up walk: seed it with every user who never appears as a `subordinate_id` (those are the roots, and each is trivially its own top manager), then recursively join `reporting` downward (`manager_id` of the recursive row matches the parent's `id`) carrying the root's name along as `top_manager` for every descendant reached. Separately aggregate `access_events` filtered to `resource_name = 'SECRET_VAULT'`, grouping by `user_id` and taking `MIN(access_time)`. Join that aggregate to your recursive ancestor table on user id, select `name, access_time, top_manager`, and `ORDER BY name`. Emit the CSV with `sqlite3 -header -csv` so the header row and formatting match exactly, redirecting straight to `/home/user/audit_results.csv`. Diff your output against a hand-traced expectation for at least one multi-level employee before finishing.
