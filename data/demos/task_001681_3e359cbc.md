Treat this as building a small ETL + API pair, and get the plumbing running before worrying about correctness: install redis/pandas/flask/jinja2, start `redis-server` bound to 127.0.0.1:6379, and run the seed script so `loc_events` is populated. For `pipeline.py`, use pandas for the resample/gap-fill step: build a DataFrame per language indexed by parsed timestamps, `set_index` and `resample('1H')` (or reindex against an explicit hourly `date_range` from 2023-10-01T00:00:00Z to 2023-10-02T00:00:00Z inclusive) then apply `ffill()` for forward gaps and `bfill()` for the leading gap before the first record — order matters: bfill only fills what ffill left empty at the start. For each resulting hourly row, render the matching Jinja2 template from `/home/user/templates/<lang>.jinja` with the row's fields to produce `summary_text`, then dump everything to `/home/user/processed_metrics.json` in the nested `{lang: {timestamp: {...}}}` shape. For `app.py`, implement `/report` reading that JSON and returning 404 on missing lang/hour keys, and `/trigger_pipeline` checking the exact `Authorization: Bearer loc-token-99` header (401 otherwise) before running `pipeline.py` via `subprocess.run` synchronously. Run Flask in the background bound to 127.0.0.1:8080 and smoke-test both endpoints with curl before finishing.
