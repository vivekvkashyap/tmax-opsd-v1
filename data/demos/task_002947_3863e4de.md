Separate this into three independent sub-problems and solve them in order, verifying each before moving on. (1) Recovery: don't try to repair the file in place — use `sqlite3 telemetry.db ".recover"` or `.dump` piped into a fresh database (`sqlite3 telemetry_recovered.db < recovered.sql`) to salvage rows despite the "malformed" corruption, then confirm row counts look sane with a `SELECT COUNT(*)`. (2) Decoding: in `triage.py`, wrap the BLOB decode in a try/except — attempt `.decode('utf-8')` first, and on `UnicodeDecodeError` fall back to `.decode('latin-1')`, then `json.loads` the result to extract each record's `temp` field into a list keyed by `id`. (3) Anomaly detection: after all temps are parsed, compute the population mean and standard deviation (e.g., via `statistics.mean`/`statistics.pstdev` or manually), then flag any `id` whose `abs(temp - mean) / stdev > 3`. Sort the flagged ids numerically and write one per line to `/home/user/anomalies.txt`. Run the script end-to-end and re-check the output file's contents against your own hand-computed mean/stdev sanity check.
