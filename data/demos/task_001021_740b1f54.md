First fix the vendored `fastcsv` build before writing any pipeline code: inspect `/app/vendored/fastcsv-1.0.4/setup.py`'s `ext_modules` definition for a preprocessor macro name that doesn't match what the C source actually checks (e.g. a "DROP" vs "KEEP" newlines flag mismatch) and for a missing local include directory (like `./src`) that would cause the C-extension compile to fail and silently fall back to a slower pure-Python path — fix the macro and the include path, then `pip install -e` it and verify `fastcsv.reader` actually loads the compiled C extension (not the `_pure_py` fallback), since only the C path correctly preserves rows with embedded newlines. For `stream_etl.py`, read raw bytes from stdin, parse CSV records respecting quoted fields that may contain embedded newlines/commas (use fastcsv's reader once it's fixed, working on the byte stream), then for each row decode the `payload` bytes with the row's own declared `encoding` using `bytes.decode(encoding, errors='replace')` so invalid byte sequences become U+FFFD rather than raising, and emit one JSON object per line (`id`, `payload_length` = len of the decoded string, `normalized_payload`) to stdout, flushing per line for streaming behavior. Use `multiprocessing` (e.g. a pool mapping over row chunks) only in a way that preserves input row order in the output, since exact ordering is almost certainly checked by the fuzz-equivalence comparison against the reference implementation.