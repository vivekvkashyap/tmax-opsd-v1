Start with the audio: transcribe `/app/auth_token.wav` yourself (e.g. install and run a local speech-to-text tool such as `whisper` or another available CLI/library — do not guess the phrase) and pull out just the spoken phrase to use as the Bearer token constant in your server. For the Rust service, scaffold with `cargo new` at `/home/user/etl_tracker`, add a lightweight async HTTP framework (e.g. actix-web or axum) plus a JSON crate (serde/serde_json), and bind to `127.0.0.1:8080`. Implement middleware/manual header checking on both `/ingest` and `/config` that compares the `Authorization: Bearer <token>` header against your transcribed token and returns 401 on mismatch or absence. For cleaning logic: implement Levenshtein distance as a standalone function, and process incoming records in order, comparing each new record's `config_hash` against every *already-accepted* record's hash (not all records) — discard if distance <= 2 from any accepted one, otherwise accept it. For null `metric` imputation, sort/iterate by `sequence`, and for interior nulls use linear interpolation between the nearest surrounding non-null values; for leading/trailing nulls just carry over the nearest valid value. Keep accumulated state in a `Mutex<Vec<...>>` or similar shared state, compile with `cargo build --release`, and leave the server running in the background.