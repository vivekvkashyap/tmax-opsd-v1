Split this into the vendored fix, the router logic, and the config files, and verify each separately. For the proxy, grep `/app/cloud-proxy-0.1.0` for the request-method check that special-cases POST with a 403 response, remove/patch that branch so POST requests flow through the same proxying path as other methods, then `pip install -e /app/cloud-proxy-0.1.0` and smoke-test with a POST curl against the running proxy. For migrator_router.py, use `urllib.parse` (urlparse/parse_qsl/urlencode) rather than manual string slicing so query-string edge cases (empty query, multiple `?`, unordered params) are handled correctly by the library; apply the four rules in the exact stated precedence with `startswith`/substring checks on the *path* (not the full URI with query) where the spec says path, and for the PHP rule sort query params alphabetically by key before re-encoding and reassembling the URL. Since this is checked by bit-exact fuzz comparison, test manually against varied inputs (trailing slashes, no query, `.php` mid-path, unicode) before considering it done. Finally write the fstab line and docker-compose.yml exactly matching the stated mount options, image, port, and volume mapping.
