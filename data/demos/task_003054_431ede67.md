Automate this as a sequence of standard binary-forensics commands inside your bash script, verifying each step manually first before scripting it. Use `objcopy --dump-section .c2_cert=/home/user/extracted_cert.pem /home/user/malware.elf` (or `readelf -x .c2_cert` and post-process) to pull the raw PEM bytes out of the custom ELF section — confirm the result parses with `openssl x509 -in extracted_cert.pem -noout` before moving on. Run `openssl x509 -in extracted_cert.pem -noout -issuer -subject > cert_info.txt` and check the output lines match the standard `issuer=...` / `subject=...` format. For the CSP string, use `strings /home/user/malware.elf | grep 'JWT:alg=none'` to locate the embedded string, then extract just the domain following `connect-src ` up to the next `;` or word boundary with `grep -oP` (e.g. a regex capturing `connect-src\s+(\S+)` and stripping the trailing `;`), writing only that URL (no extra text) to `/home/user/c2_url.txt`. Wrap all three steps in the script, `chmod +x` it, and run it end-to-end to confirm all three output files are produced correctly.