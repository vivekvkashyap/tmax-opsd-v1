Do the video/pixel extraction correctly first, since everything downstream depends on it: use `ffmpeg` to dump raw RGB frames (e.g. `-f image2pipe -vcodec rawvideo -pix_fmt rgb24`) or decode frame-by-frame, and for each frame index in the requested `[start_frame, end_frame]` range compute the byte offset of the center pixel (x=64,y=64) as `(64*128 + 64) * 3` into that frame's raw buffer, then read R, G, B. Only count an edge (R -> G) when B == 1 (Granted). Build an out-degree map for Employee IDs (R values) over Granted edges only, then pick the ID with max out-degree, breaking ties by choosing the lowest ID — implement this tie-break explicitly since a naive `max()` over a dict won't guarantee it. In Rust, either shell out to `ffmpeg` via `std::process::Command` and parse its stdout, or use a crate that can decode MP4/video frames directly. Build in release mode so the binary lands at `/home/user/audit_query/target/release/audit_query`, print exactly `<Employee_ID>,<Out_Degree>` with no extra text, and test it yourself across several start/end ranges (including the same-frame case) before considering it done, since the verifier fuzzes many random ranges and requires bit-exact stdout matching a reference implementation.
