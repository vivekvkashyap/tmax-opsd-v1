Fix the vendored package first: `make install` failing usually traces to a bad toolchain or env var — check the `Makefile` for a `CC=` line pointing at something that isn't a real compiler (fix it to `gcc`), and check `setup_env.sh` for an exported path variable (e.g. `TRACKER_HOME`) that doesn't match the package's actual install location; correct it so `bin/track_exp` can find its runtime files, then confirm `track_exp --init` runs cleanly. For `filter.sh`, order the checks so failures short-circuit fast: use `awk` to count fields (`NF`) per line and reject if any row isn't exactly 50 columns; then sample 100 rows with replacement (`shuf --repeat` or an awk-based sampler), compute each sampled row's Euclidean distance to a vector of constant 0.1s, average across the sample, and reject if outside the given bounds. Call `track_exp --log <filename> --status clean|evil` for every file evaluated, and ensure the script's exit code matches the classification exactly, since the grader checks exit codes directly.
