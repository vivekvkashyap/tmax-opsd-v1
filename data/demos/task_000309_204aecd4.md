Build the pipeline as a sequence of composable shell stages inside `clean_and_correlate.sh`, not one giant awk one-liner. Sort both CSVs by `id` (skip headers, e.g. `tail -n +2 file | sort -t, -k1,1`) so `join -t, -1 1 -2 1` on the id column works regardless of input order. Pipe the joined output through `awk -F,` to drop rows where `feature_x` or `feature_y` is empty (`$2==""`) and where either value is `>50.0` or `<-50.0` (use numeric comparison, not string), emitting only `feature_x,feature_y` to `/home/user/cleaned_features.csv`. For the Pearson correlation, don't hand-roll it in awk unless confident — a short `python3 -c` snippet reading the two columns with basic sum/sumsq/sumxy formulas (or `scipy.stats.pearsonr`) is more reliable; round the result to 3 decimals and write only that value to `/home/user/correlation.txt`. Test each stage's output independently (join result, then filter result, then correlation) before chaining them, and make the script executable.