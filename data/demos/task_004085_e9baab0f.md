You don't need to reverse-engineer /app/run_sim's internals — the three rejection rules are already stated in the task description, so focus on implementing them robustly in Bash/awk against FASTA parsing quirks. Parse by tracking header lines (`>`) and concatenating all subsequent non-header lines into one sequence string until the next header or EOF (sequences can span multiple lines — don't assume one line per record). For each sequence check: (a) every character is a standard amino acid letter; (b) length falls within the given bounds — read them carefully; (c) the G+C+P+W count ratio to length stays under the given threshold, using awk/bc for the float comparison. Only sequences passing all checks get header+sequence appended to the output, preserving formatting; failures are dropped (empty output file if none pass). Before finalizing, run against every file in /home/user/data/clean/ (output must equal input exactly) and /home/user/data/evil/ (output must be empty) to catch edge cases like trailing blank lines or multi-line sequences.