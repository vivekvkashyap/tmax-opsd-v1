This task has two independent halves — get each working and verifiable on its own before considering the task done. For the services half: start a real `redis-server` bound to 127.0.0.1:6379 (install/launch it, don't stub it), then launch `/app/tracker_api.py` in the background with `REDIS_HOST=127.0.0.1 TRACKER_PORT=8080` set in its environment, and confirm both ports are listening (`ss`/`curl`) and that `GET http://127.0.0.1:8080/health` returns 200 (it internally pings Redis, so this only passes if Redis is actually reachable). For the processing half, treat it as a byte-for-byte fuzz-equivalence problem: build `/home/user/process.py` strictly from the written spec — filter users/events with exact type checks (`isinstance(x, int)` and not bool where relevant, since Python bools are ints), take the *first* occurrence per user_id when duplicates exist, inner-join on user_id, and compute Pearson correlation manually (mean/variance/covariance) returning JSON `null` when fewer than 2 joined records or either series has zero variance. Output must be exactly `json.dumps(..., separators=(',', ':'))` with no trailing newline surprises or extra prints — any stray debug output to stdout will break exact-match comparison, so route logs to stderr only.
