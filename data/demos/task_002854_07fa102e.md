Do the OCR extraction first and treat its output as the source of truth: run `tesseract /app/legacy_dashboard.png stdout` (adjust `--psm` if needed) and locate the `unix:/...sock` path mentioned in the text — use that exact path, don't assume a conventional one. Edit `/home/user/nginx.conf`'s `location /` block, replacing the existing `proxy_pass` target with `http://unix:<extracted-path>:;` syntax (note nginx's unix-socket proxy_pass requires the trailing colon). For `/home/user/ci_test.py`, launch the backend with `subprocess.Popen(["python3", "/app/backend.py", extracted_socket_path])` and nginx with the daemon-off invocation given, both as background subprocesses you can later `terminate()`/`wait()` in a `finally` block. Poll readiness by attempting a real TCP connect or HTTP GET to `127.0.0.1:8080` in a retry loop with a timeout and short sleep between attempts rather than a fixed sleep, so the script is robust to slow startup. Then issue 50 sequential GET requests to `/health`, count status-200 responses, and print only `successes/50` as a float — wrap each request in a try/except so network errors count as failures rather than crashing the script. Clean up both subprocesses before exit regardless of outcome.