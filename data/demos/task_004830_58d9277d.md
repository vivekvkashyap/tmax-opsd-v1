This is a straightforward three-way join/aggregation — the risk is double-counting rather than parsing. In `/home/user/verify_backups.py`: load `services.csv` with `csv.DictReader` into a `service_id -> team_owner` map. Load `endpoints.jsonl` line by line with `json.loads`, and for each service accumulate its endpoint list into a per-team set (use a set, not a running count, since "unique endpoints" implies duplicates within a service's list — or across services owned by the same team — should not be double-counted; check the task's own definition of uniqueness against how endpoints are scoped per service before deciding set-per-team vs set-per-service-then-summed). Load `dependencies.json` as a list of `{source, target}` edges, and for each edge look up the *source*'s team via the service map, incrementing that team's `outbound_dependencies` counter by 1 per edge (not deduplicated — this is a count of edges, not unique targets). Build a dict keyed by team name with `total_endpoints` and `outbound_dependencies`, and serialize it to `/home/user/team_summary.json` with `json.dump`. After running, sanity-check the output structure by eye against the exact keys required (`total_endpoints`, `outbound_dependencies`) and confirm every team appearing in `services.csv` shows up even if it has zero dependencies.
