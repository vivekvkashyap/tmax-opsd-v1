First get the backend working: run `/app/vendor/simple_service/server.py` with one port argument, note it crashes, trace the traceback to the `sys.argv` index it misuses (expects an index not populated when only one CLI arg is passed), and correct it so the same port is used for both binding and the response text. Launch three instances backgrounded on ports 8001/8002/8003, verifying each with `curl` before wiring up the proxy. For the load balancer, write `/home/user/setup_balancer.py` to render an idempotent nginx.conf with a plain `upstream` block (default round-robin order — no `least_conn`/weights, needed for even CV distribution), `listen 127.0.0.1:8080`, `access_log /home/user/nginx/access.log`, and `pid /home/user/nginx/nginx.pid`. Write `/home/user/start_nginx.sh` to launch nginx with `-c` against that config as the current user (local prefix, no root needed). For rotation, `/home/user/rotate_logs.py` should rename the log then signal nginx to reopen it (`SIGUSR1`/`-s reopen`) using the pid from the pidfile. Verify by curling port 8080 many times and tallying which backend answered each time.
