Start by black-box fuzzing `/app/legacy_processor` with the provided clean/evil samples in `/home/user/samples/` (and hand-crafted variants) to empirically pin down its crash/exploit conditions — vary the length of the `reading` field and the character set of `sensor_name` field independently, observing exit codes/crashes, rather than guessing constraints from the description alone. Once you've derived the exact validation rules, implement `filter_and_fill.sh` in pure Bash/awk: read the CSV, check each row against the derived constraints with `awk` or `grep -E`, and if any row is invalid, `exit 1` immediately with no stdout. If all rows are clean, do the gap-fill pass: sort/process by `timestamp_sec`, and whenever consecutive timestamps differ by more than 1, synthesize the missing intermediate rows by forward-filling the `sensor_name`/`reading` values from the prior row (awk's associative arrays and a simple loop `for (t = prev+1; t < cur; t++)` work well here). For `batch_process.sh`, use `find <input_dir> -name '*.csv' | xargs -P <n> -I{} bash -c '...'` to invoke `filter_and_fill.sh` on each file in parallel, redirecting stdout to `<output_dir>/<basename>` only when the sanitizer exits 0, and discarding/removing any empty or failed output. Test against both sample directories before trusting the logic.