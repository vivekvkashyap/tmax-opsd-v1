Start by reverse-engineering the SQLite schema before writing any aggregation logic: run `sqlite3 /home/user/warehouse.sqlite ".tables"` and `.schema` (or `PRAGMA table_info(<table>)` for each table) to find which obfuscated table holds the user-to-group-code mapping and which holds the group-code-to-human-name mapping — don't assume table/column names, inspect them. Once you know the schema, join it against the JSON events: load `/home/user/raw_events.json`, filter to `event_type == "purchase"`, group by `u_id`, map each `u_id` -> group code (from the user dimension table) -> human-readable group name (from the group reference table), and count purchases per human-readable name. Use a script (Python with sqlite3+json modules is simplest) rather than manual/ad hoc counting so the join is exact and reproducible. Drop any group with a zero purchase count instead of emitting it with count 0. Sort the final list by `purchase_count` descending, tie-broken by `group_name` ascending, and write it as a JSON array of `{"group_name": ..., "purchase_count": ...}` objects to exactly `/home/user/purchase_summary.json`. Double check key names and integer typing match the schema exactly.