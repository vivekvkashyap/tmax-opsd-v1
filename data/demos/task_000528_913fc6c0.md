Run `tesseract /app/attacker_note.png stdout` (installing tesseract-ocr if needed) to recover the secret uppercase word, and strip whitespace/newlines from the OCR output before using it — OCR often appends trailing newlines or misreads characters, so double check the extracted word looks like a clean uppercase token. Then build `/home/user/detector.py` to reject on: absolute paths, `../` or `..\`, null bytes (raw `\0` and literal `%00`), and crucially all the encoded/obfuscated variants — decode percent-encoding iteratively (including double-encoding like `%252e`) and check for traversal patterns both before and after each decode pass, and also catch non-standard encodings like `..%c0%af` (overlong UTF-8 slash) by checking for suspicious byte sequences rather than relying on a single decode. A naive single-pass `urllib.parse.unquote` will miss double-encoded and overlong-UTF8 payloads, so iterate decoding until stable or use a denylist covering all listed variants. On rejection, print `REJECTED_<word>` and exit 1; on a clean simple filename, print `SAFE` and exit 0. Test against both provided corpora before finalizing.
