This is a known-plaintext attack on a repeating-key XOR cipher: read `generate_token` in the C++ source — it XORs each plaintext byte with `key[i % key.length()]` and hex-encodes the result. For each intercepted `username : hex_token` pair, hex-decode the token and XOR it byte-by-byte against the known username string; each resulting byte equals a byte of the repeating key at position `i % key.length()`. Try different candidate key lengths (the pairs are short, so brute-force lengths 1-8) and check which length produces a consistent, printable, repeating key across all three known pairs — that recovered string is your secret key. Save it to `key.txt`, then reuse the same XOR-and-hex-encode logic (by hand, in Python, or in a small script) to forge the token for `system_admin` and save it to `forged_token.txt`. For the injection: note that on failed validation the code builds a shell command string by directly concatenating the raw `username` into `system()` — craft a `username` value containing shell metacharacters (e.g. a semicolon or backticks) so it breaks out of the `echo '...'` context and appends `touch /home/user/investigation/pwned` as a second command; save that exact payload string to `payload.txt`.
