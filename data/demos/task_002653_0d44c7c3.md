Work through the pipeline stage by stage and validate each before chaining them into the bash script. First extract frames with `ffmpeg -i /app/radar_scan.mp4 -vf fps=10 -pix_fmt gray /home/user/frames/frame_%04d.pgm` and confirm the numbering/timing convention so you can map a `T:<ms>` timestamp to the right frame index (e.g. ms/100 at 10fps, watch off-by-one between 0- and 1-indexed frame files). Parse `/app/sensor_meta.txt` with a regex matching lines like `\[VALID\] T:(\d+) S:ACTIVE`, collecting only the valid timestamps. In the C++ program, read each selected PGM (skip the P5 header, then raw grayscale bytes), normalize intensities to [0,1], and compute the intensity-weighted centroid (sum(x*intensity)/sum(intensity), same for y) to locate the bright blob — then take Euclidean distance from the frame's geometric center. Write the CSV with the exact header. Wire steps 1-3 into `/home/user/etl_pipeline.sh` in sequence (extract frames, parse metadata, compile+run the C++ program), and separately write the cron line for "every 5 minutes" (`*/5 * * * *`) plus the script path into `/home/user/cron_schedule.txt`.
