Write a C++ program that opens /home/user/research_data.db via sqlite3_open, then issues a single SQL query using three self-joins of citations (c1 JOIN c2 ON c1.target_id = c2.source_id JOIN c3 ON c2.target_id = c3.source_id AND c3.target_id = c1.source_id) combined with joins to papers on all three ids to filter year >= 2020, rather than trying to compute cycles in application code — the database engine can find 3-cycles efficiently this way. To avoid duplicate/rotated triangles (A-B-C, B-C-A, C-A-B all being the same cycle), add a WHERE clause enforcing c1.source_id is the strict minimum of the three ids (or simply collect all matches, then normalize each triple by sorting its three ids ascending and dedupe using a set/sorted structure in C++ before writing output). Iterate the sqlite3_stmt results with sqlite3_step, insert each normalized (min,mid,max) triple into a std::set<std::tuple<int,int,int>> to get uniqueness and natural lexicographic ordering, then write them tab-separated to /home/user/cycles.tsv with no header. Compile with `g++ -std=c++17 extract_pattern.cpp -lsqlite3 -o extract_pattern` and run it before finishing so the output file actually exists on disk.
