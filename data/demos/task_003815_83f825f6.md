Do the video/extraction steps first since they're mechanical: `mkdir -p /home/user/frames` then `ffmpeg -i /app/sensor_feed.mp4 -vf fps=1 /home/user/frames/%04d.jpg`, then for each produced JPEG in numeric order get its byte size with `stat -c%s` (or `wc -c`) and write either the size or the literal string `NaN` (if size < 5000) one per line to `/home/user/raw_sizes.txt`. For `process_stream.sh`, implement the state machine carefully with `awk` (simplest for floating history + arithmetic), maintaining a rolling buffer of the last 3 *output* ImputedValues (not raw inputs): on a numeric line, ImputedValue = that integer; on `NaN`, ImputedValue = floor(mean of up to the last 3 outputted ImputedValues, or 0 if none exist yet) — use integer truncation via `int()` in awk, not rounding. After computing/updating the value, shift it into the 3-slot history, then FilterValue = `1*newest - 2*prev1 + 1*prev2` if at least 3 values have been output so far, else 0. Print `ImputedValue,FilterValue` per input line. Test your script against small hand-crafted sequences (mixing NaN and numbers) and manually trace the expected output before trusting it, since it will be fuzzed against a hidden reference implementation — edge cases (first line, first two lines, consecutive NaNs at the start) are where naive attempts diverge. Finally pipe `/home/user/raw_sizes.txt` through the script into `/home/user/processed_metrics.csv`.
