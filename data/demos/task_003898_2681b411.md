Diagnose before rewriting: run `make` and read the linker error carefully — it will point to an undefined symbol, meaning the function name called from `main.c` doesn't match the one defined in the implementation file (check the header declaration against both). Fix the naming mismatch first and confirm it links. Then reason about the recursive/iterative sequence logic itself: a stack overflow on certain inputs almost always means a termination-decreasing step was inverted (e.g. an odd-branch that should shrink `n` is instead growing it), so trace the recursion by hand on a small odd input to find where `n` fails to move toward the base case, and fix the arithmetic rather than adding recursion depth or converting blindly to iteration unless needed. After fixing, compile and manually diff your program's output against `/app/oracle_bin` across a spread of inputs (small, large, edge case 0) to build confidence before considering it done — don't rely on a single test value.