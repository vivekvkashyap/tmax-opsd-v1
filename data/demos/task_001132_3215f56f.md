Parse `/home/user/export.txt` streaming line-by-line (don't slurp then hold big decoded payloads in memory) — split each line on ` | ` and then on `:` to pull `STATUS` and `FILE`, filtering for `STATUS:OK` exactly and filenames ending in `.bin`. For each match, base64-decode the `PAYLOAD` value (standard `base64.b64decode`, not urlsafe, since the payloads use standard base64 charset/padding) and feed the decoded bytes through `hashlib.sha256().hexdigest()` — do this without writing the binary to disk. Collect `(filename, hexdigest)` pairs, sort by filename, and write each as `<hash>  <filename>` (exactly two spaces, matching `sha256sum` output format) to `/home/user/valid_bins.manifest`. Double check your STATUS and extension filters against every record in the file so you don't accidentally include a `FAIL`/`CORRUPT` status or a non-`.bin` file, and don't include a trailing blank line unless the format calls for it.
