Start by OCR'ing `/app/experiment_schema.png` (e.g. `tesseract` or a Python OCR library) to recover the schema/tolerance text embedded in the image — do not assume its contents, read them off the image and use them to inform formatting/tolerance decisions in your C++ code. Since correctness is judged by bit-exact fuzz equivalence against a black-box oracle at `/opt/oracle/pipeline_ref`, treat the oracle as a spec to reverse-engineer: run it interactively on small, hand-crafted stdin inputs (including edge cases like missing size/metric fields) and diff its exact stdout byte-for-byte against your own program's output as you iterate — don't just eyeball behavior. Implement the metadata parsing so integer fields (artifact size) stay in an integer type end-to-end and are never promoted to float/double just because a metric value is missing/NaN elsewhere in the same record — keep missing-value handling and the numeric type per-field, not shared. For the embedding/CV/confidence-interval logic, mirror the oracle's exact output formatting (field order, decimal precision, whitespace) since fuzz equivalence checks exact string match, not just numerical closeness. Compile with the exact command given and iterate against random-input diffs until outputs match across many generated cases.