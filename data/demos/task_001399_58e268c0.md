Do the three sub-tasks in order since later steps depend on values recovered earlier. First, run `tesseract /app/config_params.png stdout` (or convert/preprocess with imagemagick first if OCR is noisy) to read off the `EMA_ALPHA` and `MAX_PAYLOAD` values printed in the image — use exactly the numbers you OCR, don't assume typical defaults. Second, locate the crash transaction ID in the memory dump by searching for the literal marker: `grep -a -o "CRASH_TX:[0-9a-f]\{32\}" /app/memory.dmp` (use `-a` to force text-mode search on a binary file) and write only the 32-hex-char ID after the prefix into `/home/user/crash_tx.txt` with no trailing prefix or newline surprises. Third, write `sanitiser.c` to read the whole log file, then evil-flag it if it contains the exact crash TX string, OR if it contains a `SIZE:<number>` where the number (parse as a 64-bit/long type to avoid the very overflow bug you're guarding against) is negative or exceeds the OCR'd `MAX_PAYLOAD` — return exit code 1 for evil and 0 for clean via the process's actual exit code, not just stdout text. Compile with gcc and validate against both `/home/user/corpora/clean/` and `/home/user/corpora/evil/` with a small loop checking `$?` for every file before considering it done.