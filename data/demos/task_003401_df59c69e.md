Write logrotate.conf targeting /home/user/logs/api.log with a block containing `size 1k`, `rotate 2`, `compress`, and `delaycompress` together (delaycompress alone is a no-op without compress). Since you're not root, run `logrotate -s /home/user/logrotate.state -f /home/user/logrotate.conf` (use `-f` to force rotation if size-based triggering doesn't fire in a single manual run, and always pass `-s` to point at your own state file instead of the default /var/lib one you can't write). Confirm api.log.1 actually appears before moving on. For parse.sh, chain `grep` for lines containing both `[API]` and `ERROR` (e.g. `grep '\[API\]' file | grep 'ERROR'`), then use `grep -oE '[0-9]+ms'` or `awk`/`sed` to pull just the numeric part before "ms" from the last field, and pipe the numbers into `awk '{sum+=$1; n++} END {print sum/n}'` to print only the bare average. For port forwarding, write proxy.sh with `socat TCP-LISTEN:9090,fork,reuseaddr TCP:127.0.0.1:8000 &`, launch it, then `curl -s http://localhost:9090/health` and redirect the body into health_check.txt to prove the forward works end-to-end.
