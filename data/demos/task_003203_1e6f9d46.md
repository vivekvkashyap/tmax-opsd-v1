Treat this as four separate defects to isolate one at a time rather than a single rewrite. First, just try running `pipeline.py` and fix import/environment errors as they surface (missing packages via `pip install`, misconfigured paths) until it executes at all. Second, look specifically at how concurrent workers write results — a common race is multiple threads/processes appending to a shared Python list or writing to the same file without synchronization; fix by having each worker return its result and aggregating in the main thread/process (or protect shared state with a lock/queue), and ensure the pool is properly joined so no work is silently dropped. Third, for the iterative weighted-centroid solver, treat `/app/reference_oracle` as a black-box ground truth you can invoke to get correct numeric outputs (`./reference_oracle input output`) and diff against your pipeline's intermediate values to locate divergence — suspect the learning rate and iteration count/convergence criterion first, since a too-large learning rate causes oscillation/non-convergence. Fourth, check for `float32` vs `float64` usage causing precision drift; switch any numpy dtype to `float64` (matching the oracle's `double` precision). Iterate: run the pipeline on `data.csv`, compute MSE of your `output.csv` against the oracle's output yourself, and keep narrowing the gap until well under the required threshold before considering it fixed.
