Build this as three cooperating pieces and get each working in isolation before wiring them together: (1) an HTTP server on `127.0.0.1:8080` with `/sync` and `/query` routes, (2) a WebSocket client that connects to `ws://127.0.0.1:8081/stream` only when `/sync` is invoked, and (3) SQLite + Redis state management. In the WS handler, process messages in order and keep a live schema map per table (columns discovered so far) so you know how to interpret later patches/migrations: on `schema`, `CREATE TABLE` with the given columns typed appropriately (int→INTEGER, string→TEXT), first column as PRIMARY KEY; on `base_data`, `SET` the full JSON object in Redis under `<table>:<id>` and `INSERT` a row into SQLite; on `patch`, apply the RFC 6902 JSON Patch to the Redis-stored baseline object (use a library, e.g. Python's `jsonpatch`, rather than hand-rolling op handling — hand-rolled patch application is the most common source of subtle bugs), persist the patched object back to Redis, and `UPDATE` the corresponding SQLite row's changed columns; on `schema_migration`, run `ALTER TABLE ... ADD COLUMN` and update your in-memory schema map. Close the WS connection and respond 200 only after receiving `{"type":"done"}`. For `/query`, execute the given SQL against the same `data.db` connection/file and return rows as a list of column-keyed objects. Test by curling `/sync` then `/query` and comparing row contents manually.