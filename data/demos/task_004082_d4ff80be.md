Treat /app/solve_ode as a black box: write many small config files sweeping alpha, beta, gamma across negative/zero/positive ranges (including boundary values near where alpha*beta ≈ gamma), run each through `timeout 2 /app/solve_ode <file>; echo $?` in a loop, and log the exit status/crash type (segfault, FPE, timeout/hang) alongside the parameter values. Look for the pattern by isolating one variable at a time — e.g. fix beta/gamma and vary alpha to find the sign boundary, then fix alpha/beta and vary gamma. You should notice two independent crash boundaries: one purely on alpha's sign, and one on the relationship between the product of two parameters versus the third. Once you've deduced both conditions from your black-box sweep (don't just guess — confirm each boundary with test cases straddling it), write /home/user/filter.sh so it parses `alpha=`, `beta=`, `gamma=` lines from the given config file (grep/sed/awk to extract the values) and evaluates the two conditions purely in Bash/awk/bc floating-point comparisons — exit 1 if either unsafe condition holds, exit 0 otherwise. Never invoke /app/solve_ode from inside filter.sh itself; only use it for your own exploratory testing.