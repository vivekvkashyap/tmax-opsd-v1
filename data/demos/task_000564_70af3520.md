Tackle this in independently-testable stages. First write and unit-test `filter.c`'s `validate_math` against your own valid/invalid strings: enforce max length 50, an allowed character set (digits, `+-*/()`, spaces), and paren nesting depth tracked with a counter that rejects if it goes negative, exceeds 5, or doesn't return to 0; compile as a shared library (`gcc -shared -fPIC -o libfilter.so filter.c`). Next fix the Rust borrow-checker error in `main.rs` — likely a value borrowed from a `TcpStream` read buffer being held/returned past the buffer's scope; own the data (clone/convert to owned `String`/`Vec<u8>`) before it outlives the borrow. Wire the FFI call via `extern "C"` with a null-terminated `CString`. Write a `Makefile` building the `.so` first, then `cargo build --release` linked against it so the release binary finds `libfilter.so` at runtime (e.g. rpath). Configure nginx as a reverse proxy on 8080 to `127.0.0.1:3000`, start the service, and test `/eval` end-to-end with a clean and an evil sample before the full corpora run — when invoking `legacy_calc`, pass the expression as a single argv element via subprocess (never through a shell) to avoid reintroducing injection risk.
