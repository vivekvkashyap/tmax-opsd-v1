Work through the pipeline stage by stage and print intermediate shapes/counts to sanity-check before moving on. Load the CSV with pandas, drop rows where `text` is NaN or `== ""` (careful: `dropna` alone won't catch empty strings, you need an explicit mask), and record `initial_rows`/`cleaned_rows` from `len(df)` before/after. For `author_age`, first build a boolean mask of valid ages (`15 <= age <= 100`, non-NaN), compute the median over only that valid subset, then use `.fillna()`/`.mask()` to replace both NaN and out-of-range ages with that median — a common bug is computing the median after imputation instead of before, which biases it. Vectorize TF-IDF with `TfidfVectorizer(max_features=50, lowercase=True)`, densify with `.toarray()`, then fit `PCA(n_components=2, random_state=42)` on that dense matrix and sum `pca.explained_variance_ratio_`. Build the output DataFrame with columns in the exact order `id, pca_1, pca_2` and write with `to_parquet` (need `pyarrow` or `fastparquet` installed). Cast all logged metrics to native Python `int`/`float` before `json.dump`, since numpy scalar types aren't JSON serializable and will raise or silently misformat.
