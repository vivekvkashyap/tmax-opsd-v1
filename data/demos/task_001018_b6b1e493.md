First unblock the vendored package: open `/app/vendored/fast_vocab-1.2.3/setup.py` and its tokenizer module, look for a `packages=[...]` list whose string doesn't actually match the real package directory name (a typo) and fix it to reference the true package name, and separately look for an `if` statement in the tokenizer missing its trailing colon (a plain syntax error) — fix both, then `pip install -e /app/vendored/fast_vocab-1.2.3` and confirm `import fast_vocab` (or `from fast_vocab.tokenizer import tokenize`) works before writing any pipeline code. In `train_evaluate.py`: load `/app/data/dataset.csv`, tokenize `text` via `fast_vocab.tokenize`, vectorize with `TfidfVectorizer`, fit one `LogisticRegression` on the full matrix, then do 200 bootstrap iterations — each iteration draws `n` indices with replacement (`numpy.random.choice(n, n, replace=True)`), the out-of-bag set is the indices NOT drawn that round, score the already-fitted model's accuracy on that OOB subset, and write all 200 floats one-per-line to `/home/user/oob_accuracies.csv`. In `calculate_ci.R`, read that CSV, compute `mean()` and the 2.5th/97.5th percentiles via `quantile()`, and emit `{"mean":..., "ci_lower":..., "ci_upper":...}` to `/home/user/experiment_results.json` using `jsonlite::toJSON`/`write`. The mean OOB accuracy must clear the required threshold, so if it's too low check that tokenization and vectorization are working end-to-end (not producing empty/degenerate features).