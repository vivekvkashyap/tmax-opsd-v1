Build the pipeline as a sequence of well-isolated steps and test each against both a clean and an adversarial record before wiring it all together. Fetch `http://localhost:8080/config.json` once at startup (requests or urllib) to get `forbidden_tags`, lowercasing them for case-insensitive matching against each record's `description`. For encoding validation, don't just check `isinstance(str)` — decode bytes with `errors='strict'` in a try/except, or check for the unicode replacement character `�`, since a record that already parsed as JSON may still contain mangled/invalid sequences. For dedup, connect to Redis (localhost:6379, db=0) and use `SADD` on the `seen_ids` set, checking its return value (0 means already present, so drop) rather than a separate SISMEMBER+SADD which risks a race — but either works if used correctly. Strip `title` whitespace only on accepted records. Build the exact report string with the input file's basename (os.path.basename) and RPUSH it to `processing_reports`. Test explicitly with malformed UTF-8 bytes, forbidden-tag text, and pre-seeded duplicate IDs to confirm each is rejected, and with clean records to confirm 100% pass-through.