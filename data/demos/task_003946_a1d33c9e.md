Stream `/home/user/raw_translations.jsonl` one line at a time with a `for line in open(...)` loop and `json.loads` each line; never read it whole or use pandas. Apply the validation gate first: keep a record only if `status == "approved"` AND `chars` is a positive integer (`isinstance(x, int) and x > 0`, and beware booleans passing as ints). Skipped records must not affect anything downstream. Maintain a separate sliding window per language using `collections.deque(maxlen=3)` keyed by `lang`: on each valid record, append its `chars`, then compute the rolling average over the current deque contents (which auto-covers the "fewer than 3" case) and `round(avg, 2)`. The two common mistakes are mixing all languages into one window and letting invalid records enter the window. Write rows in the original chronological order as you process them to `/home/user/rolling_stats.csv` using `csv.writer`, with the exact header `ts,lang,chars,rolling_avg`. Emit `chars` as the integer and the rolling average as-is from `round`.
