This is a fuzz-equivalence task against a black-box binary, so treat the reverse-engineering as the real work, not the given LCG description alone — verify it empirically before trusting it. Run `/app/vol_estimator` with small, controlled inputs (e.g. seed=1, N=1..3, a fixed R) and manually recompute what the LCG-based x/y/z sequence and inside-intersection test would produce by hand or in a small throwaway script, comparing against the binary's actual output, to confirm the exact bit-level behavior (32-bit modulus via masking, the order x,y,z, the `-R + 2R*val/2^31` mapping, and the `<= R*R` boundary condition with `&&` between the two cylinder constraints). Only once your understanding matches observed outputs for several cases, write `/home/user/fast_vol.c` using unsigned 32-bit integer state matching the same recurrence and masking, compile with `gcc -O3`, and diff its output against the legacy binary across a range of seeds/N/R values (including edge cases like N=0 or R near boundary) before considering it done. Separately, write `/home/user/validate.py` to sweep N from 1000 to 10000 by calling the legacy binary via subprocess, compute Vest and the analytical 16/3*R^3 volume, and save the absolute-error plot to `/home/user/convergence.png`.
