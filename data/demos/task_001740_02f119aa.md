Write this as a straightforward Python code generator driven by `sys.argv[1:4]` (vm_name, timezone, admin_email) — no need for templating libraries, just build the exact strings with f-strings and write them verbatim, since the grading is a strict string match. Create the configs directory with `os.makedirs(..., exist_ok=True)` first. For the fstab and aliases files, write exactly one line each matching the specified format character-for-character (watch spacing and punctuation). For the apply.sh script, build a multi-line string containing the shebang, an `export TZ="<timezone>"` line, the `read -p "Proceed with hardening <vm_name>? (y/n): " confirm` line verbatim, and a conditional (`if [ "$confirm" = "y" ]; then ... fi`) that echoes the exact required message — remember this file's content includes `$confirm`, which must NOT be expanded by Python (use a plain string, not an f-string, for that part, or escape braces carefully). After writing, `os.chmod(path, 0o755)` on the apply script. Do not execute the generated script — only create and chmod it.
