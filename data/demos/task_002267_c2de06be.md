First transcribe `/app/incident_alert.wav` to recover the failing node's IPv4 address — use `whisper-cli`/`ffmpeg`+a speech-to-text model, or listen carefully if manual transcription is feasible, converting spoken digits ("ten dot zero dot forty dot fifty-five" style phrasing) into the correct dotted-quad literal; don't guess an IP. Build the C++ daemon with two concurrent responsibilities: (1) a raw POSIX-socket HTTP/1.1 server on 127.0.0.1:8080 that parses the request line for `GET /api/v1/fault` and replies with a well-formed response including `Content-Type: application/json` and a correct `Content-Length` header matching the exact JSON body byte length; (2) a background thread or loop (e.g. using `stat()`/`std::filesystem::file_size` polled periodically) that watches `/home/user/net_logs/traffic.log`, and once it exceeds 500 bytes, atomically `rename()`s it to `/home/user/net_logs/archive/traffic_<epoch>.log` (using `time(NULL)`) and immediately `chmod()`s that archive file to exactly `0444`. Create the `archive/` directory before starting, run the daemon detached/backgrounded so it survives the invoking shell exiting, and test both paths yourself (curl the endpoint, then write >500 bytes to the log and watch it rotate) before considering the task done.
