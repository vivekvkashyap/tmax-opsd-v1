Handle the symlink-loop hazard first since it's the part naive recursive walkers get wrong: don't use `os.walk(followlinks=True)` blindly (it will infinite-loop on the self-referential symlink directory) — instead do your own traversal that tracks visited `(st_dev, st_ino)` pairs (via `os.stat` on the resolved path) in a set, and skip any directory/file whose resolved identity you've already seen, which also naturally handles deduplicating `app_link.conf` against `app.conf` if they resolve to the same inode. Sort discovered files by their path relative to `config_root` before processing, alphabetically. For each file, `open()` it, `mmap.mmap(fd, 0, prot=mmap.PROT_READ)` to read bytes, and note the original size (`len(data)`) for the manifest before compressing. Implement RLE by scanning consecutive identical bytes/chars, emitting `count[char]` for each run capped at 9 — when a run exceeds 9, split it into multiple `9[char]` chunks plus a remainder chunk (e.g. 12 identical chars = "9x" + "3x"), and be careful this splitting logic is exactly reproducible since the grader checks the literal encoded string. Build the whole archive content in memory or a temp file, write it to a `tempfile.NamedTemporaryFile(delete=False)` in the same directory as the destination, then `os.replace(tmp_path, '/home/user/backup.ccf')` for atomicity — never write directly to the final path.