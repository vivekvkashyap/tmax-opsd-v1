Start `/app/start_services.sh &` first so the two upstream mock services on 9001/9002 are up, then poll them once with curl to confirm before writing your app. In your Flask/FastAPI service, fetch `/edges` and `/nodes`, build a `networkx.DiGraph` (or your own adjacency map) with edges waiter->holder. For `/api/deadlocks`, use `networkx.simple_cycles` (or a manual DFS-based simple-cycle finder) to enumerate all simple cycles — then for each cycle, rotate its node list so it starts at the lexicographically smallest tx_id (careful to preserve the existing cyclic direction/order, not re-sort the nodes). Sort the resulting list of cycles by `(len(cycle), cycle)` using Python's natural tuple/list comparison for the lexicographic tiebreak. For `/api/pagerank`, call `networkx.pagerank(G, alpha=0.85)`, round each score to 4 decimals, join in user metadata (default `"unknown"` when absent), sort by `(-score, tx_id)`, then implement 1-indexed pagination by slicing `[(page-1)*limit : page*limit]` and include `total_nodes` as the full node count before pagination. Run the API in the background (e.g. `nohup python3 etl_api.py &`) and verify it responds on port 8080 before finishing.
