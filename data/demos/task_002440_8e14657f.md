Work through the five requirements as independently verifiable steps. 1) Use `jq '.credentials.api_key = "***MASKED***" | .credentials.password = "***MASKED***"' config.json > config_secure.json` so only those two nested keys change and everything else (including `credentials.user`) is preserved exactly. 2) Apply the patch with `cd /home/user/project && patch -p0 < security.patch` (check the patch's path prefix/strip level against its header — `-p0` vs `-p1` — by inspecting the `---`/`+++` lines first) so `src/server.js` gains the exact added line. 3) Write `ci_check.sh` using `grep -rq 'SECRET_' /home/user/project/src/` and exit 1 if found, exit 0 otherwise (note: `grep -q` already returns the right exit codes for a simple `if`/`exit $?` pattern), then `chmod +x` it. 4) Find the leaking file with `grep -rl 'SECRET_' /home/user/project/src/` — expect it to be a file separate from the one you already patched — and delete it with `rm`. 5) Write only its basename (via `basename`) to `deleted_file.log`. Do steps 3/4 in the right order: write the checker first, then delete the leak, then re-run the checker to confirm it now exits 0.