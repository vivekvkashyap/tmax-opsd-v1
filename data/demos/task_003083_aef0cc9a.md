The task explicitly tells you the algorithm family (mean + sample variance) and warns about catastrophic cancellation with large-offset data — treat this as a strong hint to implement Welford's online (single-pass) algorithm rather than the naive sum/sum-of-squares formula, since only an incremental mean-and-M2 update reproduces bit-exact f64 results under floating-point drift. In Rust: read stdin as raw bytes, chunk into 8-byte little-endian doubles (`f64::from_le_bytes`), and maintain running `count`, `mean`, and `m2` exactly per the textbook Welford update (`delta = val - mean; mean += delta / count; delta2 = val - mean; m2 += delta * delta2`), computing `variance = m2 / (count - 1.0)` for `count > 1`, else `0.0`. Write both doubles back to stdout with `to_le_bytes`. Before trusting your implementation, confirm identical output against the actual `/app/astro_filter` binary on a handful of crafted inputs (including a large-mean, small-variance dataset) — piping the same bytes to both and comparing with `cmp`/`diff` — since operation order matters for bit-exactness. Scaffold with `cargo new /home/user/astro_fast` and build with `cargo build --release`.