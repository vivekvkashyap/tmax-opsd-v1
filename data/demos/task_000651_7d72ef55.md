Follow the spec literally and exactly — this task is graded on bit-for-bit reproducible output, so any deviation from the stated procedure (seeding, sample sizes, worker distribution) will change the result. Load both sample files with e.g. `numpy.loadtxt`, compute the baseline distance once via `scipy.stats.wasserstein_distance(A, B)`. For the bootstrap, use `multiprocessing.Pool(4)` and `pool.map` over `range(1000)` so each iteration index `i` is passed into the worker function; inside the worker, set `numpy.random.seed(42 + i)` as the very first random-related call (before any resampling), then draw a with-replacement resample of A (size == len(A)) and of B (size == len(B)) — use `numpy.random.choice(len(A), len(A), replace=True)` style indexing so the seeding controls the exact draw — and compute the Wasserstein distance on the resampled pair. Collect all 1000 bootstrap distances, take the 2.5th and 97.5th percentiles with `numpy.percentile`, and write `Distance:`, `CI_Lower:`, `CI_Upper:` lines to `/home/user/results.txt`, each value formatted to exactly 4 decimal places. Guard multiprocessing code with `if __name__ == "__main__":` so it runs correctly, then execute the script and inspect `results.txt`.