Write this as a straightforward single-pass C program: open `/home/user/dataset.csv`, skip the header line, then for each row parse `id,feature_x,target` with `fscanf`/`sscanf` or manual `strtok` on commas. Treat any row whose `feature_x` equals exactly `-999` (compare as integer/exact match, not a fuzzy threshold) as missing and skip it entirely — don't count it in any denominator. For each remaining valid row, derive `feat_bin = (feature_x > 50) ? 1 : 0`, and accumulate four running counters as you go: total valid rows, rows with target==1, rows with (target==1 && feat_bin==1), and rows with (target==0 && feat_bin==1). After the loop, compute the three probabilities as simple ratios of those counters (guard against division by zero if a class is empty), and write them to `/home/user/bayes_results.txt` with `fprintf(..., "%.4f", ...)` in the exact three-line label format specified. Compile with `gcc` and actually run the binary to produce the output file — don't just leave source unbuilt — then re-open the CSV by hand to double check your counts match your program's before trusting the result.
