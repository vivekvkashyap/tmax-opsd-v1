Run `tesseract /app/sanction_list.png stdout` (or output to a file then read it) to OCR the sanctioned entity IDs; clean up the raw OCR text (strip whitespace, filter to lines that parse as integers) before writing one ID per line to /home/user/sanctioned.txt — don't assume OCR is perfect, print/inspect the raw output first to catch misreads (e.g. confusing 0/O, 1/l) before trusting it.

For detector.c: parse the CSV with fscanf or manual line splitting into (src, dst, amount) triples, build an adjacency list (array of dynamic lists indexed by node ID, capped under 10000) representing directed edges. Load sanctioned IDs from the given file into an array/set. Then for each sanctioned node as a source, run a bounded BFS/DFS up to depth 3 (three hops), and at each visited node check membership in the sanctioned set — if you reach a *different* sanctioned node within depth <= 3, immediately print `EVIL` and exit; if no sanctioned pair is connected within 3 hops from any starting sanctioned node, print `CLEAN`. Be precise about hop-counting (edge traversals, not node count) and about excluding the trivial case of a node "reaching itself". Compile with `gcc -o /home/user/detector /home/user/detector.c`, then iterate: run your compiled binary against every file in /app/clean/ and /app/evil/, checking it prints exactly the right verdict for each, and debug any BFS depth-off-by-one or graph-loading bugs until you hit 100% before finishing.