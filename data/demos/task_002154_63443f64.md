Recover the Trace ID first: run OCR (e.g. `tesseract /app/alert.png stdout`) on `/app/alert.png` and pull out the ID string exactly as printed, don't guess its format. The SQLite file `/app/telemetry/logs.db` is corrupted, so don't try to open it normally — use SQLite's recovery path, e.g. `sqlite3 logs.db ".recover" > recover.sql` (or `.dump`/`.clone` as fallback) then replay the SQL into a fresh database file to get a queryable copy; from there `SELECT dump_text FROM dumps WHERE trace_id = '<recovered-id>'` to pull the goroutine dump for that incident. Read that dump to find the specific fully-qualified function name where goroutines are stuck blocked on a channel send after context cancellation — that function name (e.g. a `pkg/path.funcName` string) is the actual leak signature you must detect. Write a small Go program taking one CLI arg (a dump file path), read its contents, and check for a stack-trace line naming that exact leak function; `os.Exit(1)` if present, `os.Exit(0)` otherwise. Build with `go build -o /home/user/detector`, then self-test against every file in `/app/corpus/evil/` and `/app/corpus/clean/`, confirming exit codes match expectations for all of them before finishing.