Write the Go program in clear stages and check each with a print/debug pass before moving on. Parsing: read `/home/user/input.csv` with `encoding/csv`, and for the `id` column convert the float-looking string to an int by parsing as float64 then truncating/rounding to int (`strconv.ParseFloat` then `int64(...)`) rather than string-manipulating off the decimal point, so values like "3.0" become "3". Imputation: for every row where `val3 == "NaN"`, compute Euclidean distance in `(val1, val2)` space to every *other* row that has a valid (non-NaN) `val3`, track the single closest such row (ties broken by first occurrence in file order, since 1-NN needs a deterministic tie-break), and copy that neighbor's `val3` value into the missing row — do this using only the originally-valid val3 rows as candidates, not other imputed rows, and compute distances independently per missing row (don't let one imputation affect another). Output formatting: format `id` as a plain integer with `strconv.Itoa`/`%d`, and format `val1`,`val2`,`val3` with `%.1f` (exactly one decimal place) using `fmt.Sprintf`, writing the header row first then each data row via `encoding/csv` or manual string joins to `/home/user/output.csv`. Use only the Go standard library throughout.