First recover the required output template: transcribe `/app/memo.wav` with a speech-to-text tool (install `openai-whisper` via pip, or use any available STT CLI) and listen for the dictated template phrase — it will spell out bracket/equals/underscore punctuation verbally ("open square bracket", "close square bracket", etc.), which you must convert into a literal Python f-string format with placeholders for sensor, temp, and timestamp, then append a space and the status field as instructed. Do not guess the template from the task description alone — transcribe the audio to get it exactly right, including the decimal precision on temp. For `etl.py`: read `sys.stdin` line by line (never `.read()`/`json.load()` on the whole stream, to satisfy the streaming/memory requirement), `json.loads` each line, maintain a `dict` mapping sensor -> last valid temp for forward-fill, defaulting to `0.0` when a sensor has no prior reading and its current temp is null; update the stored value only when the incoming temp is non-null. Format each line with your derived template (use `f"{temp:.1f}"`-style formatting matching the memo's stated decimal precision) and `print()` it, ensuring proper UTF-8 handling of the unicode status strings (Python's default stdout encoding is normally sufficient, but flush/print normally). Test against a small handcrafted JSONL sample with consecutive nulls per sensor before trusting the pipeline.