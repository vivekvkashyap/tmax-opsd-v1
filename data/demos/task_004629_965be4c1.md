The core difficulty is bit-exact reproduction of C++'s `std::mt19937` + `std::normal_distribution<double>` sequence, not just "a" normal RNG — implement your simulation in C++ using exactly those standard-library types seeded with `N` (the sequence length) so the underlying stream matches the legacy oracle; do not substitute a hand-rolled or different-language RNG, as distributions differ in how they consume the generator's output (e.g. Box-Muller polar method internals) even with the same seed. Implement the 2x2 Cholesky decomposition (`L00=sqrt(C00)`, `L10=C10/L00`, `L11=sqrt(C11-L10*L10)`) once per request, then run the M=100000-iteration Monte Carlo loop of N correlated steps each, tracking running max-x and min-y per iteration and averaging the `max_x - min_y` values. Wrap this in a simple blocking TCP server on 127.0.0.1:9000 (fork or thread per connection is fine) that reads one CSV line, parses fields, computes the score, and writes back the result formatted to exactly 4 decimal places plus newline. Validate against `/app/legacy_oracle` directly on a few fixed sequence/covariance inputs before wiring up the server, since RNG mismatches are invisible until you diff outputs precisely.