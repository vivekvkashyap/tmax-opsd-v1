Read the binary spec carefully and model it exactly with Go structs and `encoding/binary` (LittleEndian) via a `bytes.Reader` or manual offset tracking — never assume fixed-size records since Name and Code are variable-length, so parse sequentially: magic(4)+version(2)+count(4), then per record ID(4)+NameLen(1)+Name(NameLen)+CodeLen(4)+Code(CodeLen). The critical trap is decoding the bytecode stream itself: you cannot scan byte-by-byte treating every byte as an opcode, because opcodes 0x01 and 0x04 consume a 2-byte operand that must be skipped, while 0x02/0x03/0x05 take no operand. Maintain an explicit instruction pointer, read one opcode byte, then branch on its value to decide whether to advance the pointer by 1 or 3 total. Collect the set of Python-2-specific opcodes (2, 3, 5) encountered per rule, dedupe, and sort ascending. Only include rules with at least one violation in the output, keyed by id/name/violating_opcodes, and write it with `json.MarshalIndent` to the exact output path. Test against a hand-decoded trace of the bytes before trusting your parser.
