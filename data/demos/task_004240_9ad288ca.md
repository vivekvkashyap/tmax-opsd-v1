Treat Part 1 as black-box reverse engineering, not a guess: run `/app/legacy_router_configurator` with several controlled inputs — start with short, distinct, easily-traceable strings for each of the 4 args (varying one at a time) — and study the output's length and character structure relative to the input. Note whether the output length scales predictably with total input length (a strong hint about an encoding like hex, which doubles byte-length), whether swapping/reordering args changes output in a way that reveals a separator or ordering transformation, and whether reversing your input string produces a recognizable relationship to the output. `strings`/`objdump -d` on the (unstripped-locally-compiled-analog is not available, but) the binary itself can supplement this, but empirical black-box testing on a small 4-arg CLI is usually faster than static analysis here. Once you have a hypothesis (e.g. "concatenate args with some separator, then apply transform X, then encode"), implement it in `migrator.py` and validate by diffing your script's output against the real binary's output across dozens of varied random inputs before trusting it — don't stop at the first input that happens to match.

For Part 2, extract timezone from `/etc/timezone`, `LANG` from `/etc/default/locale` (`grep '^LANG=' | cut -d= -f2`), and root filesystem 1K-blocks via `df / | awk 'NR==2{print $2}'`, then call `migrator.py` with those plus the hardcoded region. For Part 3, write the systemd unit with `[Unit]`/`[Service]` (`Type=oneshot`, `ExecStart=/home/user/generate_tag.sh`)/`[Install]` (`WantedBy=multi-user.target`) sections exactly as specified, without starting/enabling it.
