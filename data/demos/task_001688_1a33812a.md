Do not try to run the vulnerable binary against untrusted input to "see what happens" — analyze it statically first. Use `strings /app/legacy_auth_worker` to surface literal substrings (look for things like `session=`, format markers, or hash-related text) and `objdump -d` / `gdb` to trace how it validates `compliance_token` and how it branches on cookie content — you're looking for exactly two things: (1) the checksum scheme used to validate `compliance_token` (likely split-and-hash-compare of some kind — confirm the exact algorithm from the disassembly rather than guessing), and (2) the specific cookie substring pattern that triggers an insecure `system()`/shell-out call. Build `/home/user/auditor.py` to independently re-implement those two checks (token checksum validity, cert chain signature validation, and pattern-matching the dangerous cookie shape) purely in Python — never invoke the legacy binary itself. Iterate against the clean and evil corpora, checking your ACCEPT/REJECT decisions file-by-file against your own reasoning about which specific rule each adversarial file is designed to violate, and sort output lines alphabetically by filename to match the required log format.
