Write a C parser that reads each `.bkp` file's binary header field-by-field in the exact order specified: 4-byte magic `BKP1`, 1-byte filename length N, N raw filename bytes, 4-byte little-endian original size, 4-byte little-endian compressed size, 1-byte encoding flag, then the compressed payload (use `fread`, and for the little-endian u32 fields either read byte-by-byte and shift or verify your platform is little-endian). Decompress the RLE payload by reading `[count][value]` byte pairs and emitting `value` repeated `count` times into a buffer sized to the "original uncompressed size" field. Then convert ISO-8859-1 to UTF-8 one byte at a time: bytes 0x00-0x7F pass through unchanged; bytes 0x80-0xBF become `0xC2` followed by the same byte; bytes 0xC0-0xFF become `0xC3` followed by `(byte - 0x40)` — get this branch exactly right since it's the main source of subtle bugs. Write each decoded file to `/home/user/extracted/<filename>`, track its final UTF-8 byte length, then after processing all archives write one sorted-by-filename summary line per file to `/home/user/extraction_log.txt` in the exact required format. Compile with `gcc` and run over every `.bkp` in `/home/user/archives/`.
