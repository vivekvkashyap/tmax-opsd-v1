Reverse-engineer `/app/net_health_checker` empirically rather than disassembling blindly: run `strings /app/net_health_checker` for clues about format strings and error text, then construct several small synthetic log files by hand (valid lines, a mix of SUCCESS/TIMEOUT/other statuses, malformed lines, an empty file, a nonexistent path) and run the binary on each, recording stdout, stderr, and exit code precisely for every case — this is how you discover the exact output format, the field being counted, the integer-division rounding behavior of the success-rate calculation, and error handling, without guessing. Only after your Python script's output matches the binary's output character-for-character (including trailing newlines, or lack thereof) on all your hand-built test cases should you consider it done — since the verifier will fuzz-test against the real binary across many random logs, so favor building your own difference-checking loop (`diff <(python3 script.py f) <(/app/net_health_checker f)`) over many generated files as a self-check before finishing. Keep the script's argument handling and exit codes matching too, not just stdout. For the logrotate config, write the exact block requested (daily, rotate 7, compress, create mode 0644) to `/home/user/logrotate.d/net_health`, creating any needed directories first.
