Avoid the cross-join bug by building explicit indexes instead of a naive nested loop over all researcher pairs: parse `researchers.csv` into an `id -> name` map, then parse `authorship.csv` into a `paper_id -> set(researcher_id)` map (or `researcher_id -> set(paper_id)`). Look up the target researcher's id by name, get their set of paper_ids, then union together the sets of researcher_ids attached to each of those specific papers — this only pulls in people who actually co-occur on a shared paper_id, not everyone in the dataset. Remove the target's own id from that unioned set, map remaining ids back to names via your first map, sort the names alphabetically (`std::sort` on a `std::vector<std::string>`), and hand-build the JSON array output yourself (since no JSON library is allowed) by concatenating `{"name": "..."}` objects with commas, being careful with the exact key/quote formatting shown in the spec. Compile with `g++ -std=c++17 -o /home/user/coauthor_query /home/user/coauthor_query.cpp`, then run it with the target name argument and redirect stdout to `/home/user/coauthors.json`.
