Transcribe `/app/instructions.wav` first — install/use `openai-whisper` (or `SpeechRecognition`) and run it on the file rather than guessing the API shape; read the transcript carefully for the exact port, route, response fields, aggregation logic (look for phrases implying a running/cumulative sum grouped per entity ordered by time), and any filter threshold on a total. Do not assume standard REST conventions until you've confirmed them from the transcript's actual wording. Then connect to `/app/data/analytics.db` with sqlite3/python and write the required window-function query (e.g. `SUM(revenue) OVER (PARTITION BY user_id ORDER BY event_timestamp)`), combined with a `HAVING SUM(revenue) > <threshold>`-style filter per user derived from the transcript. Build a small Flask/FastAPI app exposing exactly the route and port stated in the transcript, returning a JSON array of objects with the field names specified there, and launch it in the background (`nohup ... &`), then confirm with `curl` that it responds correctly before finishing.