Build and test the pipeline stages independently before wiring them into `run.sh`. In `pipeline.py`: read all three CSVs with `pd.read_csv` and `pd.concat(..., ignore_index=True)`, apply `df.dropna(subset=["target"])` then the inclusive `feature_0` range filter using boolean masking — apply dropna before the range filter, matching the order implied by the spec, since order can affect which rows survive if NaNs exist in feature_0 too. Create `/home/user/processed_data/` with `os.makedirs(exist_ok=True)` before calling `df.to_hdf(path, key="dataset")` (requires `tables`/`pytables` installed). Fit `Ridge(alpha=1.0, random_state=42)` on the cleaned `feature_0..feature_9` columns against `target`, compute training MSE with `mean_squared_error(y, model.predict(X))` and `float(model.coef_.sum())`, and dump both to `metrics.json` with exactly the keys `mse` and `coef_sum`. Only after confirming `python pipeline.py` runs cleanly standalone, wrap it in `run.sh`: export the four thread/hash-seed env vars, then `exec python /home/user/pipeline.py`, and `chmod +x` the script. Rerun via `./run.sh` to confirm reproducibility end-to-end.
