Parse `/home/user/docs/raw_notes.txt` with a regex like `^\[(\d{2}):(\d{2})\]` per line to pull MM:SS pairs, converting to seconds for ffmpeg seeking. For each timestamp, extract a single frame with ffmpeg using accurate seeking (`-ss` placed appropriately, or `-ss <secs> -i video -frames:v 1`), applying `-vf "scale=320:240,format=gray"` and an aggressively low `-q:v` (JPEG quality knob, try values like 20-31) to shrink file size — start high-quality then iterate downward only if the total archive exceeds the byte budget, checking actual output size after each attempt rather than guessing. Build `final_doc.md` by substring-replacing each `[MM:SS]` token with `![MM:SS](frame_MM_SS.jpg)`, leaving the rest of the line untouched.

For the custom archive, write raw bytes yourself (Python's `struct.pack` is ideal): 8-byte ASCII magic `DOCARCH1`, then `struct.pack('<H', count)`, then per file `struct.pack('B', len(name)) + name.encode('ascii') + struct.pack('<I', len(data)) + data` — use only the basename, no directory component. Double-check byte order (little-endian `<`) and that filename length fits in one byte. After building, verify the archive size in code, and if still over 150000 bytes, lower JPEG quality further and re-extract rather than truncating data. Sanity-check by writing a small parser that reads your own archive back and compares against the source files before finishing.