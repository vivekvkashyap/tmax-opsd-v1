First reverse-engineer the schema empirically rather than guessing: run `sqlite3 /home/user/data/experiment.db ".schema"` (and `.tables`) to see the obfuscated table/column names, then `SELECT * FROM t_00X LIMIT 5` on each to infer which table holds entities (id/label/category-shaped columns) and which holds interactions (two id-like columns plus a numeric strength column). Once identified, write `/home/user/process.cpp` using the SQLite3 C API (`sqlite3_open`, `sqlite3_prepare_v2`, `sqlite3_step`/`sqlite3_column_*`) to: load all category-'A' entities into a map keyed by id; load interactions where strength > 5.0 AND both endpoints are in that category-A id set (filter using the in-memory set, not a SQL join, since you must apply both conditions together); build an undirected degree count per entity by incrementing both endpoints for each valid edge. Emit `nodes.csv` (id,label,degree) for every category-A entity including degree-0 ones, sorted by id ascending; emit `edges.csv` (src,dst,value) for valid interactions sorted by src then dst ascending. Compile with `g++ -std=c++17 process.cpp -lsqlite3 -o process` and run it, then diff your CSVs against manual expectations you derive from inspecting the raw data.