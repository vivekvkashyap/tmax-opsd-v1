Work through this as a chain of dependent fixes, verifying each stage before moving to the next. Extract the archive (`tar -xzf /home/user/data/raw_docs.tar.gz -C /home/user/data/extracted/`, creating the dir first) and create `/home/user/www/docs/`. Next fix the build: try `make` in `/app/docserve-1.0`, read the linker error, and note that `cpp-httplib` on Linux typically requires pthreads — add `-lpthread` (and possibly `-pthread`) to the compile line in the `Makefile`. Then implement `setup_docs`: read `/home/user/data/index.csv` line by line (no header, `doc_id,relative_path` per line) with `std::ifstream`, split each line on the comma, and for each row create a symlink with `symlink()` from `<absolute path to /home/user/data/extracted/relative_path>` to `/home/user/www/docs/<doc_id>.txt` — make sure the symlink target is an absolute path so it resolves correctly regardless of the server's working directory. Rebuild with `make`, then launch `./server &` from inside `/app/docserve-1.0` (it hardcodes 127.0.0.1:8080 and mounts `/home/user/www`), and verify end-to-end yourself with `curl http://127.0.0.1:8080/docs/<id>.txt` before considering it done, checking the actual file content matches what's in the extracted archive.