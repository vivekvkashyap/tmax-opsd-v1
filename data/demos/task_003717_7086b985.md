Write the C sanitizer to read the full stdin query into a buffer first (e.g. loop `fread` until EOF into a growable/fixed buffer), then check with `strstr` for the two forbidden literal substrings `USING INDEX stale_rels` and `:CORRUPT` — on a match, print exactly `REJECT\n` and `exit(1)` without touching the child process at all. For safe queries, since you must both write the query to the child's stdin and read its stdout, `popen` alone won't work (it's one-directional) — use two `pipe()` pairs, `fork()`, `dup2` the correct ends in the child before `execl("/app/graph_engine", ...)`, close unused fds in both parent and child, write the query, close the write end so the child sees EOF, then read all of the child's stdout into a buffer. Parse the captured PSV: split the first line on `|` for field names, then split each subsequent line on `|` for values, and emit a JSON array of objects pairing them, escaping any special JSON characters if present. Test against every file in both `/home/user/clean/` and `/home/user/evil/` via `./sanitizer < file` to confirm the exit codes and output format hold for all cases before considering it done.
