Scaffold with `cargo new /home/user/ode_validator --bin`. Implement the Forward Euler recurrence exactly as specified: start y=1.0 at t=0.0, and at each step compute the new value as `y = y + dt * (-0.5 * y)` before advancing t by dt=0.1, iterating so that t=10.0 inclusive is covered (101 total points, watch off-by-one — loop `for n in 0..=100` and derive t as `n as f64 * dt` rather than accumulating floating point t repeatedly, which avoids drift). At each of the 101 points compute the analytical value `(-0.5 * t).exp()`, take the absolute difference from the numerical value at the same step, and track the running maximum. Use only `std` — no crates needed for this. Format the final max error to exactly 6 decimal places (Rust's `{:.6}` format specifier) and write only that string (no extra whitespace/newline issues you haven't checked) to `/home/user/max_error.txt`. Build with `cargo build --release` or `cargo run`, then confirm the file contents by reading it back.