Write the pipeline as a bash script that shells out to inline Python for the pandas-dependent parts. When reading `metrics.csv`, load `user_id` as a string/object column (e.g. `dtype={'user_id': str}` or `keep_default_na=False`) so empty entries stay as `""` instead of pandas silently upcasting the whole column to float via NaN — that's the exact bug the task warns about. Drop rows with empty `user_id` and rows where `feature_y` equals `0` or `0.0` (compare as float after casting) before computing `ratio = feature_x / feature_y`, so you never divide by zero. For the bootstrap step, call `df.sample(n=2000, replace=True, random_state=42)` exactly as specified — any deviation (different seed, sampling before feature engineering, or wrong row order) will change the correlation. Compute Pearson correlation between `ratio` and `target` on that exact sample (`.corr()`), round to 4 decimals, and write only that value to `/home/user/output.txt`. Make the script executable with `chmod +x`, and verify it exits 0 and produces deterministic output on repeated runs before finishing.
