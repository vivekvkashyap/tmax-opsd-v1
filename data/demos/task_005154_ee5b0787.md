Work this as a chained investigation, not independent steps. First OCR `/app/dashboard_alert.png` with `tesseract` to recover the exact fatal error code string, then grep/search for that code inside `/app/monitor.dump` (it's large/binary-ish, so use `grep -a` or read in binary mode) to locate the nearby `PRECISION_THRESHOLD=<value>` signature — don't hardcode a guessed value. For the git bisection: `cd /app/uptime-monitor`, write a small test script that runs `calc.sh` against an input built around the threshold you found and fails when precision is lost (e.g. checks decimal places / diff against expected scale), then run `git bisect start`, mark current `bad` and an early commit `good`, and use `git bisect run <your-test-script>` to let git walk history automatically rather than eyeballing commits; capture the reported bad commit hash into `/home/user/bad_commit.txt`. For the fix, avoid `bc` with a low `scale`/floating doubles — use arbitrary-precision decimal arithmetic (e.g. `bc -l` with high scale, or Python `decimal` called from bash) computing `(total*100)/(days*86400)`. Grading is bit-exact equivalence against the reference calculator described in the task, so match its exact rounding/format (6 decimal places) and self-test your script on several random input files, checking the output format character-for-character before finishing.