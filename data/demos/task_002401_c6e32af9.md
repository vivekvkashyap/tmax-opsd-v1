The key danger here is infinite recursion through symlink loops, so avoid naive `find` traversal that follows directories blindly — use `find /home/user/dataset -type f` (which by default does NOT follow symlinks and only matches regular files, naturally excluding the symlink loop entries) rather than `find -L` or manual recursive globbing that could dereference the loops. For each regular file found, read its first several bytes safely without executing it — use something like `head -c 8 "$file" | xxd` or `od -An -tx1 -N4` to get the hex header, and compare against the ELF (`7F 45 4C 46`) and WAL (`37 7F 06 82` or `37 7F 06 83`) magic byte sequences; for GCODE, check if the file's very first bytes as text literally start with `;FLAVOR:` (careful: a leading space or different casing must NOT match, per the UNKNOWN edge case). Anything not matching one of the three signatures is UNKNOWN. Write `path: TYPE` lines for every file, then pipe the whole result through `sort` before writing it to `/home/user/dataset_inventory.txt` to satisfy the alphabetical-by-path ordering requirement. Test against a small directory with a manually-created symlink loop first to confirm your script doesn't hang.
