Build the pieces bottom-up and test each independently. Compile first: `gcc /home/user/simulator.c -o /home/user/simulator -lm`, and manually run `./simulator -x 0 -y 0` to confirm you can parse the `Runtime: <value>` line (e.g. with a regex or simple split) before writing the optimizer. In Python, wrap a call to the simulator via `subprocess.run(...)`, extract the float, and pass an objective function wrapping that call into `scipy.optimize.minimize(objective, x0=[0.0, 0.0], method='Nelder-Mead')`. Because the simulator's output is noisy, don't average just one call per objective evaluation if it's too jittery — let Nelder-Mead's own robustness handle it, but consider evaluating multiple noisy samples per iteration if convergence is unstable. After optimization converges, round both parameters to 1 decimal place with standard rounding. Then collect exactly 20 runtime samples at the baseline (0,0) and 20 at your rounded optimal point, and run `scipy.stats.ttest_ind(baseline, optimized, equal_var=False)` for Welch's t-test. Write `optimal_x`, `optimal_y`, `p_value`, `t_statistic` to `/home/user/report.json` as plain floats (not numpy types — cast with `float(...)` since numpy floats can break JSON serialization).