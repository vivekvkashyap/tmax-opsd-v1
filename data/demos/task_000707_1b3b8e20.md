Work through each vulnerability class systematically rather than patching ad hoc: (1) SQL injection — replace the string-concatenated query with `sqlite3_prepare_v2` + `sqlite3_bind_text` for the username parameter, verify manually that a payload like `admin' OR '1'='1` no longer alters query semantics; (2) weak hashing — compute SHA-256 of the incoming password using OpenSSL's EVP API (or `SHA256()` from `<openssl/sha.h>`), convert the digest to a lowercase hex string, and compare against the stored `password_hash` column instead of the plaintext checksum; (3) insecure cookie — append `; HttpOnly; Secure` to the `Set-Cookie` header value; (4) reflected XSS — write a small helper that escapes `< > & " '` to their HTML entities and apply it to the `name` param before interpolating into the response body, and confirm by hand that a `<script>` payload comes back escaped. Install `libssl-dev`/`libsqlite3-dev`, compile with exactly the flags given, start `server_fixed` in the background on `8080`, and manually curl each endpoint (bad login, good login, XSS payload) to confirm behavior before writing `audit_trail.json`.
