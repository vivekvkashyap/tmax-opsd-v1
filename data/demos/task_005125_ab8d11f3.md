Work the recovery in order, verifying each stage before moving on. First, force WAL checkpointing so uncommitted writes land in the main DB file: run `sqlite3 data.db "PRAGMA wal_checkpoint(TRUNCATE);"` (or `FULL`) inside the service directory, then confirm with `sqlite3 data.db "SELECT * FROM measurements;"` that the data.db-wal contents are now merged in. Second, treat memory.dump as raw bytes, not text — run `strings memory.dump | grep SECRET_MULTIPLIER` to pull out the cleartext config value without trying to parse the binary structurally. Third, do not trust the .pyc file's logic as correct — decompile it (`decompyle3 calculator.pyc` or disassemble with `python3 -m dis` after loading the code object) to see exactly what formula it computes, but treat the task's stated correct formula (sum of values times multiplier, not plus) as authoritative over what the buggy code does. Finally, write solve.py to independently query the recovered SQLite DB, sum the `value` column, multiply by the extracted multiplier, and write the numeric result as a plain string to final_metric.txt — don't just patch and rerun the broken .pyc, since replicating the exact fixed formula yourself is more reliable and auditable.
