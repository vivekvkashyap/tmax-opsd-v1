Start by running `tesseract /app/diagram.png stdout` (or output to a text file then read it) to OCR the namespace and port values from the image — do not guess them, and expect OCR noise so trim whitespace/punctuation around the extracted `ALLOWED_NAMESPACE:` and `FW_PORT:` values before embedding them in your generated code. Write `validator.c` as a straightforward string-matching program: read the whole manifest file into a buffer, then use `strstr` to check for the required `namespace: <extracted_name>` substring and the absence of the three forbidden substrings (`privileged: true`, `hostNetwork: true`, `hostPath:`), returning exit code 0 only if the namespace matches AND none of the forbidden strings appear, else 1 — this must be simple substring matching since the corpora are adversarial and testing exact-string presence, not YAML semantics. In `ci_pipeline.sh`, compile with gcc, then `chmod 711` the binary exactly (not 755), and iterate both corpus directories logging pass/fail per file for your own sanity, but remember the grader invokes the compiled `/home/user/validator` binary directly, not your log. For `fw_config.sh`, write (but do not execute) the `iptables -t nat -A PREROUTING -p tcp --dport 80 -j REDIRECT --to-port <extracted_port>` command using the OCR'd port value.
