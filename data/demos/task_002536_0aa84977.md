Implement this as a straightforward Python aggregation pipeline over the JSONL, mirroring a NoSQL group/aggregate/sort stage rather than reaching for a database. Stream-read `/home/user/backups_metadata.jsonl` line by line with `json.loads`, filter to `status == "SUCCESS"` first. Build a nested dict (or a dict keyed by `(region, database_name)`) accumulating `total_size_bytes` via running sum and `latest_backup` via comparing ISO-8601 timestamp strings — since these are all UTC `Z`-suffixed ISO strings, plain string comparison sorts correctly, but parsing with `datetime.fromisoformat` (after replacing `Z` with `+00:00`) is safer and avoids surprises with mixed formats. After aggregating, flatten to a list of `{region, database_name, latest_backup, total_size_bytes}` dicts, then sort with a compound key: region ascending, then total_size_bytes descending (`sorted(records, key=lambda r: (r["region"], -r["total_size_bytes"]))`). Write with `json.dump(..., indent=2)` to `/home/user/backup_report.json`, matching key order and names exactly, then actually run the script so the file is produced, and re-check by eye that FAILED/IN_PROGRESS records were excluded from both the sums and the latest-backup calculation.