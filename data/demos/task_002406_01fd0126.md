This is a two-stage task: first decode a hidden spec from a video, then implement exactly what it says. To decode: extract frames from `/app/transmission.mp4` (e.g. `ffmpeg -i transmission.mp4 frame_%05d.png`, or sample per-frame average pixel brightness directly with ffprobe/python+PIL), classify each frame as black (bit 0) or white (bit 1) by average luminance thresholding, concatenate the bits in frame order, group into bytes of 8 MSB-first, and convert each byte to its ASCII character to reconstruct the plaintext instruction message — read that message carefully, it fully specifies `/home/user/recover.py`'s argument format and the exact byte-range transformations (an additive/modular correction on one range, an XOR correction on another) to apply to the hex string. Implement `recover.py` to parse the two CLI args, apply precisely the operations the decoded spec describes to the specified character ranges of the hex string (treat those ranges as big-endian integers), and print the corrected 48-character hex string with proper zero-padding via Python's format specifiers. Since it will be fuzzed for bit-exact equivalence, test with several hand-computed examples before trusting it.