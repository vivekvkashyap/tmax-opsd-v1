Get the checksum computation right first, in isolation, before touching Nginx templating: write the C program to `fread` the entire contents of `/home/user/runner_id.bin` into a buffer, then call zlib's `crc32(0L, buf, len)` — link with `-lz` (e.g. `gcc calc_proxy.c -lz -o calc_proxy`) and print the resulting unsigned integer to verify it looks sane before embedding it in config output. Then have the program `printf` a complete Nginx config to stdout: minimal `events {}`/`http {}` boilerplate with `pid`, `access_log`, `error_log` all pointing into `/tmp/` (so `nginx -t` works without root), a `server { listen 8080; location /logs { proxy_pass http://127.0.0.1:9090; ... } }` block, standard WebSocket headers (`proxy_set_header Upgrade $http_upgrade;` and `proxy_set_header Connection "upgrade";` — get the exact directive names since the verifier greps for them), and `proxy_set_header X-Runner-Checksum <computed_crc32>;` with the checksum value interpolated as a decimal integer, not hex. Redirect the run to `/home/user/nginx_ws.conf`, then run `nginx -t -c /home/user/nginx_ws.conf` and iterate on any syntax errors until it exits 0 — don't just trust the C program's output blindly.