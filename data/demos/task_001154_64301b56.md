Fix the Flask app first: write a small `is_safe_relative(path)` helper that returns True only when the string starts with `/` and does not start with `//` (guard against protocol-relative redirects) — apply it to the `next` query param in the login route, falling back to `/dashboard` whenever it's missing, fails the check, or looks like an absolute URL (e.g. contains `://`). Add the CSP header via an `@app.after_request` hook that sets `response.headers['Content-Security-Policy'] = "default-src 'self'"` on every response, rather than setting it ad hoc per-route, so it's guaranteed present everywhere. For the log analysis, parse `/home/user/access.log` line by line with a regex capturing the IP, HTTP method, path+query, and status code from the Combined/Common log format; an entry only counts as an exploit if method is exactly GET, path is exactly `/login`, its `next` query value starts with `http://` or `https://` (parse the query string properly, e.g. with `urllib.parse`, not substring matching, so a path like `/login?next=/profile` isn't a false positive), and status is exactly 302. Collect matching IPs into a set (to dedupe), sort with a proper IP-aware sort or simple lexical sort of the dotted strings (numeric octet comparison matters if IPs could tie unusually, but standard string sort works for typical dotted quads here), then write one per line. Generate block_rules.sh by iterating that same sorted IP list into `iptables -A INPUT -s <ip> -j DROP` lines, preserving order.
