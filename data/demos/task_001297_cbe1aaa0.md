Read deps.json and compute a topological order manually (or with a small script) honoring the rule "if A depends on B, -lA must appear before -lB": since A depends on B depends on C, the correct link order is `-lA -lB -lC` (the current broken Makefile likely has it reversed or wrong) — reorder the `app:` target's gcc command accordingly and run `make` to confirm the executable builds and linker errors disappear. Run `./app` to confirm it prints "Result: 44" exactly. For test_deploy.py, subprocess-run the binary, capture stdout, compare it to the exact expected string, and on match open the status.log file in binary write mode encoding the string with `'DEPLOY_READY'.encode('utf-16le')` (Python's `open(..., 'w', encoding='utf-16-le')` also works but be careful about BOM — utf-16-le typically omits it, while plain 'utf-16' would add one) — verify by reading the file back and comparing decoded bytes. Finally scaffold a minimal but valid `.github/workflows/ci.yml` with a `test` job whose run step executes `python3 test_deploy.py`, and actually run your Python script once so status.log exists before finishing.