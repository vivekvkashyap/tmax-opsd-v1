Build the pipeline in clear stages so you can inspect intermediate output at each step. Normalize both sources into a common tab/CSV-like stream first: parse `stream1.csv` with `awk -F,` (skip header, skip rows with empty temp field) and parse `stream2.jsonl` with `jq -r` (skip rows where `.v == null`), emitting each as `timestamp,sensor,value` lines into one combined stream. Deduplicate exact-match rows with `sort -u` on the full line (do this before bucketing, since dedup is defined on the raw records, not the aggregated ones). Truncate each timestamp to the top of the hour by replacing minutes/seconds with `00:00` (a `sed`/`awk` substitution on the `:MM:SSZ` suffix) to form the bucket. Group by (bucket, sensor) and average the values — `awk` with a composite key in an associative array (`sum[key]+=value; count[key]++`) computed in an END block is a clean way to do this — formatting each average to exactly 2 decimals with `printf "%.2f"`. Finally sort the aggregated rows by bucket then sensor (`sort -t, -k1,1 -k2,2`) and prepend the header before writing to `/home/user/etl_output/aggregated.csv`. Diff your output against your own hand-traced expectation for a couple of buckets to sanity-check before finishing.
