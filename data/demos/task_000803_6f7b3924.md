Structure `/home/user/curator.py` as: walk `/home/user/incoming_artifacts` recursively with `os.walk`, open each file in binary mode and check `f.read(4) == b'\x7fELF'` — ignore the filename/extension entirely, since some ELF files are deliberately mislabeled. For each real ELF, compute the whole-file SHA256 first, then reopen (or seek to 0) and read in 524288-byte chunks, writing each chunk to `/home/user/repo/chunks/<sha256>_chunk_<NNNN>` with a zero-padded 4-digit index, computing each chunk's own SHA256 as you go (don't reuse the whole-file hash for individual chunks). Accumulate a manifest dict matching the required schema, and after processing everything write it with `json.dump` to `/home/user/repo/manifest.json`. Wrap the whole script's execution so stdout/stderr redirect to `/home/user/repo/curation.log` (e.g. run it as `python3 curator.py > curation.log 2>&1`). Only after the manifest and chunks exist, launch `python3 -m http.server 8080 --bind 0.0.0.0 --directory /home/user/repo &`, capture `$!` into `/home/user/repo/server.pid`, and verify with `curl` that `manifest.json` and a sample chunk are servable and that the chunk's content hashes to the value recorded in the manifest.
