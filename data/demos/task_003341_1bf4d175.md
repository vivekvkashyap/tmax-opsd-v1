Before writing any server code, manually inspect the WAL format: `xxd` a WAL file to confirm the 4-byte "WALR" magic, then hand-parse a record or two (little-endian record id, little-endian length, then that many bytes of payload) so you know your offsets are right. Resolve `/app/wal_data/active.wal` with `readlink`/`realpath` — don't assume it's static, since rotation changes the target — and be prepared to scan older rotated files (likely named/numbered siblings in the same directory) when a requested record id isn't in the active file; list the directory to see the naming convention for rotated files. Write the C++ service as a raw `socket()`/`bind()`/`listen()`/`accept()` loop (no frameworks allowed), parsing just the request line to extract `/record/<id>`, and return either the raw payload with `200 OK` or a `404` with correct `\r\n\r\n` framing and `Content-Length`. Test it directly with `curl http://127.0.0.1:8080/record/<id>` before touching nginx. Only then edit the nginx config's `location /api/record/` block to `proxy_pass http://127.0.0.1:8080/record/` (mind trailing slashes affecting URI rewriting), reload/restart nginx, and confirm end-to-end via `curl http://127.0.0.1:8000/api/record/<id>`.
