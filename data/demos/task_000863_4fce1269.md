Derive the classification threshold empirically rather than guessing a number. First, extract frames from `/app/baseline_dash.mp4` (e.g. with `ffmpeg` or `opencv`) at 1fps, measure the two bars' pixel heights per frame (Speed = left bar, RPM = right bar), and compute their baseline Pearson correlation — this establishes what "normal" correlated telemetry looks like. Then write `/home/user/detector.py` to: load a CSV via `--input`, impute missing values with the column median (`fillna(median)`, handling NaNs from `pd.read_csv`), compute the Speed-RPM correlation (`np.corrcoef` or `df.corr()`), and compare it against a threshold informed by the baseline video correlation, printing exactly `CLEAN` or `EVIL`. Use the small labeled corpora at `/home/user/data/training/clean/` and `/home/user/data/training/evil/` to calibrate and validate the threshold — run your detector against every file in both directories and adjust the cutoff until every clean file prints CLEAN and every evil file prints EVIL, since the hidden holdout set requires 100% accuracy on both classes, not just a reasonable approximation.
