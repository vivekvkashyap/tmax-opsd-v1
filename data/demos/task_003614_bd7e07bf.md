First recover the truncation parameter: run `tesseract /app/config_memo.png stdout` (installing tesseract-ocr if needed) and parse the `SVD_TRUNCATION_K=...` line to get `k` — don't guess a value. Then modify `/app/spectral_denoise.py` to use `mpi4py`: initialize `MPI.COMM_WORLD`, have rank 0 load `/app/raw_spectra.npy` and scatter contiguous slices of the 1000 matrices across the 4 ranks (evenly, e.g. via `np.array_split` or manual slicing), have each rank compute `np.linalg.svd` per matrix, truncate to the top-`k` singular values/vectors, reconstruct (`U[:, :k] @ diag(S[:k]) @ Vt[:k, :]`), then gather results back on rank 0 in the original order before saving to `/home/user/output_spectra.npy`. Wrap only the actual parallel compute region with `MPI.Wtime()` (or similar) on rank 0 and write the elapsed seconds as a float to `/home/user/timing.log`. Validate correctness against `/app/reference_spectra.npy` yourself (compute MSE) before finishing, and run with `mpirun -n 4 python /app/spectral_denoise.py` to confirm both the MSE and timing thresholds are met — a naive per-rank redundant computation of all 1000 matrices would defeat the speedup requirement.
