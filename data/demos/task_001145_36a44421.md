For Stage 1, run `ffmpeg -i /app/screencast.mp4 -vf fps=1/5 /home/user/frames/frame_%03d.png` for one frame every 5 seconds, then `cd /home/user/frames && sha256sum frame_*.png > /home/user/frames_manifest.txt` so manifest paths are relative filenames. For Stage 2, use `find /home/user/docs_raw -maxdepth 1 -name '*.md' -size +512c -exec cp {} /home/user/filtered_docs/ \;` (`-size +512c` means strictly greater than 512 bytes). For Stage 3, write the C++ parser to read the file as raw bytes: validate the 4-byte "TDOC" magic and 1-byte version==1 up front (INVALID_HEADER/exit 1 if too short or mismatched), then loop reading each record's fixed 8-byte header (timestamp+doc_id+content_length) plus variable-length content, checking after every read that enough bytes remain — if not, print already-parsed records, then TRUNCATED and exit 2. Checksum content as a mod-256 sum of unsigned bytes. This is fuzz-tested against malformed/random inputs, so validate byte counts precisely before every read (never read past EOF); test your parser yourself against hand-crafted valid, truncated-mid-header, truncated-mid-content, and garbage-byte files to confirm exact boundary behavior.