Break this into independently verifiable pieces before wiring the API. First, OCR `/app/schema.png` with `pytesseract` and print the raw extracted text so you can see exactly how the `Column/Type/Nullable` table lines are laid out, then parse it line-by-line into a schema list of (name, type, nullable) — don't hardcode the schema from the description, actually derive it from the image so your parser is robust. Second, load `/app/transform_matrix.csv` as a numeric matrix and confirm its row count matches the number of non-identifier numeric feature columns in your derived schema (order matters — top-to-bottom schema order, excluding the first/identifier column). Third, for schema enforcement, use pandas' nullable `Int64` dtype for integer columns marked nullable so missing values stay integers instead of silently upcasting to float — this is the naive attempt's most common failure. For the matmul, build the feature matrix with missing values filled as 0 only for that computation, multiply by the transform matrix to get `proj_1`/`proj_2`, then re-insert true nulls (as JSON null, not NaN) into the final output before serializing to records. Finally wire a FastAPI/Flask `POST /process` on 0.0.0.0:8000 that checks the exact `Authorization: Bearer ds-secret-token` header (401 otherwise) and accepts multipart file uploads.
