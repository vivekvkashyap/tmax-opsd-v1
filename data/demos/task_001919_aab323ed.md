Build this as a pure-awk pipeline rather than reaching for Python. Skip the header and `run_id` column, then for each of the four metric columns compute the population (or sample, be consistent) variance in a single `awk` pass — accumulate sum and sum-of-squares per column, then variance = sum_sq/n - (sum/n)^2 — and compare the four variances to find the column with the max; do this without hardcoding which column "should" win, since the script must generalize to the real 100-row file, not just eyeball it. For the winning column, extract its full 100 values into an array/temp file, then bootstrap: run a loop 1000 times, each iteration using `awk`'s `rand()` (seeded via `srand()`, e.g. seeded from `$RANDOM`/time so runs aren't degenerate) to sample 100 indices with replacement from that array and compute the mean of the resampled values — accumulate each bootstrap mean. Compute the true mean of the full column and the grand mean across the 1000 bootstrap means, round both to 2 decimals (`printf "%.2f"`), and compare `abs(true_mean - grand_mean) < 0.5` to decide Yes/No. Write the four-line report to `/home/user/artifact_report.txt` in the exact specified format/labels, `chmod +x` the script, and actually execute it once to produce the report file.
