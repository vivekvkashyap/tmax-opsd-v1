Treat `/app/sim_oracle` purely as a black-box scoring function you calibrate empirically — never assume its internal formula. First write a quick Python/shell loop that invokes `/app/sim_oracle <sample_file> reference.json` for every file in `sample_clean/` and `sample_evil/`, collecting the printed similarity scores into two lists. Compute basic stats (mean, std) for each group and run a Welch's t-test (e.g. via `scipy.stats.ttest_ind(equal_var=False)`) to confirm the separation is statistically significant, then pick a threshold that cleanly separates the two score distributions (look at the gap between max(evil) and min(clean), or use the midpoint/percentile that satisfies your confidence requirement) rather than hardcoding a guessed number. Only after deriving this threshold from real data should you write the C++ filter: iterate `<input_directory>` with `std::filesystem::directory_iterator`, shell out to `/app/sim_oracle` per file (e.g. via `popen` or `fork/exec` capturing stdout), parse the printed score, compare against your derived threshold, and `std::filesystem::copy` files that pass into `<output_directory>`. Test your compiled `filter_data` against the given sample directories before trusting it on the larger hidden verification corpus — the hidden corpus follows the same generating distributions as the provided samples.
