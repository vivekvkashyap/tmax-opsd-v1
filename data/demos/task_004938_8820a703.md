Follow a fuzz-then-fix-then-repro workflow. First write `/home/user/fuzzer.sh`: generate random small dependency graphs in `Item:dep1,dep2` format (including some with deliberate cycles), write each to a temp db file, and run `timeout 2 /home/user/resolve_deps.sh <target> <db>` capturing whether it hangs (timeout exit code 124) — this demonstrates the hang is caused by unbounded recursion when a dependency graph has a cycle, since `resolve()` never tracks what it has already visited on the current call path. Fix `/home/user/resolve_deps.sh` by threading a "currently visiting" set through the recursion (e.g. pass a colon-separated path string or use an associative array keyed by item name as an additional argument/global), and before recursing into a dependency check whether it's already in that active chain; if so, print `Error: Circular dependency detected for <item>` to stderr and `exit 1` immediately (propagate the failure out of nested calls). Keep normal, non-circular resolution exiting 0. Finally hand-craft a small `/home/user/repro_db.txt` (e.g. A depends on B, B depends on A) that reliably exercises this exact cycle-detection path when resolved.