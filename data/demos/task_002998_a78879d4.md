Treat this as three independent deliverables. For SSH hardening, open `/home/user/.ssh/authorized_keys` and remove only the line whose trailing comment/email is `evil@hacker.com`, leaving every other key line byte-for-byte untouched (use a targeted `grep -v` or manual edit, not a rewrite of the whole file). For the forged token, read `auth_daemon.cpp`'s `generate_token` function carefully: it seeds glibc's `srand(timestamp)`, takes one `rand()` call to get a secret, then XORs each username character with `secret % 256`, hex-encoding the result — replicate this exact sequence (same srand call, same single rand() call, same modulo, same XOR and hex formatting) in a small C++ program compiled and run with `timestamp=1700000000` and `username="admin"`, since the actual byte value depends on your platform's libc `rand()` implementation, not a value you should assume — compute it by actually running the code, then write the resulting hex string to `/home/user/admin_token.txt`. For secure coding, rewrite `generate_token` to compute `SHA256(username + "SecureSecretKey")` using OpenSSL's `SHA256()` from `<openssl/sha.h>`, convert the digest bytes to lowercase hex (e.g. via `std::stringstream` with `std::hex` and zero-padding), and compile with `g++ ... -lcrypto -o /home/user/auth_daemon_fixed`, verifying it links and runs.