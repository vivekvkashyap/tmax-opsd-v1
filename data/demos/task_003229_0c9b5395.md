This is a black-box reverse-engineering task verified by fuzz-equivalence, so derive the exact algorithm from observed behavior rather than guessing at plausible logic. Run `/app/legacy_lb_gen` with a range of hand-crafted inputs — single backend, multiple backends with tied weights, weights that differ only in modulo-5 effect, IPs of varying string length — and diff the outputs to pin down: the exact sort key ordering (try changing one field at a time to isolate whether it sorts by weight first, then IP length, then IP lexically, then port), the header/footer text and exact whitespace/braces, and the per-line format including the `weight % 5` and `weight * 10` transforms and the 4-space indentation. Confirm your hypothesis by predicting output for a new untested input combination and checking it matches before writing code. Then implement `/home/user/lb_gen` (any scripting language) that parses the comma-separated `IP:PORT:WEIGHT` list, applies that exact sort and formatting, chmod +x it, and run it side-by-side against the legacy binary on several fresh random inputs to confirm byte-for-byte match before finishing — since the grader fuzzes with 200 random cases including edge weights/ports.
