Start by running `tesseract /app/architecture.png -` (or convert to a higher-res PNG first with `convert`/`pillow` if raw OCR output is noisy) to recover the four config values — version string, Proxy Port, Route-to address, and Secret Key. Print and sanity-check the OCR text before hardcoding anything, since OCR can mangle digits/underscores; verify the extracted port and key look plausible (numeric port, key with expected punctuation intact) rather than trusting the raw output blindly. Build `/home/user/proxy.py` with Python's `http.server`/`BaseHTTPRequestHandler` (or `socketserver`) binding to `127.0.0.1:<extracted port>`, comparing the `X-Deploy-Auth` header against the extracted secret with exact string equality, returning 401 vs 200 + the exact JSON shape with the extracted upstream address, and appending a log line (using `NONE` when the header is absent) on every request including failed ones. For `/home/user/deploy.sh`: launch the proxy with `&` and `disown`/`nohup`, then run a separate backgrounded loop that wakes every 2s, counts lines in access.log, and when it exceeds 5 lines does a proper reverse-order rotation (.2→.3, .1→.2, then move access.log→.1, then `> access.log` to truncate) creating `/home/user/archive` first — get the shift order right so you don't clobber files. Finally, execute deploy.sh and leave both background processes alive (don't let the script exit and reap its children).
