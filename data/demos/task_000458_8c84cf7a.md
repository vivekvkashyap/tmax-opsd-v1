Start with forensic extraction from `/app/incident_record.mp4`: sample frames across the video (e.g. `ffmpeg -i incident_record.mp4 -vf fps=1 frame_%04d.png`) and OCR each with `tesseract` to locate the frame showing the SSH `ed25519` public key text and the later frame showing the ELF hex-dump/malicious URL — read the exact strings off the frames rather than assuming a value. For SSH hardening, run an unprivileged `sshd` instance (custom config + `-p 2222 -o ListenAddress=127.0.0.1`) with `PasswordAuthentication no`, `PermitRootLogin no`, `KexAlgorithms curve25519-sha256@libssh.org`, and an `AuthorizedKeysFile` for user `user` containing only the exact public key you OCR'd — verify a real SSH client can auth with the matching private key and is rejected otherwise. For the gateway, write `/home/user/gateway.sh` using `socat TCP-LISTEN:8080,fork,reuseaddr ... system:...` (or an nc loop) that parses the `target` query param from the request line: exact match to the extracted malicious URL or any other absolute external URL → `403 Forbidden`; a path starting with `/` → `302 Found` with `Location:` set to that path. Start both services backgrounded, and write the extracted URL then the extracted key as lines 1 and 2 of `/home/user/deployment.log`.
