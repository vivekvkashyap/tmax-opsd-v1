Do the cleaning step in shell using a pipeline that preserves the id/text comma boundary: split on the first comma to isolate `id` and `text`, then apply lowercasing and punctuation stripping only to the text portion (e.g. with `awk -F',' 'NR==1{print;next}{id=$1; $1=""; text=tolower(substr($0,length(id)+2)); gsub(/[^a-z0-9 ]/," ",text); gsub(/ +/," ",text); gsub(/^ | $/,"",text); print id","text}'` or an equivalent sed/tr combo) — the common mistake is running a single global `tr`/`sed` over the whole line and accidentally mangling or dropping the separating comma. In Python, build the vocabulary as a sorted list of unique tokens across all cleaned documents, construct the term-frequency matrix with numpy (rows=docs, cols=vocab), then compute cosine similarity via `dot = X @ X.T` and `norms = np.linalg.norm(X, axis=1)`, dividing `dot[i,j] / (norms[i]*norms[j])`. Iterate only over pairs with `id1 < id2`, round to 4 decimals, sort descending by similarity, and write exactly the top 3 rows with header `id1,id2,similarity` — watch for ties, since multiple pairs can share the same similarity score and any consistent tie-break is acceptable as long as the required pairs appear.