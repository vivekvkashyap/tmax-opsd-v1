Treat `/app/telemetry_processor` as a black box to probe, not something to reverse-engineer statically. Write small scripts to construct minimal valid TLV files (correct "TLVM" header, correct checksum = sum of remaining bytes * 31 mod 2^32) and confirm the processor accepts them; then systematically mutate one property at a time — a TLV record with `Length == 0`, a record whose `Length` exceeds the bytes actually remaining in the file, a record with an unusual `Type` byte, a corrupted checksum, a wrong magic header — and observe which cause a crash, hang (use a timeout when invoking the binary!), or nonzero exit versus which are handled fine, so you build an empirical rule set rather than guessing. Implement `/home/user/sanitiser.cpp` as a static parser that walks the TLV records the same way the processor would: verify magic and checksum first, then iterate records checking each declared `Length` against remaining bytes and rejecting zero-length or out-of-bounds records and any suspicious type value, returning exit code 0 only if the entire file parses cleanly and safely. Compile with `g++ -O2` and validate against self-generated valid/invalid samples (and a timeout-guarded harness) before trusting it on the hidden corpora.
