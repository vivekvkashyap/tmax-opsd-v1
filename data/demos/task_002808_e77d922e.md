First reproduce the hang: `cd /home/user/repo`, checkout `v2.0`, and run `timeout 5 python process.py /home/user/data/input.txt` to confirm it hangs (nonzero/timeout exit). Then bisect: use `git bisect start`, mark `v2.0` (or current HEAD) as bad and `v1.0` as good, and at each step run a quick reproducer like `timeout 5 python process.py /home/user/data/input.txt; echo $?` (a `git bisect run` script wrapping this timeout check works well) to let git automatically binary-search the ~50 commits down to the exact one that introduced the hang. Once found, write the full 40-char hash via `git rev-parse HEAD` (during bisect) to `/home/user/bad_commit_hash.txt`. Then check out `v2.0` again (`git bisect reset` first) and inspect `process.py`'s subprocess handling — a classic deadlock is writing large input directly to a `Popen` child's stdin pipe while also trying to read stdout without concurrent draining, which blocks once the OS pipe buffer (~64KB) fills. Fix it using `subprocess.run(..., input=data, capture_output=True)` (or threads to concurrently write stdin and read stdout) instead of the blocking write-then-read pattern, without changing the external command invoked. Run the fixed script and redirect its stdout to `/home/user/fixed_output.txt`.