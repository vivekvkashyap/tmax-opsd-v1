First fix only the syntax error in `main.go` on `master` (e.g. a missing comma in an argument list) so the code compiles — don't touch `main_test.go`. Then run `git bisect` between the `v1.0` tag (good) and the pre-fix broken `master` tip (bad), but since HEAD won't compile, bisect using a check that only cares about the logic: at each candidate commit, apply/keep the same syntax fix if needed, build, and compare `CalcHealth`'s output for a few sample success/failed/total triples against the mathematically correct `((success-failed)/total)*100` formula rather than against `v1.0`'s literal code, since the regression is a subtle formula change (e.g. dropping the `failed` term) not a crash. Record the exact 40-char hash `git bisect` converges on into `/home/user/bad_commit.sha` with no extra text. Finally, on `master`, correct the formula in `CalcHealth` to `((Success-Failed)/Total)*100`, confirm `go test` passes, and build the binary to `/home/user/metric-calc/calc`.