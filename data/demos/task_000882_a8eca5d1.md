Build the C server in two independently testable pieces before wiring them together. First, the WAV rolling-sum computation: open `/app/audio.wav`, `fseek`/skip the first 44 header bytes, read the remaining bytes as an array of 16-bit signed little-endian samples (be careful with endianness — read raw bytes and combine as `(int16_t)(low | (high << 8))` rather than assuming native struct layout matches), take absolute values, then compute the maximum sum over any contiguous window of exactly 500 samples using a sliding window (subtract the value leaving the window, add the value entering, rather than recomputing from scratch each time — this also avoids the classic off-by-one on window bounds). Second, the `\uXXXX` decoder: scan the incoming line for the literal `\` followed by `u` and 4 hex digits, parse the hex to a codepoint, and encode it to UTF-8 by hand (1-3 byte cases suffice for BMP codepoints) — do this with straightforward string scanning, no external JSON library. Wire both into a simple blocking TCP server (`socket`/`bind`/`listen`/`accept` on 127.0.0.1:7777) that reads one line, decodes it, computes the rolling sum (recompute per request or cache it, either works), and writes back the exact JSON line format specified. Compile with `gcc` to `/home/user/server`, run it backgrounded (`&` with output redirected so the shell doesn't block), and test manually with `nc` or a raw TCP client before finishing.