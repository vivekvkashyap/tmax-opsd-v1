Bring the multi-service stack up methodically and verify each hop before moving to the next: start `redis-server` and confirm it answers on 6379, then edit `/app/backend/.env` so `REDIS_URL` points at `127.0.0.1:6379` (not a container hostname), build and background the Rust backend with `cargo run --release`, and edit `/app/nginx/nginx.conf` so the `proxy_pass` directive targets the backend's actual listening port (8081), then start nginx and confirm an end-to-end curl through port 8080 reaches the backend and gets a 302 with the redirect location echoed. Study the sample attack log to understand realistic bypass techniques (protocol smuggling, decimal/hex IP obfuscation, userinfo `@` tricks, the cloud metadata IP, localhost variants) before designing detection logic — don't rely on a single naive substring check. In the Rust WAF filter, parse each input URL properly (a URL crate helps), resolve/normalize hostnames and any embedded IP-like tokens to catch obfuscated forms, and flag as EVIL any URL whose target resolves to internal/metadata/loopback ranges or that hides its real destination via userinfo/protocol tricks; everything else is CLEAN. Validate against both corpora directories exhaustively before considering the filter final, since it must be 100% accurate on unseen but similarly-styled entries.
