Locate the `for` loop in `/home/user/mutator/src/main.rs` and identify everything inside it that has no dependence on the loop variable: the `fs::read_to_string` call and the `ChaCha8Rng::from_seed(seed)` construction are both loop-invariant and must be hoisted above the loop so the file is read exactly once and the RNG object persists and advances across all 10000 iterations (only the `rng.gen_range` call and mutation logic should remain inside the loop). Do not touch the seed value or iteration count — the fix is purely about *where* initialization happens, not what it initializes to. After editing, `cargo build --release` and run the binary directly first to sanity-check `/home/user/results/output.txt` shows a p_value strictly between 0 and 1 (not exactly 0.0 or 1.0, which is the signature of the original bug). Only then run `jupyter nbconvert --execute --to notebook --inplace /home/user/workflow.ipynb`, which invokes the binary and writes `/home/user/results/summary.txt` — check that file's content afterward to confirm the pipeline actually ran end-to-end rather than trusting a clean exit code.
