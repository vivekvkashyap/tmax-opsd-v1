Fix the build before writing any detection logic: run `cargo build` in `/home/user/wav_filter`, read the linker error carefully — it will point at the missing symbol for `check_header_integrity` from the static lib `libwavcheck.a` — then fix `build.rs` by adding `println!("cargo:rustc-link-search=native=lib");` and `println!("cargo:rustc-link-lib=static=wavcheck");` (adjust the exact directory to wherever the .a file actually lives) so the linker can find it. Once it compiles, implement the WAV-parsing detection in `src/main.rs`: read the WAV header (a crate like `hound` handles this, or parse the RIFF/fmt chunk manually) to confirm it's 32-bit IEEE float PCM, then scan every sample and check `is_nan()` or `is_infinite()`; exit 1 if any malicious sample is found, exit 0 for clean 16-bit PCM or well-formed 32-bit float files. Validate the binary at `target/debug/wav_filter` against `/app/corpus/clean/` (all exit 0) and `/app/corpus/evil/` (all exit 1) before moving on. For the quarantine file, convert/clean it with `ffmpeg` if needed (NaN/Inf samples can break decoders) and run a local speech-to-text tool (e.g. `whisper`) on `/app/quarantine/incident_099.wav`, then write the recognized phrase lowercase and stripped of punctuation to `/home/user/transcript.txt` — listen/inspect the transcription output yourself to confirm it's a coherent short phrase before saving.