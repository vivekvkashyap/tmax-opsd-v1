Treat this as black-box reverse engineering: don't try to disassemble the stripped binary by hand — instead generate diverse synthetic log files (varying line counts, response sizes, occasional malformed lines) and run `/app/log_anomaly_detector <file>` on each, capturing stdout to build an empirical spec. First nail down the normalization: compare a known input line against the printed output line to determine field order, separators (tab vs space), and which fields get dropped (HTTP version) or have brackets stripped (timestamp). Then isolate the anomaly-score line by testing tiny inputs (1-2 valid lines) to reverse the running statistic — try hypotheses like a plain average, EWMA, or RMS-of-residuals, and confirm the smoothing/decay constant by feeding a sequence with a sharp jump in response size and checking how fast the score reacts. Handle malformed lines carefully: feed lines missing tokens and see whether the binary skips them silently in both the normalized output and the score, versus erroring. Write your Python reimplementation to match this observed behavior exactly (same rounding/formatting on the final `ANOMALY_SCORE:` line), then re-verify by diffing your script's output against the binary's on fresh random inputs before finalizing.