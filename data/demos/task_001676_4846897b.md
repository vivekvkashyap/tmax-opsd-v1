Start by reverse-engineering the schema yourself: run `sqlite3 /home/user/backups.db ".schema"` and `SELECT * FROM <table>` on each table to see how jobs and parent/child dependency edges are actually represented (likely a jobs table plus a separate dependency-edge table) before writing any SQL. Then design the query in two clearly separated pieces: (1) a recursive CTE seeded by the row where `name = 'full_cluster_backup'` that walks the dependency edges outward (direction matters — check whether the edge table stores child->parent or parent->child and traverse accordingly) to collect that job plus all its transitive descendants; (2) apply this descendant set as a filter, then compute a window function `RANK() OVER (PARTITION BY suite_id ORDER BY size_bytes DESC)` (standard/competition ranking, not dense) to get `size_rank` per suite. Write this as one SQL string embedded in your Go program using `database/sql` with the `mattn/go-sqlite3` driver, iterate `rows`, and write CSV with `encoding/csv` (or manual formatting) matching the exact header and sort order (suite_id asc, size_rank asc, job_name asc) specified in the task. Set up the Go module (`go mod init`, `go get`) before compiling, and actually run the binary to produce `/home/user/report.csv`.