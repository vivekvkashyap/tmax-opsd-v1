Extract the tarball first and actually read both scripts before touching anything — the bug isn't in `aggregate.sh`'s math, it's that `mcmc_generator.sh` launches `N_SAMPLES` background jobs that append to `raw_output.txt` in whatever order they finish, so the EMA (which is order-dependent) reads that file in racy order. Fix it by either (a) removing the parallel `&`/backgrounding for the per-sample step and running the loop sequentially, or (b) keeping the parallel writes but having each job tag its line with its step index and then sorting `raw_output.txt` numerically by that index (`sort -n -k1,1`) before it's handed to `aggregate.sh` — the second approach preserves speed and only requires editing the pipeline stage that consumes the file. Verify determinism by running the same seed twice and diffing the resulting EMA. For `generate_dataset.sh`, drive the 100 seeds (1000-1099) through `xargs -P<n>` or GNU parallel, capturing each seed's final EMA into `seed,value` lines, then merge and `sort -t, -k1,1n` into `/home/user/workspace/dataset.csv` with exactly 100 rows.
