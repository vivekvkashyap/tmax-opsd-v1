Handle the three parts independently. For the payload log, read `/home/user/forensics/encoded_payloads.log` line by line, base64-decode each line, parse the resulting URL with `urllib.parse.urlparse` (or equivalent) to pull out just the netloc/hostname, then dedupe and sort alphabetically before writing one domain per line to `domains.txt` — don't hardcode domains, derive them from decoding. For SSH keys, read `/home/user/.ssh/authorized_keys` line by line and keep only lines whose trailing comment field is exactly `analyst@soc.local` (the comment is the last whitespace-separated token); write the retained line(s) to `/home/user/forensics/clean_authorized_keys` with a single trailing newline and nothing else. For the CSP test, write a standard `unittest.TestCase` in `/home/user/app/test_security.py` that imports `app` from `app.py`, uses `app.test_client()` to GET `/login`, asserts the `Content-Security-Policy` header is present, and asserts its value equals exactly `default-src 'self'; script-src 'self'` (match the header byte-for-byte, including the single quotes and semicolon spacing) — run `python3 -m unittest /home/user/app/test_security.py` yourself to confirm it passes before finishing.
