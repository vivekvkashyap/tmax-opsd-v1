Implement this as an ordered streaming pipeline over CSV rows, being careful that each stage's output feeds correctly into the next. Use Python's `csv` module (not manual string splitting) to correctly parse embedded newlines inside quoted fields from stdin. Step order matters: (1) dedupe by `tx_id`, keeping the first occurrence only; (2) in the `comment` field, replace any `\n`/`\r\n` sequences with a single space; (3) resample to a continuous 1-second grid — find min/max `timestamp` in the deduped data, and for every missing integer timestamp in that range insert a synthetic row with `tx_id=GAP`, `comment=FILLED`, and `price` forward-filled from the most recent real/synthetic row seen so far; (4) while emitting rows in timestamp order, maintain a sliding window of up to the last 3 rows' `price` values, compute their average, and print `LOG: <timestamp> <avg>` (2 decimals) to stderr per row — do this on the same pass you write stdout so ordering and window contents stay correct. Write output with `csv.writer` so quoting is standard-compliant. You can sanity-check pipeline output against `/app/price_aggregator` to see how row-dropping manifests on malformed input, but your Python filter's own correctness against the clean/evil corpora (100% match required on both) is what's graded, not mimicking the binary's bugs.
