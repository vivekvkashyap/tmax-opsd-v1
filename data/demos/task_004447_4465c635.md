Treat this as two separate bugs to isolate before touching code. For the deadlock: read every code path after `pthread_mutex_lock` in the request-handling function and trace each `return`/early-exit branch (especially error-handling around malformed/empty messages) to confirm it calls `pthread_mutex_unlock` before returning or closing the socket — a hang right after a bad message is the classic signature of a lock acquired but never released on an error path; add the missing unlock call(s) on every exit path, or better, restructure to a single unlock-then-return point. For the parsing bug: look at how the message is scanned (likely an sscanf-style format like `"[%15[^]]] %[^\n]"`); trace what happens when the message content itself starts with `[` right after the level bracket closes — the format string's literal space between `]` and `%[^\n]` can swallow or misalign a leading `[`. Fix by using explicit pointer arithmetic (find the closing `]` with strchr, then copy everything after the following space as the message) rather than relying on ambiguous scanf spacing. After each fix, run `make` then `make test` and confirm it completes without hanging (use a timeout the first few tries so you don't need to hard-kill). Finally write fix_summary.txt with one sentence per fix.