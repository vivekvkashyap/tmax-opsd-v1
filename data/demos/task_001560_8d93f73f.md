Work the environment issues and the security filter separately, then wire them together. First fix the filesystem: create `/home/user/deploy/current` as a real directory (git worktree/checkout target), symlink `/home/user/www -> /home/user/deploy/current`, and chmod the deploy tree to at least 755 so Nginx's worker user can traverse and read it — a common mistake is leaving the parent directory unreadable even after fixing the symlink. For the crash filter, don't guess: run `strings` and a hex/diff comparison between files in `/app/corpus/clean/` and `/app/corpus/evil/` to spot the byte pattern unique to evil files (look for a distinctive tag-like token and whether it's left unclosed by a matching closing delimiter). Encode that exact detection logic in `/home/user/filter.sh`, exiting 1 only when the malicious pattern is present, 0 otherwise, and validate it against every file in both corpora before moving on. Finally implement `pre-receive` to unpack each pushed commit's changed `.md` files (via `git diff-tree`/`git show` into a temp dir) and run `filter.sh` on each, rejecting the push on any exit code 1; implement `post-receive` to `git --work-tree=/home/user/deploy/current checkout -f` the new `master` so the live symlinked webroot updates automatically.