First fix the vendored bug in `/app/vendored/patch_manager/patcher.py`: correct the `import diflib` typo to `import difflib` and remove the hardcoded early `return ""` so `apply_diff` actually falls through to real diff-applying logic (implement it properly with `difflib`, e.g. building a patched result rather than relying on the toy "Universe" special-case left in the stub) — test it directly with a small original/patch pair before wiring it into the server. Next author `/app/artifact.proto` exactly matching the spec (package `artifact`, service `PatchService`, RPC `ApplyPatch`, `PatchRequest{string original_content=1; string patch_content=2;}`, `PatchResponse{string patched_content=1;}`), then generate Python stubs with `python -m grpc_tools.protoc -I. --python_out=. --grpc_python_out=. artifact.proto`. Write `/app/server.py` implementing the generated servicer, calling the fixed `patch_manager.apply_diff`, catching failures to return an empty string, and serving insecurely on `127.0.0.1:50051` in the background (`nohup ... &`). Finally write `/app/nginx.conf` with `listen 8080 http2;` and `location / { grpc_pass grpc://127.0.0.1:50051; }`, then launch nginx with that config file and confirm both processes stay running (check with `ps`/`curl` or a small grpc test client) since the verifier connects only through the 8080 proxy.
