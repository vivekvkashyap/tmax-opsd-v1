Split this into extraction, merge, and graph-algorithm stages and validate each independently. First, run `tesseract /app/patch.png stdout` (try different `--psm` page-segmentation modes if the default misreads text) to OCR the patch edges, then manually inspect the raw OCR text output for errors before trusting it — OCR on small screenshot text often misreads digits, so sanity-check the extracted `source,target,weight` lines look plausible (e.g. no impossible characters) rather than blindly parsing them. Second, in your C program, parse `/home/user/network.csv` into an edge list/adjacency structure keyed by (source,target), then overlay the OCR'd patch edges on top, overwriting the weight whenever a (source,target) pair from the patch matches one already in the base network, and adding any brand-new pairs as new edges/nodes. Third, run Dijkstra's algorithm (all weights are non-negative integers, so a simple priority-queue or O(V^2) Dijkstra works) from node 0, and average the finite shortest-path distances over all nodes that are reachable and not node 0 itself — explicitly exclude unreachable nodes (infinite distance) from both the sum and the count. Write the result with enough decimal places using `printf("%.4f", avg)` or better.
