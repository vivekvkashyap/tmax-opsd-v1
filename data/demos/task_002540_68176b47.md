For video analysis, extract all frames with `ffmpeg -i /app/config_dashboard_feed.mp4 frame_%05d.png` (or read frames directly with OpenCV/PIL to avoid dumping 1800 files), then for each frame check whether the top-left 50x50 pixel block is pure red (255,0,0) — compare against a real red-detection threshold (e.g. mean channel values), not a guess, and record which frame indices actually match; the frame numbering convention (0- vs 1-indexed, ffmpeg's 1-indexed `%05d` output) matters for correlating to `config_batch_<F>.conf`, so verify by checking a known frame's content matches expectations before trusting the mapping. Extract the tar.gz to `/home/user/configs/`, then locate `config_batch_<F>.conf` for each detected frame number under the extracted tree (use `find` since it's nested). For the patcher, open the target file with `os.open`+`mmap.mmap` (or `mmap` module in read-write or read+write-new-file mode), scan the mmap'd bytes for the literal byte string `[ROGUE_OVERRIDE_ENABLED=TRUE]` and replace occurrences — since the replacement is the same length as the original, in-place mmap byte replacement is fastest and avoids any full-file read/write, which is what will let it beat the naive reference by >2.5x. Run it over `/home/user/configs/`, then tar the corrected tree to `/home/user/restored_configs.tar.gz`.
