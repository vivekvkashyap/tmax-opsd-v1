Start with OCR: run `tesseract /app/runbook_scan.png stdout` (or similar) to extract the algorithm description text, then read it carefully to derive the exact transformation steps (order of operations matters — e.g. whether you reverse before or after converting characters, what offset is added, and what the join delimiter is). Implement /home/user/verify_header.py as a pure function of sys.argv[1] applying exactly what the runbook describes, and sanity-check it manually against a couple of characters by hand before trusting it, since the fuzz-equivalence check requires bit-exact output on hundreds of random inputs — any off-by-one in the added offset or wrong string order will fail every case. For the server: build backend_server.py as a stdlib `http.server` + `ssl` HTTPS server bound to 127.0.0.1:8443, generate a self-signed cert/key with `openssl req -x509 -newkey rsa:2048 -nodes -keyout key.pem -out cert.pem -days 365 -subj ...` into /home/user/certs/, implement /health and /verify (parsing the `data` query param, applying the same transform function, importing it or reimplementing identically), and append timestamped log lines on each /verify hit, creating /home/user/logs/ if missing. Implement rotate_logs.py as a simple three-slot shift-then-clear routine, checking file existence before each `os.rename`.
