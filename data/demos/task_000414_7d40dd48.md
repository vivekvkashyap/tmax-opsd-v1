Do not guess at the algorithm — extract it precisely from the image first. Run `tesseract /app/token_rules.png stdout` (try adding `--psm 6` or similar if the default output looks garbled) and read the resulting text carefully to identify each numbered step of the token generation rule; if OCR output is noisy, re-run with different page-segmentation modes or crop/preprocess the image (grayscale, threshold, upscale) until the text is clean and unambiguous. Implement each step literally in `/home/user/generate_token.py`: reversing strings, arithmetic transforms, concatenation order/separators, and any conditional suffix based on a prefix check — get the exact separator characters and conditional logic right, since fuzz testing against thousands of inputs will catch even a single wrong character or off-by-one in string indexing. Make the script `chmod +x`, ensure it takes `SERVICE_NAME` and `TIMESTAMP` as positional argv, and prints only the token (no extra whitespace/newlines beyond what a normal `print` produces). Before finishing, test your script against a handful of hand-picked inputs including edge cases implied by the rules (e.g. a case that would trigger any conditional branch) and reason about whether your output matches what the extracted rules describe.
