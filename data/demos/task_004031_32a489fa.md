Transcribe `/app/voicemail.wav` first (e.g. with `whisper`) and read the transcript closely — it specifies the exact validation rule, the unicode-escape decoding format, the tokenizing regex, and the sliding-window hashing/dedup scheme; implement each step in C++ literally as described rather than guessing a "reasonable" text-cleaning approach, since the fuzz-equivalence check compares byte-for-byte against an oracle built from the same instructions. Pay special attention to edge cases the transcript implies: what counts as "invalid" input, exactly which escape pattern to recognize (and what to do when it doesn't match), and how ties/duplicates in window hashes are counted. After writing `/home/user/cleaner.cpp` and compiling to `/home/user/cleaner`, do NOT try to inspect or run the oracle binary to reverse-engineer behavior — build your implementation purely from the transcript, then self-test against your own varied inputs (empty string, missing quotes, malformed escapes, short strings, non-ASCII) to catch logic bugs before the fuzz harness runs. Read stdin as a single line and write to stdout exactly the format described.
