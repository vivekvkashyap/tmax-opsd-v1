First OCR `/app/magic_spec.png` (e.g. `tesseract /app/magic_spec.png -`) to recover the exact binary format spec — do not guess the format, read it precisely including byte order, magic bytes, and the exact error-recovery behavior on corrupt/truncated blocks. Implement the C++ extractor as a byte-scanning state machine over stdin: scan for the 2-byte magic sequence, read the next 4 bytes as a little-endian uint32 length N, read N bytes and attempt zlib inflate (via `zlib.h`'s `inflate`/`uncompress`); on success print the decompressed bytes plus newline, on failure or premature EOF do NOT skip the whole block — back up to just after the first magic byte and resume the byte-by-byte scan from there, since this exact recovery semantic is what fuzz testing will probe hardest. Build with `g++ -lz`. For curation, use `find /app/incoming -type f -size +1024c -perm -4000` to find setuid files over 1024 bytes, concatenate them in a deterministic order and pipe through your extractor into final_manifest.txt, then use `stat -c '%Y %n'` to find the most-recently-modified listed file for the `ln -s` symlink, and `ln` (hard link) the rest into `/home/user/curated_archive/` by basename.