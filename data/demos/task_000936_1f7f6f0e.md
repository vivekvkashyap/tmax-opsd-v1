Work the pipeline in strict order and verify each stage before moving on. First, inspect `generate_base.py` to find the exact environment variable name it reads via `os.environ.get(...)`, then run it as `VARNAME=/home/user/microservices/lb/haproxy.cfg python3 generate_base.py` (creating the `lb/` directory first if needed) and confirm the file was written there, not to `/tmp/base.cfg`. Second, use `grep`/`awk` on `registry.txt` to select only lines whose status field is exactly `UP` (avoid substring matches like `UPGRADING`), preserving the file's original order, and capture name+port pairs. Third, for the legacy socket, write a small Python script using the `socket` module (AF_UNIX, SOCK_STREAM) to connect to `/home/user/microservices/legacy.sock`, read the `Enter PIN: ` prompt, send `8472\n`, then read and parse the `Backend port is <PORT>` response with a regex — don't guess the port. Finally, append the `backend web_cluster` block to the generated cfg file with one `server <name> 127.0.0.1:<port>` line per UP service (in registry order) followed by the legacy server line, matching the exact indentation/format shown in the task. Double check the final file byte-for-byte against the required format before finishing.