Work through the three derivation steps before writing any C code, and keep them as separate, checkable stages. First, transcribe `/app/legacy_passcode.wav` with an available speech-to-text tool to recover the spoken old passcode — read carefully for phonetic-alphabet-style spelling (letters/digits spoken individually) and convert it to the literal string form. Second, `grep`/read `/app/auth_logs.txt` for the rotation salt line, then compute the new master seed yourself as `sha256(old_passcode + rotation_salt)` (verify this by running `echo -n "<recovered>" | sha256sum` in the shell before hardcoding it into your C source, so you catch any transcription mistakes early). Third, implement `/home/user/token_filter.c`: split the token file's content on the last `.` into base64 payload and hex signature, recompute `SHA256(new_seed + payload)` using OpenSSL's EVP or SHA256 API and compare hex digests exactly (constant-time compare not required, just correctness); separately check whether the signature instead matches the *old* passcode's seed for the legacy-block rule; base64-decode the payload and case-insensitively search the decoded bytes for `<script>`, `javascript:`, `onload=`. Compile with `-lssl -lcrypto`, then validate by running the compiled `token_filter` over every file in `/app/corpus/clean/` (expect exit 0) and `/app/corpus/evil/` (expect exit 1), fixing any rule ordering or comparison bug until both corpora classify perfectly.