Work through this PR fix systematically, verifying each piece before moving to the next. First create the venv (`python3 -m venv /home/user/project/venv`), activate it, and `pip install -r requirements.txt`. Second, inspect the `Makefile` building `libcompute.so` — a linking/format error when loading via `ctypes` almost always means the shared object wasn't compiled as position-independent code or wasn't marked as a shared library; fix the gcc invocation to include both `-shared` and `-fPIC` flags (e.g. `gcc -shared -fPIC -o libcompute.so compute.c`). Third, apply the schema migration consistently: since `schema_migration.sql` renames `user_id` to `account_id`, grep through `db.py` for every reference to `user_id` in SQL query strings and Python variable/column access and update them to `account_id` so the code matches the post-migration schema — don't just rename one occurrence, check all queries. Finally, activate the venv and run `run_ci.sh`, which builds the library, applies the migration, and runs pytest, capturing output to `/home/user/ci_results.log`; check that log for `passed` with zero failures before declaring done, and re-run if anything fails rather than assuming success.