Build the three pieces independently and integration-test last. For the Rust generator, the compile error is a lifetime problem — most likely a function returning a `&str` borrowed from a local `String` that's dropped at function end; fix by returning an owned `String` (or restructure so the borrow's lifetime matches its use), or if that's more friction than it's worth, just write a small Python/C script that reads `routes.json` and emits `#define` constants into `/home/user/router.h` directly — the task explicitly allows either path. For the video, don't eyeball frames — sample deterministically at each whole-second timestamp with `ffmpeg -i signal.mp4 -vf "select='eq(n,0)+eq(n,30)+...'" ...` or extract at `fps=1` and read the dominant/solid pixel color of each extracted frame (average or sample a single pixel, since frames are solid colors) to build the instruction sequence, stopping at the White/RET frame. For the C server, implement a minimal blocking TCP accept loop parsing the request line's method/path/query string yourself (no framework), route only exact match on the configured exec path with a 404 fallback, run the extracted VM program starting from the `init` query param, and background the server (`&` plus `disown`, or a proper daemonization) bound to `127.0.0.1:8080` before finishing.
