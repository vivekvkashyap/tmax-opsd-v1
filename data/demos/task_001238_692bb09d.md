Work through this as three clearly separated numerical steps, sanity-checking each before moving to the next. Step 1: load all 10 datasets and the target vector, compute each dataset's column-wise mean (centroid) as a 50-d vector, then compute cosine similarity (dot product over the product of L2 norms) between each centroid and the target — sort descending and take the top 3, keeping the exact ordering (most similar first) since it matters for the output. Step 2: for only the single best dataset, compute `numpy.cov(data, rowvar=False)` (which uses sample covariance, ddof=1 by default) and separately import and call `compute_fast_cov` from `/home/user/fast_cov.py` on the same array — do not assume they use the same normalization convention (population vs. sample); read `fast_cov.py`'s implementation to understand exactly what it computes, since that's the source of any expected numerical gap. Compute the Frobenius norm of the elementwise difference between the two resulting matrices (`numpy.linalg.norm(diff, 'fro')` or `numpy.linalg.norm(diff)` on a 2D array), and round to 4 decimals. Step 3: write both results to `/home/user/experiment_log.json` with the exact key names and structure specified, double-checking JSON validity before finishing.