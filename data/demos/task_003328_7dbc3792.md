Fix the pieces in dependency order. First get `process.py` to valid Python 3: replace `print "x", y` with `print("x", y)`, `xrange` with `range`, and fix the `ctypes.CDLL` path — it currently loads `./libprocessor.so` relative to the cwd, which breaks once CMake builds it elsewhere, so point it at the actual built artifact's absolute path (or copy/symlink the .so next to process.py). Build the library first: `cmake -S /home/user/migration/src -B /home/user/migration/src/build && cmake --build /home/user/migration/src/build` (or the classic `mkdir build && cd build && cmake .. && make`), then confirm `libprocessor.so` exists before wiring the ctypes path to it. For `orchestrator.py`, parse `deps.txt` lines of the form `X depends on Y` into a DAG (edge Y->X), implement Kahn's algorithm or DFS-based topological sort (there may be multiple valid orders — any valid one is accepted), run `python3 process.py <file>` for each file in that order, and concatenate the resulting `.out` files into `final_output.txt` in the same order. Diff that against `expected_output.txt` into `diff_result.txt`, and write the comma-separated order used to `execution_order.txt`. Run everything end-to-end rather than hand-authoring the outputs.