Start by transcribing `/app/evidence/intercept.wav` — install and run a speech-to-text tool (e.g. `openai-whisper` via `whisper intercept.wav --model base`, or `ffmpeg` + `SpeechRecognition`/`pocketsphinx` if whisper is too heavy) and read the transcript carefully; it will state the exact conditions that make a certificate malicious (likely phrased as an "or" of a few distinct checks — an issuer field value, a weak/legacy signature algorithm, and a file-permission property). Parse each `.pem` in the target directory with the `cryptography` package (`x509.load_pem_x509_certificate`), inspect the issuer's Organization Name attribute, check the certificate's `signature_algorithm_oid`/`signature_hash_algorithm` name for the weak algorithm mentioned, and separately `os.stat()` the file to check its permission bits for world-writability. Flag a cert EVIL if it matches ANY one of these three conditions (logical OR, not AND) — this is the naive mistake to avoid. Print exactly `<filename> - CLEAN` or `<filename> - EVIL` per file, iterating in a stable order. Test against both `/app/corpus/clean/` and `/app/corpus/evil/` before finalizing to confirm 100% correct classification.