The API contract you must implement is not in the task text — it's embedded as text inside `/app/architecture.png`, so your first step is OCR: use `tesseract` (or another OCR tool you install) on the image to extract the host, port, endpoint paths, request/response JSON shapes, and any parameter names, and treat that extracted text as the authoritative spec (re-run/inspect the OCR output carefully since small formatting details like exact key names matter for the verifier). Once you have the spec, load `/app/transactions.jsonl` line by line into a directed graph (networkx, edges weighted/labeled by amount) for the centrality endpoint, computing PageRank with the alpha value the image specifies. For the aggregation endpoint, implement a small pipeline that filters transactions by the request's minimum-amount threshold, sums amounts per sender, and returns the maximum such sum — mirror this as a simulated aggregation stage sequence even if implemented as plain Python filtering/grouping. Stand up an HTTP server (Flask/FastAPI) bound to exactly the host:port from the image, implement both endpoints with exactly the response field names from the image, and leave it running in the background; verify with curl against both endpoints before finishing.
