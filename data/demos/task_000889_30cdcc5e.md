Work through this pipeline stage by stage and validate each before moving on. For audio: load the WAV, split into 100 contiguous 1-second windows, compute 13 MFCCs per window (e.g. librosa.feature.mfcc), and average each coefficient over the window to get a 100x13 matrix — then apply sklearn PCA(n_components=2) with no prior scaling, fit_transform to get pca_1/pca_2 per row in original order so they align with the metadata's sequential IDs. For cleaning: join on row order/id, drop rows with heart_rate > 150 first, then compute the median age from the surviving rows only and use it to fill NaNs in age — order matters here, since imputing before dropping outliers would contaminate the median with soon-to-be-removed rows. Train LogisticRegression with default hyperparameters on features in the exact order [pca_1, pca_2, age, heart_rate]. For the plot script, the bug is a headless-incompatible matplotlib backend (e.g. TkAgg) — set `matplotlib.use('Agg')` before importing pyplot, then scatter pca_1 vs pca_2 and savefig to the required path. Finally stand up FastAPI/Flask on 127.0.0.1:8080 with /ping and /predict using the trained model object in memory, and leave it running in the background; test both endpoints yourself with curl before finishing.