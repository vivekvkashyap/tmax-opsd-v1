Build `query_sales.sh` as a single `awk` + `sort` + pagination pipeline, no other language allowed. Read the 5 positional args ($1 region, $2 min_amount, $3 sort_col, $4 page_num, $5 page_size). Use `awk -F, -v region="$1" -v min="$2" 'NR>1 && $3==region && $5>=min'` on the CSV to skip the header and filter (careful: column indices in the file are 1-based and match the task's column numbering exactly — Region is field 3, Amount is field 5). Pipe that into `sort`, choosing numeric sort (`-t, -k${SORT_COL},${SORT_COL}n -r` i.e. `sort -t, -k${3},${3}nr`) when sort_col is 1 or 5, otherwise alphabetical descending (`sort -t, -k${3},${3}r`) — get the field/flag selection right or ordering will silently be wrong for text columns. Finally paginate with `tail -n +$(( (page_num-1)*page_size + 1 ))` piped into `head -n $page_size`. Chain these into one pipeline inside the script, `chmod +x` it, and manually run the example invocation from the prompt to eyeball that the returned rows and their order match your own hand-computed expectation before considering it correct.
