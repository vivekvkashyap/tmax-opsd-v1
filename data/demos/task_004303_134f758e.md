Do OCR first since it's a hard dependency for Part 2: run `tesseract /app/arch_diagram.png stdout` (try multiple `--psm` modes if the first pass garbles text) and locate the line matching `MANAGEMENT_CIDR: <subnet>`; parse out the CIDR value programmatically rather than hardcoding what you visually guess, since OCR errors on digits are common — sanity check the recovered string looks like a valid CIDR (e.g. with the `ipaddress` module) before using it. For `restore_filter.py`, parse each `.conf` file line by line and check three independent conditions: (1) inbound-allow rules (iptables/ufw syntax) from `0.0.0.0/0` targeting ports 22/3306/5432; (2) cron-schedule lines (start with 5 whitespace-separated time fields) containing `nc -e`, `bash -i`, or `/dev/tcp/`; (3) any rule granting port 22 access from a source CIDR that isn't exactly the recovered MANAGEMENT_CIDR — use the `ipaddress` module for real subnet/CIDR comparison, not raw string equality, so partial or reformatted CIDR strings are still handled correctly. A file is "evil" if ANY condition is true. Print one flat JSON object `{filename: "clean"|"evil"}` to stdout with no extra logging mixed in. Finally write the crontab line to `/home/user/backup_cron_job` matching `15 3 * * * python3 /home/user/restore_filter.py /home/user/incoming_backups > /home/user/backup_scan.log` exactly.