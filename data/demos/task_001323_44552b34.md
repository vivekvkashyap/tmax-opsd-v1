Do the plumbing fix before writing any filter logic: inspect `/app/config.env`, resolve the port collision so `model_api` and `vector_db` bind to distinct ports (8001/8002) and correct the dimension mismatch, then run `/app/start.sh` and confirm both services answer with `curl` before touching the filter script. For the filter, build the shell pipeline stage by stage and verify each stage's raw JSON with `curl | jq` manually first: extract `.abstract` from the input file with `jq -r`, POST it as `{"text": ...}` (use `jq -n --arg` to safely JSON-encode, avoiding manual string interpolation which breaks on quotes/newlines) to the embed endpoint, capture `.vector`, forward that array as-is in a second POST to the distance endpoint, then extract `.distance`. Implement the three rejection checks explicitly: numeric distance > 5.0 (use `awk` or `bc` since bash can't compare floats natively), exact string match on `metadata.source`, and a vector-length/error check on the embed response. Exit 1 the moment any check fails; only exit 0 after all pass. Test against both corpora directories before finishing, and `chmod +x` the script.