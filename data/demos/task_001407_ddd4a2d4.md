Work top-down through the pipeline stages and validate each before moving on. Create `/home/user/inbox` and `/home/user/outbox`, then `ln -s /app/input.wav /home/user/inbox/target.wav`. For the C++ analyzer: open the file in binary mode, seek/skip exactly 44 bytes (the standard WAV header) since it's known to be 16-bit PCM mono, then read the remainder as a stream of `int16_t` samples until EOF, compute RMS as sqrt(mean of squared samples) using a double accumulator (avoid integer overflow — sample^2 can be up to ~1e9, sum across many samples needs double, not int/int16), and write the result to the output path formatted with exactly 2 decimals (`std::fixed << std::setprecision(2)`), taking the input/output paths from argv[1]/argv[2]. Build to `/home/user/bin/analyzer` after `mkdir -p /home/user/bin`. For the systemd user service, write a unit with `ExecStart=/home/user/bin/analyzer /home/user/inbox/target.wav /home/user/outbox/rms.txt`, then `systemctl --user daemon-reload && systemctl --user start audio-analyzer.service` and confirm `systemctl --user status` shows success/exited-cleanly (a Type=oneshot service is appropriate here). Finally write `health_check.sh` to grep the `Active:` line from `systemctl --user status` output and append it to `health.log`, then run it once.