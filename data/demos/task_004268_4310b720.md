Work in two clean phases. First, build a Rust binary (Cargo project at `/home/user/elf_auditor`) that (a) parses the ELF header of `/home/user/app_binary` to get the entry point — use an ELF-parsing crate rather than hand-rolling offsets, and format it as lowercase hex with `0x` prefix exactly as `readelf -h` would report it; (b) locates and reads the raw bytes of the custom section named `.audit_policy` (most ELF crates expose sections by name — don't assume section index, look it up by name string) to get the `PORT=XXXX` string; (c) parses `/home/user/cert.pem` with a certificate/X.509 crate to pull the Subject CN (a plain string comparison against `openssl x509 -noout -subject` output is a good sanity check while developing). Serialize all three fields into `/home/user/audit_report.json` with exactly the keys `entry_point`, `policy`, `cert_cn`. Second, once you've determined the port number from the policy string, hand-write `/home/user/fw_config.sh` as two plain iptables lines (no shebang wrapper logic needed) — one ACCEPT rule scoped to `-s 192.168.1.100 --dport <port>`, and one DROP rule for the same port with no source restriction, in that order so the ACCEPT rule is evaluated first.