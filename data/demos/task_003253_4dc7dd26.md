Write this as a single-pass streaming C program: open the input file, skip/print nothing for the header line but remember to write your own output header, then read line by line with `fgets` (or `getline`), parsing `timestamp,temp,humidity,pressure` with `sscanf`/`strtok`, keeping a "last seen timestamp" variable initialized to a sentinel — if the current row's timestamp equals the last one, skip it entirely (do not reset the sensor values, just discard the whole duplicate row); otherwise process it and update the last-seen timestamp. For normalization, clamp each of the three sensor values independently: if a value is `< 0.0`, treat it as `0.00`. For output, emit three lines per kept row in the fixed order temp, humidity, pressure, formatting each value with `printf("%.2f", value)` so it always has exactly two decimals (watch for negative-zero formatting artifacts). Since input is already sorted by timestamp and duplicates are adjacent, this single forward pass naturally produces correctly-ordered output — no sorting step is needed after the fact. Compile with `gcc -o /home/user/cleaner /home/user/cleaner.c`, run it, and diff your output against a mental trace of a few example rows to confirm the header, decimal formatting, and dedup logic all match before considering it done.