The trap here is comparing semver strings lexicographically — `"1.12.0" < "1.9.0"` as strings but not as versions — so in `proxy.py`, split the `X-API-Version` header (or treat it as `"0.0.0"` if missing) on `.`, cast each of MAJOR/MINOR/PATCH to `int`, and compare component-by-component (or build a tuple `(major, minor, patch)` and compare tuples) against `(2, 0, 0)` to decide V1 vs V2 routing. Implement the proxy as a simple `http.server` handler whose `do_POST` reads the body, decides the backend by version, then relays the request with `http.client`/`urllib` to `localhost:8001` or `localhost:8002`, forwarding the response body and status back verbatim. For `backend_v2.py`, split the POST body on the first space into `command` and `arg`, dispatch on `ECHO`/`REVERSE`/`UPPER` (Python slicing/`.upper()`), and return exactly `ERROR` for anything else. Start all three servers backgrounded with `&`, add a short sleep to let them bind, then run the five `curl` commands in the given exact order, appending each raw response body (no added newlines beyond what curl itself emits) to `results.log`.
