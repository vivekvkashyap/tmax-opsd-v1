Implement this as two clearly separated passes over the data inside one Go program: a streaming Welford pass and a closed-form Bayesian pass, both driven by reading `/home/user/measurements.csv` line by line with `bufio.Scanner`. For Welford, maintain `n, mean, M2` and update per the standard recurrence (`delta := x - mean; mean += delta/n; delta2 := x - mean; M2 += delta*delta2`), using sample variance `M2/(n-1)` (guard n==1). Every time `n % 1000 == 0`, marshal `{"n": n, "mean": round(mean,3), "variance": round(variance,3)}` and append as one line to `welford_stats.jsonl` — round explicitly (e.g. `math.Round(x*1000)/1000`) before formatting so JSON doesn't print extra float noise. For the Bayesian step, don't try to update incrementally per-sample; instead use the closed-form conjugate-Normal update directly from the final running sum and count: posterior variance = `1/(1/prior_var + n/sigma2)`, posterior mean = `posterior_var * (prior_mean/prior_var + sum_x/sigma2)`, both evaluated once at n=10000 and rounded to 5 decimals. Compile with `go build` (no third-party imports) and run it before checking output files exist.
