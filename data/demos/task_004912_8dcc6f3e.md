Process the log line-by-line with a decode pipeline: split each line into `<timestamp> <payload>`, then `base64.b64decode(payload)` -> `zlib.decompress(...)` -> `json.loads(...)`. Before writing the diffing logic, print/inspect one decoded record to confirm the nested structure and that `config["database"]["max_connections"]` is the field you need. Then iterate through records in file order, tracking a `previous_value` variable: initialize it from the first record without emitting a CSV row (that's the baseline), and for every subsequent record only append a row (`timestamp,old_value,new_value`) when the current value differs from `previous_value`, updating `previous_value` afterward regardless of whether it changed. Write header + rows to `/home/user/connection_changes.csv` preserving chronological (i.e., input) order. Watch for the common mistakes: comparing against the immediately-previous record's raw dict instead of the last *value seen*, and accidentally emitting a row for the first baseline snapshot.
