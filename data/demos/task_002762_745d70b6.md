Do this entirely in-memory using Python's `tarfile` and `zipfile` modules layered on `io.BytesIO` — never call `.extractall()` or write archive contents to disk. Walk `/home/user/archive_mess/` with `os.walk`/`glob` to find `*.tar` files, then for each: `tarfile.open(path)`, iterate members, and for entries whose name ends `.zip`, extract the member's file object via `tar.extractfile(member).read()` into `io.BytesIO(...)`, then open that with `zipfile.ZipFile(...)`. Within the zip, iterate its namelist for `*.txt` entries, read each via `zf.open(name)`, decode, and split into lines. For every line containing the exact substring `ERROR_STATE_RACE_CONDITION`, write a line to `/home/user/found_errors.log` in the format `<tar_basename> -> <zip_basename> -> <txt_basename>: <line>` — use `os.path.basename` on the outer tar path but the raw member/zip-entry names (which are already just filenames) for the inner two, and preserve the line's own trailing newline exactly as read from the file rather than adding your own. Since traversal order across tar files isn't guaranteed, the grader sorts before comparing, so you don't need to force a specific order, just correctness of every line.
