In the C parser, don't rely on splitting by whitespace — PDB ATOM/HETATM lines have fixed-width columns, so extract X/Y/Z by slicing exactly the 1-based columns 31-38, 39-46, 47-54 (i.e. 0-based offsets 30,38,46 with widths 8) and check the line prefix is exactly "ATOM  " or "HETATM" before parsing. Implement gradient descent literally as specified: initialize w1=w2=b=0, and on every iteration compute the batch gradients (mean of (pred-Z)*X plus lambda*w1, similarly for w2, and mean of (pred-Z) for b with no regularization on b) using the *old* weights for all m predictions before applying any updates (simultaneous update, not per-sample/stochastic updates) — a common bug is updating w1 then using the new w1 to compute b's gradient in the same iteration. Print with exactly `%.4f` formatting. In process_data.sh, compile with `gcc -O2 -lm`, iterate `*.pdb` files in `/home/user/pdb_data/` in alphabetical (default glob) order, invoke with the exact learning_rate/iterations/lambda given, and append one formatted line per file to results.txt. Run the script yourself and diff a hand-computed or scripted check on one small file to confirm your gradient math and column-slicing are correct before trusting the full output.