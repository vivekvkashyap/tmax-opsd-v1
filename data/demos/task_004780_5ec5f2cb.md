Treat `/app/audit_filter` as a pure black-box function and characterize it empirically before writing any bash: feed it hand-crafted CSV lines (`timestamp,src_ip,uid,pid,command_line`) covering the interesting cases — uid=0 with/without `password=`/`token=` substrings in the command, uid!=0 with/without `/etc/shadow`/`/etc/passwd` substrings, missing fields, empty command — and observe exactly what it prints for each, to nail down the exact output format and field ordering (e.g. `LEAK|<pid>|<src_ip>` vs `ESCALATION|<pid>|<uid>`) rather than guessing. Use `strings /app/audit_filter` to spot literal format strings and substrings baked into the binary, and `ltrace`/`strace` if available to watch which library calls (`strstr`, `printf`) fire on which inputs. Once you're confident in the two independent rules (one keyed on uid==0, one on uid!=0, each checking for either of two substrings in the command field), implement `/home/user/filter.sh` in bash using `awk -F,` or manual `IFS=,` parsing per line — be careful that `command_line` may itself legitimately contain commas, so split on the first 4 commas only and treat the remainder as the full command field, mirroring the oracle's likely `strtok` behavior. `chmod +x` the script and fuzz-test it against the oracle with randomly generated multi-line inputs, diffing outputs exactly before finishing.
