Decompose this into independent, individually-testable background components rather than one monolithic script. For init, check `[ -f /home/user/infra_project/data/data.json ]` and if absent, `tar -xzf backup.tar.gz -C /home/user/infra_project` (verify the archive's internal path layout first with `tar -tzf backup.tar.gz` so the extracted file actually lands at `data/data.json` relative to the project dir). For supervisor.sh, use a `while true; do date "+[%Y-%m-%d %H:%M:%S] Starting server" >> logs/server.log; python3 server.py; done` loop — the log line must be appended on every loop iteration (i.e., every start/restart), and the loop naturally restarts on any exit/crash since the `while true` re-enters after the process dies. For port forwarding, launch `socat TCP-LISTEN:9090,fork,reuseaddr TCP:127.0.0.1:8080 &` (backgrounded, not blocking deploy.sh). For health_monitor.sh, loop `sleep 1; curl -sf http://127.0.0.1:9090/health >/dev/null && echo OK >> logs/health.log || echo FAILED >> logs/health.log`. For logrotate.conf, target `/home/user/infra_project/logs/*.log` with `daily`, `rotate 3`, `compress`, `missingok`, and non-root state via logrotate's `-s /home/user/infra_project/logrotate.status` flag (don't rely on default `/var/lib/logrotate.status`, which needs root). Ensure every script is `chmod +x` and deploy.sh backgrounds the long-running pieces with `&` so it returns promptly.
