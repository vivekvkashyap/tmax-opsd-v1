Break this into: OCR a key, safely extract a zip, XOR-decode, then rename. First run `tesseract /app/obfuscation_key.png stdout` (or similar OCR invocation) to read the on-image text formatted `KEY=XXX`, and parse out the integer following `KEY=` — don't hardcode a guessed number, derive it from the OCR output. Next write the extraction script: iterate the zip's members with Python's `zipfile`, and for each entry compute the resolved destination path (`os.path.normpath(os.path.join(target_dir, member_name))`) and verify it still starts with the target directory's absolute path before writing — reject/skip any entry that resolves outside `/home/user/clean_dataset/` (this is the zip-slip defense; a naive `extractall()` is unsafe and must not be used). For each safely-extracted file, XOR every byte with the recovered integer key and overwrite it in place, and confirm it starts with a JPEG magic header after decoding. Finally, sort the *original* member names alphabetically (not extraction order) and rename the decoded files sequentially to `dataset_001.jpg`, `dataset_002.jpg`, etc. following that sorted order.