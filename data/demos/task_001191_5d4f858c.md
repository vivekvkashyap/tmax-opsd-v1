First isolate the build failure before touching the filter script: run `pip install /app/fastText` and read the actual compiler error output — it will point to the C++ standard flag. Open `/app/fastText/setup.py` and look for `extra_compile_args` on the Extension definition; find where the C++11 standard flag has been swapped for an incompatible older standard (e.g. c89) and restore `-std=c++11`, then reinstall and confirm `import fasttext` and `fasttext.load_model('/app/cleaner_model.bin')` succeed before writing any pipeline code — don't assume the fix worked without importing the module. For `filter_script.py`, keep the pipeline exactly as specified: read stdin line by line, strip the trailing newline, preprocess with a regex that lowercases then strips everything outside `[a-z ]` (order matters — lowercase first, then filter), feed the processed string to `model.predict(text)`, and only print the *original* (unstripped-of-content, pre-lowercasing) line when the top label is exactly `__label__keep` and its probability is `>= 0.60`. Since grading is exact-match fuzzing against a hidden oracle, mirror the preprocessing regex and threshold comparison operator (`>=`, not `>`) precisely, and test manually with varied punctuation/edge-case strings (empty after filtering, all-numeric, etc.) before finishing.
