Work in order: first run the legacy binary to produce `/home/user/graph.db`, then diagnose the "stale edges" symptom empirically rather than guessing — run the same `SELECT * FROM edges WHERE src = X` query twice (once via index lookup, once forcing a full scan with e.g. `SELECT * FROM edges WHERE src+0 = X` or `INDEXED BY`/`NOT INDEXED`) and compare results to confirm the index is corrupt. Fix it with `REINDEX;` (or drop and recreate `idx_src`) before any further extraction — do this in a way you can verify (row counts/checksums match a full scan after repair). For the C++ program: use a `WITH RECURSIVE` CTE via libsqlite3 to pull only the subgraph reachable from node 0, load edges into an adjacency list, and implement plain power-iteration PageRank exactly as specified (uniform 1/N init, d=0.85, exactly 25 iterations, sink nodes' mass is simply not redistributed — do not add teleportation for dangling nodes beyond the standard damping term). Write the CSV with a stable float format (e.g. enough decimal digits, like `%.10f`) since the grader computes MSE against your values. Test correctness by re-deriving PageRank on a tiny hand-checked subgraph before trusting the full run.
