This task chains three separate forensic extractions before you can write any logic — do them independently and verify each before combining. (1) Video: extract frames from `/app/incident_feed.mp4` (e.g. `ffmpeg -i incident_feed.mp4 frame_%03d.png` or sample pixel brightness per frame with a script), inspect the top-left 10x10 block's average brightness per frame across frames 0-31, threshold bright vs dark to get a 32-bit binary string in frame order, then convert to an 8-hex-char zero-padded string — double check your bit ordering (frame 0 = MSB or LSB, whichever convention the task implies from "form a 32-bit integer"). (2) Memory dump: search `/app/coredump.raw` for the literal marker `SALT_START=` (e.g. `grep -a` or `strings` combined with byte offsets, or Python binary search) and extract exactly the 16 characters up to `_SALT_END`. (3) Git forensics: in `/app/query_service`, use `git log -p compute_query.py` (or `git bisect`/`git diff` between commits) to find the commit that introduced the bug and recover the original hashing formula that combines the input integer with the key and salt. Only after confirming all three independently should you assemble `/home/user/fixed_query.py`, then validate it against `/app/oracle_query` behavior locally on sample integers before relying on the fuzz suite.