Work through this in layers, verifying each before moving on. First fix the build: inspect the Makefile for the `libecc` link line — a missing `-L.` (library search path) or `-fPIC` on the shared-object compile step is the classic cause of a link failure; fix flags so both `.so` files build and link against each other correctly. Then open `extractor.c` and find the fixed-size stack buffer copied into with `strcpy` from attacker-controlled header data — replace it with a bounds-checked copy (`strncpy`/explicit length check against `sizeof(buffer)-1` plus null-termination) so malformed headers can't overflow it. For frame extraction, use `ffmpeg -i <video> -vf "select=eq(n\,42)" -vsync vfr -frames:v 1 <outfile>` to grab exactly the 42nd 0-indexed frame, then pipe it through your rebuilt `fm_tool` and capture its checksum output verbatim into the required file. For the detector, don't reimplement the C logic in Bash — invoke `fm_tool` as a subprocess and inspect its exit status/output for corruption or ECC-checksum-failure signals, mapping those to your own exit codes. Test against both provided clean/evil corpora before wiring up the Nginx proxy and Bash TCP listener, and launch all services in the background (`&`, `nohup`, or a session manager) so they persist after your script finishes.