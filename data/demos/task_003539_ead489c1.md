Build the pipeline bottom-up and test each stage in isolation before wiring them together. In extract.py, use a regex like `\(X:\s*([-\d.]+),\s*Y:\s*([-\d.]+)\)` applied twice per line (departure and arrival), plus a `Route (\S+):` capture and `Status:\s*(\S+)` — only emit a record when both coordinate pairs matched AND status is exactly SUCCESS; skip the line entirely (don't error) if the regex fails to match, since one line is deliberately malformed. In compute.py, use `math.hypot(x2-x1, y2-y1)` and round to 2 decimals with `round(d, 2)` (careful: Python's banker's rounding — verify against a couple of hand-computed cases like a 3-4-5 triangle). In pipeline.sh, loop over `*.log` files, call extract.py then compute.py (or a combined call), append `route_id,distance` lines to the CSV with the header written once, then `mv` each processed log file into `/home/user/archive/` — do this only after successful processing so failures don't lose data. For the cron file, use exactly `*/15 * * * * /bin/bash /home/user/pipeline.sh` on one line. Test end-to-end by running `./pipeline.sh` manually and inspecting the CSV and archive directory.
