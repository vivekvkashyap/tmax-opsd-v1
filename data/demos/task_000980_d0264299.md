Locate the bug first: open `/app/vendor/github.com/phys-sim/wavegraph/integrator.go` and look inside the `Run` method's stepping loop for the step-size adaptation logic — you'll find it's disabled (e.g. a commented-out line updating `dt` toward `maxDt`), which pins the integrator at its minimum step and forces millions of iterations. Re-enable/rewrite that line so `dt` grows toward `maxDt` each successful step, and confirm the fix by timing a quick standalone `go run` that just calls `NewGraphSimulation(100)` then `Run(10.0)` — it should complete in well under a second once fixed, versus taking many seconds/timing out when broken. Only after confirming the vendored fix, write `/home/user/analyze.go`: call `Run(10.0)` to get the sink signal, compute its DFT amplitude spectrum (implement a straightforward O(n^2) DFT or use `gonum/dsp/fourier` if available), normalize the amplitudes to sum to 1.0, load `/home/user/baseline.json` as the reference distribution Q, and compute KL divergence as Σ P[i]*log(P[i]/Q[i]) (guard against P[i]=0 terms, which contribute 0 to the sum by convention). Write the divergence value to `/home/user/kl_divergence.txt` and produce a comparison line plot (e.g. with `gonum/plot`) of both spectra to `spectrum_plot.png`.