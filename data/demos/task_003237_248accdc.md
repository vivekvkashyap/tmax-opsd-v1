Build this as a chain of small, independently testable pieces. First write the `expect` script: use `spawn /home/user/legacy_metrics_cli`, then `expect "Username: "` / `send "admin\r"`, `expect "PIN: "` / `send "1234\r"`, then `expect eof` so all remaining output (including the metrics lines) is flushed to stdout before the script exits — test it standalone first (`./fetch.exp`) to confirm you see the raw `MEM_MB=` lines. For the C++ aggregator, read stdin line by line, use a regex or simple prefix check (`line.rfind("MEM_MB=", 0) == 0`) to pull out the integer after `=`, accumulate sum and count, then print `{"avg_mem": N}` with integer division and no extra whitespace/newline issues — verify with `g++ -o aggregator aggregator.cpp` and pipe sample input manually before wiring it to expect. For the ACL, use `setfacl -m u:nobody:r /home/user/dashboard.conf` (not chmod, since the requirement is user-specific ACL) and confirm with `getfacl`. Write `tunnel.sh` with the literal ssh flags `-f -N -i <key> -L 9090:localhost:8080 user@backend-server` — don't execute it. Finally chain fetch.exp | aggregator > metrics_out.json in run_pipeline.sh and run it to confirm the JSON file is produced correctly.
