Write `/home/user/process_graph.sh` as a single script that pipes SQL through `sqlite3 /home/user/graph.db`. Create `nodes`/`edges` tables matching the CSV headers, then import with `.mode csv` and `.import` (skipping the header row, e.g. import into a staging table then INSERT ... SELECT skipping row 1, or use `sqlite3 ... ".import --skip 1 file table"` if your sqlite3 version supports it). Add indexes on `edges(source)`, `edges(target)`, and a composite covering `timestamp`/`amount` used in the join/filter. For the core query, self-join `edges e1` to `edges e2` on `e1.target = e2.source` with the chronological and escalating-amount conditions, compute `total_amount = e1.amount + e2.amount`, then use a window function like `ROW_NUMBER() OVER (PARTITION BY e1.source ORDER BY total_amount DESC, e2.target ASC)` in a CTE and filter to `rn = 1` per starting node — a naive `GROUP BY` with `MAX()` won't correctly recover the associated middle/end node names, which is the trap here. Join to `nodes` three times to resolve A/B/C names, write to `top_paths.csv` unquoted CSV without a header (`.mode csv`, `.headers off`, `.once` or redirect), and separately run `EXPLAIN QUERY PLAN` on the identical query into `query_plan.txt`.