First identify the target file set precisely: use `grep -rl 'status: published' /home/user/docs --include='*.md'` to get exactly the published Markdown files — don't match non-.md files even if they contain the same string, and don't match .md files lacking that exact string. For each matching file, perform the replacement atomically: read the file, use `sed`/text substitution to replace `LegacyBrand` with `NovaBrand`, write the result to a temp file in the *same directory* (so the later rename is on the same filesystem, e.g. `mktemp` in that dir or `file.md.tmp`), then `mv` the temp file over the original — this guarantees no partial write is ever visible even if interrupted mid-write. After processing, compute `sha256sum` over exactly the set of files you modified (relative filenames, matching the format `sha256sum` produces) and write that to `/home/user/docs/manifest.sha256`, generated fresh from the post-replacement file contents so `sha256sum -c` on it succeeds. Leave draft `.md` files and non-Markdown files untouched, and verify with `grep` afterward that unmodified files still contain the old string while modified ones don't.