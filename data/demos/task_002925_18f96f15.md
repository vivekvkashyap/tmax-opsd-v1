Build the HTTP service with Flask (or http.server) bound to 127.0.0.1:9090 and check the `Authorization: Bearer GraphOrgSecret` header first thing in the handler, returning 401 immediately if it is missing or wrong — do this before touching the graph so unauthenticated requests never trigger a SPARQL query or subprocess call. Use `rdflib.Graph().parse("/home/user/datasets.ttl", format="turtle")` and build a SPARQL query with the `author`/`domain` query-string values bound as URIs (e.g. via `initBindings` with `URIRef(...)`, not raw string interpolation, to avoid injection and malformed queries), matching `dc:creator` and `dc:subject` on the same subject. Collect the `?dataset` bindings, sort them alphabetically as plain strings, join with `\n`, and pass that as stdin to `subprocess.run(["/app/dataset_hasher"], input=..., capture_output=True, text=True)`; strip the captured stdout before using it as the digest. Return the JSON body with `results` and `digest` exactly as specified. Keep the service running (background process or long-lived terminal) so the verifier's requests can reach it — don't let it exit after one request.
