Sequence this as: OCR first, then unpack, then filter, then build the concurrent C scaler, then orchestrate, then package. Run `tesseract /app/blueprint.png stdout` (installing tesseract if needed) and parse the line containing "SCALE: " to pull out the numeric factor programmatically — don't hardcode a guessed value, actually extract it from the OCR text output, and sanity-check it looks like a real decimal number since OCR can misread digits. Extract `/app/data.tar.gz` into `/home/user/raw_data`, then filter for valid GCode files by checking both the `.gcode` extension AND that one of the first 5 lines contains the exact string `; TYPE: PART` (use `head -n5 file | grep -q`). For `scaler.c`, parse each line looking for `X`/`Y`/`Z` tokens followed by a numeric value (e.g. tokenize on whitespace, check each token's first character), multiply just that numeric value by the scale factor, format with exactly 3 decimals (`%.3f`), and leave other parameters untouched. Crucially, wrap the append to `/home/user/process_wal.log` with `flock(fd, LOCK_EX)` before writing and unlock after — omitting this causes corrupted interleaved log lines when `run.sh` fires the scaler concurrently (e.g. via `xargs -P 4` or backgrounded loop). Finally zip the flat contents of `/home/user/scaled_data/` (no nested directory paths inside the archive) to `/home/user/final_project.zip`.