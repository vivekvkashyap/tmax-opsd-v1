Do not assume the aggregation formula from the task text alone — the exact rule (which trades to include, how to average, and how to work around the corrupted index) is only stated in the audio file, so transcribe `/app/audit_instructions.wav` first (e.g. with a local speech-to-text tool, or by listening carefully and writing down the instructions verbatim) and use that transcript as your spec before writing any SQL. Once you have the rule, write `/home/user/auditor.c` using the SQLite3 C API: split argv[1] on commas, and for each symbol run the query the voicemail describes, being careful to bypass the corrupted `idx_symbol` index (SQLite supports a `NOT INDEXED` clause on a table reference to force a full scan — test this by comparing query results with and without it to confirm the corrupted index actually changes results). Aggregate per the transcribed formula, format each score with exactly two decimals (`%.2f`), and emit a single-line JSON array sorted alphabetically by symbol with no extra stdout output. Compile with `-lsqlite3` and validate manually against a few symbols by hand-checking the DB with `sqlite3` CLI (using `NOT INDEXED`) before trusting the fuzz-equivalence grader.