Diagnose the race and the bottleneck as two separate fixes, and verify each independently before wiring up the full deploy script. For the startup race, modify processor.go's dial-to-port-9002 logic to retry (loop at least 5 times with a ~1-second sleep between attempts, e.g. wrapping `net.Dial` and only fataling after exhausting retries) instead of failing immediately, since aggregator.py has a fixed ~3s startup delay before it binds. In supervisord.conf, set `autorestart=true` on the processor program and give the aggregator a lower `priority` value than the processor (lower priority numbers start first in supervisord) so it's launched first, though the retry logic in Go remains the real fix for the race. For the throughput bottleneck, find where `regexp.MustCompile` (or any regex build) sits inside the per-line processing loop and hoist it to a package-level var or before the loop so it compiles once — better yet, since the log format is fixed-width space-separated fields, replace the regex entirely with `strings.Fields`/`strings.Split` parsing, which will comfortably clear the 50,000 logs/sec bar the regex-per-line version cannot. Write deploy.sh to `go build -o processor_bin processor.go`, then run `supervisorctl -c supervisord.conf update` and `restart all`, and make it executable. Benchmark manually against the throughput target before considering the task done.