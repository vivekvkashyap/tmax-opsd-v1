Build this bottom-up and verify each stage before chaining them: first get CFITSIO reading working standalone (`fits_open_file`, `fits_read_col` for the `M` and `V` double columns, 100 rows) and print the values to confirm they load correctly. Implement Newton-Raphson for Kepler's equation as its own small function `solve_E(M, e)` — start at E0=M, iterate `E -= (E - e*sin(E) - M) / (1 - e*cos(E))` until the update is below 1e-6 or 50 iterations, and unit-test it against a couple of known (M, e) pairs by hand before wiring it into the likelihood. For Metropolis-Hastings, seed with `srand(42)`, propose `K' ~ N(K, 1.0^2)` and `e' ~ N(e, 0.05^2)` via Box-Muller using `rand()`, reject immediately (never call the likelihood) if the proposal falls outside `[0,100]`/`[0,0.99]`, otherwise compute the log-likelihood via the solver over all 100 points and accept with the standard Metropolis ratio; run exactly 10000 steps including the t=0 state, storing K and e at every step. Write the two chains to `/K` and `/e` datasets of length 10000 in HDF5 using `H5Dcreate`/`H5Dwrite` with `H5T_NATIVE_DOUBLE`. Write the Makefile linking `-lcfitsio -lhdf5 -lm` with a `clean` target, and a `test.sh` that does `make clean && make && ./mcmc_fitter && h5dump` (or a `python3 -c "import h5py..."` one-liner) to assert the `/K` dataset has 10000 elements, exiting non-zero on any failure.