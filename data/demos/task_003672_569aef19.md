Work through the four stages in dependency order since later stages need earlier ones. Stage 1: grep the vendored library under `/app/PyJWT-2.8.0` for the HMAC verify method (in `jwt/algorithms.py`) — look for a suspicious line that always returns False or otherwise short-circuits the signature check, and restore it to a proper constant-time comparison (`hmac.compare_digest(sig, self.sign(msg, key))`), then `pip install -e` the package so your service imports the fixed version. Stage 2: disassemble the `.pyc` with `python -m dis` or `uncompyle6`/`decompyle3` (or just load and inspect the code object's constants) to recover the hardcoded secret string used for signing — do not fabricate a key, it must exactly match what the bytecode produces. Stage 3: build a minimal HTTP framework service exposing `/token` (sign a JWT with HS256 and the recovered key, embedding the `user` claim) and `/validate` (decode+verify, returning 401 on any exception). Stage 4: a raw socket server reading newline-delimited messages, computing `hashlib.sha256(msg.encode()).hexdigest()`, and appending `[<hex>] <msg>\n` to the log file. Launch both listeners as background processes before finishing, and manually test each endpoint with curl/netcat first.