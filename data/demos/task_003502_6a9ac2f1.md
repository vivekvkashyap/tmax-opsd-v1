Split this into the extraction step and the validator step, and test each independently before combining. For extraction, use `ffmpeg` or `opencv-python` (`cv2.VideoCapture`) to read `/app/experiment_record.mp4` frame-by-frame, stop after collecting frames 0-9, convert each to grayscale (`cv2.cvtColor(frame, cv2.COLOR_BGR2GRAY)` or ffmpeg's `format=gray`), compute the mean pixel value as a float, round to 2 decimals, and write a CSV with header `frame_id,intensity,event_flag` where `event_flag` is the literal string `False` for every row — double-check you're reading frame indices from 0, not 1, and that rounding happens after averaging, not before. For the validator, write `/home/user/validate_csv.py` to parse the CSV manually or with `csv.reader` (avoid pandas type-coercion surprises), enforce the exact header string, require frame_id to parse as int and be >= 0, intensity to parse as float in [0.0, 255.0], event_flag to be exactly "True" or "False" (case-sensitive, no other truthy strings), reject any row with wrong column count or empty fields, and exit(0) only if every row passes and exit(1) on any violation. Before finishing, hand-craft a few edge-case CSVs yourself (negative id, intensity 255.1, "TRUE", missing column, trailing comma) and confirm your validator rejects them, since this is exactly what an adversarial corpus test will probe.