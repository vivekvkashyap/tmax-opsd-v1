First recover the hidden threshold from `/app/reference_signal.wav` by transcribing it — install a speech-to-text tool (e.g. `pip install openai-whisper` plus `ffmpeg`, or `SpeechRecognition`) and run it on the file; read the spoken number carefully (it will be phrased in words, e.g. "zero point X Y") and convert it to the correct float threshold, don't guess. Then build `/home/user/etl_filter.py` to implement the three checks independently and exit non-zero the moment any fails: (1) split `text` on a single space and compare token count to `len(metrics)` exactly; (2) compute the Pearson correlation between the `feat_A` and `feat_B` sequences (e.g. `numpy.corrcoef` or `scipy.stats.pearsonr`) and require it strictly greater than the transcribed threshold; (3) bootstrap the `feat_C` values — set `np.random.seed(42)` immediately before the loop, do exactly 1000 resamples with replacement of the same length as `feat_C`, take the mean of each resample, then use `np.percentile` on the 1000 means at 2.5 and 97.5, and require the lower bound `>= 0.0`. Test your script against varied JSON files (including edge cases like mismatched token counts, weak correlation, negative feat_C) before trusting it, since it will be fuzzed against a clean/evil corpus expecting exact accept/reject behavior.