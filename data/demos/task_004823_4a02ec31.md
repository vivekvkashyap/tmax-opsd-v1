Do the reconnaissance before writing any Go code. First, transcribe `/app/voicemail.wav` to text (use an available speech-to-text tool/library, or inspect for any accompanying transcript/metadata) to recover the exact authorization passphrase — the header comparison must match this phrase exactly in lowercase with single spaces, so verify your transcription carefully rather than assuming. Second, inspect `/app/routing.sqlite` with `sqlite3` CLI (`.schema`, `.tables`, sample `SELECT`s) to confirm the actual table/column names for nodes, parent pointers, and root detection (root = row with NULL parent) rather than assuming names. Then write the Go service: use `net/http` with a handler for `POST /api/v1/route-to-root` that (1) checks the `Authorization: Bearer <phrase>` header against the recovered passphrase and returns 401 on mismatch, (2) parses `{"target_node": int}` from the JSON body, (3) runs a recursive CTE query (`WITH RECURSIVE ... `) against the SQLite DB via `mattn/go-sqlite3` to walk parent pointers up to the root, returning 404 if the node doesn't exist, and (4) responds with `{"path": [...]}` ordered from target up to root. Bind to `127.0.0.1:9090`, build the binary, and run it in the background so it stays reachable for testing.