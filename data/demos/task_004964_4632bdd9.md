OCR /app/bug_report.png with tesseract to read off the intensity multiplier mentioned in the report text, then write that exact number as a single float into multiplier.txt — don't guess it. To find the bugs in processor.c, write a small standalone C or Python harness that calls the extension in a loop while watching RSS (e.g. via /proc/self/status or psutil) to confirm unbounded growth, and separately call it with large pixel values/multipliers to see incorrect results from wraparound. The leak: the code builds a `PyObject* leaked` list every call and never Py_DECREF's it — remove that dead allocation entirely (it serves no purpose) or decref it before returning; also double check every PyObject you obtain (e.g. from PyList_GetItem, which returns a borrowed reference — don't decref that one, but do audit any owned references you create). The overflow: accumulation happens into a 16-bit `int16_t sum`, which wraps for realistic pixel*multiplier products — switch the accumulator to a wide type (`double` or `int64_t`) sized to the actual value range instead of truncating each intermediate sum. Rebuild with `python setup.py build_ext --inplace` (or install), restart server.py in the background on port 8000, and load-test it yourself (thousands of POSTs) to confirm memory stays flat and results are numerically correct before finishing.
