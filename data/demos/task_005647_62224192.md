Watch the data format: `M.csv` is real-valued, but `b.csv` holds complex numbers written with a generic string formatter, so a plain `np.loadtxt('b.csv', delimiter=',')` will likely mis-parse — load it with a converter that parses Python-style complex literals (or read lines and `complex()`-parse them manually) to get a proper complex128 array. For each candidate lambda in the given list, solve `(M^T @ M + lambda*I) @ x = M^T @ b` with `np.linalg.solve` (use `M.conj().T` for the complex transpose), take `v = np.fft.ifft(x)`, and compute tail energy as `np.sum(np.abs(v[50:])**2)`. Evaluate all candidates and explicitly track the *smallest* lambda whose tail energy falls below the 0.05 threshold — don't just stop at the first one tried. For that winning lambda, compute `np.max(np.abs(v[:50]))` and write both values into the required JSON structure, rounding the peak to 4 decimal places.
