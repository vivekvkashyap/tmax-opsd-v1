This task has four independent pieces — tackle them in order and verify each before moving on. (1) Read `/app/build_arch.png` (view the image directly or OCR it, e.g. with `tesseract`) to recover the exact gRPC port number and requests-per-minute rate limit written in the diagram; use those literal values verbatim later, don't approximate. (2) Apply `fix_abi.patch` in `/home/user/libmobilecore/` with `patch -p1 < fix_abi.patch` (or `git apply`), then rebuild with the provided Makefile so `libmobilecore.so` lands in `build/`; check the patch actually applied cleanly. (3) Generate Python gRPC stubs from `build_cache.proto` with `python -m grpc_tools.protoc`, then write `server.py` that loads `libmobilecore.so` via `ctypes.CDLL`, rejects empty cache keys, and implements an in-memory sliding/fixed-window rate limiter keyed appropriately that returns a `RESOURCE_EXHAUSTED` grpc status once the extracted-from-image limit is exceeded within a minute — test this boundary explicitly (e.g. fire >limit requests quickly) before trusting it. (4) For the diff parser, profile why it's slow (repeated string concatenation is the classic O(n^2) culprit) and rewrite using list accumulation/joins or streaming line-by-line parsing so output stays byte-identical to the original JSON while achieving at least 2.5x speedup — benchmark both versions on the actual large diff file to confirm the ratio before finishing.
