Before scripting, run `/home/user/sg-console.sh` manually by hand to observe its exact prompts (`Username:`, `Password:`, `SG>`) and behavior, since `expect` scripts are brittle to prompt-text mismatches. Add the two `export` lines to `/home/user/.bashrc` exactly as specified. Install `expect` via the system package manager if `which expect` fails. Write `/home/user/auto-sg.exp` using `spawn` on the console script, then chained `expect "Username:"` / `send "$env(SG_USER)\r"` pairs for each prompt in order (remember `expect` needs `send_user`/`log_user 1` or default logging so console output actually reaches stdout — don't suppress it), ending with `send "exit\r"` and an `expect eof`. Test this expect script standalone against one route before wrapping it in bash. For `/home/user/diagnose.sh`, read `routes.txt` with a `while read -r dest gw` loop, call `./auto-sg.exp "$dest" "$gw"`, capture its full output (e.g. into a variable via command substitution), and grep for the literal substring `"Reply from$dest"` (or "Reply from" followed by the destination) to decide success — then append the exact `[SUCCESS] <destination> via <gateway>` line to `/home/user/success.log`. `chmod +x` both scripts, then actually execute `diagnose.sh` and inspect the resulting log for correctness before finishing.
