First OCR `/app/spec.png` with `tesseract` and read the recovered spec text carefully rather than guessing at the logic — it defines the exact filter/sampling/aggregation steps and column order you must replicate. Implement the C++ program as a streaming pass over stdin: parse each CSV row (int ID, single-char Category, float Value), drop rows with negative ID, then per-category maintain a valid-row counter and only keep even-numbered occurrences (2nd, 4th, ...), and per-category maintain a sliding window (e.g. a small deque or fixed buffer of the last 3 kept values) to compute the moving sum, printing "Category,SampledValue,MovingSum" for each kept row. Since the grader requires bit-exact output against `/app/oracle_bin`, use plain `std::cout << value` for floats (default stream precision, no `setprecision`/`fixed`) to match the oracle's formatting, and handle empty input / fewer-than-3-samples windows without crashing. Before finalizing, exercise your binary yourself against a range of hand-crafted and randomized CSV inputs (negative IDs, single-category streams, categories A-F, mixed decimals) and diff the output against the oracle to catch formatting or off-by-one sampling mismatches.
