Build this as a Unix-pipeline-friendly transformation, keeping each script single-purpose so it composes cleanly in a shell loop. Write `/home/user/transformer.py` to read all of stdin, split off exactly the first three lines (Title/Author/Date), rewrite them as YAML frontmatter (`---` / `title: ...` / `author: ...` / `date: ...` / `---`) preserving the label values verbatim, then pass the remaining lines through completely unchanged including blank lines — don't accidentally strip trailing/leading blank lines when reassembling. For bulk processing, loop over `raw_docs/*.txt`, parse each filename with a regex like `draft_(\d+)_(.+)\.txt` to extract NNN and topic, pipe the file's contents through `python3 transformer.py` (via `subprocess` or shell `cat file | python3 transformer.py > out`), and save as `processed_docs/<topic>_<NNN>.md`. After all files are generated, compute `sha256sum processed_docs/*.md` but store only the basename (not full path) in each manifest line before writing to `processed_docs/manifest.txt` — do this before compression. Finally write `/home/user/compress.py` to read each `.md` file's raw bytes, `zlib.compress` them, write to `<name>.md.zlib`, then delete the original `.md` (never touch `manifest.txt`). Run both scripts end-to-end so the final directory only has `.zlib` files plus the manifest.