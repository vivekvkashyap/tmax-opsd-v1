Open `/home/user/paper_recommender/src/main.rs` and trace the TF-IDF computation step by step: term frequency should be `count of term in doc / total terms in doc`, but check the types involved in that division — if both `count` and `total_terms` are integer types (e.g. `usize`/`i32`), the division truncates to 0 whenever count < total_terms, which zeroes out almost every TF value and thus every similarity score. Fix it by casting to float BEFORE dividing (e.g. `count as f64 / total_terms as f64`), not after. Scan the rest of the file for any other places where an integer division happens before a cast to floating point — the same truncation bug pattern can hide in IDF or normalization code too. After fixing, run `cargo build` and `cargo run` against `/home/user/papers.json`, and confirm `/home/user/recommendations.csv` has one line per document with two distinct, non-self similar document IDs, in the format `document_id,most_similar_id,second_most_similar_id`. Sanity check a couple of rows by reasoning about shared vocabulary between docs.