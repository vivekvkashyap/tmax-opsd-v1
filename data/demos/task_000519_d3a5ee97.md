Do the OCR extraction first since the filter's thresholds depend on it, then build and adversarially test the Rust binary. Run `tesseract /app/calibration_specs.png stdout` (installing tesseract via apt if not present) to read off `EXPECTED_PEAK_CENTER` and `MAX_FWHM` from the image — use whatever numeric values you actually recover, don't guess round numbers. Scaffold the project with `cargo new spectro_filter`, then in `main.rs` parse the CSV (wavelength,intensity pairs) with a simple manual split or the `csv` crate, find the index/wavelength of minimum intensity, compute `I_half = 1.0 - (1.0 - I_min)/2.0`, and determine FWHM as the span between the smallest and largest wavelengths where intensity drops below `I_half` — be careful to scan the full array for all points below threshold rather than assuming a single contiguous dip, since noisy/multi-dip inputs should fail. Apply the two validity conditions (peak-center tolerance and FWHM ceiling against the OCR'd values) and `std::process::exit(0)` or `exit(1)` accordingly. Build with `cargo build --release`, then before declaring done, hand-construct a few edge-case CSVs (borderline FWHM, off-center peak, multi-dip noise) and confirm your binary accepts/rejects them as expected — the real grading corpus will probe exactly these boundaries.
