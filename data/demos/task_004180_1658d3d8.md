Work through the four subtasks independently and verify each before moving on. (1) Use `groupadd archive_grp` then `useradd -g archive_grp archive_usr`. (2) For the obfuscator, black-box test `/app/oracle/obfuscator_oracle` with a few known byte sequences via stdin/stdout redirection to confirm the exact transform order (reverse-then-XOR vs XOR-then-reverse give identical results here since XOR is per-byte, but still verify with a 1-2 byte input and an empty input); write straightforward C that reads up to 1024 bytes from stdin into a buffer, reverses in place, XORs each byte with 0x5A, and writes to stdout, then compile and diff its output against the oracle across several test inputs including empty and max-size before moving on. (3) Fill in the supervisord.conf skeleton with `[program:redis]`, `[program:smtp_receiver]`, `[program:archive_worker]` sections, each with `autostart=true`, no daemonizing (redis needs `--daemonize no` or use `--port 6379` alone since redis-server foregrounds by default), set `user=archive_usr` on the two Python programs, and add `environment=OBFUSCATOR_BIN="/home/user/obfuscator"` on archive_worker. (4) Write rollout.sh calling `supervisorctl restart archive_worker`, `sleep 2`, `supervisorctl restart smtp_receiver`, then chmod +x it. Do not start supervisord yourself.