Write a C++ program that reads `/home/user/survey.csv` with a simple CSV parser (fields have no embedded commas here, so splitting on `,` is safe, but the `text` field may itself contain the group value literally — parse by column position, not regex). Filter: skip rows where `group` is empty, or not exactly "A" or "B" (string equality, not substring). For surviving rows, tokenize `text` by splitting strictly on single spaces (careful: don't collapse multiple delimiters or trim in a way that changes the count) and count tokens; discard rows with token count > 20. Then for each of group A and B, compute sample mean, sample standard deviation using the **n-1** (Bessel-corrected) denominator, and the 95% CI as `mean ± 1.96*std/sqrt(n)`. Print each line with `printf`/`std::setprecision(2)` and fixed formatting so results are exactly two decimal places, matching the literal format `Group A: Mean=X.XX, CI=[Y.YY, Z.ZZ]`. Compile with `g++ -O2 -o analyze analyze.cpp`, run it to produce `/home/user/summary.txt`, and hand-verify your intermediate token counts and variance calculation against a couple of rows to catch off-by-one/rounding errors before finishing.