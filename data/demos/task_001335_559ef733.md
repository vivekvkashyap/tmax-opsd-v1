Start by running `cargo check` inside `/app/vendored/graph-mapper` to see the actual compile error — it will point to a missing dependency needed by a `#[derive(Deserialize)]` (or similar serde-derived) struct; add the missing crate (with the `derive` feature if needed) to that crate's `Cargo.toml` rather than rewriting the source. Then scaffold `/home/user/record-validator` with `cargo new`, and add `graph-mapper` as a `path` dependency pointing at the vendored directory in its `Cargo.toml`. For validation logic: build a directed graph from `graph.nodes`/`graph.edges`, and implement/reuse the library's cycle-detection (DFS with recursion-stack tracking, not just a visited set) and reachability/path-finding functions — a record is only valid if the graph is acyclic AND the doc_id node exists with type "Document" AND every dataset_ref exists with type "Dataset" AND each is reachable from the document node via directed edges. Print `ACCEPT <basename>` or `REJECT <basename>` per file, sourced from just the filename not the full path. Test against both `/app/data/clean_corpus` and `/app/data/evil_corpus` yourself before finishing — since 100% accuracy is required, check each rejection reason individually (cycle vs missing node vs wrong type vs unreachable) rather than trusting a single heuristic.