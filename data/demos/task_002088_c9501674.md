Use a bisection/delta-debugging mindset rather than eyeballing 50 files: run `/home/user/pipeline/ingest.sh` and note it dies on the first bad decode; loop over files in `/home/user/pipeline/data/*.enc` yourself with `base64 -d < "$f" >/dev/null 2>&1 || echo "$f"` to programmatically isolate the one file that fails standard base64 decoding (it's actually encoded with a different scheme — try `base32 -d` on the failing file to confirm). Move that exact file into `/home/user/pipeline/quarantine/`, preserving its filename, and leave the rest untouched. Next, read `ingest.sh` carefully for the state-leak bug: look for a variable (e.g. an "alert" or "critical" flag) that gets set inside the loop but is never reset at the top of each iteration, so once one file trips it, it stays true for every subsequent file — fix by explicitly resetting/unsetting that variable at the start of each loop iteration (or scoping it locally). After both fixes, rerun `ingest.sh`, confirm it exits 0, and diff the resulting `/home/user/pipeline/out/summary.txt` against what you'd expect from manually decoding each remaining file and checking for a literal `STATUS: CRITICAL` line.
