Two independent pieces feed into each other. First, recover the threshold: run `tesseract /app/config_formula.png stdout` (installing tesseract-ocr if needed) to OCR the handwritten note, then parse the resulting text for the numeric value after "THRESHOLD=" — don't guess the number, read it from the OCR output, and sanity-check the OCR result looks like a plausible float before hardcoding it into your Rust code. Second, scaffold with `cargo new filter_logs` in /home/user/filter_logs, add the `csv` and `serde` crates to Cargo.toml, and write main.rs to take the CSV path as `std::env::args().nth(1)`, deserialize each record's `inference_time_ms` and `batch_size` as f64, accumulate the sum of their per-row products, divide by row count to get the mean, then `std::process::exit(1)` if `mean > threshold` else `exit(0)` — use strict `>` vs `<=` exactly as specified since boundary cases in the adversarial corpus likely probe this. Ensure no `println!`/`eprintln!` remain in the final binary. Build with `cargo build --release` and manually test against a couple of clean/evil-style CSVs you construct yourself before trusting the corpus run.
