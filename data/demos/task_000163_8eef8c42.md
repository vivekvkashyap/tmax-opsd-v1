Work in two phases. First get the three services actually running: start `redis-server --daemonize yes` and confirm with `redis-cli ping`; edit `/home/user/nginx.conf` so `listen` matches port 8080 and `root` points at `/home/user/schemas/`, then start nginx and curl a schema file to confirm 200 OK; then run `/home/user/start_generator.sh` and verify it appends to files under `/tmp/project_logs/` (it will likely fail silently if nginx/redis aren't reachable first, so fix those before starting it). Second, write `wal_parser.py` as a state machine over lines: open with `gzip.open` if the filename ends `.log.gz` else plain `open`, in text mode; scan for `[WAL_START] TXN=<id>`, then the `ENCODING=` line, then collect payload lines until `[WAL_END] <id>`; discard any block that never reaches END (don't buffer half-open state past EOF). For utf-8 blocks, join payload lines as-is; for utf-16le blocks, each payload line is hex text — `bytes.fromhex(line)` then `.decode('utf-16le')` per line before concatenating, since decoding must happen line-by-line, not by hex-joining raw bytes across newlines. Strip the final concatenated payload. Emit `json.dumps` with stable key order and no extraneous whitespace differences from a reference — test round-trips with `json.loads` equivalence, not exact string match assumptions.
