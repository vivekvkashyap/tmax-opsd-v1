Build and test the C filter program standalone before wiring it into the pipeline: read stdin line by line with `fgets` (buffer sized for at least 1024+ chars, growing or truncating safely for longer lines), compare each line's prefix against the literal string `"[DEBUG] "` (note the trailing space — match exactly that prefix, not just `[DEBUG]`), and write every non-matching line unchanged to stdout; compile with `gcc` to `/home/user/filter` and sanity-check it with a small piped example before running it over real data. For the main pipeline, process each `.log.gz` file individually: `zcat file | iconv -f ISO-8859-1 -t UTF-8 | ./filter | gzip > /home/user/new_logs/<name>` — get the iconv direction right (from ISO-8859-1, since that's the source encoding, to UTF-8) and insert `.filtered` before `.log.gz` in the output filename (e.g. via a small shell/sed manipulation of the basename), not appended after it. After generating all filtered files, `cd` into `/home/user/new_logs` and run `sha256sum *.filtered.log.gz > /home/user/manifest.sha256` so the manifest paths are basenames only, then confirm with `sha256sum -c` from that directory that the manifest verifies successfully.