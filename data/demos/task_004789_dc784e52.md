First write a standalone regression test that exercises the analyzer's timeline reconstruction with a log sequence spanning a midnight boundary plus a clear latency spike, asserting the spike is reported as an anomaly — make sure this test fails on current HEAD and passes on the `v1.0` tag before trusting it. Then run `git bisect start`, mark `HEAD` bad and `v1.0` good, and use `git bisect run` with a small wrapper script that checks out the repo state, installs/reloads the package, runs your regression test, and exits 0/1 accordingly — this automates the ~200-commit search to the exact offending commit. Once found, read the diff of that commit rather than blindly reverting: look specifically at how timestamps are parsed/truncated and how variance/mean are computed when reconstructing timelines, since crossing-midnight bugs typically come from a broken or lossy timestamp parse (e.g. discarding the date and keeping only time-of-day) rather than the statistics formula itself. Apply a targeted fix preserving other commits' features, confirm your regression test and any existing test suite pass, then start the server with `python -m log_timeline_api.server --port 9000` bound to `127.0.0.1:9000` in the background and manually POST/GET the `/ingest` and `/anomalies` endpoints to confirm midnight-crossing anomalies are detected before leaving it running.