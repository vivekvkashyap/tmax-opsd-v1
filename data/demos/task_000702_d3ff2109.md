Do this in strict pipeline order, checking intermediate artifacts before trusting the next stage. First `rustc sim.rs` and run it to produce trace.csv, then actually inspect the file (row count, value ranges) rather than assuming it worked. In analyze.rs, implement the DFT directly from its definition (nested loop over candidate integer frequencies 1..10, computing real/imag sums of y[t]*cos/sin(2*pi*f*t) over all samples, then picking the frequency with max magnitude) — verify by printing the magnitude spectrum and confirming one frequency clearly dominates before hardcoding anything downstream. For the Euler integrator, implement the two coupled first-order updates literally (`v += dt*(-k*v - (2*pi*f_dom)^2*y); y += dt*v`, updating in the correct order/using the same-step or leapfrog convention consistently) starting from y=1,v=0. For MCMC, write a small LCG (`state = state*a + c mod m`, mapped to [0,1)) to drive proposals, implement a Metropolis accept/reject step comparing log-likelihoods (sum of squared residuals between simulated y and trace.csv's y at matching t, scaled by 1/(2*sigma^2)), run 5000+ iterations, discard the first 20% as burn-in, average the rest, and round to 1 decimal before writing to k_estimate.txt. Sanity-check convergence by printing acceptance rate and a running average.