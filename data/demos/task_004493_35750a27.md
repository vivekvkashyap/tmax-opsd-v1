Don't guess the fix from reading the code alone — reproduce the crash first (`cd /home/user/workspace/async_engine && make test`, or run the binary under `gdb -ex run -ex bt --args ./test_cancellation`) and get a backtrace to confirm exactly which function faults and what signal it is. Since the test cancels the engine before any tasks complete, suspect any place that divides by a "count" that could be zero (e.g. computing an average/mean over completed tasks) — SIGFPE from integer division is the classic signature of divide-by-zero, distinct from a segfault. Locate that function in `engine.cpp`, add a guard (return 0 or another sensible default when the denominator is zero) without touching `test_cancellation.cpp`, rebuild, and confirm `make test` now exits 0. Then write `/home/user/workspace/bug_report.txt` naming the exact function where the crash occurred and explicitly stating it was caused by a division producing a domain/zero error, so grep-based checks for the function name and for "divide/zero" both match.
