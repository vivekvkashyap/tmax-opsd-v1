Design this as a streaming pipeline, not a script that loads the whole file — use a tool that understands CSV quoting semantics (embedded newlines inside quoted fields are legal CSV and a naive `while read line` will split them incorrectly). A proper CSV-aware reader (e.g. Python's `csv` module reading line-by-line/row-by-row with `csv.reader`, or a streaming AWK/Perl CSV library) will already merge a quoted multi-line field into one logical record; from there, join any embedded newline characters within that field into a single space as part of your row-processing step. Normalize the level column with a simple uppercase transform (e.g. `.upper()` or `tr`), filter rows where it equals exactly `ERROR`, then tally counts per service using a dict/associative array, and emit rows sorted by service name alphabetically with the required header. Make the script executable (`chmod +x`), pipe input directly rather than reading the whole file into a variable, and run it end-to-end against the real log file to confirm the aggregated output is produced before considering it done.