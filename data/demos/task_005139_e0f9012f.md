Do the graph work inside SQLite using recursive CTEs rather than hand-writing tree-walking logic in C — C should only issue the SQL, capture results via `sqlite3_exec`/prepared statements, and format output files. For leaf-chain totals: first identify leaves as backups whose `id` never appears as a `parent_id` (`NOT IN (SELECT parent_id FROM backups WHERE parent_id IS NOT NULL)`), then for each leaf use a recursive CTE that starts at the leaf and repeatedly joins to `parent_id` until reaching a NULL parent, summing `size_mb` along the way; order the final leaf list by total size descending. For the top-ancestor: use a recursive CTE seeded from each candidate root/node that walks all descendant chains, count descendants per starting id (excluding the node itself), and pick the max count with smallest id as tiebreaker (`ORDER BY count DESC, id ASC LIMIT 1`) — don't just eyeball the small sample data, since the real check may run against a different or hidden dataset with the same schema. Compile with `gcc analyzer.c -lsqlite3 -o analyzer`, run it, and diff both output files' exact formatting (no headers, comma-separated, trailing newlines) against what the spec describes before considering it done.