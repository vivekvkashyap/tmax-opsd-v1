Diagnose before editing configs. First tail/grep the supervisor logs in `/home/user/logs/` to find the exact FATAL reason from `api-service` — it's a startup check tied to the size of `/home/user/app_data/`, so `du -sh /home/user/app_data/` to confirm, then remove or shrink the oversized file(s) there so the directory is under whatever threshold the service enforces (check the service's own startup script/code for the exact check if present). Second, edit `/home/user/supervisor/supervisord.conf` under the `[program:api-service]` stanza and set `autorestart=true` and `startretries=5` explicitly (don't just remove the false setting — write the correct values). Third, open `/home/user/nginx/nginx.conf` and fix the `proxy_pass`/upstream directive so it points at `127.0.0.1:5000` (the actual api-service port) and that nginx itself binds `listen 8080`. Apply changes with `supervisorctl -c /home/user/supervisor/supervisord.conf reread && update`, or restart supervisord cleanly, then use `supervisorctl status` to confirm all three programs show RUNNING. Finally curl `127.0.0.1:8080/ping` and POST to `/data` yourself to confirm before considering it done — don't just trust config edits.
