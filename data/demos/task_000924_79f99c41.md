Write a single-pass streaming Python script: open `/home/user/raw_logs.txt` and iterate line by line (never `read()` the whole file), applying one compiled regex that anchors the full expected format — timestamp, `[LEVEL]`, `[UID:...]`, `Action <NAME>`, `resulted in code <3-digit>`, `Latency: <int>ms`, `Msg: ...` — and silently `continue` on non-matches (most lines are junk). For stratified top-3-per-code sampling without loading everything into memory, maintain a `dict[code] -> list` of at most 3 current best entries; for each new match, either append if under 3, or replace the current minimum-latency entry if the new latency beats it (ties broken by keeping earlier-appearing entries, so only replace on strictly greater latency) — a small heap or just a linear min-scan over 3 items is fine. After scanning, sort codes ascending, and within each code sort its (≤3) entries by latency descending. Read `/home/user/report_template.md` and substitute your generated `## Code: <code>` sections into it (check the template's exact placeholder before assuming its structure), formatting each entry line exactly as `N. UID: <uid>, Action: <action>, Latency: <latency>ms`, then write the result to `/home/user/final_report.md`.