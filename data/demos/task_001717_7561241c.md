Memory dumps store base64 payloads as contiguous printable-ASCII runs mixed into random binary noise, so a plain grep for base64 alphabet characters works well. Use `strings -n <minlen> /home/user/process.dmp` (or a regex scan) to pull out long runs of `[A-Za-z0-9+/=]` characters, since real base64 payloads will be noticeably longer than incidental matches in random bytes. For each candidate string, try to base64-decode and zlib-decompress it to confirm it is valid JSON before treating it as a real payload — this filters out junk. Then loop over the surviving valid-looking payloads, invoking `python3 /home/user/math_service.py "<payload>"` for each one and capturing stderr; only one will raise an `AssertionError` (the others will either succeed silently or fail some other way that gets swallowed). Capture that exact traceback's assertion message text, and write the exact matching base64 string (no extra whitespace/newlines) to `/home/user/crashing_payload.txt`, and the exact assertion message to `/home/user/error_msg.txt`. Read `math_service.py` first to understand exactly what triggers the crash so you can distinguish it from payloads that merely fail silently.