Work through this as five discrete, verifiable steps rather than one long pipe. First, find candidate files with `find /home/user/app_config -mount ... -type f -mtime -1` — but plain `find` will hang or loop on the self-referential symlinks described, so add `-maxdepth` bounding and/or track visited inodes, or simply exclude symlinked directories from descent (`find` without `-L`, i.e. don't follow directory symlinks, since that's what creates the infinite loop) while still resolving symlinks that point to regular files. Test this find command in isolation first and confirm it terminates and returns exactly the recently-modified regular files, excluding the old file and any broken/looping links. Sort the matches by basename alphabetically, then `cat` them in that order into the pipeline feeding `split -b 100`. Write split's output chunks into a fresh `/home/user/staging_backup` directory first. Only after `split` completes there, move the chunk files into `/home/user/backup_chunks/` using `mv` (moves within the same filesystem are effectively atomic) rather than writing directly into the final directory, so a crash mid-split never leaves partial chunks visible in the backup location.