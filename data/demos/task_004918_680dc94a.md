Use OpenCV (or ffmpeg) to read `/app/integrator_profile.mp4` frame by frame; for each frame find pixels matching pure red (255,0,0) via `np.where`, and record the y-coordinate of the match (average if the marker spans multiple pixels). Split the resulting 100-length series into frames 0-49 and 50-99. Compute the 1st Wasserstein distance between the two phases with `scipy.stats.wasserstein_distance` rather than hand-rolling EMD. For the MCMC step, implement a simple Metropolis-Hastings sampler over (mean, std) with a Gaussian log-likelihood on Phase 2 data and uniform priors U(0,480)/U(0.1,200): propose small Gaussian jumps, accept via the log-likelihood-ratio rule, run 10,000+ iterations, discard the first 1,000 as burn-in, and average the remaining samples for posterior_mean/posterior_std. Finally stand up a small HTTP server (Flask/http.server) bound to 0.0.0.0:8888 exposing GET /analysis that checks the exact `Authorization: Bearer perf-agent-secret` header and returns the three floats as JSON; launch it as a background/daemon process so it stays alive for the verifier to query.
