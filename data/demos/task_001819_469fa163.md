Reverse-engineer the checksum algorithm from the core dump and logs rather than guessing: load the core dump in gdb (`gdb /app/legacy_hasher /app/cores/core.legacy_hasher.12345`), inspect the backtrace/registers/stack to see the hash state and constants in use at crash time, and cross-reference /app/logs/container.log to identify the exact input line that triggered the crash (this tells you both the crash-triggering pattern to defend against and, combined with disassembly of the hash loop with `objdump -d` or gdb's `disas`, the hashing constants/operations). Also feed known simple inputs (empty string, single chars) to /app/legacy_hasher directly and record its output to cross-check your derived formula before finalizing it. In fast_hasher.go, implement the derived per-byte hash update in a tight loop with no dynamic allocation per line for speed, and explicitly guard against whatever caused the legacy crash (e.g. a specific substring or byte pattern) so your Go version never segfaults or panics on it — read the crashing case's behavior from the logs, don't hardcode a fix only for the literal string you see if the underlying bug is more general. Read stdin line by line, hex-encode the checksum in lowercase, and write test_harness.sh to diff outputs between the two binaries on safe (non-crashing) sample inputs to confirm bit-exact equivalence.
