Read `vnc_manager.go` first to find the exact block that checks `X-VNC-Hardening` and hijacks/closes the connection when it's missing or wrong — replace that hijacking logic with a normal `http.Error(w, "Hardening check failed", http.StatusForbidden)` (or equivalent `WriteHeader(403)` + plain-text body write), keeping the rest of the handler (including any TZ check) intact for when the header IS present. For `deploy.sh`, write a script that compiles the Go file with `go build`, then explicitly `export TZ=Asia/Tokyo` and `export VNC_PORT=8080` in the same shell before launching the compiled binary in the background (`&`), so the exported env vars are inherited by the child process — env vars set after backgrounding or in a subshell that doesn't reach the binary won't work. For the diagnostic tool, write plain Go (or run it with `go run`) that issues an `http.Get` to `/status` without ever setting the hardening header, reads the status code and body, and appends a line to the log file in exactly the `Result: {code} - {body}` format — open the log file in append/write mode and check for write errors. Run `deploy.sh` first, wait for the server to be listening, then run the diagnostic.
