Build `process_payload` as a pure, testable function separate from the file-walking/POST logic so the hidden adversarial-corpus test can import and call it directly. For interpolation: group records by `station_id`, sort by `timestamp` within each group, then for each null `temperature`/`humidity` scan backward and forward for the nearest non-null neighbor and apply standard linear interpolation `v = v0 + (v1-v0)*(t-t0)/(t1-t0)`; if either bound is missing, drop the record. After imputing, apply the numeric range checks and drop violators. For the `note` field, work on Unicode codepoints, not bytes: use `unicodedata.normalize` cautiously (don't strip valid non-ASCII) and `unicodedata.combining()` / `unicodedata.category()` to count consecutive combining marks (`Mn`/`Me`) attached to each base character — reject if any base character has more than 2. Separately scan for the explicit BiDi control codepoints (U+202A–U+202E) and reject on any match. Test your function against constructed clean and adversarial note strings (legit Arabic/Japanese/diacritics vs. zalgo/BiDi payloads) before wiring up the HTTP POST step.