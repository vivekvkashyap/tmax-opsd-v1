Tackle the three parts in order. First, `make` in `/app/safe-router-1.1.0` and check whether the failure is a "missing separator" error — Makefiles require tab-indented recipe lines, so if the file was edited with spaces, replace the leading whitespace on each recipe line with actual tab characters. Then in `main.go`, find where the upstream Unix socket path is hardcoded (something like `/var/run/broken.sock`) and change it to read from `os.Getenv("UPSTREAM_SOCK")` instead. Second, implement `IsMalicious` in `filter.go` to check, in order: the URL path for `../` and its encoded variants (decode percent-encoding, including double-encoding, before checking, similar to detecting `%2e%2e%2f`); whether `X-Forwarded-Host` header is present and differs from the `Host` header; and whether `User-Agent` contains `sqlmap` or `nmap` case-insensitively (lowercase both sides before substring match). Validate against `/app/corpus/evil/` and `/app/corpus/clean/` using the binary's own `verify` subcommand until you get 100% on both. Third, write `/home/user/ci-run.sh` (chmod +x) that builds the binary, creates `/home/user/backend/`, symlinks `/home/user/active-upstream.sock` to `/home/user/backend/app.sock`, exports `UPSTREAM_SOCK` to that symlink path, runs the verify command, and exits non-zero on any build or verification failure.
