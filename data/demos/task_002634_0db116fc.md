For the video metric, extract frames with `ffmpeg -i /app/dashboard.mp4 -vf fps=<source fps> frame_%04d.png` (match the source frame rate so you count actual frames, not a resampled subset), then for each frame check whether every pixel is exactly pure red (255,0,0) — e.g. with Python/PIL/numpy, load each frame and test `np.all(arr == [255,0,0])` or use `convert`/`identify -format "%[max]"` tricks; count frames that pass and write that integer to the output file. For the Git cost control, `git init --bare /home/user/finops_rules.git`, then place an executable `hooks/pre-receive` script that reads the pushed refs from stdin, uses `git diff <old> <new>` (or inspects the new commit's tree) for each ref, greps the diff for the exact banned string, and on a match echoes the exact rejection message to stderr/stdout and exits non-zero (any non-zero exit aborts the push). Test it yourself with a throwaway clone before moving on. For the API, run a background HTTP server on 9090 implementing `/metrics` (returns the JSON with your counted value) and `/webhook` (checks the exact `Authorization: Bearer <token>` header value, 200 if it matches, 401 otherwise) — verify both endpoints with `curl` while the process is still running.
