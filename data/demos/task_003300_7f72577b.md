Start by transcribing `/app/instruction.wav` with an available tool (e.g. `whisper` CLI or an ASR Python library) to recover the exact algorithm spec spoken in the audio — the spoken instructions define every detail you need (which features to compute from the FASTA sequence, any per-character rules, and the precise output format/line structure), so don't guess or paraphrase from assumptions; transcribe carefully and re-listen to resolve ambiguous words. Then implement `/home/user/extractor.c` in C strictly per that transcript: read stdin, skip the FASTA header line up to and including its newline, then process the remaining characters (ignoring embedded newlines) to compute exactly what was specified, printing output in the exact format described. Be meticulous about exact output formatting (decimal places, trailing text, line endings, letter case handling) since a fuzz-equivalence check will diff your program's output byte-for-byte against a hidden reference across ~1000 randomized FASTA inputs — test your own binary against several hand-built FASTA strings (including edge cases like empty sequence, lowercase letters, and sequences with newlines inserted at odd positions) before finishing, since silent off-by-one or case-handling bugs will fail many fuzz cases at once. Compile with `gcc -O2 -o /home/user/extractor /home/user/extractor.c` and leave the binary in place.