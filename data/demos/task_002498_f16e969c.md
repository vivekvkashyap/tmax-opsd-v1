Use `git bisect` methodically rather than eyeballing the log: reproduce the hang first with the known-bad HEAD against /home/user/capture.pcap (e.g. `timeout 3 cargo run -- /home/user/capture.pcap`, confirming it never returns / times out), then find a known-good early commit (e.g. the first commit) and run `git bisect start`, `git bisect bad HEAD`, `git bisect good <early-commit>`, and at each step build and run with a short timeout, marking `git bisect good`/`bad` based on whether it hangs — automate this with `git bisect run` calling a script that does `cargo build && timeout 3 cargo run -- capture.pcap; test $? -ne 124` for a fully unattended bisection. Once found, record the full 40-char hash (`git rev-parse HEAD` at the bad commit) into /home/user/bad_commit.txt. For the actual fix in src/parser.rs, look specifically at the branch handling a TLV with `len == 0`: if it pushes a node but never advances `offset`, the while loop spins forever re-reading the same bytes — fix it by always advancing offset by at least 2 (the type+length header) even in the zero-length case, so the parser makes forward progress on every iteration regardless of len. Rebuild and confirm `cargo run -- /home/user/capture.pcap` completes quickly without hanging or panicking.