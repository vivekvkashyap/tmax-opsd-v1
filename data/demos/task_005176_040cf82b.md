This is a constrained-optimization / dependency-resolution problem, not a simple greedy pick — solve it with exhaustive or backtracking search over the small version space, since greedily picking the cheapest version per package can violate a shared dependency's constraint (e.g. two packages both depending on the same package with different version ranges). Approach: parse the JSON, then recursively/BFS expand from `root`: for a candidate version of a package, look up its dependency requirements, and for every package touched anywhere in the tree, track the intersection of all constraints imposed on it by different parents (a package chosen must satisfy every depender's min/max simultaneously — compute the intersection, not just one parent's range). Since the graph is small, enumerate all self-consistent global assignments (all combinations of valid versions for every package reachable from root, respecting per-package single-version and all incoming constraint intersections) and pick the one with minimum summed cost. Parse versions as floats for comparison as instructed. Write `/home/user/min_cost.txt` as a 2-decimal formatted float and `/home/user/resolution.json` as a flat package->version mapping, then actually execute the script and check the output files exist with sane values before finishing.
