For the pipeline, inspect `start_pipeline.sh` and the Nginx config under `/app/pipeline/` to see what's disconnected — Nginx needs a `proxy_pass` block routing all `/` traffic on 8080 to `127.0.0.1:5000`, and each service (Redis default port, Flask on 5000, Nginx on 8080) must actually be started and bound correctly; test with a manual `curl -X POST` to `localhost:8080/data` and check Redis via `redis-cli` before moving on. For `stable_stat`, implement Welford's algorithm exactly as specified — a single pass updating `count`, `mean`, and `M2` incrementally, never computing sum-of-squares or subtracting large sums (that reintroduces the cancellation you're avoiding) — using double-precision floats throughout, since bit-exact matching against the oracle depends on operation order. Derive `variance = M2/(count-1)` (0.0 if count<2), and print the single output line with both numbers formatted to exactly 8 decimal places using your language's fixed-point formatting (e.g. `%.8f`), matching the exact `Mean: ..., Variance: ...` text.
