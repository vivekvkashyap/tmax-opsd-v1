Port the C extension methodically: replace `PyInt_FromLong` with `PyLong_FromLong` (Python 3 has no separate int type), and replace the `PyMODINIT_FUNC initc_math(void) { Py_InitModule(...); }` pattern with a `static struct PyModuleDef` describing the module (name, doc, size, method table) plus a `PyMODINIT_FUNC PyInit_c_math(void) { return PyModule_Create(&moduledef); }` — the init function name must match `PyInit_<modulename>` exactly or the import will fail. For the LITE_MODE conditional, wrap the division branch in `#ifdef LITE_MODE ... return -999; #else ... normal division ... #endif` (compile-time preprocessor branching, not runtime) so the two builds produce genuinely different binaries. In setup.py, read `os.environ.get('BUILD_MODE')` and conditionally pass `define_macros=[('LITE_MODE', '1')]` to the Extension() call only when BUILD_MODE == 'lite'. Since setuptools caches build artifacts, do a clean rebuild between the two variants (e.g. `rm -rf build` or use `--force`) so Step B's binary isn't accidentally the stale lite-mode one — verify this by actually testing that Step A's evaluation differs from Step B's before appending both results, in order, to run.log.