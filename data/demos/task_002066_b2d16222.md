Build this in isolated, independently-verifiable stages. First, transcribe `/app/sensor_log.wav` with a local speech-to-text tool (e.g. `whisper.cpp` or Python `speech_recognition`/`vosk`) and print the raw transcript to inspect it yourself — don't assume perfect transcription; parse the operator ID and "Second X, value Y" pairs with a tolerant regex since ASR output can have minor formatting quirks. Second, in your C++ parser: drop any value <0 or >100 first, then resample onto the fixed 10-second grid by detecting gaps in the surviving timestamps and linearly interpolating between the two nearest surviving points for any missing 10s slot. Mask the operator id to the literal string "MASKED" in the response, never the real parsed value. Compute MAE against `/app/baseline.csv` only over timestamps present in both series after resampling. Third, for the HTTP server (roll your own minimal one, or vendor `cpp-httplib`), enforce the exact header `Authorization: Bearer secret_ts_2024` on both routes, returning 401 otherwise, and implement `/api/telemetry` and `/api/mae` per the given JSON shapes. Finally write `run_server.sh` to compile and launch it in the background, then curl both endpoints yourself (with and without auth) to confirm behavior before finishing.