Start by OCR'ing /app/disk_alert.png with tesseract to identify which volume (e.g. `VOL-XXX`) is flagged — read the OCR output carefully rather than assuming a format, since the exact volume name determines which inner archive you extract next. List the contents of /app/storage/backups.tar (`tar -tf`) to find the `.tar.gz` entry matching that volume name, extract only that one member (`tar -xf backups.tar <member>`), then untar it to get the volume's files.

Within the extracted volume, distinguish file types by content, not extension: read each file's first bytes — ELF executables start with the magic `\x7fELF`, WAL files start with the 4-byte magic `WAL\0`. Sort ELF files into /home/user/elf_backup/ renamed sequentially (elf_001.bin, elf_002.bin, ...) and collect WAL files for compaction.

Write compact_wal.c to parse the WAL binary format precisely: after the 8-byte header (4-byte magic + 4-byte little-endian version, use e.g. `uint32_t` reads with correct endianness), loop reading each record's 1-byte type and 4-byte little-endian length, then that many payload bytes; copy the header and every record whose type is NOT 0x03 (Debug) — types 0x01 and 0x02 — verbatim to the output file, skipping (not writing) Debug records and their payloads entirely. Be careful to advance your read offset by exactly `5 + length` bytes per record regardless of type, or you'll desync on the very next record. Run your compiled binary over every identified WAL file, writing sequentially numbered outputs (clean_001.wal, clean_002.wal, ...) into /home/user/wal_clean/, and validate your compaction logic against a small hand-crafted WAL file with a known record layout before trusting it on the full set, since the verifier checks an exact output byte size.