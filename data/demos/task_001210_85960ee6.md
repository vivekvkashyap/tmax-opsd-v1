Start by transcribing the audio (e.g. `whisper /app/analyst_notes.wav` or another available speech-to-text tool) and re-listen/re-read the transcript carefully to capture the exact traversal rule (which relationship types to follow), any exclusions (e.g. whether the start node itself is included), the required output ordering, and the exact output format — these fine details, not the general "graph traversal" concept, are what the bit-for-bit comparison against the reference implementation will catch. Implement `/home/user/kg_query.cpp` to parse the headerless 3-column CSV (`source,target,relationship_type`), build an adjacency structure, and perform a BFS/DFS from `argv[2]` following only edges whose relationship type matches the criteria you transcribed, tracking visited nodes to avoid infinite loops on cycles. Apply the exact output rules from the audio (ordering, inclusion/exclusion of the start node, one item per line vs. other formatting) precisely, since the grader diffs raw stdout against a compiled oracle across hundreds of randomized graphs. Compile with `g++ -std=c++17 -O2 /home/user/kg_query.cpp -o /home/user/kg_query` and test manually against a small hand-built CSV where you know the expected reachable set before trusting it on larger inputs.