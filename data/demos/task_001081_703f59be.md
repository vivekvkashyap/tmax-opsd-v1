Install the dependency first (`apt-get install -y libgsl-dev` or equivalent) and confirm `gsl/gsl_statistics_double.h` is findable before writing code that includes it. In `process_data.c`, read the file line by line; for each line, strictly validate it splits into exactly two comma-separated fields and that both parse fully as floating point (e.g. `strtod` with an end-pointer check that nothing but optional whitespace/newline remains) — any violation anywhere in the file (wrong column count, non-numeric text, stray blank line) means the whole file is invalid: print exactly `SCHEMA_ERROR` and stop, don't partially process. If fully valid, collect the second-column values into a `double[]` array and call `gsl_stats_variance` (sample variance, i.e. divides by n-1) on it, printing the result with `printf("%.4f\n", ...)`. Compile with `-lgsl -lgslcblas -lm`. For `run_experiments.sh`, glob `/home/user/data/*.csv`, sort the list (e.g. `for f in $(ls /home/user/data/*.csv | sort)`), invoke `process_data` on each, and append `<absolute_path>,<program_output>` lines to `/home/user/tracker.csv`, using `realpath`/`$(pwd)` to ensure paths are absolute even if the script is run from elsewhere.