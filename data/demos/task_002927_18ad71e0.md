Start by inspecting the core dump with `gdb /app/worker/math_worker /app/cores/<core>` — run `bt`, `bt full`, and examine the frame where the crash occurred to localize the memory corruption (likely an off-by-one buffer/index bug or unbounded write in the polynomial-hashing loop in `math_worker.cpp`). Cross-check the suspect logic line-by-line against the reference oracle's Python implementation to find where the arithmetic or bounds diverge (wrong loop bound, wrong modulus/base, sign error, etc.), fix it, and rebuild with the same compiler flags the project uses so the binary stays at `/app/worker/math_worker`. Before trusting the fix, generate many random string inputs yourself and diff `math_worker`'s output against the oracle script's output — don't declare it fixed until outputs match bit-for-bit across varied lengths/edge cases. Separately, the service wiring is a config mismatch, not a code bug: update `/app/api/.env` (`REDIS_PORT`, `REDIS_CHANNEL`) and `/app/worker/config.ini` (`queue_channel`) so both sides agree on `math_jobs`, then restart Redis, the Node API, and the worker, and confirm the full round trip with a `curl -X POST http://127.0.0.1:3000/compute` request.
