Treat this as a git regression hunt, not a blind rewrite. First reproduce the slowdown against a small sample log so a single check runs in well under a second. Use `git log` to see recent history, then either `git bisect` (with a script that builds and times the tool) or manually diff recent commits touching the parsing loop to find where a cheap check was swapped for something expensive — a classic culprit is a fast library call being replaced by a regex or backtracking-prone matcher that chokes on the malformed edge-case lines (mismatched brackets, escaped quotes) mentioned in the prompt. Before touching the fix, run `nm -D` (or `objdump -T`) on the shared library to confirm which fast symbol is still exported and callable — don't assume it was removed. Patch the C++ source to restore the efficient call path while still correctly handling the edge cases (don't just special-case them away and reintroduce the bug elsewhere). Rebuild with `cmake .. && make` inside `build/`, then benchmark against a large synthetic log before trusting the result — correctness on edge cases and speed both matter.