Debug this by running the existing script first and reading the actual traceback rather than guessing at fixes. The merge will fail or silently drop everything because the three CSVs don't share a common join-column name — inspect each CSV's header with `head` and rename the mismatched time-index column (e.g. `timestamp` -> `time`) on one dataframe before merging, using inner joins so only timestamps present in all three survive. For invalid values, don't just cast to float blindly: use `pd.to_numeric(col, errors='coerce')` (or replace known sentinel strings like `'N/A'` with `NaN` first) so bad strings become `NaN`, then `dropna()` the merged frame before computing correlation — verify row count and values match what you'd expect by manually tracing which timestamps get dropped. For the heatmap, the headless-matplotlib bug is import-order/backend related: set `matplotlib.use('Agg')` before importing `pyplot`, and never call `plt.show()` in a headless script — call `plt.savefig(...)` directly (before any `show()`) so the figure buffer isn't already cleared. After fixing, actually execute the script and check `heatmap.png`'s file size and `correlation_matrix.csv`'s column order (`temp, pressure, humidity, vibration, light`) to confirm real output, not just no-crash.
