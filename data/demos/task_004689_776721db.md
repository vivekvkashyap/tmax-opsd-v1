Implement this as a streaming C++ filter with explicit state, and validate against the sample file before trusting the sort/hash step. Read stdin line by line, split each line into `TIMESTAMP`, `IP_ADDRESS`, `ERROR_CODE`, `MESSAGE` on whitespace (message may itself contain no spaces per the sample format, so a simple split is fine, but confirm this against the actual input). Validate `ERROR_CODE` is purely numeric and in [400, 599]; discard otherwise. Maintain a `set<pair<string,string>>` (or `unordered_set` of a combined key) keyed on `(ERROR_CODE, MESSAGE)` to drop duplicates, and a `map<string,int>` counting how many unique messages have been kept per `ERROR_CODE`, refusing new ones once that count reaches 2 for that code — check this cap before the dedup insert so ordering/early-stop logic matches "first 2 unique messages encountered". Compile with `g++ -O2 stratified_dedup.cpp -o stratified_dedup`, run it piping in `/home/user/input_logs.txt`, then pipe/sort the output with `sort -k3,3n -k4,4` (numeric on the error-code column, then alphabetic on message) into `/home/user/investigation_results.txt`, preserving the original line format including brackets around the timestamp. Diff your final file's content carefully against what the filtering/sampling rules imply before finishing.
