Treat this as an unpivot-filter-aggregate-sort pipeline and build it incrementally, checking intermediate output at each stage rather than writing one monolithic awk one-liner blind. Use `awk` with a field-position-to-time-bucket mapping (v0->00-04, v1->04-08, ..., v5->20-24) driven off the CSV columns v0..v5 (skip the header line), emitting one `metric,time_bucket,value` record per reading while skipping records where the value is negative. Pipe that into an aggregation step (awk associative arrays keyed by `metric,time_bucket`, accumulating sum and count) to compute averages, formatting with `printf "%.2f"` for exact 2-decimal rounding — be careful that awk's default rounding matches standard rounding, and prefer computing the average as a float division rather than integer truncation. Finally sort with `sort -t, -k1,1 -k3,3nr` to get metric ascending then average value descending (note: sort on field 3 as a number, reverse). Write the result with no header directly to `/home/user/clean_aggregated.csv`, and diff against your own hand-computed expectations for a couple of buckets to sanity check the aggregation logic before finishing.
