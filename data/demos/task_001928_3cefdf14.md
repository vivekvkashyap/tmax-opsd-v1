This task has two independent halves — fix and build the C tool first, then write the filter script, and validate each on its own before combining. For the Makefile, find the `-DMAX_LEN=10` flag in CFLAGS and change it to `-DMAX_LEN=1024`, then run `make` in `/app/fast-extractor-1.0` and confirm the `fast-extract` binary builds and runs on an input line longer than 10 characters without the "Line too long" failure. For transform.sh, build it as a proper Unix filter chain: pipe stdin through `iconv -f WINDOWS-1252 -t UTF-8` first (do the encoding conversion before any field parsing, since parsing raw Windows-1252 bytes as UTF-8 text will corrupt special characters like € or ñ), then use `awk -F':' 'BEGIN{OFS=":"} $2=="CRITICAL"{$2="RESOLVED"; print}'` to filter and rewrite the second field while preserving all other colon-separated fields and their exact count. Test with a small manual example containing 3-6 fields, a mix of CRITICAL and non-CRITICAL lines, and at least one non-ASCII Windows-1252 character, confirming the output is valid UTF-8 with only matching lines transformed. Make the script executable with chmod +x and confirm it works both when piped and when invoked directly.