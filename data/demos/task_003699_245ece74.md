Break this into the five stages in order and validate each before moving to the next, since later stages depend on earlier state. (1) For each `backup_N.tar` in `/home/user/docs_backup/`, open it with Python's `tarfile` and inspect every member's `.name` via `TarInfo` — reject (move to `/home/user/quarantine/`) the whole archive if any member name contains `../` or starts with `/`; only extract members from archives that pass, and process archives in numeric order (`backup_0`, `backup_1`, ...) into `/home/user/extracted_docs/`. (2) Parse `/home/user/migration.log` by splitting on `---` into blocks, extract the block type (`[ERROR]`/`[INFO]`) and the `File:` value with a regex per block, and delete (searching recursively) any file named in an `[ERROR]` block from `/home/user/extracted_docs/`. (3) Recursively `os.walk` and rename any file ending in `.HTM` (case-sensitive extension match) to the same name with `.html` lowercase. (4) The `/app/doc_indexer` binary is a black box — run it with no args or `--help` first to see its usage output, then invoke it correctly against `/home/user/extracted_docs/` and move/ensure its output lands at `/home/user/extracted_docs/index.dat`. (5) Write a small HTTP server (Flask or `http.server`) on `0.0.0.0:8080` serving `/api/index` as raw bytes of `index.dat` and `/api/docs?file=<name>` by walking the extracted-docs tree for the first matching filename, returning 404 if none found — start it in the background and curl-test both routes.
