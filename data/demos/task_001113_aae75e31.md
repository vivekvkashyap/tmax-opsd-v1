Start by OCR'ing `/app/archive_map.png` (e.g. `tesseract /app/archive_map.png -`) to recover the exact `CHUNK_PREFIX` and `MAGIC_SIG` strings from the image — use whatever text is actually recognized rather than assuming a value, and manually verify the OCR output looks like a sane token before hardcoding it into your scripts. Then recursively find all files under `/home/user/artifacts/` whose basename matches that prefix (`find /home/user/artifacts -type f -name "<prefix>*"`), sort the matches alphanumerically by filename, and concatenate their bytes in that order into `/home/user/reconstructed.bin` — verify the first bytes equal the magic signature you OCR'd. For the C sanitizer, read the file, skip past the magic-sig line, then repeatedly read null-terminated strings; for each path, split on `/`, walk the components maintaining a stack, and whenever you encounter a `..` component pop the last real component off the stack instead of appending it (a naive string-replace of "../" will not correctly handle multiple/nested traversals or a leading `..` with nothing to pop) — join the surviving stack back into a relative path and write it null-terminated to `/home/user/clean_paths.bin`, preserving output order to match input order. Finally use `awk`/`sed` with `-z`/RS="\0" style null-byte splitting to pair up original and cleaned paths into `/home/user/rename_map.txt` as `original -> cleaned` lines.