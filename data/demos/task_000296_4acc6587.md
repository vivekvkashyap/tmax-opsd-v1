Fix the two named bugs precisely rather than rewriting the aggregation logic. Precision loss: the accumulator map is declared/used as `float` for a value that should sum `double` amounts — change the aggregate storage/accumulation to `double` throughout so summing `10000000.0 + 0.25 + 0.25` doesn't lose the fractional part to float rounding. Corrupted-input handling: before dereferencing the amount field, check `payload_len < 16` (4 symbol + 4 seq + 8 amount bytes) and if so, skip reading `*(double*)(payload+8)` entirely and instead append the packet's sequence number (still safely readable from bytes 4-7) to `/home/user/corrupted_seq.log`, one per line, then move to the next packet — never read the amount when the payload is truncated, since that reads garbage/out-of-bounds memory. Then replace the hardcoded `AAPL`/`MSFT` printouts with logic that reads `/home/user/queries.txt` line by line and writes `SYMBOL: SUM` for each queried symbol (using the accumulated double sums, formatted to match expected output) to `/home/user/results.log`. Compile exactly with `g++ -o trade_aggregator trade_aggregator.cpp -lpcap`, run it against `/home/user/capture.pcap`, and inspect both output log files for correctness before finishing.
