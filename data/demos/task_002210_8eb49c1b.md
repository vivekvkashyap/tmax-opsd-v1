Start by trying `pip install -e .` on the vendored package to see the actual linker error — it will point at undefined symbols, telling you the `setup.py`'s `sources` list is missing a `.c` file that defines functions declared in a header but implemented elsewhere; add that file to `sources` and reinstall. Next, locate the variance computation in the C source and recognize the classic instability pattern: computing `E[X^2] - E[X]^2` from running sums of `x` and `x^2` loses precision catastrophically when values are large but variance is small (float sum-of-squares dwarfs the true variance signal). Replace it with a numerically stable approach — Welford's online algorithm (single pass, running mean and M2) or a two-pass mean-then-sum-of-squared-deviations approach — keeping it a tight C loop for speed. For the pcap, use `dpkt` (faster than `scapy`) to filter UDP dest port 5000 packets in file order, unpack each payload as little-endian doubles with `struct.unpack`, and pass the resulting list straight to the compiled extension. Time your script end-to-end to confirm it's well under the threshold before finalizing.
