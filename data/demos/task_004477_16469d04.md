Start by reverse-engineering `/app/bin/legacy_auth` rather than guessing its behavior: run `strings /app/bin/legacy_auth` to find the hardcoded secret key, and exercise it with a variety of hand-crafted JWTs (`header.payload.signature`, valid HMAC-signed, tampered signature, `alg:none`, malformed) to learn its exact stdout/exit-code format for each case before writing `secure_auth.py`. Implement JWT validation manually or with `PyJWT`, but explicitly reject `alg` of `none`/missing by checking the decoded header before any signature verification step — this is the exact bypass the legacy binary is vulnerable to, so your replacement's first check must be on the algorithm field. Match `legacy_auth`'s success/failure JSON output byte-for-byte for all other cases since the verifier fuzzes thousands of tokens for exact stdout equivalence. For the service wiring, update `nginx.conf`'s auth-forwarding location plus add a regex/map rule rejecting requests whose `Authorization` header contains base64 fragments matching `alg":"none` (check both the literal and the base64url encoding, e.g. `eyJhbGciOiJub25lIg`) with a 403. Update `start_services.sh` to drop the old binary and launch your Python service under `bwrap` with only `/home/user` and `/usr` bind-mounted read-only and networking unshared except the bound port.