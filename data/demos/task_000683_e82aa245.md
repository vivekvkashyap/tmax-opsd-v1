Build the script in clear stages and verify each before moving on. First load `/home/user/campaign_data.csv` with pandas (or csv module), group by `campaign_id`, and sum `clicks`/`conversions` per campaign — print intermediate totals to sanity-check against the raw file. Second, compute the Beta(1,1) posterior for each campaign as Beta(conversions+1, clicks-conversions+1); the posterior mean is a/(a+b) — implement this directly rather than hardcoding numbers, since the grader may swap the CSV contents. Third, for the Monte Carlo comparison, call `numpy.random.seed(42)` exactly once, then draw Alpha's 1,000,000 samples via `numpy.random.beta` BEFORE drawing Beta's samples (order matters for reproducibility with a single seed) — compute `np.mean(alpha_samples > beta_samples)`. Round every float to exactly 5 decimal places with Python's `round()` right before writing JSON (not during intermediate computation) to avoid precision drift. Finally dump the exact nested JSON structure (Alpha/Beta objects with clicks, conversions, posterior_mean, plus top-level prob_alpha_greater_beta) to `/home/user/report.json`, and actually execute the script so the file exists.
