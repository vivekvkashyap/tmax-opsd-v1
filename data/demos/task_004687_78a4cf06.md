This is a multi-stage incident-response task — fix each layer and validate before moving to the next, don't try to fix everything blind. (1) WAL repair: the file has a 16-byte header followed by fixed 32-byte records; compute `(filesize - 16) % 32` to find the trailing partial-record byte count, then `truncate -s <good_size> /app/data/engine.wal` to drop only the incomplete tail record (don't touch complete records). (2) Build fix: run `make` in `/app/compute_engine`, read the actual linker error, and check the Makefile's `LDFLAGS`/`LIBS` for a missing pthread link flag. (3) Concurrency bug: use `git log`/`git bisect` between a known-good and known-bad commit, running the load test at each step to bisect automatically; once you find the regression (likely in the ring-buffer boundary check in `queue.c`), compare the buggy condition against a standard circular-buffer "full" test (`(tail+1) % capacity == head`, not `<=`) and fix the off-by-one/wrong-operator so worker threads block/sleep correctly instead of spinning. (4) Bring up all three services via `/app/start_services.sh`, then iterate: run `python3 /app/load_test.py`, read the `RESULT_RUNTIME_SECONDS:` line, and keep fixing/restarting until it's comfortably under 3.0 seconds.
