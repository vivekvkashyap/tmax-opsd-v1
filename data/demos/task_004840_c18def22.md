Write the script step by step and capture PIDs explicitly with `$!` right after each backgrounded command (`python3 -m http.server 8443 --directory /home/user/app & PY_PID=$!` and `socat TCP-LISTEN:9443,fork,reuseaddr TCP:localhost:8443 & SOCAT_PID=$!`) so you can target them precisely later instead of relying on fragile `pgrep` guesses mid-script. After `sleep 2`, loop exactly 3 times with `sleep 1` between iterations: `curl -s http://localhost:9443/ > /dev/null`, then get RSS via `ps -o rss= -p $PY_PID` (trim whitespace), and append `$(date +%s),$PY_PID,<rss>` to the CSV. At the end, kill both `$PY_PID` and `$SOCAT_PID` (e.g. `kill` then `wait` to avoid leaving zombies), and verify afterward that no stray `python3 -m http.server 8443` or `socat...9443` processes remain — the grading explicitly checks with `pgrep -f`, so an incomplete kill (e.g. only killing one PID, or killing the wrong subprocess of socat) will fail it.
