Parse the JSONL manually with regex rather than a JSON library: for each line, use something like `std::regex(R"(\"id\":\s*(\d+).*\"text\":\s*\"(.*)\"\s*\})")` to capture the integer id and the raw text substring between the outer quotes. To strip malformed unicode escapes, apply `std::regex_replace` with a pattern matching a literal backslash, then `u`, then zero to four hex digits — something like `R"(\\u[0-9a-fA-F]{0,4})"` — and replace matches with an empty string; be careful the `{0,4}` is non-greedy-safe here since hex-digit runs are naturally bounded, and that the regex correctly handles zero hex digits (bare `\u` with nothing following, or followed by a non-hex char). Implement Levenshtein distance with the classic O(n*m) DP table (two rows suffice) comparing the cleaned text case-sensitively against the literal target string, cost 1 per insert/delete/substitute. Collect `(id, distance)` pairs, sort by id ascending, write the CSV with header. Compile with `g++ -std=c++17`, run it, and diff your output structurally against the expected format (header + one row per input id) rather than assuming correctness.
