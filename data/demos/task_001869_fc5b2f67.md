Do not extract anything to disk — open `/home/user/corrupted_backup.tar` with `tarfile.open(path, mode='r|')` (streaming mode) and use `tar.extractfile(member)` to get a file-like object for the single nested tar member, then pass that file object directly as the `fileobj=` argument to a nested `tarfile.open(fileobj=..., mode='r|')` for the next layer — recurse this way layer by layer until you find a member named `secret_artifact.bin` instead of `layer_N.tar`. Since streaming mode (`r|`) only allows sequential forward reads and you can't seek backward or list members ahead without consuming the stream, write the recursion as: iterate `tar.getmembers()` is NOT compatible with pure streaming — instead iterate `for member in tar:` and check `member.name`, descending into whichever inner tar member you find until you hit `secret_artifact.bin`. Once found, stream its bytes through `hashlib.sha256()` in chunks (don't read the whole thing into one giant string if avoidable, though at this size it's fine) and write the resulting `.hexdigest()` to `/home/user/artifact_hash.txt` with no extra whitespace/newline mismatch.