Build the pipeline as a set of small, testable stages orchestrated by the bash wrapper rather than one monolithic script: (1) normalize users.csv — lowercase email, title-case name, and parse both `MM/DD/YYYY` and `YYYY-MM-DD` date formats into `YYYY-MM-DD` (detect format per-row, e.g. by checking for `/`); (2) deduplicate by computing an MD5 hash of the normalized email per row and keeping only the first row per hash in original file order (a stable dict/ordered-set keyed by hash works well — do not use a plain unordered set that could reorder rows); (3) parse activity.jsonl, count occurrences per normalized email, and left-join those counts onto the deduplicated users (default 0 for no match), preserving the users' row order; (4) write the merged CSV with the exact required header. Ensure the output directory exists, write the CSV before appending the log line so the row count is accurate, and make the log line match the required timestamp/format exactly (e.g. via `date '+%Y-%m-%d %H:%M:%S'`). Make the script executable, run it once manually, and install the cron entry with `crontab -e`/`crontab -l | { cat; echo "30 2 * * * /home/user/run_pipeline.sh"; } | crontab -` — verify with `crontab -l` afterward.