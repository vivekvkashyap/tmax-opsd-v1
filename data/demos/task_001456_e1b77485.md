Implement this as a clear multi-pass pipeline in Go rather than trying to filter and join in one loop. First parse both CSVs with `encoding/csv` into slices of structs, tracking `initial_rows` as the raw sensor_data row count. Second, drop rows with any empty field — check each column string (including Temperature/Humidity) for `== ""` before attempting numeric conversion, since an empty numeric field must be dropped, not parsed as zero; record `rows_after_missing_drop`. Third, parse Temperature as float64 and drop rows where `temp > 50.0 || temp < -10.0` (strict inequalities, so boundary values like exactly 50.0 or -10.0 are kept); record `rows_after_outlier_drop`. Then join the surviving sensor rows to metadata by ID (build a map from sensor_metadata first for O(1) lookup) and write the joined CSV with the exact column order specified. Compute `final_mean_temperature` as the arithmetic mean of Temperature over the final rows using float64 arithmetic, and marshal all four metrics into the JSON file with `encoding/json`. Compile and run the program yourself to confirm both output files are actually produced before finishing.
