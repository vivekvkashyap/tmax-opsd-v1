Use `tesseract /app/password.png stdout` to OCR the password text, then strip trailing whitespace/newline before using it. For archiving without following the self-referential symlink loop, use `tar --no-recursion` is not enough by itself — the key is that `tar` by default does NOT dereference symlinks (it stores them as symlink entries), so avoid any `-h`/`--dereference` flag; a plain `tar czf - /home/user/docs | openssl enc -aes-256-cbc -pbkdf2 -pass pass:<password> -out /home/user/docs_backup.tar.gz.enc` (or archive then encrypt in two steps) will correctly store `shortcut` as a symlink entry instead of recursing infinitely. Verify by checking the archive size stays small (a real bloat means the loop was followed). For `/home/user/verify.sh`, accept the archive path as `$1`, decrypt with `openssl enc -d -aes-256-cbc -pbkdf2 -pass pass:<password> -in "$1"`, pipe directly into `tar tzf -` (list-only, never `-x`) so nothing touches disk, and propagate the exit code of the pipeline (watch out for pipefail — check `${PIPESTATUS[@]}` or use `set -o pipefail`) so a corrupted stream causes a non-zero exit.