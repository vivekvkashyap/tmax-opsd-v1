Transcribe `/app/voicemail.wav` first (install/use a speech-to-text tool such as `whisper` or `ffmpeg`+an ASR CLI) and listen carefully for every distinct rule stated, including exact thresholds, special-cased usernames, and any address treated as reserved/broadcast — write each rule down before coding so you don't miss one clause while focused on another. Implement `/home/user/filter.py` to read one line from stdin, split on a single space into exactly two tokens (reject/DENY if the split doesn't produce exactly two parts), then apply the transcribed rules in order: check for banned exact username matches, check minimum username length, validate the IP is well-formed IPv4 (four dot-separated octets each in 0-255, using `str.isdigit()` plus range checks rather than a lenient regex so out-of-range or malformed octets are caught), then check any special-cased trailing octet, and only print `ALLOW` if all checks pass — print exactly `ALLOW` or `DENY` with nothing else. Test against edge cases yourself: malformed input, boundary username lengths, boundary IP octets, and the special reserved address before considering it done. Separately, add a `Host local_tunnel` block to `/home/user/.ssh/config` pointing at `127.0.0.1` port `2222`.
