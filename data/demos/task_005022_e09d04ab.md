First run `tesseract /app/algorithm_spec.png stdout` (try a couple of `--psm` modes if the text looks garbled) to recover the exact numeric algorithm steps — implement precisely what the OCR reveals, don't guess at constants. For the Rust core, read `src/main.rs` carefully and find the specific ownership conflict (e.g. an immutable borrow held live across a later mutable borrow) — fix it by reordering the borrow/print so the immutable reference isn't used after the mutable borrow, rather than restructuring the whole program. Replace the dummy logic with the OCR'd algorithm, applying integer/bitwise operations exactly as described (careful with operator precedence and any modulo/XOR ordering). Build with `cargo build --release` so the binary lands at `target/release/compute_engine`, and test it against a few hand-computed values before wiring up the API. For the FastAPI wrapper, implement per-IP rate limiting with a simple in-memory sliding/fixed window counter keyed on `request.client.host`, returning HTTP 429 once the 6th request within 60s arrives; write the two-target `Makefile` last since it's just `cargo build --release` and `uvicorn`/`python` invocation wrappers.
