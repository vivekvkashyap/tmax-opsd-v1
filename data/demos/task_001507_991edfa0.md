Work in stages, verifying each before moving on. First, run `tesseract /app/model_params.png stdout` (or similar) to OCR the whiteboard image, then read the extracted text carefully to recover the mean/sigma pair for each of `alpha_signal`, `beta_flux`, `gamma_resonance` — do not guess these values. Next, write a small script (or the Rust tool itself) that joins `signals.csv`, `fluxes.csv`, `resonances.csv` from a given directory on `instance_id` to reconstruct each instance's three features. In the Rust CLI at `/home/user/sanitizer`, compute the Gaussian log-likelihood (or per-feature z-score) of each instance against the OCR'd priors, and pick a threshold on that score — since gamma_resonance is the feature most likely to differ between clean and corrupted data, make sure your interval is tight enough that legitimate in-distribution samples pass while out-of-distribution ones fail; test empirically against both corpora and adjust the cutoff rather than hardcoding one blindly. Build with `cargo build --release` so the binary lands at `target/release/sanitizer`. Write `/home/user/benchmark.sh` to time execution with `time`. Finally run the sanitizer on `/app/corpus/clean/` and `/app/corpus/evil/`, save stdout to the two results files, and confirm 100% ACCEPT/REJECT split before finishing.