Work the four phases in order, verifying each before moving on. (1) For Nginx mTLS, edit `/app/nginx/nginx.conf` to add `ssl_client_certificate /app/certs/ca.crt;` and `ssl_verify_client on;` on the SSL server block listening on 8443, proxying to `127.0.0.1:5000`; restart via `/app/start_services.sh` and confirm with `curl --cacert ... https://127.0.0.1:8443/...` (no client cert) returns a TLS/400 failure, and with `--cert/--key` succeeds. (2) For the token cipher, don't guess — disassemble the `.pyc` with Python's `dis` module (`import dis, marshal; ...` or `uncompyle6`/`decompyle3` if available) to recover the exact key string and cipher logic (expect something XOR-based followed by hex encoding); confirm your understanding by re-deriving one of the known clean-corpus tokens and checking it decodes to an `internal.corp.local` URL. (3) Write `/home/user/filter.py` to reverse the same transform, exit 0 only if decoding succeeds and the URL string starts exactly with `https://internal.corp.local/`, else exit 1 — test it directly against every file in `/app/corpus/clean/` and `/app/corpus/evil/` before integrating. (4) In Flask's `/login`, call `subprocess.run(["/home/user/filter.py", tmpfile_path])`, check the return code, and return 403 on failure instead of redirecting.