Treat this as two independent checks that must both pass, and implement each as a standalone function you can unit-test against `/home/user/samples/` before trusting it. For kinematic consistency: read the CSV's `t`, `x`, `y` columns, compute the cumulative trapezoidal integral of `y` over `t` up to each index i (sum of `(y[i-1]+y[i])/2 * (t[i]-t[i-1])`), and compare that running integral to `x[i]-x[0]`, requiring `abs(diff) < 0.05` at every step, not just the endpoint. For the conservation law: since you don't have the analytic formula for Q, shell out to `/app/q_oracle <x> <y>` for every (x,y) row (use `os/exec` in Go, capturing stdout and parsing the float), collect all Q values, and compute their sample variance, requiring `variance < 0.001`. A file is CLEAN only if both conditions hold; otherwise EVIL. Watch for two easy mistakes: forgetting the trapezoidal rule needs the actual variable time steps (not assuming uniform dt), and computing variance with the wrong denominator (population vs sample) — check both formulas against a hand-computed example. Test your compiled `/home/user/classifier` against the provided samples and reason about what would make an evil file before finalizing.
