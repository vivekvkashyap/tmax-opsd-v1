Implement Naive Bayes from scratch in Go, being precise about the tokenization rule since it drives the vocabulary size everything else depends on. For each file, lowercase the text, then replace every character that is not `[a-z0-9]` with a space (use a regexp or manual byte scan), split on whitespace, and drop empty tokens. Build per-class (spam/ham) token counts from the training files, and compute the global vocabulary as the union of unique tokens seen across both training classes — this |V| feeds Laplace smoothing. Class priors are doc-count ratios; conditional P(w|c) = (count(w,c)+1) / (total_tokens_in_c + |V|). For each test document, sum ln(prior) + Σ ln(P(w|c)) over only the tokens that exist in the training vocabulary (skip OOV tokens entirely, don't smooth them), pick the class with higher score, and break exact ties in favor of "ham" per spec. Compute accuracy as correct/total on the test set. Write `/home/user/results.json` with exactly the keys `vocab_size` (int) and `test_accuracy` (float). Compile with `go build` and run it to produce the actual output file — don't hand-compute or hardcode the JSON values.