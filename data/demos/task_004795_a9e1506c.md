Start by reverse-engineering `/app/legacy_masker` as a black box, not by disassembling it blindly: run it on several known inputs of different lengths and character sets (letters, digits, punctuation, an email, an SSN-like string) and diff the outputs against the inputs. Look for hex-encoded output; try decoding the hex back to bytes and compare byte-by-byte against the input reversed and against the input forward to spot a fixed-length transform (e.g., a per-byte XOR with some constant) combined with a possible string reversal — testing single-character and two-character inputs makes the transform trivial to isolate. Once you've derived the exact algorithm, implement it natively in your C++ service so masking is done in-process (no shelling out to the legacy binary). For the server: use raw POSIX sockets (or a small select/poll loop) to accept the `BULK_LOAD`/CSV-lines/`EOF` protocol per connection, parse each CSV row, apply your masking function to `email` and `ssn`, batch-insert into SQLite (create the `users` table with `sqlite3_exec` before listening), and respond with the exact `SUCCESS <n>\n` line. Compile with `-O3` to `/home/user/data_service` and launch it in the background before verification.