Phase 1: extract frames from `/app/ci_diagnostic.mp4` with `ffmpeg -vf fps=1 out_%03d.png` (or similar), then OCR each frame with `tesseract` (the text is clean black-and-white, so OCR should be reliable) to recover each "Node_A depends on Node_B" edge; build the dependency edge list from what you actually read off the frames rather than assuming it. Phase 2: from the recovered edges, compute a topological order (Kahn's algorithm or DFS post-order reversed) so that a node is only patched after all nodes it depends on are patched; apply each corresponding `.patch` file in `/home/user/patches/` with `patch -p1 < Node_X.patch` (or `git apply`) against `/home/user/legacy_lib/`, logging each filename to `/home/user/patch_order.txt` as you go, in order. Phase 3: after patching, build with `make`, run `make test`/`valgrind --error-exitcode=1 ./binary` to surface leaks and the buffer overflow, then fix root causes in the C++ source (add missing `free`, correct the off-by-one bound) rather than suppressing the error. Phase 4: scaffold a Rust project with `cargo new`, use a lightweight HTTP framework (or raw `std::net::TcpListener`) bound strictly to `127.0.0.1:8080`, implement `GET /graph` returning the topo-sorted patch list as JSON and `POST /verify` that checks the `Authorization: Bearer <token>` header before shelling out to run the compiled C++ binary and returning its stdout; launch it with `nohup ... &` or similar so it persists in the background.