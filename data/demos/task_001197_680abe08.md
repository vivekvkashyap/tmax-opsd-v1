Build and verify each service independently before wiring them together. Sync script: use `curl -u ftpuser:ftppass 'ftp://localhost:2121/data/' ...` or `lftp -u ftpuser,ftppass localhost -p 2121 -e "mirror /data /home/user/local_data; quit"` to pull all `.csv` files into `/home/user/local_data/`, then register it with `crontab -e` (or `crontab -l | { cat; echo "* * * * * /home/user/sync.sh"; } | crontab -`) to run every minute — confirm the crontab entry actually persists with `crontab -l`. Go service: the critical correctness point is parsing CSV with embedded newlines inside quoted fields — use Go's `encoding/csv` `Reader` (which already handles quoted multi-line fields and Unicode correctly) rather than splitting on `\n` yourself, which is exactly the legacy bug you're fixing. Aggregate integer `Amount` per `Category` in a map, then sort the resulting slice with a comparator that orders by total descending and breaks ties alphabetically by category, and serve it as JSON on `GET /metrics` at `127.0.0.1:9090`. Nginx: write `/home/user/nginx.conf` with a `server` block on port 8080 proxying `location /api/metrics { proxy_pass http://127.0.0.1:9090/metrics; }`, launch with `nginx -c /home/user/nginx.conf`, and leave both processes running.
