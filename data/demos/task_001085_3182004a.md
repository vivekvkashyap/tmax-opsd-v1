Fix the Rust project layer by layer: first get it compiling, then get the algorithm correct, then wire up the network layer, then produce the log. In `src/math.rs`, implement `solve_diophantine(r)` by iterating `x` from 0 up to `floor(sqrt(r))`, computing `y_squared = r - x*x`, checking whether `y_squared` is a perfect square (compare `y = (y_squared as f64).sqrt().round() as i32` against `y*y == y_squared`, guarding against float rounding by also checking neighbors), and pushing `(x, y)` when valid — this naturally yields pairs sorted by ascending x, then fix `format_and_sort_solutions` to sort by `(x, then y)` ascending (the given code buggily compares `a.1.cmp(&b.0)` — mixing fields — and needs an explicit `sort_by(|a,b| a.0.cmp(&b.0).then(a.1.cmp(&b.1)))`). In `src/server.rs`, uncomment/complete the WebSocket handler: parse incoming JSON as `SolveRequest`, call `solve_diophantine`, format the response, send it back over `tx`, and silently `continue` on any parse error instead of unwrapping/panicking. After `cargo build` succeeds, run the server in the background, write a small client script (Python/websockets or similar) to send `{"r": 25}`, `{"r": 65}`, `{"r": 1105}` sequentially to `ws://127.0.0.1:8080/solve` and append each raw response line to `/home/user/ws_output.log`.