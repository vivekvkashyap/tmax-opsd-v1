This is a fuzz-equivalence + integration task, so treat `/home/user/bin/manifest_checker_ref` purely as a black-box oracle you query, never disassemble/copy: write a small fuzz-driver script that generates many JSON manifests (valid ones with kind/metadata.userGroup/metadata.quotaLimit/spec, and malformed/edge-case ones — missing fields, wrong types, truncated JSON), run them through the reference binary, and diff its stdout/exit-code/output-file behavior against your own `/home/user/workspace/manifest_checker.cpp` implementation as you iterate, fixing mismatches until behavior converges (parsing rules, numeric formatting, error handling, whitespace) — don't just implement from the description alone. For the cron script, run `bash /home/user/cron/backup_job.sh` first to see it fail, then fix the concrete causes: missing `PATH` entry for your compiled binary's directory, hardcoded `/tmp/...` log path that should be `/home/user/backups/manifest.log`, and a missing `QUOTA_API_URL` env var pointing at the quota service. For the compose/services piece, check what's currently listening on 8080/8081/8082 and adjust the startup config so the mock API and quota service URLs the cron script uses actually resolve, then re-run the full cron script end-to-end and confirm the backup sink directory receives the expected payload.