Break this into DB, query, and server layers and fix each independently before wiring them together. In `/app/query.sql`, replace the accidental cross join (`FROM employees e1, employees e2`) with a proper `WITH RECURSIVE` CTE: an anchor branch selecting the requested manager with `depth = 0`, a recursive branch joining `employees e` to the CTE on `e.manager_id = cte.id` with `depth = cte.depth + 1`, parameterized by the `:v_manager_id` psql variable already used. In `/app/optimize.sql`, add `CREATE INDEX idx_manager_id ON employees(manager_id);` and make sure `/app/start.sh` runs it against the `company` DB after Postgres is confirmed ready (e.g. poll with `pg_isready` or a `psql` no-op loop) but before serving traffic. Fix `/app/handle_req.sh` to emit real HTTP: read the request line, extract `manager_id`, then explicitly print `HTTP/1.1 200 OK\r\nContent-Type: application/json\r\n\r\n` followed by the jq-built JSON array — the broken version has no status line/headers at all, which is why responses are invalid. Fix `/app/server.sh` so `socat` correctly forks per-connection (e.g. `TCP-LISTEN:8080,fork,reuseaddr`) rather than serving once/hanging, then background it from `start.sh`.