Run OCR on the sticky-note image first (`tesseract /app/legacy_creds.png stdout`), inspecting the raw text carefully to separate the password from the base port number — OCR can misread characters, so sanity-check the extracted values look plausible before using them. For port discovery, write a quick concurrent scanner (e.g. Python with threads or asyncio) that GETs `/health` with header `X-Legacy-Ping: ping` across the base-port..base+1000 range and stops at the first 200 response, rather than scanning serially which would be slow. Use the discovered port and OCR'd password to POST `/rotate` with the required JSON body, and write the returned token byte-for-byte (no reformatting) to `/home/user/new_token.txt`. Edit `/app/service_config.json` by loading it as JSON, adding the `csp` key, and re-serializing (don't hand-edit text, to avoid breaking existing JSON structure). For the audit script, the 2-second budget for 5000 requests against a local server means you must use `asyncio` + `aiohttp` with a single shared `ClientSession` and a bounded number of concurrent tasks (e.g. `asyncio.gather` with a semaphore or connection-limited connector) rather than sequential `requests` calls, which would be far too slow; print the count of HTTP-200 responses at the end.
