The core bug fix is an ordering change: move the split before the statistics computation, not just relabel variables. Read all 100 rows first, then take rows[0:80] as train and rows[80:100] as test as separate containers; compute per-column mean and population std-dev (divide by N, i.e. the C++ pipeline's own `sqrt(sum/n)` form) using only the train container; then apply `(x - train_mean) / train_std` to both train and test rows using those same train-derived stats — the test set's normalized values will not center on 0, which is expected and is what step 3 measures. Write both CSVs with `setprecision(6)` and `fixed`, matching the original comma-formatting logic exactly. For the confidence interval, compute it either in a follow-up C++ program or Python script that reads back `test_norm.csv` column 0: sample mean, *sample* std-dev (N-1 denominator, not population), t-critical ≈2.093 for df=19, margin = t*s/sqrt(20), then write `[lower, upper]` to `/home/user/ci_output.txt` rounded to 4 decimals with that exact bracket formatting. Compile with `g++ -std=c++17 pipeline.cpp -o pipeline` and actually run it before considering the task done — verify the CSVs look sane (test set not centered at 0) as a sanity check that the leak was actually fixed.
