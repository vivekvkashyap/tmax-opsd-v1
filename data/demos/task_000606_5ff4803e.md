For each `node_XX.log` file, this is a sequential-pair comparison problem: for every consecutive line pair (row i, row i+1), check `if EnergyError[i] > 0.005 and StepSize[i+1] > 0.5 * StepSize[i]` and count it as a violation. Implement this with `awk`, tracking the previous row's `EnergyError` and `StepSize` as you iterate line by line (compare current row's StepSize against 0.5 * previous row's StepSize, gated on previous row's EnergyError) rather than trying to do it with paste/join tricks. Use a small epsilon tolerance when comparing floats with `bc`/awk to avoid false positives from floating-point rounding right at the 0.5x boundary. Accumulate a per-file violation count, then build the final dict with only nodes whose count is >= 1, and pipe through `jq` (e.g. constructing key/value pairs and reducing with `add`, or using `jq -n --argjson`) to pretty-print valid JSON to `/home/user/report.json`. Make the script executable and run it end-to-end against the real `/home/user/sim_logs/` files, then inspect the output JSON to sanity-check the counts look plausible before concluding.