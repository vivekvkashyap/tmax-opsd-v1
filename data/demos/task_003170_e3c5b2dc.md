First extract the pipeline rules from the image at `/app/pipeline.png` — open/OCR it (or view it) to read off the exact filter condition, sort key(s) and direction, and pagination behavior; do not assume generic rules, read what the image actually specifies. Then implement `/home/user/pipeline_exec.c` as a strict stdin/stdout processor: read the first line as integer N (the limit), then parse each subsequent CSV line `id,status,age,score` (use a bounded sscanf/fgets loop, capping status length to avoid buffer overflow, and handle up to 50,000 rows with a dynamically or statically sized array). Apply the filter condition from the image, sort the remaining records using qsort with a comparator matching the image's sort key and tie-breaker exactly, then truncate to the first N and print each as `{"id": <id>, "score": <score>}\n` with exactly one space after the colon and no trailing formatting differences. Since this will be fuzz-tested for bit-exact equivalence against a hidden reference, pay special attention to comparator tie-breaking rules and integer overflow/negative handling in qsort return values, and test manually with edge cases (N larger than result count, empty input, all-filtered-out input) before considering it done. Compile with gcc.