This is a classic `/proc`-style join across three flat files. Parse `net_tcp` by splitting on whitespace and skipping the header line — remember the UID column is index 7 (0-indexed) in the standard `/proc/net/tcp` layout, not the first or last field, so count fields carefully rather than guessing an offset. Build a UID->username->primary-GID map from `passwd` (colon-delimited, GID is field 4, index 3), then a GID->group-name map from `group` (colon-delimited, GID is field 3, index 2). For every connection row, resolve UID -> primary GID -> group name, and tally a counter per group name (skip/ignore rows whose UID doesn't resolve rather than crashing). After tallying, set `status` to `"CRITICAL"` if any single group's count is strictly greater than 10, else `"OK"`, and only include groups with at least one connection in the output. Write the script to `/home/user/net_capacity.py`, make it robust to variable whitespace, then actually execute it so `/home/user/capacity_report.json` is produced with this exact structure.