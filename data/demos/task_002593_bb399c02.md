Grep the Makefile for the variable used to build the install path and compare it against how it's defined — a single dropped letter in the variable reference means it expands empty or wrong, so fix the misspelling and build with `make PREFIX=/home/user/local install`. Initialize the deploy repo with `git init --bare /home/user/deploy.git`; accepting pushes to main on a fresh bare repo generally works out of the box once its default branch is set to main (via `git symbolic-ref HEAD refs/heads/main` or init.defaultBranch config). For validate_manifest.sh, since it will be fuzz-tested against thousands of adversarial CSV-like lines, implement each field's rule as a precise regex/pattern match (`[[ $field =~ ^svc-[a-z0-9]{1,10}$ ]]`, digit-range checks for port and replicas, etc.) and explicitly verify the line splits into exactly 4 comma-separated fields (e.g. with `IFS=',' read -ra`) before validating individual fields — sloppy splitting or overly permissive regexes are the usual source of divergence from a strict oracle. Print the exact `[VALID]`/`[INVALID]` formats specified, then symlink the script as the pre-receive hook.
