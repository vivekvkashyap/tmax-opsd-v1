The core pitfall here is parsing CSV with naive line-splitting (`split('\n')` or `readlines()`), which breaks on quoted embedded newlines inside `source_text`/`target_text` — instead use a proper CSV parser (Python's `csv` module, opened in text mode, iterating rows via `csv.reader`/`DictReader`) which correctly treats a quoted field spanning multiple physical lines as one logical row. Process each file this way, computing `len(source_text)` and `len(target_text)` in characters (post-parsing, so embedded newlines count normally as characters). Apply the invalidity checks in this order: empty source, empty target, then ratio `len(target)/len(source)` outside `[0.2, 5.0]` inclusive — use `<=`/`>=` boundaries correctly since the range is inclusive. For valid rows compute `cost = len(target_text) * rate_per_char` and accumulate per-locale sums in a dict; round each locale's total to exactly 2 decimals only at the very end (not per-row) to avoid compounding rounding error, then `json.dump` to `/home/user/locale_costs.json`. Collect invalid row IDs into a list, sort numerically (`sorted(ids, key=int)` since IDs may be read as strings), and write one per line to `/home/user/dropped_ids.log`. Process every file found in `/home/user/inputs/` (glob `*.csv`), not just one.
