Build and test each stage of the pipeline independently before chaining them. In `process.py`, parse each line by splitting on `|` with `maxsplit=2` (message itself may contain `|`), extract the message, tokenize on whitespace, and clean each token by stripping non-alphanumeric characters only from the two ends (e.g. `word.strip(string.punctuation)` or a regex like `re.sub(r'^\W+|\W+$', '', w)`) rather than removing all punctuation everywhere in the word — then lowercase and skip empties. Aggregate frequencies per `user_id` with a `Counter`, then emit CSV rows sorted with a compound key: `sorted(rows, key=lambda r: (r.user_id, -r.frequency, r.word))`. Write the CSV with Python's `csv` module and no header. For `etl.sh`, make it executable (`chmod +x`), have it invoke `python3 process.py`, drop and recreate `stats.db` with `sqlite3 stats.db "DROP TABLE IF EXISTS word_stats; CREATE TABLE word_stats(...)"`, then bulk-load via `sqlite3 stats.db -cmd ".mode csv" ".import /home/user/processed.csv word_stats"`. Finally add the cron line with `(crontab -l 2>/dev/null; echo "15 3 * * 1 /home/user/etl.sh") | crontab -` — verify with `crontab -l` afterward that the exact schedule and path match what the verifier expects.