First establish a correctness baseline: compile and run the original `/home/user/app/mesh_align.c` as-is and save its `top_nodes.txt` output (or at least the score values) so you have a ground truth to diff your optimized version against — never change scoring semantics, only performance. Then profile or just read the code to spot the two classic bottlenecks: `strlen(target)`/`strlen(query)` being recomputed on every iteration of nested loops inside `align_score` (hoist them into local variables computed once before the loops), and the O(N^2) bubble sort at the end (replace with `qsort` using a comparator that reproduces the same descending-score ordering, including consistent tie-breaking so ties don't silently reorder in a way that breaks equivalence). Also reconsider whether the nested `i==j` character-comparison loop can be turned into a single linear scan instead of two nested loops, since it only ever compares same-index characters. After rewriting, recompile to `/home/user/app/mesh_align_opt`, run it, confirm it finishes in well under a second, and diff its `top_nodes.txt` against your saved baseline to prove mathematical equivalence before finishing.