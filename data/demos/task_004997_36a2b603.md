Do each step and check the intermediate artifact before moving to the next. First read `versions.expr`, extract the MAJOR and MINOR expressions as text, and evaluate them arithmetically (shell `$(( ))` handles `+`,`/`,`*`,`-` but not `**`; for exponentiation use `python3 -c` or `bc` with `^`, being careful about integer vs float division semantics matching what the file's expressions imply) — don't hardcode or guess the numbers, compute them from the file's actual expressions. Next apply the patch with `patch -p0 < memory_leak_fix.patch` (or `-p1` depending on the path prefix in the diff headers — inspect the patch header first to pick the right strip level) against `/home/user/src/app.c`, then confirm the `free(arr);` line landed correctly. Write `ci_vars.env` with the exact `VERSION="MAJOR.MINOR"` format using your computed integers. Finally compile with `gcc -DVERSION='"MAJOR.MINOR"' /home/user/src/app.c -o /home/user/bin/app` — remember the macro needs to expand to a quoted C string literal, so the shell-level quoting must produce literal double quotes in the preprocessor definition. Run the binary afterward and confirm its printed version matches the ci_vars.env value.
