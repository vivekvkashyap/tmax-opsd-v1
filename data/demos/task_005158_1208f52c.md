Build the pipeline as a real Jupyter notebook (use `nbformat` or `jupyter nbconvert` on a `.py:percent` script converted to `.ipynb`, or write cells via `nbformat.v4`) rather than a plain script, since the task requires headless execution via `nbconvert --execute --inplace`. Steps: load the CSV, filter `sensor_id == "A1"`, sort by `wavelength`. Apply `scipy.signal.savgol_filter(intensity, window_length=5, polyorder=2)` with default mode. Build the baseline mask from wavelengths outside [4002, 4008], but center the x-values by subtracting 4005.0 before forming the design matrix — this normalization is essential or `X^T X` will be near-singular. Construct `X` with columns `[1, x_norm, x_norm**2]`, solve `beta = np.linalg.inv(X.T @ X) @ X.T @ y` on the masked smoothed data. Then evaluate the baseline over ALL (not just masked) wavelengths using the same centering, subtract from the smoothed intensity, and integrate the difference over the full wavelength range with `np.trapz(y_sub, wavelength)`. Write `c0, c1, c2, peak_area` as floats to the JSON path specified, then actually run `jupyter nbconvert --execute --to notebook --inplace` and confirm the JSON file was produced with no exceptions.