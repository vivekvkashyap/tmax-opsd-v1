Treat `/app/legacy_filter` as a black box: script a loop that feeds it many crafted single-argument strings and records exit codes, rather than guessing. Cover three hypothesis classes separately: (1) PII-like numeric sequences (e.g. card-number shaped digit groups with/without spaces or dashes, varying lengths) to find a regex boundary; (2) common SQL-injection keywords and clauses in different cases/positions; (3) Unicode confusables — inject look-alike characters (Cyrillic, Greek) next to ASCII letters at different positions to find which codepoint and adjacency pattern trips it. Binary-search each dimension (length, delimiter, case, position) to pin exact boundaries, using the sample logs in `/home/user/train/` as a starting corpus, then expand your own. Only once you've derived exact rules from observed exit codes should you encode them in C. For gap-filling, process rows per `user_id` in timestamp order, and for each consecutive pair with delta > 1, emit `N-1` `[GAP]` rows for the missing integer seconds before writing the next real row. Validate your compiled `/home/user/pipeline` output against the legacy binary on your own generated corpus before considering it done.
