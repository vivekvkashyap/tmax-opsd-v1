Treat this as a two-bug build-and-patch job, not a from-scratch parser. First, try `pip install -e /app/libwalparse` and read the actual linker error — it will fail resolving a zlib symbol used in the C source (something like `gzopen`); fix it by adding the missing library to the `libraries=[...]` list in `setup.py` (link against `z`), then reinstall. Second, once it builds, running the parser on `/app/suspicious.wal` will crash or hang on some records — inspect the C code's record-length field: it's likely declared as a signed integer type read from untrusted bytes, so a maliciously large value wraps negative and a bounds/size check that assumes non-negative length gets bypassed, causing OOB access or an infinite loop advancing the wrong direction. Fix by changing the length field to an unsigned type (or add an explicit `if (rec_len < 0) skip/continue`) so corrupted records are detected and safely skipped rather than crashing the parser. After the extension rebuilds cleanly and survives corrupted headers, write `/home/user/recover.py` to call the fixed `walparse` module, iterate parsed records, and write each valid string payload to `/home/user/extracted.log` one per line — check the resulting recall against the corpus size to confirm most/all valid records were recovered despite the corrupted ones interspersed.