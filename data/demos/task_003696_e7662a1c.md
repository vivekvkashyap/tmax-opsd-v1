Write a Python script that walks `/home/user/legacy_project` with `os.walk`, collecting absolute paths. For archive integrity, don't just check the extension exists — actually attempt to open each file: for `.zip` use `zipfile.is_zipfile()` plus `zipfile.ZipFile(path).testzip()` inside a try/except, and for `.tar.gz` use `tarfile.open(path, 'r:gz')` wrapped in try/except (catching `tarfile.ReadError`/`gzip` errors), treating any exception as corrupted — note a `.tar.gz` that isn't valid gzip will raise on open, so catch broadly. For staleness, use `os.path.getmtime(path)` compared against the UTC epoch timestamp for `2023-01-01T00:00:00Z` (build it with `datetime(2023,1,1,tzinfo=timezone.utc).timestamp()` to avoid local-timezone bugs), and only match files ending in `.log`, strictly before that cutoff. Collect two lists, `sorted()` each alphabetically (plain string sort on the absolute paths), and dump to `/home/user/audit_report.json` with `json.dump` using exactly the two keys shown in the spec — verify the output by re-reading and diffing against your own manual trace of the directory contents.
