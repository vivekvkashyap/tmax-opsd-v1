Automate the bisect rather than testing commits by hand across ~200 commits. Write a small test script (e.g., `bisect_test.sh`) that: builds via `make`, runs `timeout 2 ./tlv_decoder /home/user/payload.bin`, and exits nonzero if the timeout fires (hang = bad) or zero if it completes (good). Then run `git bisect start`, `git bisect bad main`, `git bisect good v1.0`, and `git bisect run ./bisect_test.sh` to let git binary-search automatically to the offending commit; record the hash it identifies. Read the diff at that commit (`git show <hash>`) to find the actual logic bug — look specifically at how the loop index/cursor is advanced for each TLV entry, especially for entries with a zero-length value, since that's the classic edge case that causes an index to never advance and the loop to spin forever. After confirming the root cause, apply a fix on `main` (not by reverting, but by correcting the increment logic so it always advances past type+length+value), rebuild, rerun against the payload, capture stdout to the output file, and compute its byte length yourself for the JSON report rather than guessing.
