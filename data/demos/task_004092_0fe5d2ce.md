Start by OCR'ing /app/specs.png (e.g. `tesseract /app/specs.png stdout`) to recover the exact target metric string the filter must enforce — don't hardcode a guessed metric name, extract it from the image and use that value in your script. Then build filter_artifacts.sh as a strict allowlist validator, not a blocklist patched reactively: first confirm the input is valid JSON (`jq empty file` or check `jq` exit code), then check the `metric` field with `jq -r .metric` exactly equals the OCR'd target string, and validate `model_path` against a strict allowed-pattern (e.g. `[[ "$model_path" =~ ^[A-Za-z0-9_./-]+$ ]]`) combined with an explicit check that it does NOT contain the substring `..` — checking for a small blocklist of characters individually is fragile and easy to bypass, so prefer whitelisting the allowed character set plus the specific `..` traversal check called out in the spec. Exit 1 on any failure (malformed JSON, missing fields, wrong metric, or unsafe path), exit 0 only when everything passes. Before finishing, hand-test your script against a few clean and a few adversarial JSON files you construct yourself (including edge cases like backticks, `$()`, and `../../`) to confirm the exit codes match expectations.