Build the pipeline stage by stage and verify each output before moving to the API. For extraction, use `ffmpeg -i input.mp4 -vf fps=1 -q:v 2 /home/user/frames/frame_%03d.jpg` but note ffmpeg's `%03d` numbering starts at 1 by default — adjust with `-start_number 0` (or rename afterward) so files land as `frame_000.jpg`, `frame_001.jpg`, ... matching the required convention exactly, since the verifier's frame-1-second check depends on correct indexing. For embeddings, open each frame with PIL, `.convert('L')`, get the flat pixel list (`img.getdata()`), bucket each 0-255 value into one of 8 bins of width 32 (`value // 32`, clamping 255 into bin 7), normalize by total pixel count so the 8 values sum to 1.0, and round each to 4 decimals — do this arithmetic yourself rather than relying on a histogram library's default binning, since off-by-one bin edges will fail the verifier's mathematically-defined comparison. Store results keyed by integer frame id in `embeddings.json`. For the API, use Flask/FastAPI bound exactly to `127.0.0.1:8888`, run it as a background/daemon process (e.g. `nohup ... &` or `flask run &`) so it stays up after your script exits, and implement `/api/tokenize` by lowercasing, stripping all characters except alphanumerics and spaces via regex, then splitting on whitespace.
