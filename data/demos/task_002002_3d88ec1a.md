Structure this as: one SQL query to pick the latest backup per node, then a separate in-memory shortest-path computation in C++ — don't try to force Dijkstra into SQL. Use a window-function query against `backups` like `SELECT id, node_id, size_bytes FROM (SELECT id, node_id, size_bytes, ROW_NUMBER() OVER (PARTITION BY node_id ORDER BY timestamp DESC) rn FROM backups WHERE node_id != 0) WHERE rn = 1` to get each node's latest backup in one pass via `sqlite3_exec`/prepared statements. Separately load all rows from `network` into an adjacency list (`source_node -> [(dest_node, transfer_cost)]`), then run Dijkstra from every non-zero node to node 0 — the network here is directed, so respect edge direction exactly as stored rather than treating it as undirected; nodes with no path to 0 must be dropped from output entirely, not given an infinite/placeholder cost. Join the shortest path costs back to each node's latest-backup row, sort ascending by `node_id`, and write the CSV with the exact header and column order given. Compile with the exact `g++` command specified and run the binary to actually produce the file before considering the task done.
