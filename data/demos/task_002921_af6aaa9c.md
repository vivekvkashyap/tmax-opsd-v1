Break this into three independently checkable stages. First, parse the FASTA file correctly: read line by line, treat each `>`-prefixed line as starting a brand-new independent sequence (do not concatenate across headers), concatenate the following sequence lines for that record into one string, then slide a window of 2 across each record's string separately to count dinucleotide occurrences into a 4x4 count matrix ordered [A,C,G,T] for both rows and columns — a common bug is accidentally bridging the last char of one record into the first char of the next. Second, normalize each row of the count matrix by its row sum to get the stochastic matrix P, then iterate `pi = pi.dot(P)` starting from `[0.25]*4`, using the L2 norm of the difference between successive vectors with a strict `< 1e-7` stopping threshold, counting iterations precisely (increment once per multiplication, and be careful whether you count the iteration that satisfies the threshold). Third, run a chi-square goodness-of-fit test (e.g. `scipy.stats.chisquare`) on the raw counts of row A against a uniform expected distribution (total_A_count/4 for each of the four expected cells), not on probabilities. Write the three results into the required JSON keys with correct types (list of 4 floats, int, float).
