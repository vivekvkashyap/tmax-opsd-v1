First fix the vendored package before writing any filtering logic: inspect `/app/unicodenorm-0.2.1/setup.py` for an impossible/broken pinned dependency version in `install_requires` (a classic vendoring perturbation), relax or remove the offending version pin, then `pip install .` from that directory and confirm `import unicodenorm` succeeds and its malicious-detection function actually flags a known bad string (e.g. one containing a bidirectional override character like U+202E) before trusting it inside your cleaner. For `cleaner.py`, process stdin line-by-line (don't `.read()` the whole stream into memory) preserving each line's original newline exactly as read. Drop a line if `unicodenorm.is_malicious(line)` is True, OR if a sliding 20-character window has more than 10 punctuation/symbol characters — check Unicode category via `unicodedata.category(ch)[0] in ('P','S')` for every character in every 20-char window (a naive whole-line percentage check will miss localized punctuation bursts); for lines under 20 chars, instead compute punctuation-ratio over the whole line and drop if it exceeds 50%. Test against small hand-built clean/malicious/heavy-punctuation samples to confirm both rules trigger correctly before trusting it on the full corpora.