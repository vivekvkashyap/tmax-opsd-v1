Write a single Python script that: (1) loads `ground_truth.csv` once and sets `id` as the join key; (2) for every CSV in `predictions/`, merges on `id` (use pandas `merge` with `on='id'`, not positional concatenation, since row order across files is not guaranteed to match) and computes MSE as `mean((pred - true)**2)`; (3) picks the model with minimum MSE as best; (4) builds each model's prediction vector strictly ordered by ascending `id` (sort explicitly before vectorizing — do not rely on file read order), then computes cosine similarity between the best model's vector and every other model's vector using `dot(a,b)/(norm(a)*norm(b))`, excluding the best model itself; (5) picks the model with the highest cosine similarity as most-similar — note this can differ sharply from the model with second-best MSE, since cosine similarity is invariant to positive scalar scaling of a vector, so a badly-biased-but-proportional model can still be "most similar" by this metric. Round both float outputs to exactly 4 decimal places with `round(x, 4)` before writing the JSON with the exact required keys to `/home/user/report.json`.