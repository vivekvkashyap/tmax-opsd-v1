Two independent subproblems here — solve them separately before wiring together. (1) OCR: run `tesseract /app/project_spec.png stdout` (try `--psm 6` if default segmentation garbles it), locate the line containing "PREFIX: " and take the token that follows as the prefix string; don't hardcode any prefix. (2) Rust parser: write a `.pza` reader using `byteorder` or manual `u32::from_le_bytes`/`u16::from_le_bytes` on a `Vec<u8>` read via `std::fs::read`, validating the 4-byte magic first. Critically, do path validation in a *dedicated pre-pass over every entry* before any file is written: reject if the path is absolute (`Path::new(p).is_absolute()`), reject if any component is `..` or `.` via `Path::components()` (don't just string-match "../" — also catch things like "..\\" and mixed separators), and independently verify the fully joined output path still starts with the canonicalized output directory. If validation of *any* entry fails, print to stderr and `std::process::exit(1)` without touching disk at all — a partial-write-then-fail approach will not satisfy "no files written on rejection". Only after full validation passes should you create subdirs and write prefixed files.