Open the log with `open(path, encoding="utf-16le")` in Python (or `iconv -f UTF-16LE -t UTF-8` first) — reading it as plain UTF-8/bytes will produce garbled or mojibake text and break your parsing. Split the decoded text into records by the `=== RECORD ===` / `=== END ===` delimiters (e.g. regex split, or iterate line by line tracking state), then extract the `Path:`, `Type:`, `Action:` fields per record with simple string splitting or regex groups — don't assume fixed line positions since blank lines or ordering could vary. Filter to records where `Action` is exactly `BackedUp`. For each surviving path, verify the file exists on disk and compute its SHA-256 with `hashlib.sha256(open(p,'rb').read()).hexdigest()` (stream in chunks for large files). Sort the resulting (path) list alphabetically before writing, and write the manifest explicitly as UTF-8 with the exact format `<hex> *<path>` per line (note the asterisk-no-space-before-path convention from `sha256sum`). Double check you exclude symlinks/directories and any non-`BackedUp` actions like `InfiniteLoopAborted` or `Skipped`.
