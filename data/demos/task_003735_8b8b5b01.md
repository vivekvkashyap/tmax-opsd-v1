Build bottom-up and test each layer before composing the pipeline. First write `/home/user/anomaly.c` with `double compute_anomaly_score(double* values, int length)` implementing RMS (`sqrt(sum(v_i^2)/n)`), and compile it as a shared object with `gcc -shared -fPIC -o /home/user/libanomaly.so anomaly.c -lm`. Verify the symbol loads correctly with `ctypes.CDLL` and set `.argtypes`/`.restype` explicitly (a `POINTER(c_double)` array and `c_int`, returning `c_double`) — this is the most common ctypes FFI mistake and silently gives garbage without it. For `process_logs.py`, open both input files with `encoding='utf-16'` explicitly (do not rely on default encoding, since Python's default is not UTF-16 and will mis-decode or error), parse each `SensorID,val1,val2,...` line, merge sensors from both files, call into the shared library per sensor, then sort by score descending with a secondary ascending alphabetical tiebreak on SensorID (use a sort key like `(-score, sensor_id)`). Write output lines as `SensorID: <score>` with the score formatted to exactly 4 decimal places, opening the output file with `encoding='utf-8'`. For `test_process.py`, load `libanomaly.so` via `ctypes` directly in the test (not by importing your pipeline script) and assert the RMS of `[3.0, 4.0]` is close to the expected value with `assertAlmostEqual`. Run both scripts, redirecting the test run's stdout+stderr to `/home/user/test_results.log`.
