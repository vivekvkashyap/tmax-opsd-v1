Break this into the three DB operations in strict order, verifying each with direct SQL queries before moving to the next. First, deduplicate `edges`: for each `(source, target)` pair keep only the row with `MAX(updated_at)` — e.g. `DELETE FROM edges WHERE rowid NOT IN (SELECT rowid FROM edges GROUP BY source, target HAVING updated_at = MAX(updated_at))`, then `CREATE UNIQUE INDEX idx_unique_edges ON edges(source, target)`. Second, read the CSV row by row and use a parameterized upsert (`INSERT ... ON CONFLICT(source, target) DO UPDATE SET weight=?, updated_at=? WHERE excluded.updated_at > edges.updated_at`, or an explicit SELECT-then-conditional-UPDATE/INSERT) so stale CSV rows are correctly rejected. Verify row counts and specific weight values with manual SELECTs afterward. Third, write a recursive CTE (`WITH RECURSIVE reach(node, hops) AS (SELECT target, 1 FROM edges WHERE source='ROOT_42' UNION ALL SELECT e.target, r.hops+1 FROM edges e JOIN reach r ON e.source=r.node) SELECT node, MIN(hops) FROM reach GROUP BY node`) to compute minimum hop counts, excluding ROOT_42 unless a cycle brings it back. Dump the result as `{node_id: hop_count}` JSON to `/home/user/graph_data/downstream_nodes.json`, sanity-checking a couple of hop counts by hand against the graph structure.