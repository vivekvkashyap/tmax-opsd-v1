Handle each step in order, verifying by inspection rather than trusting filenames. (1) For PNG detection, don't trust extensions — check the actual magic number of every file in project_dump (e.g. `file <name>` or check first bytes via `xxd -l8`/`head -c8` for the PNG signature `89 50 4E 47 0D 0A 1A 0A`); move only files whose bytes truly match into assets/images/, appending `.png` to the existing filename, and explicitly leave non-PNG files like a fake image.png untouched. (2) For logs, filter by size first (`find ... -name '*.log' -size +100c`), grep only the exact uppercase string "CRITICAL", and redact IPv4s with a sed/awk regex substitution (e.g. `sed -E 's/[0-9]{1,3}(\.[0-9]{1,3}){3}/[REDACTED]/g'`) applied to matched lines only, writing all matches to consolidated_critical.txt. (3) Write split_logs.go to read that file and chunk it into 15-line files under assets/logs/ named critical_chunk_N.txt, then `go run` it (or build and execute) — make sure both destination directories exist (`mkdir -p`) before any writes.