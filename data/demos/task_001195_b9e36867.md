Build this as a straight-line ETL + stats pipeline, checking each stage's output before moving to the next. Step 1: read `/home/user/raw_calibration.csv`, keep only rows where `x >= 0`, `status == "valid"` exactly, and `y` parses as a finite number (reject empty, `NaN`, `null`, and non-numeric strings — a naive `float(y)` check misses that `float('nan')` succeeds but should be excluded, so explicitly test `math.isnan()`); write the survivors (with header) to `/home/user/clean_calibration.csv`. Step 2: compute OLS by hand with the standard closed-form sums — `m = (N*Σxy - Σx*Σy) / (N*Σx² - (Σx)²)` and `b = (Σy - m*Σx) / N` — rather than pulling in a heavy library, since N is small. Step 3: compute MAE as the mean of `|y_actual - (m*x + b)|` over the same cleaned rows. Step 4: write exactly three lines to `/home/user/report.txt` — slope, intercept, MAE — each rounded to 4 decimal places with consistent rounding (Python's `round()` or `"%.4f"`), one value per line, no extra labels.
