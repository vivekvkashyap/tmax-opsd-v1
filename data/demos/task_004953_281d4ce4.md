First locate the deliberate build breakage in the vendored `/app/tablib-0.14.0` package — inspect `setup.py` for something that unconditionally forces a failure during install (e.g. an env var flag or a broken C-extension build step) and remove/bypass just that faulty piece so `pip install -e /app/tablib-0.14.0` (or equivalent) completes and pure-Python `tablib` import works; don't rewrite the whole package, just excise the injected fault. Then implement `/home/user/etl_pipeline.py` to load both CSV and JSON inputs via `tablib.Dataset().load(...)`, perform an inner join on `product_id` (build a dict/lookup from one dataset keyed by product_id, then iterate the other, keeping only matching keys), fill missing `quantity` values with integer `0` (be careful to cast/parse as `int`, not let it become `float`/NaN through any pandas-free path), compute `total_value = quantity * price`, then use `numpy.cov` with `ddof=1` over the three numeric columns stacked as rows/variables (check numpy's expected input orientation — rows-as-variables vs columns-as-variables — since `np.cov` defaults to treating each row as a variable). Flatten the resulting 3x3 matrix in row-major order and print all 9 values comma-separated with exactly 4 decimal places to stdout, matching the hidden oracle's exact formatting since this is fuzz-tested bit-for-bit.
