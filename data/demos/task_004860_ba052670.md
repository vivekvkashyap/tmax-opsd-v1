Treat this as two independent deliverables. For Stage 1, extract frames from `/app/reaction_video.mp4` with `ffmpeg` (e.g. `-vf select` or dump all frames as PNGs), sample the red-channel intensity at the center of the left and right squares per frame to build time series A(t), B(t), then estimate finite-difference derivatives dA/dt, dB/dt between consecutive frames and fit the linear system `dA/dt = -k*A + c*B`, `dB/dt = k*A - c*B` via least squares (e.g. stack both equations and use `numpy.linalg.lstsq`, or fit k and c jointly from both rows since they share parameters). For confidence intervals, bootstrap by resampling the (dA/dt, dB/dt, A, B) time-difference pairs with replacement 1000 times, refitting k and c each time, then take the 2.5/97.5 percentiles. For Stage 2, write `stability_filter.c` that reads the N×N matrix, and for efficiency avoid literally simulating 1000 Euler steps with full matrix multiplies if N is large — but simulating directly (X_{n+1} = X_n + 0.1*M*X_n, checking |X_i|>1e6, up to 1000 iterations) is the straightforward, correct approach; make sure to break early and exit 1 the moment any component blows up, and exit 0 only after surviving all 1000 steps. Test your filter against a few hand-built stable and unstable matrices before trusting it on the corpora.