Work top-down: first get file selection right, then binary parsing, then the security check, then wire it together with shell. Use `find /home/user/artifacts -maxdepth 1 -name '*.pkg' -size -100k` (or check with `stat`/`du` for byte-precision, since `find -size` rounds to blocks — verify the boundary case explicitly against the 100KB fixture) to select only candidate packages. In curate.c, read the fixed-format record: 4 magic bytes, then a 1-byte length prefix N, then read exactly N bytes for the path — check that substring for `../` and reject immediately without touching the payload if found, since the traversal check must happen before any decompression. For safe paths, read the 4-byte little-endian GZ size, read that many bytes, and use zlib's `inflate` (with `windowBits` set for gzip, e.g. 16+MAX_WBITS) to decompress just enough of the stream to get the first bytes of the ELF payload — you don't need the whole payload, only enough to reach offset 0x12 for the 2-byte little-endian e_machine field. Format output exactly as specified (lowercase hex, no leading zeros beyond what's needed). Finally, drive the whole thing from a shell one-liner or small script that finds matching files, invokes your compiled binary on each, and pipes the combined output through `sort` into curation_report.txt — test against each planted fixture individually to confirm ACCEPT/REJECT and hex values before trusting the aggregate.