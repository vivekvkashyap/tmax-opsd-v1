Work phase by phase, testing each before moving on. Set up the venv (`python3 -m venv /home/user/sim_env && source /home/user/sim_env/bin/activate && pip install numpy scipy h5py biopython`) and always activate it before running any script. For the ODE phase, define the coupled derivative function `f(t, y)` returning `[dT/dt, dC/dt]` per the given equations, call `scipy.integrate.solve_ivp` with `t_span=(0,50)`, `y0=[37.0, 0.0]`, and `t_eval=np.linspace(0, 50, 500)` so you get exactly 500 evenly-spaced points; then integrate the resulting `C` array over `time` with `scipy.integrate.simpson(C, x=time)`. For HDF5 output, use `h5py.File(...,'w')` with `create_dataset` for each array and set the integral via `f.attrs['total_yield'] = value`. For primer design, read the reference sequence from the given HDF5 file, slice `seq[300:500]` (Python slicing already gives you the correct 200bp region — don't add/subtract an extra index), take `target[:20]` as forward, and compute the reverse complement of `target[-20:]` for reverse (write a simple complement-map + reversed loop rather than trusting external libraries' primer defaults). Compute GC% as count(G or C)/length*100, round per spec, and assemble the exact JSON keys requested.