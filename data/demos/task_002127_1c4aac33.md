Work top-down: first flesh out `/home/user/monitor_src/monitor.cpp` to take `argv[1]` as the output path, read `/etc/fstab` line by line, count lines that are non-empty after trimming whitespace and don't start with `#`, get the current time and format it as UTC (e.g. `gmtime`/`std::gmtime`, not `localtime`, so the hook's TZ setting actually matters), and append one line in the exact `[YYYY-MM-DD HH:MM:SS UTC] FSTAB_ENTRIES: <count>` format using zero-padded fields — build and test it standalone first (`g++ monitor.cpp -o monitor_run && TZ=UTC ./monitor_run /tmp/test.log`) before touching the hook. Then edit `/home/user/monitor_src/.git/hooks/post-commit` (bash, must stay executable) to `cd` into the repo path explicitly (don't rely on relative paths), compile `monitor.cpp` into `monitor_run`, ensure `/home/user/monitoring_logs/` exists (`mkdir -p`), and invoke `monitor_run` with `TZ=UTC` prefixed specifically on that command (not globally exported) so the log timestamp is UTC regardless of the invoking shell's timezone, passing `/home/user/monitoring_logs/uptime.log` as the argument. Finally make a commit to `monitor.cpp` in that repo to actually trigger the hook, then inspect the log file to confirm the format and count are correct.
