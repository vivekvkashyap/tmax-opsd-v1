Start by extracting the secret: run `tesseract /app/target_info.png stdout` (or similar), then trim trailing/leading whitespace from the result and store it in a shell variable so you can reuse it while testing — never hardcode a guessed token. Build the Rust server as a `std::net` project with two threads (or async tasks), one `TcpListener` bound to 127.0.0.1:8080 parsing raw HTTP/1.1 requests manually (read until `\r\n\r\n` for headers, then read Content-Length bytes for the body), and one bound to 127.0.0.1:8081 doing simple line-based TCP. For HTTP: always attach the CSP header, check for POST /audit with a case-correct `Authorization: Bearer <token>` header match, compute the response's SHA256 with a crate like `sha2` (lowercase hex via `format!("{:x}", hash)`), and route everything else to 404 while still including the CSP header. For TCP: read until `\n`, strip it, compare the full `SCAN <token>` string exactly. Test locally with `curl` and `nc` using your extracted token before backgrounding the process (e.g. `cargo run &` or nohup) and confirming both ports still respond after that.