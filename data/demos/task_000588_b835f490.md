Work through the requirements in dependency order since later steps reference earlier artifacts. First generate the TLS material with `openssl req -x509 -newkey rsa:2048 -nodes -keyout key.pem -out cert.pem -days 365` into `/home/user/microservices/certs`, confirming both files exist and the key has no passphrase. Write `nginx.conf` as a full non-root-safe config: set `pid`, `error_log`, `access_log` to paths under `/home/user/microservices`, use a `user` directive appropriate for non-root or omit it, and inside an `http`/`server` block listen on `8443 ssl` referencing the cert/key paths, with `root` pointing at the `www` directory. Validate the config syntax with `nginx -t -c <path>` before proceeding. Write `supervisord.conf` with `[supervisord]` logfile/pidfile pointing into the microservices dir, plus `[program:webapp]` invoking the nginx binary with `-c <config> -g "daemon off;"` and `[program:mailer]` invoking `bash mailer.sh`, both with `autorestart=true`. Also include a `[unix_http_server]`/`[supervisorctl]` rpc section if `supervisorctl` needs to connect over a socket, then start `supervisord -c ...` in the background, wait/poll until both programs report RUNNING, and finally capture `supervisorctl -c ... status` output into `/home/user/status.log`.
