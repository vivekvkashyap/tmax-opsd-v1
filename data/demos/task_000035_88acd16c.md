Diagnose the root cause first: the existing `main.rs` uses `BufRead::lines()` and naive `split(',')`, which breaks whenever a quoted CSV field (`location_notes`) contains an embedded newline, because that splits the record across two "lines". Fix this by replacing the manual line/comma parsing with the already-available `csv` crate's `Reader`, which correctly handles RFC 4180 quoting and embedded newlines within quoted fields regardless of how many raw text lines a single record spans. Use serde or manual field indexing to pull `timestamp`, `x`, and `y` from each parsed record (ignore `location_notes` for output). Compute Euclidean distance as `(x*x + y*y).sqrt()`, and apply the quality-gate filter to drop rows where distance is strictly greater than 50.0 — note this means distance exactly 50.0 is kept, only `> 50.0` is dropped. Write the surviving rows to `cleaned.csv` with a `timestamp,distance` header, formatting distance with `{:.2}` for two-decimal rounding. After editing, run `cargo build` and `cargo run` from `/home/user/pipeline`, then inspect `cleaned.csv` to confirm rows with embedded-newline notes are no longer silently dropped and the row order/count matches the input.