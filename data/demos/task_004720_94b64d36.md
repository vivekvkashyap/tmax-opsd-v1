Work this as a three-stage pipeline debug. First, extract payloads with tshark: filter on UDP port 8080 and pull only the application-layer bytes, e.g. `tshark -r traffic.pcap -Y "udp.port==8080" -T fields -e data.text` (or decode the hex `data` field yourself if `data.text` isn't populated) — one payload per line, discarding any lines that aren't valid `SENSOR_ID,VALUE` pairs like malformed packets. Second, read `container.log` to see the crash: the bash script does `int_val=${value%.*}` and arithmetic like `sum[$id]=$(( ... + int_val ))`, so (a) a malformed line with no comma/value causes an empty operand and blows up the `((...))` expression — guard with input validation (skip lines that don't match the `ID,NUMBER` pattern) — and (b) using bash integer arithmetic truncates decimals, which is the precision-loss bug. Fix precision by switching the sum/average computation to `awk` or `bc` (floating point), keeping bash as the driver. Third, ensure any temp/state files used for tracking sums are truncated or recreated at the start of each run so reruns don't accumulate stale state. Finally regenerate final_averages.txt sorted alphabetically by sensor ID with values to exactly 2 decimals.
