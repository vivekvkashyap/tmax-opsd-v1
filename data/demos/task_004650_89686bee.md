Do not assume the manifest lines are all relevant or already in the right shape — parse `/home/user/staging/manifest.txt` in your C program by reading it line by line and only acting on lines that start with the exact literal prefix `CHUNK: ` (skip comments, blank lines, and any other prefixed line like a decoy `IGNORE_THIS:` entry), extracting the filename that follows. Preserve the order these `CHUNK:` lines appear in the manifest, since that determines assembly order — don't sort filenames alphabetically or by any other heuristic. For each chunk filename in order, open it (path-joined with `/home/user/staging/`) in binary mode, read it fully, and append its raw bytes to the open output file `/home/user/staging/assembled.tar.gz` (open the output once in write-binary before the loop, then just keep appending via `fwrite`). After compiling and running the merger, sanity-check the result is valid gzip before extracting: `gzip -t assembled.tar.gz` or `file assembled.tar.gz`, then `mkdir -p /home/user/release && tar -xzf assembled.tar.gz -C /home/user/release`. If integrity check fails, first suspect a wrong manifest line being included or chunks concatenated out of order rather than a compression issue.
