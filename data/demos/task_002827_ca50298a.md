Work this as a real forensics chain, in order. First, recover the secret: `cd /app/vendored/legacy-engine && git log --all -p` to find the commit that added then deleted a secret config file, then use `strace -f ./bin/engine <any-file> 2>&1 | grep -i open` (or `strings`/`ltrace`) to see exactly which path the binary `open()`s for the secret (it may differ from the file's old repo path), and write the recovered value there. Second, once the binary runs cleanly on a benign sample, `strace` it against each file in `/app/samples/` and diff syscalls between files that succeed vs. ones triggering unexpected `execve`/`system` calls — that difference marks the trigger condition. Also try byte-level analysis (hexdump, or brute-force single-byte XOR against an expected plaintext prefix) on the payload bytes to find their encoding and the marker separating malicious from benign. Third, write `/home/user/filter.py` to detect that marker directly from file bytes (no need to invoke the binary), printing exactly `CLEAN`/exit 0 or `EVIL`/exit 1, and validate 100% accuracy against `/app/corpora/clean` and `/app/corpora/evil` before finishing.