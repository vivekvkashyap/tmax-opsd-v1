Read `/home/user/etl.c` looking for the type used to hold the timestamp field — `atoi` returns `int` and `%d` formats it, both of which silently wrap/truncate for values beyond ~2.1 billion, which is exactly why timestamps near or above 10^12 come out negative or garbled. Fix by switching the parsing to `strtoll` (or `atoll`) returning a `long long`, changing the variable's declared type to `long long`, and updating the `fprintf` format specifier to `%lld` to match. Recompile with a standard `gcc -o /home/user/etl_fixed etl.c` (or your build flags of choice), then run the resulting binary against `/home/user/data.csv` to regenerate `/home/user/data_clean.csv`. Confirm no other narrow-int handling in the file (e.g. an `int id` field) hides a similar overflow if the id column ever grows, and diff the output CSV against the input to confirm every row round-trips without sign flips or truncation before considering it done.
