Build this as two clean layers with a well-defined interface between them, not one tangled script. For `shortest_path.c`: parse `edges.csv` by skipping the header line, then build an adjacency list — since max node ID ≤ 1000, use fixed-size arrays indexed by node ID (e.g. arrays of int lists per node, sized 1001) rather than a dynamic graph library. Implement classic BFS from the start node: a visited array, a parent/predecessor array, and a FIFO queue; when you pop the end node, stop and reconstruct the path by walking parents backward from end to start, then reverse it before printing space-separated IDs. If BFS finishes without reaching the end node, print `NO_PATH` and exit cleanly (pipeline.sh must handle this case rather than crash). For `pipeline.sh`: use `gcc`/`cc` to compile the C program first (check the compiler's exit status), then capture the BFS program's stdout into a variable/array. Parse `nodes.csv` (skipping its header) into an id→name lookup, e.g. with awk or a bash associative array, to translate each path ID to a name. Build the JSON by hand or with a tool like `jq -n --argjson ...` to guarantee valid, well-formed output matching the exact schema (array of `{id,name}` objects plus integer `length` = edges = node_count-1), and write it to `/home/user/path_output.json`. Finally, invoke `pipeline.sh 1 7` as instructed and confirm the JSON file was created correctly.
