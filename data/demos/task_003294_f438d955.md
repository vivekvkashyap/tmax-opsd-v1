Build and test each stage script independently before wiring up the orchestrator — run `extract.py` alone and inspect `features.jsonl` by hand, then `aggregate.py` alone, then `sort.py` alone, so a bug in one stage doesn't get masked by the next. Read/write all text as UTF-8 explicitly (`encoding="utf-8"`) since reviews contain non-Latin scripts and emoji, and use the `emoji` package's counting function (e.g. `emoji.emoji_count(text)`) rather than a hand-rolled regex, which easily misses multi-codepoint emoji. For `has_latin`, use a regex like `re.search(r'[a-zA-Z]', text)` rather than checking `str.isalpha()` on the whole string. In `aggregate.py`, group with pandas `groupby('product_id').agg(...)`, rounding `avg_char_count` with `round(x, 2)`. In `sort.py`, use `sort_values(by=[...], ascending=[False, False, True])` for the three-key tie-break. In `pipeline.py`, call each stage with `subprocess.run([...], check=False)`, inspect `.returncode`, and `sys.exit(result.returncode)` immediately on nonzero rather than continuing to the next stage.
