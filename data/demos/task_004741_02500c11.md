Start by recovering the master key: run `tesseract /app/rotation_memo.png stdout` to OCR the memo text and pull out the printed hex key — use that recovered value in your script rather than assuming a fixed constant. Then build `/home/user/payload_filter.sh` in two independent checks that must BOTH pass for exit 0: (1) extract the `user` field with `jq -r .user` and test it against a character class of the forbidden metacharacters (`;`, `|`, `&`, `$`, `>`, `<`, backtick) using something like `[[ "$user" =~ [\;\|\&\$\>\<\`] ]]` — reject on match; (2) recompute the expected HMAC-SHA256 over the literal string `"$user:$nonce"` using the OCR'd key via `openssl dgst -sha256 -hmac "$key"` and compare it (case-insensitively, trimming whitespace) to the `hmac` field from the JSON — reject on mismatch or missing fields (check with `jq` that user/nonce/hmac are all present first). Iterate the script against every file in `/app/corpus/clean/` (must all exit 0) and `/app/corpus/evil/` (must all exit 1) until 100% pass, since the evil corpus includes both forged-HMAC and injection-payload cases that must each be caught by the right check.
