Start by interactively running `/app/lb_admin` by hand (or piping input) to observe its exact prompt strings and required sequence (username prompt, password prompt, command prompt, then how it responds to a reload command and to quit) — treat it as a black box you reverse-engineer through observation, not by disassembling it. Write `/home/user/reload.exp` using Tcl `expect`/`send` pairs that match those exact observed prompts, log in with the given admin/secret123 credentials, issue the reload command, then cleanly quit so the process exits (don't leave it hanging, which would block the git hook). For the git side, `git init --bare /home/user/lb_sync.git`, then add a `hooks/post-receive` script that does a `git --work-tree=/home/user/lb_config --git-dir=/home/user/lb_sync.git checkout -f` (reading refs from stdin) to materialize pushed files, then invokes `expect /home/user/reload.exp`. For the Rust proxy, use `std::net::TcpListener` with a thread (or async runtime) per connection, read the first line up to `\n` for the `ROUTE <target>` command, look up the target in a cached parse of `routes.json`, and re-check/reload that cache only when `reload_flag`'s mtime changes (stat it once per connection, not the whole file) rather than re-parsing JSON every request. Use `io::copy` in both directions (two threads or `try_join`) for bidirectional forwarding until either side closes.