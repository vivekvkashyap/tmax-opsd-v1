Discover the vulnerability trigger empirically before writing the filter, since the task explicitly says you must deduce it. Fuzz `/app/legacy_parser` with small crafted multi-line configs (feed via stdin or as a file argument, whichever it accepts) varying directive order and values — try isolated `SetPrivilege: true`, isolated large `Timeout` values, and the two adjacent in different orders/spacings — and watch for exit code 139 (segfault) or 42 (backdoor string) versus normal 0/1, to confirm the exact adjacency condition (e.g. must `Timeout` immediately follow `SetPrivilege: true` with no line between, and must its value exceed 9999) rather than assuming the description's example is the complete rule. Once confirmed by direct experiment, write `/home/user/config_filter.py` to take `<input_dir> <output_dir>`, locate all `.log` files recursively (e.g. `os.walk` or `pathlib.rglob`, matching "metadata-based search" across nested subdirs), and for each file `mmap` it read-only and split into records on `---END RECORD---`, scanning each record's lines for the dangerous adjacent-directive pattern with a regex or simple line-scan (not loading the whole gigabyte file into memory as one string). Write only benign records to the mirrored path under `<output_dir>`, preserving exact original text and filenames, and test your script against both `/app/corpora/clean/` and `/app/corpora/evil/` yourself to confirm 100% correct classification before finishing.
