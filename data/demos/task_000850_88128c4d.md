Work the four phases in order since each feeds the next. Phase 1: read /home/user/app/server.py and classify each endpoint's flaw by its actual pattern — string-formatted SQL passed straight into a query execution call is CWE-89 (SQL Injection), and unescaped user input interpolated directly into HTML output is CWE-79 (XSS); don't guess CWE numbers from memory, verify the mechanism in the code matches the CWE definition. Phase 2: locate the hardcoded Fernet key constant in server.py, then write a small Python snippet using `cryptography.fernet.Fernet(key).decrypt(...)` on the bytes read from /home/user/evidence/staging_logs.enc to recover the plaintext log, and extract the SECRET_FLAG value from that decrypted text (do not fabricate it). Phase 3: write a Python script that attempts a TCP connect (socket.connect_ex) to localhost on each port 13000-13050, recording which one succeeds — that's the backdoor port, discovered not assumed. Phase 4: assemble the JSON report with CWE ids sorted alphabetically as strings, matching the exact key names and types (backdoor_port as an integer) specified in the task.
