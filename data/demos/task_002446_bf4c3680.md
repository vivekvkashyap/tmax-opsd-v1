In `/home/user/process_data.py`, load `/home/user/datasets/topics.ttl` with `rdflib.Graph().parse(...)` and find all subtopics of `<http://example.org/topics/ArtificialIntelligence>` transitively via `rdfs:subClassOf` — a single SPARQL query with a property path like `?topic rdfs:subClassOf* ex:ArtificialIntelligence` captures the target itself plus every direct and indirect subtopic in one shot (the `*` is what makes it recursive; don't just do one level of `?topic rdfs:subClassOf ex:ArtificialIntelligence` and miss deeper descendants). Collect those topic URIs into a Python set, then connect to `/home/user/datasets/authors.db` with sqlite3 and join `papers` to `authors` on `author_id`, filtering `papers.topic_uri` against that URI set (e.g. build a parameterized `IN (...)` query). Group results by author name, sort each author's paper titles alphabetically, drop authors with zero matching papers, and dump the dict to `/home/user/ai_authors.json` with `json.dump(..., indent=2)`. Run the script and spot-check the output structure against the required schema.