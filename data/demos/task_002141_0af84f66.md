Build this in three layers and test each in isolation before wiring them together. (1) Write `validator.c`, compile it as a shared library with `gcc -shared -fPIC -o libvalidator.so validator.c`. Implement `validate_patch` by reading the file line by line and checking each line's *first* character: only lines starting with a single `+` or `-` count as added/removed lines, but you must explicitly exclude lines starting with the literal prefixes `+++` or `---` (diff file headers) before checking for the `[RESTRICTED]`/`[METADATA]` substrings — a naive substring-only check without excluding headers will misclassify patches whose header lines happen to be safe but whose reasoning is wrong, and won't handle `+++`/`---` correctly. (2) Write `main.go` using `cgo` (`import "C"`, `#cgo LDFLAGS: -L. -lvalidator`) to call into the .so; use goroutines with a WaitGroup/channel to process discovered `.patch` files concurrently, not in a loop. Set rpath (`-Wl,-rpath,$ORIGIN`) or export `LD_LIBRARY_PATH` at build/run time so the binary finds `libvalidator.so` at runtime. (3) For `thumb`, shell out to `ffmpeg -ss 00:00:05 -i <video> -vframes 1 thumb.jpg`. Test against `/app/corpus/clean/` and `/app/corpus/evil/` before finishing.