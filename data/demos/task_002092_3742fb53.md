Write a Python script to parse each line of ops.txt into (version, op, operand). Compare semantic versions correctly by splitting each on '.' and comparing the resulting integer tuples component-wise (e.g. `tuple(map(int, v.split('.')))`) rather than comparing version strings lexically — this is the trap, since naive string comparison would misorder something like "2.10.0" vs "2.1.0" (string compare treats "2.10.0" < "2.1.0" incorrectly). Keep only lines whose version tuple is >= (2,1,0), then apply operations in file order to a register that you mask to 32 bits (`& 0xFFFFFFFF`) after every operation, including subtraction (which should wrap on underflow) and multiplication/shift (which should also wrap). Write the final value into magic.rs as exactly `pub const MAGIC_NUMBER: u32 = <value>;`, compile with `rustc main.rs` from inside the project directory without touching main.rs, then run `./main > output.txt`. Double check your filtering and wraparound logic against the ops.txt content by hand before trusting the compiled output.