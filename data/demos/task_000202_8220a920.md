Part 1 is a build-fix, not a rewrite: inspect `/app/graph-backup-extractor-v2.1.0/Makefile` for the recipe line under the `build` target — Make requires recipe lines to be indented with a literal tab character, so if it looks visually indented but uses spaces, `make` will error; fix that indentation. Separately, open `config.go` and look for the `NEO4J_URI` environment-variable wiring — if the line reading it via `os.Getenv("NEO4J_URI")` is commented out or missing, uncomment/restore it so the URI actually gets populated. Confirm `make build` succeeds and produces `bin/extractor`.

Part 2 is a classifier: write `/app/sanitizer.go` that walks a given directory's `.json` files and flags each as rejected if it contains known malicious NoSQL patterns (`$where` with embedded JS, nested `$ne` operators used for brute-forcing) or an unencrypted SSN matching the `XXX-XX-XXXX` digit pattern (use a regex), printing `ACCEPTED: <filename>` or `REJECTED: <filename>` per file. Compile to `/app/sanitizer`, run it against both `/app/corpora/clean/` and `/app/corpora/evil/`, and save the combined output to `/app/sanitizer_results.log`.
