Work through this in strict order since each step depends on the last. First, extract the hardcoded salt from `/home/user/legacy_auth.bin` using `strings` (a printable 16-char constant embedded in the binary should stand out) rather than trying to disassemble the logic — confirm it's exactly 16 characters. Second, write `/home/user/cracker.cpp`: for each candidate word in `/home/user/wordlist.txt`, concatenate `word + salt`, compute SHA-256 via OpenSSL's EVP or legacy SHA256 API, hex-encode it, and compare against the hash stored for `compliance_admin` in `/home/user/db.txt` (parse the `username:hash` format and find that specific line) — compile with `-lssl -lcrypto` and stop at the first match. Third, once you have the recovered plaintext password, POST JSON `{"username": "compliance_admin", "password": "<recovered>"}` to `http://127.0.0.1:9090/login` with `curl` or similar, capturing both the HTTP status code and the exact raw response body text (don't reformat/reparse it — the verifier wants the literal bytes). Finally write `/home/user/audit_trail.json` with exactly the four specified keys, ensuring the JSON string values (like the response body) are properly escaped.
