Implement this as a straightforward two-pass C program, matching the spec's exact arithmetic order. Pass 1: read all `x,y` pairs from `/home/user/points.csv` into arrays (or a dynamically-sized buffer, since you don't know the count in advance — don't hardcode 1000), and compute the centroid as the mean of x and mean of y. Pass 2: compute each point's Euclidean distance to that centroid with `sqrt`/`hypot`, then compute the mean and *population* standard deviation of those distances (divide by N, not N-1 — this is the easy place to get subtly wrong). Use `mu_d + 2.0*sigma_d` as the cutoff and mark `d_i > threshold` (strictly greater, not `>=`) as an outlier. Write kept points in original file order with `printf("%.6f,%.6f\n", x, y)` to `/home/user/cleaned_points.csv`, and write the outlier count as a plain integer to `/home/user/outlier_count.txt`. Compile with `-lm` for `sqrt`. Before finishing, spot-check by reimplementing the same logic quickly in Python/awk over the same CSV and diffing the outlier count against your C program's output — this catches sign/threshold/division mistakes cheaply.
