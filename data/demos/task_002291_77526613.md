This is a curve-fitting-then-optimization pipeline; use Python with `scipy` for both stages rather than hand-rolling gradient descent. First filter the CSV to `Status == 'Success'` only — the 'Timeout' rows are corrupted/inflated observations that must not influence the fit. Second, fit `T(N,C) = a*(N/C) + b*C^c` with `scipy.optimize.curve_fit` (or `least_squares`), passing `p0=[1.0,1.0,1.0]` and `bounds=([0.1,0.1,0.1],[5.0,5.0,5.0])` so the search stays within the required box constraints; define the model function to take `(N,C)` as a tuple of arrays and fit against the actual `Time` column, since curve_fit minimizes squared residuals which is equivalent to minimizing MSE. Third, once you have `a,b,c`, find the concurrency `C` minimizing `T(50000, C)` subject to `C >= 1` — use `scipy.optimize.minimize_scalar` with `bounds=(1, some_large_value)` and `method='bounded'`, or solve analytically by setting the derivative w.r.t. C to zero and solving for the positive root, then confirm it's a minimum. Round all four values to exactly 4 decimal places and write them to `/home/user/model_results.json` with the exact key names `a`, `b`, `c`, `optimal_C`.
