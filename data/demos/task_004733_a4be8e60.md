Work strictly in order since each step's output feeds the next. First, recover the embedded secrets from the binary with `strings /home/user/auth_binary | grep -E 'HASH:|SALT:'` (or grep the raw bytes if strings misses it) to get the hash and salt values — don't hardcode guesses. Second, brute-force the password: for each candidate word in `/home/user/wordlist.txt`, compute `echo -n "<word><salt>"` (concatenated with no separator/newline — use `-n` and be careful `echo` vs `printf` don't add trailing newlines) piped into `sha256sum` or `openssl dgst -sha256`, and compare against the extracted hash until you find a match; script this as a loop rather than checking by hand. Third, once you have the cracked password, generate the token with `echo -n "admin_access" | openssl dgst -sha256 -hmac "<cracked_password>" -hex`, extracting just the hex digest (strip any `(stdin)=` prefix) and writing it lowercase with no trailing whitespace/newline issues to `/home/user/admin_token.txt`. Finally, edit `/home/user/policy.conf` so only the first line changes from `DENY ALL` to `ALLOW ADMIN <token>` (e.g. via `sed -i '1s/.*/.../'`), leaving all other lines untouched — verify with `head -n1` and `diff` against the original for the rest.