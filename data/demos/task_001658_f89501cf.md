Implement the port check as an actual TCP connection attempt (e.g. `bash`'s `/dev/tcp/127.0.0.1/5902`, `nc -z`, or a Python socket connect with a short timeout) rather than checking for a process by name — the spec is about port 5902 listening. Branch on success/failure: on failure, launch `/home/user/qemu_mock.py -vnc :2` in the background (so the script doesn't block) and append the exact DOWN status line; on success, append the exact UP status line — match the literal strings precisely since they're checked verbatim in rotated logs. Make the script executable. For `rotate.conf`, write a logrotate stanza targeting the log's absolute path with `size 10` (bytes), `rotate 2`, `compress`, and `missingok` as separate directives inside the block. Run the supervision script once (port down → starts VM, logs DOWN), sleep ~2s for the mock server to bind, run it again (port up → logs UP), then trigger rotation with `logrotate -s /home/user/lr.state /home/user/rotate.conf` — verify afterward with `zcat` that the rotated file contains both log lines in order.
