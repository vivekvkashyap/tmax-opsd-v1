Work the pipeline in strict stages and verify each before moving on. First, parse the FASTA file by skipping any line starting with `>` and stripping whitespace/newlines from the rest, then count remaining characters to get `L` — don't assume a fixed line width, concatenate all sequence lines first. Second, implement Kahan summation manually (running `sum`, `c` compensation term, doing `y = x - c; t = sum + y; c = (t - sum) - y; sum = t` per line) rather than calling `math.fsum` or a naive accumulator loop — the task specifically wants compensated summation because naive float accumulation across millions of small values plus one large value loses precision. Third, implement Newton-Raphson from scratch for `f(x)=x^3+L*x-S`, `f'(x)=3x^2+L`, iterating `x_new = x - f(x)/f'(x)` from `x0=1.0`, logging `abs(x_new - x_old)` to convergence.txt each step, stopping once that diff drops below `1e-7`. Finally write the JSON with `L` as int, `kahan_sum` rounded to 4 decimals, `root` rounded to 6 decimals. Sanity-check by manually recomputing f(root) is near zero.