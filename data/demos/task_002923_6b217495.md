Work through this as a strict, order-sensitive pipeline in Python with numpy/h5py. Load the `spectra` dataset with `h5py.File(...)['spectra'][:]` and inspect its shape first. For "dead channel" removal, compute `np.std(col)` per column and drop columns where it is exactly 0 (use exact equality since the fixture generates true zero columns, not near-zero noise). For duplicate removal, compare columns for exact elementwise equality (e.g. `np.array_equal`) against columns you've already kept, iterating left-to-right by original column index and keeping only the first occurrence of each distinct column — do this after removing dead columns, or make sure zero-columns don't interfere with duplicate detection depending on your order of operations. Run SVD with `np.linalg.svd(cleaned_matrix, full_matrices=False)` and take the singular values array's first three entries (already returned sorted descending by numpy). Round each to exactly 4 decimals with Python's `round()` or `f"{x:.4f}"` formatting, join with commas, and write as a single line (no trailing newline requirements need double-checking) to `/home/user/top_singular_values.csv`. Test your column-cleaning logic against a small toy matrix with known duplicates/zeros before running it on the real file.
