Set the thread-limiting environment variables (`OMP_NUM_THREADS=1`, `OPENBLAS_NUM_THREADS=1`) at the very top of your script, before importing numpy/sklearn — setting them after import or only in the shell for unrelated commands won't affect BLAS's internal thread pool since it's initialized at import/first-use time. Load `/home/user/raw_embeddings.npy`, fit `PCA(n_components=10, random_state=42)` and transform, then fit `BayesianGaussianMixture(n_components=5, random_state=42, max_iter=500)` on the 10-D output and call `.score_samples()` to get one log-likelihood per sample. To find the bottom 5%, use `np.argsort(scores)[:50]` to get the 50 lowest-likelihood original row indices, then sort those indices ascending for the output text file — don't confuse "sorted by score" with "sorted by index," you need the final `outliers.txt` sorted by index value, not by likelihood. Build `clean_embeddings.npy` by deleting those same 50 rows from the 10-D PCA output (`np.delete(X_pca, idx, axis=0)`), verify the result has shape `(950, 10)`, and save both output files to the exact given paths. Keep every random_state and hyperparameter exactly as specified since determinism is what's being checked.
