This is a small constraint-satisfaction / knapsack-with-dependencies problem — with only a handful of packages, brute-force over all subsets is simplest and safest. Initialize the module (`go mod init mathpack` in `/home/user/workspace`), then in `solver.go` load `packages.json` into a map of structs with `Size int`, `DependsOn []string`, `ConflictsWith []string`. Enumerate all subsets of the package universe (bitmask over N packages, N is small), and for each subset check: contains "core-app"; every package's dependencies are also in the subset (closure check — don't just check direct deps if any package could have transitive ones, though here it's shallow); no conflicting pair coexists; total size <= 120. Among valid subsets, keep the one maximizing package count, breaking ties by maximizing total size — track the current-best candidate as you iterate rather than sorting all subsets. Write the winning subset's names as a sorted JSON array to `install_plan.json` (use `sort.Strings` then `encoding/json`). Write `solver_test.go` with at least one test asserting the solver returns a specific expected set on a small mock input, confirming your constraint logic (dependency closure, conflict exclusion, size cap, tie-break) behaves as intended. Run `go test` then run the binary to produce the output file.
