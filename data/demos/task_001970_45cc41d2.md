Run OCR on the manifest image first (`tesseract /app/manifest.png stdout`, preprocessing/upscaling the image if needed for accuracy) and read off the labeled decryption key integer from the extracted text — use that recovered value, don't assume a fixed key. Write the Go program to read all of stdin as bytes, XOR every byte with the key to undo the encryption, then decode the RLE: since RLE here is `[count_byte, char_byte]` pairs, iterate two bytes at a time and repeat `char_byte` `count_byte` times, appending to an output buffer, then print that buffer — verify by manually tracing a couple of pairs from the fixture before trusting the full decode. Pipe it together as `cat /app/repository/blob.bin | your_decoder > decoded.csv` and sanity-check the header line reads `id,name,size`. Parse the CSV, keep only rows where `size < 1000` (numeric comparison, not string), and build the JSON structure `{"artifacts": [...]}` preserving row order. For the server, use `net/http` with a single handler on `/artifacts` that sets `Content-Type: application/json` and writes the marshaled struct, listening on exactly `127.0.0.1:8080`; start it with `go run` or a compiled binary backgrounded (`&`) so it stays up for verification, and hit it once with `curl` yourself to confirm the response shape matches before finishing.