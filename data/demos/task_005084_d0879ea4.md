Fix the build chain before touching security logic. In `/app/libmsgparse-1.2.0/Makefile`, check the compile and link rules for a shared library: the `.c.o` rule needs `-fPIC` and the final link needs `-shared -o libmsgparse.so` (a corrupted Makefile producing a static `.a` or missing `-fPIC` is the classic failure mode here) — run `make` and confirm `libmsgparse.so` actually appears. Then fix `/home/user/msg_gateway/build.rs` so it emits the correct `cargo:rustc-link-search` and `cargo:rustc-link-lib=dylib=msgparse` pointing at that build directory, and remember you'll need `LD_LIBRARY_PATH` set to find the `.so` at runtime too. The real work is in `src/main.rs`: before calling into the C FFI at all, read the input file's bytes, check length >= 3, read the big-endian 16-bit payload length from bytes 1-2 (`((b[1] as usize) << 8) | b[2] as usize`), and require `file.len() == 3 + payload_len` exactly — on any mismatch, print an error and `std::process::exit(1)` without ever calling the unsafe C function, since the whole point is that the C library segfaults on untrusted lengths and Rust must be the gatekeeper. Only call `parse_msg` via FFI once validation passes, and test manually against a few hand-crafted valid/invalid byte files before running against the full corpora.
