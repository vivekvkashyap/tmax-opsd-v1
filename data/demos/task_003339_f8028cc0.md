Build the DAG logic first and test it standalone before wiring up the bash trigger. In `build_manager.py`: load the manifest, filter each artifact by whether the target `--platform` is in its `platforms` list, then build an adjacency structure only from the surviving artifacts (drop edges to dependencies that aren't valid for this platform — don't just filter the artifact list and keep stale deps). Do a topological sort (e.g. Kahn's algorithm or DFS post-order) using in-degree counts you compute yourself, not any library. To generate content, recurse bottom-up: a leaf's content is just its name; a node's content is `name(dep1_content,dep2_content,...)` where deps are sorted alphabetically by dependency name and any dependency invalid for this platform is simply omitted from the list (not replaced by anything). Write each `.out` file to `/home/user/artifacts/<platform>/<name>.out`, creating directories with `os.makedirs(..., exist_ok=True)`. Test this script directly with `--platform linux` and inspect the `.out` file contents before writing `run_ci.sh`. In the bash script, extract all `[ci:<platform>]` tags with a simple grep/regex loop (there can be multiple) and invoke the Python script once per tag.
