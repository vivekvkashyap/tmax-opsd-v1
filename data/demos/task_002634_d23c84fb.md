Write a Python script that parses the FASTA file manually (track header lines starting with `>`, concatenate subsequent sequence lines until the next header), building a list of (header, sequence) pairs in file order. For each sequence, build a numeric signal by mapping each base to its EIIP value via a small dict, then compute `np.fft.fft(signal)`, take `np.abs(fft)**2` as the power spectrum, and normalize by dividing by its sum so it becomes a probability mass function. Keep the first sequence's normalized PMF as the fixed reference, then for every sequence (including the first) call `scipy.stats.wasserstein_distance(reference_pmf, this_pmf)` — note this treats the PMF values as sample weights over index positions 0..N-1, so make sure both arrays are the same length (all sequences here are equal length) since `wasserstein_distance` needs paired support/weight semantics. Write results with Python's `csv` module, formatting the distance with an f-string like `f"{dist:.6f}"`, using the exact FASTA header text (without `>`) as `Sequence_ID`. Create `/home/user/output/` with `os.makedirs(..., exist_ok=True)` before writing.
