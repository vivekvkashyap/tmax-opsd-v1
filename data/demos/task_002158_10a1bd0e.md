Treat `/app/legacy_signer` purely as a black-box oracle: run it on a range of crafted inputs (empty string, single char, known ASCII sequences, strings with digits/punctuation) and diff the outputs to form hypotheses — check for string reversal, a constant XOR (try XOR-ing output-decoded bytes against input bytes at matching reversed positions to solve for the XOR constant), and standard encodings (test if the output alphabet/padding matches base64 by trying to base64-decode it and inspecting the byte pattern). Once you can reproduce the binary's output byte-for-byte with pure Python across many trial payloads, harden it into a `sign(payload: str) -> bytes` function with no `subprocess` calls. Then implement `/home/user/proxy.py` as a threaded/async HTTP server on `localhost:8080` (e.g. `http.server` with `ThreadingHTTPMixin`, or `asyncio`) that for each GET request computes the signature of the full path+query, adds it as the `X-Custom-Signature` header, and forwards the request to `http://localhost:9000` (using `http.client` or `requests`, reusing connections/keep-alive for speed), relaying the backend's response back to the caller. Benchmark locally with a quick loop of 2000 requests to confirm you're comfortably under the 1.5s threshold before considering it done.
