Start by reading legacy_sorter.py carefully to pin down the exact semantics you must replicate: it globs all CSVs in /home/user/data/, parses each with csv.DictReader (so column order/types follow the header), casts timestamp to int, concatenates all rows across files, then sorts descending by integer timestamp using a stable sort — Rust's `sort_by`/`sort_by_key` is also stable, so replicate the same descending-by-int-timestamp key and keep all other fields as strings exactly as read. In the Rust service (using the pre-seeded axum/tokio/csv/serde Cargo.toml), implement a `GET /process` handler that reads the same CSV directory, builds structs (or a HashMap/Vec of ordered fields) serialized with serde_json to match Python's field order and JSON formatting (no extra whitespace differences matter once you `jq -c` both), and returns them as an array. For the nginx.conf, since you lack root, set `pid`, and log paths under /home/user/, use `daemon off;` is not required for `-c` invocation but avoid directives requiring root (chown, bind <1024), listen on 127.0.0.1:8080, and `proxy_pass http://127.0.0.1:3000;` in `location /`. After starting both services, generate py2/rust outputs, canonicalize both with `jq -c '.'`, and diff — an empty diff.txt is your correctness signal, so if diff is non-empty, compare field-by-field rather than guessing.
