This task has two independent halves — tackle them separately. For Part 1, inspect `/app/legacy_deploy.sh` first to see its exact prompt text and order, then write a `pexpect.spawn` script that sets `os.environ['TZ']='Europe/Berlin'` and `os.environ['LANG']='de_DE.UTF-8'` (and pass `env=os.environ` into spawn, or spawn a subshell that exports them) BEFORE spawning the script, then use `child.expect()` on each prompt and `child.sendline()` with the exact required answers in order, finishing with `child.expect(pexpect.EOF)` and checking the transcript/exit status for the success message. For Part 2, this is black-box reverse engineering, not guesswork: run `strings /app/auth_signer` to look for format strings and locale/timezone hints, and `objdump -d /app/auth_signer` or trace with `strace`/testing known timestamps to infer the exact `strftime`-style format and timezone conversion logic. Reproduce it in Python using `zoneinfo.ZoneInfo('Europe/Berlin')` and `locale.setlocale(locale.LC_TIME, 'de_DE.UTF-8')` (installing the locale if needed via `locale-gen`) with a matching `strftime` format string. Validate your Python port by running `/app/auth_signer <ts>` alongside your script across many timestamps spanning DST transitions (spring/fall boundaries within the 1600000000–1800000000 range) and diffing outputs byte-for-byte until they match exactly, since the DST rule (CET vs CEST) is the part most likely to diverge.
