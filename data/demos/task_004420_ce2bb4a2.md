Build the pipeline exactly to spec since the grader diffs your JSON numerically against a reference run of the same statistical procedure — deviating from any specified parameter (fold count, alpha grid, CI method, test variant) will silently produce different numbers. Set up the venv and install the four packages first, then write `quality_check.py` top-to-bottom: call `np.seterr(divide='raise')` before any math; load both CSVs; for Phase 1 fit `Ridge` inside `GridSearchCV(cv=5)` over exactly `[0.1, 1.0, 10.0, 100.0]` with default scoring and pull `best_params_['alpha']` and `best_score_`; for Phase 2 compute the batch revenue mean and `scipy.stats.sem`, then feed both into `scipy.stats.t.interval` with `df=n-1` to get the CI bounds; for Phase 3 run `scipy.stats.ttest_ind(historical_revenue, batch_revenue, equal_var=False)` and threshold the p-value strictly `< 0.05`. Round every float to exactly 4 decimal places with Python's `round()` before serializing (not just formatting on print) so JSON types stay numeric, and write the dict straight to `/home/user/pipeline/report.json` with `json.dump`. Run the script yourself afterward and inspect the output file to catch type/formatting mistakes (e.g. numpy scalar types not being JSON-serializable) before considering it done.