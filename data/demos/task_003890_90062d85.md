Build the pipeline as clearly separated, independently testable stages, since each has its own numerical subtlety. Write the log-posterior as log-prior (uniform box on A, lambda, B; return -inf outside bounds) plus log-likelihood (Gaussian, fixed sigma=0.5) exactly as specified, then feed the negative of it to `scipy.optimize.minimize(method='Nelder-Mead')` from the given initial guess to get the MAP. Seed `np.random.seed(42)` immediately before constructing the emcee walker initial positions (a tiny Gaussian ball around the MAP with std=1e-4, 10 walkers) — seeding order matters for reproducibility, so do it right before that call, not earlier. Run the `emcee` sampler for 500 steps, then discard the first 100 as burn-in and flatten the A-column of the remaining chain. Fit `scipy.stats.gaussian_kde` on that 1D sample, evaluate it over a 1000-point grid from 0 to 10, and take the argmax as the KDE peak. Plot a histogram of the A samples with the KDE curve overlaid and save to `posterior_A.png`. Write the four float keys to `results.json` with `json.dump` (cast numpy floats to native `float()`). Separately, write a pytest test that reruns just the optimization and asserts `A_map` is in [4.9, 5.1], then run pytest and redirect output to `test_results.log`.
