Build this as a multi-stage pipeline, handling each server's quirks separately before merging. 1) Copy the three `.log` files from `/home/user/remote_share` to `/home/user/local_logs` (mkdir -p first). 2) Convert encodings per-file with `iconv`: alpha is UTF-16LE, beta is ISO-8859-1, gamma is already UTF-8 — use `iconv -f UTF-16LE -t UTF-8` and `iconv -f ISO-8859-1 -t UTF-8` respectively, overwriting or writing to new files. 3) For each converted file, grep only lines containing `[CRITICAL]` or `[ERROR]`, then use a regex (awk/sed/grep -oP) to pull out the timestamp field, IP address, and the 3-digit error code following "Code". 4) Normalize timestamps to strict `YYYY-MM-DDTHH:MM:SSZ` UTC: epoch values need `date -u -d @EPOCH`, US `MM/DD/YYYY HH:MM:SS` needs `date -u -d "MM/DD/YYYY HH:MM:SS"`, and the ISO-like `YYYY-MM-DD HH:MM:SS` (already UTC) just needs a `T`/`Z` reformat — be careful with GNU vs BSD `date` flag differences. 5) Combine all rows into one CSV with header `Timestamp,IP_Address,Error_Code`, then `sort -t, -k1,1` chronologically by the normalized timestamp column before writing the final file to `/home/user/suspicious_activity.csv`.
