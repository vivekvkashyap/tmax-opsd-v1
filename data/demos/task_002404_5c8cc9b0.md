This has two independent halves — do the credential-leak remediation and the IDS filter separately, then verify end-to-end.

For the SSH hardening: generate a passwordless keypair with `ssh-keygen -t rsa -N "" -f /home/user/.ssh/id_rsa`; edit `/app/sshd/sshd_config` to set `Port 2222`, `PasswordAuthentication no`, `PubkeyAuthentication yes`, `AuthorizedKeysFile /app/sshd/authorized_keys`; copy the public key into that authorized_keys file with `chmod 600`. Rewrite `helper.py` to build an SSH command/paramiko client that authenticates via the private key file (never a `--password` CLI arg — CLI args leak through `/proc/<pid>/cmdline`), and update `flask_app.py`'s `subprocess.Popen` call to drop the `--password` argument entirely. Restart via `start_services.sh` and confirm `curl localhost:8080/trigger` still succeeds and no process listing shows a plaintext credential.

For the IDS filter: iterate `.txt` files in the input dir, `base64.b64decode` then `json.loads` each, check both `username` and `command` fields against the metacharacter set and `../`, and write `filename,label` rows to the output CSV — treat this as a pure string-membership check, no regex edge cases skipped.