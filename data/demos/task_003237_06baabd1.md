Abandon the broken numerical-library approach entirely — this needs an exact integer scorer, not floating-point cosine similarity. First OCR `/app/scoring_rules.png` (e.g. `tesseract`) to recover the exact `MIN_TOKEN_LENGTH` and `GLOBAL_MULTIPLIER` integers stated in the message; read the numbers carefully since every score depends on them. Then write `relevance.cpp`: take `argv[1]` as the document, scan character by character treating any non-alphanumeric character (test with `std::isalnum`) as a token boundary, lowercase each accumulated token (`std::tolower`), and only count tokens whose length is `>=` the OCR'd `MIN_TOKEN_LENGTH` (don't forget to flush/check the last token after the loop ends, since the input may not end in a delimiter). Tally token frequencies in a hash map, then compute the dot product against the fixed target dictionary `{"anomaly":5,"dataset":3,"clean":4,"null":2}` by summing `count(token) * weight` only for tokens present in the target map, multiply the sum by the OCR'd `GLOBAL_MULTIPLIER`, and print just that integer plus a newline — no other output. Compile with `g++` to `/home/user/relevance` and `chmod +x` it; your logic will be fuzz-compared against a hidden oracle on random ASCII strings, so match tokenization edge cases exactly (empty input, leading/trailing punctuation, consecutive delimiters).