Write and test the C verifier in isolation first. Read the whole `.rle` file into a buffer (`fread`), check its size is at least 4 bytes or immediately print `CORRUPT` and exit; then sum every `count` byte from consecutive 2-byte (count, char) blocks over the portion of the file before the trailing 4 bytes, being careful that the block-parsing loop stops exactly at `filesize - 4` so it never reads into the trailer as if it were another block. Read the last 4 bytes as a little-endian uint32 (`buf[n-4] | buf[n-3]<<8 | ...`) for the expected length, compare it to your summed count, and print exactly `OK` or `CORRUPT` (no extra whitespace/newline surprises) accordingly. Compile with `gcc -o /home/user/verify /home/user/verify.c`. For the shell driving logic, read `directories.conf` line by line, `find "$dir" -name '*.rle'` recursively for each, run `verify` on each hit, and append `"$file $status"` lines to `summary.log`; finally sort the whole file in place with `sort -o /home/user/summary.log /home/user/summary.log` (plain lexicographic path sort) rather than sorting during collection.
