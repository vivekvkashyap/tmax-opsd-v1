In setup.py, branch on `os.environ.get('ENABLE_C_EXT') == '1'` and only pass `ext_modules=[...]` to `setup()` when that's true (omitting/empty-listing it means setuptools won't try to compile anything) — don't leave the `Extension()` call unconditional. In parser.py, implement a small 2-state machine (outside-block / inside-block): only open a record on a `BEGIN tx=...` line, only emit it on `END`, and silently skip any line that doesn't match `ATTR key=value` or `END` while inside a block, and any line at all while outside one. In migrator.py, transform each dict by popping `tx` into `transaction_id` cast to int, adding `schema_version: 2`, and nesting every remaining key/value pair under `properties` (empty dict if none remain). Wire run_pipeline.py to import both functions from the installed package, read raw_data.log, and write one JSON object per line to migrated_output.jsonl. Install with `ENABLE_C_EXT=0 pip install -e .` and run the pipeline script to produce the output for verification.
