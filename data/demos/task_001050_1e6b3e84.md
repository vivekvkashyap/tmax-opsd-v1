Build the pipeline as discrete, testable stages in Python rather than one monolithic pass. (1) Parse the CSV with the `csv` module, and for each row extract the substring between the literal marker `"Equation: "` and the next `"."` using `str.find`/slicing (or a narrow regex anchored to that exact marker) — do not use a greedy regex that could span multiple periods. (2) Evaluate the extracted expression with Python's `eval()` (safe here since input is a controlled fixture) and convert with `str()`, preserving Python's native formatting (e.g. integer division vs float results look different — don't force formatting). (3) Mask the email by keeping the first character of the local part, appending literal `***`, then `@` and the domain unchanged — split on `@` first. (4) Group rows by category (a dict of lists), sort each group's rows by numeric evaluated result descending with id ascending as tiebreak, then keep only the top 2 per group. (5) Sort categories alphabetically for final output. (6) Emit the report with an exact blank line between category blocks, and double check there's no trailing/leading whitespace mismatch or extra newline versus the specified template before writing to `/home/user/report.md`.