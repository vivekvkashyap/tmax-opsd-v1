Do not guess the security policy — derive it from the audio first. Transcribe /app/voicemail.wav with an ASR tool (e.g. `whisper`) to recover the exact required VNC bind address and the exact allowed network bridge name; these become two additional rules alongside the explicit ones in the task (block `-virtfs`/`-fsdev` host-mount flags, and block any hostfwd rule forwarding to/from port 25). Implement /home/user/qemu_filter.py to read the target script's text (handling missing/unreadable files by exiting 1), parse out the `-vnc` argument and compare it against the transcribed bind requirement, parse the netdev/bridge argument and compare against the transcribed bridge name, and regex/string-search for virtfs/fsdev flags and any hostfwd pattern targeting port 25 — reject (exit 1) if any rule is violated, otherwise exit 0. Rather than trusting your parsing logic blindly, iterate: run your script against every file in /app/corpus/clean/ and /app/corpus/evil/, and refine the parsing (e.g. handling multi-line backslash-continued qemu commands, address matching precision) until every clean file exits 0 and every evil file exits 1.