First recover the secret key from the video before writing any validation logic. Extract one frame per second from `/app/key_transmission.mp4` (e.g. with `ffmpeg -i ... -vf fps=1 frame_%02d.png`, or read frames directly with OpenCV), classify each extracted frame as black or white by sampling pixel brightness, and decode the protocol precisely: skip the idle frame at second 0 and the start bit at second 1, read the next 16 frames (seconds 2-17) as bits MSB-first (white=1, black=0) to reconstruct the 16-bit key, then confirm frames at second 18 (stop=white) and 19 (idle=black) match expectations as a sanity check on your frame indexing. Convert the bit string to an integer. Then write `/home/user/validator.py` implementing `token_integer = (zlib.crc32(username.encode()) & 0xFFFF) XOR master_secret_key`, format it as 4-character lowercase hex, and compare against each line's `token_hex` after splitting on the first `:`; print `ACCEPT` only if every line in the file matches, otherwise `REJECT`. Test your script manually against a couple of hand-constructed valid and forged lines using your own recovered key before relying on the hidden corpora, and be careful with hex formatting (zero-padding to 4 digits, lowercase) since a mismatch there alone will fail otherwise-correct logic.