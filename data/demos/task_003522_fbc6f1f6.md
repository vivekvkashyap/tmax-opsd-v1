Work this in clearly separated stages and verify each before moving on. First fix `setup.py`: inspect the `Extension(...)` sources list and add the missing `src/helper.c` so both C files are compiled and linked together — a linking error where symbols from helper.c are undefined is the telltale sign. Second, determine N by analyzing `/app/glitch.mp4` programmatically (don't guess): extract frames (e.g. with ffmpeg to PNG/raw or via a Python video-reading library) and count frames whose pixels are exclusively pure red (255,0,0) — write a small script that checks each frame's pixel value set rather than eyeballing it. Third, open `src/processor.c`, locate the unsafe `strcpy`/buffer-overflow pattern (likely copying attacker-controlled string into a fixed buffer without a length check or with an off-by-one on the null terminator), and fix it by clamping/truncating the input to N characters using a bounded copy (e.g. `strncpy` plus explicit null-termination, or manual length check) before any further processing, preserving whatever transformation logic the original code intended (inspect helper.c for the transform semantics). Finally build `run_processor.py` as a thin CLI wrapper importing the compiled extension. Test with hand-crafted strings at length N-1, N, and N+50, plus empty and non-ASCII input, and consider running under valgrind or hypothesis-based fuzzing to confirm no overflow/crash before finishing.
