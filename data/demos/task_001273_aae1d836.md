Write the C program to do a single streaming pass: for each line, split on commas and strictly validate the field count (1 ID + 16 floats) and that every field parses cleanly (use `strtol`/`strtod` and check the end pointer to catch trailing garbage or non-numeric text) — any failure means the raw line (verbatim, with its original newline) goes to rejected.log and is skipped entirely, not just when the wrong type is obviously wrong. Store valid records in an array of {id, float[16]}. For similarity, compute cosine similarity for every unique pair (i<j) via dot product over the two L2 norms; only keep pairs >= 0.9500, always emitting with id1<id2 regardless of file order, and sort by id1 then id2 before writing with %.4f formatting. Wrap only the pairwise-comparison double loop (not I/O or parsing) with `clock_gettime(CLOCK_MONOTONIC,...)` before/after, and format the elapsed seconds with exactly 6 decimals into benchmark.txt. Compile with `-O3 -lm` and actually execute the binary so the three output files exist before finishing.