Build the Go pipeline as separate, verifiable stages. (1) Parse raw_data.csv with encoding/csv, collecting all non-empty X1 values as ints; compute the median by sorting the slice and taking the middle element for odd count or the lower of the two middle elements for even count (the spec says round down when between two integers — do NOT average-then-round, take floor explicitly, e.g. `sorted[n/2 - 1]` for even n after 0-indexed sort, or more robustly sort and pick index `(n-1)/2` after averaging then applying `math.Floor`). Fill blank X1 cells with this integer median and keep the column as `int`, not `float64`, throughout so it never gets serialized with a decimal point. (2) Compute X3 = X1*X2 per row. (3) For CV, split rows into 5 contiguous, non-shuffled blocks in original row order; for each candidate T in {10,20,30,40,50}, predict per row using the *other* folds is not required here — spec says compute MSE on the validation set per fold with the fixed rule (X3>=T→100 else 0), then average the 5 fold-MSEs; pick the T with lowest average MSE (break ties toward the smallest T unless told otherwise). Write clean_data.csv with the exact header and result.txt with the exact two-line format, MSE formatted with `%.2f`.