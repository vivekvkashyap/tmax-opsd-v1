Start by extracting the schema rules from the image rather than guessing: run `tesseract /app/schema.png stdout` (try a couple of `--psm` modes if the first pass is garbled) to recover the allowed node types, allowed relation edges, and the topology constraints in the diagram — read the OCR output carefully since it defines your validation rules, and don't hardcode assumptions before confirming it. Model the CSV rows as a directed graph in Go (adjacency structure keyed by node ID with type/relation info), validate: (1) every SourceType/TargetType pair matches an allowed relation from the schema, (2) no self-loops (SourceID == TargetID within a triple-hop), and (3) no forbidden cycle pattern as described in the image — implement cycle detection (e.g. DFS or explicit path-tracing for the specific two/three-hop pattern) rather than a general expensive algorithm, since the graphs are small per-CSV. Iterate against `/app/corpora/clean/` and `/app/corpora/evil/` with `go run graph_validator.go <file>` to confirm ACCEPT/REJECT matches expectations for every sample before considering the task complete, since the hidden grading corpus will test the same distinguishing patterns.
