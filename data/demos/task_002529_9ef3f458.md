Start with OCR: run tesseract (or an equivalent OCR tool) on `/app/hyperparameters.png` and parse out the three key-value lines (`CV_FOLDS`, `PCA_COMPONENTS`, `K_NEIGHBORS`) — print the raw OCR text first and sanity-check it parses cleanly as an int and two integer lists before hardcoding anything, since OCR can misread characters. Clean `/app/data.csv` by dropping any row with NaN/empty values (`pandas.dropna` after `read_csv`, treating empty strings as NaN). Build the pipeline with `sklearn.pipeline.Pipeline([StandardScaler(), PCA(), KNeighborsClassifier()])` and run `GridSearchCV` with `cv=CV_FOLDS` over `{'pca__n_components': PCA_COMPONENTS, 'knn__n_neighbors': K_NEIGHBORS}` scored on accuracy, then refit the best pipeline on the full cleaned data. For serving, use a lightweight framework (Flask/FastAPI or `http.server`) bound to `127.0.0.1:8000` with a `POST /recommend` handler: transform the incoming features through the fitted scaler+PCA, predict the class with the fitted KNN for `recommended_class`, and separately find the best-k nearest neighbors in PCA-space among the cleaned training rows (e.g. via `NearestNeighbors` fit on the PCA-transformed cleaned data, or reuse the KNN's `kneighbors` method) to return their original `id` column values ordered by distance for `similar_ids`. Run the server in the background and confirm with a curl POST before finishing.
