Work through the three layers in dependency order: Rust ABI first, then Python, then CI glue. In `src/lib.rs`, the bug is a classic dangling pointer — a `CString` is created as a local and its `.as_ptr()` is returned, but the `CString` is dropped at function end, invalidating the pointer. Fix it by leaking ownership across the FFI boundary with `CString::into_raw()` instead of `as_ptr()`, and add a companion `#[no_mangle] extern "C" fn free_string(ptr: *mut c_char)` that reconstructs the `CString` via `CString::from_raw(ptr)` and lets it drop, which frees the memory correctly. In `parser.py`, set `restype` appropriately (a raw pointer, not `c_char_p`, if you want to call the free function afterward), decode the JSON with the `json` module, pull `["payload"]["id"]`, write just that integer to `output.json`, and call the new `free_string` via ctypes after use to eliminate the leak. Finally fix `ci.sh` to compile the Rust file into `librust_parser.so` with `rustc --crate-type cdylib` (matching the actual lib name expected by parser.py), run `parser.py`, and propagate a clean exit code — test by running `./ci.sh` end to end.