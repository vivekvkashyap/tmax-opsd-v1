Debug this in three separate layers and test each before moving to the next. First, fix the build in `/app/libprime-1.0.0`: inspect the Makefile for tab-vs-space issues in the recipe line (make requires literal tabs, not spaces, before commands) and for a missing math-library link flag — if `prime.c` calls `sqrt()` or similar, add `-lm` to the link command; run `make clean && make` until `prime` builds cleanly and `./prime 1` returns the correct first prime. Second, fix `/app/prime_encoder.sh`: read it carefully for an off-by-one when passing N to the compiled `prime` binary (compare against what `./prime 1`, `./prime 2` etc actually return to confirm the correct indexing), and fix the base64 encoding — use `printf` instead of `echo` to avoid an extra trailing newline before piping to base64, and add `-w 0` to `base64` to disable line wrapping so the whole output is one line. Since the verifier fuzzes with random N against a hidden oracle for bit-exact equivalence, test your fixed script against several N values manually and diff the byte-level output (including absence of trailing newline) rather than trusting visual inspection.