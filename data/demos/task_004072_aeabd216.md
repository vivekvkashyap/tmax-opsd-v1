Implement and validate this in stages, cross-checking against the analytical posterior rather than only trusting the simulation output. In `posterior_estimation.cpp`, construct one `std::mt19937 rng(42)` instance and reuse it for both the data generation (`std::normal_distribution<double>(5.0,1.0)` drawn 50 times) and later the MCMC proposals/uniform draws — never create a second RNG, since the spec requires a single continuous stream. For gradient ascent, implement the closed-form derivative `L'(mu) = -mu/100 + sum(x_i - mu)` and iterate `mu += lr * L'(mu)` for 100 steps at lr=0.01; sanity-check by deriving the analytical MAP yourself (it's a conjugate Normal-Normal update, so compute it by hand from the prior variance 100 and likelihood variance 1 with N=50) and confirm gradient ascent converges near that value. For MCMC, propose `mu_prop ~ N(mu_curr, 0.5^2)` using the same RNG, compute the log-posterior ratio via the L(mu) formula (avoid recomputing the full sum from scratch each time if possible, but correctness first), accept via a uniform(0,1) draw compared to `min(1, exp(L(prop)-L(curr)))`, run 100000 iterations, discard the first 50000, and compute empirical mean/variance of the remainder — these should converge near the same analytical posterior mean/variance you computed for the gradient-ascent check. Write the three lines in the exact required format to `/home/user/results.txt`, and build/run via an executable `run_pipeline.sh` compiling with `g++ -std=c++17`.