Profile before you optimize: read `/app/log-alerter/src/main.rs` and look for anything happening inside the per-line loop that should only happen once — a compiled regex being rebuilt on every iteration is a classic perf bug that turns O(1) matching into O(n) recompilation. Move the `Regex::new(...)` call outside the `for line in reader.lines()` loop, and also switch the Makefile/build invocation to `cargo build --release` (release mode alone often yields a large speedup) — combine both fixes to comfortably clear the 1.5s bar. For the path issue, notice the binary resolves its output directory from the `ALERT_DIR` env var, defaulting to `.` if unset; since monitor.sh will be invoked via `env -i bash monitor.sh` (a stripped environment), you must explicitly `export ALERT_DIR=/home/user/alerts` inside monitor.sh (and set correct `cd`/`PATH` as needed) before invoking the binary, rather than relying on inherited environment state. For the SMTP piece, write a small Python script that opens a raw TCP listener on 127.0.0.1:2525, accepts the Rust binary's connection, reads the SMTP-like commands it sends, and appends the captured email content to /home/user/emails.log — launch it as a persistent background process (`nohup ... &` or similar) before you test monitor.sh end-to-end.