Write this as a small parsing pipeline with a single regex driving extraction, then a two-stage aggregation. Use a regex anchored to the exact given line format (capturing timestamp, severity, user_id after `user:`, and error_code after `code:`) applied only to lines containing `code:` — count total lines read separately from lines successfully matched/parsed so your metrics stay accurate even if some `code:`-containing lines don't fully match the format. For stratification, iterate records in original (chronological) file order, group by `error_code` in a dict, and only keep appending to each group's list while its length is < 2 — this naturally captures "first 2 chronological occurrences" without needing to sort first. After building the per-code lists, flatten them into one list and do a final sort by `(error_code, timestamp)` ascending, since chronological insertion order alone doesn't guarantee alphabetical grouping in the output. Compute `unique_error_codes` as the size of the group dict. Write both output files with `json.dump`, using plain Python ints for the metrics numbers, and re-read them afterward to confirm valid JSON and correct key sets.