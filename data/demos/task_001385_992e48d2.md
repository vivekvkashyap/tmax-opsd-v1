Split this into extraction and algorithm work, and get the algorithm exactly right since it will be fuzz-tested against thousands of random graphs. For "lexicographically first topological sort", don't just alphabetically-sort each layer of a BFS — use Kahn's algorithm with a min-heap of currently-zero-indegree nodes: repeatedly pop the smallest available index, append it to the result, decrement indegree of its neighbors, and push any that newly reach zero indegree; this greedy choice at each step is what "lexicographically first" actually requires, and it differs from naive per-level sorting once dependencies unlock nodes out of order. If the result length ends up short of N, output exactly `CYCLE DETECTED`. Make resolver_cli fast for N up to 50 (heap operations are already O(N log N), avoid anything worse). Separately, extract the 10x10 matrix from `/app/pipeline_state.mp4` by pulling all 100 frames (e.g. `ffmpeg -i ... frame_%03d.png` or reading raw grayscale frames) and thresholding each frame's mean pixel brightness at 128 to get a 0/1 bit, filling the matrix row-major before feeding the resulting 100-character string to your own resolver_cli.
