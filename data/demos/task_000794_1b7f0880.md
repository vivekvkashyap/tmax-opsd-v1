Work incrementally: read the existing `expr_ext.c` first, then strip it down to plain C. Identify the core arithmetic logic (the function that actually parses and evaluates an expression string) separately from the CPython glue (`#include <Python.h>`, `PyObject*`, `PyArg_ParseTuple`, `PyMethodDef`, module init) — copy only the former into `libexpr.c`, rewriting its signature to match `double evaluate(const char* expression);`, and declare that exact signature in `libexpr.h` with include guards. In `config_parser.c`, parse each line by splitting on the first `": "` occurrence to separate key from expression, call `evaluate()` on the expression, and write `Key=Value\n` using `%.2f` formatting — watch for trailing newline/whitespace in lines from `fgets`/`getline` corrupting the key. In the Makefile, compile the shared library with `-shared -fPIC` and link the parser against it with `-L. -lexpr` plus `-Wl,-rpath,.` (or set `LD_LIBRARY_PATH` when running) so the executable can find `libexpr.so` at runtime — a very common failure is a successful `make` but a runtime "cannot open shared object" error. Build, run against the given config, and diff the output.
