Work in two clearly separated stages. First, load `/home/user/spectra.csv` and compute each observed concentration by integrating `Intensity_1` and `Intensity_2` over the subset of rows where 400 <= Wavelength <= 500, using the trapezoidal rule (e.g. `numpy.trapz(y, x)`) — don't just sum/average the intensity values, and don't integrate over the full wavelength range. Second, for the optimization: write a helper function `solve_reactor(C0, k)` that, given the previous stage's concentration, solves the scalar nonlinear equation `C_prev - C - k*C**2 = 0` for `C` using a root finder (e.g. `scipy.optimize.brentq` restricted to positive C, or `fsolve` with a positive initial guess) — chain it twice to get C1(k) then C2(k) from C1(k). Then define an SSE objective over k comparing (C1(k), C2(k)) to the observed values from stage one, and minimize it with a 1-D bounded/scalar optimizer (e.g. `scipy.optimize.minimize_scalar` with k>0 bound, or `brentq` on the derivative). Round all three final values to 4 decimals and write them to the specified JSON with the exact keys `C_obs_1`, `C_obs_2`, `k`.
