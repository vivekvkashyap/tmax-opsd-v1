Reverse-engineer `/app/migrator_legacy` by piping several varied JSON inputs into it (`echo '{"uid":..,"cmd":..,"params":..}' | /app/migrator_legacy`) and observing outputs — infer the exact v1→v2 mapping (uid zero-padding, cmd case change, params being a URL-encoded query string turned into a `config` object) rather than assuming; test edge cases (multiple params, missing fields) to nail the format precisely, then reimplement that transform natively in `jq`/bash so the running server never calls the binary. Separately, diff the files in `/app/corpus/evil/` against `/app/corpus/clean/` to infer the exact rejection rule (e.g. shell metacharacters in `params`, or a `cmd` that isn't purely alphabetic) — write this as an explicit `jq`/regex check in your handler, and validate it rejects 100% of the evil samples and accepts 100% of clean ones before moving on. For the server itself, use `nc -l -k -p 8000` or `socat TCP-LISTEN:8000,fork,reuseaddr` in a loop, parse the HTTP request to isolate the body (read past the blank line, use Content-Length if present), then return a correctly formatted `HTTP/1.1 403 Forbidden` / `200 OK` response with proper headers before the body. Leave the process running in the background at the end.
