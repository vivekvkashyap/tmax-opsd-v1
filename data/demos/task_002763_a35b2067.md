Split this into three independently testable pieces. (1) In `link_assets.sh`, glob `/home/user/tenants/tenant_*/config.json`, compare each to the baseline with `cmp -s` or `diff -q` against `/home/user/tenants/baseline/config.json`, and only for byte-identical matches replace the tenant's file with `ln -f /home/user/tenants/baseline/config.json <tenant_path>` (remove the old file and hard-link, or use `ln -f` which does this atomically) — never touch files that differ; verify success with `stat -c %i` inode comparison. (2) In `rotate.sh`, rotate from the highest number down to avoid clobbering (`mv billing.log.2 billing.log.3`, then `mv billing.log.1 billing.log.2`, then `mv billing.log billing.log.1`, each guarded with an existence check), then read the PID from `/home/user/logs/billing.pid` and `kill -SIGUSR1 $(cat ...)` so the running process reopens a fresh log; make the script executable with `chmod +x`. (3) In `monitor.c`, loop forever: `stat()` the given path, compare `st_size` to the `atoi`'d second argument, and when it strictly exceeds the limit call `system("/home/user/rotate.sh")`, then `sleep(1)` regardless. Handle `stat()` failure (missing file) by printing an error and continuing/retrying rather than crashing.
