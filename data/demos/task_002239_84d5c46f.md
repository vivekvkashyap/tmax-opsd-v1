Fix the pipeline stage by stage, verifying each in isolation before chaining. (1) Dependency conflict: pin a numpy version compatible with the pandas version in `requirements.txt` (a newer numpy major version can break an older pandas' internals) — either downgrade numpy below its breaking major version or upgrade pandas to one that supports it; install into the venv and confirm `import pandas` works standalone first. (2) Transform bug: run `transform.py`, then `diff output.csv expected.csv` yourself and read the diff output carefully — the actual issue in this class of bug is usually the script truncating/casting the value to an integer before multiplying by 2.5 (losing the fractional part), so fix the dtype handling to multiply as floats, and match `expected.csv`'s exact formatting (decimal places, no extra spaces) via `float_format` or explicit rounding. (3) build.sh score bug: use `bc -l` or explicit `scale=4` inside the `bc` expression (not just as a flag) since scale must be set before the division happens, and fix the operator precedence by parenthesizing explicitly — `(sum / count) * 3.14159` computed as `scale=4; (sum/count)*3.14159` piped to `bc`, not shell arithmetic which truncates to integers. After each fix, rerun `./build.sh` from scratch and confirm exit code 0 and that `final_score.txt` contains a properly-scaled decimal, not an integer or truncated value.