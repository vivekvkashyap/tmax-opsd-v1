First transcribe `/app/incident_report.wav` (ffmpeg + a whisper-style tool) and listen/read carefully for the specific field name it names as restricted for `$group` blocking — use that recovered field name in your rule, don't guess a plausible-sounding one. Then write `filter.c` against the provided `cJSON` library (compile with `cJSON.c` alongside your file) that reads all of stdin, parses it as JSON (it may be a single object or an array of pipeline stages), and recursively walks the parsed structure looking for: any key literally named `$where` anywhere, any key literally named `$lookup` anywhere, and any `$group` stage whose `_id` value is the string `$<restricted_field>` (with the leading `$`) from the transcript. If any rule matches, print `EVIL` and exit 1; otherwise print `CLEAN` and exit 0. Recurse into nested objects and arrays generically (don't assume the violation is only at the top level) since MongoDB pipelines are arrays of stage objects. Compile to `/home/user/query_filter` and validate against every file in `/app/corpus/clean/` and `/app/corpus/evil/`, confirming exact match on classification before finishing.
