Build this as a straight numeric pipeline in Go, verifying each stage independently before chaining them. (1) Read the DNA file, map each base with the given encoding (A=1.0, C=-1.0, G=2.0, T=-2.0), and zero-pad the slice up to the next power of two (compute via `math.Ceil(math.Log2(n))` or a bit-shift loop, not just doubling until >=n from a wrong start). (2) Run an FFT over the real-valued sequence (via the suggested `go-dsp/fft` package or a hand-rolled radix-2 FFT if that package proves unavailable — check with `go get` and fall back early rather than late). (3) Compute per-bin power as Re^2+Im^2, drop index 0 (DC), and compute population mean/stddev (divide by N, not N-1) over the remaining bins. (4) Z-score = (max power - mean)/stddev; classify Periodic if Z >= 3.00 else Random, format Z to exactly 2 decimals with `%.2f`. (5) Print exactly the specified one-line format per file. Write `run_analysis.sh` to `go build` the analyzer then invoke it on `seq1.txt` then `seq2.txt` in order, redirecting combined stdout to `results.log`. Validate your FFT implementation against a known simple case (e.g., a pure sine or a short DC-only signal) before trusting the Z-scores on the real sequences.