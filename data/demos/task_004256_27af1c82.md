In `/home/user/filter.go`, read stdin with `encoding/csv`, validate every field strictly and independently per row, and skip (not error on) any row that fails any check or has the wrong column count — malformed rows must be silently dropped, never crash the program. Parse `timestamp_sec` with `strconv.Atoi` and require `>= 0`; parse lat/lon/altitude with `strconv.ParseFloat` and check the exact inclusive bounds (-90..90, -180..180, altitude >= 0); check `status` against the literal set {"OK","WARN","ERR"} with a switch/map, not a loose substring check. Preserve the header line verbatim on stdout. Build the binary as `telemetry_filter` (compiled at `/home/user/telemetry_filter`) so it can be piped into directly: `cat file.csv | ./telemetry_filter`. Test it against edge cases yourself (out-of-range values, wrong column counts, non-numeric fields, boundary values like exactly -90.0/90.0) since a hidden corpus of clean vs. adversarial CSVs will be piped through it and every clean row must survive while every evil row is rejected. For video features, use `ffmpeg -vf fps=1` to dump one frame per second, convert to grayscale and average pixel values per frame in Go (or via ffmpeg's own grayscale filter), writing `timestamp_sec,brightness`. Finally, `pipeline.sh` should build the Go binary, run both stages, then do a simple inner join on `timestamp_sec` (sorted, joined by matching keys) into `merged_output.csv`, exiting 0 only on full success.