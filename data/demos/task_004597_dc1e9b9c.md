Treat this as three separable stages you can verify independently. First, transcribe `/app/audio/network_intel.wav` (install `openai-whisper` or use another ASR tool) and parse the sentence pattern "Node X connects to node Y with a latency of Z" into (source, target, weight) triples — spoken numbers may need normalizing to digits, so print the raw transcript and sanity-check the extracted edges before trusting them. Second, inspect `known_edges.csv`'s header and column order carefully (source, target, latency) and merge those rows with the audio-derived edges into one unified directed edge list, treating edges as strictly directed (do not add reverse edges unless the transcript states them). Third, implement the C++ engine with an adjacency-list representation and a standard Dijkstra (min-heap/priority_queue) over non-negative weights, reading argv[1]/argv[2] as integers, printing a single integer (or -1 if unreachable), and compiling exactly to `/home/user/query_engine`. Since correctness is checked via fuzzed random source/target pairs against a hidden reference, test your binary yourself on several pairs including unreachable ones and self-loops (source==target should output 0) before considering it done.