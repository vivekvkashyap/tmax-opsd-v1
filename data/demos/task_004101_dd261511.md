Design the traversal as a recursive/iterative walk that carries a "current branch" stack of (device, inode) pairs for directories already visited on the path from the root of this call chain — use `lstat`/`stat` to get device+inode for each directory entry before descending, and push/pop the pair around each recursive call so siblings don't falsely trigger loop detection. When you `lstat` a symlink and it resolves (via `stat`, following the link) to a directory whose (dev, inode) is already on the current stack, that's a loop: record the symlink's absolute path (not the target) to `/home/user/loop_report.txt` and skip descending into it — don't call `readlink` recursively yourself, let `stat` do the following. For plain files, use `lstat` to make sure you only process regular files ending in `.dat` (skip symlinked files entirely), get the inode via `stat`, and compress with zlib's `gzopen`/`gzwrite`/`gzclose` writing to `/home/user/compressed/<name>_<inode>.gz`. Wrap all syscalls with error checks and just `continue`/skip on `EACCES` rather than aborting. After compiling with `-lz` and running, validate every archive with `gzip -t /home/user/compressed/*.gz` to confirm none are corrupt.