Because this is graded by fuzz equivalence against a hidden oracle binary, prioritize matching exact output format/semantics over cleverness. First, transcribe `/app/voicemail.wav` (e.g. install `ffmpeg` plus a speech-to-text tool such as `whisper`, or `whisper.cpp`) — don't skip actually listening/transcribing, since the join keys, schema, filter arguments, and output column names/order are only specified verbally, not in the task text. From the transcript, determine the three CSVs' schemas and the exact join/filter/aggregation logic (e.g., join transactions to users and products, filter by two CLI args, group by user, sum amounts) and the exact output format (header line, column order, sort order, decimal precision). Implement this in C++ reading CSVs from `/home/user/data/` with a simple line-based parser (these fixtures have no embedded commas/newlines, so split-on-comma is fine), performing the join/filter/aggregation with a `std::map` for deterministic sorted-by-key output, formatting floats to exactly the decimal places requested (e.g. `std::fixed << std::setprecision(2)`) to avoid trailing-zero or rounding mismatches. Compile to exactly `/home/user/query_engine` and test it yourself against a couple of the sample rows from the CSVs before considering it done, since the automated fuzzer will invoke it directly with varying region/category arguments including invalid ones (expect empty output, not a crash).
