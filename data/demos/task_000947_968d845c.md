Work the pipeline stage by stage and verify each before moving to the next. First run `tesseract /app/evidence.png stdout` (install tesseract-ocr if missing) to read the "TargetEncoding: ..." string off the image — don't hardcode a guessed encoding, use what OCR actually returns. Use that encoding name to decode `/home/user/raw_logs.dat` in Python (`open(path,'rb').read().decode(<ocr_encoding>)`), then split into lines and each line on `|` into timestamp/ip_address/message. Deduplicate by computing `hashlib.md5((timestamp+ip_address).encode()).hexdigest()` per row and keeping only the first row seen per hash (use a seen-set, preserve original order). Extract `error_code` with a regex like `ErrCode:(\d+)` on the message field, and left-join against `/home/user/ip_map.csv` by ip_address to attach department. For serving, use Python's built-in `http.server` (or Flask) bound to 127.0.0.1:9090, implement `GET /api/threats` reading the `dept` query param, check the `Authorization` header equals `Bearer SOC-Analyst-2024` exactly and return 401 JSON/text if not, otherwise return the matching records as a JSON array with the five required keys. Launch the server as a background process (nohup/`&` or a daemon thread) so it stays up for the automated verifier to curl.