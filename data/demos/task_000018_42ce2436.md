Recover the hidden parameters before writing any classification logic. Transcribe `/app/lab_notes.wav` with a speech-to-text tool to get the exact optimization method name and the numeric spectral-peak threshold the task references — use those literal recovered values in your code rather than assuming defaults. Then build `/home/user/filter_signals.py` around `mpi4py`: have rank 0 list the `.npy` files in `<input_dir>` and split the list across ranks (e.g. slicing by `rank`/`size` or `comm.scatter`), so each rank processes a disjoint subset. Per file: `np.load`, take `np.abs(np.fft.fft(signal))` for the magnitude spectrum, smooth it (e.g. moving-average), then fit an exponential decay `A*exp(-B*f)+C` to the smoothed curve using `scipy.optimize.minimize` with the transcribed method name; subtract the fitted curve from the raw magnitude spectrum and take the max of the residual. Compare that max strictly against the recovered threshold to classify. Critically, avoid concurrent writes: have each rank return its local rejected filenames, `comm.gather` them at rank 0, and have only rank 0 write the combined, sorted list to the log file.
