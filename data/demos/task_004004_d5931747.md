First fix the build: run `bash /home/user/legacy_pipeline/build_helper.sh`, read the linker error carefully (an "undefined reference to `sqrt`/`pow`" pattern means the math library isn't linked), and add the missing `-lm` flag to the gcc invocation in the script (link flags must come after the object/source files on the command line). For the validator, treat `/app/core_engine` as a black box: generate a handful of your own JSON files with `"coordinates"` arrays containing normal values, then progressively more extreme ones (very large magnitudes, NaN, Infinity, high-precision decimals), and run `/app/core_engine <file>` under each, observing exit codes/segfaults (`echo $?`, or wrap with `ulimit -c unlimited` and inspect a core dump if needed) to bisect the exact numeric thresholds that crash it — don't guess a threshold, derive it from repeated trials narrowing the boundary (e.g. binary-search magnitude between a value that crashes and one that doesn't). Encode the discovered rules in `validator.py`: parse the JSON, reject (exit 1) if any coordinate's absolute value exceeds your discovered magnitude threshold, is NaN/Infinity, or has excessive decimal precision beyond what you found safe; otherwise exit 0. Validate your rules against several more self-generated crashing and non-crashing examples before finalizing.