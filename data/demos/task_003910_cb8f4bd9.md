First restore the pipeline before touching any binaries: read `/app/nginx.conf` and find the upstream/proxy_pass block, then point it at `127.0.0.1:5000` (the real Flask port); read `/app/.env` and correct the Redis port variable to `6379`. Apply both with `/app/restart_services.sh` and verify with a single test upload (`curl -F "file=@somefile" http://127.0.0.1:8080/upload`) before batch-processing — don't skip this smoke test, since 50 failed uploads waste time debugging blind. For each of the 50 ELF files: use `readelf -h <file>` (or `file`) to pull the Machine/architecture field, normalize spaces in that string to hyphens, then run `strip --strip-debug` (or `strip -s`) on the file in place to shrink it, then `mv` it to the new name `arch_<architecture_string>_<original_filename>` — strip before renaming so you don't lose track of originals. Loop over the renamed/stripped files and `curl -F "file=@$f" http://127.0.0.1:8080/upload` each one, checking HTTP response codes. Finally confirm all 50 land in `/home/user/artifacts/final/` and that the summed size is minimized (stripping, not recompression, is what gets you under the threshold).