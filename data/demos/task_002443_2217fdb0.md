Run `tesseract /app/policy.png stdout` (try `--psm 6` if the default segmentation garbles lines) to recover the exact rule text — don't guess at the policy from the JSON schema alone, since a naive attempt often invents plausible-sounding rules that don't match what's actually written. Read the OCR output carefully to identify the allowed node labels, allowed edge types, and the specific numeric/conditional constraints (e.g. a retention limit, and a cross-region backup rule with an exception tied to a boolean property on a related node). In `/home/user/validator.py`, parse the JSON into a node-id lookup and an edge list, then for each edge/node check every rule: reject unknown labels/edge types, reject any Backup node whose numeric field exceeds the OCR'd threshold, and for the cross-region rule, traverse from Database to its Service via the BELONGS_TO edge to check the `is_global` exception before flagging a BACKUPS_TO edge as a violation. Print exactly `VALID`/`INVALID` and exit 0/1. Test against both `/app/clean/` and `/app/evil/` to confirm perfect separation, paying attention to edge cases like exactly-at-threshold values.