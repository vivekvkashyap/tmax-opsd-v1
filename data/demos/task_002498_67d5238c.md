First recover the exact Cypher template text from the whiteboard image with OCR (`tesseract /app/schema_diagram.png stdout`), reading it carefully for the precise node labels, relationship type, property names, and quoting/formatting conventions shown — this determines your printf format string, so transcribe it exactly rather than paraphrasing. Then write build_queries.c to read stdin line-by-line (e.g. `scanf("%d,%31s", &id, issn)` or `fgets` + `sscanf`, sized generously since ISSNs can be up to 20 chars) in a loop until EOF, and for each parsed (id, issn) pair `printf` the query using the recovered template with the values substituted in, ending with a newline, and nothing else written to stdout (no prompts, no debug text, no trailing garbage on malformed/partial lines). Compile with `gcc -O2 build_queries.c -o /home/user/build_queries`. Since this will be checked against a hidden oracle via randomized fuzzing across many lines per run, test your program yourself with multi-line piped input (`printf "1234,1234567X\n42,ABC123\n" | ./build_queries`) and confirm formatting matches exactly for edge cases like minimum/maximum-length IDs and ISSNs before considering it done.