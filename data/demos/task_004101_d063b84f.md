Treat this as a numerical regression hunt, not a rewrite from scratch. Read every function in `/app/libdatatransform-1.0/src` and look specifically for any place a `double` value gets narrowed to a lower-precision type (e.g. an intermediate cast to `float`) before being stored back or fed into the next iteration of a stateful/recursive filter — that's the classic source of "silent precision loss" in an otherwise double-precision pipeline. Confirm your hypothesis with delta debugging: run the unmodified tool on a small crafted input, diff the output against hand-computed double-precision expected values, and see where the error magnitude jumps. Once you find the offending cast, remove it so every intermediate stays `double` end-to-end, without changing the algorithm's formula or control flow. Rebuild via the provided `Makefile` (don't hand-invoke gcc with different flags), then run the fixed binary on `/home/user/sensor_input.bin`, writing to `/home/user/sensor_output.bin`. Since the grader compares your output to a hidden reference via Mean Absolute Error with a very tight threshold, sanity-check by running your tool twice and confirming outputs are bit-identical (determinism), and verify the output file has exactly 10,000 doubles (same byte size as input).