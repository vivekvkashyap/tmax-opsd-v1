Model this as graph construction plus DFS/BFS cycle detection using only bash/awk/grep/sed. First, build the dependency edges from the CSV: for each transaction row with status `WAITING` on some resource R, find the row(s) where that same resource R has status `GRANTED` and read that row's transaction — that GRANTED transaction is the dependency target (WAITING-transaction depends on GRANTED-transaction). Use `awk -F,` to do these lookups rather than manual string parsing, since it handles the CSV fields cleanly. Second, implement the traversal iteratively or via a bash function with recursion, following depends-on edges from the given start transaction, keeping a visited list; stop as soon as you revisit a transaction already in your current path, and print the path from that repeat back to itself, closing with the start ID. Take care to only follow the WAITING→GRANTED-holder edge (a transaction may have multiple WAITING or GRANTED rows) and pick the one consistent with the WAITING resource. Write the discovered path in the exact `T_START -> T_NEXT -> ... -> T_START` format to `/home/user/deadlock_report.txt`, `chmod +x detect_cycle.sh`, and run it with `TX_994` as the argument, then manually trace the CSV by hand to confirm the printed cycle is actually correct before finishing.
