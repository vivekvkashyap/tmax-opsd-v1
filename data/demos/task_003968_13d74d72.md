First recover the tolerance: run OCR (install tesseract-ocr if missing) on `/app/params.png` to read the printed TOL value verbatim rather than guessing it. Then open `/home/user/adaptive_integrator.sh` and locate the broken step-size update inside the awk block; replace it with the specified formula `h_new = h_old * 0.9 * sqrt(TOL / err)`, using the OCR'd TOL as a constant passed into awk (e.g. via `-v TOL=...`). Clamp the resulting h to [1e-5, 0.5] immediately after computing h_new, before the next iteration uses it. Keep the existing err computation (full-step vs two-half-steps Euler) untouched, and only accept/advance the step when err is within tolerance — otherwise retry with the shrunk h. Since the grader fuzzes bit-exact equivalence against a Python reference, mirror the reference's control flow precisely: compute h_new every iteration (not only on rejection), guard against err==0 (avoid div-by-zero, e.g. treat as a tiny epsilon), and use awk's printf with %.5f for final rounding. Test with several manual (y0, t_end, h0) triples and hand-trace a few steps to confirm step count and final y agree with a from-scratch Python re-implementation of the same logic before trusting the fuzz harness.