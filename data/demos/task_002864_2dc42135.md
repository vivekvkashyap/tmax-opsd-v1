Reverse-engineer the CLI contract first: run `/app/legacy_score_predictor` with no/wrong arguments and inspect its stderr usage message (and cross-check with `strings /app/legacy_score_predictor`) to identify exactly which 3 of the 10 raw columns it needs and what cleaning each one implies (e.g. a currency-formatted column needs `$`/comma stripped and cast to float, a column with missing values needs imputation such as mean-fill, and a skewed column may need a log transform — infer the right treatment for each by inspecting the raw values in `/home/user/train_data.csv`, don't guess blindly). Build a small script that cleans the training data into those 3 features, then calls the binary once per row via subprocess (`subprocess.run([...], capture_output=True)`), parsing its stdout as the ground-truth label. With features + labels in hand, fit a simple regression model (linear regression is a safe first choice since scoring binaries like this are frequently linear combinations) using scikit-learn or numpy least squares. Apply the identical cleaning pipeline to `/home/user/test_data.csv`, predict with the trained model, and write one prediction per row, in original order, as a headerless single-column CSV to `/home/user/predictions.csv`. Sanity-check MSE against a held-out slice of your own training data before submitting.