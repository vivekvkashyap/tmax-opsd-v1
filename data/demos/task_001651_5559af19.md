Phase 1 — wire up the compose stack methodically and verify each hop before moving to the next: start Redis (`service redis-server start` or `redis-server --daemonize yes`) and confirm with `redis-cli ping`; launch the Flask app on port 5000 (`python3 /app/api.py &`) and confirm with a direct `curl localhost:5000/api/enqueue`; then edit `/etc/nginx/sites-available/default` to `listen 8080;` with a `location /api { proxy_pass http://127.0.0.1:5000/api; }` block, reload/restart nginx, and only then test the full chain via port 8080 — check `redis-cli lrange sensor_jobs 0 -1` after the curl to confirm the payload actually landed in the queue. Phase 2 — in `detector.c`, parse the CSV skipping the header line, load X/Y into arrays, then implement plain batch gradient descent updating `m` and `c` together each iteration using the MSE gradient (partial derivatives w.r.t. m and c, learning rate 0.01, 1000 iterations, no early stopping), compute final MSE over all points, and branch strictly on `mse > 5.0` for REJECT/exit 1 vs ACCEPT/exit 0 — test against a handful of clean/evil samples you construct yourself first to confirm the threshold behaves before trusting the hidden corpus.