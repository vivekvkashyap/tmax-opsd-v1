Don't trust file extensions at all — for every file in `/home/user/recovery/inbox/`, determine the true type with `file --mime-type -b <path>` (or `xxd`/magic-byte inspection) and map the MIME type to the correct extension per the given rules (image/png -> .png, application/pdf -> .pdf, text/plain -> .txt, application/gzip -> .gz). Preserve each file's original base name (strip the old extension, keep the stem) when copying/renaming into `/home/user/recovery/organized/`, and crucially preserve the original modification timestamp during the move (`cp -p` or `mv`, not a fresh `cp` that would reset mtime) — the timestamp comparison in the next step depends on it. For the incremental backup, compare each organized file's mtime against `/home/user/recovery/last_backup.stamp` using `find /home/user/recovery/organized -type f -newer /home/user/recovery/last_backup.stamp`, then `tar` those results into `/home/user/backup_incr.tar` using `-C` (cd into the organized dir first, or use `--transform`/`-C` plus bare filenames) so the archive contains only bare filenames with no leading path components. Verify with `tar -tf` that only the expected newer files appear and older ones are excluded.