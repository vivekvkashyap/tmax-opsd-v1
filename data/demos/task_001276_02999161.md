Fix `setup.py` by defining `Extension('_bucket', sources=['bucket.c'])` (module name must exactly match the `PyModuleDef`/`PyInit__bucket` name in bucket.c) and build in-place with `python3 setup.py build_ext --inplace` run from `/home/user/rate_limiter/`, then confirm `import _bucket` works from that directory. For the property test, install `pytest` and `hypothesis` first, then write a `@given(st.lists(st.integers(min_value=0, max_value=20), min_size=1, max_size=50))` test that constructs a fresh `TokenBucket(capacity=100, fill_rate=10)` per hypothesis example, loops over the elapsed-time list calling `bucket.consume(1, elapsed)`, and after every single consume call (not just at the end) asserts `0 <= bucket.get_tokens() <= 100` — checking only the final value would miss transient invariant violations that hypothesis is meant to shrink to. Run with `pytest -v test_limiter.py > test_report.log 2>&1` (or equivalent) so the exact pytest output including "passed" is captured in test_report.log.