Extract frames from `/app/feed.mp4` at 1 fps in a deterministic, ordered way (e.g. `cv2.VideoCapture` reading sequential frames, or `ffmpeg -vf fps=1` dumping numbered PNGs) so your 0-based frame index matches the source timeline exactly — don't hardcode which frames are black, compute average brightness per extracted frame (e.g. mean of grayscale pixel values) and flag any frame whose average is exactly/near 0 as an anomaly. Build `monitor.py` with `http.server`/`BaseHTTPRequestHandler` (or a minimal framework) exposing `/health` returning `{"status":"ok"}` and `/alerts` returning `{"black_frames": [...]}` computed at startup, binding to 127.0.0.1:8080. For `manage.sh`, implement `start` (spawn with `nohup ... &`, save `$!` to the pid file, redirect stdout/stderr), `stop` (read pid, `kill` it, then remove the pid file only after confirming termination), and `status` (check `kill -0 $(cat pidfile)` and exit 0/1 accordingly — the common bug is not exiting nonzero when the pid file is stale). `deploy.sh` should call stop (ignoring failure) then start, then poll `curl -sf http://127.0.0.1:8080/health` in a retry loop before exiting 0/1.
