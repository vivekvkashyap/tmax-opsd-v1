Treat this as four separate forensics sub-tasks; solve and verify each before moving on. (1) Video: extract frames from `/app/flight_dashboard.mp4` with `ffmpeg -i ... frame%d.png`, then for each frame read the pixel at (10,10) (e.g. with Python/PIL: `img.getpixel((10,10))`) and count frames where it's exactly `(255,0,0)`; write that integer alone to `/home/user/flash_count.txt`. (2) Recovery: mount `/app/drone_data.img` read-only or use a carving tool (`photorec`, `extundelete`, `debugfs` on the ext filesystem, or manual `grep`/`strings` + `dd` block carving) to recover the deleted `telemetry_raw.log` and save it byte-for-byte to `/home/user/telemetry_raw.log` — verify recovered content looks like intact log lines, not garbage. (3) Reconstruction: write `reconstruct.sh` that extracts the `SEQ:NNN` numeric ID embedded in each log line (e.g. with `grep -oP` or `awk`/`sed`), sorts lines numerically by that ID (`sort -t: -k2 -n` after isolating the key, or `sort` with a custom key extraction), and writes the reordered lines to `timeline.log`. (4) Decoder: reverse-engineer `/app/legacy_decoder` with `objdump`/`strings`/`ghidra` to confirm it downcasts a double to float before its arithmetic; write a replacement in any language that parses the 8-byte hex string as a double bit pattern (no intermediate float cast), performs the identical arithmetic in double precision, and prints in the same format — wrap it as an executable `/home/user/fixed_decoder`, then self-test by feeding it varied hex inputs and manually checking outputs are plausible double-precision results before trusting it.
