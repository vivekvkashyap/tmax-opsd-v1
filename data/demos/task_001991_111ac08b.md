Do the two independent extraction tasks first (SQL fix, OCR of the video sequence), then combine them last. For the SQL, inspect `/home/user/export_edges.sql`: the bug is an implicit cross join (`FROM nodes n1, nodes n2, links l` with no join conditions), which cartesian-products every node pair against every link row — rewrite it as a proper query that selects `source_id, target_id, distance` directly from the `links` table (joining to `nodes` only if you need names, which you don't here), with no extraneous cross product. For the video, extract one frame per second with ffmpeg (`-vf fps=1` or similar) since it's already 1fps for 5 seconds, then run `tesseract` on each frame image to OCR the large black digit — verify each OCR'd digit against the frame image visually/logically before trusting it, since OCR on a single digit can misread. This gives you an ordered sequence of 5 node IDs representing the traversed path. Finally, using your corrected edge list, sum the `distance` for each consecutive (node[i], node[i+1]) pair in the OCR'd sequence (matching direction as stored in `links`), and write just the total integer to `/home/user/total_distance.txt`.