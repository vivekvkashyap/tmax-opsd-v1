Use `git bisect` (ideally automated with `git bisect run`) instead of manually eyeballing commits. Write a small test script that, at each candidate commit, sources or greps `daemon.sh` and checks whether the cleanup conditional actually resets the `ITEMS` array once it reaches the documented threshold — e.g. simulate enough loop iterations (or statically inspect the comparison operator/value in the `if` guarding `ITEMS=()`) and exit non-zero ("bad") if the array is never cleared, zero ("good") if it is. Mark the known-good early commit as good and current `main` as bad, let bisect converge, then capture the first bad commit with `git rev-parse <sha>` and write it to `/home/user/leak_commit.txt` (no trailing whitespace beyond a newline). For the fix, checkout `main`, locate the same conditional, and correct the comparison so it reliably clears `ITEMS` whenever its length reaches the intended cap — don't restructure the script, just repair the boundary/logic condition and confirm by tracing a manual run that the array never grows past the threshold.
