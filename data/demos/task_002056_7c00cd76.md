Correlate the two logs by timestamp, not by request order: in worker.log, find the WARN "Memory threshold exceeded" / CRITICAL "OOM Killed" entries and note which request was "Processing..." at that time (the request whose processing never logged a "finished" line before the crash). Cross-reference that request_id against api.log to pull its exact `start` and `lr` values from the "Received parameters" line — copy them verbatim, don't guess. Write mre.py to import `compute_trajectory` from `app.processor` and call it with those two values. For the fix, inspect the gradient-descent loop in processor.py: it only breaks when `abs(step) < 1e-8`, but for a divergent trajectory `val` grows to inf then becomes nan, so that convergence check silently never fires (comparisons with nan are always False), and the loop runs to the huge MAX_ITER failsafe, appending millions of floats to `history` — that's the leak. Add an explicit divergence guard right after computing the new `val` (or at loop top) using `math.isinf(val)`/`math.isnan(val)` that breaks immediately and returns the history accumulated so far. Finally write report.txt with the request_id on line 1 and the lr value on line 2, matching what you extracted from the logs.
