Structure this as three distinct numerical stages in Go, each independently checkable. First, read `signal.csv` with `encoding/csv`, parsing `t` and `y` into `[]float64` slices. Second, compute a naive O(N^2) DFT yourself (loop over candidate frequencies k, sum `y[i]*cos(2*pi*k*t[i])` and the sine equivalent to get real/imaginary parts, then take the magnitude) across integer frequencies 1..20, and pick the `f` with the largest magnitude — don't forget you're searching over integer Hz values only, per the spec. Third, given the fixed integer `f`, fit `A` and `phi` by minimizing MSE between `A*cos(2*pi*f*t+phi)` and the data using gradient descent (or, since the model is linear in `A*cos(phi)` and `A*sin(phi)`, you can instead do a direct least-squares fit for those two linear coefficients and convert back to amplitude/phase via `A=sqrt(a^2+b^2)`, `phi=atan2(-b,a)` or similar — this avoids gradient-descent convergence issues and is more numerically reliable). Make sure `phi` is normalized into `(-pi, pi]`. Round `A` and `phi` to 2 decimals and write `{"f_peak": <int>, "A": <float>, "phi": <float>}` to `/home/user/result.json` using Go's `encoding/json`. Run the program and inspect the output file to confirm it's valid JSON with the right keys.