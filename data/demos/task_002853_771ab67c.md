Treat this like an SSH-silent-drop diagnosis: services start but a specific request path is silently rejected, so trace requests end-to-end instead of just reading configs. Install and start supervisord against `/home/user/restore_test/supervisord.conf`, then tail each service's stdout/stderr log under `logs/` to see which process is unhealthy. For the proxy, read `proxy.py` looking for a conditional that inspects incoming headers before forwarding to the backend on port 9000 — remove or relax that header-based gate so a plain `GET /ping` with no special headers succeeds, then confirm with `curl http://127.0.0.1:8080/ping`. For the mailer, grep `healthcheck.py` for the SMTP connection call and change the hardcoded privileged port to the mock SMTP server's actual listening port (8025), since port 25 needs root and will always fail unprivileged. Finally write `/home/user/restore_test/verify_restore.py` using `urllib`/`http.client` for the GET check and `smtplib` for the email, asserting the HTTP status/body and that the SMTP send doesn't raise, and only append the exact success marker string to `restore_status.txt` if both checks pass — run it yourself once and confirm the marker and mail log entry appear.