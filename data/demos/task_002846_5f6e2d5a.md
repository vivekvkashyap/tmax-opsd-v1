Start by reading `/home/user/app/nginx.conf` to find the exact upstream socket path it proxies to (likely `unix:/home/user/app/wrong_backend.sock` or similar) — the fix is to make the Go server listen on that exact path rather than changing nginx, since nginx is already running and reload is cheap. In main.go, before calling `net.Listen("unix", path)`, always `os.Remove(path)` first (ignore not-exist errors) since a stale socket file from a previous run will cause "address already in use". After listening, `os.Chmod` the socket (e.g. 0666) or use ACLs so the nginx worker user can connect — a common naive mistake is leaving default permissions that block cross-user access. For `/status`, implement a recursive `filepath.Walk` over `/home/user/app/data/` summing `info.Size()` only for `!info.IsDir()` regular files (don't count directory entries or symlinks incorrectly). Write deploy.sh to: build with `go build -o /home/user/app/server`, `pkill -f` or find+kill any existing running instance of that binary, launch the new one in the background with `nohup ... &`, then reload nginx. Test end-to-end with `curl http://127.0.0.1:8080/status` and confirm the JSON body before considering it done.