Diagnose the Makefile before rewriting the rest of the stack: inspect `libfastcrc-1.2/Makefile` for the compile line and check it actually passes `-shared -fPIC` (required to produce a loadable `.so`, not just an executable) and a strong optimization flag like `-O3` (the task explicitly warns latency is graded, so weak flags like `-O0` will fail the threshold even if functionally correct). Fix and rebuild with `make`, then confirm with `file libfastcrc.so` that it reports as a shared object. For the C++ side, write `/app/server.cpp` using `httplib.h`'s `Server` class listening on `127.0.0.1:9000`, read the raw POST body in the `/checksum` handler, call `compute_crc32c()` from the library, and return the integer as plain text — link with `-lfastcrc -L. ` and set `LD_LIBRARY_PATH` (or use rpath) in `start.sh` so it doesn't fail to find the `.so` at runtime, a common oversight. In `nginx.conf`, add `limit_req_zone $binary_remote_addr zone=req_limit_per_ip:10m rate=100r/s;` in the `http` block and `limit_req zone=req_limit_per_ip;` plus a method check (`if ($request_method != POST) { return 405; }`) and `client_max_body_size 5M;` in the `location /checksum` block. Test correctness and latency locally with `curl` and a timing loop before relying on the grader.
