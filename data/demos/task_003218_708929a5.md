Tackle the build failure first, then the concurrency bug. For the build: run `pip install -e /app/vendored/ujson-5.7.0` and read the actual compiler error output rather than guessing — it will point to a specific file and line inside the C source (likely under `python/`). Trace each reported error to its exact line and fix only the minimal typo (e.g., a misspelled header include or a misspelled macro name) rather than rewriting the file; recompile after each fix until `import ujson` succeeds and `ujson.loads()` works. For the race condition in `/home/user/log_collector.py`: the bug is that multiple multiprocessing workers call `open(path,'a').write(...)` concurrently, which is not atomic and interleaves/drops lines. Fix it either by having each worker return its parsed result to the main process (via the pool's return value) and let only the main process write sequentially, or by protecting each write with a `multiprocessing.Lock`/file lock shared across workers, writing one complete JSON line at a time. After fixing both, run the collector over all of `/app/raw_logs.txt` and confirm `/home/user/diagnostics.jsonl` has one valid JSON object per line for every input line — spot check by counting valid lines with `json.loads`.