First inspect the schema and data with `sqlite3 /home/user/backup_metadata.db ".schema"` and a few `SELECT *` queries so you understand parent/child links and storage tiers before writing logic. Filter to `status = 'success'` early (in the SQL WHERE clause or in code) — including 'failed'/'running' rows will pollute both the chain traversal and the anomaly detection. Use a recursive CTE (`WITH RECURSIVE chain(id, root_id) AS (SELECT id, id FROM backups WHERE parent_id IS NULL UNION ALL SELECT b.id, c.root_id FROM backups b JOIN chain c ON b.parent_id = c.id ...)`) to compute each backup's chain root, since the anomaly report requires `chain_root_id` for every flagged pair, not just the immediate parent. Then join each backup to its parent via `parent_id`, join both to `storage` on `backup_id`, and select rows where the child's tier is 'hot' and the parent's tier is 'cold'. Build the output as a Python/language-native dict matching the schema exactly (`{"anomalies": [...]}` with the three required string keys per item), and serialize with a JSON library rather than hand-built strings to guarantee valid JSON. Validate the result yourself against the given JSON Schema before finishing.
