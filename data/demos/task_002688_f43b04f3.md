Start by inspecting the actual data (`sqlite3 company.db "SELECT * FROM employees; SELECT * FROM projects;"`) so you can sanity-check your query's output by hand rather than trusting it blindly. Initialize the Go module with `go mod init` and `go get github.com/mattn/go-sqlite3` (needs CGO enabled). Fix the buggy query: keep the recursive CTE that walks `employees` from id=1 via `manager_id`, but replace the comma-join (`FROM subordinates s, projects p`) with an explicit `LEFT JOIN projects p ON p.employee_id = s.id` — the comma-join is a cross join producing every subordinate paired with every project, which is the actual bug. In the Go scan loop, use `sql.NullString` for the project name and coerce NULL to `""` before appending to results, matching the required schema. After fixing the query, create indexes explicitly: `CREATE INDEX idx_emp_manager ON employees(manager_id);` and `CREATE INDEX idx_proj_employee ON projects(employee_id);` via the `sqlite3` CLI (or from Go). Finally run `sqlite3 company.db "EXPLAIN QUERY PLAN <your query with id=1>"` and redirect to `plan.txt` — confirm it mentions SEARCH/SCAN so it reflects a real query plan, not placeholder text. Verify `results.json` by comparing it against your manual read of the employees/projects tables.
