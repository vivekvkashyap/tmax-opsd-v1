Work bottom-up through the pipeline layers. First install libgsl-dev (try `apt-get install -y libgsl-dev`, drop `sudo` if not needed) and confirm with `dpkg -s libgsl-dev`. Then fill in simulate.c: write a small PDB-line parser that checks columns for the atom-name field equal to exactly "CA" (PDB fixed-column format — don't just substring-match "CA" anywhere, since other atom names can contain those letters) and pulls X/Y/Z as floats from their fixed column ranges. Seed `gsl_rng_mt19937` with `atoi(argv[2])`, then loop: pick two distinct random CA-atom indices with `gsl_rng_uniform_int`, compute Euclidean distance, accumulate a running sum and a per-block-of-100 sum. Every 100th sample, compare this block's mean to the previous block's mean; if `fabs(diff) < 0.01`, print the overall running mean and exit. Compile with `gcc -o simulate simulate.c -lgsl -lgslcblas -lm`. In test_pipeline.py, shell out to run `./simulate 1xyz.pdb <seed>` for seeds 1..50, collect the 50 printed means, then implement bootstrap CI by resampling the 50-value array with replacement 1000 times, taking the mean of each resample, and reading off the 2.5th/97.5th percentiles of that distribution. Compare the overall mean to baseline.txt's expected value within the given tolerance and write regression_report.log in the exact specified format.
