First recover the mandatory tag policy from `/app/policy.png` using OCR (`tesseract /app/policy.png stdout`, installing it if needed) — read the extracted text carefully to get the exact key=value string required, don't guess at a plausible-looking tag. Write `/home/user/filter.cpp` using a JSON library (e.g. vendor/install `nlohmann/json` or write a minimal parser) that loads the file's `"tags"` array and exits 0 only if that exact recovered string is present as an element, exiting 1 on any parse error, missing tag, or mismatched value; compile with g++ to `/home/user/filter`. Validate your binary against `/app/corpus/` samples (both compliant and non-compliant) before relying on it. For `pipeline.sh`: compile the C++ tool, `mkdir -p /home/user/approved_deployments`, loop over `/app/corpus/raw/*.json`, run `/home/user/filter <file>` and on exit code 0 create a symlink (`ln -s`) with the original basename inside `approved_deployments/`. For `auto_deploy.exp`, use `spawn /app/fake_deployer /home/user/approved_deployments/`, `expect "Are you sure you want to deploy * files? (y/n): "`, then `send "y\r"` and let it finish. Test the adversarial classification, the pipeline's symlinks, and the expect script separately before combining.
