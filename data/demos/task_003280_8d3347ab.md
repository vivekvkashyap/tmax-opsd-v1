Approach this as a wrap-and-clean pipeline, not a binary rewrite. First experiment with `/app/legacy_etl` directly (`echo "..." | /app/legacy_etl`) to confirm it reads CSV lines from stdin and emits one JSON object per line to stdout, occasionally repeating previous lines — dedupe by tracking `record_id`s already seen and keeping only the first occurrence, in original stream order. For masking, use `re.sub` on the `ssn` field to replace exactly the first 5 digit characters with `*` while preserving the surrounding dashes/structure (test against the exact example in the task to get the character count right), and for `email` split on `@`, keep only the first character of the local part, append a single `*`, then rejoin with `@domain`. Build the HTTP service with Python's standard library (`http.server`) or Flask bound to exactly `127.0.0.1:9090`: `/health` returns a static 200 JSON body, and `/process` reads the raw request body, pipes it into the legacy binary via `subprocess.run(..., input=..., capture_output=True)`, applies dedupe+masking to each parsed JSON line, and returns the array as JSON. Run the server in the background and curl-test both endpoints before finishing.
