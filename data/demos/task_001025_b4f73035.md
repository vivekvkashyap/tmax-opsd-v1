Treat this as two orthogonal fixes to the existing script, not a rewrite. First, replace `solve_ode_euler` calls inside the log-likelihood with `scipy.integrate.solve_ivp(rhs, (t_data[0], t_data[-1]), y0, method='RK45', t_eval=t_data)`, where `rhs(t, y)` encodes the same ODE (`r*y*(1-y/K) - d*y`); wrap the call and check `sol.success` / for NaNs before computing the likelihood, returning `-inf` on failure just like the current guard does. Second, for parallelism, open a `multiprocessing.Pool(processes=4)` (guarded under `if __name__ == "__main__":`) and pass it via the `pool=` argument to `emcee.EnsembleSampler(nwalkers, ndim, log_probability, pool=pool)` — remember the log-probability function and any objects it references must be picklable, so keep the ODE function and data at module scope rather than as closures/lambdas. Keep walker count, step count, and burn-in exactly as specified, then compute per-parameter posterior means from the flattened post-burn-in chain and round to 3 decimals when writing the JSON. Verify by checking the chain has no NaNs and the acceptance fraction is reasonable before trusting the output.
