Build the three programs in dependency order and validate the physics/numerics at each step rather than trusting the final number blindly. In `heat.c`: compute `dx = 1.0/N`, `dt = dx*dx/(4*alpha)` exactly as specified (note this makes the diffusion coefficient `alpha*dt/dx^2` a fixed constant, so the update simplifies algebraically — derive that simplified coefficient yourself as a sanity check on your formula), initialize `u[i] = sin(pi*x_i)`, iterate the FTCS update for K=T/dt steps using a double-buffered array (never update in place, since each new value depends on old neighbor values), enforce `u[0]=u[N]=0` every step, then compute the analytical solution `exp(-alpha*pi^2*T)*sin(pi*x_i)` at each point and print the max absolute difference. In `regression.c`: read (N, error) pairs from stdin until EOF, take natural logs of both columns, and fit OLS by computing means and covariance/variance sums directly (`slope = sum((x-xbar)(y-ybar)) / sum((x-xbar)^2)`), printing `Slope: %.3f\n`. In `run_simulation.sh`: compile both with `-lm`, loop N over 10/20/40/80 capturing each error via command substitution, feed `N error` pairs (newline-separated) into the regression binary's stdin, and redirect its stdout into `/home/user/convergence_rate.txt`. Since FTCS is second-order accurate in space, verify your own error values shrink by roughly 4x each time N doubles before trusting the final slope.