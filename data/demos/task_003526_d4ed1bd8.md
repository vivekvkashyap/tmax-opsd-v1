Treat this as three independent, testable pieces rather than one monolithic edit. First, implement storage-quota check as an early-return guard at the very top of `main()` in `operator.py`: walk `/home/user/data/` recursively (`os.walk` + `os.path.getsize` on each file), sum bytes, and if strictly greater than 1024000 write "STORAGE_QUOTA_EXCEEDED" to the log and `sys.exit(2)` before touching manifests — order matters, this must happen before any manifest parsing. Second, implement dependency ordering as a proper topological sort (Kahn's algorithm or DFS with a visiting/visited/in-progress coloring) over the `depends_on` lists; if you detect a back-edge/cycle during traversal, write "CIRCULAR_DEPENDENCY" to the log and exit 1 instead of generating the launcher — a naive attempt that just sorts by declared order or ignores cycles will fail the circular-dependency test. When writing `launcher.sh`, emit the "Starting <name>" echo to `start_order.txt` immediately before each service's `command &`, followed by `sleep 1`. Third, just add the `webhook.json` manifest exactly as specified with its `depends_on: ["backend"]`. Finally chmod +x the operator, run it, then run the generated launcher, and confirm `start_order.txt` reflects a valid topological order (db before backend before frontend/webhook).