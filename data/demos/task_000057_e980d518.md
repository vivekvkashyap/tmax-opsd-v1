Write `/home/user/deploy_update.sh` so every step is idempotent by construction — check-then-append rather than blindly appending. For the fstab entry, use `grep -qxF "UUID=99A1-B2C3 /home/user/app_backup ext4 defaults 0 2" /home/user/mock_etc/fstab || echo "..." >> /home/user/mock_etc/fstab` (exact-line match with `-F` to avoid regex-metacharacter surprises), and the same `grep -qxF ... || echo ... >>` pattern for the firewall rule. Create `/home/user/app_data` with `mkdir -p` (safe to re-run) then `chmod 700` it explicitly every run so permissions stay correct even if something else changed them. For the log file, decide whether to truncate-and-rewrite each run or only append-if-missing each of the three `[SUCCESS] ...` lines — truncating (`> /home/user/deploy.log` at script start, then appending each success line as you complete that step) is simplest and guarantees exactly three lines with no duplicates across repeated runs. `chmod +x` the script and test correctness by running it two or three times in a row, then diffing `fstab`/`firewall.rules`/`deploy.log` to confirm no duplicate lines appear.
