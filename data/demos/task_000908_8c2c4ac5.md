In `/home/user/emitter.c`, replace the hardcoded `"health.log"` path with logic that calls `getenv("METRICS_OUT_DIR")`, checks for NULL (print an error to `stderr` via `fprintf(stderr, ...)` and `exit(1)` if unset), then builds the full path by concatenating the directory value with `"/health.log"` (use a fixed-size buffer with `snprintf` to avoid overflow) before opening it in append mode and writing `"status=OK\n"`. For `/home/user/setup_observability.sh`, make it safe to re-run: compile the emitter unconditionally (recompiling is harmless), create the metrics directory with `mkdir -p` (naturally idempotent), and when generating `/home/user/run_emitter.sh` overwrite the file fresh each time (`cat > file <<EOF ... EOF`) rather than appending — appending is the classic idempotency bug that causes duplicate `export`/exec lines to accumulate on repeated runs. Mark both `/home/user/emitter` and `/home/user/run_emitter.sh` executable via `chmod +x`. Test the full flow manually: run setup twice in a row and confirm the wrapper script's line count doesn't grow, then unset `METRICS_OUT_DIR` and directly invoke `./emitter` to confirm it fails with a non-zero exit code before finishing.
