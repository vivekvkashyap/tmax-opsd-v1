Write the C++ program methodically in pieces you can verify independently. First, GC content per sequence = count('G')+count('C') over sequence length (use exact character counts, not case-insensitive unless the data is uppercase already — check the files). Compute sample mean and sample variance with N-1 in the denominator for each group separately. For Welch's t-test, use t = (MeanA - MeanB) / sqrt(varA/nA + varB/nB) exactly in that numerator order — sign matters since the expected output can be negative. For the Jukes-Cantor step, first compute d = |MeanA - MeanB|, then invert the given formula d = -(3/4)ln(1 - (4/3)p) algebraically to solve for p directly (p = (3/4)(1 - e^(-4d/3))) rather than doing unstable numerical root-finding — it's a closed-form solve. Watch for the formula being undefined/complex if 1-(4/3)p would go negative; with the given tiny sequences this shouldn't occur but log intermediate mean/variance values while debugging. Round both outputs to 4 decimal places with fixed-precision formatting, write exactly the two labeled lines to results.txt, then compile with `g++ -std=c++17` and run it, and manually recompute the numbers by hand/calculator to cross-check before considering it done.
