Build this pipeline in stages and test each independently before wiring them together. First, load `/app/data/support_call.wav` with a Hugging Face `pipeline("automatic-speech-recognition", model="openai/whisper-tiny")` (resample to 16kHz if needed via `librosa`/`soundfile`) and print the transcript to confirm it looks sane before moving on. Second, chunk the transcript using `re.split(r'(?<=[.?])\s+', transcript)`, strip whitespace, and drop empty strings — verify the chunk count/content manually. Third, embed each chunk with `sentence-transformers/all-MiniLM-L6-v2`, build `faiss.IndexFlatL2(dim)`, add embeddings as float32, and persist with `faiss.write_index` to `/home/user/faiss_index.bin` plus `json.dump` the chunk list to `/home/user/chunks.json`. Finally write a FastAPI/Flask app bound to `127.0.0.1:8080` with a `POST /search` route that first checks the raw `Authorization` header equals exactly `Bearer etl-secure-token-2024` (return 401 otherwise), then embeds the query with the same model, does `index.search(q_emb, k=1)`, and returns `{"best_match": chunks[idx]}`. Start the server as a background process (e.g. `nohup ... &` or uvicorn in a subprocess) and confirm with `curl` that it's actually listening before finishing.