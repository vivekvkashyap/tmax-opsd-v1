Debug this like a real crash triage: compile the existing C source first, run it under a debugger/with sanitizers (`gcc -g -fsanitize=address,undefined`) against `/app/corrupted_telemetry.wav` to actually observe where and why it crashes, rather than guessing. Expect two distinct classes of bug per the task description, and fix them separately: (1) the sample-read loop lacks NaN/Inf validation — after reading or converting each frame's floating-point representation, add explicit `isnan()`/`isinf()` checks (from `<math.h>`) and replace any bad value with `0.0` before it propagates into the filter, since a single NaN in an IIR recursive filter poisons all subsequent output; (2) the IIR filter's accumulator and coefficients are declared as single-precision `float`, causing precision drift/divergence over many samples — change the accumulator variable and any filter coefficient variables it interacts with to `double`, keeping I/O sample conversion at 16-bit PCM but doing internal math in double precision. Recompile, rerun with the two real CLI args (input wav, output wav), and confirm the output is a valid 16-bit PCM WAV at the same sample rate as the input before comparing to the reference via MSE.