Start by extracting UDP/8888 payloads from the pcap: use `tshark -r /home/user/traffic.pcap -Y "udp.port==8888" -T fields -e data` or a small `scapy` script (`rdpcap`, filter `UDP` with `dport==8888`, decode `bytes(pkt[Raw]).decode()` as JSON) — install tools via `sudo apt-get install tshark` or `pip install scapy` if missing. Parse each payload's JSON `value` field in packet order and note the last one before the capture ends — that is the value that triggered the infinite loop. Then open `/home/user/math_service.py`, find `compute_collatz(n)`, and add a guard at the top: if `n <= 0`, return `[]` immediately, before the `while n != 1` loop runs (the bug is that non-positive n never reaches 1, looping forever). Run `python3 /home/user/validate.py` and confirm it exits 0 with no AssertionError — fix any remaining logic if it fails. Finally write exactly two lines to `/home/user/incident_report.txt`: the discovered poison-pill integer on line 1, and the literal word `SUCCESS` on line 2 — no extra whitespace or blank lines.
