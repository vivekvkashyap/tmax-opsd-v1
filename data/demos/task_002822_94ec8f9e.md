Start with static+dynamic analysis of the stripped binary rather than guessing: run `strings /app/log_hasher` to look for format hints, then `objdump -d /app/log_hasher` (or a disassembler/decompiler) focused on the hashing loop — look for a small state variable initialized to a constant, updated per input byte with bitwise rotate/shift and XOR operations, since that's the typical shape of a lightweight streaming checksum. Confirm your hypothesis empirically: create a few small known-content test files, run `/app/log_hasher <file>` on each, and check whether your from-scratch Python re-implementation reproduces the same hex output byte-for-byte before trusting the disassembly reading. Separately, grep /var/log/upload_events.log for suspicious filenames (traversal sequences, shell metacharacters) to understand what a real attacker payload looks like, and use that to design your input-sanitization allowlist (reject `..`, `/`, `;`, `|`, backticks, etc., printing `ERROR` on any hit) — do NOT shell out to `cat`/`system()` at all in your secure version; read the file directly in Python with `open()` after validating the filename resolves inside an intended directory. Optimize the hashing loop (e.g. read in large chunks, avoid per-byte Python-level overhead via `array`/`struct`/bytearray operations) to keep runtime within the required 5x-of-C threshold.
