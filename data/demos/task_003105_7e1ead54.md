Two independent extraction problems, don't conflate them. First, OCR the threat intel image: `tesseract /app/threat_intel.png stdout` (upscale/threshold first if needed for cleaner OCR), then parse out the CIDR subnet strings from the recovered text — verify each looks like a valid CIDR before trusting it (regex for `\d+\.\d+\.\d+\.\d+/\d+`). Second, for each ELF in `/app/binaries/`, extract embedded IPv4s: run `strings` (or read raw bytes and regex for the IPv4 pattern directly, since `strings` may miss binary-embedded values that aren't null-terminated ASCII runs cleanly) over the whole file rather than just `.rodata`, since section parsing may be unreliable on stripped/odd binaries. For each candidate IP found, use Python's `ipaddress` module (`ipaddress.ip_address(ip) in ipaddress.ip_network(subnet)`) to test membership against every malicious subnet — don't do string-prefix matching, which breaks on subnet boundaries. Flag a binary if ANY of its extracted IPs falls in ANY malicious subnet; write flagged basenames to `/home/user/flagged.json`. Since scoring is F1-based, tune for both precision and recall — validate your IP-extraction regex isn't grabbing false positives like version numbers or matching too narrowly to miss real IPs. Also emit `/home/user/block.sh` with one `iptables -A OUTPUT -d <IP> -j DROP` line per confirmed malicious IP found.