Extract the hashing rule by listening to (or transcribing) `/app/artifact_routes.wav` first — do not guess the formula; write down exactly what algorithm is described (it involves summing something derived from the path characters and reducing it by a modulus) before coding it. Separately, rewrite `router.cpp` to be memory-safe: replace fixed-size stack buffers with `std::string`/`std::string_view` for the path and use a `std::vector<std::pair<std::string,std::string>>` or `std::map` instead of a manually-managed linked list for query parameters, so there's no manual `new`/`free` bookkeeping to leak. Parse by finding the first `?`, splitting params on `&` then each pair on `=`. Implement the hash exactly per the audio instructions, applied to the parsed path string. Match the required output format precisely: `Path: `, `Hash: `, and `Params: key=value;key=value;` lines, with `Params: none` when there are no parameters. Build with `-fsanitize=address,undefined` first and fuzz it yourself with long/malformed/empty-query URLs to confirm zero leaks and no UB before compiling the final optimized binary to `/home/user/router_fixed`, since it will be fuzz-tested against a hidden oracle for bit-exact output and memory safety.