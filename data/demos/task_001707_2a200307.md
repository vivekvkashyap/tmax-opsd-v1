Set up a Cargo project under /home/user/metrics_processor with dependencies for JSON parsing (serde_json), parallelism (rayon), and Unicode normalization (the `unicode-normalization` crate). Read /home/user/data/raw_metrics.jsonl line by line, and use rayon's parallel iterators (e.g. `par_iter`) to parse each JSON line and process the `id` field concurrently: first un-escape literal `\uXXXX` sequences into real Unicode code points (careful — this is a *literal* backslash-u sequence embedded in the JSON string value, not the JSON decoder's own escape handling, so you likely need a manual/regex-based unescape pass after JSON parsing, since json string that already contains an escaped backslash decodes to a literal `\uXXXX` substring), then apply NFKC normalization via `.nfkc().collect::<String>()`. After normalizing, group records by the normalized id, sort each group by timestamp ascending, and compute the trailing 3-point simple moving average (average of however many of the last up-to-3 measurements are available, per the recursive rule given). Write results sorted by (normalized_id, timestamp) to /home/user/data/normalized_metrics.csv with the exact header and 2-decimal-place formatting (e.g. `format!("{:.2}", value)`) for measurement and rolling_sma. Watch for distinct Unicode inputs (composed vs decomposed forms, fullwidth vs halfwidth) that should normalize to the same NFKC output and therefore must be grouped together.