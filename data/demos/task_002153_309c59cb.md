In the C program, read `raw_metrics.csv` line by line preserving original row order, and track a "last seen value" per unique (`locale`,`string_id`) pair — a hash map or small growable array of structs keyed by the concatenation of locale+string_id works well since the key set isn't huge. For each row, if `access_count` is present, output it and update the tracked last value for that pair; if it's missing (field is an empty string between commas — watch for this vs. a missing trailing field), output the pair's last tracked value, or `0` if this pair has never had a value yet (LOCF must be tracked independently per locale+string_id combination, not globally). Write cleaned rows to `/home/user/clean_metrics.csv` in the same 4-column format, one row per input row, same order. For the wrapper `/home/user/run_pipeline.sh`: append the `[INFO]` log line before invoking `gcc` to compile the C source into `./impute_loc`, run it, and append `[SUCCESS]` only if it exits 0; on any compile or runtime failure append `[ERROR] Pipeline failed` and `exit` non-zero instead of continuing. Make the script executable with `chmod +x`.