Install and run `tesseract` (or another OCR tool) on `/app/manifest.png` to recover the raw manifest lines yourself — don't assume the text, actually extract it and inspect the result since it will contain OCR noise/typos. Split into lines, drop empty ones, and build a normalized form (lowercase, strip punctuation/quantity prefixes like "1x") for matching, while keeping the original raw line for `ocr_text` in the output. Load `/app/catalog.csv` and, for each normalized OCR line, score it against every `product_name` using a fuzzy string metric (Levenshtein ratio, Jaccard on tokens, or difflib's `SequenceMatcher`) and pick the argmax — a naive exact-match or substring approach will fail on the typo'd lines. Write `/home/user/matches.json` as a list of `{"ocr_text":..., "matched_product_id":...}` objects in OCR-line order, and `/home/user/report.txt` with one line per match using the exact template `Match: [{product_id}] {normalized_catalog_name} <- {raw_ocr_text}`. Since scoring only requires 3/4 correct, don't over-engineer — but do sanity-check each match against the catalog by eye before finishing.
