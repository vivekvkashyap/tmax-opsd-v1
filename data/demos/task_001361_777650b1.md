Start by diagnosing the simdjson build failure before touching your pipeline code: `grep -n CMAKE_CXX_STANDARD /app/simdjson/CMakeLists.txt` — simdjson requires C++17, so if it's pinned lower, edit it to 17 (or 20), then `cmake -B build -S /app/simdjson && cmake --build build -j && sudo cmake --install build`, and confirm `<simdjson.h>` is findable and `-lsimdjson` links in a trivial test program before writing the real pipeline. For the pipeline itself: load `server_meta.csv` into a hash map (server_id -> {tier, max_changes_per_min}) first, then stream-parse `config_changes.jsonl` line by line with simdjson's dom/ondemand parser, applying the three validation gates in order (missing required fields, timestamp < 1700000000, unknown server_id) and dropping any line that fails any gate — don't silently continue processing malformed lines. Bucket valid records into tumbling windows via `window_start = (timestamp / 60) * 60` using integer division, accumulate per-(server_id, window_start) counts, then flag only where count strictly exceeds that server's max_changes_per_min from the CSV join. Write anomalies.csv with the exact header, then sanity-check a few rows by hand against the raw JSONL before trusting the F1 score.