Split this into two independent tracks and verify each before combining. Track A (algorithm): implement the C program to read V, E, then E edge pairs, then V weights from stdin; build a degree array by incrementing both endpoints for each edge; sum the weights of vertices with degree >= 2, divide by their count using integer (truncating) division, print that integer plus a newline, or "0" if no such vertices exist — the worked example in the task (4 vertices, 3 edges, weights 10 20 30 40 -> answer 20) is a good sanity check for your own build before submitting. Because the grader is a fuzz-equivalence test against a hidden oracle, write a handful of your own edge cases (V=1 with E=0, all vertices degree 0, ties in degree exactly 2) and eyeball that your logic matches the stated spec exactly, especially the floor-division and the "no such vertices" case. Compile to `/app/bin/mol_analyze`. Track B (integration): fix `/app/services/config.json` so `redis_port` matches the actual Redis port (6379, not 6380), start `redis-server` and `python3 /app/services/api.py` in the background, then hit `curl -X POST http://localhost:5000/trigger_test` and confirm `/home/user/integration_output.txt` is populated — this only works once your compiled binary at `/app/bin/mol_analyze` exists and matches the API's static test input.
