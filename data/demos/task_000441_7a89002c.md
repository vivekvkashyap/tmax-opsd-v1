Work through the three layers independently and verify each before moving on. First, diagnose the vendored `liblbfgs-1.10` build failure by actually reading the compiler error output — a naive attempt guesses at the fix; instead run `./configure && make` first, note the specific error (missing header causing an implicit-declaration failure, or an over-strict `-Werror` flag in the generated `Makefile`/`CFLAGS`), fix the source/build-file directly, then `./configure --prefix=/app/local && make && make install` so the static lib lands under `/app/local`. Second, fix `/app/project/Makefile` to add `-I/app/local/include` and link `/app/local/lib/liblbfgs.a` (plus `-lm` if needed). Third, in `main.c`, sanitize `sensor_data.csv` values by checking each parsed float with `isnan()`/`isinf()` and zeroing bad ones before they reach the optimizer — don't just filter lines, since a bad token mid-row still needs replacing. Finally, carefully re-derive the gradient of the Rosenbrock-like cost by hand from the objective function rather than trusting the existing code, compare term-by-term against the correct analytic gradient to spot the sign/missing-term bug in `evaluate()`, and confirm convergence by rerunning until `result.txt`'s "Final cost" is very small.