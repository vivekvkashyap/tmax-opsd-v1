Break this into a data-extraction stage and a serving stage, and validate each before combining. Use `ffmpeg -i /app/fluor_seq.mp4 frame_%03d.pgm` (grayscale PGM is easy to parse in C without a codec library) to dump all 50 frames, then write a small C routine that scans each frame's pixel buffer for the maximum-intensity pixel and records its (x, y) — sanity-check on a couple of frames by eye/inspection that you're finding a single bright dot, not noise. Compute per-step squared displacement between consecutive frame centroids (Δt=1 always), which gives you 49 samples to feed the sampler. For the MCMC step, implement Metropolis-Hastings with a symmetric proposal on D (e.g. Gaussian random-walk step in log-D or clipped to the [0.1, 100] prior), accept/reject using the log-likelihood ratio built from the 2D-diffusion Rayleigh-like density (avoid computing the un-normalized product literally — sum log-likelihoods across the 49 displacements to prevent underflow), run well past the requested burn-in, and report the posterior mean/std over the retained samples formatted to 2 decimal places. For serving, use raw POSIX sockets (`socket`/`bind`/`listen`/`accept`) to build a minimal HTTP/1.1 responder: parse just enough of the request line to detect `GET /posterior`, and always return a well-formed `200 OK` response with `Content-Length` and the exact JSON shape, keeping the server process alive in the background (fork/daemonize or run under nohup) so `curl` can hit it after your script exits.