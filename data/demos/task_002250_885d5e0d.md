Treat `/app/oracle_processor` as a black-box spec to reverse-engineer via probing, since your script must match it byte-for-byte under fuzzing — feed it small hand-crafted transcripts (well-formed lines, malformed tags, out-of-order timestamps, nested/duplicate tags, background-noise tags) and diff its stdout against what a naive implementation would produce to infer the exact filtering/sorting/dedup rules (e.g., which tags count as noise and get dropped, the exact sort key, and the output format spacing/delimiters) before writing your own logic. For `event_parser.c`, fix only the memory-safety bugs (bounds-check array/buffer writes for deeply nested tags, pair every `malloc` with exactly one `free`, avoid use-after-free) without altering the state-machine's parsing decisions — a common mistake is "fixing" bugs in a way that changes observable behavior; test with valgrind or ASan on nested/malformed inputs to confirm no crashes and unchanged output on well-formed ones. Then build `process_audio.sh` as a pipeline that runs input through your parser, applies the sort/merge/filter logic you inferred, and produces output; validate it against the oracle on many varied synthetic inputs (including edge cases) yourself before considering the task done, since fuzzing is exact-match over thousands of cases.