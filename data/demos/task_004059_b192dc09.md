Use `git bisect run` with an automated test script so the bisection is exact and reproducible: write a small script that pipes the current commit's generated payload (via whatever bash script in the repo serializes it) into `/app/log_parser` and checks the exit status/signal — a segfault (exit code 139, or a SIGSEGV) means "bad", clean exit means "good". Run `git bisect start HEAD HEAD~199` then `git bisect run <your-script>` to let git converge automatically rather than testing manually; capture the resulting commit's full SHA with `git rev-parse HEAD` (after bisect finishes, `git bisect reset` and use the identified commit) into `/home/user/bad_commit.txt` — note the task wants the FULL 40-char hash, not short. To find the encoding anomaly, diff the payload-generation logic between the good and bad commit and/or hexdump sample outputs from each — look for a specific byte sequence appearing in bad output that's absent from good output (a TLV-style parser like this often crashes on an unescaped magic/sentinel byte pattern inside a value field). Once you know that exact byte sequence, `/home/user/filter.sh` should take a file path argument, use `grep -a -P` (Perl-mode, binary-safe) or `hexdump`/`xxd` piped to `grep` to test for that sequence, exiting 1 with no output if found (evil) or `cat`-ing the file verbatim and exiting 0 if not (clean). Validate against constructed clean/evil sample files before trusting it against the hidden corpus.