Implement brute-force grid search with awk doing the numeric heavy lifting, since bash/bc looping over ~2500 (m,b) pairs with per-point bc calls is slow and error-prone. Read `/home/user/data.csv` once into an awk array (or pass it as a file argument to awk), then use nested awk `for` loops over m from 0.0 to 5.0 step 0.1 and b from 0.0 to 5.0 step 0.1, computing SSE = sum((y - (m*x+b))^2) for every data row inside the innermost loop, tracking the minimum SSE and its (m,b) as you go. Beware floating point step accumulation (0.1 added repeatedly drifts) — instead loop over integer counters i=0..50 and compute m=i/10.0, b=j/10.0 to keep values exact. After finding the optimum, format the single output line with awk's printf using `%.2f` for m, b, and SSE, and write it directly to `/home/user/optimal_params.txt` — do not round only for display, round the value itself so the required exact string is produced. Test manually by computing expected SSE for a couple of (m,b) pairs by hand before trusting the grid search.