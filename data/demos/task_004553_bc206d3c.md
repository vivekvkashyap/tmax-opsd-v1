First recover the rejection rule from the image rather than guessing: run `tesseract /app/divergence_note.png stdout` (try `--psm` variants if the first pass is noisy) and read the resulting text carefully to find the target k-mer and the numeric threshold it specifies, including whether the comparison is strict (`>`) or inclusive (`>=`). Then write `/home/user/detector.sh` so it takes the FASTA path as `$1`, strips header lines (`grep -v '^>'`), concatenates the remaining sequence lines into one string (`tr -d '\n'`), and counts *overlapping* occurrences of the k-mer — a naive `grep -o` count only finds non-overlapping matches, which will misclassify sequences where the k-mer repeats with overlap (e.g. use a sliding-window loop or `grep -oP '(?=KMER)'` to count overlapping hits). Compare the count against the recovered threshold using the exact comparison operator from the note and `exit 0` for clean vs `exit 1` for diverged. Test your script against a handful of self-constructed edge-case sequences (exactly at the threshold, one below, one above, with overlaps) before considering it done, then `chmod +x` it.
