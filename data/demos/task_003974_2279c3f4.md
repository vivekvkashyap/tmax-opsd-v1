Treat this as a graph-aggregation problem: load both CSVs (pandas or plain csv module), build a `backup_id -> parent_id` map from dependencies.csv and a `backup_id -> size_mb` map from backups.csv. For each backup, compute its total restoration cost by walking up the parent chain (a simple loop or memoized recursion) summing `size_mb` at each hop until you reach a node with no parent (a FULL backup) — memoize results so shared ancestor chains aren't recomputed. Watch for two common mistakes: (1) forgetting to include the backup's own size in the sum, and (2) not memoizing, which is fine at this scale but matters if you generalize. Once you have `total_restore_size_mb` per backup_id, implement dense ranking yourself (don't assume a library gives ties the way you want): sort by size descending, and when sizes tie, break the tie by ascending alphabetical backup_id — rank increments only when the size value changes (dense rank, not standard competition rank). Write the final CSV with header `backup_id,total_restore_size_mb,cost_rank`, rows ordered by cost_rank ascending, to `/home/user/restore_costs.csv`.
