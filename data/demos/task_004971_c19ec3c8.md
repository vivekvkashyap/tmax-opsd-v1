Extract the exact algorithm from the scanned image before writing any code: OCR `/app/format_spec.png` (e.g. `tesseract /app/format_spec.png -`) and read every detail of the run-length scheme carefully — specifically how the control-byte's high bit distinguishes "repeat" vs "literal" chunks, how many bits encode the count, whether the count is stored as-is or offset by one, and how many data bytes follow in each case. Implement `custom_decompress.py` reading raw bytes from `sys.stdin.buffer` and writing raw bytes to `sys.stdout.buffer` (never `sys.stdin`/`stdout` text streams, which mangle binary data on some platforms) — loop reading one control byte at a time, checking its high bit to branch between the two chunk types, and stop cleanly when a `read(1)` returns empty (EOF) rather than raising or hanging. Since verification is bit-exact fuzz equivalence against a hidden oracle across many random binary streams, test your own implementation by hand-round-tripping a few crafted byte sequences (both chunk types, edge cases like max count and single-byte chunks) before considering it correct — off-by-one errors in the count field are the most common failure mode here.