Do this entirely with `awk` in one or two passes over `/home/user/observational_data.csv`, using `-F,` and skipping the header line (`NR>1`). First filter: a row qualifies only if the Sequence field matches `^ATGC` at the start AND `CGTA$` at the end (use `substr()` or a regex like `$2 ~ /^ATGC.*CGTA$/`) AND the Concentration field is strictly greater than 10.0 (`$4>10.0` as a numeric comparison, not string). For each qualifying row, compute GC content X by counting G/C characters in the sequence (e.g. `gsub(/[^GC]/,"",tmp)` on a copy of the sequence, then `length(tmp)/length(seq)`) and pair it with Y = Binding_Affinity. Accumulate running sums needed for OLS: n, sum(X), sum(Y), sum(X*Y), sum(X*X) as you go — do not try to store full arrays if avoidable, though for n~100 rows arrays are fine too. After the loop (in an `END` block), compute slope = (n*sum(XY) - sum(X)*sum(Y)) / (n*sum(XX) - sum(X)^2) and intercept = (sum(Y) - slope*sum(X)) / n — this is the standard OLS closed form, equivalent to using means. Format all three output lines with `printf "%.4f\n"` for the numeric values (count is an integer) and write them in the exact required format to `/home/user/regression_results.txt`. Watch for two common mistakes: comparing Concentration as a string instead of numerically, and using `awk`'s default floating point rounding (`%f`) instead of matching the exact 4-decimal rounding convention.
