Build this as a straightforward pandas pipeline, but get the two subtle transformations exactly right. For `non_ascii_count`, count characters whose ordinal is >= 128 (i.e. `sum(1 for c in text if ord(c) > 127)`) — remember multi-byte scripts and emoji each contribute per-codepoint, not per-byte, so decode the CSV as UTF-8 and count Python string characters, not raw bytes. For `rolling_avg_score`, sort by `date` ascending within each `entity_id` group, then use a rolling window of size 3 with `min_periods=1` (so early rows average over however many prior rows exist, not NaN) via `groupby('entity_id')['daily_score'].rolling(3, min_periods=1).mean()`, then round to 2 decimals. Write only the four required columns to `/home/user/cleaned_data.csv` in the correct dtype. Load into SQLite either with `pandas.to_sql` (specifying dtypes) or the `sqlite3` CLI's `.import`, and manually inspect a few rows against your own recomputation before finishing to catch off-by-one window or encoding mistakes.
