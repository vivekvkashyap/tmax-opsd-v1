Work top-down: dependencies, codegen, translation, tests. Install `grpcio-tools protobuf pytest hypothesis`, then compile the proto with `python -m grpc_tools.protoc -I/home/user --python_out=/home/user /home/user/build_graph.proto` to produce `build_graph_pb2.py`. Read `/home/user/legacy_solver.rb` closely: it's Kahn's algorithm for topological sort, but the tie-breaking detail matters — it maintains the ready queue as a sorted list and always pops the alphabetically-first ready node next (not just sorting once at the start), so newly-freed nodes get merged into sorted order each step; replicate this exactly with e.g. a Python list you `sort()` after each insertion, or a heap. In `/home/user/solver.py`, implement `resolve_build_order(graph_pb)` reading `Module.name`/`depends_on` from the protobuf, building in-degree and adjacency maps the same way the Ruby does, and raising `ValueError("Cycle detected")` if not all nodes get emitted. For `/home/user/test_solver.py`, use Hypothesis strategies to generate random DAGs (build nodes then only add edges that don't create cycles, e.g. by assigning each node a random topological rank and only allowing dependencies on lower-rank nodes), decorate with `@given`, and assert every dependency appears before its dependent in the output plus that output length matches input. Run pytest and redirect output to `/home/user/test_report.txt`.