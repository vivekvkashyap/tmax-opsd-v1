Break this into three independent sub-problems and solve them separately before wiring the service together. (1) Audio transcription: install `whisper.cpp` (or use `ffmpeg` to convert to 16kHz mono WAV first, whisper models often require this) and run it against `/app/intercepted_comms.wav`; lowercase and strip punctuation from the transcript before writing it to `/home/user/secret_token.txt`. (2) Git bisection: run `git bisect start`, mark current `master` bad and an early commit good, then at each step write a tiny test harness that calls `smooth_signal` with a negative input under a timeout (e.g. `timeout 5s cargo run ...` or a unit test) so `git bisect run` can automatically classify hang-vs-terminate; capture the resulting commit hash into `/home/user/bad_commit.txt`. (3) Fix: in `src/smoothing.rs`, take `.abs()` of negative inputs before the iterative loop so the termination condition can be reached, recompile, then launch the axum/actix service in the background (`... &`) bound to 127.0.0.1:8080 and confirm with a curl POST to `/process` before finishing.
