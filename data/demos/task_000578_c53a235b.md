Start by grepping the vendored source for suspicious delay calls: `grep -rn "usleep\|sleep(" /app/pen-0.34.1/*.c` — the injected "rate limiting" is almost certainly a single `usleep(...)` call inside the accept/connection-handling loop (likely `do_accept()` in `pen.c`), not a real feature; delete it rather than trying to "tune" it. Build with an unprivileged prefix: `./configure --prefix=/home/user && make && make install` (or set `PREFIX=/home/user` if there's no configure script) so the binary lands at `/home/user/bin/pen`. Launch three `python3 -m http.server` style processes (or a tiny `http.server` subclass returning "OK") on 8001/8002/8003 in the background with `&`, confirm each responds via `curl`, then start `pen` listening on 8080 round-robining to those three ports — check `pen -h` for the correct flag syntax for backend lists. For `monitor.sh`, use a `while true; do ... ; sleep 1; done` loop with `curl -o /dev/null -s -w "%{time_total}\n" --max-time <t>` to measure latency, comparing against 0.1s, appending timestamped alerts on failure/slowness, and run it with `&`. Verify throughput with repeated curl loops before declaring done — a lingering sleep anywhere kills your RPS.
