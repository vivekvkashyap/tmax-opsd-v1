Diagnose before patching. Run `strace -f -o trace.log python3 run_pipeline.py input.json` (it will hang, so run it in the background or with a timeout, then inspect the tail of trace.log) to see which syscall the main process is blocked in — look for the last syscall entry with no matching return before the timeout; that unfinished call is the one blocking the parent process (it's waiting on the pipe connected to the C child's stdout). Record the exact syscall name you observe in hanging_syscall.txt. Next, inspect flatten_data: trace through a nested-list input by hand — when an element is itself a list, the code recurses and extends but never advances the loop index `i`, causing it to reprocess that same nested element forever; fix by incrementing `i` after handling both branches (list or non-list). After that, look at how Python serializes floats to the C aggregator's stdin — check the C source for the byte width it reads per value (fread of `double`, 8 bytes) versus the struct.pack format code being used, and correct the mismatch so both sides agree on precision/width. Finally regenerate output.txt and diff it against expected.txt to confirm exact equality.