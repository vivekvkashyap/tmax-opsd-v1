Build this as a single-pass Go program using only `encoding/csv`, `strconv`, and `math`. First read all rows into a slice of structs so you can do two passes: (1) for each sensor compute the posterior via Bayes' rule — numerator = prior_fail * (is_high==1 ? p_high_given_fail : 1-p_high_given_fail), denominator = numerator + prior_ok * (is_high==1 ? p_high_given_ok : 1-p_high_given_ok), where prior_ok = 1-prior_fail; posterior = numerator/denominator. (2) for nearest neighbor, do an O(n^2) scan over all *other* sensors computing squared Euclidean distance on (val_x, val_y) and track the minimum, being careful to skip self-comparison and to break ties deterministically (e.g. first-encountered minimum in original row order). Round the posterior to 4 decimals using proper rounding (not truncation) — e.g. math.Round(x*10000)/10000 — and format with `%.4f` so trailing zeros are preserved. Write results in the same row order as the input CSV. Compile with `go build -o /home/user/process /home/user/process.go`, then run the binary so `/home/user/results.csv` is actually produced — writing the code without executing it will fail verification.