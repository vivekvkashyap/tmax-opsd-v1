Break this into extraction, parsing, and generation, verifying at each boundary. First extract the archive with `tar -xzf /home/user/incoming_docs.tar.gz -C /home/user/extracted_docs/` (creating the target dir first) and inspect `doc_map.ini` directly to confirm the `key=relative/path|expected_size` format before writing any parsing code. In your C++ program, parse the INI by finding lines after `[Docs]`, splitting on `=` then `|` to get the relative path and expected size as an integer; for each entry use `std::filesystem::exists` and `std::filesystem::file_size` (or `stat`) to get the actual size and compare exactly against the expected value — a mismatch or missing file means skip, don't error out. Only keep survivors whose path ends in `.txt` (check the literal suffix). For each surviving file, open it and read only the first line with `std::getline` for the title (watch out for trailing `\r` or newline characters bleeding into the title string — strip them). Collect `(path, title)` pairs, sort by path with `std::sort`, then write each as `- {path}: {title}` to doc_index.md. Compile with `g++` and run the binary to produce the final file, then manually diff against what you'd expect given the sizes in the INI.
