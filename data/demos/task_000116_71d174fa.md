Separate the file-selection logic (shell) from the decoding/parsing logic (Go) rather than trying to do everything in one place. For the Go program, read each input file's raw bytes and decode them from ISO-8859-1 to UTF-8 using `golang.org/x/text/encoding/charmap.ISO8859_1.NewDecoder()` wrapped around the byte reader (a naive `string(bytes)` cast will corrupt accented characters since Go strings are UTF-8) — then parse the decoded text line by line for `key=value` pairs, skipping lines starting with `[`, and print `<name> -> <server_url>` for each file argument. For selecting files, use `find /home/user/configs -name '*.conf' -mtime -5 -size -10k -print0 | xargs -0 go run tracker.go` (or build first then run the binary) — get the `-mtime`/`-size` operators exactly right, since `-size -10k` uses 1024-byte units and off-by-one comparisons are easy to get wrong; test against the known file mtimes/sizes to confirm your find command selects exactly the expected subset before trusting the final output. Redirect the Go program's stdout into `/home/user/active_servers.txt` and verify the accented characters render correctly (not mojibake) when you inspect it.