Recover the two secrets from the image first, since the filter's logic depends entirely on them: OCR `/app/auth_spec.png` with `tesseract` (install via apt if missing) and read off the `SECRET_KEY` value and the full `RESTRICTED_STRINGS` list exactly as printed — double check OCR output character-by-character for lookalike-character errors (0/O, 1/l, punctuation) since a wrong key means every signature check fails. Then implement `filter.c` as a straight validation pipeline: read the whole file as one hex string, hex-decode with a simple lookup table (reject on any non-hex char or odd length), require decoded length > 32, split into a 32-byte signature and the remaining Data bytes, compute `SHA256(Data || SECRET_KEY)` with OpenSSL's EVP or SHA256 API (link `-lssl -lcrypto`), and `memcmp` against the signature — only then scan the ASCII Data for any of the restricted substrings (plain `strstr` per restricted word is fine). Exit 1 immediately on the first failing rule (bad hex, short length, sig mismatch, or restricted substring found), exit 0 only if every rule passes. Test manually against a couple of clean/evil corpus files before trusting the full 50/50 run.
