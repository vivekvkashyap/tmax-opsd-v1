Build the pipeline in the order dependencies require: first `mkdir -p /home/user/bin` and `gcc -O2 -o /home/user/bin/score_ext /home/user/src/scoring.c` and confirm the binary runs standalone on a sample primer/target pair before scripting around it. For reproducible primer generation, embed a Python one-liner/heredoc inside the bash script that does exactly `random.seed(123)` then generates 100 primers via `''.join(random.choices('ACGT', k=12))` in a loop — the seed and generation order must match this exactly since determinism is what's being checked, so don't substitute `random.sample` or a different RNG call shape. Read `/home/user/target.txt` once, then for each primer invoke `/home/user/bin/score_ext "$primer" "$target"` and capture its stdout as the score. Append each `primer,score` pair (no header) to `/home/user/dataset.csv`, and confirm the file ends up with exactly 100 lines. Make the bash script executable and actually run it end-to-end rather than just writing it.