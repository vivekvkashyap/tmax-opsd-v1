First extract the secrets from `/app/dashboard_recording.mp4`: since the leak frame flashes for a fraction of a second, use `ffmpeg` to dump frames at a high rate (e.g. `-vf fps=30` or higher, or extract every frame) across the whole video, then run `tesseract` OCR over each extracted frame image and grep the OCR text output for lines mentioning secret/fingerprint — do not assume a specific timestamp, scan systematically. Once you have the real `HMAC_SECRET` and `TRUSTED_FINGERPRINT` strings from OCR output, hardcode them as constants in `/home/user/jwt_filter.py`. Build the validator by manually base64url-decoding the JWT header/payload (or using a JWT library while explicitly forbidding `alg: none`), checking `alg` is `HS256` or `RS256` case-insensitively, verifying signatures properly (HMAC-SHA256 for HS256; for RS256 parse the `x5c` cert with a crypto library, verify the RSA signature, and compute its own SHA-256 fingerprint to compare against the trusted one), enforcing the exact `csp` claim string, and rejecting any `role: admin` token that isn't RS256-with-trusted-cert. Wrap all parsing in try/except so malformed tokens exit 1, and test against both corpora directories before finishing.