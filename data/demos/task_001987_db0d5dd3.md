Break this into recover-the-secret, then build-and-wire-the-deploy-pipeline. First run `tesseract /app/backup_config.png stdout` (try `--psm` variants if the default segmentation misses the token line) to get the text, then locate the `AUTH_TOKEN: ` line and extract the UUID that follows it — treat this recovered value as a variable in your server code, never hardcode a guess. Set up git: `git init --bare /home/user/deploy.git` then `git clone /home/user/deploy.git /home/user/workspace`. Write `server.c` as a minimal blocking TCP server (`socket`/`bind` to 127.0.0.1:9090/`listen`/`accept` loop) that on each connection writes the token string plus `\n` then closes the socket. For the `post-receive` hook in the bare repo: read `oldrev newrev refname` from stdin, `git --work-tree=/home/user/deployed_service checkout -f <newrev> -- .` (or archive+extract) to deploy the tree, `gcc server.c -o server_bin`, kill any running `server_bin` (e.g. `pkill -f server_bin` or track a pidfile) before starting the new one with `nohup ./server_bin &`, and `chmod +x hooks/post-receive`. Finally commit+push from workspace to trigger it, and confirm with `nc 127.0.0.1 9090`.