Start by isolating the vendored-package fix: locate the broken import in `/app/vendor/python-dateutil/dateutil/parser/_parser.py` (search for a misspelled standard-library module name near the top of the file), correct the typo, then install the package into your environment editable/from-source (e.g. `pip install -e /app/vendor/python-dateutil` or equivalent) and sanity-check with a quick `python3 -c "from dateutil import parser; parser.parse('2023-01-01')"` before writing any pipeline code. For the ETL script, implement the five steps in the exact stated order and pay attention to subtle correctness traps: use Python's stable `sort()` (not `sorted` with a mutating comparator) keyed on the parsed datetime so ties preserve original order; deduplicate by keeping strictly the first occurrence per `id` after sorting; for imputation, scan forward/backward from each null within the deduplicated (not original) sequence, handling all-null and edge (leading/trailing null) cases separately, and round only the imputed value to 2 decimals; mask emails by splitting on `@` and keeping only the first local-part character; finally filter to even `id`s. Emit compact JSON (`separators=(',', ':')`) with no extra fields, and test against several handcrafted inputs including duplicates, edge nulls, and empty arrays before trusting it against the fuzzer.