Use pandas to keep the resampling/merging logic precise rather than hand-rolling bucket math. For metrics: read the CSV, melt the cpu/memory/disk columns into long format (timestamp, metric, value), parse timestamps as UTC datetimes, then floor each timestamp to the 5-minute bin (`dt.floor('5min')` or integer-divide the epoch seconds by 300) and group by (bin, metric) taking the mean. Critically, you must generate the *complete* set of 5-minute bins spanning the data range per metric (a full date_range at 5-min freq) and reindex before forward-filling — merely grouping existing timestamps will silently skip bins that have zero raw samples, which breaks the required forward-fill-from-preceding-bin behavior. For logs: parse timestamps (note they lack a timezone suffix but should be treated as UTC), floor to the same 5-minute bins, and for each log's message lowercase it, strip non-alphanumeric/non-whitespace characters (e.g. regex `[^a-z0-9\s]` -> ''), split on whitespace, and count occurrences of exactly "error" or "timeout" as separate tokens (not substring matches, so "errors" or "timeouts" plural should not count unless it tokenizes to the exact word). Sum alert counts per bin, defaulting missing bins to 0. Merge metrics and log counts on the bin timestamp, round mean_value to 2 decimals, format timestamps as ISO8601 with a trailing 'Z', sort by timestamp then metric name, and write the CSV with the exact column order specified.
