Start by inspecting `fw_export.dat` with `head`/`cat -A` to find the delimiter (likely `|`) and count fields per line with `awk -F'|' '{print NF}'` — map the field order to logical names (Source_Host, Destination_Host, Protocol, Port, Rule_ID) based on which values look like hostnames vs numeric ports vs rule identifiers. Build an edge list with `awk` extracting just source and destination columns into a scratch file. Implement BFS (not DFS) in pure Bash/awk to guarantee minimum hops: maintain a queue array, a visited set, and a parent map (associative array keyed by node); pop from the front of the queue, expand neighbors found via `grep`/`awk` over the edge file, and stop as soon as `PublicInternet` is dequeued. Reconstruct the path by walking parent pointers backward from the target to `SecurePaymentGateway`, then reverse it before printing with ` -> ` separators. Write both the SCHEMA block and VIOLATION PATH block to the exact output format specified. Avoid Python/Perl entirely — everything must be Bash/awk/grep/sed.
