Split this into the two independent objectives and verify each end-to-end rather than assuming config edits are correct. For the header-spoofing fix, inspect `/app/ecommerce_stack/nginx/nginx.conf` and add `proxy_set_header X-Internal-Role "";` inside the public-facing `location` block that proxies to Flask on port 5000 — this strips any client-supplied header before forwarding, while leaving direct requests to the Flask port (bypassing Nginx) unaffected. After editing, test all three curl cases yourself: through Nginx without the header (should reach Flask normally), through Nginx with a forged `X-Internal-Role: admin` header (must come back 403 from Flask's own permission check since Nginx stripped it), and directly to `:5000` with the header (must return 200, proving Flask's internal-role logic itself is intact and only reachable via legitimate internal traffic). For the WAF, implement `verify_request(headers: dict, cookies: dict) -> bool` in `/home/user/waf_filter.py` checking headers/cookies for path traversal, SQL injection fragments, and forged admin/role cookie values — build and test the checks against the given `/app/corpus/clean/` and `/app/corpus/evil/` JSON fixtures iteratively until 100% pass on both. Finally write `/home/user/audit_complete.log` containing "AUDIT FINISHED".
