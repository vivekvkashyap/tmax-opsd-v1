Read `/home/user/sensor_service.cpp` looking for heap allocations (`new`) that lack a matching `delete` on every early-return path — the leak pattern is an object allocated per-line, with `delete` only reached on the success path while error/exception and NaN/Inf branches return early without freeing it. To build the MREs: non-numeric text hits the `catch` branch and leaks — put that in `/home/user/mre_corrupt.txt`; a numeric value that drives a downstream denominator/subtraction to exactly zero or near-zero produces NaN/Inf and leaks via that branch — put that single number in `/home/user/mre_math.txt`. For `/home/user/fixed_service.cpp`, prefer RAII (stack objects, `unique_ptr`, or restructuring so no raw `new` needs manual cleanup) over adding `delete` before every return, since it's robust to future changes; also add an epsilon check (`fabs(denominator) < 1e-9`) so near-zero divisors are rejected before producing NaN/Inf. Compile with `g++ -O2` and confirm with `valgrind --leak-check=full --error-exitcode=1` on both MREs and mixed content before trusting the fix.
