Use `tesseract /app/arch_screenshot.png stdout` to OCR the terminal screenshot, then locate the base64 token following `--password` in the recognized text and decode it with `base64 -d` (or Python's `base64.b64decode`) — write the decoded plaintext to `/home/user/old_password.txt` exactly, without OCR artifacts like stray newlines or misread characters, so double-check the OCR output against the visible image before trusting it. For the detector, inspect the sample files in `/home/user/corpus/evil/` and `/home/user/corpus/clean/` to enumerate real attack patterns (script tags, SQL boolean/UNION injection markers like `' OR 1=1` or `--`, shell metacharacters like `;`, backticks, `$(...)`, `rm -rf`), and build `/home/user/detector.py` to read the file argument, URL-decode it first (since payloads may be percent-encoded to evade naive matching), and check the decoded content case-insensitively against a broad set of signatures for XSS/SQLi/command-injection rather than a narrow literal list — print `EVIL`/exit 1 or `CLEAN`/exit 0 accordingly. Since it's graded against a hidden, larger corpus, favor generalizable pattern classes (e.g. regex for `<[a-z]+[^>]*on\w+=`, SQL comment/tautology patterns, shell control characters) over memorizing the exact sample strings, and test against the given corpora directories yourself before considering it done.