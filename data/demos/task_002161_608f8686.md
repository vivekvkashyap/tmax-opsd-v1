Build and unit-test the formatter script in isolation before touching the video at all, since it will be fuzzed against a hidden reference implementation. Use a single anchored regex like `^\[(.*?)\]\s*(\{.*\})$` to split the level from the JSON payload (don't hand-roll bracket counting), then `json.loads` the payload in a try/except, and check that `user`, `action`, and `ip` are all present as keys before formatting — any failure at any stage (no match, JSONDecodeError, or a missing key) must print exactly `INVALID LOG` and nothing else. Match the valid-output template's field order and literal labels precisely, including the trailing newline behavior of `print`. Test extensively with malformed/edge-case inputs (missing prefix, garbage JSON, extra keys, empty string) before moving to Part 2. For the video, use `ffprobe`/`ffmpeg` to locate and extract the subtitle stream (e.g. `-map 0:s:0`) to SRT text, then filter out sequence-number and timestamp lines, keeping only lines starting with `[`, and pipe each such line through your script via subprocess, joining valid outputs with a blank line between blocks.