Reverse-engineer the legacy binary's transformation empirically rather than guessing: pipe isolated character classes through it separately (just the alphabet, just digits, just punctuation/spaces) so each class's behavior is easy to read off independently, and check whether letters look shifted, digits look transformed, and other characters look untouched. Once you have a hypothesis, write `new_processor.py` to implement it and validate by generating your own random test strings, running them through both the binary and your script, and diffing stdout — don't stop at a few hand-picked examples, since the grader fuzzes broadly (including punctuation, mixed case, and non-letter/digit characters that must pass through unchanged). For `check_and_run.sh`, test port 8080 with something reliable like `bash -c "echo > /dev/tcp/127.0.0.1/8080"` or `nc -z`, and branch cleanly on its exit code. For logrotate, set `daily`, `rotate 7`, `compress`, `delaycompress`, and `missingok` as separate directives inside the block for `/home/user/processed/*.log`. For the git hook, use `git diff --cached --name-only --diff-filter=ACM -- '*.py'` and grep each staged file's content for the forbidden string before allowing the commit.
