Load `/home/user/profiling_data.csv` with pandas or the csv module, group by `N` and take the mean of `time_ms` per group to get your aggregated (N, mean_time) pairs. Because the model is `Time = c * N^2` with no intercept, don't use a general linear-regression routine that fits an intercept by default — instead derive the closed-form OLS solution for a single coefficient through the origin directly: with `X = N^2`, the least-squares estimate is `c = sum(X * Time) / sum(X * X)` (equivalently `np.polyfit` won't give the no-intercept fit you need, so compute it by hand or use `np.linalg.lstsq` on the single-column design matrix `X` with no ones-column appended). Compute predicted values as `c * X`, then `SS_res = sum((Time - predicted)^2)` and `SS_tot = sum((Time - mean(Time))^2)` using the aggregated means (not raw per-run data) for both, and `r_squared = 1 - SS_res/SS_tot`. Round both `c` and `r_squared` to 6 decimal places and write them to `/home/user/fit_results.json` in the exact specified key order/format. Double check by hand-computing one or two of the aggregated means before trusting the script's output.
