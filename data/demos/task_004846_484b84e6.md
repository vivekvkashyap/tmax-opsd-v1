Use Go's `debug/elf` package: open `/home/user/target_bin`, iterate `f.Sections` to find the one named `.sec_policy`, and call `.Data()` on it to get its raw bytes — note that ELF sections are often padded to alignment boundaries, so after reading raw bytes you may need to trim trailing null bytes/whitespace before base64-decoding, otherwise decoding will fail or leave garbage. Base64-decode with `encoding/base64` (StdEncoding), then unmarshal the decoded bytes as JSON into a `map[string]interface{}` (or a struct) so `extracted_policy` is emitted as a real JSON object, not a string. Separately compute `sha256.Sum256` over the raw decoded bytes (before any re-marshaling, since JSON key ordering/whitespace could otherwise change the hash) and hex-encode it lowercase with `encoding/hex` or `%x`. Marshal the final `{"extracted_policy": ..., "sha256": ...}` struct to `/home/user/verification_log.json` with `json.MarshalIndent` or similar, then run `go run extractor.go` (or build it) so the output file actually gets produced before finishing.
