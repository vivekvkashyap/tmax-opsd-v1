Split this into two independent, separately-testable pieces. Part 1: use `ffmpeg` to extract the exact frame at each integer second (e.g. `ffmpeg -ss 00:00:0N -i /app/profile_viz.mp4 -frames:v 1 -f image2 frame_N.png`, or extract all frames and pick the ones nearest each 1s boundary), then compute each frame's average grayscale intensity — convert to grayscale first (`-vf format=gray`) and compute the mean pixel value with a small script (PIL/`numpy.array(img).mean()`, or `ffmpeg`'s `signalstats`/`showinfo` filters), rounding to an integer; write one integer per line, in second order, to `/home/user/extracted_loads.txt`. Verify visually by dumping a couple of extracted frames as images and eyeballing their brightness before trusting the numbers. Part 2: implement the described 3-candidate hill-climbing loop literally in bash/awk/bc exactly as specified — initialize with min/max/floor(avg) of the comma-separated input, run exactly 10 generations recomputing sum-of-abs-differences fitness for each candidate, break ties by lowest T, then mutate by ±5 around the best; output only the final best_T. Test edge cases (single-element list, duplicate values, negative differences) against your own hand-traced iterations, since this is fuzz-tested bit-for-bit against a reference bash implementation.