Approach this as three separate, verifiable fixes rather than one blind rewrite. First, get the Makefile right for a shared library: compile the .c source with `-fPIC`, link the .so with `-shared` (not a plain executable link), and make sure the test_runner links against `-L. -lpolyhash` with `-Wl,-rpath,.` or sets `LD_LIBRARY_PATH` at run time so it can find the freshly built .so — a common mistake is forgetting `-shared`/`-fPIC` or omitting the rpath so the binary fails to locate the library at runtime. Second, find the leak with a profiler: build with `-g`, run `valgrind --leak-check=full ./test_runner`, and read the stack trace to see which `malloc` in polyhash.c is never freed — free the buffer after you're done using it, without altering the polynomial math. Third, re-run valgrind and redirect stderr (`2> valgrind.log`) to capture the exact leak-check summary showing zero bytes lost. Finally run test_runner once more so it writes the hash of the given string to result.txt.
