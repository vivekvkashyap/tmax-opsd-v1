Read app.py carefully to map the full request path: PIN check via SHA256, then AES-CBC decryption of a `filename` parameter, then a naive substring WAF check for `../`, then `os.path.join` and file read. Crack the PIN first — it's only 4 digits, so brute-force all 10000 candidates locally, hashing each with SHA256 and comparing to the hash in crypto_config.json, rather than guessing. For the file-read bypass, notice that `os.path.join(base, filename)` in Python discards `base` entirely if `filename` is an absolute path (starts with `/`) — so encrypt the literal target path (e.g. the flag's absolute path) rather than a `../`-style traversal, which sidesteps the WAF's substring check entirely. To build the malicious payload, replicate the app's AES-CBC encryption using the key/IV from crypto_config.json with standard PKCS7 padding, base64-encode the ciphertext, and POST it as the `filename` form field alongside the cracked `pin` to `/download`. Start the Flask app in the background first so you can test iteratively, then script the whole exploit in exploit.py, writing the raw response body to extracted_flag.txt and its SHA-256 hex digest to flag_hash.txt.