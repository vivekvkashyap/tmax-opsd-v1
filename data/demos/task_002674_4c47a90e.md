Build this as a Rust binary crate with `cargo new`, adding `csv`, `chrono`, and `serde` as dependencies rather than hand-parsing CSV. Process rows in a single pass, tracking the 1-based row index for logging. For each row, apply validations strictly in the specified order (timestamp parse, then value parse, then bounds check) and stop at the first failure — do not check later rules once an earlier one fails, since only the first failure reason is logged. Try parsing `recorded_at` with `chrono::DateTime::parse_from_rfc3339` first, falling back to `NaiveDateTime::parse_from_str` with format `"%m/%d/%Y %H:%M"` and treat the result as UTC; if both fail, that's TIME_PARSE_ERROR. Parse `value` with `str::parse::<f64>()`; failure is PARSE_ERROR. Convert Fahrenheit before the bounds check, and round to 2 decimals only at output time (use a proper rounding function, not truncation) so the average computed later matches. Keep running totals (processed, valid, rejected, sum of valid celsius) as you go, then do simple string replacement of the `{{PLACEHOLDER}}` tokens in the template. Write the log, clean CSV, and report.md exactly as specified, then `cargo build --release` and run the binary so the output files actually land in /home/user/.
