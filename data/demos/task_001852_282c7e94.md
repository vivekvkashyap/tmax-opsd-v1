Implement the ODE integrator exactly as specified rather than reaching for a library solver — this is a from-scratch Forward Euler step so subtle deviations (using y'(t) at the updated vs. current point, wrong step order) will silently shift every downstream number. In generate_features.py, maintain two state variables y and v starting at 1.0 and 0.0; each of the 100 iterations, first record the current y (these are your 100 samples for t=0..0.99), then update using the *current* (pre-update) values: `y_new = y + v*dt`, `v_new = v - (2*pi*f)**2 * y * dt`, then advance y,v to the new values — order matters, don't use the updated y when computing v_new (that would make it semi-implicit/symplectic Euler instead of the plain Forward Euler asked for). Compute max abs error against `cos(2*pi*f*t)` elementwise over the 100 samples. For the FFT step, use `np.fft.rfft` on the 100 y-values and `np.fft.rfftfreq(100, d=0.01)`, then pick the frequency at the index of maximum magnitude — verify this peak lands near the expected f you passed in as a sanity check. Format output to the exact decimal places specified, then have build_dataset.sh loop f=1..5, capture each script invocation's stdout line, and assemble the CSV with the required header.