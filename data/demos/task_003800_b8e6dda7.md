Work through the pipeline from the data source outward, verifying each hop with `curl` before wiring the next one. First implement `clean_and_tokenize` in `/home/user/app/cleaner/src/main.rs`: lowercase the text, strip everything but alphanumerics/whitespace (a simple char filter, not a heavy regex crate), split on whitespace, and serialize to a JSON array — get `cargo build --release` passing before touching the other services. Set `CLEANER_URL=http://localhost:8080/data` in `/home/user/app/serving/.env` so `model_serving` knows where to fetch cleaned tokens. Start services in dependency order and confirm each with a manual request before moving to the next: `python3 -m http.server 8000` in the ingest dir (verify `curl localhost:8000/data.json`), then the built `cleaning_worker` binary bound to 8080 (verify `curl localhost:8080/data` returns the tokenized array), then `model_serving` on 9000 (verify `curl localhost:9000/predict` returns the expected JSON shape `{"status":"success","score":<float>}`). Run each with `nohup ... &` or similar so they persist in the background. Only after confirming the full chain responds correctly should you write "READY" into `/home/user/pipeline_status.log` — writing it prematurely, before all three ports are actually bound and reachable, is the main way this task fails verification.
