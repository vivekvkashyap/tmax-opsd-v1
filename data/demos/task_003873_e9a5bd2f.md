Work through this as a pipeline with an ordering dependency: drop NaN-target rows FIRST, then compute per-column medians only from the remaining rows (so median imputation doesn't leak information from rows that will be dropped), then impute. For correlation-based pruning, compute the full Pearson correlation matrix on the (now-imputed) features, find all pairs with |corr| > 0.85, and for each flagged pair drop the feature name that sorts later alphabetically — do this considering that dropping one feature could change which pairs still exceed the threshold only if a dropped feature reappears in another pair; handle iteratively (recompute or track already-dropped features) so no surviving pair exceeds 0.85. Train GaussianNB on exactly the retained feature columns against is_anomaly. For the Flask service, keep the exact list and order of retained feature names consistent between training and prediction (extract features from the incoming JSON dict by name, not by position), return predict_proba(...)[:,1] for anomaly_probability as a plain float and the predicted class as a plain int (watch out for numpy types not being JSON-serializable — cast with float()/int()). Launch the app in the background (e.g. nohup ... &) and confirm it's listening with a curl/nc check on 127.0.0.1:8080 before finishing.