Write the bash script so each step is idempotent and self-contained: create the venv with `python3 -m venv /home/user/venv`, activate it, `pip install numpy` inside the script rather than assuming it's pre-installed. Discover CSV files dynamically with a glob (`for f in /home/user/data/raw/*.csv`), and for each, invoke a small inline Python snippet (`python3 -c "..."` or a heredoc) that loads the CSV with `numpy.loadtxt` or `numpy.genfromtxt` and computes `numpy.trace(matrix)` — pass the trace back to bash via command substitution. Compare the trace to the `>= 20.0` threshold in bash using a numeric comparison (bash can't do float comparisons directly — use `awk` or `python3 -c` for the `>=` test, not `[[ ]]` with raw floats). On pass, copy the file to `/home/user/data/processed/` and append `filename.csv: <trace>` to `summary.log`, formatting the trace to exactly two decimals (`printf "%.2f"` or Python's `f"{trace:.2f}"`); since files are discovered via glob in alphabetical order already, the log will naturally end up sorted — but verify this rather than assuming it. Finally `tar czf /home/user/archive.tar.gz -C /home/user/data processed` and `chmod +x` the script before running it.
