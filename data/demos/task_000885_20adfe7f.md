First find the live process: `pgrep memory_tracker` (or `ps aux | grep memory_tracker`), then look in `/proc/<pid>/fd/` for the file descriptor whose target shows `alloc.log (deleted)` via `ls -la /proc/<pid>/fd/`. Recover its contents by reading that fd path directly, e.g. `cat /proc/<pid>/fd/<N> > /home/user/recovered.log` — this works because Linux keeps deleted-but-open files' data accessible through the fd symlink. Parse the recovered log for `ALLOC <id> <addr>` / `FREE <id> <addr>` lines and compute the set difference (allocated IDs minus freed IDs), sorted numerically, into `/home/user/leaked_ids.txt` — awk with associative arrays is a natural fit here. Then read `/home/user/memory_tracker.c` to find the conditional that skips freeing under some condition (look closely at the loop and any modulus/skip logic), remove or invert that condition so every allocated pointer is freed unconditionally, add `#include <assert.h>` if missing, and insert `assert(ptr != NULL);` immediately before the `free()` call you fix. Recompile exactly as specified to `/home/user/memory_tracker_fixed`; you don't need to touch the still-running original process.