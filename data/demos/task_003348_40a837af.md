Do the cleaning step literally with awk over the CSV, working on `response_time` (a numeric field) while leaving other columns untouched: use `-F,` with `OFS=,`, and per data row (skip header via `NR==1`) first replace an exact `"NA"` value in that column with `250`, then drop the row entirely if the (possibly-just-imputed) value is `< 0` or `> 5000` — order matters, since NA must be imputed before the range check, not treated as an implicit outlier. Print the header untouched, then filtered/cleaned rows, redirecting to `cleaned_logs.csv`. For the Python half, load the cleaned CSV with pandas, lowercase the `message` column before fitting `TfidfVectorizer(max_features=50, stop_words='english')`, `hstack` (via `scipy.sparse.hstack`) the resulting sparse matrix with the raw (unscaled) `response_time` column to get 51 features, and feed both into `GridSearchCV(LogisticRegression(random_state=42, max_iter=1000), {'C':[0.01,0.1,1.0,10.0]}, cv=5)`. After `.fit()`, pull `best_params_['C']` and `best_score_`, format the score to 4 decimals, and write both lines exactly as specified to `/home/user/metrics.txt`.
