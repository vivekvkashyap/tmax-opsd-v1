Implement this as a clean sequence of stages in C, testing intermediate values by hand-computing a couple of rows before trusting the full run. Load both CSVs into arrays/structs keyed by integer `ID` (e.g. sort both by ID or build a simple hash map), then perform an inner join by only keeping IDs present in both — iterate the smaller set and look up in the other rather than a nested O(n^2) scan if performance matters, though correctness is the primary check here. For each joined row, apply the fixed 6x2 projection matrix exactly as given to get `Z1, Z2` (be careful to multiply each feature by the correct matrix row, not transpose it), then compute the logistic regression logit `L = W1*Z1 + W2*Z2 + b` and probability `p = 1/(1+exp(-L))` using `<math.h>`'s `exp`, classifying `1` if `p >= 0.5` else `0`. Format the probability with exactly 4 decimal places (`%.4f`) and sort output rows by ascending `ID` before writing the header and rows to `/home/user/predictions.csv`. Compile with `gcc -lm`, run the binary, and double check formatting edge cases (e.g. probabilities that round to `1.0000` or `0.0000`) since the grader does an exact `diff` against a golden file.
