Treat this as three separable subsystems: crash discovery, TLS setup, and the proxy itself. First black-box fuzz `/app/traffic_inspector` by feeding it varied HTTP header sets via stdin and watching for a nonzero/SIGSEGV exit — narrow down which single header and value pattern triggers the crash (the task hints it relates to an open-redirect-style `Referer`/`Location` header with unusual slash/traversal patterns); confirm the exact trigger reproducibly before hardcoding a detection rule, rather than guessing a filter that's too broad or too narrow. Generate the wildcard cert with `openssl req -x509 -newkey rsa:2048 -keyout /home/user/certs/server.key -out /home/user/certs/server.crt -days 365 -nodes -subj "/CN=*.internal.corp"` (add a SAN of `*.internal.corp` too, since some clients require it). In `proxy.c`, use raw sockets (or OpenSSL for the 8443 listener) to accept connections on both ports, parse headers, and check each request against your discovered crash signature; on match, close the connection without forwarding and append a log line in the exact format `[DROP] IP=<ip> Payload=<base64 of the offending header value>` — base64-encode with a standard library, not by hand. On safe requests, pipe the raw headers to `/app/traffic_inspector` via a forked subprocess with redirected stdin/stdout, capture its output, and wrap it in a `200 OK` response back to the client. Compile with plain `gcc`/OpenSSL libs and leave the binary running in the background bound to both ports before finishing.