Structure the C program in three clear stages. First, parse the adjacency matrix from `network.txt` into a 2D int array, compute each row's degree by summing its row, and pick the argmax with a strict tie-break rule (lowest Node ID wins on ties — use `>` not `>=` when scanning so the first/lowest max is kept). Second, stream `spectra.csv`, skip the header, and filter rows where `NodeID` matches the selected node, collecting Wavelength/Absorbance pairs into dynamic arrays (or a fixed-size buffer sized generously). Third, build the normal-equations matrix X^T X (a 2x2 symmetric matrix: [n, sum(x); sum(x), sum(x^2)]) and vector X^T y ([sum(y), sum(x*y)]) directly from the collected pairs rather than materializing the full design matrix, then use `gsl_matrix`/`gsl_vector` with `gsl_linalg_cholesky_decomp` followed by `gsl_linalg_cholesky_solve` to get beta. Format output with `printf("%.4f", ...)` to guarantee exactly 4 decimals (watch for -0.0000 edge cases). Compile exactly with the given gcc command including `-lgsl -lgslcblas -lm`, then run the binary — don't forget to actually execute it so `solution.txt` exists on disk.