Load both CSVs into SQLite tables, then use a recursive CTE anchored on a user's direct roles from `user_roles.csv`, walking `role_hierarchy.csv`'s parent->child edges until you hit nodes that never appear as a `parent` (those are the leaf permissions) — union/distinct the results so a permission reached via multiple inheritance paths is only counted once. Build the HTTP server (Python's built-in `http.server` or a minimal framework) listening on exactly `127.0.0.1:8443` with no auth. For `/api/v1/audit/<user_id>`, run the recursive query, sort permissions alphabetically, then shell out to `/app/sod_oracle` passing the permission list as separate argv entries (not one combined string) and parse its stdout — treat it purely as a black box that emits `SAFE` or a `TOXIC: a, b` line; parse the two permission names out of that line for the `toxic_pair` field. For `/api/v1/stats/centrality`, aggregate across ALL users' resolved leaf-permission sets (a `GROUP BY permission, COUNT(DISTINCT user_id)`-style query after flattening every user's inherited permissions), take the top 3 by count. Test both endpoints yourself with `curl` against a couple of the given users before finishing, to confirm the JSON shape and toxic-pair detection actually work end-to-end.