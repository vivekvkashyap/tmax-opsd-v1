This task's correctness hinges entirely on implementing the specified numerical procedure exactly, not on approximation — read every step literally. Load the CSV with pandas, compute `np.fft.fft(y)` and `np.fft.fftfreq`, restrict to strictly positive frequencies before taking the argmax of the magnitude spectrum to get f_init (mixing up the positive-frequency mask/index alignment is the most common bug here), and set A_init = 1.0. Call `np.random.seed(42)` immediately before the sampling loop starts, not any earlier, then run exactly 20000 Random-Walk Metropolis-Hastings iterations proposing A and f jointly from independent N(0, 0.05) steps, rejecting any proposal outside [0,10]x[0,10] via a -inf log-prior, and using the Gaussian log-likelihood with sigma fixed at 0.5. Discard the first 5000 samples as burn-in, average the remaining 15000 per parameter, and write "A_mean,f_mean" rounded to exactly 2 decimal places to results.txt.
