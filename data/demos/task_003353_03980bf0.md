Before writing code, black-box characterize the binary at /app/cluster_verifier: feed it small hand-crafted edge lists (a chain, a disconnected pair, a self-loop, reversed-direction edges) via `--in`/`--out` and diff the JSON against your hypotheses. Use `strings` and `strace -f` for extra clues, but rely mainly on I/O behavior since it's stripped. Specifically test whether edge direction matters (isolated A->B vs B->A) to determine if connectivity is directed or undirected, and whether each component's assigned id is min, max, or first-seen. Once confirmed, implement Union-Find (or BFS/DFS) over all nodes appearing as source or target, unioning endpoints treating the graph as undirected, then assign every node in a component its component's minimum node id. Output a JSON object keyed by string node ids in ascending numeric order, matching the binary's exact formatting (spacing, trailing newline) since equivalence is checked byte-for-byte via fuzzing across many random graphs — validate against several generated inputs before finishing.
