Set up the Rust project with `cargo new /home/user/proj`, then add `csv`, `serde` (with derive), and `byteorder` to `Cargo.toml`. For tokenization, lowercase the text and extract maximal runs of `a-z` (e.g., iterate chars, split on any non-alphabetic byte, or use a regex/manual scan) — do not naively split on whitespace only, since punctuation like commas/periods/exclamation marks must also act as delimiters. For each row build the 5-element count vector for the exact vocabulary words given in the task, then multiply it against the given 5x2 matrix by hand (row_i_of_freq * matrix_row_i, summed over i, per output column) — this is a simple dot-product loop, no need for a linear-algebra crate. Write output with `byteorder`'s `WriteBytesExt` in little-endian: `write_u32` for id, `write_f32` for each projected value, giving exactly 12 bytes per CSV row in original row order. Build with `cargo build --release` and run the binary against `/home/user/data.csv`, then verify `/home/user/output.bin` size is `12 * num_rows` bytes before trusting it.
