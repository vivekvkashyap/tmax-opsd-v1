Tackle this as three independent fixes that must all land before the pipeline works. For `payload_filter`, parse the CLI-arg JSON with `serde_json` into a `Value`, then recursively walk every object (checking each key for exact match `__proto__`) and every string (checking `.contains("<script>")`), rejecting if a match is found anywhere in the tree including nested arrays/objects — a shallow top-level-only check fails hidden nested cases; exit 1 on reject/parse-failure, exit 0 on accept. For the routing bug, edit `/home/user/gateway/config/settings.toml` and correct `upstream_port` to `8081` to match where the backend listens (leave `bind_port` at 8080), then rebuild. For `deploy.sh`: build the workspace in release mode, create `/home/user/deploy/releases/$(date +%s)`, copy both binaries in, check if `/home/user/deploy/current` is an existing symlink (`[ -L ... ]`) and if so `readlink` its target into `/home/user/deploy/backups/last_active.txt` before overwriting, then repoint the symlink (`ln -sfn`) to the new release. After running `deploy.sh`, launch both services and confirm `curl http://127.0.0.1:8080/api` returns the backend's response.