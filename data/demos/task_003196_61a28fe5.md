Use pandas rather than naive line-splitting/bash CSV parsing — the description explicitly warns that translation_text has embedded newlines and commas, and a proper RFC-4180 CSV reader (pandas.read_csv, or Python's csv module) handles quoted multi-line fields correctly where a bash pipeline would split mid-record. Steps: read both CSVs with pandas; normalize `timestamp` to a date via `pd.to_datetime(...).dt.date` (or string-slice the ISO prefix) to get `YYYY-MM-DD`; filter out rows with `clicks < 0`; inner-merge metrics with translations on both `string_id` and `locale` (this naturally drops metrics without a matching translation, including any string_id not present at all); group by `date` and `locale` summing `clicks`; pivot with `.pivot_table(index='date', columns='locale', values='clicks', aggfunc='sum', fill_value=0)`; reset the index, sort columns alphabetically (locales) with `date` first, sort rows by date ascending, and write to CSV with `index=False`. Double-check the aggregation math per date/locale by hand against the source rows before trusting the pivot output, and actually run the script to produce `/home/user/daily_locale_clicks.csv`.