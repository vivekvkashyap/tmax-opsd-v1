Start by recovering the hidden config from the image: run `tesseract /app/config_screenshot.png -` (or via `pytesseract`) to get raw text, then parse out the `TARGET_PORT` and `RESAMPLE_INTERVAL` lines with a regex/split rather than assuming fixed values — OCR output can have stray whitespace, so strip and validate the parsed port is numeric and the interval looks like a pandas offset alias before using them. Load the CSV with pandas, call `.drop_duplicates()` on the raw rows first (exact-duplicate removal happens before resampling). Then, per `user_id`, set the timestamp as a DatetimeIndex and use `.resample(interval).agg('max')` on latency; after resampling, apply `.ffill()` but only for gaps after the first observed bucket — if the very first bucket in the group has no data, leave it NaN/null rather than forward-filling from nothing. Drop any remaining NaN latency values before serializing. Build the HTTP server (Flask/FastAPI or `http.server`) bound to 127.0.0.1 on the OCR'd port, implement `/api/user_latency?user_id=...` to filter the resampled frame for that user, sort by timestamp, and return the exact JSON shape requested (timestamp as `YYYY-MM-DD HH:MM:SS` string, `max_latency` as float). Launch it in the background and curl it to confirm before finishing.
