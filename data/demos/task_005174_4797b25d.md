Build and validate the pipeline stage by stage rather than writing the whole chain blind. First confirm you can decode the video to raw grayscale correctly: `ffmpeg -i /app/traffic.mp4 -f rawvideo -pix_fmt gray -` piped into your C program, and in C read exactly `width*height` (320*240) bytes per frame in a loop until EOF, tracking the previous frame buffer to compute mean absolute difference per frame (motion score = sum(|cur[i]-prev[i]|) / (width*height), first frame's score = 0). Group every 25 consecutive frame scores into one window and average them — get window boundaries exactly right (window 0 = frames 0-24) since an off-by-one here shifts every downstream probability. For the Bayesian step, compute the Gaussian likelihood `P(M|C)` for both classes using the given means/sigmas, combine with the 0.3/0.7 prior via Bayes' rule (`posterior = prior*likelihood_1 / (prior*likelihood_1 + (1-prior)*likelihood_0)`), being careful with floating point (use `double`, not `float`, and standard `exp`/`pow` from `math.h`, linking with `-lm`). Print CSV rows with `%.4f` for `p_congested`. Test your C program against a manually computed example (pick simple avg_motion values and hand-check the posterior formula) before trusting the full pipeline's output.