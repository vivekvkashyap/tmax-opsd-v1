Treat this as two independent hard requirements — correctness and a sub-1s latency budget — and solve them separately before combining. First, OCR the diagram with `tesseract /app/network_latencies.png stdout` and parse each output line as `<NodeX> <weight> <NodeY>` to build an edge list/adjacency structure dynamically (don't hardcode node names or weights even if you can read them, since the script must parse OCR output at runtime per the constraints). Second, for the database, first measure the naive query time for "node of the max-timestamp row where is_full=1" against 2M rows — it will be slow (full scan) — then create a covering/composite index, e.g. `CREATE INDEX idx_full_ts ON backup_catalog(is_full, timestamp DESC)`, so the query becomes an index-only lookup; verify with `EXPLAIN QUERY PLAN` that it uses the index rather than scanning. In plan_restore.sh, query the source node via sqlite3 CLI, run a shortest-path (Dijkstra) computation over the OCR-derived graph — a small embedded python/awk snippet invoked from bash is fine — from that source to the target node argument, and print only the integer total latency to stdout with no extra text. Time the whole script end-to-end yourself to confirm it's comfortably under 1.0s before finishing.
