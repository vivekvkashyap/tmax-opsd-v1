First compile the C scorer with a standard `gcc -O3 score_calc.c -o /home/user/bin/score_calc` after `mkdir -p /home/user/bin`. Write a Python wrapper that shells out to the binary (via `subprocess.run`, capturing stdout as a float) for a given `x`, then use `scipy.optimize.brentq` or `fsolve` on `f(x) = score_calc(x) - 500.0` to find the root — note the model is `GC_mean*x^2 + AT_mean*x`, so it's a simple quadratic and you can cross-check your optimizer numerically against the closed-form quadratic solution derived from the mean GC/AT counts in the dataset, rather than trusting either blindly. For the bootstrap, call `numpy.random.seed(42)` exactly once immediately before starting the 1000-iteration loop (not inside each iteration), and in each iteration draw 100 sequences with replacement from the original list, write them to a temp file, invoke the binary+optimizer on that resample, and collect the resulting x. Compute the 2.5th/97.5th percentiles of the collected bootstrap x-values with `numpy.percentile`, round all three numbers to 4 decimal places, and write them to `/home/user/result.json` under the exact keys `optimal_x`, `ci_lower`, `ci_upper`.
