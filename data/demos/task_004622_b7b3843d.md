Fix the vendored library before writing any application logic: open `lzconfig`'s core decompression module, note which standard-library calls it makes (look for `NameError`-triggering names like `zlib.*` or `json.*` used without a corresponding `import`), add the missing imports, then `pip install -e /app/vendored/lzconfig_v1.0.0` so it's importable, and confirm with a quick `python3 -c "import lzconfig; print(lzconfig.decompress(open('<some .lzc file>','rb').read()))"` against one clean-corpus file before building the filter. For `config_filter.py`: decompress to a dict, then check the three rejection rules precisely as specified — any key with an `_internal_` prefix, a `role` key whose value isn't exactly `"user"`, and any key ending in `_dir`/`_path` whose value starts with `/` or contains `../` (check substring, not just startswith, since traversal can appear mid-path). On any violation, `sys.exit(1)` and write nothing; otherwise dump the dict to YAML (`yaml.safe_dump`) at `/home/user/approved_configs/<basename>.yaml` and exit 0. Validate your logic against every file in both `/app/corpus/clean/` and `/app/corpus/evil/` yourself before considering the task done, since the grader will run the identical corpora.
