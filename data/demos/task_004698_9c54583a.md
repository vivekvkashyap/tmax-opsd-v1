Work through this as a strict pipeline where each stage's output feeds the next, deriving values from their real source rather than guessing. Transcribe /app/capacity_memo.wav (e.g. via a local speech-to-text tool) to recover the two spoken numbers — CPU threshold percentage and API port. Compute the admin PIN yourself: `stat`/`ls -l` on /app/init_capacity_db.sh for its actual octal permission mode, then concatenate that three-digit value with itself (e.g. mode "755" → PIN "755755"). Use `expect` or Python `pexpect` to drive /app/init_capacity_db.sh non-interactively with the transcribed threshold and derived PIN, confirming /home/user/capacity_config.json is produced. Lock down that file with `setfacl` (remove group/other access, grant only `user` read) and confirm via `getfacl`. Write the HTTP API bound to 127.0.0.1 on the transcribed port, requiring header `X-Planner-Auth` to equal the derived PIN, returning the config JSON plus `"status": "analyzing"`. Finally wire it up as a systemd --user unit and `enable --now` it, confirming it stays up via `systemctl --user status`.
