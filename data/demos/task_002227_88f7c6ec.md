Phase 1: analyze `/app/query_viz.mp4` frame by frame (OpenCV `cv2.VideoCapture`, iterate with `.read()`) and count frames whose pixels are exactly pure green (R=0,G=255,B=0 everywhere, or check the mean/min/max per channel across the frame equals that exactly) — don't estimate this visually, compute it programmatically and let that count be your `N`; do not hardcode a guessed frame count. Phase 2: write `/home/user/graph_query.py` to read one JSON line from stdin, then build in-memory indexes first (an adjacency list from edges, and a dict from node id to node) so lookups are O(1) rather than re-scanning the edge list per node — this matters because the equivalence test uses graphs with hundreds of nodes/edges across ~200 fuzz iterations and a naive nested loop will still pass correctness but you should still index properly per the spec. For each `User` node, check its neighbors (via the adjacency index) for any `Product` neighbor with `price > N`; collect qualifying ids, sort descending alphabetically, take the first 5, and print them comma-joined (print nothing but a newline if empty — avoid stray whitespace or extra newlines since the equivalence checker will diff output exactly). Test your script against several hand-built or randomly generated JSON graphs piped via stdin, checking edge cases like nodes with no `price` field and users with no product neighbors, before considering it done.
