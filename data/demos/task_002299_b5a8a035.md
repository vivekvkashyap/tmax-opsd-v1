Read the existing `traverse()` function in `/home/user/backup_planner.c` first: it recurses over every outgoing edge with no visited-tracking, so shared dependencies get revisited exponentially and cycles cause infinite recursion. Fix it by adding a `visited[MAX_NODES]` boolean array (or bitset) sized to node_count, checking and marking it before recursing/printing, and short-circuiting immediately if a node was already visited — this converts the naive path-enumeration into a proper DFS/BFS reachability traversal that visits each node at most once. Keep the existing graph-loading and node-lookup logic intact since it isn't buggy. Recompile with `gcc backup_planner.c -o /home/user/backup_planner`, run it with `DB_WEB` as the argument, and pipe the output through `sort` before writing to the final output path — the graph has multiple paths converging on shared dependency nodes, so the fix must guarantee each reachable database appears exactly once regardless of how many paths lead to it.
