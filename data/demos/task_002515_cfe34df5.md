First recover the fixed covariance matrix from `/app/target_cov.png` using OCR (e.g. `tesseract /app/target_cov.png stdout`, or Python's `pytesseract`) — inspect the raw OCR text carefully for misread digits/brackets before trusting it, and cross-check by re-rendering the image at higher contrast/scale if characters look ambiguous; this 2x2 matrix Σ is a fixed constant used only in the likelihood, not something to estimate. Then implement a from-scratch Metropolis-Hastings sampler in C++: propose new mean vectors via a small Gaussian random-walk step, compute the log-posterior as log-prior (bivariate normal, mean [0,0], diagonal covariance 10I) plus the log-likelihood of the observed data in `/app/sequence_features.csv` under N(mu, Σ), and accept/reject via the standard Metropolis ratio. Run at least 4 independent chains with different starting points/seeds, and after each batch of iterations compute the Gelman-Rubin R-hat statistic per-parameter (between-chain vs within-chain variance) on post-burn-in samples; keep sampling until R-hat < 1.02 for both mu_1 and mu_2. Pool the post-burn-in samples across all chains, average them, and write the two resulting floats space-separated to `/home/user/posterior_mean.txt`. Sanity-check your sampler's acceptance rate (aim for roughly 20-50%) by tuning the proposal step size.