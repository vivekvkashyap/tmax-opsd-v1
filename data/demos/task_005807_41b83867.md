First inspect the actual schema with `sqlite3 /home/user/research_data.db ".schema"` — don't assume column names, confirm them. Build the query in layers: (1) a subquery/CTE that joins papers to citations and does `COUNT(*)` grouped by paper to get total citations per paper, filtered to `year > 2010`; (2) apply `RANK() OVER (PARTITION BY author_id ORDER BY TotalCitations DESC)` on that result — use `RANK`, not `ROW_NUMBER`, so tied top scores for the same author both survive; (3) filter to `rank = 1`; (4) join back to authors for the name; (5) final `ORDER BY TotalCitations DESC, PaperTitle ASC LIMIT 10`. Write this as one `sqlite3` invocation (heredoc or `-cmd`/`.mode`/`-separator '|'` flags) inside `/home/user/analyze.sh`, redirecting output to `/home/user/top_papers.txt`, and `chmod +x` the script. Run it yourself and diff the output shape against the pipe-separated `AuthorName|PaperTitle|TotalCitations` spec before considering it done — pay special attention to getting the tie-breaking and the year cutoff (strictly greater than 2010) exactly right.
