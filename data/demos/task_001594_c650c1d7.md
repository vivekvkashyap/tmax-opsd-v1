Implement this as a single streaming pass over stdin in Go, being careful about the order of operations. Steps: (1) Decode each JSON line with `encoding/json` into a struct with fields `ts`, `id`, `tk`, `h` — use pointer/optional handling or check the raw value so a missing `tk` is distinguishable from `tk: 0` if needed, though here both should be rejected. (2) Apply the validation gate exactly as specified: reject if `tk <= 0`, reject if `h < 0 || h > 100`, reject if `id == ""` — apply this BEFORE computing any derived features, since rolling stats must only be computed over valid records. (3) Convert Kelvin to Celsius with `tk - 273.15`. (4) Maintain a sliding window of the last up-to-3 valid `temp_c` values (e.g. a slice you append to and truncate from the front once it exceeds length 3), and average whatever is currently in the window — this average, not a full-history average, is the "rolling" stat, and it must be recomputed after each valid record including when the window isn't yet full. (5) Format both `temp_c` and the rolling average with `%.2f` and write CSV rows with `ts,id,temp_c,rolling_avg_temp_c`, only for valid rows. Test by running the program against `/home/user/input.jsonl` and manually hand-tracing a couple of rows to confirm the window/average logic before trusting the output.