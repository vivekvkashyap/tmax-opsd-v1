This is a two-bug vendored-package fix — locate both bugs by trying to build first, reading the compiler/linker error, then fixing precisely rather than rewriting the project. Run `make` in `/app/wal-serve-v1.0` and observe the linker error; it will point to unresolved zlib symbols (e.g. `gzopen`), so fix the `Makefile` by adding `-lz` to the linker flags (LDFLAGS/LDLIBS), keeping the flag order correct (library flags typically go after the object files being linked). Separately, grep `server.cpp` for the hardcoded data directory macro/string and change it to point at `/home/user/dataset/` (match trailing slash conventions used elsewhere in the file). Rebuild with `make` until it succeeds, then start the server explicitly in the background so it survives your shell session ending, e.g. `nohup ./wal_server 9090 >/tmp/wal.log 2>&1 &` (or use `disown`), and confirm with `curl http://127.0.0.1:9090/records` that it returns HTTP 200 and valid JSON before finishing. If the dataset directory or gzip fixture files are missing, check whether they were meant to already exist versus needing you to create test data consistent with what the server code expects to read.