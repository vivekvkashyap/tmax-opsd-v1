Start by transcribing `/app/voicemail.wav` yourself — install an offline STT tool (e.g. `whisper` via pip, or apt's `pocketsphinx`) and run it against the file, then read the transcript carefully rather than guessing at the spec; the exact I/O format, sort order, and edge-case rules all come from that audio and must be extracted precisely (don't rely on the task description alone, it's deliberately incomplete). Once you have the requirements, implement diff_tool.c to read the "old" manifest from stdin and the "new" manifest from the argv[1] file path, using a consistent line format (path + separator + checksum) as described; sort both entry sets by path, then walk them in merge fashion to classify each path as add/remove/modify per the transcribed rules, remembering to apply every special-case rule mentioned in the recording (e.g., certain extensions being ignored, zero-checksum handling) exactly as stated. Sort final output the way the recording specifies, print one line per change, compile with `gcc -O2 diff_tool.c -o diff_tool`, and stress-test manually with a handful of hand-built manifests (including edge cases from the audio) before trusting it against the fuzzer.
