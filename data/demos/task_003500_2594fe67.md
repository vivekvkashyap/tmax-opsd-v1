Work phase by phase and validate each output before feeding it forward. For feature extraction, use ffmpeg's scene-detection/frame-difference filters (e.g. the `select='gt(scene,0)'` filter with `showinfo`, or extract per-frame stats via `-vf "signalstats"` and parse the loglevel output) to get a per-frame difference score, and deliberately inject/preserve NaN for the frames that are described as periodically corrupted — write frame_index,diff_score pairs to frame_stats.csv with literal `NaN` text (not 0) for those rows, and inspect the CSV by eye to confirm both real scores and NaN rows are present before writing the C++ side. In pipeline.cpp, parse each diff_score field carefully: check for the literal "NaN" string (or use a strtod-based parse that flags failure) and skip/drop that row entirely rather than letting a naive stod/atof fallback silently coerce it to 0 — this exact silent-coercion bug is what the task warns will skew your regression. Compute the 5-window rolling average only over consecutive valid (post-drop) frames, implement ordinary least squares (closed-form slope/intercept from scratch, no external libs) to predict row N+5's rolling average from row N's, split chronologically 70/30 train/validation, and time only the inference loop over the validation set with a wall-clock check to confirm it's under 50ms. Compile with `g++ -O3`, run it, and check the resulting predictions.csv's actual vs predicted columns visually before trusting the MSE is below 0.05.
