Work through the three layers in order, verifying each before moving on. First, run the failing install and read the pip error carefully — a requirements.txt often has one bogus/unpublished package name mixed in with valid pins; remove or correct just that line rather than rewriting the whole file. Second, once the venv builds, run the script and tail `logs/process.log` (it uses Python logging) to see exactly which row was being processed when it crashed — a LEFT JOIN between users and transactions can produce a NULL/None aggregate for a user with no matching rows, so any comparison like `row['total'] > 0` will throw on None; guard with a None/COALESCE check (either in SQL via `COALESCE(SUM(...),0)` or in Python) and decide whether that user should be excluded per the spec (total 0 or null must be filtered out). Third, re-read the requirement text precisely: filter status='active', exclude zero/null totals, and sort by total descending before writing JSON — check for a missing `ORDER BY` or missing `sort()` call. After each fix, rerun `run.sh` end-to-end rather than assuming a single fix resolves everything, since bugs are layered (build, then runtime crash, then logic).