Treat Phase 2 as a black-box equivalence problem: your C++ program's stdout must match /app/reference_organizer for arbitrary (including malformed) input lines, so infer its exact parsing rule empirically — pipe a range of hand-crafted and random test strings (with/without subdirectories, with/without a trailing 6-digit-before-.jpg number, extra digits, missing extension) through the reference binary and compare against your implementation before trusting it, rather than guessing the regex from the prose alone. Key edge cases to get right: the number must be exactly the last 6 digits immediately preceding `.jpg` at end of string (use a regex anchored with `$`), subdirectory flattening replaces every `/` with `_` in the target name (including no leading path when there's no subdir), and anything not matching goes to `ACTION: IGNORE` verbatim. For Phase 1, extract at exactly 5 fps into 6-digit zero-padded jpgs with ffmpeg's `-r 5` and printf-pattern output naming. For Phase 3, drive your organizer with the real filenames read from /home/user/raw_frames (feeding relative paths on stdin), then actually create hardlinks (`link()`/`ln`) for HARDLINK targets and *absolute-path* symlinks (`ln -s $(realpath ...)`) for SYMLINK targets in the corresponding train_set/val_set directories — verify with `stat -c %i` (inode match) and `readlink` afterward.