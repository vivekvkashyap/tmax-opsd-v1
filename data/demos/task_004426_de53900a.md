Build `/home/user/proxy_worker.sh` to read the raw HTTP request from stdin line by line (a `while IFS= read -r line` loop works, but watch for lines with trailing `\r` — strip it) until the blank line ends the headers; while reading, check case-sensitively (or normalize case) whether any header line matches `X-Block-Me: true`, and if so immediately print a well-formed `HTTP/1.1 403 Forbidden` status line plus a blank line and exit without touching the backend. Otherwise, buffer the whole request (headers + any body, using `Content-Length` if present) and forward it verbatim to `127.0.0.1:8080` using a `/dev/tcp` file descriptor (`exec 3<>/dev/tcp/127.0.0.1/8080`) or `nc`, then read the backend's full response. Process the response header-by-header as you forward it back to stdout: inject `Content-Security-Policy: default-src 'self'` as a new header line; for any `Location:` header whose value starts with `http://` or `https://`, replace the value with `/error` (leave relative paths untouched); for any `Set-Cookie:` header lacking `HttpOnly`/`Secure`, append `; HttpOnly; Secure` before writing it out. Preserve the body unmodified. Start `/app/auth_service`, launch `socat TCP-LISTEN:9000,reuseaddr,fork EXEC:/home/user/proxy_worker.sh`, and manually curl each vulnerability case to confirm before considering it done.
