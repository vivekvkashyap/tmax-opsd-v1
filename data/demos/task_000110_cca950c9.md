Fix the three layers independently and test each with direct tool commands before wiring them together. First replace the broken `$lookup` in the aggregation pipeline inside `/app/server.js` with `$graphLookup` (`from: "employees", startWith: "$_id", connectFromField: "_id", connectToField: "managerId"`, or equivalently start the pipeline by matching `managerId: manager_id` and recursing) so it only walks the hierarchy under the given manager rather than joining the whole collection — test this stage alone in `mongosh` against the `corp` database until the returned document set is exactly the subtree you expect. Second, after unwinding/flattening the graphLookup results, add `$sort: { name: 1 }`, then `$skip: (page-1)*limit` and `$limit: limit` as pipeline stages in that order (skip before limit), converting the query params from strings to integers. Third, in `mongosh` run `db.employees.createIndex({managerId: 1})` to add the missing index, and verify with `db.employees.getIndexes()`. Finally start the server with `node server.js` bound to `127.0.0.1:8000` in the background and confirm with `curl` against a few page/limit combinations that the JSON output is sorted and paginated correctly before finishing.
