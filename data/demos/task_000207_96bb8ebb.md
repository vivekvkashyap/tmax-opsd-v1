Split this into an independent video-analysis step and a classifier-build step. For the video: use `ffprobe -v error -count_frames -select_streams v:0 -show_entries stream=nb_read_frames /app/experiment_run.mp4` (or extract frames with ffmpeg to a temp dir and count files) to get the exact frame total, and extract just the first 10 frames (e.g. `ffmpeg -i ... -vframes 10 frame_%02d.png`) to compute their average pixel intensity (grayscale mean across all pixels/frames, e.g. via Python+PIL or ImageMagick `convert -colorspace Gray -format "%[mean]"`); write "total_frames,avg_intensity" to video_stats.txt. For the classifier: don't guess a model — inspect the given clean/evil sample CSVs directly (load a few files, compute per-column mean/variance) to see which of columns 3/4 actually separates the classes and by roughly what variance magnitude, then write simple C++ that reads a directory (opendir/readdir over *.csv), computes the variance of that column per file, and thresholds it to output 0/1 predictions into predictions.txt as "filename,label". Validate against /app/data/clean and /app/data/evil (100% accuracy required) before trusting it will generalize to the hidden corpus, and compile to /home/user/classifier exactly.
