Load both sources with pandas: read `run_1.csv` normally (timestamps are already Unix epoch seconds — convert with `pd.to_datetime(..., unit='s', utc=True)`), and parse `retry_1.json` line-by-line (JSON Lines, not a single JSON array) with `pd.read_json(..., lines=True)`, converting its ISO 8601 `datetime` strings with `pd.to_datetime(..., utc=True)` so mixed timezone offsets are correctly normalized to UTC rather than naively stripped. Concatenate both into one frame with unified column names (e.g. rename `val_a`/`val_b` to `sensor_a`/`sensor_b`), then round the UTC timestamp to the nearest second (`.dt.round('S')`) before grouping — do this rounding precisely, since jittered timestamps a fraction of a second apart must land in the same bucket to be merged. Group by the rounded timestamp and take `.mean()` per sensor column with `skipna=True` (pandas default) so nulls don't poison the average, then drop any group where either mean is still NaN. Sort by timestamp, format it back to `...Z` ISO strings, round sensor values to 3 decimals, and write the CSV with the exact three-column header. Separately compute the Euclidean distance as `sqrt(sum((sensor_a - sensor_b)**2))` over the final aligned arrays (not a raw statistical distance metric) and round to 2 decimals for `distance.txt`.