First get the vendored tool working in isolation: run `/app/txt2csv-tools-1.2/convert.sh` on a sample `.dat` file, read the error, and inspect the script for a variable holding the name of the text-processing binary it invokes — it's misspelled, causing a "command not found"; correct the spelling so it resolves to the real binary on `$PATH`. Confirm the fix by running `convert.sh` directly against one file in `/home/user/raw_data/` and checking valid CSV comes out on stdout. Then build `organize.sh`: for atomic writes, redirect each conversion's stdout to a `mktemp` file in `/home/user/processed/` and `mv` it into place only after the command succeeds — never write directly to the final filename. Parse the date from line 1 of the produced CSV (matching `YYYY-MM-DD`), `mkdir -p /home/user/by_date/<date>/` and `ln -s` back to the processed file (use absolute paths in the symlink target). For performance, do not use a sequential `for` loop over thousands of files — instead export the per-file logic as a function/one-liner and drive it with `find /home/user/raw_data -name '*.dat' -print0 | xargs -0 -P "$(nproc)" -I{} ...` (or GNU `parallel`) so files are converted concurrently. Time your script (`time ./organize.sh`) against the full dataset to confirm it meets the threshold before finishing.
