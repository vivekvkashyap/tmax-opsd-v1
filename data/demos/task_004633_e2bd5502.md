The bug is that trapezoidal integration with variable step sizes requires each segment's own width (t[i] - t[i-1]), not a single constant delta multiplied across all points. Rewrite the core loop (in `awk` inside the bash script) to track the previous time/value pair and accumulate `area += (t_i - t_prev) * (y_i + y_prev) / 2.0` for each consecutive pair, skipping the header line (`/^>/`), and only start accumulating once you have a previous point. Print the final sum with exactly 4 decimal places (`printf "%.4f\n", area`). Since correctness is fuzz-tested against an oracle across many random variable-step files, test your script against several hand-crafted inputs with irregular time gaps (not just uniform ones) to catch edge cases like single-point files or duplicate timestamps. For the service wrapper, use `curl` to fetch from `http://127.0.0.1:8000/signal/test1` into `/tmp/test1.dat`, pipe that file through your fixed integrator, and store the result with `redis-cli -p 6379 SET integral:test1 <value>` — confirm both services are reachable first with a quick `curl`/`redis-cli PING` check.