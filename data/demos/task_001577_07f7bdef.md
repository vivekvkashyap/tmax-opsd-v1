Work through the linear algebra by hand/script first to know what your C program should produce, then implement it faithfully. Parse `/home/user/observations.txt` splitting each line on `;` to get y, x1, x2 as doubles (using `sscanf` or `strtok`/`strtod`). Build the 2x2 matrix `X^T X` (sums of x1*x1, x1*x2, x2*x2 across rows) and the 2-vector `X^T y` (sums of x1*y, x2*y), add the prior precision `0.1` to both diagonal entries of `X^T X`, then invert this 2x2 matrix analytically (adjugate/determinant formula for a 2x2 — no need for a full LAPACK dependency) and multiply by `X^T y` to get the posterior mean vector. Watch two common failure points: accumulate sums as `double` (not `float`) to avoid precision loss, and use correct 2x2 matrix inversion (swap diagonal, negate off-diagonal, divide by determinant) rather than a naive Gaussian elimination that can introduce bugs for a 2x2 case. Print and write results with `printf("beta1=%.3f, beta2=%.3f\n", ...)` matching the exact required format, then verify by compiling with `gcc -o posterior posterior.c -lm` and running it, checking the output file matches the printed console output.
