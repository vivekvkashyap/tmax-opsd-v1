Write `curate.go` to strictly follow the JSON config rather than hardcoding: unmarshal `/home/user/config.json` into a struct with `source_file`, `chunk_size`, `chunk_prefix`, `dest_archive` fields. Read the source file fully, then slice it into `chunk_size`-byte pieces (last chunk shorter), naming each with the prefix and a two-digit zero-padded index (`fmt.Sprintf("%s%02d", prefix, i)`) — write these chunk files to a temporary working directory (e.g. via `os.MkdirTemp`). Build the tar.gz by adding each chunk file at the archive root (set the tar header `Name` to just the base filename, not a path with the temp directory prefix — this is the easiest place to get wrong) using `archive/tar` wrapped in `compress/gzip`, writing first to a temp file in the same directory as `dest_archive` (so the rename is on the same filesystem), then calling `os.Rename` to atomically move it into place — this atomicity is explicitly checked by the verifier via a `grep` for `os.Rename`, so don't shell out to `mv` for the final step even though sourcing chunk creation via `os/exec` is allowed. Finally, delete the temp directory and any leftover intermediate files. Run `go run curate.go` and inspect the resulting archive by extracting and diffing reconstructed bytes against the original firmware file.
