Fix the vendored build before writing any of your own code: open the `Makefile` at `/app/vendored/seq-mcmc-1.0` and look closely at the CFLAGS line for an OpenMP flag typo (a misspelled `-fomp`-style flag will fail to compile) — correct it to the real flag and rebuild the library to a `.o`/`.a` so `compute_artifact_posterior()` is available. Only after the library builds cleanly should you write `detector.c`: parse the FASTA file yourself (split on `>` headers, concatenate sequence lines per record), then score each parsed sequence by calling into the library function, using `#pragma omp parallel for` to parallelize across sequences within the file (make sure each thread writes to its own array slot, not a shared scalar, to avoid races). Track whether any sequence's posterior exceeds 0.80 strictly; exit 1 if so, else exit 0. Compile `detector.c` linking the fixed library and `-fopenmp` on the compiler invocation itself too (both the library and your C file need the flag) — verify with `ldd /home/user/detector | grep libgomp` that it actually links dynamically. Put the working compile command in a bash cell of `/home/user/pipeline.ipynb` and sanity-test against `/app/corpus/clean/` and `/app/corpus/evil/` before finishing.
