Two inputs feed fixed simulation parameters, so extract them first and treat them as given constants inside your Go program (do not hardcode arbitrary numbers). Use `ffprobe` (part of ffmpeg, likely preinstalled) on `/app/cell_video.mp4` to read the video's pixel width `W` and total frame count `F` (e.g. `ffprobe -v error -select_streams v:0 -count_frames -show_entries stream=width,nb_read_frames -of csv=p=0 /app/cell_video.mp4`) — these become your mesh size and step count respectively, discovered from the file rather than assumed. In the Go program, read all of stdin, strip any line starting with `>` (FASTA header) and concatenate the remaining lines (trimmed of whitespace/newlines) to get the sequence; its length is `L`. Allocate a float64 slice of size `W`, set `mesh[W/2] = L` (integer division), and run exactly `F` explicit finite-difference diffusion steps with `alpha=0.25`, updating into a second buffer each step and swapping (synchronous update, not in-place) so you don't read partially-updated neighbors — critical for numerical correctness. Keep the boundary elements (index 0 and W-1) pinned at 0.0 every step. Print only the final `mesh[W/2]` with `%.6f\n`. Since a fuzz-equivalence check compares your compiled `/home/user/solver` against a reference across many random FASTA inputs, get the update order and rounding exactly right.