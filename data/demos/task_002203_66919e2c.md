This task has two independent fixes plus a streaming pipeline; don't try to patch around the vendored library instead of fixing it. First, grep through `/app/vendored/jsonlines-3.1.0/jsonlines/jsonlines.py` for a conditional that inspects each raw line for the substring `\u` (unicode escape marker) and raises an error — this is a deliberate sabotage that rejects perfectly valid JSON containing escapes like `°`; delete or neutralize that check so normal `json.loads`-based parsing proceeds. Second, write `/home/user/process_series.py` to accept `<input> <output>` argv, and stream the input line-by-line via the fixed vendored reader (never load the whole file into a list). For each parsed record: (a) recursively search the full record for a key named `ssn` or `email` at any nesting depth and drop the record entirely if found (also exclude it from being used to interpolate neighbors); (b) skip writing if `sensor_payload` is identical to the immediately preceding kept record; (c) when consecutive timestamps have a gap, synthesize the missing integer timestamps, copying the previous payload's structure but linearly interpolating the nested `value` float between the two bounding records. Test against the sample data in `/home/user/sample_data/` before trusting the pipeline against the hidden clean/evil corpora.
