Build this as a short bash/awk pipeline, being careful about off-by-one and indexing details. First flatten the sequence: use `grep -v '^>'` (or `tail -n +2`) on `/home/user/dna_sequence.fasta` and concatenate all lines into one string with `tr -d '\n'`, e.g. via `awk`. Then implement the sliding window in awk or a bash loop: for window index i starting at 0, take the substring starting at position `i*50` (1-indexed for awk's `substr`, so offset by +1) of length 100, stopping once fewer than 100 bases remain — get this boundary condition right or you'll generate one extra/missing window. For each window, count G's and C's (e.g. `tr -cd 'GC' | wc -c` or an awk character loop). Store all window GC counts in an array, then compute the absolute difference between each consecutive pair with `awk` (`d = (a>b)?a-b:b-a`). Track the maximum difference and its first-occurring index as you go (so ties resolve to the lowest index). Write the final result to `/home/user/gc_gradient_max.txt` in exactly the `Index: <idx>, MaxDiff: <val>` format — verify your window count and boundaries against the file length before trusting the output.