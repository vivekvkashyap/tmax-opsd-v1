Work in three passes. First, audit the C loop that walks the sensor array: an off-by-one bound (using `<=` instead of `<` against the length) is the classic cause of intermittent crashes/garbage on large inputs — fix the comparison operator so the loop never reads past the last valid index, then rebuild the static/shared library cleanly. Second, write the Go service with cgo bindings (`import "C"`, cgo LDFLAGS/CFLAGS pointing at the legacy sources) exposing `POST /api/v1/process` on 127.0.0.1:8080; check the `Authorization: Bearer <token>` header on every request and return 401 before touching the C call if it's missing or wrong. Third, wire in a Redis client (e.g. go-redis) to SET `batch:<batch_id>` to the computed aggregate before writing the HTTP JSON response, so cache and response never disagree. Finally, edit `/app/start.sh` to launch `redis-server --daemonize yes`, wait briefly for it to accept connections, `go build` your service, then exec the resulting binary in the foreground so the script's own process stays alive. Test with a small payload and then a very large one (thousands of ints) to confirm the C fix actually prevents the crash under load.