Start by inspecting the schema directly (`sqlite3 /home/user/audit.db ".schema"` and `.tables`) to identify the access-log table and its column names, rather than assuming names — the task deliberately doesn't give you the table/column names up front. Once found, write a C program using the SQLite3 C API (`sqlite3_open`, `sqlite3_prepare_v2`, `sqlite3_step`, `sqlite3_column_*`) whose query does two specific things: uses `FROM <table> NOT INDEXED` to force a full table scan bypassing any corrupted index, and uses a window function like `ROW_NUMBER() OVER (PARTITION BY employee_id ORDER BY event_time DESC)` filtered to `action = 'DENIED'` and `row_num = 1` (a subquery/CTE selecting rank 1 per employee) to get each employee's most recent denial. Order the final result by `employee_id` ascending, either in SQL with `ORDER BY employee_id` or by sorting in C after fetching rows. Write each result row as `employee_id,timestamp` (no header) directly to the output CSV via `fprintf`. Compile with `gcc audit_extractor.c -o audit_extractor -lsqlite3` and run it, then diff the CSV against what you'd expect from manually reasoning through the DENIED events per employee.