Treat the stripped binary as a black box you must characterize purely through input/output probing, then reimplement in C++ to match byte-for-byte. Systematically probe with: empty input, single lines with/without commas, lines with embedded emails in different positions/formats, consecutive lines to observe the rolling-distance metric, and multi-line inputs to see the output field separator/ordering. Key things to nail down by experiment: (1) exactly what happens to lines lacking a delimiter (dropped vs errored), (2) the precise email-matching pattern (test edge cases like uppercase, subdomains, missing TLD) and its replacement token, (3) whether the distance metric is computed against the raw or already-masked previous text, and against which field, (4) the exact output line format/ordering/delimiters, including any trailing newline behavior. Since the bug preserving line-by-line reading (breaking on embedded newlines in "CSV" rows) is explicitly part of the spec you must mimic, do not try to correctly parse CSV — replicate the naive `getline`-based behavior exactly. After writing `solution.cpp`, compile with the exact flags given, then self-test by diffing your binary's output against the oracle on many random inputs (including edge cases) before considering it done.
