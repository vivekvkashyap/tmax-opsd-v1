Fix the C library first, in isolation, before touching Rust: inspect the Makefile's link rule for `libcsched.so` — a shared library build needs both `-fPIC` on the compile step and `-shared` on the link step, so add both. Then read `calculate_schedule` in schedule.c carefully for the loop bound writing into `output_schedule`; an off-by-one that iterates through `num_tasks` inclusive will write one past the allocated array, so tighten it to a strict `<` bound. Run `make` and test the resulting `.so` standalone (e.g. via a tiny C test harness or `ctypes` from Python) before wiring up Rust FFI, to isolate whether crashes come from C or Rust. In Rust, declare the `extern "C"` signature matching the C header exactly, allocate an output buffer sized to the input task count, and pass raw pointers/lengths correctly. Build the auth check as middleware or an explicit header comparison against the literal bearer token, returning 401 otherwise. Finally set `LD_LIBRARY_PATH` to the vendor library's build directory when launching the compiled Rust binary, run it in the background, and write its PID to the required file so the verifier can find it listening.