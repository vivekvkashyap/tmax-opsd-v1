Reproducibility of exact numeric output depends entirely on matching the specified generation and consumption order of the two PRNGs, so implement it literally rather than optimizing prematurely. Initialize `rand.New(rand.NewSource(42))` once and generate all 200 30x30 matrices sequentially first (row-major, calling `Float64()` once per element, in one single-threaded loop) before any goroutines start — parallelizing generation itself would desynchronize the RNG stream from the spec. Then set up exactly 4 worker goroutines (e.g. a work-queue/channel of matrix indices) that each call `gonum.org/v1/gonum/mat`'s SVD to get singular values per matrix and compute condition number = max singular value / min singular value, collecting results back keyed by original index (order doesn't matter here since results are sorted next) via a mutex or channel — use `sync.WaitGroup` to wait for completion. Sort the 200 condition numbers ascending, compute their mean, then create a second RNG `rand.New(rand.NewSource(99))` and run the 10,000-resample bootstrap strictly sequentially: each resample draws 200 indices via `rand.Intn(200)` from the sorted slice, computes that resample's mean, collect all 10,000 means, sort them, and take index 250 and 9750 as the CI bounds. Initialize the go module, `go get gonum.org/v1/gonum`, run the program, and write mean/ci_lower/ci_upper to `/home/user/results.json`.
