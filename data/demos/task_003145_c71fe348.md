Work through the binary file in stages rather than trying one command to do it all. First, use `dd` or Python file seeking to split `/home/user/doc_bundle.bin`: bytes [0:128) are an opaque header (skip it), and starting at offset 128 is a gzip stream — locate where the gzip stream ends (gzip has a known trailer; you can let `gzip`/`tarfile`/`zlib` decompress and report how many bytes it consumed, or scan for the next gzip magic won't help since there's only one — instead decompress with a streaming decompressor and note the consumed byte count) so you know where the trailing build log begins. Extract the tar stream to a temp buffer and iterate over `tarfile` members manually rather than calling `extractall` directly — for each member, strip/normalize `..` path components (e.g. via `os.path.normpath` on a path forced relative, or reject/redirect leading `../`) so nothing writes outside `/home/user/docs_safe/`. For each extracted file, sniff/attempt-decode as UTF-16LE (check BOM) then ISO-8859-1, and rewrite as UTF-8 in place. For the trailing log bytes, decode as ISO-8859-1, re-encode to UTF-8, then use a regex that splits on lines starting with `[TAG]` to group multi-line records, keeping only records tagged `[WARNING]` (with their continuation lines) into `/home/user/warnings.log`.