Work top-down through the failure chain rather than guessing. First run `python3 /home/user/process_logs.py` and read the RuntimeError message carefully — it will name a required environment variable and its expected value; `export` that variable before rerunning. Once it runs, it will crash on a malformed JSON line in `/home/user/server.log`; instead of manually eyeballing 1000 lines, write a small temporary bisection script that does a binary search over line ranges — call `process_line` (or `json.loads`) on chunks of the file, narrowing to a smaller and smaller range containing the bad line by catching the exception, until you isolate the exact single offending line, then write that exact line's raw text (no extra whitespace/newline) to `/home/user/bad_log_line.txt`. Next, edit `process_logs.py`'s `process_line` to wrap the `json.loads` call in a try/except for `json.JSONDecodeError` and `raise LogFormatError("Invalid log format")` (use the exact message string, and the exception class already defined in the file). Finally write `/home/user/test_regression.py` using `unittest`, importing `process_line, LogFormatError` from `process_logs`, and assert `process_line(<the bad line>)` raises `LogFormatError` via `self.assertRaises`. Run `python3 -m unittest /home/user/test_regression.py` yourself (with the env var set) to confirm it passes before finishing.
