Parse `/home/user/etl_logs.txt` line by line with a strict regex anchored to the exact format `[YYYY-MM-DDThh:mm:ssZ] LEVEL JOB_ID - Message text`; any line that doesn't fully match should simply be skipped, not error out. For each parsed record, normalize the message with Python's `unicodedata.normalize('NFKC', text)`, then `.lower()`, then `.strip()` — do this normalization only for comparison/output purposes while preserving the original timestamp string as-is in the output. For deduplication, process records in file order and maintain, per `(job_id, normalized_message)` key, the timestamp of the most recent kept occurrence; parse timestamps with `datetime.strptime`/`fromisoformat` and compute the delta in seconds — if a new record's delta from the last kept occurrence of that same key is <= 300 seconds, discard it as a duplicate; if it's >= 301 seconds (or no prior kept occurrence exists), keep it and update the "last kept" timestamp for that key. This is a rolling/chained window comparing against the last *kept* record, not the immediately-preceding raw line. Write kept records as one JSON object per line to `/home/user/cleaned_logs.jsonl` with keys `timestamp`, `level`, `job_id`, `message`, preserving overall file order.