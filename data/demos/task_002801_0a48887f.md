First read `sim_expression.py` to find exactly where non-determinism enters: it shuffles the mesh's `(i,j)` keys before summing in `float32`, so the accumulation order (and thus rounding) varies run to run. Fix this minimally by iterating over `sorted(mesh.keys())` (lexicographic tuple order) when doing the `np.float32` accumulation, leaving everything else (the mesh values, output file format) untouched. For `analyze.py`, invoke the fixed script via subprocess or exec for N=16/32/64 (e.g. `python3 sim_expression.py 64` as a subprocess call, or run three times with sys.argv patched), then parse each `output_N.txt`: first line is total_sum, remaining lines are the flattened grid in the same sorted order used in the fix. For N=64, load those grid values into a numpy array, normalize by dividing by their sum so they form a valid probability distribution, build a uniform distribution of the same length (`1/(N*N)` each), and pass both to `scipy.stats.entropy(normalized, uniform)` to get the KL divergence (mind argument order: first array is P, second is Q). Write `sum_64` and `kl_div_64` as floats into the required JSON schema, and run the whole pipeline end-to-end to confirm `results.json` is produced.
