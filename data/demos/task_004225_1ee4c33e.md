Bring up the databases first (`docker-compose up -d` for the Mongo/Postgres services), then inspect what data actually landed in each (query Mongo's `activity.events` collection and Postgres's `users` table directly) before writing the endpoint logic — don't guess field names. In `etl_service.py`: (1) check the `Authorization` header equals `Bearer etl-secret-token` and return 401 otherwise; (2) run a Mongo aggregation pipeline (`$match: {status: "completed"}`, `$group: {_id: "$user_id", total_duration: {$sum: "$duration"}}`) to get per-user completed-duration totals; (3) either bulk-insert those aggregates into a temp Postgres table or join them in Python against the `users` table pulled via psycopg2/SQLAlchemy; (4) build the final ranking with a SQL window function — `DENSE_RANK() OVER (PARTITION BY department ORDER BY total_duration DESC)` — either by writing the aggregated rows into a temp table and running this in SQL, or replicating the same dense-rank semantics in Python per department group; (5) serialize to JSON preserving the field names `user_id, username, department, total_duration, department_rank`. Bind Flask to `127.0.0.1:8080` and test both the unauthenticated (401) and authenticated (200) paths with curl before finishing.
