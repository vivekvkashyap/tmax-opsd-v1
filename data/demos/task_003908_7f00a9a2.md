Tackle the three layers in order, verifying each before moving on. C library: inspect `src/strproc.c` in `/app/vendored/libstrproc-1.0.2` for the memory-unsafe pattern — a fixed-size stack buffer with an unbounded copy (like `strcpy` into `char buf[256]`) causes the segfault past 255 chars; fix by allocating a buffer sized to the actual input length (`malloc(strlen(input)+1)`) or bounding the copy, then recompile and confirm with a >255-char test string that it no longer crashes. Python migration: port `server.py` and `wrapper.py` to Python 3, paying attention to `ctypes` string handling — Python 3 `ctypes` calls expect `bytes`, so encode strings before passing to the C library and decode returned `bytes` back to `str`; also replace `BaseHTTPServer` with `http.server` or a small Flask/FastAPI app implementing `POST /process`. API bring-up: start the service in the background on 127.0.0.1:8080, curl it manually with a short and a >255-char string to confirm 200/correct JSON, then write `test_e2e.sh` sending 3 curl requests (including the long one) and capturing status codes to `/app/test_results.log`, leaving the server running.