Since the binary is stripped and the source is lost, treat it as a black box you characterize empirically rather than something you disassemble. Generate small hand-crafted CSV pairs covering edge cases — overlapping/non-overlapping ids, negative `value_x`/`value_y`, zero values, duplicate/out-of-order ids, missing ids — and run `/app/legacy_cleaner file_x.csv file_y.csv` on each, capturing stdout byte-for-byte (including whether there's a header line, exact delimiter, trailing newline, and sort order) so you understand the join/filter/format rules precisely before writing any bash. Implement `/home/user/cleaner.sh` using a tool with real numeric/string control such as `awk` (associative arrays keyed by id, inner join by only emitting when both files have the id, filtering rows where either value is negative, computing the product, then piping through `sort -t, -k1,1nr` for descending numeric id order) rather than shell built-ins alone. After a first draft, run a fuzz loop generating many random CSV pairs, diff your script's output against the binary's output for each, and fix any discrepancy (especially in header presence/format and how ties or missing ids are handled) until outputs are identical; `chmod +x` the script when done.
