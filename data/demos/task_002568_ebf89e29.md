Reverse-engineer `/app/legacy_filter` methodically before writing any C++: run `strings /app/legacy_filter` for clues, `objdump -d` to inspect the compiled arithmetic (look for float comparisons and a per-row `usleep`/delay call), and black-box it — feed small hand-crafted CSVs (`id,category,v1,v2,v3`) and observe which rows survive, testing boundary cases to nail down the exact filter predicate and how many rows per category get kept and in what order. Confirm your hypothesis (e.g. a row is kept when `v1^2+v2^2 <= v3`, capped at some fixed count per category) against several test inputs before committing to it in code — the exact match requirement means guessing wrong is fatal. Write `/home/user/fast_filter.cpp` replicating that exact logic, using parallelism (threads, or SIMD/vectorized math on the filter predicate) to eliminate the legacy binary's dominant cost, which is a per-row artificial delay — parallel filtering plus efficient I/O should comfortably exceed the required speedup. Keep the stratified selection deterministic and order-preserving to match legacy output byte-for-byte. Then write `/home/user/pipeline.sh` that compiles the C++ (`g++ -O3 -pthread`), and installs a `*/5 * * * *` user cron entry running the compiled binary over `/home/user/incoming/*.csv`, moving results into `/home/user/processed/`.