Write the pipeline straightforwardly in numpy/pandas rather than hand-deriving numbers: load the CSV with `pandas.read_csv` (no header, or as given), `np.clip(x, -10.0, 10.0)` then apply the sigmoid `1/(1+exp(-x))` twice — once after casting the array to `float64` and once after casting to `float32` — keeping the two pipelines fully separate so precision differences are real, not accidental (e.g. don't clip in float64 then just downcast the sigmoid output, since that understates the float32 error; cast to float32 before applying exp/sigmoid too). Compute `max_abs_error` as `np.max(np.abs(float64_result - float32_result.astype(float64)))` and `cleaned_data_mean` as the mean of the float64-cleaned array. For the benchmark, run the float32 dot-product-plus-sigmoid inference exactly 1000 times in a loop, timing the whole loop with `time.perf_counter()` before/after (not per-iteration), and report the total in seconds. Load `weights.npy` with `np.load`, make sure the matmul shape lines up (1000x5 dot 5 -> 1000 predictions), and write all three values into `/home/user/report.json` as native Python floats (`float(...)`) since numpy scalar types can break `json.dump`.
