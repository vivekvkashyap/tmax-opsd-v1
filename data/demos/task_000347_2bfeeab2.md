This task has three independent deliverables plus a virtualenv; do them in order and validate each before moving on. (1) OCR `/app/params_notes.png` with `tesseract` (or `pytesseract`), read the actual recognized text carefully to recover `CLASH_CUTOFF` and `TARGET_EPSILON` — verify the digits look sane (OCR often confuses 0/O, 1/l, and decimal points) rather than assuming a "nice" value. (2) For `filter_pdbs.py`: parse PDB `ATOM` lines by fixed-width columns (not naive whitespace split, since PDB fields can run together) to pull x/y/z and residue/atom-name; reject a file if any pairwise atom distance is strictly less than the extracted cutoff (use vectorized numpy pairwise distances for speed on large files, not a naive O(n^2) Python double-loop if avoidable), if any coordinate is NaN, or if a standard amino-acid residue lacks a CA atom; accept everything else by copying unmodified. Test against small hand-built clean/evil PDB fixtures before trusting the corpus run. (3) For `calc_com.py`: replace the shuffled-order accumulation with `math.fsum()` on the collected x, y, z coordinate lists separately (per axis) instead of `sum()`/`np.sum()`, since ordinary summation order-dependence is exactly what a naive fix misses — verify determinism yourself by shuffling atom order locally and confirming identical output across runs before considering it done.
