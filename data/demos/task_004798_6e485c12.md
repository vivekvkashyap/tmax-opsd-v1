Before editing anything, read `processor.cpp` carefully and reproduce each bug with a small manual test against `/app/oracle_processor` (pipe the same small stdin sequence, including a `-1.0` reset, into both and diff outputs) so you can see the divergence directly rather than guessing. For the memory leak: find wherever the reset path (`-1.0`) discards the old window's storage (raw `new`/array/linked list) and ensure every allocation is properly freed there — switching the window container to `std::deque`/`std::vector` and just calling `.clear()` on reset is the simplest fix and avoids manual leak bookkeeping; verify with `valgrind --leak-check=full` after a stream containing several resets. For precision, replace the `E[x^2]-E[x]^2` formula with Welford's online algorithm (track `mean` and `M2`, update incrementally as elements enter/leave the window) since naive variance catastrophically cancels for large values with small spread. For the boundary bug, make sure you evict the oldest element so the window never holds more than exactly `K` elements while computing stats — check whether the current code updates stats before or after evicting, and fix the ordering to match a strict sliding window of size `K`. Recompile to `./processor` and diff outputs against the oracle across many randomized streams (varying value ranges and K) before considering it done.