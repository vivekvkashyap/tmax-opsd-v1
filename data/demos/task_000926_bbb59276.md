Build the three components to interoperate exactly as specified, not independently. `watchdog.py`: use `subprocess.Popen` to launch `python3 /home/user/mock_email_daemon.py` in a loop; call `.wait()`, and on any exit (regardless of code) immediately append the exact string `[SUPERVISOR] daemon restarted\n` to `/home/user/watchdog.log` and relaunch — use append mode and flush so the log is visible to external checks quickly. `net_route.py`: a TCP proxy on `127.0.0.1:9025` that, per accepted client connection, opens its own new socket to `127.0.0.1:8025`, sends the literal token string first (before any client bytes reach the daemon), then relays bytes bidirectionally — implement this with two threads (or select/asyncio) per connection so it doesn't block one direction while data is flowing the other way, since the metric exporter needs a request/response round trip to work. `metric_exporter.py`: on a 1-second loop, open a fresh connection to 9025, read the banner, send `EHLO monitor\r\n`, check if the response starts with `250`, append `email_service_up 1` or `_ 0` accordingly to `/home/user/observability.log`, then `QUIT`. Test the whole chain locally end-to-end (start daemon, proxy, exporter, watch the logs grow) before considering it done, since a bug in any one piece breaks the others silently.