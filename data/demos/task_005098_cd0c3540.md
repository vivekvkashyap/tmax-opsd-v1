Fix the three bugs in `/home/user/analyze_artifacts.py` in order and verify each fix independently. First, the Matplotlib backend: replace `matplotlib.use('TkAgg')` with `matplotlib.use('Agg')` (must be set before importing `pyplot`) so it works headless. Second, the PCA math: the script runs SVD directly on the raw stacked data without mean-centering, which is mathematically wrong — PCA requires centering each feature column first (`X_centered = X - X.mean(axis=0)`) before `np.linalg.svd`, then project with `X_centered @ Vt.T[:, :2]`; verify by checking that projected data has ~zero mean, confirming the centering took effect. Third, add the missing computations: Euclidean distance between the 2D centroids of Model A's and Model B's projected points (`np.linalg.norm(mean(A_pca) - mean(B_pca))`), and an independent two-sample t-test with equal variances (`scipy.stats.ttest_ind(A_pca[:,0], B_pca[:,0])`, default `equal_var=True`) on PC1 only — PC1 is whichever of the two output columns has larger variance, so verify column order rather than assuming column 0 is always PC1. Round/format the distance to 4 decimals and the p-value in scientific notation with 4 decimals, writing each to its respective file, then rerun the script to regenerate `pca_plot.png`.