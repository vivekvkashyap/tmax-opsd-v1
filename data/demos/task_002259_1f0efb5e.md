Open the SQLite file with the `sqlite3` CLI first and inspect `edges`/`backup_runs` row counts to confirm which columns are hot in the query plan. For the graph traversal, prefer a recursive CTE inside SQLite itself (`WITH RECURSIVE reach(id) AS (SELECT target_id FROM edges WHERE source_id=(SELECT id FROM nodes WHERE name='db_sales') UNION SELECT e.target_id FROM edges e JOIN reach r ON e.source_id=r.id) ...`) rather than pulling the whole edge table into Python — it's both simpler and demonstrates the indexing benefit. For "most recent status per node", don't naively fetch all runs and sort in Python; use a correlated subquery or `MAX(run_time)` group-by joined back to `backup_runs`, and add an index like `CREATE INDEX idx_edges_source ON edges(source_id)` and `CREATE INDEX idx_runs_node_time ON backup_runs(node_id, run_time)` so both the recursive traversal and the latest-run lookup use an index instead of a full scan. Write the final Python script to open the DB, run these queries, filter status='FAILED', sort alphabetically, and dump to JSON with `json.dump(sorted(names))`. Test by running the script and printing the JSON before trusting it.
