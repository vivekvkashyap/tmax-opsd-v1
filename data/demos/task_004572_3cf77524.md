Set up a standard Rust binary crate with `cargo new`, then implement the pipeline in stages you can test independently. First, parse the FASTA file line-by-line: skip lines starting with `>`, and for the rest count occurrences of `K` and `R` characters across all sequence lines to get N — don't worry about multi-line sequences, just accumulate the count as you go. Second, parse the CSV: skip the header row, then parse each remaining line as an f64. Third, implement the log-likelihood-ratio comparison directly from the given formula — since the `-1/2 ln(2*pi*sigma^2)` term is identical for both hypotheses, it cancels in the difference, so LLR reduces to `(sum((x_i-mu1)^2) - sum((x_i-mu0)^2)) / (2*sigma^2)`; compute mu0 = N*5.0 and mu1 = N*8.0 first. Round LLR to 3 decimal places using proper rounding (not truncation). Determine Best_H from the sign of LLR. Serialize the result to `/home/user/result.json` with exact field names and types (KR_count as integer, LLR as float, Best_H as string) — use `serde_json` or manual string formatting, then `cargo build --release` and run the binary so the file is actually written to disk.
