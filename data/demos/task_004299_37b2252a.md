Install `papermill`, `jupyter`, `numpy`, `scipy` via pip first. Do not edit `/home/user/calc_fft.ipynb` — treat it as a fixed function: it takes parameters `valA,valC,valG,valT`, reads `/home/user/sequence.txt`, and writes a score to `/home/user/current_score.txt`. Write `/home/user/optimize.sh` to enumerate all 24 permutations of {1,2,3,4} assigned to the four variables (nested loops or an explicit permutation list in bash), and for each one invoke `papermill /home/user/calc_fft.ipynb /tmp/out.ipynb -p valA <v> -p valC <v> -p valG <v> -p valT <v>`, then read the resulting float from `/home/user/current_score.txt` (e.g. with `cat` or `read`) immediately after each run so you don't reuse a stale value from a previous iteration. Track the best (permutation, score) pair seen so far using bash variable comparisons (careful with floating-point comparison — use `awk` or `bc` for numeric comparison, not bash's native integer `-gt`). After the full grid search, write the winning permutation and its score to `/home/user/best_mapping.csv` as `valA,valC,valG,valT,score`. Make the script executable and run it end-to-end to produce the file.