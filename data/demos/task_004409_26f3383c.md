Start by probing `/app/eval_source` at a handful of points across [0,1] to characterize where f(x) is large/sharp (call it with a coarse uniform sweep first, e.g. 20-50 points, batched since it accepts up to 1000 per call) so you know where to concentrate refinement. Build the AMR loop as: (1) solve the discrete Poisson system on the current non-uniform grid via a tridiagonal (Thomas algorithm) solver using the standard non-uniform second-derivative finite-difference stencil, (2) estimate error per interval/node (e.g. compare against a locally halved-mesh solve, or use the discrete second-derivative/curvature of the current solution as a refinement indicator), (3) insert new points only in the highest-error intervals, (4) repeat until convergence or the point budget is nearly exhausted. Keep the total grid points well under 150 by refining adaptively rather than uniformly — a uniform grid fine enough to resolve a sharp spike would blow past the point budget. Write the final `<x_i> <u_i>` pairs sorted ascending by x, including both boundary points, to `/home/user/solution.txt`, and sanity-check your own error by comparing against a much finer reference solve before finishing.
