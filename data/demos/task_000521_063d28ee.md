Build the classifier as pure bash+coreutils, no interpreters. For each CSV line: split fields with `IFS=,` read (be careful of possible commas inside payload — assume none per spec), then branch on PayloadEncoding: `plain_utf8` passes through as-is; `base64_utf8` decode with `base64 -d`; `base64_utf16le` decode with `base64 -d` then convert with `iconv -f UTF-16LE -t UTF-8`. Pipe the resulting UTF-8 string to `/app/config_checker` via stdin and check `$?` — any nonzero exit anywhere in the file makes the whole file evil, so short-circuit and exit 1 immediately. For flood detection, normalize each ISO8601 timestamp to its hour bucket (strip minutes/seconds, or use `date -u -d "$ts" +%Y-%m-%dT%H` — GNU date; if unavailable emulate with string slicing since format is fixed-width) and tally counts per DeviceID+hour using an associative array (bash 4+) or a temp file processed with `sort | uniq -c`; if any count exceeds 50, exit 1. Only after scanning every line with neither rule tripped, exit 0. Test thoroughly against both corpora directories before finalizing, since a single missed encoding path or off-by-one hour boundary will fail many cases at once.
