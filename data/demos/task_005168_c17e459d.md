Start with `cargo new --lib rust_validator` at `/home/user/rust_validator`, then edit `Cargo.toml` to add `[lib] crate-type = ["cdylib"]` so it produces a `.so`. Implement `compute_signature` in `src/lib.rs` mirroring the legacy formula `((token_id ^ salt) * 11400714819323198485) % 2**64` using `u64` arithmetic with explicit wrapping (`.wrapping_mul`, since a plain `*` on `u64` will panic on overflow in debug builds and silently wrap only in release — use `wrapping_mul`/`wrapping_add`/`^` to be correct in both), and mark the function `#[no_mangle] pub extern "C" fn compute_signature(token_id: u64, salt: u64) -> u64`. Build with `cargo build --release` so the library lands at `target/release/librust_validator.so`. In `/home/user/py3_wrapper.py`, load that `.so` via `ctypes.CDLL`, set `.argtypes = [ctypes.c_uint64, ctypes.c_uint64]` and `.restype = ctypes.c_uint64` on the function before calling it, and expose a plain `compute_signature(token_id, salt)` Python function wrapping the call. For `/home/user/test_validator.py`, use `hypothesis.strategies.integers(min_value=0, max_value=2**64-1)` for both arguments and assert the wrapper's result equals the same formula computed independently in Python (mod `2**64`, since Python ints don't wrap automatically). Run `pytest /home/user/test_validator.py > /home/user/test_results.log` and confirm it reports all tests passed before finishing, avoiding root privileges throughout.