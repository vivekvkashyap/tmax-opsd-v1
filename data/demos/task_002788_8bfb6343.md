Build this as a strict linear awk pipeline matching each stated step in order, since the grader checks numeric closeness (MSE) against a hidden reference computed the same way. Extract with `sox /app/sensor.wav -t dat -` and strip the leading `;` comment lines. In awk, maintain running state: take `abs()` of amplitude; when exactly `0.00000000`, carry forward the last non-zero abs value instead (impute, don't drop the row — row count must match). You need the global max of the imputed series before normalizing, so buffer rows across two awk passes (or compute max in pass one, normalize in pass two). For the SMA use a truncated centered window of ±2 samples (not trailing) — average whatever indices exist in `i-2..i+2`, shrinking only near the boundaries. Write final `time,smoothed_normalized_amplitude` rows to `/home/user/processed_signal.csv`, preserving sox's original time precision, and hand-check a few rows before finishing — small imputation or window-boundary mistakes will blow the MSE threshold.