First isolate and fix the vendored bug before building on top of it: inspect `/app/bash-ops-etl-0.1.0/bin/extract_valid_metrics.sh` and look at how it tests the `val_loss` field in its awk filter — if it uses the field itself as a truthy condition (e.g. `&& $3 {print $3}`), awk treats the string "0.0"/"0" as false and silently drops legitimate zero-loss rows. Change the condition to an explicit non-empty-string check (e.g. `$3 != ""`) so only truly missing values are dropped, while `0.0` rows survive. Then write `run_etl.sh` to loop over `/home/user/data/raw_logs/*.csv`, call the fixed extractor on each file, and accumulate all extracted val_loss values (across all 50 files) into a running sum and count — do this in one pass rather than per-file re-averaging, since the Bayesian update needs a single n and sum(x) over the full dataset. Apply the given conjugate normal-normal formulas exactly as specified (posterior variance from precisions, then posterior mean as a precision-weighted combination of prior and data), using `bc -l` or `awk` for the floating point math to avoid integer-division bugs. Write the final posterior mean as a plain float to `/home/user/posterior_mean.txt`, make the script executable, and run it end to end.