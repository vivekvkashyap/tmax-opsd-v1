Tackle the three deliverables separately. For the sanitizer: validate `device_id` with a strict 8-char alphanumeric regex, `ip_address` with `net.ParseIP` plus a check it's IPv4 (`.To4() != nil`, rejecting injection strings), and `temperature` as a float within [-50.0, 150.0] inclusive — on any parse error, missing field, or out-of-range value, `os.Exit(1)`; only exit 0 when every field is perfectly valid. Test against a few clean and deliberately malformed payloads yourself before trusting it, since hidden corpora require 100% accuracy. For tamper detection: extract all 100 frames from `/app/camera_feed.mp4` (`ffmpeg` or `opencv`) and compute per-frame average grayscale brightness; frames whose brightness is well above near-zero indicate light leaked in — write their 0-indexed frame numbers, comma-separated in order, to `/home/user/tamper_frames.txt`. For `deploy.sh`, use `ssh -N -f -L 8080:localhost:9090 user@localhost` for a backgrounded local port forward with no remote command, and also compile the sanitizer from within the script.