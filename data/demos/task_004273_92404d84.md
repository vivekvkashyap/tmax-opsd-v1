Treat `/app/cleaner_oracle` as an unknown function and characterize it with deliberate black-box probes before writing any C: (1) a line where every value is identical, to learn what it emits when max==min (must avoid division by zero); (2) a two-value line at a few different spreads to see whether the scaling rounds or truncates toward zero; (3) a single-value line; (4) mixed-sign and negative values to confirm min/max detection happens before offsetting; (5) an empty/blank line to check EOF/blank-line handling. Compare outputs byte-for-byte across these probes rather than assuming standard rounding. You may also run `objdump -d` or `ltrace`/`strace` on the stripped binary to corroborate what you infer from black-box testing, but always trust actual output over assumption. Once behavior is nailed down, write `/home/user/cleaner.c` to: read stdin line-by-line until EOF, tokenize on whitespace, compute min/max per line, scale each element into the observed 0-255 mapping, and print space-separated results matching the oracle's exact spacing and newline behavior. Compile with `gcc -O3` and re-run your own edge-case probes against both binaries before trusting the fuzzer.
