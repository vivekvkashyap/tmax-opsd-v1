Build the script as a straight-through pipeline and be precise about each library's default behavior. Load the JSONL with one `json.loads` per line. Fit a single `TfidfVectorizer(lowercase=True, stop_words='english')` on the list of historical descriptions with the target description appended as the LAST element — fitting on the combined set (not fitting on historical only then transforming target separately) ensures the vocabulary/IDF weights match what the task expects. Compute cosine similarity between the target row and all historical rows (e.g. `sklearn.metrics.pairwise.cosine_similarity`), then take the 3 highest-similarity indices, sorted descending by similarity (watch tie-breaking and off-by-one indexing into the concatenated matrix). Concatenate the `metrics` arrays of those three experiments into one flat numpy array. Call `np.random.seed(42)` exactly once, immediately before the bootstrap loop (not earlier), then loop exactly 10000 times, each iteration doing `np.random.choice(combined, size=len(combined), replace=True)` and recording the sample mean — the exact call order/count matters for reproducibility, so do not add other randomness calls in between. Finally compute mean and 2.5/97.5 percentiles via `np.percentile`, round all floats to 4 decimals, and write the JSON with exactly the specified keys to `/home/user/report.json`.