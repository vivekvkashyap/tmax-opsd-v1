Read the existing `/home/user/generate_report.py` end-to-end before touching anything, since all three bugs are localized and structural. Fix the headless-plot crash by setting the Agg backend before pyplot is imported anywhere (`import matplotlib; matplotlib.use('Agg')` prior to `import matplotlib.pyplot as plt`) — this must happen before any other import triggers a GUI backend. Fix the vectorizer crash by imputing missing descriptions with `df['description'] = df['description'].fillna('')` right after loading the CSV, before calling `TfidfVectorizer`. Fix the self-similarity/duplicate-pair bug by only iterating the upper triangle of the similarity matrix (`for j in range(i+1, n)` instead of `range(n)`) rather than post-filtering, so diagonal (self=1.0) and symmetric duplicate pairs never enter the candidate list at all. Sort remaining pairs by score descending, take the top 3, round each score to 3 decimals, and dump as a JSON list-of-lists to `/home/user/top_pairs.json`. Install missing packages with pip, then actually run the script and confirm both output files exist and the JSON has no self-pairs before finishing.
