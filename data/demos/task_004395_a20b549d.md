Work in order: reproduce the leak, fix it, then automate the build. First compile the existing (buggy) sources manually into a throwaway binary and run `valgrind --leak-check=full ./that_binary 2> /home/user/valgrind_report.txt` to capture and confirm the leak report before changing anything. Inspect `processor.cpp`: since `libparser.c`'s `parse_request` returns a `malloc`'d buffer via a C ABI (declared `extern "C"` in the header), the C++ caller must `free()` that pointer itself once done — C++'s `delete`/RAII won't apply to a `malloc`'d C buffer, and forgetting the matching `free` is exactly the leak; add the `free()` call (and `#include <cstdlib>` if needed) right after the pointer is last used. For `build.py`, write concrete `subprocess`/`os.system` calls (not the stubbed TODOs): `gcc -c libparser.c -o libparser.o`, then two separate `g++` link commands compiling `processor.cpp` together with `libparser.o` into `processor_native` and `processor_web`, differing only by `-DTARGET_NATIVE` vs `-DTARGET_WEB`. Run `build.py`, then re-run Valgrind against `processor_native` to confirm "0 bytes in 0 blocks definitely lost" before finishing.