Split into three independent deliverables and verify each against ground truth you compute yourself from the raw inputs. For the video: extract all 60 frames with `ffmpeg -i /app/traffic_monitor.mp4 -vf fps=1 frame_%02d.png`, then for each frame sample a pixel (or average color) within each of the three horizontal bands (top third, middle third, bottom third of the height) and classify it green vs red by comparing RGB channels (e.g. with ImageMagick `convert frame.png -crop WxH+0+Y +repage -format "%[fx:mean.r],%[fx:mean.g]" info:` at the y-offset for each band); count green frames per node across all 60 frames. Compute each node's weight as its green-frame count divided by the sum of all three green-frame counts, rounded to 3 decimals, and write `generate_routes.sh` that reproduces this counting+computation and writes the single `route_weights node1=... node2=... node3=...` line to `/home/user/routing.conf` — do this via script, not by hardcoding numbers you eyeballed once, since exactness matters (MSE-based check). For the user-group mock: read `/app/team.csv` with a shell loop/awk, group usernames by team preserving first-seen order, assign sequential GIDs starting at 2000 per distinct team and sequential UIDs starting at 3000 per user in CSV row order, and emit `group_mock`/`passwd_mock` in the exact colon-delimited formats specified.