Loop over `run_00.npy` through `run_49.npy` in alphabetical/numeric order, loading each with `np.load` and keeping the array as `float32`. For each run compute per-particle `V^2 = vx^2+vy^2+vz^2` (still float32), then reduce it two ways: Method A sums with `np.sum(..., dtype=np.float32)` (forces the accumulator to stay low precision, which is what makes it diverge from the reference on wide-dynamic-range data), and Method B flattens and sums with Python's `math.fsum()`, which uses exact/compensated summation and is effectively your ground-truth reference. Collect the 50 Method-A and 50 Method-B totals into arrays, take elementwise `A - B` differences, and from those compute the mean difference and max absolute difference. Run `scipy.stats.ttest_rel(energies_A, energies_B)` on the two paired 50-length series (not on the differences) to get t-statistic and p-value. Plot a histogram of the differences with matplotlib (use a non-interactive backend since this is headless) and save to `/home/user/difference_plot.png` before any `show()`. Write the four required floats to `/home/user/analysis_results.json` with exactly the specified keys.