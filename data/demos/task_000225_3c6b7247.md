Build this as a single shell pipeline using awk for the grouping/aggregation logic, since Python/Perl are disallowed. Key steps: (1) skip the header row, (2) filter rows where column 2 (TargetSeq) starts with `GATTACA` — use `index($2,"GATTACA")==1` or a `^GATTACA` regex anchor in awk, not a substring-anywhere match; (3) compute sequence length with `length($2)`; (4) for each length, accumulate a count and a list of Score values, e.g. store scores per length key in an awk array as a growing string; (5) after processing all rows, for each length group split the accumulated scores, sort them numerically descending (pipe through `sort -t, -k1 -rn` per group, or collect into an array and sort with awk's `asort`/manual comparison), then join with commas; (6) print `SequenceLength\tMatchCount\tSortedScores` for each group, and finally sort the whole output block by SequenceLength ascending with `sort -k1,1n`. Watch two traps: matching "starts with" instead of "contains", and getting the descending-vs-ascending sort direction right for scores vs. the final ascending order by length. Verify by diffing your output against the expected content shown in the task's example.
