Start by OCR'ing /app/docs_config.png with `tesseract docs_config.png stdout` (or similar) to recover the byte offset and the OLD_TERM=NEW_TERM mapping — read the raw OCR text carefully rather than assuming formatting, since line breaks/spacing in the image can confuse a naive parse of the `KEY=VALUE` lines. Use the recovered offset to strip the leading garbage bytes from the corrupted binary, e.g. `tail -c +$((OFFSET+1)) /app/corrupted_docs.bin > /tmp/clean.tar.gz`, then verify it's a valid gzip/tar stream with `tar -tzf` before extracting — if extraction fails, your OCR'd offset is likely off by a byte and you should recheck the image. After extracting, apply the OCR'd replacements to every `.txt` and `.xml` file with `sed -i "s/OLD/NEW/g"` for each pair (order matters if any term is a substring of another — apply longer/more specific terms first to avoid partial corruption). Repack only the modified files (no extraneous directories or originals) into /home/user/updated_docs.tar.gz with `tar -czf ... -C <dir> <files>`, then spot-check a couple of extracted files yourself to confirm zero old terms remain before finishing, since the scoring metric penalizes leftover OLD_TERM occurrences.