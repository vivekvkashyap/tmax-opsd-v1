Design the C program in clear passes rather than one big loop: (1) parse all rows from the input CSV, skipping the header; (2) filter out rows failing validation (cpu_usage outside [0,100], memory_mb < 0); (3) deduplicate by timestamp, keeping the row with max cpu_usage, then max memory_mb as tiebreaker — a hash map or sorted array keyed by timestamp works well; (4) after dedup, sort the remaining valid records by timestamp ascending; (5) walk from min to max timestamp second-by-second, and for each second either use the exact record if present, or forward-fill from the last known valid record if the gap since the last real record is <= 5 seconds, or emit `-1,-1` if the gap since the last real record exceeds 5 seconds. Pay close attention to the gap-length definition — it's measured relative to the distance between consecutive real (post-dedup) records, not an arbitrary counter, and the fill decision must be made per missing second based on that gap. Write results with the exact header and format, then compile with `gcc -o /home/user/processor /home/user/process_metrics.c` and run it against `/home/user/raw_metrics.csv`, checking your output line-by-line by hand against the input logic before trusting it.