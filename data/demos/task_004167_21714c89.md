Read `pcr_model.py` to find the forward-Euler integrator: a diverging/oscillating simulation from Euler's method on a stiff-ish logistic ODE usually means the step size `dt` is too large relative to the growth rate, causing overshoot each step. Fix it either by shrinking `dt` substantially (e.g. sub-stepping so many small updates occur per unit time) or, more robustly, replace the hand-rolled loop with `scipy.integrate.solve_ivp` on `dY/dt = r*Y*(1-Y/K)` with `Y0=2.0`, then confirm `python /home/user/pcr_model.py --test` reports success — the built-in regression test compares against a known analytic solution, so let that be your correctness signal rather than guessing. For curve fitting, load `/home/user/reference_yield.csv` and fit the logistic growth model's `r` and `K` to the `(time, yield)` data using nonlinear least squares (e.g. `scipy.optimize.curve_fit` with the closed-form logistic solution or by integrating and minimizing residuals), rounding results to 2 decimals. Use your fitted `r` to compute `L = int(r * 20)`, then parse `/home/user/candidates.fasta` (FASTA records: `>id` header lines followed by sequence lines) and `/home/user/gene_sequence.txt`, and search for the FASTA entry whose sequence length equals `L` and is an exact substring of the gene sequence. Write all results to `/home/user/solution.json` in the exact required schema.