Install `h5py`/`scipy` if missing, then write the script as a direct pipeline: open `/home/user/profiling_data.h5` with `h5py.File(...)`, iterate `run_0..run_49` under `/v1_slow` and `/v2_fast` separately, compute `numpy.std()` on each dataset's array (population std, default ddof=0, matching the straightforward reading of "standard deviation of each run") to build two length-50 lists. Feed those two lists — not the raw sample arrays — into `scipy.stats.ks_2samp` for the two-sample KS test; this compares the *distributions of per-run standard deviations* between the two versions, not the raw data. Round both `statistic` and `pvalue` to 4 decimals, set `is_equivalent` as a native Python bool from `p_value > 0.05`, and dump to `/home/user/report.json` with exactly the three specified keys. Run the script yourself and sanity-check the JSON is valid and the boolean serializes as `true`/`false` (not a numpy bool) before finishing.
