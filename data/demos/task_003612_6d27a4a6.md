Tackle this in the stated order — cert validation, sandboxed transcription, then redaction — testing each independently. For cert chain validation, use Go's `crypto/x509`: load `root.pem` as a `CertPool`, load `intermediate.pem` into an intermediate pool, then call `server_cert.Verify(x509.VerifyOptions{Roots: rootPool, Intermediates: intermediatePool})` and `os.Exit(1)` on any error — test this against the actually-generated certs in `/app/certs/` rather than assuming format. For sandboxing, invoke `whisper` via `exec.Command("bwrap", "--unshare-net", "--ro-bind", "/", "/", "--dev", "/dev", "--proc", "/proc", "--tmpfs", "/tmp", "--bind", "/home/user", "/home/user", "whisper", "/app/voip_intercept.wav", "--model", "tiny", "--output_format", "txt", "--output_dir", "/home/user/")` — run this manually first to confirm whisper actually produces a `.txt` transcript file under the read-only root before wiring it into Go, since bwrap flag ordering/read-only root can silently break tool execution. For redaction, write Go regexps that match IPv4 addresses and 16-digit card numbers allowing optional dash/space separators between groups, and replace matches with the literal string `[REDACTED]` — test the regex against the transcribed text directly (transcription wording may vary slightly, so verify your regex still catches the digit/IP patterns whatever surrounding words whisper produces) before writing the final report.