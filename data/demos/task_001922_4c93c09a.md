Don't guess the crash input — write a short throwaway script (or use a Python REPL) that iterates every integer from -100000 to 100000, calling `process_number(n)` inside a `try/except ZeroDivisionError`, and records whichever `n` triggers the exception; write that single integer to `/home/user/crash_input.txt`. Read `process_number`'s source to see exactly which arithmetic expression can divide by zero and fix it by guarding that specific condition (e.g. special-case it to return 0) rather than a broad try/except that could mask other bugs. For the race condition, recognize the pattern: a shared global accumulated via non-atomic read-modify-write across threads. Prefer refactoring so `calculate_all` collects each thread's return value via `executor.map(...)` into a list and sums them in the main thread afterward (no shared mutable state, no lock needed) — only fall back to a `threading.Lock` around the shared variable if you must keep the global-accumulator structure. Update `__main__` to call `calculate_all(list(range(-100000, 100001)))`, run the fixed script, and write the printed sum to `/home/user/final_result.txt`.
