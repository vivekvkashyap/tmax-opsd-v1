First OCR the threshold: run `tesseract /app/mcmc_spec.png stdout` (try `--psm 6` if the default segmentation garbles it) and parse out the numeric "max allowed R-hat" value from the text — don't hardcode a guessed number, extract it programmatically or read it carefully and embed the extracted value in your script. Then implement the standard Gelman-Rubin diagnostic in your bash script's helper (Python/awk) for exactly 2 chains: split the CSV by chain, compute each chain's mean and within-chain variance W (average of chain variances), compute the between-chain variance B from the two chain means and step count, then combine as R-hat = sqrt(((n-1)/n * W + B/n) / W) using the standard formula — get the algebra exactly right since off-by-factor errors will misclassify borderline evil/clean traces. Guard division by zero: if W is 0 (a near-singular/degenerate chain with zero variance), treat this as invalid and exit 1 regardless of the computed ratio. Wire the script to read the `value` column, compute R-hat, compare against the OCR'd threshold (accept if R-hat <= threshold), and exit 0/1 accordingly; then run it against every file in `/app/traces/clean/` and `/app/traces/evil/` to confirm 100% correct classification before finishing, and `chmod +x` the script.