Reverse-engineer before you reimplement: run `/app/legacy_analyzer` against small synthetic grayscale files you construct yourself (e.g. all-same-value files, files with a known max byte) to infer its formula empirically, and/or disassemble it (objdump/strings/ghidra-style static inspection) to confirm the exact arithmetic and where it can crash — don't assume a formula. Extract frames from `/app/test_sequence.mp4` with ffmpeg (e.g. `ffmpeg -i test_sequence.mp4 -vf format=gray -f rawvideo -` piped per-frame, or extract to raw grayscale PNGs and read pixel bytes) so you can compute the same per-pixel statistic your reversed formula needs for an arbitrary frame index N. Once you understand the boundary condition that makes the original divide-by-zero/overflow, replicate the scoring logic in your HTTP service but wrap that specific case in a check that returns -1 instead of crashing. Implement the service (any language) listening on 127.0.0.1:9000, handling `GET /score?frame=<N>` by extracting frame N, computing the score or detecting the boundary condition, and responding with plain text — start it and leave it running.
