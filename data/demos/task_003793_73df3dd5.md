Two separable pieces: recover a hidden value from an image, then write a robust stream parser. First, use OCR (e.g. `tesseract /app/policy.png stdout`) to read the text in `/app/policy.png` and locate the line matching `PREFIX=<value>`; extract that value programmatically rather than hardcoding what you read, in case whitespace/case needs cleanup — inspect the raw OCR output carefully since OCR can introduce stray characters. Then write `/home/user/generate_links.py` (or equivalent) to read all of stdin, split it on lines containing exactly `===` to get records, and for each non-empty record parse its lines independently by matching the `File:`, `Ext:`, `Integrity:` prefixes (case-sensitive, but don't assume field order — the spec says order is shuffled) with generous whitespace stripping on both the label and the value. Since this is fuzz-tested against a hidden oracle across many shuffled/whitespace-perturbed inputs, be strict about only emitting the two exact command formats specified (`ln -s ...` for VALID, `rm -f ...` for CORRUPT) with nothing else on stdout — no debug prints, no trailing blank lines beyond what one `print` naturally adds. Make the script executable with `chmod +x`.