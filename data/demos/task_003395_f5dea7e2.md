Write the C program to read the GCode file line by line (fgets or getline), and only act on lines whose first two characters are exactly "G1 " (use strncmp, not a substring search, so "G10" or lines with G1 elsewhere don't false-match). For each qualifying line, scan for tokens starting with 'X' and 'Y' (e.g. with sscanf/strtof after locating the character), and only emit a pair if BOTH are present — skip the line entirely if either is missing, including E-only or Z-only moves. Open the output file with O_APPEND, take an exclusive lock via flock(fd, LOCK_EX) (or fcntl with F_SETLKW) immediately before the write and release it right after, so concurrent `xargs -P 4` invocations don't interleave partial 8-byte float pairs — this is the step naive attempts skip, causing corrupted/torn writes under parallelism. Write raw binary with fwrite of two `float`s (not text), matching struct.unpack('ff', ...) on the reading side. Compile with gcc, then run the exact xargs pipeline given in the task and confirm the final .bin file size is a multiple of 8 bytes.
