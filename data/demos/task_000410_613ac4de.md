The core of this task is correct path normalization, not string matching. In C++17, use `std::filesystem::weakly_canonical` (or manually resolve `.`/`..` segments against `base_dir` since the target files/dirs referenced by traversal paths like `../../.ssh/id_rsa` may not exist under `base_dir`, so `canonical()` alone can throw) to turn each raw `Path` into an absolute, normalized path. Then check escape by comparing whether the normalized path's string, after ensuring `base_dir` ends with a trailing separator, starts with `base_dir + "/"` — a naive substring/prefix check without the trailing slash will wrongly accept a sibling directory that merely shares the prefix (e.g. `/home/user/app_data_backup` vs `/home/user/app_data`). Parse the log by splitting on blank lines and reading the three `Key: value` lines per record with simple string parsing (no JSON library needed there; use a small JSON parser or manual key extraction for `config.json`'s single `base_dir` key). For each escaping record, shell out to `sha256sum` on `/home/user/quarantine/<RecordID>.dat` (via `popen`) or implement SHA-256 yourself, and write `RecordID,NormalizedPath,Checksum` lines to the CSV — no header row.
