First fix and build the vendored dependency: inspect `/app/simdjson-3.6.0/CMakeLists.txt` for the C++ standard setting — simdjson requires C++17+, so if it's pinned to an older standard (e.g. `CMAKE_CXX_STANDARD 11`), change it to 17, then configure/build/install with CMake into `/home/user/local` (`cmake -DCMAKE_INSTALL_PREFIX=/home/user/local .. && make && make install`), verifying the resulting headers/libs land where you expect. Write `validator.cpp` using `simdjson::ondemand::parser` (or the DOM API) wrapped in a try/catch (or checking simdjson error codes) around every parse and field access, since malformed JSON and missing/wrong-typed keys must all cleanly resolve to printing `REJECT` rather than crashing — never let an uncaught exception or segfault occur, treat every kind of validation failure or parse error identically. Explicitly validate: `status == "success"`, `data` is an array of exactly 256 numeric elements, and each element is finite (`std::isfinite`) and within `[-10.0, 10.0]` inclusive — check these one by one and short-circuit to REJECT on the first violation. Compile linking against your installed simdjson (`-I/home/user/local/include -L/home/user/local/lib -lsimdjson -std=c++17`), then before trusting it, hand-construct a few edge-case JSON files yourself (wrong count, out-of-range value, missing key, malformed) and run the binary against them to confirm exact stdout/exit-code behavior.
