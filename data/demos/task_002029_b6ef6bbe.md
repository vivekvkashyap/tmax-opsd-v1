Work from evidence to fix, in this order. First, parse `/home/user/service.log` to find every request that hit a `RecursionError` / max-recursion-depth traceback, extracting its Request ID — write those IDs sorted numerically, one per line, to `/home/user/recursion_ids.txt` (don't hand-pick IDs from reading the log casually; scan programmatically so you don't miss any). Second, in `/home/user/math_service.py`, add an early `if n < 0: return -1` guard at the top of `compute_sequence` to stop the runaway negative-input recursion. Third, find `process_request` and notice the deadlock: one branch (e.g. even n) acquires `lock_A` then `lock_B`, while the other branch (odd n) acquires them in the opposite order — fix only the inconsistent branch so both branches always acquire `lock_A` before `lock_B`, keeping the `with` structure. Fourth, write `/home/user/test_math_service.py` that imports `process_request`, spawns 50 threads with a mix of even/odd/negative `n` values, joins them all with a reasonable timeout, and asserts no exceptions/hangs occurred, exiting 0 on success and non-zero otherwise. Run it and confirm it finishes in well under 5 seconds.
