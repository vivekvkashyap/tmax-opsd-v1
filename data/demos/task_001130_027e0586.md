The validation rules are only stated in the audio file, not in the task text — you must extract them yourself before writing any classifier logic. Use an ASR tool (e.g. `whisper /app/requirements.wav --model base --output_format txt`, installing it via pip if missing) to transcribe `/app/requirements.wav`, then read the transcript carefully to pin down the exact two rules (a join-condition requirement between specific tables, and a LIMIT-clause ceiling for a specific table) before coding anything. Only after you have the transcript, implement `/home/user/query_validator.py`: parse the SQL file (regex or `sqlglot`) to detect (a) implicit cross joins — comma-joins or CROSS JOIN between the two tables named in the transcript without a proper join condition on the column named in the transcript, and (b) missing/oversized LIMIT clauses on the table named in the transcript, using the exact numeric threshold stated in the audio. Print exactly `CLEAN`/`EVIL` and exit 0/1 accordingly. Test against `/app/corpus/clean/` and `/app/corpus/evil/` to confirm 100% separation before considering it done, since the hidden test set follows the same rules.
