Build the C++ program in the exact order the spec implies, since each stage's determinism depends on the previous one being exact: (1) parse `/home/user/graph.txt` into an adjacency list keyed by vertex id, then sort each vertex's neighbor list ascending — this ordering matters because it determines which neighbor index `k` maps to later; (2) run a standard BFS from vertex 0 to get integer shortest-path distances into a `dist` array; (3) create a single `std::mt19937 gen(42)` before any simulation and never reseed it — walk 10,000 walkers one at a time, each for 100 steps, using `std::uniform_int_distribution<int>(0, degree-1)` freshly constructed per step (distribution objects are stateless, so this doesn't affect determinism) to pick the neighbor index into the sorted adjacency list; accumulate squared `dist[]` values into an MSD sum per timestep, then divide by 10,000 at the end. For alpha, do ordinary least-squares linear regression on (ln t, ln MSD(t)) restricted to t in [10,100]. For the integral, apply the trapezoidal rule with step 1 over t=1..100. Match the output format `Alpha: <v>\nIntegral: <v>` rounded to 3 decimals exactly, and compile/run to actually produce `/home/user/results.txt` rather than just leaving source code.