First OCR /app/evidence.png (e.g. `tesseract evidence.png stdout`) to recover the `ATTACKER_DOMAIN: <domain>` string, and extract just the domain value for use in your server's matching logic — don't hardcode a guessed domain. Next, generate the TLS material with openssl: `openssl req -x509 -newkey rsa:2048 -nodes -keyout /app/certs/server.key -out /app/certs/server.crt -days 365 -subj "/CN=localhost"`, then explicitly `chmod 600 /app/certs/server.key` since default umask permissions won't satisfy the check. Write server.py using Python's `http.server` with an `ssl.SSLContext` wrapping the socket (loading server.crt/server.key) or a framework's built-in TLS support, bound to 127.0.0.1:8443. In the /login handler, check conditions in this order: (1) if `next` contains the attacker domain recovered via OCR → 403 "Intrusion Detected"; (2) else if `next` starts with `http://`, `https://`, or `//` → 400 "Invalid Redirect"; (3) else treat it as a relative path → 302 with `Location` set to `next`. Launch the server as a background/daemonized process, write its PID to /app/server.pid, and verify with `curl -k` against all three cases before finishing.