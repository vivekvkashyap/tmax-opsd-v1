Find and clear the port conflict first with standard tools: `lsof -i :8443` or `ss -ltnp | grep 8443` to get the PID bound to the port, then `kill <pid>` (escalate to `-9` only if needed) — do this before trying to start your own service or the bind will fail. Edit `main.go` to replace the hardcoded `/etc/ssl/certs/...` paths with `/home/user/vnc-api/certs/server.crt` and `server.key`. Generate the matching self-signed cert/key with `openssl req -x509 -newkey rsa:2048 -keyout server.key -out server.crt -days 365 -nodes -subj '/CN=localhost'` at that exact path. Write `deploy.sh` to `cd` into the project dir, `go build -o vnc-manager .`, then launch it detached and redirected: `nohup ./vnc-manager > /home/user/vnc-api/api.log 2>&1 & disown`, and `chmod +x deploy.sh`. After running it, give the server a moment to bind, then verify with `curl -sk https://127.0.0.1:8443/status` — capture that exact raw response body (don't reformat or pretty-print the JSON) and write it byte-for-byte to `/home/user/result.json`, since the check is an exact string match.
