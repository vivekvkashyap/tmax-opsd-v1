First recover the actual matrix values from the file rather than assuming any numbers — run `h5dump /home/user/transitions.h5` (or a short Python `h5py` snippet) to print the `/matrix` dataset and read off the real 4x4 values, confirming each row sums to 1.0. Then in `/home/user/solve_markov.c`, hardcode those recovered values and solve for the stationary distribution π satisfying πP = π with Σπ = 1: the standard approach is either (a) iterative power-method — repeatedly multiply an initial uniform vector by P until it converges (many iterations, e.g. thousands, for 4-decimal precision), or (b) solve the linear system (P^T - I)π = 0 with the normalization constraint via Gaussian elimination substituting one equation for Σπ=1. Iterative power-method is simpler to implement correctly in C without a linear-algebra library. Compile with `gcc`, run it, and format the four resulting probabilities to exactly 4 decimal places, comma-separated, with no trailing whitespace/newline issues — write the result to `/home/user/stationary_dist.txt` and double check the row order (A, C, G, T mapping) matches the matrix's row/column convention.
