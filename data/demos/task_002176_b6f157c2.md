Work through this as a strict pipeline: mount, merge, parse, filter, unmount. First, `mkdir -p /home/user/mnt` and mount the ext4 image without root using `fuse2fs /home/user/suspect_data.img /home/user/mnt` (fuse2fs supports userspace ext4 mounting; if not installed, install it first). Second, recursively walk `mnt/payload/` with Python (`os.walk` or `pathlib.rglob`) to find every `*.log.chunkN` file, group them by the service name prefix (strip the `.log.chunkN` suffix), and for each group sort by the integer N (not string sort — chunk10 must come after chunk2) before concatenating file contents in order into `/home/user/merged_logs/<service>.merged.log`. Third, when reading each merged line, strip the trailing `!!!`, then convert single-quoted pseudo-JSON to valid JSON — the safest approach is `ast.literal_eval` on the stripped line (since it's Python-dict-like) rather than fragile string replacement of quotes, which can break on quoted content inside string values. Fourth, filter for `level == 'FATAL'`, and write only `timestamp`, `service`, `message` keys as compact JSON per line to `/home/user/fatal_alerts.jsonl` using `json.dumps` (double quotes, no `!!!`, no extra keys). Finally, unmount with `fusermount -u /home/user/mnt` — do this even if earlier steps errored, so the mount doesn't leak.
