Diagnose the two bugs independently before touching code. First, run `/app/start.sh` and read the actual error — trace what `ARTIFACT_STORE_PATH` is set to and check with `ls -la`/`file` whether that path exists as a regular file instead of a directory (a script that does `touch` on a path meant to be a directory is a classic setup bug); fix the startup script so the store path is created as a directory (e.g. `mkdir -p`), removing any stray file first if needed. Second, for the multipart parsing bug, don't guess — reproduce it: craft a small multipart POST with a filename containing a space and watch the framework raise or misbehave inside `mini_http_framework/parser.py`. Find the function that extracts the filename from the `Content-Disposition` header; if it naively splits the whole header on spaces, it will break because the filename itself may contain spaces inside the quoted value. Replace that logic with a regex or parser that specifically matches the quoted `filename="..."` value (e.g. `re.search(r'filename="([^"]+)"', header)`), preserving everything between the quotes regardless of internal spaces. After both fixes, start the server in the background with `/app/start.sh &`, then verify independently: a raw TCP PING to the admin port, and an authenticated multipart upload/download round-trip for a filename with spaces.
