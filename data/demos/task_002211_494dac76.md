Parse the log with awk/grep treating blank lines as record separators (e.g. `awk -v RS='' -F'\n'`) to pull ID, Status, and File per 3-line block; only act on APPROVED entries. For each candidate archive, first test integrity without extracting (`unzip -t` for zips, `tar -tzf` for tar.gz — a non-zero exit means corrupt, skip it). Then, still without extracting, list member paths (`unzip -l` / `tar -tzf`) and scan each path: flag as malicious if it starts with `/` or contains a `../` path segment anywhere — this is the Zip Slip check, and it must happen before any extraction call touches disk. Only extract (`unzip`/`tar -xzf` into `/home/user/projects/<ID>/`) archives that pass both integrity and safety checks, incrementing your safe/malicious counters accordingly. For step 3, use `find /home/user/projects -name '*.py' -mtime -7 -print0 | xargs -0 -I{} cp {} /home/user/recent_code/` (or equivalent piping), counting copied files. Tally all three counters into the exact summary format, then run the script yourself and confirm the output files exist before finishing.
