Combine the two data sources before building the graph, not after: query `employees` from `/home/user/employees.db` (sqlite3) into a set of valid emp_ids, then stream-parse `/home/user/delegations.jsonl` line by line with `json.loads`, keeping only delegation records whose `from_emp` AND `to_emp` are both in that valid-employee set — this filters out "ghost" IDs referenced in delegations but absent from the employees table. Build a `networkx.DiGraph()` from the surviving edges. Run `nx.pagerank(G, alpha=0.85)` and take the top 3 node ids by score for `top_bottlenecks`. Separately, find directed cycles of exactly length 3 using `nx.simple_cycles(G, length_bound=3)` (or filter `nx.simple_cycles(G)` results to len==3) — for each cycle, rotate its element order so the lexicographically smallest emp_id comes first (preserving cyclic direction), then sort the whole list of 3-cycles lexicographically by (first, second, third) element. Write both computed lists into the exact JSON structure specified at `/home/user/deadlock_audit.json`, matching key names `top_bottlenecks` and `deadlock_cycles` precisely.
