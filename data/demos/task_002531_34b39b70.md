Break this into independently testable pieces before wiring them together. First, run `tesseract /app/heat_schematic.png stdout` (try `--psm 6` or similar if default OCR misses the text) and parse the "SOURCE_COORD: X=..., Y=..." line with a regex to get integer X,Y — don't hardcode coordinates, extract them at runtime so your code works if the image changes. Second, write the C++ Poisson solver as a standalone function: allocate a 50x50 double grid, zero the boundary rows/cols, inject the source term Q at (X,Y), and iterate Gauss-Seidel/Jacobi updates until max(|T_new-T_old|) < 1e-4 — test it manually with a fixed Q first to confirm it converges and peaks near the source before adding MCMC on top. Third, wrap that solver in a Metropolis-Hastings loop over Q: propose Q' = Q + N(0,sigma), accept if it gets |peak-target| closer (or via an acceptance ratio), and stop once peak is within ±0.5 of target_peak — a naive binary search on Q also works since peak is monotonic in Q, but MCMC is requested. Fourth, do the quadratic fit on the y=Y row via manual normal-equations least squares (no external libs). Only after all three pieces work standalone should you glue them into the HTTP server on 127.0.0.1:8080 and background it (e.g. `nohup ... &`) so it survives after your turn ends.