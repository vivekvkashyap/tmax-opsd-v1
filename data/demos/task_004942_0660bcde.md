Tackle this in dependency order: fix the build, then the numerical bug, then use the working library for recovery, and finally write an independent classifier. First inspect `/app/vendor/quant-db-1.2.0/setup.py` for the C-extension build config — look for a hardcoded include directory that doesn't exist on this system and fix it to point at the correct headers, and check the linker flags for a missing `-lm` (math library) needed by the extension; then `pip install -e .` (or equivalent) and run the package's own test suite (e.g. `pytest tests/`) to confirm it builds and imports cleanly. Next open `aggregator.c`: if it accumulates a running sum of many small floats using single-precision `float`, that's the classic precision-drift bug — switch the accumulator to `double`, or implement Kahan (compensated) summation, and re-run the test suite to confirm precision holds. For WAL recovery, first hex-dump `/app/data/transaction.wal` (e.g. `xxd`) to reverse-engineer its record framing — look for a repeating fixed-width magic number marking record starts, followed by a length field, followed by payload bytes, and use that discovered magic/length pattern to parse records with the now-working library's decode function; when a magic or length looks invalid, skip forward byte-by-byte until you resync on the next valid magic rather than aborting, and dump all successfully recovered dicts to `recovered_records.json`. Finally, for `sanitizer.py`, diff the structure of files in `/app/corpus/evil/` against `/app/corpus/clean/` (headers, field lengths, checksums, magic bytes) to find the concrete anomaly signature that separates them, then classify each `.dat` file accordingly and print `<filename>: ACCEPT/REJECT` per file.