Debug the vendored C package methodically rather than rewriting it from scratch: build first (`make`) to see the actual compile error — if it's a Makefile issue, check for tabs vs spaces in recipe lines (Make requires literal tabs) and for missing link flags the source actually needs (e.g. `-lpthread` if threads are used). Once it compiles, read `main.c`'s use of `getenv("BASE_DIR")`/`getenv("WWW_DIR")` — since these aren't NULL-checked, add explicit checks that exit with a clear error if unset, which also protects the server from segfaulting under the automation script. Then find the symlink path-construction logic and fix the string concatenation that's missing the `/` separator between the base path and username (e.g. building `$WWW_DIR<username>` instead of `$WWW_DIR/<username>`), which is why `symlink()` fails or points to the wrong target. Test the `/provision` endpoint manually with curl against a temp `BASE_DIR`/`WWW_DIR` to confirm directories, the `www` symlink, and `route.sh`'s exact content are all correct before writing `setup_and_run.sh`. Make that script idempotent by using `mkdir -p` and only rebuilding if the binary doesn't already exist, then export the env vars and launch `provisioner` in the background, verifying with a final curl that port 8080 responds before finishing.