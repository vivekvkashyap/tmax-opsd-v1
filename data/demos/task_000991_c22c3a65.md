Work through the four sub-problems in dependency order. First, diagnose why `patch` fails on `kiss_opt.patch` — check for CRLF line endings with `file` or `cat -A` and normalize with `dos2unix` before retrying `patch -p1`; also inspect the vendored `kissfft` Makefile for a missing `-fPIC` flag, since without it you cannot link a `.so`. Second, resolve the Go circular import between `pkg/processor` and `pkg/utils` by extracting the shared type/interface both packages depend on into a third, lower-level package (or an appropriately placed subpackage) so the dependency graph becomes acyclic — don't just merge the packages unless `main.go`'s API expectations allow it. Third, implement the CGO wrapper: allocate a C array with `C.malloc`/`C.CBytes` sized to the input, call `C.kiss_fft_alloc` then `C.kiss_fft`, copy results back into a Go slice, and free C memory to avoid leaks. Fourth, write the Makefile so `bin/fftapp` can locate `libkissfft.so` at runtime — either pass `-Wl,-rpath,$ORIGIN/../lib` via CGO_LDFLAGS or wrap execution with `LD_LIBRARY_PATH`. Validate correctness/perf against the metrics.json thresholds the pipeline must hit: MSE against pure-Go FFT ≤ 1e-4, and CGO speedup ≥ 1.2x.
