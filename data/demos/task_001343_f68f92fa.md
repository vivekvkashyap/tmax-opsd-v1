Write one Python script that does extraction, stats, and reporting in a single reproducible pass rather than manual/ad-hoc shell parsing. For extraction, iterate each log file line by line, use `"Inference time:" in line` as the filter, then split on that substring and take the first whitespace-delimited token after it as the value — wrap the float conversion in a try/except (or regex with a numeric-only pattern) so malformed lines, `NaN` entries, and ERROR/WARNING lines are silently skipped rather than crashing or polluting your sample. Use `scipy.stats.ttest_ind(a, b, equal_var=False)` for the two-sided Welch's t-test to get the p-value directly. For the 95% CI on the mean difference, don't hand-roll a z-interval — compute Welch-Satterthwaite degrees of freedom (`res.df` from the ttest result, or derive it from the two sample variances/sizes), get the critical t-value via `scipy.stats.t.ppf(0.975, df)`, and combine with the pooled standard error (`sqrt(var_A/n_A + var_B/n_B)`) around `mean_A - mean_B`. Round every output field to exactly 4 decimals with Python's `round()` before writing the JSON so the tolerance-based comparison passes.
