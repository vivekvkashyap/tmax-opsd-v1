Two independent deliverables: signal analysis to recover the oscillation period P, and a separate RK4 ODE integrator in C that hardcodes omega derived from P. For the signal analysis, use `ffmpeg` to extract frames (e.g. `-vf format=gray` piped to raw output, or extract PNGs and average pixel values with a small Python/numpy script using OpenCV or PIL) to build a per-frame mean-intensity time series. Detrend/center the series, then find the dominant period via autocorrelation (find the first strong peak lag) or via FFT (find the peak frequency bin and invert to a period in frames) rather than eyeballing it — visual guessing is unreliable and this is graded by fuzzing your compiled binary against a hidden oracle across many random initial conditions, so the omega you hardcode must be numerically exact given P = 2*pi/P. For the C program, implement RK4 for the coupled system dx/dt=y, dy/dt=-omega^2*x with a literal `dt=0.1` and exactly 100 fixed-step iterations, reading x0 and y0 as two space-separated doubles via `scanf("%lf %lf", ...)`, and print final x,y with `printf("%.6f %.6f\n", x, y)`. Compile with `gcc -O2 -lm` and verify the binary at `/home/user/ode_sim` matches expected physical behavior (e.g., conserved oscillation amplitude) before considering it done.