Start by reading the existing naive script to understand its exact transform (regex/replace logic) and the Redis/Flask contract before rewriting, so correctness is preserved while speed improves. The two bottlenecks to fix are (1) sequential per-file processing and (2) synchronous blocking HTTP calls per file — replace the file loop with a process pool (`multiprocessing.Pool`/`concurrent.futures.ProcessPoolExecutor`) or an async pipeline (`asyncio` + `aiohttp`) so I/O and CPU work overlap across workers; batch or pipeline the `/report` POSTs (e.g. a persistent `requests.Session` with connection pooling, or async concurrent requests) instead of one blocking connection per file. For atomic writes, never write directly to the destination filename — write to a `tempfile.NamedTemporaryFile(dir=dest_dir, delete=False)` (same directory, so the rename is on the same filesystem) then call `os.replace(tmp_path, final_path)`. Use `re.sub` with a compiled pattern for the `<legacy-ref target="X">` → `[X](/docs/X)` transform, and count replacements per file to send in the payload. Verify by starting services with the provided script, timing a full run of `python /app/processor.py`, and confirming Redis has exactly 10,000 keys and `/home/user/docs_processed/` has 10,000 files before considering it done.