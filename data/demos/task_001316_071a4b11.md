First run `/home/user/legacy_app/start_service.sh` manually to see the exact prompt text and order, since `expect` scripts are fragile to whitespace/wording mismatches — use `expect "Prompt text:"` (matching substrings, not full-line anchors) followed by `send "answer\r"` for each of the three prompts, then `expect eof` so the script blocks until the wrapped process (and its backgrounded server) has actually started, not just until the last prompt is answered. Make `deploy.sh` idempotent by testing `[ -f /home/user/legacy_app/service.conf ]` up front and short-circuiting with the exact success message before invoking the expect script — this is the detail naive attempts skip, causing a second run to re-prompt or fail. Run `deploy.sh` once to actually launch the background HTTP server on the configured port. For `health_monitor.sh`, use `curl -s` (not `-f`) so you can inspect the body directly, compare it to the literal string `OK`, and always append exactly one of the two STATUS lines even when curl fails (e.g. capture exit status and treat non-zero as UNHEALTHY). Run it once to seed the log. Remember `chmod +x` on all three scripts.
