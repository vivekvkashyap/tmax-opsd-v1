Run `tesseract /app/policy_label.png -` to recover the `APPROVED_PREFIX` string exactly (watch for OCR confusables like 0/O, 1/l) and hardcode it as a constant in the Go program. Build the filter using only `archive/zip`, `archive/tar`, `compress/gzip`, and `path/filepath`: for each entry, before trusting its name, call `filepath.Clean` on it and reject if the cleaned path is absolute, starts with `../`, or otherwise escapes a virtual root when joined (`filepath.Join(root, name)` then check it still has `root` as prefix) — this is the standard zip-slip/tar-slip defense. Separately enforce the policy rule: every entry's cleaned relative path must begin with `<prefix>/` (reject bare root-level files and any other top-level directory name). For tar, also treat symlink/hardlink entries with `Typeflag == TypeSymlink/TypeLink` suspiciously if their `Linkname` resolves outside the root. Wrap archive parsing in error handling so any read/header error (corrupt zip/tar) causes exit code 1 rather than a panic. Test against both provided corpora directories, checking `echo $?` after each invocation, and iterate until every clean archive exits 0 and every evil archive exits non-zero.
