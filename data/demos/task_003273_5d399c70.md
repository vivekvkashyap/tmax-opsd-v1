Implement `solve_t` as a standard Newton-Raphson loop: starting from t=1.0, repeatedly compute `t_new = t - f(t,p)/df(t)` using the given f and f' formulas, update t, and stop as soon as `fabs(f(t,p)) < 1e-6` or after 100 iterations — check convergence on the *updated* t each iteration, not before the first step. Compile with `g++` and run the program against `/home/user/sequence_data.txt`, writing `p,t` pairs to the CSV with t formatted via `std::fixed << std::setprecision(4)` so it always shows exactly 4 decimals (verify p is also printed exactly as read, matching the input's decimal formatting). For `/home/user/plot.sh`, parse each CSV line, print the p value verbatim, then ` | `, then a run of `*` characters whose count is `floor(t * 10)` — compute this with integer arithmetic (e.g. `awk`'s int() or bash with `bc`) rather than string manipulation, since floating point formatting errors easily produce an off-by-one asterisk count. Run the plot script and redirect to `/home/user/plot.txt`, then sanity-check the asterisk counts against the CSV values by hand for a couple of rows.
