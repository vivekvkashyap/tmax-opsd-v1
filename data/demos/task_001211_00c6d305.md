Write the C pipeline to read `/app/telemetry.wav` by skipping exactly the 44-byte standard WAV header and interpreting the remainder as 16-bit signed little-endian PCM samples (cast each pair of bytes to `int16_t`, being mindful of endianness if run on a big-endian host, though typically not an issue here). Split the sample array by index: the first 70% (using integer truncation like `(int)(n * 0.7)`) is Train, the rest is Test — do not shuffle, since the split must be time-based/sequential as specified. Compute mean and standard deviation (decide population vs sample stddev consistently, and be aware the grader tolerates a small MSE so either is likely acceptable, but stay consistent) using ONLY the Train samples — this is the whole point of the leakage fix, so double check you never touch Test samples when computing these statistics. Standardize both sets with `(x - train_mean) / train_std`, but only Test set values need to be exported. Compute the 95% CI of the standardized Test set's mean using z=1.96: `mean ± 1.96 * std_of_test / sqrt(n_test)` (use the standardized values' own std here, not the train std), writing `mean,lower,upper` to `/home/user/test_stats.txt`. Write the standardized Test array as raw `double` values (8 bytes each, native/little-endian) to `/home/user/test_features.bin` via `fwrite`, and verify sample count and byte size look right before finishing.