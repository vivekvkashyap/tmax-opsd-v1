Design the Rust parser as an explicit TLV state machine over the raw byte slice: loop while bytes remain, read a 1-byte type, bail out with `-1` immediately if there aren't enough bytes left for the declared length or the type is neither 1 nor 2, read the 1-byte length, then read that many value bytes. For type 1, treat the value as ASCII/UTF-8 and JSON-escape it if needed (not required for the simple ASCII case but be safe); for type 2, interpret exactly 4 bytes as little-endian `i32` (`i32::from_le_bytes`). Build the JSON manually with `String` and `write!`/`format!` — no serde — keeping the exact compact format (no spaces) and remembering that `out_json` must be null-terminated and never overrun `out_max_len`; if a full record wouldn't fit, that's a distinct failure case to consider. Return 0 only after a fully successful parse with the buffer written, and change nothing in `out_json` on any failure path. Compile with `rustc --crate-type=cdylib -o /home/user/libparser.so parser.rs`. In `app.py`, use `ctypes.CDLL`, set `argtypes`/`restype` explicitly, allocate a 1024-byte `ctypes.create_string_buffer`, call the function, check the return code, and decode the buffer up to its embedded null terminator before writing `output.json`.
