Build this as two clear passes to avoid subtle mismatches with a strict CSV diff check. Pass 1 (parallel): use `concurrent.futures.ProcessPoolExecutor` (or `multiprocessing.Pool`) to have each worker parse one log file into tuples of (epoch, ip, endpoint, response_time) — convert timestamps with `datetime.strptime(...).timestamp()`, being careful to treat them as UTC (e.g. use `calendar.timegm` or construct with `timezone.utc` rather than naive `.timestamp()`, which uses local time and will silently shift every epoch). Pass 2: aggregate per-endpoint mean/stddev across ALL parsed records first (skip endpoints with <2 samples or std==0), then in a second loop compute z-scores and keep only z_score > 3.0 strictly. Round z_score with `round(z, 3)` and format with `f"{z:.3f}"` so trailing zeros are preserved. Sort the final rows by (timestamp_epoch, ip_address) before writing the CSV with the exact header given. Only use stdlib — no pandas/numpy.
