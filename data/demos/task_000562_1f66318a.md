Read `auth_server.cpp` carefully to reconstruct the exact token pipeline: hex-decode the incoming token, XOR it against the raw bytes of the SSH public key file (used as a repeating XOR key), then split the plaintext on `|` into three fields. Look specifically at how the last field (the signature/hash) is checked — an `alg=none`-style bug means there's a special-case string value that the verifier accepts without ever validating a real signature, bypassing authentication entirely. To exploit it: read the exact bytes of the public key file (including any trailing newline it actually contains), construct the plaintext token as `username|role|<bypass-value>` using the privileged role you want to grant, XOR that plaintext against the key bytes the same way the server does (repeating the key cyclically), then hex-encode the resulting ciphertext bytes in uppercase. Write this as a standalone C++ program, compile it to the exact path required, run it, and redirect/save its stdout to the final token file as a single line with no trailing artifacts.
