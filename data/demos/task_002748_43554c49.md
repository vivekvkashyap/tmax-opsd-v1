Fix the build first: `parser.h` is missing `#include <stdint.h>` (needed for `uint16_t`/`uint32_t`), and the Makefile is missing `-pthread` even though `server.c` uses `pthread_create` — add both, then confirm `make` succeeds. To isolate the leaking log without brute-force testing all 100 files by hand, run the server under `valgrind --leak-check=full` (or track RSS growth), replay logs via `test_runner.sh`, and use delta debugging: bisect by replaying halves of the log set against a fresh server instance until a single file is implicated, then confirm it alone triggers the leak. Read `server.c`'s payload-parsing loop closely: it does `malloc(item_len)` for each length-value item *before* checking whether `offset + 1 + item_len` exceeds the total payload length, and on the truncated/malformed branch it `break`s out without freeing that last allocated `item` — that's the leak. Fix it by freeing `item` (or reordering: validate bounds before allocating) on the malformed-payload exit path. Recompute the exact leaked byte count for the faulty log by inspecting its raw bytes (the length-value fields) before your patch, and write the faulty filename and leaked-byte count to `/home/user/report.txt` in the exact specified format.