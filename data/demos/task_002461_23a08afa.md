Start services first, in order: launch `redis-server` in the background, then start the FastAPI app with `REDIS_HOST=127.0.0.1 OMP_NUM_THREADS=1 uvicorn app:app --host 127.0.0.1 --port 8000` from `/app/api/`, backgrounded, and poll (e.g. curl a request) until it responds before moving on. Then write `/home/user/clean_pipeline.py`: read the whole stdin as JSON, and for each item apply the cleaning steps in the exact stated order — strip, lowercase, replace any char outside `[a-z0-9 .,]` with a space, then collapse runs of spaces into one. For embeddings, URL-encode the cleaned text (e.g. `urllib.parse.quote`) before hitting `/embed`, and keep results in original input order (don't reorder via concurrency without re-sorting). Build the feature matrix and use `sklearn.model_selection.KFold(n_splits=3, shuffle=False)` with `Ridge` for each alpha in `[0.1, 1.0, 10.0]`, average MSE per alpha across folds, and pick the alpha with the lowest average MSE, breaking ties toward the smaller alpha. Print only the final JSON object to stdout — route any debug/log output to stderr — and `chmod +x` the script.
