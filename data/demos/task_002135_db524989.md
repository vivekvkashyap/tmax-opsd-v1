First read extractor.py to locate the two bugs precisely. For the ASCII crash: `bytes.decode('ascii')` throws on any byte >= 0x80; switch to `.decode('ascii', errors='ignore')` or `errors='replace'` (or operate on bytes with a regex against a bytes pattern) so parsing survives noise around the `C2CFG{...}` signature without altering the extracted length/payload semantics. For the infinite loop: trace by hand how padding is added and stripped each iteration — the bug is that padding gets appended then unconditionally stripped again on a later condition, so the string oscillates; fix it so padding is only added transiently for the `b64decode` call (not carried into the next iteration's state) and the loop exits once decoding stops changing the string or a URL prefix (`http://`/`https://`) is reached, with a hard iteration cap as a safety net. Run the fixed script against memdump.bin to get the real decoded URL. For the minimal-crash task, treat the ORIGINAL (unfixed) `parse_config_block` as a black box and use delta debugging (bisect/remove characters from each fuzz_corpus.txt entry while re-running the original function) to shrink an input to the smallest string still raising exactly `ValueError: Corrupt Header`.
