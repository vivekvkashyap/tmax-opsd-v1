Break this into its independent pieces and validate each before wiring them together. First inspect the DB schema (`sqlite3 /app/access_logs.db ".schema"`) to confirm `badge_scans`/`camera_events` columns. Second, process the video: use `cv2.VideoCapture` (or ffmpeg frame extraction) to step through frames, check the specific pixel region in the top-right corner for a solid red color match (RGB near (255,0,0)) above a coverage/area threshold to avoid noise false-positives, and record the frame's timestamp (frame_index / fps, or use `CAP_PROP_POS_MSEC`) whenever it's present; insert each detected event into `camera_events` with the appropriate `door_id`. Third, fix `generate_report.py`'s query: the current comma-join produces a Cartesian product with no join predicate — rewrite it as an explicit JOIN (or WHERE with both conditions) requiring matching `door_id` AND `ABS(scan_time - event_time) <= 5`, so only temporally-and-spatially correlated pairs count. Finally build `/home/user/audit_query.py` to accept `badge_id start_time end_time`, reuse that corrected join/window logic filtered to scans within `[start_time, end_time]` for the given badge, and print a single integer count with no extra text — test it yourself against a few manually-reasoned (badge, window) cases before trusting the fuzz harness.
