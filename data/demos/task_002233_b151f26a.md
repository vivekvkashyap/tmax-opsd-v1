Stage 1: run `tesseract /app/schema_clue.png stdout` and parse the recognized text to pull out `ROOT_NODE` and `EDGE_TYPE` — don't guess these; sanity-check the OCR'd values against what actually exists in the DB before trusting them. Stage 2: inspect the schema with `sqlite3 /home/user/network_topology.db ".schema"` and `.indexes edges`, identify the corrupted index (check `sqlite_master`/`PRAGMA index_list`), then `DROP INDEX` it. Stage 3: write a Python script using a recursive CTE (`WITH RECURSIVE paths(...) AS (base UNION ALL recursive step) SELECT ...`) that expands only edges matching the recovered `EDGE_TYPE`, tracking cumulative cost and path string; since recursive CTEs enumerate all paths, keep only the minimum-cost path per reachable target (in SQL or Python) and write the three-column CSV. Stage 4: in `query_sanitizer.py`, read the file, reject if it contains `--`, `/*`, a semicolon followed by more content, or DROP/DELETE/UPDATE/INSERT/ALTER (case-insensitive), and require the statement start with `SELECT`/`WITH` to accept; construct your own clean/evil test cases to validate before finishing.
