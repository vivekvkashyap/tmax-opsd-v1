Work in stages and verify each before moving to the next. First, install ffmpeg/libmicrohttpd-dev/build-essential, then extract frames with ffmpeg's rawvideo+gray pixel format and scale filter to guarantee 640x360 8-bit grayscale output, naming files frame_1.gray..frame_50.gray (ffmpeg's `-vframes 50` plus a `-vf select`/numbering trick, or just extract sequentially and rename to 1-indexed). Second, read /app/analyzer.c and locate the multithreaded reduction: the bug is a shared `global_sum` updated under a mutex in unpredictable thread-finish order, which changes floating-point rounding across runs. Fix it by giving each thread its own slot in a `double partial_sums[NUM_THREADS]` array (no locking needed since each thread only writes its own slot), then after `pthread_join`ing all threads, sum `partial_sums[0..N-1]` sequentially in fixed index order in the main thread — this makes summation order deterministic regardless of thread scheduling. Recompile with gcc and relink pthread/libmicrohttpd, then start it backgrounded on 0.0.0.0:9090 (e.g. `nohup ./analyzer &`) and confirm with `curl` that repeated requests for the same frame return byte-identical %f output before writing visualize.py to loop frames 1-50, hit the endpoint, and write results.csv + plot.png (matplotlib).