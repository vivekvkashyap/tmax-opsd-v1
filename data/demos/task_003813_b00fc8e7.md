Start by reading `src/bin/etl.rs` to locate where mean/std are computed — the leak is that standardization stats are derived from the full dataset before the K-fold split. Fix it so that, inside each fold's loop, mean and std are computed only from that fold's training rows, then those exact parameters (not global ones) are applied to transform both the training and validation rows of that fold before threshold tuning; rerun the ETL against the mock embedding service and write the newly (lower, since leakage previously inflated it) tuned threshold to `/home/user/app/best_hyperparameter.txt`. Start the auxiliary services first via `start_services.sh` and confirm ports 9000/9001 respond before touching the server. For `src/bin/server.rs`: bind to `127.0.0.1:8080`, implement `POST /retrieve` requiring header `Authorization: Bearer ds_secret_token` (401 if absent/wrong), fetch the query embedding from `http://127.0.0.1:9000/embed`, standardize it using the *global* mean/std computed over the entire dataset (production inference uses the full-dataset stats, deliberately different from the leak-fixed CV path), compare against documents from `http://127.0.0.1:9001/data`, and return the closest match meeting the tuned threshold as `{"match_id": ..., "confidence": ...}` or nulls if none qualify. Test with curl before finishing.
