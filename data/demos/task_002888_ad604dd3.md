Write `/home/user/doc_parser.c` to stream the compressed file directly with `gzopen`/`gzgets` (never shell out to `gunzip` first, and never load the whole file into memory) — read line by line, checking each line against the delimiter pattern `==== DOC_START: <filename> ====` with `sscanf` or `strstr`+manual parsing to extract the filename, then open a new output file under `/home/user/recovered_docs/<filename>` for each section found. For each line's content, run it through `iconv_open("UTF-8", "ISO-8859-1")` and `iconv()` before writing to the output file — do this per-line or per-buffer, not by assuming the whole content is ASCII-safe, since bytes like 0xA9/0xBF/0xE6 are only valid as single bytes in Latin-1 and must be converted to their correct multi-byte UTF-8 sequences. Remember to create `/home/user/recovered_docs/` with `mkdir` before writing into it. After compiling with `-lz` and running the parser, verify with `file` or `iconv -f utf-8 -t utf-8 <file> >/dev/null` that outputs are valid UTF-8, then `tar czf /home/user/clean_docs.tar.gz` the directory, and write the sorted (alphabetical) list of recovered filenames — one per line — to `/home/user/recovery.log`.