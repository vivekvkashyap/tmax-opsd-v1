Since the JSON has broken unicode escapes, don't try to parse it as real JSON — extract the `value` field with a regex/text tool that tolerates garbage elsewhere on the line, e.g. `grep -oP '"value"\s*:\s*\K([0-9.-]+|null)'` per line (or equivalent with `sed`/`awk`), which sidesteps the invalid `\u` sequences entirely since you never run a JSON parser over the whole line. In a `while read -r line` loop, extract the value; if it's empty or the literal `null`, forward-fill by reusing the last valid numeric value seen (start that "last valid" variable at 0 so the very first missing value defaults to 0). Accumulate a running sum and count with `bc -l` (since values are floats) and compute the mean once stdin closes. For the baseline reference, run OCR (e.g., `tesseract /app/baseline_target.png stdout`) to recover the printed number from the image — don't hardcode a guessed baseline value, actually extract it. Compute `abs(mean - baseline)` with `bc`/`awk` and print with `printf "%.2f\n"`, with no other stdout output. Make the script executable and test it by piping sample JSON-lines (including malformed unicode and nulls) into it.