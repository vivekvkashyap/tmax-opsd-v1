First fix and install the vendored simulator: inspect `/app/GraphSpecSim-1.0.0/graphspecsim/simulator.py` for the noise-generation call — `np.random.norm(...)` is not a real numpy function (the correct one is `np.random.normal(...)`); fix the typo, then `pip install -e /app/GraphSpecSim-1.0.0` (or equivalent) so `from graphspecsim import simulate` works, and sanity-check it with a tiny adjacency matrix before moving on. Then write `/home/user/pipeline.py` to: parse stdin exactly per the spec (seed, then N and E, then E edge pairs) into a dense symmetric NxN adjacency matrix; call `simulate(adj, seed=seed, steps=1024)`; take `np.fft.fft` of the resulting 1D signal and compute power as `abs(fft)**2`; slice indices 1..512 inclusive (drop DC at 0); apply a length-5 moving-average with `np.convolve(power_slice, np.ones(5)/5, mode='valid')` to get a 508-length array; find the top-3 indices by value with ties broken toward the lower index (e.g. `np.lexsort` on `(indices, -smoothed)` then take the first 3) and print them space-separated. Match the reference bit-for-bit — use the exact same operation order (FFT before smoothing, slice before smoothing) since this is checked against a fuzz oracle across many random graphs.
