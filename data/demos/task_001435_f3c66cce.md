Split this into three sub-goals. First get the vendored package building: fix the Python-2-style bare `print "..."` in `setup.py` to valid Python 3 syntax, then check whether the Makefile/`make install` references an unset environment variable — either export/patch it or just install with `pip install .` directly; confirm by importing the package and printing its version. Second, read `token_decode.py` to find where signature verification is skipped (e.g. it may only inspect the header's declared algorithm without cryptographic checking), then construct a JWT with `alg: none` and payload `{"sub":"admin","role":"superuser"}`, base64url-encoding each segment yourself and joining with periods, saving that string to the required path. Third — the highest-value work — write `/home/user/secure_validator.py` as an independent, from-scratch HS256 validator (`hmac`/`hashlib`, not the vulnerable library) that only accepts `alg: HS256` tokens, verifies the signature against the secret at `/etc/app_secret.key` with constant-time comparison, and prints exactly `INVALID`/exit 1 for anything malformed, unsigned, or mismatched, else `VALID`/exit 0. Test edge cases yourself (empty string, missing parts, alg=none, bad signature, valid token) since it's fuzzed for exact equivalence against a hidden reference.
