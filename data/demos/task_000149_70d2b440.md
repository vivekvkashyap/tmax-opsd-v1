Write the Go program in stages you can test independently. First, RLE-decompress: read the file two bytes at a time as (count byte, value byte) pairs and repeat the value byte count times into an output buffer — do this for the entire file since it's pure pairs with no other framing. Second, decode that byte buffer from UTF-16LE to UTF-8/native Go strings; Go's standard library doesn't do this directly, so use `golang.org/x/text/encoding/unicode` (module `golang.org/x/text`) with `unicode.UTF16(unicode.LittleEndian, unicode.IgnoreBOM).NewDecoder()` wrapped around a transform reader, or manually pair up little-endian uint16 code units and convert via `utf16.Decode` from the standard `unicode/utf16` package into a `[]rune`/string — the latter avoids needing an external module. Third, split the decoded text into lines and count lines whose first non-whitespace token is exactly `G1` (trim leading whitespace, then check the string starts with "G1" followed by a space/end, case-sensitive — don't match "G10" or "g1"). Iterate files in `/home/user/projects/cnc_data/*.rle`, sort filenames alphabetically before writing output, and format each line as `<name>.gcode: <count>` to /home/user/gcode_summary.txt.