Work through this stack from the bottom up, testing each layer before moving to the next. (1) Read `/home/user/service/main.cpp`, find where it binds the Unix domain socket, and correct the path literal so it binds to `/home/user/service/backend.sock` exactly as specified (also make sure any earlier `unlink()` of a stale socket targets the same corrected path). Implement the `/api/migrate` handler to read `/home/user/data/migration_target.txt` verbatim (no trailing-newline mismatches) and write `MIGRATION_COMPLETE_TO_<target>` to `/home/user/data/migration_status.txt`. Compile to `/home/user/service/backend_service` and launch it in the background; verify with `curl --unix-socket /home/user/service/backend.sock http://localhost/api/migrate` before touching Nginx. (2) Edit `/home/user/nginx/nginx.conf`: set `listen 8080;`, point `proxy_pass` at `http://unix:/home/user/service/backend.sock:` for the `/api/` location, add `allow 127.0.0.1; deny all;`, and make sure all paths (pid, logs, temp dirs) stay writable by the current user (no port 80, no root). Start with `nginx -c <path>` and check `nginx -t` first to catch config errors. (3) Use `socat TCP-LISTEN:9090,fork,reuseaddr TCP:127.0.0.1:8080 &` to forward 9090→8080. (4) Only then run the final `curl http://127.0.0.1:9090/api/migrate` and redirect its body to `/home/user/test_result.txt`. Confirm all three background processes (service, nginx, socat) are still alive at the end.