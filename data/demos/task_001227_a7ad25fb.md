Start by reading `extract_version` in `/home/user/workspace/artifact_utils.c` carefully: it copies characters before the first `_` into a local temp buffer and then into a fixed 16-byte `version_out` using an unbounded copy — the fix is to bound the copy to at most 15 characters plus a null terminator (e.g. use `strncpy(version_out, temp, 15); version_out[15] = '\0';` or truncate the loop itself), not to enlarge the buffer, since the ABI contract is a 16-byte output. Next build `build.sh`: compile the C file as a position-independent shared object (`gcc -shared -fPIC -o libartifact.so artifact_utils.c`) and separately invoke `python -m grpc_tools.protoc` (installing `grpcio-tools` if missing) against `artifact.proto` to generate the `_pb2.py`/`_pb2_grpc.py` stubs — these are two independent build steps, don't conflate them. In `server.py`, load the shared library with `ctypes.CDLL('./libartifact.so')`, declare argtypes so the C function receives a real writable 16-byte `ctypes.create_string_buffer(16)`, call it per RPC, and only start `server.start()`/blocking wait under `if __name__ == "__main__"` so importing the module doesn't hang tests. Finally write `test_client.py` to open a grpc channel to `localhost:50051`, call `GetVersion` with the given long filename, and write `Extracted: <version>` to `test_result.log` — verify manually that the returned string is exactly 15 characters plus terminator, not silently truncated further or overflowing.
