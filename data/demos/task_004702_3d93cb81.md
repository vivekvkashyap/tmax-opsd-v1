Treat the crash analysis and the timestamp-precision fix as separate debugging problems that both need fixing in the same rewritten script. For the crash, inspect /home/user/core.dump with `gdb` (e.g. `gdb python3 core.dump`, `bt`) to find the frame index / native call where the out-of-bounds read happened, then write a minimal reproducer that isolates just that frame's metadata read rather than re-running the whole pipeline each time. For the precision bug, notice that the buggy script likely uses Python `float()` to parse timestamp/microsecond strings from the video's metadata stream — floats lose precision on large microsecond values causing off-by-one frame-boundary errors; switch timestamp parsing to `decimal.Decimal` (parsing the string directly, not via float) to preserve exact precision. Also make sure argument handling uses `sys.argv` directly (no shell string interpolation) so paths containing spaces work correctly. Since grading is via fuzz-equivalence against a hidden oracle across many path/offset combinations, write your own fuzz harness that runs your fixed script against varied video paths (including ones with spaces) and offsets, and manually reason about correctness for edge cases (negative offsets, malformed metadata) since you won't have the oracle to diff against directly.
