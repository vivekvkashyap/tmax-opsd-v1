Start by inspecting the schema yourself: run `sqlite3 /home/user/audit.db ".schema"` (or query `sqlite_master`) to see the real table/column names for employees, systems, and direct access — don't assume names, discover them. Then build a recursive CTE that, for each employee, walks DOWN the management hierarchy (employee -> direct reports -> their reports, etc.) and unions in every direct-access row for that employee and all descendants; group by the top employee and aggregate the distinct system IDs they can reach. From that, filter to employees whose aggregated system-id set contains both the Trading system id (101) and Auditing id (202) — write this as a second CTE or subquery, not in Python, per the requirement to push work into SQL. Write the first CSV (`emp_id,department`) sorted by emp_id ascending. For the second report, use `RANK() OVER (ORDER BY COUNT(*) DESC)` grouped by department on the CoI-violating employees, keeping only departments with count >= 1, and break rank ties alphabetically with a secondary ORDER BY on department name. Test the script by running it and diffing output against your own manual trace of the hierarchy before trusting it.