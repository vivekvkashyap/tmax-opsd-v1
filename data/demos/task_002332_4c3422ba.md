Work in the given order: first compile the C filter with `gcc /home/user/filter_seqs.c -o /home/user/filter_seqs`, run it on the FASTA input to produce filtered.txt, then inspect that file to find the exact TARGET_SEQ line before writing any optimization code — don't hardcode assumptions about sequence content. In analyze.py, parse the `SequenceName,SequenceString` CSV format, locate TARGET_SEQ, and build the DFT objective: map each base character to its assigned weight to form a numeric array of length N, take `scipy.fft.fft`, and use the magnitude at index N//3 as the period-3 power. Since `scipy.optimize.minimize` minimizes by default, negate the magnitude to turn maximization into minimization. Set up the equality constraint (weights sum to 1) and bounds (0 to 1) exactly as specified, use the given initial guess and SLSQP method, and let the solver converge on its own rather than presetting the answer — verify convergence via `res.success`. Round the four resulting weights to 4 decimal places and write them into the exact nested JSON structure requested, matching key names precisely.