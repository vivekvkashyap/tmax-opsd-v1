The core pitfall here is precision loss: `doc_id` values exceed 2^53, so any code path that converts them through a Python float, a pandas default numeric dtype, or JSON's typical float parsing will silently corrupt the digits. Read `metadata.csv` and `corpus.jsonl` while forcing `doc_id` to be treated as a string or Python `int` throughout (e.g. read CSV with `dtype={'doc_id': str}` or parse it manually, avoid `pd.read_csv` defaults / `pd.merge` on a column that got upcast to float because of NaNs elsewhere in the same column, and when emitting JSON use big-integer-safe serialization rather than a library that stringifies via float). After the inner join, drop rows missing `author`. Tokenize `text` by splitting strictly on single space characters (not `str.split()`'s "any whitespace" default) and count frequencies across all remaining rows, breaking ties alphabetically after sorting by descending count. Write `/home/user/output/clean_dataset.jsonl` and `/home/user/output/top_tokens.txt` in the specified formats, then re-read the JSONL output yourself and confirm the doc_id digits match the originals exactly (not off by rounding).