Structure the C program around three sequential concerns: loading, indexing, and exporting. Read `/home/user/flights.csv` with straightforward line-by-line parsing (skip the header row, split on commas), then open an SQLite connection via `sqlite3_open`, create a matching table, and bulk-insert rows using prepared statements (`sqlite3_prepare_v2`/`sqlite3_bind_*`/`sqlite3_step` in a loop) for efficiency and correctness. Create the composite index with `CREATE INDEX idx_airline_delay ON flights(airline, delay)` before running your query — this is required by the task even though the dataset is small, so don't skip it. For the paginated query, use SQL itself rather than manual C filtering: `SELECT id, source, dest, delay FROM flights WHERE airline='SkyCorp' AND delay > 0 ORDER BY delay DESC LIMIT 3 OFFSET 3` gives you page 2 directly and avoids off-by-one errors in your own pagination logic. Serialize those 3 rows to `/home/user/page2.json` as a JSON array of objects with exactly the field names/order shown in the task (build the JSON string manually with `sprintf`/`fprintf`, escaping isn't needed for this clean numeric/short-string data). For the graph edge list, run a second query selecting all delayed SkyCorp flights ordered by delay descending, and stream `source,dest,weight` rows straight to the CSV with a header line. Compile exactly as specified (`-lsqlite3`) and run the binary once, then diff the two output files against the field values you expect from the CSV to catch ordering or filter mistakes.