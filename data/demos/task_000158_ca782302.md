Tackle this as three separable fixes layered on the existing codebase, verifying each independently before combining. First, read `main.cpp`/`processor.cpp` closely for classic C++ lifetime bugs: functions returning references/pointers to stack locals, raw `new` without matching `delete` (or missing RAII/smart pointers), and objects captured by reference in threads/lambdas that outlive their scope — fix these with value semantics, `std::unique_ptr`/`std::shared_ptr`, or by extending object lifetimes explicitly; compile with `-fsanitize=address` while iterating to confirm the crashes and leaks are actually gone. Second, replace the sequential chunk loop with a producer-consumer design: a `std::queue` (or similar) guarded by a `std::mutex` and `std::condition_variable`, with the main thread pushing WAV chunks and a `std::thread` pool of workers popping and computing RMS — since output order must be preserved despite concurrent completion, tag each chunk with its original index and either use a fixed-size results vector indexed by that number, or collect (index, rms) pairs and sort before writing JSON. Third, restructure the JSON serialization to the new `metadata`/`measurements`/`chunk_index`/`rms_value` schema exactly as specified. Build with CMake, benchmark with `time ./audioproc ...` to confirm you're comfortably under 200ms (tune thread pool size / chunk size if not), and validate the JSON shape with `jq` before finishing.