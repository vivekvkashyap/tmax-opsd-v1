Start by reading `/app/schema_clue.png` (e.g. with an OCR tool, or open it and inspect visually) to recover the actual mapping from obfuscated NoSQL keys to canonical field names (node id, parent id, backup size, timestamp) — do not guess key names, extract them from the image. Then design `/home/user/restore_planner.py` to: (1) parse each JSONL line using the discovered key mapping into a dict of node records; (2) build a lookup by node id and compute `total_restore_size` per node via memoized recursion/iteration that sums its own size plus all ancestors' sizes by walking up parent links until reaching a node with no parent (or a parent not present in the input); (3) compute `sibling_rank` by grouping nodes into partitions keyed by their parent id (using a single partition key, e.g. `None`, for all root nodes), sorting each partition's nodes by timestamp descending, and assigning ranks starting at 1 within each partition; (4) emit one JSON object per node with exactly the three required keys, printed to stdout sorted alphabetically by node_id. Handle malformed/missing parent references defensively (treat unknown parents as roots for ancestor summation) since the fuzz harness will generate randomized tree-shaped inputs of varying size. Test your script against several hand-built JSONL fixtures with known parent/child chains before trusting it against the real fuzz inputs.