Do these three pieces independently, in this order, verifying each before moving on. First, transcribe `/app/voicemail.wav` with the local `/usr/local/bin/whisper` tool (e.g. `whisper /app/voicemail.wav --model base --output_format txt`) and read the resulting transcript file to extract the exact timezone, locale, and log-directory path the speaker states — do not guess or hardcode plausible-sounding values, listen/read carefully since the verifier checks bit-exact/semantic correctness against what was actually said. Second, for `generate_manifest.sh`, treat `/app/oracle_generator` purely as a black-box you probe: run it with several different sample pod-name/timezone/locale combinations and diff the outputs to reverse-engineer the exact YAML structure, indentation (spaces vs tabs, indent width), quoting, and field order — then reproduce it in bash using only `cat <<EOF ... EOF` with `$1`, `$2`, `$3` substituted, matching whitespace character-for-character. Verify by running both your script and the oracle with the same arguments and `diff`-ing stdout until it's empty. Third, write `cron_wrapper.sh` to work with an empty `PATH` — use absolute paths for every command (e.g. `/bin/mkdir -p`, and invoke `/home/user/generate_manifest.sh` by its absolute path since a bare relative call may fail with no PATH), create the log directory if missing, and redirect stdout to `latest_manifest.yaml` inside it. chmod +x both scripts.