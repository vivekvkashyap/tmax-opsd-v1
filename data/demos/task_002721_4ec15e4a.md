Set up an isolated venv first: `python3 -m venv /home/user/prep_env && /home/user/prep_env/bin/pip install numpy`, and always invoke your script via that venv's python so the environment requirement is actually satisfied. In `/home/user/preprocess.py`, parse `/home/user/raw_data/sequences.fasta` yourself (simple `>` header split, don't assume a FASTA library is installed) to build a pdb_id -> sequence length map, and parse each `.pdb` file's ATOM lines yourself, filtering strictly on the atom-name field (columns 13-16 in fixed-width PDB format, or split by whitespace and check the atom name token equals `CA`) to avoid picking up other atom types. Compute center of mass as the mean of CA x/y/z, then per-atom Euclidean distance to that CoM, then the *population* standard deviation (divide by N, not N-1) of those distances. Add `1e-8` to the denominator before dividing (not conditionally only when std is exactly zero) to match the required numerical-stability behavior, take the max normalized distance, and round every float to exactly 4 decimals. Write rows sorted alphabetically by pdb_id with the exact header given.
