Build `/home/user/analyze.py` to accept exactly four positional args in order (video, users_csv, events_csv, output_jsonl) via `sys.argv`, since the automated fuzz tester invokes it directly with varying file pairs — do not hardcode any of the sample paths. Join the two CSVs on `event_id` (an inner join, e.g. via pandas merge or dict intersection). Validate strictly: username must fully match `^[A-Z]{3,4}$` (use `re.fullmatch` or anchored regex, not a loose `re.search`), and timestamp_sec must parse as a float and satisfy `0.0 <= ts <= 10.0` — wrap the float conversion in try/except so malformed numeric strings are silently dropped rather than crashing the script. For each surviving event, open the video once with `cv2.VideoCapture`, read its `fps`, compute `frame_idx = int(ts * fps)`, seek with `cap.set(cv2.CAP_PROP_POS_FRAMES, frame_idx)`, read the frame, convert to grayscale, take `gray.mean()`, and apply `math.floor()` — not `round()` — to get an integer. Sort the final results by `event_id` before writing one JSON object per line. Test locally against `/app/surveillance.mp4` with synthetic CSVs covering edge cases (negative/out-of-range timestamps, near-boundary usernames) since exact bit-for-bit matching against a reference implementation is required.