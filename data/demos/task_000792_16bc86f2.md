Split this into archive recovery and service stages, testing each independently. First, extract `/app/backup_root.tar.gz` and traverse it with loop protection: use `find <dir> -name 'chunk_*.bin.gz'` combined with a visited-inode/path tracking mechanism (e.g. Python's `os.walk` with `followlinks=False`, or track realpath of each directory visited and skip if already seen) so circular symlinks don't cause infinite recursion — never blindly follow symlinks during the walk. Decompress each found chunk (`gzip.open` or `zcat`), sort the chunk files by the numeric index embedded in their filename (extract with a regex, not lexical sort, since chunk_2 must sort before chunk_10), concatenate their timestamp lines in that order, then also sort the full timestamp list numerically ascending for the `/api/timestamps` response. Second, build the HTTP server (Python's `http.server` or similar) binding to `127.0.0.1:9090` with the three routes: `/ping` returns literal `pong`; `/api/timestamps` returns the comma-joined sorted timestamps; `/api/frame_brightness?time=<t>` shells out to ffmpeg to extract exactly one frame at that timestamp as raw 8-bit grayscale (`-pix_fmt gray -f rawvideo`), reads the raw bytes, computes `sum(bytes)//len(bytes)` (floor division, matching Python's `//`), and returns that integer as plain text. Print `SERVICE_READY` once the server is listening, then test all three endpoints with `curl` before finishing.
