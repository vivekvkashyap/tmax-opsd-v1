Chain independent, individually-verifiable stages. (1) OCR: run `tesseract /app/molecule_data.png stdout` (try `--psm` variants if text is missed) and parse the resulting lines into integer edge pairs — don't hardcode edges, derive them from the OCR text and build an adjacency list. (2) Simulation: write `/home/user/simulate.c` that, from the adjacency list, runs many independent random walks starting at node 0, at each step picking a uniformly random neighbor (`rand()` mod degree, seeded properly), counting steps until returning to node 0, and averaging over 1,000,000 walks (note: mathematically the expected return time equals `2*|E|/deg(0)` for a random walk on an undirected graph, so use your simulation result — sanity-check it converges near that closed form rather than assuming it, since it's a good way to catch simulation bugs). (3) Read `/app/reference.csv`, extract node 0's expected time, compute `abs(simulated - reference)`. (4) Plot the first 1000 walks' return-time distribution (e.g. matplotlib histogram) to `/home/user/return_distribution.png`. (5) Stand up a lightweight HTTP server (Python `http.server`/Flask) bound to `0.0.0.0:8080` exposing `GET /stats` returning `{"average_return_time":..., "error":...}` as JSON, launched in the background (e.g. `nohup ... &`) so it stays reachable, and curl it yourself to confirm before finishing.