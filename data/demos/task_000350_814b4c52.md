Work bottom-up: fix the build first, then write the query. Inspect `/app/sqlite/build.sh` and check every linker flag character by character — a single-character typo in a flag name (e.g. a misspelled `-lpthread`) will cause a silent-looking link failure; fix it so the script compiles a given C file plus the SQLite amalgamation successfully. In `shortest_path.c`, open an in-memory SQLite db (`sqlite3_open(":memory:", ...)`), create an `edges(source TEXT, target TEXT, latency REAL)` table, then parse `/app/data/network.csv` line by line with `fscanf`/`strtok` (splitting on commas, converting the latency field with `strtod`) and insert each row via a prepared statement in a loop (much faster than string-concatenated INSERTs). For the shortest path, write a recursive CTE: an anchor selecting the start node with distance 0, a recursive step joining `edges` to extend paths by one hop and accumulating latency, then `SELECT MIN(total_latency) FROM ... WHERE node = 'NODE_END'`; bound the recursion (e.g. with a hop-count column and a WHERE limit) to avoid runaway expansion on cycles. Compile with the fixed `build.sh`, run the binary, and write only the resulting float to `/home/user/min_latency.txt` — the verifier checks absolute error <= 0.01 against the true shortest path, so validate your CTE logic against a small hand-computed graph before trusting it on the full CSV.
