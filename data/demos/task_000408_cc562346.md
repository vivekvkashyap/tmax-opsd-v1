Build and test each moving part in isolation before chaining them in the hook. First get the git mechanics right: a `post-receive` hook reads `<old-sha> <new-sha> <ref>` lines on stdin; to materialize the pushed tree into a staging directory from within the bare repo, use something like `git --work-tree=/home/user/deploy_staging --git-dir=/home/user/edge-firmware.git checkout -f main` (creating the staging dir first), then `os.chmod(path, 0o600)` on the specific file. Second, test the pexpect flow against `/home/user/edge_vm.py` standalone (outside the hook) first: spawn it, and expect/send each prompt in order exactly as specified, reading the target file's contents fresh each time so the payload sent matches what's on disk. Only after that round-trip logs `Update SUCCESS` should you add the `smtplib`-based email send (talk to `localhost:1025` with `sendmail` or raw SMTP commands, using the exact sender/recipient/subject/body given). Finally wire the hook into git, chmod it executable, and trigger the whole path end-to-end via a real `git push` from the source repo.
