Phase 1 is video forensics: use `ffmpeg -i /app/incident_record.mp4` to extract frames (e.g. one image per frame or per short interval), then OCR each with `tesseract` to detect which frames' on-screen text contains the literal substring `--db-pass=`; count them precisely (avoid double-counting near-duplicate frames or missing frames due to too coarse a sampling rate — extracting every frame is safer than downsampling) and write the integer to `/home/user/leak_frame_count.txt`. Phase 2 will be fuzz-tested against a hidden oracle across thousands of random inputs, so implement the spec literally rather than approximately: read one stdin line, use a regex like `(--db-pass=)(\S+)` to replace the captured value with `[REDACTED]` (keep the flag itself), then check the *redacted* result for the literal substrings `<script>` or `; rm -rf` and append ` [FLAG:INJECTION_XSS]` if either is present, printing the final line with a trailing newline. Handle lines with no match, multiple flags, or edge whitespace consistently. Also create `/home/user/secure_audit_logs/` with `chmod 700`. Test your script against varied crafted inputs (with/without the flag, with/without injection markers, combinations) before finishing.