OCR `/app/legacy_diagram.png` (e.g. `tesseract /app/legacy_diagram.png stdout`) to recover the exact `PORT` and `QUOTA` integers the diagram specifies — visually confirm the digits against the image since OCR on stylized diagrams can misread numerals. Scaffold the Rust service with `cargo init /app/service` and pick a lightweight HTTP framework (e.g. `actix-web`, `axum`, or even a minimal `std::net::TcpListener` handler if you want zero dependencies) bound to `0.0.0.0:<recovered PORT>`. Implement `GET /api/status` to return the JSON literal with the recovered `port`/`quota` values embedded and `"status": "running"`, and `POST /api/backup` to shell out (`std::process::Command`) to `tar -czf /app/backup/archive.tar.gz -C /app/storage .` (or equivalent), then respond with the success JSON only after confirming the command exit status was successful. Build with `cargo build --release`, launch the release binary detached (`nohup ./target/release/<bin> &` or similar) so it keeps listening after your session ends, and verify with `curl` against both endpoints before finishing — leave the process running.
