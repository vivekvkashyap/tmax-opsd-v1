Since the source is lost, treat `/app/loc_oracle` as a black box and reverse-engineer it empirically before writing any Go code: feed it small, deliberately varied CSV rows on stdin (mixed case, digits, punctuation, unicode, empty fields, extra spaces) and observe stdout byte-for-byte to pin down the exact output delimiter/column order, whether/how strings get lowercased, exactly which characters survive normalization (test punctuation, tabs, and multiple consecutive spaces separately to see if they're stripped or preserved), and confirm the numeric distance column with known Levenshtein-distance test pairs. Only after you're confident in the normalization and distance rules, implement `main.go` reading stdin CSV rows, applying your normalization, computing edit distance, and printing the two output rows (ES then FR) per input line in the observed format. Before finishing, script a fuzz-comparison harness that generates many random CSV rows across the same character space, runs both `/app/loc_oracle` and your compiled `/home/user/loc_tool`, and diffs stdout exactly — fix any mismatches found, especially at edge cases like empty strings or strings with only symbols.
