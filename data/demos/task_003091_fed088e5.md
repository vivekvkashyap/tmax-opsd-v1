For safe extraction, open `/app/artifacts.zip` with Python's `zipfile`, and for each `ZipInfo` entry compute the fully resolved destination path (`os.path.realpath(os.path.join(dest_dir, entry.filename))`) and check with `os.path.commonpath` (or a prefix check on the resolved path) that it still lives inside the resolved `/home/user/extracted` directory — skip (don't write) any entry that resolves outside, including ones using `../` traversal; never trust `zip.extract()` alone since it doesn't guard against zip-slip. After safely extracting `metadata.json` and `transmission.wav`, load the JSON to get `message_start_sec`, `message_end_sec`, `smoothing_window`, and read the WAV with a library like `wave` or `scipy.io.wavfile` to get the sample rate and samples. Crop the sample array using `start_sec/end_sec * sample_rate` as integer sample indices, then apply a causal moving-average filter of the given window size (each output sample i is the mean of samples `max(0, i-W+1)` through i — not a centered window), and write the result to `/home/user/clean_transmission.wav` preserving the original sample rate and dtype/format.