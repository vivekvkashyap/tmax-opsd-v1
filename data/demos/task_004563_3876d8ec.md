Work through the polyglot pieces in dependency order. First run `tesseract /app/server_config.png stdout` to OCR out the port, API key, and target migration version — don't hardcode values, actually extract them. Fix `/home/user/clib/math_ops.c`'s `add` function (check the operator) and `Makefile` (check indentation — `make` recipe lines must be tab-indented, not space-indented) so `make` produces `libmathops.a`. In the Rust project, add the missing `unsafe { ... }` block around the FFI call to the C `add` function and fix any type mismatch (the C function likely returns `c_int`/`i32`; make sure the Rust binding's signature and argument types match exactly), then confirm `cargo build --release` produces the binary. For the schema migration, write a Python script that lists `v*.sql` files in `/home/user/schema/`, sorts them by their numeric version, concatenates only the files up to the OCR'd target version, and executes them against `/home/user/app.db` via `sqlite3`. Finally write `server.py` (Flask/FastAPI) that validates the `X-API-Key` header against the OCR'd key, listens on the OCR'd port, calls the compiled Rust binary via `subprocess` with the two integers, parses its stdout, and returns `{"result": ...}`; launch it in the background (e.g. `nohup ... &`) so it's live for testing.