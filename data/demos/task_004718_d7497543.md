Treat the legacy binary strictly as a black box you may execute for observation, never inspect or copy — build your own independent solution. First fix the SQL: the starter query joins `restore_points` and `datacenter_map` without a join condition, producing a cartesian product; add an explicit `WHERE r.environment = d.environment` (or proper `JOIN ... ON`) so each restore point maps to exactly one `cost_multiplier`. Then build the weighted directed graph: for each restore point ID, read its `<id>.json` file in the deps directory to get `{target_id, base_cost}` edges, and multiply each `base_cost` by the *source* node's `cost_multiplier` (looked up from the fixed SQL join) to get the true edge weight — a common mistake is using the target's multiplier instead of the source's. With the graph assembled, run Dijkstra's algorithm from source to target, reconstruct the path via predecessor pointers, and print it as a comma-separated ID list, or `UNREACHABLE` if no path exists. Test your script by comparing its output to the oracle across several source/target pairs before considering it done, since the grader will fuzz many random pairs for bit-exact equivalence.
