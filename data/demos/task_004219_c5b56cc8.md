Debug this as a layered systems problem, not a rewrite: first get `libscheduler.so` to load by inspecting `/home/user/app/clib/scheduler.c` for missing `extern "C"` linkage or symbol visibility issues (build with `gcc -shared -fPIC -o libscheduler.so scheduler.c`, then `nm -D libscheduler.so` to confirm the expected symbols are exported undecorated), fixing whichever prevents `ctypes.CDLL` from resolving function names. Next, align the Python `ctypes.Structure` field order, types, and sizes in `worker.py` exactly with the C `struct Graph`/`Node` layout (check with `ctypes.sizeof` against the C `sizeof` to catch padding mismatches) — this is almost always where memory corruption comes from. Get the Flask API, Redis, and worker talking end-to-end first with a single test job before optimizing anything; only after correctness is confirmed should you profile the FFI boundary (e.g. batch constraints into one call instead of per-node ctypes calls, avoid unnecessary copies) to hit the 1.5s/5000-job target. Write `integration_test.py` to submit all 5000 jobs, poll/wait for completion via Redis, and write the elapsed wall-clock seconds as a bare float to `/home/user/app/perf.log`.