Build the script around a per-file try/lock/verify/parse pipeline: for each `.tar.gz` in the backups directory, open it, call `fcntl.flock(fd, fcntl.LOCK_EX | fcntl.LOCK_NB)` and catch `BlockingIOError`/`OSError` to skip on lock failure (release the lock in a finally block after processing). To check integrity, don't just trust the `.tar.gz` extension — actually attempt to open it with `tarfile.open(path, 'r:gz')` inside a try/except, since corrupted or truncated gzip data will raise on open or on `getmembers()`/read. Only if the archive opens cleanly, look for a member literally named `metadata.json` at the archive root, extract and `json.loads` it, and pull `app_name`/`version`; if the member is missing or the JSON is malformed, mark the archive `corrupt` with `N/A`/`N/A` rather than crashing. Accumulate rows in memory, sort them alphabetically by filename before writing, and write the CSV with the exact header row. Run the script once at the end to produce `/home/user/backup_audit.csv`.
