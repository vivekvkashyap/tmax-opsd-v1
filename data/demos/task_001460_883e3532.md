Do not parse the CSV line-by-line with naive `str.split(',')` — embedded newlines inside quoted fields will break that. Open the file with `open(path, encoding='cp1252', newline='')` and feed it to Python's `csv.reader` (or `csv.DictReader`), which correctly handles quoted multi-line fields per RFC 4180; this is the key place naive attempts fail. For each row: replace `\r\n` and `\n` inside the `metadata` field with a single space (do this after CSV parsing, not before, so you don't corrupt the quoting). Try `float(temperature)` in a try/except (or check for empty string) to drop unparseable/empty temperature rows. Group remaining rows by `sensor_id` using a dict of lists, sort each group's rows by `timestamp` string ascending (ISO timestamps sort correctly as strings), then walk chronologically and keep only the first 2 rows per group where `temp > 20.0`. Write each kept record as one JSON object per line to `/home/user/processed_data.jsonl` with keys `ts, id, temp, meta` in that field order, then actually run the script to produce the output file.