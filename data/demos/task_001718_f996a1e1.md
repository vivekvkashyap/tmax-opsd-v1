Build this as a straightforward sequential ETL, checking intermediate state at each step rather than chaining everything blind. Extract the tarball (`tar -xzf .../demographics.tar.gz`) to get `demographics.csv`, load it and `biomarkers.csv`, and merge on `patient_id` (inner or outer as appropriate — check whether every patient_id appears in both files before deciding). Apply imputation and clipping in the specified order and only to the named columns: missing `age` -> 50, missing `bmi` -> 25.0, `heart_rate` > 150 -> clipped to 150 (strictly greater than, not >=). Load `weights.json` and compute each patient's risk score as the dot product of (age, bmi, heart_rate) with their respective weights — apply weights only after imputation/clipping, since the model expects clean inputs. Format `risk_score` with a fixed 2-decimal format string (e.g. `f"{score:.2f}"`), sort rows by `patient_id` ascending numerically (cast to int for sorting, not lexicographic string sort), and write with the exact header `patient_id,risk_score`. After writing, diff your output against your own hand-computed expectation for one or two rows to catch imputation-order or weight-lookup mistakes before considering it done.
