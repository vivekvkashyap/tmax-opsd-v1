Treat /app/netdiag_obfuscated as a black-box oracle you must characterize empirically, then confirm with static analysis — don't guess an algorithm. Feed it small, controlled inputs (single characters, then short known strings, then strings with repeated/incrementing characters) and diff the outputs to spot patterns like byte reordering, arithmetic/bitwise transforms, or encoding changes; cross-check hypotheses with `strings`, `objdump -d`, or `gdb`/`ltrace` to see what operations the binary actually performs on each byte. Once you have a hypothesis, write a small test harness that runs both the binary and your draft Python replica over a batch of random inputs and compares outputs byte-for-byte, iterating until they always match — this is far more reliable than eyeballing a handful of examples. Only after replication is verified should you build /home/user/deploy_service.sh: make it idempotent by using `mkdir -p` for the systemd user directory, writing the unit file with `> `overwrite (not append), and always running daemon-reload/enable/restart via `systemctl --user` regardless of prior state, redirecting the service's stdout into /home/user/netdiag.log via StandardOutput=append or shell redirection in ExecStart.