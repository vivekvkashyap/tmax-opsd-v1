Do the four parts in order and verify each before moving on. (1) Create the image with `dd if=/dev/zero of=/home/user/backup.img bs=1M count=20` then format it with `mkfs.ext4 /home/user/backup.img` (no mount needed/possible without root — just confirm formatting with `file /home/user/backup.img`). (2) Append a single fstab-style line to `/home/user/system_fstab` with fields in the standard order: device, mountpoint, fstype, options, dump, pass — space-separated, matching exactly what the task specifies for each field. (3) Since `/home/user/legacy_backup.sh` prompts interactively, don't try to pipe answers in via plain stdin redirection (fragile with `read -p` prompts) — use Python's `pexpect.spawn(...)`, then call `child.expect('Password:')`, `child.sendline(...)`, and repeat for each of the three prompts in the exact order given, finishing with `child.expect(pexpect.EOF)` to let the script complete and produce the tarball. Confirm the tarball actually appears at the expected path after running your automation script. (4) Compute `BACKUP_ARCHIVE_CREATED` and `BACKUP_IMG_SIZE` from the real filesystem state (`os.path.exists`, `os.path.getsize`) rather than hardcoding, and write the three-line log in the exact specified format.