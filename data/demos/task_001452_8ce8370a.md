Write a straightforward Python ETL script rather than trying to do regex parsing in the shell. Use `csv.DictReader` on `/home/user/raw_logs.csv`, and for each row apply a regex like `r'(\d+)%'` to pull the CPU integer and `r'(\d+)GB'` to pull the memory integer out of `report_text`. Normalize CPU by dividing by 100.0; convert memory to MB by multiplying by 1024. Accumulate per-`server_id` lists (or running sum/count) of normalized CPU and running total of MB using a dict keyed by server_id. After processing all rows, for each server compute the average CPU with `round(sum/count, 2)` — use Python's `round`, and double check the exact rounding behavior expected (round-half-to-even vs round-half-up) matches simple test cases if results seem borderline. Sort server_ids alphabetically, then write rows with `csv.writer` to `/home/user/db_import.csv` with header `server_id,avg_cpu_norm,total_mem_mb`, memory as integer, average CPU as the rounded float. Run the script and inspect the output file's exact formatting (no extra quoting, standard comma delimiter) before finishing.