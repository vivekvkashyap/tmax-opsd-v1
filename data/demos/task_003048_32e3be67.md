Do the backup first, before editing anything, so it captures the true "pre-hardening" state: `tar -czf /home/user/backup/pre_hardening.tar.gz -C /home/user backend nginx` (create `/home/user/backup` first if missing). Then edit `/home/user/nginx/nginx.conf` and change only the `proxy_pass` directive's socket path to `unix:/home/user/backend/secure.sock`, leaving every other directive (listen port, pid path, etc.) untouched. In `/home/user/backend/server.c`, change the hardcoded socket path constant to `/home/user/backend/secure.sock`, and immediately after the successful `bind()` call add a `chmod("/home/user/backend/secure.sock", 0600);` (include `<sys/stat.h>` if needed) so the socket file itself gets restrictive permissions right after creation. Compile with `gcc /home/user/backend/server.c -o /home/user/backend/server`. Write `/home/user/ci_deploy.sh` as an orchestration script: start the compiled server in the background, start nginx with the exact `nginx -c ... -p ...` invocation given, `sleep` briefly, `curl` the `/api/status` endpoint through nginx (port from the conf) and redirect output to `/home/user/pipeline_result.log`, then explicitly stop nginx (`nginx -s stop -c ... -p ...`) and kill the backend process, and finally `chmod +x` the script itself. Run the script once to produce the log and confirm all files/permissions land exactly at the specified paths.