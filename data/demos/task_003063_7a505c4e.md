For Part 1, open /app/loctool-1.0.0/loctool/extractor.py and find the retry loop around the transient-failure simulation; the bug is that `self.records` (or an equivalent accumulator) is appended to on each attempt without being reset before a retry, so a failed-then-retried extraction leaves stale entries mixed with the successful ones — fix it by clearing/reinitializing the records list at the start of each attempt (or only committing accumulated records after a fully successful pass). Verify by setting `SIMULATE_FLAKE=1` and running extraction, checking output length equals the input size exactly, not inflated by duplicates. For Part 2, write validate.py with argparse for `--source`/`--target`, load both JSON files, check target's top-level key set equals source's exactly (no missing/extra), then for each key extract the set of `{name}`-style format placeholders from both strings (e.g. via a regex like `\{(\w+)\}`) and require the sets match regardless of order or intervening text; exit 0 only if every key passes both checks, else exit 1. Test your script directly against the clean/evil examples implied by the task before considering it done, since it will be fuzzed against many more hidden variants of the same two rule types.