For Part 1, don't try to eyeball frames: use `ffmpeg` to crop the top-left 50x50 region with `-vf "crop=50:50:0:0"`, then either pipe frames through a brightness-measuring filter (`signalstats` or `blackframe`-style analysis) or extract frames as PNGs with `-vf "crop=50:50:0:0,fps=<video_fps>"` and compute average grayscale per frame with a small script (e.g. Python/PIL or ImageMagick `convert ... -format "%[fx:mean*255]"`). Convert frame index to seconds using the video's actual frame rate (probe it with `ffprobe`), not an assumed value, and format timestamps to exactly 2 decimals. For Part 2, resist the urge to do full graph traversal in Bash loops — use `awk` to bucket edges by timestamp into a nested associative array (`edges[time][src][dst]=1`), then for each timestamp check every edge (u,v) for a direct return path v->w->u forming a 3-cycle; exit 1 immediately on first match, exit 0 if none found across all timestamps. Test against both provided corpora directories to confirm correct exit codes before finalizing.