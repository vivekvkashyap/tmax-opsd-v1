First recover the schema mapping from the image — run `tesseract /app/schema_rules.png stdout` (try different `--psm` values if the table layout garbles) and manually verify each Entity->Node/RecursiveEdge row you extract against the image before hardcoding it, since every generated Cypher string depends on getting this mapping exactly right. Once you have the mapping, write the C program as a straightforward line-based dispatcher: read each stdin line, tokenize on whitespace, switch on the first token (MATCH_HIERARCHY / AGGREGATE_REACH / FIND_INTERSECT), look up the entity's mapped node label and recursive edge name from your recovered table, and printf the exact format string given in the task description for each command — pay close attention to quoting, spacing, and the trailing semicolon since the verifier does exact string matching against a reference generator. Handle unknown entity names or malformed lines defensively (the fuzz harness may send edge cases). Compile with `gcc -O3 cypher_generator.c -o cypher_generator`, then manually pipe the worked example from the task description through your binary and diff the output character-for-character before considering it done.
