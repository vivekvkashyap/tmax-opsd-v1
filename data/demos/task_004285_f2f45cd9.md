Fix compilation first, then build up functionality layer by layer, testing each service in isolation before composing them. In `/home/user/app/chat-server`, read `main.go`/`router.go` for leftover Rust syntax (e.g. `fn` instead of `func`, `let` instead of `:=`, wrong import paths) and correct it to valid Go, running `go build ./...` repeatedly until it compiles clean. Implement `ParseRoomID(path string) (string, error)` to extract the `{room_id}` segment from a path like `/ws/room/<id>` (validate alphanumeric), and use a WebSocket library (e.g. `gorilla/websocket`, check `go.mod` for what's already vendored) to upgrade `/ws/room/{room_id}` connections, checking the `Authorization: Bearer <token>` header against the required token and returning 401 on mismatch before upgrading. On each received text message, write back `Room <room_id>: <message>`. For the property test in `router_test.go`, use `testing/quick.Check` with a generator restricted to alphanumeric strings, asserting `ParseRoomID` round-trips the input; run `go test -v` and redirect output to `/home/user/app/test_results.log`. Fix `nginx.conf`'s `proxy_pass` and add the `Upgrade`/`Connection` header forwarding required for WebSocket proxying. Start Redis, the Go binary, and nginx in the background, verify end-to-end with a manual WebSocket client test, then write `ready.txt`.
