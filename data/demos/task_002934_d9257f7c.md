Handle the two parts independently and validate each in isolation. For the vendored Gunicorn bug: grep the socket-binding code under `/app/gunicorn` for where it strips the `unix:` prefix from a bind address (look in the sock module for a slice like `address[N:]`); `unix:` is 5 characters, so a slice using `[6:]` is off-by-one and drops a leading character from the path — fix the slice index, then verify directly by importing the class and constructing it with a known `unix:/path` string, checking the resulting socket path matches exactly (don't just trust the code looks right, actually run it). For the config generator: since you're told to match an oracle binary byte-for-byte, don't guess the output format — call `/app/oracle_gen` yourself with a few hand-crafted JSON payloads (varying users, empty list, quota above/below the threshold) via stdin, and diff its exact stdout (spacing, indentation, newlines, comment text) against a first draft of your Python script using the same payloads. Iterate until every test payload's output matches byte-for-byte, paying close attention to indentation width and the exact `# Quota exceeded` comment text/placement, and handle the empty-list case explicitly (print nothing, not even a blank line).
