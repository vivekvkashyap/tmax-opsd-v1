Break this into recovering the issuer value and then implementing a robust, generic exploit script. Run `tesseract /app/diagram.png stdout` (or similar OCR invocation) and search the output text for a line matching `ISSUER_ID: <value>` to recover the exact issuer string — don't hardcode a guessed value, read what OCR actually returns and strip any stray whitespace/noise characters OCR commonly introduces. For the exploit, manually construct a JWT with header `{"alg":"none","typ":"JWT"}` and payload `{"admin":true,"iss":"<recovered_value>"}`, base64url-encoding each part (no padding, per JWT spec) and joining as `header.payload.` (empty signature, trailing dot) — this is the canonical `alg=none` bypass, so don't rely on a JWT library that might reject `none` or add an unwanted signature. Send this as `Authorization: Bearer <token>` in a GET to `<base_url>/api/admin/dump` using `requests`, read `sys.argv[1]` as the base URL, and print only the raw response body (no extra logging) so automated parsing works. Since it'll run against 20 hidden servers, make the script resilient to network hiccups (reasonable timeout, no crash on non-200) and test it yourself against a local Flask stub implementing the same vulnerable check before finishing.