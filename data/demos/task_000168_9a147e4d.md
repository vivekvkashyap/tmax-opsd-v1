Work through each artifact methodically. First find the service: scan the range with something like `for p in $(seq 8400 8450); do curl -sk --max-time 1 https://localhost:$p/ >/dev/null && echo $p; done` (or `ss -tlnp`/`nc`) to identify which port actually answers TLS — write only the digits to `suspicious_port.txt`. Extract its certificate with `openssl s_client -connect localhost:<port> -showcerts </dev/null 2>/dev/null | openssl x509 -outform PEM > extracted_cert.pem` (grab the leaf cert, not the whole chain dump). For permissions, iterate `.dat` files and check each one's mode bits for any "other" read/write/execute (e.g. `stat -c '%a'` and inspect the last digit, or `find -perm -o+rwx`); only chmod 600 the ones where an other-bit was actually set, and log exactly those basenames sorted with `sort`, skipping files that were already secure. Finally edit `decrypt_data.py` to point its URL at the discovered port and pass `verify='/home/user/incident_report/extracted_cert.pem'` to `requests.get` (never `verify=False`) so the connection is genuinely authenticated against your extracted cert, then run it and confirm `decrypted_intel.json` is produced.
