Start with the log: grep `/home/user/web.log` for the anomalous entry (the one whose content looks like a shell command rather than a normal message — e.g. contains `curl`/pipe-to-shell patterns), then pull the IP address embedded in that payload string with a regex (e.g. an IPv4 pattern match) rather than assuming a value. In `logd.c`, the vulnerability is that a log line is interpolated unsanitized into a `system()`/`popen()` shell command string — remove all shell invocation entirely and replace it with direct `fopen("/home/user/alerts.log", "a")` + `fprintf`/`fputs` plus an explicit `\n`, writing the raw line content safely without shell interpretation. Compile the patched file with `gcc -o /home/user/logd_fixed logd.c` and confirm it compiles cleanly. Compute SHA256 of the original file with `sha256sum` before you start editing (or via `git`/a backup copy), and of the patched file afterward. Build `remediation.sh` as a one-line `iptables -A INPUT -s <extracted-ip> -p tcp -j DROP` and `chmod +x` it. Finally run `logd_fixed` against the original log to confirm the payload lands harmlessly in `alerts.log` with no command execution.