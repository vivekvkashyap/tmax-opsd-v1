Read hash_impl.cpp carefully and reimplement compute_hash exactly (same FNV-1a-style constants 0x811c9dc5 and 0x01000193, same XOR-then-multiply order, same unsigned 32-bit width) in cracker.cpp — an off-by-one in operator order or using signed int instead of unsigned will silently produce wrong hashes for every password. Since the keyspace is only 26^4 (~456,976) combinations of exactly 4 lowercase letters, brute force it directly: nested loops (or one loop with base-26 encoding) over all 4-letter combos, computing the hash for each and comparing against a hash-set built from compromised_accounts.txt (map hash_hex -> list of usernames, since a hash can theoretically match multiple accounts). Parse compromised_accounts.txt as username:hash pairs, look up each recovered password by matching hash, then write audit_report.csv sorted alphabetically by username in the exact `username,hash_hex,recovered_password` format (no header line per the example given). Finally compute the checksum with `sha256sum audit_report.csv > audit_report.sha256` from within /home/user so the recorded filename matches for `sha256sum -c` verification, and self-check by running `sha256sum -c audit_report.sha256` before finishing.