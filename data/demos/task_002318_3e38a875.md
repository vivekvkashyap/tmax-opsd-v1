Extract one frame per second from `/app/credential_sync.mp4` with `ffmpeg -i credential_sync.mp4 -vf fps=1 frame_%d.png`, then decode each frame with `zbarimg -q frame_N.png` to get the raw QR text, Base64-decode it, and parse out the `PT:`, `CT:`, and `HASH:` fields with a regex or simple string split. For each payload, recompute `sha256sum` of the `CT` hex string exactly as it appears (no added newline, no re-casing) and compare to the given `HASH`; discard any payload whose hash doesn't match rather than assuming all frames are valid — some are deliberately corrupted. For the surviving valid payloads, recover the Master Key with a known-plaintext XOR attack: convert PT and CT hex strings to bytes and XOR them byte-for-byte (`key_byte = pt_byte ^ ct_byte`); do this for every valid payload and confirm they all agree on the same key before trusting it. Compute the rotated key as `sha256sum` of the derived key hex string *with* a trailing newline (per the spec). Then write a small bash loop using `nc -l` or `socat` that, on each connection, reads the request line and unconditionally responds with a well-formed HTTP/1.1 response (status line, `Content-Type`, blank line via `\r\n\r\n`, then the JSON body) containing `{"new_key": "<rotated_key>"}`, and leave it running bound to 127.0.0.1:9090.
