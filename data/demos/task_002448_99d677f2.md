Treat this as a regression-hunting task, not a debugging task. First run `test.sh` once to confirm it fails, then use `git log --oneline` to see the commit range and `git bisect start`, `git bisect bad HEAD`, `git bisect good <first-commit>` to let git binary-search for you — at each step run `test.sh` (or the compile+run steps it does) and mark `git bisect good`/`bad` based on whether the output is close to the expected sum of squares. Once bisect identifies the culprit, don't just trust the commit message — inspect the actual diff (`git show <commit>`) in both `encode.py` and `compute.c` to confirm the real cause: a serialization/deserialization mismatch between how Python packs floats with `struct.pack` and the byte width/type the C program expects with `fread`. Compute the correct file size yourself from the (unbroken) Python logic: number of records times the correct per-record byte width used by `struct.pack`, not the buggy width. Get the commit SHA with `git rev-parse` and write both lines to `/home/user/bad_commit.txt` without amending or checking out any commit destructively (`git bisect reset` when done).
