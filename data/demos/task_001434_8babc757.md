Since the binary's source is unavailable, treat this as a black-box discovery problem, not a coding problem you can shortcut. Run `/app/sensor_simulator` and capture a large sample (thousands of lines) to a file — piping through `head -n 5000` or a timeout is safer than trusting it to terminate on its own. Load the JSON lines into a dataframe and explore: plot or bucket v1, v2, v3 distributions, and specifically look for rows that appear to form a distinct cluster (a small minority of rows, since the prompt says spikes are rare) — check sums/combinations of pairs of fields (v1+v2, v1-v3, etc.) since simple synthetic anomaly rules are usually a threshold on a linear combination of one or two channels, not a single raw feature. Once you find a boundary that cleanly separates a small cluster from the rest (verify it holds across your whole sample, not just a subset), you can either hardcode that decision boundary or train a classifier (e.g. a shallow tree or logistic regression) on features engineered from your hypothesis — a tree will make the boundary interpretable and let you sanity-check it matches your manual analysis. Build the Flask/FastAPI service on 127.0.0.1:8080 with a `POST /predict` handler that checks the `Authorization` header for exact equality against `Bearer etl-pipeline-secret-2024` before touching the model, returning 401 immediately otherwise. Test both the classification correctness and the auth rejection path manually with curl before leaving the server running.
