Work this in strict order since each step unblocks the next. First get the project compiling: run `make` in `/home/user/audiostreamer`, read the compiler errors carefully — look for missing includes (a threading-related function used without its header) and simple typos in variable names between declaration and use sites in the server source; fix only what's broken, don't rewrite working code. Second, hunt the livelock: search the time-handling source file for a function that repeatedly calls `mktime`/`gmtime` in a `while` loop comparing timestamps — the classic bug is calling `mktime` on a `struct tm` without first setting `tm_isdst = -1`, causing DST ambiguity that makes the loop oscillate forever; write a tiny standalone reproducer that calls just that function with a boundary timestamp to confirm the fix before touching the server. Third, do git archaeology: use `git log --all -S "AUTH_TOKEN" --oneline` (or `git log --all --diff-filter=D -- '*.h.backup'` style searches) to find the commit that deleted a backup config file, then `git show <commit>^:<path>` or diff it to recover the hardcoded token. Fourth, transcribe `/app/incident_report.wav` with a local STT tool to recover the required timezone string for `SERVER_TZ`. Finally, export both env vars, start the server bound to `0.0.0.0:8080`, and background it (`&` plus `disown`, or `nohup ... &`) — then manually curl `/status` with the Authorization header yourself to confirm it responds within a couple seconds before finishing.
