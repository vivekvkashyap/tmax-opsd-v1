Start by reading the RK45 adaptive step-size logic in `/app/vendor/go-rk45/integrator.go`: find where it computes the local truncation error by comparing the 4th-order and 5th-order solution estimates for each state component. An adaptive RK45 error estimate must be the *difference* between the two order estimates (`y5[i] - y4[i]`), since that difference approximates the truncation error used to scale the next step size; if the code instead sums them (or otherwise doesn't subtract), the estimated error is wrong and the step-size controller will misbehave (steps grow unbounded or collapse), causing energy to drift. Locate that exact arithmetic operation and correct it to a subtraction so the error norm properly reflects the discrepancy between orders. After fixing, rebuild/rerun the simulator in `/home/user/simulator` to regenerate `/home/user/dataset.csv`, then independently check energy conservation yourself: parse the `t, x, y, vx, vy, E` columns and compute `max(|E_t - E_0|/|E_0|)` across all rows before declaring success, since the task requires this to fall under a tight threshold.
