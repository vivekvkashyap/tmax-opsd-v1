First get the build working: inspect the Makefile's `gcc` link line and the C source's use of `gz*` functions from `<zlib.h>` — the missing piece is the `-lz` link flag, so add it to the Makefile's compile command and rerun `make`. Run `./extractor /app/raw_data.bin /home/user/logs.txt` with its two positional args to decompress the archive. For the corruption filter, read the extracted file in **binary mode** (not text mode, which can silently mangle or error on non-UTF8 bytes) and split it into records delimited by `RECORD_START`/`RECORD_END` markers; for each record, scan every raw byte and flag it corrupted if any byte falls outside 32–126 (printable ASCII) and isn't tab(9)/newline(10)/carriage-return(13) — a single bad byte anywhere in the record disqualifies the whole record, so discard it entirely rather than trying to sanitize it. From surviving records, pull `ID` (from the `RECORD_START <ID>` line), `Value:`, and `Status:` with a simple line-based parse, and write them to the CSV with header exactly `ID,Value,Status`. Since grading is F1-based, precision matters as much as recall — don't over- or under-filter.
