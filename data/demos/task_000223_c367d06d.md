Recover the normalization spec from the image before writing logic: run OCR on `/app/mapping.png` (e.g. `tesseract /app/mapping.png stdout`), and read the extracted pipeline steps carefully and in exact order — since this will be fuzz-tested for bit-exact equivalence, the order and precise wording of each transformation step (what gets replaced, whether filtering happens before or after lowercasing, whether tokens are deduplicated/sorted, what the join separator is) all matter and must be implemented exactly as specified, not paraphrased. In `/home/user/process_log.py`, read one JSON line from `sys.stdin`, extract the `"message"` field, first decode the double-escaped unicode sequences (regex for `\\uXXXX` literal patterns, replacing with `chr(int(hex,16))` — note this is different from the JSON string already being decoded once by `json.loads`, so watch for the double-escaping specifically), then apply each remaining step from the OCR'd pipeline in the stated order, and `print(result, end="")` with no trailing newline and nothing else written to stdout. Before trusting your implementation, hand-test it against a few synthetic inputs containing `@`, `#`, mixed case, and escaped unicode to confirm each pipeline stage behaves as OCR'd.