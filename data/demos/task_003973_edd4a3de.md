Fix the parser's two real bugs precisely rather than rewriting from scratch. Split each line on the *first* `=` only (`line.split('=', 1)`), not `split('=')`, so values containing `=` are preserved intact. For interpolation, use `re.sub(r'\$\{([^}]+)\}', lambda m: env.get(m.group(1), ''), value)` so it replaces *every* `${VAR}` occurrence in the value and substitutes an empty string for missing keys instead of raising a `KeyError`. Strip key and value whitespace, and skip comment (`#`) and blank lines. Add `pytest` and `hypothesis` to `requirements.txt` and `pip install` them. For the property test, use `hypothesis.strategies` to generate a dict of alphanumeric key/value pairs, format it into `key=value\n`-per-line config text, parse it back with an empty `env` dict, and assert the round-trip equals the original dict — this is what actually exercises your fix, especially for values containing literal `=`. Then write `run_example.py` to call `parse_config` on `example.conf` with the given env dict and dump the result via `json.dump(..., indent=4)` to `output.json`, verifying by hand-tracing each line of `example.conf` through your fixed logic first.