Scaffold with `cargo new pipeline` inside `/home/user/etl`, add `rusqlite` (with the `bundled` feature to avoid system libsqlite3 issues) and `serde_json` to `Cargo.toml`. Open the connection to `/home/user/etl/supply_chain.db` and immediately execute `REINDEX;` (via `conn.execute_batch("REINDEX;")`) before running any SELECT queries — this rebuilds corrupted indexes so subsequent reads aren't stale, and must happen before you load nodes/edges into memory. Load all `nodes` (id, name, is_raw) and `edges` (parent_id, child_id, lead_time_days) into in-memory maps/adjacency lists. Find the node id for `"Product_Omega"`, then run a shortest-path search (Dijkstra or simple BFS/DFS with weighted relaxation, since this is a DAG a topological/priority-queue approach both work) from that node to every node with `is_raw = 1`, keeping the minimum total `lead_time_days` and its path. Reconstruct the path as a list of node names in traversal order and serialize it with `serde_json` as a plain string array to `/home/user/etl/shortest_path.json`. Watch for the common bug of comparing path costs only from direct children instead of accumulating full path cost recursively across multiple hops, and for treating raw materials as terminal even when a cheaper multi-hop route exists.