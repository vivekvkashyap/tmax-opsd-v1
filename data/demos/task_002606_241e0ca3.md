OCR the handwritten token first: run tesseract against `/app/project_auth.png` (try preprocessing — upscaling, thresholding — if raw OCR output looks noisy) and use the recovered 12-character alphanumeric string as the auth token your server checks against, rather than hardcoding a guess. Compile `custom_hash.c` into a shared library with `gcc -shared -fPIC -o libcustom_hash.so custom_hash.c`, then in Python load it with `ctypes.CDLL('./libcustom_hash.so')`, set `.argtypes`/`.restype` on `compute_hash` correctly (it takes a C string, returns an int) and pass Python strings via `.encode()`. Build the WebSocket server with the `websockets` library bound to `127.0.0.1:8765`; on each connection, require the first message to be the auth JSON, verify the token exactly, and close the connection (e.g. via close code) on mismatch before accepting further messages. For `organize` messages, sort `files` alphabetically, hash each with the ctypes wrapper, and respond with the exact `type: result` schema. Run the server as a background/daemon process so it stays listening for the verifier, and write `pytest` tests in `/home/user/tests/test_server.py` using the `websockets` client to exercise auth and organize flows end-to-end.