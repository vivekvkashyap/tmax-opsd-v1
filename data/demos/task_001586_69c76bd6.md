Work the two phases independently and test against the given corpora, not assumptions. Phase 1: run the existing `/app/extract_telemetry.py` on `/app/exfiltration_capture.mp4` to see exactly how it crashes or loses precision (likely candidates: using low-bit-depth image formats, float32 casts, or unguarded exception paths on corrupted/artifact frames); fix it to preserve full float64 precision end-to-end and to catch/skip or robustly parse corrupted frames instead of crashing, then confirm `/app/extracted_payload.json` round-trips exactly (diff bit patterns, not just "looks close"). Phase 2: before writing any detection logic, actually inspect several files in `/app/corpus/clean/` vs `/app/corpus/evil/` numerically (min/max magnitude, presence of denormals near float64 underflow, NaN/Infinity values, oscillating magnitude patterns, divide-by-zero-inducing near-zero values) to find the discriminating property empirically rather than guessing; then implement `/app/detector.py` to check those properties (e.g., detect NaN/Infinity, extreme denormal/epsilon values, or values whose ratio between adjacent elements swings wildly) and validate it achieves 100% classification on both corpora before finishing, iterating on the rule until every file passes.