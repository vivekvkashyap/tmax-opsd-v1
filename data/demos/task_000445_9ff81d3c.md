Start by diagnosing the broken integration, not by rewriting services blindly: run `/app/start_services.sh`, then `curl -v http://127.0.0.1:8000/dataset` to see the 500 error, and inspect `/app/redis/redis.conf` for the actual Redis port versus `/app/api/.env` (or wherever the API reads its Redis connection settings) for the port it's trying to connect to — a mismatch there is the classic cause of "connection refused" in a two-service compose. Fix the env file to match the configured Redis port, kill the stale API process, and restart via the startup script (or manually), then re-curl to confirm you now get a streaming FASTA response before moving on. For the Rust side, scaffold a project with `cargo new`, use `reqwest::blocking` (simpler than async here) to GET the dataset, and parse it by iterating lines: track state so a header line beginning with `>` starts a new record (parse the `tm=` value after the id) and subsequent lines accumulate the sequence body until the next header or EOF. Compute L as sequence length and GC as the count of 'G'/'C' characters per record, then solve the multiple linear regression T_m = b0 + b1*L + b2*GC via ordinary least squares on the design matrix [1, L, GC] — either implement normal-equations solving by hand with basic linear algebra, or pull in `nalgebra`/`linfa`. Since the target formula is linear and noise is small, a correct OLS fit should comfortably beat the MAE threshold; write the three coefficients to the exact JSON path and format specified.
