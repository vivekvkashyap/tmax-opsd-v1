Apply the provided patch precisely rather than reimplementing the parsing logic from scratch — use `patch -p0 < negative_fix.patch` (or apply the diff manually if patch context doesn't line up) inside `/home/user/vector_api/`, then confirm the fix by inspecting the diff: it changes the numerator scan format from `%u` to `%d` so negative numerators parse correctly instead of wrapping to huge unsigned values. Build with the given `Makefile` (`make`) to produce `libvector.so`. In your Python integration test, load the library with `ctypes.CDLL`, and critically set `dot_product.restype = ctypes.c_char_p` (and `argtypes` to two `c_char_p`) — this is the detail naive attempts skip, since without an explicit `restype` ctypes defaults to interpreting the return as a plain `int` and corrupts the pointer/string. Read `test_cases.json`, call `dot_product` for each case's `v1`/`v2`, decode the returned `c_char_p` (already reduced to lowest terms by the C code, so no extra Python-side fraction math needed) to a Python string, and write the `case_id -> result` mapping to `test_results.json`. Sanity-check one case by hand (multiply-and-sum the fractions yourself) to confirm the patched C logic is producing correct signed results before trusting the full run.