First get the vendored library building: `cd /app/libcsv-3.0.3`, run `make`, read the compiler error, and look for a simple typo in a type name (a missing underscore in a standard C type like `size_t`) in the library source, plus check the `Makefile`'s `CFLAGS` for a bogus/invalid flag that would abort the build — fix both, then `make` and `make install` (or otherwise produce `libcsv.so`/`.a` in a linkable location) so `gcc detector.c -lcsv -L/app/libcsv-3.0.3 -I/app/libcsv-3.0.3` succeeds. For `/home/user/detector.c`, use libcsv's callback-based parser to read each CSV row's `edge_pattern` field, split it on `;` for edges and `:` for the two endpoint nodes of each edge, then build a union-find (disjoint-set) structure over the node labels seen in that row: union the two nodes of each edge, and afterward count distinct roots among all nodes that appeared — more than one root means the query's pattern is disconnected. As soon as any row across the whole file is found disconnected, `exit(1)` immediately without needing to check the rest; if the file is fully parsed and every row was single-component, `exit(0)`. Test against the clean/evil corpora structure described in the task (two-column CSV with header) with manual runs before considering it complete.
