In `etl.c`, read `/home/user/raw.tsv` with `fgets` into a fixed 256-byte buffer, split on tabs (`strtok` or manual scanning) into `user_id, item_id, rating, latency_ms`, parse rating with `atoi`/`strtol` and latency with `atof`/`strtod`, and apply the filters (`10.0 <= latency <= 5000.0` and `rating` in {1..5}) before writing the untouched original line to `clean.tsv` — preserve original tab formatting exactly rather than reformatting the floats. While scanning, accumulate n, sum, and sum-of-squares separately for the `rating>=4` group and the `rating<4` group so you can compute sample mean and sample variance (divide by n-1) in a second pass or via Welford's method, then apply the Z formula and print with `%.3f` to `z_stat.txt`. For `pipeline.sh`, compile with `gcc -O2 etl.c -o run_etl -lm`, run `./run_etl` twice into `clean_run1.tsv`/`clean_run2.tsv` (renaming `clean.tsv` after each run so it isn't overwritten), diff them with `md5sum` and write `Reproducible`/`Failed` to `run_log.txt` accordingly — determinism should fall out naturally since there's no randomness in the ETL logic.
