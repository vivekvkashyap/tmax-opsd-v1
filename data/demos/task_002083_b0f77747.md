Work the four sub-problems in order and verify each with a quick manual check before moving on. First fix `decode_entry`: read the encoding byte at `offset`, the 2-byte little-endian length at `offset+1:offset+3`, then slice exactly that many raw bytes and call `.decode('utf-8')` or `.decode('utf-16le')` based on the byte value (1 vs 2) — don't hardcode utf-8. Return `(string, new_offset)` where `new_offset = offset + 3 + length`. Second, write `test_organizer.py` with hypothesis: use `@given(s=st.text(), enc=st.sampled_from([1,2]))`, but note UTF-16LE strings with surrogate codepoints or huge lengths can break the 2-byte length field, so either filter/assume small ASCII-safe inputs or catch the length-overflow case — check what `encode_entry` in utils.py actually does before assuming it's safe for all hypothesis-generated text. Third, replace naive string concatenation in `read_index` with list-append plus `''.join()` or generator yielding, and wrap `main()` with `tracemalloc.start()` / `tracemalloc.get_traced_memory()` to capture peak bytes, writing that integer to `/home/user/mem_peak.txt`. Finally, run the fixed script against `/home/user/project/index.bin`, sort the decoded paths, and write them one-per-line to `/home/user/organized_files.txt`. Run `pytest` to confirm your property test passes before finishing.
