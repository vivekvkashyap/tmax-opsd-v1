Treat this as recovery-then-fix-then-verify. For recovery, do not just copy the `.db` file — SQLite WAL recovery requires opening the original `.db` alongside its `.db-wal` (same directory, same base name) with a normal `sqlite3` connection so SQLite auto-checkpoints the WAL into the main file, then copy/vacuum that consistent result into `/home/user/recovered.db` (e.g. `sqlite3 indexer.db "VACUUM INTO '/home/user/recovered.db'"` after opening it once to trigger checkpoint, or use the `.dump`/backup API) — renaming files without triggering a checkpoint will lose the WAL's uncommitted-but-valid entries. Read `/app/logs/service.log`'s traceback carefully to identify the exact failing SQL statement/pattern (likely unparameterized string-formatted INSERT causing a quoting/integrity error on certain frame metadata); reproduce that exact failure mode in `/home/user/mre.py` against a fresh `/home/user/test.db` so it prints the same exception type/message. Fix the root cause in `server.py` (parameterize the query) and repoint its DB path constant to `/home/user/recovered.db`. Start the service bound to `127.0.0.1:8080` and manually curl `/health` and `/api/frames/count` yourself before finishing to confirm the recovered row count is accessible.
