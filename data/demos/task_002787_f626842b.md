Do the three sub-tasks in order since each later step depends on the earlier ones. First recover the deleted script from the trash location `/home/user/.local/share/Trash/files/sanitize_input.py` by copying it to `/home/user/sanitize_input.py`, then read it to learn the exact function name/signature it exposes (you'll need to call this, not reinvent sanitization). Second, fix the vendored package by reading `/app/complex_collatz-0.1/core.py` closely for the transition rule — trace through by hand what happens for inputs divisible by 7, since a branch that leaves `n` unchanged instead of applying the intended transformation is what causes the infinite loop; check nearby comments/other branches in the same function for what the correct multiplier/increment should be for that case, and apply the same modified-Collatz style consistently rather than reverting to the standard rule. Third, write `fast_collatz.py` to import both fixed modules, sanitize the raw CLI string into an int, run the corrected `evaluate_length`, and print only the integer. Since grading is bit-exact fuzz-equivalence against a hidden oracle across hundreds of malformed and valid inputs, test your CLI script yourself with edge cases (empty digits, embedded commas/letters, very large numbers) before considering it done.