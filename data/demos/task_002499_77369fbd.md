Start by running `tesseract` (install via apt if missing) against `/app/policy.png` to recover the target size threshold and the archive password — do not guess these, read them from the OCR output. Then build the pipeline in stages rather than one monolithic script: (1) write a recursive extraction loop that repeatedly scans for `.zip` and `.tar.gz` files and extracts them in place (unzip with `-P <password>` for encrypted ones, falling back to unencrypted extraction otherwise), looping until no more archives are found anywhere in the tree — nested archives (zip-in-tar-in-zip) require multiple passes, not a single pass. (2) Once only bare files remain, compute a content hash (e.g. `sha256sum`) for every file, group by hash, and keep only one file per unique hash, deleting the rest — do not deduplicate by filename, since duplicates have different names. (3) tar up the surviving unique files and compress with `xz -9e` (or similar aggressive settings) to `/home/user/optimized_pool.tar.xz`. (4) Finally remove everything else under `/home/user/storage_pool/` so only the final archive remains. Verify the resulting file size against the threshold you OCR'd before declaring done.