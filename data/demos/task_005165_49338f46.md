Build `/home/user/pipeline.sh` as a sequence of pure shell-utility stages, no Python. Use `awk -F'|'` twice: first pass computes the mean of column 3 (`{sum+=$3; n++} END{print sum/n}`), then a second `awk` pass filters rows where `$3` is strictly greater than that mean and reformats with `OFS=','` (or use `tr '|' ','` after filtering) to produce `/home/user/filtered.csv` in `timestamp,sensor_id,reading` order. Pass the computed mean into the second awk invocation as a `-v mean=...` variable rather than recomputing inline. For the database step, create the table with `sqlite3 /home/user/sensors.db "CREATE TABLE IF NOT EXISTS valid_readings (timestamp INTEGER, sensor_id TEXT, reading REAL);"` then bulk-load with sqlite3's dot-commands: `.mode csv` followed by `.import /home/user/filtered.csv valid_readings` (run via `sqlite3 db <<EOF ... EOF` or `-cmd`), being careful this doesn't re-import duplicate rows if the script is re-run (recreate/clear the table each run, or use IF NOT EXISTS plus a DELETE before import, so re-execution is idempotent). Make the script executable with `chmod +x`, run it once to populate the DB, and finally install the hourly cron job with `(crontab -l 2>/dev/null; echo "0 * * * * /home/user/pipeline.sh") | crontab -`, verifying with `crontab -l` afterward.