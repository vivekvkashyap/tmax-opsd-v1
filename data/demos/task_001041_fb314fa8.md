Work through this as three sequential investigations. First fix the build: inspect the `Makefile` for a wrong/single linker flag where it should instead link the separate ffmpeg libraries (avformat/avcodec/avutil, etc.), and check `video_processor.cpp` for a missing standard header causing a compile error, then rebuild until it links and runs. Second, load the core dump with `gdb ./video_processor /home/user/core`, run `bt` for the stack trace and `info locals`/`frame N` to inspect the crashing frame's variables, and cross-reference against `video_processor.cpp`'s source around that function — you're looking for a specific per-color-channel arithmetic condition (involving a division) that only triggers for a particular exact channel value combination in a frame, causing a fault. Once you've identified that exact condition from the debugger (don't guess it), write `detector.cpp` to use `ffprobe`/`ffmpeg` (via `popen` or library calls) to extract per-frame pixel/channel statistics and flag any file containing a frame matching that same triggering condition as malicious, printing REJECT/ACCEPT accordingly. Validate against `/home/user/samples/` first.