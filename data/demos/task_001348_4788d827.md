First fix the vendored package before touching the sanitizer. Inspect `setup.py` for an unreasonable `python_requires` bound and relax/remove it; then grep the package source for a typo'd import (a module name that looks like a real stdlib module but isn't, e.g. an extra letter) and correct it to the real stdlib module. Install the package with `pip install -e /app/vendored/cypher-validator-1.0` and confirm `python3 -c "from cypher_validator.core import extract_patterns; print(extract_patterns('...'))"` works before writing any sanitizer code. For the sanitizer, iterate every `*.cypher` file in the given directory, call `extract_patterns` per query, and for each returned bracket-content string parse the variable-length pattern (forms like empty, `:REL`, `*`, `*N`, `*N..`, `*..M`, `*N..M`). Treat unbounded forms (`*`, `*N..`, `*..`) and any resolved max depth > 5 as REJECT; everything else (including plain `:REL` or empty, single exact depth <= 5, or bounded range with max <=5) as ACCEPT. Print one `<filename>: ACCEPT|REJECT` line per file, then test against both accept and reject example queries from the spec before considering it done.