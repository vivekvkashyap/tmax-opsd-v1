Work through the fixes systematically rather than patching in place blindly. First run `python3 -m grpc_tools.protoc` (or equivalent) to generate `calc_pb2.py`/`calc_pb2_grpc.py` from `proto/calc.proto` inside `/home/user/project/`, and confirm the server imports them and starts before touching logic. For the rate limiter, replace the single shared counter with a per-`client_id` dict of timestamp deques (or similar), pruning entries older than 1 second on each check and comparing the remaining count to 5 — test it by simulating rapid calls for one client while another client's calls remain unaffected, and by waiting >1s to confirm the window resets. For deserialization, wrap the base64 decode and UTF-8 decode each in their own try/except mapped to `INVALID_ARGUMENT`. For evaluation safety, do NOT sanitize with a blocklist — validate with a strict allowlist regex (e.g. `^[0-9+\-*/().\s]{1,50}$`) checked before calling `eval`, and separately catch exceptions from `eval` itself (ZeroDivisionError, SyntaxError, etc.) and map them to `INVALID_ARGUMENT`. Start the server with `python3 server.py &` and manually test each failure path with a quick gRPC client before considering it done.