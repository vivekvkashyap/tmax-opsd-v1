Scaffold a real Cargo project (`cargo new graph_pipeline`) and add `csv`, `serde`/`serde_json`, and optionally `petgraph` to Cargo.toml rather than hand-parsing CSV with string splits. The core logic has two stages that must not be conflated: (1) deduplication — group edges by the `(source, target)` key and, for each group, keep only the row with the strictly maximum `timestamp` (ties aside, just pick the max), discarding all other rows for that pair entirely, even if an earlier row for the same pair had a positive weight; (2) filtering — after picking the latest row per pair, drop it from the graph if its weight is `< 0` (a "delete" marker), otherwise add the directed edge. Build the graph only from what survives both stages. For the reachability query, do a BFS/DFS from `N001` restricted to exactly hop-depth 1 and 2 (collect nodes visited at those two depths, not all nodes reachable within depth ≤2 confused with unlimited depth), dedupe, sort alphabetically, and emit `{"reachable_nodes": [...]}` via `serde_json` to guarantee valid JSON formatting. Compile and actually run the binary to produce output.json — don't hand-write the JSON.