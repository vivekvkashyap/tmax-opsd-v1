Build this pipeline stage by stage and test each piece in isolation before wiring them together. Transcribe `/app/call_recording.wav` (e.g. with `whisper`), then normalize the transcript yourself: lowercase, strip punctuation, split on whitespace, and write `index,word` rows to `/home/user/transcript.csv` starting at index 0 — verify the word count and content look sane before moving on. For the C++ server (`/home/user/analyzer.cpp`), keep it simple: poll `/home/user/stream.csv` for new lines (track how many you've already consumed), maintain a `std::deque` or circular buffer of the last 10 word lengths, recompute the average on each new word, and increment a persistent anomaly counter whenever that rolling average strictly exceeds 6.0 — the anomaly check must happen incrementally as words arrive, not just once at the end, since the rolling average crosses the threshold multiple times during ingestion. Implement `/metrics` on `127.0.0.1:8080` with raw POSIX sockets (`socket`/`bind`/`listen`/`accept`) parsing a minimal HTTP GET request and returning JSON with `rolling_avg` rounded to 2 decimals and the anomaly count. Write `/home/user/feed.sh` to append the next 10 unprocessed lines from `transcript.csv` to `stream.csv`, tracking an offset file so repeated runs are idempotent and cron-safe; install it via `crontab` with `* * * * *`, but also verify by calling `feed.sh` manually and curling `/metrics` before relying on cron timing.
