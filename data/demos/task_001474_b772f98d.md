Structure the Rust program around `nalgebra`'s built-in linear algebra so you don't hand-roll numerically fragile code. Load the CSV into a `DMatrix<f64>` (or fixed 100x3 / 100x1), split into X (first 3 cols) and y (4th col). Compute the mean vector by column-averaging, then center X and form the population covariance as `(Xc^T * Xc) / n` — use N, not N-1, per the spec. Use `nalgebra::Cholesky::new(cov)` to get L, and be careful about the whitening formula: `w_i = L^{-1}(x_i - mu)` means you need `L`'s inverse (or better, solve the triangular system directly rather than literally inverting L, for numerical stability) applied per-row, not a matrix-level shortcut that silently transposes the operation. For regression, form the normal equations `W^T W * beta = W^T y` and solve via `nalgebra::LU::new(WtW).solve(&Wty)` as required — don't use a different solver like QR even though nalgebra supports it, since the spec is explicit about LU. Serialize everything to the exact JSON key structure given, and actually run the compiled binary to produce results.json before considering it done.