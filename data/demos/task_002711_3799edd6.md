Use a Go SQLite driver to load all users into memory, and encoding/json to load the interactions array. Build weighted degree centrality by iterating every interaction once and adding its weight to both the src and dst user's running total (a user's score accumulates from both directions, not just one) — initialize every user's score at 0 first, including ones with no interactions, since they must still be considered before filtering. Filter out any user with centrality strictly less than the threshold, then sort the remaining slice with a custom less-function: primary key centrality descending, secondary key name ascending for ties — implement this as a two-part comparison, not two separate sorts. For pagination, compute the slice bounds for the requested page directly from page size and page number (e.g., start = (page-1)*size, end = start+size) rather than iterating page by page. Marshal exactly that slice to JSON matching the given field names and types (centrality as an integer). Run the program and inspect the output file's contents against your own hand-traced calculation of each user's score before concluding.
