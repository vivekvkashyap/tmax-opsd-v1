Build the pipeline as a single ffmpeg call piped into coreutils, matching the hinted filter chain exactly: use `ffmpeg -i "$1" -vf "fps=1,scale=1:1,format=gray" -f image2pipe -vcodec rawvideo -` to emit one raw grayscale byte per sampled second, redirecting stderr away from the pipe. Feed that raw byte stream into `od -An -v -tu1` to print unsigned decimal byte values, then squeeze/split whitespace into one value per line (`tr -s ' ' '\n'`) and filter blanks. Load all brightness values into an awk array indexed from 1..n so you can do the 3-point moving average with explicit zero-padding at the boundaries: for index i, use 0 in place of val[i-1] when i==1, and 0 in place of val[i+1] when i==n — do not skip or wrap these edge cases. Compute `(prev+curr+next)/3.0` as a float without rounding, then classify with strict boundary semantics: `< 85.0` is LOW, `<= 170.0` is MID (so exactly 170.0 is MID, not HIGH), otherwise HIGH. Print one token per line in order. Test against `/app/raw_data.mp4` before considering it done, since the grader will fuzz with freshly generated videos of varying duration.
