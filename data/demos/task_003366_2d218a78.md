First run `tesseract /app/config.png stdout` (or output to a file then read it) to OCR the card, then parse the resulting text yourself: find the `RATE_LIMIT=<number>` line with a regex/string split, and collect every line following `VALID_KEYS:` as the set of valid keys (trim whitespace, stop at blank line or EOF) — do not hardcode any rate limit or key values, since the image content varies and must be recovered by the agent, not assumed. In Go, write `/app/server.go` using only `net/http` and `encoding/json`: implement `POST /validate` that decodes `{"keys": [...]}`, checks each key for membership in the OCR'd valid-key set (a `map[string]bool` lookup is simplest and satisfies the "sort/diff efficiently" requirement), and returns 200 if all keys are valid or 400 if any is missing. Layer a simple global counter (guarded by a mutex, or an `int32` with `atomic.AddInt32`) that permits exactly `RATE_LIMIT` requests to `/validate` and returns 429 for every request after that, regardless of key validity — increment the counter before deciding accept/reject-for-validity so even a 400-worthy request still consumes a rate-limit slot. Build with `go build`, run the binary in the background bound to `127.0.0.1:8080`, and verify with a couple of manual curl calls that the count-then-429 behavior matches ordering (valid requests counted first, so a sequence of exactly RATE_LIMIT allowed calls then 429s).
