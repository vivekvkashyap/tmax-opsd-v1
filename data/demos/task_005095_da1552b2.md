Build this GitOps pipeline in layers and test each layer in isolation before wiring them together. First stand up the bare repo (`git init --bare /home/user/config.git`) and write the `post-receive` hook as a script that reads the pushed ref, checks out the new tree into `/home/user/active_configs/` via `git --work-tree=... --git-dir=... checkout -f`, tars the *previous* contents to `/home/user/backups/config-$(date +%s).tar.gz` beforehand, then `touch .reload` — test by pushing manually and confirming the tarball and extracted files appear. Second, write `supervisor.sh` as a loop that launches nginx and backend.sh, records their PIDs, and on each iteration checks `kill -0 $pid` to detect crashes (restart if dead) and checks for `.reload` (if present: remove it, kill both processes, relaunch). Third, get nginx.conf and backend.sh's socket path to match exactly (`/home/user/active_configs/backend.sock`) — a naive attempt often typos or uses different absolute vs relative paths here, breaking the proxy silently. Use `socat UNIX-LISTEN:...,fork` for the backend to shell out to `/opt/transcribe.sh /app/transmission.wav` on GET `/transcribe` and return a proper `HTTP/1.0 200 OK` response with headers and blank line before the body. Test end-to-end with `curl http://127.0.0.1:8080/transcribe` before considering it done.