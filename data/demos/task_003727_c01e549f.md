Fix `compute_total_heat` by implementing Kahan (compensated) summation: keep a running `sum` and a compensation term `c`; for each value compute `y = val - c`, `t = sum + y`, then `c = (t - sum) - y`, `sum = t` — this cancels the rounding error that causes naive float32 summation to plateau well below the true total when adding millions of small increments. For `compute_determinant_lu`, implement straightforward Doolittle LU decomposition without pivoting: for each pivot row k, eliminate below it (L[i][k] = A[i][k]/A[k][k], subtract L[i][k]*row k from row i), and the determinant is simply the product of the diagonal entries of U after elimination — no need for permutation sign tracking since pivoting isn't required here. Read the 4x4 values from `/home/user/sim/matrix.txt` with simple `ifstream >>` extraction. Keep grid/data types as `float` throughout; do not switch to `double`. After compiling with `g++ -O3`, run the binary and redirect/copy its exact stdout into `/home/user/sim/result.log`, matching the two-line `Total Heat: <value>` / `Determinant: <value>` format precisely, including the fixed decimal formatting already set up via `std::setprecision`.