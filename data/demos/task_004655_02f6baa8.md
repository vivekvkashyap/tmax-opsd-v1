Treat this as three independent repairs plus an integration test. First fix `frontend.sh`'s space-handling bug by quoting every expansion of the filename variable (`"$FILENAME"` in the `cat`/read/forward commands) so filenames with embedded spaces survive word-splitting. Second, recover the missing secret with git forensics: use `git log --all --diff-filter=D` to find where a secret header/define was deleted from the backend repo, `git show <commit>^:<path>` to recover its content, and reintroduce it into the source (fixing linker errors like missing `-lm` or a missing object file as needed) so `gcc`/`make` produces `/app/backend-repo/backend_processor`. Third, since the hash formula is flawed, treat `/app/oracle_processor` purely as a black-box spec: feed it varied stdin, observe its stdout, and adjust your C formula (e.g. reordering multiply/xor/shift and getting the secret constant right) until your binary's output is bit-for-bit identical across many test inputs — don't guess, diff outputs systematically. Finally wire up `start_services.sh` so frontend listens on 8000, backend on 8001, and confirm end-to-end with a socket client sending a filepath and checking the returned hash.
