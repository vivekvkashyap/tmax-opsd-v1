Work in stages and verify each independently before chaining them. First, use `find /home/user/configs -name '*.bak'` to enumerate targets. Write the C++ decoder as a standalone tool that: reads the whole file as raw bytes, splits all bytes except the last one into 2-byte (count, char) pairs, expands each pair by repeating `char` `count` times, sums the *uncompressed* byte values mod 256, and compares against the final trailing byte — on mismatch, skip writing output entirely (no partial/empty file). For the ISO-8859-1 to UTF-8 conversion, don't just copy bytes: iterate each decompressed byte as a Latin-1 code point and re-encode multi-byte UTF-8 sequences for values >= 0x80 (e.g. an ISO-8859-1 byte 0xA3 becomes UTF-8 bytes 0xC2 0xA3) — a naive byte copy will silently corrupt any non-ASCII characters. Write output next to the input with `.bak` replaced by `.cfg`. Once all `.cfg` files exist, use `grep`/`awk` across them to pull lines starting exactly with `TRACKED_CHANGE=`, format each as `basename: value`, sort by filename, and redirect to `/home/user/tracked_changes.log`. Test the checksum-invalid file specifically to confirm it's correctly excluded.