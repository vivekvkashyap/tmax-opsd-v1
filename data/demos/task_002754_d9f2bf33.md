Write `/home/user/organize.sh` as a sequence of clearly separated stages, each verifiable independently before moving to the next. Wrap the whole body in `flock -n /tmp/project_organizer.lock` (or a `flock` fd redirect at the top of the script) so concurrent runs are blocked. Iterate only over `*.dat` files in `/home/user/project_inbox/` (glob, not all files) — for each, read the first line and parse it against the literal pattern `ID:<name> VER:<num>` (e.g. with a regex or `awk`/`sed` field extraction), skipping/logging files that don't match rather than crashing. For valid files: copy to `/home/user/tmp/` first, then `mv` that temp copy into `/home/user/organized_data/<ProjectName>_v<VersionNumber>.dat` (mv is atomic within the same filesystem, which is the point of the temp-then-move pattern) and then remove the original from `project_inbox` only after the move succeeds. After processing all files, for each distinct project name find the max version number present in `organized_data/` (compare as integers, not strings — a version 10 must beat version 3) and re-create the symlink `/home/user/latest_data/<ProjectName>_latest.dat -> /home/user/organized_data/<ProjectName>_v<max>.dat`, removing any stale prior symlink first. Finally, `cd` into `organized_data` and run `sha256sum *.dat > /home/user/manifest.txt` so the manifest lists bare filenames matching the required format. Make the script executable and run it once against the inbox as the final step.