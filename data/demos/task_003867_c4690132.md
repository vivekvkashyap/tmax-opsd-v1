Work phase by phase, verifying each before moving on. Phase 1: `cd /home/user/workspace/c_src`, apply the patch with `patch -p0 < security_fix.patch` (check the patch's path prefix and adjust `-p` level if it fails to apply cleanly), then `make` to produce `libhtmlparser.so`. Phase 2: `setup.py`'s `Extension` needs `include_dirs=['../c_src']` (or the absolute path) so it finds `htmlparser.h`, `library_dirs=[...]` and `runtime_library_dirs=[...]` pointing at the same directory so the compiled `.so` both links and is found at runtime (without `runtime_library_dirs`/rpath the extension will build but fail to import) — then `pip install -e .` from `python_pkg`. Phase 3: read `payloads.js` carefully and hand-translate its literal list of `{id, payload}` dicts into a `get_payloads()` function in `payloads.py` returning the identical structure — this is direct transliteration, not reimplementation. Phase 4: run `test_runner.py` to produce `results.xml`, parse it with `xml.etree.ElementTree`, and for each `Result` element build a JSON object with `id` (int), `original`, `sanitized`, and `is_safe` computed by checking whether `<script>` (exact substring) is absent from the sanitized value — write the array to `/home/user/workspace/summary.json`.
