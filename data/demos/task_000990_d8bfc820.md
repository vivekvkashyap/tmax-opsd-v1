Work the three sub-problems in dependency order and verify each in isolation before wiring the services together. First, recover the corrupted SQLite DB using the built-in recovery path: `sqlite3 /home/user/app/data/cache.db ".recover" > dump.sql` then `sqlite3 /home/user/app/data/recovered_cache.db < dump.sql`, and sanity-check the recovered DB with a few `SELECT` queries before moving on. Second, open `engine.cpp` and look specifically for a float-typed accumulator used in an iterative/Taylor-style calculation loop with a termination condition like `while (term > <eps>)` — a term that can go negative will never satisfy a positive-only comparison and hangs forever; fix by comparing the absolute value, and switch `float` to `double` throughout the calculation to kill the precision drift. Recompile with `g++ -O3 -std=c++17 engine.cpp -o engine` and test the engine standalone on port 8001 before adding the gateway. Third, start the engine, then the Flask gateway on port 8000, and leave both running in the background (not foreground blocking) so the verifier's HTTP requests to `/price?id=...` can complete. Finally, sanity-test a few `/price?id=...` calls by hand and compare against manual high-precision arithmetic to catch remaining precision issues before relying on the automated MSE check.
