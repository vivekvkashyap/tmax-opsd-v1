Work through this as five separable diagnostic steps rather than one big fix. (1) Build failure: inspect `CMakeLists.txt` for a hardcoded `-Lvendor -lavcodec_stub`-style link line pointing at `vendor/`; replace it with `pkg-config`-based discovery (`find_package(PkgConfig)`, `pkg_check_modules(FFMPEG REQUIRED libavcodec libavformat libavutil)`) or plain `-lavcodec -lavformat -lavutil`, then re-run `cmake` + `make` in a `build/` dir until it links against the system FFmpeg. (2) Secret: the token isn't in the working tree — use `git log --all --oneline` and `git log -p -- config.ini` (or `git show <commit>:config.ini`) to find a prior commit that had `AUTH_TOKEN=...`, and copy that exact value into the current `config.ini`. (3) Crash: read `src/analyzer.cpp`'s frame loop carefully for an off-by-one (`<=` where it should be `<`) against `total_frames`, and add a check on `av_read_frame`'s return value / corrupted-packet flag so a bad frame near the end is skipped, not dereferenced. (4) Performance: profile or just read the pixel-brightness routine for an accidental O(N^2) buffer copy per frame; replace repeated full-buffer copies with a single pass. (5) Only after each piece verifies independently (compiles, token matches git history, video processes without crashing, and runs fast) build with `make`, launch the server in the background, and manually `curl`/`nc` both ports yourself to confirm the protocol responses before leaving it running.
