Start by reverse-engineering `/app/legacy_hasher`: since it's stripped, disassemble with `objdump -d` or `ghidra`/`radare2`, and run it on a handful of known short inputs (single chars, simple strings) to correlate observed 32-bit hex outputs with candidate bit operations — look for the shape of a rolling hash (XOR with input byte, rotate, add a constant) since that's the classic pattern for an 8-hex-digit unsalted hash; confirm your reconstructed algorithm reproduces the binary's output exactly on several test strings before trusting it. Because the hash is unsalted, the fast approach is to precompute the hash of every word in `words.txt` once into a `std::unordered_map<uint32_t, string>`, then do a single pass over `dump.txt` doing O(1) lookups per entry — this turns what would be an O(users × words) naive nested loop into O(words + users), which is required to hit the sub-0.5s target. Compile with `g++ -O3`, write `username:password` pairs to `cracked.txt`, then locate `admin`'s cracked password and use it as the `-N` passphrase to `ssh-keygen -t ed25519 -f /home/user/.ssh/id_ed25519 -N '<recovered-password>'`.