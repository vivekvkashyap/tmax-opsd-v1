Recover the token first since both the auth check and your manual testing depend on it: run `tesseract /app/auth_screenshot.png` and read off the token string precisely (watch for OCR ambiguity between similar characters like O/0 or l/1). In `handle_req.sh`, the latency-parsing bug is that raw fields like `120ms`, ` 120 ms`, and `120.0` aren't pure numbers — before summing, strip all non-digit/non-decimal-point characters from each value (e.g. `gsub(/[^0-9.]/, "", $2)` in awk) so `120ms` and `120` normalize identically, then average and round with something like `awk '{printf "%.0f", sum/count}'` rather than truncating. Add the auth check by reading headers in the existing header-parsing loop, capturing the `Authorization:` line, extracting the bearer value, and comparing it exactly to the recovered token — emit `HTTP/1.1 401 Unauthorized\r\n\r\n` and stop processing immediately if it doesn't match, before even reading the payload. Test the fixed script directly (feeding it a raw HTTP request via a pipe) before wiring it to the network, then launch with `socat TCP-LISTEN:9090,reuseaddr,fork EXEC:/home/user/handle_req.sh &` and confirm with `curl` that it's actually listening and backgrounded (not blocking your terminal) before finishing.
