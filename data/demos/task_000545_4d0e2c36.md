Open `/home/user/evidence/auth_wrapper.sh` and read it line by line looking for user input reaching a shell-interpreting sink — specifically an `eval` (or unquoted command substitution) that concatenates a user-supplied variable directly into a command string; note that exact line number for `VULN_LINE`. Then open `/home/user/evidence/ps_dump.log` and scan each logged invocation of the script for one whose username/argument field contains shell metacharacters (like `;`, `|`) rather than a clean username — that's the exploited line; the text between the injection point and the trailing token/semicolon boundary is the injected command (strip the legitimate username prefix and any separating semicolons around it). That same log line's final field is the base64 token argument the attacker passed — decode it with `base64 -d` to get the plaintext token. Write all three findings to `/home/user/forensics_report.txt` in exactly the specified `KEY=value` line format, double-checking there's no extra whitespace or trailing characters that would break an exact-match grep.