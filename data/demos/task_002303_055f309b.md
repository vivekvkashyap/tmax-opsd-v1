Inspect the schema and data in `/home/user/citations.db` first (e.g. `sqlite3 /home/user/citations.db .schema` and a few sample queries) so your joins match the real column names. In `/home/user/analyze.py`, compute the score per author by: (1) counting, for each paper the author wrote, how many distinct papers cite it directly (weight 1.0 each), and (2) counting, for each paper that cites one of those direct citers, how many such depth-2 paths exist (weight 0.5 each) — this is naturally expressed as two SQL joins (papers -> citations -> citations) or two passes over an adjacency structure in Python; be careful not to conflate "number of citing papers" with "number of citation edges" if duplicates could exist. Sum both weighted counts per author, then rank descending by score with ties broken by ascending alphabetical name (a dense rank, so tied scores share a rank number and the next rank isn't skipped). Take the top 3 and write them as a JSON array of `{"author_name", "score", "rank"}` objects to `/home/user/results.json`, then actually run the script and inspect the output file to confirm the format matches exactly.
