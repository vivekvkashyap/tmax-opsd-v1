Start by extracting the hardcoded token with static analysis: `strings /home/user/app/server | grep -i -E "auth|token|bearer"` (or use `go tool objdump`/`nm` if strings alone is too noisy) — Go compiles string literals directly into the binary's rodata, so it should appear in plain text. Confirm the service is live on port 9090 and probe `/upload` with `curl` to understand its actual response codes for missing/invalid auth before writing the exploit, so you're not guessing blind. For the path traversal, remember the server does `filepath.Join("/tmp/uploads", dest)` — Go's `filepath.Join` cleans `..` segments, so your `dest` value needs enough `../` components to walk from `/tmp/uploads` back up to `/` and then down into `home/user/incident_report.txt` (count directory levels carefully — verify by testing with `curl` first and checking for `/tmp/uploads/incident_report.txt` appearing wrongly before you get the traversal depth right). Then write the Go exploit program to send a `POST` to `http://127.0.0.1:9090/upload?dest=<escaped-path>` with header `Authorization: Bearer <extracted-token>` and body `VULNERABLE`, and run it with `go run exploit.go`.
