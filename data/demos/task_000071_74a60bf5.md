Build the orchestration script `/app/build_system.py` to shell out to `g++` for `/app/src/cpp/engine.cpp -> /app/bin/engine_cpp` and `go build -o /app/bin/engine_go` for the Go source, creating `/app/bin/` first and checking each subprocess's return code before exiting 0. For `is_safe_math` in `sanitizer.py`, don't try to blacklist dangerous substrings one-by-one (an adversarial corpus will find gaps) — instead use a strict allowlist regex over the entire expression, e.g. `^[0-9xyepi\.\+\-\*/\^\(\)\s]+$` (matching only digits, the permitted variable/constant names, whitespace, and the listed operators), and additionally reject expressions above a reasonable length cap; this structurally blocks `__import__`, `eval`, attribute access, and similar injection patterns without needing to enumerate them. Wire `app.py`'s `/evaluate` route to call `is_safe_math` first (403 on failure), then branch on whether the expression contains `x`/`y` (proxy to the C++ backend on 8082) vs `pi`/`e` (proxy to Go on 8081) vs neither (evaluate locally with a restricted evaluator, not bare `eval`). Write `nginx.conf` with a `location /api/math/` block that strips the prefix and proxies to `http://127.0.0.1:8000/evaluate`, listening on port 8080.
