Write the script so every random draw happens in a fixed, deterministic order: call `np.random.seed(42)` first, then compute `x = np.linspace(0, 10*np.pi, 1000)`, then draw the noise via `np.random.normal(0.0, 0.5, size=1000)` and form `y = 2.5*np.sin(x) + noise`. The order matters — generating `x` (which uses no randomness) before drawing noise keeps the RNG state aligned with what a grader recomputing the same sequence expects; drawing noise sample-by-sample in a loop or reordering steps will desync the stream and change every value. After computing `y`, calculate `np.mean(y)` and `np.var(y)` and branch on the strict bounds given in the task, writing exactly "SUCCESS" or "FAILED" (no newline surprises) to the log — only write the CSV/JSON in the success branch, and exit early on failure without writing training_data.csv. Save the JSON config with the exact key names and value types specified (ints for seed/num_samples, floats for noise params). Save the CSV with pandas using `df.to_csv(path, index=False)` so the header row is exactly `x,y` and there are 1000 data rows. Finally, actually execute the script (`python3 generate_data.py`) so all three output files exist on disk, not just write the source.
