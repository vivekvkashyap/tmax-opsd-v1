Build this as a pipeline of small, individually-testable `awk`/shell stages rather than one monolithic script, printing intermediate output after each stage to sanity-check it. Stage 1 (reshape): read `/home/user/daily_loc_stats.csv`, and for each data row emit three lines — one per language column (EN, FR, ES) — as `Date,Language,WordCount`, using `awk -F,` with `NR>1` to skip the header. Stage 2 (aggregate): group the long-format rows by `(Date,Language)` and sum `WordCount` across projects — an associative array keyed on `Date SUBSEP Language` in `awk` handles this cleanly; make sure output is sorted by Date first (chronological string sort works since dates are ISO `YYYY-MM-DD`). Stage 3 (rolling average): for each Language independently, walk its dates in chronological order and, for each date, average that day's total plus the prior two available days' totals (window size 1, 2, or 3 depending on how many prior days exist), flooring the result — this requires processing each language's time series separately (e.g. `sort` by Language then Date, track a small sliding array of up to 3 recent sums per language). Stage 4: write the header plus all rows to `/home/user/rolling_loc_stats.csv`, finally sorted by Date then Language as required, and diff your output against the spec's expected values to catch off-by-one window or floor-vs-round errors.
