Start by diagnosing the build break in /app/xxHash-0.8.2 rather than guessing: inspect the Makefile for a broken `CC` assignment and either restore it to `gcc` or build with `make CC=gcc libxxhash.a` — confirm libxxhash.a and the headers actually exist before writing any C. In organizer.c, implement deduplication with `mmap` for reading file contents and `XXH64` (from the compiled static lib, linked via `-L/app/xxHash-0.8.2 -lxxhash -I/app/xxHash-0.8.2`) to hash each file; group by (size, hash) match, keep one canonical file per group, and replace the rest with `link()` (hard link), not copies. Parse rules.csv line by line (`CategoryName,MinSizeBytes,MagicBytesHex`), decode the hex string to raw bytes, and for each unique (post-dedup) file check size >= MinSizeBytes and that its leading bytes match, then `mkdir -p` (mode 0755) the category dir and `symlink()` into it. Compile and actually run the program — don't just assert it "would work" — then spot check with `stat -c %i` on a few files to confirm hardlinks share an inode, and `ls -l` in categories/ to confirm symlinks resolve.
