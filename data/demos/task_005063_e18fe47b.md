Fix the leakage in `train.py` by wrapping PCA and the classifier in a single `sklearn.pipeline.Pipeline`, and only calling `.fit` on the pipeline with the training split after `train_test_split` — never call `PCA.fit_transform` on the full dataset first. Compute the 95% CI for test accuracy using the normal approximation: `acc ± 1.96*sqrt(acc*(1-acc)/n_test)`. Persist the fitted pipeline with `joblib.dump` or `pickle` to `/home/user/model.pkl`. For `detector.py`, load the pipeline, run `predict_proba` on every row of the input CSV, average the probability of class 1 across all frames, and print exactly `EVIL` if that average exceeds 0.5, else exactly `CLEAN` (no extra whitespace/newline variants). Run `extract.py` on `/app/test_video.mp4` to produce `video_features.csv`, then run `detector.py` on it and redirect stdout to `/home/user/video_result.txt`. Since grading uses a hidden adversarial corpus of clean/evil feature CSVs, sanity-check your detector generalizes (not just memorizes) by holding out part of `train_data.csv` and confirming both classes separate cleanly after the leakage fix.
