Bring up each dependency layer in isolation before wiring the detector. First fix the vendored package at `/app/vendored/py-graph-ext-1.2/`: inspect `setup.py` for anything that aborts installation (e.g. an environment-variable gate) and set that variable when running `pip install -e`; if you then hit a `SyntaxError` in a module like `centrality.py`, fix the malformed function definition directly (e.g. a missing colon). Confirm by instantiating `py_graph_ext.Graph` and calling the betweenness method on a small known graph. Second, for each CSV, load rows into an in-memory SQLite table (`source_id`, `target_id`, `amount`, `timestamp`), index `source_id`/`timestamp`, and write a window-function query giving a rolling 3600-second trailing sum of `amount` per `source_id` ordered by `timestamp` — test it on a hand-built CSV with known expected sums. Third, feed the same CSV into `py_graph_ext.Graph` and compute betweenness centrality. Only once each piece is independently verified, combine them in `/home/user/detector.py` per the exact fraud rule (centrality > 0.45 AND velocity > 10000), validate against `/app/corpus/clean_samples/` and `/app/corpus/evil_samples/`, then create `/home/user/DONE`.
