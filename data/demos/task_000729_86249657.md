Connect to the SQLite DB with `sqlite3.connect`, and select edges with a parameterized query — `SELECT source_job, target_job FROM job_dependencies WHERE is_active = ?` passing `(1,)` — never string-format the value in, and never skip the filter (the whole point is that stale/inactive rows must not leak into the graph). Build a `networkx.DiGraph`, add edges via `add_edges_from`, and call `nx.pagerank(G)` with fully default parameters — do not pass custom alpha, tolerance, or personalization, since the verifier recomputes with defaults too. Convert the resulting dict into a list of `{"job": ..., "score": round(v, 4)}` objects. Sort by score descending, breaking ties alphabetically by job name — implement this as a single sort key `(-score, job)` rather than two separate sorts. Wrap the list under a single top-level key `"pagerank_scores"` and write with `json.dump`. Run the script yourself afterward and inspect the JSON to confirm key names, rounding, and sort order match exactly before considering it done.