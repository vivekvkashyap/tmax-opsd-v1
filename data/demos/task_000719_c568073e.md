Do the two independent computations first, then wire up the server. Transcribe `/app/voicemail.wav` with a speech-to-text tool (install `whisper`/`whisper.cpp` or similar if unavailable) to learn which vulnerability class and mitigation the message specifies — read the transcript carefully rather than assuming a generic security header, since the exact header value must match what's said. Separately compute the file's hash with `sha256sum /app/voicemail.wav` (or Python's `hashlib.sha256` reading the file in binary mode) and capture just the hex digest, no filename or extra whitespace. Then implement a minimal HTTP server (Python's `http.server` with a custom handler, or `socat`/raw socket loop) bound to exactly `127.0.0.1:8080` that, on any GET to `/`, replies `200 OK` with a `Content-Security-Policy` header set from what you learned in the transcript, and a body containing exactly the hex digest you computed (test the header name case-insensitively and the body with an exact string match, e.g. via `curl -i`). Run the server as a persistent background process so it keeps listening after your setup script finishes, and confirm with a local `curl` request before finishing.
