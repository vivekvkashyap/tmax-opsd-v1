Do not guess the algorithm — reverse-engineer it methodically by probing the binary with controlled inputs before writing any Go code. Feed it simple diagnostic sequences one at a time via stdin/pipe and study the output stream: (1) a run of identical constant values to see how many leading lines print a trivial/zero-like output before real values start (this reveals the size of its rolling window); (2) a perfectly linear ramp (0,1,2,3,4,5,...) to see if the output settles to a constant once the window fills, which would indicate it's fitting a straight line (e.g. a regression slope) rather than something nonlinear; (3) sequences with a single outlier to see how much a single new point moves the output, which helps confirm whether it's a simple average, a difference, or a least-squares slope over a fixed window. Cross-check hypotheses by computing candidate formulas by hand for a small window against the binary's actual output before committing. Once your hypothesis reproduces the binary exactly on several handmade sequences, implement it in Go matching the same warm-up behavior and printf-style precision, then fuzz-compare against the original.