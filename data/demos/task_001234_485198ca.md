This task has two independent parts — solve them separately and verify each before moving on.

For the transcription: inspect `/app/reference.wav` (check duration/sample rate with `ffprobe`), install a speech-to-text tool (e.g. `openai-whisper` via pip, or `ffmpeg` plus a local ASR model) and run it on the file. Post-process the raw transcript by lowercasing and stripping all punctuation before writing to `/home/user/audio_secret.txt`, and sanity-check the result reads as coherent English before finalizing — don't guess the text.

For `audio_sanitizer`: write a minimal tar-header parser in C++ that reads each 512-byte header block, extracts the `name` field (and `prefix` field if using ustar), and the `typeflag` byte, rejecting on `../`, `..\\`, leading `/`, or typeflag `'2'` — remember to correctly skip over each file's data blocks (rounded up to 512-byte boundaries) to reach the next header, or you'll misparse the archive. For `optimize`, implement the zero-run RLE exactly as specified (max 255 per run-length byte, split longer runs), then group files by their compressed byte content (e.g. hash the compressed payload) and use `link()`/`std::filesystem::create_hard_link` for duplicates instead of rewriting them.