Stream the log with a plain `for line in open(path):` loop (never `readlines()` or loading the whole file) and use a compiled regex to match the timestamp/server/`max_connections`/`timeout` fields, skipping any line that doesn't match. Normalize timeout by regex-splitting the numeric part from its unit suffix and multiplying by 60 when the suffix is `m`, leaving `s` as-is — always store as `int`. Build the daily state via an explicit day-by-day walk from 2023-12-31 through 2024-01-31 for each of the three target servers: maintain a "current state" dict seeded with the baseline (100/30), and for each day check whether any log entries exist for that server on that date — if so, take the *last* (latest-timestamp) one and update current state; if not, simply carry the previous day's state forward (forward-fill). Compute anomalies while doing this walk by comparing each day's `max_connections` to the prior day's and flagging when the increase exceeds 50% of the prior value. Serialize the three servers' `daily_state` (31 entries each) and `anomalies` lists into the exact nested JSON schema shown in the task and write it to `/home/user/config_report.json`.
