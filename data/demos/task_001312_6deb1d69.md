Split this into pipeline wiring and Rust bug-fixing, and validate each independently. For the pipeline, inspect `/app/pipeline/start.sh` and the three services' config files under `/app/pipeline/config/`; align the ports end-to-end so `pcap-streamer`'s target port matches `math-analyzer`'s listen port, and `math-analyzer`'s downstream/sink port matches `alert-sink`'s listen port — use `netstat`/`ss` or `strace -f -e trace=network` on `start.sh` to see where a connection is refused, rather than guessing at config keys. For the Rust bugs in `/app/math-analyzer/src/`: fix the deadlock by finding the two `Mutex`es locked in inconsistent order across threads and enforcing a single consistent lock-acquisition order (or scoping locks so only one is held at a time); fix the infinite recursion by ensuring the graph-traversal function adds each node to its "visited" set *before* recursing into neighbors, not after; fix precision loss by changing the accumulating variable(s) in the compounding/interest-style loop from `f32` to `f64` (keep the surrounding formulas unchanged). Rebuild with `cargo build --bin filter_cli`, then iteratively test `filter_cli <file>` against every file in `/app/corpora/clean/` and `/app/corpora/evil/`, checking stdout is exactly `ACCEPT`/`REJECT` with matching exit code, and keep fixing until both corpora are 100% correct before wiring up the live pipeline.