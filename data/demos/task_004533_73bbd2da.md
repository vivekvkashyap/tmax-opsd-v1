This task has two independent deliverables — solve them separately and verify each in isolation. For the connectivity fix, inspect `/home/user/app/config.json` and `start.sh` to see how the frontend reads the API port/host, correct the misconfigured port so it points at 8080 (the API backend's actual port), restart the stack via `start.sh`, and confirm with `curl http://127.0.0.1:8000/status` that it returns the exact expected JSON. For the packet parser, write `/home/user/packet_parser.py` reading one hex line from stdin: first check the hex string length is at least 14 hex chars (7 bytes) or emit `INVALID_HEADER`; parse byte 0 as version, bytes 1-2 as big-endian total length, bytes 3-6 as big-endian timestamp (use `int(hexstr, 16)` on the right slices, or `bytes.fromhex` + `int.from_bytes(..., 'big')`); then verify the declared length equals the actual number of bytes represented by the full hex string (total bytes = len(hex)//2) — mismatch means `LENGTH_MISMATCH`; otherwise decode the remaining payload bytes to ASCII, replacing any byte outside 32–126 with `.`, and print in the exact `V:<v> LEN:<l> TS:<t> DATA:<payload>` format. Test manually against the two worked examples in the task before trusting it, since a fuzzer will exercise many edge cases (odd-length hex, exact boundary lengths, non-printable bytes across the full 0x00-0xFF range). Make the script executable with the correct shebang.