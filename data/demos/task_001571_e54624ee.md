Compile the Go side first and verify it in isolation before writing any Python. Build with `go build -buildmode=c-shared -o /home/user/libmatheval.so /home/user/math_eval.go` (this Go file already has the `//export` CGO directive, so no source changes should be needed) — this also produces a `.h` header you can peek at to confirm the exact exported symbol name and signature. In the Python script, load the library with `ctypes.CDLL('/home/user/libmatheval.so')`, then explicitly set `lib.EvaluateRPN.argtypes = [ctypes.c_char_p]` and `lib.EvaluateRPN.restype = ctypes.c_double` before calling it — skipping this step is the most common way this integration silently returns garbage, since ctypes defaults to assuming `int` return type. Encode the RPN string to bytes (e.g. `expr.encode('utf-8')`) when passing it in, since `c_char_p` expects a bytes object, not a Python str. Call the function, convert the returned double to a Python float, then `str()` it and write to `/home/user/math_output.txt`. Run the script and check the output file's contents directly rather than assuming the call succeeded.