Reverse-engineer `/app/detector.bin` empirically before writing server code: run it with varied `history1 history2 history3 current` combinations, observe where NORMAL flips to ANOMALY, and binary-search the boundary to pin down the exact threshold formula — don't guess and move on without confirming against multiple probe points. Then implement a C TCP server on 127.0.0.1:7777 using `socket`/`bind`/`listen`/`accept`, parsing incoming `timestamp,value\n` lines incrementally (buffer partial reads since TCP isn't line-aligned). Track a running sum/count for the current 10s bucket (`ts_start = (timestamp/10)*10`); when a timestamp lands in a new bucket, finalize the previous one immediately: compute its average, apply your reverse-engineered rule using the last 3 finalized averages as history (first 3 buckets of a session are always NORMAL), write the `BUCKET ...` response to the client, and append a matching line to `/home/user/pipeline.log`. Also finalize any pending bucket on client disconnect (EOF). Compile, leave the server running in the background, and test with a raw TCP client to confirm bucket boundaries and response format match exactly.
