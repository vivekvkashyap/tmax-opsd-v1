Before writing any wrapper code, characterize the bug empirically: feed `/app/bin/stat_analyzer` varied byte streams via stdin (clean floats, then streams injected with control/escape bytes) while watching with `strace -f` and `timeout` to see which byte sequence causes it to hang/spin instead of returning output — narrow this down by bisecting which injected bytes trigger the hang versus which are harmless, rather than guessing. Once you've identified the exact trigger sequence, build the HTTP service using only `http.server`/`socketserver` (no Flask/FastAPI) listening on 127.0.0.1:9000, checking the `X-Auth-Token` header against the required value and returning 401 on mismatch. For `POST /process`, sanitize the payload by stripping/replacing the exact trigger byte sequence you discovered, run the binary via `subprocess.run` with the sanitized bytes as stdin and a timeout so a still-bad payload can't hang the server (treat a timeout or unrecoverable pattern as HTTP 400), and return the binary's stdout otherwise. Finally write `regression_test.sh` using `curl -s -o - -w '%{http_code}'`-style calls against clean and corrupted payloads, capturing results into `regression_results.log`, and start the service in the background so it stays running for the verifier.