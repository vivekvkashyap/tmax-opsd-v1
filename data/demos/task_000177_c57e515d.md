Start by reverse-engineering the dropper: decompile /app/dropper.pyc with a tool like `uncompyle6`/`decompyle3` or `dis` on its marshalled code object to recover the payload encoding logic (look for how it builds a JSON body with a base64 payload and a hash-based checksum, and what byte-level obfuscation, e.g. XOR, it applies before hashing). Confirm your understanding by round-tripping a sample payload by hand. Build the WAF as a small HTTP server bound to 127.0.0.1:9000 that: base64-decodes the payload, reverses the XOR transform, recomputes the checksum over the decoded bytes and compares it to the request's checksum field, then scans the decoded text for the exploit signatures you identified (e.g. dangerous eval/import-style calls or backend-command strings) — reject with 403 on any mismatch or signature hit, else 200. Wire Nginx's `auth_request` directive at :8080 to call your WAF's endpoint before proxying to Flask on :5000, remembering that auth_request needs the request body forwarded/buffered to let the WAF inspect POST payloads. Lock down direct access with iptables DROP rules on ports 5000 and 6379 for all interfaces except lo. Validate end-to-end against the clean/evil corpora and log your manual test results.