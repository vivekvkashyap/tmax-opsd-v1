Build the pipeline stage by stage and check each output before proceeding. In `build_orchestrator.py`, read `ops.dat` in fixed 4-byte chunks with `struct.unpack('<BBh', chunk)` to get `op_id`, `op_type` (0=add, 1=subtract), and the signed little-endian `const_val`; generate `encoded_ops.rs` with a `match op_id { N => input + C, ... _ => input }` arm per record (subtracting for op_type 1), keeping the function signature exactly `pub fn apply_op(op_id: u8, input: i32) -> i32`. After writing the file, invoke `cargo build` as a subprocess from within `/home/user/rust_calc`, checking its return code rather than assuming success. For the test suite, since the compiled CLI fires an HTTP GET to `localhost:8080/log` on every invocation, spin up a lightweight mock server (`http.server`/`socketserver` in a background thread, or a `pytest` fixture using `threading`) bound to 8080 before invoking the binary via `subprocess.run`, so the real call doesn't hang or error. Use `hypothesis.strategies.integers(min_value=-10000, max_value=10000)` to drive each op_id's property test, invoking the built binary and asserting its printed output equals your independently computed add/subtract result — this cross-checks the generated Rust against your own Python arithmetic rather than trusting either blindly.
