Work in two clear phases. First, fix the vendored crate: `cd /app/vendored/dataset_graph && cargo build` and read the compiler error — a broken `Cargo.toml` manifest typically has an unquoted value (e.g. `edition = 2021` needs to be `edition = "2021"`); TOML requires strings in quotes. Fix only that syntax, then confirm `cargo build` succeeds and check `src/lib.rs` for the actual public API (`Graph::from_json`, `shortest_path`) before using it. Second, scaffold the binary with `cargo new /home/user/dataset_filter`, add the vendored crate as a path dependency in `Cargo.toml` (`dataset_graph = { path = "/app/vendored/dataset_graph" }`), then implement `main.rs`: read the two file args, parse both JSONs (serde_json is fine), validate required fields exist, parse and check the timestamp year, build the graph via the library, compute shortest path between source and target, and exit 1 if any check fails (missing fields, bad year, no path, or path length > 3) else exit 0. Test against `/app/corpus/clean/` and `/app/corpus/evil/` before finishing, iterating over every file in both directories and confirming the exit code matches expectations for each.
