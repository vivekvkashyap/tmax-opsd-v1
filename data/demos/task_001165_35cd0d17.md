Implement schema enforcement and modeling as separate, verifiable stages, and be strict about the "strictly compliant" wording. Load `raw_data.csv` with pandas keeping values as strings/object dtype initially (don't let pandas auto-coerce), then for each row attempt `float()`/`int()` parsing per-column with try/except (or `pd.to_numeric(..., errors='coerce')` then check for NaN) — a row is dropped if any field fails to parse, is null, `f2` is not strictly greater than `schema.json`'s `min_value`, or `cat` (parsed as int) is not in `allowed_values`. Read the bounds/allowed values from `schema.json` itself rather than hardcoding them, since the task says to enforce "its schema." Build the sklearn `Pipeline` using the step names and order given in `pipeline_spec.json` (map the JSON step name strings to actual sklearn classes) and pull the hyperparameter grid from `pipeline_spec.json`'s `param_grid` key rather than inventing your own values. For reproducibility testing, loop over `[10, 20, 30]`, construct `KFold(n_splits=3, shuffle=True, random_state=seed)` fresh each iteration, fit a fresh `GridSearchCV(pipeline, param_grid, cv=kf)` on the cleaned X/y, and pull `best_params_` and `best_score_` (rounded to 4 decimals) directly from the fitted object rather than recomputing manually. Write the JSON in the exact nested structure specified.