Break this into independent stages before wiring the server. (1) Video: extract all raw frames from `/app/server_cam.mp4` with `ffmpeg` (it's 10fps), compute each frame's average grayscale brightness, and count per-second "flashes" as frames within that second exceeding brightness 180 — bucket by second using all 10 raw frames, not a downsampled 1fps sample. (2) Logs: read `/home/user/sparse_logs.csv`, build a strict per-second timeline spanning the video duration from the first timestamp, and forward-fill `error_rate` for missing seconds. (3) Merge flash counts and filled error rates, compute `anomaly_score = flash_count*0.5 + error_rate` per second, then a trailing rolling average over the current and up to 4 prior seconds (fewer near the start). (4) Use `text/template` to render the incident report with the actual computed max rolling score and its second offset — don't hardcode. (5) Serve a Go HTTP server on `0.0.0.0:9090` with `/stats` (JSON array) and `/report` (plain text), running continuously in the background so the verifier can query it live.