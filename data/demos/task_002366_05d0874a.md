Break this into independently testable pieces. First, frame extraction: use `ffmpeg -i /app/thermal_experiment.mp4 -vf "select=eq(n\,20)" -vframes 1 frame20.png` to grab the exact 20th frame, then compute the grayscale average pixel intensity yourself (e.g. Python with PIL: convert to `L` mode and average all pixel values, rounding/truncating to an integer as `T_init`) — verify your frame index convention matches "20th frame, 0-indexed" precisely, since an off-by-one here breaks the whole downstream simulation. Second, implement the Thomas algorithm (tridiagonal LU) in C for the implicit 1D heat equation with the given dx/dt/alpha and the specified initial condition (T_init in indices 40-59, zero elsewhere, zero boundaries) — this is the correctness-critical numerical core, so unit-test it against a known steady-state or symmetric-decay sanity check before wiring the HTTP layer. Third, build the HTTP server with raw POSIX sockets (or libmicrohttpd if available) binding `127.0.0.1:8000`, implementing `/health` and `/simulate?steps=N` (parse the query param, run N steps, format each of the 100 outputs to 2 decimals into a JSON array). Finally, profile a 10,000-step run (gprof/perf or manual timing wrapped around your solver function) to identify which C function dominates runtime, and write both derived values to `/home/user/profiling_report.txt`. Launch the compiled server in the background so it stays listening.
