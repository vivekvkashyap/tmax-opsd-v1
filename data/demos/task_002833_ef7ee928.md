Implement Welford's algorithm as a single pass that tracks running mean and M2 (sum of squared deviations), updating both incrementally per element rather than computing mean first and then a second pass over raw values — this is what gives numerical stability for large offsets, so test it mentally against something like values near 1e9 to confirm no precision loss. Sample variance divides the final M2 by (n-1); guard the n<2 case with an explicit `raise ValueError` before that division. For the property tests, use `hypothesis.strategies.lists` of `floats(min_value=-1000.0, max_value=1000.0, allow_nan=False, allow_infinity=False, min_size=2)` and write two `@given`-decorated tests: one asserting the result is >= -1e-9, another asserting variance is unchanged (via `math.isclose` with the specified tolerances) after adding a random shift constant to every element. For the CI script, create the venv, `source venv/bin/activate`, `pip install pytest hypothesis`, run `pytest test_welford.py`, and branch on `$?` to write PIPELINE_SUCCESS or PIPELINE_FAILURE to the log — remember to `chmod +x` the script and actually execute it so the log file is produced before finishing.
