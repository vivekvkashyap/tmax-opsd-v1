Break this into an offline transcription/analysis step and a serving step, and get the numbers right before wiring up the API. Transcribe `/app/data/telemetry_audio.wav` with a local STT tool (`SpeechRecognition` + a local recognizer, or `whisper`), then parse the transcript text for the interleaved "Sensor Alpha <number>" / "Sensor Beta <number>" pattern — watch for numbers spoken as words ("twenty two point five") which STT may render as digits or words depending on the engine, so normalize/parse defensively (a word-to-number fallback or regex on digit output) rather than assuming a clean numeric transcript. Once you have the two float arrays, compute Welch's t-test via `scipy.stats.ttest_ind(alpha, beta, equal_var=False)` for the p-value. For the Bayesian step, implement the standard normal-normal conjugate update analytically: posterior variance = 1/(1/prior_var + n/likelihood_var), posterior mean = posterior_var * (prior_mean/prior_var + n*sample_mean/likelihood_var), and build the 95% CI as posterior_mean ± 1.96*sqrt(posterior_var) (or use `scipy.stats.norm.ppf`). Round every float to 3 decimals only at JSON-serialization time, not earlier, to avoid compounding rounding errors. Serve via Flask/FastAPI bound to 127.0.0.1:8080 with a single GET /metrics handler, started in the background so it stays up for verification.
