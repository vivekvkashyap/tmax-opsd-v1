First extract the two spec values from the image: run `tesseract /app/backup_specs.png stdout` (or via pytesseract), print the raw OCR text, then parse the Magic Header bytes and Checksum Salt out of it with a regex — verify by eye that what you parsed matches what's printed before hardcoding it into your parser. Implement `/home/user/parse_dump.py` as a byte-scanner using `mmap` over the input file: scan for occurrences of the 2-byte magic header, and at each candidate offset attempt to read the 4-byte little-endian payload length, then that many payload bytes, then the 4-byte little-endian checksum — bounds-check every read against the mmap length so a truncated/false-positive match near EOF doesn't throw, and on any failure (out-of-bounds, non-utf8 payload, JSON parse error, or checksum mismatch) just advance the scan by one byte past this candidate and keep looking for the next magic-header occurrence, rather than skipping over the whole payload length. Compute the checksum exactly as specified: `zlib.crc32(payload_bytes + salt_bytes) & 0xFFFFFFFF`, where salt_bytes is the salt value's ASCII/UTF-8 representation. Emit only verified records, in file order, as CSV with header `file_id,path,symlink_target` to stdout.
