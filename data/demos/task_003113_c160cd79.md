Debug this by tracing data through the pipeline stage by stage rather than rewriting blindly — three distinct, independent bugs need fixing in `/home/user/scanner.sh`.

1. Inspect the raw input with `xxd` or `od -c` to confirm it has embedded `\x00` bytes; strip them before using each line as an ID, e.g. pipe through `tr -d '\0'` (via `cat file | tr -d '\0' | while read -r ID; do ...`), not by reading the file directly.
2. Run the equivalent `sqlite3` query manually on `/home/user/intel.db` and inspect its raw output format — sqlite3's default column separator is `|`, not `,`, so parsing with `cut -d','` silently fails; fix the delimiter used to split `base_score` and `multiplier` (or use `sqlite3 -separator` explicitly).
3. For the score calculation, `bc` defaults to `scale=0` (integer truncation); set `scale=2` inside the `bc` expression (e.g. `echo "scale=2; $BASE * $MULT / 7" | bc`) so the division retains two decimal places instead of truncating early. Do not touch `intel.db`.
