Treat this as reverse-engineering an unknown deterministic function purely from black-box input/output samples — do not try to disassemble or guess the binary's internal formula analytically; instead, generate a large, well-distributed synthetic dataset by calling `/app/legacy_extractor x y z` (as a subprocess) across many random (x,y,z) triples in [0,5]^3, capturing stdout as the label. Store X (2000x3) and y (2000,) into `/home/user/training_data.h5` via h5py. Train a nonlinear regressor (RandomForest or GradientBoosting) since the underlying function is likely nonlinear/non-monotonic — a plain linear model will fail the 5% error bar; use `GridSearchCV` over at least one hyperparameter (e.g. `max_depth` or `n_estimators`) with cross-validation to avoid overfitting, then pickle the best estimator to `/home/user/surrogate.pkl`. For serving, write a minimal Flask or FastAPI app bound to exactly `127.0.0.1:8000` with a `POST /predict` route that parses the JSON body's `x`, `y`, `z` fields, feeds them through the loaded model as a single-row array, and returns `{"prediction": <float>}` — watch for JSON serialization of numpy floats (cast to native `float()` before returning). Leave the server process running (foreground or backgrounded with `&`/nohup) so the verifier's live HTTP requests succeed.