Read each buggy module before editing, and fix them independently so each can be unit-checked. For `semver_utils.py`, replace naive string comparison with a proper parser: split each version on `-` to separate the numeric core from an optional pre-release tag, split the numeric core on `.` into integer major/minor/patch and compare those tuples numerically (not lexically, so `1.10.0` correctly beats `1.2.0`); only if the numeric cores are equal do you compare pre-release presence, where a version WITH a pre-release tag is considered lower precedence than the same version with none. For `test_semver.py`, use `hypothesis.strategies` to generate random valid semver-like strings (composed from small integer ranges and optional tags) and assert `compare_versions(a, b) == -compare_versions(b, a)` as a property test — install `hypothesis` via pip first. For `sym_emulator.py`, find the `WEAK_RESOLVED` branch in the state machine and add the missing transition: when a `STRONG` symbol type is seen while state is `WEAK_RESOLVED`, upgrade the symbol's state to `STRONG_RESOLVED` (mirroring the `UNRESOLVED` branch's STRONG handling). After all three fixes, run `python build.py`, then run the produced `app` with `LD_LIBRARY_PATH=.` redirecting output to `success.log`.