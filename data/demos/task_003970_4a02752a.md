Before writing any parsing logic, extract the ground-truth rules from the video: use `ffmpeg -i /app/release_demo.mp4 frames/%03d.png` (or similar) to pull frames, then inspect them (visually or via OCR, e.g. tesseract) to read off the exact ordered state sequence and the maximum-retries number shown on screen — do not guess these. Once you have them, implement `/home/user/release_parser.py` as a strict state machine: track a `current_index` into the discovered sequence and a `retries` counter, starting "before" the first state. On each `STATE: <NAME>` line, only advance if it matches the next expected state in order — any other observed state (including a repeat or out-of-order name) must immediately print `LINKING_ERROR` and exit 1. On `VALIDATION_FAILED`, increment retries and check "strictly exceeds" (i.e. `> max`, not `>=`) before failing with `RATE_LIMIT_EXCEEDED`. Reaching the final state in the sequence should immediately print `SUCCESS` and exit 0 rather than continuing to read stdin. Ignore unrecognized lines silently. Since it's fuzz-tested for exact behavioral equivalence, write a handful of hand-crafted input sequences (both valid and edge-case) and manually trace expected output before trusting it.