Split this into service-wiring and sanitizer-logic, validating each independently. For nginx.conf, add a `server` block on 8080 with `location /api/ { proxy_pass http://127.0.0.1:5000; ... }`; for app.py, read `REDIS_HOST`/`REDIS_PORT` via `os.environ.get(...)` instead of hardcoding, then curl `127.0.0.1:8080/api/...` to confirm proxying works. For `process_translations`, read both CSVs with `|` delimiter, `pandas.merge` on `string_id` against the base CSV, and fill missing/empty translations from the base English column. Build row-filtering as boolean masks combined with `&`/`|` rather than exceptions mid-loop: flag embedded newlines, forbidden `<script`/`<iframe` tags (case-insensitive), and brace mismatches by counting `{` vs `}` per string (not just presence) — drop any row where a mask is true. Because malformed CSVs can break `pd.read_csv` on unquoted newlines outright, read leniently (e.g. `on_bad_lines='skip'` or manual line parsing) so one bad row doesn't crash the load. For `write_stratified_sample`, group by `context_tag` and `.sample(n=min(2, len(group)))` per group before exporting to JSON. Validate against the clean/evil fixtures under `/home/user/corpus/` to confirm evil inputs are rejected and clean inputs pass through.