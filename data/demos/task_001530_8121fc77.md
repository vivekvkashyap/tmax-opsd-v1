Run `make` first and read the linker/compiler error carefully — a multi-threaded C program using `pthread_create`/`pthread_mutex_*` needs `-pthread` (or `-lpthread`) added to the Makefile's compile/link flags; add it and confirm `make` succeeds cleanly. To find the crashing line in `data.txt` without brute-force guessing every line, apply delta debugging: bisect the file (e.g., run `./calc` against halves, then quarters, of the 1000 lines) or scan for operations whose arguments could cause undefined behavior in the given operation set (think about which arithmetic operator has an input value that is unsafe when a specific operand equals zero) — confirm the exact single offending line by isolating it into a minimal one-line `data.txt` and reproducing the crash. Save that exact line verbatim to `crash_line.txt`. Fix `calc.c` so the worker function detects that unsafe condition before performing the operation and treats the result as 0 (skip it) instead of crashing — don't change any other operation's semantics. Recompile, rerun on the full `data.txt`, and capture the `Total: <number>` line into `final_result.txt`, verifying the sum is computed correctly by re-deriving it independently (e.g., cross-check with a quick script) rather than trusting the first run blindly.
