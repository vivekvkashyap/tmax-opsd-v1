Treat this as three independently testable pieces. First, run `/usr/local/bin/whisper-cli` against `/app/emergency_alert.wav` and save its transcription verbatim to `/home/user/transcribed_alert.txt`; the last word of that transcription is the storage error code you'll need later — extract it programmatically (e.g. `awk '{print $NF}'`), don't hardcode it, since your parsing script must reproduce it exactly for the fuzz comparison. Second, since your `parse_metrics.sh` is checked bit-for-bit against a hidden reference on thousands of fuzzed inputs, build the stdin pipeline literally as described — `grep` for lines containing "CRITICAL", use `awk` to pull field 3 (the PID), then append a dash plus the transcribed code to each — and test it yourself against varied synthetic log lines (including edge cases like extra whitespace or multiple CRITICAL lines) to catch any off-by-one field or formatting mismatch before trusting it. Third, for the wrapper script, explicitly set `PATH=/usr/bin:/bin:$PATH` at the top before calling `du -sh` and piping `/app/sample_logs.txt` into your parser, and redirect only the final combined output to `/home/user/alert_output.log`.
