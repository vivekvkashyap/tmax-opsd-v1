Sequence the script so each stage's output feeds cleanly into the next, and do the rename before anything else touches the files. For renaming, iterate the files (`for f in "$dir"/*` with quoting, since names contain spaces), split each name into base and extension at the last dot, lowercase only the extension (`${ext,,}` in bash or `tr`), replace spaces in the whole resulting name with underscores, and `mv` only if the new name differs — don't accidentally rename subdirectories or non-file entries. After renaming, generate the manifest with `sha256sum /home/user/raw_data/* > /home/user/manifest.sha256` (run from a location where the paths recorded match what verification expects — relative vs absolute paths matter, check the task's implied format). Archive with `tar -czf /home/user/archive.tar.gz -C /home/user raw_data` so the tarball contains the `raw_data/` directory itself, not just its loose contents. Finally compute `sha256sum /home/user/archive.tar.gz` on the *already-created* archive (order matters — hash after archiving, not before) and count files with `find /home/user/raw_data -type f | wc -l`, writing both into `backup_log.txt` in the exact two-line format specified.
