Use `git bisect` methodically rather than manually scanning commits: define a reliable "good"/"bad" test first — e.g. a script that starts the service at a given commit, POSTs `/app/test_audio.wav` to the analyze endpoint, and checks whether the response is well-formed and numerically sane (crashes, NaN-like frequencies, or garbled transcripts count as bad) — then run `git bisect start`, mark current `HEAD` bad and a commit from ~200 back good, and let bisect narrow it down automatically, recording the culprit hash. Once found, look at that commit's diff directly (`git show <hash>`) to see the actual three bugs bundled together, and fix each on `main`: restore a default fallback for `WHISPER_MODEL_PATH` in the config so the model file is found; find the FFT peak-frequency computation and make sure it stays in 64-bit float throughout (don't cast to `np.float32` before computing the peak bin), since that cast is what's destroying precision; and fix the audio-chunking slice so the last chunk isn't dropped or duplicated (check the boundary/off-by-one in the chunk-size arithmetic, e.g. using `ceil` division and including the final partial chunk exactly once). Write the culprit's commit hash to the report file, then start the fixed service bound to `127.0.0.1:9090` and manually curl the endpoint with the Bearer token and test audio to confirm the JSON response looks right before leaving it running in the background.
