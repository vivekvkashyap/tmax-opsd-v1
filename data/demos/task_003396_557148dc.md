Read the raw log file in binary mode and decode each line independently with a try/except: attempt `.decode('utf-8')`, and only on UnicodeDecodeError fall back to `.decode('iso-8859-1')` — do not pick one encoding for the whole file. Parse each decoded line with a regex/split on the `[TIMESTAMP] | JobID:<id> | <message>` structure, then normalize the message: lowercase, strip all non-alphanumeric/non-space characters (regex substitution), then strip whitespace. For burst detection, group entries by (job_id, normalized_message) and use a proper rolling-window approach: sort each group's timestamps, then for every timestamp count how many other timestamps in the same group fall within the following 300 seconds (a sliding window, not fixed non-overlapping bins) — a job/message pair bursts if any window contains >=4 occurrences; track the max count seen. Only job_ids with at least one bursting message qualify. Sort the resulting job list by max_burst_size descending, then job_id ascending, and render through the given Jinja2 template rather than hand-building the markdown, to keep formatting consistent with report_template.md.
