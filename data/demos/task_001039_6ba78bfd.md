Start by transcribing `/app/voicemail.wav` with a speech-to-text tool (e.g. `whisper` CLI, or a Python speech-recognition library) and read the transcript carefully — it names the exact chunk identifier and the exact integer-parsing bug that causes the leak, which you need before writing any detection logic. Do not guess the chunk name or bug mechanism; derive both from the transcript. Once you know the chunk ID and the sign/width of the integer field involved, write `detector.py` to parse each WAV file manually and safely: read the RIFF header, then loop over subchunks reading an 8-byte header (4-byte ID + 4-byte size), and specifically decode that one chunk's size field using the same signed/unsigned interpretation the buggy parser used (per the transcript) to check whether it would go negative — if so, classify as adversarial. For all other chunks, skip forward using the size interpreted as a normal unsigned length so you don't misparse legitimate chunks or hang/crash on real files. Output must be a dict of `{filename: "ACCEPT"/"REJECT"}` written as JSON to the given output path. Test against the provided `/home/user/samples/` before trusting the logic on the hidden corpus.