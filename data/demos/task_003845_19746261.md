Work in three clean passes using coreutils only. Phase 1: build a lookup of active node ids (e.g. `awk -F, '$2=="active"{print $1}' nodes.csv`), then use awk with two input files to join edges against that active set on BOTH source and target (skip the header, drop rows where cost<=0), and pipe through `sort -t, -k1,1 -k2,2` to get the required ordering — write to projected_edges.csv with no header. Phase 2: implement BFS (not DFS) in `find_path.sh` since you need minimum hops — load projected_edges.csv into an awk adjacency array grouped by source, then do a level-by-level BFS from START, tracking a predecessor per node; a naive attempt often does DFS or Dijkstra by cost, which is wrong since you must ignore cost. To satisfy "lexicographically first among equal-length paths," visit each node's outgoing edges in sorted order (rely on your sorted input, or sort neighbor lists inside awk) so the first BFS path found to any node is the correct tie-break. Phase 3: reconstruct the path from predecessors, walk START to END, and emit `step_index,node_id` starting at 0. Test end-to-end with the sample data before trusting the script, and make find_path.sh executable (chmod +x).