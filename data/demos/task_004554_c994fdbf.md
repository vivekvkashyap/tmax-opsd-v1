Start by extracting the two required values from the image with OCR — run `tesseract /app/network_spec.png stdout` and read off the port number and the auth token exactly as printed (watch for OCR ambiguity like 0/O or 1/l and sanity-check by re-running with different `--psm` settings if the text looks garbled); use those recovered values verbatim in your code, don't hardcode guesses. Write `server.c` as a minimal single-threaded (or loop-accept) HTTP/1.1 server using raw sockets: parse the request line and headers yourself, look for a case-correct `Authorization: Bearer <token>` header on `GET /status`, and respond with a literal `HTTP/1.1 200 OK` plus `OK\n` body on match, or `HTTP/1.1 401 Unauthorized` otherwise — make sure it loops to `accept()` again after each connection so it survives multiple sequential requests. Build the `Makefile`'s `deploy` target to `mkdir -p /home/user/bin` then `gcc` the source into that exact binary path. For the watchdog, use `pgrep -f metric_server` (or check `pidof`) to detect a running instance and start it detached (`nohup ... &`) only if absent; install it into cron via `crontab -l | { cat; echo "* * * * * /home/user/bin/watchdog.sh"; } | crontab -`. Finish by running `make deploy`, running the watchdog once, and confirming with `curl` that both the authorized and unauthorized cases behave as expected.
