First isolate the regression before touching the reconnection logic. Use `git log`/`git diff`/`git bisect` (or manual inspection if the repo history is small) inside `/app/repo` on `filter.sh` to find where the boundary check for the metric was changed from inclusive (`>= 0` and `<= 100`) to exclusive (`> 0` and `< 100`) — this is what silently drops legitimate `Metric=0` and `Metric=100` lines. Fix the comparison operators to be inclusive again, and while you're in there, harden the whole validator: the timestamp format, `[LEVEL]` must be exactly `INFO`/`WARN`/`ERROR` (reject anything else, including lowercase or missing brackets), and the metric must be a clean integer in [0,100] (reject non-numeric, out-of-range, or malformed lines silently — i.e. produce no output for a bad line, don't error out). Test the fixed filter directly against known-good and known-bad sample lines via stdin before copying it to `/home/user/final_filter.sh`. Then wire up the pipeline: read continuously from `localhost:8001` (e.g. `nc localhost 8001` or bash's `/dev/tcp`), pipe through `/home/user/final_filter.sh`, and forward stdout to `localhost:8002` via another `nc`, running the whole chain as a persistent background process so it keeps forwarding new lines as they arrive.