Scaffold with `cargo new retrieval_tool` in `/home/user/`, and add a CSV-reading dependency (`cargo add csv serde serde_json` or just parse lines manually with `split(',')` since the format is simple and fixed). Parse `/home/user/embeddings.csv`, skip the header, and parse each row's `id` as integer and `v1..v5` as `f64` — use `f64` throughout, not `f32`, to match the required precision. Implement cosine similarity as a small function: dot product of the two 5-vectors divided by the product of their L2 norms; compute this for the fixed draft vector `[0.12, 0.88, 0.34, 0.71, 0.55]` against every row. Collect (id, similarity) pairs, then sort by similarity descending, breaking ties by ascending id (implement the tie-break explicitly with a comparator — Rust's default `partial_cmp` on floats won't tie-break for you), and take the top 3. Round each similarity to exactly 6 decimal places at output time (use formatted string construction or manual rounding rather than trusting default float `Display`, since `serde_json` doesn't fix decimal places by default) and serialize as a JSON array of `{id, similarity}` objects to `/home/user/top_matches.json`. Run the binary and re-check the file's contents match the expected structure before finishing.
