Implement the three stages as separate, clearly ordered passes over the data so bugs in one don't silently corrupt the next. When unmarshalling each JSONL line, use a strict Go struct/decoder approach (e.g. `json.Unmarshal` into a struct with the exact field types, or decode into `interface{}` first and type-assert) so that wrong types (string where float expected), missing fields, or a `features` array not exactly length 3 all fail the same validity check and get silently skipped — don't just check for JSON parse errors alone, also check field presence/type/array length. Count valid records as you go. For each valid record compute the L2 norm with `math.Sqrt(x*x+y*y+z*z)` and keep only records with norm `>= 10.0` (careful with the boundary — it's inclusive), counting selected records. For p95, sort the selected `latency_ms` values ascending with `sort.Float64s`, then use the exact index formula given (`int(math.Ceil(0.95*float64(N)))-1`) rather than a library percentile function, since different percentile conventions will disagree at small N. Marshal the three fields to `/home/user/summary.json` with `encoding/json`, then `go run` it to confirm the file is produced.