Tackle the three parts in order and validate each independently. (1) OCR `/app/reference_gel.png` with `tesseract` (or a Python OCR lib) to get raw text, then extract just the sequence after the `TARGET_SEQ:` prefix with a regex/string split, stripping whitespace, and write it to `/app/target_sequence.txt`; sanity-check the extracted text looks like a plausible DNA string (only A/C/G/T) before moving on — mis-OCR'd characters here will cascade into every later step. (2) Run `jupyter nbconvert --to notebook --execute --inplace /app/regression.ipynb` (installing `jupyter`/`nbconvert`/`ipykernel` first if missing) and check its exit code; the notebook asserts your extracted sequence matches its internal expectation, so a failure here is a signal your OCR extraction was wrong, not a notebook bug — go re-check the extraction. (3) For the Bash HTTP server, use `socat TCP-LISTEN:8080,fork,reuseaddr SYSTEM:'...'` or a `nc -l` loop that parses the request line for `GET /optimize?min_len=<N>`, reads `/app/candidates.csv`, filters rows where `length(Sequence) >= min_len`, computes a per-candidate score by comparing the candidate to the target sequence position-by-position (matching characters up to the shorter length) using `awk`, picks the max score (first occurrence wins ties), and emits a properly formatted `HTTP/1.1 200 OK` response with correct `Content-Length` computed from the actual body bytes. Run it in the background and test with `curl` before finishing.