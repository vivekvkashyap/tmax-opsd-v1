Work through this as four independent, separately-verifiable sub-tasks. First, run `tesseract /app/server_instructions.png stdout` (or similar OCR) and read the extracted text carefully to pull out the exact timezone string and the internal backend port — write the timezone verbatim (no extra whitespace) into `/home/user/timezone.txt`. Second, start a reverse proxy from local port 8080 to the extracted backend port using `socat TCP-LISTEN:8080,fork,reuseaddr TCP:127.0.0.1:<port> &` (or nginx/socat equivalent), confirmed running in the background. Third, for `/home/user/bio_filter.sh`, implement the two independent rejection rules precisely as specified — reject on any of the literal metacharacters `| ; > < $` or a backtick, OR on case-insensitive "admin" anywhere in the text — then test it against every file in `/app/clean_bios/` (must exit 0) and `/app/evil_bios/` (must exit 1) before finishing, since 100% accuracy on both corpora is required; watch for false positives like "administrator" text or benign punctuation. Fourth, edit `/home/user/sync_users.sh` to add `export PATH=$PATH:/usr/bin` near the top and change the log redirect to the absolute path `/home/user/sync.log`.