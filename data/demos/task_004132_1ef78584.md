First recover the hidden config from the audio: use `ffmpeg`/`whisper` (install via pip if needed) to transcribe `/app/pipeline_specs.wav`, then carefully parse the spoken numbers into the two weights per stream (Alpha/Beta/Gamma) and the sampling modulus — re-listen or re-run transcription if a number seems ambiguous rather than guessing. Design the pipeline as: three `awk` (or similar) transforms run in the background with `&`, each computing `val1*w1 + val2*w2` per row and writing a sorted-by-id temp file, then `wait` for all three before joining. Use `join -t, -1 1 -2 1` twice (chained) on the sorted intermediate files to do the inner join on `id`, then sum the three per-stream scores into `Total_Score`, filter by `id % modulus == 0`, format the score to exactly 2 decimals with `printf "%.2f"`, and sort by `id` descending numerically (`sort -t, -k1,1nr`). Watch out for: floating point formatting precision, correct field separators through the join, and ensuring the parallel jobs' outputs are fully flushed/sorted before the join runs.