Handle the two objectives independently. For the vendored router, `cat` router.sh to find the hardcoded config path assignment (it forces a system path the sandbox can't create); change that single line so it respects a pre-set `ROUTER_CONFIG_PATH` env var while still defaulting sensibly, e.g. `ROUTER_CONFIG_PATH="${ROUTER_CONFIG_PATH:-/etc/bash-router/config}"`, then verify with `mkdir -p /tmp/rconf && ROUTER_CONFIG_PATH=/tmp/rconf .../router.sh --health-check; echo $?` returns 0. For the validator, build it as layered checks in order, not one big regex: (1) `jq empty` to reject malformed JSON outright; (2) check api_version is present, is a JSON number (`jq 'type'`), and integer >1 — reject string-typed "2"; (3) request_type is exactly "mutation" or "query"; (4) payload object exists; (5) branch on request_type and validate the payload sub-fields' presence, type, and bounds precisely as specified (numeric range checks with jq's numeric comparisons, not string comparisons; string prefix/length checks with jq's `startswith`/`length`). Iterate against both corpora in /home/user/corpora/{clean,evil}/ using a loop that asserts each clean file exits 0 and each evil file exits 1, refining until 100% pass — evil cases specifically probe type confusion (numbers-as-strings) and out-of-range/extra-field tricks.
