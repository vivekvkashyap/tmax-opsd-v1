Structure the script in the exact order the task specifies: call `np.seterr(invalid='raise', divide='raise')` before any numpy math so silent NaN/Inf propagation becomes a hard failure instead. Load the CSV with pandas/numpy, verify it has 4 columns, drop rows with any NaN (`dropna()`), then cast the remaining data to `float64` — do the NaN drop *before* slicing X/y so the row with a NaN feature is fully excluded, not just masked. Split into `X = data[:, :3]`, `y = data[:, 3]`. Compute `np.linalg.cond(X)` (default 2-norm, no extra args) for the condition number. Solve the least-squares fit with `np.linalg.lstsq(X, y, rcond=None)` to get weights `w`, predict `X @ w`, and compute MSE as `np.mean((y - preds) ** 2)`. Create `/home/user/output` with `os.makedirs(..., exist_ok=True)` before writing, and dump `{"condition_number": ..., "mse": ...}` as JSON — don't manually round to a fixed string, just use the float values so the verifier's epsilon comparison passes.