First recover the exact formula from the image: run `tesseract /app/math_spec.png stdout` and read the spec carefully — note precisely which statistic is used (population vs sample standard deviation, i.e. ddof=0 vs ddof=1), the exact denominator smoothing constant, and whether the window is expanding-from-start or something else; getting ddof wrong is the classic naive mistake here since many people default to sample std. Implement the scaler in `scaler.py`: read the full JSON array from stdin once, then for each index i compute the mean and population standard deviation of all elements from index 0 through i (inclusive), apply the transformation exactly as specified in the image, and round each output value with Python's `round(val, 4)` before serializing. Since this will be fuzz-tested against a hidden reference implementation across many random arrays (including length-1 arrays and arrays with all-identical values, which stress the sigma+epsilon term), test your script manually with `echo '[...]' | python3 scaler.py` on edge cases like a single-element array and a constant array before considering it done. Print nothing but the final JSON array to stdout — route any debug prints to stderr or remove them.