Treat `/app/legacy_filter` purely as a black box to reverse-engineer via systematic probing, not disassembly: script a loop that calls it with many crafted strings and records output, varying one dimension at a time — string length (especially near short boundaries), character content, and presence/absence of specific suspicious substrings — to isolate what drives each of the four output categories (e.g. try empty/very short strings first to find a length cutoff, then fix length and vary content to see if particular keywords change the result, then look for numeric patterns like sums-of-character-codes modulo small numbers by testing strings with controlled character values). Cross-check your hypothesis against dozens of random strings, comparing your predicted output to the oracle's actual output, before writing code. Once your derived rule reproduces the oracle exactly across your own test corpus, implement `/home/user/filter.go` with identical logic, build it, then set up the bare repo at `/home/user/repo.git` with an executable `hooks/post-receive` script that on push checks out the pushed commit into a temp dir and runs `go build -o /home/user/filter`, then clone/commit/push from `/home/user/workspace` to trigger it. Finally fuzz your own binary against the oracle yourself before submitting.
