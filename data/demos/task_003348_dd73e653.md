Build this bottom-up: get the Bash pipeline correct and tested standalone before wiring the HTTP layer around it. In `pipeline.sh`, use `tail -n +2` to skip the CSV header, then loop rows with `IFS=, read -r id v1 v2 v3`, calling the provided scoring binary with `"$v1" "$v2" "$v3"` per row (path given in the task) and capturing the printed score (use `bc` or `awk` for the running sum and average since scores are floating point, not integer bash arithmetic). Count anomalies with a comparison against `5.0` done in `awk`/`bc` rather than bash's integer-only `[[ ]]`. Append the tracking line using `date -u +"%Y-%m-%dT%H:%M:%SZ"` for the timestamp, formatted exactly as `[<ts>] BATCH=<name> AVG=<avg> ANOMALIES=<count>`. Test `bash pipeline.sh test_batch` alone first and check `tracking.log` and stdout look right. Then write a small Python HTTP server on `127.0.0.1:8080` handling `GET /process?batch=<name>` that shells out to `subprocess.run(["bash","/home/user/pipeline.sh", batch], capture_output=True)`, parses the average/anomaly count from its output (or re-derive from the script's own stdout), rounds average to 2 decimals, and returns the JSON with status 200, run persistently in the background.
