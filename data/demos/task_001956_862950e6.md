Run OCR on /app/evidence.png (tesseract) to recover the leaked secret text embedded in the screenshot — read it out programmatically rather than assuming a value, and sanity-check the OCR output since misread characters (e.g. 0/O, 1/l) would break every signature check downstream. With the recovered secret, write a Go program that loads a JWT from the given file path and rejects it (exit 1) if either: (a) its header decodes to an `alg` of `none` (or the signature segment is empty/stripped) — the classic alg-none bypass — or (b) it verifies successfully under HS256 using the leaked secret, since any token an attacker could forge with a known leaked key is compromised regardless of payload content; also treat obviously malicious claim values (e.g. path-traversal strings in claims) as a red flag if signature checks alone aren't discriminating on the samples. Accept (exit 0) only tokens that are well-formed HS256 JWTs that do NOT verify under the leaked secret (i.e., signed by the real unknown key). Test your binary against every file in /app/clean_tokens/ and /app/evil_tokens/ before declaring done, since the grader also runs a hidden corpus testing the same underlying logic, not just these filenames.