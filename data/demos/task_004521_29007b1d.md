Start by inspecting the schema (`sqlite3 /home/user/graph.db .schema`) to confirm `nodes(id, name)` and `edges(source, target)` (directed friendship). The existing query is a Cartesian product over five unjoined tables — replace it with an explicit path query using proper joins: join `edges e1` where `e1.source = ?` (the starting user) to get direct friends, then join `edges e2` on `e2.source = e1.target` to reach friends-of-friends, then join `nodes n` on `n.id = e2.target` to get their names. Exclude the starting user themselves (`n.id != ?`) and exclude anyone who is already a direct friend — do this with a `NOT IN (SELECT target FROM edges WHERE source = ?)` subquery (or a `LEFT JOIN ... IS NULL` anti-join) rather than trying to filter in application code. Use `SELECT DISTINCT` to dedupe, since multiple 2-hop paths can reach the same person. Keep the script's CLI signature (`db_path`, `user_id`) unchanged, run it as `python3 get_2nd_degree.py /home/user/graph.db 1 > /home/user/result.txt`, and confirm the names come out already alphabetically sorted (the existing `sorted()` call should still work once the query is correct).
