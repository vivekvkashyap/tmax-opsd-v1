Work through the three bugs in order, verifying each fix before moving to the next. First, run `pip install /app/data_aggregator-1.0.0` and read the actual failure — it's a dependency version conflict in `setup.py`'s `install_requires` (a package pin incompatible with another required package); relax or bump the offending pin so pip can resolve a compatible set, then confirm `pip install .` succeeds cleanly. Second, open `data_aggregator/server.py` and trace the lock usage around the `/process` handler: a deadlock under concurrency where a single thread reacquires a lock it already holds means it's a plain `threading.Lock()` being acquired twice on the same call path (directly or via a helper function called while already holding the lock) — the fix is either to switch it to `threading.RLock()` or remove the redundant inner acquisition, not to add more locking. Third, add `assert isinstance(payload, dict)` as the very first statement inside `process_payload`. Finally, install, then start the server with `AGGREGATOR_API_KEY=secret-ops-token python -m data_aggregator.server` in the background (`&` or `nohup`), and sanity-test it yourself with a couple of concurrent `curl` POSTs to `127.0.0.1:8080/process` (with the API key header) before considering it done, to confirm no hang occurs.
