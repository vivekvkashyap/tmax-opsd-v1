Work the pipeline stage by stage and verify each before moving on. First, extract frames from the video with `ffmpeg -i dashboard_recording.mp4 -vf fps=<N> frames/%04d.png` (pick a frame rate high enough to catch the flashing text), then run `tesseract` on each frame and grep the OCR output for the `JOB_ID: <alphanumeric>` pattern, deduplicating results — inspect the raw OCR text yourself to confirm the pattern is actually being matched before trusting it, since OCR often introduces noise/misreads around punctuation like the colon. Second, build query.sh so it takes a job_id, runs a `sqlite3` query joining jobs and servers on server_id to get hostname/expected_gb, separately extracts the matching record from the JSONL dump with `jq 'select(.job_id == "...")'` to get actual_gb/status, and merges both into one flat JSON object with `jq -n` or by constructing it directly — test this script manually against a known job_id first. Third, stand up the HTTP server (Python's http.server with a custom handler, or a socat/nc loop) so that GET /?job=<id> parses the query string, shells out to query.sh, and returns the JSON with Content-Type application/json; run it in the background and curl it yourself to confirm the response before considering it done.
