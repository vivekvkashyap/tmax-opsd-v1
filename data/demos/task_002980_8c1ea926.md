Build the script incrementally and verify reproducibility as you go, since that's the hard requirement here. Load the CSV with pandas, filter to `machine_id == 'M-101'`, drop NaN temperatures. For the Bayesian model, use PyMC's `pm.StudentT` likelihood with the exact priors specified (Normal for mu, HalfNormal for sigma, Exponential for nu) — don't silently substitute a Normal likelihood, since the whole point is robustness to outliers. Set `np.random.seed(42)` globally AND pass `random_seed=42` explicitly to `pm.sample(...)` with the exact draws/tune/chains/target_accept given — PyMC's own RNG is independent of numpy's global seed, so both must be set or reruns will differ. Use `arviz.summary` or `az.hdi` on the posterior trace to get mu's mean and 94% HDI bounds. Round all three values to 3 decimals and write them to JSON with the exact key names shown. Before finishing, actually run the script twice yourself and diff the two `results.json` outputs — if they differ, the sampler isn't fully seeded (check for missing `chains=` seeding or nondeterministic multiprocessing) and fix that before considering the task done.