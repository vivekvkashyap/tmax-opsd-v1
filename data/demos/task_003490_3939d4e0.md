Everything must happen inside `fix_and_run.py` when it is executed — don't run any fixes manually in the shell first, since the grader re-runs only the script. Rewrite `/home/user/app/deploy.sh` by writing the exact target content directly (don't try to sed/patch the old relative paths — just overwrite the file with the absolute-path version), and make this idempotent by writing unconditionally rather than appending or checking-then-modifying in a way that could duplicate content. Set permissions with `os.chmod(path, 0o755)`. For the passkey prompt, remember `pexpect` reads a pseudo-terminal, not redirected stdout — if `worker.py`'s prompt were sent to stdout it would vanish into `deploy.log` before pexpect ever sees it, so the prompt must appear somewhere pexpect can observe (spawn the shell script itself with pexpect, which runs on a pty, and match on the literal `Passkey:` string it emits before any internal redirection swallows it — inspect how `deploy.sh` redirects output to reason about what pexpect can and cannot see). Use `pexpect.spawn(...)`, `child.expect("Passkey:")`, `child.sendline("DEPLOY_V2_READY")`, then `child.expect(pexpect.EOF)` to block until completion, and finally confirm `/home/user/app/deploy.log` contains the success message before the script exits.