This will be checked by fuzzing against a hidden reference implementation, so match standard path-containment semantics exactly rather than inventing your own. OCR `/app/policy.png` (e.g. `tesseract`) and read the recognized text carefully to extract the "ACTIVE REPO ID" value — don't assume a fixed string, verify what OCR actually reports. For sanitization: take `target_base_dir`, make it absolute with `os.path.abspath`; for each manifest line, strip any leading slash from the entry (since an absolute-looking archive path should be treated as relative to the target, not literally absolute) then join it onto the target dir and normalize with `os.path.abspath`/`os.path.normpath` to collapse `..`/`.`. A path is safe only if the resolved path starts with `target_dir + os.sep` (strictly inside, not equal to the base dir itself) — get this boundary check exactly right, since off-by-one errors here (e.g. using plain `startswith(target_dir)` without the separator) let sibling-directory paths like `target_evil/` slip through as false positives. Print safe lines as `[<REPO_ID>] <resolved_path>` to stdout. For the log file, open `/tmp/artifact_db.log` in append mode, acquire `fcntl.flock(f, fcntl.LOCK_EX)` before writing and release with `fcntl.flock(f, fcntl.LOCK_UN)` after, so concurrent writers don't interleave.
