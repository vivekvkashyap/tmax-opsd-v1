Tackle each subsystem independently and confirm it works before moving to the next, since later steps depend on earlier ones being live. First fix `/home/user/nginx.conf`: find the `proxy_pass http://unix:/home/user/tmp/wrong.sock;` line under the `/upload` location and change the socket path to `/home/user/api.sock`, then reload with `nginx -s reload -c /home/user/nginx.conf` (or restart it) as the current user — verify with `curl -v http://127.0.0.1:8080/upload` that you no longer get 502 before proceeding. Second, edit `/home/user/start_backend.sh` to `export TZ=Europe/Paris` and `export LC_ALL=fr_FR.UTF-8` before the python invocation, then (re)start the backend and confirm it's listening on the unix socket. Third, write `/home/user/cleanup.sh` using `find /home/user -maxdepth 1 -name '*.tmp' -mtime +2 -delete`, chmod it executable, and add `0 * * * * /home/user/cleanup.sh` via `crontab -e`/`crontab -l` piped through a temp file — confirm with `crontab -l`. Fourth, process the audio with `ffmpeg -i /app/voicemail.wav -ar 8000 -ac 1 -af lowpass=f=3000 /home/user/processed_audio.wav`, matching resample rate, channel mixdown, and filter flags exactly as specified. Finally curl-POST the processed file to `/upload` and save the response body.