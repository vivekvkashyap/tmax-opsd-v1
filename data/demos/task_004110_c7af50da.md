Work through the four deliverables in order, verifying each with a real command rather than assuming correctness. For the CWE: the vulnerability class is "Invocation of Process Using Visible Sensitive Information" (passing secrets via argv, exposed through `/proc/[pid]/cmdline` and `ps`) — look this up in the CWE catalog to get the precise, correctly-numbered identifier rather than guessing a plausible-looking number. Rewrite `auth_helper.c` to call `getenv("AUTH_PASS")`, checking both for a NULL pointer and an empty string (`strlen(secret) == 0`) and exiting with status 1 in either case; keep the success `printf` message text byte-identical to the original format. Compile with `gcc -o /home/user/audit/auth_helper_fixed auth_helper.c` and manually test both the success path (`AUTH_PASS=x ./auth_helper_fixed`) and failure path (unset, then run) to confirm exit codes and output text match exactly. For certificate validation, run `openssl verify -CAfile ca.crt server.crt` and use its actual result (don't assume "VALID") to decide what to write. Compute the checksum with `sha256sum auth_helper.c` on your *modified* file (order matters — hash after editing, not before) and write all three findings as exactly three lines in `/home/user/audit/report.txt`.