Parse `/home/user/config.env` (`source` it or `grep`/`cut` the `MULTIPLIER=`/`THRESHOLD=` lines) to get the two numeric parameters — don't hardcode them, since the point of the config file is that they're the given inputs. For the 500k-row CSV, split it into N chunks (e.g. `split -n l/8 particles.csv chunk_`) and process each chunk in a background job (`&` + `wait`, or `xargs -P`), computing `Energy = Mass * MULTIPLIER * sqrt(X^2+Y^2+Z^2)` per row with `awk` (which handles floating math and sqrt natively and is far faster in a loop than shell arithmetic) — have each parallel worker emit only the energy values that exceed THRESHOLD, then merge all chunk outputs and compute final count and sum in one pass (e.g. another `awk 'END{...}'` over the concatenated results) so the parallel split doesn't corrupt the aggregate. Round the sum to exactly 2 decimal places with `awk`'s `printf "%.2f"` (or bc) rather than shell truncation. Read `/home/user/report.tmpl` and substitute `{{THRESHOLD}}`, `{{COUNT}}`, `{{SUM}}` with `sed` (be careful of `sed` special characters in numeric values, usually fine but escape `.` if using it in a pattern) writing the result to `/home/user/final_report.md`, then diff-check the template's non-placeholder text is preserved verbatim.
