Approach this as a chained forensic investigation: each artifact unlocks the next. First, port-scan localhost across 8000-8100 (e.g. `nmap -p8000-8100 localhost` or a quick loop with `curl`/`/dev/tcp`) to find the hidden service, then fetch `manifest.sha256` from its root and compute sha256 for every file under `/app/server/` to diff against the manifest and identify the single tampered file. Second, transcribe `/app/voip_intercept.wav` (whisper or similar) and carefully convert any spoken/spelled-out characters (e.g. NATO-style words) into the actual 4-character alphanumeric prefix — don't guess, read what the transcript literally says. Third, open the modified source file you identified and read its backdoor condition precisely: it likely requires a specific Cookie value starting with the recovered prefix AND a specific custom header name used for command execution — note the exact header name and cookie key from the code, don't assume generic names. Build `/home/user/detector` (any language, chmod +x) that parses a raw HTTP request file, checks for both conditions exactly as the backdoor code does, and exits 1 if both match, 0 otherwise. Validate your detector against a few files you construct yourself (with/without each condition) before trusting it against the hidden corpora.