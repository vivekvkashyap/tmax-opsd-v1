Follow the four phases literally and in the specified order — this is a deterministic numerical pipeline where the exact API calls matter more than cleverness. Load `signals.csv` into a (1000,50) NumPy array. For the FFT low-pass filter, compute `np.fft.fftfreq(1000)` once, and for each column independently take `np.fft.fft`, zero out coefficients wherever `abs(freq) > 0.1` (use boolean masking on the full complex array, not just the positive-frequency half), then `np.fft.ifft(...).real` to reconstruct — do this per-column, not on the whole matrix at once, since `fft`/`ifft` need to operate along the time axis for each sensor separately (watch the `axis` argument if you vectorize). For SVD, call `np.linalg.svd(X, full_matrices=False)` and build the 3-component feature matrix as `U[:, :3] * S[:3]` (elementwise column scaling, equivalent to `X @ Vt.T[:, :3]` — use one method and trust it, don't mix). Load `target.csv` as a flat 1D array (watch for accidental 2D shape from `loadtxt`), then solve with `np.linalg.lstsq(F, y, rcond=None)[0]` for the no-intercept fit. Write the 3 weights to `/home/user/weights.txt` as a single comma-separated line, each formatted with exactly 6 decimal places via an f-string, not `str()` or `round()`.
