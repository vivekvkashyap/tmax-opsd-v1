Do the reconnaissance with static tools before writing any code. Run `strings malware_bin | less` (and cross-check with `readelf -a` / `objdump -d` if strings alone is ambiguous) to find the embedded C2 IP:port, the custom exfiltration header name, and the hardcoded auth cookie value — these are typically plain ASCII literals in the binary's rodata. Then write `/home/user/forensics.go` in three clear phases: Phase 2 (file integrity) should walk `/home/user/evidence/`, compute `sha256.Sum256` per file, and compare against the parsed `<hash>  <filename>` lines in `known_hashes.txt` to find the one mismatch — don't assume which file is altered, compute it. Phase 3 should read `/home/user/traffic.log` line by line, `json.Unmarshal` each JSON-lines record, and filter for the entry whose IP/port/cookie match what you found in Phase 1 (not just any POST), then base64-decode the value under the custom header name you discovered to get plaintext. Finally marshal all findings into the exact JSON shape specified and write it to `/home/user/forensics_report.json`, then `go build`/`go run` the program so the file is actually produced.
