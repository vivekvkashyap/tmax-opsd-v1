Implement Base64 decoding yourself in C (build a 64-entry lookup table mapping A-Z/a-z/0-9/+//, handle `=` padding) since no external libs are allowed — test it against a couple of filenames you decode by hand first to make sure your table and padding logic are correct before wiring it into the pipeline. Parse `/home/user/requirements.txt` line by line splitting on `>=` to get a package name and a required major.minor.patch, and parse each decoded filename with the `[name]_v[major].[minor].[patch].meta` pattern (careful: package names can't be split naively on `_` since some may contain it — split from the `_v` marker instead). Compare versions field-by-field as integers (not lexicographically, since e.g. "10" vs "2" would be misordered as strings) to decide valid vs. outdated per SemVer precedence rules. Skip any decoded package name absent from requirements. Copy (not rename) each file into `/home/user/valid_packages/` or `/home/user/outdated_packages/` under its *decoded* name, create both directories first, compile to `/home/user/organizer`, and run it with stdout redirected to `/home/user/run.log`.
