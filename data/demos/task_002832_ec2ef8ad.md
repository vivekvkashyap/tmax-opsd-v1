Start by getting the multi-service plumbing correct before touching the numerics: launch `/app/start.sh` in the background, confirm Redis (6379) and Flask (5000) are actually listening, then fix `worker.py` to GET `http://127.0.0.1:5000/job` and parse the returned `alpha`/`duration`/`dt`, later POSTing the final list to `/submit`. For the PDE, the naive explicit-Euler-on-5-points approach is unstable — discard it. Build a 101-point spatial grid, enforce the two boundary conditions (sin combination at x=0, zero at x=1) directly on the state rather than evolving them via the PDE, and integrate only the interior points using `scipy.integrate.solve_ivp` with a stiff/implicit method (e.g. `BDF` or `Radau`) since explicit methods will blow up at this spatial resolution; request output at the exact 1001 evenly spaced time points from 0 to duration. Extract the row at x=0.5 (the middle grid index), take `np.fft.fft`, divide by the number of samples, and keep indices 0 through len//2 inclusive (501 values) — do not keep the full symmetric spectrum. POST that as a JSON list to `/submit` and check the resulting file lands at `/home/user/result_spectrum.json`.
