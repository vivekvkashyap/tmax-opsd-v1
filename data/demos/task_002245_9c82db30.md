Extract per-frame packet sizes first with `ffprobe -show_frames -select_streams v:0 -of csv=p=0 -show_entries frame=pkt_size /app/traffic.mp4`, redirecting to a CSV, and map each row's 0-indexed position to `node_id = index % 500`, summing pkt_size per node_id to get each node's base traffic. In your C program, load `/app/topology.csv` (node_id, parent_id, region) into arrays/structs, then compute the recursive rollup carefully: build a children-list (or adjacency) per node from the parent_id column, and do a proper post-order traversal (recursive DFS with a visited-check, or an explicit stack) from each root so every node's rolled-up total is its own base traffic plus the already-computed rolled-up totals of its children — avoid re-summing a subtree more than once. Finally, sum the rolled-up totals of only the nodes with an empty `parent_id`, grouped by their `region`, and print `region,total_region_traffic` sorted alphabetically by region to `/home/user/region_traffic.csv`. Since verification allows MSE < 1.0, floating point summation order differences are tolerated, but the aggregation logic (which nodes count, and the recursive-sum semantics) must be exactly right — validate on the small 11-node topology shown in the task description before trusting your logic on the full 500-node file.
