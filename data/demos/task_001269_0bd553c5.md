Start with the OCR step: run `tesseract /app/query_criteria.png stdout` (or another OCR tool) to recover the exact text listing the target root node names and the minimum aggregate value threshold — don't hardcode guessed values, parse them out of the actual OCR output, and sanity-check the extraction since OCR can misread characters (verify node names look like plausible identifiers and the threshold parses as a float). Inspect `/app/knowledge_graph.db`'s `nodes` schema with `sqlite3` to confirm the `parent_id` self-reference. In `/home/user/aggregate.py`, for each extracted root name, look up its `id`, then run a recursive CTE (`WITH RECURSIVE subtree AS (SELECT id FROM nodes WHERE id = ? UNION ALL SELECT n.id FROM nodes n JOIN subtree s ON n.parent_id = s.id) SELECT SUM(value) FROM nodes WHERE id IN subtree`) to sum that node's entire descendant subtree including itself. Filter out roots whose subtree sum is strictly below the OCR'd threshold. Before writing output, load `/app/output_schema.json` and validate your constructed list with `jsonschema.validate()`, catching failures rather than ignoring them, then write the validated JSON to `/home/user/results.json` with full float precision (avoid premature rounding).
