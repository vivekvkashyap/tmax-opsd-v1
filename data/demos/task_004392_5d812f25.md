Work through the remediation checklist methodically rather than rewriting the client from scratch. Start `/home/user/server/server.py` in the background and confirm with `curl -k` that it responds before touching the client. In `vulnerable_client.py`: remove the `--token` argparse argument entirely and instead read the token via `os.environ.get('SECRET_TOKEN')`, failing clearly if unset; replace `verify=False` with `verify='/home/user/certs/root_ca.pem'` so the request validates the server cert against that specific CA rather than disabling verification. After a successful request, base64-decode the `payload` field from the JSON response into a UTF-8 string, and inspect the response's `Set-Cookie` header(s) (via `response.cookies` or the raw header string) to identify any cookie missing the `Secure` and/or `HttpOnly` attribute — check for the literal flag substrings case-appropriately rather than assuming. Assemble the four required fields into `/home/user/audit_report.json` exactly as specified: the env var name you used, the CA path you used, the decoded payload string, and a list of the insecure cookie names. Run the client end-to-end with `SECRET_TOKEN=AuditToken2024` set to produce the real report.
