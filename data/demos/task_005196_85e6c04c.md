Handle the two fixes independently, then wire them together. First, data reshaping: read /home/user/raw_obs.csv, keep only the `x` and `value` columns (drop id and sensor_type), sort rows numerically by x ascending, and write them space-separated with no header to /home/user/clean_obs.txt — a quick awk/pandas one-liner is enough, just make sure the sort is numeric not lexicographic. Second, open /home/user/simulation/solver.cpp and locate `solve_system`; before the Gaussian elimination loops begin, add a loop that adds exactly `1e-5` to every diagonal element `A[i][i]` (a Tikhonov/ridge regularization) — this must happen before any elimination/pivoting touches those entries, and must not otherwise change the elimination or back-substitution logic. Do not touch the header interface. Then `g++ main.cpp solver.cpp -o mcmc_sim` from /home/user/simulation, run it (it reads clean_obs.txt), and confirm it completes without NaNs/crashes and writes a finite float to /home/user/posterior_mean.txt. If it still diverges or crashes, check that the regularization is applied to every diagonal entry, not just A[0][0], and that it runs before the loop that divides by A[i][i].