This is a black-box reverse-engineering task, not a graph-traversal-only task: the correctness of your classifier hinges entirely on correctly inferring the compliance oracle's decision rule, not just on parsing JSON graphs. Approach it methodically: (1) write the recursive DFS/BFS traversal first, enumerating every path from each `User`-type node to each `Resource`-type node, translating node ids to `label`s in order; (2) build a small test harness that calls the oracle binary on many synthetic comma-joined path strings (vary role labels, insert/omit intermediate nodes like proxies or gateways, vary keywords such as "Contractor", "PII", "Financial", "VPN", "Audit") and record exit codes to infer the underlying boolean rule — treat the oracle purely as a black box you query many times, don't try to disassemble it; (3) once you have high confidence in the rule (test edge cases: does an intermediate node override the trigger? does substring order matter?), either shell out to the oracle per-path from Python or reimplement the same logic natively for speed — both are allowed per the task. Finally, aggregate: if ANY discovered path is illegal, print "NON-COMPLIANT" and exit 1; otherwise print "COMPLIANT" and exit 0. Test your script against constructed clean/evil sample graphs before finishing, since the hidden corpus will grade exit codes exactly.