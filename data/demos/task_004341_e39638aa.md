Build this in the four phases as specified, verifying each in isolation. Phase 1: write `version_check.py` using simple tuple comparison of parsed `(major, minor, patch)` integers from the config's `engine_version` string against 1.5.0 and 2.0.0 bounds — don't use string comparison, since that breaks on multi-digit version segments. Phase 2: write the `.proto` file exactly matching the specified message/field names and types (string id, string operation, double value, repeated string dependencies; map<string,double> results), then compile with `python3 -m grpc_tools.protoc -I. --python_out=. --grpc_python_out=. math_dag.proto` run from inside `/home/user/math_port/`, producing `math_dag_pb2.py` and `math_dag_pb2_grpc.py`. Phase 3: implement the servicer's `ResolveGraph` by building a dependency graph from the request's nodes, then resolving values via memoized recursion or topological order (each node's operation depends only on already-resolved dependency values) rather than assuming input order is already topologically sorted. Phase 4: in the client, construct the exact graph described, call the RPC, and write the results map to JSON sorted alphabetically by key using `json.dump(..., sort_keys=True)`. Start the server in the background before running the client, and confirm you can independently derive the expected node values by hand (A=3.5, B=2.0, then C, D, E computed via ADD/MULTIPLY chaining) to sanity-check your server logic before trusting the RPC output.
