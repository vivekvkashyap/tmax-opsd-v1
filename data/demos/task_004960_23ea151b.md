Write `/home/user/process_logs.py` as a clear pipeline of independent, testable stages. Use a single anchored regex (e.g. `^\[(?P<ts>[\d-]+ [\d:]+)\] (?P<device>[A-Za-z0-9_]+) - STATUS:(?P<status>\d+) - TIME:(?P<time>-?\d+)ms - MSG:.*$`) to parse each line, since the whole-line format must match exactly — lines that don't fit this shape (garbled text, wrong delimiters) should just be dropped, not crash the script. After extracting fields, apply the validation gates as separate checks: `status_code` must be exactly 3 digits (reject 2 or 4-digit values even if numeric), `response_time_ms` must parse as an integer >= 0 (reject negative values caught by the regex group), and `device_id` must match `^DEVICE_[A-Za-z0-9]+$` (reject anything not starting with that literal prefix). For summary stats, group valid rows by device_id, compute count and mean response time rounded to exactly 2 decimals, sort by device_id, and write the CSV with the exact header given. For stratified sampling, within each device group sort by (response_time_ms ascending, then original file order ascending) to break ties deterministically, take the first 2, then write the *original raw line text* (not reconstructed) ordered by device_id then response_time_ms to the sample file.