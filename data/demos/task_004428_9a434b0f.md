In `extract.py`, open the log file and iterate over it directly (`for line in f:`) rather than `f.readlines()` or reading the whole file into a string, so memory stays bounded regardless of file size — this streaming property may be checked separately. Use one regex to find all timestamp matches (`\[\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2}\]`) and another for email matches on each line, and only emit a row when each regex finds exactly one match in that line — lines with zero or two-or-more of either must be silently skipped. Strip the surrounding brackets from the timestamp before writing, and write `timestamp,email` rows to `/home/user/extracted.csv` as you go (or buffer and write once, either is fine as long as memory use per line stays small). In `pipeline.sh`, `chmod +x` it, have it invoke `python3 extract.py` then pipe `extracted.csv` through `sort` (on the timestamp field, which sorts correctly as plain lexicographic text since the format is zero-padded and fixed-width) piped into `uniq` (after sorting, since `uniq` only catches adjacent duplicates) to drop exact duplicate rows, writing the result to `/home/user/clean_data.csv`. Trace through the sample log by hand to confirm which lines get skipped (ambiguous lines, duplicate lines) before running end-to-end.
