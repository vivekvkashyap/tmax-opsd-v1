Work in order and don't skip the integrity gate: read `/home/user/dataset/checksum.sha256`, compute `sha256sum` of `archive.zip`, and only proceed to `unzip` if they match (abort/report otherwise). After extraction, parse `config.ini` with Python's `configparser`, reading the `[Headers]` section into an old-name→new-name dict. List the extracted `.tsv` files and sort them alphabetically by filename before processing, so row order in the output is deterministic. For each TSV, use `csv.DictReader` with `delimiter='\t'` to get rows keyed by original headers, then remap each row's keys through the config dict before serializing with `json.dumps`. For the atomic-write requirement: write all JSON lines to a temp file in the same directory (e.g. via `tempfile.NamedTemporaryFile(dir=..., delete=False)`), flush and close it, then use `os.rename()` to atomically move it to `/home/user/dataset/output.jsonl` — never write directly to the final path, since a partial write there would corrupt the output.