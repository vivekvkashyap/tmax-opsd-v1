Work through the five sub-tasks independently and verify each in isolation before wiring them together. (1) Get the exact frame count with `ffprobe -v error -count_frames -select_streams v:0 -show_entries stream=nb_read_frames -of csv=p=0 security_cam.mp4` (container metadata alone can be inaccurate; forcing a frame count via `-count_frames` avoids under/over-counting). (2) Append `export` lines to `.profile` using the actual counted value, not a guess. (3) For the sanitizer, do a plain substring check for `"../"` inside the JSON's `mount_path` value (not a full path-canonicalization check — the spec is explicit about the literal substring), copying qualifying files byte-for-byte and skipping the rest; test it directly against both a clean and an evil sample dir to confirm behavior before relying on it for step 4. (4) Run your own filter script against the given payload set to populate `accepted_payloads/`, then derive `container_fstab` lines from the resulting `mount_path` values — don't hand-write the fstab. (5) In `nginx.conf`, gate the proxy_pass with an `if ($http_x_microservice != "accepted") { return 403; }` block (or an equivalent `map`), point `proxy_pass` at `http://127.0.0.1:8080`, and validate syntax with `nginx -t -c /home/user/nginx.conf` before finishing — do not start the server.