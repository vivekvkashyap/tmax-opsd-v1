Start by transcribing `/app/brief.wav` (e.g. with `openai-whisper` or `SpeechRecognition`) to recover the exact PCA component count and the variance-explained threshold the architect specifies — do not guess these numbers, read them from the transcript output. Then implement `/home/user/sanitizer.py` following the pipeline literally: load the audio with `librosa.load(path)` (default sr), compute `librosa.feature.mfcc(y=y, sr=sr)` with default parameters, transpose so shape is `(n_frames, n_mfcc)`, standardize each column with something like `sklearn.preprocessing.StandardScaler`, fit `sklearn.decomposition.PCA(n_components=<value from transcript>)` on the standardized features, and inspect `pca.explained_variance_ratio_[0]` (the fraction of variance explained by the first principal component). Compare that value against the threshold recovered from the transcript: if it meets the "clean" condition, `sys.exit(0)`; otherwise `sys.exit(1)`. Validate your script against a few files you synthesize locally (e.g. pure noise vs. structured tones) before trusting it, since the real judgment corpora are hidden.
