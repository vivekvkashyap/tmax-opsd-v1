Work through the four sub-problems in dependency order. First run `cargo build` in /home/user/auditor to see the actual dependency error, then inspect Cargo.toml and adjust version constraints/feature flags (e.g. loosen or align the `image`/`regex` version requirements) just enough to resolve the conflict without downgrading a major version — check `cargo tree` to see what's colliding. Next apply the patch with `git apply patches/extractor.patch` (or `patch -p1 <`) from the repo root, then open `legacy/analyzer.py` and read its bit-extraction logic carefully: it samples specific frames, checks a center-pixel-region brightness threshold, and assembles bits into ASCII characters — translate that same per-frame sampling stride, region, and threshold logic into `src/analyzer.rs` using the `image` crate's pixel access APIs, keeping the exact same bit-to-character packing order. Fix any resulting borrow-checker/API-mismatch errors from the newer crate version (e.g. changed method names on `image::DynamicImage`). Build `--release`, run the binary against /app/auth_feed.mp4, and write the exact token string it outputs (no extra whitespace or trailing newline issues) to /home/user/token.txt — since the verifier requires an exact/1.0 similarity match, double check there's no stray formatting like brackets or quotes around the token before writing.