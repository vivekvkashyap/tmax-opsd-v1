Work the two forensic sub-problems independently before writing any code. First, grep `/home/user/logs/api_requests.log` for the injected debug response (look for the SQL injection payload / verbose JSON body among the log noise) and pull out the hex-encoded encrypted master key and the IV it reports. Second, reverse-engineer `/home/user/legacy_encrypt` with `objdump -d`/`ghidra`/`gdb` (or trial-and-error against known IV/plaintext if you can find test vectors) to recover the stream-cipher construction: it's very likely a linear-congruential generator reseeded per byte (`state = state*A + B mod 2^32`) whose keystream byte comes from a fixed bit-shift/mask of the state (e.g. bits 16-23), applied via XOR against the plaintext one byte at a time starting from the given IV as the initial state. Reimplement that exact LCG+XOR loop yourself (in Python or C) seeded with the leaked IV, run it forward the ciphertext's length, and XOR against the ciphertext bytes to recover the plaintext master key — validate by checking the recovered bytes are printable ASCII. Then write `/home/user/rotate.c`: validate the CLI-argument key against `[A-Za-z0-9!@#$]+` only, exiting 1 on any other character; otherwise SHA-256 it via OpenSSL's EVP or `SHA256()` API and print `{"status": "rotated", "new_key": "<hex>"}` (lowercase hex) to `/home/user/new_creds.json`. Compile with `-lcrypto` and run it with the recovered plaintext key.