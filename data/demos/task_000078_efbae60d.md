First run `/app/start_services.sh` and then explore both databases interactively before writing any code: use `psql` to inspect `jobs`, `nodes`, `files` (note foreign keys, and how root nodes vs children are distinguished, e.g. a nullable parent_id), and use `mongosh` to inspect a few `chunks` documents to see how `file_id` and `chunk_index` map to file content. Build the path reconstruction with a recursive CTE (`WITH RECURSIVE`) starting from the root node for the given job_id, concatenating path segments down to each leaf file node — get the separator and root naming convention right by testing against sample paths. For each file, query Mongo's `chunks` collection filtered by `file_id`, sorted by `chunk_index` ascending, and concatenate the `data`/content field in that order. Serialize the final list as JSON, sort strictly by the `path` key (not insertion order), and be careful to send ALL logging/debug output to stderr, not stdout, since the verifier does exact byte-for-byte JSON comparison against a hidden oracle. Test with multiple job IDs and edge cases (empty folders, deep nesting) before finishing.