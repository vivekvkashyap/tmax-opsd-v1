Scaffold with `cargo new anomaly_detector` at the required path, then add `reqwest` (with `blocking` or `tokio` feature), `regex`, `csv`, and `serde_json` to Cargo.toml. Fetch each of the three CSV URLs from the running local server, then process them concurrently — either spawn one `tokio::task` per file or use `rayon`'s parallel iterator over the fetched bodies, don't process them sequentially since the task explicitly requires parallelism. Parse each CSV with the `csv` crate reading the `log_message` column, and apply a regex anchored to the literal prefix `E-500: Failed connection from ` followed by an IPv4-shaped capture group (four dot-separated groups of 1-3 digits) — decide up front whether to bound each octet to 0-255 or accept any 1-3 digit group, since edge-case inputs test this. Collect matches into a `HashSet<String>` across all three files to dedupe, then sort alphabetically (string sort, not numeric) and serialize with `serde_json` to `/home/user/anomalies.json`. Run `cargo build` then execute the binary to actually produce the output file — don't just write code, verify the file materializes and is valid JSON.
