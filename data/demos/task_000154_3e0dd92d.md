Treat this as three independent deliverables and verify each before moving to the next. Stage 1: extract frames from `/app/dashboard_recording.mp4` with `ffmpeg -i ... -vf fps=1 frame_%03d.png` (one frame per second), then for each frame programmatically check the top-left 16x16 pixel block (e.g., with PIL/cv2) for pixels matching exactly `#FF0000`, and write the seconds where the red square is present (one integer per line) to `/home/user/outage_seconds.txt` — inspect actual pixel values rather than eyeballing, since the exact color match matters. Stage 2: the socket-permission problem is that the backend daemon creates `/run/api-backend/api.sock` as root with restrictive mode; fix this in the systemd unit or its startup script by adding a step after socket creation (or a `SocketMode`/`umask`/`chmod`/`chown` directive) that grants `www-data` rw access, then restart the service and confirm with `curl --unix-socket` or by checking nginx no longer 502s. Stage 3: write `sanitizer.c` to read the full raw request from stdin, parse out the `X-Trace-Id` header value, and reject (`exit(1)`) if its length exceeds 64 or it contains any character besides alphanumerics and hyphens; accept (`exit(0)`) otherwise. Test the compiled binary against both corpora directly before writing `deploy.sh`.