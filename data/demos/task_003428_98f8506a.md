Parse `/home/user/molecule.edgelist` in C, determining N as (max node id seen) + 1, and build an adjacency list (the graph is undirected, so add both directions per edge line). Run BFS from every node to get all-pairs shortest distances — with N around 100 this is cheap (O(N^2)) — and while doing so, tally a histogram of distances over unique pairs u<v only (don't double count (u,v) and (v,u)), tracking the max distance seen as the diameter D. Compute `P_emp(d) = count[d] / (N*(N-1)/2)` for d=1..D. For the theoretical side, compute unnormalized weights `T(d) = pow(3,d)/factorial(d)` for d=1..D (use `double` and iterative factorial/power to avoid overflow for larger D), sum them, and divide each by that sum to normalize into P_theory. Then TVD = 0.5 * sum of |P_emp(d) - P_theory(d)| over d=1..D. Write exactly two lines to `/home/user/fit_results.txt` — `Diameter: <D>` and `TVD: <value>` formatted with `printf("%.4f", tvd)` — with no extra whitespace or lines, then compile with `gcc -lm` and actually execute the binary so the file gets produced.