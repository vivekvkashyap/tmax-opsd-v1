Extract the numeric constant from /app/matrix_coef.png with `tesseract` first (double-check the OCR'd digits look sane — re-run with different settings if uncertain) since the correct fix depends on hardcoding this exact coefficient. Then use `git log --oneline` in /home/user/math_solver to see the ~200 commits, and run `git bisect start`, marking current HEAD as bad and an old-enough commit as good; write a small test/build script that compiles the solver at each bisected commit and runs it against a couple of known matrix inputs, asserting the output is well-formed (no NaNs, correct dimensions) — feed this script to `git bisect run` to automate narrowing down the exact commit that introduced the regression. Once found, inspect that commit's diff to see what constant or fast-path logic changed in src/transform.c, replace the wrong hardcoded value with the coefficient you OCR'd from the image, and add/verify a fallback path for degenerate inputs (NaN entries, zero-determinant matrices) rather than letting them propagate garbage. Compile to exactly /home/user/math_solver/bin/solver, reading 16 floats from stdin and printing 16 space-separated floats formatted to 4 decimal places, and manually cross-check a few known transforms by hand before trusting the fuzz comparison.
