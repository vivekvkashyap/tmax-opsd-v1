Design for correctness first, then optimize for the strict runtime budget. Walk the tree with `fdopendir`/`readdir` (or `nftw` with `FTW_PHYS`) rather than following symlinks blindly; before descending into any directory, `fstatat` it and record its (st_dev, st_ino) in a visited-set (hash table) so a symlink loop back to an ancestor is detected and skipped instead of recursed into forever. For each `.wav` file, `mmap` it read-only and scan chunk headers manually (4-byte ID + 4-byte little-endian size) walking past RIFF/fmt/data chunks until you hit `UTF1`; convert its UTF-16LE payload to UTF-8 yourself (a simple codepoint loop is fine, no iconv needed) rather than shelling out. Stream output through zlib's `gzwrite`/deflate API incrementally instead of buffering everything in memory. To hit the 0.25s threshold with 10,000 directories, minimize syscalls (batch stat info from readdir's d_type where available), avoid unnecessary malloc/free churn, and compile with `-O3`. Test against `/app/speech.wav` to validate chunk parsing before running the full traversal.
