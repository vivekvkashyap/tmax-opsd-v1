Use pandas and build this as a clear pipeline of discrete steps rather than one dense transformation. First parse the CSV with `pd.read_csv` (it has embedded newlines inside quoted fields, so pandas' default CSV engine handles that correctly as long as you don't pre-split lines manually). Melt the three service columns into long form with `pd.melt(id_vars='timestamp', value_vars=[...])`, drop rows where message is null/empty, then map service names to the abbreviations. Clean messages with `.str.replace(r'\r\n|\n', ' ', regex=True).str.strip()`. Convert timestamp with `pd.to_datetime` and floor to the hour via `.dt.floor('h')`, then groupby (service, floored timestamp) joining multiple messages with `" | "`. For gap-filling, build the full hourly range with `pd.date_range(min_hour, max_hour, freq='h')` crossed with every service (e.g. `pd.MultiIndex.from_product`), reindex, and fill missing messages with `"NO_DATA"` — this step is where naive solutions miss gaps at the start/end or per-service ranges. Compute the error indicator by checking for the exact substring `"ERROR"` in each message, then use `.groupby('service')['error_ind'].rolling(3, min_periods=1).sum()` reset properly aligned back to rows (watch for index misalignment after groupby-rolling). Sort by service then timestamp, save to CSV. For the report, pull each service's max rolling value and the message at its max timestamp, and match the markdown template exactly (headers, spacing, wording).