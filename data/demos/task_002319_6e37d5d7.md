Start by trying `make` in `/app/json11` to see the actual compiler errors — json11 uses C++11 features (e.g. `override`, initializer lists) so a Makefile pinned to an older `-std` flag will fail with cryptic syntax errors; locate the `CXXFLAGS` line and bump the standard version, then rerun `make` until `libjson11.a` is produced. For the validator, parse each field defensively: check the JSON parses, then check each required field exists AND has the correct type AND (for numeric fields) falls within the allowed range, using json11's type-checking methods (e.g. `is_number()`, `is_string()`) rather than assuming presence — a naive validator that just calls `.number_value()` on a missing field will silently return 0 and pass invalid input. Compile with static linking against `libjson11.a` and verify each constraint (cv_folds >= 2, 1.0 <= outlier_threshold_z <= 5.0, vocab_size >= 1000, experiment_id present as string) independently so a single bad field can't slip through. Finally write the bash driver to loop both corpus directories, run the validator, and route files to `/home/user/accepted/` or `/home/user/rejected/` based on exit code, testing against every clean and evil file individually to confirm 100% correct classification.