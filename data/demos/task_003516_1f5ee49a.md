First OCR the kinetics image with `tesseract /app/kinetics.png -` and carefully read off the two labeled rate constants (alpha, beta) from the recognized text rather than guessing typical values — double check for OCR misreads of digits before hardcoding them into the C program. In `filter.c`, parse the FASTA file by skipping any line starting with `>` and counting `G`/`C`/`g`/`c` characters across all remaining lines to get N_GC (ignore newlines and any other non-sequence characters). Implement the Forward-Euler loop exactly as specified: `y += dt * (alpha * N_GC - beta * y)` each step with `dt=0.1`, tracking the previous y to test the convergence criterion `fabs(y - y_prev) < 1e-5` before stopping — don't cap iterations arbitrarily low, since convergence depends on beta and could take many steps; guard against infinite loops with a generous max-iteration ceiling well beyond the expected convergence point. Return exit code 1 if converged y is strictly greater than 250.0, else 0. Since a hidden corpus with clean/evil FASTA files will exercise this binary directly, test it manually against constructed sequences near the boundary GC count to confirm the strict-inequality behavior is correct before moving to the plotting step (redirect the same simulation's (t,y) pairs to timeseries.csv and plot with gnuplot).
