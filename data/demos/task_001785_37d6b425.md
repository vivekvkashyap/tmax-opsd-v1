Read `server.py` first to spot the vulnerable line — it calls `render_template_string()` directly on user-supplied input, a classic Flask SSTI. Start the app (`pip install flask` if missing, run in background) and confirm it binds to `127.0.0.1:5000`. Craft a Jinja2 SSTI payload posted to the `template` form field that reads an arbitrary file — the standard approach is chaining through `{{ }}` object introspection to reach `os.popen`/`__builtins__` (e.g. via `''.__class__.__mro__[...]` gadget chains) to execute a `cat`/`open().read()` of `/home/user/secret_permissions.json`, then capture the HTTP response body as the exfiltrated JSON — do not read the file directly from disk, since the task requires proving the exploit through the web app. Once you have the raw JSON text extracted via HTTP, write `/home/user/process_audit.py` that parses it, walks the structure recursively (it has nested lists of dicts) replacing any key exactly named `ssn` or `password` with `"***"`, then dumps with `json.dump(..., indent=4, sort_keys=True)` to `/home/user/redacted_report.json`. Finally compute `hashlib.sha256` over the exact bytes of that written file (read it back rather than re-serializing in memory, to avoid whitespace/formatting mismatches) and write the lowercase hex digest to `/home/user/report_hash.txt`.
