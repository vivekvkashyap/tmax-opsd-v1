Work top-down, validating each stage before building on it. First `cd /app/extractor` and try `make`; a link-step failure with unresolved `sqrt`/math symbols means a missing `-lm` flag — add it to the link command (after the object files) and rebuild. Second, run `./extractor /app/fixture.wav > /home/user/commands.txt` and inspect the file to learn the actual instruction sequence — don't assume it. Third, write `/home/user/interpreter.sh`: read each line, split opcode/operand, apply LOAD/ADD/SUB/MUL/DIV to an integer accumulator via bash arithmetic, printing only the final integer; test it standalone and hand-verify the result against `commands.txt`. Fourth, write `/home/user/server.sh` as a loop around `nc -l -p 9090` (or `socat`) that, per request, calls `interpreter.sh` and returns a valid `HTTP/1.1 200 OK` with correct `Content-Type` and JSON body reflecting the freshly computed value — test with `curl` for several sequential requests before backgrounding it.
