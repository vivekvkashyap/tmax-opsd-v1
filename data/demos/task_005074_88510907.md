Before touching concurrency, get the per-row math and validation gates correct on a sequential pass, then parallelize. Handle the `π` anomaly by string-replacing the literal escape sequence `π` in the raw JSON text with the numeric literal `3.14159` before calling `json.Unmarshal` — do not try to make Go's JSON decoder interpret it as a float directly, since `π` is a valid JSON unicode-escaped string character, not a number, and will only unmarshal cleanly into a []float64 after the substitution. Apply Quality Gate 1 by treating any unmarshal error (or non-array JSON) as a discard, and Quality Gate 2 by discarding any successfully parsed slice with length < 3. For the feature transforms, compute L2 norm as sqrt(sum of squares) and population variance as mean of squared deviations from the mean (divide by N, not N-1) — verify your formulas against a couple of rows by hand before trusting the Go code. For parallelism, fan out rows to worker goroutines over a channel and collect results back through another channel (or use a sync.WaitGroup with a mutex-protected results slice), then sort the collected valid results by ID ascending before writing the CSV with values formatted to exactly 4 decimal places via `%.4f`.
