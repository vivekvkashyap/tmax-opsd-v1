Write check_health.exp using `spawn` on cli_tool.sh, `expect "Please enter diagnostic PIN:"`, `send "8472\r"`, then let it run to `expect eof` while logging output, and filter the captured output down to only the `[STATE] ` line (either within the expect script's post-processing or by piping through `grep '^\[STATE\]'` from the calling wrapper — the simplest robust approach). In alert_manager.sh, first `mkdir -p` both alert directories, then capture the state line from your expect script. For CRITICAL_ERROR, check with `-e`/`-L` whether current_alert.link already exists before doing anything — if it does, do nothing (idempotency); if not, create an `incident_<timestamp>.log` file (using `date +%s`) containing the exact state string, then symlink it. For HEALTHY, check whether the symlink exists; if so, resolve its target with `readlink`, `mv` that target file into alerts/resolved/ keeping its basename, then remove the symlink; if not, do nothing. Verify both branches manually by toggling internal_state.dat and re-running the wrapper twice in a row to confirm no duplicate actions occur.
