Work through this in stages, testing each one before moving on. First, diff the broken `start.sh` mentally against what `socat` actually needs: the listen spec must be `TCP-LISTEN:8080,reuseaddr,fork` (check for typo'd flag/command names like an extra letter on `socat`), the port must match what the verifier hits, and any `SERVER_ROOT`/`cd` must point at the real vendored package directory, not a placeholder web root — otherwise the CGI handler can't find scripts. Second, patch `login.sh` and `diag.sh` in pure Bash: validate the `next` param with a pattern requiring a leading `/` and rejecting a leading `//` before ever emitting a `Location:` header, falling back to `/index.sh`; validate `ip` with a strict IPv4 regex (four dot-separated 0-255 octets) before interpolating it into any command, emitting `400` on failure — never sanitize by blacklisting characters, only by whitelisting the exact format. Third, for the ELF binary, run `strings` on it to find the hardcoded secret string it checks for in an SSH key comment, then write `generate_key.sh` using `ssh-keygen -t ed25519 -N ""` with `-C` set to that discovered string. Finally launch `start.sh` in the background and confirm with `curl` that port 8080 responds before finishing.
