Parse each line with one regex that captures all fields case-insensitively, e.g. `User:\s*(\S+)\s+performed Action:\s*(\S+)\s+on Item:\s*(\S+?)\.\s+Status:\s*(\S+)` — strip the trailing period from item_id via the capture group boundary or an explicit `.rstrip('.')`. Filter to `status.upper() == 'SUCCESS'` before any normalization. Uppercase user_id, action, and item_id. Deduplicate on the tuple (timestamp, user_id, action, item_id) after normalization, keeping first occurrence — use an ordered set/dict or pandas `drop_duplicates(keep='first')`, being careful that dedup happens after normalization/uppercasing, not before (raw-case duplicates that only differ by case must still collapse). Group by user_id and compute total_actions (row count), unique_items (`len(set(item_ids))`), and most_frequent_action — for ties, use `Counter` then sort candidates by (-count, action_name) and take the first, since Python's `Counter.most_common()` does not guarantee alphabetical tie-breaking on its own. Emit a JSON object keyed by user_id sorted alphabetically (e.g. build via `dict(sorted(...))` or `json.dumps(..., sort_keys=True)`) with the three fields exactly named `total_actions`, `unique_items`, `most_frequent_action`, written to /home/user/user_features.json.