Start by recovering the validation rules from the image, not by guessing: run OCR (e.g. `tesseract /app/architecture_diagram.png stdout`) on `/app/architecture_diagram.png` to extract the permitted ACTIONS list and the SALT string embedded in the diagram text, and use those recovered values as constants/variables in your filter script — do not hardcode assumed action names or a made-up salt. Then implement `/home/user/filter.sh` (bash calling out to python/awk as needed) to: extract the top-level `.tar` archive to a temp dir, find every nested `.tar.gz` inside it and extract those too (recursively, since nesting could be more than one level — loop until no new archives are found), then locate every `.wal` file and validate each line by parsing `[TIMESTAMP] ACTION | KEY | VALUE | CHECKSUM`, checking ACTION is in the recovered permitted set, and recomputing `md5(ACTION + KEY + SALT)` (concatenated with no separators, matching the exact order specified) to compare against CHECKSUM. Exit 0 only if every entry in every nested `.wal` file validates; exit non-zero the first time any entry fails. Clean up temp extraction directories, and test your script against a couple of your own hand-built clean/evil `.tar` fixtures before trusting it against the hidden corpus.
