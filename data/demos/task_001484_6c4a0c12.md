Transcribe `/app/vip_call.wav` with an actual ASR tool (e.g. `openai-whisper`) rather than assuming its contents, and inspect the transcript text yourself to pull out the spoken customer reference ID (a `CUST-xxxx` pattern — extract it with a regex, don't hardcode a guess) and the product category name mentioned. Feed these two extracted values as parameters into `build_profile.py` rather than embedding literals. Load `customers.csv` and `interactions.csv` with pandas/csv, and stream-parse `purchases.json` as JSON Lines (one `json.loads` per line), filtering records whose `customer_ref` matches the extracted ID. Compute: mean of `resolution_time` over that customer's rows in interactions.csv; sum of `price` over `items` entries whose `category` equals the extracted category, across all matching purchase records; and the deduplicated set of `issue_type` values for that customer. Validate your final dict against the required schema (keys and types) before serializing, and write it to `/home/user/final_profile.json` with `json.dump`. Double check the audio-derived values actually match what appears in the CSV/JSON data — if the extracted category doesn't line up with any purchase records, re-check your regex/transcription rather than emitting a zero spend.
