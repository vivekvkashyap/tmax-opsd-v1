Write the Python script incrementally and inspect intermediate state before trusting the final numbers. Connect with `sqlite3`, and query with the literal SQL clause `SELECT sender_id, receiver_id FROM communications NOT INDEXED` (the `NOT INDEXED` keyword goes right after the table name) to force a full table scan bypassing the corrupted `idx_sender` index — verify the row count returned looks larger/complete compared to a naive indexed query, as a sanity check that the bypass actually worked. Build a `networkx.DiGraph()` by adding an edge per row (sender -> receiver), then call `nx.in_degree_centrality(G)`, sort the resulting dict by value descending, and take the top 3. Round each centrality to 4 decimal places only at output time (don't round intermediate values). Write the top 3 as a JSON array of `{"employee_id": ..., "centrality": ...}` objects in that exact key order and format to `/home/user/central_actors.json`. Print or inspect the full sorted list while developing to confirm the top few nodes make sense given the graph structure before trusting the automated output.
