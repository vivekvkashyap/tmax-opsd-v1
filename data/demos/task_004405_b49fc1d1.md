First transcribe `/app/dba_notes.wav` (whisper or similar ASR) to recover the DBA's exact three rules in plain English — don't assume rule wording, get it from the audio. Parse each rule into a concrete, checkable predicate: (1) for statements/transactions touching both `users` and `orders`, walk the parsed statement list in order and reject if an UPDATE on `users` appears before an UPDATE on `orders`; (2) for any window function call (RANK, ROW_NUMBER, etc.), inspect its OVER clause and reject if there's no PARTITION BY; (3) for any SELECT referencing the `transactions` table, reject unless the WHERE clause filters on `created_at`. Use `sqlglot` or `sqlparse` to get a real AST/tokens rather than regex-matching raw SQL text, since transactions can contain multiple statements and window functions can appear in subqueries. Print exactly `ACCEPT` or `REJECT` with nothing else to stdout. Test against `/home/user/samples/` first, then validate against the hidden evil/clean corpora logic by writing your own similar adversarial test cases covering edge cases (multiple window functions, nested rules, unrelated tables) before finalizing.
