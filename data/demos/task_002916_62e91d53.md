Work through this in strict sequence since each stage depends on the previous being fixed. (1) Run `make` in `/home/user/signal_decoder`, read the actual linker/compiler error, and fix it directly — a common cause for multi-threaded C++ code is a missing `-pthread` flag or an unlisted object file in the Makefile. (2) Extract the crash payload from `/app/signal.mp4` by pulling all 240 frames in order (`ffmpeg -i signal.mp4 frame_%04d.png`) and classifying each as black=0/white=1 by sampling average pixel brightness; pack the 240 bits MSB-first into 30 bytes and hex-encode them — don't hardcode any payload string, derive it from the actual frames. (3) Feed that hex string via stdin to your compiled binary repeatedly (loop it many times) to reproduce the intermittent crash/garbled output; inspect the threaded chunk-processing code for a shared container (e.g. a `std::vector` collecting per-thread results) being mutated without synchronization, and fix it with a `std::mutex`/lock_guard around each write (or thread-local buffers merged after joining). (4) Once stable, compare outputs against `/app/oracle_decoder` for varied hex inputs, then use `git bisect start`/`git bisect bad`/`good` with a script that builds each candidate commit and diffs its output against the oracle to pinpoint the exact commit that introduced the logic regression (look for suspicious off-by-one comparisons); port that fix forward onto the current `main` without reverting your earlier compile/mutex fixes.