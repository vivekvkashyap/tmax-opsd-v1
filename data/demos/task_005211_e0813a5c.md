Tackle this in order: fix the vendored bug first, then build the pipeline, then wrap it in a service. Open `/app/datacleaner-1.2.0/datacleaner/schema.py` and find the `age`-column enforcement function; it currently coerces non-numeric ages to NaN via `pd.to_numeric(..., errors='coerce')` but never drops those rows — add a `df.dropna(subset=['age'], inplace=True)` (or equivalent) immediately after the coercion so unparseable ages are removed entirely, not left as NaN. Reinstall the package locally (`pip install -e /app/datacleaner-1.2.0` or `pip install .` from that dir) so your fix takes effect for imports. Build the ETL: load `raw_users.csv`, call the fixed schema enforcement, one-hot encode `category`, median-impute `income`, then train a `RandomForestClassifier` against `is_active` and persist the fitted model plus the exact preprocessing steps (e.g. the one-hot column set and the median value) so inference-time records get identical transforms. In `/home/user/serve.py`, expose `POST /predict` on `127.0.0.1:8000`, reject requests missing/mismatching `Authorization: Bearer secret-token-99X` with 401, run incoming raw records through the same ETL/predict path, and return predictions as JSON. Start it in the background, then write and run a concurrency benchmark script (e.g. asyncio/httpx or a thread pool issuing ≥50 simultaneous requests) and append results to `/home/user/benchmark_results.txt`.
