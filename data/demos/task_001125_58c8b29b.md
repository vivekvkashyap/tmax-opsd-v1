Break this into independently-testable pieces. For signal extraction, decode /app/experiment.mp4 frame-by-frame (a Go video-decoding library, or shell out to ffmpeg to dump raw frames you then parse), average the grayscale pixel intensity within each node's fixed bounding box per frame to build three time series, then run a DFT over each series and pick the frequency bin (excluding k=0) with the largest magnitude, converting bin index to Hz using the video's frame rate and total frame count — verify your DFT on a synthetic pure sine wave first to make sure your bin-to-Hz conversion is correct before trusting real results. For the graph, parse graph.json's adjacency list and run plain BFS from A to C for shortest-path edge count, and count list lengths for degree. For MCMC, implement Metropolis-Hastings with a symmetric proposal (e.g. Normal(current, small step)), reject proposals outside [0,10] (uniform prior), use the given Gaussian likelihood with sigma=0.5 over the three (degree, frequency) pairs, run 10000 iterations, discard the first 1000, and average the rest for the posterior mean. Wire it all behind an HTTP server on 127.0.0.1:9090 checking the exact Bearer header before serving each route.