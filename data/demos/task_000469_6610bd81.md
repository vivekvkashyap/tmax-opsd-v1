Build this pipeline bottom-up and test each layer before automating it. First set up the venv (`python3 -m venv /home/user/venv`) and pip-install `numpy scipy jupyter papermill` inside it. Second, write and manually run the notebook logic as a plain script first to validate correctness: read `y0.txt` values, and for each y0 call `scipy.integrate.solve_ivp(fun, t_span=(0,5), y0=[y0], method='Radau')` with `fun(t, y) = -0.5 * y**2`, taking the solution's value at t=5 (use `t_eval=[5]` or read `.y[0][-1]`). Collect the array of final concentrations. Set `np.random.seed(42)` exactly once before bootstrapping, then resample the collected array with replacement `n_bootstrap` times, computing the mean of each resample, and take `np.percentile` at 2.5 and 97.5 of those means for the CI. Write `lower,upper` to the results file. Only after this logic is verified correct should you convert it into a notebook with a `parameters`-tagged cell (required for papermill parameter injection — the tag must be exact), then write the bash wrapper that sources/activates the venv and calls `papermill /home/user/analysis.ipynb /home/user/analysis_out.ipynb -p n_bootstrap 5000`. A common mistake is using a non-stiff-aware ODE method that diverges — Radau is specified precisely to avoid that.
