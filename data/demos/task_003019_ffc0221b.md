Focus entirely on your own filter program, not on reverse-engineering the stripped oracle binary — the task only needs you to detect floating-point reduction-order sensitivity in a raw float32 array. In C: `fopen` the file in binary mode, `fseek`/`ftell` to get size, divide by `sizeof(float)` to get N, `fread` the array. Compute `Sum_LR` by iterating index 0..N-1 in a simple accumulating loop (do NOT let the compiler auto-vectorize/reorder it differently from your right-to-left loop — write both loops with a plain scalar accumulator, and compile without aggressive `-ffast-math` reordering so the two directions genuinely differ on ill-conditioned inputs). Compute `Sum_RL` the same way but iterating N-1..0. Use `fabsf`/`fabs` for the difference and compare to the 0.01 threshold. Also scan every value with `isnan`/`isinf` and reject immediately if any are found, before or during summation. Print exactly `EVIL`/exit 1 or `CLEAN`/exit 0 per the spec. After compiling with `gcc -O2 -o /home/user/filter_signal /home/user/filter_signal.c -lm`, loop over every file in `/app/corpus/clean/` and `/app/corpus/evil/`, run the binary, and check exit codes match expectations for all of them — fix any misclassifications by inspecting the actual float magnitudes in the offending files (e.g. with a small Python script using `struct.unpack`).
