Treat `/app/validator` as a black-box oracle you must characterize empirically, then cross-check against static analysis — don't guess the format. Use `file`, `strings`, and a disassembler (`objdump -d` / `radare2` / `ghidra` if available) to look for the format constants (magic bytes, comparisons against fixed offsets, byte patterns near `..` or `/` checks) while simultaneously crafting small synthetic `.pdat` files (start from `/app/sample.pdat`, mutate bytes) and observing exit codes to confirm your hypothesis about the header layout, entry table, and filename encoding. Once you understand the structure (magic, entry count, per-entry offset+size, then a filename), replicate the exact validation rules in C: check magic bytes, verify every entry's offset+size does not exceed the file's actual byte length (bounds/overflow-safe arithmetic — watch for integer overflow when adding offset+size), and scan the raw UTF-16LE filename bytes for the two-byte-per-char encodings of `..` and `/` rather than converting to UTF-8 first (encoding-conversion bugs are the likely trap here). Handle truncated/malformed files by failing safe (exit 1) instead of crashing. Test your C binary against as many self-crafted edge cases as possible, comparing exit codes to the oracle, before trusting it on the hidden corpus. Then write `watch.sh` using `inotifywait -m -e close_write` in a loop, invoking your filter and moving/logging per the exact log line format specified.