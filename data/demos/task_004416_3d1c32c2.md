Model this as a single unified graph rather than treating hierarchy and messages as separate problems. Connect to `/home/user/company.db` with `sqlite3`, read all rows of `employees` and `messages`, and build one `networkx.Graph`-plus-directed-edges structure: for each employee with a non-null `manager_id`, add an undirected edge between the employee and their manager (since hierarchy communication works both ways); for each row in `messages`, add a directed edge from `sender_id` to `receiver_id` (message flow only goes one direction, sender to receiver, not back). A `networkx.DiGraph` where you add undirected hierarchy edges as two directed edges (one each way) plus the one-way message edges as single directed edges captures this correctly. Look up the node IDs for "Alice" and "Bob" by name via a query on `employees`, then use `networkx.shortest_path_length(G, source=alice_id, target=bob_id)` on the directed graph you built (not an undirected shortest path, since messages are one-way) to get the integer distance, and write just that integer to `/home/user/result.txt`. Double check by tracing the path manually against the DB contents before trusting the networkx result.