Build and test each layer independently before wiring them together. In the Rust `axum`/`tokio` project at `/home/user/proxy`, implement a POST `/process` handler that reads the `X-Tier` header (case-sensitive match on the value `Spot` vs anything else/absent), forwards the raw request body via an HTTP client (e.g. `reqwest`) to port 8082 or 8081 accordingly, and returns the upstream response body/status verbatim — use a shared atomic counter (e.g. `Arc<AtomicUsize>` or `Arc<Mutex<u64>>`) incremented once per successfully processed request. In parallel, spawn a separate `tokio::net::TcpListener` bound to 127.0.0.1:8084 that reads incoming bytes until it sees `STATS\n`, then writes back `PROCESSED={count}\n` using the same shared counter, then closes the socket — run this listener as its own async task alongside the axum server (e.g. via `tokio::join!` or `tokio::spawn`). Edit `nginx.conf`'s `proxy_pass` for `/process` to point at 127.0.0.1:8083. For `deploy.sh`, build with `cargo build --release`, launch the binary backgrounded with output redirected to `proxy.log`, then poll port 8084 in a loop (e.g. `nc -z` or a bash `/dev/tcp` check) every second until it accepts a connection before reloading nginx via the provided script — don't reload nginx before the proxy is actually listening, or the health check will race. For `backup.sh`, gzip the log to a timestamped path under `/home/user/backups/`, creating that directory first if missing.