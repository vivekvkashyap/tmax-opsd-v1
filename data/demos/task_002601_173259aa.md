Use delta debugging methodically rather than guessing: take samples from `/app/corrupted_samples.txt`, and for each one that hangs, repeatedly remove/shrink chunks (binary-search style — try removing halves, then quarters, etc.) while re-running the parse under a short `timeout`, keeping only reductions that still reproduce the hang, until you reach a minimal repro. Given the bug description (unbounded loop in object parsing on truncated/malformed input), focus your minimization on cases with unclosed `{`/trailing commas and unexpected end-of-string. Once you have a minimal repro, open `/app/simplejson-3.19.1/simplejson/decoder.py` and find the `JSONObject` function's loop that consumes key-value pairs; look for a code path where advancing past the end of the string or hitting an unexpected token fails to `break`/raise and instead loops again appending garbage — add the missing bounds check/`raise JSONDecodeError` so it terminates correctly on truncated input, and confirm your minimal repro now raises promptly instead of hanging. Then write `/home/user/parse_fuzz.py` to read stdin, call the patched (vendored) simplejson's `loads`, and print exactly `SUCCESS` or `ERROR` depending on whether a `JSONDecodeError` is raised — make sure it imports the vendored package (not the system json module) so your fix is actually exercised, and confirm valid JSON still parses correctly (no regressions).
