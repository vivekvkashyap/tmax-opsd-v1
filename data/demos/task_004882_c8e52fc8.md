Read `/app/legacy_cli.py` and `/app/api_gateway.py` first to see their exact prompt/response text and existing skeleton before writing the adapter — the pexpect script must match the CLI's literal prompts (e.g. `expect("Username: ")`, `expect("Password: ")`, `expect("Command: ")` or whatever strings the CLI actually prints), not assumed generic ones. Build `/app/adapter.py` using `pexpect.spawn` to launch `python3 /app/legacy_cli.py`, send `admin`/`supersecret` at the login prompts, then send the `QUERY` command for `/home/user/data_mount/config.json`, capture the CLI's response text, and parse/return it as JSON (strip prompt echoes/whitespace before `json.loads`). Wire `/app/api_gateway.py`'s `GET /api/v1/data` handler to call the adapter fresh per-request (or cache carefully), check the `Authorization: Bearer migrate-token-99` header exactly and return 401 otherwise, and shape the success response as `{"source": "legacy", "data": <parsed json>}`. Also actually create `/home/user/data_mount/config.json` with the specified content before testing. `/home/user/start_services.sh` should background the Flask app (`nohup python3 /app/api_gateway.py &`) and write `$!` to `/home/user/api.pid`; test end-to-end with `curl` against `127.0.0.1:5000` before considering it done.