First confirm each `.dat` file really is gzip (`file` command or check the `1f 8b` magic bytes with `xxd`), then build `/home/user/parser.py` to read raw bytes from stdin, unpack the fixed 16-byte header with `struct.unpack('<4sIQ', data[:16])` (little-endian: 4-byte magic, uint32 id, uint64 timestamp — verify the magic equals `b'BLOB'` before trusting the rest), convert the timestamp with `datetime.utcfromtimestamp(ts).strftime('%Y-%m-%dT%H:%M:%SZ')`, and print one JSON object (`filename`, `id`, `timestamp`, in that key order for consistency) via `json.dumps` — the filename comes from `sys.argv[1]`, not from parsing the stream. Drive it from the shell with a loop like `for f in /home/user/artifacts/*.dat; do gunzip -c "$f" | python3 /home/user/parser.py "$(basename "$f")"; done >> /home/user/manifest.jsonl`. Finally produce the alphabetically sorted version with `sort /home/user/manifest.jsonl -o /home/user/manifest_sorted.jsonl`.
