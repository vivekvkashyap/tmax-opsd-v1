Treat `/app/imputer` as an unknown compiled tool: first inspect it (`file /app/imputer`, `strings /app/imputer | grep -i usage`, or just run it with no args) to learn its expected CLI arguments rather than guessing, then run it on `/home/user/sensor_data.csv` to produce `/home/user/imputed.csv`. For the C program, parse the `id,timestamp,value` CSV line by line (skip the header), converting `value` to a double, and explicitly skip/exclude any row where `value < 0.0` or `value > 1000.0` from every statistic — do this filtering before accumulating sums. Implement sample standard deviation correctly: compute the mean over kept rows, then sum of squared deviations, divide by `(N-1)` (not `N`), and take the square root — a common bug is using population variance (N) instead of sample variance (N-1). Print/write the result with at least 4 decimal places (e.g. `%.6f`) to `/home/user/metric.txt`. Since grading is a numeric tolerance (not exact string match), sanity-check your result by recomputing the mean/stddev independently (e.g. a quick Python one-liner over the same filtered rows) before finishing.
