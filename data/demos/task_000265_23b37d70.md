Break this into three independently verifiable pieces and check each before moving on. First, extract per-frame particle coordinates from `/app/trajectory.mp4`: decode frames (e.g. with ffmpeg to raw images, or a small script using a video-reading library) and for each of the 20 frames scan for the single pixel equal to (255,255,255), recording its (col,row) as (X,Y) in frame order — verify you get exactly one hit per frame before trusting the data. Second, write a C Metropolis-Hastings sampler that proposes small perturbations to (A,B,C), accepts/rejects based on the sum-of-squared-residuals likelihood against your extracted (X,Y) pairs, runs >=100k iterations, discards the first 10% as burn-in, and averages the retained samples per parameter — sanity-check the posterior means by predicting Y at your own sampled X values and comparing to the extracted Y before wiring it into the service. Third, build an HTTP server (raw C sockets, or C logic behind socat) bound to 127.0.0.1:8080 that parses `/predict?x=` from the request line, requires and validates the exact `Authorization: Bearer perf-token-2024` header (reject otherwise, no crash), computes Y from your fitted A,B,C rounded to nearest int, and returns it as a bare-integer text body with a proper 200 status — then leave it running as a background process and confirm with curl before finishing.
