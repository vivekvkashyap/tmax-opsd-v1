Treat this as three separable subtasks that only combine at the end. For the regex: run `tesseract /app/regex_spec.png stdout` (or output to a file and cat it) to OCR the image text, then parse out just the regex pattern portion from whatever label precedes it — inspect the raw OCR output yourself rather than assuming a clean read, since OCR can misrender similar characters (e.g. 0/O, 1/l), and sanity-check the extracted pattern against a few sample log messages before relying on it. For the C cleaner: complete the unicode-escape unescaping by scanning for `\uXXXX` sequences in the message field, converting the 4 hex digits to a codepoint, and re-encoding as UTF-8 bytes (handle basic multi-lingual plane cases; a small lookup/bit-shift routine suffices for values under 0x10000); implement forward-fill by tracking a `last_valid` float that only updates when the current row's `sensor_val` is present, otherwise reusing the tracked value — process rows in file order since fill direction matters. Output valid CSV with proper header. Then use `grep -oP` or `awk` with the OCR'd regex against the `message` column to pull matching row IDs into `anomaly_ids.txt`, one per line — cross-check a handful of matches/non-matches by eye. Finally bulk-load the CSV into SQLite with `sqlite3 telemetry.db ".mode csv" ".import cleaned.csv logs"` and confirm row counts with a `SELECT COUNT(*)`.
