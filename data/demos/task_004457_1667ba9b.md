Design the C++ program in clear pipeline stages. First, scan `/home/user/raw_configs/` with a directory-listing call, parse each filename against the pattern `server_<id>_<role>_<timestamp>.conf` (split on `_` and strip `.conf`), and group by role, keeping only the entry with the maximum numeric timestamp per role — compare timestamps as integers, not strings. For the winning file per role, read it line by line: skip blank lines and lines whose first non-whitespace char is `#`, split remaining lines on the first `=`, trim whitespace from both key and value with a helper (don't trim only one side), lowercase the key, then collect into a container you can sort alphabetically by key (e.g. `std::map<std::string,std::string>`, which sorts automatically). Load the template file's raw text and do literal substring replacement of `{{ROLE}}`, `{{TIMESTAMP}}`, `{{SERVER_ID}}`, `{{CONFIG_PAIRS}}` (joining sorted pairs as `key=value` lines with no spaces around `=`). Process roles in alphabetical order for both the report and the log, appending `[INFO] Processed <filename>: <N> valid keys found.` to `pipeline.log` for each, where filename is the original selected file's basename.