Work through the three sub-problems independently and verify each before combining. First, `ln -s /home/user/capacity-config /home/user/config` so the hardcoded path `/home/user/config/settings.conf` resolves correctly — symlink the directory itself, not just the file inside it. Second, open `/home/user/planner.c`, add the missing `#include <sys/socket.h>`, `#include <netinet/in.h>`, and `#include <arpa/inet.h>` headers required for `socket()`/`inet_pton()`/`connect()`, add the missing `return 0;` at the end of `main()`, and compile with `gcc /home/user/planner.c -o /home/user/planner`, checking for zero warnings/errors. Third, write `/home/user/start_services.sh` with `set -euo pipefail` for robust error handling, launch `python3 /home/user/net_sim.py &` in the background, then actively poll for the port instead of a fixed sleep — loop checking `nc -z 127.0.0.1 8888` (or `/dev/tcp/127.0.0.1/8888` via bash) with short sleeps until it succeeds or a timeout, since the daemon needs ~2s to bind. Only after the port is confirmed open, run `/home/user/planner > /home/user/planner_output.log`. Make the script executable with `chmod +x`, then actually execute it and inspect the resulting log file to confirm the connection message appears exactly as expected.