Build and test each pipeline stage independently before chaining them. First, write `pipeline.sh` with an `rsync -a /tmp/remote_drop/ /home/user/processing_data/` step and run it standalone to confirm files actually land in the destination. Second, write the similarity script implementing Jaccard exactly as specified: lowercase, replace non-alphanumeric characters with spaces (a regex substitution, not just stripping punctuation), split on whitespace into a `set()` of words, then `len(intersection)/len(union)` per candidate file — compute this for every file in `processing_data` and pick the max score, tracking the corresponding filename. Format the winning line as `<filename>,<score>` where score is rounded to exactly 4 decimal places (use string formatting like `%.4f`, not Python's `round()` alone, to guarantee trailing zeros are preserved) and have `pipeline.sh` redirect this script's stdout to `/home/user/latest_match.log`. Chain: `pipeline.sh` should call rsync then the analysis script in sequence. Make it executable, run it manually once to produce the log, then add a crontab entry (`*/15 * * * * /home/user/pipeline.sh`) via `crontab -e`/`crontab -l | { cat; echo ...; } | crontab -`, and dump it with `crontab -l > /home/user/crontab_dump.txt`.
