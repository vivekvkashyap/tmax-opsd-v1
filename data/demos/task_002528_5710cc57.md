Parse the PDB file using fixed-column slicing exactly as specified (X at columns 31-38, Z at 47-54, 1-indexed, so 0-indexed offsets 30-38 and 46-54) rather than splitting on whitespace, since PDB ATOM records can have columns that touch without separating spaces — use `strncpy`/manual buffer copy of the substring then `atof`/`strtod`, guarding against non-`ATOM` lines by checking the record prefix first. Stream the file line-by-line with `fgets` (don't load the whole thing unless you also handle long lines), and maintain running sum/count for domain 1 (X >= 0.0) and domain 2 (X < 0.0) rather than storing all values, to keep it fast. Compute means, then apply the two-sample Z-test formula exactly as given with fixed variance 1.0 for both domains: `Z = (mu1 - mu2) / sqrt(1/n1 + 1/n2)`. Print/write with `%.3f` and exactly the literal prefix `Z-score: `. Before trusting your C result, cross-check by writing a quick equivalent one-off in a scripting language (or by hand-computing on a small sample) that parses the same fixed columns, to catch off-by-one column indexing bugs — those are the most common failure mode here, not the statistics.
