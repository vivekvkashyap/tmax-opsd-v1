Split this into the two independent halves and verify each end-to-end yourself. For Nginx: edit `/app/nginx.conf`'s proxy block to explicitly forward the Authorization header — by default Nginx passes most headers through, but if it's being stripped, add `proxy_set_header Authorization $http_authorization;` (and ensure no `proxy_hide_header`/blank override is discarding it), then reload/restart Nginx and confirm with an actual `curl -H "Authorization: Bearer test_token" http://localhost:8080/api` that the backend on port 5000 echoes the header back correctly — don't just eyeball the config. For the filter script: a valid JWT has exactly three dot-separated base64url segments with non-empty header/payload/signature; split on `.` and check the count and that no segment is empty. Decode the first segment (base64url, remembering to pad `=` correctly and translate `-_` to `+/` if using standard base64 tools) and parse the JSON to read the `alg` field, then reject case-insensitively if it equals "none" (lowercase both sides before comparing, e.g. with `tr` or `jq`'s `ascii_downcase`). Return exit 0 only when both conditions pass (three real parts AND alg not "none"), exit 1 otherwise. Test the script against several handcrafted valid/invalid tokens before finishing.