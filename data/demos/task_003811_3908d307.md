This is a knowledge-distillation task: treat the legacy binary purely as a labeling oracle you query once to generate training targets, not something to reverse-engineer or call at inference time. Pipeline: clean the raw CSV (drop null/whitespace-only `text`), embed all texts with `sentence-transformers/all-MiniLM-L6-v2` (384-dim vectors), write those embeddings as a headerless CSV matching exactly the column count/order the binary expects, and run `/app/legacy_scorer <embeddings.csv> <scores.csv>` to obtain the teacher's target scores for your training set — this is a one-time offline step, and you get to choose/generate the training texts (use the raw_data.csv provided) since you control what you feed the oracle. Fit a simple regression model (Ridge or a small MLP) from embedding → score using scikit-learn, since the underlying function is a fixed linear-plus-sigmoid transform and should be easy to approximate well; validate on a held-out split of your own oracle-labeled data to estimate MSE before finalizing. Save the fitted model with `joblib.dump`. Write `predict.py` to independently recompute embeddings for new text and call `model.predict` — critically, it must never invoke the legacy binary, only the saved student model — outputting the exact `id,score` CSV format. Confirm your held-out MSE is comfortably below 0.01 before considering the task done, since the hidden test set will be similar in distribution.