Part 1: in `/home/user/sanitize.py`, load the JSON file, and only if `post_install_script` key exists, check its string value for each forbidden substring (`wget`, `curl`, `nc`, `/dev/tcp`, `mkfifo`) with a simple substring/regex search (case-sensitivity and exact matching against the literal strings, not just whole-word matching, since attackers may embed them anywhere) — exit 1 on any match, otherwise print the raw JSON text as-is to stdout and exit 0; test against varied benign scripts that don't happen to contain these substrings incidentally. Make the script executable with a shebang and `chmod +x`. Part 2: extract frames from `/app/boot_sequence.mp4` with `ffmpeg -i boot_sequence.mp4 frame_%04d.png` (pick a frame rate matching the video's fps so you don't over/under-sample), then for each extracted frame read the pixel at exactly `(width/2, height/2)` (e.g. with Pillow) and count frames where R>200, G<50, B<50; write the resulting count as a single integer to `/home/user/led_flashes.txt`.
