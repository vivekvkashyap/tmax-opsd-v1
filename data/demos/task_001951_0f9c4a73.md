Write `hasher.c` as a streaming passthrough: read from stdin in a loop (e.g. `fread`/`getchar` in a buffer loop) until EOF, write each chunk immediately to stdout unchanged, and maintain a running `unsigned char checksum` that you XOR with every byte read as you go — don't buffer the whole input in memory first if avoidable. After EOF, format the checksum as two uppercase hex digits (`printf("%02X %s\n", checksum, argv[1])`) and append that line to `/home/user/manifest.txt` (open in append mode). For the backup step, use `find /home/user/legacy_logs -type f -newer /home/user/last_backup.stamp` to identify only the newer files, then sort the result alphabetically (`find ... | sort`) before looping, since order matters. For each qualifying file, pipe it through `iconv -f UTF-16LE -t UTF-8` and then through your compiled `hasher` binary with the bare filename (`basename`) as its argument, appending hasher's stdout to `/home/user/archive.bin` (`>>`). Verify manually that the manifest's XOR values and the archive's concatenated UTF-8 text match what you'd expect by tracing through one file by hand before trusting the full run.
