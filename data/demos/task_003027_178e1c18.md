First recover the required port and bearer token by inspecting the image at `/app/dashboard_config.png` — use an OCR tool (e.g. `tesseract`) or view it, and read off the exact numeric port and token text; do not guess or hardcode values from elsewhere. Build the C++ server with the vendored `httplib.h`, binding to `127.0.0.1` on the discovered port, and implement `GET /api/metrics` to check the `Authorization` header equals `Bearer <recovered-token>` exactly (401 otherwise), returning the raw contents of `/home/user/metrics_output.json` with `Content-Type: application/json` when authorized. For the cron fix, diagnose why the script fails non-interactively: cron runs with a minimal PATH and a different working directory, so replace relative paths (`cd source_repo`, bare `git`, bare `metrics_output.json`) with absolute paths and, if needed, an explicit `PATH=` line or full binary paths (`$(command -v git)`). Install it with `crontab -e`/`crontab -l | { cat; echo "* * * * * ..."; } | crontab -` for `* * * * *`. Compile the server, run it in the background, manually trigger the cron script once to seed the JSON file, and verify with `curl` using the correct bearer token that you get 200 with valid JSON, and 401 with a wrong token.
