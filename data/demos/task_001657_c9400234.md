Two independent sub-problems: extract a scaling constant from video metadata, and write a streaming Go filter — get both exactly right since correctness is checked by byte-for-byte comparison against a reference implementation on the same random input. For the video, use `ffprobe` (e.g. count frames via `-count_frames` or compute from duration × frame rate) on `/app/traffic_cam.mp4` to determine the frame count precisely — don't assume a round number, derive it from the file. For the Go program: read stdin line-by-line with `bufio.Scanner`/`NewReader` (never `ioutil.ReadAll` on stdin) so it stays streaming; maintain a `map[string]bool` (or similar) of seen IDs and skip any record whose ID repeats; on JSON parse failure or a missing `id` field, print exactly `invalid record\n` to stderr and drop the line; otherwise multiply `value` by your derived scaling factor and marshal a struct whose fields are declared in alphabetical order (`Id`, `ScaledValue`, `Value` with matching lowercase JSON tags) so Go's default marshaling naturally emits keys alphabetically. Build with `go build` to the exact required binary path, then stress-test it yourself against varied streams (duplicates, malformed lines, mixed valid/invalid) before trusting it.
