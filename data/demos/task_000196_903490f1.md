Work through the Go project bottom-up: algorithm, then API, then build script, then run/verify. In `algo/algo.go`, implement `CalculateSignature` by iterating bytes with their 1-based position, accumulating `uint32(b) * uint32(index+1)` into a `uint32` (or wider) accumulator, and taking `% 1000003` at the end (also fine to mod on each addition to avoid overflow concerns, but confirm the modulus is applied to the final sum, not to each term). In `api/api.go`, implement the handler to set `Content-Type: application/json`, then `json.NewEncoder(w).Encode(s.Manifest)` (or `json.Marshal` + `w.Write`), returning implicit 200 OK. Fix `build.sh`: it currently does `go mod init wrongname` (must be `manager`, matching the internal import paths `manager/algo` and `manager/api`) and builds to the wrong output name (`server` instead of `artifact-server`) — correct both, and make sure `go build -o artifact-server .` runs from the right directory. After building, run the server in the background, `curl` `http://localhost:8080/manifest`, and save the raw JSON body to `/home/user/final_manifest.json`; verify by hand-computing the expected signature for each artifact's bytes before trusting the output.
