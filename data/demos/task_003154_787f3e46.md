Read `/home/user/incident/app.py` line by line and identify: the route that builds a SQL query via raw string interpolation of user-controlled input (an f-string or `%`/`+` concatenation feeding directly into `cursor.execute`) — that line number is the SQLi finding; and the route that interpolates user input directly into HTML/`render_template_string` without escaping — that line is the XSS finding. Use `grep -n` or just count lines in the file to get exact 1-indexed line numbers rather than estimating. For the certificate chain, don't assume ordering — parse `/home/user/incident/cert_chain.pem` with `openssl crl2pkcs7 -nocrl -certfile ... | openssl pkcs7 -print_certs` or split it into individual certs and inspect each with `openssl x509 -noout -subject -issuer`; the Root CA is the certificate whose `issuer` equals its own `subject` (self-signed) — extract its CN from that record, don't assume it's the first or last cert in the file. Write the three findings into `/home/user/investigation_summary.json` with exactly the keys `sqli_line`, `xss_line` (integers) and `root_ca_cn` (string), and leave the original artifact files untouched.