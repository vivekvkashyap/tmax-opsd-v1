Work the phases independently and verify each. Phase 1: `runner.sh`'s `for f in $(ls data/)` word-splits on spaces — replace with a glob loop (`for f in data/*; do ... "$f" ...; done`) or `find -print0`/`read -d ''`, always quoting `"$f"`. Phase 2: in `server.py`, find the manual `acquire()`/`release()` pair where the error branch returns early without releasing — this leaks the lock and hangs later clients. Fix with `with stats_lock:` (or a `finally` release) so it's always freed regardless of branch. Trace the exact path an `"ERROR_TRIGGER"` payload takes to confirm. For pcap analysis, use `tcpdump -r capture.pcap` or Python `scapy`/`dpkt` to read the last packet's TCP header and pull its destination port. Phase 3's `mre.py` should launch the server as a subprocess/thread, fire 5 concurrent threads sending `ERROR_TRIGGER`, and assert clean exit within 5s — run it repeatedly yourself to confirm no flakiness before calling it done.