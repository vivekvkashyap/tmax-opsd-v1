Tackle the two parts independently but in order. For the library: inspect `/app/libkmer-svd-1.2.0/Makefile` — a shared library build needs `-fPIC` on the compile step (add it to CFLAGS) and must link `-lm` since it does floating point math (add it to LDFLAGS or the link line), otherwise `make` either fails or produces a `.so` that crashes/errors when dlopen'd or linked into another binary. Fix the flags, run `make`, and confirm the `.so` exists and a small test program can link against it and call `kmer_matrix_condition_number`. For the filter program: write the FASTA parser to skip the header line and concatenate sequence lines; build the binary signal (G/C=1.0, A/T=0.0) directly from that; implement the O(N^2) DFT power spectrum yourself rather than guessing thresholds; compute the 4x4 dinucleotide transition matrix by counting consecutive-pair frequencies and normalizing each row to sum to 1; call into the fixed library for the condition number. Apply the ACCEPT/REJECT decision exactly as specified, then iteratively test against every file in both `/home/user/data/clean/` and `/home/user/data/evil/` until all classify correctly, debugging any misclassified file by inspecting its computed ratio and condition number.