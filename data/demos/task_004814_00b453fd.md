Build this bottom-up and verify each piece before moving on. Create the directories first, then `ln -s /home/user/capacity/logs /home/user/capacity/active_logs` (symlink target is the real dir, link name is the shortcut). For the C program, parse `/proc/loadavg` with `fscanf(fp, "%lf", &load)` (first token only), then format the timestamp with `strftime` using a format string ending in `%Z` for the timezone abbreviation and `strftime`'s `%Y-%m-%d %H:%M:%S %Z`, using `localtime()` on the current `time_t` — the abbreviation printed depends entirely on the `TZ` environment variable seen by the process at runtime, not on any code logic. Compile with `gcc /home/user/capacity/tracker.c -o /home/user/capacity/tracker`. To force Hawaii time for a single invocation without touching system config, run it as `TZ=Pacific/Honolulu /home/user/capacity/tracker <path>` — this only affects that one process's environment. Double check the output line matches the exact bracket/space format requested, including appending to the file at the symlinked path. Finally write `rotate.sh` using `mv` between the two exact paths, `chmod +x` it, but do not execute it yourself.