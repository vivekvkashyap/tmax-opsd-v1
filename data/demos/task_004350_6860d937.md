Two distinct bugs to fix in resolve.c, verify each separately. First, replace `compare_versions`'s `strcmp` with a proper semver comparison: split each version string on '.' into three integers (`sscanf(v, "%d.%d.%d", &major, &minor, &patch)`), then compare major, then minor, then patch numerically, returning negative/zero/positive accordingly — this fixes cases like "1.10.0" vs "1.2.0" where lexical comparison is wrong. Second, handle the UTF-16LE constraints.txt: rather than modifying the C parser's fscanf logic to handle wide characters (error-prone), write a `run.sh` wrapper that transcodes the file to UTF-8 first with `iconv -f UTF-16LE -t UTF-8 constraints.txt > constraints_utf8.txt` (watch for a possible BOM at the start, strip it if present), then invokes the compiled binary on `available.txt` and the transcoded file. Compile resolve.c with gcc, run it via run.sh, and confirm each constraint line parses as an operator (`>=`, `<`, `!=`, etc.) plus a version by printing intermediate parsed values before trusting the final filtered/sorted result. Write the single highest satisfying version (no extra whitespace) to /home/user/resolved_version.txt.
