Build the pipeline in verifiable stages. First confirm you can load `/app/data/field_recording.wav`, resample to 16kHz if its native rate differs (check with `soxi`/`ffprobe` first, don't assume), then extract 13 MFCCs with a 25ms window / 10ms hop — match the standard librosa convention (`n_fft` corresponding to 25ms at 16kHz, `hop_length` corresponding to 10ms) since your output is graded by MSE against a librosa-generated reference; small parameter mismatches (wrong window/hop length, wrong number of mel filters, or not resampling first) will blow the error threshold even if your code "runs fine." For transcription, use an actually installed lightweight ASR tool (check what's available, e.g. `SpeechRecognition`+CMU Sphinx or a small Whisper model) rather than hardcoding text. Compute mean/variance of each of the 13 MFCC coefficients across all frames and write the required JSON keys exactly. Only after the script works standalone should you write `setup_cron.sh` to install a `*/15 * * * *` cron entry invoking the script and redirecting output to `/home/user/output/pipeline.log` — test the script runs cleanly end-to-end manually first.