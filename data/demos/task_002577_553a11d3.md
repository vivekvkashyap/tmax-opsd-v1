Build this as a pipeline of small, independently-testable stages rather than one monolithic script. First compile `/home/user/math_lib.c` to a shared object with `gcc -shared -fPIC -o /home/user/libmath.so /home/user/math_lib.c`. Write the orchestration script (e.g. Python with `ctypes.CDLL`, or Node with `ffi-napi`) that: loads the library, sets the function's `argtypes`/`restype` correctly (int -> int) if using ctypes, iterates every `test_*.json` in `/home/user/test_data/`, calls `compute_collatz_steps(value)` for each file's `value` field, computes the destination dir `/home/user/organized_data/<steps>/`, creates it if needed, and moves (not copies) the JSON file there preserving its filename. Then write `/home/user/orchestrate.sh` that ties together: compiling the .so, creating `organized_data`, running the categorization script, and finally building `summary.csv` by counting files per subdirectory (e.g. `for d in /home/user/organized_data/*/; do echo "$(basename $d),$(ls "$d" | wc -l)"; done`) sorted numerically by the steps column (`sort -t, -k1,1n`), prefixed with the `steps,file_count` header. Make the script executable and run it — verify counts against directory listings before finishing, since an off-by-one in the Collatz loop or a wrong ctypes signature silently produces wrong bucket numbers.