In a new or existing Cargo project under `/home/user`, parse `binding_data.csv` with the `csv` crate, and for each 20-character sequence compute the GC fraction by counting `'G'`/`'C'` chars and dividing by the sequence length (use the actual length rather than assuming exactly 20, as a safety check). Implement OLS by hand: compute mean(x), mean(y) over all rows, then `slope = sum((x_i-mean_x)*(y_i-mean_y)) / sum((x_i-mean_x)^2)` and `intercept = mean_y - slope*mean_x` — this is standard single-variable linear regression, no external stats crate needed. For the normal fit, compute the sample mean of `score` directly, and the sample standard deviation using Bessel's correction (divide the sum of squared deviations by `n-1`, not `n`). Round all four output values to 4 decimal places (Rust `{:.4}` formatting or explicit rounding before serialization) and serialize to `/home/user/analysis_results.json` with `serde_json` or manual string formatting, matching the exact key names given. Run with `cargo run` from `/home/user` and check the JSON output is well-formed before finishing.
