Start by writing a small script to enumerate targets: `find /home/user/storage_pool -type f -name '*.blob' -size +1048576c`. For the binary parser, open each target file in binary mode and loop: read exactly 32 bytes (stop cleanly at EOF), unpack the header with a fixed layout — 4-byte magic, 8-byte little-endian unsigned timestamp, 4-byte little-endian unsigned payload size N, 16-byte filename field (use Python's `struct.unpack('<4sQI16s', header)`) — then read exactly N bytes of payload immediately following. Write the chunk (header+payload) to the output file unmodified only if `timestamp > 1700000000`, otherwise skip it entirely (don't just skip the payload — skip the whole chunk). Process file-to-file streaming with fixed-size reads rather than loading the whole file, so large blobs don't blow up memory. For each target, track its original size (`os.path.getsize`) and the size of the newly written compacted file, sum both across all targets, then write `/home/user/reclaimed_space.txt` as `"{total_original} {total_compacted}\n"`. Mirror the base filename into `/home/user/compacted_pool/`, ignoring the original subdirectory structure.
