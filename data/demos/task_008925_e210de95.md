Treat the concurrency bug as the core fix: multi-threaded writers hitting the same SQLite file cause "database is locked" because each thread opens its own connection and writes independently. Rewrite the pipeline as a single-writer pattern — parse the JSONL fully (or via a channel) on worker threads if you like, but funnel all INSERTs through one connection/thread wrapped in a single transaction (BEGIN/COMMIT), using parameterized `?` placeholders, not string formatting. Remember the graph is undirected: when you see `{"researcher": A, "collaborators": [B, C]}`, insert both (A,B) and (B,A) edges (or dedupe symmetric pairs before insert) so degree counts reflect true undirected connectivity. Compute degree centrality with a single SQL aggregation (e.g. `SELECT node, COUNT(DISTINCT neighbor) ...` over a normalized edges table) rather than doing it in Rust — that's the "optimized SQL aggregation pipeline" the task wants. For the top-3 output, sort by degree DESC, then name ASC as a tiebreaker, and match the exact `"N. Name - Degree"` format. Verify by running `cargo run` end-to-end and inspecting `graph.db` and `top_nodes.txt` yourself before declaring done.
