First fix and install the vendored package before writing any pipeline logic: open `/app/vendored/loc-math-parser-0.1.0/loc_math_parser/utils.py` and look for a misspelled attribute call on `datetime.datetime` used for timestamp parsing (a name that's close to but not exactly a real `datetime` method) — correct the typo, then `pip install -e /app/vendored/loc-math-parser-0.1.0` (or equivalent) so `import loc_math_parser` works from anywhere. For the pipeline script, read every `.json` file in the input dir, and before any transformation apply a strict adversarial filter to `math_text`: reject the record (write nothing) if it contains anything matching an email pattern (`\S+@\S+\.\S+`) OR any HTML/XML-like tag pattern (`<[^>]+>`) — be careful your tag/email regexes aren't so loose they also flag clean math notation, and aren't so tight they miss obfuscated variants used in the hidden evil corpus. For records that pass, call the now-fixed `parse_time` to get a datetime, format it with `.isoformat()`, tokenize `math_text` via the vendored tokenizer, and write the exact output schema (`id`, `iso_timestamp`, `tokenized`) to `<output_dir>/<id>.json`. Test explicitly against a clean and an evil sample to confirm rejection/acceptance behavior before finishing.
