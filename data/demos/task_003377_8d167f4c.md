Write a Python script that treats this as a read→normalize→dedupe→join→sort pipeline. Read the log with `encoding='utf-16'` (or utf-16-le) — a plain UTF-8 read will produce garbage or fail. Use a regex like `\[(.*?)\]\s*CONFIG_UPDATE:\s*ServerID:(\S+)\s+upgraded package '([^']+)' to version:(\S+)` with generous `\s*` around delimiters since spacing varies, and skip non-matching lines entirely. Walk lines top-to-bottom maintaining a "last valid timestamp" variable: whenever you hit `[MISSING]`, substitute in that last-seen timestamp instead of the literal string; only update the tracker when a real timestamp is parsed. Deduplicate using a set/dict keyed on the tuple (imputed_timestamp, server_id, package, version) — do this after imputation, not before, since duplicate detection depends on the filled-in timestamp. Load the CSV with the csv module or pandas, build a ServerID→IP_Address map, and drop any record whose server_id isn't a key in that map. Sort the remaining records by (timestamp, server_id, package) ascending, then write with `json.dump(..., indent=2)` using exactly the five required keys, creating the output directory first with `os.makedirs(..., exist_ok=True)`.