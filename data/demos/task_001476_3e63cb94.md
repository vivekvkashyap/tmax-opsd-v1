Start by transcribing `/app/compliance_memo.wav` (e.g. with `whisper` or another available STT tool) to recover the spoken target user ID — read the transcript output rather than assuming a value. Next inspect `/app/trading.db`: since the existing `idx_trades_user` index is described as corrupted/stale, don't trust query results until you either `DROP INDEX idx_trades_user` and `REINDEX`, or force the query planner to ignore it (e.g. `sqlite3 ... "PRAGMA ... "` or add `INDEXED BY` a fresh rebuilt index) — verify by comparing a query with and without the index. For the rolling-average condition, use a window function: `AVG(amount) OVER (PARTITION BY user_id ORDER BY trade_date ROWS BETWEEN 3 PRECEDING AND 1 PRECEDING)` and filter rows where `amount` exceeds that value, restricted to the extracted target user. Build the HTTP server in bash using `nc` in a loop (or a small embedded Python http.server invoked by bash) bound to 127.0.0.1:8080, parsing the request line to route `/target` and `/report` to the right JSON responses, and run it in the foreground so it stays up for the verifier's requests.