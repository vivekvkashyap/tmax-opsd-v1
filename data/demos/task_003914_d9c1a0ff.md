This is a black-box reimplementation task, so treat the oracle as ground truth for behavior, not something to inspect via reverse engineering shortcuts. First run `tesseract /app/proxy_rules.png stdout` (try `-l eng --psm 6` if output is garbled) to recover the routing rules and their precedence order — read the extracted text carefully rather than assuming standard proxy conventions, since the exact rule wording/order determines correctness. Implement the Go program to base64-decode stdin, then check conditions in the precedence order stated in the image (decode failure first, then each content rule down to the default), printing exactly one decision string. Before finalizing, write your own small differential test harness: generate a batch of random and edge-case inputs (empty string, invalid base64, boundary lengths, strings containing each trigger substring) and run both `/app/proxy_oracle` and your `/home/user/filter_bin` on the same inputs, diffing outputs — fix any mismatches this reveals, since the real verifier fuzzes with thousands of cases and any precedence or edge-case error (e.g. off-by-one on the length check) will be caught. Also build the `/home/user/edge_deploy/{logs,conf,bin}` directories and a relative or absolute symlink `edge_deploy/bin/filter_bin -> /home/user/filter_bin`, verified with `readlink`.