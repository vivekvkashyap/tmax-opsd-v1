Diagnose before fixing: read `/home/user/app/backend.sh` to find the exact port it binds, then compare that against the `proxy_pass` target in `/home/user/nginx/nginx.conf` — the 502 is almost certainly a port mismatch, so fix only that value rather than rewriting the config. For the supervisor, write `/home/user/scripts/supervisor.sh` as an infinite `while true; do ... done` loop that: checks the log file's size with `stat -c%s` (or `wc -c`) before each start and, if it exceeds 1024 bytes, `mv`s it to the `.old` backup (overwriting); runs the backend in the foreground with stdout/stderr redirected via `>> logfile 2>&1`; captures its exit code with `$?` immediately after it returns; and if that code is nonzero, appends the exact alert line to `/home/user/mail/alerts` before looping back to restart — get the ordering right (rotate-check happens before start, exit-code check happens right after the backend process exits, not after any other command resets `$?`). Make the script executable, create all required directories first, launch the supervisor and nginx in the background, then `curl -s http://localhost:8080` and redirect the raw output to `/home/user/success.txt` to confirm the whole chain works end-to-end.
