First recover the 16-byte signature from the video: extract frames with `ffmpeg -i /app/evidence.mp4 -vf fps=<rate> frame_%04d.png` (a few frames per second is enough) and run OCR (e.g. `tesseract`) or visually inspect frames around the point the text flashes to read the exact ASCII string shown in the terminal command — don't guess it. Read BUFFER_SIZE out of /app/backup.conf by parsing the config file at runtime (don't hardcode 12 either, though you may hardcode the signature once recovered). In the C filter, read stdin in chunks of exactly BUFFER_SIZE bytes and maintain a small sliding "carry" buffer of the last (signature_len - 1) bytes from the previous read so a scan for the signature considers content that straddles two reads — search each new chunk together with the trailing bytes of the prior chunk, not just the chunk in isolation. On finding a match, exit immediately with code 1 without flushing more output. If a chunk (or its tail) doesn't contain the signature, write out only the bytes that are now confirmed safe (i.e. hold back just the last signature_len-1 bytes as carry for the next comparison, flush the rest), and at EOF flush any remaining carry bytes untouched, then exit 0. Validate byte-for-byte fidelity on clean files with `diff`/`cmp` against originals, and test signature placements that intentionally straddle the 12-byte boundary.