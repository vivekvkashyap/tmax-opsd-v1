Compile `/home/user/seq_sim.c` twice — `gcc -O0 seq_sim.c -o /home/user/seq_strict` and `gcc -O3 -ffast-math seq_sim.c -o /home/user/seq_fast` — then actually run both and capture their four-line `A:/C:/G:/T: value` outputs to separate files; the difference is entirely empirical (driven by `-ffast-math` reassociating the reduction and changing rounding), so don't try to predict it analytically. Use `paste` to align the two outputs line-by-line, then `awk` to strip the `X: ` prefix from each field, compute `diff = $val1 - $val2` (absolute value), and track a running max across all 4 lines with an `END` block. Format the final max with `printf "%.8f\n"` and write only that single value (no extra prefix, whitespace, or trailing content beyond a possible newline) to `/home/user/max_diff.txt`. Double check your `awk` field extraction handles the `A: `/`C: ` labels correctly rather than accidentally differencing the wrong columns.
