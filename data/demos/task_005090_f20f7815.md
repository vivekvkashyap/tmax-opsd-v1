Write `/home/user/infra_agent.rs` performing the three checks in sequence, short-circuiting on any failure: read the `USER` env var (`std::env::var("USER")`) and compare exactly to `"user"`; check directory existence with `std::path::Path::new(...).is_dir()`; attempt a TCP connection with `std::net::TcpStream::connect_timeout` or `connect` to `127.0.0.1:8888` (use a reasonable timeout so the program doesn't hang forever if nothing is listening) and treat a connection error as check failure rather than panicking. Only if all three succeed, open `/home/user/health.log` in append mode and write `SYSTEM_READY\n`. Compile with `rustc infra_agent.rs -o /home/user/infra_agent` and confirm it's executable. For the systemd unit, create the directory path `/home/user/.config/systemd/user/` first (`mkdir -p`), then write `infra_agent.service` with a `[Service]` section containing `ExecStart=/home/user/infra_agent` and `Restart=always` — do not attempt to actually enable/start it via `systemctl` since systemd isn't running in this container; just create the files.