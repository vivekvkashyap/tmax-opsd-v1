Build `/home/user/run_restore.py` in clear stages and test each before moving on. (1) Parse `/home/user/user_backup.fstab` line by line, splitting on whitespace into the six fstab-style columns; for each entry check `os.path.isdir(source)` and `continue` silently (no print/log) if it's missing. (2) For existing sources, `os.makedirs(dest, exist_ok=True)` and iterate the source directory's files, creating a symlink in dest for each via `os.symlink(os.path.join(source, f), os.path.join(dest, f))` — use absolute paths for the symlink target so it resolves correctly. (3) Set up `logging.handlers.RotatingFileHandler('/home/user/restore_process.log', maxBytes=1024, backupCount=2)` attached to a logger, then `subprocess.Popen` the mock container with the current interpreter (`sys.executable`) and the list of successfully mounted dest dirs as args, capturing stdout (e.g. via `stdout=subprocess.PIPE`) and feeding each line to your rotating logger as it arrives — don't just capture all output at the end, since rotation needs to happen incrementally as the verbose subprocess runs. (4) After `time.sleep(2.0)`, send `SIGTERM` via `proc.terminate()`/`os.kill(proc.pid, signal.SIGTERM)`, then `proc.wait()` before exiting. Run it once yourself and inspect the resulting symlinks and rotated log files to confirm behavior.