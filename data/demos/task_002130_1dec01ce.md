Structure `process_releases.py` as a per-file loop with clear failure isolation. For integrity checking, use Python's `tarfile` module (e.g., open with `tarfile.open(path, 'r:gz')` inside a try/except, or call `getmembers()`/`extractall()` and catch `tarfile.ReadError`/`EOFError`/`gzip.BadGzipFile`) rather than trusting the extension — a corrupted file should be caught here and logged as `CORRUPT: <filename>`, then skipped entirely (not backed up, not added to the WAL). For valid archives, extract to a `tempfile.mkdtemp()` directory, then walk all extracted files: for ELF detection, read the first bytes of each file and check for the ELF magic (`\x7fELF`) or shell out to `file <path>` — do not trust file extensions, since one test binary is deliberately named `.bin`. Parse the architecture from the `file` output (or `readelf -h`). For `.gcode` files, scan lines for one that starts exactly with `; TIME:` and take the substring after it, stripped. Build the JSON record with both lists sorted by `name` before appending as one line to the WAL. Only after successful parsing should you copy the original `.tar.gz` (not the extracted contents) into `/home/user/archive_backup/`. Run the script once at the end to populate all three outputs from the current dropzone contents.
