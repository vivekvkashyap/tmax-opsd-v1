Parse `/home/user/api_topology.txt` line by line, building an adjacency map from `Service -> Dependency` (dependency may be empty). Implement Kahn's algorithm (or DFS post-order) for topological sort: compute in-degree/dependency counts per service, repeatedly emit services whose dependencies are already emitted, and be careful to treat a trailing-space empty dependency as "no dependency" rather than a literal empty-string node. Write the order one-per-line to `/home/user/startup_order.log`. For the memory step, invoke `valgrind --leak-check=full --log-file=/home/user/valgrind_auth.log /home/user/bin/auth_service` from inside the script and wait for it to exit. Then parse the log for the line containing "definitely lost" — extract the numeric field with something like `grep "definitely lost" file | grep -oE '[0-9,]+ bytes' | head -1 | tr -d ', bytes'` (strip commas and non-digits) and write the resulting integer to `/home/user/leak_report.txt`. Wrap all three stages in one script, `chmod +x` it, and execute it end-to-end so all three output files exist.