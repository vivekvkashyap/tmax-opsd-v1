Work through this incrementally, testing each hook in isolation before chaining them. First diagnose the pre-receive hook: it reads commit messages via `subprocess.check_output`, which returns `bytes` in Python 3, then does a substring check with a `str` literal — that comparison silently mismatches types (or raises), so decode the output (`.decode()`) before checking `"RESTORE_TEST" in commit_msg`, and make sure the exit code/stdin parsing logic actually reads all lines correctly. Test the fix directly by piping a fake `oldrev newrev refname` line into the script locally. Second, write the post-receive hook as a bash script: use `git --work-tree=... --git-dir=... checkout -f` (or `git archive`) to populate the staging dir from the pushed ref, then `cp -r` (or `rsync`) staging's contents into prod, and `mkdir -p` the logs directory — remember post-receive reads refs from stdin too, so parse that just like pre-receive. Third, write the Python rotator to shift files from highest backup number down (process app.log.2→3 before app.log.1→2 before app.log→1, i.e. iterate in descending order to avoid overwriting), then create a fresh empty app.log. Finally run the full sequence: commit with the required message substring, push, then invoke rotate.py twice around the two `echo` log-writing steps.
