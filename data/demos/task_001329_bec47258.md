Break this into three independently verifiable phases and check each before moving on. First, decode the config: use `ffmpeg` (or `opencv-python`) to extract every frame of `/app/secret_architecture.mp4`, read the pixel at (0,0) for each frame, and take its Red channel value (note OpenCV loads frames in BGR order, so red is the last channel, not the first) as an ASCII code; convert each frame's code to a character in frame order and concatenate — stop appending once you hit a null/zero byte or non-printable padding frame, then `json.loads` the result to confirm it parses (if it doesn't, you likely have an off-by-one in frame indexing or grabbed the wrong channel). Second, use the decoded `file_mapping` dict to create the target directory tree under `/home/user/clean_project/` and move each file from `/home/user/messy_files/` to its mapped relative path — don't hardcode filenames, drive this entirely from the parsed JSON. Third, launch each backend script from its new location with `subprocess.Popen(..., "--port", str(port))` for every entry in `routes`, then write a lightweight async/threaded proxy (FastAPI+httpx or `http.server`) on port 8000 that looks up the incoming path prefix in the decoded `routes` dict and forwards method, headers, and body to `127.0.0.1:<port>`; start it as a background process and confirm with a local curl to each route before finishing.