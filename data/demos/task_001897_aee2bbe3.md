Open `backend.cpp` and find the `strncpy(server_addr.sun_path, "...", ...)` call — replace only the legacy path string literal with `/home/user/migration/run/app.sock`, leaving the rest of the socket/bind/listen/accept logic untouched. Before compiling, `mkdir -p /home/user/migration/run` so the bind target directory exists. Compile with `g++ backend.cpp -o /home/user/migration/backend_server`. Write `watchdog.sh` to: check liveness with `pgrep -f /home/user/migration/backend_server`; if not running, first `rm -f` the socket path (stale sockets left from a crashed process block re-binding with "address already in use"), then launch the server with `nohup ... & disown` so it survives the watchdog script exiting. `chmod +x watchdog.sh`, run it once manually, and confirm success by checking the process is alive (`pgrep`) *and* the socket file exists as a socket type (`test -S`) — a common mistake is only checking the process, missing that bind silently failed. Finally write the crontab line (`* * * * * /home/user/migration/watchdog.sh`) into `crontab.conf` as plain text, without needing to `crontab -` install it.
