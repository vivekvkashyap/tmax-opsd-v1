Decode the video into a table first, then build the query separately. Use `ffmpeg` or OpenCV/PIL to read every frame of `/app/graph.mp4`, resize each to 1x1 (equivalent to averaging all pixels) to get one RGB triple per frame, and insert `(frame_number, R, G, B)` as `(frame, src, dst, weight)` rows into a new SQLite3 DB at `/home/user/graph.db` — frame numbering starts at 1, matching decode order. Add indexes on `src` and `frame` since the recursive query filters/joins on both. For `query.c`, use a recursive CTE starting from edges where `src = ?`, recursively joining to edges whose `src` equals the current path's `dst` AND whose `frame` is strictly greater than the last used frame (this "no time travel" ordering constraint is the crux of a *temporal* shortest path — omitting it gives a wrong, non-temporal answer), picking minimum total weight tie-broken by earliest arrival frame. Print the exact `Weight: ..., Frames: ...` or `No path` format. Test with a handful of manually-traced start/end pairs against the raw edge table before trusting the compiled binary broadly.