Parse each CSV line with `strtok` (or manual splitting) on commas, keeping every field as a string first — do not call `atof`/`atoi` directly on a token that might be empty. After tokenizing, check `strlen(token) == 0` (or that the token contains only whitespace) for the score field and `continue`/skip the row if so; this is exactly the trap the naive `atoi`-on-empty-string bug the task warns about. Only include rows where the group field, compared with `strcmp`, is exactly `"A"`. Accumulate valid scores in a dynamic array or fixed-size buffer, compute the mean, then compute sample variance with `n-1` denominator (not `n`), take `sqrt` for std dev, and build the margin of error as `1.96 * stddev / sqrt(n)`. Write the result with a single `fprintf(f, "Mean: %.2f, CI: [%.2f, %.2f]\n", mean, mean-moe, mean+moe)` matching the exact required format string. Compile with `gcc analyze.c -o analyze -lm` (math lib must come after the source file) and run it against the CSV to produce `result.txt`, then double check formatting by eye before finishing.
