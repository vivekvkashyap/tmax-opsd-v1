Treat `/app/legacy_waf_oracle` purely as a black-box oracle to validate against, not something to disassemble or copy from — implement `calculate_risk_score` in `waf.c` directly from the stated state-machine spec (vowel doubles state mod 10, digit adds its value mod 10, other chars increment state mod 10, then accumulate `(int)c * state` per character), then run the oracle binary on a handful of test strings and compare its printed score to your C function's output to confirm you matched the spec exactly (off-by-one errors in when the state update happens relative to the score accumulation are the classic mistake here — update state, then add to score, in that order). Compile `waf.c` as a shared object with `gcc -shared -fPIC -o /home/user/libwaf.so waf.c`. Write the exact `.proto` service/message shapes specified, run `python -m grpc_tools.protoc` to generate stubs, then implement `server.py` using `ctypes.CDLL` to load `libwaf.so` and call the C function per request, wiring it into the generated gRPC servicer listening insecurely on `127.0.0.1:50051`. Start it backgrounded and do a quick manual gRPC call to confirm it's live before finishing.