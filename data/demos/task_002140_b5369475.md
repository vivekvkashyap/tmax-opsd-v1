Set up the Rust project with `cargo new temp_analyzer`, then add a crate that provides the Student's t-distribution quantile function (e.g. `statrs`) to `Cargo.toml` since you cannot hardcode t-critical values for arbitrary sample sizes. Read the log file line by line, split each line on whitespace, and attempt to parse each whitespace-delimited token as `f64` using `str::parse::<f64>()` — this naturally skips tokens like "N/A", "err", or alphanumeric mixes like "Sensor-2" or "temp_val=" that fail to parse cleanly (do not try to strip trailing colons or extract embedded digits from words; only pure standalone float tokens count). Once you have the vector of valid floats, compute mean and sample standard deviation (dividing by n-1, not n), then use the t-distribution with df = n-1 at 95% confidence to get the critical value and compute the margin of error (t_crit * std_dev/sqrt(n)). Round every output value to exactly 2 decimal places before serializing to JSON with keys "mean", "std_dev", "ci_lower", "ci_upper". Build and run the binary so the JSON file is actually written to disk, not just printed.