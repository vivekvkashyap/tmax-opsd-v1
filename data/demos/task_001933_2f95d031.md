Build this as a single-pass Python script that streams `/home/user/raw_events.jsonl` line by line rather than loading everything blindly. For each line: parse JSON, check all 4 required keys are present (else increment a missing-fields counter and drop), then normalize `action` by checking substrings in this priority order — "view" in lowercase, "click", then "purchase" or "buy" — and if none match, increment an unknown-action counter and drop. Only after a record survives both filters should you check it against a `seen` set keyed on (user_id, item_id, normalized_action, timestamp) to count and drop duplicates — duplicates counted at this stage, not before normalization. Keep the surviving deduplicated records grouped by category in lists. For stratified sampling, sort each category's list by (timestamp, user_id) ascending, then slice the first 100 (or fewer if the category is smaller) — do this deterministically, not with random.sample. Write the sampled records (normalized action value) to the output JSONL, then compute and write the pipeline_run.json log with the four counters plus final per-category counts derived from what you actually wrote. Double check total lines read equals sum of dropped + duplicates + kept-before-sampling, as a sanity check.
