Set up the venv and install numpy/biopython/networkx/scikit-learn first, then look at why the current implementation breaks: it inverts `X.T @ X` directly, which is singular because one feature column is an exact linear multiple of another, making the matrix non-invertible (or numerically unstable). The fix is to replace that manual normal-equations inversion with `sklearn.linear_model.Ridge(alpha=1.0, fit_intercept=False)` fit on the same X and target vector — Ridge's L2 penalty regularizes the singular direction without needing to change the feature matrix itself. Keep `fit_intercept=False` since the bias column is already explicit in X. After fitting, pull coefficients from `.coef_` and write each to its own line formatted to 4 decimal places, preserving the script's existing I/O structure and column order. Run the script end-to-end afterward and read back /home/user/results/coefficients.txt to confirm exactly three well-formed numeric lines are produced, rather than assuming the edit compiled cleanly.
