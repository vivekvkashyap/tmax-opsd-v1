Work top-down from the 502: the proxy target (port 9000) is down because /app/data_service-1.2.0/disk_monitor.py raises an exception during startup. Read that file carefully — it's checking free disk space against a threshold that uses the wrong power of 1024 (e.g. comparing against terabytes instead of megabytes), so a normal /tmp free-space value always fails the check; fix the exponent/multiplier so a realistic free-space value passes for a genuine 500MB requirement. Next, since bin/start_service.sh is an interactive script that blocks on a y/n prompt, don't try to pipe "y" via simple redirection — write a pexpect (or expect) driver at /home/user/start_backend.py that spawns the script, expects the exact prompt text, sends "y\n", and then detaches/backgrounds the child process (e.g. via nohup-style disowning or pexpect's spawn with logfile redirected) so it keeps running after your automation script exits. Verify manually with `curl http://127.0.0.1:8080/api/data` that you get 200 and the expected JSON before writing verify_deployment.py, which should send 50 sequential requests, catch connection errors gracefully (try/except around each request, counting failures as non-200), and write the success ratio as a single float to /home/user/success_rate.txt. Do not touch the Nginx config.
