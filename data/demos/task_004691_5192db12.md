Write the script to read `/home/user/build_manifest.bld` line by line (e.g. `while IFS='|' read -r lang name sha; do ... done < file`, skipping blank lines), then for each dependency use `curl` to fetch `http://127.0.0.1:8080/deps/<name>`, capturing both the body and the HTTP status code (`curl -s -o tmpfile -w '%{http_code}' ...`). Respect the server's 1-request-per-second rate limit by sleeping between requests (e.g. `sleep 1` before/after each curl, or retry-with-backoff on a 429 response) rather than firing requests back-to-back. On HTTP 200, compute `sha256sum tmpfile` and compare it against the manifest's expected hash string; on match, `mkdir -p /home/user/build_cache` and move the file there plus append `SUCCESS: <name>` to `/home/user/build.log`; on mismatch, discard the temp file and append `CHECKSUM_FAILED: <name>`. On HTTP 404, append `NOT_FOUND: <name>` and don't create a cache file. Make sure log lines are appended in manifest order and the script processes every entry even after failures. Finish with `chmod +x /home/user/fetch_deps.sh` and test it end-to-end against the running server.
