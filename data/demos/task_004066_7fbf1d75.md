Build and validate the two halves independently before chaining them. For the server, use the `websockets` Python library (`pip install websockets`) with an `async def handler(websocket)` bound to `localhost:9090`; inside, receive the text message, split on whitespace, and evaluate it as prefix notation with a recursive or stack-based parser that consumes tokens left-to-right (recursive descent: read a token, if it's an operator recursively evaluate two operands and combine, if it's a number return it) — a naive reversed-postfix approach will silently break on nested prefix expressions like `- * + 5 10 2 8`. Format the result with `f"{result:.1f}"` before sending it back. Start the server as a background process (e.g. `nohup python3 ws_mock.py &` or `&` plus disown) and give it a moment to bind before connecting. For the client, use the `websockets` client API to connect, send the exact expression, `await` the single response, and write it verbatim to `/home/user/ws_output.txt` (no trailing newline surprises — check what the verifier expects). Manually recompute the prefix expression by hand first (apply each operator to the next two evaluated operands, working outward from the innermost) so you know what correct output should look like before you run anything, then confirm the file content matches your independently-derived expectation.