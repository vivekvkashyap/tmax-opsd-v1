Break this into the deployment fix, then the detector, then the cron job, testing each stage before moving on. For Nginx: add a `location /api/ { proxy_pass http://127.0.0.1:5000/; }` block (or equivalent) to `/home/user/nginx/nginx.conf`, being careful about trailing slashes on `proxy_pass` since that affects path rewriting; repoint the `current` symlink with `ln -sfn /home/user/deploy/v2 /home/user/deploy/current`; add `export REDIS_HOST=127.0.0.1` before the app-start line in `start.sh`. Start Redis, then the Flask app via the fixed start script, then Nginx, and verify with `curl http://127.0.0.1:8080/api/health` that you get the exact expected JSON before moving on — debug each service's logs individually if it fails rather than guessing. For the WAF detector, look at `/home/user/corpora_sample/` to see realistic payload shapes, then implement case-insensitive substring/pattern checks for path traversal, SQL injection, and XSS signatures, printing exactly `EVIL` or `CLEAN`; test it against the sample corpus yourself since the hidden corpus will be similar in style. For monitoring, write the awk/grep filter for 403/500 status codes from the nginx log format actually in use, then register the cron entry via `crontab -e` (or `crontab -l | { cat; echo "..."; } | crontab -`) with the correct schedule syntax and output redirection.