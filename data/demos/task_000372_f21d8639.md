Start by extracting the spoken instructions from the audio file — use an available speech-to-text tool (e.g. whisper) or inspect/convert the file with ffmpeg-based utilities to get a transcript; treat whatever it says about the corrupted index name and the adversarial graph rule as the actual parameters to use, don't guess these. Then: (1) open the SQLite DB and drop the specific index the audio names — list existing indexes first (e.g. via `sqlite3 ... ".indexes"` or querying sqlite_master) to confirm the exact name before dropping it. (2) Write a SQL window-function query using `SUM(amount) OVER (PARTITION BY sender ORDER BY timestamp)` for the cumulative sum, and export sender/timestamp/cumulative_amount to CSV, being careful about tie-breaking order when multiple transactions share a timestamp. (3) For the classifier, build a directed graph from the source/target CSV columns (e.g. with networkx), compute each node's in-degree and out-degree, and apply exactly the threshold/condition given in the transcript (a specific out-degree cutoff combined with an in-degree condition) to decide EVIL vs CLEAN — handle CSVs robustly (headers, blank lines, isolated nodes with zero degree). Test your classifier against both provided example corpora before finishing.
