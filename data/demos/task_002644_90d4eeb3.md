Implement this as a straightforward, faithful transcription of the spec into code rather than trying to shortcut the math — the verifier recomputes the same simulation from scratch and does an exact string match on the rounded result, so any deviation in order of operations, RNG call sequence, or FFT convention will fail. Key points: call `np.random.seed(42)` exactly once before the trial loop (not inside it); draw one `A ~ Normal(2.0, 0.5)` per trial via `np.random.normal(2.0, 0.5)` in trial order; define the ODE as a function of `(t, y)` returning `[v, -0.1*v - x - x**3 + A*np.cos(1.2*t)]`; call `solve_ivp` with `method='RK45'`, `t_eval=np.linspace(0, 50, 1000)`; compute `power = np.abs(np.fft.fft(x))**2` per trial and average across all 100 trials elementwise; get frequencies with `np.fft.fftfreq(1000, d=50/999)`, restrict to strictly positive frequencies, take `argmax` of the averaged power there, and write the frequency rounded to 3 decimals (e.g. via Python's `round` or f-string `.3f`) to `/home/user/dominant_freq.txt` with no extra whitespace/newline surprises.