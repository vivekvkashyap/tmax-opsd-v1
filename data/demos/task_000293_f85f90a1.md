Fix the vendored toolkit before building your pipeline around it — don't work around its bugs in your own script. Inspect `/app/awk-ts-tools-1.0/Makefile` or any wrapper for an environment variable pointing at a nonexistent awk binary and correct it to the system's actual `awk`/`gawk` path. In `interpolate.awk`, find the linear-interpolation slope calculation and check the arithmetic: a correct linear interpolation slope between two known points is `(y2 - y1) / (x2 - x1)` — if you see `+` where subtraction of the y-values belongs, that's the bug to fix, and verify afterward by feeding it a small hand-computed example. For the pipeline script: use `iconv -f ISO-8859-1 -t UTF-8` to convert each raw CSV, determine min/max timestamps across all files, generate the full grid of 60-second-aligned timestamps between them, merge in existing readings, then pipe the merged/sorted stream through the fixed `awk -f interpolate.awk` to fill gaps, and append the sorted, gap-filled result to `/home/user/data/processed/master.csv`. Finally add the cron entry with `(crontab -l 2>/dev/null; echo "*/15 * * * * /home/user/pipeline.sh") | crontab -u user -`. Validate your interpolated values look smooth/reasonable before finishing, since grading is by MSE against a hidden reference.
