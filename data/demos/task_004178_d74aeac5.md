Build the RPN evaluator as a small C program: split the input string on spaces, maintain an integer stack, push operands, and on each operator pop two, apply it (careful with subtraction/division operand order — the earlier-pushed value is the left operand), push the result, then print the final stack value with a trailing newline; compile with `gcc /home/user/eval.c -o /home/user/eval`. For the ordering bug in test.sh, do not remove the two backgrounded subshells that compute weights concurrently — instead make each subshell write its own line to a private temp file (or use named pipes) so their filesystem writes never race, then after `wait` returns, deterministically concatenate backend1's output before backend2's output into `/home/user/proxy.conf` in a final synchronous step. This preserves concurrency in the computation while removing non-determinism in the write order. After editing test.sh, run it once and inspect the resulting proxy.conf to confirm the two server lines appear in the required order with correctly computed weight values.