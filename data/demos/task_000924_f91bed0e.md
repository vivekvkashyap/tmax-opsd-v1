Part 1 (video): use ffmpeg to seek precisely to 2.5s and extract exactly one frame, converting to 8-bit grayscale raw at 640x480 — e.g. `ffmpeg -ss 2.5 -i /app/experiment.mp4 -vframes 1 -vf scale=640:480,format=gray -pix_fmt gray -f rawvideo /home/user/reference_frame.raw`. Verify the output is exactly 640*480 = 307200 bytes with `stat -c%s`; if size is off, check that `-pix_fmt gray` and scale filter are both applied (not just one). Part 2 (sanitizer): write `/home/user/sanitizer.cpp` to read the whole file as bytes, then validate strictly: reject if total size < 16; reject if bytes[0:8] != "SENSORDT" (compare exactly, no null-terminator assumptions); read Payload Size as a little-endian uint32 at offset 12 (be careful with byte order — read individual bytes and shift, don't rely on struct alignment/endianness of the host); reject unless `file_size - 16 == payload_size` exactly (covers both truncation and padding). Exit 0 only if all checks pass, else exit 1. Compile with `g++ -O2 -o /home/user/sanitizer sanitizer.cpp`, then self-test against a few hand-crafted clean/corrupted files before trusting it — the real corpus paths are hidden, so your own edge-case files (empty, 15 bytes, wrong magic, truncated payload, padded payload) are how you validate the logic.