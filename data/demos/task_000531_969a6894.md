Do this as a shell pipeline, not a script in another language. First sort both `/home/user/models.csv` and `/home/user/evals.csv` on `model_id` (excluding headers) and use `join -t, -1 1 -2 1` to combine them on that key, producing rows with model_type and the accuracy/loss columns. Then filter with `awk -F,` using numeric regex checks (e.g. match fields against `^-?[0-9]+([.][0-9]+)?$` before treating them as numbers) so non-numeric strings like "err" or "NaN" are dropped, and additionally enforce `0.0 <= accuracy <= 1.0` and `loss > 0.0` inside the same awk filter — this is the step naive attempts skip, letting invalid rows corrupt aggregates. Compute `efficiency_score = accuracy/loss` per valid row, then use `awk` associative arrays keyed by model_type to accumulate a running sum and count, and at END compute mean and print `model_type,count,mean` with `printf "%.4f"` for the mean. Finally pipe through `sort -t, -k3 -rn` for descending mean_efficiency order and write the header plus sorted rows to `/home/user/report.csv`, dropping any model_type with zero valid runs.
