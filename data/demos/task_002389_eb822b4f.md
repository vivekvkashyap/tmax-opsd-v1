First recover the clamping ranges from the whiteboard image by running OCR against `/app/validation_specs.png` (e.g. `tesseract /app/validation_specs.png -` or via `pytesseract.image_to_string`), then read off the min/max for each of Alpha, Beta, and Gamma from the extracted text — don't guess at plausible-looking numbers, actually parse what OCR returns and sanity-check it. In `etl_pipeline.py`, load the CSV with pandas, coerce each of the three columns to numeric with `pd.to_numeric(..., errors='coerce')` so unparseable strings become NaN — if a column ends up with NaNs where original raw cells weren't blank (i.e. a genuinely unparseable value existed), or any value's absolute magnitude exceeds 10x that column's OCR'd max-bound magnitude, treat the file as adversarial and `sys.exit(1)` without writing output. Otherwise fill remaining missing values with `df[col].median()`, clamp with `df[col].clip(lower, upper)` using the OCR'd bounds, write the CSV to `--output`, and `sys.exit(0)`. Test both corpora explicitly against your script before considering it done, since the accept/reject boundary conditions are easy to get subtly wrong.