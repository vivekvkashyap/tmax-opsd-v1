OCR `/app/dashboard_spec.png` first (`tesseract`) to recover the exact fstab tmpfs line and the correct upstream socket path — transcribe both verbatim into `/app/fstab_entry.txt` and into the C source, don't paraphrase. In `exporter.c`, two separate fixes are needed: update the `SOCKET_PATH` macro/string to the socket path read from the image, and separately hunt for a performance bottleneck unrelated to the socket fix — scan the per-line processing loop for anything that burns CPU without doing useful work (e.g. a tight busy-wait loop with a volatile counter) and delete it; a correct fix should make per-line processing effectively free. Recompile with plain `gcc`. For `pipeline.sh`, chain `grep -v ' 404 '` (or `awk`) over `/app/access.log` piped directly into `/app/exporter` — avoid intermediate temp files or extra subprocesses since the whole pipeline must run in under 0.5s over 500k lines, so the bottleneck removal in the C program is what actually makes the deadline achievable, not clever shell tricks. Time it yourself with `time bash /app/pipeline.sh` before considering it done, and don't try to modify `/etc/fstab` directly since you lack root — just write the recovered line to the requested text file.
