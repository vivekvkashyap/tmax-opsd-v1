Set up infrastructure first: a background Python HTTP server on port 8080 in `start_api.sh`, then an SSH tunnel forwarding local port 9090 to localhost:8080 (e.g. `ssh -L 9090:localhost:8080 localhost -N -f`, assuming local sshd access) so the operator talks to 9090. For the WAV parsing in `operator.cpp`, don't assume a fixed header offset — properly parse the RIFF/WAVE header to find the `data` chunk's actual byte offset and size (chunk sizes/positions can vary with extra metadata chunks), then read 16-bit signed PCM samples from there. Process in chunks of exactly 4410 samples, computing RMS as `sqrt(mean(sample_i^2))` using double precision throughout (accumulate the sum of squares as double, not int, to avoid overflow/precision loss) — this precision matters directly since the grader checks MSE against a reference RMS calculation at a tight 0.1 threshold. Compute replicas as `max(1, (int)(RMS/500))`, and append each chunk's `Chunk_ID,RMS,Replicas` line with full float precision (not truncated) to scaling_plan.txt. Compile to `/home/user/operator_bin`. In `deploy.sh`, run the binary, then loop over the output lines building a JSON payload and POSTing it via curl to `http://localhost:9090/scale`. Test the RMS numbers against a quick numpy/scipy reference calculation yourself before considering the pipeline correct.