Use a regex like `(\d+(?:\.\d+)?)\s*/\s*(\d+(?:\.\d+)?)` (via Python `re.search`, taking the first match per comment) to pull the `X` and `Y` rating fraction out of free text, rather than trying to hand-parse words like "Rating:" or "stars" — those are just surrounding noise the regex should ignore. After extracting, apply the validation rules precisely: no match found -> drop; `Y == 0` -> drop (avoid a ZeroDivisionError by checking before dividing); compute `score = X / Y` and drop if it's strictly `> 1.0` or strictly `< 0.0` (so exactly `0.0` and exactly `1.0` are valid boundaries). For surviving rows, group by `category` and compute both the mean of `score` and the count of valid rows per group — pandas `groupby('category')['score'].agg(['mean','count'])` is a clean way to do this. Round the mean to 4 decimal places only at the very end when building the output dict, and write it as `{category: {"mean_score": ..., "valid_count": ...}}` to `/home/user/summary.json` with `json.dump`.
