Work through each artifact with the right tool rather than eyeballing: for `http_traffic.log`, search for the literal `' OR 1=1` substring to locate the malicious request block, then read the `Cookie:` header from that same request (not the first request in the file) and extract the `session_id=` value. For `shadow.bak`, isolate the `admin` line's hash field, then crack it by iterating candidate passwords from `wordlist.txt` through Python's `crypt.crypt(candidate, existing_hash)` (reusing the salt embedded in the stored hash) and comparing for equality — don't try to reverse the hash directly. For `server.pem`, use `openssl x509 -in server.pem -noout -issuer` (or `cryptography`/`pyOpenSSL`) to pull the Issuer CN field rather than regexing the PEM text. Assemble the three findings into the exact JSON keys requested, write the file, then lock it down with `os.chmod(path, 0o400)` (or `chmod 400`) as the very last step — setting permissions before you've finished writing/verifying the file will block subsequent edits.
