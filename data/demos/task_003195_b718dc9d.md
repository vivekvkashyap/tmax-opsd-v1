Load each `rank_N.csv` (N=1,2,4,8) and compute the mean latency per rank count first — print/inspect these four means to sanity-check they decrease with N before fitting anything. Fit Amdahl's Law `T(N) = T(1) * ((1-P) + P/N)` to the (N, mean) pairs using `scipy.optimize.curve_fit` (or a similar least-squares approach) with T(1) fixed to the observed N=1 mean and P as the free parameter to estimate — don't solve it algebraically from just two points, use all four data points for a proper fit. For the distributional shift, compute the 1D Wasserstein distance between the *raw* (not aggregated) latency arrays for N=1 and N=8 using `scipy.stats.wasserstein_distance`. Write both numbers to `/home/user/results.json` under exactly the keys `parallel_fraction` and `wasserstein_distance` as floats, then run the script and check the output looks like a parallelizable fraction between 0 and 1 and a Wasserstein distance on the same scale as the latency values before finishing.
