Start by inspecting the SQLite schema directly rather than guessing column names: run `sqlite3 /home/user/users.db ".schema users"` to find the boolean/integer column that marks active users, then query `SELECT user_id FROM users WHERE <active_col> = 1` (or truthy) to get the set of active user IDs — this is the reverse-engineering step the task calls for. In your Python script, load that active-ID set, then stream-parse `events.jsonl` line by line with `json.loads`, keeping only events whose `user_id` is in the active set. Group events by `user_id`, sort each group's events by `timestamp` (parse ISO-8601 timestamps, e.g. with `datetime.fromisoformat` after stripping/handling the `Z` suffix, or plain string sort since the format is lexically sortable), then slide a window of size 2 over the sorted `points` values: for each position i, rolling sum = points[i] + points[i-1] if i>0 else points[i]; track the maximum such rolling sum per user. Output `{"<user_id>": <max_rolling_int>}` as JSON to `/home/user/max_rolling.json`, keyed by string and valued by integer — watch for negative `points` values, which are valid and must be included in the sum, not filtered out.
