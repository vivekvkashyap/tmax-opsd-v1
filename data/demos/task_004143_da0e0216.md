This is an aggregate-then-sort-then-format problem in C — the main pitfalls are string handling (dynamic node names) and getting the exact output formatting/sort order byte-for-byte. Steps: (1) parse `argv[1]`/`argv[2]` as input CSV and output path; (2) read the CSV line by line, skip the header, split each line on commas to get source, target, and an integer weight; (3) maintain a small dynamic structure (array or simple hash) keyed by the (source, target) string pair, accumulating summed `data_transferred` per unique directed edge — use `strcmp`-based lookup/insert since node counts are small, no need for a real hash table; (4) after reading all rows, sort the aggregated edges with `qsort` using a comparator that orders first by `source_node` (strcmp) then by `target_node` (strcmp) — implement this as one comparator function, not two separate sorts; (5) for each sorted edge, `fprintf` the exact Cypher line format given in the spec (`MERGE (a:Node {id: 'X'}) MERGE (b:Node {id: 'Y'}) MERGE (a)-[r:TRANSFERRED {total_data: W}]->(b);`) with no extra whitespace or trailing differences. Compile with gcc, run against the given CSV, and diff your output against your own hand-computed expected sums before trusting it.