Treat this as an ETL-then-graph problem: first project the relational schema into a directed graph, then run shortest-path search on it. Connect to `/home/user/audit.db` with `sqlite3`, build a `networkx.DiGraph`, and add edges in two passes: (a) for each employee, join `employee_roles` to `role_access` on `role_id` to find every system their role(s) grant access to, and add an edge from the employee's name to each such system's name; (b) for each row in `system_connections`, add an edge from the source system's name to the target system's name (look up names via the `systems` table, don't use raw ids as node labels). Make sure you resolve ids to `name` values consistently as node identifiers on both sides of every edge. Then call `nx.shortest_path(G, source="Eve", target="Main_Ledger")` (unweighted BFS shortest path, since no weights are specified) to get the node list, and join the names with commas exactly (no spaces) into `/home/user/vulnerability_path.txt`. Double check there could be multiple paths through the graph — trust the algorithm's shortest-path result rather than eyeballing the schema, since indirect multi-hop routes may be shorter than they look.
