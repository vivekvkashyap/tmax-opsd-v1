Use `h5dump -d /Position <file>` and `h5dump -d /GC_content <file>` to dump each dataset, then extract just the numeric values from the `DATA { ... }` block with `sed`/`awk` (strip braces, commas, and the `(0):` index prefixes), producing whitespace-separated number lists you can feed into `awk`. Compute all the sums (N, ΣX, ΣY, ΣXY, ΣX²) in one `awk` pass over paired X/Y values, then apply the slope and intercept formulas either inside `awk` (which does floating point natively) or via `bc -l`, and format both to 2 decimal places with `printf "%.2f"`. Write the two lines to the results file in the exact required format (`Slope: ...` / `Intercept: ...`), then compare it against the baseline file with `diff -q` or `cmp`: on match write `PASS`, otherwise `FAIL`, to the log file. Keep the whole script POSIX/awk/bc — no Python/R/Perl anywhere in the math or HDF5 extraction, and remember to `chmod +x` the script and actually execute it against the given HDF5 path so the output files exist afterward.
