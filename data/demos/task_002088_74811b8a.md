Pipeline this in clear stages, validating each output before moving to the next. Use `ffmpeg -i /app/data/experiment.mp4 <frame>.png` (or a raw-pixel pipe) to extract frames, then for each frame compute mean R, G, B by averaging pixel channel values (a quick Python or ffmpeg `signalstats` pass works, or write it directly in your C++ tool). Join on `frame_id` with `/app/data/sensor_a.csv` and `/app/data/sensor_b.csv`, producing a fixed 23-column row per frame ordered exactly R, G, B, sensor_a's 10 columns (original order), sensor_b's 10 columns (original order) — get this column order right since downstream PCA is order-sensitive only insofar as reconstruction must map back consistently. In the C++ step, standardize each of the 23 columns (subtract column mean, divide by column stddev) before PCA — skipping standardization will bias variance-dominant columns; use Eigen's `SelfAdjointEigenSolver` on the covariance matrix (or `JacobiSVD`) to get the top-2 eigenvectors, project each row to get PC1/PC2, then reconstruct via `PC-loadings * eigenvectors^T` and compute per-row MSE against the standardized original as the reconstruction error. Finally expose `GET /api/pca/<frame_id>` on `0.0.0.0:9090` returning exactly the four specified JSON fields — a lightweight Python HTTP wrapper reading your C++ tool's output file is simplest. Keep the server running and test with `curl` before finishing; remember PCA sign is not uniquely determined, so don't worry about matching a specific sign.
