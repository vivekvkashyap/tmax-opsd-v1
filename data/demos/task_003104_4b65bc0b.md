The exploit signature must be recovered from the image, not assumed — use OCR (e.g. `tesseract /app/malware_note.png`) to read the sticky-note text, which describes x86 assembly instructions (things like zeroing a register, then pushing values onto the stack). Manually assemble what you read into the correct machine-code byte sequence (each instruction has a well-known short encoding you can look up/derive — e.g. xor of a register with itself, a single-byte push of that register, and a push of a 4-byte immediate) rather than guessing bytes; verify your assembled sequence's length and structure make sense before hardcoding it into the proxy.

For the Go proxy: use `net/http` with a `ReverseProxy` (or manual `httputil.NewSingleHostReverseProxy` pointed at `http://127.0.0.1:9000`), but intercept in a handler that reads the full request body into memory (`io.ReadAll`) for POST requests, checks `bytes.Contains(body, maliciousSequence)`, and if found writes `403` directly without forwarding — otherwise re-wrap the body (`io.NopCloser(bytes.NewReader(body))`) before passing to the proxy so the backend still receives it intact. Compile with `go build -o /home/user/proxy /home/user/proxy.go` and test manually against a few crafted clean/malicious payloads with `curl` before trusting the full corpus run.