Derive the exact rules from the recovered spec rather than guessing: OCR the spec image (`tesseract /app/spec_sheet.png -`) carefully and re-check ambiguous characters (digits, operators) since a transcription slip will cascade into every row's output. Build the bash pipeline in clear stages: a filter step using a strict regex (three comma-separated fields, first two purely numeric, third numeric-or-empty, rejecting any embedded literal newline/CR or stray characters) with awk or grep -P, then apply the numeric masking and imputation formulas from the spec per valid row, then format with the exact template string recovered from the image (mind spacing, punctuation, and brackets — a fuzz test checking bit-exact stdout will fail on any whitespace mismatch). Test edge cases explicitly with hand-crafted inputs: malformed rows, missing TempB, embedded control characters, boundary values for the masking formula, before finalizing. Make the script executable and reason through several example rows by hand to confirm your formulas and template match the spec exactly, since it will be fuzzed at scale for bit-exact equivalence.