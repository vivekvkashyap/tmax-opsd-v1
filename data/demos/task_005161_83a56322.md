Write the C encoder to operate purely on raw bytes: open `/app/carrier.wav`, copy the first 44 bytes (standard WAV header) unchanged, then for each subsequent 16-bit little-endian sample, read it as a signed/unsigned short, clear only bit 0, and OR in the next payload bit if there are still payload bits left — leave every sample after the payload is fully embedded completely untouched (copy byte-for-byte) so PSNR stays high, since modifying more samples than necessary directly hurts the PSNR score. The payload is the literal ASCII string `AUTH_TOKEN_9942_EXFIL` plus its terminating NUL byte, encoded one byte at a time from LSB (bit 0) to MSB (bit 7) — get the bit order and per-sample LSB-clear-then-set logic exactly right, since the verifier decodes by reading bit 0 of each 16-bit sample and reassembling bytes, and any bit-order mismatch means the token won't decode at all (score becomes 0 regardless of PSNR). Write the full modified buffer to `/home/user/payload.wav`, preserving file length and all non-header, non-payload data identically to the source. Before finishing, mentally trace or script-check that the number of samples you modify equals exactly `8 * (len(token)+1)`, and that decoding your own output byte-by-byte reproduces the token.