OCR `/app/distribution_spec.png` with `tesseract` and parse the six labeled numbers (two weights, two means, two std-devs) — sanity-check them (weights near summing to 1) before hardcoding into C, since OCR can misread digits/signs. In `gmm_sampler.c`, sample each point by first picking a component via the weights (uniform draw vs. cumulative weight), then draw a Gaussian for it with Box-Muller. For the KS statistic against `/app/baseline_samples.txt`, sort both sample arrays and do a proper two-sample KS scan (merge the sorted arrays, track each empirical CDF, take max absolute difference) rather than a histogram approximation. Accept `n` as argv and print the KS stat plus samples to stdout in a format easy to parse. For the API, use Flask/FastAPI, check the `Authorization: Bearer ML-DATA-2024` header before any work (401 immediately if missing/wrong), shell out to the compiled binary, parse its stdout, return the specified JSON shape, and launch the server detached (e.g. `nohup ... &`) so it stays up for verification.
