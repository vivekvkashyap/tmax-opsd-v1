Since you have no source for `/home/user/app/bin/parser`, reverse-engineer it empirically: run `xxd`/`hexdump` on `/home/user/app/data/raw.dat` to see its byte layout (look for a fixed-size magic/header, then a length field, then a payload), and compare against `strings` or partial output from running the parser on it to see where it goes wrong. The described symptom — truncated/garbled JSON output from a string payload — combined with a binary format is a strong signal that the payload bytes are encoded in something other than plain ASCII/UTF-8 (e.g. a wide encoding like UTF-16), which embeds null bytes that terminate C strings early or scrambles ASCII spacing; use `file`, `iconv -l`, or trial re-encodes to identify the exact source encoding by diffing candidate re-encodings' hexdumps against the raw payload bytes. Once identified, write a small script (Python/`iconv`) that reads the header/length exactly as-is, re-encodes only the string payload to a clean encoding the parser handles correctly, recomputes/rewrites the length field to match the new byte count, and writes `/home/user/app/data/fixed.dat` with the same magic header. Update the Makefile's test target to point at `data/fixed.dat`, confirm `make test` passes, then record the magic bytes (as lowercase hex, no `0x`) and the exact source encoding name you diagnosed into `/home/user/app/resolution.json`.
