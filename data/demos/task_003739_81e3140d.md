Work in stages and validate each before moving on. First, run `tesseract /app/spec.png` (or convert to a clean bitmap first if OCR is noisy) and manually inspect the extracted text to pull out the hidden-layer sizes and the target mean/std for the synthetic feature — do not guess these values. Second, write the data-generation script exactly as specified: `np.random.seed(42)`, first 20 features via `np.random.rand(1000,20)`, 21st feature via `np.random.normal(loc=<ocr_mean>, scale=<ocr_std>, size=(1000,1))`, concatenated into a (1000,21) float32 array — get the seed placement and distribution calls exactly right since any deviation changes every downstream number. Third, build the PyTorch MLP with `torch.manual_seed(42)` called before instantiating the layers (Linear->ReLU pairs per the OCR'd hidden sizes, ending Linear->10 with no activation), and capture the activation of the last ReLU (the layer just before the final output) as the embedding for cosine similarity. Finally, implement `/recommend` (cosine similarity over those embeddings, top-5 excluding self) and `/stats` (`scipy.stats.ttest_1samp` and `t.interval` on feature 20 against popmean 0.0) as a FastAPI/Flask app bound to 127.0.0.1:8000, and leave the server running.