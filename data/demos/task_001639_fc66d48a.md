Build and verify this pipeline stage by stage rather than all at once, since later stages depend on exact output from earlier ones. First, filter `/home/user/billing_raw.csv` with awk/grep using `-F,` field comparisons: keep rows where field 4 equals `ACTIVE` exactly and field 5 (as a number) is strictly greater than 0 — remember the header row and any zero-cost or non-ACTIVE rows must be excluded, and write only data rows (no header) to `billing_active.csv`. Second, write the Go service to read that CSV, sum cost per service into a map, and expose `/health` (200, body `OK`) and `/metrics` (one `cost{service="X"} NN.NN` line per service, costs formatted with `%.2f`); build it and launch the binary in the background, confirming it's actually listening on 9090 before moving on. Third, start `socat TCP-LISTEN:8080,fork,reuseaddr TCP:127.0.0.1:9090` in the background to bridge 8080→9090. Finally write `check_health.sh` to curl `/health` through port 8080, and only if the body is exactly `OK`, curl `/metrics` through 8080, sort the lines alphabetically, and save to `final_metrics.txt` — then actually execute this script so the output file exists at the end.
