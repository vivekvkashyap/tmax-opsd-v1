Chain the four sub-problems in order since each output feeds the next. (1) Validate the cert chain with `openssl verify -CAfile root.crt -untrusted intermediate.crt leaf.crt`; once it verifies, pull the CN with `openssl x509 -in leaf.crt -noout -subject`. (2) For the binary, don't try to disassemble blindly — run `strings /home/user/forensics/malware.bin | grep -i DECRYPT_KEY_PREFIX` (or `objdump -s`/`gdb` if strings alone doesn't show it) to locate the embedded hex value near that label. (3) Build the composite XOR key by concatenating the recovered prefix and CN exactly (order matters — re-read the task spec for concatenation order), then write a small Go program that reads `evidence.enc` as bytes and XORs each byte with `key[i % len(key)]`, writing the result to `recovered_evidence.txt`; run it with `go run` and sanity-check the output is printable text. (4) For the audit, read `processor.go` looking for unsanitized path-joining from user input (path traversal) and any use of a weak hash/cipher — map each to its correct CWE number, don't guess.
