Build the pieces in order and test each independently before wiring them together via the hook. First write and compile `vnc_parser.cpp` standalone, feeding it a sample `VM_ID=... CMD="..."` line by hand to confirm the parsing regex/split logic (extract the IP:PORT following `-vnc`) produces the exact `VM_ID: <ID> | VNC: <IP>:<PORT>` format. Then init the bare repo and clone, and write the `post-receive` hook as a script using only absolute paths for every input/output/binary (this is the key trap: hooks run with a minimal PATH and cwd set to the bare repo, so relying on relative paths or the invoking shell's PATH will silently write output to the wrong place or fail to find compilers). Inside the hook: `git --work-tree=/home/user/build_env --git-dir=<bare-repo-path> checkout -f`, compile with a full path to g++, then filter `/home/user/logs/qemu_raw.log` for lines containing START, strip the timestamp bracket prefix with sed/awk, and pipe the remaining `VM_ID=... CMD="..."` portion into the compiled parser, redirecting stdout to `/home/user/logs/vnc_endpoints.log`. Set the hook to mode 700. Test end-to-end by committing vnc_parser.cpp in the clone and pushing, then inspect the resulting log file.
