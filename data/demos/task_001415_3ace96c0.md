Start by transcribing `/app/config_audio.wav` to discover the port and window size — install and use a speech-to-text tool (e.g. Python `SpeechRecognition` with `pocketsphinx`, or `whisper`/`vosk` if available) and listen carefully for spoken numbers, since digits may be spoken individually (e.g. "eight zero eight zero") rather than as one number — parse them accordingly rather than assuming a common default like 8080. Once you have the port `P` and window size `W`, write a C HTTP server: implement a minimal socket loop that accepts a connection, reads headers until it finds `Content-Length`, then reads exactly that many more body bytes (don't rely on a single `recv` call). Split the body on whitespace into words, then for each contiguous window of `W` consecutive words compute the count of distinct words using a simple hash set/table (e.g. a small open-addressing or chained hash of string keys) — iterate windows with a sliding start index from 0 to `word_count - W`. Emit the per-window unique counts as a JSON array like `[2, 3]` in the HTTP response body with a `200 OK` status line. Compile to `/home/user/server` and launch it in the background bound to `127.0.0.1:<discovered-port>` so it stays reachable for the verifier.