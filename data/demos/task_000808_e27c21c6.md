Treat the provided binary as a black box: don't try to reverse-engineer it, just characterize its I/O contract by piping a few sample strings into `/app/config_hasher` via stdin and observing stdout — confirm it's deterministic and note the exact output format (e.g. case, trailing newline or not) before wiring it into your server, since your log-line formatting must match byte-for-byte. Build the ingestion server in whatever language you're fastest in (Python's http.server/Flask is fine); implement strict validation first: use a proper ISO8601 regex/parser for timestamp (reject anything that doesn't match `YYYY-MM-DDThh:mm:ssZ` exactly) and `^[a-z0-9_-]{3,16}$` for service, returning 400 on any mismatch before calling the hasher. On success, invoke the binary as a subprocess passing config_data on stdin, capture stdout exactly (strip only the trailing newline if the binary adds one), and append the log line in the precise format given — test the exact string against the spec, not just "looks right". Launch the server as a background/daemon process so it survives your test session. For the cron file, write a single standard 5-field crontab line for daily midnight execution of the exact logrotate command given.
