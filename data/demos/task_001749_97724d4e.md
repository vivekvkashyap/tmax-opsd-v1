Scaffold a Rust binary crate (`cargo new audit_tool` in `/home/user/audit_tool`) and add `base64`, `regex` (or manual string matching), and `serde`/`serde_json` to `Cargo.toml`. Parse `/home/user/auth.log` line by line with a regex or split on `|` to extract the timestamp and base64 payload fields, base64-decode each payload, then case-insensitively check the decoded string for the given malicious substrings (`union select`, `or 1=1`, `<script>`) — only lines matching at least one signature go into the final report. For each match, invoke `/home/user/auth_service` as a subprocess passing the *decoded* payload as argv[1], and inspect its exit status via `std::process::Command`/`ExitStatus` (checking for `.code() == Some(0)` or `Some(139)` to mark `vulnerable: true`, anything else `false` — remember 139 typically corresponds to a process killed by SIGSEGV). Build the output as a `Vec` of structs with `timestamp`, `decoded_payload`, `vulnerable` fields deriving `Serialize`, preserving log order, and write it as a JSON array to `/home/user/audit_report.json` with `serde_json::to_writer` or similar. Build and run the project (`cargo run`) to actually produce the file before finishing.
