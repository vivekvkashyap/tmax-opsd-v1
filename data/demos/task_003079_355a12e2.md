Drive every path and rule from `/home/user/repo_config.json` at runtime — never hardcode the architectures, extensions, or output paths you observe while testing, since the grader can vary the config. For each `.zip` in `/home/user/incoming`, extract it to a temp directory (Python's `zipfile` module), then walk the extracted tree and, for every file, check two independent conditions before accepting it: the filename's extension matches one of `allowed_extensions` (careful with multi-part extensions like `.tar.gz` — a naive `os.path.splitext` only grabs `.gz`, so match the suffix string directly instead), and the filename contains one of `allowed_architectures` as a substring. Copy accepted files into `<output_dir>/<architecture>/<filename>`, creating directories as needed, and compute each file's SHA-256 over its actual bytes (hash after extraction, not any intermediate representation) using `hashlib.sha256(...).hexdigest()` lowercase. Build the manifest as a JSON object with an `artifacts` list matching the exact schema given, with `path` relative to the output directory, and write it to the `manifest_path` from the config. Run the script against the real `/home/user/incoming` contents and inspect the resulting manifest and copied files yourself before finishing, rather than assuming the filtering logic is correct.