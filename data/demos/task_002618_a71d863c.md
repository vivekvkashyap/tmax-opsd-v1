Use `pandas.read_csv` (not naive line-splitting or `csv` with manual newline handling) so the standard CSV quoting rules correctly keep embedded newlines inside quoted `notes` fields as part of one record — this is the classic naive-attempt failure mode here. Parse the `timestamp` column with a timezone-aware parser that handles mixed formats (e.g. `pd.to_datetime(..., utc=True)`, which correctly interprets both explicit offsets like `-04:00` and trailing `Z`), then format to the exact `YYYY-MM-DDTHH:MM:SSZ` string only at write time. For anomaly detection, sort by `sensor_id` then by the parsed UTC timestamp, group by `sensor_id`, and use a per-group `diff()` on `temperature` compared against the absolute-value threshold — remember the first row in each group's diff is NaN and must map to `is_anomaly=0`, not be dropped or errored on. Load the transformed DataFrame into SQLite with `sqlite3` or `pandas.DataFrame.to_sql(..., if_exists='replace')` into a table named `readings` with the five required columns, then actually execute the script so the `.db` file exists on disk before finishing.