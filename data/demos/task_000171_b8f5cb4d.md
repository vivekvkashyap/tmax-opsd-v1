Start by OCR'ing `/app/rate_limit_specs.png` (e.g. `tesseract` or a Python OCR binding) to recover the exact capacity and refill-rate numbers stated in the image — do not guess these, read them off the image text. Then implement a standard token-bucket in `ratelimit.c`: keep capacity/tokens/refill-rate/last-refill-timestamp in a small global struct, and in `allow_request()` compute elapsed time since last check (use a monotonic clock, e.g. `clock_gettime(CLOCK_MONOTONIC, ...)`, matching the millisecond units implied by the spec), add `elapsed_ms * refill_rate` tokens capped at capacity, then decrement and return true/false. Fix the Makefile to compile with `-shared -fPIC -DOPTIMIZED_BUILD -O2` into `/app/libratelimit.so` (the naive `gcc file.c -o libratelimit.so` produces a non-shared, non-PIC binary that either fails to load via ctypes or errors on the `#error` guard). In `validator.py`'s `CRateLimiter`, load the .so with `ctypes.CDLL`, set `.restype = ctypes.c_bool` (or c_int, matching your C return type) on `allow_request`, and call it directly — avoid Python-side overhead that would erode the speedup. Run `benchmark.py` and confirm `/app/speedup.txt` shows the required threshold; if speedup is marginal, check you're not doing per-call ctypes attribute lookups or unnecessary Python wrapping inside the hot loop.
