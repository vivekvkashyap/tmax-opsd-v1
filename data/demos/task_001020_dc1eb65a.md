Before writing any script logic, recover the magic signature: use an OCR/image-reading tool (or your image-viewing capability) on `/app/tape_label.png` to read the label text and locate the string identified as the "MAGIC" signature — use whatever value you actually read from the image, don't guess a placeholder. Then write `/home/user/filter_backup.sh` as a streaming line-by-line reader (`while IFS= read -r line; do ... done`, avoid slurping all of stdin first) that, for each line, parses it against the strict `MAGIC|SIZE|DATA` shape — a bash regex like `^([^|]+)\|([0-9]+)\|(.*)$` captured via `[[ "$line" =~ ... ]]` and `BASH_REMATCH` is a reliable way to split on the first two `|` delimiters while letting DATA itself contain `|` or any other characters. Only emit `OK: <DATA>` when the captured MAGIC exactly equals the recovered signature AND the numeric SIZE exactly equals the actual character length of DATA (`${#data}` in bash); any regex mismatch, wrong magic, or size mismatch must emit `ERR: <the original raw line unchanged>`. Make the script executable with `chmod +x`, and test it by piping crafted good/bad lines through it to confirm both branches and the exact `OK:`/`ERR:` prefixes match.