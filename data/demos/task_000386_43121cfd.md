Start by OCR'ing the spec image (`tesseract /app/arch.png stdout`) and reading it carefully to recover the instruction set, register names, and rate-limit number/window — these are the ground truth your server must implement exactly, so don't guess at semantics not present in the image. Build a small line-by-line interpreter: initialize registers to 0, split `asm` on newlines, tokenize each line by whitespace into opcode + operands, and implement each opcode's exact semantics as literally described in the spec text. For rate limiting, implement a sliding/fixed window limiter keyed by client IP (e.g. a deque of request timestamps per IP, evicting entries older than the window, rejecting with 429 once the limit is exceeded) — a naive global counter that never resets will fail the burst test. Return the full register-state JSON after each run. Bind the server (FastAPI/Flask + uvicorn) to `127.0.0.1:8000`, run it in the background, and manually POST a few test programs plus a rapid burst yourself to sanity-check correctness and the 429 behavior before finishing.
