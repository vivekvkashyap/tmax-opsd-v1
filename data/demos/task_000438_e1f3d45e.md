Break this into graph-analysis logic and networking/service integration, and get the detection rule precise before wiring up the sockets. First, `cargo init` the filter_service crate and write CSV parsing (source,target,weight) into an adjacency structure (e.g. HashMap<String, HashSet<String>>) per file. To detect the "evil" bipartite pattern, for each node compute its exact set of outgoing targets, then group nodes by identical target-sets (or by target-sets that are supersets containing a common core of >=10 targets) and check whether any such group of source nodes sharing >=10 common targets has size >=5 — this is the K_{5,10} check; re-read the exact thresholds (m>=5 source nodes, n>=10 shared targets) directly from the task rather than guessing, and reject (skip entirely, print "REJECTED: <path>") the moment a file matches. For clean files, run a plain BFS (unweighted, since it's shortest path by hop count from node "0") to get distances to all reachable nodes, then dense-rank nodes by distance (ties get the same rank, next rank is consecutive). Second, for I/O: open a TCP connection to port 8001, read newline-delimited file paths in a loop, process each, and for accepted files open a TCP connection to port 8002 and write one JSON line per node in the required schema — test incrementally against start_services.sh before trusting the corpus-wide pass rate.
