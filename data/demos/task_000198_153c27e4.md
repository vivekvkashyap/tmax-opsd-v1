Treat `/app/fit_model` strictly as a black box — do not disassemble it, just use it as an oracle. Run it against every file in `/app/train_data/` and record, per file, whether it printed a fitted parameter (clean) or `ERROR: Divergence detected` (evil); this gives you labeled training data. For each labeled file, load the `"signal"` variable with a NetCDF library (Python's `netCDF4`/`scipy` for quick exploration is fine even though the final detector must be C) and compute a numerical feature that distinguishes the classes — the hint in the task points at derivative-based roughness, so compute discrete second differences and look at how a simple aggregate (e.g. sum of absolute second derivative) separates the two label groups. Pick a threshold that cleanly separates all observed clean vs evil values with margin, not just the midpoint of your training set, since the held-out corpus may have points near the boundary. Then implement that exact same check in `/home/user/detector.c` using the NetCDF C API (`nc_open`, `nc_get_var_double`), compile with `-lnetcdf`, and print exactly `ACCEPT` or `REJECT`. Validate against both `/app/test_corpus/clean` and `/app/test_corpus/evil` before finishing.
