Do this as a pandas pipeline, not shell text processing — the task explicitly calls out that naive line-based parsing corrupts rows with embedded newlines inside quoted `remarks` fields, so use `pandas.read_csv` (which correctly handles RFC-4180 quoting/embedded newlines) after extracting the tarball, not `csv` module line iteration or awk/sed. Steps: extract sensor_dump.tar.gz into /home/user/workspace/, load raw_sensors.csv with read_csv, drop exact duplicate rows (`drop_duplicates()`), normalize the timestamp column by trying multiple format parses (e.g. `pd.to_datetime(col, format=..., errors='coerce')` for each known format and coalescing, or a flexible parser) and rendering as ISO 8601 (`strftime('%Y-%m-%dT%H:%M:%S')`) — watch for the format missing seconds (`HH:MM` only) as one of the mixed formats. In remarks, replace `\r\n`/`\n` with a single space (`str.replace` with regex for both variants). Reshape with `pd.melt` on the three sensor_* columns into sensor_name/value, keeping timestamp and remarks fixed. Drop rows with missing/NaN value, sort by (timestamp, sensor_name), and write to /home/user/workspace/processed_sensors.csv with `index=False`. Diff your header/row count against the spec before finishing.
