For Part 1, extract every frame with `ffmpeg -i /app/led_strobe.mp4 frame_%04d.png`, then for each frame compute the mean grayscale intensity (convert to grayscale first, e.g. with PIL/`np.array(Image.open(f).convert('L')).mean()` or ImageMagick `convert frame.png -colorspace Gray -format "%[fx:mean*255]" info:`). Build the intensity sequence in frame order (0-indexed) and scan it looking for the first index where intensity < 10.0 and the next 14 frames also stay < 10.0 — implement this as a sliding-window check over the whole array, not just "first drop below threshold", since a brief dip that pops back up doesn't count. Write only the resulting integer to `/home/user/stable_frame.txt` with no trailing text.

For Part 2, write `/home/user/stream_integrator.sh` as an executable Bash script that reads stdin line by line with `awk` (or a `while read` loop plus `bc -l`), maintaining running state for the last two raw values and the previous smoothed value S_{t-1} to compute the trapezoidal integral incrementally — do not try to buffer the whole stream in memory. Handle the t=1 and t=2 edge cases exactly as specified, and use `printf "%.4f\n"` for output formatting since naive `bc` output can drop leading zeros or use scientific notation for negative small numbers.
