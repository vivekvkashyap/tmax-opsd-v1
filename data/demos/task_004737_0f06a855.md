Separate extraction from querying so you can validate each independently. To extract edges, open `/app/data_stream.mp4` with `cv2.VideoCapture` (or `ffmpeg`) and read each of the 100 frames; since each frame is uniform-color, sample a single pixel (e.g. `frame[0,0]`) per frame — critically, OpenCV returns pixels in BGR order, not RGB, so unpack as `b, g, r = frame[0,0]` and map R→source, G→target, B→weight, not the raw channel order. Cache the resulting 100 `(source, target, weight)` tuples (e.g. to a file or in your script) so you don't need to re-decode video on every query. In `query_graph.py`, parse the two positional int args, filter edges to `weight <= weight_limit`, build a directed adjacency list, and run BFS/DFS from `start_node` to find the reachable set (including the start node itself even if it has no edges). Compute out-degree per reachable node by counting its outgoing edges *within the filtered graph only*, then pick the max out-degree node breaking ties by the lowest numeric ID. Print strict single-line JSON with exactly the two required keys. Since grading is bit-exact fuzz equivalence against a hidden oracle, test with several start/limit combinations including nodes absent from the filtered graph (should just be reachable_count=1, itself) and verify output formatting matches exactly (no extra whitespace/keys).