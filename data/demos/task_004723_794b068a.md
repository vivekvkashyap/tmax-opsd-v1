Tackle the three pieces separately and test each in isolation. For setup_env.sh: use `timedatectl set-timezone Asia/Tokyo` (or write `Asia/Tokyo` to `/etc/timezone` + relink `/etc/localtime`), generate the locale with `locale-gen ja_JP.UTF-8` guarded by checking `locale -a` first so reruns don't error, and install/configure postfix for local relay (set `inet_interfaces = loopback-only`, add `admins: user` to `/etc/aliases`, run `newaliases`, `systemctl restart postfix` or equivalent) — make every step check current state before acting so a second run is a no-op that still exits 0. For video extraction: use `ffmpeg` to pull frames at the relevant intervals (e.g. `ffmpeg -i diagnostic_console.mp4 -vf fps=1 frames/%04d.png` or seek to specific timestamps) then OCR each candidate frame with `tesseract` to read the on-screen UTC timestamps, writing the 5 unique ones you recover (deduplicated, one per line, in the given format) to raw_timestamps.txt — don't guess the values, actually extract them from frames. For log_formatter.cpp: parse `YYYY-MM-DD HH:MM:SS` strictly (reject anything that doesn't match, or has out-of-range month/day/hour/etc, printing `INVALID_FORMAT`), convert via `mktime`/`timegm` treating input as UTC, then `setenv("TZ","Asia/Tokyo",1); tzset();` before `localtime()` and format with `std::setlocale(LC_TIME,"ja_JP.UTF-8")` and `strftime("%c", ...)` or hand-format matching the required Japanese pattern exactly — validate against varied edge-case inputs (leap years, invalid dates, garbage strings) yourself before trusting it.
