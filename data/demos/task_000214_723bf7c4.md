Build incrementally and test each piece in isolation before combining. Start with the math: write and unit-test a standalone determinant function for a 3x3 row-major matrix using the standard cofactor expansion, confirming it against a couple of hand-computed examples. Then build the HTTP layer: fetch `httplib.h` and `json.hpp` as single-header includes, wire up `POST /api/determinant` to parse the JSON body, base64-decode the `payload` field, split on whitespace, and explicitly count the tokens — return 400 immediately if the count isn't exactly 9 (this is the most commonly missed validation). For rate limiting, maintain a global (mutex-protected, since httplib serves requests from multiple threads) sliding window of request timestamps; on each request, prune timestamps older than 1000ms, and if the window already holds 2 recent requests, reject with 429 before doing any determinant work — a naive per-minute or fixed-window counter will fail the "3rd request within 1s of the 1st" test. Compile with `g++ -std=c++17 -lpthread`, launch the binary in the background, and only then write `test_server.sh` to `curl` your own running server (valid payload, invalid-length payload, then 4 rapid valid requests) and capture status codes with `-o /dev/null -w "%{http_code}\n"` into `/home/user/verification.log`.
