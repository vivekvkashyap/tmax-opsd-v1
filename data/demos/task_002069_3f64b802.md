Build and verify each layer independently before wiring them together, entirely under `/home/user/` since you lack root. First, extract every frame from `/app/anomaly_feed.mp4` to `/home/user/extracted_frames/frame_%04d.jpg`, and separately detect the single frame that is exactly solid red (255,0,0) by checking all pixels match (not just average color, to avoid false positives on near-red frames) — save that index for the API. Second, write a Python backend (Flask/FastAPI) bound to a Unix socket at `/home/user/backend.sock` (e.g. gunicorn `--bind unix:/home/user/backend.sock`), exposing `GET /api/anomaly` returning `{"anomaly_frame": <index>}`; test it directly first with `curl --unix-socket`. Third, create `/home/user/web_root/media` as a symlink to `/home/user/extracted_frames/`. Fourth, write a self-contained `/home/user/nginx.conf` with all paths (pid, error_log, client_body_temp, etc.) redirected under `/home/user/nginx_temp/`, listening on `127.0.0.1:8080`, proxying `/api/` to the Unix socket, serving `/static/` from the symlinked media dir, and using `if ($http_authorization != "Bearer syseng-secure-2024") { return 403; }` (or a `map` block) to gate `/api/` specifically. Launch nginx with `nginx -c /home/user/nginx.conf` and curl-test all three verifier cases (missing auth -> 403, correct auth -> 200 with right JSON, static jpeg -> 200) yourself before finishing.