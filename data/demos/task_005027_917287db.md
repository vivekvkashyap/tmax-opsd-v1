First run `tesseract /app/rules.png stdout` (or `tesseract /app/rules.png -` ) to OCR the four mapping rules out of the image — read the OCR text carefully rather than guessing, since punctuation/operators must be exact. Then implement `/home/user/cleaner.c` as a strict streaming line reader: use `fgets` in a loop (never read the whole file into memory), strip the trailing newline, and split on commas by counting exactly 4 fields — reject the line if there are fewer or more fields (extra commas mean malformed). Validate the email column ends with exactly ".edu" or ".com" using suffix comparison (not substring search), and require a well-formed "local@domain" shape before masking. Apply the four transformations you OCR'd from the image precisely, including the exact rounding/format specifier for the floating value (match printf's `%.2f`-style behavior). Because this is graded by fuzz-differential testing against a hidden oracle across many random inputs, edge cases matter: empty domain, single-char email, negative/zero timestamps, and integer overflow in the modulo arithmetic should all be handled using the same integer types the spec implies (e.g. long long for timestamp/user_id math). Compile with `gcc` to `/home/user/cleaner` and chmod +x it, then test manually with crafted CSV lines covering valid, malformed, and non-matching-domain cases before trusting it.
