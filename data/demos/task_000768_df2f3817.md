This task has two independent halves — tackle them separately. For the video: extract frames from `/app/console_panic.mp4` with `ffmpeg` (e.g. every frame or at a high fps around suspected crash timing) and run OCR (`tesseract`) on each frame, grepping the OCR text for a line matching `Kernel Panic Code: 0x[0-9A-Fa-f]+`; since only a few frames near the crash carry the text, scan systematically rather than guessing a timestamp, and once found write only the hex code (nothing else) to `/home/user/panic_code.txt`. For the parser: first reproduce the crash — feed `log_parser.c`'s compiled binary malformed inputs (no `]`, no `:`, huge lines, embedded NULs) under `gdb`/`ASAN` to see exactly which buffer/sscanf call overflows; the description calls out that unsafe `strcpy`/unbounded `sscanf("%[^]]]", ...)` patterns are the likely culprit, so replace them with bounded parsing using `strnlen`, `strchr` to locate `]`/`:` positions explicitly, and fixed-size buffers with length checks (reject/emit `INVALID` for lines ≥1024 bytes) instead of unbounded copies. Rebuild with `gcc -O2 log_parser.c -o /home/user/log_parser`, then fuzz it yourself against many malformed/random inputs, including binary garbage and lines with no trailing newline, checking it never crashes and always emits either `Parsed: LEVEL | message` or `INVALID` per the exact spec.
