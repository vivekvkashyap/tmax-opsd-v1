Install Eigen (`apt-get install libeigen3-dev`) so the existing Makefile's `-I/usr/include/eigen3` path resolves. In `likelihood.cpp`, replace the `Eigen::LLT` block entirely with an SVD-based pseudo-inverse: construct `Eigen::JacobiSVD<Eigen::MatrixXd> svd(cov, Eigen::ComputeThinU | Eigen::ComputeThinV)`, get the singular values, and only keep those `>= 1e-6` — count them as `rank`, sum their logs for `log_pseudo_det`, and build the pseudo-inverse as `V * diag(1/s_i for kept i, else 0) * U^T` restricted to the kept singular vectors. Compute the quadratic form `(x-mu)^T * pseudo_inverse * (x-mu)` and assemble `log_L = -0.5*(rank*log(2*PI) + log_pseudo_det + quad)` exactly as specified — getting `rank` (not the full dimension) into the `log(2*PI)` term is the detail most naive attempts miss. Since the MCMC uses a fixed seed (`mt19937 gen(42)`) and a hardcoded near-singular covariance matrix, your output is fully deterministic once the math is correct — build with `make`, run `./mcmc_sampler data.csv`, and write its exact stdout (unmodified) into `/home/user/bio_mcmc/final_mean.txt` rather than retyping or reformatting the numbers.
