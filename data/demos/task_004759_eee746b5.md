Write `/home/user/run_analysis.sh` as a self-contained bash pipeline: use `sqlite3 /home/user/company.db` with `.mode csv` / `.import` (skipping header rows appropriately, e.g. `.import --csv --skip 1`) to load the four CSVs into tables, then `CREATE INDEX` on the join/lookup columns (`employees.manager_id`, `employees.dept_id`, `projects.dept_id`, `assignments.emp_id`/`project_id`). For management depth, write a recursive CTE anchored on `manager_id IS NULL` (depth 0) and recursing via self-join on `manager_id = parent.id`, incrementing depth each level. Then join employees→assignments→projects→departments (twice — once for the employee's dept, once for the project's owning dept) and filter `WHERE employee_dept_id != project_dept_id`. Use `sqlite3 -json` or manual `jq` construction to emit the exact key set requested, then pipe through `jq 'sort_by(.employee, .project)'` to get the required stable ordering. Run the script once yourself and diff the JSON output against your own hand-derived expectation for the small fixture before considering it done, and `chmod +x` the script.
