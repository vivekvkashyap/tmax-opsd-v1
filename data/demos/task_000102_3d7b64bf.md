Decode the video first, in isolation, and validate it before touching the database: use `ffmpeg` (or `opencv`/`imageio` in a helper script) to extract all 60 frames of `/app/node_status.mp4` as raw pixel arrays, then for each frame and each of the 100 blocks average or sample the 10x10 pixel region for node `R*10+C` and classify ONLINE (white) vs OFFLINE (black) by checking pixel values are near 255 vs near 0 (use a threshold rather than exact equality in case of minor lossy-adjacent artifacts) — spot-check a few frames visually or by printing raw pixel values to confirm your row/column-to-node-ID mapping matches the spec before loading anything into SQLite. Build `/home/user/network.db` with an edges table (indexed on `src`) and a node-status table keyed by `(time_sec, node_id)` with an index supporting fast "is node X online at time T" lookups — do this parsing once up front, not per query. In `/home/user/router.py`, use parameterized queries (`?` placeholders) to pull the online node set and valid edges for the given `time_sec`, then implement Dijkstra by hand (a heap-based priority queue) over that filtered subgraph; if `src` or `dst` is offline or unreachable, print `NO PATH`, otherwise print the exact `PATH:...` / `COST:...` format shown in the spec. Test the script directly against varied `(src,dst,time_sec)` triples for correctness and speed, since it will be fuzzed against an oracle.
