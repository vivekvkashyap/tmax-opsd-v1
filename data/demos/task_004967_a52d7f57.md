Build this as a small numeric pipeline in Python (numpy + scipy), following the spec's exact algorithm rather than shortcuts like `np.linalg.pinv` with default tolerance — the task requires you to do the SVD yourself and zero out singular values below the stated cutoff, since a default pinv tolerance may not match. Steps: load both `.npy` arrays; compute `U, S, Vt = np.linalg.svd(cov)`; build `S_inv` by inverting each singular value only where it's >= the given threshold, else 0; reconstruct the pseudo-inverse as `Vt.T @ diag(S_inv) @ U.T`. For each row vector `x_i` of `kmer_diffs`, compute the quadratic form `x_i @ pinv @ x_i.T` and take its square root — vectorize this across all 1000 rows rather than looping, e.g. via `np.sum((diffs @ pinv) * diffs, axis=1)`. Fit `scipy.stats.gamma.fit(distances, floc=0)` (do not omit `floc=0`, it changes the fit materially), then get the 95th percentile via `gamma.ppf(0.95, shape, loc=loc, scale=scale)`. Format the result to exactly 4 decimal places and write only that string to the output file — check for trailing newline/whitespace issues.