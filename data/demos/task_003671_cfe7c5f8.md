Work in order: first run `tesseract /app/spec.png` (or convert with imagemagick first if OCR is noisy) to recover the numeric threshold string embedded in the image — do not guess or hardcode a number without OCR'ing it, and sanity-check the extracted text against expected formatting (e.g. strip stray characters, parse as float). Implement the scanner's instability metric as sum(abs(x[i]-x[i-1])) over consecutive floats in the file, compare strictly-less-than the recovered threshold for CLEAN (exit 0) vs. greater-or-equal for EVIL (exit 1), and print CLEAN/EVIL accordingly. Validate against every file in `/app/corpora/clean/` and `/app/corpora/evil/` before considering the scanner done — a single misclassification means the threshold was mis-read or a boundary condition (strict vs. non-strict) is wrong. For the nginx config, use `limit_req_zone $binary_remote_addr zone=upload_limit:10m rate=10r/s;` in the http context and `limit_req zone=upload_limit;` inside the `listen 8080` server block that proxies to `127.0.0.1:9000`; verify syntax with `nginx -t -c <path>` without starting the server.