Recover the scrubbed key by searching full git history, not just HEAD: `git log --all --oneline`, `git reflog`, and `git show <sha>:config.json` on commits that touched it, since a reverted commit is still reachable via its SHA even if not on any branch tip. Recover the reverted fast implementation the same way (`git show <sha>:fast_pipeline.py`), then locate its off-by-one — look for a loop bound like `range(len(frames)+1)` and fix it to stay in bounds, skipping the last partial/out-of-range index instead of indexing past the array. To match the legacy engine's math exactly without disassembling it, treat it as a black box: pipe individual 32-byte frames to `/app/legacy_engine` via stdin and diff its stdout against candidate transformations of the same frame to reverse-engineer the per-sample formula and which frames get skipped (look for a fixed marker byte pattern that flags corruption). Rewrite the hot loop with vectorized `numpy` operations (frombuffer, boolean masking to drop corrupted frames, vectorized arithmetic) instead of a Python per-element loop to hit the runtime threshold.
