Build and unit-test the Rust decoder in isolation first before wiring up the pipeline. In `decode_rle.rs`, read the file's bytes with `std::fs::read`, iterate over them two at a time (`chunks(2)` or a manual index step), and for each `(count, byte)` pair push `byte as char` repeated `count` times into an output buffer, then print it — take the file path from `std::env::args().nth(1)`, and compile with `rustc decode_rle.rs -o decode_rle`. Sanity-check it manually on one `.rle` file before the full pipeline. Then chain shell tools: `find /home/user/storage_logs -type f -name '*.rle'` to enumerate files recursively (any depth, so don't assume a fixed directory layout), `xargs -I{} ./decode_rle {}` (or a `while read` loop) to decode each, pipe through `grep 'CRITICAL_OOM'` to keep only matching lines, then `sort` to alphabetize, redirecting the final result to `/home/user/oom_events.log`. Finish with `ln -s /home/user/oom_events.log /home/user/latest_incident` — use a relative or absolute symlink but make sure `readlink` resolves it back to that exact path.
