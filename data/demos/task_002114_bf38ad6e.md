Separate this into robust CSV parsing and independent-axis Bayesian precision-weighted averaging. In `processor.c`, parse each data row field-by-field with strict validation: use `strtod`/`strtol` with endpointer checks (not blind `atof`/`atoi`) so malformed numbers, wrong column counts, or non-numeric strings are detected and cause that row to be skipped (not crash the program); also explicitly reject rows where `variance <= 0`. Because x and y are independent and all covariances here are diagonal, you don't need real matrix inversion — treat precision as a scalar per axis: prior precision = 1.0 (from `Σ0 = I`), prior precision*mean = 0.0 for both axes; for each valid row accumulate `precision_i = 1/variance_i` and `precision_i * x_i` (and same for y). Sum these across all valid rows plus the prior, then posterior_mean = (sum of precision*value) / (sum of precision), computed separately for x and y. Format both to exactly 4 decimal places, comma-separated, on one line to `/home/user/posterior.txt`. Write the Makefile with `all` compiling `processor.c -o processor` and `run` executing `./processor`, and confirm `make all && make run` succeeds end-to-end.