Parse the binary format field-by-field with explicit byte offsets rather than guessing struct layout: verify bytes 0-3 equal the ASCII "RAWD" magic, read bytes 4-7 as a little-endian uint32 (DatasetID), bytes 8-9 as a little-endian uint16 (payload length L), then read exactly L bytes starting at offset 10 as the payload — use Go's `encoding/binary` with `binary.LittleEndian`. For the Windows-1252 to UTF-8 decode, use `golang.org/x/text/encoding/charmap` (`charmap.Windows1252.NewDecoder()`) rather than assuming it's ASCII-compatible, since bytes like 0x80 map to non-ASCII code points (e.g. the Euro sign) that differ from Latin-1. Implement atomic writes by writing to a `<name>.tmp` file in the processed directory first, then calling `os.Rename` to the final `dataset_<ID>.txt` path — rename is atomic on the same filesystem, a direct write is not. For `watch.sh`, use `inotifywait -m -e create -e moved_to` in a loop watching the raw directory, invoking your Go program (compiled or via `go run`) on each new `.dat` file, and launch it in the background so it keeps watching. Validate your own pipeline manually with a hand-crafted binary file before relying on the hidden background dropper.
