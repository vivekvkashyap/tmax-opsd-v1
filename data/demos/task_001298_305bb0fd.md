Don't try to reverse-engineer the stripped `/app/access_oracle` binary via disassembly — instead treat it as a black-box spec: the task description already tells you the algorithm (directed graph reachability from EntityID to TargetID using edges in /app/access_graph.tsv, tab-separated Source/Target, no header). Implement this directly, e.g. with a recursive SQLite CTE: load the TSV into a temp table (or query it via a CTE reading from a CSV virtual table), then `WITH RECURSIVE reachable(node) AS (SELECT target FROM edges WHERE source = :entity UNION SELECT e.target FROM edges e JOIN reachable r ON e.source = r.node) SELECT 1 FROM reachable WHERE node = :target LIMIT 1` — print GRANTED if a row is returned, else DENIED. Watch for self-loops/cycles causing infinite recursion (the recursive CTE naturally terminates once no new distinct nodes are added, but confirm your query doesn't revisit nodes infinitely) and make sure path length must be >=1 (EntityID itself doesn't count as reaching itself unless there's an actual edge path back to it). Before finalizing, run your script against many EntityID/TargetID pairs and directly diff its output against `/app/access_oracle`'s output for the same inputs to catch discrepancies.