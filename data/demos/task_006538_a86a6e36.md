Work SQL-first, not row-by-row in Python. First add indexes on `backup_jobs(parent_backup_id)` and on `(db_name, start_time)` to support the recursive walk and the window function. Write a Recursive CTE that starts from rows where `parent_backup_id IS NULL` (the roots) and recursively joins children via `parent_backup_id = id`, tagging each descendant row with its `root_backup_id`; then `GROUP BY root_backup_id, db_name` and `SUM(size_bytes)` to get `total_chain_size` per chain. In a second query stage, order chains per `db_name` by the root's `start_time` and use a window function like `AVG(total_chain_size) OVER (PARTITION BY db_name ORDER BY start_time ROWS BETWEEN 2 PRECEDING AND 1 PRECEDING)` to get the average of exactly the previous two chains — treat the first two chains per `db_name` (fewer than two prior rows available) as not anomalous rather than computing a partial average. Compare `total_chain_size > 1.5 * prev_avg` for the anomaly flag, coercing to boolean. Finally build the output list in Python, validate it against the given JSON schema with the `jsonschema` library before writing, and only write to disk after validation succeeds so failures are caught early.