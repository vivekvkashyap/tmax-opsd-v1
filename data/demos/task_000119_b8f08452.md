Work each rule as an independent extraction step, then combine into one Bash script. First, decode the DTMF PIN: use `sox` or `multimon-ng` on `/app/voicemail.wav` (e.g. `multimon-ng -t wav -a DTMF /app/voicemail.wav`) to recover the numeric override PIN, and store it as a variable your script checks with a `case "$pw" in "$PIN"*)` or `[[ $pw == $PIN* ]]` prefix match. Second, find the backdoor string in `/app/legacy_validator` with `strings /app/legacy_validator | grep bckd` and filter to entries longer than 8 chars — use that exact literal in a substring check (`[[ "$pw" == *"$backdoor"* ]]`). Third, parse `/app/auth_logs/auth.log` with `grep -oP 'FAILED_ATTEMPT: \K\S+'` and `base64 -d` each match to build a list of compromised passwords for exact-match rejection. Fourth, reject any password under 10 characters (`${#pw}`). Combine all four checks with early exits printing `REJECT`/exit 1, falling through to `ACCEPT`/exit 0. Validate against both `/app/corpus/evil/` and `/app/corpus/clean/` before finishing, since a false positive on clean data fails the task just as much as a missed evil case.
