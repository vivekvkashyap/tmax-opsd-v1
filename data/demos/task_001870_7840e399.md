First transcribe `/app/archiver_spec.wav` (install `openai-whisper` or use `SpeechRecognition`+`pydub`/`ffmpeg`, e.g. `whisper /app/archiver_spec.wav --model base`) and read the transcript carefully to extract the exact Magic Bytes and the path field padding length — do not assume common values, the spec is deliberately custom and must match precisely for the fuzz-equivalence check to pass. Then implement `/home/user/archiver.go`: open/create the lock file argument and call `syscall.Flock(fd, syscall.LOCK_EX)` (blocking, no LOCK_NB) before touching stdin; use `bufio.Reader`/`bufio.Writer` around stdin/stdout for performance; write the magic bytes first. Parse commands line-by-line: for `WRITE <path> <size>`, read exactly `<size>` raw bytes immediately following the newline via `io.ReadFull` (don't assume another newline follows — the spec says data is followed immediately by the next command with no implicit newline separator), then emit `'W'` + padded/truncated path (fixed-width, right-padded with nulls or truncated per the transcript) + little-endian 8-byte size + raw data. For `HLINK`, emit `'H'` + padded link_path + padded target_path. On stdin EOF, flush the writer, unlock, and exit 0 — get this ordering exactly right since a fuzzer will diff your output byte-for-byte against an oracle across thousands of random command sequences.