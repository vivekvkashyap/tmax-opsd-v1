Follow the pipeline stages in order and keep intermediate DataFrames explicit so the row alignment between text-length/embeddings/predictions stays correct. Load reviews.csv, drop rows with NaN text, then fit TfidfVectorizer(max_features=500, stop_words='english', lowercase=True) on the remaining text to get X — keep X's row order aligned with the DataFrame's row order (use boolean masks, not re-filtering, to split later). Build the valid_mask as `rating.between(1,5)` (inclusive) and note NaN compares as False so it lands in corrupted automatically. For the t-test, compute character length of text for each group and run `scipy.stats.ttest_ind(len_valid, len_corrupt, equal_var=False)`, keeping the p-value. Slice X with the boolean mask (not re-vectorizing) to get X_valid/X_corrupt so the feature space matches; fit Ridge(alpha=1.0, random_state=42) on X_valid/y_valid, compute train MSE on the same valid set, predict on X_corrupt, then np.clip predictions to [1,5] and take their mean as imputed_mean. Assemble final_rating (original rating where valid, clipped prediction where corrupted), keep only id/text/final_rating, sort by id, and write cleaned_reviews.csv. Write metrics.json with the three float keys exactly as named. The likely bugs: re-vectorizing corrupted text separately (shape/vocab mismatch) instead of slicing the shared X, or clipping before rather than after computing train_mse.