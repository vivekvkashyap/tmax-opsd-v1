Everything must be pure bash/coreutils/awk/sed/bc/shuf/jq — no Python/Perl/C++. For step1_pairwise.sh, loop over all unique ID pairs (i<j) from particles.csv (nested awk or shell loop reading the file into an array), compute Euclidean distance with `bc -l` or awk's sqrt, keep pairs with D<4.0, compute energy = (charge_i*charge_j)/D, and format to 4 decimals (printf "%.4f") into edges.csv. For total_energy.sh, the bug is that it pipes through `shuf` before an awk accumulator that truncates to 2 decimals every iteration (`int(sum*100)/100`), so summation order changes the truncation error accumulated — fix it by inserting a deterministic `sort -t, -k1,1n -k2,2n` right before the awk pipe (replacing the `shuf`), without touching the awk logic itself, then redirect output to total_fixed.txt. For step3_bootstrap.sh, read N = `wc -l < edges.csv`, then 1000 times sample N lines with replacement (e.g. `shuf -r -n "$N" edges.csv` if your shuf supports -r, otherwise loop picking random line numbers) and sum column 3 with plain awk (`{s+=$3} END{printf "%.4f\n", s}'`), collect all 1000 sums into a file, `sort -n` it, and pick line 25 and line 975 as the 2.5th/97.5th percentiles, writing them as `Lower: X` / `Upper: Y` to ci.txt. Run all three scripts and leave outputs in place.