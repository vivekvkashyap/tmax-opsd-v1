Build this as a straightforward `networkx` pipeline rather than hand-rolling graph algorithms: install `networkx`, load `/home/user/citations.csv` (header `source,target`) into a `DiGraph` via `nx.read_edgelist`/`pandas`+`add_edges_from`, ensuring node IDs stay integers (cast from CSV strings). Compute `nx.pagerank(G)` with default damping (0.85, default tolerance) and take the argmax as the "Top Paper." For the longest path *to* the top paper, don't run `nx.dag_longest_path` on the whole graph (that finds the longest path anywhere) — instead restrict to paths ending at the top node, e.g. compute longest-path lengths via a DAG topological/DP approach over predecessors, or reverse the graph and do a longest-path-from-source computation from the top node. For the ancestor subgraph, use `nx.ancestors(G, top_node)` plus the top node itself, build an induced subgraph, and export with `nx.write_graphml`. Write `/home/user/summary.json` with `top_paper_id` as int, `pagerank_score` rounded to exactly 4 decimals (Python `round(x, 4)`), and `max_depth_to_top` as int (edge count, not node count) — double check off-by-one between "number of edges" vs "number of nodes" in your longest-path computation.