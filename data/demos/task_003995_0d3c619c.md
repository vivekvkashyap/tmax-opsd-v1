Tackle each sub-analysis independently and verify its output before combining. For frame count, run `ffprobe -v error -select_streams v:0 -count_frames -show_entries stream=nb_read_frames -of csv=p=0 /app/experiment.mp4` (count_frames forces an accurate decode-based count rather than relying on container metadata, which can be wrong/missing). For the moving average, write a SQLite window query using `AVG(value) OVER (ORDER BY timestamp ROWS BETWEEN 2 PRECEDING AND CURRENT ROW)`, then pick the `id` with `ORDER BY ma DESC LIMIT 1` — test the raw query output with `sqlite3 -header /app/sensors.db "..."` before scripting it. For the taxonomy path, write a recursive `jq` function that walks `.children[]?`, appending `.name` to a path array, and emits the full dotted path once it finds the node named `"TargetAnomaly"` (e.g. `def walk(path): if .name=="TargetAnomaly" then (path+[.name])|join(".") else (.children[]? | walk(path+[.name])) end;`). Assemble the three values into `/home/user/www/data.json` with `jq -n --argjson ...`, then start `python3 -m http.server 8080 --bind 0.0.0.0 --directory /home/user/www &` and confirm with `curl -s http://127.0.0.1:8080/data.json` before finishing.