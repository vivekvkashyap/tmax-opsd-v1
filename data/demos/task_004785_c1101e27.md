Fix the three layers independently, then wire them together. In `/home/user/src/service.c`: replace the hardcoded `/var/log/service.log` path with `getenv("LOG_FILE_PATH")`, exiting with code 1 if it's NULL/unset before opening any file; keep using `localtime`+`strftime("%Z", ...)` for the timezone abbreviation but make sure `tzset()` is effectively invoked (setting `TZ` in the environment before running is enough since libc reads it on first `localtime` call) so `%Z` reflects the exported `TZ=UTC`. Compile with `gcc -o /home/user/bin/service /home/user/src/service.c` after `mkdir -p /home/user/bin`. In `/home/user/run_service.sh`: make every step idempotent (`mkdir -p /home/user/data_spool`, check-before-create), `export TZ=UTC` and `export LOG_FILE_PATH=/home/user/data_spool/service.log` before launching anything, and fix the broken SSH tunnel line to run in the background non-interactively: `ssh -N -f -o StrictHostKeyChecking=no -L 8888:127.0.0.1:9999 user@127.0.0.1` (the original script's plain `ssh -L ...` blocks forever without `-N -f`). Launch the compiled binary with `&`. Validate with `ss -tln | grep 8888` and by checking the log file content after running the script.
