Diagnose the stiffness before touching the solver: `k_deg_m = 1000.0` with `dt = 0.1` means each Euler step multiplies the mRNA error by roughly `(1 - 1000*0.1) = -99`, which explodes — that's why you need an implicit method. Replace the manual Euler loop with `scipy.integrate.solve_ivp`, defining a `dydt(t, y)` function returning `[k_tx - k_deg_m*y[0], k_tl*y[0] - k_deg_p*y[1]]` with `y0=[0,0]`, `t_span=(0,10)`, and `method='BDF'` (or `'Radau'`) — both are implicit and handle stiff systems without needing a tiny step size; leave `k_tx`, `k_deg_m`, `k_tl`, `k_deg_p` and the ODE right-hand side exactly as derived from GC content. Request dense output or evaluate at `t=10` directly (`t_eval=[...,10]` or read `sol.y[:, -1]`) to get the final Protein value, round to 4 decimals, and write it. Plot `sol.t` vs the Protein trajectory with matplotlib and save the PNG in the same script run. For the pytest file, structure `simulate()` so it's importable and testable without re-running the whole script (e.g. wrap the core logic in a function returning `(t, protein_array)`), and assert the final value is `math.isfinite(...)` and `> 0`.
