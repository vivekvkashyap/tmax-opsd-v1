First inspect the vendored tool's `Makefile` and source for OpenMP usage (`#pragma omp` in the .c file) — if the pragmas are present but the binary runs single-threaded, the compiler is silently ignoring them because the `-fopenmp` flag is missing from `CFLAGS` (and often also needed at the link step); add it and rerun `make`, then confirm with something like checking `omp_get_max_threads()` or observing multi-core CPU usage during a run. For the pipeline script, stream `/app/bin/h5_to_fasta.py /app/data/reads.h5` directly into your fixed `seqcalc` binary via a pipe (avoid writing an intermediate file, which costs time you can't afford under the 4-second budget). Feed `seqcalc`'s per-sequence GC-content output into a single `awk` invocation that accumulates count, sum, and sum-of-squares in one pass to compute mean and (population or sample, per spec) variance, printing both rounded to 4 decimals with `printf("%.4f", ...)`. Write the final line in the exact literal format `Mean: <value>, Variance: <value>` to `/home/user/stats_result.txt`. Time the whole script end-to-end with `time bash pipeline.sh` to confirm you're well under budget before considering it done.
