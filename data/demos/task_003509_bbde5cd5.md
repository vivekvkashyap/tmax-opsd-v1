Build the org chart from `employees.csv` as a parent-to-children adjacency map (manager_id -> list of emp_ids), then do a recursive/BFS traversal starting at Bob's emp_id to collect every descendant (direct and indirect reports) — don't just take direct reports. Union the server sets from `access.json` for Bob himself plus all those descendants to get Bob's base access set. Then parse `network.txt` into an undirected adjacency map and run BFS/DFS from all of Bob's base servers simultaneously (multi-source) to find every server reachable via any path — the whole graph may end up connected except isolated nodes, so don't assume the subgraph is small. Restrict degree computation strictly to edges where both endpoints are in the reachable subgraph (an isolated node correctly gets degree 0). Find the max-degree node, breaking ties by picking the alphabetically first server name (careful: use string comparison, not insertion order). Write exactly `ServerName,Degree` with no trailing newline/extra text to `/home/user/audit_result.txt`.