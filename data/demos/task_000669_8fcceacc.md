Diagnose the Go leak by reading `fetcher_test.go` first: it fails because when `ctx.Done()` fires and the function returns early, the worker goroutines are still blocked forever trying to send on the unbuffered `ch` (nobody is left reading). Fix it by making the channel buffered with capacity `len(urls)` so sends never block regardless of whether the consumer keeps reading, while keeping the function signature identical — don't add extra select-on-ctx logic inside each goroutine unless you also keep the signature and behavior test-compatible. For `aggregator.py`, replace the float accumulator with `decimal.Decimal` built from each price's string representation (`Decimal(str(p))`, not `Decimal(p)` directly, which would import float imprecision), sum those, and format via `str(total.quantize(Decimal('0.01')))` — the test also checks the source literally contains "Decimal", so make sure the import and usage are real, not just a comment. Write `regression_test.sh` to `cd` into the pipeline dir (or use absolute paths), run `go test ./...` and `python3 -m unittest test_aggregator.py`, propagate a non-zero exit if either fails, and `chmod +x` it. Do not edit the two test files — run them as-is to confirm both fixes actually pass.
