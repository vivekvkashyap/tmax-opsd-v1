First isolate the bug: open `janitor/functions/fill.py`'s `fill_empty` and look for logic that special-cases integer columns — it likely converts to float and then assigns `np.nan` instead of the actual fill value; replace that branch so it just does a straightforward `fillna(value)` (or equivalent) that preserves dtype when the fill value and existing non-null values are integral. Reinstall the package locally (`pip install -e /app/pyjanitor` or equivalent) so your fix takes effect, and sanity-check on a small integer-column DataFrame with NaNs before moving on. For modeling: load the CSV, apply the fixed fill, split `target` from features, then loop `i` in `range(10)`, drawing a bootstrap sample via `df.sample(n=len(df), replace=True, random_state=i)` and fitting `MLPRegressor(hidden_layer_sizes=(32,16), activation='relu', max_iter=500, random_state=i)` on each — keep the 10 fitted models in memory (or pickle them) for serving. For the server, use Flask/FastAPI, check the `Authorization: Bearer secret-ds-token-8819` header on every request (401/403 otherwise), and on `/predict` compute `mean` and sample stdev (`ddof=1`) across the 10 models' predictions for the given feature dict. Launch it detached (e.g. `nohup ... &`) so it stays alive, then curl-test it yourself.
