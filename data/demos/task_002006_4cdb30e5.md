This is three separate subproblems stacked together — solve and verify each in isolation before wiring them together. (1) Numerical instability: when computing the pseudo-inverse of the Laplacian for effective resistance, don't rely on a plain Cholesky/LU solve on a possibly-singular/near-singular matrix; instead regularize by adding `epsilon * I` (eps=1e-6) before inversion, or compute eigenvalues/eigenvectors via `gonum`'s `mat.EigenSym` and reconstruct the pseudo-inverse using only eigenvalues above the threshold (zeroing out near-zero modes) — this handles disconnected components gracefully. (2) Performance: profile first to find the actual bottleneck (likely frame decoding or the O(n^3) matrix work per frame); parallelize per-frame processing with a worker-pool of goroutines feeding results into a synchronized slice/channel, and avoid reallocating matrices every frame. (3) KL divergence: bin both frames' pairwise-distance histograms into the same 20 fixed-width bins over [0,200], normalize to probabilities, add 1e-9 smoothing to zero bins, then sum `p*log(p/q)` only over bins where p>0. Validate frame-extraction and centroid-detection correctness on frame 0 alone before running the full 300-frame pipeline, and re-time after each optimization to confirm you're under 5s.