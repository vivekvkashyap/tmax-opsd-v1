Work in this order: first run `tesseract /app/routing_spec.png` (try a couple of `--psm` values if the table doesn't parse cleanly) to recover the route/rate-limit/parameter table — don't guess at it. Then inspect `url_parser.cpp`: the crash pattern described (string_view returned from a function that builds a local std::string during parameter splitting) is a classic dangling-reference bug — the local string's buffer is freed when the function returns, so the view points at freed memory. Fix it by returning `std::string` by value (or storing the split results in a container owned by the caller) instead of a view into a temporary. In `router.cpp`, implement `validate_request` to: parse the path against the OCR'd routes and their allowed parameter names, reject unknown params or `../` traversal or disallowed characters, and enforce per-route requests-per-minute by processing timestamps in chronological order with a sliding/rolling window per account or route. Build via the given CMakeLists so the binary lands at the exact required path, then run it against both corpora and diff behavior — a single false ACCEPT or false REJECT means the parsing or rate-limit window logic still has an edge case.
