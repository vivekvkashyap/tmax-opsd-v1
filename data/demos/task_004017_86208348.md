Start by inspecting the schema yourself — run `sqlite3 /home/user/research_data.db ".schema"` (or `.tables` then `.schema <table>`) rather than assuming table/column names, since the task explicitly says they're undocumented and you must discover the actual names and the foreign-key chain linking patients -> trial assignments -> observation metrics. Init a Go module in /home/user/ (`go mod init ...`, `go get github.com/mattn/go-sqlite3`), and write organize_data.go using the `flag` package for --min-age and --condition, with a parameterized query (using `?` placeholders, not string concatenation) that joins the three tables on their actual key columns, filters by age>=min-age and exact condition match, and computes AVG(recovery_days) GROUP BY treatment_arm. Apply the validation pass in Go after querying: keep only treatment_arm values that are exactly "Arm A", "Arm B", or "Placebo" (case-sensitive, exact match — reject anything else including NULL/unknown labels) and require avg_recovery_days >= 0.0. Round each average to 2 decimals, marshal as a JSON array sorted alphabetically by treatment_arm, write to /home/user/summary_results.json, and run the compiled program once with --min-age 30 --condition "Asthma" to produce the file.
