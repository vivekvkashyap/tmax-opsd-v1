Use C++17's `<filesystem>` to recursively walk `/home/user/cloud_storage`: for each entry, the first path component under the root is the username (derive it from `relative_path` of the entry with respect to the storage root, taking its first segment), and `path.extension()` gives you the file suffix to classify. Accumulate per-user total bytes (via `file_size(entry)`) unconditionally, and per-user total cost by multiplying bytes by the rule-specific multiplier: 1 for `.log`, 5 for `.db`, 0 for `.tmp`, 2 for anything else (including no extension) — implement this as a small if/else chain comparing the extension string exactly. Store results in a `std::map<std::string, ...>` so iteration is already alphabetically sorted by username, then write the CSV with the exact header `Username,TotalBytes,TotalCost`. In `run_analysis.sh`, compile with `g++ -std=c++17 cost_analyzer.cpp -o cost_analyzer` (note: some environments need `-lstdc++fs` for filesystem support), check the compiler's exit code and `exit 1` on failure before attempting to run the binary, then execute it and confirm `cost_report.csv` exists. Test the C++ logic against the given sample directory and manually recompute one user's expected totals by hand to catch multiplier or extension-matching mistakes.
