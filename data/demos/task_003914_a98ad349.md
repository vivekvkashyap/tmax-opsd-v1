Diagnose the two failures independently before running anything end-to-end. First, `pip install grpcio grpcio-tools`, then from inside `/home/user/project/` run `python -m grpc_tools.protoc -I. --python_out=. --grpc_python_out=. service.proto` to generate `service_pb2.py` and `service_pb2_grpc.py` — check they import cleanly with `python3 -c "import service_pb2, service_pb2_grpc"` before moving on. Second, inspect the existing `Makefile` rule for `libfastparser.so`: a plain `gcc -o libfastparser.so fast_parser.c` produces an executable, not a loadable shared object, which is why `ctypes.CDLL` fails. Fix the compile line to include both `-shared` and `-fPIC` (position-independent code is required for a `.so` that gets loaded into another process's address space), e.g. `gcc -shared -fPIC -o libfastparser.so fast_parser.c`. Run `make` and confirm with `file libfastparser.so` that it reports "shared object", not an executable. Only then run `python3 /home/user/project/test_server.py > /home/user/result.log` and check the log for the success line — if it fails, re-check import errors first (missing pb2 files) before touching the C build again.