Start by understanding why the current Makefile picks the wrong directory: `ls -d lib-* | sort | head -n 1` does lexicographic string sort, which is wrong for semver (e.g. "1.11.0" sorts before "1.5.2" as a string, and neither beats "2.0.5" the way you'd want with a naive sort). Fix the version selection using `sort -V` (version-sort) and `tail -n 1` to reliably get the highest semantic version directory. For the shared library, compile with position-independent code and the shared flag, e.g. `g++ -shared -fPIC -o libmathalg.so $(LATEST_LIB_DIR)/mathalg.cpp`. For linking `test_suite`, link against the library and set an rpath pointing at the current directory so it resolves at runtime without `LD_LIBRARY_PATH`, e.g. `g++ -o test_suite test_suite.cpp -L. -lmathalg -Wl,-rpath,'$$ORIGIN'` (rename/symlink to `libmathalg.so` naming convention `-lmathalg` requires, or link the .so directly by path). Do not touch `test_suite.cpp` or any `mathalg.cpp`. After `make`, verify with `ldd test_suite` that it resolves `libmathalg.so` without needing `LD_LIBRARY_PATH`, then run `./test_suite > test_result.log` and check the output confirms the highest version (2.0.5) was linked and the test logic passed.