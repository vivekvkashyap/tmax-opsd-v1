For part 1, don't manually inspect commits — run `git bisect start`, mark the current HEAD as `bad` and the earliest commit as `good`, then at each step run `python3 test_edge.py` (or a wrapper script) to decide `git bisect good`/`git bisect bad`; let bisect converge to the exact offending commit, capture its full 40-char hash (`git rev-parse HEAD` while bisect is paused on it, or from `git bisect log`), write it to `/home/user/bad_commit.txt`, then `git bisect reset`. Inspect that commit's diff to understand what broke convergence handling in `wrapper.py` (look for a hardcoded/too-low iteration or tolerance limit) and fix it so slow-converging-but-valid cases succeed, then confirm `test_edge.py` passes. For part 2, treat `/app/root_finder` as fragile and write `filter.py` to validate CSV inputs mathematically before ever invoking the binary: parse `a,b,c,d,x0`, reject non-numeric/NaN/malformed fields, compute the cubic's derivative $f'(x_0)=3ax_0^2+2bx_0+c$, and reject (exit 1) whenever $|f'(x_0)|$ is very small (near-zero derivative causes Newton-Raphson division blowup) or other structural issues are present; accept (exit 0) otherwise. Validate your filter against both provided corpora directories before assuming it generalizes to the hidden ones.