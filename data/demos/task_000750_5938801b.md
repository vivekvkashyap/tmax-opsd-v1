First run OCR on the image to recover the exact lambda value: install `tesseract-ocr` and `pytesseract`, then `pytesseract.image_to_string(Image.open('/app/baseline_model.png'))` and parse the numeric lambda from the returned text — do not guess it. Then implement `/home/user/compute_kl.py` exactly per spec, being careful about order of operations: (1) parse the comma-separated floats, clamp any value below 1e-9 up to 1e-9, THEN normalize by dividing by the sum so P sums to exactly 1.0; (2) compute the unnormalized Poisson pmf Q_raw(k) = lambda^k * e^-lambda / k! for k=0..N-1 using the recovered lambda, clamp values below 1e-9, THEN normalize to get Q; (3) compute KL divergence sum(P*ln(P/Q)) with natural log; (4) print only the value formatted to exactly 6 decimal places, no extra text. Common pitfalls: normalizing before clamping (wrong order), using math.factorial incorrectly for large k causing overflow/precision issues, or printing extra whitespace/labels that break exact-match fuzz comparison against the oracle. Test against several comma-separated inputs of varying length to confirm numerical stability.