Build this bottom-up: get each component working standalone before wiring them together under supervisord. Write `backend.py` using Python's built-in `http.server`/`BaseHTTPRequestHandler` to accept a port arg, handle POST `/upload` by reading `Content-Length` bytes from the request body and appending them to `/home/user/app_data/backend_<port>.dat`, then respond `200 OK from <port>`. Write `lb.py` as a simple HTTP server on 8080 that round-robins POST requests across ports 8081-8083 (e.g. using `http.client` or `urllib.request` to forward the body and relay the backend's exact response back to the caller — track an index that increments each request, wrapping modulo 3). Write `monitor.py` as a loop that sleeps 2s, sums file sizes under `/home/user/app_data/`, and if the total exceeds 1,048,576 bytes, finds the largest `.dat` file (`max` by `os.path.getsize`), deletes it, and appends a timestamped alert line in the exact required format. For supervisord, define one `[program:x]` stanza per process (3 backends with distinct port args, lb, monitor) each with `autorestart=true`, point logs at `/home/user/supervisor_logs/`, then launch with `supervisord -c /home/user/supervisord.conf` and leave it running.