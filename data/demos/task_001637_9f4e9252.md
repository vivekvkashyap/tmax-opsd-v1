Set up the environment correctly first: `python3 -m venv /home/user/venv`, `source /home/user/venv/bin/activate`, then `pip install numpy scipy`, and run your script with this venv's python for every subsequent step (a common failure is installing packages in the venv but then invoking the script with system python). Write a FASTA parser that handles multi-line sequences generically (accumulate lines until the next `>` header or EOF) rather than assuming one line per sequence. Build the Kyte-Doolittle lookup table exactly as given and map each residue character to its float value, raising/skipping clearly if an unexpected character appears rather than silently defaulting. For the spectral step, use `numpy.fft.fft` on the raw hydrophobicity array (length N), take squared magnitude `|X[k]|^2` for the power spectrum, and slice `power[1 : N//2 + 1]` (integer floor division) to exclude DC and keep only the specified positive-frequency band — off-by-one errors in this slice are the most likely source of wrong answers. Integrate that slice with `numpy.trapz` (or `scipy.integrate.trapezoid`) using default unit spacing. Write `Sequence_ID,Total_Energy` as CSV header, one row per protein in file order, with energy formatted to exactly 2 decimal places, and diff your output against nothing but your own recomputation logic to sanity check before finishing.
