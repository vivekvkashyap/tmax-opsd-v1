Treat this as three independent sub-tasks and verify each before moving on. First, extract the token: run `strings /home/user/audit/target_binary | grep -i admin` (also try `objdump -s -j .rodata`) to locate the hardcoded string beginning with `X-Admin-Token: `; copy exactly what you find, since even whitespace differences will break the matching logic later. Second, write `cookie_extractor.cpp` to read `http_logs.txt` line-by-line, tracking the most recent `IP:` line seen, and when a `Cookie:` line containing the exact extracted token substring appears, emit the tracked IP; compile with `g++` and run it, then inspect `admin_ips.txt` to sanity-check the IPs look plausible against the log content. Third, for privilege escalation, recursively search `/home/user/audit/sys_files/` for world-writable files using `find /home/user/audit/sys_files -type f -perm -002` (checking the "others write" bit specifically, not just permissive-looking files), and write the single matching absolute path to `vuln_file.txt` — if multiple candidates appear, re-check your permission bitmask logic rather than guessing. Keep each output file's format minimal and exact (no extra headers or blank lines) since exact-match verification is likely.
