Build this as two layers wired by FFI, not one Rust program guessing at parsing. First compile the C file to a shared object with position-independent code: `gcc -shared -fPIC -o /home/user/libcalc.so /home/user/calc.c`. Then write the Rust side: declare `extern "C" { fn evaluate_addition(expr: *const c_char) -> c_double; }`, convert each Rust `&str` line into a `CString` before passing it across the FFI boundary (this is the step naive attempts get wrong — passing a raw `&str` pointer will not work), and call the function inside an `unsafe` block. Read `/home/user/requests.txt` line by line, applying validation in order: skip lines starting with `#`, skip lines longer than 15 characters, then stop entirely once 3 valid lines have been evaluated (don't just skip further ones — halt processing). Format each accepted result with exactly one decimal place and write `<original_line> = <result>` to `/home/user/results.log`. Compile with `rustc processor.rs -L /home/user -l calc`, then run with `LD_LIBRARY_PATH=/home/user ./processor` so the dynamic linker can locate `libcalc.so` at runtime.