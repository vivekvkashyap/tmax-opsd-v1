Diagnose the build failure directly: run `pip install -e /app/primer_score-0.1.0` and read the compiler error — it will point at an unrecognized `-std=` flag passed via `extra_compile_args` in `setup.py`. Open `setup.py`, find that flag (likely referencing a nonstandard C standard string GCC doesn't recognize), and change it to a valid one like `-std=c99` (or remove the flag entirely), then reinstall and confirm `import primer_score; primer_score.compute_affinity("ATGC")` succeeds before writing the analysis script. In `analyze_primer.py`, take `sys.argv[1]` as the sequence, call `compute_affinity` to get array A, compute the forward difference `D[i] = A[i+1] - A[i]` for consecutive elements, then apply the trapezoidal rule to `|D|` exactly as specified — sum `(|D[i]| + |D[i+1]|) / 2` over consecutive pairs of D, not of A. Since your output will be checked bit-for-bit against a hidden oracle across many random sequences, use straightforward, order-preserving float arithmetic (avoid reordering summation or using numpy reductions that might round differently) and format the final print with `f"{value:.4f}"`, printing nothing else to stdout. Test manually on a few sequences of varying length to make sure edge cases (very short sequences) don't crash before considering it done.