Treat this as three independent subtasks, validating each in isolation. (1) WAL recovery: scan corrupted.wal byte-by-byte for the 4-byte magic `WAL1`, read the next 4 bytes as little-endian uint32 length, read that many payload bytes, read a trailing 4-byte little-endian CRC32(IEEE), and emit the payload only if the computed CRC32 matches; on any failure don't crash — advance one byte and resume scanning for the next magic occurrence. (2) Sanitizer: diff the evil vs clean corpora (e.g. check the length of the `auth_token` field in each) to find the threshold the crash log implies, then have sanitizer.go parse the JSON, check that field's length against the threshold, and exit 1 for evil / 0 for clean — test against both provided corpora first. (3) Video: extract frames with ffmpeg, compute mean pixel intensity per frame (Python/PIL or `identify -format "%[mean]"`), and locate the contiguous run below the given threshold, writing the actual discovered `START-END` frame numbers.