Build the pipeline as a sequence of clear pandas steps, logging progress with Python's `logging` module configured to write INFO-level messages to `/home/user/pipeline.log` (set up the FileHandler and formatter before any processing, and log "Pipeline started" first). Load `/home/user/raw_surveys.csv`, then use `pd.melt(df, id_vars=['user_id','age'], value_vars=['q1_response','q2_response','q3_response'], var_name='question_id', value_name='raw_text')` to reshape — log the resulting row count as "Melted data: X rows". Drop rows where `raw_text` is NaN or an empty/whitespace-only string (after `.str.strip()`), then deduplicate with `drop_duplicates(subset=['user_id','question_id'], keep='first')` — log "Cleaned data: Y rows" after this step. Build `normalized_text` from `raw_text` by lowercasing, stripping all non-alphanumeric-non-space characters (e.g. `re.sub(r'[^a-z0-9\s]', '', text)`), collapsing repeated whitespace to a single space, and trimming ends — watch out for hyphens and punctuation merging words together unexpectedly (they should just disappear, not add a space). Select exactly the four output columns, sort by `user_id` then `question_id`, write to `/home/user/final_surveys.csv` with `index=False`, then log "Pipeline completed".