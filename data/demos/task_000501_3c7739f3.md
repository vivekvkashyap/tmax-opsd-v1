Two independent bugs to fix in `telemetry_parser.c` — locate both before recompiling. First, the parser currently splits the bracketed sensor name with a format specifier that stops at whitespace (e.g. `%s` inside `[%s]`), so multi-word names like "Pressure Sensor A" get truncated; fix this by matching everything between `[` and `]` explicitly (e.g. locate the brackets with `strchr`/pointer scanning or a `%[^]]` scanset in `sscanf`) rather than a naive `%s`. Second, the variance calculation currently accumulates `sum` and `sum_sq` and computes `E[X^2]-E[X]^2`, which loses precision for large values with tiny spread; replace it with Welford's online algorithm (track `count`, `mean`, `M2`, updating incrementally per sample) using `double` throughout, and report sample variance as `M2/(count-1)`. After fixing both, `make`, run the binary on `traffic.pcap`, and write the four-line report to `/home/user/forensics/report.txt` in the exact specified format, rounding mean to 2 and variance to 4 decimal places. Install `libpcap-dev` first if headers are missing.
