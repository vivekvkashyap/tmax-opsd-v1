Build this as one recursive CTE plus window functions, no temp tables and no procedural loop. Structure: (1) a base case selecting rows where `parent_id IS NULL` with `depth=0` and `path_duration=duration`, and `root_id=id`; (2) a recursive case joining `tasks t JOIN cte c ON t.parent_id = c.id`, incrementing depth by 1, adding `t.duration` to the running `path_duration`, and propagating `c.root_id` unchanged. Then wrap the recursive CTE in an outer SELECT that adds `RANK() OVER (PARTITION BY root_id ORDER BY path_duration DESC, id ASC) AS rank_in_root` — remember RANK (not DENSE_RANK/ROW_NUMBER) so ties share a rank and skip the next value, and the secondary ORDER BY id ASC only breaks ties, it doesn't affect the primary ordering. Finish with `ORDER BY id ASC` in the final output. Use `sqlite3 -header -csv /home/user/tasks.db "<query>"` redirected to the CSV file inside the executable script. Verify column order and header spelling exactly match the spec, and test against the actual hierarchy (multiple roots, multi-level nesting) before finalizing.