Build this as a straight pipeline: (1) load `items.csv` and `bom.csv` into an SQLite DB with `.mode csv` + `.import`, or via Python's `sqlite3`/`csv` modules; (2) add indexes explicitly — `CREATE INDEX idx_bom_parent ON bom(parent_id)` for downward traversal and rely on `items.item_id` as PRIMARY KEY (or an explicit index) for lookups, since a naive import without a schema won't create these; (3) write the query as a `WITH RECURSIVE` CTE that starts from the target item with an initial multiplier of the target quantity, and at each recursion step multiplies the running quantity by the edge's `quantity` column as it descends — the classic bug here is summing quantities instead of multiplying them down the path, or forgetting to carry the multiplier through each recursive step. (4) Filter the recursive result to rows where `items.item_type = 'raw_material'`, GROUP BY leaf item and SUM the multiplied quantities (a raw material may be reachable via multiple paths, so aggregation is required, not just picking the deepest leaf). (5) Export to CSV sorted alphabetically by `leaf_item_id`, with exactly the header `leaf_item_id,leaf_item_name,total_quantity_needed`. Test the recursive query on paper with a small subgraph before trusting the full output.
