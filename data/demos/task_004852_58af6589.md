Fix the leakage by reordering the pipeline: read all rows, split chronologically into train (first 80%) and test (last 20%) BEFORE computing any statistics. Compute the mean and population standard deviation (divide by N, not N-1) using only the training rows' `value` column. Then apply that single (mean, std_dev) pair to normalize both the training rows and the test rows — the test set must reuse the training-derived parameters, never its own. Watch for two common mistakes: (1) computing mean/std over the full dataset before splitting (the original bug), and (2) accidentally recomputing separate statistics for the test set instead of reusing the training ones. Keep the `format!("{:.4}", ...)` formatting and output CSV header exactly as specified. After editing `main.rs`, rebuild with `cargo build --release` and run the resulting binary so both output CSVs are regenerated from scratch (delete old outputs first if unsure they'll be overwritten). Verify by spot-checking that the first train row and first test row values are consistent with a single shared mean/std derived only from the 80 training values.