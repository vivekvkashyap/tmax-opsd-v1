In `etl_filter.sh`, load the CSV into a fresh SQLite database (in-memory `:memory:` won't persist across `sqlite3` CLI invocations, so use a temp file DB per run, e.g. under `/tmp`) with `.import` or a `CREATE TABLE` + `.mode csv` + `.import`, then immediately `CREATE INDEX idx_parent ON accounts(parent_account_id);` before running any hierarchical queries. Implement each rule as its own SQL check: (1) cycle detection via a recursive CTE that walks parent chains and fails if a node reappears in its own ancestor path (or if the recursion doesn't terminate/hits a depth cap); (2) max depth via a recursive CTE computing depth from each root (`parent_account_id` empty) and checking `MAX(depth) <= 7`; (3) summary balance via `GROUP BY parent_account_id` summing child balances and comparing to the parent's own balance where `account_type='Summary'`; (4) leaf constraint via checking no row's `parent_account_id` matches an `account_id` whose `account_type='Leaf'`. Run each query, and if any fails, `exit 1` immediately; only `exit 0` if every check passes. Test your script against your own crafted valid/invalid CSVs covering each rule individually before trusting it broadly.
