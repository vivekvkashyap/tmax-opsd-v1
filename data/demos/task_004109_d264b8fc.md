Handle the C extension and the API as two separately-verifiable stages. First, in `solver.c`, hunt specifically for a loop bound that reads one element past the valid range (a classic pattern is `for (i = 0; i <= size; i++)` where it should be `i < size`) causing out-of-bounds access on large matrices — fix the comparison operator without altering the constraint-solving algorithm, then rebuild and sanity-test with a matrix sized well beyond any small fixed test case to confirm no crash. Second, in `setup.py`, add aggressive compiler optimization flags (e.g., `extra_compile_args=['-O3']` in the `Extension(...)` definition) since the unoptimized build is what's causing the latency failure — reinstall with `pip install -e .` or `python setup.py build_ext --inplace && pip install .` and confirm the change took effect by timing a single `cspsolver.solve()` call in a Python REPL before wiring up the server. Only then write `/app/server.py` with FastAPI's `POST /solve` calling `cspsolver.solve(matrix)` inside a try/except that returns `{"error": ...}` on invalid input instead of crashing. Start it with `uvicorn` on port 8000 and run `/app/benchmark.py` yourself first to confirm you're under the 15ms threshold before considering the task complete — don't rely on the final verifier as your first test.