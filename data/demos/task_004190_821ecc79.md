Build this as a small ETL pipeline, not a one-pass parser. First write a `std::regex` to capture the five fields from each line; skip lines that don't match instead of crashing. Normalize `Target` (lowercase, then collapse repeated slashes with a regex like `//+` -> `/`) before using it as a dedup/grouping key — do this normalization once and reuse the result everywhere. For deduplication, keep a `map<string /*normalized target*/, pair<Action, Details>>` of the last accepted event per target, and compare each new event against only that target's last-accepted tuple (not a global last event) before accepting or discarding it. Extract `lines_added` with a small regex on `Details`, defaulting to 0 when absent. Truncate timestamps to the hour by zeroing minutes/seconds in the string (keep it a string, don't parse to time_t) and accumulate per-hour-bucket counters (total changes, a `set<string>` of unique normalized targets, and summed lines) in a `map<string,...>` so bucket keys come out chronologically sorted for free. Write the CSV with the exact header, verify column order and that compilation with `-std=c++17 -O2` succeeds, then run the binary and inspect `summary.csv` before declaring done.
