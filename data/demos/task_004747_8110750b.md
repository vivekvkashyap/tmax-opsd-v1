Fix each broken piece independently and verify it in isolation before wiring the system together. In `nginx.conf`: declare a `limit_req_zone` in the `http` block (e.g. keyed by `$binary_remote_addr`, rate `5r/s`), then reference it with `limit_req` inside the `/api/` `location` block that does `proxy_pass http://127.0.0.1:8000;`, with `listen 8080;`; test syntax with `nginx -t -c <path> -p <prefix>` before starting it. In `app.py`, fix the route to read `request.args.get('seq')` and `request.args.get('file')` (not whatever mismatched names exist currently), call `transcriber.py` on `/app/audio/<file>.wav` to get spoken number words, convert both that list and the comma-split `seq` values to integers, and sort with `sorted(merged, key=int)` (or sort the int list directly) instead of default string sort — string sort is the named bug and will misorder anything past single digits. Start Flask bound to `127.0.0.1:8000` and then start nginx with the fixed config, both backgrounded (e.g. `nohup ... &` or `setsid`), and manually curl `http://127.0.0.1:8080/api/merge?seq=...&file=...` plus fire rapid repeated requests to confirm the rate limiter actually returns 503 before considering it done.
