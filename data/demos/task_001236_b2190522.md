Design `operator.py` around content-addressing: for each `.yaml` file, compute its SHA-256 over the exact current bytes on disk, and use that hash (not a version counter) to determine both the backup filename and whether a backup already exists — this is what makes reruns naturally idempotent, since an unchanged file always hashes to the same backup name and the "does it exist" check becomes a simple file-existence test. Write the operator to iterate files, compute the hash, build the expected `<filename>.<hash>.bak` path, and only copy + write the `.eml` notification if that exact path is missing — no other state tracking needed. Match the `.eml` content byte-for-byte against the required template (To/Subject/Hash lines). Walk through the required demonstration sequence literally and check results after each step: run once (expect 1 backup+email), run again immediately (expect zero new files — verify by listing directory contents before/after), append the exact modification string to the yaml, run a third time (expect exactly one more backup+email, for a hash different from the first). Confirm final state has exactly two `.bak` and two `.eml` files before finishing.