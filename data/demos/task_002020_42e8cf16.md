Fix the C source first: open `/home/user/account_monitor.c` and replace the relative-path string literals for the database and HTML report with their absolute equivalents so the tool works regardless of the caller's cwd, then compile with `gcc` to `/home/user/account_monitor`, creating `/home/user/public_html` if it doesn't exist. For automating the interactive script, write an Expect script that `spawn`s `/home/user/interactive_admin.sh`, uses `expect "Enter new username: "` then `send "charlie\r"`, `expect "Enter password: "` then `send "secure_pass_123\r"`, and finally `interact` or `expect eof` to let it finish — run this script so `charlie` actually lands in `users.db`, then manually invoke your compiled `account_monitor` binary once to regenerate the report. For the web layer: generate a self-signed cert/key with `openssl req -x509 -newkey rsa:... -keyout key.pem -out cert.pem -days ... -nodes`, write a small Python `http.server`-based script wrapped in an `ssl.SSLContext` (loading that cert/key) serving `/home/user/public_html` on `0.0.0.0:8443`, then create a systemd **user** unit file under `~/.config/systemd/user/monitor-web.service` pointing at that script, and bring it up with `systemctl --user daemon-reload && systemctl --user enable --now monitor-web.service`. Verify end-to-end with `curl -k https://localhost:8443/report.html`.