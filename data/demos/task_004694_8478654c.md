For the pcap: use `tcpdump -r /home/user/incident/traffic.pcap -A` (or `-X` for hex+ASCII) to dump packet contents, `grep` for the literal string `FATAL_POISON`, then read the corresponding packet's IPv4 header to get the source address (tcpdump's parsed output usually prints `src > dst` directly; if the pcap is minimal/synthetic, fall back to reading the IP header bytes at the right offset with `xxd`/`od` and decoding the 4-byte source address field). For the core dump: run `strings /home/user/incident/core.dmp | grep -E 'TOKEN-[0-9a-fA-F]{32}'` to pull the token pattern directly from the raw bytes — no need for a full debugger since it's a memory dump, not a live process. For the script: `compute_checksum` recurses without a base case — add `if n <= 0: return 0` (or similar) so it terminates while preserving the "sum down to 0" semantics; separately, install the exact pinned dependency with `pip install urllib3==1.26.15` (use `--break-system-packages` if needed) since the script hard-checks `urllib3.__version__`. Run the fixed `parser.py` to get its printed checksum, then write all three findings into `/home/user/report.txt` matching the exact three-line format given, using the real IP, real token, and real checksum you derived.
