Use `ffmpeg` (or an OpenCV/Python helper you write) to decode `/app/spectroscopy.mp4` frame-by-frame and compute each frame's mean grayscale pixel value, producing your 1D signal array — save it to a reusable location (e.g. a header/data file your harness includes) rather than recomputing it inside every compile. Write the C test harness so it: reads that fixed signal, tiles/repeats it 100,000× into one big float array, computes a robust reference sum yourself in `double` precision (or Kahan summation) as the ground truth, then dynamically links/compiles the candidate `integrate.c` file together with your harness (e.g. `gcc harness.c candidate.c -o test_bin -lm`) and runs it, comparing `fabs(result - reference) / fabs(reference) < 0.001` to decide accept/reject. The core risk: don't compute your own "true sum" using naive `float` accumulation either, or your baseline itself will be unstable and unable to distinguish clean from evil implementations — accumulate the reference in `double`. `detect.sh` should take `$1` as the candidate path, do the compile+run+compare, and `exit 0`/`exit 1` accordingly; test it manually against a couple of files in each corpus directory before trusting it against the full hidden set.
