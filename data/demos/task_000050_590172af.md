Diagnose the nondeterminism at its root before patching: the bug is (1) iterating an unordered `set` (`largest_cc`) to build the per-node signal list, and (2) reducing FFT results via `pool.imap_unordered`, both of which let floating-point addition happen in a different order each run — and since float addition isn't associative, the sum's low bits drift. Fix both: after computing `largest_cc = max(nx.connected_components(G), key=len)`, immediately do `sorted_nodes = sorted(largest_cc)` and build the signal list from that fixed order. Drop the multiprocessing pool and any injected `time.sleep`/`random` jitter entirely — compute each node's FFT peak power sequentially in a plain loop over `sorted_nodes`, accumulating into `total_power` in that exact order, with no re-sorting of results afterward. Keep the FFT/peak-power computation itself and the print formatting identical to the original (same precision) so only the ordering changes. Save this corrected version as `/home/user/fixed_aggregate.py`, run it, and redirect/capture its stdout into `/home/user/stable_result.txt`; re-run it a second time to confirm the output is byte-identical before finishing.