Set up a Cargo project at `/home/user/analyzer` with `serde`/`serde_json` in `Cargo.toml`. Parse `metadata.csv` into a map from `paper_id` to `impact_score`, and `citations.txt` into an adjacency list of directed edges. Since the problem guarantees cycles are disjoint, find them with a straightforward DFS-based cycle detection: track visited/in-progress nodes per traversal, and when you re-encounter a node currently on the DFS stack, extract the cycle as the stack slice from that node onward — no need for a general Tarjan/Johnson's algorithm here, but do make sure you only report each disjoint cycle once (skip nodes already assigned to a found cycle) rather than re-detecting it from every starting node. For each cycle, sort its `paper_id`s lexicographically for the `"cycle"` array, and look up the max `impact_score` among its members for `"max_score"`. Sort the top-level array of cycle-objects descending by `max_score`, breaking ties by ascending first-`paper_id`. Serialize with `serde_json` to `/home/user/output/cycles_report.json`, matching the exact key names `cycle` and `max_score`.