Work the three phases independently and verify each before moving on. Phase 1: inspect `/app/tinyexpr/Makefile` for aggressive optimization flags (`-ffast-math`, `-Ofast`) that break IEEE-754 compliance and cause `make test` precision assertions to fail — remove the offending flag, keep others, and re-run `make test` until it exits 0. Phase 2: since `formulas.db` is corrupted but `-wal` is intact, don't repair the header by hand — instead point a fresh/matching SQLite file at the same name, place the `-wal` (and `-shm` if present) beside it, then open with `sqlite3` and let a `PRAGMA` checkpoint replay the WAL into a valid database; dump `clean_formulas.expression` and `evil_formulas.expression` into numbered `.txt` files in the two target directories. Phase 3: build `filter_expr` as a real evaluator (not a keyword blacklist) — parse and evaluate the expression (e.g. Python `ast` + `eval` with a restricted namespace), explicitly catch division-by-zero, domain errors, and overflow, and check finiteness (`math.isfinite`) on the result before choosing exit code 0 vs 1. Test against your Phase 2 corpora first.