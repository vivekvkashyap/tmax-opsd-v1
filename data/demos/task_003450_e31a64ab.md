Build and test each layer independently before wiring them together. First write `check_vm.exp`: use `spawn /home/user/bin/mock_qemu`, `expect "Press 'c' to continue boot..."`, `send "c\r"`, then `expect eof` (or similar) so the script prints all output including the final boot result and exits cleanly — run it standalone with `expect /home/user/check_vm.exp` and confirm you see the success message before moving on. Next, scaffold the Rust project with `cargo new vm_monitor` in `/home/user/`, and in `main.rs` use `std::process::Command::new("expect").arg("/home/user/check_vm.exp").output()` to capture stdout as a string; check it for the success marker (exit 0, do nothing) versus the failure marker or a non-zero/errored expect invocation (append the alert line to `/home/user/alerts.log`, creating it if needed, via `OpenOptions::new().append(true).create(true)`). Build with `cargo build` and manually test both branches by temporarily breaking `mock_qemu` to confirm the alert file gets written correctly. Finally write `/home/user/monitor_cron` containing a single standard 5-field cron line using `*/5 * * * *` pointing at the absolute path of the compiled debug binary — don't install it into the real crontab, just create the file.
