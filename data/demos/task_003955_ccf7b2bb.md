Build the three endpoints as independent, testable pieces of one HTTP server (any language is fine, but Python's http.server or a small Flask app is simplest). For `/video-analysis`, use `ffmpeg` frame-by-frame analysis (e.g. the `blackdetect` filter, or extract frames and check if every pixel is exactly RGB 0,0,0) against `/app/disk_monitor.mp4`, and count frames precisely rather than estimating from duration — test the ffmpeg command standalone first and read its actual output before wiring it into the handler. For `/storage-scan`, walk `/home/user/storage_dump/` using an approach that tracks visited real paths (e.g. `os.walk` with `followlinks=False`, or manually resolve symlinks with `os.path.realpath` and skip any that point to an already-visited ancestor) to avoid infinite loops from the deliberately planted symlink cycle; for each `.dat` file found, read its first 4 bytes and hex-encode them lowercase, then sort results by path before returning JSON. For `/clean-config`, parse the incoming XML body and strip `<loop_detected/>` elements (and their surrounding whitespace) — either with a real XML parser (safer, preserves structure) or a careful regex/text substitution if the input is always well-formed — and return the cleaned XML as the response body. Start the server listening on `127.0.0.1:8080` and keep it running (foreground or backgrounded) for the verifier; test each endpoint yourself with `curl` before finishing.