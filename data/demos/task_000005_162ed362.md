Treat the three parts independently. (1) Service: try `systemctl --user start datashaper.service` and read `journalctl --user -u datashaper.service` for the real failure — check `worker.py` for any environment-variable gate that causes an early exit (add the required `Environment=` line to the unit file if so) and check the vendored `Makefile`'s `install` target for a wrong file-permission mode on the installed script (fix the `install -m` bits so it's executable), then reload (`systemctl --user daemon-reload`) and restart until `is-active` reports `active`. (2) Networking: add a persistent local redirect from port 9090 to 8080 (e.g. `iptables -t nat -A OUTPUT/PREROUTING -p tcp --dport 9090 -j REDIRECT --to-port 8080`, or a lightweight socat/ssh forwarder if iptables access is restricted), and confirm idempotency by re-running your setup without duplicating rules. (3) Transform script: don't guess — script a loop that feeds `/app/oracle_transform` a range of crafted inputs (single chars, repeated-character runs, mixed runs, edge lengths) and diff the outputs against inputs to reverse-engineer the exact transformation, then implement matching logic in `/home/user/transform.py` reading stdin/writing stdout, and self-fuzz it against the oracle across random alphanumeric strings before finishing.
