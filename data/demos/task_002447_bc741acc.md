Create `/home/user/incoming` and `/home/user/outgoing`, then write `/home/user/organizer.sh` as an infinite `while true; do ... sleep 2; done` loop that globs `/home/user/incoming/*.tar.gz` (guard against the no-match literal glob with a nullglob check or `[ -e "$f" ]`). For each archive: extract into a fresh `mktemp -d` directory with `tar -xzf "$file" -C "$tmpdir"`, then `find "$tmpdir" -name '*.src' -type f` and run `iconv -f ISO-8859-1 -t UTF-8 "$f" -o "$f.tmp" && mv "$f.tmp" "$f"` on each (never edit non-`.src` files). To preserve the original archive's internal directory layout without a wrapper folder, `tar -czf /home/user/outgoing/<basename> -C "$tmpdir" .` (cd into the extraction root before taring, don't tar the tmpdir path itself). Count converted `.src` files with the `find` results and append `PROCESSED: <filename> | CONVERTED_FILES: <count>` to `/home/user/organizer.log`, then `rm` the original from incoming and the tmpdir. Make the script executable, launch it in the background (`nohup ./organizer.sh &`), copy the staging archives into incoming, wait a few seconds, and check the outputs.