Work this in clear phases and verify each before moving on. Phase 1 (OCR): install `ffmpeg` and `tesseract-ocr`, extract frames from `/app/backup_topology.mp4` (e.g. `ffmpeg -i backup_topology.mp4 -vf fps=1 frame_%03d.png`), run `tesseract` on each frame, and parse lines matching `REPLICATE: <src> -> <tgt>` with a regex — dedupe edges into a directed graph; sanity-check the extracted edge count/text since OCR can mis-read hyphens or characters. Phase 2 (analytics): compute out-degree centrality per the standard formula `out_degree(node) / (n-1)` where n is the total distinct node count across the whole graph (not just sources) — a common mistake is forgetting to count sink/leaf nodes with zero out-degree. Phase 3 (API): build a minimal Flask/FastAPI app bound to `127.0.0.1:9090` that checks `Authorization: Bearer DBRE-SEC-991` on every request and returns 401/403 otherwise; implement `/critical_nodes` with proper `limit`/`offset`/`min_centrality` filtering plus sort-by-centrality-desc-then-name-asc, and `/backup_chain/<node>` via a transitive-closure traversal (BFS/DFS over reachable nodes) sorted alphabetically. Launch the server detached (e.g. `nohup ... &`) and write its PID to `/home/user/api.pid`.