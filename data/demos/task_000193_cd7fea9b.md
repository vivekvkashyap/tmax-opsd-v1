Build this as a filter-then-aggregate pipeline (awk is a natural fit for the whole thing in one pass). Step 1: skip the header, then for each row validate that temperature parses as a number in [-50.0, 150.0] and humidity parses as a number in [0.0, 100.0]; use a numeric-format regex check (e.g. awk's `$3 ~ /^-?[0-9]+(\.[0-9]+)?$/`) before comparing, so empty or non-numeric fields are rejected rather than causing awk to silently treat them as 0. Step 2: truncate the ISO8601 timestamp to the minute by chopping the seconds portion (string slicing to replace `SS` with `00`, keeping the `Z` suffix) rather than doing full date arithmetic. Step 3: accumulate a sum and count per truncated-timestamp key (an associative array keyed by the minute string), then after processing all rows compute each average and format it to exactly one decimal place (`printf "%.1f"`, watching for correct rounding). Step 4: emit `timestamp,avg_temperature` header plus one row per minute that had at least one valid reading, sorted chronologically (`sort` on the timestamp field, which sorts correctly lexicographically since ISO8601 is fixed-width). Test against the given input before finalizing.
