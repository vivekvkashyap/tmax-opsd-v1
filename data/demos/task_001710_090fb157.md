Pipe /app/sensor_stream's stdout straight into sqlite3's CSV import (e.g. `sqlite3 /home/user/metrics.db ".mode csv" ".import /dev/stdin readings"` after running the binary, or import from a captured file) to load timestamp,value into a `readings` table. In Python, query all rows ordered by timestamp/rowid into a pandas Series/DataFrame and use `.rolling(window=200, min_periods=200)` to get the trailing rolling mean and std (this naturally leaves the first 199 rows as NaN, matching the "ignore first 199 rows" requirement) — do not implement a naive O(n*window) rolling loop unless you also want to hand-verify it matches pandas' definition of a trailing/inclusive window. Flag anomalies where `abs(value - rolling_mean) > 4 * rolling_std`, collect their timestamps into a JSON array of ints written to /home/user/anomalies.json, and double check your window is truly trailing (uses only current + 199 prior rows, not centered) since that's the most common source of a low F1 score. For the report, build /home/user/report.html with simple string formatting (f-string or .format, Jinja2 optional) embedding the anomaly count exactly as `<div id="anomaly-count">N</div>`.