Handle each data-quality issue explicitly rather than relying on defaults. For sensor_1.csv, read with `pandas.read_csv` (or Python's `csv` module) which correctly handles quoted fields containing embedded newlines by default — don't split the file on raw newlines yourself. For sensor_2.csv, open/read with `encoding='cp1252'` (or `windows-1252`) explicitly; detect this is needed by first trying utf-8 and noticing garbled/failing decode, or just knowing legacy Windows exports commonly use cp1252. After loading both, concatenate and merge with sensor_metadata.csv on sensor_id (left join so location is attached). For resampling: per sensor_id, set the timestamp as a DatetimeIndex, `resample('5min')`-align to the top of hour, then `.ffill(limit=3)` on temperature/humidity so gaps beyond 3 intervals stay null (and null rows should probably be excluded from signing/output). For each remaining valid row, build the exact pipe-delimited string `f"{ts_iso}|{sensor_id}|{temperature}"`, pipe it via subprocess to `/app/data_signer`'s stdin, and capture its stdout hex string as the signature column — call the real binary at request/build time rather than reimplementing whatever it does internally. Finally serve the joined, filled, signed data through the specified GET endpoint, sorted by timestamp, running the server in the background.
