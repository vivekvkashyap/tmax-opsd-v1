Structure cracker.cpp as three composable pieces: a base64 decoder, a wordlist-driven hash cracker, and a log-line parser, and test each against a known example before wiring them together. Implement base64 decoding manually with a standard lookup table mapping the 64 alphabet characters (plus '=' padding) back to 6-bit values, then pack 4 input chars into 3 output bytes — this is a well-known, mechanical algorithm, so get the padding/edge cases right rather than improvising. Include `custom_hash.h` and, for each auth_logs.txt line, parse out `user=`, `redirect_payload=`, and `hash=` fields (simple substring/regex extraction on the `key=value` format), then iterate the wordlist calling `compute_hash()` on each candidate and compare against the hex hash value converted to `unsigned int` (parse hex with `std::stoul(hash_str, nullptr, 16)`) — stop at the first match per line since the wordlist is small. Decode the base64 payload for the matched line and format output lines exactly as `username:cracked_password:decoded_redirect_url`, writing them to compromised_accounts.txt. Compile with `g++ -o cracker cracker.cpp` and run it. Separately, fix the file permission issue directly with `chmod 400 /home/user/ssh_key_backup` (owner read-only, no group/other access) — this is unrelated to the C++ program and should be done as a plain shell command.
