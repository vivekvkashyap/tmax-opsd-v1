Run `make` in `/app/cjson-1.7.15/` first and read the actual failure — a shared-library build failing at the `gcc -shared` step commonly means position-independent-code flags (`-fPIC`) aren't being applied, which happens if the Makefile references a misspelled/wrong variable name for compiler flags (grep the Makefile for the flags variable used on compile lines vs. the one actually defined — a subtle typo like a missing letter is easy to miss by eye, diff the variable name used in the `%.o` rule against the one in the `CFLAGS =` definition). Fix the name mismatch, rebuild, and confirm `libcjson.so` exists before writing any C code against it. Build the Python `/limits` server first and test it standalone with `curl` before wiring the C program to depend on it. In the C validator, fetch resource limits over HTTP (simplest: `popen("curl -s http://127.0.0.1:8080/limits", "r")` and parse with cJSON, or raw sockets if you want no `curl` dependency), then parse the pipeline JSON with cJSON, sum cpu/mem across jobs, and detect dependency cycles with a standard DFS (track visiting/visited state per job, not just a visited set, so you catch cycles even in disconnected subgraphs). Compile linking `-L/app/cjson-1.7.15 -lcjson` (and set `LD_LIBRARY_PATH` or use rpath so runtime linking succeeds), then test manually against the sample clean/evil corpus files before assuming the hidden corpus will pass.
