Work top to bottom and verify each stage before moving to the next. First, parse `raw_reads.dat` as interleaved (ID line, sequence line) pairs — read two lines at a time, and keep only sequences whose first 7 characters exactly equal `GATTACA`; write just the sequence strings (no IDs) to `filtered_reads.txt`, one per line, preserving read order. Second, install FFTW3 dev headers (e.g. `apt-get install -y libfftw3-dev`) so `sim.c` compiles. The core fix is about determinism, not algorithm changes: remove the `#pragma omp parallel for reduction(+:total_energy)` loop entirely; instead, for each sequence, compute each bin's energy `real*real + imag*imag` into a plain `double[]` array, sort that array ascending with `qsort` (write a comparator for doubles), then sum the sorted array sequentially in a simple `for` loop — summing smallest-to-largest in a fixed order eliminates both non-determinism and reduces round-off versus reduction-order-dependent summation. Compile with `gcc sim.c -lfftw3 -lm -o sim` (no `-fopenmp` needed once the pragma is gone). Run it, redirect stdout to `final_scores.txt`, then diff it against `reference.txt` to confirm exact agreement before finishing.