Run `./build.sh` first and read the actual error message rather than guessing — it will fail at the `.import data.csv users` step because the CSV's header row (`id,username,status`) gets inserted as a literal row, and the string `"id"` violates the `INTEGER PRIMARY KEY` column type; fix by skipping the header before import (e.g. `tail -n +2 data.csv | sqlite3 -csv db.sqlite ".import /dev/stdin users"` or writing a header-stripped temp file). Next, run `parser.py` directly and observe the traceback: `line.strip().split('|')` assumes exactly 3 fields, but one log line has an embedded `|` in its message giving 4 fields — fix with `split('|', 2)` to cap the split count so the message field can contain extra pipes. Finally, inspect the SQL query's `status = 'ACTIVE'` filter against the actual data in the CSV/DB and notice a case mismatch (data is lowercase `active`) — fix the comparison to be case-insensitive or match the stored case exactly. After each fix, rerun `./build.sh` end-to-end to confirm it exits 0 and `report.txt` lists the correct active users, rather than assuming a single fix resolves everything.