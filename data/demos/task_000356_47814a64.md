The root cause is catastrophic cancellation in `mean_of_squares - mean*mean`: when both terms are huge and close together, subtracting them loses precision and can go slightly negative, making `sqrt()` return NaN. Fix it either by clamping (`if (variance < 0.0) variance = 0.0;` right before the `sqrt` call) or by switching to Welford's numerically-stable online algorithm if you want a more thorough fix — either satisfies "no NaN" but the clamp is the minimal change matching the existing state-file format. After patching `uptime_monitor.c`, write `/home/user/regression_test.sh` as a self-contained script: it recompiles with `gcc -O2 uptime_monitor.c -lm -o uptime_monitor`, writes a state file with count/sum/sum_sq values chosen to trigger the original bug (large sum, sum_sq combination), runs the binary, greps the output for the string "nan" (case-insensitive) and exits 1 if found, 0 otherwise. Then separately create the exact `/home/user/state.txt` specified in the task, run the fixed binary with the given new-latency argument, and redirect its stdout into `/home/user/fixed_variance.log`.
