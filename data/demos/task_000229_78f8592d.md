Work top to bottom, verifying each step before moving on. Extract the public key with `openssl x509 -in /home/user/server.crt -pubkey -noout > pub.pem`, then verify the signature with `openssl dgst -sha256 -verify pub.pem -signature /home/user/token.sig /home/user/token.enc` and confirm it reports "Verified OK" — this just validates chain of custody, it doesn't give you the key. Find the running process with `ps aux | grep legacy_auth` (or `ps -eo args` to see full arguments), and read its command-line argument directly from the process listing — that is the leaked secret XOR key. In `/home/user/decrypt.sh`, decode the hex file two characters at a time (`printf '%d' "0x${hex:i:2}"` or use `${hex:$i:2}` slicing) and XOR each byte against `key[i % keylen]` using arithmetic expansion `$(( ))`, building the output with `printf '\x'"$(printf '%02x' "$result")"` — avoid invoking python/perl/awk for the XOR math itself, keep it pure bash per the requirement. Redirect the decrypted bytes to `/home/user/decrypted_token.txt` with no extra trailing newline, then confirm correctness yourself with `sha256sum` against `/home/user/expected_hash.txt` before finishing.
