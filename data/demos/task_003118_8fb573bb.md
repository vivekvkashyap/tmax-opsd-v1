Approach this as differential cryptanalysis by observation: never run the vulnerable binary on raw untrusted input, and derive the distinguishing rule empirically from the two labeled corpora rather than assuming a fixed formula.

1. Load many samples from `/app/corpora/clean/` and `/app/corpora/evil/`, and for each 32-hex-char token, compute simple derived statistics across byte pairs/segments (e.g. XOR of the first 4 bytes against the next 4 bytes, byte value distributions, checksums) to spot which statistic cleanly separates the two sets — evil tokens will share some structural regularity that clean (effectively random) tokens don't exhibit.
2. Once you've identified the distinguishing relationship from the data, encode that exact check in `/home/user/token_filter.py` (parse the hex string into bytes, e.g. via `bytes.fromhex`), printing exactly `EVIL` or `CLEAN` based on whether the token matches the evil pattern.
3. Separately, write `/home/user/sandbox_test.sh` to invoke `/app/legacy_auth_service <token>` only through `bwrap` with read-only root (`--ro-bind / /`), full namespace isolation (`--unshare-all`), and no privilege elevation — this sandboxes the vulnerable binary in case you need to probe it for cross-validation, but your Python classifier must not rely on calling it live for the graded predictions.
4. Validate your filter against both `/app/corpora/clean/` and `/app/corpora/evil/` directly, confirming 100% correct classification on every sample before finishing, since the hidden test set will follow the same generation rule.
