Implement the LCG exactly as specified — do not substitute `rand()` or any standard library RNG, since reproducibility depends on the precise recurrence `current_seed = (1103515245 * current_seed + 12345) % 2147483648` using an unsigned 64-bit integer (to avoid overflow issues) and `get_index(N) = next_random() % N`. Read the 5 (x,y) pairs from `/home/user/training_data.csv` (skip the header line) into arrays. For each of the 1000 bootstrap iterations, draw N=5 indices in order via `get_index(N)` (calling it exactly once per index, in the same order the RNG state advances), build the resampled x/y arrays, then compute the closed-form ridge weight `w = sum(x_i*y_i) / (sum(x_i^2) + lambda)` with lambda=2.0. Collect all 1000 w values, compute their mean, then compute the *sample* standard deviation using the M-1 divisor (not population variance). Format both results to exactly 4 decimal places and write them to `/home/user/bootstrap_results.txt` in the literal `Mean: <value>` / `StdDev: <value>` layout — mismatched formatting or using population (M) variance instead of sample (M-1) variance is the most common way this goes wrong.
