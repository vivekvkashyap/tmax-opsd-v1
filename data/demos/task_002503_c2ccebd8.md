Write a small script (Python is easiest for multi-line record parsing) that splits `/home/user/raw_data/experiments.log` on `=== RUN START ===` / `=== RUN END ===` markers to recover each record as a block, then extracts `RunID` and `Status` from within it using a regex or line scan rather than fixed line offsets (measurement line counts may vary). Normalize status with simple rules: `OK` -> `SUCCESS`; anything starting with `ERROR-` -> `FAILED`; anything already `SUCCESS`/`FAILED` unchanged — and rewrite the `Status: ...` line in place within the record text (keep every other line, including the `=== RUN START/END ===` markers, verbatim). Write each updated record to `/home/user/processed_data/<RunID>.txt`, creating the directory first. Then create `/home/user/by_status/SUCCESS/` and `/home/user/by_status/FAILED/` and, for each record, make a symlink named `<RunID>.txt` in the matching status directory pointing at the absolute path of the processed file (`ln -s /home/user/processed_data/<RunID>.txt ...`, not a relative path) — only these two status directories should exist afterward. Double check no leftover raw status strings (e.g. `ERROR-`, `OK`) remain in the processed files.