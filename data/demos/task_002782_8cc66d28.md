Recover the spec from audio before writing any detection logic — don't guess column names or thresholds.
1. Transcribe `/app/voicemail.wav` (install a speech-to-text tool if needed) carefully to learn: which CSV columns are the true source/destination nodes (they differ from the obvious `src`/`dst` columns), what rolling-window aggregation to compute per node and over how many rows, what threshold marks an anomaly, and what graph-structural anomaly (e.g. a cycle) also disqualifies a batch.
2. In `/home/user/sanitiser.cpp`, parse the CSV (by hand or via embedded SQLite), and implement: (a) a rolling/windowed sum of the byte column per the true source column ordered by timestamp, checked against the recovered threshold, and (b) a cycle check over the source→destination graph. If using SQLite, add a composite index on the source column plus timestamp for the window query, as hinted in the audio.
3. Exit 1 (reject) if either anomaly condition trips, else exit 0.
4. Compile to `/home/user/sanitiser` and test it against every file in `/app/corpora/clean/` (must all exit 0) and `/app/corpora/evil/` (must all exit 1) before considering it done, since the hidden grading set follows the same generation rules.
