Build this with SQLite doing the heavy lifting rather than hand-rolling graph logic in Python. Load both CSVs into `nodes`/`edges` tables using `sqlite3` with parameterized `INSERT ... VALUES (?, ?, ?)` calls (never string-format values into SQL). For the top-per-department ranking, use a window function like `RANK() OVER (PARTITION BY dept ORDER BY total_weight DESC, id ASC)` where `total_weight` comes from a `SUM(weight)` aggregate of outgoing edges per node joined to `nodes` — sum only rows where the node is `source`, and make sure departments/nodes with zero outgoing edges are still included (e.g. via `LEFT JOIN` so nodes with no outgoing messages get 0, not excluded); putting `id ASC` as the tiebreaker in the ORDER BY is what correctly resolves ties, don't handle ties separately in Python. For triangles, self-join `edges AS e1, e2, e3` matching `e1.target = e2.source`, `e2.target = e3.source`, `e3.target = e1.source`, and all three node ids distinct; then dedupe in Python by sorting each triple and putting them in a set before final output, since the same directed cycle will be found multiple times starting from different nodes. Write the final structure to `/home/user/report.json` matching the required key names exactly.