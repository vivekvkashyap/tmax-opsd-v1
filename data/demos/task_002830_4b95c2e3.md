First recover the two hidden parameters, don't guess them: run `tesseract /app/config.png stdout` (installing tesseract if needed) and read off the window size W and bandwidth h values from the OCR text — clean up any OCR misreads (e.g. confirm digits look sane) before using them. Then read `/app/oracle.py` only to confirm the algorithm's exact steps (do not just copy it verbatim without understanding it, since the grading is fuzz-based equivalence, so subtle behavior like the `INVALID` short-sequence case and exact rounding matter): read a DNA string from stdin, if its length is below W print `INVALID` and stop; otherwise slide a window of size W across the sequence step 1, compute each window's GC fraction, then for each of the 5 fixed target points evaluate the unnormalized Gaussian sum `sum(exp(-(x-gc_i)^2 / (2*h^2)))` over all windows, and print the 5 values comma-separated rounded to exactly 4 decimal places. Write your own clean/optimized implementation at `/home/user/fast_kde.py` (e.g. vectorized with a rolling GC-count instead of recomputing each window from scratch) and validate it against the oracle yourself on a range of random ACGT sequences of varying lengths, including lengths shorter than W, before considering it done — matching bit-for-bit on the rounded output is what's graded.