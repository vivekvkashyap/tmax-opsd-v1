Parse `/home/user/raw_logs.txt` by filtering lines containing `[METRIC]` (grep or a simple line-by-line scan), then extract `service=` and `response_time=` values (regex or `awk`/split on `=` and spaces) and accumulate sum+count per service name; compute each average and floor it with integer division (not `round()`), writing the result as strict JSON via `json.dump` to `/home/user/parsed_metrics.json`. Generate TLS material with `openssl req -x509 -newkey rsa:2048 -nodes -keyout /home/user/certs/key.pem -out /home/user/certs/cert.pem -days 365 -subj "/CN=localhost"` (the `-nodes` flag is what keeps the key unencrypted). Implement `/home/user/secure_api.py` as an `http.server`/`ssl`-wrapped server (or Flask with `ssl_context=('cert.pem','key.pem')`) bound to `localhost:9443`; on `GET /metrics` read and re-serve the JSON file's exact contents with `Content-Type: application/json`, and 404 everything else. For `/home/user/manage_services.sh`, find any existing server by matching the process on `secure_api.py` (e.g. `pgrep -f secure_api.py`), `kill` it and wait for it to exit before relaunching, start a fresh background instance (`nohup python3 secure_api.py & `), and write `$!` to `/home/user/api.pid` — the idempotency check re-runs this script twice and expects a genuinely different, live PID the second time, so make sure the kill actually completes before respawning.
