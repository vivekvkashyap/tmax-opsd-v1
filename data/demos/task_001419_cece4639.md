First reverse-engineer the archive format empirically rather than guessing: run `xxd`/`hexdump` on a few files in `/home/user/dataset/clean/` and `/app/extractor` on a copy in a scratch dir to observe what it writes, comparing the extracted filenames/paths against the raw bytes to infer the header layout (a magic number, then repeated entries each with a length-prefixed path string and a length-prefixed data blob — work out the exact byte widths and endianness by cross-referencing offsets in a hex editor against known path strings). Write `/home/user/sanitizer.c` to parse just the header/path portions (skip over data blobs using the size fields, don't need to fully "extract" for this stage) and, for every embedded path, reject (exit 1) if it starts with `/` or contains a `../` segment anywhere, otherwise accept (exit 0) after checking all entries — make sure to check every entry, not just the first, and to guard against malformed/truncated archives so the parser doesn't crash (bounds-check every read against the file size). Validate against both corpora directories before trusting it. Then, using only archives your sanitizer accepts, actually extract them (you can shell out to `/app/extractor` in a controlled directory since the sanitizer already confirmed safety, or extract manually in C using the same format), compute `sha256sum` of each extracted file, rename it to `<hash>.log`, and append `<hash> <original_filename>` lines to `/home/user/dataset/manifest.txt`.