Debug this as three separate, independently-verifiable layers rather than guessing at a combined fix. First run `bash -x pipeline.sh` (or just read it) to see how it invokes the Python script — a common bug is calling the script by bare filename without `./` or without going through the `python3` interpreter, so it isn't executed correctly; fix the invocation to explicitly run `python3 /home/user/process_data.py > /home/user/final_ema.txt` (or ensure the shebang and executable bit make direct execution work). Second, inspect the SQL query in `process_data.py`: check both the table name against what's actually in the SQLite schema (`sqlite3 /home/user/data.db ".schema"`) and the `ORDER BY` direction — the requirement is oldest-to-newest chronological order, so verify it's ascending on the date column, not descending. Third, re-derive the EMA formula by hand from the spec: `EMA = current*alpha + previous_EMA*(1-alpha)`, and compare term-by-term against the code's weighting — a subtly swapped `alpha`/`(1-alpha)` pairing is a classic bug here. After each fix, re-run the pipeline standalone and check `/home/user/final_ema.txt` for a plausible EMA trend before trusting the final rounded value.