Work this as a staged forensics pipeline. First, integrity-check: compute `sha256sum` on every file in `/home/user/evidence/system_files/` and diff against `/home/user/evidence/checksums.txt` to find the mismatch; write just that file's basename to the output file. Second, reverse-engineer the dropper: run `strings` and `objdump -d` on `/home/user/evidence/dropper.bin` to understand its obfuscation loop (look for a per-byte transform like XOR against an integer key, printed as hex), then parse `/proc_cmdline.log` carefully — remember `/proc/[pid]/cmdline` entries are null-byte separated, so use a tool/method that shows raw bytes (e.g. `xxd` or `tr '\0' '\n'`) to correctly split out the numeric key argument rather than reading it as a normal string. Third, write a C++ decoder that is the mathematical inverse of the dropper's obfuscation (if it's a byte-wise reversible operation, applying it a second time with the same key undoes it) — extract the hex ciphertext between the `===BEGIN PAYLOAD===`/`===END PAYLOAD===` markers in the tampered file, hex-decode it, apply your inverse transform with the recovered key, and write the resulting plaintext exactly (no extra whitespace/newlines) to the final output path. Compile with the exact `g++` command given and confirm the decoded text reads as a plausible SSH public key line.
