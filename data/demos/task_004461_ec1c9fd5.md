Start by recovering the prefix, since everything downstream depends on it: run `tesseract /app/diagram.png stdout` (or output to a file) and extract the exact string following `PREFIX:` in the OCR text — don't hardcode a guess, read what OCR actually produces. Then write `organizer.c` to: parse each `.json` file (a tiny hand-rolled parser or a JSON library if available is fine, since the schema is just two string fields); before doing anything else with `"filename"`, explicitly reject/skip it if it contains `/`, `\`, or the substring `..` — check this on the raw string, not the resolved path, since path traversal defenses must reject the pattern itself. For valid entries, build the output path as `<output_dir>/<PREFIX><filename>` and write atomically: call `mkstemp()` on a temp path inside `<output_dir>` (so it's on the same filesystem), write the content, `fsync`/close, then `rename()` it to the final name — never write directly to the final path. Test manually against a couple of files from `/app/corpus/clean` and `/app/corpus/evil` to confirm clean files land correctly and malicious ones are rejected without touching anything outside `<output_dir>`, then run the full corpora before finishing.
