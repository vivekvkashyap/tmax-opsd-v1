Write a single Python script (not ad-hoc REPL work) so the environment variables can be set consistently, then run it as `OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 VECLIB_MAXIMUM_THREADS=1 NUMEXPR_NUM_THREADS=1 python3 script.py` — set these before importing numpy/pandas/sklearn, since some BLAS backends read them only at import/thread-pool-init time. Load the CSV, compute `df[['f1','f2','f3','f4','f5']].corr()`, and scan the upper triangle for pairs with `abs(corr) > 0.85`; when a pair is flagged, drop whichever column name comes later in the original CSV column order (not by correlation strength) — iterate pairs in column order and track a drop-set so a feature already dropped doesn't cascade oddly. Fit `sklearn.linear_model.Ridge(alpha=1.0, fit_intercept=True)` on the surviving features vs `y`, compute MSE on the training set (`sklearn.metrics.mean_squared_error`), and write the JSON report with `round(x, 4)` applied to the MSE and every weight in `model_weights` (keyed by surviving feature names, using `model.coef_`).
