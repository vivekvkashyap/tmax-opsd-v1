Create the venv first (`python3 -m venv /home/user/venv && /home/user/venv/bin/pip install numpy scipy h5py`) and use that interpreter for everything. Load `/home/user/input_data.h5` with `h5py` and read the `matrices` dataset (shape (100,50,50)) as a single array. Compute per-matrix 2-norm condition numbers with `np.linalg.cond(matrices)` (vectorized over the batch, avoid a manual SVD loop unless you want to double-check). Find indices where `cond >= 1e12`, sort ascending, and write one integer per line to `/home/user/singular_indices.txt` — make sure there's no trailing blank line mismatch or extra formatting. Take `log10` of all 100 condition numbers, feed them into `scipy.stats.gaussian_kde(log_conds)` with default (Scott's rule) bandwidth, evaluate `kde(12.0)`, and write the scalar rounded to exactly 4 decimal places (e.g. `f"{val:.4f}"`) to `/home/user/kde_result.txt`. Double-check you're taking log10 of the condition numbers (not the matrices) and evaluating the KDE at the query point 12.0, not 1e12.
