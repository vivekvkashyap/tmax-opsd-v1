Treat correctness and performance as separate concerns, then satisfy the git hook last since it re-validates both. First generate the telemetry log by running the stripped binary and redirecting stdout to the target log path; inspect a few lines to confirm the exact format (`[TIMESTAMP] VM_ID CPU_USAGE MEM_USAGE`) before writing your parser. Write analyzer.cpp to read the file with fast I/O — avoid `std::cin`/formatted `>>` on a stream tied to stdio, and avoid per-line `std::getline` plus `std::stringstream` (both are common naive choices that are too slow for 2.5M lines); instead memory-map the file with `mmap` or use a single large `fread`/`std::ifstream` buffered read plus manual pointer-based tokenizing (find `vm-`, compare digits by hand, parse the float with `strtof`/manual parsing) to stay well under the 0.15s budget. Filter lines matching the given VM_ID argument, accumulate sum and count of the CPU_USAGE field, and print only the average as a float. Test manually with `g++ -O3` and time it before committing. Only after it's fast and correct, `git add`/`git commit` — if the pre-commit hook rejects it, read its output (it compiles your file and checks a known small case) rather than blindly resubmitting.
