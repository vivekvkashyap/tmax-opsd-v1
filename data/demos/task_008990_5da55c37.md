Build this as independent pieces, testing each before chaining. Generate TLS materials with `openssl req -x509 -newkey rsa:2048 -nodes -keyout /home/user/certs/key.pem -out /home/user/certs/cert.pem -days 365 -subj "/CN=localhost"`. Write `server.py` as a simple HTTPS static file server (e.g. `http.server.HTTPServer` wrapped with `ssl.SSLContext.wrap_socket` using the cert/key, or `ThreadingHTTPServer` with `directory=` pointed at the resolved `/home/user/www-current` symlink target) listening on 8443, appending startup/access lines to `/home/user/logs/server.log`. For `deploy.sh`, achieve atomicity by cloning into a fresh timestamped directory first, THEN swapping the symlink with `ln -sfn <new-release-dir> /home/user/www-current` (the `-n` flag is essential — without it, `ln -sf` on an existing symlink creates a nested link inside it rather than replacing it) — only after the clone fully succeeds, so a failed clone never leaves a half-updated symlink. For process lifecycle, find the old `server.py` PID (e.g. `pgrep -f server.py`), send it a graceful `kill` (not `-9`) and wait briefly, then launch the new one detached with `nohup ... &` or `setsid`. Write `logrotate.conf` with `daily rotate 3 compress missingok notifempty`, then force a rotation via `logrotate --force -s /home/user/logrotate.state /home/user/logrotate.conf` after ensuring the log file is non-empty — verify a `.gz` or `.1` file actually appears in `/home/user/logs/` afterward.