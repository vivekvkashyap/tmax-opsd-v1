Treat the pipeline fix and the sanitizer as separate, independently testable pieces. Pipeline: inspect `/home/user/app/nginx.conf` for the `proxy_pass` directive — it likely points at the wrong port (Flask actually runs on 5000) — fix it, then check `/home/user/app/receiver.py` for a missing `import redis` and missing job-queue write (push the saved file path onto a Redis list, e.g. `redis.Redis(host='localhost', port=6379).lpush('doc_queue', filepath)`); restart nginx/flask/redis and confirm end-to-end with a real POST through nginx's port before moving on. Sanitizer (`/home/user/sanitizer.py`): use Python's `zipfile` module — call `ZipFile.testzip()` (or catch `BadZipFile`) to detect corruption, and check each `ZipInfo.file_size` (uncompressed size) against the 1MB cap before extracting anything, aborting immediately with exit code 1 on either failure — never partially extract a bad archive. Recurse into any `.zip` found inside extracted content up to depth 2, applying the same integrity/size checks at each level. For `.md`/`.txt` files, filter out lines containing the literal `<script>` and rename `.txt` outputs to `.md`. Build your own small test zips (corrupt CRC, oversized file, nested zip, script-tag line) to validate both success and failure paths before trusting the hidden suite.
