Treat this like real forensic triage: first run `strings /home/user/memory.dump | sort | uniq -c | sort -rn | head` (or similar) to find which `SESSION_PAYLOAD_<ID>` string appears with an anomalously high duplicate count compared to the others — that's the leaked session, and its numeric ID is what you extract (don't guess; let the frequency count tell you). Write just that numeric ID to `leak_report.txt`. Next, `grep` `service.log` for that ID to see the surrounding event sequence — look specifically for how the session's disconnect was handled (e.g. graceful vs. forceful/abrupt) to understand which code path skipped cleanup. Then inspect `session_manager.cpp`: find where a `Session*` is heap-allocated (`new`) and check each branch that removes it from the tracking map — one branch frees it and another silently doesn't. Add the missing `delete` in the branch matching the disconnect type you traced in the log, without altering any function signatures. Overwrite the original file with your fix, and double check the deletion is really inside the correct conditional block, not just present somewhere in the file.
