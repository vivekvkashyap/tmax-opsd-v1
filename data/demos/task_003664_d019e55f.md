Work the pcap forensics phase before touching exploit code. Use `scapy` (or `tshark`/`tcpdump -r`) to load `/home/user/capture.pcap`, filter for TCP traffic to port 7777, and identify the client IP from the SYN packet's source address — write it verbatim to `attacker_ip.txt`. Extract the raw payload bytes from the PSH/ACK packet's Raw layer. To recover the XOR key: brute-force all 256 single-byte keys against the payload and keep the one that decodes to a printable, sensible 6-character Linux enumeration command (e.g. iterate `bytes([b ^ k for b in payload])` for k in 0..255 and check `str.isprintable()`/known commands) — don't guess a key by hand. Once you have the verified key, reuse it (not a new one) in `craft_exploit.py` to XOR-encode `cat /etc/passwd` and write raw bytes with `open(path,'wb').write(...)`. For the firewall script, write the `iptables -A INPUT` DROP rule referencing the discovered IP and port 7777, no `sudo`. For SSH hardening, generate the Ed25519 key with `ssh-keygen -t ed25519 -f /home/user/admin_key -N ""`, then read the base config and explicitly set/append PasswordAuthentication no, PermitRootLogin no, X11Forwarding no, and Protocol 2 in the hardened copy — don't just rely on defaults being commented out.
