This is a video-to-spec extraction task, not something to answer from memory. First extract frames with `ffmpeg -i /app/loc_rules.mp4 -vf fps=1 /tmp/frame_%03d.png` (or scene-detect to grab distinct slides), then run `tesseract` on each frame to OCR the text; visually inspect a couple of frames yourself too if the tool lets you view images, since OCR of small text can be noisy — cross-check ambiguous characters against context. Record precisely: (a) every character-encoding replacement pair shown, and (b) every imputation rule with its exact key and template string, including where `{name}` appears. Only after you have the verified rules from the video should you write `/home/user/process_loc.py`. Implementation-wise: read one JSON line from stdin, apply the encoding substitutions only to non-null string values under `strings`, compute `null_keys` count *before* mutating anything, then fill nulls using the OCR'd templates interpolated with the top-level `name`, add the `stats` object, emit the stderr log line in the exact required format, and print compact JSON (no extra whitespace) to stdout. Since correctness is checked by fuzzing against a hidden reference, get the substitution order and null-counting-before-imputation exactly right.
