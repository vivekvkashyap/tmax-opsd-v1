Parse `--backends` with `argparse` and split/int-cast the comma-separated ports. The core challenge is idempotency: don't just append text to config files on every run — instead, always fully regenerate each file's content from scratch in memory (build the nginx.conf string, logrotate.conf string, and manifest dict fresh each invocation from the current `--backends` argument) and overwrite the file, rather than checking-and-appending line by line; this guarantees running twice with the same args produces byte-identical output with no duplicate `server` lines, and also handles the case where a *different* backend list is passed later. Use `os.makedirs('/home/user/iot_env', exist_ok=True)` for the idempotent directory step. Build the nginx config with an f-string/template containing the `upstream backend_cluster { server 127.0.0.1:<port>; ... }` block (one line per port), a `server { listen 8080; location / { proxy_pass http://backend_cluster; } access_log /home/user/iot_env/access.log; } }`. Write the manifest as `{"deployed_backends": [ints...], "status": "success"}` with real ints, not strings. Run the script twice manually and grep-count a `server 127.0.0.1:<port>;` line to confirm it appears exactly once.
