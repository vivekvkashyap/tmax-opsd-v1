Reverse-engineer the binary format empirically rather than guessing: use `file`, `strings`, and a hex dump (`xxd`/`od`) on any sample graph.bin you can construct, and probe the oracle's exit code against small hand-crafted binary files where you vary one field at a time (header magic bytes, node-count field, per-node record layout, edge-count field, edge record layout) to confirm field sizes/offsets/endianness before committing to a writer implementation — this fixed-record binary layout is a well-known style (magic header, count, fixed-size records) so validate each guess against the oracle's exit code rather than assuming. Once your C++ writer reliably produces a graph.bin the oracle accepts (exit 0) from raw_datasets.csv, build the HTTP service: implement a minimal raw-socket HTTP/1.1 request parser sufficient for `curl` POSTs, run shortest-path (Dijkstra, since edges have weights) restricted to nodes whose stored date >= min_date, and return paginated, ordered JSON edges plus total cost. Use a thread pool or one-thread-per-connection model for concurrency, start it detached and listening, and test with real `curl -X POST` calls including pagination and the min_date filter before finishing.
