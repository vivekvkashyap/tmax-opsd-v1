Implement this exactly to spec rather than using library RNG or histogram functions, since the grader diffs against a bit-for-bit deterministic reference. Copy the LCG and Box-Muller formulas verbatim (same `unsigned int` modulus arithmetic, same `+1.0`/`2147483649.0` normalization, same order of `u1` then `u2` per particle) — using `rand()`, `drand48()`, or reordering the two uniform draws will silently produce a different (still plausible-looking) but wrong sequence. Read the 10,000 reference values from `/home/user/reference.txt` with `fscanf`/`%lf`, bin both datasets into 100 bins of width 0.1 over `[0,10)` using `idx = (int)(x/0.1)` and strictly excluding out-of-range values from both the bin counts and the normalization denominator (do this separately for reference and simulated totals, since they need not match). Normalize each histogram into a probability vector by dividing by its own valid-point count, then sum `sqrt(P_i * Q_i)` over all 100 bins in `double` precision and take `-log()` of that sum. Write the result with `%.4f` and a trailing newline to `/home/user/distance.txt`. Compile with `-lm`, run once, and re-derive one or two bin counts by hand to sanity-check before trusting the output.
