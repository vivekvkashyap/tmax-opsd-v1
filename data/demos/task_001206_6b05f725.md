Start by trying to build the vendored package as-is to see the exact compiler/linker error: `cd /app/fast_graph_analytics-1.2.0 && pip install .` (or run its Makefile directly). On Linux, a shared library (`.so`) built from an object file that wasn't compiled with position-independent code fails to link with an error mentioning "recompile with -fPIC". Locate the `gcc` compile step producing the `.o` file (in `src/Makefile` or `setup.py`) and add the `-fPIC` flag to that compile invocation, then rebuild. Once it builds and installs cleanly, verify the C extension actually loads (e.g. import the module and check no fallback/warning is printed) before writing the analysis script — a silent fallback to pure-Python would defeat the precision requirement. Then write `/home/user/analyze_logistics.py` to load the CSVs into the library's graph structure, compute shortest paths from `HUB_001` and PageRank using the library's own functions (not a hand-rolled algorithm, since the grader compares against the C backend's floating-point behavior), and dump results to `/home/user/analysis_output.json` in the exact required schema. Test incrementally: confirm the library import works before writing analysis logic.