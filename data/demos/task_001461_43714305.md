This task has two independent parts; tackle them separately and verify each on its own before moving on.

Part 1 (video): don't eyeball frames visually — use ffmpeg to extract every frame as an image (e.g. `ffmpeg -i /app/ui_walkthrough.mp4 frame_%04d.png`), then script a check (Python + PIL/numpy) that loads each frame and tests whether all pixel values are exactly 0 (pure black), recording the 0-indexed frame number for every match. Sort ascending and write one number per line to `/home/user/glitch_frames.txt`. Be careful with off-by-one indexing between ffmpeg's 1-based output filenames and the required 0-indexed frame numbers.

Part 2 (C stream processor): write `process_loc.c` to read stdin line-by-line with `fgets` using a 1024+ char buffer, and for each line: split on `|` counting exactly 4 fields and validate the timestamp against the literal pattern `YYYY-MM-DDThh:mm:ssZ` character-by-character (don't use a regex library if unavailable — hand-check digit positions and literal separators). On any violation print `INVALID_RECORD` and move to the next line. For masking, only touch the local part before `@`; leave the rest of the string untouched. Track the previous valid record's `translation_key` to drop consecutive duplicates, incrementing separate valid/duplicate counters. Since your binary will be fuzzed byte-for-byte against a hidden reference, be meticulous about edge cases: empty lines, missing trailing newline, extra pipes, multiple `@`, and very short local parts — get the exact edge-case behavior stated in the spec exactly right (e.g., local part ≤2 chars stays unmasked) rather than guessing.