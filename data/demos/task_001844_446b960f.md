Handle the schema migration and the code fix as separate verifiable steps. Run `sqlite3 /home/user/auth_service/db.sqlite "ALTER TABLE users ADD COLUMN role TEXT DEFAULT 'user';"` and confirm with `PRAGMA table_info(users);` that the column, type, and default all match exactly (the verifier checks the literal PRAGMA output format including the default value). For the SQL injection fix in `auth_api.cpp`, replace the string-concatenated query in `authenticate_user` with a parameterized statement: call `sqlite3_prepare_v2` with placeholders (`?`) in the SQL text, then bind each user-supplied value via `sqlite3_bind_text` before `sqlite3_step` — never interpolate `username`/`password` into the query string directly, since that's exactly what lets `admin' --` short-circuit the WHERE clause. Rebuild with `make`, then run `test_auth.sh > /home/user/security_test.log` and inspect the log to confirm the legitimate `admin`/`password123` login reports success while the `admin' --` injection attempt reports failure — if the injection still succeeds, the parameterization wasn't applied correctly somewhere in the query construction.