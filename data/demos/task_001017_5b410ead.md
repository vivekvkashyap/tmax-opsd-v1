Start by reading `solver.c` and the `Makefile` to locate the two distinct classes of bugs. In the Makefile, a plain `gcc -o libsolver.so solver.c` produces an executable, not a loadable shared object — it needs `-shared -fPIC` (and typically `-o libsolver.so`) so `ctypes.CDLL` can dlopen it; verify afterward with `file libsolver.so` showing "shared object". In solver.c, look closely at how the DP array is sized versus how it's indexed: if the array is allocated for exactly `target` elements but the fill/access loop runs through index `target` inclusive (`i <= target`), that's an off-by-one heap overflow — allocate `target + 1` elements (or otherwise make the allocation match the maximum index actually touched) while keeping the `subset_sum(int*, int, int)` signature unchanged. After fixing both, `make`, then write `/home/user/solve_api.py` using `ctypes.CDLL('./libsolver.so')`, declaring `argtypes`/`restype` for the C array pointer, call `subset_sum` for each of the six target durations against the given ad-length array, and dump a `{"target": bool}` JSON dict to `/home/user/results.json`. Sanity-check a couple of results by hand (which combinations of 15/30/45/60/90/120 sum to each target) rather than trusting the C call blindly, in case the fix is still subtly wrong.