For the audio, install a local STT tool (e.g. `pip install openai-whisper` and run the `tiny.en` model, or `pocketsphinx`) to transcribe `/app/recording.wav` into text, then apply the regex `[A-Z]{3}-\d{4}` to pull out Alert Codes — don't hand-guess codes from the description's examples, actually run the transcription. For the binary log, open `/app/legacy_logs.bin` in binary mode and decode robustly: since it mixes UTF-8 and UTF-16LE with corrupted bytes, try decoding chunks with `errors='ignore'` or `errors='replace'` under both encodings (e.g. attempt `.decode('utf-8', errors='ignore')` and `.decode('utf-16le', errors='ignore')` and concatenate/scan both results) so codes encoded either way are recoverable, then apply the same regex. Union both code sets, uppercase them, dedupe with a `set`, and for each unique code compute `hashlib.sha256(code.encode('utf-8')).hexdigest()`. Sort the hash strings alphabetically and write one per line to `/home/user/unique_alert_hashes.txt` with no extra content — the grading is Jaccard similarity against a hidden hash set with an 0.85 threshold, so prioritize correctly recovering all codes from both sources over guessing extras.