Get MongoDB running locally first: download and extract the tarball, start `mongod --dbpath /home/user/mongodb_data --port 27017 --fork --logpath <logfile>` (create the dbpath dir first), install `pymongo`, then load `/home/user/transactions.json` into `backup_db.transactions` with `insert_many`. For the diagnostic script, use `argparse` for `--txid`, then build an aggregation pipeline with `$graphLookup` on the `transactions` collection: `startWith: "$waits_for"`, `connectFromField: "waits_for"`, `connectToField: "_id"`, `as: "chain"`. To reconstruct the actual cycle order (graphLookup returns an unordered set), don't just dump the `chain` array — instead walk the `waits_for` pointers manually starting from `--txid` (follow `waits_for` repeatedly, collecting each doc) until you land back on the starting txid, confirming a cycle exists via the graphLookup result first. Number each hop starting at 1 for the given txid, and write `sequence_order,tx_id,waits_for,operation` rows to `/home/user/deadlock_report.csv` for the chain up to (but not repeating past) the point it loops back.
