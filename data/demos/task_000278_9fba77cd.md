First locate the bug: inspect `/app/ijson-vendored/setup.py` for the `ext_modules = [...]` list — it has been emptied out. Look inside the package (e.g. `ijson/backends/`) for the C/Cython source of the yajl2 backend to find the correct extension name and source file(s), then restore an `Extension(...)` entry referencing it, linking against the system `yajl` library (install `libyajl-dev` first). Reinstall with `pip install -e /app/ijson-vendored --force-reinstall --no-deps` and confirm `import ijson.backends.yajl2` succeeds — that's the real fix-verification signal, not just "pip install succeeded". In parse_logs.py, explicitly select the C backend (don't rely on ijson's auto-fallback, which silently uses pure Python if the C extension is missing) and stream-parse each file's array with `ijson.items(f, 'item')`, filtering `severity == "CRITICAL"` and tallying `error_code` counts in a dict; write sorted rows to the CSV. For backup.sh, find the most recent prior timestamped subdirectory of the destination (sort lexically, since ISO timestamps sort correctly), and run `rsync -a --link-dest=<that dir> src/ dest/<new-timestamp>/`.
