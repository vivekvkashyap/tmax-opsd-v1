Recover the subnet first: run OCR (e.g. `tesseract /app/dashboard_screenshot.png -`) or view the image, and locate the "PRIMARY SUBNET" line to get the CIDR block your validation logic will use — don't hardcode a guessed subnet, read it from the image output. Write `setup_env.sh` to be idempotent: use `mkdir -p` for the three directories, `chmod 700` on each (not just at creation, reapply chmod every run so permissions self-heal), and for the symlink use `ln -sfn active /home/user/monitor_state/latest` — the `-f` (force overwrite) plus `-n` (treat existing symlink as file, don't nest into it) combination is essential so re-running doesn't create `latest/active` nesting or fail on an existing link. For `classify_routes.sh`, parse each line with `grep -E`/`awk` to extract TARGET_IP and GATEWAY_IP, validate both are syntactically valid dotted-quad IPv4 (four 0-255 octets, reject malformed tokens), then check numeric containment within the CIDR by converting each octet group to a 32-bit integer and comparing against the network/mask boundaries (bash arithmetic, not string prefix matching, since prefix matching breaks at non-byte-aligned masks). Exit 0 only if every line in the file is well-formed and both IPs fall inside the subnet; otherwise exit 1. Test against the provided clean/evil examples before relying on the hidden corpus.