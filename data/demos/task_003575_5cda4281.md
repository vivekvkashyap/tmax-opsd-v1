Work in the order the task implies: recover the salt, bisect, then fix. To pull the salt out of the core dump, treat it as a binary blob and search it for printable ASCII strings referencing the salt variable — `strings /app/dsp_crash.core | grep -i salt` (or similar grep on the raw file if `strings` misses it) should surface the assignment; extract just the value that follows. Write a small bisect test script that imports `DSPPipeline`, initializes it with that recovered salt, runs it against the wav file, and exits nonzero on obviously-wrong output (NaN/Inf, hang, or divergence from a sane range) so `git bisect run <script>` can walk between the `v1.0-good` and `v1.1-bad` tags automatically — cap the script's runtime with a timeout since the bug can cause infinite loops. Once bisect finds the culprit commit, go back to inspecting `src/filters.py` on the `main`/bad branch (don't stay on the bisected commit): rewrite the recursive IIR step to avoid subtracting two nearly-equal large float64 values (restructure the recurrence algebraically instead of naive cancellation) and fix any loop-termination condition that assumes samples are never exactly equal after precision loss. Finally write the fingerprint-generation script and validate your output stays numerically close to expectations by re-deriving it independently, not by eyeballing.