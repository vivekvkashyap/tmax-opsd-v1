Split this into the audio-feature script and the sanitizer, and design the sanitizer defensively rather than trusting pandas defaults. For the MFCC extraction, `pip install librosa pandas`, set `OMP_NUM_THREADS=1` and `OPENBLAS_NUM_THREADS=1` (export before running the script, or set inside via `os.environ` before importing numpy/librosa) so BLAS uses one thread, load the wav with `librosa.load`, compute `librosa.feature.mfcc(..., n_mfcc=13)`, and write to CSV with `pandas.DataFrame(...).to_csv`. The key insight for the sanitizer: `pandas.read_csv` silently upcasts an int column to float64 the moment it sees a NaN, empty string, or a value like "5.0" — so relying on `df['col'].dtype == int` after a naive `read_csv` will miss corruption. Instead, read the target columns as raw strings (`dtype=str` or `read_csv(..., dtype=object)`, or parse the CSV text directly) and validate each cell with a strict integer regex (e.g. `^-?\d+$`), rejecting anything empty, containing a decimal point, or literal "NaN". Exit 1 on any violation in `speaker_id` or `frame_index`, exit 0 only if every value in both columns passes the strict integer check across all rows. Test your sanitizer against both a clean and a hand-crafted evil CSV (with "5.0", empty string, and NaN cases) before finishing.