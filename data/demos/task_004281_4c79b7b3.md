Two independent subproblems: extract the revoked-ID list from the video, and implement strict, spec-exact path safety logic. For the video, use `ffmpeg` to sample frames (e.g. `ffmpeg -i /app/revocation_list.mp4 -vf fps=1 frame_%03d.png`) and either read the on-screen text visually or run an OCR tool (e.g. `tesseract`) on the extracted frames to recover the exact revoked artifact IDs — don't hardcode a guess, verify what the frames actually show. Load that set into your script. For Zip Slip prevention, mirror POSIX path normalization precisely: reject if the path starts with `/`, contains a null byte, or if `os.path.normpath(path)` equals `..` or starts with `../` (this correctly handles multi-segment traversal like `a/../../b`). Iterate the manifest's `files` array in original order, printing `SAFE: <filename> - <sha256>` only for paths that pass all checks, after printing `STATUS: APPROVED`/`STATUS: REVOKED` first. Since grading is bug-for-bug fuzz equivalence against a hidden oracle, test your script against many synthetic manifests covering absolute paths, null bytes, and varied `../` combinations to shake out edge cases in your normalization logic.
