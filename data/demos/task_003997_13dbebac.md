Start with `cd /app/telemparser && git log --all --oneline` and `git log --all --full-history -- '*key*'` (or `git log -p` scanning for hex strings) to find the commit where the decryption key was committed before removal — check out that commit's version of the file (`git show <commit>:<path>`) to recover the hex key without altering current history. Use that key plus AES-256-GCM (Go's `crypto/aes` + `crypto/cipher.NewGCM`) to decrypt `testdata/samples.bin.enc` and confirm you can read the records. For the bug, search the aggregation code for the accumulator type used when summing decoded values — look specifically for `sum` declared as `int32` next to a loop adding many 4-byte decoded integers; overflow with mostly-positive-but-large values would show up as it wrapping negative, matching the bug report. Fix by widening the accumulator to `int64` (or `float64`) and dividing by count as a float at the end. Add a regression test in `parser_test.go` that sums enough large values to overflow `int32` but not `int64`, asserting the correct (positive) mean. Build `statstool` with a `go.mod` `replace` directive pointing to `/app/telemparser`, and verify manually against the decrypted sample before submitting.