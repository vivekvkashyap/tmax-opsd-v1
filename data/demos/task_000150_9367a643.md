Set up the environment first: `python3 -m venv /home/user/venv && /home/user/venv/bin/pip install kuzu`, capturing pip's install output somewhere other than stdout (e.g. redirect to a log file or `/dev/null`) so it never lands in the final JSON. Inside the inline Python (heredoc), initialize a Kùzu database at /home/user/kuzu_db, define a `User` node table keyed on `user_id` and a `Transferred` relationship table between User nodes carrying an `amount` property, then use Kùzu's `COPY ... FROM` to bulk-load users.csv and transactions.csv. Write a parameterized Cypher query matching a variable-length-2 path: `MATCH (a:User {user_id:$sid})-[t1:Transferred]->(b:User)-[t2:Transferred]->(c:User) WHERE t1.amount > $min AND t2.amount > $min AND c.user_id <> a.user_id RETURN DISTINCT c.name` — pass suspicious_user_id and min_amount as query parameters rather than string-interpolating them. Have the Python step print only the raw names (one per line, or as JSON) to stdout so the Bash wrapper can capture exactly that; in Bash, collect the names, sort and dedupe (`sort -u`), and build a JSON array (e.g. via `jq -R -s 'split("\n")|map(select(length>0))|sort|unique'`) written to /home/user/suspects.json. Run the script with the given arguments to produce the file.