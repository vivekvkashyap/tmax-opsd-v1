Read `/home/user/etl_validation.py` fully before editing — there are three separate, independent bugs to fix, and fixing only one won't satisfy the verifier. (1) Install missing packages (pandas, numpy, matplotlib, scikit-learn) via pip. (2) For the headless plotting crash, add `matplotlib.use('Agg')` immediately after importing matplotlib and before importing `pyplot` (or before any plotting call) — this must happen before the pyplot import binds a display backend. (3) For the bootstrap logic bug, trace how the resample is drawn from the `target` column: true bootstrap sampling draws WITH replacement, so check the `np.random.choice` call's `replace` argument and correct it if it's sampling without replacement (which biases the distribution and breaks the CI). Do not touch `np.random.seed(42)`. After fixing, run the script directly and confirm `/home/user/bootstrap_dist.png` is a non-empty valid image and `/home/user/metrics.json` contains exactly the four required keys — cross-check your numbers make sense (CI should bracket the mean, MSE should be small and positive) before declaring done.
