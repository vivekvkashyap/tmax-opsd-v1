Diagnose each subsystem before optimizing for the CPU-time metric. For `build.sh`, compile `frame_processor.c` linked against `libprocess_v2_opt.so` (not `_v1`), and since the two `.so` files expose an ABI-incompatible `process_frame` symbol, make sure the *linker* resolves against v2's symbol table at build time (e.g. `gcc frame_processor.c -L. -lprocess_v2_opt -o processor` with correct rpath, or `LD_LIBRARY_PATH=/home/user/video_toolkit` set when running) — check with `ldd ./processor` and `nm -D` that it actually binds to v2 and not v1. For `pipeline.sh`: validate the input is a real MP4 (e.g. `ffprobe -v error <file>` and check exit code), extract exactly the first 50 frames with `ffmpeg -i input.mp4 -vframes 50 frames/frame_%02d.jpg`, then loop over frames calling `./processor` on each — implement the "5 frames per second" rate limit as an explicit `sleep 0.2` between iterations (not a burst-then-wait pattern), and append each run's stdout to `/home/user/processing.log`. For `prop_test.sh`, generate 100 random-sized files with `dd if=/dev/urandom of=file bs=1024 count=$((RANDOM % 50 + 1))`, run `processor` on each, capture `$?` right after (139 indicates SIGSEGV), tally successes vs segfaults, and write `Success: X, Segfaults: Y` to `/home/user/test_report.txt`. Since the grader measures CPU time, confirm end-to-end that the optimized library path is actually taken at runtime, not just present on disk.
