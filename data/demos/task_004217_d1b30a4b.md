Generate the cert first with a single non-interactive openssl command so the subject has only CN set: `openssl req -x509 -newkey rsa:2048 -nodes -keyout /home/user/new_key.pem -out /home/user/new_cert.pem -days 365 -subj "/CN=test.local"` — verify afterward with `openssl x509 -in new_cert.pem -noout -subject` that no other subject fields leaked in. For the redaction fix, open `/home/user/deploy_test.py` and find the logging call that interpolates the raw password into the log message; change it so the literal string `***REDACTED***` is logged in place of the password value while username and URL stay untouched — don't just filter the log file after the fact, fix it at the source so nothing sensitive ever reaches disk. Then run the deploy script with the given credentials and grep `/home/user/deploy.log` yourself to confirm the plaintext password is absent and `***REDACTED***` is present. Finally run `auth_test.py` with the same cert/credentials and check `/home/user/auth_result.txt` for success; if it fails, re-check the cert's CN matches exactly `test.local` (with or without spaces around `=`) since that's what the check parses.