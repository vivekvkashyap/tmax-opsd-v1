Read `/home/user/analyze.py` closely: the bug is that `plt.savefig(...)` is called before `plt.hist(...)`/`plt.title(...)`, so the figure is written to disk empty before any data is plotted onto it — reorder the calls so all plotting statements (`plt.hist`, `plt.title`, and any labels) run first, and `plt.savefig('/home/user/bootstrap_dist.png')` runs last, with `plt.close()` afterward to avoid stale figure state. Also make sure `matplotlib.use('Agg')` stays set for headless operation (it already is) and that the script still writes `/home/user/tracking.json` correctly. Run the fixed script against `/app/experiment_feed.mp4` to generate both artifacts. Then stand up an HTTP server bound to `0.0.0.0:8000` (Flask/FastAPI/`http.server` all work) with `GET /api/tracking` reading and returning the JSON file's contents with `Content-Type: application/json`, and `GET /api/plot` streaming the PNG file back with `Content-Type: image/png` (verify by checking the file begins with the PNG magic bytes). Launch the server as a background/daemon process (e.g. `nohup python3 server.py &` or a threaded run) so it keeps listening after your session ends.