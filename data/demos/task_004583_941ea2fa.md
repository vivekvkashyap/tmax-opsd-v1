Separate the two hard parts and validate each independently before combining. First, reverse-engineer /app/token_validator purely by black-box probing plus static inspection (`objdump -d`, `strings`, or `gdb`) rather than guessing: since it takes a 16-hex-char argument, try structured inputs (fix most bytes, vary one) and watch the exit code to infer per-byte constraints (e.g. a fixed first byte, a checksum/XOR relation across the decoded bytes) — script a small brute-force/differential harness that calls the binary with many candidate tokens and exit-code-checks results, rather than reading disassembly cold. Re-implement whatever rule you derive natively in C (no calling the binary). Second, build the mTLS server with OpenSSL's SSL_CTX using SSL_VERIFY_PEER|SSL_VERIFY_FAIL_IF_NO_PEER_CERT, load ca.crt as the trust store, and after the handshake extract the peer cert's subject CN (via X509_NAME_get_text_by_NID) and compare it exactly to "Rotation-Admin", closing the connection otherwise. Parse the `ROTATE <service_id> <token>\n` line, run it through your reimplemented validator, and on success append `<service_id>:<new_password>\n` to /home/user/creds.log — create the file with `open(..., O_CREAT, 0600)` (or chmod immediately after creation, before any content could be read by others) so permissions are correct from the first write. Compile with -lssl -lcrypto, run it in the background, and test each of the five verifier scenarios yourself with openssl s_client before considering it done.
