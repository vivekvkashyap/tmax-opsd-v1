Write a single recursive CTE in SQLite rather than looping in bash — bash should just invoke `sqlite3` with the SQL and redirect to the CSV. Structure the CTE's base case as: select `source_id`, `target_id`, hop count 1, from `AccessGrants` joined to `Entities` on `source_id` where that entity is `type='USER' AND status='REVOKED'`, and grant `status='ACTIVE'`. The recursive case joins the CTE's current frontier (`target_id`) as the new `source_id` in another `AccessGrants` row with `status='ACTIVE'`, incrementing hops by 1 — be careful to carry the *original* revoked user's id/name through the recursion (not the intermediate node) so the final report attributes hops correctly back to the source user, and to avoid infinite cycles add a hop-count cap or a "visited" guard if the graph could cycle. Then select final rows only where the reached entity's type is `DATA_LAKE` or `FINANCE_DB`, picking the minimum hop count per (source, target) pair if multiple paths exist (verify from the described topology whether dedup/min is needed). Output header + rows via `.mode csv` and `.headers on` or a manual header echo, ordered by `Hops, Source_User, Target_Entity` using `ORDER BY` in the query itself. Make the script executable and run it once to sanity-check the CSV content against the graph you can reconstruct from the schema.
