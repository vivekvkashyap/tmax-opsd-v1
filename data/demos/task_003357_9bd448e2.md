Work phase by phase, testing each in isolation before wiring them together. First fix the Makefile: a static archive must be built with `ar rcs libpld.a pld.o`, not `gcc -shared` or `ld -r` (those don't produce a valid `.a` archive) — verify with `ar t libpld.a` and `nm libpld.a` showing `parse_pld`. For the Rust service, scaffold with `cargo new`, add `tonic`/`prost`/`tonic-build`, define the `.proto` with a `bytes payload` request and `bool is_safe` response, and write `build.rs` to run `tonic_build::compile_protos` and emit `cargo:rustc-link-lib=static=pld` plus `cargo:rustc-link-search=native=/app/libpld` so the archive links via FFI (declare an `extern "C"` block matching the C struct layout exactly). For detection, scan the returned `code` slice as raw bytes for the two-byte sequences `0x0F 0x05` and `0xCD 0x80` as a sliding byte window (not opcode-aligned instruction decoding — unnecessary here). Build the CLI client separately as `src/bin/client.rs`, then start the server in the background before finishing so it stays reachable on 127.0.0.1:50051.
