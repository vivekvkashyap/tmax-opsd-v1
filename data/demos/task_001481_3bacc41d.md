Reverse-engineer `/app/legacy_emu` empirically before writing any server code: run `strings` on it for clues, then create small test input files by hand (e.g. one line pushing a couple of integers and one output op) and run `./legacy_emu file.txt`, iterating on token guesses (push, add, subtract, multiply, output) and observing stdout for each, including deliberately-broken inputs (empty stack ops) to learn the exact error string it prints on underflow — don't guess the opcode set or error word, derive them from actual runs. Once you have a confirmed grammar, implement a small Python class holding a list-as-stack and a token dispatch loop, raising/catching underflow the same way the binary does, and reuse this class inside `server.py`'s message handler — never shell out to the binary from the server. For rate limiting, keep a per-`client_id` deque/list of recent request timestamps and reject if more than 5 fall within the last second, checked before doing any emulation work. Build `e2e_test.py` to launch the server with `subprocess.Popen`, wait briefly for the socket to be ready, use the `websockets` client to send a known arithmetic program and assert the result, then fire >5 rapid requests to confirm the rate-limit error appears, finally terminating the subprocess in a `finally` block.
