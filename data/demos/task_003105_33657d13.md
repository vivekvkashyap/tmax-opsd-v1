Work through the three bugs independently, testing after each fix. First, export `DATA_DIR=/home/user/data` (or set it inline when running the binary) so the program can locate `wal.log`. Second, open the WAL-parsing loop in `pipeline.c`: instead of calling `exit(1)` when `sscanf(line, "%d %f", ...)` doesn't return 2, just `continue` to the next line so malformed entries are skipped while valid ones still accumulate into the state array. Third, inspect the convergence loop: the `diff` calculation is missing an absolute value, so it can go negative and cause the `while (diff > threshold)` condition to falsely exit early or let the sequence diverge/oscillate — wrap the difference in `fabs()` (include `<math.h>`, link with `-lm` if needed) so it measures true magnitude of change between iterations. After all three fixes, recompile with `gcc pipeline.c -o /home/user/pipeline -lm`, run it with `DATA_DIR=/home/user/data /home/user/pipeline`, and confirm `/home/user/output.txt` is produced. Verify by manually tracing a few WAL lines by hand to sanity-check the recovered state before trusting the final convergence output.