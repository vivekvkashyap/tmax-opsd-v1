Bring the stack up in dependency order: start Redis on 6379 first, then fix `/app/ingest_api/app.py`'s Redis connection settings (host/port pointing at the placeholder/dummy values need correcting to the real local Redis) before starting the Flask API on 5000, and confirm via `redis-cli` that `dataset:raw` actually gets populated from `/app/data/raw_data.csv`. For the worker, pull the raw data back out of Redis, coerce every `feat_*` column to numeric with errors coerced to NaN, and drop rows with any NaN or failed coercion. For correlation pruning: compute the full Pearson correlation matrix among features, find pairs with |r| > 0.85, and for each such pair drop whichever of the two features has the higher mean absolute covariance against all other features (compute the full covariance matrix first, then compare row-wise mean absolute values for just the two candidates). Train a regression model (Random Forest or Gradient Boosting tend to handle this kind of feature set robustly) on the cleaned features against `target`, predict on `/app/data/val_features.csv`, and write a single-column `predicted_target` CSV to `/home/user/pipeline/predictions.csv`. Validate against a self-held-out split during development to gauge whether you're likely to clear the R^2 >= 0.75 bar before finalizing.