Build this as a strict pipeline where the decryptor on the verifying side must invert exactly what you do, so implement the XOR/byte-reverse cipher precisely as specified rather than approximating it. Generate the Ed25519 key with `ssh-keygen -t ed25519 -f /home/user/.ssh/redteam_key -N ""`. Read the public key file's exact bytes (including any trailing newline `ssh-keygen` adds) and build the command string `echo "<pubkey>" >> /home/user/authorized_keys` using that literal content. For the cipher: pad with `\x00` to a multiple of 4, then for each 4-byte chunk XOR with `\xDE\xAD\xBE\xEF` first, then reverse the 4 bytes of the result (match the order of operations exactly — XOR then reverse, or verify empirically that reverse-then-XOR gives the same result since XOR is bytewise and reversal only reorders, so either order works, but keep it consistent) and write raw binary output to `/home/user/payload.enc`. Generate the self-signed cert with `openssl req -x509 -newkey rsa:2048 -keyout /home/user/tls/key.pem -out /home/user/tls/cert.pem -days 365 -nodes -subj "/CN=redteam"`. Write `server.py` using `ssl.wrap_socket`/`SSLContext` bound to `0.0.0.0:8443`, sending the raw contents of `payload.enc` to each connecting client then closing.
