Use Biopython's `SeqIO.read('/home/user/gene.fasta', 'fasta')` to get the sequence, then compute length L and GC ratio R = (count('G')+count('C'))/L directly from the parsed sequence string — don't hand-parse the FASTA header/wrapping yourself, that's where naive attempts introduce off-by-one errors. Define the ODE `dY/dt = -R*Y + (L/1000)*cos(t)` as a function `f(t, y)` for `scipy.integrate.solve_ivp`, and generate the evaluation grid with `t_eval = np.arange(0, 50.0 + 0.005, 0.01)` (or equivalent) so you get exactly the points 0.00, 0.01, ..., 50.00 — a plain `np.linspace(0,50,5000)` will silently produce the wrong spacing/count due to floating point endpoints, so verify `len(t_eval)` and the last value before trusting it. Call `solve_ivp(f, [0,50], [L], method='RK45', t_eval=t_eval)`. Integrate the resulting Y array over `t_eval` with `scipy.integrate.simpson` (not `np.sum`/`np.trapz`, which accumulate more error over many small steps). Round the final scalar to exactly 3 decimals and write only that string to `/home/user/result.txt`.
