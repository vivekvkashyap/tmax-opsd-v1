Start by getting the vendored `microtar` library to build: inspect `/app/vendored/microtar-0.1.0/Makefile` for the specific breakage (bad compiler flags, tabs-vs-spaces in recipe lines) and fix it minimally so `make` produces `libmicrotar.a` — don't rewrite the library itself, just make the build succeed. Then write `dataset_scanner.c` to open the archive with `mtar_open()`, loop `mtar_read_header()`/`mtar_next()` over every entry, and for each entry's `name` field check for traversal patterns: any occurrence of `../` or `..\`, or a path starting with `/` (absolute path) — flag as malicious on the *first* match found, regardless of position within the path (don't only check the start of the string, since traversal segments can appear mid-path like `data/../../home`). Print exactly `MALICIOUS` and `exit(1)` on detection, or `CLEAN` and `exit(0)` if no header in the archive triggers a match. Compile linking against `libmicrotar.a` (`gcc dataset_scanner.c -I/app/vendored/microtar-0.1.0/src -L/app/vendored/microtar-0.1.0 -lmicrotar -o /home/user/dataset_scanner`). Before finishing, loop your own scanner over every file in `/app/corpora/clean/` and `/app/corpora/evil/` and confirm each exit code matches expectations — this is exactly what the automated verifier will do.
