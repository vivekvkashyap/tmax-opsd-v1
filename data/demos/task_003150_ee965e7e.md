Work the three phases in order, verifying each before moving on. First fix `/home/user/clib/Makefile`: it needs a proper target rule (tab-indented recipe lines under `all:`), and the compile step must add `-fPIC -shared` (position-independent code is mandatory for a `.so`) — compile the `.c` to a `.o` with `-fPIC -c`, then link with `-shared -o libhwscore.so`. Run `make` and confirm `libhwscore.so` exists and `nm -D` shows `get_hw_score`. For the Go tool, use `import "C"` with a `#cgo CFLAGS: -I<path>` and `#cgo LDFLAGS: -L<path> -lhwscore` pragma pointing at `/home/user/clib`, and set rpath (`-Wl,-rpath,<path>`) or export `LD_LIBRARY_PATH` at runtime so the binary can find the shared lib without manual intervention. Parse each line of `rules.txt` as space-separated RPN tokens with a simple stack evaluator; when you hit `HW`, call the CGO function with the 1-indexed line number as seed, push the int result. Write one result per line to `eval_results.txt`. For testability, factor the stack-evaluation core into `EvaluateRPN(expr string, hwFunc func(int) int, seed int) int` so `pipeline_test.go` can inject a fake `hwFunc` and avoid needing CGO at test time; write at least two table-style tests exercising different operators and confirm `go test pipeline_test.go pipeline.go` passes.