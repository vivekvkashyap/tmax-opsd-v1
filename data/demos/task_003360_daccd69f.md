This is a multi-service pipeline outage — diagnose each hop independently before assuming the fix works end-to-end. First reverse-engineer `/home/user/oracle_transformer` by piping known inputs through it (`echo -n "AABBBC" | /home/user/oracle_transformer`) to confirm the RLE rule and how non-uppercase bytes pass through, then write equivalent Rust reading all of stdin as bytes, scanning for runs of identical uppercase A-Z bytes and emitting `<count><char>`, passing everything else through unchanged; compile it to `/home/user/app/transformer` and diff its output against the oracle on varied fixtures before moving on. Next run `server.py` under systemd — write a user unit with `Environment="TRANSFORMER_PATH=/home/user/app/transformer"` and `Restart=on-failure`, then `daemon-reload` and enable/start it, confirming with `curl` on port 8080 directly. Only then check `nginx.conf`'s `proxy_pass` — compare its target port against where the backend actually listens and correct the mismatch, reload nginx, and curl the 9090 endpoint. Finally write `healthcheck.sh` to POST `XYZ`, check the response matches the expected RLE, append `OK <timestamp>` to `status.log`, and schedule it via `crontab -e` for user `user` every minute.
