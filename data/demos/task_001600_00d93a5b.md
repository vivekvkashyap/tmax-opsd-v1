Work through this in dependency order since later steps read outputs of earlier ones. (1) For symlinks: iterate every symlink in `/home/user/dataset/`, resolve its target with `readlink -f` or by following chains manually — a link is "broken" if the final target doesn't exist, and "looped" if resolution revisits a path already seen (classic mutual A->B->A cycle) or exceeds a sane hop limit; delete only those, leave valid symlinks alone. (2) For archives: test each `.zip` with `unzip -t` (or `zipfile.testzip` in Python) — nonzero exit or a bad-CRC entry means corrupt, so `mv` it to `/home/user/dataset/corrupt/`; otherwise extract it into `/home/user/dataset/extracted/`. (3) Bulk rename: glob `raw_data_*.dat`, capture the `<ID>` from the filename, read the file, append a `VERIFIED` line, and write to `processed_<ID>.txt` (remove or rename in place, don't leave stale `.dat` files if the task implies replacement). (4) Use `ffmpeg -ss 00:00:02 -i experiment_video.mp4 -frames:v 1 video_frame.jpg` (seek before `-i` for exactness) to grab the frame. (5) Build one Python script combining `http.server`'s SimpleHTTPRequestHandler rooted at the dataset dir on 8080, and a raw socket TCP server on 8081 that reads until `\n`, checks for `GET_FRAME_SIZE`, replies with `os.path.getsize(...)` plus `\n`, then closes the connection — run both concurrently (threads) and detach/nohup so they persist after your session ends.