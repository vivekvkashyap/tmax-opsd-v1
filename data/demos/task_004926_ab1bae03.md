Do the three parts independently, testing each in isolation before wiring them together. Compile the shared library with `gcc -shared -fPIC -o /home/user/libmath.so /home/user/math_lib.c`, then quickly sanity-check it loads via `ctypes.CDLL("/home/user/libmath.so")` and that `calc_variance` returns a sane value for a known input before moving on. For the schema migration, use `ALTER TABLE records ADD COLUMN variance REAL` via sqlite3 (either the CLI or Python's `sqlite3` module) rather than recreating the table, so existing data/constraints survive. In `process.py`, parse `--data` with `argparse`, split on commas, and validate strictly: must be exactly 5 tokens, each must parse as an integer (reject floats/non-numerics) and lie in [0, 100] — on any failure write the literal string `INVALID_INPUT` to `/home/user/error.log` and return before touching ctypes or the DB. On the valid path, set `argtypes`/`restype` on the ctypes function (`c_float` array pointer and `c_int`, returning `c_float`) to avoid garbage results, build a `(ctypes.c_float * 5)(*values)` array, call `calc_variance`, compute the Python-side mean separately, and `INSERT INTO records (input_mean, variance) VALUES (?, ?)`. Finally run both example invocations to populate the DB row and the error log as specified.