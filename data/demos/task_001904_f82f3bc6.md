Work through the four objectives in order, verifying each in isolation before wiring services together. First, read `/app/frontend/app.py` looking specifically for user input flowing into a file-open or path-construction call (e.g. `open(f"/app/data/{user_input}")`) — that pattern is a path traversal flaw (CWE-22), not command injection, unless you actually find `os.system`/`subprocess` with unsanitized input (which would be CWE-78); write only the matching ID to `/home/user/frontend_cwe.txt` and fix the code by validating/sanitizing the filename (e.g., `os.path.basename` plus an allowlist directory check) rather than blocklisting characters. Second, in `backup.sh` find the wildcard-glob `tar` invocation and replace it with a form immune to argument injection from crafted filenames (e.g., `tar -cf backup.tar .` or `--` plus explicit paths), never `tar ... *`. Third, treat `cipher.py` as a black-box XOR-style cipher: use the known plaintext/ciphertext pair in `test_vectors.txt` to algebraically recover the repeating key (XOR each ciphertext byte with the matching known plaintext byte), then apply that recovered key to decrypt `secret.enc` and write the exact resulting string to `/home/user/flag.txt`. Finally, fix the `.env` files to point to `127.0.0.1:5000` and `127.0.0.1:6379`, run `/app/start_services.sh`, and confirm `curl http://127.0.0.1:8080/health` returns the expected JSON before finishing.
