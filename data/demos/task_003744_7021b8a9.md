Diagnose the vendored package before writing any new service code. In `/app/mlops-artifact-utils/mlops_artifact_utils/plot_ops.py`, change the hardcoded GUI backend (`matplotlib.use('TkAgg')`) to `matplotlib.use('Agg')` so plotting works headlessly. In `math_ops.py`, inspect `cosine_similarity_matrix`: it currently divides the dot-product matrix by the *sum* of row/column norms (`norms[:, None] + norms[None, :]`), which is mathematically wrong for cosine similarity — fix it to divide by the *product* of norms (`norms[:, None] * norms[None, :]`) as true cosine similarity requires. Reinstall the package (`pip install -e /app/mlops-artifact-utils`) after each fix and sanity-check `cosine_similarity_matrix` on a small known vector pair by hand (e.g. identical vectors should give similarity 1.0) to confirm the fix is correct before moving on. Load `/home/user/data/artifacts.csv`, use the fixed package to generate and save a PCA scatter plot to `/home/user/artifact_clusters.png`. Then build the API service on `127.0.0.1:8080` with `GET /api/v1/similar?id=...`, enforcing the exact header `Authorization: Bearer mlops-secret-77` (401 if missing/wrong), computing top-3 most similar artifacts (excluding self) via the fixed cosine similarity matrix over the artifact feature columns, and returning the JSON shape specified. Leave the service running and test it yourself with curl for both authorized and unauthorized requests before finishing.