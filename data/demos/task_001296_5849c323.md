Handle the three subtasks independently and verify each with a direct check before moving to the next. (1) Find world-writable files with `find /home/user/auditing_workspace/www -type f -perm -002` (checks the "others write" bit regardless of other permission bits), log their absolute paths to /home/user/vuln_files.log, then normalize all files to 644 and all directories to 755 (e.g. `find ... -type f -exec chmod 644 {} \;` and `find ... -type d -exec chmod 755 {} \;`) — do this normalization step for everything in www/, not just the flagged files. (2) For the CSP header, locate where the HTTP response string is built in main.rs and insert the exact header line ending in `\r\n` within the header block, before the blank-line separator that precedes the HTML body — then run `cargo build` and fix any compile errors from string escaping. (3) For SSH hardening, use `grep -q` to check if `PermitRootLogin`/`PasswordAuthentication` directives already exist; if present, edit them in place to `no` (e.g. with sed matching the directive at line start) rather than leaving the old `yes` line active alongside a new one; if absent, append `Directive no` lines. Leave all other lines untouched.