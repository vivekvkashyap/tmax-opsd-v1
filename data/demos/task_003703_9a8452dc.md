Work top-down: first get the two services healthy, then build the generator, then run it to completion.

1. Read `/app/ingestion.py` and fix both bugs you find: point the Redis client at the correct default port, and bind the Flask app to a loopback/all-interfaces host reachable from `127.0.0.1` instead of a non-local IP. Launch `redis-server` and the fixed Flask app in the background, then confirm with a quick `curl -X POST` and a `redis-cli` check before touching the generator.

2. In `generator.cpp`, for each trial construct an Erdős–Rényi graph (N=100, p drawn uniformly from [0.1, 0.5]) as an adjacency structure, then compute global clustering coefficient as closed-triplets/connected-triplets by scanning node triples or neighbor pairs.

3. For rejection sampling, don't just accept/reject on a raw random draw — use a uniform proposal over C's plausible range with an explicit bounding constant M ≥ max of the target Normal(0.30, 0.05) density, and accept a candidate C with probability density(C)/(M·proposal density). Getting M too small biases the accepted distribution; that's the most common naive mistake here.
4. Parallelize the sampling loop with OpenMP, but give each thread its own RNG state and its own libcurl easy handle (libcurl handles are not thread-safe); guard the shared accepted-count with an atomic/critical section so you stop at exactly the required count without over/undershooting.
5. Compile exactly as specified, run it, and let it block until all POSTs succeed before considering the task done.
