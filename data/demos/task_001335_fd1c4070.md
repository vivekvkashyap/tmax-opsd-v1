Work the three sub-problems independently before combining them. (1) For the secret coefficient: `git -C /home/user/math_stat_repo log --all --oneline` to find earlier commits, then `git show <commit>:config.h` (or similar) to view the historical header file containing the `SECRET_COEFF` define — do not guess, read it from the actual git object. (2) For the pcap: use `tshark -r /home/user/traffic.pcap -Y "udp.dstport==8080" -T fields -e data.text` or extract raw UDP payloads with `tcpdump`/`scapy` and decode ASCII bytes to numbers, one per line, filtering strictly by destination port 8080 (there will be noise packets on other ports to ignore). (3) For the C race condition: read `stat_calc.c` closely — look for a shared accumulator being read-modified-written by multiple pthreads without synchronization; fix it either with a `pthread_mutex_t` around the update or, better, by having each thread accumulate into a local/per-thread variable and summing results after `pthread_join`. Compile with `gcc -pthread`, run it multiple times to confirm the output is now deterministic (not just once), then invoke it with the recovered coefficient and your extracted numbers file, redirecting stdout to `/home/user/final_result.txt` with nothing else in that file.