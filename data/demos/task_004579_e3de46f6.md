Build this incrementally, validating at each stage rather than writing the whole pipeline blind. First set up the venv (`python3 -m venv /home/user/venv && source .../bin/activate && pip install numpy scipy pandas`) and always invoke your script through that venv's python. Implement the Van der Pol RHS as a small function `f(t, y)` returning `[y2, mu*(1-y1**2)*y2 - y1]` with mu=3.0, and pass it to `scipy.integrate.solve_ivp` with `method='BDF'` (or `Radau`), `rtol=1e-6, atol=1e-8` — a default explicit method (RK45) will blow up on this stiff system, which is the naive failure mode to avoid. Read reference.csv, integrate each of its initial conditions to t=10.0, and assert `abs(computed - reference) < 1e-3` per component, raising if any fail — this is your correctness gate before generating anything else. Only after validation passes, call `np.random.seed(42)` exactly once, then draw the 100x2 initial-condition array with `np.random.uniform(-3,3,size=(100,2))` in one call (drawing y1 and y2 separately or in a loop will desync the seed from what verification expects). Integrate each pair, collect y1_final/y2_final, write the CSV with the exact column names, then write success.log last, only on success.
