Read the full `/home/user/image_service.py` first to locate the actual leak and race, since both are described structurally (unbounded module-level accumulation, and a shared mutable buffer touched by multiple request threads without synchronization) rather than being subtle algorithmic bugs. For the memory leak: look for any global list/dict/cache that requests append to (e.g. a "history" or results cache) without ever evicting/bounding it, and either remove the accumulation, cap it with an LRU/maxlen, or scope it to the request instead of module level. For the race condition: look for a shared/global buffer or matrix used as scratch space for the SVD/gradient math that concurrent threads read-modify-write into — protect it with a `threading.Lock` around the critical section, or better, allocate a local buffer per request instead of sharing one, since locking a hot path can hurt throughput but a per-thread/local copy avoids contention entirely. After fixing, actually load-test locally (spin up the server, fire concurrent requests with e.g. `concurrent.futures.ThreadPoolExecutor` or `ab`/`hey` against `/app/reference_sample.png`) and watch RSS via `psutil` or `ps` yourself to confirm it stays well under 150MB and that repeated runs return identical signatures, before considering the fix done.