Read all three scripts first (`supervisor.sh`, `sensor.sh`, `processor.sh`) before editing anything, since `sensor.sh` must not be modified. Fix the three issues independently: (1) In `processor.sh`, replace the one-shot `if [ ! -f ready.flag ]; then exit 1; fi` check with a polling wait loop (e.g. `while [ ! -f /home/user/app/data/ready.flag ]; do sleep 0.05; done`) so it blocks until the flag appears instead of racing and exiting early. (2) Fix the timezone by making `processor.sh` itself compute dates in UTC (e.g. `TODAY=$(TZ=UTC date +%Y-%m-%d)`), or by fixing how `supervisor.sh` invokes it — the `TZ=America/New_York` prefix in the existing supervisor is the bug to correct/override so the date comparison matches the UTC dates `sensor.sh` writes. (3) Replace the sequential `for file in ...; do sleep 0.5; cp ...; done` loop with a parallel version — background each iteration with `&` and `wait` at the end, or pipe the file list through `xargs -P <n> -I{}` — so total wall time for 20 files stays under the 3.5s budget once combined with the ~1s sensor boot delay. After editing, run `./supervisor.sh` yourself and time it (`time ./supervisor.sh`) and count files in `output/` to confirm both the count and the timing requirement before finishing.