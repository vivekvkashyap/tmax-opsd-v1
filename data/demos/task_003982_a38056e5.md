Tackle the three parts independently and test each in isolation. For the plot script, the classic headless-Linux failure is matplotlib trying to use an interactive backend with no display — set `matplotlib.use('Agg')` before importing `pyplot`, then confirm the fix by checking the output PNG's file size/pixel variance isn't degenerate (e.g. load it back and verify it isn't a single uniform color). For the threshold, OCR `/app/threshold_image.png` (e.g. `tesseract`) and parse out the numeric value following `THRESHOLD=` — don't hardcode a guess, actually run OCR and sanity-check the parsed float is in a reasonable correlation range (0-1). For the filter tool, load each `.npy` with `numpy.load`, compute `numpy.corrcoef(data, rowvar=False)`, mask the diagonal (self-correlation is always 1), take the max absolute value of the off-diagonal entries, and compare against the OCR'd threshold with `>=` to reject; copy only surviving files to the output dir, creating it if needed. Time each per-file processing call (e.g. `time.perf_counter()` deltas) and write the mean to `benchmark.txt`. Validate end-to-end against both corpora directories before finishing — a threshold off by even reading a wrong digit will misclassify entire batches.
