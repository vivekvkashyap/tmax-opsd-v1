First derive the numeric modulus, don't guess it: use `ffprobe -v error -count_frames -select_streams v:0 -show_entries stream=nb_read_frames -of default=nokey=1:noprint_wrappers=1 /app/calibration.mp4` (or `ffmpeg` frame-counting) to get the exact frame count N, then compute M = N % 100 — bake this derivation into a comment or a startup computation in your script rather than hardcoding a number you assumed. Then build the router/validator as a strict allow-list, not a blacklist: parse the path and query string yourself (don't shell out to `eval` or unguarded parameter expansion on attacker-controlled input), reject anything that isn't exactly `/api/v1/route/calculate` or `/api/v1/route/update`, reject any query keys other than exactly `x` and `y`, and validate each value against a strict `^[1-9][0-9]*$` (or `^[0-9]+$` excluding zero per "positive") regex before ever using it in arithmetic — never pass raw parameter text into `bc`/`expr`/arithmetic expansion without validating it first, since that's how command injection or non-numeric junk sneaks through. Finally check `(x+y) % M == 0` using safe integer arithmetic (`$(( ))` only after validation). Test against both provided corpora directories to confirm 100% correct accept/reject before finishing, and `chmod +x` the script.
