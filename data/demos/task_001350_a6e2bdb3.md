Build `/home/user/analyze.sh` as a strict pipeline using only awk/join/sort/sed, verifying each stage's output before adding the next. First, validate headers: read the first line of each CSV with `head -1` and compare exactly (string equality) to the required header; on mismatch `exit 1` immediately. Second, since `join` requires sorted input on the join key, sort both CSVs (minus header) by `id` numerically before joining them on that column to produce a combined stream of `id,spring_id,displacement,force`. Third, for each `spring_id`, compute the no-intercept regression slope `k = sum(x*y) / sum(x^2)` — do this in a single `awk` pass keyed by `spring_id`, accumulating `sumxy` and `sumxx` and a count, then emit `spring_id,k,count` at the end; use awk's `printf "%.2f"` for rounding to exactly two decimals. Sort the results alphabetically by `spring_id` before writing `/home/user/results.csv` with its exact header. Finally, filter rows where `k > 50` (compare the rounded or raw value per the spec) to build `/home/user/stiff_springs.txt`, sorted alphabetically, one id per line. Make the script executable and run it, then diff the outputs against the expected header/format described in the task.