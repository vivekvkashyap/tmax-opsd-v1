Locate the adaptive step-size block in `/home/user/seq_model/src/main.rs` — it computes an error estimate by comparing an Euler predictor against a Heun corrector, then adjusts `dt`. Read the two branches literally: the current code multiplies `dt` by 1.2 when error EXCEEDS tolerance (growing the step when it should shrink) and by 0.5 when error is within tolerance (shrinking when it should grow) — this is inverted from correct adaptive-stepping logic, which explains the divergence to NaN (steps keep growing after a bad estimate, compounding error). Swap them: on `error > tol`, halve `dt` and retry/skip advancing state; on acceptable error, advance `state`/`t` and grow `dt` by 1.2x. Don't touch anything else — the Heun predictor/corrector math itself is correct. Rebuild and run with `cargo run --release` from `/home/user/seq_model`, confirm the printed final vector contains three finite (non-NaN, non-inf) numbers, and write that comma-separated line exactly as printed into `/home/user/final_population.txt`.