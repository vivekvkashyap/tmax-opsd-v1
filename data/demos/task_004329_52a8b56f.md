Attack the three parts in order; later stages depend on earlier ones being correct and fast. First, inspect vendored `libfastgraph-1.2.0` for injected perturbations: check the `Makefile` for a low optimization level (e.g. `-O0`) and bump it to `-O3`, and check `include/fastgraph_config.h` for a cache-disabling flag set wrong and correct it — then rebuild and install headers/libs under `/home/user/local/{include,lib}`. Second, in `pipeline.cpp`, connect to the SQLite DB and write a recursive CTE (`WITH RECURSIVE ...`) that walks each employee up their `manager_id` chain, emitting an edge per hop with weight halved each level, then join/union that with the `interactions` table into one unified edge list — sanity-check the SQL via the `sqlite3` CLI before embedding it in C++. Third, feed the unified edges into your fixed `libfastgraph` PageRank routine, write `id,pagerank` to the CSV, and profile `pipeline_exec` end-to-end — if it's not comfortably under 1.5s, the library perturbation fix is almost certainly the bottleneck, so re-check the Makefile/config header before micro-optimizing your own code.
