Fix the infrastructure bottom-up: socket path, then the decoder service, then the proxy, then the orchestration script. First correct `/home/user/nginx.conf`'s `proxy_pass` to point at `unix:/tmp/dtmf.sock` instead of the wrong socket. Then write `/home/user/server.go` to listen on that Unix socket (use `net.Listen("unix", "/tmp/dtmf.sock")` with an `http.Serve`), handling `GET /decode`. For the actual DTMF decoding, don't try to hand-roll Goertzel/FFT tone detection in Go from scratch under time pressure — shell out to a proven decoder like `multimon-ng` (install it if missing) piping `/app/signal.wav` through it, then parse its DTMF output line(s) into a plain digit string. Test the decoder standalone against the wav file first, independent of the web stack, so you know what raw output format to parse before wiring it into the handler. Finally write `/home/user/start.sh` to compile and background the Go binary, then background nginx with the corrected config and a custom pid file path (`-c /home/user/nginx.conf -g "pid /home/user/nginx.pid;"`), and manually curl `http://127.0.0.1:8080/decode` to confirm the returned digit string closely matches the tones actually present in the audio before declaring done.
