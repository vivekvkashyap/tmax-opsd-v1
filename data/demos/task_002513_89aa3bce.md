Build the SQLite window-function query first in the `sqlite3` CLI against `/home/user/metrics.db` before embedding it in C, so you can validate the logic quickly. Use SQL's native window function support: `AVG(cpu_usage) OVER (PARTITION BY server_id ORDER BY timestamp ROWS BETWEEN 2 PRECEDING AND CURRENT ROW)` computes the moving average over the current row plus the 2 preceding rows directly — avoid hand-rolling a manual sliding-window loop in C, which is error-prone at the edges (rows 0 and 1 naturally have fewer than 3 values in the window, and that's correct/expected, not a bug). Wrap this as a subquery/CTE, then filter in an outer `WHERE cpu_usage > moving_avg * ?` clause, using `sqlite3_bind_text` for `server_id` and `sqlite3_bind_double` for the threshold multiplier (never string-interpolate parameters into the SQL text). Iterate results with `sqlite3_step`, pull columns via `sqlite3_column_*`, and format each output line with `%.2f` for the floating point fields exactly as `Server: <id>, TS: <ts>, CPU: <cpu>, MA: <ma>`, appending to `/home/user/anomalies.log`. Always check return codes from `sqlite3_open`, `sqlite3_prepare_v2`, and `sqlite3_step`, and call `sqlite3_finalize`/`sqlite3_close` before exit. Compile with `-lsqlite3`, then run against `server_A 1.5` to produce the log.