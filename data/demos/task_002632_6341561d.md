Build this as a single ordered pandas pipeline so each stage only sees data that already passed the prior gate. Load both CSVs, then filter customers to rows where `email.str.contains('@')` and transactions to rows where `amount >= 0.0` (strictly excluding negative), applying these filters before any grouping. Compute each user's median amount with `groupby('user_id')['amount'].transform('median')` on the already-filtered transactions, then flag `is_anomaly` as `amount > 3.0 * median` (strict greater-than) — do this before masking or joining so anomaly detection uses raw amounts and medians. Mask `last_name` to a fixed placeholder and `email` by splitting on `@` and replacing only the local part, keeping the domain. Then `merge` the masked customers with the anomaly-scored transactions on `user_id` using an inner join, reorder/select the exact required output columns, and write to CSV with `index=False`. Separately compute total row count and anomaly count from the final joined frame and dump a JSON summary with exactly the two specified keys. Create the output directory with `os.makedirs(..., exist_ok=True)` before writing.
