Treat this as three independent forensic sub-tasks. First, parse `requests.json`, split each JWT on `.`, base64url-decode the header of each token, and flag any token whose `alg` field (case-insensitively) is `none` — then base64url-decode its payload JSON and pull out the `username` claim. Second, isolate the tokens that use `HS256`, and write a small Python brute-force loop that reads each line of `wordlist.txt` as a candidate secret, recomputes `HMAC-SHA256(candidate, header.payload)` base64url-encoded (careful to strip `=` padding to match typical JWT encoding), and compares it to the token's actual signature segment — the first candidate that matches all valid tokens is the cracked secret. Third, use `openssl x509 -in server.crt -noout -issuer` (or the `cryptography` Python library) to read the certificate and pull just the `CN=` component out of the issuer string. Assemble the three findings into `/home/user/audit_trail.txt` in the exact `KEY:value` format requested, one per line, no extra spaces or quoting.