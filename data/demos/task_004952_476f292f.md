For the vendored fix, inspect `/app/smtpd-stub-0.0.1/run.sh` for the hardcoded port assignment (something like `PORT=0`) and replace it with logic that reads `$SMTP_PORT`, defaulting to `2525` when unset or empty — the idiomatic bash form is `PORT="${SMTP_PORT:-2525}"` — then pass `--port "$PORT"` through to `server.py` unchanged. Test both cases directly: run with `SMTP_PORT=8888 ./run.sh` and confirm the invoked command shows port 8888, then run with `SMTP_PORT` unset and confirm it falls back to 2525. For the classifier, write `/home/user/classify_email` as an executable script that reads the whole file, counts lines matching `^[Ss]ubject:` case-insensitively (grep -ci or equivalent) and checks for the literal substring `X-Admin-Bypass: 1`; exit 1 if either condition holds, else exit 0. Validate it against every file in `/app/emails/clean/` (expect exit 0) and `/app/emails/evil/` (expect exit 1) before moving on. For the profile script, use plain `export VAR=value` lines and define `start_tunnel` as a bash function whose body is exactly the specified `ssh -f -N -L ...` command, then `chmod +x` and source-test the profile to confirm all three checks (env vars + function body via `type start_tunnel`) pass.
