Start by transcribing the audio to pin down the exact filter criteria before writing any C: play/inspect `/app/incident_alert.wav` with a speech-to-text tool (e.g. `whisper` if available, or another local ASR) and note the node type and the two timestamp bounds it specifies — use those recovered values as constants in your program rather than guessing. In `recovery_processor.c`, avoid the O(N^2) trap: build a hash table (or sorted array + binary search) keyed by `node_id` while streaming `nodes.csv` once, and simultaneously build an adjacency structure from `edges.csv` filtered to `DEPENDS_ON` relationships so each node's neighbor lookup is O(1)/O(log N) rather than rescanning the whole edge file per node — this is what the performance check (runtime < 2s) is actually testing. Filter nodes by the recovered type and timestamp range, then for each match gather its `DEPENDS_ON` neighbors, sort them by timestamp, and compute a size-3 rolling moving average over their `value` field (decide and stay consistent about how to handle windows with fewer than 3 neighbors, e.g. average over however many are available). Write `node_id,moving_average` lines to `recovery_summary.txt`, compile with `gcc`, and run it once to confirm the output file is produced and the program completes quickly.
