Build this as a single reproducible Python script rather than ad-hoc shell commands. Install the three packages first, then write one script that: loads the CSV with pandas, imputes only `sensor_3`'s missing values with `df['sensor_3'].fillna(df['sensor_3'].median())` (compute median before any scaling), fits `StandardScaler` on the five sensor columns and replaces them with the scaled values, then does `train_test_split(..., test_size=0.2, random_state=42)` — apply the split consistently to features and target together so rows stay aligned. Set `mlflow.set_tracking_uri('file:///home/user/mlruns')` and `mlflow.set_experiment('Sensor_Failure_Exp')` before opening `with mlflow.start_run():`. Inside the run, train the RandomForest with the exact given hyperparameters, compute `accuracy_score` on the held-out test set, then call `mlflow.log_param`, `mlflow.log_metric('test_accuracy', ...)`, `mlflow.sklearn.log_model(model, 'rf_model')`, and `mlflow.set_tag('pipeline_version', 'v1.0')` — all within the same run context so they attach to one run. Finally write the exact same accuracy float you logged to `test_results.json` (don't recompute it separately, or rounding/reruns could make the two diverge) and re-run the script once, checking `mlruns/` populated and the JSON is valid before finishing.
