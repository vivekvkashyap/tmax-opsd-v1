Treat the binary purely as a black box you must characterize empirically, not something to disassemble by hand. Generate a broad range of probe sequences (short, long, homopolymer runs like AAAA, alternating patterns, mixed) and run `/app/seq_evaluator <seq>` on each, recording input/output pairs — save these to your HDF5 file and log your reasoning/observations in the notebook as you go. Look for patterns by isolating variables: test single-character sequences of each base to infer per-base weights, then test how the total scales with length to infer whether it's a simple sum or a weighted average with special edge handling (compare a sequence's score to (sum of per-base values) to detect boundary effects — try sequences differing only at the first/last character to detect if endpoints are treated specially, which is the hallmark of a trapezoidal-style integration rule). Once you have a formula that reproduces every probe exactly, implement it in Go, being careful to match the binary's print formatting (decimal precision) exactly since the equivalence check is byte-for-byte on stdout. Before finishing, fuzz your own Go program against the binary locally on many random sequences to catch precision or edge-case mismatches (e.g. sequences of length exactly 1 or 2).