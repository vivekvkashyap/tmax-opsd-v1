Start by reading `/home/user/legacy/parser.rb` closely to extract the exact `pack`/`unpack` format directives (they tell you field sizes and byte order — Ruby's `N` means big-endian 32-bit, etc.) and reproduce that layout with C++ struct reads or manual byte shifts, not `reinterpret_cast` onto a struct (that breaks on endianness/padding). If a black-box reference tool is available for the classification logic, use it only to probe edge-case behavior (feed it sample packets and observe exit codes) to sanity-check your own independently-derived parsing/classification logic — never disassemble or copy from it directly; your C++ implementation must stand on its own from the Ruby source and SDK headers. For linking, the circular `libpacket.so`/`libcrypto.so` dependency needs both libraries wrapped with `-Wl,--start-group -lpacket -lcrypto -Wl,--end-group` (plus the appropriate `-L`/`-I` flags for the SDK) so the linker resolves symbols in both directions in one pass. Compile with g++, set `LD_LIBRARY_PATH` to the SDK directory at runtime, then iterate against the clean and evil packet corpora checking exit codes 0/1/2 until both classify correctly.
