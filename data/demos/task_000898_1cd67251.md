Split this into an OCR-recovery step and a C++/SQL implementation step, and don't let one contaminate the other. Run `tesseract /app/query_spec.png stdout` (or similar) to read off the dashboard's target sensor ID, status filter, sort field/direction, and pagination limit — inspect the raw OCR text carefully for misreads of similar characters (e.g. `0`/`O`, `1`/`I`) in the sensor ID before trusting it, and cross-check by re-running OCR with different `--psm` page-segmentation modes if the first pass looks garbled. In the C++ program, connect via `sqlite3_open`, run `REINDEX;` (or `REINDEX idx_sensor_time;`) before querying to address the corrupted-index concern, then build the query with `sqlite3_prepare_v2` and bind the sensor/status values with `sqlite3_bind_text` placeholders (`?`) rather than string-concatenating them into SQL — this satisfies both the injection-safety requirement and correctness. Apply the OCR-derived `ORDER BY timestamp <dir> LIMIT <n>` and write `id,value` rows (with header) to `results.csv`. Since grading compares your retrieved ID set via Jaccard similarity against a hidden golden set, sanity-check your query manually against the database (e.g. with the `sqlite3` CLI) before finalizing, since any mismatch in sensor ID, status, or limit will tank the score.