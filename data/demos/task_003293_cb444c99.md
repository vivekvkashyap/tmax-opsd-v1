Split this into OCR extraction, then a performance-critical Rust graph tool. OCR `/app/alert_screenshot.png` with `tesseract` and parse out the transaction ID that follows the literal label `AUDIT_NODE:` in the recognized text — trim whitespace/OCR noise around it rather than assuming a fixed format. For the Rust project at `/home/user/deadlock_analyzer`, since it must handle a hidden graph of ~2M edges in under 0.5s, load the CSV with a fast reader (avoid the `csv` crate's per-field allocation overhead if possible, or at least avoid re-parsing/re-allocating strings repeatedly) and build an adjacency list keyed by a hashmap from string ID to a dense integer index, storing edges as index pairs for cache-friendly traversal. Find the shortest cycle through the target node via BFS from the target back to itself along outgoing edges (BFS gives shortest-path length; to get the lexicographically-smallest among ties, when multiple next-nodes are available at the same BFS layer, prefer expanding/tracking them in sorted order or compare full candidate cycles once found). Emit the JSON array starting at the target node without repeating it at the end. Build with `cargo build --release` — debug builds will almost certainly miss the timing bar — and run once against `/app/locks.csv` to produce `/app/cycle.json`.