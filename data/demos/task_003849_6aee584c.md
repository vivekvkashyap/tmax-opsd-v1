First inspect the schema and decide indexes that actually help the query pattern you'll write: an index on dependencies(target_id) speeds up "who depends on X" lookups (the recursive step goes source->target, so you traverse by matching target_id to already-found ids), plus an index on datasets(domain, size_mb) to help the final filter+sort; create them with `sqlite3 datasets.db "CREATE INDEX ..."` before writing the Go code. In graph_query.go, open the sqlite3 driver, then build a `WITH RECURSIVE` CTE: seed it with the row in datasets matching the input name (via a parameterized `?` placeholder, never string-concatenated), recursively join dependencies where `target_id` equals the previous step's id to walk downstream dependents, collecting all reachable source_ids. Join that CTE's ids back to datasets, filter `domain = 'Genomics'` in SQL (or in Go after the query — SQL is cleaner), `ORDER BY size_mb DESC`, and `LIMIT 3`. Print just the dataset names, one per line. Compile and run it with `Base_Sequences` as argv[1], redirecting stdout to top_consumers.txt, and manually trace the dependency chain from the seed data to confirm your recursive join direction is correct (a common mistake is joining on source_id instead of target_id, which walks the graph backwards).