Transcribe `/app/audio/intercept.wav` first (try `whisper`/`whisper-cli` if installed, else install a lightweight STT tool, else careful manual listening) — the attacker's dictation names a specific custom HTTP header used for C2 beaconing and spells out an HTML tag + event-handler combination used for their XSS payloads (listen for it being spelled out phonetically, e.g. word-by-word, and reconstruct the exact tag/attribute string it describes). Don't assume common defaults like `X-Forwarded-For` or generic `<script>` tags — use exactly what the audio specifies. Then write `/home/user/scanner.go` to read a raw HTTP request file, parse headers and the request line/URI (and body if present), and flag EVIL if: the custom C2 header from the audio is present; the exact XSS tag/handler pattern from the audio appears in body or URI; a URI query parameter named `password`, `pwd`, or `secret_token` has a non-empty value (parse the query string properly, not by substring matching which would false-positive on `clean_2.txt`'s POST body); or the URI contains `UNION SELECT` or `' OR 1=1`. Print exactly `CLEAN` or `EVIL` with no extra whitespace/newlines beyond what stripping handles.