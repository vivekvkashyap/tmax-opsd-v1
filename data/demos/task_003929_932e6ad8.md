In Rust, read `/home/user/incident_log.txt`, locate the `Cookie:` header line, and parse it as `;`-separated `key=value` pairs to pull out the `auth_token` value (a Base64 crate, or hand-rolled decode, works — `base64` crate is simplest). Decode it to raw bytes and check the first 4 bytes equal the ELF magic `0x7F 0x45 0x4C 0x46`; if not, print an error and exit non-zero. Interpret bytes 5-8 as a big-endian (network byte order) u32 and format it as dotted IPv4 by taking each byte in order as one octet (e.g. `u8,u8,u8,u8` joined with `.`) — do this generically from the decoded bytes rather than assuming any particular address, since the actual value depends on what's in the cookie you decode. Write `/home/user/mitigation.sh` starting with `#!/bin/bash` followed by an `iptables -A INPUT -s <ip> -j DROP` line using the IP you computed, then `chmod +x` it (do this from Rust with `std::fs::Permissions` / `set_permissions`, or shell out). Also write `/home/user/csp_header.txt` with exactly the literal CSP header line specified in the task. Compile with `rustc analyzer.rs -o /home/user/analyzer` and run it once to produce both output files.
