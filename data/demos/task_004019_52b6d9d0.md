Read `interpreter.rs` carefully to find where a `String` is moved into a function call and then referenced again afterward (e.g., a debug `eprintln!` using the original variable after it was consumed by value). Fix it minimally and locally: either pass a borrowed reference (`&content`) into the consuming function and adjust its signature to take `&str`/`&String`, or clone the value before the move, or compute the needed length/info before the move happens. Do not touch the actual decoding logic (state machine over the opcode characters) — only resolve the ownership conflict. After `cargo`/`rustc` compiles cleanly, run the build script to produce the binary, then run the wrapper script against the encoded data file, redirecting its stdout exactly into the required output file with `>` (not `tee`, which would risk extra behavior) so the byte-for-byte output — including any trailing newline the program prints — is preserved.
