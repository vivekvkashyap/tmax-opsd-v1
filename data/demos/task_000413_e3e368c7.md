Write the C program in clear stages: (1) open and parse `/home/user/backups/index.csv`, skipping the header line, splitting each row on commas into `BackupID`, `RelativePath`, `HeaderOffset` (use `strtok` or manual scanning; `HeaderOffset` via `atol`/`strtol`). (2) For each row, build the full path by concatenating `/home/user/backups/` with `RelativePath`, `fopen` it in binary mode (`"rb"`), `fseek` to `HeaderOffset`, and `fread` exactly 16 bytes. (3) The first 4 bytes are the magic number — print them as uppercase hex with `sprintf("%02X%02X%02X%02X", b[0],b[1],b[2],b[3])` (read byte-by-byte, no endian conversion needed since you're just concatenating hex digits of raw bytes in file order). (4) Bytes 4-7 are a little-endian uint32 timestamp — reconstruct it as `b[4] | (b[5]<<8) | (b[6]<<16) | ((uint32_t)b[7]<<24)`, not a direct cast, to avoid endianness bugs on different architectures. (5) Emit a JSON array manually via `fprintf` with proper comma placement between objects (careful with the trailing comma on the last element). Compile with `gcc`, run it, and inspect the resulting `/home/user/archive_summary.json` yourself for valid JSON structure before finishing.
