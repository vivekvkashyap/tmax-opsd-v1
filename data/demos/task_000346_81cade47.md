Work through the three artifacts independently, then merge results into the report. For the token: read `auth_capture.txt`, locate the line with `Authorization: Bearer `, strip the prefix to get the raw token string, base64-decode it, and `json.loads` the result to pull out `client_id` and `file_hash` as dict fields — don't hardcode offsets, parse the header line properly since whitespace/casing could vary. For integrity: compute `hashlib.sha256(open(server.bin,'rb').read()).hexdigest()` and compare (case-insensitively is safest) against the `file_hash` field from the token; write "Valid" or "Invalid" based on the actual comparison, not an assumption. For the certificate: parse `server.crt` to get the Issuer's CN — either shell out to `openssl x509 -in server.crt -noout -issuer` and extract the `CN=` substring, or use Python's `cryptography` library (`x509.load_pem_x509_certificate`) and read `cert.issuer.get_attributes_for_oid(NameOID.COMMON_NAME)`; note the issuer CN is distinct from the subject CN since this is likely a CA-signed (not self-signed) cert. Write all three fields to `/home/user/pentest/report.txt` in the exact three-line format specified, then run the script to produce that file.
