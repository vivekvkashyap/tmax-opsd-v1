First recover the actual instruction by transcribing `/app/audio_directive.wav` (use a speech-to-text tool such as whisper if available, or install one) — don't assume what the directive says; the exact target employee and traversal direction (upward through management vs downward through reports) matters for the query result. Once you know the target, load both CSVs into a Go program and build an in-memory graph or recursive CTE over `employee_id -> manager_id` to compute the full management hierarchy (direct and transitive reports/managers as the transcript specifies), then join against `projects.csv` to collect the distinct set of projects tied to that hierarchy — dedupe by project name, not by row count. Serve the count via `net/http` on `127.0.0.1:8080` at `GET /api/v1/projects`, checking the `Authorization: Bearer <token>` header exactly (return 401 on missing/wrong token) before writing `{"result_count": N}` as JSON with status 200. Run the binary in the background (e.g. `nohup ./server &`) so it stays up for the verifier, and manually curl it with and without the header to confirm both paths work before finishing.
