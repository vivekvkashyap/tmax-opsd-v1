Work through this in the stated order and verify each artifact independently before moving to the next. First, rewrite the query as a CTE using `ROW_NUMBER() OVER (PARTITION BY source, target ORDER BY timestamp DESC)` after properly joining edges to nodes twice (once for source's region, once for target's region) with an equality condition `n1.region = n2.region` — this replaces the junior dev's cross-join-without-conditions bug; filter to `rn = 1` to keep only the latest edge per pair. Save this as fixed_query.sql and test it directly in sqlite3 before exporting. Second, add indexes that actually support the join/window (e.g. on edges(source), edges(target), and nodes(id) or nodes(region)) via CREATE INDEX statements in indexes.sql, and confirm with `PRAGMA index_list('edges')` that they were applied. Third, use sqlite3's `.headers on .mode csv` (or `.output`) to export the query results with header row to recent_edges.csv. Fourth, in graph_analytics.py build an undirected adjacency list by adding both directions for each edge row, then run a standard BFS from node 1 to node 5 to get the shortest path (track parent pointers to reconstruct the path, not just the distance), and separately compute each node's unique-neighbor count to find the max-degree node, breaking ties by lowest ID. Write report.json with exactly the two required keys.