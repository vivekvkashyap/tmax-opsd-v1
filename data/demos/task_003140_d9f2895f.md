Fix each layer separately and test each in isolation before wiring them into deploy.sh. (1) In `client.c`, locate the `if (connect(...) < 0)` block and add the missing error handling: write exactly `Error: Connection refused\n` to `stderr` (use `fprintf(stderr, ...)`, not `printf`) and `return 5;` (or `exit(5);`) — verify by compiling and running against a closed port (nothing listening on 9090) and checking both the exit code (`echo $?`) and captured stderr match exactly. (2) In `netclient.service`'s `[Unit]` section, add both `After=netserver.service` and `Requires=netserver.service` — `After` alone only affects ordering, not the hard dependency, so both directives are needed together. (3) Write `deploy.sh` so every step is idempotent: use `mkdir -p` for the systemd user dir, `cp -f` (or plain `cp`, which naturally overwrites) for the unit file, and have gcc compilation check `$?` (or rely on `set -e`) to abort on failure. End the script by writing the exact required string to `deploy.log`. Make the script executable (`chmod +x`) and actually run it, then re-run it a second time to confirm no errors occur on repeat execution.
