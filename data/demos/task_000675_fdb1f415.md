Do a depth-first traversal of `/home/user/dataset` in C using `opendir`/`readdir`, and for each entry use `lstat` to distinguish symlinks from real directories (never `stat`, which would follow the link and recurse into it blindly). Maintain a stack/list of the (device, inode) pairs of directories currently on the active path from the root down to the current position. When you encounter a symlink, resolve it with `readlink`/`realpath` to its target, `stat` that target, and check whether its (device, inode) matches any directory already on your current ancestor stack — if so, following it would create a cycle, so record that symlink's absolute path as loop-causing and do NOT descend into it. If the target is a directory not on the current stack, you may still descend through it as normal (pushing its identity while recursing) but must pop it back off afterward, since revisiting the same directory via a different path than an active ancestor isn't itself a loop. This ancestor-stack-of-inodes technique correctly catches both direct ancestor links and mutual A<->B cycles. Emit all discovered paths as a JSON string array to `/home/user/loops.json`.
