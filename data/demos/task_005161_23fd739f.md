Build the parser in `/home/user/parser.py` as an explicit finite-state machine over lines (states like WAITING_BEGIN, IN_BLOCK, EXPECTING_FILE) using only `str.startswith`/`str.split`, not regex — this matters because the task explicitly forbids regex for block extraction and verification inspects the source for state-machine structure. Ignore blank lines and lines starting with `#`. On `BEGIN <name>` while already inside a block, raise `ValueError`; on `FILE` without a preceding pending `ACTION`, raise `ValueError`; require every `ACTION` to be immediately followed by `FILE` before another `ACTION`/`END`. Return a dict mapping project name to a list of `{"action":..., "file":...}` dicts, preserving file order. For benchmarking, import the same class in `/home/user/benchmark.py`, time exactly 50 repeated full parses of `/home/user/large.actlog` with `time.perf_counter()`, divide by 50, and write only the float to the result file. For the API, instantiate and parse `/home/user/data/project.actlog` once at startup (module-level, not per-request) with FastAPI on `0.0.0.0:8000`, implement `GET /projects` (list of names) and `GET /actions/{project_name}` (list of action/file dicts, or a 404 with the exact `{"detail": "Project not found"}` body via `HTTPException`). Launch the server in the background (e.g. `uvicorn ... &`) so it stays listening, then run the benchmark script and curl-test both endpoints before finishing.