Process the CSV as a true stream: open it with the `csv` module and iterate row by row (or `csv.DictReader`), never call `pandas.read_csv` on the whole file or collect all rows into a list. Since interpolation needs the *next* value for a sensor, keep a small rolling state per sensor: the last confirmed (timestamp, value), and if the current row is blank for a sensor, remember that gap (timestamp) but don't emit it yet — hold at most one pending row per sensor since the problem guarantees no two consecutive missing values. When the next non-blank reading for that sensor arrives, compute the linear interpolation between the last confirmed value and this new value at the gap's timestamp, emit the completed long-format record for the gap, then emit the current record too. This way you only ever hold a handful of pending values in memory, not the whole file. Accumulate the running total sum incrementally as you emit each `(timestamp, sensor, value)` triple rather than storing them all, then write the final integer/rounded sum to `/home/user/result.txt`.
