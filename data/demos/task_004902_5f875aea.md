This is graded by MAE against a reference pipeline, so get the pipeline order exactly right rather than approximating: pipe `/home/user/events.csv` (header included) through `/app/feature_extractor` first since dedup/sort must operate on its output, not the raw input. For dedup, since the binary injects duplicates but doesn't reorder within-duplicate content, key on `id` and keep only the first (or any single) occurrence per id — e.g. `awk -F, '!seen[$1]++'` (skip the header row specially, or reattach it after processing). Sort numerically by `timestamp` ascending (`sort -t, -k2,2n`), being careful this is a numeric field not a string sort. For the rolling average, use `awk` with a sliding window buffer of the last up to 3 `extracted_value`s (current + up to 2 preceding, in chronological order) computing `sum/count` and formatting with `printf "%.2f"`. For stratified sampling, group the chronologically-sorted, rolling-averaged data by `category` and keep only each category's first 2 rows in time order (e.g. `awk` tracking a per-category counter, since `head`/`sort -u` on category alone won't preserve per-category counts correctly). Write the final CSV with header `id,timestamp,category,extracted_value,rolling_avg` to `/home/user/final_sample.csv`, chmod the script `+x`, and manually verify a few rows' rolling averages by hand before trusting the pipeline.