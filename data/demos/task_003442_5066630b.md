Transcribe `/app/memo.wav` first (e.g. `ffmpeg` + a Python speech-to-text library, or `whisper`) to recover the two hidden parameters — the rolling-window size `W` and the exact stop-word to filter — and use those values (not guesses) as constants in your Rust program. Parse each stdin line as JSON (e.g. with `serde_json`), then for the `note` field decode `\uXXXX` escapes to real UTF-8 (this typically happens automatically once you deserialize the JSON string properly — make sure you're not double-escaping or leaving literal backslash-u sequences by using a real JSON parser rather than manual string replace). Tokenize on whitespace, strip ASCII punctuation from each token's edges/interior per the given punctuation set, lowercase, drop empties, then drop tokens exactly equal to the transcribed stop-word. For the rolling sum, maintain a `VecDeque<i64>` of the last `W` values plus a running sum you increment on push and decrement when popping once the deque exceeds length `W`; for the first `W-1` records the sum is just of everything seen so far, which falls out naturally if you only start evicting once the deque length exceeds `W`. Emit one JSON object per input line to stdout in the required schema, then build with `cargo build --release` (or equivalent) to produce the binary at exactly `/home/user/pipeline`, and self-test against a few crafted inputs including punctuation, unicode escapes, and window-boundary cases before trusting it.
