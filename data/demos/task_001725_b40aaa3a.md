Tackle this in independent, verifiable stages. First, get the frame count: run `ffprobe -v error -select_streams v:0 -count_packets -show_entries stream=nb_read_packets -of csv=p=0 /app/surveillance.mp4` (or an equivalent ffmpeg frame-counting invocation) and capture the integer output — do not guess or hardcode a count. Second, fix SSH: read `~/.ssh/config` for the `auth-service` host block and correct any directive that disables key auth (e.g. `PubkeyAuthentication no` must become `yes`), then verify plain `ssh auth-service` works before adding tunneling. Establish the local forward with `ssh -f -N -L 8000:localhost:8000 auth-service` (or via `LocalForward` in the config) and confirm the tunnel is live with `curl http://localhost:8000/token` — capture that exact response text as your token, don't invent one. Third, write server.py to read `os.environ["API_PORT"]` (export API_PORT=8080 in ~/.bashrc and also export it in the current shell before launching, since a new bashrc line won't affect the running session) and bind 0.0.0.0. In the `/stats` handler, compare the incoming `Authorization: Bearer <token>` header exactly against the retrieved token; return 401 on mismatch/missing, else 200 with `{"total_frames": N}` using the ffprobe-derived N. Launch with `nohup ... &` or `disown` so it survives, then curl it yourself to confirm both the 200 and 401 paths before finishing.