Work through this as four discrete, verifiable stages rather than one big script. First, bulk-rename with a loop (`for f in raw/backup_cfg_*.dat; do ...`) extracting the number via parameter expansion or `sed`/`basename` and moving to `archive_<number>.cpk`. Second, write the C parser carefully to the exact binary layout: read the 8-byte magic and validate it, read a little-endian uint32 for `num_entries`, then loop reading a fixed 64-byte null-padded name buffer (use `strnlen`/ensure null termination) plus a little-endian uint32 `file_size`, then read exactly `file_size` raw bytes. Track a per-archive dedup set of filenames already written (e.g. a small in-memory list checked before writing) — the key bug to avoid is applying dedup globally across archives instead of per-archive, or dedup based on read order rather than "first occurrence wins". Open each extracted file in "wx"/create mode inside `extracted/`, since only the first occurrence should ever be written. Third, use `sed -i 's/db_host=legacy-db.local/db_host=db-cluster.aws.internal/' extracted/*` for the text substitution. Fourth, iterate over `extracted/` files sorted alphabetically (`ls | sort` or a glob that sorts by default), writing `=== <filename> ===` then the file's raw content into the merged output — do not add extra blank lines beyond what's naturally in the source files.
