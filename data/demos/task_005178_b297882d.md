Break this into independent stages and verify each before moving on. (1) Extract frames from `/app/config_dashboard.mp4` with `ffmpeg -i ... frames/frame_%04d.png` (or via OpenCV), install `tesseract-ocr`/`pytesseract`, and OCR each frame for the `TS: <ts> | HASH: <hash>` pattern with a regex — treat a frame as corrupted when OCR yields nothing (near-black frame) rather than assuming a fixed corruption list. (2) For corrupted frames, linearly interpolate the missing UNIX timestamp from the nearest valid timestamps before and after (round to nearest int) and mark hash `"unknown"`. (3) Stream `/app/agent_logs.jsonl` line-by-line (never `.readlines()`/`json.load` on the whole file — it's huge) parsing each line as JSON and keep only records where `timestamp` matches one of your target timestamps and `event_type == "config_commit"`; build a timestamp→payload lookup as you go rather than re-scanning the file per timestamp. (4) Normalize each matched `config_payload`: lowercase, strip punctuation except `_`, split on whitespace, sort tokens, then compute Jaccard similarity against the previous timestamp's token set (first entry = 1.0) and set `drift_score = round(1 - jaccard, 3)`. Write the ordered list to `/home/user/drift_report.json`.
