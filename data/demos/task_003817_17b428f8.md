Handle the two deliverables independently. For the video CSV, don't hand-roll frame-size parsing — use `ffprobe -v error -select_streams v:0 -show_entries packet=pts_time,size -of csv=p=0 /app/experiment_video.mp4` to get per-packet sizes directly from the container's own packet metadata, then number the rows sequentially starting at 0 to build `frame_index,pkt_size` in `/home/user/video_frames.csv` (matching ffprobe's packet order exactly is what the grader compares against). For the C validator, parse the WAL format as a strict sequential binary reader with careful bounds checks at every step (never trust that a "length" field is safe to read into — check remaining file size before every read): (1) verify the first 4 bytes equal `WAL\x00` exactly; (2) read the next 4 bytes as a little-endian uint32 JSON-length and confirm that many bytes remain in the file before reading them; (3) parse those bytes as JSON (a minimal hand-written or small JSON-parsing routine is fine) and require a `"checksum"` key to be present; (4) treat everything after the JSON as the payload and attempt zlib inflation (`inflate()` from zlib.h), rejecting on any decompression error or size mismatch. Any violation at any stage means print `EVIL`; only a fully valid, cleanly-parsed file prints `CLEAN`. Test your compiled `wal_filter` against the sample files under `/dataset/` before writing the final classification log, iterating on edge cases (truncated length, corrupted zlib stream, malformed JSON) until your local pass rate is 100%.
