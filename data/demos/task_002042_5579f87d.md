For each file in `/home/user/raw_data/`, don't assume a fixed encoding order — detect it robustly, e.g. try `utf-16` decode first (check for a BOM or null-byte pattern typical of UTF-16LE text), then fall back to `utf-8`, then `iso-8859-1` as a last-resort (it never raises on arbitrary bytes, so try it last). For each successfully decoded line: strip leading/trailing whitespace, collapse internal whitespace runs to one space with a regex, lowercase, apply `unicodedata.normalize('NFKC', s)`, and skip lines that end up empty — do this normalization pipeline in one consistent order since order affects the result. Hash the UTF-8 bytes of the final normalized string with `hashlib.sha256().hexdigest()` and dedupe on that hash (first occurrence wins), then write `HASH:STRING` lines to `/home/user/clean_unique.txt` sorted alphabetically by the normalized string, ending with a trailing newline. Run the script and diff/inspect the output file to sanity check that accented characters and casing collapsed correctly across the three differently-encoded source files before considering it done.
