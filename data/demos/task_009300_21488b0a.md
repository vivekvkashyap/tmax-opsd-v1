Load `/home/user/access_graph.ttl` with `rdflib`, then run a SPARQL query selecting `?user ?system` where `?user <http://example.org/audit#accessed> ?system` — use the full predicate URI exactly as given rather than a shortened prefix mismatch, since a wrong prefix binding silently returns zero rows. Build a dict mapping each system URI to the *set* of distinct users that accessed it (a set, not a list/counter, so repeated triples for the same user-system pair don't inflate the count), then compute `user_count = len(set)` per system. Produce the output list as `{"system": <uri as plain string>, "user_count": <int>}` objects, and sort with a compound key: descending by `user_count` first, then ascending alphabetically by the system URI string for ties — Python's `sorted(..., key=lambda x: (-x["user_count"], x["system"]))` handles this cleanly. Write the result as pretty or compact JSON to `/home/user/system_audit.json` and eyeball it against a manual tally of the triples in the Turtle file (count how many distinct subjects point to each object) to sanity-check before finishing — the ordering is graded strictly, so ties must be broken correctly.