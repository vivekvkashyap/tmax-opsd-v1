Split this into compose vs. algorithm work. For compose: compile `sim.c` with `-llapack -lblas`, then launch the three processes in dependency order (logger first, then sim_daemon with `TELEMETRY_URL=127.0.0.1:9002`, then gateway with `BACKEND_URL=127.0.0.1:9001`), each on its specified port, and confirm end-to-end with a curl through the gateway before moving on. For `trace_analyzer.sh`: parse `[src] [dst] [latency]` lines into an adjacency structure, then since the graph is a DAG, compute the longest weighted path with a topological sort followed by DP (for each node in topo order, track best predecessor and cumulative latency), reconstructing the path by backtracking; when latencies tie, compare full path strings lexicographically to pick the winner deterministically. For the histogram, compute global min/max latency across all edges, derive bin width `W=(MAX-MIN)/5`, and bucket each latency with careful boundary handling (last bin inclusive of MAX, watch off-by-one at exact bin edges and floating point rounding with `bc`/`awk`). Fuzz-test your script on many generated DAGs of varying size/latency distributions and compare its output byte-for-byte against the environment's existing reference tool to catch edge-case mismatches (ties, single-bin latencies, boundary values).
