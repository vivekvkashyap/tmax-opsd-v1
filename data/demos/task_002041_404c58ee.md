Reverse-engineer `/app/filter_oracle` first by probing it with varied `feature_1,feature_2,feature_3` triples (extremes, zeros, single-feature sweeps) and recording CLEAN/DIRTY outputs; since the task states it's a linear inequality, fit the coefficients by testing enough points to pin down the boundary (e.g. vary one feature at a time holding others fixed to find where the label flips, then solve for the linear combination) rather than assuming any particular weights. Once you're confident in the rule, implement it directly in `awk` (fast enough for 500k rows) reading `raw_data.csv`, computing the same inequality per row, and writing only CLEAN rows to `/home/user/data/clean_data.csv` — verify your awk logic against a sample of rows you already ran through the oracle itself to be sure they match before running the full file. For serving, write a small bash script using `socat TCP-LISTEN:8888,fork,reuseaddr EXEC:handler.sh` (or an `nc` loop) that parses the request line for the `<id>`, does an `awk -F, '$1==id'` lookup against `clean_data.csv`, and emits the exact HTTP status/blank-line/body format specified — test manually with `curl` before trusting the verifier.
