Extract the event stream first, then build the lock-state simulator, then reason about cycles. Use OpenCV (`cv2.VideoCapture`) to read frames in order and sample the pixel at `(width//2, height//2)` from each frame, remembering OpenCV gives BGR order so map channels back to R,G,B carefully; skip/ignore frames where R==0. Replay the resulting (Tx, Res, Action) sequence through a simple lock-manager simulation: maintain `held = {resource: tx}` and `waiting = {tx: resource}`; on acquire (B=2) set `held[res]=tx` and clear any wait for that tx; on request (B=1) if the resource is free, treat it as immediately grantable per the log (only mark as waiting if `held[res]` is set to a different tx); on release (B=3) clear `held[res]`. After replaying all frames, build a directed graph tx -> resource it's waiting for -> tx holding that resource, and detect cycles via DFS with a visited/in-progress marker (classic cycle detection, not just checking mutual pairs — cycles can be longer than 2 nodes). For `/home/user/detect_deadlock.py`, given a Tx ID, walk this graph from that node; if you return to the start node, print `DEADLOCK: Tx.. -> Res.. -> Tx.. -> ...` following the actual wait-chain edges in order, otherwise print `NO DEADLOCK` exactly. Test by manually tracing the video's event log yourself before trusting the script's output.
