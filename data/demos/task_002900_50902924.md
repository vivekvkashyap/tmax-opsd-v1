Recognize that OLS on collinear reference spectra can yield negative "fractional" coefficients, which is physically meaningless here — the fix is to constrain the fit to be non-negative, not to regularize or clip afterward. Load the CSV with pandas/numpy, build the design matrix from the three reference columns and the target from `observed`, then solve with `scipy.optimize.nnls(A, y)` rather than `np.linalg.lstsq`. After solving, normalize the returned weight vector by dividing by its sum so the three values sum to exactly 1.0 — do this normalization before computing the reconstruction error. Compute the MSE using the *normalized* weights (i.e. reconstruct `A @ w_normalized` and compare to `observed`), not the raw NNLS solution, since the task explicitly asks for MSE of the normalized-weight reconstruction. Format and write the weights (order: alpha, beta, coil) to 4 decimal places as a comma-separated string, and the MSE to 6 decimal places, each to its own specified file, taking care not to add extra whitespace/newlines beyond what's expected.
