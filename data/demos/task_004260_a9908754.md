Work in three independent stages and verify each before moving to the next. First, inspect the vendored package's build configuration (Makefile/CMake) for compiler flags that undermine floating-point determinism — flags like `-ffast-math` let the compiler reorder additions, which silently defeats a hand-rolled Kahan-summation compensation term even though the source code itself is correct; strip such flags, rebuild with `make clean && make`, and confirm two consecutive runs of the generator produce byte-identical output before trusting it. Second, pipe the executable's output into `dataset.csv` and compute the OLS slope analytically (e.g. via covariance/variance, not a canned regression call that might differ numerically), then implement the bootstrap yourself: seed with `np.random.seed(42)`, draw exactly 2000 resamples with `np.random.choice` on indices (with replacement, same size as data), recompute the slope each time, and take the 2.5/97.5 percentiles of the resulting distribution. Third, stand up a minimal HTTP server (e.g. `http.server` subclass or a lightweight framework) bound to 127.0.0.1:8000 exposing GET /metrics with the three required JSON keys, and background it (nohup/&) so it stays alive for querying.
