First establish the crash boundary: inspect `/home/user/logs/build.log` and use a tool like `tshark`/`scapy` (or `tcpdump -r ... -A`) to read `/home/user/logs/traffic.pcap`, following the TCP stream to find the highest TXN_ID whose response was actually completed/acknowledged by the server before the connection was abruptly closed (FIN/RST) — the next TXN_ID sent after that point was never acknowledged and must be excluded. Then write a small Python script that reads `/home/user/data/cache.wal` line by line with a regex matching `[TIMESTAMP] [TXN_ID] SET <key> <value>`, skips any malformed/truncated trailing line, applies only entries with TXN_ID <= the acknowledged boundary you found, and writes the resulting `{key: value}` dict as JSON to `/home/user/data/recovered.json`. For the leak, open `/app/async-build-cache-1.0.0/async_build_cache/server.py` and find the `except asyncio.CancelledError:` block inside the client-handling loop — a bare `pass` there lets the loop spin forever instead of exiting, leaking the task and its socket/fd; fix it by adding `break`/`return` (or re-raising) so the coroutine actually terminates and resources are released on disconnect. Reinstall with `pip install -e`, restart the server, then run `verify_throughput.py` to confirm throughput clears the 800 req/sec bar without fd exhaustion.