First recover the formula: run `tesseract /app/step_rule.png stdout` (install via apt if missing) and read the resulting text carefully — it defines `h` as a simple arithmetic expression in `beta` and `gamma`. Verify the OCR output looks sane (no garbled characters) before hardcoding it into your script; if OCR is noisy, try preprocessing (e.g. grayscale/threshold with `convert` or PIL) and re-run. Then build `/home/user/sim_mcmc.py` to parse stdin exactly as specified: N, E, E edge lines, a `---` separator, then beta/gamma pairs. Build an adjacency list once. For each proposal, compute `h` from the recovered formula (constant across the 50 steps for that proposal), then simulate with synchronous (Jacobi-style) updates: compute all three deltas (dS, dI, dR) per node from the *current* state before writing any new state — do not mutate S/I/R in place mid-step, since that would let later nodes see partially-updated neighbors. Check for any negative S/I/R after each step and reject immediately if found. After 50 successful steps sum I across nodes and apply the `0.5 < I_total < 5.0` acceptance window. Print `1`/`0` per proposal, one per line, using plain floats (no numpy) to match expected numeric behavior exactly.