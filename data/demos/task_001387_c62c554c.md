Recover the spec first: run `tesseract /app/spec.png -` (try different `--psm` values if the first pass is garbled) and read the extracted text closely — it will describe a header string/encoding, a fixed chunking size, a length-prefix format (width, byte order, signed/unsigned), and a per-byte transformation with some key. Implement archiver.py exactly as specified: write the header once, then loop over the input in fixed-size chunks, writing each chunk's length prefix before the (transformed) chunk bytes — get the encoding, byte order, and prefix width exactly right, since off-by-one or wrong-endianness bugs will only show up on specific input lengths. Since correctness is judged by bit-for-bit equivalence to a reference implementation across many random inputs, self-test by generating your own varied byte strings (empty, exactly one chunk, chunk boundary +/-1 byte, multi-chunk) and inspecting the hex of your output to confirm no debug text leaks into stdout and the structure matches the OCR'd spec precisely.
