Build the pieces separately and verify each in isolation before assuming they compose. For `monitor.cpp`: open the input file, read line by line, and for each line containing the exact substring `REJECT key_auth`, append to the output file the fixed email template with a blank line before the body — get the current local time with `std::time`/`localtime` and format it with `strftime` using `%Y-%m-%d %H:%M:%S` exactly, since a regex will check this precisely; test by compiling manually and feeding a small mixed sample log to confirm only matching lines produce output and the format is byte-exact. For `deploy.sh`, make every step idempotent: `mkdir -p` for the directory, `chmod 700` unconditionally (not conditionally created) so re-running always converges to the same state, and `g++ -o monitor_bin monitor.cpp` (overwriting the binary is naturally idempotent). Write the systemd user unit with `[Service]`, `ExecStart=` pointing at the compiled binary with the two log paths as arguments, `Restart=always`, `RestartSec=3`, and `Environment="TZ=Asia/Tokyo"` — you do not need to start it, just get the file contents exactly right. Run deploy.sh twice yourself to confirm it exits 0 both times, and test the timezone behavior by exporting `TZ=Asia/Tokyo` and running the binary directly against a scratch log.