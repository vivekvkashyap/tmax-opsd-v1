Implement this as a small, deterministic Python pipeline rather than approximating it. Load every JSON file in `/home/user/artifacts/`, keep only records where `status == "SUCCESS"`, and for each build the feature string by sorting the `hyperparameters` dict's keys and joining `f"{k}={v}"` pairs with single spaces — use the value's natural Python string form (don't reformat numbers) since the target string must be built with the identical rule. Fit a single `TfidfVectorizer(analyzer='char_wb', ngram_range=(2,3), lowercase=True)` on the list of successful feature strings (fit once, on the training corpus only), transform the normalized target hyperparameter string with that same fitted vectorizer, and compute cosine similarity (`sklearn.metrics.pairwise.cosine_similarity`) between the target vector and every experiment vector. Take the 3 highest-similarity experiment_ids, breaking ties consistently (e.g. stable sort keeps original file-iteration order), and write just their experiment_ids, one per line, in descending similarity order to `/home/user/top_experiments.txt`. Iterate the whole csv->file pass over `glob.glob` results in a deterministic (sorted) order so your run is reproducible, and never modify the source JSON files.