Split this into three independent pieces and build/test them separately before wiring together. (1) Video decode: use OpenCV (`cv2.VideoCapture`) or ffmpeg to pull all 30 frames in order, crop each frame's top-left 10x10 region, average the channels over that region, and classify green-vs-red by comparing mean G to mean R (remember OpenCV loads frames as BGR, so index channels carefully). Map frame index i to line i+1 of `versions.txt` and keep only lines whose frame was classified Success. (2) Semver caret-range matching: implement `^X.Y.Z` matching yourself (don't assume a library is installed) — parse major/minor/patch as integers, compute the upper bound per the caret spec (bump the left-most nonzero component and zero the rest), and filter/compare candidate versions numerically (tuple comparison), not lexicographically. (3) Networking: run the HTTP resolver (stdlib `http.server` or Flask) on 127.0.0.1:8080 returning 404 when no candidate survives filtering, and a separate raw `socket` TCP listener on 127.0.0.1:8081 that reads until it sees the literal bytes `STATUS\n` and replies with the count of Success frames found in step 1, then closes the connection. Launch both listeners as background processes so they persist after your turn ends, and verify with `curl`/`nc` before finishing.
