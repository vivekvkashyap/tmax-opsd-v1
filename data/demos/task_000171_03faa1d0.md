Build this as a straight-line ETL script, being careful about filter order and exact-match semantics. Read `/home/user/raw_data.txt` line by line, keep a line only if it contains the literal case-sensitive substring "AI" (use `in`, not a case-insensitive check) AND its whitespace-split word count is strictly greater than 5 — apply the word-count filter to the *original* line before any lowercasing/punctuation stripping, since punctuation removal could otherwise merge/split tokens and change counts. For the transform step, lowercase the surviving lines, strip only the punctuation characters listed in the task (`.`, `,`, `!`, `?`) rather than all non-alphanumerics (an overly aggressive regex could drop things you shouldn't), then `.split()` on whitespace. Write each result as its own JSON line `{"tokens": [...]}` to `/home/user/clean_data.jsonl`, preserving input order. For the benchmark, wrap only the filter+tokenize loop in `time.perf_counter()` calls (not file I/O or imports), compute `total_raw_lines / elapsed_seconds`, and format it with exactly two decimals to `/home/user/benchmark.txt` matching `Throughput: X.XX lines/sec`. Double check the regex/format string produces two decimal places even for whole numbers.
