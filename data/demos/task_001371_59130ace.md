First build a reproducible failure check: write a small script that repeatedly runs the variance computation against `/app/telemetry_signal.wav` (many iterations, since the bug is intermittent/a race condition) and fails if the result is non-deterministic or diverges from a stable value. Use this script as your `git bisect run` test between `v1.0.0` (good) and `HEAD` (bad) to let git binary-search the ~200 commits automatically rather than checking commits by hand; capture the identified commit's full 40-char SHA into `bad_commit.txt` once bisect converges. For the fix itself, look specifically for concurrent/parallel chunk-aggregation code — an unguarded shared accumulator written from multiple threads (or an unsafe narrowing cast, e.g. f64 to f32, done mid-aggregation) is the classic source of this kind of intermittent precision loss; replace the shared mutable state with a proper synchronization primitive (Mutex, atomic, or a fold/reduce pattern that combines per-thread partial results) and make sure NaN/corrupted frames are filtered out (e.g. via `is_nan()` checks) without poisoning the running sum. After fixing, compile, start the server on the exact host:port with the exact endpoint/header/payload contract described, and leave it running; hit it repeatedly yourself to confirm the variance value is now stable across repeated calls before finishing.
