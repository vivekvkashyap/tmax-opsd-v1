Since a Python reference implementation will diff your exact output files, implement the math literally and match its conventions precisely rather than approximating. In Rust, write a `median(&[f64])` helper that sorts a copy and, for even length, averages the two middle elements — reuse it for both the value median and the MAD (median of `|x - median|`). Split `train.csv` rows into 4 contiguous, non-shuffled chunks of equal size by index (fold i = rows `[i*chunk, (i+1)*chunk)`). For each fold as validation, fit median/MAD on the concatenation of the other 3 folds, then for each `k` in `[1.0,1.5,2.0,2.5,3.0]` classify validation rows as anomalous when `|value-median| > k*MAD`, and compute F1 against the `is_anomaly` labels (0.0 if precision+recall is 0). Write every `(k, fold, f1)` row to `cv_results.csv` with 4-decimal formatting as you go. After all folds, average F1 per k, pick the max (ties → smaller k), refit median/MAD on the full `train.csv`, apply to `test.csv`, and write only non-anomalous rows to `cleaned_test.csv`. Use `cargo init`, add `csv`/`serde` via `cargo add`, and run the binary to actually produce both output files.
