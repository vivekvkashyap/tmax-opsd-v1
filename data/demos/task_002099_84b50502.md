This is a multi-step forensics chain — each artifact feeds the next, so work in order. First extract frames from `/app/c2_session.mp4` (e.g. `ffmpeg -i c2_session.mp4 frame_%d.png`, or step through with `-vf select`) and OCR each frame with `tesseract` looking for a shell `export` line that sets an environment variable holding the encryption key; don't assume the frame index, scan several frames since the video is short. Second, reverse-engineer `/app/c2_crypto.pyc` — since it's Python 3.10 bytecode, use a decompiler (`uncompyle6`/`decompyle3`/`pycdc`, or manual `dis` module inspection of the bytecode ops if a decompiler isn't available) to recover the exact `encrypt`/`decrypt` source, then port that logic verbatim into your own script rather than trying to import the pyc directly (avoids Python version mismatch issues). Validate your ported implementation by round-tripping a test string through your `encrypt`/`decrypt` and confirming it matches, before relying on it to decode real payloads. Third, write `filter.py` per the exact CLI contract: decrypt the input bytes with the recovered key and algorithm, check the decrypted text for the literal strings `EXFILTRATE` or `C2_EXEC`, and on match exit(1) without creating the output file at all (not even empty) — on no match, copy the original *encrypted* bytes verbatim to the output path and exit(0). Test against a few payloads you construct yourself with the recovered key before trusting the hidden corpus.