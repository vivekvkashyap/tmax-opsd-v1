Recover the three parameters from the image with OCR (e.g. `tesseract /app/arch.png`) rather than assuming any values — read the actual Hostname, Port, and RejectionCode strings from the OCR output and use them verbatim. Write the Rust TCP server (`src/main.rs`) to bind `127.0.0.1:<extracted port>`, and on each accepted connection immediately write the extracted rejection code plus a newline, then close/drop the stream — since the throughput requirement is 100+ req/s with many short-lived connections, use a threaded or async accept loop (e.g. `TcpListener::incoming()` spawning a thread per connection, or a lightweight async runtime) rather than handling connections strictly one at a time with blocking work in between. For the supervisor, write a bash loop that runs the compiled binary, waits on it, and immediately restarts it in a `while true; do ./gateway_simulator; sleep <1s>; done`-style pattern so a crash triggers a restart within a second, and launch this in the background (`&`/`nohup`) so it persists. Populate `migration_summary.txt` with the exact `HOST=`, `PORT=`, `CODE=` lines using the OCR'd values, and load-test your own server locally before finishing to confirm it sustains the required rate.
