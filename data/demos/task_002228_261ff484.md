Work through the three artifacts independently. First, open `/home/user/launcher.sh` and just reorder the two function calls under the `# START ORDER` comment so `start_proxy` runs before `start_capacity_tool` — do not touch the function bodies. Second, write `/home/user/proxy.conf` as a standard Nginx config: top-level `daemon off;`, `pid /home/user/nginx.pid;`, `error_log /home/user/error.log;`, an empty `events {}` block, and an `http { server { listen 8080; location / { proxy_pass http://127.0.0.1:9090; } } }` block — get the brace nesting right since it's statically checked. Third, inspect `/home/user/capacity_cli` to see its exact `read -p` prompt strings, then write an Expect script that spawns it and uses `expect` blocks matching each prompt verbatim before `send`-ing the required value followed by `\r`; log all output (e.g. via `log_file` or by capturing `$expect_out(buffer)`/tee) into `/home/user/capacity_report.txt`, and finish with `expect eof` so the script doesn't exit before the tool completes. Test each script by actually running it, not just by inspection.
