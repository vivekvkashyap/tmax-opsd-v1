Build this as a multi-stage TCP daemon in C++, and structure it so each stage is independently testable before wiring them together. Use a standard blocking `socket()`/`bind()`/`listen()`/`accept()` loop on `127.0.0.1:9090` (accept in a loop so the server survives multiple connections — don't exit after the first client). On receiving `RESTORE <version>\n`, shell out via `system()` or `popen()` to run `curl -s -o /home/user/staging.tar.gz http://127.0.0.1:8080/backups/<version>.tar.gz` then `tar -xzf ... -C /home/user/restore_deploy/` (creating that directory first with `mkdir -p` if missing) — check each subprocess's return code and bail out (don't send SUCCESS) on failure. Set exact permissions with `chmod()` calls (0750 on app.bin, 0600 on config.json), not just relying on the archive's stored permissions. For the SMTP notification, open a second raw TCP socket to `127.0.0.1:2525` and manually write the SMTP dialogue lines (HELO/EHLO, MAIL FROM, RCPT TO, DATA, headers including Subject, body, terminating `.`) reading server responses between steps rather than blindly firing all commands at once. Only after all of download/extract/chmod/SMTP succeed should you write `SUCCESS\n` back to the client and close just that connection (keep the listening socket open). Compile with the exact `g++` command given, then launch it detached (`&` plus `disown`, or `nohup ... &`) so it keeps running after your shell command returns.