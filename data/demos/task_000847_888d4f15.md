Reverse-engineer the crash condition empirically rather than guessing: feed /app/bin/log_ingest varied single-line inputs (via echo/printf piped in) and observe exit status ($?) or signal (segfault = 139) to confirm the two failure modes — malformed `\u` escapes (not exactly 4 hex chars following) and raw unescaped control bytes (0x00-0x1F) inside a JSON string. Use tools like `strings`/`objdump`/`gdb` on the binary only to corroborate, not as your primary method — empirical probing with small crafted inputs is faster and more reliable. Build filter_stream.sh as a streaming `while IFS= read -r line` loop (or awk) so it never buffers the whole input: for each line, apply a regex check for `\u` not followed by exactly 4 valid hex digits and a check for literal control characters in the payload (e.g. using `grep -P` or awk's `gsub`/byte inspection) — drop lines matching either. For lines that pass, extract the `[...]` timestamp prefix, detect its format (slash vs dash separators), and convert with `date -d "<parsed>" +%s`, then reassemble `[epoch] {json}` leaving the JSON untouched. Test against /app/data/sample.log and verify no crashes occur when piping your output into the real binary.
