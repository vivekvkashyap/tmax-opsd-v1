Locate the `step_update` function and check the exponent term: an adaptive-step RK integrator must shrink `h` when `error` exceeds `tol` and grow it otherwise, so the ratio inside `pow(..., 0.2)` must be `tol/error`, not `error/tol` — swapping them makes the step grow on large error, causing overflow/divergence. Fix only that ratio, keep the 0.9 safety factor and the 0.2 exponent unchanged. Compile with `gcc -O2 -lm -o /home/user/integrator /home/user/integrator.c` (the `-lm` must come after the source file on some toolchains, or use `-lm` at the end of the link line). Run the executable to produce `/home/user/output.bin`, then read it as a flat sequence of little-endian IEEE754 doubles in (x,y) pairs — seek to the last 16 bytes (or read all pairs and take the last one) rather than assuming a fixed record count. Compute `sqrt(x*x+y*y)` on that final pair and write it to `/home/user/final_distance.txt` using a format string with exactly 4 decimal places (e.g. Python `f"{d:.4f}"` or C `printf("%.4f", d)`), with no extra whitespace or trailing newline mismatches if the checker does exact string comparison.