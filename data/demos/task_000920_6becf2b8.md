Break this into audio extraction, data transform, storage, serving, and scheduling, and validate each stage before moving to the next. First, transcribe `/app/field_notes.wav` (e.g. with `ffmpeg` to convert/inspect it and a speech-to-text tool available in your environment, or by listening to characteristics of the audio if you have transcription tooling) to recover the exact spoken passphrase and the calibration multiplier as a number — do not guess these values, derive them from the audio content itself. Second, load the CSV with pandas, use `.interpolate(method='linear')` on the `reading` column (make sure timestamp is sorted/used as the index if needed for correct interpolation), multiply by the recovered multiplier, then add `rolling_avg` via `.rolling(window=3, min_periods=1).mean()` on the calibrated column. Third, load this into SQLite via `to_sql('processed_data', conn)`. Fourth, build the HTTP service (Flask/FastAPI or stdlib `http.server`) binding `127.0.0.1:8000`, implementing `GET /data?timestamp=<ts>` that queries the DB and returns the three required JSON fields, and gate it behind an `Authorization: Bearer <token>` check comparing against the passphrase you extracted — return 401 otherwise. Run it detached (e.g., `nohup ... &` or a background process) so it stays up. Finally, write `export_db.sh` doing `sqlite3 telemetry.db .dump > backup.sql`, chmod it executable, and add a `*/15 * * * *` line to the user's crontab via `crontab -l | { cat; echo "..."; } | crontab -`.