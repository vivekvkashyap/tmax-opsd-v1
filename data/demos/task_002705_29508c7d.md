Start by transcribing `/app/lab_notes.wav` yourself (use available speech-to-text tooling, or listen carefully and transcribe manually) to recover the four parameters spoken in it — the target gene name, primer length, target Tm, and Monte Carlo iteration count — do not guess these; get them from the audio. Then in `/home/user/pipeline.sh`, extract the named gene's sequence from `/app/genome.fasta` (match the FASTA header, concatenate the sequence lines), slide a window of the recovered length across both the forward strand and its reverse complement to enumerate all candidate primers, and for each compute Tm via pure bash arithmetic using the Marmur-Doty formula `2*(count_A+count_T) + 4*(count_G+count_C)` (count bases with `grep -o` or parameter expansion tricks), keeping only exact matches to the recovered target Tm. For every valid forward/reverse pair, invoke `/app/simulate_pcr.ipynb` headlessly (via `jupyter nbconvert --execute` with papermill-style parameter injection, or `papermill` directly) passing the primer sequences and iteration count, and parse the resulting efficiency score from its output. Track the best-scoring pair across all invocations and write only that pair, comma-separated forward,reverse, to `/home/user/best_primers.txt`. Since the grader recomputes efficiency independently and requires it above a strict threshold, make sure your Tm filtering and GC-clamp-friendly primer selection are both correct rather than picking an arbitrary valid-Tm pair.
