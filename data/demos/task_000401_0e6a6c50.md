Start by reverse-engineering the schema directly: run `sqlite3 /home/user/tracing.db ".schema"` and inspect a few rows of each table to understand how services and their weighted directed edges (calls, with latency) are represented before writing any Go code. Implement the critical path search as a genuine recursive CTE in SQL (not by pulling all rows into Go and doing the graph search in application code) — something like a `WITH RECURSIVE` that walks from the starting service through `calls` joined to `services`, accumulating a running total latency and a path string/array at each step, then in the outer query picks the row with the maximum accumulated latency among terminal (or all) paths. Use the Go `database/sql` package with a placeholder (`?`) for the starting service name — never format the service name directly into the SQL string, since the task explicitly requires a parameterized query. After running the query, marshal the winning path and its total latency into the exact JSON structure requested. Initialize the Go module and fetch the sqlite3 driver before running, and confirm your CTE actually finds the maximum-latency path by tracing it manually against the schema you discovered, not just the single path that happens to appear first.
