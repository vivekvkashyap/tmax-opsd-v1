Do this entirely with awk/grep/sed, no Python. Approach: (1) for each CSV in `/home/user/experiments/`, extract the `run_id` column (field 1, skipping the header line) and validate each value is a strict integer with something like `awk -F, 'NR>1{if ($1 !~ /^[0-9]+$/) exit 1}'` — note `105.0` fails this regex (contains a dot) and `NaN` fails too, so anchoring to `^[0-9]+$` (optionally allow a leading `-` if negatives are possible, but here none needed) correctly rejects both; track exit status per file to decide inclusion, don't just grep for "NaN" since floats like `105.0` also need catching. (2) For files that pass, use `awk -F, 'NR>1{print $1, ($3+$4+$5)/3}'` to get per-row mean CV, and track the running max across all valid files' rows (compare in awk or accumulate into a variable across multiple awk calls). (3) For the winning run_id, pull `w_1,w_2,w_3` and compute L2 norm with `awk` or `bc -l` using `sqrt(w1^2+w2^2+w3^2)` — bc's `sqrt` needs `scale=` set for precision, or do the square/sum in awk and pipe to `bc -l` for sqrt. (4) Format both values to exactly 4 decimals with `printf "%.4f"` in awk, and write the final `run_id,mean_cv,l2_norm` line to `/home/user/best_model_stats.txt` with no header and no trailing extra content.