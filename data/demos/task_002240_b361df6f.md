Before touching code, reproduce the crash: `make` the tool, run it under `gdb --args ./recover system_state.wal` (or build with `-fsanitize=address`) to get an exact fault location. The crash will point into the code that turns a raw byte buffer read from the file into a `std::string`. Inspect how key/value bytes are read: the WAL format stores an explicit length prefix followed by raw (non-null-terminated) bytes, but the code constructs the string using the `std::string(const char*)` constructor, which scans for a `\0` terminator that may not exist within the allocated buffer, causing an out-of-bounds read that is intermittent depending on adjacent heap contents. Fix it by using the length-aware constructor `std::string(buf, len)` for both the key and value, using the length fields already parsed from the WAL, instead of relying on null-termination. Do not touch the output formatting (`KEY=VALUE` printing). After fixing, rebuild with `make`, rerun the tool against `system_state.wal`, and redirect stdout to `recovered_metrics.txt`, verifying every record in the WAL (including any very long value) is emitted correctly and the process no longer segfaults.
