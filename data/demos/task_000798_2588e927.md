Work phase by phase, verifying each artifact before the next. First transcribe `/app/audit_interview.wav` (e.g. `openai-whisper`) to recover the stated password prefix — listen for the exact word given, don't guess. Since the password is that prefix plus an unknown 3-digit PIN, brute-force the missing digits (000-999) against `/app/evidence.zip` (Python `zipfile`/`pyzipper` trying each candidate). Once extracted, parse `access.log` for `/login?next=` requests with a 302 response whose `next` value's host is `evil.com` (parse the URL properly rather than substring-matching the whole line, to avoid false positives), collect unique IPs, sort ascending, one per line. Finally, run `oracle_redirect` interactively with your own battery of inputs (absolute URLs, `//host` protocol-relative, backslash variants, paths with a `token=` param, paths with other params) to reverse-engineer its sanitization rules purely from observed input/output pairs, then reimplement that exact logic in `safe_redirect.py` with `urllib.parse` — match edge cases precisely (does it strip only `token`? keep other params? preserve fragments?) since the check is byte-exact output equivalence under heavy fuzzing.
