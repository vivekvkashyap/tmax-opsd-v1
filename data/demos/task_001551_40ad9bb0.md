Split the C++ program into three clear stages so each is independently checkable: a CSV reader (skip header, parse id/a/b/c), a Newton-Raphson solver, and a stats/output stage. For the solver, implement `f(x)=a*exp(x)+b*x-c` and `f'(x)=a*exp(x)+b`, iterate `x -= f(x)/f'(x)` from x0=0.0, and stop on `fabs(f(x)) < 1e-6` or 100 iterations — use `double` throughout to match expected precision, and hand-verify your solver against one row by computing a couple of Newton iterations by hand to confirm convergence direction before trusting the loop. For the metrics, sort both the computed-x array and the ref_x array ascending, then average the elementwise absolute differences for Wasserstein (only valid because both arrays are the same length N here); compute Welch's t-statistic using unbiased sample variance (divide by N-1) for both groups. Write processed.csv with id,computed_x at 6 decimals, and report.txt with the two lines in the exact literal format `Wasserstein Distance: <value>` / `T-Statistic: <value>` rounded to 6 decimals. Create the Makefile with a `process_data` target using `g++ -O3 -std=c++17`, then build and run it against the four CLI arguments to actually produce both output files.