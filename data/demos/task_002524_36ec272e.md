Treat the stripped binary as a black box: run `/app/qemu_probe` (and `--help` or with no args) to read its usage/error output, since a well-behaved CLI prints a usage message on bad args — this reveals the required flag name and argument count without needing to disassemble it. Set the env var and permissions before invoking it: `chmod 400` the state file, and confirm the exact required mode by re-reading the tool's stderr if it complains. Build the Python service with the standard library: use `ssl.SSLContext` wrapping `http.server`/`socketserver` for the HTTPS side on 8443 (generate the cert/key first with `openssl req -x509 -newkey rsa ... -nodes`), and a separate `socket.socket` TCP listener on 9090 (run each in its own thread, or use two separate background processes). For `/health`, shell out via `subprocess.run` with the env var injected and capture stdout exactly, forwarding it verbatim as the response body. For quota checking, recursively sum `os.path.getsize` over all files under the monitor directory (not just top-level) so files added after your script starts are picked up on each new connection. Launch the script with `nohup ... &` or equivalent so it survives your shell exiting, and verify both ports are listening with `curl -k` and a raw `nc`/Python socket test before finishing.
