This is two independent subtasks — tackle them separately and verify each in isolation before combining. Part 1 (infra): create `/home/user/metrics_data/raw` and `chmod 700` both it and its parent explicitly (a single recursive chmod on the parent after creating subdirs is the reliable way — verify with `stat -c %a` on each level, since some `mkdir -p` + chmod orderings leave the wrong mode). Symlink `/home/user/active_metrics -> /home/user/metrics_data/raw` with `ln -s`. Write `/home/user/nginx.conf` with an `upstream` block listing `127.0.0.1:5001` and `127.0.0.1:5002` each with `max_fails=3 fail_timeout=10s`, and a `server` on port 8080 proxying `/ingest` to that upstream. Export `METRICS_DIR=/home/user/active_metrics` before launching `/app/start_services.sh`, then start nginx with `nginx -c /home/user/nginx.conf`; confirm both are actually listening (`curl`/`ss`) before moving on. Part 2 (equivalence rewrite): do NOT try to read or disassemble `/app/oracle_planner` — instead implement `/home/user/planner.py` purely from the written spec (drop negative values, group by service+metric, floor the average, format max to 2 decimals, sort output alphabetically by service then metric), then self-test by piping sample multi-line input through both your script and the oracle and diffing outputs to catch formatting mismatches. Make the script executable with the correct shebang.
