First reproduce the leak deterministically: `make` in `/home/user/service`, then run `valgrind --leak-check=full ./tx_service` and read its report — it will point to the allocation site in `tx_proc.c` and show which malloc'd block was never freed. Read the function that decodes hex payloads end-to-end and look specifically for early `return` statements that occur after the buffer is allocated but before the normal free path — that's the bug, not anywhere else. Before editing, `cp tx_proc.c tx_proc.c.orig` so you can produce `diff -u tx_proc.c.orig tx_proc.c > /home/user/fix.patch` afterward. Fix it by freeing the decoded buffer on that early-exit branch too (be careful not to free it twice or use it after freeing — free exactly once, right before the early return). Rebuild and re-run under valgrind to confirm zero leaked bytes. To find the specific leaked transaction, either watch valgrind's leak trace/backtrace for the allocation, or run under `gdb`/add temporary debug prints to see which decoded buffer failed validation before you patch it — then write that exact decoded (not hex-encoded) string, which starts with `ERR_TX_`, to `/home/user/leaked_tx.txt`.
