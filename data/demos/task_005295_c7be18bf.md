Do not guess the schema/encoding/ports — actually OCR the image with `tesseract /app/system_diagram.png stdout` and parse its text output for the schema definition, the character-encoding name, and the two port numbers, since these are the task's real given values hidden in the picture. Build the SQLite table exactly matching what you extracted (column names/types) and insert the one specified user row. Compile `legacy_hash.c` with `gcc -shared -fPIC -o legacy_hash.so legacy_hash.c` and load it via ctypes, setting `argtypes`/`restype` for `generate_token(int)->int`. Write the Python server so the HTTP handler builds the `id,name,role` string and encodes it with `.encode(<extracted_encoding>)` (not utf-8) before writing raw bytes to the socket — this is the main correctness trap since a non-ASCII character will silently mis-encode under the wrong codec. For the TCP service, read until you see the literal `TOKEN <id>\n`, call the loaded C function, and write back `<int>\n`. Launch both listeners as background threads/processes so the server stays up after your task completes.
