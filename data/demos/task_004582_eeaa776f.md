Reverse-engineer the WAL format empirically before writing any Rust: run `/app/legacy_tracker` on the small `/home/user/sample_wals/` sample, inspect the outputs, and hex-dump (`xxd`) the raw `.wal` files to work out the binary record layout (timestamp, an op-type byte for create/update/delete, a path, and a payload) — don't guess the layout from the task description alone, confirm it against actual bytes. Pay special attention to any file extension mentioned as needing special decoding (e.g. an XOR or encoding step) during extraction: compare a sample file's raw bytes to legacy_tracker's decoded output byte-by-byte to recover the exact transform. Once the format is confirmed, implement the Rust tool to: parse all WAL files in a directory, collect every record with its timestamp, sort all records globally by timestamp (across files, not per-file), replay them in order into a HashMap keyed by path (later ops overwrite/delete earlier ones), decode special-extension payloads accordingly, and write the final map to the output directory, stripping any modifier suffix from filenames as legacy_tracker does. Validate correctness first with `diff -r` against legacy_tracker's own output on the sample set before ever touching the large dataset — do not optimize before you're byte-exact. Avoid needless I/O syscalls per record and unnecessary sleeps/locks in your Rust version to get the speedup.
