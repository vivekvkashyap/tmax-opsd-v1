Work the four sub-tasks in order and verify each with curl/exit codes before moving on. For the 502: run `/app/start.sh`, then check what the Flask app actually binds to (a UNIX socket) versus what `/home/user/nginx.conf`'s `proxy_pass http://unix:...;` line points at — fix the mismatched socket path and reload nginx (`nginx -s reload` or restart), then confirm `curl http://localhost:8080/health` returns 200. For the firewall rule, add a `deny <ip>;` directive inside the relevant `server { }` block (not just `location`), and reload again — test with a spoofed `X-Forwarded-For` or a real deny check per how the proxy is configured. For the wrapper script, keep it a thin `env`-style shim: export `TZ` and `LC_ALL`, then `exec python3 /home/user/cost_filter.py "$@"` so arguments pass through cleanly. For the admission controller, parse the JSON's `resources` list and reject (exit 1) if any item is `type == "compute"` and `size` starts with `x` or `p`, or if `monthly_cost > 500`; otherwise exit 0. Test your script directly against every file in `/app/corpus/clean/` and `/app/corpus/evil/` to confirm the exit codes match before declaring done — don't just eyeball the rules.
