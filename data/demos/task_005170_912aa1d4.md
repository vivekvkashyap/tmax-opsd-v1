Tackle this in order: fix the vendored library first, then build the streaming/resampling pipeline, then the API on top. For the `parse` library, don't rewrite the matching logic — locate the specific method where a `.search(...)` call was likely swapped for `.match(...)` (or similar substring-vs-anchored-match confusion) in `parse.py`, since that's the classic single-line perturbation that breaks substring extraction while looking almost correct; fix that one line, then `pip install -e /app/vendored/parse` and confirm `parse.search(pattern, line)` extracts fields from a sample log line before writing the pipeline. For processing, stream the log file line-by-line (never `.read()` the whole file), parse each line with the fixed library, group readings by sensor and by minute (truncate/floor the timestamp), average duplicates within a minute, then build a complete per-minute time index per sensor and fill gaps with linear interpolation (e.g. pandas `resample('1min').mean().interpolate(method='linear')`) rather than forward-fill, since the spec requires true linear interpolation. Then for each hour pick exactly the 0/12/24/36/48-minute samples. Finally wrap this in a background-served HTTP endpoint that checks the exact bearer token header before returning results, and confirm the process is actually listening (not just started) before leaving it running.