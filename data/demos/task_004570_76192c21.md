Work the two parts independently. For the stack: fix `nginx.conf` first (add a `location /api/ { proxy_pass http://127.0.0.1:5000; }` block inside the port-8080 `server` block), then fix `api/.env` (set `REDIS_URL=redis://127.0.0.1:6379/0` so the Flask app's Redis client actually connects). Start Redis, then the Flask API, then Nginx, in that order, backgrounding each with `&` and checking each is actually listening (`curl`/`ss`) before moving to the next — a common failure is starting Nginx before Flask is up, so proxied requests fail even though the config is right. Then open the SSH tunnel with `ssh -N -L 9000:127.0.0.1:8080 user@localhost &` and verify end-to-end with `curl http://127.0.0.1:9000/api/health`, iterating on whichever hop breaks.

For the log filter, don't hand-write a permissive regex — instead derive the pattern empirically: diff the structure of `/home/user/corpus/clean/*` against `/home/user/corpus/evil/*` to identify what legitimate lines always share (method GET, path prefix `/api/`, status 200, known monitoring user agents) and use a strict allowlist match (grep/awk) rather than trying to blacklist every attack signature — a denylist will miss unseen payload variants in the hidden test corpus. Validate your script against both sample directories before finishing, checking it reproduces every clean line verbatim and drops every evil line, then `chmod +x /home/user/sanitize_logs.sh`.
