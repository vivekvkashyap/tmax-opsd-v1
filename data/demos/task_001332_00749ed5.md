Don't touch the analysis script until the integrator bug is fixed and reinstalled — the fix location and correction are explicitly given in the task: find the adaptive step-size update in `pydynsim/integrator.py` and change it so the new step size scales by `(tolerance / error)**0.5` instead of the inverted `(error / tolerance)**0.5`, since scaling up the step when error is already high is what drives the integrator to diverge/NaN. After editing, reinstall the package into the active Python environment (e.g. `pip install -e /app/vendored/pydynsim-1.2.0` or equivalent) so `import pydynsim` picks up the change — verify by running a quick sanity simulation and confirming no NaNs appear in the returned array before writing the full analysis script. In `analyze.py`, call `pydynsim.simulate(filepath, steps=5000)`, index into the returned `(steps, num_atoms, 3)` array as `[:, 4, 2]` to get atom index 4's Z-coordinate series (atom ID 5 is 0-indexed as 4), run an FFT (`numpy.fft.rfft` or similar) on that series, build the frequency axis using `dt=0.01` ps exactly as instructed, and pick the frequency bin with maximum power spectral density (exclude the DC/zero-frequency bin, which is usually the largest and not meaningful here). Write only the resulting float to `/home/user/frequency.txt`.