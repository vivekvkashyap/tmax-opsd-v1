Start with the pcap: run `tcpdump -r /home/user/ticket/crash.pcap -A` (or `-X`) to dump packet payloads in ASCII/hex and read out the exact bytes of the METRIC line that was sent right before the crash — look for a non-numeric token among the metric values, since that's what breaks arithmetic downstream. Then read `/app/bash-metrics-daemon-1.0/server.sh` end to end: note it has `set -e` at the top, so any command that returns non-zero (like `bc` failing to parse a non-numeric operand) kills the whole client-handling function/connection instead of just that request — replace the raw `bc` calls with checked calls (e.g. validate each value is numeric with a regex before doing arithmetic, or check `bc`'s exit status) and on failure `echo "ERROR: BAD INPUT"` and continue the read loop rather than exiting; also consider disabling `set -e` for that section or handling it in a subshell so one bad line doesn't tear down the fork'd connection handler. For precision, pass `scale=4` into the `bc` expression (e.g. `echo "scale=4; $sum / $count" | bc`) so results are formatted to exactly 4 decimals, padding with trailing zeros if `bc` trims them. Finally launch the patched script with `socat`/nohup so it persists in the background listening on 127.0.0.1:9090, and manually test with `nc` sending the AUTH line, a normal METRIC line, and a malformed METRIC line to confirm behavior before finishing.