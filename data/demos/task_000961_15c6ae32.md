Get the stack running first: edit `/app/nginx/nginx.conf` so requests are proxied with `proxy_pass http://127.0.0.1:5000;` for the API location block, then run `/app/start_services.sh` and confirm with `curl` that port 8080 reaches Flask before writing any scanner code. In `scanner.cpp`, use libcurl to hit each endpoint through the proxy; for `/api/v1/auth`, capture the leaked hash from the response body. Crack it by reading `/app/dict.txt` line by line, computing SHA-256 of each candidate with libcrypto's EVP API (or the older `SHA256_*` functions), hex-encoding the digest, and comparing against the leaked hash — stop at the first match, that candidate is the cleartext token (don't assume or hardcode it, the dictionary is how you're meant to recover it). Send that token as `Authorization: Bearer <token>` to `/api/v1/data` to retrieve the flag. Write both recovered values plus a vulnerability score into `/home/user/scan_report.json` under the exact keys the grader expects (`token`, `flag`, plus a score field), compile with the exact flags given, and run the scanner to confirm the JSON is produced before finishing.
