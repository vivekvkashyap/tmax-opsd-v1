Reverse-engineer the rules empirically rather than assuming — run `strings /app/legacy_filter` to look for hints (error messages, keyword literals like directory names or reserved words), and also craft small test `.proto` files varying one dimension at a time (import paths, nesting depth, service names) and run them through `/app/legacy_filter` to observe pass/fail (exit code) for each, binary-searching the exact thresholds (e.g. is 2 levels of nesting allowed but 3 rejected, or vice versa?). Do this systematically for all three rule categories before writing any bash.

Then implement `/home/user/proto_filter.sh` taking a file path arg and checking each rule with plain text tools: grep for `import` lines and reject if the path contains `../` or is absolute; count nested `message { ... }` depth by tracking brace balance alongside `message` keyword occurrences (a stack-counting approach, e.g. incrementing a depth counter on each `message` opening brace and decrementing on its matching close) rather than naive line counting, since messages can have non-message braces inside; grep for `service` declarations and reject specific disallowed names. Exit 0 only if all checks pass, non-zero otherwise. Validate against several hand-built clean/evil cases spanning edge cases (deep nesting, tricky import paths) before finalizing.
