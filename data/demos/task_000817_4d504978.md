This is a fix-then-build-then-use task, in that order. First inspect `/app/sec-audit-lib-1.0/Makefile`: its link step is missing the OpenSSL flags needed for HMAC/crypto calls, so add `-lcrypto -lssl` (order matters, put crypto libs after object files) to the linking command. Second, open `src/token_validator.cpp` and find `SecAudit::ValidateToken` — it's currently short-circuited to always `return true`, bypassing signature checking; replace that with a call into the library's real internal HMAC verification routine so it actually checks the token against the secret/expected signature instead of trusting everything. Rebuild to produce `libsecaudit.a`, confirming it links cleanly. Then write `audit_tool.cpp`: read `/home/user/targets.txt` line by line (Port,ConfigFilePath,ExpectedSHA256), compute each config file's SHA256 (via OpenSSL EVP or `sha256sum` subprocess) and compare to the expected hash for TAMPERED, else check file mode bits for world-writable (`o+w`) for VULNERABLE_PERMS, else open a raw TCP socket to the port, read the token line the service sends, and pass it through your now-fixed `ValidateToken` to classify VULNERABLE_TOKEN vs SECURE. Write results as `Port,Status` CSV rows to `/home/user/audit_results.csv`.
