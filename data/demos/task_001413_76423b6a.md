Set up the venv (`python3 -m venv /home/user/venv && source /home/user/venv/bin/activate && pip install numpy scipy Pillow pytesseract`) since `tesseract-ocr` is already on the system PATH for `pytesseract` to call. Extract lambda from the image with `pytesseract.image_to_string(Image.open('/app/reg_param.png'))` and parse the numeric value following "lambda"/"=" out of the returned text with a regex — don't hardcode a value, and validate it parses as a sane positive float. Load `/app/train_data.npy` and `/app/test_data.npy` with `np.load`. Compute the sample mean and covariance with `np.cov(X_train, rowvar=False, bias=True)` (population covariance, matching an MLE), form `Sigma_reg = Sigma_sample + lambda * I` and a diagonal `Sigma_diag = diag(var(X_train, axis=0))`. Use `scipy.stats.multivariate_normal(mean, cov).logpdf(X_train)` to get per-sample log-likelihoods, sum them for `log_L`, then apply the BIC formula with the two different `k` values given in the spec. Pick whichever model has lower BIC, and evaluate its NLL on `X_test` as `-multivariate_normal(mean, chosen_cov).logpdf(X_test).sum()`. Write all four required keys to `/home/user/results.json` via `evaluate_models.py`.
