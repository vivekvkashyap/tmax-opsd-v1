Split this into extraction, detection, and threshold-search stages, verifying each before moving to the next. Use `ffmpeg -i /app/experiment.mp4 /home/user/frames/frame_%04d.pgm` (grayscale PGM output) to dump frames. In extract_tracking.c, parse the PGM header (P5 magic, width, height, maxval) then scan the raw pixel buffer for the single highest-intensity pixel per frame, recording its (x,y); iterate frames in filename order and write frame_id,x,y rows to extracted_trajectory.csv, one row per frame with an integer id starting appropriately. For artifact_filter.c, read a CSV of frame,x,y triples, treat `-1,-1` rows as missing/skip them when computing velocity, and compute the Euclidean or per-axis pixel displacement between consecutive *valid* frames; classify EVIL if any consecutive displacement exceeds a velocity threshold (make the threshold a compile-time define or CLI arg so it's tunable) or reject-worthy missing-value patterns are present, else CLEAN. In pipeline.sh, loop the threshold candidate values from 5 to 30, for each one recompile (or invoke) artifact_filter against both corpora, and stop at the first (or best) threshold value that yields zero false EVILs on clean/ and zero false CLEANs on evil/ — then use that threshold for the actual classification run whose per-file `filename: CLEAN`/`filename: EVIL` output the script prints.
