Treat the log as the sole source of truth — do not guess header names or tokens. Parse `/home/user/waf.log` for the entry where `STATUS="200"` and `WAF="ALLOW"` alongside evidence of an `/etc/shadow` path-traversal from the `admin` user; that line's `HEADERS=` field names the custom header used to smuggle the payload, and the value's format (paired hex digits) tells you the encoding — confirm by decoding it yourself and checking it spells a `../` traversal to `etc/shadow`. Pull the admin session token from that same successful line's `COOKIE=` field, not an earlier blocked attempt's stale token. In `payload_gen.c`, hex-encode `argv[1]` byte-by-byte (two lowercase hex chars per byte, no separators) to match that scheme, then write the raw request with explicit `\r\n` after every header plus a trailing blank `\r\n`, using the discovered header name and token. Compile, run against the given target path, and hex-dump `/home/user/exploit.txt` to confirm CRLFs are literal and correctly placed.