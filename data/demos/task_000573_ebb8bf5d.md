First recover the ground-truth ordering: use ffmpeg to extract frames from /app/reference_video.mp4 (e.g. `ffmpeg -i reference_video.mp4 -vf fps=<rate> frames/frame_%03d.png`), then run tesseract OCR on each frame to read the flashing Event ID text; dedupe consecutive identical reads and assemble the sequence in the order frames appear — don't assume a pattern, actually OCR each frame since IDs are only known by reading them. Use this sequence (plus each ID's ancestor chain via parent_event_id) to decide which rows of /app/data/export.csv are valid; for duplicate event_ids, keep only the row with the max timestamp per id. Then implement /home/user/query_engine.go: parse the single JSON-array argument, build an in-memory map from event_id to its row (parent_event_id, timestamp), and for "ancestors" queries walk parent_event_id pointers up to the root (an event with no parent or a parent not in the dataset), collect ancestor IDs, sort them by timestamp per the "sort" field (asc/desc), then truncate to "limit". Be precise about whether the queried event_id itself is included as an "ancestor" and about tie-breaking on equal timestamps, since the reference implementation will be diffed against yours on thousands of randomized inputs — inspect the CSV data yourself to confirm exact semantics rather than assuming.