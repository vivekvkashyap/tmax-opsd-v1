Start the target (`/app/vuln_uploader &`) and reverse engineer it before writing any proxy code: run `strings /app/vuln_uploader | grep -i cookie` (and cross-check with `objdump -d` around the request-handling routine) to recover the exact hardcoded Cookie header value the `/download?file=...` endpoint requires, and separately confirm the path-traversal shape works by hand with `curl` (e.g. `curl -H "Cookie: <recovered>" "http://127.0.0.1:8081/download?file=../home/user/flag.txt"`) before relying on it from C++. Then build the proxy: use a small HTTP library (cpp-httplib works well for a quick embedded server) to listen on 127.0.0.1:9090 and handle `POST /exploit`. Order your checks exactly as specified — first reject with 403 if `X-Proxy-Auth` isn't exactly the required value, then validate the request body's PEM cert against /home/user/ca.pem (either via OpenSSL's `X509_verify`/`X509_STORE` APIs, or by writing the body to a temp file and shelling out to `openssl verify -CAfile /home/user/ca.pem <tmpfile>` and checking its exit code), returning 401 on failure. Only then issue the outbound GET with the recovered cookie to pull the flag, save it to /home/user/loot.txt with permissions set explicitly in code via `chmod(path, 0400)` (not umask), and return 200 with the flag body. Keep both processes running in the background after setup.
