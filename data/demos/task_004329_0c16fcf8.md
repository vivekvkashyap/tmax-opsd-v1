Build and test this bottom-up: first get the Expect script working standalone by running `expect /home/user/auto_restore.exp` directly and confirming it produces `restored_data.txt` in the staging directory, before wrapping it in Rust. In the Expect script, `spawn /home/user/legacy_restore`, then `expect "Enter restore password: "` followed by `send "backup2023\r"`, then `expect "Enter staging directory: "` followed by `send "/home/user/staging\r"`, and finish with `expect eof` so the script waits for the child to actually terminate rather than exiting prematurely. Only once that works reliably, write the Rust program using `std::process::Command::new("/usr/bin/expect").arg("/home/user/auto_restore.exp")` with `.status()` or `.output()` to wait for completion and check the exit status, then read `/home/user/staging/restored_data.txt` and write/copy its contents to `/home/user/prod/restored_data.txt` (creating the prod directory if needed). Compile with `rustc /home/user/deploy_restore.rs -o /home/user/deploy_restore` and run it, verifying the final file lands correctly before considering the task done.
