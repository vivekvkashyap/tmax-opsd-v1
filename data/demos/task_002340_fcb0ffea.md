Implement the C program in small, independently-testable pieces since this will be checked by fuzzing against a hidden oracle on random 4096-byte inputs — exact stdout match matters, so follow the spec literally rather than approximating. Read exactly 4096 bytes from stdin with `fread`. Compute the 64 block averages: for pixel at (x,y), block index is `(y/8)*8 + (x/8)` in row-major block order — accumulate per-block sums as you scan the image once, then divide each by 64 to get the float average, and also track a running integer `total_sum` of all raw byte values as you go (this becomes your LCG seed). Implement the LCG exactly: `seed = (seed * 1103515245 + 12345) & 0x7FFFFFFF` before each draw, then `idx = seed % 64` selects which block average to sample; do this 64 times per bootstrap sample, 100 samples total, tracking each sample's mean and the running min/max/sum. Print with `printf` using the exact given format string with `%.4f`. Compile with `gcc -O3` and test manually with a fixed input before wiring into the ffmpeg pipeline (`ffmpeg -ss 00:00:02 -i /app/raw_footage.mp4 -vframes 1 -vf scale=64:64 -pix_fmt gray -f rawvideo - | /home/user/process_frame > /home/user/frame2_stats.txt`).