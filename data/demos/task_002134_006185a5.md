First recover the threshold `D` from the video before writing any classifier logic. Extract frames from `/app/reference.mp4` with `ffmpeg -i /app/reference.mp4 out_%04d.png` (or process frame-by-frame in Python with `opencv`/`PIL` via `ffmpeg`-piped raw frames) and for each frame check whether every pixel equals pure white (255,255,255) — e.g. compute the frame's min pixel value, or check `frame.min() == 255`. Count how many frames satisfy this exactly (avoid near-white false positives from compression artifacts by checking equality, not a lenient threshold, and consider checking a few pixels or the average instead of one corner pixel) — that count is your `D`. Once you have `D`, write `classify.py` to: parse the edge-list JSON into an adjacency structure, run a cycle check (e.g. DFS with recursion-stack tracking, or Kahn's algorithm — if fewer nodes get zero in-degree than total nodes, there's a cycle) and mark cyclic graphs `evil` immediately; for acyclic graphs compute the longest path in node-count from any root to any leaf via topological-order DP, and classify as `evil` if that depth exceeds `D`, else `clean`. Write results to the CSV with the exact header, then test your script against `/app/corpus/clean/` and `/app/corpus/evil/` (if visible) to validate before considering it done.
