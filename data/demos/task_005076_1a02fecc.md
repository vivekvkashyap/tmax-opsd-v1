Split this into a reverse-engineering phase and an implementation phase. First, port the given JS CRC32 (the classic reflected polynomial 0xEDB88320 table-based algorithm) to Python exactly, and confirm it works standalone against a known CRC32 test vector. Second, treat `/app/telemetry_oracle` purely as a black-box: feed it several base64-encoded inputs of your choosing (including the empty string and single-byte inputs) and compare the printed 8-hex-char output against your plain CRC32 result for the same bytes — look for a simple, constant relationship between the two (a bitwise XOR with a fixed 32-bit mask is a common "proprietary mixing step" in these tasks); once you find a candidate mask, verify it holds across all your test inputs, not just one, before trusting it. Once your Python function matches the oracle on every probe, write `test_checksum.py` using `hypothesis.strategies.binary()` (not just `text()`, since the protocol takes arbitrary decoded bytes) with `@given` to assert equality against oracle calls (invoke the oracle as a subprocess per generated example) or against a derived formula you've already validated. Finally implement the `websockets` server on `127.0.0.1:8765/telemetry` that base64-decodes `data`, applies your checksum function, and replies with the specified JSON shape, then leave it running in the background.
