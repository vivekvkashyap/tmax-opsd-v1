Treat this as black-box reverse engineering, not decompilation. First run `strings /app/legacy_auditor` to find literal output templates (JSON keys, error strings) — these tell you the exact print format you must replicate byte-for-byte. Then use `ltrace`/`strace` on several crafted inputs to see which libc calls (base64 decode, string search) fire, and black-box test the binary directly with varied tokens: well-formed JWT-shaped strings with different `alg` values, malformed base64, wrong dot-counts, and payloads containing suspicious substrings. Vary one thing at a time (alg value, admin boolean, injection substring, malformed structure) and record the exact output for each, building up the branch logic from observed behavior rather than guessing. Pay special attention to precedence: which check happens first (format vs decode vs alg vs admin vs injection-scan), since order determines which error message wins. Also nail exact substring matches for the injection scan (case-sensitivity, exact quoting) by testing near-miss variants. Only after this empirical map is complete, write the Python replica so that whitespace, key order, and boolean casing match exactly — then fuzz your own script against the binary's live output before finalizing.
