Read both gzipped vectors without fully extracting to disk: stream-decompress each with `zcat`/`gunzip -c` (or Python's `gzip` module opened in text mode) directly into your computation rather than writing an uncompressed copy. Compute the dot product as the sum of elementwise products across the paired lines, and the Pearson correlation coefficient using the standard formula (covariance of the two vectors divided by the product of their standard deviations) — if using shell tools, `paste <(zcat vector_a.txt.gz) <(zcat vector_b.txt.gz) | awk '{...}'` can accumulate the needed sums (Σx, Σy, Σxy, Σx², Σy², n) in a single pass and compute both metrics from those running totals at the end; if using Python, `numpy.dot` and `numpy.corrcoef` are the direct equivalents but read the gz streams instead of loading pre-extracted files. Round the dot product to exactly 2 decimal places and the correlation to exactly 4 decimal places, and write them to `/home/user/etl_metrics.log` in precisely the two-line `DOT_PRODUCT=...`/`CORRELATION=...` format with no extra whitespace or trailing content, since the check is an exact-match diff against a reference file.