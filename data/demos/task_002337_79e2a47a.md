Build the supervisor as an explicit polling loop, not one-shot subprocess calls: parse .service_env into a dict and merge it into `os.environ.copy()` when calling `subprocess.Popen` for each child, redirecting stdout/stderr into a shared logger (use `logging.handlers.RotatingFileHandler('/home/user/logs/services.log', maxBytes=1024, backupCount=3)`) — since Popen doesn't pipe directly into a Python logger, either poll and read each child's pipe in a non-blocking way and log the lines yourself, or redirect subprocess stdout/stderr to a file object and let the rotating handler manage the file lifecycle. Every loop iteration (1s sleep), call `Popen.poll()` on each child; if it returns non-None (process exited), mark that service "dead" in a status dict, respawn it via a fresh Popen with the same env, and increment a persistent `restarts` counter — then always write `/home/user/monitor_status.json` reflecting current status and the counter total. Start the two target scripts by their given absolute paths, launch supervisor.py detached in the background (nohup), and manually verify by killing one child process and confirming restarts increments and status flips back to "running" within a few seconds.