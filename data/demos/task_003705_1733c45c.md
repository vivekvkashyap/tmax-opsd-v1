Work in stages and verify each before moving on. First, run `tesseract /app/sensor_config.png -` (install via apt if missing) to recover the actual `BucketSize` and `ZThreshold` values from the image — print the raw OCR output and sanity-check it looks like two labeled numbers before hardcoding them into your program; do not guess these values. Next, write the C++ pipeline: read `/app/sensor_data.csv` with a fast parser (memory-map or buffered read, split work across threads by row range), reshape wide rows into (timestamp, sensor_id, value) tuples, and bucket by `floor((timestamp - min_timestamp) / BucketSize) * BucketSize + min_timestamp`-style arithmetic (or simplest: `timestamp - (timestamp % BucketSize)` if buckets align to epoch) — get the bucket boundary convention right, since an off-by-one here silently shifts every anomaly's bucket and tanks F1. For each (bucket, sensor) group compute mean and population/sample stddev, then flag `|value - mean| > ZThreshold * stddev` (strict inequality, as specified). Aggregate counts, drop zero-count rows, sort numerically by bucket_start_time then alphabetically by the sensor_id string (not numeric sensor index — "sensor_10" sorts before "sensor_2" alphabetically), and write the exact header to `/home/user/anomalies.csv`. Compile with `-O3 -pthread` and test on a small synthetic slice before running on the full 1M-row file.
