Build the report with three independent shell checks, each writing into a variable, then assemble the file in the exact required order. For Machine: run `readelf -h /home/user/audit_target/app_bin`, grep the `Machine:` line, and strip only leading whitespace after the colon while preserving the rest of the string verbatim (don't reformat or re-case it — the value must match `readelf`'s own text exactly). For certificates: `openssl verify -CAfile root.pem -untrusted intermediate.pem leaf.pem` and check the command's output for the string `OK` (or check its exit code is 0) to decide VALID vs INVALID — don't assume success, actually inspect the result since a broken chain should fail. For CSP: grep `headers.txt` for a `Content-Security-Policy` header line and confirm it contains the exact case-sensitive substring `default-src 'none'`; presence of the header alone with a different directive should count as MISSING. Write the three lines under the literal header `[AUDIT TRAIL]` to `/home/user/audit_report.txt`, matching the field names and order given in the task exactly, with no trailing extra whitespace.
