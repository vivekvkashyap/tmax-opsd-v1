Handle this as four independently checkable deliverables. For the Expect script, `spawn /app/legacy_vault`, then a strict sequence of `expect`/`send` pairs matching each prompt with the credentials given in the task, then capture the program's final line of output (e.g. via `expect eof` with `log_user` toggled, or parsing `$expect_out(buffer)`) and print only that token — no prompt echoes — to stdout. For the fstab line, construct exactly one space-separated NFSv4 entry with the given remote share, mount point, fstype `nfs4`, the specified comma-joined options, and `0 0` for dump/pass, per the task's own spec. For the video, use ffmpeg to reduce resolution/bitrate (`-vf scale=...` plus a lower CRF or explicit `-b:v`), then measure quality with ffmpeg's `-lavfi ssim -f null -` against the source, iterating compression settings until SSIM clears the required threshold while keeping file size minimal — don't guess one setting and stop. For the systemd unit, write a standard `[Unit]`/`[Service]`/`[Install]` file with the exact keys the task specifies (Description, ExecStart, Type=oneshot, WantedBy=default.target), and chmod +x both scripts.
