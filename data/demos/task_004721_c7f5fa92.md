Build and test each stage locally before wiring up the git hook automation. Write parser.c to read `timestamp value [tags...]` lines from stdin, convert with `time_t` -> `localtime()` after setting `TZ`, format with `strftime(..., "%Y-%m-%d %H:%M:%S", ...)`, print `[formatted] value tags`, and after EOF call `exit(1)` deliberately (this simulated crash is required, not a bug to fix). For supervisor.sh, use an infinite `while true; do ... done` loop that runs `TZ=Asia/Tokyo LC_TIME=C ./parser < raw_data.txt | grep -v ANOMALY | awk '{print "VALIDATED: " $0}' >> processed.log` then checks `$?` from the parser (capture its exit status before the pipe consumes it, e.g. via `PIPESTATUS[0]`) — if it's 1, `sleep 2`, append `RESTARTING PARSER` to the log, and loop again. For the post-receive hook, read the ref from stdin, `git --work-tree=/home/user/deploy --git-dir=... checkout -f`, compile parser.c there, `pkill -f supervisor.sh` before restarting, then launch the new supervisor detached with `nohup .../supervisor.sh >/dev/null 2>&1 &` or `setsid`. Test each piece manually first, then commit+push from the workspace clone and wait to observe the log converge to the expected two-cycle pattern.
