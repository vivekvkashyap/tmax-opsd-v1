Start by locating the crash payload: run `strings /home/user/memory.dump | grep CRASH_PAYLOAD:` (or `grep -a -o 'CRASH_PAYLOAD:[^[:space:]]*'` on the raw dump if it's not newline-delimited) and write the exact matched text with `printf '%s' "$match" > /home/user/bad_payload.txt`, being careful not to add a trailing newline or alter the string. Then read `service.c` and trace `parse_telemetry`: find the recursive call and check whether it advances past the character that triggered recursion (e.g. it should recurse on `msg + 1`, not `msg`) — that's the classic infinite-recursion/leak bug when a repeated character (like 'Z') keeps matching the same branch forever. Fix the call to consume one character per recursive step, and confirm `free()` on the allocated buffer happens on every path (including before recursing, or via a structure that doesn't leak on the recursive branch). Rebuild with `gcc -O2 -o /home/user/service_fixed service.c`, then test end-to-end: `timeout 2s /home/user/service_fixed < /home/user/bad_payload.txt; echo $?` should return quickly with exit code 0, not hang (124) or crash.