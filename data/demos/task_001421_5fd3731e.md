Write the pipeline as a single Python script that you execute, not by hand. Load both CSVs with pandas, `pd.merge(df_a, df_b, on='id', how='outer')`, then `sort_values('id')`. The key trap: an outer join on integer `id` columns with non-overlapping key ranges introduces NaNs into `id` itself, which silently upcasts the whole `id` column to float64 — so a naive `.to_csv()` will emit `1.0` instead of `1`. Before writing output, explicitly cast the id column back with `.astype(int)` after you've already filled the feature NaNs (fillna on `id` isn't needed since every id is present in at least one source, only the dtype is wrong). Fill NaNs in the five feature columns with integer 0 via `df[features] = df[features].fillna(0)`, select columns in the exact order f1..f5, then run `sklearn.decomposition.PCA(n_components=2, svd_solver='full', random_state=42).fit_transform(...)`. Assemble the output DataFrame with columns in the exact order `id, pc1, pc2` and write with `to_csv(..., index=False)`. Double check the saved file's first data lines to confirm no `.0` suffix appears on ids.