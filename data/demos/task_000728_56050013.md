Treat the binary strictly as a black box for behavior discovery, but you already know the recursive structure (base case, midpoint split, 1-mer distribution, L1 distance) from the spec — the only unknown is the numeric threshold T. Discover it via binary search / bisection: construct short crafted sequences (length just over 10, e.g. two 5-character halves with controllable nucleotide composition) where you can compute the exact L1 distance between left/right 1-mer distributions by hand, then run `/app/seq_domain_splitter <seq>` on sequences straddling different candidate distances and observe whether a split boundary appears in the output — narrow the bracket by adjusting composition until you find the crossover point where behavior flips from "no split" to "split". Log each probe sequence and the binary's raw output to regression_log.txt as you go. Once confident in T, implement seq_splitter.py mirroring the exact recursion order (process left half, append mid, process right half — this determines ascending order naturally since it's an in-order traversal), using `>` (strict) not `>=` for the threshold comparison, and match the exact output formatting (comma-separated, no trailing comma, newline, empty string if no splits). Validate bit-exactness against the oracle on many random sequences before finishing.