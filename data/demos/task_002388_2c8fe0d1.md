Build this bottom-up and test each service in isolation before wiring them together. Start Redis, then start `/app/ingestion.py` and `/app/query.py` unmodified and confirm they work with plain curl on their own ports before touching Nginx. For `worker.py`: use Python's `csv` module (e.g. `csv.reader` or `csv.DictReader` over `io.StringIO(data)`) instead of `data.split('\n')` — the whole point of the bug is that embedded newlines inside quoted CSV fields must not be treated as row separators, and only a real CSV parser respects quoting. For timestamp normalization, detect whether the field is all-digits (treat as Unix epoch, `datetime.fromtimestamp`) versus a date string (parse with `datetime.strptime` using the given `MM/DD/YYYY HH:MM` pattern), then format both to `YYYY-MM-DD HH:MM:SS`. Compute `word_count` via `len(review_text.split())`. Use `INSERT OR REPLACE`/`ON CONFLICT` when writing to SQLite so reprocessing is idempotent. Write the Nginx config with two `location` blocks proxy_passing POST /ingest and GET /query to the two Flask ports, verify `nginx -t` before starting it, and launch every process with `&` or `nohup` before finishing.