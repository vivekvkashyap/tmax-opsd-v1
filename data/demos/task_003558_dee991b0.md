Since the log is already chronologically sorted, dedup by first-seen hash is a single streaming pass — exploit that rather than sorting or buffering the whole file. In `/home/user/aggregate.c`, read stdin line by line, split on commas, and maintain a hash set keyed by the MD5-hash field (a simple open-addressing table or `hsearch`/custom chaining is fine given the stated 50,000-entry bound); skip any line whose hash has already been inserted. For lines that pass, convert the Unix timestamp to a UTC `YYYY-MM-DD` string with `gmtime()`/`strftime` (not `localtime`, which would shift dates by timezone — a common bug here), and accumulate word counts into a map keyed by `(date, language_code)`. At end of input, print `date,lang,total` for every bucket. In `/home/user/pipeline.sh`, compile with `gcc -O2 aggregate.c -o aggregate`, pipe the log file through it, then `sort -t, -k1,1 -k2,2` (chronological by date, then alphabetical by language) into `/home/user/daily_velocity.csv`. Make the script executable and runnable with no arguments, and spot-check a few duplicate hashes manually to confirm only the first occurrence contributed to the totals.
