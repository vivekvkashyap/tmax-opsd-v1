Start by inspecting `/home/user/data.csv` with `head`/`awk` to see the column layout — don't assume column order; identify the employee_id column by finding which column's value set is a superset containing every value referenced by the candidate manager column, and confirm the manager column contains `0` for exactly one row (the root). Write a small script (Python or awk) that emits `manager_id,employee_id` for every row except the root, to `/home/user/hierarchy.csv`. For the C server in `/app/org-server/`: first `cat Makefile` and diff its linker/compiler flags against what actually exists on the system — a bad flag like a nonexistent `-l` library needs to be removed or corrected, don't just add `-w` to suppress the error. Then read `graph.c`'s `get_descendants` function carefully: it likely only appends direct children to the result and returns, missing a recursive call that walks each child's own descendants before returning — fix it to recurse depth-first (or BFS) over the tree so all levels are included, then rebuild with `make` and verify manually with a small test hierarchy before wiring in the real CSV. Finally launch the compiled server listening on `127.0.0.1:8888` in the background and confirm the `REPORTS <id>\n` protocol with a quick `nc` test.