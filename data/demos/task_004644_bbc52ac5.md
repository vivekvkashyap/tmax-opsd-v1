Treat the binary as a black box: first run `strings -a /home/user/audit_agent | grep -A0 TARGET_IP` to locate the IP prefixed by `TARGET_IP=`, and separately grep for `-----BEGIN CERTIFICATE-----` through `-----END CERTIFICATE-----` to pull out the embedded PEM block. In Python, use `re.search` with a raw-bytes read of the ELF file (open in `'rb'` mode) rather than assuming clean text, since PEM lines may be interspersed with binary padding — extract the exact substring from `-----BEGIN CERTIFICATE-----` to `-----END CERTIFICATE-----` inclusive of newlines. Decode/normalize the matched region into a valid PEM string before handing it to `cryptography.x509.load_pem_x509_certificate`. From the parsed certificate object, read `.subject.get_attributes_for_oid(NameOID.COMMON_NAME)[0].value` for the CN, and `.not_valid_after` (or `.not_valid_after_utc` on newer library versions) formatted with `strftime('%Y-%m-%d %H:%M:%S')`. Build the iptables DROP rule string using the extracted IP. Write all four lines to `/home/user/audit_trail.log` exactly matching the required format, then execute the script and confirm the file was created without exceptions.
