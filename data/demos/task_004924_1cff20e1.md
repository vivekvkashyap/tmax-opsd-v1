Work the four objectives in order and re-run `python3 /app/tester/run_tests.py --score-only` after each fix to see the score climb. First, grep the vendored PyJWT for the "none" algorithm bypass: search `jwt/api_jws.py` (and `api_jwt.py`) for any special-case branch that returns/accepts a token early when `header.get("alg")` is "none" — remove that shortcut so decoding falls through to normal signature verification, which must raise when a key was supplied and alg is "none". Second, in `app.py`, replace the raw redirect to `next` with strict validation: only allow strings starting with exactly one `/` and not `//`, and reject anything containing `:` or a scheme like `http:`/`javascript:`; fall back to `/dashboard` otherwise. Third, `chmod 700 /app/logs` and `chmod 600 /app/logs/access.log`. Fourth, write `analyze_logs.py` to regex-extract tokens from both `Authorization: Bearer <token>` headers and `token=` query params in the log, then import your now-patched local pyjwt package (not any system-installed one) and call `jwt.decode(token, "supersecret_key_123", algorithms=[...])` in a try/except, counting only tokens that decode successfully without raising expiry/signature errors, and write that integer to the output file.