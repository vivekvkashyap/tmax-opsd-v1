Build in this order: environment, signal extraction, bug fix, service. Create the venv and install ffmpeg/opencv/numpy/scipy bindings plus a web framework. To extract the signal, decode the video frame-by-frame (e.g. `ffmpeg -i ... -f rawvideo -pix_fmt gray -` piped to numpy, or OpenCV's VideoCapture), compute the mean grayscale value per frame, then run `numpy.fft.rfft` on the resulting time series; the true frequency axis is `numpy.fft.rfftfreq(n, d=1/60)` since the video is 60fps — pick the frequency bin with peak magnitude (excluding the DC/zero-frequency bin) and round to 2 decimals for the output file. For the simulation bug, inspect how forces are accumulated per node: if neighbors are drawn from a `set` or dict without ordering, floating-point summation order becomes nondeterministic across runs because addition isn't associative — fix it by iterating neighbors in a fixed, sorted order (e.g. `sorted(neighbors)`) before accumulating, not by changing the math itself. Validate by running the simulation twice on the test graph and confirming the dominant frequency (found via the same zero-crossing/FFT logic as the script already uses) is bit-identical. Finally wrap `run_simulation` in a FastAPI/Flask POST `/simulate` handler bound to 0.0.0.0:8080, and leave the process running in the background (e.g. with nohup or a background shell) so the verifier can hit it with new graph topologies.
