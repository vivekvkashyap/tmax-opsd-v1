Work the phases in dependency order since later phases assume earlier ones succeeded. Create all five directories first. Write `operator.py` to glob `*.json` in `manifests/raw/`, compute each file's MD5 (`hashlib.md5(data).hexdigest()`), copy it to `manifests/versions/<name>_<hash>.json`, then create/replace the symlink at `manifests/active/<filename>` pointing at that versioned file — use `os.symlink` but first `os.remove` (or `os.path.lexists` check + unlink) any existing symlink/file at that path so reruns don't crash on "file exists" (this is the idempotency requirement). For `validator.py`, don't modify it — it already exists and just needs its precondition satisfied. Write `run_system.sh` to: write `READY\n` to the init flag path, run `operator.py`, then run `validator.py` redirecting stdout to `logs/validation_report.txt`; `chmod +x` the script. Run it once manually to populate the report and prove the pipeline works end-to-end before moving on. Finally install the cron job with `crontab` (build the new crontab content, e.g. via `(crontab -l 2>/dev/null; echo "* * * * * /home/user/run_system.sh") | crontab -`), confirming no duplicate entries accumulate if this task is retried.
