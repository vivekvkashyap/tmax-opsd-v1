Diagnose the crash by actually running the failing script and reading its error/exit behavior rather than guessing at the fix. Execute `/home/user/launch.sh` first, observe it fails, then inspect `/home/user/app/app.py` to see it exits non-zero unless `DEPLOY_ENV=production` is set in the environment AND the current working directory's basename is `app` — so fix `launch.sh` to `cd /home/user/app` and `export DEPLOY_ENV=production` (or prefix the command) before invoking `python3 app.py`, then run it and confirm (e.g. via `curl localhost:8081`) it responds instead of crashing. For `configure.py`, read the existing `/home/user/app/config.json` if present, merge in `proxy_active: true` and `backend_port: 8081` without dropping other keys (load-modify-write, defaulting to `{}` if the file is missing), and run it — verify idempotency by running it twice and checking existing keys survive. For `proxy.py`, implement a minimal HTTP server on localhost:8000 whose GET handler makes an HTTP request to `http://localhost:8081/` and relays the exact response body back to the client; launch backend, run configure.py, launch the proxy in the background, then confirm end-to-end with `curl -s http://localhost:8000/ > /home/user/proxy_test.log` and inspect the log's content before finishing.
