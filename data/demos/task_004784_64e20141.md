Part 1: extract every frame of `/app/storage_dashboard.mp4` (e.g. `ffmpeg -i storage_dashboard.mp4 frame_%04d.png`), then for each frame compute the mean R, G, B (e.g. via ImageMagick's `identify -format "%[fx:mean.r*255],%[fx:mean.g*255],%[fx:mean.b*255]"` or a small Python/Pillow script), count frames satisfying R>150 and G<50 and B<50, and write that integer count (nothing else) to `/home/user/failure_count.txt`. Part 2: build `/home/user/process_manifests <input_dir> <output_dir>` that, per file, detects its encoding (try UTF-8, UTF-16, ISO-8859-1, Shift-JIS in a sensible order — e.g. `chardet`/`charset_normalizer` or explicit try/decode fallback) and decodes to UTF-8 text; reject the file (do not copy it) if the *decoded text* or the *filename* contains `../`, `..\`, a null byte, or references to `/etc/`, `/var/`, `/root/` — check the filename independently of decoding since a malicious filename itself is disqualifying even if content is clean. Approved files get copied to `<output_dir>` re-encoded strictly as UTF-8, and you must also emit `<output_dir>/manifest.sha256` with SHA256 hashes (standard `sha256sum` format) computed over the copied (UTF-8) file contents. Test against both corpora to make sure no evil files leak through and no clean files are wrongly rejected.
