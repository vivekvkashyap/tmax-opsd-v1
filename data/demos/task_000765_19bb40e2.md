For extract_logs.sh, use ffmpeg to pull per-frame pixel data rather than guessing brightness values visually: extract frames to raw images or use `ffmpeg -i transaction_monitor.mp4 -vf "crop=10:10:0:0" ...` (top-left) and a second crop for top-right (x-offset = width-10), then compute average grayscale per frame — a practical approach is exporting frames as PNG/PPM and averaging pixel values in a small script (awk/python/ImageMagick's `convert ... -format "%[fx:mean*255]" info:`), then round to the nearest integer encoded ID; write timestamp (derive from frame index and frame rate),transaction_id,resource_id rows to transactions.csv, one row per frame, in frame order. For detect_deadlocks.sh, build a wait-for graph from the CSV (track which transaction currently holds each resource vs. which transaction is waiting on it), then implement cycle detection in awk/bash using DFS with a recursion/visited-state stack (unvisited/in-progress/done) to find all distinct cycles — output each cycle's transaction IDs comma-separated and numerically sorted, one cycle per line. Since it's fuzz-tested for bit-exact equivalence against a hidden oracle, handle edge cases carefully: self-loops, multiple simultaneous cycles, and resources released before being deadlocked. Make both scripts executable.
