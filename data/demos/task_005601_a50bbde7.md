Treat this as a graph-reachability problem, not a flat query. Load `policies.json` and pull the `allowed_groups` list for the target document. For resolution, either write a recursive Python function that, given a group name, looks up its `group_id`, then for every row in `group_members` with that `group_id`: if `member_user_id` is set, add that user; if `member_group_id` is set, recurse into that group — and keep a `visited` set of group ids to guard against cycles in nested memberships even though none may exist in this data — or equivalently write a SQLite `WITH RECURSIVE` CTE that starts from the seed groups and unions in nested `member_group_id` rows level by level, then joins the terminal `member_user_id` rows to `users`. Either approach must start from *all* groups listed for the document, not just the first, and must traverse arbitrary depth, not just one level of nesting. Collect the resulting usernames into a `set()` to dedupe, `sorted()` alphabetically, and write the exact JSON shape `{"document": ..., "authorized_users": [...]}` to the output path.
