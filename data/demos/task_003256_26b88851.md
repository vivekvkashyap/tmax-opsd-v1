Do the OCR extraction first since it drives both the schema change and the query filter logic — run `tesseract /app/schema_hint.png stdout` and read the recovered text carefully for the exact index-creation statement and any filter condition mentioned (e.g. an "is_deleted" or similar flag to exclude). Apply the index exactly as specified via `sqlite3 /app/knowledge.db "CREATE INDEX ..."` (match the discovered index name and column order, since the verifier checks for that specific index name in the schema). Before writing the HTTP server, first prototype and test the recursive CTE directly in the sqlite3 CLI: something like `WITH RECURSIVE reach(node, depth) AS (SELECT target_node, 1 FROM edges WHERE source_node=? AND edge_label=? AND is_deleted=0 UNION SELECT e.target_node, r.depth+1 FROM edges e JOIN reach r ON e.source_node=r.node WHERE e.edge_label=? AND e.is_deleted=0 AND r.depth < ?) SELECT DISTINCT node FROM reach ORDER BY node` — verify it against the sample data in your head before wrapping it in code. Then write a Python `http.server`-based (or similar) handler for `GET /api/traverse` that parses `start_node`, `edge_label`, `max_depth` from the query string, runs this CTE, and returns sorted JSON with a `count` field matching the list length exactly; start it backgrounded and curl-test it against a couple of known paths before finishing.