First run `tesseract /app/stamp.png stdout` to recover the stamp text, then strip trailing whitespace/newlines from the OCR output and store it in a shell variable for reuse — do not hardcode a guessed string. To enumerate corrupted_docs safely, don't use `find` with default symlink-following (it will loop forever); instead use `find /app/corrupted_docs -type f` (which by default does not descend into symlinked directories and only lists real files) or explicitly use `find -L ... -type f` combined with a visited-inode guard, then dedupe by content: compute `sha256sum` on each real file, sort by hash, and keep one path per unique hash so symlink-duplicated files aren't processed twice. For each unique file, use `sed "/DEPRECATED/d"` to drop lines containing that literal word, then prepend the recovered OCR line via `sed "1i$stamp_text"` or by writing the stamp line followed by `cat`. Concatenate the processed files in ascending SHA256-hash order into `/app/compiled_docs.txt` for deterministic output, then compress with `xz -9 -k /app/compiled_docs.txt` (or `tar` first, then `xz -9`) to produce `/app/compiled_docs.tar.xz`. Wrap the whole procedure in `/home/user/recover.sh`, make it executable, and run it to generate both output files. Validate correctness by re-running the script and confirming byte-identical output (determinism), not by eyeballing content.
