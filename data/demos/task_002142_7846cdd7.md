Treat this as a log-correlation forensics task, not a code-fix task. Cross-reference the three log files by timestamp and transaction ID: in `worker.log`, look for a transaction ID that gets "Assigned" to two different thread names at essentially the same timestamp (that's the double-assignment race), rather than assuming the last-mentioned or most-frequent TXN is the culprit. Confirm your suspicion by checking `db.log` for a corroborating warning about multiple write locks on that same TXN ID. Once you have the culprit TXN ID, grep the `/home/user/wal/` directory (`grep -l STATUS:UNCOMMITTED /home/user/wal/*.dat` or similar) for the specific file containing that exact TXN ID alongside the `STATUS:UNCOMMITTED` marker — don't just pick the first file with that marker if multiple exist; the correct one must contain the specific culprit TXN ID string too. Write the two-line report to `/home/user/report.txt` in the exact literal format `TXN_ID: <id>` / `WAL_FILE: <absolute path>`, matching field names and no extra whitespace or trailing content.