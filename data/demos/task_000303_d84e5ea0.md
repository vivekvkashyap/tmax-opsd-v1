Fix the pipeline stage by stage rather than rewriting blindly. In `load_and_clean_data`, coerce `sensor_a/b/c` with `pd.to_numeric(..., errors='coerce')` then `dropna` on those columns — this removes the `"ERR"` rows cleanly. For rolling covariance, use pandas' built-in `df['sensor_a'].rolling(window=3).cov(df['sensor_b'])`, then explicitly fill the first two rows (where a full window isn't available) with `0.0` via `.fillna(0.0)` — don't hand-roll the windowing, it's easy to off-by-one the "current + 2 previous" definition. Apply the formula from `model_spec.txt` verbatim as a vectorized column operation. For the plotting crash, set `matplotlib.use('Agg')` before importing `pyplot`, and replace `plt.show()` with `plt.savefig('/home/user/anomaly_plot.png')`. Finally sort by `anomaly_score` descending (`df.nlargest(3, 'anomaly_score')`) and dump the `timestamp` values as a JSON list in that order to `/home/user/top_anomalies.json`. Run the fixed script end-to-end and confirm both output files are produced.
