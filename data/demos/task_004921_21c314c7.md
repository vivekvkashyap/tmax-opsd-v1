Start with OCR: install `tesseract-ocr` and run it against `/app/alert_trace.png`, then read the extracted text closely to find the referenced commit hash. Use that hash to inspect the git history in `/home/user/ingest-repo` — `git show <hash>` and `git log -p -- validate.sh` (or `git log --diff-filter=D -- validate.sh` to find when it was deleted) to recover the deleted validation script's logic, and separately search history for a since-removed secret with `git log -p | grep -i -A2 -B2 secret` or `git log --all -p -- '*'` around the same commits. From the recovered script you should find it was checking for a UTF-8 BOM (`\xEF\xBB\xBF`) at the start of files, and from the diffs a hardcoded API key string. Write `/home/user/filter.sh` to read the target file's raw bytes, exit 1 if it starts with the BOM byte sequence or contains the recovered secret string anywhere in its content, and exit 0 otherwise; `chmod +x` it and test against a few hand-made BOM/non-BOM, secret/non-secret files before trusting it against the hidden CI corpus.
