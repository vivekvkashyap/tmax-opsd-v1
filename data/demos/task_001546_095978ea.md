Do the fix-and-verify loop before writing new code. First, try to build the vendored package with `cd /app/mcparser && go build ./...` and read the compiler error precisely — it will point at a type name used in a function signature that doesn't match the type actually returned/constructed in the body; grep the package source for the mismatched identifier and correct the typo so the declared return type and the literal type used inside the function agree (don't rename broadly — find the exact single misspelled occurrence). Confirm it builds cleanly before moving on. Then write `/home/user/detect.go`: take the file path as `os.Args[1]`, call `mcparser.Parse(path)` to get `[]float64`, then compute the specified roughness metric `R` in a straightforward loop over consecutive pairs — implement exactly the formula given (average squared consecutive difference), and print exactly `CLEAN` or `EVIL` per the given thresholds (values strictly between 2.0 and 5.0 shouldn't occur in the real corpora, so don't overthink the boundary — but do implement the two threshold checks precisely as stated). Validate your classifier by running it with `go run` against every file in `/app/corpus/clean` and `/app/corpus/evil` and confirming each prints the expected label before considering the task done.