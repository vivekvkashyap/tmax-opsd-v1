In cleaner.c, first parse the 50x10 CSV into a 2D array of doubles (no header to skip). Implement Pearson correlation between two columns using the standard formula (covariance over product of standard deviations, or the sum-based formula: `(n*sum_xy - sum_x*sum_y) / sqrt((n*sum_x2 - sum_x^2)*(n*sum_y2 - sum_y^2))`) and take `fabs()` of the result before comparing to the 0.85 threshold — link `-lm`. For the drop logic, iterate `i` from 0 to 8, and for each `i` not already dropped, check all `j > i` not already dropped; if `|corr(i,j)| > 0.85`, mark `j` dropped (never re-drop or re-examine an already-dropped column, and never let a dropped column cause further drops — only compare against still-active earlier columns). This asymmetric column-order rule is the key detail naive implementations get backwards. Write surviving columns in original order, each value formatted with `%.6f`, comma-separated, no trailing comma. For test_pipeline.sh, compile with `gcc cleaner.c -o cleaner -lm`, run it twice to two different output filenames, `md5sum` both and compare the hashes as strings, then print the pass/fail message and exit code accordingly; `chmod +x` the script.
