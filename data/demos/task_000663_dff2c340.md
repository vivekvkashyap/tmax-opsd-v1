Model this as a recursive-CTE graph traversal problem, not a manual join. Write a WITH RECURSIVE query that starts from every FULL backup row for `db_name='auth_prod'` and recursively joins child rows where `parent_id = id` of the previous row, carrying along a running chain (as a list or concatenated path) plus running sums of `size_mb` and `duration_sec`. Critically, filter `timestamp < '2023-10-27 14:00:00'` on every row as you build each candidate chain (not just at the end) — a chain must stop extending the moment the next incremental would exceed the crash time, and any backup for a different db_name or created after the crash must never enter the recursion. Among all resulting candidate chains for auth_prod, pick the one whose last backup has the maximum timestamp still under the crash cutoff (i.e., the chain that gets closest to, but not past, the crash). Once you've identified that chain's ordered list of ids, sum size_mb/duration_sec over exactly those rows. Use Python's sqlite3 module to run the CTE, then assemble and json.dump the result to the exact schema and path requested, ordering the `chain` array chronologically from FULL to latest INC.