Break this into three independent verifiable stages. Stage 1: OCR the constant — run `tesseract /app/blueprint.png stdout` and parse out the numeric value from the recognized text (it will look like `CONSTANT=<number>`); don't hardcode a guessed number, actually read tesseract's output and extract the digits programmatically or by inspection. Stage 2: in `/app/calculator/src/main.rs`, find the marker/gap where the constant should be declared (likely a `const CONSTANT: i64 = ...;` line is missing or referenced but undefined), insert it with the OCR'd value, then `cargo build --release` and manually test the resulting binary (`./target/release/calculator <n>`) against a couple of inputs to confirm it prints `n * CONSTANT`. Stage 3: write a Python HTTP server bound to `0.0.0.0:8888` (using `http.server`'s `BaseHTTPRequestHandler`, or Flask/FastAPI) exposing `POST /calculate`, which reads a JSON body `{"number": N}`, invokes the compiled Rust binary via `subprocess.run([...binary_path, str(N)], capture_output=True)`, parses the integer from its stdout, and responds with `{"result": <int>}` and a 200 status. Run the server in the foreground/background and verify with `curl -X POST localhost:8888/calculate -d '{"number": 4}'` before concluding, since the grader will hit this exact endpoint over real HTTP.