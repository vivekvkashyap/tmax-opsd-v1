Build `/home/user/api_filter.sh` as a strict pipeline where any failure aborts immediately with `exit 1` and no stdout — use `set -e` carefully or explicit checks after each stage rather than letting later stages run on bad data. Stage 1: parse the JSON (validate it's well-formed first, e.g. with `jq empty`, rejecting malformed input and missing `auth_token`/`base_record` fields), then call `/app/legacy_validator "$token"` and check its exit code — treat any non-zero as rejection; you don't have its source, so treat it strictly as a black box via its documented argument/exit-code contract, don't try to reimplement or bypass it. Stage 2: apply the RFC 6902 `record_patch` array to `base_record` — a small `jq`/`python3` json-patch application (handling at least `replace`, `add`, `remove` on JSON pointer paths) is safer than a hand-rolled string patch. Stage 3: after patching, explicitly check the resulting `role` is not `"admin"` and reject if so — this must run after patch application, since patches are exactly how an attacker escalates. Stage 4: transform to v2 schema (`user_id`→`account_uuid` as `UUID-<n>`, flatten `profile.status` to root, map `"active"/"inactive"`→boolean). Test against both `/app/corpus/clean/` and `/app/corpus/evil/`, checking exit codes and `jq .` parseability of stdout for clean cases.