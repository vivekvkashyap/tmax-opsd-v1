Install the missing toolchain first (`sudo apt-get update && sudo apt-get install -y g++ make libboost-math-dev`) so the Boost header is resolvable. Then open `/home/user/tracker/artifact_tracker.cpp` and find the MAP computation: it casts `p_alpha`/`p_beta` to `int` before dividing, causing integer (truncating) division that zeroes out results — replace it with pure `double` arithmetic per the given formula `(alpha - 1.0) / (alpha + beta - 2.0)`, keeping `p_alpha`/`p_beta` as doubles throughout. Leave the CSV joining, Bayesian posterior updates, and cosine similarity logic alone since they're already correct. Build with `make` inside `/home/user/tracker/`, then run `./artifact_tracker /home/user/data/query.txt` from that directory (the program writes `output.csv` relative to cwd, so run it from `/home/user/tracker/`). Confirm `/home/user/tracker/output.csv` has 4-decimal-place floats for map_estimate, similarity, and final_score for both models before finishing — sanity-check that neither map_estimate nor final_score is zero, which would indicate the bug is still present.
