Work bottom-up: first get SQLite embeddable by downloading the amalgamation zip and unzipping it so you have `sqlite3.c`/`sqlite3.h` to compile directly into your program (no system package needed). Write the C++ program using the plain SQLite C API (`sqlite3_open`, `sqlite3_exec`/`sqlite3_prepare_v2`), and read each CSV with simple line parsing (split on commas, watch for empty `manager_id` fields meaning NULL) using parameterized INSERTs inside a transaction for speed. After bulk insert, explicitly run `CREATE INDEX` statements on `employees(department_id)` and `sales(employee_id)` — this is graded separately by inspecting `sqlite_master`. For the analytical query, build it in stages and test incrementally: first a recursive CTE walking `manager_id` up to the row where it IS NULL to get each employee's top manager, then a separate window-function query (`SUM(...) OVER (PARTITION BY employee)`, then `RANK() OVER (PARTITION BY department ORDER BY total_sales DESC)`) joined to departments, filtering to employees with sales >0, ordered by department name then rank. Format each output row exactly as specified and write with `sqlite3_prepare_v2`/step loops, checking return codes so ingestion errors don't silently corrupt the report.
