Tackle this as three independent subsystems, verifying each before wiring them together. First, write and compile the C filter (`gcc -o /home/user/doc_filter doc_filter.c`): read the whole file, scan for markdown link syntax `[...](...)` (a simple state-machine or regex-free scan looking for `](` followed by capturing chars until the matching `)`  is usually more robust in C than a full regex), and for each captured target check if it starts with `/` or contains the substring `../`; exit 1 on any match, exit 0 if none found across the whole file. Test it against every file in `/home/user/corpora/clean/` (expect exit 0 for all) and `/home/user/corpora/evil/` (expect exit 1 for all) using a shell loop before moving on. Second, edit `/home/user/services/uploader/config.json` to set `FILTER_BIN` to the absolute path of your compiled binary, `PUBLISH_DIR` to `/home/user/docs_published`, and `BACKUP_URL` as specified — check the existing JSON key names rather than guessing. Third, fix the Nginx `root` directive in `nginx.conf` for the port-8080 server block to point at `/home/user/docs_published`. Then create the publish directory and a symlink `index.md -> home.md` inside it (`ln -s home.md /home/user/docs_published/index.md`; the target need not exist yet). Finally run `/home/user/services/start_all.sh` and confirm uploads and serving work end-to-end.