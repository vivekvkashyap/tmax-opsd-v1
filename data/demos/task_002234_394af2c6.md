Work the pipeline in order: first fix the config-only bug, then build the standalone detector, then wire it in and confirm end-to-end. (1) Inspect `/app/services/emitter/` for its Redis connection settings (e.g. an env file) and correct the port/host so it matches the actually-running local Redis instance — check what port Redis is really listening on rather than assuming. (2) For `filter_corpus.py`, don't hand-pick thresholds by eyeballing a few files — load each CSV in `/app/corpus/clean/` and `/app/corpus/evil/` as a matrix and compute a numerically principled separability measure (e.g. condition number via SVD, or matrix rank) using numpy/scipy; find a decision boundary that cleanly separates the two known corpora with margin, since the hidden holdout will have the same statistical shape. Never hardcode filenames or per-file logic — the function must generalize from the numeric property alone. (3) Import this function into the worker and gate the NMF call behind it so bad matrices are dropped, not crashed on. (4) Start the whole system via `start.sh`, then tail `/app/services/worker/success.log` to confirm it's growing and the worker process hasn't died.