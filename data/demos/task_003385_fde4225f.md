First run /app/bin/fs_event_gen with a small seed to produce a sample log and hex-dump the first few records (`xxd`) to confirm the exact byte layout matches the spec: 8-byte timestamp, 4-byte event type, 2-byte path length, then the raw path bytes, all little-endian and unpadded. Write fast_filter.c using `mmap` (or a large fixed buffer with `fread`) over the whole file rather than `fread`-ing record-by-record or byte-by-byte, since the performance threshold (under 0.25s on a ~450MB file) rules out syscall-heavy or unbuffered reads; walk a single pointer through the mapped region, reading the header fields with `memcpy` into fixed-width integer types (respecting little-endian layout on typical x86/ARM Linux), and advance the pointer by 14 + path_len each iteration. For each record, check event type == 3 and that the path bytes end with ".conf" (compare the last 5 bytes directly rather than building a null-terminated string with strcmp, to avoid extra allocation/copies), and if matched write the path followed by a newline to stdout using buffered output (e.g. `fwrite`/`putchar_unlocked` or one `write` per batch, not `printf` per byte). Compile with `-O3` in build_and_test.sh, then chain the generator and your filter, redirecting output to /home/user/filtered.log, and make the script executable with `chmod +x`.
