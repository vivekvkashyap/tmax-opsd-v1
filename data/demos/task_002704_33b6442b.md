Build the three pieces independently and test each before wiring them together in the deploy script. For `proxy.cpp`, read one line via `std::getline(std::cin, line)`, check with `line.rfind("COST", 0) == 0` (or `.substr(0,4)=="COST"`) to decide FORWARD vs DROP, and on DROP open `/home/user/proxy.log` in append mode and write `[BLOCKED] <line>\n` — compile and manually test both branches with `echo ... | ./a.out` before moving on. For `logrotate.conf`, target `/home/user/proxy.log` and include the four required directives (`daily`, `rotate 2`, `compress`, `missingok`) inside the block. For `deploy.sh`, perform the steps in order and don't skip verifying each: compile `proxy.cpp -o /home/user/proxy_v2`, `mv /home/user/proxy_active /home/user/proxy_backup` (must be a real move, not a copy, so the original file no longer exists at that path), then `ln -s /home/user/proxy_v2 /home/user/proxy_active` — the target of the symlink must be exactly the `proxy_v2` path (check with `readlink`), and remember to `chmod +x deploy.sh`.
