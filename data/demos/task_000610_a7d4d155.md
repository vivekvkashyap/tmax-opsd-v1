Build the validator as a strict field-by-field parser, not a loose regex/scanf. Split each line on commas and first reject any row that doesn't have exactly 5 fields. Then check: id is exactly 8 characters, all lowercase hex digits (0-9a-f) — reject wrong length or any uppercase/non-hex char; sensor_id parses fully as an integer (no trailing junk, no decimal point) in [1,999]; temperature parses fully as a float in [-50.0,150.0]; timestamp parses fully as an integer in [1600000000,1700000000]; active is the exact literal "true" or "false" (case-sensitive, not "True"/"1"). Use strict numeric parsing (e.g. strtol/strtod and verify the whole token was consumed) so trailing garbage or extra whitespace fails validation. Any parse failure, extra/missing column, or empty file should yield INVALID. Print exactly VALID/INVALID with matching exit code 0/1.

For process_all.sh: curl the files.list, loop over each name, curl-download to a temp dir, run processor and check its exit code. Only for exit 0 files, compute the average temperature over active=="true" rows (awk works well) formatted to exactly 2 decimals, then pipe "<filename>:<avg>\n" into nc against 127.0.0.1:9090 — make sure nc actually flushes/closes the connection (e.g. `nc -q1` or closing stdin) so the accumulator receives the full line before the socket closes. Skip sending anything for invalid files. chmod +x the script.
