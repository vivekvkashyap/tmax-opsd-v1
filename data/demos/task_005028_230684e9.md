Reverse-engineer the .bkp format from the provided binary before writing any server code: run `strings /app/legacy_extractor` to look for clues, and `xxd`/`hexdump` on a sample file you craft to test byte-by-byte how the extractor behaves (e.g. feed it a small handmade file and observe via `strace -e trace=open,openat,read,write` what it reads and where it writes), inferring the header/length-prefixed record structure rather than assuming one. Once the format is known, write a plain C TCP/HTTP server (raw sockets or a minimal HTTP parsing loop is fine — no framework needed) listening on 127.0.0.1:8080 that reads the raw POST body into a buffer and parses records sequentially. The critical security fix is: for each archived filename, strip any directory component (use something like `basename()` or manually find the last '/' and take the substring after it) before joining with `/home/user/extracted/`, so `../../../home/user/hacked.log` becomes just `hacked.log` in that directory — never pass the raw archived name to fopen. After writing a file, parse its `Date:` and `Time:` lines (simple string scanning for the fixed label prefixes) to build the `YYYYMMDD_HHMMSS_` prefix and rename() the file accordingly, and capture the first file's `Event:` value to return as the HTTP response body. Compile to /home/user/server and run it backgrounded before verification.
