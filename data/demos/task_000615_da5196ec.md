Split the work between the shell (selecting/filtering candidate files) and the C program (safe splitting + atomic writes), since the spec calls for both. In shell, find `.txt` files under `/home/user/legacy_docs/` whose first line is exactly `Status: ReadyForArchive` (e.g. `head -n1` each file and compare, or `awk 'FNR==1'`), and only pass those paths to your compiled `/home/user/processor`. In C, read `/home/user/doc_rules.conf` line by line splitting on `=` to get SEARCH/REPLACE/DELIMITER dynamically (don't hardcode them). Process each input file: skip its first (Status) line, then split remaining content into chunks wherever a line exactly equals the delimiter (the delimiter line itself is dropped, and consecutive delimiters should produce an empty-but-valid chunk boundary), applying the SEARCH→REPLACE substitution to each chunk's text. For atomic output, write each chunk to `<name>_partN.tmp`, `fflush`+`close` it, then use `rename()` (which is atomic on the same filesystem) to move it to `<name>_partN.txt` — this avoids readers ever seeing a partially written file. Finally `tar -czf /home/user/final_docs.tar.gz -C /home/user processed_docs` and confirm draft_notes.txt's content never appears in the archive.
