This is a deterministic file-transform task — read the input JSON to know the exact keys/values rather than assuming, then derive every output from it. Parse `/home/user/app_config/incoming/v2_update.json` (e.g. with Python `json.load`), emit `key,value` pairs sorted alphabetically by key with no header row, and write them to `/home/user/app_config/history/v2_update.csv` (watch for exact value formatting — e.g. numeric JSON values should render as their plain literal, not with trailing `.0` or quotes). Create the symlink with `ln -s /home/user/app_config/history/v2_update.csv /home/user/app_config/active/current.csv`, making sure the target is the absolute path as specified (verify with `readlink`). Count the actual number of top-level keys in the parsed JSON programmatically (`len(data)`) rather than assuming a fixed number, and write `/home/user/app_config/deploy_log.xml` matching the exact given structure/indentation with that count substituted for `[NUMBER_OF_KEYS]`. Diff your generated files against the spec mentally (or with `cat`) before finishing to catch stray whitespace/newline mismatches, since exact-content verification is strict.