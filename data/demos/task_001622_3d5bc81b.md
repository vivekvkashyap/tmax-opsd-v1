This is a black-box reverse-engineering task: don't guess the algorithm from the description alone, derive it empirically. Generate several small synthetic CSVs by hand (a 2-3 level hierarchy, a disconnected/orphan employee, two departments at the same depth, duplicate scores), run `/app/legacy_parser <file>` on each, and diff the outputs against your hand-computed expectations to pin down exact behavior — especially rounding mode (does 2.345 round to 2.34 or 2.35 — check `round()` vs half-up decimal), how `chain_length` is computed for the root and for orphans with a missing manager, and whether peer averages include the employee itself. Once your Python replica matches the binary byte-for-byte on your hand-built cases, generalize it into `/home/user/process_data.py`: parse the headerless CSV, build parent-pointers, BFS/DFS from roots to assign depths, group by `(department, chain_length)` for peer averages, and emit JSON sorted by `emp_id` matching the exact key names and formatting. Before finishing, feed several more random CSVs through both programs and diff raw stdout to confirm exact equivalence, not just semantic similarity.