Work in two passes over the two files rather than trying to merge them in one loop. First, stream `pipeline_logs.jsonl` line by line with `json.loads`, collecting the set of `job_id`s whose `status` field equals "RETRYING" at least once. Second, read `output_data.csv` with `csv.DictReader` and build a per-job list (or `collections.Counter`) of `record_id` values, but only for jobs in that retried set — skip everything else. For each retried job, compute duplicates as `count(record_id) - count(distinct record_id)`, i.e. sum over each record_id of `(occurrences - 1)`; do not just count rows minus unique rows incorrectly if a record_id appears 3+ times (each extra occurrence beyond the first is one duplicate, so use `Counter` values minus 1 per key, summed). Track the running total across all retried jobs and separately track which single job has the largest per-job duplicate count. Sort the retried job list alphabetically before writing. Write the final dict to `/home/user/anomaly_report.json` with `json.dump`, matching the exact key names given in the task (`retried_jobs`, `total_duplicates_from_retries`, `max_retry_spike_job`), then execute the script so the file actually gets created on disk.