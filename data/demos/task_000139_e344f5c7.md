Break this into build, data-structure, and glue-code stages and verify each independently. For the Go cross-compile step, build the shared library with `CGO_ENABLED=1 GOOS=linux GOARCH=amd64 go build -buildmode=c-shared -o /home/user/build/aggregator.so aggregator.go` (CGO must stay enabled for c-shared mode and the target OS/arch must match your host or a cross toolchain), and separately build the Windows exe with `CGO_ENABLED=0 GOOS=windows GOARCH=amd64 go build -o /home/user/build/aggregator.exe aggregator.go` (CGO must be disabled here since you likely lack a Windows C cross-compiler) — verify each artifact's file type with the `file` command afterward. For `ip_trie.py`, implement a real binary/radix trie over the 32 bits of each IPv4 octet (nested dict or node-with-children structure) so `count_subnet` walks only the prefix-length bits of the CIDR and sums the subtree, rather than storing a flat list and filtering it in Python — that flat approach technically produces correct numbers but violates the "use the trie" requirement checked by AST inspection. In `app.py`, load the `.so` via `ctypes.CDLL`, declare `restype = ctypes.c_char_p` on `GetIPs`, decode and split the comma-separated string, insert into the trie, then query and dump the three counts to JSON with the exact keys given. Run the build then `python3 app.py` to produce `results.json`.
