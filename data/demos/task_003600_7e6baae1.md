Split into two independently verifiable pieces; get the sanitizer solid first since it's fuzz-tested against a hidden corpus. For sanitize: parse JSON with serde_json into a `Value`, then recurse through it tracking nesting depth (reject if it exceeds 3), and for every string value at any depth, case-insensitively scan for forbidden SQL fragments like DROP, UNION, "OR 1=1" (normalize case/whitespace so light obfuscation is still caught) — reject on first match anywhere in the structure, not just top-level fields. Print exactly ACCEPT or REJECT with no extra output. Construct your own varied depth/keyword edge cases to test before trusting the hidden corpus. For the video/aggregate side: run ffprobe with the `select='gt(scene,0)'` lavfi filter to emit scene-change scores with timestamps, load frame/timestamp/score rows into SQLite, then write a CTE that flags rows where score > 0.1 as segment boundaries and uses a window function to group rows into segments, computing each segment's duration and picking the top 3 longest.