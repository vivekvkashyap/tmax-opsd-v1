Split this into the transcription task and the Go sanitizer, and don't let one block the other. For the audio, transcribe `/app/audio/incident_report.wav` with an available ASR tool (e.g. `whisper` if installed, or another local STT) and write exactly what it outputs to `/home/user/transcript.txt` — if no ASR binary is preinstalled, check for one via package manager before assuming it's unavailable; don't fabricate a transcript. For the Go program, the core technique is: parse the tar using Go's `archive/tar` package, and for every header check (a) `Name` isn't absolute and doesn't escape the archive root after resolving `..` segments (use `filepath.Clean`/`path.Clean` on a joined path and check it still starts within the root, rather than a naive substring check for `".."` which both over- and under-rejects), and (b) for `TypeSymlink`/`TypeLink` entries, apply the same absolute/traversal check to `Linkname`. Reject on first violation, printing `REJECT` and exiting 1; otherwise print `ACCEPT` and exit 0 after consuming the whole archive. Test manually against a few hand-built tar files (safe relative symlink, absolute path, `../` escape) with Go's `tar` writer before trusting it on the full corpus.
