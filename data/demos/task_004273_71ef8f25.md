Diagnose both bugs by reading `pipeline.py` before editing generically. The MAE bug: the two features (pressure ~10^4-10^5, friction ~10^-3) have wildly different scales, and Ridge regression's L2 penalty shrinks coefficients uniformly regardless of feature scale, so the small-scale feature's coefficient gets crushed — fix this by standardizing features (e.g. `StandardScaler`) fit on the training split and applied to both train and test before calling `Ridge.fit`/`.predict`, not by changing the regression algorithm itself. The blank-plot bug: look for a `plt.show()` call placed before `plt.savefig()` — in a non-interactive/script context this can clear or leave the figure in a state where the saved file is blank, so remove the `show()` call (or reorder so `savefig` happens first) when running non-interactively. After fixing both, add code to serialize the test MAE to `/home/user/metrics.json` with the exact key `mae`. Finally write `/home/user/run_pipeline.sh` to create the venv, install the four listed packages, run `generate_data.py` then `pipeline.py`, and chmod it executable — run it end-to-end yourself to confirm metrics.json shows MAE < 50 and residuals.png is a real (>5KB) scatter plot before finishing.