Build `/home/user/prepare_data.sh` as a pipeline of discrete, testable stages using only shell tools: (1) filter with `grep -v '?'` (or `awk '!/\?/'`) to drop any row containing a literal `?` — do not use sed substitutions that could turn missing values into `0`/`NaN` strings, just drop the whole row; (2) for correlation, use `awk` to compute, for each of the 10 feature columns against `target`, the Pearson correlation via the standard formula (n*Σxy − Σx*Σy) / sqrt((n*Σx²−(Σx)²)*(n*Σy²−(Σy)²)) — accumulate sums in a single awk pass per column, or one pass computing all 10 at once; (3) take absolute value of each correlation, sort features descending by |r| (`sort -t, -k2 -rn` on a small correlation table you emit), and `head -5` to get the top 5 column names; (4) use `cut` or `awk` with the header row to build `train_clean.csv` selecting exactly those 5 feature columns plus `target` last, preserving row order and the header. After writing the script, actually run `/app/model_evaluator /home/user/train_clean.csv` and check the ACCURACY value — if it lands in the 0.5–0.7 range, that's a strong signal your cleaning or feature-selection step is subtly wrong (e.g. rows not truly dropped, or a correlation sign/column-index bug), not that you need a different modeling approach.