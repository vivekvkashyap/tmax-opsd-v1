Read `analyze.py` fully before editing — note that `adaptive_simpson` already has correct recursive-refinement logic; the real problem is that a single coarse initial evaluation of the whole `[0,10]` interval sees near-zero everywhere near a very narrow peak, so its own error estimate never triggers refinement. First find the peak location precisely: use `scipy.optimize.brentq` (or `root_scalar` with bracket `[4.0, 5.0]`) on `f_prime` to solve for where the derivative crosses zero — don't guess a peak location by eyeballing samples, since the peak is too narrow to spot on a coarse grid. Then fix the integration by splitting the domain at that solved peak center and calling `adaptive_simpson` separately on `[0, peak_center]` and `[peak_center, 10]`, summing the results — this forces the recursion to actually evaluate near the peak instead of assuming flatness across the full range. Keep using the provided `adaptive_simpson` function itself rather than swapping in `scipy.integrate.quad`. Finally write `/home/user/run_analysis.sh` (chmod +x) that runs the Python script and formats both the peak center and total area to exactly 6 decimal places into `/home/user/results.txt` in the exact two-line format specified.
