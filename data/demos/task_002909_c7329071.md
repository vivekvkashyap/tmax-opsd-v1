Split this into service wiring and a pure computation script, and get the script bit-exact before worrying about the services. For nginx.conf, add a `location /api/ { proxy_pass http://127.0.0.1:5000/; }` block (note the trailing slash on proxy_pass strips the /api/ prefix when forwarding) and start redis-server, `python3 /app/api/app.py`, and nginx, then smoke-test with a curl to port 8080 before moving on. For process_signal.py, implement the pipeline in the exact stated order: load the JSON float array, apply the Hanning window w[n] = 0.5 - 0.5*cos(2*pi*n/(N-1)) elementwise, run `numpy.fft.rfft`, take |X[k]|^2 for the power spectrum, then critically use `math.fsum()` (not numpy.sum or a Python `+=` loop) for both total_energy and the weighted centroid numerator/denominator, since ordinary summation drifts in the last bits and will fail the bit-exact fuzz comparison against the oracle. peak_index is just `int(numpy.argmax(P))`. Print a single JSON object with exactly the three keys, nothing else on stdout — test standalone against several JSON input files before wiring it behind Flask/Nginx.