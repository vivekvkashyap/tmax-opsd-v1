Work in three independently verifiable stages. (1) For extract.go, shell out to ffmpeg to dump frames as PNG/PPM (e.g. `ffmpeg -i pendulum.mp4 frame_%03d.png`), decode each with Go's image package, and compute the intensity-weighted (or thresholded >200 grayscale) X centroid as sum(x_i)/count over qualifying pixels — then join by frame_id against sensor_time.csv's timestamp_ms and write raw_trajectory.csv. (2) For clean.go, implement the moving average and min-max normalization as genuinely online/causal computations: maintain a running window buffer of the last 5 values for the average, and running min/max variables updated incrementally as you scan forward — never precompute global min/max or a centered window over the whole array, since that is exactly the leakage bug you're asked to eliminate. (3) For detector.go, don't hand-tune thresholds against the two given corpora only; instead build a general causality test — e.g. compare the smoothed signal's response before vs. after each detected impulse/spike (find local derivative maxima), and reject if the smoothed value already rises in the frames strictly preceding the spike index. Validate detector against every file in both corpora and iterate on the causality metric until 100% separate cleanly, rather than curve-fitting to a threshold that happens to work by accident.