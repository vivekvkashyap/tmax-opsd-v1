Write process_spectra.c to open spectra.dat and read it as 1000 contiguous blocks of 2048 doubles each (fread into a buffer sized N*M, or per-signal), then parallelize the outer loop over signals with `#pragma omp parallel for`, computing the Hanning window inline per index (0.5*(1-cos(2*pi*i/(M-1)))) and accumulating the sum of squared windowed samples per signal into a private/per-index result array (avoid a shared accumulator across threads causing races — each thread should own its own signal's energy value, so writing to `energies[j]` per iteration `j` is safe under `parallel for` without extra synchronization). Write results to energies.csv with header `Signal_ID,Energy` and `%d,%.6f` formatting for exactly 1000 rows. Compile with `gcc -fopenmp process_spectra.c -o process_spectra -lm` and run it. Then write plot_energies.py using matplotlib to read the CSV and scatter-plot Energy vs Signal_ID, saving to energies_plot.png, and run it. Double check the Hanning formula and file layout match the spec exactly since the CSV values will be numerically checked.