Work through this in dependency order rather than jumping straight to the Git hook. First write the Expect script: use `spawn` on the interactive tool, then `expect "prompt text"` / `send "value\r"` triples for each read prompt, ending with `expect eof`; run it three times (once per user) or loop over a list of username/role/status triples inside the script. Second, write the C checker with straightforward file I/O (fopen/fread or line-by-line read) and a plain substring search (e.g. `strstr`) for the literal marker text, mapping presence/absence to the two required exit codes — no need for anything fancier. Compile with gcc. Third, `git init` the profiles directory and write the pre-commit hook as a shell script: get the staged file list with `git diff --cached --name-only --diff-filter=ACM` piped through `grep '\.txt$'`, loop over matches invoking the compiled checker, and `exit 1` as soon as one fails (remember hooks run with CWD at the repo root, so use relative or absolute paths consistently). Finally exercise the workflow exactly as described — stage all three files, watch the commit get rejected, unstage the offending file with `git reset`, commit the rest, then capture `git log -1 --format=%s` into the requested log file.
