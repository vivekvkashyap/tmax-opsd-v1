Design the per-file script first, test it standalone, then parallelize. For a given top-level path, detect its type (by extension or `file`) and recursively extract: `.tar.gz`/`.tar` with the tarfile module or `tar -tf`/`tar -xf` into a temp dir, `.zip` similarly — when you find a nested archive inside (e.g. a `.zip` member inside a `.tar.gz`), extract it to a temp location and recurse into it, tracking depth so you stop after 2 levels. For every `.md` file found, count lines (e.g. `wc -l` or reading and counting newlines) and record `TopLevelFileName::InternalPath -> LineCount`, building the InternalPath by joining nested archive names and internal paths as shown in the example (e.g. `legacy.zip/v1.md`). For files ending `.stream.gz`, do NOT write them to disk — open with `gzip.open(path, 'rt')` (or `zcat file | wc -l` in bash) and count lines directly from the stream, using the filename with `.gz` stripped as the InternalPath. For the locking requirement, open `/home/user/unified_index.log` in append mode and wrap the write in `fcntl.flock(f, fcntl.LOCK_EX)` / `flock(fd, ...)` in bash, releasing after writing, and actually launch multiple invocations concurrently (background `&` jobs or `xargs -P`) against all files in `/home/user/submissions/` to prove no interleaved/corrupted lines result. Diff the final log's line set against your own hand-traced expectations of what should be found.
