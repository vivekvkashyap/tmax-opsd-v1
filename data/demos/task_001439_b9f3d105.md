Break this into reshape, resample, tokenize, similarity, and report — validate each stage's output before moving to the next. Use `pandas.melt` or manual concatenation to go from the wide `temp_A/log_A/temp_B/log_B` columns to long format with a `sensor` column ('A'/'B'). Convert `timestamp` with `pd.to_datetime`, then for each sensor group independently, `set_index('timestamp').resample('15min')` across the *global* min-to-max range of the whole dataset (not just that sensor's own range) — reindex against a shared `pd.date_range` before resampling if needed so both sensors align to the same 15-minute grid. Interpolate `temp` linearly (`.interpolate(method='linear')`) and fill missing `log` with `""` (not interpolation, which doesn't apply to strings). For `normalized_log`, lowercase, strip non-alphanumeric/space chars with regex, and split on spaces into token lists — empty logs should tokenize to an empty list. For similarity, only compare pairs of *distinct, non-empty* logs in the resampled data using Jaccard on token sets (`len(a&b)/len(a|b)`), keep pairs with `0.6 <= sim < 1.0`, dedupe by the original log text (order each pair alphabetically), and sort the final list alphabetically by the first log. Render `report.md` via a Jinja2 template with temperatures and similarity scores formatted to exactly 2 decimals, and manually spot-check a couple of computed averages/similarities against the raw CSV before finishing.
