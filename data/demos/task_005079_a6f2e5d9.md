First locate the bug by reading the vendored package's compiler module (the file implementing `PipelineBuilder`, likely under `mongogen/compiler.py` inside `/app/mongogen-audit-0.5.0`) and find the `$lookup`-stage-building method; it hardcodes the localField to `"_id"` instead of using the method's actual `local` parameter — fix that one line so the dynamic local field is used. Then write `/home/user/build_pipeline.py` to `json.loads(sys.argv[1])`, instantiate `PipelineBuilder(source_collection)`, and call its match/lookup/projection-adding methods in the order the task specifies, using exactly the field names given in the input schema, then print `builder.to_json()`. Since correctness is checked by exact-output fuzzing against many random inputs, be meticulous about matching field ordering, key names, and stage shapes implied by the class's own methods (don't hand-build JSON yourself — trust the builder's `to_json()` to keep behavior consistent) and test locally with a handful of varied inputs, including edge cases like empty-length fields lists, to confirm no crashes or extra/missing whitespace in the printed JSON.
