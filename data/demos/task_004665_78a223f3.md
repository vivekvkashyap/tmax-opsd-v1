First reverse-engineer the oracle's implicit schema empirically: construct several small JSON edge-list files (valid ones, and ones with missing fields, non-string src/dst, `$`-prefixed operator-like keys, non-numeric or non-positive weights) and run `/app/graph_oracle <file>` against each, observing exit codes/crashes to infer exactly which properties are required — do not guess or open/patch the binary itself. Once you've enumerated the failure conditions, encode them as a single `jq -e` boolean assertion (root is an array; every element is an object with `src`, `dst`, `weight`; `src`/`dst` are strings without `$`; `weight` is a number > 0) inside `/home/user/filter.sh`, redirecting jq's stdout to `/dev/null` and letting jq's own exit status (0 if the assertion holds, 1 otherwise) become the script's exit code. Make the script `chmod +x`. Test against a few of your own crafted clean/evil files to confirm the exit codes match your empirical findings before relying on it against the hidden corpora — a naive filter that only checks required keys but not type/sign constraints will fail on the more adversarial evil files.
