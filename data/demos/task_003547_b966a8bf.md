Build this bottom-up and test each piece before combining. First write `analyze.sh` as a plain script that reads `/home/user/raw_logs.txt` line by line, splits each line on the first `:` into LogID and the base64 payload, decodes with `base64 -d`, then checks the decoded string against the XSS markers (`<script`, `javascript:`, `onerror=`) and SQLi markers (`UNION SELECT`, `' OR 1=1`) using case-sensitive substring matching (e.g. `grep -F` or bash `[[ "$decoded" == *pattern* ]]`), and appends one CSV line per log entry with no header. Handle payloads containing spaces correctly by quoting variables and reading with `IFS=` / `read -r`. Test `analyze.sh` standalone first and diff its output against your own hand-decoded expectations before touching isolation. Only after analyze.sh is verified correct, write `run_isolation.sh` that wraps it with `unshare` using both user-namespace and network-isolation flags (e.g. `unshare --user --map-root-user --net -- bash /home/user/analyze.sh`), and chmod both scripts executable. Finally run `run_isolation.sh` and confirm `/home/user/audit_trail.csv` was produced with no header and correct decoded text.
