Start by inspecting the schema (`sqlite3 /home/user/backups.db ".schema"`) to confirm table/column names before writing any query. To bypass the corrupted `idx_active` index, use SQLite's `NOT INDEXED` hint directly in the query, e.g. `SELECT src, dst FROM dependencies NOT INDEXED WHERE is_active = 1` — a plain WHERE clause risks the query planner silently choosing the stale index. Load the filtered rows with Python's `sqlite3` module, then build a `networkx.DiGraph()` adding an edge for every (src, dst) active dependency; compute `nx.in_degree_centrality(G)` over all server nodes (make sure every server from the `servers` table is added as a node even if it has no active edges, otherwise centrality/denominator will be wrong). Separately compute `total_backup_size` per server as its own `size_gb` plus the sum of `size_gb` for each direct dependency target (a simple dict lookup keyed by server id/name, not a graph traversal). Finally sort the result list by `in_degree_centrality` descending then `total_backup_size` descending, and dump to `/home/user/restore_priority.json` with the exact key names and structure specified.