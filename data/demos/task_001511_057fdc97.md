Work through the objectives in order since later steps depend on earlier fixes. First reproduce the crash: compile and run the original `/home/user/app/src/monitor.c` to see it segfault, then look at the `fopen()` call — it never checks whether the returned `FILE*` is NULL before writing/closing, and the target log directory doesn't exist yet. Fix it by creating `/home/user/app/logs` (e.g. with a `mkdir`-like syscall or shelling out) before opening the log, and add a NULL check that prints a clear error and exits gracefully instead of crashing if opening still fails; then compile to `/home/user/app/bin/monitor` (creating `bin/` first) and run it once to confirm `/home/user/app/logs/monitor.log` is created without a segfault. For the backup script, use `tar -czf` with a `$(date +%s)` timestamp in the filename, targeting `/home/user/data`, and `chmod +x` it. For restore, have the script take `$1` as the tarball path, clear `/home/user/data/*`, then `tar -xzf "$1" -C /home/user/data`. Add the cron entry with `crontab -e`-equivalent (e.g. `(crontab -l; echo "0 * * * * /home/user/app/scripts/backup.sh") | crontab -`). Finally write the logrotate config with `daily`, `rotate 3`, `compress`, and `missingok` directives targeting the monitor log path exactly.