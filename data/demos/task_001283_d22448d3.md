Fix the three layers in build order, verifying each with a compile/test before moving to the next. First, `rust_interner/Cargo.toml` is missing `crate-type` under `[lib]` — the C Makefile links a static lib, so add `crate-type = ["staticlib"]` (and `name` if needed) so `cargo build` actually produces `librust_interner.a`. Second, run `cargo build` and read the compiler error in `intern`: it's inserting a `&str` into a `HashMap<String, usize>` — fix by converting with `.to_string()` or `.to_owned()` before `insert`. Third, once it compiles and `make` links `test_runner`, the C test still leaks: `interner_free` calls `std::ptr::drop_in_place(ptr)` which runs the destructor but never deallocates the `Box`'s heap allocation — replace it with reconstructing and dropping the box, e.g. `unsafe { drop(Box::from_raw(ptr)); }`, which frees the allocation correctly. After all three fixes, rebuild with `make`, install `valgrind` if missing, run `valgrind --leak-check=full ./test_runner 2>&1 | tee /home/user/valgrind_report.txt`, and confirm the report shows zero bytes definitely lost before considering the task done.