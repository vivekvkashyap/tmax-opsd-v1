Parse the binary format field-by-field with `fread`, being careful with the two length-prefixed strings: the filename is NOT null-terminated in the file, so read it into a buffer sized `len+1` and manually set the last byte to `\0` before treating it as a C string. Check maliciousness with `strstr(name, "../") != NULL || name[0] == '/'` — check this before any filesystem write. For legitimate files, recursively create parent directories under `/home/user/extracted/` (e.g. build the directory path and shell out to `mkdir -p` on it, or walk-and-mkdir component by component) before writing the file bytes exactly as read (use the file-size field, not string length, and use `fwrite` for exact byte counts to handle non-text data safely). For malicious ones, append `[THREAT] <name> : <size>` lines to the log without extracting. Finally, generate the CSV with a single `awk`/`sed` pass that strips the `[THREAT] ` prefix and turns ` : ` into `,` — e.g. `sed 's/^\[THREAT\] //; s/ : /,/' malicious.log > threat_report.csv`. Verify extracted file contents and sizes byte-for-byte against the archive.
