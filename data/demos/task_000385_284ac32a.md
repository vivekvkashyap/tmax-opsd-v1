Work the three phases in order, verifying each before moving to the next. First, fix the environment: run the script, read the traceback (likely a broken/misspelled dependency in requirements.txt causing an ImportError), correct the requirements file and any matching bad import statement in monitor.py so the script actually executes. Second, do the bisection properly: `cd` into the repo, run `git bisect start`, mark `main` as bad and `v1.0-stable` as good, then write a small test script that checks out-of-range SLA output (>100%) or directly inspects the `calculate_sla` formula/behavior at each candidate commit, and drive it with `git bisect run <script>` — do not guess the commit by manual inspection alone, let bisect converge and capture the resulting commit hash via `git rev-parse` into `/home/user/bad_commit.txt` (full 40-char hash, on its own, no trailing junk). Third, after bisect finishes (`git bisect reset` back to main), edit `calculate_sla` on main to implement `(total_time - downtime) / total_time * 100.0` — compare current code carefully against this exact formula rather than assuming which operator is wrong. Finally run monitor.py against the given log path/output path and confirm the JSON report is produced and looks sane before finishing.
