Set up the environment first and confirm it works before writing pipeline logic: `python3 -m venv /home/user/etl_env`, activate it, `pip install pandas "scikit-learn==1.3.0"`, and always invoke your script through that venv's interpreter. For schema enforcement, apply each rule as an explicit, separately-testable filter on `raw_data.csv` — non-null `user_id`; numeric `age` coerced with `pd.to_numeric(errors='coerce')` then bounded `[18,100]`; numeric `annual_income` coerced then `> 0`; `profession` restricted to the exact allowed set via `.isin([...])` — and count drops by comparing `len()` before and after each stage (or before/after the combined filter) so `invalid_rows_dropped` and `valid_rows_trained` are consistent and sum to the original row count. Extract `age` and `annual_income` in that exact column order as the feature matrix for `IsolationForest(random_state=42, contamination=0.1)`, fit on the valid historical rows, then `.predict()` on `new_batch.csv`'s same two features without applying any schema dropping to it. Count predictions equal to `-1` for the anomaly count. Write the three integer fields to `/home/user/pipeline_results.json` with `json.dump`, and sanity-check the JSON is well-formed and keys match exactly.
