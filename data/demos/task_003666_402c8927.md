Treat this as two separable bugs, not one. First reproduce the panic: read the frame parser in the vendored `fastmath` package under `/app/vendor/fastmath` — look for code that reads a length byte `N` then slices the buffer to `N` bytes without checking `len(remaining) >= N`; fix it to return an explicit error (don't just recover() and swallow it) when the buffer is too short, and make the caller in `main.go` catch that specific error and emit `ERR_CORRUPT\n` for that frame while continuing the stream — don't let one bad frame stop processing. Second, find the shared/global state used during concurrent evaluation (look for a package-level map or cache accessed from goroutines) and protect it with a `sync.Mutex`/`sync.RWMutex`, or eliminate the shared cache entirely if it's unnecessary — a `go test -race` or manual review of concurrent map access will reveal it. Since output must preserve input order despite parallel evaluation, use a pattern like a worker pool that returns results tagged with sequence index, or per-frame goroutines writing into a pre-sized results slice, then print sequentially after collecting rather than printing directly from goroutines. Build with the local vendor dir only (no network) and test by piping crafted valid+truncated frames through the compiled `/home/user/poly-eval-fixed`.
