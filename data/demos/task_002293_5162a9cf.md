The whole task hinges on correctly scoping the `StandardScaler.fit()` call — this is the classic train/test leakage bug, so isolate it deliberately: build the boolean mask `split == 'train'`, call `scaler.fit(X[train_mask])` on the 7-column feature matrix, then `scaler.transform(X)` on the full dataset (both splits), never `fit_transform` on everything. For audio features, load each `audio_path` with `librosa.load(path, sr=22050)`, compute `librosa.feature.mfcc(y=y, sr=22050, n_mfcc=5, n_fft=2048, hop_length=512)`, and take `np.mean(..., axis=1)` to collapse the time axis into 5 scalars per file — use these exact parameter names/values since any deviation changes the numeric output and the grader requires bit-exact match against a hidden oracle. Assemble the feature vector in the exact order `[f1, f2, mfcc0..mfcc4]`, scale, then write output columns in the exact order `id,split,f1,f2,mfcc0,mfcc1,mfcc2,mfcc3,mfcc4`, sorted by `id` ascending, every float formatted to exactly 4 decimal places (e.g. via an explicit `"{:.4f}".format` rather than pandas' default float formatting, which can differ). Test locally against `/app/sample.wav` with a small hand-built CSV before trusting the pipeline, since grading is via output-equivalence fuzzing across many random inputs.
