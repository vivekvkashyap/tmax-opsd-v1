Load `density` from `/home/user/input_pdf.h5` with `h5py`, then use `scipy.interpolate.interp1d(z_old, density, axis=2, kind='linear')` (with `z_old = np.linspace(0,1,50)`) evaluated at `z_new = np.linspace(0,1,99)` to refine along the Z-axis in one vectorized call — passing `axis=2` is what avoids looping over X and Y, since interp1d broadcasts over the other axes automatically. After getting the refined `(50,50,99)` array, integrate along that same axis with `numpy.trapz(density_refined, x=z_new, axis=2)`, which collapses it to `(50,50)`. Double check the axis argument matches on both the interpolation and the trapezoidal step — a mismatched axis is the most common way to silently produce a wrong-shaped or wrong-valued result here. Write the result to `/home/user/marginal_xy.h5` under dataset name `marginal` using `h5py.File(...,'w').create_dataset('marginal', data=result)`. Before finishing, sanity-check the output shape is exactly `(50, 50)` and that no explicit Python-level loop over the spatial grid was used anywhere in the script.