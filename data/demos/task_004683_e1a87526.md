Treat this as black-box binary reverse engineering, not guesswork. First run `file`, `strings -n 6 /app/doc_compiler`, and `objdump -d` or `strace -f` on it to see what it touches (config file, syscalls). Then build small, controlled `.tdoc` test files byte-by-byte with a script (e.g. Python `struct`/`open('wb')`) rather than by hand, starting with a minimal valid file (magic bytes + one record + terminator) and confirm the binary accepts it and produces output. Vary one thing at a time: flip a byte in the magic header to find the exact required signature and the exact error text/exit code; try every record-type byte to enumerate valid types and their rendering; test truncated files (cut off mid-record and after a valid record but before terminator) to capture the "unexpected end" case; and toggle each option in `compiler_config.ini` to see which settings change output. Log every (input, config) -> (stdout, stderr, exit code, output file bytes) mapping in a table before writing any Python. Only after you have exhaustively confirmed the record format, per-type formatting, config-driven behavior, and every error path should you implement `/home/user/py_compiler.py` — mirror byte lengths, newline placement, and exit codes exactly, since the grader diffs raw bytes.
