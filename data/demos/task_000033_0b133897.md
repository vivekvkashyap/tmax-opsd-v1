First inspect the schema before writing any SQL: run `sqlite3 /home/user/compliance.db ".schema"` (or `.tables` then `.schema <table>`) to learn the real table/column names for employees, systems, and access events — don't guess names. Once you know the manager-reference column, write a recursive CTE (`WITH RECURSIVE subs AS (SELECT ... WHERE emp_id = :target UNION ALL SELECT e.* FROM personnel e JOIN subs ON e.manager_id = subs.emp_id) ...`) to collect the target and all transitive subordinates. Join that set against access events and the systems table, filtering to rows where the required clearance for the accessed system exceeds the employee's own clearance level. Bind the CLI argument as a parameter (e.g. `sqlite3 -cmd ".parameter set :target \"$1\""` or `.param set`, or build the query with `sqlite3 db "..." "$1"` using `?` placeholders via a here-doc with `.param`) rather than interpolating `$1` directly into the SQL string, to avoid injection. Order results by access date ascending then employee name ascending, and format as CSV with the exact header row via `.mode csv` / `.headers on` or manual `printf`. Test against both a manager with subordinates and a leaf employee to confirm the recursion and ordering are correct before finalizing.