Step 1: extract per-frame grayscale brightness from `/app/experiment_run.mp4` — e.g. use `ffmpeg` to scale each frame to 1x1 and dump pixel values (a common trick: `-vf scale=1:1 -f rawvideo -pix_fmt gray`), producing one intensity value per frame index in decode order; join these by index with `/app/telemetry.csv` (which is 0-indexed with a header) to build `brightness,temperature` rows. Step 2: implement `/home/user/feature_extractor.go` to read CSV lines from stdin, maintain a sliding window of exactly the last 10 rows, and starting at row 10 compute for the window: mean brightness, temperature's 95% CI bounds using sample stddev (n-1=9 denominator) and `1.96 * s/sqrt(10)`, and an anomaly flag comparing the *current* row's brightness against `mean_brightness + 1.5*s_brightness` (strictly greater). Match formulas exactly since output is checked by fuzzing against a hidden oracle for bit-level equivalence — implement variance/stddev carefully (population vs sample matters) and format each output line to exactly 4 decimal places for the three floats plus the integer flag. Compile to `/home/user/bin/feature_extractor`, then pipe your Step 1 joined data through it into `/home/user/training_features.csv`.