First fix the MPI bug: in `simulate.py`, find the `comm.reduce(...)` call combining each rank's inside-circle count — an MPI reduce with `op=MPI.MAX` only keeps one rank's count instead of summing all ranks, explaining the wrong Pi estimate; change it to `MPI.SUM`. Then build the orchestration script: use `subprocess` to run `mpirun -n 4 python .../simulate.py --samples 10000 --seed <i>` for each of the 50 run indices, capturing stdout and parsing the printed Pi estimate. For statistics, implement bootstrap resampling deterministically: seed the RNG with exactly 42 once, then for 10,000 iterations draw a size-50 sample with replacement from your 50 estimates and record its mean; take the 2.5th/97.5th percentiles of those bootstrap means as CI bounds, and the plain mean of the 50 estimates as `mean_estimate`. Match the exact library convention (e.g. `np.random.seed` + `np.random.choice`) since the check compares against a tightly-toleranced reference computed the same way.
