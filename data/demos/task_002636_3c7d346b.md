Scaffold with `cargo new citation_processor`, add `rusqlite` (with the `bundled` feature to avoid system sqlite issues) and `serde`/`serde_json` to Cargo.toml. Open the database read-only and load the `citations` edges into an adjacency list in memory, then run a plain BFS from id=1 to id=5 tracking parent pointers so you can reconstruct the actual shortest path (don't assume a specific path — compute it, and if multiple exist any shortest one is acceptable). For year_rank, push the ranking logic into SQL itself rather than reimplementing DENSE_RANK by hand: `SELECT id, title, year, score, DENSE_RANK() OVER (PARTITION BY year ORDER BY score DESC) as year_rank FROM papers`, and join that result set with your BFS path by id. Then reorder the joined rows to follow the exact path sequence (source to target), not database or SQL result order. Serialize with `serde_json` ensuring `score` stays a float (e.g. an `f64` field) and `id`/`year`/`year_rank` stay integers, then write the JSON array to the target path with `serde_json::to_writer` or `to_string_pretty`. Build and run with `cargo run` and inspect the output file afterward.
