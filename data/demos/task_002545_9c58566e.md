Build this as three layers that you can test independently before combining. (1) DB: create `/home/user/data.db` with the exact `records` schema given (id, category, description, metric, emb_0..emb_3) using `sqlite3`/`sqlalchemy`. (2) Validation + scoring: implement `POST /upload` (Flask/FastAPI) that parses the multipart CSV, and for every row apply the strict schema checks in order — id parses as int, category is one of the three exact literals, description non-empty, metric parses as float and is >= 0.0 — skipping (not erroring on) any row that fails any check. For rows that pass, shell out to `/app/scorer_bin` with the description as a single argv (quote it so multi-word descriptions survive as one argument, e.g. `subprocess.run(["/app/scorer_bin", description], capture_output=True)`), parse its four space-separated floats from stdout, and insert everything into SQLite. (3) Retrieval: `GET /record/<id>` looks up by primary key and returns the exact JSON shape on 200, or `{"error": "Not found"}` with 404 otherwise. Start the server backgrounded (e.g. `nohup ... &`) on `127.0.0.1:5000` and smoke-test with curl before finishing.
