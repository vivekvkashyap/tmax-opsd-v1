Start by running the script and capturing the traceback to confirm the failure mode (FileNotFoundError on the intermediate manifest file), then read the code around the cleanup thread's age check rather than guessing. The key technique: reproduce the race deterministically by running the script several times and/or adding temporary print statements showing the computed file age each run — since the bug is a timezone bug, not just a race, it will trigger consistently once you notice the effective process timezone. Look specifically for any place a "local" time value produced by `datetime.fromtimestamp()`/`os.path.getmtime()` is compared against a value produced by `datetime.utcnow()` (or vice versa) — mixing naive local and naive UTC clocks makes a brand-new file look artificially old (or new) by exactly the UTC offset, which under a non-UTC TZ can push a fresh file's "age" over the cleanup threshold. Fix it by making both sides of the age comparison use the same clock consistently (either both UTC via `datetime.utcfromtimestamp()`/`datetime.utcnow()`, or both local via `datetime.fromtimestamp()`/`datetime.now()`). After patching, rerun `python3 /home/user/project/build.py` multiple times to confirm exit code 0 and that `/home/user/project/build_artifact.txt` is produced consistently, not just on lucky timing.
