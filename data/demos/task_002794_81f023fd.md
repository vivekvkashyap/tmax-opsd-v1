Process the JSONL in clear stages rather than one messy pass. Timestamp parsing: use `datetime.fromisoformat` after normalizing quirks (replace a trailing `Z` with `+00:00` since Python's isoformat parser doesn't accept bare `Z`, and the format also includes offsets like `-04:00` and space-separated dates) then convert to UTC with `.astimezone(timezone.utc)` and format with `strftime('%Y-%m-%dT%H:%M:%SZ')`. Deduplicate using a `set` keyed on `(utc_timestamp_string, msg)`, keeping only the first occurrence in original file order — do this before filtering/sorting so duplicates removed are judged on identical (ts, msg) pairs, not on any derived field. Filter by checking the literal Unicode substring for "failure" is present in `msg` (compare code points, don't ASCII-encode). Sort remaining records by `(utc_time, msg)` ascending. Compute the rolling average over the sorted sequence using a sliding window of the current plus up to 2 previous records (i.e. `mean(vals[max(0,i-2):i+1])`), rounding to exactly 2 decimals with proper rounding (e.g. `round(x, 2)` or format string `%.2f`), and write the CSV with the exact header and column order given, ensuring UTF-8 encoding so the source-language text round-trips unmangled.