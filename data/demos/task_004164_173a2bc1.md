Treat the four parts as independent deliverables that only converge in the final orchestrator. Run `tesseract /app/architecture.png stdout` and parse every line matching the `ModuleA -> ModuleB` pattern into `(dependent, dependency)` edges — remember the arrow means the left module depends on the right one, so the right one must be built first; don't hand-guess the module names, read them from the OCR text. For `libgraph.c`/`libgraph.so`, implement a standard topological sort (Kahn's algorithm with an in-degree queue, or DFS with post-order + reverse) over a small graph representation (arrays of edges, or adjacency lists) and expose a C function with a signature simple enough to call from Python's `ctypes` (e.g. pass module count, an edges array, and an output buffer pointer, or write results into a caller-provided array) — compile with `gcc -shared -fPIC -o libgraph.so libgraph.c`. For `sanitizer.py`, do static text/pattern matching over the script contents flagging dangerous commands (network tools like curl/wget, destructive deletes like `rm -rf`, and writes targeting `/etc` or `/` or other system paths) case- and whitespace-insensitively, exiting 1 on any match and 0 otherwise, and test it against both corpora before trusting it. Finally wire `build_system.py` to load the OCR'd edges, call into `libgraph.so` via `ctypes.CDLL`, and write the resulting valid build order (any correct topological order) comma-separated to `/home/user/build_order.txt`.