Fix the build before touching logic: run `go mod init artifact-builder` if `go.mod` is missing/broken, then `go mod tidy`/`go get` for any test deps, and inspect `build.sh`'s `CGO_LDFLAGS`/`CGO_CFLAGS` — a broken CGO link usually means the flags don't point `-l`/`-L` at where `libextractor.c` actually compiles to, or `CGO_ENABLED` isn't set; fix incrementally and confirm with `go build` before moving on. For the C bug, look specifically at the frame-header parsing loop in `libextractor.c` for an off-by-one such as `for(int i=0; i<=len; i++)` writing one past a buffer's end — change the boundary condition, then stress-test the extractor against `/app/security_feed.mp4` to confirm it no longer segfaults. Once `raw_events.json` is produced, write `migrate/main.go` as a straightforward, deterministic transform: parse the V1 array, base64-decode `raw_data` to bytes and hex-encode them (lowercase, no `0x` prefix — check convention against how downstream JSON typically renders hex), truncate (not round) `timestamp` seconds to milliseconds via integer math, then sort by `ts_ms` ascending with `id` as the tiebreaker using Go's `sort.Slice`. Because this will be fuzz-tested for byte-exact equivalence against a hidden oracle, pay close attention to JSON key order/formatting and edge cases like empty input arrays.