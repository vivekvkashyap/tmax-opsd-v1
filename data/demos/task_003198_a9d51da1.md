Work this incident in strict stages, verifying each before moving on. First, run OCR (`tesseract /app/incident_screenshot.png stdout` or similar) on the screenshot and read off the passphrase it reveals — don't guess or hardcode a phrase, use exactly what OCR recovers (re-run with different preprocessing/psm modes if the first pass looks garbled). Use that recovered passphrase to decrypt the GPG archive (`gpg --batch --passphrase <recovered> -d corpus.tar.gz.gpg | tar xz -C /home/user/corpus/`) and confirm `clean/` and `evil/` subdirectories appear. Second, reverse-engineer `/app/legacy_deployer` with `strings`, `objdump -d`, or `nm` to find the literal flag strings it recognizes for sensitive values — look for argument-parsing patterns (comparisons against flag literals) rather than assuming standard flag names; there are exactly three, and one uses `=`-attached syntax while the others take the next argv token. Third, write `redactor.c` to tokenize stdin on whitespace, detect each of the three flags (both "next token" and "flag=value" forms), replace only the matched value with `[REDACTED]`, and print everything else byte-for-byte unchanged — test it manually against a few clean and evil sample lines before trusting it against the full corpus, since accidental redaction of look-alike tokens (e.g. `-K-not-the-key`) is the classic failure mode.
