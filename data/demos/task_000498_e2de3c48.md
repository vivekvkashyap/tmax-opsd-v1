Get correctness working sequentially first, then parallelize — don't try to optimize and validate simultaneously. First study `/app/baseline_inference.py`'s `process()` function closely: it hashes the exact image file's bytes to seed a deterministic RNG, so your `object_count` will only match if you extract frames byte-for-byte comparably (same codec/format/quality as whatever the baseline expects) — reuse the baseline's `process` function directly via import rather than reimplementing the math, to guarantee identical results. Extract frames at exactly 1 fps (e.g. `ffmpeg -i <video> -vf fps=1 frame_%04d.png`, with frame N's timestamp being N-1 seconds, or frame index directly as the second). Then parallelize the per-frame inference call using `multiprocessing.Pool.map` (or `concurrent.futures.ProcessPoolExecutor`) across CPU cores — this is where your speedup comes from, since the baseline is single-threaded and each inference call is a heavy CPU-bound matrix multiply; keep frame extraction itself lightweight so it doesn't dominate. Merge the per-frame `(timestamp, object_count)` results with `/app/telemetry.csv` on `timestamp`, sort ascending, and write columns in the exact order `timestamp,object_count,speed,lat,lon` to the given output path. Benchmark against a sequential loop calling the baseline script per-frame to confirm you're meaningfully faster (target speedup ≥2x) before considering it done.
