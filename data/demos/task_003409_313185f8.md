Work through this bottom-up rather than jumping straight to starting supervisord. First fix the filesystem layer: `mkdir -p /home/user/real_conf /home/user/real_logs`, then `ln -s` them as `conf` and `logs` inside `/home/user/service/` — verify with `readlink` before moving on, since the script checks `os.path.exists` on those exact paths. Second, inspect `netmon.py --setup` yourself (read the source) to see the exact prompt strings and required PIN, then write an `expect` script using `spawn`/`expect`/`send` blocks matching those prompts verbatim (trailing colons and spacing matter for expect's pattern match); run it with `expect /home/user/setup_netmon.exp` and confirm the conf file was actually written before proceeding. Third, edit `supervisord.conf`'s `[program:netmon]` block, setting `command=python3 /home/user/service/netmon.py --serve` (install supervisor/expect first if missing). Start supervisord with `-c`, then poll `supervisorctl status` (or the socket) until `netmon` shows RUNNING rather than assuming success immediately. Only after confirming `logs/status.log` contains `SERVICE_ONLINE` should you write `/home/user/success.txt`.
