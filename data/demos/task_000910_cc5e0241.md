First extract the exact policy text from `/app/policy.png` (view/OCR the image) — read the ordered checks and required output format precisely, since the grading is a strict stdout diff against a reference and any wording/formatting deviation will fail. Implement the checks in `/home/user/parse_elf.sh` using only `readelf`/`file`/coreutils, evaluated in the image's specified order with early exit on the first failing check (e.g. run `readelf -h "$1"` and check its exit status/output first to detect non-ELF files before inspecting architecture, then check the `Machine:` line for the exact x86-64 string, then check `Type:` for EXEC/DYN, then check `readelf -S` for a section named exactly `.rodata`, being careful with whitespace/anchoring in your grep since section names can be substrings of others). Only if every check passes should you print the success line with the entry point address formatted exactly as `readelf -h` reports it (e.g. `0x401000`). Test against real files you can construct locally (compile trivial x86-64 binaries with and without `.rodata`, a stripped/relocatable object with `-c`, plain text files, and if a cross-compiler is available, a non-x86-64 ELF) to exercise every branch, not just the happy path. Ensure the script takes exactly one argument and is `chmod +x`.
