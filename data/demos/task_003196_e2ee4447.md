Chain four distinct sub-problems, verifying each stage's output before moving to the next. (1) OCR `/app/incident_memo.png` with `tesseract` and extract the `NODE_ID` following `AUDIT_ROOT:` from the recognized text — print the raw OCR output first to confirm it read cleanly before parsing it. (2) Using `jq`/`awk`/`sqlite3` on `/app/network_traffic.csv` and `/app/node_metadata.json`, find all 2-hop paths: first find intermediate nodes `Y` reached from the extracted root via `protocol=HTTPS`, then find all `Z` reached from any of those `Y` via `protocol=SCP`, summing `bytes_transferred` across *all* matching Y->Z SCP edges (not just one) grouped by `Z`. (3) Join each `Z`'s total against `node_metadata.json` to attach its `department`. (4) Sort the results descending by total bytes and write a minimal raw-socket HTTP server in Bash (`nc -l` in a loop, or `socat`) at `/home/user/serve_report.sh` that binds `127.0.0.1:8080`, parses incoming requests for `GET /audit-report`, and responds with a correct `HTTP/1.1 200 OK` status line, a `Content-Type: application/json` header, a blank line, then the JSON array body — recompute or cache the JSON so the response is well-formed on every request, and start the server in the background (looping to accept repeated connections) before finishing.
