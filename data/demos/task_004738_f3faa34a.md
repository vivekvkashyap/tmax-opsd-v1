Work stage by stage. First locate the bug: open `/app/text-pipeline-server-1.0/text_pipeline/parser.py` and find the regex validating `X-Batch-ID` — compare it carefully against the required format `batch-<digits>-<word>` (e.g. is it matching digits-then-letters when the spec wants letters-then-digits, or vice versa?); fix only the pattern, then `pip install -e /app/text-pipeline-server-1.0` so the fix takes effect for the running server. Second, implement `process_logs` in `/home/user/etl.py`: use a single regex with named groups to extract timestamp/level/message per line, skipping non-matching lines entirely (don't try to partially salvage malformed lines); lowercase and strip non-alphanumeric/non-space chars from the message for cleaning. For deduplication, compute each cleaned message's word set, compare every pair with Jaccard similarity (|intersection|/|union|) using a 0.75 threshold, group transitively-similar entries, and within each group keep only the earliest timestamp — do this after cleaning, not before. Finally start the server exactly as specified (`text-pipeline-server --host 127.0.0.1 --port 8080 --handler /home/user/etl.py:process_logs`) and leave it running so the automated HTTP verifier can POST to `/ingest`.
