Start by running `tesseract /app/schema_guide.png -` (or `tesseract /app/schema_guide.png stdout`) to OCR the schema image and capture the exact numeric thresholds and rules it specifies — do not guess at the rules from the task description alone. Then design `/home/user/filter_data.sh` as a short-circuit validator: parse the single CSV row's four fields with awk, and for the user_id field use a strict regex check like `[[ "$id" =~ ^-?[0-9]+$ ]]` (not a numeric comparison) so that values like `1001.0` or `NaN` are rejected outright rather than silently truncated/coerced by arithmetic context — this is the classic naive-attempt failure mode. Join against `/app/db/users.csv` with awk/grep on user_id to fetch status and require it equals the exact value the image specifies. Compute the vector's L2 norm with awk (`sqrt(x*x+y*y)`) and compare against the two bounds from the image using strict inequality as stated. Exit 1 immediately on the first failing rule, exit 0 only if every rule passes. Test your script manually against a few constructed clean and corrupted rows before relying on it.