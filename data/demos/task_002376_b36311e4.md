Write `/home/user/validate_stability.sh` as a self-contained orchestration script: create the venv with `python3 -m venv /home/user/venv`, activate it (`source /home/user/venv/bin/activate`) and `pip install numpy` inside it — the common mistake is running `find_peak.py` with the system `python3` instead of `/home/user/venv/bin/python3`, which breaks if numpy isn't globally installed. Loop `for seed in 1 2 3 4 5; do ... done`, calling `./run_sim.sh "$seed"` (with `cd` into `/home/user/sim_env` or absolute paths) then capturing the printed frequency from `/home/user/venv/bin/python3 find_peak.py` (redirect stderr away from the captured value, and strip whitespace/newlines) into an array or running sum. Average the five captured numbers using `awk` (e.g. pipe them one-per-line into `awk '{s+=$1; n++} END{printf "%.3f", s/n}'`) rather than integer bash arithmetic, since the values are floats. Write the final line to `/home/user/stability_report.txt` in the exact literal format `Average Frequency: X.XXX Hz` — double check there's no extra whitespace or missing newline that would break an exact-match grep. Finally, actually execute the script you wrote, don't just leave it unrun.
