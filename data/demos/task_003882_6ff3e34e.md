Since you lack root, fetch and extract Eigen locally first (`wget`/`curl` the tarball into `/home/user/eigen`, `tar xzf`) and confirm the include path works with a trivial test compile before writing the real program. Structure the C++ program in the exact order the task implies: (1) parse the CSV, treating both literal `NaN` tokens and values with `|x| > 100.0` as missing — mark them, don't drop the row; (2) per column, compute the mean over only the valid entries and substitute it for every missing/outlier cell in that column; (3) subtract the (post-imputation) column means so each column has mean 0 — recompute means after imputation, don't reuse the pre-imputation mean; (4) build the 3x3 sample covariance matrix with Eigen (divide by N-1) and use `Eigen::SelfAdjointEigenSolver` since the covariance matrix is symmetric — this gives eigenvalues already sorted ascending, so the top PC is the last column of the eigenvector matrix; (5) fix sign ambiguity by negating the whole eigenvector if its first component is negative. Format the 3 output components to exactly 4 decimal places with `std::fixed << std::setprecision(4)` and write comma-separated to `/home/user/top_pc.txt`. Double check your imputation happened before centering/covariance, not after — reordering these steps is the most common way to get a subtly wrong answer.
