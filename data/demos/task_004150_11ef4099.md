Build the index first, then the query script, testing each with `jq` at every step rather than assuming the JSON shape. For `build_index.sh`: loop over `/home/user/backups/*.json`, use `jq -r '.timestamp, .id'` (or a combined `jq -r '"\(.timestamp)-\(.id)"'`) to derive each symlink's name, and `ln -s` (relative path back into `../backups/<file>.json`) into `/home/user/backup_index/`, creating that directory first with `mkdir -p`. For `get_chain.sh <timestamp>`: list the index directory, extract the leading timestamp from each symlink name, filter for those <= the argument, and pick the max (sort numerically, e.g. `sort -n | tail -1`, being careful not to sort lexicographically since that breaks on differing digit counts) to find the starting backup. From that backup's JSON, read `parent_id` and walk backwards — `jq` each linked file in turn — until you hit a record with `type == "full"` (parent_id null), collecting the chain of ids/types/files as you go, then reverse the collected list so it reads full-first, and emit it with `jq -n --argjson chain '[...]' '$chain'` (or build the array directly in jq) containing only the `id`, `type`, `file` keys per object. Validate the final output with `jq -e` against the expected shape before finishing, and `chmod +x` both scripts.
