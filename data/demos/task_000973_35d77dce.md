Do the three stages in strict order since each depends on the previous one's output. First, write a Python script that lists files in `/home/user/raw_data/`, computes each new name by lowercasing the full filename and replacing both spaces and underscores with hyphens (apply this transformation to the whole string including the extension), then renames in place with `os.rename` — watch for potential name collisions after normalization and process files individually rather than assuming names stay unique by coincidence. Second, after renaming is complete, generate the checksum manifest by running `sha256sum` against the renamed files from within `/home/user/raw_data/` (so only basenames appear, not full paths) and redirect to `/home/user/checksums.sha256` — don't hand-roll SHA-256 formatting, use the real `sha256sum` tool so the two-space-separated format matches exactly. Third, `tar -czf /home/user/secure_backup.tar.gz -C /home/user raw_data checksums.sha256`-style invocation (adjust paths so `tar -tzf` shows `raw_data/...` and `checksums.sha256` at the archive root) — verify with `tar -tzf` afterward. Clean up any scratch files you created along the way.
