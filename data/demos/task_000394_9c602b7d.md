The bug is that `TfidfVectorizer.fit_transform` and `PCA.fit_transform` are both called on the *full* dataset before the train/test split, so the test set's statistics leak into the vectorizer's vocabulary/IDF weights and the PCA components. Fix the ordering: call `train_test_split` on the raw text/labels first (`test_size=0.3, random_state=42`), then `fit_transform` the vectorizer only on `X_train` and `transform` (not fit) `X_test`, then likewise `fit_transform` PCA only on the training TF-IDF vectors and `transform` the test vectors with the same fitted PCA object — never call `.fit` or `.fit_transform` on anything derived from `X_test`. Keep `LogisticRegression(random_state=42)` and all provided random seeds untouched so results stay reproducible. After fitting on train and predicting on test, write `/home/user/metrics.json` with `test_accuracy` as a float from `accuracy_score` and `first_5_predictions` as `preds[:5].tolist()` (cast numpy ints to plain Python ints/JSON-serializable types).