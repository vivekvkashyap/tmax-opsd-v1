Run `tesseract /app/instructions.png stdout` to recover the exact `TARGET`, `REPLACE`, and `PREFIX` values — don't assume placeholder-like names, read what OCR actually returns (retry with `--psm 6` if the output looks noisy). For each `.log` file in `/home/user/logs/`, use `sed -i "s/TARGET/REPLACE/g" file` (with the real recovered strings) to substitute, then rename each modified file by prepending PREFIX (`mv file "${dir}/${PREFIX}${base}"`). Archive the renamed files with maximum gzip compression specifically — plain `tar -czf` uses gzip's default level, so instead pipe through `tar -cf - files | gzip -9 > /home/user/logs/archive.tar.gz` (or `GZIP=-9 tar -czf ...` depending on your tar/gzip) to actually hit level 9, since the verifier checks a tight byte-size threshold that default compression may miss. Create the symlink with `ln -s /home/user/logs/archive.tar.gz /home/user/latest_archive`. Double check the archive only contains the renamed/sanitized files (no leftover originals or temp files bloating it) and verify its size with `stat -c%s` before finishing.