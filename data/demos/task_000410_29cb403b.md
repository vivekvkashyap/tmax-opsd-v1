This task has two independent parts — do them separately and verify each on its own.

Part 1 (vendored fix): Open `/app/cred-rotator/rotator/cli.py` and find the `subprocess.run` call that passes the master key as a positional argument to `bin/generate_key.sh`. Remove the secret from the `args` list, and instead pass it via `env=`, e.g. `subprocess.run(['/app/cred-rotator/bin/generate_key.sh'], env={**os.environ, "ROTATOR_MASTER_KEY": master_key})`. Then edit `bin/generate_key.sh` to read `$ROTATOR_MASTER_KEY` instead of `$1` wherever it previously referenced the positional arg. Confirm the fix by mocking/wrapping `subprocess.run` (or inspecting `ps`/`/proc` during a call) to check the secret never appears in argv, while the script still produces the same derived key as before.

Part 2 (algorithm replication): Do not try to disassemble the oracle — instead reason from the described algorithm and validate empirically. Write `token_generator.py` computing `sha256(f"{session_id}|{client_ip}".encode())`, XOR the digest's first 16 bytes with its last 16 bytes, then output `hex(xor_result) + hex(second_half)` (64 hex chars total). Test it yourself against `/opt/oracles/token_oracle` on several sample inputs to confirm bit-exact match before finishing — this catches subtle byte-order or slicing mistakes immediately rather than after the fuzz suite runs.
