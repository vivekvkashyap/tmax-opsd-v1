Build this as a bash orchestrator calling out to `ffmpeg` and a Python helper, keeping each stage independently testable. In the bash script, recreate `<output_dir>` and `<output_dir>/chunks` cleanly (remove-then-mkdir, or mkdir -p plus rm of prior contents) before chunking, then use `ffmpeg -i <input> -f segment -segment_time 1 -c copy` (or re-encoding equivalent) with an output pattern like `chunk_%02d.wav` to get exact 1-second, zero-padded, non-overlapping segments — verify the segment boundaries land on exact 1-second marks since `-c copy` can sometimes misalign on non-keyframe boundaries for compressed formats (safer to decode/re-encode PCM WAV). In Python, for each chunk read all 16,000 samples, slice into 10 contiguous windows of 1,600 samples each (no overlap, no gaps), and compute the mean of absolute values per window to build the 10-dim feature vector; stack all chunks' vectors into an N×10 matrix and use `numpy.corrcoef` (rows are chunks) to get the N×N Pearson matrix directly, rather than computing pairwise correlations manually. Write it to `correlation.csv` as comma-separated floats formatted to exactly 6 decimals with no header/index (`numpy.savetxt` with `fmt='%.6f'` works). For `similar.txt`, sort chunk_00's correlation row (excluding self) by value descending, breaking ties by ascending chunk index, and take the top 3 indices.