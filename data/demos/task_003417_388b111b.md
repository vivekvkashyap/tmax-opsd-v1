Work through this pipeline sequentially, validating each stage's output before feeding it to the next. Compile with `gcc /app/seq_stats.c -o /app/seq_stats`, then run it against `/app/reads.fasta` and redirect stdout to `/app/gc_data.txt` — inspect the file to confirm one GC fraction per FASTA record. For the image, run `tesseract /app/gel_band.png stdout` (or via `pytesseract`) and parse the OCR text with a regex like `PRIOR_MU=([\d.]+).*PRIOR_SIGMA=([\d.]+)` rather than assuming any particular values — OCR output can have spacing/noise so validate the parsed floats look sane before using them. For the MCMC sampler, follow the exact spec: seed `numpy.random.seed(42)` immediately before the loop, and within each iteration draw the proposal first then the uniform acceptance draw (order matters for reproducibility), use a Gaussian likelihood with sigma=0.05 per data point and the extracted Gaussian prior, accept via the log-density-ratio Metropolis rule, discard the first 1000 of 10000 samples as burn-in, and average the remaining 9000. Finally expose `/prior_params` (JSON) and `/posterior_mean` (plain text, 4 decimals) on `127.0.0.1:9090` via `http.server`, and keep the process alive/backgrounded so the verifier can hit it.
