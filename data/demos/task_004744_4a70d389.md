Split this into two independent fixes before touching migration logic. First, diagnose the build failure directly: run `./build.sh`, read the compiler error, and check the `--std=` flag against what the bundled `include/json.hpp` actually requires (a single-header JSON library targeting C++17 features will fail loudly under `--std=c++14` — inspect the header or the error message rather than guessing). Second, for the migration rules, use `ffmpeg -i /app/migration_rules.mp4 -vf fps=1 frames/frame_%03d.png` (or similar) to extract one frame per second, then read each extracted PNG image yourself (view/OCR the frames) to recover the exact V1→V2 field-mapping rules shown as on-screen text — do not assume a mapping, transcribe what the frames actually say. Implement `src/migration.cpp` to read one line of V1 JSON from stdin, apply each mapping/type-conversion exactly as read from the frames (including how the boolean and nested-object conversions are defined), drop unlisted fields, emit one line of V2 JSON to stdout, and exit 0. Rebuild with `./build.sh` and manually pipe a few hand-crafted V1 JSON lines through `build/processor` to confirm the mapping before considering the task done.
