Parse the CSV with Python's `csv` module and build the directed graph as an adjacency dict mapping each delegator to the list/set of its delegatees, while also collecting every distinct name seen in either column into a set for the unique-user count. For the shortest path, don't assume a naive DFS or hand-picked route — implement a proper breadth-first search from the source node, tracking each node's predecessor as you discover it, so the first time you reach the target you can reconstruct the guaranteed-shortest path by walking predecessors back to the source and reversing; BFS guarantees minimum edge count where a plain DFS would not. Handle the case where multiple shortest paths of equal length exist by just taking whichever BFS finds first (the task allows any of them) — don't try to enumerate all shortest paths. Write the result to `/home/user/audit_report.json` using `json.dump` with exactly the two keys specified (`total_unique_users`, `shortest_path`) and the path as a list of user-name strings starting with the source and ending with the target. Run the script and open the JSON to sanity-check the path is actually connected edge-by-edge against the CSV before finishing.
