Build the service as a real HTTP server (Flask/http.server/FastAPI) bound to 127.0.0.1:8080 with a POST /upload_project handler, and run it in the background so it stays reachable for testing. For zip-slip protection, before extracting anything, iterate every `ZipInfo.filename` in the archive and reject the whole request (400, exact JSON `{"error": "Invalid archive"}`, no files written) if any resolved path escapes the target extraction directory — compute this with `os.path.realpath(os.path.join(target_dir, name))` and check it still starts with the target dir's real path (don't just string-match on `..`, since normalized/absolute paths can bypass naive checks). Only extract if safe, then read `manifest.json` explicitly decoding as `windows-1252` and re-encode/parse as UTF-8 JSON. Pipe the UTF-8 JSON string's bytes into `subprocess.run(["/app/metadata_signer"], input=..., capture_output=True)`, strip trailing newline from stdout, and return the manifest dict plus signature as JSON with 200. Test with a normal zip and one crafted with a `../` entry to confirm both paths behave correctly and no stray file appears outside the target dir.