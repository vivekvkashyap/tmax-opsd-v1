Follow the workflow steps strictly in order since each depends on the previous artifact existing. Apply the patch with `patch /home/user/app/src/libauth.c /home/user/app/patches/CVE-2024-9999.patch` (or `patch -p0`/`-p1` depending on the diff header paths — try both if one fails) and diff the file afterward to confirm the vulnerable `strcpy` call was actually replaced with a bounds-checked `strncpy`. Compile with `gcc -shared -fPIC -o /home/user/app/build/libauth.so.1.0.1 src/libauth.c`, then create the symlink with `ln -sf libauth.so.1.0.1 /home/user/app/build/libauth.so` (relative target, created from inside the build directory, so the link stays valid). For the manifest updater, implement a simple tuple-based semver comparison — split each version string on `.`, cast parts to int, and compare tuples (`(1,0,1) > (1,0,0)`) rather than doing naive string comparison, which breaks on multi-digit version segments; only write the file back if the new version is strictly greater. Run `update_manifest.py` and confirm the JSON was actually rewritten. Finally run `e2e_test.py` and check it exits cleanly without segfaulting (this is the real regression test for the patch) and that `/home/user/app/test_results.log` contains the expected PASS line.
