Write /home/user/clean_pipeline.py as a sequence of explicit, loggable pandas steps, computing and printing/logging the counts at each stage rather than hardcoding numbers. Load raw_data.csv, record the initial row count. For dedup, build a per-row hash via `hashlib.md5((str(id)+user_name+email).encode()).hexdigest()` and use `df.drop_duplicates(subset='hash_col', keep='first')` to preserve original ordering while dropping later duplicates; record how many rows were dropped. Fix the mojibake by re-encoding: the string was UTF-8 bytes wrongly decoded as latin-1/cp1252, so recover it with something like `user_name.encode('latin1').decode('utf-8')` (wrap in try/except per-row in case some names are already clean). Sort by id ascending, then use `df['score'].interpolate(method='linear')` and count how many NaNs were filled (NaN count before minus after) for the log line. Extract email_domain via `email.str.split('@').str[1]`. Write the log file with the four exact `[INFO] ...` lines using the real computed integers (initial count, duplicates removed, count of scores actually interpolated, final row count), then save clean_data.csv with headers.