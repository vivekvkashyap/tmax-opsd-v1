Recover the AES key by locating the known prefix string `"AES256Key"` inside `malware.bin` (e.g. `strings malware.bin | grep AES256Key`, or grep the raw binary) — the full 32-byte key is embedded as a printable string near that prefix, so read it directly rather than guessing padding. Write a small Go program that reads `exfil_data.enc`, splits off the first 12 bytes as the GCM nonce, treats the rest as ciphertext+tag, and calls `cipher.NewGCM(block).Open(nil, nonce, ciphertext, nil)` with the recovered key to get the plaintext; write that exact plaintext (no extra newline) to `decrypted.log`. Parse `decrypted.log` line by line, split on `|`, and collect the IP from lines where the ACTION field is exactly `exfil_success` (trim whitespace around fields), then dedupe and sort the IPs (string sort works for these dotted-quad examples) into `attacker_ips.txt`. For `server.go`, replace the naive string concatenation with `filepath.Join` plus a check that the resolved absolute path still has `/var/www/uploads/` as a prefix (or use `filepath.Clean` and reject any `..` component before joining) — reject with 400/403/404 rather than crashing, and confirm both a normal filename and a `../../etc/passwd`-style value behave correctly before compiling to `server_patched`.
