Treat this as black-box reverse engineering: don't guess the cost formula, derive it empirically. Build small, controlled topology files (e.g. two or three nodes with one or two edges) and vary exactly one metric at a time (latency only, then jitter only, then drop-rate only) while holding the others at zero, running `/app/legacy_router` on each and reading the reported "Total Cost" to isolate each metric's linear coefficient — then confirm with a combined test that the coefficients sum as expected (additive vs. multiplicative). Separately confirm the routing rule by constructing a topology with multiple candidate paths of different total cost and checking the binary always returns the minimum-cost path, which points to a shortest-path algorithm (implement with Dijkstra using a binary heap/priority queue for good asymptotic performance, since you need a large speedup on 100k edges). Match the exact output string format character-for-character (arrows, spacing, "Total Cost:" label) by diffing your output against the legacy binary's on the same inputs before optimizing further. Only after correctness matches exactly should you focus on performance: use efficient I/O parsing (avoid repeated string ops), adjacency lists, and a proper priority-queue-based Dijkstra to comfortably clear the required speedup on the large hold-out graph.
