Build the script in layers and test each layer before combining. First write a single-file "process one document" function/subshell that computes word count with `wc -w`, calls `/usr/local/bin/mock_embed <filepath>` to capture the embedding JSON string, and inserts a row via `sqlite3 /home/user/dataset.db "INSERT INTO documents VALUES (...)"` (use parameterized insertion carefully to avoid quoting bugs with the embedding string, e.g. write to a temp SQL file or use `sqlite3 -cmd`). Initialize the DB schema once up front with `CREATE TABLE IF NOT EXISTS documents(...)` and a `DELETE FROM documents;` before each of the three runs. For concurrency, drive the per-file worker via `xargs -P N -I{} bash -c '...' _ {}` (or GNU `parallel`) over the file list from `/home/user/raw_data/*.txt`, and time each full run using `SECONDS=0; ...; echo "Concurrency N: ${SECONDS} seconds"` or wrap with `/usr/bin/time -f '%e'` — append each line to `/home/user/benchmark.log` in the exact `Concurrency X: Y seconds` format. Since SQLite serializes writes, be aware that concurrent inserts from parallel jobs can hit `database is locked` errors — use `sqlite3 -cmd ".timeout 5000"` or serialize writes through a single writer process fed by a pipe from the parallel workers to avoid corrupting the run.
