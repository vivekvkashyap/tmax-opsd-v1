Treat Phase 1 as a black-box behavioral reverse-engineering task, not disassembly-first. Since `/app/legacy_router` is stripped and UPX-packed, first try running it directly with several sample tokens and observing the integer output — look for it always landing in a narrow range (e.g. 4 consecutive port numbers), which strongly suggests `base_port + (something % 4)`; test tokens of length 1 with known ASCII values, and tokens differing by one character, to reverse-engineer that "something" as a sum of character codes modulo 4 (dynamic tools like `ltrace`/`strace`/`gdb -batch -ex run` or `objdump -d` after `upx -d` can confirm if black-box testing is ambiguous, but don't modify the binary). Write `new_router.c` implementing that formula exactly and compile it; validate against the legacy binary yourself on many random strings (loop comparing outputs) before considering Phase 1 done. For Phase 2/3, append `export ROUTER_MODE=migration` to `.bashrc`, write a straightforward nginx `upstream`+`listen 9000` load-balancer config across ports 8080-8083, write an Expect script spawning bash that sets the env var and runs `gcc new_router.c -o new_router` while asserting a `0$` exit code, and write a `tar czf` backup script that mkdir's `backups/` first.
