Install `sentence-transformers` (and its torch dependency) first, then load `all-MiniLM-L6-v2` once and reuse it for both the query and all dataset files — do not re-instantiate the model per file. List every `.txt` file in `/home/user/dataset_metadata/`, read each one's full text, and encode all descriptions in a single batch call for efficiency and determinism. Encode the query string separately with the same model. Compute cosine similarity between the query embedding and each dataset embedding (normalize vectors and take the dot product, or use the library's built-in `util.cos_sim`). Sort dataset filenames by descending similarity score and take the top 3 — track filenames alongside embeddings so ordering is never lost during sorting. Create `/home/user/results/` with `os.makedirs(..., exist_ok=True)` before writing. Write the final JSON with the query string and `top_3_files` as a list of bare filenames (not full paths) in similarity order, and validate the file parses back as valid JSON with `json.load` before finishing.