Work the four sub-problems independently and verify each before moving on. (1) OCR: run `tesseract /app/pr_assets/token.png stdout` (or output-file mode), then trim whitespace/newlines before saving to `/home/user/admin_token.txt` — don't hand-guess the token. (2) Build: inspect `CMakeLists.txt`, note `http_parser` is built as a library target but never linked into `miniwaf_server`; add a `target_link_libraries(miniwaf_server http_parser)` line, then confirm `cmake . && make` succeeds. (3) Memory safety: grep `parser.c` for the unsafe copy call, replace it with a bounded equivalent (e.g. `strncpy` sized to the buffer minus one byte, explicitly null-terminating) so headers over the buffer size are truncated/rejected instead of overflowing — test by feeding an oversized header locally. (4) Orchestration script: build first, read the token file, then launch `./miniwaf_server <token>` bound to 127.0.0.1:8080, backgrounded (e.g. `nohup ... &` or `disown`) so it stays alive after the script exits. Manually curl the server to confirm 200/403 behavior before finishing.
