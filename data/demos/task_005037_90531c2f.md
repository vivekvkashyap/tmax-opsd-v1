Set up the venv (`python3 -m venv /home/user/fit_env && source /home/user/fit_env/bin/activate && pip install numpy scipy pandas`) and load `/home/user/spatial_data.csv` with pandas. Fit the Gaussian-plus-background model with `scipy.optimize.curve_fit`, providing sensible initial guesses (e.g., amplitude ~ max(y)-min(y), mu ~ x at argmax(y), sigma ~ a fraction of the x-range, C ~ min(y)) so the fit converges to the true underlying parameters — since the data is noise-free here, a good initial guess should let curve_fit converge essentially exactly. After fitting, take `abs(A)` and `abs(sigma)` to satisfy the positivity constraints. Build the refined mesh with `np.linspace(mu-2*sigma, mu+2*sigma, 500)`. For root finding, restrict the search to x > mu (e.g., bracket the root with `scipy.optimize.brentq` between mu and mu+2*sigma, or give `fsolve` a starting guess to the right of mu) to find where the fitted curve equals 3.0 — verify you found the right-side root, not the (symmetric) left-side one, by checking x_target > mu. Round every output value to exactly 4 decimals and write the dict as JSON to `/home/user/solution.json`.