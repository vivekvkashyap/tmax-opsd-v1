Work through this as decrypt -> match -> restore -> lock down -> report, verifying each step before moving to the next. Decrypt both backups with `openssl enc -d -aes-256-cbc -pbkdf2 -in backup_alpha.enc -out alpha.key -pass pass:S3cr3tKeyB4ckup` (and the same for beta) — get the flags exactly right (`-d` for decrypt, `-pbkdf2` must match how it was encrypted) or you'll get garbage/errors. To find which key matches the cert, extract the RSA modulus from each candidate key and from `server.crt` with `openssl rsa -noout -modulus -in <key>` and `openssl x509 -noout -modulus -in server.crt`, and compare the strings directly (or hash both and compare hashes) rather than assuming — do this for both candidates since either alpha or beta could be the real one. Rename/copy the matching key to `/home/user/web_certs/server.key`, then delete the non-matching decrypted key and both original `.enc` files. Set permissions with `chmod 600` on `server.key` and `chmod 644` on `server.crt`. For the audit report, compute `sha256sum server.key` for line 1, compute the MD5 of the modulus *string* (not the key file) for line 2 via `openssl rsa -noout -modulus -in server.key | openssl md5`, then list every remaining file in the directory with `stat -c "%n %a"`, sorted alphabetically by path, for the remaining lines.
