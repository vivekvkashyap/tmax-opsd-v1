Recreate the systemd `ExecStartPre`/`After=` ordering explicitly as a shell dependency chain instead of relying on any service manager. Write `init_storage.sh` idempotently: use `mkdir -p` (safe to rerun) and guard the config-file write with `[ -f /home/user/app_data/quota_config ] || echo "QUOTA=8192" > .../quota_config` so a second run never clobbers an existing value; end with `exit 0`. `start_services.sh` should simply call `init_storage.sh` first (and check its exit status), then launch `./supervisor &` in the background. For the C fix, after `fopen()` immediately check `if (f == NULL) { perror(...); exit(1); }` before any `fgets`/read — this is the actual crash cause, not a cosmetic issue. Then parse the quota file's `QUOTA=<int>` line yourself (e.g. `sscanf(line, "QUOTA=%d", &max_quota)`) instead of hardcoding the value, and recompile with `gcc -o supervisor supervisor.c`. For the metrics pipeline, use `awk -F' ' '$2=="CRITICAL"{sum+=$3} END{print sum}'` (or equivalent grep+awk) on `metrics.log`, redirecting the integer total to `/home/user/critical_sum.txt`. `chmod +x` every script, then run `start_services.sh`, wait for the worker to finish logging, and run `parse_metrics.sh`.