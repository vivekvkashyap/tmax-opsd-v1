Profile before rewriting: the sequential backup_service.py is likely slow because it opens/gzips/deletes files one at a time in a single process, with per-file process/module overhead. Rewrite it using Python's `multiprocessing.Pool` (or `concurrent.futures.ProcessPoolExecutor`/ThreadPoolExecutor) to fan the 5,000 files across worker processes, having each worker read a raw file, gzip-compress it (`gzip.open`) into `/home/user/data/backup/<name>.gz`, then delete the original — batch the file list and pass chunks to `pool.map` rather than spawning one task per file naively if overhead dominates; measure with `time python backup_service.py` and iterate on worker count/chunksize until comfortably under 2.0s. Make sure both `/home/user/data/raw` and `/home/user/data/backup` directories are created up front. For the CLI automation, write an Expect script that `spawn`s `python3 /app/microservices/cli_admin.py`, uses `expect "Enter admin password:"` then `send "admin123\r"`, `expect "Confirm restore? (y/n):"` then `send "y\r"`, and finishes with `expect eof` so the script exits 0 only after the child completes.