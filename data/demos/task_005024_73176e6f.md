In `/home/user/ci_gen.py`, first glob all `.proto` files in `/home/user/protos/` and, for each, use a regex like `import\s+"([^"]+)"\s*;` to extract its dependency filenames (strip `.proto` for the service name). Build an adjacency map of service -> list of dependency services, then compute each service's layer with a recursive/memoized function: layer(s) = 0 if no deps, else 1 + max(layer(d) for d in deps(s)) — this naturally handles the DAG regardless of file declaration order, so don't assume the files are listed in dependency order. Once every service has a layer, group them to find the max layer for the `stages:` list (`stage_0` ... `stage_N`). Since you can't use PyYAML, hand-format the YAML text carefully: consistent 2-space indentation, `stages:` as a `- ` list, and each job block with `stage:` and a `script:` list containing exactly the one `echo "Building <name>"` line — write it as a Python string template rather than trying to serialize a dict, and validate visually against the example format in the task before writing the file.
