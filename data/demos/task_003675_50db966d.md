Read `extractor.c` closely before compiling — it's a custom FASTA-like parser (headers start with `>`, sequence chars accumulate a length counter, non-header/non-newline whitespace is skipped) so trust its actual behavior rather than assuming a generic FASTA length rule; compile with `gcc extractor.c -o extractor`. Pipe both `run_A.fasta` and `run_B.fasta` through it to produce two length lists. In the Python analysis, follow the spec exactly since results must match a precise numeric target: fit `scipy.stats.gaussian_kde` separately to each length array (default Scott bandwidth), evaluate both on `numpy.linspace(0, 2000, 2000)`, normalize each evaluated array by dividing by its own sum so it becomes a valid PMF, then compute `scipy.spatial.distance.jensenshannon` between the two PMFs. Round to exactly 4 decimal places when writing to `/home/user/jsd_output.txt` — double-check you're normalizing the KDE evaluations (not the raw data) and using the same grid for both distributions, since a grid or normalization mismatch will shift the JS distance noticeably.