First fix the build before touching logic. Inspect the `Makefile` for bogus flags being passed to `cargo build --release` (e.g. an invalid `RUSTFLAGS` target-cpu setting will make rustc fail) and remove/correct them; then inspect `Cargo.toml` for a misspelled dependency key (Rust's serde derive feature key must be `features`, not a singular typo) — fix it so `cargo build --release` succeeds and produces `target/release/telemetry-processor`. Only then implement `src/main.rs`'s filtering: read the file line by line, parse each as JSON — a line that fails strict JSON parsing, contains raw control characters, or contains an unpaired/lone UTF-16 surrogate escape (validate this at the string level, not just via `serde_json`'s default leniency) must be dropped entirely. Then check `value` is numeric and within `[0.0, 1000.0]`. Track a per-`sensor_id` "last accepted (sensor_id, message)" pair to drop consecutive duplicates. Maintain a per-sensor sliding window of up to the last 3 accepted `value`s to compute `rolling_avg`, appending it to the emitted JSON object. Build and manually run your binary against a few hand-crafted clean/evil-style lines before trusting the hidden corpora.