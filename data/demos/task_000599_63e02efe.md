Build this as a real multi-service pipeline, not a shortcut. Steps: (1) install and start redis-server, confirm with `redis-cli ping` on localhost:6379; (2) start `/app/simulator.py` in the background so it's listening/streaming on localhost:9000; (3) write `processor.py` that opens a TCP client socket to localhost:9000, reads line by line, splits each line on commas into timestamp_ms, encoding_type, payload — decode payload with `bytes.decode(encoding_type)` inside a try/except, dropping the line on any decode error or unrecognized encoding name; (4) tokenize the decoded text into lowercase alphanumeric words via a simple regex/split, and maintain a windowing structure keyed by `timestamp_ms // 10000` (event-time, not wall-clock) — when a new event's window differs from the current one, finalize the closed window by computing the top-5 most frequent words (Counter.most_common) and writing that JSON array to Redis key `window_top_words`; (5) write `api.py` as a Flask/FastAPI app on 0.0.0.0:8080 with `GET /top-words` that checks for header `Authorization: Bearer SecretAutomationToken2024` (401 if missing/wrong) and otherwise reads `window_top_words` from Redis and returns `{"top_words": [...]}`. Launch processor.py and api.py as background processes and confirm each is alive before finishing.