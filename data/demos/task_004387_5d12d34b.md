Approach this as a chain of fixes: (1) In `analyze_struct.c`, add `argv[3]` parsing with `atof`/`strtod` defaulting to 0.0, and add `epsilon` to `dist[i][i]` for every `i` right after the distance matrix is built and before calling `lu_decompose` — this is what prevents the "Zero pivot" crash on distance matrices that are exactly zero on the diagonal. (2) Use the HDF5 C API (`H5Fcreate`, `H5Screate_simple` with a 1-D dataspace of length n_atoms, `H5Dcreate2`/`H5Dwrite` with `H5T_NATIVE_DOUBLE`) to write `u_diag` to a dataset named exactly `/U_diag` at the file root, and compile with `-lhdf5 -lm` (install `libhdf5-dev` first if missing). (3) Run the compiled binary on both PDB files with epsilon=0.01 to produce the two `.h5` outputs. (4) In `compare.py`, read both `/U_diag` datasets with `h5py`, compute Pearson correlation via `scipy.stats.pearsonr`, write it to `correlation.txt` with `f"{corr:.4f}"`, and produce any simple matplotlib comparison plot saved to `plot.png`. Verify each stage's output before moving to the next rather than debugging everything at once.
