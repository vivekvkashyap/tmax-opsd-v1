Reverse-engineer the transform empirically before writing any C++: call `/app/legacy_auditor <hex> <key>` with controlled, isolating inputs — hold the key fixed and vary one ciphertext byte at a time, then hold ciphertext fixed and vary the key, then vary only a byte's position within a longer ciphertext under a repeated key character — and diff the outputs to see how each output byte depends on the input byte, its index, and the corresponding key byte. Use these isolated probes to separate positional/index effects from key-dependent effects and derive a per-byte formula. Validate the candidate formula against many more random hex/key pairs, including multi-block lengths where the key repeats, before trusting it — don't stop at the first formula that fits a few examples. Implement `/home/user/fast_auditor.cpp` to read `<input_file>` line by line, decode hex efficiently, apply your derived transform, and check for the exact substring `escalation=granted` with `std::string::find`. Buffer output writes and compile with `-O3`, avoiding per-line flushing or dynamic allocation in the hot loop, since 500k lines must process in under 2 seconds.
