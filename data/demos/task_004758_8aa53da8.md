Begin with black-box/static analysis of `/app/legacy_data_node`: run `strings` on it to look for a plausible status-check command string and any hardcoded port, then confirm empirically by starting the binary and using `nc`/a small script to connect to candidate ports and send candidate commands, observing the reply — don't guess blindly, verify the exact request/response bytes since your bridge must forward them precisely. Once confirmed, write the auth-bridge (any language, e.g. Python `http.server` or a small script) listening on `127.0.0.1:8080`: check the `Authorization` header equals `Bearer cloud-migration-token` exactly (401 otherwise), then on authorized `GET /status` open a fresh TCP socket to the legacy port, send the discovered command, read the full response, and echo it back with 200. For lifecycle, write two user-level systemd unit files under `/home/user/.config/systemd/user/` with `legacy-bridge.service` declaring `After=`/`Requires=legacy-node.service`. Create the log directory with `chmod 700`, and write a logrotate stanza with `daily`, `rotate 7`, `compress` targeting the log path. Finally leave the bridge process actually running (background it, e.g. `nohup ... &` or `systemctl --user start`) so the verifier's live HTTP checks succeed.
