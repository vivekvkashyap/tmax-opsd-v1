Write a CSV parser as a proper state machine, not `fgets` line-by-line: track whether you're inside a quoted field, and only treat a newline as a record separator when you are outside quotes (quote-count-parity or an explicit in-quotes flag). Read the file byte-by-byte (or with `fread` into a buffer) so embedded `\n` inside quoted `feedback_text` gets appended to the field instead of ending the record. For masking, split the email at `@`, keep the email's first byte, then emit literal `***` before the `@`, keeping the domain verbatim. For encoding, iterate every byte of feedback_text and replace any byte `> 127` (cast to `unsigned char` before comparing, since `char` may be signed) with `?`. Derive the year-month bucket by taking the first 7 characters of the ISO8601 timestamp (`YYYY-MM`) and open/append to `/home/user/reports/report_<that>.md`, creating the directory first with `mkdir -p` equivalent (`mkdir()` + check `EEXIST`, or system("mkdir -p ...")). Build and append output exactly matching the required template, including the trailing `---` line, per record, opening each month file in append mode across records so multiple entries accumulate correctly.
