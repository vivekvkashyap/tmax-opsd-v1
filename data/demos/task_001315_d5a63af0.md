Work the C++ memory bug first, in isolation, before touching networking: build `AudioChunkPool.cpp` with ASan/UBSan (or run under Valgrind) using a small test harness that exercises allocate/free/reuse cycles on audio buffers, and read the leak/overflow report to find the exact line — typical causes are a pool slot freed without clearing its "in-use" flag, an off-by-one on buffer size vs. requested chunk size, or a missing `delete[]`/free on the error/exception path. Fix only what the sanitizer flags, then rebuild and rerun the harness clean before wiring in whisper.cpp. Have the fixed service bind and listen on `127.0.0.1:9090` exposing `POST /transcribe`. Install nginx separately, write a server block listening on `127.0.0.1:8080` that proxy_passes `/api/transcribe` to `http://127.0.0.1:9090/transcribe`, and add an `auth_basic` block with a `htpasswd`-generated user file for `ci_runner`/`build_pass_2024` — verify with an unauthenticated curl (expect 401) and an authenticated one before writing `test_pipeline.sh`. Start both processes in the background (nohup/systemd-style), confirm both ports are listening with `ss`/`curl`, then have the script curl with `-u` and pipe the JSON body to `/home/user/workspace/result.log`.
