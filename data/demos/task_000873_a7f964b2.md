Approach this as bisect-then-fix-then-match. Before bisecting, write a small test/check script that builds the C extension, runs process_data.py, and compares its output against /app/oracle_bin's output within a numeric tolerance — treat any build/link failure as a distinct outcome ("skip") rather than pass/fail, since some commits in the range are known to fail to compile due to a missing linker flag (e.g. missing `-lm`); in `git bisect run`, exit code 125 tells git to skip a commit, so detect the linker error specifically and return 125 for it rather than misclassifying it as good or bad. Once bisect converges, write the first-bad commit hash to /home/user/bad_commit.txt. For the fix, recognize that naive one-pass variance formulas (`sum_sq/n - mean^2`) suffer catastrophic cancellation when the data has a huge mean and tiny variance — replace with a numerically stable online algorithm that accumulates using running-mean deviations rather than raw sums of squares. Validate by rerunning your comparison script against the oracle until the absolute error is negligible.