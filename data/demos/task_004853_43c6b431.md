Build `/home/user/audit.py` in three clearly separated stages so each is easy to debug independently. Stage 1: for each of the three data files, compute `hashlib.sha256(open(path,'rb').read()).hexdigest()` and compare against the corresponding line in `manifest.sha256` (parse it as `<hash>  <filename>` pairs, don't assume a fixed order); record "PASS"/"FAIL" per file — expect at least one mismatch since files can be modified after the manifest was generated. Stage 2: iterate `auth_logs.json` entries and flag an id as a violation if the request's `Authorization` header exists and doesn't start with exactly `"Bearer "`, OR the response's `Set-Cookie` header exists and is missing `Secure` or `HttpOnly` as distinct directives (split on `;`, strip whitespace, exact match — don't use substring search, which would false-positive on things like `NotSecure`). Stage 3: use `ipaddress.ip_network()`/`ip_address()` to check each connection's `src_ip` against every rule's CIDR range with matching `dest_port`; default to DENIED. Assemble the exact JSON structure given in the task and write it to `/home/user/audit_report.json`.