Work in dependency order: first write the exact config file, then write the Rust source, then compile, then run. Parse `/home/user/edge_config.txt` with simple line splitting on `=` into a HashMap rather than hardcoding values, so the program genuinely reads them. To get the numeric UID of `user`, either shell out to `id -u user` from Rust (`std::process::Command`) or parse `/etc/passwd` yourself by splitting each line on `:` and matching the first field against `user`, taking the third field as the UID — do not hardcode `1000`, since it must reflect the real environment. Build the QEMU command string with `format!()` using the parsed MAC, VNC display, host port, guest port, and UID in the exact field order given in the task, then write it as the sole line of `/home/user/run_vm.sh`. After writing the file, set it executable from within Rust (via `std::fs::Permissions`/`set_mode`, or by shelling out to `chmod +x`) — don't forget this step, it's a common miss. Compile with `rustc /home/user/build_edge_node.rs -o /home/user/build_edge_node`, then execute the binary once so the script is actually generated on disk.