OCR the diagram with `tesseract /app/diagram.png stdout`, then regex-extract the values following the literal `Log File:` and `Service Name:` labels from the OCR text — don't hardcode any guessed path/name. Initialize the repo with `git init --bare /home/user/repo.git`, then write `/home/user/repo.git/hooks/post-receive` as a shebang script that invokes `python3 /home/user/deploy.py` with absolute paths (hooks execute with `GIT_DIR` set and an unpredictable cwd, so `deploy.py` must never rely on relative paths — use `git --git-dir=/home/user/repo.git ...` explicitly). Inside `deploy.py`, read the `old new ref` triple from stdin; when `old` is all zeros, diff against git's well-known empty-tree hash (`4b825dc642cb6eb9a060e54bf8d69288fbee4904`) instead. Use `git diff --name-only <old> <new>` to get changed files and check extensions for `.conf`. For idempotency, maintain a small persisted set of already-processed `new` hashes (a plain state file alongside the log works) and check-and-append atomically before writing to the log, so re-triggering the hook for the same commit never duplicates a log line.
