Treat the three parts as independent deliverables. Part 1: count keyframes precisely with a frame-type probe rather than a rough estimate — run `ffprobe -select_streams v -show_frames -show_entries frame=pict_type -of csv /app/network_simulation.mp4 | grep -c I` (or equivalent) against /app/network_simulation.mp4 and write the resulting integer to /home/user/keyframe_count.txt; don't hardcode a guessed count. Part 2: write sanitize_graphs.sh to loop over `*.json` in `<input_dir>`, first validate JSON well-formedness with `jq empty file.json 2>/dev/null` (skip/reject on failure before doing anything else), then use `jq` queries to check: every node id matches `^[A-Za-z0-9_-]+$` (test each id with `jq -r '.nodes[].id'` piped through a regex check, e.g. bash `[[ $id =~ ^[A-Za-z0-9_-]+$ ]]`), every edge weight is non-negative (`jq '.edges[].weight'` and reject if any value `< 0`), and every node with `"indexed": true` also has `"idx_key"` present (`jq` select/has checks). Only if a file passes ALL checks, copy it to `<output_dir>`. Validate against both /app/corpora/clean/ and /app/corpora/evil/ to confirm 100% correct classification before moving on. Part 3: write convert.sh to read one clean JSON graph with jq and emit the three required formats — CSV via `jq -r '.edges[] | [.source,.target,.weight] | @csv'`, JSONL with one line per node listing its outgoing edges, and a DOT file with `digraph { ... }` syntax listing nodes and edges. Make both scripts executable.