Treat this as two independent deliverables and tackle the sanitizer first since it's the more heavily-weighted/testable piece. For the audio: play or transcribe `/app/interview.wav` (install `whisper`/`ffmpeg` as needed, or pipe through `ffmpeg -i interview.wav -ar 16000 -ac 1 out.wav` before feeding to a speech-to-text tool) — listen carefully for the exact directory name, hard-link source/target filenames, and symlink source/target, since these are spoken and must be reproduced exactly as dictated, not guessed. Use `ln` (hard link) and `ln -s` (symlink) accordingly under `/home/user/organized_docs/`, then `tar -czf /home/user/final_docs.tar.gz -C /home/user organized_docs`. For the sanitizer, write `/home/user/sanitizer.py` to walk the target directory (`os.walk`) and check each anomaly class explicitly: parse every `.json`/`.xml`/`.csv` with strict libraries (`json.load`, `xml.etree.ElementTree.parse` with entity expansion disabled, `csv.reader`) catching exceptions as "evil"; for archives, inspect member names before extracting — reject any `.zip`/`.tar` member whose resolved path escapes the target directory (check for `../` after normalizing) or whose extracted/uncompressed size is implausibly large (zip-bomb guard via `ZipFile.infolist()` `file_size` sums, not blind `extractall`); for symlinks, resolve with `os.path.realpath` and reject any pointing outside the target dir; detect symlink loops by catching `OSError`/`ELOOP` on `os.path.realpath` or `os.stat`. Return exit code 1 on first anomaly found, 0 if the whole tree passes clean. Test against both corpora and confirm exit codes with `echo $?` before finishing.