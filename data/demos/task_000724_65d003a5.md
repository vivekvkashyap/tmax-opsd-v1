Treat the three deliverables as independent and verify each before moving on. For the video: extract frames with `ffmpeg -i /app/switch_monitor.mp4 frame_%04d.png` (or pipe raw frames), then for each frame check programmatically whether it's uniformly pure red — e.g. with Python/PIL, load each frame and confirm every pixel equals RGB (255,0,0) exactly (don't eyeball it or sample only the center pixel, since "completely red" means the whole frame), count how many frames satisfy that, and write just that integer to the output file. For the log classifier: define the allowed character set exactly as specified (alnum, space, `-`, `_`, `:`, `.`, `[`, `]`) and use something precise like `grep -qP '[^A-Za-z0-9 _:.\[\]-]'` against the file — if it matches (finds a disallowed char) exit 1, otherwise exit 0; make the script executable and test it against a few clean and injected-metacharacter strings you construct yourself. For the system configuration: tar+gzip the config directory preserving the directory structure inside the archive (so `tar -tzf` shows `netmon_config/...` entries, not absolute paths), write the systemd user unit with proper `[Unit]`/`[Service]` sections and the given `ExecStart=`/Description, and append the export line to `.bashrc` rather than overwriting the file.
