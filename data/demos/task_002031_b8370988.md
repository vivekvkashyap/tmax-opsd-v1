Treat this as two separable deliverables and test each in isolation before wiring them together. For `filter.py`: load the JSON records, group `value`s by `sensor_id`, join against `/app/reference_baselines.csv` (pandas or csv module) to get each sensor's `baseline_mean`; if a sensor_id is absent from the baseline file, flag anomalous immediately (short-circuit); for sensors with >=2 samples run `scipy.stats.ttest_1samp(values, baseline_mean)` and flag if `pvalue < 0.01` (strict, not <=); sensors with <2 points are always treated as valid regardless of t-test. Print exactly `EVIL`/exit 1 or exactly `CLEAN`/exit 0 — no extra text. Build a few synthetic JSON files by hand to confirm both branches trigger correctly before trusting the corpus test. For the pipeline: edit `nginx.conf`'s server block to add `location / { proxy_pass http://127.0.0.1:5000; }` on port 8080, populate the three required keys in `/app/data_app/.env` exactly as named, then run `bash /app/start.sh` and confirm with `curl -X POST http://127.0.0.1:8080/upload -d '{...}'` that a request actually reaches Flask, invokes your filter script as a subprocess on the payload, and returns 200/400 based on its exit code — check logs/process status if the curl hangs or errors rather than assuming config alone is enough.
