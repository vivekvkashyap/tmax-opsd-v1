In C, read `/home/user/changes.txt` line by line and pull out the three fields with straightforward string scanning (e.g. `sscanf`/manual parsing for `cfg_id` and `delta`, and locate the `"status"` field's quoted value substring). For the status string, write a decoder that scans for the literal 6-character pattern `\uXXXX`, parses the 4 hex digits with `strtol(..., 16)`, and appends the resulting ASCII char to a decode buffer — apply this decoding uniformly whether or not the string actually contains escapes, so plain `active`/`inactive` strings pass through unchanged. Accumulate a running sum and count per `cfg_id` (e.g. via a fixed-size array indexed 1..100, since IDs are bounded) but only add to the accumulator when the decoded status equals exactly `"active"` — inactive/other records must not affect the average at all, not even as a zero. After processing all lines, iterate cfg_ids in ascending order, skip any with zero active records, and print `id,avg` with `printf("%d,%.2f\n", ...)` to build the CSV with header `cfg_id,avg_delta`. Compile and run, then diff your output against the expected values you derive by hand from the input file.