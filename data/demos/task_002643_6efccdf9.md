Work through the four sub-bugs independently, then wire the services together. (1) OCR: run `tesseract` (or similar) on `/app/alert_config.png` to recover the exact `AuthToken` string, then require `Authorization: Bearer <that token>` on every dashboard request, returning 401 when missing/wrong. (2) Git forensics: the key isn't in the current tree — search history with `git -C /home/user/monitor-stack log --all -p | grep -i STATE_KEY` (or find the commit that deleted `config.env`) to recover the `STATE_KEY` value, then export it as an env var when launching the Go checker. (3) Cyclic dependency crash: find the Go function recursively walking service dependencies and add a `visited map[string]bool` parameter so a node already on the current path returns "healthy" instead of recursing forever. (4) Variance instability: replace the naive `sum_sq/n - (sum/n)**2` formula with a numerically stable two-pass (mean-centered) or Welford's algorithm, and write the corrected `db-service` variance to `/home/user/variance_debug.txt`. Finally build/run the Go checker (writes `/tmp/health.json`), start the Python dashboard on `127.0.0.1:8080` exposing `GET /api/status`, and curl it with the recovered token to confirm 200 before finishing.
