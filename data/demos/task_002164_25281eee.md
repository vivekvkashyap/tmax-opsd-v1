Do the vendored-library fix first and verify it builds standalone before writing any C that depends on it: inspect `/app/libcsv-3.0.3/Makefile` for the deliberate breakage (an invalid compiler name and/or a `prefix`/`PREFIX` pointing somewhere unwritable), then either edit the Makefile directly or override at invocation time, e.g. `make CC=gcc PREFIX=/home/user/local install` (or edit `CC=gccc` -> `CC=gcc` and the prefix), making sure headers land in `/home/user/local/include` and libs in `/home/user/local/lib` — confirm with `ls` before moving on. For `imputer.c`, parse the two-column `timestamp,value` CSV using the newly-built `libcsv`'s `csv_parse` API (the verifier specifically checks for its use), treat both empty fields and the literal string `NaN` as missing, and implement linear interpolation strictly between the nearest valid neighbors, with constant carry-forward/backward at file boundaries — get the single-threaded logic bit-exact against hand-worked edge cases (leading/trailing gaps, all-missing-but-one) before adding `#pragma omp parallel for` around the fill pass, since parallelizing before correctness is verified just hides bugs. Output must be exactly `%lld,%.6f\n` per line. Finally write `pipeline.sh` (looping incoming -> imputer -> processed, then deleting the source) and a `pipeline.cron` line using `*/5 * * * *`.