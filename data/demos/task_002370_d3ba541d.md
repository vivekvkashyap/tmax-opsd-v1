Structure the bash script as three sequential stages: (1) import — use `sqlite3 citations.db` with `.mode csv` and `.import` (or `--csv`), being careful to skip/handle the CSV header row correctly (e.g. `.import --skip 1 nodes.csv nodes` in modern sqlite3, or create the table explicitly first then import with header skip) so you don't end up with a bogus header row as data; (2) traversal — write a recursive CTE seeded with the row where id = 'P001' at depth 0, unioning in edges where source = the current node's id going in the citation direction (source cites target), incrementing depth, and capping with `WHERE depth < 3` in the recursive term so it stops after depth 3; join the recursive result against `nodes` to pull in `title`. (3) export — use `sqlite3 -json` or a `SELECT json_group_array(json_object(...))` query, ordered by `depth ASC, paper_id ASC`, and redirect/write straight to `/home/user/citation_tree.json` so the JSON is generated dynamically from the DB, not hardcoded. Common trap: forgetting `DISTINCT` or that a node reachable via multiple paths would duplicate — check the edges fixture doesn't require special handling, but structure the CTE to avoid infinite loops on cycles regardless. Make the script executable with `chmod +x` and verify by actually running it end-to-end.
