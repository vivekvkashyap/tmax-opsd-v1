Locate the leak by reading the vendored `pydatatransform-1.2.0` source directly rather than fuzzing blindly first — memory leaks in small vendored packages are usually a single unbounded global accumulator. Grep the package for module-level mutable containers (lists/dicts defined outside functions) that get appended/inserted to inside the request-handling function (e.g. `apply_diff` or similar) but never cleared or bounded — that append call is almost certainly the leak, likely tracking call history for "audit" purposes. Confirm your hypothesis with the MRE: write `/home/user/mre.py` that imports the function directly, calls it once with a small hardcoded payload, and checks that nothing suspicious grows (or just eyeball the source diff). Fix it by removing or bounding that accumulation without touching the function's actual merge/transform logic or return value. Then start `/home/user/server.py` in the background on port 8888 and sanity-check the `/api/v1/diff` endpoint manually with `curl` (POST `{"base": {...}, "diff": {...}}`) to confirm both correctness and that memory stays flat under repeated calls before declaring it fixed.
