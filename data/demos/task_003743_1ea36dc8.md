First recover the regex, don't assume its shape. Extract all frames from `/app/pattern_source.mp4` (e.g. `ffmpeg -i /app/pattern_source.mp4 frame_%03d.png` or read via OpenCV/PIL), and for each frame in order compute the average grayscale pixel intensity, round to the nearest integer, and map that integer through `chr()` to an ASCII character; concatenating all frames' characters in order yields the regex pattern string — print/inspect it to confirm it looks like a valid Python regex with named groups `job` and `data` before hardcoding it into your script. Then implement `/home/user/process_logs.py` (executable, `#!/usr/bin/env python3` shebang) reading stdin line by line: apply the recovered regex per line via `re.match`, silently skip non-matches, normalize `data` by collapsing runs of 2+ whitespace chars to a single space, stripping, and lowercasing, then MD5-hash the normalized string. Keep a `dict[job] -> set[hash]` for dedup; on first occurrence for that job+hash, print `f"{job}\t{normalized_data}\t{md5_hash}"` to stdout and record it, otherwise drop the line silently — no other output to stdout. Test locally by piping synthetic log lines (including duplicates and near-duplicates with extra whitespace/case differences) through your script and manually verifying dedup and formatting before trusting the fuzz test.