Build and validate each layer independently before chaining them. First establish the tunnel: `ssh -f -N -L 8080:localhost:9090 user@localhost` (background, no remote command) and confirm with `curl localhost:8080/health` that it proxies to the dummy service on 9090. For the Rust monitor, use `reqwest::get` in an async main (`#[tokio::main]`), match on the Result and on `.status().is_success()`; on any failure branch (connection error OR non-200), build a `lettre::Message` with the exact Subject/From/To strings and send it through a `FileTransport::new("/home/user/mail_spool/")` — verify lettre's FileTransport actually writes a `.eml`-style file per message rather than assuming; check the crate docs/examples if unsure of the API shape. For deploy.sh, use `set -euo pipefail`, run `cargo build --release` inside monitor_src, copy the resulting release binary to staging, `chmod +x`/test executability, then copy to production, and only append "Deployment successful" to the log after every prior step succeeded. Finally execute the up/down sequence exactly as described (run once with tunnel alive, kill the ssh process, run again) to prove both code paths actually fire before declaring done.
