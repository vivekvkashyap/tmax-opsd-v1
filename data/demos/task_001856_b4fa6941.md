Write the Go program to process files in a single deterministic pass: list `/home/user/raw_backups/*.dat`, sort filenames alphabetically (Go's `filepath.Glob` result isn't guaranteed sorted — call `sort.Strings` explicitly), then for each file read exactly the first 16 bytes with a bounded read (`io.ReadFull` on the first 16 bytes, not the whole file) and hash them with `crypto/sha256`, formatting the digest as lowercase hex (`fmt.Sprintf("%x", ...)`). Maintain a map from header-hash to the first filename that produced it. On a new hash, copy the full file into `processed_backups/` (e.g. `io.Copy`); on a repeat hash, call `os.Link(firstPath, newPath)` to create a real hard link, not a copy. Build the manifest lines in the same alphabetical order, write them to a temp file inside `processed_backups/` (e.g. `manifest.txt.tmp`), then `os.Rename` it to `manifest.txt` for atomicity. Verify correctness afterward with `stat -c %i` on the linked files to confirm shared inodes, and diff your manifest against the expected `filename hash` format.