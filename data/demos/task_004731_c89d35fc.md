Work the three phases in order and test each before moving on. For Phase 1, write the profile file with plain `export VAR=value` lines — no quoting tricks needed. For Phase 2, write the script so it starts with `source /home/user/.monitor_profile`, then loop over `"$LOG_DIR"/*/` to get service names via `basename`. For each `app.log`, count total lines with `wc -l` and count matching lines with `grep -cE '\[ERROR\]|\[TIMEOUT\]'` — use integer arithmetic (`awk` or shell `$(( ))`) to floor the percentage, e.g. `rate = errors*100/total` using integer division which truncates naturally. Only append the `ALERT: ...` line when `rate >= ALERT_THRESHOLD`. Collect all alert lines and pipe through `sort` before writing the final file so the output is alphabetical by service name, then `chmod +x` the script. For Phase 3, `git init` the repo, create `.git/hooks/pre-commit` as a script that `cd`s to the repo root (or uses a fixed path) and runs `bash -n alert_pipeline.sh`, exiting 1 on failure and 0 on success, then `chmod +x` it. Finally run the script once so `/home/user/active_alerts.log` is generated — verify its contents directly rather than assuming correctness.