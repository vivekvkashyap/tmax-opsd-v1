Tackle the four pieces in dependency order. (1) Fix `auth.c`: replace the `sprintf`-built query string with `sqlite3_prepare_v2(db, "SELECT * FROM users WHERE username=? AND password=?", -1, &stmt, 0)` followed by `sqlite3_bind_text` for each parameter, then compile to `/home/user/auth_service/auth_fixed` linking `-lsqlite3`. (2) Reverse-engineer `hasher`: since it hex-encodes a per-byte transform, run it on known inputs of your choosing (e.g. `./hasher "AAAA"` or `./hasher "\x00\x00"`) and compare the hex output byte-by-byte against the ASCII codes of your input to solve for the single-byte XOR key algebraically (hex_byte XOR known_input_byte = key) — or disassemble with `objdump -d`/`gdb` to read the immediate constant directly; write the discovered key to `/home/user/xor_key.txt`. (3) Pull the admin row's stored hash from `users.db` via `sqlite3`, XOR-decode each hex-pair byte with your key and convert to ASCII to recover the old plaintext password, save to `/home/user/old_password.txt`. (4) Use `hasher` itself (or your understood algorithm) to compute the hash of `Secure_Rotated_2024`, then `UPDATE users SET password=... WHERE username='admin'` in the DB via `sqlite3 users.db "UPDATE ..."`.