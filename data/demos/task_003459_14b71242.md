Build the detector as a real inference pipeline: define the Autoencoder exactly as specified (Conv2d 3→16 stride2 pad1 + ReLU encoder, ConvTranspose2d 16→3 stride2 pad1 output_padding1 + Sigmoid decoder), load `/app/ae_weights.pth` with `torch.load`/`load_state_dict`, and set the model to `eval()` mode with `torch.no_grad()` during inference to get deterministic, reproducible reconstruction error — a common false-positive source is forgetting `eval()` (leaving dropout/batchnorm-like randomness or gradient tracking on) or mismatched input scaling (must be [0,1] float, resized to exactly 64x64). Compute MSE between input and reconstruction, then don't guess a threshold — actually run your script over every file in `/app/corpora/clean/` and `/app/corpora/evil/`, collect the MSE distributions, and pick a threshold value that cleanly separates the two sets (e.g. midpoint between max-clean-MSE and min-evil-MSE), verifying 100% separation before moving on. For part 3, extract frames at exactly 1 fps with `ffmpeg -vf fps=1` into `/home/user/frames/%03d.png`, run the calibrated detector on each, and write the sorted filenames of clean-classified frames (exit code 0) to `/home/user/clean_frames.txt`, one per line.