Build and unit-test the Go filter in isolation before wiring the pipeline, since the corpus test scores it standalone. Read CSV rows from stdin with `encoding/csv` (or manual split, but a real CSV reader handles quoting correctly), and for each row check, in order: exactly 5 fields; `amount` parses via `strconv.ParseFloat(field, 64)` and is `>= 0` (reject negative, reject parse errors); `category` matches only `[a-zA-Z0-9]+` (use a compiled regex, reject on any non-alphanumeric char including empty); and `timestamp` parses as an integer and is strictly greater than the maximum timestamp seen so far among ACCEPTED rows only — track that max as running state, initialize it so the very first valid row is always accepted regardless of value, and only advance the max after a row passes all other checks too (a common bug is updating max before checking other fields, or updating it on rejected rows). Emit passing rows to stdout preserving original formatting. Test `/home/user/filter` directly against hand-crafted good/bad CSV files via `<`/`>` redirection to confirm each rejection rule fires independently before touching the pipeline. Then write `pipeline.sh` as `curl -s http://localhost:5000/data | /home/user/filter | nc localhost 5001` (or streaming equivalent), made executable, and confirm end-to-end behavior against the running Redis/producer/consumer services.