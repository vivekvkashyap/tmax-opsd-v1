Build this as three clear stages inside one Python script using only the standard library. First load `tx_locks.json` and index it two ways: a map from resource -> list of (tx_id, timestamp) for GRANTED entries, and a list of (tx_id, resource, timestamp) for WAITING entries. Parse `resource_graph.nt` with a simple regex/split on each line (format `<R10> <dependsOn> <R15> .`) into a dict from resource -> resource it depends on (don't reach for `rdflib`). Then, for every WAITING entry (TxA, Res1, ts1), look up Res1's dependency Res2, find who holds Res2 GRANTED (TxB, ts2); then check if TxB itself has a WAITING entry (Res3) whose dependency Res4 is GRANTED to TxA — that four-hop cycle is exactly the deadlock condition described, so implement it as literal lookups/joins rather than a generic cycle-detection algorithm. For each detected pair, normalize ordering so the lexicographically smaller tx_id is `tx_1`, dedupe pairs, and track the minimum of all four involved timestamps. Sort the deduped results by that minimum timestamp ascending (tie-break by `tx_1` ascending), keep only the first 5, and write them to `/home/user/deadlocks_report.csv` via the `csv` module with header `tx_1,tx_2,min_timestamp`. Run the script and inspect the CSV to confirm ordering and pagination look right.