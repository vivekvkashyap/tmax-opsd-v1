Install `ffmpeg` and `openai-whisper` (or `pip install whisper`/`faster-whisper`), then transcribe `/app/interview.wav` with the whisper CLI or API, extracting only the plain text (no timestamps/segments) and writing it verbatim to `/home/user/transcript.txt` — don't hand-transcribe or guess the content, let the model produce it, and strip any leading/trailing whitespace or metadata whisper adds. For the C++ extractor at `/home/user/fast_extractor.cpp`: read all int16-range samples from a single stdin line into a `std::vector`, slide a window of size 256 with stride 128 (loop while `i + 256 <= n`, incrementing `i` by 128, discarding a trailing partial window), inside each window compute consecutive differences `D[i]=X[i+1]-X[i]` for 255 diffs, sum their squares as `E` using a wide integer type (avoid overflow — use `long long`), then output `E_mod = (E * 137) % 999983` per window as a single space-separated stdout line — get the modulus/multiplier and integer-overflow handling exactly right since correctness here is checked by comparing against many random fuzzed inputs, not just the sample file. Compile with `g++ -O2 fast_extractor.cpp -o fast_extractor`. Then write `/home/user/plot_features.py` to load the wav as PCM16 (e.g. via `scipy.io.wavfile` or `wave`+`struct`), pipe the samples space-separated into your compiled binary via `subprocess`, parse its stdout, and plot with matplotlib (title + labeled axes) to `/home/user/sec_plot.png`.
