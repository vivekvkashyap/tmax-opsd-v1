Fix the two named bugs independently, then match the oracle's exact output format. For URL-param parsing, don't rely on fragile `grep -o`/`cut` chains on the raw request line — extract the query string robustly (split on `?` then `&`, then `=` per pair) so `start`/`end` are parsed even with trailing HTTP-version text or missing params, and guard against empty values causing SQL/query crashes. For the graph query, replace the buggy cross-join with a recursive CTE that does BFS/Dijkstra-style expansion: seed with the start node at cost 0 and empty path, recursively join `edges` to extend each partial path by one hop tracking accumulated weight, then select the minimum-cost row(s) reaching the end node (in SQLite, order by cost and `LIMIT 1` after the recursive step, guarding against cycles by not revisiting nodes already in the path). Run `/app/path_oracle <start> <end>` yourself for several node pairs and diff its stdout against your server's HTTP response body byte-for-byte to nail the exact JSON key order/spacing before considering it done. Start the server backgrounded and confirm it actually stays listening on 127.0.0.1:9000 for repeated requests.
