Debug this like a real on-call: reproduce first, then fix root causes one at a time, restarting/reloading services after each change. Curl `127.0.0.1:8080/api/health` to confirm the 502, then read `nginx.conf`'s `proxy_pass` target port and compare it against the port the Python app in `server.py` actually binds — pick one side to change so they match, then reload nginx (`nginx -s reload -c ...` or restart it). Next, run the app directly (or check supervisor's stderr log) to see it exits immediately, read the code to find which `os.environ` key it requires, and add an `environment=` line for that variable with the value stated in the task to the `[program:backend-api]` section of supervisord.conf, then have supervisor pick it up (`supervisorctl -c ... update` or restart supervisord). Fix deploy.sh by adding the correct `supervisorctl -c /home/user/supervisor/supervisord.conf restart backend-api` line. Finally re-curl the health endpoint and save the exact output to success.txt.
