Build the three data sources and the C++ API as separate, independently-testable pieces, then integrate. (1) Video: use `ffmpeg -ss <t> -vframes 1` (or extract all frames at 0.5fps aligned to epoch boundaries) to grab the frame at each 2-second epoch start, convert to grayscale, and read the center pixel's intensity — write a quick script to verify a couple of extracted frame values look sane before writing an INSERT loop; use SQLite's C API with `sqlite3_prepare_v2`/`sqlite3_bind_int` (parameterized, not string-concatenated SQL) to insert each `(subject_id=1, epoch_index, activity_level)` row into the `epochs` table. (2) Graph: parse `/app/graph.json` (a nlohmann::json or hand-rolled parser is fine) into an adjacency map, then implement Dijkstra with a min-heap/priority_queue tracking distances and predecessor pointers so you can reconstruct the path list, not just the cost. (3) API: use a header-only HTTP library (e.g. cpp-httplib) to bind `127.0.0.1:8080`, wire `/path?start=&end=` to your Dijkstra function returning `{"path": [...], "cost": N}`, and `/activity?epoch=` to a parameterized SELECT against the SQLite DB returning `{"epoch": N, "activity": level}`. Compile with C++17, launch the server backgrounded (e.g. `./server &`), and curl both endpoints yourself to confirm the JSON shapes match exactly before finishing.