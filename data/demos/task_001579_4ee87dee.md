Build this in clear stages and check each before moving to the next, since bugs compound. First, parse the CSV keeping `id` as a true integer type in C++ (e.g. `long`/`int`) parsed separately from the float columns — never route it through a double, which is exactly the precision-loss bug the task calls out. Parse `f1`/`f2`/`y` as `double`, detecting the literal string `"NaN"` during parsing (not relying on `strtod`'s NaN handling, since you need to distinguish "missing" from a computed NaN) and marking those cells for imputation. Second, compute the mean of each column using only the non-missing values (sum and count separately, skipping flagged cells), then fill the missing ones and write `/home/user/clean_data.csv` with exactly 4 decimal places via `printf("%.4f", ...)` — replicate this on the *imputed* dataset, not the raw one, for downstream steps. Third, implement Ridge Regression using either Eigen (`-I/usr/include/eigen3`) or manual 2x2 matrix inversion (features are just f1,f2, no intercept), doing 5-fold sequential (non-shuffled) cross-validation over folds of fixed size, computing MSE per fold for each of the three lambda values, averaging across folds, and selecting the lambda with lowest average MSE — write that single number, not a rounded/formatted string with extra characters, to `/home/user/best_lambda.txt`.
