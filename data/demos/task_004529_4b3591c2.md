Split into topology fixes and a standalone C validator, and test each independently. For topology: add an nginx `location /api/ { proxy_pass http://127.0.0.1:8000/; }` block, and set `/app/backend/.env` values exactly to the given Postgres user/pass/db and Redis host/port so the API's own internal health checks succeed; start services via `/app/start_services.sh` (or manually in order: Postgres, Redis, API, Nginx) and confirm with `curl http://localhost:8080/api/health` before moving on. For the sanitizer, use the provided `cJSON` library (don't hand-roll JSON parsing) to parse stdin into a `cJSON*`, then apply checks in order and reject on first failure: (1) the root object must have exactly the three keys `backup_id`/`query`/`deps` with correct types — reject on any extra or missing field; (2) scan the `query` string for a literal `'` byte and reject if found; (3) treat `deps` as directed edges and run cycle detection — build an adjacency list from the integer pairs and do a DFS with a "visiting/visited" recursion-stack marker (three-color DFS) to detect back-edges, rejecting if any cycle exists. Print exactly `ACCEPT\n` or `REJECT\n` and exit 0 either way (exit 0 even on reject — only I/O failure should be non-zero, per the entry-point contract). Build your own small test payloads covering each rejection rule before compiling the final binary.
