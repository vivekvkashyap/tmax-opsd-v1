Write a script that, for each package directory, reads `meta.dat`, reverses the two encoding layers in order (base64-decode first to recover the hex string, then hex-decode that to recover the UTF-8 JSON), and parses the resulting `{"name", "deps"}` object — verify the decode by printing one intermediate result to check it looks like valid JSON before trusting the rest. Build an adjacency structure from `deps` (dependency -> dependent) and compute in-degrees, then run Kahn's algorithm for topological sort: repeatedly select the ready package (in-degree 0) with the alphabetically smallest name using a min-heap or by sorting the ready set each iteration, emit it, and decrement the in-degrees of its dependents. This tie-break is essential — picking an arbitrary ready node instead of the alphabetically-first one will produce a valid-but-wrong order. Join the result with `", "` (comma-space) and write it as a single line to `/home/user/build_order.txt`, then cat the file back to confirm the exact formatting (no trailing newline issues, no extra spaces) before finishing.