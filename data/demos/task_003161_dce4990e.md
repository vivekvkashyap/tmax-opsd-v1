Treat the three deliverables as separable. (1) For the video, use `ffmpeg` to extract every frame to disk (e.g. `ffmpeg -i backup_monitor.mp4 frame_%04d.png`), then write `analyze_frames.py` to load each frame (Pillow/OpenCV/numpy), compute mean pixel brightness across all channels, count frames below the 5/255 threshold, and write only that integer count to `/home/user/failure_count.txt` — don't hardcode a count, derive it by actually running the analysis. (2) For `build_query.py`, write `get_anomaly_query(table_name)` returning a SQL string using a window function like `AVG(backup_size_mb) OVER (PARTITION BY db_name ORDER BY timestamp ROWS BETWEEN 5 PRECEDING AND 1 PRECEDING)` aliased as `rolling_avg`, wrapped in an outer query (or CTE) that filters `backup_size_mb > 2 * rolling_avg`, selecting exactly the required column names/order; test it locally against an in-memory SQLite table before trusting it. (3) For `sanitizer.py`, prefer a strict allowlist approach for `is_safe_filter` (permit only simple comparison expressions on known-safe column names/operators/literals, reject semicolons, UNION, DROP/ALTER, comments, subqueries) over a blacklist of bad keywords, and validate it yourself against `/app/corpus/clean/` and `/app/corpus/evil/` before considering it done, since grading requires 100% separation on a hidden corpus too.