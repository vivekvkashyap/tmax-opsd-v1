Unpack the nested archive first with standard tools to isolate the real problem: `tarfile.open(...).extractall()` to get the inner zip, then `zipfile.ZipFile(...).extract('application.log')`. Before writing extraction logic, confirm the encoding by inspecting the file's byte order mark or just decoding with `utf-16le` and checking it parses cleanly — don't assume UTF-8. For the streaming requirement, open the file in binary mode and either `mmap.mmap(fd, 0)` it or iterate with a buffered reader, decoding chunks/lines as UTF-16LE (careful: UTF-16LE lines aren't newline-delimited at the byte level the same way UTF-8 is, so decode incrementally or read the whole stream through a `TextIOWrapper(BufferedReader, encoding='utf-16le')` for line-by-line iteration without loading the full string into memory at once). Parse records by starting a new one whenever a line begins with `[` followed by a bracketed level, and appending subsequent non-bracket-prefixed lines to the current record's buffer. Keep only records whose tag is exactly `[ERROR]`, re-encode each kept record to UTF-8, and write them concatenated (preserving original line breaks/indentation, no extra blank lines) to `/home/user/extracted_errors.log`.