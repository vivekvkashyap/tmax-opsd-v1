This is a two-phase infra-repair-plus-parser task; get the plumbing working before worrying about parser edge cases. Phase 1: diagnose why SSH key auth silently fails — check `/home/user/.ssh/monitor_key` permissions (private keys must be `chmod 600`, sshd rejects world/group-readable keys) and verify `/app/sshd/authorized_keys` ownership/permissions match the sshd_config's expectations; test with `ssh -p 2222 -i /home/user/.ssh/monitor_key localhost` until it connects without a password prompt. Then install a per-minute cron entry (`crontab -e`, add `* * * * * /home/user/monitor_sync.sh`). Phase 2: write `/home/user/log_parser.py` to read `[UNIX_TIMESTAMP] SERVICE EVENT` lines from stdin, strictly validating the format and event value, tracking a per-service "last accepted timestamp" so any line with a timestamp strictly less than that service's last valid timestamp is silently dropped (this ordering check is the easy part to get wrong). Maintain running OK/FAIL counts and current/best consecutive-OK streak per service, then emit the sorted CSV. Since correctness is judged by bit-exact matching against a hidden reference across many fuzzed inputs, reason very carefully about edge cases (first line for a service, ties, malformed lines) rather than testing only the example.