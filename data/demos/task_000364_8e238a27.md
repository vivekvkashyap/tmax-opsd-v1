Write this as a single bash script using `awk` for both statistics and generation, since the grader will re-derive the golden output using an equivalent `awk` pipeline with a fixed `srand(42)`, so any deviation in the RNG call order or formula will produce mismatched output. First pass: filter `/home/user/raw_telemetry.csv` with `awk -F,` for rows where field 2 is exactly `T-800` and field 3 is exactly `VALID`, accumulating the sum and count of the `reading` field to get the sample mean, then a second pass (or stored array) to compute the population standard deviation (divide the sum of squared deviations by N, not N-1). Second pass: in one `awk` program, call `srand(42)` exactly once right before a loop of 5000 iterations; each iteration draws two independent `rand()` calls (`u1`, `u2`, guarding against `u1==0` before the log), applies the Box-Muller formula exactly as given with `pi=3.14159265359`, scales by `mean` and `stddev`, and prints with `printf "%.4f\n"` — do not seed inside the loop or draw extra `rand()` calls elsewhere, as that shifts the whole sequence out of sync with the reference. Write output to `/home/user/synthetic_T800.csv` and `chmod +x /home/user/generate_synthetic.sh`.
