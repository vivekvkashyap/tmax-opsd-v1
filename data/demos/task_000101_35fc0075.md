Split this into the two independent deliverables and verify each separately. For the redactor: write `/home/user/redactor.py` to read `/home/user/raw_logs.jsonl` line by line, `json.loads` each line, and mutate only the `headers` dict in place before re-serializing — preserve every other field and the original line order exactly. For `Authorization`, if the key is present replace its whole value with the literal string `[REDACTED]` regardless of scheme (Bearer/Basic/etc). For `Cookie`, don't blindly replace the whole header — split the cookie string on `;`, find the segment whose name (after stripping whitespace) is `session_id`, replace only its value while leaving the `key=value` structure and all other cookie pairs (and their order/spacing) untouched, then rejoin with `; `. Write output with `json.dumps` per line to `/home/user/redacted_logs.jsonl`, and check it against the raw file field-by-field for regressions like reordered keys or altered non-target headers. For SSH hardening: generate a passphrase-less Ed25519 keypair with `ssh-keygen -t ed25519 -N "" -f /home/user/.ssh/log_backup_key`, then append (or create) `/home/user/.ssh/config` with a `Host backup.local` stanza setting `PasswordAuthentication no` and `IdentityFile /home/user/.ssh/log_backup_key` using the exact paths given.
