Design the MPI script around correct, even data distribution and per-rank FFT peak finding rather than trying to do a single global FFT: have rank 0 load `/home/user/data/signal.npy`, use `comm.Scatter` (or scatter via `np.array_split` + `Scatterv` if the length isn't perfectly divisible by the process count) so each rank gets an equal contiguous chunk, compute `np.fft.fft` on its local chunk, take `np.argmax` of the magnitude to get the LOCAL peak index (relative to that chunk, not the global array), then `comm.gather` all local indices at rank 0 and have rank 0 print `Average Peak Index: <mean>` formatted to exactly 2 decimal places. In `run_profiling.sh`, invoke `mpiexec -n <N> python3 fft_mpi.py` for N in 1, 2, 4 (in that order), and append each run's captured stdout to `/home/user/workspace/scaling_results.txt` prefixed with `Cores: <N>, ` exactly as specified — capture the script's output with command substitution or redirection rather than assuming it prints in the right place. Make the bash script executable and run it end-to-end, then inspect the log file to confirm three well-formed lines exist, one per process count.