Build this as a pipeline of clearly separated stages rather than one monolithic script. First, stream-parse `/home/user/data/messages.jsonl` line by line and aggregate `(sender, receiver) -> sum(bytes)` into a dict; use this to build a `networkx.DiGraph()` adding edges with `weight=` the summed bytes (don't add one edge per message — collapse duplicates first, since PageRank must run on the aggregated weighted graph, not the raw multigraph). Run `nx.pagerank(G, alpha=0.85, weight='weight')`, sort by score descending then by name ascending for ties, and take the top 3. Next, filter the original messages to only those top-3 senders, group by sender, sort each sender's messages by `(timestamp, msg_id)`, and compute a running/cumulative sum of `bytes` per sender (a groupby-then-cumsum, e.g. with pandas `groupby('sender')['bytes'].cumsum()` after proper sorting — a common mistake is computing cumulative sum across all senders globally instead of per-sender). Then merge back, sort the whole filtered set globally by `(timestamp, msg_id)`, and slice index 5:10 for page 2 (0-indexed, size 5). Write exactly the five required fields per object to `/home/user/report.json` as a JSON array.
