Since the binary is stripped and UPX-packed, don't try to statically reverse it — treat it as a black box and reverse-engineer its graph semantics by feeding it small hand-crafted CSVs and observing exit codes. Start by testing a trivial two-node cycle vs. an acyclic case to confirm the direction convention: for `HELD` rows, hypothesize an edge from resource→transaction (resource is available and consumed by the transaction holding it), and for `WAITING` rows, an edge from transaction→resource (transaction is blocked waiting on resource); verify this hypothesis with several targeted test CSVs comparing your prediction to the binary's actual exit code before committing to it, since getting the edge direction backwards will invert deadlock detection. Once confirmed, implement standard DFS cycle detection in C++ (visited + recursion-stack sets, or Tarjan's SCC) over this bipartite directed graph built from `transaction_id`/`resource_id` nodes, returning exit code 1 on any cycle found, else 0. Compile to `/home/user/detector` and cross-validate against `/app/deadlock_detector` on a range of self-generated graphs (including multi-node cycles and disconnected components) before trusting it against the hidden corpus.