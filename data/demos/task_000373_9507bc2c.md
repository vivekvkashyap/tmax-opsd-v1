First transcribe the audio at /app/bug_report.wav with a speech-to-text tool to obtain the three corrupted row indices — these are given by the recording, not something to infer from the CSV, so don't guess based on which rows "look" like NaNs since the CSV itself is plain integers. Load /app/dataset.csv, drop exactly the three 0-indexed rows named in the transcript, and keep the rest as integers. For the covariance, remember population covariance divides by N (not N-1, which is numpy's default `ddof=1`) — compute it explicitly or pass `bias=True`/`ddof=0` if you prototype in Python before porting the math to C. Write the C TCP server carefully: parse newline-terminated ASCII commands from the socket (COV vs TRACE), format all floats to exactly 3 decimal places with a trailing space/newline as shown in the spec, and make sure the server can be tested with tools like `nc`/`printf | nc`. Compile to /home/user/data_server, launch it detached (e.g. with `&` plus `disown` or `nohup`) so it keeps listening on 127.0.0.1:8080 after your session ends, and verify both commands manually before finishing.
