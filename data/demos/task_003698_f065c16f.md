Structure `/home/user/pipeline.py` as four clearly separated functions/steps that each append their own `Step <name> completed` line to `/home/user/outputs/pipeline.log` (create the outputs dir first with `os.makedirs(exist_ok=True)`). Step 1: load the JSON list and dedupe by `id` keeping the first occurrence (e.g. iterate and only add to a dict if the key isn't already present — do NOT let a later duplicate overwrite the earlier one, since the field values differ between the two P001 duplicates). Step 2: apply `unicodedata.normalize('NFKC', desc)` (which handles full-width chars and ideographic spaces too, not just accents), then `.strip()`, then collapse any run of whitespace (including the NFKC-normalized ideographic space `　`→ascii space) via `re.sub(r'\s+', ' ', desc)`. Step 3: after normalization, do case-insensitive substring checks for the Footwear/Apparel keyword sets (including the Japanese substrings, which don't need case-folding) to set `category`, defaulting to "Other". Step 4: inner-join with the CSV on `id` (pandas `merge(how='inner')`), select exactly the 5 required columns in order, and write with `to_parquet` (install `pyarrow` if missing). Verify column dtypes and row count against the input files by hand before finishing — a common bug is normalizing before dedup or joining before cleaning, which changes results.
