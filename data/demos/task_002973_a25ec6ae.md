Before touching anything, read `log_likelihood` in `mcmc.c` carefully — the bottleneck is that it reopens and re-reads `/home/user/data.txt` from disk on every single call (called on every proposal, tens of thousands of times), even though the data is already loaded into the `data` array in `main` and passed in as a parameter that's simply unused. Fix it to compute the log-likelihood directly from the passed-in `data`/`n` array instead of touching the filesystem at all — do not change `my_rand`/`my_randn`, the LCG seed, the proposal step sizes, or the Metropolis acceptance logic, since any change there breaks bit-for-bit reproducibility of the sample trace. Compile with `gcc -O2 -lm -o /home/user/mcmc_opt /home/user/mcmc.c` and redirect its stdout to `trace.csv` (it already prints `mu,sigma` per line — confirm whether it also needs a header row matching how you'll parse it downstream). For the statistics, load the CSV, discard the first 1000 rows as burn-in, take the mean of the remaining 9000 mu and sigma values, and format each to exactly 2 decimal places when writing `results.txt`. For the plot, a simple matplotlib scatter of the post-burn-in (mu, sigma) pairs saved via `savefig` to the exact path satisfies the requirement.