Use `git bisect` to automate the search: `git bisect start`, mark the current main/HEAD as bad and v1.0.0 as good, then write a tiny script that runs `go test ./...` in the repo and exits 0/1 accordingly, and drive the whole search with `git bisect run <script>` so git checks out and tests each candidate commit for you rather than doing it by hand. Once bisect reports the first bad commit, capture its full 40-char SHA with `git rev-parse` before running `git bisect reset`, and write that SHA into bad_commit.txt. Then checkout main, and look in parser.go for a loop bound or index into a split/sliced value that isn't properly guarded — an off-by-one loop condition or an unchecked field-count assumption is the usual shape of this bug — and fix it defensively without touching parser_test.go. Re-run `go test ./...` on main until it passes with exit code 0.
