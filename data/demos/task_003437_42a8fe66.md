Begin by transcribing `/app/voicememo.wav` (install a speech-to-text tool such as `openai-whisper` or `SpeechRecognition`, or use `ffmpeg` plus a local ASR model) to recover the exact archive format spec — magic bytes, field widths/endianness for name length and file size, and the exact zip-slip sanitization rule — before writing any C code, since guessing the format will fail the fuzz comparison. Write the C reader to: read the magic from stdin and exit(1) immediately if it doesn't match; then loop reading one entry at a time, checking each read's return value so truncated/EOF data causes a clean exit(0) instead of processing garbage or crashing (use `fread`/`read` and bail as soon as a short read is detected, even mid-field). Apply the sanitization rule to the filename before printing — skip the entire entry silently if it matches, still consuming the entry's data bytes from stdin so subsequent entries parse correctly. Compile with `gcc -O2 -o /home/user/archive_tool` and self-test against hand-crafted inputs (valid, truncated, wrong magic, and slip-path cases) to check exit codes and stdout formatting before relying on the fuzz harness.
