Parse `/home/user/server.log` with simple regexes rather than manual string slicing: search for the 32-character hex string following the AES key leak marker (e.g. `re.search(r'([0-9a-f]{32})', line)` on the line that mentions the key), and separately find every CSP-nonce DEBUG line, taking the *last* (most recent) match rather than the first if there are multiple. Build the script string by substituting the extracted nonce into the exact literal template given, base64-encode it, and embed that as the `payload` value in the exact JSON structure specified (mind key order/spacing doesn't matter since it'll be re-parsed as JSON). For encryption, use the `cryptography` library's `Cipher(algorithms.AES(key_bytes), modes.CBC(iv))` with PKCS7 padding (`padding.PKCS7(128)`) — decode the hex key to raw bytes first (`bytes.fromhex`), generate a fresh random 16-byte IV (`os.urandom(16)`), and concatenate `iv + ciphertext` *before* base64-encoding the combined bytes, not each separately. Compute the MD5 of the resulting base64 token *string* (its UTF-8 bytes), not of the raw ciphertext, and write the final `token=...&checksum=...` line with no trailing newline.
