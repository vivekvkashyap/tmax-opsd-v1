Implement the checks strictly in the specified order, since seccomp strict mode makes later steps unforgiving of mistakes. Validate `AUTH_TOKEN` via `getenv` and `strcmp` before anything else, exiting 1 on mismatch. Validate the path with `strncmp` against the required prefix and a substring search for `..`, exiting 2 on violation — do this before opening the file. Open the file with `open()`/`fopen()` and exit 3 if it fails, but do all buffered I/O setup (and any other libc calls that might touch `brk`/`fstat`/`mmap` internally) BEFORE calling `prctl(PR_SET_SECCOMP, SECCOMP_MODE_STRICT, 0, 0, 0)` — strict seccomp only allows `read`, `write`, `_exit`, and `sigreturn`, so after enabling it use raw `read(fd, buf, n)`/`write(STDOUT_FILENO, buf, n)` in a loop, never `printf`/`fread`/`exit()` (use `_exit` or ensure no atexit handlers run). XOR each byte with `0x5A` as you read it, write immediately, then call `_exit(0)`. Test incrementally: verify each exit code case first, then verify seccomp doesn't kill the process on the final read/write/encrypt path.
