Build the ETL as clearly separated stages you can test individually. For extraction, use a `multiprocessing.Pool` mapping a worker function over the list of CRM file paths; in each worker apply regexes like `Customer ([\w\s]+?)(?: called| sent)` for names and a standard email pattern for addresses, being careful the name regex doesn't swallow trailing words before "called"/"sent". For masking, keep the email's first character then `***@domain`, and for credit cards keep only the last 4 digits, replacing everything before with `*` of the same total length as the removed portion. For the similarity join, implement or import Levenshtein edit distance and pair a CRM record with a transaction record only when the distance between their names is <= 2 — iterate all CRM×transaction pairs (data is small) rather than assuming a 1:1 ordering. Load transaction JSON files, apply the same cc-masking there, then emit the joined rows with the exact header and column order given, sorted alphabetically by `tx_id`, to `/home/user/output/joined_data.csv`. Run the script and inspect the CSV output before finishing.