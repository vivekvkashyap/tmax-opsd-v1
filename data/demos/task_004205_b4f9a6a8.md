Solve this with a single-pass awk script rather than multiple tool invocations, since that keeps rounding and field parsing consistent. Skip the header row (NR==1{next}), and for each data row compute the squared Euclidean distance to the target profile (85.0, 20.0, 60.0) as (cpu-85)^2 + (mem-20)^2 + (disk-60)^2 using awk's arithmetic — don't take a square root, since squared distance preserves the same ordering and avoids float precision noise. Track the minimum distance and corresponding hostname in awk variables as you go (initialize min to a very large number or the first row's distance), and print only the winning hostname at END. Pipe that directly into the output file with `>`, and be careful not to introduce a trailing newline issue or extra whitespace — use `printf` instead of `print`, or `echo -n`, and confirm with `cat -A` or `wc -c` that there's no stray newline or spaces. Recompute the distances by hand for the given numbers to sanity-check which server should win before trusting the script's output.
