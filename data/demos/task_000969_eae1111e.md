Start by reading the ERD image at `/app/schema.png` carefully (open/view it) to recover the exact table and column names — do not assume names like `accounts`/`transfers`; the schema must be read off the image itself. Then open `/home/user/auditor/src/main.rs` and inspect the SQL: a 3-way self-join over a transfers table that only imposes two of the three chain conditions (e.g. `t1.receiver = t2.sender` and `t3.receiver = t1.sender` but omits `t2.receiver = t3.sender`) is an implicit cross join that inflates and slows the result — add the missing join predicate so all three edges actually form a closed A→B→C→A cycle using the real column names from the ERD. Sort by the first account's ID ascending, `LIMIT 1000`, and write results to `/home/user/cycles.csv` per the existing Rust output code. Optionally add indexes on the sender/receiver columns to speed the self-join. Compile with `cargo build`, run it, then sanity-check a few rows of `cycles.csv` manually against the raw DB before assuming correctness — the automated F1 check requires near-exact match against a hidden ground truth.
