Avoid any approach that decodes/dumps every video frame to disk — that's what makes the original script slow. Instead: (1) use a fast packet inspection tool (`tshark` or `tcpdump -r`) with a display filter for TCP SYN packets to destination port 1337 against `/app/trigger.pcap`, and extract the packet's relative timestamp (e.g. `tshark -r file -Y "tcp.flags.syn==1 and tcp.flags.ack==0 and tcp.dstport==1337" -T fields -e frame.time_relative`, taking the first match). (2) Feed that timestamp directly into ffmpeg's `-ss <time>` seek option placed *before* `-i /app/camera.mp4`, then use `-frames:v 1` to grab exactly one frame and write it to `/home/user/evidence.jpg` — this seek-then-decode-one-frame approach is what keeps the whole script under a few hundred milliseconds. Test end-to-end by timing the script and comparing the output image visually/structurally against a manually extracted reference frame at the same timestamp to confirm accuracy before finalizing.
