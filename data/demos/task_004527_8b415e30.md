Chain independent diagnostic techniques, verifying each before moving to the next. Run `tesseract /app/architecture.png stdout` (or via a temp txt) and read the two port values from the output text yourself — don't assume standard defaults. For the regression: `cd /app/backend && git bisect start HEAD v1.0`, then at each step run/inspect `worker.js` (e.g. exercise `calculateData` or check for the infinite-loop pattern) and mark `git bisect good`/`bad` until git reports the first bad commit; record its full SHA with `git rev-parse HEAD` into `/app/bad_commit.txt`, then `git bisect reset`, fix the loop condition in `worker.js` (replace the unbounded `while` with a proper bounded loop), commit the fix, and make sure you land on `main` with that fix applied (not on a detached HEAD from bisecting). For the frontend, run `strace -f -e trace=open,openat,write python3 server.py` to see which syscall fails (look for `EACCES` on a file path) and fix `server.py` accordingly (e.g. write logs somewhere writable instead of the failing path) rather than guessing. Finally launch both services in the background with the correct `FRONTEND_PORT`/`BACKEND_PORT` env vars and confirm `/status` and the raw TCP `ping` both respond successfully.
