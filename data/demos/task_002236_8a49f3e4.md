Fix dependencies before anything else: open `/app/datacleaner/setup.py`, remove the nonexistent `broken-dep-123` entry from `install_requires`, then `pip install -e /app/datacleaner` (or equivalent) so `from datacleaner.utils import clean_features` actually imports. Critically, apply `clean_features` to your training features *before* fitting the model — the model must be trained on the same transformed feature space the filter script will use at inference, or predictions will be systematically wrong on held-out data. For the classifier, don't just fit once and hope: use cross-validation (e.g. `GridSearchCV` or `StratifiedKFold`) over a small hyperparameter grid for the RandomForest/GradientBoosting model until you get perfect separation on the training data — since the task's underlying decision boundary is a simple function of the (transformed) features, a well-tuned tree ensemble should reach 100% easily. Pickle the fitted model to `/home/user/model.pkl`. In `filter.py`, parse `--input`/`--output` with argparse, load the pickled model, read the CSV, apply `clean_features` (not the raw features) to get model inputs, predict, and write only rows predicted clean, preserving the `f1,f2,f3` header. Test your own script against a synthetic input CSV that resembles the train_data distribution before trusting it against the hidden corpora.