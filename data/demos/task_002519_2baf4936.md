First recover the signature: use `ffprobe`/`ffmpeg` to inspect `/app/incident_record.mp4`'s streams, then extract the subtitle track (Stream #0:2) — e.g. `ffmpeg -i /app/incident_record.mp4 -map 0:2 -f srt subs.srt` — and parse the resulting text with `grep`/`sed`/`awk` to pull out the hex byte sequence it announces; convert that hex string into the exact raw byte pattern you'll search for. Then write `/home/user/classifier.rs`: set up a small cargo project (or rustc invocation) that links `memmap2`, `mmap`s the input file given as `argv[1]`, and does a byte-wise substring search of the mapped bytes for your extracted signature (a naive sliding-window scan is fine; watch for the signature possibly straddling read boundaries — mmap avoids that since the whole file is one contiguous view). Return exit code 1 if found, 0 if not, and make sure no other exit paths (panics, missing file) leak a wrong code. Compile with `rustc`/`cargo build --release` to the exact path `/home/user/classifier`. Finally walk `/app/corpus/incoming/` recursively, invoke your classifier per file, and create hard links (`ln`, not `ln -s`) into `/home/user/repo_verified/` for clean files and symlinks (`ln -s`) into `/home/user/repo_quarantine/` for evil ones — creating both directories yourself first.
