Follow the spec's exact procedure step by step rather than approximating, since the grader does an exact-string comparison against a canonical run. Merge `features.csv` and `targets.csv` on `sample_id` with pandas, preserving row correspondence. For alpha selection, loop over `[0.1, 1.0, 10.0]`, and for each build a fresh `KFold(n_splits=5, shuffle=True, random_state=42)` and call `cross_val_score(Ridge(alpha=alpha), X, y, cv=kf, scoring='neg_mean_squared_error')`, negate and average to get MSE, keeping the alpha with the lowest MSE — use strict `<` comparison so ties keep the first (smallest) alpha checked, matching iteration order over the list. For the bootstrap, iterate `i` from 0 to 99, call `sklearn.utils.resample(df, random_state=i)` on the *joined* dataframe (not just X/y separately, so id-row correspondence is preserved), refit with the best alpha, and reuse the identical `KFold(n_splits=5, shuffle=True, random_state=42)` pattern per iteration — reusing the same random_state values matters for bit-exact reproducibility. Compute 5th/95th percentiles with `numpy.percentile` on the 100 MSE values, round to 4 decimals, and write the three-line text file in exactly the format shown, matching field labels and spacing precisely.