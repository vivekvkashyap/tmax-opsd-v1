Structure analyze.py around `MPI.COMM_WORLD`, with rank/size read at the top and every rank-0-only step (Shapiro test, SVD, plotting, JSON write) explicitly guarded by `if rank == 0:` after a `comm.gather`/`comm.Gather` call — a common bug is having all ranks redundantly do the reduction work or, worse, having non-root ranks try to write the same output file. Split sensor indices deterministically as `range(rank*25, (rank+1)*25)` (25 = 100/size for size=4) so the partition is exact and order-preserving when gathered back with `comm.gather(local_results, root=0)` (which preserves rank order, so concatenate gathered lists in rank order to reconstruct the 100-length array). For each local sensor, compute `np.fft.rfft` and pick the peak via `freqs[np.argmax(np.abs(fft_vals))]` using `freqs = np.fft.rfftfreq(1024, d=1/1024)` — get this d value right, it's easy to invert. On rank 0: run `scipy.stats.shapiro` on the full 100-length peak-frequency array, run `np.linalg.svd(data, full_matrices=False)` on the original 100x1024 matrix (not on gathered FFT results) and take `S[:3]`, plot `Vh[0]` over the 1024 samples, and dump the JSON with exact key names. Test first with `mpirun -n 1` to debug logic serially, then confirm with `-n 4`.
