Do not guess the rules — first actually read the image at `/app/optimization_rules.png` (view it or OCR it, e.g. with `tesseract` if available) to extract the exact three constraints on path depth, ordering, and pagination limit as specified in that slide; base every regex on what you actually read there, not assumptions. Then write `query_validator.py` using `re` with case-insensitive matching to: (1) find variable-length path patterns like `[*..N]` or `[*M..N]`, extract the upper bound number, and reject if it exceeds the depth limit from the image or if the path is unbounded (`[*]` with no numbers); (2) require an `ORDER BY` clause to be present; (3) require a `LIMIT` clause whose numeric value doesn't exceed the image's stated max. Handle whitespace/case variation robustly (e.g. `\s*` between tokens, `re.IGNORECASE`). Print exactly `VALID`/`INVALID` and exit 0/1 accordingly. Before finishing, run the script against every file in `/app/clean/` and `/app/evil/` and confirm all clean pass and all evil fail — iterate on the regexes for edge cases (extra whitespace, mixed case keywords, missing bound in `[*..N]` syntax) until 100% pass.