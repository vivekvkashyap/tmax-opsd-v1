Split this into infrastructure repair and modeling, and validate each stage before moving to the next. First open `/app/docker-compose.yml` and diff the MLflow service's env against what Postgres and MinIO actually expose: fix `MLFLOW_BACKEND_STORE_URI` to point at Postgres's real port (5432, not whatever wrong port is configured) with correct credentials, and fix the MinIO endpoint URL/access-key/secret-key to match the `minio` service's actual settings; bring the stack up with `docker-compose up -d` and confirm `curl http://localhost:5000` responds before proceeding. For the modeling part, load `/home/user/train_data.csv`, impute NaNs per-column with `SimpleImputer(strategy='mean')`, apply `PCA(n_components=10)` (no scaling step first, as the columns are already commensurate), then fit `Ridge(alpha=1.0)` — wire these into a single `sklearn.pipeline.Pipeline` so it can be `joblib.dump`ed as one object to `/home/user/model.pkl`. Compute training MSE and log it plus an experiment named `Feature_Engineering_Exp` to the MLflow tracking URI `http://localhost:5000` via `mlflow.set_tracking_uri` / `mlflow.start_run` / `mlflow.log_metric`. Since a hidden test set checks R^2 >= 0.75, sanity-check your pipeline's in-sample fit and PCA component count before finalizing.