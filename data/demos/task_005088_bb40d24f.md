Read the script first and trace the exact bug: the loop range and an index access must both be checked — a `range(1, n)` style loop starting at 1 instead of 0 produces the wrong count and a later `table[n-1]` access is what throws IndexError, so fix the loop bounds so it iterates over all 50 indices from 0 to 49 inclusive. Separately, look for any intermediate rounding/truncation (e.g. `math.floor(x * 100) / 100`) applied to the computed value before formatting — remove that truncation entirely and let the raw floating-point result from the decay formula flow straight into the final `.6f`-style string formatting, since truncating to 2 decimals before formatting to 6 decimals destroys precision even though it looks correct. After fixing both issues, run the script and confirm the output file has exactly 50 lines via `wc -l`, and spot-check the first and last values make sense for exponential decay (starts at 1.0, decreases monotonically).