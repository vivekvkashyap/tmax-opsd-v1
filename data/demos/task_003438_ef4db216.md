First run OCR (`tesseract`/`pytesseract`) on `/app/spec.png` to recover the exact 8-character magic header string — don't hardcode a guess. For multi-line record parsing, treat a line matching the `[YYYY-MM-DD HH:MM:SS] LEVEL` timestamp pattern (regex anchored at line start) as the start of a new record; all subsequent lines belong to that record until the next timestamp line or EOF. For safe rotation while the writer is actively appending: open the log with an exclusive `fcntl.LOCK_EX`, read its full current contents, determine how many complete records are present (be careful not to cut a record that might still be mid-write — only take fully-terminated records, e.g. up to the last complete timestamp boundary before EOF unless EOF itself is stable), truncate/rewrite the remaining unconsumed tail back to the file (or use atomic temp-file swap) before releasing the lock, so no writer append is lost mid-operation. For output, prepend the exact magic bytes (as raw bytes, not text) to the gzip-compressed (`gzip.compress`/`zlib` with gzip wrapper) extracted text and write to `/home/user/archive/app_rotated_N.arc`. Run the rotator repeatedly (loop with short sleeps) while the background writer is active to accumulate the required ≥0.95 recovery ratio, and verify by decompressing your own `.arc` files and counting timestamp lines against expectations.
