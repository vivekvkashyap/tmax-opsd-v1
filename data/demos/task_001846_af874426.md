Structure the Rust program around three distinct concerns: directory walking, encoding conversion, and linking. Use `std::fs::read_dir` recursively (or the `walkdir` crate if available) to find every `.log` file under `/home/user/dataset/`. For each file, read the raw bytes and decode as UTF-16LE — read `u16` little-endian code units from the byte buffer and use `String::from_utf16` (or `char::decode_utf16` for lossy handling), do not just reinterpret the bytes as UTF-8 since that will produce garbage/null-byte-laden output, which is exactly what the verification checks against. Apply the literal string replacement of `ERROR_CODE_99` with `RESOLVED_01` on the decoded UTF-8 text. Write the result to `/home/user/processed/<basename>` as proper UTF-8, then create a hard link (via `std::fs::hard_link`, not a symlink or copy) at `/home/user/links/<basename>` pointing to the just-written processed file — verify with `stat -c '%i'` that both paths report the same inode. Create the output directories up front if they don't exist, compile with `rustc process.rs -o process`, and run it before checking results.