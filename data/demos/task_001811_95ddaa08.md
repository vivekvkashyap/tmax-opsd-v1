First inspect the SQLite schema (`sqlite3 /app/etl_data.db ".schema"`) to learn the actual table/column names for the parent-child hierarchy — don't assume names. Note that a prior migration corrupted the index metadata (the on-disk index no longer matches its declared column), so before querying run `sqlite3 /app/etl_data.db "REINDEX;"` (or drop/recreate the index) inside your script every time, since stale index state can silently return wrong rows. Recover the batch code by running `tesseract /app/run_label.png stdout` (or similar OCR) and trimming trailing whitespace/newlines with e.g. `tr -d '\n' `/`sed`. Write `/home/user/process_node.sh` to accept `NODE_ID` as `$1`, run a recursive CTE (`WITH RECURSIVE subtree(id, val, depth) AS (SELECT ... WHERE id = NODE_ID UNION ALL SELECT child.., depth+1 FROM nodes JOIN subtree ON ... )`) to walk descendants, then `SELECT SUM(metric_value), MAX(depth) FROM subtree` in one sqlite3 call. Parse the pipe-delimited result and print exactly one line in the format `[BATCH_CODE] Node NODE_ID -> Total: SUM_VALUE, MaxDepth: DEPTH`, suppressing all other sqlite3/tesseract output (redirect stderr, use `-batch -noheader`). Test against several NODE_IDs manually to confirm output matches the expected single-line format before relying on the fuzz verifier.