Since you have black-box access to the real vulnerable binary, use it as your oracle rather than guessing at rules: feed the `/app/samples/` examples to `/app/legacy_processor` and also run `strings /app/legacy_processor` (and objdump/gdb if needed) to confirm the expected `USER:`/`ACTION:`/`DATA:` line structure and where the DATA field gets passed to a shell (look for calls resembling `system`/`popen`/`sprintf` patterns in the symbol/string output). Then write `/home/user/filter.cpp` to strictly validate structure first (exactly three lines matching the USER/ACTION/DATA prefixes, correct character classes for USER and ACTION) and reject anything with structural anomalies, then scan the DATA field specifically for shell metacharacters — build an explicit denylist covering at least `; | & $ \` < > \n` and any other characters that could enable command injection, since injection filters are graded against a large hidden corpus and must have zero false negatives. Read all of stdin (don't stop at the first newline if DATA itself could span lines) before deciding. Compile exactly as instructed with `g++ -O2 -o /home/user/filter /home/user/filter.cpp`, then self-test against the sample clean/evil files in `/app/samples/` to confirm correct exit codes before considering it done.