Start by recovering the spec text from the image: run `tesseract /app/schema_doc.png stdout` (or via a Python `pytesseract` call) and read the OCR'd output carefully — it defines the table names, the bridging/junction table, and a filtering constraint you must not skip. Do not guess the schema from assumptions; verify it against the OCR text, re-running with `--psm` variations if the output looks garbled. The bug class you're fixing is a classic accidental cross join: naively joining `wrote` to itself on `document_id` without also joining through the `documents` table's `year` filter, or joining without excluding the researcher's own ID, silently inflates counts. Write the SQL so the year constraint from the OCR'd rule is applied via a JOIN to `documents` (not a WHERE clause on an unjoined table), exclude self-pairs, and use `COUNT(DISTINCT ...)` on the co-author id to dedupe multiple shared documents between the same pair. Build the script to take `--db-path` and `--researcher-id` via argparse and print only the bare integer — no logging to stdout (redirect any debug prints to stderr). Test locally against a small hand-built SQLite db before trusting the fuzz harness.
