Transcribe `/app/vm_auth.wav` yourself using an installed tool (e.g. `whisper /app/vm_auth.wav --model base` or similar), then normalize the output to lowercase with punctuation stripped and words separated by single spaces — verify by listening to/inspecting the transcript output rather than assuming a passcode, since the exact words are the recovered secret you must derive. In `prov_daemon.c`, write a standard blocking TCP server (`socket`/`bind`/`listen`/`accept`) bound to `127.0.0.1:8111`; per connection, read until a newline, strip it, and `strcmp` against the recovered passcode. On match: `send()` back exactly `VNC_ACL_UPDATED\n`, then open `/home/user/qemu_vnc.acl` for writing (`O_CREAT|O_TRUNC|O_WRONLY`), write exactly `VNC_ACCESS=GRANTED\n`, close it, and call `chmod("/home/user/qemu_vnc.acl", 0400)` (or `fchmod`) — set permissions strictly after writing, since creating the file with restrictive mode first can block the write itself. On mismatch, just close the socket without touching the file. Compile with `gcc -o /home/user/prov_daemon prov_daemon.c`, then launch it in the background (e.g. `./prov_daemon &`) and confirm with `ss -ltnp` or a test connection that port 8111 is actually listening before finishing.