Build `/home/user/clean.sh` as a `jq`-centric pipeline processing `/home/user/raw_data.jsonl` line by line (or via `jq`'s native line-by-line/slurp mode), and handle each transformation as a distinct `jq` step so you can debug them independently: (1) lowercase all top-level keys using `jq`'s `with_entries(.key |= ascii_downcase)` or similar — note some records use different original key names (e.g. `Name`/`eMail`/`Timestamp` vs `User_Name`/`Email`/`TIMESTAMP`), so don't hardcode a rename map, genuinely lowercase whatever keys are present; (2) Unicode escapes like `é` are automatically decoded to real characters by `jq` when it parses/re-emits JSON, so just make sure you're not double-escaping output; (3) mask the email's local part by replacing everything before `@` with literal `***`, and separately capture the substring after `@` into a new `email_domain` field; (4) normalize `timestamp`: detect whether the string is all-digits (epoch seconds — convert with `date -u -d @<epoch>` or `jq`'s `todateiso8601`) or already `YYYY-MM-DD HH:MM:SS` (convert by replacing the space with `T` and appending `Z`, treating it as already UTC). Emit one compact JSON object per line (`jq -c`) to `/home/user/clean_data.jsonl`. Test each transformation against all four sample records, including the ones missing an `email`/`timestamp` under different original casings, before considering it done.
