Treat this as an OCR-plus-scripting pipeline. First extract frames from the video with `ffmpeg` (e.g. sample frames at a rate that captures each distinct scroll position of the table without excessive duplicates), then run `tesseract` on each frame to read the `UID | USERNAME | DEPARTMENT` rows. Deduplicate rows across overlapping frames (same UID appearing in consecutive samples) and normalize case/whitespace before writing `username:department` lines (lowercase) to extracted_users.txt — OCR noise and duplicate frames are the main source of errors, so cross-check a few frames manually to sanity-check your parsing logic rather than trusting a single pass. For deploy_users.sh, use a `while IFS=: read -r user dept` loop over the file, `mkdir -p /home/user/accounts/$user`, `chmod 750` the directory, then write `.bash_profile` with `export DEPT="$(echo $dept | tr a-z A-Z)"` — keep all of this in pure bash/awk/sed as required. Since scoring is accuracy-based (>=0.85), prioritize getting permissions and file format exactly right for every user you did extract, since a malformed but present record scores zero for that user.