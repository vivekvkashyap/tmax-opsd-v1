Install `tesseract-ocr`, run it against `/app/policy.png` (e.g. `tesseract /app/policy.png stdout`), and read the output to find the exact Magic Integrity String the policy names — do not hardcode a guessed string. Write `/home/user/artifact_filter.c` so `main` takes the bundle directory path and performs checks in order, exiting 1 immediately on any failure: (1) `opendir`/`readdir` the directory, track the `.txt` entry with the newest `st_mtime` via `stat`; (2) open it, read raw bytes, and convert with `iconv_open("UTF-8", "UTF-16LE")` / `iconv()` into a UTF-8 buffer — get the byte-order and null-terminator handling right, this is the easiest step to get subtly wrong; (3) `strstr` the converted text for the OCR'd magic string; (4) find the single `.tar.gz` file and validate it via `system("gzip -t <file>")` or `system("tar -tzf <file> > /dev/null 2>&1")`, checking the exit status; (5) for every symlink in the directory (`lstat` + `S_ISLNK`), `realpath()` its target and confirm the resolved path's prefix stays within the bundle directory's own realpath — reject any that escape (e.g. via `../`). Compile with `gcc -o /home/user/artifact_filter artifact_filter.c`, then run it against every subdirectory in `/app/corpus/clean/` and `/app/corpus/evil/`, checking exit codes match expectations before finishing.
