Work bottom-up through the build chain rather than trying to fix everything blindly. First compile the C library standalone (`gcc -shared -fPIC c_src/filter.c -o libfilter.so`) and confirm it builds cleanly before touching Rust. Then inspect the Rust FFI boundary in `src/main.rs`/wherever `process_audio_chunk` is declared via `extern "C"`: the described bugs are lifetime/FFI issues, so look specifically for (a) passing a Rust slice/Vec to C without calling `.as_ptr()`/`.as_mut_ptr()` correctly, and (b) holding a borrowed reference across an `.await` point, which the Rust borrow checker will reject — restructure so buffers are converted to raw pointers only within a synchronous scope, or copy data before awaiting. Write `build.sh` to compile the C lib, export `LD_LIBRARY_PATH` to include its directory, then `cargo build`. Start the server, run `client.py` against `/app/intercepted_comms.wav`, and once `/home/user/clear_audio.wav` exists, transcribe it with a local tool (e.g. `whisper.cpp`) and save the raw transcription text verbatim to `/home/user/transcript.txt` — don't hand-type a guess at the content, let the transcription tool produce it, and listen/inspect the output for obvious ASR errors before finalizing.