Scaffold with `cargo new /home/user/frame_etl` and pick a lightweight HTTP approach (std TcpListener with manual HTTP parsing, or a small framework like `axum`/`tiny_http`) — a full async framework is overkill but is fine if it compiles quickly. Handler logic for `GET /process?sec=<N>`: (1) parse the `sec` query param from the request line. (2) Build the expected cache path `/home/user/datastore/frame_<N>.jpg` and check with `std::path::Path::exists()` — if present, skip extraction entirely (this caching behavior is explicitly tested by hitting the same endpoint twice). (3) If missing, create `/home/user/datastore` if needed and shell out via `std::process::Command` to run `ffmpeg -ss <N> -i /app/video_feed.mp4 -frames:v 1 -q:v 2 <path> -y`, waiting for it to finish before proceeding. (4) Get the file size with `std::fs::metadata(path)?.len()`. (5) Classify: `"Complex"` if size > 40000 else `"Simple"`. (6) Return a strict JSON body `{"second": N, "size": bytes, "classification": "..."}` with a 200 status and correct Content-Type. Bind to `127.0.0.1:9090` and leave it running in the background (e.g. `cargo run &` or build first then run the binary) so the test harness can curl it; manually curl the endpoint yourself twice to confirm caching and the JSON shape before finishing.