This is a black-box reverse-engineering + memory-safety task, not a from-scratch design task — treat the existing legacy binary as ground truth and derive behavior empirically rather than guessing. First get the build working: read setup.py's error message carefully, add the missing `Extension` import from `setuptools`/`distutils`, and fix whatever syntax error blocks parsing, then confirm `pip install -e .` succeeds before touching the C logic. Second, black-box probe `/app/bin/legacy_evaluator` with a range of crafted inputs (valid-looking custom-base64 strings, edge cases with underscores/hyphens, truncated/malformed strings, RPN expressions that under/overflow the stack, division by zero) piping stdin/observing stdout/exit codes, to nail down: the exact base64 alphabet mapping (compare against standard base64 to find the substitution), the RPN token/operator grammar, and the precise error-output text and exit behavior for each failure mode. Third, fix decoder.c's memory bugs: the malloc for the decode buffer must reserve room for a null terminator (`len*3/4 + 1`, or size it from actual decoded length) — under-allocation causes an out-of-bounds write; also add explicit bounds checks in the RPN stack loop (check stack has 2 elements before an operator, check final size is exactly 1) instead of trusting well-formed input. Only after the C extension matches your probed model should you wire up run_api.py to call it and compare byte-for-byte against the oracle on many test cases.