Write a single Python script rather than trying to hand-parse with shell one-liners: recursively walk `/home/user/tech_logs/` with `os.walk` or `glob(..., recursive=True)` collecting every `*.log` file. For each file, read the whole text and split it into records using a regex that matches the `=== DOC-ID ===` ... `================` block structure (e.g. `re.findall` with a pattern spanning `Type:` and `Content:` up to the closing `================`, using `re.DOTALL`), since a naive line-by-line state machine is easy to get wrong around multi-line Content sections. For each record, check `Type` equals exactly `API_DEPRECATION` (strip whitespace, avoid partial/substring matches), and capture the DOC-ID from the header line. Load `/home/user/authors.json` once with `json.load`, and look up each DOC-ID's `author` field — handle the possibility that a matched ID isn't present in authors.json. Deduplicate if the same DOC-ID could appear twice, then sort the resulting list of (DocID, Author) tuples alphabetically by DocID before writing. Use Python's `csv` module (not manual string concatenation) to write the header `DocID,Author` and rows to `/home/user/deprecation_report.csv`, so quoting/newline edge cases are handled correctly.
