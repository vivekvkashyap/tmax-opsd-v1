Start with `cargo test` in `/home/user/oscillator_fit` to see the failing assertion in `src/integrator.rs`, then read the `step` function carefully: the two branches are swapped relative to standard adaptive step-size control, so fix it so that when `error > tolerance` the step *shrinks* (`*dt *= 0.5`) and when `error < tolerance * 0.1` it *grows* (`*dt *= 2.0`) — re-run `cargo test` until it passes before touching anything else. For `src/mcmc.rs`, implement the textbook Metropolis-Hastings acceptance: compute `acceptance_prob = (proposed_log_lik - current_log_lik).exp().min(1.0)`, draw a uniform random number in `[0,1)` via `rng.gen::<f64>()`, and accept (return `true`) if the draw is less than `acceptance_prob`. After both fixes compile and `cargo test` passes, run `cargo run --release`, capture the printed `c: ..., k: ...` line verbatim into `/home/user/results.txt`, and confirm `/home/user/fit.png` was written by the plotting code in `main.rs` (which you must not modify). Do not hand-edit `data.csv` or `main.rs`.
