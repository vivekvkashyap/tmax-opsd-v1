Tackle the ELF validator and the Nginx config separately, then wire them together and test end-to-end. For the validator, `#include <elf.h>`, `mmap` or `read` the file, check the magic bytes `ELFMAG`/`e_ident` and that `EI_CLASS == ELFCLASS64` (reject anything else as evil), then walk the program header table (`e_phoff`/`e_phnum`/`e_phentsize`) looking for a `PT_GNU_STACK` entry and reject if its `p_flags` has `PF_X` set, and separately walk to find `PT_DYNAMIC`, iterate its `Elf64_Dyn` entries, and reject if any tag is `DT_RPATH` or `DT_RUNPATH`. Return/exit 1 as soon as any violation is found, exit 0 only if the file passes every check — test it directly against a few binaries you construct or find locally before trusting it on the hidden corpora. For Nginx, add `add_header Content-Security-Policy "default-src 'none'; frame-ancestors 'none';" always;` (the `always` flag matters so it applies to error responses too) at the server or location level, and add a `location /upload { proxy_pass http://127.0.0.1:9090; }` block. Start everything via `/app/start_services.sh`, then manually `curl -X POST --data-binary @<file> http://127.0.0.1:8080/upload` with both a clean and evil sample and check the status codes, headers, and that Redis received an audit entry before considering it done.