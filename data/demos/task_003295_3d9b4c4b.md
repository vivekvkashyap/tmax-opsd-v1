Approach this as a build-system debugging task: run `make` first in `/home/user/polyglot-data` and read the actual compiler/linker error before touching anything — don't guess at fixes blindly. A shared library that other code links against must be compiled with position-independent code (`-fPIC`) when building `libprocessor.so` from `processor.c`; add that flag to the Makefile rule. The `app` target links `main.c` but never tells the linker about `processor.c`'s symbols — add `-L. -lprocessor` (and ensure the shared lib is built first as a prerequisite) so `app` dynamically links against `libprocessor.so` instead of failing with undefined references. After `make` succeeds, remember a dynamically linked executable won't find a `.so` in the current directory at runtime unless you tell it where to look — export `LD_LIBRARY_PATH=.` (or the absolute path to the directory) before invoking `./app` inside `test.sh`, or the run will fail with "cannot open shared object file" even though the build succeeded. For `test.sh`, loop 10 times generating random integer `id`/`value` pairs, format them as `id:<n>,value:<n>`, invoke `./app` with each as argv[1], and append each line of stdout to `output.log`.