Build this as a pipeline of clearly separated stages in Go: (1) parse all three CSVs with `encoding/csv`, keeping headers; (2) validate by building a set of known node IDs from nodes.csv, then scanning every edge's src/dst against that set — collect any missing IDs into a Go `map[string]struct{}` to dedupe, then sort the keys alphabetically before writing one per line to validation_errors.log; (3) build your graph adjacency list while filtering edges — only add an edge if `rel == "CONNECTED_TO" || rel == "DEPENDS_ON"`, dropping everything else (this is the step naive attempts skip, causing wrong path costs); (4) implement Dijkstra with a min-heap (`container/heap`) over the filtered graph, run it once per query row; (5) accumulate total_queries, successful_paths (only count queries where a finite path exists — remember the graph is directed, so some queries legitimately have no path), and total_cost, then compute average_cost as 0.0 when successful_paths is 0 to avoid division by zero. Marshal the summary struct to JSON with `encoding/json` and write it out. Test incrementally: first verify validation_errors.log content, then verify each query's path cost by hand-tracing the filtered graph before trusting summary.json.
