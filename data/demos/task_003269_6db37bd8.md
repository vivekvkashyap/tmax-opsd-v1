Address each requirement as a discrete, testable change to `server.py`/`config.json`, then verify with your own scanner before trusting it. Fix the JWT bypass by removing the `alg == 'none' -> return True` short-circuit entirely — after base64-decoding the header, only proceed to HMAC-based signature verification (never trust an attacker-controlled `alg` field to skip verification); if `alg` is `none` (or the signature check fails for any other reason), return 401. Rotate the secret by editing `config.json`'s `JWT_SECRET` value exactly, and confirm `get_secret()` (or equivalent) re-reads it. Add the CSP header only on successful (200) `/api/data` responses — set it explicitly on the response object (e.g. `resp.headers['Content-Security-Policy'] = "default-src 'self'; script-src 'self' 'nonce-random123';"`) matching the string byte-for-byte, including trailing semicolon and spacing. For `scanner.py`, construct Request A by hand-building a JWT with `{"alg":"none","typ":"JWT"}` header and empty/any signature segment, and Request B by signing a valid header+payload with HMAC-SHA256 using the new rotated secret (base64url, no padding) — send both via `requests` to `localhost:5000/api/data`, check status codes and headers, and write the boolean results to `scan_results.json` with the exact key names specified.