Read server.c to find the exact struct layout and the vulnerable copy operation — note the fixed-size buffer field's size and what field immediately follows it in memory (struct member ordering determines what an overflow overwrites). Build the mTLS chain first: generate a client key, create a CSR signed by the attacker's CA, and confirm the server accepts your cert before worrying about the exploit. Compile the server against libssl/libcrypto, run it in the background on its port, and test with a benign payload first. For the exploit, work out how many filler bytes reach past the vulnerable buffer into the adjacent flag field, then set that field nonzero; encode raw bytes with XOR 0x5A first, then base64, matching the server's decode order (decode reverses encode, so encode order is base64(xor(data))). After a successful connection triggers the file drop, hash it with sha256sum and save only the hex digest (no filename) to the target path. Verify end-to-end against a freshly started server.
