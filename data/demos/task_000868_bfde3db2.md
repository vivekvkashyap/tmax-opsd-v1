Write a script (Python is convenient) that walks `/home/user/app_configs` recursively collecting every path ending in `.ini`. For link classification, the order of checks matters: first test `os.path.islink(path)` (or `lstat` and check the mode bits) — if true, classify `symlink`; only if NOT a symlink, check `os.stat(path).st_nlink > 1` to classify `hardlink`; otherwise `regular`. A common bug is calling `os.stat` (which follows symlinks) before checking `islink`, which would misclassify symlinks as regular/hardlink based on the target's link count — always check symlink-ness first with `lstat`/`islink`. To parse each file's version, read it as INI text and track the current `[section]` header; only capture a `version = ...` (or `version=...`) line when the active section is exactly `app`; if you exit the file without having seen `[app]`+`version`, emit `UNKNOWN`. Note that for symlinks you must still parse the *target's* file content (open() naturally follows symlinks) since the version reflects what the link points to. Sort all rows by FilePath (absolute path) before writing the CSV with header `FilePath,LinkType,Version` to `/home/user/audit_report.csv`.
