Work this in strict phases and verify each before moving on. (1) Build fix: inspect `/app/vendored_logparser-1.2.0/setup.py`'s `sources=[...]` list for the C extension and compare it against the actual filenames under its `src/` directory — a mismatched filename is a classic "silent fallback to pure-Python" bug; fix the path, then confirm success by running `pip install -e .` and checking that `import logparser.fast` (or equivalent) works without `NotImplementedError`, not just that pip exits 0. (2) Disk forensics: the crash string was deleted from an ext4 filesystem inside `/app/server_disk.img` — mount or use `fls`/`icat` (or `debugfs`) with the sleuthkit tools to list deleted inodes and recover file content, then grep the recovered text for the `CRASH_INP:` prefix to find the exact malformed input. (3) Git forensics: in `/app/internal_tools`, the oracle binary was deleted and purged from the working tree but still exists in history — use `git log --diff-filter=D --summary` (or `git log --all --full-history -- '*parser_oracle_bin*'`) to find the commit before deletion, then `git show <commit>:<path> > /home/user/oracle` and `chmod +x` it. (4) Equivalence: run the oracle on the recovered crash string and other varied inputs (quotes, colons, brackets, nulls, edge lengths) to reverse-engineer its parsing rules by observation, then hand-write a pure-Python `/home/user/fixed_parser.py` that reproduces those exact outputs — do not import the vendored package. Continually diff your script's output against the oracle's output for many probe inputs before finishing.