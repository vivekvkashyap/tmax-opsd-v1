Design the pipeline as two cooperating layers: bash for orchestration/text-munging, Python for the ML math. In `run_pipeline.sh`, parse `--max_features` and `--c_value` with a simple flag-parsing loop (shift-based `while [[ $# -gt 0 ]]` matching `--flag value` pairs), then do the lowercase/punctuation-strip step in bash/awk/sed on `dataset.csv` — lowercase with `tr '[:upper:]' '[:lower:]'`, strip punctuation with `sed`/`tr -d`, but be careful to only touch the `text` field and preserve the comma delimiters between `text` and `label` (e.g. process field-by-field with awk -F, rather than a blanket regex over the whole line, which would eat the separating commas or need explicit exclusion of `,`). Call your Python trainer with the two args, have it print only the bare accuracy float to stdout (capture with `$(...)` in bash) and pickle the model to `model.pkl` in the cwd. Generate the experiment id via `$(date +%s)`, `mkdir -p` the artifact dir, `mv` (not copy) the model and processed CSV into it, and append the CSV row using printf with the exact column order `id,max_features,C,accuracy`, creating the header first only if the file is new (`[ -f file ] || echo header > file`). Test end-to-end with the example invocation and inspect the resulting files before finishing.
