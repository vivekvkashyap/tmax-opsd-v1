Do not attempt to reverse-engineer the reference binary by disassembly — instead build a correct implementation from the well-known `inih` library semantics and let the fuzz-equivalence check validate it. First fix `/app/vendor/inih-53/Makefile`: run `make` there, notice it fails (likely a tab-vs-spaces indentation error in a recipe line, and/or a case-mismatched object filename passed to `ar`), correct the recipe formatting and the filename casing so `make clean && make` cleanly produces `libinih.a`. Then write `/home/user/artifact_parser.c` using the standard inih pattern: `#include "ini.h"`, define a handler callback matching `ini_parse_stream`'s signature that prints `[%s] %s=%s\n` for `(section, name, value)`, and call `ini_parse_stream((ini_reader)fgetc, stdin, handler, NULL)` (or `ini_parse_file(stdin, ...)` if that's what the vendored header exposes) to read all of stdin until EOF. Compile with `gcc /home/user/artifact_parser.c /app/vendor/inih-53/ini.c -I/app/vendor/inih-53 -o /home/user/artifact_parser` (or link `libinih.a`), matching stock inih's tolerant behavior on malformed input (skip bad lines silently, don't crash) since the fuzzer will throw arbitrary bytes at it including brackets, `=`, and newlines in odd combinations.