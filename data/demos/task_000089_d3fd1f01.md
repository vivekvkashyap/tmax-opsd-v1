Build this as a pipeline of `awk`/`sort` stages rather than one monolithic script, and process in this order: (1) trim whitespace and lowercase `param_name` per field (awk's `gsub(/^[ \t]+|[ \t]+$/,"",x)` on each field, `tolower()` on the param column), (2) filter to only rows where param_name equals `max_workers`, (3) resolve same-timestamp conflicts by keeping the max value per timestamp — group with awk into an associative array keyed by timestamp, updating only when a larger value is seen, (4) once you have one value per observed timestamp, determine min/max timestamp and generate the full 10-second-step sequence between them, forward-filling any timestamp not present in your deduped map using the last-seen value as you iterate the sequence in order (a common bug is forward-filling from the *previous generated row* instead of tracking a running "last known value" variable that persists across gaps — make sure a gap of multiple missing steps all get the same carried-forward value, not a chain of copies), (5) compute `delta` as current minus the immediately preceding output row's value, with the first row's delta forced to 0. Write the header first, then the sorted rows, to `/home/user/clean_config.csv`. Diff your output against your own manually worked-out expected values for a few rows to catch off-by-one errors before finishing.