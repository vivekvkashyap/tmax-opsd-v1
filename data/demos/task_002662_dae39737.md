Split this into a build fix and a statistical classifier. For the build: run `make` in `/app/simkit-1.2/`, read the compiler error — an undefined reference to a math function (like `sqrt`) means the linker needs the math library; edit the Makefile's compile line to append `-lm` after the source file (link order matters: `-lm` must come after `sim.c` on the gcc command line), then rerun `make` to confirm `sim` is produced. For the classifier: inspect a handful of files from both `/app/corpus/clean/` and `/app/corpus/evil/` to understand the numeric distributions (e.g. compute mean/variance/percentiles with `awk` or a quick Python one-liner) before picking a rule — don't guess a threshold blindly. Since evil data has similar mean but higher variance/outliers, a robust discriminator such as sample variance, the 95th percentile of |value - mean|, or an IQR-based outlier count is more reliable than a naive mean-based check. Implement `/home/user/classifier.sh` with an inline `awk` or `python3` computation, compare against a threshold chosen from your inspection (with margin so it generalizes to the hidden corpus, not just the visible 50/50 files), exit 0 for clean and exit 1 for evil, and `chmod +x` the script. Validate against every file in both provided directories before finishing.