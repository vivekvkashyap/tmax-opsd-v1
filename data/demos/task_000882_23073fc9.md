Do this in two clearly separate passes over each archive, both without ever extracting files to disk. Pass 1 (safety scan): for every `*.tar.gz` in `/home/user/incoming_logs/`, list its member paths with `tar -tzf <archive>` and check each path string against the three unsafe patterns — starts with `/`, starts with `../`, or contains `/../` — classifying the whole archive as MALICIOUS if any member matches. Pass 2 (streaming parse of safe archives only): for safe archives, stream file contents with `tar -xzO -f <archive>` (piped, never writing extracted files) into an awk/bash parser that tracks a state machine across lines: start accumulating on an `ERROR_START` line, watch for `Disk-Impact: <int>` lines while inside a block, and reset on `ERROR_END`, summing all Disk-Impact integers found across all safe archives into one running total. Compute `sha256sum` of each safe archive file itself (not its contents) for the manifest. Sort SAFE_ARCHIVES and MALICIOUS_ARCHIVES lines alphabetically by filename, and assemble `/home/user/processed_manifest.txt` matching the exact section headers, spacing, and blank-line layout given in the spec — a diff-based check will likely be used, so match formatting exactly rather than approximately.