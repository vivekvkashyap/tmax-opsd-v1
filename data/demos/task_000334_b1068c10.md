Two independent sub-problems: recover a hidden timestamp from video, then build a generic file sanitiser. For the video, don't guess the timestamp — extract the actual frame at the 4-second mark with `ffmpeg -i /app/rotation_trace.mp4 -ss 4 -frames:v 1 out.png` (or step through nearby frames if timing is imprecise) and read the on-screen `TIMESTAMP=YYYY-MM-DD_HH-MM-SS` text via OCR (e.g. `tesseract`) or careful visual inspection of the saved frame; use that recovered value to locate the matching `config_<timestamp>.conf` inside `/app/backups.tar.gz` (`tar -tzf`/`tar -xzf` to inspect member names) and confirm it's free of redirection/pipe characters. For `/home/user/sanitiser.py`, build reusable validation functions rather than one-off checks: scan each `.conf` file's values for characters/substrings like `>`, `<`, `|`, `>>`, `&>`; parse referenced paths and confirm (via `os.path.normpath`/prefix check, not naive string matching, to catch `../` tricks) they resolve under `/var/log/` or `/etc/app/`; check `os.path.getsize` against the 1MB limit and `os.stat().st_mode` for executable bits. Emit `{filename: "ACCEPTED"|"REJECTED"}` as JSON to stdout, then self-test against representative good/bad configs before trusting it against the hidden corpus.