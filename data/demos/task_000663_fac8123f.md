First recover the business rule by transcribing the audio: use a locally available tool (e.g. `whisper /app/voicemail.wav` or an ffmpeg+speech-recognition pipeline) to get the exact wording of the staleness condition — don't guess at the rule from the task description alone, since the precise comparator and which field triggers rejection is only stated in the recording. Once you have the transcript, implement the check as a script (Python invoked from a bash wrapper is fine, since `/home/user/check_payload.sh` just needs to be executable and take one JSON path argument) that recursively walks the `dependencies` tree: for every node, compare each direct child's `last_modified` against that same parent's `last_modified`, and combine that comparison with the child's `status` field exactly as the voicemail specifies — get the direction of the inequality and which side (parent vs child) is "older" right, since a naive implementation often flips this or checks status of the wrong node. Recurse into all depths (a node deep in the tree can independently trigger staleness) and short-circuit to exit 1 as soon as any violation is found; otherwise exit 0. Test your script against both directories in `/app/corpora/clean/` and `/app/corpora/evil/` before considering it done, checking exit codes with `echo $?` after each run.