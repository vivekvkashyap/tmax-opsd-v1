First run `tesseract /app/schema.png stdout` (or similar OCR invocation) to extract the routing rule text embedded in the diagram, and read the resulting text carefully to identify the exact sequence/ordering rule it specifies — do not guess or reuse the old sequence baked into `/app/legacy_router`, since the task explicitly requires the NEW sequence from the image. Once you have the rule, implement `/home/user/router.sh` in pure Bash: split the single comma-separated argument into an array (`IFS=',' read -ra fields <<< "$1"`), validate it has exactly 5 elements (exit 1 otherwise), then reorder/merge/format the fields according to the OCR'd sequence and print the result. Before trusting the fuzz-equivalence verifier, hand-test your script against several inputs and manually trace through the sequence rule to confirm the output ordering and formatting (e.g. delimiter, casing) match what the rule describes — small formatting mismatches (extra separators, wrong join character) are the most common way this kind of task silently fails equivalence checks against the hidden oracle.
