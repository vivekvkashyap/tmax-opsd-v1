Work through the three broken layers independently, testing each before moving to the next. (1) Nginx: read `/app/nginx.conf`'s existing `location` blocks and fix the `proxy_pass` targets so `/api/v1/eval/legacy` points at `127.0.0.1:5000` and `/api/v1/eval/fast` points at `127.0.0.1:5001` — don't just swap symptoms, verify the whole block syntax with `nginx -t`. (2) The interpreter: in `/home/user/rpn_evaluator.c`, trace the stack `push`/`pop` pointer arithmetic by hand against a 2-3 token expression — a classic off-by-one is incrementing/decrementing the stack-top index on the wrong side of the read/write (pre- vs post-increment mismatch between push and pop). Fix so push writes then advances and pop retreats then reads (or vice versa, but consistently). For version comparisons, stop using `strcmp` entirely: split each version string on `.` into three integers and compare Major, then Minor, then Patch numerically in order, short-circuiting on the first difference — this is what makes `10.0.0 > 2.0.0` correct. (3) Compile the CLI and web service exactly as specified, then sanity-test `/app/rpn_cli` yourself on hand-picked expressions (integer arithmetic and version comparisons) against expected results you compute by hand before trusting the fuzzer, and confirm end-to-end parity by curling both nginx routes with the same expression.
