Recover the thresholds from the image first, don't hardcode guesses: run `tesseract /app/thresholds.png stdout` (or equivalent OCR) and parse out the per-port maximum-duration values it reports, building a lookup table (port -> max_duration) from that extracted text rather than typing in numbers you assume. Then write `/home/user/metric_processor.sh` as a `while read -r ip port duration; do ... done` loop over stdin: for each line, look up `port` in your extracted table; if absent, print `UNKNOWN IP PORT DURATION`; if present, compare `duration` against the threshold with strict `-gt` for ALERT and `-le` for OK, printing `ALERT IP PORT DURATION` or `OK IP PORT DURATION` accordingly, preserving the original three fields verbatim in the output. Make the script executable with `chmod +x`. Before finishing, sanity-check your script's behavior against the provided black-box reference program by piping the same synthetic "IP PORT DURATION" lines into both it and your script and diffing the outputs, adjusting your extracted thresholds/logic until they match exactly across edge cases (duration exactly at threshold, unknown ports).
