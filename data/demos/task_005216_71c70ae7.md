Set up a Rust binary crate with `cargo new organizer`, add `serde` (with `derive`) and `serde_json` to `Cargo.toml`, and define structs matching the manifest fields (`name`, `lang`, `port: Option<u16>`, `deps: Vec<String>`, `route: Option<String>`) to deserialize `manifest.json`. Implement Kahn's algorithm for the topological sort: build an indegree map and adjacency list from `deps`, repeatedly pop nodes with indegree 0, and to make the sort deterministic when multiple nodes are ready simultaneously, process ready nodes in the manifest's original declaration order (or sorted) rather than arbitrary HashMap iteration order — this is the most common source of nondeterministic/incorrect output order. Write the comma-joined names to `build_order.txt`. For `build_all.sh`, iterate the same resolved order and match on `lang` to emit the corresponding echoed command line per service, then chmod it executable if needed. For `nginx.conf`, filter to services with both `route` and `port` non-null, sort those by route alphabetically (not build order), and emit the block in the exact literal format given, including indentation. Finally, `cargo build` (or run) so the three output files actually get produced on disk.