Build this as a straightforward three-source join, but get the connection plumbing right first since that's where naive attempts fail. Connect to the SQLite file read-only, connect to Redis on localhost:6379 (`redis.Redis(host='localhost', port=6379)`), and use `requests` against the Flask API on localhost:8080. Load all rows from `edges`, then for each unique node referenced, check `node:<id>:status` in Redis and cache the active/inactive result (avoid re-querying Redis per edge if the same node reappears — memoize into a dict) — a node is only active if the key exists AND its value is exactly `"active"`, anything else (missing key, other string) means deleted. For each edge with both endpoints active, call the validate endpoint and only keep the edge if `{"valid": true}` is returned; this API call is also worth memoizing per source-target pair in case duplicates exist. Once you have the filtered valid edge list, build a directed adjacency structure (dict of source -> set of targets) and compute 1-hop as direct successors of the start node, then 2-hop as the union of successors of each 1-hop node, minus the start node itself and minus anything already in the 1-hop set. Print only the final JSON line — route all diagnostics to stderr — and make the script `chmod +x` with the correct shebang.