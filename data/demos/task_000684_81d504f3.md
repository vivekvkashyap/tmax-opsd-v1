Build this as a small pipeline of composable pieces rather than one monolithic Bash blob. First, manually test the raw data source: `nc localhost 9001` (or write a tiny helper) to confirm you get a `time,value` CSV header plus 1024 lines — write a short Python/awk helper script that your Bash server calls to (a) fetch the 1024 samples via a socket connection to 127.0.0.1:9001, (b) run an FFT (e.g. numpy.fft.rfft) on the value column and find the frequency bin with max magnitude excluding index 0 (DC), and (c) do a 1000-iteration bootstrap resample of the value column to get a 95% CI for the mean (2.5th/97.5th percentiles of resampled means). Round frequency to 1 decimal and CI bounds to 2 decimals as specified. For the HTTP layer in Bash, use `nc -l` or `socat` in a loop to accept connections, read just the request's first line to check for `GET /analyze`, branch to your analysis helper or return 404 otherwise, and always emit correct `HTTP/1.1` status line + `Content-Type: application/json` header + blank line before the body. Make the script executable, launch it backgrounded, and test with repeated `curl` calls to confirm persistence across requests.
