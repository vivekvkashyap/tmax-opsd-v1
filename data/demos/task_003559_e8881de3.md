This is a two-part task: forensic extraction, then robust string/pattern matching in C++.

Part 1: Use `ffmpeg -i /app/incident.mp4 frame_%04d.png` (or similar) to extract all frames, then run `zbarimg` over the frames (e.g. loop each PNG through `zbarimg --quiet <file>` or batch with a glob) to decode embedded QR codes. Scan the decoded strings for one starting with `BYPASS_SEED:` and write that exact recovered string, verbatim, to `/home/user/bypass_seed.txt` — don't guess or hardcode it, derive it from the decode output.

Part 2: Write `/home/user/waf.cpp` reading two lines from stdin (filename, description). Before matching, normalize case (lowercase both fields) but keep the ORIGINAL substring semantics for the bypass seed check (verify whether the task wants exact-case matching for BYPASS_SEED vs case-insensitive for XSS/traversal — test against the provided `/app/corpus/clean` and `/app/corpus/evil` samples to be sure). Implement path-traversal detection covering literal `../`, and URL-encoded variants (`%2e%2e%2f`, `%2e%2e/`, `..%2f`) case-insensitively. Implement XSS substring checks (`<script>`, `javascript:`, `onerror=`, `onload=`). Also reject if either field contains the bypass seed you recovered. Print exactly `REJECT` or `ACCEPT` (no extra whitespace/newlines beyond one). Compile to `/home/user/waf` and validate against every file in both corpus directories before finishing, since the hidden grading corpus follows the same distribution.