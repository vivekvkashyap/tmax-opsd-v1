Attack the C++ server issues one at a time before touching the orchestration script. First, `CMakeLists.txt` is missing the protobuf/grpc code-generation step and library linkage — add the `protobuf_generate_cpp`/gRPC C++ plugin invocation for `calc.proto` and a `target_link_libraries(calc_server gRPC::grpc++ gRPC::grpc++_reflection protobuf::libprotobuf)` line (adjust target names to match your gRPC/CMake package as discovered via `find_package` output), then iterate `cmake .. && make` until it builds cleanly. Second, in `evaluator.cc`, the RPN evaluator's `*` branch actually performs addition (`*res = *a + *b` under the `token == "*"` check) — fix that one operator to multiply, and verify with the example `3 4 + 5 *` = 35 by tracing the stack by hand. Third, the stack holds heap-allocated `int*`s that are never freed — either switch the stack to hold plain `int` values (simplest, eliminates the leak class entirely) or explicitly `delete` each popped pointer after use and `delete` the final result pointer once you've copied its value; confirm with `valgrind --leak-check=full` showing `0 bytes in 0 blocks definitely lost`. Only after these compile/pass locally, write `build_and_test.sh` to build both sides, launch the server under valgrind in the background, sleep briefly, run the Rust client with output redirected, then `kill -SIGTERM` and `wait` on the server so valgrind flushes its report before the script exits.