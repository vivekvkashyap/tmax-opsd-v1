Treat `/app/legacy_scorer` purely as a black-box CLI oracle — call it via subprocess with the absolute file path and parse the single float it prints; don't try to reimplement entropy yourself. In your `/analyze` handler, for each file: check existence with `os.path.exists`/`os.path.isfile` and skip missing ones, get `os.path.getsize`, invoke the scorer, then compute `pcf = raw_entropy * math.log2(max(size, 2))`. Implement the ranking structure as an actual max-heap (Python's `heapq` is a min-heap, so push negated PCF, or wrap items in a class with reversed `__lt__`, using `(path)` as the tie-breaker key so equal PCFs sort alphabetically) rather than just sorting a list each time — that's the "custom data structure" requirement. For `/top?n=X`, pop-and-restore or use `heapq.nlargest` equivalent logic to read the top N without mutating the heap. Bind the Flask/FastAPI app to exactly `127.0.0.1:8080`. In `setup_and_run.sh`, create the venv, `pip install` your dependencies inside it, then launch the server with `nohup ... &` so it stays running in the background after the script exits.
