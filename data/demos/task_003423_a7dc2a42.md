Start by reading `/home/user/analyze_network.py` to see the exact buggy query (an implicit cross join with f-string interpolated region, which produces the full cartesian product of nodes x edges rather than filtered edges). Fix it to `SELECT e.source, e.target, e.weight FROM edges e JOIN nodes n ON e.source = n.id WHERE n.region = ?`, passing the region as a parameter tuple to `cursor.execute(query, (region,))` — never format the region into the SQL string. Install networkx, build `nx.DiGraph()` from only the filtered edge rows, and compute `nx.in_degree_centrality(G)` on that subgraph only (not the whole database) — in-degree centrality here should be raw in-degree divided by (n-1) over the induced subgraph's node set. Sort by centrality descending, tie-break by node id ascending, take top 3, and write a header-less CSV with `centrality` rounded to 4 decimals via Python's `round()` (verify the formatting produces e.g. `0.8000` not `0.8`). For `/home/user/ranking.sql`, write a single SELECT joining edges and nodes on `e.source = n.id`, selecting `RANK() OVER (PARTITION BY n.region ORDER BY e.weight DESC)` — test it directly with `sqlite3` before finishing.