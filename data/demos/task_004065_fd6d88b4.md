Handle extraction recursively since the archive nests other archives: extract the top-level vendor_docs.tar.gz to a scratch directory, then recursively detect and extract any `.zip` or `.tar.gz` found inside it (loop until no more nested archives remain), then use `find <scratch> -type f \( -iname '*.md' -o -iname '*.txt' \)` to collect only the target file types, ignoring everything else (e.g. binary/image files) and ignoring original subdirectory structure — copy each matched file flat into /home/user/extracted_docs/. For renaming, lowercase each basename and replace spaces with underscores (`tr 'A-Z' 'a-z'` and `tr ' ' '_'`, or `sed`); when copying, check if a destination name already exists and if so append `_1` (or similar) before writing so nothing is silently overwritten. Build the manifest with `sha256sum /home/user/extracted_docs/* | sort -k2` (sort by filename field, not hash) redirected to /home/user/doc_manifest.txt. For the symlink, compare file sizes with `stat -c%s` or `ls -la` across all files in extracted_docs, find the single largest, and `ln -s` it as /home/user/primary_doc.md — use a relative or absolute path that actually resolves.