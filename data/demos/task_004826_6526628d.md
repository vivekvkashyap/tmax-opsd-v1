Work file-by-file with pure shell tools, no Python/jq. First, `wc -c` each `.jsonl` in `/home/user/app_logs/` and sum for the "before" total (or `stat -c%s`) — capture this before you delete anything. Load `/home/user/dict.csv` into an awk associative array (split on comma) so you can map long strings to short codes in one pass. For each log file, use `grep '"status": *"FAILED"'` (or awk) to isolate matching lines, then use sed/awk with regex captures (e.g. `grep -oP '"timestamp": *"([^"]*)"'` or awk field splitting on quotes) to pull out `timestamp`, `server_id`, `error_code` — avoid relying on fixed field order since JSON key order in the line is fixed here but be precise with the quote-delimited extraction. Substitute the extracted server_id/error_code through your dictionary awk array, then print `timestamp,short_id,short_code` with no header, writing to `/home/user/condensed_logs/<base>.csv`. Only after `tar -cf` succeeds against `/home/user/condensed_logs/` should you `rm -rf` the two source directories — order matters, since the size math and archive both depend on artifacts that no longer exist afterward. Finally compute saved bytes as (sum of original sizes) minus `stat -c%s` of the tar, and write that integer to `/home/user/space_saved.txt`.
