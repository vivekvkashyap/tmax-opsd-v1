Start by understanding the given binary's interface empirically rather than guessing: run `/app/score_calc "a,b,c" "d,e,f"` with a few sample vectors from items.csv and observe how the score varies with distance (it should behave like a similarity measure — higher for closer vectors). Write recommend.sh to: parse items.csv (skip the target item_id itself), call score_calc once per other item passing its feature vector against the target's, capture the numeric score, then sort all (item_id, score) pairs numerically descending and take the top 3 item_ids (comma-separated). Wrap this scoring+ranking logic in a function so it can be reused per incoming request. For the TCP server, use `nc -l -k -p 8888` (or socat with `fork`) piped through a `while read -r item_id; do ...; done` loop that calls your ranking function and writes the comma-separated result plus newline back to the client; verify with `-k`/fork that it survives multiple sequential connections instead of exiting after the first. Launch it backgrounded (e.g. with `&` or `nohup`) before finishing, and confirm with `ss -ltnp` or a manual `nc` test that port 8888 is listening.