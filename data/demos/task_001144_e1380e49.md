Load `/app/acoustic_test.wav` (e.g. `scipy.io.wavfile.read`), run `scipy.signal.welch(signal, fs=sample_rate, window='hann', nperseg=4096, noverlap=2048)` exactly as specified to get the empirical PSD, then mask it to `0 <= f <= 2000` Hz. Use `scipy.optimize.curve_fit` (or a manual least-squares routine) to fit the sum of a constant plus three Lorentzians `c + sum(a_i / ((f-f_i)^2 + w_i^2))` to that masked data — initialize the three `f_i` guesses at the visually apparent PSD peaks in that range (inspect the PSD array/plot first to find rough peak locations) since Lorentzian fits are very sensitive to initialization and can converge to a bad local optimum otherwise; bound `w_i` and `a_i` to be positive to keep the fit physical. Once you have the fitted (c, a_i, f_i, w_i), integrate M(f) from 0 to 2000 Hz either analytically (Lorentzian integral has a closed form involving arctan) or with `scipy.integrate.quad` for high precision. Sort f1<f2<f3 and write the JSON exactly as specified to `/home/user/resonance_results.json`; the grader checks total_energy_integral within roughly 5% relative error, so prioritize getting a clean, well-converged fit over a fast one.