Implement the simulator exactly as specified — precision in the RNG mechanics is what determines correctness here, not approximate probability. Seed a single `std::mt19937 gen(seed)` per run of N simulations (re-seed fresh for each N value, don't reuse state across the three convergence runs), and for each simulation reset capital to 10, then loop up to 100 steps calling `gen()` once per step and testing `gen() % 100 < 45` for a win (+1) else loss (-1); break out of the inner step loop immediately when capital hits 0 and count it as ruined, otherwise the trial simply ends uncounted after 100 steps. Print only `ruined_count / (double)N` per invocation — no extra text, since the bash orchestrator captures this stdout directly to build the report lines. In the bash script, compile with `g++ -O3`, then loop over N=1000,10000,100000 with a fixed seed=42, capturing each program's stdout into a `P=<value>` line formatted as `N=<N>, P=<value>`. Verify determinism by running the whole pipeline twice and diffing convergence_report.md — any mismatch means you've introduced RNG state that differs between runs (e.g., static/global generator reused across N values, or off-by-one in the step-vs-check ordering).