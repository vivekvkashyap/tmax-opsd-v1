First transcribe `/app/data_request.wav` (install/use a speech-to-text tool such as `whisper` via pip, or `ffmpeg` + any available ASR) to recover the exact business-logic instructions spoken in the memo — do not guess the pipeline logic, derive it from the transcript, since the specific filter conditions, join logic, and centrality metric it specifies are the actual spec you must implement. Once you have the transcript, write `/home/user/process_graph.py` to: read a JSON string from stdin with `users` and `transfers`; load both into an in-memory SQLite database (`sqlite3.connect(':memory:')`); run a SQL query implementing exactly the filter/join conditions described in the transcript (pay attention to strict vs. non-strict inequalities and which status values qualify); build a `networkx.DiGraph` from the filtered result edges; compute the specific centrality metric named in the transcript; and print a JSON object of node -> score rounded to the precision stated in the transcript, printing `{}` for empty results. Verify your output is deterministic (sorted keys) since it will be checked for bit-exact equivalence against a hidden oracle across many random inputs.
