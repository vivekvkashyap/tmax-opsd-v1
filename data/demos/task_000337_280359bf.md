Break this into independent, testable pieces. For the loop mount, use `losetup`/`mount -o loop /app/user_data.img /home/user/user_data_mnt` (create the mount point first); since you lack root for real `/etc/fstab`, write the equivalent line (image path, mount point, filesystem type, `loop` option) into `/home/user/local_fstab` as documentation. For the proxy, write `/home/user/proxy.py` with a minimal HTTP server (`http.server`, `flask`, or `aiohttp`) exposing `/health` (return 200 JSON) and `/process` (POST): before forwarding anything to the legacy binary on port 9090, validate the JSON payload's string fields against a strict denylist/allowlist — reject on shell metacharacters (`;`, `|`, `&`, `` ` ``, `$`, backticks, redirects), on traversal sequences (`../`, absolute paths escaping the intended dir), and on any field longer than 256 chars, returning 403 on any violation and otherwise proxying the exact payload and relaying the legacy service's response. Validate your sanitiser directly against the given `/app/corpus/clean/` and `/app/corpus/evil/` directories yourself (loop over each file, POST it, check the status code) before finalizing, since 100% pass on both is required. Finally write `/home/user/start_services.sh` to launch the legacy binary and the proxy in the background, then write "READY" to `/home/user/setup_complete.log` only after confirming both are actually listening.