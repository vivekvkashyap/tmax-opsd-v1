Structure the C++ program as clear stages: read configs.csv into a map from run_id to model_type; read metrics.csv into a map from run_id to a 5-element vector<double> (use `std::stod` for parsing, not int-truncating parsers); read weights.txt into a fixed 5-element vector<double>, one weight per line. Join by iterating metrics (or configs) and looking up the matching run_id in the other map — skip or assert on any run_id present in one file but not the other. For each joined run, compute the composite score as the dot product `sum(metric[i] * weight[i] for i in 0..4)` using double precision throughout (don't accumulate in float). Group scores into a `map<string, vector<double>>` keyed by model_type, then compute the mean per group. Sort the resulting (model_type, mean) pairs by mean descending — use `std::sort` with a custom comparator, not the default map ordering which sorts by key. Write each line as `model_type,mean_score` with the score formatted via `std::fixed << std::setprecision(4)`, no header row, no trailing blank line. Compile with `g++` and run once to produce report.csv, then manually recompute one group's mean by hand to sanity-check your join and dot product logic before trusting the full output.
