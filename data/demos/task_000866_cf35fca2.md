Work through this as a strict data pipeline in C: (1) read the binary file record-by-record using the exact packed struct layout (`sensor_id` u32, `timestamp` u64, `value` f32, `checksum` u32, little-endian) — use `#pragma pack(1)` or manual byte offsets to avoid struct padding bugs, which is the most common naive mistake here; (2) validate each record with `checksum == (sensor_id ^ (uint32_t)(timestamp & 0xFFFFFFFF))` and drop mismatches entirely; (3) maintain a per-sensor_id "last valid value" map (array indexed by sensor_id, since IDs are small integers) and compute delta = current - previous only from the second valid record onward per sensor, preserving arrival order; (4) for sensors with at least 2 deltas, compute the sample mean and sample standard deviation (N-1 denominator) of the deltas, then the 95% CI as mean ± 1.96*std/sqrt(N) — a sensor is "drifting" if 0.0 falls outside [ci_lower, ci_upper]; (5) create the SQLite DB and table exactly as specified using the SQLite C API (`sqlite3_open`, prepared statements with `sqlite3_bind_*`) and insert one row per qualifying sensor_id. Compile with `-lsqlite3`. Sanity-check by querying `is_drifting` per sensor and confirming that sensors with a nonzero injected drift bias diverge from those without.
