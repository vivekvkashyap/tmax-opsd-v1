Start by running OCR on `/app/ws_rules.png` (e.g. `tesseract /app/ws_rules.png -`) to recover the exact routing security rules — do not guess at them. Read the extracted text carefully and translate each numbered rule into a discrete validation step in C++: scheme check (exact prefix match, not substring), a path-substring rejection check, presence of a specific query parameter, and a strict format check on that parameter's value (length and character class). Parse the URL manually with string operations (find `://`, then `?`, then split on `&`) rather than a regex library, since none is linked. Handle malformed input defensively — missing scheme, missing `?`, empty strings — by returning DENY (exit 1) rather than crashing or reading out of bounds; every buffer access must be bounds-checked since the binary is compiled with AddressSanitizer and any OOB read/write or leak fails the whole task. Avoid dynamic allocation you don't free, and avoid `substr`/iterator patterns that can go past `string::npos` unchecked. Test locally against both plausible valid and invalid URLs before finalizing exit codes.