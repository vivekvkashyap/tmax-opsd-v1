Structure the script in two clearly separated phases. Phase 1 (initial scan): use `find /home/user/legacy_projects -name '*.json' -o -name '*.csv'` to enumerate files, then for each JSON file pull `project_name`/`status` with `jq` and for each CSV skip the header and parse columns with `awk -F,`/`cut`, accumulating into an associative array (`declare -A`) keyed by project name. Build the master JSON from that array with `jq -n` fed key/value pairs (or by constructing valid JSON text carefully, watching escaping) — critically, write it to a temp file in the *same directory* (e.g. `mktemp /home/user/master_index.json.XXXX`) and `mv` that temp file over `/home/user/master_index.json`, since `mv` within the same filesystem is atomic while writing directly to the target is not. Phase 2 (watch loop): run `inotifywait -m -e close_write --format '%w%f' /home/user/dropzone/` in a loop; for each new path reported, detect its type by extension, parse it the same way as phase 1, merge the single new key into the current index (read existing file, merge with `jq`, write to temp, `mv` again), and continue watching. Install `inotify-tools`/`jq` via `sudo apt-get install -y` only if missing (check with `command -v` first), `chmod +x` the script, and launch it with `&` so it stays resident for later drop-tests.
