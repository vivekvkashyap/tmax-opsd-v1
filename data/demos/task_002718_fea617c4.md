Do the build fix first since the sanitizer is independent and easier to validate on its own. For Part 1, inspect `/app/tiny-graph-db/Makefile`'s `CFLAGS`/compiler invocation for a flag that doesn't belong (an unrecognized `-D` define or similarly invalid option is a common injected perturbation) — remove or correct just that flag, keep the rest of the build logic intact, run `make` in `/app/tiny-graph-db`, and confirm the resulting binary at `bin/tiny-graph-db` runs and reports the expected version string. For Part 2, write `/home/user/query_sanitizer.sh` as a pattern-matcher over the query string: search for a variable-length path token `[*...]` (e.g. via `grep -oE` or bash regex) and parse what's inside the brackets — if there's no `*`, it's safe by definition; if there is a `*` with no digits at all, or the upper bound after `..` is missing (dangling `..` with nothing after it, or `M..` with nothing following), that's unbounded and must REJECT; if both bounds are present, extract the upper bound number and reject only if it's greater than 5, otherwise accept. Be careful with regex edge cases like `[*]` (no numbers at all) vs `[*1..5]` vs `[*..10]` vs `[*3..]`. chmod it executable, then validate every line of `/app/corpora/evil_queries.txt` returns REJECT (exit 1) and every line of `/app/corpora/clean_queries.txt` returns ACCEPT (exit 0) by looping your script over both files before considering it done.
