This is a multi-stage pipeline — build and validate each piece against the provided training corpora before chaining them. For the glitch detector, inspect the sample files in `/app/training_data/clean_corpus/` and `/app/training_data/evil_corpus/` directly (file sizes with `ls -la`, and pixel stats with ImageMagick's `identify -verbose` or `convert ... -format "%[fx:standard_deviation]"`) to find a discriminating signal — e.g. tiny file size or near-zero variance often indicates blackouts/noise — rather than assuming a threshold; derive it empirically from the actual corpus files and confirm `/home/user/detector.sh` classifies 100% of both training sets correctly (exit 0 clean, exit 1 glitch) before trusting it on extracted video frames. Extract frames with `ffmpeg -i /app/factory_feed.mp4 -vf fps=1 -q:v 2 /home/user/extracted_frames/frame_%04d.jpg`. Run the detector over each extracted frame, deleting glitches and recording classification. For surviving frames, compute average brightness via `convert frame.jpg -colorspace Gray -format "%[fx:mean*255]" info:` (or similar), threshold at 100, and update the Bayesian posterior sequentially using Bayes' rule with the given priors/likelihoods, carrying forward the last known probability for skipped/glitch rows. Write the CSV in the exact specified column order and format.
