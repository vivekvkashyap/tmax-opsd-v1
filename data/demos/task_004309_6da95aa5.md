Write `/home/user/process.c` to stream-read `data.csv` line by line, skip the header, split each line on commas with `sscanf`/`strtok`, and accumulate per-store totals in a small fixed array or simple linked structure keyed by `store_id` (track sum revenue, sum visitors, and running max revenue per store). After processing all rows, sort the distinct store_ids ascending and print each row as `store_id,total_revenue,total_visitors,max_revenue` with revenue values formatted via `%.2f` (no header line) to `/home/user/summary.csv`. Also append the three exact log lines to `/home/user/pipeline.log`, substituting the real row count for `<N>` — write this log directly from the C program, not from a wrapper script, so it reflects actual rows parsed. Compile and run the program, then verify `summary.csv` matches expectations by hand-checking the sums. Finally use the `sqlite3` CLI to create `/home/user/analytics.db`, `CREATE TABLE store_stats (store_id INTEGER, total_revenue REAL, total_visitors INTEGER, max_revenue REAL);`, and bulk-load with `.mode csv` + `.import /home/user/summary.csv store_stats` (set the mode before import so commas aren't split as one field).