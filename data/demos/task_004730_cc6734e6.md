Read the current `/app/sh-graph-query-1.0/bin/query_edges.sh` first to find the bug: look closely at the awk filter condition for a logical short-circuit mistake (something like `cond || 1`, which is always true and effectively returns every row regardless of the requested node_id) and note that the script also lacks any sort step. Fix it by making the awk pattern strictly `$1 == id` with no trailing `|| 1`, printing `target_id,weight`, and piping the awk output through `sort` with numeric flags on both the target_id and weight fields (e.g. `sort -t, -k1,1n -k2,2n`) so ties on target_id break by ascending weight. Test manually with a sample `edges.csv` and a few node IDs, including one not present in the file (should output nothing), before considering it done — the verifier does fuzz-equivalence against a Python oracle across 100 random node IDs and checks exact stdout match, so any residual filtering or sort-order bug will fail broadly across cases even if a few spot checks look right.
