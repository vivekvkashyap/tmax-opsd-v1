Recover the trigger parameters from the voicemail first, since the detector's math depends on them: transcribe `/app/ticket_1492_voicemail.wav` (e.g. with `whisper` or another ASR tool available in your environment) and parse out the spoken `resonance` and `cutoff` values exactly as stated — do not guess typical filter defaults. Then read `/app/audio_filter.c` closely to understand the IIR biquad structure (coefficients derived from cutoff/resonance, and the recurrence updating state variables `z1`/`z2` using `float` precision) so your detector replicates the *same* arithmetic, not just a generic biquad — subtle differences in coefficient formulas or update order will misclassify borderline files. Implement `/home/user/detector.c`: parse the WAV file's PCM samples (handle the standard WAV header to skip to sample data), run them through a faithful reimplementation of the filter using the extracted parameters, and after each sample check `isnan()`/`isinf()` on the filter state — exit 1 immediately if triggered, else finish processing and exit 0. Compile with `gcc`, then validate against every file in `/app/corpus/clean/` (must exit 0) and `/app/corpus/evil/` (must exit 1) yourself, iterating on any mismatches (often caused by float-vs-double precision mismatches with the original filter) before considering the task complete.
