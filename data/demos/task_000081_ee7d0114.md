Fix the three layers independently and verify each before moving to the next. First, `make` and read the exact error — Makefiles require recipe lines to start with a literal tab character, so a Makefile edited with spaces will fail with a "missing separator" error; open it in a way that reveals whitespace (e.g. `cat -A Makefile`) and replace leading spaces on recipe lines with tabs. Second, test `./process_pcap.sh traffic.pcap` in isolation and check its output looks like a clean list of large integers — the fix is generally to pass `-S` to `tcpdump` (absolute, not relative, sequence numbers) and to make sure the awk/field extraction isolates only the numeric sequence number, stripping any trailing characters like colons. Third, don't patch the naive variance formula — replace it: squaring numbers near 1e9 overflows double precision's exact-integer range and causes catastrophic cancellation in `sum(x^2)/n - mean^2`. Rewrite `calc_variance.awk` to use Welford's online algorithm (running mean and M2 updated incrementally per value, `variance = M2/n` for population variance) which never squares the raw large values. Finally run the fixed pipeline end-to-end and write the resulting variance, formatted to exactly 2 decimal places, to `/home/user/result.txt`.
