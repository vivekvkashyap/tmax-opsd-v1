Break this into independent, individually testable pieces before wiring them together. Git hook: `git init --bare /home/user/finops_repo.git`, then write `hooks/pre-receive` as a bash script that reads `oldrev newrev refname` lines from stdin, runs `git diff oldrev newrev` (or `git diff-tree`) restricted to `*.tf` files, and greps the diff's added lines for a pattern matching `+` followed by optional whitespace then exactly `instance_type = "p4d.24xlarge"` — use `grep -E '^\+\s*instance_type = "p4d\.24xlarge"'` on the diff output. On match: print "Cost limit exceeded!" to stderr and exit 1; on no match: exit 0. In both branches, append the specified `<li>` line (with the actual newrev substituted) to `/home/user/www/cost_report.html` — this append must happen regardless of accept/reject, so do it before the exit. Remember `hooks/pre-receive` must be executable. Web/TLS: generate a self-signed cert with `openssl req -x509 -newkey rsa:2048 -nodes -keyout key.pem -out cert.pem -days 365 -subj '/CN=localhost'`, then write a minimal Python `http.server` wrapped with `ssl.wrap_socket`/`SSLContext` serving `/home/user/www`, backgrounded with its PID captured via `$!` into `https.pid`. Forwarding: `socat TCP-LISTEN:8080,bind=127.0.0.1,fork,reuseaddr TCP:127.0.0.1:8443 & echo $! > socat.pid`. Test each piece (push via `git push` to a local clone, curl the HTTPS server directly, then curl through 8080) before declaring done.
