Treat this as two separate subtasks that both feed the fix. First, OCR/read the target percentage embedded in `/app/uptime_graph.png` (open it, use whatever image-reading tool is available, or extract text programmatically) — this tells you the required precision magnitude, don't guess it. Second, use `git bisect start`, mark `HEAD` as bad and `v1.0` as good, and at each bisect step build/run the tool against a large synthetic ping array to check whether output shows precision loss (e.g. compare against a simple independent double-precision sum in a throwaway script), calling `git bisect good`/`bad` accordingly until git identifies the exact regressing commit — inspect that commit's diff to confirm it changed the accumulator from double to a lower-precision float loop. Then fix `src/uptime_calc.c` by using a `double` accumulator (or Kahan/compensated summation) for the aggregation loop rather than plain `float`, since naive float summation over tens of thousands of values loses precision through repeated rounding (catastrophic cancellation). Rebuild with `make` and confirm `/home/user/uptime_repo/build/uptime_calc` reads whitespace-separated floats from stdin and prints one aggregate double to stdout, then test it manually on a large synthetic input before finishing, since it will be fuzz-compared against a hidden reference binary.