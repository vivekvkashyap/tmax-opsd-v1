Read evaluate.py first and locate the exact leakage pattern: `scaler.fit_transform(X)` called on the full dataset before `train_test_split`. Fix it by reordering: call `train_test_split` on the raw (unscaled) X/y first with the same `random_state=42` and `test_size`, then create a fresh `StandardScaler()`, `fit_transform` it only on `X_train`, and `.transform()` (not fit) on `X_test`, feeding the transformed sets into the model. Keep every other seed and hyperparameter (data generation seed 42, model `random_state=42`) untouched, since the accuracy the verifier checks is derived from those exact seeds — only the split/scale ordering should change. Keep the inference timing block wrapping only `model.predict(X_test_scaled)`. Run the corrected script (`python3 /home/user/evaluate.py`) so metrics.json is actually regenerated on disk, and sanity check the JSON keys/types match the required `{"accuracy": <float>, "inference_time": <float>}` schema exactly (same key names, no extra nesting).