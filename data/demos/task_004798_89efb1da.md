Write `/home/user/profiler.go` to parse `/home/user/metrics.csv` (skip the header, parse cpu/mem/latency as float64). Implement batch gradient descent from scratch for each hypothesis: for Hypothesis A the prediction is `alpha*cpu + beta*mem`, for Hypothesis B it's `gamma*cpu^2 + delta*mem`; for each, initialize weights to 0, use learning rate 0.0005, and at every epoch compute predictions over the whole dataset, the MSE loss, and the gradients of MSE w.r.t. each weight (standard linear-regression-style gradients, just with `cpu^2` as the feature for Hypothesis B instead of `cpu`), then update all weights simultaneously. Stop when `|MSE_epoch - MSE_prev_epoch| < 1e-5` or after 50000 epochs, whichever comes first — track the previous epoch's MSE to check convergence correctly. After both hypotheses converge, compare their final MSE values and write whichever letter had the lower MSE to `/home/user/best_model.json` in the exact `{"hypothesis": "A"|"B"}` format. Sanity-check by also trying to eyeball whether latency looks closer to linear or quadratic in cpu from the raw data, as a check on your gradient descent implementation.