Work through this as a sequence of independent verifications, not guesses. First run `git bisect start`, `git bisect bad HEAD`, `git bisect good v1.0` inside `/app/anomaly_pipeline/worker_repo`, then let `git bisect run python3 test_convergence.py` (or manual step-through) narrow to the single offending commit; capture its full 40-char SHA with `git rev-parse` before you exit bisect (`git bisect reset` afterward so the tree lands on a clean HEAD to edit). Inspect the diff of that commit (`git show <sha> -- compute_engine.py`) to see exactly what changed — look for a dtype cast to a narrower float type and a shrunk iteration/tolerance limit, since both symptoms (truncated values, non-convergence) point to those two independent causes. Fix both, then commit on `main` (not in detached HEAD state). Separately, open `/app/anomaly_pipeline/config.json` and correct the Redis port to match the live Redis instance (check with `redis-cli -p 6379 ping` or similar rather than assuming). Restart via `restart_all.sh`, then confirm end-to-end with an actual `curl -X POST` including the Bearer token, and save exactly what that call returns to the two required output files — don't fabricate the JSON.