Work in three clean stages: extract, reconstruct, then serve. First, pull frames from `/app/backup_topology.mp4` with `ffmpeg -i /app/backup_topology.mp4 frame%d.png` (one per second as stated), then decode each with `zbarimg --raw frame1.png` etc. to get the embedded JSON edge-list fragments — don't assume frame count or content, actually decode all frames the video contains. Second, merge the per-frame JSON fragments' `edges` arrays (each edge is `[nodeA, nodeB, latency]`) into one in-memory undirected weighted graph (adjacency list mapping node -> list of (neighbor, weight)), adding each edge symmetrically since it's undirected. Third, implement `/home/user/topology_server.cpp` as a simple blocking TCP server on `127.0.0.1:8080` (socket/bind/listen/accept loop, one connection handled with a read-line/write-response cycle, supporting multiple sequential requests per connection) that parses `PATH <src> <dst>` by running Dijkstra's shortest-path over your reconstructed graph and replying with the comma-separated node path plus total latency, and parses `DEGREE <node>` by replying with the count of that node's adjacency-list entries. Compile with `g++`, launch the server in the background (e.g. `./topology_server &`), and test it yourself with `printf 'PATH a b\n' | nc 127.0.0.1 8080` style commands before finishing, comparing against latencies you compute by hand from the reconstructed graph.
