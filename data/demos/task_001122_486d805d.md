Write the C program to read the `mapped_sequence` variable from /home/user/sequence_data.nc via the netcdf C API (nc_open, nc_inq_varid, nc_get_var_double), confirming the length is 4096 before proceeding. For the FFT, either use `fftw_plan_dft_1d` on a complex array with zero imaginary parts, or use `fftw_plan_dft_r2c_1d` for efficiency — but if you use r2c, remember it only returns N/2+1 bins, so when computing E_freq you must double the contribution of all non-DC, non-Nyquist bins to correctly reconstruct the full symmetric spectrum's energy sum before dividing by N (Parseval's theorem: sum|x[n]|^2 = (1/N) sum|X[k]|^2 over ALL N bins, not just the returned half). Compute E_time as a plain sum of squares in the time domain. Search k from 1 to N/2 (skip k=0) for the bin with max |X[k]| = sqrt(re^2+im^2). Print the four lines to /home/user/analysis_result.txt using the exact labels and precision specified, then compile with `gcc analyze_seq.c -o analyze_seq -lnetcdf -lfftw3 -lm` and run it.