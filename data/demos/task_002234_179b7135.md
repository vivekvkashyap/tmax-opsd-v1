Rewrite the analysis in `awk` rather than patching the broken bash loop line by line — awk handles CSV field splitting, floating-point math, and grouping natively without spawning `bc` per row. First unset/avoid any locale override (`LC_NUMERIC`) that makes decimal separators non-dot, since that silently corrupts arithmetic; run with `LC_ALL=C` to be safe. In the awk script: skip the header, split on comma, and for each row skip if the value field is empty (`$3==""`) or falls outside `[-10.0, 50.0]`. Accumulate sum and count per sensor in associative arrays. At the END block, iterate sensor names, sort them (feed through `sort` afterward, or collect names and sort in awk), compute avg = sum/count, format to two decimals with `printf "%.2f"`, and derive the asterisk count as the floor of avg — careful with negative or fractional averages, use `int()` correctly (note `int()` in awk truncates toward zero, which matches floor for non-negative results here since outliers already remove negatives extreme enough to matter, but clamp at 0 if avg is negative). Write output lines in the exact `Sensor: X | Avg: Y | Plot: ***` format, then execute the script and confirm `/home/user/sensor_report.txt` was produced.