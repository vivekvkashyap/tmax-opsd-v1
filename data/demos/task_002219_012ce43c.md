Do the straightforward cleanup and linking steps first with plain shell commands: `rm /home/user/provisioning/logs/*.old`, then `ln -sf /home/user/provisioning/configs/prod.conf /home/user/provisioning/active.conf` (use an absolute or otherwise correctly-resolving target so `readlink` checks pass). For run_check.sh, count files in the logs directory with something like `ls /home/user/provisioning/logs | wc -l` (or `find ... -maxdepth 1 -type f`) and bail with the exact message and exit code 1 if it exceeds 5 — get this check right since deleting the .old files changes the count before this runs. Then read active.conf line by line (`while read -r host port; do ... done < active.conf`, skipping blank lines), and for each pair run `nc -z -w 2 "$host" "$port"` checking its exit status to decide SUCCESS vs FAILURE, appending exactly `SUCCESS: <hostname>:<port>` or `FAILURE: <hostname>:<port>` to connectivity.log for each line in order — don't buffer/reorder them. Make the script executable with `chmod +x` and actually run it once so connectivity.log is populated, then inspect it to confirm the format matches exactly (no extra whitespace, correct host:port pairing).