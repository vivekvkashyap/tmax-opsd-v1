This is a vendored-package-perturbation-plus-race-condition task. First, inspect `/app/ijson-3.2.3/setup.py` for a flag controlling whether the C extension (yajl2) backend is built/enabled — it's been hardcoded off — flip it back so the C backend gets compiled, then `pip install -e /app/ijson-3.2.3` (or build+install) and confirm at runtime that `ijson.backend` (or equivalent introspection) reports the C extension, not the pure-Python fallback, since only the C backend meets the time budget on large files. Second, in `backup_errors.py`, avoid the log-rotation race by snapshotting: `os.link()` each `.json` file in `active_logs/` into `backup_staging/` to freeze inodes before any parsing begins, rather than reading files in place. Third, stream-parse each staged file with `ijson.items(f, 'item')` (matching the array-of-objects structure) and filter/write only `level == "CRITICAL"` rows directly to CSV via the `csv` module in a single streaming pass — avoid loading entire files into memory or building intermediate Python lists of all records, since that would erase the C-extension's speed advantage. Benchmark your script locally against a large synthetic JSON file before submitting, to confirm you're well under the time threshold.
