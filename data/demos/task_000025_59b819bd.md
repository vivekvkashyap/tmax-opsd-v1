Treat this as several independent checklist items plus one reverse-engineering deliverable. For the filesystem work: create the directory tree with `mkdir -p`, then `ln -s ../sites-available/app.conf /home/user/nginx/conf/sites-enabled/app.conf` (relative symlink pointing back up a directory, matching how nginx sites-enabled links are conventionally structured). Back up `/home/user/nginx.conf.orig` unchanged to `/home/user/backup/nginx.conf.bak`, then copy it into `sites-available/app.conf` and edit only the proxy_pass port from 8080 to 9090 (leave the rest of the config intact) to resolve the 502. Write `/home/user/fake_fstab` with a plain-text fstab-style line containing the source image path, mount point, filesystem type, and options in that order. For the Expect script, use `spawn`, `send "PING\r"`, `expect "PONG"`, then a clean exit. For the binary: disassemble/trace `/app/legacy_auth` (objdump, strings, or run it with sample inputs and diff outputs) to find the exact byte-level transformation from input string to output token — it's likely a simple deterministic character-wise or hash-based transform; replicate it precisely in `/home/user/new_backend.py` reading `sys.argv[1]`, make it executable, and self-test against the real binary on varied inputs before finishing since it will be fuzz-compared on 500 random strings.