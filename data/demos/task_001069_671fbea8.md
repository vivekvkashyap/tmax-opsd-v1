Write router.cpp with `extern "C"` around the `parse_route` function signature so the compiler emits the plain C symbol name instead of a mangled C++ one — this is what lets ctypes find it by name later. Inside the function, find the first `?` in the input url (e.g. `strchr` or manual scan); if found, copy the substring before it into `path` and the substring after it into `query` (remember to null-terminate both); if not found, copy the whole string into `path` and set `query[0] = '\0'`. Compile as a shared object with position-independent code and no name mangling exposed, e.g. `g++ -shared -fPIC -o /home/user/librouter.so router.cpp`, then confirm with `nm -D librouter.so | grep parse_route` that the symbol is unmangled (plain `parse_route`, not `_Z...`). In benchmark.py, `ctypes.CDLL` the library, declare argtypes if needed, allocate two 256-byte `create_string_buffer`s, time a 100,000-iteration loop calling parse_route with the given test URL, and write the three required lines (Route, Query, Time) to benchmark_results.txt using the actual measured elapsed seconds, not a placeholder.
