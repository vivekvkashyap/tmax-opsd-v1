Build this as a single bash pipeline, watching disk usage carefully: extract frames one at a time or in small batches rather than dumping all frames at once (use `ffmpeg -i traffic.mp4 -vf fps=1 frame_%04d.png` into `/home/user/processing`, but delete each frame right after processing it if disk space is a concern, or process then bulk-delete at the end as the task allows). For each frame, get its average RGB cheaply by asking ffmpeg to scale it to 1x1 and dump raw RGB bytes (`ffmpeg -i frame.png -vf scale=1:1 -f rawvideo -pix_fmt rgb24 -`), then parse the 3 raw bytes with `od -An -tu1` or `hexdump`. Compute Euclidean distance to (110,115,120) in awk: `sqrt((R-110)^2+(G-115)^2+(B-120)^2)`. Frame index i (0-based) maps to timestamp_sec = i (since 1fps, first frame = t=0) — get this mapping right, it's the most common bug. Filter distance > 35.0, then join matching timestamp_sec rows against `/app/sensor_data.csv` (e.g. via awk associative array or `join` after sorting both on timestamp_sec) to pull sensor_id. Write header + rows to `/home/user/anomalies.csv`, round distance to 2 decimals (printf "%.2f"), sort numerically by timestamp_sec (`sort -t, -k1,1n`), then `rm` the extracted frames.