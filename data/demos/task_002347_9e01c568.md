Work this multi-part task in isolated, independently-testable stages. (1) OCR: run OCR (e.g. `tesseract`) on `/app/capacity_limits.png` to recover the three integer thresholds (MAX_CPU/MAX_MEM/MAX_DISK) — verify the recovered numbers look sane before trusting them, since OCR can mis-read digits. (2) Expect script: inspect `/home/user/bin/register_limits` to see its exact prompts, then write `/home/user/setup_limits.exp` using `spawn`/`expect`/`send` to answer each `read -p` prompt with the OCR'd integer in order, and run it — success is indicated by a `SUCCESS` marker file it writes. (3) C sanitiser: write `/home/user/sanitiser.c` that reads `timestamp,hostname,cpu,mem,disk` lines, rejects any hostname containing non-alphanumeric/non-hyphen characters (this blocks shell metacharacters like `;`), and rejects any cpu/mem/disk that isn't a valid positive number (reject negatives and non-numeric strings); exit 0 only if every line passes, exit 1 otherwise. Compile it, then test directly against every file in `/app/corpus/clean/` (expect exit 0) and `/app/corpus/evil/` (expect exit 1) before moving on. (4) Git hook: `git init --bare /home/user/metrics.git`, then write an executable `hooks/pre-receive` that, for each pushed ref, extracts changed `.log` files and pipes/pushes them through the compiled sanitiser, rejecting the push (non-zero exit) if any file fails. (5) Nginx: write `/home/user/nginx.conf` running as a non-root user, listening on 8080, with an `upstream` block round-robin balancing `127.0.0.1:8081` and `127.0.0.1:8082`, then start nginx with `-c` pointing at that config and confirm it's listening.