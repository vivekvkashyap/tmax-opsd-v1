Handle the three sub-problems in order since later steps depend on earlier ones being unbroken. First, fix `run_pipeline.sh`'s filename handling: a `for f in $(ls data/)` (or unquoted glob) pattern will word-split on spaces — replace it with `for f in data/*; do ./solver "$f"; done`, always quoting `"$f"`. Second, find the regression with `git log --oneline` and `git bisect` (or `git log -p -- solver.c`) driven by actually running the compiled solver against a known input at each candidate commit and checking for "Failed to converge"; write the full hash of the offending commit to `/home/user/bad_commit.txt`. Third, inspect `solver.c` for the race: with `NUM_THREADS` worker threads each iterating Newton's method, look for any variable used to hold the per-iteration delta/state that is declared at file/global scope instead of inside the per-thread loop — that's the classic multithreaded shared-mutable-state bug; move it to thread-local (declare inside the function/loop) while keeping the pthread structure intact. Recompile with `gcc -O2 -pthread solver.c -o solver -lm`, rerun the fixed script, and redirect output to `/home/user/pipeline_output.txt`, verifying it matches the `File: <name> | Sum of roots: <value>` format for every file with no convergence failures.