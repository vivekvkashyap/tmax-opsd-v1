Build and validate the pipeline stage by stage rather than writing it all at once. Download the PDB file only if absent, then parse `ATOM` records where the atom-name field (fixed columns, typically 13-16) equals `CA`, extracting x/y/z from their fixed-width columns in file order — don't rely on splitting by whitespace since PDB columns can be joined without spaces. Build the contact graph with an O(N²) pairwise distance loop (N is small, ~46 atoms), adding an edge when Euclidean distance is strictly `< 8.0` and excluding i==j; derive the degree signal `D` directly from this adjacency. For the spectral part, follow the exact recipe given — `np.fft.fft` (not `rfft`) on `D`, magnitude via `np.abs`, then slice `[1:N//2]` for positive frequencies — and reproduce the null model exactly: seed `np.random.seed(42)` once, then generate 100 permutations of `D` via `np.random.permutation`, computing each one's positive-frequency magnitude and averaging them before the paired t-test. Wrap your `main()` call in `cProfile.run(..., '/home/user/profile.txt')` or `cProfile.Profile()` + `pstats.Stats(...).dump_stats(...)`/print to file. Verify each JSON field against your own manual recomputation before finishing.