Write straightforward C that reads `/home/user/data.csv` (skip the header line with `fgets`/`fscanf`, or fixed since the dataset is small and known-shape), loads x and y arrays, and calls `gsl_fit_linear(x, 1, y, 1, n, &c0, &c1, &cov00, &cov01, &cov11, &sumsq)` from `<gsl/gsl_fit.h>` to get intercept c0 and slope c1 directly — do not hand-roll your own least-squares formula, use the GSL call as required. Format both output lines with exactly 4 decimal places (`%.4f`), writing `/home/user/model_output.txt` (overwrite) with the intercept/slope line, and appending (open in append mode, `"a"`) a `Run <ID>: c0=..., c1=...` line to `/home/user/experiment_log.txt`, where `<ID>` comes from `argv[1]` parsed as an integer. Compile with `gcc /home/user/train_model.c -o /home/user/train_model -lgsl -lgslcblas -lm` (link order matters: gsl before gslcblas before m), then run it once with `1042` as the argument. Double check the CSV parsing skips the header correctly and that the program errors clearly if argc != 2, rather than silently producing wrong output.
