Do the steps in the given order since later steps depend on earlier state. First `mkdir -p /home/user/backup` and `tar -czf /home/user/backup/manifests.tar.gz -C /home/user manifests` (or equivalent) to snapshot the originals before any edits. Second, write a script (Python is easiest for timezone math) that, for each YAML file in /home/user/manifests/, extracts the `deployed-at` value, parses it as a UTC timestamp, converts to Asia/Tokyo (use zoneinfo/pytz — Tokyo is a fixed UTC+9 offset with no DST, so the conversion is a straightforward hour addition, but let the timezone library do it rather than hardcoding), formats it as `YYYY-MM-DD HH:MM:SS JST`, and writes the modified file (with the original filename) into /home/user/processed/, changing only that field's value in place. Third, build a grep/awk/sed pipeline over /home/user/processed/*.yaml that pulls out just the deployed-at annotation's quoted value, sorts the resulting lines chronologically (since the format is already YYYY-MM-DD HH:MM:SS, a plain lexicographic `sort` works correctly), and redirects to /home/user/timestamps.txt — check the exact quoting/format matches what's in the YAML (keep the surrounding double quotes if present).