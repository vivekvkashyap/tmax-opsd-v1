Break this into extraction, C processing, and C serving, testing each stage independently before wiring them together. Use `ffmpeg -i /app/sensor_feed.mp4 -f rawvideo -pix_fmt gray output.raw` to dump a raw 8-bit grayscale stream (confirm the resolution/fps with `ffprobe` rather than assuming), giving you 320*240=76800 bytes per frame. In `processor.c`, read frames of exactly that size in a loop, sum all bytes per frame and integer-divide by 76800 for the average, track a running `last_emitted` value, and only append a (timestamp_ms, brightness) pair when it differs from `last_emitted` — always keep frame 0 regardless. Timestamps increment by 100ms per raw frame read (not per emitted frame). Write the CSV header exactly as `timestamp_ms,brightness` and the log line matching the literal format `Processed <N> raw frames, saved <M> cleaned frames.`. For `server.c`, build a standard TCP listener on 127.0.0.1:9000, on each connection read until you see `FETCH_SERIES\n`, build a compact JSON array (no whitespace) from the CSV, then convert the ASCII/UTF-8 JSON bytes to UTF-16LE manually (each char byte -> 2 bytes, low byte first, high byte 0x00, no BOM) before writing to the socket, then close only the client connection and keep accepting. Test with a raw TCP client that hex-dumps the response to confirm UTF-16LE encoding before finishing.
