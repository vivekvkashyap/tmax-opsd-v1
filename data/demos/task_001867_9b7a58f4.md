Use SQLite's `WITH RECURSIVE` CTE to do the graph walk inside SQL rather than pulling rows into Python and doing BFS in memory — build a CTE that starts from `SELECT owner_id, subsidiary_id, CAST(owner_id AS TEXT) AS path, 1 AS depth FROM ownership_graph WHERE owner_id = 1001`, recursively joins `ownership_graph` on `subsidiary_id = owner_id` while appending to the path string, and stops (via a WHERE guard or LIMIT-based termination) as soon as a row's `subsidiary_id` equals `1001`; select the shortest such path (`ORDER BY depth LIMIT 1`) to avoid runaway recursion on cycles/branches. Write the resulting comma-joined path to `/home/user/cycle.txt` exactly, with no trailing whitespace or extra newline formatting issues. For the index, since the recursive step repeatedly looks up "all subsidiaries of a given owner_id", the correct index is on `owner_id` (e.g. `CREATE INDEX idx_owner_sub ON ownership_graph(owner_id)`); create it via your script or a direct `sqlite3` call, then query `sqlite_master` for `sql` where `type='index' AND name='idx_owner_sub'` and write that text verbatim to `/home/user/index_schema.txt`.