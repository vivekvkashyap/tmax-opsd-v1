First read `/home/user/analyze.sh` closely to find why it loops forever: it's re-globbing/re-`find`ing `*.dat` after each pass, and files it creates during extraction (named like `..._extracted.dat`) still match that same glob/find pattern, so the driving loop condition never becomes false. Fix this in `/home/user/analyze_fixed.sh` by snapshotting the list of original `.dat` files ONCE up front (e.g. `files=(/home/user/suspicious_data/*.dat)` or `find ... -name '*.dat' > /tmp/list` captured before any extraction begins) and then iterating only over that fixed list — never re-glob mid-run, and explicitly skip/exclude anything matching `*_extracted.dat` as a belt-and-suspenders check. To find the crashing payload, don't just run the extractor blind: either binary-search/bisect the file set by running the extractor over halves of the directory and narrowing to the failing subset, or trace execution (`strace -f` on the wrapper, or add per-file logging around each extractor invocation before it runs) so you can see the exact filename in flight when it aborts/panics. Write only that one absolute path, with no extra whitespace, into `/home/user/crashing_file.txt`. Confirm the fixed script is `chmod +x` and exits 0 on the remaining valid files.