Treat this as three independent, verifiable sub-tasks. (1) HMAC: read the raw bytes of `/home/user/keys/secret.key` (don't add a trailing newline) and compute HMAC-SHA256 over the literal string `admin-access-request`, e.g. `openssl dgst -sha256 -mac hmac -macopt key:"$(cat /home/user/keys/secret.key)" <<< -n "admin-access-request"` or `openssl dgst -sha256 -hmac "$(cat ...)"`, then write only the lowercase hex digest (no extra text) to the output file — double check no newline/label leaks in from the tool's default output format. (2) SSH: generate the key with `ssh-keygen -t ed25519 -f ~/.ssh/ops_key -N ""`, then append to `authorized_keys` by prepending the exact literal string `restrict,from="10.0.0.0/8" ` directly before the `ssh-ed25519 ...` content of the `.pub` file (verify with `cat` that the space and quoting are exact). (3) Headers: use `curl -sI http://127.0.0.1:8080/` to fetch response headers, check case-insensitively for `X-Frame-Options` and `Strict-Transport-Security`, and append a `MISSING: <Header-Name>` line to the log only for each one that is absent — don't write anything for headers that are present.
