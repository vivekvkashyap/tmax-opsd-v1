Treat `/app/legacy_cleaner` purely as a black-box oracle since there's no source: generate many small synthetic CSV fixtures yourself (embedded newlines inside quoted fields, duplicate messages, missing first/last/middle timestamps, unicode) and run the binary on each to observe its exact behavior before writing any Python. Pay special attention to edge cases a naive port gets wrong: (1) missing timestamps at the very first or last row — check experimentally whether the binary extrapolates or drops these rows, since standard linear interpolation is undefined there; (2) the exact tie-breaking/sort order of output rows (bucket_start_ms then user_id); (3) whether dedup happens before or after interpolation, which changes which timestamps are "nearest valid neighbors"; (4) exact CSV quoting/escaping conventions in the output (quote character, newline handling). Use Python's built-in `csv` module with correct newline handling for parsing, `hashlib.sha256` on UTF-8-encoded message bytes for dedup, and diff your script's output against the oracle's output byte-for-byte on your generated fixtures until they match exactly before considering the task done.
