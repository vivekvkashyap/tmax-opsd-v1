Build this Go program as a clear multi-stage pipeline rather than one tangled loop. (1) Parse both CSVs, truncate each timestamp down to the minute, and determine the global min/max minute across both files combined; generate every minute in that inclusive range as your canonical time axis. (2) For each minute, bucket raw readings per sensor and take the arithmetic mean where present, leaving a nil/NaN sentinel where absent — do this aggregation before any imputation, and keep it separate per sensor. (3) Impute pressure by scanning the aggregated series for gaps and linearly interpolating strictly between the nearest valid prior and next values (fraction-of-distance weighting, not a flat average). (4) Impute temperature by forward-filling from the last known aggregated value. (5) Compute the temperature rolling average over a trailing window of up to 5 minutes (fewer at the start) using the *filled* temperature series, not the raw one — this ordering matters. (6) Format each timestamp as RFC3339 (`2006-01-02T15:04:05Z`) and every float with exactly two decimals (`%.2f`), writing rows in chronological order to `/home/user/output.csv` with the exact header given. Trace through a couple of minutes by hand before running to catch off-by-one interpolation errors.