Start with the core dump: use `gdb /app/telemetry_parser /app/crash.core`, run `bt` to find the crashing frame, and inspect the stack/registers (`x/200xb <addr>` around the overflowed buffer, or `info registers`) to recover the actual input string that was being copied when it faulted — this tells you both the vulnerable code path and roughly how long/what shape the crashing input is. Next fix the build: add the missing `#include <string.h>` for the implicit `strcpy` declaration, and add `-lm` to the Makefile's link flags to resolve the undefined `sqrt` reference — compile and confirm a clean build before touching logic. Then locate the fixed-size stack buffer that receives the DEBUG-level message via `strcpy` and replace it with a bounded copy (e.g. `strncpy`/`snprintf` sized to the buffer, with explicit null-termination) so arbitrarily long messages are safely truncated rather than overflowing, while preserving byte-identical JSON output for all inputs that previously worked correctly. Build the result to `/home/user/telemetry_parser_fixed`, then self-test by piping both normal `[LEVEL] Message value` lines and deliberately oversized `[DEBUG]` lines through it, confirming no crash and consistent output on the benign cases, before relying on the 1000-case verifier.
