Fix the infrastructure before writing the script, and validate each service in isolation with `curl`/`redis-cli` before chaining them. In the Nginx config, add `proxy_pass http://127.0.0.1:5000;` inside the `/api/` location block, and change the `/data/` location to serve directly from `/app/backup_data/` (using `alias`, being careful that `alias` requires the location and target paths to correspond correctly, unlike `root`). In `redis.conf`, switch from unix-socket-only binding to `bind 127.0.0.1` with `port 6379`, then restart both services and confirm with `curl http://127.0.0.1/data/<file>` and `redis-cli -h 127.0.0.1 ping` that they work before scripting. In `archive_processor.sh`, read filenames from stdin in a `while IFS= read -r filename` loop; fetch metadata with `redis-cli --raw GET "file_meta:$filename"` and pipe it through `iconv -f UTF-16LE -t UTF-8` (mind that `--raw` avoids redis-cli's own escaping mangling binary data); fetch payload via `curl -s http://127.0.0.1/data/$filename`; append the audit line inside a `( flock -x 200; echo ... >> log ) 200>/tmp/audit.lock` subshell so the lock is held only for that append; and print the record block with `echo`, making sure the payload is emitted with `echo -n "$payload"` followed by exactly one newline before `---END_RECORD---` so byte-exact comparison against the oracle succeeds. Keep all diagnostic output on stderr.
