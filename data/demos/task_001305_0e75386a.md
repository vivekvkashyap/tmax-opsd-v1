Debug the vendored parser first: run `python3 -m legacy_doc_parser /home/user/legacy_docs.dat` in `/app/legacy-doc-parser-1.2.0` and observe it crash or hang trying to read an absurd payload length — this points to a byte-order bug in the binary header unpacking (`struct.unpack`) in the reader module; check whether it's using big-endian `'>I'` where the file was actually written little-endian `'<I'` (or vice versa) and flip it, then re-run to confirm it streams valid JSONL. Once it works, pipe its stdout through `jq -r '[.doc_id, .category] | @csv'` (or `jq -r '.doc_id + "," + .category'` for no quoting) to build the CSV, strip any header, then gzip it to `/home/user/summary.csv.gz`. The critical constraint is file size < 250 bytes — use `jq -r` with unquoted comma-joined output (avoid `@csv`'s quoting overhead if it pushes you over), and use `gzip -9` for max compression; check the result with `stat -c%s /home/user/summary.csv.gz` (or `ls -l`) and iterate on formatting if it's too large.
