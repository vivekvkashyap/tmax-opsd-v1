Work stage by stage and verify each before moving on. Stage 1: run `tesseract /app/audit_policy.png stdout` (or convert/preprocess with ImageMagick first if OCR is noisy) to recover the printed SHA256 hash text from the policy image; then write a small Python brute-force loop over all 4-digit PINs `0000`-`9999`, forming candidate `"<pin>audit"`, hashing each with `hashlib.sha256(...).hexdigest()`, and comparing against the OCR'd hash to recover the plaintext password — don't hardcode a guessed password. Stage 2: use that recovered password as `-passout pass:<password>` (or interactively) with `openssl req -x509 -newkey rsa:2048 -keyout /home/user/audit_key.pem -out /home/user/audit_cert.pem ...`, making sure the key is actually passphrase-encrypted (not `-nodes`). Stage 3: for the classifier, tokenize the invocation and flag EVIL if any argument/value contains shell metacharacters (`;`, `&&`, `|`, `$(`, backtick) or if a `--password`/`--secret`/`--token` flag is immediately followed by a non-empty cleartext value; otherwise print CLEAN — test against a few of your own constructed examples covering each rule before finishing, since the hidden corpus checks both false positives and negatives.
