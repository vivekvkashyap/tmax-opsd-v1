Structure `solve.py` as: parse the three sys.argv integers, connect to Redis (`localhost:6379`, `db=0`), build the cache key `f"graph:{node_id}:{limit}:{offset}"`, and if `r.get(key)` hits, print it verbatim and exit immediately (do not touch SQLite in that path). On a cache miss, connect to `/app/data/graph.db` with `sqlite3` and write a JOIN across `edges` and `nodes` filtering `src = ?` and `active = 1`, ordered by `score DESC, id ASC`, with `LIMIT ? OFFSET ?` — the key correctness trap is the corrupted `idx_edges_src` index, so you must force the query planner to ignore it, e.g. by adding `NOT INDEXED` on the `edges` table reference in the FROM clause (or an equivalent trick like `+src = ?`) rather than dropping/rebuilding the index, since you're told not to alter schema. Serialize the result as a JSON array of `{"id":..., "name":..., "score":...}` objects using compact separators (no spaces) so the string is deterministic and byte-identical to any reference implementation, `SET` that exact string into Redis under the same key, then print it to stdout with nothing else on stdout. Test manually against a few (node_id, limit, offset) triples, including cache-hit and cache-miss paths, to confirm the JSON formatting matches exactly.