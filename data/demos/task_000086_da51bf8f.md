Avoid the O(E^3) triple-nested-loop temptation: build indexed in-memory structures first. Parse `nodes.csv` into a node_type lookup (array or hash keyed by node_id), then parse `edges.csv` twice into two separate adjacency structures — a "knows" adjacency list (A -> list of B) and a "lives_in" adjacency list (Person -> City), the latter ideally as a hash map since each person likely lives in one city, giving O(1) lookup. Then for each "knows" edge A->B, do an O(1) lookup of A's city and B's city and check if they match — this turns the join into O(E) instead of scanning all edge triples. Use a simple array-based hash map or even a fixed-size array indexed by node_id if IDs are small/dense (check the actual ID range in nodes.csv first). Write matches to a dynamic array of (A,B,C) triples, then sort them with `qsort` using a comparator that orders by A_id, then B_id, then C_id before writing to `/home/user/matches.csv`. Compile with `gcc -O2 -o query query.c` and run it, then sanity-check the output file's line count and ordering manually against the input edges before considering the task done.