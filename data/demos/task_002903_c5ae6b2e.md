Build and wire the three pieces independently, testing each before chaining them. (1) In `compute_dist.cpp`, implement standard Levenshtein edit-distance (classic DP table, O(len1*len2) per pair) as a function, read all word pairs from the input file up front, then parallelize across pairs using OpenMP (`#pragma omp parallel for` over the pair list, writing results into a pre-sized results array/vector so threads don't race on shared output) or `std::async`/`std::thread` with a work-splitting approach — write results to the output file only after all threads finish, in original order. Compile with `-fopenmp` if using OpenMP. Verify correctness on the known kitten/sitting=3 style examples by hand before moving on. (2) `run_pipeline.sh`: loop `for f in /home/user/incoming/*.csv`, run the compiled binary on each with matching output filename in `/home/user/outgoing/`, then `rm` the original only after the binary succeeds — check exit status. (3) Install the cron job with `(crontab -l 2>/dev/null; echo "*/15 * * * * /home/user/run_pipeline.sh") | crontab -`, then confirm with `crontab -l`. Do not execute the pipeline yourself as instructed — only set it up and verify via inspection.
