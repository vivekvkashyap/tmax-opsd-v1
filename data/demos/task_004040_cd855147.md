Build the operator as an event-driven state machine, not a fixed sleep-and-hope script. Start `db` with `subprocess.Popen(..., stdout=subprocess.PIPE, text=True)` and read its stdout line-by-line (or use `pexpect.spawn` with `expect("SYSTEM_READY")`) so you detect readiness the moment it's printed rather than guessing a delay — log `[EXEC] Starting db` before spawning and `[STATE] db is ready` the instant the marker line appears. Only then spawn `app` similarly, logging `[EXEC] Starting app`. After spawning `app`, call `.wait()` (or poll `.poll()`) to get its real exit code; a non-zero code means log `[WARN] app crashed, restarting` and relaunch, incrementing a restart counter. Track restarts against `max_restarts` (3, i.e. 4 total attempts) — when the counter is exceeded, log `[ERROR] app max restarts exceeded` and stop, but if `app` exits 0 before hitting the limit, stop cleanly too (don't keep restarting a succeeded process). Open the log file in append mode and flush/write each line exactly as specified so ordering and text match precisely. Parse `manifest.json` generically (don't hardcode values) so the script reflects the config-driven design implied by the task.
