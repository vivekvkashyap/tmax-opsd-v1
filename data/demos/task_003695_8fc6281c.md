Set up a Go module first (`go mod init tracker && go get golang.org/x/text/encoding/unicode` and `golang.org/x/text/encoding/charmap` for ISO-8859-1) since UTF-16LE and ISO-8859-1 need real decoders, not naive byte casts. List `/home/user/configs/`, extract the `YYYY-MM-DD` date from each filename with a regex, and sort the file list by that date string (which sorts correctly lexicographically). Decode each file per its extension: `.utf8` as-is, `.iso88591` via `charmap.ISO8859_1.NewDecoder()`, `.utf16le` via `unicode.UTF16(unicode.LittleEndian, unicode.IgnoreBOM).NewDecoder()`. Extract `ServerName` and `MaxConnections` with case-insensitive regexes that tolerate arbitrary whitespace around the colon (e.g. `(?i)servername\s*:\s*(\S+)`). Validate the server name against `^[A-Z]{3}[0-9]{2}$` exactly — reject and skip the whole file (not just the field) if it fails or is missing, even if that file's MaxConnections looks anomalous. Walk files in date order, tracking the first valid file where MaxConnections > 1000 strictly, and write only that file's date/value/server to the JSON in the exact required key order and format. Double-check your regex isn't matching invalid names like `DB00` (2 letters) or `INVALID99` (9 letters) as valid.
