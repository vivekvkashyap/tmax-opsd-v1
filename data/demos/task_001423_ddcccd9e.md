Tackle this as four independent hardening steps, checking each against `/app/verifier/score.py` incrementally rather than only at the end. (1) Generate the cert with `openssl req -x509 -newkey rsa:4096 -keyout server.key -out server.crt -days 365 -nodes ...`, place both under the certs path, then edit nginx.conf's `ssl_certificate`/`ssl_certificate_key` directives and set `ssl_protocols TLSv1.2 TLSv1.3;` explicitly (removing any older protocol versions). (2) Add an `add_header Content-Security-Policy "..."` line to the relevant server/location block using the exact policy string given — nginx requires `add_header` to be repeated in every nested block that should inherit it, so verify it actually appears via `curl -kI`. (3) Generate a new 16-char alphanumeric secret (e.g. `python3 -c "import secrets,string;print(''.join(secrets.choice(string.ascii_letters+string.digits) for _ in range(16)))"`), update `requirepass` in redis.conf, restart redis, and update wherever Flask reads the password (env var or hardcoded) so the two stay in sync — then confirm `redis-cli -a old_secret_pass ping` fails and the new one succeeds. (4) In start.sh, wrap the Flask launch command with `bwrap --unshare-pid --ro-bind / / --bind /tmp /tmp --share-net -- <flask start command>`, restart the stack, and check the end-to-end curl flow still works through nginx before writing the new password to `/home/user/rotation_complete.log`.