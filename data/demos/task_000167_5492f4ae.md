Reverse-engineer the binary before writing any exploit code: run `strings /home/user/app_cgi` to look for `HTTP_`-prefixed environment variable names and any literal comparison strings (role/token values, cookie prefixes) embedded in the binary — these reveal what `getenv()` calls it makes and what values it checks for. Cross-check with `objdump -d` or a disassembler around calls to `getenv`/`strcmp`/`system` if strings alone are ambiguous, since the backdoor likely gates on an exact string match before invoking a shell command. Once you've identified the required header/cookie names and their expected values from the binary itself (do not guess), write `exploit.cpp` that calls `setenv()` for each required variable with the values you discovered, then invokes `/home/user/app_cgi` via `system()` or `execve()` so its `getenv` calls see them as if set by a CGI server. For the payload itself, set whatever variable the binary uses to smuggle a command so it runs `cat /home/user/secret_flag.txt > /home/user/exfiltrated.txt`. Compile with `g++ exploit.cpp -o exploit`, run it, then diff `exfiltrated.txt` against `secret_flag.txt` to confirm an exact match.
