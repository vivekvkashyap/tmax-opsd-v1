Build and verify each piece before chaining them. Compile `alerter.rs` with `rustc -O /home/user/alerter.rs -o /home/user/alerter`, then create `/home/user/data/releases/v1` and `ln -s /home/user/data/releases/v1 /home/user/data/active` (symlink, not a copy). For the supervisor, write a bash loop that: starts `collector.sh &`, then polls (`while [ ! -f /home/user/data/ready.flag ]; do sleep 0.5; done`) before touching the alerter at all — this dependency ordering is the whole point of the task. Then start `alerter &`, capture its PID, and enter a monitoring loop using `wait "$pid"` (or repeatedly `kill -0 "$pid"` in a poll loop) so that when the process dies for any reason you immediately relaunch it with a fresh `alerter &` and update the tracked PID — a naive one-shot `&` without a respawn loop will fail the "kill it and confirm it restarts" test. Launch `supervisor.sh` itself in the background (`nohup ... & `), and write `$!` to `/home/user/supervisor.pid` — that PID must belong to the still-running supervisor process itself, not a subshell that already exited. Test manually: `kill -9` the alerter PID, wait a couple seconds, and confirm via `pgrep` that a new alerter PID appears.
