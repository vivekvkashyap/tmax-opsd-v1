First get the rules out of the image: install tesseract-ocr/pytesseract (or use the `tesseract` CLI) on `/app/schema_rules.png`, then correct obvious OCR artifacts by re-checking against the rendered image — don't guess a rule you can't actually read. With the literal rule text in hand, implement cleaner.py to parse exactly 4 comma-separated fields (ID, Temperature, Status, Date) and apply each rule precisely: the ID field's numeric/padding rule and missing-value fallback, Temperature's missing/invalid fallback and rounding rule (watch for the string "NaN" as well as empty values), Status code mapping with a fallback for unrecognized/empty input, and Date passed through unchanged. Make it robust to malformed field counts. Since grading fuzzes many random inputs against a hidden oracle, test your script yourself across edge cases (missing fields, negative/decimal temperatures, unexpected status letters, garbage dates) to confirm your reading of the rules is self-consistent, not just correct on the obvious examples.
