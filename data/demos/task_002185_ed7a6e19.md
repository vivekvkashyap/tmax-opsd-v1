For the log rotator, write a script that counts lines with `wc -l < /home/user/monitor/network.log`, and only if strictly greater than 50, shifts existing rotated logs from oldest to newest first (e.g. `[ -f network.log.2 ] && mv network.log.2 network.log.3`, then `.1`→`.2`, guarding each `mv` with an existence check so missing older files don't error) before copying the current log's content into `.1` and truncating the original in place with `: > network.log` or `cp file file.1 && > file` (not `rm`, since the file descriptor must be preserved). For the supervisor, wrap the existing status check in `while true; do ... ; sleep 1; done`, and when `pgrep -f "dummy-service.sh"` finds nothing, launch `/home/user/scripts/dummy-service.sh &` and `echo "Restarted dummy-service"`. For route parsing, read `routes.conf` line by line and validate with `awk`/regex: exactly 3 whitespace-separated fields, fields 1 and 2 matching a dot-separated-four-number pattern (no letters), and field 3 starting with `eth` or `tun` — reject lines with wrong field count (including trailing extra tokens) or non-numeric IP-like fields, writing only the exact matching lines to `active_routes.txt`. Make every script executable with `chmod +x`.