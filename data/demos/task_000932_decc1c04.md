Fix the pieces bottom-up and validate each layer before moving to the next, since later stages depend on earlier ones compiling/running. (1) Edit `proto/auth.proto` to add `map<string, string> attributes = 3;` inside `CheckRequest`, then regenerate Go stubs with `protoc --go_out=... --go-grpc_out=...` and fix `go.mod`/`go.sum` with `go mod tidy` inside `/home/user/app/auth` until it builds. (2) For `parser.go`, the fastest way to get bit-exact behavior against the reference is to build a proper recursive-descent (or precedence-climbing) parser for the grammar of `==`, `!=`, `&&`, `||`, parentheses, and identifiers/string literals — respecting standard precedence (`!=`/`==` binds tighter than `&&`, which binds tighter than `||`) and left-associativity, since a naive left-to-right token scan silently gets nested/parenthesized expressions and precedence wrong. Compile a standalone CLI (`auth_eval`) that takes the expr and JSON attribute string as argv, evaluates, and prints exactly `true` or `false` (no trailing extras). Test it manually against varied expressions with parens and mixed operators before trusting it, since the grader fuzzes hundreds of random expressions. (3) Only after auth_eval is solid, fix `start.sh` and `gateway/config.yaml` to point at the right ports/hosts and background all three services, then verify end-to-end with a curl request that should return 200.