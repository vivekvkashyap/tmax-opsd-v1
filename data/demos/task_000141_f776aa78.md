Build this as several small, independently testable pieces rather than one monolith, then background them all. First get the watcher loop right: poll (or inotify-watch) `/home/user/raw_logs/` for `*.done` markers, and derive the timestamp from the marker's filename to locate the matching `.log` file — process each `.done` exactly once (track processed names) so you don't double-archive. For splitting, use `split -b 102400 -d` (or equivalent) with a systematic suffix naming scheme, zip the resulting chunks with a deterministic archive name `archive_<timestamp>.zip` into `/home/user/archived_logs/`, then atomically update the `latest.zip` symlink (write to a temp symlink name and `mv`/`ln -sf` over it) so readers never see a broken link mid-update, then clean up the `.log`/`.done`/chunk files. For networking, don't hand-roll TCP parsing sloppily: an HTTP file server is one line (`python3 -m http.server 8080 --directory ...` or a small language-native static server), and the TCP server on 9090 must read one line per connection, strip the trailing `\n`, and branch on exact string `LATEST` vs `COUNT` — resolve `LATEST` via `readlink` on `latest.zip` and take just the basename, and `COUNT` via counting `*.zip` files in the archive directory at request time (not cached), always terminating each reply with `\n`. Launch watcher and both servers as background/daemon processes so they keep running after your script returns.
