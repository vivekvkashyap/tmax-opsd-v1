Handle each deliverable as a separately verifiable step rather than one big pass. Fix `urldecode.c`: the buffer is allocated as `malloc(len)` with no room for the null terminator and no bounds checks before reading `input[i+1]`/`input[i+2]`, which is the heap-corruption source — allocate `len + 1` bytes, and before treating a `%` as an escape sequence, verify both following characters exist within the string and are valid hex digits (`isxdigit`); if not, copy the `%` through literally instead of consuming it as an escape. Rewrite `stress.go` to launch 100 concurrent goroutines (not sequential loop iterations) each running the compiled binary with a payload, using a `sync.WaitGroup` to wait for completion and a shared counter/channel to detect any non-zero exit code, printing "PASS" only if all 100 succeeded. For cross-compilation, use plain `gcc` for the amd64 target and the `aarch64-linux-gnu-gcc` cross-compiler for arm64, passing `-DSECURE_MODE` to both invocations — verify with `file` that the arm64 binary reports the correct architecture, and confirm the amd64 binary actually behaves differently (no "INSECURE" print, exit 0) when SECURE_MODE is active. Finally compute `sha256sum` of both binaries into `/home/user/build/hashes.txt` in the exact two-line format specified, after running the updated stress test.