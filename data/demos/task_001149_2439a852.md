Work through this in strict dependency order. First, open `/app/cJSON-1.7.15/Makefile` and locate the `CC` variable — it's pinned to a cross-compiler that doesn't exist on this x86_64 box; change it to `gcc` (or `cc`), then run `make` in that directory to produce `libcjson.so`/`libcjson.a`; if `make` still fails, check for other cross-toolchain flags (e.g. `AR`, sysroot flags) that may also need clearing. Second, write `sensor_filter.c` using `cJSON_Parse` on the full file contents (read the whole file into a buffer, don't assume a fixed size), then use `cJSON_GetObjectItemCaseSensitive` to fetch `sensor_id` and `value`; validate `cJSON_IsString(sensor_id) && strlen(sensor_id->valuestring) < 32` and `cJSON_IsNumber(value)` — reject (exit 1) on any parse failure, missing key, wrong type, or failed file open, otherwise exit 0. Compile with `gcc sensor_filter.c -I/app/cJSON-1.7.15 -L/app/cJSON-1.7.15 -lcjson -o sensor_filter` and remember you'll need `LD_LIBRARY_PATH` pointing at the .so location to run it standalone before deployment. Before writing deploy.sh, test sensor_filter manually against a few of your own valid/invalid JSON samples to confirm the exit codes are correct, then loop it over every file in both corpora to check 100% pass before finalizing. Only then write deploy.sh implementing the ordered steps: mkdir -p releases/v2, conditionally back up the target of an existing `current` symlink (use `readlink` to resolve it, then `cp -r`), copy the binary and .so into releases/v2, and use `ln -sfn` for an atomic symlink update to `current`.
