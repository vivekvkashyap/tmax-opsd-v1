Work the two subtasks independently, then tie them together with a test harness. For the memory dump: don't assume the key is null-terminated or aligned — run `strings /home/user/app/memory.bin | grep -oE 'TEST_KEY_[0-9]{6}'` (or a Python regex scan over the raw bytes) to reliably pull the pattern out of binary noise, then write only that match (no trailing newline issues) to key.txt. For the C++ fix: locate the `calculate_ema` function and change the previous-EMA multiplier from `(1.0 + k)` to `(1.0 - k)` — verify by hand-tracing the recurrence with the default input values so you know what output to expect before you build the test. For test.sh: compile with `g++ analyzer.cpp -o analyzer`, capture stdout via command substitution, compare it exactly (string equality, not just checking the compile succeeded) against the expected `EMA: ...` line, and exit 0/1 accordingly — remember to `chmod +x test.sh` and make sure it also exits 1 if `g++` fails (check the compiler's exit status, not just run the possibly-stale binary).