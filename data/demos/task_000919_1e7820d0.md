Tackle this as three separate fixes verified independently before wiring them together. First, inspect `/home/user/config/` and `start_services.sh` to find the actual env vars/config keys the Flask API and worker read (e.g. `REDIS_HOST`/`REDIS_PORT` in an `.env` file, a `queue_name` in a worker conf) and make them consistent with each other and with the real Redis port — start each service individually and check logs/connectivity before assuming the wiring is correct. Second, for the non-determinism bug in the C++ ODE solver, look specifically at OpenMP parallel reduction loops (`#pragma omp parallel for reduction(...)`): floating-point addition is not associative, so thread-scheduling-dependent partial-sum ordering changes results run-to-run. Fix it by making the reduction order deterministic — e.g., replace the parallel reduction with a fixed-order sequential accumulation, or partition the array into fixed-size chunks accumulated in a fixed order and combined deterministically (a pairwise/Kahan summation with static, not dynamic, chunk assignment) — then rebuild with the Makefile and run the solver twice on identical input to confirm bit-identical output. Third, for the trajectory filter, compute each CSV's energy conservation drift (e.g., max deviation of a computed invariant across timesteps) and delete files where drift exceeds 1e-12, leaving others untouched — test it against a few known-good/known-bad files you construct yourself before running it on the real corpus. Finish by curling `/generate`, confirming the worker's CSV lands in the data directory, running your filter on that directory, and logging "PIPELINE_SUCCESS".