Fix and verify each language component independently before integrating. For the C Makefile: open it in a way that reveals whitespace (`cat -A` or `sed -n l`) since "spaces instead of tabs" errors are invisible to the eye — replace the leading indentation of the recipe line with a literal tab, add `-lm` to the gcc invocation (the code uses `sqrt`), then run `make` and confirm the `prime` binary is produced and runs. For Rust, `cargo build` and read the borrow-checker error carefully — a function declared to return `&'static str` but building a local `String` and returning `&s` is invalid because the `String` doesn't outlive the function; fix by returning an owned `String` (change the return type and drop the `&`), then rebuild with `cargo build --release`. For the Go interpreter, implement a simple loop reading `program.math` line by line, splitting each line into opcode and operand, maintaining an int accumulator, and for `PRIME`/`FACTORS` shelling out via `os/exec` to the compiled C/Rust binaries, parsing their trimmed stdout as an integer, and adding it in. Test the interpreter against the DSL file by tracing through each instruction by hand first so you know what the accumulator should be at each step, then run `go run main.go` and check `result.txt` matches your hand trace.
