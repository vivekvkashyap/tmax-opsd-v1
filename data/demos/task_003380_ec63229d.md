Tackle the four bugs independently; verify each in isolation before wiring the service up. (1) In `process_audio.sh`, find the unquoted `$1` passed to the transcription call — filenames with spaces break word-splitting — quote it, then rerun against the actual audio path (also quoted) and capture the printed token; use that recovered value, don't invent one. (2) The SQLite DB is corrupted but its WAL is intact: don't repair the main file directly — use `sqlite3 responders.db ".recover"` or replay the WAL against a fresh copy so SQLite applies the WAL frames, then confirm `SELECT * FROM responders;` returns valid coordinates. (3) Use `git log` over the commit history, then `git bisect start` with current HEAD marked bad and an early commit good, driving it with `git bisect run ./run_tests.sh` to land on the exact commit that broke the Haversine formula in `src/distance.cpp`; fix only the introduced math error, confirm tests pass, then `git bisect reset`. (4) `make`, run the server on 127.0.0.1:8080 checking `Authorization: Bearer <token>` against the token from step 1, querying the recovered DB and computing distance with the corrected formula — leave it running and curl it yourself before finishing.