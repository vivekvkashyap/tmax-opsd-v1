Diagnose before rewriting: read `process.sh`'s awk block — it computes variance as `E[X^2] - (E[X])^2`, the naive single-pass formula, which loses precision catastrophically when values are large with small spread (as with the big near-identical transaction amounts in this data), sometimes producing a value that awk's floating point renders slightly negative and trips `assert_non_negative`. Fix it by switching that awk block to a numerically stable approach: either a genuine two-pass method (first pass computes the mean, second pass sums `(x-mean)^2`), or streaming Welford's algorithm (maintain running mean and M2, updating `delta=x-mean; mean+=delta/n; M2+=delta*(x-mean)`, final variance = `M2/n`) — either keeps values bounded and avoids cancellation. Keep the same `count/avg_amount/variance` insertion logic into `user_stats` unchanged otherwise. Separately, open `report.sh` and fix the WHERE clause, which currently filters on the wrong column (`variance` instead of `avg_amount`) — change it to `WHERE avg_amount > 100.0` per the ticket. Then run `./process.sh` followed by `./report.sh > final_report.txt` and confirm the CSV rows look numerically sane (no negative or wildly large variances).
