Start by extracting the actual business rules from the audio — don't guess them. Use `ffmpeg` to convert `/app/qa_report.wav` to a format a speech-to-text tool accepts, then run a transcription tool (install `whisper` or another STT library if needed) and read the resulting text carefully to identify every distinct rejection rule it states (this determines what "evil" means — do not assume typical log-sanitizer rules without checking the transcript). Fix the Makefile by ensuring correct include paths for headers and correct link flags/order for `libmocklog.a` (static libs generally need to appear after the object files that reference them on the link line, with an explicit `-L<dir> -lmocklog` or a full path), and make sure it emits the binary at the exact `bin/log_sanitizer` path. Implement `sanitizer.c` as a single streaming pass over the file (a Trie or simple state machine works well for detecting a fixed forbidden substring while scanning), checking the rules you transcribed — likely include a literal-substring check, a check for a class of disallowed characters, and possibly HTML-like markup detection; verify each rule condition explicitly against what you transcribed. Build with `make`, then self-test the binary against every file in `/app/corpus/clean/` (expect exit 0) and `/app/corpus/evil/` (expect exit 1) before finishing, fixing any misclassifications you find.