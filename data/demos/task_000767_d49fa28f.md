Structure the Go program around Go's standard concurrency-safe primitives rather than ad hoc checks. Use `syscall.Flock` (or `golang.org/x/sys/unix.Flock`) with `LOCK_EX|LOCK_NB` on an opened `/home/user/backup.lock` file descriptor, exiting non-zero immediately if the lock fails. Parse the JSON manifest with `encoding/json` into a struct with a `Targets []string` field. For traversal, don't use `filepath.Walk` naively on symlinked directories — use `os.Lstat` to detect symlinks explicitly, resolve with `filepath.EvalSymlinks` or `os.Readlink`, and maintain a `map[string]bool` of visited real (canonicalized/absolute) paths; before descending into a symlinked directory, check whether its resolved target is already in the visited set for the current traversal branch and skip it if so — this is the core anti-loop mechanism. For each `.csv` file's contents, apply a regex substitution like `regexp.MustCompile(`SECRET_KEY=[A-Za-z0-9]+`).ReplaceAll(data, []byte("SECRET_KEY=REDACTED"))` before adding it to the archive. Build the tar.gz with `archive/tar` + `compress/gzip`, setting each entry's `Name` field relative to the target root (strip the `/home/user/config_manager/data/` prefix) so paths inside the archive are relative, not absolute.
