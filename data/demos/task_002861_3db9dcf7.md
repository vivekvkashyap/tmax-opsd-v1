Begin by transcribing `/app/memo.wav` with a speech-to-text tool (whisper, vosk, or similar) to recover the precise business rules — note especially how MIN/MAX modifiers behave (they don't push onto the stack, they update global result bounds), the default bounds when neither appears, and what happens when a modifier repeats. Then implement a standard RPN evaluator in `/home/user/rpn_eval.py`: tokenize by whitespace, maintain an operand stack, treat `+ - * /` as binary pops (integer division truncates like `int()` on the float quotient), and treat MIN/MAX tokens as consuming the next token as a bound update rather than a push. Any malformed token, too few operands, division by zero, a bad MIN/MAX argument, or anything other than exactly one value left on the stack at the end must print `ERROR`. Clamp the sole remaining value to the active bounds before printing. Since grading is fuzz-equivalence against a hidden reference, write your own broad edge-case tests (empty stack, repeated modifiers, trailing garbage) to shake out mismatches before submitting.