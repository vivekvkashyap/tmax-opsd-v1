Build the pipeline as three separable, independently testable pieces. (1) The join: since both input files are pre-sorted by `id` with no headers, use the standard `join` command (`join /home/user/artifacts.txt /home/user/metrics.txt`) rather than writing custom merge logic — verify its output columns are exactly `id size error_rate` before piping further. (2) The C++ program: implement the Bayesian posterior formula exactly as specified (P(C|E) = P(E|C)P(C) / [P(E|C)P(C) + P(E|~C)P(~C)]) using doubles, then compute the Euclidean distance to the "perfect blank" point (posterior=1.0, normalized_size=0.0) using `size/1000.0` for the size term. Read records from stdin in a loop (id, size, error_rate), store them in a vector, sort by ascending distance, and print only the top-3 ids one per line — watch for off-by-one errors in how many are printed and make sure sort is stable/correct on ties. (3) Wire it together: `pipeline.sh` should `chmod +x` itself, compile detect.cpp (e.g. `g++ -O2 -o detect detect.cpp`), and redirect `join ... | ./detect > /home/user/top_anomalies.txt`. Test by running the script and manually recomputing one or two posteriors/distances by hand to confirm your formula matches.