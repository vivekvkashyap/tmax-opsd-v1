Fix the wiring first, then the logic, testing after each change with the actual corpus files. In `/app/nginx/nginx.conf` add a `location /intake { proxy_pass http://127.0.0.1:5000; }` block inside the server listening on 8080 (make sure it forwards method/body, and reload/restart nginx via whatever `start_all.sh` uses). In `/app/flask/.env` change the Redis host/port to `127.0.0.1`/`6379` to match the locally running redis-server. For `sanitizer.py`'s `check_cycles`, the recursive function needs a per-path visited set threaded through the recursion (not a single shared "seen" set) so it can detect `node in current_path` and immediately return False (reject) instead of recursing forever on a cycle like A->B->A — passing an accumulating list/set by argument and removing the node on backtrack is the standard DFS cycle-detection pattern. For the nanosecond check, stop casting `start_ns`/`end_ns`/`duration_ns` through `float()` (which loses precision on large integers) and instead parse them with `int()` directly from the string fields, comparing `int(end_ns) - int(start_ns) != int(duration_ns)` to reject. Run `/app/start_all.sh`, then POST each file in `/app/corpus/clean/` and `/app/corpus/evil/` to `http://127.0.0.1:8080/intake` with curl and confirm 200 vs 400 responses before finishing.