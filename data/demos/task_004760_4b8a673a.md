Reverse-engineer `/app/cfg_verify` first with `strings`/`objdump`/`file` to confirm the fixed-size magic-header length and byte pattern preceding the tar.gz stream — don't assume, extract the exact magic bytes and offset from the binary's constants/behavior. Then write `/home/user/cfg_filter.c` to: read and memcmp the header bytes at a fixed offset (reject as EVIL if mismatched), then hand the remainder to `libarchive` (`archive_read_open_filename`/`archive_read_open_memory` with gzip+tar filters) rather than shelling out to `tar` — that's exactly the vulnerability you're replacing. For each entry from `archive_read_next_header`, check the pathname for `../` components or a leading `/` (reject), and for symlink entries (`archive_entry_symlink()`), reject if the link target is absolute or contains `../`. To detect nesting depth, when an entry's content itself looks like a tar/gz stream (check magic bytes or just try opening it as nested archive), recursively parse it, tracking depth, and reject once depth exceeds 2. Any corrupted/unreadable archive header should also be EVIL. Compile with `-larchive`, then self-test against both `/app/corpora/clean/` and `/app/corpora/evil/`, checking exit codes with a loop before considering it correct.
