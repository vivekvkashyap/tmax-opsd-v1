Build the pipeline in stages and validate each before moving on. First extract labels: open `/app/labels.mp4` with `cv2.VideoCapture`, read frames in order (frame index i pairs with row i of `texts.csv`), and classify each frame's dominant color by comparing mean/summed R vs G channel intensity (green-dominant -> label 1, red-dominant -> label 0) — don't hardcode exact pixel values, derive the threshold from the actual channel comparison since encoding may introduce compression noise. Sanity-check frame count matches row count (1000) before merging. Then build an sklearn `Pipeline` (e.g. `TfidfVectorizer` or `CountVectorizer` + `LogisticRegression`/`LinearSVC`), and use `GridSearchCV` or similar cross-validation to tune hyperparameters (C, ngram range, etc.) rather than picking one config blindly — the hidden test set is a similar but distinct distribution, so avoid overfitting to a single feature setting. Save the entire fitted pipeline (vectorizer + classifier together) via `joblib.dump` to `/home/user/best_model.pkl`. Finally, fix `/app/plot_results.py`: the bug is calling `plt.show()` before `plt.savefig()`, which empties the figure buffer under a non-interactive backend — either drop the `show()` call or set `matplotlib.use('Agg')` at the top and reorder so `savefig('/home/user/cv_results.png')` runs on the still-populated figure, then execute the script.
