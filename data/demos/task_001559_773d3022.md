Work this as a forensics-then-fix chain, each step feeding the next. First, extract the crash user ID from the binary dump with `strings /home/user/memory.dump | grep FATAL_USER_ID` (or grep the raw bytes directly) to recover the numeric ID — don't assume it, read it out of the dump. Second, query `sqlite3 /home/user/users.db "SELECT metadata FROM users WHERE id=<recovered_id>;"` to pull that user's JSON metadata, and inspect it for the anomalous field/value that's out of the normal valid range (compare against a couple of other users' rows to see what "normal" looks like). Third, edit `validate_user_data` in `/home/user/app.py` so that after `json.loads`, it explicitly checks the anomalous field against the invalid condition you identified and raises `ValueError("Invalid data")` in that case, otherwise returning `True` as before — keep the existing JSON-decode-failure handling intact. Fourth, write `/home/user/test_regression.py` using `unittest`, importing `validate_user_data` from `app`, with a `TestUserValidation.test_negative_points_raises_error` method that calls `assertRaises(ValueError, validate_user_data, <the exact metadata string you queried from the DB>)`. Finally run `python3 -m unittest /home/user/test_regression.py > /home/user/test_result.log 2>&1` and open the log to confirm it actually shows `OK` before considering the task done.