Start by locating the actual perturbation before touching anything else: read `/app/fast-metric-exporter/setup.py` and compare its `ext_modules` handling against what a working Cython build needs — a common break is `ext_modules` being an empty list instead of `cythonize("parser.pyx")` (or the `.pyx` glob), so fix that wiring rather than rewriting the parser itself. Build/install with `pip install .` (or `python setup.py build_ext --inplace && pip install -e .`) as the `user` account, and confirm the compiled extension actually loads (e.g. `python -c "import parser"` succeeds, not falling back to a pure-Python path) since the 3-second performance bar can only be met by the compiled version. For the timezone requirement, set `TZ=Asia/Tokyo` (and ensure the `tzdata` package is installed) via the systemd unit's `Environment=` directive rather than patching the parser source. For ACLs, use `setfacl -m u:user:rwx /home/user/app_logs` for the directory itself and `setfacl -d -m u:user:r-- /home/user/app_logs` to set the default ACL so new files inherit read-only for that user — verify with `getfacl`. Write the systemd `--user` unit under `~/.config/systemd/user/fast-exporter.service` with `ExecStart` pointing at the installed binary and the correct input/output paths, then `systemctl --user daemon-reload`, `enable`, and `start` it, checking `metrics.json` is produced before creating `solution_ready.txt`.
