Locate the leak precisely: the current `compute_zscore` computes mean/stddev once over the whole vector, so every index — including early ones — is normalized using data from the future. Rewrite it so that for each index `i`, you accumulate mean and sample standard deviation (divide by `n-1`) using only elements `[0, i-1]`, recomputing incrementally as you scan forward (a running sum and running sum-of-squares avoids O(n^2) rescans); define the result as `0.0` whenever fewer than 2 prior elements exist (i.e. `i` is 0 or 1). For `compute_pearson_correlation`, implement the standard formula: `covariance(x,y) / (stddev(x) * stddev(y))`, computed directly over both full vectors (this one is not windowed). Don't touch `main.cpp` or `CMakeLists.txt` — only `processor.cpp`. Build via `cmake ..` then `make` inside `build/`, run the executable against the CSV, capture the printed correlation to exactly 4 decimal places from stdout (don't recompute it separately in another language — use the exact printed value) for the JSON log, and inspect `predictions.csv` to sanity-check the first few rows follow the expanding-window rule by hand.
