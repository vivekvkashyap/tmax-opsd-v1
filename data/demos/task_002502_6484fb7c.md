Start by reading `walker.go` in `/app/cap-analyzer` to find where it recurses/walks directories — the bug is that it follows symlinks without tracking which inodes it has already visited, so cyclic symlink structures cause infinite/exponential traversal. Fix it by, on each file/dir visited, calling `os.Lstat` (not following the symlink blindly) and pulling the device+inode via `stat.Sys().(*syscall.Stat_t)`, storing visited `(dev, ino)` pairs in a `map` and skipping any path whose inode was already seen — this both prevents infinite loops and ensures hardlinked/duplicate files aren't double-counted toward disk usage. Rebuild with `go build` and place the binary at `/home/user/bin/cap-analyzer`; time it against a synthetic cyclic-symlink test tree to confirm it now finishes quickly instead of hanging. Add a `post-receive` hook script (executable, in `data-model.git/hooks/`) that invokes the analyzer with `--target` and appends a `[TIMESTAMP] SIZE_IN_BYTES` line to `/home/user/logs/capacity.log` (use `date` for the timestamp and capture the analyzer's byte-count output). Finally start a simple HTTP static file server (e.g. `python3 -m http.server` or similar) bound to `127.0.0.1:8080` serving `/home/user/logs/`, running detached in the background.