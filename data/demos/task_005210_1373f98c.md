Build the ETL in clear stages rather than one monolithic pass. First, read each file with its declared encoding explicitly (`open(path, encoding='utf-16-le')` / `encoding='iso-8859-1'`) — do not rely on the default locale encoding, which will corrupt or fail on these files. Parse sensor_a's `YYYY-MM-DD HH:MM:SS` timestamps and sensor_b's `MM/DD/YYYY-HH:MM` timestamps with two separate format strings (e.g. `datetime.strptime`), then truncate both to just the date for bucketing. Convert every sensor_b reading to Celsius with the given formula before merging — never mix F and C values in the same aggregation. Group all readings (from both sensors) by date, then compute min/max/avg per date; format the average with exactly 2 decimal places (e.g. Python f-string `.2f`) while min/max keep their natural precision. Read the template file's raw text and do a simple placeholder substitution (`.replace('{DATE}', ...)` etc.) rather than hardcoding a new template — the task requires reusing the exact given template. Sort dates ascending, join each populated block with a blank line, write to the output path, then actually execute the script so the output file exists on disk.
