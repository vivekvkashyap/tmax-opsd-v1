Build and test this pipeline stage by stage rather than writing all scripts then running once — each piece is independently verifiable. Create the four `mail/*` directories first. Write `sanitize.sh` to loop over `*.eml` in `incoming/`, use `sed '/^X-Internal-Route:/d'` (anchored so it only matches lines starting exactly with that string) to strip the header line, and redirect output to the same filename under `sanitized/`; `chmod +x` it. Write `backup_mail.py` using the `zipfile` module to archive everything under `incoming/` into `backup/backup_current.zip`, preserving filenames (not full paths) inside the archive. Write `processor_v2.py` as a loop that polls `sanitized/` every second, and for each `.eml` file found, appends a line `Status: Processed` and moves it into `processed/` — test this script manually first by running it briefly against a file you drop into `sanitized/` to confirm it behaves before wiring it into supervisord. Write a minimal `supervisord.conf` with `[supervisord]`, `[rpcinterface:supervisor]`, `[unix_http_server]`/`[supervisorctl]`, and a `[program:mail_processor]` section running `python3 /home/user/current_processor.py` with stdout logged. In `deploy.sh`, run backup, then sanitize, then `ln -sf` the symlink to `processor_v2.py`, then launch supervisord with your config, `sleep 5`, then write the final success log — run `deploy.sh` yourself and check each downstream file (processed .eml contents, symlink target, pid file) before finishing.