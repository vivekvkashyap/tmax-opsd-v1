Build the pipeline incrementally and verify each stage before moving on. First extract the tarball with `std::system("tar -xzf ...")` and list what landed in `/home/user/processing/` so you know what you're dealing with — do not hardcode filenames, glob the directory (e.g. with `<filesystem>` or `readdir`) and branch on `.json` vs `.csv` extension. Fetch `nlohmann/json.hpp` (e.g. via curl/wget to a known raw URL, or apt if available) and include it as a single header; use `j["metadata"]["env"]` and `j["metadata"]["version"]` — don't hand-roll JSON parsing with regex, it breaks on nested/whitespace variations. For CSV, split the header and second line on commas and locate the `env`/`version` columns by name rather than assuming fixed positions is safer, though the spec guarantees column order. Rename files using `std::filesystem::rename` in place. For manifest generation, shell out to `sha256sum <renamed files> > manifest.sha256` from inside `/home/user/processing/` so paths in the manifest are bare filenames (no directory prefix), which matters for `sha256sum -c` to pass later. Finally `tar -czf /home/user/normalized_configs.tar.gz -C /home/user/processing/ .` so archive members are rooted, not absolute paths. Compile with `g++ -std=c++17` and actually run the binary — the file must exist on disk when you're done, not just be theoretically producible.
