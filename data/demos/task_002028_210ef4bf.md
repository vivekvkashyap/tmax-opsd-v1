Chain the recovery steps in order since each unlocks the next. First transcribe `/app/evidence/voicemail.wav` (use a speech-to-text tool available in your environment, or `ffmpeg`+whisper if present) to recover the spoken passphrase word — the transcript tells you it's followed by a 4-digit PIN, which you do NOT know yet. Second, write a brute-force script (bash loop with `openssl enc -d -aes-256-cbc -pbkdf2 -in payload.enc -pass pass:"${word}${pin}"` trying pin 0000-9999, checking exit status or that output parses as valid JSON) to recover the full passphrase and decrypt payload.enc into readable JSON. Third, parse that JSON and replace every `ssn` value with the literal string `REDACTED_SSN` and every `private_ssh_key` value with `REDACTED_KEY`, preserving all other fields/order. Fourth, scaffold a Rust project (`cargo new`) at `/home/user/forensics_api` using a lightweight framework (axum/actix-web or std `TcpListener`), implement `GET /recovered-data` that checks the `Authorization` header equals exactly `Bearer forensics-auth-token` (401 if missing/wrong, else 200 + redacted JSON body), bind to `127.0.0.1:8181`, `cargo build --release`, and launch it detached/backgrounded (e.g. `nohup ... &`) so it stays running for the verifier.
