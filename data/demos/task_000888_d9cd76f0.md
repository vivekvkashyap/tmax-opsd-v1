Start by OCR'ing the spec image (`tesseract /app/profile_specs.png stdout`) to recover the exact TIMEOUT_THRESHOLD and MONTE_CARLO_RUNS values — don't guess round numbers, read them out and double check the extracted text looks well-formed before hardcoding them into your classifier. Parse the graph format carefully: N, then N cost lines, then edge triples (src dst prob); build an adjacency list per node with cumulative probabilities so you can sample an outgoing edge with one random draw compared against a running sum. For the Monte Carlo simulator, run a random walk from node 0 until it hits node N-1 (or a scenario is genuinely absorbing you need a reasonable per-run safety cap so a truly divergent walk doesn't loop forever in your own program), summing the cost of every visited node including repeats, and average the total cost over all runs. Compare that average strictly against the OCR'd threshold to decide EVIL vs CLEAN, and print exactly that word with nothing else on stdout (route any debug prints to stderr). Validate your classifier against the provided `/app/corpora/clean/` and `/app/corpora/evil/` directories before trusting it on hidden data — loop over every file in both and confirm 100% correct classification, since the hidden test set is generated the same way but with different seeds.