Do the two phases in order and verify each before moving on. First, OCR the routing graph out of `/app/sec_graph.png` (e.g. `tesseract`), and read the extracted text carefully to recover the exact directed edges (service -> service pairs) — if OCR text is garbled, try image preprocessing (scale up, grayscale/threshold) and re-run rather than guessing at plausible-looking service names. Encode the recovered edges as a directed adjacency structure (e.g. `set<pair<string,string>>` or `map<string, set<string>>`) in the C++ program. For the CLI argument, implement a real percent-decoder (handle `%XX` hex pairs case-insensitively, leave other characters as-is) before splitting on commas — decode first, then split, not the reverse, since a comma itself could arrive encoded. Walk the decoded node sequence and check each consecutive pair against the adjacency graph; on the first invalid hop (including nodes that aren't valid service names at all) print `DENY:<node>` for that node and stop, otherwise print `ALLOW` after checking every hop. Since correctness is checked by exact-output fuzz comparison against a hidden oracle binary, write your own quick random-input differential tests locally to catch decoding/off-by-one edge cases before relying on the grader. Also add the Makefile with the `g++ -O3 -std=c++17` target and the dummy `proxy:` echo target.