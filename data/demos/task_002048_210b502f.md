Do the recon before writing the service. Extract the two pieces of text embedded in `/app/arch_diagram.png` — run OCR (e.g. `tesseract`, or a Python OCR library) rather than assuming the values, and read the raw output carefully since it's the exact port number and exact debug token you must report. Then read `/home/user/legacy_service.py` and identify precisely which sink handles the debug-token-authenticated code path — a legacy service using `eval()` (or similarly `exec()`) on attacker-controlled input classifies as CWE-94 (code injection); note the exact function/parameter name so your exploit payload targets it correctly, and craft a payload string that would actually execute when passed through that sink (e.g. an `__import__`-based one-liner), don't just invent syntax that looks plausible. Finally write a minimal Python HTTP server (stdlib `http.server` is fine) bound to `127.0.0.1:9090` serving `GET /audit_report` with a JSON body containing the OCR'd port (as an integer), the OCR'd token (exact string), the CWE id, and your exploit payload string; launch it in the background so it stays reachable when the verifier connects, and leave the legacy service file itself unmodified.
