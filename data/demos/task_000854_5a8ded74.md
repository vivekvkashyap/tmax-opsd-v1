Do not trust the provided `transform.h` — the task warns it may not match the compiled ABI. Run `nm -D --defined-only lib/libtransform.so` (or `objdump -T`) to find the real exported symbol; the header's function likely doesn't exist under that name. Determine the real calling convention with `objdump -d` or by writing a small FFI test harness that tries candidate signatures on known input and watches for crashes/garbage vs sane output — a length argument plus a separate output buffer is a common mismatch pattern worth trying if a plain two-pointer call fails. With a working FFI call, build a backend with two listeners: HTTP on 127.0.0.1:9000 for `POST /process`, and raw TCP on 127.0.0.1:9001 that replies `"PONG\n"` to an exact `"PING\n"` line. Edit the nginx `http {}` block to add a `server` on `0.0.0.0:8080` with a `location /api/process` that strips `/api` and proxies to `127.0.0.1:9000/process`. Start backend and nginx in the background and confirm both ports are listening.