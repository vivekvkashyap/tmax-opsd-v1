This task has three independent legs — treat each as a checkpoint you validate before moving on. (1) Video/subtitle extraction: use `ffmpeg -i /app/build_dashboard_capture.mp4 -map 0:s:0 out.srt` (or similar) to pull the embedded subtitle track, then parse the SRT with a small script that strips sequence numbers and `-->` timestamp lines, keeping only the payload text of each cue; concatenate/parse each cue's text as JSON (skip cues that aren't valid JSON), and write the valid ones sequentially to `/home/user/video_configs/config_N.json`. (2) migrator.py: implement strict SemVer validation with a regex like `^\d+\.\d+\.\d+$` (reject `1.2`, `v1.0.0`, `1.a.0`), branch on major version to apply the V1→V2 migration (group `build_steps` by `lang` preserving order, requiring lang to be one of the three allowed values) versus pass-through V2 validation (requiring `orchestration` keyed by allowed languages with a `commands` list), and reject anything that fails any structural check — write rejected filenames (not full paths) to the reject-log and never emit an output file for them. (3) Test your migrator against `/app/corpora/clean/` and `/app/corpora/evil/` yourself using the documented CLI signature before running it on the extracted video configs, so you catch schema-edge-case bugs (missing keys, wrong types, bad langs) before the final run.