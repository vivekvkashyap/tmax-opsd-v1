Start by reverse-engineering the format from the video before writing any code: use `ffmpeg -i /app/config_session.mp4 -vf fps=2 frame_%03d.png` to dump frames, then read each frame carefully (a hex dump onscreen) to determine the magic number, how filename length/content/encoding are laid out, and how file content length is stored — don't guess field sizes or endianness, verify them from the frames. Note filenames are likely a non-ASCII encoding (check the video for the exact width, e.g. 1 vs 2 bytes per char) and file content is length-prefixed. Write `/home/user/safe_unpacker.c` to parse this binary layout precisely with fread-based binary I/O (no text-mode reads), convert the filename encoding to UTF-8 yourself byte-by-byte or via iconv. The critical correctness piece is path sanitization: for every extracted filename, resolve it against the target directory (e.g. via realpath or manual normalization of `..` segments and stripping leading `/`) and compare the resolved path's prefix against the target directory before writing; if it escapes, print the exact required warning and skip that file only, continuing to process the rest of the archive. Compile with gcc, test manually with crafted archives containing both benign and traversal filenames before relying on the fuzz harness.
