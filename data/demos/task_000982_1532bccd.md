Build `/home/user/pipeline.sh` in clear stages and make each stage deterministic. For tokenization, lowercase the text column, use `tr` or `sed` to replace every non a-z character with a space, split on whitespace, then filter tokens with length >= 3 that are not present in `stopwords.txt` (a fast approach: pipe through `awk`/`grep -vxFf stopwords.txt`), tally frequency with `sort | uniq -c`, and sort by count descending then word ascending to break ties (`sort -k1,1nr -k2,2`) before taking the top 3. For bootstrap sampling, you must literally invoke `shuf -r -n <num_rows> --random-source=/home/user/random_seed.dat` on the data rows (header excluded) exactly three separate times in sequence — do not shuffle once and slice, and do not substitute a different RNG, since reproducibility depends on consuming the random-source stream in that exact call order. For each sample and each of the 3 words, filter rows whose tokenized text contains that exact word (as a whole token, not substring) and average the `rating` column with `awk`, rounding to 2 decimals. Finally sort output rows by Sample_ID then Word and write the header + rows to `/home/user/report.csv`, then `chmod +x` and run the script.