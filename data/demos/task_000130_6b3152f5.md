Replace the described nested-loop Python logic with one SQL query built entirely from CTEs and a window function — resist the urge to do any hop-counting or ranking in Python. Structure the query as: (1) a CTE `hop1` selecting distinct target_id from transfers joined to entities where entities.region = :region_name; (2) a CTE `hop2` selecting distinct target_id from transfers whose source_id is in hop1's targets; (3) a `reached` CTE that is the union of hop1 and hop2 targets (both count as "1 or 2 hops away"); (4) a `totals` CTE computing `SUM(amount) AS total_received` from transfers grouped by target_id across the *entire* transfers table (not restricted to the traversal), then join that to entities to get type/region; (5) apply `RANK() OVER (PARTITION BY region ORDER BY total_received DESC, id ASC) AS regional_rank` over the joined reached+totals+entities rows; (6) filter to only the reached entities, order by regional_rank ASC then id ASC, and apply `LIMIT ? OFFSET ?` with the CLI's limit/offset args bound as query parameters (never string-interpolated). In analyze.py, just open the connection, execute this one parameterized statement with `(region_name, limit, offset)`, fetch rows, and json.dump them to output.json with exactly the five required keys per row.
