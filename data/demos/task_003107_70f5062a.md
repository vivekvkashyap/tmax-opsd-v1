Start by extracting the text from the image with OCR: `tesseract /app/capacity_rules.png stdout` (try `--psm 6` if default segmentation garbles lines), then read the recovered CPU threshold, MEM threshold, and the "Ignore Path" regex carefully — do not guess at these values, they must come from what OCR actually returns (re-run with different page-segmentation modes or crop/upscale the image if the first pass is noisy). Once you have the real numbers and regex, write `/home/user/classify_hog.py` with a shebang (`#!/usr/bin/env python3`), make it executable (`chmod +x`), and have it read one CSV line from stdin, split into `PID,USER,CPU_PERCENT,MEM_PERCENT,COMMAND` (use `float()` for the percentage fields, and be careful the COMMAND field itself may contain no further commas), then apply the exact boolean logic: reject if `(cpu > max_cpu or mem > max_mem) and not re.match(ignore_regex, command)`, else accept — printing exactly `REJECT` or `ACCEPT` with no other stdout output. Test manually with a few crafted lines that straddle each threshold and both sides of the ignore-path regex before considering it done, since the verifier uses adversarial evil/clean corpora that specifically probe boundary conditions.