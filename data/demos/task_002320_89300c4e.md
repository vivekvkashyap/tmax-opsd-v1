Work in clear, ordered stages, writing each result to disk so you can sanity-check it before moving on. First melt the wide CSV into long format by iterating the station prefixes (S1..S4) and stacking their `_T`/`_H` columns under `Date, Station, Temperature, Humidity`. Second, add `THI = Temperature + 0.5*Humidity` (must be NaN when either input is missing — don't let pandas silently coerce), then sort by Station and Date before computing `Temp_Rolling_3` as a per-station rolling mean with `min_periods=1`, since rolling must run on time-ordered data per group, not the whole frame. Third, apply the quality gate per station using the long-format frame: flag Rule A via any Temperature outside [-40, 60], and Rule B via the fraction of NaN THI rows exceeding 5% — compute the missing fraction on the per-station slice, not globally. Write the sorted list of surviving stations to `valid_stations.txt`. Finally, restrict to valid stations, extract the month from Date, group by (Station, Month), and sample exactly 2 rows per group using pandas' `.sample(n=2, random_state=42)` so results are reproducible — using a different sampling method or seed will silently produce mismatched output. Sort the final CSV by Station then Date before writing.