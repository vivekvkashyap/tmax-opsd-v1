Follow the spec's exact frame/feature parameters precisely, since the grader compares statistical distributions — small deviations from `center=False`, frame_length/hop_length=320, or n_mels=40 will shift results. Load `/app/input_audio.wav` at 16kHz mono with librosa, compute RMS and ZCR with `frame_length=320, hop_length=320, center=False`, and 13 MFCCs with `sr=16000, n_mfcc=13, n_fft=320, hop_length=320, n_mels=40, center=False`, then stack into a (15, n_frames) array in the specified order [RMS, ZCR, MFCC1..13]. Compute per-feature variance across all frames with `np.var(axis=1)` and take the 4 highest-variance feature indices — don't re-sort them arbitrarily, keep track of which original feature each selected column is. Compute the median RMS across ALL frames (before filtering) and keep only frames with RMS strictly greater than that median. Set `np.random.seed(42)` immediately before calling `np.random.choice(n_valid_frames, size=10000, replace=True)` — seeding order and the exact call matter for reproducibility. Write the resulting 10000x4 array to CSV with `np.savetxt(..., delimiter=',')`, no header, no index.
