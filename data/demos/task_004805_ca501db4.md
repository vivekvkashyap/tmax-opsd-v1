Use `concurrent.futures.ProcessPoolExecutor` (or `multiprocessing.Pool`) to `map` a per-file parsing function across all CSVs in `/home/user/locales/updates/`, having each worker return a parsed list of `(locale, string_id, translation)` tuples reshaped from the wide `string_id,en,es,fr,de` rows — remember `en` is itself a locale to include like the others unless the task's downstream steps only reference non-en locales for INI output (it is used for the JSON but excluded from INI generation). Load `base.json` first, then merge: iterate the collected updates and overwrite `merged[locale][string_id] = value` for every locale/string_id pair found in the CSVs, so CSV data wins over base on conflicts. Write `final.json` with `json.dump(..., indent=2, sort_keys=True)` to get alphabetically sorted keys at every level. For each non-`en` locale present, write `final_<locale>.ini` starting with a `[translations]` header line followed by `key=value` lines sorted alphabetically by string_id — build these directly from the same merged dict rather than re-reading files, to guarantee consistency between the JSON and INI outputs. Create `/home/user/locales/output/` before writing, and manually diff a couple of INI/JSON entries against `base.json` + CSV inputs to confirm overwrite behavior is correct.
