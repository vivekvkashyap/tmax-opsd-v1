Tackle this in dependency order: fix the vendored package first, since everything downstream needs it importable. Inspect /app/fast-tracker-1.0/Makefile for the CFLAGS line — a `-Werror` combined with an unused-variable warning in src/math_utils.c will hard-fail the build; either remove `-Werror` from CFLAGS or eliminate the unused variable in the C source, then `make && pip install .` and confirm `import fast_tracker` works before moving on. For the data step, load dataset.csv with pandas, compute `df[feature_cols].corr()`, and programmatically scan the upper triangle for |corr| > 0.85 pairs, dropping whichever column name sorts later alphabetically for each flagged pair (iterate deterministically, e.g. sorted column order, and drop as you go so cascading correlations are handled once) — don't hardcode which columns to drop, derive it from the actual correlation matrix. Save the filtered CSV, train Ridge(random_state=42) on it, log MSE via fast_tracker.log_metric, and pickle the model with joblib. Finally stand up a Flask/FastAPI app bound to exactly 127.0.0.1:8080 with GET /metrics returning the logged MSE and POST /predict accepting a "features" array matching the filtered column count/order and returning the model's prediction; run it in the background and curl both endpoints yourself to confirm before finishing.
