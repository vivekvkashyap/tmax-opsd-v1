Do the phases in order and validate each before moving on. Extract the tar.gz, then `find` all `.zip` files under the extracted tree and unzip each into `/home/user/csv_data/` (use `-o`/overwrite flags so re-runs don't hang on prompts). For the C program, open the target file with `fopen`, parse each `id,project_name,status` line with something robust like `strtok`/`sscanf` (watch for trailing newlines), and format the JSON line yourself with `snprintf`. The critical part is locking: open `/home/user/master_output.json` with `O_APPEND|O_CREAT|O_WRONLY`, then call `flock(fd, LOCK_EX)` (or `fcntl` with `F_SETLKW`) immediately before writing and unlock right after — do this around each write, not once for the whole program, and make sure the lock is released even on error paths. Compile with `-Wall -Wextra` and fix every warning. Test the pipeline single-threaded first to confirm correct JSON output, then run it under `xargs -P 4` to confirm no interleaved/corrupted lines appear. For backups, use `tar --create --file=... --listed-incremental=/home/user/backup.snar`: the first invocation against a fresh `.snar` file produces the level-0 archive, and a second invocation with the same `.snar` after appending the dummy record produces the level-1 incremental.