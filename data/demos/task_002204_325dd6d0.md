Write `/home/user/process.go` as a straightforward linear-algebra-plus-nearest-neighbor program, keeping numeric precision consistent with a plain float64 matrix multiply so results match a reference computed the same way. Load `embeddings.csv` (100x10) and `projection.csv` (10x3) with a CSV reader, parsing to `float64`, and compute the 100x3 product `Reduced = Embeddings * Projection` via straightforward triple-nested-loop or a small matrix-multiply helper — no need for a linear algebra library. Then for each row `i` in Reduced, compute the Euclidean distance to every other row `j != i`, sort the (distance, index) pairs ascending, and take the first two indices as the top-2 nearest neighbors, being careful to break ties consistently (stable sort by distance) and to always exclude `i` itself. Marshal the result into a `map[string][]int` keyed by string row index, JSON-encode it, and write to `/home/user/recommendations.json`. Compile with `go build` (or `go run`) and actually execute the program so the output file is produced, then spot-check a couple of entries by eye for sanity (e.g. distance-zero or near-duplicate rows) before finishing.
