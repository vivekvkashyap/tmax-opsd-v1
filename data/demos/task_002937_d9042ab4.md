Solve each of the three checks as an independent `jq`/awk pipeline and validate output on the known sample files before trusting it. For schema enforcement, iterate every `.json` file except `exp_ref.json`, and for each use `jq -e 'has("experiment_id") and has("learning_rate") and has("batch_size") and has("schema_hash") and has("model_weights_hash") and has("accuracy")'` (or check each key individually) to flag files missing any required key; collect failing basenames, sort them, write to invalid.log. For similarity search, first filter to valid files only (reuse the schema check), extract `learning_rate`/`batch_size` from each with `jq`, and from exp_ref.json, then compute Euclidean distance for each with `awk` or `bc`/shell arithmetic, tracking the minimum; write only the winning filename. For reproducibility violations, again restrict to valid non-ref files, extract `(learning_rate, batch_size, schema_hash, model_weights_hash, filename)` tuples, group by the first three fields (e.g. sort by them), and within each group where more than one distinct `model_weights_hash` appears, emit every distinct filename pair sorted alphabetically within the pair and across output lines. Create `/home/user/report/` first, and manually trace through the sample experiment files by hand to confirm your pipeline's output matches your own manual calculation before finalizing.
