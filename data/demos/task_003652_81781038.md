First add the index: `CREATE INDEX idx_employees_manager ON employees(manager_id);` — this is what makes the recursive graph traversal fast. In the C program, use `sqlite3_prepare_v2` with a single recursive CTE containing `?` placeholders bound via `sqlite3_bind_int`: seed the CTE at `depth=0` for the given manager_id, recursively join `employees e ON e.manager_id = d.id` incrementing depth, then in the outer SELECT filter to `depth > 0` (the manager itself is depth 0 and should not appear in the paginated descendant list per the sample), `ORDER BY depth ASC, name ASC`, and apply `LIMIT ? OFFSET ?` bound from argv. Step through results with `sqlite3_step` in a loop, building a JSON array manually (or with a tiny in-code JSON writer) with fields `id`, `name`, `depth`, escaping strings as needed, and write it to `/home/user/output.json`. Compile with `gcc hierarchy.c -lsqlite3 -o hierarchy` and confirm `pkg-config sqlite3` / `libsqlite3-dev` is installed before troubleshooting linker errors.
