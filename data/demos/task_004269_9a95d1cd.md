Treat this as two separable deliverables with a shared JSON schema. For the video: extract frames with ffmpeg, then OCR each frame with tesseract (since it's plain black text on white background, tesseract should work directly on the raw frame without heavy preprocessing — but verify a few frames manually to catch OCR misreads of transaction/resource IDs before trusting the pipeline). Each frame's text is one log line of the form "<TX> HOLDS <RES>" or "<TX> WAITS_FOR <RES>" — map each line to an edge object with matching "type", then dedupe/collect all edges chronologically into `/home/user/video_graph.json` under the required `edges` array schema. For the detector: build a directed graph where WAITS_FOR edges point transaction->resource and HOLDS edges point transaction->resource, then to find deadlocks you need to compose the wait-for relation between transactions (a transaction A effectively waits on transaction B if A WAITS_FOR some resource R and B HOLDS R) and run cycle detection (DFS with recursion-stack tracking, or Tarjan/Kahn's) over that transaction-to-transaction graph — don't just check for cycles in the raw HOLDS/WAITS_FOR edge list, since HOLDS and WAITS_FOR edges alone form a bipartite structure and cycles only emerge once you compose through shared resources. Print exactly `DEADLOCK` or `SAFE`, and validate against both corpora before finishing.