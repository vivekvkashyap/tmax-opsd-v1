Fix the vendored package first: open `/app/fastpca-1.2/setup.py`, and where `include_dirs=[]` is passed to the Extension, populate it with `pybind11.get_include()` and `numpy.get_include()` (both `pybind11` and `numpy` must be importable in setup.py itself). Then run `pip install .` from that directory and confirm `import fastpca` works before touching the ML code. For the pipeline: load train.csv, split off `target`, call `fastpca.reduce_dimensions(X.values, n_components=20)`. For each of the 20 resulting columns, do proper bootstrap: resample the column (with replacement, same length) 1000 times, take the mean of each resample, then use `np.percentile` on the 1000 means at 2.5 and 97.5 to get the CI; keep only components whose interval excludes 0. Fit a classifier with `GridSearchCV`/`RandomizedSearchCV` (cv=5) on just the retained components, pickle the full pipeline (component indices + scaler + model) to `/home/user/model_pipeline.pkl`. Write `predict.py` so it re-applies the exact same reduce → select-indices → predict_proba chain, not a re-derived selection.
