Fix the plumbing first, worker last. In `/home/user/pipeline/nginx/nginx.conf` add/repair a `location /api/ingest { proxy_pass http://flask:5000; }` (or an upstream block) so requests actually reach Flask; in `/home/user/pipeline/flask/app.py` point the Redis client at `host='redis', port=6379` (the compose service name, not localhost) and confirm it `rpush`es onto `raw_csv_queue`. Bring the stack up with `docker-compose up -d` and manually curl `/api/ingest` to confirm a message lands in Redis (`redis-cli llen raw_csv_queue`) before writing any C++. For the worker: link `hiredis`, loop on `BLPOP raw_csv_queue 0`, parse `batch_id,feature_id,val1..val10`, load `/home/user/data/reference_stats.csv` once at startup into a feature_id -> (mean, stddev) map, compute the sample mean of the 10 readings, then the Z-score `(sample_mean - ref_mean) / (ref_stddev / sqrt(10))` for a one-sample Z-test, and classify evil when `|Z| > 2.576`. Time each inference with a steady_clock, append to the correct CSV (`clean_output.csv`/`evil_output.csv`), and log "WORKER READY" to `run.log` once BLPOP polling starts. Test end-to-end by posting known clean/evil payloads yourself before relying on the grader's stream.
