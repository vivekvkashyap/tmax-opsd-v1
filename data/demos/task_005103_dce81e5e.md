Scaffold a Rust binary with `cargo new ols_pipeline` in `/home/user/`, add the `nalgebra` crate (and a CSV-parsing approach — either `csv` crate or manual `split(',')` since the format is simple) to `Cargo.toml`. Parse `/home/user/data/input.csv` row by row into `f64` columns for x1, x2, y, treating any cell that fails to parse as `f64` (empty string, "NA", "NaN" literal text — distinct from an actual `f64::NAN` value, be careful not to conflate them) as missing. For each feature column independently, compute the mean over only the successfully-parsed values, then substitute that column's mean into every missing cell for that column — do this per-column, not globally. Build the design matrix with a leading column of `1.0`s (intercept), then `x1`, then `x2`, and the target vector `y`, both as `nalgebra::DMatrix`/`DVector`. Solve the normal equations `w = (XᵀX)⁻¹Xᵀy` using nalgebra's transpose, matrix multiply, and `.try_inverse()` (check it isn't singular). Format each of the three resulting coefficients to exactly 4 decimal places (e.g. with `format!("{:.4}", w[i])`), one per line, and write to `/home/user/output/coefficients.txt`, creating the `output` directory first if needed.
