Do this as a straightforward shell pipeline, being careful about numeric formatting and off-by-one indexing. Loop `for i in $(seq 1 100)`, invoke each solver with `$i`, and append each solver's stdout number to its own file (e.g. `alpha.txt`, `beta.txt`) — don't try to hold 100 values in a shell variable, just accumulate to files. Compute the mean with `awk '{s+=$1} END{printf "%.2f", s/NR}'`. For P95, `sort -n` the file and take the value at line 95 with `sed -n '95p'` (i.e., the 95th value in ascending order — confirm against the task's exact definition of which index counts as "95th" before trusting it), formatting to 2 decimals with `printf`/`awk`. Compare the two means numerically (`awk 'BEGIN{if (a<b) ...}'` or `bc`) to decide which solver is faster, then assemble perf_report.txt with `printf` using the exact label format given, double-checking there's no trailing whitespace or extra blank lines that would break an exact-match check.
