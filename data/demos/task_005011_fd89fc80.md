This is a two-hop BFS over a co-authorship graph derived from a join table, followed by strict JSON/SPARQL formatting. Query `author_paper` to build, for the given `author_id`, the set of co-authors: authors sharing any `paper_id` with the target (distance 1) — exclude the target itself. Then, from that distance-1 set, find their co-authors (distance 2), excluding both the target and anyone already at distance 1 (dedupe by taking the *first* distance found — closer wins). Do this either with SQL self-joins on `author_paper` or by loading the edges into Python and doing a two-level BFS/set expansion; the latter is easier to get right for the "already seen at a closer distance" exclusion. Build the network list with `author_id`, `name`, `distance`, `wikidata_q` pulled from `authors`, sort by `(distance, author_id)`, and dump to `/home/user/network_<id>.json` matching the required schema exactly. For the `.rq` file, collect the `wikidata_q` values of everyone in the network (not the target) and emit a SPARQL query with a `VALUES ?author { wd:Q... wd:Q... }` clause plus `?dob` (`wdt:P569`) and `?employer` (`wdt:P108`) triple patterns — generate the VALUES list programmatically from your query results rather than typing it by hand.
