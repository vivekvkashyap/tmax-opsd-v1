Sequence this as: audit script -> keypair/service setup -> tunnel -> verify. First back up the config with `cp /home/user/bastion/sshd_config /home/user/backup/sshd_config.bak`. Write `key_auditor.py` by parsing the standard 2- or 3-field pubkey line (type, base64 blob, optional comment/options) — do not trust `ssh-keygen -l -f` alone for bit-length since it won't catch the `command=` or `admin`-comment rules; instead check the raw line text for `command=` anywhere, check whether the last whitespace-separated field (if it doesn't look like base64 key material) contains `admin`, and for `ssh-rsa` keys decode the base64 blob to extract the modulus length (or use `ssh-keygen -l -f <file>` for bit count, cross-checking it isn't fooled by options) — accept `ssh-ed25519`/`ecdsa-sha2-nistp256` unconditionally otherwise, reject RSA under 2048 bits. Test it against a few keys you generate locally before trusting it. Then generate the host key and your own ed25519 keypair, add the pubkey to `authorized_keys` only after your own auditor accepts it, edit `sshd_config` for port 2222 plus `StrictModes no`/`UsePAM no` with the correct `HostKey` path, launch `sshd -f ... &` and the Flask app with `python3 /app/api.py &`, then open the local forward with `ssh -f -N -L 8080:127.0.0.1:5000 -p 2222 -i ~/.ssh/id_ed25519 -o StrictHostKeyChecking=no user@127.0.0.1`, and finally `curl` through the tunnel to populate `api_status.log`.