Start by diffing the clean vs. evil corpora byte-for-byte (`diff`, a hex viewer, or Python `repr()` on raw bytes) rather than guessing from the crash log alone — the stack trace/hex dump hint at a UTF-8/UTF-16 encoding issue, but confirm the discriminating pattern by scanning all 100 files programmatically (e.g. search for unpaired surrogate escape sequences, or attempt strict UTF-8 decode of the JSON string content and see which files fail). Implement `is_safe` to detect that general malformed-encoding pattern (e.g. scan decoded JSON string values for surrogate codepoints in the `\uD800`-`\uDFFF` range that aren't validly paired) rather than matching only the sample messages verbatim — the grader tests against the full corpora, not hardcoded literals. For the proxy, forward the raw request body untouched when safe, return 400 with no forwarding when unsafe, and update `router_config.json` to point at your proxy's port so the full chain routes through it.
