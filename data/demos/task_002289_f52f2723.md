The core risk here is silent nondeterminism in the RNG and timer placement — implement the LCG exactly as specified, not with `rand()` or `<random>`. Declare `uint64_t state = seed;` once before the outer iteration loop (not reset per iteration), and for every sampled row do `state = (1103515245ULL * state + 12345ULL) % 2147483648ULL; int row_index = state % N;` in that exact order, called once per drawn sample so the total number of LCG advances equals `num_iterations * sample_size`. Read the CSV with a simple line parser skipping the header, storing `x` and `true_y` as doubles. Start the `high_resolution_clock` timer immediately before computing predictions for the drawn sample and stop it immediately after — do not include the RNG draws or MSE computation inside the timed region, since the spec is explicit that only inference is benchmarked. Compute the model `f(x) = 2.5 + 1.2x - 0.3x^2` per drawn row, then per-sample MSE against `true_y`, average across iterations for both MSE and time (converting the timer duration to microseconds), and write the output with `Avg_MSE` formatted to exactly 4 decimals via something like `printf("Avg_MSE: %.4f\n", avg_mse)`. Compile with the exact `g++ -O3 -std=c++17` command given and run with the specified arguments to produce `/home/user/results.txt`.
