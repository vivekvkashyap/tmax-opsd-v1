Fix the Makefile first: add `-O3` to `CFLAGS` and make sure `-lm` is added to the link line *after* the object files (linker flag ordering matters for resolving math symbols). In `graph_solve.c`, look specifically for the loop that walks each node's edge list — an off-by-one bound like `for (int i = 0; i <= node->edge_count; i++)` reads one element past the array's end; change the comparison to `<`. Write `cli_wrapper.c` to parse a semver requirement string like `>=1.4.0` by splitting off the leading operator, then comparing (major,minor,patch) as integers (not strings) component-by-component against the hardcoded `1.4.2`, applying whichever operator was given; on success, call into the existing solver logic on the input file, else exit non-zero. Add a `mathgraph_cli` target to the Makefile that links `cli_wrapper.o` and the fixed `graph_solve.o` with `-O3 -static -lm`. After building, actually time it against the large graph file — if it's still too slow after fixing the bug and adding `-O3`, look for other obvious inefficiencies (e.g. quadratic traversal, excessive I/O) before writing `READY` to the status file.