First recover the sensitive table name from the audio: convert `/app/voicemail.wav` to a format Whisper/speech-to-text tooling accepts if needed (ffmpeg can transcode), then transcribe it and read the exact table identifier out of the transcript — do not guess a name. Once you have the real table name, write `filter_backup.sh` as a case-insensitive scan for that identifier appearing anywhere it could be referenced: after `FROM`, after `JOIN`, inside CTEs (`WITH ... AS`), and wrapped in single/double quotes or backticks. A robust approach is a single case-insensitive regex/grep for the table name as a whole word/token (accounting for optional surrounding quote or backtick characters) rather than trying to special-case every SQL clause keyword, since the corpus tests many syntactic variations. Iterate the script over every file in both `/app/corpus/clean/` and `/app/corpus/evil/` yourself before finishing, fixing any false positive/negative, and make sure exit codes and stdout (`CLEAN`/`EVIL`) match exactly and the script is `chmod +x`.
