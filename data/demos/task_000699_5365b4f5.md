In pipeline.py, after `df['category_id'].fillna(-1)`, explicitly cast the column with `.astype('int64')` (or `.astype(int)`) before calling `to_csv` — pandas keeps a float dtype after fillna on a mapped column even with no remaining NaNs, since the dtype was already upcast to float64 the moment `.map()` produced any NaN, and only an explicit cast reverts it. Verify by inspecting `df.dtypes` and by checking the raw CSV text (not just the DataFrame) for stray `.0` suffixes, since `to_csv` will still print floats as `1.0` even if you eyeballed the DataFrame and it looked like integers. For `test_pipeline.py`, import `run_pipeline` from the pipeline module (add the containing directory to `sys.path` if needed), call it, reload `features.csv` with pandas, assert `df['category_id'].dtype == 'int64'`, compute and assert the exact mean value using the fixture's actual mapped/filled values, then write the literal marker string to the result file only if both assertions pass — let an assertion failure propagate rather than swallowing it, so a partial fix is visibly caught.