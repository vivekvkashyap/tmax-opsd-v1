Work through the three build/link/serve stages in order, verifying each before moving on. First, run the existing build.sh once to produce both .so files, then use `nm -D --defined-only libmath_alpha.so` and the same for beta, extract just the symbol names (e.g. awk on the last column), sort each list, and use `comm -12` (or diff on sorted lists) to find the names present in both — write that intersection to conflict.txt. Second, understand why the app currently gives the wrong answer: with both libraries exporting the same symbol, link order in build.sh's -l flags determines which implementation wins, which is why it's fragile in CI. Fix it robustly (not just by reordering flags) by writing a linker version script (beta.map) with `global:` listing only `beta_specific_math;` and `local:` (or `*;` as catch-all local) hiding `calculate_metric`, then compile libmath_beta.so with `-Wl,--version-script=beta.map` so its symbol is no longer externally visible — this way linking order can no longer cause ambiguity. Recompile main against both libraries and confirm ./app prints the alpha-only result. Third, write api.sh as a loop that repeatedly invokes `nc -l -p 8080` (or similar), reads the HTTP request line, checks for `GET /api/math/result`, executes ./app to capture its output, and echoes a well-formed HTTP/1.1 200 response with the JSON body — start it backgrounded and confirm with curl before finishing.
