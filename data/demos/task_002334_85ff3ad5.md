Structure the script around Python's `tarfile` module: for each `.tar.gz` in /home/user/incoming/, open it and iterate `tar.getmembers()` yourself rather than calling `extractall()` blindly — for each member, resolve what its extraction path would be (`os.path.normpath(os.path.join(target_dir, member.name))`) and check it still starts with the target directory; if it doesn't (i.e. the normalized path escapes via `../` or an absolute path), skip that member and log `REJECTED: <archive> - <member.name>` to security.log instead of extracting it, then extract only the safe members. After extraction, load deploy.json's `deploy` list, and for each listed relative path, copy the file from within the extracted directory to /home/user/configs/ using just its basename (flattening directory structure). For each successfully copied file, compute `hashlib.sha256(data).hexdigest()` and append a manifest line in the exact `<hash>  <filename>` format (two spaces). Finally delete the original tarball. Run the script once on the pre-populated incoming files and confirm incoming/ ends up empty.