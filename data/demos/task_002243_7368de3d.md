This is a known-plaintext brute-force attack on a DJB2-style hash. In your C brute-forcer, reimplement `custom_hash` exactly as given (seed 5381, `hash = hash*33 + c`, using `unsigned int` so it wraps mod 2^32 naturally), then iterate over all 26^4 combinations of 4 lowercase letters, concatenate `"guest"` with each candidate secret, hash it, and compare against the captured signature `594879208` — when you find the match, that is the recovered secret (do not hardcode a guess; the loop must actually find it). Once recovered, concatenate `"admin"` with that same secret string and run it through the identical hash function to compute the new signature. Format the final line exactly as `Cookie: user=admin;sig=<computed_signature>` with no extra whitespace or trailing newline issues, and write only that string to `/home/user/admin_cookie.txt`.
