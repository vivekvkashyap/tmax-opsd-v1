Write `evaluator.c` to include `kernel.h` and link `kernel.c` at compile time (e.g. `gcc -O3 -o evaluator evaluator.c kernel.c -lm`). For each N in {100,200,300}: malloc the four float arrays, fill `weights`/`inputs` with `0.01f` and zero the outputs, then compute `ref_outputs` yourself with a plain double/float nested-loop matmul-vector product matching the spec exactly (same summation order as the kernel, i.e. row-major `weights[i*N+j]*inputs[j]` summed over j) so floating point rounding differences stay tiny. Call `kernel_infer` once to fill `outputs` and compute MAE = max over i of `fabs(outputs[i]-ref_outputs[i])`; keep this call separate from timing so a single warm-up call doesn't skew the first measurement. For timing, wrap a `for` loop calling `kernel_infer` 1000 times back-to-back in `clock_gettime(CLOCK_MONOTONIC, &start/&end)`, compute elapsed nanoseconds, convert to total microseconds (do not divide by 1000 iterations — the report wants total time for the whole run based on the example format, so double check the spec's expected magnitude). Format each CSV line as `%d,%.6f,%.1f\n` with N, MAE, time in that order, no header, exactly 3 lines, and actually run the compiled binary so `report.csv` gets generated.
