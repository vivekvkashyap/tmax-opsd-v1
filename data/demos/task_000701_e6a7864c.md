Split this into the config fix and the adversarial C validator, verifying each in isolation. For nginx, add or correct a `location /auth { proxy_pass http://127.0.0.1:5000; }` block (matching the actual upstream host/port the flask service binds to) inside the server block, then reload nginx and confirm end-to-end with `curl -I http://localhost:8080/auth?next=/dashboard` returning a 302 rather than 502/404 before moving on. For the C validator, work from the provided skeleton's `base64_decode`, split the decoded buffer on the two `|` delimiters to get `user_id`, `redirect_uri`, `signature` (careful with `strtok`/manual scanning if any field could theoretically contain no data), and implement rejection as an ordered set of independent checks: signature must be exactly 64 chars of hex; redirect_uri must start with a single `/` and its second character must not also be `/`; must not begin with any known scheme prefix (`http://`, `https://`, `javascript:`, case-insensitively) after normalizing case for the prefix check only; must contain no bytes below 0x20. Print exactly `VALID`/`INVALID` and match exit codes 0/1. Test against both provided corpora directories before considering it done, including edge cases like escaped-slash bypasses.