Parse `/home/user/graph_dump.csv` with Python's `csv` module (stdlib only) into a list of edges, building both a node-type lookup (`src_id -> src_type`, `dst_id -> dst_type`) and a directed weighted adjacency map keyed by node id with `latency_ms` as edge weight. For the schema, collect the set of `(src_type, dst_type)` pairs seen on any edge, dedupe, and sort first by source type then destination type (plain string sort on the tuple works). For the shortest path, implement Dijkstra's algorithm (or since the graph is small, even BFS with priority queue via `heapq`) from `Gateway_Alpha` to `DB_Omega`, tracking predecessors to reconstruct the path, then sum edge weights along that path for the total latency — do not assume the path with fewest hops is the shortest by latency, they can differ. Format the output file exactly as specified: `SCHEMA:` header, one `SourceType -> DestType` line per relationship, a blank line, then `SHORTEST_PATH_LATENCY: <int>` and `SHORTEST_PATH: A -> B -> C` joined with the literal ` -> ` separator. Double-check whitespace and blank-line placement match the example precisely since comparison is exact-string.
