Sequence this as OCR -> sanitize -> serve, since the server's auth depends on the OCR'd token. Run `tesseract /app/legacy_watermark.png stdout` and locate the line labeled `AUTH_TOKEN:`, extracting exactly the token string that follows it (try preprocessing/`-psm` variants if the raw pass looks garbled) — use that recovered value as your Bearer token, don't invent one. Extract `/app/docs_archive.tar.gz` into a temp directory (e.g. under `/tmp` or `tempfile.mkdtemp()`), sanitize each file's text with two regex/string replacements — an SSN pattern like `SSN: \d{3}-\d{2}-\d{4}` replaced by `SSN: [REDACTED]` (redact only the digits, keep the `SSN: ` label) and the literal `STATUS: DRAFT` -> `STATUS: ARCHIVED` — then write to `/home/user/clean_docs/` atomically: write to a temp file in the same directory, then `os.rename` into place so no partial file is ever visible. For the HTTP service, check the `Authorization: Bearer <token>` header on every route before doing anything else, return 401 on mismatch, implement GET/POST as specified, and for the append-note endpoint use `fcntl.flock` (or O_APPEND with a lock) around the read-modify-write so concurrent writers can't corrupt the file. Launch the server as a background process so it stays reachable for testing.