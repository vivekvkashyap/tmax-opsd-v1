Build and test each layer in isolation before wiring the full pipeline. First write `/home/user/protocol_converter.py` as a strict stdin-to-stdout line filter: for each line, check `COMMIT:` and `AUTHOR:` prefixes first (str.startswith, stripping the prefix and any trailing newline only — preserve internal spaces in author names), then match the git name-status regex `^([AMD])\s+(.+)$` and map A/M/D to FILE_ADD/FILE_MOD/FILE_DEL, otherwise silently drop the line (including blank lines) — test it manually against the example input/output before moving on, since a fuzzer will feed it many edge cases (garbage lines, mixed content) and any extra/missing output line fails equivalence. Second, write the `expect` script to follow the exact login handshake (expect `LOGIN:` -> send `admin\n`, expect `PASSWORD:` -> send `legacy_secret\n`, expect `READY>`), then loop sending each command line followed by expecting `OK`, finishing with `LOGOUT\n`, logging everything via `log_file -a /home/user/logs/legacy_sync.log`. Set up `socat TCP-LISTEN:9999,fork,reuseaddr TCP:localhost:8888` backgrounded. Write logrotate.conf with `daily`, `rotate 5`, `compress`, `notifempty`. Finally wire the git bare repo's executable `post-receive` hook to chain `git log -1 --name-status --pretty=format:"COMMIT:%H%nAUTHOR:%an"` through the converter and into the expect script, and test end-to-end with an actual `git push` before finishing.