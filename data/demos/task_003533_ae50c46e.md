Work through this as a layered debugging chain rather than jumping straight to the final curl. First inspect `/home/user/nginx/nginx.conf` and find the `proxy_pass` line inside the `location /api/` block — compare its port against the backend's actual listening port (8081) and correct any mismatch. Next open `/home/user/scripts/run_job.sh`: it currently calls the generator script with no environment set, so add `export REPORT_DIR=/home/user/app_data`, `export TZ=UTC`, and `export LC_ALL=C` before invoking `generate_report.py`, since the generator reads `REPORT_DIR` from the environment and formats timestamps using the process locale/timezone. Run the job script and confirm `/home/user/app_data/report.json` appears with a timestamp string ending in UTC. Then start the backend (`python3 .../server.py &`) and nginx (`nginx -c ... -p ... &`), checking each process actually bound its port with `curl -v` or `ss -ltnp` before moving on — nginx needs its `logs`/`temp` directories to already exist to run unprivileged. Finally curl the proxy endpoint and redirect the raw output to `/home/user/success_response.txt`, verifying it's valid JSON with a success status.
