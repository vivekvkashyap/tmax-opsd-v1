Build this as a real TLS client with proper certificate pinning to a custom CA, not a quick script. In Go, load `/home/user/ca.crt` into an `x509.CertPool` and set it as `RootCAs` in a `tls.Config` used by your `http.Client`'s `Transport` — do not set `InsecureSkipVerify`. For the JWT bypass, construct the token manually as three base64url (no padding) segments joined by `.`: a header `{"alg":"none","typ":"JWT"}`, the payload `{"role":"admin"}`, and an empty third segment (signature omitted for `alg: none`), then send it as `Authorization: Bearer <token>` to `GET /api/ssh-config`. Inspect the raw `Set-Cookie` response header string directly (not a parsed cookie jar, which may silently normalize flags) to check for the literal substrings `Secure` and `HttpOnly`. Parse the returned `sshd_config` text field line-by-line, checking for `PermitRootLogin yes` and `PasswordAuthentication yes`. Since the server implements the "none" alg vulnerability and lacks cookie flags by design, each of your five checks should evaluate true if your requests and parsing are correct — write all five booleans to `/home/user/audit_report.json`, then actually compile and run the Go program to regenerate the file rather than hand-writing it.
