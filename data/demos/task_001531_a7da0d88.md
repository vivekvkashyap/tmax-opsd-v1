Build and verify each service in isolation before wiring them together. First run `start_env.sh`, then manually connect to `127.0.0.1:9000` (e.g., `python3 -c "import socket; ..."` or `nc`) to confirm you receive the JSON array of 1024 floats and understand its shape — this is your ground truth for testing the FFT logic, not something to assume. Implement `/api/signal` in Flask to open that raw TCP socket, read until the connection closes, parse the JSON, run `numpy.fft.rfft` (or `fft`) over the 1024 samples, take magnitudes, exclude index 0 (DC), and report the index of the peak magnitude as the frequency in Hz (since sample rate 1024 Hz and length 1024 means bin index equals Hz directly) — verify this by testing against a synthetic sine wave of a known frequency you construct yourself. For `/api/graph/<freq>`, load `molecule.json`, filter nodes where `abs(base_freq - freq) < 3.0`, build the induced subgraph using only edges where both endpoints pass that filter, then find connected components (union-find or BFS/DFS) and report the largest component's node count. Fix `nginx.conf` to listen on 8080 and `proxy_pass` any `/api/` prefix to `127.0.0.1:5000`, keeping the path suffix intact. Start Flask in the background and hit the Nginx endpoint end-to-end to confirm both routes work before finishing.
