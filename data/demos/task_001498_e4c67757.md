Implement extraction manually with Python's `tarfile` module (open with `tarfile.open(..., 'r:gz')`) rather than shelling out to `tar`, so you can inspect each member before writing it. For each member, resolve the intended output path with `os.path.realpath(os.path.join(dest_dir, member.name))` and check with `os.path.commonpath` (or a prefix check) that it still falls strictly inside the realpath of `/home/user/docs_raw` — reject absolute paths, `../` traversal, and any symlink tricks, incrementing a skipped counter and not extracting those members. For valid members, extract only regular files (guard against symlinks/hardlinks too) and count them. Then write small format-specific parsers: `json.load` for the array-of-objects, `csv.DictReader` for the CSV, and `xml.etree.ElementTree` for the XML `<docs><doc>` structure, each yielding (title, content) pairs. For each pair, write a markdown file named by replacing spaces in the title with underscores, with the body exactly matching the two-line template (heading, blank line, content), and count generated files. Finally emit report.json with the three exact integer keys, and actually run the script so the output files exist on disk.