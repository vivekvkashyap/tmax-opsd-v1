Read `build_config.json` with a JSON parser (don't hardcode the flag) and extract the `cflags` array to pass to `gcc`. Compile in two steps: first `gcc -shared -fPIC src/hw_mock.c -o lib/libhwmock.so`, then compile `metrics.c` with the JSON-derived flags plus a link against the first library: `gcc -shared -fPIC src/metrics.c -o lib/libmetrics.so <cflags> -L lib -lhwmock` (the `-D` flag from JSON is required or metrics.c's `#ifndef` guard fails the build). For the schema migration, run `ALTER TABLE results ADD COLUMN status TEXT` via sqlite3 — this adds a nullable column, so pre-existing rows will have `status = NULL`, which matters for the final JSON dump. In the Python integration script, load the shared library with `ctypes.CDLL` using an absolute path (or set `LD_LIBRARY_PATH` so `libmetrics.so` can resolve its dependency on `libhwmock.so` at runtime), declare `restype = ctypes.c_double` on `get_system_score`, call it, and insert the returned value plus `status="PASS"` into `results`. Finally query all rows ordered by `id ASC` and dump them as a JSON array with keys `id`, `metric_name`, `metric_value`, `status` (letting NULL map to JSON `null`) to `/home/user/output.json`.