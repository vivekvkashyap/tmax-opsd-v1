Work through the script's checks in order and satisfy each precisely. Create the target directory first (`mkdir -p /home/user/data_mount/active`), then create the parent app directory (`mkdir -p /home/user/app`) and the symlink pointing at the exact absolute path the script expects: `ln -s /home/user/data_mount/active /home/user/app/data` (use `os.readlink` or `readlink` to confirm it matches exactly, not a relative or trailing-slash variant). Create `/home/user/config/containers.json` with `mkdir -p /home/user/config` and valid JSON containing `{"ingest-worker": {"status": "running"}}` — validate it parses with `jq` or `python -m json.tool` before moving on. Save the given Python service code to `/home/user/app/ingest.py` unmodified. Finally run it with the required timezone set for that process only: `TZ=UTC python3 /home/user/app/ingest.py` (setting `TZ` inline on the command, not just exporting it elsewhere, ensures the check sees it). Confirm success by checking the script's own stdout message and by `cat /home/user/app/success.log` to see the exact expected string was written.