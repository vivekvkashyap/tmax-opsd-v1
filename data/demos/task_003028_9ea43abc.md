Build the C program first in isolation: read stdin line-by-line with a large enough buffer, tokenize each line on whitespace into an integer array, and if the token count is not a multiple of 3 (including empty lines), skip the line silently and continue — don't exit or print an error. For each valid line, iterate consecutive RGB triples computing Euclidean distance `sqrt((r1-r2)^2+(g1-g2)^2+(b1-b2)^2)` and print `idx1 idx2 r1 g1 b1 r2 g2 b2 dist` with `%.2f` for the distance; reset frame indices to 0 at the start of each line. Test this program directly with hand-crafted stdin (including a line with a non-multiple-of-3 token count) before wiring it into the pipeline, since the fuzz verifier will feed it many such edge cases. For the bash pipeline, use `ffmpeg` to extract 1 frame/sec from `/app/video.mp4` (e.g. `-vf fps=1`), then for each frame get its average color efficiently by resizing to 1x1 (e.g. `ffmpeg -i frame.png -vf scale=1:1 ...` or `convert`/`magick` with `-resize 1x1`) and extract the RGB triple, appending all triples as one space-separated line to `/home/user/rgb_wide.txt`. Pipe that file into `/home/user/calc_similarity` and redirect output to `/home/user/distances.txt`. Make the script executable and run it end-to-end to confirm real output is produced.
