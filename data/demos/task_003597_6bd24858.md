In decode_url, find the conditional that special-cases hex digits and skips the actual `sscanf` hex parse in certain cases — remove that broken branch so every `%XX` sequence is always parsed via sscanf into the byte value (keep bounds-checking near the end of the string). Implement run_property_tests(): loop 1000 times generating a random alphanumeric string of length 5-20, run it through encode_url then decode_url, and compare to the original — on any mismatch print an error and exit(1); after the loop, print "PASS" and exit(0). Compile with the exact g++ flags given. For the log pipeline, extract the quoted request path from each access.log line (the token between the HTTP method and "HTTP/x.x", e.g. via awk/sed), pipe those paths through your fixed url_tool to decode them, then `sort -u` the decoded result. Use `comm -12` against a matching-sorted known_bad.txt to get the intersection into matches.txt (make sure both inputs are sorted the same way, e.g. `LC_ALL=C sort`). For proxy.conf, write minimal `http { server { listen 127.0.0.1:8080; location / { proxy_pass http://127.0.0.1:9000; } access_log /home/user/access.log; } }` plus a top-level `events {}`, `daemon off;`, `pid /home/user/nginx.pid;`, and no `user` directive.
