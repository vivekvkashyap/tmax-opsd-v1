Do the cleaning step first and get it exactly right before benchmarking: load raw_data.csv with pandas, fill missing `income` with the median of the non-null income values (compute median after dropping NaNs, not including them), fill missing `score` with 0, then cap any income strictly greater than 100000 down to 100000 — apply the cap after the median fill, since a filled-in median value could itself need capping. Write this to clean_data.csv. For benchmarking, time each feat_*.py invocation from your own script using wall-clock timing around the subprocess call (e.g. `time.perf_counter()` before/after `subprocess.run([...])`), not the shell `time` builtin's output, so you can round consistently to 3 decimals and build the JSON dict in one pass; write timing.json with those three keys. For selection, only merge in feature columns from scripts whose measured time is strictly under 2.000 seconds — check the actual timing.json values you produced, not assumptions about which script "should" be fast. Merge selected feature CSVs onto clean_data.csv via an inner/left join on `id`, and write final_training_data.csv with a header row and comma separators.