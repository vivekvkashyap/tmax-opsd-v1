Build the pipeline as discrete filtering stages so each rule is easy to verify independently. First use `jq` to validate schema strictly per file: check `id` is a string, `accuracy` and `loss` are numbers, and `vector` is an array of exactly 3 numbers — skip (don't error on) any file failing this, since malformed files must be silently ignored, not crash the script. Next apply the outlier filters with numeric comparisons via `jq` or `awk`/`bc` (accuracy in [0.0, 1.0], loss > 0) — be careful with floating point comparisons in bash, prefer `jq`'s native number comparisons or `bc -l` rather than string comparisons. For similarity, compute squared Euclidean distance to `[1.0, 2.0, 3.0]` per surviving experiment (sum of squared component differences) using `jq` or `bc`, keeping full precision. Track the minimum distance and its `id` as you iterate (e.g. accumulate in a loop with a running best), then write only that `id` (no extra whitespace/quotes) to `/home/user/closest.txt`. Make the script executable and run it to produce the output before finishing.