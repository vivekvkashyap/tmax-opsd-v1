Start with OCR, don't skip straight to coding. Run `tesseract /app/specs.png stdout` (installing tesseract-ocr if missing) and inspect the raw text output carefully — OCR can misread digits/decimals, so sanity-check the extracted Mean/StdDev/Sigma-threshold numbers look physically reasonable before trusting them. Then scaffold a Rust project (`cargo new /home/user/etl_pipeline`), add a CSV-reading crate or hand-roll a simple split-based reader (avoid pulling in heavy dependencies if network access is limited — check `csv` crate availability first), and stream `readings.csv` row by row rather than loading it all into memory, since it's ~10k rows but the pattern should scale. For each row, look up the sensor's expected Mean/StdDev by name from the OCR'd constants (hardcode only the two sensor names you extracted, not values you didn't verify), skip unknown sensors, and flag `abs(value - mean) > sigma_threshold * stddev` using the exact `>` (strict) comparison specified. Collect flagged row_ids, sort ascending, and serialize as a JSON array to `/home/user/anomalies.json`. Build with `cargo build --release` and run it, then spot-check a few flagged/unflagged rows by hand against your OCR'd constants before considering it done, since the grading metric is F1 against a hidden ground truth.
