Fix the root cause first, then automate: open `/home/user/app/server.cpp` and find the deliberately wrong `socket_path` constant, changing it to `/home/user/run/upstream.sock`, and make sure the code (or your deploy script) creates `/home/user/run/` before the bind call. Then write `/home/user/deploy.sh` as a careful sequence: `set -e` at the top; compile with the exact `g++` command given; `rm -f` the old socket before starting the server so a stale socket file doesn't block `bind()`; launch the C++ binary in the background (`&`) and capture its PID; launch nginx with the exact `-p`/`-c` flags given and capture its PID too; sleep a couple seconds to let both come up; use `curl -s` (not `-i`) against `http://127.0.0.1:8080/` so only the body lands in `/home/user/test_result.log`, not headers. Install a `trap '... kill $PID1 $PID2 2>/dev/null' EXIT` near the top of the script so both processes are always cleaned up whether the script exits normally or via `set -e` failing partway through. Verify manually by running the script and checking the log file content and that no stray `server_bin`/`nginx` processes remain (`pgrep`) afterward.