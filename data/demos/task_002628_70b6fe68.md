OCR `/app/policy.png` (e.g. `tesseract /app/policy.png -`) to recover the exact `POLICY_SALT` string and `BANNED_CWE` id — don't guess these values, read them off the image and use them verbatim (the salt likely has mixed case/punctuation, so copy it exactly). In `policy_check.sh`: read the target file, strip the first line (the `# SIGNATURE: <hash>` header) to get the remaining content, concatenate the OCR'd `POLICY_SALT` directly onto the end of that content with no extra newline, and compute `sha256sum` (via a pipe with `printf '%s'` or careful `tail -n +2` handling to avoid introducing a stray trailing newline) — compare it against the hash captured from the header line. Independently, `grep`/regex-scan the file body for `os.system(` calls or `shell\s*=\s*True` (allowing spaces around `=`) inside subprocess calls; either signature mismatch or a CWE-78 pattern hit means reject (exit 1), otherwise accept (exit 0). Test against `/app/corpus/clean` and `/app/corpus/evil` to confirm 100% pass before finishing, paying special attention to exact byte-for-byte payload reconstruction since a single stray newline breaks every signature check.