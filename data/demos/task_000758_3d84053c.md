Build this pipeline in stages and test each independently before wiring them together. First write the decoder: for each raw_payload hex string, bytes.fromhex() it, then try decoding as UTF-8 first, falling back to UTF-16LE or cp1252 (Windows-1252) with errors='ignore'/'replace' as needed, to get a clean UTF-8 string. Second, confirm /app/log_decoder's calling convention by running it directly on a sample string argument and inspecting its `KEY=VAL|KEY=VAL` output format, then parse out CPU_TEMP as a float from that output (skip rows where it's missing/unparseable). Third, use concurrent.futures.ProcessPoolExecutor or ThreadPoolExecutor to parallelize subprocess calls to log_decoder across all CSV rows for throughput. Fourth, bucket each row's timestamp into its hour (truncate to the hour, format as YYYY-MM-DDTHH:00:00Z) and average CPU_TEMP per bucket, keeping this in an in-memory dict. Finally, stand up an HTTP server on 127.0.0.1:8080 with a GET /api/temperature?hour=... handler that looks up the bucket and returns the exact JSON shape specified (200 with rounded avg, or 404 with the error JSON), and keep the process running (background it) so the verifier can query it after your script finishes setup.