Treat this as two independent extraction problems feeding one classifier. First transcribe `/app/auditor_notes.wav` (try `whisper`, or Python `SpeechRecognition`, or any ASR tool available) to recover the attacker's exact indicator strings — listen for the precise XSS payload pattern (a URI scheme prefix) and the exact ELF section name being tampered with; don't assume, extract them verbatim from the audio. Cross-check what you hear against the sample files in `/app/corpora/clean/` vs `/app/corpora/evil/` — diff the JSON fields between benign and malicious examples to confirm which field/value combination is the actual signal (e.g. a substring in `data.url`, or an exact string in `data.tampered_section` for `event_type: elf_integrity`). Then scaffold a Rust binary at `/home/user/classifier` (`cargo new`) that parses a single JSON file argument (use `serde_json`), checks for those two indicators, exits `1` if either matches and `0` otherwise (handle parse errors safely, defaulting to non-crashing behavior). Build in release mode (`cargo build --release`) so the binary lands at `target/release/classifier`. Test against all provided sample files before trusting it — the hidden eval corpus will be scored strictly on exit codes.
