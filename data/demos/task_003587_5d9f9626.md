Two independent subproblems: extracting a numeric parameter from an image, and building a Rust classifier around it. For the image, run `tesseract /app/lab_notes.png stdout` (installing tesseract if needed) and parse the `GC_MAX_THRESHOLD=...` line from the OCR output programmatically rather than reading it once and hardcoding the number — OCR can be noisy, so sanity-check the extracted value looks like a plausible fraction between 0 and 1 before using it. For the Rust tool, `cargo new seq_filter`, read the FASTA file, skip header lines (starting with `>`), concatenate sequence lines, strip whitespace, count G/C vs A/C/G/T case-insensitively, and compute the ratio. Compare that ratio to the threshold you parsed from the image (not a literal you typed in) using strict greater-than, and `std::process::exit(1)` for "evil" or exit 0 (normal return) for "clean". Test against both `/home/user/sample_clean/` and `/home/user/sample_evil/` locally, and double-check boundary behavior (GC ratio exactly equal to threshold must be "clean") since the hidden corpus will likely include near-threshold cases.
