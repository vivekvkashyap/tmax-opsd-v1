Start by reverse-engineering the schema before writing any code: run `sqlite3 /home/user/citation_graph.db ".schema"` (or `.tables` then `.schema <table>`) to find the node table and the edge table and their real column names — don't assume names like "papers"/"citations", confirm them. To repair duplicates, first inspect with `SELECT source, target, COUNT(*) FROM edges GROUP BY source, target HAVING COUNT(*)>1` (using the real column names), then delete duplicates keeping one row per pair — a reliable SQL pattern is deleting rows whose `rowid` is not the `MIN(rowid)` for each (source,target) group. After dedup, add `CREATE UNIQUE INDEX ... ON edges(source, target)` so it can't recur. For the C program: link against `-lsqlite3`, use `sqlite3_prepare_v2`/`sqlite3_step` to pull all edges, accumulate an in-degree and out-degree count per node in a hash map or array (size by max node id or by distinct node count), sum them into total degree, then sort descending by degree with ties broken by ascending node_id. Write JSON manually (or with a tiny helper) matching the exact schema, taking only the top 3. Compile with `gcc graph_analyzer.c -lsqlite3 -o /home/user/graph_analyzer`, run it, and confirm `/home/user/top_nodes.json` is produced.