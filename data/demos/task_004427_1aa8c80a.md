First convert encoding at the shell level rather than fighting UTF-16LE in C++: `iconv -f UTF-16LE -t UTF-8 /home/user/raw_metrics.csv -o /tmp/raw_metrics_utf8.csv`, then have your C++ program read that UTF-8 file (or accept a path argument so this is scriptable). Parse the header row to get server names in column order, then for each data row split on commas, and for each server column check the latency against `>= 0.0 && <= 1000.0` — if a value fails, simply exclude just that (server, value) pair from that server's running sum/count rather than dropping the row, since the other columns in the same row may still be valid. After processing all rows, compute each server's mean from its own valid-count accumulator (not a fixed row count), format each to exactly 2 decimal places (watch for banker's-rounding vs standard rounding differences — use `printf`/`std::setprecision` consistently), and build the replacement block with one `ServerName: value ms` line per server. Read `/home/user/report_template.txt`, replace the literal `{{REPORTS}}` placeholder with that block (preserving surrounding template text exactly), and write the result to `/home/user/final_report.txt`. Wire the iconv step, compilation (`g++`), and execution into a single shell sequence so it's reproducible.
