Port the C rule-evaluation logic literally into Python, then build the ELF introspection around it with `subprocess`. For each plugin `.so`, get its class by running `readelf -h <file>` and checking the `Class:` line for `ELF32`/`ELF64`; get its exported dynamic symbols by running `readelf --dyn-syms <file>` (or `-d`/`--syms`) and matching the symbol name in the `Name` column, restricting to defined (non-UND) entries so you don't false-positive on undefined/imported symbols. Parse `/home/user/plugin_rules.txt` with a simple line format split on `:` and then on whitespace to get `CLASS=`, the `AND`/`OR` operator, and `SYM=`. Evaluate exactly like the C snippet: `class_match = (actual_class == expected_class)`, then `AND` means both must hold, `OR` means either. Write the per-plugin boolean results as a JSON dict to `/home/user/load_plan.json` with `json.dump`. Watch for readelf output format differences (extra whitespace, symbol visibility columns) — parse robustly rather than assuming fixed column offsets.
