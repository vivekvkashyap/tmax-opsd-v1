Work through this like a real on-call debugging session, not a rewrite. First resolve the pinned-version conflict in requirements.txt by aligning numpy/pandas versions so `pip install -r requirements.txt` succeeds in a fresh venv. Then read service.py for three distinct bugs: (1) the EMA update uses the wrong sign on the decay term — compare against `new = value*alpha + prev*(1-alpha)` and fix the arithmetic; (2) `json.loads` has no exception handling, so wrap it and return 400 with exact text "Invalid JSON" on failure; (3) the handler creates a task and adds it to `active_tasks`, but on `CancelledError` it just re-raises without cancelling the inner task or removing it from the set — fix this so the task is cancelled and discarded regardless of how the handler exits. After fixing, start the service in the venv, run the unmodified client.py to generate the traffic sequence, send SIGUSR1, and inspect diagnostics.json to confirm active_tasks is 0 and the EMA reflects your corrected formula — recompute the expected EMA by hand to sanity-check before finishing.
