Build the checker as three independent, composable checks rather than one tangled function. (1) Certificate validation: use OpenSSL's verification APIs (e.g. `X509_STORE` + `X509_STORE_CTX` with `X509_verify_cert`, or simply shell out to `openssl verify -CAfile ca.pem cert.pem` from within the C program if easier) to confirm `cert.pem` chains to `ca.pem` — don't just check the two files parse, actually verify the signature/chain. (2) Integrity: parse `manifest.sha256` lines (`<hash>  <relpath>`), recompute SHA-256 of each referenced file relative to the bundle directory using OpenSSL's EVP digest API, and compare hex strings exactly — a bundle fails INTEGRITY if any listed file's hash differs. (3) CSP: read `csp.txt`, locate the `script-src` directive (it's one of several semicolon-separated directives), and fail only if that directive's value contains the literal substring `'unsafe-inline'`. Match the required output format byte-for-byte (labels, colons, spacing, PASS/FAIL casing). Compile with `gcc ... -lssl -lcrypto`, then invoke the binary on each of the four bundle directories in the exact order dev, staging, prod, dr, redirecting concatenated stdout to `/home/user/final_report.log`.