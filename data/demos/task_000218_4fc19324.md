Build this as a pure Git-hook pipeline, not an interactive tool. First `git init --bare /home/user/account_manager.git`. Write the `hooks/post-receive` file as a Python 3 script starting with `#!/usr/bin/env python3` and `chmod +x` it — Git only invokes hooks that are executable. Inside the script, read `sys.stdin` line by line, split each line into `oldrev newrev refname`, then shell out to `git show <newrev>:users.json` (via `subprocess.run` with `cwd` set to the bare repo, or `GIT_DIR` env var) to get the pushed file's content, and `json.loads` it. Iterate the list in order and build the output lines: for "add", first emit one `groupadd -f <g>` per group in list order, then emit `useradd -m -G g1,g2,... <username>` — but only include `-G ...` when the groups list is non-empty (a common bug is emitting `-G ` with a trailing empty argument). For "remove", emit `userdel -r <username>`. Prepend `#!/bin/bash` and write the joined lines to `/home/user/apply_users.sh`, then `os.chmod(path, 0o700)`. Test end-to-end by cloning the bare repo elsewhere, committing a sample `users.json`, and pushing to `master` to confirm the hook fires and the script content/permissions are exact.