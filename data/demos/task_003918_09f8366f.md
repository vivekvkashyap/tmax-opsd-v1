Work through this in dependency order: first get ffmpeg extraction working (`ffmpeg -i /app/video.mp4 -vf fps=10 -pix_fmt gray /home/user/frames/%04d.pgm`, creating PGM in P5/P2 format), then fix the Makefile — check for missing tabs before recipe lines (Make requires literal tabs, not spaces) and make sure the link line includes `-lm -lsqlite3` after the object files. Apply the provided patch with `patch -p1 < list_fix.patch` (or `patch -p0`, check the diff header) inside src/, then try to build; if it segfaults or gives wrong results even after patching, read list.c's append/traversal logic closely for classic doubly-linked-list bugs — forgetting to update the tail pointer or prev pointer on insert, or iterating one node past the actual end. Use gdb or valgrind to localize crashes rather than guessing. Once it compiles cleanly, run `./motion_analyzer /home/user/frames/ /home/user/analytics.db` and verify rows land in frame_stats with frame_num, diff_score computed as mean absolute pixel difference between consecutive frames, and timestamp_ms = (N-1)*100. Migrate the schema with `ALTER TABLE frame_stats ADD COLUMN diff_score REAL; ALTER TABLE frame_stats ADD COLUMN timestamp_ms INTEGER;` before running the analyzer.