Handle the two data types with their native conversion tools rather than a generic script. For `win_logs/`, convert each UTF-16LE file with `iconv -f UTF-16LE -t UTF-8 input.txt -o output` (verify with `file --mime-encoding` afterward), then write it out under a lowercased filename with `.log` replacing `.txt` into `/home/user/processed_data/logs/` — do the rename and the encoding conversion together so you don't lose track of which file is which. For `csv_metrics/`, read each CSV with `csv.DictReader` (Python) to preserve header-keyed rows as strings (don't cast numeric-looking values, since the expected JSON keeps them as the original CSV strings), `json.dump` the list of row-dicts, and save under a lowercased `.json` filename in `/home/user/processed_data/metrics/`. Once both subdirectories are populated, archive with `tar -czf /home/user/final_archive.tar.gz -C /home/user processed_data` so the tar root contains `processed_data/` itself (extracting it should not need an extra directory level). Spot-check by extracting the tarball to a scratch dir and inspecting a log's encoding and a JSON file's structure before finishing.
