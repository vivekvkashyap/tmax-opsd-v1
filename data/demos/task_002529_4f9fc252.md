Work top-down through the three failure layers. First, the link failure: read `CMakeLists.txt` and notice it builds `math_utils` and `solver_core` as separate shared libs but never links them together — add `target_link_libraries(solver_core math_utils)`, and to avoid needing `LD_LIBRARY_PATH`, add RPATH so the loader finds `libmath_utils.so` next to `libsolver_core.so` at runtime (e.g. `set_target_properties(solver_core PROPERTIES INSTALL_RPATH "$ORIGIN")` plus `BUILD_WITH_INSTALL_RPATH TRUE`, or rely on `CMAKE_BUILD_RPATH` pointing at the build dir) — rebuild in `/home/user/project/build` and confirm with `ldd`/loading via ctypes that no manual env var is needed. Second, run `valgrind --leak-check=full` (or just read the C carefully) on `solver_core.c`'s `compute_score`: look for off-by-one loop bounds relative to a `malloc`'d buffer size (a classic pattern is looping `<=` instead of `<` against an array sized to `num_items`) and a missing `free()` of any allocated buffer — fix both, then re-run valgrind to confirm zero errors/leaks. Third, treat the coloring problem as a small CSP: with only 5 nodes and 3 colors, brute-force or backtrack over all assignments checking each edge constraint and the fixed node-0 constraint, take any valid solution, pass it via `ctypes` (POINTER(c_int), c_int) into `compute_score`, and write the returned int to `/home/user/result.txt`.
