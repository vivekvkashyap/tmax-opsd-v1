Write this C program in stages you can sanity-check independently: binary loading, exact cosine similarity, quantization, and the statistics. Read `/home/user/embeddings.bin` as 100 contiguous `float32` little-endian vectors of 64 dims each (a flat `fread` into a `float[100][64]` works on a little-endian host); treat index 0 as the query. For cosine similarity, cast each component to `double` before accumulating the dot product and both norms — accumulating in `float` will introduce visible error over 64-dim sums, which the task explicitly guards against. Track the top-3 indices among 1..99 by exact similarity (e.g. keep a small running top-3 as you scan, careful to handle ties consistently). For quantization, apply `round(val*127.0)/127.0` component-wise to build quantized copies of every vector, then recompute cosine similarity the same double-precision way on the quantized vectors. For the CI, compute `error_i = exact_i - quantized_i` over the 99 non-query vectors, take the mean, then the *sample* standard deviation using N-1=98 in the denominator (not population stddev, which is a common naive mistake), and multiply `1.96 * stddev/sqrt(99)`. Format all output values to exactly 6 decimals as specified.