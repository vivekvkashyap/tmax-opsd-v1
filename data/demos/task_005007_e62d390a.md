Build the script as four independent extraction stages, each using a targeted tool, then assemble the fixed-format report:
1. PID: `grep` the line containing `"backdoor"` in `ss_output.txt` and pull the `pid=NNNN` value out of the process field with `grep -oP` or `sed`/`awk` — don't assume a fixed column position, parse around the `pid=` marker.
2. C2 URL: run `strings` on `backdoor_bin` and `grep` for a line matching `^https://.*\.evil\.com/payload` (the URL always has that prefix/suffix, so anchor on it rather than guessing the exact domain).
3. Attacker IPs: `grep` `access.log` for lines containing `jndi:ldap`, extract the leading IP with `awk '{print $1}'`, then `sort -u` for unique + alphabetical order.
4. Allowed scripts: isolate the `script-src` directive from `csp.txt` (e.g. `grep -oP 'script-src[^;]*'`), tokenize on whitespace, and filter out tokens that don't start with `http` (drops `'self'`/`'unsafe-inline'`), then `sort -u`.
Join the IP list and script list with commas (no spaces) and write all four `KEY=value` lines to `/home/user/report.txt` using `printf`/`echo` with exact key names. Make the script idempotent and re-run it to confirm the file matches the exact format.
