Build the Rust pipeline in the same order a careful ML engineer would validate it: (1) inner-join features.csv and labels.csv on user_id, keeping only rows present in both (this will drop some feature-only rows — verify the count) — (2) split rows into train/test using membership in test_ids.txt as the ground truth, everything else is train; (3) the critical step to get right is normalization — compute mean and sample standard deviation (divide by N-1, not N) for f1/f2/f3 using ONLY the training rows, then apply that same mean/std to standardize both train and test features; never let any test-set value influence the mean/std computation, that's the data leak the task warns about; (4) for each test user compute Euclidean distance in normalized-feature space to every train user, pick the minimum, and on exact ties choose the train user with the smallest user_id (iterate train rows in ascending user_id order and use a strict "<" comparison so the first/lowest-id tie wins); (5) predict using that neighbor's label; (6) write predictions.csv sorted ascending by user_id with the exact header `user_id,predicted_label`. Test your normalization logic against a hand-computed example before trusting the full pipeline.
