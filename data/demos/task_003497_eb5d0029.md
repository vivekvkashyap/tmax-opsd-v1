Structure the program as clear pipeline stages you can test independently. First, detect UTF-16LE by checking for the FF FE byte-order-mark (or, if no BOM, heuristically by checking for the alternating-null-byte pattern typical of ASCII-range UTF-16LE text) and convert to UTF-8 before handing bytes to the JSON parser — use `#include <codecvt>`/`std::wstring_convert` or manual byte-pair decoding, and unit-test this conversion against a UTF-16LE sample file you create yourself. Second, parse with nlohmann/json from /app/include/json.hpp inside a try/catch so malformed JSON fails cleanly (return 1) rather than throwing uncaught. Third, validate: require "target_path" to not contain the literal substring ".." anywhere, and require "auth_token" to exactly equal the passphrase spoken in /app/auth_token.wav — since this is audio, transcribe it yourself first (e.g. with a speech-to-text tool available on the system, or by listening if you have that capability) and hardcode the exact recovered phrase as the comparison string; don't guess at plausible-sounding phrases. Fourth, for the atomic write, write to a temp file in the same output directory (e.g. via mkstemp or a fixed temp name) then call rename() — never write directly to the final path. Before considering it done, run your compiled ingest binary against every file in both /app/corpus/clean/ and /app/corpus/evil/ and confirm the accept/reject exit codes match expectations for each one individually, not just a couple of spot checks.
