Model this as directed-graph cycle detection, not a brute-force permutation search: load `transactions.csv` into an adjacency structure (dict of source -> list of (target, tx_id, amount)) and `accounts.csv` into a risk_level lookup. Since you need cycles of exactly length 4, do a bounded DFS/backtracking from each node that only follows edges to accounts with `risk_level` in {`high_risk`, `offshore`}, tracking the path and accumulated amount, and stops as soon as depth reaches 4 — at depth 4, check if the edge back to the start node exists. Prune early: abandon a branch as soon as you hit an account whose risk_level isn't high_risk/offshore, or once you already have 4 nodes without a closing edge. Because a naive script might also match cycles of other lengths or accounts of mixed risk, be strict about checking exactly 4 distinct accounts, exactly 4 edges, all 4 accounts risk-qualified, and total transaction amount strictly greater than 10000. Once found, sort the account IDs alphabetically and write them as a JSON array to `/home/user/suspicious_cycle.json` with `json.dump`. Test your script's logic against a couple of hand-verified paths before trusting the result, and confirm only one cycle satisfies all criteria in the given data.