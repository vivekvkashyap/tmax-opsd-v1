First locate and read `/app/lib_dataclean/cleaner.py`'s `Deduplicator.is_duplicate` — it currently always returns `False` even when a row equals `self.last_row`, which is the deliberate bug; fix it so it returns `True` (duplicate) when the row matches the stored last row, and still updates `self.last_row` appropriately so subsequent rows compare against the most recently seen row, not the last emitted one (be careful about exactly which row updates the "last" state — re-derive the intended semantics from how it's meant to be used in the pipeline). Reinstall/reference the fixed package before importing it in `/home/user/process.py`. Build the stream processor as: parse each stdin line, drop rows where `reading` fails `int()` conversion, run through the fixed deduplicator per (or across) rows, then maintain a per-`device_id` rolling deque of up to 5 valid non-duplicate readings, checking after each insert whether the window's sum is `>= 100`; on an anomaly, look up `device_id` in `/app/device_map.json` defaulting to `"UNKNOWN"`, and print the exact CSV line format specified. Test against small handcrafted input sequences (including consecutive duplicates and invalid readings) to confirm exact output matching before relying on the fuzz comparison.