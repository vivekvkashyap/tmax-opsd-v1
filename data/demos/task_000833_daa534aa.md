Inspect existing indexes first with `sqlite3 /home/user/graph.db ".indexes edges"` and `.schema edges`, then `DROP INDEX` the stale one — an index only on `dst` (or similar) doesn't help a 3-way self-join chain on `src`/`dst` and can mislead the planner. For a directed-triangle join (e1.dst=e2.src, e2.dst=e3.src, e3.dst=e1.src), create composite indexes covering both join columns per hop, e.g. `CREATE INDEX idx_edges_src_dst ON edges(src, dst)` (and consider one on `(dst, src)` too) so SQLite can use `SEARCH` instead of `SCAN` at every join step — verify with `EXPLAIN QUERY PLAN` before wiring it into the C program. In `triangle_analyzer.c`, use `sqlite3_exec` or `sqlite3_prepare_v2`/step over the `EXPLAIN QUERY PLAN` statement and write only the `detail` column (index 3 in the result set) to `/home/user/query_plan.txt`, one line per step, then run the real aggregate query and format the single top result as `NodeID,TriangleCount` to `/home/user/max_triangle.txt`. Compile exactly as specified with `-lsqlite3` and run the binary to actually produce both output files before finishing.