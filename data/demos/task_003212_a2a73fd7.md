Build the pipeline in `/home/user/audit_pipeline.py` as distinct, testable stages. First, connect to `/home/user/finances.db` and run `REINDEX` (or `REINDEX idx_sender`) to rebuild the corrupted index before querying — this is required even though SQLite will still return correct rows without it, since the task explicitly checks for the repair step. Second, write a parameterized SQL query using a window function like `AVG(amount) OVER (PARTITION BY sender ORDER BY tx_date ROWS BETWEEN 2 PRECEDING AND CURRENT ROW)` and select senders where this moving average strictly exceeds 50000 in at least one row — collect the distinct set of "High Risk" sender names. Third, load all transactions into a `networkx.DiGraph()` with sender→receiver edges. Fourth, use `networkx.simple_cycles(graph)` (or manually enumerate 3-node cycles) filtered to length exactly 3, since simple_cycles returns cycles of any length. Fifth, keep only cycles where the cycle's node set intersects the High Risk sender set. For each surviving cycle, sort its account names alphabetically into an inner list; sort the outer list of these inner lists lexicographically; write the result as JSON to `/home/user/compliance_report.json`. Watch for the common pitfall of confusing "3 preceding rows" window sizing (use last 3 transactions total, i.e. current + 2 preceding) and for cycle-detection false positives from non-simple or longer cycles.