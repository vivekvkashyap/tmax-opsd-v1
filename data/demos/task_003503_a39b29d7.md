Start with the build failure: open the `Makefile` in `/app/libsemver-v1.0.0/` and look at the `shared` target's compile rule — a shared object requires every `.o` file it's built from to be compiled as position-independent code, so add `-fPIC` to the `CFLAGS` (or the specific compile recipe) used for those objects, then confirm `make shared` produces `libsemver.so`. For the filter itself, don't try to reimplement semver validation logic yourself — link against the real `libsemver.so` (e.g. via Python `ctypes.CDLL` or a small C wrapper) and let its actual parser decide validity, since that's the whole point of "strict sanitiser" matching the library's real behavior. Read the library's header/source briefly to find its parse entry point and how it signals success/failure. For robustness against adversarial input (huge numbers, deeply nested/garbage strings, absurdly long lines), don't just trust the library call directly in-process if it might segfault on bad input — isolate each parse attempt (e.g., fork a subprocess per line, or wrap the call so a crash in the library only fails that one line rather than killing your whole filter) and only forward a line to the output file if the isolated call returns a clean success. Preserve input order and write only validated lines.