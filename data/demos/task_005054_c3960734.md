Treat this as a forensics walkthrough where each artifact needs a specific extraction technique, not just eyeballing. (1) For the JWT bypass, grep `requests.log` for `Authorization: Bearer` lines, split each token on `.`, base64url-decode the header segment (pad as needed, `base64 -d` after replacing `-_` with `+/`), and find the one whose decoded header shows `"alg":"none"` — the IP is on that same log line/request block. (2) For the SQL injection, open `auth.js` and locate the line where a request field is concatenated with `+` directly into a SQL query string (not parameterized) — report that line number via `grep -n` or a text editor's line count. (3) For SSH hardening, diff `sshd_config` against your knowledge of secure defaults, or grep for directives that weaken root login (e.g. `PermitRootLogin`) and take the exact `Directive value` line. (4) For the binary swap, run `md5sum -c checksums.md5` (or compute md5sum on each file and compare) inside `/home/user/evidence/bin/` to find the mismatching file, then `strings` it and grep for a token starting with `c2.` to recover the embedded domain. Write all four findings into `/home/user/findings.ini` in the exact specified `[Forensics]` key=value format.