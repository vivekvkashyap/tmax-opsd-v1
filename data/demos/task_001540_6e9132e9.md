Handle each sub-task independently and verify each before moving on. For the memory dump, use `strings` or a Python byte-scan on `/home/user/uptime_project/memory.dmp` to locate the literal marker `SLA_TARGET_HOST=` and extract the printable text immediately following it up to the next null/non-printable byte, writing only that hostname (no marker, no trailing junk) to `/home/user/crashing_host.txt` — inspect the raw bytes first (e.g. `xxd`) rather than assuming a fixed length. For the Makefile, open it and change the include-path flag from `./legacy_headers` to `./modern_headers` (look for a `-I` compiler flag). For the SLA bug, open `src/uptime_monitor.c` and find the `calculate_sla` function: the classic bug is integer division happening before the multiply/cast to float, so ensure at least one operand is cast to `float`/`double` early enough that the division itself produces a fractional result (casting only the final product doesn't fix it). After making all three fixes, run `make` inside `/home/user/uptime_project` and confirm the binary builds cleanly and produces a non-zero, correctly rounded percentage on a manual test invocation before considering the task done.