Work through the checklist in order, verifying each step's output before moving to the next. (1) `sha256sum -c` or manually compare `sha256sum /home/user/validator.c` against the value in `checksum.sha256`; only compile with `gcc` if it matches. (2) Run `openssl verify -CAfile /home/user/ca.crt /home/user/server.crt` and redirect stdout to `/home/user/cert_verify.log`. (3) Read `validator.c` carefully to find the auth-bypass logic: look at how the header is parsed and what condition skips the signature check (a JWT-style `"alg":"none"` bypass is a classic pattern — search for where the header string is checked against literal substrings). Also read the XOR decryption function to find the hardcoded key. (4) To craft the forged token, hex-encode a header that triggers the bypass condition, XOR-encrypt the literal bytes of `{"role":"admin"}` with the hardcoded key and hex-encode the result, and use any placeholder hex for the signature segment since it's skipped — assemble these three hex-encoded pieces joined by `.` in the format the program's parser expects (check how it splits the token, e.g. by `strtok` on `.`). (5) Before writing the final token to `forged_token.txt`, actually run the compiled validator against your crafted token and confirm it prints the admin-granted success message.