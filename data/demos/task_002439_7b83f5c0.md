Reverse-engineer `/app/legacy_auth` methodically rather than guessing: run `strings /app/legacy_auth` to look for library hints (e.g. an MD5/hash implementation signature or format strings revealing how inputs are concatenated), and if needed disassemble with `objdump -d` or `radare2`/`ghidra` to trace how the username/password/timestamp arguments are combined before hashing — pay attention to the exact separator characters and whether the timestamp is used raw or transformed (e.g. divided/windowed) before being folded into the hash input. Cross-check your hypothesis by running the binary yourself with known inputs (`./legacy_auth <user> <pass> <timestamp>`) and comparing the output token against your own MD5 computation of candidate concatenation formats until they match exactly — do this empirically, don't assume a single scheme. Once confirmed, implement the identical construction in Go: for each log line's `(username, timestamp)`, hash each wordlist candidate password using your reverse-engineered formula and compare to the observed `X-Auth-Token`; the first match is the recovered password. Write `/home/user/recover_passwords.go` to take `(logfile, wordlist, outputJSON)` as args and output a username->password JSON map. Test against the provided sample log/wordlist before submitting, since the real grading uses held-out logs and requires ≥99% recovery accuracy.