Break this into independently-checkable stages and print intermediate results as you go, since the final JSON must match a precise recomputation. Read all CSVs in /home/user/datasets/ with pandas, `pd.concat` them, and `groupby('group')` — use `.agg` with sum for trials/successes and mean for f1/f2/f3 in one call so the row alignment across the two aggregations stays consistent, and sort the resulting groups alphabetically by index (`.sort_index()`) since matrix row order for the PCA step depends on it. For the Bayesian step, recognize that with a Beta(1,1) prior and Binomial likelihood, the posterior is Beta(1+successes, 1+trials-successes), so the posterior mean is simply (1+successes)/(2+trials) — compute this directly per group rather than doing MCMC or numerical integration. For the PCA step, build the 3-column f1/f2/f3 matrix using the same sorted group order, mean-center each column, run `np.linalg.svd` on the centered matrix, take `Vt[0]` as the first principal component, and flip its sign if the first element is negative (a fixed, deterministic sign convention). Write raw unrounded floats into analysis_report.json in the exact schema given, keyed by group name.
