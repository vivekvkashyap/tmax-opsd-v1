First extract the routing-rule table from `/app/firewall_rules.png` with an OCR tool (e.g. `tesseract`), then inspect the raw OCR text carefully and fix likely misreads (O/0, l/1, stray spaces) before parsing it into a service→CIDR map — do not hardcode assumed values, derive them from what OCR actually returns. Build `/home/user/fw_check` (Bash or Python, executable, correct shebang) that: takes exactly two args or prints `DROP`; looks up the source service in the parsed map, printing `DROP` if unknown; validates the destination is a strictly well-formed IPv4 address (reject things like `256.0.0.1`, extra octets, leading/trailing whitespace, non-numeric parts) using a real IP library/parser rather than a loose regex; then checks CIDR membership (e.g. Python's `ipaddress` module) and prints exactly `ALLOW` or `DROP` with no trailing extra output. Test against edge cases: empty/whitespace source names, malformed IPs, and addresses just outside a CIDR boundary, since a naive octet-range regex often accepts invalid IPs or misjudges CIDR membership.
