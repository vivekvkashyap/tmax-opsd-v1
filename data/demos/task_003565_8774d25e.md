Write this as a C++ CSV pipeline in clear stages, testing each against the actual input file as you go. Parse `/home/user/data/raw_metrics.csv` with a proper CSV reader that respects quoted fields (the `EventDescription` values are quoted and contain commas/brackets, so naive `split(",")` will break them) — a small hand-rolled state-machine parser or a simple quote-aware splitter is sufficient. For each row, extract the event code from `EventDescription` using the substring between the first `[` and its matching `]` (e.g. a regex like `\[([^\]]+)\]` or manual `find('[')`/`find(']')`). Conceptually melt the three node columns (NodeAlpha/Beta/Gamma) into (NodeName, Count) pairs per row — so each input row contributes three (NodeName, EventCode, Count) observations. Aggregate by summing Count into a `map<pair<string,string>, long>` keyed by (NodeName, EventCode) (a `std::map` with a composite key or nested maps gives you sorting for free, or aggregate then sort explicitly). Drop groups whose summed count is exactly 0 before output. Sort by NodeName then EventCode ascending, write the header `NodeName,EventCode,TotalCount` followed by unquoted comma-separated rows to `/home/user/data/summary.csv`. Compile with `g++ -std=c++17`, run it, and `diff` your output against your own hand-traced expected sums for a couple of rows to confirm correctness before finishing.