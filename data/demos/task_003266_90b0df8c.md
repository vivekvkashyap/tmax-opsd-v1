This is a black-box reverse-engineering task: treat `/app/db_recover` as an oracle you query, not something to disassemble exhaustively. Build small, targeted test cases and pipe them into the binary via stdin to observe stdout: (1) single transaction, single key, to learn basic output formatting (e.g. `Key=Value` per line, ordering); (2) two SET transactions on the same key with different timestamps/offsets to figure out how local timestamp + timezone offset combine into a comparable absolute time (try offsets that push one earlier/later than the other to isolate the sign convention); (3) two transactions with identical resulting absolute time but different TxIDs to find the tie-breaker; (4) a SET followed by a DEL (and vice versa) on the same key to see whether DEL removes the key from output entirely or leaves a tombstone; (5) confirm final key ordering (likely lexicographic) by mixing key names out of order. Iterate hypothesis -> test -> refine rather than assuming the algorithm from the spec text alone — the point is to match the binary's *actual* behavior bit-for-bit, including edge cases like negative offsets and equal timestamps. Once your Python mirrors observed behavior across many varied trials, write it to `/home/user/recover.py` reading stdin and writing stdout with no extra formatting.