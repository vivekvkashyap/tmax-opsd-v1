Work the three sub-problems independently before wiring them together. Read `/app/incident.log` chronologically to find the last/anomalous request just before the crash and decode its base64 payload to see its size/shape (likely far exceeding the buffer). To recover the auth token, run `strings /app/data-server/lib/auth.o` (and/or `objdump -d`/`nm`) — the object file has no source, but the comparison string is typically embedded in plaintext in the `.rodata` section, so grep the strings output rather than trying to disassemble the whole thing. For the fix, open `/app/data-server/src/processor.cpp`, find the `memcpy` that copies `len` bytes into a fixed buffer with no size check, and add a bound so any `len` over `MAX_PAYLOAD_SIZE` (256) is clamped before the copy — keep the rest of the logic (including how it terminates/echoes the buffer) unchanged so existing behavior for small payloads doesn't shift. Build with `make` inside `/app/data-server/`, then start the compiled service backgrounded (`nohup ... &` or `&` plus `disown`) listening on `127.0.0.1:8080`, and manually curl `/process` with both a small and an oversized payload plus the recovered bearer token to confirm it no longer crashes before finishing.
