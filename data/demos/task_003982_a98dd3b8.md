Replace the naive OLS matrix inversion with proper Ridge regression rather than patching the instability in place: compute X^T X, add lambda * I (lambda = 0.1, I sized M x M matching the number of feature columns), then solve (X^T X + lambda I) beta = X^T y directly (prefer `ndarray_linalg::Solve::solve` over explicitly inverting when possible, since solving a linear system is more numerically stable than an explicit inverse even after regularization). Read X and y from the HDF5 file with `read_2d`/`read_1d` exactly as the current code does — don't touch the file itself. After computing beta, format each coefficient to exactly 6 decimal places per line with `{:.6}` when writing `solution.txt`, matching the row order of the coefficient vector. Build incrementally: get it compiling with `cargo build --release` first, then run it and sanity-check the output coefficients are small/stable (not exploding) compared to the original OLS run, which is itself strong evidence the multicollinearity issue is now handled. Watch dimension-order pitfalls: verify X is truly N x M (samples x features) with `ndarray`'s shape before building X^T X, so the identity matrix and solve line up.
