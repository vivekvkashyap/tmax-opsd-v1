Since you don't have source for `/app/anonymizer`, treat it as a black box: feed it a handful of known `user_id event_id` pairs via stdin and observe the `secure_token` outputs to confirm the exact input/output framing (space-separated on one line, one token per invocation) before wiring it into your pipeline — don't assume a format. Build `/home/user/etl.sh` in stages: first iterate `logs/*/events.tsv`, use `awk`/`cut` to skip the header and filter rows where `user_id` is empty or `metric_value` is empty/non-numeric (test with a regex like `^-?[0-9]+([.][0-9]+)?$`) or numerically `< 0`; then join surviving rows against `users.csv` on `user_id` (e.g. `join` after sorting both on the key, or an `awk` associative-array join loading users.csv first); then for each joined row pipe `user_id event_id` into the anonymizer binary to get `secure_token`, and append the final TSV row. Track a counter of valid rows for the run-summary log line with a real timestamp (`date '+%Y-%m-%d %H:%M:%S'`). For `serve.sh`, a hand-rolled `nc`-based HTTP loop is fragile for header parsing — prefer a small Python `http.server`/socket script that reads the request line and headers, checks the exact `Authorization: Bearer etl_secret_2024` header, parses the `region` query param, sums `metric_value` for matching rows from the final dataset, and returns proper `HTTP/1.1` status lines with `Content-Length` and a blank line before the JSON body. Start it in the background and test with `curl -i` for all four described scenarios (valid x2, missing auth, missing params) before considering it done.