This is a service-orchestration-plus-search task: get the infrastructure working before optimizing. First inspect `/app/services/api.py` to see how it constructs its Redis client (likely reading a `REDIS_URL` env var or defaulting to a specific host/port/db) — start `redis-server` locally, then either export the matching `REDIS_URL` env var when launching the Flask app or edit the connection line so it points at your running Redis instance, and launch both in the background (e.g. `nohup ... &`), confirming with a quick curl that `POST /score` responds. For primer selection, don't just pick the first valid substring — for each target, enumerate every contiguous 20-bp window within the allowed index ranges (0-39 for forward, 60-99 for reverse, remembering the reverse primer must be reverse-complemented before scoring/output), query the `/score` endpoint for each candidate, and keep the one with the lowest returned score per position type. Write `/home/user/design.py` to do this enumeration and querying automatically, run it, and confirm `/home/user/optimized_primers.csv` has exactly the three required columns and one row per target — the grader independently recomputes and sums the off-target scores, so correctness of the substring extraction and reverse-complement logic matters more than shortcuts.