Build this as a straightforward simulate -> analyze -> validate -> persist pipeline (Python with numpy/scipy/h5py is the natural fit). For each of the 1000 signals: draw `f0` uniformly in [50,150] Hz, build the time vector for 10s at 1000Hz (10000 samples), compute `sin(2*pi*f0*t) + N(0, 2.0)`, then take the FFT (`np.fft.rfft`) and corresponding frequency bins (`np.fft.rfftfreq(10000, d=1/1000)`), and pick `f_peak` as the frequency at the magnitude-argmax — make sure you use `rfft`/`rfftfreq` (or manually restrict to positive frequencies) since including negative-frequency mirror bins from a full FFT will corrupt the peak-picking. Accumulate all signals into one `(1000, 10000)` array and all peak frequencies into a `(1000,)` array, write them to `/home/user/benchmark_data.h5` as datasets named exactly `signals` and `peak_frequencies` via `h5py`. Compute MAE as `mean(abs(f0_array - peak_array))` across all 1000 trials, and write `MAE: <value>` (matching that literal format) to `/home/user/validation.log`. Sanity-check afterward by reloading the HDF5 file and recomputing the FFT peak for a couple of rows to confirm they match the stored `peak_frequencies`.