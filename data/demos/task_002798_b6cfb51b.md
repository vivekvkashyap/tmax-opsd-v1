Work through the four subtasks independently and verify each output file separately before moving on. (1) Use `find /home/user/audit_target -type f -perm -o+w` (world-writable test, not just `644`/`777` guessing) to list vulnerable files and redirect to the perms file. (2) Read `encrypt.sh` carefully: a hardcoded shift-based letter substitution is a classic Caesar cipher, i.e. a broken/risky crypto algorithm (look up its CWE), and the fact the shift amount is baked into the script is a hardcoded-credential/secret issue (a different CWE) — write both IDs sorted numerically ascending. (3) For the ciphertext, don't guess the shift — derive it by comparing the first few ciphertext letters against the known plaintext prefix `CONFIDENTIAL: ` letter-by-letter (mod 26 difference), then apply the inverse shift to the whole file with `tr` using the deduced rotation amount (colons, spaces, digits, punctuation are unaffected by a letter-only Caesar shift). (4) For redaction, use `grep -oE`/`sed`/Python regex with strict anchors: exactly 16 contiguous digits for card numbers, and `API_KEY=` followed by exactly 16 alphanumeric chars for keys — replace only the matched span, not the whole line, and double check word boundaries so you don't over- or under-match adjacent digits.
