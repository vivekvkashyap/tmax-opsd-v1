Write `health_monitor.cpp` using `<filesystem>` to iterate `std::filesystem::directory_iterator("/home/user/containers/")`, skipping any entry that isn't a directory or lacks a `status.txt` (check with `std::filesystem::exists`). For each valid container directory, read `status.txt` fully (trim trailing newline/whitespace before comparing) and check if it equals `"UNHEALTHY"`. If so: copy the backup file `/home/user/backups/<id>_data.bin` over the container's `data.bin` (use `std::filesystem::copy_file` with `overwrite_existing`, or read+rewrite manually), overwrite `status.txt` with `"HEALTHY"` (truncate first, no leftover bytes from the longer "UNHEALTHY" string), and append (open in `std::ios::app`) a line `RESTORED <id>\n` to `/home/user/health_monitor.log` where `<id>` is the directory's base filename (`entry.path().filename().string()`). Do not hardcode container names — derive everything from the directory listing so it generalizes to any set of containers. Compile exactly with `g++ -std=c++17 .../health_monitor.cpp -o .../health_monitor`, fix any compiler errors, then actually execute the resulting binary once (not just compile it) so the log and restored files are produced before you finish.