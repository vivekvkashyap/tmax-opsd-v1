For optimize.sql, target the actual access pattern: purchase documents are looked up by their JSON fields, so create a partial/expression index using SQLite's `json_extract(doc, '$.user_id')` and `json_extract(doc, '$.product_id')` (or the `->>'user_id'` shorthand), scoped with `WHERE json_extract(doc,'$.type')='purchase'` if using a partial index, so lookups don't need to deserialize every document. For pipeline.py: query all purchase-type documents, parse the JSON, and build a mapping of user_id -> set of product_ids purchased (a plain dict of sets works fine — you don't need a graph library, though networkx's bipartite tools are a fine alternative if you prefer). Then for every unique pair of users (iterate combinations, not all ordered pairs, to avoid duplicate/self work), compute `len(products[userA] & products[userB])` and keep the pair only if the intersection size is EXACTLY 3, not >=3 — this is easy to get wrong and silently include pairs that share 4+ products. Enforce UserA < UserB lexicographically when forming each pair, sort all qualifying pairs alphabetically, and write them one per line as `UserA,UserB` to matching_users.txt with no extra whitespace or trailing blank lines. Sanity-check by manually tracing through the document data which pairs should and shouldn't match before trusting your output.
