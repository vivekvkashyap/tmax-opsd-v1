Break this into independent, separately-verifiable steps. (1) Mount: `sudo mount -o loop /app/evidence.img /mnt/evidence` (or via `-t ext4`), then add a matching line to `/etc/fstab` (device, mountpoint `/mnt/evidence`, fs type `ext4`) so it persists; verify with `mount | grep evidence` and `cat /etc/fstab`. (2) Video: don't eyeball frames — use `ffmpeg` to extract frames (e.g. `ffmpeg -i network_status.mp4 -vf select='eq(...)' ...` or extract all frames as images and script a per-frame pixel check) and programmatically test each frame's dominant/full-frame color for exact RGB (255,0,0); count matches and write the integer to the drop_count file — do not hardcode a guessed number. (3) Classifier script: parse each log line, match status code `499` combined with source IP matching subnet `10.99.*.*`, count occurrences, and only emit `EVIL` at count >= 3 else `CLEAN`; make it robust to varied log line formats since it's tested against a hidden corpus. (4) Containerized nginx: build a self-signed cert with `openssl req -x509 -newkey rsa ...` into `/home/user/certs/`, write an nginx conf enabling TLS on 8443, create `index.html` containing `CRITICAL_ALERT: <your counted number>`, then run the container named `alert-web` with `-p 8443:8443` and confirm `curl -k https://localhost:8443/` returns it, keeping the container running.
