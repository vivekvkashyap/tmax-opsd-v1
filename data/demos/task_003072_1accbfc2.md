Treat `/app/rpn_oracle` purely as a black-box equivalence target: probe it with hand-picked RPN expressions (each operator, nested expressions, negative numbers, division truncation direction) via `./rpn_oracle "..."` and compare outputs — don't disassemble the stripped binary. Fix `/home/user/src/Makefile` first (common culprits: spaces instead of required tabs before recipe lines, or a missing `-o rpn_calc`) and confirm `make` builds cleanly. Then walk `rpn.c`'s stack-based evaluator token by token: check operator argument order for `-` and `/` (a common bug is computing `b op a` instead of `a op b`, only visible on non-commutative ops), confirm integer division truncates the same direction as the oracle (test negative operands, e.g. `-7 2 /`), and check stack push/pop indices for off-by-ones. Write `/home/user/test.sh` to generate several random valid RPN expressions (build bottom-up: start with a random integer, then repeatedly push a new integer or apply a random operator to the top two stack values so the expression stays balanced), run both binaries on each, diff stdout, print PASS/FAIL; make it executable and run it to confirm equivalence.