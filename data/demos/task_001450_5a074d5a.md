The key constraint is avoiding data leakage: any statistic used for imputation/capping must be computed from rows 1-800 (the training set) only, then applied to all 1000 rows. Implement this as inline Python inside the bash script for reliability rather than pure awk, e.g. `python3 - "$1" <<'PY' ... PY`, reading the CSV with the `csv` module. Steps: split rows into train (first 800 data rows after the header) and full set; compute the mean of non-missing `age` in train, floor it with `math.floor`, and use that integer to fill any empty `age` cells across all rows; compute the mean `income` in train, multiply by 2, floor it to get the cap, and clamp every row's `income` (train and test) to that cap using `min(income, cap)`; then emit only `id,target,age,income` columns in that order, preserving original row order, as CSV to stdout with a header line. Make the script executable with `chmod +x`. Before finishing, manually run it against the input and sanity-check a few rows (e.g. confirm no row's income exceeds the cap, and previously-missing ages are now the same integer).