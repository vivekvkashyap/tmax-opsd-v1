Since you must match a stripped binary bit-for-bit under heavy fuzzing, reverse-engineer by black-box probing rather than guessing: feed the oracle a range of crafted byte sequences (valid single frames, multiple frames concatenated, close frames, masked vs unmasked, truncated frames, garbage/random bytes, empty input) via stdin and record its stdout/exit code for each, using that to pin down exact framing rules before writing code. Implement the parser as a loop reading byte 0 (top bit = FIN, low nibble = opcode; only opcodes 1 and 8 are valid), byte 1 (top bit = MASK flag, low 7 bits = payload length, must be <=125), then conditionally 4 mask key bytes, then the payload bytes — unmask by XOR-ing each payload byte with `mask[i % 4]` if MASK was set. For opcode 1, print the (unmasked) payload followed by `\n`; for opcode 8, print `CLOSE\n` and exit 0; on any invalid opcode, oversized length, or truncated/unexpected-EOF mid-frame, print `ERROR\n` and exit 1; clean EOF between frames exits 0 with no error. Pay special attention to exact exit codes and newline placement, since fuzz equivalence checks stdout and exit status byte-for-byte. Test your C binary against the oracle locally with a wide set of inputs (including edge cases at length boundaries) before considering it done.