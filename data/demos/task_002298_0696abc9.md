Don't rely on mean/variance checks — the corrupted distribution is deliberately constructed to match the clean one's mean and variance, so simple moment comparisons will fail. Start by exploring: run the oracle several times with different seeds to build a baseline "clean" reference sample, then plot histograms/QQ-plots of the clean vs. evil training examples in `/app/training_data/` side by side to see where they actually differ (tails). Use a statistic that's sensitive to tail shape rather than just spread — e.g. a goodness-of-fit test like `scipy.stats.kstest(data, 'norm', args=(mean,std))`, or a discriminator based on excess kurtosis (heavier-tailed distributions score noticeably higher than a Gaussian's ~0). Pick a decision threshold by evaluating on the labeled `clean/` and `evil/` training folders until you get 100% separation there, then make sure the threshold is a general statistical cutoff and not overfit to those exact 40 files, since the real grading corpus is much larger and hidden. Have `detector.py` load `/sensor_data` via h5py, compute the statistic, and exit 0 (clean) or 1 (corrupted) accordingly.