Start by actually compiling the Rust project (`cargo build` inside `/home/user/rust_emulator`) and reading the compiler error rather than guessing — a type-mismatch/borrow error between `main.rs` and `interpreter.rs` is typically a function expecting `&str` being called with an owned `String` (or vice versa); fix it either by passing a borrowed reference (`&value`) at the call site or by adjusting the function signature to accept `String`, whichever keeps the rest of the code consistent. After it compiles, verify the TCP server on `127.0.0.1:9000` actually parses commands like `ADD 10 5` and returns the integer result terminated appropriately. For frame counting, don't guess — invoke `ffprobe -v error -count_frames -select_streams v:0 -show_entries stream=nb_read_frames -of csv=p=0 /app/demo.mp4` (or an equivalent ffmpeg approach) to get the exact frame count programmatically, and embed that computed value into the `/status` JSON dynamically rather than hardcoding a guessed number. For `/execute`, open a fresh TCP socket to port 9000 per request, write the command plus `\n`, read the full response line, parse it to int, and return `{"result": <int>}`. Wire both services together in `run.sh` (`cargo build --release`, background the Rust binary, then launch the Python server), and confirm both are actually listening before finishing.