Work through this pipeline in strict order, verifying each stage before the next. First, fix the vendored package: open `/app/labschema-1.2.0/Makefile` and look for a typo'd command invoking Python (a misspelled interpreter name) in the `install` target; correct it, then run `make install` and confirm `import labschema` succeeds in a Python shell before moving on. Second, load both datasets and validate each row/object using `labschema.validate_clinical`/`validate_biomarkers` inside try/except blocks, discarding (not crashing on) any row that raises `ValidationError` — some rows are deliberately mistyped to test this. Inner-join the surviving records on `patient_id`. Third, standardize the 10 biomarker columns (`sklearn.preprocessing.StandardScaler`), fit a `sklearn.decomposition.PCA(n_components=2)`, and compute `scipy.stats.pearsonr` between PC1 and `recovery_time`. Fourth, build a minimal Flask/FastAPI app bound strictly to `127.0.0.1:8080` with an auth check (compare the `Authorization` header against `Bearer LAB_SECURE_TOKEN_99`, returning 401 on mismatch/missing) guarding `GET /api/pca` and `GET /api/stats`, returning the computed values as JSON with the exact key names specified. Run it in the background (e.g. `nohup python3 app.py &`) and self-test with `curl` including a request without the header (expect 401) and one with it (expect 200 + correct JSON shape) before finishing.