Run `/home/user/project/test.sh` first to see the actual diff between output.txt and expected.txt rather than guessing at the bug. Then read `compute.c`'s discriminant calculation line by line against the real quadratic formula — the discriminant of ax^2+bx+c is `b*b - 4*a*c`; check the sign on the `4*a*c` term specifically, since a single sign flip there is a classic injected bug that still compiles and runs but produces wrong roots (or spurious NaNs) for some inputs. After correcting the formula, recompile and rerun `test.sh`, confirming it prints "BUILD SUCCESS" and exits 0, and that the regenerated `output.txt` diffs clean against `expected.txt`.
