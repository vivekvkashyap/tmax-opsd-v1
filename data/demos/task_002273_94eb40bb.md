Do this in two clearly separated passes so you can debug each independently. Pass 1: use `find /home/user/logs -name '*.log'` piped to a loop, and for each file run something like `awk '{ $1=""; $2=""; print }'` then clean up the resulting leading/collapsed whitespace with `sed 's/^  *//'` (awk's field removal leaves the original separators as extra spaces) — write to a temp file and move it back in place per file. Verify against one file by hand that the output exactly matches "field3 field4 ..." with no leading space before trusting it at scale. Pass 2: write `/home/user/compress.py` implementing run-length encoding via a regex like `re.sub(r'(.)\1{2,}', lambda m: f"~{len(m.group(0))}{m.group(1)}", content)` — note the threshold is 3+ (2 identical chars stay untouched), test it against a hand-traced example first (e.g. "AAAAA" -> "~5A", "!!" -> "!!") before running it over real files. Walk the tree, read each `.log`, transform, write to the matching `.clog` path, then delete the original `.log` only after the `.clog` write succeeds. Finally sum `os.path.getsize` over all `.clog` files recursively and write the integer to `/home/user/final_size.txt`.
