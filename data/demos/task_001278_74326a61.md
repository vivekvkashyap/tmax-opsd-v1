Read `src/main.rs` closely around the step-size update: standard embedded RK error control shrinks the step when the local error estimate exceeds tolerance and grows it when error is small, via `h_new = h_old * (TOL/err)^p` — check whether the current code has the ratio inverted (i.e. `err/tol` instead of `tol/err`), which would make step size explode exactly when error is large, causing divergence/NaN. Fix the ratio direction (keep the exponent and clamping bounds as-is unless they're also clearly wrong) and rebuild with `cargo build --release` or `cargo run`. Before trusting the output, sanity-check that decreasing error indeed shrinks the step in a couple of manual trace iterations. Run the corrected binary, capture the printed final-state vector at t=0.5 exactly as printed (space-separated, no extra text) into `/home/user/result.txt`, and re-run to confirm the simulation converges without NaN/panic.