Use pandas' CSV reader as-is (`pd.read_csv`, default engine) rather than manual line-splitting — it natively handles quoted fields with embedded newlines and Unicode, so a naive `open().readlines()` approach is the naive-attempt trap that silently drops/mangles rows. Parse `timestamp` with `pd.to_datetime`, and compute `msg_len` as `df['message'].str.strip().str.len()` (character count, not byte count — Unicode chars like emoji/Japanese must count as one character each via Python's str length semantics). To resample: `groupby('chat_room')` then `set_index('timestamp').resample('1H').sum()`, but you must first build the full hourly range from `floor(min_ts)` to `floor(max_ts)` and reindex each room's series onto it (filling missing sums with 0) so rooms with gaps aren't silently omitted from certain hours. Apply `.rolling(3, min_periods=1).sum()` per room on the hourly-filled series for the 3-hour trailing window. Finally pivot rooms into columns (sorted alphabetically) against the shared hourly timestamp index, fill any remaining NaN (e.g. a room that starts late) with `0.0`, format the index as `YYYY-MM-DD HH:00:00` strings named `timestamp`, and write with `to_csv`. Validate a couple of hand-computed cells against your own trace of the arithmetic before finishing.