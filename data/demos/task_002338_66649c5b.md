Work top-down: first OCR the image (`tesseract /app/config_spec.png -` or install tesseract-ocr if missing) to recover the Gateway HTTP port, Backend TCP port, and Modulus Constant — do not guess these, read them out of the image. Then inspect `/app/traffic.pcap` with `tcpdump -r`/`-X` or `scapy` to confirm the wire protocol is fixed 8-byte little-endian integers exchanged over a plain TCP connection (no framing/length prefix). Update `gateway.py` and `backend.py` to use the extracted port/modulus values instead of the placeholders. For the performance bug, run `strace -f -p <pid>` (or `strace python backend.py`) while it hangs on a request — you'll see it blocked in repeated single-byte `read()` calls on `/dev/random`, which blocks when entropy is depleted; the fix is to remove that bogus padding loop entirely (or switch to `/dev/urandom`, but the loop itself is dead weight and should just be deleted) since it has nothing to do with the actual `(val**2) % Modulus` computation. Start both services in the background with `nohup ... &` or similar, verify both the HTTP `/compute` endpoint and raw TCP protocol respond correctly and fast (<10ms) before finishing.