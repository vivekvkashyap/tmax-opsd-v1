Build the pipeline as chained awk passes over sorted input, and get the OCR salt first since masking depends on it. Run `tesseract /app/salt.png stdout` (or similar) and parse out the value following `MASK_SALT:` from the recognized text — treat OCR as inherently possibly-noisy, so trim whitespace and sanity-check the extracted token before using it. For the ETL: (1) sort input CSV rows (skip header) by timestamp numerically; (2) forward-fill missing `val` per device using an awk associative array keyed on device, defaulting to `0.0` if that device has no prior value yet; (3) bucket into `int(ts/3600)*3600` windows and track a running max per (window, device) pair; (4) for each (window, device, max) triple, concatenate `device + salt` (no trailing newline — use `printf` or `echo -n` before piping to `md5sum`), take the first 8 hex chars as the anonymized device id; (5) format each row as JSON with `max_val` to 1 decimal place via `printf "%.1f"`, and sort final lines by window ascending then anonymized device ascending before printing. Also write a valid 15-minute cron line (`*/15 * * * * /home/user/etl.sh < /data/in.csv > /data/out.jsonl`) to `/home/user/cron.txt`, and make `etl.sh` executable.