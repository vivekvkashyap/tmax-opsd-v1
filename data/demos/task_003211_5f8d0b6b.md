Work in three passes: correctness, precision, then verification. First write `FuzzParseLine(f *testing.F)` in `parser_test.go` seeding a few sample lines (well-formed, too-few-fields, non-numeric fields) and calling `ParseLine` inside `f.Fuzz(func(t *testing.T, line string) {...})`; run `go test -fuzz=Fuzz -fuzztime=10s` to let it find the crashing input (missing comma-separated parts or unparsable floats indexed directly without bounds/error checks). Fix `ParseLine` to split on commas, check the resulting slice has exactly 4 parts before indexing, and check every `strconv.ParseFloat` error, returning `(nil, err)` instead of panicking or silently discarding the error. Separately, change the `Point` struct fields and `Distance` to use `float64` end-to-end — the naive bug is parsing with `strconv.ParseFloat(str, 32)` or casting through `float32`, which truncates precision and can make two distinct high-precision coordinates collapse to the same value, causing distance to compute as zero. Keep all math in `float64` until output. Finally write `/home/user/verify.go` importing the package, parsing the two given lines, calling `Distance`, and writing the result via `fmt.Sprintf("%v", ...)` to `/home/user/distance_result.txt`. Confirm `go test` and `go test -fuzz=Fuzz -fuzztime=10s` both run without panicking before finishing.