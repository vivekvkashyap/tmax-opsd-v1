The core pitfall this task is testing is naive line-based CSV parsing (e.g. `readlines()` + split on comma), which breaks on embedded newlines inside quoted fields — always use Python's `csv` module (`csv.reader` on the opened file object) so RFC4180 quoting/embedded-newline handling is correct for free, and count `total_logical_rows` from the parsed rows (post-csv-parsing), not from counting `\n` characters. Filter rows to `level == "ERROR"`, then for each such row apply a regex like `ErrorCode:\s*([A-Z]{3}-\d{4})` against the full multiline message field, taking only the first match (or none). Build the hourly bucket key by truncating the ISO8601 timestamp to `YYYY-MM-DDTHH` (simple string slicing works since the format is fixed), and accumulate a nested dict of bucket -> code -> count, incrementing only when a code was actually extracted. Write that dict to `/home/user/output/hourly_errors.json` and write the three required integer counts to `/home/user/output/checkpoint.json` (creating `/home/user/output/` first). Run the script and confirm it exits 0 and both JSON files were written before finishing.
