Do the two subsystems independently, then combine. For the C tool: `cd /home/user/ml_data/stats_tool && make` — read the Makefile/stats.c first to confirm the target name and just build it, no need to modify source unless it fails to compile. For the generator: the root cause is that manual fixed-step RK4 is explicit and conditionally stable, and it blows up on a stiff system (μ=50 Van der Pol) at the given step size. Replace the hand-rolled RK4 loop with `scipy.integrate.solve_ivp`, passing the same derivative function, `t_span=(0,100)`, the same `t_eval = np.linspace(0,100,1000)`, the same initial state `[2.0, 0.0]`, and an implicit stiff method (`method='BDF'` or `method='Radau'`) so it can take large stable steps despite stiffness. Keep μ=50 unchanged. Write the resulting `y1,y2` columns to `dataset.csv` exactly as before. Then run `generate.py`, run `./calc_stats dataset.csv > stability_report.txt`, and sanity-check the CSV has no NaN/Inf and the reported max amplitude looks like a bounded limit-cycle value, not a divergent one.
