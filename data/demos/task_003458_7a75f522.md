Reproduce the crash first: run `cargo run -- data/sensors.log` (or `cargo build` then run the binary) inside `/home/user/telemetry_app` and read the panic message to confirm it's a parse failure on a non-numeric field. In `src/main.rs`, replace the `.unwrap()` on the parse call with a fallible pattern — `if let Ok(val) = parts[2].trim().parse::<f64>() { ... } else { continue; }` (or a `match`) — so malformed lines are skipped instead of panicking, rather than pre-filtering lines with shell tools, since the fix must live in the Rust logic itself. Separately, change the accumulator and parsed-value types from `f32` to `f64` throughout (sum, count-division, and the parse target type) to fix the precision loss — a common mistake is fixing only the parse type or only the sum type and leaving a lossy `f32` somewhere in between. Rebuild, run the binary against `/home/user/telemetry_app/data/sensors.log`, capture stdout exactly as printed (the `Average: <value>` line, no extra formatting) and redirect/write it verbatim to `/home/user/diagnostics.txt`.