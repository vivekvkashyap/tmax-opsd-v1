First recover the exact CRC-16 parameters from the image: run `tesseract /app/spec_fragment.png stdout` (or similar OCR invocation) and read the polynomial and XOR-out value directly from the extracted text rather than guessing common CRC-16 variants — the spec text will state them explicitly in hex. Implement a CRC-16 routine matching those exact parameters with no input/output reflection (bit-by-bit table-free implementation is easiest to get right: for each byte, XOR into the top of a 16-bit register and shift left 8 times, XOR-ing in the polynomial when the top bit is set, then XOR the final value with the XOR-out constant). In `/home/user/filter.py`, validate in order: (1) strict Base64 decode of the whole file content (reject any non-base64 characters, only trailing newline whitespace allowed); (2) after decoding, read the first 4 bytes as an unsigned little-endian uint32 length; (3) confirm the remaining decoded bytes' length exactly equals that value; (4) compute your CRC-16 over the payload bytes (excluding the 4-byte header and the trailing 2-byte CRC) and compare little-endian to the last 2 bytes. Exit 0 only if all checks pass, else exit 1, catching all exceptions as failures. Test against both `/app/corpus/clean/` and `/app/corpus/evil/` before finishing to confirm 100% accuracy.
