The core trap here is C's numeric parsers silently treating an empty field as 0 — avoid `atoi()`/plain `sscanf("%d", ...)` on the error_code column. Instead, split each CSV line into raw string fields yourself (e.g. with `strtok` or manual pointer scanning by comma), and for the error_code field explicitly check if the substring between commas is empty (zero-length) before converting; if empty, assign -1, otherwise use `strtol`/`atoi` on the non-empty string. Parse temperature with `strtod`/`atof`. Build per-sensor accumulators (sum, count) in a small array or a simple linked structure keyed by sensor_id, applying the outlier check (`temp < -50.0 || temp > 150.0`) and the valid-error-code check (`error_code == 0 || error_code == -1`) before including a row in aggregation — both filters must pass. After processing all rows, sort the distinct sensor_ids ascending (simple insertion sort or qsort is fine for small N) and print each with `printf("%d,%.2f\n", id, avg)` to get exactly two decimal places. Compile with `gcc -O2 -lm -o process_sensors process_sensors.c`, run it against `/home/user/sensors.csv`, and manually check a couple of rows' math by hand against `summary.csv` before considering it done.
