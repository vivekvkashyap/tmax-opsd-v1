Work this incident response as a strict pipeline where each stage's output feeds the next — don't skip straight to writing the decryptor with assumed values. First, transcribe `/app/voicemail.wav` (install `ffmpeg`/whisper-style tooling as needed) to recover the exact passphrase spoken; use it verbatim, don't paraphrase it. Second, find the rogue process via `ps aux` / `/proc/*/cmdline` grep for `rogue_worker.py` and read its `--salt` argument directly from the process listing — this is a live artifact, not something to fabricate. Third, write a Python script that opens `/home/user/evidence.enc`, splits the first 16 bytes as IV from the rest as ciphertext, derives a 32-byte key via `PBKDF2HMAC(SHA256, 100000 iterations, salt=<recovered salt>)` on the recovered passphrase, decrypts AES-256-CBC, strips PKCS7 padding, and parses the resulting JSON to get `auth_token` and `listen_port` — write that JSON to `/home/user/decrypted_evidence.json`. Finally, stand up an HTTP server bound to `127.0.0.1:<listen_port>` (from the JSON, not hardcoded) with a `POST /callback` handler that checks the `Authorization: Bearer <token>` header exactly, returns 401 on mismatch/missing, otherwise reads the raw request body, computes its SHA-256 hex digest, and responds 200 with `{"status": "received", "sha256": "<digest>"}`. Keep the server running in the background (e.g. `nohup ... &` or a detached process) so the verifier can reach it.