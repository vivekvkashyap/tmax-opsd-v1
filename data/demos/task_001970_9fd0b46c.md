Normalize both streams into a common intermediate record shape (timestamp, device_id, value, error_code) before applying any filtering — this avoids duplicating validation logic per source. For stream_alpha, parse `YYYY/MM/DD HH:MM:SS` as naive local time, treat it as UTC-4 (i.e., add 4 hours to get UTC — use `datetime` + `timedelta`, not a timezone library that might apply DST rules you don't want), then format as `...Z`. For stream_beta, `datetime.utcfromtimestamp(int(ts))` directly gives UTC. Apply the value-range and error-code==0 filters after normalization, on the unified records. For deduplication, compute the MD5 over the string `f"{device_id}_{round(value,1)}"` — be careful that Python's `round()` and your later float formatting agree on trailing-zero representation (e.g. round(0.0,1) prints "0.0", not "0"), since the hash input string must match exactly what's expected. Group by hash, and among duplicates keep the record with the lexicographically/chronologically earliest ISO timestamp (string comparison works since the format is zero-padded and UTC). Finally sort surviving records by (timestamp, device_id) and write one JSON object per line to `/home/user/output/clean_sensors.jsonl` with exactly the four required keys.
