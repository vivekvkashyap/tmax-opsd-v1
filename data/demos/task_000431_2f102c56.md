Build this as a pure Unix-pipeline chain, verifying each intermediate stage by eyeballing it before piping into the next: (1) skip the header and dedupe `user_id,product_id` pairs with `tail -n +2 purchases.csv | cut -d, -f1,2 | sort -u`; (2) self-join this list on product_id (sort by product, `join` on the product field, or an `awk` script that groups by product and emits all user pairs within each product group) to get every co-occurring user pair per shared product; (3) normalize each pair so the lexicographically smaller user_id is always first (compare strings, swap if needed) so `(u1,u2)` and `(u2,u1)` collapse to one canonical form, and dedupe per-product-per-pair so a user pair sharing one product only counts once even if awk emits it twice; (4) count DISTINCT products per (source,target) pair via `sort | uniq -c` grouped by the pair, keeping the pair only if the count is `> 1`; (5) sort the surviving pairs by source then target, and feed them into `jq -R -n '[inputs | split(",") | {source:.[0], target:.[1], weight:(.[2]|tonumber)}]'` or build the array incrementally with `jq -s` to get default pretty-printed indentation. Test intermediate `awk`/`sort` output against the manually-traceable small dataset before trusting the final jq formatting.