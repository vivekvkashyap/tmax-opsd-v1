Build this as a straightforward streaming/sorting pipeline in C++, not a naive nested-loop dedup. Read every CSV row from all input files into one vector of structs (parse timestamp_ms as a 64-bit integer), then sort the whole set by timestamp_ms ascending before doing anything else — dedup and the 5s-window rule only make sense on chronologically ordered data. Walk the sorted list keeping a map from `(user_id, action)` to the timestamp of the last *kept* record in that group; for each record, if a kept record with the same key exists within 5000ms (inclusive, i.e. `diff <= 5000`), drop it, otherwise keep it and update the map to this record's timestamp. Track `total_input_count` and `dropped_count` to compute the drop rate; compare with `>` (strictly greater than 0.20) against `2` for exit code, matching the spec's "strictly greater than 20%" wording exactly. Mask `user_id` with `"MASKED_" + last 3 chars` using substring from the end, and divide timestamp by 1000 (integer division) for `timestamp_sec`. Parse CLI args by treating everything before `-o` as input files and the argument after `-o` as the output path. Write the bash wrapper and cron line last, then run the pipeline once manually to produce the required output/log files before finishing.
