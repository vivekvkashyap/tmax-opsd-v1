Build and unit-test the validator in isolation first, since it will be fuzzed against a corpus directly. Implement `is_clean` to: json.loads the string in a try/except (any parse failure or non-dict top-level returns False); check `set(payload.keys()) == {"device_id", "readings"}` exactly (extra or missing keys fail); validate device_id with a regex like `^[A-Za-z0-9-]+$` (reject empty string too); validate readings is a dict where every value's type is `int` or `float` but NOT `bool` (Python bools are ints — explicitly exclude `isinstance(v, bool)`) and reject NaN/Infinity if json permits them (Python's json module accepts `NaN`, `Infinity` as valid floats by default, so explicitly check `math.isfinite(v)`). Then build proxy.py as a simple `http.server` handler that reads the POST body, calls `is_clean`, and on True forwards the exact bytes via `urllib.request`/`requests` to `http://127.0.0.1:5000/telemetry` and relays the response verbatim with 200, otherwise responds 403 immediately without forwarding — test this proxy directly with curl on 8081 before touching nginx. Edit nginx.conf's `/telemetry` location to `proxy_pass http://127.0.0.1:8081;` and reload (`nginx -s reload` or restart using the config path, as your own user, not root). Last, `git init` the waf dir, add a `.git/hooks/post-commit` script that kills any existing proxy.py process and relaunches it backgrounded, then commit to trigger it and confirm the proxy is alive afterward.