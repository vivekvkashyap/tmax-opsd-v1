Start by transcribing `/app/audio/rule.wav` with an available tool (`whisper-cli`, or an ffmpeg+Python speech pipeline) and read the transcript carefully — it specifies an additional forbidden condition (a byte sequence and/or a path pattern) beyond the SYMLINK/HARDLINK rule already given in the task text; do not guess this condition, derive it from the audio. Write the C sanitizer to read all of stdin into a buffer (don't assume text/line-based input, since one hidden rule is about raw bytes), then apply each rule independently: reject SYMLINK/HARDLINK directives whose target path is not under `/backup/config/` (parse the directive's target token, compare prefix), reject if the buffer contains the byte sequence discovered from the audio anywhere (memmem-style scan, not a fixed offset), and reject any other condition from the transcript. Return 0 only if no rule is violated, non-zero otherwise. Before finishing, compile and iterate against the provided `/app/corpus/clean/` and `/app/corpus/evil/` directories, checking exit codes for every file — since grading uses a separate hidden corpus, make each rule check generic/robust rather than tuned to just the sample files.