For step 1, parse `gateway.log` with `jq`: extract `auth_header`, strip the `Bearer ` prefix, split the JWT on `.` and take the first (header) segment, base64url-decode it (pad it back out since JWT base64url strips `=` padding — you may need to append `=` characters until the length is a multiple of 4, and translate `-`/`_` back to `+`/`/` before decoding, or use `base64 -d` with URL-safe handling), then check the decoded JSON's `alg` field case-insensitively equals "none", and only keep IPs where `status == 200`. Collect unique matching IPs, sort them, and emit the exact `{"blocked_ips": [...]}` structure with `jq -n` or a heredoc, in `/home/user/block_ips.sh`. For step 3, build the JWT forwards instead of decoding: construct the header JSON `{"alg":"none","typ":"JWT"}` and payload JSON `{"user":"<username>","role":"admin"}` as literal strings (no spaces, keys in that exact order), base64url-encode each with `base64 | tr '+/' '-_' | tr -d '='`, and join as `header.payload.` (note the trailing dot with empty signature) — print only this string, and make sure the username is inserted verbatim without re-encoding issues. Chmod both scripts executable.