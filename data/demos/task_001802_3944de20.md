Work this pipeline stage by stage and verify each before moving on. First extract frames from the mp4 (e.g. with `ffmpeg` or Go's image libraries) and confirm you can see a bright blob against black. Write a Go program that spawns a goroutine per frame (with a WaitGroup and a mutex or channel to collect results in order) to threshold the image and compute the centroid as the intensity-weighted mean of pixel coordinates — this gives you a trajectory X_0..X_n. For fitting A in X_{t+1}=A X_t, stack the data into an overdetermined system and solve it via QR decomposition using `gonum.org/v1/gonum/mat` (e.g. `mat.QR` then solving the least-squares system) rather than forming X^T X directly, since squaring the design matrix amplifies conditioning issues. Initialize the Go module (`go mod init`, `go get gonum.org/v1/gonum`) before building. For the server, bind exactly to `127.0.0.1:8080`, implement `/matrix` returning the four matrix entries as JSON, and `/predict?t=N` by repeatedly applying A to X_0 (matrix power via t multiplications), returning the resulting x,y. Test each endpoint locally with curl before leaving the server running in the background.
