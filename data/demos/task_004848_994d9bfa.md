Diagnose the failure first: inspect `/app/edge-telemetry/start.sh` closely, line by line — the bug is a subtle typo in an environment variable name (an extra/wrong letter) that's set on one line but referenced under a different, correct-looking name on the next, so `$HOST` silently expands to empty and the server binds to the wrong interface. Fix the exact variable name with `sed -i` (don't just rewrite the whole file blindly) so the assigned and referenced names match and the exported value is `0.0.0.0`. Then `mkdir -p /home/user/logs`, run the fixed `start.sh` so the daemon backgrounds itself with output redirected into `/home/user/logs/telemetry.log`, and verify manually with `curl http://127.0.0.1:8080/health` and by sending `PING\n` to port 8081 with `nc`/`ncat` before moving on — the task requires the daemon actually listening on both ports, not just the script exiting cleanly. Wrap the fix-and-start logic into `/home/user/deploy.sh`. Finally write the logrotate conf with `daily`, `rotate 7`, `compress`, `missingok`, `notifempty` for `/home/user/logs/telemetry.log`, and sanity-check it with `logrotate -d <conf>`.
