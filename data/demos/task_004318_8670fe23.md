Work through the three failure points independently rather than guessing at a global fix. First try `make` and read the linker error carefully: a missing `pow()` symbol from `<math.h>` usage means the Makefile's link rule needs `-lm` appended, and check the target/executable name matches `fast-det` exactly. Second, compile `src/determinant.c` directly with gcc to see the implicit-declaration warning/error for `pow`; add `#include <math.h>` to fix it — do not touch the recursive algorithm itself. Third, inspect `tests/test_runner.sh`: a common bug is piping the CSV into a `while read` loop, which runs in a subshell so counter variables (PASSED/FAILED/TOTAL) never propagate back to the parent shell — use process substitution (`< <(...)`) or a `for` loop over lines instead. Also verify the CSV field splitting (`cut`/`awk` with comma delimiter) correctly isolates the space-separated matrix elements before passing them as separate positional args to `./fast-det <size> <elements...>`. After each fix, rerun the full pipeline end-to-end and confirm `/home/user/pr_summary.json` reflects actual pass/fail counts, not just zeros.