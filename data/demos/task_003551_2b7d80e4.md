This combines OCR extraction, binary reverse-engineering, and pcap parsing — do each step in isolation and validate before combining. (1) OCR the key from `/app/intercepted_key.png` with `tesseract`; sanity-check that the extracted string is exactly 16 alphanumeric characters, re-crop/reprocess the image if OCR output looks garbled or has stray characters. (2) Reverse `/app/auth_client` with `strings`, `objdump -d`, or a disassembler/decompiler to determine the wire format — the description already tells you it's XOR-with-key plus a binary layout, so focus disassembly effort on confirming field order and length-prefix sizes (e.g. 2-byte length fields, whether they're big or little endian) rather than rediscovering XOR from scratch. (3) Use the local mock service on port 9090 as a live test oracle: connect to it, capture a request/response, and check that decoding your captured bytes with the extracted key and inferred format yields a sane username/token before running against the full pcap. (4) Write decode_traffic.py to parse the pcap (e.g. with `scapy` or `dpkt`), filter TCP payloads to port 9090, apply your decode logic, and emit the JSON list with 1-based packet_index. Test against `/app/traffic.pcap` first and manually check a few decoded entries for plausibility before it's run against the larger hidden eval.pcap.
