First handle encoding conversion in isolation: use `iconv -f ISO-8859-1 -t UTF-8 raw_sales.csv -o utf8_sales.csv`, then verify with `file -i` that it reports utf-8 — a no-op conversion would silently fail the verifier. Then write the C program: read utf8_sales.csv line by line skipping the header, parse the 4 comma-separated fields per row (StoreID, Date, SalesAmount as double, Currency — consume but discard currency so parsing doesn't break on the special-character bytes), storing records in a dynamic array. Sort all records by StoreID then by Date using qsort with a comparator that compares StoreID then Date strings lexicographically (YYYY-MM-DD sorts correctly as a string), using original index as a tiebreaker for equal dates so relative order is preserved (stable sort). Within each StoreID group, walk chronologically maintaining a sliding window of up to the last 3 SalesAmounts (current + up to 2 immediately preceding) to compute the rolling average, printing StoreID,Date,SalesAmount,RollingAvg with `%.2f` for both amounts. Compile with `gcc -o process_sales process_sales.c` and run it.