Treat this as black-box reverse engineering plus one hidden audio clue, not pure guessing. First, script dozens of calls to the legacy binary with varied paths (`/api/...`, `/graphql...`, `/secure/...`, `/public/...`, `/admin/...`, and unusual ones like paths ending `.php`, missing query strings, malformed query pairs) and diff the JSON outputs to infer the general parsing rule: how it splits path vs query, how it builds the params dict from `urllib.parse.parse_qsl`-style parsing, and what the default status/authorization logic looks like for ordinary paths. Do this systematically before touching the audio. Then transcribe `/app/dev_notes.wav` (e.g. with `ffmpeg`/`whisper` or any local speech-to-text tool) carefully — the note almost certainly describes an edge-case constraint (a numeric condition on a specific query parameter under a specific path prefix, plus a special-case extension rule) that black-box probing alone won't surface exhaustively; feed candidate constraints back into the oracle by testing values that straddle the boundary (e.g. values just inside/outside a modulus condition) to confirm exactly how it's enforced. Only after both baseline behavior and the audio-derived constraint are verified against real oracle outputs should you encode the logic in `/home/user/new_router.py`, matching JSON key order/format exactly.