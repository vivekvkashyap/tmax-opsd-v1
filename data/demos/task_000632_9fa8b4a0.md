Diagnose by reading the Go source first, not by guessing — `daemon.go` encodes every requirement (env vars, PIN, config schema) explicitly, so grep it for `os.Getenv` and any hardcoded comparison strings/PINs rather than trying arbitrary values. Fix `config.json` to be valid JSON with a numeric (or string-coercible) port field of `8080`, matching whatever struct field the Go code unmarshals into — check the exact JSON key name the code expects. For the expect script: set `TZ` and `LANG` as environment variables (via `set env(...)` in Tcl/expect or exporting before invoking `spawn`) to exactly the values the Go source checks for, `spawn go run /home/user/service/daemon.go`, `expect` the known prompt text, `send` the PIN literal you found in the source followed by a newline, then `expect eof` (or a timeout matching the ~2s sleep) so the script blocks until the process exits rather than killing it prematurely. For logrotate, write a stanza referencing the exact log path with `daily`, `rotate 3`, and `create` directives. Run the expect script yourself afterward and check `daemon.log` for the success line before declaring done.