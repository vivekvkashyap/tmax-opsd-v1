Extract frames first with `ffmpeg -i /app/data_stream.mp4 frame_%04d.png` (or similar) into a working directory, keeping track of the 1-based frame number encoded in each filename — that number is what must end up in `index.json`, not any renumbering you do later. For brightness, convert each frame to grayscale and compute the mean pixel value (OpenCV: `cv2.cvtColor(img, cv2.COLOR_BGR2GRAY).mean()`, or ffmpeg's own grayscale stats) and copy only frames with mean strictly greater than 128 into `/home/user/staging/`, preserving each frame's original frame number in its filename so the watcher/indexer can recover it later. Implement the watcher (a simple polling loop is fine — inotify-based or a `while true; sleep` loop checking directory contents) that batches newly-arrived staging files into groups of exactly 5 in arrival/frame order, tars each group with `tar -czf` into `/home/user/repository/bundle_N.tar.gz` sequentially numbered from 0, and records each bundle's 5 (or fewer, for the last) original frame numbers into `/home/user/repository/index.json`. Since scoring is F1 over reported frame numbers, be precise about the >128 boundary (strict inequality) and double check you're not off-by-one on frame indexing (first extracted frame should be labeled 1, not 0), since small indexing mistakes tank F1 significantly.
