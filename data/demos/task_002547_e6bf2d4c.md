Get the join right before touching the C++/CV logic. Inner-join `features.csv` and `targets.csv` on `id` (drop ids present in only one file), sort numerically ascending by id (not lexically), and write `joined.csv` with no header. In the C++ program, split the sorted rows sequentially into 5 equal folds (fold i = rows [i*20%, (i+1)*20%)). For each fold as validation, the critical correctness point is to compute mean/stddev of f1,f2,f3 using *only* the 4 training folds, then apply those same fixed statistics to normalize both training and validation data — normalizing before splitting, or recomputing stats on the validation set, is a leak and will fail. On the normalized training data, compute Pearson correlation of each feature against target to form a 3-element weight vector, predict validation targets via dot product, and compute that fold's MSE. Average MSE across the 5 folds and format to exactly 4 decimal places when writing `/home/user/final_mse.txt`. Implement the CV loop, correlation, and stats with plain loops/`std::vector` — no external linear-algebra libraries — and compile with `g++ -O3 pipeline.cpp -o pipeline` before running.
