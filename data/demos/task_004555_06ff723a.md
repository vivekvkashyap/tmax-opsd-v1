Treat this as ETL-then-patch-then-serve. First convert `manifests.jsonl` to `processed_graph.csv`: parse each JSON line, map `_id`→`backup_id`, `metadata.parent`→`parent_backup_id` (write an empty string, not the literal "null", when the parent is JSON `null`), `metadata.size`→`size_bytes`, `state`→`status`, and write the exact header given. Then read `/app/backup-lineage-server/server.py` carefully to find the two bugs described: a hardcoded `DATA_FILE` path that must instead resolve your CSV (either hardcode your CSV's path or, better, read `os.environ.get('DATA_PATH', ...)` and set that env var when launching), and a loop that does `if not p_id: continue` before adding nodes — fix it so `G.add_node(b_id)` always runs even when `p_id` is empty, only skipping the `add_edge` call in that case, so root backups still appear as graph nodes. After patching, launch the server as a background process bound to `127.0.0.1:9090` and verify with `curl` against `/lineage/<id>` for a root, a mid-chain, and a deep node before finishing — note the existing code returns *all* ancestors via `nx.descendants`, so check the ordering requirement matches what the endpoint currently produces and adjust if the verifier expects a specific chronological order.
