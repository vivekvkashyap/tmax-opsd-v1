Work through the four subsystems in dependency order, verifying each with a direct probe before moving on. (1) Auth service: open `/app/compliance_system/auth/main.go`, find the `/login` handler's open-redirect call, and add a check on the `redirect_to` value — if it starts with `http://`, `https://`, or `//`, force the redirect target to `/dashboard`, otherwise pass relative paths through unchanged; `go build -o auth-svc` and run it in the background, then sanity-check with a local curl before moving on. (2) Certificates: build the full chain by concatenating leaf then intermediate PEM content into `server.crt` (`cat leaf.crt intermediate.crt > server.crt`) — order matters, leaf must come first. (3) Report binary: since you don't have source, use `strings /app/compliance_system/report/report-bin | grep -E '127\.0\.0\.1|[0-9]{4,5}'` to find the hardcoded port, then launch it in the background and confirm it's listening with `ss`/`curl` on that port. (4) Nginx: edit `nginx.conf` to `listen 127.0.0.1:8443 ssl;` with the new cert/key, and add `location /login { proxy_pass http://127.0.0.1:8081; }` and `location /audit { proxy_pass http://127.0.0.1:<discovered_port>; }`, then start nginx with `-c` pointing at that config file. Leave all three processes running in the background and verify end-to-end with `openssl s_client` and `curl -k` before finishing.
