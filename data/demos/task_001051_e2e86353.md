Structure `process_logs.sh` as: (1) a per-file worker function/subshell that reads one CSV, interpolates missing timestamps, and filters rows, writing its result to a temp file; (2) a dispatcher that launches one worker per `.csv` in `/home/user/raw_logs/` in the background (`&`) and `wait`s for all of them, or uses `xargs -P`; (3) a final step that concatenates all temp outputs and sorts numerically by timestamp (`sort -t, -k1,1n`) into `/home/user/critical_events.csv`. For the imputation step, process each file sequentially within its own worker: track the previous row's timestamp as you read line by line (e.g. with `awk` maintaining a `prev` variable, or a bash loop with array indices), and when you hit `MISSING`, look ahead to the next line's timestamp, average with `prev` using integer arithmetic (`$(( (a+b)/2 ))`, which truncates), and substitute it back into that row before continuing. For the Unicode filtering, be careful with byte-vs-character matching — grep/awk must operate on UTF-8-aware locale (set `LC_ALL=en_US.UTF-8` or `C.UTF-8`) or use `grep -P` with literal UTF-8 bytes for the emoji and the Japanese characters, since ASCII locale matching can silently fail. Keep a row if SEVERITY is exactly `CRITICAL` OR the message contains the emoji OR contains the Japanese failure term — a plain substring/regex OR condition. Remember `chmod +x` the script and actually execute it to produce the output file.