Start by reading the source file to locate the hot loop rather than guessing. In C++, a `std::vector<std::vector<double>>` stores each row contiguously, so the loop that walks memory linearly should have the row index as the OUTER loop and the column index as the INNER loop (`for i { for j { ...data[i][j]... } }`); walking column-major (outer index selecting the column, inner index sweeping rows) forces a new cache line load on almost every access and tanks performance. Diagnose which loop currently has the indices swapped, then flip only the loop order — do not touch the accumulation logic, the bounds, or introduce restructuring beyond nesting order, since the task requires identical arithmetic with a different traversal order. After editing, compile exactly as instructed with `g++ -O3 -o /home/user/mc_sim /home/user/mc_sim.cpp`, run the resulting binary, and redirect its real stdout into `/home/user/report.txt` (e.g. `./mc_sim > /home/user/report.txt`) rather than typing a number by hand — the value must come from actually executing the fixed program, since hardcoding it would not reflect a genuine fix and could mismatch if any detail was misread.
