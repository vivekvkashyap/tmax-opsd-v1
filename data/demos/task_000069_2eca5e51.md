For Part 1, open `/app/experiment.mp4` with `cv2.VideoCapture`, read every frame, and classify each as Success or Failure by checking whether the frame is (near-)uniformly white or black — e.g. compare the mean pixel value against a midpoint threshold, or check all pixels equal 255 vs 0 — then tally counts and write `/home/user/video_results.json` with the exact `{"successes": ..., "failures": ...}` structure. For Part 2, write `artifact_filter.cpp` using a JSON library (e.g. vendor/pull in `nlohmann/json` or write a minimal strict parser) that: rejects malformed/missing-field JSON; checks `successes`/`failures` are non-negative integers and `prior_alpha`/`prior_beta` are strictly positive; rejects NaN/Infinity in the posterior fields (`std::isnan`/`std::isinf`); and independently recomputes `alpha_post = prior_alpha + successes`, `beta_post = prior_beta + failures`, then the mean and variance formulas given in the task, comparing each to the file's reported values with `std::abs(computed - reported) <= 1e-4`. Exit 0 only if every check passes, else exit 1. Compile with a C++ standard supporting `<cmath>` NaN checks, then self-test by looping your binary over `/app/corpus/clean/` (expect all exit 0) and `/app/corpus/evil/` (expect all exit 1) before considering it done.
