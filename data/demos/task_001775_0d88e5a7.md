The key constraint is never writing archive contents to disk (no `tar -xf`), because paths inside may contain `../` traversal. Instead, stream the archive: use `tar tzf prints.tar.gz` (or a library's streaming member iterator, e.g. Python's `tarfile.open(mode='r:gz')` iterating `tar.getmembers()` and reading each member via `tar.extractfile(member)` into memory) to enumerate entries without ever calling `extract()`/`extractall()`. For each member whose name ends in `.gcode`, read its content as an in-memory stream/bytes, split into lines, and keep only lines that start exactly with `M109` — strip trailing `\r` but preserve all other spacing exactly. For the basename, strip all leading directory components (including any `../`) from the member's path — use something like `os.path.basename()` rather than manual string splitting, since traversal paths may have unusual segments. Write `<basename>: <matched_line>` per match to `/home/user/temperatures.log`, one per line. Since ordering across files/lines may not be guaranteed by iteration order, consider sorting your output for stable, reproducible results.
