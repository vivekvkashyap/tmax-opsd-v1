Reverse-engineer `/app/jacobian_oracle` empirically before writing any server code: run it with a couple of trial `<x> <y>` float argument pairs (e.g. via `strace`/`ltrace` if needed, or just trial-and-error with a few coordinate values) and observe stdout to infer that it takes two positional float args and prints 9 space-separated floats in row-major order forming a 3x3 matrix — confirm the pattern holds across several different (x,y) inputs (e.g. check known special cases like x=y=0 should look like an easily-recognizable matrix) rather than assuming a formula. Once the CLI contract and output layout are confirmed, implement the matrix math yourself in C++: power iteration for the dominant eigenvalue (repeatedly multiply a random vector by the matrix, normalize, track the Rayleigh quotient until convergence) and a Gram-Schmidt or Householder QR decomposition to get R's diagonal. Build the TCP server with raw POSIX sockets (bind/listen/accept on 127.0.0.1:9090), parsing newline-terminated `EIGEN x y` / `QR_R x y` commands per connection and replying with newline-terminated results within the stated tolerance. For the CSV, loop x and y from 0.0 to 1.0 in 0.1 steps (11x11=121 rows), calling the oracle (via subprocess, matching the exact CLI form you reverse-engineered) for each grid point, computing the eigenvalue, and writing `x,y,eigenvalue` rows. Leave the server running in the background at the end.