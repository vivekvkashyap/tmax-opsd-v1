Work top-down: get the crate graph compiling first, then fill in the handler, then the property test, then run the suite. `cargo build` will surface the tonic/prost/tonic-build conflict — resolve it by pinning all three to a mutually compatible release line (check crates.io/tonic's changelog for which prost minor version each tonic minor requires) rather than arbitrarily bumping just one; re-run `cargo build` until it's clean. In `src/rest.rs`, implement `relay_handler` as an Axum handler taking `Path<String>` for `:id`, `HeaderMap`, and the raw `Bytes` body; parse `X-Checksum` as a `u32`, compute `crc32fast::Hasher::new()` over the body bytes, and compare — return `(StatusCode::OK, ...)` on match (calling the provided `forward_to_grpc` stub) and `StatusCode::BAD_REQUEST` when the header is missing, unparsable, or mismatched. Wire the route into an Axum `Router` with `.route("/api/v1/relay/:id", post(relay_handler))`. For `src/security_tests.rs`, use `proptest!` to generate arbitrary `String` payloads, compute their real CRC32 with the same `crc32fast` crate to build valid checksums (and mutate/increment for invalid ones), then assert your handler's checksum-validation logic accepts the valid case and rejects the invalid one. Finally run `cargo test > /home/user/test_results.log 2>&1` and inspect the log for `test result: ok` before finishing.