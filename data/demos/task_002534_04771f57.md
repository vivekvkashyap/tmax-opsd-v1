Solve the three subsystems independently, then compose. For video OCR: extract one frame per second with `ffmpeg -i /app/backup_run.mp4 -vf fps=1 frame_%03d.png`, run `tesseract` on each, and record which whole-second timestamp(s) contain the text "CORRUPT" — these integer seconds become your `job_id`s, so don't hardcode them, derive them from OCR output. For the SQL: avoid a cross join by writing a query that starts from `nodes` filtered to the corrupted ids, joins to `edges` on `source_id`, and joins the result to `nodes` again on `target_id` (a single-hop join, not a full Cartesian product) — save this to `/home/user/optimized_graph.sql` and test it directly against `/home/user/backups.db` with `sqlite3` before wiring it into the server. For the HTTP layer, since the task requires a Bash server using `nc`/`socat`, write a loop that listens on 127.0.0.1:9090, parses the request line for `GET /impacted_backups`, runs your saved SQL query via `sqlite3 -json` or manual formatting, and writes back proper `HTTP/1.1 200 OK` headers plus the JSON body matching the `{"corrupted_jobs": [...], "impacted_downstream_jobs": [...]}` schema — test with `curl` yourself before backgrounding it (`nohup ... &`) so it stays alive for the automated verifier.