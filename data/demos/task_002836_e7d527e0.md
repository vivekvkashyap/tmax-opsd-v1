Split this into a standalone security filter and a routing fix, and verify each independently. For the C++ sanitizer, read the whole input file and branch on content type: if it looks like a unified diff (has `---`/`+++` headers), scan those header lines for `../` sequences or absolute paths under sensitive prefixes like `/etc/` and reject (exit 1) if found; if it looks like assembly, scan for the specific dangerous patterns named in the spec (`execve`, the `mov eax, 59` + `syscall` combo, `int 0x80`) using substring/regex search — treat these as string patterns you look up in the file text, not something you need to disassemble. Default to exit 0 (accept) when no rejection criteria match, and validate against the sample corpus paths conceptually before assuming correctness. For nginx, the config template is missing `proxy_pass` directives — add `location /api/patch { proxy_pass http://127.0.0.1:9001; }` and the analogous block routing `/api/asm` to port 9002, keeping the existing `listen 8080` server block. Compile the sanitizer, then run `/app/start_services.sh` and confirm both dummy backends respond through nginx before finishing.
