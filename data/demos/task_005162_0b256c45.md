Structure `/home/user/disk_analyzer.py` as: (1) scan `/home/user/reports/*.json`, sum `storage_used_mb` per `project_id` into a dict of new-run totals; (2) open `/home/user/project_totals.csv` for read+write (mode like `'r+'`, creating it if absent), immediately call `fcntl.flock(f, fcntl.LOCK_EX)` before any read or write and keep the lock held across both the read-existing and write-updated steps so no concurrent process can interleave — release via `fcntl.flock(f, fcntl.LOCK_UN)` or simply let it release when the file closes; parse existing `project_id,total_mb` rows into a dict, add the new-run totals on top (summing per id, not overwriting), then truncate and rewrite the file sorted by `project_id` ascending; (3) after computing the final merged totals, write `/home/user/binary_dump.bin` using `struct.pack('<IQ', project_id, total_mb)` per project in the same sorted order, concatenating without any separator. Double check off-by-one issues: the CSV values already present in the file must be added to, not replaced by, the new report totals. Run the script once at the end and verify the CSV and binary file contents look right before finishing.