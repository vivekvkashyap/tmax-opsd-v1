Start by actually opening `nodes.json` and `edges.json` and inspecting field names — don't assume a schema; confirm exact keys like id/type/relation and how "Financial_Records"/"Audit_Logs" are identified (by a name field on System nodes, not by ID string matching). The core algorithmic point: access is transitive through MANAGES edges, so for each employee you must compute the full set of reachable HAS_ACCESS targets by traversing the manager→managee chain (DFS/BFS from each employee over MANAGES edges, unioning in each visited node's direct HAS_ACCESS edges) — a naive check of only direct edges will miss violators. Build this in Rust: parse JSON with serde/serde_json into structs, build adjacency maps (managee lookups and access lookups) for O(1) traversal, then for every employee do a graph traversal to collect all reachable system IDs and flag any employee whose reachable set contains both the Financial_Records and Audit_Logs system identifiers. Sort violator IDs alphabetically and write one per line. Separately, hand-write the equivalent Cypher (a variable-length MANAGES* traversal combined with HAS_ACCESS, grouped/filtered to employees touching both target systems) to `query.cypher`.
