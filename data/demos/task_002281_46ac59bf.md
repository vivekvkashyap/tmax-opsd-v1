Build this as a straight-line ETL in C++: (1) read both CSVs into one vector of records with a small hand-rolled CSV line splitter; (2) normalize `lang` by splitting on whatever separator is present (`_` or `-`), lowercasing the first token, uppercasing the second, and re-joining with `-` — do this before anything else so dedup/grouping keys are consistent even though inputs mix `En_us`, `fr_FR`, `es_es` styles; (3) deduplicate with a `map<pair<msg_id,lang>, Record>` (or a string key `msg_id+"|"+lang`), keeping the record with the larger `timestamp` on each insert; (4) group the survivors by normalized `lang`, sort each group by `timestamp` ascending; (5) compute the rolling average with a small sliding window (deque or index math) over the last up-to-3 scores per group, not per whole dataset — the window resets per language group; (6) write output rows sorted by `lang` alphabetically then `timestamp` ascending, formatting `rolling_avg_score` with `std::fixed << std::setprecision(2)`. Watch two common traps: normalizing case only for one half of the code (`ll-CC`), and computing the rolling window across the globally-sorted list instead of per-language groups. Create the output directory before writing.
