Two independent subtasks — do both. (1) Fix the vendored package: inspect `/app/sql-graph-proj-1.0.0/setup.py` for an invalid dependency declaration (something that can't actually be pip-installed, e.g. a stdlib module listed as a version-pinned requirement) and remove/correct it so `pip install -e /app/sql-graph-proj-1.0.0` succeeds; then grep the package source (look in `compiler.py` or similar) for a hardcoded join-type string that's wrong (it emits a Cartesian-product-style join keyword where it should emit a proper explicit join) and fix that string so generated SQL uses a real inner join. Verify by actually importing the package and calling its query-building function to confirm the output SQL contains a proper join. (2) Write `/home/user/sql_filter.py` as a CLI that takes a `.sql` file path, reads it, and decides accept/reject: treat any query with an explicit `JOIN ... ON` (including `INNER JOIN`) as safe (exit 0); treat `CROSS JOIN` or a comma-separated `FROM a, b, ...` list that lacks any `WHERE` clause as an implicit/explicit cross join and reject (exit 1). Use case-insensitive regex/token matching robust to whitespace and line breaks, not naive substring search. Test against every file in `/app/corpus/clean/` and `/app/corpus/evil/` yourself to confirm 100% correct classification before finishing.
