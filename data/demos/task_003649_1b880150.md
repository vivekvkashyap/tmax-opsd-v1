Reverse-engineer the artifact format empirically using the provided corpora and the (unmodifiable) legacy extractor as an oracle, rather than guessing blindly. Run `/app/bin_extractor` against sample files in `/home/user/corpus/clean/` and `/home/user/corpus/evil/` to see what it outputs, and use a hex dump (`xxd`/`od`) of a few small sample bundles to identify structural patterns: look for a fixed-size magic/header prefix, then repeated chunk records, each likely carrying some kind of length field, a type marker, and payload bytes — compare several sample files to confirm which fields are constant vs. variable. Pay attention to endianness (try little-endian first) and figure out which type marker corresponds to macro payloads versus plain data by comparing hex dumps of clean vs evil files (evil files should have chunks whose payload doesn't look like other, non-obfuscated data). For chunks that look like scrambled/non-printable text, try simple reversible transforms (single-byte XOR against a few candidate keys) until the decoded bytes look like readable macro/script text — cross-check by scanning candidate decoded strings against the forbidden patterns listed in `/home/user/policy.ini`. Once you can reliably parse chunks and decode macro payloads for both corpora, implement this exact parsing/decoding logic in `/home/user/filter.py`, load and apply the policy rules from `policy.ini`, and exit 0/1 accordingly. Validate against 100% of both corpora before considering it done, since the hidden test set uses the same format.