Use Eigen (`#include <Eigen/Dense>`, headers at /usr/include/eigen3, compile with `-I/usr/include/eigen3`) and structure the program in clear stages: (1) parse data.csv treating cells literally equal to "NaN" as missing — accumulate per-column sums and valid counts while parsing, so you can compute each column's mean over only its non-missing entries; (2) do a second pass filling missing cells with their column's mean, building a dense NxM Eigen matrix; (3) compute population covariance by centering the imputed matrix (subtract column means from every entry) then computing `(Xc.transpose() * Xc) / N` — critically use N in the denominator, not N-1, since sample covariance would give a different, wrong answer; (4) parse weights.csv into a column vector and compute predictions as `X_imputed * W`. When writing CSVs, use consistent default double formatting (Eigen's IOFormat or plain `<<` with default precision) and avoid extra trailing spaces or altered delimiters. Sanity-check a couple of your own computed values against a hand calculation (e.g. verify one column's mean or one covariance entry arithmetically) before trusting the full pipeline, since verification does exact numeric comparison with a small tolerance.