Run `make` first to see the actual compiler errors rather than guessing — since `-Wall -Wextra -Werror` is in effect and you must not touch the Makefile, missing standard headers (e.g. `<stdio.h>`, `<stdlib.h>`) in the file that uses `exit`/`printf`-family calls will show up as implicit-declaration errors; add the needed `#include`s to the `.c`/`.h` files only. For memory safety, add explicit bounds checks in the push/pop primitives: before pushing, check `sp >= MAX_STACK` and print exactly `Error: Stack Overflow` then `exit(1)`; before popping (including inside ADD, which pops twice, and PRINT, which pops once), check `sp <= 0` (and for ADD, that at least 2 items exist) and print exactly `Error: Stack Underflow` then `exit(1)` — do this at the point of the operation, not just inside a generic pop helper, so ADD's second pop is also guarded. For `test_runner.py`, glob `tests/*.asm`, derive the matching `.expected` file, run `./simple_vm tests/<name>.asm` via `subprocess.run` capturing stdout and returncode, compare stdout exactly (including trailing newline handling) and infer expected exit code from whether the `.expected` content matches an error message pattern, then write pass/fail per file into `report.json` keyed by filename.
