Work through this as three sequential unlocks. First, brute-force the 3-digit numeric password on `users.db.enc`: loop `for p in $(seq -w 0 999)` trying `openssl enc -d -aes-256-cbc -pbkdf2 -in users.db.enc -pass pass:$p` and check whether the output decrypts to readable text (non-error exit code, or grep for an expected pattern like `admin_username=`) — this recovers the real admin username, don't assume or hardcode it. Second, read `auth_cli.cpp` closely to find the signature-bypass flaw: look at how it splits the token on `.` into header/payload/signature and what condition it actually treats as "valid" — many toy JWT validators like this accept an `alg` field claiming no signature is required (an "alg none" style bypass) and skip real signature verification entirely. Third, write `exploit.cpp` that base64url-encodes a header JSON asserting that alg and a payload JSON setting `user` to the username you decrypted and `role` to admin, joins them with `.` and an empty/placeholder signature, and prints the resulting token (or writes it directly) so you can invoke `/home/user/audit_target/auth_cli --verify <token>`. Capture its stdout into `/home/user/flag.txt` with nothing else in the file.
