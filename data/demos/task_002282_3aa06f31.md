Debug this like a real crash triage: first run `./start.sh` briefly and capture the traceback aggregator.py prints to see exactly which exception fires (UnicodeDecodeError vs ValueError from math.sqrt) rather than guessing. For the encoding bug, don't assume which sensor is at fault — make the decode itself resilient: try `data.decode('utf-8')`, and on `UnicodeDecodeError` fall back to `data.decode('utf-16le')` (or try both and keep whichever produces valid JSON via a try/except around `json.loads`). For the crash on `math domain error`, add an explicit filter right after parsing the JSON: drop (skip, don't append to the rolling window) any record whose `value` is `< 0.0` or `> 100.0`, before it ever enters the stddev calculation — filtering there also avoids the negative-variance edge case in the naive sum-of-squares formula. Only edit `aggregator.py`; leave the three sensor scripts untouched. After fixing, rerun `./start.sh` for at least 5-10 seconds and confirm `output/metrics.log` accumulates multiple lines without the process dying (check exit status/timeout code, not just "it printed something").
