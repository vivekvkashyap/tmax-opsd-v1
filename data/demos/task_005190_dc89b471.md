First fix the Makefile: BSD/macOS `sed -i '' 's/.../.../g' file` has an extra empty-string argument for the backup suffix that GNU sed on Linux interprets as the script itself, causing it to fail — change it to plain `sed -i 's/.../.../g' file`, then run `make setup` to produce the test ELF binaries. For the extraction script, parse `readelf -W -S <file>` (the `-W`/wide flag is essential — without it, readelf wraps each section's fields across two lines on narrow terminals, which breaks naive single-line awk/column parsing and is a common bug here). Use awk to match section-header rows whose Name column starts with `.cfg_`, and read the Size and Offset columns (both printed in hex by readelf, no `0x` prefix) directly from that line. Convert Size hex→decimal (e.g. `strtonum("0x"..)` in awk or `printf %d`), and format Offset as a lowercase `0x`-prefixed hex string. Emit lines in the exact order sections appear in readelf's output, with the precise `WAL_RECORD: name=... size=... offset=...` format and nothing else on stdout. Test against a self-made ELF with `objcopy --add-section` before trusting it on the fuzzed inputs.
