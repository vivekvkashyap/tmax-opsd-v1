First extract the actual spoken specification from `/app/voicemail.wav` — use a speech-to-text tool available in your environment (check for `whisper`/`speech_recognition`/similar; install one if needed) and transcribe the audio fully before writing any code, since the exact windowing size, rounding rule, and the data-cleaning edge case are stated only in the recording and must not be guessed. Pay special attention to any instruction about how to handle malformed or embedded characters within a CSV field (e.g., literal newlines inside a quoted column) — that edge case is easy to miss with a naive line-by-line CSV reader, so use Python's `csv` module (which correctly handles quoted multi-line fields) rather than splitting on `\n` yourself, and detect the described corruption condition before parsing further. Implement `/home/user/pipeline.py` to read CSV from stdin, apply exactly the aggregation window and rounding described in the recording, and write a JSON array to stdout with compact/consistent formatting. Since correctness is judged by byte-exact match against a hidden reference on thousands of fuzzed inputs, test your script yourself against varied synthetic CSVs (including rows with embedded newlines, boundary timestamps at window edges, and single vs. multiple rows per window) to shake out formatting and rounding discrepancies before submitting.