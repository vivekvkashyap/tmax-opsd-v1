Work in this order: fix and start infra, then build and validate the classifier against both corpora before trusting your own thresholding logic. First diff `/app/start_services.sh` against the required bindings (Redis on 127.0.0.1:6379 — check for a wrong port like 6380 — Flask needs `REDIS_HOST=127.0.0.1` and port 5000, Express on 3000) and fix only the misconfigured lines; confirm each service is actually listening (`curl`/`redis-cli ping`) before writing Rust. In your `sequence_filter` Cargo project, parse each `.fasta`, build a 64x64 matrix of adjacent-3-mer transition counts, fetch `substitution_weights` from Redis and repeatedly matrix-multiply until the max cell delta between iterations drops below `1e-4` (implement this as an explicit convergence loop, not a fixed iteration count, since the pass/fail threshold depends on where it settles). Sum the diagonal for the trace, GET `/baseline_score` from the Flask service, and classify "evil" only if trace > 1.5x baseline. Copy clean-classified files to the output dir and POST `{"filename":...}` to `/flag` for evil ones — never copy evil files. Before finishing, manually run your binary against both `/app/corpora/clean/` and `/app/corpora/evil/` and inspect the actual output directory contents and Express logs to confirm 100% separation, logging counts to `/home/user/verification.log`.