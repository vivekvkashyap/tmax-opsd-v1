First recover the hidden constraints: transcribe `/app/lab_notes.wav` with a speech-to-text tool (e.g. `whisper`, or any ASR available) and read off the Tm range and the max homopolymer run length spoken there; combine that with the GC-content bound given explicitly in the task (40-60%). Do not guess these numbers from the corpus alone — derive them from the audio. Then write `primer_filter.cpp`: parse the FASTA by skipping the `>` header line and stripping whitespace/newlines from the sequence body, compute GC% = (G+C)/length*100, compute Wallace Tm = 2*(A+T) + 4*(G+C), and scan for the longest run of identical consecutive bases. Reject (exit 1) if Tm is outside the transcribed range, if GC% is outside 40-60, or if homopolymer length exceeds the transcribed max; otherwise accept (exit 0). Validate iteratively against both `/app/corpus/clean/` (must all exit 0) and `/app/corpus/evil/` (must all exit 1) — use a shell loop over each directory checking `$?` to catch off-by-one boundary errors (inclusive vs exclusive comparisons) early. Only after the filter is 100% correct on both corpora, run it over the clean corpus to collect accepted GC% values and bucket them into bins to draw an SVG histogram (rectangles scaled by bin counts) at `/home/user/gc_density.svg`.