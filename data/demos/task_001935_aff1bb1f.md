Write the C parser as a byte-scanner over the whole file buffer: at each offset, check for the two magic bytes `0xBE 0xEF`; when found, read the next 2 bytes as a little-endian `uint16_t` length `L`, bounds-check that `L` bytes plus a checksum byte remain in the buffer (and that `L <= 1024`), then XOR-fold all `L` payload bytes and compare against the checksum byte immediately after the payload — only treat it as a valid frame if the checksum matches, otherwise advance just one byte and keep scanning (don't skip the whole claimed frame length on a checksum mismatch, since it may just be a false-positive magic-byte match in garbage data). For redaction, after extracting a valid payload, search for the literal substring `API_KEY=` and check that the following 16 characters are all alphanumeric before overwriting them in your output buffer with 16 `*` characters — leave everything else in the payload untouched. Print each payload followed by `\n`, avoiding a double newline if the payload itself ends in `\n`. After compiling and running to produce `/home/user/recovered_logs.txt`, generate the hash with `sha256sum /home/user/recovered_logs.txt > /home/user/recovered_logs.sha256` so the path inside matches exactly.
