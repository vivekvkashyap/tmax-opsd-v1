Structure the Python pipeline as clear sequential stages so each is independently verifiable: load both CSVs (pandas or csv module), inner-join on `doc_id`, then enforce schema by coercing `doc_id` to int inside a try/except (dropping rows that fail) and filtering `category` against the exact allowed set. Tokenize by lowercasing, stripping non-alphanumeric/non-space characters with a regex substitution, splitting on spaces, and discarding empty tokens — do this only after schema filtering, and make sure you're tokenizing the joined, cleaned rows, not the raw file. For the top-10 vocabulary, count tokens across all valid documents combined and sort by `(-count, token)` so ties break alphabetically; build each document's TF vector against that fixed top-10 ordering, then compute the L2 norm with `math.sqrt(sum(x**2 for x in vec))` and round to 4 decimals — be careful that Python's rounding matches standard rounding for the `.5` cases the checker expects. Sort final records by `doc_id` ascending and write one JSON object per line with exactly the required keys to `/home/user/output/processed_data.jsonl`, creating the output directory first if missing.
