Run `tesseract /app/params.png stdout` (or similar) to OCR the frequency and decay values out of the image rather than guessing them; parse the two numbers from the recognized text programmatically. For the C fix, the root cause is a `#pragma omp critical` reduction inside the parallel loop, whose accumulation order varies run-to-run because thread scheduling is nondeterministic — do not just add more locking. Instead, have each thread write `fabs(signal[i])` into a per-index array (or just reuse the signal buffer) during the parallel region, then after the `#pragma omp parallel for` loop finishes, sum the array sequentially in a plain `for` loop from i=0 to N-1; this fixed traversal order makes the float sum bit-identical every run. Compile with `-fopenmp`, plus HDF5 and math libs (`-lhdf5 -lm`), producing `/home/user/run_sim`. Run it with the OCR'd F0 and DECAY to produce `signal.h5`. Write a small Python script (h5py + numpy) to load the `signal` dataset, run `np.fft.rfft`, compute frequency bins with `np.fft.rfftfreq(10000, d=0.001)`, and pick the frequency at the index of maximum magnitude (excluding the DC bin at index 0). Finally serve both computed values as JSON from a lightweight HTTP server bound to 0.0.0.0:8080 at GET /api/report, keeping it running in the foreground.