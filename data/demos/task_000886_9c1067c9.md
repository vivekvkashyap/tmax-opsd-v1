First recover the configuration values from the image rather than assuming defaults: run `tesseract /app/config.png stdout` (installing tesseract if missing) and parse the resulting text for the HTTP port, TCP port, and secret token — treat these as unknowns to extract, and sanity-check the OCR output since it can misread characters (compare against expected key=value line shapes). Once you have the three values, build the Python gateway with two concurrent listeners bound to 127.0.0.1: an HTTP handler (http.server or asyncio) that checks the `Authorization: Bearer <token>` header exactly, returning 200 with `{"status":"ok"}` on match and 401 otherwise, and a raw TCP server that reads lines and echoes back `ACK: <line>` while keeping the connection open across multiple lines until the client disconnects. Run both listeners in the same process (e.g. via threads or asyncio tasks) so one script serves both ports. For supervision, write a bash loop that execs the Python script, redirects both stdout and stderr to the log file, and restarts it immediately in a `while true; do ...; done` loop whenever it exits. Launch the supervisor in the background (nohup/disown) so ports stay listening for the verifier, and manually curl/nc-test both endpoints before finishing.