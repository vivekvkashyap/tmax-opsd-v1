Build and test the matcher standalone before wiring the pipeline. In C++, implement standard Levenshtein distance with a 2D (or rolling 1D) DP table; normalize names by trimming whitespace (don't just strip leading/trailing spaces on the ends — the task's data has embedded multi-space runs that must be preserved, only leading/trailing whitespace is stripped) and lowercasing via `std::tolower` per character. For each input row, loop over every master-list line, compute the distance, and track the minimum with strict `<` comparison (not `<=`) so ties keep the first-seen master entry. Write output rows manually with `<<` using exact comma separators, no extra spaces, matching `ID,NormalizedName,BestMatchName,Distance`. Compile with `g++ -O2 -o matcher matcher.cpp` and test it directly on the sample CSV before writing `pipeline.sh`. The pipeline script is just `cp`, run `./matcher`, then `cp` again — keep paths absolute so it runs correctly from cron. For the cron line, use standard 5-field syntax for "top of every hour" (minute field `0`, all others `*`) pointing at the pipeline script's absolute path, written into `crontab_entry.txt` without actually installing it via `crontab -e`.
