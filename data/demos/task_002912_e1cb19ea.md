Work in two clearly separated phases. First write the SPARQL query in `/home/user/query.rq` mirroring the triple pattern described: `?x` follows the target, `?y` follows the target, `?x` knows `?y` — keep the predicate URIs exactly as they appear in the N-Triples file (e.g. `<follows>`, `<knows>`) rather than guessing a namespaced form. Second, write the Go ETL: parse `graph.nt` with a simple N-Triples line parser (split on the first two whitespace runs, keep the object token as-is including brackets, strip the trailing ` .`), bulk-insert into a SQLite `triples(subject, predicate, object)` table inside a single transaction for speed, then create indexes that make the SPARQL join efficient — since the query filters by `predicate` and joins on `object`/`subject`, add composite indexes like `(predicate, object)` and `(predicate, subject)` before running the query, not after. Translate the SPARQL into the equivalent SQL self-join/join across two `follows` matches and one `knows` match, strip `<`/`>` from URIs on output, and sort rows by `user1` then `user2` before writing the CSV with `encoding/csv`. Close/exec `db.Close()` so the file isn't left locked.