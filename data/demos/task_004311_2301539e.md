Do not modify `/home/user/mock_server.py` — just launch it in the background (`python3 /home/user/mock_server.py &`) and pip install `websockets` and `packaging` before writing the client. In `ci_monitor.py`, connect via `websockets.connect('ws://localhost:8765')` and implement a proper accumulation buffer: append each incoming fragment to a running string buffer, and after each append attempt to extract complete JSON objects (e.g. track brace-depth or repeatedly try `json.loads` on progressively longer substrings / split on newlines once a full line has arrived) — don't assume each `recv()` call yields one JSON object, since messages are deliberately fragmented mid-object. Stop consuming when you receive the literal string `"EOF"`. For each parsed `{pkg, old, new}` object, compare semantic versions (the `packaging.version.Version` class or manual major.minor.patch tuple comparison works) applying the policy: downgrade -> REJECTED, major version increase -> REJECTED, everything else (including pre-release-to-release of the same major.minor.patch) -> ACCEPTED. Accumulate results into a dict keyed by `pkg` and write it as pretty JSON to `/home/user/ci_report.json`, then exit cleanly.