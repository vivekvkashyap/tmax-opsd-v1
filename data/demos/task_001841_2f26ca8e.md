Treat this as two separate parsing problems joined by a shared narrative, not one blended algorithm. First, write a small script to parse `service.log` line by line with a regex matching `[TIMESTAMP] [THREAD_ID] [LEVEL] Message`; look specifically for the WAIT-type log lines near the end of the file (just before the watchdog/SIGKILL entry) that describe two threads each waiting on a resource the other holds — that mutual-wait pattern identifies the deadlocked pair, not simply the two most frequently mentioned threads. Second, parse `transactions.wal` independently: split each line on `:`, keep only lines with exactly 4 fields where the 4th field matches `^[A-Za-z0-9]{8}$` (reject the truncated/malformed final line). From the valid entries compute `END_TIMESTAMP - START_TIMESTAMP` per transaction, take the median duration, and flag any entry whose duration is at least 10x that median as anomalous — do not just pick the max value blindly, verify it actually clears the 10x-median bar. Separately, the last-recovered transaction is simply the highest TXN_ID among valid (non-corrupted) entries, which may not be the same as the anomalous one. Write all four keys to `/home/user/incident_report.json` exactly as specified.