Start by getting the vendored library to compile: inspect `/app/vendored/tinyhttp/Makefile` and `tinyhttp.c` for the build failure — run `make` there first to see the actual error rather than guessing, since a -Werror build turning a warning (like an unused variable) into a hard failure is a common vendoring gotcha; fix it minimally (either strip `-Werror` from CFLAGS or remove/use the offending unused variable) without changing the library's actual behavior, then confirm `libtinyhttp.a`/`.so` builds cleanly. Once the library builds, write config_server.c against its header, using `regcomp`/`regexec` from `regex.h` to extract txn_id and bytes from lines like `[TXN:<id>] SET <key>=<value> SIZE=<bytes>` — build the regex once, reuse it per request. Maintain a simple in-memory set of seen txn_ids (array + linear scan is fine at this scale) so repeated `/config` POSTs with an already-seen txn_id are silently ignored (no state change), while first-time txn_ids push their `<bytes>` value into a fixed-size circular buffer of length 5 (overwrite the oldest entry once full). For `/stats`, average only the currently-populated slots (fewer than 5 early on) and print `Average: %.2f\n` with a trailing newline exactly. Compile linking your library and pthread if needed, then background the service and curl-test both endpoints, including the dedup and rolling-window-overwrite behavior, before finishing.