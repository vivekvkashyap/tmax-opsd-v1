Build and test each of the three scripts independently before assuming they work together. For the forwarder, use `socket` to bind/listen on `127.0.0.1:9090`, and on each accepted connection open a new socket to `127.0.0.1:8080`, then relay bytes both directions — since true bidirectional forwarding of arbitrary-duration streams needs concurrency, use two threads (or `select`) per connection so data can flow client->backend and backend->client simultaneously, then append `FORWARD_SUCCESS\n` to `/home/user/logs/traffic.log` right after accepting the connection (not after it closes). Test it manually with a dummy backend socket server and `nc` before trusting it. For the rotator, use `os.path.getsize` to check `> 1024` bytes, then `os.replace`/`shutil.move` `traffic.log` to `traffic.log.1` (overwriting is automatic with `os.replace`), and recreate an empty `traffic.log` — test by writing a file of exactly 1025 and exactly 1024 bytes to confirm the boundary behaves per spec ("strictly greater"). For the quota monitor, walk `/home/user/logs/` recursively summing `os.path.getsize` for every file, and write the result in the exact literal format `TOTAL_BYTES: <n>` with no extra text. Create `/home/user/logs/` up front, and keep all three scripts plain, non-daemonized, and runnable via `python3 <script>.py`.
