Work in order: first grep/inspect `/app/nginx/access.log` for the anomalous request (look for URL-decoded script injection or a suspicious external host/query string) and extract the numeric key embedded in that request's query parameter — decode any URL-encoding first. Then reverse-engineer `/app/locker`: since it's stripped, use `objdump -d`/`Ghidra`/`radare2` or simply `strace`/`ltrace` while running it on sample input to observe its I/O pattern (reads a key arg, an input path, an output path, XORs each byte against a running PRNG state). Identify the PRNG update formula (look for multiply-add-mask constants in the disassembly — a classic LCG has the form `state = state * A + C` masked to some bit width, then XORs the low byte with each plaintext byte) and note it's a stream cipher, so XOR-decryption is self-inverse if you replicate the same key schedule. Write `/home/user/decrypt.c` re-implementing that exact same state update and XOR loop (reusing the recovered key), compile with `gcc`, then run it over every `.enc` file in `/app/evidence/`, stripping the `.enc` suffix, into `/home/user/recovered_files/`. Validate one file's plausibility (e.g. `file`/`strings` output looks sane) before batch-processing the rest.