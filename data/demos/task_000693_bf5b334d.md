Follow a measure-then-optimize workflow: first `cd /home/user/sim`, run `bash extract.sh`, then `make` and time the baseline `./mesh_sim` (or profile with `perf`/`gprof`) to confirm the O(N^3) Gaussian elimination in `solver.c` dominates runtime — do not touch `main.c`'s tracking/matrix-setup logic. Since the matrix A is SPD and diagonally dominant, replace the naive elimination with Cholesky decomposition: factor A = L·L^T (only need to compute/store the lower triangle, roughly halving work and improving cache locality vs. full Gaussian elimination), then solve via forward substitution (L y = b) followed by back substitution (L^T x = y), instead of doing full row reduction with pivoting on the whole augmented matrix. Keep the solver's function signature/interface identical so `main.c` doesn't need changes. After rewriting `solver.c`, recompile with `make`, rerun `./mesh_sim`, and diff/compute MSE between the new `forces.txt` and the original baseline output (regenerate a baseline copy before editing, since you'll need it for comparison) to confirm numerical equivalence within 1e-4, then re-time to confirm the required speedup.