Write the pipeline as three clear phases so bugs are easy to isolate. First, glob all files in `/home/user/raw_configs/`, load each JSON, and flatten nested dicts programmatically (e.g. a recursive helper that joins keys with `_`) rather than hardcoding field names, so it generalizes to the given schema; when flattening, convert `is_active`'s string `"true"`/`"false"` to int 1/0 explicitly (don't rely on Python truthiness of the string, since a non-empty string like `"false"` is truthy). Second, create the SQLite table with the exact column names/types specified, then bulk-insert with `executemany` for efficiency and correctness of typing. Third, compute the summary with a single SQL query — `SELECT server_id, COUNT(*), MAX(hardware_ram_gb), SUM(software_is_active) FROM server_configs GROUP BY server_id ORDER BY server_id` — which naturally sorts alphabetically and gives you all three aggregates in one pass. Write results to CSV using Python's `csv` module with the exact header row specified, one row per server, ordered by the SQL query's ORDER BY. Run the script and inspect both the DB row count and the CSV content afterward to confirm nothing was dropped or duplicated.