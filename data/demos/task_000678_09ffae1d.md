Do the analysis in stages using standard text tools: first parse `/home/user/groups.db` (format `GroupName:User1,User2,...`) to build a user-to-group mapping, then for each row in `net_usage.csv` sum `Bytes_Transmitted + Bytes_Received` per user and roll those up by group (awk with an associative array keyed on group, or a two-pass shell script) — double check you're matching users exactly, not substrings. Identify the max-bandwidth group and compute 80% of its total (integer or exact arithmetic as the numbers dictate) — this is the value you'll feed into later steps, derive it, don't assume it. For the interactive tool, write an Expect script that spawns `/home/user/quota_mgr`, uses `expect "Enter group to restrict:"` then `send` the group name plus `\r`, then `expect "Enter new bandwidth limit:"` then `send` the computed quota plus `\r`, followed by `interact` or `expect eof` to let it finish. For the idempotent config script, don't just append blindly — for each of the two `KEY=VALUE` lines, check with `grep -q "^KEY=VALUE$" file` first and only append if absent (or use `sed` to replace an existing differing `KEY=` line rather than duplicating it); create the file if missing. Run both scripts and verify by re-running the config script a second time to confirm no duplicate lines appear.