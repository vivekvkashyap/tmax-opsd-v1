Write `/home/user/organize.sh` as a generic loop over the config file: read it line by line with a `while IFS='|' read -r source target_dir target_name target_format is_latest; do ... done < file`, skipping blank lines and lines starting with `#`. For each entry, `mkdir -p "$target_dir"` first. Determine conversion need by comparing the source file's extension to `target_format`: if source is `.json` and target is `csv`, use `jq` to flatten the JSON array of objects into CSV — e.g. extract the header from the keys of the first object, then `jq -r '(.[0] | keys_unsorted), (.[] | [.[]]) | @csv'` (or equivalent) piped to the destination file. If the source already matches the target format, do NOT copy — create a hard link with `ln -f "$source" "$target_dir/$target_name"` (the `-f` ensures overwrite if it already exists). If `is_latest` is `true`, manage the symlink idempotently: use `ln -sfn "$target_dir" /home/user/clean_data/latest_dataset` (the `-n` flag is essential so an existing symlink is replaced rather than nested inside as a new entry). Make the script executable and actually run it, then spot-check with `stat -c %i` on both hardlinked files and `readlink` on the symlink to confirm correctness.