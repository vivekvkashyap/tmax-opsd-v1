Fix the import-order bug first: in `test_suite.py`, reorder the top-level imports so `import config` executes before `import ffi_wrapper` — Python runs module-level code (including `config.init()`, which sets the env var `ffi_wrapper` checks at import time) in import order, so simply swapping the two `import` lines resolves the unsafe-default-buffer issue. For the C memory-safety bug, look at `parse_token` in `parser.c`: it copies `data_len` bytes into a fixed 64-byte stack buffer without ever checking `data_len` against 64, causing a stack buffer overflow for oversized declared lengths — add an explicit bounds check (e.g. `if (data_len > 64) return -1;`) before the `memcpy` into `internal_buffer`, so overflow attempts are rejected with the documented -1 status rather than corrupting the stack. Recompile with the given `gcc -shared -o libparser.so -fPIC parser.c` command, then run `python3 test_suite.py` and confirm `/home/user/parsed_tokens.json` is produced with valid entries for well-formed payloads and an `{"error": "Invalid token"}` entry for the malformed/oversized one.