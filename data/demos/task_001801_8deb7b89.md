Treat `/app/interactive_transcriber` as a black-box binary you can only drive via stdin/stdout, since it's precompiled — don't try to modify or relink it. First run it manually to observe the exact prompt strings and interaction order, then write `/home/user/automate.exp` using `spawn`, `expect "Enter path to audio file: "`, `send "/app/voicemail.wav\r"`, `expect "Enter output text file path: "`, `send "/home/user/transcript.txt\r"`, and `interact`/`expect eof` to let it finish; run the expect script and confirm `/home/user/transcript.txt` is actually populated with a transcript before moving on — this is the piece the accuracy check depends on. Separately, write `server.c` using OpenSSL's low-level API (`SSL_CTX_new`, a TCP `socket`/`bind`/`listen`/`accept` loop, `SSL_accept`, then read the HTTP request and write back a minimal `HTTP/1.1 200 OK` response with the transcript file's contents as the body) — generate the self-signed cert/key first with `openssl req -x509 -newkey rsa:2048 -keyout key.pem -out cert.pem -days 365 -nodes`, load them via `SSL_CTX_use_certificate_file`/`use_PrivateKey_file`, compile with `-lssl -lcrypto`, and launch the compiled server in the background (`&`) bound to port 8443 so it stays up for the curl-based check.