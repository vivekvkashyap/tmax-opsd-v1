Work through the three subtasks independently and verify each before combining into the final report. (1) Integrity: compute `sha256sum /home/user/audit/server_config.tar.gz` and compare against the value in the `.sha256` file — write `INTEGRITY_OK`/`INTEGRITY_FAILED` accordingly rather than assuming it passes. (2) Port scan: iterate candidate ports 8080-8090 and test each with something like `nc -z localhost <port>` or a Python socket connect, then write whichever one actually accepts a connection as `SERVER_PORT=<port>` — don't guess a fixed value. (3) Traffic analysis: parse each log line with a regex to pull out the IP and the `redirect`/`token` query params (URL-decode if needed), then in your C program recompute the expected token yourself by implementing the described algorithm exactly (sum of ASCII values of the *redirect URL string* mod 256, XOR 0x42, formatted as 2-digit lowercase hex) and compare it to the token in the log. Flag a line as exploited only when the redirect starts with `http://`/`https://` AND the recomputed token matches the log's token; append `EXPLOIT_IP=<ip>` per match, in the order found. Double-check off-by-one hex formatting (leading zero for values < 16).