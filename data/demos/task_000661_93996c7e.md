Process the three files in a fixed alphabetical order (glob and sort, e.g. `for f in $(ls /home/user/legacy_configs/*.cfg.gz | sort)`). For each file, pipe `zcat "$f"` into `iconv -f UTF-16LE -t UTF-8` to decompress-and-transcode in one stream without writing intermediate files to disk. From the converted UTF-8 text, grep/awk for the line beginning with `DB_PASSWORD=` and extract everything after the `=`. Derive the node name from the filename by stripping the `.cfg.gz` suffix (e.g. with `basename "$f" .cfg.gz`). Format each result as `<node_name>: <password>` and append (`>>`) to /home/user/parsed_secrets.txt, being careful that iconv doesn't leave a stray byte-order mark or trailing null/newline artifacts in the value — trim whitespace/carriage returns from the extracted password before formatting. After processing all three, run `file /home/user/parsed_secrets.txt` or check with `iconv -f UTF-8 -t UTF-8 file >/dev/null` to confirm the final log is valid UTF-8 with no leftover UTF-16 bytes.
