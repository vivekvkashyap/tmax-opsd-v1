Work incrementally: first get the existing pipeline.cpp compiling and running unmodified to produce the buggy baseline output before touching any code — this locks in a reference point to diff against later. Read the code carefully to locate exactly where the mean of `val1` is computed (it will be over the full 100-row joined set) and where it's subtracted; the fix is to compute the mean using ONLY the first 80 rows (the training partition) and then subtract that single scalar from val1 for all 100 rows (both train and test), not to recompute two different means. Recompile and rerun to produce the fixed predictions. For the statistics, don't hand-derive numbers — actually compute the training mean, and run a real paired t-test (e.g. via `scipy.stats.ttest_rel` in a small Python helper, or implement the paired-difference t-statistic formula yourself) over the 20 test-set prediction pairs from the two CSVs; be aware that a paired series with a constant difference will produce a degenerate/zero variance case, so handle that edge case in your t-test code rather than crashing. Round all four required values to 4 decimal places before writing experiment_log.json.