For Step 1, use ffmpeg with `-vf "fps=1,scale=64:64,format=gray"` and `-f rawvideo -pix_fmt gray` to emit raw 8-bit grayscale frames, then split the resulting stream into per-second 4096-byte files named frame_NNN.bin (ffmpeg can write a numbered sequence directly, or you can pipe to a small script that chunks the raw stream). For Step 2, follow the hashing spec literally and in this exact order, since off-by-one block indexing or wrong rounding will silently break bit-exactness: read the 4096 bytes as a 64x64 row-major grid, iterate 8x8 blocks in row-major block order (block 0 = top-left, incrementing across columns then rows), sum each block's 64 pixels, integer-divide by 64 for the block average, sum all 64 block averages and integer-divide by 64 for the global average, then set bit b (LSB = block 0) when block b's average strictly exceeds the global average. Compute Hamming distance via XOR + popcount. Print the three lines with a 16-hex-digit zero-padded lowercase hash. Compile with gcc, then run it on frame_001.bin and frame_002.bin, redirecting stdout to /home/user/sample_output.txt.