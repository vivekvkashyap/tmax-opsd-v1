Set up a Go module (`go mod init`, then `go get github.com/astrogo/fitsio` and `go get gonum.org/v1/gonum/mat`) before writing code. In `/home/user/solve.go`, open the FITS file, read the primary HDU's image data as float64 (check the HDU's reported dimensions/axes to correctly reshape the flat data into an NxN matrix — don't hardcode a size, derive it from the header/axis metadata), and load it into a `mat.Dense`. Build the regularized matrix by adding `0.05` to each diagonal entry (equivalent to `A + lambda*I`), not by constructing a separate identity matrix and using a generic add if that's more error-prone — either way, verify only the diagonal changes. Construct vector `b` as all 1.0s with length equal to the matrix's row count, then solve with `mat.Dense.Solve` (or an LU/Cholesky solver from gonum) rather than the raw near-singular matrix, since that's the whole point of the regularization. Marshal the resulting solution vector to a flat JSON float array with `encoding/json` and write it to `/home/user/solution.json`. Run the program and sanity-check the output has the same length as the matrix dimension.