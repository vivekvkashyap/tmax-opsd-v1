Treat the three subtasks as independent and verify each in isolation before moving on. (1) `mkdir -p /home/user/legacy_monitor`, drop a dummy `config.json` in it, then `tar -czf /home/user/legacy_backup.tar.gz -C /home/user legacy_monitor` so the archive contains the `legacy_monitor/config.json` path exactly (verify with `tar -tzf`). Write `tunnel.sh` with an `ssh -L 9999:remote-telemetry.internal:80 deploy@bastion.internal` style command and `chmod +x` it — match the exact host:port:host:port ordering the grader expects. (2) For the video: use `ffmpeg` to extract frames (e.g. to raw images or via a pipe) and inspect them in Python/PIL/numpy — for each frame, sample the pixel at exact width/2, height/2 and test R==0, G==0, B>250; count frames meeting this criterion exactly (don't use a tolerance range or nearest-frame heuristic), and write the integer to `dropped_frames.txt` with no extra text. (3) For the sanitizer, `json.load` the file, then walk all string values recursively and reject (`sys.exit(1)`) on any shell metacharacter (`;|&$` backtick, `()`) or `../` substring; be careful not to false-positive on legitimate timezone/locale strings like `America/New_York` or `en_US.UTF-8` — test explicitly against those before treating the corpus as solved.