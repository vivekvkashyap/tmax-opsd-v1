Parse each JSONL line, converting the ISO 8601 timestamp to a datetime object, then compute its tumbling 1-hour window start by truncating minutes/seconds/microseconds to zero (e.g. `dt.replace(minute=0, second=0, microsecond=0)`) — don't try to bucket by string-slicing the timestamp, since that's fragile if formats vary. Aggregate counts in a nested structure keyed by (window_start, locale, key), incrementing a counter for each log line. For each window, find the max count, then among all (locale, key) pairs tied at that max, pick the one that sorts first alphabetically by locale then by key — use Python's tuple comparison/sorting rather than just taking the first one encountered, since dict iteration order isn't guaranteed to match insertion in all contexts and ties are deliberately present in the test data. Sort the final list of per-window winners chronologically by window_start before writing. Write the CSV with the exact header `window_start,locale,key,error_count`, formatting window_start back to the same ISO 8601 style with trailing `Z`. Run the script to produce `/home/user/top_missing.csv` and spot-check a couple of windows manually against the raw log counts.