Treat `/app/sig_gen` strictly as an opaque oracle — call it on files you control (`/app/sig_gen <path>`) to learn its output format, but don't try to reverse-engineer its internal math; your server just needs to shell out to it and relay whatever hex string it prints. Fix the C++ server first: add the missing headers (`<thread>`, `<mutex>`, etc.) and the `-pthread` flag to the Makefile so `make` links cleanly, then implement the TODO — base64-decode both fields from the `VALIDATE <b64base> <b64patch>` line, write them to temp files (the patch is a standard unified diff, so use it verbatim with the Unix `patch` command against the base file, e.g. `patch base.txt < patch.diff`), check `patch`'s exit status, and on success run `/app/sig_gen` on the resulting patched file and reply `SUCCESS <output>\n`; on failure reply `ERROR PATCH_FAILED\n`. Bind and listen on 127.0.0.1:8080, spawn a thread per connection (or otherwise handle concurrent clients), and run it in the background. For the Go runner, the core bug is calling `wg.Add(1)` inside the goroutine instead of before `go func(...)` is launched — move `wg.Add(1)` to the loop body before the `go` statement to avoid the race where `wg.Wait()` can return before all goroutines have registered. Test end-to-end with `go run main.go` completing cleanly against your running server before finishing.