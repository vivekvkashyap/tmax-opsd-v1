Two independent bugs to root-cause before restarting the service. (1) For the `/sum` precision regression: use `git log`/`git bisect` in `/home/user/math-service/` to find the commit that swapped a high-precision summation (e.g. `math.fsum`) for a naive accumulator (watch for reduced-precision types like float32 causing catastrophic cancellation with mixed-magnitude inputs); fix `main` to restore a numerically stable summation algorithm (Kahan summation or `math.fsum`), not just revert blindly — confirm the fix handles cases like large-then-small-then-negative-large sequences correctly. (2) For the `/variance` race: inspect `/app/vendored/fast-variance` source for shared mutable state (e.g. a module-level or class-level list) used as a scratch buffer inside the calculation method — under concurrent requests, threads clobber each other's data in that shared buffer. Fix it by making the scratch buffer function-local, then reinstall the package into the environment (editable install) so the running server picks up the fix. Finally start the FastAPI service on 127.0.0.1:8080 as a background/daemon process, enforcing the `Authorization: Bearer calc-token-998` header on every endpoint (401 otherwise), and verify it survives concurrent load before finishing.
