Do this as a clean extract-transform-diff pipeline, verifying intermediate artifacts at each step. (1) Extract raw UDP payloads to port 5000 from the pcap — using `scapy` (`rdpcap`, then filter packets with `UDP` layer and `dport==5000`, taking `bytes(pkt[UDP].payload)`) is more reliable than tshark text parsing for exact byte-for-byte payloads; decode each as UTF-8 and split on newline if multiple JSON lines are concatenated. (2) Save each extracted JSON payload to a temp file/list, then pipe each (or all, newline-joined) through `python3 /home/user/transform.py` via subprocess, capturing stdout per line — don't assume line order is preserved if you batch them, so pair inputs to outputs by decoding each transformed JSON's own `sensor_id`. (3) Parse both original and transformed JSON per sensor, then compute `abs(original_value - transformed_value)` for each sensor and find the one exceeding `0.001` — note that the transform introduces float32 precision loss so differences are usually tiny except for one sensor with an unusually large or precise value where casting to 32-bit float loses meaningful precision. (4) Write only that sensor_id (no extra text/newlines beyond what's natural) to `/home/user/faulty_sensor.txt`.
