Sequence this as: fix the C extension for Python 3 first, verify it builds and behaves correctly standalone, then layer the Go concurrency piece and CI script on top. In semver_ext.c, replace the Python 2 module init (`initsemver_ext`/`Py_InitModule`) with the Python 3 pattern (`PyModuleDef` struct + `PyInit_semver_ext` returning `PyModule_Create(&moduddef)`), and swap `PyString_AsString` for `PyArg_ParseTuple(args, "ss", ...)` with UTF-8 C strings directly (Python 3's `s` format code already gives you a `const char*`, so the parsing logic barely changes — the main work is the init boilerplate). Fix setup.py's Python 2 print statement and confirm it as valid Python 3 syntax. Build and test in isolation: `python3 -m pip install .` then a quick `python3 -c "import semver_ext; print(semver_ext.parse_and_compare('/api/v1/pkg/1.2.4','1.2.0'))"` to confirm it returns 1, before trusting server.py. For tester.go, spawn one goroutine per URL argument with a `sync.WaitGroup`, each doing its own `http.Get`, and use a `sync.Mutex` (or a channel collecting results) to safely append successful (200) endpoints to a shared slice before writing them to success.log — a plain unsynchronized slice append from multiple goroutines is the classic bug here. Write ci.sh to install, background-launch the server with `&` plus `sleep 2`, build and run the Go tester with the four given endpoints, then kill the background server PID and `exit 0`; run ci.sh yourself end-to-end and inspect success.log before considering it finished.
