Build and unit-test `storage_optimizer.sh` on its own before wiring it into the pipeline orchestrator. Inside it: sum sizes of files directly in `TARGET_DIR` only (exclude subdirectories — use `find "$TARGET_DIR" -maxdepth 1 -type f` combined with `stat`/`du` per file, or `du -b` on each, rather than a recursive du that would include nested dirs). If the total exceeds the exact quota constant, repeatedly pick the single oldest remaining file by modification time (`ls -t` reversed, or sort by `stat -c %Y`), `mkdir -p` the archive dir if needed, `mv` that file there, append one CSV line with the current `date +%s` epoch, the file's basename, its byte size (captured before moving it), and the absolute source/dest dirs — then recompute the remaining total and repeat until under quota. Make sure the script actually exits 0 on success and propagates a non-zero code on real errors (bad args, missing target dir). For `run_finops_pipeline.sh`, call the optimizer against staging, check `$?` immediately and `exit 1` (or similar) on failure before ever touching production, then run it again against the production paths. Chmod both +x, run the orchestrator once, and manually recompute the remaining directory sizes with `du -b` to confirm they now sit at/under the 50 MiB quota.
