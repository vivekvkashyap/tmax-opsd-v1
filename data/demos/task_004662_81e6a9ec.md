Write the backup script to first parse `/home/user/backup_history.log`: split it into records (e.g. on the `--- Backup Record ---` delimiter or blank-line boundaries), extract `Status:` and `Timestamp:` fields per record with a simple regex or line-splitting, filter to `Status: SUCCESS` records, and take the maximum `Timestamp` among those — don't just take the last record in the file, since the most recent by timestamp isn't necessarily the last one listed. For traversal, use `os.walk(path, followlinks=False)` and additionally check `os.path.islink()` on each entry (both dirs and files) to be certain no symlink — including the self-referential loop — is ever followed or treated as a regular file; only include entries where `os.path.isfile()` and not a symlink. Compare each qualifying file's `os.stat().st_mtime` (or st_mtime as int) against the success timestamp with strict `>`. Build the relative path with `os.path.relpath(full, base)` so it never starts with `./` or `/`, sort all qualifying relative paths alphabetically, then for each emit `f"{relpath}\n{size}\n"` followed by the raw file bytes (decoded/encoded consistently as UTF-8), concatenated with no separators between file blocks. Compress the full concatenated string's UTF-8 bytes with `zlib.compress()` at default level and write the result to `/home/user/latest_backup.zlib`.