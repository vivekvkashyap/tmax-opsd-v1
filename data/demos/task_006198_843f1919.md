First diagnose the Makefile failure directly: run `make` and read the linker error carefully — mathcodec.c calls `sqrt`/`log2` from libm, and the compile rule is missing the `-lm` flag on the gcc command that builds the shared object; add `-lm` (order matters with some linkers, put it after the source/object files) to `libmathcodec.so`'s recipe and confirm `make` succeeds and produces the `.so`. For the Python fixture, use `ctypes.CDLL` to load the library, set `encode.argtypes`/`restype` appropriately (a `c_char_p` for input, a `POINTER(c_double)` output buffer sized to the input length via `(c_double * len)()`, and `c_int` for len), call it with the literal bytes for "TEST", then format each of the 4 resulting doubles to exactly 3 decimals and comma-join them for the output file. For the Go/cgo build, the `#cgo LDFLAGS` in processor.go already references `-lmathcodec`, so `go build` needs the shared lib visible at link time (same directory, plus `LD_LIBRARY_PATH=/home/user/project` at both build and run time) — run `go build -o processor processor.go`, then execute `./processor` with `LD_LIBRARY_PATH` set as instructed so the dynamic loader finds libmathcodec.so at runtime, not just link time.