Start by running `tesseract /app/finops_policy.png stdout` (or converting to a suitable format first if OCR quality is poor) to recover the exact policy rules from the image — do not guess the header name or cost threshold, read them off the OCR output verbatim. Then write the C filter to read the entire stdin message (headers + body) into a buffer, and implement the two policy checks precisely as extracted: (1) search for the required header line exactly as recovered, (2) locate the `Total-Cost:` field in the body, parse the numeric value after the `$`, and compare it against the recovered dollar threshold using a strict `<` comparison. Exit 0 only if both checks pass, else exit 1. Before finalizing, iterate against the provided corpora: loop over every file in `/app/corpus/clean/` and `/app/corpus/evil/`, run the compiled binary on each via stdin, and check the exit codes match expectations (0 for clean, 1 for evil) — use this as your test harness to catch parsing edge cases (extra whitespace, different token values, cost near the boundary). For the forwarder script, use `socat TCP-LISTEN:10025,fork,reuseaddr SYSTEM:'...'` or a `nc -l` loop that captures the incoming bytes to a temp file, pipes that file to `mail_filter`, checks `$?`, and only on success cats the same bytes to port 10026 via another `nc` connection — run this in a background loop so it keeps accepting new connections.