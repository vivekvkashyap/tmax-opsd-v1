Structure the script as an explicit DAG: define each node as a function, register dependencies in a dict, and run a simple topological execution (a fixed order is fine since the dependency graph is static, but write the orchestration generically e.g. track completed nodes and only invoke a node once its deps are marked done). Wrap every node call with logging: append `[INFO] - Node {name} started.` immediately before executing the node body and `[INFO] - Node {name} completed.` immediately after, opening the log file in append mode (or accumulating lines and writing once, but preserving start/complete adjacency and order). For Impute_Telemetry, group by `server_id`, sort by `timestamp`, then use pandas `.interpolate(method='linear')` per group followed by `.ffill().bfill()` to handle edge NaNs — do this per-column, not on the whole frame at once, since interpolation must respect group boundaries. For Mask_Access, hash `user_email` with `hashlib.sha256(email.encode()).hexdigest()` (lowercase hex is the default) and mask the IP by splitting on '.' and replacing the last octet with '0'. For Merge_And_Aggregate, do an inner join on `['timestamp','server_id']`, then `groupby('user_ip')[['cpu_usage','memory_usage']].mean().round(2)`. Sort the final CSV by `user_ip` ascending before writing with the exact column order `user_ip,cpu_usage,memory_usage`.