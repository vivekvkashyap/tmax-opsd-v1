Build and test each pipeline stage independently before chaining them in the master script — a naive attempt writes one big script and only discovers header/index bugs at the end. First write `pca_transform.py` to accept train/test CSV paths, fit `sklearn.decomposition.PCA(n_components=3, random_state=42)` on the training features only (exclude `id` and `target`), transform both train and test with that same fitted model, and re-attach `id`/`target` columns when writing `train_pca.csv`/`test_pca.csv`. Then write `bayesian_train_predict.py` to fit `sklearn.linear_model.BayesianRidge()` on the 3 PCA columns vs `target` from train, predict on test, and emit exactly `id,predicted_target` to `predictions.csv`. In `run_pipeline.sh`, split `raw_data.csv` carefully with `head`/`tail` so the header is preserved in both `train_raw.csv` (rows 1-800) and `test_raw.csv` (rows 801-1000) but never duplicated or dropped — verify row counts explicitly. After running both Python stages, compute MSE between `predictions.csv`'s `predicted_target` and `test_raw.csv`'s `target`, matching rows by `id` (don't assume row order is preserved) using `join`/`awk`, then round to exactly 2 decimals with `awk`'s printf and write only that number (no trailing newline issues) to `/home/user/mse_result.txt`. Chmod the script executable and actually run it to produce the artifact.
