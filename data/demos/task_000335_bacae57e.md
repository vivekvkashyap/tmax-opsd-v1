Work through the four sub-problems in dependency order. First run `make` to see the actual compile/link errors, then fix the `Makefile` — the missing include/library search paths (`-I` for the header dir, `-L`/`-l` for the library dir/name) are the usual culprit, not the source code itself. Second, since `auth.h`'s declared signature for `generate_token` is wrong, don't guess — inspect the compiled `libauth.so` directly with `nm -D`, `objdump -T`/`objdump -g` (it has debug symbols) or `gdb -batch -ex 'ptype generate_token' libauth.so` to recover the real parameter types and count, then rewrite the header's prototype to match exactly. Third, `strtok` is not thread-safe because it uses hidden static state shared across threads — replace it with `strtok_r`, which takes an explicit `saveptr` argument, updating both the declaration and call sites in `extractor.c`. Fourth, write `regression_test.c` using `pthread_create` for 20 threads all calling the parsing function concurrently, `pthread_join` each, and check every result equals the expected final token before returning 0; compile and run it repeatedly to confirm no intermittent failures remain. Only then populate `/home/user/debug_report.txt` with the actual recovered signature and function name you used.