Build the pipeline as three explicit phases with a shared record list: extract, dedupe, load. In extract, parse each format with its natural library (csv module, json.load, xml.etree) into a common list of dicts with the five logical fields, counting every record parsed (including ones that will later turn out to be duplicates) toward records_read — process files in the specified order (CSV, then JSON, then XML) since first-occurrence-wins depends on this order. For deduplication, normalize the fingerprint input precisely: format amount to exactly two decimals (`f"{float(amount):.2f}"`) regardless of whether the source stored it as a string or float, concatenate `user_id|timestamp|amount` and hash with `hashlib.sha256(...).hexdigest()`; keep a `seen` set of fingerprints, and only keep a record the first time its fingerprint appears, incrementing a duplicates counter for each subsequent match — do this incrementally as you process records in file order, not via a post-hoc groupby, so the "first occurrence" is well-defined. In load, sort surviving records by timestamp ascending, write JSONL with amount serialized as float, and append the three `[EXTRACT]/[TRANSFORM]/[LOAD]` log lines with the exact computed counts, keeping the format string exactly as specified.