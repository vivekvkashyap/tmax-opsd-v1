Work through the four pieces in dependency order since later steps rely on earlier ones being up. First fix the CMake link error: inspect `/home/user/project/src/CMakeLists.txt` and add the missing `target_link_libraries(app custom_math)` call so the `app` executable links the shared library, then build into `/home/user/project/build` and confirm `app` runs standalone before moving on. Second, write the Flask/FastAPI service so `/evaluate?expr=...` safely evaluates a simple numeric inequality string — parse the operator (`>`, `<`, etc.) and the two numeric sides explicitly rather than using bare `eval()` on untrusted input, returning `{"build": true/false}` as JSON. Third, write a minimal `nginx.conf` that overrides `pid`, `error_log`, and the various `*_temp_path` directives into `/tmp/nginx/` (create that directory first) since you lack root, with a `server` block on `127.0.0.1:8080` proxying to `127.0.0.1:5000`; test `nginx -c <path> -t` before starting it in the background. Finally write the bash trigger script to curl the proxy, parse the `"build"` boolean out of the JSON (e.g. with `grep`/`jq`/`python -c`), and only then build+run `app`, redirecting its stdout into `/home/user/pipeline_output.txt`. Start Flask and nginx in the background before running the trigger.
