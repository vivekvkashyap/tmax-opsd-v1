This task is two independent deliverables — do them separately and don't overthink the second since it's static text, not executed. For monitor.go, read device_storage.txt, parse the integer after `storage_available_kb=` (strip the prefix, `strconv.Atoi` the remainder, trim whitespace/newline), convert to MB using integer division (`kb/1024`, not float rounding — the spec calls for truncation, so watch for accidental float math introducing rounding), and marshal `{"storage_mb": <int>}` to the JSON file with `encoding/json` or a formatted string; then actually execute it with `go run` so storage_metric.json is regenerated from the current input rather than hand-written. For setup_iot.sh, write plain literal shell commands (no need to actually run them, since they require root/real network devices) matching each requirement precisely: `groupadd`, `useradd -g <group> <user>`, `ip route add <cidr> via <gw> dev <iface>`, and an SSH tunnel line using `-f -N -L <local>:<remote_host>:<remote_port> <user>@<jumphost>` — get the local/remote port ordering in `-L` right (local_port:remote_host:remote_port) and include all four required flags/args on one command.