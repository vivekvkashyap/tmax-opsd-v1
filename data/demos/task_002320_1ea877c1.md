First diagnose the vendored crate: run `cargo build` inside `/app/text-dedup-engine` and read the actual error rather than guessing — check `Cargo.toml`'s `edition` field for an invalid/unsupported value (the installed toolchain will report exactly which edition it does and doesn't support) and correct it to a supported edition string; also scan `src/lib.rs` for syntax issues once the edition is fixed. After the crate builds, scaffold a new binary project with `cargo new`, add the vendored crate as a `path` dependency in `Cargo.toml`, and implement the pipeline: read `/home/user/raw_corpus.txt` line by line, lowercase, strip all non-alphanumeric/non-whitespace characters, collapse repeated whitespace, and trim. Call `text_dedup_engine::compute_hash` on the normalized line and keep only the first line per unique hash (use a `HashSet<u64>` to track seen hashes, preserving first-occurrence order). Write survivors to `/home/user/clean_corpus.txt`. Double-check the punctuation-removal and whitespace-collapsing regex/logic against edge cases (leading/trailing spaces, multiple internal spaces, mixed case) since the grading metric is F1 against a hidden golden set and small normalization mismatches will silently tank recall or precision.