Start by reading `worker.py` to find the exact environment variable name it checks for on startup (it exits early if unset) — export it to a dummy value in your shell before anything else, since without it every test invocation crashes and masks the real bug. Write `test_bisect.py` as a self-contained regression oracle: set the required env var itself (don't rely on inherited shell state, since `git bisect run` invokes it fresh), overwrite `counter.txt` with `0`, run `worker.py 50` as a subprocess, wait for completion, read back `counter.txt`, and exit 0 if it equals the expected total operations (50 threads × 100 ops each) or exit 1 otherwise — this exit-code contract is what `git bisect run` relies on (0=good, 1=bad). Then run `git bisect start`, mark HEAD `bad` and `v1.0` `good`, and `git bisect run python3 /home/user/test_bisect.py` to auto-search the range; since the race is nondeterministic, consider retrying the test a few times per commit inside the script for reliability. Once bisect reports the first bad commit, capture its full SHA via `git rev-parse` and write it to `/home/user/bad_commit.txt`, then `git bisect reset` to restore the tree without altering history.