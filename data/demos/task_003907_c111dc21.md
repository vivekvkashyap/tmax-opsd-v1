Build the script defensively and test each sub-step in isolation before assembling: start with `set -euo pipefail` for fail-fast behavior. Extract `backup.tar.gz` into a freshly created `/home/user/restore_test/` (`mkdir -p` then `tar -xzf ... -C ...`). For the timestamp: grep `deploy_pipeline.log` for lines containing `STATUS: SUCCESS`, extract each UTC timestamp, sort them, and take the chronologically latest — don't assume log order equals chronological order, parse and compare properly (e.g. with `date -d` or by sorting the timestamp strings since they're in a sortable YYYY-MM-DD HH:MM:SS format). Convert that UTC timestamp to Asia/Tokyo using `TZ='Asia/Tokyo' date -d "<utc-ts> UTC" '+%Y-%m-%d %H:%M:%S'` (note this can roll over to the next calendar day). For the proxy config: use `sed` to substitute `listen 80;` → `listen 8080;` and `listen 443;` → `listen 8443;`, write to `local_proxy.conf`, then re-parse that output file with grep/sed to populate the JSON array rather than hardcoding port values, so the report reflects what the file actually contains. Emit the final JSON with `jq` or a heredoc matching the exact required keys, and remember `chmod +x` the script — the grader runs it standalone with no arguments.