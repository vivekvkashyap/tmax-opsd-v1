Treat `/app/pck_unpack` as an opaque black box you invoke, not something to reverse engineer: for each `.pck` file, run it via `subprocess.run([...], capture_output=True)` and iterate its stdout line by line (don't load everything into memory at once across 50 files if avoidable, but it's fine to stream per file). For each line, `json.loads` it inside a try/except so malformed/truncated lines are simply dropped; for well-formed lines, recompute `hashlib.md5(payload.encode()).hexdigest()` and compare against the `checksum` field — only keep exact matches. Accumulate valid rows as `(timestamp, severity, payload)` tuples. The key trick for the size constraint: before writing the CSV, sort all accumulated rows (e.g. by `severity` then `payload` or `timestamp`) so that repeated/similar strings cluster together — this dramatically improves gzip's LZ77 dictionary matching versus writing rows in original chronological order. Write the sorted CSV with the exact header, then gzip it (Python's `gzip` module, or pipe through `gzip -9`) to `/home/user/optimized_logs.csv.gz`. Verify the final size with `os.path.getsize` before declaring done.
