Build and validate `verify_stream.py` against the training/example files first, since it's scored against a hidden adversarial corpus. Read the file's raw bytes and attempt strict decoding (try UTF-8 first with `errors='strict'`, then fall back to UTF-16/Windows-1252 detection, e.g. via `chardet`/`charset_normalizer` or BOM sniffing) — any row that can only be decoded with replacement characters must fail the file as EVIL, so never silently use `errors='replace'` for the final parse. Validate every row strictly: parse `timestamp` with `datetime.fromisoformat` (or `dateutil`) and check it falls within 2000-01-01 to 2050-12-31; check `sensor_id` fully matches `^[A-Za-z0-9]+$` (rejects SQL-injection-style payloads); parse `temperature` as float and range-check `-100.0 <= t <= 200.0`; any single bad row makes the whole file EVIL — exit 1 immediately, else exit 0 after all rows pass. For the `.dat` decoder, since it's stripped, run it with no args to read its usage string from stderr, then invoke it programmatically as `subprocess.run(["/app/decoder", infile, outfile])` inside `pipeline.py` to get a CSV you can then run through the same validation and cleaning logic before computing a proper time-ordered 15-minute rolling average per `sensor_id` (use pandas `resample`/`rolling` on a sorted, `DatetimeIndex`) and writing rows into the `rolling_temps` SQLite table with the exact column names specified.
