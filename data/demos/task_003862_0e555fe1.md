Handle each of the four deliverables as an independent, verifiable step. For redaction, use a regex like `session_id=\w+` and replace with `session_id=[REDACTED]` via Python's `re.sub`, preserving every other character of the line exactly (don't rebuild lines from scratch, which risks losing formatting). For the open-redirect scan, match `redirect_to=` values within `/login` request lines and keep only those beginning with `http://` or `https://` (a bare path like `/home` is not an external redirect) — extract just the URL value, not the whole log line, then `sorted()` and dedupe if needed before writing one per line. For the checksum, run `sha256sum /home/user/redacted_logs.txt` and redirect its exact stdout (path and all) into `log_checksum.txt` rather than hand-formatting the hash, so the format matches exactly. For the PoC, construct the URL by combining the vulnerable app's base (`http://127.0.0.1:5000/login`) with the `redirect_to` query parameter pointing at the attacker URL — write only the bare URL string to `poc.txt`, with no extra whitespace, quotes, or trailing newline issues that would break a strict-equality check.
