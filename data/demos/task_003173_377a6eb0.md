Use `git bisect` methodically rather than guessing: `git bisect start`, mark `v1.0` as `good` and `HEAD` on main as `bad`, then at each step build/run `./run_pipeline.sh /app/test_video.mp4 output.csv` and compare drift against the earlier frames' output (or against v1.0's output) to classify the checked-out commit as good or bad, running `git bisect good`/`git bisect bad` accordingly until git reports the first bad commit. Once found, inspect that commit's diff specifically for numeric precision changes across the C++ extractor and Python tracker boundary — the classic causes are an integer/float cast that truncates a `double` (e.g. `int` cast on a fractional coordinate), or a Python `/` accidentally changed to `//` (integer floor division), either of which causes small per-frame errors that compound frame-over-frame since tracking is stateful. Fix the root cause on current `main` HEAD (not by reverting the whole commit, since ~200 commits followed it — reapply the same numeric fix in the current code). Rerun the fixed pipeline on the test video, write `frame_id,x,y,w,h` to `/home/user/final_output.csv`, and sanity-check that early frames match early results (bug shouldn't affect frame 0) while later frames no longer drift.