Implement this as: acquire lock -> build new archive in temp -> atomic rename -> release lock. Open (or create) `<archive>.lock` in a mode like `'a+'`/`'w+'`, call `fcntl.flock(fh, fcntl.LOCK_EX)` before touching the zip, and release with `fcntl.flock(fh, fcntl.LOCK_UN)` plus close the file at the end (use try/finally so the lock is always released). Use `tempfile.NamedTemporaryFile(delete=False, dir=same-dir-as-target)` (same directory ensures `os.replace` is atomic across filesystems) to write the merged archive with `zipfile.ZipFile`. Merge logic: first build a set/dict of filenames present in `<incoming_dir>` (walk it, not just top-level if nested); iterate the existing archive's entries and copy each to the new zip unless its name exists in the incoming set (in which case skip it — it'll be added from incoming); then add every file from the incoming directory (both the overrides and the brand-new ones) using their base filename as the archive entry name. Close the new zip fully before calling `os.replace(tmp_path, archive_path)` so the swap is atomic. Finally run the script with the given arguments.
