Walk `/home/user/server_configs` recursively (e.g. `os.walk` or `pathlib.Path.rglob("*.conf")`) and process only files ending in `.conf` — explicitly skip other extensions like `.txt` so secrets in non-config files are neither redacted nor archived, since the task setup includes a decoy readme with a SECRET_KEY-looking line that must be left untouched. For each `.conf` file, use a regex like `re.sub(r'SECRET_KEY=\w+', 'SECRET_KEY=REDACTED', content)` and write the file back in place before hashing it, so the manifest reflects the post-redaction content, not the original. Compute SHA256 of each sanitized file's bytes and write `/home/user/manifest.sha256` with lines formatted as `<hash>  <path-relative-to-server_configs>` (two spaces, matching `sha256sum` convention). Build the tarball with Python's `tarfile` or `tar -czf`, using `arcname`/`-C` so the archive root is `server_configs` itself (paths like `app1/settings.conf`, not the full `/home/user/...` prefix) and includes only the three `.conf` files. Finally sanity-check the archive with `tar -tzf /home/user/config_backup.tar.gz` and by extracting to a scratch dir and confirming redaction persisted inside it.
