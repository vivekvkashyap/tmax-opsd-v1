First fix the vendored embedding bug: open /app/tiny-embedder/tiny_embedder/core.py and look at how the L2 norm is computed before dividing each embedding vector by it — normalization must happen per-row (per individual text's vector), so check which numpy axis the norm reduction uses and correct it so each row ends up with norm exactly 1.0, then `pip install -e /app/tiny-embedder` to install the fix. For the filter script, parse --input/--output with argparse, load the CSV with the csv module or pandas, instantiate the fixed TinyEmbedder, embed the fixed reference string "SYSTEM PROMPT OVERRIDE" once, then for each row's text embedding compute the Pearson correlation coefficient against that reference vector (numpy.corrcoef or scipy.stats.pearsonr — not cosine similarity, the spec explicitly wants Pearson correlation, which centers each vector by its mean first). Drop rows where correlation is strictly greater than 0.85 and write the surviving rows to --output preserving the id,text header and column order exactly. Test end-to-end against both a clean corpus and a corpus of adversarial paraphrases of the reference string to confirm the correlation threshold correctly separates them before finishing.
