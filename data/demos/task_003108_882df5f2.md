This task has three independent recovery steps that all feed into one script — solve them in isolation first. (1) To recover the deleted `offsets.dat` from the unmounted ext4 image, don't try to mount it destructively; use `debugfs -R "ls -d /" /app/build_fs.ext4` to list deleted inodes (they show with a `<...>` marker and inode number), then `debugfs -R "dump <inode_num> /tmp/recovered" /app/build_fs.ext4` to extract its content, and read the recovered offset value from `/tmp/recovered`. (2) Extract formatting rules from `/app/specs.png` via `tesseract /app/specs.png stdout` (adjust `--psm` if the text is misread); read carefully what precision, notation, and error-handling rules it specifies rather than assuming defaults. (3) Write `/home/user/format_parser.sh` using `awk` (best for portable floating-point formatting and validity checks) to: parse each stdin line as a number (rejecting non-numeric input per the spec's error rule), add the recovered offset, clamp per the spec's bounds rule, and format to the exact decimal-place rule extracted from the image — avoid scientific notation in output. `chmod +x` the script, then test it against many handwritten edge cases (integers, leading dot, scientific notation input, negative zero, huge values) before trusting the fuzz-based bit-exact comparison against the oracle.