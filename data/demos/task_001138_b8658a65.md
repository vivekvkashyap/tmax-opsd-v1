Two pieces of ground truth must be recovered before writing the sanitizer. First, extract frames from `/app/dashboard.mp4` (e.g. `ffmpeg -i dashboard.mp4 frame_%04d.png` or sample at intervals) and read the on-screen timestamp text (OCR/visual inspection) to find the exact start/end second the red "OVERLOAD" text is visible — convert to Unix timestamps for the inclusive drop window; derive it from observed frames, don't guess. Second, write `/home/user/sanitize.sh` as an awk/bash filter over `$1`: split each line on whitespace into timestamp/pressure/status; drop the line if timestamp is in the observed window; drop it if pressure contains `e`/`E` (scientific notation); otherwise if pressure's fractional part has more than 4 digits, truncate (not round) to 4 decimals via string manipulation (don't use printf %.4f, which rounds); otherwise print the line unchanged, preserving exact spacing. Test against a clean and a malformed sample log to confirm untouched lines are byte-identical.
