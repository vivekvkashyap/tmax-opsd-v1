This is a git bisect task complicated by a broken build script partway through history. First confirm endpoints: check out `v1.0`, build, run `./nonce-gen 42` to confirm the known-good output, then check out HEAD and note the build breaks due to `go.mod` needing a fix before it compiles. Rather than trusting the repo's `build.sh` as-is (it likely breaks once a directory gets a space in its name, via unquoted paths), write your own bisect-run script that: cds into the repo, applies whatever build/dependency fix is needed for the commit under test (quote paths, patch go.mod if broken there), builds, runs with seed 42, and exits 0 if output matches the known-good value or 1 otherwise — use exit code `125` to skip a commit only if truly unbuildable even after fixes. Use `git bisect start`, mark HEAD bad and v1.0 good, then `git bisect run ./your_script.sh`. Once it converges, capture the commit hash with `git rev-parse` and write only the 40-character SHA to /home/user/bad_commit.txt.