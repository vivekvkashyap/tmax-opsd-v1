Two separate discovery steps feed one implementation: first OCR the image at `/app/policy_rules.png` (e.g. `tesseract /app/policy_rules.png stdout`) to recover the XOR key and the required cookie-name prefix — don't guess these, read them off the extracted text carefully since OCR can misread digits/hex. Second, probe `/app/oracle_extractor` directly: run it with a range of hand-crafted base64 payloads (valid HTTP-request-shaped plaintext with an `X-ELF-Hash:` header and a cookie line, plus deliberately malformed/truncated ones) and diff stdout/exit codes to pin down the exact output format string and the exact error string/behavior for bad input — do this via a shell loop, not by reading source (there is none). Only after both are nailed down, implement `/home/user/audit_gen.py`: base64-decode argv[1], XOR every byte with the recovered key, then regex out the hash and the specific cookie value, printing in the oracle's exact format (or its exact error text on failure). Since grading is bit-exact fuzz equivalence against the oracle over thousands of cases, test your script against the oracle on many payloads yourself before finishing, especially edge cases like missing header, missing cookie, and invalid base64.
