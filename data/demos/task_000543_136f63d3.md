Use delta debugging on `/home/user/incidents/batch_99.csv`: run `/app/financial_aggregator` on the full file to confirm the negative total, then bisect the file (split in half, run the binary on each half, recurse into whichever half still reproduces the anomaly) to isolate the specific record(s) responsible, rather than eyeballing 10k rows. In parallel, `git bisect` the `/home/user/aggregator_src` repository: mark an early commit good and the latest bad, let bisect narrow to the exact commit, then read that commit's diff to see the internal formula and the integer type/width it changed — this tells you the exact overflow boundary and expression (amount * multiplier feeding into a fixed-width signed integer). Cross-check the boundary you derived against the isolated record from delta debugging to make sure they agree before writing the filter. Implement `/home/user/sanitizer.sh` (or `.py`) as a streaming stdin-to-stdout CSV filter: for each row, recompute the same expression using the discovered formula and width, and drop (silently, don't emit) any row where it would exceed the signed limit, otherwise pass the row through unchanged and in order. Test it against small clean/evil samples you construct yourself before relying on the hidden corpora.