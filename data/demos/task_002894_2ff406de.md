Break this into three independent stages and verify each before moving on. (1) For black-frame detection, extract one frame per second with `ffmpeg -i telemetry.mp4 -vf fps=1 frame_%02d.png` (or use ffmpeg's `blackdetect` filter directly: `ffmpeg -i telemetry.mp4 -vf blackdetect=d=0.9:pix_th=0.10 -f null -` and parse the `black_start`/`black_end` lines from stderr) — don't eyeball frames, parse the filter's numeric output to get exact integer seconds, then group contiguous black seconds into windows. (2) For dedup, only compare records whose `ingest_time_sec` falls inside a detected retry window, group those by `email`, sort by `id`, keep the lowest `id` per group, and drop the rest — records outside retry windows are never touched even if their email repeats. (3) Mask only the local part before `@`, preserving everything else, and be careful to preserve Unicode `feedback_text` bytes exactly when re-serializing JSONL. Finally, stand up a persistent HTTP server (not a one-shot script) bound to 127.0.0.1:8080 with `/stats` and `/cleaned` routes, launched in the background (e.g. via `nohup ... &` or a systemd-less daemonization) so it survives after your shell session ends.