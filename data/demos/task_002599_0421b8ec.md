Break this into independent, individually-testable stages rather than writing it all at once. First run `tesseract /app/spectroscopy_meta.png stdout` to OCR the note and grep/parse out the value after `CALIBRATION_TOKEN=` — don't hardcode any token, always read it from the OCR output at runtime if the token needs to persist across restarts, otherwise capture it once and bake it into the auth check. For the Rust pipeline: parse the CSV with the `csv` crate, then implement the moving-average smoothing carefully — for each index sum only the in-bounds neighbors in the window `i-2..=i+2` and divide by however many were actually valid (not always 5), since edge points have fewer neighbors. For bootstrap CIs, resample indices with replacement, sort the resampled points by `x` before smoothing (order matters for the windowed average), rerun the same smoothing/peak-finding logic, and after 1000 iterations sort the collected peak `x` values to read off percentile indices near 2.5% and 97.5% (nearest-rank is fine). Build the HTTP layer with `axum`/`tokio`, checking the `Authorization: Bearer <token>` header exactly against the extracted token and returning 401 otherwise; bind strictly to `127.0.0.1:8080` and launch the server as a background process so it stays up for external verification.