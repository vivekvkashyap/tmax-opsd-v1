Start by inspecting the SQLite schema at `/home/user/audit.db` (`sqlite3 /home/user/audit.db .schema`) to see how `employees`, `transactions`, and `access_logs` relate via `emp_id`. The classic bug this task guards against is joining `transactions` and `access_logs` directly on `emp_id` in one query — that produces a cross-product (rows = tx_count * access_count) and inflates both sums. Avoid this by computing each aggregate independently, e.g. two scalar subqueries: `SELECT (SELECT COALESCE(SUM(amount),0) FROM transactions WHERE emp_id=?), (SELECT COUNT(*) FROM access_logs WHERE emp_id=?)`. You may cross-check a few known employee IDs against `/app/audit_oracle <id>` output to sanity-check your query logic, but implement your own SQL in the Python service — do not call or embed the oracle binary. Build the Flask/FastAPI app with a single `GET /api/audit/summary?emp_id=` route, check the `Authorization: Bearer ComplianceAudit2024` header first (401 on mismatch), then validate `emp_id` is an integer (reject non-numeric input like `1 OR 1=1` with 400/404 rather than letting it hit SQL), and always use parameterized queries (`?` placeholders) — never string-format `emp_id` into SQL. Bind the app to 127.0.0.1:8080 and launch it as a background process so it stays up after your turn ends.
