Work outside-in: get one backend instance serving `/ping` directly before wiring the load balancer and cron. First fix `gunicorn_conf.py` so `bind = os.environ.get("BIND_SOCKET", "<some-default>")` instead of the hardcoded broken path — grep the vendored app for how `bind` is currently read to match its style. Create two `systemctl --user` unit files differing only in an `Environment=BIND_SOCKET=/tmp/backend_N.sock` line (and working directory / gunicorn config path), then `systemctl --user daemon-reload`, `enable --now` both, and confirm with `systemctl --user status` that they're actually running and the sockets exist. For nginx, write a conf with `pid`/`error_log`/`access_log` pointed at writable paths under your home dir (not `/var/...`), an `upstream` block listing both `unix:/tmp/backend_1.sock;` and `unix:/tmp/backend_2.sock;` (default round-robin balances between them), a `server { listen 127.0.0.1:8080; }` proxying to that upstream, then launch with `nginx -c <path> -p <prefix>` in the background and curl `/ping` yourself to confirm 200s before moving on. Write `monitor.py` using `requests`/`urllib` with a short timeout, appending UP/DOWN based on status, and register it via `crontab -e`/`(crontab -l; echo "* * * * * ...") | crontab -` for every-minute execution — test it manually first since cron failures are silent.