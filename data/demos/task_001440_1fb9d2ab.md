Work through the bugs one at a time rather than rewriting the whole handler blind. First, sanitize the input array: iterate the raw `data` list and keep only elements that are actually `int`/`float` (in Python, reject strings and `None`, and exclude bools if you're being strict) before computing mean/variance — do this with a simple type-check filter, not a try/except around the whole pipeline. Second, fix the divide-by-zero/instability by adding `1e-6` to variance before you take `1.0/variance`, so identical or near-identical readings never crash. Third, before wiring up the binary, probe it manually from the shell with varying argument counts and values to learn its calling convention empirically — the task tells you it wants exactly 5 space-separated float args, so pad short cleaned arrays with trailing `0.0` and truncate long ones to the first 5, then parse its single stdout float. Multiply that by your inverse_variance for `final_score`. Finally, enforce the `Authorization: Bearer h3x-f0r3ns1cs` header check (401 on mismatch/missing) and bind the fixed service to `127.0.0.1:8000` on `POST /process`, launching it detached (e.g. nohup/tmux) so it stays alive for the verifier.