Do the package fix first and verify it in isolation before writing anything that depends on it. Open `/app/vendored/fastakit-0.1.0/fastakit/reader.py` and look for a `.strip(...)` call with an unexpected argument that limits which characters get stripped (rather than a bare `.strip()` that removes all whitespace/newlines) — restore it to plain `.strip()` so it only removes whitespace/newlines and doesn't eat legitimate sequence characters. Reinstall the package (`pip install -e /app/vendored/fastakit-0.1.0` or equivalent) and sanity-check by parsing a small FASTA file you write yourself, confirming the returned sequence has no embedded newlines and preserves all bases including ones at line boundaries. For `analyze.py`: read the sequence via `fastakit.read_fasta`, build the 4x4 A/C/G/T count matrix by sliding a window of 2 over the sequence (overlapping, not skip-by-2), compute per-nucleotide totals, then the expected count per pair as `(Count_X*Count_Y)/(L-1)` and sum `(obs-exp)^2/exp` skipping zero-expected terms, rounding chi2 to 4 decimals. Match the exact three-line output format (labels, comma-separated matrix in A,C,G,T row-major order). Test your script against several self-generated FASTA files and, if you have the oracle binary available, diff outputs on those same inputs to catch formatting mismatches (spacing, rounding direction) before finishing.