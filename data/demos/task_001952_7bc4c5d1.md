Build this as two artifacts that must compose reproducibly. In the Python script: load the CSV, compute the median of `age` on the raw column before any fillna (median of column with NaNs still present, using pandas' default NaN-skipping behavior), then fillna, then derive `rooms_per_age` and assemble X in the exact column order requested. For the pipeline, chain StandardScaler and RandomForestRegressor(random_state=42, n_estimators=50) inside sklearn's Pipeline (so scaling happens per-fold, not globally), build a KFold(n_splits=5, shuffle=True, random_state=42), and pass it as `cv=` to GridSearchCV with `param_grid={'rf__max_depth':[3,5,7]}` — note the pipeline step name prefix is required for GridSearchCV param keys. Write results using an f-string that rounds the CV score to exactly 4 decimals and match the two-line format precisely (no extra whitespace/blank lines). For the bash wrapper, export the four determinism env vars (OMP/OPENBLAS/MKL thread counts=1, PYTHONHASHSEED=42) BEFORE invoking python3, since a subprocess reads env at startup — setting them after or only in the python script itself won't affect BLAS threading. Test repeated runs to confirm bitwise-identical output before finishing.