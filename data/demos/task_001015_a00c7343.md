This task chains several independent sub-problems — solve and verify each before moving to the next. (1) Use `ffmpeg` to extract frames from `/app/diagnostics.mp4` (e.g. `ffmpeg -i diagnostics.mp4 frame_%04d.png`) and programmatically check each frame's pixels for pure red (255,0,0), counting exactly how many frames qualify — don't eyeball it, write a quick script since compression can make "almost red" frames ambiguous; use an exact-match or very tight tolerance check. (2) Use that count `N` to `git checkout feature/tz-fix-N` in `/home/user/analyzer`. (3) Run `go build` and read the actual compiler/linker error — a CGO link failure usually means either the C side has a header/syntax problem or CGO isn't actually needed for the logic and can be removed entirely; prefer removing the unnecessary C dependency if the Go code doesn't truly need it. (4) The DST bug is conceptual, not cosmetic: adding fixed 24-hour durations and checking `Hour()==0` can step over midnight on DST transition days when the day is 23 or 25 hours long. Replace the iterative loop entirely — compute next midnight directly via `time.Date(t.Year(), t.Month(), t.Day()+1, 0, 0, 0, 0, loc)`, which Go normalizes correctly without any loop. Rebuild to `/home/user/analyzer/analyzer` and test against a few known DST-boundary timestamps before trusting it against the fuzzer.
