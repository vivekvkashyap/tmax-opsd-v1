Do not guess the algorithm — determine it empirically and by binary inspection, since correctness on 100-length sequences depends on exactly matching a DFT-based computation the oracle can't run directly (it crashes above 32 chars). Feed `/app/seq_oracle` many short test sequences (length <= 32) covering single-nucleotide, all-same-nucleotide, and mixed patterns, and try to reverse-engineer the nucleotide-to-float mapping by testing simple cases (e.g., a sequence of all one letter should give a very characteristic, easily-checked spectrum). Also try `objdump -d /app/seq_oracle` or `strings`/`gdb` to look for floating-point constants embedded in the binary that reveal the per-base weight assignment. Once you're confident in the mapping and the overall algorithm shape (DFT → power spectrum → sum of |consecutive power differences|), implement it in a new Rust binary crate under `/home/user/analyzer/` using a crate like `rustfft` for the transform, being careful to match the oracle's exact transform convention (forward, no normalization) and the total-variation summation over frequency bins. Validate your Rust implementation against the oracle on many short sequences (length <= 32) where you can get ground truth directly, until outputs match closely, before running it on the length-100 sequences in `/home/user/sequences.txt` and writing results to `/home/user/output.txt`.