Sequence this as: link/config scaffolding, code fix, build, then systemd wiring, then verify live. Create the `/app/current` symlink to the vendored source, the `/app/etc` and `/app/var/log` directories, and write `edge-proxy.toml` exactly as specified. Before building, inspect the Rust auth code (likely `src/auth.rs`) for a logic bug that makes correct-token comparisons still return false or silently drop the connection — the classic pattern is a comparison whose true-branch was inverted or removed; fix the comparison so a matching token authenticates and the TCP session stays open for further commands, then `cargo build --release`. For the systemd unit, set `WorkingDirectory=/app/current`, `Environment=TZ=Asia/Tokyo` and `Environment=LANG=en_US.UTF-8`, point `ExecStart` at the compiled release binary, and redirect stdout/stderr to the log path via `StandardOutput=append:...`/`StandardError=append:...` (or shell redirection). Run `systemctl --user daemon-reload`, `start`, and `enable`, then manually curl the HTTP health endpoint and send a raw `AUTH <token>\nPING\n` over TCP (e.g. with `nc`) to confirm both protocols respond correctly before finishing.
