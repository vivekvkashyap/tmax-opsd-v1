Build and test each piece of the automation chain standalone before relying on the Git hook to trigger it end-to-end. First run `deploy_configs.py` manually to see exactly what it prompts and confirm it blocks on stdin. Write `auto_answer.py` using `pexpect.spawn(...)`, then `child.expect("Apply changes? \\[y/N\\]: ")`, `child.sendline("y")`, `child.expect(pexpect.EOF)` — test this script by itself first (`python3 auto_answer.py`) and confirm `deploy_success.flag` appears wherever `MICRO_DEPLOY_PATH` points, since hooks run in a stripped-down environment where env vars you set interactively won't carry over unless explicitly exported inside the hook script itself. Write `post-receive` as a bash script that reads stdin (`read oldrev newrev refname`), exports `MICRO_DEPLOY_PATH=/home/user/deployed_services`, then runs `git diff-tree -r --name-status --diff-filter=AM "$oldrev" "$newrev"` piped through `grep '\.yml$'` and `awk`/`sed` to strip directory and extension, appending each base name to `/home/user/hook_metrics.log`, then finally invoking `python3 /home/user/bin/auto_answer.py`. Remember `chmod +x` on the hook. Finally test the whole chain by committing `inventory.yml` in the workspace clone and pushing to origin — check both the flag file and the metrics log afterward rather than assuming success.
