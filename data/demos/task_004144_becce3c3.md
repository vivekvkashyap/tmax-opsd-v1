Read `/home/user/filter.conf` first to get the target version string (do not hardcode it), then write the C parser: open `repo.bin` in binary mode, verify the 4-byte `ARTF` magic, then loop reading a 1-byte record type and a 4-byte little-endian length via `fread` into a `uint32_t` (do not assume struct padding — read raw bytes or use `#pragma pack`/manual byte assembly to avoid endianness/alignment bugs). Read exactly that many payload bytes into a buffer. For type `0x01` records, parse the JSON payload — a hand-rolled minimal scanner that finds `"artifact_name":"..."` and `"version":"..."` substrings is sufficient; don't pull in a JSON library. Compare the extracted version to the value read from `filter.conf` with `strcmp`, and on match append a `name,version\n` line to `manifest.csv` via `fopen(..., "a")`. Skip type `0x02` records by advancing the file position by their length without interpreting the payload. Compile with `gcc parser.c -o parser` and run it once to produce the CSV; check for EOF/short-read conditions so a truncated record doesn't crash the parser.
