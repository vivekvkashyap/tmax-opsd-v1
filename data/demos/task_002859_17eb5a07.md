Tackle this in order. First, fix the Makefile's linking error: `evaluator.c` calls `pow()` from `math.h`, so the gcc invocation needs `-lm` appended (place it after the source file in the link line) — verify with `make` that `libeval.so` builds cleanly, then that `go build` succeeds too. Second, implement the Go logic in `main.go`: parse `/home/user/data/versions.txt` as SemVer 2.0.0 strings, implement correct precedence comparison (numeric major.minor.patch first, then pre-release identifiers rank lower than the same release without a pre-release, comparing pre-release fields per SemVer's dot-separated rules), and find the highest version satisfying `>= 1.2.0 AND < 2.0.0`. Third, read `base.txt` and `diff_<found-version>.txt`, parse `KEY=base,exponent` lines, and merge so diff entries override base entries for matching keys while unmatched keys from either file survive. Fourth, call `C.evaluate_power(C.double(base), C.double(exp))` via cgo for each merged key, marshal the sorted-key map to `/home/user/report.json` with `json.MarshalIndent` (2-space indent). Run with `LD_LIBRARY_PATH=. ./eval_tool` (or set it before `make`) since the binary needs to locate `libeval.so` at runtime.