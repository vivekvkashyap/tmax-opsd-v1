Write the orchestrator first and run it against the unfixed `algo.py` to observe the actual failure mode — don't guess the bug from reading code alone, reproduce it. The orchestrator should load `/home/user/tests.json`, call `longest_path(graph, start)` per test case wrapped in a try/except so a crash in one test doesn't kill the run, compare to `expected`, and write `{"<id>": true/false, ...}` (string keys) to `/home/user/results.json`. When you run it, watch for a KeyError or similar when the DFS recurses into a node that has out-edges pointing to a node not present as a key in the graph dict (a leaf never explicitly listed) — that's the edge case to fix, not the core longest-path recursion/memoization logic itself. Fix it minimally, e.g. by using `graph.get(node, [])` instead of `graph[node]`, so nodes with no outgoing edges default to zero further distance. Re-run the orchestrator after the fix and confirm all entries in `results.json` are `true`, then independently sanity-check with a small ad-hoc call like a single-edge graph to make sure the fix didn't change correct-case behavior.