Read `backdoor.cpp` carefully: identify that `X-CMD` is decrypted with a repeating-key XOR (ciphertext[i] ^ key[i % keylen]) — this maps to CWE-327 (use of a broken/risky cryptographic algorithm) since XOR-with-short-key is not real encryption. Determine the key length from the code (look at the modulus used when indexing the key, e.g. `% 4`). Extract the hex-encoded `X-CMD` cookie value from `traffic.log` (parse the `Cookie:` header line). Perform the known-plaintext attack yourself: convert the first 4 hex bytes of the ciphertext to raw bytes, XOR each against the corresponding byte of the known plaintext prefix `CMD_` to recover the key bytes one at a time (key[i] = ciphertext[i] ^ plaintext[i]), then reuse that key cyclically to XOR-decrypt the entire ciphertext. Implement this exact recovery logic in `/home/user/decrypt.cpp` (hex-decode, derive key from the known prefix, decrypt full buffer), compile with g++, and run it. Write the decrypted ASCII command to `recovered_command.txt` and the CWE id to `cwe.txt`.
