Part 1: use `ffmpeg` to extract frames (e.g. `-vf select='...'` or dump each frame as PNG) then check each frame for being fully black — either via `ffmpeg`'s `blackdetect`/`blackframe` filter, or by decoding pixel data and confirming every pixel is (0,0,0); count qualifying frames and write the integer to `/home/user/black_frames.txt`. Part 2: write `filter.c` that reads the whole payload file, locates the header/body separator (`\r\n\r\n` or `\n\n`), parses the `Content-Length` header value and compares it against `strlen`/byte-count of the body exactly (careful with trailing newlines and off-by-one), and separately scans the `User-Agent` header line for any of `$ | ; \``; exit 1 ("evil") if either check fails, else exit 0. Test against a small set of clean/evil samples you construct yourself, covering edge cases like extra whitespace, missing headers, and multi-value comparisons, before trusting the hidden corpus. Part 3: iterate `/app/incoming/` using a `while IFS= read -r -d '' file` with `find ... -print0` (to survive spaces), run `/home/user/filter "$file"`, then `mv` clean files into `/home/user/safe/` (created via `mkdir -p`) or `rm` evil ones, checking `$?` right after each filter invocation.