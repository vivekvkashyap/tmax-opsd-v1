Solve this as a multi-stage pure-awk/sort pipeline, verifying each stage's intermediate output before chaining further. Step 1: use awk on artifacts.csv (skip header) to compute efficiency = accuracy/training_time per run_id, then pipe through `sort -t, -k2 -gr` (general numeric, reverse) on that computed column and take the top 5 with `head -n 5` — print intermediate results to check the ranking looks right rather than trusting it blindly. Step 2: for those 5 run_ids, look up their rows in embeddings.tsv (pipe-delimited); a clean way is to build a small awk associative array keyed by run_id while reading embeddings.tsv first, then iterate your top-5 list and index into it, rather than trying to `join` two differently-delimited files. Step 3: compute the dot product against the fixed query vector [0.1,0.5,0.2,-0.1,0.8] purely with awk multiplication/summation across the 5 embedding fields. Step 4: track the max dot product and its run_id as you go (or sort the 5 results by dot product descending and take the top line). Write only `run_id,dot_product` to best_run.txt with no extra whitespace/newline issues, matching awk's default float formatting. Wrap all of this in a single bash script at the required path and test it runs cleanly via `bash /home/user/find_best_run.sh`.