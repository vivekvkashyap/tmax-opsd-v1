Split this into three independent phases and verify each before moving on. (1) Use OpenCV or ffmpeg to extract frames from `/app/experiment.mp4`, and for each frame find the brightest pixel's (x,y) location (e.g. `cv2.minMaxLoc` on the grayscale frame) to build a per-frame trajectory; fit low-degree polynomials (`numpy.polyfit`) of x and y against frame index, and dump the coefficient arrays to `/home/user/fitted_trajectory.json`. (2) In `/home/user/sim_source/integrator.cpp`, look specifically at the adaptive-step-size update: a numerically unstable RK integrator often scales `dt` linearly by the raw error ratio (`dt *= error/tolerance`) instead of the standard embedded-RK correction `dt *= pow(tolerance/error, 1.0/5.0)` (or similar order-appropriate exponent) — fix that formula so the integrator shrinks/grows the step correctly, then `make` using the provided Makefile. (3) Wire a cpp-httplib (or similar single-header lib) server bound to `127.0.0.1:8080`, add a `GET /simulate?t=<time>` handler that checks the `Authorization: Bearer sim-token-42` header and rejects otherwise, calls into your fixed simulator, and returns `{"x":..., "y":...}` JSON. Start it in the background and hit it yourself with `curl` to confirm before finishing.
