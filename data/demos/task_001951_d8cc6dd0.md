Work through the pipeline component by component rather than guessing at the fix. First check every place a Redis connection is opened (`api.py` and `worker.py`) and make sure the port argument actually matches the Redis server's real listening port (`localhost:6379`) — a mismatched hardcoded port number is a classic config-drift bug. Second, look closely at how `log_parser.py` unpacks the 16-character hex string with Python's `struct` module: the format string controls signedness per field, and a lowercase field-type character parses a 32-bit value as *signed* two's-complement, which is why large `duration_ms` values come out negative — switch that field's format code to its uppercase (unsigned) equivalent so both `event_id` and `duration_ms` are read as unsigned 32-bit little-endian integers. After fixing, sanity-check by hand-running the parser on a hex string whose upper bytes would be negative if signed (e.g. all `f` bytes) and confirming the output value is a large positive number rather than -1. Restart/redeploy the Flask API and worker after editing so the running processes pick up the corrected code.
