Split this into the Rust tool and the SQL optimization file, and validate each independently. Scaffold with `cargo new access_auditor` in `/home/user/`, add `rusqlite` (with the `bundled` feature to avoid needing system sqlite3-dev) to `Cargo.toml`, and in `main.rs` open a connection to `compliance.db`. Use a parameterized query (`?1` placeholders, never string-interpolated SQL) to find employees where `department = 'Finance'`, then for each one run a parameterized count query joining `access_grants` to `resources` filtered on `sensitivity = 'HIGH'` and matching `emp_id` — or do it in one grouped join query for efficiency. Track the max count and its employee name as you iterate, then write exactly `NAME:COUNT` to `audit_result.txt` with no extra whitespace or trailing content, using `cargo build`/`cargo run` to actually generate the file (don't hand-write the expected output). Separately, write `/home/user/optimize.sql` with `CREATE INDEX` statements covering `employees(department)`, `resources(sensitivity)`, and both `access_grants(emp_id)` and `access_grants(resource_id)` (or a composite covering both) — double check column and table names match the schema exactly since the grader likely greps for these patterns.