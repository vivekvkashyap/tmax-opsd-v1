Inspect the schema first (`.schema backups` in sqlite3) to confirm column names and nullability of `parent_id`. For part 1, write a recursive CTE (`WITH RECURSIVE chain(id) AS (SELECT 1 UNION ALL SELECT b.backup_id FROM backups b JOIN chain c ON b.parent_id = c.id)`) then sum `size_bytes` for backups whose id is in that chain — don't forget to include the root row itself. For part 2, use a window function like `ROW_NUMBER() OVER (PARTITION BY job_name ORDER BY size_bytes DESC, backup_id DESC)` and filter to rank 1 per job, which naturally resolves ties by picking the highest backup_id (a naive `MAX(size_bytes)` GROUP BY won't tell you which backup_id owns that max, and a plain MAX(backup_id) tiebreak requires the ORDER BY to be exactly size then id, both descending). Execute both queries from Python via the `sqlite3` module only, build the `largest_per_job` dict from query results, and write the final structure to `/home/user/summary.json` with `json.dump`, then run the script yourself to confirm the file is produced correctly.
