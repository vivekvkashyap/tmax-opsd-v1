Start by recovering the threshold from the audio, since the filter logic depends entirely on it — transcribe `/app/field_notes.wav` with a tool like `whisper` (or `ffmpeg` to convert then a speech-to-text tool), listening carefully for the exact numeric variance cutoff and which conditions (NaN/Inf) must also cause rejection; don't assume a round number, verify what's actually said. Compile the profiler with `gcc -O2 -o /home/user/seq_profiler /app/src/seq_profiler.c -lm`. Then write `/home/user/filter.sh`: run `/home/user/seq_profiler "$1"`, capture its stdout, extract the numeric/string value after `Variance:` with `awk`/`grep`/`sed`. Check for the literal strings `NaN`/`nan` and `Inf`/`inf` first (these must always reject regardless of numeric comparison, since a naive `bc`/`awk` numeric comparison on "NaN" or "Inf" text may not behave as expected), then use `awk` or `bc -l` for a proper floating-point comparison against the recovered threshold. Exit 0 for clean, 1 for evil. Validate against both `/app/corpus/clean/` and `/app/corpus/evil/` (if visible to you) before considering it done, since the hidden test corpus must be 100% correctly classified.
