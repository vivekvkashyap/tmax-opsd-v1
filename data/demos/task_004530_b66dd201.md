Build this as a small pipeline, not one monolithic script. First write a Python parser that splits `/home/user/raw_tickets.txt` on `===` and extracts Ticket ID / Email / Description per block using regex or line-prefix matching — be careful to strip whitespace consistently. For dedup, hash each description with `hashlib.sha256(desc.strip().lower().encode()).hexdigest()`, iterate in file order, and keep only the first record per unique hash (a dict keyed by hash preserving insertion order works well). For grouping, compare every remaining unique ticket pairwise with `difflib.SequenceMatcher(None, a, b).ratio() >= 0.85`, and merge transitively (union-find or iterative merging) since similarity isn't necessarily symmetric-transitive by construction — a ticket only similar to one already-grouped ticket should join that group. Sort each group's IDs alphabetically, and sort the list of groups by each group's first ID. Write counts to `/home/user/pipeline.log` in the exact `KEY: value` format specified, and write the JSON array-of-arrays to `/home/user/grouped_tickets.json`. Wrap the whole thing in `/home/user/run_pipeline.sh` calling your Python script(s), then actually execute it so the output files exist on disk.