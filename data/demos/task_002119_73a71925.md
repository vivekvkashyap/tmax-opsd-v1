Build and test the pure processing logic first with a standalone `go run` before wiring up systemd, so you can iterate quickly. For timestamp parsing, inspect each `reported_at` value's JSON type/shape: if it unmarshals as a number use it directly as Unix seconds; otherwise try `time.Parse(time.RFC3339, s)` and, if that fails, `time.ParseInLocation("2006-01-02 15:04:05", s, time.UTC)` — try RFC3339 first since it's a stricter format. For feature extraction, safely walk nested `config_data["features"].(map[string]interface{})["tls"]` with type assertions guarded by `ok` checks rather than panicking. Build the canonical string exactly as specified (pipe-separated, literal `true`/`false` for the bool) and hash with `sha256.Sum256([]byte(s))`, formatting hex with `fmt.Sprintf("%x", hash)` for lowercase. Group records by `server_id`, sort each group's snapshots by timestamp ascending, then walk sequentially comparing each hash only to the immediately preceding kept hash (not all previous hashes) to decide duplicates. Sort final output groups alphabetically by `server_id`. For deployment, `go build -o tracker`, write the systemd unit with `ExecStart=/home/user/app/tracker`, `WorkingDirectory=/home/user/app`, `User=user`, then `sudo systemctl daemon-reload && sudo systemctl enable --now config-tracker.service` and check `systemctl status` for success before finishing.