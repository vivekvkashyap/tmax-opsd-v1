Read /home/user/worker.sh closely to find the precision-loss bug before rewriting: it decays a value by repeatedly multiplying by 0.5 and rounding via `printf "%.1f"` inside the loop condition — rounding to only 1 decimal place means very small values (e.g. around 0.05) get rounded back up to a value that never crosses below the 0.01 threshold, so the loop spins forever. Fix this in worker_fixed.sh by either dropping the lossy intermediate rounding (only round for the final printed output, not for the value used in the loop/comparison) or by comparing with enough decimal precision in `bc` that rounding can't stall convergence — keep the same 0.5 decay semantics. For regression.sh, wrap a single invocation of worker_fixed.sh 1.0 in `timeout 2 ...`, check its exit status, and exit 0/1 accordingly. For fuzz.sh, generate 50 random floats in [0.0, 10.0] (e.g. with `awk` or `$RANDOM` scaled via `bc`), run worker_fixed.sh under `timeout 1` for each, track failures, and exit 0 only if all 50 succeed. Manually verify worker_fixed.sh terminates on inputs near the old failure point (e.g. 0.1, 0.05) before trusting the fuzzer.
