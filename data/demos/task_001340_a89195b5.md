Build the pipeline as one pass over CSV rows, applying transforms in the exact specified order. For normalization: lowercase, `unicodedata.normalize('NFKD', s)`, then `.encode('ascii','ignore').decode('utf-8')` to strip accents, then strip non-`[a-z0-9 ]` characters, then collapse whitespace and strip — order matters (NFKD before ASCII-fold is what strips accents correctly). Maintain a `deque(maxlen=5)` of normalized text across ALL rows (kept or dropped) as the lookback window; check membership in the window as it stood before appending the current row's text, to decide drop vs keep. For rolling stats, keep a separate buffer (max 3) of token counts from only KEPT rows, averaging and formatting with `f"{x:.2f}"` (not bare `round`, to avoid dropped trailing zeros). Write each kept row with the exact template, then append the three summary lines with correct kept/dropped counts. Trace the given example by hand before running, since exact string matching is required.