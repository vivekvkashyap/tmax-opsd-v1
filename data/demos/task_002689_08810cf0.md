Treat this as a numerical-linear-algebra pipeline plus a small bioinformatics tail, and get the ordering convention right before anything else — read `network.csv` and `source.csv`, collect the node name set, and sort it alphabetically once (e.g. `sorted(set(nodes))` in Python with numpy/pandas, or R) so every matrix row/column and the source vector use that identical index mapping. Build the adjacency matrix from the edge list (undirected, so set both `A[i][j]` and `A[j][i]`), the degree matrix `D` as the diagonal of row sums, and `L = D - A`. Form `M = (L - 2I) @ (L - 2I)` (matrix power 2, not squaring elementwise). Because `M` is ill-conditioned, solve with `numpy.linalg.pinv(M, rcond=1e-15) @ S` (or your language's SVD-based pseudo-inverse) rather than a direct solver — a naive `solve`/LU call will throw or blow up, which is the bug you're avoiding. Sort the resulting concentration vector descending, take the top two node names, then read the corresponding sequences from the FASTA file (parse `>header` lines and following sequence lines). Compute the Longest Common Substring with a standard DP or suffix-based algorithm — don't hardcode it. Round concentrations to exactly 5 decimals and match the required JSON key names exactly.
