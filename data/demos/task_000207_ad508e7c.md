Approach this as a recursive-CTE graph reachability problem, not a naive per-row loop. In your C++ program (linked with -lsqlite3), first run `CREATE INDEX idx_parent_id ON backups(parent_id)`. Then write a single recursive CTE: start from the base case of valid roots (parent_id IS NULL AND status='active'), recursively join backups whose parent_id equals a member already in the recursive set AND whose own status is 'active' (a chain breaks if any ancestor step lacks a valid active root or hits a cycle, since the recursion simply won't reach those nodes) — this naturally excludes chains rooted at a deleted/missing parent and excludes infinite loops disconnected from any real root, because SQLite's recursive CTE only expands from the anchor outward. Then compute the orphaned sum as: SUM(size_bytes) over all active backups whose id is NOT IN the reachable-from-valid-root set. Execute the query, write just the integer result to /home/user/orphaned_size.txt, and sanity-check your logic by hand-tracing a couple of the sample chains (a valid chain, a deleted-root chain, a missing-root chain, and the loop) against your query's output before finalizing.
