Isolate the bug before touching infrastructure: the root cause is that `.lines()` splits on every `\n`, including ones embedded inside a quoted CSV field, so a proper CSV parser (the `csv` crate) is required instead of manual line-splitting — read the whole request body and hand it to `csv::Reader::from_reader`, iterating `StringRecord`s so quoted multi-line fields are handled correctly, rather than pre-splitting the body yourself. Check the `Authorization` header first and return 401 immediately if it doesn't exactly match `Bearer data-science-token`, before doing any CSV work. For each record, pull `id`, `text_a`, `text_b` by column position or header name, compute Levenshtein distance (use the `strsim` crate's `levenshtein` function already referenced in Cargo.toml, or implement the classic DP edit-distance table yourself if needed), and format each line exactly as `[ID: {id}] Distance: {distance}\n`, concatenating in row order with no header line included. Rebuild with `cargo build --release`, restart via `start.sh` (nginx + backend), and test end-to-end with `curl` sending a raw CSV body containing an embedded newline inside a quoted field, confirming through nginx on port 8000 that the row is neither dropped nor corrupted.
