First localize the crashing sample deterministically, then bisect, then minimize. Reproduce the hang yourself: loop over all 50 files in `/home/user/samples/` running `cargo run -- <file>` with a hard timeout (e.g. `timeout 5s cargo run -- sample_N.bin`) so you can identify exactly which file hangs without blocking indefinitely. Once you have the offending file, use `git bisect start`, mark current `HEAD` (or `master`) as `bad` and a known-early commit as `good`, and let `git bisect run` drive it with a script that builds (`cargo build`) and tests the target file under a timeout, exiting nonzero if it hangs/times out and zero otherwise — this automates finding the exact commit that flipped behavior. Record the full 40-character hash from `git bisect` (`git rev-parse HEAD` at the bad commit) into `/home/user/bad_commit.txt`. For minimization, inspect the parser's TLV format (Type byte, 4-byte big-endian Length, Value bytes) and the diff introduced in the bad commit to see which record Type value it mishandles; then binary-search within the crashing file's byte stream to isolate the single minimal TLV record (just Type+Length+Value, no surrounding records) that alone reproduces the hang on the bad commit — verify by running the parser against only that extracted record before writing it to `/home/user/minimal_crash.bin`.