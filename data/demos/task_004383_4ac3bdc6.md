Since embedded newlines inside quoted CSV fields break naive `awk`/`grep` line splitting, use a CSV-aware tool (e.g. `python3 -c` with the `csv` module, invoked from inside your bash script) to properly parse `/home/user/incoming/data.csv` as records rather than lines, filtering to rows where `metric_name == "memory_leak_bytes"`. Truncate each `timestamp` to the top of the hour by replacing the minutes/seconds portion with `00:00` (string slicing on the fixed `YYYY-MM-DD HH:MM:SS` format works fine). Group by `(hour, hostname)` and compute the max integer `value` per group — do this either in the same Python invocation or by piping cleaned rows to `sort`/`awk`. Sort final rows first by `hour` ascending, then by `hostname` ascending, and write with the exact header `hour,hostname,max_value` to `/home/user/output/max_memory.csv`, no quoting. Make the script executable (`chmod +x`), run it once to produce output, then add a crontab entry for minute 0 of every hour running the script's absolute path (`crontab -e` or `(crontab -l; echo "0 * * * * /home/user/process_logs.sh") | crontab -`), and dump the crontab to `/home/user/cron_backup.txt` for verification.
