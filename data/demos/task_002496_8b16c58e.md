Write and unit-test the Go program against hand-crafted stdin input before touching the video at all — that isolates parsing/bucketing bugs from OCR noise. Parse each line with `strings.Fields` (exactly 5 tokens) and `time.Parse(time.RFC3339, ...)` for the timestamp; skip (don't error on) any line that doesn't split into 5 fields or fails timestamp parsing. Compute each event's 15-minute bucket by flooring its Unix time to the nearest multiple of 900 seconds and formatting that back to RFC3339 UTC. Aggregate into a `map[bucketStart]map[serverID]struct{total int; keys map[string]struct{}}`, incrementing total and inserting into the per-bucket-per-server key set for uniqueness. When emitting CSV, collect all (bucket, server) pairs and sort them with a comparator that orders by bucket string first, then server ID string, before printing rows with `len(keys)` as UniqueKeys. Only once this passes your own test inputs, extract frames from /app/config_dashboard.mp4 with `ffmpeg -i ... -vf fps=<n> frame_%03d.png`, OCR each frame with `tesseract` (config for monospace/single-block text improves accuracy), reconstruct the log lines in frame order, dedupe if the same line spans consecutive frames, and pipe the recovered lines into your compiled binary to produce /home/user/video_summary.csv.