Write `extractor.c` as a line-based parser: scan for the literal header `=== FILE: <filename> ===`, then read whitespace-separated two-character hex tokens on subsequent lines (using `strtok`/manual scanning + `sscanf("%hhx", ...)` or similar) until you hit the `=== END ===` footer, accumulating bytes into a growable buffer per file. The correctness-critical part is the substitution rule: scan the accumulated byte buffer for runs of `0x00`, and only when a run's length is exactly 3 replace those three bytes with `0xFF 0xFF 0xFF` — runs of length 1, 2, 4+ must be left untouched, so track run boundaries carefully (e.g. count consecutive zeros, and only flip when you close a run of length exactly 3) rather than doing a blind substring replace on `00 00 00` which would also incorrectly mutate the middle of a longer run. Write each corrected buffer to `/home/user/extracted/<filename>` (create the dir first). Finally, generate `/home/user/manifest.txt` by running `sha256sum` over the extracted files (only basenames, two-space separator as sha256sum default produces) and sort the lines alphabetically by filename before writing.