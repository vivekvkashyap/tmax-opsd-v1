Write straightforward line-based processing in Go rather than overengineering. Read `/home/user/app.log` line by line; for each line use a regex like `(PASSWORD|API_KEY)=([A-Za-z0-9]+)` to find credential assignments, and replace only the captured value (not the key name) with `***` using `regexp.ReplaceAllString` with a backreference/capture-preserving replacement — write the line unchanged if no match, preserving original order and any surrounding text exactly, to `/home/user/app_redacted.log`. Simultaneously, for lines where a credential match was found, extract the IP via a regex like `IP=(\d+\.\d+\.\d+\.\d+)` and collect it into an ordered set (e.g., a slice plus a `map[string]bool` seen-tracker) so each offending IP appears exactly once — do not add IPs from lines with no leaked credential (e.g., a plain `/status` or `/login USER=` request must be excluded even though it has an IP). Emit one `iptables -A INPUT -s <IP> -j DROP` line per unique flagged IP to `/home/user/block_ips.sh`. Compile/run with `go run sec_tool.go` and inspect both output files against the original log before considering it done.
