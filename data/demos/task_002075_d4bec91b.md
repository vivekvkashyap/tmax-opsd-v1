OCR the note first with `tesseract /app/calibration_note.png stdout` and read the extracted text carefully to get the base Tikhonov lambda and the per-parameter relative weights for [k1, k2, A0] — these numbers are the missing piece that makes the fit well-posed, so don't hardcode arbitrary regularization values. Then build the Python fit: define the ODE system (dA/dt=-k1*A, dB/dt=k1*A-k2*B, dC/dt=k2*B) and integrate it with `scipy.integrate.odeint`/`solve_ivp` given trial (k1, k2, A0), compute the modeled signal as ε_A*A+ε_B*B+ε_C*C using the given absorptivities, and form residuals against the CSV's observed signal. Instead of an unregularized `scipy.optimize.curve_fit` (which is what's failing due to near-collinearity between the parameters), implement Tikhonov/ridge-regularized least squares yourself: minimize sum(residuals^2) + lambda * sum(W_i * (param_i - param_i_prior_or_0)^2) using `scipy.optimize.least_squares` or `minimize`, where the residual vector you pass in is the data residuals concatenated with sqrt(lambda*W_i)*param_i terms (standard Tikhonov augmentation), using the lambda and W weights you OCR'd. Use reasonable positive initial guesses and bounds (k1,k2>0, A0>0) to help convergence, then write the fitted k1/k2/A0 to `/home/user/results/params.json` with exactly those three keys. Validate your fit's sanity by checking the residual sum is small and the parameters land in a physically plausible range before finishing.