Scaffold with `cargo init` inside /home/user/pipeline and add `csv`/`serde` via `cargo add`. Parse raw_products.csv, tokenize each description by lowercasing and splitting on whitespace into a HashSet<String> per row. For deduplication, compute Jaccard similarity (intersection size / union size) for every unordered pair of ids, keep pairs strictly greater than 0.75, always emit the pair with the smaller id first, and sort output rows by id1 then id2 before writing duplicates.csv. For KNN imputation, separate rows into "known price" and "missing price" sets; implement LOOCV over K in {1,2,3} using only the known-price rows — for each held-out known row, find its K nearest neighbors (by Jaccard similarity) among the *other* known rows, break similarity ties by preferring the smaller id, average their prices, and accumulate MAE per K; pick the K with lowest MAE (ties go to the smaller K). Reuse that same K to impute each missing-price row from its neighbors among all known-price rows. Format every price with exactly two decimals (e.g. Rust `format!("{:.2}", price)`), sort the final rows by id, and write cleaned_products.csv. Compile and actually run the binary so both output files are produced on disk, not just written as code.