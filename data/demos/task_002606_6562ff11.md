Parse the log with a regex that captures timestamp, node name, temperature (which may literally be the string `MISSING`), and load, e.g. `\[(.*?)\] \w+ - (\w+) - T:(\S+) L:(\S+)`. Filter to rows where node equals `omega`, preserving log order (which is already chronological). Convert timestamps to a sortable/numeric form (e.g. `pandas.to_datetime` or `datetime.strptime`) so you can interpolate against elapsed time, not row index — this matters because the gaps between omega readings aren't uniform. For each run of consecutive `MISSING` values, find the nearest valid reading before and after (by time), and compute linear interpolation as `T_before + (T_after - T_before) * (t_missing - t_before) / (t_after - t_before)` for each missing point's actual timestamp. Round the result (interpolated or original) to exactly 2 decimal places for output, but keep `load` exactly as it appeared in the log (as a string, don't reformat it). Write the CSV with header `timestamp,temperature,load`, one row per omega reading in original order, and verify your output against the input log by hand for at least the first couple of interpolated rows.