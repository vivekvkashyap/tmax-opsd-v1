This is an adversarial-corpus C parsing task, so assume the evil inputs are crafted to defeat naive checks: when validating `payload_length` (a little-endian uint32 read from bytes 4-7), do the size arithmetic in a wider type (e.g. `uint64_t` or `off_t`) before comparing to the actual file size — computing `9 + payload_length` in 32-bit arithmetic can overflow and wrap around, letting a malicious huge `payload_length` bypass your bounds check and cause an out-of-bounds read inside the mmap'd region. Always confirm `actual_file_size == 9 + (uint64_t)payload_length` using the real `fstat` size before touching payload bytes, and bail out (skip, not crash) on any magic mismatch, truncated file, wrong checksum (recompute the XOR fresh over exactly `payload_length` bytes), or oversized/mismatched length. Recurse with `opendir`/`readdir`/`stat` (or `nftw`), following into subdirectories but treating symlinks and non-regular files carefully. Use the same tmpfile-then-`rename()` atomic-write pattern as elsewhere for safety. Test your compiled binary directly against `/app/corpus/clean/` and `/app/corpus/evil/` before assuming correctness. For the video, extract frames with ffmpeg (e.g. `fps=` filter or per-frame PNGs), crop or index the exact center 10x10 pixel block per frame, convert to grayscale, average it, and compare against the 200 threshold — write your own frame-by-frame count rather than assuming a number.