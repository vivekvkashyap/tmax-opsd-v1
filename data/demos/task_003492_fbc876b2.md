Start by extracting the exact rule text from `/app/rules.png` (e.g. `tesseract /app/rules.png -` ), then re-read the OCR output carefully and correct any obvious OCR mistakes (misread digits, symbols) by cross-checking against context before treating any threshold or default value as ground truth — don't guess or invent values not actually present in the image. Once you have the real syntax (`REQ <target> [RATE <limit>] [EXPR <num1> <op> <num2>]`), implement `parser.sh` as a state-machine-style parser using positional argument parsing (`set -- $1` after stripping the leading `REQ `, or a `case`-driven loop) rather than one big regex, since the grammar has multiple optional clauses in a fixed order. Validate each token against the rules you extracted (target format, limit bounds, disallowed operator) in the order given by the image, returning the corresponding single-line error JSON the instant a rule is violated, and only compute/emit the success JSON if all checks pass. Use `bc` or arithmetic expansion for the EXPR evaluation, guarding division specially since it's explicitly disallowed per the image. Because grading is bit-for-bit fuzz equivalence against a hidden reference, test your script against a wide range of hand-constructed inputs (missing clauses, boundary limit values, invalid targets, each operator) and diff the JSON output character-by-character, not just visually.