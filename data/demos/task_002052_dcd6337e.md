First recover the PASSPHRASE by transcribing `/app/interception.wav` — use a local speech-to-text tool (e.g. `whisper`/`ffmpeg`+ASR if available) rather than guessing; trim whitespace/punctuation so you get a clean lowercase word sequence. Then build the C TCP server in stages: (1) a socket loop that accepts connections on 127.0.0.1:8080 and reads lines until it has FILENAME/HASH/PAYLOAD; (2) a strict line parser — reject anything malformed with "ERROR\n" rather than crashing; (3) base64-decode the payload (roll your own or use a small decoder, since libssl's BIO helpers are fiddly); (4) validate the filename BEFORE touching the filesystem — reject any name containing `/` or `..` immediately, discarding the payload, since path traversal must be blocked prior to any write; (5) compute SHA256 over decoded-payload-bytes concatenated with PASSPHRASE (order matters: payload first, then passphrase) using OpenSSL's EVP SHA256 API, hex-encode it, and compare case-sensitively/exactly against the client-supplied hash; (6) only on full success write the file into `/home/user/uploads/` and append `Written: <filename>\n` to `/home/user/server.log`. Compile with `-lssl -lcrypto`, create the uploads directory, and launch the binary detached (e.g. `nohup ./server & disown`) so it keeps listening after your shell exits.
