Confirm `backup_service.py` is actually reachable first (`curl 127.0.0.1:9090` should return its known response) so you know the baseline is healthy before you touch the C code. Open `health_monitor.c` and find the hardcoded target IP macro/constant (search for the literal `192.0.2.100`) — change only that value to `127.0.0.1`, leaving the port and the rest of the connection/health-check logic untouched. Compile with `gcc health_monitor.c -o monitor_daemon` inside `/home/user/restore/` and check the compiler reports no errors (this program uses raw BSD sockets, so link nothing extra is normally needed, but confirm). Export the two required environment variables (`HEALTH_PORT=8080`, `ALERT_SCRIPT=/home/user/restore/send_alert.sh`) in the same shell that launches the daemon — env vars set in one shell don't propagate to a background process started from a different shell/subprocess unless exported in that same context. Start `./monitor_daemon &` (or with `nohup ... &`) and verify with `ps`/a quick `curl` that it's actually listening on 8080 before relying on it. Finally run `curl http://127.0.0.1:8080/status` and redirect its raw output (`curl ... > /home/user/health_report.log` or use `-o`) so the log contains exactly what curl printed, not a paraphrase.
