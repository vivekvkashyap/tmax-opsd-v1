Express the whole "unauthorized access" definition as a single SQL query rather than post-processing in C++ loops. In `audit_checker.cpp`, use `sqlite3_open`/`sqlite3_prepare_v2`/`sqlite3_step` to run one query that: (1) starts from `access_events` joined to `entities` on `server_id` filtered to `is_sensitive = 1`; (2) uses `NOT EXISTS` (or a `LEFT JOIN ... WHERE ... IS NULL`) to exclude events where the user has a direct `HAS_ACCESS` relationship to the server, OR has a `MEMBER_OF` relationship to some group that itself `HAS_ACCESS` to that server (this is an OR of two permission paths — a common bug is only checking one path); (3) groups the remaining (unauthorized) rows by `user_id, server_id` with `COUNT(*)` as `unauthorized_attempts`; (4) wraps that grouped result and applies `DENSE_RANK() OVER (ORDER BY total_unauthorized DESC)` per user, where the ranking is based on each user's *total* unauthorized count across all servers, not the per-(user,server) count — compute a per-user total first, then join it back for the rank column while still emitting one row per (user, server) pair. Write the header plus rows as `user_name,server_name,unauthorized_attempts,rank_overall` to `/home/user/unauthorized_report.csv`. Compile with `g++ -std=c++17 audit_checker.cpp -lsqlite3 -o audit_checker` and run it, then eyeball the CSV against the join logic to sanity-check.