Work in clear stages and verify each before moving on. First, parse `/home/user/backup_catalog.json` and filter to just the `file_id`s whose `owner` is exactly `alice_admin`. Second, use `os.walk` (or `find`) over `/home/user/backup_vault/` to locate files whose basename matches one of those ids — since files can be nested, don't assume a flat structure. Third, for each match, open the file in binary mode and check only the first two bytes equal `\x1f\x8b` (read raw bytes, don't rely on file extensions or a library's higher-level gzip-open, since that may throw on truncated/corrupt files instead of cleanly reporting invalid magic — a plain byte comparison is the reliable check). Keep a list of only the valid, alice-owned files with their absolute paths. Write the CSV report with exact header `file_id,original_path` and one row per valid file, then build the tar with Python's `tarfile` module adding each file with `arcname=os.path.basename(path)` so the tar is flat (no full vault path baked in). Double check with `tar -tf` that only the two expected files appear and that files failing the magic-byte check or belonging to other owners are excluded.
