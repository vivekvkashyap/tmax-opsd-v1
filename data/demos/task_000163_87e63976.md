Chain three well-defined steps and verify each output before feeding it into the next. First extract the certificate's Common Name with `openssl x509 -in /home/user/policy.crt -noout -subject` (or `-subject -nameopt RFC2253`) and parse out just the CN value precisely — don't include surrounding `/CN=` markup or trailing fields from the subject line. Second, write the Rust decoder: read `/home/user/payload.txt`, base64-decode it (the `base64` crate, or hand-roll decoding if crates are unavailable), then XOR each decoded byte with the CN bytes taken cyclically (`key[i % key.len()]`) — this is the core logic, and the most common mistake is using the wrong key encoding (e.g. including a trailing newline accidentally read from the cert output, or using UTF-16 instead of the raw ASCII bytes of the CN string) or off-by-one in the modulo indexing. Third, write the resulting decrypted bytes to `/home/user/decrypted_policy.txt` as a raw string with no extra trailing newline unless one was actually part of the decrypted plaintext (use a write call that doesn't auto-append `\n`, e.g. `write!` not `writeln!`, or check the last byte before deciding). Compile with `rustc` and run it, then manually inspect the output file's bytes (`xxd` or `cat -A`) to confirm no stray newline or encoding artifact was introduced.