Set up the venv first (`python3 -m venv /home/user/venv && source /home/user/venv/bin/activate && pip install numpy scipy matplotlib pandas`) and use that interpreter for everything downstream. Rewrite `analyze.py` rather than patching around its bugs: read the CSV from its real path (`/home/user/data/conversions.csv`, not a relative/wrong filename), aggregate `clicked` into successes and total trials via pandas, and compute the Beta posterior analytically as alpha_post = alpha_prior + successes, beta_post = beta_prior + (trials - successes) — do not leave the posterior equal to the prior. From the posterior, compute the closed-form mean `a/(a+b)` and variance `ab / ((a+b)^2 * (a+b+1))`, and dump all four values to `/home/user/artifacts/metrics.json` with the exact required keys. For the plot, the key fix is the matplotlib backend and call order in a headless environment: set a non-interactive backend (`matplotlib.use('Agg')`) before importing `pyplot`, remove any blocking `plt.show()` call, and call `plt.savefig(...)` while the figure is still populated (before `plt.close()`), then confirm the resulting PNG is non-trivially sized (well over a couple KB) rather than a blank white image.
