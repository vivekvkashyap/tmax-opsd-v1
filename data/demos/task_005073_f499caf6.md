Parse each line by splitting on whitespace into timestamp, value, and the rest (status message may itself contain spaces, so join remaining tokens back or find the value token then take everything after it as status). Parse the ISO-8601 timestamp manually (fixed-width substring slicing on `YYYY-MM-DDTHH:MM:SSZ` is simplest in C++ without a date library) and build the hour bucket by zeroing minutes/seconds, keeping it as a string key like the original format. Use a `std::map<std::string,...>` keyed by hour bucket (maps are sorted by key, giving you chronological order for free) storing a running sum, count, and fail-count struct; check `status.find("FAIL") != std::string::npos` for the fail counter — that's a substring check, not an exact match. After processing all lines, iterate the map in order, compute `sum/count`, and print with `std::fixed << std::setprecision(1)`. Append the summary log line with the total record count and `map.size()` for unique hours using ofstream in append mode. Compile with `g++ -O2 process.cpp -o process` and run it against the real telemetry file, then diff your CSV output visually against expectations for formatting edge cases (trailing newline, comma spacing).
