This is a reverse-engineering-plus-plumbing task, so verify each layer independently before wiring them together. First, inspect `/app/waf_engine` with `strings`, `file`, and `ltrace`/`strace -f`/`gdb` to discover the required environment variable name/value and the exact argv[1] flag it checks for (run it with various env/args and observe exit codes and stderr until it succeeds and echoes input back with a prefix). Do NOT modify the binary — only learn its calling convention. Second, write the C TCP server (`socket`/`bind`/`listen`/`accept` on 127.0.0.1:9090) that on each connection reads a line, `fork`+`exec`s the engine with the discovered env var and arg via pipes (`pipe`+`dup2` for stdin/stdout), writes the client's line to the child's stdin, reads the child's stdout, and sends it back over the socket; test it standalone with `nc` before adding more layers. Third, run `websockify 9091 127.0.0.1:9090` to bridge WebSocket to your raw TCP server, and test with a WS client. Finally configure nginx on 0.0.0.0:8080 to proxy `/api/waf` to `ws://127.0.0.1:9091` with `proxy_set_header Upgrade $http_upgrade` and `Connection "upgrade"`, and use an `if`/`map` block to check the `Authorization: Bearer WAF-2024-SEC` header and return 401/403 otherwise. Start all three processes in the background and confirm each layer with a manual curl/websocket test before declaring done.
