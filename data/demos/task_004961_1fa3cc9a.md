First fix the two JSON config files (upload_dir and redis_port) and bring the three services up with the startup script, then verify each with curl/redis-cli before touching the parser. For cbf_filter.cpp: read each file as raw bytes (not text), then parse fields in exact byte order — magic (4 bytes), version (2-byte LE uint16), flags (2-byte LE, skip), payload size N (4-byte LE uint32), payload (N bytes), trailing CRC32 (4-byte LE uint32). Reject a file the instant any check fails: magic mismatch, version not in allowed_versions, size > max_payload_size, or — critically — total file size not exactly equal to 12+N+4 (this catches truncated/garbage-appended evil files that a naive parser would silently accept or overrun on). Implement CRC32 yourself with the standard reflected polynomial table (or link zlib's crc32) and compare against the stored value over the payload bytes only. Walk the input directory recursively (std::filesystem::recursive_directory_iterator), and for every file that passes all checks, copy it flatly into output_dir with a `.verified` suffix appended to the basename — never mirror subdirectories. Finally write process_backups.sh to invoke cbf_filter, count `*.verified` files, and push that integer into Redis with `redis-cli SET verified_backup_count <n>`. Test against both well-formed and deliberately corrupted CBF files you construct yourself before trusting the filter.
