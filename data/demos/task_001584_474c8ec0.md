Use `git bisect` properly rather than manually eyeballing commits: `cd /home/user/geo-transform`, `git bisect start`, mark `v2.0` bad and `v1.0` good, then at each step git checks out a candidate commit — run `go run main.go /home/user/input.txt` and compare the printed sum's precision against the known-good high-precision behavior (the bug causes silent truncation, so compare the full decimal output, e.g. count significant digits or diff against the v1.0 output, rather than just checking the program ran without error) to decide `git bisect good` or `git bisect bad`. Automate this comparison in a small bisect-run script so you don't have to eyeball floating point output by hand — a script that runs the program, captures stdout, and exits 0/1 based on whether the value matches full precision expectations lets you use `git bisect run <script>` for speed and reliability. Once bisect converges, `git bisect log`/`git rev-parse` gives you the exact first-bad commit hash; write it, plus a trailing newline, to `/home/user/result.txt`, then `git bisect reset` to restore the working tree.