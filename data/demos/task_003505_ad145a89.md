Build this in the standard proto-first order: write `ocr.proto` with `syntax = "proto3"`, a `service OcrService { rpc ExtractText(ExtractRequest) returns (ExtractResponse); }`, a request message with `bytes image = 1;` and `uint32 crc32 = 2;`, a response with `string text = 1;`, and a `go_package` option pointing at your module's generated-code path — then run `protoc --go_out=... --go-grpc_out=...` to generate stubs before writing any server/client Go code, so both sides share the same generated types. In the server, compute the checksum with `hash/crc32` using `crc32.ChecksumIEEE(imageBytes)` and compare to the request's `crc32` field, returning `status.Error(codes.InvalidArgument, ...)` on mismatch before doing any OCR work. For OCR, write the received bytes to a temp file (`os.CreateTemp`), then shell out to `tesseract <tempfile> stdout` via `os/exec` and capture its stdout as the extracted text — don't try to parse tesseract's output format, just trust its raw text output. In the client, read `/app/invoice.png`, compute the same CRC32 over its raw bytes, dial `localhost:50051` with an insecure gRPC connection, call `ExtractText`, and write the response's `text` field verbatim to `/home/user/extracted.txt`. Start the server backgrounded before running the client, and if OCR output looks noisy, that's expected — the grader tolerates some character-level error.