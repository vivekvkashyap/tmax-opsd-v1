Two separate discovery steps feed one implementation. First, OCR or visually inspect `/app/format_schematic.png` (crop/zoom and read the text carefully, or use an OCR tool like `tesseract`) to extract the precise encoding rules — pay attention to exactly what counts as a valid byte pair, how the RLE multiplier syntax works, the multiplier's valid range, and every condition that makes input INVALID. Second, treat `/app/legacy_decoder` as a black box: run it on hand-crafted inputs (valid RLE sequences, odd-length hex, out-of-range multipliers, malformed markers) and observe its exact stdout/exit codes to confirm or correct your understanding of edge cases from the schematic — the binary's actual behavior is the ground truth if it ever seems to disagree with your OCR read. Only after both steps agree, implement `/home/user/fast_decoder.cpp`: fast, allocation-light decoding logic, printing decoded bytes as lowercase/uppercase hex (match the oracle) or `INVALID`+exit 1 on any malformed input. Compile it, then fuzz-test it yourself against varied random inputs before considering it done.
