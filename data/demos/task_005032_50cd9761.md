Since this is graded by fuzz-equivalence against the hidden binary, your goal is to make archiver.sh produce byte-identical output to /app/legacy_archiver across many random directory trees and configs — so invest first in black-box probing of the binary rather than guessing the logic. Run `strings /app/legacy_archiver` to find literal config key names and output format clues, then construct small hand-made config files and data directories and diff your script's guesses against the real binary's stdout for each one, iterating until they match exactly (also try `strace -f -e trace=open,openat,stat,lstat` on the binary to see which config keys and paths it actually touches, and what order it walks directories in). Key behaviors to nail down empirically: how include_ext/exclude_ext are parsed from the config (comment handling, whitespace, list separators), how base_backup_dir comparison decides LINK vs COPY (exact size+mtime match via `stat`), how excluded files are reported (SKIP vs omitted entirely), how symlinks behave depending on a follow_symlinks flag, and the final sort order of output lines (alphabetical by destination path). Implement the walk in bash using `find`/`stat -c` and sort the action lines before printing, matching the legacy binary's exact `ACTION PATH -> DESTINATION` formatting including spacing. Validate against many synthetic cases, not just one, before finishing.
