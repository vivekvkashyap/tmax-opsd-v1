Separate the two concerns cleanly. First, determine which edges are "active": extract a frame at each `timestamp_sec` from `/app/signal_log.mp4` with ffmpeg (e.g. `ffmpeg -ss <t> -i input.mp4 -vframes 1 -vf signalstats -f null -` and parse the printed `YAVG` value from stderr, or extract frames and pipe through the signalstats filter), keep only CSV rows whose YAVG at that second is strictly greater than 100, and build a filtered edge list — don't guess which rows are active, derive it per-row from the actual video analysis. Second, write a C++ program that builds an adjacency list from the filtered (undirected, presumably) edges, then for each stdin query line runs Dijkstra with a min-heap/priority_queue (O((V+E) log V)) rather than a naive O(V^2) scan, since up to 10,000 queries must run fast — precompute nothing per-query that can be shared, and reuse the same graph across all queries in one process rather than re-parsing per query. Output `-1` for unreachable pairs. Wire `/home/user/query_solver.sh` to compile once (skip recompilation if the binary is newer than the source) and exec the binary with stdin passed through directly.
