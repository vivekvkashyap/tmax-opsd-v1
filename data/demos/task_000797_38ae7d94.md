Build this Go program in verifiable stages rather than all at once. First write the PDB fixed-width column parser: for each line starting with `ATOM`, slice the string by exact character positions (columns 31-38, 39-46, 47-54, 1-indexed inclusive — remember Go string slicing is 0-indexed so subtract 1 from each bound) and `strconv.ParseFloat` after trimming whitespace; verify against the sample file by printing parsed coordinates before moving on. Second, compute the sample mean per axis and the unbiased (N-1 denominator) 3x3 covariance matrix from the (X,Y,Z) triples, then form `C_reg(lambda) = C + lambda*I`. Third, implement 3x3 matrix inversion analytically (via the adjugate/determinant formula) or use `gonum/mat`, and write a function `trace(lambda)` returning `Trace(C_reg(lambda)^-1)`; numerically integrate it from 0.1 to 1.0 using the composite trapezoidal rule with exactly 1000 intervals (1001 sample points, uniform step `(1.0-0.1)/1000`) — a common bug is off-by-one in interval count or wrong endpoint weighting (endpoints get weight 1, interior points weight 2, all times `step/2`). Fourth, compute the one-sample t-statistic on X using the same unbiased sample standard deviation divided by sqrt(n). Format both results to exactly 4 decimal places and write the two required lines to the output file, then build and run to confirm.
