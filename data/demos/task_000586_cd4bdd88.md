Work in stages and validate each before moving on. First extract frames from `/app/compliance_log.mp4` with `ffmpeg -i ... frame_%04d.png` at a reasonable frame rate, then run `tesseract` over each frame and grep for lines matching the `TXN: <SENDER> -> <RECEIVER> : <AMOUNT>` pattern with a regex, discarding OCR noise/duplicate frames — print what you recover before hardcoding anything so you can sanity check the parsed transactions. Load the recovered rows into a SQLite `transactions` table at `/home/user/audit.db`. For cycle detection, write a recursive CTE that walks sender->receiver edges exactly 3 hops and checks the 3rd hop returns to the 1st sender; dedupe rotations of the same cycle and pick a canonical starting node (e.g. alphabetically first) before sorting the result list. For the deadlock function, open two separate `sqlite3.connect()` handles, begin explicit transactions, and have each update the two rows of a discovered cycle in reverse order relative to the other connection so they block on each other; catch the resulting `sqlite3.OperationalError` (locking/timeout) to report `deadlocked`. Finally stand up a Flask/FastAPI app on `127.0.0.1:9090` with middleware that checks the `X-Compliance-Key` header on every route and returns 401/403 when absent or wrong, wiring `/api/cycles` and `/api/deadlock` to the logic above, and keep the server process alive (background/nohup or long-running foreground) for the verifier to hit.
