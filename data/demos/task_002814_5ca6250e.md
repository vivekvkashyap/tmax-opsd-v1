Do the cleaning and the reverse-engineering as two independent stages, and treat the oracle purely as a black box to probe with small, controlled experiments — never assume the formula. For cleaning: load telemetry.csv with pandas, fill NaNs in sensor_val with the column median (computed before clipping), then compute the 1st/99th percentile of the post-imputation column and clip to that range. For reverse-engineering the oracle: run `/app/feature_oracle <val> "<msg>"` several times varying one input at a time — e.g. fix the log_message and vary sensor_val to see if the relationship is additive or multiplicative (try val=0, which isolates the message-only term), then fix sensor_val and vary the message (try single words of different lengths, then multi-word messages, then messages with repeated word lengths) to infer whether the score depends on word count, total character count, or something like a sum of per-word length functions. Once you have a hypothesis, confirm it against several more oracle calls including edge cases (empty message, one very long word) before trusting it. Then implement the confirmed formula vectorized (pandas string ops / vectorized numpy, no per-row subprocess calls) across all 500k rows, and write id, cleaned_sensor_val, feature_score to a Parquet file with pyarrow.
