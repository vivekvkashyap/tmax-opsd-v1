Build and test each phase against the given sample data before wiring the CI script around it.
1. In `/home/user/data_project/migrate.py`, accept `sys.argv[1]`/`[2]` as input/output dirs, glob for `*.json` in the input dir, and for each: split `user_name` on the first space into first/last name (so multi-word last names stay together), convert `epoch_time` to a UTC year with `datetime.datetime.utcfromtimestamp(epoch).year` (or the timezone-aware equivalent), and write the V2-shaped dict.
2. Create the output path as `<output_dir>/<year>/<record_id>.json`, making the year subdirectory with `os.makedirs(..., exist_ok=True)` first.
3. Test by running the script against the provided `raw/` directory and inspecting the produced files match the V2 schema exactly (field names, no leftover V1 keys).
4. In `/home/user/data_project/setup_ci.sh`, use a heredoc to emit `.github/workflows/pipeline.yml` with a `process-data` job on `ubuntu-latest`, a step invoking `migrate.py raw processed` (or equivalent paths), and a separate step/job named `cross-compile-tools` whose `run:` is exactly `make build ARCH=arm64`. Make the script executable and run it once to confirm the YAML file lands at the right path with all required substrings present.
