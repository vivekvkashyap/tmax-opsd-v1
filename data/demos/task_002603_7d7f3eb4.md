Start by inspecting the vendored package's Makefile at `/app/csv-parser-2.1.3/Makefile` — it will show a C++ standard flag that's too old for the headers it ships (the library needs C++17 features). Edit that one flag rather than rewriting the whole Makefile, then run `make` in that directory to produce `libcsvparser.a`; if it still fails, check the actual compiler error for any other C++17-only constructs the flag change alone doesn't fix. For the C++ program, read the CSV with the vendored parser, then for each row seek to `offset` in the binary file, read exactly `length` bytes, and interpret them as little-endian float32s (use `reinterpret_cast` or manual byte assembly, being careful about endianness of the host). Apply the three rejection checks precisely: byte-length mismatch against `expected_dim*4`, any NaN/Inf via `std::isnan`/`std::isinf`, and L2 norm exactly zero (use exact float comparison, not an epsilon). Compile with `-I` pointing at the vendored include dir and `-L`/`-l` linking the static archive. Test against both `/data/clean_corpus` and `/data/evil_corpus` before finishing, since the pass criterion requires 100% correctness on both.