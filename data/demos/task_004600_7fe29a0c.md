Start by reading the existing `/home/user/app/video_server.sh` end-to-end to locate the actual leak and bugs rather than rewriting from scratch — the task requires keeping `nc`/`ffmpeg` as the core tools. For the memory leak, look for an unbounded global bash array/variable that accumulates request data or connection state across requests without ever being cleared or scoped locally; fix it by resetting/unsetting per-request state or using a subshell so state doesn't persist across the accept loop. For the timestamp bug, Bash arithmetic `$(( ))` uses signed 64-bit on most systems but the described bug is likely comparing timestamps as strings or via a construct that overflows on large microsecond values — replace naive `(( a > b ))` comparisons with explicit numeric handling (e.g. `awk` or `bc` for comparison/sorting if bash's own arithmetic is unreliable, or ensure you're not truncating to 32-bit anywhere). For JSON, don't hand-splice raw log text into `{...}` — escape control/special characters (quotes, backslashes, newlines) properly, e.g. via `jq -Rs` or a small escaping function, before embedding. After fixing, write `/home/user/app/regression_test.sh` that fires 100 requests to `/frame?ts=0.5` and asserts the daemon's RSS growth (via `ps`/`/proc/<pid>/status`) stays under 5MB, then start the daemon backgrounded on `127.0.0.1:8080` and confirm both endpoints work with `curl` before finishing.