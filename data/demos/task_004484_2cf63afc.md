For the PIN cracker, write a straightforward C loop over all 10000 four-digit values (0000–9999), applying the given modular formula to each with integer arithmetic, and print/write the first (only) match zero-padded to 4 digits — compile with gcc and run it rather than trying to solve the modular equation by hand, since brute force over 10k values is trivial and avoids sign/type errors. For the redaction step, use a regex that matches exactly 64 hex characters ([0-9a-fA-F]{64}) immediately following "Bearer " and replace only that captured token with the literal word REDACTED, preserving the "Bearer " prefix and the rest of each line untouched — test with Python's re.sub or sed/grep -E rather than naive string replace, and verify no 64-char hex string is left in the output. Finally set permissions with chmod 400 on the redacted file (owner read-only, no group/other access), and confirm with `stat -c '%a'` that it reads exactly 400, not 0400 with a leading character mismatch or a different mode from a symlink/dir.
