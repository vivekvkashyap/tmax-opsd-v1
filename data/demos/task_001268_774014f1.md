Build the supervisor as a simple restart loop, not a one-shot launcher: in `/home/user/app/supervisor.sh`, use a `while true; do python3 server.py; date +%s >> restarts.log; done` style pattern so that whenever `server.py` exits (crashes), the loop immediately relaunches it and appends the current UNIX timestamp — a naive attempt that starts the server once and just waits will never actually recover from `/crash`. Run the supervisor backgrounded (`&`) so it doesn't block. For `/home/user/deploy_check.py`, use `urllib.request` or `requests` with TLS verification disabled (e.g. an `ssl.SSLContext` with `CERT_NONE` and `check_hostname=False`, or `requests.get(..., verify=False)`) since the server uses a self-signed cert — an unmodified request will raise a certificate-verify error. Hit `/health`, then `/crash` (wrap in try/except and ignore the connection-reset error), `time.sleep(2)`, then `/health` again, recording each status code, and write `/home/user/app/diagnostic.json` with exactly the `check1`/`check2` keys. Make both scripts executable and don't touch `server.py` or the cert files.
