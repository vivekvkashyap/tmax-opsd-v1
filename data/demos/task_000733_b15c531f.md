Start by automating interaction with the interactive oracle rather than trying to read the stripped binary statically — use `expect` (or a pty-driven script) to send the passphrase `monitor_auth_99` after the "Enter Passphrase" prompt, then capture whether it prints `[VALID]` or `[REJECT]`. Run it against every sample in `/app/corpus_samples/clean/` and `/app/corpus_samples/evil/`, and diff the JSON contents of accepted vs rejected files to hypothesize rules (check file size, look for suspicious keys/values, look for characters that suggest injection). Confirm each hypothesized rule by constructing minimal synthetic JSON files that isolate one variable at a time and rerunning them through the oracle — don't finalize a rule from correlation alone. Once you're confident in the full rule set, port it directly into a Rust program at `/home/user/alert_filter.rs` that reads the one file-path argument, checks each condition, and exits 0 (valid) or 1 (invalid) — no interactive prompt needed in your replacement. Also write `/home/user/filter.toml` with the given TOML content, then use `setfacl -m u:nobody:r /home/user/filter.toml` and `chmod o-rwx /home/user/filter.toml` so ACL grants nobody read-only while base "other" bits are stripped; verify with `getfacl`.