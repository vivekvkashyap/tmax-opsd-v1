Since some lines contain malformed `\u` escapes that break `encoding/json`, extract only `timestamp` and `cpu_usage` with a targeted regex (e.g. `regexp.MustCompile(`"timestamp":\s*(\d+)`)` and similarly for `cpu_usage`) applied to the raw line text rather than unmarshaling the whole line — this sidesteps the parser failure entirely. Read all `.jsonl` files in the logs directory concurrently: spawn one goroutine per file, each sending parsed records (plus the raw line for hashing) over a shared channel or into a mutex-protected slice; use a `sync.WaitGroup` to know when all goroutines finish. For deduplication, hash each raw line with `sha256.Sum256` and track seen hashes in a `map[[32]byte]bool` guarded by a mutex (or dedupe centrally after collecting, single-threaded, to avoid needing fine-grained locking) — discard a record if its raw line's hash was already seen. After collecting unique records, sort by timestamp ascending with `sort.Slice`, then compute the rolling average over the current point and up to 2 previous points in sorted order (window shrinks near the start). Write the CSV with `encoding/csv` or manual `fmt.Sprintf("%.2f", ...)` formatting for both float columns, creating `/home/user/output/` first with `os.MkdirAll`. For scheduling, write `run_etl.sh` (`go run etl.go`) as executable (`chmod +x`), then add the cron line with `crontab -l | { cat; echo "*/15 * * * * /home/user/run_etl.sh"; } | crontab -` so existing cron entries aren't clobbered.
