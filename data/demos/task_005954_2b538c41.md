This must be done with pure shell/coreutils (no Python/Perl), so drive everything from a `while IFS=, read -r orig category newname` loop over `/home/user/docs_index.csv`. For each row: use `find /home/user/docs_archive -name "$orig"` to locate the file recursively (don't assume a fixed depth), `gzip -dc` it to decompress to stdout, pipe through `sed "s/\[MACRO:COMPANY_NAME\]/GlobalTech/g"` (escape the brackets, they're not special in sed but keep it literal), then redirect into `mkdir -p /home/user/new_docs/$category` followed by writing to `/home/user/new_docs/$category/$newname.txt`. Only process filenames that appear in the CSV — do not glob every .gz file in the archive, since there are decoy files (e.g. ignore_me.txt.gz) not listed in the index. After the loop, `tar -czf /home/user/release.tar.gz -C /home/user new_docs` so the archive root contains the `new_docs/` directory, and build `/home/user/migration.log` by collecting each `<newname>.txt` as you process it and piping the final list through `sort` before writing. Verify the CSV read loop doesn't choke on the trailing newline/quoting.