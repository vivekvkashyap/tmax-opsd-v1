Separate this into an OCR/parsing step and a C server step, and validate each in isolation first. Run `tesseract /app/topology.png stdout` to extract the raw text, then parse each line matching the `X->Y` pattern (single uppercase letters) into a list of directed edges — be tolerant of OCR noise (extra whitespace, stray characters) by validating each parsed token is a single uppercase letter and discarding malformed lines. Build the graph as an adjacency structure (e.g. a fixed 26x26 or dynamic array indexed by letter) and implement BFS from `src` to `dst` to get the shortest path (BFS guarantees minimum hops in an unweighted directed graph); track parent pointers during BFS to reconstruct the path, and return `{"path": [], "length": -1}` when `dst` is unreached. For the HTTP server, use raw `sockets` (`socket`, `bind`, `listen`, `accept` in a loop or with `fork`/threads per connection) to parse the request line, extract the query string parameters `src`/`dst` via straightforward string parsing (URL-decode if needed, but single letters won't need it), run the BFS, and hand-build the JSON response with the exact keys `path` and `length`, plus a `Content-Type: application/json` header and `200 OK` status line. Compile, launch in the background, and test with `curl` against a couple of the given edges before considering it done.