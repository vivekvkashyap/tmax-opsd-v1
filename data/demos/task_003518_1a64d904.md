In process.cpp, first read all 50x10 values from dataset.csv into memory, then compute the sample variance (dividing by n-1) for each of the 10 columns independently, and pick the indices of the 3 highest-variance columns — keep them in their original left-to-right column order when building the reduced dataset (sort the *selected indices* ascending, not sort by variance value, before extracting columns). For the bootstrap step, implement the LCG exactly as given, using a `static uint32_t state = 42` that persists across all 1000*50 draws (do not reset it per iteration), and take `state % 50` as the row index for each of the 50 draws within an iteration; sum the 3 selected feature values for each drawn row and divide by 50 to get the iteration's means. Print each iteration's 3 means to the output CSV with exactly 4 decimal places, in order, one line per iteration, for all 1000 iterations — small differences in variance formula (n vs n-1), LCG state handling, or draw order will cause byte-level output mismatches against the exact-match grader, so implement precisely per the spec rather than approximating. Compile with `g++ -O3 process.cpp -o process` and run it from within `/home/user/analysis` so relative paths resolve, or use absolute paths throughout.
