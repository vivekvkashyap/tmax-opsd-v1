Work through this in dependency order. First fix and build the vendored `/app/tz-server-1.0` package: inspect the `Makefile`'s `CXXFLAGS` and cross-check against what C++ features `server.cpp` actually uses (things like `<thread>` or `auto` require at least `-std=c++11`) — add the missing standard flag rather than rewriting the source. Compile to produce `tz-server`, then launch two instances on ports 8081 and 8082 via the port as argv[1], each with `TZ=Europe/Berlin` set in its environment (e.g. `TZ=Europe/Berlin ./tz-server 8081 &`) so the JSON response reflects Berlin local time. Fix `monitor.sh` by replacing its relative `health.status` write with the absolute path `/home/user/data/health.status` (create that directory first), then run it in the background as a loop. Finally write an Nginx config at `/home/user/nginx.conf` that runs unprivileged: set `pid`, temp-path directives, and log paths all under `/home/user/data/` or `/dev/null` to avoid permission errors, listen on `127.0.0.1:8080`, proxy `/time` to an `upstream` block containing both tz-server addresses, and serve `/health` via `alias`/`root` + `try_files` pointing at the status file. Start nginx with `-c` pointing at your config and confirm all four background processes stay running.