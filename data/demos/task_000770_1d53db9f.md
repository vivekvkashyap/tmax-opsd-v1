Treat this as three chained sub-problems: OCR extraction, careful missing-data handling, and a vectorized-in-bash statistics pipeline. Run `tesseract /app/prior_specs.png stdout` (try preprocessing like grayscale/threshold with `convert`/`ImageMagick` if the raw OCR is noisy) and parse out the Alpha, Beta, and Similarity_Threshold values with a regex over the OCR text rather than hardcoding numbers you haven't verified — sanity-check the OCR result looks like plausible numbers before trusting it. In your awk/bash processing, do NOT let empty CSV fields silently become 0 via default numeric coercion — explicitly test each field with `if ($col=="")` and substitute the specified imputed value (impressions→100, clicks→0) before any arithmetic. Compute `posterior_ctr = (clicks+Alpha)/(impressions+Alpha+Beta)` and cosine similarity between `[emb_x,emb_y,emb_z]` and `[1,0,0]` using `awk` with `printf "%.5f"` or higher precision throughout intermediate steps to avoid rounding drift. Filter rows with similarity strictly less than the extracted threshold, sort by posterior_ctr descending then id ascending (`sort -t, -k2,2gr -k1,1n`), take the top 50, and wrap the whole pipeline in `/home/user/run_analysis.sh` writing to `/home/user/top_candidates.csv` with the exact header.
