Start by transcribing `/app/lab_notes.wav` (e.g. install and run `openai-whisper` or another ASR tool) and read the transcript carefully to pull out the exact chain letter, atom selection, Gaussian bandwidth, grid bounds, and HDF5 dataset name — do not guess these, they must come from what the audio actually says. Parse `/app/target_complex.pdb` yourself (ATOM records, columns for chain ID, atom name, x/y/z) to pull out only the specified chain's specified atoms; don't assume all chains/atoms are relevant. For the KDE, build a 50x50x50 grid spanning the exact bounds from the audio in each dimension, and at every grid point sum Gaussian contributions from each atom coordinate using the transcribed bandwidth as the kernel's sigma — get the Gaussian normalization and squared-distance formula right, since a small constant-factor error will blow the MSE threshold. Write the grid as a 3D float64 HDF5 dataset via the C HDF5 API (`H5Dcreate`/`H5Dwrite` with `H5T_IEEE_F64LE`, correct rank-3 dataspace) under the exact dataset name from the audio — a wrong name or dtype will fail even if the math is right. Also emit the chain's FASTA sequence separately. Validate your own output against a hand-computed sanity check (e.g. total density conservation) before finishing.
