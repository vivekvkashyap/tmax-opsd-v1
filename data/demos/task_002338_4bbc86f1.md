Inspect the schema and the current broken query first (`sqlite3 /home/user/audit.db ".schema"`) to see how role_hierarchy encodes "role_id inherits inherits_role_id", then replace the implicit cross-join with a proper recursive CTE: start from the roles row where role_name = 'SuperAdmin', then recursively find every role_id whose row in role_hierarchy has inherits_role_id equal to a role already found in the recursive set (i.e. walk up the inheritance chain to also capture roles that inherit through an intermediate role, transitively) — include the SuperAdmin role itself as the base case. Join the resulting role-id set against employee_roles and employees to get the distinct set of employees holding any of those roles, sorted by emp_id ascending. Test by running the fixed query directly against the DB and manually tracing the hierarchy chains before trusting the JSON output, since transitive inheritance bugs are easy to get partially right (e.g. only catching one level deep). Write the result to admin_audit.json in the exact list-of-dicts shape with `emp_id` and `name` keys only.