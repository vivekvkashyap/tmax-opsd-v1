First get the vendored package working: open `/app/spectro-tools-0.4.2/spectro_tools/baseline.py`, notice it calls `sparse.diags`/`sparse.eye` (SciPy sparse APIs) without the module being imported, add `import scipy.sparse as sparse` at the top, then install it locally with `pip install -e /app/spectro-tools-0.4.2` (editable/local install works fully offline since it's just local source plus already-available scipy/numpy — don't try to reach PyPI). Verify the fix by importing `spectro_tools.baseline` and calling `remove_baseline` on a dummy array in a REPL before writing the sanitizer. For the sanitizer, load each CSV's wavelength/intensity columns, run `remove_baseline`, then check for NaN/Inf with `np.isnan`/`np.isinf` on the whole corrected array, and separately compute the first derivative via `np.gradient(corrected, wavelength)` (or finite differences) and check `np.max(np.abs(derivative)) > 50.0` — reject if either condition trips, accept otherwise. Write one `FILENAME: ACCEPT`/`FILENAME: REJECT` line per file to the log, filename only (basename), and be careful your log format matches exactly since it's checked verbatim.
