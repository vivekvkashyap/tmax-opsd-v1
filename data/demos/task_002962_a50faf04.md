First reverse-engineer the schema with `sqlite3 /home/user/company.db ".schema"` to see how customers, orders, order_items, and products relate via foreign keys before writing any query. Then write `/home/user/generate_report.py` using `sqlite3` and `sys.argv[1]` for the category argument — use a parameterized query (`?` placeholder, never string-formatting the category into SQL) that joins customers -> orders -> order_items -> products, filters `products.category = ?`, groups by `customers.region`, and sums `quantity * price` as revenue. Cast the aggregated sum to `float` explicitly since SQLite may return it in a different numeric type. Sort the resulting list of `{"region": ..., "revenue": ...}` dicts with a compound key: revenue descending, then region ascending alphabetically as a tiebreaker (e.g. `sorted(rows, key=lambda r: (-r["revenue"], r["region"]))`). Write the result with `json.dump` to `/home/user/report.json`, then actually execute the script with the category `Electronics` so the output file exists on disk for verification — a common mistake is writing the script but forgetting to run it.