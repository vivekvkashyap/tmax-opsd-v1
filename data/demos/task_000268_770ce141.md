Write a Python script that walks `/home/user/storage_pool` recursively (e.g. `os.walk`) collecting every `*.vlog` path first, then processes each independently so a skip doesn't block others. For each file, check for a sibling `<name>.vlog.lock` in the same directory with `os.path.exists`; if present, skip entirely (leave both files untouched). Otherwise parse each line with a regex matching the exact verbose format (`[LEVEL] TIMESTAMP - User: X - Action: Y - Status: Z`) into a dict with keys level/timestamp/user/action/status, write one JSON object per line into a `gzip.open(..., 'wt')` file at the same path with `.vlog` replaced by `.jsonl.gz`, then only after that gzip write succeeds (flush/close it first) delete the original `.vlog` with `os.remove`. For the audit log, open `/home/user/audit_log.jsonl` in append mode and wrap the write in `fcntl.flock(f, fcntl.LOCK_EX)` before writing the JSON line (with original_file, compressed_file as absolute paths, and lines_processed as the count of converted lines) and `fcntl.flock(f, fcntl.LOCK_UN)` after — this matters even single-threaded because the verifier checks the exclusive-lock call is actually used correctly. Run the script and confirm no unlocked `.vlog` files remain except those with a lock file.
