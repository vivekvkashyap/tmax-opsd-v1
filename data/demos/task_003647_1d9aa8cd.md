Treat this as black-box reverse-engineering plus algorithmic optimization. Do not try to disassemble the stripped binary directly — instead probe it empirically: run `/app/motif_oracle /home/user/sample_edges.tsv`, inspect the input schema (`src`, `dst`, `type`, `weight`), and form hypotheses about what graph pattern produces the output node set. Build small hand-crafted TSVs that isolate one variable at a time (e.g. a graph with a single short directed cycle but edges below some weight vs. one where you bump every edge's weight up, or graphs with cycles of different lengths) and diff the oracle's output against your expectation each time — this lets you pin down both the structural pattern (what shape of subgraph qualifies) and any numeric gating condition (e.g. a weight cutoff, and whether it's inclusive or strict) purely from observed input/output behavior. Once your hypothesis reproduces the oracle's output exactly on several crafted cases, implement the matching logic in `/home/user/fast_motif.py` using adjacency dictionaries/sets for O(V+E)-ish neighbor lookups rather than nested loops over all node triples — build per-node adjacency filtered by whatever condition you discovered, then search for the pattern by walking those adjacency sets instead of brute-forcing all combinations. Print matched node IDs one per line, sorted ascending, matching the oracle's exact output format. Test against `sample_edges.tsv` first, comparing your script's output line-for-line against the oracle's before worrying about speed, then confirm it's fast enough on larger inputs.