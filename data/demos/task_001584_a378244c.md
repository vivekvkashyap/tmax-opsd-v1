Extract frames first with ffmpeg at 1fps to confirm the pipeline works end to end: `ffmpeg -i /app/video.mp4 -vf fps=1 /home/user/frames/frame_%04d.jpg`. For the C program, rather than parsing JPEG directly, convert each frame (or the whole video) to raw rgb24 via ffmpeg (`ffmpeg -i frame.jpg -pix_fmt rgb24 -f rawvideo frame.raw` or pipe rgb24 straight from the video) so your C code just reads fixed-size 3-byte-per-pixel binary blocks with `fread` and sums R/G/B per frame — this avoids writing a JPEG decoder. Compute each frame's embedding as mean R, G, B over all pixels, and brightness as `(R+G+B)/3` of those means. Find the most similar non-first frame by minimizing Euclidean distance between its embedding and frame 1's embedding (start from index 2, never compare frame 1 to itself). For the 95% CI on brightness, compute sample mean and standard error (`stddev/sqrt(n)`), then multiply by the correct Student's t critical value for `n-1` degrees of freedom at 95% confidence (not a fixed z=1.96, since results are checked against a scipy `t.interval` reference and n is likely small) — implement or hardcode a small t-table if you don't have a stats library, or approximate carefully. Write the JSON with the exact keys shown, matching the discovered filename string exactly.