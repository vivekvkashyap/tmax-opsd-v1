Write a C++ binary parser that operates on raw byte buffers with explicit bounds checks at every read, since malformed/truncated files must be safely skipped rather than crashing. For each `.bak` file found under `/home/user/backups/`: read 4 bytes and compare to `"BAK1"` (if mismatch, skip the file entirely, don't add it to the report); read a little-endian `uint32_t` entry count; then loop that many times reading a little-endian `uint16_t` path length, that many raw bytes as the path (not null-terminated — construct a `std::string` from pointer+length), a little-endian `uint32_t` file-size, then advance the read cursor past that many bytes of file data without copying it — always check the cursor doesn't exceed the buffer size before each read, and bail out of that entry loop gracefully (still reporting entries parsed so far) if a length would run past EOF. Classify a path as dangerous if it starts with `/` or contains the literal substring `../` anywhere (a plain substring search, not just a leading check). Accumulate per-file `total_entries` and the ordered list of dangerous paths (paths as found, not truncated/reformatted) into a JSON object keyed by base filename only (strip the directory), then serialize to `/home/user/report.json` — write straightforward JSON by hand or with a small library, matching the given structure exactly.