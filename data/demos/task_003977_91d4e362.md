This task is a determinism exercise, not a real Monte Carlo problem — the grading depends on your C code reproducing an EXACT bitwise-specified sequence, so implement the given LCG literally rather than approximating it with `rand()` or any other PRNG. Use `static unsigned long int state = 42;` at file scope, and for every single walker step (not once per trial) update `state = (1103515245UL * state + 12345UL) & 0x7FFFFFFF;` then derive `int bit = (state / 65536) % 2;`, stepping left on bit==0 and right on bit==1. Watch the integer types: use unsigned long (or an explicit 64-bit type) for `state` so the multiply doesn't silently truncate/overflow differently across platforms. Each trial starts the walker at x=30 and loops step-by-step until it hits x==0 (score 10) or x==100 (score 50); run exactly 100,000 trials sequentially (the state must carry over between trials, not reset each time), sum the scores, divide by 100000 as a double, and print with exactly 4 decimal places (`printf("%.4f", avg)`) to `/home/user/mc_pde_result.txt`. Compile with the exact `gcc -O3` command given and run it.
