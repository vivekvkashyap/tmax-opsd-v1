Set up the venv first (`python3 -m venv /home/user/venv`, then `pip install numpy scikit-learn` inside it) so the script's dependencies are isolated and reproducible. When implementing `analyze.py`, follow the RNG sequencing exactly as specified: load the CSV into `X`, call `numpy.random.seed(42)` exactly once immediately after loading and before the row loop — calling it inside the loop or in the wrong order will desynchronize the random stream and silently produce wrong stability scores. For each row `i`, compute cosine similarity to all other rows using all 10 dims to get the true nearest neighbor (break ties by smallest index via careful use of argmax on a stable-ordered array, not a naive max search that could pick an arbitrary tie). Then for exactly 100 bootstrap iterations, draw `numpy.random.choice(10, size=10, replace=True)` column indices, recompute cosine similarity restricted to those columns, find that iteration's nearest neighbor with the same tie-break rule, and tally matches against the original NN. Divide matches by 100 for the stability score, and dump the full `{"0":..., "99":...}` dict as JSON to `/home/user/nn_stability.json`. Double check your cosine similarity denominator uses norms of both vectors correctly, since a subtle off-by-axis bug there is the most common source of wrong results.