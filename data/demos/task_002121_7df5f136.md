First transcribe `/app/memo.wav` (e.g. with `whisper` or another available speech-to-text tool) and listen/read closely to extract the two spoken numeric parameters — the window size and the imputation value — verbatim as stated, including sign and decimal precision; do not assume round numbers. Then write `pipeline.go` reading CSV lines from stdin via `encoding/csv`, parsing each of the two columns with `strconv.ParseFloat`, and on parse error (empty string) substituting the imputation constant you transcribed. Maintain a fixed-size sliding window (a slice, dropping the oldest element once length exceeds W) of imputed (x,y) pairs, and starting exactly at the W-th row compute Pearson's r using the standard sum-based formula (`cov = W*sumXY - sumX*sumY`, etc.) rather than a two-pass mean/variance approach, since the reference implementation and any bit-exact fuzz comparison will use that exact arithmetic order — match it to avoid floating-point drift. Guard the zero-variance case (either denominator term <= 0, or the correlation is NaN) by printing `0.0000` instead of dividing. Print every result with `fmt.Sprintf("%.4f\n", r)` and nothing else on stdout. Build with `go build -o /home/user/pipeline pipeline.go` and smoke-test manually by piping a handful of known rows through it before trusting the fuzz suite.