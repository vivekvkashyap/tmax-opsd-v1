Work through the pipeline in dependency order and verify each stage independently before wiring them together. First, transcribe `/app/directive.wav`: convert it to a clean WAV/PCM with `ffmpeg` if needed, then run a speech-to-text tool (e.g. a local Whisper model via `pip install openai-whisper` or similar) and read off the spoken digits carefully — spoken-out digit words map to a single integer seed; don't guess, actually listen to or transcribe the audio. Second, compile `/home/user/mc_sim.c` with OpenMP enabled (`gcc -fopenmp -O2 mc_sim.c -o /home/user/mc_sim`) — omitting `-fopenmp` will make it single-threaded regardless of the thread argument. Third, in `profiler.sh`, time each run precisely (e.g. `date +%s.%N` around the call or `/usr/bin/time`), run thread counts 1/2/4 with the transcribed seed, capture stdout pi values, and compare wall-clock times to pick the fastest. Fourth, build the HTTP server as a real request/response loop (nc/socat in a `while true` loop) bound exactly to 127.0.0.1:8000, parsing the request line for `GET /data`, and emitting proper `HTTP/1.1 200 OK` headers plus a Content-Length/blank-line before the JSON body — many naive nc servers forget headers or don't loop, so a second request would fail. Confirm the pi value in the JSON matches exactly what /home/user/mc_sim outputs for the 4-thread run.