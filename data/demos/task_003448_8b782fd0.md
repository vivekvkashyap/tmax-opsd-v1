Build this as a clear pipeline of parse → fingerprint → dedupe → output, testing each stage independently. Parsing: read `data.csv` with the `csv` module honoring the header, `data.jsonl` line-by-line with `json.loads` mapping `uid`/`ts`/`msg` to the standard field names, and `data.xml` with `xml.etree.ElementTree` iterating `<record>` children and pulling `<id>`/`<time>`/`<text>` text content — keep a stable overall read order of CSV, then JSONL, then XML, since tie-breaking depends on it. Fingerprinting: apply `unicodedata.normalize('NFKC', s).lower()` to the message before fingerprinting, then iterate characters by index computing `ord(char) * primes[i % 25]` and sum mod `1_000_000_007` — implement this exactly, don't approximate with a hash function, since it's checked precisely. Dedup: group records by `(user_id, fingerprint)`; within each group keep the record with the minimum `timestamp`, breaking ties by first-encountered order (the read order above), and count discarded records per `user_id` to find the one with the most duplicates removed. Output: use `pandas`/`pyarrow` to write the kept records (with **original**, non-normalized message text) to Parquet with the exact column names/types specified, and `json.dump` the three stats fields exactly. Create `/home/user/output/` first if absent, and sanity-check row counts and stats by hand-tracing the small dataset logic before finishing.
