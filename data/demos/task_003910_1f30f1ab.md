Work through this as a dependency chain: build and cert generation must succeed before the services can start, and the reporter service must be ordered after the API service or it will race a not-yet-listening server. Compile with `g++ -std=c++17 /home/user/cost_api.cpp -o /home/user/cost_api -lssl -lcrypto -lpthread` (the header is already provided; just include it). Generate the self-signed cert non-interactively with `openssl req -x509 -newkey rsa:2048 -nodes -keyout /home/user/key.pem -out /home/user/cert.pem -days 30 -subj "/CN=localhost"`. Write both unit files under `/home/user/.config/systemd/user/`: `cost-api.service` with `ExecStart=/home/user/cost_api`, `Restart=always`, and `WorkingDirectory=/home/user`; `cost-reporter.service` as `Type=oneshot` running `/home/user/reporter.sh`, and — this is the crucial fix — add both `Requires=cost-api.service` and `After=cost-api.service` so systemd won't fire the reporter before the API is up. Reload with `systemctl --user daemon-reload`, then `start` both services (never use `sudo`/root — everything stays `--user`), and finally confirm `/home/user/metrics.log` contains the reporter's JSON payload exactly as sent.