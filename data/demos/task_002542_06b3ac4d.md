Unwrap the nested archive layer by layer exactly as described: `tar xzf legacy_project.tar.gz` to get `inner.zip`, `unzip inner.zip` to get `source_tree.tar`, then `tar xf source_tree.tar -C /home/user/project_root/` (creating that directory first) — verify at each step what actually landed on disk since nesting order/paths can surprise you. In `sorter.cpp`, use `std::filesystem::recursive_directory_iterator` to walk `project_root`, and for each `is_regular_file` entry open it in binary mode and read up to 4 bytes; if fewer than 4 bytes were read (`gcount()`), skip it. Compare the 4 bytes against each magic number as raw bytes (not as a null-terminated C string, since `\x89PNG` contains no null but comparing byte-for-byte avoids any signedness/encoding pitfalls) and copy matching files into the right `organized/<type>/` subdir (`std::filesystem::copy_file`, creating dirs with `create_directories`). Collect one log line per match using the relative path under `project_root`, then sort all collected lines alphabetically by path before writing `report.log` — build the list in memory first rather than writing as you traverse, since directory iteration order isn't guaranteed sorted. Compile with `g++ -std=c++17` and actually run the binary to produce the outputs.
