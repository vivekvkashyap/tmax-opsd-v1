Build and test each layer independently before wiring them into the deploy script. Write `analyzer.rs` using only `std`: parse the headerless CSV by splitting each line on commas, accumulate sums/counts for `cpu_percent` (2nd field) and `mem_mb` (3rd field) as `f64`, compute the means, and write `report.json` to the process's current working directory using `format!("{{\"avg_cpu\": {:.2}, \"avg_mem\": {:.2}}}", ...)` to guarantee exactly 2 decimal places — compile it standalone first (`rustc analyzer.rs -o analyzer`) and run it manually to confirm the JSON is well-formed and matches your own hand-computed averages. Then write `deploy.sh`: compile the Rust file, create `/home/user/planner_app/releases/$(date +%s)`, ensure `/home/user/planner_app/shared/certs` exists, generate `cert.pem`/`key.pem` with `openssl req -x509 -newkey rsa:2048 -nodes -days 365 -keyout key.pem -out cert.pem -subj "/CN=localhost"` guarded by `[ -f cert.pem ] || ...`, copy the binary into the release dir and `cd` there to run it so `report.json` lands correctly, update the `current` symlink with `ln -sfn`, kill prior `openssl s_server` processes (`pkill -f "s_server.*9443"` or similar) before launching a new one in the background, and finally run the script yourself to confirm `curl -k https://localhost:9443/report.json` returns the expected averages.
