First transcribe `/app/audio/lab_notes.wav`: install `ffmpeg` and a speech-to-text tool (e.g. `pip install openai-whisper`, or use `ffmpeg` to convert to 16kHz mono WAV then run whisper/vosk/speech_recognition against it) and read off the two spoken thresholds (max dominant frequency in Hz, max exponential growth rate) — do not guess these values, derive them from the actual transcript text. Then build `/home/user/evaluate_run.sh` as a thin wrapper around a Python helper that: loads the CSV with pandas/numpy, computes an FFT of the `value` column (via `numpy.fft.rfft`), takes `numpy.fft.rfftfreq` with the correct sample spacing from the `time` column, finds the peak magnitude bin excluding index 0 (the DC term) to get dominant frequency; separately fits `log|value|` vs `time` with a linear regression (or fits `A*exp(k*t)` directly via `scipy.optimize.curve_fit`) to estimate growth rate k, using the amplitude envelope (e.g. absolute value or Hilbert envelope) rather than raw oscillating values, since fitting the raw signal directly will give a garbage/near-zero slope. Compare both metrics to the transcribed thresholds with strict `>` and exit 1 if either is exceeded, else exit 0. Test against both `/app/corpus/clean/` and `/app/corpus/evil/` before finalizing.