Install `nlohmann-json3-dev`, `g++`, and `make` first. In your C++ program, use a regex (`std::regex` with a pattern like `\[\s*(-?[0-9.]+)\s*,\s*(-?[0-9.]+)\s*,\s*(-?[0-9.]+)\s*\]`) to scan the raw text and extract only well-formed `[X, Y, Z]` triples with all-numeric components, discarding malformed matches (like the line containing "abc"). Round each coordinate to 1 decimal place before deduplication, and store rounded triples in an `std::unordered_set` — you'll need a custom hash/equality (or encode each rounded triple as a string key) since `unordered_set` has no default hash for tuples of doubles. After dedup, compute the centroid by averaging each unique point's coordinates, then compute the max Euclidean distance from centroid to any unique point using `std::sqrt` over squared differences. Round both centroid components and max_distance to exactly 2 decimals when writing JSON to `/home/user/output_metrics.json`. Wire it all through a `Makefile` with `build`, `run` (depends on `build`), and `clean` targets so `make run` performs the whole pipeline end to end.
