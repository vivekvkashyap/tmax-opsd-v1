Work the repo in dependency order. First, run `pip install -r requirements.txt` (or try it) to surface the misconfiguration — inspect requirements.txt line by line for a misspelled package name and correct it to the real PyPI package before installing. Second, don't guess the calibration constant: use `git log -p -- solver.py` (or `git log --all --oneline` then `git show <commit>`) to walk the file's history and find the commit where the constant was hardcoded before it was refactored into an environment variable; read that literal value out of the diff. Third, run the script with `CALIB_CONST=<value> python solver.py` to reproduce the oscillation failure, then reason about the Newton-Raphson math: the cubic's derivative behavior near the current initial guess causes the iterate to bounce between two points instead of converging, so change only the starting point `x0` (try a negative value further from the oscillating basin) — do not touch f(x) or df(x). Confirm the internal `assert` passes, then run the fixed script and write its printed root to `/home/user/solution.txt`, rounded to exactly 6 decimal places using Python's `round(x, 6)` or `f"{x:.6f}"`.