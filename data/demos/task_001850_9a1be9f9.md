Transcribe `/app/audio/override.wav` yourself using an available speech-to-text tool to recover the exact override code phrase and incident time referenced in the log payloads — do not guess or invent a plausible-sounding code, use what the audio actually says, converting the spoken phrase into the exact token format it would appear as in a log payload string. Then build detector.py around three independent checks, each of which should short-circuit to exit(1) as soon as triggered: (1) substring search for the recovered override code across all TEXT_PAYLOAD fields; (2) parse each line's timestamp and USER_ID, group timestamps into 1-second buckets per user (e.g. floor each timestamp to the second), and flag if any bucket holds more than 4 events for the same user; (3) sort/iterate timestamps in order, compute the gap to the next line, and flag if a gap exceeds 10 seconds and the following line's ACTION is exactly "SYSTEM_REBOOT". Before finalizing, run your script against every file in `/app/corpora/clean/` and `/app/corpora/evil/` and confirm the exit codes match expectations exactly — the pass criterion requires 100% correctness on both corpora, so iterate on any misclassified files rather than assuming your first implementation is correct.