Build this as a pandas time-series pipeline rather than manual string math. First, extract valid lines with a regex anchored to the exact log format (`\[(\S+ \S+)\] (DEVICE_\d+) STATUS: \S+ TEMP: (\S+) HUM: \S+`), silently skipping non-matching lines, and parse the timestamp column with `pd.to_datetime`. Group by device ID, set the timestamp as index, and use `resample('1H').mean()` per device to get hourly temperature means — this naturally creates NaN gaps for missing hours. Fill gaps with `.ffill(limit=2)` so no more than 2 consecutive hours are forward-filled (hours beyond that limit stay NaN and should be dropped, not zero-filled). From the hourly series, resample again to `'1D'` taking `.max()` to get the daily maximum per device. Apply the quality gate by checking `if (daily_max > 50.0).any()` per device and dropping that device entirely from the output dict — do this check before building the final JSON, not after. Round all values with `round(x, 1)`, format dates as `YYYY-MM-DD` strings (e.g. via `.strftime`), and write the nested dict with `json.dump`. Validate by manually tracing through DEVICE_01's gap-filled hours to confirm your daily max matches your own by-hand calculation.