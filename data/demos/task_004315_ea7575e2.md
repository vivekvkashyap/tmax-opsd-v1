This task is graded by equivalence-fuzzing against a hidden reference implementation, so prioritize matching the exact spec's edge-case semantics over guessing behavior. First extract the subtitle stream with ffmpeg (e.g. `ffmpeg -i server_monitor.mp4 -map 0:s:0 out.srt` or similar) and inspect the recovered text yourself to confirm the wide-format lines look as described, rather than assuming their content. For the C++ processor: read stdin line by line, split into TIMESTAMP, REGEX_PATTERN, N, then N floats. Format each float to exactly 3 decimal places (use `std::ostringstream` with `std::fixed << std::setprecision(3)`, being careful with rounding of values like x.9995 and negative zero) and test that formatted string against the pattern with `std::regex_match` (full-string match, not `regex_search`) using ECMAScript syntax (the default flavor for `std::regex`). For every match, emit `TIMESTAMP,INDEX,FORMATTED_VALUE` preserving the 0-based index within that row's value list, and process rows independently/streaming so it scales to large inputs. Compile with a reasonably optimized C++ program (e.g. g++ -O2) to `/home/user/processor`. Build the bash pipeline as `ffmpeg ... | /home/user/processor > /home/user/anomalies.log` (extract-then-pipe, or extract-to-file-then-cat, whichever preserves exact line structure). Before finishing, hand-craft a few tricky test lines yourself (boundary decimals, non-matching values, edge regexes) and check your program's output makes sense against the spec's own worked example.
