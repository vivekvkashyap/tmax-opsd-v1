Scaffold with `cargo new polyfit` inside `/home/user`, add `nalgebra` as a dependency, then write code that: reads the CSV (skip header, parse two f64 columns), builds an Nx3 design matrix `A` with columns `[1, x, x^2]` and an N-length vector `y`, and solves the normal least-squares problem via a stable decomposition — use `A.svd(true, true).solve(&y, eps)` or `A.qr().solve(&y)` from nalgebra rather than manually inverting `A^T A` (which is numerically shakier but would still be acceptable if done carefully). Serialize the resulting 3 coefficients as a JSON array (e.g. with `serde_json` or by hand-formatting `[c0, c1, c2]`) to `/home/user/result.json`. Write `/home/user/pipeline.sh` to `cd` into the project, run `cargo build --release`, then execute the release binary; `chmod +x` it. Validate your own numbers by eyeballing that the fitted curve roughly tracks the dataset's clear near-quadratic growth before finishing, but derive the actual coefficients from your solver, don't hand-plug values.