Break this into independently verifiable stages and check each before moving on. First write `generate_hash(username, password)` in `auth_hash.py` as `hashlib.sha256((username + password + "QA_SECURE_SALT_2024").encode()).hexdigest()` — get the string concatenation order and salt exactly as specified, since this hash is checked byte-for-byte. Read `old_users` from `users_v1.db` with sqlite3, create `users_v2.db` with the specified `secure_users` schema, and for each row generate a fresh `uuid.uuid4()` string, keeping username and computing pass_hash via your function; commit and close the connection. For the Cython step, write a `setup.py` using `cythonize(["auth_hash.py"])` (or a matching `.pyx`) and `python setup.py build_ext --inplace` so the compiled `.so` lands next to the source and is importable as `auth_hash`. For benchmarking, import the plain module under one name/context and the compiled extension under another (e.g. force re-import or use `importlib` tricks, or simply time the `.py` version before compiling and the `.so` version after) — run each 50,000 times with `time.perf_counter()` around a tight loop, and write both durations plus a boolean comparing them into metrics.json in the exact key names requested. Verify each artifact (users_v2.db, the .so file, metrics.json) exists and is well-formed before finishing.
