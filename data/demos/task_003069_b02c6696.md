Treat `/app/legacy_processor` strictly as a black-box oracle you probe, not something to copy logic from — feed it crafted JSON lines (empty, single, overlapping, adjacent, non-overlapping, single-point intervals) via stdin and observe stdout to nail down exactly how it merges intervals and computes total span, before touching the Python fix. For the concurrency bugs: a hang at EOF is almost always worker threads blocked forever on `queue.get()` with no sentinel pushed after the last item — after reading all stdin, push a sentinel per worker so they exit their loop, then `join()` each before closing output. For ordering, either process sequentially or tag each item with its original line index and reorder outputs before printing, so one JSON line goes out per line in (no drops). Rewrite interval merging as the standard algorithm: sort by start, merge when `next.start <= current.end`, and sum spans across merged intervals for the total (confirm inclusive vs exclusive endpoint convention empirically against the oracle). Validate by diffing your script's output against the oracle's for the same randomized input across many runs before finishing.