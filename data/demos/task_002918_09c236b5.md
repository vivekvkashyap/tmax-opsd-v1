Build the normalization step first and check it in isolation before wiring up compilation and logging. In `pipeline.sh`, parse `server1.json` with `jq -r 'to_entries[] | "\(.key)=\(.value)"'` (values as plain unquoted text, numbers auto-stringified without quotes) and parse `server2.ini` by stripping `[section]` header lines and blank lines then keeping `key=value` lines as-is; for both, and also for `baseline.txt`, pipe through `sort` on the key (e.g. `sort -t= -k1,1`) so all three files end up alphabetically sorted with no quotes, no spaces around `=`, and a trailing newline — write the sorted baseline back into a `baseline.txt`-equivalent used for comparison rather than assuming the original file is already sorted. Write `calc_drift.c` to read two whole files into buffers and compute Levenshtein distance with the standard O(n*m) dynamic-programming edit-distance table over the full file contents (not line-by-line), then append `<target_name>,<distance>` to `drift_report.csv`. In `pipeline.sh`, compile the C program (e.g. `gcc calc_drift.c -o calc_drift`), then for each target file append the exact `[START] Processing <target_name>` log line, invoke `./calc_drift baseline_sorted.txt <target>.txt <target_name>`, and append `[END] Processed <target_name>` — run the whole script end-to-end so the CSV and log actually exist afterward.