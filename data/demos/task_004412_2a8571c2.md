Start by extracting the audio content: use `ffmpeg` to convert `/app/build_requirements.wav` to a clean WAV/mono/16kHz form, then transcribe it with an available speech-to-text tool (e.g. `whisper` if installed, or `pip install` one) rather than guessing — the exact byte-transform and checksum function are stated in the recording, so listen carefully for the transform order (a per-byte operation applied before a named checksum algorithm) and the specific constant/polynomial named. Implement exactly what you hear in Go: apply the byte-level transform to the full stdin stream first, then feed the transformed bytes into the named checksum function using its standard/IEEE parameters (don't invent your own polynomial or seed). Write `/home/user/hasher_test.go` using `testing/quick` to assert determinism (same input -> same output) and other checksum invariants (e.g. that flipping a byte changes the hash with high probability). Build a static CLI at `/home/user/build_hasher` that reads all of stdin, computes the checksum, and prints only the 8-hex-digit lowercase result plus newline — verify with `printf '' | ./build_hasher` and a few manual byte sequences before considering it done, since a fuzz-equivalence check against many random binary inputs will catch any deviation from the exact transform/algorithm order.