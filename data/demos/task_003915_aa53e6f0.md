This is a black-box behavioral-cloning task — don't guess the algorithm from intuition, derive it empirically. Start with `strings /app/firewall_validator_oracle` to look for hints, then use `objdump -d` or `gdb` to trace how the binary walks the input string and accumulates a score; alternatively (often faster and more reliable), run the oracle against many crafted single-character and short inputs (`./firewall_validator_oracle "A"`, `"a"`, `"Z"`, `"0"`, `"9"`, `" "`, punctuation, then longer strings crossing length 10 and 11) and infer the per-character rule and the length-based adjustment purely from input/output pairs — this differential approach is more robust than static disassembly alone. Implement the inferred logic in C, iterating each char and applying per-character deltas, then a length>10 adjustment at the end, printing the signed integer plus newline exactly like the oracle. Before finishing, write a quick fuzz script comparing your `/home/user/validator` against `/app/firewall_validator_oracle` on hundreds of random strings (including empty, very long, and boundary-length-10/11 strings) and fix any mismatches. Finally create `/home/user/validator.service` as a plain unit file with `ExecStart=/home/user/validator "test_string"`, `Restart=always`, `RestartSec=5` — do not attempt to `systemctl enable/start` it.