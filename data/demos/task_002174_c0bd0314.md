Build this pipeline stage by stage, verifying each artifact independently before chaining them in the orchestration script. First, read `/home/user/artifact.json` with a Python script and emit `/home/user/payload.s` that starts with `.intel_syntax noprefix`, `.global _start`, a `.text` section, then a `_start:` label followed by the instruction list from the JSON's `payload.instructions` array copied verbatim — do not hand-transcribe the instructions, generate them programmatically from the JSON so the pipeline stays data-driven. Second, write a Makefile that assembles with `as` and links with `ld` (no libc, no `-lc`, no dynamic linker) to produce a static freestanding ELF at `/home/user/payload_bin`; verify it runs and check its exit code with `echo $?` before continuing. Third, write `/home/user/notify.py` using the `websockets` library (install via pip if missing) to connect to `ws://localhost:8765` and send exactly `{"artifact_id": <id from JSON>, "exit_code": <int>}` — pull the artifact_id from the JSON rather than hardcoding it. Finally, assemble `run_e2e.sh`: start `ws_harness.py` in the background, sleep 1s, run the parser, `make`, execute `./payload_bin`, capture `$?` immediately after, call `notify.py` with that exit code, then terminate the background server gracefully (SIGTERM, not SIGKILL).
