Work through this as a systems-integration fix, not a from-scratch build. First read the Rust skeleton in `operator/src/main.rs` to see what's stubbed: env var loading, backup, and config templating are all partially there. Fix each piece in order: (1) load `MANIFEST_PATH`, `NGINX_CONF_PATH`, `BACKUP_DIR` via `env::var` and fail loudly if unset rather than silently defaulting to empty strings; (2) before overwriting the nginx config, copy the existing file to `BACKUP_DIR/nginx.conf.bak.<unix_timestamp>` using `SystemTime::now()` — do this copy BEFORE writing the new config, not after, or you'll back up the wrong content; (3) iterate the parsed manifest routes and emit one `location <path> { proxy_pass http://127.0.0.1:<port>; }` block per route inside the `server` block. Append the three env vars to `/home/user/.bash_profile` and create `BACKUP_DIR` first so the backup step doesn't fail. Write `manifest.yaml` exactly as specified. Build with `cargo build`, run the binary with the env vars set (source `.bash_profile` or export inline), then verify nginx actually reloaded by checking `curl -s http://127.0.0.1:8080/api/a` and `/api/b` return 200 from the two backend python servers — don't just trust that `nginx -s reload` exited 0.
