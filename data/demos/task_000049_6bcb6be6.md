Don't guess the query semantics from the name alone — pin them down with small, controlled black-box tests against `/app/backup_route_calc` before writing any C++. Feed it tiny hand-built graphs and issue Type 0 / Type 1 queries under a few hypotheses (e.g. "sum of outgoing edge weights" vs "count of edges" for Type 0; "shortest path" vs some other metric for Type 1), checking which hypothesis matches the observed output — including edge cases like a node with zero outgoing edges, an unreachable pair (what does it print, and in what format?), and `u == v`. Once confirmed, implement `/home/user/route_calc.cpp` reading the same stdin grammar, using an adjacency list with per-node summed weight for Type 0 and Dijkstra with a min-heap for Type 1, matching the unreachable-case output exactly. Compile with `g++ -O3 -std=c++17`, use fast I/O (`sync_with_stdio(false)`), since it will be fuzzed against graphs up to ~100 nodes with dozens of queries — re-verify your binary's behavior on fresh probe cases before finishing.