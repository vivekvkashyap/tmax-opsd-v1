Treat this as black-box reverse engineering, not guessing. Start with `file`/`strings` on the binary for hints, then design controlled test topologies rather than random ones: (a) two-node graphs where you vary only `latency_ms` to see how it maps to path cost, (b) graphs where you vary only `throughput_penalty` with latency fixed, to isolate how each field contributes to the edge weight (don't assume it's used alone — test combinations to see if they're summed, multiplied, or weighted differently). (c) graphs with several `is_backup_storage` nodes at different distances from the target, to see whether it always picks the globally cheapest one (consistent with a virtual super-source feeding Dijkstra) versus some other selection rule. (d) graphs engineered to have two paths of exactly equal cost, to discover the tie-breaking rule (check string/lexicographic ordering of the node-id sequence, not insertion order). Once you've pinned down the weight function, source-selection, and tie-break, implement it in Python with a standard shortest-path routine (e.g. Dijkstra over all storage sources simultaneously) and validate by diffing your script's output against the binary on many random topologies before finishing.
