Build the profiler in two clean stages: (1) collect N execution times by looping `for i in $(seq 1 $N)`, calling `python3 /home/user/spectroscopy_sim.py --seed "$i"`, and extracting the numeric value from the "Time: <value> ms" line (e.g. `grep -oP '(?<=Time: )[0-9.]+'` or `awk`), appending each to an array/file of samples. (2) Implement the bootstrap purely with shell/awk: for each of B resamples, draw N values with replacement from the collected samples (e.g. `awk` using `rand()` to pick random indices, or a bash loop calling `$RANDOM % N` to index into the samples array), compute that resample's mean, and append it to a list of B bootstrap means — do this efficiently since B can be 5000, so prefer a single `awk` script over spawning many subprocesses per resample. Sort the B means numerically (`sort -n`) and pick the elements at the 2.5th and 97.5th percentile positions (round the index) as your CI bounds. Format the final line exactly as `N=<N>, B=<B>, CI=[<lower>, <upper>]` with bounds rounded to 2 decimals (`printf "%.2f"`), write it to `/home/user/profile_results.txt`, make the script executable, and finally invoke it with N=50 B=5000 to populate the output file.