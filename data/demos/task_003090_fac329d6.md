Read `/home/user/deploy/daemon.c` carefully to find the exact crash trigger: it dereferences a NULL pointer when the line read equals `"POISON"`. Fix it by simply removing/skipping that dereference branch (e.g. treat "POISON" like any other line to ignore, `continue` the loop) rather than adding broad exception handling — keep the SHUTDOWN clean-exit path and normal "PROCESSED: <line>" logging behavior intact. Recompile with `gcc` to `/home/user/deploy/daemon_exec`. For the supervisor, write a bash loop: run `daemon_exec` in the foreground, check `$?` after it returns — if non-zero, append the exact warning line to `/home/user/deploy/supervisor.log` and loop back to restart it; if it exits 0 (clean SHUTDOWN), append the info line and `exit 0` out of the script. Since the daemon reads from a FIFO that blocks until reopened by a writer, make sure your restart loop reopens/re-execs the daemon promptly so a new writer to `data.fifo` after a `kill -9` is picked up. Test manually by starting the supervisor, writing test strings (including POISON, a kill, and SHUTDOWN) into the FIFO with `echo ... > /home/user/deploy/data.fifo`, and checking both log files.