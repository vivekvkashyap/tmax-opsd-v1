Work bottom-up through the dependency chain: C library, then linking, then tests, then orchestration — verify each layer builds before moving to the next. Build the CMake project with the standard out-of-source flow (`mkdir build && cd build && cmake .. && make`), and confirm `libcmath.so` actually lands in `c_math/build/` (check CMake's default output location; you may need `LIBRARY_OUTPUT_DIRECTORY` or just note wherever it lands and point build.rs there). Write `build.rs` to print `cargo:rustc-link-search=native=<absolute-path-to-build-dir>` and `cargo:rustc-link-lib=dylib=cmath` — use an absolute path since Cargo's working directory at build time won't be what you expect otherwise. For the proptest, use `proptest!` with `-1000i32..1000` strategies for `a` and `b`, calling the running server's `/add` then `/sub` endpoints via blocking reqwest and asserting the round-trip equals `a`. In `run_all.sh`, export `LD_LIBRARY_PATH` to include the build dir (needed at *runtime* for the compiled Rust binary to find the .so, separate from the build-time linking), start the server backgrounded, sleep, run `cargo test`, capture its exit code, kill the server, then write SUCCESS/FAILED based on that captured exit code — always exit 0 from the script itself.
