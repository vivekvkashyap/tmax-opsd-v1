In `/home/user/extractor.c`, first read `/home/user/backup.conf` line-by-line (stripping trailing newline/whitespace) into a small list of target extensions. Then `open()` and `mmap()` `/home/user/raw_data.bin` (do not use `fread`/`read` for the archive body, per the requirement) and walk it sequentially with a cursor: at each position verify the next 4 bytes equal the literal `BKUP` magic before proceeding — if they don't match (including at EOF-adjacent truncation), stop processing cleanly rather than reading garbage or crashing. Read the 2-byte little-endian filename length, then that many bytes as the filename, then the 4-byte little-endian file size, then that many bytes as the payload — advance the cursor by exactly header+filename+size fields each iteration. For each entry, check whether the filename's suffix matches any configured extension (simple case-sensitive suffix comparison, e.g. compare against `.log`) and if so, write only the raw payload bytes to stdout via `write()`/`fwrite()` — never write the magic, length fields, or filename itself. Compile with `gcc -O2 /home/user/extractor.c -o /home/user/extractor` and run `/home/user/extractor /home/user/raw_data.bin /home/user/backup.conf > /home/user/extracted_logs.bin`, then inspect the output file's contents to confirm only matching payloads appear, concatenated in archive order with nothing else interleaved.