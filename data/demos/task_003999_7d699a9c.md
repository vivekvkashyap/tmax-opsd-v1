Break this into two clear SQL/logic stages and test each with small ad-hoc queries against `/home/user/audit.db` before scripting the full pipeline. First find exact-2-hop users: join `system_graph` twice, `g1.source = user, g1.target = intermediate` and `g2.source = intermediate, g2.target = 'Customer_Data'`, and make sure you exclude users who reach `Customer_Data` directly in 1 hop or only via longer paths — a plain join naturally restricts to length-2 paths, so just filter target of the second hop. Second, for each such user's logs against `resource = 'Customer_Data'`, parse `details_json` with `json.loads` (or SQLite's `json_extract(details_json, '$.status')`) and keep only `status == 'SUCCESS'`, sorted by timestamp. Compute the rolling window count by, for each successful access, counting how many other successful accesses by the same user fall within `[timestamp-3600, timestamp]` (inclusive current row, prior events in that trailing window) — a two-pointer or nested comparison over the sorted timestamps works; don't use a fixed row-count window since events aren't evenly spaced. Flag a user if any such count exceeds 3. Write the flagged, deduplicated `user_id`s sorted alphabetically, one per line, to `/home/user/flagged.txt`.