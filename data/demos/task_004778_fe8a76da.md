Don't manually scan 10,000 lines — use delta debugging / bisection to isolate the crashing line efficiently: run `cargo run -- /home/user/logs/server.log`, note it panics, then binary-search the file (e.g. split it in half, test with each half via a temp file, recurse into whichever half still panics) until you converge on the single offending line; alternatively add a quick temporary `eprintln!` of the current line before each parse call and rerun to see the last line printed before the panic, or use `RUST_BACKTRACE=1`/`gdb`/`rust-gdb` to get the panicking line number directly and correlate it with input via a counter. Once found, write its exact text to `/home/user/bug_report.txt` as `Line content: <text>` with no extra formatting. Then look at `parse_line` in `/home/user/log-parser/src/main.rs`: it indexes `parts[0..3]` after splitting on spaces without checking `parts.len()` first, which panics on any line with fewer than 4 space-separated fields — fix it by checking `parts.len() >= 4` and returning `None` for malformed lines instead of indexing/unwrapping blindly. Add a `#[test] fn test_malformed_line` that calls `parse_line` directly with the malformed line you discovered and asserts it returns `None`. Confirm both `cargo run --` (exit 0, prints count) and `cargo test` pass before finishing.
