Work through this as three independent deliverables, each verified separately. (1) For the 502: read `/home/user/nginx/nginx.conf`, find the `proxy_pass` directive pointing at the wrong upstream port and change it to `127.0.0.1:8081`; then reload without sudo — since Nginx runs as the `user` account, use the project's own start/reload script or `nginx -s reload -c <path-to-conf>` against the same config/pid used to start it, and confirm with `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:8080/` returning 200. (2) For logrotate, write `/home/user/logrotate.conf` targeting `/home/user/nginx/logs/*.log` with `daily`, `rotate 7`, `compress`, and `missingok` stanzas, then sanity-check with `logrotate -d /home/user/logrotate.conf` (dry run) rather than assuming syntax is correct. (3) For the WAF, write `waf.sh` that takes `"$1"` (quote it — malicious strings will contain spaces/special chars) and greps for the literal patterns `<script>`, `UNION SELECT`, and `../` using fixed-string matching (e.g. `grep -F`) to avoid regex-metacharacter surprises; exit 1 on any match, 0 otherwise, and guard against empty/missing argument so the script never throws a bash syntax error.