Split this into the video-analysis part and the CSV-validator part and verify each independently before trusting it. For Part 1, extract frames with `ffmpeg -i /app/conveyor_belt.mp4 frame_%04d.png` (or read directly via OpenCV `cv2.VideoCapture` to avoid an extra encode/decode round-trip), then for each frame compute `mean(R) - mean(B)` over all pixels and flag frames where this exceeds 50.0 — confirm your frame indexing is 0-based and matches the extraction order before writing `cycle_frames.json`. For Part 2, implement `/home/user/quality_gate.py` as a straight-line series of independent checks (header string match, integer parsing + strict monotonic timestamp check, max consecutive-gap check, a sliding window of the last 3 `sensor_A` values with an average threshold — remember the first two rows are exempt, so only start checking once you have 3 rows — and a global mean of `sensor_B` checked with an inclusive range). Any single failing constraint should short-circuit to REJECT; only if the file passes every check should it print ACCEPT. Test your classifier by hand-writing a couple of clean and evil CSVs that each violate exactly one rule (rolling average edge case near exactly 100.0, gap exactly at 1000 vs 1001) since the hidden corpus will include such boundary cases.