Read the dataset line by line, lowercase each line, split on whitespace, and collect tokens into a `HashSet<String>` per line (duplicates within a line naturally collapse). Treat line 0's set as the fixed query set. For each other line, compute intersection and union set sizes and divide as `f64` (intersection.len() as f64 / union.len() as f64) — watch for integer-division bugs if you forget the cast. Accumulate all per-line similarities and divide by the count of compared lines (N-1) for the mean; round only at the final print step to 4 decimal places, not on each intermediate value, to avoid compounding rounding error. For the top-3 ranking, sort by (similarity descending, original line index ascending) so ties break toward the smaller index — Rust's default sort is stable but you still need an explicit comparator on similarity then index to guarantee correct tie-breaking. Write the output in the exact literal format specified (including the labels "Mean Similarity:" and "Top 3:" and comma-space-separated bracketed indices), then compile with `rustc /home/user/processor.rs` and run it to produce `/home/user/summary.txt`.
