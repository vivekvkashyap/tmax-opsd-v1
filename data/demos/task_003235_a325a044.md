Do not rewrite the tracer from scratch — diff/read the vendored code at `/app/data-flow-tracer/tracer/mapper.py` to find the deliberately-introduced bug(s) rather than guessing. Look specifically for: (a) a Cypher/query string that references the wrong node label for the document-to-graph edge (compare label names used in query construction against what the rest of the schema/graph actually uses — a subtly wrong label like a relational-store label substituted where a graph-node label belongs will silently return zero matches), and (b) any loop that yields/constructs cross-representation edges (e.g. relational-to-document-to-graph) that contains a stray early `continue`/`return`/`skip` guard which discards a whole edge-type category before it's added to the output — remove or correct that guard so all edge types are emitted. After fixing both issues, run `python3 /app/data-flow-tracer/run_audit.py --input /home/user/system_schema.json --output /home/user/audit_results.json` and sanity-check the output path count looks complete (not suspiciously low) before considering it done, since grading compares your path list's F1 against a hidden golden set at a 0.95 threshold.