Start with the audio: use a speech-to-text tool (whisper, ffmpeg+ASR, etc.) on `/app/voicemail.wav` to transcribe the spoken digits into the 5-digit PIN — don't guess it. For the repo, `cd /home/user/math_server`, run `go build` to see the exact compiler error on HEAD (likely a typo'd stdlib call), fix only that syntax issue, then commit so bisect has a clean-compiling tip. Use `git bisect start`, mark HEAD bad and the `v1.0.0` tag (200 commits back) good, and write a small test script that builds the binary at each checked-out commit, fires many concurrent requests at the math endpoint (or runs with `go test -race`), and exits nonzero on wrong results or detected races — feed this to `git bisect run`. Once the guilty commit is found, inspect its diff for a shared/global mutable variable used across concurrent handler goroutines (classic data-race pattern) and refactor it to be request-local. Finally add Bearer-token auth middleware checking against the PIN you transcribed, build, and run the server persistently on 127.0.0.1:9000.
