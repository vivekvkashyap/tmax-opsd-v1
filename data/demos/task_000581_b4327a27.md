Process the CSV in a single ordered pass rather than sorting or using pandas groupby blindly, since dedup depends on original file order within each bucket/sensor. Parse each row's timestamp, truncate to the minute to compute its bucket key (e.g. floor seconds to `:00`), and maintain a dict keyed by `(bucket, sensor_id)` mapping to a list of "accepted" (x, y) points seen so far in that group. For each new record, compute Euclidean distance to every previously accepted point in the same (bucket, sensor_id) group — if any distance is `<= 0.1`, drop the record; otherwise append it as accepted (this is important: compare against ALL prior accepted points, not just the most recent one, and the check is sequential/order-dependent, so process rows top-to-bottom exactly as they appear in the file). After the full pass, for each group compute the mean of x and y over its accepted points and round to 4 decimal places using proper rounding (careful with floating point display — ensure output always shows 4 decimal places even for round numbers like 5.0). Build the final list of records, sort by `bucket` ascending then `sensor_id` alphabetically, and serialize with `json.dump` (indent as desired) to `/home/user/aggregated_output.json`.
