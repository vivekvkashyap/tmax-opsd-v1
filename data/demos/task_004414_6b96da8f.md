Design this C program around three separable concerns: safe traversal, deterministic ordering, and byte-exact RLE encoding. For traversal, walk `/home/user/docs_input` recursively with `nftw` or manual `opendir`/`readdir`, using `lstat`/`stat` to get the device+inode of each resolved target, and maintain a visited-inode set (a simple array or hash set) — before recursing into a directory or following a symlink, resolve it (e.g. with `realpath` or by stat'ing the target) and skip it if its (dev, inode) pair was already visited, which is what prevents infinite loops from self-referential symlinks. Collect only regular `.txt` files by their canonical resolved path, deduplicating by inode so a symlink and its target only count once. Sort the unique resolved paths alphabetically before concatenating their contents into one in-memory (or streamed) buffer. For RLE, iterate the merged byte stream tracking run length and current char; whenever the run reaches 255 or the character changes, emit a `(count, char)` pair and reset the run — this correctly handles runs longer than 255 by splitting them. Write the encoded bytes into fixed-512-byte chunk files sequentially (last chunk may be shorter), and separately write the sorted list of included absolute paths to `manifest.txt`. Test with `xxd`/`od -An -tx1` against your own manual trace before trusting the output.