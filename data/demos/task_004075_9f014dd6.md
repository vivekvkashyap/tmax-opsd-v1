Do not shell out to `/app/normalizer` per line in your final Go program — it will be too slow on the hidden large dataset; instead reverse-engineer its exact normalization rule by piping varied test strings (letters, numbers, punctuation, whitespace, mixed-case, multi-byte/non-ASCII characters) into it via `echo -n "..." | /app/normalizer` and comparing outputs, to determine precisely which Unicode categories are kept vs dropped and how casing is handled. Once you've inferred the rule, reimplement it natively in Go using the standard `unicode` package (e.g. iterate runes, keep only runes satisfying the categories you observed being preserved, lowercase them, and drop everything else) rather than porting bytes/ASCII-only logic, since the binary is described as handling full Unicode text including non-Latin scripts. Parse the JSONL with `encoding/json` line by line, apply your normalize function to each `message`, track a global set of unique normalized strings, and per `lang` track counts of each normalized message to find the top one. Emit the exact JSON schema given (including nested top_message_per_lang keyed by lang) to /home/user/summary.json, and test correctness against /home/user/data/sample_logs.jsonl before trusting the hidden run.