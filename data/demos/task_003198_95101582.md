First get Eigen in place before writing any C++: download the tarball, extract it, and confirm headers resolve at `ml_prep/eigen/Eigen/Dense` etc. (the archive may extract into a versioned subdirectory like `eigen-3.4.0/`, so you likely need to move/rename it so the path matches exactly what's required). When implementing the ridge regression program, be precise about the CV fold boundaries — they are fixed, sequential, non-random slices (0-19, 20-39, ...), not shuffled or k-fold-library splits, so build them by explicit index ranges. Build the design matrix and closed-form solution with `Eigen::MatrixXd`/`.inverse()` or a solver like `.ldlt().solve(...)`, applying `lambda * I` to the full `(X^T X + lambda I)` including the intercept row as specified. Compute MSE per fold per lambda, average across the 5 folds, then select the lambda with the lowest average MSE — write both the winning lambda and its MSE (4 decimal places) in the exact required format. Reason about what a noiseless perfectly linear dataset implies for regularized vs. unregularized fits before assuming any particular lambda is "safest." Write `run_pipeline.sh` to compile with `-I` pointing at the eigen include dir and then execute the binary; run it yourself to confirm `tuning_results.txt` is produced correctly.
