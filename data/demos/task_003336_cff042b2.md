Write `/home/user/sec_audit.c` to do three independent, literal checks and format the report exactly as specified. For SSH: read `/home/user/sshd_config` line by line, skip lines starting with `#` (comments) or blank lines, and check that an active line exactly equalling `PermitRootLogin no` and another exactly equalling `PubkeyAuthentication yes` both exist — trim trailing whitespace/newlines before comparing, and remember both directives must be present with those exact values (not just present at all) for SSH_HARDENED to be YES. For certs: shell out via `popen`/`system` to `openssl verify -CAfile /home/user/ca.pem /home/user/server.pem` (or `openssl verify -partial_chain` if plain verify fails on a self-signed CA) and check the command's output/exit status for success rather than assuming; capture stdout to detect the "OK" string reliably. For intrusion detection: parse `/home/user/auth.log` line by line, match lines containing `Failed password`, then extract the token following the literal word `from` as the IPv4 address (a simple sscanf/strstr on `"from "` followed by parsing dotted-decimal works), tally counts per unique IP in a small dynamic table, and include only IPs with count >= 3, sorted alphabetically as strings (not numerically) and comma-space joined, or `NONE` if empty. Write the three lines to `/home/user/audit_report.txt` exactly matching the format, compile with `gcc`, and run once to confirm output before finishing.
