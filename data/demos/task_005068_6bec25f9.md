Since you must match a hidden oracle bit-exactly under fuzzing, don't guess at behavior — the task description already fully specifies the algorithm, so implement precisely what's written rather than improvising details. In C++: parse the two positional args (frame index as int, and the three comma-separated integers via a stringstream split on ','). Extract the frame by shelling out with `popen()` to exactly the given ffmpeg command (substitute the frame index), read all bytes from the pipe via `fread` until EOF, and compute brightness B as `sum_of_bytes / pixel_count` using integer division truncation — use a 64-bit accumulator (long long) to avoid overflow since 640*360 pixels at up to 255 each can exceed 32 bits is not actually a risk here but keeps it safe. Parse the CSV (skip header line, split each row on commas) into ItemID/E1/E2/E3, and for each row compute dynamic embedding `[E1*B, E2*B, E3*B]` and Manhattan distance to the query vector using integer (long long) arithmetic throughout — no floating point, since it must match the oracle's integer math exactly. Track the minimum distance, and on exact ties keep the lexicographically smaller ItemID by explicit string comparison, not insertion order. Print only the winning ItemID followed by a newline. Build a local test harness that fuzzes both your binary and the oracle with random inputs before finalizing.