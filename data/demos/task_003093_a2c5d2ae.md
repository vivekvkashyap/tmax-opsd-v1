Start by running the existing `locparse` package against a sample `.loc` file to see which keys it drops — compare the number of `[SECTION]` blocks in the raw file against the number of keys returned by `locparse.parse()`. This is the fastest way to localize the bug without guessing. Inspect `/app/locparse/locparse/parser.py` for the loop/regex that walks the block lines; look specifically for any slicing like `lines[:-1]` or an off-by-one range that would skip the last entry of each parsed block — fix it so all lines/blocks are processed, then re-test against the same sample file to confirm the key count now matches expectations. Only after the parser is verified correct should you write `/home/user/process_loc.py`: iterate the `.loc` files in `/home/user/data/`, parse each with the fixed `locparse`, load `/home/user/template.json`, and produce one output JSON per language in `/home/user/output/` that mirrors the template's nested structure exactly, filling in extracted values and leaving unmatched template keys as empty strings. Also implement the `/home/user/pipeline.log` line format precisely as specified, including the per-file extracted key count.
