Work through this as a chain of independently-testable fixes, verifying each before moving on. First, diagnose the SSH failure by inspecting `~/.ssh/config` for a `Host localhost` block — look specifically for `PubkeyAuthentication no` and a wrong `IdentityFile`; fix both (set `PubkeyAuthentication yes`, point `IdentityFile` at `/home/user/.ssh/iot_rsa`, and add `StrictHostKeyChecking no`/`UserKnownHostsFile=/dev/null` if host-key prompts appear), then confirm with `ssh -i ~/.ssh/iot_rsa user@localhost echo ok` non-interactively before proceeding. For the C service, read the config file, parse out the value after `DATA_MOUNT=`, build the file path by string concatenation, and use a standard `socket`/`bind`/`listen`/`accept` TCP loop on 127.0.0.1:8080 that reads the file fresh per connection and writes its raw bytes back. For deployment, create the versioned directory, copy the binary, symlink `active` -> `v1`, then launch it with `nohup ... & disown` so it survives your script exiting. For port forwarding, launch `ssh -i <key> -L 9090:127.0.0.1:8080 -N user@localhost &` also detached. Only after confirming both background processes are alive (`ps`/`pgrep`) run the final `nc` verification.
