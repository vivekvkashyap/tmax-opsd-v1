Pick a C JSON library (jansson or cJSON) and parse `/home/user/thresholds.json` into an in-memory lookup (e.g. a small hash map or linear array of mount_point->limit pairs) before touching the CSVs. For each CSV path, `open()` + `fstat()` to get the size, then `mmap(NULL, size, PROT_READ, MAP_PRIVATE, fd, 0)` and manually scan the mapped buffer for lines (find `\n` boundaries, split each line on commas) — do not fall back to `fopen`/`fgets` anywhere in the CSV-reading path, since the grader checks for `mmap` usage in the source. For each row, look up the mount point in your threshold map; if absent, skip it silently rather than treating it as zero or erroring. Emit an alert line only when usage is strictly greater than the limit (not >=), using the exact format string given, and process the CSV files in the order they're passed on the command line so output order matches the shell glob expansion. Redirect the compiled binary's stdout to `/home/user/alerts.log` when running against `/home/user/logs/*.csv`.