Design the C++ program around simple, robust CSV parsing (split on commas, trim whitespace) rather than any heavy library — the datasets are small. Load `users.csv` and `predictions.csv` into maps keyed by `user_id` (e.g. `std::map<int, Row>` gives you sorted iteration for free), then perform the inner join by iterating one map and looking up the other, silently skipping unmatched IDs. For each joined record, apply all three validation rules (age in [18,120], score in [0.0,1.0], class_label in {0,1}) — a record must pass every rule to be "valid"; if it fails any rule, its user_id goes to the error list instead. Since you used a `std::map<int, ...>`, both the joined+valid rows and the error IDs come out already sorted ascending by user_id, so just write them out — cleaned rows with header `user_id,age,region,score,class_label` to `/home/user/cleaned_output.csv`, and one user_id per line (no header) to `/home/user/validation_errors.txt`. Preserve the original numeric formatting for score (avoid unnecessary trailing zeros) so output matches exactly. Compile with g++ and actually run the binary to generate both files before finishing.
