Inspect `build.rs` first to learn the exact request shape it makes (URL path, query parameter name, expected response format) so your server's contract matches what the Rust build script actually expects — build.rs runs at compile time, so `cargo build` will hang/fail with a connection error until your server is up and answering correctly. Write `server.py` using `asyncio` (with `aiohttp` or raw asyncio streams) bound to `127.0.0.1:8080`, handling each connection in its own coroutine/task for concurrency. For `/evaluate`, parse the `expr` query parameter, then safely evaluate the arithmetic expression by parsing it with `ast.parse(expr, mode='eval')` and recursively walking the resulting AST, only permitting `ast.BinOp` with `+ - * /` and numeric literals/parentheses (reject anything else) rather than calling raw `eval()`, which would be unsafe and also not "parsing" per the spec. Time the parse+evaluate step with `time.perf_counter()` in microseconds, append a line to the perf log in the exact specified format, and respond with just the integer result as plain text. Start the server in the background (e.g. `python3 server.py &`, waiting until port 8080 accepts connections) before running `cargo build` in the Rust project directory.