Use `git bisect` methodically: mark `v1.0` as good and `HEAD` as bad, and at each bisect step build (`make`) and run the daemon against `/app/incident_stream.mp4` (or reproduce the leak condition it exercises) to judge good/bad, letting git narrow to the single culprit commit rather than eyeballing the diff. Once found, read that commit's diff to `process.c` (or equivalent) — expect two related defects in the same function: an early `return` on an "anomaly"/dark-frame condition that skips the `free()` of the frame buffer (classic leaked-on-error-path bug — fix by freeing before every return, or restructuring with a single cleanup exit point), and a sign/operator typo in the luma formula (compare against the correct `Y = 0.299*R + 0.587*G + 0.114*B` — the buggy version likely flips a `+` to `-` on one term, which over- or under-triggers the anomaly branch and thus the leak). Fix both in the same function, `make` to rebuild `/home/user/videod_repo/videod`, and measure actual peak RSS yourself (e.g. `/usr/bin/time -f "%M" ./videod /app/incident_stream.mp4`) to confirm it's comfortably under the 15000 KB threshold before finishing. Redirect stdout of the run to `/home/user/processed_frames.log` as required.
