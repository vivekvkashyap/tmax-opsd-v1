Two independent deliverables. (1) Transcription: install/use `openai-whisper` (or another local ASR) on /app/reference_interview.wav and write exactly what the model outputs (trimmed) to audio_transcript.txt — don't fabricate text, let the model produce it, and sanity-check the result sounds like coherent English before saving. (2) Zip Slip defender: for each .zip in the input dir, open it with `zipfile.ZipFile` and inspect every `ZipInfo.filename` *before* extracting anything — reject the whole archive if any entry name is an absolute path (starts with `/`) or, after normalizing (`os.path.normpath`), resolves outside the intended output directory (classic check: `os.path.abspath(os.path.join(out_dir, name))` doesn't start with `os.path.abspath(out_dir)`) or contains `..` segments; do this inspection pass fully before calling `extractall`, since extracting-then-checking is too late. Log rejected archive basenames to rejected_archives.log, and for accepted archives extract normally then parse events.log's multi-line entries (use the same BEGIN/blank-line-delimited grouping logic implied by "multi-line log entry") into a JSON array written as events.json. Test your script against both /app/corpora/incoming/clean and .../evil style inputs before the final full run.
