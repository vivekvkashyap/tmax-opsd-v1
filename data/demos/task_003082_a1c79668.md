Reverse-engineer the binary's transform by black-box probing rather than trying to disassemble the logic from scratch: feed it a variety of crafted inputs through stdin (single field, multiple fields, empty lines, fields with distinctive markers like different cases) and diff the outputs to spot the pattern. Try inputs like `a|b|c` and `1|2|3|4` and note how field order, count, and casing change. Once you have a hypothesis, confirm it against several more random inputs (including edge cases: single-field lines, empty lines, lines with many fields) before writing the Bash reimplementation — bit-exact equivalence means whitespace, delimiter placement, and case transforms must all match precisely, so test empty-line handling and single-field lines explicitly since these are the most common places a naive port diverges from the real behavior. Implement `/home/user/formatter.sh` using `IFS='|'` splitting and array manipulation (or `awk`), read stdin line by line, apply the transform, and `chmod +x` the script. Re-diff your script's output against the binary's output for the same test inputs before considering it done.