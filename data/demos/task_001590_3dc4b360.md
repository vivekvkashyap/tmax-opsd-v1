Extract the edge list from the video before writing any graph logic. Use `ffmpeg` to pull one frame per second (e.g. `ffmpeg -i schema_edges.mp4 -vf fps=1 frame_%02d.png`), then run `tesseract` on each frame to read the `SRC->DST` overlay text — check for common OCR misreads (O/0, I/1, the arrow character), since all 10 edges must be correct for downstream queries to be exact. Cross-reference recovered node labels against the `nodes` table in `/app/legacy_nodes.db`; a recovered label that doesn't match a real node signals an OCR error to fix, not a genuine unknown node. Once the edge list is confirmed, build an adjacency structure and implement `/home/user/project_graph.py` as a bounded-depth BFS/DFS from `node_label` up to `max_depth` hops, collecting all unique labels visited (including the source), and print them comma-separated and alphabetically sorted (`sorted(set(...))`). Since it will be fuzz-tested against an oracle across many `(label, depth)` pairs, test it yourself against several depths and labels, including depth beyond the graph's diameter and labels with no outgoing edges, before finishing.