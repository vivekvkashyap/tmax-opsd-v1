Treat this as several independent components to build and verify one at a time, then glue together in deploy.sh. (1) Vendored fix: inspect `/app/alert-mailer-1.0.0/src/main.rs` for a hardcoded SMTP port constant blocking use of the unprivileged port 1025 — add an environment-variable override (e.g. read `SMTP_PORT` via `std::env::var` with the hardcoded value only as a fallback default) rather than just changing the literal, since the verifier expects a configurable port; then `cargo build --release` in that directory and confirm it compiles. (2) Detector: write a small Rust binary from scratch that reads its file argument line by line and, for each line, checks for the exact substrings `<script>`, `UNION SELECT`, `DROP TABLE`, `/etc/passwd` — prefix with `[ALERT] ` if any match, else `[OK] `, printing to stdout; test it directly against small hand-written malicious/benign files before trusting it on the real corpora. (3) In deploy.sh: mount with `bindfs -o ro /home/user/raw_logs /home/user/live_logs &`, launch the debugging SMTP server bound to port 1025 in the background, launch `python3 -m http.server 8443 --directory /home/user/dashboard` wrapped with TLS (Python's http.server doesn't do TLS natively — wrap the socket with `ssl.wrap_socket`/`SSLContext` using the given cert/key, e.g. via a small Python snippet), then `cargo build --release` the detector and `cp` the binary to `/home/user/bin/detector`. Make the script executable and background all long-running processes so the script itself returns.
