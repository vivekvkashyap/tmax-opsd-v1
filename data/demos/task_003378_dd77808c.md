Build the analysis as a straight-line script matching the spec's exact statistical calls, since the verifier recomputes the same pipeline and compares values. Load the CSV with pandas, add `temp_vib_ratio = temperature / vibration`. For the hypothesis test, use `scipy.stats.ttest_ind(group_failed, group_not_failed, equal_var=False)` (Welch's t-test requires `equal_var=False`) and take `.pvalue`. For modeling, build `X` from exactly the four named features and `y` from `failed`, instantiate `RandomForestClassifier(random_state=42)`, wrap it in `GridSearchCV(rf, {'max_depth':[3,5,None]}, cv=3, scoring='accuracy')`, call `.fit(X,y)`, and pull `best_params_['max_depth']` and `best_score_`. Write these three values to the JSON report with the exact key names given (`p_value`, `best_max_depth`, `best_cv_accuracy`) — keep `best_max_depth` as a native int or null (not numpy int64, which won't serialize cleanly) via `int(x) if x is not None else None`. Double check you're not accidentally shuffling row order or reseeding differently, since GridSearchCV's cv folds depend on data order and the random_state.