First recover the policy rules by running OCR on the image: `tesseract /app/policy.png stdout` (install tesseract-ocr if missing), then read the extracted text carefully to find the forbidden word, minimum length, and any other stated constraint — treat these as the ground truth policy your validator must enforce, don't hardcode guesses. Design config_filter.cpp around three stages: (a) flock/fcntl an exclusive, non-blocking lock on /tmp/config_filter.lock, exiting 1 immediately if it's already held; (b) decompress the RLE input by reading byte pairs [count][char] in a loop, validating the file has an even number of bytes and count > 0, rejecting (exit 1) on any malformed pair, truncated input, or non-ASCII byte encountered; (c) apply the OCR'd policy rules (forbidden substring check, minimum length check, and whatever else the image specifies) to the decompressed text, exiting 0 only if every check passes. Compile with g++ to the exact path /home/user/config_filter, then iterate against both /app/corpus/clean and /app/corpus/evil, checking exit codes match expectations for every sample before considering the job done.