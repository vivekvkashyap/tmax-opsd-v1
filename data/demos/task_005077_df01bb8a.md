Implement this as two independent numeric routines over the same parsed dataset, and validate each against a quick Python/manual cross-check before trusting the C output, since floating-point formatting bugs (e.g. wrong decimal-place rounding) are the easiest way to fail silently. Parse the CSV in C by skipping the header line, then reading `f1,f2,label` per row with `fscanf`/`sscanf`, storing all rows and separately splitting into class-0/class-1 arrays as you go. For Gaussian Naive Bayes: compute mean and population variance (divide by N, not N-1) per feature per class, evaluate the two 1D Gaussian PDFs at the query point per class, multiply per-class per-feature likelihoods together (conditional independence), then combine with equal 0.5 priors via Bayes' rule to get P(class=1 | q) = (like1*0.5) / (like1*0.5 + like0*0.5) — note the priors cancel algebraically but keep them explicit to match the spec. For nearest neighbor: compute Euclidean distance from q to every row (regardless of class, using the combined dataset in its original row order) and track the 0-based index of the minimum. Write output with `fprintf` using `%.4f` for the probability and a plain integer for the index, matching the exact two-line format requested.
