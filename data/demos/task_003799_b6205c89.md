Build this as a from-scratch C Metropolis-Hastings sampler, not a library call. Structure: (1) read the whitespace/newline-separated floats from latencies.txt into an array; (2) implement a log-likelihood function for the 2-component GMM with fixed variance 1.0 — sum over data points of log(w*N(x|mu1,1) + (1-w)*N(x|mu2,1)), using log-sum-exp style care to avoid underflow if needed; (3) initialize w, mu1, mu2 to reasonable starting values within their priors; (4) loop 50,000 iterations: propose each parameter via a Gaussian random walk (std 0.1) using `drand48`/`rand()`-based normal sampling (e.g., Box-Muller), reject proposals violating the priors (w in [0,1], mu1,mu2 in [0,20]) or the mu1<mu2 constraint, then accept/reject via the standard Metropolis ratio `min(1, exp(new_loglik - old_loglik))`; (5) after discarding the first 10,000 samples, average the remaining 40,000 for posterior means. For the KL divergence, build a 50-bin histogram over [0,20] from the raw data for P, evaluate the fitted GMM PDF at each bin midpoint times bin width for Q, renormalize Q to sum to 1, then sum P_i*ln(P_i/Q_i) skipping zero-P bins. Compile with `gcc -O3 -lm mcmc_profiler.c -o mcmc_profiler`, run it, and format the output to 3 decimals exactly as specified.
