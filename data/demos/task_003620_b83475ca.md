Work top-down: create the directory tree and sensor.conf first, then build the C processor, since everything downstream depends on it reading real config/audio correctly. In processor.c, parse sensor.conf (simple KEY=VALUE lines) for THRESHOLD/MIN_DURATION_S/LOGFILE, then read the WAV file by skipping the standard 44-byte header (or properly parsing RIFF/fmt/data chunks) to get 16-bit signed samples at 44100 Hz. Track amplitude via `abs(sample)`; maintain a state machine that starts an event when |sample| >= THRESHOLD holds continuously for MIN_DURATION_S*44100 samples, and ends it when |sample| < THRESHOLD holds continuously for the same duration — get the boundary/edge-counting logic right since F1 is scored on start_time/duration within +/-0.05s, so off-by-one sample-counting errors will fail the metric. Write events.csv with 3-decimal-place times and append matching log lines to LOGFILE. For logrotate.conf use standard daily/rotate 7/compress directives targeting the log path. deploy.sh should compile with `-O3`, back up any existing binary, replace it, then run it — execute deploy.sh yourself and inspect the generated events.csv against your own understanding of the audio before considering it done. Generate the TLS cert/key with `openssl req -x509 -newkey rsa:2048 -nodes ...` and have start_server.sh launch Python's `http.server` wrapped in `ssl.wrap_socket`/`SSLContext` on port 8443 without blocking.
