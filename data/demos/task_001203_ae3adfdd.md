Investigate empirically before editing: run the log processor under `valgrind --leak-check=full` while feeding it a malformed-timezone payload to get a leak stack trace pointing at the exact allocation site; also hand-check timezone-to-UTC conversion for a known offset to spot whether the code adds when it should subtract (or vice versa). The typical bug shape is a `new` object allocated up front with an early `return`/`throw` on the invalid-timezone path that skips the matching `delete` — fix by ensuring every exit path (including exceptions) frees it, ideally via RAII (`unique_ptr`) instead of manual `delete` tracking. Recompile with the exact given `g++` command and restart the pipeline via `/app/start_services.sh` to confirm no crash under adversarial input. For the standalone `detector`, implement the *same* validation rule you just fixed (offset in [-12:00, +14:00], minutes in [00,59]) as a pure parser reading one JSON line from stdin, and test it against both corpora with a shell loop before trusting it, since 100% pass is required on each.
