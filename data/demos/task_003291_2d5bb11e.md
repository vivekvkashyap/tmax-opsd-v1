Build this as a streaming ETL script (Python is a good fit) rather than loading everything into memory at once — iterate the JSONL file line by line. For each record: (1) parse `recorded_at` robustly across multiple formats — try `datetime.fromisoformat` after normalizing 'Z'/space variants, and fall back to `email.utils.parsedate_to_datetime` for RFC-1123/log-style strings, then convert to an integer UNIX epoch (treat naive timestamps as UTC, be consistent); (2) mask any `api_key` or `password` key inside `state` by replacing its value with the literal string `***`, but only those exact key names, leaving other keys untouched; (3) serialize the masked `state` dict as compact JSON with `separators=(',',':')` and `sort_keys=True` — the exact serialization matters because you hash it with SHA-256, and getting key order or whitespace wrong will silently produce a different hash than expected; (4) dedupe per `host_id` by keeping only the first time a given hash is seen for that host, dropping later repeats even if the *unmasked* underlying value changed (masking can make two logically-different states hash identically — that's intended). Finally create the SQLite table with the exact schema/column names given and bulk-insert the surviving rows.