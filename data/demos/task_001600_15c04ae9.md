In C, read each `.bin` file as raw little-endian float32 arrays using `fread` into `float` buffers sized dim*count (64*1000 and 64*50) — do not assume a header, the files are pure binary floats. For L2 normalization, compute each vector's norm with `sqrtf(sum of squares)`; if the norm is exactly 0.0f, skip dividing (leave the vector as all zeros) to avoid division-by-zero, otherwise divide every component in place. Bracket only the nearest-neighbor search loop (not the file I/O or normalization) with `clock()` calls before/after, then compute elapsed seconds as `(double)(end-start)/CLOCKS_PER_SEC`. For each query, since vectors are already L2-normalized, cosine similarity reduces to a plain dot product — loop over all 1000 base vectors, track the max similarity and its index (ties keep the first-seen max, i.e. iterate in order and use strict `>`). Write the CSV row-by-row with `fprintf(f, "%d,%d,%.4f\n", ...)`, and the benchmark line with `fprintf(f, "Search time: %f seconds\n", elapsed)` — match the exact literal spacing shown in the spec. Compile exactly with `gcc -O3 -lm embed_prep.c -o embed_prep`, then run it and diff your CSV against a Python/numpy reference you compute yourself from the same binaries to catch floating-point/rounding mismatches before considering it done.