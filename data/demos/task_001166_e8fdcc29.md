Implement this as a strict, deterministic pipeline matching the spec's exact math, since it will be checked against reference output for a specific seed. In Go, use `flag.Int` for `-seed` and `-samples`, and create your RNG as `rand.New(rand.NewSource(int64(seed)))` — using the global `rand` package functions instead of a local source will break determinism/reproducibility. For each sample draw `r := rng.Float64()` first, then branch: uniform via `rng.Float64()*10.0`, and the two normals via `rng.NormFloat64()*stddev + mean` — draw exactly one random value per branch in this order so the sequence of RNG calls is deterministic and matches a reference implementation. Bin with `idx := int(value / 0.1)` after confirming `0 <= value < 10.0`, discarding out-of-range values without incrementing any bin. Apply the 3-point moving average exactly as specified, treating out-of-bounds neighbors as 0. Detect peaks with strict inequality on both sides plus the `> 100.0` threshold, using the boundary conventions given. Format floats with `fmt.Sprintf("%.3f", ...)` for the CSV. For `run_pipeline.sh`, compile with `go build -o simulator simulate.go`, run with the exact specified seed/samples flags, then parse `peaks.csv` (skip the header), sort by the numeric `SmoothedCount` column descending, and take the top 2 `BinCenter` values into `top_peaks.txt`. Make the script executable with `chmod +x`.