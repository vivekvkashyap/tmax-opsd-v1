Tackle the four pieces independently, verifying each before wiring them together. Rust: `cargo build` in `/app/rust_counter` to see the borrow-checker error, then fix it by reordering so the immutable borrow's last use precedes the mutable call (or clone the needed value) without changing the word-count logic, then `cargo build --release`. Schema: use Python `sqlite3` to `ALTER TABLE stats ADD COLUMN anomalies INTEGER DEFAULT 0`, then `UPDATE` the `traffic.mp4` row to `anomalies=3`, verifying with a `SELECT`. Frame: use `ffmpeg -i /app/traffic.mp4 -vf "select=eq(n\,30)" -vframes 1 /app/frame_30.jpg` and confirm it's a valid JPEG. Web service: write your own expression evaluator (recursive-descent or shunting-yard, never `eval()`) supporting `+ - * /` with correct precedence; reuse it in both an HTTP server on port 8000 (`POST /evaluate`, `GET /frame`) and a raw `socket` TCP listener on port 8001 running in a separate thread. Launch both and confirm each responds correctly before finishing, keeping the process running in the background.