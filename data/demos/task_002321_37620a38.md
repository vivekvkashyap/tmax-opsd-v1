Use `ffmpeg` to extract every frame of `/app/backup_topology.mp4` as individual images (e.g. PNG or raw PPM, one per second at 1fps) into a working directory, then for each frame read only the top-left pixel's RGB triple — a small C helper or a quick Python/ImageMagick check during development can confirm your extraction is reading the correct pixel before you wire it into the C program. Each frame's (R, G, B) becomes one directed edge (source, dest, weight) in your graph; build this edge list once at startup. Implement Dijkstra's algorithm over the resulting adjacency structure to answer shortest-path queries, tracking predecessor pointers so you can reconstruct the actual path (not just the distance) for the JSON response. For the HTTP layer, write a minimal blocking POSIX-socket server that parses the request line for `GET /restore_path?source=<ID>&dest=<ID>`, extracts the two integer query parameters, runs Dijkstra, and serializes the path array and total latency into the exact JSON shape requested (200 on success, 404 with an error JSON if unreachable). Be careful that ffmpeg's video encoding (especially with lossy codecs) can slightly alter pixel values — prefer a lossless intermediate format or pixel format during extraction and verify a few known frames decode back to exact RGB triples before trusting the rest. Compile, launch the server in the background, and test it with `curl` before considering the task done.