Build this ETL pipeline as discrete, individually-testable stages rather than one monolithic script. Start with OCR: run `pytesseract.image_to_string` on `/app/legacy_dashboard.png`, print the raw extracted text, and parse out the numeric value following "BASELINE_LATENCY:" with a regex rather than assuming a fixed position — inspect the OCR output for noise before trusting it. For cleaning, load `/home/user/raw_metrics.csv` and handle the mixed timestamp formats explicitly: try `pandas.to_datetime` with `utc=True`, and for values that are pure digit strings (Unix epoch) parse with `unit='s'` separately, since a single `to_datetime` call often mis-parses one of the two formats silently. Drop rows with negative cpu_usage or memory_usage, logging each dropped row's original timestamp to `/home/user/pipeline.log` before you overwrite/mutate that column. Set timestamp as a DatetimeIndex, `resample('1min')`, then `interpolate(method='linear')` for cpu_usage/memory_usage/latency — resampling before interpolating is essential, since interpolating first then resampling can miss newly created empty bins. Subtract the OCR'd baseline to get normalized_latency, then write to SQLite (`to_sql`) and export the final CSV with ISO-8601 timestamps.