Implement this as a literal pipeline in whatever scripting language is fastest for you (Python is a natural fit given JSON Lines input): read `/home/user/events.jsonl` line by line, parse each as JSON, and filter to keep only records where `event_type == "query"` AND the nested `metadata.status == "success"` — use strict equality, not truthy checks, and be careful to reach into the nested `metadata` dict correctly. Group the survivors by `user_id`, and for each group compute the count and the mean of `metadata.duration`. Sort the resulting groups primarily by `avg_duration_ms` descending, breaking ties by `user_id` ascending — implement this as a proper two-key sort (e.g. sort by a tuple `(-avg_duration, user_id)`), not two separate sorts. When writing the CSV, round `avg_duration_ms` to exactly two decimals using proper rounding (not truncation) and format with two decimal places even when the second digit is zero (e.g. `45.00`), write the exact header line first, and avoid any trailing whitespace or extra blank lines since the grader does an exact-diff comparison against the expected file.
