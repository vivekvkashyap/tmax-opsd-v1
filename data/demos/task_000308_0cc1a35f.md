Install `pandas`, `scipy`, `pytest` first, then do the reshape as a simple `pd.merge(df1, df2, on='timestamp', how='inner').sort_values('timestamp')`, writing straight to CSV with `index=False` so no stray index column appears. For the curve fit, prefer `scipy.optimize.curve_fit` over manually driving `minimize` — define `def model(x, A, B): return A * np.exp(-B * x)`, call `curve_fit(model, alpha, beta, p0=[1.0, 1.0], bounds=([0, 0], [np.inf, np.inf]))` to enforce `A, B > 0` and give it a reasonable initial guess so it converges reliably on this small dataset. Return `(A, B)` as native Python floats (`float(popt[0])`) before dumping to JSON, since numpy float types can break naive JSON serialization. For the regression test, build the synthetic DataFrame exactly as specified inside the test file itself (don't read it from disk), call `fit_exponential`, and assert both `abs(A - 2.5) < 0.01` and `abs(B - 0.5) < 0.01`. Finally run `pytest /home/user/test_optimization.py` and confirm it exits 0 before considering the task done.
