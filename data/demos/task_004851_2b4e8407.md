Load `/home/user/features.npy` with `numpy.load`, then run `np.linalg.svd(X, full_matrices=False)` directly on the raw matrix — the task explicitly says not to mean-center or scale first, so skip any standardization step a typical PCA recipe would include. Compute per-component variance ratios as `S**2 / np.sum(S**2)`, take the cumulative sum, and find the smallest `k` such that cumulative variance is `>= 0.95` (e.g. `np.argmax(cumvar >= 0.95) + 1`) — using `>` instead of `>=`, or forgetting the `+1` for 1-indexing, are the easy off-by-one mistakes here. Reconstruct with `U[:, :k] @ np.diag(S[:k]) @ Vt[:k, :]`. Run `scipy.stats.ks_2samp` comparing column 0 of the original matrix against column 0 of the reconstruction. Write `/home/user/report.txt` with exactly two lines: the integer `k` on line 1, and the p-value formatted to 4 decimal places (e.g. an f-string `f"{pval:.4f}"`) on line 2 — no extra whitespace or labels. Execute the script so the report file is actually produced.
