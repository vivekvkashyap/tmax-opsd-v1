Handle the three parts in order of dependency. First fix the circular import: `sorter` imports `differ` and `differ` imports `sorter` back — resolve it by pulling the shared type (`Sequence`) and any interface both packages need into a new leaf package (e.g. `models`) that both `sorter` and `differ` import, so the dependency graph becomes acyclic; then confirm `go build ./...` succeeds with no behavior change. Second, reverse-engineer `/app/seq_oracle` purely by black-box probing: feed it varied space-separated integer lists via stdin and check its exit code (0=clean,1=evil) for patterns — try sorted vs unsorted input, duplicates, small gaps, large gaps, and single/empty lists, and use the task's hint ("sorting, merging, diffing adjacent elements") to guide which hypotheses to test, converging on a precise rule (e.g. a min/max allowed adjacent gap after sorting) validated against dozens of probes before you trust it — do not disassemble or read the stripped binary's internals. Third, implement `filter.Classify` in a new `filter` package matching that rule exactly, build a CLI at `bin/classifier` that reads space-separated ints from a file argument and prints `CLEAN`/`EVIL`, and validate it against every file in `/home/user/corpora/clean/` and `/home/user/corpora/evil/` before wiring `filter.Classify` into the WebSocket handler's accepted/rejected JSON response.
