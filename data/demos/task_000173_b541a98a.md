Approach this as a two-part DB task: first add an index, then write a correct restore-chain query. For the index, run `CREATE INDEX` on `jobs(status, type, start_time)` (or separate indexes) so the filter+sort pattern used below is covered — confirm afterward it's persisted with `SELECT * FROM sqlite_master WHERE type='index'`. For the query logic, be careful with two easy mistakes: (1) the "most recent full backup" must be filtered to `status='success'` before taking the max `start_time` — don't just take the latest full job regardless of status; (2) incremental backups must be filtered to `status='success'` AND `start_time` strictly after the chosen full backup's `start_time` — a failed inc in between should be excluded but should NOT break the chain (i.e., don't accidentally cut off later successful incs just because one failed). Sum `size_bytes` from `files` only for the full backup's job_id plus the qualifying inc job_ids (a plain SQL join/GROUP BY or a short Python script using sqlite3 both work). Write the result to `/home/user/restore_plan.json` with exactly the keys `full_backup_id`, `incremental_backup_ids` (as a list, ordered ascending), and `total_files_size`, then validate the JSON with `jq` before finishing.
