Start by OCR'ing `/app/instructions.png` (e.g. `tesseract`) to recover the actual bucketing interval and dedup column name — treat these as hidden configuration you must read out of the image, not values to assume. Build `/home/user/sanitizer` as a stdin->stdout filter that parses CSV rows respecting the `timestamp,user_id,payload` header, and for the `payload` field applies a regex that matches a literal `\u` NOT followed by exactly four hex digits (`[0-9a-fA-F]`) — e.g. a negative-lookahead-style check, since plain grep-for-`\u` will also flag valid escapes; drop only rows failing this check, and validate your logic against both `/app/corpus/clean/` (must pass 100%) and `/app/corpus/evil/` (must all be dropped) before considering it done. For `/home/user/aggregate.sh`, lowercase the payload, MD5-hash it (`md5sum`) to dedup identical normalized payloads, bucket the ISO-8601 timestamp using the interval you read from the image (convert to epoch, floor-divide by the interval-in-seconds, multiply back or reformat), and emit `time_bucket,unique_event_count` counting distinct hashes per bucket. Finally write `/home/user/pipeline.cron` with a `*/15 * * * *` schedule invoking `aggregate.sh /tmp/latest.csv`, and chmod both scripts executable.
