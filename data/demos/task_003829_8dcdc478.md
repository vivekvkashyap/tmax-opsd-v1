Treat correctness and speed as separate milestones — get a correct single-threaded sampler working first, verify its output matches the oracle's statistics, then parallelize with OpenMP. Since you can't read the oracle's source, infer the exact Metropolis-Hastings model from the description given (likelihood `y = a*exp(-b*x) + noise` with known `sigma=0.5`, uniform priors `a~U(0,10)`, `b~U(0,5)`) and implement a standard MH sampler: propose `(a', b')` via a symmetric random walk (e.g. Gaussian proposal), compute the log-likelihood ratio under the Gaussian noise model plus prior-support rejection (reject immediately if outside prior bounds), and accept/reject with the standard MH acceptance probability. Don't try to match the oracle's proposal stddev or RNG bit-for-bit — the grading only checks that posterior means land within 0.05, which is a statistical tolerance, not exact reproduction. For speed, use `#pragma omp parallel` to run 4 independent chains (each with its own RNG state seeded differently to avoid correlation) generating 25,000 samples each, then concatenate to `fast_samples.csv`; compile with `-O3 -fopenmp` and confirm via simple timing that you clear 3x speedup against the oracle binary before considering it done. Build the notebook last, once the C++ program is validated standalone from the command line.