Build this bottom-up rather than trying to write the whole pipeline at once. First define `sync.proto` exactly matching the required service/message shapes, then compile it with `python -m grpc_tools.protoc` to generate `sync_pb2.py` and `sync_pb2_grpc.py` before writing any server logic — verify the generated stubs import cleanly. For `CalculateDiff`, implement a real recursive tree walk (not a flat file list): build a structure keyed by relative path where each directory node aggregates a hash of its children's hashes, so you can prune whole unchanged subtrees by comparing hashes at each level before recursing further, then only compute a real unified diff (via `difflib.unified_diff` or shelling out to `diff -u`) for files whose leaf hashes differ — remember to convert absolute paths to root-relative paths in the diff headers, since `ApplyPatch` must reconstruct paths relative to `target_dir`. For `ApplyPatch`, apply patch text via the system `patch` utility (or manual hunk parsing) inside `target_dir`, catching failures to set `success` correctly. Finally write `run_e2e.sh` to orchestrate protoc, start `server.py` in the background with a short readiness wait, drive it via `client.py`, verify with `diff -r`, print `E2E TEST PASSED` only on success, and always kill the background server.
