The nondeterminism comes from accumulating into shared globals under a mutex in whatever order threads happen to finish (floating-point addition isn't associative, so summation order changes the bit pattern). Fix it by making each thread write its four partial sums into its own slot of a pre-sized array/struct indexed by `thread_id` (no mutex needed there, since each thread only touches its own slot), then after `t.join()` for all threads, accumulate the global sums in a single-threaded loop from index 0 to NUM_THREADS-1 in that fixed order — this guarantees the same floating-point summation order on every run regardless of thread scheduling. Remove the mutex-protected increments inside `process_block` entirely; keep the rest of the regression math (slope/intercept formula and the `setprecision(9)` output) unchanged since that formatting is already correct. Recompile with `g++ -O3 -pthread`, rerun to produce `/home/user/regression_result.txt`, and rerun a second time to sanity-check the output is bit-identical across executions before considering it done.