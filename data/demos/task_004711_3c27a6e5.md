Start by extracting the key: run OCR on `/app/cert_policy.png` (e.g. `tesseract /app/cert_policy.png -`) and parse out the value after `COMPLIANCE_KEY=`; do not guess it. Then write `/home/user/audit_tool.py` reading `sys.argv[1]` as the audit string, converting both the input and key to bytes (UTF-8), and XOR-ing byte-for-byte with `key[i % len(key)]` cycling — get this modulus logic exactly right since it's the most common source of mismatch against the reference. Format the output as a continuous lowercase hex string (e.g. via `''.join(f'{b:02x}' for b in result)`) with a trailing newline, exactly matching how a reference implementation would serialize bytes to hex — no separators, no uppercase. Add `#!/usr/bin/env python3` as the first line and `chmod +x` the file so it runs directly. Since the verifier fuzzes with hundreds of random payloads including edge cases (empty string, single char, strings longer than the key, non-ASCII), test your script manually against a few hand-computed XOR values before considering it done, paying attention to how Python handles byte-level operations on str vs bytes objects.