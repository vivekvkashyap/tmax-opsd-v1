Develop against `/app/test_fixture.mp4` first to build intuition, but design the classifier to generalize, since it will be graded on a hidden corpus. Extract frames with OpenCV (`cv2.VideoCapture`) or `ffmpeg`, crop the center 50x50 region each frame, convert to grayscale, and average pixel intensity to build a 1D brightness time-series sampled at 30 FPS. Compute an FFT of this series (`numpy.fft.rfft`), take the power spectrum (magnitude squared), and normalize it into a probability distribution (divide by its sum) so it can be compared to an "ideal" spectrum that has all its mass concentrated at the 10 Hz bin. Use `scipy.stats.wasserstein_distance` (or an equivalent distance over the frequency-bin index/probability arrays) between the observed and ideal normalized spectra as your stutter metric — a clean 10 Hz pulse concentrates energy sharply at that bin (low distance), while stuttering spreads energy across other bins (higher distance). For the convergence/early-exit requirement, compute this distance on growing windows (1s, 2s, 3s, ...) and stop once the metric changes by less than 5% between consecutive window sizes, then threshold the converged value to decide CLEAN vs EVIL — pick and empirically validate that threshold using both the fixture and your own synthetic clean/evil test videos rather than guessing a number, and print only "CLEAN" or "EVIL" with no extra output.