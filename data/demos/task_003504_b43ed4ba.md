Treat this as two separate bugs plus an integration step, and confirm each fix independently before moving on. First, analyze `/home/user/dumps/core.monitor-service` with `gdb` (`gdb <binary> /home/user/dumps/core.monitor-service`, then `bt` for a backtrace) to find the exact panic site — expect an out-of-bounds slice/array index triggered by a malformed or unusually-shaped frame; once located in the Rust source, replace the unchecked indexing with a bounds-checked access (`.get(i)` returning `Option`, or an explicit length check) that skips/handles the bad frame instead of panicking. Second, for the undercounted `processed_frames`, look for a counter shared across worker threads without synchronization (a plain `i32`/`static mut`, or a `Mutex`/`Arc` used incorrectly, e.g. read-modify-write outside the lock) — replace it with an `AtomicUsize` + `fetch_add(1, Ordering::SeqCst)` or a properly scoped `Mutex` lock around the whole increment, since concurrent non-atomic increments silently lose updates under contention. Rebuild, run the fixed service against `/app/datacenter_feed.mp4` in the background bound to `127.0.0.1:9090`, then hit `GET /metrics` yourself with `curl` to confirm `processed_frames` equals the video's true total frame count (derive this from the video itself, e.g. via `ffprobe`, not by guessing) and `anomalies_detected` matches your brightness-threshold detection logic, before leaving the service running.