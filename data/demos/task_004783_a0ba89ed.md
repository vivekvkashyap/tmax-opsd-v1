Read `/home/user/calculate_sum.py` and locate the `struct.unpack` call used to parse each 32-bit reading — check the format character: `'<i'` is signed little-endian, so any value above 2^31-1 wraps to negative when Python interprets the raw bytes. Change the format string to the unsigned variant `'<I'` (uppercase I) so values are read as unsigned 32-bit integers, leaving the rest of the parsing (header read, loop, summation) untouched. After fixing, run `python3 /home/user/calculate_sum.py /home/user/sensor_data.bin`, capture its printed output, and write just that number (no extra text/whitespace) to `/home/user/correct_sum.txt`. Double check by manually summing the unsigned values via a quick sanity script if the printed sum still looks off.
