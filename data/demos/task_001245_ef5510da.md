Build this in dependency order: create the bare repo (`git init --bare /home/user/deploy_repo.git`), the source repo with `updater.c` (parse argv strictly — only act on exactly one argument equal to `--deploy`, appending the exact literal string to the log in append mode), and commit it. Write the `post-receive` hook as bash that reads `oldrev newrev refname` from stdin in a loop, checks out `newrev` into a temp dir (`git archive newrev | tar -x -C $tmpdir`, or a temp clone), attempts `gcc updater.c -o updater` there, and branches on the compiler's exit code: on success, move the binary into place, append `BUILD SUCCESS: $newrev` to the log, then execute `/home/user/deploy_target/updater --deploy` so the log picks up its output too; on failure, append `BUILD FAILED: $newrev` and `exit 1` so git rejects the push. Make the hook executable, wire up the remote, push, and verify `/home/user/deploy.log` yourself. Then test the failure path by breaking `updater.c` and pushing again to confirm the hook fails the push and logs the new commit hash correctly.
