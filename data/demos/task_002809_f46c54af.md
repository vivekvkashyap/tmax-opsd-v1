Start by running `cd /home/user/aggregator && go test -v ./...` to see the actual failure mode (timeout vs panic) before editing anything. In `models/trade.go`, check each field's Go type against how the JSON payloads actually look (the test sends a `price` like `150.25`) — a numeric JSON field with a decimal point cannot unmarshal into a Go `int`, so that field's type needs to become `float64`. In `ws/client.go`, look at `Start()`: if its message-read loop runs synchronously in the caller's goroutine, any caller expecting `Start()` to return immediately (so it can proceed to start the REST server) will hang forever waiting for it — the fix is to move the blocking `for { ReadMessage() ... }` loop's execution onto its own goroutine from within `Start()` itself (e.g. wrap the loop body in `go func() { ... }()`) so `Start()` returns right away while the read loop keeps running concurrently in the background. Keep the existing mutex-protected `Stats` map access pattern intact. Do not touch `main_test.go`. After fixing both files, rerun `go test -v ./...` until it passes, then redirect that passing run's full output to `/home/user/test_results.log`.