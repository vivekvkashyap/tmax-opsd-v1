Treat this as two linked deliverables: a database migration and a Rust ETL. For the SQL side, first `DROP INDEX idx_corrupt_source;`, then design `idx_events_optimized` to cover the columns your analytical query actually filters/orders/groups by (source_node, target_node, event_time) so it's a genuine covering index for the window-function query, not an arbitrary index. For the "current edge state" logic, use a window function like `ROW_NUMBER() OVER (PARTITION BY source_node, target_node ORDER BY event_time DESC, id DESC)` and keep only rows where that rank is 1 and action='ADD' — reason through the seed data by hand (trace each edge's event history) to confirm your query produces the right set before trusting it. For the Rust program, use `rusqlite` with proper parameterized `LIMIT ?1 OFFSET ?2` pagination in a loop (50 rows per page) rather than fetching everything at once, accumulate results into a `BTreeMap<String, BTreeSet<i64>>` (or sort explicitly) to get ascending order for free, then serialize with `serde_json`, only emitting keys with a non-empty vector. Verify by inspecting the DB's `sqlite_master` for indexes and diffing the generated JSON against your own manual trace of the seed data.