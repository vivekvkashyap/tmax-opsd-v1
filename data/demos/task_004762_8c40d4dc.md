Restrict yourself strictly to POSIX shell tools. First OCR the config image with `tesseract /app/config_matrix.png -` and carefully read off the five labeled numeric values (PRIOR_A, PRIOR_B, W1, W2, W3) — OCR can misread decimals/negatives, so sanity-check the extracted numbers look like reasonable weights/probabilities before hardcoding them into your script as shell variables. In `etl_pipeline.sh`, join `/app/table_a.tsv` and `/app/table_b.csv` on `ID` — normalize both to a common delimiter first (e.g. `tr` the TSV tabs to commas, strip the CSV header) then use `join -t, -1 1 -2 1` on ID-sorted versions of each file (join requires sorted input). Feed the joined rows into `awk -F,` (or `bc`) to compute `Score = PRIOR_A*(W1*F1 + W2*F2) + PRIOR_B*(W3*F3)` per row using the OCR'd constants, format numbers plainly (avoid scientific notation), write the `ID,Score` header plus rows sorted numerically by ID (`sort -t, -k1,1n`) to `/home/user/results.csv`. Validate your own arithmetic on one row by hand before trusting the pipeline output, since the verifier checks MSE against precise expected scores.
