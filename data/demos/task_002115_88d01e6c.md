Chain four independent sub-results into the final patch, and derive rather than guess each one. First compile and exploit `/home/user/src/upload.c` as-is: study how it builds the destination path from the filename parameter, then craft an upload filename using `../` traversal sequences to make it write/read outside its intended directory and reach `/etc/legacy_app/secret.token`, capturing its contents. Second, OCR the clue image with `tesseract /app/credential_clue.png -` (or similar) to recover the embedded secret text — read whatever text tesseract actually outputs, don't assume a value. Third, compute the new token as `sha256(<ocr_text>:<secret.token_contents>)` hex digest using `sha256sum` or a script, concatenating in that exact order with a single colon. Fourth, edit the source (save as `upload_patched.c`): reject any filename containing `../` or starting with `/` before it's used in any path construction, and replace the old static-token comparison with a comparison against your newly computed SHA-256 hex string. Validate your patch yourself by compiling it and testing both benign filenames and traversal attempts before finishing, since the grader runs an automated suite requiring 100% correctness on both fronts.