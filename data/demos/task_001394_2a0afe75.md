Start by reading `/app/services/start_all.sh` and the three service scripts it launches — compare the port the API script actually binds against the port the task requires (8080), and check the directory the archiver writes rotated `.gz` files into against where the task says logs should land; fix whichever mismatch(es) you find (port flag and/or output directory), then start all three services and let them run 15+ seconds before touching the parser. For the Go parser: use `compress/gzip` but wrap reads so that hitting `io.ErrUnexpectedEOF`/CRC or truncation errors mid-stream does not abort the whole file — read line-by-line (or record-by-record) inside a loop with error recovery, keeping everything successfully decoded before the corruption point rather than discarding the whole archive. For the multi-line `<Reading>...</Reading>` XML records, detect which encoding a given chunk uses (try UTF-8 validity first, and if it fails/produces invalid runes, decode as ISO-8859-1/Latin-1 via `golang.org/x/text/encoding/charmap` or a manual byte-to-rune mapping) before parsing. Filter out `Value < 0.0`, then POST each valid record as JSON to `http://127.0.0.1:8080/submit`. Time your own run and check it comfortably finishes under 5 seconds; if not, add concurrency (goroutines/worker pool) for file processing and HTTP posts.