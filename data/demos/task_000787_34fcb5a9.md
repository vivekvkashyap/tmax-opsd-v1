Build the Rust service incrementally and test each stage in isolation before wiring them together. First, get frame extraction right: call `ffmpeg -ss <timestamp> -i /app/video.mp4 -frames:v 1 -s 64x64 -f rawvideo -pix_fmt rgb24 -` via `std::process::Command`, capturing stdout as bytes — verify each output is exactly 12288 bytes before hashing with `sha2::Sha256`. Second, parse the CSV manually or with the `csv` crate configured to treat quoted embedded newlines as part of a single record (the `csv` crate handles RFC4180 multi-line quoted fields by default); after parsing, drop any record whose `notes` field contains a `\n` character, then match cleaned rows to frames by exact integer timestamp — timestamps missing from the cleaned set simply have no corresponding output entry. Third, apply consecutive deduplication only after joining timestamp order: walk the chronological sequence of frame+value pairs and skip any entry whose `value` equals the immediately preceding *kept* entry's value (not just the previous raw entry). Finally wire an axum/tokio handler on `/data` at `127.0.0.1:9000` that runs this pipeline (or precomputes once at startup) and serializes the result with `serde_json`. Test the endpoint with `curl` before finishing.
