Work stage by stage, testing each in isolation before integrating. Stage 1: inspect `/app/libmobilemanifest`'s Makefile for anything disabling Cgo (e.g. `CGO_ENABLED=0`) and fix it so Cgo compiles; then inspect `parser.c` for a fixed-size stack buffer being written one byte past its bound (an off-by-one on a header exactly at the buffer's size) and correct the bound so it can't overflow — build and run the module's own tests to confirm the segfault is gone before moving on. Stage 2: scaffold a Go module at `/home/user/validator` with `net/http`, wire the `/validate` POST handler to read the raw body, call into the fixed `libmobilemanifest` package to extract the JSON manifest, then json-unmarshal it and check that every string in `requires` appears in `provides` (a simple set membership check) and that `version` matches a semver regex like `^v\d+\.\d+\.\d+$`; return 200 only if extraction, JSON parsing, and both constraints all succeed, otherwise 400. Stage 3: before declaring done, manually run your server against a handful of files from `/app/corpora/clean/` and `/app/corpora/evil/` with `curl --data-binary` and inspect the actual HTTP codes — don't assume, verify — then iterate on edge cases the evil corpus deliberately probes (malformed headers, unmet requires, bad version strings). Write the exact launch command to `/home/user/start.sh` and leave the server running on :8080.
