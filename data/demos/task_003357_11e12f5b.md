Work in stages and verify each before moving on. First use `find /home/user/raw_artifacts -maxdepth 1 -type f -executable -size +100k` to list candidates (note find's `-size +100k` matches strictly greater than 100KiB in 1024-byte blocks — check the boundary case matches "102KB or more" as intended), then `cp` matches into `curated_repo` (create dirs with `mkdir -p` first). For renaming, loop over the copied files and use parameter expansion or `sed`/basename tricks: strip the extension with `"${f%.*}"`, replace spaces with `tr ' ' '_'` or `${var// /_}`, then assemble `stable_<name>.bin`; be careful with the order (extension-strip before or after prefixing must not corrupt names with no extension). For the tar backups, `cd` into `/home/user` and run `tar --create --file=/home/user/backup/full.tar --listed-incremental=/home/user/backup/repo.snar curated_repo` (relative paths avoid absolute-path warnings). After touching the new dummy file, run the exact same tar command again pointed at `inc.tar` reusing the same `.snar` file — GNU tar automatically detects unchanged files from the snapshot and only includes new/changed entries, so don't try to manually diff file lists yourself.
