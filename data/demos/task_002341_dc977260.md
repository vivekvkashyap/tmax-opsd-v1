First reverse-engineer the schema by inspecting the table (`.schema lock_events`) and sampling rows to understand that each row is a single timestamped event of type ACQUIRE/WAIT/RELEASE tied to a transaction and a resource. Express the deadlock pattern as a self-join in SQL rather than trying to hand-roll graph logic in bash: join the ACQUIRE events table to itself on complementary resources, i.e. find pairs (a1, a2) where a1.event_type='ACQUIRE', a2.event_type='ACQUIRE', a1.transaction_id != a2.transaction_id, then require a matching WAIT row where a1's transaction waits on a2's resource *after* a1's own acquire, and a matching WAIT row where a2's transaction waits on a1's resource *after* a2's own acquire — use timestamp comparisons to enforce the acquire-before-wait ordering per transaction. Normalize each detected pair so the alphabetically smaller transaction_id becomes `txn_1` and its acquired resource (which the other transaction is waiting on) becomes `resource_1`; be careful the resource columns are assigned based on which transaction acquired which, not by input order. Dedupe symmetric pair matches, sort by `txn_1` then `txn_2`, and write via `sqlite3 -csv` or manual formatting with no header row.
