Work this polyglot pipeline end-to-end, one stage at a time, and test each stage in isolation before wiring them together. First read `/home/user/legacy/calc.js` carefully and port its exact arithmetic (loop order, operator precedence, modulus) into Python — off-by-one or wrong modulus base is the most common naive-attempt error, so write a tiny Python/Node comparison test on a few sample byte arrays before trusting your port. Second, open `/home/user/rust_lib/src/lib.rs`, run `cargo build --release` to see the actual borrow-checker error, and fix ownership (e.g. clone or restructure so a moved value isn't reused) without touching the math; then confirm the .so is produced. Third, write the `.proto` file exactly as specified and run `python -m grpc_tools.protoc` from `/home/user/server` to generate the pb2 modules. Fourth, in the server, load the shared library via `ctypes.CDLL(...)`, set `.argtypes`/`.restype` to `ctypes.c_uint64` explicitly (ctypes defaults to c_int and will silently truncate/misinterpret large values), then call it with your Python-computed base signature. Start the server in the background, run the client, and only then write the integer result to the output file.