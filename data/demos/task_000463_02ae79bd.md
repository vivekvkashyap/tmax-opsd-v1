Build filter.py to read a raw email from stdin with Python's `email` module (`email.message_from_file` or `policy.default.parse`), then validate strictly: Subject must match exactly `^\[ALERT\] \S+$` (capture metric_name), the Date header must parse via `email.utils.parsedate_to_datetime` (which already understands RFC 2822 offsets and named US timezones) and then be converted to UTC (`.astimezone(timezone.utc)`) and formatted `%Y-%m-%dT%H:%M:%SZ`, and the body must contain a line matching exactly `^Value: [-+]?[0-9]*\.?[0-9]+$`. Any parse failure, missing header, malformed subject/date/value, or unexpected content should cause a caught exception path that exits 1 with no stdout — test this against both corpora directories first (loop over each file, run your script, check exit codes) and iterate until the clean set all pass and the evil set all fail, since the evil corpus is specifically designed to break naive regex/parsing assumptions (XSS-like injection in headers, missing fields, malformed dates).

For process_mail.sh, loop over files in incoming_mail/, pipe each through `python3 filter.py`, and only if it exits 0 (`$?` or use `if python3 filter.py < "$f" > /tmp/out; then`) POST the captured JSON via `curl -s -X POST -H "Content-Type: application/json" -d @/tmp/out http://localhost:10080/ingest`; always `rm -f "$f"` afterward regardless of success. For trigger_alert.exp, use standard Expect idioms — `spawn nc localhost 10023`, then repeated `expect "Username:"` / `send "admin\r"` pairs for each prompt/response in the sequence given, finishing with `expect eof` or similar. Run the expect script then process_mail.sh at the end to produce the final ingested metric.