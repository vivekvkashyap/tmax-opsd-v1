Install pandas, pyarrow, scikit-learn, and matplotlib via pip first. In /home/user/run_pipeline.py, set the matplotlib backend with `matplotlib.use('Agg')` immediately after importing matplotlib and before importing pyplot or plotting anything — doing this after other matplotlib imports or after creating a figure will fail on a headless server. Load train/test parquet files, split into the f0..f49 feature matrix and label vector, and instantiate `RandomForestClassifier(random_state=42)` with otherwise default parameters (do not tune hyperparameters) to keep results reproducible. For the benchmark, call `.predict()` on the full test feature matrix 10 times in a loop, timing each call individually (e.g. with `time.perf_counter()`), and average the 10 durations — write just that float to inference_time.txt. Compute accuracy_score on the same test predictions and write just that float to accuracy.txt. Build the confusion matrix (e.g. via `ConfusionMatrixDisplay.from_predictions` or `confusion_matrix` + manual plot) and save with `plt.savefig(...)`, then verify the file is reasonably sized (not a blank canvas) before finishing.