Implement the binary record parser in C using `mmap()` to map the whole file read-only, then walk it sequentially: at each offset verify the 4-byte magic `"DATA"`, read the next 4 bytes as a little-endian `uint32_t` timestamp (careful with endianness — don't just cast a struct pointer on a potentially misaligned/big-endian-unsafe build; assemble the integer byte-by-byte or use `memcpy` into a properly-typed variable, same for the 2-byte little-endian payload length), then read that many payload bytes as the ASCII string and print `[<timestamp>] <payload>\n`, advancing your offset by the fixed header size plus the variable payload length until you reach the end of the mapped region. Compile with `gcc -o /home/user/parser /home/user/parser.c`. For the watcher script, use an infinite `while true; do ... sleep 1; done` loop that globs `/home/user/incoming/*`, guards against the no-match case (nullglob or checking the file exists), runs the parser on each file appending stdout to the log with `>>`, then removes the processed file — run it with `&` to background it. After moving the staging files into `incoming`, sleep long enough (a few seconds, more than one watcher iteration) before checking the final log so the background loop has time to catch and process every file.
