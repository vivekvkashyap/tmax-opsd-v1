Work this as a four-stage pipeline: reshape, fix math, fix parallelism, run. First, use `awk` to pivot `raw_signals.csv` from wide (Time, Node_0..Node_999) to long format: read the header to get node IDs, then for each data row emit one `Time,NodeID,Value` line per column (loop over fields with `for(i=2;i<=NF;i++)`), writing the header `Time,NodeID,Value` first. Second, locate the magnitude computation in `src/spectral_density.c` — a correct Fourier magnitude must be `sqrt(re*re + im*im)`, not a linear combination of `re` and `im`; search for the `sqrt(` call handling the complex components and fix the squaring. Third, for the performance issue, add `-fopenmp` to both `CFLAGS` and `LDFLAGS` in the `Makefile`, and insert `#pragma omp parallel for` immediately above the main loop that iterates over nodes in `spectral_density.c` (make sure loop-carried state, like accumulators, is either private or restructured to be parallel-safe). Then `make clean && make` inside `/app/libspectral-0.1`, and run `./bin/analyze_spectra /home/user/long_signals.csv /home/user/final_densities.txt`. Sanity-check a few output rows by hand against a manually computed FFT magnitude for a short synthetic signal before trusting the full run.