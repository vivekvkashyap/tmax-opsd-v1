Build the script in the exact sequence the spec describes so seeding stays aligned: first implement a straightforward trapezoidal-rule integrator, run it at each of the five N values, and record `abs(estimate - true_value)` per N — use the given reference value directly rather than recomputing it analytically. Then, for N=16000, compute the trapezoid contribution of each of the 160 contiguous 100-interval blocks separately (be careful with the shared boundary points between adjacent blocks — each block's endpoints get the trapezoidal half-weight independently, matching how you'd split a global sum by block), and cast each block sum to `numpy.float32` to simulate reduced precision. Call `numpy.random.seed(42)` once, then loop 1000 times shuffling the float32 block list and summing it with float32 accumulation (order matters here — this is exactly what demonstrates non-associativity of floating point) to build the 1000 simulated totals. Reset the seed again to 42 immediately before bootstrapping, resample the 1000 sums with replacement 10000 times, take the mean of each resample, and pull the 2.5th/97.5th percentiles for the CI. Match the output JSON schema and key order exactly.
