Work the three subsystems independently, then integrate. First, extract the auth token by reading `/app/system_auth.mp3`'s metadata directly with `ffprobe -show_format /app/system_auth.mp3` (or `exiftool`) and pulling the `title` tag value — don't hardcode a guessed token. Second, build the CMake helper: run `cmake . && make` in `/home/user/helper_tool`, observe the undefined-symbol link error for math functions, and add `target_link_libraries(payload_eval m)` to `CMakeLists.txt`, then rebuild so `payload_eval` exists and reads a value from stdin, returning nonzero for "invalid" payloads. Third, write `server.sh` using `socat TCP-LISTEN:8080,bind=127.0.0.1,reuseaddr,fork EXEC:...` (or a read-loop against a listening fd) to parse each incoming HTTP request's method/path/headers/body by hand: check the `Authorization: Bearer <token>` header against the extracted token (401 if missing/wrong), pipe the body into `payload_eval` and check its exit code (400 if nonzero), and maintain a per-client request-timestamp log under `/tmp/api_state/` to enforce "max 3 successes per rolling 10s window" (429 if exceeded) before returning 200. Test each status path manually with `curl`/`nc` before backgrounding the server.