Read `process_transaction` carefully first: it locks `node_locks[src]` then `node_locks[dst]` unconditionally, so two threads transacting in opposite directions (e.g. 1→2 and 2→1) can each hold one lock and wait forever on the other — classic deadlock. Fix it with a consistent global lock ordering: always acquire the lower-indexed node's mutex first regardless of which is src/dst (`int first = std::min(src, dst), second = std::max(src, dst); node_locks[first].lock(); node_locks[second].lock();`), then unlock in reverse order — this guarantees no two threads can ever wait on each other cyclically. For `materialize_top_transactions`, copy `transactions` into a local vector, filter with `amount >= min_amount`, sort with a custom comparator (descending by `amount`; on ties, ascending by `src`, then ascending by `dst`), then apply `offset`/`limit` manually with bounds checks (guard `offset >= filtered.size()` producing an empty result rather than crashing or reading out of bounds). Write each surviving edge as `src,dst,amount\n` to `/home/user/view.txt`. Compile with `g++ -std=c++11 -pthread`, run it multiple times if needed to confirm the deadlock fix is reliable (no hangs) and the output file is deterministic and correctly ordered.