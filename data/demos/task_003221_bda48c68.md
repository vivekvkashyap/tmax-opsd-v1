Fix infrastructure before writing logic. Inspect `/home/user/services/config_api/settings.env` for the Redis connection setting — it points at the wrong host/port; correct it to Redis's actual address (`127.0.0.1:6379`) and restart the Flask app (`python /home/user/services/config_api/app.py &`) so `/stream` starts returning data. In `/home/user/pipeline.py`: fetch the stream, first drop events failing the constraints (mem_limit out of [512,65536], cpu_limit negative — treat null as valid and keep it for now). Then, per `server_id`, sort remaining events by timestamp and fill any null `cpu_limit`/`mem_limit` via linear interpolation using the nearest valid neighbor before and after *within that server_id's sequence*; drop events at a sequence's edge that have no valid neighbor on one side. For grouping, compute Jaccard similarity between the sets of character bigrams of adjacent `config_blob` strings (per server, in timestamp order) and classify each transition as Minor Tweak (>=0.8) or Major Update otherwise. Render the Markdown report per server matching the template exactly (2-decimal averages), write imputed events to `/home/user/processed_events.jsonl`, and POST each report as `{"server_id":..., "report_markdown":...}` to `http://127.0.0.1:5001/changelog`. Since scoring is MSE against hidden ground truth, get the interpolation math exact — verify manually on a small subset before trusting the full run.