Replace the channel-based reduction with an ordered array: have each goroutine write its partial sum into a pre-allocated `[]float64` slice at index `id` (or collect via a channel of `{id, value}` structs) instead of pushing into an unordered channel, then after `wg.Wait()` sum the slice sequentially from index 0 to G-1 with a simple `for` loop — this guarantees bit-identical reduction order every run. For profiling, wrap `main`'s work with `pprof.StartCPUProfile(f)` right after creating `/home/user/cpu.prof` and call `pprof.StopCPUProfile()` via `defer` before exit. Write the ordered partial sums to the CSV exactly in chunk-ID order using `fmt.Fprintf(f, "%d,%.15f\n", id, partials[id])` before computing the total. Keep N=10000000, G=16, and the 100-term Fourier sum unchanged — only change how partials are aggregated, not the per-chunk math. Verify determinism yourself by running the fixed program twice and diffing the outputs before finalizing result.txt. For plot.py, just read the CSV with csv/pandas and use matplotlib's bar() over chunk id vs partial sum, saving to partials.png.