Structure the C++ program as: read all three CSVs into memory (or via `std::async`/threads, one task per file, since parallel processing is required), parse each line by splitting on commas (careful: `user_name` values won't contain commas here, so simple split is safe), and deduplicate by `event_id` using a `std::unordered_map<std::string, Record>` keyed on event_id, keeping the first occurrence encountered. For the encoding fix, iterate each byte of `user_name`: any byte with value > 127 is a Latin-1 code point equal to that byte's value, so encode it into UTF-8 as `0xC0 | (byte >> 6)` followed by `0x80 | (byte & 0x3F)` (a 2-byte UTF-8 sequence) — do this byte-by-byte across the whole field, leaving ASCII bytes (<=127) untouched. Sort the deduplicated records with `std::sort` (or `std::execution::par` variant) using a comparator on `(timestamp, event_id)` lexical ascending. Write the output with the required header line, then compile with the exact `g++` command given and run the binary yourself, diffing your output against the expected CSV content structurally (same rows/columns) rather than assuming — check a few converted names (José, Müller, François, Björn) render correctly as UTF-8 in the output file.
