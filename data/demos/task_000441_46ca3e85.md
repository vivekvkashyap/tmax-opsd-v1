Write the C++ program to open the SQLite DB with `sqlite3_open`, then execute each step as a separate `sqlite3_exec`/prepared statement, checking return codes at every call so failures surface instead of silently corrupting output. First query `sqlite_master` (or use `PRAGMA index_list`) to enumerate existing indexes on `transactions` and `DROP INDEX` each one dynamically rather than hardcoding a name, since the "bad" index name isn't guaranteed. Then `CREATE INDEX` covering the exact join/filter/order columns the analytical query needs (user_id, product_id, tx_date, amount) so the planner can use an index instead of a full scan — verify this by inspecting the `EXPLAIN QUERY PLAN` output for `SEARCH`/index usage rather than `SCAN`. Build the aggregation query with the join, `BETWEEN` date bounds, `GROUP BY` user, `SUM`/`AVG`, and a two-key `ORDER BY` (volume DESC, name ASC) with `LIMIT 5` exactly as specified. When writing `report.csv`, format floats with `std::fixed << std::setprecision(2)` (from `<iomanip>`) rather than default streaming, since default float printing won't give 2 decimals. Compile and run exactly with the given g++ command before considering the task done.