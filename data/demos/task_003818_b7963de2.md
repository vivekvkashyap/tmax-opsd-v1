Work phase by phase. First run `make` in `/home/user/event_service/`, read the actual compiler errors (don't guess) — a missing C++ standard flag or use of a feature requiring a newer standard is a common cause, so check the Makefile's CXXFLAGS and add `-std=c++17` (or whatever the error message demands) rather than rewriting source unnecessarily. Once it builds, run `./event_processor backlog.txt` under `gdb --args ./event_processor backlog.txt` (or with core dumps enabled) to get a backtrace pinpointing the crash line in `EventProcessor::add_event`; inspect how the ring-buffer index is computed from `event_id`. Recall that C++'s `%` operator can return a negative result for negative operands, so an index like `event_id % 10` can go out of bounds for negative IDs — normalize it (e.g. `((event_id % 10) + 10) % 10`) so it always lands in `[0,10)`. Write `test_regression.cpp` to explicitly feed a negative event_id and assert no crash, exiting 0/1 accordingly, then add a `test` Makefile target that builds and runs it. Finally rebuild, rerun against `backlog.txt`, and capture the program's exact final summary line into `/home/user/final_state.txt` (redirect actual stdout, don't retype it by hand).
