Treat `/app/query_analyzer` as a black-box oracle you invoke and parse, not something to reverse-engineer byte-for-byte: run it against each sample in `/home/user/samples/clean/` and `/home/user/samples/evil/`, capture its stdout, and diff the outputs between the two classes to find the distinguishing signal (e.g. a `PLAN: FULL_SCAN` vs `PLAN: INDEX_SCAN` line, and/or a numeric centrality score). Once you've identified the pattern purely from observed behavior on the labeled samples, write `detector.sh` to: take the JSON file path as `$1`, invoke `/app/query_analyzer "$1"`, grep/parse its output for the plan type and any centrality number, and `exit 1` if the plan is a full/wildcard scan or the centrality score exceeds the threshold you observed separating clean from evil, otherwise `exit 0`. Validate your script against every file in both provided sample directories first, confirming 100% correct exit codes locally, before trusting it will generalize to the hidden corpus — if you see misses, go back and look more closely at what differs in the analyzer's output for those specific samples rather than hardcoding filenames.
