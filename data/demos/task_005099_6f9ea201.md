Split this into data prep, library fix, and server, testing each independently. For Step 1, load the two CSVs into `sqlite3` (in-memory), and write the normalization query using a window function: `raw_collaborations * 1.0 / MAX(raw_collaborations) OVER (PARTITION BY source_id)` gives each edge's weight relative to its source node's largest outgoing edge — export nodes/edges to the exact JSON schema given. For Step 2, read `graph_transaction_lib/graph_transaction_lib/locks.py`: a function that acquires two per-node locks in the order the caller passed them (`src` then `dst`) will deadlock when two concurrent calls use opposite orderings (A,B) and (B,A), since each holds the first lock while waiting for the second. Fix it by always acquiring locks in a globally consistent order regardless of call direction — e.g. `lo, hi = min(src,dst), max(src,dst)` and lock `lo` before `hi` — so concurrent updates on the same pair never form a cycle. For Step 3, implement `/shortest_path` with Dijkstra over the weighted graph and `/update_edge` calling the now-deadlock-free `ThreadSafeGraph.update_edge`, bind to `0.0.0.0:8080`, and stress-test yourself by firing two concurrent opposite-direction `POST /update_edge` requests (e.g. with `curl &` twice) to confirm both return quickly before leaving the server running.
