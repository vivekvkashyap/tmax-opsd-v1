Debug the build first: inspect `/app/spectrosim/Makefile`'s gcc invocation and try `import spectrosim` in Python to see the actual linker error — an `undefined symbol` error for a math function (e.g. `exp`, `sin`) at import time means the shared library was compiled without linking libm; add `-lm` to the link flags, rerun `make`, and confirm both `python3 -c "import spectrosim"` and `pytest /app/spectrosim/tests/` succeed before moving on. For the fitting script, load `/home/user/data/spectrum.csv`, use `scipy.optimize.curve_fit` (or `least_squares`/`minimize`) with `spectrosim.simulate` as the model function against the 6 parameters, and give it sensible initial guesses derived from the data itself (e.g. peak locations/heights read off the CSV, since the signal is a mixture of two Gaussian-like peaks) rather than arbitrary constants, since a bad initial guess is the most common reason non-linear fits fail to converge. Iterate/check the residual MSE locally before finalizing, and write the six best-fit parameters to `/home/user/solution.json` with exactly the keys given.