In `/home/user/analyze.py`, load `job_graph.json` as an adjacency list and run a plain BFS from `"JOB_START"` (not DFS, and not a naive "any path" search) to guarantee the fewest-edges path to `"JOB_END"`, tracking parent pointers to reconstruct the path in order. For each job on that path, open `backups.db` with `sqlite3` and pull `storage_target` using `json_extract(config_json, '$.storage_target')` rather than parsing the JSON text in Python, since the spec explicitly wants SQLite's JSON functions. For the moving average, write a single SQL query per job using a window function: `AVG(duration_seconds) OVER (PARTITION BY job_id ORDER BY run_date ROWS BETWEEN 2 PRECEDING AND CURRENT ROW)`, then select the row with the maximum `run_date` for that job_id from the windowed result (e.g. wrap it in a subquery and order by `run_date DESC LIMIT 1`) — do not just average the last 3 rows in Python, since the window must be computed inside the database as specified. Assemble the ordered list of `{job_id, storage_target, latest_3run_avg}` dicts and `json.dump` to `report.json`, preserving path order and using real floats. Separately write `optimize.sql` containing a composite `CREATE INDEX idx_backup_logs_job_date ON backup_logs(job_id, run_date);` (do not execute it, just save the file) to support the partition/sort pattern used above.
