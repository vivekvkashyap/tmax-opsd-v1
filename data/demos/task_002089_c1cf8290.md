For the video part, open the mp4 with `cv2.VideoCapture`, loop frames, convert each to grayscale (`cv2.cvtColor(..., COLOR_BGR2GRAY)`), take `frame.mean()`, round to 2 decimals, and write `frame_index,brightness` rows in frame order — don't skip frames or assume a fixed frame count, read it from the video itself.

For the classifier, first inspect the train/clean and train/evil CSVs to learn the linear relationship between temperature and pressure (fit OLS with `numpy.polyfit` or `statsmodels`/`scipy.stats.linregress` on each clean sample to see the true slope/intercept/noise scale, and do the same on evil samples to see how they diverge). Build `classifier.py` to run OLS on the input CSV's temperature vs pressure, then use the regression's standard errors to construct confidence intervals for slope and intercept and compare the residual standard error to what you observed in clean training data — reject as EVIL if slope, intercept, or residual variance falls outside the expected range with statistical margin (not a hardcoded threshold that only fits the training filenames). Test against both train/clean and train/evil before assuming it generalizes to a hidden test set — do not hardcode filenames or row counts.