Start by extracting the two hidden config parameters from `/app/config_spec.png` using OCR (e.g. `tesseract /app/config_spec.png stdout`, cropping/upscaling first if recognition is poor); read the WINDOW_SIZE and NORMALIZATION_PREFIX values off the image rather than guessing them. Then implement `sanitizer.go` as a streaming line-by-line JSONL processor: for each record, normalize `config_payload` by lowercasing, stripping all whitespace, and prepending the OCR'd prefix — but only use the normalized form for dedup comparison, never for output. Track per-host state: last valid timestamp/index for interpolating `ts==0` records (average of the surrounding valid timestamps for that host), and a per-host map from normalized payload to the last *accepted* record's timestamp, dropping a record when `ts - previous_ts <= WINDOW_SIZE` and the normalized payload matches. Process records in order per host so interpolation and windowing see correctly sequenced timestamps. Write only non-dropped records (with imputed `ts` filled in, original `config_payload` untouched) to the output file preserving all JSON keys. Test against small hand-built clean and duplicate-triggering JSONL fixtures before trusting it on the real corpus.
