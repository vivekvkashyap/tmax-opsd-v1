First recover the exempt project name: run `tesseract /app/billing_rules.png stdout` (or similar OCR) and read the output line matching `EXEMPT_PROJECT: <name>` — use that exact recovered string literally in your script's grep pattern, don't hardcode a guess. Then implement `/home/user/log_filter.sh` as an explicit rule cascade in the correct precedence order: default keep (exit 0); if filename ends in `.log` AND file contains the literal `[DEBUG]` substring, mark delete (exit 1); but if the file ALSO contains `[ERROR]` OR the exact exempt-project string, override back to keep (exit 0) regardless of the delete rule — implement the overrides as checks that run after and can flip the delete decision, not as early returns that skip them. Use `grep -qF` for literal substring matching to avoid regex-metacharacter surprises. Make the script executable (`chmod +x`). Before trusting it, manually test it against a few files matching each rule (plain debug log, debug+error, debug+exempt, non-.log with DEBUG) to confirm exit codes. Then write `logrotate.conf` with `daily`, `rotate 7`, `compress`, and `missingok` for `/home/user/logs/*.log`.