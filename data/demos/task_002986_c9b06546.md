Build this as a pipeline of clearly separable stages so each is testable independently. First, normalize encoding per file: detect `.utf16.txt` vs `.utf8.txt` by filename suffix and convert both to a UTF-8 in-memory/temp representation (e.g. `iconv -f UTF-16LE -t UTF-8` for the former). Second, split each normalized file on lines that are exactly `---` to get entries, keep only entries containing the exact line `STATE: ACTIVE`, then extract the text strictly between `<PAYLOAD>` and `</PAYLOAD>` lines — preserve internal newlines exactly (don't trim/collapse them), since the hash is computed over the payload including its embedded newlines. Third, compute MD5 over each extracted payload's exact bytes (be careful about trailing newline inclusion — match how the payload actually appears between the tags) and collect unique hashes in a set. Fourth, parallelize the per-file extraction step (e.g. `ls /home/user/configs | xargs -P4 -I{} ...` or Python `multiprocessing.Pool`), then merge results in the driver process before deduplicating and sorting. Write the sorted, deduplicated hex hashes one per line to `/home/user/unique_hashes.txt`, save the whole pipeline as `/home/user/process.sh`, `chmod +x` it, and execute it to produce the output.