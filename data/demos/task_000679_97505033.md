Bring the stack up first: run `/app/services/start_all.sh` to get Redis (6379) and the mock Data Service (9000) live, then start the Flask app from `/app/api_repo` on 127.0.0.1:8000 (check its run entrypoint/requirements before starting) and confirm the 500 by replaying `/home/user/customer_payload.json` against `POST /query`. For delta-debugging, don't hand-guess — iteratively strip keys/nesting from the payload (binary-search style: remove half, retest, keep removing while the 500 persists, back off when it stops reproducing) until you reach a minimal JSON that still 500s, and save exactly that to `/home/user/minimal_payload.json`. Check the Flask error logs/traceback for the failing line to understand the bug class (likely a type error in that code path) before touching git history. For the regression, use `git bisect start`, mark current `main` HEAD as bad and tag `v1.0` as good, then let `git bisect run` (or manual step-through) execute a test script that starts the app at each candidate commit and POSTs the minimal payload, checking for 500 vs 200 — record the exact culpable commit hash to `/home/user/bad_commit.txt`. Finally patch `app.py` at that line so the payload is handled without crashing and returns 200, restart the service, and leave all three components running.