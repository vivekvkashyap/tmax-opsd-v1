Use pandas (or plain csv) to glob all `*.csv` files in `/home/user/datasets/`, concatenate them, then group by `dataset_id` with `.sum()` on `trials` and `successes` — do not process files independently, since the same `dataset_id` can appear across multiple files and must be aggregated together first. For each grouped dataset, apply the given Beta-Binomial posterior mean formula `(alpha + s) / (alpha + beta + n)` with alpha=beta=2. Sort the resulting list of dicts alphabetically by `dataset_id` (plain string sort, not insertion order) before serializing. Round each `posterior_mean` with Python's `round(x, 4)` and make sure the JSON key order and structure match the required schema exactly (`prior_alpha`, `prior_beta`, `results`). Write the script to `/home/user/analyze.py`, then actually execute it (`python3 /home/user/analyze.py`) so `/home/user/tracking.json` is produced — writing the script alone is not sufficient.
