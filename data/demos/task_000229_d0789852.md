Reconstruct the legacy binary's transformation empirically through the provided pipeline rather than guessing constants. Compile `/app/signal_gen_src` with `make`, run `signal_gen` to produce `/home/user/raw_signals.txt`, then pipe that same file through `/app/legacy_feature_extractor` to get paired input/output ground truth in `legacy_features.txt`. Use the notebook (`analyze_transform.ipynb`, executed headlessly via `jupyter nbconvert --to notebook --execute`) to actually fit the unknown parameters: since the description says EMA-then-nonlinear-activation, treat it as `y[n] = alpha*y[n-1] + (1-alpha)*x[n]` followed by some bounded activation (tanh is a natural guess given typical output ranges) — sweep/optimize alpha and the activation's scale parameter (beta) by minimizing the error between your candidate transform's output and the legacy binary's actual output on the generated signal, rather than assuming values. Once you've nailed down parameters that reproduce the legacy output essentially exactly, write `/home/user/py_feature_extractor.py` implementing that exact recurrence, formatting each output to exactly 6 decimal places with `f"{z:.6f}"`. Validate bit-for-bit equality against `legacy_features.txt` on your generated data before considering it done, since matching must hold for arbitrary valid float sequences, not just the one you fit on.
