Start with the audio transcription since everything downstream depends on it: check whether a CLI speech-to-text tool (`whisper`, `whisper-cli`, etc.) is already installed, and if so run it against `/app/auth_message.wav` to get a transcript; if no tool is available, check the file for an embedded text/subtitle stream via `ffprobe -show_streams`/`ffmpeg -i` before falling back to installing a small speech-to-text tool. The spoken sentence ends with a 6-digit number — that number is the passcode you'll need for `X-Voice-Auth`; extract it programmatically from the transcript text (look for a trailing run of exactly 6 digits) rather than assuming a value. Build `/home/user/deploy_gateway.go` with `net/http`: on `POST /deploy`, compare the `X-Voice-Auth` header against the transcribed passcode string exactly, return 401 on mismatch/missing, and on match `exec.Command("bash", "/home/user/rollout.sh").Run()` then respond 200. For `/home/user/rollout.sh`: read `/app/servers.txt`, use `grep -vE '^\s*(#|$)'` (or equivalent awk/sed) to drop comments and blank lines, append `DEPLOYED TO: <ip>` per remaining line to `/home/user/deploy_log.txt`, and if `/app/servers.txt` is missing, write `ERROR: servers.txt missing` to the log and `exit 1`. `chmod +x` the script, `go build`/`go run` the server in the background bound to `127.0.0.1:8080`, and test the full flow with `curl` before finishing.
