Work through the failures in order and verify each with a real build/run, not inspection alone. (1) Linker error: run `make` and read the "undefined reference" symbols — they're math functions (pow, etc.), so the Makefile's link rule is missing `-lm`; add it to the link command (order matters — library flags typically go after the object files). (2) Memory bug: read `parser.c`'s token-extraction routine carefully; if it `malloc`s a buffer sized exactly to the token length and then calls a C-string function (like `atof`/`strtod`) on it, the buffer has no room for a NUL terminator — fix by allocating one extra byte and explicitly setting the last byte to `\0` before conversion. Confirm with a tool like `valgrind` or by testing an input where the bug would produce garbage. (3) Build the JSON fixture exactly matching the three specified expression/expected pairs. (4) Fill in `test_runner.py`'s missing piece: use `subprocess.run` to invoke the compiled binary with the expression as an argv, capture stdout, parse it as float, and compare against expected within the stated tolerance; write pass/fail counts to the report path. (5) Finally tar the two named files with paths stripped (`tar -czf ... -C <dir> file1 file2`) so they sit at the archive root.