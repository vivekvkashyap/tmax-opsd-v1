Work through the four requirements in order, verifying each independently. (1) Buffer overflow: in `parse_cookie()`, `sscanf(cookie_ptr, "%s", temp_buffer)` into a fixed 33-byte buffer with no width limit is the classic unbounded-copy flaw (CWE-120/CWE-119/CWE-787 family) — fix it by bounding the scan/copy to the buffer size (e.g. `sscanf(cookie_ptr, "%32s", temp_buffer)` or use `strncpy`/`snprintf` with explicit length) and always null-terminate. (2) Token policy: implement `validate_token` by reading the 16 raw bytes from `/home/user/auth_daemon/secret.key`, XORing each byte with `0x7F`, hex-encoding the result (lowercase, 32 chars), and comparing that string exactly against the provided `hex_token` — derive the expected token programmatically in your test script rather than hardcoding a value, since you can compute it directly from the key file at runtime. (3) Permissions: `chmod 400 /home/user/auth_daemon/secret.key` so only the owner can read it. (4) Build with `make` in that directory, launch the resulting `daemon` binary in the background bound to 127.0.0.1:9000, then write `/home/user/test_auth.sh` to compute the valid token, curl the three test cases (valid cookie, invalid cookie, oversized 200-char cookie to confirm no crash), and log results to `/home/user/audit_report.log` in the exact specified format.