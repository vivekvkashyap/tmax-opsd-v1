Start with static recon: `strings /app/beacon_gen | less` to spot library names, format strings, or hex-looking constants; if it's UPX-packed, try `upx -d` to unpack it first (or `file`/`readelf` to confirm), since packed code hides useful strings until decompressed. Diff a clean log against an evil log from the two provided corpora byte-for-byte to spot the extra field the beacon generator injects — this narrows down what to reverse-engineer in the binary. Use `objdump -d` (or `gdb` with breakpoints on library calls like an HMAC/hash function) to see which fields get concatenated and which crypto primitive is called, and look for a small hardcoded constant used as a key/seed — brute-forcing a 4-byte space is entirely feasible if you narrow down the algorithm first (confirm by recomputing the MAC in a scratch script and checking it matches values seen in the evil corpus). Once you know the exact construction (which fields, what separator, which HMAC variant, what key), write the Go classifier to recompute the same MAC from the JSON fields and compare it to any signature field present, exiting 1 only when it matches and 0 otherwise (missing/mismatched signature = clean). Validate against both corpora directories yourself before considering it done.