Structure `process.py` as clear sequential stages matching the spec, and get each one right before moving to the next. Normalization: lowercase the sensor name and replace spaces with underscores; for time, detect the type/format per record — if it's a string, parse as ISO8601 (e.g. `datetime.fromisoformat` after stripping a trailing 'Z' or using `dateutil`) and convert to epoch seconds; if it's an int, distinguish seconds vs milliseconds by magnitude (a millisecond timestamp is roughly 1000x larger than a plausible seconds timestamp, so a naive fixed threshold like "value > 10^12" or comparing against a reasonable epoch-seconds range works) and divide by 1000 when in milliseconds, always producing an integer epoch-seconds value. Deduplication: dedupe on the *normalized* (sensor, epoch_time) pair, keeping only the first occurrence in file order — a common mistake is deduping on raw pre-normalized fields, which misses duplicates that differ only in timestamp format. Bucketing: compute `bucket = (epoch_time // 3600) * 3600` per record, then group by (sensor, bucket) and average `value`. Load results into a SQLite `hourly_aggregates` table with the specified schema, then export to `/home/user/aggregated.csv` sorted by sensor_name then bucket_time, rounding avg_value to 2 decimals, with a header row.