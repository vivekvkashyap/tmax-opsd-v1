Build and unit-test the hash function in isolation before wiring the pipeline. Implement the polynomial rolling hash in C++ exactly as specified — accumulate `sum += (unsigned char)c * p_pow; p_pow = (p_pow * 31) % m` per character starting from p^0, taking mod 1000000009 at each step to avoid overflow (use a 64-bit integer type like `long long` for intermediate products since char*p^18-ish can exceed 32 bits) — then sanity-check it by hand on a short known string before trusting it on real data. For deduplication, group entries by computed hash (treat identical hash as identical source string per the spec) and keep only the lexicographically smallest key per group; read stdin fully first if you need multiple passes, or use a single-pass hash-map keyed by hash. For `pipeline.sh`: compile with `g++ -std=c++17`, ensure `/home/user/bin` exists first, use `jq -r 'to_entries[] | "\(.key)\t\(.value)"'` across all `.json` files in `/home/user/locales/` to produce the tab-separated stream, pipe through your compiled `dedup`, then `sort -t$'\t' -k1,1n` numerically by the hash column, and redirect to `deduped_report.tsv`. Make the script executable. For the cron line, use the standard 5-field syntax `0 2 * * * /home/user/pipeline.sh` written verbatim to `cron_schedule.txt`.
