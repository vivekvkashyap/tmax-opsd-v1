Work through the three numbered stages in order, checking intermediate results as you go. Load both CSVs with pandas, drop the `time`/`species` columns to get pure numeric matrices, and run `numpy.linalg.svd` on the mixture matrix M (with `full_matrices=False`) — take `s[:3]`, format each to 4 decimal places, and join with commas in descending order (SVD already returns singular values sorted descending, so no extra sorting needed) into `/home/user/svd_top3.txt`. For unmixing, solve `M ≈ C @ S` for C using `numpy.linalg.lstsq(S.T, M.T, rcond=None)` and transpose the result — make sure your species-column order in C matches A, B, C, matching the row order of the `species` column in pure_spectra.csv. For the kinetic fit, implement the closed-form A(t), B(t), C(t) equations exactly as given (watch the `k1==k2` singularity — guard against it, e.g. return a large penalty), define an SSE objective summing squared residuals over all three species and all time points using A0 = sum of extracted concentrations at t=0, and minimize with `scipy.optimize.minimize(objective, [0.1, 0.1], method='Nelder-Mead')`. Round k1/k2 to 4 decimals and dump as JSON with exact keys `k1`/`k2`. Write and run these as real Python scripts rather than computing by hand, since the numbers depend on the actual noisy data in the CSVs.