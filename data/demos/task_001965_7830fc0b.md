Write the C program as a single-pass streaming parser, not a naive line-by-line printf. Use `fgets`/a manual tokenizer to split each line on `" | "` into the 5 fields (avoid `strtok` on the raw pipe character alone since fields have surrounding spaces). Apply the three validation rules (level must be exactly INFO/WARN/ERROR, duration > 0, endpoint must start with `/api/`) before touching the aggregation state. For the hour bucket, just take the first 13 characters of the timestamp with `strncpy`/pointer slicing — don't try to parse it as a real date. Aggregate with a dynamic array or simple hash keyed by (hour_bucket, endpoint) string concatenation, accumulating count, sum, and max; compute avg as integer division (floor) only at the end, not incrementally. Before writing the CSV, sort your aggregated records with `qsort` using a comparator that orders first by hour_bucket then by endpoint (both plain strcmp). Write the exact header line first, then one row per record with no trailing blank line beyond what a normal loop produces. Compile with `gcc` and actually run the binary to produce `/home/user/summary.csv` — don't just leave source unbuilt.