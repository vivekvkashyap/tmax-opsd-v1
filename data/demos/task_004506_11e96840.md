Use `net/http/pprof` (already imported) with the server running: hit `curl http://localhost:8080/debug/pprof/heap` before and after generating load to confirm the leak, then find and remove the global slice append that retains every request's data forever (replace with per-request local state, or drop the tracking entirely) rather than just capping its size. Save a post-fix heap profile with `curl -o mem.prof http://localhost:8080/debug/pprof/heap`. For routing, forward the incoming request's raw query string unchanged to both backend URLs (don't re-encode or reorder params). For the schema diff: parse the legacy JSON into a generic map, recursively convert every key from snake_case to camelCase (split on `_`, capitalize each subsequent segment), re-marshal both JSON bodies with 2-space indentation, and compare them; when they differ, produce a proper unified diff (e.g. via a Go diff library or by shelling out to `diff -u` on temp files) and append only the diff text to `diff_results.log` — write nothing when they match exactly.
