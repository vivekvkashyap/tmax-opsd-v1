Separate orchestration from computation: `/home/user/run_pipeline.sh` should only create/activate the venv (`python3 -m venv /home/user/venv && source /home/user/venv/bin/activate`), `pip install numpy pandas`, and then invoke `python3 /home/user/predict.py`; keep it `chmod +x` and idempotent. In `predict.py`, load `dataset.csv` and `weights.json` first (outside any timer), then isolate the pure math — compute `scores = features_matrix.dot(weights) + bias` and `predictions = (scores > 0).astype(int)` — and time only that block with `time.perf_counter()` before/after, converting the delta to milliseconds; do not include DataFrame construction or CSV writing inside the timed region, since the check is specifically on inference-only latency. Write predictions with `id` and `prediction` as integers (cast explicitly, since numpy bool/float types serialize wrong otherwise) to `predictions.csv` with a header, and append the line `inference_time_ms:<value>` to `metrics.txt` using the exact key format. Run the pipeline script yourself end-to-end afterward and re-open both output files to confirm row counts (10001 lines) and formatting before considering it done.