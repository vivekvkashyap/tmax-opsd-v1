Work through this as independent, verifiable sub-fixes rather than one big edit. First, transcribe `/app/voicemail.wav` using an available STT tool (e.g. `whisper` if installed, or another local transcription utility) — do not guess the phrase; run the tool and read its output, then paste that exact recovered text into the `TRANSCRIPT_TEXT` variable in `/home/user/app/server.py`. Second, edit `~/.config/systemd/user/voicemail-api.service` to add `Environment=TZ=Europe/Zurich` and `Environment=LANG=fr_CH.UTF-8` (systemd unit files support multiple `Environment=` lines) under `[Service]`, then `systemctl --user daemon-reload && systemctl --user start voicemail-api` and check `systemctl --user status`/`journalctl --user -u voicemail-api` for further pathing errors before declaring it fixed. Third, once HTTP is confirmed listening on `127.0.0.1:8080`, open the SSH forward with `ssh -f -N -L 9090:localhost:8080 localhost` (or an equivalent local loopback tunnel) and verify with `curl -H "Authorization: Bearer voicemail-admin" http://localhost:9090/transcript` that the JSON response's `transcript`, `tz`, and `locale` fields all match what the service now reports. Confirm the gRPC port 50051 is also up before finishing.
