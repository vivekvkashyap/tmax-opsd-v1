Treat this as a chain of gated steps where each step's output determines the next. First, OCR `/app/spec.png` with `tesseract /app/spec.png -` (try `--psm` variants if text is garbled) to recover the polynomial coefficients and the minimum semantic-version requirement — do not guess these values, read them from the OCR output and sanity-check against typical formula formatting. Second, compare `/home/user/math_module/version.txt` against the recovered minimum using proper semver comparison (not naive string comparison, since e.g. "1.10" > "1.9"); apply `fast_math.patch` via `patch` or `git apply` only if the condition holds. Third, edit `compute_poly` in `mathops.c` to implement the exact recovered polynomial in `float` arithmetic. Fourth, fix the Makefile: compile with `-fPIC -shared` (and typically `-O2`) to produce `libmathops.so` correctly linked as a shared object — a plain `gcc file.c -o lib.so` without `-shared -fPIC` will misbehave or fail to load via ctypes. Finally in `ffi_test.py`, use `ctypes.CDLL`, build a `(ctypes.c_float * 100000)` array linearly spaced 0.0–10.0 inclusive (use `numpy.linspace` or manual step `10.0/99999`), call the C function with `ctypes.POINTER(c_float)` args and size as `int`, then write outputs one per line at 4 decimal places.