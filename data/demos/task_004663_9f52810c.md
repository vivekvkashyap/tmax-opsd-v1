Build a zero-dependency `http.server` (or `socketserver`) app binding `127.0.0.1:9090` with a custom request handler that parses the path/query string itself (`urllib.parse.urlparse`/`parse_qs`) rather than any framework. For `/analyze?frame=<N>` (1-indexed), shell out to ffmpeg with `subprocess.run([...], stdout=subprocess.PIPE)` using `-vf "select=eq(n\,{N-1})" -vframes 1 -pix_fmt gray -f image2pipe -vcodec rawvideo -` (or PNG piped and decoded) to get raw grayscale bytes for frame N — remember ffmpeg's `select` filter is 0-indexed while the endpoint is 1-indexed, so convert carefully. Compute `crc32 = zlib.crc32(raw_bytes)` and `luma_sum = sum(raw_bytes)` (or `sum(bytearray(raw_bytes))`), and respond with `Content-Type: application/json` and the exact key set `{"frame": N, "crc32": ..., "luma_sum": ...}`. For `/benchmark`, time a `time.perf_counter()` (or similar) wrapped loop that reruns the same extraction+CRC32+luma_sum computation for frames 1-5 sequentially (don't skip the ffmpeg calls to fake speed), and report `time_ms` as milliseconds elapsed with `frames_processed: 5`. Start the server as a background process/thread before finishing, and manually verify your raw-frame byte count matches width*height (e.g. 320x240=76800) so your checksum lines up with an independent ffmpeg run.