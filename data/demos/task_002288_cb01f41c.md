Break this into three independent stages and verify each before moving on. (1) Transcription: run the audio through a transcription tool (e.g. `whisper` or another ASR available in the environment) against `/app/audit_intercept.wav`, then parse the resulting text for repeated "Transaction from account X to account Y with amount Z" patterns using a regex, writing one CSV row per match to `/home/user/transactions.csv` — inspect the raw transcript manually first since ASR output can have inconsistent punctuation/casing. (2) Graph engine in C: build an adjacency list from the CSV, implement cycle detection via DFS with recursion-stack coloring (white/gray/black) to flag every node that lies on a back-edge cycle, compute out-degree centrality by counting outgoing edges per node, and implement BFS for unweighted shortest path (fewest hops) between two nodes. (3) HTTP server: implement a minimal HTTP/1.1 server in C (raw sockets or a lightweight library) bound to 127.0.0.1:8080, parse the query string for `/api/deadlocks`, `/api/centrality?limit=N`, and `/api/path?src=X&dst=Y`, emit JSON with the exact sort orders specified (deadlocks ascending; centrality by out-degree desc then id asc), and log every request line to `/home/user/server.log`. Keep the server running (background it) so the verifier's curl calls succeed.
