Install a C JSON library (`sudo apt-get install -y libjson-c-dev`) so you can parse `papers.json` reliably rather than hand-rolling a fragile parser; use a standard CSV read (line-split on comma, skip header) for `authors.csv`. Build the co-authorship graph as an adjacency structure keyed by author name: for each paper_id, collect the list of authors who wrote it (from the CSV), then for every unique pair of authors sharing that paper_id, add an edge between them in both directions if not already present (use a set/dedup mechanism, e.g. a hash set of "authorA|authorB" strings, so co-authoring multiple papers together doesn't inflate degree). Degree centrality is the count of distinct co-authors, not the count of shared papers. After computing degrees for every author, sort by degree descending, breaking ties alphabetically by author name (strcmp) — implement this as a proper multi-key sort, not just a single-key sort. Write only the top 3 lines to `/home/user/results/top_authors.txt` in exactly the `AuthorName:Degree` format, creating the `/home/user/results` directory first if needed. Compile with `gcc analyze_graph.c -o analyze_graph -ljson-c` and run it to actually generate the file before finishing.