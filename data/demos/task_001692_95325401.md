This task has three largely independent parts — tackle them in order and verify each before moving on. (1) Fix `/app/staging/nginx/nginx.conf` by inspecting its current broken state and adding a `location /api/ { proxy_pass http://127.0.0.1:8081; ... }` block listening on 8080; start Redis with its given config, start the backend binary, then confirm end-to-end with `curl http://127.0.0.1:8080/api/health` before moving on — don't assume the proxy is correct just because you edited it. (2) For the PIN, read the first request in `backdoor_requests.log` to get the exact HTTP method, URI, and the target MD5 hash, then brute-force all 4-digit PINs (0000-9999) computing `md5(pin + method + uri)` for each and comparing to the captured hash — write only the matching 4-digit PIN to `/home/user/cracked_pin.txt`. (3) In the Rust detector, check each payload file against SQLi/XSS substring patterns AND the backdoor signature (parse the first line for method+URI, extract the `X-Backdoor-Auth` header value, recompute the same MD5 using your cracked PIN, and compare) — print exactly one `[CLEAN]`/`[MALICIOUS]` line per file. Validate against the sample corpus in `/app/evidence/samples/` and ensure `cargo build --release` succeeds before finishing.
