Do not call the slow binary inside your MCMC inner loop — first characterize it, then replace it with a fast closed-form surrogate. Probe `/app/signal_model <x> <alpha> <beta>` across a grid of x/alpha/beta (including edge cases like x=0, varying alpha and beta independently while holding the other fixed) and plot/inspect the outputs to recognize the functional shape (a smooth curve that rises then decays as x grows suggests something like x times a decaying exponential — verify your hypothesized closed form reproduces the binary's outputs across your probe grid before trusting it). Once you have a fast C++ evaluator for that function, implement Metropolis-Hastings from scratch: propose `(alpha', beta')` via a small Gaussian random walk from the current state, compute the Gaussian-likelihood-based acceptance ratio against `/app/experimental_data.csv`, accept/reject, and run enough iterations with burn-in discarded to get a stable posterior. Average the retained samples for `posterior_means.txt` (`alpha_mean,beta_mean`). For the bootstrap CI, resample residuals (with replacement) many times, recompute MSE each time, and take the 2.5/97.5 percentiles for `bootstrap_ci.txt`. Finally plot the data scatter plus your fitted curve to `fit_plot.png`.