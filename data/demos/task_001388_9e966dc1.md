OCR the image first with `tesseract /app/plot_config.png -` to recover the scaling method, target dimension count, and output path — read it carefully since your Go program's parameters come directly from this text, not from guessing. Implement the scaling exactly as named (e.g. divide each column by the max absolute value in that column, computed from the data itself, not a hardcoded constant) before doing PCA. For PCA in Go with gonum, match the scaling step's convention precisely (some scalers also mean-center the data, others don't — verify which by checking what the named method conventionally means), build the covariance/Gram matrix, use `gonum.org/v1/gonum/mat`'s `EigenSym` (or SVD) to get eigenvectors/eigenvalues, sort components by descending eigenvalue, and project the scaled data onto the top-N eigenvectors. Since eigenvector sign is arbitrary, don't worry about matching sign to any reference — just be internally consistent. Write output with `%f` formatting, comma-separated, no header, and diff a few rows by hand against manual PCA math on a small subset to sanity-check before trusting the full run.
