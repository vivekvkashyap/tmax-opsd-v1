Work Phase 1 and Phase 2 in parallel tracks since they're independent. For the Redis password, run `git -C /home/user/investigation/broker-repo log -p` (or `git log --all -p`) and scan recent commits for one that removes hardcoded credentials — the deleted line in that diff is the password you need; propagate it into both `redis.conf` (as `requirepass`) and `broker-repo/config.json`. Then read `start_cluster.sh` and the broker's routing code to find why POSTs to `/process` aren't reaching the C worker — check the configured worker port/host in `config.json` against what the worker actually binds to, and fix any mismatch, then test with a manual `curl -X POST http://localhost:8080/process` once all three services are up. For Phase 2, since you have an intact but stripped oracle at `/app/oracle_worker`, feed it the same JSON stdin (lat/lon/alt tuples) as your compiled `worker.c` and diff outputs on several inputs to confirm they currently disagree; the described bug is single- vs double-precision arithmetic in a geodetic/Haversine-style distance calculation, so grep `worker.c` for `float` type declarations and single-precision math functions (`sinf`, `cosf`, `sqrtf`, `atan2f`) used in the distance/angle computation and convert them to `double`/`sin`/`cos`/`sqrt`/`atan2`, including any `f`-suffixed float literals. Recompile to `/home/user/investigation/worker`, re-diff outputs against the oracle across many random inputs until bit-exact, then leave the full cluster running.
