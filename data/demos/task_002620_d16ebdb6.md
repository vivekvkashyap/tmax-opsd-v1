Break this into independently testable pieces. First, transcribe `/app/directive.wav` with a speech-to-text tool (e.g. `whisper` or another available ASR utility) rather than guessing the phrase, and write the recovered code exactly as transcribed (normalize case/whitespace per the spec) to `node_code.txt`. Next, for the C filter, parse the WAV header manually per the RIFF spec: read the 4-byte "RIFF" tag, the declared chunk size (bytes 4-7, little-endian uint32), the "WAVE" tag, locate the "fmt " subchunk to check `AudioFormat == 1`, and compare `declared_size + 8` against the actual file size via `stat`/`fseek+ftell` — the naive mistake is trusting the file's internal length field without cross-checking it against the real file size, which is exactly what the evil corpus exploits (e.g. `0xFFFFFFFF` sizes). Compile and run the filter against every file in both `/app/corpora/clean/` and `/app/corpora/evil/`, iterating until you get 100% correct exit codes before moving on. For the pipeline script and cron job, keep them simple and idempotent (move-then-log), and for Nginx, write a minimal config with explicit non-root `listen 8080`, custom `pid`, `error_log`/`access_log` under `/home/user/nginx_logs/`, and `client_body_temp_path` etc. under `/home/user/nginx_temp/` so it starts without needing root.