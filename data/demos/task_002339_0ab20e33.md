Build the pipeline in clear stages and test each independently before wiring them together. First write the parity check as an isolated Bash function: sum ASCII codes of the filename characters with a loop (`for ((i=0;i<${#name};i++))` using `printf '%d' "'${name:$i:1}"`), get file size with `stat -c%s` or `wc -c`, then compute `P=$(( (size ^ ascii_sum) % 251 ))` using Bash's native `^` XOR operator — verify this matches `jq` parsed `expected_parity` per file from the manifest. Run this over all four artifacts and confirm your valid/invalid classification against the manifest before touching Nginx. Start the Python backend (`python3 -m http.server 9000 --directory ...`) in the background first. Then have your script emit `location /download/<file> { proxy_pass http://127.0.0.1:9000/<file>; }` for valid files and `location /download/<file> { return 403; }` for corrupted ones, appended inside the provided non-root Nginx skeleton (reusing the given pid/temp-path directives so `nginx -c` works without root). Test with `nginx -t -c` before starting, then curl each `/download/<file>` to confirm 200 vs 403 matches your parity computation.