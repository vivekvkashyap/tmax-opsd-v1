Do not feed lines straight into a strict JSON parser — invalid `\u` escapes (fewer than 4 hex digits) will make `jq`/`json.loads` throw. Instead, extract field values with targeted regex/string scanning per line (e.g. match `"id"\s*:\s*"((?:[^"\\]|\\.)*)"` style patterns, or a tolerant hand-rolled scanner) that pulls the raw substring between the field's quotes without attempting to interpret escape sequences — this preserves broken sequences like `\u202` verbatim in the output rather than crashing or silently mangling them. Process the file line-by-line (e.g. Python `for line in f`, or `awk`/`perl` streaming) so it scales to large files. For each record, check whether the extracted `fr` value is missing (key absent) or an empty string, and in either case set it to `"[TODO] " + en`. Write output with a real TSV writer (or manual tab-joining) preserving field order `id, en, fr`, starting with the header line. Be careful that your regex captures the full string field even when it contains escaped quotes, and that you handle the "key absent entirely" case distinctly from "empty string" but treat both the same for imputation.