Build each piece independently and test it before wiring them together. For api.sh, use `nc -l` (or socat) in a loop reading raw bytes; implement the HTTP parse as a small state machine: read header lines until a blank line, grep/sed out `Content-Length:`, then read exactly that many body bytes, pipe the body to `/app/evaluator`, and hand-construct the response with a correct `Content-Length` header matching the evaluator's actual output byte count. For the audit daemon, the Makefile is broken because it's missing `-lpthread` on the link line (audit.c uses pthread_create) — add `-lpthread` (and `-pthread` if needed) to the link rule, then `make`, then run the binary in the background so it listens on 9002. After sending the HTTP response in api.sh, open a second raw TCP connection to 127.0.0.1:9002 (e.g. via `exec 3<>/dev/tcp/127.0.0.1/9002` or `nc`) and write the body payload. For nginx, point pid/access_log/error_log/client_body_temp_path all under /tmp so it runs without root, listen on 8080, and `proxy_pass` /api/v1/check to 127.0.0.1:9001. For reconciliation, `sort` + `sort -u`/`comm` to merge-dedupe server_a/server_b, then `diff -u` against baseline.log into diff.out. Launch nginx, audit, and api.sh all in the background before finishing.
