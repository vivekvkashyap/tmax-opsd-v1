Work the three deliverables in dependency order. First extract the C2 key: use `readelf -x .c2_key malware_agent` or `objcopy -O binary --only-section=.c2_key malware_agent key.bin` to pull the raw 16-byte section, then hex-dump it (e.g. `xxd -p key.bin`) to get the 32-char lowercase hex string — do this via ELF tooling rather than guessing offsets in the raw binary. Second, scaffold a Rust project (`cargo new decryptor`), write a `main.rs` that reads `exfiltrated.enc` as raw bytes, XORs each byte with `key[i % key.len()]` using the extracted 16-byte key, and writes the result to `exfiltrated.dec`; build and run with `cargo run` or `cargo build --release && ./target/release/decryptor`. Third, once decrypted, `grep` the plaintext for `-----BEGIN CERTIFICATE-----`...`-----END CERTIFICATE-----` to isolate the PEM block, save it to a temp file, and get its fingerprint with `openssl x509 -in cert.pem -noout -fingerprint -sha256` (parse out the value after `=`, keeping OpenSSL's uppercase colon-separated hex format). Separately `grep` the decrypted text for the line containing `UNION SELECT` and copy it verbatim. Assemble all three recovered values into the exact JSON keys requested in `/home/user/forensics_report.json`, e.g. with `jq -n` or Python's `json.dump`.
