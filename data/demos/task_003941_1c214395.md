Read the current `/home/user/scripts/monitor.py` first to see the three concrete bugs described, then fix each with absolute, cron-safe constructs rather than assumptions that rely on the interactive shell environment: call the diagnostic tool by its full path `/home/user/bin/netcheck` in `subprocess.run(...)` instead of relying on `PATH`; open the log file with the absolute path `/home/user/network_logs/status.log` (creating the directory first if needed) instead of a bare relative filename, so behavior doesn't depend on cron's default working directory; and generate the timestamp using `zoneinfo.ZoneInfo("America/New_York")` (`datetime.now(ZoneInfo("America/New_York"))`) rather than naive local/UTC time, since cron's execution environment may not have your interactive shell's timezone. Format the log line exactly as specified — `[YYYY-MM-DD HH:MM:SS] [America/New_York] STATUS: <netcheck output>` — matching field order and spacing precisely. After editing, run `python3 /home/user/scripts/monitor.py` manually once and inspect `/home/user/network_logs/status.log` to confirm the line matches the exact expected regex before finishing; test from a different working directory to prove the path fix actually works.