Prototype the SQL in the `sqlite3` CLI before writing any C, since recursive CTE logic is much faster to debug interactively. Build a `WITH RECURSIVE hierarchy(id, name, department_id, salary, level) AS (SELECT ... WHERE manager_id IS NULL UNION ALL SELECT ... JOIN hierarchy ON employees.manager_id = hierarchy.id)` anchored at the employee with `manager_id IS NULL`, incrementing `level` by 1 per recursive step, then join to `departments` for the name, and finish with `ORDER BY level ASC, salary DESC LIMIT 4 OFFSET 3`. Confirm the CLI output matches your own hand-traced expectation of the hierarchy before porting it into C. In the C program, use `sqlite3_open`, `sqlite3_prepare_v2` with the validated query, then `sqlite3_step` in a loop, pulling columns via `sqlite3_column_int`/`_text`/`_double`, and write each row with `fprintf(fp, "%d,%s,%s,%d,%.2f\n", ...)` — pay attention to salary formatting: use `%.2f` exactly, not a manual string build, to avoid rounding/precision mismatches. Compile with `gcc /home/user/query.c -lsqlite3 -o /home/user/query`, run it, and diff `/home/user/results.csv` against your CLI-derived expectation (no header row, comma-separated, exact column order) before finishing.