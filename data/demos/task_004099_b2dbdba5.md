Work through this in three independent phases. (1) For the JWT: `grep`/parse `auth.log` for the `guest_user` line, split the token on `.` and base64url-decode the middle segment to recover the original payload JSON — keep every field but change `role` to `admin`. Build the forged token yourself: header must literally be `{"alg":"none","typ":"JWT"}`, base64url-encode the header and your edited payload (no `=` padding, use `-`/`_` alphabet, e.g. Python's `base64.urlsafe_b64encode(...).rstrip(b'=')`), join with `.` and leave the signature segment empty, so the string ends in a trailing dot. (2) For the cert, use `openssl x509 -in server.crt -noout -enddate -ext subjectAltName` (or `-text`) to pull the real Not-After date and DNS SAN entries from the file itself, reformat the date to `YYYY-MM-DD`, and write both lines in the exact requested format. (3) For the .pyc, don't try to execute it — disassemble with `python3 -m dis` on the loaded code object (or use `uncompyle6`/`decompyle3`, or inspect `co_consts` of the marshalled code) to find the literal string assigned to `API_KEY`, then write only that exact string (no quotes/prefix) to the output file.