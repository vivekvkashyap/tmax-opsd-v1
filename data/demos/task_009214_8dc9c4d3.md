The Makefile's `gcc -o libsensor.so sensor_calc.c` is missing both the shared-library flags and the math-library link: fix it to `gcc -shared -fPIC -o libsensor.so sensor_calc.c -lm` (omitting `-lm` will fail to link `pow`, and omitting `-shared -fPIC` produces a non-loadable executable instead of a real `.so`). In your processing script, read `requests.jsonl` line by line preserving order, and validate each record with both conditions: `status == "active"` AND `value` is numeric and `> 0` (reject `value == 0`, negative values, and non-active status). For valid rows, load the compiled library via ctypes/FFI, set the function's argtype to `double` and restype to `int`, and call `compute_sensor_checksum(value)`. Write one JSON object per input line in the same order — `{"id":..,"checksum":..}` for valid, `{"id":..,"error":"invalid_request"}` for invalid — to `/home/user/sensor_sim/processed_outputs.jsonl`, computing the checksum via the actual compiled library rather than reimplementing the math in your script (to guarantee bit-identical integer truncation/modulo behavior).
