First recover the forbidden-word list by OCR: install `tesseract`, run it on `/app/pii_list.png`, and preserve the words in the exact line order the image shows them — write them to your own dictionary file (this is the file path your compiled binary will take as its argument) rather than hardcoding them into the Rust source, since the program must accept the dictionary path as a CLI arg. In Rust, read stdin line-by-line with a `BufReader` (do not read all of stdin into a `String` up front — the task explicitly requires streaming). For each line: URL-decode manually (handle `%XX` hex pairs and treat `+` as literal, per standard form-encoding, unless the spec implies otherwise — implement a small decode loop rather than pulling in a crate). Then, iterating the dictionary words in file order, repeatedly find and replace each occurrence of that word with `[REDACTED]` using case-sensitive substring search (`str::find`/manual scanning), counting replacements as you go — process words in dictionary order so overlapping/nested matches resolve deterministically. Maintain a small ring buffer or fixed-size array of the last 3 lines' replacement counts to compute the rolling sum cheaply. Print `<line>\t<rolling_sum>\n` per line, flushing incrementally. Build with `cargo build --release` (or `rustc`) to produce the binary at exactly `/home/user/cleaner`.
