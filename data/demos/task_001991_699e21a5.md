Build and verify each independent piece before wiring them together with git hooks. First scaffold the Rust TCP listener (`cargo new alert_service`) that accepts connections on 127.0.0.1:2525, reads the full UTF-8 payload per connection, and if it starts with `ALERT: `, appends it (trimmed of trailing newlines, then with exactly one `\n` added) to `/home/user/dashboard_alerts.log` — `cargo build --release`, launch the binary in the background (`nohup ... &`), and write its PID (`$!`) to `/home/user/alert_service.pid`. Test this listener manually with `nc` before touching git, so you know it logs correctly in isolation. Then `git init --bare /home/user/dashboards.git` and write `hooks/post-receive` as a bash script that reads `oldrev newrev refname` from stdin, runs `git diff-tree -r --name-only --no-commit-id $oldrev $newrev` (or against the empty tree hash for first push) to check for any path ending in `.json`, and if found pipes `ALERT: Dashboards updated` into `nc 127.0.0.1 2525` (e.g. `echo -n "ALERT: Dashboards updated" | nc 127.0.0.1 2525`); `chmod +x` the hook. Clone to `/home/user/dashboards_work`, add `network_dashboard.json`, commit, and push to trigger the hook — then confirm the log actually received the line before finishing.