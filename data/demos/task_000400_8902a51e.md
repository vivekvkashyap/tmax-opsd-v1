Treat `/app/query_planner` as an opaque black box — do not try to disassemble or predict its internal logic; just study its actual input/output behavior by piping a few sample queries into it and inspecting the JSON it prints. Write `/home/user/gatekeeper.sh` to: read the query file's contents, pipe them into `query_planner` via stdin, capture the JSON output, then use `jq` to recursively sum every `est_cost` field across the whole nested tree regardless of depth — a robust way is `jq '[.. | objects | .est_cost // empty] | add'`, which walks all nested objects rather than hardcoding a fixed tree depth (a common naive mistake, since the tree can nest arbitrarily). Compare the resulting total against 5000: exit 1 if strictly greater, exit 0 if less-or-equal. Make the script executable (`chmod +x`). Before finishing, test the script against a few of your own constructed queries to confirm both the cost-summation logic and the exit-code branching work, since the actual grading corpora are hidden and only exit codes are checked.
