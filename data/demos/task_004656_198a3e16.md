Start by reverse-engineering the schema with `.schema` in the `sqlite3` CLI (or `PRAGMA table_info`) before writing any query, since the tables and their relationships aren't documented — identify how `jobs` relates to `servers` (foreign key) and how `deps` encodes edges (which column is the dependent job vs. the prerequisite). For the top-2-per-server query, use a window function: `ROW_NUMBER() OVER (PARTITION BY hostname ORDER BY duration_sec DESC)` filtered to `status='SUCCESS'`, joined against `servers`, then filter to rank <= 2, and order the final output by hostname ascending then duration descending, writing plain CSV rows with no header. For the dependency trace, treat `deps` as a directed graph (job depends_on prerequisite) and do a recursive traversal from job 14 — either a SQL recursive CTE (`WITH RECURSIVE`) walking `depends_on` edges, or a Python/bash BFS/DFS over query results — collecting all ancestor job IDs (excluding 14 itself, per the required output), then dedupe and sort numerically ascending before writing the comma-separated line. Double check off-by-one issues: is job 14 itself included or excluded, and are self-loops possible.
