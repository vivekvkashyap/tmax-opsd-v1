Locate the target archive by permission bit, not by name: `find /home/user/repo/archives/ -perm -1000 -name '*.tar.gz'` finds the sticky-bit tarball; extract it and study `schema.conf` and the sample `.arf` files. Since your replacement must be bit-exact against a stripped legacy binary under heavy fuzzing (valid and malformed inputs alike, including exact error strings and exit codes), reverse-engineer methodically rather than guessing: use `xxd`/`hexdump -C` on the sample `.arf` files to find the magic bytes and header layout, `strings` on `/app/bin/legacy_parser` to recover any embedded literal error messages (e.g. "Bad Magic", "Zlib fail", "Truncated TLV") and format strings, and `ltrace`/`strace` while running it on samples to observe library calls like `inflate`/`zlib` usage, file reads, and the order of operations. Confirm your hypothesis about the header format (magic + size field), compression (likely zlib/deflate), and the TLV record structure (type/length/value) by writing a minimal C prototype and diffing its output against the legacy binary on the sample files before generalizing. Handle every error path explicitly (bad magic, decompression failure, truncated TLV) with the exact exit codes and messages you observed, since the fuzzer will hit these edge cases directly. Compile with `-lz` and iterate by comparing outputs on a growing set of your own crafted edge-case `.arf` files against the legacy binary.