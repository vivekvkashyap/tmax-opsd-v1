Handle each inconsistency as its own explicit normalization step before merging. First detect/handle encoding per file (try utf-8, utf-16, and iso-8859-1 — e.g. attempt reads and fall back, or use `chardet`/`file` to detect, then decode accordingly) so you don't corrupt non-ASCII text. For each file's distinct schema, map its specific column names (Date/Temp/Metadata, Timestamp/Temperature/Info, Time/Temp/Meta) onto the unified target columns explicitly per source rather than assuming one shared header. Extract `device_id` from the free-text metadata column with a regex that handles both observed formats (`device_id=X; ...` and `device_id: X | ...`) — don't assume a single delimiter style. Parse each file's specific date format (space-separated 24h, US 12h with AM/PM, and ISO8601-with-Z) using explicit `strptime` format strings per source, normalizing all to `YYYY-MM-DD HH:MM:SS` with no timezone suffix. For deduplication, create the SQLite table with a `UNIQUE(timestamp, device_id)` constraint (or composite primary key) and insert using `INSERT OR IGNORE` (or equivalent conflict handling) so reprocessing the same or overlapping data never produces duplicates and the script stays idempotent across repeated runs — verify this by running the script twice and confirming the row count doesn't change.