Treat the buggy draft as a spec of what NOT to do, not a starting point to patch blindly — the two named failure modes (OOB read on trailing/invalid `%` and incorrect `<script>` stripping with overlaps) both stem from careless index arithmetic, so rewrite the decode loop to explicitly bounds-check `i+2 < len` (and validate both hex digits) before consuming a `%XX` sequence, falling back to emitting the literal `%` unchanged otherwise. For `<script>` removal, loop `strstr`/manual search-and-remove until no more matches exist, correctly shifting the remaining buffer left each time (a single non-repeating pass will miss overlapping/adjacent constructs like `<<script>script>`), operating on the buffer after URL-decoding completes, not interleaved with it. Since exact behavior must match a hidden oracle, don't guess ambiguous cases (e.g., malformed `%` at EOF, uppercase vs lowercase hex, case sensitivity of `<script>`) — instead build your own small test harness comparing byte-for-byte output against a range of crafted inputs, and let observed disagreements drive the fix. Compile with `gcc -O2`, then write ci_test.sh with at least three hardcoded payloads (one `<script>`, one malformed `%`) piped into the fixed binary, comparing actual vs expected via string equality, exiting 1 on any mismatch, and `chmod +x` it.