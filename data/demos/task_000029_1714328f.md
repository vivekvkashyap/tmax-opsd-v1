Chain three independent pieces cleanly rather than mixing concerns. First, extract crashed service names with a targeted text filter matching the exact log format: `grep '\[ERROR\] Service .* crashed with code' service.log | sed -E 's/.*Service (\S+) crashed.*/\1/' > crashed.txt` (or equivalent awk), preserving log order. Second, write `auto_extract.exp` using `spawn`, expecting the literal prompt string `Enter password: `, then `send` the password followed by a newline and `interact`/`expect eof` so the child process's exit status and output propagate correctly — test this standalone against `extract_backup.sh` before wiring it into C++. Third, in `restore_manager.cpp`, read crashed.txt line by line, parse volumes.fstab by splitting on whitespace into exactly three fields per line, match service names between the two, then invoke `auto_extract.exp <backup_file> <active_volume_path>` via `system()` (build the command string safely, e.g. with quoting) and check its return code before appending the `RESTORED <service> TO <path>` log line — only log success if the extraction actually succeeded. Do not touch fstab or the backup script. Compile with g++ and run the binary to produce the actual recovery.log and volume outputs.
