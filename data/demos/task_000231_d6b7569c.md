First recover the magic header by actually transcribing /app/voicemail.wav (e.g. with `whisper` or another ASR tool available in the environment) rather than guessing — the header string is spoken aloud in the recording, so listen/transcribe carefully for an exact-case token. Then design the C parser as a strict line-by-line validator, not a substring search: read line 1 and require it to equal `HEADER <secret>` exactly; for each subsequent `FILE: ` line, check the path (a) does not start with `/` and (b) does not contain the literal substring `../` anywhere, since a naive check might only test the start of the path and miss traversal buried mid-path like `src/../../bin/bash`. Fail (exit 1) on the very first violation encountered rather than only checking the last FILE line — a manifest with multiple FILE blocks must be rejected if ANY block is bad. Compile with `gcc -o manifest_filter manifest_filter.c`, then self-test against both `/app/corpora/clean/` and `/app/corpora/evil/` before considering it done, since the hidden test set will follow the same structure.