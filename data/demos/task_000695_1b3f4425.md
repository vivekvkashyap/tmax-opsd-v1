Tackle this as two independent deliverables. For the service wiring, inspect `/app/start_services.sh` and the worker's source to see exactly which env var names it reads (`REDIS_HOST`, `REDIS_PORT`, `MESH_API_URL`) and what values/format it expects (host:port vs full URL) — then write `/home/user/services.env` exporting those variables so that sourcing it before running the start script lets the worker reach Redis and the Flask mesh API; verify success by checking `/home/user/worker.log` for "PIPELINE_SUCCESS" after running it end to end. For the sequence filter, write `/home/user/filter_sequences.py` to parse FASTA records (header lines start with `>`, sequence lines follow), and for each sequence search for the primer `GATCGGAAGAGCACACGTCTG` allowing up to 1 mismatch — implement this as a sliding window across the sequence comparing each window of the primer's length against the primer and counting Hamming-distance mismatches (accept if <=1), not just an exact substring search. Compute the fraction of sequences containing the primer (with tolerance) over total sequences; exit 1 if that fraction is >= 5%, else exit 0. Test against `/app/data/samples/` first, but tune correctness against the mismatch/threshold logic itself since hidden corpora will stress edge cases near the 5% boundary and near the 1-mismatch boundary.