Fix and verify each layer in isolation before wiring them together. In `processor.cpp`, the bug is a fixed-size stack buffer (`char temp[50]`) receiving an unbounded `strcpy` — replace it with a buffer sized to the max input (1000 chars) plus room for the "Processed: " prefix and null terminator, and use a bounded copy (`strncpy`/`snprintf`) so it can't overflow; test by calling it directly with a >500-char string before touching the server. In the Makefile, add a target that compiles the C++ file to a shared object with `g++ -shared -fPIC -o build/libprocessor.so cpp/processor.cpp`, and a target invoking `python3 -m grpc_tools.protoc` (or `protoc` with the grpc plugin) to generate `_pb2.py`/`_pb2_grpc.py` into `python/`, with `make all` depending on both. In `server.py`, load the .so via `ctypes.CDLL`, allocate a `ctypes.create_string_buffer(1024)` for output, and implement the gRPC service method. For rate limiting, keep an in-memory dict of `client_id -> list of recent request timestamps`; on each request, prune entries older than 1 second and reject with `context.abort(grpc.StatusCode.RESOURCE_EXHAUSTED, "Rate limit exceeded")` if the remaining count is already >= 3 — test the exact 3-allowed/4th-rejected boundary and that different client_ids don't interfere. Finally write `start.sh` running `make all`, launching the server backgrounded, and writing `$!` to `server.pid`.