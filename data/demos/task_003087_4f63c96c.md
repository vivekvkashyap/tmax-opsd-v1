Recover the target cluster ID by running OCR (e.g. `tesseract`) on `/app/cluster_target.png` — read the extracted text carefully rather than assuming a value. For the C++ sanitizer, use a real JSON parsing library (or a careful hand-rolled parser) rather than regex/string matching, since JSON validity, negative `page_rank` detection, and detecting any key inside `properties` starting with `$` all require actually parsing the structure — a naive substring check on `"$` would misfire on nested/escaped content. Read stdin line by line, parse each as JSON, apply the three "clean" rules, and write only clean lines verbatim (byte-identical) to stdout; malformed or violating lines are dropped silently with no error output. Test your sanitizer manually against every file in `/app/clean/` (expect exact passthrough) and every file in `/app/evil/` (expect empty output) before trusting it on the real export. Then pipe `/app/raw_graph_export.jsonl` through `/home/user/sanitizer`, filter records where `cluster_id` equals the OCR'd target ID, and write `node_id,page_rank,cluster_id` to the CSV sorted alphabetically by `node_id`.