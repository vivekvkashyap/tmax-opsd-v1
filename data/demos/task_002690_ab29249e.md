Build this as a before/after measurement, not just a "fix and hope" — the whole point is to prove the index helped. In Go, open the DB with `database/sql` + `mattn/go-sqlite3`, read `query.sql` as a string with `os.ReadFile`. Run `EXPLAIN QUERY PLAN <query>` first (prefix the exact query text), iterate the result rows, and write only the `detail` column values, one per line, to `plan_before.txt` — inspect these column names via `rows.Columns()` since EXPLAIN QUERY PLAN returns multiple columns (id, parent, notused, detail) and you must select the right one. Then execute `CREATE INDEX idx_backup_jobs_parent ON backup_jobs(parent_id);` (or similar) via `db.Exec`. Re-run the identical `EXPLAIN QUERY PLAN` call and write the new `detail` rows to `plan_after.txt` — confirm manually that it now says `SEARCH ... USING INDEX` instead of `SCAN` before moving on. Finally execute the real recursive query, scan `root_id`/`total_bytes` into structs, and marshal to `results.json` matching the exact key names given. Compute the expected chain totals yourself by tracing the `parent_id` links in the sample data to sanity-check your program's output before finishing.
