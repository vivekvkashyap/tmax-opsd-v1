Work bottom-up: compile the provided library first (`gcc -shared -fPIC -o /home/user/libmathapi.so math_api.c`) and confirm it builds cleanly before writing the harness. Write `test_harness.c` to read lines from stdin with a bounded buffer (256 chars), strip the trailing newline, call `evaluate_url` from the library, and print either `<url> = <val>` with `%.2f` formatting or `<url> = ERROR` on non-zero return — compile it linking against the library (`gcc test_harness.c -L/home/user -lmathapi -o /home/user/test_harness`) and set `LD_LIBRARY_PATH` when running so it can find the `.so` at runtime. For `generate_urls.sh`, loop `i` from 1 to 10, compute `Y = i*2`, and echo both `/api/add?a=$i&b=$Y` and `/api/add?a=$Y&b=$i` — don't hardcode the 20 lines, derive them from the loop so the logic is verifiably correct. Finally chain everything (`./generate_urls.sh | LD_LIBRARY_PATH=/home/user ./test_harness | sort > /home/user/sorted_results.log`), and note `sort` does plain lexicographic (not numeric) ordering, so don't try to hand-order the expected output — just trust the pipeline and confirm the harness's own math and formatting are correct by spot-checking a few lines manually.