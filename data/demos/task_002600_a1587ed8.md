In your Python script, walk the directory tree yourself rather than relying on naive `os.walk`/`glob` with `followlinks=True` alone, because that will loop forever on the symlink cycle. Maintain a `set()` of `os.path.realpath()` (or `(st_dev, st_ino)` pairs from `os.stat`) for every directory you enter; before recursing into a symlinked directory, resolve it and check/add it to the visited set, skipping it if already seen — this lets you still follow symlinks for files not yet visited while breaking the cycle. When reading each `.gcode` file, open it with `encoding='cp1252'` (Windows-1252) explicitly. For comment detection, apply `line.lstrip()` and check if the very first character of the stripped line is `;` — a line like `G21 ; comment` does NOT qualify since G is first; only take the substring after the semicolon and `.strip()` it. Sort all collected comments alphabetically, then for atomic writing: write to a `tempfile` in the same directory, flush/close it, and use `os.replace()` to atomically move it onto `/home/user/extracted_comments.txt`, encoding as UTF-8.
