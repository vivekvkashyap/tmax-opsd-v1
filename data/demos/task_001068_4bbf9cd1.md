This C program has three independent, textbook concurrency/memory bugs in the same worker function — fix each on its own merits rather than patching symptoms. (1) The race: `global_records_processed++` is not atomic across threads; protect it with a `pthread_mutex_t` (lock/unlock around the increment, or use a per-thread local counter and sum after `pthread_join`). (2) The hang: the inner pointer-walk loop that looks for a special character never advances the pointer on that branch, causing an infinite spin on tokens containing it — make sure every branch of that loop increments the pointer before continuing. (3) The overflow: `fscanf(f, "%s", buffer)` reads into a fixed-size stack buffer with no length limit, so an abnormally long token overruns it — bound the read (e.g. `%127s` matching the buffer size, or switch to a dynamically sized read). After fixing, recompile with `gcc -pthread telemetry_processor.c -o telemetry_processor`, run it against the data directory, and confirm the printed count is stable across repeated runs before saving it to final_count.txt.
