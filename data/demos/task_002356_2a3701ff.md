For Phase 1, validate each row strictly: attempt to cast `sensor_id` to int and `temperature`/`humidity` to float inside a try/except (or pandas `pd.to_numeric(errors='coerce')` + dropna), then range-check temperature in [-50,50] and humidity in [0,100] inclusive — drop the row if any check fails, and write only surviving rows with original headers. For Phase 2, group clean rows by `sensor_id`, and for each group apply the exact conjugate-Normal update formulas given in the task (compute `n`, `sum(x_i)`, then `1/sigma_post^2 = 1/25 + n/4` and `mu_post = sigma_post^2 * (20/25 + sum(x_i)/4)`) rather than approximating with sample mean/variance — this is a textbook Bayesian update, implement it literally. Flag a reading anomalous only if `abs(x_i - mu_post) > 3 * sqrt(sigma_post^2 + 4.0)`, using strict `>`. Write flagged rows (original columns) to `anomalies.csv`. For Phase 3, import and time the exact same per-sensor computation from Phase 2 (not a mocked stand-in) over 100 loop iterations using `time.perf_counter()`, convert to milliseconds, and round to 2 decimals. Chain all three scripts from `run_pipeline.sh` with `chmod +x` and sequential `python3` calls, checking each step's exit status.
