Treat this as three independent deliverables you can verify one at a time. For `evaluate.sh`: source `/home/user/artifact_info.env` so its variables (MAJOR, MINOR, PATCH, etc.) are in scope, then evaluate the single argument as a Bash arithmetic expression — `$(( expression ))` will substitute the sourced variable names automatically since bash arithmetic expands bare identifiers as variables, so you don't need to manually parse/replace tokens; just print the integer result. For `benchmark.sh`: read `/home/user/expressions.txt` line by line (use `while IFS= read -r expr`), call `evaluate.sh "$expr"` 100 times in a loop to stress it (discard that output), then call it once more and append `"$expr = $result"` to `/home/user/evaluation_results.log` — watch for trailing/blank lines in the expressions file that could produce an empty extra call. Make both scripts executable with `chmod +x`. For the `.proto` file, just write a standard proto3 service definition with the two message types described — no compilation needed, this is just documentation of the future interface. After writing all three files, actually run `benchmark.sh` so the log file gets generated, then spot check the log values look arithmetically consistent with the env values.
