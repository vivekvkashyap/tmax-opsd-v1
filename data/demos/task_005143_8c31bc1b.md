Fix `start_app.sh` by adding `cd /home/user/app` (or `cd "$(dirname "$0")"` if you want it robust, but the spec wants it anchored to `/home/user/app` specifically) as the first line before the `nohup python3 server.py ...` invocation, so the socket/log always land in that directory regardless of the caller's cwd — kill any already-running instance of `server.py` first (`pkill -f server.py`) since a stale process may be holding a stale socket path. In `nginx.conf`, replace the `proxy_pass` target in `location /` with `http://unix:/home/user/app/app.sock;` exactly, and add a second `location /admin { return 403; }` block. For `rotate_logs.py`, check `os.path.exists('/home/user/app/app.log')`, and if present `os.replace()` it to `app.log.archive` (overwrites atomically), then `open('/home/user/app/app.log', 'w').close()` to create a fresh empty file and `os.chmod(path, 0o644)` to set exact permissions. Bring everything up in the right order: start the Python app first (so the socket exists), then `nginx -c /home/user/nginx/nginx.conf`, then run the rotation script — and verify with `curl -i http://127.0.0.1:8080/` (expect 200, no 502) and `curl -i http://127.0.0.1:8080/admin` (expect 403) before considering it done.
