Set up a standard Rust binary with `cargo new`, add the needed crates (`csv`, `serde` with derive) to `Cargo.toml`, and implement the pipeline in clear stages: (1) parse both CSVs into `Vec<f64>`; (2) implement the two-sample KS statistic yourself rather than trusting a library blindly — sort both transformed and reference samples, merge/scan through all distinct values (or use searchsorted-equivalent logic) computing each sample's empirical CDF at every point in the combined sorted set, and take the max absolute difference; get the tie-breaking convention right (e.g. count points <= x using a consistent "side" convention, matching a standard two-sample KS implementation) since an inconsistent convention shifts the result. (3) Do an exhaustive grid search over alpha in {0.00,0.05,...,1.00} and beta in {0.00,0.10,...,2.00} — iterate using integer step counters (e.g. `for a in 0..=20 { let alpha = a as f64 * 0.05; ... }`) rather than accumulating floats by repeated addition, to avoid drift off the intended grid. Track the minimum KS with strict `<` comparison so the first (smallest alpha, then smallest beta) tie survivor is kept. Round outputs half-up to the specified decimal places when writing JSON. Wire it all together via `/home/user/run_pipeline.sh` (chmod +x) that does `cargo build --release` then runs the binary, producing `/home/user/results.json`.
