Do the token recovery first: `git -C /home/user/legacy_wrapper log --all -p` (or `git log -p -- wrapper.py`) to walk the full commit history of the file that now shows "REDACTED" — an earlier commit will contain the real token string in a diff even though the current working tree has it redacted; grep the log output for `TOKEN =`. For the crash-condition analysis, don't just black-box guess randomly — systematically diff the clean vs evil JSON payloads' `alpha`/`beta`/`gamma` fields to spot patterns, and confirm each hypothesis by running `/app/engine <TOKEN> <file.json>` yourself on both real corpora and a few synthetic edge cases you construct (e.g. values right at a suspected boundary) to see which ones crash vs succeed; also run `strings /app/engine` to look for revealing panic/error message text that hints at what's being checked (e.g. a name mentioning a specific field). Once you're confident in both numerical conditions, write `sanitizer.py` to load the JSON, evaluate each condition with the same floating-point semantics as the engine (mind edge cases like `gamma == 0.0` and near-zero differences using a tolerance consistent with what you observed), and print exactly `SAFE`/`UNSAFE` with the right exit code — then re-run it against both full corpora to confirm 100% accuracy before considering it done.