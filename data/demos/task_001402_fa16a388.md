The core fix is a reordering, not a new algorithm: currently the code computes global mean/std over all N points and only afterward decides per-point train/test membership via `rand() % 100 < 80`. To eliminate leakage, first do a single pass assigning each index to train or test (store a boolean array or two index lists) using the same `rand()` calls in the same order — this preserves the split logic and seed behavior exactly. Only then compute mean and std using solely the values flagged as train. Standardize every point (train and test) using that train-only mean/std, and finally accumulate/report train_sum/train_cnt and test_sum/test_cnt over the now-standardized data. Do not touch the RNG call order/count, since verification depends on splits matching the original bug-for-bug (only the statistics-computation scope must change). After patching, compile with `-lm`, then loop seeds 10 20 30 40 50 in `track.sh`, appending each run's stdout to the log with `>>`. Sanity-check: train mean should land essentially at 0.0000 for every seed once the fix is correct.