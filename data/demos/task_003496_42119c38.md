The core fix is algorithmic: replace the O(N·E) re-scan-per-user pattern with a single pass over network_events.jsonl that builds an adjacency structure once — e.g. an out-edges dict (source -> list of targets) and an in-degree counter (target -> count), both populated in one file read. Compute in-degree directly from the counter. For PageRank, initialize every distinct user seen in the data to rank 1.0, then run exactly 2 iterations where each node's rank is split evenly among its out-edges and accumulated into a fresh next-ranks dict (nodes with no outgoing edges simply don't distribute, i.e. their rank sinks and isn't redistributed) — do this in-memory using only the adjacency list built earlier, never re-reading the file. Be careful with the "top 5, ties broken alphabetically" requirement: sort by (-value, user_id) so ties resolve correctly, and make sure the set of users considered includes every user appearing as either source or target anywhere in the file, not just those with outgoing edges. Write the two top-5 lists to optimized_results.json in the exact required key names and structure, and sanity-check your script's runtime dropped dramatically versus the original before finishing.
