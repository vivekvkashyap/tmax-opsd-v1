Load `perf_logs.csv` with pandas, get the unique `Function` values, and use `multiprocessing.Pool` (or `Process`) to dispatch one worker per function so the per-function analysis runs in parallel — pass each worker its function's filtered sub-DataFrame (or the function name plus a re-read/re-filter inside the worker, since DataFrames aren't always cheaply shareable across processes) rather than doing all work in the main process. Inside each worker: run `scipy.stats.linregress(DataSize, ExecutionTime)` over all rows for that function and take `.slope`; separately filter to only `DataSize == 1000` rows and fit `scipy.stats.lognorm.fit(data, floc=0)`, which returns `(shape, loc, scale)` — take shape and scale, ignoring loc since it's fixed at 0. Round all three floats to 4 decimal places. Collect worker results back in the main process (e.g. via `Pool.map` return values or a `multiprocessing.Manager` dict) into `{function_name: {"slope":..., "lognorm_shape":..., "lognorm_scale":...}}` and write it as JSON to `/home/user/perf_summary.json`. Verify the JSON has exactly the four expected function-name keys.
