Structure this as three independent numerical-methods components in Go, each verifiable in isolation before wiring together. (1) Newton-Raphson on the 2D system: define residual functions F1(x,y)=x^2+y^2-10 and F2(x,y)=xy-3, compute the Jacobian analytically ([2x,2y],[y,x]), invert it (2x2 closed form) at each iterate, and update (x,y) -= J^-1 * F, starting from (4.0, 0.5) and stopping when the step norm < 1e-6 — verify by hand that your fixed point actually satisfies both original equations before trusting it. (2) RK4: implement the standard 4-stage update with the given dx/dt and dy/dt functions, dt=0.01, and loop for exactly the right number of steps to reach t=50.0 (5000 steps) — write f(x,y) and g(x,y) as separate functions and reuse them for all four k-stages, evaluating each stage at the correctly updated intermediate point. (3) Analytical steady state: set both derivatives to zero and solve algebraically by substitution (solve one equation for one variable, substitute into the other), taking the positive nontrivial branch. Use only Go's `math`, `encoding/json`, `os` packages, marshal all six floats into the specified JSON keys, and write a `run.sh`/`Makefile` that builds and executes the binary to produce `/home/user/simulation_results.json`.
