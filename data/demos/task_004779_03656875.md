Build this as a small three-stage pipeline and get each stage working independently before wiring them together. In `/home/user/analyze.py`: load the CSV with `pandas` or `csv`, compute cumulative concentration with `scipy.integrate.cumulative_trapezoid(rate, time, initial=0)` (the `initial=0` keeps array lengths aligned and enforces C(0)=0), then fit the first-order kinetic model `C(t) = A*(1-exp(-k*t))` using `scipy.optimize.curve_fit` with a reasonable initial guess (e.g. A≈max(C), k≈0.1–0.5) since a poor guess can make the fit diverge or converge to a bad local optimum — check the fit visually/numerically against the data before trusting it. Round A and k to 3 decimals when writing `fit_params.txt` in the exact `A=X.XXX, k=Y.YYY` format, and write `integrated_data.csv` with headers `Time,Cumulative_C,Fitted_C` computed from the fitted parameters over the same time array. For `/home/user/plot.gnu`, set `set datafile separator ","`, `set terminal png`, `set output '/home/user/result_plot.png'`, and plot both columns against Time using `using 1:2`/`using 1:3` with `skip 1` or `set key autotitle columnhead` to skip the header row. Wire both into `/home/user/run_workflow.sh` (`#!/bin/bash`, calling python then gnuplot), `chmod +x` it, then run it end-to-end and inspect all four output files.
