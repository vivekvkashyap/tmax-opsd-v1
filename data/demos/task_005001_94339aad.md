Start by extracting the motor's true oscillation frequency from the video rather than guessing: use `ffmpeg` to dump frames at a fixed high sample rate (e.g. `-vf fps=N`), compute a per-frame brightness/intensity scalar (mean pixel value) with a small Python/PIL script, then run an FFT (numpy) over that intensity time series and pick the dominant peak frequency — that is your base motor frequency, use it, don't hardcode a number from intuition. For the classifier: build a real Cargo project (`cargo new`), add `rustfft` and `csv` as dependencies, parse the CSV's `latency_us` column into a Vec<f64>, apply a window function before FFT to reduce spectral leakage, compute the magnitude spectrum bins (bin_freq = k * sample_rate / N), and look for significant energy at frequencies well above the detected base frequency (a secondary/high-frequency peak) versus just noise floor — use a relative-magnitude threshold, not an absolute one, since Monte Carlo noise varies. Exit 0 only if no such anomalous peak exists, exit 1 otherwise. Test against both provided corpora before assuming correctness, since the classifier must generalize to a hidden holdout set with the same generation parameters.
