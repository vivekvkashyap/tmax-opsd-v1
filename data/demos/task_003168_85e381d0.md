Split this into extraction and implementation, and validate the implementation against the spec text precisely since it will be fuzz-tested for bit-exact equivalence. For extraction, use ffmpeg to pull all frames from `/app/rotation_data.mp4` (e.g. `ffmpeg -i in.mp4 frame_%04d.png`), then with Pillow/numpy compute each frame's mean R/G/B channel values: classify a frame as bit `1` if mean-Red > 128 and mean-Green/Blue < 50, or bit `0` if mean-Green > 128 and mean-Red/Blue < 50. Concatenate the bits in frame order, group into bytes of 8, and decode each byte as an ASCII character to reconstruct the pipe-delimited string `CREDENTIAL|<token>|CSP|<csp_value>|BLOCK|<malicious_word>` — parse these three values out programmatically, don't hardcode them. Then write `/home/user/filter.cpp`: read all of stdin into a string, find the first `\n` to isolate line 1 and check it equals exactly `AUTH <token>` (else print `401\n` and exit 1); if there's no second `\n` immediately after (empty line), print `400\n` and exit 1 unless there was no newline at all (auth-only check case per spec); otherwise take everything after the blank line as the body, check for the malicious substring (403 if found), else print `200\nCSP: <csp_value>\n` followed by the body verbatim. Compile with g++ and test against varied inputs including malformed ones before trusting it.