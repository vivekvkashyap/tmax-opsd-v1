Work request-by-request rather than guessing which one is malicious. Load `/home/user/traffic_dump.json`, and for every request compute `md5(User-Agent + "SuperSecretSalt2024")` in hex and compare it to that request's `X-Admin-Token` header — the one entry whose token doesn't match this computed hash is the malicious request; don't just eyeball token strings, actually compute and compare programmatically since forged tokens can look superficially plausible. From that request's `body`, extract the substring strictly between the literal markers `ELF_START:` and `:ELF_END` (careful with the exact marker text, not just "ELF" anywhere), `bytes.fromhex()` it, and write the result to `/home/user/payload.bin`. Then recover the C2 IP from the binary — run `strings /home/user/payload.bin | grep C2_IP=` (or scan the bytes in Python) to find the `C2_IP=<ip>` marker rather than trying to disassemble the whole ELF. Finally write the malicious request's `X-Forwarded-For` value, its forged token, and the recovered C2 IP into `/home/user/investigation.txt` using the exact three-line label format given in the task (`Malicious Source IP:`, `Forged Token:`, `C2 Server:`), since the verifier does exact string grep matching.
