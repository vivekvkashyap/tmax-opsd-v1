Compare the actual C function signature (`int calculate_checksum(const char* input, int length, unsigned int* output);`) against what the buggy `ctypes.argtypes`/`restype` declare — look specifically for an output parameter being passed by value instead of by pointer, and mismatched integer signedness/width, both classic ctypes FFI bugs that cause corruption or segfaults. Fix it by declaring `argtypes = [ctypes.c_char_p, ctypes.c_int, ctypes.POINTER(ctypes.c_uint)]`, allocating `out = ctypes.c_uint(0)`, and calling the function with `ctypes.byref(out)` so the C code writes through a real pointer. For the unit test, import `get_checksum`, compute the expected value in Python with `zlib.crc32(b"test") & 0xFFFFFFFF` (mind that `zlib.crc32` can differ in sign convention from an unsigned C CRC32 depending on Python version — mask appropriately), assert equality, and let `unittest.main()` provide the exit code. Once the wrapper works, call `get_checksum(b"MOBILE_BUILD_PIPELINE_STABLE")` and write the resulting integer as plain base-10 text (no trailing formatting) to the result file.