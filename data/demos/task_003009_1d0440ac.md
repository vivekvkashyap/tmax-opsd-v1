Extract frames with `ffmpeg -i /app/drone_feed.mp4 -vf fps=1 frame_%03d.jpg`, then record each frame's byte size (via `stat -c%s`) indexed by second into a lookup file, e.g. `second,size`. Separately, impute `telemetry.csv`: with `awk`, read timestamp/speed pairs into an associative array, then loop timestamps 1..120 carrying forward the last-seen speed (defaulting to 0.0 before the first entry appears), writing a complete `timestamp,speed` table to disk — this is the core forward-fill technique. Build `/home/user/query_range.sh` to take `$1 start` `$2 end`, then use `awk` to sum/average the imputed speeds and find the max frame size strictly within `[start,end]` inclusive, printing `AverageSpeed: %.1f | MaxFrameSize: %d`. Since the script will be fuzzed hundreds of times, precompute the frame sizes and imputed speeds once into flat lookup files up front rather than recomputing (e.g. reprocessing ffmpeg) on every invocation, so each query is a fast awk pass over cached data. Make the script executable with `chmod +x`.