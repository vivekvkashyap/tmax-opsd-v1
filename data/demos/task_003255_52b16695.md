Structure this as: traverse, classify, link, then atomically persist — don't interleave the JSON write with the traversal. Recursively walk `/home/user/messy_project/` (e.g. Python's `os.walk`), and for each regular file check if it's ELF by reading its first 4 bytes for the `\x7fELF` magic before attempting to parse it, to cheaply skip non-ELF noise. To extract Machine and Type fields, either shell out to `readelf -h <file>` and parse the "Machine:" and "Type:" lines, or use `pyelftools` (`pip install --user pyelftools`) to read `e_machine`/`e_type` from the ELF header — either is fine as long as the human-readable strings match what your directory-naming logic expects. Clean each field for use as a directory name: replace spaces with underscores and strip parentheses characters entirely (not just around them). Create the target directory structure with `os.makedirs(..., exist_ok=True)` and link with `os.link(original, target)` (a real hard link, verifiable via matching inode via `os.stat().st_ino`) — never `shutil.copy` or `os.symlink`. Accumulate inventory entries in memory as you go, and only at the very end write them to a temp file in the same target directory (e.g. `tempfile.NamedTemporaryFile(dir=..., delete=False)`) and `os.replace()` it onto `inventory.json` for atomicity.