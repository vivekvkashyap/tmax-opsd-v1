Read `main.cpp` carefully to locate the exact source of the leak: a function that computes mean/stddev over the *entire* dataset before any fold split (likely called once at the top of `main`, e.g. `scale_data_globally`). Remove that global call and instead, inside the per-fold loop, compute mean/std using only the rows assigned to the training fold for that iteration, then apply that same training-derived mean/std to scale both the training rows and the validation rows before calling `train_ridge`/`predict` — this ensures no information from validation rows ever influences the scaling statistics. For benchmarking, wrap just the `predict()` call on the validation fold with `std::chrono::high_resolution_clock`, measure elapsed microseconds, and average across folds for each alpha. Update the code to write results (instead of `cout`) as CSV rows `alpha,cv_mse,avg_inference_time_us` to `/home/user/ml_pipeline/benchmark_results.csv`, rounding `cv_mse` to 3 decimal places (e.g. `std::round(x*1000)/1000` with fixed-precision output). Rebuild with `make` and run the resulting binary to regenerate the CSV, then re-check the file has a header plus exactly one row per alpha (0.1, 1.0, 10.0).
