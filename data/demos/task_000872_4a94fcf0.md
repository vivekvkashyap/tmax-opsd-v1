Work this as a two-stage SQL-to-text mapping problem. First, in SQLite write a query that computes, per node, the count of outgoing edges (LEFT JOIN nodes to edges ON source_id, GROUP BY node) and the average latency of those edges (COALESCE to 0.0 when no outgoing edges exist). Use a window function like `RANK() OVER (PARTITION BY region ORDER BY out_count DESC, id ASC)` (or ROW_NUMBER, since ties must break by lowest id, which naturally happens with a stable secondary ORDER BY) to pick the top 2 per region — do not just sort the whole table and slice, since that breaks per-region grouping. Round the average latency to 1 decimal only at final output time, not in intermediate steps, to avoid compounding rounding errors. Second, once you have the ordered list of core-server ids, filter the edges table to only rows where both source_id and target_id are in that id set, ordered by (source_id, target_id). Finally, string-format each row into the exact Cypher syntax requested (mind spacing, quoting of text fields, and the single blank line separating node and edge blocks) and write the whole thing to /home/user/restore.cypher.