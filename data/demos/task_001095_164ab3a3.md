Replace the broken cross-join with a proper recursive CTE rather than patching the existing query. Write SQL like: `WITH RECURSIVE descendants(id, name, role) AS (SELECT id, name, role FROM nodes WHERE parent_id = (SELECT id FROM nodes WHERE name = 'Alice') UNION ALL SELECT n.id, n.name, n.role FROM nodes n JOIN descendants d ON n.parent_id = d.id) SELECT id, name, role FROM descendants ORDER BY id ASC;` — the anchor selects Alice's direct children (excluding Alice herself), and the recursive branch joins children of already-found rows. Embed this exact query string in `/home/user/fetch_team.cpp` in place of the broken one, keeping the existing CSV-writing logic intact. Compile with the exact `g++ ... -lsqlite3` command given and run the binary to produce `/home/user/team_output.txt`, then diff its contents against what you'd expect from the org chart data to confirm correctness. Separately, run the same recursive query text through `sqlite3 /home/user/org_chart.db "EXPLAIN QUERY PLAN <query>"` from the CLI and redirect the verbatim output to `/home/user/query_plan.txt` — do this as a distinct step, not by parsing it out of the C++ binary's behavior.
