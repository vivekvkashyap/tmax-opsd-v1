Build the classifier around a small set of cheap, interpretable image statistics rather than a heavy CNN: for each frame compute mean pixel intensity, pixel variance (or std), and an edge-density measure (e.g. Laplacian/Sobel magnitude mean via OpenCV), then train something simple like logistic regression, SVM, or a shallow tree on `/app/corpus/clean/` vs `/app/corpus/evil/`, using k-fold cross-validation to report `cv_accuracy`. Since black/static frames are extreme outliers (near-zero mean+variance, or extremely high variance/noise with no edge structure), the classifier should separate the classes with very high accuracy — if it doesn't, your features are wrong, not the model. Make `filter_dataset.py` load the saved model (pickle/joblib) and copy only predicted-clean images, and be careful it handles both `.jpg` and `.png` and creates the output dir. For the video step, use `ffmpeg -i <video> -vf fps=1 -start_number 1 <output_dir>/frame_%03d.jpg` to get exactly one frame per second, then count files for `total_raw_frames` and `total_clean_frames` in the report. Test your pipeline end-to-end on held-out synthetic black/noise frames you create yourself before trusting it, since the real grader uses a separate hidden corpus with the same characteristics.