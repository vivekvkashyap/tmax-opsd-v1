Parse the log with a state machine keyed by PID rather than by IP: for each line, track per-PID whether an "Offering public key" was seen and whether "Accepted publickey" followed before "Connection closed"; a silent rejection is a PID that reached "Offering public key" and then hit "Connection closed" without an intervening "Accepted publickey" line. Use a regex to extract the IP from the "Connection from" line for that PID (or from the closed line) and collect unique offending IPs in a set to avoid duplicate blackhole entries. Wrap the file open in try/except FileNotFoundError (or a broad except around the open), print "Log not found", and `sys.exit(0)` before touching the shell script — do this check first so no partial /home/user/block_routes.sh gets created. When writing block_routes.sh, create it with a shebang, append one `ip route add blackhole <IP>` line per unique IP, then `os.chmod` it executable (0o755) rather than shelling out. For the cron entry, build the exact line `*/5 * * * * /usr/bin/python3 /home/user/analyze_auth.py`, write it via `crontab -l` piped through a temp file plus the new line into `crontab -`, avoiding duplicate entries if run twice. Finally invoke the script once manually to materialize block_routes.sh.