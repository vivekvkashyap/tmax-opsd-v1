Work in order: first fix the vendored package, then the data pipeline, then the server. (1) Open `/app/miniserve-0.1.0/miniserve/response.py`, spot the missing `import json` used by the JSON-serializing function, add it, then `pip install -e /app/miniserve-0.1.0/`. (2) Load both CSVs with pandas, do an inner join on `user_id`, drop rows with NaN `age` *before* computing the mean, then compute the mean of `amount` on the post-drop data and use it to fill remaining NaN `amount` values — order matters here, don't impute before dropping. Save the result and compute Pearson correlation (e.g. `df['age'].corr(df['amount'])`) on the final cleaned frame. Write `mean_amount` and `correlation_age_amount` as floats to the JSON log. (3) Build `serve_data.py` on the fixed miniserve framework: check the `Authorization: Bearer ds-secret` header on every route and return 401 if absent/wrong before doing any other work; implement `/api/stats` (echo the log file) and `/api/data` (first 10 rows of the cleaned CSV as JSON records). Start it bound to `0.0.0.0:8888` and background it so it stays alive after your turn ends.