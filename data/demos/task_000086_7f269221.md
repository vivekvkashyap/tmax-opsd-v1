Split this into two independent subtasks and do the video work first since the filter's termination-signal behavior depends on it. For the video: use `ffmpeg -i /app/k8s_dashboard.mp4 out_%03d.png` (or a high fps like `-vf fps=10`) to dump all frames, then visually inspect (or `tesseract` OCR) each frame to find the flashing 15-character uppercase string — don't assume it's readable from a single frame, check several around where text appears since it may only show for one frame. Once you have that string, hardcode it as the exact-match halt trigger in your script — never hand-wave or guess it. For the filter script: read stdin line-by-line with a `while IFS= read -r line` loop (this preserves whitespace, which a naive `read line` or `cat` pipeline would mangle) and use `[[ "$line" == *"imagePullPolicy: Always"* ]]` for the substring replace, and a regex like `[[ "$line" =~ ^([[:space:]]*)replicas:[[:space:]]*([0-9]+)$ ]]` capturing leading spaces and the number separately so you can reconstruct `"${BASH_REMATCH[1]}replicas: 5"` exactly. Check the halt-string match before the other transformations, print `--- SYSTEM HALTED ---`, then `exit 0` immediately without processing remaining stdin.