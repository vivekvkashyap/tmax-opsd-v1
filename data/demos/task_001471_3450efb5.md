Open `ode.go` and isolate the bug precisely: the loop multiplies `dt` by 1.5 on every iteration instead of holding it fixed, which is why the integration diverges for stiff decay rates. Remove that adaptation line entirely and instead initialize `dt := 0.001` once outside the loop (do not recompute it inside the loop at all), keeping the rest of the Euler step (`y = y - dt*k*y; t += dt`) unchanged. Run `go test ./...` (or `go test` in `/home/user/nanofit`) and confirm the regression test passes — if it still diverges or fails tolerance, double check you didn't leave any step-size mutation in the loop body. Then `go build -o nanofit .` in that directory, and run `./nanofit --input /home/user/nanofit/data/signals.csv --output /home/user/results.csv` to produce the final CSV — verify the output looks numerically sane (finite, monotonically decaying values) before considering the task done.
