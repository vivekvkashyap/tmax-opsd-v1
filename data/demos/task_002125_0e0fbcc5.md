Work the three failures in sequence. First, run `/home/user/perf_app/build.sh`, read the linker error carefully (likely a missing library flag like `-lm` for `sqrt`/math functions, or missing source file in the compile command), and fix the script so it compiles cleanly. Second, recover the removed API key from history with `git -C /home/user/perf_app log -p -- compute.c` (or `git log --all -p`) and look for the commit where the hardcoded key was deleted/refactored out — the diff shows the literal string that used to be compared against. Third, run the built binary with that key and it will segfault; use `gdb --args ./metrics_calc <key>` with `run` then `bt`/`print` at the crash to locate the faulting pointer — the bug is a buffer/array that's used before being allocated (e.g., an uninitialized or NULL pointer written into via array indexing). Fix only the allocation, not the math logic, recompile, and rerun so `/home/user/profile_results.txt` is generated. Diff or checksum the output against expectations to confirm correctness.
