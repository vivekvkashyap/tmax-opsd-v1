Implement the pipeline as four clearly separated C++ stages and validate each numerically before moving to the next, since small formula deviations compound. Parse the CSV manually (getline + stringstream split on comma), treating an empty `feature_x` field as missing; collect all present integer values, sort a copy, and compute the median — for an even count take `std::floor((a[n/2-1]+a[n/2])/2.0)` — then use that integer to fill every missing cell (don't accidentally drop rows with missing data). For the t-test, split `target_y` by `group`, compute each group's mean and sample variance, then apply the pooled two-sample equal-variance formula: pooled_var = ((n0-1)*var0 + (n1-1)*var1)/(n0+n1-2), t = (mean0-mean1)/sqrt(pooled_var*(1/n0+1/n1)) — get the sign convention right (group0 - group1) since the grader compares against a specific ordering. For OLS regression on the cleaned feature_x vs target_y, use the standard closed-form slope = cov(x,y)/var(x), intercept = mean(y) - slope*mean(x) (Armadillo can do this cleanly with its stats functions). Round every float to 4 decimals with `std::round(x*10000)/10000` and emit JSON in the exact key order/format specified via nlohmann::json or manual formatting, writing to `/home/user/results.json`.