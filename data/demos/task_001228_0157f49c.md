Start by recovering the hidden parameters, since everything downstream depends on them: run `tesseract /app/experiment_config.png stdout` (or similar OCR) to extract the printed `EXPERIMENT_SEED` (integer) and `TARGET_VARIANCE` (float) text — don't guess values, read what OCR actually returns and sanity-check it looks like a plausible int/float before using it. Then build the pipeline exactly as specified: generate data with `make_classification(..., random_state=<recovered seed>)`, fit `PCA` requesting a *variance ratio* rather than a fixed component count — pass the target variance as `n_components` directly to sklearn's `PCA` (it interprets a float `n_components` as "smallest number of components whose cumulative explained variance ratio exceeds this value"), then transform and compute `numpy.cov(X_pca, rowvar=False)`. For the API, wrap Flask/FastAPI with a manual header check on every route (`X-MLOps-Auth: track-me`, else 401/403) — a common mistake is checking the header only on one endpoint or comparing case-insensitively/loosely. Round floats to 4 decimals exactly as requested for `/covariance`. Start the server as a background process and only touch `/home/user/server_ready.txt` after the socket is actually accepting connections, not immediately after spawning the process.
