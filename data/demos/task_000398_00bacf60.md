Implement this as clean sequential passes over the 100x5 matrix in C, in the exact order specified since each step depends on the previous one's output. Parse the CSV with `fscanf`/`strtok`, treating any field equal to the literal string "NaN" as missing (use `strcmp` or check with `isnan(strtod(...))` after handling the NaN token specially, since `strtod("NaN",...)` itself can also return NaN — be deliberate about detection). Pass 1: for each column, sum non-missing values and count them to get the mean; fill NaN cells with that column mean. Pass 2: recompute using the now-imputed data to get the population standard deviation (divide by N, not N-1) per column, then clip every value in that column to [mean - 2*std, mean + 2*std] using the same mean from imputation. Pass 3: parse K from argv, split rows into K contiguous blocks of 100/K rows each. Compute the query vector as the elementwise mean of fold 0's rows. For each later fold, compute squared Euclidean distance from every row to the query and track the arg-min's absolute row index (fold_start + local_index). Print/write the resulting global indices as a comma-and-space-joined string to both stdout and result.txt, matching the exact separator format shown in the example.
