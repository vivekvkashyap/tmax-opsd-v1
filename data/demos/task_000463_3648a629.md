Write generator.go with a struct like `{N int; Fib uint64; Lpf uint64}` tagged for JSON with lowercase keys `n`,`fib`,`lpf`, and generate F(0)..F(39) iteratively (use uint64/big.Int-safe arithmetic since Fibonacci grows fast — F(39) still fits in uint64, so plain uint64 is fine). For largest prime factor, implement trial division: repeatedly divide out factor 2, then odd factors up to sqrt(remaining value), tracking the largest factor found; special-case fib==0 or fib==1 to set lpf=0 as instructed. Marshal the slice of structs with `encoding/json` and write to /home/user/data.json.

For the Makefile, keep each target minimal and correct: `build` runs `go build -o generator generator.go`, `run` executes `./generator` (or the absolute path) to produce data.json, `extract` uses `jq '.[] | select(.n==20) | .lpf' data.json > answer.txt` (or index-based `.[20].lpf` if you're confident array order matches n order) redirected so answer.txt contains only the bare integer with no quotes, and `all` chains `build run extract` in order with `.PHONY`. Run `make all` from /home/user and verify data.json's 20th-index entry and answer.txt by inspecting them yourself before finishing — don't hardcode the expected LPF value, let your program compute it.