Implement this as three separable phases and validate each with small print-outs before trusting the pipeline end to end. Phase 1: read the four binary files with `ifstream` + `read((char*)ptr, n*sizeof(float))` respecting the documented shapes (5x10 row-major encoder, 10x5 row-major decoder, 10-vector bias); for each of the 100,000 records do the encode (5x10 * 10x1 -> 5x1), decode (10x5 * 5x1 + bias -> 10x1), then sum squared differences into an errors[100000] array — get the matrix-multiply index order right (row*cols+col) since that's the easiest silent bug. Phase 2: seed `std::mt19937 gen(42)` exactly once outside the loop, and inside 1000 iterations draw 10,000 indices with `std::uniform_int_distribution<int>(0, 99999)` calling `dist(gen)` in a fixed, sequential order (draw order must match the spec precisely since determinism depends on it), sort each sample, take index 9500, and average all 1000 such values for T. Phase 3: filter records with error <= T, write the kept 10-float vectors to `clean_data.bin` in original order, and write T (4 decimal places) and the count as two lines in `summary.txt`. Compile with `g++ -O3 -std=c++17`.
