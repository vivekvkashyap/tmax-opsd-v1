Start by attempting `make` in `/app/edgetelem-1.2.0` to see the actual compiler error — it will point you to a missing standard header in `server.c` (functions like `atoi`/`getenv` failing implicit-declaration checks under strict flags); add the correct `#include` rather than disabling warnings. After it compiles, grep `server.c` for where `TELEM_PORT` is read and used to bind — look for an arithmetic operation applied to the parsed port value that inflates it (e.g. multiplying by a constant) and remove that so the literal env var value is used directly. Also locate how the output log path is configured (likely another env var, `TELEM_OUT`) and set it to `/home/user/data/telemetry.log`, creating the `data` dir first. Once `edgetelem` builds and binds correctly to 127.0.0.1:8080 (verify manually with `curl` on `/ping`), write `/home/user/edge_deploy.sh` as a `while true; do ... sleep 1; done` loop that: curls `/ping` and restarts `edgetelem` in the background (with the right env vars) if it fails; checks `telemetry.log` size with `stat`/`wc -c`, and once it exceeds 100 bytes, `mv`s it to `/home/user/backups/telemetry-$(date +%s).bak` (creating that dir) and restarts the service so a fresh log file is created. Launch the script with `bash edge_deploy.sh &` and confirm restart-after-kill behavior yourself with `killall edgetelem` before finishing.