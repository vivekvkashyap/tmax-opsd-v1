Work through this in dependency order and verify each stage before moving to the next. First, launch QEMU in the background exactly as specified (`-nographic`, `-drive file=/app/legacy_vm.qcow2,format=qcow2`, `-qmp unix:/tmp/legacy_qmp.sock,server,nowait &`) and confirm the socket file appears before testing the legacy binary; then test `/app/legacy_monitor` manually with `env TZ=America/New_York LC_ALL=C PATH=/usr/local/sbin:$PATH /app/legacy_monitor` to confirm it produces real output (not "ERR") — this validates your environment assumptions before you bake them into C code, since a naive attempt often forgets one of the three env vars or runs the binary before the QMP socket exists. In your C daemon, use `fork()`/`execve` (or `popen`-style pipe capture) with an explicit `envp` array containing exactly those variables, capturing stdout via a pipe. Implement two separate listening sockets (8040 raw TCP, 8080 minimal HTTP) — for HTTP, hand-craft a valid `HTTP/1.1 200 OK\r\nContent-Length: N\r\n\r\n<body>` response with correct Content-Length. Test each port with `nc`/`curl` locally before wiring up the Git deploy hook. For deployment, build the bare repo, write a `post-receive` hook using `git --work-tree=/home/user/deploy --git-dir=... checkout -f`, compile with `gcc`, kill any old daemon PID (track it in a pidfile), then relaunch — test the whole flow end-to-end with a real `git push` before finishing.