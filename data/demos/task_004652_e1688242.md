Work through the four sub-problems independently, verifying each before moving on. For the secret: use `git log --all --diff-filter=D --summary` (or `git log --all -- secret.h`) to find the commit that deleted `secret.h`, then `git show <commit>^:secret.h` to recover its contents from the parent revision, and extract the `AUTH_SECRET` string exactly (no quotes/newline unless the source has them) into `/home/user/api_secret.txt`. For the pcap: use `tshark`/`scapy`/`tcpdump` to filter TCP payloads containing `GET /heartbeat` (e.g. `tshark -r heartbeats.pcap -Y 'http.request.uri == "/heartbeat"'` or grep raw payload bytes) and count matches exactly, writing the integer to `/home/user/ping_count.txt`. For the C bug: locate the loop bound in `calculate_rolling_average` — an off-by-one like `i <= WINDOW_SIZE` reading one element past the array — and change it to strict `<`. For tracing: add an `fprintf` (opening the log in append or write mode once) inside the summation loop with the exact format `"Step %d sum: %.2f\n"` using the loop index and running sum at that point. Finally `gcc -o tracker tracker.c` and run `./tracker` to produce the trace log deterministically.
