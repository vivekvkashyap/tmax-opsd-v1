This is an empirical-complexity measurement task, so precision in timing and fitting matters more than the C code itself. Compile `tensor_ops.c` with `gcc -shared -fPIC -o libtensor.so tensor_ops.c` (add `-O0` or leave default optimization only if needed to avoid the compiler eliminating the loop — check the function still takes measurable time). In the Python profiler, load the library with `ctypes.CDLL`, and set `argtypes` correctly (`ctypes.POINTER(ctypes.c_double)`, `ctypes.c_int`) so NumPy arrays pass safely via `array.ctypes.data_as(...)`. For each N, allocate a fresh random N×N float64 array, call the C function 3 times and record `time.perf_counter()` deltas, keeping the minimum to reduce OS-noise. Fit `T(N) = a*N^b` with `scipy.optimize.curve_fit` using `curve_fit(lambda N,a,b: a*np.power(N,b), N_values, times, p0=[1e-9,3])` — a reasonable initial guess for `b` avoids convergence to a bogus exponent given only 5 data points. Round `b` to 2 decimals and write the JSON with exactly the specified keys/order of N values; sanity-check that `b` lands near 3 (since the loop is triple-nested) before finishing.