Work through the three sub-tasks in isolation and verify each before moving on. For the log filter, use a single `grep`/`awk`/`sed` pipeline (e.g. `grep '\[CRITICAL\]' file | sed 's/ \[CRITICAL\] / | /'`) and diff the output visually against the source lines to make sure only the tag itself is replaced, not surrounding text — then `chmod +x` and run it, don't just leave it unexecuted. For the backup script, build the archive filename from the current timestamp with `datetime.now().strftime('backup_%Y%m%d_%H%M%S.tar.gz')` and use Python's `tarfile` module (or subprocess `tar`) targeting `/home/user/app_data/`; for rotation, list all `*.tar.gz` files in the backup dir, sort by `os.path.getmtime`, and delete all but the 3 newest — do this AFTER creating the new backup so it participates in the count, and be careful the sort key is modification time, not filename string order. For the email step, first count lines in `critical_summary.txt` and only send if strictly greater than 5; use Python's `smtplib.SMTP('localhost', 8025)` with no auth/TLS, construct the message with `email.message.EmailMessage` (set From/To/Subject and body to the file's exact contents), and call `sendmail`/`send_message`. Run the full `migrator.py` at the end and confirm the resulting backup directory count and any captured email output make sense.