Debug each broken layer independently before wiring the whole pipeline together. (1) Linker error in /app/ext: run `make` and read the exact undefined-reference message — a missing math function symbol (sin/cos/pow/sqrt) usually means the Makefile's link line is missing `-lm`; add it after the object files (link order matters: `-lm` must come after the objects that use it). (2) Binary parser: run `/app/bin/telemetry_gen` and hexdump its raw output (`xxd` or `od -A x -t x1z`) to see the actual byte layout (4-byte magic, 2-byte length, payload) across several invocations, especially ones with larger payloads, to discover the endianness quirk — compare a length field for a payload under 255 bytes vs. over 255 bytes; if the parser assumes one endianness always, it will misread lengths past that threshold, so make parser.py branch on length or detect the anomaly and byte-swap accordingly. (3) Wire configuration: point both API and worker .env files at the same Redis host/port so jobs actually flow through the `math_jobs` list end to end. Start services via start_services.sh, confirm redis/api/worker are all up (check ports/processes), then run evaluate.py and inspect any parsing failures it reports to iterate on the parser until accuracy clears 0.99.
