Start by understanding the oracle's I/O contract empirically rather than assuming its format: run `/app/validator_oracle < /dev/null; echo $?` and feed it small known byte strings via stdin to observe how its exit code changes, since the task says it "accepts the raw section data via stdin" but doesn't specify encoding — use the provided `/home/user/sample_corpus/clean` and `/home/user/sample_corpus/evil` files to extract their `.note.artifact` sections and probe the oracle with each, comparing exit codes to the known clean/evil labels to reverse-engineer exactly what byte pattern flips its verdict. For ELF section extraction, decompress with Python's `gzip` module (or `zlib`), write to a temp file, then use `readelf`/`objdump -s -j .note.artifact` or the `pyelftools` library to pull the raw section bytes programmatically rather than shelling out to parse text output. Once your script reliably reproduces the oracle's clean/evil verdict on the sample corpus (100% match), wire it into `/home/user/sanitizer.py`: decompress, extract, pipe the section bytes to the oracle's stdin via `subprocess.run(..., input=section_bytes)`, and exit 0/1 based on the oracle's own exit code (don't reimplement its logic — just forward the input and mirror its exit status) so it generalizes to the hidden corpus.