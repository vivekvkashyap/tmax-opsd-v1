Start by extracting the hidden config from the image with `tesseract /app/system_config.png stdout` (try upscaling or thresholding the image first if OCR output is noisy), then parse out the `API_TOKEN=` and `MIN_TX_THRESHOLD=` values from the recognized text — do not hardcode guessed values, read exactly what OCR returns and use those strings/numbers in your service. For the data model: load `employees.csv` to build a manager→direct-reports adjacency map (CEO has empty `manager_id`), and load `transactions.csv` filtering to only rows where `amount > MIN_TX_THRESHOLD` (strictly greater, as specified). Compute each employee's "team volume" via a bottom-up recursive sum: an employee's own filtered transaction total plus the team volumes of all direct reports, recursively (memoize or compute via topological/post-order traversal since the hierarchy can be deep). For the `/dept-top-earners` endpoint, filter employees to the requested `dept_id`, sort by team_volume descending with `emp_id` ascending as a tiebreaker, and take the top 3. Require an exact `Authorization: Bearer <token>` header match (from OCR) and return 401 otherwise. Initialize the Rust project under `/home/user/app-service`, build with `cargo build`, and run the binary in the background bound to `127.0.0.1:8080`, then manually curl the endpoint with and without the header to confirm both success and 401 paths before finishing.
