Build the script around `sqlite3`'s CLI: create a temp db, use `.mode csv` + `.import waits.csv lock_waits --skip 1` (or equivalent) to load the data, then write a recursive CTE that walks chains of `waiting_trx -> blocking_trx` edges, tracking the path so far as a string/list, and stops extending a chain once it would revisit a node already in the path (use something like `instr(path, next_node) = 0` as the extension guard). A transaction is part of a deadlock cycle when the chain returns to its own starting node (`current_trx = start_trx`) — select distinct starting nodes satisfying that, order alphabetically. Watch two edge cases the naive query misses: self-loops (a transaction waiting on itself) should be excluded from the base case of the recursion, and only nodes that actually close a cycle (not just any waiter) belong in the output. Format the final result as a JSON array of strings via `jq -R -s 'split("\n")|map(select(length>0))'` piped from `sqlite3 -json`/plain output, or build the JSON directly with `jq -n --argjson ...`, and write it to `/home/user/deadlocks.json` with one quoted string per line as shown in the spec.