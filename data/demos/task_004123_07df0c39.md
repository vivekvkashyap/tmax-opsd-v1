Start services and inspect the live schemas before writing any Rust — run `/app/start_services.sh`, then connect to Postgres (`psql`) and Mongo (`mongosh`) to confirm the actual table/collection structure and sample rows match the description, since your queries must be correct against the real seeded data, not just the prose spec. Parse `/home/user/db_config.env` (source it or read key=value lines) to get PG_URL and MONGO_URL rather than hardcoding connection details. Design the graph traversal carefully: a user's reachable roles come from `users.role_id` plus the `parent_role_id` chain (walk upward, or build a role->ancestors closure), and each such role may have MongoDB `events` where `actor_role_id` matches, granting access to `accessed_resource_id`; model this as a graph (user -> roles via inheritance edges, roles -> resources via event edges) and run BFS from the user node to the target resource node to get the shortest path length and the intermediate `roles_involved`. Because output must exactly match a hidden oracle byte-for-byte across 10,000 fuzzed inputs, be strict about the JSON field names, ordering, and null-vs-absent semantics — test your own binary against several hand-picked user/resource pairs (including ones with no path) before trusting it, using `cargo build --release` and running the compiled binary directly.