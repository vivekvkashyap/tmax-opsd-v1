OCR the calibration note first — `tesseract /app/calibration_note.png stdout` — and read carefully for the labeled carrier/calibration frequency value (the image has other numbers too, like a mesh refinement parameter, so make sure you pick out the one explicitly tied to frequency/Hz, not a distractor value). Compute the high-frequency cutoff as 1.2x that value yourself; don't hardcode a guessed number. In `/home/user/filter.go`, for each CSV in the given directory, read the `t,v` columns (sample rate is fixed at 2000 Hz so you can derive N and bin frequencies from `k * fs / N` without needing the `t` column for spacing), compute a DFT (roll your own O(N^2) sum or use an FFT library like gonum for larger N — accuracy matters more than speed here), then sum `|X(k)|^2` over all bins for `E_total` and only bins whose frequency exceeds your computed cutoff for `E_high`. Remember real-valued signals produce a mirrored spectrum — decide consistently whether you include both positive and negative-frequency bins (or just 0..N/2) since it affects both sums proportionally but be consistent. Threshold the ratio at >0.15 for REJECT, else ACCEPT, and print exactly `ACCEPT filename.csv`/`REJECT filename.csv` per file. Validate against `/home/user/sample_data/` with `go run` before trusting your fc extraction and cutoff logic.