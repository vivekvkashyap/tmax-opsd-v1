Compile both backends first with `-fPIC -shared`, e.g. `gcc -fPIC -shared backend_c.c -o libbackend_c.so` and `g++ -fPIC -shared backend_cpp.cpp -o libbackend_cpp.so`, and confirm each `.so` loads via `ctypes.CDLL` and returns the expected string before writing the orchestration script — this isolates ctypes/ABI issues from test-runner logic. In `test_runner.py`, launch `python3 api.py` with `subprocess.Popen`, then poll (don't sleep-guess) the `/api/v1/data` endpoint with retries until it responds, to avoid a race where requests fire before Flask is bound to the port. Fetch both endpoints, parse JSON, sort each list by the `id` field (`sorted(data, key=lambda x: x['id'])`), and write both with `json.dump(..., indent=2)` — matching indentation between the two files is essential since you'll diff them afterward. Run `diff -u v1_sorted.json v2_sorted.json` via `subprocess.run` and redirect stdout into `diff_output.txt`; if the files aren't byte-identical after sorting, check for inconsistent trailing newlines or key ordering within each JSON object (use consistent dict construction so both go through the same serialization path). Finally, terminate the background server process explicitly (e.g. `proc.terminate()` + wait) so port 5000 is free when verification checks it.