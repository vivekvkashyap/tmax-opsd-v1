Split into two independent workstreams and validate each separately. Part 1: transcribe `/app/alert.wav` (`pip install openai-whisper` if needed, then `whisper /app/alert.wav --model base` or a quick Python `import whisper; whisper.load_model('base').transcribe(...)`) and read the transcript to find the exact directory path it specifies for `LOG_DIR` — don't assume a path, extract it from the audio. Create that directory, then edit `/home/user/netconfig.git/hooks/post-receive` to `export LOG_DIR=<path-from-audio>` before its existing payload runs, and `chmod +x` the hook. Part 2 is a byte-exact C++ reimplementation problem: since your binary will be fuzzed against a reference for exact stdout equivalence, focus on strict validation — read one line via `getline`, use a regex or manual parser requiring exactly `A.B.C.D STATUS` with no extra whitespace, each octet 0-255 with no leading zeros unless the octet is literally "0", and STATUS exactly `UP` or `DOWN`; anything not perfectly matching (extra spaces, out-of-range octets, leading zeros, malformed status) must print exactly `INVALID`. On success, zero-pad each octet to 3 digits (e.g. `%03d`) and print `[AAA.BBB.CCC.DDD] STATUS`. Test both edge cases (leading zeros, 256+, negative-looking strings, missing status) manually before considering it done, since the fuzzer will hit these directly.
