Before writing your own pipeline, diagnose the vendored library's bug precisely: open `/app/pygraph_etl-1.2.0/pygraph_etl/mapper.py` and locate `expand_has_many` — look for a loop that only processes a slice of the nested array (e.g. `nested_list[:1]`) or exits early via `break` instead of `continue` on an optional missing field; the fix is to iterate the full list and use `continue` for skippable items so no valid nested elements are silently dropped. Verify your patch by unit-testing `expand_has_many` directly on a small nested-array fixture with more than one item before trusting it in the full pipeline. Then write `/home/user/build_graph.py`: set `PYTHONPATH` (or install the package in editable mode) to import the patched module, stream-parse `entities.jsonl`, and configure `GraphProjector` with node extractors for Company/Employee/Project and edge extractors for `EMPLOYS` (Company->Employee) and `WORKS_ON` (Employee->Project, likely via the employee's nested project ID list — the exact field that triggers the bug). Call `materialize()` and write `source_id,target_id,edge_type` rows (with header) to `/home/user/projected_edges.csv`. Sanity-check edge counts look complete (not truncated to one edge per employee) before finishing.