First locate the embedded configuration string inside the binary rather than assuming its content — run `strings /home/user/suspicious.elf | grep C2_BEACON` (or grep the raw bytes directly) to find the literal marker `C2_BEACON: ` followed by a base64 blob, then decode that specific base64 payload with `base64 -d` to recover the regex pattern the malware actually uses — do not hardcode a guessed regex. Once you have the decoded pattern, parse `/home/user/access.log` (standard combined log format: fields include client IP and the quoted request line containing method, path+query, protocol) and extract just the request path/query portion for each line. Apply the recovered regex as an unanchored search against that path (unless the regex itself contains `^`/`$` anchors, in which case respect them) using a proper regex engine (Python's `re` or `grep -P`) rather than approximating with shell globs, since exact character-class and quantifier semantics matter. Collect the source IPs from matching lines only, deduplicate, sort numerically by IP octets (not lexicographically — use `sort -t. -k1,1n -k2,2n -k3,3n -k4,4n` or Python's ipaddress module), and write one IP per line to the output file.
