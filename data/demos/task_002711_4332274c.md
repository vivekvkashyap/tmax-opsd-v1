Load both CSVs and join on product_id first, then apply cleaning filters in a clear order: drop rows with missing/negative price (parse price as float and check <0 or unparsable) and drop rows with empty description — verify the row count after cleaning matches your manual read of the input files. Call the embedder binary once per product description up front (at startup, not per-request) via exec, capturing stdout and parsing the comma-separated floats into a fixed-length vector; cache these embeddings in memory keyed by product_id. For the /search handler, decode the JSON body, invoke the same embedder binary on the query text, compute cosine similarity (dot product over norms) between the query vector and every cached product vector, sort descending by score, and return the top k as JSON with the exact field names shown — ties or k larger than the dataset should be handled gracefully. Start the server in the background, curl /search manually to sanity-check the response shape before writing benchmark.sh, which should invoke a load-testing tool against the endpoint with 100 requests and redirect its output to the results file.
