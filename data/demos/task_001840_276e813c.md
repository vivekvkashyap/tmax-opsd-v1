Scaffold with `cargo new /home/user/etl_pipeline` and add `serde`, `serde_json`, `csv`, and a datetime crate (e.g. `chrono`) to Cargo.toml. Parse `logs.jsonl` line-by-line with `serde_json`, and `tx.csv` with the `csv` crate. For timestamp alignment, parse each ISO8601 string with chrono and truncate to the minute by zeroing seconds (and any sub-second component) before reformatting back to the `...:00Z` string form — build a join key of `(uid, truncated_minute)`. Aggregate transactions into a `HashMap<(String,String), f64>` summing `amount` per key first, then inner-join against the logs map on the same key, keeping only keys present in both. For masking: split the IP on `.` and replace the 4th octet with `0`; split the email on `@`, keep the first character of the local part plus `***`, then reattach `@domain` unchanged. Round the summed amount to 2 decimals before serializing. Write one JSON object per output line to `/home/user/output/merged.jsonl` with the exact key names specified. Build with `cargo build --release`, run it, and manually diff a couple of output lines against hand-computed expectations from the sample inputs before finishing.
