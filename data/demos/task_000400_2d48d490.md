Work through the four requirements in order since later steps depend on earlier ones. First, in `data_proxy.cpp`, find the `strcpy(buffer, data.c_str())` call into the fixed 64-byte buffer — this is the CWE-120 overflow; replace it with a bounded copy such as `strncpy(buffer, data.c_str(), sizeof(buffer) - 1)` followed by explicitly setting `buffer[sizeof(buffer)-1] = '\0'` so long inputs are truncated and null-terminated rather than overflowing (plain `strncpy` alone does not guarantee null termination if the source is longer than the buffer). Don't resize the buffer. Second, generate the TLS materials with `openssl req -x509 -newkey rsa:2048 -nodes -keyout server.key -out server.crt -days 365 -subj "/CN=localhost"` into `/home/user/certs/`. Third, write `rules.conf` with exactly the literal line `BLOCK=10.99.0.42` (the program parses this to decide which IP's traffic to drop). Finally compile with g++ and run against the given traffic log — confirm the blocked IP's line is absent from output.log and that the long payload line appears truncated to fit the 63 usable buffer bytes rather than crashing or overflowing.
