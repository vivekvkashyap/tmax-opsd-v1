Reverse-engineer the legacy binary's algorithm empirically before writing SQL: run `/app/legacy_flow_calc <node_id>` for a few small/known node IDs and cross-check against manual computation on the schema (nodes have `base_weight`, edges have `latency`) to confirm the metric definition — sum over all descendants D reachable from root R of `base_weight(D) / (shortest-path latency sum from R to D)`, with 0.0 if there are no descendants. In `/home/user/optimized_flow.py`, connect to `/home/user/topology.db` with `sqlite3` and implement this via a recursive CTE that computes shortest cumulative latency to each descendant (careful: if multiple paths exist to the same node, you need the minimum cumulative latency, which a naive recursive CTE that just unions all paths won't give you directly — you likely need to compute per-path distances and then take `MIN(distance)` per target node, or use a Dijkstra-style iterative widening query), then join to `nodes` for `base_weight` and sum `base_weight/latency_sum` grouped appropriately. Use a parameterized query (`?` placeholder) for the input node ID, round the final float to 4 decimal places, and print only that number. Validate output against the binary across several sample node IDs before considering it done, since it will be checked bit-exact.
