Build the script in clearly separable steps and make every mutation idempotent before you run it twice. For connectivity, open a short-lived socket connection (Python's `socket` module with `settimeout(1)`) to 127.0.0.1:8081 and 8082, treating a successful connect as "ready" and any exception/timeout as "offline" — since nothing actually listens on those ports in this environment, expect both to come back offline, and write that observed state (not an assumed one) into the JSON with the required keys. For the `.bashrc` line, read the file first (or use `grep -qxF`) and only append `export TZ=Asia/Tokyo` if it's not already present, so a second run doesn't duplicate it. For the crontab entry, read the current crontab with `crontab -l` (handling the case where none exists yet), check whether the exact `*/5 * * * * ...` line is already there, and only add it if missing, then write back the full merged list with `crontab -` — never overwrite unrelated existing entries. After writing the script, execute it once, then verify idempotency yourself by running it a second time and checking no duplicates appeared in `.bashrc` or crontab.