Split this into: (1) recover the two secrets from the image, (2) build the Go service correctly. Run `tesseract /app/legacy_route_manifest.png stdout` (try `--psm` variants if the raw output is noisy) and carefully extract the exact `AuthToken` and `Target` values from the key-value text — double check for OCR confusions (0/O, 1/l/I) since these strings feed directly into auth checks and response bodies that will be tested exactly. In your Go server, parse the `Authorization` header, strip the `Bearer ` prefix, and compare against the extracted token using exact string equality; return 401 for any mismatch/missing header before doing any other processing. For normalization, lowercase `service_name` then strip everything not matching `[a-z0-9]` (a regex like `regexp.MustCompile("[^a-z0-9]+")` applied after lowercasing, not before, since case-folding must happen first). Use `text/template` with a literal template string containing the three fixed lines, executing it into the response writer, and make sure the emitted YAML has no unintended blank lines or trailing template artifacts. Bind strictly to `127.0.0.1:9090`, compile with `go build`, launch the binary in the background (e.g. `nohup ./server &`), and curl it yourself to confirm 401 and 200 cases work before finishing.
