Port the nested-loop convolution directly into C (same accumulation order as the Python reference, using `double` or `float` accumulators to keep numerical parity), compile it with `gcc -O3 -shared -fPIC -o /home/user/libfilter.so filter.c` — the `-O3` flag matters, since an unoptimized build likely won't clear the 50x speedup bar. In `fast_filter.py`, load the library with `ctypes.CDLL`, explicitly set `.argtypes`/`.restype` on the exported function (pointers via `numpy.ctypeslib.ndpointer` or `POINTER(c_float)`, plus lengths as `c_int`), and make sure the array dtype passed in is `float32`/`contiguous` so ctypes doesn't silently misread memory — this is where naive FFI wrappers crash or return garbage. Match the slow version's exact interface: `fast_convolve(signal, kernel)` returning an array of length `n+m-1`. In benchmark.py, read the WAV with `scipy.io.wavfile`, normalize int16 samples to [-1,1], build the 500-tap averaging kernel, time both implementations with `time.time()`/`time.perf_counter()`, compute MSE, and write both numbers to benchmark_results.txt in the exact specified format.
