First recover the filter configuration from the image yourself: run `tesseract /app/config_spec.png stdout` to OCR the threshold value and the list of spam template strings — use exactly what the OCR/image gives you, don't invent template text. Then write `/home/user/spam_filter.py` to take `sys.argv[1]` as a file path, read its text, build a document list of `[input_text] + templates`, fit a `TfidfVectorizer(stop_words='english')` on that combined list, compute `cosine_similarity` between the input vector and each template vector, and take the max score. Compare that max against the OCR'd threshold using `>=` (strictly follow the comparison operator specified in the task) and print exactly `EVIL` or `CLEAN` with nothing else — avoid extra prints/debug output since the verifier checks stdout exactly. Sanity-test by running your script against a few of the spam templates verbatim (should print EVIL) and against unrelated genuine review text (should print CLEAN) before trusting it against the hidden adversarial corpus.