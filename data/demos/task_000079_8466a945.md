Read `/home/user/rotator.c` carefully and identify the struct layout: locate the fixed-size buffer field that is adjacent (in memory, before) to the security-critical field it's supposed to be isolated from, and note how input is decoded into that buffer without bounds checking against its declared size. Compile the original with `gcc` first to confirm it builds and runs, then craft an HTTP request in `/home/user/payload.http` with a `Cookie: session=<hex>` header where the hex-encoded value is long enough to overflow the vulnerable buffer and land your controlled bytes exactly on the adjacent flag field — compute the required byte-encoding (2 hex chars per raw byte) and mind endianness when writing the target value's bytes. Verify by running `./rotator < /home/user/payload.http` and confirming `/home/user/rotated_password.txt` gets created. Then write `/home/user/rotator_fixed.c`: fix the overflow (e.g. bound the decode loop to the destination buffer's actual size, or reject/truncate oversized cookies) while keeping normal, correctly-sized tokens working exactly as before. Recompile and re-test the same payload against the fixed binary to confirm it no longer triggers the exploit.