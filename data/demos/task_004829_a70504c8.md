Fix the build first, bottom-up: the Makefile compiles `libasmgen.so` with a plain `gcc -o` that produces a non-shared, non-PIC object — add `-shared -fPIC` (and typically `-o libasmgen.so`) so it's a real shared library, and make sure the `generator` target's `go build` can find both the header (`CFLAGS -I.` is already in main.go's cgo comment) and the lib at link time. Confirm compilation succeeds with `make clean && make` before writing anything else. For `/home/user/interpreter.sh`, implement a simple bash array or here-string-based stack: read the `.asm` file line by line, `case` on the first token (PUSH/ADD/SUB/MUL/PRINT), track the stack as a bash array pushing/popping from the end, and remember SUB order is `B - A` (second-from-top minus top) not the reverse — a naive implementation often gets subtraction backwards. For `/home/user/run_tests.sh`, run `make` to (re)build, then run `./generator` with `LD_LIBRARY_PATH=.` (or equivalent) so the dynamic linker can locate `libasmgen.so` at runtime — this is the step naive attempts miss, since a successful link at compile time doesn't guarantee runtime resolution. Then loop `output_1.asm` through `output_5.asm` in order through your interpreter, appending each printed result to `/home/user/test_results.log`. Make both scripts executable.
