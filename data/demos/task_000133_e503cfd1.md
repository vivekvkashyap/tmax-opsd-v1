First recover the target sequence from `/app/audio/target.wav`: since it's Morse code as audio tones (not speech), don't reach for a speech-to-text model — instead analyze the waveform directly (e.g. with Python + `scipy`/`wave`/`numpy`) to detect tone-on/tone-off segments, classify short vs long pulses as dot/dash, group into letters by inter-symbol gaps, and map the resulting Morse groups through a standard Morse-to-alphanumeric table to recover the ACGT string; sanity-check the result only contains A/C/G/T before hardcoding it into your CLI invocation. Then build `/home/user/classifier.go` as a CLI taking `<target_sequence> <input_dir> <output_dir>`: for each `.txt` file, launch a goroutine (bounded by a WaitGroup and/or worker pool, with a mutex or channel to serialize any shared state) that (1) checks `strings.Contains(target, primer)` for exact substring alignment, (2) computes GC fraction and requires 0.4–0.6 inclusive, (3) builds dinucleotide count maps for both primer and target, adds +1 Laplace smoothing to every possible 2-mer bucket before normalizing to probabilities, computes KL divergence `sum(p*log(p/q))`, and requires it under 0.5. Copy the file to the output dir only if all three checks pass.