Begin by inspecting the schema directly with `sqlite3 /home/user/backups.db ".schema"` and `SELECT * FROM <table> LIMIT 5;` on each table to discover the actual table/column names (don't assume names from the prompt) — you'll find a metadata table with backup id/type/size and a lineage table linking parent and child backup ids. In `planner.c`, use the SQLite3 C API (`sqlite3_open`, `sqlite3_prepare_v2`, `sqlite3_step`) to run a Recursive CTE: start from the lineage table anchored at the target backup id, recursively join upward via parent links to the root ('full') backup, collecting the full parent-to-target ancestry chain in order; then sum the `size_bytes` for every backup id in that chain. Print `Path: <root> -> ... -> <target>` and `Total Size: <sum>` matching the exact format shown in the task. For `/home/user/optimize.sql`, add an index on the lineage table's child-id column (or a composite child_id+parent_id) since the recursive traversal repeatedly looks up "which backup is the parent of X" by child id — that's the query pattern that benefits most from indexing. Compile with `gcc planner.c -o planner -lsqlite3` and test against a known chain, verifying both the path string and total size by hand before finishing.