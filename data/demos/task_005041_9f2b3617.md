OCR the image first with `tesseract /app/cleaning_instructions.png -` to recover the exact `Correlation_Threshold=` and `Token=` values — parse them as strings/floats from the OCR output rather than hardcoding a guess, and double check the OCR text for stray whitespace/misreads before using it. For cleaning: load the CSV with pandas, compute `df.corr().abs()`, then iterate over column pairs in their original left-to-right order and, for any pair with |corr| > threshold, drop the column with the larger original column index (the one further right) — do this iteratively/carefully so a column already dropped isn't re-evaluated. Save the survivors with `to_parquet` (requires pyarrow or fastparquet installed). For the eigenvalue step, compute `np.cov` on the retained columns (rowvar=False) and `np.linalg.eigvalsh` (symmetric matrix) or `eigvals`, take the max, and round to 4 decimals matching the exact rounding the endpoint must return. Build the Flask/FastAPI/http.server app to check the `Authorization` header equals exactly `Bearer <token>` (constant-time or plain string compare is fine) returning 401 otherwise, then bind and run it on 127.0.0.1:8080 in the background (e.g. nohup + disown or a systemd-less daemon) and confirm with `curl -H "Authorization: Bearer <token>" http://127.0.0.1:8080/api/clean_stats` before finishing.