This task has two independent deliverables — solve them separately and verify each in isolation before moving on.

For Part 1 (video telemetry): use `ffmpeg` to dump frames (e.g. `ffmpeg -i /app/telemetry.mp4 frame_%04d.png`), then iterate over frames in Python/PIL or OpenCV computing per-frame mean R, G, B channel values, counting frames where R>250 and G<10 and B<10. Write the resulting integer count (nothing else) to `/home/user/restart_count.txt`. Don't eyeball a handful of frames — process the whole video programmatically so the count is exact.

For Part 2 (leakage detector): don't write a single brittle regex checking order of two substrings — that fails on adversarial variants (varied whitespace, variable names, comments, multiline calls). Instead, parse the script with Python's `ast` module, walk the tree in source order, and record the line/position of the first `train_test_split(...)` call versus the first `.fit(...)`/`.fit_transform(...)` call whose target looks like it's applied to the full dataset variable (not `X_train`/`_train` suffixed names). Compare positions to decide clean vs evil, then `exit 0` or `exit 1` accordingly from the bash wrapper (which should just invoke your Python checker and propagate its exit code). Test your script yourself against several deliberately varied clean/evil snippets you construct, not just the example in the prompt, before considering it done.