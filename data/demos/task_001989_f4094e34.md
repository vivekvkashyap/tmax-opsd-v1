Sequence this as reconstruct → deduplicate → recover-secret → serve, verifying each stage's output before moving to the next. Recombine the split archive with `cat corrupt_backup.tar.gz.parta* > corrupt_backup.tar.gz` then `tar xzf` it to get `bloated_system.conf`. Since the file is one config block repeated thousands of times, don't try to hand-parse the whole bloated file — instead extract only up to the *second* occurrence of `[SYSTEM_CONFIG]` (or the first blank/duplicate boundary) using something like `awk '/\[SYSTEM_CONFIG\]/{c++} c<=1'`-style logic, or read line-by-line and stop once you've seen the header twice, keeping the first block's key=value pairs. Separately, run `tesseract /app/api_token.png stdout` and parse out the substring following "TOKEN:" as your bearer token — treat it as a variable, don't guess it. Write an HTTP server (any language) binding 127.0.0.1:9090 with `GET /api/config`: check the `Authorization: Bearer <token>` header against your recovered token exactly, return 401 if absent/wrong, else 200 with `Content-Type: application/json` and a JSON object built from the parsed key=value pairs of the first config block. Leave the server running and test it yourself with curl (with/without/wrong auth header) before finishing. Never modify the original `.partXX` files.