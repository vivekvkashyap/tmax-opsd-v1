Implement the Bloom filter yourself with a small, well-understood design: pick `m` bits sized from expected_elements and false_positive_rate via the standard formula `m = -(n*ln(p))/(ln2)^2` and `k = (m/n)*ln2` rounded, allocate a bit array, and use k independent hash functions (a common trick is two base hashes, e.g. FNV-1a and a second seeded variant, combined as `h1 + i*h2` to simulate k hashes — avoid relying on a single weak hash or you'll get spurious false negatives). Expose the exact C ABI requested (`bloom_create`, `bloom_add`, `bloom_check`, `bloom_destroy`) via `extern "C"` (Rust/C++) or plain C, compiled with `-shared -fPIC` (or `crate-type = ["cdylib"]` in Rust) to `libbloom.so`. For property-based tests, load the .so with ctypes (Python+hypothesis) or FFI in your chosen language, and in a fixture create a fresh filter per test, add a hypothesis-generated list of random strings, then assert `bloom_check` returns 1 for every one of them (no false negatives is a hard guarantee of correct Bloom filters — if you see failures, the bug is almost certainly in the add/check hash indexing being inconsistent, not the algorithm itself). Wire `build_and_test.sh` to build the lib, install deps, run the tests, and only write `SUCCESS: ALL PROPERTIES PASSED` to result.log if the test suite genuinely exits 0.