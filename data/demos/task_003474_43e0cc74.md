Build this pipeline stage by stage, verifying intermediate outputs before moving on. First extract frames with `ffmpeg -i /app/factory_feed.mp4 -vf fps=1 /home/user/frames/frame_%04d.jpg` so filenames zero-pad to 4 digits matching the naming convention referenced later. In Go, for each frame load the JPEG, convert to grayscale (standard luma weighting), and compute the mean pixel value; any frame whose mean falls below the 5.0 threshold on a 0-255 scale is corrupted — delete that file and append its name to `/home/user/corrupted_frames.log` in extraction order. For the surviving frames, bucket grayscale pixel values into 8 equal-width bins (0-31, 32-63, ..., 224-255), normalize the counts (e.g. divide by total pixel count) into an 8-float histogram, and persist filename->histogram rows into a SQLite table with columns `filename, bin0..bin7` using a Go SQLite driver. Finally write an HTTP server with `net/http` exposing `POST /search`: parse the JSON body, reject with 401 if `token` doesn't match the required constant, look up the target frame's histogram row, compute Euclidean distance to every other row, sort ascending, and return the `top_k` filenames (excluding the target) as JSON. Launch the server backgrounded (e.g. `go run server.go &` or build+run a binary with `&`/nohup) bound to 127.0.0.1:8080, and smoke-test with `curl` before finishing.