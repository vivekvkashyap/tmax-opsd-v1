Diagnose before patching: read `optimizer.py`'s Gauss-Newton loop and locate the exact line computing `np.linalg.inv(JTJ) @ JTr` (or equivalent) — this is what throws on a near-singular normal-equations matrix. The correct, minimal fix is to swap `np.linalg.inv(...)` for `np.linalg.pinv(...)` (or use `np.linalg.lstsq(JTJ, JTr, rcond=None)[0]`), since pinv gives a well-defined minimum-norm solution when columns are collinear, rather than adding an ad-hoc ridge term that would change the converged values in ways the task's "don't change results unnecessarily" spirit discourages — pick the pseudo-inverse route so the update rule stays a faithful generalization of the original method. Do not touch the notebook's random seeds, data generation, or model structure — only edit `optimizer.py`. Then execute the notebook headlessly, e.g. `jupyter nbconvert --to notebook --execute --inplace simulation.ipynb` or `papermill`, from inside `/home/user/sim_project/`, and confirm `results.csv` is produced with three comma-separated floats on one line. Re-run and diff the output to confirm determinism given the fixed seed.
