Write a minimal, single-shot POSIX socket check: create a TCP socket with `socket(AF_INET, SOCK_STREAM, 0)`, populate `sockaddr_in` with `127.0.0.1` (via `inet_pton`) and port 9090 (careful with `htons`), and call `connect()`. Treat any non-zero return from `connect()` as failure (don't loop/retry — a single attempt is required). On success, just `return 0` with no output. On failure, open `/home/user/alert.log` in append mode and write exactly `[ALERT] Port 9090 is unreachable\n`, then invoke `system("tar -czf /home/user/data_backup.tar.gz -C /home/user app_data")` (or equivalent, preserving the `app_data/` prefix inside the archive so `tar -tf ... | grep app_data/...` succeeds), check the archive was actually created before proceeding, then `return 1`. Compile with `g++ /home/user/watchdog.cpp -o /home/user/watchdog`. Test both branches manually: run it once with nothing listening on 9090 (expect exit 1, log line, tarball), then start something like `python3 -m http.server 9090` and rerun (expect silent exit 0, no log/tarball side effects).