Do this as a three-step shell pipeline, verifying the file list at each stage before acting on it. First find candidate files with `grep -lE '(Hamming|Checksum)' /home/user/grpc_project/*.proto` — note the match must be case-sensitive (default `grep -l`, not `-i`) since the task explicitly says "Hamming" or "Checksum" and one fixture file deliberately contains lowercase "hamming" as a decoy that must NOT match. For each matching file, extract the package name with something like `grep -oP '(?<=^package )[a-zA-Z0-9_.]+(?=;)' file.proto`, convert dots to slashes (`tr '.' '/'`) to build the target directory, `mkdir -p` it, and `mv` the file in preserving its original filename — don't touch non-matching files. Finally, after all moves are done, run `sha256sum` on each moved file (using its final absolute path under `/home/user/organized_protos/...`), and pipe the combined output through `sort` (sorting by the path, which is the second field) into `/home/user/manifest.txt`, matching the two-space separator that `sha256sum` produces by default.