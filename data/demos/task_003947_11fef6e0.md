Work through the failures in dependency order rather than guessing at the 502 directly. First `make` in `/app/vendor/simple-c-server-1.0` and read the actual linker error — a bad `LDFLAGS` entry (e.g. a misspelled pthread flag) is the likely culprit; fix the Makefile so `make` succeeds cleanly. Next, run the built server manually and check with `ss -ltnp`/`netstat` which port it actually binds — compare that to the `proxy_pass` upstream in `/app/nginx/nginx.conf`, which is almost certainly pointing at the wrong port; correct the upstream to match. For the race condition, edit `/app/ci_runner.sh` so it starts the C backend first, then polls (a bash loop using `nc -z` or `ss` against the backend's port) until it's actually accepting connections before starting Nginx — starting both "simultaneously" is the naive mistake to avoid. Also check whether the C app requires `TZ=Etc/UTC` to avoid crashing at startup (an assertion tied to timezone); export that variable before launching the backend in the runner script. Finish by starting both services in the background via the fixed runner and confirming `curl -i http://127.0.0.1:8080/status` returns `200 OK` end-to-end, not just that each piece works in isolation.