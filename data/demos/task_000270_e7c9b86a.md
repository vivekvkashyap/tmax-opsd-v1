Treat this as two independent deliverables scored differently — the video extraction is only locally useful, but the event_processor is stress-tested against a hidden reference by feeding it thousands of fuzzed stdin streams, so its exact stdout formatting matters most. For extraction, use ffmpeg to dump per-frame crops of the top-left 10x10 region (e.g. `-vf "crop=10:10:0:0"` piped to rawvideo or PNG frames) and compute average luminance per frame with a small script, writing frame numbers where it exceeds the threshold to extracted_frames.txt. For event_processor: read all of stdin, split on newlines, parse only lines that are valid non-negative integers (ignore blank/malformed lines), collect into a set to dedupe, sort numerically ascending, then do a single linear scan merging consecutive integers (n, n+1, n+2...) into {start,end} events — a lone value gets start==end. Serialize with compact JSON separators (no spaces after ':' or ','), preserving key order start-then-end, and print with a trailing newline exactly once. Test by piping randomized/duplicated/unsorted integer lists into your script and manually verifying merges are correct, including the empty-input case producing `[]`.