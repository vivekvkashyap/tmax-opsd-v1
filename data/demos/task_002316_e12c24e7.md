Scaffold with `cargo new cleaner` inside `/home/user/cleaner/` and add the `csv` and `serde`/`serde_json` crates. Parse `users.csv` treating `feature_a/b/c` as strings first (not numbers) so you can reliably detect the anomaly markers `"NaN"`, `"-9999"`, and `""` before any numeric conversion — a row is dropped entirely if *any* of the three feature columns matches one of these, so check all three per row rather than stopping at the first match. Only convert the surviving rows' features to `f64`. For the group-mean step, filter cleaned rows by `group == "Treatment"` vs `"Control"`, average `feature_a` in each, and subtract. For the similarity search, compute Euclidean distance in 3D (`feature_a`,`feature_b`,`feature_c`) from every cleaned row to the row with `user_id == "U_TARGET"`, excluding U_TARGET itself, sort ascending by distance, and take the top 3 IDs. Serialize `{"mean_diff": f64, "similar_users": [String;3]}` to `/home/user/results.json` with `serde_json`, keeping full float precision (don't round unless told to) so the grader's tolerance check passes.
