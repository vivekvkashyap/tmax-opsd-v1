Break this into three independently-testable pieces before wiring them together. First, transcribe /app/pipeline_instruction.wav (e.g. with `whisper` or another local STT tool) to recover the target architecture and the exact secret token spoken in the audio — do not guess these values, extract them verbatim from the transcript and store them as constants your server reads at startup. Second, get cross-compilation working standalone: install the right toolchain package (e.g. `aarch64-linux-gnu-gcc` via apt) and manually verify `aarch64-linux-gnu-gcc -S -O0 file.c -o file.s` on a scratch C file produces assembly, then count `mov` instruction lines with a whole-word, case-insensitive match (e.g. `grep -iwc mov file.s` or a regex matching `mov` as a standalone mnemonic token at line start after whitespace, not substrings like "movl" if the task wants exact "mov" — check the audio-recovered spec/examples for how strict this must be) so you know your counting logic is correct before hooking it to the server. Third, write the asyncio/websockets server on 127.0.0.1:8081: on each connection, parse the JSON payload, compare `token` exactly against the recovered secret and `await ws.close(code=1008)` on mismatch, otherwise write `c_code` to a temp .c file, invoke the cross-compiler with `-S -O0`, count mov lines, and reply with the exact JSON schema. Launch the server as a persistent background process (not foreground-blocking) so it survives after your session ends.