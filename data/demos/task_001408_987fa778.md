Iterate the gzip files with Python's `gzip.open(path, 'rt')` and read line-by-line (a `for line in f:` loop or `f.readline()` in a loop) rather than `f.read()`, so you never load a whole file into memory; stop scanning once you've found the line that starts with exactly `TITLE: ` (use `line.startswith('TITLE: ')`, not a substring search, and don't assume it's the first line — scan forward until found). Build the slug by taking the text after the prefix, calling `.strip()`, `.lower()`, replacing spaces with underscores, then stripping any character that isn't alphanumeric or underscore with a regex like `re.sub(r'[^a-z0-9_]', '', slug)` — apply the space-to-underscore substitution before the alphanumeric filter so punctuation like `:` or `!` is simply dropped, not left as leftover underscores. Use `shutil.move` (or `os.rename`) to move each original gz file into `/home/user/final_docs/` under its slug name, creating that directory first. Print each mapping line as you process, then have your script collect and sort all filenames before printing, or pipe/sort in bash (`python3 script.py | sort > /home/user/rename.log`) to guarantee alphabetical order by original filename.
