In Go, parse the Bearer token from the Authorization header, base64-decode it, split the first 16 bytes as the IV and the remainder as ciphertext, and decrypt with `crypto/aes` + `crypto/cipher.NewCBCDecrypter` using the fixed 16-byte key. Strip PKCS7 padding carefully and validate it explicitly (check the padding byte value and length, reject with 403 rather than panicking/index-out-of-range on malformed padding — this is exactly what the bit-flipped "evil" corpus is designed to trigger). `json.Unmarshal` the plaintext into a struct with `user`, `role`, `checksum`; reject on any unmarshal error. Recompute `md5.Sum([]byte(user+role))` as hex and compare (constant-time or plain equality is fine here) against the `checksum` field; reject on mismatch, otherwise accept. Log every decision as `[TOKEN] - [ACCEPT/REJECT]` to `/home/user/audit.log`. Build with `go build -o /home/user/validator-bin` and run it as a background HTTP server on :8083. Then edit `/app/nginx/nginx.conf` to add an internal location that proxies to `http://127.0.0.1:8083/validate` and reference it via `auth_request` on the `/resource` location block, and reload nginx.