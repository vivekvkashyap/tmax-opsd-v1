Treat this as three independent deliverables and verify each with a real client connection, not just "process started." (1) OCR the secret: run `tesseract /app/secret_note.png -` (or similar OCR tool) to recover the exact text, then compute its SHA-256 with no trailing newline — use `printf '%s' "<recovered-text>" | sha256sum` (printf, not echo, to avoid an extra newline) and write the hex digest to `/home/user/old_hash.txt`. (2) SSH hardening: generate `ssh-keygen -t ed25519 -N "" -f /home/user/admin_key` and a separate host key at `/home/user/host_key`, write a minimal `sshd_config` with `Port 2222`, `HostKey /home/user/host_key`, `AuthorizedKeysFile /home/user/.ssh/authorized_keys`, `PasswordAuthentication no`, `StrictModes no`, `PidFile /home/user/sshd.pid`, append `admin_key.pub` into `authorized_keys`, then launch with `/usr/sbin/sshd -f /home/user/sshd_config` as your own user (no root) and test with `ssh -i /home/user/admin_key -p 2222 user@127.0.0.1 echo ok`. (3) Web gateway: write `httpd.sh` as a loop that accepts connections on port 8080 (e.g. with `nc -l` or a bash `/dev/tcp` read/write loop) and emits a full `HTTP/1.1 200 OK` response including the exact CSP header and body `Secure System`, then background it and confirm with `curl -i 127.0.0.1:8080`.
