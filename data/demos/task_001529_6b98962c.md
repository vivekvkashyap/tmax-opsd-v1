Run `make test` first to see the current failure output, then read `version_checker.py` methodically rather than rewriting from scratch. Two independent bugs to isolate: (1) the URL query-parsing loop strips only the substring `"version"` from each param instead of `"version="`, so the extracted value keeps a leading `=` — fix the `.replace()` call (or better, use `urllib.parse.parse_qs`/`urlparse` on the query string) so `version` and `service` are extracted cleanly; (2) `SemVer` stores `major`/`minor`/`patch` as raw strings from `.split('.')`, so comparisons like `self.major > old_version.major` and any minor-diff arithmetic on strings behave incorrectly for multi-digit numbers — cast each part to `int` at construction time. After both fixes, re-derive migration eligibility straight from the stated rule (major bump, or minor bump strictly greater than 2) rather than assuming the existing conditional logic is already right — check it against the rule text. Re-run `make test` until `migration_plan.txt` matches `expected.txt` via `diff -q`.
