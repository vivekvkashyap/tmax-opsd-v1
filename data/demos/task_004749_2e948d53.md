Work through this as four independent, individually-testable stages. First, load `/app/turbine_audio.wav` (e.g. `scipy.io.wavfile.read`) and compute PSD with `scipy.signal.welch(signal, fs, window='hann', nperseg=1024, noverlap=512)` — noverlap must be exactly half of nperseg for the specified 50% overlap, and using the default window or segment length instead of the specified ones will silently shift every downstream number. Second, fit `P(f) = A*exp(-B*f) + C` with `scipy.optimize.curve_fit`, giving it a sane initial guess (e.g. A≈max(psd), B small positive, C≈min(psd)) so the least-squares fit converges to the true decaying-exponential shape rather than a degenerate one. Third, normalize the raw PSD array and the fitted curve evaluated at the same frequency bins so each sums to 1 (divide by their own sums), add epsilon=1e-10 to both before computing KL divergence as `sum(p * log(p/q))` with p=empirical (true) and q=fitted (approx) — get the direction right, it is KL(empirical || fitted), not the reverse. Fourth, write `/home/user/analysis_results.h5` with `h5py`, creating group `model_fit` containing dataset `parameters` (array `[A, B, C]`) and scalar dataset `kl_divergence`, and sanity-check by reopening the file and printing its contents before finishing.
