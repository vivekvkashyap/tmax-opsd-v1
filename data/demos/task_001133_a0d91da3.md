Work through the stack bottom-up and verify each layer before moving to the next. First do the filesystem setup exactly as listed: `mkdir -p /home/user/backup`, copy the config there, `mkdir -p /home/user/app/logs`, and `ln -s /home/user/app/conf/nginx.conf /home/user/active_conf` (symlink target must be the absolute path, verified with `readlink`). For the C++ backend, find the `if (request.find("GET /health") ...)` branch in `server.cpp` and replace its response string with exactly `"HTTP/1.1 200 OK\r\nContent-Length: 2\r\n\r\nOK"` (byte-exact, including `\r\n`), compile with `g++ ... -o backend`, and launch it backgrounded (`./backend &`) — confirm with `curl localhost:8081/health` directly before touching Nginx. Then edit `nginx.conf`'s upstream block, changing the backend server address from port 9999 to 8081 (leave pid/error_log paths as already configured). Start nginx (`nginx -c ... &`), give it a moment to bind, then `curl http://127.0.0.1:8080/health` and redirect/save that exact output to `/home/user/resolution.log` — check it actually contains `OK` before finishing.
