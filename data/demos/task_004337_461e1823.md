Tackle the four pieces independently and verify each compiles/runs before wiring them into the build script. C: the `cumsum` loop uses `i <= n` over an `n`-length array, an out-of-bounds read/write — change the bound to `i < n`. Rust: `calculate_hash(data: String)` takes ownership, so the later `println!` using `report` fails to borrow-check — take `&str`/`&String` instead (or clone) without changing what's printed. Python: implement Simpson's 1/3 rule directly from its formula for the given function/interval/subinterval count, and separately load `libmath.so` via `ctypes.CDLL`, declare `cumsum`'s argtypes/restype correctly (pointer args, `c_int` for n), call it, and read the last element of the output buffer — print both results in the exact specified format and rounding. Shell: compile the C lib with `gcc -shared -fPIC -DRELEASE_MODE=1 ...`, compile the Rust binary with `rustc`, run the Rust binary then the Python script, appending both stdouts to the report file — run it yourself afterward and inspect the report's exact text before finishing.
