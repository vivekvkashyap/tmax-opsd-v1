This is two independent deliverables — do them separately and verify each in isolation before combining. For the services: start redis first (`redis-server --daemonize yes`), confirm it's up with `redis-cli ping`; edit the FastAPI `.env` to point at `127.0.0.1:6379` and launch uvicorn on port 8000 in the background, curl `127.0.0.1:8000/...` directly to confirm it works before touching nginx; only then edit `nginx.conf`'s `proxy_pass` to `http://127.0.0.1:8000` and listen on 8080, test nginx config with `nginx -t` before starting/reloading, then curl through 8080 end-to-end. For `analyzer.py`, implement the KDE formula literally using only `math.sqrt`, `math.pi`, `math.exp` — no numpy/scipy since bit-exact matching with a reference is required and library implementations can differ in floating-point rounding. Filter latencies to `0 < v <= 1000` (exclusive lower bound per spec) before computing N and the sum, evaluate at exactly x=10,50,100 in that order, and format each result with Python's `f"{val:.6f}"` joined by commas with no trailing newline (write via `sys.stdout.write`, not `print`, to avoid an extra newline). Make it executable with a `#!/usr/bin/env python3` shebang and `chmod +x`.