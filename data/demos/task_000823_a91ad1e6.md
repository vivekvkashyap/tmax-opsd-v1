Layer this as: encoding normalization -> CSV newline sanitization -> legacy binary call -> resampling -> HTTP wrapper, and test each stage in isolation before wiring together. Detect input encoding first (check for a UTF-16LE BOM/null-byte pattern, or non-ASCII byte sequences typical of ISO-8859-1) and convert to UTF-8 with `iconv` before anything else touches the payload, since the legacy `/app/config_processor` binary corrupts on non-UTF-8 and null bytes. For newline cleaning, don't naively strip `\n` — you must specifically find newlines that occur *inside* quoted CSV fields (track quote state character-by-character or use a proper CSV-aware parser like `csvkit`/`python -c` with the `csv` module, or careful `awk`/`perl` state tracking) and replace only those with a single space, leaving row-terminating newlines intact, otherwise `config_processor` will still drop or mis-split rows. Pipe the cleaned CSV into `/app/config_processor`, then post-process its `timestamp,config_key,config_value` output in Bash/awk: bucket timestamps into 60-second bins per `config_key`, and for any bin with no observation, carry forward the last known value for that key (forward-fill per key, not globally). Wrap all of this behind a minimal `socat`/`nc` HTTP listener on 127.0.0.1:9090 handling `POST /process`, returning 200 with the gap-filled CSV body. Finally add the cron entry (`crontab -l`/`crontab -e` with a `*/5 * * * *` line) invoking a dummy `/home/user/health_check.sh` that echoes OK to `/tmp/health.log`.