Part 1: run `ffmpeg -i /app/project_demo.mp4 -vf fps=1 /home/user/frames/frame_%04d.jpg` (ffmpeg's own numbering already zero-pads and sequences correctly at 1fps, so a separate bulk-rename step is only needed if your extraction produces a different naming pattern — check the output filenames match `frame_0001.jpg` style exactly before declaring done). Part 2: scaffold with `cargo new safe_tar`, add the `tar` crate as a dependency, and iterate archive entries with `Archive::entries()` using `.header()` to inspect each entry BEFORE writing anything to disk — do a full validation pass over every entry first (reject on absolute path via `path.is_absolute()`, reject on any `..` component via `Path::components()`, reject on `EntryType::Symlink`/`EntryType::Hard`) and only begin extraction if the entire archive passes; on any rejection print `REJECT`, ensure output_dir is left empty, and exit(1). For extraction, write each file to `<final_path>.tmp` then `std::fs::rename()` it into place atomically, creating parent directories as needed within output_dir. Print `ACCEPT` and exit(0) on full success. Build with `cargo build --release` and manually test against a few hand-crafted evil tars (traversal, absolute path, symlink) before trusting it against the real corpora.
