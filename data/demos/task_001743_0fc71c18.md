Keep the ETL script simple and literal to the spec: read the CSV with `csv.DictReader` (so column order in the dict doesn't matter), then for each row replace `ssn` entirely with the fixed literal string, and mask `email` by splitting on `@` and replacing everything before it with exactly three asterisks (`'***@' + email.split('@', 1)[1]`, not a regex that could mis-handle edge cases). Collect the transformed rows into a list of dicts and write with `json.dump(rows, f)` — an array of objects — to the exact output path, creating the file even if the directories already exist per the task's constraint. For the cron file, don't invent your own schedule syntax — write the single line matching standard 5-field cron format for "midnight every Sunday" (minute hour day month weekday, with weekday=0 for Sunday) followed by the interpreter path and script path, exactly as specified, with no extra comments or blank lines. Finally execute the script once so the JSON output actually exists on disk for verification.
