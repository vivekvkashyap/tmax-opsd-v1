Implement flattening as a recursive helper that walks nested dicts, joining keys with `.` (e.g. `flatten({"a": {"b": 1}}) -> {"a.b": 1}`) — apply it to each day's config before comparing consecutive days in chronological (filename date) order per server. For the drift score, iterate over the union of keys from both flattened dicts: missing-in-either → +10; present in both but `type()` differs → +20; present in both, same type, numeric → add `abs(a-b)`; same type, string/bool and unequal → +5; equal → +0 — be careful that in Python `bool` is a subclass of `int`, so check `type(a) == type(b)` (or explicitly treat bool separately) to avoid booleans being silently treated as numeric diffs. Sum drift scores per transition, flag any transition with score > 50 as anomalous, then per server compute `total_anomalies` (count) and `average_drift` (mean of all transition scores, rounded to 1 decimal). Load `/home/user/report_template.html` with Jinja2's `Environment(loader=FileSystemLoader(...))` or `Template(open(...).read())`, render it with a `servers` dict keyed by server name mapping to `{"total_anomalies": ..., "average_drift": ...}`, and write the rendered HTML to `/home/user/audit_report.html`. Install jinja2 via pip before running.