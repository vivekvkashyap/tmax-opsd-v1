Diagnose the numerical bug before rewriting: the current Rust code accumulates `sum` and `sum_sq` in `f32` and computes variance as `E[X^2] - E[X]^2`, which catastrophically cancels when the sensor values share a huge common offset (e.g. ~1e8) — the two large near-equal terms subtract to noise, producing NaN/0 variance. Fix it by switching every accumulator to `f64`, and prefer a numerically stable method: either a two-pass approach (compute the mean first, then sum `(x - mean)^2` in a second pass) or Welford's online algorithm, rather than the one-pass sum-of-squares formula. Compute the population standard deviation by dividing the sum of squared deviations by `N` (not `N-1`), derive each row's z-score as `(value - mean) / std_dev`, and format it with exactly 4 decimal places using Rust's `{:.4}` formatter. Write the three-column CSV with a header, then compute the file's SHA-256 (e.g. `sha256sum output.csv` or a Rust crate) and save the hex digest to `artifact_hash.txt`, making sure the hash is computed only after the CSV is fully and finally written to disk.
