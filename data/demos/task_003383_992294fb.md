Write the recursive traversal using `opendir`/`readdir`, and for each entry use `lstat` to distinguish symlinks from regular entries, then `stat` (which follows symlinks) to get the resolved device+inode pair. Maintain a visited set (array or hash of dev/inode pairs) of directories you've already entered; before recursing into a subdirectory (including one reached via a symlink), check its resolved dev/inode against the visited set and skip if already present — this is what prevents infinite loops from the deliberate symlink cycle in the fixture. For each resolved regular file, open it and read the first 4 bytes to check for the ELF magic `0x7F 'E' 'L' 'F'` before treating it as a binary to archive. Build the destination filename as `<original_filename>_<inode>.bak` using the basename of the path as encountered (not the resolved target's name) and the inode of the resolved file, then copy the file's bytes into the flat destination directory. After each successful copy, append a line to the log using `realpath()` on the original file to get an absolute path, in the exact format `COPIED: <abs_orig> -> <abs_dest>`. Compile with `gcc -o archiver archiver.c` and run against /home/user/app_tree, /home/user/backup_dir, /home/user/backup_log.txt.
