Work through the four pieces independently and verify each before combining. (1) SSH: `ssh-keygen -t ed25519 -f /home/user/.ssh/id_ed25519 -N ""`, then write a `Host *` stanza with `PasswordAuthentication no` into `/home/user/.ssh/config`, and lock down permissions (`chmod 700 ~/.ssh`, `chmod 600` on the key and config). (2) OCR: run `tesseract /app/exfil_target.png stdout` (or similar) and read its output text carefully — it will label a "Target Domain" value; use exactly that recovered domain string in your redaction logic, don't hardcode a guess. (3) Redactor script: match URL-like tokens (`https?://\S*<domain>\S*`) and replace the whole token with `[REDACTED_URL]`; separately match private-key blocks by finding a `-----BEGIN ... PRIVATE KEY-----` line through the matching `-----END ... PRIVATE KEY-----` line (inclusive, spanning multiple lines/base64 payload) and collapse the whole block to `[REDACTED_KEY]` — test against files in `/app/corpus/clean/` and `/app/corpus/evil/` to make sure clean files pass through byte-for-byte unchanged and evil ones are fully redacted. (4) Run `sha256sum /app/corpus/clean/*` and redirect to `/home/user/clean_hashes.txt`.