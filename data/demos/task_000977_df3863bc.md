Start by inspecting the existing schema and index with `sqlite3 /home/user/citation_network.db ".schema"` and `.indexes citations`. Fix the "corrupted" index by dropping and recreating it (`DROP INDEX IF EXISTS idx_citations_target; CREATE INDEX idx_citations_target ON citations(target_id);`) and also running `REINDEX;` or `PRAGMA integrity_check;` to be thorough. Add the missing complementary index on `source_id` too, since traversal needs both directions. For the metric script, write the SQL carefully: join `citations c` to `papers src ON c.source_id = src.id` filtered to `src.year >= 2018`, group by the cited paper (`c.target_id`), join to `papers` again to get the target's title, count rows per group as `recent_in_degree`, order by that count DESC then by `id` ASC for tie-breaking, and `LIMIT 5`. Decide whether papers with zero qualifying recent citations should be omitted (INNER JOIN naturally excludes them) — check the task's example output shape to see which papers are expected before choosing. Use `sqlite3 -json` or manually format each row into JSON objects, concatenated into a single top-level array with `jq -s` or string building, ensuring absolutely nothing but the JSON array goes to stdout (redirect any sqlite3 warnings to stderr).