Determine the crash conditions empirically rather than by static disassembly alone: feed `/app/legacy_parser` a range of crafted stdin lines one at a time (varying length, and inserting suspicious substrings) and watch its exit code/behavior to isolate exactly which conditions trigger a crash or bypass — treat length thresholds and marker substrings as hypotheses to confirm against the provided `/app/corpora/clean/` and `/app/corpora/evil/` samples, not assumptions. Implement `sanitiser.py` as a stdin-to-stdout line filter using `sys.stdin`/`sys.stdout` (preserving exact line content and trailing newline for lines you keep), dropping any line matching the length or pattern condition you discovered — validate it against both corpora directories with `cat file | python3 sanitiser.py` before considering the logic correct. For the systemd unit, add `After=tunnel.service` (with a corresponding `Wants=`/`Requires=`) to the `[Unit]` section so ordering is enforced, and rewrite `ExecStart` to pipe through your sanitiser before the legacy parser, e.g. `... | python3 /home/user/sanitiser.py | /app/legacy_parser`, keeping the rest of the pipeline intact. Do not modify the legacy binary or corpora files.
