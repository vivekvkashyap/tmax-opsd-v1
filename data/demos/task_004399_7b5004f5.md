This task has three independent pieces — sequence them and verify each before moving to the next. First, fix the vendored `networkx`: locate `node_link.py` under `/app/networkx-3.0/networkx/readwrite/json_graph/`, find where `node_link_graph`'s default keyword argument for the source-node key has been altered from the standard `"source"`, and revert it; confirm the fix by loading a small test node-link JSON directly with the vendored module before touching the other tasks. Second, for `/home/user/detector.py`, encode each "evil" rule as an explicit independent check against the parsed JSON (non-read `action`, restricted keys in `match_properties`, `limit > 100`, `max_depth` being the string `"unbounded"` or a number `> 5`) and exit 1 if any trigger, exit 0 otherwise — test against every file in both `/app/corpus/clean/` and `/app/corpus/evil/` and iterate until all pass, since a single missed rule variant will fail the whole corpus. Third, for `/home/user/summarize_graph.py`, load `/app/data/graph.json` with the now-fixed `node_link_graph`, compute `G.degree()` for every node, sort by degree descending then node id ascending, take the top 5, and write that list of `{"id":..., "degree":...}` dicts to `/home/user/top_nodes.json`.