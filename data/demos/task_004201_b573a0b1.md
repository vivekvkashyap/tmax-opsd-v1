Read `/home/user/legacy/sorter.go` carefully first to extract its exact behavior (which extensions go to which subdirectory, which get deleted, how many workers) — the Python translation must reproduce this precisely, using `threading.Thread` workers pulling from a `queue.Queue` fed with file paths (this is the required concurrency mapping for goroutines/channels, not just any parallel approach). Implement the move/delete logic with `shutil.move`/`os.remove`, creating target subdirectories first. For the test file, use `unittest.mock.patch` to replace `shutil.move`, `os.remove`, and `os.listdir` so no real files are touched, assert the workers invoke each mock with the paths you'd expect for a `.txt`/`.log`/`.tmp` fixture, and at the very end of a passing test run, append the literal success string to `/home/user/test_status.log` (e.g. in `tearDownModule` or after `unittest.main()` succeeds) — actually run the test suite yourself and confirm the log line appears. For `organize.sh`, branch strictly on `$USE_LANG`: for `go`, cross-compile with `GOOS=linux GOARCH=$TARGET_ARCH go build -o /home/user/bin/sorter_bin` (default TARGET_ARCH=amd64 if unset) then execute the binary against `$1`; for `python`, just exec `sorter.py "$1"`. Test both branches yourself, including overriding TARGET_ARCH, and `chmod +x` the script.