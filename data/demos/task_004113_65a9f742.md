This is a multi-part C systems task: fix the existing worker.c rather than rewriting from scratch. Inspect worker.c and the Makefile to understand the current fetch/parse/write loop first. For deduplication, compute an MD5/SHA-1 of each record's `id` (libcrypto or a small inline hash) and use hiredis (`SETNX` or `EXISTS`+`SET`) against a seen-hash key before processing; skip already-seen records. For aggregation, maintain an in-memory map keyed by category accumulating sum/count to compute averages, and track total unique count. For the stratified 10% sample, group unique records by category, then take roughly every 10th record per category so sampling stays proportional, marking those with `"sampled": true` in output_clean.jsonl. For the monitoring endpoint, run a raw TCP listener on port 9090 in a background thread/fork, parse a minimal HTTP GET request line, and hand-write a JSON response with total_unique/averages/sample_count — no full HTTP library needed. Keep the process alive so verification can query port 9090. Pass text fields through as raw UTF-8 bytes, no lossy ASCII-only handling.