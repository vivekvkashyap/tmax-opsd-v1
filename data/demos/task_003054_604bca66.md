Build the redaction script first and stress-test it against synthetic examples before touching the video. For AWS keys, match `AKIA[A-Z0-9]{16}` precisely (exactly 16 trailing uppercase-alphanumeric chars, not "16 or more") using `grep -oP` or `sed -E` with a word-boundary-safe pattern, replacing the whole 20-char token with `[REDACTED_AWS]` — be careful not to over- or under-match adjacent characters. For URL credentials, match the `scheme://user:` + password + `@host` pattern (e.g. `sed -E 's#(https?://[^:/@]+:)[^@/]+(@)#\1[REDACTED_CREDS]\2#g'`) and confirm the username and `@host` survive untouched. Test against varied edge cases (multiple keys/URLs per line, keys embedded in other text, URLs without passwords) to build confidence before assuming correctness. For the video: extract 3 frames at 1fps with `ffmpeg -i /app/admin_session.mp4 -vf fps=1 frame_%d.png`, OCR each with `tesseract`, concatenate the three texts in frame order, pipe through your `redact.sh`, and write the result to `/home/user/audit_trail.txt`. Manually inspect OCR output for artifacts/noise before feeding it through, since tesseract errors could break your regex matches.