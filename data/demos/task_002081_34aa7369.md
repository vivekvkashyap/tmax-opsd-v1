Set up the Go module first (`go mod init spectral` inside `/home/user/spectral`, then `go get gonum.org/v1/gonum/...` and `gonum.org/v1/plot/...`), and load the CSV with a simple manual parser or `encoding/csv` splitting on comma. For each N in {1024, 2048, 4096, 8192}, take `y[:N]`, run gonum's real FFT (`fourier.NewFFT(N)` / `Coefficients`), compute magnitude `|FFT[k]|` for each bin k, and explicitly skip k=0 (DC) when finding the max-magnitude bin. Convert the winning bin index to a frequency using `freq = bin * samplingRate / N` where samplingRate is 200 Hz — this bin-to-Hz conversion is the step most likely to go wrong (off-by-one on N vs N/2, or forgetting the Nyquist-limited range), so double check by verifying the max bin index never exceeds N/2. Format each log line to exactly 3 decimal places (`%.3f`) and append rather than overwrite across the loop iterations. For the N=8192 case, additionally plot magnitude vs. frequency (Hz) for bins 1..N/2, restricting the x-axis to 0-100 Hz (Nyquist), and save via gonum/plot's SVG writer to `/home/user/spectrum.svg`.