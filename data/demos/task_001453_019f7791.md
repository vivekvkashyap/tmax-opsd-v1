Work through the three fixes independently, then verify end-to-end. For Nginx, just change `client_max_body_size` in the `http`/`server` block to `500m` and reload/restart. For path traversal, never trust `TarInfo.name` directly: for each member, normalize the path (e.g. `os.path.normpath`), strip any leading `/` or `..` segments, and join it onto the target directory using something like `os.path.join(target_dir, os.path.relpath(os.path.normpath(os.path.join('/', member.name)), '/'))` so the resolved path is guaranteed to stay under `target_dir` — reject or clamp anything that still escapes after normalization, then extract member-by-member (not `extractall`) writing bytes yourself. For dedup, as you stream each regular file's contents from the tar entry, hash it with `hashlib.sha256` while writing to a temp/staging location; look up the hex digest in Redis — if present, delete/skip the just-written file and `os.link()` the existing path to the new location instead; if absent, `SET` the hash to the new file's path in Redis so later duplicates link to it. Test with `du -sb` vs `du -sb -l` mentally to confirm hard links reduce physical size while logical size stays the same.
