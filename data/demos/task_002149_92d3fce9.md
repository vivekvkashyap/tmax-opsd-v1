Start by reverse-engineering `/app/oracle_processor`'s expected input/output contract purely by black-box probing and static inspection (`strings`, `objdump -d`, feeding it sample stdin) — look for a fixed literal prefix it checks for before doing any transformation, and observe how it transforms and encodes text that follows the prefix (try simple known strings like all-letters, strings with spaces, mixed case, and diff the bytes of what goes in vs what comes out to spot the two-stage transform). Once you have a reliable oracle-vs-input relationship, write a small script that generates varied test strings (include spaces!) and runs both the oracle and `encode.sh` at each commit, diffing outputs — this is your bisection predicate (`git bisect run <script>` exiting 0/1 based on match). Run `git bisect start`, mark current `HEAD` bad and the earliest commit good, and let bisect converge on the exact breaking commit. Inspect that commit's diff to understand the introduced flaw (likely a stray string-stripping operation), then check out `main`, edit `encode.sh` to restore correct behavior end-to-end, and save the corrected script to `/home/user/fixed_encode.sh` with execute permission — verify it against the oracle on inputs containing spaces/tabs/punctuation before finishing.