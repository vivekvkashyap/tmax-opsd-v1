First get the vendored package building: `cd /app/bio-feature-extractor` and try `pip install -e .` to see the actual compiler error, which will point at a `-Werror` flag in `setup.py`'s `extra_compile_args` combined with an unused-variable warning in the C source — either drop `-Werror` from `setup.py` or remove the offending unused variable in the C file, then confirm `from bio_feature_extractor import compute_spectral_gap, local_alignment` imports cleanly before moving on. For processing, load `raw_data.json`, and for each item call `compute_spectral_gap(adj)` passing the adjacency list as a plain Python list of lists (not a numpy array or other type the C extension wasn't built to accept) and `local_alignment(seq, "GATTACA")`, storing results keyed by `id`. For the API, use Flask/FastAPI with a middleware or per-route check that compares the `X-API-Key` header against the expected secret and returns 401 on mismatch/absence before any other logic runs; implement `/health` and `/feature/{id}` with a 404 branch for unknown ids by checking your precomputed dict. Start the server and verify it's actually listening on `127.0.0.1:9090` with `curl` before finishing.
