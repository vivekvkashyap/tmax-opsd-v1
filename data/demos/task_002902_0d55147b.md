Since stability is guaranteed monotonic in `dt` (small dt stable, past some threshold everything diverges), you don't need to brute-force all 1000 grid points per parameter set if you want speed, but a straightforward correct approach is: import `integrate` from `/home/user/sim/integrator.py` (add its directory to `sys.path`), load `params.json`, and for each parameter set scan `dt` from 0.001 up in 0.001 steps (or binary-search over the 1000-value grid for efficiency), calling `integrate(a, b, dt)` and checking the result is finite AND strictly less than 1000 — treat both `inf`/`nan` and any value >= 1000 as unstable. Track the last `dt` that passed; that's your `max_dt` for that id (if none pass, that's an edge case to consider). Because it's monotonic, you can stop scanning upward as soon as you hit the first failure. Write the CSV with header `id,max_dt`, one row per parameter id in the same order as `params.json`, formatting `max_dt` with exactly three decimal places using something like Python's `f"{value:.3f}"`.
