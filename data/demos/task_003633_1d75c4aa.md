Write a script (Python is a good fit for CSV/JSON/XML handling) that: reads all CSVs in `/home/user/data/incoming/`, concatenates rows, and drops duplicate `event_id`s (keep first occurrence); sorts the deduplicated rows by `timestamp` ascending; then computes, per `device_id`, a rolling average over the current record and up to its 2 immediately preceding records for that same device (not a global rolling window — group by device first, or track a per-device deque while iterating the globally sorted sequence), rounding to 2 decimals. Emit both `/home/user/data/output/metrics.json` (array of objects with the 5 required keys, rolling_avg as float) and `/home/user/data/output/metrics.xml` (root `<metrics>`, each row as `<metric>` with the matching child elements) — build the XML with a real library (e.g. `xml.etree.ElementTree`) rather than string concatenation to avoid escaping bugs. After a successful run, move the processed CSVs from `incoming/` to `archive/` so re-running the job never reprocesses them (this is the retry-duplication fix). Finally, install a cron entry for the `user` account (`crontab -e` or write to a crontab file) using a `*/5 * * * *` schedule pointing at your script, and run the script once manually first to produce the required outputs immediately.
