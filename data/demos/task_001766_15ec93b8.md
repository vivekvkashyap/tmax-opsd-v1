Build this in the standard order: first the C++ core, then the ABI shim, then the build system, then verify end-to-end. Implement the CSP solver internally with real C++ types (e.g. `std::vector<std::vector<int>>` adjacency lists built from the conflicts array) and a recursive backtracking assignment routine that tries each worker for each task in order, checking against already-assigned neighbors, backtracking on failure, and returning failure only after exhausting all branches — the exposed `extern "C" int solve_csp(...)` function should just be a thin wrapper that builds the adjacency structure from the raw arrays, calls the internal solver, copies results into `out_assignments`, and returns 1/0. Compile `solver.cpp` as a position-independent shared object (`-fPIC -shared`) named `libsolver.so`, then compile and link `app.c` against it. For the Makefile, the key detail that's easy to miss is the rpath: link with `-Wl,-rpath,'$$ORIGIN'` (or an absolute path to the build dir) so `./app` resolves `libsolver.so` without `LD_LIBRARY_PATH` — verify this afterward with `readelf -d app | grep -i rpath` and by running `./app` from a clean shell with `LD_LIBRARY_PATH` unset. Finally run `make && ./app` and inspect `/home/user/output.txt` for both the solvable and unsolvable test cases.
