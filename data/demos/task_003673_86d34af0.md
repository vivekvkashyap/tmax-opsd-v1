This is a fuzz-equivalence task: your compiled `/home/user/encoder` will be run against many random inputs and its output byte-compared to a hidden reference, so match the exact parsing rules rather than any "reasonable" interpretation. First OCR `/app/amplitude.png` (e.g. with `tesseract`) to recover the integer amplitude — don't hardcode a guessed value, read it from the image. In the Go program, read stdin line by line (bufio.Scanner), skip lines whose first character is exactly `>`, and for every other line iterate its characters, uppercasing each and counting it only if it's A/C/G/T (ignore everything else, including `N` and whitespace); track total valid bases and G/C count separately. After EOF, if total==0 print exactly `0.000000\n`; otherwise compute `amplitude * math.Sin((gc/total)*math.Pi)` and print with `%.6f\n`. Edge cases the fuzzer will likely hit: empty input, input with only header lines, lines with mixed case and punctuation, and lines with no valid bases at all — test these manually before trusting the build.