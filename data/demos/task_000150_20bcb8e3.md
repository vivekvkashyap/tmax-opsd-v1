This is another fixture-driven correctness task with a hard pass/fail bar (must classify 100% of a clean and evil corpus correctly), so extract the exact spec before writing any Go code: run `tesseract /app/specs.png stdout` to OCR the format notes, and read the magic header bytes, compression algorithm, checksum/trailer format, and blacklist string precisely as stated in the OCR output rather than assuming common defaults. Implement the Go checker as a strict, ordered pipeline that fails closed at each step: (1) confirm the file is at least header+trailer length and the leading bytes exactly equal the magic header, (2) run the payload (everything between header and trailer) through Go's `compress/flate` reader (note: flate, not gzip/zlib, since those add extra wrapper bytes) and treat any read/EOF error as failure — this is how truncated/race-condition files get caught, (3) compute a checksum (`hash/crc32`, watching endianness when comparing to the trailer bytes) over the decompressed data and compare to the trailer, (4) scan the decompressed text for the blacklist substring. Any failure at any stage should `os.Exit(1)`; only if every stage passes should you `os.Exit(0)`. Wrap each stage in error checks (not panics) so malformed/truncated evil files exit 1 gracefully instead of crashing uncontrolled. Test explicitly against files in /app/corpus/clean/ and /app/corpus/evil/ before finishing.