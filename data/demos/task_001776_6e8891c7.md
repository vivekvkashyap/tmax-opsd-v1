First reverse-engineer the stripped binary's CLI contract empirically: run `/app/spectra_gen` with a few trial argument counts/orders and inspect the 100 printed numbers' shape (peak location/width) to confirm it takes exactly `amplitude center sigma` as positional args producing a Gaussian curve — verify by matching output against the closed-form Gaussian formula at a couple of index points before relying on it. Then build the Flask/FastAPI service on `127.0.0.1:8080`: enforce the `Authorization: Bearer ML-DATA-TOKEN` header check on the endpoint (return 401 if absent/wrong) before doing any work. In `/generate`, subprocess-call the binary to get the base spectrum, add the linear baseline (`i * baseline_slope` at index i) then Gaussian noise, and — critically — apply `scipy.optimize.curve_fit` with a matching model `A*exp(-(x-M)^2/(2*W^2)) + B*x + C` and a reasonable initial guess (e.g. `p0` near the requested a/mu/sigma/slope) so the fit converges reliably; catch `RuntimeError` from `curve_fit` as a fit failure. Return 400 with the validation-failed JSON if the fit fails or `|M - mu| > 2.0`, otherwise 200 with the spectrum and fitted mu. Launch the server as a background/daemon process (e.g. `nohup ... &` or a proper WSGI runner) so it stays up for testing.
