Build filter.py as a sequential pipeline of independent checks, short-circuiting to exit(1) on the first failure: (1) read stdin, base64-decode, split on `|` and require exactly 4 parts, else reject; (2) check the ssh_public_key field's prefix against the two allowed strings; (3) verify the signature by connecting over TCP to the daemon — since it's on a random port in 8000-9000, discover the live port at runtime (e.g. scan the range for an open socket, or check `/proc`/`ss`/`netstat` for the listening process once, rather than hardcoding a port) and send the raw token string plus newline, reading back "VALID"/"INVALID"; (4) for the password check, read `/home/user/wordlist.txt`, compute SHA-256 of each candidate password, and compare against the token's hash field — reject on any match. Run all four checks so a clean token must pass every one; exit 0 only if all pass. Test iteratively against a handful of files in `/home/user/corpus/clean/` and `/home/user/corpus/evil/` first, verifying exit codes with `echo $?` after piping each file, before assuming correctness across the full corpus.