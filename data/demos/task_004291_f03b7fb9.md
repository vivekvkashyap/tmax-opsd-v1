Treat this as pure string templating — parse the five CLI flags (`--subject`, `--min-year`, `--sort-by`, `--limit`, `--offset`) with a simple manual argv loop (no need for a heavy arg-parsing library), then emit the SPARQL template with `std::cout`, substituting values verbatim into the bracketed slots. The main risk is whitespace/formatting drift: match the exact indentation (2 spaces inside WHERE), exact line breaks, and no trailing/leading blank lines within a single query's output — build the output as one multi-line raw string or explicit `\n`-joined lines and diff it against the spec character-for-character before moving on. Compile with `g++ -std=c++17 -o /home/user/sparql_gen /home/user/sparql_gen.cpp`. Then write `/home/user/generate.sh` (executable) that calls the compiled binary three times with the three given parameter sets, redirecting/appending each run's stdout to `/home/user/queries.sparql`, and insert exactly one blank line between consecutive query blocks (e.g. `./sparql_gen ... >> queries.sparql; echo >> queries.sparql` between runs, but not after the last one — check the expected file's exact structure so you don't add a trailing blank line where none belongs). Run the script and then manually inspect the resulting file against the required template before finishing.