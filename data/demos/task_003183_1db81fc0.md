Implement this as three clearly separated stages in your C program so each is easy to verify independently. First, read `/home/user/sensor_data.csv` and bucket rows by `(timestamp / 10) * 10` using integer division, accumulating sum/count of `x_coord` and `y_coord` per bucket (data is already time-sorted, so you can accumulate into an ordered list/map keyed by bucket as you scan, flushing a bucket when the key changes). Second, once buckets are finalized in chronological order, compute each bucket's centroid (`mean_x`, `mean_y`) and its Euclidean distance from the origin via `sqrt(mean_x*mean_x + mean_y*mean_y)` (link with `-lm`). Third, compute a simple moving average over the *sequence of bucket distances* with a window of the current bucket plus up to the 2 preceding buckets (not a per-row rolling window, and not reset by anything except reaching the start of the sequence) — get this window logic right since it's the easiest part to get subtly wrong (e.g. off-by-one on which buckets are included). Write `/home/user/processed_stats.csv` with the exact header and `%.2f`-formatted floats for every field, then compile with `gcc -lm` and run the binary to produce the file.