Diagnose why the current script is slow/wrong first: its nested while-read loops re-scan the full purchases file for every customer without matching on customer_id, producing an implicit cross join. Replace it with a proper join-then-aggregate pipeline using standard tools: sort both CSVs (minus headers) on `customer_id` with `sort -t, -k2` (purchases) / `-k1` (customers) as needed, then use `join -t, -1 <col> -2 <col>` to pair each purchase with its customer's region, or do it in one pass with `awk -F,` by first loading `customers.csv` into an associative array keyed by customer_id (skipping the header row) and then streaming `purchases.csv` (skipping its header) to accumulate `sum[region] += amount`. At the end, emit the accumulated per-region totals as JSON — either build the object manually in awk (comma-handling between entries) or pipe key/value pairs through `jq -n` with `reduce inputs` to assemble a valid JSON object, ensuring amounts are numeric not quoted strings. Write the result to `/home/user/region_summary.json`, make the script executable, and actually run it once to produce the file.