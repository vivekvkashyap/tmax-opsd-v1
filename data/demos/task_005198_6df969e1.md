Treat this as three independent, verifiable sub-tasks. (1) Redaction: find `.config` files with `find /home/user/app_repo -name '*.config'`, then use a precise in-place edit (e.g. `sed -E -i 's/SECRET_TOKEN=[0-9a-fA-F]{32}/SECRET_TOKEN=REDACTED/'`) so only a true 32-hex-char token is replaced and the rest of each file is untouched. (2) Integrity: parse `/home/user/app_repo/hashes.txt` and for each listed file recompute `sha256sum` yourself, compare against the recorded hash, and write only the paths of files whose computed hash differs to `/home/user/compromised.log` (don't assume which file is bad — verify all of them). (3) Exploit: read the flagged script's source to find how it builds a shell command from its `-e`/environment argument — look for unsanitized interpolation into `eval` or a subshell — then craft an `-e` value that breaks out of the intended string and appends a command reading `/home/user/vault/secret.txt`, run the script with that payload, and redirect its full stdout to `/home/user/exploit_out.txt`.