Diagnose by reproducing the failure mode first: run the existing health_monitor.sh manually with an empty/stripped environment and a different cwd (mirroring how watcher.py invokes it) to confirm it fails because `curl` isn't found on a bare PATH and because it writes to a relative `status.log` in whatever directory it happens to be run from. Fix by adding an explicit `PATH` export (or using absolute binary paths like `/usr/bin/curl`) at the top of the script, and by hardcoding the absolute log paths under `/home/user/health_logs/` (creating that directory if needed) instead of relative filenames — never rely on the caller's cwd. After curl'ing `http://localhost:8080/ping` and appending the status code, add a size check on the status log (e.g. `stat -c%s` or `wc -c`) and append `STORAGE_ALERT` to the alerts log the moment the size strictly exceeds 100 bytes — guard so you don't have to worry about re-triggering, since the check just needs to run every invocation. Start app.py and watcher.py in the background, then wait/poll a few cycles and inspect the log files directly to confirm status codes and the alert actually appear before considering it done.