Don't try to reverse-engineer the stripped binary purely by disassembly — first run `make` in `/home/user/workspace/` to generate the clean and evil `.bin` corpora, then feed each file to `/app/bin/asset_compiler <file>` individually (in a loop, capturing exit codes) to empirically confirm which files crash it. Once you have the crash/no-crash split, diff the byte contents of evil vs clean files (e.g. `xxd` each and compare, or use a disassembler like `objdump -D -b binary -m i386` on the `.bin` files) to isolate the specific instruction byte sequence or opcode pattern unique to the crashing set — treat this as a signature-detection problem, not a full semantic analysis. Compile `pipeline.proto` with `python -m grpc_tools.protoc`, then implement `ValidateAsset` in `validator_server.py` to scan the incoming `content` bytes for that exact byte pattern (e.g. via a substring/`bytes.find` search) and return `is_valid=False` on a match. Start the server bound to `0.0.0.0:50051` in the background, then write `test_client.py` to iterate over both corpora directories and assert your server classifies every file correctly before leaving it running.
