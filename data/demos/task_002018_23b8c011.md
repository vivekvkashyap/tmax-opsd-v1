Treat this as a vendored-dependency latency bug: don't just tune application-level timeouts, trace the actual request path down into the vendored `urllib3` source at `/home/user/app/urllib3-src`. Grep the connection-establishment code (e.g. the socket `connect()` method in `src/urllib3/connection.py`) for any conditional sleep or delay keyed off a specific host/IP — a common perturbation is code that special-cases one loopback address (e.g. `127.0.0.2`) and calls `time.sleep(...)` right after the socket connects. Remove that injected delay entirely rather than reducing it. After patching the source, you must reinstall it into the venv (since it's installed, likely in editable/dev mode, a reinstall or reload may be needed — verify with `pip show`/`pip install -e` from `/home/user/app/venv/bin/pip`) so the running interpreter picks up the change, not just edit the file on disk. Then restart both the DB service and worker processes cleanly (kill old PIDs, relaunch) so no stale process is still using the old bytecode/import. Finally create the required symlink with `ln -s`. Validate by timing 20 sequential sync requests end-to-end yourself before assuming success.