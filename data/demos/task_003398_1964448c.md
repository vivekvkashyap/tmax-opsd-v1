Preserve row order throughout — read the CSV into a Vec of structs in file order, filter (drop sensor_id outside [1,100], drop reading_value > 1000.0 when present) without sorting, then split with integer division `train_count = total * 8 / 10` taking the first train_count rows as Train and the rest as Test, strictly by position. Compute mean and *sample* (n-1 denominator, not population) standard deviation of reading_value using only the non-missing values within the Train slice — never touch Test rows when computing these statistics, since that's exactly the leakage bug you're fixing. Then, as a separate pass, impute missing reading_values in both Train and Test with the Train mean, and standardize every reading_value in both sets using the Train mean/std (Test points are transformed with Train's statistics, never their own). Fit 1D OLS (w = cov(x,y)/var(x), b = mean(y) - w*mean(x)) on the Train set's scaled feature vs target_value. Use a crate like `csv` + `serde` for I/O and `serde_json` to emit the final object, rounding each float to 4 decimal places only at output time, not during intermediate computation.
