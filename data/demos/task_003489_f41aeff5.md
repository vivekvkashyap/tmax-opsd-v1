Parse `/home/user/observations.csv` (skip header) into arrays of x, y, v. Compute cell boundaries by dividing [0,1] into N equal segments per axis: cell i spans [i/N, (i+1)/N]. Implement membership as half-open `xmin <= x < xmax` except when the cell touches the domain's right/top edge (`i == N-1` or `j == N-1`), where you must also include points exactly equal to 1.0 — get this boundary condition exactly right since it changes which points land in edge cells. Compute *population* variance (divide by count, not count-1) per cell, treating empty cells as variance 0.0. Loop order must be outer-x (i) then inner-y (j) at both LEVEL1 and LEVEL2, printing each qualifying cell's line immediately as you go rather than buffering and sorting afterward — the print order is dictated by loop order, not by value. When a LEVEL1 cell exceeds threshold T, bisect it into a 2x2 grid of sub-cells (same half-open/edge-inclusive rule, using the same outer-x inner-y ordering, indices 0..1) and check each independently; do not recurse further. Format every numeric field to exactly 3 decimals with `printf`. Finally write `pipeline.sh` to compile with `gcc -O3 -lm`, invoke the binary with N=2 T=2.0, redirect stdout to `/home/user/refined_cells.txt`, chmod +x it, and actually execute it so the output file exists.