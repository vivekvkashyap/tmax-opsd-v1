Start by transcribing `/app/secret_passphrase.wav` (e.g. with `whisper`, `ffmpeg`+speech tooling, or careful listening) to get the bearer token — never hardcode a guess. Build the Flask/FastAPI server binding exactly to 127.0.0.1:8000, and check the `Authorization: Bearer <token>` header first on every route, returning 401 before touching any body/file logic. For `/upload`, read the uploaded zip's member names with `zipfile.ZipInfo` BEFORE calling `extractall` — reject if any entry is an absolute path or, after `os.path.normpath`, resolves outside the target directory (classic zip-slip check: normalize each path and confirm it stays under `/home/user/artifacts/`, not just a naive `../` substring check, since `..` can appear encoded or nested). Only extract once validated. After each successful extraction, regenerate the whole manifest.csv by walking every file currently in `/home/user/artifacts/` (excluding manifest.csv itself) and computing SHA256 fresh — don't try to patch old entries incrementally, since overwritten files need updated hashes and full recomputation avoids stale rows. Keep the server running (background process or nohup) so the test suite can hit it repeatedly.
