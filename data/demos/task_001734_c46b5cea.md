Reverse-engineer the `.arc` binary format by black-box experimentation with `/app/legacy_extract` before writing any Python: create tiny test archives by hand (using `xxd`/`printf`/a small helper script) with known magic bytes, integer counts, filename lengths, and payloads, run the legacy binary against them in scratch directories, and observe both the resulting filesystem (use `ls -i` to see inode numbers and confirm hard links share an inode) and the exact stdout JSON (byte-diff it, check indentation and key order) to infer field widths/endianness rather than guessing. Once the byte layout is understood, implement `/home/user/extract.py` to: parse the header/count, loop reading filename-length-prefixed entries and fixed-size-prefixed payloads, compute SHA-256 of each file's content, and use a dict from hash to the first filename seen — write new files normally but call `os.link(first_path, new_path)` for repeats. Build the JSON manifest incrementally in file-arrival order (not sorted alphabetically) with exactly 2-space indent and no trailing newline, matching the legacy tool's stdout byte-for-bit. Test against many random and adversarial `.arc` files you generate yourself, comparing your output and filesystem state directly against the legacy binary's, including edge cases like zero files and zero-byte payloads.