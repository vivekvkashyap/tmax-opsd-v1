Work this as a three-stage pipeline where each stage's output feeds the next. First, transcribe `/app/incident_alert.wav`: install a speech-to-text tool (e.g. `ffmpeg` plus `whisper`/`speech_recognition`, or another STT approach available offline) and run it to recover the spoken 4-digit PIN — don't guess or hardcode a number, actually extract it from the audio. Second, automate the interactive `/app/fw_unlock` binary with an Expect script at `/home/user/unlock.exp`: use `spawn`, `expect` on the literal prompt text, `send` the PIN followed by a newline, then `expect eof` (or similar) to let the process finish cleanly; run the script and confirm a success flag file appears. Third, write a small C TCP server (`/home/user/monitor_service.c`) using raw sockets (bind/listen/accept, or a minimal HTTP parsing loop) that answers `GET /status` with a proper `HTTP/1.1 200 OK` response, `Content-Type: application/json` header, blank line, and a JSON body embedding the PIN you extracted; compile with gcc and run it detached (e.g. `nohup ... &`) so it survives and stays listening on 127.0.0.1:8080 for the automated checker.