Before writing any exploit code, read `/home/user/extractor.cpp` carefully to find the trust boundary flaw: look at how it decides a JWT is valid — if it accepts the token whenever the header's `alg` field says `"none"`, regardless of the actual signature bytes, that's the classic "alg=none" JWT bypass (you don't need the HMAC secret at all if you can make the validator take that branch). Confirm this by inspecting the code path, not by guessing. To exploit it manually with shell tools: split `/home/user/legacy_token.jwt` on `.` into header/payload/signature segments, base64url-decode the header to see its current `alg` value, then construct a new token with the header changed to `{"alg":"none","typ":"JWT"}` (base64url-encode it, no padding), keep the original payload segment, and use an empty (or any) signature segment — then run the extractor against this crafted token so it takes the `alg==none` accept branch and prints the decoded payload, which is the private key. Save that key to `/home/user/.ssh/id_rsa`, `chmod 600` it, and derive the public key with `ssh-keygen -y -f /home/user/.ssh/id_rsa > /home/user/.ssh/id_rsa.pub`.
