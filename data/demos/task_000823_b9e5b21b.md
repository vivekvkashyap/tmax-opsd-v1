Write the Go program to stream-parse `/home/user/etl_dump.csv` (skip the header), classifying each row's `value` field as corrupt if it contains a `.` or equals the literal string `NaN`, otherwise clean — track `total_rows` and `corrupt_rows` as simple counters while iterating once. For the Bayesian piece, recall the Beta-Binomial conjugate update: with a flat Beta(1,1) prior and `k` corrupt observations out of `n` total, the posterior is Beta(1+k, 1+(n-k)), whose mean is `(1+k)/(2+n)` — compute this directly rather than simulating. For covariance, accumulate `record_id` (as float64, parsed from the row) and a 0/1 corruption indicator per row in a single pass (two running sums plus a second pass, or Welford-style online covariance to avoid storing all rows), then apply the sample covariance formula with `n-1` in the denominator: mean-center both series and sum the products of deviations, divided by `n-1`. Marshal the four fields into JSON with `encoding/json` using the exact key names given, write to `/home/user/audit_results.json`, then execute the program (`go run` or build+run) to produce the file. Double-check your corruption predicate handles both `.` and exact `"NaN"` matches, since partial matches would shift both counts and the covariance.