Write a recursive CTE against the `citations` table that performs a breadth-first shortest-path search: seed the anchor with the source id and an initial path (use `json_array(source_id)` or a delimited text column plus a depth counter), then recursively join `citations` on `source_id = ` the last node in the current path, appending `target_id`, incrementing depth, and guarding against cycles (e.g. `WHERE instr(path, target_id) = 0` if using a text path, or track visited ids). Because BFS shortest-path in a recursive CTE naturally explores multiple paths, select the row reaching the target with the minimum depth (`ORDER BY depth LIMIT 1` in an outer query, or `MIN` aggregation) — a naive CTE without an early depth cutoff/limit will keep expanding and may return a non-shortest or duplicated path. Build the final `iptables`-style JSON array by formatting the path column with `sqlite3`'s `-json` output or manual string construction (e.g. `'[' || group_concat(id, ', ') || ']'`), ensuring no extra headers/newlines leak into stdout (use `sqlite3 -batch -noheader` and pipe through `tr`/`sed` if needed). Test both the reachable case and the reverse (unreachable) direction to confirm `[]` is emitted when the recursive CTE finds no row hitting the target.