Break this into independently-testable layers before wiring them together. First, transcribe `/app/transmission.wav` (a spoken sequence of "zero"/"one" words) into a raw binary string using an available speech-to-text tool — verify the transcript looks sane (right length, only 0/1) before trusting it downstream. Second, implement standard Hamming(7,4) decoding in C: for each 7-bit block, compute the syndrome from the parity bits, use it to locate and flip any single erroneous bit, then extract the 4 data bits from their standard Hamming positions — build this as a shared library per the Makefile requirement and unit-test it against hand-computed encoded blocks (including ones with a deliberately flipped bit) before trusting it on the real transcript. Third, define the `.proto` service, generate the Python/gRPC stubs, and write a server that loads `libhamming.so` via `ctypes`, splits the incoming bit string into 7-bit chunks, decodes each to a nibble, packs pairs of nibbles into bytes, and decodes those bytes as ASCII. Fourth, write the client to glue transcription -> gRPC call -> print/save result. Validate incrementally: test the Hamming decoder standalone, then the gRPC round-trip with a known small payload, before running the full pipeline against the real audio and writing the final string to the exact output path with no trailing newline.