Treat `/app/legacy_archiver` as a black box and reverse-engineer its stdin-to-stdout transformation empirically rather than statically disassembling it as the primary approach: feed it small, controlled inputs (empty input, a few bytes, exactly one chunk boundary's worth, more than one chunk) and inspect the output byte-for-byte with `xxd` to spot a repeating structure — look for a fixed magic marker preceding each chunk, a length field (check its byte order and width against known chunk sizes), the raw payload itself, and a trailing byte that changes deterministically with the payload's bytes (test this by flipping one input byte and diffing the output, which will reveal a checksum in the trailing byte, and identify its exact combining operation over the payload). Cross-check hypotheses with `strace`/`objdump` if the black-box inference is ambiguous. Once you've derived the exact chunk size, header layout, and trailing-byte formula, implement it in `/home/user/archiver.cpp` reading stdin/writing stdout with proper binary I/O, handle the empty-input edge case, and compile to `/home/user/archiver`. Validate by diffing your tool's output against the original binary's output on the same random inputs (`diff <(./archiver < f) <(/app/legacy_archiver < f)`) across several sizes before finishing.