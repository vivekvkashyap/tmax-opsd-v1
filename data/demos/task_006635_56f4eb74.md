The bug is that the current script builds `results` from three independent flat lists (all employees × all roles × all HIGH systems) with nested for-loops, completely ignoring the `edges` array — that's why everyone shows access to everything. Fix it by actually traversing the graph: build a lookup from role-node-id to the set of employee names that have a `HAS_ROLE` edge into that role, and a lookup from role-node-id to the set of system nodes reached via `HAS_ACCESS` edges from that role (filtered to `sensitivity == "HIGH"`); then for each role, cross only its connected employees with its connected HIGH systems (not the global lists) to build valid `(employee, system)` pairs. Deduplicate pairs in case an employee reaches the same system via more than one role. Sort the resulting list by `employee` name first, then `system` name (a tuple sort key does this in one line), slice the first 5 entries for pagination, and write that as a JSON array of `{"employee":..., "system":...}` dicts to `/home/user/audit_results.json`. Trace through the given graph.json edges by hand for at least one employee (e.g. verify which systems an Admin-role employee actually reaches) to sanity-check your traversal logic before trusting the final sorted/paginated output.