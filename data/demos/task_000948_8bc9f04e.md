Split this into independent, verifiable stages in pipeline.py rather than one monolithic pass. Ingestion/validation: read the CSV with pandas, validate each rule per-row (parse timestamp with `datetime.fromisoformat` or `pd.to_datetime(..., errors='coerce')` and drop rows that fail to parse or fall outside the given numeric ranges for sensor_id/temperature_raw/pressure_raw — use inclusive bounds exactly as specified) and drop the whole row if any single field fails. Aggregation: convert timestamp to datetime, then group into hourly tumbling windows with `df.set_index('timestamp').resample('1H')` (or floor timestamps to the hour) and take the mean of temperature_raw and pressure_raw per window, sorted by window start. Bayesian update: implement the three matrix equations literally with numpy (`K = P @ inv(P + R)`, `x = x + K @ (z - x)`, `P = (I - K) @ P`) iterating window-by-window in chronological order, carrying x/P forward as state — a common bug is resetting the prior each iteration instead of carrying the *posterior* forward as the next prior, or transposing the innovation vector incorrectly. Round final T/P to 4 decimals and write `window_start` (formatted `YYYY-MM-DD HH:MM:SS`), est_temperature, est_pressure to `/home/user/output/estimates.csv` (mkdir -p the output dir). Have run_pipeline.sh create the venv, pip install pandas/numpy, then run pipeline.py, and chmod +x the shell script.