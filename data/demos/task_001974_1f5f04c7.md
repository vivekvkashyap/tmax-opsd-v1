Work in order: hash first, exploit second, fix third, so the hash you record is of the *original* vulnerable file before you touch it — run `sha256sum /home/user/uploader/uploader.c | awk '{print $1}'` (hash only, no filename) into audit_hash.txt. To find the path traversal, read uploader.c: it builds `uploads/<filename>` via snprintf and shell-executes a `base64 -d` command, so a filename containing `../` escapes the uploads directory. Write poc.sh so it computes `echo -n "EXPLOIT_SUCCESS" | base64` into a variable, then invokes `./uploader "../target.txt" "$B64"` (relative to the uploads dir, so two levels of `../` may be needed depending on how you invoke it — verify the resulting file lands at exactly /home/user/target.txt) and confirms the file's contents. Only after proving the exploit, patch uploader.c: at the very top of main (or before the write), reject filenames containing `/` or `.` by printing `Access Denied` and `exit(1)` immediately, before any snprintf/system call runs. Compile the patched file to /home/user/uploader_fixed without overwriting the original binary. Finally `chmod 700 /home/user/uploader/uploads`. Test uploader_fixed against a traversal filename, a filename with a dot, and a clean filename to confirm all three exit codes/behaviors match spec.
