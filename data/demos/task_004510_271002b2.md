For Part 1, extract each of the 32 one-second frames from `/app/repo_sync.mp4` (e.g. `ffmpeg -i repo_sync.mp4 -vf fps=1 frame_%02d.png`), then for each frame sample a pixel (or average) to classify it as solid black (bit 0) or solid white (bit 1) — process frames in order so frame 0 is the most-significant bit, concatenate the 32 bits, and convert that binary string to a decimal integer with `int(bits, 2)`, writing only that integer to `/home/user/repo_id.txt`. For Part 2, write `apply_wal.py` to read the WAL text file, compare its first line's REPO_ID against the integer you recovered in Part 1 (as a string comparison of the exact line), and short-circuit with a plain copy if it doesn't match. For real patches, shell out to `readelf -S <elf>` and parse the output table to find each named section's file offset (the "Off" column, hex) — do this per PATCH_SEC command since offsets differ per section — then apply PATCH_SEC/APPEND/TRUNCATE in order against an in-memory bytearray before writing the final output file, so partial state never gets flushed prematurely.
