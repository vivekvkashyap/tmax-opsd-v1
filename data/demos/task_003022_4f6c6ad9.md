Treat `/app/audit_bin` as a black-box oracle you query, not something to disassemble line-by-line — feed it small, hand-crafted JSON arrays via stdin (`echo '[...]' | /app/audit_bin`) and observe the exact stdout for each, starting from trivial cases (one node, one grant, no edges) and building up (add a chain of edges, add multiple grants to the same node, mix sensitive/non-sensitive nodes, add a cycle) to isolate the aggregation rule. Formulate a hypothesis after each probe — expect the score to be "count of distinct sensitive nodes transitively reachable from nodes the user was granted, following add_edge as directed reachability" — and confirm it against 5-10 more probes including edge cases (grants to nonexistent nodes, duplicate edges, self-loops, unreached users) before writing `/home/user/auditor.py`. Implement it as a BFS/DFS reachability closure per user over the directed graph, tally sensitive nodes reached, then serialize with `json.dumps(result, sort_keys=True, separators=(",", ":"))` to match the binary's compact sorted-key output exactly — mismatched whitespace or key order is the most common way this fails equivalence checks.
