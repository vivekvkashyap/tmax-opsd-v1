Diagnose the existing bug before rewriting: the current query lists `employees`, `departments`, `sales` in the `FROM` clause with commas and no `WHERE`/`ON` linking them, so it's an implicit cross join multiplying row counts together — replace it with explicit `JOIN ... ON` clauses tying `sales.emp_id = employees.id` and `employees.dept_id = departments.id`, which also naturally excludes employees with zero sales since an inner join drops unmatched rows. Aggregate `SUM(amount)` per employee in a CTE, then compute `RANK() OVER (PARTITION BY dept_id ORDER BY total_sales DESC)` in a second CTE layered on top — ranking needs to happen after aggregation, not inside it. Build the `WHERE` clause conditionally in bash based on whether `$1` is the literal string `ALL` or a numeric department id, and interpolate it into a heredoc SQL string passed to `sqlite3 -csv -header` so the CLI itself emits correctly quoted CSV (don't hand-format commas). Apply `LIMIT $2 OFFSET $3` in the outer query and order by `Department ASC, DeptRank ASC, Employee ASC` exactly as specified, writing to `$4`.
