Load `/home/user/data/embeddings.csv` with pandas, fit `sklearn.decomposition.PCA()` (unscaled, all 50 components) on the `v0..v49` columns, then use `np.cumsum` on `explained_variance_ratio_` and find the smallest component count where the cumulative sum reaches 0.90 — write that integer to `pca_components.txt`. Transform the data into that reduced-dimension space, then compute pairwise Euclidean distances (`sklearn.metrics.pairwise.euclidean_distances` or `scipy.spatial.distance.pdist`) and collect all pairs strictly closer than 0.25. Build an undirected graph over user `id`s with those pairs as edges (e.g. `networkx.Graph`), find connected components, and within each component of size > 1 keep only the lowest `id`, marking the rest for removal — watch out for the naive mistake of only checking mutual nearest-neighbor pairs instead of full transitive components, and of comparing indices instead of the actual `id` column values. Write sorted removed ids one per line, and write the cleaned/sorted-by-id CSV preserving all original `v0..v49` and `id` columns exactly.