Start with git archaeology: `cd /home/user/investigator_repo && git log --all --oneline` (and `git reflog`, `git fsck --lost-found` if history was rewritten) to find the commit where the Rust extraction tool source still existed, then `git show <commit>:<path>` or `git checkout <commit> -- <path>` to restore it into the working tree — don't assume it's on the current HEAD or default branch. Once recovered, build it (`cargo build`) and run it against /app/suspicious_audio.wav to reproduce the crash/incorrect output. Use a debugger (gdb/lldb via `cargo build` with debug symbols, or `RUST_BACKTRACE=1`) to locate where a sample index computation overflows a signed integer type (look for an `i32` multiply/index calculation on sample counts that can exceed i32 range or go negative). Fix by widening the type (e.g., to `i64`/`usize`) or using explicit `wrapping_*`/checked arithmetic so indices stay correct and non-negative, then re-run to confirm the extraction now succeeds and produces a sane plaintext string. Finally wrap the fixed extraction function in a minimal Rust TCP/HTTP server (std::net::TcpListener is sufficient) listening on 127.0.0.1:8000, accepting POST /analyze with a raw WAV body, running the extraction, and returning the extracted string as a 200 OK plain-text body. Leave it running in the background and test with curl --data-binary before finishing.