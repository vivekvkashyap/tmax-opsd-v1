Replace any use of `date +%s` (integer seconds) for parsing the ISO8601 timestamps in `segments.log` — it silently truncates fractional seconds and mishandles timezone offsets. Instead, for each timestamp, normalize the format (handle both `Z` suffix and `+HH:MM`/`-HH:MM` offsets, and missing/short fractional-second digits) and compute an exact fractional-second offset relative to the fixed epoch `2023-10-27T00:00:00Z`. `python3` with `datetime.fromisoformat` (after normalizing `Z` to `+00:00` and padding fractional seconds) or `date -d ... +%s.%N` combined with `bc` for the subtraction are both viable — just avoid losing sub-second precision anywhere in the pipeline. For extraction, instead of looping and invoking `ffmpeg` once per segment (slow, reloads the whole file each time), build a single `ffmpeg` filter_complex/concat command (or a filter script) that seeks with precise `-ss`/`-t` float values and outputs all segments in one pass, then concatenates them into `/home/user/final_output.wav`. Test your rewritten script end-to-end and sanity-check a couple of segment boundaries against the log before finishing.