Start by OCR'ing the runbook image to recover the exact configuration values you need — run `tesseract /app/runbook_spec.png stdout` (or output to a file and read it), then carefully transcribe the `CRON_SCHEDULE`, `ENV_PATH`, and `BACKUP_DIR` values exactly as OCR reports them (double-check ambiguous characters like `/`, `*`, `:` since OCR often mangles these in monospace-ish rendered text — re-run with different `--psm` page-segmentation modes if the first pass looks garbled). For the Go program, use `archive/tar` + `compress/gzip`, and critically construct the gzip writer with `gzip.NewWriterLevel(w, gzip.BestCompression)` (not the default `gzip.NewWriter`, which uses default compression and will produce a much larger file that fails the strict size threshold) — write each `.log` file found via `filepath.Glob(filepath.Join(activeLogs, "*.log"))` as a tar entry, close tar then gzip writers in the correct order before deleting source logs, and finally append the health-check line. In deploy.sh, compile to the required binary path, `mkdir -p` all target/backup directories, and assemble the crontab file using exactly the OCR'd values in the `PATH=...\n<schedule> VAR=... VAR=... /path/to/binary` format specified.
