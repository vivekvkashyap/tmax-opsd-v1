Start by reverse-engineering the schema: run `sqlite3 /home/user/network.db ".schema"` to see the actual vertices/edges table and column names before writing any query — don't assume names, confirm them. Then implement path-finding with a recursive CTE in SQLite: build a `WITH RECURSIVE` query that starts from the vertex matching `--start`, joins through the edges table accumulating the node path (e.g. as a comma-delimited or JSON path string) and a running weight sum, with a termination condition on hop count (`WHERE hops < :max_hops`) to prevent infinite recursion on cycles, and a filter for reaching `--end`. Among all qualifying paths, pick the one with minimum total weight (`ORDER BY total_weight ASC LIMIT 1` in an outer query, or compare in Python after fetching all candidates). Use parameterized queries (`?` placeholders) for `--start`/`--end`/`--max-hops`, not string interpolation, to avoid SQL injection. Handle the no-path-found case explicitly by emitting `{"path": [], "total_weight": null}`. Wrap the connection in a context manager or explicit `close()` in a `finally` block. Test with the exact CLI invocation given in the task before considering it done, and leave the resulting JSON file on disk.