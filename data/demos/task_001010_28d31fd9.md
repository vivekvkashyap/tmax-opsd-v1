Write straightforward C++: open `/home/user/raw_embeddings.csv`, skip the header line, and for each row parse the 8 float columns (v0..v7), ignoring the id. Compute the L2 norm with `sqrt(sum of squares)` using `double` precision internally for accuracy, then divide each component by the norm (guard the norm>0 case) before casting each normalized component down to `float` (32-bit) for output — accumulate stats (sum and sum-of-squares of normalized v0) in double precision too, since single-precision accumulation over 10,000 rows will drift and fail the 5-decimal-place tolerance check. Write the normalized vectors as raw binary using `ofstream` with `write()` on the 32-bit float buffer, in row-major order with no header/id/delimiters — verify with `xxd` or `od` that the file is exactly `10000*8*4` bytes. For the confidence interval, use Bessel's-corrected sample stddev (divide by N-1), and apply the exact formula `mean ± 1.96*stddev/sqrt(N)` given in the prompt. Print `Mean:`, `CI_Lower:`, `CI_Upper:` each on their own line formatted to exactly 6 decimal places (use `std::fixed` and `std::setprecision(6)`) to `/home/user/stats.txt`. Compile with the exact given g++ command and run it before considering the task complete.