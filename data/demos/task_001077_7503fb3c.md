Work this as three independent pipelines that feed a final merge. (1) Video: use `ffmpeg` to extract frames (or seek near likely trouble points) from `/app/symlink_crash.mp4`, then OCR each candidate frame (e.g. with `pytesseract`) looking for a fatal error line naming an archive as `backup_[ID].tar.gz` — scan around timestamps where the terminal output changes abruptly (red/error-colored text) rather than every frame, and record the numeric ID you recover. (2) Integrity: for every `.tar.gz` in `/app/backups/`, attempt `tarfile.open(...).getmembers()` inside try/except to detect corruption (truncated gzip/tar errors); discard corrupted archives AND discard the one matching the ID from step 1 without extracting it (it contains a symlink bomb — do not `tar -x` it directly, as a naive full-extract can traverse an infinite loop). (3) For remaining valid archives, extract the single member using streaming/`mmap` reads (files are padded large), detect/try both `Shift_JIS` and `cp1252` decoding for filename and content, transcode to UTF-8, and keep only lines starting with `CONFIG_SET: ` (strip the prefix). Rename outputs to `config_<archive_id>.txt` in `/app/recovered_configs/`, then aggregate into `/app/recovery_report.json` as `{archive_id: [line, ...]}`.