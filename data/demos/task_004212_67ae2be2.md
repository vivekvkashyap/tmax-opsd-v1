Diagnose the install failure before touching logic: run `pip install .` inside `/home/user/netprofiler`, read the actual pip error, and check `setup.py`'s `install_requires` for a misspelled dependency name versus what's actually imported in the package's source (`import dpkt` vs whatever string is listed) — fix the spec, not the import. Next open `netprofiler/metrics.py` and work out the jitter formula by hand on paper: inter-arrival times are consecutive timestamp differences, and jitter is the mean of the *absolute* differences between consecutive inter-arrival times — check whether the current code applies `abs()` at the right step, since a subtly wrong formula (e.g. missing abs, or measuring the wrong pair of differences) will pass small sanity checks but fail the real numbers. Reinstall the package after fixing, then write `run_analysis.py` that imports the fixed package, parses `/home/user/capture.pcap` (using `dpkt` to read packet timestamps and source IPs), tallies per-IP packet counts to find the top talker, computes total packets and mean jitter, and writes the three-field JSON to `/home/user/report.json` with the float formatted to six decimal places as shown.
