Write the C++ extractor to parse the custom binary format sequentially: verify the 4-byte `DOCP` magic, then loop reading a little-endian uint16 path length, that many raw ASCII bytes for the path, a little-endian uint32 content length, then that many content bytes — repeat until EOF. Use `std::ifstream` in binary mode with `.read()`, and be careful with endianness when reinterpreting the length bytes (don't assume host byte order matches; read raw bytes and assemble the integer manually or use explicit little-endian reconstruction). The critical security check: before extracting, test whether the path contains the literal substring `../` anywhere (not just at the start) — if so, skip that entry silently and continue parsing the rest of the archive. For valid entries, use `std::filesystem::create_directories` on the parent path before writing the file under /home/user/docs_safe/. After extraction, do the placeholder replacement across only `.md` files with `find /home/user/docs_safe -name '*.md' -exec sed -i 's/\[COMPANY_NAME\]/AcmeCorp/g' {} +`. Finally build the manifest by listing retained files relative to docs_safe, sorting alphabetically, and writing one path per line to /home/user/manifest.txt.