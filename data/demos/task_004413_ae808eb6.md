This is a three-service pipeline: fix configuration, then implement a stateful C++ filter. First, inspect `/home/user/app/flask_api/.env` (or equivalent env vars) for the Redis host/port the Flask app uses — correct them to point at your actual local Redis instance (127.0.0.1:6379) and confirm with a manual `redis-cli` push/read that POSTing to the Flask endpoint lands items in the `etl:input` list. Second, write the C++ worker to loop on `BLPOP etl:input 0` (via hiredis or a Redis C++ client), parse each JSON payload (nlohmann::json is a good fit), and apply validation in order: reject if `amount` missing/null/non-numeric; impute `user_id` to `"UNKNOWN"` if absent. Maintain a rolling deque/vector of the last 50 accepted `amount` values; once you have history, bootstrap-resample it (with replacement, many resamples) to build an empirical distribution and reject new values falling far outside it (e.g. beyond ~3 std devs of the bootstrap mean distribution) — for the first 50 valid transactions, auto-accept regardless of amount. Append accepted/rejected JSON lines to the two output files, creating `/home/user/etl_output/` first. Finally write `start_all.sh` to launch redis-server, the Flask app, and your compiled worker binary all backgrounded with `&`/`nohup`, and test end-to-end with a few curl POSTs before considering it done, since it will be graded against a large mixed clean/evil corpus requiring 100% correct routing.