Build this as a deterministic simulation — the verifier replays the same RNG sequence, so any ordering deviation fails. Steps: (1) parse the adjlist into an adjacency map of int slices; (2) for each of 10,000 walks starting at node 0, at every step sort that node's neighbor slice ascending before calling `walkRand.Intn(len(neighbors))` — resort on every lookup, don't cache, since map iteration order is non-deterministic; (3) stop a walk on return to 0 or 100 steps, counting the start as step 0; (4) compute the histogram mode (ties → smallest length) and sample mean; (5) for the bootstrap, use a second RNG seeded 123, and per resample draw 10,000 indices via `bootRand.Intn(10000)` into the *original* walk-length array (never into a previous resample), average, collect 10,000 means, sort, and take indices 249/9749 for the 95% CI. Round all outputs to 4 decimals and write valid JSON. Verify the walk-length distribution looks sane before adding the bootstrap layer.