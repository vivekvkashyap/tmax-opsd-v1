Fix the shared-library build first: a `.so` built from an object file compiled without position-independent code will fail or misbehave at link/load time, so add `-fPIC` to the `bayes.o` compilation rule in the Makefile, then `make` and confirm `libbayes.so` exists and exports `compute_posterior_mean` (e.g. `nm -D libbayes.so | grep compute_posterior`). Write `tracker.cpp` as a standard POSIX TCP server: `socket`/`bind`/`listen`/`accept` loop on 127.0.0.1:9000, and per connection, read the full payload, split on commas, parse to `double`s, call the linked `compute_posterior_mean(data, size)`, and respond `POSITIVE\n` if the result is strictly greater than 0.0 else `NEGATIVE\n`. Also append a line to `/home/user/experiment_log.txt` each time, formatted exactly `[Data size: N] Posterior Mean: M, Result: R` with M rounded to 4 decimals. Compile linking against `-L/app/bayes_infer -lbayes` (or the built .so path) with the include dir for `bayes.h`, and run the server with `LD_LIBRARY_PATH` including the directory containing `libbayes.so` so dynamic loading succeeds. Test manually with `nc` or a small Python socket script before leaving it running in the background.