Work this as three independently verifiable stages chained together. First, parse `/home/user/server.log` with a regex over each line's request line/query string to find `POST /upload` entries whose `filename` parameter contains `../`, and note the two resolved target paths under `/home/user/compromised_data/` (a cert file and an encrypted payload) — don't hardcode filenames, derive them from the log. Second, validate the certificate chain: use the `openssl` crate or shell out to `openssl verify -CAfile rootCA.pem <cert>` from your Rust code, and only proceed if verification succeeds. Third, extract the certificate's Subject CN (e.g. via `openssl x509 -noout -subject` or an X.509 parsing crate) — this CN is the hex-encoded 32-byte AES key; convert it from hex to raw bytes, don't use the hex string itself as the key. Then decrypt: split the payload file into IV (first 12 bytes), ciphertext (middle), and GCM tag (last 16 bytes), and call AES-256-GCM decryption (`aes-gcm` crate) with those three pieces plus the key. Write the resulting plaintext bytes as UTF-8 (trimmed) to `/home/user/decrypted_evidence.txt`. Build everything under `/home/user/forensics/` with a `Cargo.toml` declaring the crates you use.
