Fix things in dependency order: build the C library first (`make` in the build_pipeline directory to produce `libtelemetry.so`), otherwise the FFI wrapper has nothing to link against. Next fix the import-order bug in `main.py` — since `ffi_wrapper.py` raises at import time unless `PIPELINE_INIT` is already set, and `config.py` is what sets it, reorder `main.py`'s imports so `config` is imported before `ffi_wrapper`. Then implement `get_telemetry()` in `ffi_wrapper.py` with `ctypes`: load the shared library via `ctypes.CDLL(path)`, declare `process_telemetry`'s argtypes as `(c_char_p, c_char_p)`, allocate an output buffer with `ctypes.create_string_buffer(256)`, call the function with `b"BUILD_OK"` and the buffer, then read the buffer's `.value`, decode it, and `json.loads` it into a dict to return. Start `main.py` in the background so it binds port 8000, confirm it's actually listening (e.g. `curl -s localhost:8000/telemetry`), then `curl` the `/telemetry` endpoint and redirect/save the raw response body to `final_output.json`.
