Load `transfers` into memory as a directed edge set (sender_id, receiver_id) using `sqlite3` from Python, then find length-4 cycles via either a 4-way self-join in SQL (`transfers t1 JOIN t2 ON t1.receiver_id=t2.sender_id JOIN t3 ON t2.receiver_id=t3.sender_id JOIN t4 ON t3.receiver_id=t4.sender_id AND t4.receiver_id=t1.sender_id`) or a plain Python DFS of depth 4 that closes back to the start node — either way, explicitly require all four node ids to be pairwise distinct (reject rotations that revisit a node) and dedupe rotations/reflections of the same cycle (A-B-C-D-A is the same cycle regardless of which node you start the join from), e.g. by canonicalizing each found cycle as its 4 node ids sorted ascending before dedup. Write one line per unique cycle as `id1,id2,id3,id4` (ascending within the line), sort the lines themselves numerically (not lexically — compare the numeric ids in order), and write to `/home/user/cycles.log`. Separately, `optimize.sql` just needs a single composite `CREATE INDEX idx_transfers_sender_receiver ON transfers(sender_id, receiver_id);` to support the join pattern.
