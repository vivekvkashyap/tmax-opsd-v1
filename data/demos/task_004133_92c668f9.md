Treat this as three linked forensic sub-tasks, in order. First, mount/analyze `/app/evidence.img` as a loopback filesystem (`mount -o loop`) and use `debugfs -R 'lsdel'` or `fls`/`icat` from The Sleuth Kit to locate and recover the deleted file's inode content — this reveals the exploit's hex signature, which you must actually recover from the image rather than assume. Cross-reference timestamps in `/var/log/app_services/*` for `log_receiver`, `db_writer`, and `health_monitor` to understand the request flow and confirm the crash pattern. Second, write a small Bash loop that sends crafted payloads (prefix/padding variations wrapping the recovered signature) to port 9001 via `nc`, then poll `health_monitor`'s port/logs to detect when `log_receiver` restarts — this pins down the exact trigger pattern (e.g. a prefix keyword plus the signature substring). Third, build `/home/user/filter.sh` that greps/matches for that trigger pattern in the input file and exits 1 if found, else 0; validate it against every file in both `/app/corpus/clean/` and `/app/corpus/evil/` with a loop checking exit codes, iterating on your pattern until you hit 100% accuracy on both corpora — don't stop at "looks right," actually run the full corpus check.