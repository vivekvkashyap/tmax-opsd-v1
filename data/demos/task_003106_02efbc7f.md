Treat this as fixing a join, not a filter. The bug is that the code matches any two same-institution authors who each wrote *some* paper, without requiring it to be the *same* paper — effectively a cross join instead of an equi-join on paper. Restructure the C logic so you iterate authors pairwise (A1 < A2 lexicographically via `strcmp`), and for each pair require: both are `AFFILIATED_WITH` the identical institution I, AND there exists some paper P such that A1 `WROTE` P and A2 `WROTE` P (loop over all papers, not just the first found by each). Collect qualifying tuples into an array of structs, then sort with `qsort` using a comparator that orders by P, then I, then A1, then A2 (all via `strcmp`, in that priority). After sorting, only emit the first 3 entries (limit/offset logic — just truncate the loop or array). Write output as strict JSON using `fprintf` with exact key names `"A1"`, `"A2"`, `"P"`, `"I"`, escaping the array properly with commas/brackets. Compile with `gcc find_pattern.c -o find_pattern`, run it, redirect/write to `/home/user/output.json`, and validate the result is parseable with `jq`.