Since `/app/loc_masker` is stripped, don't try to disassemble it — treat it as a black box and reverse-engineer its I/O behavior empirically: feed it a wide variety of hand-crafted JSONL lines (plain ASCII emails, multiple emails per field, IPv4 addresses at edge octets, non-normalized Unicode like full-width or combining characters, emoji, missing fields) via stdin and diff the exact stdout bytes against your hypotheses about IP masking, email redaction, and Unicode normalization. Pay special attention to subtle formatting details a naive reimplementation misses: exact JSON key ordering (likely alphabetical), presence/absence of whitespace after `:`/`,`, and whether Unicode normalization (try NFC vs NFKC) is applied before or after regex matching. Once your Python script at `/home/user/fast_masker.py` matches on your hand test set, script an automated equivalence check: generate many random JSONL lines, pipe the same input through both `/app/loc_masker` and your script, and `diff` the outputs to catch discrepancies before considering it done. Also write `/home/user/parallel_process.sh` (split file, run chunks in parallel via your Python script, merge stdout in original order) and `/home/user/loc_sync.cron` with a valid `0 2 * * *` entry invoking it.