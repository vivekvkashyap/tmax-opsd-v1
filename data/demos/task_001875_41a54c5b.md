Diagnose the undefined-symbol failure as a missing link dependency: rebuild libgraph.so with an explicit link against libmatrix.so, e.g. `gcc -shared -fPIC -o libgraph.so graph.c -L. -lmatrix`, and update the Makefile target accordingly. At runtime ctypes.CDLL still needs libmatrix.so discoverable — either set LD_LIBRARY_PATH to include the directory before loading, or load libmatrix.so first with RTLD_GLOBAL mode so its symbols are visible when libgraph.so loads. For solver.py, implement a small state machine over graph_def.txt: track the current node id after each `NODE` line, append each `DEPENDS` value as an edge for that node, and reset at `END`; derive num_nodes as one plus the maximum node id you encounter anywhere in the file. Set ctypes argtypes/restype on init_graph, add_edge, and traverse_and_count to match graph.h's int signatures exactly, call them in the order specified, start traversal from node 0, and write the single integer result to output.log.
