Locate the two multiplicative-update loops in `/home/user/src/nmf_solver.cpp` (the H update using `W_T_V[k][j] / W_T_W_H[k][j]` and the W update using `V_H_T[i][k] / W_H_H_T[i][k]`) — these divide by a value that can be near zero for near-singular/duplicate-row input, producing NaN/Inf. Fix by adding `1e-9` to each denominator at the point of division, e.g. `H[k][j] *= W_T_V[k][j] / (W_T_W_H[k][j] + 1e-9);` and the analogous change for W — don't clamp/floor the numerator or add the epsilon somewhere that doesn't actually stabilize the division. Compile with `g++ -std=c++11 -O3 -o /home/user/nmf_solver nmf_solver.cpp`, then run it with the specified args to produce `W.txt`/`H.txt`, and sanity-check the outputs contain no `nan`/`inf` (e.g. `grep -i nan`/`inf`). For `evaluate.py`, load V, W, H with `numpy.loadtxt`, compute `V_approx = W @ H`, flatten both, normalize each to sum to 1 (add a tiny epsilon before dividing to avoid zero-division there too), then compute `sum(P * log(P/Q))` only over entries where P>0 (skip/guard zero-P terms to avoid `0*log(0)` NaNs), and write the scalar result to `/home/user/kl_divergence.txt`.