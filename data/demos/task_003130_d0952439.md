Isolate the bug first: the recursive-descent parser evaluates left-to-right regardless of operator, so `2+3*4` becomes `(2+3)*4`. To respect precedence with only single digits, `+`, and `*` (no parens), restructure the parser into two levels — a `parse_term` that consumes a run of digits joined by `*` and returns their product, and a `parse_expr` that consumes terms joined by `+` and sums them; `parse_expr` should call `parse_term` for each operand instead of directly reading one digit, so multiplication binds tighter. Recompile with `gcc` to the exact output path. For the property-based test script, generate random single-digit/`+`/`*` expressions with `$RANDOM`, feed each as `START END <expr>\n` into your compiled binary via stdin, and compare its printed shortest-path (which for a single edge equals the expression's value) against `echo "<expr>" | bc`; fail loudly and exit nonzero on any mismatch across at least 50 trials. Only after the property tests pass should you run the fixed binary against `prod_graph.txt` and write the resulting integer shortest-path from `START` to `END` to `/home/user/qa_report.txt` — compute it by tracing all paths through the graph's edges with correctly-fixed precedence, don't hand-pick a plausible-looking number.
