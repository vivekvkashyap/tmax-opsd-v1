Use `ffmpeg -i /app/config_session.mp4 -vf fps=1 frame_%03d.png` (or similar) to sample frames, then run `tesseract` OCR on each frame to recover the displayed text; dedupe consecutive frames that show the same file and stitch OCR lines back into complete config file contents, noting each file's shown filename (e.g. a comment or terminal prompt indicating the path). Reconstruct each config text carefully — OCR errors on IPs and keywords will break both the sanitizer test and archive contents, so sanity-check recovered text against plausible config syntax (sshd_config, nginx.conf, json, yaml, sysctl.conf style). For `/home/user/config_sanitizer.py`, read all of stdin, then check line-by-line/substring for the exact evil markers: `PermitRootLogin yes`, `PasswordAuthentication yes`, `ListenAddress 0.0.0.0`, and use a regex to find IPv4 addresses and verify each is inside 10.0.0.0/8, 192.168.0.0/16, or equals 127.0.0.1 (reject if any address falls outside, including any address embedded in URLs/config values). Exit 1 on first violation, else exit 0. Test the script against your own extracted files before archiving, then `tar czf` only the ones your script marks clean into `/home/user/clean_configs.tar.gz`, named by their in-video filenames.
