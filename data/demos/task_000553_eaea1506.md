Start by writing a small script (Rust or shell) that just walks `/home/user/app_logs/` recursively and prints each `.dat` file's header bytes so you can confirm the binary layout before writing the full pipeline — this catches endianness/offset mistakes early. Parse the fixed 10-byte header per spec (magic at 0-3, version at 4, status at byte 5, little-endian u32 length at bytes 6-9), and skip any file whose status byte is `\x01` entirely — do not read its payload at all, since it may still be being written and its declared length could be stale or wrong. For finished files, read exactly `length` bytes as UTF-8 text, then filter it line-by-line: drop lines whose content starts with the literal `DEBUG: ` prefix, and use a regex to find and replace IPv4-looking dotted-quad sequences with `[REDACTED]` (be careful your regex doesn't also mangle non-IP dotted numbers). Track the relative path of each file starting inside `app_logs` (not absolute, not including `app_logs` itself) for the `"file"` key. Build up a Rust `Vec` of structs and serialize with `serde_json` to `/home/user/cleaned_logs.json` as a compact array, preserving remaining newlines inside each `"contents"` string. Test against the actual files present rather than assuming file names.
