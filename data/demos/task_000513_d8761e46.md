Build this in three independent, testable stages rather than one monolithic script. First write `tokenizer.c`: process the input byte-by-byte with a small state machine — lowercase A-Z, map anything outside `[a-z0-9 ]` to a space, then collapse runs of spaces to one and trim leading/trailing spaces in a final pass (don't try to do trimming inline while scanning; it's easier to build the cleaned buffer first, then trim). Compile with `gcc -O2` and test on the small file until the output matches the transformation rules exactly (whitespace-for-whitespace, no trailing newline surprises). Second, write `benchmark.sh` using a loop of exactly 30 iterations, timing each run with something sub-second-precise (e.g. `date +%s%N` before/after, or `/usr/bin/time`), converting to milliseconds, and appending one integer per line to times.txt — redirect tokenizer stdout to /dev/null per run. Third, write `stats.c` to read the 30 integers, compute mean, sample stddev (divide by N-1), and the 95% CI via `mean ± 1.96*stddev/sqrt(N)`, printing with `%.2f` in the exact required format. Verify each phase's output against the spec before moving to the next.
