Structure the Go program around `filepath.WalkDir` for traversal, then per matched file branch on extension. For `.gz` files, wrap the opened `*os.File` in a `gzip.NewReader` and read the decompressed stream directly into memory (e.g. `io.ReadAll`) — never shell out to `gunzip` or write a temp file. To detect publish status, split the decompressed (or plain-text) content into lines and check with `strings.HasPrefix(line, "Status: Publish")` for exact, case-sensitive match on the first matching line, not a substring search anywhere in the file. Build the archive name by taking the path relative to `/home/user/docs_raw` (`filepath.Rel`), then trimming a trailing `.gz` with `strings.TrimSuffix` so `.md.gz`/`.txt.gz` become `.md`/`.txt` in the tar entry. Write entries with `tar.Header{Name: relPath, Mode: 0644, Size: int64(len(content))}` followed by the decompressed bytes, all wrapped in a `gzip.Writer` feeding `/home/user/published_docs.tar.gz`. Remember to `Close()` the tar writer before the gzip writer so trailing bytes flush correctly, and skip files that don't match the four required suffixes.