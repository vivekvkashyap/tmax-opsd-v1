Treat the binary as an unknown function and determine its algorithm purely through black-box experiments before writing any replication code. Build small hand-crafted graph files: a target with no outgoing edges (expect a base case value), a simple chain A->B->C, a diamond (A->B->C and A->D->C, i.e. two paths of different or equal length to the same descendant), and a node with multiple branches of varying depth — run `/app/impact_calculator` on each and record the outputs to infer whether it computes longest path, shortest path, path count, or sum of depths, and how it aggregates multiple paths/branches. Once the rule is confirmed (test the diamond and branching cases carefully, since these distinguish "longest path to any leaf" from other candidate metrics), implement it in bash: parse the `TARGET:` line, load all edges into parallel arrays or an associative structure keyed by source node, then write a recursive bash function (or an iterative memoized DFS using arrays, since bash recursion works but is slow for large graphs) that computes depth as 1 + max(depth of children), with leaf/no-outgoing-edges nodes returning 0. Validate against the binary on dozens of your own random acyclic graphs, including ones with many nodes, before trusting the fuzz test.
