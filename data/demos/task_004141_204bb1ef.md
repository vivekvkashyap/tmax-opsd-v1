This is a two-part task: a correctness deliverable (video events) and a performance deliverable (script must beat the original by a measured speedup, graded via `original_time / optimized_time >= 3.0`). For part 1, use `cv2` (OpenCV) to read `/app/traffic_video.mp4` frame by frame, group frames into consecutive 30-frame segments, compute `cv2.absdiff` between consecutive frames within each segment and average the pixel difference, then flag segments exceeding 5.0 as `high_traffic`, writing one JSON object per line to `/home/user/video_events.jsonl`. For part 2, first read `/home/user/query_pipeline.py` end-to-end to understand exactly what it filters/computes/outputs (its printed JSON of centralities must match) — do not change the semantics. Then profile it to find the real bottleneck (likely O(N^2) nested-loop filtering and repeated graph rebuilding over ~100k trajectory lines) and replace the slow parts: load records once, use vectorized/indexed filtering (pandas or an in-memory SQLite table with an index on filter columns) instead of nested Python loops, build the `networkx` graph exactly once, and call `betweenness_centrality` a single time. Save the optimized version to `/home/user/query_pipeline_optimized.py` and time both scripts yourself with `time python3 ...` to confirm you clear the 3x bar before finishing.