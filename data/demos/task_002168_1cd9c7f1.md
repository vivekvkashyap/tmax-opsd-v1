Work through this as a pipeline you validate stage by stage. First transcribe `/app/reference.wav` with a speech-recognition library (e.g. `SpeechRecognition` + a recognizer backend, converting to the right format with `pydub`/`ffmpeg` if needed) — print the transcript and sanity-check it reads as coherent English before trusting it downstream. Load `all-MiniLM-L6-v2` once via `sentence-transformers` and embed the reference transcript to get vector `r`. For any document embedding `v`, compute the orthogonal rejection as `v - (dot(v,r)/dot(r,r)) * r` (project out the component along `r`, keep only what's orthogonal) — do the projection math yourself rather than trusting a library shortcut, since sign/normalization errors here silently break separability. Compute `||v_ortho||` for every file in `/home/user/train_clean/` and `/home/user/train_evil/`, print both lists sorted, and pick a threshold τ strictly between the max clean norm and min evil norm (midpoint is safest). Bake that τ into `/home/user/detector.py`, which embeds the input file the same way and prints exactly `CLEAN` or `EVIL` based on the `<= τ` rule. Verify on the training files themselves before assuming it generalizes.
