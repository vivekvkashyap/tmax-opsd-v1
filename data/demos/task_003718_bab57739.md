Start Redis (`redis-server --daemonize yes` or via service manager) and confirm it's up with `redis-cli ping` before launching the Flask app. Export `REDIS_URL=redis://localhost:6379/0` and run `/app/data_api.py` in the background (`python3 /app/data_api.py &`), then `curl` `/generate` followed by `/data`, saving the JSON body to `/home/user/raw_data.json`. For `estimate.py`: implement Metropolis-Hastings by hand — propose new `mu` from a Gaussian random-walk step around the current value, compute the log-posterior as log-prior (Normal(0,10^2)) plus sum of log-likelihoods under the mixture density `0.7*N(x|mu,2.0) + 0.3*Uniform(-15,15)` for each data point, accept/reject via the standard MH ratio, and run enough iterations (e.g. tens of thousands) with a sensible proposal step size to get good mixing. Discard an initial burn-in fraction (e.g. first 20-30%) before using the trace. Compute the posterior mean as the trace's average post-burn-in. For the bootstrap CI, resample the post-burn-in trace with replacement at least 1000 times, take the mean of each resample, and report the 2.5th/97.5th percentiles as `ci_lower`/`ci_upper`. Write a small `run_pipeline.sh` that chains the curl calls and `python3 estimate.py`, and dump the three required keys to `/home/user/final_metrics.json`.