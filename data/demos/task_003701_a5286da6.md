Work the two phases independently, then wire them together. For Phase 1, first read `/app/oracle.py` to pin down its exact semantics (integer parsing, evaluation order, 32-bit overflow/truncation behavior, output formatting including newline or lack thereof) rather than guessing — a naive RPN evaluator often differs on integer overflow handling or on trailing whitespace/newline in the printed output, which breaks byte-for-byte matching. Implement a simple stack-based evaluator in C++: split the argument on spaces, push numbers, and on encountering `+`/`-`/`*` pop two operands and push the result, printing the final stack value with no trailing content the oracle doesn't emit. Compile with g++ to the exact path `/home/user/fasteval`. Test it directly against `/app/oracle.py` on several hand-picked and randomly generated valid RPN strings before touching the services. For Phase 2, edit `/home/user/nginx.conf` to add a `location /api/eval` block proxying to the Flask app's port, edit `/home/user/app.py` to invoke your compiled binary via subprocess instead of the legacy tool, and fix the Redis client's port. Restart all three services via the startup script and verify the full curl round trip end-to-end, including that a repeated request hits the cache.
