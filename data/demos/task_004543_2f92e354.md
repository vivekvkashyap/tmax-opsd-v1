Everything must happen inside the bash script, not manually — test each shell command interactively first, then assemble it into the script. Step 1: `mkdir -p` the lib dir and compile the C shared object with `gcc -shared -fPIC -o /home/user/project/lib/libmathops.so /home/user/project/c_src/ops.c`. Step 2: fix the borrow-checker error programmatically — read `main.rs` closely to see that `expr` is moved into `s` and then `&expr` is still used afterward, and use `sed` to change the code so the borrow happens before/without the move (e.g. drop the dead `let s = expr;` line or rebind the later use to a variable that's still valid) — verify locally with `rustc` that it compiles before finalizing the `sed` invocation. Step 3: link with `rustc -L /home/user/project/lib -lmathops -C link-args="-Wl,-rpath,/home/user/project/lib" -o /home/user/project/calc_app main.rs` so the binary finds the `.so` at runtime without touching `LD_LIBRARY_PATH` globally. Step 4: run `calc_app > /home/user/result.txt`, then `chmod +x` the script and re-run it end to end to confirm idempotence.
