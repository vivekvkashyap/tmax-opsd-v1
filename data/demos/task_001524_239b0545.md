Fix compilation before touching logic, then fix logic by comparing actual vs expected byte-for-byte. For the Makefile: this is C++ source (uses `<iostream>`), so a rule invoking `gcc` on a `.cpp` file will fail to link against the C++ standard library — change the compiler to `g++` (which also auto-links libstdc++); also check flag/argument ordering, since on modern GCC/binutils, linker flags like `-lpcap` must come AFTER the object/source files, not before, or symbols won't resolve. Fix both and confirm `make` succeeds. For the logic bug: run `./parser capture.pcap > current_output.txt` and `diff current_output.txt expected_output.txt` to see exactly what's wrong (e.g. extra leading bytes). Recall standard packet layout: Ethernet header (14 bytes) + IP header (variable, `ip_hl*4` bytes) + UDP header (fixed 8 bytes: src port, dst port, length, checksum) + payload. If the code computes a payload pointer but forgets to skip past the 8-byte UDP header, output will be corrupted with header bytes prefixed — locate that pointer arithmetic in `main.cpp` and add the missing offset, keeping the length calculation consistent. Recompile, rerun, re-diff until output matches exactly, then save it to `final_output.txt`.