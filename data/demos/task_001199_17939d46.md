Break this into three independent pieces and verify each in isolation before wiring them together. First, edit `config.ini` with a plain text substitution (change the port value under `[server]`), then confirm with `grep`. Second, for the interactive PIN prompt, write an `expect` script: `spawn python3 /path/to/aggregator.py`, `expect "Enter PIN: "`, `send "PIN\r"`, then `interact` (or `expect eof`) so the child process keeps running instead of the expect script exiting and killing it — a common mistake is forgetting `interact`, which lets the spawned process die when expect exits. Launch it backgrounded (`expect start_agg.exp &` or wrap in `nohup ... &`), then confirm with `ss -tlnp` or `netstat` that something is actually listening on the target port before moving on. Third, for the stdin-parsing script, use a strict regex anchored with `^...$` to match the exact line format, reject anything that doesn't fully match (including non-integer fields), then do integer arithmetic (not float) for the score, and print the correct verdict per line — silently skip malformed lines by simply not printing anything for them. Test manually by piping a few valid and invalid lines into the script before considering it done.