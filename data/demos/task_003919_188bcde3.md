Before writing any classifier code, empirically probe the embedding binary: for several descriptions in both /home/user/data/clean/ and /home/user/data/evil/, call `/app/model_exec "<description>"` and log the resulting 3-float vectors side by side for clean vs evil inputs. Look dimension-by-dimension for a discriminating signal (the task explicitly hints "look at the dimensions of the output vectors") — a sign flip, an unusual magnitude, or a value outside the normal range on one particular coordinate is the kind of pattern to search for, not a distance/clustering measure. Once you've identified which coordinate and what threshold/condition separates the two corpora, implement filter.cpp to: parse the CSV robustly (handle quoted fields), locate the "Description" column by header, invoke `/app/model_exec` per description via `popen`, parse the 3 floats from its stdout, and apply your discovered rule to every description in the file — if ANY description trips the evil condition, exit(1); only exit(0) if none do. Compile with `g++ -o /home/user/filter /home/user/filter.cpp`. Test against both local corpora before trusting it, since the hidden eval corpus will use the same underlying binary logic.