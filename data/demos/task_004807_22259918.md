Attack this as two independent correctness/perf fixes plus an integration check. Start services via the provided script/compose file and confirm Postgres and Redis are reachable before editing anything. For the SQL, note that listing multiple tables in the `FROM` clause with only unrelated `WHERE` filters (no explicit join predicate tying edges to nodes) produces an implicit cross join that multiplies rows — rewrite it as explicit `JOIN ... ON e.source_id = n1.id JOIN ... ON e.target_id = n2.id` with the `is_active` filters in the `WHERE` clause, and sanity-check the row count of `edges.csv` before/after to confirm the explosion is gone. For the C++ side, replace whatever O(V^3)-or-worse all-pairs approach exists with per-node BFS: for each source node run an O(V+E) BFS over an adjacency-list representation (not a dense matrix) to get shortest-path distances to all reachable nodes, then compute closeness centrality as `(reachable_count-1)/sum_of_distances` (0 if no reachable nodes), giving overall O(V*(V+E)). Build to `/home/user/app/analyze`, run the full pipeline (psql extraction -> analyze -> results.csv), and time the analyze step yourself against the <1.5s target on the actual edges.csv, plus spot-check a few centrality values by hand on a small subgraph to confirm correctness before finishing.
