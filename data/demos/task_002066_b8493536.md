The key constraint is streaming: never materialize the recombined `.tar.gz` on disk. Concatenate the three split parts on the fly and decompress+untar in one pipeline, e.g. `cat system_logs.tar.gz.00 system_logs.tar.gz.01 system_logs.tar.gz.02 | gunzip | tar -xf - ...` or, in Python, chain the part files' bytes into a single file-like object (concatenating `open(p,'rb').read()` streams or using a small generator/`io.BufferedReader` wrapper) and feed that directly into `tarfile.open(fileobj=..., mode='r|*')` — using streaming mode `r|` (not `r:`) since you can't seek a synthetic stream. Iterate tar members, filter to names ending in `.err.log`, and for each one extract its content, strip the trailing `.log` from the basename (ignore original directory components — flatten), and write it under `/home/user/extracted_errors/`. Create `/home/user/extracted_errors/` and `/home/user/important_errors/` first. For every extracted file, create a hard link (`os.link`, not a copy or symlink) at `/home/user/important_errors/CRITICAL_<name>`, and confirm afterward with `stat -c '%i'` that both paths share an inode. Verify no `.tar.gz` file ends up anywhere under `/home/user/` when you're done.