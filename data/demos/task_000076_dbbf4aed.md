Don't brute-force search by hand or guess — port the three constraints from `reference.py` directly and literally into a C function `int evaluate_constraints(int v1, int v2, int v3, int v4)` in `/home/user/pipeline/evaluate.c`, matching the exact arithmetic and returning 1/0. Compile it as a shared library with position-independent code, e.g. `gcc -shared -fPIC -o libevaluate.so evaluate.c`. In `solver.py`, load it with `ctypes.CDLL('./libevaluate.so')`, declare `argtypes`/`restype` as `ctypes.c_int`, then nest four loops over `v1..v4` each in `range(1, 51)` calling the C function and checking for a return of 1 — this exhaustive search over 50^4 combinations is fast in C via ctypes even though it would be slow in pure Python, which is the whole point of the exercise. When you find the (expected unique) satisfying tuple, write it as `v1,v2,v3,v4` to `/home/user/pipeline/solution.txt` with no extra whitespace, and verify by printing/logging the found tuple to confirm it independently satisfies all three original constraints before finishing.