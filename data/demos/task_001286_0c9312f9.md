First fix the vendored package before writing any pipeline code: open `/app/vendor/gocsv/gocsv.go` and `/app/vendor/gocsv/utils.go` (and check other .go files in that tree too) for an injected syntax error or a `panic(...)` call that was appended, often inside a spurious `init()` function at the end of a file — delete only the injected block, not legitimate code, then confirm the package builds standalone with `go build ./...` inside it. Then scaffold the Go module: `go mod init` in `/home/user/pipeline`, add a `replace github.com/gocarina/gocsv => /app/vendor/gocsv` directive plus a `require` line, and `go mod tidy`. For `validate`, stream/parse the CSV and reject a row the instant any field starts with `=`, `+`, `-`, `@`, contains a NUL byte, or exceeds 500 chars — test both exit codes against small hand-made clean/evil examples before trusting it. For `transform`, parse rows in original file order, maintain a per-`sensor_id` sliding window (e.g. a slice capped at 3, dropping oldest) to compute the rolling average, and preserve original row order in the output JSON rather than grouping by sensor. Write small local test CSVs to sanity-check both subcommands before considering the task done.