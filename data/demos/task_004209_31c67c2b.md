Work through this pipeline top-down before touching math. First get services up: start `redis-server` pointed at the provided `redis.conf` (note the port it declares), then start `api.py`. Run `fit_model.py` and read the actual connection error — check `config.yaml` for a mismatched Redis port/host vs what `redis.conf` actually binds, and fix the config rather than the code. Once fetching/caching succeeds, reproduce the `LinAlgError` and locate the exact line building the covariance matrix and calling `np.linalg.cholesky`. Insert regularization immediately before that call: add `1e-5 * np.eye(n)` to the covariance matrix so it becomes well-conditioned before decomposition — do this in-place on the real matrix, not on a copy that's discarded. After decomposition, extract the diagonal of `L` for each sequence, compute `scipy.stats.wasserstein_distance` against the loaded reference array, and pick the minimum. For the alignment step, implement Smith-Waterman from scratch with a scoring matrix using the given match/mismatch/gap parameters, traceback from the max-scoring cell, and slice a 20-bp window. Write all three fields to `results.json` in the exact schema requested.
