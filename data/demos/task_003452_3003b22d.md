Implement the rules in strict precedence order and test each independently before combining. Start with the salt: transcribe `/app/voicemail.wav` (e.g. with a local speech-to-text tool or by careful listening if audio playback is available) to recover the two-word phrase spoken after "the shared salt is" — use exactly what you recover, lowercase, space-separated, as the salt in your HMAC-less signature check `sha256(host + path + secret_salt)`; verify your script against a hand-computed cookie for a sample host/path before trusting it. Next implement query-param extraction with `urllib.parse.urlparse`/`parse_qs` so URL-encoding is handled correctly rather than naive string splitting. Check rule 2 (malicious substrings, case-insensitive) before rule 3 (open redirect), since precedence matters for the fuzz-graded oracle comparison. For rule 3, treat `//`-prefixed values and absolute `http(s)://` URLs whose parsed hostname differs from the JSON `host` as violations — use `urlparse(next_val).hostname` for the comparison, not raw string prefix matching. Make sure any parsing exception still results in printing exactly one of the four allowed outputs (never a traceback) to stdout, since the oracle is fuzzed with malformed/edge-case inputs too.
