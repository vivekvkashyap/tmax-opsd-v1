For the plotting bug: headless matplotlib fails silently when `plt.show()` is called instead of (or before) `plt.savefig()`, or when no non-interactive backend is set. Add `matplotlib.use('Agg')` before importing `pyplot` (or right after, before any figure is created), replace/remove the `plt.show()` call, and make sure `plt.savefig('experiment_plot.png')` runs and is followed by `plt.close()`. Verify the fix by checking the output PNG has nonzero size and reasonable dimensions.

For the oracle-replication task: never try to disassemble or trust `strings` output alone on a stripped binary — instead treat it as a black box and do black-box characterization. Run `/app/drift_oracle` on many crafted input pairs (e.g., identical arrays -> 0, single-element arrays, arrays with known small integers) and compare the printed numeric outputs against several candidate formulas (MAE, RMSE, MAPE, log-based error metrics) computed by hand or in a scratch script, picking whichever formula matches to 4 decimal places across many trials. Once you identify the formula, implement it in `/home/user/new_oracle.py`, parsing comma-separated integer strings, computing elementwise, and printing the result rounded to exactly 4 decimal places to match the binary's formatting exactly. Test your script against the real oracle on random arrays of varying length and value ranges before finishing.
