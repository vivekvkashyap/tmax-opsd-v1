For each `.gcode` file, read only the first 20 lines and use a regex/grep on lines matching `; PRINTER: ` and `; MATERIAL: ` to extract the values, defaulting to `Unknown` when a tag is absent — don't assume both tags exist or that they appear on fixed line numbers. Build target directory paths with `os.makedirs(..., exist_ok=True)` before creating each link, since `os.symlink`/`os.link` fail if parent directories are missing. For the `by_printer` view use `os.symlink(source, dest)` with an absolute (or correctly relative) path to the original file in `/home/user/raw_gcode/` — verify with `readlink` that the link target resolves correctly, since a common mistake is pointing the symlink at the wrong relative base. For the `by_material` view use `os.link(source, dest)` to create true hard links (verify same inode via `ls -i` or `stat`), not another symlink. After generating both views, actually invoke the `tree` command (via `subprocess`) against `/home/user/docs/` and redirect its stdout into `/home/user/docs_tree.txt` — do not hand-construct the tree output yourself.