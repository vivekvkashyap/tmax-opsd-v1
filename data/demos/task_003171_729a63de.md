Build this as a straightforward audio-RAG pipeline and trust the retrieval step, not manual guessing of answers. Install whisper (a small/base model is enough for accuracy at reasonable speed) and transcribe `/app/research_log.wav` to get the full text. Segment the transcript into sentences by splitting on '.', '?', '!' (or use nltk's sentence tokenizer), trimming whitespace/punctuation from each. Load `/app/search_queries.txt` line by line as the exact query strings (keep them verbatim since they become the JSON keys). Compute sentence embeddings for all transcript sentences and all queries using a sentence-transformers model, then for each query find the transcript sentence with highest cosine similarity — this is nearest-neighbor retrieval, not summarization, so return the single closest sentence verbatim rather than paraphrasing. Write `/home/user/results.json` as `{query_string: best_matching_sentence, ...}` for all queries. Since grading compares your retrieved sentences to hidden ground-truth segments via string similarity (threshold ~0.80), prioritize getting the transcription accurate and returning full, unedited sentences rather than truncated or reworded fragments. Organize the steps into a script or Makefile under `/home/user/pipeline/` and actually run it to produce the output file.