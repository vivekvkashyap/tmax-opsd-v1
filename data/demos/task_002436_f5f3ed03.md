Work the three phases in order and validate each before moving on. Phase 1: OCR `/app/attacker_screenshot.png` (e.g. `tesseract`) to recover the numeric initialization seed — read the extracted text carefully rather than assuming a value. Then `grep`/parse `/app/access.log` for `GET /login?next=http://evil-attacker.net/...exfil_data=<hex>` lines specifically (ignore normal traffic), extracting the hex ciphertext from each and noting the single source IP responsible for all of them. Phase 2: implement the LCG exactly as specified — `X_{n+1} = (1103515245 * X_n + 12345) mod 2^31`, reseeded to the OCR'd `X_0` at the start of each cookie, keystream byte = low 8 bits of each successive `X_{n+1}`, plaintext = ciphertext XOR keystream — in Rust, iterating byte-by-byte over the decoded hex. A common bug is off-by-one in when the LCG advances relative to XOR-ing the first byte, or reusing LCG state across cookies instead of resetting it; verify by manually decrypting one cookie by hand/script and checking the output looks like a plausible cookie string before trusting your tool at scale. Write results to `/home/user/recovered_cookies.txt` in log order. Phase 3: write the `iptables -A INPUT -s <attacker_ip> -p tcp -j DROP` command into `/home/user/block_attacker.sh` using the IP you identified from the log, not a guessed one.