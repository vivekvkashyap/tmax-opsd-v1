Do this in independently verifiable stages. First, transcribe /app/sysadmin_memo.wav (install and use an offline ASR tool such as `SpeechRecognition`+`pocketsphinx`, `vosk`, or `openai-whisper` if available) and print the raw transcript so you can visually confirm it before parsing — then extract the three values with simple keyword-anchored parsing (look for phrases around "maintenance mode", "max workers"/"workers", "target region"), converting the boolean and integer sensibly and normalizing the region string into the expected `xx-xxxx-n` shape. Second, load /app/initial_config.json, override just those three keys, and write the merged dict to /home/user/current_config.json, then append the audit line in the exact literal format specified (ISO 8601 timestamp via `datetime.utcnow().isoformat()` or similar). Third, build the HTTP server (Flask/http.server) holding config in memory (in sync with the file): GET /config returns it; POST /config must check the Authorization header before touching the body, validate max_workers (int, 1-100) and target_region (starts with eu- or us-) and return 400 on violation, else persist + log + 200. For /sample, read the CSV with pandas/csv, group by status_code, and for each group take min(len(group), max_workers) rows (use a fixed seed like `random.sample(..., k)` with a set seed for determinism) — return JSON keyed by status code string. Keep the server running in the background (e.g. nohup) since your task isn't done until it's live and responsive.
