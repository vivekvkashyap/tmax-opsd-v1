Read the CSV with `pd.read_csv('/home/user/raw_configs.csv', encoding='utf-16le')` — get this wrong and every downstream step silently breaks (garbled column names or a single-column parse). Use `pd.melt` with `id_vars=['Date']` to reshape into long format, then split each original column name on the first underscore into `Service` and `Parameter` (watch for parameter names that themselves contain underscores, e.g. `MemoryLimit` is fine but verify your split logic doesn't break on service/parameter names with multiple words). Convert `Date` to datetime, sort by `Service`, `Parameter`, `Date`, then use `.groupby(['Service','Parameter'])['Value'].transform(lambda s: s.rolling(3, min_periods=3).mean())` to compute `RollingAvg` per group in chronological order — do this before any filtering, since the rolling window must see all raw points. Drop rows where `RollingAvg` is NaN, then apply the two threshold rules (`Timeout` > 1000, `MemoryLimit` > 8192) only to matching Parameter rows. Round `RollingAvg` to 2 decimals, select and order the required columns, sort by Service/Parameter/Date, and write with `to_csv(..., index=False, encoding='utf-8')`. Double check numeric dtypes didn't get read as strings from the melt.