Transcribe `/app/lab_notes.wav` first (install/run a speech-to-text tool) and read off the two floating point parameters it states for Alpha and Beta precisely — don't approximate from context. Then numerically solve $x^3 - \text{Alpha}\,x^2 + \text{Beta}\,\ln(x) - 10 = 0$ for the root with $x>1$ using a root-finder (bisection or Newton's method in Python/scipy on `scipy.optimize.brentq`), and derive the GC-content threshold as `floor(x * 10)` — verify your root by plugging it back into the equation and checking it's near zero, since a wrong root silently produces a wrong, hard-to-debug threshold. In `/home/user/filter_primers.py`, parse each FASTA record's sequence, compute `(count(G)+count(C))/len(seq)*100`, and keep only sequences with GC% <= your derived threshold; use `multiprocessing.Pool` to map this over the files in `<input_dir>` in parallel, writing survivors to `<output_dir>` under identical filenames. Before trusting your threshold, sanity-test the script against the provided `/home/user/corpus/clean/` and `/home/user/corpus/evil/` directories and confirm 100% correct classification — if it doesn't, recheck the transcription and root-finding rather than the filter logic.
