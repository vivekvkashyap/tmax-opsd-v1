Start by transcribing `/app/voicemail.wav` (e.g. with `whisper`) and listen carefully — the business rule will state a latency filter threshold and a penalty condition tied to the target node's ID; write these two rules down precisely before touching code, since getting either the comparison operator (strict vs. inclusive) or which node (source vs. target) the modulus applies to wrong will silently corrupt every downstream shortest-path answer. In the C program: parse the CSV once at startup, apply the discovered filter (dropping edges outside the allowed latency) and the discovered penalty (adjusting weight based on the target node ID's divisibility) while building the adjacency list, then build an efficient index (e.g., array-of-lists keyed by node ID, sized to the max node ID seen) for O(1) lookups during traversal. For each `stdin` query line, run Dijkstra's with a binary heap or simple array-based priority selection for performance on large graphs, printing only the integer result (or `-1`) with no extra output on stdout. Test your rule-parsing against a handful of hand-computed small examples before running against the full `network.csv`, since the verifier will fuzz-test against many random query pairs and require exact equivalence to a hidden reference implementation of the same rules.
