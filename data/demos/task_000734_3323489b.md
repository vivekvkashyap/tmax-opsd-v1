Fix the Rust build before touching Python. In `Cargo.toml`, add a `[lib]` section with `crate-type = ["cdylib"]` so `cargo build --release` produces `libsorter.so`. In `lib.rs`, each exported function needs both `#[no_mangle]` and `pub extern "C" fn` (not just `pub fn`) so the symbol name is preserved and the calling convention matches C — also check for and fix any plain syntax errors that block compilation. Build with `cargo build --release` and confirm the `.so` exists via `file target/release/libsorter.so`. For the Python side: load the library with `ctypes.CDLL(...)`, and since the C signature is `void f(int32_t* data, size_t len)`, set `argtypes = [ctypes.POINTER(ctypes.c_int32), ctypes.c_size_t]` and `restype = None` — build the input arrays as `(ctypes.c_int32 * n)(*values)` so ctypes can pass a pointer. Seed `random.seed(42)` before generating the 50,000-int array, sort two independent copies with each function, and compare them elementwise for `verification.txt`. For the benchmark, reshuffle a fresh copy before each of the 5 timed runs per algorithm (don't re-time an already-sorted array), average with `time.perf_counter()`, and format to exactly 4 decimal places as specified.