Diagnose top-down: a 502 means Nginx can reach nothing on the upstream port, so first check what port the Go app actually listens on (`grep ListenAndServe main.go`) versus what `proxy_pass` in `nginx.conf` targets — fix both independently to agree on 9090 rather than just changing one side and assuming it matches. Build and background the Go binary (`go build -o /home/user/app/main /home/user/app/main.go && /home/user/app/main &`) and confirm it's actually listening (`curl 127.0.0.1:9090/health` directly) before touching Nginx. Then start Nginx with the given config file and re-test through the proxy. Create the repos symlink with `ln -s /home/user/repos /home/user/public_html/repos` (check direction/target with `readlink`, not just that a link exists). For logrotate, write a config block scoped to `/home/user/logs/webhook.log { daily rotate 5 compress missingok notifempty }` — get the option names exact since the check likely greps for these literal keywords. Finish by curling `/api/hook` through port 8080 and tailing the log file to confirm the write actually happened, not just a 200 status.
