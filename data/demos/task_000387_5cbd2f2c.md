Chain three distinct recovery techniques in sequence. First, bisect: write a minimal test script that runs the engine's failing behavior and returns nonzero on failure, then drive `git bisect start` / `bad main` / `good v1.0` / `git bisect run <script>` inside `/home/user/db_engine` to converge on the bad commit automatically rather than stepping through ~200 commits by hand — capture the hash with `git rev-parse`. Second, use `git log -p` / `git show` on commits right after the bad one (search diffs, not just final file state) to find where `RECOVERY_PASS` was briefly hardcoded before being removed. Third, read `db_read.sh` to learn the WAL entry encoding (likely base64 lines decoding to `[PASSWORD] KEY=VALUE`), decode `/home/user/data/prod.db.wal` line by line with `base64 -d`, discard entries whose password doesn't match (corrupted lines), and extract the `ADMIN_USER` value from the valid entry. Write the three results to `/home/user/report.txt` in the exact order, one per line, no extra text.
