Build the pipeline in stages and keep the tokenization rule identical across `prep.sh`'s two computations (top-N selection and per-row presence check) — a mismatch here is the most common bug. In `prep.sh`: read the `text` column, lowercase it and replace every non-alphanumeric run with a space (e.g. `tr '[:upper:]' '[:lower:]' | sed 's/[^a-z0-9]/ /g'`), tokenize on whitespace, count global word frequency across the whole dataset, and select the top `N` breaking ties alphabetically (sort by count descending, then word ascending — e.g. `sort -k2,2nr -k1,1`). Emit the CSV header as the chosen words plus `label`, then for each dataset row, cleaned the same way, mark 1/0 per word (word-boundary match against the row's token set, not a raw substring match) and append the label.

In `train.py`, load the CSV with pandas, fit `LogisticRegression(random_state=42)` on the word columns vs `label`, compute in-sample accuracy, and print just the float. In `track.sh`, loop `N` over 3, 5, 10, regenerate features via `prep.sh N`, run `train.py`, and append one JSON line per iteration to `/home/user/experiments.jsonl` with keys `n_words` and `accuracy`. Install `pandas`/`scikit-learn` via `pip` first.
