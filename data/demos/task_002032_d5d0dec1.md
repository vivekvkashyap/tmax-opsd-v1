First reverse the "blank output" issue: the binary gates its computation on an environment variable. Recover its name and required value by inspecting the stripped binary — `strings /app/analyzer` (look for an uppercase env-var-looking token and the expected string it is compared against), and confirm with `ltrace`/`strace -e getenv` or by piping test input while toggling candidates. Set that env var when you exec the binary; without it the process exits silently. Build the service in `/home/user/etl_service.py`: an HTTP server on `127.0.0.1:5050` accepting `POST /process`. Reject requests lacking header `X-Pipeline-Token: alpha-etl-883` (return 401/403). For imputation, precompute each column's mean from `/home/user/reference_data.csv` (parse all rows, per-column average); replace every `?` in the incoming 5-value CSV with that column's mean. Pass the imputed comma-separated line to `/app/analyzer` via stdin with the discovered env var set, capture its stdout float. Respond with JSON `{"imputed":[...five floats...],"score":<parsed float>}`. Traps: forgetting the env var (blank output), imputing with a global mean instead of per-column mean, or mismatching the imputed value ordering. Leave the server running in the background.