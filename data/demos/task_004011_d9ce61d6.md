Extract the hyperparameters from `/app/config.png` with `tesseract` (or another OCR tool) first, and read the output text carefully to get the exact TARGET_COL, BOOTSTRAP_B, and THRESHOLD values rather than assuming typical defaults. In `/home/user/detector.c`, parse the 3-column headerless CSV, pull the 1-indexed target column, and compute consecutive absolute differences (N-1 values from N rows). For the bootstrap: seed with `srand(42)` once at program start, then for each of `BOOTSTRAP_B` iterations sample N-1 differences with replacement (use `rand() % (N-1)` as the index) and record the mean of that resample; this reproduces the same sequence as the reference implementation only if you call `rand()` in the same nested-loop order (outer loop over B iterations, inner loop over N-1 samples) and don't reseed between iterations. Sort the B bootstrap means and take the value at the 2.5th percentile index (e.g. `(int)(0.025 * B)`, matching how the oracle computes the index — be careful about off-by-one/rounding in percentile indexing). Print `REJECT` if that lower bound is strictly greater than THRESHOLD, else `ACCEPT`, exit 0. Compile with `gcc -O3 -lm -o /home/user/detector detector.c` and validate against a few of your own synthetic clean/volatile CSVs before running against the hidden corpora.
