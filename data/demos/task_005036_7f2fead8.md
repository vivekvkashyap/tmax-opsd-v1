Write the C program to read `/home/user/measurements.csv`, skip the header, and parse both columns into an array of N doubles, computing D[i] = SensorA[i] - SensorB[i] per row. Implement the LCG exactly as given (unsigned long seed=42, the exact multiply/add/mod formula) — do not substitute `rand()` or reseed between bootstrap samples; the seed must evolve continuously across all B*N draws in the same order the reference process would generate them (outer loop over B, inner loop over N draws, index = my_rand() % N). Accumulate each bootstrap sample's mean, then average across all 10,000 to get D_boot. Apply the given Normal-Normal conjugate formula literally with mu_0=0.0, sigma_0^2=1.0, sigma^2=4.0, n=N, xbar=D_boot. Print the result with `printf("%.4f", posterior_mean)` to `/home/user/posterior.txt` with nothing else in the file (no trailing text/newline issues). Compile with `gcc process.c -o process -lm` and run it, then double check the LCG's integer types match exactly (unsigned long, modulo 2147483648) since even a signed/unsigned mismatch will silently produce a different random sequence and wrong final answer.