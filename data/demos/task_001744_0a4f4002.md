Start by extracting the prior parameters from the image: run `tesseract /app/sensor_priors.png stdout` (or open it visually) to read mu/sigma for each sensor rather than guessing them. Then write `/home/user/detector.py` to load `readings.csv` and `metadata.csv`, merge them on `id` with pandas, and score each row (or the batch as a whole) using the Normal log-likelihood/log-pdf under the extracted priors for each sensor column. Sum or average log-likelihoods across rows and sensors to get one batch-level statistic, then classify by comparing against a threshold — don't just eyeball the mean, actually compute per-batch statistics (e.g. mean log-likelihood, or z-score of the sample mean vs the prior) on the labeled `/app/training_data/clean` and `/app/training_data/anomalous` examples to calibrate a threshold that cleanly separates the two classes with margin, since anomalous batches only shift one sensor by a few standard deviations. Make the script exit 0 for clean and 1 for anomalous, and verify it end-to-end against the training data before assuming it generalizes to the hidden test corpus. Ensure the file is executable.
