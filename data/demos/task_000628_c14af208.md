Do this in dependency order. First reassemble the split archive: `cat raw_backup.tar.gz.partaa raw_backup.tar.gz.partab > combined.tar.gz` then extract into `/home/user/extracted_backup/` with `tar -xzf`. Next write `/home/user/process.py` using `os.walk` with a manual visited-inode set rather than following symlinks blindly: when walking, don't follow directory symlinks into a cycle (e.g. call `os.walk(..., followlinks=False)` or track visited real directories via `os.path.realpath`), and for each candidate `.json` file, dedupe by `(st_dev, st_ino)` from `os.stat`/`os.lstat` so hardlinks and symlinks-to-the-same-file are only processed once. Parse each unique JSON file's array of objects, merge all records into one list, dedupe if needed, and sort by `id` ascending. Chunk the sorted list into groups of exactly 3, and for each chunk write to a temp file in the *same directory* (e.g. `tempfile.NamedTemporaryFile(dir=..., delete=False)` or a `.tmp` suffix), flush+close it, then `os.rename()` it to `chunk_N.csv` — this same-directory-then-rename pattern is what makes the write atomic, writing directly to the final path or using `/tmp` for the temp file would not be. Include the CSV header row in every chunk file.
