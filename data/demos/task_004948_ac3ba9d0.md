Implement `computeTotalEnergy` using the standard trapezoidal rule: for each adjacent pair of points (freq[i], psd[i]) and (freq[i+1], psd[i+1]), accumulate 0.5 * (freq[i+1] - freq[i]) * (psd[i] + psd[i+1]) into a running sum, iterating i from 0 to n-2 (do not assume a fixed spacing — use the actual freq deltas from the CSV). Read the CSV rows into parallel vectors of doubles exactly as the skeleton already does, skipping the header line. After computing the energy, compare it to the reference value read from reference.txt using a strict greater-than test to decide Yes/No. Compile with `g++ -O2 -o /home/user/analyze_spectra /home/user/analyze_spectra.cpp` (plain g++ with no extra libs). Run the binary with the two file arguments exactly as specified so it writes `/home/user/result.txt`; verify the output uses `fixed`/`setprecision(2)` formatting so the energy prints with exactly two decimal places, matching the required "Total Energy: X.XX" / "Exceeds Reference: Yes|No" format precisely (no extra whitespace or lines).
