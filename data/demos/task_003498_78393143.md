Treat this as a multi-service system to bring up and debug component by component, verifying each hop before moving to the next. Start Redis first and confirm it's reachable with `redis-cli ping`. Inspect /app/api.py for its Redis connection settings and /app/nginx.conf for its proxy_pass target — these are described as misconfigured, so diff what's there against what's actually needed (Flask really listening on 5000, Redis really on 6379) rather than assuming the provided files are already correct; fix and restart each service, then confirm end-to-end with a manual `curl localhost:8080/ingest -d '...'` and check `redis-cli LRANGE sensor_queue 0 -1` to see the item queued before writing any C code. Only then write worker.c: loop on `BLPOP sensor_queue 0` via hiredis, split the popped string on commas, replace "NaN" entries with the mean of the record's valid numeric values, clamp remaining values to [-50, 50], compute Euclidean distance to each reference row in /app/references.csv (parsed once at startup) and keep the minimum, and only append to /tmp/cleaned_output.csv (2-decimal floats) if that minimum distance is strictly below 20.0. Compile with `-lhiredis`, run the worker in the background (e.g. with `&` or nohup), and re-run your manual curl test to confirm a row actually lands in the output file with the expected closest ref_id before considering it finished.
