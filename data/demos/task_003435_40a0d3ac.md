First fix the vendored package: open `/app/vendored/go-rollstat/Makefile` and `rollstat.go`, note the source uses `//go:build production` but the Makefile invokes `go build -tags=prod` — the tag string must match exactly, so change the Makefile's tag to `production` (or add a matching alias) rather than editing the build constraint, since the task says fix the Makefile's perturbation. For `etl.go`, structure the pipeline as clearly separable stages: (1) decode each JSONL line, reject on `ts <= 0`, `val` outside [-100,100], or `id` failing `regexp.MustCompile("^[A-Z0-9]+$")`; (2) sort survivors by `ts` using `sort.Slice`, then dedupe — track seen `(ts,val,id)` triples to drop exact repeats, and for same-`ts`-different-content rows keep only the first-encountered (stable sort preserves input order, so iterate and skip once a `ts` has already been kept); (3) walk from min to max `ts` in a loop, and whenever the next expected second isn't in your map, synthesize a record cloning the last valid `val`/`id`; (4) maintain a sliding window (last up to 3 vals) computing the rolling average as you iterate in timestamp order, using the fixed `rollstat.Window` type. Round `avg` with `math.Round(x*100)/100`. Test locally with synthetic evil and clean inputs mirroring the described rules before considering it done.
