Bring up the three services first and confirm connectivity independently before writing the sanitiser: start `redis-server` (port 6379), start the Flask app from `/app/services/api.py` (port 5000, depends on Redis), then start `nginx -c /app/services/nginx.conf` (or reload) so port 8080 proxies to 5000 — test with `curl -X POST http://localhost:8080/ingest -d '{}' -H 'Content-Type: application/json'` before trusting the pipeline. For `/home/user/sanitiser.py`: load the JSON, build numpy arrays for X and y, compute `X.T @ X` and reject (exit 1, no network call) if `abs(np.linalg.det(XtX)) < 1e-4` — note duplicate/scaled columns make this near-singular, so don't just check for exact zero. Separately, for leakage, compute the Pearson correlation between y and each individual column of X (`np.corrcoef`) and reject if any `abs(corr) > 0.9999`; guard against NaNs from zero-variance columns. Only if both checks pass, POST the original JSON payload to `http://localhost:8080/ingest` with `Content-Type: application/json`, exiting 0 only on a 200 response, and exit 1 for any failure without sending data.