Work the recovery in order: first use `git fsck --full --unreachable --no-reflogs` (or `git fsck --lost-found`) to list dangling blobs in the repo at `/home/user/project`, then for each candidate run `git cat-file -p <sha> | base64 -d` to inspect the decoded content — you're looking for the blob whose decoded content is exactly 5 space-separated integers. Write that decoded blob's raw (still base64-encoded) content back out to `/home/user/project/config.b64` using `git cat-file -p <sha> > config.b64`. Next, read `build.sh` carefully line by line rather than assuming it's close to correct: check the `base64` invocation (decoding requires the `-d`/`--decode` flag, not encoding), check the loop bound against the actual array length (a `seq 0 $count` style loop with a zero-based array is off-by-one — it should stop at `count-1`), and check the score formula against the spec (sum of `value*(index+1)`) term by term rather than trusting the existing arithmetic. Fix each bug independently, then run the script and redirect only the final integer to `/home/user/project/build_output.txt`.
