Start with transcription: install a tool like `openai-whisper` (or SpeechRecognition+an engine) and run it on `/app/config_memo.wav`; inspect the raw transcript text carefully since ASR often mis-renders numbers or names (e.g., "minus ten" vs "-10", capitalization) — normalize the text yourself before parsing rather than trusting it verbatim. Parse the normalized transcript with a regex matching the stated sentence pattern ("Set X to Y. Admin Z.") to pull out (key, value, admin) triples in order. For the C server: use a standard TCP accept loop on `127.0.0.1:9090` reading line-buffered input; maintain an in-memory table (e.g., array of structs or a simple hash map) keyed by config `key` so that a later `ADD` for the same key overwrites the earlier one rather than appending — this is how "deduplication" must work. On `ADD`, validate that `value` parses as an integer strictly greater than 0 (reject non-numeric and reject <=0, including negative values) before storing, and always store `***` instead of the parsed admin name. Implement `DUMP` to iterate the table and emit each surviving record in the exact `CONFIG: key=... value=... admin=***` format followed by `END`. Compile, launch the server in the background, then loop over your parsed records piping `ADD ...` lines to the server via `nc localhost 9090` (one connection per line is fine), and leave the server running afterward.
