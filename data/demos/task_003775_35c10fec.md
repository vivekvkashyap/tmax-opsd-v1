Process archives programmatically with `zipfile`/`tarfile` (never shell out to `unzip`/`tar`, since you need to inspect member paths before writing anything to disk). For each archive, first enumerate every member's stored path (via `ZipFile.namelist()` or `TarFile.getmembers()`) and check whether any is an absolute path (starts with `/`) or contains `../`/`..\\` traversal segments — do this check on the RAW stored path string before any `os.path.join`/normalization, since normalizing first can hide the traversal. If any member is malicious, skip extraction entirely, delete the archive from the uploads dir, and record its basename for the deleted log (sort the log alphabetically once all archives are processed, not incrementally). For safe archives, extract member-by-member into the extracted dir, preserving relative subdirectories with `os.makedirs(..., exist_ok=True)`. After extraction, walk the tree for `.txt` files, read each with `encoding='cp1252'`, and rewrite it in place with `encoding='utf-8'` — leave every other file's bytes untouched. Finally walk the whole extracted tree, compute each file's SHA-256 with `hashlib.sha256(open(f,'rb').read()).hexdigest()`, and write a manifest keyed by path relative to the extraction root.