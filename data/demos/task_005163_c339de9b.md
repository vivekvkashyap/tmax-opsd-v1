Write the C program with a two-pass, memory-efficient approach rather than a naive O(edges^2) scan. First pass: read `/home/user/wrote.csv` line by line, intern each distinct `author_id` and `paper_id` string into a hash table (or sorted array) so you can work with integer IDs instead of strings. Build, for each paper, the list of author IDs that wrote it (e.g. a dynamic array or bucket keyed by paper index). Second pass: for each paper's author list, enumerate every unordered pair of distinct authors, and for each pair increment a counter in a hash map keyed by the pair of integer author IDs (canonicalize the pair so the lexicographically smaller author_id string is first — compare using strcmp on the original strings, not the integer IDs, since the ordering must be ASCII/lexicographic). Use a hash map (open addressing or chaining) for the pair counts to keep this fast for tens of thousands of edges — do not use nested loops over all authors. Finally, collect all pairs with nonzero weight, sort them with qsort using a comparator that does strcmp on author1 then author2, and write CSV rows `author1,author2,weight`. Watch for: off-by-one buffer sizes for 32-character IDs, correct plus-newline handling when parsing CSV lines, and ensuring ties in sort order are broken deterministically by string comparison, not pointer/insertion order.