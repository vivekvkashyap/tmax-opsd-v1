Build this as ordered, independently-checkable stages, since the output must match a golden script bit-for-bit. First coerce each expected column to its numeric type with something like `pd.to_numeric(col, errors='coerce')` for both train and test, then drop any row with a resulting NaN in any required column (this handles both truly-missing and type-mismatched values in one pass) — do this separately for train.csv and test.csv. On the cleaned train data, compute `abs(corr(feature, spend))` for each of age/income/score via pandas' `.corr()`, take the top 2 by that value, then sort those two feature names alphabetically before using them anywhere downstream (both for column order and for X). Fit a default `LinearRegression()` on train X/y using only those 2 features, predict on cleaned test X, and round to 2 decimals. For the similarity search, pull test id 101's feature vector (post-cleaning, same 2 features, unscaled) and compute Euclidean distance to every cleaned train row, taking the argmin. Assemble the JSON exactly matching the schema, with predictions keyed by string test ids.