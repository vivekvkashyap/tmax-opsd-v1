Approach this as a root-cause debugging task, not a rewrite-from-scratch task. First examine `/home/user/crash/service.log` and load `/home/user/crash/core` in gdb (`gdb /app/service_worker <core>` or similar, using `bt`/`bt full`) to find the exact function and line where the crash occurred — this should point into the vendored `/app/cpp-base64/base64.cpp` decode routine. Diff the vendored file's loop/bounds logic mentally against the well-known upstream cpp-base64 implementation to spot the altered loop-termination condition that lets it read past the end of unpadded input; fix it to match safe bounds. Separately, read `/app/service_worker.cpp` for any `new[]`/`malloc` buffer whose `delete[]`/`free` is present on the success path but missing on an early-return/error path — that's the leak; add the missing cleanup on every exit path. Build via `make -C /app` (or wherever the Makefile targets), verifying it produces a binary, then copy/rename that binary to `/home/user/fixed_worker`. Before finishing, self-test with `valgrind` and a few malformed/edge base64 lines (empty, no padding, invalid chars, very long lines) piped via stdin to confirm no crash and no leaked bytes.