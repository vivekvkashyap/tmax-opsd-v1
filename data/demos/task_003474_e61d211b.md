Write the Python script to do a single streaming pass over the log rather than trying to regex the whole multi-line block in one shot, which is easy to get subtly wrong with embedded whitespace. Read the file line by line, tracking a boolean "inside dump" state: toggle it on when you hit a line exactly equal to `===BEGIN DUMP===` and off when you hit `===END DUMP===`; while inside the dump region, strip whitespace/newlines from each line and append the remaining hex characters to an accumulator string rather than emitting them to the output. For lines outside the dump markers, copy them verbatim to the cleaned-log output — except emit the single replacement line `[DUMP EXTRACTED TO PAYLOAD.BIN]` exactly once, at the point where the `===BEGIN DUMP===` marker was encountered (not per line). After the full pass, decode the accumulated hex string with `bytes.fromhex(...)` and write it as raw binary to `payload.bin` — double check there's no leftover whitespace/odd-length string causing a decode error. Verify by re-reading `cleaned_app.log` and confirming line-for-line structure matches the original minus the dump block, and by inspecting the decoded payload bytes look like sensible ASCII text before finishing. Run the script so both output files actually get produced.