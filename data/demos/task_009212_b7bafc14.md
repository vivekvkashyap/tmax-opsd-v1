Set up a Cargo project and model the wait-for graph as an adjacency list (e.g. `HashMap<String, Vec<String>>` or a `petgraph::DiGraph`) built directly from the JSON array — one edge per `{tx_id, waits_for}` entry, keeping duplicate edges since they matter for degree counting. Compute out-degree for every node by counting how many edges originate from it in the raw edge list (not deduplicated, not restricted to cycle members). Separately, find every node that participates in *any* cycle: run a strongly-connected-components algorithm (Tarjan's or Kosaraju's, or use `petgraph::algo::tarjan_scc`) and mark a node as "in a cycle" if its SCC has size > 1, or if it has a self-loop. A common mistake is using naive DFS cycle detection that only finds one cycle instead of all cycle members, or forgetting nodes can belong to multiple overlapping cycles. Filter to cycle nodes only, then sort by out-degree descending, tie-break by `tx_id` ascending, take the first 5, and write a CSV with header `tx_id,out_degree` using exact values (no quoting needed for these fields). Build and run with `cargo run` to produce the file at the required path.
