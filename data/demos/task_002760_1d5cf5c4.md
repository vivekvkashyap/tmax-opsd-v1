First fix the vendored build: inspect `/app/kalman-c-1.0/Makefile` and note the library uses `sqrt`/`exp` from libm but the link step for the test/library target omits `-lm`; add `-lm` to the relevant link command (or `LDFLAGS`) so `make` succeeds and produces `libkalman.a` with exit code 0 — don't rewrite the library's math logic, only fix the missing link flag. Then write `/home/user/clean.c`: read all 1000 lines from `/home/user/noisy_sensor.txt` into a `double[]` array with `fscanf`/`strtod`, call `kalman_filter(input, output, length, 1e-4, 0.02)` from `kalman.h` exactly as specified (don't reimplement the filter yourself — use the library function so your output matches the reference recurrence), then write each output value with `%.6f` per line to `/home/user/cleaned_sensor.txt`. Compile with something like `gcc clean.c -I/app/kalman-c-1.0 -L/app/kalman-c-1.0 -lkalman -lm -o clean`, ensuring the include path finds `kalman.h` and the linker finds `libkalman.a`. The grader checks MSE against a hidden reference, so correctness of length (all 1000 values) and the exact process/measurement variance values matter most.
