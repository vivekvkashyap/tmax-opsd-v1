Start by inspecting `/home/user/sensor_dump.csv` directly (`head`, column count) to confirm its schema (sensor_id, location, temp, timestamp, status) before writing any code. In your C++ program, build the `mongoimport` invocation as a string (specifying `--type csv --headerline --db iot --collection readings`) and run it via `system()`. For the aggregation, construct a `mongosh --eval` (or a `.js` script piped to `mongosh`) that: (1) `$match`es out corrupted/error status rows, (2) `$sort`s by timestamp descending, (3) `$group`s by `sensor_id` taking the first (most recent) document per sensor via `$first`, (4) a second `$group` by `location` computing `$avg` of temperature, then write this pipeline's output with `printjson`/`JSON.stringify` redirected to `/home/user/final_report.json`. Separately re-run the same pipeline through `.explain("executionStats")` and redirect that to `/home/user/query_plan.json`. Before running the aggregation, create a compound index on the fields your `$match`/`$sort` stages use (e.g. `status`, `timestamp`, `sensor_id`) via `db.readings.createIndex(...)` in mongosh, since the verifier checks `db.readings.getIndexes()`. Compile with `g++ analyze.cpp -o analyze` and actually execute it to confirm the report and plan files are produced correctly — do not rely on manual mongosh runs outside the compiled binary.