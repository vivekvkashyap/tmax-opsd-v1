Handle each requirement independently and verify with the exact same command the grader will use. For the cert, use `openssl req -x509 -newkey rsa:2048 -keyout /home/user/certs/new_key.pem -out /home/user/certs/new_cert.pem -days 30 -nodes -subj "/CN=localhost"`, then confirm with `openssl x509 -in ... -noout -subject` that it reports `CN = localhost`. Immediately after generating the key, `chmod 400` it — do this last in your cert steps since some tools reset permissions on write. For redaction, read `/home/user/logs/app.log` and use a regex substitution (e.g. `sed -E 's/API_KEY=[^ ]+/API_KEY=REDACTED/g'` or Python `re.sub`) that matches the key's value up to the next whitespace, writing to a *new* file `app_clean.log` — never edit the original in place, and double check every other line is untouched (byte-for-byte) except the two redacted occurrences. For the payload file, write the literal string `../../etc/passwd` with no extra text, then `chmod 600` it. Verify each artifact independently with `stat -c "%a"` and `grep -c` before considering the task done.
