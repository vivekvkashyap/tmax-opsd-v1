First isolate the vendored bug empirically: feed `/app/dpkt-1.9.8`'s TLS parser a large (>32KB) synthetic Client Hello and observe it fail or return a nonsensical negative-looking length, then inspect `dpkt/ssl.py` around the record-length `struct.unpack` call — a signed-vs-unsigned format character (`'h'` vs `'H'`) is the classic way to break large lengths, so diff against the format's other fixed-width reads to spot the mismatch and correct it. Only after the vendored fix is verified independently (e.g. a quick script parsing a big Client Hello without exceptions) should you build `traffic_analyzer.py`. Implement the framing loop defensively: read exactly 4 bytes and compare to the magic bytes, then 2 bytes big-endian for type, then 4 bytes big-endian for length, then exactly that many payload bytes — if any read comes up short (EOF) or the magic doesn't match, exit(1) immediately and print nothing, per spec. Route type 1 payloads through `dpkt`'s HTTP request parser to pull the `Host` header, and type 2 payloads through the (now-fixed) TLS Client Hello parser to extract the SNI extension. Since correctness is checked by byte-for-byte fuzz equivalence against a reference, pay special attention to exact whitespace/newline handling in your rule-line output format.
