Fix `supervisor.py` to be sequential-then-resilient rather than the current parallel launch: use `subprocess.run` (blocking) on `db_init.py` first, check its return code is 0 before proceeding, and only then loop up to 4 total attempts of `profile_gen.py` (initial run + 3 restarts), breaking as soon as one succeeds; if all 4 attempts fail, have the supervisor `sys.exit(1)`. Then write `ci_run.sh` as the outer orchestrator: invoke `python3 supervisor.py`, check its exit status, and only on success proceed to `find profiles -type f -exec chmod 640 {} \;` and `find profiles -type d -exec chmod 750 {} \;` (apply file/dir modes separately — a single recursive chmod won't give files and directories different permissions), then `tar -czf release.tar.gz -C /home/user/account_system profiles` and write `SUCCESS` or `FAILURE` to `ci_status.log` depending on outcome. Remember to `chmod +x ci_run.sh`. Test end-to-end by running the script and inspecting extracted tarball permissions with `stat -c %a`, since profile_gen.py is intentionally flaky on first run and only the restart logic makes it eventually succeed.
