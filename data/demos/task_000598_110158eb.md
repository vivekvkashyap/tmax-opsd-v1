Build the SQL query incrementally in the `sqlite3` CLI before wrapping it in a script — test the recursive CTE alone first, then add the window function, then the filter/sort. Write a `WITH RECURSIVE` CTE that starts from the row(s) where `manager_id IS NULL` at depth 0, then unions in rows joining `employees e` to the CTE on `e.manager_id = cte.id` with `depth+1` — verify depths look right with a plain `SELECT * FROM cte` before adding anything else. Layer a window function on top (either in the same CTE chain or a second CTE) computing `AVG(salary) OVER (PARTITION BY department)` per row — note this should reflect ALL employees' actual department averages, so make sure you're computing it over the full employee set (or full recursive result, which does include everyone) rather than some filtered subset. Then filter `WHERE depth >= 2 AND salary > dept_avg_salary` and `ORDER BY depth DESC, salary DESC`. Run the whole thing via `sqlite3 -header -csv /home/user/company.db "<query>" > /home/user/high_earners.csv` so the CSV headers and quoting are handled correctly by sqlite3 itself rather than hand-formatted. Wrap the final working query in `/home/user/analyze_org.sh`, `chmod +x` it, and run it end-to-end to confirm the CSV columns and ordering exactly match the required column list.