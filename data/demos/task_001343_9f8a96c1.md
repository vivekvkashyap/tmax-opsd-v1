First get the services actually running: inspect `/app/services/` for the Flask apps, fix the bind host/port in the training API so it listens where the task expects, export `MODEL_SAVE_PATH` so training writes to `/home/user/model.joblib`, then launch both with the provided start script (or run them backgrounded yourself) and confirm with `curl` that both endpoints respond before writing the pipeline script. For the ETL, curl the raw CSV, then do everything with `awk`/`sed` (no Python) in `/home/user/run_pipeline.sh`: drop rows with empty `target` or `id` fields, compute the median of valid `age` values in an awk pass and substitute it for missing ages, filter out rows with `income` > 300000, and compute Pearson correlation of `activity_score` vs `target` with an awk script that accumulates sums, sums-of-squares, and cross-products in one pass (correlation formula from those aggregates), writing the single float to `/home/user/correlation.txt`. Submit the cleaned CSV with `curl -F "file=@cleaned.csv" http://localhost:8001/train` and check the returned RMSE — if it's above 12.5, revisit which cleaning step is distorting the feature distribution (e.g. median imputation strategy or outlier cutoff) rather than just resubmitting the same data.
