Do the shell steps before writing Go. Verify archive integrity with `gzip -t measurements.tar.gz`, then extract with `tar -xzf measurements.tar.gz -C /home/user/dataset_work/extracted/` (create the directory first). Locate `metadata.txt`'s Status column (it's field 2 in the CSV) and filter with `awk -F, 'NR>1 && $2=="VALID"' metadata.txt > .../valid_metadata.txt` — skip the header explicitly, don't just grep for the string VALID since that could false-match other statuses containing it as a substring. For the Go program, open `valid_metadata.txt`, split each line on commas, parse field index 2 (Temperature) as float64 with `strconv.ParseFloat`, accumulate sum and count, compute the mean, and format it to exactly one decimal place (`fmt.Sprintf("%.1f", avg)`) when building the JSON string — don't rely on default float formatting, which can produce extra digits or drop trailing zeros. Write the exact literal `{"average_temperature": <value>}` to `/home/user/result.json` (match spacing/quoting precisely), then `go run parser.go` and inspect the file before finishing.
