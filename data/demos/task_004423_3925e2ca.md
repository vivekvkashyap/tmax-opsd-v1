Recover the calibration weights from the image rather than assuming values: crop/preprocess `/app/calibrations/weights.png` if needed and run `tesseract` (or pytesseract) on it, then parse the "CA: X.XX", "C: Y.YY", "N: Z.ZZ" lines out of the OCR text with a regex into a lookup map; atom types other than CA/C/N default to weight 1.0. For the Go simulation, parse standard PDB `ATOM` records by fixed-column offsets (not naive whitespace splitting, since PDB fields can be adjacent) to get serial number, atom name, and x/y/z coordinates. The root cause of non-determinism is iterating a Go `map` for the atom list — instead, store atoms in a slice ordered by ascending serial number (or sort by serial number after parsing), then use nested loops `for i := range atoms { for j := i+1; j < len(atoms); j++ { ... } }`, accumulating `energy += weight[i]*weight[j]/distance(i,j)` into a single `float64` in that exact sequential order so results are byte-reproducible across runs. Wrap this in an `net/http` server bound to `127.0.0.1:8080` with `POST /simulate` (raw PDB body -> `{"energy": ...}`) and `POST /compare` (diff threshold strictly `> 1e-5`), and require the `Authorization: Bearer sim-token-2024` header on every request, returning 401 otherwise. Build and launch the service in the background, then curl both endpoints yourself to confirm behavior before finishing.