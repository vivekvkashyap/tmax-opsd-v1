Don't eyeball 100 files manually — grep for the signature of the bug programmatically: search all `exp_*.py` scripts for a call that fits-and-transforms the test data instead of merely transforming it (e.g. `grep -rn "fit_transform" /home/user/experiments/scripts/ | grep -i test` or similar), since the leaking script is the one where `X_test` goes through `fit_transform` rather than `transform`. Once you've isolated the single offending experiment name, look it up as a row key in `/home/user/experiments/logs/metrics.csv` (matching on the `experiment_name` column) and read off its `model_artifact` field — cross-check that this experiment's accuracy is a suspicious outlier compared to the rest as a sanity confirmation you found the right one. Write exactly that artifact path, and nothing else, to `/home/user/leaked_model.txt`.
