Iterate files with `find /home/user/repo -type f -size +1000000c` to get the strict "larger than" threshold correctly (note `find`'s `-size +N` semantics and be careful with byte vs block units — use the `c` suffix for exact bytes). For each match, compute `sha256sum` on the original file BEFORE splitting/deleting it. Use `split -d -a 2 -b 500000 --numeric-suffixes=0 <file> <file>.part` (or equivalent) to get exactly the `.part00`, `.part01`, ... naming — verify the produced filenames match the spec exactly, since split's default suffix style differs from what's needed here. Gzip each chunk, count how many chunks were actually created (don't hardcode based on file size math — read it back from the filesystem, since exact division vs remainder affects chunk count), then delete the original file. Build manifest lines as `<abs_path>|<count>|<sha256>`, collect them all, and sort by path (e.g. `sort`) before writing the final manifest — do not write lines in traversal order and assume it's already sorted.
