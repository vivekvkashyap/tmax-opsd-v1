First recover the exact transformation spec from the audio rather than assuming a format: transcribe `/app/voicemail.wav` with a local speech-to-text tool (e.g. `whisper` if available, or another installable STT), and read the transcript carefully for the four ordered output rules it dictates (an IP masking rule, a risk-classification rule with two conditions and precedence, a numeric-encoding rule, and a case-transform rule) — get the exact wording of thresholds and formatting (e.g. hex prefix style, uppercase/lowercase) right, since the fuzz-equivalence check will compare byte-for-byte against a hidden oracle. Then implement `/home/user/audit_normalizer` as a line-oriented JSON stdin processor: parse each line, apply the rules in the order and priority stated in the voicemail (note that classification rules are usually evaluated with priority — an escalation flag should short-circuit before falling back to a score-threshold check), and print exactly one CSV row per valid input line with no extra stdout output. Make it executable with a proper shebang, and self-test against varied random inputs (edge cases like score exactly at a threshold, port at boundary values) before considering it done.
