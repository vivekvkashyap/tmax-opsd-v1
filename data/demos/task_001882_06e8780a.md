Write a Go program that processes `/home/user/data/servers.csv` in clearly separated stages so each transformation is easy to verify independently:
1. Parse the CSV, treating empty strings as missing per numeric column.
2. Compute each column's mean using only the non-empty values (not zero-substituted), then fill the missing cells with that column's own mean — do this per-column, not globally.
3. After imputation, drop any row where CPU or Mem exceeds 100.0.
4. Drop the Disk column and add NetTotal = NetIn + NetOut.
5. For similarity, build a 3D vector [CPU, Mem, NetTotal] per remaining server, compute Euclidean distance from "S-10" to every other valid server, sort by distance ascending (tie-break alphabetically by ServerID), and take the top 2.
6. Write cleaned rows to `/home/user/output/cleaned.csv` with header `ServerID,CPU,Mem,NetTotal` and all floats formatted to exactly 2 decimal places (use `strconv.FormatFloat` with `'f', 2` or equivalent — naive `%v` formatting will produce wrong precision). Write the top-2 similar ServerIDs, one per line, most-similar first, to `/home/user/output/similar.txt`. Create both `workspace` and `output` directories first, then compile and run the program to actually generate these files.
