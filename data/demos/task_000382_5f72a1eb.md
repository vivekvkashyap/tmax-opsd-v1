Do the imputation strictly from training-set statistics grouped by category: compute `train.groupby('category')['feature_A'].median()` and use that same per-category median mapping to fill NaNs in both the train and test frames (never compute a separate median from test data). After filling, cast the column explicitly with `.astype('int64')` (or round first if medians can be non-integer, then cast) — pandas will silently keep the column as float64 after `fillna` even with integer values, so the cast step is easy to miss and must be verified with `df.dtypes`. For the model, build one-hot columns with `pd.get_dummies(df['category'], drop_first=False)` on both train and test, then reindex the test dummy columns to match train's columns (`reindex(columns=train_cols, fill_value=0)`) so column sets/order are identical before fitting `Ridge()` and calling `.predict()`. Write `cleaned_test.csv` with the original test columns (imputed, int-typed feature_A) and `predictions.csv` with just `id,target`, checking the sample outputs against your own manual median calculation before finishing.