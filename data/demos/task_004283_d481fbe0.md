Work through the four stages in order and validate each before moving on. First, for archives: loop over `/home/user/archive/*.tar.gz`, test integrity non-destructively with `tar -tzf <file> >/dev/null 2>&1` (or `gzip -t`) before extracting — only run `tar -xzf <file> -C /home/user/staging/` for the ones that pass; skip and don't error out on the corrupted one. Second, for active logs: use `find /home/user/data/logs -maxdepth 1 -name '*.log' -type f` (the `-type f` is critical — it excludes symlinks like `current.log` that `ln -s` created) and for each matched file run `sed 's/SECRET/REDACTED/g' src > /home/user/staging/$(basename src)` to sanitize while preserving the filename, being careful to replace the exact word `SECRET` (not substrings) if the spec implies whole-word matching — check the sample files to confirm plain substring replacement suffices. Finally, build the archive from inside the staging dir so paths are relative: `tar -czf /home/user/final_backup.tar.gz -C /home/user/staging .` (or `cd` into staging first) so extracted files land at the archive root, not under a `staging/` prefix. Verify by extracting into a scratch dir and diffing contents.
