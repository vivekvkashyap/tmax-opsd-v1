Work through the three pieces independently and test each before wiring them together. Copy `/app/router_template` to `/home/user/router` and use an async HTTP framework/manual parsing over tokio's TcpListener to serve `GET /info` on `127.0.0.1:8000`, reading `TZ` from the environment at request time (not compile time) and formatting the exact JSON string given in the spec — test with `curl 127.0.0.1:8000/info` after setting `TZ` yourself. For the TCP proxy on `127.0.0.1:8001`, read byte-by-byte or use a buffered reader until the first `\n`, compare the line strictly against `AUTH: v2migrate\n`, and only on an exact match open a new TCP connection to the legacy VM at `127.0.0.1:9090` and spawn two concurrent copy tasks (client->legacy and legacy->client) using `tokio::io::copy` or manual loops — any other first line means close the socket immediately without echoing anything. Test the proxy manually against the legacy VM (start it via `/app/start_legacy_vm.sh` first) using `nc` or a small script sending the auth line plus payload, checking you get the legacy response back. Finally write `/home/user/run_stack.sh` to export `TZ=Asia/Tokyo` and `LANG=C.UTF-8`, launch the legacy VM script and the compiled router binary both with `&` and disown/nohup so they survive the script exiting, then confirm both ports are listening with `ss`/`curl` after running it.
