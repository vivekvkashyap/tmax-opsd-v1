Walk `/home/user/source_data` recursively (e.g. Python's `os.walk` with `followlinks=False`, or `find`), mirroring directories first. For each entry, use `os.path.islink` before `os.path.isfile`/`isdir` to distinguish symlinks from regular files, since `os.walk` by default reports symlinked dirs specially and `os.path.isfile` follows links. For regular files, use `os.link(src, dst)` to hardlink (never copy contents). For symlinks, resolve the real target with `os.path.realpath`, then check with `os.path.commonpath`/prefix comparison whether that resolved path is inside `/home/user/source_data` — if outside, append the symlink's own absolute path to a list for `unsafe_links.log`; if inside, compute where that target lands under `archive_prep` and create a *relative* symlink there via `os.symlink(os.path.relpath(target_in_archive, start=link_dir), link_path)`, not the absolute or original relative target. After building the tree, sort and write the unsafe-links list, then walk `archive_prep` for regular files only, computing `hashlib.md5(open(f,'rb').read()).hexdigest()` for each, formatting as `<hash>  ./relative/path`, sorted by path, into `manifest.txt`.
