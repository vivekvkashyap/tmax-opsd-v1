First recover the XOR key: run the available transcription tool (e.g. `whisper /app/voicemail.wav`) on the audio file and take the resulting single lowercase word as the key — do not hardcode a guessed word. Then write a small script (or manual byte-check) to XOR-decode the first bytes of one `.zdat` file with that key (repeating it cyclically) and confirm it produces readable CSV text before trusting the key. For the C server: implement a standard TCP listener (`socket`/`bind`/`listen`/`accept`) on 127.0.0.1:8050 that, per connection, reads a newline-terminated query, then for each `.zdat` file in `/app/archive/` opens it, reads the first 128 bytes, XORs each byte with `key[i % strlen(key)]`, and parses up to the first comma as `project_name`. Compare that to the query string exactly; on match write `MATCH: <basename>\n` and stop scanning, otherwise after checking all files write `NONE\n`. Close the socket per request (don't leave it open), and launch the compiled binary in the background (`./search_server &`) so it stays resident for the automated test client.
