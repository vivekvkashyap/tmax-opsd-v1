Don't guess the algorithm — extract it directly from the image. Run OCR against `/app/backup_spec.png` (e.g. `tesseract /app/backup_spec.png -` or another available OCR tool) and read out the numbered transformation rules exactly as written, in the exact order listed, since order matters for the final result. Implement `/home/user/packet_normalizer.rs` as a small CLI that takes `argv[1]` as the single input string, applies each recovered rule as a sequential string transformation (case conversion, character substitutions, reversal, etc. — whatever the OCR'd text actually specifies), and prints the result via `println!` (which already appends exactly one trailing newline — don't add extras). Compile with `rustc /home/user/packet_normalizer.rs -o /home/user/packet_normalizer`. Since the grader does bit-exact fuzz comparison against a hidden reference across hundreds of random inputs including punctuation and edge-length strings, be precise about operation order and case-sensitivity when reading the spec image, and self-test locally with a handful of manual inputs to sanity-check each rule fires correctly before considering it done.