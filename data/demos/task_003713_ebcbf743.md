First reverse-engineer the binary rather than guessing: run `strings /app/storage_agent` to look for environment-variable names and hardcoded token/key literals it compares against (grep for patterns like `_TOKEN`, `_KEY`, `AUTH`), and confirm with `ltrace`/`gdb`/`objdump -d` if strings alone is ambiguous. Once you find the required env var name and its expected value, export it and launch the binary in the background, verifying with `curl 127.0.0.1:9999/raw` that it responds; also append the `export VAR=value` line to `/home/user/.bashrc` for persistence. Then write `/home/user/monitor.py` as a small standard-library HTTP server (e.g. `http.server`) bound to port 8080 handling `/status` (plain "UP") and `/metrics` (fetch `/raw` from the local agent via `urllib`/`requests`, parse the JSON, and print it in Prometheus line format `metric_name value`). Separately, spin up a raw `socket` TCP server on port 8081 in another thread, reading newline-terminated commands from each connection; on `CHECK\n`, re-query the storage agent's JSON and compute `used > 0.9 * quota` to decide `ALERT\n` vs `OK\n`. Run both listeners concurrently (threads or separate processes) so both ports stay live.