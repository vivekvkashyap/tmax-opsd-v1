Treat the stripped binary as a black box: probe it with carefully chosen single-token inputs to isolate the two unknown coefficients before writing any Rust. Feed a token of length L with 0 vowels to read off C1's contribution modulo 256 (try several lengths, including ones that wrap past 256, to pin down C1 uniquely rather than assuming the first guess is right), then hold length fixed and vary vowel count to isolate C2 the same way. Also probe edge cases explicitly: empty stdin, a trailing newline, a single trailing/leading comma, and consecutive commas — do not assume comma-splitting matches a naive `split(',')`; C tokenizers commonly use `strtok`, which silently collapses consecutive delimiters and skips empty tokens, which is a common naive-implementation trap. Verify the empty-input edge case output separately. Once you're confident in the tokenization rule and the two constants, implement the Rust program to mirror that exact tokenization and the `(len*C1 + vowels*C2) % 256` formula, then compile with `rustc` to the required path and run a batch of random comma-separated fuzz inputs yourself, diffing your output against the original binary before declaring done.
