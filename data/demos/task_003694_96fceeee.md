Parse the log with a strict line format matcher (e.g. `sscanf` on `"[%*[^]]] %s %[^=]=%s"` or split on the closing bracket, then on the first space, then on `=`) rather than assuming fixed column offsets, since timestamps have fixed width but keys/values vary. Use a simple array/struct of up to 1000 `(server, key, value)` entries: for each parsed line, linear-scan for an existing entry with the same server+key and overwrite its value in place (this naturally keeps "last seen wins" since the file is read top-to-bottom); if not found, append a new entry. After reading the whole file, sort the entries with `qsort` using a comparator that orders first by server (strcmp), then by key (strcmp) for ties. Write the CSV with the exact header `server,key,value` and one row per sorted entry, no trailing blank line issues. Compile with `gcc -o /home/user/track_config /home/user/track_config.c`, run it, and diff the resulting CSV against what you'd expect by manually tracing the log — the main pitfalls are treating duplicate identical lines as separate entries (they must dedupe like any other update) and getting the sort tie-break order backwards.
