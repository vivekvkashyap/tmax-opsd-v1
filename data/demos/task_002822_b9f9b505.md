Build the pipeline as discrete, testable stages rather than one big sed/awk one-liner. Parallelism: split /home/user/corrupt_logs.jsonl into per-line jobs using `xargs -P$(nproc) -n1` or GNU `parallel`, each invoking a helper that transforms one line and prints the result; do the dedup pass afterward as a sequential stage since it needs a global view. Unicode fix: for each `\u` occurrence, check if the following 4 characters are all valid hex digits; if not, replace the whole 6-char sequence with `�` (a scripting language regex with a manual hex-digit check is more reliable here than plain sed for this conditional logic). IP masking: replace the last dot-segment of the `ip` field with `XXX`. Dedup: for each processed line, hash the `message` field with sha256 and keep only the first line per unique hash (do this stage single-threaded to avoid races). Add the hash as a new `msg_hash` field and write surviving lines to /home/user/clean_logs.jsonl, validating each with `jq .`.
