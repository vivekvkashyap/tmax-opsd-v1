Scaffold with `cargo new /home/user/space_saver` and add `walkdir`, `flate2`, and `tar` to `Cargo.toml` for traversal and archiving. Walk `/home/user/old_records` recursively, filtering to entries whose extension is exactly `.txt` or `.log` (skip everything else, e.g. `.csv`, untouched) — for each matching file, read it line-by-line, drop lines where the line *starts with* the exact literal `[VERBOSE-DUMP]` (use `line.starts_with(...)`, not a substring/contains check, so partial or embedded occurrences aren't mishandled), count how many lines you drop, and overwrite the file in place with the filtered contents (write to a temp file then rename, or just write the retained lines back). Sum the removed-line count across all files as you go. After all files are cleaned, build the gzip tarball of the whole `old_records` tree (flate2 GzEncoder wrapping a tar::Builder, or shell out to `tar -czf`) at the specified path, preserving the directory structure. Then remove the original directory entirely (`std::fs::remove_dir_all`) — order matters: archive before delete. Finally write the single total count (no extra text) to `/home/user/summary.txt`. Build with `cargo build --release` and actually run the binary so the final filesystem state is produced, not just compiled.
