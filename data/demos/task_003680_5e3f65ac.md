Start with OCR: run `tesseract /app/fallback_config.png stdout` to read the fallback rules (lines like `SourceLocale -> TargetLocale`); split each line on `->`, trim, and write `Source,Target` pairs to `/home/user/fallbacks.csv` — visually cross-check the OCR text against the image, since dashes/hyphens are easy to misread. For the C sanitizer, implement a stateful CSV parser (not naive split-on-comma): read char by char, tracking an in-quotes flag so commas/newlines inside quoted fields don't break boundaries, and handle doubled quotes (`""`) as an escaped quote. For UTF-8 validation, write a byte-sequence validator checking continuation-byte patterns and rejecting overlong encodings/invalid leading bytes; apply it to every column and drop the whole row if any column fails. For gap-filling, when Translation is empty, look up Locale in the loaded fallback map and if found set the field to `[FALLBACK_APPLIED]`. Load `fallbacks.csv` from argv[1] at startup, then stream stdin to stdout. Test incrementally against small hand-built multi-line/quoted/invalid-UTF-8 snippets before trusting the corpora.
