Work through the four sub-problems independently and verify each before moving on. First fix the build: inspect the Makefile's `test` target for indentation errors (make requires tabs, not spaces, before recipe lines — `cat -A Makefile` will show `^I` vs spaces) and fix `calc_core/config.py` to provide a sensible default for `CALC_LOG_DIR` instead of raising `KeyError` when it's unset (e.g. `os.environ.get("CALC_LOG_DIR", "/tmp")`). Next, in `parser.py`, trace the length==0 path: the header gives a length field, and payload slicing/`struct.unpack` must special-case zero elements to return an empty array immediately rather than calling unpack with a format string expecting at least one double. Then rewrite the variance calculation in `math_ops.py` away from `sum(x**2)/n - (sum(x)/n)**2` (which cancels catastrophically for large means) to a single-pass Welford update (track count, mean, M2 incrementally) so it matches the C oracle's likely stable algorithm bit-for-bit. Validate the whole pipeline by running `python -m calc_core.cli` against the same random binary inputs as `/app/bin/legacy_calc_ref` and diffing outputs directly, not just eyeballing them. Finally, write regression.py to construct a `test.bin` matching the documented sensor format (4-byte "SENS" signature + 4-byte length + doubles) that specifically exercises the original cancellation bug (large mean, tiny variance), run the fixed CLI on it, and log stdout/status to regression_log.txt.
