First transcribe `/app/audit_memo.wav` (e.g. with a local speech-to-text tool such as `whisper` if available, or another ASR method) and listen for the specific risk-score threshold and comparison direction the CCO states — this exact number is the filter condition your BFS must apply, so get the wording right (inclusive vs exclusive of the threshold). Then write `/home/user/audit_path_finder.py`: load `/app/corporate_entities.json`, build an adjacency structure (dict from entity_id to its "connections" list, skipping/pruning any entity whose risk_score exceeds the transcribed threshold entirely — excluded nodes must not be traversed through even as intermediate hops), and run a standard BFS from `CORP-ROOT` to the CLI-supplied target id, reconstructing the shortest path via parent pointers. Print the path as comma-joined entity IDs with no extra whitespace/text, or exactly `NO_COMPLIANT_PATH` if unreachable. Since correctness will be checked against a compiled oracle across ~200 random IDs, use efficient data structures (dict/set lookups, deque for BFS) so it scales to a 10k-node graph, and make sure stdout contains nothing but the required line.