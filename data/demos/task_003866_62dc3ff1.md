Extract frames with `ffmpeg -i /app/manufacturing_feed.mp4 -vf fps=5 frame_%05d.png` so each frame's timestamp is `(frame_index-1)/5.0` seconds. Write a first script (e.g. Python with PIL/numpy) that loads consecutive frame pairs, computes MSE of pixel intensities between them, and writes out a per-frame-transition MSE series (index/timestamp + value) to an intermediate file. Write a second script in a genuinely different language (Awk, Perl, or Bash) that reads that MSE series and computes, for each point, a rolling mean over the trailing 10 values plus a rolling standard deviation over the same window, flagging a timestamp as anomalous only where the raw MSE exceeds (rolling_mean + 3*rolling_stddev) — this "current value vs. its own trailing window statistics" changepoint approach is the key technique, not a global threshold. Round flagged timestamps to 1 decimal and dedupe adjacent triggers from the same physical event (an abrupt transition may span several consecutive frames) so you output one timestamp per real anomaly rather than a burst of near-duplicates. Wire both scripts together in `/home/user/run_pipeline.sh` and write the final JSON array to `/home/user/anomaly_timestamps.json`; validate the JSON parses and is a flat list of numbers before finishing.
