Break this into independent pieces and validate each before integrating. First, extract a single representative frame from the video with `ffmpeg` (e.g. `ffmpeg -i /app/operator_demo.mp4 -vf "select=eq(n\,0)" -vframes 1 frame.png`, trying a few frame numbers if needed since it's flashing) and decode it with `zbarimg` (install `zbar-tools` if missing) to recover the `TARGET_DIR=...;BACKING_SIZE=...` string — parse this string in the wrapper script rather than hardcoding values. Second, fix `run_operator.sh`: since it runs under `PATH=/bin`, explicitly set `PATH=/usr/local/bin:/usr/bin:/bin` (or use absolute paths to tools/binary) at the top, add `set -euo pipefail`, and export the decoded `TARGET_DIR`/`BACKING_SIZE` as env vars before invoking the compiled Go binary. Third, in `operator.go`, replace the sequential `for` loop with a worker-pool or per-manifest goroutine pattern using a `sync.WaitGroup` (and a buffered channel/semaphore to bound concurrency and avoid excessive file-descriptor use across 10,000 files) — remove the artificial `time.Sleep`, since that alone is likely the biggest slowdown. Ensure directories are created with `os.MkdirAll` and symlinks are relative. Rebuild with `go build`, run `bash run_operator.sh`, and time it to confirm it's under 1.5s before considering the task done.
