Break this into extraction-then-Rust-processing. Extract the outer tarball with `tar -xzf` first so you can inspect the nested `.zip` files and confirm they contain `.log` files before writing any Rust code. Scaffold with `cargo new archive_processor` and add the `zip` and `encoding_rs` crates to `Cargo.toml`. In the program: open each `.zip` with the `zip` crate, read each `.log` entry's raw bytes, and decode them from Latin-1/ISO-8859-1 to a Rust `String` using `encoding_rs::WINDOWS_1252` or `ISO_8859_1` (`.decode()` returns UTF-8-valid Rust strings automatically) — do NOT use `String::from_utf8` directly on the raw bytes, since that will fail or corrupt non-ASCII bytes like `\xE9`/`\xB0`/`\xE4`. Parse the first line for the `ARCHIVE-ID: <id>` prefix (strip the prefix, trim whitespace/newline) to get the id used as the output filename `<id>.txt`, and write the full decoded content (including that first line) to `/home/user/processed_logs/<id>.txt`. After running the Rust binary, list the produced files, sort their basenames alphabetically, and write them one-per-line to `/home/user/processed_manifest.txt`.
