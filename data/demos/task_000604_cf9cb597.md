Write `/home/user/operator.sh` as a sequential pure-bash script and make it idempotent from the start (truncate/recreate output files at the top of the run before appending). Step order matters: (1) `mkdir -p /home/user/backups` then `tar -czf /home/user/backups/manifest_backup.tar.gz -C /home/user/active_manifests .` to snapshot the *current* (pre-sync) state — do this before copying in new manifests; (2) `cp -f /home/user/incoming_manifests/*.yaml /home/user/active_manifests/` to sync, overwriting on name collision; (3) reset `/home/user/mock_fstab` and `/home/user/fw_rules.sh` to empty (`> file`), then for each `.yaml` under `active_manifests`, check `grep -q '^kind: PersistentVolume'`; if matched, pull the volume name and host path with `grep -oP '^  name: \K.*'` and `grep -oP '^    path: \K.*'` respecting the exact leading-space indentation given in the spec, and append the formatted fstab line; similarly for files matching `^kind: Service`, extract `nodePort` with `grep -oP '^    nodePort: \K[0-9]+'` and append the iptables rule. Use `awk`/`grep -oP`/`sed`, not Python. Run the script once at the end and inspect all three output artifacts to confirm they match the expected content exactly.