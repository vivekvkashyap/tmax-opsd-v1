Implement Newton's method exactly as specified and be careful about the stopping/iteration-counting convention, since off-by-one errors here will silently shift every downstream number. For each C, iterate `x = x - f(x)/f'(x)` where `f(x) = x^3 - 3x - C` and `f'(x) = 3x^2 - 3`, starting at `x0 = 3.0`; check `|f(x)| <= 1e-6` — decide clearly whether you check convergence before or after each update step and count iterations consistently with "if it doesn't converge within 100 iterations, record 100" (i.e. check convergence at each of the 100 allowed steps, not 100 additional steps after an initial check). Repeat identically for `C' = C + 1e-4` using the same x0 and parameters. Classify "unstable" by `|root(C) - root(C')| > 1e-2`. Build empirical PMFs of the iteration-count lists for original vs perturbed runs (a dict of iteration-count -> frequency/total), then compute TVD as half the sum of absolute differences over the union of all iteration-count keys appearing in either distribution — don't forget keys that appear in only one distribution (treat missing as probability 0). Write exactly 3 lines to the report: unstable count, TVD rounded to 4 decimals, and max iteration count over the *original* (unperturbed) runs only.
