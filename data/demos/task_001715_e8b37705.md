Diagnose the two independent bugs separately before touching anything else. First, `cat /app/backup_graph_server/start.sh` and look closely for a typo in the Flask invocation (compare each token against the real `flask` CLI and its `--host`/`--port` flags) — fix the command so it launches `flask run` bound to `0.0.0.0:9000`. Second, open /app/backup_graph_server/queries.py and read the SPARQL WHERE clause carefully: it currently selects `?server` and `?job` as two unconnected patterns, producing a Cartesian product. Fix it by adding an explicit triple linking them through the stated predicate, e.g. `?server ex:hasJob ?job .` (using `<http://backup.local/vocab#hasJob>`), so only servers that actually own a given failed job are joined to it — keep the date/status filters on the job pattern. After both fixes, start the service in the background (`nohup ./start.sh &` or similar) and verify independently with `curl` against `GET /api/failed_backups?date=YYYY-MM-DD` that the returned JSON array only contains true server/job pairs (spot-check by comparing count of returned rows to the number of actually-failed jobs for that date in /home/user/backups.ttl, not server_count * failed_job_count).