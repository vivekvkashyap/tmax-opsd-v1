Write `/home/user/process_logs.py` as a standard ETL: use `csv` module to read `/home/user/input_logs.csv`, uppercase the `method` column, and mask `ip_address` by keeping only the first two octets and replacing the last two with literal `X.X` (split on `.` and rejoin — don't use a regex that risks mangling short/malformed IPs). For deduplication, build the hash key string in the exact order `IP|METHOD|ENDPOINT|USERAGENT` using the already-masked/normalized values (order of operations matters: mask and normalize BEFORE hashing), compute `hashlib.sha256(...).hexdigest()`, and when the same hash appears more than once keep only the row with the earliest timestamp — the natural approach is to first collect all rows, then group by hash and pick the min by parsed timestamp, rather than deduping in a single forward pass (which would incorrectly keep the first-seen row regardless of timestamp order). Sort final rows by timestamp ascending before writing to `/home/user/cleaned_logs.csv` with original headers preserved. Append the stats line to `/home/user/pipeline.log` using `datetime.utcnow()` formatted as `YYYY-MM-DD HH:MM:SS`. Finally install a crontab entry (`crontab -l | { cat; echo "0 * * * * /usr/bin/env python3 /home/user/process_logs.py"; } | crontab -`) rather than editing the file by hand, to avoid corrupting existing entries.