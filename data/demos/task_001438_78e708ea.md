Follow standard signal-processing methodology rather than eyeballing the data. Load `/home/user/signal.csv` with pandas, compute the sample spacing from consecutive `t` values, and run `numpy.fft.rfft` on `y`; take the magnitude spectrum, zero out (or simply skip) the index corresponding to 0 Hz, and find the frequency bin with the largest magnitude — that's your `f_dom`. Then fit `y(t) = A*sin(2*pi*f_dom*t + phi) + C` using `scipy.optimize.curve_fit`, seeding reasonable initial guesses (e.g. A0 = (max(y)-min(y))/2, C0 = mean(y), phi0 = 0) so the optimizer converges to the true parameters rather than a local minimum. After fitting, normalize the sign convention: if the fitted amplitude comes out negative, flip its sign and shift phase by pi, then wrap phase into `[-pi, pi]` (e.g. via `numpy.arctan2(sin(phi), cos(phi))`). Round all four values to exactly 2 decimal places and write them to `/home/user/output.txt` in the exact labeled multi-line format specified — run the script and check the file's contents match the required layout before finishing.
