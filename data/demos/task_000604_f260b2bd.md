Start by running `tesseract /app/config_rules.png -` (or preprocess with ImageMagick first if the raw OCR is noisy) to recover the three `KEY=VALUE` lines; parse them into variables rather than hardcoding numbers you haven't actually read. Then write `/home/user/process_graph.py` as a stateless stdin->stdout filter: read the JSON array once, drop edges matching `EXCLUDE_TYPE` or with `weight > MAX_WEIGHT`, then build an undirected degree/weight-sum tally per node by incrementing both `src` and `dst` once per surviving edge (do not double count a node's own self-loop specially unless the data requires it — just apply the same increment rule uniformly). Filter nodes with `degree < MINIMUM_DEGREE`, then sort by degree descending, tie-broken by node id ascending. Wrap JSON parsing and the whole pipeline in a try/except so malformed or empty input still prints `[]` and nothing else touches stdout. Since this will be checked by fuzzing against a hidden oracle across many random inputs, test your script manually with edge cases (empty array, all edges excluded, ties in degree) before finishing.