Model the data as a directed graph: build an adjacency structure from `entity_id -> [(target_id, percentage), ...]`, but first filter edges to keep only those with `percentage >= 25`. Find all simple 3-cycles by, for every node A, iterating its filtered out-edges to B, then B's filtered out-edges to C, then checking whether C has a filtered edge back to A — collect the triple (A,B,C) whenever this closes. Normalize each found cycle by sorting its three entity_ids alphabetically into a tuple/list, then deduplicate using a set of these normalized tuples (since the same cycle can be discovered starting from any of its three members, dedup is essential or you'll triple-count). Sort the deduplicated list of 3-element lists lexicographically (Python's default list/tuple comparison does this correctly). For pagination, compute the 0-indexed slice for "page 2" with page size 5 as `sorted_list[(page-1)*page_size : page*page_size]` — be careful to use 1-indexed page numbers as specified but 0-indexed slicing internally. Write only that slice as a JSON array to `/home/user/compliance_report.json`.
