This is a multi-file dependency-ordering and IPC problem — sequence the scripts to match the actual runtime dependency chain, not the order they're listed. Write `init_db_auto.exp` using `expect`'s `spawn`/`expect "Enter admin password to initialize DB: "`/`send "migrate123\r"`/`interact` (or `expect eof`) pattern so it drives `init_db.sh` non-interactively. Write `env_setup.sh` to simply `export DATA_PATH=/home/user/migration_project/data` so it can be `source`d by other scripts. For the Rust fix, replace the immediate `.expect()` panic on the missing flag file with a retry loop: attempt to read `$DATA_PATH/db_ready.flag` up to 10 times, sleeping 1 second between attempts via `std::thread::sleep(std::time::Duration::from_secs(1))`, and only panic/exit non-zero after all 10 attempts fail; on success, append (not overwrite) `"Connection established\n"` to `app.log`. In `deploy.sh`, the ordering matters: source `env_setup.sh` first, `cargo build --release` the Rust app, launch the compiled binary in the background (`&`) *before* running the DB init script (this is what actually exercises the retry-loop fix, since the binary starts polling for a flag file that doesn't exist yet), then run `init_db_auto.exp`, then `wait` for the backgrounded process. Chmod all new scripts executable and run `./deploy.sh` end-to-end to confirm `app.log` gets the expected line.