Do the steps in the exact order specified, since the "before" plan must reflect the unoptimized database and the "after" plan must reflect it post-index. First write the recursive CTE (start from `id = 42`, recursively join `employees e ON e.manager_id = o.id`, select all columns, order by `id`) and confirm it returns a sensible subtree by running it in the `sqlite3` CLI. Using Python's `sqlite3` module, run `EXPLAIN QUERY PLAN <query>` against the pristine (un-indexed) database and write each row's `detail` field, one per line, to `/home/user/plan_before.txt` — at this point expect to see a full table `SCAN` on `employees` for the recursive step because there's no index on `manager_id`. Then create `CREATE INDEX idx_manager ON employees(manager_id)`, re-run the identical `EXPLAIN QUERY PLAN` call, and save that output to `/home/user/plan_after.txt` — verify it now shows a `SEARCH`/index usage referencing `idx_manager` instead of a scan. Finally re-execute the CTE (now index-accelerated) and export to `/home/user/reports.csv` using Python's `csv` writer with header exactly `id,name,manager_id`, rows sorted ascending by `id`. Do all of this via one Python script so the plan-before/plan-after ordering relative to the index creation is guaranteed correct.
