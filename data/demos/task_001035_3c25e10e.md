Use a packet-parsing tool like `scapy` or `tshark`/`tcpdump -r` in Python/scripted form to read `/home/user/traffic.pcap`, filter to UDP port 8080 packets, and extract each payload's raw bytes as a JSON string — don't hand-transcribe payloads from a hex dump. For each extracted JSON payload, pipe it into `python3 /home/user/math_worker.py` via stdin and check the exit status/stderr to see which one aborts (a segfault/abort will show a non-zero/negative return code or SIGABRT); use `strace -f` if you need to see exactly where it dies. Once you've identified the crashing payload's `data` array, perform delta debugging: repeatedly try removing elements (or subsets, e.g. binary-search style halving followed by single-element removal) and re-running the worker on the reduced array, keeping only reductions that still cause the crash, until no further element can be removed without the crash disappearing — this converges on the minimal reproducing subset. Sort the final minimal array ascending and write it as `{"data": [...]}` to `/home/user/minimal.json`.
