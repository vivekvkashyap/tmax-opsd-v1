Extract the ground-truth video parameters first: run `ffprobe` (e.g. `-v error -select_streams v -show_entries stream=avg_frame_rate,nb_frames -of csv`) against `/app/touch_recording.mp4` to get the integer FPS and total frame count — do this once by hand to understand the values, then reproduce the same `ffprobe` invocation inside `CMakeLists.txt` via `execute_process`, capturing its output into CMake variables and passing them to the compiler with `target_compile_definitions(... VIDEO_FPS=... MAX_FRAMES=...)` so the build is self-configuring rather than hardcoding numbers you read manually. In `smoother.cpp`, compute `alpha = 2.0/(VIDEO_FPS+1.0)` from the macro, read x/y pairs from stdin until EOF, apply the EMA update per the given formula, stop reading additional pairs once you've consumed `MAX_FRAMES` of them, and print with `printf("%.4f %.4f\n", x, y)`-style fixed 4-decimal formatting so results are bit-exact against the oracle. Build into `/home/user/build/`, then write `benchmark.sh` to generate random pairs, pipe them through both binaries, and `diff` the outputs — resolve any mismatch (rounding mode, alpha precision, EOF handling) before considering the task done.