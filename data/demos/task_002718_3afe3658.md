Build this pipeline as a sequence of verifiable stages in pandas rather than one monolithic block, checking intermediate output at each stage. Load both CSVs, inner-merge on `timestamp`, and sort chronologically before computing anything else — order matters because rolling windows are order-dependent. Compute `sensor_A_roll3`/`sensor_B_roll3` with `.rolling(window=3, min_periods=1).mean()` on the already-sorted merged frame, then drop any resulting NaNs (there likely won't be any given `min_periods=1`, but check). For correlation-based feature selection, compute the Pearson correlation matrix over just the 7 candidate feature columns (5 raw sensors + 2 rolling), then scan the upper triangle for any pair with `abs(corr) > 0.85`; for every such pair, drop whichever of the two names sorts later alphabetically — apply this over all flagged pairs consistently and keep the surviving names sorted alphabetically in the report. For modeling, call `np.seterr(divide='raise')` before fitting, set up `KFold(n_splits=5, shuffle=False)` (no random_state needed since shuffle=False), run `GridSearchCV` with `Ridge()`, `param_grid={'alpha':[0.1,1.0,10.0,100.0]}`, `scoring='neg_mean_squared_error'`, and `cv=` that KFold object, fit on the selected features vs `target_value`. Extract `best_params_['alpha']`, and take `-1 * best_score_` rounded to 5 decimals as `best_cv_mse`. Write the three fields to `/home/user/pipeline_results.json` exactly as specified and re-open the file to confirm valid JSON and correct key names.
