Split this into fix-the-code, fix-the-hook, and fix-the-tunnel, verifying each independently before chaining them. First, inspect the vendored `Makefile` and `processor.cpp`: fix the compiler variable so C++ is actually invoked (`g++`/`CXX`, not `gcc`, when linking a `.cpp` file with `<iostream>`), and fix the transformation so each input byte is XORed with the exact constant given in the spec (re-check against the spec, don't assume the current constant is close-enough) and printed as uppercase zero-padded two-digit hex plus a trailing space — verify manually with a single-byte `echo -n "A" | ./telemetry_processor` test. Second, `git init --bare /home/user/device-repo.git`, write `hooks/post-receive` starting with an explicit `export PATH="/usr/bin:/bin:/usr/local/bin:$PATH"` (a git hook's PATH is minimal, so unqualified `make`/`cp` calls silently fail otherwise), then checkout to `/home/user/build_workspace`, run `make`, copy the binary to `/home/user/deploy/telemetry_processor`, and write the exact success string to the log; chmod it executable. Third, init a repo in the fixed source, commit, add the bare repo as remote `local`, push `master`, and confirm the hook actually produced the deployed binary and log before setting up the SSH local port forward from 7070 to 8080.
