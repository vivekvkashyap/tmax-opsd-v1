Do the phases in order since each depends on the last. Phase 1: `mkdir src include`, `git mv`/`mv` the `.c` files into `src/` and `.h` files into `include/`, then edit the Makefile's compile rules to reference `src/*.c` and add `-Iinclude` to CFLAGS, keeping the final link target as `http_parser` in the project root (adjust object-file paths too, e.g. build `.o` files into `src/` or a build dir, whichever keeps `make clean && make` working from the project root). Phase 2: in `parser.c`'s `parse_requests`, the bug is that `strtok` returns pointers into the reused stack buffer `line` — fix by calling `strdup()` (or manual `malloc`+`strcpy`) on each of method/uri/version before storing them in the `Request` struct, so each entry owns its own memory independent of the next loop iteration; correspondingly update `free_requests` to `free()` each of the three strings per request before freeing the array itself, to avoid leaks. Phase 3: write `benchmark.sh`, `chmod +x` it, and have it run `make clean && make`, then `time ./http_parser requests.log`, piping/capturing its stdout through `sort > sorted_output.txt`. Verify output ordering and correctness by hand before finishing.
