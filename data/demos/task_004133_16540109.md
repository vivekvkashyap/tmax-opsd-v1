Build this as a clean pipeline of well-tested stages rather than one monolithic script. First write a robust FASTA parser that handles multi-line sequences (accumulate lines until the next `>` header or EOF) and preserves each sequence's ID. Map nucleotides to the given numeric values with a dict/lookup, then use a real FFT routine (e.g. `numpy.fft.fft`, not a hand-rolled DFT) on each numeric array, take `|X[k]|^2` for the PSD, and integrate it with the trapezoidal rule (`numpy.trapz(psd, dx=1.0)`) — do not use a naive rectangle-sum approximation, as it will diverge from the expected result. Parallelize with `multiprocessing.Pool(processes>=4)` mapping each sequence to a worker function that returns `(id, energy)`; verify the pool actually reports `>=4` workers used in your output. After collecting all results, find the max-energy id, sum all energies and round to 1 decimal, then write the exact three keys to `/home/user/summary.json` with `json.dump`. Sanity-check a couple of sequences by hand (or with a tiny standalone script) to confirm your FFT/PSD/integration chain is correct before trusting the aggregate numbers.