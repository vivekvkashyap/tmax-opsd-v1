Part 1 is mechanical: run the pre-installed `/usr/local/bin/transcribe /app/field_report.wav` and redirect its stdout straight to /home/user/initial_report.json — don't try to reimplement transcription yourself, just capture the tool's output. Part 2: write etl_processor.go to decode JSON records from stdin (use `encoding/json` with a `Decoder` in a loop, or `bufio.Scanner` if records are newline-delimited — check which framing the input actually uses before assuming) into a struct matching the given schema, filter out any record with confidence < 0.85 or an empty id, then compute the Levenshtein edit distance between reported_name and expected_name yourself (implement the classic DP table over rune slices — don't assume a stdlib helper exists) and the transcript_text length in characters (be careful about counting runes vs bytes if non-ASCII text ever appears). Emit one CSV line per surviving record with the five exact fields in order, no header. Build with `go build -o /home/user/etl_processor /home/user/etl_processor.go` and manually test it against several hand-crafted edge-case JSON lines (empty id, low confidence, unicode names, empty strings) piped into the binary to confirm exact output formatting before trusting it, since it will be fuzz-tested against an equivalence oracle.