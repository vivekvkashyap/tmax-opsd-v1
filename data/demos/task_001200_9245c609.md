Start by extracting the secret token: transcribe `/app/memo.wav` (use a local speech-to-text tool available in the environment, or write a small script leveraging whatever ASR library/model is installed) and read off the exact token spoken — do not guess or hardcode a plausible-looking token, verify it against the audio content. Then design the RLE codec precisely as specified: for each maximal run of identical bytes, emit the character followed by a single count byte (1-255); if a run exceeds 255, split it into multiple (char, 255) blocks plus a remainder block — get this boundary case right since naive implementations often output 256 as two bytes or forget to reset. Implement the HTTP server in C++ using raw POSIX sockets (`socket`/`bind`/`listen`/`accept`/`recv`/`send`), manually parsing the request line, headers (to extract `X-Backup-Token` and `Content-Length`), and body — don't assume a library exists. Validate the token on every request and return a non-2xx status when it's missing or wrong. For `/upload`, RLE-encode the body and write raw bytes to the archive file; for `/download`, read and decode the archive back to text and return it verbatim. Test locally with `curl -H "X-Backup-Token: <token>" --data ... http://127.0.0.1:8080/upload` before declaring done, and run the server in the background so it persists.