Three connected steps. First, patch the vendored C source in `/app/minitar-0.1`: find where the tar header's `name` field is used to open/create the extraction target (likely a raw `fopen(header.name, ...)`), and before any file is written, validate every entry's path — reject the whole archive if any entry's name starts with `/` or contains a `../` traversal component. On rejection print exactly `REJECT: <archive_name>` and exit 1 without extracting anything; on an all-clean archive, extract everything as before, print exactly `ACCEPT: <archive_name>`, and exit 0. Rebuild with the existing `make` in that directory so the binary lands at `/app/minitar-0.1/minitar`. Second, test your fix directly against `/app/corpora/clean/` and `/app/corpora/evil/` — run the binary on each archive and confirm exit codes/output match the accept/reject expectation for every file before moving on; a single archive with even one bad entry among otherwise-safe entries must still be fully rejected. Third, write `/app/watch_and_extract.sh` using `inotifywait` in a loop watching `/app/incoming/` for new `.tar` files, invoking your fixed `minitar -x -f <file>`, appending `sha256sum` results for each successfully extracted file to `/app/extraction_manifest.txt`, and redirecting minitar's stderr to `/app/extraction_errors.log`.