Follow the pipeline stages in exactly the order and with exactly the randomness protocol specified, since reproducibility hinges on precise API sequencing. Split the CSV into labeled/unlabeled subsets by whether `target` is NaN, fit `sklearn.naive_bayes.GaussianNB()` on the labeled rows' three sensor features, and call `predict_proba` on the unlabeled rows to get P(class=1) — average and round these to get `avg_posterior_class1`. For sampling, instantiate `rng = np.random.default_rng(42)` exactly once outside the loop, then for each of the 50 iterations (in order 1..50) draw `rng.random(size=num_unlabeled)` once per iteration and threshold against the same fixed posterior probabilities computed earlier (don't re-fit or re-predict per iteration) to assign imputed labels. For each completed dataset, run `GridSearchCV` with a fresh `LogisticRegression(max_iter=1000, random_state=42)`, `StratifiedKFold(n_splits=3)`, grid `{'C':[0.01,0.1,1.0,10.0]}`, scoring accuracy, fit on the full dataset (labeled rows unchanged + newly imputed unlabeled rows), and record `best_params_['C']`. After all 50 runs, take the mode of best C values, breaking ties by choosing the smaller C value (use a Counter and min of the max-count keys). Write both values to `/home/user/summary.json` with the exact key names specified.