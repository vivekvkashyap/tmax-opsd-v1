Treat this as black-box reverse engineering, not disassembly-first. Run `/app/legacy_net_quota.bin` with a handful of well-formed inputs (varying service name, port, uid, storage) and diff the outputs to spot the arithmetic pattern in each output field (the two trailing octets of the network address, and the storage quota number) — small, targeted probes reveal modular/linear relationships faster than reading stripped assembly. Also probe malformed inputs (missing fields, non-numeric port/uid/storage) to capture the exact error message and exit code. Once you have a hypothesis for each transformation, verify it against several more binary outputs before committing it to `/home/user/quota_router.py`. Read the input as a single argv string, split on whitespace, and reject anything that doesn't split into exactly four fields or where the numeric fields fail integer parsing — match the binary's exact error text and exit code, and make sure your success-path formatting (spacing, newlines, no trailing newline) is byte-identical since it will be fuzz-compared. Separately, `/home/user/setup_workers.sh` just needs `mkdir -p` for the five worker directories.
