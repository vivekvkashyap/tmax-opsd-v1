First transcribe `/app/voice_memo.wav` with `whisper` (or `whisper-cli`/`ffmpeg`+`whisper.cpp`) and read the transcript carefully — the exact filtering rule (a size threshold, comparison direction, what counts as "empty") is stated in the audio, not in the text task description, so do not guess it. Once you know the rule, write `/home/user/archive_tool` to: read a tar stream from stdin (Python's `tarfile` module opened on `sys.stdin.buffer` with mode `r|*` is a solid approach since you cannot seek stdin), iterate over members, select only regular files matching the audio's size rule, format each as `path:size`, sort the resulting lines alphabetically by path, and print them newline-separated to stdout. Common pitfalls: buffering stdin instead of streaming (wastes memory on large archives), using the wrong comparison operator (>= vs >) for the size threshold, forgetting to exclude directories/symlinks, and forgetting the final sort step. Since grading is bit-exact fuzz equivalence against a hidden oracle across many random tar inputs, test your script yourself by building small tar archives with `tarfile` and piping them through `cat archive.tar | ./archive_tool` to confirm exact formatting (no trailing whitespace issues, correct newline handling) before considering it done. Make the script executable with `chmod +x`.
