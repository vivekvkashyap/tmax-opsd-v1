Split this into an extraction phase and an alignment phase. First use `ffmpeg` to dump each of the 50 frames of `/app/base_calls.mp4` as separate images (e.g. `-vf fps=10`), then for each frame load the 100x100 image, convert to grayscale (or average RGB), split into the four 50x50 quadrants at the given coordinates, compute the mean intensity of each quadrant, and pick the base whose quadrant has the highest average — this reconstructs a 50-character query string. Do this extraction in Python/OpenCV/PIL since it's simpler for image work; you don't have to force the pixel analysis into Rust, only the FASTA parsing/comparison logic needs to be in Rust. Then write a Rust binary that reads `/app/dataset.fasta` (parse header/sequence pairs), reads the query string (e.g. from a file or stdin/arg passed by your pipeline), computes Hamming distance between the query and every 50bp reference sequence in parallel with `rayon` (e.g. `par_iter` over records), finds the minimum-distance record, and writes `/home/user/result.json` with the exact schema shown. Watch for off-by-one errors in quadrant boundaries and be careful the video codec doesn't introduce lossy compression artifacts that shift the brightest quadrant — use a lossless-friendly ffmpeg pipeline and threshold based on max quadrant rather than fixed intensity value.
