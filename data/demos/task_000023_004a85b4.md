Work in strict order: fix, install, run, analyze. First open `/app/mol_sim/mol_sim/integrator.py` and locate the step-size adaptation line — the task tells you the correct exponent is `0.2`, so find where a wrong power is used in the `(tol/err)**power` expression and correct just that exponent (don't touch other logic). Reinstall the package in editable mode (`pip install -e /app/mol_sim`) so the fix takes effect, then run `python3 /home/user/run_sim.py` to produce `/home/user/sim_output.csv` — confirm it has no NaNs before moving on. For analysis, isolate `node_0` vs `time`, fit `y = A*exp(-k*t)` (e.g. `scipy.optimize.curve_fit` with a sane initial guess like A=y[0], k=0.1, or fit `log(y) = log(A) - k*t` via linear regression if y stays positive), and separately fit a linear baseline `y = m*t + b`. Compute MSE for both fits over the same data and pick the lower-MSE model as `better_model`. For the bootstrap CI on `k`: resample the (time, y) pairs (or residuals) with replacement at least 500 times, refit the exponential model each time, collect the resulting `k` values, and take the 2.5th/97.5th percentiles as the CI bounds — do this empirically rather than assuming a closed-form CI. Write results to `/home/user/analysis.json` in the exact specified schema.