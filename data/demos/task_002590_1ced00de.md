Treat the stripped `/app/legacy_calc` binary as a black-box spec, not something to reverse-engineer byte-by-byte: feed it small, hand-crafted 12-byte records (pack a known `u32` sensor_id + `f64` reading with Python's `struct.pack('<Id', ...)`) via stdin and observe the JSON it emits to pin down the record format and aggregation rule (sum per sensor_id) before touching the Rust code. In the Rust codebase, hunt for the three classic bug categories separately: (1) precision — grep for any `f32` used to accumulate sums instead of `f64`, since repeated f32 accumulation over millions of records drifts significantly; (2) concurrency — look at how Rayon parallel chunks share the accumulator (`Arc<Mutex<...>>` or similar) for dropped updates from mis-scoped lock guards, unsafe aliasing, or chunks that don't get joined/reduced correctly; (3) serialization — check whether the JSON writer truncates precision (e.g. formatting floats with a fixed number of decimals via `{:.4}` instead of using `serde_json`'s native f64 serialization). Fix one bug at a time, rebuilding with `cargo build --release` and re-testing against small synthetic inputs where you can hand-compute the expected sums, before validating against a larger generated file compared against `/app/legacy_calc`'s output.
