This is a direct linear-algebra implementation task, so focus on numerical correctness and I/O correctness rather than exploration. Load `A` and `b` from `/home/user/input.h5` with `h5py.File(..., 'r')`, then compute Ridge regression exactly per the given closed-form: build `I = np.eye(A.shape[1])`, form `A.T @ A + lam * I` with `lam = 1e-4`, and solve with `np.linalg.inv(...) @ A.T @ b` (or `np.linalg.solve` for better numerical stability, which gives the same result within tolerance) — do not substitute an unregularized least-squares or a different lambda, since the near-singular collinearity is exactly why the regularizer matters. Write the resulting vector to `/home/user/output.h5` under dataset name `x_ridge` using `h5py.File(..., 'w')` and `create_dataset`. Separately compute `np.linalg.norm(x)` and format it to exactly 4 decimal places (e.g. with an f-string `f"{norm:.4f}"`) before writing it as plain text to `/home/user/norm.log` — watch for trailing newline/whitespace consistency since the check does an exact string comparison. Run the script once and re-open both output files to confirm shapes and formatting before finishing.
