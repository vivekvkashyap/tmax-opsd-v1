Work through the three broken layers in order, verifying each before moving on. First, fix the CMake build in `c_filter` so it actually produces `libaudiofilter.so` (check `add_library`/install rules), then fix `rust_server/build.rs` and `Cargo.toml` to correctly emit `cargo:rustc-link-lib` and `cargo:rustc-link-search` pointing at the built `.so`, and set `LD_LIBRARY_PATH` (or an rpath linker flag) so the compiled binary can find the shared library at runtime — a common naive mistake is fixing only the link-time step and forgetting the runtime loader path, causing a crash on startup despite a successful build. Next, read the Rust borrow-checker errors in `src/main.rs` literally and resolve them by restructuring ownership (e.g. cloning small buffers, scoping mutable borrows, or using `Arc<Mutex<..>>` for shared websocket state) rather than sprinkling `unsafe`. Confirm the server binds to 127.0.0.1:8080 and stays up. Then port `client_chunker.py`'s logic faithfully into `process_stream.sh`: validate the input file exists, stream it in fixed-size chunks with a `sleep` between chunks sized to enforce 50KB/s (e.g. `dd bs=<chunk> count=1` loops timed against the target rate), pipe to the server via `websocat`/`nc`, and capture the full response into `/home/user/processed_output.wav`. Test end-to-end against `/app/test_audio.wav` and compare the processed output's structure/integrity, not just that a file was produced.
