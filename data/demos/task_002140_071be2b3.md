First OCR the memo with `tesseract /app/policy_memo.png stdout` to recover the exact department prefix code and quarantine directory path — use these literal values from the OCR output, don't invent your own naming. In the Rust CLI, parse stdin by splitting on the `---` separator lines and extracting each `File:` value. For zip-slip sanitization, canonicalize manually (don't rely on filesystem `canonicalize()` since these paths don't exist on disk): join the raw path onto `/archive/` as the notional root, then collapse `.` and `..` components with a push/pop stack, never letting the stack go above the root, to get the resolved logical path. If it stays under `/archive/`, keep its directory structure but prefix the basename with the departmental code; if it escapes the root (or is an absolute path outside `/archive/`), discard the directory structure and place the prefixed basename directly under the quarantine path from the memo. Build in release mode so the binary lands at the required path, and test edge cases (`./`, multiple `../`, absolute paths) before relying on the fuzzer.
