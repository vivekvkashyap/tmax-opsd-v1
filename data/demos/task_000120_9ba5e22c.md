Structure `/home/user/run_etl.sh` as four sequential awk/shell passes over `/home/user/data/telemetry.csv`, each writing its own output file, so you can verify each phase independently before moving to the next. Phase 1 (cleaning): use awk with `-F','` to skip the header (`NR>1`), drop rows where `$3` (cpu) or `$4` (ram) is empty, and drop rows where cpu is `<0` or `>100`; write the surviving rows to `cleaned.csv`. Phase 2 (correlation): compute the Pearson correlation over `cleaned.csv` in one awk pass accumulating sum(x), sum(y), sum(xy), sum(x^2), sum(y^2), n, then apply the standard formula and `printf "%.3f"` to `correlation.txt` — double-check rounding matches typical `printf` rounding (not truncation). Phase 3 (Bayes): count rows with cpu>85 and, among those, rows with status=="CRASH"; divide and round to 3 decimals. Phase 4 (retrieval): for CRASH rows only, tokenize each log_message into lowercase alphabetic-only words (strip punctuation with `gsub`), compute Jaccard against the tokenized query set using associative arrays for intersection/union, track the max score with a tie-break on lowest timestamp, and output the message's *original* text (not the tokenized version) to `retrieval.txt`. Test each output file's exact content against your own manual recomputation before finishing.
