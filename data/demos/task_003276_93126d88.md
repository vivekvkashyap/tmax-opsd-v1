Build the HTTP server (`http.server` or a minimal framework) to listen on 127.0.0.1:9090, accept only POST `/process`, and reject requests missing the exact `Authorization: Bearer secr3t_t0k3n` header. For each request, pipe the raw POST body as stdin to `/app/sensor_decoder` via `subprocess.run(..., input=body, capture_output=True)` and parse its stdout as JSON lines — treat the binary as an opaque black box that just emits JSON, don't try to reverse-engineer its internal format. Process the parsed records in this order: (1) drop exact-duplicate timestamps keeping the first occurrence; (2) determine min/max timestamp and generate one row per integer second in that range, forward-filling `sensor_val` from the last known value for any second with no original record, and setting `text_note=""`, `user_id="SYSTEM"` on synthesized rows; (3) hash every `user_id` (including "SYSTEM") with SHA-256 hex; (4) normalize `text_note` with `unicodedata.normalize("NFC", ...)` then `.lower()` — do this normalization before comparing/deduplicating text if relevant, since inputs may arrive in NFD form. Emit the final rows as newline-separated JSON ordered by timestamp ascending, and log the record count per request to `/home/user/server.log`. Test end-to-end with a synthetic multi-record payload before considering it done.
