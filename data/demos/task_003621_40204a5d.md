Build the pipeline stage by stage and inspect output at each step rather than chaining blindly. Clone/build whisper.cpp, download the tiny.en model via its provided download script, then run the CLI binary on /app/voice_commands.wav and capture its text output to a file — check that file by eye to make sure it looks like real transcribed words before cleaning it. For cleaning, use tr/sed/grep to lowercase, strip timestamps (typically bracketed like `[00:00:00.000 --> ...]`), strip punctuation/digits, and split into one word per line into clean_words.txt — verify the word count and a sample of lines look sane. For the bootstrap step, implement it in pure awk/bash: for each of 5000 iterations, draw N samples with replacement (e.g. `shuf` with repeats via random index selection, or awk's `rand()` to pick random line numbers from clean_words.txt), compute that sample's mean word length, then after all 5000 iterations compute the standard deviation of those 5000 means (the standard bootstrap SEM) using awk/bc for the arithmetic — no Python/R permitted anywhere in this step. Cross-check your result loosely: it should be in the same ballpark as stddev(word lengths)/sqrt(n), since that's what bootstrap SEM approximates. Round both reported numbers to 4 decimal places.
