Extract the port and token first since everything else depends on the server binding correctly: run `tesseract /app/auth_spec.png stdout` (or preprocess with imagemagick if OCR is noisy) and parse out the PORT and TOKEN lines, using those exact values for the server's bind port and the X-Auth-Token comparison — don't guess or hardcode a different port. For config reconstruction, load base.csv as the state at time 2024-01-01T00:00:00, then parse changes.jsonl line by line rather than as one JSON blob — wrap each line's json.loads in a try/except (or pre-sanitize invalid `\u` escapes with a regex before parsing) so a single corrupted line doesn't kill the whole run; skip a line only if it truly can't be salvaged, but still recover fields when just the note is malformed. Discard any change event where cpu<1 or mem<128. Sort valid, in-range events by timestamp per service and implement forward-fill: for a queried time, find the latest change ≤ that time per service (or the base value if none). Build the HTTP endpoint to require the X-Auth-Token header (401 if missing/wrong) and compute the state on-the-fly per request. Keep the server running as a background process, and write the cron line for a fixed 2 AM daily job.
