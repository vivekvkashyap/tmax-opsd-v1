This task has two independent sub-problems: reading the image constraint, and implementing a robust shortest-path CLI. First, run OCR (e.g. `tesseract /app/filter_rule.png stdout`) to read the whiteboard note, and extract the exact predicate URI it names to exclude — treat the OCR output carefully, since OCR can mangle punctuation/angle-brackets in URIs, so sanity-check the recovered string looks like a well-formed predicate URI before hardcoding it into your script. Then parse `/home/user/dataset.ttl` with `rdflib`, build a directed graph (e.g. via `networkx.DiGraph`) from all triples EXCEPT those whose predicate matches the excluded URI, and compute shortest path length with `networkx.shortest_path_length`, catching `NetworkXNoPath`/missing-node errors to print `-1`. Since this will be fuzz-tested against many random URI pairs, make the script strict: print only the integer (no extra whitespace, warnings, or rdflib parser log spam — redirect/suppress those), accept exactly two positional argv args, and handle self-loops (distance 0) and unreachable/unknown nodes gracefully. Test it locally on a few known pairs from the dataset before considering it done.
