Design the SQL first in the `sqlite3` CLI, then embed it in C once correct. Build it as layered CTEs: (1) a `doc_authors` view expanding each document's `metadata` JSON authors array via `json_each(metadata, '$.authors')` joined to `documents`; (2) a `citation_pairs` CTE that joins `citations` to `doc_authors` twice (once for the citing document's authors, once for the cited document's authors) on `source_doc`/`target_doc`; (3) apply `DISTINCT` per citing-document/citing-author/cited-author/cited-document combination before counting, since a paper with 3 authors citing another paper with 2 co-authored papers should not multiply-count within one citing document to one target — reread the "co-authored same citing paper counts once" rule carefully when framing the DISTINCT/grouping. Exclude rows where citing author id equals cited author id (self-citation), then `GROUP BY citing author, cited author`, filter `HAVING COUNT(*) >= 2`, and `ORDER BY count DESC, citing name ASC, cited name ASC`. In the C program, use `sqlite3_prepare_v2`/`sqlite3_step` to run this query and write results to the CSV with `fopen`/`fprintf`, matching the header exactly. Test incrementally against the CLI output before wiring up C.