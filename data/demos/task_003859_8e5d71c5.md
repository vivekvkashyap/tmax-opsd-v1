Start by transcribing the audio with `whisper /app/emergency_voicemail.wav` (or similar) to recover the correct UNIX socket path and the secret bearer token spoken in the message — don't guess these values, read them off the transcript output. Before touching anything, `tar czf /home/user/nginx_backup.tar.gz -C /home/user nginx` to back up the config directory. Then edit `nginx.conf`: point the `proxy_pass` (or `upstream`) directive at the socket path from the transcript (NGINX syntax is `proxy_pass http://unix:/path/to.sock:;`), and add an auth check — since core NGINX has no native bearer-token comparison, use `if ($http_authorization != "Bearer <token>") { return 401; }` inside the relevant `location` block. Start the backend first via `/home/user/init_backend.sh start` (it must create the socket before nginx tries to connect to it), then launch `nginx -c /home/user/nginx/nginx.conf`. Verify end-to-end with `curl` against `127.0.0.1:8080` both with the correct header (expect 200) and without/wrong header (expect 401) before declaring done.
