Structure this as: HTTP server -> auth check -> subprocess call -> bytecode VM -> JSON response. Use `http.server` (or a minimal framework) bound exactly to `127.0.0.1:9090` with a `POST /process_artifact` handler; check the `X-CI-Token` header equals the required value and return 401 immediately if not. Parse the JSON body for `artifact_id`, run `/app/legacy_bytecode_gen <artifact_id>` in a fresh working directory (e.g. `tempfile.mkdtemp()` as cwd) so `output.bin` lands somewhere predictable, then read that file. Implement the VM precisely as specified: two 1-byte registers initialized to 0, all arithmetic mod 256, instructions read in fixed 3-byte groups (opcode, operand1, operand2), and dispatch on opcode `0x01/0x02/0x03/0x04/0xFF` exactly per the spec — stop as soon as `0xFF` is hit or bytes run out. Return `{"checksum": reg[1]}` as JSON with status 200. Test locally by invoking the binary yourself with a few artifact_id values and manually tracing a few instructions to sanity-check your emulator before trusting the server's output, since a single off-by-one in the opcode dispatch will silently produce a plausible-but-wrong number.
