Build this as a straightforward sequential ETL and check each stage's output before chaining the next. Parse `raw_expressions.csv`, strip whitespace, split each expression into left-operand/operator-word/right-operand, map the operator word to its symbol via a lookup dict, and reassemble as `"{left} {symbol} {right}"` with exactly one space on each side of the symbol (trim internal whitespace around operands too) — then evaluate arithmetically (integer division since the task guarantees clean division). Join the resulting records to `metadata.json` on `id`. Group by `category`, sort each group's records by evaluated result descending, and take the top 2 per group, breaking ties by higher `id`. Render each selected record through the exact template string given (mind spacing/punctuation precisely). Finally sort the whole output list by `category` ascending, then by `result` descending within a category, and write to `/home/user/report.txt` with a trailing newline per line. Diff your output against the required lines yourself before finishing — small formatting deviations (extra/missing spaces, wrong tie-break) are the likely failure points.