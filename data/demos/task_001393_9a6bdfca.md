First transcribe `/app/instructions.wav` yourself (play it or run it through a speech-to-text tool available in the environment) — do not guess the model or solver parameters, extract them precisely from the audio: the exact equation relating the sample mean to theta, which numerical root-finding method to use, the exact iteration count, and the exact initial guess. Once you have those parameters confirmed from the transcription, write `/home/user/fitter.cpp` to: read all whitespace-separated floats from stdin until EOF, compute their mean, then implement the specified iterative method by hand (no external solver libraries) using the exact starting value and exact number of iterations from the audio, updating theta each iteration using the function and its analytic derivative. Print the final theta with exactly 6 decimal places using stream formatting (`std::fixed << std::setprecision(6)`) followed by a newline. Compile to `/home/user/fitter`. Since this will be fuzz-tested against a reference across many random inputs, test with several manually constructed input sequences (including edge cases like a single number, negative numbers, and inputs near the function's domain edges) and confirm your output is stable and deterministic before finishing.