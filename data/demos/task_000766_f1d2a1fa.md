Treat this as a video-to-data recovery pipeline with an ordering dependency you must preserve. First extract frames from `/app/artifact_feed.mp4` at a rate matching the video's encoding (try 1 fps first; use `ffmpeg -i input.mp4 -vf fps=1 frame_%05d.png` so filenames sort in timeline order) — install `ffmpeg` and a QR decoder such as `zbar-tools`/`zbarimg` if not already present. Decode each frame's QR code with `zbarimg`, preserving the numeric frame order (glob-sort by filename, not by decode order). Some decoded strings may carry a scanner prefix like "QR-Code:" or duplicate/garbled reads — strip prefixes with `sed`/`awk` and detect/drop exact-duplicate consecutive chunks before concatenating. Concatenate the cleaned base64 chunks in frame order into one string, base64-decode it to reconstruct the `.tar.gz` bytes, and write to `/home/user/recovered_artifact.tar.gz` — verify it's a valid gzip stream (`file` or `gzip -t`) before trusting it. Extract into `/home/user/repo/`, then use `find /home/user/repo -type f -size +10k -not -newermt 2023-01-01` (checking modification/creation metadata as available) to locate qualifying files, sort names alphabetically, and concatenate into `/home/user/legacy_binaries.txt`.
