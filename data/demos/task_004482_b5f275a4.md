Design for both correctness and speed since there's a hard time limit. Fetch pages from `http://localhost:8080/transactions?page=N` until an empty response, but deduplicate rows by `tx_id` (set or `drop_duplicates`) as you accumulate — stale/overlapping rows are a known part of this API. Build a directed `networkx` graph from `sender_id -> receiver_id`, summing `amount` when the same edge repeats (aggregate before insertion, not via repeated edge writes which overwrite). Run Personalized PageRank via `networkx.pagerank(G, alpha=0.85, personalization={...}, weight='amount')`, seeding only `U-991`, `U-042`, `U-777` at 1.0. Sort remaining nodes by score descending, exclude the seed accounts, take the top 100. For performance, avoid rebuilding the graph or recomputing pagerank redundantly, and prefer vectorized pandas/networkx operations over Python loops. Push to Redis with `ZADD fraud_scores score member` (or a pipelined batch), then create the `done` marker — verify the sorted set is populated with `ZCARD`/`ZREVRANGE` first.
