Work through the three sub-problems independently, then assemble the JSON. For the log parsing, use grep/awk on `access.log` to find lines containing `/admin/keys.zip` AND a `200` status code, then extract the first field (IP) — don't just match the endpoint, the status code filter is essential since one attempt is a 403. For the password crack, read `shadow_leak.txt`, isolate the line for `audit_usr`, then iterate `wordlist.txt` line by line in Python, compute `hashlib.sha256(word.encode()).hexdigest()` for each candidate, and compare against the stored hash until you find a match — don't guess, verify programmatically. For the sudoers audit, grep `sudoers_audit.txt` for the line starting with `audit_usr` and parse out the `NOPASSWD:` binary path with a regex or split on whitespace. Finally write a small Python script that performs all three steps and dumps the three discovered values into `/home/user/audit_report.json` with exactly the keys `attacker_ip`, `cracked_password`, `privesc_command` using `json.dump`.