Solve this purely with POSIX text tools, building the pipeline stage by stage and checking intermediate output at each step. For schema/outlier filtering, use `awk -F,` to (a) require exactly `NF==4`, (b) require the cpu/memory fields to be non-empty, (c) validate they are numeric (a regex check like `$2 ~ /^-?[0-9]+(\.[0-9]+)?$/` guards against non-numeric junk), and (d) require both to fall within `[0,100]` inclusive — reject anything failing any check, but always keep the header line. For tokenization, pull the `status_msg` field from the *valid* rows only, then chain `tr` (to strip non-alphanumerics into spaces/newlines), `tr '[:upper:]' '[:lower:]'`, and `sort -u` to get a deduplicated, alphabetically sorted word list. For the Pearson correlation, compute it in a single `awk` pass over `clean_data.csv` accumulating sums of x, y, x², y², and xy, then apply the standard correlation formula and format with `printf "%.4f"`. Validate each output file against your own manual trace of the small sample CSV before finishing, since off-by-one filtering errors (e.g. mishandling the header, or comparing inclusive vs exclusive bounds) are the most common mistake here.