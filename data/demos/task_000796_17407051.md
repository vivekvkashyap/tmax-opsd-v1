Treat the security fix and the performance fix as separate, testable changes. For safety, replace `eval()` with an AST-based whitelist evaluator: parse the expression with `ast.parse(expr, mode='eval')`, then walk the tree and only allow a strict node whitelist (`Expression`, `BinOp`, `UnaryOp`, `Call`, `Name`, `Constant`/`Num`, and allowed operators) — for `Call` nodes, verify the function name resolves to one of a fixed allowed set of `math`/`numpy` functions (e.g. `sin`, `cos`, `sqrt`, ...) and reject anything else (attribute access, subscripting, `__`-prefixed names, imports); for `Name` nodes only allow `x` and the whitelisted function names, rejecting everything else outright. If validation fails at any point, `sys.exit(1)` before ever calling `eval`/`exec`/`compile` on untrusted code — never fall back to raw `eval` as a convenience. Once validated, build a small namespace mapping allowed names to numpy-vectorized equivalents and evaluate the expression once against the full sample array `x` (a numpy array) — this single vectorized call replaces the slow per-sample Python loop entirely, giving the required speedup. Finally load the WAV with `scipy.io.wavfile` or `wave`, apply the parsed expression, clip/cast back to the original dtype range as needed, write the output, and run your fixed script against the given expression `x * 2.0` on the reference audio to produce the required output file.
