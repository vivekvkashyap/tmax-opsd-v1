Don't hardcode which file uses which encoding — detect it per file (e.g. try decoding as `utf-16le` first, since it will reliably fail or produce garbage/null-byte patterns on non-UTF-16 data, then fall back to `iso-8859-1` which never raises on arbitrary bytes; or use a BOM/heuristic check) since your script must handle whatever lands in `/home/user/incoming/` generically. For each file: decode with the detected encoding, parse the first line for the `ProjectID: <ID>` pattern to get `<ID>`, then write the full content re-encoded as UTF-8 to `/home/user/organized/utf8/<ID>.txt`. Use `os.makedirs(..., exist_ok=True)` for the three output directories up front. For the archive copy, use `os.link(src, dst)` (a real hard link, verifiable by matching inode via `stat -c %i`) — not `shutil.copy`, which would produce a distinct inode and fail verification. For the symlink, use `os.symlink` with the absolute path of the new UTF-8 file as the target (verify with `readlink`), pointing at `/home/user/organized/utf8/<ID>.txt`, named `/home/user/organized/symlinks/<ID>_active`. Run the script once written and spot-check outputs with `file`, `stat -c %i`, and `readlink` yourself.