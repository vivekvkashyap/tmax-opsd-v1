Treat `/app/backup_router` purely as a black-box oracle to reverse-engineer its data model before writing any replacement logic. Construct small, deliberate synthetic topology log files (a single BACKUP_START/DEPENDS_ON/BACKUP_END block, then two/three chained blocks) and run the binary against them with known source/target pairs to determine: (a) whether an edge from `DEPENDS_ON B` inside `BACKUP_START A` points A→B or B→A — test with a topology where only one direction gives a finite path; (b) the exact output format for the src==tgt case, the unreachable case, and normal integer distances; (c) how it parses/tokenizes lines (whitespace handling, unknown node IDs). Once your hypotheses are confirmed against the oracle's actual output on several handcrafted cases, implement `/home/user/route.sh` in pure bash/awk: parse the log to build an adjacency list matching the discovered edge direction, run BFS from source to target, and print output in the exact format you observed (including trailing newline behavior). Test against the oracle on additional random graphs — including disconnected ones and self-loops (src==tgt) — before finalizing, then `chmod +x` the script.