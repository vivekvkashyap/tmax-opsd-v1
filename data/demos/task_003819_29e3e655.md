Parse the FASTA file yourself (e.g. with Biopython's SeqIO or simple line parsing) rather than assuming which records match. For step 1, implement sliding-window matching: for each sequence, slide a 9-character window across it and count mismatches against `ACTGGCCTA`; keep the sequence if any window has 0 or 1 mismatches. For step 2, build the one-hot matrix over the FULL sequence (not just the matched window) and compute its largest singular value with `numpy.linalg.svd` (or `scipy.linalg.svd`) — note as a sanity check that because one-hot columns are orthogonal, the top singular value equals sqrt(max nucleotide count), which you can use to verify your SVD result is correct, but still compute it via SVD as required. For step 3, set `numpy.random.seed(42)` immediately before the bootstrap loop, resample the singular-value array with replacement 10,000 times computing the mean each time (use `np.random.choice(arr, size=len(arr), replace=True)`), then take `np.percentile` at 2.5 and 97.5 of that distribution. Round both bounds to 4 decimals and write the exact JSON shape requested to `/home/user/features_ci.json`.
