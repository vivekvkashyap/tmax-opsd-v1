Work stage by stage, checking intermediate outputs before moving on. Ensure `scipy`/`numpy` are available (`pip install --user scipy numpy` if missing). In `fit_model.py`, load the one-float-per-line data from `/home/user/data/observations.txt` with `numpy.loadtxt`, then fit with `scipy.stats.gamma.fit(data, floc=0)`, which returns `(shape, loc, scale)` — take the shape and scale (ignore loc, it's pinned to 0), and write them to `params.csv` with the exact header `shape,scale` and the two values on the next line. For `validate.R`, read `observations.txt` (e.g. `scan()` or `read.table`) and `params.csv` (`read.csv`), then run `ks.test(data, "pgamma", shape=shape_val, scale=scale_val)` and pull out `$statistic` as D, writing it rounded to 4 decimals (`round(D, 4)`) to `ks_stat.txt` with no extra text. For the plot, use base R: `plot(ecdf(data), main=..., xlab=...)` then overlay the analytical CDF with `curve(pgamma(x, shape=shape_val, scale=scale_val), add=TRUE, col="red")`, opening the device first with `png("/home/user/workspace/cdf_plot.png", width=800, height=600)` and closing it with `dev.off()` so the file is actually flushed to disk. Run both scripts (`python3 fit_model.py` then `Rscript validate.R`) and inspect each output file afterward to confirm they were created and look sane before finishing.