Recover the target frame number from the video empirically: extract frames with `ffmpeg -i /app/camera_feed.mp4 frames/frame_%04d.png`, then scan them (ImageMagick `identify -format "%[fx:mean]"` or Python/PIL) to find the first frame whose average pixel value is exactly 0 — this is the number your C server must report, so compute it by having `video_server.c` invoke `ffmpeg`/analysis via `popen` at runtime, not by hardcoding a guess. Set ACLs with `setfacl`: `setfacl -m u:user:rwx <dir>` for direct access, plus default ACL entries (`setfacl -d -m g:users:r-- -d -m o::--- <dir>`) so new files inherit group-read/others-none; apply to both `app_data` and `logs`. Implement a raw HTTP/1.0 server in C: TCP listen on 8080, parse request line and `Authorization` header manually, return 200/401/404 as specified, log each request via `strftime` under a forced `Europe/Berlin` TZ (`setenv("TZ", ...); tzset();`) and `de_DE.UTF-8` locale (`setlocale`). Write the supervisord config to auto-restart the binary and capture stderr, plus the logrotate config with the required directives; start supervisord and confirm with curl.
