Given the dependency chain is strictly linear (init_db → load_data → detect_anomalies → export_results), implement a real topological-sort DAG resolver (task functions plus a dependency dict, run via Kahn's algorithm or DFS) even though the execution order it produces here happens to be unambiguous — this satisfies the "DAG orchestration" requirement without over-engineering. Configure `logging.basicConfig` with the exact format string `%(levelname)s:%(name)s:%(message)s`, `filename='/home/user/pipeline.log'`, and get a logger named exactly `DAG`; log `Starting task <name>` immediately before calling each task function and `Completed task <name>` immediately after it returns successfully. For `init_db`, drop-then-create the `server_metrics` table. For `load_data`, glob all CSVs under `/home/user/data/` and bulk-insert every row (10 rows total across the fixture files). For `detect_anomalies`, group inserted rows by the date portion of `timestamp` (strip the time), compute each day's average cpu_usage in chronological date order, and flag a date only when its average is strictly greater than 1.5x the immediately-preceding date's average — return dates as `YYYY-MM-DD` strings. Write `export_results`'s output as `{"anomalies": [...]}` exactly.
