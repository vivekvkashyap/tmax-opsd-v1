Handle the three parts independently. For the vendored tool, run `python3 /app/ssh-audit-3.1.0/ssh-audit.py -h` first to see the actual traceback, then look at the top of `ssh-audit.py` for a missing/commented-out import that the traceback's `NameError` points to, and restore it — a minimal targeted fix, not a rewrite. For the filter script `/home/user/validate_auth_keys.sh`, read stdin line by line in bash (`while IFS= read -r line`) and for each line: reject if it starts with (or contains as its key-type token) `ssh-dss` or `ssh-rsa`; reject if the line contains any of the shell metacharacters `| & $ \` ;` anywhere, or contains the substring `environment=`; otherwise pass the line through unchanged only if it contains a recognized strong key type (`ssh-ed25519` or `ecdsa-sha2-nistp256`) — test your script directly against every file in `/app/corpora/clean/` (must pass through byte-identical) and `/app/corpora/evil/` (must produce zero output) rather than guessing at edge cases. For key rotation, run `ssh-keygen -t ed25519 -f /home/user/admin_key -N ""` and then `chmod 600 /home/user/admin_key` explicitly since keygen's default permissions may not match the required 600.