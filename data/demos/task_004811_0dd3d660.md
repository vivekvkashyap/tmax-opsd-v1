Walk `/home/user/raw_datasets` with `os.walk`, and for every `.zip` use `zipfile.ZipFile.extractall` and for every `.tar.gz` use `tarfile.open(mode='r:gz').extractall` into a scratch directory — do this recursively/repeatedly in case archives are nested inside subdirectories, and remember archives can themselves contain directories, so re-walk the extraction output too. For every resulting plain file (original or extracted), pipe its raw bytes into the binary via `subprocess.run(['/app/data_filter'], input=data, capture_output=True)` and check whether stdout is exactly `b"VALID\n"` — treat the binary as an opaque oracle rather than trying to guess its schema from content. Copy files that pass to `/home/user/clean_datasets/` preserving only the base filename (watch for base-name collisions across nested dirs/archives, though the task guarantees uniqueness for valid files). Then build a simple HTTP server (stdlib `http.server` or Flask) on `0.0.0.0:9090` with `/list` returning the JSON array of valid basenames and `/data/<filename>` streaming that file's contents, and start it detached/backgrounded so it survives after your turn ends.
