Work through this vendored-package fix methodically: first inspect `/app/tinyweb-1.0.0/Makefile` and add the missing `-lssl -lcrypto` to LDFLAGS (order matters — link flags typically go after the object files), then inspect `server.c` around wherever it calls `getenv("TLS_CERT_PATH")` and add a NULL check that falls back to the literal string `/etc/tinyweb/cert.pem` instead of dereferencing a NULL pointer; rebuild with `make` and confirm it compiles/links cleanly before moving on. For system setup, use `groupadd`/`useradd -r -g webfiles -s /usr/sbin/nologin` guarded by existence checks (e.g. `getent group webfiles || groupadd webfiles`) so the script is idempotent when rerun. Set `/var/www/html` to `root:webfiles` with mode `750` (group-readable, others none) and drop `index.html` with the exact required content. Generate the self-signed cert with `openssl req -x509 -newkey rsa:2048 -nodes -keyout ... -out ... -days 365 -subj ...` guarded by `[ -f ... ] ||` checks. Launch the server as `tinyweb_usr` on port 8443 in the background (e.g. via `su -s /bin/sh tinyweb_usr -c '... &'` or setuid in the binary), then verify with `curl -k https://127.0.0.1:8443/index.html`.
