Work the three parts in order, letting git bisect inform the fix. Run `git bisect start`, mark HEAD `bad` and `v1.0-stable` `good`, and use a probe script (e.g. run the pipeline on a small synthetic trace and check for a negative value where the accumulator should only grow) with `git bisect run` to automate; write the resulting SHA via `git rev-parse` (full 40 hex chars). For the video step, find the buggy commit's diff for the `accumulator=$(( ... ))` pattern and replace native Bash arithmetic with `awk`/`bc -l` so sums never overflow signed 64-bit, then actually run the fixed script against the mp4 and capture stdout — don't hand-write it. For the sanitizer, detect "overflow artifact" generically at the file-format level (a monotonic column suddenly dropping negative, consistent with two's-complement wrap) rather than hardcoding sample values, since it's tested against corpora you haven't seen. Verify against both provided corpora, checking exit codes explicitly.