Load both .npy files with numpy and implement Top-K cosine-similarity retrieval as a reusable function: normalize vectors (or use `sklearn.metrics.pairwise.cosine_similarity`), then take `np.argsort(-sim, axis=1)[:, :10]` per query. Compute the baseline once on the raw 384-d embeddings. Then loop over the candidate list `[16, 32, 64, 128]` in order, fitting a fresh `PCA(n_components=k, random_state=42)` on the corpus embeddings each time (fit on corpus, transform both corpus and queries — do not fit on queries), rerun the same top-10/cosine routine on the reduced vectors, and compute per-query recall as `|baseline_topk ∩ reduced_topk| / 10`, averaged over all 100 queries. Stop at the first (smallest) k whose average recall clears the 0.85 threshold — don't just pick the max recall. For timing, wrap only the similarity-matrix + top-10 computation (not data loading/PCA fitting) in `time.perf_counter()` calls, once for raw 384-d and once for the chosen optimal-k embeddings. Finally assemble and `json.dump` the report to `/home/user/report.json` with the exact keys specified, rounding recall to 4 decimals.
