This is a multi-stage forensics task — work it in dependency order and verify each stage before moving on. (1) Transcribe `/app/voicemail.wav` (ffmpeg to extract/convert audio, then a local ASR tool such as whisper if available) to recover the spoken passcode phrase — don't guess it. (2) Use that recovered phrase to open `/app/repo.zip` (e.g. `unzip -P '<recovered phrase>' repo.zip` or `7z`/`zip2john`+`fcrackzip` as fallback). (3) In the extracted git repo, use `git log --all --diff-filter=D -- parser.sh` and `git log --oneline --all` to find the commit titled "revert bash experiment", then `git show <commit>^:parser.sh` (or checkout the parent commit) to restore the last working version to `/home/user/parser.sh`; also grep git history/blame for the API token used against `meta.db`. (4) Fix the `make`-based build in `src/` — check for missing `-l` link flags or a syntax typo. (5) Fix `parser.sh`'s magic-number validation to emit the JSON error instead of crashing, and fix the SQLite JOIN causing duplicate rows (likely a missing DISTINCT or wrong join key). (6) Since the grader checks bit-exact stdout/stderr/exit-code equivalence for arbitrary inputs (too-short files, corrupted magic numbers, valid audio headers, and pure random fuzz), implement `parser.sh` to independently satisfy the documented behavior for each case rather than reverse-engineering a black box, and self-test it against a range of hand-crafted valid/corrupted/random files to make sure your own script's output is internally consistent (correct JSON shape, correct exit codes) before considering it done.