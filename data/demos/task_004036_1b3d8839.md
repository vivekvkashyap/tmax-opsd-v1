Tackle the three parts in order since later parts depend on earlier fixes. (1) Deadlock: run `make test` under `strace -f` (or `gdb` on the hung process to get a backtrace of each thread) to see which threads are blocked in futex/mutex calls; look specifically for two mutexes locked in inconsistent order across threads (classic AB-BA deadlock) and fix by enforcing a single consistent lock ordering everywhere. (2) Formula bug: read the README's exact anomaly-score formula (likely variance × skewness of pixel intensities) and compare against the C code—look for integer-division bugs (e.g. dividing sums of ints by N using integer arithmetic instead of casting to double/float before dividing), which silently truncates and produces wrong scores; fix by using floating point throughout the statistics computation. Rebuild to `bin/scorer` and confirm `make test` passes cleanly. (3) Extract frames with `ffmpeg -i /app/drone_feed.mp4 -s 64x64 -f rawvideo -pix_fmt gray frames.raw`, then split the raw stream into 4096-byte chunks (one per frame) and pipe each into `bin/scorer`, recording its stdout score per frame index. (4) Find the frame with the max score and write its 1-based index to /home/user/max_anomaly_frame.txt. Since the binary is fuzz-compared byte-for-byte against an oracle, keep stdout format minimal (just the float, no extra text) and avoid any nondeterminism (uninitialized memory reads) in the C code.
