Start with OCR: run `tesseract /app/bug_ticket.png stdout` and read off the exact failing datetime string, the expected epoch output, and the target port from the ticket text — treat these as recovered facts, not assumptions, since they drive both your bisect test and the final bind address. In `/app/timeserver_repo`, write a small reproduction script (build + run the server + curl the `/convert` endpoint with the OCR'd datetime, checking for a crash or wrong output) that you can invoke automatically, then run `git bisect start`, mark `master` bad and `v1.0` good, and let `git bisect run <your-script>` walk the history. When bisect lands on a commit that fails to build (missing header/syntax error), use `git bisect skip` to move past it rather than declaring it the culprit — build failures are noise, not the real regression. Once bisect identifies the offending commit, inspect its diff for the timezone-suffix parsing function; look specifically for a fixed-size buffer copy (e.g. `strncpy`) that copies exactly N chars without leaving room for/writing a null terminator, causing an overflow read on 3-character zone suffixes — fix by sizing the buffer correctly and explicitly null-terminating. Return to `master` (`git bisect reset`), apply the same fix there, rebuild with the provided `Makefile`, and start the server bound to `127.0.0.1:<OCR'd port>` in the background, then confirm with curl against `/convert?datetime=...` before finishing.
