Start by reading `/app/hyperquery`'s source (likely `engine.py`) to find where it decides the DB path from an environment variable — the bug is a subtle mismatch between the env var name the code actually checks and the one documented/required (`HYPER_DB_PATH`), causing it to silently fall back to an in-memory `:memory:` connection; fix the exact string so it reads `HYPER_DB_PATH`, then confirm by setting that env var and running a quick query against `/home/user/graph_data.db` to see real data returned instead of empty results. For the SQL, write `/home/user/optimize.sql` with three `CREATE INDEX` statements: a composite index on `edges(source, rel_type)`, one on `edges(target, rel_type)`, and an expression index on `nodes(json_extract(properties, '$.category'))` — apply them by executing the script against the actual database file (e.g. `sqlite3 /home/user/graph_data.db < optimize.sql`) and confirm via `sqlite_master`. For `/home/user/server.py`, use only `http.server`/`json`/`sqlite3`, bind exactly `127.0.0.1:8080`, check the `Authorization` header equals `Bearer super-secret-dba-token` before doing anything else (401 otherwise), parse the JSON body defensively (400 on malformed JSON), call the fixed `hyperquery.engine.execute_query(query, params)` with parameterized values (never string-format SQL), and return rows as a JSON array. Launch it in the background and curl-test both the 401 and 200 paths before finishing.