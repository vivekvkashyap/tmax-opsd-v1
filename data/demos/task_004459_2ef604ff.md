Split this into three independently-testable pieces and verify each before wiring them together. (1) Bash state machine: read `/home/user/raw_data.log` line by line with a `while read` loop, track an explicit state variable (e.g. `WAITING_START` -> `HAVE_ID` -> `HAVE_VAL` on seeing ID/VAL/END in order), and reset to the idle state whenever a `[START]` appears while already mid-block or whenever a line breaks the expected sequence — only emit `id,value` to `/home/user/clean.csv` when a block completes exactly START,ID,VAL,END with nothing else interleaved; test against the given log by hand-tracing which blocks are well-formed. (2) Rust FFI: the bug is that `process_data` isn't exposed with C ABI/symbol visibility — add `#[no_mangle]` and change the signature to `pub extern "C" fn process_data(id: i32, value: f64)`, then `cargo build --release` and confirm `libtelemetry.so` exports the symbol (`nm -D` or `objdump -T`). (3) Compile `runner.c` linking against that .so (`-L.../release -ltelemetry -Wl,-rpath,...` or set `LD_LIBRARY_PATH`), then run it under `valgrind` redirecting both stdout and stderr into `/home/user/valgrind_report.txt`, and make the whole sequence happen inside an executable `process.sh`.
