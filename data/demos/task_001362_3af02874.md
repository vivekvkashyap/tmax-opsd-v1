Run `/app/legacy_logger /home/user/raw_logs` first and inspect the generated tree (`find /home/user/raw_logs -name '*.log'`) so you understand the actual file layout before writing parsing logic — don't assume a flat directory. For the C parser, walk the tree recursively (e.g. `nftw` or manual `opendir`/`readdir` recursion) and process each `.log` file line-by-line with a small state machine: track whether you're "inside" a record; on seeing the exact BEGIN marker line, start buffering; on seeing the exact END marker line while buffering, flush the buffered content as one valid record and reset; if a new BEGIN marker (or EOF) appears while already buffering without an intervening END, discard the unfinished buffer as corrupt — this is the key correctness trap, since a naive scan-for-both-markers approach will wrongly pair a BEGIN with a later END that belongs to a different record. Append valid record bodies sequentially to `clean_logs.txt`, keep a running valid-record counter for the STATS response, then shell out (`system("tar czf ...")`) to build the tarball. For the TCP server, implement a simple blocking accept loop that dispatches on the two exact request strings, and stream the tarball file's raw bytes (not text) for DOWNLOAD, closing the connection right after. Test manually with `nc` or a small client before assuming it's correct.