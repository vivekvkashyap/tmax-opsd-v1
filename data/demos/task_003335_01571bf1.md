Treat this as black-box protocol reverse-engineering: use `strings /app/dataset_normalizer` to hunt for magic bytes/format hints, `xxd`/`hexdump` on hand-crafted small input files to see how the tool reads headers, and `strace -f -e trace=open,openat,read,write,rename /app/dataset_normalizer input output` to observe file operations — pay special attention to whether it writes through a temp file (e.g. `output.tmp`) and then calls `rename()` for an atomic swap, since replicating that exact atomic-write pattern (not just producing the same final bytes) may be checked. Construct several minimal test archives by hand (a valid magic header, one chunk with a small known plaintext, correct declared sizes) and feed them to the binary, then vary the payload to see how it transforms text — check specifically whether it does simple substring replacements (try inputs containing common English words and control characters) and whether it converts line endings. Once you have a hypothesis, write it up as a Rust program at `/home/user/normalizer.rs`, compile with `rustc -O /home/user/normalizer.rs -o /home/user/normalizer`, and validate by running both binaries on the same set of hand-built test archives and diffing outputs byte-for-byte before trusting the fuzzer.
