Write the C program to enumerate `/home/user/ssh_audit` (`opendir`/`readdir`), and for each entry use `stat()` and mask permission bits with `st_mode & 0777` to get the octal value. Classify each filename with careful, mutually-exclusive string matching in this priority order: exact match `authorized_keys` -> must be 0600; exact match `config` -> must be 0600 or 0644 (accept either); ends-with `.pub` -> must be 0644; else contains `id_rsa` or `id_ed25519` (and does NOT end in `.pub`, since e.g. `id_rsa.pub` should hit the `.pub` rule first, and something like `backup_id_rsa` still counts as a private key by substring) -> must be 0600; anything else is ignored entirely, even if permissions look wrong. Collect only files that fail their category's required permission(s), format each as `filename:0XXX` using zero-padded 4-digit octal (e.g. via `%04o`), sort the results alphabetically by filename, and write them one per line to `/home/user/audit_results.txt`. Compile with `gcc`, run it against the real directory, and inspect the output file directly rather than assuming correctness.
