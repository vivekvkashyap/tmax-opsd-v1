Never call `tar.extractall()` directly on untrusted archives — inspect each member first. Open the archive with Python's `tarfile` module, iterate `tar.getmembers()`, and for each member compute the resolved destination path (`os.path.normpath(os.path.join(dest, member.name))`) and confirm it still starts with the destination directory's absolute path plus a separator; also reject any member whose `name` is itself absolute (starts with `/`) or contains `..` components, even before normalizing. Only call `tar.extract(member, path=dest)` for members that pass this check — extract each individually rather than filtering then bulk-extracting, so you never risk a symlink or ordering trick. After extraction, walk `/home/user/safe_configs/` recursively (`os.walk`) and compare `os.path.getmtime()` across all regular files to find the true newest one — don't assume the file order in the tar reflects mtime order, since malicious high-mtime entries are deliberately excluded by the safety filter, not by recency. Write the report using the exact `Filename: ...\nContent: ...` template with the base filename (`os.path.basename`) and raw file content. Double check afterward that no file exists outside `/home/user/safe_configs/` from this operation.