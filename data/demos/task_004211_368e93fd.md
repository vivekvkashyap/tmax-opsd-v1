Split this into recovery, parsing logic, and transport, and validate each independently before wiring together. First transcribe `/app/voicemail.wav` with an available speech-to-text tool (e.g. `whisper`) and convert the dictated NATO/phonetic-style words into the actual 4-character alphanumeric code yourself — don't guess it, read it off the transcript. For `handler.sh`, parse raw HTTP text from stdin: extract the token after `Authorization: Bearer `, split on `.` to get the JWT header segment, and base64url-decode it — remember JWT base64url uses `-`/`_` instead of `+`/`/` and often needs padding (`=`) added back to reach a multiple of 4 before standard `base64 -d` will accept it. Case-insensitively check the decoded JSON for `"alg":"none"` (use `tr` or `grep -i`) and branch to the exact 403 vs 200 response strings specified, injecting the code you transcribed into the `X-Policy-ID` header on the success path. Test `handler.sh` standalone by piping a crafted request into it before wrapping it with `socat TCP-LISTEN:8080,fork,reuseaddr EXEC:/home/user/handler.sh` in `start_gateway.sh`, and confirm the gateway answers with `curl` before finishing.
