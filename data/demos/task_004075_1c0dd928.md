Write the pipeline as one Python script and follow the spec's exact order of operations, since small deviations change the deterministic output: (1) load the CSV, apply all four range/NaN filters together with boolean masking (pandas `&` conditions) then `.dropna()`, and record `len(valid_df)` as valid_row_count; (2) call `numpy.random.seed(42)` exactly once, immediately before a `for _ in range(1000)` loop that does `valid_df.sample(n=len(valid_df), replace=True)` each iteration and computes the Pearson correlation of cpu_usage vs ram_usage for that resample — collecting results in a list before computing `np.mean`, `np.percentile(corrs, 2.5)`, `np.percentile(corrs, 97.5)`; the seed placement and loop order matter for reproducibility, don't seed inside the loop or before other random calls. (3) Build `KFold(n_splits=5, shuffle=True, random_state=42)`, pass it as `cv=` to `GridSearchCV(Ridge(), {'alpha':[0.1,1.0,10.0,100.0]}, cv=kf)`, fit on the 3 features vs net_tx, then pull `best_params_['alpha']` and `best_score_`. Round every float to 4 decimals with Python's `round()` before dumping to JSON with the exact key names specified, and `os.makedirs` the output dir first.