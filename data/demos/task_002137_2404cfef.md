Do not try to disassemble the binary from scratch first — instead build understanding by black-box fuzzing: feed `/app/legacy_validator` many crafted JSONL files (vary one dimension at a time: a single malformed unicode escape in `message`, out-of-order timestamps within a `host_id`, and `metric: null` after varying time gaps) and observe exit codes/crashes to empirically pin down the exact boundary conditions (which surrogate ranges crash it, what "out of order" means per-host, and the exact gap threshold that triggers rejection). Corroborate with `strings`/`objdump -d` where useful, but empirical probing is the fast path here. Once you've derived the three rules, implement them independently in `/home/user/detector.sh` (a Python or awk/bash script is fine as long as it's invoked as specified) using efficient streaming JSON parsing (e.g. Python's `json` module or `jq`) and a single pass with a dict keyed by `host_id` tracking last-timestamp and last-metric-time, so it scales to millions of lines without re-sorting or loading everything into memory. Test against `/app/corpus/evil` and `/app/corpus/clean` before finalizing, requiring exact `CLEAN`/`EVIL` output.
