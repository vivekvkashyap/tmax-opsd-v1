Implement the script exactly in the order specified since each step depends on the previous. Build the grid with `numpy.linspace(0, 1, 200)`, then slice `x[:100]` and `x[100:]` for Left/Right — note both halves have identical inter-point spacing (1/199), so don't assume they'll differ. For the covariance matrix, vectorize with broadcasting: `diff = pts[:, None] - pts[None, :]; K = np.exp(-100 * diff**2)` rather than nested Python loops, for both speed and to avoid indexing bugs. For the jitter loop, wrap `np.linalg.cholesky(K + j*np.eye(100))` in a try/except for `np.linalg.LinAlgError`, and on failure multiply j by 10 and retry — start at j=1e-8 and don't cap the loop artificially, since it should converge in a handful of iterations. Compute `np.trace(L)` (sum of L's diagonal, which for a lower-triangular Cholesky factor is the sum of the pivot values, not the same as the matrix's own trace) for each subdomain, and write both values comma-separated to cholesky_diags.txt formatted with `f"{val:.6f}"` — verify there's no extra whitespace or trailing newline mismatch versus what's expected before finishing.