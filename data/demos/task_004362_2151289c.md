Work top-down: extract the payload, reproduce the bug in a test, then fix it, then wire the plumbing, verifying each stage independently. (1) Payload extraction: `strings /app/diagnostics/worker_memory.dump | grep DIAG_PAYLOAD=` (or `grep -oa 'DIAG_PAYLOAD=[A-Za-z0-9]*'` on the raw dump) to pull the 32-char string after the prefix — use that exact recovered value in your Rust test and don't fabricate one. (2) MRE: add a Rust integration test in `tests/regression_test.rs` that POSTs `{"payload": "<recovered-value>"}` to the running service, documenting the scenario that hangs. (3) Root cause: inspect `main.rs` for two `std::sync::Mutex`/`RwLock` guards acquired in inconsistent order across two code branches (classic AB-BA deadlock) — fix by always acquiring locks in one fixed global order (e.g. always lock A before B regardless of branch), or better, avoid holding a std (blocking) Mutex guard across an `.await` point entirely by scoping/dropping the guard before awaiting, or switching to `tokio::sync::Mutex` if you must hold across await. (4) Nginx: point `proxy_pass` at the correct upstream port (3000) rather than the intentionally wrong one baked into the config, and confirm Redis is reachable on 6379. Build and background all three services from `start_services.sh`, then manually curl `/process` with the recovered payload to confirm it no longer hangs before finishing.
