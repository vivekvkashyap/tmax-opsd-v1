Fix the C bug at its root: `getenv("VNC_DISPLAY")` returns `NULL` when unset, and `printf("%s", NULL)` is what segfaults — add a check that treats both a NULL return and an empty string (`display[0] == '\0'`) as "missing" and falls back to the literal default `:1` before printing, so the same default applies whether the variable is unset or set-but-empty. Compile with `gcc -o /home/user/bin/vm_service vm_service.c` after `mkdir -p /home/user/bin`. For `deploy.sh`, write it as a plain sequential script (no need to run it yourself, just get the logic right): append the export line to `.bashrc` with `echo >> ~/.bashrc` (use `>>` not `>` to avoid truncating existing content), `export VNC_DISPLAY=:5` in the script's own shell so the subsequent binary invocation sees it, redirect the binary's stdout with `>>` to `service.log` after `mkdir -p` on the logs dir, then do the rotation as `cp service.log service.log.bak` followed by `: > service.log` (or `truncate -s 0`) to zero out the original without deleting the file object itself — a naive `rm` + recreate can break tools that hold an open file descriptor.
