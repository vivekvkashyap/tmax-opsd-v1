The two gotchas here are encoding and the inner-join semantics — get both right before worrying about formatting. Read `server_alpha.csv` with `encoding="utf-8"` and `server_beta.csv` with `encoding="utf-16le"` (verify with `file` or by inspecting a hex dump if unsure, since guessing wrong will silently produce garbled/empty parses in pandas). Parse both into DataFrames keyed by `date`, then inner-join (or intersect) on dates present in *both* files — dates unique to one file must be excluded entirely, not treated as zero. For each common date compute the Euclidean distance between the 3D vectors `[cache_size, timeout, max_workers]` using `numpy.linalg.norm` or manual sqrt-sum-of-squares, then take min/max/mean of that per-date distance list and round each to exactly 4 decimal places with Python's `round()` before writing to `/home/user/drift_summary.json` with the exact keys `min_drift`/`max_drift`/`mean_drift`. Set up `logging.basicConfig` with the exact format string `%(asctime)s - %(levelname)s - %(message)s` targeting `/home/user/pipeline.log`, and emit the three required INFO messages ("Pipeline STARTED", "Successfully parsed input files", "Pipeline COMPLETED") at the correct points in the script's execution, then actually run the script so all output files exist.