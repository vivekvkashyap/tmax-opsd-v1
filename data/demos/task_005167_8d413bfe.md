Read the unified diff in `/app/api_update.patch` carefully rather than trying `git apply`/`patch` (it's stated to conflict) — treat it purely as a specification of the new algorithm and manually port the logic into `/home/user/auth_generator.py`. From the diff, extract two independent changes: (1) the encoding switches from `base64.b64encode` to `base64.urlsafe_b64encode`, the JSON must be serialized with compact separators `separators=(',', ':')` (no spaces), and the result must have trailing `=` padding stripped with `.rstrip('=')`; (2) the checksum switches from a simple `sum(ord(c))` to a DJB2-style rolling hash computed over the `"{header_b64}.{payload_b64}"` string (`h = 5381; for c in data: h = ((h << 5) + h) + ord(c)`), then reduced with `abs(h) % 1000000` and zero-padded to 6 digits. Preserve the overall token shape `header_b64.payload_b64.checksum`. Apply both changes together, then run the updated script with the exact given payload (`user=integration_tester`, `role=admin`, `exp=1735689600`) and redirect/save its stdout to `/home/user/final_token.txt` with no trailing whitespace or extra newline (e.g. `python3 auth_generator.py | tr -d '\n' > final_token.txt`, or have the script itself write the file). Double check the JSON key order matches how you constructed the header/payload dicts, since key order affects the base64 output.