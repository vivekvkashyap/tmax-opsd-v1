This is a three-bug fix in an existing codebase — locate and read `src/router.cpp` and `tests/test_router.cpp` first, then run `./ci.sh` to see the failure mode before editing. Bug 1 (parsing): the value-extraction loop uses an integer parser (`std::stoi`) on comma-separated tokens that contain decimals — switch to a double parser (`std::stod`) so `1.5,2.0,3.5` isn't truncated. Bug 2 (use-after-move): the code moves a vector into a new variable and then keeps iterating/reading the *original* (moved-from) vector for the product computation — either don't move it at all (just copy or use the original vector throughout), or reorder logic so nothing reads the source after the move; also note `ci.sh` runs a grep-based static check for this exact pattern, so remove the inappropriate `std::move` entirely. Bug 3 (test fixture): the mock fixture object has an `initialized` flag that's never set before use, and the expected JSON key/name is wrong — set the flag, and match the response format the router actually returns (`{"result": <value>}`), not a different key. After fixing all three, run `./ci.sh` and redirect/save its exact stdout to `/home/user/ci_output.txt`.
