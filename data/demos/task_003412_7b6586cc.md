Read both buggy functions closely before editing. For `parse_date`, the current `sscanf` pattern only matches `DD/MMM/YYYY`; add a fallback path that tries a `YYYY-MM-DD` pattern (e.g. `sscanf(date_str, "%d-%d-%d", &y, &m, &d)`) when the first format fails, so both formats yield a valid year instead of silently zeroing it and getting skipped. For `calculate_score`, the convergence test `(score - old_score) > epsilon` breaks as soon as the sequence decreases (negative diff exits immediately) — replace it with `fabs(score - old_score) > epsilon` (needs `math.h`, already included) so the loop actually iterates until convergence or the 100-iteration cap. Build with the provided Makefile (`make` in `/home/user/`), then run `./log_analyzer server.log results.txt`. Verify by hand-tracing one or two log lines through the fixed convergence formula (bytes/(status+1) is the fixed point) to sanity-check the rounded 3-decimal scores before declaring done.
