Write the extraction and dedup logic as two clearly separable steps, testing each before combining. First, use a regex anchored to the exact log format — something like `r'\[.*?\] \[INFO\] Deployed config to (\S+)'` — applied line-by-line over `/home/user/deploy.log`, skipping any line whose level isn't INFO or that doesn't match; print the extracted list first and manually confirm it captures only the deployment lines in original file order (retries appear as later duplicate entries in the log, not as one line). Second, iterate that ordered list, maintaining a running list of "unique" file contents you've accepted so far; for each new path, read its text and compare against every previously-kept unique file's text with `difflib.SequenceMatcher(None, text1, text2).ratio()`, marking it a duplicate the moment any single comparison reaches `>= 0.90` (do not average across comparisons — any one match above threshold is enough), otherwise add it to the unique set. Write only the duplicate paths, one per line, in discovery order to `/home/user/duplicates.log`. Before finishing, manually diff two of the near-duplicate config files yourself to confirm your intuition about which ones should trigger the 0.90 threshold matches your script's actual output.
