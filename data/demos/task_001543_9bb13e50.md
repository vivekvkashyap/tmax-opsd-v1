Approach this as debug-then-harden-then-pipeline. First reproduce the crash: compile and run `/home/user/frame_decoder.c` against malformed 4096-byte inputs (e.g. random bytes not matching the expected magic header) repeatedly under a loop, or use `gdb`/core dumps to inspect the stack trace when it segfaults — look specifically at how the worker threads share and mutate a global/shared error-tracking variable without synchronization when a corrupted frame is detected (a classic data race from unsynchronized concurrent writes/reads to shared state). Fix it by protecting that shared state with a mutex (or atomics) and making the corrupted-frame path deterministic: detect the bad magic header early, have all threads agree the frame is invalid, print nothing, and `exit(1)` cleanly instead of racing. Compile the fix to `/home/user/frame_decoder`. Before trusting it, fuzz it yourself: generate many random 4096-byte buffers (both well-formed and corrupted) and compare your binary's stdout/exit code against your own carefully-reasoned expected behavior for repeated stability across runs. Finally, use `ffmpeg` with `-pix_fmt gray -s 64x64 -f rawvideo` (or `-vf scale=64:64,format=gray`) to extract raw grayscale frames from `/app/profiling_target.mp4`, pipe each 4096-byte frame into your decoder, sum the printed integers only for frames that don't return exit code 1, and write the total to `/home/user/video_sum.txt`.