Implement the PRNG and simulation exactly as specified — this is a determinism task, so any deviation (using C's `rand()`, reseeding differently, using `<=` vs `<`, or updating the counter before vs after the move decision) will produce a different trajectory than the grader expects. Write the custom `state`, `init_rand`, `my_rand`, `my_drand` functions verbatim in C using `unsigned int`/appropriate integer types matching the modulus arithmetic exactly. For the convergence loop, call `init_rand(42)` fresh at the start of each of the four N values (1000/10000/100000/1000000) and reset position to node 0 — do not carry PRNG state or position across runs. At each step draw `r = my_drand()`, move if `r < 0.7`, then increment the visit counter for the (possibly updated) current node; be careful about whether you count the starting position as a "visit" — visits should total exactly N per run. Compute empirical frequency = count/N per node, then the L2 norm against the uniform 0.2 vector, and append to convergence_log.csv formatted to 6 decimals. Finally read reference.csv, compute the max absolute per-node difference against your N=1,000,000 empirical frequencies, and write that single value (6 decimals) to max_diff.txt. Compile with gcc and run once, redirecting/writing outputs directly rather than hand-computing them.