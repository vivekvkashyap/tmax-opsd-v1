Fix the three layers bottom-up: C library, then Rust build config, then Rust code, then the proxy — verifying each layer works before moving to the next. Compile crypto_dummy.c into a shared object (`gcc -shared -fPIC -o libcrypto_dummy.so crypto_dummy.c`) and place it somewhere findable; then look at the Rust FFI build script/Cargo config — if `cargo build` fails to link, it's very likely missing a `cargo:rustc-link-search=native=<path>` directive alongside the existing `cargo:rustc-link-lib`, and/or the runtime linker needs `LD_LIBRARY_PATH` set to find the .so at run time — fix build.rs to emit the correct search path relative to the crate. For the borrow-check error in main.rs's interpreter loop, read the exact compiler error to see which two borrows conflict (commonly a live mutable borrow held across a call that also needs `&mut self`); resolve it by reordering statements or scoping the borrow narrowly so it's dropped before the conflicting access, without altering what the interpreter actually computes. For nginx, write a fully user-space config with explicit `pid`, `error_log`, and `access_log` paths inside nginx_temp/, `listen 8080;`, a `proxy_pass http://127.0.0.1:9000;` location block, and `add_header X-Sandbox-Secure true;`; launch nginx with `-c` pointing at your config and `-p` set to the project dir so it doesn't need root or system paths. Finally start the Rust emulator, start nginx, and curl -i the endpoint to capture success.log.
