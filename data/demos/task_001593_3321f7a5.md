Implement this as a pure awk/bash pipeline, not a multi-language script. Steps: (1) use `grep`/`awk -F,` on `users.csv` to extract the row matching the given `id` argument (`$1`), pulling out age, score1, score2 by column position, skipping the header. (2) Apply the scaling transform: multiply score1 by 10 for the user AND for every candidate row before computing distance — a common mistake is scaling only one side. (3) Feed `candidates.csv` through awk (skipping its header), and for each row compute squared Euclidean distance `(age_u-age_c)^2 + (10*score1_u-10*score1_c)^2 + (score2_u-score2_c)^2` using awk's built-in arithmetic. (4) Track the minimum distance and corresponding `cand_id` in awk variables, updating only on strictly smaller distance, and `print` just that `cand_id` at the END block. (5) Make sure the script emits nothing else to stdout — no debug echoes, no headers — and test it manually against a couple of user IDs to confirm the closest candidate by hand-checking one or two distance calculations.