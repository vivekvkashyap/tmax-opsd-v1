Split this into independent, verifiable stages rather than guessing end-to-end. First, use `ffmpeg` to extract frames from `/app/incident_monitor.mp4` (e.g. one per second, or detect frames where the dominant color is red vs green using OpenCV/PIL to avoid OCR'ing every frame), then OCR only the red frames with `pytesseract` to read the "FAILURE: JOB_<id>" text; dedupe and sort numerically before writing `/home/user/failed_jobs.txt`. Second, for each ID you recovered, open `/app/traces/job_<ID>/strace.log` and the setup script, and actually diff a few failing traces against passing ones — look at the `execve`/shell invocation arguments to see which filename characters (spaces, `;`, `|`, `$()`, quotes, backslashes, newlines) cause the legacy shell script to misparse the path into extra arguments or commands. Third, implement `/home/user/job_filter.py` as a strict allowlist regex (e.g. only alnum, `.`, `-`, `_`, `/`) rather than a blacklist of the few characters you saw, since the hidden test suite covers more evil variants than your sample; test it against both an evil-looking and a clean-looking path before finishing.
