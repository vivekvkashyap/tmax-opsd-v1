Build this as three independent, testable stages before wiring them together. First, parse the FASTA with simple line-based logic (header lines start with `>`, everything else concatenates into the sequence) and count `A` characters per sequence id. Second, load the CSV with `csv.DictReader` and group-sum `mutations` by `seq_id`; verify the two id sets line up before modeling. Third, implement Metropolis-Hastings by hand: define the log-posterior as the sum of Poisson log-likelihoods (`Y_i*log(lambda*X_i) - lambda*X_i`, guard against `X_i=0`) plus a flat prior indicator; the exact reproducibility contract matters — call `np.random.seed(42)` once immediately before the loop starts (not before setup code), and inside the loop draw the proposal with `np.random.normal(current, 0.5)` first, then the acceptance draw with `np.random.rand()`, in that order every iteration, even when the proposal is rejected on prior bounds (reject-but-still-draw affects the random stream). Discard the first 10,000 samples, keep the remaining 40,000, compute mean/std with ddof matching numpy's default, round to 3 decimals, and write valid JSON with exactly the keys `mean` and `std`.
