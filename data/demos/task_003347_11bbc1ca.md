Read the existing `StatsTracker` first to see exactly why it fails: it stores every point in `self.data` (memory growth) and computes variance from `sum(x)` and `sum(x**2)`, which subtracts two huge, nearly-equal numbers when values share a large common offset — that's the catastrophic cancellation. Replace it with Welford's online algorithm: track only `n`, `mean`, and `M2` (sum of squared deviations from the running mean) as scalar instance attributes, updated per call to `add_point` with `delta = x - mean; mean += delta/n; delta2 = x - mean; M2 += delta*delta2` — no list, no append, ever. Compute sample variance as `M2/(n-1)` only when `n >= 2`, matching the original's edge-case behavior. Keep `main()`'s file paths and output format (`f"{variance:.6f}\n"`) completely unchanged so the harness still works. After rewriting, run the script against `/home/user/data.txt` and confirm the output value is now stable and non-degenerate rather than 0.0 or wildly wrong, and grep your own file to confirm no `.append(` remains inside the class.
