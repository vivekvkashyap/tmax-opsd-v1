Start by extracting the config: `tar xzf /app/config_data.tar.gz` and pull the threshold/duration values out of `settings.ini` with `awk -F= '/amplitude_threshold/{print $2}'` (and similarly for the duration key) rather than hardcoding numbers, then pass them as CLI args to your C++ program so the logic stays config-driven. In the C++ trimmer, parse the WAV RIFF header carefully (verify "RIFF"/"WAVE"/"fmt "/"data" chunk IDs, read sample rate and bits-per-sample rather than assuming), then scan 16-bit PCM samples computing a run-length of consecutive samples with `abs(sample) < threshold`; when a run's duration (run_length / sample_rate) exceeds the configured minimum silence duration, drop that whole run from the output buffer, otherwise keep it (this leaves short pauses between words intact so transcription still works, while removing genuinely long silences aggressively for size savings). After processing, rewrite the RIFF chunk size and data chunk size fields in the header to match the new (shorter) sample buffer length — a wrong header size will produce an invalid or truncated-sounding WAV. Compile, run against `/app/voicemail.wav`, and sanity-check the result yourself: listen to word boundaries aren't cut (don't trim too aggressively at the edges of a silence run) and confirm the output file is meaningfully smaller. Finally tar up `/home/user/optimized.wav` and the `settings.ini` into `/home/user/final_storage.tar.gz`.
