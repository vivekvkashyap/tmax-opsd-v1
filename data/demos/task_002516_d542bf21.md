Work through the three bugs in `/app/secure_uploader_src` methodically by reading each file in turn rather than guessing. In `decoder.c`, find the base64-decode routine and check its length/padding-handling arithmetic (e.g. any off-by-one adjustment to `len` based on `len % 4`) — trace through a few known valid base64 strings by hand (with 0, 1, and 2 padding `=` characters) to confirm the fix decodes them to the exact original bytes with no truncation. In `handler.c`, add explicit filename validation before any `fopen`/path-join call: reject (return exit code 2) any filename containing the substring `..` or starting with `/`, checked with `strstr`/`strncmp` — do this rejection before any file I/O so a malicious path never touches the filesystem. In the `Makefile`, add the missing OpenSSL include path to `CFLAGS` and `-lcrypto` to `LDFLAGS` so cert validation compiles and links. After each fix, rebuild with `make` and manually test representative inputs — valid base64 payloads of varying lengths/padding, filenames with `../` and absolute-path traversal attempts, and normal safe filenames — comparing exit codes and output bytes against what the spec describes, until behavior is consistent and correct across many edge cases. Compile the final binary to `/home/user/secure_uploader_fixed` accepting `<base64_payload> <filename> <cert_path>`.