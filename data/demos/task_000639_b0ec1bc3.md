Treat this as a temporal self-join per category rather than a generic graph problem. Load and join the four CSVs (pandas is fine) to produce, per category, a chronologically sorted list of (user_id, order_date) purchase events — remember a user can appear multiple times and order_items can map an order to multiple products/categories, so dedupe per (user, category, date) appropriately before counting. For each purchase event (user u, category c, date D), look at all other purchase events in the same category c with date strictly between D+1 and D+7 inclusive, collect the distinct set of *other* users (excluding u itself) in that window, and mark u as an influencer if that set has size >= 3. Be careful with date arithmetic (use actual date objects, not string comparison) and with the inclusive boundaries (D+1 through D+7, not D+6 or D+8). Union all qualifying user_ids across all categories/events, dedupe, sort ascending as ints, and write to `/home/user/influencers.json` as a JSON list. Sanity-check your logic by manually tracing one or two categories against the raw CSV before trusting the script's output.
