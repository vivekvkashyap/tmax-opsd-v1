This is a black-box reverse-engineering task, so derive the routing rule from evidence rather than assuming it. Start with `strings /app/email_router_oracle` to find candidate literal substrings it might be matching against and the destination labels it can output; feed it several crafted inputs via stdin (varying which substrings are present/absent, and their order) to empirically map inputs to outputs, and use `objdump -d` or `gdb` to confirm the exact matching order/precedence when multiple substrings could apply (the first matching rule usually wins — verify by constructing inputs where two candidate substrings both appear). Once confident, reimplement the same conditional substring-matching logic in C++, reading the full stdin into a string and testing `.find()` for each pattern in the same precedence order the oracle uses, falling back to the default output when none match. Compile it, then fuzz it locally against the oracle with many random strings (including edge cases) to confirm bit-exact stdout for identical stdin before wiring it up. For the network piece, write `start_backend.sh` to launch your binary behind `socat TCP-LISTEN:9000,reuseaddr,fork EXEC:/home/user/email_router` (or `nc` equivalent) in the background, matching the port Nginx's `/route` location already proxies to, and add basic checks (e.g. verifying the port isn't already bound, logging failures) for robustness.
