This is black-box behavioral reverse engineering, not decompilation. Feed `/app/legacy_matcher` small, hand-crafted inputs via stdin (tiny graphs with 2-4 nodes, one or two edge types, simple queries) and vary one factor at a time — add a second parallel path between the same nodes, change edge weights, change the required sequence length — to isolate the aggregation rule. Look specifically for: (1) whether it matches paths by their exact ordered sequence of edge `type` labels equal to the query's comma-separated sequence; (2) whether a single path's contribution is the sum or product of its edge weights (test with weights like 2.0 and 3.0 on a 2-edge path to distinguish sum=5 from product=6); (3) whether multiple valid paths between start/end are summed together (create two disjoint paths matching the same sequence and see if the binary's output equals the sum of their individual scores); (4) what it prints when no path matches (confirm it's exactly `0.0000` via `printf "%.4f\n"`). Once you've nailed the rule, implement it in `/home/user/new_matcher.py` using DFS/path-enumeration over the DAG (since it's acyclic, exhaustive path enumeration per query is safe), matching the exact output formatting (`%.4f`, one line per query, in query order).