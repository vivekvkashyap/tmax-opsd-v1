Break this into three independently-testable pieces, then wire them together in the deploy script. (1) For frame darkness detection, don't hand-roll pixel math in Rust from scratch first — use `ffmpeg` to extract frames (e.g. `ffmpeg -i signal_test.mp4 frame_%04d.png` or pipe raw video via `-f rawvideo`) then compute each frame's average RGB with a small script/Rust program comparing against the "<15" threshold; write matching 0-indexed frame numbers, one per line, to the required output path. Double-check your frame indexing matches ffmpeg's numbering convention (adjust for 1-based vs 0-based off-by-one). (2) For the Rust sanitizer, `cargo init` at the given path, use `serde`/`serde_json` to parse the JSON, then do plain substring checks (`.contains(...)`) on `target_path` for each dangerous pattern, returning `std::process::exit(1)` on any match and `exit(0)` otherwise — test locally against a few crafted clean/evil JSON files before trusting it. (3) The deploy script should build in release mode, loop over each corpus directory checking exit codes explicitly (`if`/`$?`), and only then run the frame-extraction step; make it fail loudly and exit 1 on any check failure.
