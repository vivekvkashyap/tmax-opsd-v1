Build deploy_edge.py so every step is idempotent: use `os.makedirs(..., exist_ok=True)` for directories, check-before-write (or overwrite deterministically) for the mock_sensor script and TLS cert/key so re-running doesn't crash or duplicate work. Generate the self-signed RSA cert with the `cryptography` library or by shelling out to `openssl req -x509 -newkey rsa:2048 -nodes -keyout key.pem -out cert.pem ...`, saving to the exact paths given. For crontab idempotency, read the current crontab with `crontab -l` (handle the case where none exists), check via regex/substring whether the collect.py line is already present, and only append+rewrite via `crontab -` if it's missing — never blindly overwrite existing entries.

The critical part is collect.py surviving `env -i python3 collect.py` (empty environment, no PATH/HOME). Hardcode every path as an absolute string computed with no reliance on `os.environ`, `os.getcwd()`, or relative paths — e.g. call `/home/user/bin/mock_sensor` by its full path via `subprocess.run(["/home/user/bin/mock_sensor"], capture_output=True)`, write directly to `/home/user/webroot/metrics.html`, and send the alert email with Python's `smtplib` connecting explicitly to `127.0.0.1:8025` rather than any environment-derived mail relay. Test your collector yourself with `env -i /usr/bin/python3 /home/user/bin/collect.py` before considering the task done.