Treat this as a small benchmarking harness, not a one-off script. Use `subprocess.run([...], env={**os.environ, "OMP_NUM_THREADS": "1", "OPENBLAS_NUM_THREADS": "1"}, capture_output=True, text=True)` to invoke `/home/user/transform_step.py` so the env vars actually take effect for the child process (don't just set them in the current shell before a loop that spawns Python — pass a full env dict per call since OpenBLAS reads them at process start). Parse `float(result.stdout.strip())` from each of the 20 runs per configuration into two lists. For the stats, use `scipy.stats.ttest_ind(a, b, equal_var=False)` for Welch's t-test to get the t-statistic and p-value directly, and compute the mean difference and its 95% CI using Welch-Satterthwaite degrees of freedom (or `scipy.stats.ttest_ind` for the test plus manually compute the CI as `mean_diff ± t_crit * pooled_se`, using `scipy.stats.t.ppf` for the critical value). Cast every numeric result to plain Python `float`/`bool` before `json.dump` (numpy scalars fail strict `isinstance(x, float)` checks). Before finalizing, actually run your script end-to-end once and open the resulting JSON to confirm all four required keys are present with correct types and p_value is in [0,1].
