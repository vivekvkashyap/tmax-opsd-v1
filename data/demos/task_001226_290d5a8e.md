Treat this as building a tiny in-memory relational engine in C, not a literal SQL parser. Read each TSV with `fgets`/`sscanf` (or manual `strtok` on `\t`) into arrays of structs, keeping the schemas separate: institutions[], authors[], publications[]. Build the join by first filtering institutions where country == "Canada", then for each matching institution scan authors with matching inst_id, then for each such author scan publications with matching author_id and year > 2010, accumulating citations into a per-institution running total (a simple hash map or linear array keyed by institution name/id works fine given small data). After aggregation, sort the institution totals by total_citations descending, tie-broken by name ascending (write your own comparator for `qsort`), then take the top 3. Write results as `Name,Total` with no header and no trailing extra whitespace, matching the exact format requested. Watch for two common failure points: (1) off-by-one/wrong delimiter when parsing TSV fields with embedded spaces in names — don't split on all whitespace, only on tabs; (2) the ORDER BY has two keys, so a naive single-key sort will misorder ties. Compile exactly as instructed (`gcc -O3 ...`) and run before checking the CSV.
