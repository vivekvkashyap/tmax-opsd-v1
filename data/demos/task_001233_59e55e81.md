Reverse-engineer the proprietary archive format empirically before writing any Rust: pack a few small, controlled test directories (single file, multiple files, nested paths) with `/app/legacy_packer` and hexdump the output (`xxd`/`od -A x -t x1z`) to identify the magic bytes, length-prefix widths and endianness, and per-entry structure (filename, size, data, checksum). Cross-check by running `/app/legacy_unpacker` on your own hand-built byte sequences and see what it accepts/rejects, and by unpacking your own packed archives to confirm round-trip correctness. For the security filter, don't just check for `..` substrings — recursively walk the directory using `std::fs::symlink_metadata` (never `fs::metadata`, which follows links) so you can detect symlinks without dereferencing them, then resolve each symlink's target and canonicalize the resulting path relative to the backup root, rejecting anything that escapes the root or that you can't resolve (circular links) within a bounded number of hops. Use `memmap2` only for reading regular file contents when computing SHA-256, not for directory traversal. Validate against both `/home/user/corpus/clean/` and `/home/user/corpus/evil/` before considering it done.