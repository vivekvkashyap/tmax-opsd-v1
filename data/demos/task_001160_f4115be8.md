Work through this in stages rather than jumping straight to the server. First handle the filesystem prep: recursively find `*.tar.gz` under `/home/user/research_data/` and extract each in place (`tar -xzf <archive> -C <dirname>`), then find symlinks with `find ... -type l` and test each with `[ ! -e "$link" ]` (a broken symlink) before deleting — don't touch valid symlinks or hard links. For the Rust server, use a lightweight HTTP crate (e.g. `tiny_http` or `actix-web`) bound to `127.0.0.1:9090` with a `POST /process` handler. Inside the handler: walk the given directory recursively for `.log` files, read each file's contents, split on lines exactly equal to `---END_RECORD---` to get individual records (trim any leading/trailing blank lines from each chunk), then for each record spawn `/app/log_scorer` as a subprocess, write the record to its stdin, close stdin, and read its stdout as an integer score. Keep records scoring >= 75, join the kept ones with `\n\n`, and return that as the plain-text response body. Watch for two common bugs: forgetting to close/flush stdin before reading stdout (causes the child to hang), and mis-splitting records if the separator line has trailing whitespace — trim each line before comparing. Build in release mode and leave the server running in the background.