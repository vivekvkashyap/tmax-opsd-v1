First reverse-engineer `/app/legacy_auth` before writing any facade code: run `strings /app/legacy_auth` to see hints of usage/output text, then invoke it directly with a few candidate argument orderings (e.g. `./legacy_auth user pass`) and observe stdout and exit code for both correct and incorrect credentials — infer the calling convention (argv order, success/failure marker string, exit codes) empirically rather than assuming. Once confirmed, build `/home/user/auth_facade.py`: use `paramiko`'s `ServerInterface` (or `asyncssh`) to implement `check_auth_password` by shelling out to `/app/legacy_auth <user> <pass>` via `subprocess` and checking its output/exit code, override `check_auth_publickey` to always return failure, and on successful password auth send the literal string `Welcome <username>` over the channel then close it (no PTY/shell). Run a simple HTTP server (e.g. `http.server` subclass or a small `socketserver`) on port 8080 answering `GET /health` with 200 `OK`. In `/home/user/start_services.sh`, generate host keys with `ssh-keygen`, launch the Python script(s) in the background with `&`, and write `$!` PIDs to `/home/user/service.pid`; verify both ports are actually listening (e.g. `ss -tlnp`) before finishing.