Scaffold with `cargo new build_proxy`, add a WebSocket crate (e.g. `tokio`, `tokio-tungstenite` or `axum` with its ws feature) plus `serde_json` for parsing. Bind a listener on `127.0.0.1:9050`, accept upgrades only on the `/analyze` path, and handle each connection in its own async task so multiple messages on one connection are processed sequentially without dropping the socket. For every incoming text message: try to parse it as JSON with a `target_file` string field; on any parse failure, missing field, or wrong type, immediately reply with the literal string `ERROR` and continue the loop rather than closing the connection. On successful parse, spawn `/app/build_analyzer` as a subprocess (`std::process::Command` or `tokio::process::Command`) passing the file path as the sole argument, capture its stdout, and send that back verbatim (decide once whether you trim trailing newline and be consistent, since the verifier matches by prefix). If the target file doesn't exist the binary itself exits nonzero with no meaningful stdout — treat that as `ERROR` too. Build in release mode and leave the process running in the background (`cargo run --release &`) so it's still listening when the test connects.