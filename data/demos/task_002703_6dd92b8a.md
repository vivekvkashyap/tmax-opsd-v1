Build and test this incrementally using a real local clone rather than reasoning abstractly about Git hooks. Initialize the bare repo with `git init --bare /home/user/auth_server.git`. Write `/home/user/validate_user.py` to read the three whitespace-separated fields per line from stdin (old-rev new-rev refname); for each ref update, use `git diff --name-status <oldrev> <newrev>` (handle the all-zeros oldrev case for the first push) to find added/modified paths under `accounts/`, then for each such path verify: it ends in `.json`, its basename-minus-extension is not a case-insensitive match against any line in `/home/user/banned_users.txt`, its content (read via `git show <newrev>:<path>`) parses as JSON, and the JSON has an `email` key — reject the whole push (nonzero exit) on any violation. Make the script the `hooks/pre-receive` file (executable, with a `#!/usr/bin/env python3` shebang or a wrapper). Then write `/home/user/extract_emails.sh` using only grep/awk/sed (no python/jq) to clone the repo to a temp dir, pull username from each `accounts/*.json` filename and email via a grep+sed extraction of the `"email": "..."` line, emit `username,email` rows to `/home/user/active_emails.csv`, and clean up the temp dir. Finally write the cron line `0 2 * * * /home/user/extract_emails.sh` to `/home/user/my_cron.txt`. Test the hook yourself by pushing banned, malformed, and valid account files from a scratch clone before considering it done.
