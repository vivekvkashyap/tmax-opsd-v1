Part 1: install a speech-to-text tool (e.g. `whisper`/`whisper.cpp` via pip or apt) and transcribe `/app/meeting.wav` to text; search the transcript for the phrasing naming the root node and the baseline score, convert any spelled-out number to digits, and write it to `/home/user/root_info.txt` in exactly the required `Root: <NodeName>, Score: <Number>` format — do not guess the values, derive them from the transcription output. Part 2: build the graph in memory (adjacency list from source→target per file), then for each input file (a) detect cycles with DFS using a three-color (white/gray/black) visited scheme — any back edge to a gray node means a cycle, and (b) if acyclic, compute the longest path in edges via topological order + DP over in-edges (longest path only makes sense on a DAG, so always check for cycles first). Reject (skip copying) if cyclic OR longest path > 10 edges; otherwise copy the file byte-for-byte unmodified to the output dir. Test your script against both easy (short) and adversarial (long chain, cyclic) graphs before trusting it.
