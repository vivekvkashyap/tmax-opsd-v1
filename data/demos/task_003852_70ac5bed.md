Work through the pipeline layer by layer instead of guessing at the whole thing at once. First, try running `run.sh` and read the actual error: if it's a pip/dependency resolution failure, open `requirements.txt`, identify which two pinned packages conflict, and loosen one or both version constraints (e.g. switch a hard `==` pin to a compatible range) until `pip install` succeeds — don't just delete the file. Second, if the script hangs, use Ctrl+C plus a stack trace or timeout to locate the recursive function; recursive parsers that consume a list/block typically hang because they never handle the empty-input case, so add the missing base case (return immediately when there's nothing left to process) rather than adding an arbitrary depth limit. Third, for the binary: don't try to patch or guess-brute-force it — inspect it with `strings` on the compiled file to find embedded literals, and check how it compares argv against them (e.g. via `strings` output or disassembly with `objdump`/`gdb`) to recover the exact expected argument; then update the bash wrapper to pass that literal string when invoking the binary. After each fix, re-run the full pipeline end-to-end on the given input/output paths and inspect the resulting JSONL for correctness before declaring success.
