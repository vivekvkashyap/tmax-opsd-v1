Start by extracting the key from the binary rather than guessing: run `strings /home/user/legacy_auth | grep XOR_KEY=` (or `objdump -s -j .rodata` if `strings` doesn't surface it) to locate the `XOR_KEY=` prefixed string in the read-only data section, then take the 8 bytes immediately following that prefix as your key. Once you have the key, write the Go program: for each line of `tokens.txt`, base64-decode with `encoding/base64` (`StdEncoding`), XOR each byte of the decoded payload with `key[i % len(key)]` (repeating key cycle, not truncating), hex-encode the decrypted bytes with `encoding/hex`, and compute `sha256.Sum256` on the decrypted bytes (not the hex string) then hex-encode that digest too. Assemble each output line in the exact format `Original: <base64_token> | DecryptedHex: <hex> | SHA256: <sha256_hex>`, preserving the original base64 string as read from the file. Compile with `go build` and run the binary (or `go run`) so `/home/user/audit_trail.log` is actually produced, and diff a couple of lines by hand against a quick manual XOR of the first token to confirm your key and byte-ordering are correct before trusting the full run.