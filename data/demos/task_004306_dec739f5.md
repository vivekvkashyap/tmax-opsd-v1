Install `libhdf5-dev` first and locate the actual include/lib paths on this system with `dpkg -L libhdf5-dev | grep hdf5.h` and `find / -name libhdf5*.so*` rather than assuming a fixed path — HDF5 serial paths vary by distro/architecture. In the C program: parse the FASTA file by streaming lines, treating any line starting with `>` as a new-sequence header and accumulating the length of all subsequent non-header lines (strip newlines) until the next header or EOF. Open `ref.h5` with `H5Fopen`, then `H5Dopen2`/`H5Dread` the `weights` dataset as `H5T_NATIVE_DOUBLE` into an array sized to match the sequence count (verify counts match before pairing them index-by-index). Compute weighted_length[i] = seq_len[i] * weight[i], then the mean and population variance (divide by N, not N-1) of that array, and the max absolute forward difference between consecutive weighted lengths. Print/format all three values with exactly 2 decimal places into `/home/user/stats.txt` in the specified `Key: value` format. Compile with the discovered include/lib flags plus `-lhdf5`, then actually run the binary and inspect `stats.txt` to confirm formatting matches exactly.