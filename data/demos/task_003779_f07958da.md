Work through the three stages sequentially and validate each before moving on. Stage 1: open `setup.py` and `dumper.py`, fix the Python 2-isms one at a time (missing comma/trailing issues in `setup.py`, `print "..."` -> `print("...")`, `.iteritems()` -> `.items()` in `dumper.py`), then `pip install -e /app/py_graph_dumper` and run `dumper --output /tmp/legacy_schema.json`; inspect the resulting JSON to confirm it has the node/deps/bytecode structure before proceeding. Stage 2: implement a standard topological sort (Kahn's algorithm or DFS-based) over the `deps` arrays — build it generically rather than hardcoding the sample graph, since correctness is what's checked. For bytecode translation, iterate the hex string two characters at a time, map each byte via the given opcode table, and join with spaces. Write `MigrateSchema()` to push the sorted ID array to Redis key `v2:ordered_nodes` (as JSON) and populate the `v2:nodes` hash. Stage 3: implement the HTTP handlers and a simple TCP line-reader (bufio.Scanner) concurrently with `go func()`, start both listeners, then only write `/tmp/success.log` with `READY` once both servers are confirmed listening. Test each endpoint manually with `curl` and `nc` before declaring done.
