Work through this as a layered stack: fix the vendored code first, then wire up each service, verifying each layer before moving to the next. In `server.py`, replace the hardcoded bind address/port with `os.environ.get("BIND_HOST", "0.0.0.0")` and `int(os.environ.get("BIND_PORT", 8000))`, and grep the file for the auth-token env var name carefully — a single-character typo (like a transposed letter) is easy to miss with a casual read, so diff what the code reads against the literal string `AUTH_TOKEN`. Build the venv with `python3 -m venv`, `pip install -e` (or `pip install .`) the package, then write the wrapper script that sources the venv, exports the env vars, and backgrounds the server with `&` (and `disown`/redirect output so it survives). Test the HTTP endpoint directly with curl before touching git. For the git hook, remember `post-receive` hooks run with no TTY and a restricted PATH/cwd — use absolute paths to curl/python inside the hook script, and confirm `chmod +x` on the hook file. Finally start `socat TCP-LISTEN:9090,fork,reuseaddr TCP:127.0.0.1:8080` in the background and curl port 9090 end-to-end to confirm the full chain (git push -> hook -> curl -> daemon) before declaring done.