Implement the 1D explicit-Euler heat equation solver in C as a function `run_sim(dt)` that returns the full 11-point temperature array at t=2.0: initialize u[i]=25 for i=0..10, fix u[0] and u[10] at 25 every step (don't let the interior update loop touch the boundaries), and for i=1..9 update `u_new[i] = u[i] + dt*(alpha*(u[i-1]-2*u[i]+u[i+1])/dx^2 + P[i])` with P nonzero only at i=4,5,6; iterate `round(2.0/dt)` steps, using a separate `u_new` buffer each step so you don't read already-updated neighbors. For convergence, start dt=0.1, run, halve dt each iteration, and stop when `|center_temp(dt) - center_temp(previous dt)| < 0.01`, keeping the current (smaller) dt and its full array as "optimal" — implement this as a real loop over successively halved dt values, not a single guess. Load `/home/user/sensor_data.txt` (11 floats) and compute residuals against your optimal-dt array, then population mean and population variance (divide by 11) of those residuals. Write all four values (optimal_dt, center_temp=u[5], error_mean, error_variance) to `/home/user/results.json` with at least 4 decimal places, then compile/run and eyeball the JSON for sane values.