Isolate the fix to just `integrate_feature`: replace the fixed-step Euler loop with `scipy.integrate.solve_ivp(rhs, (0, L), [0.0], method='BDF', t_eval=None)` where `rhs(t, y)` reproduces `100 * (calculate_gc_content(seq, t) - y[0])` exactly as the original stepping logic computed it (same GC-content lookup by truncated index), and take the value at the final integration time (`sol.y[0, -1]`) as the endpoint — don't change how `calculate_gc_content` indexes into the sequence, since that's what makes this a genuinely stiff/discontinuous RHS that BDF should handle robustly where Euler diverges. Leave `matrix_feature` (SVD) and `primer_alignment` untouched. After editing, run the script end-to-end over `sequences.txt` and confirm no row in `training_features.csv` has NaN/inf in the Integration column, with values rounded to 4 decimals as before, and the header matches exactly.
