Read `queue.c` and `worker.c` carefully before editing — the two bugs are independent and both must be fixed. In `queue.c`'s `enqueue_event`, the list traversal/append (`head == NULL` check, walking to the tail, and the final `temp->next = new_node` assignment) happens without holding `queue_mutex`, so concurrent threads can interleave and corrupt the list into a cycle; wrap the entire read-modify-write critical section (from reading `head` through setting the new tail pointer) with `pthread_mutex_lock(&queue_mutex)` / `unlock`, not just individual statements — partial locking still races. In `worker.c`'s `process_events_recursive`, the recursion has no cycle protection, so a corrupted (or, even after your fix, adversarially cyclic) list causes unbounded recursion and stack overflow; make it robust by converting to an iterative loop (simplest and most reliable) or by adding an explicit depth counter/visited-node tracking that bails out around 1000 hops — don't just add a depth limit if you're unsure it's airtight, prefer the iterative rewrite since it can't stack-overflow at all. Leave `main.c`, the `Makefile`, and `test_telemd.sh` untouched, rebuild with `make clean && make`, run `./test_telemd.sh` to confirm a 0 exit and no crash, and only then write the exact string `SYSTEM_RESTORED_SUCCESS` to `/home/user/resolution.log`.