First recover the master token: run `tesseract /app/auditor_cert.png stdout` (or output to a file) and inspect the OCR'd text carefully — trim whitespace/newlines and verify visually since OCR can introduce stray characters, then use that exact recovered string as a constant/config value in your Rust code rather than hardcoding assumptions about it. Structure the Rust CLI to: read the one filename argument, read the file's contents as a hex string, decode it into raw bytes (e.g. via the `hex` crate or manual byte-pair parsing), then search the raw byte buffer for the token bytes and independently search for each of the four literal vulnerability substrings (`UNION SELECT`, `' OR '1'='1`, `javascript:`, `onerror=`) as raw byte substrings — do the search on the decoded binary, not the original hex text, since that's what the spec requires. Exit 0 only if the token is found AND none of the CWE strings are found; exit 1 otherwise (missing token, or any hit). Build with `cargo build --release` so the binary lands at `target/release/audit_filter`, then loop over every file in `/app/corpus/clean/` and `/app/corpus/evil/` running your binary and checking exit codes match expectations before finishing.
