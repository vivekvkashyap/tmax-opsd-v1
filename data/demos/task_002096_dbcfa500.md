First fix the vendored `jo` build: open `/app/jo-1.9/Makefile.in` and look for a misspelled Makefile variable near the linker flags (a variable defined under one name but referenced under a different, similarly-spelled name causes `LDFLAGS` to silently not be passed to the link step) — correct the spelling so the assignment and its usage match, then `cd /app/jo-1.9 && ./configure && make` and confirm the `jo` binary is produced. Second, build `/home/user/start_server.sh` as a loop that uses `nc -l -p 8080` (or `-l 8080` depending on your nc variant — check `nc -h`) to accept one connection at a time, reads the request line, and on `GET /api/status` responds with a raw HTTP/1.1 response: status line, a `Content-Type: application/json` header, a blank line, then a JSON body. Build that JSON body by reading both `test_results_A.txt` and `test_results_B.txt`, concatenating their `key=value` lines, sorting them alphabetically by key, and feeding them as `key=value` arguments to your compiled `jo` binary (put its directory on `PATH`) so it emits a single flat JSON object. Loop the nc invocation forever (`while true; do ... ; done`) so it survives multiple requests, and run it in the background so it stays up for testing.