Debug the build first: run `make` in `/app/log-masker-1.0.0/` and read the exact error — a "missing separator" error means the recipe line under the target uses spaces instead of a leading tab; open the Makefile in an editor that shows whitespace (or `cat -A`) and replace the leading spaces on the recipe line with a literal tab character, then confirm `make` produces the `masker` binary. Test `masker` manually against a sample line with an api_key and ssn to confirm it redacts to `[REDACTED]` before wiring it into anything else. Write `/home/user/pipeline.sh` as a thin wrapper: `<script> <input> <output>` that pipes/redirects through the compiled `masker` binary (absolute path to `/app/log-masker-1.0.0/masker`). Validate it against both `/home/user/corpus/clean` (must diff identical) and `/home/user/corpus/evil` (must have every api_key/ssn masked) before moving on. For the cron piece, write `/home/user/cron_runner.sh` to glob `*.log` in `/home/user/incoming/`, run each through `pipeline.sh` into `/home/user/outgoing/<same-name>`, then `rm` the original only after successful processing, and install the schedule with `crontab -l | { cat; echo "*/5 * * * * /home/user/cron_runner.sh"; } | crontab -` rather than overwriting any existing crontab. Make every script executable.
