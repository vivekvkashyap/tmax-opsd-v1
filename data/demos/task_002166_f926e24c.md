Guard against the pandas NaN/float pitfall explicitly: when you left-join users to purchases, users with no purchase rows get `NaN` in `product_id`, silently upcasting the whole column from int64 to float64. Handle this by dropping rows where `product_id` is null (equivalently, this drops purchase-less users if you group afterward) before casting `product_id` to `int` — or merge with an inner join on users that actually appear in purchases, then explicitly `.astype(int)` on `product_id` and confirm the dtype with `.dtypes`. Build a per-user set of purchased `product_id`s (e.g. `groupby('user_id')['product_id'].apply(set)`), then for each user other than user 1 compute Jaccard similarity = `len(intersection)/len(union)` against user 1's set, skipping user 1 itself and skipping users with empty sets. Pick the max-similarity user, breaking ties by smallest `user_id` (sort candidates by `(-similarity, user_id)`). From that most-similar user's set, subtract user 1's purchased products, and take `min()` of the remaining set as the recommendation. Write just that integer to `/home/user/recommendation.txt`.