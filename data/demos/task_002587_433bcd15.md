Don't guess the seed — reproduce the failure systematically. Read `compute_energy.py`: it seeds `np.random`, adds int32 noise to int32 data, and sums squared values — a classic fixed-width accumulator overflow. Write a small driver that calls the script (or imports `compute_energy` directly) for seed = 1, 2, 3, ... in order, stopping at the first negative result — scan increasing order since the *lowest* failing seed is required, not any working one. Once found, fix the root cause: force accumulation in a wider/exact type before summing (cast to `int64`, or use Python's arbitrary-precision `sum()`), rerun with the same seed for the corrected value, and confirm the fix no longer errors on seeds you tested. Write the three-line report with the seed and both values you computed — do not modify `data.txt`.
