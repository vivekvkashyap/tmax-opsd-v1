Parse `cron_out.log` with a stateful line-by-line scan (awk, or a small Python script) rather than a single regex over the whole file: track a boolean "inside manifest" flag, set it true on a line exactly equal to `RAW_MANIFEST_START`, false on `RAW_MANIFEST_END`, and while true, append each line verbatim (preserving leading whitespace/indentation) to the current manifest buffer; flush the buffer to `manifest_<n>.yaml` in `/home/user/operator/recovered/` when the END marker is hit, incrementing `n` sequentially, and make sure the boundary marker lines themselves are never written out. Create the `recovered/` directory first if missing. For `serve.py`, use Python's `http.server` with `ssl.SSLContext` (`wrap_socket` or `context.load_cert_chain` with the given `cert.pem`/`key.pem`) wrapping a `SimpleHTTPRequestHandler` or `ThreadingHTTPServer` bound to `0.0.0.0:8443`, with its working directory (or `directory=` kwarg) set to `recovered/`. Launch it backgrounded (`nohup python3 serve.py &`) and verify with `curl -k https://127.0.0.1:8443/manifest_0.yaml` that the extracted content round-trips correctly before finishing.