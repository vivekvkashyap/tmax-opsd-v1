Start by launching `/app/fixture/start_services.sh` and confirming both the producer (8081) and cache (8083) are actually reachable via curl before writing any code. Read `/app/legacy/ecc.js` closely and port it line-by-line into `/home/user/workspace/ecc.py`, preserving the exact bit-manipulation/parity logic (Hamming-like encode/decode, error-position detection, checksum) rather than reimplementing from a general description — subtle differences in bit ordering or parity calculation are the most common source of failure here. Validate `ecc.py` in isolation first: write `test_ecc.py` with unit tests covering zero-error, single-bit-flip-in-various-positions, and checksum-mismatch cases, and get those passing before touching the integration script. Only then build `integration.py` to loop 500 times against `GET /telemetry`, decode+correct each `raw_payload` using `ecc_block` via your ecc module, POST the corrected hex to the cache's `/verify` endpoint, and append `{"id":...,"status":"success|failed"}` per iteration to `/home/user/workspace/results.json`. Since the hidden verifier grades `ecc.py` standalone against 1000 known 1-bit-error cases requiring >=95% correct recovery, prioritize correctness of the decode/correct algorithm over the integration plumbing.
