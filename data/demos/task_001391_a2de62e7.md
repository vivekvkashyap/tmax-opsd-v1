Work through this multi-phase task in order. Phase 1: diff the vendored `/app/vendored/prober-cli` Makefile against the actual source filenames present (it likely references a file that doesn't exist) and fix the build target; find the hardcoded timeout constant in the Go source and correct its unit so requests get a real multi-second budget instead of failing instantly. Build to `/home/user/prober-cli` and smoke-test against a real URL. Phase 2: write `/home/user/sanitizer.go` reading stdin line by line — parse each URL, resolve the host as an IP, and use Go's `net` package (`IsLoopback`, `IsLinkLocalUnicast`, `IsPrivate`, plus an explicit check for the AWS metadata IP) rather than hand-rolled string prefix matching, which is easy to bypass; also reject non-http(s) schemes. Print only URLs that pass every check, unmodified. Phase 3: write an `expect` script to drive the passphrase prompt, then stand up a TLS listener on 127.0.0.1:8443. Phase 4: create the `sre-logs` group, wire the pipeline script, and set exact ownership/permissions (`chown`, `chmod 640`) on the log file. Test the sanitizer against both safe and malicious sample URLs before finishing.