Split this into two independent, separately-verifiable pieces. For the Go log redactor, write a program that reads `/home/user/auth.log` line by line and applies regexes to extract the username from each of the three specific phrasings (`Failed password for invalid user X `, `Failed password for X `, `Invalid user X `) — be careful the "Failed password for invalid user" case doesn't also get incorrectly matched a second time by the plainer "Failed password for" pattern; check for "invalid user" first or use a single alternation regex with capture groups so each line is classified once. A captured username is sensitive if it contains `@` or equals exactly `root`; replace only that captured username substring with `[REDACTED]`, leaving surrounding log text (timestamps, IPs, ports) untouched, and pass through every other line (including lines like a password accidentally typed as a username, which don't match your three phrasings) unmodified. Compile and run the Go program so the output file actually gets written, then diff mentally against the original to sanity-check line count matches. For key rotation, use `ssh-keygen -t ed25519 -f /home/user/.ssh/id_ed25519_rotated -N ""` — the empty `-N ""` gives a passphrase-less key — without touching any pre-existing key files in that directory.