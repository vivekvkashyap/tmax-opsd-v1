First fix the vendored package: grep the `filelock-3.12.2` source (especially the Unix backend file) for suspicious flag names — look for `os.O_EXCL` usage and check for a typo variant like `os.O_EXCLL` that doesn't exist in the `os` module, causing an `AttributeError`. Correct it to `os.O_EXCL`, then `pip install -e /app/vendor/filelock-3.12.2` (or equivalent) so `import filelock` works, and sanity-check with a quick `FileLock(...).acquire()` call. Then write `/home/user/curate.py` with argparse for `--input/--output/--log`. The critical correctness trap is symlink-loop detection while searching for `meta.json`: walk the directory tree tracking visited real paths (e.g. `os.path.realpath` on each directory before descending, and skip/reject if you've already visited that real path, or use `os.walk(followlinks=False)` and additionally check for symlinked directories yourself) rather than naively recursing, which would hang or crash on a loop. Wrap JSON parsing in try/except so malformed `meta.json` marks the bundle EVIL rather than crashing the whole script. Use the fixed `filelock` around your CSV log writes for concurrency safety. Test against `/app/corpus/clean` and `/app/corpus/evil` before considering it correct, since the hidden verifier corpus will exercise the same edge cases (loops, bad JSON, missing meta.json).
