Scaffold with `cargo new manifest_builder` in `/home/user/manifest_builder`, take the directory arg via `std::env::args()`. Walk the tree manually (or with the `walkdir` crate) using `fs::symlink_metadata` (not `fs::metadata`) so you can detect symlinks before following them, and maintain a `HashSet` of already-visited canonical directory paths (via `fs::canonicalize`) — before recursing into any directory (symlinked or not), canonicalize it and skip if already in the set, which is what breaks infinite loops like a symlink pointing back to its own parent. For every entry that is (or resolves to) a regular file, canonicalize its path and only hash it if that canonical path hasn't been seen before (a second `HashSet` for files), which naturally deduplicates symlinked file aliases pointing at the same target. Compute SHA-256 with the `sha2` crate over the raw file bytes, format each line as `<hex>  <canonical_path>` with exactly two spaces, collect all lines, `sort()` them lexicographically by path before writing, and write to `/home/user/manifest.txt`. Build with `cargo build --release` and run against `/home/user/project_data`.
