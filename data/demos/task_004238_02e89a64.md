Start by black-box probing `/app/auth_svc`: craft a set of test tokens (`base64(header).base64(payload).base64(signature)`) where the JSON header varies only the `alg` field (`HS256`, `none`, `NONE`, `debug`, and other values/casings), keeping everything else constant, and observe which ones the binary accepts vs. rejects (check exit status / stdout, and corroborate with `strings`/`ltrace` on the binary for any comparison calls against header text). This tells you exactly which `alg` value(s), case-insensitively, cause it to skip HMAC verification — don't assume a single hardcoded string; verify empirically which variants trigger the bypass.

Then write `detector.py`: for each base64 token line, split on `.`, base64-decode the first (header) segment — pad with `=` as needed since raw tokens often omit padding — `json.loads` it, and check the `alg` field (case-insensitively) against the bypass value(s) you discovered. If it matches, write a redacted line to the output; if not, write the original token unchanged. Handle decode/JSON errors defensively (treat malformed tokens conservatively) since the corpus will include edge cases. Validate against small hand-built clean/malicious samples before trusting it on the full corpus.
