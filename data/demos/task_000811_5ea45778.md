Work through the four phases in order since each depends on the previous extraction. Decode with `base64 -d incoming_data.b64 > archive.tar.gz`; verify it's a valid gzip/tar (e.g. `tar -tzf` succeeds) before writing PASS to integrity.log, then extract with `tar -xzf archive.tar.gz -C app_data/`. For TLS, use `openssl req -x509 -newkey rsa:2048 -nodes -keyout new_key.pem -out new_cert.pem -days 365 -subj "/CN=secure.internal"` — put the CN exactly as specified and nothing else conflicting in the subject. For the firewall policy, read allowed_ips.txt line by line (strip blank lines/whitespace) and emit them as a JSON array in file order under `inbound_allow`, with `default_action` fixed to `"deny"` — use a real JSON serializer (python json.dumps or jq) rather than hand-built string concatenation to avoid formatting bugs. For the credential rotation, locate the line assigning `SECRET_TOKEN` in app.py via grep/sed, and replace only its string value with the base64 encoding of the literal word `rotated_secure_token_999` (compute this with `echo -n rotated_secure_token_999 | base64`), keeping the variable name and surrounding code untouched.
