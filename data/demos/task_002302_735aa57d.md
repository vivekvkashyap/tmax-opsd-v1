Read `/home/user/auth_checker.rs` closely: identify the exact cipher used to "decrypt" the token payload and match it to the correct CWE category for weak/broken cryptography (a single-byte XOR is a textbook case of using a non-cryptographic or trivially-breakable algorithm) — write that CWE ID to `/home/user/cwe.txt`. To forge the token, note the code XORs each decoded byte with a hardcoded constant to produce the plaintext it checks for `role=admin`; work out that key from the source (don't hardcode a guessed value), then compute, for each character of the literal string `role=admin`, `ord(char) XOR key`, and hex-encode the resulting bytes in order (two lowercase or uppercase hex digits per byte, no separators, no newline) to `/home/user/forged_token.txt`. Verify your work by compiling the Rust source with `rustc` and actually running it against a file containing your forged hex string as an end-to-end check, rather than trusting the arithmetic alone.
