Debug by diffing the current config against the stated spec line-by-line rather than guessing. In `start_backends.sh`, check each `python3 -m http.server <port> --bind <ip>` invocation individually — one node likely has the wrong port number (not matching 8081/8082/8083) and another likely binds to the wrong IP (not `127.0.0.1`); fix each to match the three required port/bind combinations and launch with `nohup ... &` so they persist in the background, then confirm with `ss -tlnp` or `curl` that all three actually answer on 127.0.0.1. In `haproxy.cfg`, the frontend `bind` line and the three backend `server` lines both need their ports corrected to match the spec (frontend on 8080, backends on 8081-8083) — start haproxy afterward with `haproxy -f haproxy.cfg` (no root needed if ports are unprivileged) and verify with `curl 127.0.0.1:8080`. Write `check_capacity.sh` as a one-liner `curl -s http://127.0.0.1:8080/ >> capacity_log.txt; echo >> capacity_log.txt`, chmod it executable, and run it once. For `crontab.txt`, write a standard 5-field cron line with `*/5` in the minute field pointing at the script's absolute path — you don't need to install it into an actual crontab, just create the file.
