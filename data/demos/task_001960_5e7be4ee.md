First get the service running: try `python server.py` and read the startup error — a binary a Python service shells out to or dlopens needs execute permission, so check `ls -l /app/poly_solver` and `chmod +x` it if missing, then confirm it binds `/tmp/poly.sock`. Next build a fuzzing client (`socket.AF_UNIX`) that fires many randomized polynomial-coefficient requests in a loop while sampling the server process's RSS (e.g. via `/proc/<pid>/status` or `psutil`) so you can watch memory climb. To find the leaking commit, use `git bisect start`, mark `HEAD` bad and `v1.0-stable` good, and at each step run your fuzzer for a fixed request count and compare RSS growth against a threshold to decide good/bad — wire this up as `git bisect run` so it's automated. Once you have the culprit commit, inspect its diff for the leak pattern (e.g. a global list/dict accumulating every request's data or subprocess output, never cleared or bounded), then patch `main` to stop that accumulation while preserving correct request handling. Validate by rerunning the fuzz benchmark and confirming peak RSS stays under threshold.
