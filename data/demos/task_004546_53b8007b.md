Start by running `tesseract /app/schematic.png stdout` (installing tesseract if needed) to recover the three parameter values from the image — do not guess them; parse the OCR text carefully since OCR can mis-read digits, so sanity-check the extracted numbers look like a plausible seed/drift/volatility before hardcoding them into the C source. Write `mc_sim.c` to run the Monte Carlo loop for 100,000 paths; the key requirement is reproducibility of the final sum across runs — if you parallelize (OpenMP/pthreads), a naive `#pragma omp atomic` or unordered reduction gives non-associative floating point results that vary run to run, so instead either run single-threaded with a fixed accumulation order, or use Kahan summation, or have each thread reduce its own contiguous chunk deterministically and combine chunk sums in a fixed (e.g. thread-index) order. Accept `start_price` as a parameter/CLI arg to the compiled binary so both the `/simulate` endpoint and the regression step (10 runs at prices 100..190 step 10) can invoke the same deterministic binary. Fit the line with ordinary least squares (`m = cov(x,y)/var(x)`, `c = ybar - m*xbar`) over those 10 (start_price, final_price) pairs. Build the HTTP server (Python or C) to shell out to the compiled C binary per request, check the `Authorization: Bearer sim-perf-token-2024` header on every route, and have `start_service.sh` compile the C code then launch the server so the verifier's 5-second wait is sufficient.
