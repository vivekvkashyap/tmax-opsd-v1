Read the existing `/home/user/recommend.py` first to see exactly what's broken, then rewrite it in place rather than patching blindly. For schema enforcement: use `pd.to_numeric(df['id'], errors='coerce')` to turn unparseable ids into NaN, then `dropna(subset=['id','title','text'])`, then `.astype(int)` on id. For the leakage fix, the key discipline is ordering: call `train_test_split` on the cleaned dataframe (or its text/id/title columns) BEFORE creating or fitting any `TfidfVectorizer`; fit the vectorizer only on `train['text']`, then `.transform()` both train and test — never call `fit_transform` on the full/combined data. Compute mean pairwise cosine similarity within each split using `cosine_similarity(X_train).mean()` (note this includes the diagonal self-similarity of 1.0 unless you explicitly exclude it — follow the task literally as written) and write to the metrics JSON with the exact key names. For recommendations, transform the fixed query string with the train-fitted vectorizer, compute similarity against the training matrix only, take the top-2 by similarity descending, and write a 3-column CSV (id, title, similarity) in that exact order. Actually execute the corrected script and confirm both output files are produced.