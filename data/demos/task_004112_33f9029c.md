Work through the four phases sequentially, verifying output at each step before moving to the next, since later phases depend on earlier ones being exactly right. Phase 1: extract frames with `ffmpeg -i /app/incident.mp4 frame_%04d.png`, compute each frame's mean grayscale brightness (e.g., via Python + Pillow: convert to `'L'` mode and average pixel values), threshold at >100 → bit 1 else 0, then group consecutive bits into 8-bit big-endian bytes and convert to ASCII to recover the key — verify the recovered string looks like a plausible readable key before trusting it. Phase 2: write a small C (or prototype in Python first) routine that XOR-decrypts `/home/user/corrupt_backup.wal` by cycling the recovered key over every byte, then parse the resulting buffer strictly per the documented layout (2-byte LE path length, path bytes, 4-byte LE data length, data bytes) — test decryption correctness by checking the parsed structure looks sane (printable paths, reasonable data lengths) rather than assuming the key/XOR logic is right on the first try. Phase 3: for each parsed path, reject (skip extraction of) any path containing `../` anywhere or starting with `/` — implement this check before any filesystem write, not after. Phase 4: after extraction, hash each file's content (e.g., `sha256sum`) to find duplicate sets, keep one as the original, and replace the rest with `ln` hard links to it.