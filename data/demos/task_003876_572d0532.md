First extract the physical rule from the image: run OCR (e.g. `tesseract /app/system_rules.png stdout`, installing tesseract-ocr if needed) to recover the exact energy formula (in terms of the particle's position and velocity components) and the numeric rejection threshold printed in the image, rather than guessing physics constants — verify the OCR text is legible and re-run/crop/preprocess (contrast, scale, `--psm` mode) if characters are garbled, since a misread digit or symbol will silently fail every clean/evil classification; cross-check the recovered rule against the known-good corpus files (a formula/threshold that misclassifies the labeled clean/evil examples is wrong and must be re-extracted). Implement validate_particles in main.c to compute each particle's energy per the recovered formula and reject (return invalid) if any particle's energy strictly exceeds the recovered threshold — parallelize the per-particle loop with `#pragma omp parallel for` and a proper reduction (e.g. a reduction clause or per-thread flags OR'd together afterward) rather than writing to a shared int from multiple threads unsynchronized, which causes a data race. Fix the Makefile by adding `-fopenmp` to both compile and link flags. Build with `make`, place the binary at /home/user/filter, and validate by running it against every file in /app/corpus/clean (expect exit 0) and /app/corpus/evil (expect exit 1), fixing any mismatches before finishing.