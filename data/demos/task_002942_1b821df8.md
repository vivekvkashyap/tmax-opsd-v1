Work the three phases in order and verify each before moving on. Phase 1: use ffmpeg or a video library to extract all 80 frames of evidence.mp4 and classify each as black or white by sampling pixel brightness (average luminance near 0 vs near 255, with a sensible midpoint threshold); group frames into 10 chunks of 8 (MSB first) and convert each chunk's bits into one byte, concatenating all 10 bytes into a 20-char lowercase hex string — verify your bit-to-byte packing direction (MSB first within each byte, byte order = frame chunk order) by sanity-checking frame count and grouping logic before trusting the result. Phase 2: read encrypted_token.bin, XOR each byte with the recovered key repeating every 10 bytes, and read off the decoded ASCII string; extract the script name after `VULNERABLE_TARGET=`, then read that specific script in /home/user/scripts/ and classify its vulnerability by pattern — unsanitized/unquoted variable passed into a shell command (CWE-78), overly permissive file/dir permissions (CWE-732), or predictable/insecure temp file creation e.g. `/tmp/$RANDOM` or fixed temp paths (CWE-377) — pick the one that actually matches what the script does, don't guess. Phase 3: stand up a minimal HTTP server (Python's http.server or Flask) bound to 127.0.0.1:9090 serving GET /report with the exact JSON keys "key", "script", "cwe", and leave it running in the background (nohup/&) so the verifier can poll it after your turn ends.
