Write the expect script to explicitly script the two-prompt handshake rather than relying on generic timeouts: `spawn nc 127.0.0.1 8022`, then `expect "User: "` followed by `send "sre_admin\r"`, then `expect "Pass: "` followed by `send "KeepAlive42!\r"`, then a final `expect` block with multiple patterns — one matching `"AUTH_SUCCESS: System Healthy"` that does `exit 0`, one matching `"AUTH_FAILED"` that does `exit 1`, and a `timeout`/`eof` clause that also does `exit 1` (set a reasonable `set timeout N` up front so a hung connection doesn't block forever). Make it executable with a `#!/usr/bin/expect -f` shebang and `chmod +x`. For `monitor.sh`, run `./healthcheck.exp`, capture `$?` immediately after, and branch: on 0 append `STATUS: UP` to the log; on nonzero, call `/home/user/service_ctl.sh restart` first and then append `STATUS: DOWN - RESTARTED` — order matters since the log line should reflect the restart action actually being taken. Test both branches manually (service up, then stopped) before considering it done.
