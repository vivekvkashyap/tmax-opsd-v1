Inspect the schema in `/home/user/expenses.db` first (`sqlite3 expenses.db .schema`) to confirm join keys. Write `/home/user/audit.py` using `sqlite3.connect` and a single parameterized query: join `expenses` to `employees` (on `emp_id`), `departments` (on `department_id`), and `categories` (on `cat_id`), and compare each expense's `amount` against a subquery/CTE computing `AVG(amount)` per `cat_id` over the *entire* `expenses` table (not scoped to the department) — this company-wide average is the key detail naive attempts get wrong by accidentally filtering the average subquery to the department too. Use `strictly greater than` (`>`, not `>=`) for the anomaly condition, and pass the department name via a `?` placeholder to avoid injection. Build result dicts with exactly the required keys, rounding `category_average` to 2 decimals in Python (`round(x, 2)`) after computing it in SQL, and sort the final list first by `date` ascending then `amount` descending before dumping to `/home/user/audit_report.json` with `json.dump`. Run `python3 /home/user/audit.py "Sales"` at the end to actually produce the file.