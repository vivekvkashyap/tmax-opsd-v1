First get the vendored library compiling before writing a line of the classifier — run `make` in `/app/vendored/BackupUtils-1.2.0/` to see the actual compiler errors rather than guessing; a `-std=c++98` flag combined with C++17 features (`std::string_view`, `std::filesystem`) in the source will fail loudly, so bump the `CXXFLAGS` standard version in the Makefile, and separately check each source/header file for simple typos in `#include` lines (compiler errors here are very explicit about the missing header name) — fix each error the compiler reports one at a time rather than rewriting broadly. Once `libbackuputils.a` builds cleanly, write `log_checker.cpp` to detect encoding by inspecting a BOM or byte patterns (UTF-16LE has a `FF FE` BOM; distinguishing UTF-8 from ISO-8859-1 requires checking for invalid UTF-8 byte sequences), convert to UTF-8 using the library's utility, then do a case-insensitive substring search for the target marker string. Link against `libbackuputils.a` with `-L` and `-l` flags when compiling `log_checker`. Test against both `/app/corpora/clean/` and `/app/corpora/evil/`, checking every exit code — mixed-encoding evil files are the likely place a naive substring search (only checking raw bytes, not the converted text) fails.