Build and verify each layer independently before chaining them. First implement the Rust service: a Caesar cipher that shifts only ASCII letters by `shift` positions (mod 26, per-character, wrapping within same case) while passing through digits/punctuation/spaces unchanged, then base64-encode the result and return it as `{"result": "..."}`; pick a lightweight HTTP crate (axum or tiny_http) plus `base64` and `serde_json`, and test it directly with `curl -X POST http://127.0.0.1:3000/encode -d '{"text":"abc","shift":1}'` before touching Nginx — verify the cipher logic on simple known inputs like shifting "abc" by 1 to "bcd" and confirm the base64 matches expectations independently. Then write the Nginx config: since you lack root, set `pid`, `error_log`, `access_log`, and all `client_body_temp_path`/`proxy_temp_path` directives to writable paths under `/home/user/nginx_temp/`, listen on `127.0.0.1:8080`, and use a `location /v1/telemetry { proxy_pass http://127.0.0.1:3000/encode; }` block. Start Nginx with `nginx -c /home/user/nginx.conf` in the background, confirm both ports are listening with `curl` or `ss -ltnp`, then run the exact required payload through the proxy and save just the response body to `/home/user/test_result.json`.
