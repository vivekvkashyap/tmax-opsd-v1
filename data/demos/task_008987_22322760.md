Two separate, localized edits to `poly_fit.c`. First, regularization: after the `ATA`/`ATB` accumulation loop and before `solve_lu` is called, add `lambda=1e-4` to each diagonal entry `ATA[i][i]` (leave off-diagonals and `ATB` untouched) — this is standard Tikhonov/ridge stabilization and requires no other solver changes since the existing (unpivoted) LU decomposition becomes well-conditioned once the diagonal is boosted. Second, implement `integrate_poly`: the definite integral of `sum(c_i * x^i)` from `start` to `end` is `sum(c_i * (end^(i+1) - start^(i+1)) / (i+1))` for `i` in `0..degree` — loop over `coeffs`, accumulate this sum with `pow()` or iterative power tracking, and return it. Don't hand-derive closed forms per degree; the general summation formula is degree-agnostic and less error-prone. After both fixes, compile with the exact given command (`gcc -O2 poly_fit.c -o poly_fit -lm`), run it from `/home/user/app/`, and check `output.txt` matches the required label/format exactly, including `%.4f` on every value and the exact literal text `Coefficients:` / `Integral(0 to 10):`.
