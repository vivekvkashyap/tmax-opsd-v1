Before touching `jq`, inspect `/home/user/users.csv` and `/home/user/purchases.csv` and read the existing buggy `jq` pipeline in `/home/user/report.sh` closely — the bug is that it iterates `$user` and `$purchase` in nested loops without any equality condition, producing a full Cartesian product instead of a join keyed on `user_id`. Fix it by restructuring the jq filter to: filter the users array to only those with `status == "active"`, then for each active user, filter/select purchases where `purchase.user_id == user.user_id` (note CSV-derived JSON values are strings, so compare consistently or cast with `tonumber`), sum the `amount` values (cast to number) for the matched purchases, and emit `{name, total_spent}`. Use `map`/`select` or a `group_by` keyed on `user_id` rather than a nested double-iteration join to avoid reintroducing a cross join. Finally sort the resulting array by `name` with `sort_by(.name)`. Keep the CSV-to-JSON conversion steps and file paths unchanged, only replace the jq filter, then re-run the script and inspect `/home/user/summary.json` to confirm the totals look correct per user before finishing.