Build this as a three-stage Unix pipeline and validate each stage's output before chaining the next. Stage 1: run `ffmpeg -i /app/video.mp4 -vf signalstats -f null -` and capture stderr, where each processed frame emits a log line containing `YAVG:<val>` and `SAVG:<val>` tokens among others — parse those two fields per line in order (frame_num = 1-based line/frame counter) rather than assuming a fixed column position, since signalstats output has many other fields interleaved. Stage 2: emit a clean `frame_num,YAVG,SAVG` CSV from that parse (awk or a small script both work). Stage 3: apply the given logistic model per row — compute `Z = W_Y*YAVG + W_S*SAVG + B` and `P = 1/(1+exp(-Z))` using `awk` with its `exp()` builtin (avoid shelling out to `bc` per row, which is slow and error-prone at scale), printing at least 5 decimal places. Write final `frame_num,probability` rows to `/home/user/predictions.csv`. Since grading is MSE against a reference re-run of ffmpeg+model, sanity-check a handful of your own YAVG/SAVG/probability values by hand against the formula before trusting the full run, and make sure every frame ffmpeg reports gets a row (no silently dropped frames).