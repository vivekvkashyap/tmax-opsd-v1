This is an end-to-end multi-service pipeline — bring services up and test each hop before wiring the whole chain. First fix `/home/user/nginx.conf` so a `location /api/ { proxy_pass http://127.0.0.1:5000/; }`-style block correctly strips/forwards the path to match whatever route the Flask app in `/app/start_services.sh` actually listens on (check the Flask route path exactly, e.g. `/ingest`), then launch nginx with `nginx -c /home/user/nginx.conf` and curl the endpoint directly to confirm it reaches Flask before touching the worker. For the C++ worker, use `hiredis` to `BLPOP incoming_queue 0` in a loop, parse each JSON item with `nlohmann::json`, and compute the joint log-likelihood analytically: log-pdf of a Normal(mean,std) is `-0.5*log(2*pi*std^2) - (x-mean)^2/(2*std^2)`; sum the log-pdf for `a~N(50,5)`, `b~N(100,10)`, and the conditional residual `c - (0.5*a+0.8*b)` treated as `N(0,2)`. Don't hardcode a threshold blindly — run the clean and evil corpora through your log-likelihood calculation first (as a quick standalone test), observe the score distributions separate cleanly, and pick a threshold strictly between the two clusters. Push `{"id":...,"status":"CLEAN"|"REJECT"}` to `processed_queue`, and leave the compiled worker running in the background.