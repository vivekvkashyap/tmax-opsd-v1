Build and test the two Go services independently before wiring monitoring: backend.go should be a minimal net/http server on 127.0.0.1:8081 that reads the full request body, appends `ACCEPTED: <n> bytes` (byte length of the raw body, not a re-serialized version) to backend.log, and returns 200. proxy.go on 127.0.0.1:8080 should read the body into a buffer first (so it can both inspect and forward it unmodified), `json.Unmarshal` into a generic `map[string]interface{}`, and safely walk `metadata -> annotations -> "proxy.local/managed"` with type assertions that don't panic on missing/wrong-typed keys — treat any unmarshal error or missing/incorrect value as REJECTED (403 + log line), and only on exact string `"true"` forward the original body bytes via a fresh POST to the backend URL. For monitor.sh, check listening ports with `ss -tln | grep -q ':8080'` (and 8081) rather than assuming processes are still running, `go build` and launch missing services with `nohup ... &`, and make the script idempotent so re-running it doesn't spawn duplicates. Install the cron line with `(crontab -l 2>/dev/null; echo "* * * * * /home/user/manifest-operator/monitor.sh") | crontab -`, then confirm with `crontab -l`.
