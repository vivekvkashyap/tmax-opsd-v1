Install `libsqlite3-dev`, then structure the C++ program in clear stages: (1) parse the CSV with a simple line-splitter (fields have no embedded commas here) and `CREATE TABLE transactions(...)` in an in-memory SQLite DB (`:memory:`), bulk-inserting via a prepared statement inside a single transaction for speed, then add indexes on `source_account` and `timestamp` (and `target_account`) since both the window-function query and the cycle join will filter/join on these. (2) For suspicious accounts, use a window function over each source_account's transactions ordered by timestamp with `RANGE BETWEEN` an interval of 86400 preceding — SQLite's window RANGE frames work on numeric ordering columns like your integer timestamp, so `SUM(amount) OVER (PARTITION BY source_account ORDER BY timestamp RANGE BETWEEN 86400 PRECEDING AND CURRENT ROW)` gives the rolling 24h sum; select accounts where any row's rolling sum >= 50000.0. (3) For 3-cycles, self-join transactions three times (A->B, B->C, C->A) filtering on A being in the suspicious set and requiring A/B/C distinct, then dedupe cycles. (4) Emit JSON with `suspicious_accounts` alphabetically sorted and `cycles` sorted lexicographically by their JSON array string form, each cycle starting with its suspicious account. Compile with `g++ -std=c++17 ... -lsqlite3` and inspect the generated `/home/user/audit_report.json` by hand against the CSV before finishing.