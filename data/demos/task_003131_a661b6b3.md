Write this as a single bash script with a top-level idempotent setup (`mkdir -p` the three directories) followed by an infinite `while true; do ... sleep 2; done` loop. Inside each iteration, glob `/home/user/k8s/src/*.yaml` and for each file read its first line (`head -n1` or `read` from the file) and switch on the exact string: for `# DEPLOY_STATE: active`, only create the symlink in `/home/user/k8s/live/` if it doesn't already exist (check with `-L`/`-e` before `ln -s` so re-running doesn't error or thrash) pointing at the absolute path of the source file; for `# DEPLOY_STATE: archived`, first remove any existing live symlink with the same name (`rm -f`), then `mv` the file from `src/` to `archive/` — do the removal before the move so a stray symlink target isn't left dangling. Ignore any file whose first line doesn't match either pattern. At the end of every loop pass (regardless of what was processed), overwrite `/home/user/operator.status` with exactly two lines: `$$` (or `$BASHPID`, matching whatever PID the test will see for the running process) on line one, and `$(date +%s)` on line two. Make the script executable and launch it detached (`nohup ./operator.sh & disown`, or `setsid`) so it keeps running after your shell session ends, and confirm with `ps`/`pgrep` that it's alive before finishing.
