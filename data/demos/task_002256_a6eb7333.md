Fix this as three independent, testable bugs rather than one big change. First, the C extension build: inspect `/app/processor/ext/setup.py` and `entropy.c` — the linker error on `log2` almost certainly means the `Extension()` call is missing `libraries=['m']`; add it and re-run `python3 setup.py build_ext --inplace` until it compiles cleanly. Second, the math bug: Shannon entropy in bits is `H = -sum(p * log2(p))`, so find where the C code does `sum += p * log(p)` (natural log, wrong sign/base) and change it to accumulate `-= p * log2(p)`; verify with a manual case like "AABB" (expect entropy 1.0) and "A" (expect 0.0) before moving on. Third, the ingestor crash: wrap the `recv().decode('utf-8')` and `json.loads()` calls in try/except for `UnicodeDecodeError` and `json.JSONDecodeError`; on failure, decode the raw bytes with `errors='replace'` and push to Redis list `dead_letter` instead of crashing, while valid JSON still goes to `logs`. Start Redis with the given config, then the ingestor on `127.0.0.1:5000`, then the Flask processor via gunicorn on `127.0.0.1:8000`, and manually send both valid and corrupted payloads via `nc`/socket plus curl `/stats` to confirm counts and entropy before finishing.