First run OCR (e.g. `tesseract /app/spec.png -`) to extract the port, Basic Auth username/password, and the absolute log path from the image — never hardcode a guess. In `service.go`: replace the relative log file path with the absolute path from the image, opened with `os.OpenFile(path, os.O_RDWR|os.O_CREATE|os.O_APPEND, 0666)` (create the parent directory first if needed) and passed to `log.SetOutput`; fix `ListenAndServe` to bind `127.0.0.1:<port>` from the image, not a hardcoded port; wrap the `/ping` handler with real HTTP Basic Auth validation using `r.BasicAuth()` and compare against the exact extracted credentials with `subtle.ConstantTimeCompare`, returning 401 with a `WWW-Authenticate` header on failure. In `start.sh`, don't rely on the script's working directory — since the Go code now uses an absolute log path this is naturally fixed, but also `cd` to a known directory or use absolute paths for `go build`/`go run`, and launch with `nohup ... &` so it stays running in the background. Verify by curling `/ping` with and without correct Basic Auth. Finally write `/home/user/logrotate.conf` targeting the exact absolute log path with `daily`, `rotate 7`, `compress` directives.