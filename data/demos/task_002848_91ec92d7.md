Set up an isolated environment first (e.g. `python3 -m venv /home/user/ml_env`) so dependency installs don't need sudo. Parse the FASTA files simply — for a `>id` header line followed by one or more sequence lines, concatenate sequence lines until the next `>` or EOF (don't assume exactly one line per record). Implement Raw[i] carefully following the exact formula (ASCII of the char * 10 + sin(i)*5.0, radians), then implement the moving-average filter by treating out-of-range neighbors as 0.0 while still dividing by 3.0 (a common bug is dividing by a smaller denominator at the edges, or skipping the edge elements — don't do either). Round with standard rounding to exactly 2 decimals (careful with floating point representation — round right before serializing to JSON, and verify by printing values). Write output as a JSON object mapping sequence IDs to their filtered arrays. For test_pipeline.sh, run your pipeline against `/home/user/data/test.fasta` producing output somewhere temporary or reusing the training_data.json path, then diff/compare its parsed JSON content against `/home/user/data/expected_test.json` (prefer a JSON-aware comparison, e.g. `python3 -c "import json,sys; sys.exit(0 if json.load(open(a))==json.load(open(b)) else 1)"`, rather than raw text diff which is sensitive to key ordering/whitespace), exiting 0 on match and nonzero otherwise. Test this script yourself before finishing.