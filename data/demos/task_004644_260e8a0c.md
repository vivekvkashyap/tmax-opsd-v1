The key to passing the size threshold is aggressive, correct filtering before the packer ever runs — don't feed it more than the minimum required data. In your Python script: load `backup_config.json` to get `target_directories` and `last_run_timestamp`; for each directory, walk it and use `os.path.getmtime` to select only `.log` files with mtime strictly greater than the timestamp, skipping older files entirely (don't even read them). For each qualifying file, parse it as a sequence of blocks: use a regex to detect the start of a new entry (`^\[YYYY-MM-DD HH:MM:SS\] \[LEVEL\]`), and treat everything up to the next such marker (including multi-line stack traces) as belonging to the same entry — a naive per-line filter will incorrectly split multi-line entries and either drop needed continuation lines or keep unwanted ones. Keep only entries whose level is `ERROR` or `CRITICAL`, discarding `INFO`/`DEBUG`/`WARNING` entries and their continuation lines. Write the filtered content into `/home/user/staging/`, preserving each file's original name, and skip creating a file for a source that produced zero qualifying entries. Finally shell out to `/app/legacy_packer -i /home/user/staging -o /home/user/incremental.pak` and check the resulting file size yourself (`stat -c %s`) to confirm it's comfortably under the required threshold before finishing.