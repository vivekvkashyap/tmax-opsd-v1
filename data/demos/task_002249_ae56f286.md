Do the three sub-problems independently and validate each before wiring them together. First OCR the formula: run `tesseract /app/formula.png stdout` and read the coefficients and signs it recovers carefully — OCR often confuses digits or drops minus signs, so sanity-check the recovered formula makes sense as a quadratic `y = A*x^2 + B*x + C` before trusting it. Second, fix the Makefile linker error: `helper.c` uses `pow()` from `<math.h>`, and the failure is almost certainly a missing `-lm` link flag — add it to the gcc invocation and confirm `make` in `/home/user/src` succeeds and `./helper 5` runs without error. Third, rewrite `/home/user/fixed_pipeline.sh` to implement the exact recovered formula using integer bash arithmetic (`$(( ... ))`), being careful with operator precedence and negative-number handling for arguments in [-500, 500]; you can optionally call `helper` for the squared term but plain bash arithmetic is simpler and avoids floating-point rounding from `pow()`. Before finishing, manually test your script against a few chosen integers (positive, negative, zero) and hand-compute the expected result from your recovered formula to confirm exact match, since it'll be fuzz-tested against a hidden oracle for bit-exact output.