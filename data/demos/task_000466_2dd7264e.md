Process all CSVs in /home/user/events/ with a single awk pass (or a small bash loop over files piping into awk): for each row, treat the 3rd field as duration and skip the row if it's empty, non-numeric-negative (< 0), or > 5000; for surviving rows accumulate a running sum per user_id in an associative array keyed by field 1. After processing all files, emit `user_id,total` pairs and pipe through `sort -t, -k2,2nr` to sort strictly descending by the numeric total (make sure to use numeric sort, not lexicographic, and watch out for the `-k2,2` field range so it doesn't also try to sort by user_id as a tiebreaker unless needed) and redirect to /home/user/user_durations.csv with no header row.

For the archival step, run `tar -czf /home/user/events_archive.tar.gz -C /home/user events` so the tarball's internal paths are rooted at `events/` (not absolute paths or the full /home/user/... prefix — use `-C` to change directory before adding), verify the archive with `tar -tzf` to confirm the `events/` prefix is present, and only then `rm -rf /home/user/events` to remove the original directory. Do the deletion only after confirming the archive succeeded, so a failed tar doesn't lose data.