Work the four bugs independently and verify each before moving on. (1) Compile first to see the actual lifetime/macro errors; the classic fix for "reference doesn't live long enough" in a `thread::spawn` closure is to wrap shared state in `Arc<Mutex<...>>` (or use a channel/crossbeam scope) and `move` owned clones into the closure rather than borrowing. (2) Read `/home/user/logs/processor_crash.log` for the panicking line/index in `src/formula.rs` and add the missing bounds check so it stops indexing past the payload slice. (3) For the formula itself, do NOT guess — write a small script that hex-encodes many synthetic 3+ byte payloads (systematically varying one byte at a time while holding others fixed) and calls `/app/legacy_oracle <hex>` for each, then fit the relationship between each byte position and the score delta to reconstruct the arithmetic expression used in `formula.rs`; validate by re-running the oracle on new random payloads and diffing against your Rust output before considering it fixed. (4) For the concurrency bug, shrink the `Mutex<HashMap>` critical section to only the map mutation, not the whole processing loop, then `cargo build --release` and time the run on `ingress.pcap`.
