Work through this as three independently-verifiable pieces. (1) Append the three `export VAR=value` lines to `~/.bashrc` exactly as specified, then confirm with `bash -c 'source ~/.bashrc; echo $BACKEND_1'`. (2) Generate the self-signed cert with a single non-interactive openssl command that sets CN=localhost, e.g. `openssl req -x509 -newkey rsa:2048 -nodes -keyout /home/user/certs/proxy.key -out /home/user/certs/proxy.crt -days 365 -subj "/CN=localhost"` — verify cert/key match afterward by comparing `openssl x509 -noout -modulus -in proxy.crt` against `openssl rsa -noout -modulus -in proxy.key`. (3) For the nginx config, remember this must run entirely unprivileged: put `daemon off;` at top level (outside any block), set `pid /home/user/nginx.pid;`, point `error_log` and `access_log` at files under `/home/user`, and use ports/paths >1024 and user-writable so no root is needed. Build the `upstream microservices {}` block with both backend addresses, and a `server { listen 8443 ssl; ... }` referencing your generated crt/key with `location / { proxy_pass http://microservices; }`. Validate iteratively with `nginx -t -c /home/user/nginx-dev.conf`, fixing whatever syntax error it reports each time rather than guessing the whole file at once.