Treat the broken Rust project purely as a spec to read, not code to fix — extract the state machine's transition table from `state.rs` (INIT/SAFE/DANGER/ACCEPT and which ACTION moves between which states) and the parsing/validation rules from `parser.rs` (version header format, size header, per-line ACTION:data parsing, and where EVAL is rejected), then reimplement that logic natively in Python rather than trying to compile or port the Rust. Key details to get exactly right: parse the version as `MAJOR.MINOR.PATCH` and compare it as a proper semantic version tuple (not string comparison) against the bounds `1.2.0 <= version < 2.0.0`; process each `ACTION:data` line in order, checking the special injection rule (`EVAL` while not in `SAFE` state prints `REJECTED_INJECTION` and exits immediately) before applying the general transition table, and any action/state pair not in the table (or ending in a non-ACCEPT state) prints `INVALID_STATE`; on full success reaching ACCEPT print `VALID`. Each outcome must print exactly the specified string with no extra text. For the driver script, iterate `.swp` files from `/home/user/payloads/` sorted alphabetically (e.g. `for f in $(ls /home/user/payloads/*.swp | sort)`), call your parser on each, and append `<filename>: <RESULT>` lines to the results log — test against a few hand-written payloads covering each outcome before trusting it.