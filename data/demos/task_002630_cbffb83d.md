Find the listening port first: use `ss -ltnp` or `lsof -i -P -n` filtered by the `goserver` process name (or `pgrep goserver` then `/proc/<pid>/net/tcp` or `lsof -p <pid>`), then write the numeric port to the target file. Read server.go carefully: the SQLi flaw is that the login handler builds a SQL string via `fmt.Sprintf` string concatenation instead of a parameterized query, so any input containing the literal bypass fragment satisfies the query; the XSS flaw is that the search handler writes the raw `q` query parameter directly into an HTML response without escaping. Confirm both with `curl` against the discovered port before writing detection code. For the log processor, write real Go: use `bufio.Scanner` to read the file line by line, `json.Unmarshal` each line into a struct with `ip`, `method`, `path`, `status`, and body/payload fields, and check for the exact substrings specified for each endpoint's payload (not the URL alone — inspect wherever the payload actually appears, e.g. request body for POST). Collect matches into a `map[string]bool` to dedupe, convert keys to a slice, sort with `sort.Strings`, then write one IP per line to the output file.
