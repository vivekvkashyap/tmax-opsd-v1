Scaffold with `cargo new ml_prep` in `/home/user/ml_prep`, add the `csv` and `serde` crates to `Cargo.toml`. Read both CSVs into `Vec<f64>` (and `Vec<(f64,f64)>` for raw's time/signal pairs) using the `csv` crate with headers enabled. For the Wasserstein step, sort both `signal` vectors independently (not paired by original row), then average the absolute differences of same-index pairs after sorting — don't skip the sort, that's the defining trick of this simplified 1D formula. For OLS regression on `raw.csv`, compute means of time and signal, then slope = Σ((x-x̄)(y-ȳ)) / Σ((x-x̄)²) and intercept = ȳ - slope*x̄ using straightforward accumulation over all rows — no matrix inversion needed for single-variable regression. Compute residuals = actual - predicted per row, and keep rows where `residual.abs() <= 2.0` (strictly greater than 2.0 gets filtered), writing survivors to `cleaned.csv` with the original header. Format every reported number to exactly 2 decimal places (Rust's `{:.2}` format specifier) when writing `report.md`, matching the exact four-line markdown template given, and make sure the whole pipeline (read, compute, filter, write both files) runs from a single `cargo run` invocation.