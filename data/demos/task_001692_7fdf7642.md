Treat this as three small pieces wired together in sequence, each independently verifiable. First create `/home/user/edge_fstab` with exactly the specified line. Then write `sensor_logger.py` to parse it generically rather than hardcoding the mount point: split on whitespace, match the field starting with `UUID=EDGE_SENSOR_DRIVE`, and take the following field as the mount directory — this keeps the script reusable even though the path happens to be fixed here. Then write `deploy.sh` to independently re-derive the same mount point from the fstab file using `grep`/`awk` (do not just hardcode it either), `mkdir -p` that directory before running the Python script, invoke the Python script, and then explicitly check the resulting `telemetry.log` file's contents match the required string before deciding success vs failure — write `DEPLOYMENT_SUCCESS` or `DEPLOYMENT_FAILED` accordingly into `/home/user/deploy_status.log`. Remember to `chmod +x deploy.sh` and actually run it as the final step so all artifacts exist on disk, then spot-check each output file's exact contents.
