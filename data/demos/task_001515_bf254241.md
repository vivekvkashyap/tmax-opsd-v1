First recover the transformation parameters by actually listening to/transcribing `/app/artifact_summary.wav` (e.g. play it and transcribe, or use a local speech-to-text tool if available) rather than guessing — the audio states the exact matrix structure and its numeric entries, so extract those values carefully and note whether it's diagonal, general 3x3, etc. Once you have the parameters, write `/home/user/transform.py` to parse `sys.argv[1:4]` as `float()`, apply the matrix via straightforward elementwise/dot-product arithmetic using Python's native 64-bit floats (avoid numpy unless you confirm it won't change floating-point behavior), and print the three results space-separated with `f"{x:.4f}"` formatting for each component, matching the exact rounding convention (banker's vs standard round-half-up — Python's format spec does round-half-to-even, which is what a natural oracle would likely use too, so don't hand-roll a different rounding rule). Test your script manually against a few sample vectors and verify the arithmetic makes sense (e.g. check magnitudes/signs match what you transcribed) before considering it done, since the grader will fuzz-test many random inputs and require bit-exact matches.