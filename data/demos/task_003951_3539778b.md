Build this in three independently-testable layers and confirm each is actually running before moving to the next. (1) Launch QEMU with exactly the flags requested (`-S -daemonize -nographic -vnc :5 -pidfile /home/user/vm.pid` or similar), then verify with `ps`/`pgrep` and `ss -tlnp | grep 5905` that it's really listening on VNC port 5905, since display `:5` maps to `5900+5=5905`. (2) Write `monitor.c` to read the PID from the pidfile, use `kill(pid, 0)` plus checking `errno == ESRCH` to determine liveness, and write the exact byte-for-byte status strings (including the literal `\n` sequences) to `status.txt` — compile with `gcc` and run once to populate the file, then mentally trace what happens when the process is later killed (the file must flip to the STOPPED state next run, so don't hardcode based on a one-time check). (3) Generate the cert/key with `openssl req -x509 -newkey rsa:2048 -nodes ...`, then write a Python `http.server`-based HTTPS server that wraps its socket with `ssl.SSLContext` loaded from your cert/key, serving `/home/user/web/`, bound to `0.0.0.0:8443`. Launch both QEMU and the Python server as backgrounded/detached processes and confirm via `curl -k https://localhost:8443/status.txt` before finishing.