Build this pipeline stage by stage, testing each independently. First, run `ffprobe` with `-show_frames`/`-show_entries frame=pkt_pts_time,pkt_size -of csv` (or the packet-level equivalent) against `sensor_feed.mp4` to produce the headless CSV — check the row count and format match what downstream steps expect before moving on. Second, write `validate_schema.py` as a strict, adversarial-input-hardened validator, not a happy-path one: reject any row where column 1 isn't a parseable non-negative float, column 2 isn't a parseable integer > 0, or the row contains extra fields/unexpected characters (e.g. SQL-like tokens, non-numeric junk in numeric fields, malformed delimiters) — exit 1 on the first bad row, exit 0 only if every row is clean. Test it against several deliberately malformed examples you construct yourself (bad delimiters, injected text, negative sizes) as well as clean ones, since it will be fuzzed by a hidden adversarial corpus. Third, only load files that pass validation into SQLite, add an index on the timestamp column to support windowed range queries efficiently, and write a tumbling-window aggregation using `CAST(timestamp AS INT)` (or `floor`) as the window key with `GROUP BY`/window functions to get the max size per second — sanity-check a few window boundaries by hand against the raw CSV.