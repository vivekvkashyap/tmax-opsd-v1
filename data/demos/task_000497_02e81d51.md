Build this as a bash script that orchestrates a small Python venv, keeping the parsing/statistics logic in a short embedded Python snippet rather than trying to do floating-point math in pure bash. Structure `/home/user/etl_pipeline.sh` to: create the venv with `python3 -m venv /home/user/venv`, activate it (or call its interpreter directly via `/home/user/venv/bin/python`), `pip install numpy scipy`, then extract latency values from `/home/user/raw_logs.txt` — use a regex to pull the number after `latency_ms:` on each line, and explicitly discard/skip lines where that field is empty, `NaN`, `null`, or fails `float()` conversion (don't just rely on `float()` throwing, since `NaN` and `null` need distinct handling — `float('NaN')` actually succeeds in Python but should NOT be treated as valid data here, so filter it out explicitly by string match before conversion). Compute the sample mean and a 95% CI using the t-distribution (`scipy.stats.t.interval` or the manual `mean ± t_crit * sem` formula with `dof = n-1`), not a normal-distribution z-interval — using z instead of t will produce visibly wrong CI bounds for finite samples. Round all three values to exactly 2 decimal places and write the JSON with exactly the keys `mean`, `ci_lower`, `ci_upper` to `/home/user/metrics.json`, then chmod +x and run the script once.
