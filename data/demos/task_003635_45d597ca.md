Write a Python script that first enforces the schema: coerce columns `s1`..`s10` of `/home/user/sensor_data.csv` to numeric with `pd.to_numeric(col, errors='coerce')` (turning unparseable strings into NaN), then drop any row with a NaN in those columns. For the bootstrap loop, iterate `i` from 0 to 499 and generate each resample with `df_clean.sample(n=len(df_clean), replace=True, random_state=i)` exactly as specified — using a different sampling call or seeding scheme will silently produce non-reproducible results that fail verification, since the grader recomputes the same fixed-seed bootstrap independently on your cleaned data. For each resample: center by subtracting column means, compute the covariance matrix (e.g. `np.cov(centered, rowvar=False)`), get eigenvalues (`np.linalg.eigvalsh` returns them ascending, so the largest/last one is PC1's variance), and divide by the sum of all eigenvalues to get the PC1 variance ratio. Collect all 500 ratios, then compute their mean and the 2.5th/97.5th percentiles (`np.percentile`), rounding each to 4 decimal places, and write them to `/home/user/pca_stability.json` under the exact keys `mean_pc1_var`, `ci_lower`, `ci_upper`.
