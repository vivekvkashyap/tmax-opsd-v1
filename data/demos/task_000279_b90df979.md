Read `/home/user/config_audit.log` line by line, splitting on whitespace, and keep only rows whose `CONFIG_KEY` field equals exactly `worker_threads` — preserve their original chronological order (the file is already ordered by timestamp, so don't re-sort). Convert each `TIMESTAMP` (ISO-8601 with `Z`) to a UNIX epoch integer, e.g. in Python `int(datetime.fromisoformat(ts.replace("Z","+00:00")).timestamp())`, or with `date -u -d "$ts" +%s` in bash — make sure you treat it as UTC, not local time. Maintain a sliding window of the last 3 `NEW_VALUE`s seen (as a deque/list), and starting from the 3rd worker_threads event onward, compute their average and emit `epoch,server,rounded_avg`. For rounding, be careful: the spec wants standard "round half away from zero" behavior (40.5 → 41-style), which differs from Python's built-in `round()` (banker's rounding to even) for exact .5 cases — use something like `math.floor(x + 0.5)` for non-negative averages to be safe. Write the emitted lines with no header to `/home/user/worker_threads_ma.csv`.
