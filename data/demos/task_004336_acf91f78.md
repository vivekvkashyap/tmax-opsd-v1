Implement OLS with the closed-form formulas rather than a numerical library: read `sensors.csv` skipping the header, accumulate sums of X, Y, X*Y, and X^2 in a single pass (or store vectors and use `n*Σxy - Σx*Σy` over `n*Σx² - (Σx)²` for the slope, then `c = ȳ - m*x̄`), and compute MSE as the mean of squared residuals `(y_i - (m*x_i + c))²` over all rows — use `double` throughout, not `float`, to avoid precision drift over ~1000 rows. Format each output value to exactly 4 decimal places (`std::fixed << std::setprecision(4)`) and write the three lines in the exact `Label: value` format to `/home/user/output/model_metrics.txt`, creating the `output` directory if it doesn't exist. Write `run_pipeline.sh` to compile with `g++ -O3 -std=c++17 -o process_data process_data.cpp` then execute `./process_data` from the same working directory as the source (so relative input/output paths resolve, or just use absolute paths in the C++ code to be safe), make it executable, and actually run it once so `model_metrics.txt` exists before you finish.
