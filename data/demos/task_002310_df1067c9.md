Build bottom-up and test each layer in isolation before wiring them together. First compile the plugin: implement `compute_diff` with the exact `extern "C"` signature, parse comma-separated ints with `strtol`/`std::stringstream`, use `std::sort` + `std::set_symmetric_difference` (or manual set logic) on the two integer sets, and format into `out_buf` with `snprintf(out_buf, out_max_len, ...)` to respect the length bound; build with `g++ -shared -fPIC -o libdiffplugin.so plugin_diff.cpp`. Second, write the HTTP server: download `httplib.h` (a single header, e.g. via curl from its GitHub raw URL), parse the JSON body manually or with minimal string extraction (no big JSON lib needed for two string fields), `dlopen("./libdiffplugin.so", RTLD_NOW)` + `dlsym` to get a function pointer matching the signature, call it with a stack/heap 8192-byte buffer, and return the buffer as plain text. Test this directly with curl on 9090 before touching nginx. Third, write `nginx.conf` with `user` omitted (rootless), custom `pid`/`error_log`/`client_body_temp_path` etc. all pointing inside `nginx_temp/`, a server block on 8080 that checks `$http_authorization` (via an `if` or a small auth module — simplest is checking `map`/`if` against the literal header) and returns 401 on mismatch, else `proxy_pass` to 9090's endpoint. Chain build.sh/start.sh so backend starts before nginx.
