Follow the pipeline exactly as specified and let numpy/scipy do the heavy lifting rather than reimplementing math by hand, since the verifier will recompute the exact same steps and string-compare. Load `/home/user/raw_signals.npy`, derive `dx = 1.0/(M-1)` from the array's second dimension (don't hardcode M), and for each row compute `np.diff(row, n=2) / dx**2` as instructed — keep the row only if `np.max(np.abs(second_derivative)) < 100.0` (strict less-than, not <=). Stack the kept rows into a new 2D array and save it with `np.save` to `/home/user/filtered_signals.npy`, preserving row order (do not sort/shuffle) since the filter is deterministic given the fixed seed used to generate the raw data. Run `np.linalg.svd` directly on the filtered 2D matrix (not per-row) to get singular values, take the top 3 (SVD already returns them in descending order, so just slice `[:3]`), and format each to exactly 4 decimal places joined by `", "` on a single line in `/home/user/report.txt`. After running, double check the output file's row count/shape and the literal formatting (spacing, decimal places, comma separator) match the specification precisely, since the verifier does an exact string comparison.