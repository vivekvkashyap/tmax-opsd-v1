Build this in three clean stages and test each independently before wiring them together. First, write the C Bayesian filter as a tight loop: `fread` one `double` at a time from stdin until it returns 0, compare against `-999.0` with exact equality, and on a real observation update precision (1/variance) additively — `prec_new = 1/var + 1/obs_var`, `mu = (prec_old*mu + prec_obs*x)/prec_new`, `var = 1/prec_new` — then `fwrite` mu either way. Compile with `gcc -O2` to the exact path required. Sanity-check it by piping a handful of known doubles through `xxd`/Python-generated binary input and confirming output length matches input count. Second, for the video: use OpenCV or ffmpeg to decode frames in order, convert each to grayscale, average all pixel values, and threshold at `< 10.0` to substitute `-999.0`; write the sequence as raw binary doubles (native endianness matches x86/ARM little-endian, matching the spec) with Python's `struct.pack('<d', v')` or numpy `.tofile()`. Third, just pipe the two together: `./bayesian_filter < raw_measurements.bin > smoothed_measurements.bin`. The most common bugs are: comparing floats with a tolerance instead of exact `-999.0` equality, and forgetting the filter must not touch mu/var state on missing values.
