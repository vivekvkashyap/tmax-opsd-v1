Treat this as pure black-box reverse engineering of a binary format: run `/app/legacy_backup_tool` against small, controlled dummy directories you construct yourself (one file, then multiple with different name orderings, then symlinks, then a file shorter than extract_header_bytes) and inspect stdout with `xxd`/`hexdump -C`, diffing byte-by-byte across variations to isolate each field. Look for a magic-bytes prefix, a file count, then per-file records — infer field widths/endianness by changing one variable at a time (add one file, add a longer filename) and watching which bytes shift. Determine sort order with names that differ under different orderings. Modify a single byte of one file's content and diff the corresponding checksum bytes to learn the checksum's size (32 bytes strongly suggests SHA-256). Test symlink and short-file header-padding behavior with `follow_symlinks` true/false. Implement findings natively in Python — no shelling out to the binary — and validate bit-exact equality on directories you construct before finishing, since the grader fuzzes much more broadly.