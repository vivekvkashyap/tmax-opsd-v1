Approach this as fix-then-calibrate-then-wrap. First inspect `/app/fast_bayes_infer/inference.py`'s `GaussianBayes.log_likelihood`: the bug is that a Mahalanobis-distance computation like `diff @ inv_cov @ diff` works for a single 1D vector but is wrong/shape-mismatched when `diff` is a 2D batch of rows — fix it to compute per-row distances correctly, e.g. `np.sum((diff @ inv_cov) * diff, axis=1)` or `np.einsum('ij,jk,ik->i', diff, inv_cov, diff)`, so it returns one log-likelihood value per input row. Reinstall/re-editable-install the package after fixing (e.g. `pip install -e /app/fast_bayes_infer`) so your script picks up the fix. Then use `/app/data/train.csv` (labeled clean/evil) to score every row with the fixed model and inspect the log-likelihood distribution for each label — don't hardcode a threshold from intuition; compute it empirically (e.g. midpoint between the min clean score and max evil score, or via a simple search) so it cleanly separates the two classes on the training set. Finally write `/home/user/filter_pipeline.py` with argparse `--input`/`--output`, load the CSV, vectorize feature extraction, score all rows in one batched `log_likelihood` call for performance, keep only rows above the threshold, and write them out preserving the original header and column order. Validate your script directly against `/app/data/clean/` and `/app/data/evil/` before considering it done.
