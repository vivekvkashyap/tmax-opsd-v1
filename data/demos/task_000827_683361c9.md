Build this bottom-up and test each piece in isolation. First write `setup_env.sh` using `printf` (not `echo`, and definitely not heredocs that append a trailing newline) to create the three artifact files with exact contents and no trailing newline — verify with `xxd`/`wc -c` that byte counts match the string lengths exactly, since a stray newline will silently corrupt every checksum downstream. Second, scaffold the Rust project with `cargo new audit-hash`, then implement Adler-32 by hand exactly per the spec (A starts at 1, B at 0, update per byte with mod 65521, final = `(B<<16)|A`, using `u32` arithmetic) — do not pull in a crc/adler crate. Parse `migrate <legacy> <artifacts_dir> <output_csv>` from `std::env::args()`, read the legacy file line-by-line, for each filename read the corresponding artifact's raw bytes, compute the checksum, and write CSV rows `filename,checksum,security_status` with a header, hardcoding `security_status` to `VERIFIED`. Test the Rust binary manually against the fixture files and independently recompute Adler-32 (e.g. in Python) to confirm your Rust logic matches before wiring up `build_pipeline.sh`, which should just chmod+run setup, `cargo build --release`, then invoke the compiled binary in `target/release/` (not `cargo run`) with the three required paths.