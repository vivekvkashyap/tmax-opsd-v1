Fix each broken piece independently and verify it in isolation before wiring them together. First, edit `/home/user/run_sanitizer.sh` to `export LANG=en_US.UTF-8`, `export TZ=UTC`, and `export BACKEND_PORT=9000` before it execs `./sanitizer`. Second, bring up a persistent SSH local-port-forward from `localhost:9000` to port `80` inside the QEMU VM, authenticating over the VM's exposed SSH on `localhost:2222` with the given credentials (use `sshpass` or a keyed/batch-mode SSH invocation so it doesn't block on a password prompt), and run it backgrounded/detached so it survives after your session — confirm it works with a direct `curl http://127.0.0.1:9000/` before trusting it. Third, implement `/home/user/sanitizer.c`: read the whole file named in `argv[1]` into a buffer, then check for the literal substring `EVIL_PAYLOAD` and for the presence of any backtick character; if either is found print `REJECTED` and `exit(1)`, otherwise print `ACCEPTED` and `exit(0)`. Compile with `gcc` to `/home/user/sanitizer` and test it manually against a few hand-written clean and evil sample files (containing the payload string, containing backticks, and neither) before relying on the hidden corpus to grade it.