Break this into three independently verifiable stages. First, extract per-frame mean grayscale intensity from `/app/fluorescence.mp4` using `ffmpeg`/OpenCV, pairing each frame with its timestamp based on the known 10fps rate (frame i -> t=i/10), and export the (t, I) pairs to a simple text/CSV file the C program can read. Second, write the C MCMC program: implement Metropolis-Hastings for parameters A, B, C with the exponential decay model, uniform priors bounded as specified, a Gaussian likelihood with sigma=2.0, and exactly 10000 iterations — pick a reasonable proposal step size (tune it if acceptance rate looks pathological) and write the resulting chain into `/home/user/posterior.h5` as a dataset named `mcmc_chain` of shape (10000,3) in double precision using the HDF5 C API (link `-lhdf5`). Third, build a small HTTP service on `0.0.0.0:8080` that checks the `X-API-Key` header exactly against the required secret, returns 401 if missing/wrong, and on success reads the HDF5 chain, discards the first 5000 rows as burn-in, and averages the remaining 5000 per parameter into the JSON response. Validate your fit by checking the posterior means roughly match a quick decay-curve estimate from the raw intensity data before finalizing, and leave the server running.