Use `git bisect start`, mark `HEAD` bad and tag `v1.0` good, then let `git bisect run` drive a script that does `make` (or attempts to compile `trajectory.c`) and exits nonzero on failure, zero on success — this finds the first bad commit far faster and more reliably than manual inspection. Capture the reported commit with `git rev-parse` and write the full 40-char hash to `/home/user/bad_commit.txt`, then `git bisect reset` to return to HEAD. Read the compiler error at the bad commit to see what got broken in the Y-position formula, and replace it with the standard projectile motion expression `v0_y * t - 0.5 * g * t * t` using `g = 9.8` in `trajectory.c`; confirm with `make`. For the fuzz target, write `fuzz_test.c` with an `LLVMFuzzerTestOneInput(const uint8_t *data, size_t size)` entry point that bails out if `size` is too small, then reinterprets/copies bytes into two `double`s (e.g. via `memcpy`) and calls `calc_y(v0_y, t)`, discarding the result. Compile with `clang -fsanitize=fuzzer,address trajectory.c fuzz_test.c -o fuzzer` (or link `trajectory.o`), and sanity-check with `./fuzzer -runs=1`.
