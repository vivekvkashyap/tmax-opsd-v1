Write the scanner as a straightforward per-file integrity checker rather than anything clever: for each file in `/home/user/artifacts/`, parse the three labeled lines (`Encoding:`, `Checksum:`, `Payload:`) with simple line splitting on the first `: ` occurrence, `base64.b64decode()` the payload to get raw bytes, compute `hashlib.sha256(raw_bytes).hexdigest()`, and compare it (case-insensitively is safest) against the stated `Checksum`. Collect the filenames where the computed hash differs from the stated checksum — those are "tampered". Sort the tampered filenames alphabetically, and for each write a line `<filename>:<hex>` where `<hex>` is `raw_bytes.hex()` (the decoded payload's bytes re-encoded as hex, not the original base64 string and not the checksum). Write these lines to `/home/user/tampered.log` with no extra blank lines or headers, then re-run and diff against your own re-computation to sanity-check before finishing.