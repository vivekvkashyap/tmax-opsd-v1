Start by reading `vm.c` closely to understand `evaluate`'s exact signature and semantics, then build with `make` to produce `libvm.so`. In `api.py`, the core bug is missing ctypes FFI configuration: before calling `lib.evaluate`, set `lib.evaluate.argtypes = [ctypes.c_char_p]` and `lib.evaluate.restype = ctypes.c_int` so ctypes marshals correctly instead of defaulting to `int` argument/return handling, which corrupts pointer-based string args. Also fix the call site to encode the Python string to bytes before passing it in (`code.encode('utf-8')`), since `ctypes.c_char_p` requires a `bytes` object, not a `str`. Leave the endpoint path/port unchanged. After fixing, start the server in the background (`python3 api.py &`), then verify manually with `curl` against the exact required query before trusting it, and only then redirect that same curl call's output into `/home/user/result.log` (avoid extra whitespace/newlines beyond what curl naturally produces). Trace through `vm.c`'s logic by hand for the given bytecode to sanity check the number that should land in the log — don't just trust the server blindly.
