Start by transcribing `/app/voicemail.wav` — use `ffmpeg`/a whisper CLI (install if missing) to get a text transcript, and read it carefully to extract the exact query parameters (which root job ID to start the recursive chain from, what window function and aggregate to compute, which status to filter on, sort direction, and row limit) rather than assuming them. Do not hardcode values from the description; derive every criterion from what the transcript actually says. Then inspect the `backup_jobs` schema and, given 200k+ rows, create indexes on the columns used for the recursive join and ordering (e.g. `parent_job_id`, `start_time`, `status`) before running the query — an unindexed recursive CTE over `parent_job_id` joins will be far too slow. Write `/home/user/analyze.py` using `sqlite3` to: build a recursive CTE walking the parent-child chain from the transcribed root job, wrap it with a window function computing the cumulative sum of `size_bytes` ordered by `start_time`, then filter/sort/limit per the transcribed criteria, and export the selected columns to `/home/user/report.csv` with a header row matching the output columns. Run the script yourself and sanity-check the CSV before finishing.
