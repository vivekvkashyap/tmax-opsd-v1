This is a black-box format-recovery + reimplementation task — recover the spec from audio, then match the reference binary's behavior without ever shipping or depending on it.
1. Transcribe `/app/voicemail.wav` (install a speech-to-text tool such as whisper or SpeechRecognition/pocketsphinx if not present) to learn the exact binary layout: a fixed magic header, followed by a sequence of records each with a length field, a type field, and a payload, plus which record type(s) count toward the reclaimable total and the exact print format expected.
2. Implement `/home/user/parse_wal.py` using `mmap` (or chunked `struct.unpack_from` streaming) rather than reading the whole file into memory, so it scales to large WAL files.
3. Generate your own synthetic WAL files following the recovered spec (magic bytes + random records with varying lengths/types) and compare your script's stdout against the reference parser byte-for-byte — including exact wording, punctuation, and number formatting — until they always agree, including edge cases like zero records or zero-length payloads.
4. Only after many synthetic files match exactly should you consider the parser done; the grader fuzzes with many more random files than you tested against.
