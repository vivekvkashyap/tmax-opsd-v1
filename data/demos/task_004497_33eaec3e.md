Work through the pipeline layer by layer, testing each before moving on. For storage: `dd`/`fallocate` a 50MB file, `mkfs.ext4` it, and since you lack root for a normal loop mount, use `fuse2fs <img> <mountpoint>` (from the `fuse2fs`/`e2fsprogs` userspace tools) to mount it unprivileged; then create the incoming/processing/archive subdirs and the symlink. For classification, don't hardcode answers — read every file under `/app/corpora/clean/` and `/app/corpora/evil/`, extract discriminating features (e.g. keyword sets, regex patterns present in clean but absent in evil, or a simple scoring heuristic) directly from the corpus contents, and encode that logic in the Rust `alert-filter` binary so it generalizes; iterate against the corpora until 100% pass before considering it done. For the transcript, use an actual speech-to-text tool (e.g. `whisper-cli`/`ffmpeg`+ASR) on `/app/alert_sample.wav` to recover the exact spoken text rather than inventing it, and write that exact string to the incoming file. For services, write the bash wrapper to scan `current_alerts` for `*.txt`, invoke `alert-filter <file> archive/` and move/remove based on exit code, then define the oneshot service and a 1-minute `OnUnitActiveSec=1min` timer, `systemctl --user daemon-reload`, then `enable --now` the timer and confirm with `systemctl --user list-timers` and `is-active`.