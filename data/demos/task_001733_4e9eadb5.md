Build this as a single SQLite query using recursive CTEs and window functions, driven from a Bash script. First write a recursive CTE that walks `parent_account_id` up the chain for each account until it hits a NULL parent, giving you `root_account_id` per account (careful: an account with a NULL parent is its own root). Second, extract `query_time_ms` from the JSON `data` column using SQLite's `json_extract(data, '$.query_time_ms')`. Third, compute the rolling 2-log sum per account using a window function: `SUM(query_time_ms) OVER (PARTITION BY account_id ORDER BY created_at ROWS BETWEEN 1 PRECEDING AND CURRENT ROW)`, then take `MAX(...)` per account to get `max_rolling_query_time` — don't just sum all logs, and don't forget the ordering by `created_at` matters for which pairs get summed. Filter with `HAVING max_rolling_query_time > 100`, `ORDER BY max_rolling_query_time DESC, account_id ASC`, and `LIMIT 5`. Wire this into `sqlite3 -csv -noheader /home/user/app.db "..."` inside `/home/user/generate_report.sh`, redirect to `/home/user/report.csv`, and `chmod +x` the script. Never write to the source database — read-only queries only.