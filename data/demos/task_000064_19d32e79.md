Build and smoke-test each component in isolation before chaining them. Write `backend.py` as a small HTTP server (e.g. `http.server.BaseHTTPRequestHandler` with `do_POST`) parameterized by port via `sys.argv`, that appends the raw request body plus newline to `/home/user/metrics_<port>.log`; launch two instances on 8081/8082 in the background and curl each directly to confirm logging works. Write `lb.py` listening on 8080 that alternates forwarding each incoming POST body to 8081 then 8082 (keep a simple counter/flag for round-robin state), adding the `X-Forwarded-By: ObservabilityLB` header on the outgoing request (e.g. via `urllib.request` or `http.client`) — test with a couple of manual curls to confirm alternation before moving on. For the git hook, `git init --bare telemetry.git`, then write `hooks/post-receive` in Python reading old/new/ref triples from stdin, using `git log`/`git show --stat` (or `--numstat`) on the new commits to get author name and inserted-line counts per commit, and POST a compact JSON payload matching the exact schema to `http://127.0.0.1:8080` for each commit — remember `post-receive` runs with the repo as its working directory context, so use `git --git-dir=... ` or run from within the hook's own environment correctly, and `chmod +x` it. Clone, make the three commits with exact line counts per author, and push all at once so the hook processes each commit. Finally parse both log files with `awk`/`grep` to sum `lines_added` where `author == "Alice"` and write `TOTAL_ALICE=<n>` to the summary file, verifying the arithmetic by hand against the commits you made.
