Start by fixing the build, not the C++ code: grep `/app/vendor/snappy-1.1.10/CMakeLists.txt` for `CMAKE_CXX_STANDARD` and change the `98` to `11` with `sed -i`, then do an out-of-source cmake build (`cmake -DCMAKE_INSTALL_PREFIX=/home/user/local ..`, `make`, `make install`) so headers/libs land under `/home/user/local`. Link your archiver against that prefix (`-I/home/user/local/include -L/home/user/local/lib -lsnappy`, plus `-Wl,-rpath` or set `LD_LIBRARY_PATH` at runtime since it's a non-system path). For the watcher, poll `/home/user/incoming/` on an interval (e.g. `readdir` + small sleep), and for each file: read it fully, call `snappy::Compress`, then `flock(fd, LOCK_EX)` on the archive file descriptor before appending — write the null-terminated filename, an 8-byte size (use a fixed-width type like `uint64_t`, and be consistent about endianness since only your own program reads it back conceptually), then the compressed bytes, then `flock(fd, LOCK_UN)` and `unlink` the source file. Compile, launch the archiver in the background (`&` or `nohup`), then run `/home/user/trigger_incoming.sh`, and poll until `/home/user/incoming/` is empty before checking `repo.bin`'s size — don't kill the background process before it finishes draining the queue.