Do this as three separate small Rust programs (or one with subcommands), run sequentially, each writing intermediate results the next stage reads. First, parse `sensor_data.txt` (pipe-delimited) and group by `thickness_mm`, averaging `count` per group — use a HashMap<thickness, Vec<count>> or running sum/count. Second, solve for mu analytically rather than numerically: since C(x) = C0*exp(-mu*x) + B with C0=100000 and B=200 both known, pick any averaged data point (x, C_avg) with x>0 and invert directly: mu = -ln((C_avg - B)/C0) / x — this is closed-form, no root-finder needed, but you can sanity-check that different (x, C_avg) pairs give a consistent mu, which confirms you parsed/averaged correctly. Third, for the Monte Carlo step, generate 1,000,000 uniform randoms in (0,1] (careful: exclude 0, since ln(0) is undefined — reject/resample any exact 0.0 draws from your RNG, or use a distribution that only returns (0,1]), compute d = -ln(R)/mu for each, and count how many exceed 15.0; use the `rand` crate with `cargo add rand`. Finally compute the analytical probability exp(-15*mu) directly. Write all three results to the JSON with the specified rounding applied only at serialization time.
