Build this as a Unix pipeline in stages, verifying intermediate output at each step rather than writing one giant one-liner blind. Stage 1: `sort -t'|' -k1,1n /home/user/raw_telemetry.txt | uniq` (or `sort -u` then re-sort numerically) to dedupe exact-duplicate lines and order by integer timestamp — dedupe on the whole line, not just timestamp, since different servers share timestamps. Stage 2: for the rolling average, process each server's time series independently (e.g. split by server_name, or use awk keeping a per-server array of the last up-to-3 values as you scan sorted-by-timestamp rows for that server), computing the mean of the last 1, 2, or 3 seen values as specified — awk with associative arrays keyed by server_name, appending to a small ring buffer per server, works well here. Stage 3: pivot from long (timestamp,server,rolling_avg) to wide (timestamp,alpha,beta,gamma) — since there are only three fixed server names, you can build this with awk by collecting into arrays indexed by timestamp and printing sorted by timestamp, or use `join`/`paste` on three separately-filtered per-server files. Round every average to exactly one decimal with half-up rounding (`printf "%.1f"` in awk uses round-half-to-even in some cases — verify against expected values, e.g. 26.7 not 26.6/26.8, and adjust with an explicit +0.05 trick if needed before truncation).
