Open `/app/audit_checker.py` and find the buggy hierarchy query — it's almost certainly joining `entities` to itself on the wrong column (e.g. matching siblings by shared `parent_id` instead of walking parent→child links), which is what causes both wrong entity sets and inflated Mongo sums (extra bogus entity IDs pull in extra transactions). Replace it with a proper `WITH RECURSIVE` CTE: base case `SELECT id, parent_id FROM entities WHERE id = %s`, recursive case joining `entities e JOIN descendants d ON e.parent_id = d.id`, unioned to walk downward from the given entity through all descendants — verify direction matches the schema definition (`parent_id` pointing up) so you're not walking upward by mistake. Collect the full set of resulting entity IDs, then query MongoDB's `transactions` collection with `source_id: {"$in": [...]}` and `status: "COMPLETED"`, aggregating (e.g. via `$group`/`$sum` or in Python) total amount per `source_id`; entities absent from the aggregation should default to `0.0`. Build the final list, sort by `total_amount` descending then `entity_id` ascending, and print only the JSON array with `json.dumps` — remove any leftover debug prints. Test manually against a few different `--entity-id` values, including leaves (no descendants) and entities with zero completed transactions, before trusting the fix.
