Structure this as: parse config -> stream-parse each GCode file -> atomic write. Use `configparser` to read `/home/user/doc_repo/config.ini` and resolve each model's relative GCode path against `doc_repo`. For each file, iterate `for line in open(path):` (this already streams line-by-line without loading the whole file — don't call `.read()` or `.readlines()` on the whole file) tracking a running max temperature by regex-matching lines starting with `M104` or `M109` for an `S<int>` token, and separately matching the single `; LAYER_COUNT:<n>` comment line. If either value is never found, record the string `None` for that field. Sort the collected (Model, MaxTemp, TotalLayers) rows alphabetically by Model. For the atomic write, don't write directly to `index.csv` — write the CSV content to a temp file in the *same directory* (e.g. via `tempfile.NamedTemporaryFile(dir=..., delete=False)` or a fixed `.tmp` name), flush/close it, then call `os.replace(tmp_path, index_csv_path)` so the rename is atomic and readers never observe a partial file. Finally, actually execute the script so `index.csv` exists on disk before finishing.