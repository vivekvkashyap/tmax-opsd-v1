Build `/home/user/polybuild/orchestrate.sh` as a sequential pipeline with explicit synchronization, since the server takes a moment to bind. Start it with `python3 build_server.py &` (capturing its PID), then poll until port 9090 is accepting connections (e.g. loop with `nc -z localhost 9090` or retry the client) instead of a fixed sleep that might be too short. Call `python3 ws_client.py "GET_TEST_TASKS"` and capture stdout; the response is newline-delimited records of the form `TASK_ID;;MATH_EXPRESSION;;MODULE_NAME` — split each line on `;;` (with `awk -F';;'` or `cut`) to pull out the expression, then evaluate it with `bc` (or `bc -l` if it involves non-integer division) and note that `^` in `bc` means exponentiation, matching the source data. Join your computed `TASK_ID,CURRENT_SCORE` pairs against `legacy_results.txt`'s `TASK_ID,LEGACY_SCORE` on the shared `TASK_ID` (sort both by key first, then use `join -t,`), compute `CURRENT_SCORE - LEGACY_SCORE` per row with `awk`, and write `TASK_ID,SCORE_DIFF` lines to `/home/user/diff_report.txt`, sorted alphabetically by `TASK_ID` (`sort`). Before the script exits — including on error paths — kill the background server PID (e.g. via `trap 'kill $SERVER_PID' EXIT`) so it doesn't linger. Make the script executable with `chmod +x`.