Start by exploring the API manually with `curl` before writing any C: hit `GET /api/chunks?page=0`, `page=1`, etc. against the Flask service on 127.0.0.1:5000 to see the raw JSON shape, and note when pagination stops returning new data (empty array or repeated content) so your C client's pagination loop terminates correctly instead of looping forever or stopping too early. In your C program (`/home/user/graph_restore.c`, linked with `-lcurl -ljansson`), fetch and accumulate all pages into a combined node/edge collection first, then do the analysis in a second pass over the full dataset rather than per-page — inference of node "type" from edge direction requires seeing the whole graph. Derive types by grouping node IDs by whether they only appear as edge sources vs. only as edge targets. Drop nodes whose `status` property equals `"deleted"`, then drop any edge whose source or target references a dropped node. Sort remaining nodes by numeric `id` ascending, then write one JSON object per line to `/home/user/restored_graph.jsonl`, nodes first with an `inferred_label`, then edges — match the exact field names shown in the task description since the grader diffs structure, not just content.
