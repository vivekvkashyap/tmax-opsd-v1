Extract frames from `/app/cell_timelapse.mp4` (e.g. `ffmpeg -i cell_timelapse.mp4 frame_%04d.png`), then for each frame programmatically crop the top-left 10x10 pixel block and test whether it is exactly pure green (0,255,0) — use PIL/numpy for an exact equality check rather than eyeballing, since near-green compression artifacts could mislead a manual check. Count how many frames satisfy this exactly; that count is your `T`. Then write `/home/user/simulate_growth.c` implementing classic RK4 with fixed `dt = 0.01`: define `f(y, L) = a*y*(1 - y/K) - b*L*y` with the given constants, compute `steps` as the integer number of `dt` increments needed to reach `t = T` (round rather than truncate, e.g. `(int)(T/dt + 0.5)`, to avoid a floating-point off-by-one that would diverge from any bit-exact reference), run the standard four-stage RK4 update that many times starting from `y0`, and print only the final value with `printf("%.6f\n", y)` — no other stdout output, since this will be checked against a reference implementation for many random `(L, y0)` inputs. Compile with `gcc -O2 -lm`.
