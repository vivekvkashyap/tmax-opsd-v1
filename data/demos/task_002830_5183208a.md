Set up the environment first: `python3 -m venv /home/user/venv && /home/user/venv/bin/pip install numpy scipy`, then do all subsequent computation with that venv's interpreter. Load each of the three CSVs in `/home/user/matrices` as a numeric matrix (e.g. `numpy.loadtxt` with comma delimiter) and compute `numpy.linalg.matrix_rank` for each — this is SVD-based under the hood so no manual row-reduction needed. Identify which file has the strictly highest rank and which has the strictly lowest. For those two matrices only, compute each node's degree as the row-sum of weights (`matrix.sum(axis=1)`). Run `scipy.stats.ttest_ind(highest_rank_degrees, lowest_rank_degrees)` with default (unspecified) parameters — be careful about group order: Group 1 must be the highest-rank matrix's degrees, Group 2 the lowest-rank matrix's degrees, since t-stat sign depends on order. Write the result to `/home/user/result.log` following the exact four-line format given in the task (`Highest:`, `Lowest:`, `T-stat:` rounded to 2 decimals, `P-value:` rounded to 4 decimals), using the actual filenames of the matrices you identified as highest/lowest, not placeholders.