Separate the three sub-problems and solve them in the right order. First OCR the notes: run `tesseract /app/dev_notes.png stdout` and read off the exact scaling formula and target encoding it specifies — don't guess at coefficients, use what OCR recovers. Second, bisect the encoding regression: `git bisect start`, mark HEAD `bad` and the earliest commit `good`, then `git bisect run` a check that runs `process.sh` on a simple ASCII input and fails (non-zero / empty output / a decode error) when the bug is present — this narrows to the exact commit that introduced an `iconv` (or similar) call converting intermediate config to the wrong encoding; record that commit's identification, then check out or cherry-pick the encoding fix (converting to the OCR'd target encoding, e.g. UTF-8, or removing the bad conversion) without reverting the also-needed formula update at HEAD. Third, apply the OCR'd formula into `process.sh`/its `awk` snippet (matching operator precedence and constants exactly), then run `./process.sh /home/user/input_data.dat` to produce `/home/user/final_output.png`, and write `/home/user/test_regression.sh` that reruns the pipeline on a given input and exits 0/1 based on successful, correctly-encoded output. Validate visually/structurally before finishing, since success is judged by SSIM against a hidden reference.
