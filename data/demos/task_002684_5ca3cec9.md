Break this into independent, verifiable stages before combining. Run `tesseract` on the PNG (try `--psm 6` or similar table-friendly mode if default output is garbled) and inspect the raw text output yourself to see how rows/columns actually got split — expect OCR noise, so don't assume clean CSV-like structure; parse defensively with regex or manual line-splitting tuned to what you actually observe. For city matching, load `/app/aliases.json`, and for each OCR'd city string compute edit distance (e.g. `python-Levenshtein` or a hand-rolled DP) against every alias of every standard name, picking the standard name whose closest alias has the minimum distance — this is what makes it robust to OCR typos and script differences, not exact string equality. Reshape the wide CSV with `pandas.melt`, stripping the `Temp_` prefix to get bare city names matching your OCR-normalized names. Concatenate both sources, `groupby(['Date','City']).mean()` to collapse duplicates, then per-city `set_index('Date').resample('D')` over 2023-01-01 to 2023-01-10, and use `.interpolate(method='linear')` for gaps — check edge cases (missing first/last day) since linear interpolation can't extrapolate beyond the observed range. Write the parquet sorted by Date then City with the exact dtypes requested.
