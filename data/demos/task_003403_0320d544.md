Structure the SQL as a two-stage query: (1) a CTE that deduplicates `edges` by `(src, dst)` keeping only the row with the max `updated_at` — use `ROW_NUMBER() OVER (PARTITION BY src, dst ORDER BY updated_at DESC)` and filter to `rn = 1`, or `MAX(updated_at)` with a join; (2) a `WITH RECURSIVE` traversal over that deduplicated edge set that expands `path` (a string built with `||`) and running `cost`, starting from `'CORE_1'`. To prevent cycles, track visited nodes (e.g. append `,` + node to a comma-delimited path column and check `instr(path, ',' || dst || ',') = 0` before recursing) rather than relying on a depth limit alone. Once all paths from CORE_1 reaching EDGE_7 are enumerated, pick the one with `MIN(total_cost)` via `ORDER BY cost LIMIT 1`. In your C program, run this as one `sqlite3_exec`/prepared-statement query, format the result as `<path>,<cost>` matching the example separator style (`->`), and write it to `/home/user/optimal_route.txt`. The key pitfall is deduplicating edges AFTER the recursive traversal instead of before — always filter stale rows first so the recursion never even considers them.