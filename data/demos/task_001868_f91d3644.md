Edit `pipeline.go` in place rather than rewriting from scratch. For the imputation bug: change the `category_id` parsing so that when the field is an empty string it sets `catID = 0` directly instead of calling `strconv.Atoi` on empty text and using `continue` on error — only skip/error for genuinely non-numeric non-empty values, not blanks. For `ComputeEmbedding`, iterate the text's runes/bytes, tally vowels (a,e,i,o,u case-insensitive) and consonants (alphabetic and not a vowel) separately, and return `[]float64{float64(len(text)), float64(vowelCount), float64(consonantCount)}` — use `len(text)` for "total character length" as specified. For `Evaluate`, sum `Embedding[0]` across all records and divide by `len(records)` (guard divide-by-zero). After fixing, run `go build pipeline.go && ./pipeline` from `/home/user/workspace` and inspect the generated `metrics.json` to confirm `total_rows_processed` reflects all 5 CSV rows (none silently dropped) and the JSON keys/types match the required schema exactly.