Work in stages and verify each before moving on. First use `find /home/user/data -maxdepth 1 -name '*.arc' -size +10k -mtime -7` to get the matching filenames — check the exact `find` semantics for size (10 KB threshold) and mtime (within 7 days) rather than guessing, and confirm your result against `ls -la` output for each candidate file. Second, write a small, careful C program: open the file with `fopen(path, "rb")`, `fread` exactly 8 bytes into an `unsigned char` buffer, then loop printing `"%02X"` for each byte separated by spaces (watch for the trailing space vs. single-space-separated format, and the final newline) — compile with plain `gcc`. Third, for the manifest, run your compiled program on each matched file, capture its stdout, then construct the destination path by taking the basename of the original path and prefixing it with `/vault/archive/` (do not just string-replace blindly — verify the transform is correct), and sort manifest lines alphabetically by filename before writing. Finally, build the tar archive with `tar -cf` using `-C /home/user/data <matched filenames>` so paths are stored relative, not `tar -cf ... /home/user/data/*`, which embeds absolute/full paths.
