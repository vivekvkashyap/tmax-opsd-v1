Write `cleaner.c` as a true two-pass program: Pass 1 opens `signal.csv`, reads line by line, skips lines that are empty or literally `NaN` (after trimming whitespace/newline), parses the rest with `strtod`/`atof`, and accumulates count, sum, and sum-of-squares to compute mean and *population* std dev (`sqrt(sum_sq/n - mean*mean)` or the sum-of-squared-deviations form — pick whichever avoids catastrophic cancellation, since one row here is a large outlier). Rewind or reopen the file for Pass 2: for each line, if missing (empty/NaN) output the mean; else if the value is above `mean + 2*std` or below `mean - 2*std`, clamp to that exact bound; otherwise pass the value through unchanged — always print with `printf("%.4f\n", ...)`. Link with `-lm`. `pipeline.sh` must be idempotent: check for `gcc` (`command -v gcc || apt-get install -y gcc`), compile `gcc -o /home/user/cleaner /home/user/cleaner.c -lm`, then run `/home/user/cleaner`, and `chmod +x` itself. Verify your own output against a hand or Python-computed mean/std before trusting it, since std-dev formula choice affects the 4th decimal.
