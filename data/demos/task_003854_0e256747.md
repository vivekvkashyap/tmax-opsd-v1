Work in two clearly separated phases. First, run `tesseract /app/priors.png stdout` (try `--psm 6` if lines get merged) to extract the text listing P(C=0), P(C=1), and the four conditionals P(F1|C=0), P(F2|C=0), P(F1|C=1), P(F2|C=1); read the OCR output carefully and sanity-check that each pair of probabilities sums to 1.0 before trusting the digits — OCR often confuses characters like 0/O or misplaces decimal points. Hardcode the confirmed numeric constants into your C++ source (or pass them in via a small parsed config), since the task requires the ETL and math to happen in a compiled C++ program, not in the OCR step. Second, write the C++ program: parse `/home/user/raw_data.csv` skipping the header, use a `map<int, pair<int,int>>` (or similar) keyed by user_id to tally F1/F2 counts, then for each user compute Score_0 and Score_1 with `std::log`, convert to a probability via `P(C=1)=exp(Score_1)/(exp(Score_0)+exp(Score_1))` — subtract the max of the two scores from both before exponentiating to avoid overflow. Sort by user_id ascending, and write `/home/user/results.csv` with header `user_id,prob_1` and `prob_1` formatted to exactly 6 decimal places (e.g. `std::fixed << std::setprecision(6)`). Compile and run the binary yourself to confirm output before finishing.
