Fix the Makefile first: a shared object built from C needs `-shared` and `-fPIC` compile/link flags plus `-lm` for the math functions used by `core.c` (the current rule is missing these, which is why linking/loading fails) — verify with `make` then `file libcore.so` / `ldd` to confirm it's a proper PIC shared library. To recover the calibration integers, transcribe `/app/calibration.wav` with a speech-to-text tool (e.g. `whisper /app/calibration.wav` or another available ASR tool) and read off the three spoken numbers in the order they occur — don't guess; actually run the transcription and inspect its output text to get A, B, C. Build the token string as `A_B_C` using those recovered digits. For `/app/run.sh`: run `make` first, then launch a Python `websockets` server on `127.0.0.1:8765` that loads `libcore.so` via `ctypes.CDLL`, declares `process_value`'s argtypes/restype as `c_double`, and on each incoming message parses the JSON, checks `token` matches the recovered value exactly (closing the connection or sending an error otherwise), calls the C function with A, B, C, and the given `x`, and replies with the JSON result. Make the script executable and leave the server running in the background.