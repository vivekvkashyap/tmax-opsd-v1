Parse both FASTA files first with a minimal parser (or Biopython if available) into id->sequence dicts. For matching, write a reverse-complement helper (complement A<->T, C<->G, then reverse the string) and for each primer count occurrences of both the forward sequence and its reverse complement as substrings within the reference sequence (Python's `str.count` won't catch overlapping matches — if you need overlap-safety use a manual sliding-window scan instead); keep only primers where forward_count + revcomp_count == 1 exactly, discarding 0 or >1 matches. For the curve-fitting stage, group `melting_data.csv` rows by `primer_id`, and for each valid primer fit `F(T) = L/(1+exp(-k*(T-Tm))) + b` via `scipy.optimize.curve_fit`, supplying a reasonable initial guess (e.g. L≈max(fluorescence)-min, k≈0.5, Tm≈temperature at half-max, b≈min(fluorescence)) since a bad initial guess is the most common cause of fit divergence for a sigmoid. Parallelize the per-primer fits with `multiprocessing.Pool.map` over a worker function that takes a primer's (temperature, fluorescence) arrays and returns (primer_id, Tm). Round Tm to 2 decimals, sort rows alphabetically by primer_id, and write `primer_id,Tm` to `/home/user/valid_primers_tm.csv`.
