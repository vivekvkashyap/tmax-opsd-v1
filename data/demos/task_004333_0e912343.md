Work in three clearly separated passes so bugs don't compound. First, set up the venv (`python3 -m venv /home/user/venv`, activate it, `pip install setuptools`) and confirm you can build the extension in place with `pip install .` or `python setup.py build_ext --inplace` from `/home/user/project/fib_ext/` before touching the C code — this establishes your build/test loop. Second, open `fib_ext.c` and look specifically for (a) a loop bound that writes one element past the allocated array (an off-by-one on `<=` vs `<`, or writing at index `n` in an array sized `n`) and (b) a `malloc`'d buffer whose `free()` is missing on the success path in the Python wrapper before it returns the list — fix both, then reinstall into the venv and re-test with a large N to confirm no crash/leak (Valgrind or a large-input stress run helps confirm). Third, write `migrate.py`: use `sqlite3`, run `ALTER TABLE computations ADD COLUMN fib_sum INTEGER`, iterate rows with a cursor, call the now-fixed `fib_ext.get_fibs(n_value)`, sum it, `UPDATE` the row, commit, then `SELECT SUM(fib_sum) FROM computations` and write that single integer to `/home/user/project/final_sum.txt` — always let the script compute and write this value itself rather than hardcoding it.
