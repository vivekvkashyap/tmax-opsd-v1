Write the C program to parse the CSV with a simple line-by-line reader (fgets + manual field splitting on commas, or sscanf per field), since CSV fields here are unquoted and comma-delimited — do not pull in a CSV library. Read and discard the header line first, then loop over data rows keeping a running "last valid changes_count" state variable initialized to 0. For each row, split fields carefully so that a missing changes_count (trailing comma with nothing after, or nothing before a newline/EOF) is detected as an empty string rather than causing an off-by-one field misalignment — this is the most common bug. When the field is empty, substitute the running last-valid value; when it's present, parse it with atoi/strtol, use it, and update the running value. For email masking, find the first '@' in the user_email field and replace everything before it with literally "***", keeping the '@' and domain intact. Accumulate the sum of the (post-imputation) changes_count values as you go. Print "Processing row N..." to stdout for each data row using a 1-based counter, in the same loop. Write the header plus transformed rows to /home/user/clean_logs.csv, and write "Total Changes: <sum>" as the single line of /home/user/summary.log. Compile with gcc and run it, then inspect both output files to confirm formatting matches exactly (no extra whitespace, correct newline at end).
