Build and test each piece independently before wiring them together. For the C program, use `time()`/`localtime()` (or `strftime`) to format exactly `YYYY-MM-DD HH:MM:SS %Z` with a trailing newline — note the verifier runs it with `TZ=UTC` set in the environment, so rely on the C library's normal timezone handling (`tzset()`/`localtime()` respect `TZ`) rather than hardcoding a timezone. For logger_script.sh: export `TZ=Asia/Tokyo` and `LC_ALL=C` at the top so the compiled binary picks up Tokyo time when it runs; before appending, check the *current* line count of /home/user/server.log with `wc -l` — if it exists and has >=3 lines, `mv` it to server.log.old (this naturally overwrites any prior .old) *before* appending the new line, so rotation happens right when the 4th write would occur. Test by running the script 4 times in a row and inspecting server.log / server.log.old line-by-line each time, not just assuming it works. For cron_schedule, write a single line using either `*/15` or `0,15,30,45` in the minute field with the absolute path to logger_script.sh, matching the required regex exactly.
