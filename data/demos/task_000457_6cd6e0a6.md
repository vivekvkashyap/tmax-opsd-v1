Build this as an awk-centric multi-stage pipeline rather than trying to do it all in one pass with sed. Use awk's built-in quote-aware CSV parsing (`FPAT` or a manual quote-counting field splitter) so you don't naively split on commas inside quoted fields. To detect and drop multi-line logical records: read the raw file and, for each physical line, count the double-quote characters; accumulate lines into a buffer while the running quote count for the current logical record is odd (meaning a quoted field is still open), and only emit/process a record once the accumulated quote count is even — if a record was assembled from more than one physical line, discard it entirely rather than merging it. For kept records, replace the email field with `[REDACTED]`, and maintain a running total in an awk variable that you add each row's `rating` to, printing it as the `cum_rating` column. Emit everything tab-separated with the fixed header first. Test against the given example inline before running on the real file, and diff your output against expectations to catch subtle field-count or quote-parity bugs.
