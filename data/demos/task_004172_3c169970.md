Work through each broken piece independently and verify compilation/behavior after each fix rather than editing everything blind. (1) Go backend: run `go build`/`go run` on `/app/backend/main.go`, read the exact compiler error, and fix it — a concurrency bug touching a shared map from goroutines typically needs a `sync.Mutex` (or channel) guarding reads/writes, not just silencing a compile warning; confirm it serves on `127.0.0.1:8080` after the fix. (2) C proxy: run the compiler on each of `main.c`, `filter.c`, `filter.h`, `proxy.c` individually to surface syntax errors and missing `#include`s, then fix line by line. (3) Audio: the required header value is a single English word spoken in `/app/auth_token.wav` — transcribe it yourself (e.g. via a local speech-to-text tool or careful listening/analysis), don't guess; wire the recovered word into an `X-Proxy-Auth: <word>` header the proxy adds to every forwarded request. (4) WAF filter: implement `validate_payload` using pattern checks (SQLi markers like quotes/`OR 1=1`/`UNION SELECT`, command-injection metacharacters, `<script>`/XSS patterns) and iteratively test against every file in `/app/corpus/clean/` and `/app/corpus/evil/` until 100% classify correctly — false positives on clean data matter as much as missed evil payloads. (5) Write `/app/build_and_run.sh` to `gcc`-compile the proxy to `c-proxy`, launch the Go backend in the background, then start the proxy on `127.0.0.1:8000`, and actually run it end-to-end to confirm the header appears at the backend.