Start by transcribing `/app/voicemail.wav` with a local speech-to-text library (e.g. `openai-whisper`, run on CPU is fine for a short clip) and read the transcript carefully — it specifies the exact `tx_type` filter, the amount threshold, and which centrality metric to compute; do not guess these. Stand up MongoDB locally, write a loader that reads `transactions.csv` with pandas/csv and bulk-inserts into `fin_network.transactions`, then create indexes on `tx_type`, `amount`, and `timestamp` (a compound index matching your query pattern will be fastest). Build an aggregation pipeline that `$match`es on the transcribed criteria, then `$group`s by `(sender_id, receiver_id)` summing `amount` to get pairwise totals — these become directed, weighted edges. Load them into a `networkx.DiGraph()` with `weight` set from the aggregated sum, then call the appropriate centrality function (pay attention to whether the voicemail's metric is weighted and what damping/alpha parameter it names) using the exact parameter value spoken. Write `{node_id: value, ...}` to `/home/user/final_metrics.json`, and wrap the whole flow (Mongo ingest → aggregation → graph build → metric → JSON) in `/home/user/process_network.py` so it reruns end-to-end. A common failure mode is filtering on the wrong amount comparison (>= vs >) or using an unweighted graph — re-check the transcript wording for each detail.