Build each artifact to the letter of its spec, then self-test before trusting it. For `compute_sig.sh`, dump the file's raw bytes as unsigned decimal values (e.g. `od -v -t u1` or `xxd`), sum them with `awk`, and print only `sum % 256` with no extra whitespace or trailing text — verify by hand-computing the signature of a tiny known string (like "hello") and diffing against your script's output before moving on. For the `.proto` file, transcribe the schema exactly as specified: `syntax = "proto3";`, `package fileorg;`, the `FileOrganizer` service with `rpc ComputeSignature(FileRequest) returns (SignatureResponse)`, and the two messages with the exact field names/types/tag numbers given — small deviations (missing semicolon, wrong tag) will fail a grep/protoc check. For `prop_test.sh`, generate random-length random-byte files via `/dev/urandom` and `dd`, copy each exactly, run `compute_sig.sh` on original and copy, and compare outputs across 5 iterations, printing PASS/exiting 0 only if all match, FAIL/exit 1 otherwise. Remember `chmod +x` on both shell scripts.
