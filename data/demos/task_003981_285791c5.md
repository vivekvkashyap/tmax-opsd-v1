Work phase by phase and verify each before moving on. Phase 1: write `apply_fixes.py` using `configparser` to read `config.ini`, set `[Emitter] processor_port = 8002`, and write back — idempotency means checking the current value first (or simply always writing the same target value with `config.write()`, which is naturally idempotent as long as you don't append/duplicate sections); run it once and diff a second run's output to confirm no drift. Phase 2: since you cannot touch config.ini or the service code, bridge 9003->8003 with `socat TCP-LISTEN:9003,fork,reuseaddr TCP:127.0.0.1:8003 &` (or nohup it) so it survives after your shell command returns — check it's actually listening with `ss -ltnp` before moving on. Phase 3: start each service with `nohup python3 x.py >/dev/null 2>&1 &`, wait for logs to appear, then pull the first UUID with something like `grep -m1 'Transaction ID' logs/aggregator.log | grep -oE '[0-9a-f-]{36}'` > first_transaction.txt — anchor the extraction so it only grabs the UUID substring, not the whole line. Phase 4: monitor.py should loop with `requests.get(..., timeout=...)` inside try/except, sleep 1s between polls, and only write the exact JSON literal once total_received >= 5.
