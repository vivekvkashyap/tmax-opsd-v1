Treat this as three independent deliverables. Phase 1: use `find /home/user/backup_data -type f -perm -o+r` (world-readable bit set) to enumerate offenders, then `basename` each, sort alphabetically, and write to `world_readable.txt` — don't hand-pick files, let `find` do the permission check so it generalizes to hidden test fixtures. Phase 2: for `/home/user/ssh_keys`, compute each file's octal mode with `stat -c "%a"` and flag any where group or other bits are nonzero (i.e. mode is not exactly 600), `chmod 600` those specific files, and log their basenames sorted alphabetically to `fixed_keys.txt` — don't touch files already at 600. Phase 3 in Go: read the log, use a regex to replace everything after `Password: ` on a line with `[REDACTED]`, and a separate regex/scanner (with multiline/DOTALL handling) to collapse each `-----BEGIN OPENSSH PRIVATE KEY-----`...`-----END OPENSSH PRIVATE KEY-----` block into the single literal string — process line-by-line or with careful state tracking, not a naive single-line regex, so multi-line keys are fully consumed. Then load the 32-byte key, generate a fresh random 12-byte nonce per run via `crypto/rand`, AES-GCM-seal the redacted bytes, and write `nonce || ciphertext` to `secure_audit.enc`.
