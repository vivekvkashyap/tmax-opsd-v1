Build this as a pipeline of shell stages, verifying each stage's output before wiring in the next, and stay within bash/awk/sed/coreutils per the constraint. Normalization: use `tr '[:upper:]' '[:lower:]'` plus `sed`/`tr -cd` to strip everything except `[a-z0-9 ]` from both SourceText and ProposedText columns (careful with tab-field extraction via `awk -F'\t'`). Treat `/app/tqs` purely as a black-box oracle — call it as `/app/tqs "norm_source" "norm_proposed"` per row and capture its stdout score; don't try to reverse-engineer or reimplement its similarity math. Filter rows with score `>= 0.50` using `awk` numeric comparison, then stratify: group by LangCode, sort each group by score descending with ID ascending as tiebreak (`sort -t$'\t' -k<score>,<score>nr -k<id>,<id>n`), and take the top 2 per group (`awk` counter reset per group or `head -2` after grouped sort). Write the survivors to `accepted_translations.tsv`. For the TCP server, write `serve.sh` using a `socat TCP-LISTEN:9090,reuseaddr,fork EXEC:...` or a `nc -l` loop that reads one line, parses `FETCH <ID>`, looks up the *original* (non-normalized) ProposedText from your accepted set, and replies with it or `ERROR`; background it with `&`/`disown` or `nohup ... &` so it survives after your commands finish, and test it yourself with `printf 'FETCH <id>\n' | nc 127.0.0.1 9090` before finishing.