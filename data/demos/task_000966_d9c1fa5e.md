Transcribe `/app/network.wav` with a speech-to-text tool (e.g. `whisper`) rather than guessing its content, then parse the resulting spoken numbers into consecutive pairs (source, destination) in the order they were dictated, and write them as `/home/user/network.csv` with header `Source,Destination` — watch for transcription quirks like numbers being written as words ("ten twenty") vs digits, and normalize before pairing. For `/home/user/query.cpp`: take the CSV path as `argv[1]`, parse it (skip the header row), and build an undirected adjacency list (add edges both directions even though the source dictation implied direction) using a hash map from node id to neighbor list. Then read stdin line by line, split each into two integers, and for each pair run BFS from source tracking visited/distance; if source==destination print 0, if BFS exhausts without reaching destination print -1, otherwise print the found distance. Since correctness is checked by fuzzing hundreds of random pairs against a hidden reference including nonexistent node IDs, make sure your BFS gracefully handles node IDs absent from the map (treat as no path, i.e. -1) rather than crashing or throwing on missing map keys.
