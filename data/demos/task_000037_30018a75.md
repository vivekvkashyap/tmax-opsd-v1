Structure `/home/user/pack_docs.py` around the exact order of operations: first read each `.md` file in `/home/user/docs_raw/`, perform the two global text substitutions (`AcmeCorp` -> `ZenithInc`, `v1.0` -> `v2.0`) using `str.replace`, and keep the transformed content in memory per filename. Compute the SHA256 hex digest of each *transformed* file's content (not the original) with `hashlib.sha256(content.encode()).hexdigest()`, and build the manifest dict from those. Assemble the top-level JSON object with exactly the two keys `manifest` and `files`, serialize with `json.dumps`, encode to bytes, and compress with `zlib.compress`. For the atomic write requirement, write the compressed bytes to a temp file in the same directory as the destination (so the rename stays on one filesystem, e.g. via `tempfile.mkstemp(dir='/home/user')` or a fixed `.tmp` path) and then call `os.rename` to `/home/user/release.docpack` — never write directly to the final path first. Make sure the temp file is closed before renaming and is removed if any step fails (wrap in try/finally or use `tempfile` with `delete=False` and explicit cleanup) so no stray temp files remain. After running, sanity-check by decompressing the archive yourself and confirming the transformed text no longer contains the old strings and the hashes match freshly recomputed ones.