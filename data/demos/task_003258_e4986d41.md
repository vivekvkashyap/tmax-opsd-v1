Read `/home/user/legacy_sign.py` first to pin down the exact wire protocol: what fields go in the JSON payload, how the checksum is computed, and which field is read from the response. Start the mock server (`pip install --user websockets`, then `python3 /home/user/ws_server.py &`) before testing anything. Download and `chmod +x` the `websocat` binary as instructed, then verify it works standalone (`echo '...' | ./websocat ws://127.0.0.1:8765`) before wiring it into a script. In your Bash rewrite, compute the checksum with `printf '%s' "$1" | sha256sum | cut -d' ' -f1` — using `echo` instead of `printf` will add a trailing newline and produce the wrong hash, a common failure point. Build the JSON payload with correct quoting (or use `jq -n` to avoid manual escaping bugs), pipe it through `websocat -n1` (one-shot mode) to the server, and parse the `signature` field out of the JSON reply with `jq -r .signature` rather than fragile grep/sed. Finally run your script with the given artifact name and write only the extracted signature (no extra text/newline issues) to `/home/user/signature.txt`.
