Start by fixing the vendored package: `pip install -e /app/chardet` will fail with a `SyntaxError` — open `/app/chardet/setup.py` and look for the `setup()` call; a missing comma between two adjacent keyword arguments (e.g. between `author=...` and `author_email=...`) is a classic minimal-diff break, so scan argument-by-argument rather than assuming a bigger issue. After the syntax fix, reinstall in editable mode and confirm `import chardet` / `chardetect` works from the shell. Extract `/app/sensor_logs.tar.gz`, then process each file: detect its encoding with chardet, decode to UTF-8 (don't assume any single encoding — files are a mix of utf-8/iso-8859-1/windows-1252/macroman), and strip everything from the literal line `[DIAGNOSTIC_START]` through `[DIAGNOSTIC_END]` inclusive before parsing key-value lines — do this stripping on the decoded text, not byte-for-byte, to avoid corrupting multi-byte sequences. Parse `StationID`/`Temperature`/`Humidity` lines per file, aggregate mean Temperature and Humidity per StationID across all files, and write `/home/user/station_averages.csv` with header `StationID,AvgTemp,AvgHumid`, numeric-sorted by StationID, values rounded to 4 decimals. Spot-check a few files manually (view raw bytes vs decoded text) to confirm your encoding detection and diagnostic-block stripping are both correct before computing final averages, since errors here silently skew the MAE.