Transcribe `/app/dictation.wav` first (e.g. with a speech-to-text tool available in the environment, or by listening/processing it programmatically) to recover the exact prime modulus and the exact output format string it specifies — use those recovered values verbatim rather than assuming typical defaults, since the format string in particular must be reproduced exactly including punctuation and label names. For the math: fit a quadratic through three points over a prime field using Lagrange interpolation modulo p — compute each Lagrange basis polynomial's coefficients via modular inverses (extended Euclidean algorithm, since p is prime so Fermat's little theorem or extended GCD both work), sum the weighted contributions into C0/C1/C2 mod p, and take care that all intermediate subtraction results are brought back into `[0, p)` before further mod arithmetic (negative mod behavior in C++ is a classic source of subtle bugs here). Use 64-bit integers throughout to avoid overflow during multiplication. Compile with `g++ -O3` to the exact required path, and test against a variety of hand-computed small cases (including x-values that wrap near p) before trusting it against the fuzz harness, since equivalence testing will be exact and unforgiving of off-by-one modulus errors.