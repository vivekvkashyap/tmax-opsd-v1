Do not assume filename order corresponds to version order — semantic version strings sort lexicographically wrong (e.g. "2.0.1-rc.1" is actually *lower* precedence than "2.0.1" per semver, and "1.12.0" > "1.5.9" numerically despite string comparison saying otherwise). List the .proto files in /home/user/protos, parse out each `<version>` from the filename pattern, and compare them using real semver rules (major.minor.patch, with pre-release tags like "-rc.1" ranking below the same release version) — write a small script or use a semver-aware comparison rather than `sort` on raw strings. Once you've identified the true highest version, set up the Go module (`go mod init pipeline`) in /home/user/pipeline, install protoc-gen-go and protoc-gen-go-grpc via `go install`, add `$(go env GOPATH)/bin` to PATH, then run `protoc` with `--go_out=. --go-grpc_out=.` pointing only at that single winning .proto file (not all of them) so only its generated files land under pipeline/api. Write main.go embedding `api.UnimplementedDataProcessorServer` and printing the exact success string with the winning version substituted, then `go run` it to produce success.log. Confirm no generated files for the other versions exist.
