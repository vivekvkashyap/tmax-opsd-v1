Decompose into two independent feature extractions plus a persistent server, and script all of it from one Bash entry point. For atom_count, use exact-match line counting on the literal token "ATOM" at line start (e.g. `grep -c '^ATOM'` or an `awk` pattern anchored to the start of line) so you don't accidentally also count "HETATM" lines (which contain "ATOM" as a substring but shouldn't match a `^ATOM` anchor) — verify your count against a manual scan of a few lines. For peak_frequency_hz, run an FFT over the WAV samples (e.g. via a short embedded `python3 -c` using numpy/scipy since Bash alone can't do FFT: read the wav, take `np.fft.rfft`, find the frequency bin with max magnitude via `np.fft.rfftfreq`, round to nearest integer) — watch for DC-offset/zero-bin dominance and consider excluding bin 0. Wire both computed values into a JSON string built exactly as `{"peak_frequency_hz": <freq>, "atom_count": <count>}`. For the server, use a persistent loop (e.g. `while true; do ... | nc -l -p 8080 -q1; done &`) that parses the request line for `GET /features` and replies with a proper `HTTP/1.1 200 OK` status line, a `Content-Type: application/json` header, blank line, then the JSON body — test repeated `curl` calls to confirm it survives multiple sequential requests.