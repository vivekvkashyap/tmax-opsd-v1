Implement this as a classic BFS/DFS graph traversal in Bash, not a naive single grep pass. Approach: (1) load the TSV (skip header) into memory-friendly form usable by shell tools. (2) Maintain a "frontier" set starting with the given dataset ID and a "visited/output" set that's initially empty; loop: for each ID currently in the frontier, `awk -F'\t' -v id="$cur" '$1==id{print $2}'` (or equivalent grep) over the dependency file to find its direct downstream children, add any children not already in the visited set to both visited and the next frontier, and repeat until a full pass adds nothing new — this fixed-point loop is what makes it recursive/transitive rather than just one hop. (3) Never add the starting ID itself unless it's actually reached again downstream. (4) At the end, sort the visited set uniquely and alphabetically (`sort -u`) and write to stdout. Watch for cycles (even though inputs are DAGs here, your visited-set check should still guard against infinite loops) and be careful your matching is exact-field (`$1==id`), not substring, to avoid `DS_1` also matching `DS_10`. Make the script executable, run it with `DS_005` and the TSV path, and redirect output to `/home/user/impacted_DS_005.txt`.
