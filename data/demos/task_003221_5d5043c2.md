Write the C program so every operation is idempotent by construction, not by checking "if not exists" ad hoc. Use `mkdir` (or `mkdir -p` logic in C: create parent then children) and ignore `EEXIST` errors, then explicitly `chmod(path, 0750)` on each directory regardless of whether it was just created — that guarantees the final permission bits are correct even on repeated runs. For the symlink, use `symlink()` and if it fails with `EEXIST`, `readlink()` the existing link; if it already points to the correct target leave it, otherwise `unlink()` and recreate it — never let the program crash just because the link pre-exists. For `mailer.conf`, open with `O_CREAT|O_TRUNC|O_WRONLY`, write the two exact lines (each newline-terminated), close, then `chmod(path, 0640)` explicitly. After compiling with `gcc`, run the binary twice in a row and confirm both invocations exit 0 and the directory/symlink/file state is unchanged the second time — that's the real test of idempotency, not just a single successful run.