Attack this in three independent, individually-verifiable pieces. For `/home/user/validate.sh`, don't just count commas naively per-line with a simple loop — files with embedded unescaped newlines inside quoted fields will break line-based counting, so use a real CSV-aware parser (e.g. Python's `csv` module, or `csvkit`) to check every row has exactly 4 fields (`timestamp,sensor_id,value,status`); exit 0 only if every row parses cleanly with that column count, else exit 1. Validate your script against every file in `/app/corpus/clean/` and `/app/corpus/evil/` and fix misclassifications before moving on. For the bucketing rule, don't guess the sampling policy — recover the literal instruction from `/app/audio_rule.wav`'s metadata (e.g. `exiftool /app/audio_rule.wav` or `strings /app/audio_rule.wav | grep -i group`) or by transcribing the audio, then implement `/home/user/process.sh` to group input rows by the hour component of `timestamp` and emit only the first N rows per hour group as the dictation specifies, preserving original row order within each hour. For `/home/user/setup_cron.sh`, write it to install a crontab line of the form `0 */4 * * * /home/user/pipeline.sh` for user `user` via `crontab -l | { cat; echo "..."; } | crontab -` (or `crontab -u user`). Make all three scripts executable.
