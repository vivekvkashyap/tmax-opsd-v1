Work this as a staged exploit chain, verifying each stage before moving to the next. First read `backend.c` to find the SQL query construction (likely built with `sprintf`/string concatenation rather than parameterized queries) in the login/lookup path — craft an injection payload (e.g. a `UNION SELECT` or boolean-based OR clause) passed as the CLI argument that makes the query return the `admin` user's `enc_token` from `users.db` instead of (or in addition to) the intended lookup; test directly against `sqlite3 /home/user/data/users.db` to confirm your payload's SQL logic before trying it through the compiled binary. Second, locate the 16-bit cipher function in `backend.c` and use the given known plaintext/ciphertext pair for `testuser` to solve for the key algebraically (e.g. if it's XOR-based, key = plaintext ^ ciphertext; if it's arithmetic, work out add/sub or bit-rotation accordingly) — write a tiny script to brute-force or directly compute the 16-bit key space if the operation isn't obviously invertible by hand. Apply that key to decrypt admin's token, then read `admin_panel` in `backend.c` to find where it shells out (likely via `system()`/`popen()` with unsanitized input) and craft a shell-injection payload (e.g. using `;`, `` ` ``, or `$()`) that appends a command to read `/home/user/secure/flag.txt`. Compile with the exact `gcc` command given, test each payload directly against the compiled `/home/user/backend`, and record every payload/value verbatim in `/home/user/audit_report.txt` in the required format.