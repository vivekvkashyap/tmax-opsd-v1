Treat this as three independent fixes that must all land before the server will actually start correctly. First, run `sha256sum -c /app/checksums.txt` (or manually hash each file in `/app/vendored/mongoose` and compare) to find which vendored file's checksum mismatches — inspect that file's diff against what a correct OpenSSL-enabled build would need, since the description says the tampering disabled OpenSSL support; look in the Makefile/build config for missing `-D MG_ENABLE_OPENSSL=1` and `-lssl -lcrypto` flags and restore them, rather than reverting unrelated files. Second, generate the cert/key with `openssl req -x509 -newkey rsa:2048 -nodes -keyout /app/policy-daemon/certs/server.key -out /app/policy-daemon/certs/server.crt -days 365 -subj "/CN=localhost"` (create the certs dir first). Third, read `server.c`'s `drop_privileges_and_init()` call site in `main()` — the bug is that a negative return value is checked but nothing happens on failure; add an explicit `exit(1)` in that branch so a failed privilege drop actually aborts the process instead of silently continuing. Recompile with the fixed Makefile, confirm the binary links against OpenSSL (`ldd` should show libssl/libcrypto), start it in the background, and manually curl the HTTPS endpoint with the correct Bearer token before writing the completion log.