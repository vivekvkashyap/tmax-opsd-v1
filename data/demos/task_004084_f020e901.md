Four independent sub-problems — verify each before combining. (1) Bisect: build/run `v1.0` to confirm the known-good baseline, then `git bisect start`/`bad`/`good` between HEAD and v1.0, driving it with a script that builds each candidate commit and checks for the leak/anomaly (watch RSS growth, compare output stats); write the resulting SHA to /home/user/bad_commit.txt. (2) `strings /app/coredump.dat | grep -E 'CFG-[A-Z0-9]{8}'` to pull the config string from the noise, save the match to /home/user/config_string.txt. (3) Extract/convert /app/telemetry.wav (e.g. via ffmpeg) and use available speech-to-text tooling to transcribe the spoken 4-digit codes in order into /home/user/audio_codes.txt, one per line — double-check each digit, a transcription slip is silent. (4) Diff v1.0 vs the bad commit's source to see exactly how anomaly handling regressed, then reimplement the *good* v1.0 logic as a standalone Bash script at /home/user/parse_telemetry.sh (stdin→stdout) — test it against a built v1.0 binary on varied inputs (including null bytes, special symbols) since it's fuzz-tested for bit-exact equivalence against thousands of random inputs.