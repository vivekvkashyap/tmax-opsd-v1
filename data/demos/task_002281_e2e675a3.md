Treat this as three independent subsystems and verify each before moving to the next. For permissions, write `fix_perms.sh` to `chown www-data` everything under `/app/secure_data/`, then separately `chmod 0400` files matching `*.key` and `chmod 0444` files matching `*.crt` (use `find ... -name '*.key' -exec chmod 0400 {} +` style so it generalizes beyond the two named files), then run it and verify with `stat -c "%U %a"`. For service composition, edit the `nginx.conf` `location /api/` block to `proxy_pass http://127.0.0.1:9000;`, set `REDIS_HOST=127.0.0.1` and `REDIS_PORT=6379` in `backend.env`, start services via the provided script, and confirm end-to-end with `curl http://127.0.0.1:8080/api/health` before moving on — a broken proxy or bad Redis config will silently fail this curl. For the C filter, read stdin line by line; use `strstr` to reject lines containing known SQLi (`' OR '1'='1`, `UNION SELECT`) or XSS (`<script>`, `onerror=`) markers; track brute-force by keeping a running count of consecutive lines from the same IP hitting `/login` (reset the counter whenever the IP or endpoint changes), rejecting once that streak exceeds 5. Validate against both `/app/corpus/clean/` and `/app/corpus/evil/` before finalizing, and only then capture the evil test log's output into `verification.log`.
