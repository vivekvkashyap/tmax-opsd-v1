Start the log server first (`python3 /home/user/build_server.py &`, installing the `websockets` package if needed) before writing/running your client, since the client depends on it being up. Write `/home/user/solver.py` as an async WebSocket client (using the `websockets` library) that connects to `ws://localhost:8080` and processes incoming lines with a small state machine keyed on line prefixes (`[START BUILD]`, `[ANALYZE]`, `[DEPENDENCY]`, `[END BUILD]`), closing the connection and exiting the loop once `[END BUILD]` arrives. For each `[DEPENDENCY]` line, parse the expression by splitting on ` AND ` and ` OR ` (note AND binds all clauses as simultaneous constraints; OR clauses are alternative acceptable ranges — evaluate accordingly) and extract `(operator, version)` tuples per library name using a regex over tokens like `>=`, `<=`, `==`, `!=`, `>`, `<`. Discover available versions for each library by scanning `/home/user/libs/` filenames matching `lib<name>.so.<major>.<minor>.<patch>` and parse the trailing three dot-separated integers as a semantic version tuple for numeric (not lexicographic) comparison. Accumulate all constraints seen for the same library across every `[DEPENDENCY]` line (constraints combine as AND across the whole stream, per the task description), filter the available versions against the combined constraint set, and pick the numerically highest surviving version per library. Sort libraries alphabetically by name and write their absolute file paths, one per line, to `/home/user/linker_flags.txt`.