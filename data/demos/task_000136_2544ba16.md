This is an Awk-only numerics task, so implement everything (sliding window, mean, LCG-based bootstrap, percentile selection) as pure Awk arithmetic — no calling out to Python/bc for the math. Parse the FASTA with Awk (skip `>` header lines, collect each sequence line), and for each of the 50 sequences compute all 91 window GC-fractions by sliding a length-10 substring (`substr(seq, pos, 10)`) and counting G/C characters, storing results in a 2D array indexed by [seq][position]. For each position, compute the true mean directly from the 50 values, then run exactly 100 bootstrap iterations where each iteration draws 50 values with replacement using indices generated strictly from the given LCG recurrence with a single global `seed` variable that carries state across the ENTIRE computation in the exact order specified (all 5000 draws for position 1, then position 2, etc.) — reimplementing this with `rand()` or reseeding per position will not match the reference. Sort each iteration's 100 bootstrap means (a simple insertion/bubble sort in Awk is fine) and pick the 3rd and 98th values (1-indexed) as the CI bounds. Print all four columns tab-separated with `printf "%.4f"` formatting, no header. Finally write a straightforward Gnuplot script using `plot ... with filledcurves` for the CI band and a line for the mean, outputting to PNG via `set terminal png` and `set output`.