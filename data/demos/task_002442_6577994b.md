Use `ffprobe -v error -select_streams v:0 -show_entries stream=avg_frame_rate -of default=noprint_wrappers=1:nokeyprint=1 /app/stream.mp4` to get the average frame rate as a fraction, evaluate it (e.g. with `python3 -c "from fractions import Fraction; print(round(Fraction('num/den')))"`), and write the rounded integer N followed by a newline to `/home/user/video_stats.txt`. For the server, write it in C using a TCP socket bound to 127.0.0.1:9000, `fork()` or use `select`/`poll` per-connection so multiple clients/messages work, and buffer partial reads until a `\n` is found (don't assume one recv() = one message). Count UTF-8 code points by counting bytes where `(byte & 0xC0) != 0x80`, not `strlen`. Maintain a global fixed-size circular buffer of size N (shared across all connections, lifetime of the server) storing each message's code-point count; on each new message, overwrite the oldest slot once the buffer is full, sum the current window, reply with the sum plus `\n`, and append the required log line. Compile with gcc, launch it with `&` or `nohup ... &`, and verify with `ss -tlnp` that port 9000 is listening before finishing.