Write the C filter first: read the two-column raw_spectra.txt into arrays, then for each index i in range, if i has at least 2 neighbors on both sides average intensities[i-2..i+2]; otherwise (first two / last two rows) copy the raw value unchanged — use printf with `%.2f` for exact two-decimal formatting, and double-check index boundary logic (i<2 or i>=n-2) against off-by-one errors since the boundary count depends on total row count. Compile with gcc and run it to produce filtered_spectra.txt before writing the bash script. In process_spectra.sh, read filtered_spectra.txt line by line (awk is well-suited here): a row is a local max only if its value is strictly greater than both immediate neighbors (first/last rows can never be peaks since one neighbor is missing) — append matches to peaks.log. For plot.txt, first find the max Filtered_Intensity across all rows, then for each row compute stars = floor((value/max)*20) using integer/floor arithmetic (awk's int() or bc), clamping negative or zero results to 0 stars, and print "Wavelength | " followed by that many `*` characters. Run the C binary then the shell script in sequence and confirm all three output files exist with correct formatting.