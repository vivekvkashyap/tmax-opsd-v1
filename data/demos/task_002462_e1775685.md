Find candidate files with `find /home/user/server_configs -name '*.json'`, but filter by calendar year 2023 precisely — don't use relative-time flags like `-mtime`; instead check each file's mtime year directly (e.g. `date -d @$(stat -c %Y file) +%Y` or Python's `os.path.getmtime` + `datetime.fromtimestamp(...).year`) and keep only those equal to 2023. For each surviving file, `json.load` it and only accept it if it has BOTH `app_name` and `config_version` keys — reject anything missing either, even if it's otherwise valid JSON. Normalize the app name (lowercase, spaces to underscores) to build the copy's filename `<app_name>_<config_version>.json` and copy (not move) it into a freshly created `/home/user/active_configs/`. While processing, accumulate `{normalized_app_name: config_version}` into a dict for the inventory. For the atomic write requirement, serialize the inventory to a temp file like `/home/user/config_inventory.tmp.json`, flush/close it, then use `os.replace()` (or `mv`) to move it onto the final `/home/user/config_inventory.json` — writing directly to the final path defeats the requirement.
