Implement `find_best_primer()` exactly as specified, matching the pseudo-code line for line — do not "improve" it with your own optimizer logic, since the verifier depends on bit-exact reproducibility of a seeded PRNG. Key points: declare `std::mt19937 local_gen(123)` as a *local* variable inside the function (not global), use the exact `bases[]` array and modulo operations in the given order (`idx = local_gen() % 10` before `base_idx = local_gen() % 4`, called in that sequence each iteration so the generator's state advances correctly), copy the current primer into a fresh `mutated` string each iteration before mutating index `idx`, and use `>=` (not `>`) when deciding whether to accept the mutated candidate — this asymmetry matters for reproducibility. After editing the file, compile with the exact flags given (`g++ -O3 -std=c++11 ... -o /home/user/primer_sim`) and run the binary so it writes `/home/user/best_primer.txt` itself; don't hand-compute or hardcode the output — let the program derive it from the deterministic RNG sequence.