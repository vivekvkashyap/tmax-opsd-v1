Work the git-history recovery and the C bug as two separate steps. For the token: `cd` into `/app/vendored/ticket_server/`, run `git log --all --oneline` to see every commit (including ones no longer on HEAD), then `git log --all -p -- config.h` (or `git show <commit>:config.h`) to view the deleted file's content at the commit before it was removed — grep for `ADMIN_TOKEN` or similar `#define`. Copy the token exactly (no quotes) into `/home/user/admin_token.txt`. For the crash: read `ticket.h` and `server.c` together — look for a struct field whose type was narrowed (e.g. `double`→`float`) while a `scanf`/`sscanf` format specifier elsewhere still expects the wider type (`%lf` reads 8 bytes into a 4-byte slot, corrupting adjacent stack memory). Reproduce with `gdb --args ./ticket_server <port>` and a crafted high-precision input to confirm the corruption before fixing. Fix by matching the struct field type and the format specifier consistently, then rebuild with `make`, and launch the daemon backgrounded on `127.0.0.1:9090`, verifying it stays up with `ps`/`curl` afterward.