First reconstruct the timeline: `grep`/sort the three service logs by timestamp to see which of the sample manifests in build_artifacts was being processed when service C's log abruptly stops — that manifest is your crash trigger. Confirm by running `/app/bin/manifest_compiler` directly on each sample manifest (in a subshell so a segfault doesn't kill your session) and checking `$?` for signal 139. Then do black-box diffing: compare the crashing manifest against the clean ones field-by-field (`diff`/`jq`) to spot the one anomalous key/value. Since the binary is stripped, prefer `strace`/`gdb` (`run`, then `bt` on crash, `x/40xb $rsp` or similar) only to confirm which field write overflows a stack buffer, rather than blind guessing — look for a fixed-size buffer being filled via an unsafe copy from a string field, and note the exact length/prefix condition that triggers it. Encode that exact condition (field name, length check, prefix check) into `/home/user/detector.py`, using a recursive walk since the field could appear at any nesting level, and make it exit 1 only when the exact condition is met, exit 0 otherwise, and exit non-crashing on malformed JSON. Test against a handful of your own clean/evil constructions before trusting it against the hidden corpus.
