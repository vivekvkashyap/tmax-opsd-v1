Treat `/app/legacy_calc` as a black box: feed it a few hand-crafted hex strings on stdin (`echo <hex> | /app/legacy_calc`) and observe outputs. Since it "decodes a hex-encoded string", try the classic single-byte-XOR-then-ASCII hypothesis — pick a hex string, XOR each decoded byte against a small set of candidate keys, and check if the result looks like a readable arithmetic expression (e.g. "12+5"); confirm by checking the printed output equals evaluating that expression. Once you've derived the encoding scheme and confirmed it against several probes, implement matching encode/decode logic in your Python emulator (do not hardcode the binary's output table — replicate the algorithm). For the server: validate the hex string parses cleanly (even length, valid hex chars) before decoding, and validate the decoded text is a two-operand math expression before evaluating, else 400. Implement Bearer-token auth checked before any other validation. For rate limiting, track a deque of request timestamps per client IP, reject with 429 once a 4th request lands within any rolling 10-second window. Start the server backgrounded (nohup ... &) and have your CI script poll (e.g. curl retry loop) until it's accepting connections before running assertions.
