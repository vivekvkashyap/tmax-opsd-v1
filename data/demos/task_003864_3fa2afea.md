Start by OCR'ing the image with `tesseract /app/docs/auth_stamp.png stdout` (or similar) and parse out the value after `AUTH_TOKEN=` — do not hardcode a guessed token, extract it programmatically so the script works against the hidden test image too. For the tar validator, use Python's `tarfile` module: open each archive with `tarfile.open`, then iterate `getmembers()` — for every member with `issym()` or `islnk()`, resolve where the link points relative to its containing directory and check whether the resolved (normalized) path escapes the archive root or points to an absolute filesystem path; also detect self-referential/cyclic symlink chains rather than just checking the immediate target. Never call `extractall()` on untrusted archives (that's exactly how such validators get exploited) — inspect members in memory instead. For `meta.json`, read it via `tarfile.extractfile()` and `json.load`, checking both presence and that `auth_token` equals the OCR'd value. For `.gcode` members, read their content and search for the literal `; END OF GCODE` line. Wrap each archive's processing in a try/except so a malformed tar (or one that raises during member iteration) is caught and classified EVIL rather than crashing the whole run. Write the final `{filename: "CLEAN"|"EVIL"}` dict as JSON to the given `--output` path.
