Parse the registry into a structure first, then resolve constraints, then topologically sort — keep these as distinct passes. Parse `/home/user/registry.txt` by splitting on `---` into entries, extracting `PKG`, `VER`, and `DEPS` (comma-split, `NONE` means empty) per entry; group entries by package name into a list of available versions. Do a graph traversal starting from `AppX`: for each package reachable, collect ALL constraints imposed on it from every dependent package's DEPS line (not just one), then for its available versions filter to those satisfying every collected constraint using proper semver comparison (split version strings into (major,minor,patch) tuples and compare numerically, not lexicographically — a naive string compare would misorder e.g. "1.10.0" vs "1.9.0"), and pick the max satisfying version. Recurse/traverse further through that chosen version's own DEPS to reach transitive dependencies, ignoring any package never reached from AppX. For the build order, do a proper topological sort: repeatedly pick the package(s) whose dependencies are all already placed, and when multiple are ready, break ties alphabetically by package name — don't just sort all packages alphabetically upfront, since that ignores the dependency ordering requirement. Write `PackageName@Version` lines to `/home/user/build_plan.txt`, then manually trace through the resolution by hand to sanity-check the output before finishing.