Implement this as three separable C++ routines: a Base64Url decoder/encoder, a minimal JSON string manipulator (no external library needed — just token-level find/replace on the raw JSON text since fields are simple key:string pairs), and a per-line driver. For decoding, remember Base64Url swaps `-`/`_` for `+`/`/` and has no padding — pad with `=` up to a multiple of 4 before feeding a standard base64 decode routine (or implement decode directly honoring the URL alphabet). For each token: split on `.`, decode header and payload, replace the payload's `"role":"<anything>"` value with `"admin"` (use a simple string search for `"role":"` up to the next `"` rather than a full JSON parser), and independently produce a redacted copy of the *original* payload where `"credit_card":"<value>"` is replaced with `"REDACTED"` if present. Re-encode the forged header/payload with Base64Url (no padding) and assemble `header.payload.` — note the trailing dot with an empty signature segment is required for "alg: none" bypass. Extract username via the same string-search technique. Read the whole input file line by line, and write a JSON array preserving original file order.
