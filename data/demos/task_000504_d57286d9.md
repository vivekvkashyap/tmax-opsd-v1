Get the C simulator bit-for-bit deterministic first, since the bash layer just orchestrates it. In `mc_sphere.c`, implement the LCG exactly as specified — a single `unsigned long long seed = 42` reinitialized fresh at the start of each program invocation (not persisted across N values, since each run is a separate process), updated with `seed = (1103515245ULL * seed + 12345ULL) % 2147483648ULL` before generating each of x, y, z in that order for each sample, then mapped to `[-1,1)` via `(double)seed / 2147483648.0 * 2.0 - 1.0`. Count points with `x*x+y*y+z*z < 1.0`, compute volume as `8.0*inside/N`, and print `"%d %.6f\n"` with N and the absolute error against 4.188790204786. In `run_experiment.sh`, compile with `gcc -O3`, loop N from 10000 to 100000 step 10000 invoking the binary fresh each time (each process reinitializes seed=42), append raw output lines to `mc_errors.txt`, then use `awk` to format each line as N right-aligned in a 6-character field (`printf "%6d | "`), followed by `floor(error*500)` asterisks — use `int()` in awk for the floor and be careful with floating point truncation vs rounding.
