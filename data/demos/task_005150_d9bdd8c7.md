Structure the C++ program around a simple in-memory dataset (parsed rows of id/fold/x/y from the CSV) and a reusable KNN-predict routine, since it's needed both for CV and benchmarking. For prediction: compute `abs(x_train - x_test)` for every training row, then sort candidates by `(distance, id)` using a stable sort (or a comparator that breaks ties on id ascending) so tie-breaking matches the spec exactly, take the first K, and average their `y`. For 3-fold CV: for each K in {1,3,5,7,9} and each fold in {1,2,3}, build train = rows where fold != i and test = rows where fold == i, predict every test point against that train set, compute MSE, average the 3 fold MSEs to get that K's CV MSE, then pick the K with lowest average MSE (smallest K wins ties). For benchmarking: retrain (i.e., just use the full dataset as "training set") with `best_k`, wrap the full-dataset prediction loop with a `std::chrono::high_resolution_clock` timer, repeat it 1000 times, and record total elapsed microseconds — don't include CSV parsing/CV time in this measurement. Write the JSON report with `best_k`, `best_cv_mse` rounded to 4 decimals, and `benchmark_time_microseconds`, then compile with `g++ -O3 -std=c++17` and run against the given CSV/JSON paths.