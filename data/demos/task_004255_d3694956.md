Since the oracle's exact aggregation rules aren't specified, reverse-engineer them empirically before writing Go: craft small synthetic JSON log files (a handful of lines with controlled timestamps, endpoints, response_time_ms, and status codes) and run `/app/bucket_oracle <in> <out>` on each, comparing input to output to pin down (a) the time-bucket width — vary timestamps by small increments and see where bucket boundaries fall, (b) the percentile formula — feed a known small set of response times and check which percentile method (nearest-rank vs interpolation) matches the reported value, and (c) the exact error-count condition on `status`. Confirm output column order, header text, and sort order (window, then endpoint) by diffing oracle output against your expectations byte-for-byte. Once the logic is nailed down, implement it in Go using a worker-pool pattern: split the input file into chunks (e.g., by byte offsets found via a fast initial scan for newlines), parse/aggregate each chunk concurrently into per-bucket maps, then merge and sort the partial results deterministically. Print periodic progress logs. Benchmark both programs with `/usr/bin/time` on the full 200MB file and iterate on concurrency (goroutines, buffered channels, minimizing allocations) until you comfortably clear 3x speedup while still producing an identical CSV.