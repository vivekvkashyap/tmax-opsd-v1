Start by reading the Rust compiler errors carefully — `cargo build` (or `maturin develop`) inside `/app/tlv_parser/` will point at the exact ownership/lifetime violation, typically a struct field or return value holding a borrowed reference (`&str`/`&[u8]`) that doesn't live long enough. Fix it by switching that field to an owned type (`String`, `Vec<u8>`, or a PyO3-owned `Py<PyAny>`) and copying the data out of the buffer instead of borrowing it — do not just slap on lifetime annotations if the underlying data doesn't actually outlive the function call. Activate the venv (`source /opt/venv/bin/activate`) before running `maturin develop`. For the sanitizer, implement TLV parsing with explicit cycle detection: track visited byte offsets (e.g. in a set) as you follow Tag 0x04 relative-offset references, and reject if an offset revisits a node already on the current path; separately track recursion/nesting depth and reject anything exceeding 10 levels. Write property-based tests with `hypothesis` generating both well-formed and adversarial (circular/deep) TLV byte sequences to validate your exit-code logic locally before relying on the hidden corpus.