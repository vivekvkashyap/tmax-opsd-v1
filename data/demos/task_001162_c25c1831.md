Build this as a sequence of independently verifiable steps. Write the `.proto` file first with `package build;`, a `service ArtifactCache { rpc GetChecksum(ArtifactRequest) returns (ArtifactResponse); }`, and the two message types, then generate stubs with `protoc` (or your chosen language's grpc tooling) before writing server logic. Generate the TLS material with `openssl req -x509 -newkey rsa:2048 -nodes -keyout /home/user/certs/server.key -out /home/user/certs/server.crt -days 365 -subj "/CN=localhost"`. Implement the gRPC service so `GetChecksum` always returns the fixed string, then start two listeners from the same service implementation — one plain (port 50051) and one wrapped in TLS server credentials built from the cert/key (port 50052); most gRPC frameworks let you add both a `add_insecure_port` and a TLS-configured port to the same server instance. For benchmarking, write a small client loop that times 1000 sequential unary calls per port and computes RPS = 1000/elapsed_seconds; compute `overhead_percentage` from your own measured plaintext_rps and tls_rps using the given formula — don't fabricate numbers, actually run the benchmark. Keep the server process backgrounded (e.g. `nohup ... &`) so it stays reachable after you finish, and double check both ports are actually bound with `ss -tlnp` before ending your turn.