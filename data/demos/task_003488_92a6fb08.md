First isolate the link failure: build `libfastcalc.a` from `/app/libfastcalc-1.2.3` and try linking it into a C++ binary — you'll hit a relocation error because the Makefile compiles the `.o` without position-independent code. Add `-fPIC` to `CFLAGS` in the Makefile and rebuild the archive before touching anything else. Next, open `fastcalc.c` and inspect `fast_sigmoid_array`; if it uses a crude polynomial/Taylor approximation, replace the loop body with the exact sigmoid formula `1.0/(1.0+exp(-x))` (link `-lm`) so the MSE against the true sigmoid is essentially zero, comfortably under the 1e-4 threshold. Get the library's version string from `fastcalc.h` (do not hardcode it elsewhere) and expose it through `GetVersion`. Design `calc.proto` with a `FastCalc` service, an empty-request RPC for version, and a repeated-double request/response RPC for `ComputeSigmoid`; generate C++ and Python stubs from the same proto so the server and test client stay in sync. Implement `server.cc` to include `fastcalc.h`, link `libfastcalc.a` plus gRPC/protobuf libs, and serve on port 50051 as a background process left running when you finish — verification connects to that live port.