Get the vendored library building before writing any simulation code. Inspect /app/minifft-1.2.0/Makefile for the compiler flags — look for an invalid/unsupported architecture flag (e.g. an `-march=...` value that gcc/g++ rejects on this machine) and remove or correct it so `make` succeeds and produces libminifft.a; don't add unrelated flags, just fix the one that breaks compilation. Then write wave_sim.cpp: generate the exact signal specified (N=1024 samples over [0,1), sum of the two given sinusoids at the given frequencies), pass it through MiniFFT::forward_transform using the struct/vector signatures declared in minifft.h, compute magnitudes (sqrt(real^2+imag^2) or std::abs on the complex output) for bins 0-49, and write bin_index,magnitude CSV rows to spectrum.csv rounded/formatted consistently. Write run_pipeline.sh to compile wave_sim.cpp statically against libminifft.a (correct include and link paths) and then execute the binary to (re)produce spectrum.csv. Since the grader compares your magnitudes to a reference FFT via mean-squared error, sanity-check your output's rough shape (peaks near the two driving frequencies) rather than assuming correctness from compiling cleanly.