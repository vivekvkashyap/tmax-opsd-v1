Debug this as a chain of dependencies: fix each broken link, verify it in isolation with `curl`, then move to the next. First edit `nginx.conf`'s `proxy_pass` to point at port 5001 (matching what `app.py` actually binds to — don't change the app's port, since the task says the config is what's wrong). Create `config.json` with exactly `{"status": "ok"}` so `app.py` stops crashing on startup. Launch the backend first and confirm with `curl http://127.0.0.1:5001/` that it responds before starting nginx with the given `-c`/`-p` flags; then confirm `curl http://127.0.0.1:8080/` returns 200 end-to-end (a 502 here means nginx started before the backend, or the proxy port is still wrong). For the watchdog, use Python's `requests` (or `urllib`) wrapped in try/except covering connection errors, timeouts, and non-200 status codes uniformly, appending exactly `UP\n` or `DOWN\n` to `/home/user/status.log` — test it manually once to confirm it appends `UP`. Finally install the cron line via `crontab -l | { cat; echo "* * * * * python3 /home/user/watchdog.py"; } | crontab -` rather than editing files directly, and confirm with `crontab -l`.
