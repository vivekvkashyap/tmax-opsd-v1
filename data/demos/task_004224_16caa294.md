Work end-to-end from the client request backward: verifier hits Nginx on 8080, Nginx must proxy_pass GET /api/v1/costs to the analyzer on 9091, the analyzer must call the datastore on 9092 using env vars, and the datastore must accept `Authorization: Bearer <FINOPS_API_KEY>` and return its metrics JSON. First inspect the two Python services to see exactly what env var names and header format they expect, then export `DATASTORE_HOST=localhost`, `DATASTORE_PORT=9092`, `FINOPS_API_KEY=opt-cost-2024` in `/home/user/.bashrc` and also `export` them in the current shell/start script (bashrc alone won't affect a non-interactive script unless sourced or the vars are exported before launching). Fix `nginx.conf`: set it to run unprivileged on 8080, `pid /home/user/app/nginx.pid;`, error_log to `/home/user/app/error.log`, and a `location /api/v1/costs { proxy_pass http://127.0.0.1:9091/api/v1/costs; }` block (watch trailing-slash semantics on proxy_pass). Write `start_all.sh` to launch both Python services in the background with the env vars in scope, start nginx with `-c` pointing at the local conf, then loop with `curl`/`nc` checks on 8080 and 9091, restarting nginx if it stops responding. Test the full chain with curl before declaring done.
