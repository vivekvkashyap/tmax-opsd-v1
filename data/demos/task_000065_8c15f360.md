Extract the archive with `tarfile` or `tar -xzf`, then write a Python migration script that walks `/home/user/raw_docs/` manually (do not rely on `os.walk` default behavior blindly) so you can detect directory symlinks before descending: for each entry, use `os.path.islink(path)` combined with `os.path.isdir(path)` (or `os.path.isdir` on the unresolved path) to identify a directory symlink, and skip descending into it entirely — do not copy it either. For files, try `content.decode('utf-8')` first inside a try/except `UnicodeDecodeError`, and on failure decode as `iso-8859-1` (which never raises, since it accepts every byte value) — record whichever succeeded as the "original_encoding" for that file. Re-encode to UTF-8, do a literal string replace of `AcmeCorp` -> `GlobalTech`, and write the result under `/home/user/clean_docs/` preserving the relative path (using `os.makedirs(..., exist_ok=True)` for parent dirs). Collect all `(relative_path, encoding)` pairs, sort them alphabetically by relative_path, then write them to `/home/user/migration.log` in the `path - encoding` format. Test with a small tree containing a self-referential symlink to confirm no infinite recursion or hang occurs.
