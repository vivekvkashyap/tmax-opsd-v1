Apply the patch first, then build the checksum tool against the patched state — do not compute hashes before the patch lands. Run `patch -p1 -d /home/user/build_scripts < /home/user/fix_circular.patch` (or `git apply` if the patch and target are git-tracked) and inspect the resulting `.py` files afterward to confirm the circular import is actually resolved (e.g. one module no longer imports the other at module load time, or the import was changed to a targeted `from x import y`) — a failed or partially-applied patch will silently produce wrong hashes later. Then write `/home/user/ci_checksum.py` to glob all `*.py` files directly inside `/home/user/build_scripts/` (non-recursive unless subdirectories exist), read each file's raw bytes, compute `hashlib.sha256(data).hexdigest()`, and build a flat dict keyed by filename (basename only, not full path) mapped to the lowercase hex digest. Dump this dict as JSON to `/home/user/build_scripts/manifest.json` with `json.dump`, then actually execute the script so the manifest file is written to disk before finishing.
