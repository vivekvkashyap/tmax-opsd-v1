Break this into independent, checkable stages before wiring up the API. First, parse the PDB with simple line filtering: PDB `ATOM` records have a fixed-width atom-name field — count lines where the record type is `ATOM` and the atom name column equals `CA` (use column-position slicing per the PDB spec, or split on whitespace carefully since atom name isn't always a clean token), giving you N, then derive k = N/100.0. Second, use `ffmpeg` to extract frames at the 10 one-second timestamps (`-vf select` or `-ss`/`-frames` per timestamp) at the video's known 30fps, convert to grayscale (e.g. with PIL/`cv2.cvtColor`), and sum pixel intensities per column to get each frame's 1D profile. Third, fit each profile to a Gaussian (e.g. `scipy.optimize.curve_fit` with a Gaussian model, or estimate variance directly via the second moment of the intensity-weighted column distribution) to get σ²(t) per second, then linear-regress σ² against t (`np.polyfit` or `scipy.stats.linregress`) — the slope equals 2·D_app, so D_app = slope/2. Validate D_app is a sane positive number before proceeding. Finally, stand up a minimal HTTP server (stdlib `http.server` is enough) bound to exactly `127.0.0.1:8000`, checking the `Authorization` header exactly and returning 401 otherwise, serving `/stats` with the computed N/k/D_app (D_app rounded to 2 decimals) as JSON, run it in the background (`&` or `nohup`), and write its PID to the specified file. Test with `curl` both with and without the auth header before finishing.