Work through the checklist in strict dependency order and validate each stage before moving to the next, since a broken earlier stage silently produces a wrong final number. Apply `discount_fix.patch` with `patch billing_core.c < discount_fix.patch` (or `patch -p1`/`p0` depending on the patch's path prefix — try both if one fails) inside `/home/user/release_prep`, then compile with `gcc -shared -fPIC -o libbilling.so billing_core.c`. Install grpc tooling (`pip install grpcio grpcio-tools`) and generate bindings with `python -m grpc_tools.protoc -I. --python_out=. --grpc_python_out=. billing.proto`. In `server.py`, define a `ctypes.Structure` subclass mirroring `struct LineItem { int item_id; double amount; }` field-for-field in order and type, load `libbilling.so` via `ctypes.CDLL`, set `calculate_total.restype = ctypes.c_double` and appropriate `argtypes` (pointer to the Structure array, c_int count), and in your RPC handler build a `(LineItem * len(items))()` ctypes array from the protobuf request's repeated field, preserving input order, then call the C function and return the double in the response. Start the server bound to `localhost:50051`. In `verify_release.py`, build the exact three-item request in the given order, call `Calculate` via a grpc channel/stub, and write `FINAL_TOTAL: <value>` to the log — actually run this script (not just write it) so the log file exists with a real computed result.