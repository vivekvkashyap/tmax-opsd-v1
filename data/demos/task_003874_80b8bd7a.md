Extract the tarball into /home/user/restore/, then open server.c and find where it calls htons(80) (or similar) when constructing the sockaddr_in — change that literal to 8080 so it can bind without root. Compile with gcc into a binary literally named `server` in that directory. For the watchdog script, use `nc -z 127.0.0.1 8080; if [ $? -ne 0 ]; then nohup /home/user/restore/server & fi` (or equivalent) so it's idempotent — safe to run repeatedly without spawning duplicate servers. For cron.txt, write the standard 5-field crontab line using `*/5 * * * *` as the schedule followed by the absolute path to watchdog.sh — do not include a leading comment or extra text since the grader regex-matches the line exactly. Run watchdog.sh once, sleep a couple seconds for the server to come up, then use a raw TCP client (nc, curl, or a one-line python socket script) against 127.0.0.1:8080 to capture the exact bytes the server sends and redirect/save that into restore_status.log — verify the log content matches what the server actually prints (don't paraphrase or add extra text around it).