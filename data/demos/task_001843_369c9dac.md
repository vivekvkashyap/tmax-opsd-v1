Work through the four components independently and test each before wiring them together. For the Rust evaluator, run `cargo build` to see the actual borrow-checker error — a common cause is a value moved into a function call and then used again afterward; fix by cloning or passing a reference (`&expr`) instead of moving it, then confirm the binary's last stdout line is valid JSON. For the C++ cacher, compile with `g++ -o cacher main.cpp` and look for classic memory-safety bugs — undersized fixed buffers, off-by-one loop bounds, missing null terminators; fix by sizing allocations correctly (or build a `std::string` result directly instead of a raw buffer) and test with a long input to confirm no segfault. Treat `/app/mobile_signer` as an opaque black box — invoke it via subprocess with the file path and capture trimmed stdout; do not reverse-engineer it. Finally build the HTTP orchestrator (Flask/Express/etc.) on 127.0.0.1:8080, wire `/process` to invoke all three subprocesses and assemble the JSON response with keys `ast`, `hash`, `signature`, then write `READY` to `/home/user/ready.txt` only once the server is confirmed listening.