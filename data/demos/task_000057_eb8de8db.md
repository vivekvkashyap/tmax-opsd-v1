Don't assume the bug is in your test suite or invocation — since parallel execution is being silently ignored, grep the vendored `bats-core` source for anywhere the job count gets hardcoded or overridden after argument parsing. Search `/app/bats-core-1.8.2/libexec/bats-core/` (especially `bats-exec-suite`, which orchestrates parallel test-file execution) for suspicious assignments like `jobs=1` or similar debug leftovers that would override a `--jobs`/`-j` value the argument parser already set; remove or fix that line so the user-supplied job count flows through. Confirm the fix by running `bats --jobs 8` directly on a small test set and observing wall-clock speedup before wiring up `/home/user/run_tests.sh`. That script should just `exec`/call `/app/bats-core-1.8.2/bin/bats --jobs 8 /home/user/web-sec-tests` and be made executable with `chmod +x`. Since the suite has ~24 independent one-second tests, correct parallelism across 8 jobs should bring total runtime well under the pass/fail time threshold — if it's still slow after your fix, look for other serialization points (e.g. a lock file, a `wait` that blocks on all jobs before starting the next batch) rather than re-checking your own script.
