Build perf_model.c in clear stages and verify each numerically before moving to the next. First write a CSV parser that reads perf_data.csv into arrays for cpu_util, mem_util, io_wait, response_time (1000 rows), and construct the design matrix X (column of 1s plus the three predictors) and response vector y. Second, install liblapacke-dev and call LAPACKE_dgels (or form and solve the normal equations with LAPACKE_dposv) to get the OLS coefficients — sanity-check these against an independent quick computation (e.g. a small numpy script) before trusting them, since a transposition or row/column-major mixup in LAPACKE calls is the classic bug (LAPACKE expects specific matrix layout flags like LAPACK_ROW_MAJOR — get this right or your coefficients will be silently wrong). Third, implement a from-scratch Metropolis-Hastings sampler: initialize the chain at the OLS estimate, propose new coefficients via Box-Muller-generated Gaussian noise (sd=0.05) for each of the 4 parameters jointly or componentwise, accept/reject based on the Gaussian likelihood ratio (sigma=1.0) with a flat prior, run 50000 iterations, discard the first 10000, and average the rest. Print both result lines to 4 decimal places in the exact required format, then compile and run to produce perf_analysis.log, checking that MCMC_MEANS end up close to OLS_COEFS as expected given the tight proposal and flat prior.