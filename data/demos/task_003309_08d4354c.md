Do the network calls first, capture raw artifacts, then process. Use `requests.post(url, data={...}, verify=False)` to hit `/auth` and grab the `Set-Cookie` value for `legacy_session` from the response (`resp.cookies['legacy_session']`). For the TLS certificate, `requests` with `verify=False` won't expose cert details directly — instead open a raw socket with Python's `ssl` module (`ssl.get_server_certificate` or wrap a socket with an `SSLContext` that has `verify_mode=CERT_NONE`, then call `getpeercert()`) to pull the Subject and extract the CN field specifically (it may be nested as a tuple of tuples — parse it rather than assuming a flat dict). Read the AES key bytes from `/home/user/app_config/secrets.txt` exactly as stored (no extra whitespace). Hex-decode the cookie value, then decrypt with AES-128-ECB using that key via the `cryptography` library, and strip PKCS#7 padding afterward (don't assume a fixed-length trim — implement or use a real unpadder, since ECB mode encrypts each 16-byte block independently with no IV). Write the two recovered strings into the exact JSON key names requested.
