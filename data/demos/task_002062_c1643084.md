Transcribe `/app/requirements.wav` first (speech-to-text tooling, or install `whisper`/similar if needed) and extract every filtering/traversal rule stated verbatim before writing any resolution code — this task is graded by exact behavioral match against a hidden oracle across 100 random fuzz cases, so precision on each rule matters more than plausible-looking logic. Implement `/home/user/resolve.py` with the CLI `--db <path> --target <name>`, parse the JSON library list, and apply filters in the order the voicemail specifies (ABI restriction, version ceiling, metadata exclusion) using proper SemVer comparison (don't do naive string comparison — split into major.minor.patch and compare numerically, handling pre-release suffixes per SemVer 2.0.0 rules). When multiple versions of the same library qualify, keep only the highest valid version. Traverse dependencies with the exact graph algorithm described (BFS vs DFS matters for output order), skipping already-resolved names to avoid duplicates/cycles. Print only resolved `<name>.so.<version>` lines to stdout in resolution order, nothing else. Validate your implementation against a few hand-built small JSON databases where you can compute the expected resolution order by hand before trusting it against the full fuzz suite.
