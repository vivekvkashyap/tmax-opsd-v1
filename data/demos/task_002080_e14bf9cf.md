Work through the failures in the order they'd actually surface. First `make`, then run `./server` and read the dynamic-linker error — a custom `.so` in a non-standard directory (`lib/libobfuscator.so`) won't be found at runtime unless its directory is on the runtime library search path; use `ldd ./server` to confirm which library is unresolved, then export the environment variable that controls the runtime shared-library search path pointing at that `lib/` directory, and record which variable you used. Second, once it links but still won't bind, use `strace -f -e trace=open,openat ./server` to watch which config file path it attempts and fails to open — create that exact file (discovered via strace, not guessed) containing the literal word it expects, and record the absolute path. Third, run `client.py`, observe the abort/core-dump, and use `gdb --args ./server` (run, then `bt` after crash, or `catch throw`) to identify the uncaught exception type propagating out of the detached thread — wrap the thread's lambda body in try/catch for that type, print the required message on catch, and record the exact exception type name. Recompile, restart the server, and rerun `client.py` to confirm it now completes all four payloads successfully before writing `investigation.txt`.