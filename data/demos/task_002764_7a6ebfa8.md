Recover the passphrase from `/app/transmission.wav` using any transcription tool you can install (e.g. `openai-whisper`, `whisper.cpp`, or a system STT utility) — use its actual output, normalized to lowercase with punctuation stripped and whitespace trimmed, as the bearer token; don't assume the words. For the archive scan, use Go's `archive/tar` to stream `/app/artifacts.tar` into memory (no extraction to disk), collecting symlink targets and regular-file paths in one pass. To detect symlink loops without touching the filesystem, resolve each symlink's target relative to its own directory and flag a cycle when a resolution chain revisits a directory path already seen in that chain (per-resolution visited-set, plus a depth cap as backstop). Only `tar.TypeReg` entries go into `valid_files`, normalized to start with `/`. Build the HTTP server with `net/http` on `127.0.0.1:8080`: `/status` always returns 200, `/curate` checks `Authorization: Bearer <token>` equals the transcribed phrase (401 if not, else 200 with the JSON file list). Launch it detached/backgrounded so it stays up for the test suite.
