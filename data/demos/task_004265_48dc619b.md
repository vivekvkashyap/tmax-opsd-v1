First, actually transcribe /app/voicemail.wav — do not guess the parsing rules; run a speech-to-text tool (e.g. `whisper` if available, or a Python speech-recognition library) against the file and read the transcript carefully, since it specifies the exact timezone to use, the field delimiter, the field order, the required case transformation, and the precise error-handling contract. Apply any timezone/locale setup it mentions (e.g. install/verify the relevant IANA timezone data and use a timezone-aware datetime library rather than naive UTC math). Then write /home/user/parser.py to: read one line from stdin, split on the delimiter named in the memo, validate you get exactly the expected number of fields and that the timestamp field parses as an integer, convert it to an ISO-8601 string in the specified timezone, apply the specified case transformation to the severity field, and emit a single JSON object to stdout with the exact keys named in the memo — on any validation failure, catch the exception and print exactly the malformed-error JSON object specified, nothing else. Test manually against a few valid and deliberately malformed lines (missing field, non-numeric timestamp) to confirm both code paths produce clean, single-line JSON with no stray prints.
