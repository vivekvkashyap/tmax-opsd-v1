Write the C++ program in stages and test each independently before combining. First implement recursive directory traversal (`std::filesystem::recursive_directory_iterator`), reading each file's first 4 bytes to detect ELF magic (`7F 45 4C 46`) — rename matches by appending `.elf.bak`, but skip files already ending in `.elf.bak` to avoid double-processing. For `.gcode` files: read raw bytes, convert ISO-8859-1 to UTF-8 by mapping each byte 0x80-0xFF to its two-byte UTF-8 encoding (bytes below 0x80 pass through unchanged — no bespoke iconv needed), then strip everything from `;` to end-of-line per line, drop resulting empty lines, and join remaining lines with `\n`. Chunk the cleaned buffer into exact 100-byte pieces (last chunk may be shorter), naming them `<name>.part001`, `part002`, etc. with zero-padded 3-digit indices, written beside the original before deleting the original. Collect manifest lines (`RENAMED ELF: <relpath>` / `CHUNKED GCODE: <relpath>`) using paths relative to `/home/user/backup_source`, sort them alphabetically as plain strings, and write to `/home/user/manifest.log`. Process ELF detection before deciding a file is a gcode candidate so you don't misclassify binaries with a `.gcode` extension. Test incrementally: verify chunk byte counts and manifest ordering against a small manual example before running on the full tree.
