Structure the C program as: setup dirs, then an infinite inotify event loop, then a per-file handler doing mmap→checksum→atomic-write→atomic-symlink→atomic-latest→delete. Use `inotify_init1`, `inotify_add_watch` on the incoming dir with `IN_CLOSE_WRITE`, and `read()` on the inotify fd in a loop, parsing `struct inotify_event` records (remember the event struct has a variable-length `name` field, so advance your buffer pointer by `sizeof(struct inotify_event) + event->len`). For each closed file: `open` + `fstat` for size + `mmap(PROT_READ, MAP_PRIVATE)`, sum all bytes into a `uint32_t` (natural wraparound gives you mod 2^32 for free), format as `%08x`. Write to a `mkstemp`-style temp file in objects/ then `rename()` over the final checksum-named path (rename is atomic on the same filesystem — keep temp files in the same directory as the target). For the by-name symlink, create a temp symlink with a unique name via `symlink()` then `rename()` it over the target to update atomically (plain `symlink()` fails if the link already exists). Same temp-write-then-rename pattern for `latest.txt`. Finally `unlink()` the original incoming file, and only then move to the next event. Compile with the exact `gcc -O2` command and background the resulting binary before finishing.