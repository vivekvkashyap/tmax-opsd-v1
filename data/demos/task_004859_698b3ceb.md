Work this in three independent stages and verify each before moving on. First, extract frames from `/app/build_metrics.mp4` with `ffmpeg -i build_metrics.mp4 frame_%04d.png` (or read frames directly via a Python video library), then for each frame sample the pixel color and map non-gray solid colors to the given characters; collapse consecutive identical Yellow frames into a digit count as described, and split the stream into separate expressions at each Cyan frame. Write the recovered expressions to a debug log so you can sanity-check them by eye. Second, fix the C library: run `make` to see the actual linker error (missing `-lm`, wrong `.so` target flags like `-fPIC -shared`) and fix the Makefile; then read `evaluate.c` carefully for a fixed-size stack buffer that can overflow on longer expressions (resize or bound-check it) and a use-after-free (don't free memory before returning/using it) — reproduce the segfault first with a long test expression before patching. Third, build the Flask/FastAPI app last, wiring ctypes to the compiled `libmatheval.so`, enforcing the bearer token check before any work, and using a simple in-memory per-IP sliding/fixed window counter for the 3-req/min rate limit, returning 401/429 appropriately. Start the server in the background and confirm it answers with curl before finishing.