Write `/home/user/analyze.sh` as a single self-contained script that first runs `sqlite3 /home/user/workflow.db "REINDEX;"` to rebuild all indices, then in one `sqlite3 -json` invocation runs a recursive CTE: start from `id = 1` with `depth = 0`, recursively join `nodes n ON n.parent_id = h.id` incrementing depth, then wrap with `RANK() OVER (PARTITION BY depth ORDER BY cost DESC) AS cost_rank` as a window function over the recursive result, and finally `ORDER BY depth ASC, cost_rank ASC`. Use `sqlite3 -json ... > /home/user/report.json` to get valid JSON output directly rather than hand-formatting it — this avoids escaping bugs. Before finalizing, manually trace the small tree from root down through a couple of levels to confirm your expected rank ordering matches what the recursive query should produce (ties in cost need a defined tiebreak — RANK's default ordering by rowid/insertion order for equal costs should be fine, but sanity check with `SELECT` output). Verify the script is executable (`chmod +x`) and re-run it standalone to confirm `/home/user/report.json` is well-formed via `jq . /home/user/report.json`.