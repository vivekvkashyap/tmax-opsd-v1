Extract per-frame average grayscale brightness from `/app/experiment.mp4` first (via `ffmpeg` frame extraction piped into Go image decoding, or ffmpeg's own signalstats/grayscale filters), producing an ordered array of 40 values. Clean the data in two passes in the correct order: first replace any exact-0 value via linear interpolation between the nearest valid non-zero, non-outlier neighbors (so index detection for "valid" must exclude both 0s and the >200 outlier before searching for interpolation neighbors); then replace any value >200 with the median of the 5 preceding *already-cleaned* values (this ordering matters — compute outlier replacement using cleaned neighbors, not raw ones). For the SMA hyperparameter search, implement the fold/CV structure literally as specified (4 folds of 10, train-on-prefix/test-on-next-fold across 3 splits, MSE per split averaged per k in {2..6}), and pick the k with lowest average MSE — don't assume a fixed k. Predict frame 40 using the winning k and the last k cleaned values. Build the Go HTTP server with Bearer-token middleware wrapping all three routes, checking the header before dispatching, and launch it as a background/detached process; test with `curl` including and excluding the `Authorization: Bearer RES-8821` header to confirm the 401/200 behavior before finishing.