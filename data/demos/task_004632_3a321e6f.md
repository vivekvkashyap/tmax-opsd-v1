Reverse-engineer the black-box binary by observation rather than guesswork: run `strings /app/waf_evasion_encoder` to spot embedded literals like salts or format strings, then feed it several test inputs (including edge cases like empty string, single char, and strings with special/base64-sensitive characters) and diff the outputs to isolate each transformation stage. Use `ltrace`/`strace` if available to see which libc/OpenSSL functions (e.g. base64 encode, MD5) are invoked and in what order. Once you identify the pipeline (e.g. some string reversal, some encoding, salt concatenation, some hash, then a formatted print), reimplement each stage in Bash using standard tools: `rev` for reversal, `base64 -w0` for encoding (careful about trailing newlines — strip them), string concatenation for the salt, and `md5sum`/`sha256sum` piped through `cut`/`awk` to extract just the hex digest. Match the exact output format (brackets, labels, delimiters) byte-for-byte by comparing your script's output against the binary's output for many random inputs, not just one. Make the script executable and accept exactly one CLI argument.