Build this as an OCR-plus-fuzzy-matching pipeline, not a hand-typed answer. Run Tesseract (e.g. `pytesseract.image_to_string` or the `tesseract` CLI) on `/app/ui_screenshot.png`, then parse each output line by splitting on the first `: ` to get `key` and the OCR'd translation — clean whitespace and strip stray characters, since OCR often drops accents or apostrophes. Load `/app/reference_db.csv` and join on `key`. For each matched pair, compute the Levenshtein edit distance between the OCR'd string and `french_expected` (use a library like `python-Levenshtein`, `rapidfuzz`, or implement classic DP edit distance yourself — don't approximate with string equality). Sort keys by distance to find the 2 highest (worst) and 2 lowest (best) and write them to `/home/user/audit_log.txt` in the exact `WORST: k1, k2` / `BEST: k3, k4` format. Compute the mean distance across all matched keys and write `{"mean_distance": <value>}` to `/home/user/metrics.json` — let the script compute this number from your actual OCR output rather than hardcoding it, since OCR quality will determine the real value.