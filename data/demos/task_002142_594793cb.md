Scaffold with `cargo new artifact_filter` and add crates for CSV parsing and randomness (e.g. `csv`, `rand`) to `Cargo.toml`; build in release mode (`cargo build --release`) since the entry point is `target/release/artifact_filter`. Implement the pipeline in strict order: first compute the median of non-missing `loss` values (treat empty string and "NaN" as missing) and impute, then compute mean/std of the imputed column and drop rows where `|loss - mean| > 3*std`, then bootstrap — resample with replacement 1000 times from the cleaned vector, compute the mean of each resample, sort the 1000 bootstrap means, and take the 2.5th/97.5th percentiles as the CI bounds (width = upper - lower). Write the cleaned rows to a temp CSV file (matching the same header/column format the oracle expects) and invoke `/app/bin/stat_oracle <tmpfile>` via `std::process::Command`, capturing and parsing its stdout as f64. Apply the exact classification rule (CI width < 2.5 AND oracle score > 0.65 => print "CLEAN" exit 0, else "EVIL" exit 1). Test against both `/app/corpora/clean/` and `/app/corpora/evil/` — a common failure mode is not removing outliers before feeding the oracle, causing it to crash or return garbage on evil files with injected extreme outliers.