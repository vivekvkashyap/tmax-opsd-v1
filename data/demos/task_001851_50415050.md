Don't try to eyeball which of 2000 payloads hangs the parser — use delta debugging/bisection: write a small harness that feeds subsets (or individual records) of failed_batch.json through `parse_hex_tlv` with a hard timeout (e.g. run each candidate in a subprocess with `timeout` or an alarm), binary-searching down from the full batch to isolate the single offending record. Once isolated, read `parser.py` closely: the TLV loop reads a tag byte, a length byte, then advances the index by `length` — if `length == 0` the index never advances past that record's position in a way that terminates the scan for certain malformed encodings, causing an infinite loop. Fix the loop to always make forward progress each iteration (e.g. treat a zero/invalid length as an error and break, or ensure the index strictly increases every pass) and add bounds checking so out-of-range slices don't crash on malformed hex strings. After patching, start Redis daemonized, launch the FastAPI ingestion service on 8000 and the gRPC worker on 50051, then re-POST the full failed_batch.json to the ingestion endpoint and confirm the worker drains the queue without hanging or crashing — check the queue empties and a gRPC GetMetrics call returns sane data before leaving the services running.