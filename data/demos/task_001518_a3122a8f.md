Build and verify each piece independently before wiring them together. Write `/home/user/healthd.c` using standard POSIX sockets: `socket()`/`bind()`/`listen()` on `127.0.0.1:8080`, `accept()` in a loop, read the PID from `/home/user/worker.pid`, check liveness via `access("/proc/<pid>/stat", F_OK) == 0` (or `kill(pid, 0)`), write `"STATUS: OK\n"` or `"STATUS: FAIL\n"` to the client socket, then `close()` the connection and loop again (don't exit after one client). Compile with `gcc -o /home/user/healthd healthd.c` and launch it backgrounded (e.g. `nohup /home/user/healthd &`) so it persists. Start a test worker with `sleep 3600 & echo $! > /home/user/worker.pid`. Test the daemon manually with `nc 127.0.0.1 8080` before touching cron. For the cron fix, don't just edit `query_health.sh` — the task requires setting `LOG_DIR` inside the crontab itself: capture the existing crontab (`crontab -l`), prepend a line `LOG_DIR=/home/user/logs`, and reload it with `crontab -`. Finally run `LOG_DIR=/home/user/logs /home/user/query_health.sh` once manually to confirm `/home/user/logs/status.log` immediately contains `STATUS: OK`.