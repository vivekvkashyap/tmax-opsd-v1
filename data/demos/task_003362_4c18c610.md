Diagnose the two separate bugs independently before combining fixes. First, verify/fix frequency extraction: check `fft.py` correctly identifies the dominant non-zero frequency bin from the frame-intensity time series (extract mean pixel intensity per frame via `ffmpeg`/OpenCV, sample rate from video fps) — sanity check the returned frequency against the intensity signal's visible period before trusting it downstream. Second, fix the integrator: explicit (forward) Euler updates position using the *old* velocity and velocity using the *old* position within the same step, which is only conditionally stable and diverges for oscillatory systems unless dt is tiny relative to the period; switch to semi-implicit (symplectic) Euler, where you update velocity first using the current position's acceleration, then update position using the *newly updated* velocity in the same step — this ordering is what gives it energy-conserving stability. Use a small step like `dt=0.01` and iterate a bash arithmetic loop (or `awk`/`bc` for the trig) from t=0 to 2.0, writing `Time\tPosition` rows to `/home/user/trajectory.tsv`. Confirm your output visually oscillates with roughly the expected period matching the extracted frequency before finalizing.
