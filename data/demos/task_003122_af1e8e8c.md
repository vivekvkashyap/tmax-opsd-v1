Split this into a build-linking bug and a shell string-parsing bug, and fix each at its root rather than papering over it with environment variables the runner won't set. For the library: a shared object must be compiled with position-independent code (`-fPIC`) before being linked into `liburlsanitizer.so`, and the consuming binary needs its runtime search path baked in (e.g. `-Wl,-rpath,'$ORIGIN'` or an absolute rpath pointing at the build directory) so `url_tool` finds the `.so` without the caller exporting `LD_LIBRARY_PATH` — edit `build.sh` accordingly and confirm with `ldd url_tool` that the library resolves. For the URL bug: in `test_routing.sh`, after extracting the path portion of the URL, strip everything from the first `?` onward (e.g. `${var%%\\?*}` or `cut -d? -f1`) before passing it to `url_tool`, since query strings currently leak into the security check. Verify both fixes by running `ci_run.sh` yourself and confirming it prints success and produces `ci_success.log`, rather than assuming the fix is correct from inspection alone.