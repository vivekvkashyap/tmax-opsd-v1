Build `/home/user/aggregate.py` in clear stages and test each before moving on. First parse `/home/user/config.ini` with Python's `configparser` to read `zip_file` and `csv_file` under `[Target]` — do not hardcode these names since the whole point is that the config selects the target. Then extract the archives in memory or to a temp dir: use `tarfile.open('/home/user/dataset.tar.gz')` to extract the named zip member, then `zipfile.ZipFile` on that to extract the named CSV member (nested archive access, not a flat search). Read the CSV with the `csv` module (it has no header), sum `int(row[1])` over all rows. For the locking requirement, open `/home/user/summary.log` in append mode, call `fcntl.flock(f, fcntl.LOCK_EX)` before writing the line `<zip_file>/<csv_file> SUM:<sum>` and `fcntl.flock(f, fcntl.LOCK_UN)` (or rely on the `with` block closing the file) afterward — grep your own script for `fcntl.flock` and `LOCK_EX` to confirm it's present. Finally, actually execute the script once so `/home/user/summary.log` is created, then inspect its contents to confirm the exact format matches.