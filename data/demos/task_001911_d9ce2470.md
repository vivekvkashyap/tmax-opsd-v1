Handle each file's encoding explicitly rather than assuming defaults: open `dataset_alpha.csv` with `encoding='ISO-8859-1'` and `dataset_beta.json` with `encoding='UTF-16LE'` (or use `iconv` first to convert to UTF-8 on disk, then read normally) — reading with the wrong encoding will corrupt accented characters silently. For the CSV, use `csv.DictReader` with `delimiter=';'` so column order and names are preserved automatically; keep all values as strings per the spec. For the JSON, `json.load` the UTF-16LE-decoded text so numeric types stay native. Write both as JSON Lines with `json.dumps(obj, ensure_ascii=False)` per line, opened with `encoding='utf-8'`, to `/home/user/processed_data/`. Compute manifest hashes with `sha256sum` (or `hashlib.sha256` in Python) over the actual output files using only the basename, formatted as `<hash>  <filename>` (two spaces, matching `sha256sum`'s own format so `sha256sum -c manifest.txt` validates it). Finally archive with `tar -czf /home/user/final_dataset.tar.gz -C /home/user processed_data` so the tar contains a `processed_data/` prefix, and confirm with `tar -tzf` that the listing matches expectations.
