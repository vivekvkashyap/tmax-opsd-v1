Diagnose the bug by reading the existing C code structurally rather than rewriting from scratch: the current nested loop iterates every log row against every perm row and only checks `perms[j].granted == 0`, never comparing `user_id`/`resource_id` between the two arrays — that's the O(N^2) cross-join bug. Fix the join condition first (add `strcmp(logs[i].user_id, perms[j].user_id) == 0 && strcmp(logs[i].resource_id, perms[j].resource_id) == 0` to the match test), then separately address efficiency: build a hash map or sort perms by `(user_id, resource_id)` and binary-search/lookup per log row instead of the inner linear scan, so each log entry resolves to at most one matching perm entry (or none, meaning "missing permission" which also counts as a violation, same as granted=0). Only emit a violation when either no matching perm row exists for a log entry, or the matching perm's `granted` field is 0. Recompile to `/home/user/audit`, run it, sort the output lines alphabetically (`sort`), and write the result to `/home/user/violations.txt` in the exact `user_id,resource_id` format.