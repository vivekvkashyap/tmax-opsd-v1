First transcribe /app/voice_query.wav (whisper or similar) to recover the exact start and end node IDs stated in the recording — do not assume or guess them. Then inspect /app/naive_query.c to confirm the performance problem (it re-scans the CSV from disk on every node expansion), and design fast_query.c to instead read interactions.csv exactly once into an in-memory adjacency list (e.g. arrays/hash map keyed by node id), then run Dijkstra's algorithm with a binary-heap/priority-queue for near-linearithmic performance so it comfortably beats the required runtime threshold. Output must be the exact JSON schema given: `{"start": INT, "end": INT, "path": [...], "total_weight": FLOAT}`, computed from the true shortest path found, not fabricated. Finally write run_pipeline.sh that parses the transcribed node IDs (from your own transcription step or a variable you set), compiles fast_query.c, runs it with those IDs, and redirects/saves the JSON output to /home/user/final_result.json — benchmark your compiled binary with `time` yourself to confirm it's well under the speed requirement before finishing.