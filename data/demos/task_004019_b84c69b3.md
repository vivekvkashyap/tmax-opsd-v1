Break this into independent pipeline stages and verify each before wiring them together. First, use `ffmpeg` filters (e.g. `blackframe`/`scdet` or a brightness-measuring filter like `signalstats`/`showinfo`) to scan the video and extract the timestamps of bright-flash frames — parse ffmpeg's stderr/log output for the frame times rather than guessing. Second, build the SQLite database with a script: create the network log table, add indexes on `timestamp`, `src_ip`, and `dst_ip` up front (index before bulk load or immediately after via `CREATE INDEX`), then use a `±1s BETWEEN` join against the flash timestamps to flag `malicious` rows, and a self-join/CTE with a `timestamp` window filter (`dst.timestamp BETWEEN src.timestamp AND src.timestamp+10`) to derive secondary compromised nodes distinct from primary malicious src/dst pairs. Third, for the API, hand-roll a minimal HTTP/1.x response (status line, `Content-Type: application/json`, blank line, body) inside a `socat`/`nc` loop bound to `127.0.0.1:8080`, generating the JSON from your SQL query results and matching the exact schema keys requested. Launch the server with `&`/`nohup` so it stays listening, and confirm with `curl` before finishing.
