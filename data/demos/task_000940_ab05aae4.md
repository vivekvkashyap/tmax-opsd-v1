Audit the vendored C source methodically rather than pattern-matching blindly: grep for `strcpy`, `memcpy`, and other unchecked-length copies in the payload decoding path, and separately grep for the authentication comparison logic to see how each user/password pair is validated. For the overflow, the fix is to bound the copy length to the destination buffer's actual size (e.g. replace `strcpy` with a length-checked copy using `strncpy`/explicit size checks, or reject oversized decoded payloads outright before copying) — verify the destination buffer size by reading its declaration, don't assume. For the auth flaw, find any branch that special-cases a privileged username (e.g. `admin`) and returns success without checking the password, then require both the correct username AND correct password to match. After patching, rebuild with the provided `Makefile` (`make clean && make` if needed) and copy the resulting binary to `/home/user/tinyserver_patched`. Run `/app/evaluate_server.py /home/user/tinyserver_patched` yourself, read the SCORE output, and if it isn't 1.0 inspect which specific requests are failing (crashes vs wrong accept/reject) and iterate — don't declare done until the score is 1.0.