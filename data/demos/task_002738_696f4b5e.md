First diagnose the corruption before reaching for heavy tools: inspect the header bytes of `/home/user/metrics.db` (e.g. `xxd -l 32 metrics.db`) and compare against the expected 16-byte SQLite magic string `SQLite format 3\0`; a corrupted magic with intact page data can often be repaired just by restoring those header bytes (`printf`/`dd` writing the correct magic back at offset 0), after which `sqlite3 metrics.db ".dump"` or a simple `SELECT` should work again. Only fall back to `.recover`/manual page scraping if the simple header fix doesn't restore readability. Once you can query it, recreate a clean copy at `/home/user/recovered_metrics.db` (dump + reload, or `.clone`/`VACUUM INTO`) so the original stays untouched as evidence.

For the variance bug, recognize the root cause: `E[x^2]-(E[x])^2` computed via `awk` in double precision loses precision when values are large (~1e9) with small spread, because the squared sums are enormous compared to the true variance. Replace it with Welford's single-pass online algorithm (track `n`, running `mean`, and `M2`, updating incrementally per value, then `variance = M2/(n-1)`) or a two-pass approach that first computes the mean, then sums squared deviations from it — both avoid catastrophic cancellation. Point the script at the recovered database, run it, and confirm the output file has exactly two decimal places with no extra text.