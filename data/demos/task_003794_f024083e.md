Write the Go traversal as an explicit recursive/iterative walk using `os.Lstat` (not `os.Stat`/`filepath.Walk`, which won't give you the control you need over symlinks) so you can detect symlinks and decide whether to follow them. To avoid infinite loops, maintain a set of visited directory identities — use the OS-level device+inode pair (`syscall.Stat_t`'s `Ino`/`Dev` via `os.Stat` on the resolved target) each time you're about to descend into a directory reached through a symlink, and skip if you've already visited that (dev, inode) pair. For files, resolve each path to its real underlying file with `os.Stat` (which follows symlinks) to get the true inode, and keep a separate "seen file inodes" set so a file reached via multiple symlinks is only reported once. For type detection, open each unique regular file and read exactly the first 4 bytes, comparing against the ELF magic bytes and the literal `"; G-"` prefix — do not rely on filename extensions at all. Build the CSV rows as (inode, type, size) tuples, sort them numerically by inode (not as strings — beware lexicographic sort putting "10" before "2"), and write comma-joined lines with no header row to `/home/user/report.csv`.