Work top-down through the pipeline. In `semver_filter.cpp`, parse each version by splitting on `-` first (to isolate an optional prerelease), then splitting the remainder on `.` into three integers; compare major/minor/patch numerically, and only fall back to comparing prerelease strings when major.minor.patch are equal — remember a version with no prerelease outranks one with the same major.minor.patch and a prerelease (e.g. `2.0.0` > `2.0.0-rc2`), and two prereleases compare by ordinary string comparison per the task's simplified rule. Write every version from `<input_file>` whose comparison is `>=` `<min_version>` to `<output_file>`, preserving input order. In `deploy_workers.go`, build a real worker pool: create a buffered `chan string` of versions, launch exactly 3 goroutines with `go func(){...}()` each reading from the channel until it's closed, and either serialize file writes through a `sync.Mutex`/dedicated writer goroutine or send results back over a second channel that the main goroutine drains and appends to `<log_file>` sequentially — do not let multiple goroutines write to the file concurrently. Finally, `ci_pipeline.sh` should `g++` compile the filter to `/home/user/semver_filter`, invoke it with the specified min version and file paths, then `go run` the deploy program, checking each step's exit status and propagating failure with `exit 1` if any step fails, `exit 0` otherwise. Run the pipeline script yourself at the end to produce the logs.