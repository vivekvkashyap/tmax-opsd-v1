Work strictly in the order the spec implies: first parse `security.log` for lines containing the literal marker `[LEAK_DETECTED]` and extract the service name from each (read the actual log format rather than assuming a fixed column position). For each leaked service, recompute its config file's SHA-256 (`hashlib.sha256` over the raw file bytes, matching how `sha256sum` hashes) and compare against the recorded hash for that same filename in `config_hashes.txt` — parse that file's `hash  path` format carefully, since `sha256sum`-style output uses two spaces and a path, not just the bare filename. If the hash differs, log `TAMPERED:` and skip rotation entirely for that service — do not touch its JSON. If it matches, read the key from `master.key` (strip trailing newline), build the JWT payload exactly as specified, sign with `PyJWT`'s `HS256`, and update only the `"token"` field in place in the JSON config (read-modify-write the full object so other keys like `port` are preserved untouched). Append one report line per leaked service, in the order encountered in the log, to `/home/user/rotation_report.txt`.
