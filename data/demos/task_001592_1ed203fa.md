Break this into three independently-verifiable stages rather than writing one monolithic program. First, transcribe `/app/network_log.wav` with a speech-to-text tool (e.g. install `openai-whisper` or use `SpeechRecognition`) and inspect the raw transcript text yourself before parsing — transcription errors on proper nouns/numbers are the main failure mode, so sanity-check the parsed node names and latency numbers look sane (e.g. no stray words attached to numbers) before trusting them. Second, in C++, parse each "Node X connected to Node Y with latency Z milliseconds" sentence with simple string parsing, then use the SQLite3 C API with `sqlite3_prepare_v2`/`sqlite3_bind_*`/`sqlite3_step` (parameterized, not string-concatenated) to insert each record into `connections`. Third, run a `GROUP BY source, target` SQL query with `AVG(latency)` to project the graph, round each average to the nearest integer, and write it to `/home/user/graph_export.csv` with header `source,target,avg_latency`. Compile with the exact given g++ command and actually execute the binary to produce the CSV — verify by rerunning and eyeballing row counts/edges match what the transcript implies before considering the task done.