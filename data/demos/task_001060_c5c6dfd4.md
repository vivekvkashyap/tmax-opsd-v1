Recover the calibration offset first: run OCR (`tesseract /app/calibration.png stdout`, installing tesseract-ocr if needed) and parse the printed `OFFSET=...` line from the extracted text — don't hardcode a guessed number. Build filter.sh as a strict stdin-to-stdout validator: read line by line, pass through anything that looks like a header (non-numeric third field or known header names) unchanged, and for data rows validate all three fields — timestamp against an ISO8601 regex or `date -d`/`python3 -c` parse check, sensor_id against `^[A-Za-z0-9]{8}$` exactly, and value as a float strictly within [-50.0, 150.0] — logging any rejected row with a reason to stderr and only ever writing genuinely valid rows to stdout, since the hidden clean/evil corpora will be checked row-for-row. For etl.sh, stream every CSV in the input dir through filter.sh, add the OCR'd offset to each valid value, deduplicate by (timestamp, sensor_id) keeping first occurrence (e.g. with awk `!seen[$1,$2]++`), truncate timestamps to the hour, group by (hour, sensor_id) to compute averages (awk or a small Python/awk aggregation), then sort by hour then sensor_id and format each line as `YYYY-MM-DDTHH,sensor_id,avg_value` rounded to 2 decimals.