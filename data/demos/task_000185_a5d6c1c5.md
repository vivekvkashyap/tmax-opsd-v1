Parse with two separate regexes (or one regex plus a per-match timestamp-format branch): match the pipe-delimited fields `Task:(\w+)`, `Target:(\w[\w-]*)`, `Latency:(\d+)ms`, and for the timestamp try ISO `YYYY-MM-DDTHH:MM:SSZ` via `datetime.strptime(...,"%Y-%m-%dT%H:%M:%SZ")` first, falling back to US format `MM/DD/YYYY HH:MM:SS` via `"%m/%d/%Y %H:%M:%S"` — skip (don't crash on) any line that matches neither pattern, since garbage/error lines are mixed in. Deduplicate by keeping, for each Task ID, only the record with the max parsed timestamp (a dict keyed by task_id, overwriting only when the new timestamp is later, handles this in one pass). After filtering to `fr-FR` and sorting ascending by timestamp, compute the rolling average with a `collections.deque(maxlen=3)`: append the current latency, then average whatever's currently in the deque (naturally handles the first two records having window size 1 and 2). Write the CSV via the `csv` module with the exact header `TaskID,TimestampUTC,RollingAvgLatency`, formatting timestamps with `strftime("%Y-%m-%dT%H:%M:%SZ")` and rounding the average to exactly 2 decimals (use `f"{x:.2f}"` rather than `round()` alone so trailing zeros are preserved).
