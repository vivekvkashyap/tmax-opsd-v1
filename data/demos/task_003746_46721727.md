Start the stack with `/app/start_services.sh` and wait for ports 5432/6379/8000 before connecting. Build the classifier around two independent SQL checks against Postgres (psycopg2 or similar): (1) a recursive CTE over `network_graph` that expands `source_node -> dest_node` edges accumulating `SUM(cost)`, tracking visited nodes per path to avoid infinite cycles, then takes `MIN(cost)` grouped by destination — an event is EVIL if no row reaches the target or the minimum cost exceeds 15; (2) load each file's events into a temporary/staging table and use a window function like `COUNT(*) OVER (PARTITION BY user_id ORDER BY timestamp RANGE BETWEEN INTERVAL '10 minutes' PRECEDING AND CURRENT ROW)` to detect any user with more than 3 accesses in a rolling 10-minute window. Short-circuit to `EVIL` on the first violation found across all events in the file, else print `CLEAN`. Keep stdout clean — no debug prints — and check `EXPLAIN ANALYZE` on your CTE/window queries to keep runtime under 2s per file (index the graph table and avoid re-scanning per event where possible).
