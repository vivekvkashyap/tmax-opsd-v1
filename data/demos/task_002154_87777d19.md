Write a script (Python is easiest for binary handling) that walks `/home/user/legacy_docs/raw_assets`, uses `os.path.getsize` to filter files strictly greater than 10240 bytes, then opens each survivor in binary mode and reads only the first several bytes to compare against the magic-number prefixes for PDF (`%PDF-`), PNG (`\x89PNG\r\n\x1a\n`), and JPEG (`\xff\xd8\xff`) — do not guess type from the existing (meaningless) extension. Create `/home/user/organized_docs/{pdfs,pngs,jpegs}` with `os.makedirs`, then for each matched file call `os.symlink(absolute_source_path, new_link_path)` where the link name is the original filename plus the correct extension (`.pdf`/`.png`/`.jpg`) — get the absolute source path via `os.path.abspath` or `os.path.realpath`, since a relative symlink target would be wrong once viewed from the link's directory. Finally collect all created symlink absolute paths, sort them alphabetically, and write one per line to `/home/user/organized_docs/summary.txt`. Double-check byte counts: JPEG only needs its first 3 bytes checked, PNG needs 8, PDF needs the literal 5-byte string.
