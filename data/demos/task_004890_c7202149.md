Use pandas and build the pipeline in clearly separated stages, checking intermediate output at each step rather than writing one monolithic transform. First melt the wide CSV (`pd.melt` with `id_vars='user_id'`) into long form with columns user_id/date/feedback, coercing `date` to a proper datetime. Second, for gap-filling: compute the full date range from the actual min to max date present in the columns (`pd.date_range`), then for every user_id reindex against that full range (e.g. groupby + reindex, or a cross join of unique user_ids × full date range merged with a left join back to the melted data) so every user has one row per day in range — fill any resulting NaN/missing feedback (both newly injected gap rows and pre-existing empty/NaN cells) with `""`. Third, only for non-empty feedback: lowercase, strip punctuation with a regex keeping only `[a-z0-9 ]`, collapse repeated spaces (`re.sub(r'\s+',' ',...)`), and strip — apply this before hashing, not after. Fourth, hash with `hashlib.md5(text.encode('utf-8')).hexdigest()`, but special-case empty string to map to `""` directly, never hashing an empty string. Sort the final frame by user_id then date before writing processed_logs.csv, and build hash_mapping.json only from non-empty hashes, deduplicating by hash key.