Build this as separable stages with clear intermediate data, since correctness depends on getting the join/resample/window logic right. First parse `calibrations.log` with a regex capturing timestamp, sensor ID, and multiplier only from lines matching the calibration pattern (e.g. `r'\[INFO\] (\S+) - Sensor \[(\S+)\] calibrated\. New multiplier: ([\d.]+)'`), ignoring non-matching lines. Load `metadata.json` for baselines and `raw_readings.csv` for raw values; for each raw reading, find the calibration multiplier by taking the most recent calibration for that sensor at or before the reading's timestamp (sort calibrations per sensor and binary-search or scan; default to 1.0 if none precedes it), then compute `true_value = raw*mult - baseline`. Next, resample per sensor onto exact 1-minute marks across the full inclusive range using forward-fill (the value at each minute mark equals the most recent true_value at or before that timestamp; the very first mark stays null if nothing precedes it) — don't confuse this with resampling raw timestamps directly, it must land on the strict minute grid. Then compute a rolling window of size 3 (current + 2 prior minutes) with min_periods=1, treating nulls as excluded from the average but not making the whole window null. Wire all of this into a `Makefile`'s default `all` target invoking your script(s), producing the sorted, rounded CSV, then run `make` and inspect the output for sanity before finishing.