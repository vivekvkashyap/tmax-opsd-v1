Work through this as restore -> reconfigure -> rebuild -> verify, testing each piece before chaining them. Extract with `tar -xzf /home/user/archive/multiplexer_backup.tar.gz -C /home/user/restore` (create the target dir first). Edit `config.ini`'s upstream line from `/var/run/backend.sock` to `/home/user/backend_service/app.sock`, and append `backup_operator` as a new line in `allowed_users.db`. Build the mock backend supervisor script with an outer `while true; do ...; done` loop wrapping a `socat UNIX-LISTEN:/home/user/backend_service/app.sock,fork,reuseaddr SYSTEM:'echo RESTORE_VALID'` (or an `nc -lU` equivalent) invocation, so if the listener process ever exits the loop immediately restarts it; run this script backgrounded. For the C fix, open `multiplexer.c`, find `main()`, and insert `setenv("TZ", "UTC", 1);` as the very first statement (before the license check runs) rather than modifying the check itself. Compile with `gcc`, start the multiplexer backgrounded, then use `socat`/`nc` to send `backup_operator:PING` to `127.0.0.1:8080`, capture the raw response bytes, and save them verbatim to `/home/user/restore_success.log`.
