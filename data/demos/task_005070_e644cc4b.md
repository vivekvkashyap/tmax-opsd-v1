Use pandas and follow the pipeline stages in the exact stated order, since each stage's output feeds the next and reordering changes results. First, load the long-format CSV and deduplicate/reshape with `pivot_table(index=['timestamp','ip_address'], columns='metric_name', values='metric_value', aggfunc='max')` (aggfunc='max' handles the duplicate-retry rows), then `reset_index()`. Second, sort by `ip_address` then `timestamp` ascending, and forward-fill missing metric columns per-IP with `groupby('ip_address')[col].ffill()` — do this per group, not globally, or values will leak across IPs. Third, compute the 3-period rolling mean of `cpu_usage` per IP via `groupby('ip_address')['cpu_usage'].transform(lambda s: s.rolling(3).mean())`, which naturally yields NaN until 3 periods exist. Fourth, flag rows where `cpu_usage` is not NaN's rolling mean and `cpu_usage > 1.2 * cpu_rolling_mean` strictly. Fifth, hash `ip_address` with `hashlib.sha256(ip.encode()).hexdigest()` into a new `masked_ip` column. Finally select/reorder exactly the four required columns, round the two numeric ones to 2 decimals, and write with `to_csv(..., index=False)`. Double-check column order and rounding match the spec precisely since the verifier does an exact string/content comparison.