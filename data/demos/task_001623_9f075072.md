Split this into extraction, static analysis, and a hardened validator. Transcribe `/app/incident_report.wav` (with `whisper` or an available ASR tool) to recover the attacker's IP and the HMAC-SHA256 secret key spoken in the recording — use these exact recovered values. Verify `/home/user/vulnerable_app.sh`'s SHA256 against `/home/user/checksums.txt` with `sha256sum -c`, then read the script to pinpoint which check is missing (it trusts the `alg` field and skips signature verification when it says `none`) — look up the CWE catalog for the class of weakness this represents (improper verification of a cryptographic signature / improper authentication) and write the matching identifier to `/home/user/cwe_identified.txt`. Write the iptables drop rule for the extracted IP to `/home/user/block_rule.txt`. For `/home/user/jwt_filter.sh`: split the JWT on `.`, decode the header and reject immediately if `alg` is `none` or not `HS256`; otherwise recompute HMAC-SHA256 over `header.payload` with the recovered secret (e.g. via `openssl dgst -sha256 -hmac`) and compare against the token's signature — print `ACCEPT`/`REJECT`. Test against forged, empty-signature, and `alg:none` tokens plus valid ones before finalizing.