Treat this as three separable subsystems — audio transcription, project reorganization, and a C/C++ ABI boundary — and verify each before moving on. First transcribe `/app/spec_memo.wav` (install and run `openai-whisper`, or another STT tool you can install, plus `ffmpeg` if needed for format conversion); read the transcript carefully since it defines the exact character-shift-and-checksum algorithm — don't assume a standard Caesar cipher, note precisely how wraparound and the checksum format are described. Move the legacy files into `src/`, `include/`, `bin/` under `/home/user/project/`, fixing any missing `extern "C"` guards so C++ symbols get C linkage for the shared library. Implement `encode_data` in `libencoder.so` matching the transcribed spec exactly (character shift amount, wraparound bounds, checksum computation over which byte set, and the exact output separator/format), then write `encoder_cli` to read stdin line-by-line until EOF, call `encode_data` per line, and print results. Since a hidden fuzz-equivalence check will compare byte-for-byte against a reference across many random inputs, test your `encoder_cli` manually against edge cases (empty lines, boundary ASCII values near your wrap point) before considering it done. Write the Makefile so `make` alone produces `bin/encoder_cli`.