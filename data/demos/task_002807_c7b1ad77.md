Start by OCR'ing `/app/compliance_schema.png` (e.g. `tesseract /app/compliance_schema.png stdout`) to recover the exact non-compliance rule set — do not assume or invent criteria, read them from the image text. Then inspect each data source's actual schema before writing joins: use `sqlite3`/`sqlite3 module` to inspect `users.db` table/column names, `jq`/manual JSON parsing to see `profiles.jsonl` fields (e.g. a `tags` array), and open `activity.ttl` to see the predicate/namespace vocabulary (e.g. `ex:` prefix, `ex:ACCESSED`, `ex:hasType`) so your SPARQL query (via `rdflib`) uses the real predicate names. Build three separate ID sets — one from a SQL query matching the DB-side condition, one from filtering JSONL records for the required tag, one from a SPARQL query for the graph-side condition — then intersect all three sets as the final answer, since the rule requires ALL criteria (AND, not OR) with `user_id` as the join key across systems (note the graph nodes are named like `ex:User_<user_id>`, so you need to strip that prefix when joining). Write the intersected IDs, one per line, to `/home/user/non_compliant.txt`. Since scoring is F1 against a hidden ground truth (>=0.95 required), double check your join key extraction/formatting (e.g. no stray whitespace, consistent ID string form) rather than guessing — mismatched ID formats across the three systems is the most common way to silently fail here.