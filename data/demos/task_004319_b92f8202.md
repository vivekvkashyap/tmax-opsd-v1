Build this as a straightforward graph pipeline rather than hand-rolling algorithms: load both CSVs with `pandas` (or plain `csv`), then construct the directed graph (e.g. with `networkx.DiGraph`) only after filtering — drop nodes whose `is_active` is `False` first, then drop edges touching removed nodes, then separately drop any edge with `edge_type == 'maintenance'`. The key trap is filtering order: if you filter edges before nodes, or forget that removing a node must also remove its incident edges, you'll compute shortest paths over a graph that includes a decoy low-weight path through a maintenance-only or inactive-node route. Use node `name` as the lookup key (build a name<->node_id mapping) since inputs/outputs work in names but the graph can be keyed by either as long as you translate correctly. For the shortest path, use a weighted algorithm (e.g. `networkx.shortest_path(G, source, target, weight='weight')` plus `dijkstra_path_length` for total weight) and write `path`/`total_weight` to `/home/user/report.json` exactly as specified. For the top-5 nearest reachable nodes from 'Alpha', compute single-source shortest-path distances (e.g. `nx.single_source_dijkstra_path_length`), exclude 'Alpha', sort by (distance, node_name), take the first 5, and write to `/home/user/top_destinations.csv` with headers `node_name,distance`.