Build this as a single pipeline of standard text tools, careful with locale/Unicode. Concatenate both log files (`cat /home/user/server_A/app.log /home/user/server_B/app.log`), then use `grep -E '\[(ERROR|CRITICAL)\]'` to keep only the relevant levels. Use `awk` (with `LANG=en_US.UTF-8` or `LC_ALL=C.UTF-8` set so multi-byte characters aren't mangled) to: extract the timestamp's first 13 characters and replace the `T` with `_` to get the hour bucket (`YYYY-MM-DD_HH`), and extract everything after the `] ` following the level tag as the message (strip only leading spaces, keep the rest of the line verbatim including non-ASCII text). Pipe through `sort | uniq -c` (or an `awk` associative array keyed on `hour SUBSEP message`) to get counts per (Hour, Message) pair. Reformat into CSV `Hour,Message,Count`, then sort with `sort -t, -k1,1 -k3,3nr -k2,2` to get ascending hour, descending count, then alphabetical message as tiebreaker — note field 3 needs numeric-reverse (`-k3,3nr`) not lexicographic. Write the header line first, then the sorted rows, to `/home/user/error_summary.csv`.