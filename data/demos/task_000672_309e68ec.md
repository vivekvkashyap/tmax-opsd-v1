Parse the `.pkg` binary format exactly as specified: read 1 byte for N, then for each entry read 1 byte path-length, that many path bytes, a little-endian u32 payload length (`u32::from_le_bytes`), then the payload bytes. For zip-slip mitigation, don't just string-match `../` — normalize/resolve the joined target path (e.g. build the destination as `extracted_configs.join(path)`, canonicalize its parent after creating dirs, or manually reject any path component equal to `..` before joining) and skip the entry entirely if it would land outside the target directory; also reject absolute paths. Convert each surviving path's `.csv` extension to `.json`, and parse the CSV payload as strictly 2-column `key,value` lines into a flat JSON object (build a `serde_json`-style map or hand-roll simple JSON string escaping). For atomic writes: write the JSON bytes to `<target>.tmp`, then call `std::fs::rename` from the tmp path to the final path (rename is atomic on the same filesystem) — writing directly to the final path defeats the requirement. Create parent directories with `create_dir_all` only for accepted (non-traversal) entries, then compile with `rustc` and run against `/home/user/configs.pkg`.
