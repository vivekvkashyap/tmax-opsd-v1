Read the broken `transformer.sh` first to see what it's missing (it currently only does a flat copy, `cp /home/user/docs/src/* /home/user/docs/out/`, with no renaming or macro substitution), then rewrite it to, for each file in the src directory: determine the output name — if it matches `draft_*.md`, rename the prefix `draft_` to `pub_` and the extension `.md` to `.html`; otherwise keep the original name — then apply text substitution of the literal macros `%%STATUS%%` -> `PUBLISHED` and `%%DATE%%` -> `2024` (e.g. via `sed` on a copy, being careful sed's `%%` isn't a special sed delimiter conflict, or a per-file loop with `sed -e 's/%%STATUS%%/PUBLISHED/g' -e 's/%%DATE%%/2024/g'`) and write the result to the corresponding path in `/home/user/docs/out/`. Test the script standalone first (drop a sample `draft_x.md` in src, run the script directly, check `out/pub_x.html`) before trusting the watcher loop. Then start `/app/watcher.py` and `/app/server.py` in the background (e.g. `nohup python3 /app/watcher.py & nohup python3 /app/server.py &`) so both keep running after your session ends, and verify end-to-end with `curl` against `127.0.0.1:8080` after creating a new draft file and waiting briefly for the watcher's poll interval.
