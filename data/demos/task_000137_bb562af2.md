Work in strict order and re-read state fresh at each step rather than caching values. First, source/parse `monitor.conf` to get `DEPLOY_BASE`, `CURRENT_LINK`, `ARCHIVE_DIR` (e.g. `source` it in bash, or grep/cut each `KEY=value` line). Use `readlink` (or `readlink -f`) on `CURRENT_LINK` to get the currently active deployment directory, and extract just its basename as the "current version". Read `<active_dir>/health.txt`; only if its trimmed content is exactly `DEGRADED` do you touch the symlink. For rollback, load `history.txt` into an ordered list, find the index of the current version, and pick the entry immediately before it (not just "the previous line in the file" blindly — match by name). Re-point `CURRENT_LINK` to that directory (remove and recreate the symlink, don't leave a stale one or nest symlinks). Only after the symlink is finalized, re-resolve it again to know the truly active directory before checking `app.log` for the substring `ERROR` — do not check the pre-rollback log. If found, move (not copy) the log to `ARCHIVE_DIR/<version>_app.log.archive` using the post-rollback version name, then create a fresh empty `app.log` in the active dir. Finally, execute the script once so the described end state actually exists on disk.
