Build the script to match the exact I/O contract, then test it manually with piped input before considering it done. Use `input("Enter username: ")` so the prompt text is emitted without a trailing newline issue, and use `shutil.disk_usage("/home/user/tenants")` to get free bytes on that partition — compare directly against `100 * 1024 * 1024` and, on insufficiency, print the exact error string and `sys.exit(1)` immediately (don't create any directories first). On the success path, use `os.makedirs` with `exist_ok=True` for both the `data` and `logs` subdirectories, write the literal string `10GB` (no extra whitespace/newline requirements beyond what's stated) to the `.quota` file, and append `Provisioned <username>\n` to `/home/user/provision_log.txt` using append mode (`"a"`) so repeated runs accumulate rather than clobber. Finally print `Success` and exit 0. Test end-to-end yourself with `mkdir -p /home/user/tenants && echo testuser | python3 /home/user/provision.py` and inspect the created directories, `.quota` contents, and log file to confirm exact formatting before finishing.
