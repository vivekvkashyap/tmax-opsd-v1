Begin by OCRing the policy image with `tesseract /app/network_policy.png stdout` to recover the exact blocked subnet/keyword string rather than assuming its format — pay attention to whether it's a full IP, a prefix, or a quoted variable assignment, since that determines your matching logic. Write `/home/user/validate_config.py` to read the target file's contents and perform a substring (or prefix/regex, if the recovered value is a network prefix) match against the recovered blocked value, exiting 1 if found and 0 otherwise; before finalizing, run it against every file in both `/app/clean/` and `/app/evil/` and confirm it produces the correct exit code on all of them — a too-loose or too-strict match pattern is the most common way this fails to generalize to the hidden corpus. For the git hook, `git init --bare /home/user/provisioning.git`, then create `hooks/pre-receive` as an executable bash script that sets `STRICT_MODE=1` and invokes your validator (e.g. via `python3 /home/user/validate_config.py <path>` on relevant changed files) — full pre-receive wiring isn't required, just that the script exists, is executable, and calls your validator correctly.
