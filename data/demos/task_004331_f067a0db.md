Treat this as three separate deliverables verified independently, and get the plumbing right before optimizing. First, write `supervisord.conf` with `[program:...]` blocks for all three processes (web_server.py, git_auditor.py, rust_monitor binary) each with `autorestart=true`, correct `command=`/`directory=` paths, and a `[unix_http_server]`/`[supervisorctl]` section usable without root (use a user-writable socket path). Verify the auditor actually listens on 8080 and commits POSTed alert payloads into the bare repo at `/home/user/git/alerts.git` (test with a manual `curl` POST and `git log` before relying on the pipeline). For logrotate, set `daily`, `rotate 7`, `compress`, and `create 0644` explicitly in the stanza. For the Rust optimization, first identify why the current tailer/parser is slow (usually O(n) reallocation, syncronous per-line I/O overhead, or repeated regex compilation) — fix by reading the file in one pass with buffered I/O and a cheap string check instead of a full regex per line, then wrap only the parsing loop in `Instant::now()` timing and write just the millisecond number to `/home/user/app/metrics.txt`. Compile with `cargo build --release` and re-run the timing to confirm it's comfortably under the threshold before wiring it into supervisord.
