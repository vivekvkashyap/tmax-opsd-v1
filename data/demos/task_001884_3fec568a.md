Recover the binary format spec from the image first — run OCR (`tesseract /app/schema.png stdout`) and read off the exact magic-byte sequence and the evil-signature byte sequence it specifies, then convert those hex byte lists to their ASCII string form yourself (don't hardcode assumed values). Then implement `/home/user/filter.go` (invoked as `go run filter.go <input_dir> <output_dir>`) to, for each file in the input directory:
1. Reject it if its leading bytes don't match the recovered magic bytes.
2. Reject it if the recovered evil-signature byte sequence appears anywhere in the file contents (scan the whole file, not just the prefix).
3. Otherwise, strip the magic-byte prefix, treat the remainder as UTF-8 text, and marshal it into `{"config_data": "<payload>"}` JSON.
4. Write atomically: write to a temp file in the output directory first, then `os.Rename` it to `<output_dir>/<original_filename>.json` — a partial/non-atomic write is a common failure mode under concurrent/fuzzed testing.
Validate your implementation yourself against `/home/user/corpus/clean/` and `/home/user/corpus/evil/`, confirming 100% of clean files produce output and 100% of evil files produce none, before considering it done.
