First inspect the schema with `sqlite3 /home/user/knowledge_graph.db ".schema"` (or `PRAGMA table_info` per table) to learn the real table/column names for users, skills, user_skills, projects, and project_members — do not assume names. Then write `/home/user/query_optimizer.py` to accept `skill_name`, `limit`, `offset` from `sys.argv`, and build a single parameterized SQL query (using `?` placeholders, never string formatting, to avoid injection) that: self-joins project_members on project_id to find co-membership pairs, joins user_skills twice (once per user) filtered to the given skill, enforces `userA.name < userB.name` to dedupe symmetric pairs and drop self-pairs, uses `COUNT(DISTINCT project_id)` grouped by the pair for the shared-project count, and orders by that count DESC, then user_a ASC, then user_b ASC, applying `LIMIT ?/OFFSET ?` at the SQL level rather than slicing in Python. Execute the query, fetch rows, and dump them to `/home/user/optimized_results.json` as a JSON array of objects with exactly the keys `user_a`, `user_b`, `shared_projects_count` in that field order/naming.
