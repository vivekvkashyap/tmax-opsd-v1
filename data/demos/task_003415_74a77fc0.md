Stand up the infrastructure first and confirm each piece independently: start `redis-server --daemonize yes` and check `redis-cli ping`; launch the Flask app in `/app/services/metadata_api.py` in the background and curl `/get_name?hash=...&ext=...` once to see its actual response shape before writing code against it. For the file classification, read only the first several bytes of each file (not the extension) and match against the exact magic-number byte sequences given for PNG/JPG/PDF; anything else is skipped entirely (not moved, not in the manifest rewrite). Hash file contents with SHA-256 for dedup identity. Structure your worker pool (e.g. `multiprocessing.Pool` or `concurrent.futures`) so each worker, before copying a unique file into `/home/user/organized/<ext>/`, acquires a Redis lock via `SET lock:<hash> 1 NX EX <ttl>` — only the lock-winner performs the copy; others just reuse the already-computed canonical path. Rewrite `manifest.txt` line-by-line: swap `<old_relative_path>` for the new absolute organized path only for classified files, leaving unknown-type lines untouched. Finally sanity-check a handful of manifest lines against the actual files on disk before trusting `/app/verify.py`'s score.
