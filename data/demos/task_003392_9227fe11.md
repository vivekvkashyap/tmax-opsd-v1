Treat this as three separable subtasks and validate each before moving on. (1) For `pytest` failures: read `processor.py` to find where multiple threads write `/home/user/math_pipeline/metrics.json` concurrently without synchronization — fix with a lock (e.g. `threading.Lock` around the read-modify-write, or file locking via `fcntl.flock` if it's cross-process) so writes can't interleave/corrupt the file; separately, read `wav_parser.py`'s fmt-chunk parsing — it likely assumes a fixed 16-byte 'fmt ' chunk and fails to skip the 2 extra bytes some WAV files include (chunk size 18) — fix it to read the chunk's declared size and skip any trailing bytes rather than assuming a fixed length. Rerun `pytest` until it passes reliably (rerun several times to catch flakiness). (2) For the sanitizer, read consecutive 16-bit PCM samples and flag files where any adjacent pair's absolute difference exceeds 20000; exit 0/1 accordingly, and test against both corpora directories. (3) For the mystery constant, actually transcribe `/app/mystery_signal.wav` (e.g. via a local speech-to-text tool or `ffmpeg` plus a STT library) — listen to what number is spoken, don't guess a famous constant, and write only its digits to the output file.
