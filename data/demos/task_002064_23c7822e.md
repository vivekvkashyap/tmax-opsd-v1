Treat this as: dump flat data -> load into sqlite -> recursive CTE -> JSON. In `/home/user/etl.sh`, pipe `/app/fetch_sales_graph` output straight into `sqlite3` (e.g. via a heredoc or `.import`/`.mode csv` with `|` as separator) into a table `employees(employee_id, manager_id, direct_sales_amount)`; treat an empty-string manager_id as the root (no parent) — don't let it accidentally self-join or get excluded. Write a `WITH RECURSIVE` CTE that, for each employee, walks all descendants (base case: the employee itself; recursive case: join children via `manager_id = employee_id`), summing `direct_sales_amount` across the employee and every descendant. Because the tree can be large (tens of thousands of nodes), make sure the recursive query is written so SQLite can execute it efficiently rather than doing per-node subqueries in a loop — a single set-based recursive CTE over the whole table is the right shape. Finally use `sqlite3 -json` or pipe rows through `jq`/`awk` to emit the exact `[{"employee_id":...,"total_rollup_sales":...}, ...]` array, preserving full float precision. Test on a small synthetic 3-level tree by hand before trusting it on the full dataset, and confirm every employee_id from the raw dump appears exactly once in your output.