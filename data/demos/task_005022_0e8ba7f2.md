Decode with `base64 -d /home/user/audit_log.b64` to see the raw log text first, so you know exactly what patterns you're matching against. In your Rust program, use a regex crate (or hand-rolled parser) to match IPv4-shaped tokens (four dot-separated 1-3 digit groups) and replace each with the literal string `[REDACTED_IP]`, being careful to only replace whole IP-looking tokens and not partial digit sequences. Scan the same decoded text for the literal substrings `sudo ` (note trailing space) and `chmod 777` — a match of either anywhere sets a flag; append the warning line only if the flag is set. Preserve line ordering and original line content exactly (only the IP substitution changes anything) when writing `/home/user/processed_audit.log`. Compile with `rustc /home/user/policy_checker.rs -o policy_checker` and run it before doing the final step. Finally, append the certificate subject as the very last line using `openssl x509 -in /home/user/server.crt -noout -subject >> /home/user/processed_audit.log` — do this with a shell append, not inside the Rust program, since it's a separate bash step per the task.
