Build this in the three phases the task specifies, and get the exact output format right since verification parses `test_report.log` line by line. Phase 1: write `server.js` using the `ws` package to listen on port 8765, parse each incoming JSON message's `version` field, and classify it with the npm `semver` package — `semver.valid(version)` to check validity, then `semver.gt(version, '2.0.0')` for success vs rejected, falling back to an `{"status":"error"}` reply for anything invalid, missing, or non-string. Phase 2: write `test.py` using the `websockets` library to connect, generate exactly 13 versions (5 valid and > 2.0.0, 5 valid and <= 2.0.0, 3 deliberately malformed strings like `not-a-version` or `2.0.0.1`), send each sequentially, and append `<version>,<status>` lines with no extra whitespace to `test_report.log`. Phase 3: write `run_tests.sh` that sets up `node_modules` (`npm install ws semver`), creates/activates a Python venv installing `websockets` (and `packaging` if you use it for local sanity checks), starts `node server.js` in the background, polls/sleeps briefly until the port is accepting connections, runs `test.py`, then kills the server process on exit — make the script executable and actually run it to produce the log.
