Approach this as an extract-then-serve pipeline. First, extract one frame per second from `/app/cctv_topology.mp4` (e.g. `ffmpeg -i cctv_topology.mp4 -vf fps=1 frame_%02d.png`), then OCR each frame with `tesseract` to recover the `ZONE_A <-> ZONE_B` text — inspect the OCR output carefully and clean up whitespace/typos before trusting it, since OCR errors will silently corrupt your graph. Parse each line into a pair of zone names and load them into the `doors` table in `/home/user/audit.db`; since edges are bidirectional, either insert both `(a,b)` and `(b,a)` rows or make your path query treat the table as undirected, and add an index on `zone1` (and `zone2`) to speed lookups. For `path_check.py`, implement BFS (not DFS) over the graph built from the table — BFS guarantees shortest path — and when multiple neighbors tie, iterate them in a deterministic (e.g. sorted) order so your output is reproducible under fuzzing. Print exactly the comma-separated path or `NO_PATH`, with absolutely no other stdout output (no debug prints, no logging), since the verifier diffs raw stdout against a hidden oracle across many random zone pairs.