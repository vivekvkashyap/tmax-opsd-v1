Work the three failure modes independently and verify each before moving on. For the Makefile, the runtime loader error means the binary has no way to find `libs/` at runtime without `LD_LIBRARY_PATH`; add an rpath linker flag (`-Wl,-rpath,'$ORIGIN/libs'` or an absolute path to `libs/`) alongside `-L./libs -lalpha -lbeta`, then confirm with `readelf -d app | grep -i rpath` (or RUNPATH) that it actually landed — a build that "just runs because LD_LIBRARY_PATH happens to be set in your shell" will fail verification. For the memory-safety bug, the vulnerability is fixed-size stack buffers being `strcpy`'d from arbitrary-length input; don't just enlarge the buffers (still unsafe) — parse the version strings directly with `sscanf(v1, "%d.%d.%d", ...)` on the original pointers, no intermediate copy needed, and compare major/minor/patch in order. For the test script, use `subprocess.run([...]).returncode` for each of the three invocations and assert against the required exit codes before printing the success marker. Finally run the built `app` with no args and redirect stdout to `success.log`.
