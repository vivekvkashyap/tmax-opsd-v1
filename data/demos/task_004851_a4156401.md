First fix the vendored build: inspect `/app/sqlite-amalgamation/Makefile` closely for the `sqlite3.o` rule — the perturbation is a filename typo where the target depends on `sqlite3.c` but the recipe compiles a different, non-existent source name; correct the compile command to reference the actual `sqlite3.c` file, then run `make` to confirm `sqlite3.o` is produced. Second, write `backup_validator.c` using the SQLite C API (`sqlite3_open`, `sqlite3_prepare_v2`, `sqlite3_step`) to look up the row with `id = 100`, then repeatedly follow `parent_id` upward, maintaining a visited-IDs set (array or hash) — if you ever revisit an already-seen id before reaching a row with `parent_id IS NULL`, that's a cycle; a naive traversal without a visited-set will infinite-loop on cyclic corpora instead of detecting them. Print exactly `ACCEPT`/exit 0 for a clean path to root, `REJECT`/exit 1 for a detected cycle. Compile with something like `gcc backup_validator.c /app/sqlite-amalgamation/sqlite3.o -I/app/sqlite-amalgamation -lpthread -ldl -lm -o /home/user/backup_validator`. Test it manually against a couple of files in `/app/corpora/clean/` and `/app/corpora/evil/` before considering it done.
