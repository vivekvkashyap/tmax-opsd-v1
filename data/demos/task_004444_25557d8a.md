Parse `protein.pdb` using fixed-width column slicing per the PDB spec (don't split on whitespace, since fields can run together) — the atom name field and the B-factor field are at specific fixed column ranges, so slice by character position and filter to rows where the atom-name field, after stripping, equals exactly `CA`. Implement Metropolis-Hastings from scratch: maintain current `(mu, sigma)`, propose a new state by adding small Gaussian jitter to each (tune the jitter scale so acceptance rate isn't near 0% or 100%), compute the log-likelihood of all B-factor samples under `Normal(mu, sigma)` plus the uniform prior (reject proposals outside `[0,100]`/`[0.1,30]` outright), and accept/reject via the standard Metropolis ratio using only `random`/`math` from the standard library (no `numpy`/`scipy`). Run 20,000 iterations, discard the first 5,000 as burn-in, and average the remaining `mu`/`sigma` samples. Sanity-check convergence by comparing your posterior mean against the simple empirical mean/stdev of your extracted CA B-factors — they should agree closely with a flat prior and this much data. Write results to `/home/user/posterior_stats.txt` rounded to one decimal in the exact specified two-line format.
