Diagnose before rewriting: run the existing `transform.sh` against `traffic.pcap` and diff its output against `expected.csv` to see precisely which fields are wrong — this tells you if the bug is field-splitting or protocol detection. Inspect raw `tcpdump -nn -q -r traffic.pcap` output directly (not through the buggy awk) to see the true token layout of `src`/`dst`; both include a trailing `.port`, so stripping it means removing only the last dot-separated segment, not blindly assuming a fixed format — a naive `gsub` on the wrong field is the classic mistake. Fix `awk` in `transform.sh` to reliably strip the trailing port from both addresses and to correctly classify protocol for both TCP and UDP lines (check what token tcpdump actually emits for each, since UDP/TCP lines can differ in column layout). For `test.sh`, capture `./transform.sh traffic.pcap` to a temp file, run `diff -u expected.csv <that file>` into `/home/user/diff.log`, and exit 0/1 based on whether diff produced output. Make both scripts executable and iterate until `./test.sh` exits 0 with an empty diff log.