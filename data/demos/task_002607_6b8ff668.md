Do not call the `/app/config_hasher` binary per record — with tens of thousands of records that subprocess overhead will blow the 5-second budget. Instead reverse-engineer its algorithm: run it on a handful of known short strings (single characters, then short combinations) and compare outputs to common hash constructions (DJB2-style shift/xor patterns are typical for these); alternatively, if `strings`/`objdump -d` is available, disassemble the stripped binary to read the accumulation loop directly and infer the seed constant, the per-character operation (shift-and-add or multiply, then some form of XOR with the char code), and the bit-width mask. Verify your reimplementation against the real binary's output on several test strings — including edge cases like empty string, unicode, and repeated chars — before trusting it at scale. Then in `process.py`, normalize timestamps from both CSV (parse "YYYY-MM-DD HH:MM:SS" as UTC) and JSON (epoch seconds) to ISO8601 UTC strings, bucket by the top of the hour, dedupe within a bucket by (server,val) keeping the earliest timestamp, hash each unique config_val with your fast native function, and write the bucketed, alphabetically-sorted signature lists to `/home/user/output/aggregated.json`.