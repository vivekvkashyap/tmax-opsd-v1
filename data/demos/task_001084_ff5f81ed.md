Do this as a pipeline of filters, verifying each stage's output before chaining the next. First run checksum verification with `cd /home/user/legacy_project && md5sum -c checksums.md5 2>/dev/null` and capture only the lines ending in `: OK` (grep for `OK$`) to get the set of files that pass integrity checking — don't skip this step or assume all files are valid, since one file is deliberately corrupted. From that OK-set, extract just the filenames (strip the trailing `: OK`). Then, for each surviving file, test it against the exact extended regex `^[[:space:]]*print[[:space:]]+["']` using `grep -lE` (a naive `grep print` would over-match Python 3's `print(...)` calls, so the regex must specifically require print followed by whitespace then directly a quote, not a paren) — apply it only to files that already passed the checksum, not the full source tree. Combine both filters (checksum OK AND regex match) using something like `comm` or by iterating the checksum-OK list and grep-testing each one individually. Sort the final relative paths alphabetically and write one per line to `/home/user/migration_targets.txt`, without modifying any source or checksum files.