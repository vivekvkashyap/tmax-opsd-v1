Treat this as two independent fixes plus one integration test. For Part 1, inspect the vendored package at `/app/vendor/sqlite_bulk_loader-1.0.0`: run `make install` and read the actual error — a common injected bug is that the Makefile's recipe lines use spaces instead of tabs (Make requires literal tabs for recipe indentation), so fix the indentation with a real tab character; then check `load_csv.sh` for a hardcoded wrong `DB_PATH` (e.g. pointing outside `/home/user` or to a permission-denied location) and change it to `/home/user/metrics.db`. Verify the fix by actually running `./load_csv.sh` against a small test CSV and confirming the sqlite file is created with rows in it. For Part 2, write `/home/user/sanitize_logs` (Python is easiest) that reads stdin line by line: first regex-scan the raw line for ``, ``, or a literal ESC byte and drop if found; then try `json.loads`, dropping on failure; then parse `timestamp` handling both epoch numbers and ISO8601 (with/without offset) via `datetime`, converting to UTC and formatting as `YYYY-MM-DDTHH:MM:SSZ`, dropping if unparseable; finally emit CSV `timestamp,user_id,action,message` with no header. Test against small clean/evil samples you construct yourself, and chmod +x it before finishing.