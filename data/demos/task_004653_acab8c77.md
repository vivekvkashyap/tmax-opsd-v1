First unblock the build: inspect `/app/fast-covar-1.2.0/setup.py` for a platform check that unconditionally raises on Linux (look for `sys.platform` comparisons), remove or neutralize that guard, then `pip install -e /app/fast-covar-1.2.0` and confirm `import fast_covar; fast_covar.compute_cov(...)` works on a small array before writing any detection logic. For the detector, load `train.csv`/`test.csv`, drop `id`/`target`, keep only `feature_0..feature_9`, drop null rows, and convert to numpy float arrays. Compute `cov_train = fast_covar.compute_cov(train_matrix)` and `cov_concat = fast_covar.compute_cov(np.vstack([train_matrix, test_matrix]))`, then measure closeness to the identity matrix via the Frobenius norm `np.linalg.norm(cov - np.eye(n))` with tolerance 1e-4. The key discriminator: clean data has train-alone covariance near identity; leaky data has the concatenated covariance near identity but train-alone covariance far from it — so check train's closeness first, and if it's not near-identity but concat's is, classify EVIL; exit 0 for CLEAN, exit 1 for EVIL. Validate against every subdirectory in both `/home/user/data/clean/` and `/home/user/data/evil/` before finishing.
