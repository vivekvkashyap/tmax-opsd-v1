Treat this as a black-box behavioral-cloning task: since source is lost, infer the exact semantics of the oracle by feeding it many crafted stdin inputs and observing stdout byte-for-byte, rather than guessing from the prose spec alone. Work through the spec carefully: parse each tab-separated line (timestamp, key, hex value), hex-decode the value to ASCII and lowercase it, then maintain a per-run history of (timestamp, key) events and count, for the current line's key, how many recorded events (including itself) have timestamp >= current_timestamp - 60 and <= current_timestamp (an inclusive trailing 60-second window over ALL keys' history, filtered by matching key). Write your C implementation in `/home/user/tracker.c`, streaming line-by-line with a growable/dynamic history structure (don't hardcode a small fixed-size array that could silently drop events on long streams). Pay special attention to edge cases: multi-digit vs single-digit hex bytes, empty values, lines with malformed field counts, and very large inputs (thousands of lines) — test with your own generated inputs of varying key pools, timestamp deltas, and value lengths before considering it done. Compile exactly as specified and confirm stdout matches expectations for constructed edge cases.
