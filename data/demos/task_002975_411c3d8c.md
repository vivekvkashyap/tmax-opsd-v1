First diagnose the encoding: run `file /home/user/telemetry_logs/raw_data.log` to confirm it's not UTF-8 (legacy Windows systems commonly emit UTF-16LE), then convert it in the script with `iconv -f UTF-16LE -t UTF-8` before piping into `jq` — `jq`/`awk` will silently mis-tokenize or error on raw UTF-16 bytes. Second, fix the precision loss: standard `awk` sums using IEEE double floats, which cannot represent integers beyond 2^53 exactly, so instead extract each `bytes` value as a decimal string (e.g. via `jq -r '.bytes'`) and accumulate the sum with an arbitrary-precision tool like `bc` or `python3`'s integer arithmetic rather than `awk`'s `+=`. For the delta-debugging part, stream through the log in order (preserving line order after decoding/conversion) and, using exact big-integer comparison (not float comparison, which would falsely flag/miss boundary values), find the first record whose `bytes` is strictly greater than 9007199254740992, then write its `tx_id` — don't rely on sorting, since order must stay chronological as given in the file.