Write the C++ parser as a streaming state machine over the binary file: read 4 bytes and verify "ARTF", read the next 12 bytes raw as the artifact ID (do not treat it as a null-terminated C string), read 4 bytes and interpret as a little-endian uint32_t (use memcpy or a byte-shift reconstruction, not a naive cast that assumes host endianness/alignment), then read exactly that many payload bytes, and finally verify the 4-byte "DONE" footer before advancing to the next record. XOR each payload byte with 0x42 while writing it out. For the atomic-write requirement, open/write to `<id>.tmp` in `/home/user/extracted/`, close it, then call `rename()` (or `std::filesystem::rename`) to move it to `<id>.bin` — a naive approach that writes directly to the final filename or forgets to close the file handle before renaming breaks the atomicity guarantee. After extraction, use `grep -l "CONFIRMED" /home/user/extracted/*.bin` (or similar) to find matches, extract just the basenames without `.bin`, sort them, and redirect to `/home/user/confirmed.txt`. Watch for false matches from IDs like partial substrings — use exact string search, not fuzzy matching.