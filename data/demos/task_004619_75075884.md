Start with OCR: run `tesseract /app/legacy_schema.png stdout` (try a couple of `--psm` values if the first pass is garbled) and read the recovered text carefully — it states an exact substring-based constraint tied to the `target_version` and `pre_deploy_hook` fields; encode that literal condition faithfully in your validation logic rather than paraphrasing it, since the graders will send payloads crafted to probe the exact rule. Scaffold the Rust service with `cargo new`, add a web framework (`axum` is simplest) plus `serde`/`serde_json` for deserializing the manifest struct, and bind the router to `127.0.0.1:3030`. In the `/validate` handler: reject malformed/non-JSON bodies and empty `services` arrays with 400, apply the OCR-derived rule, and return 200 only when every check passes. Write unit tests that specifically target malicious `pre_deploy_hook` strings (injection attempts, command chaining) to convince yourself the check isn't easily bypassed by whitespace/case tricks before wiring it into the handler. Build in release mode, launch the server backgrounded (e.g. `nohup ... &` or a systemd-less `disown`), poll `curl` against the endpoint until it responds, then write "DONE" to `/home/user/status.txt`.
