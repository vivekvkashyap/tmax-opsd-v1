Separate the two problems and solve them independently. First, recover the circuit definition from `/app/circuit_def.png` by actually viewing/OCR-ing the image (e.g. `tesseract` or opening it visually) — it's a small textual list of node assignments (INPUTS and a chain of ADD/MUL/SUB ops referencing prior nodes); transcribe it exactly rather than assuming a formula, since a fuzz-equivalence check against many random integer inputs (including negatives) will expose any transcription or operator-precedence mistake. Second, fix the AST memory bugs: give the `OpNode` a destructor that deletes its children (recursive cleanup), remove the intentional dangling-pointer construction, and use `valgrind --leak-check=full` on a small run to confirm zero leaks and no invalid reads before moving on. Then discard the placeholder dummy-circuit construction in `main()` and build the real AST per the transcribed graph, wiring X/Y/Z as leaf `ValueNode`s. Compile to `/home/user/evaluator`, and self-check by comparing outputs across several hand-picked (X,Y,Z) triples worked out by hand from your transcribed formula before considering it done.
