For the memory leak, first confirm it exists (e.g. with `tracemalloc`, `objgraph`, or `gc.collect()` + checking `gc.garbage`/refcounts) rather than guessing — the standard cause in a DAG-with-back-pointers design is a parent↔child reference cycle that CPython's refcounting alone can't free promptly. Fix it by making one direction of the reference (typically the "dependents"/back-pointer list) use `weakref.ref` or `weakref.WeakSet` instead of a strong reference, without changing `BuildGraph`'s public API — deref weakrefs safely wherever the back-pointers are traversed (they may return `None` if the referent was collected). Document the change and why cycles caused leaks in the log file. For the proto file, write it exactly to the given message/service/field spec and compile with `python3 -m grpc_tools.protoc` to generate the `_pb2.py`/`_pb2_grpc.py` stubs into `/home/user/`. In `server.py`, run the gRPC server (`grpc.aio` or a thread) and the `websockets` server concurrently via `asyncio.gather`; have `Resolve` and the websocket handler both call `BuildGraph.get_build_order()`, streaming steps with the required 0.1s delay. Start it as a background daemon, record its PID, and test both endpoints yourself before finishing.
