Part 1: extract frames from the mp4 with `ffmpeg` (e.g. one PNG per frame), then for each frame in order load it (PIL/numpy) and compute the fraction of pixels that are exactly `[255,255,255]`; walk frames sequentially and stop at the first one where that fraction is strictly greater than 5% — write that 0-indexed frame number to the output file. Don't eyeball a handful of frames; check every frame programmatically since the explosion point isn't visually obvious. Part 2: implement Welford's online algorithm in `stable_var.py` — iterate the CLI floats once, maintaining running `n`, `mean`, and `M2` (`delta = x - mean; mean += delta/n; M2 += delta*(x-mean)`), then compute sample variance as `M2/(n-1)` (Bessel's correction), never the naive sum-of-squares formula which loses precision. Print with `f"{var:.6f}"`, and print `0.000000` if fewer than 2 args are given. Fuzz-test your own script against edge cases (empty, one value, negatives, mixed magnitudes) before trusting it — the grader compares byte-for-byte against a reference across hundreds of random inputs.