This is a targeted single-bug fix, not a rewrite — locate exactly where the silent corruption happens. In `etl_processor.c`, the two tokens for `val1`/`val2` are passed straight to `atoi()`, which returns 0 for any non-numeric string including `"NA"`, indistinguishable from a real 0. Before calling `atoi()` on each token, add an explicit check with `strcmp(token, "NA") == 0`; if either token is `"NA"`, print `%s,NA\n` for that row instead of computing a product, otherwise multiply the two parsed ints and print `%s,%d\n` as before. Keep the header-skip behavior — either skip the first `fgets` before the loop in C, or pipe with `tail -n +2 dataset.csv | ./etl_processor` so the header line is never parsed as data. Compile with `gcc etl_processor.c -o etl_processor`, run it against `/home/user/dataset.csv` (with header skipped), redirect stdout to `/home/user/processed.csv`, and confirm the line count equals the data-row count (total lines minus 1).