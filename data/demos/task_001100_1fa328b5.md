For the audio step, transcribe `/app/audit_interview.wav` (listen to it or use a speech-to-text tool) to recover the spoken SHA-256 hash as digits/letters spelled out in words — carefully convert the spoken hex string to its exact 64-character lowercase hex form, then verify it against `sha256sum /app/logs.tar.gz` before writing it to `/home/user/archive_hash.txt` (write only the bare hex string, no extra text). For the filter script, process input line-by-line in bash: first check case-insensitively for any of the banned substrings (`<script>`, `javascript:`, `onerror=`, `' OR 1=1`, `UNION SELECT`) using something like `grep -iE` on each line and skip/drop matching lines entirely; for lines that pass, apply an SSN regex substitution (`\b[0-9]{3}-[0-9]{2}-[0-9]{4}\b` → `XXX-XX-XXXX`) with `sed -E`; otherwise print the line unchanged. Be careful about ordering (check injection patterns before redaction, since a dropped line shouldn't also be redacted) and about exact-match case-insensitivity for the SQLi/XSS patterns. Test your script manually against sample malicious and clean lines before relying on the adversarial corpus grading.