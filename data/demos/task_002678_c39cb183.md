Work through the vendored package fixes one compile error/behavioral bug at a time rather than rewriting from scratch. Run `make` first to see the actual linker error — a missing `-pthread` in `CXXFLAGS`/`LDFLAGS` typically manifests as undefined-reference errors for `std::thread`/`pthread_create` symbols; add it and rebuild. Then grep `main.cpp` for the `getenv` call locating the data directory — look for a call to a name that's off by a subtle typo (missing/transposed letter) versus the `TARGET_DIR` the task specifies, and fix the string literal to match exactly. Recompile with `make` until clean. For the mount step, since you lack root, use `archivemount -o ro /app/metrics_bundle.tar.gz /home/user/mnt_data` (creating the directory first) rather than `mount`/loop devices. Add both `export` lines to `~/.profile`, then write `start_exporter.sh` to `source ~/.profile`, check if the archivemount is already active (e.g. via `mountpoint` or `findmnt`) and mount if not, then launch the compiled binary with `&` and redirect its output. Install the cron entry with `crontab -l | { cat; echo '@reboot /home/user/start_exporter.sh'; } | crontab -`, then manually run the script yourself and confirm with `curl` that the JSON is actually served before finishing.
