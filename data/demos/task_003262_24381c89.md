Set up the module first (`cd /home/user/sysmetrics && go mod init sysmetrics && go get gonum.org/v1/gonum/stat`) before writing logic, so dependency resolution doesn't block you later. Read each CSV with `encoding/csv`, skip the header, and parse numeric columns into `[]float64` slices per server — keep the three servers' data separate since each computation uses a different pair/server. Use `stat.Correlation(cpuA, memA, nil)` for the Pearson coefficient on Server A. For the regression, be careful about which variable is x and which is y — the task predicts CPU from Memory, so call `stat.LinearRegression(memB, cpuB, nil, false)` (or equivalent) and take the slope return value, not the intercept; getting x/y swapped is the most common mistake here. For cosine similarity, compute each server's 4-dimensional mean vector (CPU, Memory, NetIn, NetOut) manually with simple averages, then implement cosine similarity directly (`dot(A,C) / (norm(A)*norm(C))`) since gonum's helper for this is less standard — verify it stays in [-1,1] as a sanity check. Round all three outputs to exactly 4 decimal places and marshal them to `/home/user/sysmetrics/results.json` matching the exact key names given, without touching the original CSVs.
