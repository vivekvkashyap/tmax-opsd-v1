Build this as a pipeline of clearly separated stages so you can debug each independently: (1) use ffmpeg (`ffmpeg -i archive_broadcast.mp4 -vf fps=1 frame_%03d.png`) to extract 1 fps frames; (2) crop each frame to the bottom 10% by height using PIL or ffmpeg's crop filter; (3) run `pytesseract.image_to_string` on each cropped strip (install `tesseract-ocr` via apt and `pytesseract`/`Pillow` via pip) and inspect a few raw OCR outputs manually before automating further; (4) since the ticker text was corrupted by encoding original UTF-8 bytes and misreading them as Latin-1/cp1252, reverse it per-string with `text.encode('latin-1', errors='replace').decode('utf-8', errors='replace')` (try cp1252 too if latin-1 doesn't produce clean text) and manually verify the result looks like readable words before trusting it at scale; (5) lowercase, strip non-alphanumeric-except-space characters with a regex, split into tokens; (6) since the ticker scrolls, frames will show overlapping/repeated substrings — merge across frames by deduplicating consecutive/near-duplicate token windows (e.g. using longest-common-suffix/prefix stitching) rather than a naive set-based dedup which would scramble word order. Verify final output against the 0.85 SequenceMatcher threshold requirement by checking it reads as coherent, ordered text.
