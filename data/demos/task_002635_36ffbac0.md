First fix `sshd_config` for an unprivileged instance: set `Port 2222`, point `AuthorizedKeysCommand` at `/app/bin/auth_hook` with `AuthorizedKeysCommandUser` set to your current user, and route logging to the required file (e.g. run a small local syslog-compatible listener, or configure `SyslogFacility`/`LogLevel` plus a userspace rsyslog rule writing to `/home/user/ssh_logs/auth.log`) — start it and confirm sshd actually comes up before moving on. To find the backdoor string, don't guess: run `strings /app/bin/auth_hook | less` first (stripped binaries still usually leak literal C strings), and if that's ambiguous, disassemble with `objdump -d` or `gdb`/`ghidra` and look for a `strcmp` call comparing `argv[1]` against a hardcoded constant near the entry point. Once you have the real backdoor string, write `detector.py` with a robust regex over standard `sshd` `Accepted ... for <user> from <ip>` log lines (handle whitespace/format variance, since the held-out test log is large and possibly obfuscated), filtering for exactly that username and printing only the matched source IPs, one per line, deduplicated is optional but false positives/negatives directly hurt your F1 score — test your regex against varied log lines before trusting it.
