Handle this as three independently verifiable pieces. First, run `tesseract /app/token_spec.png stdout` (or equivalent) to OCR the auth token — don't guess or hardcode it, extract it and parse out the value after "TOKEN:". Write processor.c so it takes exactly one argv string, reverses it with a simple two-pointer or loop-based reversal (mind off-by-one on string length and null terminator), and printf's `"PROCESSED: %s | AUTH: %s\n"` with the reversed string and the OCR'd token substituted in — since this will be fuzz-tested against a hidden oracle across many random ASCII inputs including edge cases like empty-ish/whitespace/special-character strings, test your own reversal against a few hand-computed examples (including length 1) before compiling. Compile with gcc to `/home/user/bin/processor`. Second, edit only the `fastcgi_pass` line in nginx.conf to point at 127.0.0.1:9000, leaving other directives untouched. Third, write a logrotate stanza targeting `/home/user/nginx/logs/*.log` with `daily`, `rotate 7`, `compress`, and `missingok` directives, matching logrotate's standard block syntax.
