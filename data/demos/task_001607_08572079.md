Start by reverse engineering the schema before writing any C: run `sqlite3 /home/user/knowledge.db ".schema"` (and `.tables`) to see the actual table/column names for nodes and edges — do not assume conventional names like `nodes`/`edges`, inspect what's really there. Once you know the schema, design the C program around three phases: (1) use parameterized queries (`sqlite3_prepare_v2` + `sqlite3_bind_text`) to look up the integer IDs for the start/end node names, never string-concatenating names into SQL; (2) load the full edge list (or query neighbors on demand) into an adjacency structure keyed by node id; (3) run BFS from the start id, and at each BFS layer, when expanding a node's neighbors, sort or iterate them in lexicographic order of node name so that ties in shortest-path length are broken by picking the lexicographically first node at each step — track parent pointers to reconstruct the path once the destination is dequeued. Reconstruct the path by walking parent pointers backward from destination to start, reverse it, and look up each id's name to write one per line to the output file. Compile with `-lsqlite3`, run it with the two given node names, and inspect the output file manually before finishing.