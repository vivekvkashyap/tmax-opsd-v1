Break this into decode -> patch -> build -> serve. Decode the video by reading it frame-by-frame at 1 fps (`cv2.VideoCapture` or `ffmpeg`), classifying each frame black=0/white=1 by sampling pixel brightness, grouping bits into bytes of 8 (MSB first), and converting to ASCII — derive the text purely from pixel data, don't guess it. That text holds a unified diff plus a trailing `AUTH_EXPR=...` line; save the diff and apply it with `patch`/`git apply` against `parser.cpp` rather than hand-editing. Evaluate the `AUTH_EXPR` expression yourself to get the integer deployment token, which becomes your bearer credential. Write a Makefile compiling `main.cpp`+`parser.cpp` with g++. Fill in `main.cpp`'s handler: parse `Authorization: Bearer <token>` exactly, reject anything else with a raw 401, otherwise pass the POST body through `evaluate_expression` and return the integer after a 200 OK header. Compile, launch backgrounded on 127.0.0.1:8080, and sanity-check with a manual `curl` before finishing.