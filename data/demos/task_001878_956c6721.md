Handle the three parts in sequence. First, extract frames from `/app/incident_record.mp4` (e.g. `ffmpeg -i incident_record.mp4 -vf fps=1 frame_%03d.png` or similar) and visually/OCR-scan them for the terminal showing an `openssl req ... -subj "/CN=..."` command; read off the exact CN value shown (not a guess) and write only that CN string into `/home/user/rogue_cn.txt`. Second, write `/home/user/detector.py` using `pyelftools` (`from elftools.elf.elffile import ELFFile`) to open the target binary, iterate sections, and check two independent conditions: (a) whether the `.rodata` section's raw bytes contain the recovered rogue CN string encoded as ASCII (use bytes-substring search, e.g. `cn.encode() in section.data()`), and (b) whether any section is literally named `.mal_tls` (iterate `elf.iter_sections()` and compare `sec.name`). Print `EVIL` if either is true, else `CLEAN` — test this against `/app/corpus/clean/` and `/app/corpus/evil/` to confirm 100% accuracy before finishing. Third, generate fresh credentials with `openssl req -x509 -newkey rsa:2048 -keyout /home/user/new_key.pem -out /home/user/new_cert.pem -days 365 -nodes -subj "/CN=legit-update.local"`, and verify the resulting cert's CN and validity period with `openssl x509 -in new_cert.pem -noout -subject -dates`.
