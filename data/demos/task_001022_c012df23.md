Reproduce the crash first: run `python pipeline.py incident_809.wav` and confirm the segfault, then open `audio_ops.cpp` and look specifically at the sample-processing loop bounds — a loop condition like `i <= num_samples` instead of `i < num_samples` reads one element past the end of the sample buffer, which is the classic off-by-one segfault; fix the comparison operator. Separately check whether `math.h` is included and whether the RMS formula correctly divides the sum-of-squares by the sample count before taking the square root (an arithmetic bug here would produce a wrong-but-non-crashing result, distinct from the bounds bug). For `build.sh`, a `g++ -fPIC -shared -o libaudio.so audio_ops.cpp` invocation that references `sqrt` will fail to link on some toolchains unless the math library is explicitly linked — add `-lm` at the end of the compile command. After fixing all three issues, rebuild `libaudio.so`, rerun `pipeline.py` against the same WAV file, and sanity-check the resulting `anomaly_score.txt` value against what RMS energy should look like for this file's actual sample data (compute peak-amplitude/sqrt(2) by inspecting the WAV yourself, e.g. with Python's `wave`/`numpy`, as a cross-check) rather than trusting the C++ output blindly.