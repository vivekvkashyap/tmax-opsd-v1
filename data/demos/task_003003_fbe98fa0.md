Split this into three independent workstreams and validate each before moving on. (1) In `/app/legacy_tracker/`, port `fastmath.c` off the Python 2 C-API: replace `Py_InitModule3` with the Python 3 `PyModuleDef` + `PyModule_Create` pattern, add a `PyInit_<module>` entry point, and fix `setup.py`'s Python 2 syntax (print statements, etc.) so `pip install .` (or `python3 setup.py build_ext --install`) succeeds; then extract frames with `ffmpeg -i /app/experiment.mp4 frames/frame_%04d.png` and run `extract.py <frames_dir>` to produce `/home/user/video_stats.json` — verify it actually reflects the true frame count you extracted, don't hardcode a number. (2) Write `/home/user/server.go`: a `POST /validate` handler that computes Euclidean distance between consecutive `points`, rejects (400) if any consecutive gap exceeds 50.0 else accepts (200), and enforces a global 10 req/s cap using `golang.org/x/time/rate` or a manual token-bucket goroutine, returning 429 when exceeded — go's built-in `net/http` plus a middleware wrapping the limiter is the standard approach. Run it on :8080 in the background. (3) Before declaring done, POST every file in `/app/corpus/clean/` and `/app/corpus/evil/` yourself with `curl`, retrying on 429, and confirm clean→200 and evil→400 for all of them — this is your own check, not a shortcut to skip.
