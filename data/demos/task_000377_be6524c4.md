Transcribe /app/research_memo.wav with an available speech-to-text tool (or listen/inspect via ffmpeg-based playback if audio tooling is unavailable) to get the exact feature-engineering rules spoken — do not assume standard embedding techniques, use precisely what the recording specifies for how each of dataset_name/num_rows/is_public should be transformed into numeric features and in what order. Implement dataset_encoder.py to read one JSON object from stdin, apply exactly those transformations (get the operators, e.g. division/truncation vs rounding, exactly right — integer vs float semantics matter since this will be diffed against a reference implementation), and print a single comma-separated line with a trailing newline and nothing else on stdout (route any debug prints to stderr instead). Because grading is by exact-match fuzzing against a hidden oracle across many random inputs (varying string lengths, large/small num_rows, both boolean values), test your script manually against a handful of edge cases (empty-ish name, num_rows near a boundary, both true/false) via `echo '...' | python3 dataset_encoder.py` to confirm formatting and value correctness before considering it done.
