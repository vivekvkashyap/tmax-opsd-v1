Build the operator as a straightforward validate-then-merge pipeline, not ad-hoc string checks. Steps: (1) `glob('/home/user/manifests/*.json')` sorted lexicographically so precedence order is deterministic; (2) for each file, wrap `json.load` in try/except to catch JSON errors first, logging `ERROR: Invalid JSON in <filename>`; (3) only for files that parse, validate schema explicitly — must have exactly the keys `route`, `target_port`, `allowed_ips` with types str/int/list-of-str (use `type(x) is int`, not just truthy checks, and reject bool since `isinstance(True, int)` is True in Python); log `ERROR: Invalid schema in <filename>` on failure; (4) build a dict keyed by route, overwriting on each valid file processed in filename-sorted order so the lexicographically-latest filename naturally wins; (5) write `/home/user/proxy_config.json` from that dict; (6) write the log with STARTED first, then error lines (any order), then SUCCESS lines sorted by `sorted(routes)`, then COMPLETED last. Open the log file once and build lines in a list to control ordering precisely rather than appending as you go, since SUCCESS lines require a second pass after all routes are known. Actually execute the script at the end so both output files exist on disk.