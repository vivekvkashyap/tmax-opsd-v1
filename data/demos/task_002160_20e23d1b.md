Write `/home/user/analyze.cpp` to read `/home/user/energy_signal.csv` (skip the header line, parse `x,E` pairs into two `std::vector<double>`). Implement the trapezoidal rule as a simple loop summing `(y[i-1]+y[i])/2 * (x[i]-x[i-1])` over all consecutive points — do not assume a fixed step size, read it from the actual x deltas even though it's uniform here. Implement the central-difference max derivative as `|y[i+1]-y[i-1]| / (x[i+1]-x[i-1])` for interior points `i=1..n-2`, tracking the maximum absolute value, and explicitly skip the first and last samples since central difference is undefined there. For the analytical integral, derive it yourself: `E(x) = x*cos(x) + sin(x)` is the derivative of `x*sin(x)` (check by differentiating `x*sin(x)` with the product rule), so the definite integral from 0 to 10 is `10*sin(10) - 0`; compute that with `std::sin` in C++, don't hardcode a value you haven't derived. Take `abs(numerical - analytical)` for the error. Compile with `g++ -O2 analyze.cpp -o analyze`, run it, and write the four lines to `/home/user/analysis_output.txt` using `std::fixed << std::setprecision(4)` exactly matching the required label format.
