Start by trying the build to see the actual failure: `cd /app/vendored/fast_forensic_hash && pip install -e .` and read the linker error carefully rather than assuming what's wrong — it will point at an undefined `-l<name>` library. Inspect `setup.py`'s `Extension(...)` definition; on Linux, the linker flag `-l<name>` maps to a shared library file `lib<name>.so`, so a `libraries=['zlib']` entry looks for `libzlib.so` (which doesn't exist) instead of the real system zlib (`libz.so`) — fix the string to the correct bare library name and reinstall. Once installed, confirm `import fast_forensic_hash; fast_forensic_hash.compute_hash(b"test")` works from Python before moving on. Then write `/home/user/analyze.py` to take `sys.argv[1]`, attempt `base64.b64decode(arg, validate=True)` inside a try/except (catching decode errors specifically, since permissive base64 decoding silently ignores bad characters) — on failure print `INVALID_INPUT` and `sys.exit(1)`; on success call `compute_hash` on the decoded bytes and print `.hex()` of the result (ensure lowercase). Do not try to read or reverse-engineer `/opt/oracle/analyze_oracle` — build the module correctly and the hash algorithm will match by construction since it's the same underlying zlib-based logic; only compare your own output against the oracle to sanity-check behavior on a few sample inputs.