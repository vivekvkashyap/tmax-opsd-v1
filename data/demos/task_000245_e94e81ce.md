Structure `sync_configs.py` as clearly separated steps: (1) validate archive integrity by opening it with `tarfile.open(..., "r:gz")` inside a try/except and exiting with status 1 on failure; (2) extract the baseline into a temp directory (e.g. `tempfile.mkdtemp()`) so you can diff it against the live directory without polluting anything; (3) for each `.conf` file under `/home/user/live_config/`, compare byte-for-byte against its baseline counterpart (if any) using plain content comparison, not just mtime/size — files with identical content but different metadata must NOT be touched, and files with different content or no baseline counterpart must be modified; (4) when prepending `# TRACKED CHANGE\n`, read the original content first, then write the header followed by the original content back to the same path, being careful not to double-prepend if the script is run twice or to corrupt files you shouldn't touch; (5) only after all in-place edits are done, `tar.add()` the whole updated `/home/user/live_config/` directory into a fresh gzip tarball at `/home/user/tracked_config.tar.gz`. Run the script yourself afterward and inspect each live file's exact contents to confirm untouched files really are byte-identical to before.