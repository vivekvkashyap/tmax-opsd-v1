Start by transcribing `/app/issue_42.wav` (whisper or similar) and extracting the exact field names for the new schema and the precise order of the mathematical operation described — get the operation order right (e.g. whether an offset is added before or after a scaling multiply, and whether a modulo is applied last) since a fuzz-equivalence check will compare against a hidden reference across many random inputs and any order mistake fails broadly. Update `/home/user/math_service/proto/service.proto` so `ProcessRequest`/`ProcessResponse` declare the fields exactly as named in the audio (types and field names matter even though this file isn't the primary thing scored). Rewrite `/home/user/math_service/process.sh` to read the JSON request from stdin (use `jq` to parse the array and scalar fields), apply the transformation element-wise across the array using the exact formula from the memo (integer arithmetic — watch out for shell integer overflow or negative-modulo edge cases if the language's `%` differs from Python's), and emit the JSON response shape via `jq -n`/`jq -c`. Keep the script executable. Before finishing, generate your own random test JSON inputs (including edge cases: empty array, zero multiplier, large values) and manually compute expected outputs to sanity-check your script's math independent of any oracle.