Design the C++ program around three clear phases so each is easy to get right. (1) Traversal: use `<filesystem>` (`std::filesystem::recursive_directory_iterator`) to walk `/home/user/project_scatter` and collect every regular file whose name ends in `.chunk`, regardless of depth or prefix. (2) Ordering: for each matching filename, extract the integer that follows the literal substring `_part` (e.g. locate `"_part"` in the filename, then parse the digits immediately after it up to the next non-digit or `.chunk`) — do not assume a fixed prefix or fixed-width number; sort the collected files ascending by that extracted integer. (3) Assembly: open each chunk file in binary mode in that sorted order and append its bytes to `/home/user/reassembled_project.tar.gz` using `std::ifstream`/`std::ofstream` with `std::ios::binary`. After writing the archive, use `system("tar -xzf /home/user/reassembled_project.tar.gz -C /home/user manifest.txt")` (or equivalent) to extract `manifest.txt`, then read only its first line in C++ and write it plus a trailing newline to `/home/user/manifest_summary.txt`. Compile with g++ and run the resulting `assembler` binary to confirm it actually reproduces a valid tar.gz before finishing.
