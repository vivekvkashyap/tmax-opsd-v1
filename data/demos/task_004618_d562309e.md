First localize the crippled build: grep the SQLite source tree's Makefile (or configure output) for `CFLAGS`/`OPT_FEATURE_FLAGS` entries that disable CTE or window-function support (look for `-DSQLITE_OMIT_*` defines), strip those flags, then rebuild with `make sqlite3` and sanity-check by running a trivial `WITH RECURSIVE ... SELECT` and a `RANK() OVER (...)` query against the freshly compiled binary to confirm they no longer error. For the data model, treat `sales_data.csv` as an adjacency list (employee -> manager) and use a recursive CTE to walk each employee's downline, summing `individual_sales` over the closure, then apply `RANK() OVER (PARTITION BY dept_id ORDER BY total_team_sales DESC)` and filter to rank <= 3. Before finalizing the query, create indexes on the manager/employee id columns (and dept_id) after loading the CSV via `.import`, since recursive traversal over 100k unindexed rows will blow the time budget — index creation plus `EXPLAIN QUERY PLAN` checks are the standard way to confirm the join/recursion isn't doing full table scans. Wrap the whole load-index-query-export sequence in the bash script driving the compiled `sqlite3` binary non-interactively.
