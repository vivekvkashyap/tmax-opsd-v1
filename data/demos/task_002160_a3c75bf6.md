For Part 1, open `/app/vendored/pysm-lib/setup.py` and find the `install_requires` list specifying two conflicting `typing-extensions` version constraints (e.g. an exact-pin alongside a lower-bound) — collapse them into one satisfiable constraint, then `pip install /app/vendored/pysm-lib` and confirm with `pip show pysm-lib`/`import pysm` that it installed. For Part 2, treat `/app/oracle/legacy_parser` as a black-box oracle you may run and diff against, but implement `/home/user/new_parser.py` independently in Python: maintain `state` (`IDLE`/`ACTIVE`) and an integer `acc`, read stdin line by line, `strip().split()` each line (this naturally normalizes whitespace padding), and dispatch on the first token — `INIT` requires exactly one integer argument and transitions to ACTIVE; `ADD`/`MUL` require ACTIVE state and one integer argument, else print the state or parse error; `EMIT` requires ACTIVE state, prints `RESULT: <acc>`, and resets to IDLE; anything else, malformed argument counts, or non-integer tokens print `ERROR: UNKNOWN_CMD`. Get argument-count and integer-parsing edge cases (extra tokens, missing tokens, non-numeric values, blank lines) exactly right, since equivalence is fuzz-tested against the oracle on thousands of random command streams including malformed ones — write a quick local fuzz-diff script comparing your output to the oracle's before finishing.
