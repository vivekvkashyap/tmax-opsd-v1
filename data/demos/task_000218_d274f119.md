Split this into two independent pieces and get each working before combining. First, establish the SSH tunnel as a detached background process: `sshpass -p siteadmin ssh -p 2222 -N -L 8080:127.0.0.1:9090 admin@<host> &` (or use `-f` to background it natively), then verify with `nc -zv 127.0.0.1 8080` that the forward is actually listening before writing any client code — a stale/dead tunnel is the most common failure mode here. Second, write the C++ client to connect to `127.0.0.1:8080`, read a username from stdin, then drive the interactive protocol: read until you see "Enter username: ", write `<username>\n`, read until "Enter password: ", write `querypass\n`, then read the full response until the socket closes and parse the `QUOTA <used> <limit>` line (or error text). For exact output formatting, do NOT guess — run the provided oracle binary yourself with several sample usernames piped via stdin, diff its stdout byte-for-byte against your program's output, and adjust formatting/whitespace/error-message wording until they match exactly, including on edge cases like empty input or names the mock service treats as errors. Compile to `/home/user/quota_cli` and leave the tunnel running.