Handle each phase separately and verify in isolation before combining. In the vendored package, inspect env_setup.sh for an `alias awk=...` line that shadows real awk with something broken — remove or unset that alias so the script can source cleanly and actually invoke `/usr/bin/awk`. Then read process_entry.sh's parse_line function for a recursive call with no length/base-case guard; add a proper termination condition (e.g. stop recursing once the line is fully consumed or below a length threshold) so it can't recurse forever on long lines. For the deleted file recovery, open inode_table.txt to understand how it maps block indices/pointers to files under fs_recovery_img/blocks/, then reconstruct crash_evidence.log by concatenating the referenced blocks in the pointer-specified order (do not just guess file order — follow the recorded pointer sequence) and write the result to /home/user/crash_evidence.log. For sanitize.sh, read stdin line by line, call the fixed process_entry.sh on each line internally, and drop lines matching a run of 50+ consecutive hex characters or containing the literal strings `0xNaN` or `infinity_loop_ptr`, passing everything else through unchanged; keep exit code 0.