Work through this migration in dependency order: understand the reference algorithm first, then port it, then fix the build, then wire up the server. Read `reference.py` closely and note it reads raw grayscale frames at a fixed resolution via an ffmpeg pipe, computes sum-of-absolute-differences between consecutive frames, and tracks the 1-based index of the frame with the largest diff versus its predecessor — replicate that exact loop and indexing convention in C, translating the pixel-diff summation with `abs()` on `unsigned char` casts to `int`. In `analyzer.c`, fill the TODO with an accumulation loop over all pixels; for the memory leak, don't just add a `free()` blindly — either free the outgoing `prev_frame` right after copying `curr_frame` into a fresh buffer, or (cleaner) swap the `prev`/`curr` pointers each iteration and allocate only once, freeing at the end. For the Makefile, shared libraries loaded via ctypes require compiling with `-fPIC -shared` (not a plain `gcc file.c -o lib.so`, which will crash or misbehave when loaded). After `make` succeeds, verify `server.py` actually loads the `.so`, checks the exact Bearer token, and returns valid JSON, then launch it in the background bound to 127.0.0.1:8080 and confirm with a curl request including the auth header before considering the task done.