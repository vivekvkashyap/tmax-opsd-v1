First fix the vendored library build: inspect `/app/miniz-3.0.2/Makefile` for a broken `CC` value (e.g. a nonexistent compiler name) and switch it to `gcc`/`cc`, and check `CFLAGS` for something like `-O0` that would tank compression throughput — change it to `-O3` — then run `make` to produce `libminiz.a`. For the archiver, the correctness-under-concurrency requirement is the crux: open the input file, take a shared lock with `flock(fd, LOCK_SH)` around the read so you never observe a torn write from the background writer (which holds an exclusive lock while writing), `mmap` the file for the read instead of `read()` calls, then use miniz's C API (`mz_zip_add_mem_to_archive_file_in_place` or equivalent) to compress the mapped buffer straight into `snap_<i>.zip`, unmap, unlock, close, and `usleep(10000)` before the next of exactly 50 iterations. Compile with `-O3` and link against the fixed `libminiz.a`. Measure timing yourself and check zip validity/JSON-parseability of a few snapshots to catch races before relying on the automated metric.
