Scaffold with `cargo new bayesian_data_prep` in `/home/user/`, add `csv`, `nalgebra`, and `serde_json` to `Cargo.toml`. Parse `raw_measurements.csv` by header name (not fixed column index) so column order in the file doesn't silently break feature selection, build the design matrix X with columns [1.0 (bias), temp, pressure, humidity] in that exact order — explicitly dropping `vibration` — and the target vector Y from `failure_score`. Compute the ridge/MAP solution W = (XᵀX + λI)⁻¹XᵀY using `nalgebra`'s matrix multiply and `.try_inverse()` (or a linear solve) with λ=2.0 and I as the 4x4 identity matching XᵀX's dimension — a common mistake is sizing I to n_samples instead of n_features, or forgetting the bias column entirely. Write the 4 weight floats as a flat JSON array (full precision, no manual rounding) to `/home/user/experiment/weights.json`, and write `{"n_samples": <rows>, "n_features": 4}` to `/home/user/experiment/metadata.json`, creating the `/home/user/experiment` directory first. Run `cargo build --release` then execute the binary, and independently sanity-check your matrix math against a quick Python/numpy computation of the same ridge formula before trusting the output.