Scaffold with `cargo new /home/user/etl_project` and add `csv`, `serde`, `serde_json`, `regex`, `sha2` to Cargo.toml. Use the `csv` crate's `Reader` (not manual line-splitting) so embedded newlines inside quoted CSV fields are parsed correctly — this is the main trap naive parsers fall into. For each record, run a regex like `ErrorCode: (E\d+)` against `log_message`, capture group 1, and skip rows with no match. Compute SHA256 of the exact `log_message` string with the `sha2` crate, format as lowercase hex, and dedupe using an in-order-preserving structure (e.g. a `HashSet<String>` of seen hashes checked while iterating rows top-to-bottom, keeping only the first occurrence — do not sort before deduping or you'll keep the wrong instance). After dedup, sort the remaining records by `(user_id, error_code)` ascending, both as string comparisons. Serialize to the exact JSON shape (`user_id`, `error_code`, `hash` keys, array order) with `serde_json` and write to `/home/user/summary.json`; run `cargo run` yourself and diff against manually worked-out expected values for the sample rows to catch ordering or dedup mistakes before considering it done.
