Work phase by phase and verify each before moving on. First run `tesseract /app/experiment_params.png stdout` (install tesseract-ocr if missing) and parse the `K_BASE:` and `THRESHOLD:` lines with a regex/awk to get the two floats — don't hardcode guessed values. For the MPI simulation, write one Python script run via `mpiexec -n 4 python3 script.py`; build the Watts-Strogatz graph identically on every rank with `networkx.watts_strogatz_graph(200, 4, 0.1, seed=42)` so results are reproducible, and compute each rank's own K as `K_BASE + rank*0.01`. Implement the synchronous update as a full array copy per timestep (update all nodes from the previous timestep's values, not in-place) and stop when `max(abs(x_new - x_old)) < THRESHOLD`. Use `comm.gather` to bring final arrays and iteration counts to rank 0, write them into an HDF5 file with h5py using nested groups/attributes as specified, and plot per-rank variance-over-time curves with matplotlib before converging. Finally, build a small Flask/FastAPI app that loads the HDF5 file once at startup, checks the `X-API-Key` header before responding, and returns the JSON fields with `k_used` rounded via `round(k, 3)`; start it detached (e.g. nohup/background) so it keeps serving after your session ends.
