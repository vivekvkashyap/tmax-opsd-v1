Three independent deliverables — tackle them separately and test each in isolation. (1) Use `find /app/restored_data/raw/ -name '*.log' -o -name '*.wav'` piped to `ln -s` (with absolute target paths) into `/home/user/backup_view/`, flattening regardless of subdirectory depth; watch for filename collisions across subdirectories since flattening can silently overwrite links — if collisions occur, decide on a stable disambiguation. (2) Write a small `http.server`-based static file server bound to port 9000 serving `/home/user/backup_view/`, and a separate `proxy.py` on port 8080 that forwards incoming requests to it (e.g. using `http.client` or `urllib.request` to relay the request/response, or `socketserver` with a handler that proxies bytes) — start both processes and keep them alive so a request to `localhost:8080/<file>` returns the proxied content. (3) For the audio compression step, load `alert_001.wav` with the `wave` module, use `audioop.ratecv` or `audioop.lin2lin` to downsample the sample rate and/or reduce bit depth, and write the result with `wave` — check the resulting file size is reduced by at least 30% and spot-check that the waveform still resembles the original (e.g. via a quick sample-by-sample comparison) before considering it done, since the verifier checks MSE against the original samples.
