Structure this as classic graph-index-then-traverse: parse both CSVs into Go structs, then build two maps — one from node id to type, and one from source node id to list of outgoing (target, relation, weight) edges — so you can look up "all follows-edges out of this Bot" and "all likes-edges out of this User" in O(1)/O(degree) rather than scanning the edge list repeatedly. Iterate all nodes of type Bot, for each look up its outgoing `follows` edges to nodes of type User, and for each such User look up its outgoing `likes` edges to nodes of type Post, accumulating `(bot_id, user_id, post_id, total_weight)` where `total_weight` is the sum of the two edge weights — a Bot/User pair can have multiple valid extensions if either edge type is not unique, so don't assume one path per Bot. Sort the resulting path list with a custom comparator: `total_weight` descending first, then `bot_id` ascending, then `user_id` ascending as tiebreakers — implement this explicitly with `sort.Slice`, don't rely on map iteration order. Take exactly the first 3 after sorting, marshal to the exact JSON key names required (`bot_id`, `user_id`, `post_id`, `total_weight`), and write to `/home/user/top_patterns.json`. Recompute a couple of paths by hand from the CSVs to sanity-check your weight sums before trusting the sort.
