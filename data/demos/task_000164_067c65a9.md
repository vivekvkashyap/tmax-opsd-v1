Start by making the vendored dependency buildable: open `/app/graph/go.mod`, find the `go 1.99` directive, and lower it to a version your Go toolchain actually supports (e.g. `go 1.21`) — nothing else in that file needs changing. Then in `/home/user`, run `go mod init` and add a `replace github.com/dominikbraun/graph => /app/graph` line pointing at the local fixed copy so the build stays fully offline; `go mod tidy` may fail without network, so hand-edit `go.mod`/`go.sum` or vendor manually if needed. For `simulate.go`: split the sequence into overlapping 3-mers in order, add each as a vertex via `graph.New(graph.StringHash, graph.Directed())`, and add an edge between consecutive 3-mers exactly as they appear in the sequence (not from a generic 2-char-suffix/prefix match over all pairs — only adjacent-in-sequence links, since duplicate k-mers must not create spurious extra edges). Seed `math/rand` with `rand.Seed(seed)`, then for 100 steps pull the current node's outgoing edges via the graph's adjacency/edges API, sort destination node names lexicographically, and pick `rand.Intn(len(dests))` to advance — stop early if there are no outgoing edges. Print the final node string with no extra formatting. Build with `go build -o /home/user/simulate simulate.go` and sanity-check determinism by running it twice with the same seed.
