Install `libsqlite3-dev` (and `sqlite3` for manual inspection) first, and explore the schema/data with `sqlite3 /home/user/citation_graph.db ".schema"` before coding, so you understand the `papers`/`citations` tables firsthand. In the C program, open the DB with `sqlite3_open`, then use `sqlite3_prepare_v2` on a query with `?` placeholders (never string-interpolate `argv` into SQL) and `sqlite3_bind_int` for `source_id`/`target_id`; the bridging pattern A->X->B is a self-join: select X's id/title where an edge exists from source to X and a separate edge exists from X to target. Iterate rows with `sqlite3_step`, and for each candidate title call `sqlite3_column_text`, then explicitly skip the row in your C code if the pointer is NULL or the string length is zero — don't rely on SQL alone for that filter. Build the JSON array yourself (escaping isn't needed for these plain titles, but keep it simple and correct), writing `[]` when there are zero valid rows. Compile with `gcc process_graph.c -lsqlite3 -o /home/user/process_graph`, then actually run it with `10 50` and inspect the resulting file.
