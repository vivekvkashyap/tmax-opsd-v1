The core technical challenge here is numerical, not architectural: naive `sum += value` accumulation over millions of near-cancelling large floats loses precision catastrophically, so you must implement compensated summation (Kahan or Neumaier/Kahan-Babuska summation, which tracks a running low-order correction term alongside the running sum) for each coordinate's accumulator. Structure the program to stream `/app/cluster_sim`'s stdout line by line (parse CSV fields as float64, don't buffer all 5M rows), splitting work across goroutines by chunk, where each goroutine maintains its own compensated-sum accumulator (sum + correction) and its own mass accumulator; when combining per-goroutine partial sums into the final total, don't just naively add the partial sums together — combine them also using compensated addition (or at minimum add correction terms back in) since even the final combine step can reintroduce cancellation error, and the task's 1e-10 error tolerance leaves no room for sloppy reduction. For the confidence interval, treat each goroutine chunk's partial CoM as one bootstrap sample point, resample with replacement 1000 times from these chunk partial-sums, and compute the 2.5th/97.5th percentile bounds. Validate correctness before trusting the numbers: test your Kahan summation on a small synthetic case with known cancellation by hand, and only then run against the full binary output, writing exact-format `result.json`.
