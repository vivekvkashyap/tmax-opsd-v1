Rewrite `/home/user/extract.py` rather than patching it: read `/app/telemetry.wav` with `scipy.io.wavfile.read`, then instead of naive fixed-size chunking, detect each of the 50 pulses by thresholding the *absolute* value of the signal against a low noise floor (e.g. `abs(signal) > small_epsilon`) to find contiguous non-silent regions (silence gaps separate the 1kHz bursts), and for each contiguous region take `max(abs(segment))` as its peak — this fixes both the "misses low-amplitude pulses" and "ignores negative-phase peaks" bugs in the original. Write all 50 peaks (one float per line) to `/home/user/parsed_data.txt`, and sanity-check the count is exactly 50. For `/home/user/compute.c`, find the fixed-size stack array reading floats from the file and enlarge/replace it with a size that safely covers 50+ values (or better, read dynamically/resize) to fix the buffer overflow, then have it sum the squares of all values and print the result. Compile with `gcc -o /home/user/compute /home/user/compute.c`, run the full pipeline (extract.py then compute), and save the printed sum-of-squares float to `/home/user/final_result.txt`. Validate your extraction against the known pulse count (50) before trusting the final number.