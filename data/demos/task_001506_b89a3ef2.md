Do this entirely with shell tools, in clearly separated stages so each transformation is independently checkable. First, `curl -o /home/user/raw_logs.csv http://127.0.0.1:8080/raw_logs.csv`. Second, collapse embedded newlines inside the quoted `message` field: since a real record always starts with a numeric `id` at the start of a line, use `awk`/`sed` logic that joins any line not beginning with `<digits>,` onto the previous line with a space (e.g. iterate with awk, buffer the current record, and flush only when the next line starts a new record) — a plain "replace all newlines" is wrong because it would merge separate log rows together. Third, strip invalid UTF-8 with `iconv -f UTF-8 -t UTF-8 -c` (the `-c` flag discards invalid sequences). Fourth, validate the `status` column (field 3) against the allowed set {200,301,404,500} and drop non-matching data rows, e.g. with `awk -F',' 'NR==1 || $3~/^(200|301|404|500)$/'` (careful with quoted commas inside message — treat CSV parsing carefully, e.g. limit the status-code check to before the message field). Fifth, rewrite the `MM/DD/YYYY HH:MM:SS` timestamp (field 2) to `YYYY-MM-DD HH:MM:SS` with `sed`/`awk` regex capture-group reordering. Write the result to `/home/user/clean_logs.csv` keeping the header. Finally tally status codes from the cleaned data with `awk`/`sort -n`/`uniq -c` into `/home/user/status_counts.txt` formatted as `CODE: count`, numerically sorted ascending, omitting codes with zero occurrences.