Diagnose the two problems separately before touching the build. For the memory-safety bug, read the parsing loop in libmathparse.c closely: it reads a length byte then loops over the payload — check the loop bound against the allocated buffer size character by character; an off-by-one (using `<=` where it should be `<`, or allocating N elements but writing N+1) is the classic heap overflow here, so trace exactly how many elements are malloc'd versus how many the loop index reaches, and correct the loop bound (or allocation size) so they match. For the linking/runtime issue, `ldd ./main` after `make` to confirm the missing library, then fix the Makefile so `main` is linked with an rpath pointing at the current directory (e.g. add `-Wl,-rpath,'$$ORIGIN'` or `-Wl,-rpath,.` to the link command) rather than relying on the caller to export LD_LIBRARY_PATH — a proper fix belongs in the Makefile/link flags, not a one-off shell export, since verification just runs `./main` directly. After both fixes, `make clean && make` (or just `make`) and run `./main`, then confirm result.txt was produced with the expected computed sum.
