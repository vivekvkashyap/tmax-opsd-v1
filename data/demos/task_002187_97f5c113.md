Fix each bug in setup.py in isolation, re-running the build after each change so you know which fix resolved which failure:

1. Version check: `ENV_VERSION < MIN_VERSION` compares dotted version strings lexicographically, so "1.10.2" < "1.5.0" is true because "1" < "5" character-by-character — this wrongly rejects a newer version. Replace it with a numeric comparison: split each string on "." and compare the resulting integer tuples (or use `packaging.version.parse`), not raw strings.
2. Checksum check: the hardcoded `expected_ops_hash` constant does not match the real file, and the file is also opened in text mode (`'r'`) instead of binary (`'rb'`) before hashing, which is the wrong practice for checksums even though it isn't the actual cause of the mismatch here. Fix the read to use binary mode, then compute the real SHA256 of the actual `src/ops.h` on disk (e.g. `sha256sum src/ops.h` or Python's `hashlib.sha256` on the binary content) and put that correct hex digest into `expected_ops_hash`.
3. Linking: the C code calls `pow()` from libm, but the `Extension()` in setup.py lists no libraries. Add `libraries=['m']` (or `extra_link_args=['-lm']`) to the Extension definition.

After all three fixes, rebuild with `python3 setup.py build_ext --inplace`, run the pytest suite, and only then write the exact success marker to `/home/user/success.log`.
