Write a streaming parser rather than a naive `json.loads` per line: open the file with `open(path, 'r', encoding='utf-8')` and iterate line by line so memory stays flat. For each line, first try `json.loads` directly; on `json.JSONDecodeError`, repair before retrying — use a regex like `\\\\u[0-9A-Fa-f]{0,3}(?![0-9A-Fa-f])` (or scan character-by-character) to find incomplete `\uXXXX` escapes and dangling/isolated surrogate halves, and replace each broken escape with the literal Unicode Replacement Character (U+FFFD) or strip it, then re-parse. Never skip a line that fails to parse on the first try — every one of the input lines must end up counted, or your total event count will be short. For each successfully parsed record, parse the `ts` field as ISO 8601 and truncate to the top of the hour (zero out minutes/seconds, keep the `Z` suffix and format). Accumulate counts in a dict keyed by `(hour, type)`, then emit rows sorted first by hour ascending, then by type ascending, to `/home/user/summary.csv` with header `hour,type,count`. Use only the standard library (`json`, `re`, `datetime`, `csv`, `collections`).