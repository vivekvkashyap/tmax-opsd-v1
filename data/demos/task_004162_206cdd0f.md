First reproduce the crash: try decrypting a sample token with the vendored `crypt_auth_lib` and read the traceback — it will point at the IV-extraction line in `decryptor.py`. AES-CBC requires a 16-byte IV, so check exactly how many bytes are being sliced off the front of the ciphertext for the IV versus what the encryption side actually prepended; fix the slice so IV length and the corresponding ciphertext offset agree (both should reference the same 16-byte boundary), then reinstall/reimport the package (`pip install -e /app/crypt_auth_lib-2.1` or equivalent) and confirm decryption round-trips on a token you construct yourself before writing the detector. In `detector.py`, parse the raw header file line by line to build a dict, extract the `Session-Token` value out of the `Cookie` header (cookies are typically `key=value; key2=value2` pairs), decrypt it with the fixed library and the given key, then case-insensitively search every header value AND every string value inside the decrypted JSON for each of the six listed payload substrings — check the JSON recursively/at all key levels, not just top-level ones, and be defensive around malformed/undecryptable tokens. Print exactly `EVIL` or `CLEAN` with nothing else, and validate against both corpora directories yourself before trusting it against the hidden test set.