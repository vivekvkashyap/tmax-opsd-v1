Step 1: compile `/app/schema/video_events.proto` for Python (`protoc --python_out=... video_events.proto`, or `grpc_tools.protoc`) and import the generated module in `/home/user/validate_logs.py`. Parse the binary file argument into a `VideoEventLog` message (`ParseFromString`), then validate the three invariants explicitly and in order: every event's `start_time_ms >= 0`; every event's `start_time_ms < end_time_ms`; and no pairwise overlap — since events aren't guaranteed sorted, sort a copy of the events by `start_time_ms` first, then walk adjacent pairs checking that each event's start is >= the previous event's end (equal is allowed, touching is fine, strictly less is an overlap). Print `VALID`/exit 0 or `INVALID`/exit 1 accordingly, and test against both `/app/corpus/clean` and `/app/corpus/evil` before considering it done — pay special attention to zero-duration and negative-duration edge cases since those are classic corpus traps. Step 2: run `ffmpeg -i /app/reference_video.mp4 -vf blackframe -f null - 2>&1`, count the lines in stderr that the `blackframe` filter emits (one per detected black frame), and write that integer count to `/home/user/black_frames_count.txt`.