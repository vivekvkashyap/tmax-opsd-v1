Study the actual expression parser in `/home/user/app/api/parser.py` to understand how nesting and field-access paths are represented internally before writing detection rules — don't write a generic sanitizer blind to the real grammar. For `is_safe_query`, extract each `@filter(expr: "...")` argument (regex or a lightweight parser) and reject the query if the expression's nesting depth (count balanced parentheses/brackets depth) exceeds the safe bound, or if it references any restricted/internal schema field by name — return `True` only when neither condition triggers. Validate iteratively against both a clean and an evil sample set of your own construction (shallow filters vs. deeply nested or restricted-field filters) before trusting the logic, since both under- and over-blocking will fail the check. Separately, edit `nginx.conf`'s `/graphql` location block to `proxy_pass` to the FastAPI backend's actual host:port, then run the provided `start_services.sh` and confirm end-to-end with a `curl` through the Nginx port that it reaches FastAPI successfully.
