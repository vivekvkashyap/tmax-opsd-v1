Before writing any SQL, run `DROP INDEX IF EXISTS idx_stale_size;` (or an equivalent statement executed via the SQLite C API at program startup) so the corrupted index can never be chosen by the query planner — do this unconditionally each run, not just once. Then design a single SQL statement using `WITH RECURSIVE` to walk the `fs_nodes` tree from each node down through all descendants via `parent_id`, summing `size` per starting node to get its total recursive size (a node with no descendants just contributes its own size; directories have size 0/NULL and should be treated as 0 in the sum). Apply `RANK() OVER (ORDER BY total_size DESC, id ASC)` on top of that aggregated result to get the tie-aware ranking (equal total_size values must receive the same rank, and the next rank should skip appropriately, matching SQL RANK semantics — not DENSE_RANK). Execute this single query through the SQLite C API in your C program (prepare/step/finalize), and for each result row build one JSON line by hand with exact key order and types (`id` as integer, `name` quoted string, `total_size` integer, `rank` integer) and append it to `/home/user/analytics.jsonl`. Compile with `gcc ... -lsqlite3`, run it, and spot-check a few lines against the recursive sums by hand.
