Recover the constants first, separately from writing simulation code: run OCR (e.g. `tesseract /app/reaction_rates.png stdout`) on the image, and if the raw output is noisy, try preprocessing (grayscale, threshold/upscale with ImageMagick) before re-running OCR, then parse the recovered text for the three named values `ALPHA`, `BETA`, `GAMMA` rather than assuming their values — extract them programmatically from whatever text you recover. Once you have the three constants, implement the Go program: take exactly 3 CLI args as `float64` initial concentrations, then run a plain forward-Euler loop for exactly 100 steps with `dt = 0.1`, at each step computing the three derivatives from the *current* X, Y, Z simultaneously (compute all three `dX/dt, dY/dt, dZ/dt` from the same pre-update state before applying any updates — updating X in place and then using the new X for dY is a classic bug that breaks equivalence with a reference Euler implementation). Print the final X, Y, Z space-separated with exactly 6 decimal places via `%.6f`. Compile with `go build -o /home/user/sim_runner`, then sanity-check by running it with a few different initial conditions and confirming the output format matches exactly (no extra whitespace, trailing newline as needed).