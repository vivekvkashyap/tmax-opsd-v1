Split this into two independent pieces and solve them separately. For Part 1, extract the subtitle stream from the video rather than trying to read frames visually — use `ffmpeg -i /app/build_metrics.mp4 -map 0:1 -f srt /home/user/blacklist_raw.srt` (or similar) to pull out stream index 1, then strip SRT numbering/timestamps/blank lines with a script (e.g. a regex removing lines that are pure numbers or `-->` timing lines) so only the bare semantic version strings remain, one per line, in `/home/user/blacklist.txt`; verify by inspecting the extracted file rather than assuming the format. For Part 2, implement semver comparison and range resolution natively in Bash: parse each version into major/minor/patch integer arrays (handling malformed/pre-release strings gracefully by skipping them), filter out anything present in blacklist.txt, then implement `^X.Y.Z` (allow changes preserving the left-most nonzero component) and `~X.Y.Z` (allow only patch bumps) matching logic against the filtered set, and numerically compare remaining candidates (not lexicographically) to pick the highest. Since this will be fuzz-tested against a hidden oracle across hundreds of random cases, test your script manually on edge cases (single-digit versions, exact-version requests, empty result) before considering it done.