Split this into an ETL step and a numerically-exact C++ step. For Part 1, use `ffmpeg -i /app/experiment.mp4 -vf fps=1 frame_%03d.png` (or pipe raw frames) to extract one frame per second, then for each frame compute the mean R, G, B pixel values (e.g. with Pillow/numpy: load the image, average each channel across all pixels) and write rows to `/home/user/video_features.csv` with header `frame,R_avg,G_avg,B_avg`. For Part 2, since your C++ program will be fuzz-tested byte-for-byte against a hidden reference binary, prioritize exact-format matching over cleverness: read `W` then stream comma-separated R,G,B floats, maintain a sliding window of the last `W` triples (e.g. a deque), and once you have `W` samples compute the sample covariance matrix using Bessel's correction (divide sums of squared/cross deviations by `W-1`, not `W`) with `double` precision throughout. Print the 6 upper-triangular entries (Var R, Var G, Var B, Cov RG, Cov RB, Cov GB) comma-separated using `std::fixed << std::setprecision(6)` on each qualifying line, continuing until EOF. Test edge cases like W=2 and verify stdout formatting exactly (no trailing whitespace/extra newlines) since the check is byte-identical output comparison.