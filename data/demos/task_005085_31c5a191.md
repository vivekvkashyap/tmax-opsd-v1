Run `tesseract /app/rules.png stdout` (or similar OCR invocation) to extract the threshold rule text, then parse the numeric threshold out of that text programmatically (do not hardcode a guessed number). For the CSV, the key trap is that embedded newlines inside quoted fields break naive `awk`/line-based parsing — use a CSV-aware tool (Python's `csv` module, or `csvkit`/`mlr`) that correctly handles quoted multi-line fields rather than splitting on `\n`. Load the cleaned wide CSV, melt/reshape it from `date,ServerA,ServerB,ServerC` into long form `date,server_name,value` (e.g. pandas `melt`), coercing values to numeric (the embedded-newline fields may need whitespace stripped after parsing). Filter rows where `value > threshold` using the OCR'd threshold. Write the result to `/home/user/anomalies.csv` with header exactly `date,server_name,value`, ensuring numeric formatting matches the source values (don't introduce trailing zeros or scientific notation) since the grader does an exact-row F1 comparison. Verify by re-reading your own output file to check row count and no stray blank/duplicate rows from the newline cleanup.