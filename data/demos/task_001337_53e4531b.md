Process `/home/user/access_logs.jsonl` with a `for line in f:` loop and `json.loads` per line — never load the whole file or build a list of all records. For IP masking, split on `.` and replace the last octet with `XXX`, applied only to the field written to the CSV, not to logic. For the rolling average, maintain a running sum and count of latencies seen so far (a true running average of all prior records, since fewer than 50 exist for the first requests). Compare the current record's latency strictly greater than 3x that rolling average (only from record 2 onward) to flag anomalies, then update the running sum/count with the current record's latency afterward — order of update matters. Write matches to CSV with `csv.writer`, exact header, and format rolling_avg with `f"{x:.2f}"` (not bare `round`) so trailing zeros aren't lost.