Start by extracting the hidden config from audio rather than assuming values: install a speech-to-text tool (e.g. `whisper` or another available STT CLI) and transcribe `/app/alert.wav` to recover the spoken fallback port number and secret token — read digits/words carefully since numbers may be spoken individually. Use only the recovered port/token in all subsequent steps. For the C library, write `compute_checksum` as a simple loop XOR-ing each `unsigned char` of the input string, compile with `gcc -shared -fPIC -o /home/user/libchecksum.so checksum.c`. For the bash HTTP backend, use `socat TCP-LISTEN:<port>,reuseaddr,fork EXEC:handler.sh` (or an `nc`-based accept loop) where the handler script parses the request line and headers itself: match `GET /health` to reply `HTTP/1.1 200 OK\r\n\r\nOK`; for `POST /process`, extract the `Authorization` header value, compare against `Bearer <recovered-token>` and return 401 on mismatch, otherwise read the body (respecting `Content-Length`), pipe it into a short inline `python3 -c` snippet using `ctypes.CDLL('/home/user/libchecksum.so').compute_checksum` with correct `argtypes`/`restype`, and return the integer result as the body. Set up `socat` (or similar) as a transparent TCP proxy listening on `127.0.0.1:9000` forwarding to `127.0.0.1:<recovered-port>`. Finally loop `curl` 50 times against `/health` through port 9000, redirecting output/timing into `/home/user/benchmark.log`, and leave both processes backgrounded.
