Start by isolating the vendored library's build sabotage: grep `/app/csv-parser`'s headers (especially the main `csv.hpp` or any `internal/*.hpp`) for suspicious top-of-file directives like `#error`, a forced `#define __cplusplus <old-standard>`, or `#undef __cplusplus` blocks that would break compilation under C++17 — remove or neutralize just that sabotage without altering the library's real logic, then confirm a trivial `#include "csv.hpp"` program compiles with `-std=c++17`. Then write `/home/user/deadlock_detector.cpp`: use the library to read the `tx_id,waiting_for_tx_id` CSV, build an adjacency list (directed edges from `tx_id` to `waiting_for_tx_id`), and detect a cycle with a standard DFS three-color (white/gray/black) or recursion-stack approach — a "gray" node revisited during traversal signals a cycle. Return/exit `0` for no cycle, `1` (or any nonzero) for a cycle found; be careful to check *all* connected components, not just one starting node, since a graph can have disconnected cycles. Compile to `/home/user/deadlock_detector` and validate it against the provided `/home/user/corpora/clean/` and `/home/user/corpora/evil/` CSVs before considering it done, since the real grader uses a similar adversarial corpus check.