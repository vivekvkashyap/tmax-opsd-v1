Work through the failures in dependency order, verifying each with a real test rather than assuming it's fixed. First inspect the broken symlink (`ls -la`/`readlink`) to see what real directory it points to, then create that target directory with `mkdir -p` so writes through the symlink succeed — you do not need to change the symlink itself. Second, set up traffic forwarding from the hardcoded port to the real backend port: a simple reliable option is a small background `socat TCP-LISTEN:9999,fork,reuseaddr TCP:127.0.0.1:8080 &` (or an equivalent Python proxy thread) — confirm the backend is actually listening on its port first before wiring the forward, and confirm the forward works with a quick manual request before moving on. Third, edit the script to replace the relative `config.json` open with the full absolute path, so it stops depending on the caller's working directory. Finally run it as instructed from the home directory, then also re-test from a different directory (e.g. /tmp) to confirm the path fix actually generalizes, and confirm the output file appears at the real (symlink-resolved) location with the expected response content.
