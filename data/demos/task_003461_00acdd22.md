Peel the nested archive layer by layer: `tar -xzf /app/repo_archive.tar.gz` (or Python `tarfile`) to get `internal_data.zip`, then unzip that to get `metadata.xml`. Since the XML is UTF-16LE, read it with the correct encoding (e.g. Python `open(path, encoding='utf-16-le')` or detect the BOM) before parsing with `xml.etree.ElementTree` — parsing raw bytes as UTF-8 will garble or fail. Locate the `<artifact id="audio_1">` element and pull its `<author>` child's text. For the audio, use a real transcription tool (`whisper`, or another ASR library/CLI available in the environment — install if needed) on `/app/artifact.wav` to get a transcript; don't guess the content. Take the first word of the transcript, lowercase it, and strip surrounding punctuation with something like a regex (`re.sub(r'[^\w]', '', word)`) to get the `transcription_keyword`. Stand up a minimal HTTP server (Python `http.server`/`socketserver`, or Flask/FastAPI + `uvicorn`) bound to `127.0.0.1:8080` that answers `GET /api/artifact` with a 200 and a JSON body containing exactly the `author` and `transcription_keyword` keys, and keep the process running in the background (e.g. `nohup ... &`) for the verifier to query.