Load the WAV with scipy.io.wavfile, cast to float and normalize by its own max absolute value. Set dt = 1/fs and T = duration from the sample count. Derive dx from the CFL target: dx = c*dt/0.9, then set Nx = ceil(L/dx)+1 and recompute dx = L/(Nx-1) so the grid exactly spans [0,L]. Implement the damped wave update with a centered/leapfrog scheme: precompute C2=(c*dt/dx)^2 and use the standard semi-implicit form that averages the damping term between time levels, i.e. solve the discretized PDE for u_next given u, u_prev, gamma and C2, rather than naively adding a forward-difference damping term (that is where naive attempts go unstable/inaccurate). Left boundary = source sample each step; right boundary fixed at 0. Record the value at the grid index nearest x=5.0 each timestep, normalize the recorded trace to [-1,1], and save as 16-bit PCM at the same sample rate. For attenuation, take max|u| across time at x in {1..5}, fit A(x)=A0*exp(-alpha*x) (e.g. linear-regress log(A) vs x), save alpha. Write the pytest to compare simulation error at two grid resolutions (dx vs dx/2) against a reference and check the error ratio is near 4.
