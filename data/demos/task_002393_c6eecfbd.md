Write `analyze.py` to load the three arrays from `/home/user/dataset.npz` with `np.load`, then process each of the N graphs/signals independently. For graph diameter, build a `networkx.Graph` from each adjacency matrix with `nx.from_numpy_array` and call `nx.diameter` (relying on all-pairs shortest paths under the hood) rather than reimplementing BFS by hand, since correctness of the diameter matters directly for the regression's x-values. For smoothing, implement the length-996 moving average carefully to match the exact windowing described — `np.convolve(signal, np.ones(5)/5, mode='valid')` produces exactly this window definition (value at i = mean of original[i:i+5]), so verify your implementation's output length is 996 and its first value equals the mean of the first five raw samples. For peak extraction use `np.argmax` on the smoothed signal and index `frequencies[peak_idx + 2]` — that offset comes directly from the smoothing window shrinking each side by 2. Fit OLS with `numpy.polyfit(diameters, peak_freqs, 1)` or `sklearn.linear_model.LinearRegression`, compute MAE against the same training points, round every reported number to 4 decimals, and write valid JSON to `/home/user/results.json`.