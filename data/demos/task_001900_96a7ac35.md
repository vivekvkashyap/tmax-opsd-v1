Work in stages and validate each before moving to the next. First extract the two custom ELF sections with `objcopy --dump-section .cert_chain=/tmp/chain.pem <binary>` and similarly for `.auth_token` (or parse the ELF section headers directly with `pyelftools` if available) rather than guessing offsets by hand — this avoids brittle manual binary parsing. Load the extracted PEM chain with the `cryptography` library's `x509.load_pem_x509_certificates` (it can parse multiple concatenated PEM blocks from one buffer); the first cert is the leaf, the second is the root. Verify the leaf's signature was produced by the root's public key (use `root_public_key.verify(leaf.signature, leaf.tbs_certificate_bytes, padding, leaf.signature_hash_algorithm)`), and verify the root is self-signed the same way against its own public key. Then use the leaf's public key with `PyJWT`'s `jwt.decode(token, leaf_public_key_pem, algorithms=["RS256"])` to validate the token and pull out the `sub` claim — wrap each stage in try/except so any failure degrades the corresponding boolean to `false` and `jwt_subject` to `null` rather than crashing. Finally, write the four-key JSON exactly as specified to `/home/user/result.json`.
