First diagnose the vendored package build failure directly: run `go build ./...` inside `/app/graph` (or try to import it) and read the exact compiler error rather than guessing — a package-name mismatch between the `package X` declaration and the import path is a classic vendoring typo, so grep the top of the .go files for the `package` clause and compare it against what other files in the same directory declare, fixing the outlier to match. Wire your own module with a `go.mod` `replace` directive pointing the graph package's module path at the local `/app/graph` directory, then `go mod tidy` to resolve it. In `classifier.go`, parse the CSV (skip header, handle empty parent_id as "no edge"/root), build a directed graph with the vendored library's API, and use its cycle-detection function (e.g. `graph.CreatesCycle`)—check incrementally while adding edges, or build the whole graph then run the cycle check once, matching whatever the library exposes. Print exactly `EVIL`/`CLEAN` and exit with the specified codes (1 for evil, 0 for clean) — no extra stdout text, since the adversarial corpus checker likely does exact string matching. Test against a few small hand-built cyclic and acyclic CSVs before trusting the corpus run.
