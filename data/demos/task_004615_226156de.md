Tackle this in independent stages and verify each before moving on. (1) TLS: generate a key+CSR for CN=localhost with `openssl req -newkey rsa:2048 -nodes -keyout ... -out ...`, sign it with the internal CA using `openssl x509 -req -CA /app/ca/ca.crt -CAkey /app/ca/ca.key -CAcreateserial -days 30`, then point `ssl_certificate`/`ssl_certificate_key` in nginx.conf at the new files and reload Nginx (SIGHUP or restart) — verify with `openssl s_client -connect localhost:8443 -CAfile /app/ca/ca.crt`. (2) Add the exact CSP header via `add_header Content-Security-Policy "..."` in the server/location block (check it isn't dropped when other add_header directives exist, since nginx only keeps headers from the most specific block that declares any). (3) SUID: use `find /app/bin -perm -4000 -type f` to list SUID binaries with absolute paths into `/home/user/suid_audit.txt`. (4) Brute-force detection: parse the JSON log lines by IP and timestamp, and for each IP use a true sliding 60-second window over its sorted `login_failed` timestamps (e.g. a two-pointer/deque approach) rather than fixed-size bucketing, flagging IPs with >5 failures in any window — this window-boundary logic is the main source of F1 loss. Test end-to-end with `curl --cacert` against the live services before declaring done.