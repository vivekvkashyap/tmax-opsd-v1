Do the aggregation and the regression as two clearly separated steps on the same combined dataframe. Load both CSVs and concatenate them (e.g. `pandas.concat`) before doing anything else, so both outputs are computed from the full combined data. For the aggregation, group by `sensor_id`, take the mean of `temperature`, round to exactly 2 decimals, sort ascending by `sensor_id`, and write with exactly the headers `sensor_id,avg_temperature` (watch out for pandas adding an index column — use `index=False` when writing the CSV, and make sure rounding happens before writing, not after, since `to_csv` won't round for you). For the regression, fit `sklearn.linear_model.LinearRegression` (which includes an intercept by default) with `temperature` and `humidity` as the feature matrix `X` and `pressure` as target `y`, predict on the same training rows, compute RMSE as `sqrt(mean_squared_error(y, preds))`, and write only the RMSE rounded to exactly 4 decimals to the metrics file. Double check both output files contain no extra whitespace or stray index columns, since verification is an exact diff.
