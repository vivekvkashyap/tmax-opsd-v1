Work through this polyglot stack layer by layer, verifying each before moving to the next. First fix CMakeLists.txt: change the library target from `STATIC` to `SHARED` (or `MODULE`) so `ctypes` can `dlopen` it — a static `.a` will silently fail to produce a loadable `.so`. Rebuild in a clean `build/` directory (`cmake -S . -B build && cmake --build build`) and confirm `libsecure_hash.so` exists. Next, inspect `compute_hash` in secure_hash.c: it `malloc`s exactly `strlen(input)` bytes but then writes a null terminator at `temp[len]`, one byte past the buffer — fix the allocation to `malloc(len + 1)`. Then write server.py: load the `.so` with `ctypes.CDLL`, wire up a Flask route `POST /api/hash` on port 5000, and run an `asyncio` websocket server on port 8765 concurrently (e.g. one in a background thread, or both under asyncio with Flask run via a thread/executor) — both paths must call the same C function and hex-encode its output identically. Write test_e2e.py as a client that exercises both the REST and websocket endpoints and asserts the returned hashes are non-empty/consistent, writing `E2E_TESTS_PASSED` or `E2E_TESTS_FAILED` accordingly. Start the server in the background before running the test script, and check both processes' logs if something hangs.
