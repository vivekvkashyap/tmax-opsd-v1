Treat this as two independent phases and don't let the crypto work block the code fix. For evidence recovery: run `tesseract /app/evidence/ransom_note.png stdout` (or via pytesseract) to recover the password prefix text exactly as printed, trim whitespace/newlines carefully. Since the password is "<prefix><3-digit number>", write a small bash or Python loop over `000`-`999`, trying `openssl enc -d -aes-256-cbc -pbkdf2 -salt -pass pass:"${prefix}${n}" -in /app/evidence/secret.enc -out /tmp/try.txt 2>/dev/null` and check the exit code / that the output is valid text (openssl fails cleanly on wrong passwords with `-pbkdf2 -salt`, so exit status is a reliable success signal) — save the winning candidate's output to `/app/evidence/decrypted.txt`. For remediation, read `/app/server/main.go`'s upload handler: it trusts the client-supplied multipart filename directly in `filepath.Join`, allowing `../` traversal — sanitize with `filepath.Base(header.Filename)` before joining to the uploads dir, and reject/normalize any result that still escapes it. Add the `/token` GET handler returning the recovered token text verbatim, then start the server backgrounded and confirm with `curl localhost:8080/token`.