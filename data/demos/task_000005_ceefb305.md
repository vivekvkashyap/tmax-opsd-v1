Build `/home/user/archiver.c` incrementally and test each piece independently before combining. For the lock: `open()` (or `fopen`) `/home/user/archive_index.wal` with `O_CREAT`, then `flock(fd, LOCK_EX)` before writing and `flock(fd, LOCK_UN)`/close when done — verify with a quick two-process test that the lock actually excludes concurrent writers. For encoding: read `/home/user/incoming/legacy_log.iso88591` byte-by-byte and convert with `iconv_open("UTF-8", "ISO-8859-1")` + `iconv()`, not a hand-rolled table, then write the converted bytes into the WAL file. For ELF scanning: `opendir`/`readdir` over `/home/user/incoming/`, check each regular file's first bytes for the ELF magic (`\x7fELF`) and `EI_CLASS == ELFCLASS64`, then `mmap` or `fread` the file and cast/parse an `Elf64_Ehdr` to pull `e_entry`, appending `ELF: <name> ENTRY: 0x<hex>` lines (match `readelf -h`'s hex formatting) to the WAL. No `system()` calls anywhere — only `<elf.h>`, `<iconv.h>`, `<sys/file.h>`/`<fcntl.h>`. Compile with `gcc`, run it, then `tar -czf` both `/home/user/incoming/` and the WAL file and split with `split -b 1M - backup.tar.gz.part` (using suffix length 2, alphabetic) into `/home/user/backup_parts/`.
