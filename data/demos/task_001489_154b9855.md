Start by extracting the actual instructions from the audio rather than guessing the fix — convert `/app/voicemail.wav` to text with a speech-recognition tool (e.g. install `SpeechRecognition`+`pydub`, or use `ffmpeg` to get a compatible wav and feed it to a recognizer, or try `whisper` if available) and read the transcript carefully; it names specific formula/parameter changes to apply to the Newton-iteration-style solver in `/home/user/processor.py` (look for wording about which trig function belongs in the derivative denominator, what step-size/learning-rate multiplier to apply to the update, and how many iterations to run). Apply exactly the changes the voicemail specifies to the math and loop bounds — do not alter the CLI argument handling or the `.6f` print format. Since correctness is checked by fuzz-equivalence against a hidden oracle across many (x, y) pairs, write a couple of manual test cases yourself and hand-trace a few iterations to sanity-check convergence before trusting the fix, then save the corrected version to `/home/user/fixed_processor.py`.
