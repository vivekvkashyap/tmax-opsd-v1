Work this as three separable subsystems that must ultimately connect via ctypes. First, reverse-engineer `/app/bin/core_evaluator_v1.bin`: run `file`, `strings`, and disassemble with `objdump -d` or `radare2`/`gdb` to see how it consumes a coefficient array and an x value — look for repeated multiply-add patterns against ascending powers of x, which points to a polynomial/Taylor-style evaluation (`c0 + c1*x + c2*x^2 + ...`); confirm by feeding it known small test inputs if it's runnable and checking outputs against hand-computed polynomials. Second, fix the Rust borrow-checker error in `src/lib.rs` — the pattern of taking an immutable reference into an array you then mutate in a loop needs restructuring (e.g., copy the referenced value into a local scalar before the mutation loop instead of holding a live reference across mutations). Fix the C wrapper's Makefile by ensuring the link line includes the correct `-L<path-to-target/release> -l<cratename>` flags in the right order (libraries after the objects that need them) and that `cargo build --release` runs before the C link step. Finally, rewrite `app.py` for Python 3 (replace `print` statements, `except X, e:` syntax, `urllib2`), load `libmathcore.so` via `ctypes.CDLL`, wire the polynomial coefficients extracted from expression parsing (use a safe parser like `ast` or `sympy`, never bare `eval`) into the C function, catch parse errors to return 400 with the exact error JSON, and background-launch Flask on 127.0.0.1:8080 logging to `/home/user/server.log`.