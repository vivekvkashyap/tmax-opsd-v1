Write the C processor to read stdin in large buffered chunks (avoid one `fread` per 36-byte record — batch reads into a big buffer and parse records out of it) since the volume is 2M records. Define a packed struct (or manually parse each field via memcpy/offsets, being careful about the compiler not padding your struct — use `#pragma pack` or manual byte offsets since the on-wire layout is tightly packed little-endian) for timestamp/user_id/value/hash. For deduplication, maintain a fixed-size circular buffer of the last 5000 hashes and, for each incoming record, linearly compare against all entries in that window (or use a hash set keyed by the 16 bytes for speed) — only treat it as a duplicate if it matches something currently *in* the window, then evict the oldest entry as the window slides. Load `/app/users.csv` into a hash map (user_id -> group_id) once at startup, defaulting missing ids to group 0. For each group_id maintain a small ring buffer of up to the last 10 values to compute the trailing SMA in O(1) per record (sum minus outgoing, plus incoming, divided by current window count). Print `timestamp,group_id,sma` per valid record with `%.4f` formatting, buffering stdout writes for performance, and compile with `-O3`.
