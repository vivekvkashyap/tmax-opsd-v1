Write a FASTA parser that reads headers (stripping the leading `>`) and concatenates subsequent lines into each sequence. Build the overlap graph by comparing, for every ordered pair of distinct sequences, whether the last K=5 characters of one exactly equal the first K=5 characters of the other (skip sequences shorter than K). For each sequence compute nucleotide frequencies over A/C/G/T as counts divided by total length. Implement Jensen-Shannon divergence from first principles: compute the midpoint distribution M=(P+Q)/2, then KL divergence sum(p_i * log2(p_i/m_i)) skipping any term where p_i is 0 (0*log2(0)=0), and JSD = 0.5*KL(P||M) + 0.5*KL(Q||M). Iterate over all graph edges, track the maximum JSD and its source/target IDs, format the value with exactly 4 decimal places, and write `SourceID,TargetID,JSD_Value` to /home/user/result.txt (or `None` if no edges exist). Use only Go's standard library (math, strings, bufio, os) — no external modules. Double-check your edge-detection logic against the actual overlap definition (suffix-of-A equals prefix-of-B) rather than assuming symmetry, since directionality matters for which sequence is source vs target.