Build this as a strict three-phase pipeline and get each phase exactly right since later phases compound on the first. Phase 1: parse `raw_metrics.jsonl` line by line, drop records missing `host` or `timestamp`, drop records where `cpu_usage`/`memory_mb` are null/missing/non-numeric (try float() and catch failure), normalize timestamps by first trying to parse as an int/int-string epoch, falling back to ISO8601 UTC parsing (`datetime.fromisoformat` after replacing a trailing `Z`, or `strptime`) — treat unparseable timestamps as invalid and drop the record, then dedupe on the exact tuple (host, normalized_timestamp, cpu_usage, memory_mb). Phase 2: group by host, bucket each record's timestamp by flooring to the nearest 3600 (`ts - ts % 3600`), and within each bucket compute mean cpu (round to exactly 2 decimals) and max memory. Phase 3: read the template file's placeholder text and do literal string replacement of `{host}`, `{num_windows}`, and `{metrics_list}` (windows sorted ascending by epoch, one `Window: <epoch> | Avg CPU: <avg> | Max Mem: <max>` line per window) — match number formatting exactly (Python's default float-to-str, not extra trailing zeros) since the grader does exact string comparison. Ensure `/home/user/output/` exists before writing `<host>_report.txt` files.
