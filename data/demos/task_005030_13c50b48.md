This is a multi-service integration task — get each hop working independently before wiring them together. Start by inspecting the skeleton at /home/user/app/src/archiver.c to see what's already stubbed, then confirm Redis is reachable on 127.0.0.1:6379 and Nginx is configured/running to proxy authenticated POSTs on 8080 to your daemon's TCP port 9000 (check/adjust the nginx site config so the Authorization header gates the proxy). For the excludes file, read it as raw bytes and convert from UTF-16LE to UTF-8 with `iconv()` (link -liconv or use glibc's built-in iconv) before splitting on newlines to build the exclusion list — don't assume 1-byte chars. For symlink loop detection during traversal, use `lstat()`/`stat()` on each entry, and when you follow a symlink, record its (st_dev, st_ino) in a visited set (a simple growable array is fine) checked against ancestor directories in the current traversal path; skip descending into it if seen already, but still allow first-time valid symlinks. Write the manifest as `relative/path:size\n` for every included file, then shell out via `system()`/`execvp` to run `tar czf <archive>.tar.gz -C <basedir> <files...>` using the manifest paths. Only after the archive+manifest exist, connect to Redis (raw RESP protocol or libhiredis) and issue `SET backup_status:<basename> COMPLETED`, then respond `OK\n` on the TCP socket. Compile with the needed dev libs installed first (libhiredis-dev etc.), start the daemon backgrounded, and test end-to-end with a raw curl POST through Nginx before trusting it.
