Run OCR first, don't guess the parameters: use `pytesseract.image_to_string(Image.open('/app/cleaning_rules.png'))` to recover the actual outlier thresholds, imputation strategy, and hidden-layer sizes as written in the note — inspect the raw OCR text carefully since numbers can be misread, and cross-check against the shape of `weights.pth` (load it with `torch.load` and print each tensor's `.shape` — the `Linear` weight shapes directly reveal the hidden dimensions, giving you a way to verify OCR results against ground truth from the weights file itself). Apply the recovered thresholds to drop temperature outliers and impute missing humidity, counting drops and imputations exactly as you go (order matters: filter first, then impute, or vice versa — match what the note says). Reconstruct the `nn.Sequential(Linear(3,h1), ReLU, Linear(h1,h2), ReLU, Linear(h2,1))` with the discovered `h1`/`h2`, `load_state_dict` from `weights.pth`, and set to `eval()` mode before inference. Build the FastAPI/Flask app with both endpoints gated by checking the `Authorization: Bearer data_ops_2024` header exactly, run it bound to `127.0.0.1:8080` in the background, and verify both `/stats` and `/predict` respond correctly before finishing.
