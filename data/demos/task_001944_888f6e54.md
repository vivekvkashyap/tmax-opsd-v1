Treat this as three independently verifiable phases and validate each before moving to the next. Phase 1: load the CSV, sort by `sensor_id` then `timestamp`, and use `groupby('sensor_id')[cols].rolling(3).mean()` (or equivalent) so the rolling window resets per sensor rather than bleeding across sensor boundaries — this is the most common mistake here; drop NaN rows afterward and check the resulting row count makes sense (each sensor group loses its first 2 rows to the window). Phase 2: engineer features strictly from the three rolling-mean columns (not raw values), run `GridSearchCV(cv=3)` over the given `n_estimators`/`max_depth` grid with `random_state=42` fixed on the classifier, then persist the fitted best estimator via `joblib.dump` and the `best_params_` dict as JSON with exactly the two expected keys. Phase 3: build a FastAPI app that loads the joblib model once at startup, wraps single predictions into the model's expected feature order in `/predict`, run it with `uvicorn` in the background on port 8000, and write a benchmarking script that sequentially fires 100 requests, checking each response status before finally writing `SUCCESS` to the status file — do this only after confirming the server responds correctly to one manual curl.