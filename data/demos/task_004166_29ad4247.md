Set up an isolated Python environment first (`python3 -m venv /home/user/venv && source /home/user/venv/bin/activate`) and install `pandas`, `scikit-learn`, `scipy` inside it so the analysis is reproducible. Load the CSV with pandas, then follow the exact pipeline specified: select the four system-metric columns, fit `StandardScaler` on them (standardize before PCA — never skip this, since PCA on unscaled features would be dominated by whichever column has the largest numeric range), then fit `PCA(n_components=1, random_state=42)` on the scaled matrix and call `.fit_transform` to get the single `System_Load_Index` column. Compute the Pearson correlation between that index and `Inference_Time_ms` with `scipy.stats.pearsonr`, and take the absolute value. Separately, split `Inference_Time_ms` by `Model_Type` into two groups and run `scipy.stats.ttest_ind(group1, group2, equal_var=False)` (Welch's, not Student's — the unequal-variance assumption matters, so don't call the default equal-variance version) to get the p-value. Round both results to 4 decimal places and write them to `/home/user/analysis_results.json` matching the exact key names and structure given in the task.