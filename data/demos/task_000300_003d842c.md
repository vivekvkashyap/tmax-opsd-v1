In C, load `transactions.csv` into an adjacency structure (each `tx_id` has one or more `waits_for_tx_id` edges) and `costs.csv` into an id-to-cost lookup — use a hash map or a sorted array plus binary search since IDs aren't guaranteed contiguous. Detect the single cycle with a standard DFS cycle-finder: maintain a recursion-stack marker per node; when DFS reaches a node already on the current recursion stack, you've found the cycle — walk back along the DFS path (or record parent pointers) from the current node to that ancestor to recover exactly the cycle members, not the whole path leading into it. Sum the costs of only those cycle members, sort their IDs ascending, and print them comma-separated. Watch two common traps: (a) collecting all nodes visited during DFS instead of isolating just the cycle subset, and (b) mixing up "first node found again" with the actual set of nodes between the repeated node and itself. Write the two-line report to `/home/user/deadlock_report.txt` in exactly the specified format, then compile and run to confirm it matches.
