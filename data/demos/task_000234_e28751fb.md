Write a Python script that processes the log as a state machine: scan line-by-line, and only start capturing when you see a line matching the `@@@ CONFIG_START | HOST: ... | TIME: ... @@@` header (parse hostname/timestamp with a regex), buffer subsequent lines until you hit the exact `@@@ CONFIG_END @@@` line, then flush the buffer to `/home/user/configs/<hostname>/<epoch_timestamp>.conf` — use `os.makedirs(..., exist_ok=True)` for the subdirectories, and make sure lines outside any block (plain system/warning logs) are simply skipped, not accidentally included. Build the manifest by hashing each written file's actual bytes with `hashlib.sha256` and keying the JSON by the path relative to configs/ (e.g. `db_master/1690000500.conf`). For the archive, use Python's `tarfile` (or `tar -czf configs_backup.tar.gz -C /home/user configs`) with `-C` so the archive root is `configs/`, not an absolute path. For chunking, use `split -b 1024 -d -a 2 configs_backup.tar.gz backup.part_` but note `split` needs `-a 2` with alphabetic suffixes (default `--suffix-length=2` uses letters only without `-d`) — verify by reassembling with `cat backup.part_* > rebuilt.tar.gz` and diffing against the original to confirm lossless round-tripping before finishing.