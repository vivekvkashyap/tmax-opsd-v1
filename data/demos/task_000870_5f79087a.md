Build and test the proxy incrementally against the two upstream services rather than all at once. Start the internal services first (`/app/start_services.sh`), then confirm you can fetch and base64-decode a binary from the Binary Service and that `readelf -S` on the decoded bytes shows section headers, including `.rodata` — use the ELF section header table (parse `e_shoff`/`e_shentsize`/`e_shnum`, or a library like `pyelftools`) to locate the exact byte range of `.rodata` rather than guessing offsets. Within that byte range, scan for ASCII text and apply a proper email regex (e.g. `[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}`) to find matches; replace each matched substring's bytes with `*` characters of the identical byte length, in place, so no offsets after `.rodata` shift — do NOT reconstruct/rewrite the ELF, just mutate bytes within the existing section boundaries, otherwise headers/offsets elsewhere in the file break. Re-encode to base64 for the response. For logging, compute `hmac.new(b"AUDIT_KEY", user_email.encode(), hashlib.sha256).hexdigest()` and POST `{"id":..., "user_hash":...}` to the Log Service. Serve this as a long-running background process (Flask `app.run(host="0.0.0.0", port=8000)` or similar, backgrounded) so the test suite can hit it repeatedly. Validate with `readelf -S`/`readelf -x .rodata` on your redacted output before finishing.
