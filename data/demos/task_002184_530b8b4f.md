Write `deploy.sh` to first `mkdir -p` the backup/spool directories, then run `cargo init --name edge_agent /home/user/edge_agent` (or `cargo new`) only if the project doesn't already exist, `cargo build --release` inside it, run the resulting release binary, and `chmod +x` the script itself. In `main.rs`, for the backup step, shell out with `std::process::Command::new("tar").args(["-czf", "/home/user/backups/telemetry_latest.tar.gz", "-C", "/home/user", "telemetry_data"]).status()` (using `-C` plus a relative path keeps the archive's member names sane so `tar -tzf` finds `sensor_1.json`/`sensor_2.json` regardless of leading-path expectations) and check the command succeeded. For health checking, read `/home/user/device_health.log` into a String and check `contains("CRITICAL")` exactly (case-sensitive substring, not a specific line format). If found, write `/home/user/smtp_spool/alert.eml` using `write!`/`fs::write` with the exact header block, making sure there is precisely one blank line between the `Subject:` header and the body line — a common bug is an extra or missing blank line, which the verifier's `awk` check will catch. Finally run `/home/user/deploy.sh` once to confirm both the tarball and the alert file are produced.