Write `/home/user/update_mail_routes.py` as an idempotent merge, not a blind append. Load `/home/user/deploy_data.json`, and for each entry: apply the `email_domain` default of `<service_name>.internal` only when the key is absent; if `ip_address` or `port` is missing, skip adding it to the config and instead append the exact error line to `/home/user/deploy_errors.log` — but truncate/clear that log at the very start of each run so re-running doesn't duplicate error lines. For the `.conf` file, parse the existing content into an ordered structure of named blocks (e.g. with `configparser` using `[section]` headers, or a manual parser that finds `[name]` blocks) so you can update a block in place when the service name already exists, append a new block when it doesn't, and leave any unrelated existing blocks (like one not present in the JSON) completely untouched. Preserve exactly one blank line between blocks when writing back. The idempotency check will run your script twice — make sure the second run produces byte-identical `.conf` output and doesn't append duplicate errors on top of the fresh log.
