Build the C sanitizer around a clear multi-line record model: recursively walk `<input_dir>` (e.g. `nftw` or manual `opendir` recursion) for `*.log` files, then parse each file line-by-line, starting a new record whenever a line matches the exact `[YYYY-MM-DD HH:MM:SS]` prefix (validate the literal bracket/digit/colon/dash structure, not just "starts with `[`") and appending any non-matching line to the current record's buffer. Drop the whole record only if `[SPAM-MODULE]` appears anywhere in its accumulated text. Mirror the relative path from input to output (create subdirectories with `mkdir -p`-equivalent logic), and achieve atomic writes by writing to `<file>.tmp` in the destination directory then `rename()`-ing over the final name — never write the final filename directly, and make sure no `.tmp` files are left behind on success. Test your binary directly against the two given corpora directories before touching the service stack, since the verifier fuzzes your binary's correctness there. Only after that, edit `webapp/config.yaml`'s `log_dir` to point at the raw directory, add the `X-Sanitizer-Enabled: true` proxy header inside nginx's `/` location block, restart via the provided script, and finally run your sanitizer once end-to-end from raw to processed.
