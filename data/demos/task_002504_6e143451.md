First transcribe `/app/architect_memo.wav` (e.g. with `openai-whisper` or `SpeechRecognition`/`pocketsphinx`) and carefully read the transcript to extract the exact alphanumeric authorization token spoken in it — treat it as case- and spelling-sensitive, don't guess or normalize it. Build the Flask/FastAPI app bound strictly to `127.0.0.1:8000`, with a dependency/middleware that checks the `Authorization: Bearer <token>` header against the recovered token on every protected route, rejecting with 401/403 when missing or wrong before any endpoint logic runs. For `/path`, load `nodes`/`edges` from `/app/infrastructure.db` with parameterized queries, build a directed weighted graph (e.g. with `networkx`'s `shortest_path`/`dijkstra`, or a recursive CTE) keyed on `latency`, and return the path as node *names* (not ids) plus the summed latency — double check you're minimizing total latency, not hop count. For `/export`, aggregate `SUM(latency)` grouped by each node's outgoing edges (nodes with no outgoing edges should still appear with 0), order descending by that sum, and emit as `text/csv` with the exact two-column header `node_name,total_outgoing_latency`. Start the server and hit both endpoints yourself with `curl` (with and without the header) to confirm the auth gate and computed values before finishing.