Start by extracting the three baseline parameters (max allowable feature correlation, minimum allowable MSE, and the exact model architecture with hyperparameters) from `/app/experiment_spec.png` using OCR — install `tesseract-ocr` and `pytesseract` (or run `tesseract` directly on the image), and sanity-check the extracted text since OCR often mangles decimal points, punctuation, or hyperparameter syntax; re-crop/preprocess (grayscale, threshold, upscale) the image if the first pass looks garbled. Build `/home/user/gatekeeper.py` to: load the CSV, compute the pairwise absolute Pearson correlation across feature columns (excluding `target`) and reject if any off-diagonal value exceeds the extracted threshold; otherwise do a strictly sequential (non-shuffled) 80/20 split, instantiate and fit exactly the model type/hyperparameters read from the image, compute test-set MSE, and reject if it's below the extracted MSE floor (indicating leakage), else accept. Print exactly `STATUS: REJECT` or `STATUS: ACCEPT` with nothing else on stdout. Before finishing, test your script against a few datasets you construct yourself that clearly trigger each rejection branch and the accept branch, to make sure the correlation and MSE comparisons use the correct direction and strictness (`>` vs `>=`).