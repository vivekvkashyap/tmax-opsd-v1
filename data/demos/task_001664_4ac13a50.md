Before optimizing for speed, read `/app/slow_processor.py` carefully and replicate its exact filtering/normalization logic in Go — correctness (matching the reference DB state) is graded alongside the 2.5s runtime threshold, so don't drift from the spec while chasing performance. Start the Redis/Postgres services from `/app/docker-compose.yml`, create the `cleaned_metrics` table, and confirm connectivity manually first. In the Go program: use a blocking/batched pop (e.g. `LPOP` in a loop or `BLPOP`, or better, pull multiple items per round-trip with a Lua script or pipelining) since single-item round-trips to Redis for 200k messages will likely blow the time budget — pipelining is the key trick to hit 2.5s. Maintain the previous raw point for the distance-dedup check (first point always passes), then a fixed-size rolling window (slice/ring buffer of last 50 valid distances-from-origin) to compute mean and population stddev (divide by N, not N-1) for the z-score of the current point, filtering out |z|>3 and only inserting once the window has reached 50 entries. Batch your Postgres inserts (multi-row INSERT or `COPY`) rather than one-row-at-a-time to stay under the time budget. Build and test incrementally on a small synthetic Redis list before running the full 200k-message benchmark.
