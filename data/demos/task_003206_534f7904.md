Scaffold with `cargo new compliance_auditor` in `/home/user/`, add `rusqlite` (with the `bundled` feature to avoid needing system sqlite) and `oxigraph` to `Cargo.toml`. First query the SQLite DB for `SELECT e.name, er.role_uri FROM employee e JOIN employee_roles er ON e.id = er.emp_id WHERE e.department = 'Contractor'` and collect (name, role_uri) pairs. Then load `/home/user/resources.ttl` into an oxigraph `Store` via `load_from_reader` (or equivalent) with Turtle format. For the transitive check, don't hand-roll a fixed-depth chain — either write a SPARQL query using a property path like `role:X (ex:inheritsFrom)*/ex:canRead ex:CustomerData` to capture arbitrary-depth inheritance in one shot, or do it programmatically by repeatedly resolving `ex:inheritsFrom` edges (following the role graph to a fixpoint) and then checking `ex:canRead ex:CustomerData` at any level reached. The common bug is only checking one level of inheritance instead of the full transitive closure — make sure a role that inherits from a role that inherits from a role with access still counts. Cross-reference the query results against the contractor list, sort violators alphabetically by name, and serialize to the exact JSON shape at `/home/user/violation_report.json`.