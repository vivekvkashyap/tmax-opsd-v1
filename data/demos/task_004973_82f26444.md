This has two independent deliverables — fix the infra first, then write the validator, and test each in isolation. For the infra: edit `/app/nginx.conf`'s `proxy_pass` target from port 8000 to 5000 (matching where the Flask/gunicorn app actually binds per `start.sh`), then run `bash /app/start.sh` and confirm end-to-end with a real `curl -X POST http://localhost:8080/upload` followed by `redis-cli LLEN data_queue` to see the count increment, rather than assuming the config edit alone is sufficient. For `validate_data.py`, implement the four checks as a strict ordered pipeline using pandas: inner-join the input CSV with `/app/sensor_metadata.csv` on `sensor_id`, keep only rows with `status == 'active'`; among those, treat both NaN and empty-string as missing in `value`, reject if missing fraction is strictly greater than 10%, else drop missing rows; compute sample mean/std of the remaining `value`s and drop rows deviating more than 3 standard deviations (recompute stats from the surviving active-and-non-missing set, not the original file); run `scipy.stats.ttest_1samp` against a population mean of 50.0 and reject if the two-sided p-value is strictly less than 0.01. Print exactly "ACCEPT" or "REJECT" and set the exit code (0/1) accordingly — test against both clean and adversarial-style CSVs you construct yourself to confirm boundary behavior (e.g. exactly 10% missing should NOT reject, since the rule is "strictly more than").