Handle the three deliverables independently since the verifier checks each separately. For the vendored agent: run `make` in `/app/sensor-agent-1.0.0`, read the linker error carefully — a missing math-library link (`undefined reference to sqrt`-style errors) means the Makefile's compile/link command is missing `-lm`; add it, rebuild, then launch `./sensor-agent &` and confirm the process stays alive (check with `ps`/`pgrep`). For setup_env.sh: make directory creation and `chmod 750` idempotent (they naturally are), but for ACLs use `setfacl -m u:nobody:r-x /home/user/sensor_data` — `setfacl -m` is itself idempotent (reapplying the same rule doesn't error or duplicate), and verify with `getfacl`. Capture the agent's PID via `pgrep -f sensor-agent` (or the PID from the `&` launch) into sensor.pid. For the C filter: write a line-buffered reader using `fgets` with a buffer sized well beyond 2048 (or dynamically grow via `getline`, which handles arbitrarily long lines) so long lines aren't truncated; for each line, scan for `;`, `&`, `|`, `$`, and the literal substring `../` with simple character/substring checks, and only `fputs` the line through if none match — test against both malicious and benign sample lines yourself before considering the filter done.
