Design the pipeline in stages and verify losslessness at each step before optimizing for size. (1) Use `ffmpeg` once to extract exactly 5 seconds at 10fps as grayscale raw frames (e.g. `-t 5 -r 10 -pix_fmt gray -f rawvideo`), reading the output via a pipe or temp file, and determine frame width/height from `ffprobe` so you can reshape the byte stream into individual frames. (2) Group frames into 5 chunks of 10 frames each (1 second per chunk at 10fps). (3) Within a chunk: store frame 0 raw; for each subsequent frame compute a byte-wise difference from the *previous* frame (mod 256, since pixel bytes are unsigned) — this exploits temporal redundancy in slow/static video, which is what makes RLE effective afterward. (4) Apply RLE to the flattened delta bytes (e.g. (value, run-length) pairs); this must be reversible so you can undo it exactly during decode-side verification. (5) When appending each chunk to `/home/user/dataset.archive`, wrap the file open/write/close in `fcntl.flock` (LOCK_EX) to simulate safe concurrent writers. (6) Track each chunk's write offset and compressed byte length as you go, and hash the *original uncompressed* raw payload (before delta+RLE) for the manifest — verification will decode your chunks and compare byte-for-byte (MSE=0) against fresh `ffmpeg` extraction, so test your own decoder against your encoder before finishing.
