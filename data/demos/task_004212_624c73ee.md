Reverse-engineer methodically before writing any C: run `strings /app/legacy_checker` for embedded literals, `objdump -d` (or `gdb`/`ltrace`) to trace what the binary checks — look specifically for a fixed byte-prefix comparison, a count of some repeated byte (e.g. newlines) compared against a constant, and a substring search that causes rejection when present. Confirm each hypothesized rule by crafting small test files by hand and running them through `/app/legacy_checker`, checking its exit code after each, until your mental model matches its behavior on edge cases (e.g., off-by-one counts, boundary strings). Only then write `/home/user/fast_checker.c` implementing exactly those rules with straightforward file I/O and string/byte comparisons, compile with `gcc -o /home/user/fast_checker fast_checker.c`, and run a bash loop over every file in `/app/corpora/clean/` and `/app/corpora/evil/`, diffing your binary's exit code against the legacy binary's exit code for each file until they match 100%. Don't stop at "looks right" — the adversarial corpora are designed to catch subtle off-by-one or case-sensitivity mistakes.
