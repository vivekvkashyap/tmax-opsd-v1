Do the OCR extraction first and verify it: run `tesseract /app/sensor_metadata.png stdout` and manually check the resulting `sensor_id,location` mapping makes sense (re-run with different `--psm` values if the output looks garbled) before joining it to `/app/sensor_readings.csv` on `sensor_id`. Build the HTTP server (Flask/FastAPI is simplest) listening on `127.0.0.1:8000` exposing `GET /api/v1/sensor_stats?location=<name>`, filtering the joined dataframe by location per request. For each stat: fit `sklearn.decomposition.PCA(n_components=1)` on the raw (unscaled) `temp`, `pressure`, `vibration` columns and read `explained_variance_ratio_[0]`; compute a bootstrap 95% CI for the mean of `output_metric` by resampling with replacement 1000 times with `numpy.random.seed(42)` set before resampling, taking the 2.5/97.5 percentiles of the resample means; run `scipy.stats.ttest_1samp(output_metric, 50.0)` and take the p-value. Round every numeric value to 3 decimals and match the exact JSON key names/shape given. Test manually with `curl` for both `North_Wing` and `South_Wing` before finishing, and keep the server running in the background.