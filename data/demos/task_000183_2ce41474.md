Because the grader recomputes an identical reference pipeline and compares numbers with tight tolerance, exact adherence to the specified order of operations and RNG usage matters more than the science itself. Set `numpy.random.seed(42)` once at the very start, then draw the full betas and gammas arrays (size M=1000, via `numpy.random.uniform`) up front before the simulation loop — drawing per-iteration or in a different order will desynchronize the RNG stream from the reference. Solve the SIR ODE with `scipy.integrate.odeint` using the exact initial conditions and `numpy.linspace(0, 100, 1000)` time grid, and record the max of the infected compartment per run into an I_max array of length 1000, preserving iteration order. Compute the sample mean, then bootstrap: run exactly 2000 resamples, each drawing 1000 items from I_max with replacement using `numpy.random.choice` (continuing to draw from the now-advanced RNG state, not reseeding), take the mean of each resample, and use `numpy.percentile` at 2.5 and 97.5 for the CI. Write mean_I_max, ci_lower_95, ci_upper_95 to the JSON at the specified path with exactly those key names.