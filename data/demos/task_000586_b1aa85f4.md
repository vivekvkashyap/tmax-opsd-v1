Install prerequisites first (`pip install rdflib`, `apt-get install jq` or equivalent) and confirm `python3 /home/user/run_sparql.py --data ... --query ...` runs before scripting around it. In `check_deps.sh`, build the SPARQL query by substituting the `$1` argument into the URI template with `printf`/heredoc into a temp file (e.g. `mktemp`), being careful that the server name is inserted only inside the URI angle brackets, not used to construct raw SQL/shell injection surface. Run the query against `/home/user/graph_backup.nt` and pipe the SPARQL JSON results through `jq` to pull out `.results.bindings[].dep.value`, then use `sed`/`awk`/`basename`-style string manipulation (e.g. `${uri##*/}`) to strip the `http://example.org/server/` prefix down to the bare server name. Sort the resulting names alphabetically with `sort`, then use `jq -n --arg` or manual string building to emit the exact JSON shape `{"server": ..., "dependsOn": [...]}` to `/home/user/deps_<name>.json`. Append one line per run to `/home/user/query_index.csv` in `ServerName,Dep1|Dep2` form (joined with `paste -sd'|'` or `jq -r 'join("|")'`), leaving the right side empty when there are no dependencies. Clean up the temp query file when done.
