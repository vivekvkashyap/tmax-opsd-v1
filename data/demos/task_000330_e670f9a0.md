Build this incrementally and verify each layer with curl/openssl before adding the next. Generate the cert first (`openssl req -x509 -newkey rsa:2048 -nodes -keyout key.pem -out cert.pem -days 365 -subj "/CN=Migration Gateway"`) and confirm it loads. Create the three backend directories with their `health.txt`/`data.txt` contents, then get plain HTTP backends serving correctly (e.g. spawn `http.server` per port via subprocess, one per directory) and curl each `/health.txt` and `/data.txt` directly before adding TLS or proxying. Implement the health-check loop as a background thread that GETs `http://127.0.0.1:{port}/health.txt` every 2s per backend, tracks each backend's healthy/unhealthy state, and on failure kills the process (by PID) and respawns it — log the exact `[HEALTH] ... DOWN. Restarting...` and `... UP.` lines only on state transitions (not every check) so the log doesn't spam duplicate UP lines. Build the HTTPS layer (e.g. `http.server` with an SSL-wrapped socket using your cert/key, or `ssl.wrap_socket`/`SSLContext`) that proxies incoming GET requests to a round-robin pick among currently-healthy backend ports, forwarding the backend's response body. Launch it backgrounded and manually curl `https://localhost:8443/...` with `-k` multiple times, then kill one backend process and confirm the log records the DOWN/UP cycle and the proxy keeps serving, before leaving the process running.