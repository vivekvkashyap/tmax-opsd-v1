Use `git bisect` rather than manually scanning 200 commits. Write a small bisect-test script that, for a given commit, builds/runs processor.go against data.json and checks whether the computed totals match the known-correct formula `Total = Quantity * Price * (1.0 - Discount)` (you can compute the expected totals yourself from data.json's values and compare against the program's output for a fixed known input) — exit 0 if correct, 1 if wrong. Then run `git bisect start`, mark `HEAD` bad and the `v1.0` tag good, and either step through interactively or run `git bisect run <your-test-script>` to let git binary-search automatically; capture the reported first-bad commit hash and `git bisect reset` afterward. Write only the full 40-char hash (no extra whitespace) to bug_commit.txt. Once found, look at that commit's diff (`git show <hash>`) to see exactly what changed in the discount formula, and apply the inverse fix at HEAD so `go run processor.go data.json /home/user/fixed_output.json` reproduces the correct totals — verify by hand-computing the two expected totals before trusting the output file.
