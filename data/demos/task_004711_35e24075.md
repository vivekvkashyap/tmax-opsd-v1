Build the Bash script as a thin wrapper around one carefully constructed SQL query. First ensure `sqlite3` is present (`command -v sqlite3 || apt-get install -y sqlite3`) and run `REINDEX;` against `/home/user/audit.db` before querying. For the core query, write a `WITH RECURSIVE` CTE that starts from `role_inheritance` to expand each role into the full set of roles it inherits from (walk parent→child links transitively, e.g. anchor on each `(parent_role_id, parent_role_id)` and recurse via `JOIN role_inheritance ri ON ri.parent_role_id = cte.child_role_id`, or however the schema's direction implies "parent inherits child's perms") — then join `user_roles` to that expansion to get each user's full effective role set, join `role_permissions` to get their effective `resource_name`s, and LEFT/anti-join that against `access_logs` to find log rows whose `(user_id, resource_name)` pair is NOT in the effective-permissions set (a NOT EXISTS or NOT IN subquery works well). Rank each user's violations by `timestamp` descending using `ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY timestamp DESC)` and keep only rank <= 2. Order final results by username ascending, timestamp descending, and use sqlite3's `-header -csv` mode piped to the output file. Make the script executable and run it standalone to produce `/home/user/recent_violations.csv`.