First recover the gradient threshold: run `tesseract /app/threshold_note.png stdout` (try different `--psm` values if the first pass garbles digits) and read off the numeric value from the "Gradient threshold" line — don't guess a round number, use exactly what OCR recovers (sanity-check it against the clean/evil corpora once your script is written). Then write `/home/user/classifier.py` to open the HDF5 file with `h5py`, load the `density` dataset as a numpy array, and check two conditions in order: (1) `np.isnan(data).any() or np.isinf(data).any()` -> evil; (2) `np.max(np.abs(np.diff(data))) > threshold` -> evil. Exit 1 if either holds, else exit 0. Wrap file access in try/except and treat unreadable/malformed files as evil (exit 1) as a failsafe. Before trusting your threshold value, validate empirically: loop over every file in `/app/simulations/clean/` and `/app/simulations/evil/`, run the classifier, and confirm exit codes are 0 and 1 respectively for 100% of each set — adjust your OCR reading (not the underlying logic) if any file misclassifies, since a slightly-off threshold is the most common naive mistake.