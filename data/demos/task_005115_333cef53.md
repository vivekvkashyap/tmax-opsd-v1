Split this into two independent deliverables and verify each separately. For the vendored package, open `/app/root_finder_pkg/setup.py` and read it as a Python literal — a `setup(...)` call needs commas between every keyword argument; find the missing comma (likely between two adjacent `key=value` lines) and add it, then confirm with `pip install -e /app/root_finder_pkg` followed by `python -c "import root_finder_pkg"` actually succeeding rather than assuming the fix worked. For the sanitizer, load the `.npz` with `numpy.load`, and use vectorized (not per-element Python-loop) operations for efficiency: compute the residual array `x**3 + a*x + b`, then check two conditions across the whole arrays at once — `np.any(np.isnan(x)|np.isnan(a)|np.isnan(b)|np.isinf(...))` for NaN/Inf, and `np.any(np.abs(residual) > 1e-4)` for the equation violation. If either condition is true anywhere in the arrays, treat the file as evil (`sys.exit(1)`); otherwise clean (`sys.exit(0)`). Before finishing, manually run your sanitizer against a few files in `/app/corpus/clean` and `/app/corpus/evil` to confirm exit codes match expectations, since silent logic errors (e.g. using `np.all` where you need `np.any`) are the most common failure mode here.