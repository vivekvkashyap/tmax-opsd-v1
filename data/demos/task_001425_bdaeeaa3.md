Break this into independent, individually-testable stages before wiring them together. First, extract frames from the mp4 with `ffmpeg -i pendulum_experiment.mp4 frame_%04d.png` (or read directly via OpenCV `cv2.VideoCapture`), then per frame use an HSV color mask to isolate red pixels (red wraps hue 0, so mask both near-0 and near-180) and take the centroid x-coordinate (e.g. via `cv2.moments` or contour center) to build a time series at 30fps; normalize by dividing by the first peak/initial displacement so the max is exactly 1.0 — this is your reference trajectory, not something to hardcode. Second, open `integrator.cpp` and look closely at the step-size adaptation: the bug is that step size gets multiplied by the same factor regardless of whether the error check passes or fails, so it never shrinks on rejection — replace the accept/reject branching with proper RK45 logic where the step is scaled by a factor derived from `tolerance/error` (e.g. `pow(tol/error, 0.2)`), and step is only accepted (advancing time) when error is within tolerance. Compile and test the standalone binary against a few known-decaying-oscillation inputs to sanity check before hooking it into `orchestrator.py`, which should sweep c in [0,1] (fine enough grid to get 2 decimal precision), interpolate simulated output onto the video's 30fps timestamps (`numpy.interp` or `scipy.interpolate`), compute MSE against your reference trajectory, and keep the minimizing c. Finally stand up the HTTP service on 127.0.0.1:8050 with a GET /api/best_fit handler that checks the `X-Auth-Token: physics-2024` header (reject with 401/403 if absent/wrong) and returns the JSON with your computed optimal_c, mse, and datapoints_compared, then leave it running in the background.