In preprocess.py, load the CSV with pandas, compute standardization per column using `(col - col.mean()) / col.std(ddof=1)` (sample std, not population — a common mistake is leaving ddof at its numpy default of 0), and write out with `to_csv(..., index=False, float_format='%.6f')` to get exactly 6 decimal places. For evaluate_pipeline.sh: run the preprocess script twice to separate output paths (you'll need to either parameterize the script's output path or copy `/home/user/processed_data.csv` to `run1.csv`/`run2.csv` between runs), then use `diff -q run1.csv run2.csv` or `cmp` to decide REPRODUCIBLE vs DIVERGENT and write that word (with trailing newline) to reproducibility.log. For the accuracy check, embed a `python3 -c "..."` (or heredoc) inside the bash script that reads processed_data.csv with pandas, computes per-column mean/std(ddof=1), and writes ACCURATE/INACCURATE to accuracy.log based on the given tolerances. For the benchmark, wrap the preprocess.py invocation with `/usr/bin/time -p python3 /home/user/preprocess.py 2> /home/user/benchmark.log` — note `time -p` writes to stderr, so you must redirect stderr, not stdout, to capture the real/user/sys lines. Make the script executable with `chmod +x`.