Fix the linking problem structurally, not with an environment-variable workaround (the task explicitly forbids `LD_LIBRARY_PATH` or copying the `.so`). In `setup.py`, add `include_dirs`, `library_dirs`, and `libraries=['processor']` pointing at `/home/user/app/legacy`, plus `runtime_library_dirs=['/home/user/app/legacy']` (or pass `extra_link_args=['-Wl,-rpath,/home/user/app/legacy']`) so the built `.so` embeds an rpath and the loader finds `libprocessor.so` at runtime without extra env vars — verify with `ldd` on the built extension that it resolves. Build with `python3 setup.py build_ext --inplace` and confirm `import _legacy_wrapper` works from a plain shell. Then implement `main.py` as a true line-by-line state machine (states like IDLE/IN_BLOCK) tracking `START <ip>` → accumulate `PAYLOAD:` lines (strip only the `PAYLOAD:` prefix and trailing newline, keep everything else including internal spaces/case) → `END`; maintain a per-IP counter and skip (don't even buffer) any block once that IP has already completed 2 blocks. For each accepted block, concatenate its payload lines in order, call `_legacy_wrapper.process(...)`, and append the integer result as its own line to `/home/user/app/results.txt` in the order blocks complete.
