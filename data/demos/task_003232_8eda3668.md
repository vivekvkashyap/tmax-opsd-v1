First recover the cost table: run OCR (e.g. `tesseract /app/plan_costs.png stdout`) on the image and carefully parse each `OPERATION_NAME: <number>` line into a dict — double-check OCR didn't mangle digits (0/O, 1/I) since these costs drive every downstream calculation. Then write `/home/user/plan_evaluator.py` as a pure recursive tree-cost evaluator: read one line of JSON from stdin, define `cost(node)` that looks up `node["operation"]` in your extracted cost map (default 0 if missing/unknown), then recurses into `node["left"]` and `node["right"]` if present, and into every element of `node["children"]` if that key holds a list — summing all contributions — and print only the final integer total (no extra text, since correctness will be checked against a hidden oracle across many random nested plans, so make traversal exhaustive and defensive against missing/malformed subkeys rather than assuming only one child-representation style per node).