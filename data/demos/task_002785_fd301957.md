Fix the build first: open `/app/extractor/Makefile` and check for missing tabs (make requires tab-indented recipe lines, not spaces) and typo'd target/variable names, then compile `main.c` and fix compiler errors one at a time (a common one here is a missing `#include <stdint.h>` needed for fixed-width integer types, plus a straightforward syntax error) — iterate with `make` until it builds `extract_features` cleanly. Run it on `/app/signal.wav` to produce `features.bin`, and before writing the Go parser, use `xxd`/`hexdump` on a few records to confirm your assumed 8-byte little-endian layout (uint32 timestamp + float32 power) actually matches. In Go, read the binary with `encoding/binary.LittleEndian`, compute the 5-point centered moving average with truncated edge windows (window shrinks near the boundaries rather than wrapping or zero-padding), then scan the smoothed series for strict local maxima above the 0.5 threshold (strictly greater than both neighbors), and marshal the results to `/app/peaks.json` in the given object shape. Since scoring is F1 against hidden ground-truth peak times with a ±30ms tolerance, validate your peak-detection logic against the known synthesized beep pattern in the WAV rather than assuming your first implementation is correct — check for spurious peaks caused by noise near the threshold.