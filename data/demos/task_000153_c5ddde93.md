Extract the routing rules from the image with `tesseract /app/waf_rules.png stdout` (try different `--psm` values if text comes out garbled) and read the OCR output carefully rather than assuming a generic WAF shape — the diagram encodes specific per-path rules (which prefixes trigger which parameter checks, which regex/substring conditions decide ALLOW vs BLOCK, and a default fallthrough rule for unmatched paths). Implement `waf_tester.py` as a straightforward if/elif chain over `path` matching against the rules you actually read off the image (e.g., prefix checks with `str.startswith`, parameter regex/substring checks with `re.match`/`in`), applied in the priority order shown in the diagram, with a final default rule for anything unmatched. Load `/app/requests.json`, evaluate each entry's `path`/`query_params` through your state machine in order, and write a JSON array of `"ALLOW"`/`"BLOCK"` strings to `/home/user/predictions.json` in the same order as the input. Since scoring requires 100% exact match, test your logic against a couple of requests by hand before finalizing, double-checking edge cases like missing parameters.