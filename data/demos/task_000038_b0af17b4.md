Split this into the OCR extraction phase and the algorithm phase, and get each fully correct independently. For extraction: use `ffmpeg -i /app/db_logs.mp4 -vf fps=<rate> frame_%03d.png` to pull frames, then run `tesseract` on each frame image to get text, concatenate and deduplicate the recognized `LINK NODE_A NODE_B CAPACITY` lines (adjacent frames of a screen recording often repeat the same visible lines, so dedupe exact-duplicate lines while preserving first-seen order), and write the cleaned lines verbatim to `/home/user/base_graph.txt`. Manually spot-check a handful of OCR'd lines against the actual video frames (view a few extracted PNGs) since OCR misreads digits and node numbers easily — this is the step most likely to introduce silent errors. For the algorithm: implement the widest path / maximum bottleneck path query in C++ using a Dijkstra-like approach where you relax by `min(bottleneck[u], weight(u,v))` and maximize instead of minimize (a priority queue ordered by descending bottleneck works well); read N, E, edges, Q, and queries exactly per the stdin format, and print `-1` for unreachable pairs. Since grading fuzzes random graphs against a hidden oracle for byte-exact stdout, test your compiled `/home/user/backup_router` on small hand-computed graphs (a simple path, a graph with multiple parallel routes, and disconnected components) to confirm both correctness and exact output formatting (no extra whitespace or trailing text).