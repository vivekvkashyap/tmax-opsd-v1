Start by reading `/app/bayes-metrics-tracker-1.2.0/setup.py` in full to find the deliberate perturbation rather than guessing — expect a hardcoded exception guarded by an environment-variable check (fix by either setting that env var before installing or removing the guard) plus a missing runtime dependency (add it to `install_requires`, likely `numpy` since the package does covariance math) that would otherwise break the install even after the exception is cleared. Install with `pip install -e /app/bayes-metrics-tracker-1.2.0/` and confirm `import bayes_metrics_tracker` works before writing the experiment script. In `/home/user/run_experiment.py`, load the sentences, embed them with the specified sentence-transformers model, then implement 5-fold cross-validation manually (split embeddings into 5 folds, for each candidate `prior_variance` in the given list fit `CovarianceEstimator` on 4 folds and score negative log-likelihood on the held-out fold, average across folds) and pick the value that minimizes average NLL — don't just eyeball one split. Refit the estimator with the best hyperparameter on the full dataset, then use `bayes_metrics_tracker.Logger` to save the resulting `(embedding_dim, embedding_dim)` covariance matrix to `/home/user/artifacts/best_cov_matrix.npy`, and sanity-check the matrix shape and symmetry before finishing.
