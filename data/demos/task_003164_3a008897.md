Build this as a pipeline with clear stages, not one monolithic script. First run `ffmpeg -i /app/test_video.mp4 -vf fps=1 /home/user/frames/frame_%d.png` (adjust so numbering starts at frame_1, e.g. post-process rename since ffmpeg's `%d` starts at 1 by default with fps filter, but verify actual output count N). Then write a Python script using Pillow/numpy to load each frame, compute per-channel mean and std (R,G,B order) into a feature matrix indexed by sorted frame ID. The critical correctness point is normalization: compute the StandardScaler's mean/std using ONLY the first N//2 frames (by ID order), then apply that same transform to ALL frames including the test half — fitting on the full set would leak test statistics into training and fail the leakage check. Persist the fitted means/stds (e.g., to a small JSON or in-memory at server start) so `/stats` can report them rounded to 2 decimals. For `/recommend`, compute Euclidean distance in the scaled feature space between the query frame and every other frame, sort ascending, and return the 3 nearest IDs (excluding self) as integers. Run the HTTP server (Flask or http.server) as a background process bound to 127.0.0.1:8080, and verify it responds before finishing.