Build this bottom-up and verify each piece with a direct command before wiring it together. Create the directory tree, then generate the exact-size dummy file with `dd` or `fallocate` (e.g. `dd if=/dev/zero of=dummy.dat bs=1 count=0 seek=5242880` or `truncate -s 5242880`) and confirm with `stat -c %s`. Write `settings.json` with exactly the two required keys, valid JSON, no extras. For the Go service: parse the JSON config with `encoding/json`, walk `monitor_dir` with `filepath.Walk`/`os.ReadDir` summing file sizes, convert bytes to MB with integer floor division (`totalBytes / (1024*1024)`), and serve `/status` on `127.0.0.1:8080` via `net/http` returning the exact JSON field order/keys specified. Run `go mod init monitor`, build the binary, then launch it detached (e.g. `nohup ./monitor_service &` or `setsid`) so it survives the task and doesn't die when your shell session ends — verify with `curl` immediately after starting. For `check_conn.sh`, check the port is listening (e.g. `nc -z 127.0.0.1 8080` or `/dev/tcp`), then `curl -s` the endpoint and write only the raw JSON body to the log file, chmod +x, and run it once to confirm the log matches the live curl output exactly.