For Step 1, use `awk -F'|'` to filter `/home/user/raw_obs.txt` on the subject_id field (trim whitespace around the pipe-separated fields), then emit `time,B_conc` as a comma-separated header+rows to `/home/user/clean_obs.csv` — verify by eye that only subject 42's rows survive and values look sane. For Step 2, in `mc_fit.py`, set `np.random.seed(42)` before generating exactly 100,000 (k1,k2) pairs from `Uniform(0.1,1.0)` each — order and count of RNG calls matters for reproducibility, so generate all k1's then all k2's (or paired draws) consistently and don't call np.random anywhere before the seed. For each pair, solve the 2-compartment ODE analytically (closed form `B(t)=k1*A0/(k2-k1)*(exp(-k1 t)-exp(-k2 t))`) or numerically (`scipy.integrate.odeint`/`solve_ivp`) at the CSV's time points, compute MSE against observed B_conc, and track the minimum. Save best params rounded to 3 decimals as JSON, and the fitted curve as CSV. For Step 3 in R, load jsonlite for the params, recompute the analytical B(t) with the same closed-form formula, compare max abs difference against the numeric CSV (should be tiny, well under 0.05), read `k2_threshold` from `/home/user/reference.json`, and write `VALID`/`INVALID` then `FAST`/`SLOW` on two lines to `/home/user/validation_result.txt`.