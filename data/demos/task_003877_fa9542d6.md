Since /app/score_activity is a black box with only 5 numeric inputs, treat formula recovery as controlled experimentation before writing the pipeline: call it repeatedly holding all-but-one input fixed (e.g. vary only clicks while age/location/impressions/duration stay constant) and observe how the output changes — linear terms show up as constant differences per unit change, ratios (like clicks/impressions) show up as non-linear coupling between two specific inputs, and modular effects (like location_code % something) show up as periodic jumps when you sweep location_code across a wider range than the modulus. Once you have a hypothesis, verify it by predicting several outputs analytically and comparing exactly against the binary's real output over varied ranges (including edge cases like impressions=0) before committing to it in C++. For the data pipeline itself: stream activity.csv while accumulating sums of clicks/impressions/duration_sec keyed by uid in a hash map (avoid re-reading the file per user), then join against demographics.csv by uid (defaulting missing activity to zeros), apply your recovered formula, and write uid,score sorted ascending with 4 decimal places. Validate your recovered-formula scores against a handful of direct binary calls as a final sanity check before trusting the full-dataset MSE.