Build this as a straight-line pandas/scipy pipeline and validate each intermediate value before moving to the next. Load `/home/user/raw_sensor_data.csv` and use `groupby('date')['reading'].mean()` to get the true per-day mean — don't hand-roll the averaging, let pandas handle grouping to avoid off-by-one or missing-date bugs. Load `/home/user/legacy_daily_summary.csv` and merge the two on `date` with an inner join so only dates present in both are compared. Compute `abs_diff = (true_mean - legacy_mean).abs()`, then `max_absolute_error = abs_diff.max()` and `mean_absolute_difference = abs_diff.mean()`. For the confidence interval, use `scipy.stats.sem(abs_diff)` for the standard error and `scipy.stats.t.interval(0.95, df=len(abs_diff)-1, loc=mean_absolute_difference, scale=sem)` — using the t-distribution (not normal) with `n-1` degrees of freedom is the detail a naive attempt often gets wrong. Round all four output values to exactly 4 decimal places with Python's `round()` before writing them to `/home/user/report.json` with the exact key names given. Print the intermediate DataFrame after the merge to sanity-check row counts and spot-check a couple of `abs_diff` values by hand before trusting the final rounded numbers.