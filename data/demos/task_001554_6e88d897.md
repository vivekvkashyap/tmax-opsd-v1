Install `tesseract-ocr` first, then OCR `/app/motif_spec.png` (e.g. `tesseract /app/motif_spec.png stdout`) to recover the nucleotide-to-integer mapping and the convolution kernel array — treat OCR output as noisy and manually re-check ambiguous characters (0/O, 1/l/I, negative signs) against the image before trusting the numbers. Do not hardcode any guessed mapping/kernel values; only use what you actually read from the image. Implement `/home/user/process_seq.sh` in pure Bash: read `$1` as the sequence, build a Bash array by iterating characters (`for (( i=0; i<${#seq}; i++ )); do c=${seq:i:1}; ...`) and mapping each to its integer value via a case/if chain built from the recovered mapping, defaulting unrecognized characters (like 'N') to 0. Then slide the recovered kernel across the numeric array: for each start index i from 0 to L-K, compute the dot product of value[i..i+K-1] with the kernel using Bash integer arithmetic (`$(( ... ))`), collecting results into an output array, and print them space-separated with `echo "${out[*]}"`. Test your script against several sequences of varying length (including edge cases where L < K) to sanity-check the sliding-window logic before finalizing, and make the script executable.