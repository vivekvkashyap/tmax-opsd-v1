Write the C program to read `REPORTER_HOST`/`REPORTER_PORT` via `getenv`, failing with a stderr message and exit 1 if either is NULL. Call `statvfs("/home/user/data_dir", &st)` and compute free bytes as `(unsigned long long)st.f_bavail * st.f_frsize` — use a wide enough integer type to avoid overflow, and format it into the exact string `DATA_DIR_FREE_BYTES:<bytes>\n` with no extra whitespace. Open a TCP socket (`socket`, `getaddrinfo` or manual `sockaddr_in` with `inet_pton`/`atoi` on the port string), `connect()`, then `send()` the formatted message and `close()`; on any syscall failure print to stderr and exit 1. Compile with `gcc -O2`. Append the two `export` lines verbatim to `/home/user/.bash_profile` (don't overwrite existing content — append). Build `run.sh` that does `source /home/user/.bash_profile` then `exec /home/user/health_reporter` (or plain call), and `chmod +x` it. Before finishing, sanity-test yourself: start `nc -l <port>` locally, run `run.sh`, and confirm the exact line format arrives — a common bug is including the port as a literal string without proper numeric parsing or forgetting the trailing newline.