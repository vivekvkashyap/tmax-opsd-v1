This is a graph/tree problem that must be solved with pure shell tools — do it iteratively, not with a fixed-depth chain of joins. For the subordinate-tree salary sum: start a "frontier" set containing E001, and loop: on each iteration use `awk`/`grep` over `emp.csv` to find all rows whose `manager_id` is in the current frontier, add their `emp_id`s to the accumulated set and to the next frontier, and stop looping once an iteration adds no new ids (this correctly handles arbitrary depth without hardcoding it). Sum the `salary` field for every emp_id ever collected. For cross-functional projects: join `proj.csv` to `emp.csv` on `emp_id` (via `awk` associative arrays or `join` on a pre-sorted file) to get `(proj_id, dept_id)` pairs, then group by `proj_id` and count *distinct* `dept_id`s per group (e.g. `awk` with a nested associative array keyed by `proj_id` and `dept_id`), keeping only projects with count >= 3. Sort the qualifying proj_ids alphabetically and emit both results into `/home/user/result.json` using `jq -n` to build valid JSON safely rather than hand-concatenating strings.