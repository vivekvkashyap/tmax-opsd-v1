Start by getting the Rust project to build at all: run `cargo build --release` in `/app/bom_extractor`, read the compiler/dependency error, and check `Cargo.toml`'s `sqlx` (or equivalent) feature list — a missing `sqlite` feature flag is a common cause of a SQLite backend failing to compile even though the crate is present, so add it and rebuild. Next, inspect `/app/data/sample.db` directly with `sqlite3` (`.schema`) to confirm the real column names of `parts` and `assemblies` before touching the query — don't assume names from the prose alone. Replace the placeholder query in `src/main.rs` with a recursive CTE: an anchor member selecting the direct children of the given root part with their own quantity, and a recursive member joining assemblies to the CTE on parent_id = child's id, multiplying quantity by the parent row's quantity, and finally aggregate/group by part_id summing the accumulated quantities (multiple paths to the same sub-part must combine, not just show one path). Output must be JSON sorted ascending by part_id with `part_id`, `part_name`, `total_quantity` keys exactly. Test against sample.db with a few different root part_ids, including a multi-level assembly, before considering it done.
