Build the script in clear stages and verify each before moving to the next. (1) Walk `/home/user/source_data` recursively (`os.walk`), compute SHA-256 per file by streaming in chunks (don't load huge files fully into memory), and store hashes keyed by the path relative to `source_data` (use `os.path.relpath`) in a dict, then `json.dump` to the manifest. (2) Build the tar with Python's `tarfile` module using `arcname` set to the relative path for every file added (not the absolute path) so the archive root matches `source_data`'s contents directly — this is the detail naive attempts get wrong, since a plain `tar.add(source_data)` would prefix every entry with `source_data/`. Use an uncompressed mode (`"w"`, not `"w:gz"`). (3) Hash the finished tar file the same streaming way and write the hex digest (no extra whitespace/newline issues) to `backup_hash.txt`. (4) Split the tar into fixed 512000-byte chunks by reading the file in that block size and writing each block to `backup.tar.chunkNNN` with zero-padded 3-digit numbering, letting the final chunk be shorter naturally. Run the script at the end so all four output artifacts actually exist on disk.