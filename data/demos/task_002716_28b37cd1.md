Locate all inputs first with `find /home/user/dataset_raw -name '*.cst'` so you don't miss files nested under subdirectories like `zone_B/subzone`. For decoding, write a small script (Python is fine here, or a `while read` loop plus `base64 -d`) that processes each `.cst` file line by line: split on the first `:` to get `repeat_count` and the base64 payload, base64-decode the payload, and emit the decoded pipe-delimited string `repeat_count` times (this is run-length expansion, not deduplication — a repeat_count of 2 means the record appears twice in the output, including in the final CSV). After decoding, split each record on `|` into `timestamp,sensor_id,value,status`, keep only rows where `status` is exactly `CRITICAL` (case-sensitive exact match, not a substring check), and join the fields with `,` instead of `|`. Collect filtered rows from every `.cst` file across all directories into one stream, prepend the exact header `timestamp,sensor_id,value,status`, and sort the data rows (not the header) by timestamp ascending — since the timestamps are ISO-8601 strings, a plain lexicographic `sort` works correctly here. Write the result to `/home/user/summary/critical_sensors.csv`, creating the `summary` directory first, and manually trace one small file end-to-end to confirm your decode/filter logic before running it over everything.
