Treat this as a classic regression hunt: first reproduce the failure by building HEAD and running it against the noisy input to confirm garbage/NaN output. Then use `git bisect start HEAD HEAD~15` (or an equivalent manual bisection over the last ~15 commits), building and testing at each step, to isolate the exact commit that broke convergence — don't just skim commit messages, actually build and run at candidate revisions. Once you have the bad commit, read its diff carefully rather than guessing; look specifically at the weight-update function in the LMS filter code for a variable that is declared but never assigned before being used in an arithmetic update — an uninitialized local reading stack garbage is a common cause of sudden divergence to NaN/Inf that "used to work." Fix it by initializing the step size/learning-rate variable to a stable, sane default (or restore the pre-regression value) rather than papering over symptoms elsewhere. Use gdb/valgrind or print-based instrumentation to confirm weights stay bounded after the fix. Then rebuild, run the fixed binary on `/app/noisy_input.wav`, and write the cleaned audio as 16-bit PCM WAV to the required output path so it can be scored against the hidden reference by MSE.
