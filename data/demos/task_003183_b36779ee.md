Build this in dependency order: core evaluator first, then the gRPC wrapper, then the audio-driven end-to-end call. For `math_core`, set up a `cdylib` crate implementing a standard recursive-descent or shunting-yard expression parser/evaluator supporting `+ - * / ()` with correct operator precedence, expose it via `#[no_mangle] pub extern "C" fn evaluate_expr(expr: *const c_char) -> f64` that converts the C string safely (checking for null/invalid UTF-8) and returns `f64::NAN` on any parse error instead of panicking across the FFI boundary. For `calculator_grpc`, define the `.proto` exactly as specified, generate code with `tonic-build`, implement the service to call into `libmath_core.so` (via FFI linking or `libloading`) for evaluation, and add a gRPC interceptor that checks the incoming metadata's `authorization` header equals exactly `Bearer MIGRATE_PY3_TOKEN`, returning `Status::unauthenticated(...)` otherwise — test both the authorized and unauthorized paths. Start the server listening on `127.0.0.1:50051` in the background. Finally, transcribe `/app/legacy_equation.wav` (e.g. with `whisper`), convert the recovered spoken-word expression into a numeric expression string faithfully (word-for-word, preserving operator/parenthesis order), call the running gRPC service with that expression using `grpcurl` or a small client script including the auth header, and write the numeric result to `/home/user/audio_result.txt`.