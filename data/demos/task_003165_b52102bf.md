Treat the stripped binary as a black-box function evaluator — do not try to disassemble or reverse-engineer it beyond calling it. Load `measurements` from `/home/user/experiment.h5` with h5py. For each x, compute the central finite-difference derivative as (f(x+h) - f(x-h)) / (2h) with h=1e-3, where f(v) means invoking `/app/model_exec <v>` as a subprocess and parsing its stdout as a float — this requires two subprocess calls per data point, so batch/parallelize if performance matters, but correctness first. Take the mean of these derivatives. For the bootstrap CI: call `np.random.seed(42)` exactly once right before the resampling loop (not before), then perform exactly 1000 iterations of sampling-with-replacement from the derivative array (same size as original), computing the mean of each resample, and take the 2.5th and 97.5th percentiles of the 1000 bootstrap means as the CI bounds. Round all three output floats to 4 decimal places. Build an HTTP server on 0.0.0.0:8000 exposing GET /stats that checks for header `X-API-Key: model-fitter-88` (return 401 if missing/wrong) and otherwise returns the JSON with exact keys `mean_derivative`, `ci_95_lower`, `ci_95_upper`. Keep the server running so the verifier can hit it.