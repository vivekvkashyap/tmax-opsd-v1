Read `analyze_latency.c` line by line before touching it — the bug pattern to look for is a variance computed into an `int` variable when it should be `double` (integer truncation kills small fractional variances, zeroing the margin), and/or the margin formula referencing that truncated variance. Fix the declaration/typing so mean, sum-of-squares, and variance stay `double` throughout, keeping the confidence formula `1.96 * sqrt(variance / N)` with `variance = (sum_sq/N) - mean*mean`. Compile with `gcc -o analyze_latency analyze_latency.c -lm` (don't forget `-lm` for `sqrt`). Run it against each of the three `.dat` files in `/home/user/storage_metadata/` — these are raw binary files of packed IEEE-754 doubles (read with `fread` in double-sized chunks, not text parsing). Write `Node,Mean,CI_Lower,CI_Upper` to `/home/user/experiment_tracking.csv`, formatting each number to exactly 3 decimals with `%.3f`, rows sorted alphabetically by node name. Verify by recomputing mean/variance independently (e.g. quick Python check reading the same binary files) before trusting the C program's output.
