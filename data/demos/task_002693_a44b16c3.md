Split this into two independent pieces and verify each in isolation before combining. (1) Nginx: open `/home/user/app/nginx.conf`, find the `location /api/` block and fix/add `proxy_pass http://127.0.0.1:9000/api/;` (keep the trailing slash on both sides so the prefix is preserved correctly), reload/restart nginx via the start script, then confirm with `curl -s http://localhost:8080/api/...` against the mock listener on port 9000. (2) Rust filter: scaffold with `cargo new etl_filter`, add a JSON crate (e.g. `serde_json`), parse the file argument, then implement two independent checks — (a) walk `metadata` recursively (it may nest objects) and flag any key starting with `$`; (b) build a directed graph from `edges` and detect a cycle via DFS with a recursion-stack/visited-state array (three-color marking), not just a simple visited set, since a plain visited set gives false negatives on diamond shapes but false positives on cycles depending on your traversal order. Build with `cargo build --release`, then run the binary against every file in `/home/user/corpus/clean/` and `/home/user/corpus/evil/` and check exit codes match expectations before finishing.
