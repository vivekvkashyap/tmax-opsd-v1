Build this in the standard order: repo/hooks plumbing first, then the scanning logic, verified end-to-end with a real push. Initialize the bare repo with `git init --bare /home/user/alert_repo.git`, create `/home/user/global_git_hooks`, then remove the existing `hooks` directory inside the bare repo and replace it with `ln -s /home/user/global_git_hooks /home/user/alert_repo.git/hooks` (a symlink, not a copy). Set the config with `git -C /home/user/alert_repo.git config receive.denyDeletes true`. Write `pre-receive` as a bash script that reads each line of stdin (`while read oldrev newrev refname`), runs `git log -p "$oldrev..$newrev"` for that ref, and pipes the diff into `secret_scanner.py` via a pipe — check the scanner's exit code (`$?` or the pipeline's return, being careful about `set -o pipefail` so the script's exit status isn't masked by the pipe). In `secret_scanner.py`, read the diff from stdin, look line-by-line for lines starting with exactly one `+` (not `+++`) containing the exact marker string, and `sys.exit(1)` if found. On rejection, append the exact required log line (with the real refname substituted) to `/home/user/rejected_pushes.log` and have `pre-receive` exit non-zero to actually block the push. Test by cloning the repo and pushing both a benign and a secret-containing commit to confirm the accept/reject behavior and log line match exactly.