Structure `/home/user/audit_pipeline.sh` as a clear pipeline of discrete steps. First run `sqlite3 /home/user/sys_audit.db "REINDEX;"` to repair the corrupted indexes before querying. Then write a single SQL query that joins `edges` three ways: ACCESSED edges (User->Resource) joined to BELONGS_TO edges (Resource->Department) joined via a LEFT JOIN (or NOT EXISTS subquery) against MEMBER_OF edges (User->Department) to find rows where no matching MEMBER_OF edge exists for that exact (user, department) pair — that's the violation condition. Join back to `nodes` to resolve names instead of raw IDs. Output the query results in a mode `sqlite3` can pipe to `jq` cleanly — use `.mode json` or emit lines and reshape with `jq -R`/`jq -s`, then use `jq` to build objects with exactly the keys `user`, `resource`, `resource_department`, and sort with `sort_by(.user, .resource)`. Redirect the final JSON array to `/home/user/violations.json`, and `chmod +x` the script. Test by rerunning the script and diffing output against your own manual trace of the graph before trusting it.