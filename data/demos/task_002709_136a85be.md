Run OCR on the image first (`tesseract /app/fw_spec.png -`) and carefully read out the exact port values it reports for SSH_PORT and EMAIL_PORT — don't assume standard port numbers, use what the image actually contains. Append proper `export VAR=value` lines to .bashrc with those recovered values. Write fw_generator.py to read the env vars (os.environ) and format the two iptables lines exactly as specified, substituting the actual port numbers. For the detector, study the sample logs in both corpora directories to identify the precise distinguishing signal (SSH's key-based auth silently failing due to permission issues produces a specific "bad ownership or modes" message distinct from password-failure or success messages) — build a regex/substring check on that exact pattern rather than something looser, since it must generalize to a held-out set with 100% accuracy. Test the detector against every file in both corpus directories before considering it done, not just a few samples. Finally write the logrotate config with daily/rotate 7/compress/missingok directives targeting the mail log path.
