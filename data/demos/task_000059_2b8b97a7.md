Build the validator rule by rule, testing incrementally against both corpora as you go rather than writing the whole script blind. First determine the expected frame count N by running `ffprobe -v error -count_frames -select_streams v:0 -show_entries stream=nb_read_frames -of csv=p=0 /app/data/conveyor.mp4` (or an equivalent counting method) — do this once and compare it against `wc -l` on the CSV minus the header. Then implement each rule as an early-exit check: exact header string match, row count == N+1 total lines, frame column strictly sequential starting at 1 (track expected value in a loop variable, don't just check for duplicates), timestamp column strictly increasing (compare as floats, e.g. with `awk` or `bc`, not lexicographically as strings), and event_code matching `^0x[0-9A-F]{4}$` exactly (case-sensitive, exactly 4 hex digits). Since the script must produce no stdout/stderr, redirect all internal tool output (`ffprobe`, `awk` errors) to `/dev/null` and rely solely on the exit code. Iterate over every file in `/app/corpora/clean/` and `/app/corpora/evil/` after writing the script to confirm 100% correct classification before considering the task done.
