Start by OCR'ing `/app/instructions.png` (`tesseract /app/instructions.png -`) to recover the exact target file extension, the string to find, and its replacement — use those extracted values verbatim rather than assuming. Find candidate files with something like `grep -rl` (or `find ... -name '*.<ext>'` piped to `grep -l`) scoped to that extension, then for each match perform the replacement by writing to a temp file (e.g. `mktemp` in the same directory) and `os.replace`/`mv`ing it over the original — this atomic-write pattern avoids partial writes if interrupted, so don't edit files in place with sed -i alone. As you touch each file, record its path (relative to `/app/messy_project/`) and compute its post-edit SHA256, accumulating these into a dict you dump as `/app/manifest.json`. Critically, build `diff_backup.tar.gz` containing only the exact set of modified files with their relative directory structure preserved — pass explicit file paths to `tar` (e.g. `tar czf diff_backup.tar.gz -C /app/messy_project <relative_paths...>`) rather than archiving the whole tree, since the grader scores archive size and a full-tree backup will blow the 10KB threshold.