Start the MLflow tracking server first and confirm it is actually listening before writing any client code: `mlflow server --host 127.0.0.1 --port 5000 &`, then poll `curl http://127.0.0.1:5000` (or check the process/log) until it responds — a common failure mode is the script trying to connect before the server has finished booting. In your processing script, compute `global_mean` once over the whole dataset, then for each `zip_code` group compute `n` (count) and `mean_price`, and apply the smoothing formula exactly as given — do this with a pandas groupby/transform rather than row-by-row loops to avoid mismatched indices when merging back into the original dataframe. Set `mlflow.set_tracking_uri(...)`, wrap the run in `with mlflow.start_run(run_name="Bayesian_Encoding"):`, and log the param/metric with the exact names requested (`smoothing_weight`, `global_mean_price`) — MLflow silently coerces types, so log the param as a plain value and the metric with `mlflow.log_metric`, not `log_param`. Save the CSV first, then call `mlflow.log_artifact(path)` referencing the same output path so the run's artifact matches what's on disk.
