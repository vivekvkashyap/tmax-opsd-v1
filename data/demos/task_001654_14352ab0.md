First read `legacy_filter.py` to understand its exact filtering predicate (skip blank lines; drop lines whose key, the text before `=`, starts with either of two specific prefixes), then reimplement that same predicate as a pure-Bash function — no Python subprocess calls should remain in `process.sh` (verify with `grep python process.sh` yourself before finishing). For the scope bug, look at how the existing helper function's loop variable is declared: if a `while read -r line` (or similar) inside a called function uses the same variable name as an outer loop without `local`, the inner loop clobbers the outer iterator on each call — declare loop/temp variables `local` inside every helper function. Build the two-file diff pipeline: filter each manifest, sort each filtered output, then `diff -u` the sorted-old against sorted-new into `build.patch`. For the BATS step, `git clone` bats-core, run its `install.sh <prefix>` to install into `/home/user/local`, write a `.bats` test file that invokes your fixed `process.sh` against fixture manifests and asserts on the diff output, then run the installed `bats` binary against it and redirect output to the results log.
