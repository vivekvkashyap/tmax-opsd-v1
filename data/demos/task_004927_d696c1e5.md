Treat this as a standard graph-reachability problem, not a full graph library task. Parse `access_log.csv` line by line (no header) into a `Source,Target` directed edge list, and build an adjacency list keyed by a hash map from entity name string to an integer node id (e.g. `std::unordered_map<std::string,int>`), assigning ids on first sight of each name. Then, for each node, run a bounded-depth search (BFS or DFS capped at depth 3, or simply check paths of length exactly 1, 2, and 3 from that node) to test whether `SecureVault` is reachable within 1-3 hops — the cleanest approach is a single BFS/DFS *from* `SecureVault` over the *reverse* graph, tracking hop-distance, and collecting every node found at distance 1, 2, or 3 (excluding SecureVault itself unless it reappears via a cycle). Collect the resulting node names into a `std::set<std::string>` for automatic dedup and alphabetical ordering, then write one name per line to `/home/user/compliance_violations.txt`. Compile with the exact command given (`g++ -std=c++17 -O3 ...`) and run the binary so the output file is actually produced — don't just leave source code uncompiled.