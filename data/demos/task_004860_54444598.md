Design the script around three separable concerns rather than one big loop: streaming I/O, parallel decoding, and ordered output. Read `/home/user/input.csv` line-by-line (or in bounded chunks) with the `csv` module so the file is never fully materialized in memory; feed batches of lines to a `concurrent.futures.ProcessPoolExecutor` (or `multiprocessing.Pool`) worker function that does the actual unicode-escape decoding and validation, but submit work in order and collect results via `executor.map` (which preserves order) rather than `as_completed` (which does not) — this is the key place naive attempts break chronological ordering. In each worker, first re-parse the row with `csv.reader` to check it has exactly 3 fields (drop and log the id, or `UNKNOWN` if unparseable, to `/home/user/dropped.log`); for valid rows, decode `\uXXXX` sequences in the `text` field back to real unicode characters — `bytes(s, "utf-8").decode("unicode_escape")` is a decent starting point but be careful it doesn't mangle already-correct UTF-8, so test against a row with an accented character before trusting it. Write results to `/home/user/cleaned.csv` using `csv.writer` for correct quoting, preserving input order throughout.