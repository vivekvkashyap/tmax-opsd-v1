Load `/app/distorted_commands.wav` with `scipy.io.wavfile.read`, and immediately convert the int16 samples to float by dividing by 32767.0 to recover `y[n]` in the original normalized scale — this division is easy to forget and will silently corrupt everything downstream. The core step is inverting `y = x - 0.15*x^3` per-sample: this is a monic cubic in `x`, so either (a) solve it analytically per-sample with `numpy.roots` or a closed-form cubic formula and pick the real root closest to `y` (since the true signal is bounded and near-identity for small values, the "identity-like" real root is the physically correct one — reject complex/spurious roots), or (b) run vectorized Newton-Raphson: `x_{n+1} = x_n - (x_n - 0.15*x_n**3 - y)/(1 - 0.45*x_n**2)` starting from `x0 = y` and iterating to convergence for the whole array at once (much faster than looping in Python). Validate on the first 1 second (a 1kHz tone) by checking that a 3kHz-band FFT peak is now negligible compared to the distorted version. Finally clip/scale the recovered `x[n]` (already ≤1.0 in magnitude), multiply by 32767, cast to `int16`, and write with `scipy.io.wavfile.write` to `/home/user/clean_data.wav` at 16kHz.