Reproduce the crash first: run the compiled binary against the CSV under gdb (or with core dumps enabled) and let it segfault, then use `bt` to see the crashing frame and inspect the local variables (particularly the parsed id) to understand why an out-of-range array index corrupts memory. Once you see the id value at the crash, don't assume — locate the actual source line in the CSV via `grep -n` for that value to get its 1-indexed line number, and confirm it's the same record by cross-checking with gdb's state. Copy exactly that one raw line (no header, no trailing extra lines) into the MRE file. Fix the C source by adding a bounds check on the parsed id before it's used to index the array, skipping the record (continue) when out of range instead of dereferencing invalid memory — recompile with the same gcc invocation. Write the resolution report with the crashing id on line 1 and its source line number on line 2, then rerun the patched binary against the full CSV and confirm the exit code is 0.
