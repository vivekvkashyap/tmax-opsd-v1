Write the C program in clear stages so each is easy to get right: (1) a line-parsing pass over `/home/user/embeddings.csv` that splits each line on commas, verifies you get exactly 100 tokens and that every token parses fully as a float via `strtod` (check the end pointer to catch trailing garbage/non-numeric characters), discarding any line that fails either check; (2) parse `/home/user/reference.csv` the same way, trusting it's the single valid reference row; (3) for each retained embedding compute its dot product with the reference vector and accumulate into a growable array (malloc/realloc) of doubles. Only start your `clock_gettime(CLOCK_MONOTONIC, ...)` timer after all parsing/loading finishes, and stop it right after you've computed mean, sample standard deviation (use `n-1` in the denominator, i.e. sample stddev, not population), and the 95% CI (`mean ± 1.96 * stddev/sqrt(n)`) — the compute-time window must exclude I/O. Classify VALID/INVALID by comparing the mean strictly greater than 25.0, then write the six-line log with each float formatted to exactly 4 decimal places (`%.4f`) except compute time (`%.6f`). Compile with `gcc -O3 -lm` and sanity-check the output format against the spec before considering it done.