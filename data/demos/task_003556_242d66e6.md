Build and test each pipeline stage in isolation before wiring them together via the git hook. First `git init --bare /home/user/iot_ingest.git`. Second, write `/home/user/process_audio.py` using `whisper.load_model("tiny")` and `model.transcribe(path)["text"]`, printing only the trimmed text — test it standalone on `/app/device_audio.wav` first so you know the script works before it's invoked from a hook (hook failures are hard to debug blind). Third, write the `post-receive` hook as a shell script: read stdin for the ref update, use `git archive $newrev | tar -x -C /home/user/latest_push` to materialize the pushed tree, extract the `owner:` value from `metadata.txt` with something like `grep '^owner:' metadata.txt | awk '{print $2}'` (or `sed`), invoke your transcription script on the extracted `audio.wav`, and append exactly `Owner: <name> | Transcript: <text>` to `/home/user/ingest.log` — make the hook executable (`chmod +x`). Finally clone the repo, drop in the test `audio.wav` and a `metadata.txt` with the given fields, commit and `git push origin master`, then inspect `/home/user/ingest.log` to confirm the format and that the transcript is reasonably close to the spoken content (the grader uses a similarity threshold, not exact match).
