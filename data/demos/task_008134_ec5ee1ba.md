Work in strict dependency order since each step feeds the next. First, write a script that walks all `.proto` files under `/home/user/proto_deps`, parses each `import "...";` statement, and checks whether the referenced relative path exists anywhere under the proto root — collect all imports referenced but not present as files, and write the one missing path found to `/home/user/missing_dep.txt` exactly as it appears in the import statement (don't reformat or normalize it). Apply the given patch with `patch -p0 < /home/user/fix_imports.patch` run from inside `/home/user/proto_deps` (check the patch's path prefix/header to pick the right `-p` level — try `-p0` or `-p1` and confirm with `--dry-run` first if unsure) so the broken import is corrected in place. Then compile all protos with `python -m grpc_tools.protoc -I/home/user/proto_deps --python_out=/home/user/gen --grpc_python_out=/home/user/gen $(find /home/user/proto_deps -name '*.proto')`, making sure `/home/user/gen` exists first and mirrors the proto package subdirectories (add `__init__.py` files if the generated imports need them as a package). For the test script, add `/home/user/gen` to `sys.path`, implement the servicer per the spec, start the server on port 50051, run the client call, and only write `TEST_PASSED` to the log after the assertion genuinely passes — run the script yourself and check the log file's exact contents before finishing.