Write `processor.c` so the regex filtering happens naturally from line-based reading: read fileA with `fgets`/`getline` one line at a time and use POSIX `regcomp`/`regexec` with the exact ERE given (`^[0-9]+,-?[0-9.]+,-?[0-9.]+,[A-Za-z0-9_ ]+$`) to test each line — a corrupted row with an embedded newline will simply appear as multiple malformed lines to a line reader, so each fragment naturally fails the regex and gets silently dropped without special-casing. Load valid A rows into a hash/lookup keyed by `id` (a simple array or small hash table since ids are sequential), then stream fileB and for each row whose id is present in the A lookup, compute `sqrt(x*x+y*y+z*z+w*w)` and print `id,%.4f\n`. Compile with `-lm` for `sqrt`. In `workflow.sh`, launch the 4 processor invocations as background jobs (`processor A_i.csv B_i.csv > out_i.tmp &`), `wait` for all of them, then `cat` the temp outputs together and `sort -t, -k1,1n` before writing to `/home/user/output.csv` — sorting numerically by id (not lexically) is required since the grader diffs the exact sorted output.
