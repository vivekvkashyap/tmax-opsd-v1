Parse each log line with a compiled regex capturing the fields in brackets/labels precisely (e.g. `re.compile(r'\[(?P<timestamp>[^\]]+)\].*?IP:\s*(?P<ip>\S+).*?STATUS:\s*(?P<status>\S+)')` and the analogous pattern for `app.log` with `USER`/`ACTION`), keeping the timestamp as the raw string exactly as it appears so it can be used as a join key without reformatting mismatches. Load both parsed sets (e.g. as lists of dicts or DataFrames) and inner-join on the exact pair `(timestamp, ip)` — records that don't match on both fields must be dropped entirely (note some IPs/timestamps only appear in one log). For the stratified sample, group the joined rows by `status`, sort each group by timestamp ascending, and keep only the first 2 rows per group — do this per-group truncation explicitly rather than a global sample, since some status codes have more qualifying rows than others. Concatenate the kept groups, sort the final list by timestamp, and serialize to `/home/user/output.json` as a JSON array of objects with exactly the keys `timestamp`, `ip`, `status`, `user`, `action` in that field set (status as the string exactly as parsed). Run the script and diff the output structure against the required schema before finishing.