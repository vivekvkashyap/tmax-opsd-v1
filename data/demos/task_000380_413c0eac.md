Split this into two independent fixes before recompiling. First, in `server.cpp`, replace the naive `E[X^2]-E[X]^2` variance formula with a numerically stable two-pass (compute mean, then sum of squared deviations) or Welford's online algorithm, dividing by N-1 for sample variance — the naive formula is what causes catastrophic cancellation on large near-equal values. Second, don't assume the JSON precision bug is in your own code: since `nlohmann_json` is vendored at `/app/nlohmann_json`, grep its `single_include/nlohmann/json.hpp` for a suspiciously hardcoded literal `2` used as float serialization precision (in the dump/float-formatting logic) and restore it to use `std::numeric_limits<number_float_t>::max_digits10` (or `digits10`) as the library normally does — a hardcoded low precision is a classic vendored-dependency perturbation, not something to patch around in `server.cpp`. Rebuild with the provided `Makefile`, start the server in the background on `127.0.0.1:8080`, and sanity-check with `curl -X POST` against `/variance` that the returned JSON has full float precision (not truncated to 2 decimals) and the correct small variance value.