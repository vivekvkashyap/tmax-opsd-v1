Reverse-engineer `/app/cred_rotator` methodically: run `strings`, `objdump -d`/`ghidra`/`radare2`, or just trace behavior with test HTTP requests piped into it (if it can run standalone) to see exactly what order it performs URL-decoding, substring checks, and character substitutions in on the `X-Cred-Path` header. The classic bug shape in this kind of parser is a security check that runs *before* a normalization/substitution step, so a payload can be crafted to be "safe" at check-time but become dangerous after the later transformation (e.g. checking for one path-separator style before converting another separator style into it). Once you've identified the exact sequence of operations, replicate that same sequence in `/home/user/waf.sh`: extract the header value from the request file, URL-decode it, and check whether it *would* traverse outside `/etc/creds/` after applying the exact same transformations the binary applies, in the same order — get the order wrong and you'll be bypassable in one direction or over-blocking in the other. Validate against both a `../`-style traversal attempt and a case that merely looks suspicious but is benign, to make sure you don't have false positives or false negatives before finalizing.