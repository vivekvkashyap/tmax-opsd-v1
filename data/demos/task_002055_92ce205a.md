Build the pipeline stage by stage and print intermediate shapes/values to sanity-check before moving on: load the array with `np.load`, confirm it is (100, 1024), then multiply each row elementwise by `np.hanning(1024)` (broadcasting works directly, no loop needed). Run `np.fft.rfft` with `axis=1` so all 100 rows transform independently, take `np.abs`, then `np.argmax(..., axis=1)` to get the 100 peak-bin indices in one vectorized call rather than looping — this avoids ordering bugs. For the nonlinear step, define `f(y, p) = y + exp(y/100) - p` and call `scipy.optimize.fsolve(f, x0=p, args=(p,))` per peak index, using the peak index itself as the initial guess as specified; collect the 100 scalar roots into a flat array. The reproducibility-critical part is step 7-8: sort the solutions ascending with `np.sort` before saving to `solutions.npy`, and separately compute the mean from that same sorted array using `math.fsum` (not `np.sum` or a naive Python loop with `+=`, which can differ in the last bit depending on order) divided by 100. Format the mean with Python's `f"{mean:.6f}"` so it has exactly six decimal places, and write it to `result.txt`. Double check dtype/shape of `solutions.npy` matches a plain 1D float array before finishing.
