Scaffold with `cargo new strict_etl` at `/home/user/strict_etl`. Read both CSVs with the `csv` crate; when parsing `age`, treat an empty field specially — parse into `Option<i64>` (or a sentinel) instead of letting it fall through to float parsing, then impute `None`/missing with the integer `0` explicitly, never routing through `f64` at any point so the column stays a true integer type in your struct and output. Perform the inner join by building a `HashMap<user_id, UserRecord>` from `users.csv` and iterating `events.csv`, only emitting rows where the `user_id` exists in the map — this naturally drops event rows like user_id 6 that have no matching user. For `score_normalized`, compute `(score * 10.0).round() as i64` (banker's/away-from-zero rounding via `.round()` in Rust matches typical expectations) so it prints as a plain integer with no decimal point. Collect output rows, sort by `event_id` ascending, and write them with the exact header `event_id,user_id,age,signup_source,score_normalized` to `/home/user/pipeline_out/dataset.csv`, creating the output directory first if needed. Compile and actually run the binary so the CSV exists on disk before finishing.