First diagnose why simulate.py diverges: it uses a fixed Euler step of dt=1.0, which is far too coarse for this stiff-ish ODE at larger k — confirm by running it with k=1.5 or 2.0 and observing the oscillation/blow-up. Fix it by substituting a proper adaptive/stable integrator (e.g. `scipy.integrate.solve_ivp`) or by sub-stepping Euler/RK4 internally with a much smaller dt (e.g. 0.001) while only *printing* one line per integer second — the required output cadence (t=0..10, one point per second) must stay unchanged even though your internal step size shrinks. For fit.sh, launch each k's simulation as a background job (`simulate.py "$k" > out_$k.txt &`), track PIDs, `wait` for all, then compute each SSE against observed.txt (e.g., with awk or a small Python one-liner) and pick the k with minimum SSE — do not run the grid sequentially. For the KS statistic, rerun the fixed simulator at the best k, extract just the population column from both observed and simulated series, and compute the two-sample KS distance (e.g. via `scipy.stats.ks_2samp`), rounding to 3 decimals before writing.
