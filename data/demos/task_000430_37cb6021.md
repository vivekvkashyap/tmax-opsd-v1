Don't guess the metric — reverse-engineer it empirically first. Generate small hand-crafted spectra (e.g. N=2, M=3-5 bins, simple distributions you can compute distances for by hand), run `/app/spec_oracle <input> <output>`, and compare the outputs against candidate formulas (L1/L2 on raw values, KL divergence, and 1D Wasserstein/Earth-Mover distance computed via absolute difference of cumulative sums). Since inputs are pre-normalized 1D probability distributions, prioritize testing the CDF-based L1 distance: for each pair of spectra, compute the running cumulative sum (CDF) of each, then sum `|CDF_P[i] - CDF_Q[i]|` over all bins — verify this reproduces the oracle's numbers exactly (to the printed decimal precision) on your test cases before committing to it. Once confirmed, write `fast_spec.c` computing this metric for all N*(N+1)/2 unique pairs (matrix is symmetric, so compute once and mirror, or just compute both cheaply), parallelize the outer pair-loop with `#pragma omp parallel for`, and format output to at least 6 decimal places matching the oracle's row/column layout. Compile with `gcc -O3 -fopenmp fast_spec.c -o /home/user/fast_spec -lm` and test on a large synthetic dataset to confirm both accuracy and the sub-3-second runtime requirement before finalizing.