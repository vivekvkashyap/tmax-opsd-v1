Fix the three bugs in `etl_pipeline.sh` independently and verify each before moving on. For deduplication, don't rely on curl retries not overlapping — instead dedupe after collection by unique timestamp (e.g. pipe the fetched JSON lines through `jq`/`sort -u` keyed on the `ts`/timestamp field, or load into an associative array/dict keyed by timestamp) so repeated retries can't produce duplicate records. For resampling, iterate minute-by-minute across the fixed window `2024-01-01T12:00:00Z` through `12:59:00Z` (60 buckets), and for each minute select the first record (by timestamp) whose timestamp truncates into that minute; where no record falls in a minute, synthesize a record with message `"GAP_DETECTED"` for that minute stamp — this requires building the 60-bucket skeleton first, then filling it, rather than only ever emitting buckets you have real data for. For the Unicode length bug, replace `wc -c` (byte count) with a character-count check, e.g. `echo -n "$msg" | wc -m` under a UTF-8 locale, or better, do the length/validation logic in a language with proper Unicode string handling (Python/jq) rather than raw bash byte counting. Run the fixed script and check the warehouse's `/api/v1/stats` yourself to confirm accuracy before finishing.