First recover the passphrase: transcribe `/app/intercept_77.wav` (play it or use a speech-to-text tool such as `whisper`), normalize it to lowercase words separated by single spaces with no trailing newline, then compute `printf '%s' "<phrase>" | sha256sum` to get the session-cookie value the server must check — don't hardcode a guessed hash, derive it from the actual transcription.

Then build the Rust service: `cargo new c2_honeypot`, add a TLS-capable framework (axum + axum-server with rustls, or actix-web with openssl), generate `cert.pem`/`key.pem` in `/home/user/` via `openssl req -x509 -newkey rsa:2048 -nodes -keyout key.pem -out cert.pem -days 365 -subj /CN=localhost`. Implement `POST /analyze` to: check the `session` cookie against the derived hash (401 if missing/wrong), read the raw body and check first 4 bytes equal `7F 45 4C 46` (400 if not ELF), else compute SHA-256 of the whole body and return it hex-encoded as plain text (200). Build with `cargo build --release`, run it bound to `127.0.0.1:8443` in the background, and redirect its output to `/home/user/server.log`.