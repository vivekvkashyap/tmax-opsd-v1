Build the script in clear stages and validate each before moving on. Load the adjacency matrix with `np.loadtxt(..., delimiter=',')` and the initial vector with `np.loadtxt`. Construct the Laplacian by copying the adjacency matrix and overwriting the diagonal with the negative row sums (do this in-place on a float copy, not the original integer array, to avoid dtype truncation). Define the ODE right-hand side as a closure `D * L @ C - lambda_ * C` and pass it to `scipy.integrate.solve_ivp(f, [0, 5.0], C0, dense_output=True)` — use `dense_output=True` (or `t_eval=[5.0]`) so you can evaluate the solution at exactly t=5.0 rather than relying on the last adaptive step, which is a common source of small numerical error. Normalize the final concentration vector to sum to 1 to get P, build the uniform Q as `np.ones(N)/N`, and call `scipy.spatial.distance.jensenshannon(P, Q, base=2)`. Watch for negative or near-zero concentrations before normalizing (decay/diffusion can push values slightly negative numerically) — the JS distance function expects a proper probability vector. Round both outputs to exactly 6 decimals with `f"{x:.6f}"` when writing to the two output files, and run the script to confirm it produces no errors.
