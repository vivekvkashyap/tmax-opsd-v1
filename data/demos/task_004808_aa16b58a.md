Write a small "good/bad" test script that builds the repo at the current checked-out commit, runs the binary against `/home/user/corrupted_payload.json`, and exits 0 if it handles the input gracefully or nonzero if it panics/crashes — this is the oracle `git bisect run` needs. Start with `git bisect start`, mark current `HEAD` as bad and the `v1.0` tag as good, then run `git bisect run <your-script>` to let it binary-search automatically instead of testing commits by hand. Once it reports the first bad commit, use `git bisect reset` to return to your original branch without altering history (never `git checkout -b` over existing branches or rewrite commits). Then inspect that specific bad commit's source non-destructively, e.g. `git show <hash>:src/main.rs` or `git diff <hash>~1 <hash>`, to find and copy out the hardcoded `recovery_token` value before it was removed in the next commit. Finally write both the commit hash and the recovered token to the required output file in the exact two-line format specified.
