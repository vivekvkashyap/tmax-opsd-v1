Set up a small Rust binary crate rather than a single script, since you need to read a CSV and parse binary headers. First parse `/home/user/inventory.csv` into a `HashMap<String, u32>` of filename -> retention_days. Then, for each `.bkp` file in `/home/user/backups/`, open the file and read only the header bytes (avoid loading the whole payload): read exactly 4 bytes and verify they equal `BKP1`, then read 8 bytes as a little-endian u64 timestamp (`u64::from_le_bytes`), then 2 bytes as a little-endian u16 filename length, then that many bytes as a UTF-8 filename — use `Read::read_exact` for each field rather than assuming fixed offsets, since filename length varies per file. Look up the filename in your inventory map, falling back to 30 days if absent, and compute `expiration_timestamp = creation_timestamp + retention_days * 86400` using integer/u64 arithmetic to avoid overflow or float rounding. Collect results into a Vec of structs, sort by `archive_filename` alphabetically before serializing, and write pretty/compact JSON matching the exact key names and order shown in the spec to `/home/user/audit_report.json`. Use `serde`/`serde_json` if available, or hand-format carefully if not.
