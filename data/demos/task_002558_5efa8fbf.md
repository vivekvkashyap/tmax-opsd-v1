Compile and run the given C generator first to produce the real `dataset.csv` — do not fabricate or assume its contents. Then in Go, parse the CSV (skip the header line), and implement ordinary least-squares linear regression from first principles: compute means of x and y, then slope `m = sum((x-xbar)*(y-ybar)) / sum((x-xbar)^2)` and intercept `c = ybar - m*xbar`. Round both to 4 decimal places only at output time (keep full precision during computation) and write them in the exact `m=<value>\nc=<value>` format. For the SVG: compute a bounding box from the actual data (min/max x and y with some padding), map data coordinates to SVG pixel coordinates (remember SVG y-axis is inverted vs. typical plot y-axis), draw a `<circle>` per data point, and draw the fitted line as a `<line>` element spanning the plot's x-range using your computed m and c — don't hardcode coordinates. Verify your regression numerically against an independent computation (e.g., a quick numpy/manual check) rather than trusting the first pass, since off-by-one mean/variance errors are easy to introduce.