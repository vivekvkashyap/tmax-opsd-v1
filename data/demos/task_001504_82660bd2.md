Model this as a standard weighted directed graph in Go, not ad-hoc string processing. Parse `/home/user/edges.csv` with `encoding/csv`, and build two maps keyed by node ID: `inWeight` and `outWeight`, accumulating the `weight` column onto the target node's inWeight and the source node's outWeight for every edge (remember to register both endpoints of every edge as nodes, even ones like U5 that only self-loop or U6 that only appears as a source). Collect the node set from both source and target columns (a `map[string]struct{}` works well) so no node is missed even if it never appears in one role. For each node compute `Activity = InWeight + OutWeight` and classify by comparing InWeight vs OutWeight (strictly greater = net_sink or net_source respectively, equal = balanced) — watch the direction carefully, it's easy to invert sink/source. Group node IDs by role, sort each group's node slice lexicographically with `sort.Strings`, sum Activity per group, and marshal to `/home/user/aggregated_roles.json` matching the exact nested structure (`{"role": {"nodes": [...], "total_activity": N}}`) for each role that has members. Spot-check your totals by hand against a couple of nodes before writing the final file.