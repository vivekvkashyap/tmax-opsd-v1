Tackle this in build → correctness → security → wiring order. First get a clean compile: read `Makefile`'s linker/include flags and check what serialization library version is actually installed under `/opt` versus what the Makefile points at, then correct the path so it links the compatible version. Next, inspect `ingestor.cpp`'s worker pool: look for a shared queue or buffer accessed by multiple threads without a mutex/lock guard around push/pop — wrap those critical sections properly (std::lock_guard/std::mutex) rather than adding a global lock everywhere, to avoid deadlock. For decoding, find where byte offsets index into the payload without checking they stay within a valid UTF-8 sequence boundary before dereferencing, and add `assert()` checks (or bounds checks) before decoding steps. For sanitization, don't hardcode signatures for the sample files you can see — instead derive general rules (e.g. reject payloads containing path-traversal sequences, shell metacharacters, or invalid UTF-8) by diffing what's characteristic of `corpora/evil/` vs `corpora/clean/`, and log drops to `/tmp/dropped_payloads.log`. Finally rebuild, start `sink.sh` then the ingestor then `generator.sh`, and confirm end-to-end delivery/dropping manually before relying on the automated check.