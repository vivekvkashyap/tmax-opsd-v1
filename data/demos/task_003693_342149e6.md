Read /home/user/analyze.py first to see both bugs before touching anything. For the plotting issue, add `matplotlib.use('Agg')` before importing pyplot (must happen before the first `import matplotlib.pyplot` or right after importing matplotlib itself, before any plotting calls) so it works headlessly and actually writes a real PNG. For the numeric issue, the naive_cov/naive_corr functions compute sum(x*y) - sum(x)*sum(y)/n style formulas, which catastrophically cancel when the data has a huge shared offset — don't patch the naive formula, replace it entirely: use df.cov() for covariance (pandas defaults to ddof=1, i.e. Bessel's correction, which matches the requirement) and df.corr() (Pearson by default) directly on the DataFrame instead of the custom loop. Round both resulting matrices to 4 decimals and write them to /home/user/correlation.csv and /home/user/covariance.csv with to_csv(), preserving the index (sensor names) as the row header. Re-run the fixed script end-to-end and confirm heatmap.png is non-trivial in size and the CSVs' values are sane (correlations within [-1,1], covariances symmetric) rather than just checking the script exits without error.
