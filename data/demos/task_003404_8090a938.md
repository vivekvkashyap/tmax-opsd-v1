First transcribe `/app/lab_notes.wav` to text — install a speech-to-text tool (e.g. `pip install openai-whisper` or use `ffmpeg`+`whisper`/`vosk`) and run it on the file, then read the transcript to identify the three spoken node IDs (they'll be spelled out as words, e.g. "twelve" -> 12) rather than assuming any values. Parse `/app/citations.txt` as a weighted directed graph (source, target, weight per line) into an adjacency list, excluding the three recovered node IDs entirely — treat any edge touching them as nonexistent, and if a query's start or end node is one of them, answer -1 immediately. Implement Dijkstra's algorithm reading repeated `start end` query lines from stdin, printing the shortest distance (or -1 if unreachable) per line to stdout, since the tool is tested with thousands of automated queries and must match exactly. Write the code to `/home/user/pathfinder.cpp` and compile with something like `g++ -O2 -o /home/user/pathfinder /home/user/pathfinder.cpp`. Common pitfalls: filtering excluded nodes only at the query level but still traversing through them internally, and forgetting to flush stdout per line for streaming query/response interaction.