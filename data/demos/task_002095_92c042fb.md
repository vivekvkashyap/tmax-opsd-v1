Work in two independent tracks. For the pipeline: start the services via `/app/start_services.sh`, then in `query_lineage.py` replace the implicit cross join (comma-separated tables in the WHERE clause or no join condition) with an explicit `JOIN backup_runs r ON s.schedule_id = r.schedule_id`; add a MongoDB aggregation using `$group` on `run_id`; create the Mongo index with `db.backup_logs.create_index([("run_id", ASCENDING)])`; and populate `/app/.env` with the three connection strings (Postgres on 5432, Mongo on 27017, Redis on 6379) matching whatever credentials the start script configures. For the verifier: write `verify_manifest.py` to load the JSON, then check three independent failure modes — (1) cycle detection via a standard DFS/topological-sort over the dependency graph (three-color or Kahn's algorithm, not just "visited once"), (2) regex-validate every `schedule_id` against `^SCH-\d{4}$`, (3) recursively walk all dict keys (including nested objects/lists) looking for any key starting with `$`. Fail INVALID if any check trips; print exactly `VALID` or `INVALID` with no extra whitespace. Test against small hand-built cyclic/malformed/injected examples before trusting it against the hidden corpus.