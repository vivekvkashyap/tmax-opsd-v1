Use Python's `csv` module (`csv.DictReader`) to read the raw file instead of splitting on newlines manually — proper CSV parsing already handles embedded newlines inside quoted fields, so a naive `for line in file` approach will corrupt or drop multi-line `message` entries. After parsing, drop rows where `session_id` is empty/missing before writing JSON.

For stage 2, group parsed records by `session_id` into a dict of lists, then sort each list by `timestamp` (string ISO timestamps sort correctly lexicographically, but converting to `datetime` is safer and unambiguous).

For stage 3, iterate each session's sorted records with a sliding window of size 3: for index i, average `response_time` over records `[max(0,i-2):i+1]` (so the first two windows are naturally shorter than 3). Track the maximum of these rolling averages per session, then `round(value, 2)` before writing.

For the Makefile, declare `parse`, `group`, `stats`, `all`, `clean` as `.PHONY`, give each target correct prerequisites (script + input file) and a recipe invoking the matching python script, and make `all` depend on `rolling_stats.json`. Test by running `make clean && make all` and inspect intermediate JSON files to confirm each stage's contents before trusting the final output.
