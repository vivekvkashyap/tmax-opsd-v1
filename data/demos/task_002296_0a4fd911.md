Work through the vendored C source in `/app/micro-tar-server-1.0` methodically. First locate the port definition (likely a `#define DEFAULT_PORT` or similar constant near the socket setup) and change it to 8080 so binding succeeds without root. Second, find the directory-walking/archiving routine: it currently calls `stat()` on each entry and unconditionally `mmap()`s the file for streaming — change the stat call to `lstat()` so symlinks are detected via `S_ISLNK(st.st_mode)` on their own metadata rather than the link target's. When a symlink is found, skip the mmap path entirely: write a tar header with the symlink typeflag (`'2'`) and populate the tar header's linkname field using `readlink()` on the path, rather than trying to map its "contents". Recompile (likely `make`), then launch the server bound to `127.0.0.1:8080` in the background (trailing `&`, or nohup) pointed at the required dataset directory, and confirm it stays up — the automated verifier will connect later and expects the `Authorization: Bearer ds-secret-token` header to gate access to a valid tar stream containing intact regular files, hardlinks, and symlinks.
