Structure the Go program around a worker-pool pattern: walk the dataset tree with `filepath.WalkDir`, dispatch each matching file to a goroutine (bounded via a channel or sync.WaitGroup + semaphore), and have every goroutine write its JSON line by opening the manifest file with `os.OpenFile` in append mode and wrapping the write in `syscall.Flock(fd, LOCK_EX)` / `LOCK_UN` so concurrent appends never interleave. For SHA-256, just hash the raw file bytes. For `.gcode`, scan line by line for `G0`/`G1` commands, extract the `Z` token's trailing float, and track the running maximum. For `.elf`, don't hand-parse everything — read the standard ELF header layout (magic, EI_CLASS byte at offset 4 determines 32 vs 64-bit, and the entry-point field sits at a fixed offset afterward) and decode it as little-endian; consider using `debug/elf` from the standard library instead of hand-rolling offsets. For `.wal`, read the 32-byte header, extract the big-endian uint32 page size at offset 8, then compute `(fileSize-32)/(24+pageSize)` for frame count. Test each parser against one sample file with hand-computed expected values before running the full concurrent pipeline, then confirm `manifest.jsonl` has one line per source file with valid JSON.