Start by reverse-engineering `/app/api_oracle` rather than guessing: run `strings /app/api_oracle` to look for clues, then disassemble with `objdump -d` or `gdb`/`radare2` to trace how the argument is validated — look for a prefix-comparison (likely `strncmp`/manual byte compare against a short literal) and a running accumulator over the bytes (loop with XOR or add, compared against a constant at the end) to figure out the exact prefix string and the exact accumulator operation/target value; confirm your understanding by testing the real oracle against several crafted tokens (e.g. `/app/api_oracle SEC_xxxx; echo $?`) until your hypothesis predicts its exit code correctly on tokens you construct. Once you've derived the precise rule, implement it in Rust for speed: read `/home/user/tokens.txt` line by line, apply the byte-level check with zero subprocess overhead, and print matches to stdout. Compile with optimizations (`cargo build --release` or `rustc -O`), then run `/home/user/fast_validator /home/user/tokens.txt > /home/user/valid.txt` and sanity-check the count/samples of valid tokens against a handful of `/app/api_oracle` invocations before finishing, since a wrong rule will silently fail the accuracy check underlying the speed benchmark.
