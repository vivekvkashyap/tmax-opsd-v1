Work through the pipeline layer by layer rather than jumping straight to the Bash script: first apply the DB migration with `sqlite3 /home/user/workspace/data.db < /home/user/workspace/v2_schema.sql`. Next fix `c_eval/eval.c` — inspect line 42 for a type mismatch (likely an implicit float-to-int truncation that needs an explicit `(int)` cast) — and fix the `Makefile` by appending the missing linker flags `-lsqlite3 -lm` to the build command, then `make` and start the compiled daemon in the background listening on port 5002. Then start the Python Flask proxy in `proxy/` on port 5001 in the background, confirming it actually forwards to port 5002 correctly. Fix `nginx.conf`'s `proxy_pass` to point at `127.0.0.1:5001` instead of the stale port, then start/reload nginx on 8080. Only after confirming `curl 'http://127.0.0.1:8080/compute?v1=1&v2=2&op=add'` returns a sane plaintext integer should you write `/home/user/process.sh`: URL-decode/split the query string on `&` and `=` to get val1/val2/op, `curl` the compute endpoint, and print `RESULT: <response>`. Make it executable and smoke-test with several val1/val2/op combos before finishing.