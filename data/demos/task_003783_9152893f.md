Diagnose before rewriting: run the existing code against `/home/user/uptime-app/logs/raw_metrics.txt` (or a small sample of it) and observe both the parse failures and the average drift, so you understand exactly which edge cases (leading/trailing whitespace, detached `%`, `\r`, `\t`) break `ParseMetric`. Fix `ParseMetric` by trimming whitespace with `strings.TrimSpace` on the full string (not just suffix) and stripping any `%` wherever it appears (not just as a suffix) before `strconv.ParseFloat`. For `CalculateAverage`, the root cause is float32 accumulation: change the signature to return `float64`, and critically accumulate the running sum in a `float64` variable throughout the loop (converting each `float32` input up as you add), only dividing by count at the end — do not sum in float32 and convert at the end, since that's where precision is already lost. Write `FuzzParseMetric(f *testing.F)` using `f.Add` seed corpus entries and `f.Fuzz(func(t *testing.T, s string) { ParseMetric(s) })`, asserting no panic occurs; run `go test -fuzz=FuzzParseMetric -fuzztime=10s` briefly to sanity-check. Finally write `main.go` to read the log, parse every line, average, and format the result to exactly 5 decimal places with `fmt.Sprintf("%.5f", avg)` into `/home/user/result.txt`.
