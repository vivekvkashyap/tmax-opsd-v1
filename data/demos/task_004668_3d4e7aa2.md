This is a query-optimization task: correctness of the rewritten query must be preserved while making the binary's evaluation trivially fast, so first understand `/app/pattern_matcher`'s input formats empirically (feed it tiny hand-built graph/query files and observe its output format, e.g. how it reports a match count) rather than guessing. The core technique is graph materialization: in `/home/user/optimize_pipeline.py`, load `raw_graph.tsv` (e.g. with pandas), and for the specific 3-hop pattern in `slow_query.txt` (`?a knows ?b`, `?b follows ?c`, `?c likes ?d`), perform the actual joins in Python (knows ⋈ follows ⋈ likes) to enumerate every valid (a,d) pair reachable via that chain, then emit those as new edges under a new relation name (e.g. `knows_follows_likes`) into `/home/user/projected_graph.tsv` — decide whether to also keep or drop the original edges based on what the query file needs. Then write `/home/user/fast_query.txt` as a single-hop query using that new relation name so the binary only does a linear scan instead of nested-loop backtracking. Verify equivalence by comparing the match count the binary reports for the original slow query against the new fast query on the projected graph — they must match exactly — and time the fast run to confirm it's well under the threshold.
