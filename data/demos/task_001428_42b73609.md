Start by running `/app/start_services.sh` and confirming Flask (port 5000) and Redis (port 6379) actually respond before touching the pipeline code — check the current script's hardcoded host/port values against reality, since the seeded bug is exactly wrong connection settings (fix the Redis port/host and the Flask URL). For the leakage bug: never call `.fit()` or `.fit_transform()` on TF-IDF/SVD/target-encoding transformers against the full dataframe. Do `train_test_split` first (80/20), then build a single `sklearn.pipeline.Pipeline` wrapping a `ColumnTransformer` — TF-IDF+TruncatedSVD on `description` via a sub-Pipeline, and a target encoder (fit only on training `category`/`price` pairs, e.g. via `category_encoders.TargetEncoder` or a custom `TransformerMixin`) on `category` — combined with Ridge as the final estimator. Fit this whole composite Pipeline object only on the training split, then `joblib.dump` it whole so `pipeline.predict(raw_df)` works end-to-end on unseen raw text/category columns. Set the Redis key with `r.set('pipeline_status', 'success')` as a connectivity proof. Validate locally against a held-out portion of your own data before trusting the MAE < 45.0 threshold, since the real check uses a hidden dataset.