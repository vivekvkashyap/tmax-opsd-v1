Do this in strict dependency order since later steps consume earlier outputs. First run `tesseract /app/legacy_token.png stdout` (preprocess with `convert`/`PIL` — grayscale, threshold, upscale — if the raw OCR output is noisy) to recover the passphrase text; trim whitespace/newlines carefully since OCR often adds stray characters. Use that exact recovered string as the `-passout pass:<recovered>` / `-aes256` encryption password when generating the key: `openssl req -x509 -newkey rsa:2048 -keyout /home/user/certs/server.key -out /home/user/certs/server.crt -days 365 -subj "/CN=secure-api.internal" -passout pass:<recovered>`. Verify the key decrypts with that exact passphrase (`openssl rsa -in server.key -passin pass:<recovered> -check -noout`) before moving on — a typo here fails the whole task. For the C validator, keep parsing simple and robust: split each CSV line on commas, check `strlen(token)==32` and that every char is a valid hex digit (use `isxdigit`), and check the CSP header string contains both required substrings via `strstr` (order in the header can vary, so check each substring independently, not as one combined string). Print `RequestID: PASS` or `RequestID: FAIL` per line exactly. Compile with plain `gcc -o /home/user/auth_validator /home/user/auth_validator.c` and hand-test against a few crafted CSV lines covering both pass and each individual failure mode before trusting it against the hidden dataset.