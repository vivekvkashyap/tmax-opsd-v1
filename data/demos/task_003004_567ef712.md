Design the C classifier around the statistic, then get GSL working before worrying about corpora. Install the library first (e.g. `apt-get install libgsl-dev` if missing) and compile/link with `gcc classifier.c -o classifier -lgsl -lgslcblas -lm`. In the program: open the CSV, skip the header line, parse the `amplitude` column (second field) into a `double` array with `strtod`/`sscanf`, then compute the *sample* standard deviation using `gsl_stats_sd` (which already uses the n-1 denominator) rather than hand-rolling variance, since an off-by-one in the denominator (population vs sample) would misclassify borderline files. Compare against the fixed threshold `10.5`: `> 10.5` exits 1, `<= 10.5` exits 0 — use strict vs non-strict comparison exactly as specified, since the corpora are likely constructed near that boundary. Before considering it done, generate your own quick synthetic clean/noisy CSVs and confirm the exit codes behave as expected, then run the compiled binary against every file under `/app/corpus/clean/` and `/app/corpus/evil/` yourself (checking `$?` after each) to catch parsing edge cases like trailing blank lines or scientific notation before the real grading pass.
