Diagnose before patching: read `/home/user/etl_pipeline.py` and trace how the `left` merge of texts.csv with labels.csv produces NaN for ids present in texts but absent from labels, and how pandas silently upcasts an int column containing NaN to float64 — that's exactly why `LogisticRegression.fit` rejects `y`. Fix by dropping rows with a missing label right after the merge (e.g. `df = df.dropna(subset=['label'])`) and then explicitly casting the label column back to int (`df['label'] = df['label'].astype(int)`) before building `y`, rather than trying to coerce NaN into an int directly. Set up an isolated environment first (`python3 -m venv /home/user/venv`, activate it, `pip install pandas scikit-learn`) so the fix runs cleanly. After editing, run `python /home/user/etl_pipeline.py` and confirm it completes without error and writes `/home/user/metrics.json` with a valid `{"accuracy": <float>}` — inspect the file afterward to make sure it's well-formed JSON and the accuracy is a proper float in [0,1].