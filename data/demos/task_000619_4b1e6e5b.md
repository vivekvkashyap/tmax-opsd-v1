Start by OCR'ing `/app/settings.png` (e.g. `tesseract settings.png stdout`) to recover the three config values `BLOCK_ID`, `MAX_ROLLING_SUM`, and `WINDOW` — don't hardcode guessed values, parse whatever text tesseract actually returns and clean up any OCR noise before using the numbers. Then build the Rust sanitizer as a small `cargo` binary that parses JSON (e.g. `serde_json`) input, iterates records in order, and applies rule A (drop if `tag == BLOCK_ID`) before rule B. For rule B, maintain a sliding window of only the *accepted* values' `val` (size WINDOW, so up to WINDOW-1 previous plus current): sum the window-including-current and drop the current record if that sum exceeds `MAX_ROLLING_SUM` — the key subtlety naive solutions miss is that dropped records must never enter the window, and the window must be checked against the *proposed* new record before deciding to accept it, not after. Test against hand-crafted edge cases (window boundary, block_id match, sum exactly at threshold vs over) before trusting the hidden corpus. Finally write the cron line as `*/15 * * * 1-5 /home/user/run_pipeline.sh` (or an equivalent valid 15-minute weekday expression) to the cron file.