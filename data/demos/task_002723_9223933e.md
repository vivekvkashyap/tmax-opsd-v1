This task has three independent parts — do them separately and verify each before moving on. (1) Transcribe `/app/migration_memo.wav` with `whisper-cli`/`ffmpeg`+whisper to recover the two-word directory name spoken in the memo; use exactly what you hear (lowercased, spaces as underscores) as `{DIR_NAME}` — don't guess a name, derive it from the actual audio. (2) For the systemd fix, edit `~/.config/systemd/user/storage-sync.service`'s `[Unit]` section to add both `After=ssh-tunnel.service` and `Requires=ssh-tunnel.service`, then `systemctl --user daemon-reload && systemctl --user enable --now storage-sync.service`. (3) For the Go sanitizer, since it will be fuzz-tested against a hidden reference binary across thousands of random strings, implement the rules as a strict, literal pipeline in order — trim, lowercase, replace spaces with hyphens, strip any character outside `[a-z0-9\-_/.]` via a regex, collapse repeated `/` into one, ensure a leading `/` (default to `/` if the string is empty), then prepend `/storage/backups/{DIR_NAME}`. Read all of stdin with no trailing newline in the output. Test edge cases yourself (empty input, only symbols, embedded slashes) against your own understanding of the spec before considering it done, since exact byte-for-byte matching against an unseen oracle is required.
