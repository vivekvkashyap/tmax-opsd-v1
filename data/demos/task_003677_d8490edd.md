Open the SQLite file with the `sqlite3` CLI and inspect `.schema` and `sqlite_master` first to learn the entity/transaction table layout and foreign-key columns. Before trusting any query results, run `PRAGMA integrity_check;` and `REINDEX;` (or `REINDEX <index_name>;`) on the database to repair stale/corrupted indexes — do this even if a naive SELECT already "looks" complete, since a stale index can silently hide or duplicate rows. Then, in Python, load the full transaction table (sender/receiver pairs) with `sqlite3`, build a directed graph (e.g. adjacency dict or `networkx.DiGraph`), and run a breadth-first search (BFS) from the `Nexus_Global` node to `Apex_Solutions` — BFS guarantees the fewest-hops path, unlike DFS. Reconstruct the path by tracking parent pointers during the BFS rather than just the hop count. Write the resulting entity names as a single comma-separated line to `/home/user/shortest_path.txt`, with no trailing extra whitespace or newlines beyond one line.
