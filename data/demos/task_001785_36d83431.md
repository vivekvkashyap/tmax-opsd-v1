Start the server in the background, then reproduce the slowness first (e.g. time a single `smtplib` send) to confirm where the delay is before editing code — don't guess. Since this is a vendored package with an injected regression, grep the SMTP transaction handling path in `/app/aiosmtpd/aiosmtpd/smtp.py` (look at methods like `smtp_DATA` or the client-handling coroutine) for any stray `await asyncio.sleep(...)` call that doesn't belong there, and remove it — this is the classic "someone left a debug sleep in the vendored source" bug pattern. After the fix, restart the server process so the edited module is actually loaded (Python won't pick up source changes in an already-running process). Then write `/home/user/stress_test.py` using `smtplib.SMTP` to connect to `127.0.0.1:8025`, send 50 emails sequentially with the exact envelope/subject/body given, wrapping the loop in `time.perf_counter()` calls, and print `Total time: X.XXX seconds` with three decimal places. Rerun the stress test to confirm it's now comfortably under 2.0 seconds.
