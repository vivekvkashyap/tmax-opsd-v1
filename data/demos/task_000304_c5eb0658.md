Split this into two independent halves. First, fix the config plumbing: edit `/app/config/api.json` so the Flask API's port is `8080`, and edit `/app/config/orchestrator.json` so `broker_url` points at the local Redis (`redis://127.0.0.1:6379`) — check each file's existing key names/JSON shape before editing rather than assuming a schema, then restart/let the startup script relaunch the services so they actually pick up the change. Second, write `/home/user/processor.py` as a pure numpy stdin/stdout filter: read the JSON `{"data": [[...]]}` into an `N x M` array, compute column means and mean-center (`X - X.mean(axis=0)`), then the sample covariance with `np.cov(X, rowvar=False, ddof=1)` (guard the degenerate M=1 case where `np.cov` returns a 0-d array — wrap it as a 1x1 matrix), multiply the centered data by the covariance matrix (`X_c @ C`), and round every float in both outputs to exactly 4 decimal places before `json.dumps` — since this is checked by fuzzing against a hidden reference implementation across hundreds of random matrices, exact rounding and shape handling matter more than raw correctness.
