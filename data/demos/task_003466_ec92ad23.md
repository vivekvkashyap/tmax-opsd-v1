The key correctness trap here is embedded newlines inside CSV fields — never split on raw `\n`; always parse with Python's `csv.reader`/`csv.DictReader`, which correctly treats a quoted field containing a newline as one logical row, so read each file with `csv.DictReader(open(path, newline=''))` (the `newline=''` on open is required per the csv module docs). For timestamp normalization, parse each timestamp with a library that understands both explicit offsets (`+02:00`) and `Z` (UTC) — e.g. `datetime.fromisoformat(ts.replace('Z','+00:00'))` — then convert to UTC with `.astimezone(timezone.utc)` and format as `strftime('%Y-%m-%dT%H:%M:%SZ')`. For parallelism, use `concurrent.futures.ProcessPoolExecutor` (or `multiprocessing.Pool`) with one worker task per file, each returning its parsed+normalized records, then merge lists in the main process. After merging, sort the combined list with a stable `sorted(records, key=lambda r: (r['timestamp'], r['lang_code']))` — note timestamp before lang_code, both ascending. Write output with `json.dumps(record, ensure_ascii=False)` per line (to preserve non-ASCII text and embedded `\n` as escaped `\n` inside the JSON string) to `/home/user/compiled_locales.jsonl`. Verify by comparing line counts and spot-checking a multiline field round-trips correctly.