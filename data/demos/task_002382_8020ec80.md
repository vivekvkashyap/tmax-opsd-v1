Build the script in the exact order the requirements describe, since ordering affects correctness: create `/home/user/edge_logs` first, then configure `logging.handlers.RotatingFileHandler(filename=".../status.log", maxBytes=40, backupCount=3)` with a formatter of just `%(message)s`, attach it to a logger, then start the apptainer instance via `subprocess.run([...], check=True)` (or similar), then loop 8 times calling `subprocess.run(["apptainer","exec","instance://iot_worker","ping","-c","1","127.0.0.1"])` and logging "Ping successful" or "Ping failed" based on returncode, then stop the instance with `apptainer instance stop iot_worker` after the loop. Reason carefully about log rotation math: each log line is written with a trailing newline, so compute the exact byte size of the message you log (including the `\n`) and how many full 40-byte rotations occur across 8 writes — this determines how content splits across status.log, status.log.1, status.log.2, status.log.3. Run `python3 /home/user/edge_monitor.py` yourself afterward and inspect the resulting files with `ls` and `cat`/`xxd` to confirm the rotation and instance list (`apptainer instance list`) show the container stopped.