This is primarily a build-configuration fix, not a code-writing task — resist the urge to rewrite the C source. Locate `/app/backup-metric-parser-1.0.0/Makefile` and inspect the `CC` variable/assignment at the top; it's pinned to a nonexistent compiler alias, which is why `make` fails with a "command not found" style error. Fix it by either changing that line to `CC=gcc` or removing the override so Make falls back to its default `cc`/`gcc` — don't touch the compilation rules, source files, or optimizer flags beyond that. Run `make` (or `make clean && make` if a partial build artifact exists) inside that directory and confirm a binary is produced without errors. Then copy that binary to exactly `/home/user/backup_parser` and `chmod +x` it. Before considering it done, manually test it by piping a sample input like `<mount> <used_bytes> <total_bytes>` via stdin and confirming it prints a floating-point metric line, since correctness is checked by fuzzing many random inputs against a hidden reference and comparing outputs exactly.
