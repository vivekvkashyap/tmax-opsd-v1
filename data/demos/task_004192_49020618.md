Treat this as two separate problems: architecture (circular imports, Py2→Py3) and correctness (matching the oracle's hash). For the circular import, don't just patch around it with inline imports again — restructure so `math_utils.py` has no dependency on `router.py`/`server.py`, and anything `router.py` needed from `server.py` (like the port) should be passed in or read from a shared config module instead of importing the entrypoint module. Swap `BaseHTTPServer`→`http.server` and `urlparse`→`urllib.parse`, and be careful to decode URL-encoded query params and handle the text as a proper Python 3 `str` (not bytes) throughout. For the hash itself, don't hand-derive a formula from the buggy Python 2 code — instead probe `/app/reference_oracle` with varied inputs (short ASCII, empty string, multi-byte UTF-8 characters) and observe how output changes per byte and per position to infer the arithmetic; verify your derived formula reproduces the oracle's output exactly across many test strings including non-ASCII before trusting it, or alternatively call the oracle via `subprocess` from your endpoint so you never need to reverse-engineer it at all. Finally launch the Python 3 server on `127.0.0.1:8080` in the background and confirm `GET /compute?text=...` returns `{"hash": <int>}` matching the oracle for a UTF-8 test case.
