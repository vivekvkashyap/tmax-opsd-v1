Treat this as three separable subtasks and verify each independently before moving on. For the setup script, since you lack root, mock `/etc/passwd`-style entries in `/home/user/passwd_mock` and `/home/user/group_mock` (colon-separated fields, valid uid/gid), make the script idempotent by checking `grep -q` for existing entries before appending, create `/home/user/telemetry_data` with `mkdir -p`, symlink it with `ln -sfn`, and write a proper `fstab`-style line for `/home/user/mock_fstab` using the mock group's gid in the `gid=` mount option. For service composition, export `LOG_PATH` and `DATA_SOURCE` both pointing at `/home/user/telemetry_link/usage.log` before backgrounding `log_generator.py` and `telemetry_api.py`, then start `prom_scraper.sh` and confirm `/home/user/scraper_output.log` is actually growing with valid JSON (poll it, don't just assume). For the forecaster, don't guess the formula — treat `/app/oracle_forecaster` as a black box and query it with a range of crafted `CPU:x MEM:y IO:z` inputs (edge cases like 0, small, large values) to empirically reverse-engineer the exact arithmetic (including any rounding/truncation behavior on each term), then reimplement that in `/home/user/forecast_calc.py` and cross-check several outputs against the oracle before finishing.