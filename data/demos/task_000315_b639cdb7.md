First compute the GC ratio yourself: read the FASTA sequence, take the first 20 nucleotides, count G/C occurrences, divide by 20 — don't hardcode a guess, derive it from the actual file contents. For the C++ fix, the root cause is `#pragma omp atomic` on a floating-point accumulator inside a parallel loop: because threads add to the shared total in a nondeterministic interleaving order, and floating-point addition is not associative, the final sum varies run to run even though every summand is identical. Fix it by having each iteration write its `simulate_droplet` result into a private slot of a preallocated array (indexed by loop index, no shared-state contention), then after the parallel region do a single-threaded sequential `for` loop summing the array in a fixed order — this keeps the expensive per-droplet computation parallel while making the reduction itself deterministic. Compile with `g++ -O2 -fopenmp`, then run the binary 5 times passing your computed GC ratio as the argument, appending each stdout line to `/home/user/stability.log`, and confirm all 5 lines are byte-identical.
