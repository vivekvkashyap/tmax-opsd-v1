Don't call the oracle 250k times — it's deliberately slow; the point is to replicate its decision logic natively in SQL. Reverse-engineer the rule by sampling: for a handful of chunks, compute candidate features from the schema (siblings sharing `server_id`+`file_path_hash`, how many are strictly newer by `timestamp`, their cumulative `chunk_size`, and `is_encrypted`), then call the oracle on those same chunks and see which feature combinations correlate with a `1` output — vary one dimension at a time to isolate exact thresholds. Once you have a hypothesis (e.g. unencrypted, at least N newer siblings, whose total size exceeds some multiple of the current chunk's size), implement it in SQLite with window functions — `COUNT(*) OVER (PARTITION BY server_id, file_path_hash ORDER BY timestamp DESC ROWS BETWEEN UNBOUNDED PRECEDING AND 1 PRECEDING)` and the analogous `SUM(chunk_size) OVER (...)` — to get per-chunk "count of newer" and "sum of newer sizes" in one pass, then filter and write chunk_id to CSV. Re-validate thresholds with more oracle samples before finalizing, since the bar is exact F1=1.0 within a 5-second runtime (SQL set operations, not per-row Python loops).
