Open `/app/artifact_raw.wav` in binary mode, `mmap.mmap(fd, 0, access=ACCESS_READ)`, and slice from byte offset 44 onward to skip the standard WAV header — write that slice to `sys.stdout.buffer` in chunks (e.g. loop writing fixed-size slices and `flush()`) rather than one giant write, since the task emphasizes streaming. Keep the script's stdout free of any extra prints (logging/errors must go to stderr) since anything else written to stdout would corrupt the piped byte stream. Run it as `python3 script.py | gzip > /home/user/artifact_payload.gz` in a single shell command so the pipe is exercised end-to-end. Before finishing, verify correctness yourself: `gunzip -c artifact_payload.gz | cmp - <(tail -c +45 /app/artifact_raw.wav)` (or equivalent Python byte comparison) should show zero differences, since the automated check requires an exact, lossless match of the raw PCM data chunk — get the header-skip offset and byte-for-byte fidelity exactly right rather than approximate.
