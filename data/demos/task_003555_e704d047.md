Break this into independent, verifiable stages rather than one monolithic script. First extract video frames with `ffmpeg -i /app/server_dashboard.mp4 -vf fps=1 frame_%d.png` (or OpenCV), then compute each frame's mean grayscale pixel value programmatically (e.g. PIL `.convert('L')` then average pixel array) — do not eyeball it — and threshold at >128 per second index to get the alert vector; print/inspect the computed averages to sanity-check before trusting them. Second, clean the CSV: validate `ip_address` with a strict IPv4 regex/library (reject malformed octets like `999.999.999.999`), forward-fill missing `cpu_load` only among the rows that survive validation (default `0.0` if the first valid row is missing it), and compute the 3-row rolling average of `response_time` over the previous 1-3 valid rows (shrinking window for the first two). Merge on `timestamp` and write `/home/user/cleaned_logs.csv` with the exact header order given. Bulk-load it into SQLite (`sqlite3 /home/user/logs.db` with `.mode csv` and `.import`, or Python's `csv`+`sqlite3` module) into a `metrics` table. Finally implement `/alerts` and `/stats` as a Python `http.server` subclass, launched in the background (e.g. `nohup ... &`) so it stays alive after your script exits, and manually `curl` both endpoints to confirm correct JSON before finishing.
