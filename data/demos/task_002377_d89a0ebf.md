Write `/home/user/query.c` to link against `sqlite3.h`/`-lsqlite3`: open `/home/user/network.db` with `sqlite3_open`, then execute a single SQL statement combining a recursive CTE with JSON extraction rather than doing the tree-walk or JSON parsing in C. The CTE should be: `WITH RECURSIVE sub(id) AS (SELECT 1 UNION ALL SELECT nodes.id FROM nodes JOIN sub ON nodes.parent_id = sub.id) SELECT SUM(json_extract(config,'$.cpu')), SUM(json_extract(config,'$.ram')) FROM nodes WHERE id IN (SELECT id FROM sub);` — the key correctness point is starting the anchor row at the target id itself (not just its children) and joining child rows via `parent_id = sub.id` so it correctly recurses to grandchildren etc., stopping naturally when no more children exist. In C, prepare this with `sqlite3_prepare_v2`, step once with `sqlite3_step`, pull both aggregate columns with `sqlite3_column_int`, then `fprintf` them to `/home/user/output.txt` in the exact literal format `CPU: <int>, RAM: <int>` (no extra text/newline mismatch). Compile with `gcc query.c -o query -lsqlite3` (install `libsqlite3-dev` first if headers are missing) and run it to produce the output file.
