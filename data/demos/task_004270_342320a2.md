Start by inspecting the schema and data with `sqlite3 /home/user/activity.db` before writing any code. Create indexes on the foreign-key-like columns actually used in your join/filter (posts.author_id, comments.post_id, comments.commenter_id) — these speed up the join but don't change correctness, so get the query right independently. Write the core SQL as a join from comments to posts (to get the post's author) to users (for names), grouping by (commenter_id, author_id) and counting, then explicitly filter out rows where commenter_id = author_id (self-loops) — do this filtering in the SQL WHERE/HAVING clause, not as an afterthought in Python, so the materialized weights are correct from the source. In `/home/user/etl_graph.py`, separately query the full `users` table for the `nodes` array (every user must appear even with zero edges — a naive query built only from the edges join will silently drop isolated users), and query the aggregated edges for the `edges` array, matching the exact JSON key names given. For `top_influencer.txt`, sum incoming edge weights per target user_id after your edges are computed and write just the integer id of the max — watch for ties and confirm your aggregation direction (target = comment recipient, not commenter) matches the spec.