Treat the WAF as a black-box reverse-engineering problem, not something you can guess. Build a fuzzing/probing harness: repeatedly call `/app/legacy_auth_daemon '<json>'` with varied `username` lengths and varied `token` contents (include common SQLi keywords like UNION, SELECT, DROP, --, and clean alphanumeric tokens), check the exit code (non-zero or a crash/signal means malicious), and log which field/pattern flips the result. Look for a boundary on username length (bisect around common thresholds like 16/32/64 chars) and for which specific substrings/regex in token trigger the crash — build your filter_request() logic from what you actually observe the daemon do, not from assumptions. Implement filter_request(payload_string) in /home/user/waf_filter.py to parse the JSON, apply the discovered length/pattern rules, and return False (reject) on any pattern that matches your observed crash conditions, True otherwise — make it resilient to malformed JSON too (return False, don't crash). Separately, launch QEMU with `-netdev user,hostfwd=tcp::8080-:80` against /app/backend_vm.qcow2 in the background, generate a self-signed cert with openssl and serve it via a Python HTTPS server on 8443, and wire both into /home/user/start_services.sh so they persist after the script returns.