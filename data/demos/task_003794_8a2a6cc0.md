Break this into independently testable stages before wiring them into a server. First, write the video-extraction step standalone: open `/app/oscillator.mp4` with OpenCV, iterate frames, and for each frame threshold/mask for the green circle (pure RGB 0,255,0 against black — a simple color mask plus centroid via `cv2.moments` or contour bounding box works), recording the x-coordinate per frame; print/verify a handful of values manually before trusting it, and note the trajectory array length must equal the actual frame count. Second, implement the ODE simulator as its own function using `scipy.integrate.solve_ivp` with method `RK45` and tight `rtol`/`atol`, converting the 2nd-order ODE into a first-order system (state = [x, xdot]) with initial conditions x(0)=trajectory[0], xdot(0)=0, evaluated at the same time points as the video frames (t = i/30). Third, implement the Monte Carlo search using `concurrent.futures.ProcessPoolExecutor` (or `multiprocessing.Pool`) to evaluate MSE for randomly sampled (c,k) pairs in parallel, tracking the best. Only after each piece works alone should you wrap them behind a Flask/FastAPI app on 0.0.0.0:8000 with the three required routes, launched in the background so it stays alive for external requests (e.g. via `nohup`/`&` or a proper daemonized process manager) — verify with `curl` yourself that /ping, /trajectory, and /fit actually respond correctly before finishing.