Debug this the way you'd triage a real incident: reproduce first, then fix each layer, then confirm end-to-end. Trace `preprocess.sh`'s handling of the audio path directly — run it manually against `/app/incident report 01.wav` (note the spaces in the filename) and watch it break; the likely cause is an unquoted variable expansion (`$1` instead of `"$1"`) in a command like `cat`/`ffmpeg`, which word-splits the path — fix by quoting every use of the path variable throughout the script. Separately verify `math_engine.py`'s `calculate_roots` against the standard quadratic formula by hand: recompute the discriminant `b**2 - 4*a*c` and the two roots `(-b ± sqrt(discriminant)) / (2*a)` yourself for a known example and compare to what the current code returns to spot exactly which term is wrong (sign of discriminant, missing division by 2a, etc.), then correct only that bug. Once both are fixed, transcribe the actual audio content (use whisper/ffmpeg) to confirm which a/b/c values it encodes and validate your fixed pipeline reproduces the correct roots for it. Finally wire `POST /calculate` in the Flask/FastAPI app to call the fixed preprocessing and math functions, sort roots ascending, handle zero/negative-discriminant cases per spec, and leave the service running on 0.0.0.0:8080.
