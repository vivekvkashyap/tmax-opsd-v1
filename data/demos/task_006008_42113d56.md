In `/home/user/optimizer.service`, add `After=billing-api.service` (and typically `Wants=` too) under `[Unit]`, and point `ExecStart` at `python3 /home/user/cost_optimizer.py` (the current `ExecStart=/bin/false` is the reason it "fails to start reliably" — it's a stand-in bug, not a real timing race). In the Python script, compute current time with `datetime.now(ZoneInfo("America/New_York"))` — never rely on the system/container timezone or `time.localtime()`, since the verifier will run under a faked system clock/timezone and only the explicit zoneinfo conversion will be correct. Business hours means `9 <= hour <= 17` inclusive (up to 17:59). Read `port_mapping.json` inside a try/except so any missing-file or JSON-decode error falls through to the exact `echo "Error: missing config"` output. Generate the `socat TCP4-LISTEN:...` lines only during business hours, the off-hours line otherwise, and finish by `os.chmod(path, 0o700)` on the generated script.
