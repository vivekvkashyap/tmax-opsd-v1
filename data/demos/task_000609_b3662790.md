Read the C code carefully to find exactly where the leakage happens: the mean/stddev computation loops over ALL records regardless of `split`. Fix it by computing `sum`/`sq_sum`/`mean`/`stddev` using only rows where `split == "train"` (same population-stddev formula, `sqrt(sq_sum/count)`, just restricted to the train subset), then apply that single train-derived mean/stddev to normalize both train and test rows in the existing normalization loop. Do not touch the bootstrap seed (`srand(42)`), iteration count (1000), sampling-with-replacement logic, or output formatting (`%.4f`) — those must stay bit-identical to the original. After editing, recompile with `gcc normalize_and_bootstrap.c -lm -o run_pipeline` and rerun it against `/home/user/data.csv` to regenerate both output files. Sanity-check your fix conceptually: since train stats now don't include test data, the bootstrap mean of the (now correctly self-normalized) train split should land very close to 0.0, while the test mean will shift compared to the buggy version — if your test_mean number is nearly identical to the pre-fix leaky output, you likely didn't actually change which rows feed the mean/stddev computation.