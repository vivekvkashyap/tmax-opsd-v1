Write and run the Go program in small verifiable steps rather than all at once. Use `compress/gzip` wrapped around `os.Open` plus `encoding/csv` (or manual line splitting) to read every `part-*.csv.gz` under `/home/user/artifacts/` — glob the directory first and iterate all matches, since missing even one file will skew every downstream statistic. Parse `id,y_true,y_pred_A,y_pred_B` as float64s, accumulate squared errors for A and B to get MSE_A/MSE_B, and separately accumulate the per-record difference D_i = SE_A_i - SE_B_i into a slice. Compute the paired t-statistic carefully: sample mean of D, sample variance of D using N-1 (not N) in the denominator, then t = mean(D) / (stddev(D)/sqrt(N)) — getting the ddof wrong here is the classic bug. Set `is_significant` to true only if t is strictly greater than 1.96. Marshal the four fields to `/home/user/report.json` with `encoding/json`, matching field names and types exactly (booleans as JSON booleans, floats as numbers). Compile with `go build` or `go run` and confirm the output file is actually produced before considering the task done.