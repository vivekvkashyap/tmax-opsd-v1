Write the Go program to walk `source_dir` using `filepath.WalkDir` (or `filepath.Walk`), and at each entry check its `DirEntry.Type()`/`os.Lstat` result: if the entry is a symlink (`Mode()&os.ModeSymlink != 0`), skip it entirely (do not follow it, do not descend into it) — this is what prevents the infinite loop from the two symlink cycles in the fixture, and using `filepath.Walk` without an explicit symlink check is the classic mistake that hangs. Only process entries where `Info.Mode().IsRegular()`. For each regular file, stream its bytes through `sha256.New()` to get the hex digest, and keep a `map[string]bool` of seen hashes so you only copy the *first* file with a given hash (in walk order) and skip later duplicates. Ensure `dest_dir` exists via `os.MkdirAll`, then copy accepted files there named `<first8ofsha>_<originalBasename>`. Collect `{original_path, new_filename, sha256}` for each copied file, sort the slice by `new_filename` before marshaling, and write it as JSON to `manifest_path`. Parse `config.json` first with `encoding/json` to get the three paths rather than hardcoding them. Compile with `go build` and run it once so `cleaned_data/` and `manifest.json` are actually produced before finishing.
