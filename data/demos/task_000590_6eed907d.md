Do not guess the fix from the current buggy code — recover ground truth from the two independent sources first. Run `tesseract` (or similar OCR) on `/app/whiteboard_notes.png` to extract the exact recurrence relation text (the constants and bit-shift amount), and independently build a small regression harness that calls `/app/oracle_prng <seed> <iterations>` for a range of seed/iteration combinations and records the outputs as your ground-truth test vectors. Then use `git log`/`git bisect` on `/home/user/prng_project`: mark the earliest commit as good and HEAD as bad, and at each bisection step compile the C++ at that revision and compare its output against the oracle for your test vectors to classify good/bad, which will pinpoint the exact commit that introduced the regression. Cross-check that the diff introduced at the bad commit matches what the whiteboard image says the formula should be — the two sources should agree on the correct constants/shift. Apply the fix on top of the current HEAD (don't just revert to an old commit, since later commits may contain unrelated legitimate changes), recompile, and verify bit-for-bit equivalence against the oracle across many random seed/iteration pairs before producing the final binary at `/home/user/fixed_prng`.
