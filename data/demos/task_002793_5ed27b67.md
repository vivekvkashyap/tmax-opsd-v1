Implement this as ordered, testable stages in Go, matching the spec's math exactly rather than approximating. Parse the CSV preserving row order (critical for the rolling average, which is defined over the file's row order, not any resorted order). One-hot encode server_role into three int columns via simple string comparison. Min-max scale memory_usage using the dataset's global min/max computed in a first pass. For the rolling average, implement the "shrinking window at the start" rule literally: row 0 = itself, row 1 = mean of rows 0-1, row i>=2 = mean of rows i-2..i — this is different from pandas' default behavior only in edge naming, so getting index bounds right at i=0,1 is the main trap. For the deterministic split, hash the timestamp string's exact bytes with MD5 (`crypto/md5` in Go), take byte[0], and split on `(byte0 % 10) < 8` — mismatched string formatting of the timestamp (e.g. extra whitespace) will silently produce a wrong split, so hash exactly the CSV field as read. Output floats formatted to exactly 4 decimal places and columns in the specified order. Compute the baseline accuracy only over the test set and format as `Accuracy: 0.XXXX`.
