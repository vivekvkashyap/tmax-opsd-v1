This is a single-pass awk transformation over `/home/user/raw_metrics.csv` — write it as one awk script rather than chaining many separate sed/grep passes, since each field needs coordinated per-row transforms. Skip the header row (NR==1), then for each data row: split the ISO timestamp on `T` and `:`/`Z` to get date (before T) and hour (first two digits after T); split ip_address on `.` and replace the 4th octet with `XXX` (rebuild with the first three octets + "XXX"); compare cpu_usage numerically (not as a string) against 80.0 using `>=` to set usage_category to HIGH or NORMAL; print fields in the exact required column order `date,hour,user_id,masked_ip,cpu_usage,usage_category` with commas as OFS, preserving cpu_usage's original numeric formatting (don't let awk reformat 80.0 as 80). Print the header line first before the data loop. After generating clean_metrics.csv, count data rows with `wc -l` on the original file minus 1 (or count rows processed inside awk via a counter and END block) and write `Processed <N> rows.` to pipeline.log — get N from actual rows processed, not a hardcoded count.