First inspect the streams with `ffprobe /app/network_topology.mp4` to confirm which stream index carries subtitles, then extract with `ffmpeg -i /app/network_topology.mp4 -map 0:s:0 subs.srt`. Clean the SRT with a script (strip sequence-number lines, timestamp lines like `00:00:01,000 --> ...`, and blank lines) leaving only `Source,Target,Weight` CSV rows — don't hand-guess the data, derive it from what ffmpeg actually extracts. Load into SQLite via `sqlite3 /app/graph.db` with `.mode csv` / `.import`, then add indexes on `source` and `target` columns (a composite index or two single-column indexes both satisfy "optimized filtering"). For the Go service, the key fix is replacing the cross-join query with an explicit `JOIN edges e2 ON e1.target = e2.source` condition so the middle node actually connects both hops — build the JSON response with `path` strings joining all three nodes and `total_weight` summed, sorted ascending by weight. Check the `Authorization` header manually before running the query and return 401 early if it doesn't match; run the compiled server in the background (`go build && ./server &`) so it stays listening on port 8080.
