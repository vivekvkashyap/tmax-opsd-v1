Start by recovering the source account ID yourself: transcribe /app/whistleblower.wav with a speech-to-text tool (e.g. `whisper` or any available ASR), read the transcript to find the account ID it names — don't assume a value, extract it from the audio output. Then inspect /app/finance.db's `transfers` schema with `sqlite3 /app/finance.db .schema`, and write a recursive CTE (`WITH RECURSIVE reached(acct) AS (SELECT to_acct FROM transfers WHERE from_acct = :source UNION SELECT t.to_acct FROM transfers t JOIN reached r ON t.from_acct = r.acct) SELECT DISTINCT acct FROM reached ORDER BY acct;`) to find all downstream accounts reachable from the source — watch out for cycles (a self-loop or cycle back to an already-visited node) causing infinite recursion, so dedupe as you go (UNION, not UNION ALL, handles this) and cap iterations if needed. Add indexes on `from_acct` (and `to_acct` if helpful) with `CREATE INDEX` and check the plan with `EXPLAIN QUERY PLAN` to confirm the recursive step uses them. Finally stand up a small HTTP server (e.g. Python http.server/Flask) bound to 0.0.0.0:9000 that on GET /chain checks the `X-Compliance-Auth: auditor_key` header (403 if missing/wrong) and otherwise returns the deduplicated, ascending-sorted JSON array of downstream account ID strings; leave it running in the background for the verifier.