Implement this as a straight pipeline of well-defined numeric steps in Go using only the standard library (encoding/csv, encoding/json, math), and get each stage right before chaining to the next. Parse spectra.csv into an ordered slice of 100 intensities. For smoothing, apply the boundary rules exactly as specified — 2-point average at index 0 and index 99, 3-point average everywhere else — off-by-one errors here (e.g. treating index 1 as a boundary, or averaging over the wrong window) will silently corrupt every downstream value. Split the smoothed slice at x=50 into Domain A (indices 0-49) and Domain B (indices 50-99), then normalize each domain's smoothed values by its own sum so each forms a probability distribution summing to 1.0 — do this separately per domain, not globally. For each domain build a uniform comparison distribution with probability 1/N_domain per point, and compute KL divergence `sum(p_i * ln(p_i / q_i))` using natural log (`math.Log`), summing only over the domain's own points. Write the two scalars to fit_results.json with exactly the keys `kl_domain_a` and `kl_domain_b`. Before finalizing, re-derive the numbers independently (e.g. spot-check a couple of terms by hand) to make sure your indexing and normalization match the spec, since the verifier compares against a reference computation with tight (1e-5) tolerance.