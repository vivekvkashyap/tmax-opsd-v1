Start by reading `train_model.c` end to end and locating exactly where mean/stddev are computed relative to the bootstrap loop — the bug is that scaling statistics are computed once over the whole dataset before the `for` loop that draws bootstrap samples. Fix it by moving the mean/stddev computation inside the loop body so it is recomputed from only the current bootstrap sample's rows each iteration, then apply those same statistics (not fresh ones) to both the in-bag sample and the OOB rows for that iteration. Keep `srand(42)` and the sampling/index-selection logic byte-for-byte identical so the same bootstrap draws occur — only the scaling stage moves. Compile with a normal `gcc -O2 -lm` invocation into `/home/user/bin/train_model_fixed` (create the directory first), run it, and capture the single printed OOB MSE value. Before writing `/home/user/metrics.txt`, sanity-check your number is plausible for the dataset (not wildly different from the leaky version) and write only the bare float to the file, no extra text. Double-check you didn't accidentally still use the leaked global stats anywhere (e.g. a leftover variable).
