This is an interactive-process automation task — use `pexpect.spawn` to drive `/home/user/legacy_service/setup.sh`, matching each `read -p "..."` prompt with `child.expect()` on a distinctive substring of the prompt text and responding with `child.sendline()` for the four required values in the order the script asks for them; end with `child.expect(pexpect.EOF)` (or similar) so the script fully completes and writes its config file before your wrapper exits. Build `run_pipeline.sh` as an ordinary sequential bash script: create the venv with `python3 -m venv`, source `venv/bin/activate` (or call `venv/bin/pip`/`venv/bin/python` directly) to install pexpect and run automate_setup.py, then launch the daemon with `nohup ... &`, capture `$!` into service.pid, `sleep 3`, and use `curl -s` to hit the health endpoint, redirecting stdout to health_check.log. Make the script executable with `chmod +x` and actually run it — don't just leave it written — and confirm the daemon process is still alive (not exited) before finishing, since the task requires it to remain running.
