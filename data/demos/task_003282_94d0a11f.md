Do NOT try to disassemble the stripped binary — instead treat it as a black-box oracle and infer the rejection rule empirically by diffing the two corpora: run `/app/deploy-validator` against every file in `/app/corpus/clean/` and `/app/corpus/evil/`, note pass/crash for each, then `diff`/inspect the evil files against clean ones to spot the structural patterns unique to crashers (e.g. specific `-netdev` subnet/CIDR combinations, or malformed `mac=` address field shapes) — form a hypothesis, then verify it correctly separates 100% of both sets before coding your Rust filter. Implement matching logic in Rust (parse the config text, extract the relevant substrings/CIDR and MAC fields with regex or manual parsing, and apply your inferred rule) that exits 0 for safe and non-zero for dangerous, then build with `cargo build --release` and copy/link the binary to exactly `/home/user/qemu_net_filter`. Append a correctly-timestamped ACCEPTED/REJECTED log line per invocation to `/home/user/filter.log` using the exact format given. Write `/home/user/logrotate.conf` with `daily`, `rotate 5`, `compress`, and a `create 0644` directive, then sanity check it with `logrotate -d`.
