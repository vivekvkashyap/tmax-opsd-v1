Fix each layer, then integration-test through the full stack.
1. Nginx: in `/home/user/ci_env/nginx.conf`'s `location /` block, add `proxy_set_header Upgrade $http_upgrade;` and `proxy_set_header Connection "upgrade";` (with an `http { map $http_upgrade $connection_upgrade {...} }` block if you want it robust to non-upgrade requests) — `proxy_http_version 1.1` is already set but WebSocket upgrades still need those headers forwarded explicitly. Start it with `nginx -c /home/user/ci_env/nginx.conf`.
2. Emulator: implement `process_command` as a simple stack machine — split on whitespace, iterate tokens, on `PUSH` consume the next token as int and push, on `ADD`/`MUL` pop two, combine, push result; wrap in try/except so malformed input or an empty/invalid final stack returns `"ERROR"` instead of raising. Launch it in the background before testing.
3. Benchmark: use the `websockets` library to open one connection (or 100 sequential sends) to `ws://127.0.0.1:8080`, send the fixed payload 100 times, await and verify each reply, time the whole loop with `time.perf_counter()`, and write the exact JSON shape required (integer count, milliseconds).
4. Validate end-to-end by manually connecting through the proxy before trusting the benchmark numbers — that's what confirms nginx, emulator, and script are all wired correctly.
