Treat `/app/seq_embedder` as a black-box binary: run it on `/home/user/data/train.fasta` (check its usage/help or try common invocation patterns like `./seq_embedder input.fasta output.bin` or stdin/stdout piping) and load the resulting output as raw little-endian float32 with `numpy.fromfile(path, dtype='<f4').reshape(-1, 64)` — get N from the reshape, don't hardcode it. Fit `sklearn.decomposition.TruncatedSVD(n_components=3)` on the training embeddings and persist both the fitted SVD object and a `sklearn.mixture.GaussianMixture(n_components=2)` fitted on the 3D projection (pickle them, e.g. with `joblib`, alongside `evaluate.py` or embedded via loading at import time) so `evaluate.py` can reuse the *exact same* trained transform rather than refitting per file — refitting SVD/GMM on a new test set would silently change the projection and break scoring. In `evaluate.py`: invoke the binary on the given FASTA path argument, load the float32 output the same way, apply `svd.transform(...)` (not `fit_transform`), call `gmm.score(...)` (returns mean log-likelihood already), and print only that float, nothing else, to stdout.