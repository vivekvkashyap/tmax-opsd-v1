Start by trying `pip install .` on the vendored package to see the exact traceback rather than guessing — build failures usually point straight at the broken file and line. A common cause with vendored packages that read a README inside setup.py is a locale/encoding mismatch: `open('README.md')` uses the platform default encoding, and if the README has non-ASCII characters (emoji, unicode symbols) this raises a `UnicodeDecodeError` under a non-UTF-8 locale. Fix by passing `encoding='utf-8'` explicitly to the `open()` call in setup.py, then reinstall and run `pytest tests/` to confirm the decomposition routines pass before relying on them. For the detector, use the installed library's SVD/eigen utilities to check symmetry (compare the matrix to its transpose within a 1e-5 tolerance) and positive-definiteness (all eigenvalues strictly greater than 0); print exactly `CLEAN` or `EVIL` with no extra whitespace, and make sure exit code is always 0 even when the matrix is evil. For visualize.py, iterate `.npy` files in the given `--dir`, compute SVD per matrix, collect the first two singular values, and scatter-plot them with matplotlib, saving to the exact output path.