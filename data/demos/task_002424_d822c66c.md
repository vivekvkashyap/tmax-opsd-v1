Diagnose the broken service chain before writing pipeline logic: check `/app/api/config.ini` for a Redis host setting pointing at an unreachable hostname (a Docker-style service name) and change it to `localhost`/`127.0.0.1`, then restart the API process (`python3 /app/api/server.py &`) and confirm with `curl` that `http://127.0.0.1:8000/api/v1/sequences` now responds before building the rest of the script. In `/home/user/process_pipeline.sh`, loop over sequence IDs from that endpoint (`jq` to parse JSON), download each HDF5 file, and use `h5dump` to pull out the `kmer_frequencies` and `melting_curve` datasets — parse `h5dump`'s text output carefully with `awk`/`sed` to extract just the numeric values, since it includes lots of structural boilerplate. Compute the Manhattan distance of `kmer_frequencies` against a uniform 1/16 reference distribution, integrate `melting_curve` with the trapezoidal rule (1-degree spacing) using `awk`/`bc` for floating point, then combine as `Integration / (Distance + 1)`. Sort results by Sequence_ID and write the CSV. Validate a couple of your computed scores by hand against the raw HDF5 values before finishing, since correctness is checked by comparing against precise ground-truth numbers.