Parse `/home/user/waits_for.csv` (skip the header row) into an adjacency list keyed by transaction ID, treating each row as a directed edge waiter -> holder. To find the globally shortest cycle, run a BFS from every node: for node `v`, BFS over outgoing edges and, the moment you reach `v` again via a path of length >= 1, that path length is the shortest cycle length starting at `v` — track the minimum over all starting nodes, and when a BFS finds a return-to-start edge, reconstruct the actual node set on that cycle (via parent pointers) rather than just its length. Because ties must be broken by "numerically smallest transaction ID among all such cycles" (not smallest starting node), you need to compare the full node-id sets of every cycle achieving the minimum length and pick the one whose smallest member is smallest — so don't stop at the first BFS that finds the minimum length; run BFS from every node, collect all cycles tied for shortest, then compare their minimum IDs. Sort the winning cycle's node IDs ascending and write them as a JSON array `[a, b, c]` to `/home/user/deadlock.json`, matching the bracket/comma-space style shown in the task description. Verify by hand-tracing the two shortest cycles in the sample data to make sure your tie-break logic picks the right one.
