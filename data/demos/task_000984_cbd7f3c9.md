Start with OCR: run `tesseract /app/config.png stdout` (or similar) and parse out the `PORT` and `AUTH_TOKEN` lines from the recognized text rather than assuming their values — verify the OCR output looks sane before using it. For the race condition in `build.sh`, notice both `build_part_a` and `build_part_b` write to the same shared `temp.c` file concurrently before compiling — give each background job its own temp file name (e.g. `temp_a.c`/`temp_b.c`) so parallel `gcc -c` invocations don't stomp on each other; keep the rest of the build logic (backgrounding with `&`, `wait`, final link step) unchanged. For `calc_factor.sh`, replace the plain `bc` invocation (which defaults to integer-ish output) with `echo "scale=6; 104348 / 33215" | bc` so you get 6 decimal places instead of a rounded integer. Then run `./build.sh` to produce `server.sh`, and finally launch `./server.sh <PORT> <AUTH_TOKEN> <FACTOR>` in the background using the OCR'd port/token and your computed factor, confirming with a local curl that it responds correctly with the Authorization header before finishing.