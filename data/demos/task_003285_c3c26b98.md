Use a small script (jq, python, or grep/sed) to parse `/home/user/graph_export.json`: extract the `.label` field from every entry in `nodes`, dedupe, and sort alphabetically for line 1; extract the `.type` field from every entry in `edges`, dedupe, and sort alphabetically for line 2. For line 3, open `/home/user/db_error.log` and read the two Cypher queries tied to the deadlocked transactions — find the relationship variable that appears in both queries' `MATCH ... -[r:TYPE]-` (or reversed `<-[r:TYPE]-`) pattern AND is the target of a `SET` clause in both; that shared TYPE is the conflicting edge type (note the pattern can be traversed in either direction between the two queries, so match on the type name in the `[r:TYPE]` bracket, not just on textual query order). Write exactly three lines, comma-separated with no spaces after commas, to `/home/user/deadlock_report.txt`, matching the given example format precisely.