Use `tesseract` on `/app/corrupted_nodes.png` to recover the actual list of corrupted node IDs — don't hardcode a guessed set, read it from the image. However, note the core requirement doesn't actually depend on branching per node: since the corrupted index silently misleads any indexed query, the safe fix is to always bypass the index for every lookup, not just the ones on the recovered list. In your C program, open the SQLite DB with `sqlite3_open`, prepare a parameterized statement using `SELECT count(*) FROM routes NOT INDEXED WHERE src = ?` (the `NOT INDEXED` clause forces a full table scan and sidesteps the stale index entirely), bind the CLI argument as an integer, step once, and print only the resulting count followed by a newline. Compile with `gcc -o /home/user/route_degree route_degree.c -lsqlite3`. Since correctness is checked by fuzzing many src values and comparing against the expected true-degree computation, focus on exact output formatting (bare integer, no extra text) and correct SQLite error handling rather than trying to special-case the OCR'd node list.