Start by running `tesseract /app/system_architecture.png -` (or convert/preprocess with ImageMagick first if OCR output is noisy) to recover the allowed dependency edges as text; parse that output into a set of (source, target) pairs rather than hardcoding anything, and sanity-check it by eyeballing the raw OCR text before trusting it. Write a manifest parser that reads `Target:`, `Depends-On:`, and `Cmd:` lines with a simple line-based or regex parser, splitting `Depends-On` on commas and trimming whitespace. For each dependency, reject (INVALID, exit 1) unless the (Target, Dependency) pair is exactly in the OCR'd allowed-edges set. For the `Cmd` field, don't just regex-search for `$(` or backticks — build a small character-by-character state machine that tracks string/quote context so it also catches obfuscated variants (e.g. nested/escaped substitutions, backtick pairs split across quoting, whitespace tricks). Only print `VALID`/exit 0 when both dependency and command checks pass cleanly. After writing the script, iterate against every file in `/app/corpora/clean/` and `/app/corpora/evil/`, checking exit codes and stdout match expectations, and refine the state machine/graph check for any misses.
