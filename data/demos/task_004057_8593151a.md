Notice the math simplifies drastically before writing brute-force SVD code: for a 15x4 one-hot matrix where each row has exactly one 1, the singular values reduce to the square roots of each column's total count (the counts of A, C, G, T in that window), so the nuclear norm for a window equals sqrt(count_A) + sqrt(count_C) + sqrt(count_G) + sqrt(count_T). You may still compute it via actual `numpy.linalg.svd` on the constructed one-hot matrix for correctness/robustness, but recognizing the shortcut helps you sanity-check results and write faster code over many windows. Implementation steps: load the JSON, slide a length-15 window (step 1) over `background`, compute each window's nuclear norm, take the max as `B_max`; slide the same window over `target`, keep windows whose nuclear norm is strictly greater than `B_max`; deduplicate the resulting substrings, sort them alphabetically, and write one per line to `/home/user/candidates.txt`. Be careful with edge cases: window boundaries (`len(seq)-14` windows total), exact strict-greater comparison (not >=), and case-sensitivity/alphabet validity of the sequence characters.