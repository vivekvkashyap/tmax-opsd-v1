OCR the spec image first (`tesseract /app/spec.png -` or similar) to recover the exact formula and any special-case handling it describes — read it carefully rather than assuming a formula shape, since the special case (e.g. behavior at x=0) is easy to miss and is exactly what the crash bug is masking. Then rewrite `/home/user/compute.c`: parse the input with `strtoull` into a `uint64_t` (not `atoll` into a signed `long long`, which mishandles large unsigned values and negative-looking bit patterns), remove the artificial null-pointer-dereference crash and instead implement the documented special case as a normal return path, and perform all arithmetic (multiply, add, XOR) using `uint64_t` operands throughout rather than truncating through a 32-bit `int32_t` intermediate — that 32-bit truncation is exactly the "numerical instability" described, and unsigned 64-bit math is well-defined (wraps predictably) so it avoids UB while matching what an unsigned reference implementation would produce. Strip any debug/tracing output from the final build. Compile the fixed program to `/home/user/compute_fixed`, then sanity-check it yourself against several inputs (0, small values, very large values near 2^64) by hand-computing expected results from the formula before relying on the fuzz verifier.