Structure this as a graph-construction step followed by a separate statistics step — get each right independently before combining them.

1. Parse `/home/user/sequences.fasta` into an ordered list of (id, sequence) pairs using a simple line-based reader (header lines start with `>`).
2. For overlap detection, for every ordered pair (i, j) with i != j, find the maximum k >= 10 such that the last k characters of sequence i equal the first k characters of sequence j (a standard suffix-prefix overlap check, e.g. by checking decreasing k from `min(len_i, len_j)` down to 10 and stopping at the first match). Add a directed edge i->j only if such a k exists; the edge is present/absent regardless of the exact overlap length.
3. Compute out-degree per node, then build the frequency histogram (out-degree x -> count of nodes y), dropping any x with y=0.
4. Take natural log of each y, and run OLS linear regression of ln(y) against x (standard least-squares slope/intercept formulas). Recover `b = -slope` and `A = exp(intercept)`.
5. Write `total_nodes`, `total_edges`, `A`, `b` as a JSON object to `/home/user/analysis_results.json`, with floats at 4+ decimal places. Implement this all as a Rust binary crate under `/home/user/sequence_graph` using `serde_json` for output; do not modify the FASTA input.
