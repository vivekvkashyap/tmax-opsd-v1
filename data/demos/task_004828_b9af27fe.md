Start by reading `process_data.sh` end-to-end before touching anything — the bug is in how it prepares text for length counting, not in the counting arithmetic itself. Check for two classic pitfalls: (1) a locale setting like `LC_ALL=C` forces byte-oriented string handling in `awk`/`wc`, which miscounts multi-byte UTF-8 characters as multiple characters — switch to a UTF-8-aware locale (e.g. `C.UTF-8` or `en_US.UTF-8`) so `awk`'s length()/substr() operate on codepoints. (2) Any manual sed-based unicode-escape "fixing" applied before piping to `jq` is likely fighting jq's own native `\uXXXX` decoding — jq already decodes `\uXXXX` (including surrogate pairs) into proper UTF-8 when you use `jq -r`, so remove any pre-processing that mangles the escapes and let jq do the decode. Test your fix in isolation first: pipe a line with a `\uXXXX` escape and a line with raw multi-byte UTF-8 through your fixed pipeline and confirm `jq -r` outputs the correct character, then confirm length counts match expectations for both cases. Once decoding is correct, implement/verify the valid/invalid split (`len <= max_chars`), running average of valid lengths, and rolling average of the last 3 valid lengths (fewer if <3 exist), formatting both floats to exactly 2 decimals. Finally launch with `./start.sh &` and confirm with a manual `curl -X POST` that the JSON response matches the required schema before leaving it running.
