Recover the schema first: run `ffprobe -v quiet -show_format /app/voicemail.wav` (or `ffmpeg -i`) to read the `artist` tag, which holds a comma-separated list of column names — use that literal list (do not invent your own columns) when writing `/home/user/migrate.sh` to `CREATE TABLE audio_logs (...)` in `/home/user/metadata.db` with every one of those columns typed `TEXT`. For the validator, replace the skeleton `wav_validator.c` with real header parsing: read the file, confirm the first 4 bytes equal `RIFF` and bytes 8-11 equal `WAVE` (both exact byte comparisons, not just length checks), then walk the chunk list looking for a `fmt ` chunk, validating its declared size is at least the minimum expected (16 bytes for PCM) and that it doesn't run past the file's actual length — reject with exit code 1 on any short read, truncated chunk, bad chunk size, or missing `fmt ` chunk, and only exit 0 when every check passes. Write a `Makefile` compiling with `gcc -Wall -Werror -o wav_validator wav_validator.c` and fix any warnings that surface (uninitialized reads, sign-compare, etc. are common with WAV parsing) rather than suppressing them. Before finishing, self-test against both a real WAV (e.g. one you generate with `ffmpeg -f lavfi -i sine=...`) and deliberately corrupted copies (truncated file, `dd if=/dev/urandom` garbage, altered RIFF/WAVE magic) to confirm your exit codes match expectations, since the hidden grader runs a full adversarial + clean corpus.