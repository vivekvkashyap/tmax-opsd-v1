Set up the environment first (venv + numpy, then `gcc -o scorer scorer.c` in `/home/user/motif_project`), and confirm `./scorer <sequence>` works on a few manual test strings before writing the optimizer, since you need a reliable score oracle to optimize against. For the search itself, don't try to reverse-engineer the exact scoring formula from source — you don't have it and aren't meant to guess it structurally; instead treat `./scorer` as a black-box objective and run a local-search metaheuristic: start from a random 50-character ACGT string, repeatedly propose a random single-base mutation (or small block change), call `./scorer` on the candidate, and accept it if the score improves (or with a Metropolis/simulated-annealing acceptance criterion to escape local optima), looping until you reach or exceed a score of 150. Keep iteration counts and subprocess calls reasonable (batch by only calling scorer once per candidate) so the search finishes in reasonable time. Once you have a qualifying sequence, write it and its actual measured score to `/home/user/motif_project/optimized_sequence.txt` in the exact two-line format specified, using the venv's Python interpreter to run `optimize.py`.