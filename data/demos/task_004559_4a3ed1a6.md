Scaffold the Rust side with `cargo new --lib safe_eval`, then in `Cargo.toml` add `[lib] crate-type = ["cdylib"]` so it builds a shared object. In `src/lib.rs`, expose `#[no_mangle] pub extern "C" fn evaluate_expr(expr: *const c_char) -> i32`: inside, use `CStr::from_ptr` to convert the pointer to a `&str` (mark the function `unsafe` or wrap the pointer deref in an `unsafe` block), then tokenize on whitespace and fold left-to-right, applying `+`/`-` as encountered — don't use a generic expression evaluator, since operator precedence isn't needed here (just strict left-to-right). Build with `cargo build --release` so the `.so` lands in `target/release/libsafe_eval.so`. On the Python side, use `ctypes.CDLL(path)`, set `.argtypes = [ctypes.c_char_p]` and `.restype = ctypes.c_int32` on the function, encode each input line to bytes before calling (`line.strip().encode()`), and write one integer result per line to the output file in input order. Run the script yourself afterward to actually produce `secure_output.txt` — don't just leave it as dead code.