Start by transcribing `/app/alert_019.wav` (e.g. `whisper` or another STT tool) to recover the affected tenant ID and the authorization token spoken in the recording — these are the exact values your API's auth check and default test query must use, so listen/transcribe carefully rather than guessing a format. Inspect `/app/backup_catalog.db` with `sqlite3` to confirm the `backups` schema and sample rows. Build the Flask/FastAPI app so the `GET /api/backups` handler: (1) checks the `Authorization: Bearer <token>` header against the token you transcribed and returns 401 on mismatch/absence before doing anything else; (2) reads `tenant_id`, `page`, `limit` query params with the stated defaults; (3) queries SQLite filtered by tenant, ordered by `backup_timestamp DESC`, applying pagination via `LIMIT`/`OFFSET`; (4) converts `size_bytes` to `size_mb` rounded to 2 decimals; (5) groups the paginated rows by `datastore_type` into separate lists under `recovery_plan`, only including datastore types actually present in that page's results, nested exactly per the given schema. Run the service on `0.0.0.0:8080`, verify both an authorized and unauthorized request manually with `curl`, then write `/home/user/service_ready.txt` containing `READY`.
