Use the C oracle as your correctness reference for the algorithm, not as something to copy output from directly — compile it (`gcc /app/oracle/integrator.c -o /app/oracle/integrator`), read its source carefully to understand the exact RKF45 error-norm and step-adaptation formulas it implements, then compare that logic line-by-line against `calculate_error`/`adapt_step` in the Python service to find where they diverge (the task hints the adaptation uses a fixed multiplier instead of the proper `(tol / (2*error))**0.25` scaling — verify this yourself by reading both implementations rather than assuming). Fix the Python to match the oracle's formulas exactly, including any clamping/min-max step bounds the oracle applies. Bring up Redis, fix the Flask config's Redis host/port, and fix Nginx's missing `proxy_pass` for `/simulate`; test each service directly before chaining. Validate your fix by running the C oracle and the Flask endpoint on the same handful of inputs and diffing the numeric outputs yourself. Finally execute the validation notebook with `jupyter nbconvert --to notebook --execute` against the Nginx endpoint and save the executed copy.
