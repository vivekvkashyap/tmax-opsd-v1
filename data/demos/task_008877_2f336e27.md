Diagnose before scripting: read `/home/user/scripts/extract.py` closely for any conditional branch that gates real processing behind an environment variable (e.g. checking `os.environ.get(...)` and falling back to producing an empty dataframe when it's unset or wrong) — that's almost certainly why output is empty, not a code bug per se. Once you find the required variable/value, set it via `export` inside your orchestration bash script before invoking the Python script, rather than editing extract.py itself. Build `/home/user/pipeline.sh` to: create the venv and install pandas, export the needed variable, create `/home/user/processed/`, run extract.py against both raw CSVs to their processed outputs, then merge the two processed CSVs (concatenate rows, not columns), select only the four required columns in order, write a single header, save to `/home/user/final_features.csv`, and finally count data rows (excluding header) into `/home/user/row_count.txt`. Make the script executable and actually run it end-to-end yourself, then inspect both output files to confirm row counts and column values look sane before finishing.
