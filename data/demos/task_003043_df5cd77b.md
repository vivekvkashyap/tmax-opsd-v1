The "database is locked" error comes from two `sqlite3 .import` invocations writing to `graph.db` concurrently with `&`; the simplest robust fix is to not run them concurrently at all (drop the backgrounding and `wait`, or serialize the two imports with `;`), or alternatively enable WAL mode (`PRAGMA journal_mode=WAL;`) plus a busy timeout (`PRAGMA busy_timeout=5000;`) before each import if you want to keep them concurrent — either way, verify afterward that `SELECT count(*) FROM edges` reflects rows from both CSVs. For `process_results.sh`, write a single SQL query using `GROUP BY source`, `SUM(weight) AS w`, `HAVING w > 50`, `ORDER BY w DESC, source ASC`, `LIMIT 3`; format each row as `Node: <source>, Weight: <w>` with `sqlite3 -separator ", "` or manual string building, avoiding sqlite3's default pipe-delimited output. For the final aggregation line, don't just re-sum from the report text — re-query the database restricting to those same top-3 source names (e.g. via a subquery or `IN (...)`) and append `Total Top 3 Weight: <sum>` as the last line, matching the exact wording/spacing given in the spec.