First get the vendored library compiling: grep `/app/vendored/cpp-httplib/httplib.h` for the mangled class name near the top of the file and rename it back to `httplib::Server` so standard usage (`#include "httplib.h"`, `httplib::Server svr;`) compiles cleanly — don't rewrite the whole header, just fix the renamed identifier(s) consistently. Then translate `/app/reference/waf.go`'s decode loop faithfully into C++: it repeatedly (3 iterations) replaces the literal substrings `%2f`, `%2F`, `%2e`, `%2E`, `%c0%af`, `%C0%AF`, `%c0%ae`, `%C0%AE`, and `%25` in that exact order, then checks whether `"../"` appears in the fully decoded result — implement this as literal string replacement passes (not general percent-decoding), since replicating the reference's exact behavior (including double-decoding via the `%25`→`%` step and the fixed 3-pass loop) is what the verifier's probe requests are designed to catch. Wire this check into an httplib GET handler that returns 403/"Blocked" on detection and 200/"OK" otherwise, build to `/app/server/waf_server`, and self-test with curl against paths like `/..%2f` and `/%25252e%25252e%2f`-style double-encodings before writing the Go concurrent tester and starting the server in the background.
