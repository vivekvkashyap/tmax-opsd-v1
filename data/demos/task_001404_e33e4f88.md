Break this into independent pieces and verify each before wiring them together. (1) For latency filtering, use awk to parse the `latency=Xms` field numerically (strip "ms", compare > 150, not >=) and print the IP column, then dedupe with `sort -u` or an awk associative array — don't just grep for a threshold string since latency values vary. (2) The C server: use a plain blocking TCP socket (socket/bind/listen/accept) bound to 127.0.0.1:<port from argv>, and on each connection call `tzset()` after setting/respecting the process's `TZ` env var, get `localtime()`/`strftime(..., "%Y-%m-%d %H:%M:%S %Z", ...)`, wrap it in a minimal valid HTTP/1.1 200 response, write it, then close the socket — loop accept() forever. (3) haproxy.cfg needs a `frontend` bound to 127.0.0.1:9000 in `mode http` with `default_backend`, and a `backend` in `mode http` listing both servers with `balance roundrobin`. (4) Launch two server instances with different `TZ=` env vars and haproxy, sleep to let them bind, then loop `curl` 4 times with 1s gaps appending to the log — round-robin means alternating timezones across the 4 lines, so verify that pattern manually before finishing.