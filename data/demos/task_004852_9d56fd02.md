This task has two independent parts. First, transcribe `/app/intercept.wav` to recover the attacker's encoding recipe — use `whisper`/`whisper-cli` if available, or another local speech-to-text tool, or fall back to careful manual listening with `ffplay`/`aplay`; the spoken phrase describes an ordered sequence of transformations (a base encoding, a byte-order operation, and a bitwise operation with a specific key) applied to build the payloads. Do not guess the transform order — derive it from what the audio actually says, then implement decode logic that reverses those steps in the correct order (decoding is often the inverse order of encoding). Second, build `/home/user/payload_detector.py` to take a directory arg, read each file, run your decoder, and inspect the decoded text for CSP-bypass indicators (e.g. unsafe-inline directives, suspicious external exfil URLs/domains, reused/fixed nonces) versus clearly benign CSP policy strings. Write results to `/home/user/report.json` as filename->"evil"/"clean". Test your decoder end-to-end on a hand-crafted sample before trusting classification logic, since a decoding error will look like a classification failure.