First recover the hidden instruction sequence from the audio: convert `/app/issue_report.wav` to a compatible format with `ffmpeg` if needed, then transcribe it with an available speech-to-text tool (e.g. `whisper.cpp`/`whisper`), and transcribe carefully since spoken numbers ("fifteen", "one G", "four") must be converted to the exact token format the parser expects (e.g. `ADD 15`, `HEX 1G`, `MUL 4`, `END`) — write the resulting line(s) verbatim to `/home/user/regression_input.txt` and manually sanity-check the transcript against the described instruction grammar before trusting it. Reproduce the crash by piping this file into the current `fast_parser.cpp` binary, then use `valgrind`/`gdb`/ASan to pinpoint the exact fault — the likely bug class here is a HEX-argument parser that assumes the next two characters are always valid hex digits and reads/writes out of bounds or corrupts state when they aren't, instead of validating and safely skipping malformed HEX values. Fix the validation (not just this one input) so all invalid-hex, boundary, and malformed-command cases degrade gracefully like a correct implementation would, then recompile to `/home/user/fast_parser_fixed`. Since grading is by black-box fuzz-equivalence against a hidden oracle, write your own randomized differential test harness (many random instruction sequences, edge cases, empty/huge inputs) comparing your binary's stdout/exit code against your own best understanding of correct behavior, not just the one dictated regression case.
