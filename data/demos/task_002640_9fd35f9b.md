Fix the three layers independently and re-test after each. (1) Linking: the `Makefile`'s `LDFLAGS=-lcustommath` doesn't tell the linker where to find `libcustommath.so` — add `-L./lib` so it links, and add `-Wl,-rpath=./lib` (or an absolute rpath) so the runtime loader finds it too, since `LD_LIBRARY_PATH` won't be set when the verifier runs the binary. (2) Crash: run `gdb ./route_optimizer` with `run 5 10`, get the backtrace on the segfault, and look at how `depth` changes across recursive calls in `cost_calc.c` — a base case that's never reached because depth grows instead of shrinks is the classic cause; the recursive call should move `depth` toward 0, not away from it. (3) Formula bugs: re-derive the two cases directly from the spec in the task description (base case must use the `custom_multiply` library function rather than ad hoc arithmetic; the recursive step's next-node and cost formulas must match exactly what's stated) rather than trusting the existing code's structure — check each operator and multiplier against the written spec line by line. After `make` succeeds, run `./route_optimizer 5 10` and redirect stdout to `/home/user/incident_1029/solution.txt`, formatted to 4 decimal places by the C code's own printf, not manually.
