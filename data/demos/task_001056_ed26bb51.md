Work through this as a pipeline of independently verifiable stages. First, use `sox /app/sensor_audio.wav sensor.csv` (sox can dump raw samples) or `ffmpeg` to decode the WAV into a text/CSV stream of per-sample amplitude values with a known sample rate (8000 Hz here, so sample index / 8000 = timestamp in seconds) — check the tool's actual output format before writing the parser, since sox's `.dat`/CSV output includes a time column you can use directly instead of recomputing it. Second, write `detector.cpp` to stream through samples and flag a changepoint only when the amplitude magnitude exceeds the threshold (0.8 normalized, or 26214 in raw int16) for at least 5 consecutive samples — track a running consecutive-count that resets whenever a sample drops below threshold, and record the timestamp of the *start* of each qualifying run only once (avoid re-triggering mid-run or double counting overlapping windows). Emit `/home/user/anomalies.csv` with header `Anomaly_Time_sec`. Third, join against `/app/machine_logs.csv` in shell: for each anomaly timestamp find the closest log row within a 1.0s window (e.g. an `awk`/`python` script that computes `abs(diff)` against all log timestamps, keeps the minimum if <=1.0, else drops the anomaly) and write `/home/user/final_report.csv` with columns `Anomaly_Time_sec,Machine_State,Batch_ID`. Test the detector on a small synthetic WAV with a single known transient before running it on the full file, since the consecutive-sample threshold logic is the easiest part to get subtly wrong.