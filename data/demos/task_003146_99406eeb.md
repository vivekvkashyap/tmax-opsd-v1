This is a fuzz-equivalence task: your C++ binary at `/home/user/vparser_cpp/vparser` must match a hidden oracle byte-for-byte on stdout/stderr across many random inputs, so precision in formatting (spacing, ordering, punctuation) matters more than "reasonable" output. First recover the proprietary pre-release weighting rule from `/app/legacy_rules.png` using OCR (e.g. `tesseract /app/legacy_rules.png -`), reading the extracted text carefully rather than guessing standard SemVer precedence — legacy systems often deviate (e.g. a specific tag ordering, a default weight for missing pre-release tags). Design your version parser as an explicit state machine (character-by-character over MAJOR/MINOR/PATCH/PRERELEASE fields) so malformed input is handled predictably rather than crashing, since the fuzz corpus will include malformed versions. Sort using the recovered weight combined with numeric major/minor/patch comparison, then print the sorted line, followed by a second line of adjacent-pair diffs — infer the exact diff line format (separators, spacing, arrow style) by testing small, controlled inputs against your own implementation logic and reasoning about what "diff between each adjacent pair" naturally means, then stress-test your binary against a wide range of hand-constructed edge cases (empty pre-release, boundary weights, malformed tags) before considering it correct.