Build this as a strictly sequential streaming loop in Rust, not batch/parallel — the no-leakage requirement means the transform for record i must only see records 0..i. Open the source pipe for reading and sink pipe for writing (opening a FIFO can block until the other end connects, so set both ends up per `/app/start_services.sh`'s expectations). Parse each line with `serde_json`, keeping a `HashMap<String, VecDeque<f64>>` per `sensor_id` tracking the last final value plus a bounded rolling window (max 20). For each record: if `value` is missing/null, substitute the sensor's last final value (0.0 if none yet). Compute the window's mean/stddev over its *current* contents (before inserting this record) — only clip if z-score exceeds ±3, using stddev=0 when the window has fewer than 2 elements. After deciding the final value, push it onto the window (evicting the oldest past size 20), then write the record back with the same keys. Test with a short manual pipe before running the real stream.
