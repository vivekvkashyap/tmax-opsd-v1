Do the reconnaissance first, then implement. Transcribe `/app/voicemail.wav` (e.g. via `whisper` or another local ASR tool) to learn which PII categories to redact and the admin's password-generation pattern — use exactly what the audio says, don't assume a pattern. For cracking, write a small brute-force script that only searches the space described in the transcript (a base word plus a bounded numeric suffix pattern), hashing each candidate with `hashlib.sha256` and comparing to the hex string in `/app/leaked_hash.txt` until you find a match — an unconstrained brute force would be intractable, so the transcript's hint is what makes this feasible. For redaction, use regexes that match the exact PII formats mentioned (SSN as `\d{3}-\d{2}-\d{4}`, credit card as four groups of 4 digits separated by spaces) and replace the whole matched span with `[REDACTED]`, applied to `/app/raw_logs.json`'s message fields while preserving JSON structure and untouched fields. For the API, implement `/api/logs` and `/api/admin_password` in a minimal Python HTTP server bound to `127.0.0.1:8080`; check the `Authorization` header against the exact required bearer token and return 401 otherwise, and attach the specified `Content-Security-Policy` header to every successful response — test both endpoints with and without the header using curl before finishing.
