Treat this as writing a small interpreter, not a text scraper: read each `.bld` file line by line, maintaining a variable dictionary and a "currently active" flag (initially true) plus a stack if you want to support nesting, though the spec here is flat. On `TARGET <arch>`, record the target (only valid as first line, default `null` if absent). On `DEFINE <VAR> <VALUE>`, store the variable. On `IF <VAR> == <VALUE>`, evaluate against the current variable dict and only EMIT commands inside a true block should be collected — push/pop an "in conditional, condition true/false" state so `ENDIF` correctly closes it. On `EMIT <filename>`, append to the emitted-files list only if not inside a false conditional. Be careful with whitespace/indentation inside IF blocks (lines may be indented) — strip and tokenize each line by whitespace rather than relying on exact spacing. After emulating all `.bld` files in `/home/user/legacy_builds/`, write the manifest keyed by filename to `/home/user/build_manifest.json`. Then write `test_organizer.py` with `unittest`, feeding your interpreter a mock multi-line script string containing at least one IF block and asserting the emitted files and target match your interpreter's own logic.
