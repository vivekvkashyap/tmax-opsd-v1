Start by inspecting the schema blind: `sqlite3 /home/user/company.db ".schema"` to see the `employees` and `messages` table structures and column names before writing any query. Find the root employee with `SELECT * FROM employees WHERE manager_id IS NULL`, then write a recursive CTE (`WITH RECURSIVE hierarchy AS (SELECT ... WHERE manager_id IS NULL UNION ALL SELECT e.* FROM employees e JOIN hierarchy h ON e.manager_id = h.emp_id) SELECT * FROM hierarchy`) to confirm you can walk the full org tree from the root — this validates your understanding of the schema even though centrality only needs the messages table. Load all `(sender_id, receiver_id)` pairs from `messages` and build a `networkx.DiGraph()` with `add_edges_from`. Call `nx.betweenness_centrality(G)` with default parameters (don't pass custom weight/normalized args), sort the resulting dict by score descending, map the top 3 node ids back to employee names via the `employees` table, and write one name per line to `/home/user/bottlenecks.txt` in that order.
