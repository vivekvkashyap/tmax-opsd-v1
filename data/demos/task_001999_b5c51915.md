Build this as a straightforward numerical-simulation program, not a graph library problem. Parse `/home/user/graph.txt` edges into an adjacency structure for 4 nodes, form the degree matrix D and adjacency matrix A as plain 4x4 float slices, and compute L = D - A directly by hand (no external graph package needed for a 4-node graph). Implement Forward Euler explicitly: at each step compute `Lu` via matrix-vector multiply, then `u_next[i] = u[i] - dt * (Lu)[i]` for every node simultaneously (do not update u in place mid-step, or you'll mix old/new values within one iteration — a common bug). Record every state including the initial one before stepping, format floats to exactly 4 decimals when writing CSV rows, and match the header exactly as specified. After simulating, verify convergence to the analytical steady state yourself by checking the final row against the expected equilibrium (equal split of the initial total across nodes) within the given tolerance, and exit non-zero if it fails — this catches sign errors in the Euler update or an incorrectly built Laplacian before you call the task done.
