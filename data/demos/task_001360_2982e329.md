Sequence this as extract, then verify, then edit — don't skip straight to sed. Extract with `tar -xf /home/user/project.tar -C /home/user/src/` after `mkdir -p`. Write `verify.c` to `mmap()` the whole of `hashes.bin` (open with `open()`, `fstat` for size, `mmap(..., PROT_READ, MAP_PRIVATE, fd, 0)`), then walk it in fixed 32-byte strides, treating each record's first 31 bytes as a null-terminated filename and the 32nd byte as the expected XOR checksum. For each record, open and read the corresponding file from `/home/user/src/`, fold all its bytes with XOR into a `uint8_t` accumulator starting at 0, and compare to the stored value. When a file is intact, append its name+newline to `/home/user/verified.log` — but wrap the open+write in `flock(fd, LOCK_EX)` (or `fcntl` with `F_SETLKW`) before writing and unlock immediately after, since the task explicitly requires this for concurrent-pipeline safety and the verifier checks the source for it. Only after running the compiled `verify` binary and inspecting `verified.log`'s actual contents should you loop over those specific filenames (not all files in `src/`) with `sed -i 's/DEPRECATED_API_CALL/MODERN_API_CALL/g'`, leaving any unlisted (corrupted) file untouched.