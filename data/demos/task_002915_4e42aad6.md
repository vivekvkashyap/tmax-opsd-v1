Treat this as a SQL-first problem: query `readings` grouped/deduplicated by `(ts, sensor_id)` (e.g. `SELECT DISTINCT ts, sensor_id, value FROM readings` or a `GROUP BY` with `MIN(value)`/`MAX(value)` since duplicates share identical values here) rather than trying to dedupe in application code after a naive full scan. Load the deduplicated rows into Go structs, then compute the rolling average per sensor by sorting each sensor's own readings by `ts` ascending and tracking the previous value as you iterate: first row's rolling_avg equals its own value, every subsequent row's rolling_avg is `(value + prevValue) / 2` — do this per-sensor-group, not globally across all sensors mixed together, or you'll leak one sensor's values into another's window. Format both `value` and `rolling_avg` with exactly one decimal place (`fmt.Sprintf("%.1f", ...)`), write the CSV with header `ts,sensor_id,value,rolling_avg`, and order output rows by `sensor_id` ascending then `ts` ascending. Initialize the Go module and fetch `github.com/mattn/go-sqlite3` before building, then run the program so `/home/user/output.csv` is actually produced.