Start by running OCR on `/app/thresholds.png` (e.g. `tesseract /app/thresholds.png -`) to recover the baseline mean/std values for the 3 dimensions — inspect the raw OCR text carefully and correct obvious character-recognition mistakes (O vs 0, decimal points) before hardcoding the numbers into your program. Write the C program to parse the CSV with no header, accumulate sums per column while counting rows, then divide to get the centroid — handle files with varying row counts and trailing newlines robustly (e.g. with fgets/strtok or sscanf, skipping blank lines). Compute per-dimension Z-scores using the OCR'd baseline mean/std, then the Euclidean norm sqrt(sum(Z_i^2)), comparing against the 2.50 threshold with <= for "clean". In `/home/user/detect.sh`, compile the C program once (e.g. into a fixed binary path) if not already compiled, then invoke it with the CSV argument and translate the numeric classification into the required shell exit codes (0 clean, 1 evil) — remember bash `exit` status is what the grader checks, not stdout text. Test against a handful of synthetic CSVs you construct yourself, both near and far from the baseline, to validate boundary behavior before trusting the adversarial corpus.
