Design this as one SQL query with explicit precedence, then wrap it in Python and a shell driver. In SQL, join access_logs to employees and systems, and compute violation_type with a CASE (or equivalent) that checks the TERMINATED condition first — `employee.termination_date IS NOT NULL AND date(access_timestamp) > date(termination_date)` — and only falls through to CLEARANCE (`employee.clearance_level < system.required_clearance`) when TERMINATED is false; only rows where at least one condition holds should appear in the result (i.e. filter with a WHERE that is the OR of both conditions, but SELECT the CASE that prioritizes TERMINATED so a row failing both is still labeled TERMINATED, per the "if both, classify as TERMINATED" rule). Compare dates as date strings/`date()` calls rather than naive string prefixes since termination_date and access_timestamp have different formats. Order the final result by log_id ascending and serialize to JSON with exactly the four required keys in a plain list (use `json.dump` with an explicit key order via dicts). Then write run_pipeline.sh to invoke the Python script and validate the JSON is parseable and list-typed, chmod it executable, and actually run it to produce violations.json before finishing.
