Treat this as three separable fixes, verifying each before integrating. First, open `/home/user/dataset.sqlite` with the `sqlite3` CLI, run `.schema` to find the papers/citations tables and any citation index, then check for corruption with `PRAGMA integrity_check;` — if it flags the citation index, rebuild it with `REINDEX <index_name>;` or `DROP INDEX ...; CREATE INDEX ... ON citations(<source_column>);`, then re-run integrity_check and manually spot-query citations for a known paper to confirm rows are no longer missing. Second, inspect `/app/kg-exporter-1.0.0`'s Makefile/build config for anything disabling CGO (the sqlite3 Go driver requires cgo) and fix it, then `go build` and confirm it compiles cleanly. Third, read the recursive CTE in the Go source carefully — check which columns the join uses to walk from a paper to what it cites versus what cites it; if the join condition has the roles of source/target reversed, swap them so traversal follows outgoing citation edges (root -> papers it cites), not incoming ones. After fixing all three, build to `/home/user/kg-export` and manually test a few `(root_paper_id, max_depth)` combinations against your own understanding of the schema, checking output is a properly sorted, deduplicated JSON integer array with no extra stdout text.
