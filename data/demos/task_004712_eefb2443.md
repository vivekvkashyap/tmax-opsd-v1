Apply the patch with `cd /home/user/ws_server && git apply /home/user/feature.patch` (or `patch -p0`), then run `cargo build --release` and read the compiler error carefully — it will point to a value moved into an `async move` closure inside a loop (a `String` captured by the spawned task on each iteration, which can only be moved once). Fix it by cloning the value before each spawn (e.g. `let name = server_name.clone();` inside the loop, used inside the `async move` block) rather than restructuring the whole handler. After it compiles, start the server in the background on port 8080, run `python3 /home/user/test_client.py` (do not edit it) and confirm `/home/user/test_results.json` is produced. Then restart the server wrapped in `/usr/bin/time -v ... &> /home/user/memory_profile.txt &`, run `/home/user/load_test.py` which sends a shutdown message to end the process cleanly, and `wait` for the backgrounded process so the time output is fully flushed before reading it. Finally grep the "Maximum resident set size (kbytes):" line from the profile and write just that integer (no extra text) to `/home/user/max_memory.txt`.