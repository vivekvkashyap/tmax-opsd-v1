Set up the Rust project with `cargo new`, adding `ndarray`, `ndarray-linalg` (pick a LAPACK backend feature like `openblas-static` or `netlib` that actually links on the target Linux environment — this is the step most likely to fail to compile, so verify the feature flag against what's installed before writing the math), and `plotters`. Build the 5x3 matrix `X` and 5x1 vector `y` exactly as given, then implement ridge regression by literally computing `X^T X`, adding `lambda * I` (lambda=0.05) to regularize the otherwise near-singular matrix, computing `X^T y`, and solving the resulting 3x3 linear system (via `ndarray-linalg`'s solve or an explicit inverse) to get theta. Feed theta into the ODE `du/dt = θ0*u + θ1*sin(t) + θ2*cos(t)` and integrate with plain forward Euler at dt=0.001 from t=0 to t=10, storing the trajectory as you go (don't recompute it separately for the plot vs. the final value — reuse the same simulation loop's data). Write the final u formatted to 4 decimal places to final_u.txt, and use `plotters` to render the stored (t, u) series as an 800x600 PNG line chart to trajectory.png.
