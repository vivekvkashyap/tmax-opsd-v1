Split this into three independently testable pieces. First fix the vendored papermill: search `/app/vendored/papermill-2.3.4/papermill/cli.py` (and nearby modules) for an import that references a package name not actually installed — compare against what's declared in `setup.py`/`requirements` to spot the typo, correct it, then `pip install -e /app/vendored/papermill-2.3.4` and confirm `papermill --help` runs cleanly before moving on. Second, build `detect_bad_spectrum.sh` as a pure classifier: read the CSV's first column and check it is strictly increasing row-to-row (reject any tie or decrease), and scan every field for NaN/Inf markers (case-insensitive, since values may appear as `NaN`, `nan`, `Inf`, `inf`, `-inf`) — a naive numeric comparison in bash/awk will silently mis-handle non-numeric tokens, so validate each field is a parseable finite number before comparing. Test this script directly against `/app/corpora/clean/` and `/app/corpora/evil/` and confirm 100% correct classification before building the orchestrator. Third, `orchestrate.sh` should loop over `*.csv` in the input dir, call the classifier, and only invoke papermill (passing `-p input_file <csv path>`) for files reported VALID, writing output notebooks to `<output_dir>/<basename>_out.ipynb`. Make both scripts executable (`chmod +x`).