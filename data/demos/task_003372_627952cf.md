Fix the vendored C project first: run `make` in the uroute directory, read the linker error carefully (it will complain about an undefined symbol whose implementation exists in a .c file not listed in the Makefile's object list), then edit the `OBJS`/similar variable in the Makefile to include that missing object file, and rebuild until `uroute_cli` links and a known-valid path exits 0. For the Python filter, implement three independent checks in order: (1) split the URL into path and query, compute the XOR of ASCII codes of every character in the path, format as 2-digit hex, and compare case-insensitively against the `chk` parameter; (2) decode the `ops` hex string into opcode bytes and run a small stack VM — opcode `01` pushes the following byte, `02` pops two and pushes their sum mod 256, `03` pops two and pushes their product mod 256, treat stack underflow as malicious, and after execution check the top-of-stack value is not greater than 100; (3) only if both checks pass, shell out to the compiled `uroute_cli` with the path and require exit code 0. Print `ACCEPT` only if all three succeed, else `REJECT`. Test against both corpora directories before finalizing.
