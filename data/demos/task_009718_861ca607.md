Init a module in `/home/user/tracker` and use Go's `encoding/csv` reader with default settings — it already handles quoted fields containing embedded newlines, so DO NOT split on `\n` manually or you will drop the multi-line rows. Preserve overall file order while keeping independent per-`(Server,Key)` state.

Process rows top-to-bottom. For each `(Server,Key)` maintain: the last accepted SHA-256 hash, the last accepted `Value` string, and a list of accepted byte-lengths. For each row:
1. Compute `sha256(Value)` as lowercase hex. If it equals that key's immediately-preceding accepted hash, silently skip (no change).
2. Drift = Levenshtein distance between the previous accepted `Value` and the current `Value`; use `0` for the first accepted row of a key. Compute distance over bytes/runes of the raw value (newlines included).
3. Append the current byte length; RollingAvg = mean of the last up-to-3 accepted lengths, formatted with exactly 2 decimals (`%.2f`).

Emit accepted rows to `/home/user/processed_configs.csv` in original arrival order with header `Timestamp,Server,Key,ValueHash,Drift,RollingAvg`. Watch the two traps: hash dedup compares only against the last accepted (not any earlier) value, and length is byte length of the value string.
