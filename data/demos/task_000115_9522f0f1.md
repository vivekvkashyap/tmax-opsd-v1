Design the C program around three separable subroutines, then wire them together: (1) a line-by-line reader for `chats.txt` that splits on `|`, skips the header, and uses `regexec` with a compiled `[0-9]+` (or literal `[USER:...]`) pattern to pull the numeric UserID out of the message text; (2) a POSIX ERE built with `regcomp(REG_EXTENDED)` for the exact email pattern given in the task, applied repeatedly with `regexec` in a loop (advancing past each match) to replace every occurrence with `[REDACTED]` — don't assume only one email per message; (3) a simple linear or hash lookup into `profiles.csv` (also skipping its header) keyed by UserID, defaulting to `Unknown`/`Unknown` when absent. For the Unicode length requirement, call `setlocale(LC_ALL, "")`/`setlocale(LC_ALL, "en_US.UTF-8")` before using `mbstowcs` (or iterate with `mbrtowc`) to count code points in the *masked* message, not bytes — a naive `strlen` will overcount multi-byte UTF-8 characters like accented letters or Japanese kana. Write results as CSV rows in the exact given header order, compile with `gcc -o processor processor.c` (linking `regex.h`/`wchar.h` as needed, no extra libraries), then run it and diff the output against the CSV format expectations before considering it done.