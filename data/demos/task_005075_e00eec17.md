Design the parser as an explicit `enum`-driven state machine that consumes the manifest one character at a time (e.g. states like START, IN_TYPE, AFTER_TYPE, IN_NAME, AFTER_NAME_V, IN_MAJOR, DOT, IN_MINOR, DOT2, IN_PATCH, END_LINE), transitioning on each byte and rejecting immediately (exit 1) on any character or transition that doesn't fit the grammar — do not fall back to `sscanf`/`strtok`/regex, since the task explicitly forbids high-level string splitting. Enforce each field's constraints inline as you scan: Type must land exactly on APP/LIB/SYS, Name characters must be `[a-z0-9-]` and length-bounded 1-64, and each version component must be a numeric run of 0-999 with exactly two `.` separators — track digit counts to reject things like `1000` or empty components. Gate all `stdout` output behind `#ifdef SERVER_MODE` (only that mode prints the `SUCCESS: Parsed N packages.` line) and make `EDGE_MODE` silent but still exit 0/1 correctly; both modes share the same validation core, so factor the state machine into one function returning success/failure and count, called differently per mode. Write the Makefile targets with the exact flags (`-O3` for server, `-Os` for edge) and separate output binary names, and in benchmark.sh generate the 50,000-line fixture with a loop emitting guaranteed-valid lines, then time both binaries and append DONE.
