Build this as a chain of independently-verifiable stages. First, use `awk`/`grep` on `raw_requests.txt` to select lines with status exactly `APPROVED` (match the field precisely, not a substring), then extract username and email into the comma-separated CSV — check the output file by eye before moving on. Second, write the C++ program to read that CSV line by line, split on the comma, and build a JSON array by hand or with simple string concatenation (escaping isn't critical here since fields are simple), writing it to the exact output path; compile with `g++` and run it, then confirm `users.json` is valid JSON. Third, start the two background services in order — the HTTP server on 8080 serving the target directory, then `socat` forwarding 9090→8080 — and give each a moment to bind before testing (e.g. curl 8080 directly first to isolate failures). Fourth, curl through the forwarded port (9090) rather than the origin port, since that's what the task requires, and save the response. Validate the whole chain by checking each stage's output before proceeding to the next, rather than assuming success.