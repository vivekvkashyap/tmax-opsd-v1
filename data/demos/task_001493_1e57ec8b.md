Work the three phases in order and verify each before moving on. Phase 1: grep the log for the `--pin-hash` argument to get the MD5 hash, then write a short Python loop trying `f"{i:04d}"` for i in 0..9999, comparing `hashlib.md5(candidate.encode()).hexdigest()` to the target — do not guess the salt, the PIN is hashed directly. Phase 2: use `openssl rsa -in encrypted_app.key -passin pass:<pin> -out decrypted_app.key` to strip the passphrase, then `openssl req -x509 -new -key decrypted_app.key -out app.crt -days 365 -subj "/CN=secure.local"` to build the cert — get the day count and CN exactly right since verification checks them. Phase 3: iterate `.sh` files under the scripts directory with `enumerate(open(f), start=1)`, and for each line use a regex that matches both `--password=VALUE` and `-p VALUE` forms (non-whitespace token), collecting file, 1-based line number, and captured value. Sort the leak list by file then line_number before dumping JSON in the exact schema requested, then actually execute the scanner so the report file is produced.