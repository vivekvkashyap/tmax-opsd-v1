Reverse-engineer the regularization by controlled experimentation, not by assuming a formula. Generate your own synthetic HDF5 systems where you control the singular values exactly: build `A = U @ diag(S) @ V.T` from random orthogonal `U, V` (via QR of random matrices) and an `S` you choose to include values spanning several orders of magnitude, some very close to zero; pick a known `b` and feed this through `/app/bin/legacy_solver`. Compare its output `x` against a plain least-squares/pseudoinverse solve — where they diverge tells you the solver is truncating or damping small singular values. Binary-search on the threshold: for a given near-zero singular value, vary it across trials and observe at what point the solver's output stops matching a straightforward SVD-pseudoinverse-with-that-singular-value-zeroed versus one that keeps it — this pins down the exact cutoff and whether it's a hard truncation (TSVD, zero the term) or a smooth Tikhonov-style damping. Once confirmed, implement `/home/user/custom_solver.py` using `np.linalg.svd`, apply the discovered rule per singular value when building the pseudoinverse, and validate by re-running both the binary and your script on several fresh random systems, checking the max absolute difference is essentially zero before considering it done.
