Work through this forensics task as a pipeline of extraction steps that each feed the next. First, grep `/home/user/evidence/network_log.txt` for lines matching the attacker IP `192.168.1.100` with `ACTION` field `AUTH_SUCCESS`, and pull the `DATA` field from that line — this is your XOR key (a plaintext token, not something you compute). Second, grep/filter all lines from that same attacker IP with `ACTION` `EXFIL`, preserving their original file order (don't re-sort), and concatenate their `DATA` fields (hex strings) end-to-end into one long hex string. Third, write `/home/user/decoder.cpp` to: decode that hex string into raw bytes (process two hex chars at a time with `strtol`/`std::stoi(..., 16)` or a lookup table), then XOR each byte with the corresponding byte of the key cycling with modulo (`key[i % key.length()]`), and print the resulting bytes as a string. Compile with `g++ -o /home/user/decoder /home/user/decoder.cpp`, run it, and redirect/write its output exactly to `/home/user/evidence/decrypted_secret.txt` with no extra whitespace or trailing newline issues. Double-check by manually verifying the first few decoded characters look like readable ASCII before trusting the full output.