Build this bottom-up and test each piece before wiring the hook. First append the two env vars to `/home/user/.bashrc`. Write `edge_service.py` reading `EDGE_PORT` from `os.environ`, using `http.server`/`BaseHTTPRequestHandler` to answer 200 "pong" on `/ping`; make it daemonize (e.g. `os.fork()` + parent exits, or `subprocess.Popen` with `start_new_session=True` from a wrapper) and write `os.getpid()` of the actual listening process to `/home/user/edge_service.pid` — a common failure is writing the wrong PID (the launcher's, not the server's) or the process dying when the hook's shell exits, so detach it fully (redirect stdio, setsid) so it survives the git hook's process tree ending. Write `health_monitor.py` to retry the GET to `127.0.0.1:$EDGE_PORT/ping` up to 5 times with 1s sleeps before declaring failure, appending the exact success/failure line using `EDGE_DEVICE_ID`. Init the bare repo with `git init --bare`, and in `iot-source` commit `edge_service.py` on `master`. The `post-receive` hook must `source ~/.bashrc` (env vars aren't inherited from a login shell into hooks otherwise), checkout via `--work-tree`/`--git-dir`, then exec both scripts, and be `chmod +x`. Finally add the remote and `git push local master`, then confirm the log file and PID/port are live.