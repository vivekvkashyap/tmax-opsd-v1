OCR `/app/lab_notes.png` (e.g. `tesseract /app/lab_notes.png stdout`) and parse out three things from the text yourself: the null-model sequence length and alphabet, the exact metric definition, and the percentile used for the threshold — do not assume values, read them from the image. Implement the Monte Carlo simulation in pure Bash: generate each random sequence using `$RANDOM` to pick from `ACGT` (e.g. build a 4-element array and index with `$((RANDOM % 4))`) at the length given in the note, compute the metric described (a GC-count comparison across the three modulo-3 position classes) with `awk`, and parallelize the N simulation runs using background jobs (`&` + `wait`) or `xargs -P` so it completes in reasonable time. Collect all N metric values, sort them numerically, and pick the value at the percentile rank specified in the note (careful with 0-indexing/interpolation choices — read enough surrounding evil/clean samples in `/app/corpora/` to sanity-check your threshold lands between typical clean and evil metric values). Build `detector.sh` to recompute the same metric on its input file's sequence and exit 1 if it's `>=` your threshold, else exit 0 — reuse the same Awk/Bash metric logic as the simulator so they can't drift apart. Save the threshold integer to `threshold.txt` and an ASCII histogram of the simulated distribution to `histogram.txt`. Test against `/app/corpora/clean/` and `/app/corpora/evil/` before finishing.
