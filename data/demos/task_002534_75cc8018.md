Read the skeleton `/home/user/extractor.c` and `split.conf` carefully before writing any code — the parsing and file-opening logic is already provided; you only need to fill in the TODO. For each parsed `(filename, offset, length)` entry, `fseek` the blob to `offset`, `fread` exactly `length` bytes into a buffer, XOR every byte with `0x42` in place, then open (or create) `/home/user/output/<filename>` for writing, call `flock(fileno(fp), LOCK_EX)` on it before `fwrite`-ing the decoded bytes, and `flock(..., LOCK_UN)` (or just close the file) afterward — `mkdir -p /home/user/output` first since the program won't create it. Compile with `gcc /home/user/extractor.c -o /home/user/extractor` and run it, then sanity-check by eyeballing the output files as readable Markdown text (XOR is symmetric, so re-XORing the wrong key or wrong offsets will produce garbage — if the output isn't legible text, your offset/length parsing or XOR key is off, not the lock logic). Verify with `grep "flock(.*LOCK_EX)" /home/user/extractor.c` yourself to confirm the required call is present before finishing.