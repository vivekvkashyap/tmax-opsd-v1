Do the two objectives independently, then combine at the end. For the audio: run the local `whisper` tool against `/app/incident_report.wav` (e.g. `whisper /app/incident_report.wav --model base --output_format txt` or however the installed CLI expects it), read the resulting transcript, and extract the single word following "The archive key is" — lowercase it yourself in a shell/python step rather than assuming casing; don't hardcode a guessed word. For the Go archiver: write a CLI that takes a directory arg, lists `*.conf` files, and for each applies checks in order — read content and reject if it contains `../`, `/bin/sh`, or `eval(`; separately reject if the filename (not content) contains spaces or non-printable/hidden characters (check byte-by-byte, not just `strings.Contains(" ")`, to also catch things like tabs or unicode control chars). For accepted files only, do a literal string replace of `DEBUG_LEVEL=0` -> `DEBUG_LEVEL=1` and keep the file content for archiving. Print `ACCEPTED: <name>`/`REJECTED: <name>` per the spec, run the built binary against both corpora to confirm correct classification, then use `tar`/`awk` to package only the accepted (already-patched) clean files into `/home/user/final_backup_<key>.tar.gz`, substituting the transcribed key.
