Fix the vendored library correctly first, since everything downstream depends on it: `filepath.Join(dest, filename)` alone does not stop `..` traversal because Join cleans the path but a malicious filename can still resolve outside `dest`. The correct pattern is to `filepath.Clean` the dest, join it with the filename, then verify the resulting path has `dest + string(os.PathSeparator)` as a prefix (or equals dest); if not, return an error whose message contains the exact substring `"zip slip detected"` — a naive fix that only calls Clean without the prefix check will still leak. For the extractor, read all of stdin into a byte slice, recreate `/tmp/config_dest` fresh each run (remove-then-mkdir), call `securezip.Extract` and on any error print the single-line `{"error": "..."}` JSON and exit 0 (never non-zero, and never partial output). On success, walk the tree, `json.Unmarshal` each `.json` file to confirm validity (skip invalid ones silently), sort remaining files by base filename, and marshal the exact success schema. Since output must be bit-exact against a fuzzed oracle, be disciplined about JSON key order, whitespace, and always cleaning up `/tmp/config_dest` at the end regardless of outcome.