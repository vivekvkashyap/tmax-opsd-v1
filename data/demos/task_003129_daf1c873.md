Use `git bisect` properly rather than guessing: start it with `git bisect start`, mark current `HEAD` (or `main`) as `bad` and the `v1.0.0` tag as `good`, and at each step build the checked-out commit and run it against `/app/test_signal.wav` with a timeout (e.g. `timeout 5s ./audiostat /app/test_signal.wav`) so a hang is detected as a nonzero/timeout exit rather than blocking bisect forever — script this as a `git bisect run <script>` that builds, runs with a timeout, and exits nonzero on hang/timeout so the whole bisection is automated instead of manual. Once you land on the offending commit, inspect the anomaly-detection loop for a window-pointer/index that fails to advance on some branch (classically: a `continue` inside a manual `for`-style loop that skips the increment when a special condition like a zero/silent sample is hit) — fix it by ensuring the index always advances every iteration regardless of which branch executes, without changing the surrounding detection logic or CLI output format. Build the fixed code to `/home/user/audiostat_fixed`. Since this will be checked by fuzzing many random WAV files for byte-identical stdout, don't special-case the fix around the one provided test file — fix the general loop-advancement bug.
