Do the vendored-package fix first since it's self-contained: open `/app/vendor/bash-graph-tools-1.0/Makefile`, find the hardcoded `PREFIX` pointing at a path you can't write to, change it to a location under the project or your home directory (e.g. `$(PWD)/build` or the vendored dir's own `bin/`), then run `make install` and confirm `/app/vendor/bash-graph-tools-1.0/bin/gextract --version` runs and prints its version string. For the validator, design `validate_backup.sh` around two independently testable checks using `jq` for the JSON and `awk`/a simple loop for the CSV: (1) dangling-edge detection — extract the set of valid `node_id`s from the CSV into a lookup (e.g. an associative array or a sorted temp file), then for every `src`/`dst` pulled via `jq -r '.edges[] | .src, .dst'` confirm membership, failing on any miss; (2) supernode detection — use `jq -r '.edges[].src'` piped to `sort | uniq -c` and check no count exceeds 50. Exit 1 if either check trips, else 0. Build a couple of your own tiny fixtures (one dangling, one with 51+ edges from one src, one clean) to exercise the script manually before trusting it against the real corpora.
