Build this as three separate, independently testable artifacts rather than one monolith. First, aggregate_flows.sh: use awk with a composite key of DST_IP+CURRENT_INTERFACE to sum BYTES_TRANSFERRED per group (e.g. `awk '{key=$3" "$5; sum[key]+=$4} END{for (k in sum) print k, sum[k]}'`), writing DST_IP CURRENT_INTERFACE TOTAL_BYTES to aggregated_flows.txt. Second, cost_optimizer.c: parse the three input files with fgets/strtok (watch for trailing newlines and variable field counts in allowed_routes.txt), convert bytes to GB by dividing by 1073741824.0 as a double, compute current cost = GB * current_price, then scan the allowed interfaces list per DST_IP to find the minimum price and its cost, accumulate (current - optimized) into a running total, and print it with printf("Total Savings: $%.2f\n", total) so it rounds correctly. Emit update_routes.sh lines as `ip route replace <DST_IP> dev <OPTIMIZED_INTERFACE>` for every destination, with a #!/bin/bash shebang, then chmod +x it. Third, run_optimization.sh: compile with gcc, then execute the two scripts in order. Run it once at the end so all output files exist.