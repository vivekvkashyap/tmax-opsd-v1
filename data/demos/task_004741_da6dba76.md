Build `/home/user/etl_pipeline.py` as a clear sequence of transformations, verifying each stage's intermediate state before moving to the next. First dedupe by reading the JSONL file's raw lines and dropping exact string duplicates (use a set on the raw line, not on parsed fields, so whitespace-identical duplicate lines collapse) before parsing JSON. Then group parsed records by `vehicle_id`, sort each group by `ts`, and fill `null` `speed` values via linear interpolation using the surrounding non-null readings' timestamps and values (pandas' `interpolate(method='linear')` on a time-indexed series works well, or compute it by hand with the time deltas). Truncate each timestamp to the top of its hour (e.g. `dt.replace(minute=0, second=0)`) to form the bucket key, then group by `(vehicle_id, bucket)` and aggregate mean speed / max temp. Write results into SQLite with `sqlite3`/pandas `to_sql`, first dropping/recreating or `DELETE FROM hourly_stats` so reruns don't duplicate rows. Finally write the crontab line for "every hour at minute 0" (`0 * * * * python3 /home/user/etl_pipeline.py`) to `/home/user/cron_schedule.txt`, matching standard 5-field cron syntax.
