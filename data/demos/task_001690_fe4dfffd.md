Build this as two independent, testable pieces. In `train.py`: load the CSV, compute the `memory_mb` median from the training data (impute missing values with it) and clip `disk_io > 1000.0` to `1000.0` before fitting; train `Ridge` for `latency` and `LogisticRegression` for `is_anomaly` on the same cleaned feature matrix; persist a dict `{"latency_model":..., "anomaly_model":..., "memory_median":...}` via `joblib.dump` to `/home/user/artifacts/pipeline.joblib` (create the directory first); then connect with `redis.Redis(host='127.0.0.1', port=6379)` and `SET model:latest <json>` matching the exact key names/structure specified. For the serving app, implement schema validation explicitly before touching the models — reject out-of-range `cpu_usage`, non-positive `memory_mb`, and missing required `disk_io` with 400/422 — then apply the identical median-fill and disk_io clipping logic used in training (load `pipeline.joblib` once at startup, don't retrain) so predictions are consistent, and return `{"latency_pred": float, "anomaly_pred": int}`. Run the service in the background and smoke-test each schema-violation case with `curl` before finishing.
