For Phase 1, use ffmpeg (or OpenCV/PyAV) to decode every frame of the video, convert each to grayscale, and average pixel intensity — write `frame_index,timestamp_ms,avg_brightness` with `timestamp_ms = frame_index*40` and brightness formatted to exactly 2 decimals; confirm you actually get the full frame count the fixture implies (60s at 25fps), don't silently drop frames. For Phase 2, write the C join as a straightforward hash map (or sorted-array binary search) keyed by integer `timestamp_ms`: build it from the first file, insert only first-seen timestamp per key, then stream the second file and on each first-seen match emit the joined row immediately, preserving the first file's row order for output. Handle edge cases robustly — empty files, no matches, duplicate timestamps, malformed/short lines — since your binary is fuzz-tested for byte-exact stdout equivalence against a hidden reference; write small test CSVs yourself covering these edge cases and eyeball your output format (field order, no trailing whitespace/newline quirks) before trusting it. For Phase 3, fit OLS with `numpy.polyfit` or `statsmodels`/`sklearn`, extract the brightness coefficient, and round to 4 decimals.
