Build the Rust HTTP server first with a minimal `/run_mcmc` handler that echoes back a fixed JSON so you can confirm axum/actix binds to `127.0.0.1:9090` and accepts POST before adding any MCMC logic. To call the external `score_oracle` binary from Rust, use `std::process::Command::new("/app/score_oracle").arg(mu.to_string()).output()`, parse stdout as f64, and treat failures/non-numeric output defensively. Implement Metropolis-Hastings carefully: propose `mu_new ~ Normal(mu, 0.1)` (use the `rand_distr` crate), compute `log_prior(mu_new) - log_prior(mu) + log_lik(mu_new) - log_lik(mu)` where log_prior is 0 inside [0,10] and -infinity outside, accept if `log(uniform(0,1)) < log_ratio` (careful with -infinity arithmetic — comparisons with -inf work fine in IEEE754 but avoid subtracting inf-inf). Discard the first 20% of samples as burn-in, then average the remainder for the posterior mean. Since each iteration spawns a subprocess, this can be slow for large iteration counts — make sure the HTTP handler is async-friendly (e.g., spawn_blocking) so the server doesn't stall other requests. Test end-to-end with curl against the running server before considering it done.