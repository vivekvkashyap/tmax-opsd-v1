Start by running `/app/start_services.sh` and connecting manually (`psql`, `redis-cli`) to confirm connectivity and inspect the actual schema/data shape before writing Go code. Implement `/home/user/audit.go` so it first checks Redis for key `override:<source>:<target>` — if it exists and equals `ALLOW` or `DENY`, short-circuit and print the corresponding JSON immediately without touching Postgres. Otherwise, query `access_edges` and run a breadth-first search from `source_node` to `target_node` over directed edges to get the shortest path length; BFS naturally gives shortest paths, but when multiple shortest paths tie in length you must deterministically pick the lexicographically-smallest sequence of node IDs — do this by, at each BFS layer, expanding neighbors in sorted order and/or comparing full candidate paths of equal length lexicographically rather than just returning the first one found by insertion order. Serialize output as compact single-line JSON matching the exact key names and structure given (`status`, `path`, `reason`) with no pretty-printing and nothing else on stdout — send any debug logging to stderr. Compile to `/home/user/audit` and test with a handful of manually-inserted graph edges (including a deliberate tie) before trusting it against the fuzz harness.