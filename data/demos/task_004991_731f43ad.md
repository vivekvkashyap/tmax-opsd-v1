Work through this in the stated order rather than jumping to the corpus run. First fix `/home/user/workspace/Makefile`'s link line by appending the missing `-lcurl -lhiredis` (installing `libcurl4-openssl-dev` and `libhiredis-dev` via apt if needed) and confirm `make` succeeds. Second, run the program under Valgrind or build with `-fsanitize=address` and feed it a sample file to reproduce the crash in `src/decoder.cpp`'s base64 decoder — look specifically for an off-by-one in the output buffer allocation (decoded length calculation or missing null terminator byte) rather than guessing; fix the allocation size, don't just suppress the symptom. Third, in `src/main.cpp`, implement CRC32 over the *decoded* bytes and compare against the allowed-list fetched from `GET http://127.0.0.1:5000/config`, and implement strict UTF-8 validation (reject invalid continuation-byte sequences) on the decoded payload — a file must pass CRC32 AND UTF-8 checks to be written to the output dir. Start services via `start_services.sh` first, then loop the binary over every file in both `corpus/clean/` and `corpus/evil/` into `out_clean/`, and manually spot-check that Redis's `processed_files` counter matches total files processed, incrementing exactly once per file regardless of pass/fail.
