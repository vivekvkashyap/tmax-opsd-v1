First recover the validation thresholds and multiplier by running OCR on the image: `tesseract /app/cleaning_rules.png -` (or convert/preprocess first if OCR output is noisy — try upscaling or thresholding with ImageMagick if raw tesseract output is garbled). Read off THRESHOLD_V_MIN, THRESHOLD_V_MAX, and CLASS_MULTIPLIER from the OCR text rather than assuming values. Then write `cleaner.cpp` to parse a single comma-separated argument into an integer ID, a floating-point VALUE, and a single-character CATEGORY (use `std::stringstream` with `getline(ss, token, ',')`, and validate token counts/parse failures defensively since the fuzzer will throw malformed/edge-case rows at it). Apply the inclusive-range check using the OCR'd min/max — get the boundary comparisons exactly right (`<` vs `<=`) since off-by-one behavior at the exact threshold values is a classic bug fuzz testing will catch. If in range, multiply by the OCR'd multiplier and print with `std::fixed << std::setprecision(2)` in the exact `ID,VALUE,CATEGORY` format with a trailing newline; if out of range, print exactly `REJECTED` and still exit 0. Compile with `g++` to `/home/user/cleaner` and manually test a handful of boundary and random values before relying on the fuzzer to validate correctness.