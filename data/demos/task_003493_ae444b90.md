In C, read `/home/user/raw_data.bin` as raw bytes and reinterpret in groups of 3 `float`s (fread into a `float[3]` buffer or a flat array, record count = filesize/12). For each record, check whether ANY of the three fields exactly equals `-999.0f` using an exact float comparison (the sentinel is written as an exact literal, so `== -999.0f` is safe here — do not use an epsilon/tolerance comparison, which could wrongly include/exclude records) and skip the entire record if so, keeping only fully valid rows. Accumulate sums and cross-products incrementally (or store valid rows and do two passes) to compute the three means, then the six unique entries of the symmetric 3x3 sample covariance matrix using the standard formula `Cov(A,B) = sum((a_i-mean_a)*(b_i-mean_b)) / (N-1)` where N is the count of valid (non-corrupted) records — mind that N must reflect only valid rows, not the original total. Print the full symmetric 3x3 matrix (all 9 entries, diagonal and mirrored off-diagonal) to `/home/user/expected_covar.txt`, each row as three comma-space-separated values formatted with `%.4f`. Compile exactly with `gcc /home/user/clean_covar.c -o /home/user/clean_covar -lm`, run it, then sanity-check the matrix is symmetric and the diagonal entries are positive before finishing.