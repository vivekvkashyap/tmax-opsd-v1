Work through the layers bottom-up. Build fix: inspect `/app/microservice-gateway-1.2/Makefile` for a linking-related syntax issue (e.g. a target using spaces instead of required tabs, or a missing library flag like `-lpthread`) and correct it; separately grep `gateway.c` for the misspelled env var read (`GETWAY_PORT`) and correct it to `GATEWAY_PORT`, then `make`. Env setup: write `/home/user/setup_env.sh` so it only appends each `export VAR=value` line to `.bashrc` if not already present (e.g. check with `grep -qxF` before `>>`), making reruns safe. Credentials: grep the C source for hardcoded username/password strings rather than guessing them. Expect script: use `spawn nc 127.0.0.1 9000` (or telnet), then `expect "Username:"`, `send` the username, `expect "Password:"`, `send` the password, then send `ENABLE HTTP`, handling newlines correctly. SSH tunnel: generate a keypair with `ssh-keygen`, append the public key to `~/.ssh/authorized_keys` with correct permissions (700/600), then start `ssh -f -N -L 8080:127.0.0.1:9001 localhost` in the background. Test the full chain with `curl` before finishing.
