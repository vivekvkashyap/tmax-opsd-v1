This is a multi-stage forensics task — work sequentially since later steps depend on earlier ones. First, run OCR (`tesseract /app/admin_token_screenshot.png stdout`) to recover the 16-character alphanumeric token; don't guess it, and clean up OCR noise (whitespace/case) before using it. Use that token to unlock/recover `/app/telemetry_cache.db` (check its WAL file for uncommitted transactions — a plain `sqlite3 .recover` or replaying the WAL may be needed since the DB is in a corrupted state), then query the `config` table for `float_precision_config` and write that exact value to `/home/user/recovered_config.txt`. Next, inspect `/app/core.dump` and `/app/container.log` (e.g. `gdb -c core.dump`, `strings`, or reading the log) to confirm the crash is from NaN/inf floats at high precision, then patch `/home/user/telemetry_parser.py` to catch/clamp such values to the recovered precision limit instead of raising. Finally write `/home/user/log_filter.py` as a standalone script matching the exact CLI signature given; test it yourself against `/app/corpus/clean/` and `/app/corpus/evil/`, confirming every clean file lands in the valid dir and every evil (NaN/inf/malformed/oversized-float) file lands in the invalid dir before finishing.
