Build the C++ program around the SQLite3 C API (`sqlite3_open`, `sqlite3_exec`/`sqlite3_prepare_v2`), not by shelling out to the `sqlite3` CLI. Load `/home/user/network.csv` by streaming it line-by-line with a simple CSV split (no header, split on comma, watch for the header row itself) and inserting each row via a parameterized `INSERT` into an in-memory table `edges(source, target, weight, timestamp)`. For the window-function query, write raw SQL using `DENSE_RANK() OVER (PARTITION BY source ORDER BY weight DESC, timestamp ASC)` in a CTE, then filter `WHERE rnk <= 2` and `ORDER BY source, rnk, target` — let SQLite do the ranking rather than reimplementing it in C++. For the graph query, write a `WITH RECURSIVE` CTE that self-joins `edges` twice (or joins one level then re-joins) to build exact-length-2 paths from `'A'`, computing `MIN(weight)` per path as the bottleneck, then an outer `GROUP BY destination` taking `MAX()` of those bottlenecks, ordered by destination. Iterate result sets with `sqlite3_step`, writing each row directly to the two output CSVs with no header line. Double-check the DENSE_RANK tie-breaking and the exactly-2-hop constraint (not <=2 hops) since these are easy to get subtly wrong.