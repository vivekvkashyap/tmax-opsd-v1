First recover the exact normalization/dedup/sort rules by transcribing `/app/voicemail.wav` (install `openai-whisper` or use `ffmpeg` plus a speech-to-text library, then run it on the file and read the transcript carefully — do not guess the pipeline from the task description alone, since exact step order and details matter for bit-for-bit matching). Implement `/home/user/cleaner` as a stdin-to-stdout filter that applies the transcribed steps in the precise order given (e.g. trim whitespace, collapse internal whitespace, strip disallowed characters, normalize case, dedupe, then sort per the specified order/direction) — get the order right since these operations are not commutative. Since a fuzz-equivalence verifier compares your output byte-for-byte against a hidden oracle across thousands of random inputs including edge cases (empty input, lines that collapse to the same string, unusual whitespace/punctuation), write your own small test harness generating similar random inputs and self-check your program's behavior on edge cases like empty lines, all-whitespace lines, and lines that become identical after normalization, rather than trusting a single manual test. Make the entry point executable (`chmod +x`) with a correct shebang.