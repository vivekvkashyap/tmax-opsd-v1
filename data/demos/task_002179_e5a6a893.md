Do Step 1 first since its output (N) is a hard dependency for Step 2's logic. Extract frames from `/app/fault_log.mp4` with `ffmpeg` (e.g. `ffmpeg -i fault_log.mp4 frame_%04d.png`), then programmatically check each frame for being solid blue — don't eyeball it; use Python/PIL or ImageMagick to check that every pixel (or a robust sample/histogram) equals exactly RGB(0,0,255), and count how many frames pass. That count becomes your window `N` for the C program's dedup logic — treat it as a derived constant you compute, not a value to guess. For Step 2, write the C sanitizer to read stdin line by line: validate the license_plate field against `^[A-Z0-9-]+$` only (reject on any other char, including lowercase/spaces/special chars) and drop non-matching rows; maintain a hash map (simple open-addressing or a small array/linked-list keyed by vehicle_id is fine) storing the last accepted timestamp per vehicle_id, dropping a record if `timestamp - last_ts <= N`; then mask all but the last two characters of the plate with `*` before printing. Be careful with buffer sizes when parsing untrusted CSV fields (use bounded reads, not unbounded `gets`/`sscanf %s`) since the evil corpus includes injection/overflow attempts.
