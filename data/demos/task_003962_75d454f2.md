Phase 1: merge the three logs with `cat web.log db.log api.log | sort -k1,2` (sort keys covering the `YYYY-MM-DD HH:MM:SS` prefix) into merged_timeline.log. Phase 2/3 hinge on one real Bash bug: backgrounding a function with `check_service &` then `kill $pid` only kills the function's own shell process, not the `sleep 10 &` it spawned internally as a separate child — that orphan keeps running after cancellation. To reproduce in mre.sh, replicate that exact pattern, kill the parent, then `pgrep sleep` (or `ps` filtered) and print the count, which will be > 0. To fix it in uptime_fixed.sh, make check_service's children killable as a unit — e.g. run the function in its own process group with `setsid`/`( set -m; ... )` and kill the whole group with `kill -TERM -$pid`, or explicitly capture and kill the inner sleep's PID, or use `trap 'kill $(jobs -p)' TERM` inside the function. regression.sh must run the fixed logic, confirm zero orphaned sleep processes afterward, and exit 0/1 accordingly. Use only bash/coreutils throughout, and chmod +x all three scripts.
