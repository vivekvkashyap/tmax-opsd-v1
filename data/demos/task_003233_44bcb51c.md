Start by running OCR (e.g. `tesseract /app/server_spec.png stdout`) on the spec image to recover the port number and secret token it specifies — read the text carefully rather than guessing, since both values drive the rest of the task. Extract `/app/configs.tar.gz` to a scratch directory and inspect each file's actual encoding (use `file` or try decoding as UTF-16LE/ISO-8859-1/UTF-8 and see which succeeds cleanly) rather than assuming — do not blindly treat everything as UTF-8, since the files were deliberately saved in mixed encodings. For each file: decode with its real encoding, re-encode as UTF-8, replace every literal occurrence of `REPLACE_ME_TOKEN` with the token you OCR'd, lowercase the entire filename and force the extension to `.conf`, and save into `/app/processed/`. Finally write a small Python script using `http.server`'s `SimpleHTTPRequestHandler` (or equivalent) bound to `0.0.0.0` on the OCR'd port, serving `/app/processed/` as the directory root, and leave it running (e.g. via `nohup ... &` or a background process) so GET requests like `/webserver.conf` succeed with the substituted token in the body.