Recover the trigger rule from the image before writing any detection logic — run OCR (`tesseract`) on `/app/leak_memo.png` and carefully read out the exact opcode value and the comparison operator/threshold on the length field it specifies; don't assume a plausible-sounding rule, extract the literal condition from the OCR text. Once you have the two conditions, inspect a couple of sample files from `/app/corpus/clean/` and `/app/corpus/evil/` with `xxd`/`od -A x -t x1z` to confirm the payload's byte layout (opcode at offset 0, the length-like field at the offset the memo implies) matches your reading of the memo, since OCR can misread digits like 0x4A/0x8A or greater-than/greater-or-equal. Write `/app/detector.rs` to read the file as raw bytes, check both conditions together (both must hold to flag malicious), and exit 1 only when both match, exit 0 otherwise — combine with logical AND, not OR, matching the memo's wording. Compile with `rustc -O detector.rs -o /app/detector`, then script a loop over every file in both corpora comparing actual vs expected exit codes and fix any mismatches you find before finishing.
