Implement this as an in-memory undirected adjacency-set graph, not a naive Python-style rewrite: parse edges.csv once into a HashMap<u32, HashSet<u32>> (or Vec<HashSet<u32>> indexed by node id) adding both directions per edge. For each node, compute its "friends" set from the adjacency map, then build the "friends-of-friends" set by unioning the adjacency sets of each friend, then subtract the direct-friends set and remove the node itself — this two-hop union naturally handles the "not a direct friend, not self" exclusion correctly. Collect (node, count) pairs for every node, sort by count descending with ties broken by node id ascending, and take the top 5. Write the result as a JSON array of [node_id, count] pairs (integers, not floats) to result.json using serde_json. Initialize the project with `cargo init` or `cargo new` if not already present, add csv/petgraph only if you actually use them (a plain HashMap/HashSet implementation is sufficient and simpler), then `cargo build --release` and run it, and manually re-derive one or two nodes' second-degree counts by hand from the CSV to sanity-check your algorithm before trusting the full output.
