Work phase by phase and validate each output before moving on. Phase 1: load `/home/user/sensor_data.csv` with pandas, sort by `device_id` then `timestamp`, and per-device compute a rolling sample covariance between `sensor_A` and `sensor_B` over a trailing window of 3 rows (e.g. `groupby('device_id')` then a rolling apply computing `np.cov(a,b)[0,1]` or the equivalent sample-covariance formula on each 3-row window); explicitly set the first two rows per device to 0.0 since no full window exists yet — don't let pandas' rolling default (which would produce NaN) leak through. Phase 2: load `/home/user/model_weights.json`, build feature vector `[sensor_A, sensor_B, rolling_cov]` per row in the exact given order, compute `z = bias + w·features` and `sigmoid(z) = 1/(1+exp(-z))`, round to 4 decimals, and write `/home/user/processed_data.csv` with only `device_id,timestamp,anomaly_prob`. Phase 3: the blank/crashing plot in `/home/user/plot_heatmap.py` is almost certainly caused by no display backend — set `matplotlib.use('Agg')` before importing pyplot (or set `MPLBACKEND=Agg`), then rerun so `/home/user/heatmap.png` is written; verify it's a real PNG by checking its magic bytes.
