Build and test each stage separately before chaining them. (1) Extract one frame per second from `/app/traffic.mp4` with `ffmpeg -i /app/traffic.mp4 -vf fps=1 ...`, then for each frame compute mean R, G, B channel values (via ImageMagick `convert frame.png -format "%[fx:mean.r*255]..."` or a quick Python/PIL/numpy averaging script). (2) Join those per-frame RGB means with `/app/telemetry.csv` on `frame_index` (frame N pairs with telemetry row `frame_index=N`). (3) Parse `/app/model_weights.txt` for the linear coefficients and bias, and compute each row's anomaly score exactly as specified — a row is anomalous (must be dropped) only when the score is strictly greater than 0. (4) Write `/home/user/filter_data.sh` as a pure row-filter: read the 6-column headerless CSV line by line, recompute the score in awk/bash arithmetic using the same weights, and print only rows whose score is `<= 0`, preserving original CSV formatting. Validate this script directly against `/app/corpora/clean/*` (every row must survive) and `/app/corpora/evil/*` (every row must be dropped) before running the full pipeline and redirecting the filtered joined data into `/home/user/final_output.csv`. A common mistake is reading weights column order wrong or applying a `>=` instead of strict `>` threshold.