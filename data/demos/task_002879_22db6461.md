Work the decoding pipeline in the stated order and verify each stage independently before moving to the next: first read artifact.txt, subtract 1 from every byte's value to undo the +1 ASCII shift, then base64-decode the resulting string, and print/inspect the intermediate result to confirm it looks like a valid space-separated RPN expression before writing evaluator code — this catches encoding-order mistakes early. Implement the RPN evaluator with a simple integer stack: push numbers, and on an operator pop twice, being careful that for SUB and DIV the first popped value is the right operand and the second popped is the left operand (i.e., compute second-popped OP first-popped, not the reverse — this operand order is the most common bug). Write only the final integer to output.txt with no extra text or newline issues. Before editing processor.go, keep a copy of the original file (e.g., `cp processor.go processor.go.orig`) so you can generate `diff -u processor.go.orig processor.go > fix.patch` afterward — a valid patch needs correct --- / +++ headers, so verify it applies cleanly with `patch --dry-run`.