Design the query before writing argparse boilerplate. You need a self-join style query: join `access_logs` to `employees` (on emp_id) to get the accessing employee's dept, join to `restricted_assets` (on asset_id) to get the asset's owning dept, join to `departments` twice (or filter employees.dept_id by name via a subquery) to resolve `--dept` by name, then filter `access_time BETWEEN ? AND ?` and `employees.dept_id != restricted_assets.owning_dept_id`. Use `?` placeholders bound via a params tuple passed to `cursor.execute(sql, params)` — never string-format the date/dept values into the SQL, since the task explicitly requires a parameterized query. Because dates are date-only but access_time has a time component, make the end bound inclusive of the whole end day (e.g. compare against `end_date + ' 23:59:59'` or use `date(access_time) BETWEEN ? AND ?`). Create indexes with `CREATE INDEX IF NOT EXISTS ...` on the join/filter columns (emp_id, asset_id, dept_id, access_time) before running the query, then call `cursor.execute('EXPLAIN QUERY PLAN ' + sql, params)` and dump that raw output to plan.txt. Sort results by access_time then log_id before writing the CSV with Python's `csv` module and the exact required header.
