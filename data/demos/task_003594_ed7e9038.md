First run `tesseract /app/kinetics.png stdout` (or output to a text file) to OCR the parameter table, then parse the resulting text with a regex per line (e.g. matching `k_base:\s*([\d.]+)` etc.) to recover the five numeric constants — don't hardcode values, since OCR text can have minor artifacts to clean up. In simulate_decay.cpp, read rna_data.csv skipping the header line, compute per-sequence base counts and the rate k per the given formula, then integrate dC/dt = -k*C - 0.002*C^2 with classic RK4 (four k-slopes per step, dt=0.1, 200 steps from C(0)=100 to t=20). Parallelize the per-sequence loop with `#pragma omp parallel for` and compile with `g++ -O3 -fopenmp`; make sure each thread writes to its own array slot (no shared-state races). After computing all final concentrations, implement bootstrap CI: resample the final-concentration array with replacement 2000 times, compute the mean of each resample, then take the 2.5th/97.5th percentiles of that bootstrap-mean distribution as Lower95/Upper95, and the sample mean as Mean. Write the exact CSV header/row format requested.
