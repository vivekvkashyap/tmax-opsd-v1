First diagnose the corrupted `idx_time` index: open `/home/user/app/data/research.db` with `sqlite3`, run `PRAGMA integrity_check;`, and if it flags the index, the safe fix is to `DROP INDEX idx_time;` then `CREATE INDEX idx_time ON measurements(timestamp);` again — this repairs it without touching any row data. Since a background ingestor writes to the DB every couple seconds while your C++ server also reads it, put the SQLite connection in WAL mode (`PRAGMA journal_mode=WAL;`) and set a busy timeout (`sqlite3_busy_timeout` or `PRAGMA busy_timeout`) in the C++ server to avoid "database is locked" errors under concurrent access. For the endpoint, complete `/home/user/app/server/main.cpp` using the vendored `httplib.h` to register a GET handler for `/api/v1/stats`, read the `exp_id` query param, and run a single SQL query using the window function `AVG(sensor_value) OVER (PARTITION BY exp_id ORDER BY timestamp ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)` aliased as `cumulative_avg`, ordered by timestamp ascending. Serialize each row into the specified JSON shape (schema_version, data array) and return with Content-Type application/json. Compile with `g++ -std=c++17 main.cpp -lsqlite3 -lpthread -o server`, then start the ingestor in the background and run the server bound to 127.0.0.1:8080 so both stay alive concurrently.