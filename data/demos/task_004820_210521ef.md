Work in two clearly separated phases. First, fix the vendored library: open `/app/sql-parser-c/Makefile` and look carefully for a misspelled variable name used to set compiler flags (compare the variable as defined vs. as referenced) — a typo there means flags silently never apply. Then check `src/parser.c` for a commented-out or missing standard-I/O include, which causes implicit-declaration build failures; restore it. Run `make` then `make test` in that directory until both succeed before moving on. Second, write `/home/user/query_sanitizer.c` as a static analyzer (not a real SQL executor): read the whole query file, then use pattern/regex-style scanning to flag (a) a `FROM` clause listing multiple tables separated by commas with no `WHERE` clause joining them, or an explicit `CROSS JOIN` with no bounding limit, and (b) `WITH RECURSIVE` blocks whose final `SELECT` lacks a `LIMIT`. Be careful with case-insensitivity and whitespace/newlines in keyword matching — naive single-line regexes often miss multi-line SQL. Compile linking `libsqlparser.a`, then iterate over both corpora to confirm exact ACCEPT/REJECT and exit-code behavior before writing the completion log.