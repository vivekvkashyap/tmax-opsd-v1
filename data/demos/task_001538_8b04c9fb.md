Write the script as a sequence of clearly separated stages, each verified independently before moving to the next. Wrap the whole extraction body in `flock -n /tmp/docs_process.lock <command>` (or use `exec 200>/tmp/docs_process.lock; flock 200` at the top) so concurrent invocations serialize or exit early. For reassembly, `cat` the sorted `docs_archive.tar.gz.part*` files together (glob-sort works if parts use consistent zero-padded suffixes; verify with `ls` first) into one file, then `tar -xzf` it into `/home/user/docs_extracted/`, creating that directory first. For nested zips, use `find /home/user/docs_extracted -name '*.zip'` in a loop, `unzip -o "$zip" -d "$(dirname "$zip")"` then `rm "$zip"` — do this in a loop since extraction can reveal further nested zips only if the task implies multiple levels (here it's one level, but write it so it re-scans after extraction to be safe). For the manifest, `cd` into the extracted directory so paths come out relative, `find . -name '*.md' | sed 's|^\./||' | sort | xargs sha256sum` and redirect to the manifest — the common mistake is generating checksums with absolute or `./`-prefixed paths instead of the exact relative form expected, or forgetting to sort before hashing. Finally `chmod +x` the script.
