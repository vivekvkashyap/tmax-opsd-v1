Start by extracting ground truth from the artifacts rather than guessing the spec: OCR or visually inspect `/app/spec.png` to read the exact formula and constraints (variable names, division-by-zero condition, type restrictions, extra-key rejection), and use `scapy` (installing/fixing the environment as needed, e.g. via pip in the right venv) or `tshark`/`tcpdump` to dump `/app/traffic.pcap` and inspect the JSON POST bodies for the payload shape the API actually expects. Before rewriting validation logic, run the existing `/app/legacy_validator.py` against a few adversarial inputs (strings with whitespace, missing keys, extra keys, equal values) to see exactly how and why it crashes — this delta-debugging step tells you which edge cases your rewrite must handle defensively. Then write `/home/user/sanitizer.py` so it never raises: wrap all JSON parsing and type coercion in try/except, explicitly type-check each required variable (reject non-numeric types outright, don't coerce), reject unknown/extra keys, reject the equality edge case that causes division by zero, and exit 0 only when every constraint from the image and pcap schema is satisfied, else exit 1. Validate against both `/app/corpus/clean/` and `/app/corpus/evil/` before finishing, iterating on any mismatches.
