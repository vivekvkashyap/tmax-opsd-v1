Start by reverse-engineering the binary rather than guessing the obfuscation: run `strings /app/exfil_tool` for clues, then disassemble the relevant function with `objdump -d` (or open in `gdb`/Ghidra) and trace how it transforms input bytes before the CSP-shaped JSON is printed — look for a small XOR loop (fixed-length repeating key applied byte-by-byte) followed by a base64-encode call, since that combination is the classic "looks encrypted but isn't" pattern for this kind of tool. Confirm your understanding by running the binary on a few known plaintexts and comparing the `violated-directive` output. Once you have the exact key bytes and their cycle length, write `detector.py` to: load the JSON, extract the string after the `exfil-` prefix in `violated-directive` (handle files that lack this field entirely as CLEAN), base64-decode it, XOR each byte with the recovered repeating key, and check whether the decoded bytes contain the substring `PRIVATE KEY`. Print `EVIL`/exit 1 only when that check passes; print `CLEAN`/exit 0 otherwise — including for decoy `exfil-` payloads that decode to harmless text and for malformed/non-base64 data (wrap decode in try/except so it doesn't crash). Test against varied crafted samples before trusting it on the hidden corpus.