Build this as an ordered pipeline and verify each stage before moving to the next, since later stages depend on earlier ones running correctly. First create the two directories, then generate the self-signed cert with a single non-interactive openssl command, e.g. `openssl req -x509 -newkey rsa:2048 -nodes -keyout /home/user/certs/edge.key -out /home/user/certs/edge.crt -days 365 -subj "/CN=localhost"` (the `-nodes` flag is required for an unencrypted key, and `-subj` avoids interactive prompts). For the C program, read two lines with `fgets` into fixed buffers sized for 50 chars, strip trailing `\n` with something like `strcspn(buf, "\n")` before writing them into the JSON string with `fprintf`/`sprintf` — get the exact key names and quoting right. In `deploy.sh`, compile the C file with `gcc`, pipe the two required lines into the compiled binary via a heredoc or `printf "...\n...\n" | ./iot_setup`, launch `python3 -m http.server 8080 --directory /home/user/edge_data &` and capture `$!` immediately as PYTHON_PID, then launch socat's OpenSSL listener (`socat OPENSSL-LISTEN:8443,cert=...,key=...,verify=0,fork TCP:localhost:8080 &`) and capture its `$!` as SOCAT_PID, writing both to `run.log` in the exact `KEY=value` format. Run the script and confirm with `curl -k https://localhost:8443/config.json` that the chain actually works end-to-end.