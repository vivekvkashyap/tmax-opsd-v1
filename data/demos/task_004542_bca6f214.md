Do the three recovery sub-tasks independently before touching the bisect. For the deleted file, mount/inspect `/app/workspace.img` with a forensic tool that can recover deleted-but-unallocated inodes on ext4 (e.g., `debugfs -R 'lsdel' workspace.img` to list deleted inodes, then dump the matching one with `dump.e2fs` or `debugfs -R 'dump <inode> output'`); don't assume the file is simply hidden — it's genuinely unlinked. For the parameters, run OCR (e.g., `tesseract`) against `/app/equation.png` and read off the exact `ALPHA`/`BETA` values printed there rather than inventing plausible-looking numbers. Once you have `test_runner.py`, read it to learn how it calls into `sim.py` with the two parameters, then use it (or a similar harness) as your bisect test across the ~200 commits in `/home/user/sim_repo`, using `git bisect` (scripted, since manual testing across 200 commits is impractical) to find where floating-point summation changed from a stable form to naive accumulation. Fix by replacing naive `+=` accumulation with `math.fsum` (or equivalent compensated summation) in `fixed_algo.py`, reading ALPHA/BETA from stdin numbers exactly as the recovered test harness does.
