Scaffold with `cargo new req_validator`. Implement `compute_checksum` by folding over bytes, skipping any byte equal to 0x00, and XOR-ing the rest into an accumulator starting at 0. For the proptest, add `proptest = "1"` under `[dev-dependencies]` in Cargo.toml, then inside `#[cfg(test)] mod tests { use proptest::prelude::*; proptest! { #[test] fn proptest_checksum_property(payload in any::<Vec<u8>>()) { ... } } }` — construct a new vector that appends the checksum byte to a clone of payload, recompute the checksum, and assert it equals 0; this holds because XOR-ing a value with its own running XOR always cancels to zero (null bytes don't disturb this since they contribute nothing either way). For `bench_mode`, add `[features] bench_mode = []` to Cargo.toml and gate an extra `println!("Bench mode active")` with `#[cfg(feature = "bench_mode")]` in main.rs; read stdin with `read_line`, trim only the trailing newline (not other whitespace), and format the checksum with `format!("Checksum: {:#04x}", val)` or equivalent zero-padded 2-digit hex. Build with `cargo build --release --features bench_mode`, then pipe the literal string via `printf '%s' "HelloRust" | ./target/release/req_validator > /home/user/bench_output.txt` (printf avoids adding a trailing newline to the byte payload).
