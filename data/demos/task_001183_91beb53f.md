Work incrementally in the Go module: first read the existing ArtifactV1 struct in artifact.go so field names match what Migrate() needs to read (ID, Size, Hash, Data). Add ArtifactV2 and Manifest struct definitions with the exact field names/types specified. Implement Migrate using `crypto/sha256` on the Payload bytes, hex-encode with `encoding/hex`, set Bytes to `len(Payload)`, Version to 2, and Identifier from the V1's ID field. Implement GenerateManifest by concatenating the Checksum strings of all artifacts in order (simple string concatenation, not JSON), hashing that concatenation with SHA256, and hex-encoding it into ManifestHash. For the property-based test, use `testing/quick.Check` with a function `func(v1 ArtifactV1) bool` that calls Migrate and asserts each invariant (Bytes==len(Payload), Version==2, Checksum matches recomputed SHA256 of Payload, Identifier==v1.ID) — note that `testing/quick` generates ArtifactV1 automatically via reflection since its fields are basic types. Write TestGenerateManifest as a standard table-driven or fixed-input unit test that independently computes the expected ManifestHash and compares. Run `go build ./...` first to confirm compilation, then run the tests with the exact command given, redirecting to test_results.log.
