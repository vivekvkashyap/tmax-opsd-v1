Start by extracting ground truth from the image, not assumptions — run `tesseract /app/alert_rules.png stdout` (or similar OCR) and read the actual severity thresholds and output format string it reveals; use exactly what OCR recovers, don't guess plausible numbers. Then write `/home/user/alert_parser.py` to read stdin line by line: attempt `json.loads` on each, and if that fails or any of the four required keys is absent, print exactly `INVALID_LOG`; otherwise map `severity` through the thresholds from the image (get boundary conditions — inclusive/exclusive — exactly right, since edge values like 90 and 70 will be fuzzed) and print using the exact format string, substituting the mapped label, source, timestamp, and message. Since this is checked via byte-exact fuzz equivalence against a hidden reference, pay close attention to exact spacing/punctuation and edge cases like empty strings or unicode in `message` — test your script against a few self-constructed inputs (valid, malformed JSON, missing key) before finishing.
