Work in two independent phases. For the passphrase crack: write a small script (Python with `cryptography`/`paramiko`, or a loop calling `openssl rsa -in encrypted_key.pem -passin pass:<word> -noout` and checking the exit code) that iterates every line of dict.txt as a candidate passphrase until decryption succeeds without error — do not try to guess the password by inspection. Once found, write the exact word (no trailing newline surprises, no extra whitespace) to passphrase.txt, decrypt the private key in memory or to a temp file, then use it to `openssl pkeyutl -decrypt` (or the cryptography library's RSA decrypt) on secret.enc, writing the raw plaintext bytes to secret.txt. For the redaction phase, use a regex that matches strictly 16 consecutive digits OR 4 groups of 4 digits joined by hyphens (anchor on digit boundaries, e.g. `\b\d{16}\b|\b\d{4}-\d{4}-\d{4}-\d{4}\b`) — avoid greedy patterns that would also match embedded digit runs inside larger numbers or that would eat surrounding punctuation. Replace only the matched span with the literal text `[REDACTED]`, leaving all other characters, spacing, and line structure untouched, then write the result to clean_logs.txt.