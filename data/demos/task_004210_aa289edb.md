Treat this as refactor-then-prove, in that order. First run `go mod init example.com/logprocessor` inside `/home/user/logprocessor`. Rewrite `ProcessLogs` to open the file with `os.Open` and read it with `bufio.Scanner` (or `json.Decoder` over the stream) line by line instead of `os.ReadFile`/`ioutil.ReadFile` — this is the crux of the memory fix, and the verifier greps for the absence of the old calls and presence of `os.Open`, so don't leave any bulk-read path in the file. Decode each line into a struct with `status`/`message`, upload only on exact `"ERROR"`, and accumulate/return the count. Write `main_test.go` with a small mock `Uploader` struct implementing the interface (record calls in a slice, no network), backed by an in-memory or temp-file NDJSON fixture, and confirm `go test` passes locally before moving on. In `main()`, wire up `runtime/pprof.WriteHeapProfile` bracketing a real call to `ProcessLogs` against `/home/user/logprocessor/data.jsonl` with a dummy uploader, then write the returned integer count to `result.txt`. Compile and run the whole thing manually to confirm `mem.pprof` and `result.txt` are produced before finishing.
