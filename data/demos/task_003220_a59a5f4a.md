Do the simple text edit first: `sed -i 's/v1/v2/g' /home/user/data/file3.txt`. For the Rust backup tool, write `/home/user/archive.rs` that: creates `/home/user/backup_v2`; for each file in `/home/user/data`, reads its bytes and looks for a same-named file in `/home/user/backup_v1`; if that file exists AND its content is byte-identical, create a hard link (`std::fs::hard_link`, not a copy) from the old backup file into `backup_v2` under the same name — this is what preserves the shared inode; otherwise, transform the file by reversing the characters of each line (read line by line, reverse each line's characters, keep newlines) and write the result to `backup_v2` with `.rev` appended to the filename. Compile with `rustc /home/user/archive.rs -o /home/user/archive`, run it, and check with `stat -c %i` that hard-linked files really share inodes with their `backup_v1` counterparts (a `cp` would not). Finally create `/home/user/backup_latest` as a symlink to `/home/user/backup_v2` with `ln -s`. Verify each expected output file's exact content/permissions before considering the task done.