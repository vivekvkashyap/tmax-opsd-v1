Read the CSV with the `csv` module (or pandas), keeping text fields exactly as-is — do not strip or normalize whitespace, since Strategy A depends on literal single-space splitting (`text.split(' ')`), which differs from `str.split()` (the latter collapses consecutive spaces and would silently change token counts). For Strategy B use `re.findall(r'\w+', text)` per row. Build two parallel lists of per-row token counts, then compute Pearson correlation and a *paired* t-test (`scipy.stats.pearsonr` and `scipy.stats.ttest_rel`, not the independent-samples test) across the whole dataset — paired because each row contributes one count from each strategy. Compute the two means directly from the count lists. Round every output number to exactly 4 decimal places with Python's `round()`, and write the five keys as plain JSON floats (watch out for numpy scalar types not being JSON-serializable — cast with `float()`). Finally, actually execute the script so `/home/user/experiment_results.json` is produced, and verify it parses as JSON with the right keys.