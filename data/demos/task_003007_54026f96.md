Don't reimplement Postgres semantics from scratch — reverse-engineer the classification rule empirically. First inspect what the oracle `/app/plan_validator` outputs on the provided `/home/user/corpus/clean/` and `/home/user/corpus/evil/` files, and diff the JSON structure of clean vs evil plans (e.g. `jq` to pretty-print and grep for `"Node Type"`, `"Join Filter"`, `"Index Name"`, `"Relation Name"`) to find the distinguishing patterns — likely something about unconstrained nested-loop joins and full scans on a specific large table. Use `strings`/`objdump -d` on the stripped binary only to corroborate hypotheses (e.g. literal strings like table/index name prefixes), not to fully disassemble it. Write a Go program that recursively walks the "Plan"/"Plans" JSON tree, checking each node against your derived rule, printing exactly `VALID` or `INVALID`. Validate against both provided corpora by scripting a loop that runs `/app/plan_validator` and your `fast_validator` side by side on every file and diffs outputs before declaring done, since a hidden eval set will apply the same rules.