Solve the three sub-bugs independently and verify numerically before trusting the full pipeline. Parse `molecule.pdb`'s two `ATOM` records by fixed-column or whitespace-split extraction of the x/y/z coordinates, compute the Euclidean distance between them, and hardcode that value as the initial `x` in `simulate.cpp`'s `main` (do not leave the placeholder `0.0`). For the adaptive step-size bug, read `adaptive_euler_step` carefully: it currently increases `dt` when error exceeds tolerance and decreases it when error is small — that's backwards; correct control should shrink `dt` (multiply by <1) when `error > tol` and grow it (multiply by >1) when error is comfortably below tol, so swap which branch does which action rather than swapping the comparison operator. After fixing both bugs, compile with `g++`, run the program, and save its stdout to `/home/user/result.txt`. For `validate.sh`, compute (or use the closed-form solution to the separable ODE derived from your actual initial condition) the analytical value at t=10, compare it against the simulation's printed value using `bc`/`awk` for the absolute-difference check, and exit 0 only if the difference is under 0.005 — run the script yourself to confirm it actually exits 0.