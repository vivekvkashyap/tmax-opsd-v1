For Part 1, parse `/home/user/metadata.json`'s `extract_frames` array in order, and for each timestamp run ffmpeg with `-ss <timestamp> -i /app/tutorial.mp4 -frames:v 1 -q:v 2 figure_N.jpg` into `/home/user/docs/images/` (create the directory first) — seek accurately (place `-ss` before or use accurate seek) so the extracted frame actually corresponds to that timestamp, and avoid unusual pixel formats/codecs so the JPEG is standard 24-bit. For Part 2, build the leak detector as two independent regexes: one matching `api\.internal\.corp:8080/v[1-9]\b` (note: must NOT match a following digit like `/v10`, so anchor the digit class correctly), and one matching `test_user_[a-z]+:[A-Za-z0-9]{12}\b` (exactly 12 alphanumeric chars, not fewer — test the clean-corpus example that's deliberately one char short). Return exit code 1 if either regex matches anywhere in the file content, else 0; write no stdout/stderr that matters, but do `chmod +x` the script and give it a shebang. Test both parts against constructed positive/negative examples mirroring the exact boundary cases described (v9 vs v10, 12 vs shorter credential) before finishing.