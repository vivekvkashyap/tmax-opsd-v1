Process files in two passes: first determine each file's plaintext, then classify and act on all of them together (so permission changes don't interfere with your own scan). For `.enc` files, brute-force the single-byte XOR key by trying all 256 candidates, XOR-decoding the whole file with each, and accepting the first (or best) candidate whose result is fully printable ASCII (e.g. all bytes in the printable range plus common whitespace) — don't assume a specific key, discover it per file. For every file's plaintext (decoded `.enc` content or raw text-file content), scan for the exact literal signatures `../`, `<script`, and `onerror=` as substrings — any hit marks the file malicious. Apply permission changes with `os.chmod(path, 0o000)` for malicious files and `0o644` for safe ones, doing this scan-then-act so you don't accidentally lock yourself out of a file before reading its content (read via `open` before chmod, or open first then chmod). Build `report.json` as a simple filename-to-status dict and dump it with `json.dump`, keyed by basename (not full path) to match the expected format.
