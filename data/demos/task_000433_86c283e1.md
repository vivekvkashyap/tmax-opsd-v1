Fix the three named bugs in main.rs directly rather than rewriting the algorithm from scratch, then verify by running it. (1) Termination: replace the exact-equality check `term == 1e-10` with a threshold comparison `term.abs() < 1e-12`, since floating point recursion will essentially never hit an exact value and was falling through to the k>5000 stack-overflow-avoidance fallback (which is what caused the "hangs / wildly wrong" symptom for large x). (2) Formula: the cosine Maclaurin series is `sum_{k=0}^∞ (-1)^k x^(2k) / (2k)!`, so each term's recurrence from the previous is `next_term = term * (-x*x) / ((2k)*(2k-1))` — check the existing code's denominator pattern (it currently matches sine's `(2k)(2k+1)` recurrence, not cosine's) and correct the multiplier accordingly, keeping the "start at k=1, term=1.0" structure or adjusting indices consistently. (3) Precision: change `f32` to `f64` everywhere — the function signature, `x`, `term`, `next_term`, `k`, and the parsed CLI arg — since single precision causes catastrophic cancellation for larger arguments like 15.0. After editing, `cargo build`, run `cargo run -- 15.0`, and write the exact printed `Result: <value>` line to `/home/user/result.txt`; sanity-check the magnitude is bounded within [-1, 1] as any cosine value must be, which is a quick way to catch a still-broken formula or precision bug before finalizing.