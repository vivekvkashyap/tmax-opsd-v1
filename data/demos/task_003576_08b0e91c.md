Do the log extraction first since it's independent: use `grep` to isolate lines containing the exact marker string, then `awk`/`sed`/`cut` to pull just the leading ISO-8601 timestamp field off each matching line, writing one per line to the output file — avoid matching partial strings like just "FATAL" that could also hit unrelated lines. For the daemon crash, compile with debug symbols and run it under `gdb`/`valgrind` (or just careful code reading) to find the exact fault: look for pointer usage before allocation/initialization, a `fopen` return value that's never checked before being dereferenced, or reading more lines from the config than actually exist — fix the root cause (initialize/allocate before use, check return values, guard against short files) rather than papering over the symptom. Commit that fix inside the existing repo. For the CI/CD hook, write a `post-receive` script that reads the pushed ref, does a `GIT_WORK_TREE=<build dir> git checkout -f`, `cd`s into the build dir, compiles with `gcc`, and launches the resulting binary detached (backgrounded with output redirected, e.g. via `nohup ... &` or `disown`) so the hook process itself can exit while the daemon keeps running. Make the hook executable, add the bare repo as a remote, then push and verify the daemon wrote its PID and success log as specified.