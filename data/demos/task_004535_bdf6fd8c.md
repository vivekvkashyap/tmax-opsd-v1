Tackle the three parts in order, testing each before moving on. For the frame extraction, use `ffmpeg -ss 00:00:03 -i /app/demo.mp4 -frames:v 1 <tmpfile>.jpg` writing to a temp path (e.g. in `/tmp` or same target dir with a `.tmp` suffix), then use `mv`/`rename` to atomically move it to `/home/user/assets/thumb_raw.jpg` (creating the `assets` directory first) — a plain direct write to the final path would violate the "atomic write" requirement even though functionally similar, so keep the temp-then-rename pattern. For the nested archive, extract `/app/bundle.tar.gz` with `tar xzf`, which yields an inner `.zip`; unzip that to get `project_data.txt`, move it into `/home/user/assets/`, then create `/home/user/public/` and symlink `thumb.jpg` there pointing at the absolute path `/home/user/assets/thumb_raw.jpg` (use `ln -s <target> <link>`, not a copy). For the Go server, write `/home/user/server.go` with `net/http` handlers: `/data` reads and writes the exact bytes of `project_data.txt`, and `/video-thumb` should read through the symlink (opening `/home/user/public/thumb.jpg` via `os.Open`, which Go resolves transparently) and serve it with `http.ServeContent` or by setting `Content-Type: image/jpeg` and writing the bytes. Run it with `go run server.go &` or build+background it, and confirm both endpoints respond correctly with `curl` before leaving it running.