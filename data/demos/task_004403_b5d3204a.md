The nondeterminism comes from four threads racing to add their local partial sums into shared globals under a mutex — addition order (hence floating point rounding) varies run to run. Fix it by having each thread only ever touch its own local sums (already true) and store each thread's four partial sums into a per-thread slot (e.g. arrays `t_sum_x[NUM_THREADS]` etc., or a struct written back into the ThreadData) instead of accumulating into the shared globals inside the worker. After `pthread_join` on all threads, in the main thread sequentially sum slot 0, then 1, then 2, then 3 in that fixed order into `sum_x/sum_y/sum_xy/sum_xx` — this guarantees the same floating-point reduction order on every run, making output reproducible. Compile with `-lpthread -lm`. Then write `nginx.conf` with a `server` block on 8080 proxying `/api/fit` to `127.0.0.1:5000/analyze`, checking the exact `Authorization` header (via `if` on `$http_authorization` or `map`, returning 401 otherwise). Start broker, engine, and nginx in the background, verify identical output across repeated curls, and save one response body to `success.log`.
