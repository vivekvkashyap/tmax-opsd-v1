First recover the deleted files: mount /app/recovered_workspace.img read-only (`mount -o loop,ro`) or use a forensic tool like `debugfs`/`extundelete`/`icat` on the ext4 image to pull back format_spec.txt and the detector.cpp skeleton — a plain mount may not show deleted inodes, so if the files aren't visible after mounting, use `debugfs -R "lsdel" <image>` to list deleted inodes and `debugfs -R "dump <inode> <outfile>"` to extract them. Read format_spec.txt to confirm the on-disk layout, then reverse-engineer /app/legacy_parser with objdump/gdb (disassemble the function that reads input, note the fixed-size stack buffer and how it's filled) to pin down the exact overflow trigger condition — don't just assume "any overflow crashes it"; check whether the crash depends on a specific byte value at a specific offset past the buffer boundary, since stack layout only produces a crash (vs silent corruption) under a narrow condition. Implement that exact condition in detector.cpp (parse magic bytes, length field, and check the payload byte at the critical offset), compile to /home/user/detector, and validate against a few hand-crafted files matching and violating the condition before trusting it against the hidden corpus.
