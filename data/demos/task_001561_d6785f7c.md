Read `/home/user/model/network.json` generically rather than hardcoding layer sizes: iterate over `layers`, and for each dense layer load its `weights` matrix and `biases` vector as NumPy arrays, computing `Z = A @ W + b` (input row-vector times weights) then applying the named activation (`relu` = `np.maximum(0,x)`, `sigmoid` = `1/(1+np.exp(-x))`) to get the next layer's activations — chain this across all layers in order so the script works even if the architecture changes. Load `/home/user/data/input.csv` with pandas/numpy, select the three sensor columns in the same order the network expects, run every row through the forward pass in a single vectorized batch (not a Python loop) for correctness and speed, then threshold the final sigmoid output at `>= 0.5` to get integer 0/1 predictions. Create `/home/user/output/` if it doesn't exist, then write `id,prediction` to `/home/user/output/predictions.csv` preserving the original row order and integer `id` values exactly as given in the input. Double-check no high-level ML framework is imported — only NumPy is allowed for the math.