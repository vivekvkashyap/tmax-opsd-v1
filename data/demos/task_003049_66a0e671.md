Build this incrementally rather than writing the full MCMC program at once. First write and test a standalone Newton-Raphson solver for x^3 + mu*x - lambda = 0 (initial guess x=1.0, tolerance 1e-6, max 100 iters) as its own function, and sanity-check it against a couple of hand-computed cases (e.g. mu=0 gives x=lambda^(1/3)). Then implement the Metropolis-Hastings step: propose mu' ~ N(mu, 0.1^2) and sigma' ~ N(sigma, 0.05^2) (reject immediately if either falls outside its uniform prior bounds), compute the log-likelihood by solving for x_pred at each observed lambda via your Newton-Raphson routine and evaluating the Normal(x_pred, sigma) density at the observed x, then accept/reject via the standard Metropolis ratio. Launch 4 independent chains as goroutines synchronized with a `sync.WaitGroup`, each running 15,000 iterations and discarding the first 5,000 as burn-in, then average the post-burn-in samples within each chain before averaging the 4 chain-means. Wrap `pprof.StartCPUProfile` around the sampling loop and `StopCPUProfile` after, writing to `/home/user/cpu.prof`. Read `/home/user/observations.csv` with the standard `encoding/csv` package. Write the final comma-separated mu,sigma to `/home/user/results.txt`. Run the program once at the end to produce both output files, and sanity check that the recovered mu/sigma look consistent with the shape of the observed lambda-vs-x data.