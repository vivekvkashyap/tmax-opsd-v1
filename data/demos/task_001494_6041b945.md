Build the pipeline as discrete, testable stages rather than one monolithic script: read raw_events.jsonl, filter out `event_type == "system_ping"`, then mask `ip_address` by regex-replacing only the final dot-separated octet with `xxx` (careful not to touch the other three octets). Group remaining events by `user_id` and sort each group by `timestamp` ascending — Python with `itertools.groupby` after a full sort, or a dict-of-lists, is simpler than trying to do this in awk/sed. For the rolling average, maintain a per-user sliding window of up to the last 3 durations seen so far (including the current event), compute their mean at each step, and track the maximum such average per user, rounding to 1 decimal only at the end. Also accumulate the count of valid events and the set of unique masked IPs per user (sorted alphabetically, joined with `;`). Write the four required columns in exact order to summary.csv, then make run_pipeline.sh executable and actually invoke it so the CSV is generated on disk.