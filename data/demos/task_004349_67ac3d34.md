Order your C program's pipeline strictly as: join first (only keep ExpIDs present in both files AND Category=="Target" in metadata), then per-experiment cap outliers (>100.0 -> 100.0, <0.0 -> 0.0) on whatever values are present, then compute the mean of the present+capped values and substitute it for any missing (empty-string) field — the task explicitly requires capping before averaging for imputation, which is the detail naive solutions get backwards (imputing first with raw outlier values would corrupt the mean). Parse each series.csv row with manual comma-splitting rather than scanf, since empty fields between commas must be detected as zero-length tokens, not skipped. After building cleaned 5-vectors for all valid Target experiments, compute pairwise Euclidean distances (nested loops over pairs, `sqrt(sum of squared diffs)`) and track the minimum, breaking ties consistently. When writing the winning pair, compare ExpID strings and always print the lexicographically smaller one first (e.g. `strcmp`), format the distance with `%.4f`, and write exactly one line with no header. Compile with `gcc -lm` and run to produce closest_pair.txt; hand-verify one or two pairwise distances against your own calculation before trusting the output.
