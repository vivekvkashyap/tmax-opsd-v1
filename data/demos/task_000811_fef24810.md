First recover the schema regex from the video before writing any detection logic: extract frames with ffmpeg (e.g. 1 fps or all frames around the dashboard's flash), then run tesseract OCR on candidate frames to find the line labeled "STRICT CSV SCHEMA:" and read off the regex that follows it — verify by testing the extracted pattern against a couple of known-good/bad lines before hardcoding it into the script. For schema validation in pure bash, use `grep -Ev` with the anchored regex against every line of the input file and immediately print INVALID_SCHEMA/exit 1 on any non-match. For deadlock detection without Python, model this as a graph-cycle problem solvable with a bash-driven DFS: build adjacency using associative arrays or a temp file of edges, then implement recursive/iterative DFS with a "visiting" (on current path) vs "visited" (fully explored) coloring — a cycle exists iff you re-encounter a node that's still in the "visiting" state on the current DFS path (not just any previously-visited node, which would cause false positives on shared ancestors in a DAG). Test against deep chains (10+ hops) to ensure your traversal doesn't wrongly terminate early or miss recursion depth.
