Work through the failures in the order the build pipeline hits them. First, `go build`/`go mod tidy` and read the actual error to see which dependency version in go.mod doesn't resolve; replace it with the real, publishable stable version of that module (check what versions actually exist rather than guessing a number) so the build succeeds. Next, run the tool and read the panic/stack trace to find the malformed-line crash: guard the line-parsing loop with a check that `len(parts) == 4` before indexing into it, skipping (continuing past) any line that doesn't have exactly four comma-separated fields. Then re-read the code's own comment describing the intended formula ("50% CPU, 10% Memory") and compare it against the actual multiplier used in the score expression — fix the constant so it matches the stated percentage exactly. Finally check the averaging step: dividing by `count-1` instead of `count` is a classic off-by-one — confirm by counting how many lines actually pass your new 4-field filter and dividing the accumulated total by that exact count. After fixing all three, `go run` (or build+execute) the tool, capture the printed average from stdout, and write just that numeric string to solution.txt.