Use pandas with `unicodedata.normalize('NFC', s)` applied to the `station` column first — do this before any duplicate check, since rows that look identical to a human may differ byte-for-byte due to NFC vs NFD encoding (e.g. combining diacritics). Count rows before and after `drop_duplicates(keep='first')` to get the exact removed count for logging. For missing `temp`, group by `station`, sort each group by `date`, then use pandas' `interpolate(method='linear')` on the temp column (convert empty strings to NaN first with `pd.to_numeric(errors='coerce')`); count NaNs before interpolation for the imputed-count log line. Write the log with exactly two lines in the specified order. For the JSONL output, sort the final dataframe by `station` then `date`, and emit one JSON object per line with keys in order `date, station, temp` using `df.to_dict('records')` plus `json.dumps` (avoid pandas' `to_json` since key ordering/float formatting may not match exactly). Double check float values aren't turned into strings.