Diagnose the Makefile failure first: run `make install` in `/app/text_prep_serve`, read the actual error (likely a misspelled filename reference), and fix the target so it installs from the real `requirements.txt`. For the data leak, open `src/prepare.py` and trace the order of operations — the bug is that `TfidfVectorizer().fit_transform(...)` is called on the full dataframe before the train/test split; the fix is to compute the 80/20 split index first (keeping the split boundary identical), then `fit_transform` only on the training slice and `transform` (not fit) on the test slice, so test-only vocabulary never leaks into the vectorizer. Re-run `python src/prepare.py` to regenerate models/vectorizer under `models/`. Then launch `python src/server.py` as a background process (e.g., `nohup ... &` or `disown`) bound to `127.0.0.1:8000`, and manually curl `POST /predict` with a sample document to confirm it returns `{"prediction": <int>}` before leaving it running — the verifier depends on the server still being alive when it sends its own requests.
