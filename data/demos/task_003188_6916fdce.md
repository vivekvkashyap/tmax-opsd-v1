Work through the diagnostics one broken piece at a time and check the daemon's failure mode as you go. Re-point the symlink with `ln -sf /home/user/service/configs/production.json /home/user/service/config.json` (use `-f` to replace the existing broken link) and verify with `readlink`. Create the missing cache directory with `mkdir -p /home/user/service/cache`. For the supervisor, write a bash script with a `while true; do ...; done` loop that runs `python3 /home/user/service/daemon.py` inside it so any crash/exit triggers an automatic restart on the next loop iteration — test that the loop syntax is correct before backgrounding it. Launch it detached with `nohup bash /home/user/service/supervisor.sh > /dev/null 2>&1 &` so it survives shell exit, then give it a moment to actually bind the port. Only once the daemon is confirmed listening should you run `curl -s http://127.0.0.1:8181/health` and redirect the exact output to `/home/user/service/health_check.txt` — if curl fails, re-check the config/cache fixes rather than the supervisor logic, since the daemon exits silently on each individual missing precondition.