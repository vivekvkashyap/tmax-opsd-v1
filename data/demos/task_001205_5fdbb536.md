Start by extracting the ground truth from the image: open/OCR `/app/ticket_attachment.png` to read off the exact byte layout of the 10-byte packet (which fields are big vs little endian, their sizes and offsets) and the exact convergence equation, initial guess, iteration cap, and convergence tolerance — do not assume a layout, read it directly from the image content. Implement the parser in Python: use `bytes.fromhex` on the CLI argument, then `struct.unpack` with the correct format/endianness per field as read from the image. For the Newton's-method solve, replicate the iteration faithfully including the specified initial guess and convergence check, and — critically — fix the division-by-zero edge case by checking whether the derivative term is exactly zero before dividing, returning the fallback value specified in the image's spec instead of crashing (this is exactly the bug in the stack trace you were given). Round the result to the number of decimals the spec calls for and emit compact JSON matching the exact key names and structure from the spec. Since this is checked by byte-for-byte fuzz equivalence against a hidden oracle across many random hex inputs, test your script yourself against varied hex strings (including ones that hit the zero-derivative and non-convergence edge cases) to make sure formatting and numeric behavior are robust, not just correct on one example.