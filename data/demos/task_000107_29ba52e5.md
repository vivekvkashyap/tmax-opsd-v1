Implement the C++ processor first and validate it in isolation before wiring up the tuning script. Parse `/home/user/data.csv` line by line (id, v1, v2), raise each value to power `p` using `pow()`, and accumulate per-id sums in a map keyed by id — don't reset the map between rows, and don't forget rows sharing an id must combine their v1^p and v2^p contributions before taking the norm. After processing all rows, iterate the map once, compute `sqrt(S1^2+S2^2)` per id, sum across ids, and print with `std::fixed << std::setprecision(4)` and only the number (no extra text). Compile with plain `g++ etl_processor.cpp -o etl_processor -lm` and manually run it for p=1..5 to sanity-check the numbers look monotonically increasing before writing the wrapper. For `tune.sh`, loop p in 1..5, capture each score via command substitution, compute `|score - 150.0|` with `awk` or `bc` (bash can't do floating point natively), track the minimum, and write `p=<best_p>,score=<best_score>` to `/home/user/best_param.txt` with the score formatted to exactly 4 decimals matching what the C++ program printed. Execute `tune.sh` yourself at the end to confirm the file is generated.
