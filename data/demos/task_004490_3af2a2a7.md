Work the four artifacts in order, each feeding the next. First, OCR `/app/equation.png` with `tesseract` to read the `MODULUS=<number>` line — don't guess it visually. Second, recover the uncommitted token from the WAL: just query the DB normally, e.g. `sqlite3 /app/state.db "SELECT value FROM config WHERE key='AUTH_TOKEN';"` — SQLite auto-replays the `-wal` file on connect, so no manual WAL parsing is needed as long as both `state.db` and `state.db-wal` sit together in the same directory. Third, open `requests.pcap` (e.g. `tshark -r requests.pcap -Y http -T fields -e http.file_data` or similar) to inspect the last POST body sent before the crash — reproduce the crash locally first to see which value trips the exception (likely a math domain error from a negative input to a sqrt/log-type function), then wrap that computation in a try/except and return 400 instead of crashing. Wire the recovered modulus and token into `server.py`, require the Bearer token on POST `/compute`, then launch the Flask app bound to `127.0.0.1:8888` in the background (e.g. with `nohup ... &`) so it stays reachable for verification.
