Work through this as three separate, verifiable fixes rather than guessing at all of them at once. First get `make` to succeed: shared libraries need `-fPIC` on the compile step, not just `-shared` on the link step — check the Makefile's compile rule. Second, reproduce the crash before fixing it: compile, run the service with a large batch, and confirm with a debugger (`gdb --args python3 service.py`, or add prints) that it segfaults; then inspect the C source for any place a packet-count/packet-size multiplication is stored in a fixed-width signed type — with large inputs that product overflows and can go negative, so `malloc` receives a huge or garbage size. Fix by widening the multiplication and buffer-size variable to `size_t` throughout, not just at the malloc call site. Third, for the leak, use a tool like `valgrind` or just reason about the code path: whichever allocated buffer is never freed after use needs an explicit `free()` (or a small `free_buffer` export called from Python via ctypes) once its data has been consumed. Finally write `test_regression.py` to actually call `ctypes.CDLL(...)`, run the specified large-batch and iteration cases, measure RSS growth with `psutil` before/after the loop, and only emit the exact pass string if both conditions truly hold.
