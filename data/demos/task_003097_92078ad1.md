Design the Rust program around three clear steps: (1) walk the target directory recursively (e.g. via `walkdir` or manual `fs::read_dir` recursion), computing each file's path relative to the target root; (2) for each target file, check whether a file with the same relative path exists under the base directory and whether its byte contents are identical (read both files fully and compare, or compare lengths first as a fast-path then bytes) — files missing from base or with differing bytes count as "changed", identical files are skipped entirely (no output); (3) for changed files, RLE-encode the ASCII content by scanning consecutive runs of the same character and emitting `<count><char>` per run (count as plain base-10 digits, no separators, even single-character runs get printed as `1<char>`), then print `FILE: <relative_path>\n` immediately followed by the RLE string and a trailing newline. Compile with `rustc` or `cargo build --release`, run it as `./tracker /home/user/etc_backup /home/user/etc_mock > /home/user/config_diff.out`, and manually verify the output contains entries only for new/modified files and omits unchanged ones.
