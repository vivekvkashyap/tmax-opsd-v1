Handle the video extraction before writing any Rust. Dump frames with `ffmpeg -i /app/dashboard.mp4 -vf fps=30 frame_%03d.png` (or similar high frame rate since the config text may appear briefly), then run `tesseract` (install via package manager if missing) over the frames — or visually inspect a handful if OCR is unreliable — to find the line matching `ALERT_CONFIG: W=<seconds>s, T=<count>`; scan multiple frames since the exact frame containing it isn't predictable. Extract W and T as concrete numbers and hardcode/parametrize them into the detector's logic — don't guess without confirming from the frame text. For the Rust program: parse each log line with a regex or manual split matching exactly `[<timestamp>] WARNING Failed authentication for user <username> from <ip_address>`, ignoring non-matching lines. Group timestamps per IP into a sorted `Vec<i64>`, then use a two-pointer sliding window (not O(n^2) pairwise comparison) per IP: advance a right pointer through sorted timestamps, shrink the left pointer while `timestamps[right] - timestamps[left] > W`, and track the max window count (right - left + 1) seen; if that max strictly exceeds T for any IP, the file is evil. Exit with code 1 immediately upon finding one malicious IP (no need to scan further), else exit 0 after all IPs are checked cleanly. Test against `/app/corpora/clean/` and `/app/corpora/evil/` yourself before trusting the build, checking exit codes with `echo $?`.