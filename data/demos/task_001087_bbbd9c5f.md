Locate the exact source of non-determinism before touching anything else: it's the `sample.par_iter().sum()` call in the bootstrap loop in `/home/user/gc_bootstrap/src/main.rs` — parallel (Rayon) floating-point summation reorders additions across threads, and floating-point addition isn't associative, so results vary run to run. Fix this by replacing that call with a sequential reduction, i.e. `sample.iter().sum()` (or an explicit fold), so the summation order is deterministic every run — do not touch the RNG seed (42), the bootstrap iteration count (1000), or the sampling logic itself, since only the reduction order is the bug. After making this single targeted change, `cargo build --release` and run the program, redirecting its printed output (the single formatted floating-point mean) exactly as printed into `/home/user/result.txt` with no extra whitespace or trailing content. Run it twice to confirm you now get bitwise-identical output before finalizing, since that reproducibility is the actual thing being tested.