Because grading is exact numerical equivalence against a hidden oracle across random inputs, implement each step literally per the spec rather than approximating. OCR `/app/config_spec.png` (`tesseract` or `pytesseract`) to recover `w0` and `dt` as floats, checking the raw OCR text carefully (e.g. confirm decimal points weren't misread). In process_signal.py: read one line from stdin, split on commas to floats, fit `scipy.stats.gaussian_kde(data)` with default bandwidth, evaluate on `numpy.linspace(-10.0, 10.0, 200)`, and take the max as amplitude A — use these exact numpy/scipy calls since default KDE bandwidth/kernel choices must match the oracle bit-for-bit. Then run the Forward Euler loop exactly as specified: iterate n=0..199, t_n = n*dt, update y and v in that order (compute both new values from the old y,v before overwriting either — don't update v using the already-updated y), append y_{n+1} each step to a 200-element list. FFT that list with `numpy.fft.fft`, take `np.abs()`, find the max, and print with `f"{val:.4f}"` (4 decimals) — match this exact rounding/formatting so stdout is directly diffable against the reference. Self-check by running the script against a couple of synthetic inputs and re-deriving the numbers by hand/spreadsheet for a tiny case before trusting it against the fuzz harness.