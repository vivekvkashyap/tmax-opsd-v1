Run tesseract directly on the PNG (`tesseract /app/scan_config.png stdout`) and read the recovered text carefully—it will contain a distinctive User-Agent string and/or URL path that the attacker's tool uses; use whichever is clearly extracted (cross-check both if OCR is ambiguous on one). Build detect.sh as a simple, robust grep-based classifier: `grep -qE '<recovered-pattern>' "$1" && echo MALICIOUS || echo CLEAN`. Key correctness points: match literally on the exact recovered string (escape special regex characters like `/` and `.` if using extended regex, or use `grep -F` for a literal substring match to avoid false negatives from regex metacharacters), ensure the script only ever prints exactly "MALICIOUS" or "CLEAN" with no extra output, and test it against varied log line formats (the pattern might appear in the User-Agent field or the request path field) so it isn't overly narrow and misses valid evil-corpus entries or too broad and flags clean traffic. Remember to `chmod +x` the script.
