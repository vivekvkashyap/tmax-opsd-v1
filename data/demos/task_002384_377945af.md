Work the three sub-problems independently and verify each before moving on. (1) For the leaked key: `git -C /home/user/telemetry_pipeline log --all --oneline` to see full history including commits not on the current branch, then `git log --all -p -S DIAGNOSTIC_API_KEY` (pickaxe search) to find commits that introduced/removed it, and `git show <commit>:<path>` or grep the diff to extract the exact value into `/home/user/leaked_key.txt` with no extra whitespace/newline. (2) For the regression: use `git bisect start`, mark a known-bad and known-good commit, and at each step build/run `telemetry_encoder` against a coordinate from the diagnostic video (extract frames 45-60 with `ffmpeg -i /app/diagnostic_clip.mp4 -vf "select='between(n,45,60)'"`) to script a pass/fail test for `git bisect run`; record the full 40-char hash it lands on. (3) For the Python reimplementation: run the pre-regression binary (checkout the commit before the bad one, rebuild) across a wide range of sample integers, compare output patterns, and hypothesize the bit-exact transformation (shift/mask/xor) — verify your Python function reproduces the binary's output exactly across many random and edge-case inputs (negatives, zero, large values) before finalizing `encoder_fix.py`.