Implement least squares manually via QR rather than calling `lstsq`: load both CSVs with `np.loadtxt(..., delimiter=',')`, compute `Q, R = np.linalg.qr(A)` (reduced/economic mode, which is numpy's default), then solve the resulting upper-triangular system `R X = Q.T @ B` with `np.linalg.solve(R, Q.T @ B)` — this avoids explicitly inverting R and is the standard QR-based least-squares approach. Reconstruct the fitted spectra as `A @ X` and compute per-sample RSS as `np.sum((B - A@X)**2, axis=0)`, one value per of the 5 samples. Save `X` to `/home/user/concentrations.csv` with `np.savetxt(..., delimiter=',', fmt='%.6f')` and RSS to `/home/user/rss.txt` similarly with one value per line at 6 decimal places — match formatting exactly since the checker does a value/format comparison, not just numeric tolerance. Double check matrix shapes: A is 50x4, B is 50x5, so X should be 4x5 and RSS should have 5 entries.