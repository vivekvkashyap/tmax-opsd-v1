Run `tesseract /app/user_matrix.png stdout` (try `--psm 6` for a clean tabular layout if the default segmentation garbles columns) and inspect the raw output before writing any pipeline. Pipe it through `grep -E '^[0-9]+'` to keep only data rows (dropping the header and any OCR noise lines), then normalize whitespace with `awk '{print $1, $2, $3}'` or `sed` so each line is `USER_ID DEPT LEVEL` with single-space separation — this is the cleaned intermediate the C program will parse. Write `generate_config.c` to read each line with `fscanf`/`sscanf` into `(int id, char dept[], int level)`, lowercase `dept` in place (loop with `tolower()`), and `fprintf` the two required lines per user in order to `/home/user/final_config.txt`. Compile with `gcc generate_config.c -o /home/user/generate_config`, run it, then manually diff a few lines of your output against what you'd expect from re-reading the image to catch OCR misreads (e.g. `0`/`O`, `1`/`I` confusion) before finishing — accuracy is graded per-line so a single systematic OCR error can cost several points.
