Build the header check first, standalone: run `/app/legacy_sig_check` against a few files and cross-check with `xxd -l 16 <file>` to learn exactly which bytes it requires (magic bytes + version field), then reimplement the equivalent check in pure Bash using `dd`/`head -c`/`od`/`cmp` so you avoid spawning an external binary per file — this is what makes the parallel pipeline fast. For gate 2, extract text after byte 16 with `tail -c +17` and use `grep -c`/`grep -q` to enforce: exactly one `hostname <alnum-hyphen>` line, zero occurrences of the literal `password 7 ` substring, and exactly one `DeviceType: <type>` line — don't just check presence, check counts/format strictly since the evil corpus will include near-misses (wrong version byte, missing hostname, embedded weak-password string, etc). Drive the whole per-file pipeline (header check + policy check + extract DeviceType + copy) as a function, then fan it out with `find <input_dir> -type f | xargs -P <n> -I{} bash -c 'process "$@"' _ {}` (or `parallel`), using `mkdir -p` per-device-type subdir before copying. Clean up any scratch files you create.
