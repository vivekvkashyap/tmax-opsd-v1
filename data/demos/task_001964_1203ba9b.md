Use `find` with combined predicates rather than separate passes, so you don't rename files that only satisfy one criterion: `find /home/user/repo -type f -name '*.bin' -size +100k -not -newermt '2023-01-01'` (verify the size and date operators against `find`'s docs/manpage yourself, since off-by-one boundary semantics matter here) to locate qualifying `.bin` files anywhere in the tree, then for each match rename in place by prepending `legacy_` to the basename only (preserve the directory), e.g. via `mv "$f" "$(dirname "$f")/legacy_$(basename "$f")"`. Separately, `find /home/user/repo -type f -name '*.conf'` and run `sed -i 's/backend: legacy-storage/backend: cloud-storage/' <file>` on each match only — don't touch `.txt` or other extensions even if they contain the same string. While processing the `.conf` files, grep each one for a line matching `^id: ` and extract the value after the prefix, trimming whitespace; collect all extracted IDs, sort them alphabetically, and write one per line to `/home/user/extracted_ids.txt`. Do these as three clearly separated passes (rename, then edit conf, then build manifest) so you can verify each step's output before moving to the next.
