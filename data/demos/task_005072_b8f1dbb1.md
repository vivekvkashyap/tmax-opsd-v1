Treat the compiled binary as a black box and probe it empirically rather than guessing its internals: feed it small hand-crafted CSVs (one with an embedded newline inside a quoted field, one with a malformed timestamp, one totally clean) and observe its exit code/output directly (`/app/ts_ingester <file>; echo $?`) to confirm the two failure signatures described in the task. Then implement detector.sh as two independent checks: (1) detect embedded newlines inside quoted fields by counting unescaped double-quotes per logical record or using a CSV-aware parser (python's csv module or a quote-tracking awk/state loop) rather than naive line-splitting, since a naive `grep` for newlines won't distinguish "newline inside quotes" from ordinary line breaks between records; (2) validate every value in column 1 against exactly the two allowed strptime-style formats (`YYYY-MM-DD HH:MM:SS` and `YYYY-MM-DDTHH:MM:SSZ`), rejecting anything else including epoch numbers or slash-separated dates — do this with a regex plus a real date-validity check (e.g. python's `datetime.strptime`) so things like month 13 are also caught. Exit 0 only if both checks pass on the whole file, else exit 1, and always append the log line with current timestamp, basename, and CLEAN/EVIL before returning.
