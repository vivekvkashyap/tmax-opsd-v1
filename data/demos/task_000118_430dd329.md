Model this as directed graph cycle detection, not manual inspection. Parse `locks.csv` and build edges the way the description defines: for each transaction with a non-empty `resource_requested`, find whichever transaction currently holds that resource (match `resource_requested` against another row's `resource_held`) and add a directed "waits-for" edge from the requester to the holder. Skip rows with empty held/requested fields appropriately rather than creating spurious edges. Once the graph is built, run a cycle-finding traversal (e.g. DFS tracking the current path and a visited/on-stack set) starting from any node — since the problem guarantees exactly one isolated cycle, the first cycle you detect is the answer; don't assume you need to search from every node exhaustively, but do verify the cycle by walking it forward and confirming each edge really is a wait-for relationship implied by the resource ownership. Once you have the cycle's node set, pick the lexicographically smallest ID as the rotation start point and walk the cycle in its natural directed order from there (not alphabetically sorted overall) to produce the comma-separated output — rotate, don't re-sort. Write that single line to `/home/user/deadlock.txt`.
