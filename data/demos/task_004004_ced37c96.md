Load both CSVs (with `csv.DictReader` or `pandas.read_csv`) and build in-memory indexes: a node lookup by `node_id` (for label/weight), and adjacency lists of edges grouped by `source_id` filtered by `rel_type`. Implement the 3-hop pattern match explicitly rather than trying to be clever: for every edge where `rel_type == args.action` and the source node has label `User` and the target node has label `Item` with `weight >= args.min_weight`, then for every outgoing edge from that Item with `rel_type == 'BELONGS_TO'` whose target has label `Category`, emit a path record with `path_score = user.weight + item.weight`. Sort the collected paths by `(path_score, user_id)` — reverse only the score component when `--sort-order desc` (ascending alphabetical `user_id` tie-break must NOT flip), e.g. sort by `user_id` ascending first as a stable secondary key, then sort by `path_score` with `reverse=` based on the flag (Python's stable sort lets you sort by the tie-breaker first, then by score). Apply `results[offset:offset+limit]` and dump the paginated list as JSON with the exact keys `user_id, item_id, category_id, path_score` to `--output` using `argparse` for all six flags.