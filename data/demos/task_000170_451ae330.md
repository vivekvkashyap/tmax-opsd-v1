Build this as a straight-line Python pipeline, not shell tools, since the task explicitly calls out that naive shell CSV parsing drops rows with embedded newlines. For video: open `/app/machine_feed.mp4` with `cv2.VideoCapture`, convert each frame to grayscale, and average pixel values in the `[0:50, 0:50]` slice per frame — keep these in fps-order. Bucket into seconds by grouping consecutive runs of `fps` frames (read the video's actual fps rather than hardcoding, though the spec says 10) and average within each bucket, rounding to 2 decimals only at final output time (not before changepoint math, to avoid compounding rounding error). For changepoints, compare each second's mean to the previous second's *unrounded* mean and flag `True` when `abs(diff) > 15.0`, with second 0 always `False`. For the CSV, use Python's `csv.reader` (or `pandas.read_csv`) which correctly handles quoted multi-line fields per RFC 4180 — this is exactly what shell tools like `cut`/`awk` get wrong — then replace embedded `\n` in `Notes` with spaces. Merge on `Second`, write the 5 required columns in order for all 60 seconds, and log frame/row counts plus start/end timestamps to `pipeline.log`.
