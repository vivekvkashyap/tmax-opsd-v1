Read topo.py carefully to understand its exact algorithm: it does a DFS-based topological sort, visiting nodes in sorted-alphabetical order as the outer loop, following each node's listed dependencies recursively, and inserting each node at the front of the order list once fully visited (post-order, prepend). Your Bash version must reproduce this precise traversal, not just any valid topological order, since the Makefile expects an exact object-file link sequence. Parse deps.txt with awk into edge pairs (first column = node, remaining columns = its deps; a line with only one token means the node has no deps but must still appear). Consider using `tsort` on the edge-pair list combined with reversing/sorting to match the DFS-insert-at-front semantics, or implement the DFS logic directly with associative arrays and recursion in Bash. Test topo.sh's output against topo.py's output on deps.txt before wiring it into the Makefile. Make topo.sh executable, avoid invoking python/python3 anywhere inside it, then run `make test` and inspect test_results.log for the correct linked program output.