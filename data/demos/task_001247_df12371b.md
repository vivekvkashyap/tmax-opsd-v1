Tackle this as two separable pieces. (1) Nginx: edit `/app/nginx/nginx.conf` to add `client_max_body_size 20M;` inside the `http` (or `server`) block, then reload/restart nginx via the provided `/app/start_services.sh` or `nginx -s reload` — verify with a real upload test (e.g. `curl -F file=@bigfile.csv http://localhost:8080/upload`) rather than assuming the config is picked up. (2) The sanitizer at `/home/user/csv_sanitizer`: build it with a real CSV parser (Python's `csv` module, or a proper RFC4180-aware parser) — never split naively on commas/newlines, since quoted fields can contain embedded `\n` and escaped commas and a naive line-splitter will silently corrupt rows. For CSV-injection defense, check the raw (unescaped) value of every field in every row for a leading `=`, `+`, `-`, or `@` and immediately reject (nonzero exit, no output file) if any match. For accepted files: extract only `user_id`, `timestamp`, `transaction_amount`, hash each row's concatenated raw values with SHA-256 to dedupe (keep first occurrence), then write a JSON array of objects. Make the script executable with `chmod +x`, and test it directly against a few crafted clean/injection CSVs of your own before trusting it against the hidden corpora.