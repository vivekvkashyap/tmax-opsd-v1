Initialize a Cargo project in `/home/user/auditor/` and add crates for base64, sha2, and json serialization (e.g. `base64`, `sha2`, `serde_json`). For each numbered subdirectory under `/home/user/proc_dump/`, treat the directory name as the PID (parse as integer for sorting). Read `cmdline` as raw bytes and split on the NUL byte (`\0`) to recover individual arguments — do not treat it as a normal string, since splitting on whitespace will break arguments containing spaces. Find the argument with prefix `--token=`, base64-decode the remainder, then redact by keeping the first 2 chars of the *decoded* string and replacing every remaining character with `*` (length must exactly equal the decoded string's length). For integrity, compute SHA-256 over the raw bytes of each PID's `exe` file and check for an exact-line match against `/home/user/hashes.txt` (trim trailing newlines when comparing). Build the final JSON with `findings` sorted ascending by numeric PID, and match the exact key names/order shown in the spec (`csp_policy`, then `findings` with `pid`, `redacted_token`, `integrity_valid`). Run `cargo build --release` then execute the binary to produce `/home/user/report.json`.
