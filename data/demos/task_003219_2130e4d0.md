Work top-down: fix the C code, compile, wire up the proxy, then run under valgrind. In `server.c`, implement `compute_fletcher16` with the textbook algorithm — accumulate `sum1 = (sum1 + byte) % 255`, `sum2 = (sum2 + sum1) % 255` per byte, return `(sum2 << 8) | sum1`; hand-verify on a short string before trusting it. For the leak, find where `on_message_received` (the libwebsockets receive callback) `malloc`s a per-message buffer and never frees it — add the matching `free()` after its contents are used, without freeing before the response is built. Build with `make` in `/home/user/divn/`, confirming `divn_server` binds `127.0.0.1:9000`. Write `/home/user/nginx.conf` with `events {}`, `error_log`/`pid` under the home dir (unprivileged), and `listen 8080` proxying to `127.0.0.1:9000` with `Upgrade`/`Connection` headers set for websocket support. Start nginx, then the server under `valgrind --leak-check=full`, leaving both running so the automated client can connect, exchange a message, and the valgrind log can later be checked for zero leaks.