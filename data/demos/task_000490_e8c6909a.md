Start by inspecting the schema directly rather than assuming a layout: run `sqlite3 /home/user/datasets.db ".schema"` and `.tables` to see the actual table/column names for datasets and their dependency edges, and sample a few rows with a `SELECT *` to confirm which column represents "depends on" vs the target of the dependency. Find mutual pairs with a self-join on the edges table: join the edge table to itself where the direction is reversed (A depends on B in one row, B depends on A in the other), and to avoid emitting each pair twice (once per direction) restrict to rows where one node's id is less than the other's, or dedupe after generating both orderings. For "combined in-degree," be precise about what counts as an incoming dependency edge for a given dataset — count all rows where that dataset is the depended-upon side (the source of the dependency), including the mutual edge itself, summed across both datasets in the pair; verify your interpretation on paper against a couple of rows before trusting the query. Resolve node IDs to names via a join to the datasets/nodes table, format each result line as `Name1,Name2,CombinedInDegree` with `Name1` alphabetically first, and pipe the whole query's output through `sort` to guarantee deterministic ordering by `DatasetName1`. Make the script executable and ensure it prints nothing but the CSV lines.
