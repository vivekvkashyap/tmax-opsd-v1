Start by transcribing `/app/instructions.wav` (e.g. install/use `whisper` or another speech-to-text tool, or ffmpeg to inspect the audio) — do not guess the compromised manager ID or the malicious NoSQL operator names; extract them from the audio content itself. Once you have the compromised manager ID, build the reporting hierarchy from `/app/employees.csv` recursively/transitively: start from that manager's `emp_id`, then repeatedly collect all employees whose `manager_id` is already in your compromised set, iterating until no new IDs are added (BFS/DFS over the manager_id graph, not just one level). For each `.json` ticket in the input directory, mark it `evil` if either `author_id` is in that computed hierarchy set, or the `query_payload` object (recursively, since it may be nested) contains any key matching the operator names you transcribed from the audio; otherwise `clean`. Write `filename,status` rows to the output CSV using just the basename. Before finishing, run your script against `/app/corpus/clean/` and `/app/corpus/evil/` separately and confirm every clean file is `clean` and every evil file is `evil` — treat any mismatch as a bug in either your hierarchy traversal or your operator/audio extraction, not something to special-case.