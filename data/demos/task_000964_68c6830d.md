Load the HDF5 file with `h5py` and get `f['trajectories'][:]`, giving shape `(num_chains, num_steps, num_dims)`. For each chain index, test `np.all(np.abs(chain) <= 1000.0)` — keep the chain only if this holds for every value across all steps and all dimensions (a single outlier anywhere in the chain disqualifies the whole chain, not just that step). From the surviving chains, take dimension 0 (`chain[:, 0]`) and concatenate/flatten across chains into one 1D array of valid samples. Implement the Gaussian KDE exactly as specified — don't substitute `scipy.stats.gaussian_kde`, since its default bandwidth selection (Scott's/Silverman's rule) differs from the fixed `h=0.5` required here; instead vectorize the formula directly: for each of 100 evaluation points from `np.linspace(-5.0, 5.0, 100)`, compute `mean(norm.pdf((x - samples)/h)) / h` or the equivalent explicit sum. Round each resulting density to 6 decimal places and write one per line to `/home/user/kde_output.txt`, verifying the file has exactly 100 lines afterward.
