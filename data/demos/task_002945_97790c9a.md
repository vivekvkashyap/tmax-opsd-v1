Treat the audio file as a container carrying metadata, not a signal to transcribe. Use `ffprobe -v quiet -show_format /app/schema_instructions.wav` (or `ffmpeg -i ...`) to dump format tags and locate the comment field holding the plain-text ETL rules — read it carefully, it specifies exact filtering, join, and aggregation steps plus the required output shape. Once you have the rules, scaffold a Rust binary crate at `/app/etl_pipeline` (`cargo new`), add `serde`/`serde_json` for parsing stdin and building output, and implement the pipeline precisely as specified: filter/discard logic first, then aggregation, then serialize as compact JSON with deterministic key ordering (use a `BTreeMap` so keys sort automatically rather than hand-rolling sort logic). Build in release mode (`cargo build --release`) so the binary lands at `/app/etl_pipeline/target/release/graph_etl`. Before finishing, self-test by piping a few hand-crafted JSON payloads (including edge cases like inactive nodes, non-CONNECT actions, and duplicate src/dst pairs) through the binary and manually verifying the aggregation against the extracted rules, since the grader will fuzz against a hidden reference.
