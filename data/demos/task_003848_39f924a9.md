Build and test the two ends of the pipeline independently before wiring them together. First write mock_backend.py using Python's http.server on 127.0.0.1:9090 returning the fixed 200 body for any GET, and launch it in the background; confirm with curl. Then write secure_proxy.c: use a plain blocking TCP server loop (socket/bind/listen/accept), read the raw request into a buffer, and parse it with simple string scanning rather than a full HTTP library — split the first line on spaces to get the URI, then scan subsequent header lines (until the blank line) case-sensitively for "X-Secure-Checksum:" and trim whitespace from its value. Implement Adler-32 exactly as specified (mod 65521 after each byte, not just at the end) over only the URI string, and compare it as an integer to the parsed header value (missing header = treat as invalid, log "NONE"). On mismatch, write the literal 403 response and the DENY log line; on match, open a fresh TCP connection to 127.0.0.1:9090, forward the exact original request bytes unmodified, relay the backend's full response back to the client, then write the ALLOW log line — always fflush/close so log lines and sockets aren't left buffered. Compile with gcc, run it backgrounded, and test each case (no header, correct checksum, wrong checksum) with curl against 8080 before finishing, checking proxy.log matches after each request.