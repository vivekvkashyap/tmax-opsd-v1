Model this as classic dependency-resolution plus topological sort, and get the drop/cascade logic right before worrying about output formatting. First parse every `pkg_info.json` under `/home/user/packages` into a map of name -> (version, dependencies). Write a small version-comparison function that splits `MAJOR.MINOR.PATCH` into three ints and compares them numerically (not as strings) to correctly evaluate `==`, `>=`, `<=`, `>`, `<` against the installed version of each dependency. Determine which packages are "broken": a package is dropped if any dependency is missing entirely OR the available version fails the constraint — then propagate: repeatedly re-scan and drop any package that depends (directly or transitively) on an already-dropped package, iterating until a fixed point (a single pass may miss multi-level cascades). On the remaining valid packages, run Kahn's algorithm for topological sort: at each step, take all packages whose dependencies are already satisfied, sort that ready set alphabetically before appending (this is the tie-break the spec requires — don't rely on insertion order or a heap that isn't explicitly alphabetical), then remove them and repeat. Write the build order to `/home/user/build_order.txt` and the alphabetically-sorted dropped names to `/home/user/dropped.txt`.
