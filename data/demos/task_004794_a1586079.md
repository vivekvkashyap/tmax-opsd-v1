Build the bash pipeline as a sequence of discrete, independently-checkable steps rather than one big pipe. First count raw lines (`wc -l`), then run a `sed`/text substitution pass replacing the literal string `\uZZZZ` with `[REDACTED]` across the whole file, then feed each line through `jq -c .` (or `jq empty` to test, `jq -c .` to normalize) discarding lines that fail to parse — count survivors as "valid parsed lines". For masking, use `jq` itself to conditionally rewrite the `email` field only when it exists and is non-null, splitting on `@` and replacing the local part with exactly `***` (e.g. `(.email | if . != null then "***@" + (split("@")[1]) else . end)`), leaving other records untouched. For deduplication, group by `event_id` and within each group keep the record with the lexicographically smallest `timestamp` — sorting the jsonl by `event_id,timestamp` first and taking the first occurrence per `event_id` (e.g. with `jq -s`/`sort`/`awk '!seen[key]++'` after sorting) is a reliable approach. Write the final lines, count them, gzip a copy into `/home/user/archive/`, tally `event_type` counts into the sorted CSV, and write the three exact count lines to `pipeline.log`. Chmod +x the script and run it once to confirm every output file materializes with the right counts before finishing.