For each CSV separately: drop rows with missing/NaN `latency_ms` (`dropna`), then compute that dataset's own P99 of the *remaining* values with `.quantile(0.99)`, and filter to keep only `0 <= latency_ms <= P99` — the P99 threshold must be computed per-model after dropping NaNs but before any other filtering, not from a shared/global threshold, and outliers below 0 must also be excluded (not just the high tail). Use `scipy.stats.ttest_ind(a, b, equal_var=False)` for Welch's t-test (do not use the paired or equal-variance test). For the 95% CI on the mean difference, compute Welch-Satterthwaite degrees of freedom from each group's standard error (`scipy.stats.sem`) rather than assuming a fixed df or normal quantile, then use `stats.t.ppf(0.975, df)` as the critical value applied to the combined standard error of the difference. Round every reported value to exactly 4 decimal places and write the four required keys as plain JSON floats (cast numpy scalars with `float()` to avoid serialization errors). Actually run the script so `/home/user/report.json` is produced, and sanity-check the numbers are in a plausible range before finishing.