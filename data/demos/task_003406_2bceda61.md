First fix the vendored package: inspect `/app/vendored/bluemonday/go.mod` for a mismatch between the declared module name and the import path you intend to use — correct the `module` line so it reads exactly `github.com/microcosm-cc/bluemonday` (check carefully for stray characters). Then in `/home/user/pipeline`, run `go mod init pipeline`, `go mod edit -replace github.com/microcosm-cc/bluemonday=/app/vendored/bluemonday`, and `go get github.com/microcosm-cc/bluemonday` so `bluemonday.StrictPolicy()` is importable. Write `prepare_data.go` to accept `--corpus`, `--out`, `--metrics` flags, iterate `.txt` files, sanitize each with `StrictPolicy().Sanitize(...)`, and apply both filter checks (sanitized length < 10 bytes, OR contains the literal uppercase substring "REJECT_SPAM") before deciding whether to write the output file — reject on either condition, don't only check one. For accepted docs compute a 5-length vowel-count vector (case-insensitive a/e/i/o/u) as the mock embedding. Time the whole corpus pass with `time.Now()` before/after the loop and write `processed_count`, `accepted_count`, `duration_ms` as JSON to `--metrics`. Test against both `/home/user/corpora/clean/` and `/home/user/corpora/evil/` to confirm 100% correct accept/reject behavior before considering it done.