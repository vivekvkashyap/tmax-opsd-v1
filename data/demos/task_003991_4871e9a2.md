Recover the validation thresholds from the image first, and treat that as your source of truth rather than guessing numbers: run `tesseract /app/etl_rules.png stdout` and read off the exact max RetryCount and the CPU/Memory range bounds before writing any C code — print/inspect the OCR text yourself to confirm it parsed cleanly. In the C program, parse the CSV (skip header), and for duplicate detection build a hash set (or sort a copy of EventIDs) to catch repeats across the whole file — this can't be parallelized per-row so do it as a separate pass. For per-row rule validation, use `#pragma omp parallel for` over the row array, having each thread mark a shared invalid flag (or write per-row results into a preallocated array) if RetryCount/CPU_Usage/Memory_Usage fall outside the OCR'd bounds; avoid data races by not using shared mutable counters without atomics/reduction. Only after confirming zero duplicates and zero rule violations should you reshape wide-to-long (two output lines per input row, one for CPU_Usage and one for Memory_Usage) and print to stdout; on any failure, print nothing and exit non-zero. Compile with `-fopenmp` and self-test against a few crafted valid/invalid CSVs before trusting it on the hidden corpus.
