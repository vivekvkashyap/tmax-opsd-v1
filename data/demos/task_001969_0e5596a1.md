First transcribe the audio to recover the encoding method and magic string — run a speech-to-text tool (e.g. `whisper /app/architect_memo.wav`) and read the transcript carefully rather than assuming a standard format; the memo describes an ordered pair of transformations (a byte-level reversal step and an encoding step) plus the exact signature string files must start with after undoing them — extract both facts from the transcript, don't hardcode a guess. In `process_doc.sh`, first use `[ -f "$1" ] && [ ! -L "$1" ]`-style checks (or `test -f` combined with `readlink -f` bounded/careful handling) to safely reject symlinks/loops/directories without hanging — a naive `cat` on a self-referential symlink will error out predictably, but make sure you check file type before attempting any read. Reverse the transformation order the memo describes exactly (undoing must be the inverse sequence of the encoding steps, so figure out which operation was applied last and undo that first) using standard utilities like `rev`/`tac -rs .`/`base64 -d`, redirecting stderr so decode failures cleanly produce a non-zero exit rather than corrupting output. Check the decoded content's prefix against the magic string with `head -c` + string comparison. On success, write the output file and append its basename to `/home/user/master_index.txt` guarded by `flock -x` on a lock file to avoid races under parallel invocation; exit 0. On any failure at any stage, exit 1 without touching the output path or index. Validate against both corpora before finishing.