First reproduce the crash: run the vendored `checksum_calc` binary against small crafted single-record inputs (extracted from the WAL) to see which DATA_VALUEs cause a panic — you should find it's specific edge cases like zero or negative values, not all inputs. Separately, parse `ingest.wal` with a regex/awk pattern matching the valid record shape `[TIMESTAMP] (ID) DATA_VALUE` (numeric timestamp, numeric ID, numeric DATA_VALUE) and write only lines that match to `recovered_records.txt`, discarding malformed timestamps or non-numeric values. To reverse-engineer the checksum: run the binary on several distinct non-crashing DATA_VALUEs you control (e.g. 1, 2, 3, 10, 100), tabulate input→output pairs, and look for a simple arithmetic pattern (multiplication, modulo, additive offset) that fits all observed pairs — verify your hypothesis against more sample inputs before committing. Then implement that exact formula in pure Bash arithmetic (`$(( ... ))`, careful with bash's modulo semantics on the domain you're given), taking one integer argument and printing only the result, matching the binary's behavior on valid (non-crashing) inputs.
