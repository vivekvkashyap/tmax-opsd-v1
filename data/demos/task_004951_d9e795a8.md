Fix `check_version` by splitting both `actual` and `required` on `.`, converting each component to `int`, and comparing the resulting tuples numerically (`tuple(map(int, actual.split('.'))) >= tuple(map(int, required.split('.')))`) instead of the current naive string `>=`, which mis-orders values like "1.10.0" vs "1.2.0". Fix `get_build_order` by implementing a correct Kahn's-algorithm topological sort: the existing skeleton already builds `graph` (dep -> dependents) and `in_degree`, and seeds `queue` with zero-in-degree nodes, but is missing the decrement-and-requeue step inside the while loop — add `in_degree[neighbor] -= 1` before the `if in_degree[neighbor] == 0: queue.append(neighbor)` check (the comment in the skeleton literally flags this as the bug). Verify correctness by running the parser against `/home/user/polybuild/examples/project.poly` and confirming dependencies appear before dependents in the emitted order. Write tests in `test_parser.py` covering version comparisons and the topological sort, run `pytest` and redirect stdout to `/home/user/polybuild/test_results.log`, then run `python3 build_parser.py examples/project.poly > /home/user/polybuild/build_order.txt`.
