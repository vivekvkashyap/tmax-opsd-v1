Start by editing `/home/user/app_config.yaml` exactly as specified, then focus effort on the C++ validator since that is what the adversarial corpus tests. Model the problem as a classic deadlock detector: maintain a map from resource_id to its current holder (or none), and a FIFO wait-queue per resource for transactions blocked on it. Process the CSV strictly in row order. On ACQUIRE: if the resource is free, grant it immediately; if held, add a directed edge tx_id -> holder in an adjacency list (the wait-for graph) and enqueue tx_id on that resource's wait-queue, then re-run cycle detection (DFS/coloring, or incremental check) on the whole graph — if a cycle exists, print REJECT and exit(1) right away, don't keep processing. On RELEASE: remove the edge/ownership, then if the resource's wait-queue is non-empty, pop the earliest-queued transaction and grant it the resource, updating the graph accordingly. After the full file processes cleanly, print ACCEPT and exit(0). Compile with `g++ -std=c++17`, and test manually against a few hand-built cyclic and acyclic CSVs before trusting the corpus check. Also start the services via `/app/start_services.sh` and confirm ports 8000/8001 are bound.
