Start by diagnosing the actual linker error: `go build` calling into cgo will fail with "undefined reference to check_sig" — confirm the exact symbol name and calling convention expected by the cgo-generated C shim (System V AMD64 ABI: first argument in %rdi, return value in %eax/%rax). Write mock.S as a minimal `.text` section with `.globl check_sig` and a function that loads 1 into %eax and executes `ret` — no prologue/epilogue needed for a leaf function this simple. Verify it links by running `go build` before writing tests. For the test suite, use `httptest.NewRecorder()` + `httptest.NewRequest()` per goroutine (not real HTTP sockets) inside a `sync.WaitGroup` or a buffered `chan int` sized 100, spawn exactly 100 goroutines each invoking `AuthHandler` directly, collect each recorder's `.Code` through the channel, then tally how many equal `http.StatusOK` after joining all goroutines. Write the final integer count as plain text (no trailing formatting surprises) to result.txt, and run `go test` to confirm the file is produced as a side effect of the test.