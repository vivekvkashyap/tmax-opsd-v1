Work through this as a linear pipeline, validating each intermediate artifact before moving to the next. Verify the zip's integrity (`unzip -t financial_records.zip` or `zip -T`), then extract into `/home/user/processing/` (`unzip -o financial_records.zip -d /home/user/processing/`). For each `.csv` found there, parse it (e.g. Python's `csv.DictReader`, which naturally keeps values as strings matching the expected output) and write a JSON array of row objects to a same-named `.json` file — keep string types exactly as read, do not cast numbers, since the expected output keeps `"amount": "100.50"` as a string. Compute SHA-256 of each newly written `.json` file's bytes (`sha256sum` or `hashlib.sha256(open(f,'rb').read())`) and write `manifest.sha256` with lines formatted as `HASH  filename.json` (exactly two spaces, matching `sha256sum`'s own output format, which you can generate directly with `sha256sum *.json > manifest.sha256` run from inside the processing directory). Finally tar+gzip only the `.json` files and the manifest at the archive root — run `tar czf .../financial_records_converted.tar.gz -C /home/user/processing file1.json file2.json file3.json manifest.sha256` (using `-C` to avoid embedding the `processing/` directory prefix in archive member names) rather than tarring the whole directory.