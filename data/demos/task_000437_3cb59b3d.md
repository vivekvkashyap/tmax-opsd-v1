Scaffold with `cargo new log_sanitizer`, add `serde_json` and `rayon` (and `chrono` for timestamp validation) to Cargo.toml, and parse args for `--input`/`--output`. Parse each JSONL line into a `serde_json::Value` and write a recursive validator that walks the whole tree (objects, arrays, and nested objects) checking every string leaf, case-insensitively, for `<script>` or `DROP TABLE` — a validator that only checks top-level string fields will miss nested injections. Separately check top-level `status_code` (must be an integer 100-599 if present) and `timestamp` (must parse as strict ISO-8601, e.g. via `chrono::DateTime::parse_from_rfc3339`, if present). Use `rayon`'s `par_iter` to validate lines in parallel, then filter and write survivors in original order. Build with `cargo build --release` so the binary lands at `target/release/log_sanitizer`. Test directly against `/app/corpus/clean/clean_logs.jsonl` (expect 100% survival) and `/app/corpus/evil/evil_logs.jsonl` (expect 0% survival) before wiring up the FIFO wrapper. For streaming through the FIFOs, read/write line-by-line and flush per line (don't buffer whole-file) so the bridge doesn't stall waiting for EOF on a pipe that stays open.
