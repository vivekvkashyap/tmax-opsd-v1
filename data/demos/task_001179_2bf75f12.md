Build and test the CSV parser in isolation first since it's the trickiest part. Write a state-machine reader (don't use a naive line-by-line split): read the file character by character or line by line while tracking whether you're inside an open double-quote; only treat a newline as a record separator when you're not inside quotes, and when inside quotes accumulate the raw newline into the field content (with no escaped-quote handling needed per the spec). Once a row's 6 raw fields are recovered, parse ts as int and cpu/mem/disk as double, and expand each row into three long-format records (order cpu, mem, disk) sharing ts/hostname/event_log. For dedup, compute `std::hash<std::string>{}(key)` where key is `ts_str + "_" + hostname + "_" + metric_name`, and keep a `std::unordered_set<size_t>` of seen hashes, skipping any record whose hash was already seen (keep first occurrence, preserving input order). For changepoint detection, maintain a `map<pair<hostname,metric_name>, double>` of the last-seen value processed so far (only over records that survived dedup, in original order) and flag `current > 3.0 * previous` (strict), skipping the very first occurrence of each hostname+metric pair. Write all deduped long records to processed.jsonl and only the flagged ones to anomalies.jsonl, one compact JSON object per line via nlohmann::json.
