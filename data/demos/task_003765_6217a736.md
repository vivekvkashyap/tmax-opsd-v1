Start by running `tesseract /app/rules.png -` (or convert to png first if needed) to OCR the sanitization rules — do not guess the rules, read them exactly as printed since edge cases (which substring triggers rejection, which characters get replaced, exact prefixes like "ACCEPTED:"/"REJECTED:"/"END ACCEPTED") must match a hidden oracle byte-for-byte. Then write the bash script as a line-by-line state machine using `while IFS= read -r line || [ -n "$line" ]`: track an `in_file` flag and a `reject` flag set when parsing the `BEGIN FILE: <path>` line. Apply the rejection check first (using the rule you OCR'd) before any sanitization, since rejected files must skip content entirely including the END marker. For sanitization, use bash parameter expansion (`${var//pattern/repl}`) to replace forbidden path characters, and pipe content lines through `tr` for case conversion per the rule. Ignore lines outside BEGIN/END blocks and any junk lines. Make the script executable with `chmod +x`. Since it will be fuzz-tested against a reference implementation across many random inputs, test manually with edge cases: paths containing traversal sequences, backslashes, and empty content blocks, before finalizing.