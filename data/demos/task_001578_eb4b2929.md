Do not trust `user_id`-only joins — the task explicitly flags that as the bug. First, run OCR (`tesseract /app/schema.png stdout`) or view the image to extract the diagram's stated join condition; expect it to specify joining `orders` to `sessions` on matching `user_id` PLUS a temporal containment condition (the order's timestamp falling within the session's start/end window) — inspect the OCR output carefully rather than assuming standard equi-join semantics. Second, inspect the SQLite schema/data directly with `sqlite3 /app/data.db ".schema"` and sample rows to understand column names and types before writing SQL. Third, write the C++ program using the SQLite C API (`sqlite3_open`, `sqlite3_prepare_v2`, `sqlite3_step`) executing a query that joins sessions to orders using both the user_id equality and the time-range condition (order_time between session start and end), grouping by session_id and summing amount — decide whether to include sessions with no matching orders based on what the golden aggregation would look like (an inner-style join is typically expected for "total order amount per session" when the reference only lists sessions that have orders). Format amounts with exactly 2 decimals in the CSV, compile with `g++ -o /home/user/etl_bin /home/user/etl.cpp -lsqlite3`, run it, and inspect the resulting CSV for correctness before finishing.
