Fix the three problems as separable root causes rather than patching symptoms. Run the script first to capture the actual traceback — it will point at the `rolling_vars[i]` write when `i` reaches `n`, since the loop runs `i` from `1` to `n` inclusive but the list only has indices `0..n-1`; fix by either sizing the list to `n+1` or shifting the store index to `i-1` (align with how the value is actually used/read afterward). For the performance bottleneck, don't just tolerate it — trace with `strace -c` to confirm the syscall storm comes from opening and appending to `progress.log` inside the per-element loop; move the `open()` outside the loop (open once, write buffered lines, close at the end) so it becomes O(1) opens instead of O(n). For the precision issue, replace the naive `sum_sq - n*mean^2` formula (which cancels two large nearly-equal numbers) with Welford's online algorithm, updating `mean` and `M2` incrementally per element and deriving the final sample variance as `M2/(n-1)`; this is required, not optional, because the naive formula's error scales with the data's absolute magnitude. Write the final variance to `result.txt` at 6 decimal places.
