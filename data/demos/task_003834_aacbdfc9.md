Fix this script bug-by-bug and re-run after each change rather than rewriting everything at once. First fix the SQL/fetch layer: change the query (or filter after fetch) to require `latency > 0 AND status = 'OK'`, and confirm with a quick manual SELECT what rows remain — this is what removes the NULLs, negative sensor-reboot values, and non-OK rows. Second, replace the naive `(sum(x^2) - sum(x)^2/N)/N` variance formula, which suffers catastrophic cancellation on large-magnitude/low-spread data, with Python's `statistics.pvariance` (or `statistics.variance`, matching whether the script intends population vs sample variance) or a manual Welford one-pass accumulator — verify by hand-checking the variance isn't negative or absurd before moving on. Third, fix the convergence loop: it needs a proper stopping condition on `abs(delta) < 1e-7` (not an exact equality check) and must not call `sqrt` on a negative or crash on variance==0 — guard with `max(variance, 0)` or an early-return path when variance is zero. Run the corrected script and write its exact printed float to the output file; don't hardcode a value, since the pipeline must actually recompute it from the database contents.