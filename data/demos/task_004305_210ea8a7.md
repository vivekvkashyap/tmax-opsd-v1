Write a binary format parser in Go carefully following the spec byte-by-byte: read and validate the 4-byte magic `CBA1`, then loop reading records until EOF — path length (uint16 LE), path bytes, operation byte, and (only if operation is 0x00) a uint32 LE compressed size followed by that many zlib-compressed bytes (use `compress/zlib` for decompression). Apply the three `.cba` files in the given chronological order against the same output directory, so later records for the same path overwrite/delete earlier state — process them strictly in sequence, not in parallel or out of order. For zip-slip protection, resolve each record's target path against the output directory (e.g. `filepath.Join` then `filepath.Clean`, or check for `..` path segments and leading `/`) and reject anything that would escape the root *before* writing or deleting — on rejection, append the literal path string exactly as it appeared in the archive to `/home/user/malicious_paths.log`, one per line, and skip that record entirely (do not extract it). After processing all three archives, walk the final output directory recursively, compute SHA-256 of each remaining file's contents, and write `/home/user/manifest.txt` with lines `relative/path  hexhash` (two spaces) sorted alphabetically by path — verify deleted files are truly absent and overwritten files reflect only the latest content.