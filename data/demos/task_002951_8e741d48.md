Inspect archive contents before touching the filesystem — never call `extractall()` blindly. For each `.zip` in `/home/user/docs_incoming/`, open it with `zipfile.ZipFile` and iterate `namelist()` (or `infolist()`), checking each entry's path for the two malicious patterns: starts with `/` or `\`, or contains `../` or `..\` anywhere (use `os.path` normalization carefully, or simple substring checks, rather than trusting the OS to sanitize on extraction). Collect the base filenames of any archive that trips either check into a sorted list and write them one per line to `/home/user/malicious_zips.log`. For archives that pass, extract member-by-member into `/home/user/docs_safe/` (creating parent dirs as needed via `os.makedirs`) — since you've already validated paths, `extractall()` is now safe to use, but you could also extract member-by-member for defense in depth. Finally, walk `/home/user/docs_safe/` and build `/home/user/safe_docs_bundle.zip` with `zipfile.ZipFile(..., 'w')`, writing each file with an *arcname* relative to `docs_safe` (not including that folder name) so files land at the archive root/subpaths correctly. Verify by listing both output locations.
