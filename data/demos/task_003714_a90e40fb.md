Sequence the work as extract, then process, then archive, verifying each stage independently. Extract with `tar -xzf incoming_logs.tar.gz -C /home/user/raw_logs` (create the dir first). In the Go program, walk the tree with `filepath.WalkDir`, collect all `.txt` paths, then spawn a goroutine per file (or a worker pool) coordinated by a `sync.WaitGroup`. Per file: read content, use a compiled regexp for IPv4 addresses (four dot-separated 0-255 octets — a simple `\d{1,3}(\.\d{1,3}){3}` is close enough here) to count and replace matches with `[REDACTED]`, read the first line to extract `ProjectName` from `Project: <name>`, then `os.MkdirAll` the destination project folder and write the sanitized file there. The critical shared-resource bug to avoid is concurrent writes corrupting `redaction_index.log`: open it with `O_APPEND|O_CREATE|O_WRONLY`, and around each write acquire an exclusive lock with `syscall.Flock(fd, syscall.LOCK_EX)` and release with `LOCK_UN` after — a `sync.Mutex` alone is insufficient if you ever have multiple processes, but here it also demonstrates OS-level locking as required. After all goroutines join, verify the index has one line per file with correct redaction counts, then `tar -czf processed_logs.tar.gz -C /home/user organized_logs`.
