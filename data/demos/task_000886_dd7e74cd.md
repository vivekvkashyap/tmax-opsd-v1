Break this into three independently checkable stages inside one C program. (1) Alignment: slide the primer over the target at every valid offset, scoring +1/-1 per position, and track the maximum — this is a simple nested loop, no external libs needed; print/log the intermediate score while developing to confirm it's sane. (2) ODE integration: implement classic RK4 generically (a function taking the current state, dt, and the derivative dC/dt = kon*(P0-C)*(T0-C) - koff*C) so you can reuse it for every dt candidate without duplicating logic — derive kon from the alignment score you computed in step 1, don't hardcode it. (3) Stability sweep: iterate over the given dt list from t=0 to t=0.01, and after every single RK4 step check that C stays within [0,1] and is not NaN (use `isnan` from math.h); the first violation at a given dt marks it unstable — track the largest dt that never violates across the whole trajectory. Be careful that even a single mid-step blow-up (not just the final value) must be checked, since a large dt can bounce out of bounds and still numerically return by t=0.01. Finally write the JSON report with a plain fprintf, rounding the final concentration to 4 decimal places, and verify by cat-ing the file.