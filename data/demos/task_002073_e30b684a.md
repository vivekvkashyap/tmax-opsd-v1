Resolve the build first, in isolation from behavior work. Run `cargo build` in the workspace and read the circular-dependency error precisely — it will point at the two crates each depending on the other's path in their `Cargo.toml`s. The clean fix is to extract whatever shared type each side pulls from the other (an error struct used by one, a string wrapper used by the other) into a brand-new leaf crate (e.g. `core_types`) that both `semver_parser` and `text_encoder` depend on one-directionally, breaking the cycle without collapsing the two crates together. Get `cargo build --release` clean before touching CLI logic. For behavior, treat `/app/oracle` as ground truth you must match bit-for-bit, not a spec to reinterpret — feed it varied inputs (valid/invalid semvers, edge-case strings, empty text) yourself and record exact outputs before implementing the same in Rust: parse `<semver> <text>`, drop lines whose semver doesn't strictly match Major.Minor.Patch, RLE-encode the text (count runs of identical chars, appending the count even when it's 1), emit `<encoded>\t<semver>`, and sort by semver descending with alphabetical tie-break on encoded text. Diff your binary's output against the oracle's on the same inputs before trusting it. Finally copy the release binary to `/home/user/bin/log-processor` and write the CI workflow YAML with build+test steps.
