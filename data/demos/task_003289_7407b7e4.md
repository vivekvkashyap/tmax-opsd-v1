Start by recovering the hidden config: transcribe `/app/transmission.wav` with a speech-to-text tool (install `whisper`/`whisper-cpp` or similar) rather than guessing — the audio states both the port and the security token; use exactly those recovered values in your server. For the `.artf` parser in C: read bytes 0-3 against the literal `ARTF` magic, read bytes 4-7 as a little-endian `uint32_t` (do explicit shift-and-OR, don't rely on a raw pointer cast), then read exactly N bytes from offset 8 and write them to a temp `.tar.gz`. Shell out to `tar -xzf` to extract (simpler/safer than hand-rolling gzip parsing in C), recursively walk the result, and shell out to `unzip` for any nested `.zip`. For each extracted file, compare its first 4 bytes to the ELF magic `\x7F E L F` to collect valid executable basenames. Implement the HTTP layer (raw sockets or libmicrohttpd) returning 401/400/200 with a JSON array body per the spec, and leave the compiled server running in the background on the recovered port.