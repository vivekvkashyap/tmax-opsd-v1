Work through this as a chain of small, verifiable fixes rather than one big rewrite. First, edit `validator.py` and `storage.py` to bind their HTTP servers to `127.0.0.1` instead of `0.0.0.0`, leaving the ingester on `0.0.0.0:5000`. Second, grep `/app/logs/security.log` for the line with `EventID=4421` and `Status=AuditMode`, pull out the `AuditKey` value, and base64-decode it yourself (e.g. `python3 -c "import base64; print(base64.b64decode('...'))"`) to get the plaintext seed — don't guess it. Third, open `/app/legacy_hasher.py` and trace it by hand (or run it with sample inputs) to recover the exact token formula it implements (seed + payload combined and hashed); replicate that same logic in `ingester.py` rather than inventing a new scheme. Fourth, wire the ingester to compute `X-Audit-Token` from the decoded seed and the outgoing payload, add it as a header, and forward to the validator on `127.0.0.1:5001`. Finally run `/app/startup.sh &`, then curl `POST /ingest` on port 5000 yourself to confirm the full chain returns 200 before declaring done.