Build the pipeline in clear sequential stages inside `/home/user/process_logs.sh`, testing each stage's intermediate output before chaining further. First, normalize both line formats into a single delimiter-consistent stream: use `jq` (or a simple detector like "does the line start with `{`") to pull `timestamp,service,status` from JSON lines, and plain field-splitting (`awk -F,`) for CSV lines, emitting a uniform `timestamp,service,status` stream. Second, apply the quality gate by filtering with a regex that requires the status field to be exactly three digits (e.g. `^[0-9]{3}$` — this correctly rejects "20", "ERROR", "None", etc.). Third, sort the filtered stream with `sort -t, -k2,2 -k1,1` (service alphabetically, then timestamp chronologically — ISO timestamps sort correctly as strings). Fourth, compute the rolling average per service over a window of up to the last 3 valid requests for that service: process the sorted stream sequentially (e.g. in `awk`), maintaining a per-service queue/array of the last up-to-3 status codes, and on each row output the floor of their average using integer division. Write the header `timestamp,service,status,rolling_avg` followed by the data to `/home/user/processed_logs.csv`, make the script executable (`chmod +x`), and run it to produce the file. Verify a couple of rolling-average values by hand against the raw log to catch off-by-one windowing errors.