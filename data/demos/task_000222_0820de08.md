Use `jq` to do the JSON-to-tabular translation so your C code only has to deal with plain delimited fields. Write a `jq` filter that flattens each log entry from `/home/user/logs.json` into a simple line (e.g. `timestamp,user_id,resource_id,action`) and pipe it into your compiled `audit` program's stdin, or invoke jq from within the C program via `popen`. In `audit.c`, open `/home/user/audit.db` with `sqlite3_open`, and for each incoming log line run a prepared JOIN query across `users -> departments -> permissions` (matching on `dept_id` and `resource_id`) using `LEFT JOIN` for permissions so a missing permission row naturally yields NULL, which you must then also treat as "no access" (NULL is not 1) — this is the easy-to-miss edge case, since a naive `can_read = 0` check misses rows where there's no permission record at all. For each line, decide READ vs WRITE which permission column to check, and if it evaluates to a violation, append a CSV row. Preserve the log's original order by processing sequentially rather than re-sorting. Write the header line first, then compile with `gcc -o audit audit.c -lsqlite3`, run it, and diff your `/home/user/violations.csv` against a hand-worked-out expectation from the DB and log to sanity check the join logic.