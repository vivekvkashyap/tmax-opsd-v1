This is a race-condition-aware file-watcher problem: only ever act on files ending in `.md` (never `.tmp`), and since the generator writes to `.tmp` then renames to `.md`, a simple `os.listdir` + immediate open is actually safe here because rename is atomic — the danger is instead re-processing a file mid-move or missing files due to polling too infrequently, so poll in a tight loop (e.g. every 0.05-0.1s) and immediately move/rename each detected `.md` file into a "claimed" state (or just read-then-delete promptly) before another pass can double-process it. For each file: read text, do a literal string replace of `INTERNAL_CONFIDENTIAL` -> `[REDACTED]`, compute `hashlib.sha256(text.encode()).hexdigest()`, compress with `zlib.compress(text.encode())` (default level), track a running byte offset as you append each compressed payload to `/home/user/archive/docs.archive` (open in append-binary, or better, open once and keep the file handle/offset in memory to avoid reopening overhead), write the manifest CSV row before deleting the source `.md` file. Start the generator in the background (`bash /home/user/generator.sh &`) if not already running, then run your processor until you've handled all 50 files and the incoming directory is empty of `.md` files, then exit — don't leave the watcher loop running forever.