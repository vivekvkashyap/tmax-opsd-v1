Set up a real Cargo project (`cargo new /home/user/redact_tool`) and add the `hound` crate for WAV I/O rather than hand-parsing RIFF headers. First parse `/home/user/audit.log` with a regex extracting the floating-point `start` and `duration` values from lines matching `[CRITICAL] Leakage detected - start: X s, duration: Ys` — ignore INFO/WARN lines. For each interval convert seconds to sample indices with `(start * sample_rate).round() as usize` and an end index of `((start+duration) * sample_rate).round()`, being consistent about rounding vs. truncation since off-by-a-sample errors will fail the near-exact accuracy threshold. Read all samples from `token.wav` with `hound::WavReader`, zero out every sample whose index falls within any leak interval (half-open range, index < end), leave all others untouched, and write the result with `hound::WavWriter` using the same spec (sample rate, bit depth, channels) as the source file. After writing, `std::fs::set_permissions` (or `Command::new("chmod").arg("600")`) on `/home/user/redacted.wav` to exactly mode 0o600. Sanity-check by writing a small Python script to compare a few known leak/non-leak sample indices before considering it done.
