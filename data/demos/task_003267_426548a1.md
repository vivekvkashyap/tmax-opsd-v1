Parse the log with a simple state machine or regex that captures each `=== RECORD START === ... === RECORD END ===` block as a dict of its fields, rather than trying to regex the whole file at once — this avoids record-boundary mistakes when multiple records are present. Filter to `Status == "PUBLISHED"` exactly (case-sensitive, exact match, not "contains"). For directory naming, lowercase the Title and replace spaces with underscores exactly as specified — watch for any other punctuation that might need preserving as-is (don't over-sanitize beyond what's asked). Copy both the TextFile and AssetFile referenced in each published record from `raw_assets/` into the new directory, preserving binary content exactly (open `.bin` files in binary mode, don't accidentally treat them as text and corrupt bytes/newlines). For the manifest, walk `/home/user/clean_docs/` recursively, compute SHA-256 per file (skip the manifest file itself, and be careful about the file-walk order — sort the final list explicitly by relative path rather than relying on OS directory order), and format each line as `<hex_digest>  <relative_path>` with exactly two spaces, one line per file, terminated normally. Double check paths in the manifest are relative to `clean_docs/`, not absolute or relative to cwd.