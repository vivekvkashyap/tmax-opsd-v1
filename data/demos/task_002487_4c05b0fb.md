Build the wrapper in the exact order the spec implies: (1) validate argc == 2; (2) check path prefixes with a strict `strncmp` against `/app/data/in/` and `/app/data/out/` — print `POLICY_VIOLATION` to stderr and exit 1 on violation before touching the filesystem; (3) compute SHA-256 of the input file (use OpenSSL's EVP API, reading in chunks, not the whole file into memory) and compare it byte-for-byte (as lowercase hex) against the contents of `<input_file>.sha256`, printing `INTEGRITY_ERROR` and exiting 2 on any mismatch or missing sidecar; (4) only after both checks pass, `fork()`, and in the child call `prctl(PR_SET_NO_NEW_PRIVS, 1, 0, 0, 0)` then install a seccomp BPF filter (libseccomp's `seccomp_rule_add` with `SCMP_ACT_KILL` or `SCMP_ACT_ERRNO`) that denies `socket`, `connect`, `bind`, `listen`, `accept`, `clone` before calling `execve` on `/app/bin/data_parser`; the parent waits and forwards the child's exit status. Test incrementally with each of the four failure modes (bad input path, bad output path, corrupted hash, and a mock parser that opens a socket) before relying on the full suite. Compile exactly as specified with `-lseccomp -lcrypto`.
