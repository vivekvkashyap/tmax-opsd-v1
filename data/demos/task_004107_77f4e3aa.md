Work bottom-up through the pipeline and validate each layer in isolation before wiring them together. First, get `solver.cpp` correct and safe: read it carefully for any fixed-size stack/heap buffers indexed by a runtime job count (a classic bug is `int in_degree[100]` written for `jobs > 100`) and any array read before every element is initialized (e.g., a `visited[]` array only set inside a conditional branch of the traversal) — fix both by sizing buffers dynamically (`new`/`vector`) to the actual job count and by zero-initializing all tracking arrays before use, without touching the topological-sort algorithm or wire protocol. Compile with the Makefile (`g++ -std=c++17 -pthread solver.cpp -o solver`) and test it directly with a small raw TCP payload (e.g., `python3` one-liner using `socket` and `struct.pack('<II', ...)`) before touching Python. In `gateway.py`, use `struct.pack('<II', jobs, len(deps))` plus packed dependency pairs, open a `socket.socket(AF_INET, SOCK_STREAM)` to 127.0.0.1:8081, `send()` then read until you have the full expected response length (`recv` may return partial data — loop). Hash with `hashlib.sha256(canonical_json.encode()).hexdigest()` for the Redis key, checking cache before contacting the solver. Start all three processes (`redis-server`, `./solver`, `python3 gateway.py`) in the background and confirm each port is listening before declaring done.