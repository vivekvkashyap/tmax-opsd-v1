Treat this as three independently-verifiable components wired together at the end. First, OCR `/app/config_scan.png` with `tesseract` (the image may be blurred, so print the raw OCR text and sanity-check it rather than trusting it blindly — retry with image preprocessing like thresholding/upscaling via Pillow if the text looks garbled) to recover the exact API token string and the alpha significance level. Second, write an R script that reads `/app/sensor_data.csv`, and for two given column names computes `cov(x,y)`, and uses `cor.test(x, y, conf.level = 1 - alpha)` to get both the correlation estimate and its confidence interval bounds (`$estimate` and `$conf.int`) — make the columns and alpha parameterizable (e.g. via command-line args passed from Python) rather than hardcoded once, since the endpoint must work for arbitrary var1/var2 query params. Third, in Python (Flask/FastAPI), implement `GET /api/v1/stats` bound to `127.0.0.1:9090`: check the `Authorization: Bearer <token>` header against the OCR'd token exactly, returning 401 on mismatch/missing; on success, shell out to your R script (e.g. `Rscript stats.R <csv> <var1> <var2> <alpha>`) or use `rpy2`, parse its output, round all four values to 4 decimal places, and return them as JSON with the exact keys `covariance`, `correlation`, `ci_lower`, `ci_upper`. Start the server backgrounded and confirm with `curl` that port 9090 responds before finishing.