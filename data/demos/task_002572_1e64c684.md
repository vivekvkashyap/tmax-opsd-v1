Work in dependency order: first harden the server's expression evaluation, then bound its memory, then write a client that exercises it end-to-end. Replace `eval()` with an `ast`-based evaluator: parse the expression with `ast.parse(expr, mode='eval')`, walk the resulting tree, and only allow `ast.Expression`, `ast.BinOp` with `Add`/`Sub`/`Mult`/`Div`, and numeric constants — reject (return "ERROR") anything else such as `Call`, `Name`, `Attribute`, etc., which is what neutralizes the `__import__(...)` injection attempt in the test sequence. For the memory leak, cap the global `history` list at exactly 10 items — e.g. append then trim from the front, or use `collections.deque(maxlen=10)`, and grep your own file afterward to confirm no unbounded list growth remains. Keep the existing "SHUTDOWN" -> stop event loop behavior intact. The test client must be a real WebSocket client (the `websockets` library) that sends each expression sequentially, awaiting each response before sending the next, collects the five responses (the last one is "ERROR" because it's a malicious non-arithmetic expression your parser now rejects) into a JSON array file, then sends "SHUTDOWN" and closes cleanly. Launch the server first under `mprof run --output ... python math_api.py` in the background, confirm it's listening, then run the client — check both output files exist and are well-formed before finishing.