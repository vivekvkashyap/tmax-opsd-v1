Follow the spec's exact order of operations since sklearn/numpy pipelines are sensitive to it. First load the CSV and apply the three range filters (`temp` in [-50,50], `humidity` in [0,100], `pressure` > 0) plus drop NaN rows — do the NaN drop and range filters together before any modeling, and use inclusive bounds exactly as stated. Set `np.random.seed(42)` before fitting (even though `RidgeCV` with default solver is largely deterministic, seeding as instructed is part of the spec and keeps results reproducible if any randomized solver path is used). Fit `sklearn.linear_model.RidgeCV(alphas=[0.01,0.1,1.0,10.0,100.0], cv=5)` on the filtered features (`temp`,`pressure`,`humidity`) and target (`error_margin`) — use the fitted model's `.alpha_` for `best_alpha` and compute R² via `r2_score` (or `.score()`) on that same filtered dataset (not a held-out split, per the spec). Round the R² to exactly 4 decimals before writing. Double check your filtering logic against a couple of rows by hand (e.g. the row with `temp=100.0` and the row with `humidity=-10.0` should be dropped) to catch off-by-one or wrong-direction comparisons before trusting the final metrics.