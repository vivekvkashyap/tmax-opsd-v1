Transcribe `/app/audio/memo.wav` with an offline STT tool (install `openai-whisper` or `whisper.cpp`) and read the resulting text carefully to identify the exact pipeline: scaling method, dimensionality-reduction algorithm, number of components, and random seed — do not guess these, extract them from what was actually said. Reproduce the canonical pipeline in `/home/user/filter_artifacts.py` using scikit-learn: fit the scaler and reducer on the full `/app/data/raw_features.csv` (order matters — scale first, then reduce, using defaults for anything unspecified) to get reference embeddings once at startup or on each run. The script should load the `--input` CSV (headerless numeric matrix), check its shape matches the reference, then compare element-wise against the canonical embeddings with `numpy.allclose(..., atol=1e-5)` (or an equivalent per-element tolerance check) — print exactly `ACCEPT`/exit 0 on match, `REJECT`/exit 1 otherwise, wrapping shape mismatches or read errors in a REJECT path too. Test your script against a range of synthetic "good" (tiny-noise) and "bad" (wrong seed, wrong scaling, wrong component count, large noise) artifacts you construct yourself to confirm the tolerance and comparison logic correctly separates near-identical from meaningfully-different embeddings before finalizing.