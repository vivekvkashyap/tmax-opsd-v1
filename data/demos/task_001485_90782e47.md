Fix the Makefile first: a shared library's object files must be compiled position-independent, so add `-fPIC` to the `parser.o` compile rule (and typically also acceptable on the link line), then confirm `make` in `/app/libparser` produces `libparser.so` cleanly. Recover the magic header value from `/app/protocol_spec.png` yourself via OCR (`tesseract`) or by viewing the image — don't guess it, read it off the diagram exactly as rendered. Write `validator.c` to: open the given file, read its bytes, compare the leading bytes against the recovered magic string, and only if that matches, call `parse_data(buffer + header_len, remaining_len)` from `libparser.so` (declare it via the existing header) and check its return is 0. Exit 0 only when both checks pass; exit nonzero for missing files, wrong magic, or a failing/short payload. Compile with `gcc validator.c -L/app/libparser -lparser -Wl,-rpath,/app/libparser -o /app/validator` (or set `LD_LIBRARY_PATH`) so the dynamic linker can find `libparser.so` at runtime — verify with `ldd /app/validator`. Before finishing, manually loop your validator over every file in `/app/corpus/clean/` and `/app/corpus/evil/`, checking exit codes with `echo $?`, to confirm 100% pass/reject before considering it done.
