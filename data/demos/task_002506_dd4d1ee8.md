First OCR `/app/data_spec.png` with `tesseract` to recover CHARSET, RECORD_SIZE, and THRESHOLD — if the raw OCR text is noisy, crop/threshold the image with ImageMagick first and re-run, and sanity check the parsed values look like a real charset name and small integers before hardcoding them as Go constants. Then write the Go program to read STDIN in fixed-size chunks of RECORD_SIZE bytes using `io.ReadFull`, discarding any trailing short read (do not error out — just stop). Decode each record from the OCR'd charset to UTF-8: for single-byte legacy charsets like ISO-8859-1/Windows-1252, use `golang.org/x/text/encoding/charmap` (or manually map each byte if you avoid extra deps) rather than assuming the bytes are already UTF-8. Extract ID as the first 8 decoded characters and Value as characters 8-15 trimmed and parsed as an integer, skipping records that fail to parse. Track seen IDs in a map for deduplication, and compare each new valid Value against the previously accepted Value (not the previous raw record) using strict `>` on absolute difference for anomaly detection, printing the anomaly message and exiting immediately when triggered. Compile with `go build -o /home/user/processor processor.go` and test against hand-built duplicate/anomaly byte streams before trusting it against the fuzzer.