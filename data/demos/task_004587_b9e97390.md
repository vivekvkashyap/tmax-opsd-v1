First recover the hidden rules by transcribing the audio — use a local speech-to-text tool (e.g. `whisper /app/pager_recording.wav`) or, if unavailable, install one, then read the transcript carefully to extract the exact rounding precision and the validity rule (a "must be a multiple of some fractional step" condition) it describes; do not guess these numbers from the task description alone, they live in the audio. Once you have the rule, implement it precisely: load `tz_offset_hours` from the JSON argument file, round it to the stated number of decimal places using Python's `round()`, then check divisibility against the stated step by scaling to avoid float comparison bugs (e.g. multiply by a power of 10 so the step becomes an integer and use `%`), rather than comparing floats directly with `==` or a naive `% 0.25 == 0` (which fails due to floating-point representation error). Exit 0 for values satisfying the rule, exit 1 otherwise. Before finalizing, hand-test your script against a few clean-looking and adversarial-looking offset values (values very close to a valid multiple due to float noise, and values just outside one) to confirm the rounding-then-modulo order matches what the dictation specified, since doing modulo before rounding could reject values that should pass.
