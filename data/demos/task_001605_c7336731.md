Fix the pieces bottom-up since each layer depends on the one below compiling correctly. In `compute.c`, the bug is a loop boundary excluding N itself when N is prime (e.g. `for (i = 2; i < n; i++)` should be `i <= n`) — read the existing sieve/trial-division logic carefully and correct only the boundary, not the algorithm. The Makefile is broken (classic Make gotcha: recipe lines must be indented with a literal tab, not spaces, and check the target output name matches what's expected: `compute`, not something else) — rewrite it with a real tab before the `gcc` command. In `main.go`, replace the TODO with a goroutine-per-number pattern: launch one goroutine per input int that runs `exec.Command(computeBinaryPath, strconv.Itoa(n))`, captures stdout, trims/parses it to an int, and sends a `{key, value}` struct on a channel; a wait group or a receive loop sized to `len(numbers)` collects results into the map before marshaling to JSON — guard the shared map with a mutex or only write to it from the single collecting goroutine to avoid data races. For `build_and_test.py`, shell out to `make` and `go build` via `subprocess.run(..., cwd=...)`, check return codes, then pipe `[10,20,30]` as JSON into the worker's stdin via `subprocess.run(..., input=...)`, parse its JSON stdout, and compare each sum against your own independently-verified correct prime-sum computation before writing the three booleans to `test_results.json`.