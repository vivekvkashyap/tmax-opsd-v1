Debug this vendored service by reading engine.py carefully before touching anything else. The get_neighbors query is an implicit cross join because it selects from two tables (edges and nodes) with a WHERE clause that only filters on nodes.id, so every edge row gets paired with the matching node — replace it with a query that selects target_id from edges alone filtered on source_id = ?, since neighbors only ever need the edges table. Separately, during database initialization after creating the edges table, add `CREATE INDEX idx_edges_source ON edges(source_id)` — without it, BFS shortest_path does a full table scan per node and will time out on a 1000-node graph. After fixing both issues, start the server with the exact given command line (port 8080, the /data CSVs) and leave it running in the background (e.g., `nohup ... &` or a background job), then sanity-check yourself with `curl 'http://localhost:8080/shortest_path?src=1&dst=50'` and confirm the response returns a short well-formed path quickly — if it hangs or returns near-all nodes as neighbors, one of the two fixes is still wrong.
