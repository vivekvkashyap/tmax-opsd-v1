Build analyze.py in clear stages and validate each before moving to the next. Load the JSON with pandas, then for each distinct gene_id `sort_values('time_hours')` to get a clean chronological series (the raw records are shuffled, so grouping+sorting is required, not optional). For each gene's series, run `numpy.fft.rfft` on the (roughly evenly-spaced) expression values, find the index of the largest-magnitude non-DC frequency component, convert it to a period via `period = N * dt / peak_index` (using the sampling interval and total span), and use that as your initial guess for T in curve_fit — a poor initial guess is the most common way this optimization fails to converge to the true global minimum. Define `y(t) = A*cos(2*pi/T*t + phi) + B` as the model function passed to `scipy.optimize.curve_fit` with `p0=[A_guess, T_guess, phi_guess, B_guess]` (a reasonable A guess is half the data's range, B guess its mean). After fitting, enforce A>0 by flipping sign and shifting phase by pi if needed, then reduce phi into [0, 2*pi) with modulo. Round to 3 decimals and write the CSV sorted alphabetically by gene_id with the exact required header.