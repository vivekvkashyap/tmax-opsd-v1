Follow the spec literally and in the exact order given — this task is verified by exact reproducibility of a deterministic simulation, so any deviation in RNG call order, seed placement, or solver settings changes the final numbers. Create the venv, install numpy/scipy, then write analyze_epidemic.py: define the SI ODE dS/dt=-beta*S*I, dI/dt=beta*S*I-gamma*I; set gamma=0.15, y0=[99,1], t_eval=np.linspace(0,50,100); call np.random.seed(42) once, then draw beta_group1 (500 samples, mean 0.30, std 0.02) immediately followed by beta_group2 (500 samples, mean 0.28, std 0.02) from the SAME seeded stream in that order — do not reseed between them. For each beta, call scipy.integrate.solve_ivp with default RK45 and t_eval, take max of the I trajectory; collect into D1 and D2 arrays. Compute wasserstein_distance(D1, D2). Then call np.random.seed(100) freshly right before the bootstrap loop, and for each of 1000 iterations resample indices with np.random.choice(len, len, replace=True) for group1 THEN group2 in that order, compute wasserstein_distance on the resampled arrays, then take the 2.5/97.5 percentiles via np.percentile. Round all three outputs to 4 decimals and write the JSON with the exact key names given. Run the script once to produce results.json.