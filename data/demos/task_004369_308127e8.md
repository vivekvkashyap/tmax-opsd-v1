Work the three phases independently and verify each before moving on. Phase 1: point `nginx.conf`'s `/upload` location at `proxy_pass http://127.0.0.1:5000;` on `listen 8080;`, and fix the Flask Redis client's host/port; before writing any file the handler must acquire a per-`backup_id` Redis lock (e.g. `SET key val NX EX ...`) and bail if unavailable, releasing it when done. Phase 2, the validator, must be strict: parse `index.xml` with a real XML parser (any parse error is evil); percent-decode each `<file>` path before rejecting absolute paths or `../` traversal; compare the *set* of manifest-listed files against the *set* of files actually in the tar member-by-member (any mismatch either direction is evil); and reject based on each tar member's recorded uncompressed size exceeding 5MB without fully extracting untrusted archives. Test against every file in both `/home/user/corpora/clean` and `/home/user/corpora/evil` individually before trusting it. Phase 3: convert XML to JSON with a real parser, chunk files in fixed 100KB reads (final chunk may be short), name chunks with zero-padded 4-digit sequence numbers, and only then curl-test the full end-to-end upload flow.
