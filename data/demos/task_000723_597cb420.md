First transcribe `/app/policy.wav` with an STT tool to recover the exact uppercase rejection code string spoken in the message — read it precisely, since your script's output must match it character-for-character. Then implement the path logic to mirror standard "safe path join" semantics used by extraction tools: compute `abs_base = os.path.abspath(base_dir)`; if `untrusted_path` is itself absolute, strip its leading separator so it's treated as relative to base rather than replacing it (since `os.path.join` normally discards the first argument when the second is absolute — this is the case naive implementations get wrong); join it onto `abs_base` and normalize with `os.path.abspath` to collapse any `..`/`.` segments; then check containment using `os.path.commonpath([abs_base, resolved])` — if the common path equals `abs_base` AND the resolved path is not exactly equal to `abs_base` itself (equal-to-base should also be rejected, not just outside), print the resolved absolute path; otherwise print exactly the recovered rejection code. Wrap the whole thing in a try/except that also prints the rejection code on any error, and print nothing else to stdout. Test with nested `../`, absolute-path injections, and boundary cases like an empty untrusted_path.
