First transcribe /app/voicemail.wav (e.g. with `whisper` or another available speech-to-text tool) to recover the exact arithmetic formula stated by the engineer — listen for the sequence of operations (a multiply, an add, then a modulo) and the specific constants used, rather than assuming any particular formula. Delete the corrupted checksum.sh, then write a new bash script at /home/user/checksum.sh that takes `$1` as the integer argument and echoes the single-line integer result of applying the transcribed formula using bash arithmetic (`$(( ... ))`), taking care with operator precedence and using modulo correctly so the result matches for all inputs including large ones and edges near zero. Make it executable with `chmod +x`. Since the grader fuzzes with hundreds of random integers for bit-exact equivalence, test your script manually against several known integers first to sanity-check the formula before finalizing, and avoid any stray debug output — stdout must contain only the final integer.