Start by reverse-engineering the stripped binary's requirements rather than guessing: run `strings /app/telemetry_gateway | grep -i -E 'gateway|port|token|dir|path|curl|nc'` to find the environment variable names it reads (likely something like a PORT, TOKEN, and DATA_DIR variable), and use `strace -f -e trace=execve,open,openat /app/telemetry_gateway` (with plausible env vars set) to confirm which external commands (e.g. curl) it shells out to and observe it failing on missing env vars or PATH lookups. Note the existing wrapper deliberately sets `PATH="/opt/custom_empty_bin"`, which hides standard binaries like `curl`/`nc` — fix by setting `PATH=/usr/bin:/bin:$PATH` (or appending, not replacing). Update start_service.sh to export the discovered variable names with the values given in the task (port binding 127.0.0.1:8443, the auth token, and the data dir — create the data dir with `mkdir -p` first), then launch the daemon in the background (`&`) and confirm with `ss -ltnp` or `curl` that it's listening on 8443. Write health_check.sh to `curl -sf -o /dev/null -w '%{http_code}' -H "Authorization: Bearer <token>" http://127.0.0.1:8443/status` and exit 0/1 based on success, matching curl's own exit code or the HTTP status.