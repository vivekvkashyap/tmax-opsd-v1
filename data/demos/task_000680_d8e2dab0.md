Write `/home/user/process.py` as a straightforward stdlib pipeline: read the CSV with `csv.DictReader`, for each row compute `hashlib.md5(f"{timestamp}|{sensor}|{value}".encode()).hexdigest()` using the exact field values as they appear in the row (watch formatting, e.g. `10.5` vs `10.50`, must match the source string exactly since dedup keys off the literal string), and keep only the first row seen per hash in a dict/set while counting kept vs dropped. Sort survivors by `(sensor, timestamp)` — note timestamp here is numeric so sort as int/float, not lexicographically, to get true ascending order — then group by sensor to compute count and `round(avg, 1)`. Write the grouped CSV with the exact header `sensor,event_count,avg_value` and no extra formatting (avoid trailing `.0` inconsistencies — Python's `round` plus normal float printing should match if you follow the spec precisely). Append the log line using `datetime.now().strftime('[%Y-%m-%d %H:%M:%S] Processed %s unique records, dropped %s duplicates.')` with the counts substituted. Create output directories with `os.makedirs(..., exist_ok=True)` before writing. After running the script, write the single required crontab line to `/home/user/schedule.cron` verbatim.