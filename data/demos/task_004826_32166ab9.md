Break this into independent, testable stages. Stage 1: use `ffmpeg` to pull frames from `/app/security_feed.mp4` at 1fps, convert each to grayscale, load as a numpy matrix. Stage 2: run `numpy.linalg.svd` per frame and sum the top 10 singular values (already sorted descending, so slice `s[:10].sum()`); store one score per frame index, matching `timestamp_sec`. Stage 3: bootstrap the CI — resample the score array with replacement 1000 times, compute each resample's mean, take the 2.5th/97.5th percentiles of that distribution as lower/upper bounds; flag frames scoring above the upper bound as anomalous. Stage 4: join anomalous indices against `/home/user/sensor_logs.csv`'s `sensor_active` column by `timestamp_sec` and compute the fraction with `sensor_active==1` as precision. Stage 5: wrap it all in a Flask/FastAPI app on `127.0.0.1:8080` with `/stats`, `/anomalies`, `/validation` GET endpoints returning JSON, computing results once at startup rather than per-request, and keep the server running persistently in the background so the verifier can query it.