This is a stiff ODE (mu=1000 makes the Jacobian very ill-conditioned in the fast-transition regions) — a naive explicit/non-stiff integrator (e.g. default RK45) will either take forever or fail to converge. Recognize the stiffness and pick an implicit stiff solver: with `scipy.integrate.solve_ivp`, use `method='BDF'` or `method='Radau'` (with `dense_output=True` and reasonably tight `rtol`/`atol`) rather than the default method. Define the Van der Pol system as two first-order ODEs (`dy/dt=v`, `dv/dt=mu*(1-y**2)*v - y`) with the given initial conditions and integrate over `t in [0,3000]`. Evaluate the dense solution on `np.linspace(0, 3000, 30000)` to get `y(t)`, then integrate `y(t)**2` over that grid with the trapezoidal rule (`np.trapz`). Round the final scalar to exactly 2 decimal places and write it to `/home/user/result_integral.txt`. Sanity-check by trying a tighter tolerance and confirming the result is stable before finalizing.
