Fix the bytes/string mismatch at its source: `zlib.crc32()` in Python 3 requires a `bytes`-like argument, so encode the JSON payload string to UTF-8 (e.g. `payload.encode('utf-8')`) before passing it to `crc32`, while still embedding the original string form of `payload` in the outgoing message JSON — don't let the encoding change what gets serialized as `payload`. Leave `/home/user/data.json` and `/home/user/echo_server.py` untouched. Build the venv with `python3 -m venv /home/user/venv`, activate it (or call its `bin/pip`/`bin/python3` directly), and `pip install websockets` inside it. Launch the echo server in the background using that venv's Python interpreter (so it can import `websockets`) and give it a moment to bind to port 8765 before running the processor — checking the port is listening (e.g. with a short retry loop or `nc -z`) is more reliable than a fixed sleep. Then run the fixed `ws_processor.py` (also via the venv's Python) and confirm `/home/user/success.log` is written with the echoed JSON, matching the exact `json.dumps(data, sort_keys=True)` serialization used for the checksum computation.