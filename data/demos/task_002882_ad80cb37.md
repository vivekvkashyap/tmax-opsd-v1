For the video part, don't eyeball it — extract every frame with ffmpeg (e.g. `ffmpeg -i legacy_tests.mp4 frame_%04d.png`) and write a small script (PIL/OpenCV or ImageMagick) that checks, per frame, whether all pixels equal pure red (255,0,0) rather than just "mostly red," since a near-red frame should not count; sum the qualifying frames and write that integer plus a newline to /home/user/schema_version.txt. For the migrator, build it in two layers: a text-rewrite pass for known Python 2 idioms (xrange→range, `print "x"`→`print("x")`, `.iteritems()`→`.items()`), then parse the rewritten code with `ast.parse` and walk the tree rejecting anything outside a strict allow-list of node types (numeric/string literals, basic binary/logical ops, safe Call nodes to a fixed math-function whitelist, Print) — explicitly reject Name lookups to `eval`, `exec`, `__import__`, `os`, or any Call whose function isn't in your whitelist. Exit 1 and leave output untouched on rejection, exit 0 and write the transpiled code otherwise. Run both corpora locally before finishing to confirm 100% pass/reject rates.