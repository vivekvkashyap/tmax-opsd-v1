Diagnose the cycle first: `core` currently imports `api` for `UptimeResponse`, while `api` already depends on `core` — resolve it by moving the `UptimeResponse` struct definition into `core/src/lib.rs` (deriving `Serialize` there, so `core`'s `Cargo.toml` needs the `serde` dependency too) and deleting `core`'s dependency on `api` in its `Cargo.toml`; have `api` re-export or reference `core::UptimeResponse` instead. Next, change `read_uptime` to take a `proc_dir: &str` parameter and build the path as `format!("{}/uptime", proc_dir)` instead of the hardcoded `/proc/uptime`, updating all callers (including `create_response`, which should also take/pass the dir). Create `/home/user/mock_proc/uptime` containing exactly `12345.67 890.12` (parse only the first whitespace-separated token as f64). In `api`, expose the JSON-returning function taking a proc_dir argument, and add a `#[test]` (in `api/src/lib.rs` or `api/tests/`) that calls it with `/home/user/mock_proc`, asserts the parsed uptime equals 12345.67. Finally run `cargo build` and `cargo test --workspace`, redirecting the latter's output with `> /home/user/test_results.txt`, and confirm the "test result: ok" line and passed-test count appear before considering the task done.