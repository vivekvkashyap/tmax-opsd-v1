Solve routing and parsing bugs separately, then build the detector from what you learn — don't guess the detector logic without reading the actual C parsing code. Fix `/home/user/nginx.conf`'s `location /process` block so `proxy_pass` points at `http://127.0.0.1:9000`, restart via `/app/start_services.sh`, and confirm `curl -X POST http://127.0.0.1:8080/process` returns 200 before moving on. To find the hang trigger, read `/home/user/backend/parser.c`'s tokenizing loop closely for any place the pointer can move backward or fail to advance after matching a specific literal token, and confirm experimentally by sending a crafted request and observing the worker hang (`top`/`strace`) rather than guessing. Once you've identified the exact token, write `/home/user/detector.c` to scan the request body for that token appearing as a whitespace-delimited word (not merely a substring — tokens embedded in longer words must not false-positive), exiting 1 if found else 0. Compile it, then run it against every file in `/app/corpus/clean/` and `/app/corpus/evil/` to confirm 100% correct classification before finishing.