Split this into extraction, graph reasoning, and serving. For extraction, use `ffmpeg` to sample frames from `/app/entry_log.mp4` (e.g. every fraction of a second or all frames if short) and decode QR codes per frame with `pyzbar`/`opencv-python`, deduplicating decoded strings into a set of unique employee IDs — don't assume a fixed count, discover whatever IDs actually appear. For authorization, parse `/app/org_graph.ttl` with `rdflib`, then for each extracted employee ID walk the `org:reportsTo` chain transitively (a small loop or recursive function, watching for cycles) checking at each hop whether that node has `org:hasRole org:Admin`; an ID is authorized if it or any ancestor in the chain holds Admin, otherwise unauthorized. Note authorization is not directly about `org:hasRole` value equality with strings — use RDF term comparison against the `org:Admin` resource. Finally start a lightweight HTTP server (stdlib `http.server` or Flask) bound to `0.0.0.0:9090` exposing `GET /unauthorized` that returns a JSON array of the unauthorized IDs sorted alphabetically, and leave the process running (background it, e.g. with `nohup ... &` or a daemon thread) so the verifier's request succeeds after your turn ends. Test with `curl` locally before finishing.