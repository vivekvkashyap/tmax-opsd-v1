Fix the C layer before touching Go, since a memory bug there will crash or corrupt every request. Read parser/libaudio.c looking specifically for classic off-by-one/overflow patterns around buffer copies (e.g. a loop condition using `<=` instead of `<` when copying `len` bytes into a `len`-sized buffer) and for missing `free()` calls on allocated text buffers — fix the bounds and ensure every malloc has a matching free (or that ownership is clearly transferred to Go via cgo and freed there). Get `go build` succeeding first as a checkpoint before adding features. For the HTTP layer, implement `/api/v1/extract` reading the `X-App-Version` header and doing a real semantic-version comparison (parse major.minor.patch[-prerelease] and compare numerically/lexically per semver rules, not naive string comparison) against `2.1.0-beta`, returning 426 when the client version is strictly lower. When the version check passes, call into the fixed C parser on /app/transmission.wav, then remap the old field names (req_id, raw_text, timestamp) to the new JSON shape (transmission_id, transcript, migrated: true) — the field renaming is a plain manual mapping, not something to infer. Start the server listening on 127.0.0.1:8080 and actually curl both an old-version and new-version request yourself to confirm the 426 and the 200+JSON responses before finishing.
