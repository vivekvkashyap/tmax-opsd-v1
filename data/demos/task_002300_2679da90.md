Start by transcribing the audio at `/app/net_instructions.wav` yourself — play/analyze it (e.g. with a speech-to-text tool or careful manual listening) to recover the exact health-check formula being described; do not guess at the arithmetic, since the fuzz verifier will compare your script's output bit-for-bit against a compiled oracle across thousands of random IP/latency pairs. Once you have the rule, implement `/home/user/health_eval.py` to parse the two positional args, split the IPv4 string on `.` into four octets, combine the specific octets and multiply by latency exactly as described, then print only the single-word verdict token with no extra text (any stray debug print will break exact-output fuzz comparison). For the deployment script, make directory creation and file copy idempotent naturally (`mkdir -p`, `cp` overwrite is fine), but guard the crontab addition explicitly: capture `crontab -l` output (tolerating an empty/missing crontab), check with `grep -F` whether the exact job line already exists, and only append+reinstall via `crontab -` if it's absent — this is the step naive scripts get wrong, duplicating the line on every re-run.
