Parse each JSON line of the audit log and pull the hex string after `--payload` out of `cmdline` (e.g. with a regex or split on the flag). Decode the hex to bytes, then recover each payload's 4-byte XOR key via known-plaintext attack: XOR the first 11 ciphertext bytes against the known plaintext `AUTH_TOKEN:` (11 bytes) to get 11 key-bytes, then take the first 4 distinct bytes as the repeating key (they must repeat with period 4 across those 11 bytes — verify this before trusting it) and use that key to XOR-decrypt the entire payload. Check whether the decrypted text contains `ADMIN`; if so, collect that record's dest_ip. Deduplicate the IP set, then sort IPs numerically (split each octet as an int, compare tuples — not lexicographically, since e.g. "172" must sort after "10" but plain string sort would misorder multi-digit octets) before emitting `iptables -A OUTPUT -d <ip> -j DROP` lines under a `#!/bin/bash` shebang. chmod +x the resulting script and diff its exact text against your own hand-derivation to confirm no stray whitespace or ordering bugs before finishing.