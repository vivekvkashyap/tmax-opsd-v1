Diagnose systematically before editing. First reproduce the hang/segfault: run the wrapper on a single file with spaces to isolate the bug. In `run_all.sh`, the classic cause of hangs/crashes with filenames containing spaces is unquoted variable expansion in the for-loop (`./processor $file` word-splits "file 2.txt" into two args). Fix by quoting: `./processor "$file"`, and consider using `find ... -print0 | while read -r -d ''` for extra robustness against globbing edge cases. In `processor.cpp`, trace the recursive `processFile` function: it recurses on failure to open a file with no base case, causing unbounded recursion (stack overflow -> segfault). Add a `retries` parameter/counter, and when it reaches 3, print an error to stderr and return instead of recursing further. After fixing both, recompile with `g++ processor.cpp -o processor`, rerun `./run_all.sh > /home/user/success.log`, and manually inspect the log to confirm every file in `/home/user/data` (including the one with a space in its name) appears with correct contents and no duplicate/garbled lines.
