Scaffold with `cargo new deadlock_detector` inside `/home/user/deadlock_detector/`. Parse the CSV (use the `csv` crate or manual line splitting) into a directed adjacency map from `waiting_txn` to `holding_txn`. To find the cycle, run DFS from each unvisited node while tracking the current recursion path (e.g., a `Vec` or ordered set representing the active call stack) — when you reach a node that is already in the current path, the cycle is the sub-path from that node's first occurrence to the end, not the whole graph and not just the two endpoints. The graph may contain nodes and edges that are NOT part of the cycle (dangling chains feeding into it), so don't assume every node in the CSV belongs to the answer — only extract nodes that are actually on the closed loop. Once you have the cycle's node set, sort the strings alphabetically and serialize as a JSON array of strings (e.g., with `serde_json` or manual formatting) to `/home/user/deadlock_report.json`, matching the exact bracket/quote/comma format shown in the task (no trailing newline issues, no extra whitespace inconsistencies). Build with `cargo build --release` and run it to produce the file, then double check the JSON parses and the sort order is correct.