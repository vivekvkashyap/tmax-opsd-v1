Structure the pipeline as two clean phases: (1) parallel per-graph computation, (2) deterministic serial reduction. Parse each graph file by counting edge endpoints into a degree dict (undirected edge => both endpoints get +1 degree). Use `multiprocessing.Pool.map` (or similar) to dispatch the 100 per-graph energy computations across workers — but do NOT rely on the order results arrive back from the pool for the final sum, since imap_unordered or worker completion order is non-deterministic. Instead, always pass `(graph_id, filepath)` tuples into the worker function so each result carries its own ID, collect all results, then explicitly `sorted(results, key=lambda r: r[0])` before doing the running sum — sum in that fixed ascending-ID order every time, regardless of which order workers finished in. Round each individual energy and the final total to exactly 8 decimals with Python's `round()` or format string, and write the CSV sorted by graph_id ascending with a header. Test by rerunning the script twice and diffing the two output files — they must be byte-identical.