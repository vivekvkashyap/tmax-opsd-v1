Work in stages: first `mkdir -p` the new src/include/lib/tests tree under /home/user/project, then `mv` (not copy) the existing .hpp into include/ and .cpp into src/ so the messy workspace files relocate cleanly. For the C-FFI wrapper, write c_api.cpp with `extern "C" { ... }` around each exported function, using `new StringHistory(capacity)` in `History_create`, `delete` in `History_destroy`, and simple pointer casts (`static_cast<StringHistory*>(obj)`) to call the real methods — remember `History_get` returning `const char*` must return NULL/nullptr (not throw or segfault) when the class's `get()` returns nullptr, since ctypes will treat that as `None`. Compile both .cpp files together into the shared lib in one command, e.g. `g++ -fPIC -shared -I/home/user/project/include -o /home/user/project/lib/libhistory.so src/string_history.cpp src/c_api.cpp`, and double check the `-I` path actually resolves the header. In the Python test, declare `ctypes.CDLL(...)` and correctly set `restype`/`argtypes` for each function (especially `History_get` as `c_char_p`, which python's ctypes auto-converts NULL to None) before asserting the eviction-order behavior of the ring buffer, then write the exact success string only if every assertion passes.