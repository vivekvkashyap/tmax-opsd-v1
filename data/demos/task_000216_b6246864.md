Design the C parser as an explicit character-by-character state machine, not string-splitting: track states like SCANNING, READING_TAG, READING_NAME, READING_TIMESTAMP so you can recognize `START_TRACE::name::ts` and `END_TRACE::name::ts` lines with a single pass and no per-line allocation/regex — read the file with a buffered `fread` loop, not line-by-line `fgets` if you want to avoid string overhead, though `fgets` is acceptable as long as you avoid heavy parsing per line. For each `START_TRACE`, store the timestamp keyed by `target_name` (a fixed small hash table or simple linear list is fine given the fanout); on the matching `END_TRACE`, if `target_name` starts with `mobile_module_`, compute `end - start` and call `stats_add()` from the provided `stats.h`/`libstats.a`. Write the Makefile to compile `fast_parser.c` with `-O3 -I include -L lib -lstats -o fast_parser`, then link statically — confirm with `nm` or just successful linking that `libstats.a` symbols resolved. Benchmark both parsers with `time` on the same `build_trace.log` and only trust your C output's mean/variance if it exactly matches (to reasonable float precision) what `slow_parser.py` computes independently — that agreement is your correctness check, not just something to report blindly. Format `report.txt` with the three exact required lines, values to 2 decimal places.
