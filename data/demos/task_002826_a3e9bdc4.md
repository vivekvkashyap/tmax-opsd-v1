This task has three independent deliverables — do them in order and verify each before moving on. (1) OCR: run `tesseract /app/policy_screenshot.png stdout` (install tesseract if missing) to read the exact required TZ and LANG values out of the image — don't assume a value, read what the OCR actually returns and clean up any OCR noise before trusting it. Then append proper `export TZ=...` and `export LANG=...` lines to `/home/user/.bashrc`. (2) Build the classifier at `/home/user/config_auditor.py` (argv: input_dir, output_json) by first inspecting the sample files in `/app/corpora/clean/` and `/app/corpora/evil/` to derive concrete signatures of malicious intent — e.g. `/dev/tcp/` reverse-shell redirection, `curl ... | bash` / `wget ... | sh` pipelines, and direct writes to `/etc/shadow` or similar sensitive files — then implement pattern/regex matching against those signatures generically enough to catch unseen files with the same techniques, not just the literal training examples. Output a JSON dict of basename -> "clean"/"evil". Test your script against both corpora and confirm 100% correct classification before considering it done. (3) Register a cron job for the `user` account (via `crontab -e`/`crontab -l` piping) that runs every 15 minutes and executes the exact script invocation on `/opt/configs` writing to `/home/user/audit_report.json`.