Read `main.go` and locate the mean/stddev computation that loops over all rows. Refactor so you first build two slices (or filter): rows where `Split == "train"`, and all rows. Compute the mean and sample standard deviation (dividing the sum-of-squared-deviations by n-1, not n) using only the train-split values, for each feature column separately. Then, in a second pass over the FULL dataset (all rows, preserving original order), apply `(value - trainMean) / trainStdDev` to every row's features, including test rows, using the train-derived statistics — never recompute stats from test rows. Keep the original row order and the original CSV header exactly. Format every scaled numeric value with `%.4f` (fixed 4 decimal places). After editing, run `go run main.go` (or build and run the binary) from `/home/user/csv_processor` so it reads `dataset.csv` and writes `processed.csv`. Double check by hand-computing train mean/std for one feature column and confirming the test row's scaled value matches your formula before considering the task done.