Since the CLI is strictly interactive and refuses arguments, drive it with `pexpect.spawn` rather than piping stdin, because plain pipes can deadlock or desync if the tool's prompt/response timing matters. Read `/home/user/device_inventory.csv` row by row (no header, four comma-separated fields), and for each row spawn `/home/user/iot_setup_cli`, then use `child.expect("Device ID: ")` followed by `child.sendline(dev_id)`, repeating that expect/sendline pattern for each prompt in the exact order given (Auth Token, Region, telemetry y/n, then the final "Commit configuration? (yes/no): " which must always be answered `yes` to persist the config) — matching the exact prompt text (including trailing colon/space) is important since a loose pattern can match the wrong prompt. Loop this over all rows in the CSV, and after the script finishes for all devices, confirm `/home/user/device_configs/` contains one JSON file per device before considering the task done — if pexpect isn't installed, `pip install pexpect` first.