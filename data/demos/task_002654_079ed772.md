Structure the C++ program as: parse both JSON files with nlohmann::json (`#include "json.hpp"`) into a node-load map and an adjacency list of (dst, latency) per source, run Dijkstra's shortest-path algorithm from "Gateway" to "Database" tracking predecessors so you can reconstruct the actual path (not just the distance), then reconstruct the path by walking predecessors backward from Database to Gateway and reversing it. Sum the `load` field for every node id that appears in the reconstructed path (including both endpoints) — this is a separate pass over the path list joined against the node-load map, easy to get wrong if you only sum edge weights instead of node loads. Build the output JSON with the library's object/array construction (or manual formatting) matching the exact key names and structure given, then write it to `/home/user/result.json`. Compile with the exact flags provided and run the binary, then sanity-check by manually tracing the small graph's few candidate paths to confirm your program picked the true minimum-latency one before trusting the output.
