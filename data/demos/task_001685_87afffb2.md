The billing script deliberately checks `[ -t 0 ]` and reads from `/dev/tty`, so a plain piped `echo password | ./billing_portal.sh` will fail — you need an `expect` script that spawns the process on a real pty, waits for the `Password:` prompt, sends `finops_secure`, and captures the subsequent `STORAGE_RATE_PER_MB=<value>` line from its output (e.g. `spawn ./billing_portal.sh; expect "Password:"; send "finops_secure\r"; expect eof` combined with logging/capturing stdout). Extract the rate value from that captured output rather than hardcoding it. For the archive size, use `du` with byte-accurate summation and convert to MiB yourself with ceiling rounding (e.g. `du -sb /home/user/data_archive` then divide by 1048576 and round up) rather than trusting `du -m` which may round differently than the spec requires. Scaffold the Rust project with `cargo new`, read `STORAGE_RATE_PER_MB` and `ARCHIVE_MB` via `std::env::var`, parse the rate as f64, multiply, and format the result with exactly two decimals (`format!("Total Cost: ${:.2}", cost)`) into `/home/user/cost_report.txt`. Run the compiled binary with both env vars explicitly set/exported before finishing.
