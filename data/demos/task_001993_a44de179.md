Build the pipeline in two clean stages and keep the interface between them (summary.csv) rigid. For extract.sh: loop over `run_*.csv` files, for each one take only the last data row (not the header) and pull the `accuracy` and `latency_ms` columns by field position (awk/cut with `-F,`), derive run_id by stripping the `.csv` suffix from the filename, and append `run_id,accuracy,latency` lines; then sort the whole output by run_id (e.g. `sort -t, -k1,1`) before writing summary.csv, with no header line. For analyze_runs.c: parse argv[1] to dispatch on "similarity" vs "bayes"; in similarity mode, read summary.csv line by line, compute the given weighted squared-distance formula per run (note the 100x scale on the accuracy term — get that multiplier exactly right, it's applied before squaring), track the minimum, and print only the winning run_id with no extra text; in bayes mode apply Bayes' theorem directly from the three CLI floats and print the result with `printf("%.4f\n", ...)`. Compile with the exact gcc command given, then chain the two example invocations, redirecting each program's stdout to the two required output files.