Read `/app/query_spec.png` with `tesseract` to recover the exact aggregation pipeline steps (match/filter condition, group-by field, aggregation function, sort order, and limit) — don't assume the pipeline shape. Study `/app/baseline.sh` to confirm its logical semantics so your optimized version matches it exactly, but replace its slow `while read` line-by-line subshell processing with a single-pass streaming tool: `jq` is ideal here since the input is JSONL — use `jq -s` (slurp) with `select()`, `group_by()`, `map()` for the sum, `sort_by()` plus `reverse` (or `-sort_by`), and array slicing `.[0:N]` to get the top results, writing the JSON array straight to `/home/user/result.json`. Avoid spawning a subprocess per line; a single `jq` invocation over the whole file is what delivers the required order-of-magnitude speedup against the 50k-line baseline. Double check your filter/group/sort semantics against the image's spec produce identical output to a manual equivalent query before optimizing further. Make the script executable and confirm timing manually (e.g. `time ./optimized.sh`) against the baseline before finishing.
