Before writing any classifier code, diagnose the vendored library bug directly: grep rapidcsv.h for `SeparatorParams` and inspect its constructor's default separator argument — if it's set to something other than `','` (e.g. `'|'`), that's why comma-separated files parse incorrectly (it's silently treating the whole line as one field or splitting on the wrong char); fix the default in the header itself. Then write classifier.cpp: load features.csv and labels.csv with rapidcsv, join rows on `user_id` by building a lookup map (not assuming row order matches between files), and implement KNN with straightforward squared/Euclidean distance over all numeric feature columns. For hyperparameter search, implement 5-fold CV yourself (split indices into 5 folds, train-on-4/validate-on-1, rotate), evaluating only odd k in [1,15], and pick the k with best average validation accuracy across folds. Retrain (or just use vote-counting, no separate "training" needed for KNN) on the full joined dataset with the chosen k, predict for every row in test_features.csv, and write predictions.csv with exactly the `user_id,prediction` header and rows. Compile with the exact g++ command given (pointing `-I` at the fixed vendored header) and rerun to confirm accuracy improves once the parser fix is in place — a low accuracy after implementing KNN correctly usually means the CSV parsing bug wasn't actually fixed.