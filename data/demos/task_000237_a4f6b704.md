This is a data-leakage fix: the correct order of operations is split first, then compute statistics only on the training partition, then apply those statistics to both partitions. Read the existing `src/main.rs` to see the CSV loading code and understand its current (buggy) order — it currently computes means over the full dataset and centers everything before splitting. Restructure it so you (1) load all rows, (2) slice rows 0..80 as train and 80..100 as test before any transformation, (3) compute per-column means using only the train rows, (4) subtract those train means from every value in both train and test sets, (5) sum all values in the transformed test set. Use `rustc` or `cargo build`/`cargo run` to compile and execute, redirecting the printed line into the output file with the exact `Test sum: <value>` format and the value rounded to 4 decimal places (Rust's `{:.4}` formatting). Double check you're reading the CSV path relative to the correct working directory when running.
