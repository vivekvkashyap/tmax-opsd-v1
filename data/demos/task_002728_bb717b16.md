Extract the hardcoded secret from `/app/legacy_auth` using static analysis first — `strings /app/legacy_auth | grep -i` for plausible key-like substrings (fixed-length alphanumeric/symbol strings) is far safer and faster than trying to disassemble it, since it's likely embedded as a literal in the binary's rodata section; confirm the length matches "32-character" before trusting it. Save that exact string to `/app/jwt-service/secret.key`. Generate the TLS pair with `openssl req -x509 -newkey rsa:2048 -nodes -keyout /app/jwt-service/tls/key.pem -out /app/jwt-service/tls/cert.pem -days 365 -subj '/CN=localhost'`. For the Makefile, `make` in `/app/jwt-service` and read the linker error — it's almost certainly missing `-lssl -lcrypto` in `LDFLAGS`; add them. For the vulnerability, open `src/auth.cpp` and find where it checks the token/algorithm for a "none" value and short-circuits to accept it unconditionally — remove or invert that bypass branch so tokens claiming `alg: none` (in any case) are always rejected, while preserving the legitimate signature-check path. After fixing both Makefile and auth.cpp, run `make`, then start the resulting binary in the background bound to `0.0.0.0:8443` with the secret and TLS files in place, and don't modify the port or endpoint paths.