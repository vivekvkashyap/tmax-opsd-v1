Work in three clearly separated phases. First fix `/app/c_src/Makefile`: run `make` there and read the linker error carefully — a shared library that uses a math function (pow, etc.) but doesn't link `-lm` will fail or produce runtime undefined-symbol issues, so check the gcc invocation includes `-lm` alongside `-shared -fPIC`, and confirm `libtransform.so` is actually produced. Second, transcribe `/app/architecture_notes.wav` with an offline tool such as the `whisper` CLI (e.g. `whisper /app/architecture_notes.wav --model base --output_format txt`) and read the transcript closely — it describes exactly how to combine the C transform output with a graph-derived structural property (a length/depth measurement on the DAG in `/app/graph.json`, computed via topological traversal to find the longest path) and what final arithmetic operation (multiplication, then a modulus) to apply; do not skip re-reading the transcript, since getting the combination order or the specific graph property wrong is the main way this goes wrong. Third, implement `/home/user/evaluator.py`: load `libtransform.so` via `ctypes.CDLL`, declare `transform`'s `argtypes`/`restype` as `c_int`, compute the graph property with your own traversal code (e.g. DFS/BFS on the adjacency lists respecting DAG order), combine per the transcript, and print only the final integer. Validate against several hand-computed inputs before trusting the fuzzer.
