Tackle this in dependency order: C build first, then FFI wiring, then validation translation, then DB migration, then start services and test end-to-end. (1) Compile analyzer.c with `gcc -shared -fPIC -o libanalyzer.so analyzer.c`; inspect the C struct/function signature carefully so your ctypes definitions match field order, types, and alignment exactly (mismatched ctypes.Structure layout is the most common failure here — verify field sizes with `ctypes.sizeof`). Load the library with `ctypes.CDLL('./libanalyzer.so')`, set `.argtypes`/`.restype` on `compute_risk_score`, and call it from `process_payload`. (2) Read legacy_validate.js closely and port its exact conditionals (field presence checks, numeric bounds like attempts>5) into Python — don't guess the rules, translate them literally, then wire the check into the `/ingest` handler returning HTTP 400 + the exact error JSON on failure. (3) For the SQLite migration, use the standard SQLite "add column, backfill, then rebuild table to drop/rename" pattern since SQLite can't directly drop columns in older versions: add `iso_time`, backfill via `datetime(timestamp, 'unixepoch')`-style conversion to ISO 8601 with a trailing 'Z', then recreate the table without the old column and rename. (4) Start backend_tcp.py (port 9090) before ingest_api.py (port 8080) so the API can connect on startup, and do a manual curl test of the full chain before finishing.
