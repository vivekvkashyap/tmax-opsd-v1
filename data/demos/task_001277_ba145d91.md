Transcribe `/app/voice_memo.wav` first (speech-to-text) to learn the exact custom encoding rule described — don't assume a generic RLE scheme, the details (e.g. which byte value triggers run compression, how the count byte is encoded, whether non-target bytes pass through raw) matter and must come from what you actually hear. Implement both scripts to handle arbitrary binary data safely: avoid tools/constructs that mangle bytes (e.g. plain `read`, unquoted variable expansion, or text-mode pipes) — process the stream with `xxd`/`od` to get a byte-level representation, transform it according to the algorithm, and reassemble with `xxd -r`, keeping everything in binary-safe pipelines. Build the compressor and decompressor as exact inverses of each other and test round-tripping arbitrary byte strings (including embedded nulls, 0xFF runs, empty input) before trusting them, since a fuzz harness will check bit-exact equivalence against a reference binary over many random byte streams. Finally, use `find /app/logs_incoming -type f -mtime +7` to locate logs older than 7 days by mtime and rename each with `mv "$f" "$f.archive_pending"`.