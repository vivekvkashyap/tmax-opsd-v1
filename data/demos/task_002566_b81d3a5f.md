Iterate every `*.csv` file in `/home/user/experiments/` with pandas/numpy, but explicitly cast `y_true` and `y_pred` to `numpy.float32` (e.g. `.to_numpy(dtype=np.float32)`) before computing MSE — since MSE = mean((float32(y_true)-float32(y_pred))**2) can differ slightly from a float64 computation due to reduced precision, and the verifier checks against the float32-computed values specifically. Filter to keep only models with `MSE < 0.5` (strictly greater-or-equal is excluded). Format each MSE to exactly 4 decimal places (`f"{mse:.4f}"`) and sort the surviving rows ascending by MSE before writing. Derive `model_name` by stripping the `.csv` extension from the filename, not by any other heuristic. Write the CSV with the exact header `model_name,mse` to `/home/user/valid_models.csv`, then actually execute the script so the file exists on disk.