Before writing any code, fix the plumbing: edit `/app/flask_service/config.env` so it points at the actual running Redis (6379) and Postgres (5432) ports instead of the stale legacy ones, then rerun `/app/start_services.sh` and confirm all three services are reachable (e.g. `psql`, `redis-cli ping`, `curl` the Flask endpoint) before writing the planner — debugging against misconfigured services wastes time. Then build `/home/user/workspace/restore_planner.py` in layers: (1) recursively query the `dependencies` table in Postgres to build the full upstream dependency graph for the given `--db-id` (a graph traversal/BFS-DFS or a recursive CTE); (2) for each database in that graph, call the Flask topology endpoint to get its incremental backup chain, and use Redis lookups (`chunk:<chunk_id>`) to resolve chunk IDs to storage URLs; (3) compute out-degree centrality across the dependency graph to decide database restore order (databases more depended-upon go first), while preserving strict chronological order for chunks within a single database's chain; (4) join everything and print a single comma-separated line of URLs to stdout. Test against several different `--db-id` values and inspect intermediate structures (print the graph, the chain, before assembling final output) to catch ordering bugs early, since the verifier checks bit-exact output against an oracle across many random IDs.