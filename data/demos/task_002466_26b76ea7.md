First derive the per-frame calibration weights from `/app/calibration.mp4`: extract raw grayscale frame bytes (e.g. `ffmpeg -i calibration.mp4 -pix_fmt gray -f image2pipe -vcodec rawvideo -`), and for each of the 100 frames sum all its pixel byte values modulo 256 to get `W_i`; bake this 100-entry lookup table into your C++ program (as a static array/header) since it's derived once from a fixed fixture, not recomputed at runtime from the CSV. In the CLI tool, parse the CSV manually (simple `getline`/`stringstream` split on commas is fine — no external library needed), group rows into a `map<string, vector<Row>>` keyed by `node_id`, and within each group sort by `frame_index` ascending. The sliding window is over *rows present for that node*, not over frame-index values — for each row at position `i` in that node's sorted sequence, sum `value * W[frame_index]` over rows `i`, `i-1`, `i-2` (clamped at the start), even if those rows' frame indices aren't consecutive. Print `frame_index,node_id,smoothed_value` (2 decimal places) sorted by `node_id` then `frame_index`, with no other output. Compile with `g++ -O2 -o /home/user/analyzer`, matching output format exactly since it's checked against many randomized inputs.
