Start by OCR'ing the design notes: run `tesseract /app/obfuscation_rules.png stdout` and read the recovered text carefully — it will spell out a precise byte-transformation rule (likely an XOR with some constant plus a special-case swap between two specific byte values); don't paraphrase loosely, re-derive the exact hex constants and swap pairs from what tesseract actually returns, cross-checking ambiguous OCR characters (0 vs O, 1 vs l, 5 vs S) against context. Implement `/home/user/obfuscator.py` to read raw bytes via `sys.stdin.buffer.read()`, apply the transformation per-byte over a `bytearray`, and write raw bytes via `sys.stdout.buffer.write()` — never use text-mode stdin/stdout since that will corrupt non-UTF8 byte sequences. Add a shebang line, `chmod +x` the script, create `/home/user/bin/`, and symlink it with `ln -s /home/user/obfuscator.py /home/user/bin/obfuscate_stream`. Before finishing, self-test for correctness properties any well-formed byte transform should have (e.g. check whether your transform is its own inverse if the notes imply an involution, and confirm piping random binary data through it twice, if applicable, round-trips) using `head -c 4096 /dev/urandom | ./obfuscate_stream | xxd | head`.