First recover the hidden rules from the audio before writing any code: run the wav through a speech-to-text tool (`whisper /app/admin_instructions.wav` or a Python `whisper`/`speech_recognition` package — install if needed) and read the transcript carefully for the base directory path and any file-extension exclusion rule; don't guess these values. Then write the C++ sanitizer as a proper multi-line record parser: read stdin line by line, detect `RECORD START`/`RECORD END` boundaries, capture the `Path:` value per record, and implement your own path-normalization routine that resolves `.` and `..` components using a stack of path segments (split on `/`, push non-`.`/non-`..` segments, pop on `..`) rather than relying on `realpath` (which would fail on nonexistent paths) — this exactly mirrors what a reference implementation would do. Apply the transcribed base-directory containment check on the *normalized* path (verify it stays under the base dir, careful about prefix-matching pitfalls like `/mnt/storage2` sharing a string prefix with `/mnt/storage`) and the extension rule on the *original* (un-normalized) path string. Print `ACCEPTED: <normalized>` or `REJECTED: <original>` per record. Compile with `g++` to `/home/user/sanitizer`, then self-test with edge cases (multiple `../../..`, trailing slashes, paths with no leading `/`) since the grader fuzzes heavily.
