Reverse-engineer `/app/legacy_auth` empirically rather than guessing: run `strings /app/legacy_auth | less` to find the hardcoded secret key embedded in the binary, then use `file`, `objdump -d`, or `ltrace`/`strace` plus systematic test invocations (well-formed tokens, malformed ones, missing parts, alg=none, alg=md5 with correct/incorrect signatures) to nail down exact behavior for each branch: number-of-parts check, base64url decode failures, JSON parse failures, algorithm dispatch, signature comparison, and the redaction rule for sensitive field names. Pay special attention to exact stdout wording/formatting (prefixes like "INVALID: ..." vs "VALID: ...", whether payload JSON has sorted keys and no spaces) and exit codes — these must match byte-for-byte since the grader diffs outputs across thousands of fuzzed inputs. Implement `/home/user/auth_migrator.py` mirroring this logic precisely (base64 padding handling, JSON key ordering, hash construction order of concatenation before hashing), then self-test by running both the binary and your script side-by-side on a wide variety of crafted and randomly mutated inputs before finalizing.