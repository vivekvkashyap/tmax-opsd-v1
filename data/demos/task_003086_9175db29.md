First recover the calibration frequency: transcribe the spoken sentence in `/app/calibration_signal.wav` (use a speech-to-text tool or careful listening) and extract the numeric Hz value it states — do not guess or hardcode a frequency. Then implement `/home/user/process_signal.py` to read the input file path from `sys.argv[1]`, parse one float per line, and for each line index i compute `x_i * cos(2*pi*f_c*i/16000)` using `math.cos`/`math.pi` (avoid numpy here since it may use different reduction/precision internals than the oracle). Critically, accumulate the modulated values with `math.fsum(list_of_values)` rather than a running `+=` loop or built-in `sum()` — naive summation drifts from the oracle's exact/compensated summation under floating-point rounding, especially over thousands of lines, and the fuzz test checks bit-exact equivalence. Print only the final sum formatted with `f"{total:.10f}"` and nothing else (no extra prints or trailing text) since the oracle compares stdout exactly.