Install a local speech-to-text tool (e.g. `pip install openai-whisper` plus ffmpeg, or `SpeechRecognition`) and run it against /app/interview_tape.wav to get a raw transcript — inspect the transcript text yourself before parsing, since ASR output on names/proper nouns is often noisy and may need manual correction (e.g. capitalization, homophone fixes) informed by context clues like "Project X" patterns. Write process_graph.py to parse sentences of the form "<Person> manages <Person>" and "<Person> works on Project <Name>" using simple pattern matching (regex or keyword-splitting on "manages"/"works on"), extracting subject/object pairs per sentence — don't assume a fixed sentence count, iterate over however many sentences the transcript actually contains. Emit Cypher exactly in the required MERGE syntax shown in the task (Employee nodes keyed by name, Project nodes keyed by title, relationship types MANAGES/WORKS_ON in caps) plus the two index-creation statements for Employee(name) and Project(title). Before finalizing, re-read your own generated graph_init.cypher against your transcript sentence-by-sentence to catch missed or misattributed relationships, since the grader scores F1 against the true edge set and both false positives and misses hurt the score.