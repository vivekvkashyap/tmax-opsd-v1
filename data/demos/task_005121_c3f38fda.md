Tackle the three phases independently and verify each before moving on. Phase 1: read matrix_trace.c looking for an off-by-one loop bound when summing the diagonal (a loop that runs one iteration too far, indexing past the allocated array) and for allocated buffers that are never freed; fix the bound and add matching `free()` calls without changing the scanf/printf I/O format, then generate the patch with `diff -u matrix_trace.c.orig matrix_trace.c > fix.patch` (apply and re-test with `patch` to confirm it applies cleanly) and validate memory-safety under `valgrind --leak-check=full`. Phase 2: compile the fixed C program, then write a bash script that loops `jq -c '.[]'` output over test_vectors.json, for each object extracts `.id`, `.size`, and `.matrix[]` with `jq -r`, feeds `size` then the matrix values (space/newline separated, matching scanf's expectations) into `./matrix_trace` via a pipe or heredoc, captures stdout as the trace, and accumulates results into a JSON array with `jq -n` or by building objects and passing through `jq -s`. Phase 3: write a self-contained nginx.conf with `events {}`, a `pid` directive, `error_log`/`access_log` paths under /home/user/ci/, and inside `http { server { listen 127.0.0.1:8080; location /report { proxy_pass http://127.0.0.1:9090; } } }`; validate syntax with `nginx -t -c <path>` before finishing, since standard nginx configs require `user`/`http` blocks that must be adjusted to avoid needing root.