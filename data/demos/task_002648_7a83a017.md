First transcribe `/app/memo.wav` (e.g. `whisper` or `SpeechRecognition`+`ffmpeg`) to recover the two integer parameters (domain count D, bootstrap count B) spoken by the PI, reading numbers-as-words carefully, and hardcode them only after confirming the transcript. Then implement the algorithm literally, since it's fuzz-tested bit-exact against a reference: split the input into contiguous domains of length `N/D` (integer division) for the first D-1, with the remainder folded into the final domain. Implement the exact LCG given as a single global `uint32_t` that persists across the whole run — never reseed between domains or iterations. For each domain, run B bootstrap iterations, each building a resampled string where character i comes from `domain[next_rand() % L]`, counting G+C; after collecting B counts per domain, sort ascending and read `counts[(int)(0.025*B)]` and `counts[(int)(0.975*B)]` for the CI width. Track the domain with max width (first occurrence wins ties) and print its 0-based index. Compile with `gcc -O2` and hand-trace a small input to confirm your LCG sequencing and indexing before trusting the fuzz suite.