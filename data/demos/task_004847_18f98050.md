Start by OCR'ing `/app/vm_config.png` (e.g. `tesseract` or another OCR tool) and read the resulting text yourself to recover the SSH port number and VM instance ID — do not assume values, extract them from the image. Then: create `/home/user/provision/<instance-id>/` and inside it a symlink `latest-disk -> /app/base.qcow2` (`ln -s`). Set up the SSH tunnel with `ssh -f -N -L 8080:localhost:<port> <host>` (or equivalent), capture its PID (e.g. `echo $!` after backgrounding, or `pgrep -f` the exact command) into `/home/user/tunnel.pid`. For the Go proxy filter, write a program that reads stdin line-by-line with `bufio.Scanner`, does a literal string replace of `"DEV"` with the instance ID via `strings.ReplaceAll`, and writes each line to stdout with a trailing newline — since this is checked by fuzzing against a hidden reference binary on thousands of random lines, keep the replacement logic exactly literal (no regex word-boundary tricks, no case-insensitivity) and preserve any non-DEV content and line structure verbatim. Compile with `go build -o /home/user/proxy_filter proxy_filter.go` and verify it's executable.
