Start by reading `packet_processor.c` closely, focusing on the payload-parsing loop inside the packet handler — this is a classic TLV (type-length-value) parser where the loop advances an offset by the parsed length field. Use `tshark -r traffic.pcap -x` or `tcpdump -r traffic.pcap -X` to dump raw packet bytes and locate the one UDP packet whose payload has a length/tlen field of 0 (or otherwise causes the offset increment to be zero) — that packet's source IP is the attacker; write it to `/home/user/attacker_ip.txt`. Then fix two separate bugs in `process_payload`-style code: (1) the loop-termination bug, where the offset increment must also account for the fixed TLV header size (type+length bytes) so a zero-length value still advances the offset, preventing an infinite spin; (2) the memory leak, where each iteration allocates a buffer for the value that is used but never freed — add a `free()` at the right point (or restructure to avoid the leak) without introducing a use-after-free. Rebuild with `make`, and confirm `timeout 2s ./packet_processor traffic.pcap` exits 0 quickly before finishing.