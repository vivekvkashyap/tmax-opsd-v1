Start by reproducing the divergence: call `simulate_B` with the suggested initial guess and confirm you see NaNs, then open `/app/bio_kinetics/bio_kinetics/solver.py` and inspect the adaptive step-size controller. A correct adaptive-step scheme shrinks the step when local error exceeds tolerance and grows it when error is small — check the direction of the `err`/`tol` ratio (and whether it's inverted, e.g. `sqrt(err/tol)` where it should be `sqrt(tol/err)`) in the `h_new` update; an inverted ratio makes the step grow exactly when it should shrink, causing blow-up. Fix the ratio, reinstall the package (`pip install -e /app/bio_kinetics` or equivalent), and confirm `simulate_B` now returns finite, non-NaN arrays across the data's time range. Then fit `k1, k2` to `/home/user/data/spectroscopy.csv` using `scipy.optimize.curve_fit` or `minimize` with `simulate_B` as the model function and `[1.0, 1.0]` as the initial guess, minimizing squared error against the `signal` column. Write the two fitted values comma-separated to `/home/user/fitted_params.csv`, then independently recompute the MSE yourself against the CSV data to confirm it's under 0.005 before finishing.