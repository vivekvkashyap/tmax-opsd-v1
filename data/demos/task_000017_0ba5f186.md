Break this into extraction, fetching, and two separate statistical computations. Parse the FASTA by splitting on `>` headers and concatenating each record's sequence lines; for each sequence, only keep it if it contains both primers, then locate the first occurrence of the forward primer and, searching strictly after it, the first subsequent occurrence of the reverse primer — the extracted region is the substring strictly between them (excluding both primers). Compute GC-content as (count of 'G' + count of 'C') / length for each extracted region. Launch `/home/user/server.py` in the background (`python3 server.py &` or `subprocess.Popen`) and confirm it's listening before calling `GET http://localhost:8080/synthetic`; compute the same GC-content formula for each returned `sequence`. For the distance, use `scipy.stats.wasserstein_distance(real_gc, synthetic_gc)` directly on the two GC lists. For the probability mass, fit `scipy.stats.gaussian_kde(real_gc)` with default bandwidth and integrate its `pdf` from 0.4 to 0.6 with `scipy.integrate.quad`. Write all three values to `/home/user/results.json` in the exact required shape, keeping floats at full precision rather than manually rounding.
