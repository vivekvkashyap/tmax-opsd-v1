Tackle the build fix before scripting the sanitizer. Diff or inspect `/app/vendored/pigz-2.8/Makefile` for the perturbation — compiling will fail with an "undefined reference" linker error, which tells you a required library flag (likely `-lz`) is missing from the `LIBS`/`LDFLAGS` line; restore it, `make`, then copy/symlink the resulting `pigz` binary to `/home/user/bin/pigz`, `chmod +x`, and add that directory to `PATH` (verify with `pigz --version`). For `/home/user/sanitize_backup.sh`: extract the input archive into a fresh temp dir under your control (e.g. `mktemp -d`), then loop recursively — use `find <dir> -name '*.tar.gz' -o -name '*.tar'` repeatedly (or a `while` loop until none remain) to expand nested archives into same-named directories and delete the nested archive file each time, since one pass may reveal further-nested archives. For link sanitization, `find <dir> -type l` each symlink, resolve its target with `readlink`, and compute whether the resolved absolute path stays inside the archive root (compare the resolved path's prefix against the root, watching out for `..` traversal that escapes it or an absolute target) — delete any link that escapes, keep ones that stay inside. Finally `tar`/`pigz` the sanitized tree to `<output>.tmp` and only `mv` it to the final output path after compression exits successfully, giving you atomicity. Test against both a hand-built clean sample and one with planted absolute/relative-traversal symlinks and nested zip payloads before trusting it against the real corpora.