Work this in order: first get the vendored C extension building. Inspect `/app/vendored/libsectoken-1.2.0/src/*.c` for a broken `#include` (a typo'd header name) and fix it to the correct system header, then check `setup.py`'s `Extension(...)` call — if it links against OpenSSL/crypto functions but is missing the linker flag, add the missing library to the `libraries=[...]` list so linking succeeds; run `pip install .` inside that directory and confirm `import libsectoken` works before moving on. Second, reverse-engineer `/app/oracle/token_oracle`: run `strings` on it to spot embedded format strings, header names, or the CSP text; use `objdump -d` (or `ltrace`/`strace` while feeding it sample input) to see the order of operations — what gets hashed/signed, where the CSP header text and metadata get embedded relative to the signature, and the exact base64 variant used. Third, write `token_rotator.py` using the fixed `libsectoken` to reproduce that exact byte layout (metadata block containing the CSP string, then base cryptographic signature, then base64-encode). Validate by running your script and `token_oracle` on the same handful of JSON inputs and diffing outputs byte-for-byte before considering it done — exact match matters, not just "looks similar".
