Build this as a clear pipeline of stages in C, verifying each stage's intermediate output before moving to the next, rather than writing everything blind. First, `fread` the binary file into an array of the 16-byte struct (watch alignment/padding — pack or read field-by-field to avoid struct padding bugs, and confirm record count = filesize/16). Second, apply the OpenMP-parallelized distance filter: for each record compute `sqrt(x*x + y*y)` (or compare squared distance to avoid sqrt if you're careful) and mark records with distance > 100.0 as invalid; be careful that OpenMP parallel writes to per-record flags don't race — write to distinct array slots, not shared accumulators, inside the parallel region. Third, do the bucketing/averaging as a strictly sequential pass afterward (a hash map or sorted array of `(ts/10)*10` keys, summing values and counts), since correctness here matters more than parallelism. Fourth, sort buckets by key and compute the rolling average per the exact formula given (first bucket = its own average, else average of current and prior bucket average) — implement this only over buckets that actually have valid records. Finally, format each row with `%.2f` and sorted ascending order, writing the header first. Compile with `-fopenmp -lm` and diff your output against expectations by re-running your own script over the same input to check internal consistency.