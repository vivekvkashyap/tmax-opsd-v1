Work top-down through the request chain, verifying each hop independently before testing end-to-end. First fix `nginx.conf`'s `proxy_pass` to point at the correct unix socket path (`proxy_pass http://unix:/home/user/app/private/api.sock:;` — note the required trailing colon syntax for nginx unix-socket proxying), then reload/restart nginx and confirm with `nginx -t` before curling. Second, grant ACLs precisely as specified: `setfacl -m u:guest:rw /home/user/app/private/api.sock` for the socket and `setfacl -m u:guest:x /home/user/app/private` for the parent directory (traversal requires execute on the directory even if rw is granted on the socket itself) — verify with `getfacl` on both paths rather than assuming success. Third, write `sanitize_subject.c` as a simple stdin-to-stdout byte filter: loop `getchar()` until EOF, and for each byte check `isalnum()` or space, uppercase and print with `putchar(toupper(c))`, otherwise discard it silently (including newlines) — compile with `gcc` and confirm it exits 0. Test each layer separately (echo bytes directly into the compiled binary; curl the socket directly bypassing nginx; then curl through nginx) before checking the full SMTP negotiation via the Expect script, since a fuzz-equivalence check will compare your program's behavior against a reference across many byte-sequence inputs.
