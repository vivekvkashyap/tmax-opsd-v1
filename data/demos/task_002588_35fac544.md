Fix each of the three bugs independently and verify each in isolation before wiring the pipeline together. In `server.py`, wrap the extracted path segment in `urllib.parse.unquote(...)` before passing it to `mathstack.eval()`, so `%20`/`%2D` etc. decode to spaces/minus signs. In `mathstack.c`, the fixed `int stack[5]` is the real bug — bump it to `int stack[100]` (and add a bounds check on push if you want to be defensive) so deep expressions don't overflow the stack and segfault. For the DIV operator, C's `/` truncates toward zero but Python's `//` floors toward negative infinity, so they differ whenever signs differ and there's a remainder; replace the raw `a / b` with a floor-division formula, e.g. compute `q = a / b; if ((a % b != 0) && ((a < 0) != (b < 0))) q -= 1;`, and test it directly against a case like `-17 / 5` to confirm you get -4, not -3. Rebuild with `python3 setup.py build_ext --inplace`, restart the server on 8080, then curl the exact URL-encoded expression given in the task and save its plain-text response to `/home/user/verification.log`.
