Build this as a single Python script executed step by step. Use the `tarfile` module to extract `/home/user/artifacts.tar.gz` into `/home/user/extracted`. For each extracted file, open it in binary mode and read exactly the first 24 bytes: bytes[0:4] must equal `b'ARTF'` (if not, delete the file with `os.remove` and skip it); bytes[4:20] is the name field — decode as ASCII and strip trailing `\x00` with `.rstrip(b'\x00').decode()`; bytes[20:24] is a little-endian uint32 version, unpack with `struct.unpack('<I', ...)`. Rename the file (keep its full contents, header included, unless the task implies otherwise — check byte offsets carefully) to `<name>_v<version>.bin` using `os.rename` inside `/home/user/extracted`, recording old→new name pairs as you go. Build the registry dict as `{new_name: original_name}`, serialize with `json.dump` to a temp file in the same directory (e.g. via `tempfile.NamedTemporaryFile(delete=False, dir='/home/user')`), then call `os.replace(tmp_path, '/home/user/registry.json')` for atomicity — this is the key non-obvious requirement, a plain `open(...).write()` directly to the final path would not satisfy the atomic-write constraint. Finally use `zipfile.ZipFile` to write only the renamed `.bin` files into `/home/user/curated.zip`, passing `arcname=os.path.basename(path)` so no directory prefix is embedded.
