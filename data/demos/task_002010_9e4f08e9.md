Reverse-engineer by combining behavioral testing with light static inspection. Run `/app/legacy_scanner /home/user/sample_logs.txt` and look line-by-line at which entries get which status; cross-reference each `LogID | Hash | Payload` row against the payload to see when the classification changes. Compute the hash of each payload yourself with a few candidate algorithms/inputs (e.g. `md5sum` on just the payload substring, trying with/without a trailing newline or delimiter) to figure out exactly what's being hashed and with what algorithm — mismatches here explain `INTEGRITY_FAIL` rows. For rows where the hash matches, look for payloads containing case-insensitive `UNION SELECT` or `DROP TABLE` substrings to identify what triggers `INTRUSION_DETECTED` vs `CLEAN`; use `strings`/`objdump -d /app/legacy_scanner` to confirm the crypto library used (e.g. OpenSSL MD5 symbols) and check for case-folding logic in the disassembly if unsure. Implement `scanner.c` reproducing this three-way branch exactly (integrity check first, then pattern match, then clean), reading the file path from argv, printing `ID: STATUS` lines in the same order. Validate your C binary's output against the legacy binary's output on the full sample file (diff them) before compiling the final optimized version with gcc -O3 and linking `-lcrypto`.