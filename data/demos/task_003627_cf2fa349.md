Parse the PDB by iterating `MODEL`/`ENDMDL` blocks and, within each, collecting only ATOM lines whose atom-name field (columns 13-16 in fixed-width PDB format, or the second whitespace-split token if the file is simply formatted) equals exactly `CA`; store per-model coordinates indexed by the residue sequence number so the same residue's atom lines up across models even if their order varies. Stack the coordinates into shape (num_models, num_residues, 3) via numpy, compute the mean position per residue across the model axis, then compute RMSF per residue as the square root of the mean squared Euclidean deviation from that mean across models (per the formula given) — this is straightforward with `numpy.mean` and `numpy.linalg.norm` or manual sum-of-squares, so verify your implementation on one residue by hand-computing it from the raw coordinates before trusting the full result. Find the residue index with the maximum RMSF, then open the FASTA file, strip the header line, and index into the concatenated sequence string using 1-based residue numbering to get the corresponding amino acid letter. Write the result to `/home/user/highest_fluctuation.txt` in exactly the specified format, rounding RMSF to 3 decimal places with standard rounding (not truncation).
