First run `tesseract /app/schema_spec.png -` to recover the exact extraction regex pattern and the retry deduplication window (in ms) printed in the image — do not hardcode guessed values, read them from the OCR output (clean up any OCR artifacts by eye before using the pattern). Apply that regex to every line in `/home/user/raw_data/*.txt` to pull out event ID, raw user token, and the legacy Year/Day/Month timestamp string; parse the timestamp with `datetime.strptime(s, "%Y/%d/%m %H:%M:%S.%f")` (note day-before-month, easy to invert by mistake) and convert to a UNIX timestamp for comparison and to ISO 8601 for output. Normalize the token: lowercase, strip whitespace, then remove non-alphanumeric chars via `re.sub(r'[^a-z0-9]', '', token)`. Group records by normalized token, sort each group by timestamp, and greedily keep a record only if no earlier-kept record in that group is within the recovered window (in milliseconds) — this correctly collapses chains of near-duplicate retries. Write the surviving records sorted globally by timestamp to `/home/user/clean_events.txt` using the exact template, and log counts to `/home/user/etl.log`.