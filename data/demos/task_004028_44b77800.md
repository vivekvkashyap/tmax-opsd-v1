Build this bottom-up and test each stage in isolation before wiring the server. First load both files into SQLite (`sqlite3 /home/user/finance.db` with `.import` after converting `clients.json` to CSV via `jq -r`), then run a join query filtering `status='active'` to get the valid transaction set — verify the row count by hand against the input files. Next, for each valid transaction, shell out to `/app/tx_signer <tx_id> <amount>` and capture its stdout as the signature; treat it strictly as a black-box oracle — don't try to reverse-engineer its algorithm, just call it correctly with the exact positional args and amount formatting it expects (test with the example given first). For the TCP server, use `socat TCP-LISTEN:9000,fork,reuseaddr EXEC:handler.sh` (or an `nc -l` loop) pointing at a bash handler script that reads a line matching `GET <client_id>`, greps/queries the precomputed valid-transaction+signature data for that client (a flat file or SQLite query keyed by client_id is simplest), prints each match as `tx_id,amount,signature`, then always prints `END`. Test manually with `nc localhost 9000` for an active, a suspended, and a nonexistent client before leaving it running.
