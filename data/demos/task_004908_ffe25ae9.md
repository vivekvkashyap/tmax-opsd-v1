Start by transcribing `/app/directive.wav` yourself (use an available speech-to-text tool or library) to discover the exact operational rejection rule stated in the audio — do not guess the field/value from the task description, since the description deliberately withholds it and only the audio has the real constraint. Once you have the transcript, identify the concrete JSON field and value it names and implement that as your second validation rule. For the C program: parse each JSONL line looking for `\u` escape sequences and verify each is followed by exactly 4 hex digits (reject on EOF, wrong length, or non-hex chars) — a naive scanner that just checks "starts with \u" misses truncated or malformed sequences at line boundaries. Implement the operational-rule check as a substring/field match on the field the audio specifies. Parallelize with OpenMP or pthreads by splitting lines across threads for validation (e.g. `#pragma omp parallel for` over a pre-read array of lines) but preserve original line order and 1-indexed line numbers for both the output file and `/home/user/dropped.log` — parallel workers must not race on writing dropped.log or reorder output; collect per-line pass/fail into an array first, then write sequentially. Test against both a clean and a deliberately malformed/critical-severity sample before considering it done.
