Build `/home/user/fix_project.py` as three independent, testable stages. (1) Patch decoding: read `/home/user/encoded_patch.txt`, strip the `ENC: ` prefix from each line, hex-decode with `bytes.fromhex(hexstr).decode('utf-8')`, concatenate the decoded lines back into a valid unified diff, write it to a temp file, then apply it — either invoke the system `patch` command (`subprocess.run(['patch', target_file, '-i', tmp_diff])`) or implement a small unified-diff applier; verify afterward that `math_ops.rs` no longer contains the buggy `a - b`/`a / b` lines and instead has correct `+`/`*` operators. (2) Prime generation: implement a straightforward sieve or trial-division primality check to compute the first 100 primes programmatically (do not hardcode the list from memory — compute it, since an off-by-one in "2 is the first prime" is an easy naive mistake), then format them into the exact Rust const-array syntax and write `constants.rs`. (3) Benchmarking: for each of the inputs 10, 100, 1000, invoke `python mock_rust_binary.py <n>` as a subprocess, wrap the call with `time.perf_counter()` before/after to get wall-clock seconds, and dump a dict keyed by the string of each input to `benchmark.json`. Run the whole script end-to-end and re-check each output file against its own requirement (patched file, exact 100-prime array, benchmark JSON keys/types) before finishing.