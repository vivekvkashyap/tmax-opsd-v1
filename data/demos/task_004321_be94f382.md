Set up a standard Rust binary crate (`cargo new`) using the `csv` and `serde` crates, but read rows manually field-by-field rather than deserializing directly into a strict struct, so a parse failure on one field lets you skip the row instead of aborting the whole read. For each record, attempt `id.parse::<u32>()`, and `feature_a`/`feature_b`/`target` as `f64`; if any parse errs (empty string, or a float literal like `"4.0"` failing u32 parsing), `continue` to the next row rather than panicking or unwrapping. Only accumulate valid rows for the regression. Compute `pca_approx = (feature_a + feature_b) / 2.0` per valid row, then run ordinary least squares by accumulating running sums of X, Y, XY, X² across valid rows (not by pulling in a heavy regression crate) and applying the closed-form slope/intercept formulas exactly as given. Round both final values to 4 decimal places before serializing to `/home/user/regression_result.json` with `serde_json` or manual string formatting matching the exact key names and shape. Build with `cargo build --release` (or run via `cargo run`) and actually execute the binary so the JSON file is produced, not just compiled.