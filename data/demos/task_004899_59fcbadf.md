Build this in stages and validate each numerically before moving on, since a bug anywhere in the FFT→normalize→dot-product→Cholesky chain silently produces plausible-looking but wrong diagonals. Read the 4 lines from the input file, map A/C/G/T to 1.0/2.0/3.0/4.0 into `double[1024]` arrays. Use `fftw_plan_dft_r2c_1d(1024, in, out, FFTW_ESTIMATE)` — remember the r2c output has 1024/2+1 = 513 complex bins, not 1024; allocate with `fftw_alloc_complex`. Power spectrum is `re*re+im*im` per bin. Normalize each 513-length vector to sum to 1.0 (sum first, then divide — don't divide before summing). Build the 4x4 matrix M as pairwise dot products (M is symmetric, so compute only the upper/lower triangle and mirror it), then add 0.01 to each of the 4 diagonal entries. Call `LAPACKE_dpotrf(LAPACK_ROW_MAJOR, 'L', 4, M, 4)` and check its return code is 0 (nonzero means M wasn't positive-definite — a sign your dot products or diagonal bump are wrong). The four diagonal elements of the now-overwritten M are your answer; print with `%.6f`. Wrap the 4 sequences' FFT+power+normalize step in an OpenMP `#pragma omp parallel for`. Compile with `-fopenmp -lfftw3 -llapacke -lm`. Sanity-check by re-deriving the same pipeline in a scratch Python/numpy script and diffing before trusting the C output.