Write `/home/user/pipeline.go` as a straightforward sequential pipeline using `encoding/csv` and `encoding/json` from the standard library — no need for concurrency here. Read all rows, skipping the header. Stage 1: drop any row where `error_code` is non-empty (careful with whitespace-only values — trim before checking). Stage 2: parse `reading` as float64 with `strconv.ParseFloat` and drop rows outside `[0.0, 1000.0]` inclusive (the spec says drop strictly less than 0 or strictly greater than 1000, so boundary values 0.0 and 1000.0 are kept). Stage 3: accumulate a `map[string]struct{Sum float64; Count int}` keyed by `sensor_id`, then compute `average = Sum/Count` per sensor when building the output. Stage 4: define a struct with JSON tags `average`/`count` (or an equivalent map), marshal with `json.MarshalIndent` or `json.Marshal`, and write to `/home/user/clean_summary.json`. After writing the Go file, run it with `go run pipeline.go` (or build+execute) to actually produce the output file — the task is only satisfied once the JSON exists on disk with real computed values.