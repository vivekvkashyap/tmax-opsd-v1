Break this into a clean pipeline of independently-testable stages. First, extract frames from the video with `ffmpeg` (e.g. sample every frame or use scene detection) and decode QR codes from each with `pyzbar`/`opencv`, deduplicating identical decoded payloads if the same code spans several consecutive frames; parse each decoded string as JSON to get `{src, dst, ts, action}` events — verify your decode count/sample against the frame-interval description before trusting it. Second, build a directed multigraph (e.g. with `networkx` or a plain adjacency structure keyed by node) where each edge retains its timestamp, since the graph is NOT a simple reachability graph — a path A→B→C is only valid if the edge timestamps are non-decreasing along the path (time-respecting walk), so a naive BFS/DFS ignoring timestamps will overcount reachable nodes and paths. Implement path counting via DFS/BFS from `IP_EXT_994` that only extends a path through an edge whose timestamp is >= the last edge's timestamp in that path, counting each distinct time-ordered path to each `System_*` node as its vulnerability score (watch for cycles/revisits inflating counts — decide and apply a consistent policy, e.g. no repeated nodes per path). Finally sort by score descending then alphabetically, filter to `System_` prefixed nodes only, take the top 50, and write the two-column CSV with a header row and exactly 50 data rows.
