First fix the vendored library in isolation: open `/app/tiny_sql_builder/builder.py`, find the `Window.__str__` method, and add the missing branch that appends `ORDER BY {self.order_by}` (with correct spacing/comma handling when both `PARTITION BY` and `ORDER BY` are present) before the closing paren — verify by instantiating `Window(partition_by="user_id", order_by="timestamp")` directly in a REPL and checking the string equals `OVER (PARTITION BY user_id ORDER BY timestamp)` exactly, since the verifier checks this string literally. Then write `aggregate.py`: read JSONL from stdin, load rows into an in-memory `sqlite3` connection (`:memory:`), filter to `status='deadlock'`, and build a query using the fixed `tiny_sql_builder` with a window function (`LAG(timestamp) OVER (PARTITION BY user_id ORDER BY timestamp)`) to compute consecutive gaps per user, then aggregate with `MAX` grouped by `user_id`, keeping only users with count(deadlocks) >= 2. Sort results by `user_id` ascending and emit the exact JSON schema (`{"results": [...]}`, or `{"results": []}` when empty). Test with hand-built small JSONL streams covering edge cases (0, 1, and 2+ deadlocks per user) to confirm determinism before relying on the fuzz harness.
