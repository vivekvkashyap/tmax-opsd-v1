Build the FastAPI endpoint first for correctness: on startup, pre-extract every frame of `/app/project_video.mp4` (it's only a few seconds) into an in-memory list or `/tmp` cache — e.g. one `ffmpeg` invocation that dumps all frames as JPEGs (`ffmpeg -i video.mp4 -q:v 2 /tmp/frames/%04d.jpg`), loading them into memory/dict at startup — rather than shelling out to `ffmpeg` per request, which is the throughput killer the task warns about (5-10 req/s vs the required ≥150 RPS threshold). The handler for `GET /api/v1/frame/{frame_number}` should just look up the precomputed bytes, base64-encode them, then reverse the resulting string (`encoded[::-1]`) before returning it in the JSON body. Write `/home/user/app/server_ready.log` containing `READY` only after pre-extraction completes, not before, since the verifier polls that file before load-testing. For the pytest suite, patch/mock the frame-extraction function with `unittest.mock.patch` to return a small fixed black JPEG buffer, then assert the response's `data` field, when reversed and base64-decoded, matches the mocked JPEG bytes exactly. Start the server with `uvicorn` in the background (e.g. `nohup ... &`) before finishing.