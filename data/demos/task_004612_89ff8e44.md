Diagnose the 502 systematically instead of guessing: first check whether `/home/user/app/sockets` is a symlink (`ls -la`) — if it's dangling, `rm` the symlink and `mkdir` a real directory in its place, then `chmod 755` it explicitly. Add `export BACKEND_SOCKET=/home/user/app/sockets/backend.sock` to `/home/user/.bashrc`, but remember `.bashrc` isn't sourced by non-interactive scripts, so also export it in the current shell (or source it) before starting the backend so `start_backend.sh` actually sees it. Read `start_backend.sh` to see how it binds the socket (likely via `socat` or similar) and add a permission fix (e.g. `chmod 777` or appropriate `umask`) right after the socket is created so Nginx (running as a different/same user) can read/write it. Write `rotate_logs.sh` to `mv` the error log to `.log.1` then send `SIGUSR1` to the Nginx master PID (read from the pid file Nginx writes) and `chmod +x` it. Finally start Nginx with the given `-c`/`-p` flags, launch the fixed backend script in the background, and only then `curl` — checking each layer (socket dir, permissions, env var, nginx config) if the curl still fails rather than restarting blindly.