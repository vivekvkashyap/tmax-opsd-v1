Fix both CWEs by reading the reqwest/rustls docs for certificate pinning rather than guessing. For CWE-295: remove `.danger_accept_invalid_certs(true)` and instead load `/home/user/ca.crt`, parse it with `reqwest::Certificate::from_pem(&bytes)`, and add it to the client builder via `.add_root_certificate(cert)` (keep default validation on, don't disable hostname checks). For extracting the cookie: after the GET request, inspect the response's `Set-Cookie` header value (via `response.headers().get_all("set-cookie")` or similar), and parse out the substring between `session_token=` and the next `;` — do this programmatically with string splitting, not by hardcoding. Hash that extracted value with the `sha2` crate (`Sha256::digest`), and format the digest as lowercase hex. For CWE-732: when creating `/home/user/cookie_hash.txt`, don't rely on default umask — explicitly set permissions to `0600` using `std::os::unix::fs::OpenOptionsExt::mode(0o600)` on the `OpenOptions` used to create the file (or `fs::set_permissions` immediately after). Compile with `cargo build --release` and run the binary, then verify with `stat -c "%a"` and by recomputing the SHA-256 yourself to sanity-check the output.
