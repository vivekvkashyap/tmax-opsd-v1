First recover the secret 4-digit ID from `/app/intercept.wav` by transcribing it — try `ffmpeg`/audio tools plus a local speech-to-text approach (e.g. pocketsphinx, whisper if available, or even careful manual listening via spectrogram/playback) rather than guessing; the ID is spoken as words, so map spoken digits to the numeric string. Do not hardcode a guessed number — derive it from the actual audio. Then write `/home/user/validator.cpp`: read argv[1], skip the header line, split each remaining line on commas into the 5 fields, and reject the file (exit 1) as soon as any row fails: user_id equals the recovered secret, timestamp is empty/non-numeric/negative (validate every character is a digit, optionally with a leading '-' making it negative), or status is not exactly "OK"/"WARN"/"ERR" (use strict string equality, not substring match). If every row passes, exit 0. Compile with `g++ -O2 -o /home/user/validator /home/user/validator.cpp`. Before finishing, loop over every file in `/app/corpus/clean/` and `/app/corpus/evil/`, run the validator, and check the exit codes match expectations — fix any mismatches by re-checking edge cases like trailing whitespace or extra CSV columns.