Work through this as three separable fixes plus an integration, each independently checkable. (1) Fix the Makefile: rename the compiler variable `CX` to `CXX` (or reference it consistently) and add `-fPIC` to the compile flags so the shared library actually builds. (2) Fix the non-determinism: in `bootstrap.cpp`, the `#pragma omp parallel for reduction(+:sum)` over bootstrap samples produces different floating-point rounding depending on thread scheduling — remove the pragma (or otherwise force strictly sequential summation) so the sum accumulates in the same order every run; rebuild the `.so` and confirm two consecutive runs on identical input produce byte-identical output before moving on. (3) Write `analyzer.cpp`: read the raw float32 file, infer N from file size / (128*4), transpose so you extract each of the 128 bins' N values contiguously (input is row-major by observation, so bin i's values are at stride 128 starting at offset i), call `compute_ci(data, n, 42 + i, mean, lower, upper)` per bin using the fixed per-bin seed, and write the 128 `[mean, lower, upper]` float triples in bin order to the output file. Compile with `-O3`, link against your fixed `libfast_bootstrap.so`, and validate determinism by diffing repeated runs before trusting equivalence to any reference.
