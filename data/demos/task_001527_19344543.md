Work top-down through the request chain: fix the C++ service first (it's the root cause), then Nginx, then supervision. For the C++ backend, read `backend.cpp` and `httplib.h` closely — check the listen address/port passed to `svr.listen(...)`, and note there is no code at all for the raw TCP status socket on port 9001, so you must add a second thread/socket that accepts connections, reads until it sees `STATUS\n`, and writes `OK\n`. For `/process`, shell out to `ffmpeg -i /app/surveillance.mp4 -vf fps=1 <dir>/frame_%03d.png` (or similar) to extract frames, then decode each PNG and check whether its pixels are (near-)exactly RGB(255,0,0) to count red frames — don't hardcode a count. Use a JSON/image library or write minimal PPM/PNG decoding logic; consider `stb_image.h` if simplicity is needed. Archive the frame directory with `tar -czf /home/user/backup/frames.tar.gz <dir>` after `mkdir -p`. Only after the binary correctly binds 9000/9001 should you fix the Nginx `proxy_pass` typo pointing at the wrong port. Finally write `supervisor.sh` as a `while true; do ./backend; done` loop and launch it with `&`, then start nginx and verify both ports respond before finishing.
