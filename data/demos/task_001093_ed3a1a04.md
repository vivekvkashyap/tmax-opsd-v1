Build the pipeline in clear stages and verify each numerically before moving on. Parse `protein.fasta` by stripping the header line and counting remaining sequence characters; parse `protein.pdb` with a simple fixed-column or whitespace-aware ATOM-line parser (or Biopython's PDBParser) filtering for `CA` atom records, and raise `ValueError` if the CA count doesn't match the FASTA length — check this invariant explicitly rather than assuming it holds. Compute the center of mass with `numpy.mean(coords, axis=0)` and per-atom distances with `numpy.linalg.norm(coords - com, axis=1)` — do the math yourself rather than approximating, since a downstream check recomputes CoM/distances independently. Fit with `scipy.stats.gamma.fit(distances)` and dump `{"a":..., "loc":..., "scale":...}` as native Python floats (not numpy types, which aren't JSON-serializable) via `json.dump`. For the plot, use `matplotlib`: `plt.hist(distances, density=True)` plus a line plot of `gamma.pdf` over a linspace, saved with `plt.savefig`. For the pytest file, hardcode the small 3-point example and assert against manually derived expected distances with `np.testing.assert_allclose`, and assert `params[0] > 0` for the gamma shape test — run `pytest` yourself and confirm exit code 0 before finishing.
