Split this into two independent deliverables and test each against its own fixture before wiring them together. For frame extraction, run `ffmpeg -i /app/video.mp4 -vf fps=10 /home/user/frames/frame_%04d.jpg` (zero-padded numbering keeps `frame_stats.csv` naturally sortable), then loop over the frames with ImageMagick `convert <frame> -format "%[fx:mean]" info:` to get mean brightness per frame, writing frame_number,mean_brightness sorted numerically (not lexicographically — extract the numeric index rather than relying on string sort if padding could differ). For the leakage detector, the anomaly is specifically exact duplicate rows appearing in both the first half and second half of a CSV (not just any duplicate row) — read each file's data rows (excluding header if present), split at the midpoint, and check whether any row content in the first half exactly reappears in the second half (e.g. hash each row and compare set intersection across the two halves); a script that only checks global duplicates anywhere in the file will misclassify files where duplicates happen to sit within the same half. Make both scripts executable, run filter_datasets.sh against both corpora redirecting stdout to the two results files, and confirm exit code 0 in both runs.
