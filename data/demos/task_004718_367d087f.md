Write `/home/user/test_pipeline.sh` starting with `export LC_NUMERIC=C`. In an `awk` pass, filter valid rows with the extended regex `^-?[0-9]+(\.[0-9]+)?$` on field 2 (skipping the header via `NR>1`), accumulating `sum`, `sumsq`, and `count` to compute `mean = sum/count` and population `stddev = sqrt(sumsq/count - mean^2)` (population, not sample — divide by `count` not `count-1`). Then do a second pass (a second `awk` invocation, or re-reading the file inside `END` via `getline`) that re-validates each row's numeric format, computes `z = (value-mean)/stddev`, and keeps rows where `abs(z) <= 2.0` (i.e., excludes strictly `> 2.0`), writing the header plus surviving rows in original order to `/home/user/clean_output.csv`. Wrap steps 2-4 in a loop that runs 5 times, capturing elapsed time with `time` or `$SECONDS`/`date +%s.%N` deltas around the loop, and append `Benchmark complete. 5 iterations took [X] seconds.` to `/home/user/report.log` with the measured total substituted for `[X]`. Use only `awk`/`grep`/`sed`, no Python/Perl/R. `chmod +x` and run the script to produce both output files.