Debug this incrementally rather than guessing. First run `/home/user/run_processor.sh` as-is and read the traceback: it will show `int()` failing because the exported env var has a non-numeric suffix — fix the script so the value passed to `int(os.environ.get(...))` is a clean integer string. Second, rerun and observe the next crash: it's `json.loads` raising `JSONDecodeError` on a truncated line in `uptime.jsonl`. Wrap the per-line `json.loads` call in `process_logs.py` in a `try/except json.JSONDecodeError`, and on failure append the original raw line (unmodified, including its trailing `\n`) to `/home/user/invalid_logs.txt` via a file handle opened in append mode — do not re-serialize or strip it, since the raw bytes must match exactly. Keep incrementing `valid_count` only for lines that parse successfully. Third, factor that identical try/except parsing logic into a standalone `/home/user/mre.py` that hardcodes the two given JSON strings inline (no file I/O) and demonstrates the same catch behavior. Run both scripts at the end to confirm no crash and that the invalid log file contains exactly the one bad line.