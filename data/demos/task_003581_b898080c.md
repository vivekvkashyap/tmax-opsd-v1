Diagnose before patching: read `/home/user/account_daemon.c`'s socket setup carefully — a "connection refused" despite the process seemingly running is a classic symptom of the port field being set in host byte order instead of network byte order. Confirm this by inspecting the `sin_port` assignment; fix it by wrapping the port number in `htons()` rather than touching any other logic (the task explicitly forbids changing prompts/interaction flow). Compile with `gcc -o /home/user/account_daemon account_daemon.c` and launch it detached (e.g. `&` plus `disown`, or `nohup ... &`) so it survives your session and keeps listening on `127.0.0.1:8888`; verify with `ss -tlpn | grep 8888` before moving on. For the Expect script, `spawn nc 127.0.0.1 8888`, then use `expect`/`send` pairs matching the daemon's exact prompt strings (`expect "ADD/DEL/QUIT): "`, `send "ADD\r"`, `expect "Username: "`, `send "admin_xyz\r"`, then `expect "Success. Account created."`) — test it interactively first so you know the exact prompt text before scripting blind matches. Run it with output redirected to `/home/user/result.log` and re-check the daemon is still alive afterward.