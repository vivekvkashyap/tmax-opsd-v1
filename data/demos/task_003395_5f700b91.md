Parse the log with a strict regex anchored to the exact pipe-delimited format, filtering on all three conditions at once (user == netadmin, action == KEY_EXCHANGE, status == REJECTED_SILENTLY) so the 9,900 distractor lines with different users/actions/statuses are excluded. Extract only the KEY_DATA field and deduplicate with a set before writing authorized_keys (one key per line) — missing dedup or over/under-matching lines directly hurts the Jaccard-similarity component the oracle scores. For fstab_snippet, write one syntactically valid tmpfs mount line for /home/user/keys_mnt including noexec, nosuid, and nodev as comma-separated mount options (the oracle checks these tokens are literally present). For group_snippet, match the exact form the oracle expects: `groupname:x:GID:member` with group "ssh_mgrs" and member "netadmin" — test against a regex like `^ssh_mgrs:x:\d+:netadmin$` mentally before writing. Build the whole pipeline in the Python script itself (parse → write bundle → subprocess.run the oracle binary → print its JSON), then iterate: run it, read the accuracy breakdown, and adjust whichever sub-score (fstab/group/keys) is low.
