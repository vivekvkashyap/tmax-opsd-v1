Treat this as two separable problems: (1) get the multi-service pipeline wired correctly, (2) make the Python 3 evaluator numerically match the old Python 2 worker. For wiring, read `gateway.js` and `start.sh` carefully and fix the Redis port and queue-name constants exactly as specified, then start services with `./start.sh` and confirm Redis, the gateway, and your worker are all actually running (check ports/processes, don't just assume). For the worker, write a small recursive-descent parser for the `(OP arg arg)` prefix grammar (arguments can themselves be nested expressions), evaluate over 32-bit signed integers, and implement `--cli <b64>` mode first so you can unit-test parsing/eval in isolation before wiring up Redis BLPOP/LPUSH in service mode. The core correctness trap is DIV: Python 3's `//` truncates toward negative infinity same as Python 2's `/` for ints, but Python 2's C-level integer division in some implementations truncates toward zero — verify the actual expected behavior by testing signed cases like `-10 // 3` yourself and implementing whichever floor-vs-truncate rule matches, don't assume `//` is automatically correct. Validate end-to-end with the curl command given before considering it done.
