Work in three strictly ordered phases: fix the library, pick the model, build the server. In the vendored `Makefile`, look for a hardcoded `CFLAGS` that ignores overrides and a missing `-lm` on the link line; fix so `CFLAGS ?= -O3` works and math functions link, then confirm `make CFLAGS="-O3"` actually rebuilds with the new flag. In `cml.c`, find where batch inference calls a `fit_transform`-style function that recomputes scaling stats from the current batch — that's the leak; replace it with the existing `cml_transform_scale`, which uses the model's own pre-stored mean/std, so a given input predicts identically regardless of batch. Verify by running the same feature vector in two different batches and confirming identical output. For model selection, load each `.bin` model, score it against `val.csv` (4 features + target, no header) by MSE, and keep the lowest. For the TCP server, use a blocking accept loop (`socket`/`bind`/`listen`/`accept`), read line-by-line, strictly require the AUTH line before anything else, and format predictions with `%.4f`.
