Work the three sub-problems in order and verify each before moving on. First, diagnose the cJSON build failure yourself: run `make` in `/app/cJSON-1.7.15`, read the actual compiler error, and check both the Makefile's compiler flags (a flag like `-fPIC` for shared-library builds is easy to typo) and the C source for an invalid type name where a standard type (e.g. `double`) is expected — fix only what's broken, then confirm `libcjson.so` is produced. Second, in `inference_pipeline.c`, find the CSV field-parsing logic; when a field is empty or the literal "NaN", force the parsed value to the integer `-1` before it reaches JSON serialization, and use `cJSON_CreateIntArray`/`cJSON_AddNumberToObject` in a way that writes true JSON integers (not floats) — check the actual output for stray decimal points. Third, locate `malloc`/`free` calls executing inside the per-row inference loop and hoist the allocation outside the loop (allocate once, reuse per iteration), since that's the main win for the required 2x+ speedup. Compile, run `/home/user/bench.sh` yourself to measure the achieved speedup and inspect `experiment_log.json` for stray floats before declaring done.