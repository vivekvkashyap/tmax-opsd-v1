Do the backup before touching any files, since the CSVs must still exist at backup time. Use `rsync -a` (archive mode preserves structure/timestamps) with trailing slashes on both source and destination, e.g. `rsync -a /home/user/old_projects/ /home/user/backup_drive/projects_backup/`, so it copies only new/changed files and leaves the already-current backup file untouched.

For the Python script, walk the tree with `os.walk`, and for each file ending in `.csv`: open it, parse with `csv.DictReader` to get header-mapped rows, collect them into a list of dicts, then write that list with `json.dump` to a sibling file with the same base name but `.json` extension in the same directory. Only after the JSON write succeeds, delete the original `.csv` with `os.remove`. Skip any file already ending in `.json` — never open, rewrite, or touch those. Track every path you just created (not pre-existing JSON files) in a list, `sorted()` alphabetically, then write it to `/home/user/conversion_log.txt` as absolute paths, one per line, using `os.path.abspath`. Watch for edge cases: mutating a directory listing while iterating it, and making sure the log only contains conversions from this run.
