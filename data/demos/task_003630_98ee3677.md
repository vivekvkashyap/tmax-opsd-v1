Read `simulation.c` first and actually run it against `sensor_data.txt` to observe the hang, rather than guessing: the bug is that `fscanf(file, "%f", &value)` on a non-numeric line returns 0 without consuming the offending token, so the `feof`-driven loop spins forever re-reading the same bytes. Fix this by explicitly consuming/skipping unparseable input on failure — e.g. when `fscanf` doesn't return 1, read and discard the rest of that line (or the next token) before retrying — or restructure to read line-by-line with `fgets` and attempt `sscanf` per line, silently skipping lines that don't parse as a float. Separately, change the accumulator (and the variable receiving each parsed value if it affects precision) from `float` to `double` to avoid precision loss from summing many small values; keep the `printf("Total Sum: %.4f\n", ...)` format exactly as specified. Compile with `gcc simulation.c -o sim_fixed`, run it redirecting stdout to `/home/user/sim_profiling/final_result.txt`, and sanity-check the result against what you'd expect from summing the known-good (non-corrupted) values in the dataset.
