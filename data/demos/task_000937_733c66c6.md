Treat this as three independent deliverables that will each be tested automatically, so build and self-test each before moving to the next. For Phase 1, edit `dnsmasq.conf` to add an `address=/c2-malware.local/127.0.0.1` style entry and edit `nginx.conf` to listen on 8080 with a `proxy_pass` to `127.0.0.1:5000`; restart/reload both services, then self-check with `dig @127.0.0.1 c2-malware.local` and `curl http://c2-malware.local:8080/ping` before moving on. For Phase 2, read `parser.c` carefully around the TLV/length-prefixed parsing loop — look specifically for a boundary check using `<=` where it should be `<` (off-by-one buffer write) and a chunk-reading loop whose termination condition can never become false on certain inputs (infinite loop); use the pcap and core dump to locate the exact crash site (e.g. `gdb parser core.parser`, `bt`) rather than guessing, fix both bugs conservatively without changing legitimate parsing behavior, and compile to `parser_fixed`. For Phase 3, base `classifier.py` directly on the specific boundary condition you just fixed (e.g. the length field threshold that triggers the overflow) so it generalizes correctly across the hidden evil/clean corpora — reason from the vulnerability, don't hardcode filenames.