This is a strict reproducibility task: the grader recomputes the exact same pipeline independently and diffs your output ID, so any deviation from the specified parameters (stop_words, max_features, n_components, random_state, n_clusters, n_init) will fail even if your approach is "reasonable." First do the ETL step with plain shell tools: keep the header line, then filter rows where the Status column is exactly `Verified` (e.g. `awk -F',' '$3=="Verified"'`), being careful that the naive filter doesn't also match values like "Unverified" if such a status exists — match the field exactly, not a substring. In the Python pipeline, fit TfidfVectorizer(stop_words='english', max_features=200) on the Review_Text column, feed its sparse output into TruncatedSVD(n_components=5, random_state=42), then KMeans(n_clusters=4, n_init=10, random_state=42) on the 5-D embeddings — do not skip or reorder these steps, and do not add any extra preprocessing (lowercasing/stemming) not specified, since it would change the vector space and thus the resulting closest-to-centroid ID. Finally, restrict to rows where the cluster label equals 0, compute Euclidean distance of each such point to kmeans.cluster_centers_[0], find the argmin, and write only that row's original ID (no newline padding, no extra text) to representative_review.txt.