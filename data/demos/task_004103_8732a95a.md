Approach this as black-box characterization before writing any code. Compute the Pearson correlation coefficient (or covariance) between columns X and Y for every file in `/app/sample_data/` yourself (a quick Python/awk script using the standard formula — sum of products minus means, normalized by std devs), then run each file through `/app/preprocess_bin input output` and check whether the output contains `NaN` (`grep -i nan output` or load with numpy and check `isnan`). Tabulate correlation vs. NaN-or-not to find the exact cutoff — test values straddling suspected round numbers (e.g. does |r|=0.99 pass but 0.991 fail?) to pin the boundary precisely rather than assuming it. Once you've empirically nailed the threshold, implement Pearson correlation from scratch in C (running sums of x, y, x², y², xy — no external stats libs), take the absolute value, and compare strictly against the boundary you measured, exiting 0 for safe/1 for evil accordingly. Validate your C validator against the same `/app/sample_data/` files before considering it done, since the hidden grader corpus will test many files close to the boundary from both sides.