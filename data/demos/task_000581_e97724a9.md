Work stage by stage and verify each before proceeding. Stage 1: grep the vendored PyJWT source for an unconditional/injected exception — search `/app/vendored/pyjwt-2.8.0/jwt/api_jwt.py`'s `decode` method for a `raise` statement that isn't part of normal validation logic (e.g. an explicit `raise DecodeError(...)` near the top of the function) and remove just that injected line, preserving the real validation flow; then `pip install -e /app/vendored/pyjwt-2.8.0/` so your scripts import the patched package. Stage 2: read the secret from `/home/user/jwt_secret.key`, iterate lines of `/home/user/captured_tokens.txt`, call `jwt.decode(token, secret, algorithms=["HS256"])` inside a try/except catching `InvalidSignatureError`/`ExpiredSignatureError`/`DecodeError`, collect `user_id` from claims of tokens that decode successfully, sort alphabetically, and write one per line to `/home/user/valid_users.log`. Stage 3: build `redactor.py` with `sys.argv` positional args; use targeted regexes — 13-19 digit sequences allowing dash/space separators for credit cards, `AKIA[A-Z0-9]{16}` for AWS keys, and a JWT pattern requiring three dot-separated base64url segments each of meaningful length (to avoid falsely matching UUIDs, git SHAs, or short hex strings) — replace matches with `[REDACTED]` while leaving everything else byte-identical, including line endings. Stage 4: compute `hashlib.sha256` over the exact bytes of `valid_users.log` and write only the hex digest to `/home/user/audit_checksum.txt`.
