Run `tesseract /app/vnc_screenshot.png -` (or use pytesseract) first and read the extracted text carefully — the exact policy wording (which filesystem type is targeted, which mount option must be present, and where to insert it) only exists in the image, so do not guess a generic fstab hardening rule. Then write the script to: strip the line; if empty or starting with `#`, echo it unchanged; otherwise split on any whitespace (use `.split()` with no separator so multiple spaces/tabs collapse), inspect the filesystem-type field and options field per the discovered rule, apply the modification only when the condition from the policy applies and the target string is not already present in the comma-separated options, then rejoin fields with single spaces. Test manually against edge cases before trusting it: lines with fewer than 4 fields, options fields with existing values vs. empty, non-matching filesystem types, and comment/blank lines — since the verifier fuzzes thousands of randomly generated fstab lines including tabs and irregular spacing, any deviation in the split/join or the append condition will fail equivalence.