Build the pipeline in clear stages within `/home/user/pipeline.py`: (1) use Python's `csv` and `sqlite3` modules to create `/home/user/analytics.db` and load `users.csv`/`transactions.csv` into matching tables with appropriate column types (integer user_id, real amount, text status/tx_date). (2) Design indexes that actually match the query's access pattern — the query filters `users.status='ACTIVE'`, filters `transactions.tx_date` by range, and joins on `user_id`, then groups/orders. Think about which columns SQLite needs to seek through fastest: an index on `transactions(user_id, tx_date)` or `transactions(tx_date, user_id)` to speed the join+range filter, and an index on `users(status, user_id)` (or similar) so the covering conditions avoid full scans. Create indexes with `CREATE INDEX` before running `EXPLAIN QUERY PLAN`. (3) Run `EXPLAIN QUERY PLAN <query>` via `cursor.execute` and write only the `detail` column text (one per row) to `/home/user/query_plan.txt` — check it actually contains "USING INDEX" wording, not "SCAN TABLE", by iterating the plan rows programmatically. (4) Execute the real query, format `total_spent` with `f"{value:.2f}"`, and write `top_users.csv` with a header row via the `csv` module. Test by running the script end-to-end and inspecting the plan output before finishing.