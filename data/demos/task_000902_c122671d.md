This task chains OCR recovery, SQL bug-fixing, graph shortest-path, and an HTTP API. First OCR `/app/schema_diagram.png` with `tesseract` to recover the actual join condition and the "Recovery Key" value written on the whiteboard — read the extracted text carefully rather than guessing column names. Fix `/app/report.sql`: the current query does `FROM backups, data_centers` with no join predicate (an implicit cross join, producing a row for every backups×data_centers pair); rewrite it as an explicit `JOIN data_centers ON backups.dc_id = data_centers.id` using the condition recovered from the image, and add a `WHERE backups.encryption_key = '<recovered-key>'` filter using the key you OCR'd. For the graph endpoint, implement Dijkstra (or since likely small, even Bellman-Ford) over `network_links(source_dc, target_dc, latency_ms)` to find lowest-total-latency path between `src` and `dst` query params, returning `{"path": [...], "total_latency": int}`. Wrap the fixed SQL execution and the pathfinder in a Python HTTP server (Flask/FastAPI) bound to `127.0.0.1:8000` with `/api/report` and `/api/route`, matching the exact response schemas, and leave it running in the background for verification.
