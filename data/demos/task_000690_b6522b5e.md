Run `tesseract /app/experiment_label.png stdout` (or similar) to OCR the label image, then parse out the identifier following the literal prefix `EXP_ID:` — trim whitespace/newlines carefully since OCR often adds trailing junk. Use that recovered `<IDENTIFIER>` (do not hardcode a guess) for every subsequent step. To find matching files, recursively walk `/app/raw_data/` with `find`/`os.walk`, and for each file dispatch by extension: for `.json` load and check top-level `experiment_id`; for `.csv` use a CSV reader and check the `experiment_id` column across rows; for `.xml` parse with `xml.etree.ElementTree` and look for an `<experiment_id>` tag anywhere in the tree. Copy every match (flattened, no subdirectories) into `/home/user/organized_dataset/<IDENTIFIER>/`, then compute `hashlib.sha256(...).hexdigest()` for each copied file and write a `{filename: hexdigest}` manifest.json (excluding itself). Finally serve `/home/user/organized_dataset/` with a background HTTP server on `0.0.0.0:8000` (e.g. `python3 -m http.server 8000 --bind 0.0.0.0 --directory /home/user/organized_dataset &`) and confirm it responds before finishing, since the process must stay alive for verification.