Run `tesseract /app/config.png stdout` first to recover the actual PORT and AUTH_TOKEN values from the image rather than assuming defaults — parse the OCR text output for the labeled fields. For the C bug, read `fix_algo.patch` and `algo.c` to understand the diagonal-indexing error (`matrix[i]` vs `matrix[i*size+i]`), then apply the patch with `patch -p0 < fix_algo.patch` or by hand-editing to match it exactly; verify the Makefile actually uses TAB-indented recipe lines (a common silent `make` failure is spaces instead of tabs) and correct compiler flags (`-shared -fPIC`) before running `make`. Test `libalgo.so` standalone via a quick `ctypes` snippet computing a known weighted trace to confirm the fix before wiring the server. Build the Flask/http.server app to bind to `127.0.0.1:<extracted-port>`, check the `Authorization: Bearer <extracted-token>` header on every request and return 401 on mismatch/missing, then in `POST /process` flatten the incoming `matrix` (row-major) and `weights` lists into `ctypes` arrays (`(ctypes.c_double * n)(*data)`) and call `weighted_trace` with proper `argtypes`/`restype` set on the loaded `CDLL`. Start the server backgrounded and write its PID to `/home/user/server.pid`, then confirm with a manual authenticated `curl` before finishing.