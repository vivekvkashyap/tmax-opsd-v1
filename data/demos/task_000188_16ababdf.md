Parse `/home/user/transactions.wal` line-by-line with a regex capturing the action and filename, and build a dict keyed by filename that's overwritten on each subsequent line — since the file is chronological, the last write per key naturally gives you the final status; keep only filenames whose final status is `APPROVE`. For each approved file in `/home/user/raw/`, validate signature by reading raw bytes: check `open(path,'rb').read(4) == b'\x7fELF'` for ELF, and for GCode read as text and confirm the first line starts with `;` (or is blank) and that at least one subsequent line matches a G-code command pattern like `^(G|M)\d+` — reject anything that's neither (e.g. `approved_invalid.txt` in the fixture is approved-but-invalid-signature and must be excluded despite its WAL status). For files passing both filters, invoke `/app/packer pack <in> <out>.pkg`, writing output into `/home/user/repo/`; do this via subprocess, not by hand-crafting the pack format yourself. Then implement `/home/user/server.py` using only `http.server`/`BaseHTTPRequestHandler` (no Flask): `/manifest` lists `os.listdir('/home/user/repo')` sorted alphabetically as JSON, `/artifact/<name>` streams the file bytes with a 404 for missing files. Launch it as a background process and confirm it's actually listening on 127.0.0.1:8000 before finishing.
