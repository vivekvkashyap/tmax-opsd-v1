This is a black-box reverse-engineering task graded by fuzz equivalence against `/app/legacy_aggregator` — do not try to disassemble/patch the binary itself; instead build your own `log_cleaner` in C from the described pipeline semantics and validate it by piping crafted inputs into both programs and diffing raw output bytes (`xxd`/`cmp`). Implement in stages: (1) read all 24-byte input records per metric_id into per-metric buffers; (2) dedupe same-timestamp records for a metric by keeping max `is_retry`, and on ties keep whichever arrived later in the stream (i.e. iterate in input order and overwrite on tie, not just on higher priority); (3) resample to 1-second ticks from each metric's first to last observed timestamp, forward-filling missing seconds with the last known value (undefined/no value before the first record simply doesn't exist — the loop starts at the first seen timestamp, so there is no "prior" gap to fill there); (4) compute a trailing window sum over the current plus up to 4 previous filled seconds, using fewer terms only at the very start of a metric's series; (5) emit one 20-byte record per second per metric, grouped by metric_id then ordered by timestamp, matching the struct packing (uint64,uint32,double) with no padding surprises — verify struct sizes with `sizeof`/`static_assert`. Use pthreads to parallelize independent metric streams, but keep output ordering deterministic (buffer per-thread, then write out in the correct grouped order) since the oracle's output ordering is fixed. Test incrementally: first bit-match on tiny hand-crafted single-metric inputs (with duplicates and gaps) before fuzzing larger multi-metric streams.