Work in three stages and verify each before moving on. First, fix the vendored Rust CSV parser: the bug is that it reads line-by-line and drops any row whose column count doesn't match, so multi-line quoted fields get split and discarded. Rewrite the reader to track whether you are inside an open double-quote as you consume characters/lines, and only treat a newline as a record separator when quote-state is closed (accumulate buffered text across physical lines while inside quotes, and handle escaped `""` inside quotes if present). Test the fixed crate in isolation against a tiny CSV with an embedded newline in a quoted field before wiring it into the processor. Second, scaffold `/home/user/ts_processor` as a Cargo project with a path dependency on the fixed local crate, parse and filter rows where sensor_name equals the literal string containing an embedded newline, then sort ascending by timestamp. Third, implement the rolling-anomaly logic exactly as specified: a strict 10-element trailing window, sample standard deviation (dividing by n-1, not n), and a strict `>` comparison against `3.0 * stddev` — off-by-one errors in window size or using population stddev instead of sample stddev are the most common ways this drifts from the hidden ground truth. Write anomaly timestamps as a JSON array and the report in the exact template given.
