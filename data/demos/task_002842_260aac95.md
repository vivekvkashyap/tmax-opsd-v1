Scaffold with `cargo new signal_analysis`, adding `nalgebra` (with its SVD support), `rand`/`rand_chacha`, `csv`, and `serde_json`. Parse the CSV, determine M/N from the distinct `time_idx`/`sensor_idx` values, and build an M×N `DMatrix` by indexing readings at `[time_idx][sensor_idx]` rather than assuming file order is sorted or complete-by-row. Compute the full SVD once for the "actual" top-3 singular values. For the bootstrap: seed `ChaCha8Rng::seed_from_u64(42)` exactly once before the loop (not once per iteration, which would make every resample identical), then for each of 1000 iterations sample M row-indices uniformly with replacement, assemble the resampled matrix, run SVD, and record its top-3 singular values into per-component vectors; after the loop, sort each vector and index the 2.5th/97.5th percentiles (be consistent about percentile interpolation — nearest-rank on a sorted 1000-element vector is simplest and defensible). For the L2 distance: reconstruct the rank-3 matrix as U₃Σ₃V₃ᵀ, flatten both original and reconstructed matrices, bin values into 50 equal-width bins over [-10,10] (values ==10.0 go in the last bin, out-of-range values dropped), normalize each histogram to sum to 1, then take the Euclidean norm of the elementwise difference. Write the JSON with exactly the specified keys/nesting and run the compiled binary to produce the file.