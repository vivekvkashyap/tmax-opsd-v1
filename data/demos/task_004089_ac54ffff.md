Start by running OCR on /app/schema.png (e.g. `tesseract /app/schema.png stdout`) to recover the actual categorization rules and target folder structure — don't guess the mapping, read it off the image, and double check ambiguous characters (OCR often confuses similar glyphs) by cross-referencing against the file extensions you actually find in /app/messy_project. Then write organize.py to: walk messy_project recursively, classify each file by extension against the OCR'd rules, and copy/move it into the corresponding subfolder under /home/user/organized_project using relative paths that match the rule's target folder. For .txt log files, parse out a timestamp and message (e.g. from a bracketed prefix like `[2023-01-01 12:00:00] message`) and write an equivalent .json file with those two keys instead of copying the raw text. Compute SHA-256 over each file's final bytes (after any conversion) with `hashlib.sha256`, and build manifest.json keyed by the file's relative path within organized_project, valued by its hex digest — keep the key format consistent since the grader matches on relative-path keys before comparing checksums. Finally tar the whole organized_project directory. Validate your own output against the rules you extracted before finishing, since scoring is by F1 over correctly placed+checksummed manifest entries.