Write the Go program using `filepath.Walk` (or `filepath.WalkDir`) to recurse through `/home/user/uploads`, reading each regular file's contents and checking with `strings.Contains` for the exact marker `-----BEGIN OPENSSH PRIVATE KEY-----`. For matching files, locate both the begin marker and the `-----END OPENSSH PRIVATE KEY-----` marker's index, and replace everything strictly between them (not the markers themselves) with `\n[REDACTED]\n`, preserving any surrounding file content outside the key block untouched — build the new content as `prefix + header + "\n[REDACTED]\n" + footer + suffix`, matching the exact expected format (no extra whitespace/newlines beyond what's specified). Overwrite the file with `os.WriteFile`, then `os.Chmod(path, 0600)`. Collect every modified file's absolute path into a slice, `sort.Strings` it before writing, and write one path per line to `/home/user/audit_log.txt` (overwrite, not append, so re-runs stay idempotent and sorted — or open once and write all lines after the full scan completes, rather than appending line-by-line as files are found, since discovery order from Walk is not guaranteed alphabetical). Leave files without the marker completely untouched, including their original permissions. Run with `go run /home/user/audit_keys.go` to verify the log and file contents match expectations.