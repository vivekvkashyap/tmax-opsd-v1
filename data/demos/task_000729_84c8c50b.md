Start with the video: extract frames with `ffmpeg -i /app/leak_capture.mp4 frames/out_%04d.png` (or seek near frame ~145) and visually/OCR-inspect the frame(s) around there to read the exact CLI argument format the daemon uses to leak its key (a `--tls-key=<hex>` style flag) — don't guess the flag name, read it off the frame. For the scanner, first use `xxd`/`strings`/`file` on a couple of sample dumps in /app/proc_dumps/ to see where the fake ELF header ends and where the argv/key string sits, so your C parser reads at correct fixed or delimiter-based offsets. To hit the 0.25s budget for 20,000 files, avoid per-file `fopen`+`fread` loops with tiny reads: use `mmap()` per file (or one large batched read) and a fast substring search (e.g. `memmem` for the flag prefix) rather than naive strstr over huge buffers repeatedly. Sort filenames alphanumerically (zero-padded numeric suffixes should already sort correctly with plain `strcmp` if the fixture is generated that way — confirm before trusting it) before concatenating extracted 32-char hex keys, then SHA-256 with OpenSSL's EVP API and write hex output. Add the seccomp BPF filter (via libseccomp's `seccomp_rule_add` denying execve/execveat/socket/connect) before any file parsing begins, and compile/link exactly as the verifier will.