Split this into independent, verifiable stages rather than one big script. First, OCR `/app/calibration_specs.png` with `pytesseract.image_to_string` and print the raw text so you can visually confirm the extracted linear formula `y = M * x + C` before hardcoding M and C into your pipeline — don't assume a formula, read it off the image. Second, for `/app/data/raw_sensors.jsonl`, since some lines have broken `\u` escape sequences that break `json.loads`, pre-clean each line with a regex that strips or repairs malformed unicode escapes (e.g. match `\u` not followed by exactly 4 hex digits and remove/fix it) before parsing — wrap parsing in try/except per line so a handful of unrepairable rows don't kill the whole job. Third, reshape each wide record (`s1`, `s2`, `s3`) into long rows of (timestamp, sensor_id, raw_value), apply the OCR'd calibration formula, floor each timestamp to the containing minute, and group by (minute, sensor_id) to average `cal_value`. Fourth, parallelize the per-line cleaning/transform step with `concurrent.futures.ProcessPoolExecutor` or `multiprocessing.Pool`, then write the aggregated result as CSV with columns `minute_timestamp,sensor_id,calibrated_mean` (ISO 8601 timestamps, values rounded to 3 decimals) to `/app/processed_data.csv`. Sanity-check a few rows by hand against the OCR'd formula before finishing.