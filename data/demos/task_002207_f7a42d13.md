Work the three parts in order and verify each before moving on. First, fix the C library: apply the patch with `patch < CVE-2023-mathcheck.patch`, then read `mathcheck.c` yourself rather than trusting the patch fixed everything — check the accumulator variable is initialized before the loop, and check the loop bound uses strict `<` not `<=` against `len` (an off-by-one causes an out-of-bounds read past the buffer). Rebuild the Makefile to compile with `-fPIC -shared` producing `libmathcheck.so`, not a static `.a`/`.o`. Second, scaffold the Go module (`go mod init release-manager`), write the HTTP handler with cgo: `import "C"`, cast the decoded `[]byte` to `*C.uint8_t`/`C.size_t` and call `calculate_parity`. Implement SemVer comparison for the prerelease version carefully (a semver library is safer than manual string comparisons) and return 426 for anything below the specified minimum. Validate the Authorization header, base64-decode the payload with proper error handling (400 on bad input), and compare parity. Finally, run the service with `LD_LIBRARY_PATH` pointing at your `.so`, confirm it's listening with curl, then write the ready file.
