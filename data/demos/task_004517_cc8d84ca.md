Do the restructuring and the fix as separate, verifiable steps rather than one big edit. First move files with `mkdir -p src/app tests && git mv` (or plain `mv`) `main.py`, `router.py`, `utils.py` into `src/app/`, then `touch src/__init__.py src/app/__init__.py tests/__init__.py`, and fix the now-broken imports in `main.py`/`router.py` (relative imports or adjusted `sys.path`) so the package still runs. Next, open `src/app/router.py` and identify the leak: a global dict that accumulates one entry per unique URL forever, never evicted. Delete that dict entirely and replace the manual caching logic with `from functools import lru_cache` plus `@lru_cache(maxsize=128)` directly on `parse_url` — don't keep both the dict and the decorator. Write `tests/test_router.py` with `unittest.TestCase` covering (a) that parsing a URL returns the right params dict and (b) that after repeated identical calls, `parse_url.cache_info().hits > 0` (or similar) proves the LRU cache is active. Run `python3 -m unittest discover -s tests -t .` to confirm. Finally write the two-line report with the new absolute router path and the exact name of the dict variable you deleted.
