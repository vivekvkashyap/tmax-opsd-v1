Treat the audio file as a spec you must transcribe yourself before writing any code — do not guess at the corruption rule. Check what's already installed (`which ffmpeg whisper`), and if a transcription tool isn't available, install one (`pip install openai-whisper` or use `ffmpeg` plus any available speech-to-text) and run it against `/app/profiling_notes.wav` to get the literal text; only after reading that transcript should you encode the anomaly condition(s) in C++ — expect the memo to describe an impossible-physics invariant relating the CSV's numeric columns (e.g., a count that can never be negative, or a ratio between two columns that has a hard physical ceiling). Parse the CSV robustly (header line skipped, comma-split, handle trailing newline/whitespace), evaluate the condition on every row, and exit 1 the moment any row violates it, else exit 0 after all rows pass. Validate against both `/app/corpora/clean/` and `/app/corpora/evil/` with a small shell loop checking `$?` on every file before considering it done, since a single misclassified file fails the whole task.