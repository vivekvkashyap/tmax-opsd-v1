Build this as a straightforward two-pass C program: (1) load `sourceA.csv` into an in-memory array/hash keyed by integer `id`, (2) stream `sourceB.csv`, and for each row look up the matching `id` in the loaded table to perform the inner join — skip rows with no match on either side rather than assuming sorted/aligned files. Apply the fixed linear combination formulas for PC1/PC2 exactly as given, using `double` arithmetic. Before writing output, sort the joined records ascending by `id` (e.g. qsort with an int comparator) since input order isn't guaranteed to match the required output order. Format each float with `%.1f` (not `%f`) to get exactly one decimal place, and write the `id,PC1,PC2` header first. Create `/home/user/output/` if it doesn't exist (e.g. `mkdir -p` in the program or Makefile) before opening the output file for writing. The Makefile's `all` target must both compile with `gcc -O3` and then execute the resulting binary — verify by running `make -C /home/user/pipeline` yourself and diffing the generated CSV against your own hand-computed expectation before finishing.
