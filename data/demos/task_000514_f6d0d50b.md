Work through the three parts in order since each depends on the last. For Part 1, inspect `rl_check.c` for the missing `argc` bounds check (add a guard before dereferencing `argv[1]` to avoid the segfault) and missing `#include <string.h>`/`#include <ctype.h>`; then inspect the Makefile — a classic broken-Makefile bug is spaces where a tab is required in the recipe line, so check with `cat -A Makefile` and replace leading spaces with a literal tab. Run `make` until `rl_check` builds and exits 0 only for exactly-10-char alphanumeric tokens. For Part 2, write `validate_job.sh` using `jq -e` to require all four fields exist and are strings, then apply strict anchored regex checks (`^[A-Za-z0-9_]+\.mp4$` for video with no `..` or `/`, `^[0-9]{2}:[0-9]{2}:[0-9]{2}$` for timestamp, `^[A-Za-z0-9,=]+$` for filters), and pipe the token to `rl_check`, exiting 0 only if every check and the rate limiter succeed — test against varied malicious inputs (injection chars, path traversal) before trusting it. For Part 3, run your validator against the given master job, then use `ffmpeg -ss <timestamp> -i dashcam.mp4 -vf <filters> -frames:v 1 output_frame.jpg`, and wrap that ffmpeg call with `/usr/bin/time -v ... 2> benchmark.txt`.
