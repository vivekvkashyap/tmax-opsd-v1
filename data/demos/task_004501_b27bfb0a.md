Derive the variance analytically rather than trying to numerically invert a large matrix at runtime for every N — set up the standard 1D Poisson finite-difference matrix A = (1/h^2)*T where T is tridiagonal with 2 on the diagonal and -1 off-diagonal, then use the known closed-form inverse of that specific tridiagonal matrix (T^{-1}_{i,j} = min(i,j) - i*j/(N+1), 1-indexed) instead of a general dense solve — this avoids numerical-library overhead in Go and gives exact results. From Σ_u = A^{-1} Σ_S A^{-T} with Σ_S = σ²I, the variance at the midpoint row m=(N+1)/2 reduces to σ² * h^4 * Σ_j (T^{-1}_{m,j})², so compute that single sum in your Go program directly from the formula rather than building/inverting a full matrix. Format the printed value with Go's `%e` verb adjusted to exactly 6 decimal places, and double check the exponent format matches examples like `1.234567e-05` (Go's default `%e` may need a custom format string to match). Write run_meshes.sh to `go build` once, then loop over N=3,9,19,49,99 appending `N=<N>, Var=<value>` lines to results.log — verify by hand-deriving one or two small-N cases yourself to sanity check the implementation before trusting the loop's output.