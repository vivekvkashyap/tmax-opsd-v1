Treat this as three independent extraction/debug tasks with separate deliverables. For the pcap, don't guess — parse it programmatically (`scapy`'s `rdpcap`/`sniff`, or `tcpdump -A -r`) and grep the reassembled TCP payloads for an HTTP `GET` request line; extract just the path component and write it verbatim to `/home/user/crash_path.txt`. For the memory dump, treat it as opaque binary and search for the flag pattern rather than assuming a location: run `strings /home/user/crash_dump.bin | grep -E 'FLAG_\{[^}]*\}'` (or `grep -a -o`) to pull out the ASCII flag substring and write exactly that to `/home/user/flag.txt`. For the code bug, read `handle_client` in `server.py`: notice a background task is created via `asyncio.create_task(...)` per connection but never referenced again after the connection closes — that's a classic leaked-task bug. Fix it by keeping a handle to the task and explicitly cancelling (and awaiting the cancellation) when the connection handler finishes, rather than just letting it fall out of scope. Verify by running `python3 /home/user/test_build.py` and confirming it prints `PASS` with exit code 0 — check `asyncio.all_tasks()` stays clean after cleanup.
