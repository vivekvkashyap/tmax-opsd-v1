This is a determinism/reduction-order bug, not a logic bug: `compute_energy` in `/home/user/mc_energy.py` currently sums `eval_conf` results as a `ThreadPoolExecutor` futures complete, so floating-point addition happens in a nondeterministic order each run, producing tiny drift. Fix it by computing the 1000 per-conformation energies in a plain sequential list comprehension over index 0..999 (drop the thread pool entirely, or at least collect results indexed by original order before summing), then reduce with `math.fsum(results)` instead of a running `+=` accumulator — `fsum` gives an exact, order-stable sum, but you still must feed it a fixed-order list. Leave `eval_conf` and the seed generation untouched. Next, read the 100-character string from `/home/user/data/raw_reads.txt` and split it into 5 chunks of exactly 20 characters each, in file order — don't re-sort or shuffle. In `optimize.py`, for each sequence enumerate every position × the 3 other nucleotides (never the same base) to generate all single-point mutants, score each with the fixed `compute_energy`, and pick the minimum; break ties by Python string comparison (alphabetically first) rather than by insertion order. Format all energies to exactly 6 decimal places when writing the CSV.
