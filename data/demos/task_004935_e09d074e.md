Read `/home/user/prepare_data.py` to locate the exact leak: it calls `StandardScaler().fit_transform()` on the `amount` column before `train_test_split`. Reorder the pipeline so the join and NaN-fill (`fillna(0)`) stay as-is, but `train_test_split(df, test_size=0.2, random_state=42)` runs on the *unscaled* data first; then fit `StandardScaler()` only on the training split's `amount` column with `fit_transform`, and apply that same fitted scaler's `.transform()` to the test split's `amount` column — never call `fit_transform` on test data. Compute the mean of the scaled test-set `amount` column and write it, rounded to exactly 4 decimal places (`round(x, 4)`), to `/home/user/fixed_mean.txt` with no extra whitespace or labels. Because `random_state=42` is fixed, verify your fix by checking which row indices actually land in the test set after the split (print `test.index` while debugging) so you can sanity-check the scaling math by hand before trusting the automated output — a sign-flip or off-by-one in split ordering is the most common way this kind of fix goes subtly wrong.