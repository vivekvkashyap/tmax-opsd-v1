First locate the precision-loss bug by grepping the vendored parser's C source for the field extraction functions (`grep -rn "PyFloat_FromDouble\|(float)" /app/vendored_parser/src/`) — look specifically for a `double` being narrowed to `float` before being returned to Python, which silently truncates 64-bit coordinates to 32-bit precision. Fix by removing the erroneous `(float)` cast so the full double is passed to `PyFloat_FromDouble`. Rebuild with the provided Makefile (`make` then `make install`, or `pip install -e .`/`python setup.py build_ext --inplace` depending on what's there) inside `/app/vendored_parser`, and confirm the fix by importing the module and checking that a round-tripped double doesn't lose bits (e.g. compare `struct.pack('d', x)` before/after). Then write classifier.py to iterate `.pcap` files in the given directory, use the fixed library to parse each UDP packet's lat/lon, compute `math.hypot(lat, lon)` per packet, and flag a file EVIL if any packet's hypot equals 1337.0 exactly (use `==` comparison now that precision is preserved — with the bug still present this exact equality would never trigger, which is the tell that your fix didn't take). Print each result as `[FILENAME]: EVIL/CLEAN`.
