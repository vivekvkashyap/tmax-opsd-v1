Work through the three failures in dependency order, verifying each before moving to the next. First, fix the Rust borrow-checker bug in `/home/user/test_env/rust_src/src/lib.rs`: the `extern "C"` function is returning a pointer into a `CString`/`String` that gets dropped at function end, leaving a dangling pointer — resolve this by returning a pointer to memory with `'static` lifetime (e.g. a byte-string literal cast to `*const c_char`, or `Box::leak`/`std::mem::forget` on an owned allocation) rather than a pointer tied to a local's lifetime. Build with `cargo build` from the `rust_src` directory and confirm `target/debug/libversion_check.so` (or platform equivalent) exists. Second, when running the Go tests, the dynamic linker won't find the shared library unless you set `LD_LIBRARY_PATH` to point at that build output directory alongside `go test` — set it inline on the same command rather than in a separate shell. Third, the test also requires `STRICT_SEMVER=1` in the environment. Combine all needed env vars on one `go test -v` invocation and redirect combined output to `/home/user/test_output.log`, then inspect the log to confirm a PASS before finishing.