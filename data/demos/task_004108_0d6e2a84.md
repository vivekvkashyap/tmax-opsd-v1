Extract the payload with plain text tools rather than trying to parse the whole binary dump: `grep -a -o 'C2_OBF_START{[^}]*}' /home/user/forensics/memdump.raw` (the `-a` flag is essential to force grep to treat the binary file as text) pulls out the marker-delimited float list even though it's embedded between random bytes. Strip the `C2_OBF_START{` prefix and trailing `}`, split on commas, and feed the array to `decode.py` to see the garbled output first — this confirms you're extracting the right slice before debugging the arithmetic. The corruption is a float-to-int truncation bug: converting a float back to a character code via naive division/`int()` can land just under the true integer (e.g., 111.999999 truncating to 111 instead of rounding to 112) due to floating-point representation error. Fix it by rounding instead of truncating (`round()` per character, or use `decimal`/add a small epsilon before truncating) without changing the underlying encode/decode formula. Re-run the fixed decoder on the extracted array, strip any wrapper braces/whitespace from the result, and write only the clean URL string to `/home/user/c2_domain.txt`. Verify the URL looks well-formed (no stray "htto"-style corruption) before finishing.