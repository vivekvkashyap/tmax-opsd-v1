Work in the two clear sub-steps the task lays out. First, parse `/home/user/mock_fstab` to find the mount point associated with `/dev/sda1` (e.g. `awk '$1=="/dev/sda1"{print $2}' mock_fstab`) rather than hardcoding it, then `mkdir -p` that directory. Write `/home/user/auto_fetch.exp` using standard expect idioms: `spawn /home/user/vault_log_fetch`, `expect "Username: "`, `send "sre_admin\r"`, `expect "PIN: "`, `send "998877\r"`, then capture the remaining output until EOF (e.g. `expect eof` with logging, or `log_file`/`interact` redirected) into the target `raw_logs.txt` — a common mistake is not waiting for both prompts before sending each credential, causing the script to race ahead. Run the expect script and confirm `raw_logs.txt` has the 3 raw log lines. For the Go program, again dynamically parse the fstab for the mount point (don't hardcode the path there either, even though it will resolve to the same value — the task explicitly asks both components to derive it from fstab), parse each line's ISO8601 UTC timestamp with `time.Parse(time.RFC3339, ...)`, convert with `.In(time.LoadLocation("Asia/Tokyo"))`, and format exactly as `2006-01-02 15:04:05 JST - <message>` using Go's reference-time layout. Run the expect script then `go run analyzer.go` to produce the final file.