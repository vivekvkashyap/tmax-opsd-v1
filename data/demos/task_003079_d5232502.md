Work in two independent halves. First, locate the bug: grep `lib/request_parser.sh` inside `/app/bashttpd-1.0` for where it derives `QUERY_STRING` from `REQUEST_URI` (likely a `cut -d'?'` call). It currently selects the field before the `?` (the path) instead of after it; fix the field index (or write a proper split) so `QUERY_STRING` holds everything after the first `?`, verbatim. Test this in isolation with a fake `REQUEST_URI` before touching the handler. Second, write `/home/user/handler.sh`: parse `QUERY_STRING` into `author` and `limit` (e.g. split on `&`, then `=`, URL-decode if needed), then join `documents.jsonl` and `metadata.csv` on `doc_id` — `jq` for the JSONL side and `join`/`awk`/`sqlite3` to merge with the CSV is simplest via loading both into a temporary SQLite DB. Filter by `author`, sort by `timestamp_published` descending, and take the first `limit` rows with `jq -s`. Always emit the HTTP status line and `Content-Type: application/json` header followed by a blank line before the JSON body. Start the server in the background (`&` or `nohup ... &`) and confirm with `curl localhost:8080/dataset?...` before finishing.