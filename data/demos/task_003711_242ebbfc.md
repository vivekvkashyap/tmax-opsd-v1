Compile `validate.c` as-is with `gcc -o validate validate.c` — don't try to reimplement its checksum logic in shell; instead treat it as a black-box CLI oracle and call it once per token, checking its exit status (`$?`). Parse each `server.log` line to pull out the IP and token (e.g. with `awk`/`sed`/`grep -oP`, matching the `IP: <ip> - Token: <token>` pattern), run `./validate "$token"`, and if the exit code is nonzero (invalid) record that line's IP. Collect all such IPs, deduplicate (`sort -u`), and sort in proper dotted-decimal numeric order using `sort -t. -k1,1n -k2,2n -k3,3n -k4,4n` (or `sort -V`) rather than plain lexicographic `sort`, since lexicographic order would misorder multi-digit octets. Write the final unique, correctly-ordered IP list, one per line, to `/home/user/blocked_ips.txt`, making sure valid-token IPs are excluded even if that same IP also appears with an invalid token elsewhere (dedupe happens after classifying, per IP that had at least one failure) and that no extraneous text besides the plain IPs ends up in the file.