First recover the key: extract the frame at 00:00:03 from `/app/surveillance.mp4` with ffmpeg (e.g. `ffmpeg -ss 3 -i surveillance.mp4 -frames:v 1 frame.png`), then OCR that single frame with tesseract to read the 8-character string after `KEY:` — verify visually or by checking OCR confidence that you got exactly 8 characters, since a misread character will break every valid-key test. Then build the C++ server (httplib.h + a JSON library) implementing `POST /restore`: parse the BKP1 header, compare the 8-byte key field to your recovered key and return 403 immediately on mismatch, then loop over entries reading path-length/path/size/data. For each entry, treat it as malicious (skip, don't extract) if the path starts with `/` or contains `../` as a literal substring — do this check on the raw path string before any path-joining, since normalizing first could hide the traversal. For legitimate entries, write to `<target>/<path>.tmp`, flush/close, then `rename()` to the final path, creating parent directories with something like recursive mkdir first. Track and return correct extracted/skipped counts in the JSON response. Compile with `-std=c++17 -lpthread`, launch it backgrounded so it stays alive, and manually curl both a bad-key request and a crafted archive with a `../` entry to confirm the 403 and skip logic work before finishing.
