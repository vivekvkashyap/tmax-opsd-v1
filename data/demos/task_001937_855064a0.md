In `main.go`, launch two listeners concurrently (e.g. one `http.ListenAndServe` on 8080 in a goroutine, plus a raw `net.Listen("tcp", ...)` accept loop on 8081 in another/the main goroutine) so both run simultaneously. For the HTTP handler, check the exact `Authorization: Bearer operator-token-8831` header first and return 401 if absent/wrong before doing any work; otherwise derive `duration_seconds` and `size_bytes` for `/app/voicemail_backup.wav` — easiest is shelling out to `ffprobe -v quiet -show_entries format=duration -of csv=p=0 <file>` and `os.Stat` for size, or parsing the WAV header manually if ffprobe isn't available. For the TCP endpoint, accept a connection, read until you see the literal `STATUS\n`, write back `OK\n`, then close the connection. For the watchdog: `go build` the source once, then loop `while true; do ./restore_svc >> /home/user/restore_service.log 2>&1; sleep 2; done &` (or equivalent) so any crash restarts it after a short delay, and background the watchdog itself so both ports stay listening after your turn ends. `chmod +x watchdog.sh`.
