This is a black-box reverse-engineering task: you must infer the oracle's exact filtering and analytics logic purely by probing `/app/analyzer_oracle` with crafted CSV inputs, since you cannot read its source. Work methodically: (1) start with a tiny CSV (2-3 rows) and vary one field at a time — sweep `amount` values to find the filtering threshold (binary-search around suspicious round numbers like 100, 500, 1000), and try each distinct `transaction_type` value in isolation to see which get dropped from the graph entirely. (2) Once filtering rules are pinned down, feed graphs with known simple structure (e.g., a star, a chain, a fully-connected triangle) and compare the oracle's numeric outputs against hand-computed values for candidate metrics (out-degree centrality, in-degree centrality, PageRank, clustering coefficient) to identify which one and its exact normalization (e.g., divide by N-1) reproduces the oracle's numbers bit-for-bit, including the rounding rule (round to 4 decimals) and JSON key ordering (check whether keys are sorted). Implement the deduced pipeline in `/home/user/graph_pipeline.py` using pandas/networkx or plain Python, then re-test against the oracle across many random CSVs (including edge cases: N=0,1 nodes, self-loops, duplicate edges) before finalizing, since the verifier will fuzz-test bit-exact equivalence.
