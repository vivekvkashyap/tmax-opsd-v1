Debug the existing script rather than rewriting the whole tool from scratch — the task is a targeted bash-parallelism bug, not a full reimplementation. Read `/app/bash-fasta-density-1.0/bin/calc_density.sh` and look specifically at how it invokes `xargs` with a bash function (e.g. `xargs -P N -I {} bash -c 'some_function "{}"'`): when `xargs` spawns subshells, those subshells do NOT inherit shell functions defined in the parent script unless the function is explicitly exported with `export -f function_name` before the `xargs` call. That's almost always the root cause when a function "isn't found" or silently no-ops inside xargs -P/-I. Add the missing `export -f` line, then run the script against a hand-made small FASTA file to confirm it now produces numeric GC-density output instead of failing silently. Only after confirming the fix works, write `/home/user/run_density.sh` as a thin wrapper that execs the fixed script with the given FASTA path argument passed through unchanged, and make it executable. Test it end-to-end on a few synthetic FASTA files (varied sequence counts/lengths) to build confidence before considering it done, since correctness will be checked via exact stdout equivalence across many random inputs.