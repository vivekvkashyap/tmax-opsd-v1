Treat this as a vendored-package perf fix, not a rewrite. Inspect `src/process.c` and `Makefile` first to locate exactly two defects: an O(N^2) `bubble_sort` invocation and `-O0` in CFLAGS. Replace the bubble_sort call with `qsort(records, count, sizeof(Record), cmp)` where `cmp` is a comparator using `strcmp` on the `endpoint` field (cast void* pointers to `const Record*` correctly) — keep the rest of the aggregation logic (grouping/averaging) untouched since it already assumes sorted input. Change `-O0` to `-O3` in the Makefile, then rebuild with `make clean && make` from `/app/fastlog_etl-1.0.0`. Before declaring victory, time the binary yourself (`time ./bin/fastlog_etl < /home/user/api_logs.csv`) to confirm it's well under the 1-second threshold — don't just trust that qsort+O3 is "obviously fast enough." Redirect stdout to `/home/user/summary.csv`. For the HTML report, write a small awk/bash script that reads each CSV row and emits one `<li>` line per endpoint in the same order as the CSV, matching the template's exact literal text (including static header line) — do it programmatically rather than hardcoding endpoint names, since the CSV order/content is data-driven. Finally write the two exact log lines to pipeline.log.