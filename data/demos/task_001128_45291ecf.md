Use `find /home/user/app_logs -type f -newer /home/user/last_run.stamp` to get the exact set of "recent" files — do not rely on eyeballing timestamps or globbing by name. Loop over that list (e.g. `while read -r f`) and for each: strip `[TRACE]` lines in place with `grep -v '\[TRACE\]' "$f" > "$f.tmp" && mv "$f.tmp" "$f"` (avoid `sed -i` in-place editing pitfalls across old files you must NOT touch), then `grep '\[ERROR\]' "$f" >> /home/user/error_summary.txt`. Do the ERROR extraction after the TRACE removal so the summary reflects cleaned content, and be careful to only append lines from files in your "recent" list — untouched old files must not contribute to the summary or the archive. Finally, `cd /home/user/app_logs` before tarring (or use `tar -C`) and pass only the recent filenames to `tar -czf /home/user/processed_logs.tar.gz file1 file2 ...` so the archive stores relative paths, not absolute ones and not files newer-check missed.
