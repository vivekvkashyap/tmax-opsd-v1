First inspect the database's actual contents (`sqlite3 /home/user/routing.db "SELECT * FROM network_links;"`) so you understand the real graph rather than assuming it matches the example. Extract only active links with `SELECT DISTINCT source, target, latency FROM network_links WHERE is_active = 1;` (the DISTINCT matters — the corrupted index can yield duplicate rows that would otherwise skew nothing here but should still be deduped per the task). In `find_path.py`, build a directed weighted adjacency structure from that query result and run a standard shortest-path algorithm (Dijkstra, since latencies are non-negative) from `START` to `END`, reconstructing the path via predecessor tracking rather than just the final distance. Compute cumulative latency at each node along the reconstructed path (starting at 0 for START) and write it as CSV with header `node,cumulative_latency` to `/home/user/shortest_path.csv`, one row per node in path order. Verify by manually tracing the graph you extracted to confirm your algorithm didn't pick a suboptimal or stale-data-influenced route.
