Model this as a weighted DAG traversal, not a simple lookup. Load `/home/user/ownership.csv` into a directed graph (e.g. networkx `DiGraph` with edge attribute `weight` as a fraction 0-1) from source (owner) to target (owned company). For each individual I_x and company C_y, sum the products of edge weights along every distinct directed path from I_x to C_y (indirect ownership multiplies weights along a path; multiple paths add) — do this by recursively/iteratively propagating ownership downstream in topological order rather than hardcoding path enumeration, since the graph can have many paths. Flag as a hidden UBO any (I_x, C_y) pair where total computed ownership > 25.0% strictly, AND there is no direct CSV row from I_x to C_y — check the direct-edge condition against the raw edge list, not the computed totals. Format each as `"{individual} owns {pct:.1f}% of {company}"`, sorted by individual then company. Separately, build the same graph as unweighted and directed, run `networkx.betweenness_centrality`, filter to nodes with the `C_` prefix, and take the top 2 by centrality score (ties broken alphabetically by ID). Write both lists into `/home/user/audit_report.json` with keys `hidden_ubos` and `top_intermediaries` in the exact structure given. Trace the ownership math by hand for at least one multi-hop path to confirm your propagation logic before trusting the full output.