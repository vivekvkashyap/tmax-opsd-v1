Bisect first: in `/app/telemetry_ingester`, run `git bisect start`, mark `HEAD` bad and `v1.0.0` good, then drive it with a script that builds each candidate commit (`cargo build --release`) and sends a deliberately truncated length-prefixed payload to the ingester over TCP to see if the process panics/crashes (exit nonzero) vs. handles it gracefully — automate this as the `git bisect run` command. Once found, write the 40-character commit hash to `/home/user/bisection_result.txt`. Then check out/return to `main`/`HEAD` and fix `src/parser.rs`: find the zero-copy slice indexing that does something like `&buffer[4..4+len]` without checking `4+len <= buffer.len()`, and add that bounds check, returning a `ParseError::Truncated` (or equivalent) instead of letting the slice indexing panic. Rebuild with `cargo build --release`, then start Redis (default port 6379), the Python API (`python3 /app/api/main.py` on 9090), and your fixed Rust ingester (port 8080) all in the background. Manually test by sending both a truncated and a valid length-prefixed TCP packet to 8080, confirming the process stays alive after the malformed one and that `curl localhost:9090/metrics` reflects the valid payload.