Three layered fixes, verify each in isolation. (1) Linking: write `/home/user/project/libbackend.c` exporting exactly `void backend_init() {}`, compile with `gcc -shared -fPIC libbackend.c -o libbackend.so` in the project dir, then make sure `ctypes.CDLL` can resolve it at load time — the loader needs `libbackend.so` on the dynamic linker's search path, so set `LD_LIBRARY_PATH` to include `/home/user/project` (in the test-running shell/subprocess env, not just interactively) rather than trying to patch `libdata.so` itself. (2) Emulator: implement the exact binary framing — read 2 bytes as big-endian unsigned length (`struct.unpack('>H', ...)`), read that many following bytes as UTF-8, reverse the decoded string, re-encode, and write back a 2-byte BE length prefix plus payload, then close after one exchange. (3) Tests: convert `test_client.py` to pytest with a fixture that does `subprocess.Popen(['python3','emulator.py'])`, gives it a moment to bind the socket before the test runs, and terminates/joins it in teardown; assert the round-tripped reversed string. Run via `pytest --json-report --json-report-file=/home/user/project/test_report.json` (install `pytest-json-report` if missing) and confirm the summary shows all tests passed.
