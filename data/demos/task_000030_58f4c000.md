Derive the ground-truth frequency yourself from the video rather than assuming a round number: extract frames from `/app/calibration.mp4` (e.g. `ffmpeg -i calibration.mp4 -vf fps=30 frame_%03d.png` since it's a 30fps clip), classify each frame as black or white (mean pixel value thresholding), count how many frames the strobe stays on each state before switching, and convert transitions-per-second into Hz using the known 30fps rate and 10s duration — don't hardcode a guessed value. In `/home/user/evaluate_sample.py`, load the given CSV's `signal_1`/`timestamp`, derive `sample_id` from the filename, and pull matching rows from `/app/telemetry.db`'s `sensor_data` table by exact timestamp join (an inner join, dropping unmatched rows). Fit `signal_2 ~ signal_1` with ordinary least squares (numpy.polyfit degree 1, or scipy.stats.linregress) to get the slope, then compare `abs(slope - F) <= 0.1` where F is the frequency you measured from the video, exiting 0 if within tolerance and 1 otherwise. Test against a few self-constructed clean/corrupted CSVs and confirm the join actually matches rows (empty joins should fail obviously, not silently pass) before trusting the hidden corpus.
