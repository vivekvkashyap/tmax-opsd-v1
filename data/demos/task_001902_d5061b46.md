Tackle each phase independently and match output formatting precisely, since the grader recomputes expected values and string-compares. Phase 1: get the cert fingerprint with `openssl x509 -noout -fingerprint -sha256 -in server.crt`, then strip the `SHA256 Fingerprint=` prefix — keep it uppercase with colons exactly as OpenSSL prints it, don't reformat. Phase 2: parse `headers.log` for the `Content-Security-Policy` line, extract the `script-src` directive's token list, and check membership of `'unsafe-eval'` (including the quotes) rather than a naive substring search on the whole header, which could produce false positives from other directives. Phase 3 is the core exercise: read `weak_hash.c`'s algorithm and reimplement the exact same integer arithmetic (16-bit wraparound: `hash = (hash<<5)+hash+c`, truncated to uint16_t) in Python or a small C harness, then search for a collision — either brute-force short strings incrementing lexicographically, or exploit the linear/multiplicative structure of the hash algebraically to construct two differing strings that collide; verify by actually compiling/linking `weak_hash.c` and calling it on both candidate strings before finalizing. Phase 4: assemble the JSON with the four exact keys.
