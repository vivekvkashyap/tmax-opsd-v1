Treat /app/oracle_engine strictly as a black-box ground-truth generator: run it once on a sample CSV to learn its exact input/output CSV format, and build a small comparison wrapper script that runs both the oracle and `python /home/user/py_engine/solver.py <csv>` on the same input and diffs/MSEs the outputs — never try to disassemble or reimplement the oracle's internals. Use that wrapper as the bisect test: `git bisect start`, mark `v2.0` bad and `v1.0` good, then at each step run the wrapper against a fixed small input and call `git bisect good`/`bad` based on whether the Python output matches the oracle within tolerance — automate this with `git bisect run <script>` if possible across the 200 commits rather than testing by hand. Once bisect lands on the offending commit, `git show <commit>` to read its diff and look specifically for a precision-losing cast (e.g. a stray `float32` conversion) or a convergence epsilon/tolerance value that was loosened, either of which causes early stopping before full float64 convergence. Fix that specific line in current HEAD (don't just revert the whole commit if unrelated changes were bundled in), rerun the wrapper to confirm output now matches the oracle closely, then run the repaired solver on /home/user/eval_data.csv and save one float per line to /home/user/predictions.csv.