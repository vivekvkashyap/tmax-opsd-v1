Do the housekeeping first: `mkdir -p` the three subdirectories, then `mv` routes.txt into `data/`, and only then write the C program against its final location so paths are consistent. For `mapper.c`, parse each line by splitting on ` -> `, strip `{...}` segments from the URL (including the braces) using a simple scan that skips characters between `{` and `}` inclusive rather than a fragile string-replace, then sum the exact ASCII (byte) values of every remaining character in the stripped URL and separately in the method name — be precise about what counts as part of each string (no trailing newline/whitespace bleeding into the sum). Compute the checksum as `(Sum_URL * Sum_Method) % SEED`, where `SEED` comes from a `-DSEED=<n>` compile-time macro guarded by `#ifndef SEED`/`#define SEED <default>` in the source. Compile twice with `-DSEED=17` and `-DSEED=31` into the two named binaries under `build/`, then run each against `data/routes.txt`, redirecting to the specified log files, and manually verify a couple of checksum computations by hand before trusting the output format `Checksum: <VALUE> | Method: <NAME>`.