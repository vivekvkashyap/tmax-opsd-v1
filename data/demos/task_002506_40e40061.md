Automate the interactive prompts with `expect` rather than piping answers via `echo`/heredoc, since sequential `read -p` prompts need to be matched and responded to individually (`spawn ./legacy_config_gen.sh; expect "Enter deployment environment:"; send "production\r"; expect "Enter service name:"; send "api_worker\r"; expect "Confirm token generation"; send "y\r"; expect eof`). After the script runs, parse the generated token from `/home/user/service_config.txt` with something like `cut -d= -f2` — don't hardcode any token value since it's randomly generated each run. For the Rust server, use only `std::net::TcpListener`/`std::io::{Read,Write}` to accept connections in a loop, read the raw HTTP request bytes, check if the request line targets `GET /status`, and write back a proper `HTTP/1.1 200 OK` response with `Content-Length` matching the token body. In deploy.sh, compile with `rustc`, launch the server backgrounded and detached (e.g. `nohup ... & disown`) so it outlives the script, `sleep 2`, then `curl -s` the status endpoint and write the exact log line using the value just curled (not the file value) to avoid formatting mismatches.