Start by extracting the exact spectral-analysis spec from `/app/filter_specs.png` — run it through OCR (e.g. `tesseract /app/filter_specs.png -`) and read the resulting text carefully to get the encoding scheme, the target frequency/period, and the threshold ratio; do not guess these parameters. Then implement the C++ tool: for a sequence of length L, one-hot-encode each of the 4 bases into separate 0/1 signals, compute the DFT (a simple O(L^2) or FFT implementation is fine at these lengths) power spectrum for each channel, sum them, and compare the power at the specified frequency index against the average power across all frequencies using the extracted threshold multiplier. Print exactly `ACCEPT` or `REJECT` with no extra text. Before finalizing, sanity-check your implementation against the labeled examples in `/app/reads/clean/` and `/app/reads/artifact/` — every clean file should yield ACCEPT and every artifact file REJECT — and fix any off-by-one in frequency-index calculation (k should be derived from L exactly as the note specifies) before compiling to `/home/user/seq_filter`.
