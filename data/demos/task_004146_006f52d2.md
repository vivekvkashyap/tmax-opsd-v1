This task has two independent halves — do them separately and verify each in isolation before combining. For the services: add a `location /search { proxy_pass http://127.0.0.1:5000; }` block to `/home/user/nginx.conf` (check the existing config's structure — server block, listen 8080 — before inserting), and in `/home/user/search_app.py` replace the dummy Redis host with `redis.Redis(host='127.0.0.1', port=6379)`; start Redis, the Flask app, and reload/restart nginx, then confirm with `curl http://127.0.0.1:8080/search?q=test` that you get 200 and JSON back before moving on. For the archiver, write `/home/user/archiver.py` reading all of stdin, using a state machine or regex to pull out each JSON blob between `[REQ_START]`/`[REQ_END]` markers (there can be many), build `endpoint,status` CSV rows sorted by endpoint, then implement your own RLE pass over the resulting string: scan left-to-right counting runs of the *same alphabetic* character, and only when a run is length >= 3 emit `~<count><char>`, otherwise emit the run's characters literally — digits, commas, and newlines are never compressed even if repeated. Test your RLE against hand-built strings with runs at boundary lengths (2 vs 3) and mixed-case runs (which don't merge) before trusting it on real CSV output, since off-by-one run-length or case-sensitivity mistakes are the usual bugs here.
