Ignore `hierarchy_cache` entirely — query only `employees` directly, since the task is specifically testing that you don't trust a stale materialized view. In Go, use `database/sql` with the `mattn/go-sqlite3` driver: load all rows once into memory (id, name, manager_id, role), build a map from `manager_id` to a list of child rows (nil/NULL `manager_id` marks the root), then recursively construct the JSON tree starting from the root, and initialize each node's `subordinates` slice as `[]Node{}` rather than leaving it as a nil slice — Go's `encoding/json` marshals a nil slice as `null`, not `[]`, which will fail schema validation, so explicitly `make([]Node, 0)` before appending. Recurse depth-first, sorting each node's children by `id` if determinism matters, marshal with `json.MarshalIndent` (or plain `Marshal`), and write to `hierarchy.json`. Before finalizing, mentally walk the schema in `schema.json` against your output structure (required keys `id`, `name`, `role`, `subordinates` on every node) and run `go build`/`go run` to confirm it compiles and produces valid JSON.
