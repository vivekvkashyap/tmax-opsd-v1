First recover the two configuration parameters from the image with `tesseract /app/rules.png -` (or similar OCR invocation) and read the extracted text carefully — don't guess the bucket size or encoding name, verify what OCR actually reports since misreads are common with similar characters. Hardcode those two recovered values into your script once confirmed. In the script, read stdin line by line; use a strict regex anchored to the full line to validate the ISO-8601-timestamp-space-message format, discarding anything that doesn't match rather than trying to partially salvage malformed lines. Convert the timestamp to epoch with `date -d ... +%s`, then bucket by integer division/multiplication with the bucket size in seconds (convert "Xm"/"Xh" units carefully). For the message: pipe through `iconv -f <SOURCE_ENCODING> -t UTF-8 -c` to drop invalid bytes, then `tr -cd 'A-Za-z0-9 '` to strip everything else, `tr 'a-z' 'A-Z'` to uppercase, and `tr -s ' '` to squeeze spaces — get this pipeline order right since encoding conversion must happen before byte-level filtering. Test against random/binary input to confirm the script never crashes on garbage.
