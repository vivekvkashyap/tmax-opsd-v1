Fix `start_pipeline.sh` by actually piping the three processes together — `./receiver.sh | /home/user/firewall.sh | ./aggregator.sh` — instead of the current setup that runs them disconnected with `/dev/null` on either end. Write `firewall.sh` as a `while IFS='|' read -r ts ip user msg; do ... done` loop over stdin, and avoid any buffering command (like block-buffered `awk`/`sort` stages) that would delay output — plain shell `read`+`echo` per line stays line-buffered naturally, satisfying the real-time requirement. Per line: mask SSNs only inside the message field with a regex like `[0-9]{3}-[0-9]{2}-[0-9]{4}` → `***-**-****`; strip zero-width-space bytes (`\xe2\x80\x8b`) from the username with `sed`/`tr`; if the timestamp is ISO-8601 but missing a trailing `Z`, append one. For rate limiting, track each IP's recent event timestamps (converted to epoch via `date -d`) in an associative array, using the *log's* timestamp, not wall-clock time — drop the 4th and later events per IP within any rolling 10-second window until it clears. Validate exhaustively against the clean/evil corpora before considering it done.