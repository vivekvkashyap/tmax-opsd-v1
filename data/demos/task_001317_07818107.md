Fix issues one at a time and verify each in isolation before moving on. For the linker error, run `bash build.sh` to see the exact undefined-symbol message — a shared library built from code that calls `math.h` functions (pow, sqrt, etc.) needs `-lm` appended to the gcc command; add it and confirm `libmatrix.so` builds and `nm -D` shows the symbols resolved. For the recursion bug, open `simulate.py` and replace the equality-based termination `if guess == next_guess:` with a tolerance check `if abs(guess - next_guess) < 1e-7:` — floating point rarely reaches exact equality, which is exactly why it recurses forever/hits Python's recursion limit. For the SQL bug, find the query selecting `SUM(duration)` and change it to `AVG(duration)` so it matches the printed label "Average execution time". After both fixes, run `./build.sh` then `python3 simulate.py`, capture its stdout exactly as printed (format `Average execution time: <number> ms`), and write that exact string into `/home/user/profiling_sim/final_report.txt` — don't hand-compute or hardcode the number, let the fixed script produce it and redirect/copy its real output.
