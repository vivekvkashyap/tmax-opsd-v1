Do not read the CSV line-by-line. Use Python's `csv` module (or pandas `read_csv`) opened in text mode, which correctly handles quoted fields containing embedded newlines — the whole file is one CSV document, not one record per physical line. Parse into records with the six columns, coercing CPU/Memory to float. For per-server stats, group by ServerID and compute mean CPU and max Memory. For the Euclidean distance, first sort each server's rows by Timestamp (do not trust file order), pair up CPU values index-by-index, then apply sqrt(sum((a_i-b_i)^2)). For the stratified sample, group by LogLevel, sort each group by Timestamp ascending, and take the first two rows per group, then flatten and sort the final list by LogLevel then Timestamp for output. Build the API with Flask/FastAPI, add a before-request/dependency check that requires the exact `Authorization: Bearer pipe-auth-token-xyz` header on every route (return 401 otherwise), and launch it bound to 127.0.0.1:8080 as a background process (e.g. nohup ... & or a systemd/tmux-free daemonization) so it stays up after your shell command returns — verify with a local curl before finishing.