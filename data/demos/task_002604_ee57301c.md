Split this into a pure filter task and a config-wiring task, and verify each independently before combining. For `filter.sh`, read stdin line by line in a loop (`while IFS= read -r line`), split on commas, and check all five conditions explicitly: exactly 4 fields (watch for rows with extra/missing commas), field 1 and 2 match `^[0-9]+$`, field 3 is a strictly positive number allowing decimals (regex like `^[0-9]+(\.[0-9]+)?$` combined with a numeric check that it's not `0` or `0.0`), and field 4 matches `^[A-Za-z0-9 ]+$` — silently `continue`/skip on any failure rather than erroring out, since the script must keep processing subsequent lines. Test it directly yourself with `printf` piping known-good and known-bad rows before touching the services. For the multi-service side, `nginx.conf`'s `/upload` location block needs its `proxy_pass` target corrected to `http://127.0.0.1:5000` exactly, and `settings.env` needs both `REDIS_PORT` and `FILTER_SCRIPT` set to the literal values given (the Flask app reads these to know where to send data and which script to shell out to for filtering). Only after both are fixed, run `start_services.sh` and confirm with `curl` and `redis-cli` that a mixed payload through the full stack ends up correctly filtered in Redis.
