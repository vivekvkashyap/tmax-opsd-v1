Treat this as two separate defects, not one. First reproduce the concurrency bug: write a small bash loop that runs the current parser repeatedly against a large multi-threaded log directory and diffs output across runs (or watches for hangs with `timeout`) to confirm nondeterminism/deadlock, then inspect the pthread code for shared mutable state (e.g. a global/static checksum-validation buffer or counter) that multiple worker threads read-modify-write without a mutex or with data that should be thread-local — fix by giving each thread its own local buffer/state or adding proper locking around the truly shared resource, without serializing the whole pipeline (that would blow the 2s budget). Use `git log`/`git bisect run <your regression script>` only if manual inspection of recent history isn't enough. Second, fix the TLV parsing edge cases directly: ensure a zero-length record is still emitted (as an empty JSON object, not skipped or crashed on), and ensure checksum-invalid records are silently dropped rather than corrupting the read cursor. Validate by running your fixed binary against sample logs and diffing line-by-line (as sorted sets, since ordering may differ across threads) against `/app/ref_parser`'s output, then time it to confirm it stays under 2 seconds with threading intact.
