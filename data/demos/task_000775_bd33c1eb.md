Diagnose the environment-variable bug first by reading `server.c`: find where `getenv("APP_CERTS_DIR")` is used directly (e.g. passed to `strlen`/`sprintf`) without a NULL check — that's the segfault when systemd's minimal environment doesn't set it. Fix it with a fallback (`cert_dir = cert_dir ? cert_dir : "/home/user/certs";`) and recompile to `/home/user/app/server`. Next, check the systemd user unit at `~/.config/systemd/user/secure-backend.service` for correctness (ExecStart path, WorkingDirectory, any missing Environment= lines), run `systemctl --user daemon-reload`, then enable and start it — verify with `systemctl --user status secure-backend`. For the tunnel, launch `ssh -f -N -L 8443:localhost:4433 localhost` (the `-f -N` backgrounds it without a remote command) and make `setup.sh` idempotent by checking `pgrep -f "ssh.*-L.*8443.*4433"` before starting a new one, and checking the binary's mtime vs source mtime (or just `[ ! -x server ] || [ server.c -nt server ]`) before recompiling. Finally `curl http://localhost:8443/status` and redirect the exact output to `/home/user/result.log`. Remember: no sudo anywhere, everything as user-level systemd (`--user`).