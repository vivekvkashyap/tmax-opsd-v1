Get the WAV binary format exactly right before worrying about the math. In C, read the 44-byte RIFF/WAVE header as raw bytes and write it back to the output file unchanged (don't try to reconstruct or reinterpret header fields you don't need to modify — just copy the bytes through), then read the sample data as an array of 16-bit signed integers (`int16_t`), scale each by 0.5, and write the scaled samples back — cast/round carefully to `int16_t` (the reference implementation does `(data * 0.5).astype(int16)`, i.e. truncation toward zero via numpy's default cast) since off-by-one rounding differences between rounding and truncation are exactly what an MSE<=1.0 threshold is designed to catch. Confirm sample count and byte layout match input (don't accidentally miscount header size or samples-per-frame for mono 16-bit audio). Wrap compilation (`gcc -o processor processor.c`) and execution in `deploy.sh`, run it to produce `/home/user/output.wav`, and sanity-check by loading both wavs in Python (`scipy.io.wavfile`) yourself to compute MSE before considering the task done. Finally launch `python3 -m http.server 8080 --directory /home/user &`, capturing its PID to `/home/user/server.pid`.