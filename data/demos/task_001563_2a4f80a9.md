Build each pipeline stage as an independent, testable script before wiring the Makefile. In `extract.py`, parse each line of `/home/user/raw_reviews.txt` with a regex that pulls `id`/`text`/`meta`, then apply three alternative regex patterns against the META field to catch `rating:X`, `X stars`, and `score=X` — try each pattern in turn (or one combined regex with alternation and a capture group) since only one will match per line, and write `id,rating,text` to `/home/user/extracted.csv`. In `sample.py`, group the extracted rows by rating, sort each group by integer `id` ascending, and take the first 5 per rating (5 ratings x 5 = 25 rows) to `/home/user/sampled.csv`. In `similarity.py`, build the character-trigram set for each of the 25 texts, then use `multiprocessing.Pool` to compute pairwise Jaccard similarity over all `C(25,2)` unique pairs in parallel, filter to `>= 0.4`, and emit `[id1,id2]` pairs (with `id1<id2`) sorted by `id1` then `id2` into `/home/user/similar_pairs.json`. Write the `Makefile` with the exact dependency chain described (each target depending on its input file and its script), then run `make` from `/home/user/` to confirm the whole DAG executes cleanly end-to-end and regenerates outputs when inputs change.