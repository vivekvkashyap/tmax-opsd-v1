Write one script that produces all three findings from the given files, parsing each format on its own terms rather than assuming fixed data. For brute-force detection, parse auth.log lines matching "Failed password for admin from <ip>", tally failures per IP, and keep only IPs with a count strictly greater than 3 (don't count failures for other usernames, and don't conflate "Failed" with "Accepted" lines). For sudoers, use a regex tolerant of spacing variants around `NOPASSWD:` and `ALL` (e.g. `NOPASSWD:\s*ALL` after a user's `ALL=(ALL...)` clause), extract the leading username token, and explicitly exclude `root` and group entries (lines starting with `%`) from the result. For ports.json, load it as JSON, filter to entries where `state == "open"`, and collect the `port` values not in the authorized set {22, 80, 443} as integers. Assemble the three lists into a dict with exactly the keys `brute_force_ips`, `unauthorized_sudo_users`, `unauthorized_open_ports` and write it as JSON to /home/user/compliance_report.json — order within each array doesn't matter, but the values must be exact matches for the underlying data, so re-run your script against the real files rather than hand-verifying against the example.