Diagnose before coding: inspect `/app/graph.db`'s schema with `sqlite3 /app/graph.db ".schema edges"` and `.indices edges`, then run `EXPLAIN QUERY PLAN SELECT source, target FROM edges WHERE weight >= ?` to confirm it's doing a full table scan. Fix it by creating a covering index that includes all columns the query touches, e.g. `CREATE INDEX idx_edges_weight ON edges(weight, source, target);`, so SQLite can satisfy the query from the index alone — verify with `EXPLAIN QUERY PLAN` again that it now shows an index/search rather than a scan. Then implement `compute_and_cache_pagerank()` in `/app/etl_api.py`: `requests.get` the edges endpoint, build a `networkx.DiGraph` by adding an edge per (source, target) pair, run `nx.pagerank(G)` with default parameters, sort nodes by score descending and take the top 10 IDs, `json.dumps` that list into Redis via `r.setex('top_pagerank', 60, json.dumps(top10))`, and return it. Restart services with `/app/start.sh` after each change and use `curl` timing (`curl -w '%{time_total}'`) against `/pagerank` to confirm both the cold-cache and warm-cache latency requirements are met, and diff your top-10 IDs against a manually computed PageRank on the same edge set to catch ordering/off-by-one bugs.
