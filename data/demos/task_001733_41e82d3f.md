This task has two independent halves — solve them separately. For the video part, use ffmpeg to extract frames (e.g. `ffmpeg -i /app/deadlock_dashboard.mp4 frame_%04d.png`) and then scan each frame programmatically (Python with PIL, or an ffmpeg filter like `blackframe`/`signalstats`, or just sampling pixel values) to find frames that are solid red (all pixels ≈ RGB 255,0,0) — don't eyeball it, write a script that checks every extracted frame and collects the matching frame indices, then write them comma-separated to `/home/user/deadlock_frames.txt`. Be careful with frame numbering (1-indexed vs 0-indexed) to match how the corpus generator counted frames. For the Go deadlock detector, parse each JSON file's transactions, build a directed graph where transaction A has an edge to transaction B if A is waiting on a lock held by B, then run a standard cycle-detection algorithm (DFS with a recursion/visiting stack, or Tarjan's SCC) over that graph — a cycle means deadlock. Iterate `.json` files in alphabetical order (`filepath.Glob` + `sort.Strings`), and print only the filenames of cyclic (malicious) files to stdout, nothing for clean ones. Test against both `/app/corpus/clean/` and `/app/corpus/evil/` before finishing.