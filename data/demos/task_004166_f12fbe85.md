Diagnose the SSH tunnel failure methodically: read `/home/user/.ssh/config` for the `local-monitor` entry, check the `IdentityFile` it references actually exists and is valid, verify its public counterpart is in `/home/user/.ssh/authorized_keys`, and check permissions (private keys/`.ssh` need `600`/`700` or sshd silently refuses). If the config points at a wrong/missing key, generate/fix the key pair and correct the `IdentityFile` path. Only after `ssh -N -f local-monitor` succeeds without a password prompt is the LocalForward 9090->8080 live. For the Go daemon: compute disk usage by walking `/home/user/deploy_artifacts/` and summing file sizes, and for the video error-fraction, shell out to `ffmpeg` to extract 1 fps frames, scan each frame's pixel buffer for pixels matching the given RGB thresholds, check that ratio against 5% of total pixels, then divide flagged-frame count by total frames. Expose `/metrics` as JSON over `net/http`, compile to the exact binary path, write the systemd unit with correct `ExecStart`, and manually background the binary so 8080 is listening.