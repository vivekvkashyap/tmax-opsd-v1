Do not assume you know the business rules — OCR `/app/business_rules.png` first (e.g. `tesseract /app/business_rules.png -` or a Python OCR library) and read the extracted text carefully to learn exactly which sales rows to exclude and which centrality metric is requested; treat this as data you must recover, not something to infer from the task description alone. Before querying, address the "corrupted index" hint defensively: run `REINDEX;` and/or `PRAGMA integrity_check;` (or drop and recreate the relevant index) against `/app/data.db` via `sqlite3` so stale/duplicate rows from a broken index don't leak into your query results. Then write the SQL/Python logic to apply the OCR'd filter rule and compute the OCR'd centrality metric per vendor by joining `sales` to `clients`/`vendors` appropriately — for a "count of unique clients" style metric this typically means `SELECT vendor_id, COUNT(DISTINCT client_id) ...` after filtering, but confirm the exact metric name from the image rather than assuming. Finally serve the result with Flask/FastAPI on `127.0.0.1:8000` exposing `GET /centrality`, returning a JSON dict of `{"<vendor_id>": <int_score>}`, and keep the server process alive (background it, e.g. `nohup ... &` or run inside a persistent shell) since the grader queries it live.
