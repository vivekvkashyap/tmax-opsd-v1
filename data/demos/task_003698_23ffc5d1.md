Write `/home/user/detector.c` as a recursive directory walker using `nftw()` or manual `opendir`/`readdir` + `lstat` recursion (use `lstat`, not `stat`, so you see symlinks themselves rather than following them blindly). For each entry: if it's a symlink, resolve it with `realpath()` and check the resolved path has the scanned base directory's realpath as a prefix — reject if it escapes; detect cycles by tracking visited (device,inode) pairs of directories you've descended into (a symlink loop will revisit one) or simply cap recursion depth and treat a readlink failure/ELOOP as unsafe. For regular files, check `st_nlink > 1` via `lstat` and reject. For content: read the file, scan line-by-line for lines starting with `[YYYY-MM-DD HH:MM:SS] ` (validate literal digit/dash/colon/bracket positions, not just any bracket), count consecutive non-timestamp lines following each timestamp line, and fail if any run exceeds 49 continuation lines; also fail immediately if you encounter a `\0` byte anywhere while reading (treat as binary). Print no output, just `exit(0)`/`exit(1)`. Compile with `gcc -O2 -o /home/user/detector /home/user/detector.c` and test manually against a couple of directories you construct yourself (one clean, one with a symlink loop) before trusting it against the real corpora.
