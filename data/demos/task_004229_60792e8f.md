Start by running `tesseract /app/schema.png -` (or similar OCR invocation) to recover the actual table/column names and relationship description — do not guess the schema. Once you know the tables, open `/app/dataset.db` with the sqlite3 CLI to confirm columns match what OCR reported. In your C server, before serving any request, run `CREATE INDEX IF NOT EXISTS ...` on the join column(s) used to find co-authors (the column linking papers to authors) so lookups are fast on a large dataset. The co-author query itself should be a self-join or subquery: find all paper_ids for the given author, then all distinct other author_ids sharing those paper_ids, excluding the original author, sorted ascending, joined with commas. For the TCP server, use a standard accept() loop that keeps the listening socket open across multiple sequential connections/requests rather than exiting after the first one — parse the line-based request with a buffered read up to `\n`, and always terminate the response with `\n` even when the co-author list is empty. Test manually with `nc 127.0.0.1 9000` before finishing.
