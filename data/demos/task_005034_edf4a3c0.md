Orchestration fix: edit `/app/start_services.sh` to start redis first, then insert a polling wait loop before launching the Python services — e.g. `while ! nc -z 127.0.0.1 6379; do sleep 0.1; done` (or use `redis-cli ping` in a loop) — so `receiver.py`/`worker.py` never race Redis startup. Export `REDIS_URL` and `FILTER_SCRIPT` env vars in that same script (or a sourced `.env`) before launching the Python processes so they inherit them.

Filter script: write `/home/user/telemetry_filter.py`, make it executable (`chmod +x`), and have it read the JSON payload from stdin. Implement strict validation in order: JSON parses and has exactly the three keys `device_id`, `timestamp`, `metrics` (reject any extras); `device_id` matches `^DEV-\d{4}$`; `timestamp` is an int; `metrics` is a dict whose values are all int/float (not bool, not nested structures); if `temperature` key is present, its value must be within [-50.0, 100.0]. Exit 0 only if every check passes, else exit 1. Test the script directly against the sample files in `/app/corpora/clean` and `/app/corpora/evil` (piping each JSON file to stdin) before wiring it into the Flask receiver, then launch the fixed `start_services.sh` in the background (e.g. with `nohup ... &`) and leave it running.