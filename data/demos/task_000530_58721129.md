Set up a Rust binary crate at `/home/user/kmer_kl` (with `cargo new`) and add `serde_json` for output. Parse the FASTA at `/home/user/reads.fasta` into (header, sequence) pairs. For primer matching, implement fuzzy search-from-left: slide an 8-character window across each sequence and count mismatches char-by-char against `ATGCGTAC`, accepting the first (leftmost) window with mismatch count <= 1 — this is plain Hamming-distance-per-window, not edit distance, since only substitutions are allowed. On a match, take everything after the matched window as the truncated read; discard reads with no such window. Keep only the first 4 valid truncated reads in FASTA order. For each, build a 64-entry (all A/C/G/T 3-mers) frequency map, initialize every entry to `1e-8` before counting overlapping 3-mers in the substring, then normalize to probabilities summing to 1. Compute pairwise KL divergence with natural log for all 16 ordered (i,j) pairs (diagonal is 0), round each to 4 decimals, and write the 4x4 matrix as nested JSON arrays to `/home/user/features.json`. Verify manually on the given fasta before trusting the output.
