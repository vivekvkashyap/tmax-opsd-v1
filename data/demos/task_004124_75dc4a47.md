Chain the four steps in order and validate each output before proceeding to the next, since later steps depend on earlier ones being exactly right. First, detect/confirm the source encoding is ISO-8859-1 and convert with `iconv -f ISO-8859-1 -t UTF-8 sensor_data_raw.csv > sensor_data_utf8.csv`; verify with `file -i` afterward rather than assuming success. Second, write a Python script that groups the UTF-8 CSV by `sensor_id`, sorts each group by `timestamp` ascending, and takes the first `int(0.2 * len(group))` rows per group (truncating, not rounding) — this integer-truncation detail is the easy place to get the row counts wrong, so compute it explicitly per group rather than sampling globally or randomly. Third, load the sampled CSV into a fresh SQLite DB (`sqlite3 sensors.db` with `.mode csv` + `.import`, or Python's `sqlite3`/`pandas.to_sql`) into a table literally named `readings`. Fourth, run `SELECT sensor_id, AVG(temperature) AS avg_temperature FROM readings GROUP BY sensor_id ORDER BY sensor_id ASC` and export to CSV with headers via `.headers on .mode csv` in the sqlite3 CLI or `pandas.to_csv`, checking the numeric formatting matches expected precision.