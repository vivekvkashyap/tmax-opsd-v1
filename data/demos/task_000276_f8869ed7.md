Do this in three bash-only phases, being careful with UTF-8 character (not byte) counting throughout since locale strings contain multi-byte characters. Phase 1: for each *.tsv in incoming/, derive the locale from the filename, then within each file resolve duplicate string_keys by keeping the entry with the most characters — sort/group with awk, e.g. read each line, track `length()` in a UTF-8-aware locale (`LC_ALL=en_US.UTF-8` or `LC_ALL=C.UTF-8` so awk's length() counts characters, not bytes) keyed by string_key, keeping the longer translation on ties/conflicts. Load the deduped rows into SQLite using `sqlite3 locales.db ".mode tabs" ".import"` or `INSERT OR REPLACE INTO translations VALUES(...)` per row (INSERT OR REPLACE handles the "insert new or overwrite existing" requirement on the primary key). Phase 2: read template.html and for every `__KEY__` placeholder, query the jp translation for KEY from the now-updated DB and substitute with sed/awk, leaving placeholders with no jp match untouched (do a single query fetching all jp key/value pairs, then loop substitutions rather than one query per placeholder). Phase 3: query es rows sorted by string_key, compute a trailing window (up to 3) average of character length per line with awk, rounding to 1 decimal (`printf "%.1f"`), writing key:avg lines.