Scaffold with `cargo new risk_pipeline` inside `/home/user/`, then add `csv` and `serde` (with the `derive` feature) to `Cargo.toml`. Define a `#[derive(Deserialize)]` struct matching the input columns (`id`, `age`, `income`, `category`) and use `csv::Reader::from_path` to stream `/home/user/data/records.csv` record by record rather than loading it ad hoc with manual string splitting — this avoids quoting/whitespace bugs. Implement the feature engineering (normalize age/income, one-hot encode category with 'C' as baseline) as a small pure function so it can be called both from `main` and from a `#[cfg(test)] mod tests` block containing `test_reproducibility`; compute the expected score for the given fixed inputs by hand-evaluating the formula rather than guessing, and assert with a tolerance-free `assert_eq!` after formatting to a fixed precision. When writing `predictions.csv`, use `csv::Writer` and format the score with `format!("{:.3}", score)` written as a string field (not the raw f64) so trailing zeros are preserved. Run `cargo build`, execute the binary to produce `predictions.csv`, then run `cargo test` and confirm `test_reproducibility` appears in the output and passes before finishing.
