You don't need to reverse-engineer `/app/arc_tool` — treat it purely as a black-box compressor you invoke as `/app/arc_tool <input> <output>` on the filtered text; focus your effort on the log filtering, concurrency, and TCP protocol. Extract CRITICAL blocks with an `awk` state machine keyed on the `--- RECORD START ---`/`--- RECORD END ---` markers: buffer lines between them, and only emit the whole buffered block (preserving its internal newlines) if it contains `LEVEL: CRITICAL`. Build the server as a loop around `socat TCP-LISTEN:9000,fork,reuseaddr` (or an `nc -l` accept loop) invoking a per-connection handler script; inside that handler, wrap the extract+compress work in `flock -x <fd> /tmp/archive.lock` but still use per-request unique temp filenames (`mktemp`) so concurrent requests for different dates don't collide even while serialized by the lock. Respond with `SUCCESS <size>\n` followed immediately by the raw `.arc` bytes (get size via `wc -c` or `stat -c%s`) or `ERROR NOT_FOUND\n` if no log exists for that date. Append one line per request to `/home/user/server.log` in the exact timestamp/status format, then background the server and confirm it's actually listening on 9000 before finishing.
