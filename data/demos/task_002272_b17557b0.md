First recover the error code: extract frames from `/app/system_incident.mp4` in the 00:02-00:05 window with `ffmpeg -ss ... -to ... -vf fps=... frame_%03d.png`, then read the flashing text off the frames (OCR with `tesseract` or visually inspect) to get the 5-character alphanumeric code — do not hardcode a guess. Then build `/home/user/storage_cleaner.py` with three independent, order-safe passes over `os.walk` (use `topdown=True` and `followlinks=False` so you control traversal): (1) for symlinks, use `os.path.realpath` and compare against each ancestor of the symlink's directory to detect self/ancestor-referential loops, then `os.unlink` and log; (2) for `.zip`/`.tar.gz`, verify integrity with `zipfile.ZipFile(...).testzip()` / `tarfile.open(...)` inside a try/except and delete on any exception; (3) for `.log` files, read content, check for the recovered code, and if present write output to a `tempfile` in the same directory then `os.replace()` it over the original for atomicity — never edit the log in place. Test against both `/app/corpora/clean/` and `/app/corpora/evil/` to confirm clean files are untouched and evil ones are fully sanitized.
