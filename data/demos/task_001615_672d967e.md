Separate the two deliverables and validate each independently. To get `B`, extract frames from `/app/server_logs.mp4` (e.g. `ffmpeg -i /app/server_logs.mp4 frame_%04d.png`, or decode via OpenCV) and count frames whose pixels are exactly all-black (RGB 0,0,0) — check the whole frame, not just a sample pixel, since a frame with a tiny black region isn't fully black; verify your count with a script before wiring it into the C++ program since an off-by-one here silently breaks every downstream operation. For `/home/user/solution.cpp`: keep the in-memory map as `std::map<std::string,std::string>` (auto-sorted for DUMP/COMMIT). For obfuscation, treat each character as an `unsigned char`, add `B` mod 256 to encode and subtract `B` mod 256 to decode — do the arithmetic in an unsigned 8-bit domain so it wraps correctly rather than relying on signed `char` overflow, which is undefined behavior in C++. Implement each command exactly as specified (SET/DEL/COMMIT/LOAD/DUMP), read commands line-by-line from stdin until EOF, and make LOAD fully clear the map before repopulating. Compile to exactly `/home/user/config_manager`, and self-test with a few manual SET/COMMIT/LOAD/DUMP sequences to confirm round-tripping before considering it correct — the grader fuzzes many random command sequences and checks byte-exact stdout and file output against a reference.
