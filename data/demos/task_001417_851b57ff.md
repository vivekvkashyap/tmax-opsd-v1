Run `tesseract /app/system_diagram.png stdout` and grep the output for the `DELIMITER: <hex>` line to discover the actual 4-byte magic sequence — don't assume a value, extract it from OCR text (OCR can be noisy, so sanity-check the extracted hex is exactly 8 hex characters). Implement `log_parser.c` using `mmap`/`munmap` on the input file descriptor's size (`fstat` for length, handle the empty-file edge case where mmap of length 0 is invalid), then scan byte-by-byte: whenever the next 4 bytes match the delimiter (careful with the tail of the buffer where fewer than 4 bytes remain — those can't match), emit `[LOG_SPLIT]\n` instead of consuming them raw and increment a counter, otherwise emit the single byte. After the scan, print `\nEOF_CHUNKS: <N>\n`. Before declaring done, generate your own edge-case fixtures (empty file, all-delimiter file, no-delimiter file, delimiter-straddling-EOF) plus a batch of random binary files, and manually trace/verify your program's byte-for-byte output against what the spec requires for each case, since grading checks strict byte-for-byte equivalence against a reference implementation of this exact spec — build and validate your own solution independently rather than depending on any provided comparison target.
