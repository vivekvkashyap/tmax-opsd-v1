Treat each of the four stages as independently verifiable before chaining. First, transcribe `/app/audio/directive.wav` (e.g. with a local speech-to-text tool or by listening/analyzing the waveform) to recover the exact spoken authorization phrase after "The authorization code is..."; lowercase it and replace spaces with underscores exactly as specified, and write it to `/home/user/auth_prefix.txt` — verify the transcription carefully since every later stage depends on this string being exactly right. Second, build the Rust log filter as a real JSON parser (e.g. `serde_json`) that rejects any entry whose `file_path` contains `../` or is an absolute path outside `/backup/`, and rejects any entry whose `size_bytes` is negative or non-integer; test it manually against a few crafted valid/invalid JSON lines before trusting it on the hidden corpora, since silently passing through malformed JSON or off-by-one path checks (e.g. missing a leading-slash absolute-path case) is the likely failure mode. Third, bulk-rename files in `/app/raw_backups/` by prepending the recovered prefix and a hyphen — do this only after confirming the prefix file is correct. Fourth, copy `/app/baseline/baseline.snar` to `/home/user/secure_archive/backup.snar`, then run `tar --create --listed-incremental=/home/user/secure_archive/backup.snar -f /home/user/secure_archive/diff_backup.tar -C /app raw_backups`, and confirm with `tar -tf` that renamed files appear as expected.
