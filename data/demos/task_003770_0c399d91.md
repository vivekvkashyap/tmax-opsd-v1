Work the three phases in order, since Phase 3's input depends on Phase 2's finding. Phase 1: extract frames from `/app/breach_console.mp4` with `ffmpeg -i /app/breach_console.mp4 frames/frame_%03d.png` (or step through with a video-reading tool), then visually/OCR-scan the frames for the line showing a connection IP — read the literal digits shown rather than assuming a value; write that exact IPv4 string to `/home/user/attacker_ip.txt` and build `/home/user/block_ip.sh` with an `iptables -A INPUT -s <ip> -p tcp -j DROP` line (leave it unexecuted). Phase 2: write a C program that opens `/app/manifest.sha256`, parses each `hash  filename` line, computes SHA-256 over each referenced file under `/app/webroot/` (use an available crypto lib like OpenSSL's `EVP_Digest` or shell out to `sha256sum` if linking is painful), and reports any filename whose computed hash disagrees — write its absolute path to `tampered_file.txt`. Phase 3: write a C program that scans the tampered file's bytes for 16-digit sequences optionally separated by consistent runs of spaces or dashes between groups of 4 digits, and replaces only digit characters with `X`, leaving separators and all other content untouched — be careful with your regex/state-machine boundaries so you don't falsely match longer/shorter digit runs or ordinary numeric IDs. Since grading is character-level accuracy against a hidden golden file, test your program manually on a few hand-crafted cases covering all three CC formats plus edge cases (digits embedded in larger numbers) before running it on the real dump.