Scaffold with `cargo new sensor_analysis` in the target directory, add `csv` and `serde` (with `derive`) to `Cargo.toml`. Define a struct with fields typed `u32` for id and `f64` for temperature/humidity, and deserialize each CSV record individually (e.g. iterate `reader.deserialize::<Record>()` and match on `Ok`/`Err` per row) rather than collecting all-or-nothing — this way malformed rows like "ERR" or "invalid" simply produce a parse `Err` you skip, while valid rows continue to be collected. After filtering, implement Pearson correlation manually: compute means of the two valid-row vectors, then `sum((x-mean_x)*(y-mean_y)) / sqrt(sum((x-mean_x)^2) * sum((y-mean_y)^2))` — do not pull in an external stats crate for this part per the task's constraint. Format the final coefficient to exactly 4 decimal places (Rust's `format!("{:.4}", value)`) and write it with no trailing newline or extra whitespace unless you've confirmed the verifier tolerates it, since the check is a strict string-equality comparison against the file's contents.
