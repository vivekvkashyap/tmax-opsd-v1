Implement this as a literal translation of the described signal-processing pipeline (e.g. in Python with librosa/numpy/scipy) rather than approximating any step, since the verifier checks near-exact MSE against a reference computed by running exactly this recipe. Load `/app/sensor_data.wav`, compute `librosa.stft(y, n_fft=1024, hop_length=256, window='hann')`, split into magnitude and phase (`np.abs`, `np.angle`/unit-phase). Run `np.linalg.svd(mag, full_matrices=False)` to get U, S, Vt — take the first 3 columns/singular values. For each of the 3, take `np.abs(U[:,i])`, normalize to sum 1, and treat frequency bin centers (`librosa.fft_frequencies(sr=16000, n_fft=1024)`) as the support. Build the analytical Gaussian `exp(-((f-1000)**2)/(2*150**2))` over those same bin frequencies, normalize to sum 1. Compare each empirical distribution to the analytical one via `scipy.stats.wasserstein_distance(freqs, freqs, u_i, analytical)` and pick the argmin. Reconstruct `mag_clean = S[i] * np.outer(U[:,i], Vt[i,:])`, recombine with the ORIGINAL phase (not the clean magnitude's own phase), inverse-STFT with matching hop_length/window and the original signal length, then write 16-bit PCM at 16000 Hz to `/home/user/clean_training_data.wav`. Be careful with SVD sign ambiguity — it doesn't affect the verifier since it checks both polarities, but keep the reconstruction internally consistent (same U column sign used throughout).