Work each artifact independently and verify with real files before combining. For the C header parser, open each `.art` file in `/home/user/artifacts/`, read exactly the byte layout given (4-byte magic check against `ARTF`, then `fread`/manual byte assembly of a little-endian `uint32_t` version at offset 4, a 32-byte name buffer at offset 8 — treat it as null-terminated so stop at the first `\0` or use `strnlen`, then a little-endian `uint32_t` payload size at offset 40) and skip the payload bytes rather than reading them. Build one CSV line per file, write everything to `/home/user/inventory.csv.tmp` using `fopen`+`fprintf`, `fflush`+`fclose` (or `fsync` the fd) to ensure data hits disk, then call `rename("/home/user/inventory.csv.tmp", "/home/user/inventory.csv")` for atomicity — verify this call appears in your source since it will be checked directly. For deprecation filtering, use `grep 'DEPRECATED: '` on `deprecated.txt`, extract the name with `sed`/`awk` (e.g. cut after the literal prefix), then use `grep -v -f` or `awk -F,` matching against column 2 of `inventory.csv` to produce `final_inventory.csv`. For archive verification, run `tar -tzf backup.tar.gz` (or `gzip -t`) and check its exit status: 0 means `OK`, nonzero means `CORRUPT`, writing exactly that word to `backup_status.txt`.