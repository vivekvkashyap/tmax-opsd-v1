Translate the Cypher pattern into a sequence of joins over the two CSVs rather than trying to write one giant awk one-liner. Steps: (1) filter nodes.csv for User rows and Product rows (price > 500) and the single Category row named 'Electronics'; (2) filter edges.csv into PURCHASED edges and BELONGS_TO edges; (3) join PURCHASED edges to the filtered Products on product id to keep only purchases of qualifying products; (4) join the result to BELONGS_TO edges to keep only products whose category target is the Electronics category id; (5) join in User node names by user id. Use `awk -F,` to filter/reshape each intermediate file, `sort` on the join key plus `join -t,` for equi-joins (both sides must be pre-sorted on the join field), and finally `sort -t, -k5,5r` (or equivalent) to order by purchase_date descending — remember `sort` on date strings like YYYY-MM-DD sorts correctly lexically. Write intermediate files to a temp dir, assemble the header plus the joined rows into `/home/user/results.csv`, then `chmod +x /home/user/query_graph.sh`. Test by running the script and diffing against manually working out the expected joins from the CSV contents.