Diagnose the 502 by first checking what's actually missing between "socket bound" and "client gets a response" — read `backend.c` carefully: look for a missing `unlink()` of the socket path before `bind()` (causes "address already in use" on redeploy), and check whether the accept loop actually reads the request and writes+closes the response socket before looping again (a backend that accepts but never writes/closes will hang every request, which Nginx reports as a gateway failure). Fix both. For `deploy.sh`, make idempotency the design goal: compute a timestamp, `mkdir -p` a fresh `releases/<timestamp>` dir every run, compile into it as `backend_service`, then atomically repoint the `current` symlink (e.g. `ln -sfn`) to the new release dir — never assume `current` exists yet on first run. Before starting the new process, find and kill any previously running `backend_service` (e.g. via `pkill -f` or a saved pidfile) so restarts don't collide with a stale listener on the same socket path. Launch the new instance with `&` in the background, passing `$(readlink -f /home/user/app/current)/backend.sock` (or the `current` symlink path itself) as the socket argument, then start Nginx with the given config afterward and verify with `curl` before declaring done.
