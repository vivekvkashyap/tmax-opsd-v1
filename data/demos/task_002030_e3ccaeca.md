Break this into the three phases as specified and verify each before moving to the next. For Phase 1, read each line of `/home/user/doc_config.txt` as a `sed` expression and apply it to every `.md` file with `sed -i -f /home/user/doc_config.txt <file>` (or loop applying each rule) — check the config's regex syntax carefully (it may use capture groups like `\(...\)` / `\1`) and confirm the transformed files actually match by inspecting them afterward. For Phase 2, scaffold with `cargo new indexer`, add `fs2` (or `fs3`) to `Cargo.toml` for cross-platform advisory locking, spawn one `std::thread` per markdown file, have each thread find the first line starting with `# ` for the title, and before appending `Filename: Title` to `index.txt` acquire an exclusive lock via the crate's `lock_exclusive()`/`unlock()` on the open file handle — plain `OpenOptions::append` alone is not sufficient to prevent interleaved writes under concurrency. For Phase 3, after `thread::join`-ing all workers, compute SHA256 (e.g. via the `sha2` crate) over every `.md` file plus `index.txt`, and write lines formatted exactly like `sha256sum` output (`hash<space><space>filename`, filename only, no path) to `manifest.sha256`. Finally, actually run everything so the described final state exists on disk, and validate with `sha256sum -c manifest.sha256`.
