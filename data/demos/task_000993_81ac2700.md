Load the graph in Python (sqlite3 + networkx, or a hand-rolled adjacency structure), but build it as a *labeled, typed* graph so you can enforce the semantic pattern rather than doing a generic shortest-path search that would wrongly accept invalid hops like a direct User→Server edge. The cleanest approach: for each User node, do a constrained BFS/DFS where step 1 must traverse exactly one edge with relation `HAS_ROLE` landing on a `Role` node, step 2 must traverse exactly one edge with relation `CAN_ACCESS` landing on a `Server` node, and step 3+ must traverse one or more `CONNECTS_TO` edges (allowing revisits of the same relation type but always ending on a `SecureDatabase` node) — track path state (which phase you're in) alongside the current node so you don't accidentally accept an edge of the wrong relation/type at the wrong phase. Among all valid User→SecureDatabase paths found this way, take the minimum total edge count as `min_hops`, then count how many *distinct* valid paths achieve exactly that minimum (not just count reachable databases). Exclude users with zero valid paths from the output entirely. Write results as a JSON array sorted alphabetically by user id, and don't mutate the original SQLite file — open it read-only or just run SELECT queries.
