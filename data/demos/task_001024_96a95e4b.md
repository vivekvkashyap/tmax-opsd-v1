Structure the C++ program as clearly separated passes so each is independently checkable: (1) parse the CSV into a vector of (time_min, temperature, humidity) rows; (2) build a dense 1-minute grid from min to max time_min, and for each missing minute do linear interpolation strictly between its two nearest bracketing known points (not global mean-fill, not forward-fill) — index carefully so the boundary points themselves are copied exactly, not interpolated; (3) compute mean and *population* stddev (divide sum-of-squares by N, not N-1) over the gap-filled column, then z-score normalize; (4) compute the 5-point trailing rolling mean over the normalized series, using a shrinking window (all available points, not zero-padding) for indices before the 5th; (5) write CSV and JSON with fixed 4-decimal formatting (`std::fixed << std::setprecision(4)`) matching the exact header/keys requested. Install g++ via the system package manager if missing, compile with warnings enabled, and manually trace the interpolation/rolling-mean math on the first few rows by hand to catch off-by-one errors before trusting the full output.
