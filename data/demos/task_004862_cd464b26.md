Structure `/home/user/processor` so each requirement is a distinct, testable function, since it will be fuzz-compared against a hidden reference implementation on many random inputs. First, OCR `/app/priors.png` (e.g. with `tesseract` CLI or `pytesseract`) to recover the `BAYES_PRIOR=0.XX` text, parse out the numeric prior with a regex, and treat it as a fixed internal constant — don't hardcode a guessed value, actually read the image. Then read stdin line by line, splitting each row on commas with a `maxsplit` of 2 (since Text_Data could itself contain commas — check the oracle's exact split behavior by testing edge cases) into text/X/Y; tokenize text on whitespace via `.split()` and count tokens. Accumulate all X and Y values, computing population covariance (divide by N, not N-1) using the standard mean-of-products-minus-product-of-means formula. Compute likelihood as the product of all X's and all Y's respectively, apply Bayes' theorem with your OCR'd prior, and special-case a zero denominator to output `0.0000`. Match the output format exactly — token/X/Y lines each rounded to 4 decimals, one blank line, then `COVARIANCE:` and `POSTERIOR:` lines — with nothing else on stdout, since exact byte-for-byte matching against a reference is being tested.