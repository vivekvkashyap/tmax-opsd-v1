Debug the vendored build methodically: run `make all` in `/app/vendored/cJSON` and read the actual linker/compiler errors rather than guessing. Undefined references to math functions (`pow`, `floor`, etc.) during linking point to a missing `-lm` in `LDFLAGS` — restore it. If `make test` fails on numeric parsing even after linking succeeds, check the `Makefile`'s `CFLAGS` for something like `-DENABLE_LOCALES=1` and check the shell environment for a non-`C`/`en_US` `LC_NUMERIC` (e.g. `fr_FR.UTF-8`, which makes locale-aware code parse floats with commas) — remove the flag and/or unset/reset the locale variable so `make all && make test` pass cleanly. For the filter script, write `/home/user/filter.sh` to validate a JSON file robustly: check for valid UTF-8 (e.g. `iconv -f utf-8 -t utf-8`), reject on parse failure or on structural red flags (excessive nesting depth, non-finite/overflowing floats like `1e999`) with `python3 -m json.load` or `jq`, exiting 1 for anomalies and 0 only for clean logs. Test against both corpora before finalizing and inspect any misclassified file to refine the checks.
