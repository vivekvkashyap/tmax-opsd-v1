Recover the binary format empirically before writing the guard, then implement the guard as a pure Python parser — do not shell out to `/app/pka_extract`. Use `xxd`/`hexdump` on `/home/user/samples/safe.pka` and `malicious.pka` side by side, and `strings`/`objdump -d` on `/app/pka_extract`, to work out the header magic, entry count field, and per-entry layout (path length, path bytes, a type flag distinguishing file/dir/symlink, and a data-size field followed by data) — confirm your byte offsets by manually decoding one entry from `safe.pka` against what you'd expect. Implement `pka_guard.py` to stream-parse entries using `struct.unpack` on fixed-size headers, seeking/skipping over file data payloads (never reading large `Data` blocks fully into memory — use `f.seek(size, 1)` to skip past regular-file contents), and for each entry: reject if the path is absolute (starts with `/`) or if `os.path.normpath` on it would escape the extraction root (traversal via `../`), and for symlink entries apply the same absolute/traversal check to the *target* path read from Data. Exit 0 only if every entry passes; exit non-zero on first violation. Validate against both sample files before trusting it on the corpora.