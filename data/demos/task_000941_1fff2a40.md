Structure this as a pipeline of small, independently-verifiable steps in Python against `/home/user/backup_catalog.db`, checking intermediate results before writing the final script. First identify root jobs (`parent_id IS NULL`) and join to `configs`, parsing each root's `settings` JSON with `json.loads` and keeping only roots where `retention_policy == "long_term"` exactly. Second, for each valid root, reconstruct its full descendant tree recursively (either a recursive SQL CTE with `WITH RECURSIVE` on `parent_id`, or a Python BFS/DFS following `parent_id` links) — be careful to include multi-level chains and multiple branches under one root, not just direct children. Compute `total_chain_size_bytes` as root size plus all descendants' sizes, `num_incrementals` as the descendant count (root excluded), and `avg_incremental_size_bytes` rounded to exactly 2 decimals (0.00 when there are no descendants — watch for division by zero). Third, sort by `total_chain_size_bytes` DESC then `root_job_id` ASC, and slice for page 2 with page size 2 using `results[2:4]` (0-indexed) since "page 2" means items 3-4, not items 1-2. Finally write the CSV with `csv.writer`, exact header order, and confirm row count and field formatting by re-reading the file.