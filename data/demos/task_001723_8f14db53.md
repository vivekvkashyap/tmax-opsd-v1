First inspect `/home/user/graph.db`'s schema and sample rows to understand the actual node labels and edge structure before writing queries. For indexing, add indexes on the columns used for joins/filtering — e.g. `edges(source)`, `edges(target)`, and `nodes(id)`/`nodes(label)` — write these as `CREATE INDEX` statements in `/home/user/indexes.sql` and execute them against the DB with `sqlite3 graph.db < indexes.sql`, then confirm they exist via `sqlite_master`. In `/home/user/etl_pipeline.py`, use `sqlite3` to join `edges e1` (Supplier->Manufacturer) to `edges e2` (Manufacturer->Retailer) on `e1.target = e2.source`, then join both `nodes` to confirm the label of each of the three nodes matches Supplier/Manufacturer/Retailer respectively — this label check is essential since node IDs alone don't tell you the role, and a naive query without it would include invalid chains through wrong-labeled intermediate nodes. Sum `e1.cost + e2.cost` as `total_cost` per chain, then for each distinct manufacturer keep only the minimum-cost chain (e.g. via `MIN()` grouped by manufacturer, or window functions/ROW_NUMBER, or a Python-side min-reduce over query results). Sort the final list by `total_cost` ascending and write it as a JSON array matching the exact field names to `/home/user/optimized_chains.json`.