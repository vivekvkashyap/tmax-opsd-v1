Read `/home/user/leaky_pipeline.py` first and identify every place a transform is `fit` on the full dataset before the train/test split (imputer, percentile clipping, PCA) — that's the leakage. Rewrite the script so the very first operation is the 800/200 split, then for each step: `fit` the transformer (median imputer, percentile thresholds, PCA) only on the training slice, and apply (`transform`, or manual clipping with the training-derived percentiles) to both train and test using those training-fitted parameters — never call `fit` or compute percentiles on test data or on the combined array. Keep `PCA(n_components=10, random_state=42)` and `KNeighborsClassifier(n_neighbors=5, metric='euclidean')` exactly as specified so results are reproducible. Save predictions with the exact two-column header `user_id,recommended_item`. Finally write `/home/user/run.sh` that exports `OMP_NUM_THREADS=1`, `OPENBLAS_NUM_THREADS=1`, `MKL_NUM_THREADS=1` before invoking `python3 leaky_pipeline.py` (single-threaded BLAS avoids nondeterministic reduction order affecting PCA/KNN reproducibility), `chmod +x` it, and run it to produce `/home/user/recommendations.csv`.