Work this incident in stages, verifying each before moving on. First, recover the leaked secret from the core dump with `strings /app/dumps/worker_crash.core | grep PAYMENT_SEC_` and wire it into the services (e.g. write `/app/.env` with `PAYMENT_API_KEY=...` and make sure `start_services.sh` sources it, or `export` it before launching). Second, repair the SQLite DB by checkpointing the WAL into the main file, e.g. `sqlite3 /app/data/bookings.db "PRAGMA wal_checkpoint(TRUNCATE);"`, then confirm with `PRAGMA integrity_check;` that corruption is cleared and WAL records were merged. Third, read `ticket_worker.py` closely for two independent bugs: a lock-ordering deadlock where different threads acquire `db_lock`/`api_lock` in opposite orders (fix by making acquisition order consistent everywhere) and an off-by-one in the connection pool's limit check (a `>` that should be `>=`, or an increment-before-check ordering that lets `active` exceed `max_limit`). Fix both, then start all services via `./start_services.sh` and confirm each responds on its port before load testing. Finally run `python3 /app/tests/load_test.py`, saving its output to `/home/user/load_test_results.txt`, and iterate on the pool sizing/connection reuse if throughput falls short of the 800/sec target.
