Translate each compliance rule into its own SQL query rather than trying to do everything in application code — SQLite is capable of expressing all three directly. For "Toxic Permissions", a simple `WHERE permissions LIKE '%APPROVE_FUNDS%' AND permissions LIKE '%REQUEST_FUNDS%'` over the JSON-array-as-text column suffices. For "Anomalous Volume", use a window function: `COUNT(*) OVER (PARTITION BY employee_id ORDER BY strftime('%s',timestamp) RANGE BETWEEN 259200 PRECEDING AND CURRENT ROW)` (259200 = 3 days in seconds) and select employees where this ever exceeds 5. For "Unauthorized Access", build the full management chain per employee with a recursive CTE (`WITH RECURSIVE ... employees.manager_id`) walking upward from each owner, then flag access_logs rows where the accessor is neither the owner nor anywhere in that owner's ascending chain of managers. Add indexes on `access_logs(employee_id, timestamp)` and `employees(manager_id)` before running these queries (the task explicitly requires this), capture `EXPLAIN QUERY PLAN` for the unauthorized-access query to the required file, and make sure every output list is deduplicated and sorted ascending as integers before writing the JSON.