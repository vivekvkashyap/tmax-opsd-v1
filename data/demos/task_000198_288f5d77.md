Build this as a strict pipeline and follow the spec's exact parameters rather than defaults, since scoring depends on matching them precisely. Step 1: fit one shared `TfidfVectorizer(analyzer='char', ngram_range=(2,3))` on both query columns combined (so both are embedded in the same vector space), compute pairwise cosine similarity between each `stats.csv` row and all `text.csv` rows, take the best match per row, and drop rows whose best similarity is not strictly greater than 0.4. Step 2: derive `feature_X = val_A * val_B` on the merged frame. Step 3: scale `feature_X` with `StandardScaler`, separately vectorize `doc_content` with a word-level TF-IDF (`ngram_range=(1,1)`, `max_features=100`), horizontally stack the two feature blocks (e.g. `scipy.sparse.hstack`), then run `GridSearchCV(Ridge(), {'alpha': [0.1, 1.0, 10.0]}, cv=3, scoring='neg_mean_squared_error')` with no shuffling. Read `best_params_['alpha']` and take the absolute value of `best_score_`, rounded to 4 decimals, and write both fields to `/home/user/model_results.json` in the specified JSON shape.
