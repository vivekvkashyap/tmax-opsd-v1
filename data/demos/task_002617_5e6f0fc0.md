Start by black-box probing `/app/legacy_parser` rather than guessing its interface: run `file`, `strings`, and try invoking it with no args, `--help`, and a sample .bin path from the clean/evil corpora to see what it expects and what it prints. Confirm its output shape (likely CSV rows of floating-point numbers) by running it against a few files from both corpora and diffing the row/column counts. Once you can reliably extract a numeric matrix per file via `subprocess`, the real work is choosing a discriminative statistic: since the task hints at a "dimensionality/covariance constraint" that clean data respects and evil data violates, compute the SVD (or PCA) of each extracted matrix (numpy's `np.linalg.svd` on the mean-centered data) and look at how many singular values are effectively non-zero versus how many are comparable in magnitude — clean signals should collapse onto a lower-rank subspace while poisoned ones will spread across all dimensions. Empirically determine the rank/threshold cutoff by running this analysis over the full known clean and evil corpora first, picking a boundary that cleanly separates the two populations before finalizing the exit-code logic, then test against the corpora to confirm 100% separation before submitting.