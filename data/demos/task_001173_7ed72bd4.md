Build and validate this system layer by layer rather than all at once, since the grader exercises each phase separately. Start with `dag.py` in isolation: implement `ArtifactDAG.add_artifact` storing each `name@version` node with its dependency list, detect cycles at insertion time (e.g. DFS from the new node through its dependency chain looking for a path back to itself, or attempt a full topological sort and raise `CyclicDependencyError` if it fails), and implement `get_build_order` via standard DFS-postorder or Kahn's-algorithm topological sort so dependencies always precede dependents — write and run quick manual tests for a simple chain and a cycle before wiring in the API. For the API, wrap the DAG in a single in-memory instance shared across requests, add the two endpoints with the exact status codes specified (201/400/404), and add real per-IP rate limiting (e.g. `slowapi`'s `Limiter` tied to `get_remote_address`, decorated with `@limiter.limit("10/minute")` on the POST route) — test it manually by curling the endpoint more than 10 times before trusting it returns 429. Write `test_app.py` against the real `ArtifactDAG`/API objects (not mocks that assume behavior). Finally, `start_server.sh` should activate the venv, launch the server with `nohup ... &` (or similar), and exit 0 immediately, leaving the process running on port 8080.
