In `main.rs`, use the `csv` crate's `Reader` in its default streaming mode (iterate `records()`/deserialize row-by-row) rather than reading the file into a `String` first, so you never materialize the whole input in memory. Normalize `email` with `.to_lowercase()` and `.trim()`. Build the ID-extraction regex once outside the row loop (e.g. `Regex::new(r"(?i)id[^\d]*(\d{6})").unwrap()`) and apply `captures()` to each `notes` field; if there's no match, discard the row immediately. Maintain a `HashSet<String>` (or similar) of IDs already written and skip any row whose extracted ID is already present, so only the first occurrence per ID survives — process rows strictly in file order for this to be correct. Write survivors immediately via a `csv::Writer` with header `extracted_id,name,email` rather than buffering all rows. After `cargo build --release`, run the binary once manually to produce the output file, then verify it matches expectations by inspecting each row's normalization and dedup outcome yourself. Finally install the cron entry with `(crontab -l 2>/dev/null; echo "30 2 * * * /home/user/csv_processor/target/release/csv_processor") | crontab -`, using the absolute path to the release binary.
