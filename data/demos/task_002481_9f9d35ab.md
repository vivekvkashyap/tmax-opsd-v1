Structure the C program around the Linux inotify API: `inotify_init1`, `inotify_add_watch` on the parsed WATCH_DIR with `IN_CLOSE_WRITE`, then a blocking `read()` loop parsing `struct inotify_event` records (remember event structs are variable-length, buffer size must account for `name_len`). Parse `/home/user/config.txt` simply by finding the substring after each `=` on its two lines. For each closed-write file, skip if `S_ISDIR` (use `stat`/`lstat`), then read its full contents and `memcmp` against each regular file currently in ARCHIVE_DIR (read those fresh each time, don't cache) to find an exact byte match. On match: `unlink` the file in WATCH_DIR, then `link()` a hardlink at the same path pointing to the ARCHIVE_DIR match; on no match: move (`rename`) the file into ARCHIVE_DIR keeping its basename, then `symlink()` an absolute path back into WATCH_DIR under the original basename. Append the exact log line formats (`NEW: <f> -> <f>` / `DUPLICATE: <f> -> <matched>`) to `/home/user/dedup.log` after each action, and fflush/fsync so the log is visible even though the program loops forever. Compile with `gcc /home/user/storage_dedup.c -o /home/user/storage_dedup`.