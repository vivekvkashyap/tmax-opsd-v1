Write a single Python script that does the full pipeline end-to-end and dumps JSON at the end, rather than computing things interactively and retyping numbers. Load both CSVs with pandas. For "incorrect" standardization, compute each test column's own mean and std with `ddof=0` (population std, not the pandas default `ddof=1` sample std — this is the most common mistake) and apply `(x - mean)/std` per column. For "correct" standardization, reuse the same formula but source mean/std from the train columns instead, applied to the test data. Run Welch's t-test with `scipy.stats.ttest_ind(a, b, equal_var=False)` comparing the incorrect vs. correct standardized `feature_1` series — do not use the default equal-variance t-test. Compute the dot product of the correctly-standardized `feature_1` and `feature_2` vectors with `numpy.dot` (or `@`), not element-wise multiplication summed incorrectly or a correlation. Round every numeric output to exactly 4 decimal places with Python's `round()`, cast to native `float` (not numpy float64, which won't serialize cleanly), and write a JSON object with exactly the keys `t_statistic`, `p_value`, `dot_product` to `/home/user/leakage_report.json` using `json.dump`. Double check ddof=0 is applied consistently everywhere std is computed.
