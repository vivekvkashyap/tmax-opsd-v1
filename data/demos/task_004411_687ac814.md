First recover the leaking class name by OCR'ing the screenshot (`tesseract /app/dashboard.png stdout` or similar) rather than guessing — read the extracted text carefully since dashboard alerts often include exact identifiers you need verbatim to grep for in the source. Search `/home/user/service.py` for every place that class is instantiated and look specifically for an unbounded global collection (a module-level list/dict/cache) that instances get appended to but never removed from — that unbounded accumulation, not the class definition itself, is almost always the actual leak in this kind of task. Fix it by adding a bounded eviction policy appropriate to how the collection is used (e.g. cap its size with an LRU/ring-buffer eviction, or drop entries once their associated processing is complete, or switch to a weak reference) while preserving the daemon's real aggregation output. Confirm the fix empirically yourself: run the script for several thousand iterations locally while sampling RSS via `psutil` or `/proc/<pid>/status`, and check growth between an early and late checkpoint is small and roughly flat (not just decreasing more slowly) before finishing, since the grader specifically measures RSS growth between iteration 500 and 5000 with a 2MB budget.
