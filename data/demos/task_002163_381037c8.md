Load the CSV with pandas and first get deterministic point estimates via `scipy.optimize.curve_fit` on the 4-parameter logistic $F(T)=L+(U-L)/(1+\exp(-k(T-T_m)))$, using sensible initial guesses (e.g. L≈min(F), U≈max(F), Tm≈midpoint of the temperature range, k≈0.5) — a common failure mode is letting curve_fit diverge from a bad initial guess, so inspect the fitted params before proceeding. Use those as the starting point / prior center for MCMC: either use `emcee` with a Gaussian likelihood (residual variance estimated from the fit or as an extra nuisance parameter) and broad uniform priors on L, U, k, Tm, or implement a simple Metropolis-Hastings sampler with a reasonable proposal step size, run enough iterations with burn-in discarded, and check chain mixing/acceptance rate informally. From the post-burn-in Tm samples, compute the mean and the 2.5th/97.5th percentiles for the 95% credible interval — write these as floats into the exact JSON keys `tm_mean`, `tm_2_5`, `tm_97_5` at `/home/user/results/tm_posterior.json`. Also save a plot overlaying the raw data with the best-fit curve (mean posterior params) to `/home/user/results/fit_plot.png`, creating the results directory first.