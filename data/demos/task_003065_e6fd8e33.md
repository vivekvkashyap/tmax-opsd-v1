cd into /app/geo_mask-1.2 and inspect the Makefile and main.c: the build fails because the compile/link command omits `-lm`, which is required since the program calls `sqrt()`. Fix the Makefile by adding `-lm` to the gcc invocation (order matters on some linkers — put `-lm` after the source/object files), then run `make` to produce the executable, and copy it to /home/user/processor. Before considering it done, read main.c's actual per-line behavior closely (masking the user_id field, formatting distance to 2 decimals, and the exact ASCII-only rule for the notes field) and manually test the binary with a few crafted CSV lines via stdin — including lines with non-ASCII notes (should be dropped) and edge-case numeric formatting — since the grader performs bit-exact fuzzing against a hidden oracle built from the same unmodified main.c, so your only job is fixing the link step correctly, not altering program logic.