Diagnose before rewriting: the root cause is that fitting a degree-2 polynomial in raw Unix-timestamp scale (~10^9) makes the Normal Equation's $X^TX$ matrix involve terms up to ~10^36, which loses all precision under f64 inversion — confirm this by observing NaN/garbage coefficients from the current code before touching anything. First bring up the stack via `/app/telemetry_system/start_services.sh` (Redis, telemetry generator, JupyterLab, and later your own Rust service). You may use the Jupyter notebook to prototype the fix in Python/numpy first — verify that Z-score normalizing the x values (`x_scaled = (x - mean) / std`) before fitting the polynomial, then de-normalizing consistently at predict time, keeps the MSE low — before porting the working approach into `/app/telemetry_system/analyzer/src/regression.rs`. In the Rust fix: compute mean and std of the training timestamps once, store them alongside the fitted coefficients, transform all training x values through them before building $X^TX\beta=X^TY$, and in the `/predict` (or `fit_and_predict`) endpoint apply the *same stored* mean/std to incoming timestamps before evaluating the polynomial — a common mistake is normalizing during training but forgetting to normalize prediction inputs identically. Rebuild with `cargo run --release`, then run `/app/telemetry_system/evaluate.py` and check the printed `FINAL_MSE` is below 0.05 before concluding.