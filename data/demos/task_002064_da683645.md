Work this as a three-step pipeline, verifying each stage before moving to the next. First, open `/home/user/mcmc_fit.ipynb` and locate the single hardcoded `dt = 0.5` inside the ODE integrator function — change only that literal to `dt = 0.01`, leaving all `np.random.seed(...)` calls and the MCMC step count untouched so the run stays deterministic. Second, execute the notebook headlessly with something like `jupyter nbconvert --to notebook --execute --inplace /home/user/mcmc_fit.ipynb` (or `papermill`), and check the process exits cleanly and `/home/user/posterior.h5` was actually created with a `chain` dataset — inspect it with `h5py` or `h5dump` to confirm shape and that it's free of NaN/inf before proceeding. Third, load `chain` with `h5py`, slice `chain[100:, 0]` to drop the first 100 burn-in rows and select the alpha column, compute `np.mean(...)`, and write the value to `/home/user/alpha_mean.txt` formatted to exactly 2 decimal places (e.g. via Python f-string `f"{val:.2f}"`, not manual truncation). Sanity-check the mean is a small positive finite number before finishing.