Build this as a manifest-driven incremental loop: read `processed_manifest.txt` into a set up front, then for each file in `raw_logs/`, compute its target filename first and check set-membership before doing any I/O on that file — this is what makes it correctly skip already-processed files rather than reprocessing and overwriting. Derive the target name with a regex or simple split on `System Log - (\d{4}-\d{2}-\d{2}) - (.+)\.csv`, converting the date by stripping the dashes. Open each source file with `encoding='iso-8859-1'` and write the output with `encoding='utf-8'` — get this direction right, since reading as UTF-8 would raise or corrupt on the accented characters in the fixture. When filtering lines: strip leading whitespace before checking for a `#` prefix (comment lines), keep the header line unconditionally, and for data rows split on comma and check the 3rd field (0-indexed position 2) equals exactly `SUCCESS` to drop it — keep everything else including FAILED/ERROR. After successfully writing each output file, immediately append its filename to the manifest (so a crash mid-run doesn't leave you in an inconsistent state) rather than batching all manifest updates at the end.
