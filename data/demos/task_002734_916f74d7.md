Write `/home/user/curate.py` to walk `/home/user/artifacts_raw/` recursively (`os.walk` or `glob('**/*.tar.gz', recursive=True)`) collecting every `.tar.gz`. For each, open it with `tarfile.open(path, 'r:gz')` and use `tar.extractfile('meta.ini')` to get a file-like object without extracting to disk, then parse it with `configparser.ConfigParser` (read via `.read_string()` on the decoded bytes) to pull `id` and `payload_file` from the `[Artifact]` section. Write `/home/user/artifacts_curated/<id>.json` as `{"id": ..., "payload_file": ...}` — create the target directory first with `os.makedirs(exist_ok=True)`. Then use `tar.extractfile(payload_file)` to read the raw binary payload bytes (do not decode/re-encode as text — read in binary mode throughout, since payloads are arbitrary binary data including null bytes), prepend the literal 8-byte ASCII sequence `CURATED_` to the front, and write the result to `/home/user/artifacts_curated/<id>.bin` in binary mode (`open(..., 'wb')`). Process all three archives found under the nested group directories. After running, spot-check one output file's hex bytes against the archive's known payload to confirm the prepend is exactly 8 bytes and nothing else was altered.