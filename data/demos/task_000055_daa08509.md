Build `/home/user/edge_manager.py` as a strict sequence of independently-testable checks, writing to the log incrementally so partial failures are diagnosable. Use `shutil.disk_usage('/home/user/device_data')` (creating the dir first) to check free bytes against a 5MB threshold, appending the exact `STORAGE: OK`/`STORAGE: FAILED` line. For connectivity, attempt a raw `socket.socket(...).connect(('127.0.0.1', 9090))` wrapped in try/except (with a short timeout) rather than shelling out to `nc`, since that's more portable — append `CONNECTIVITY: OK`/`FAILED` accordingly, and remember to actually start `mock_service.py &` first or this will always fail. For the interactive part, use `pexpect.spawn('python3 /home/user/interactive_cli.py')` and drive it with `expect()`/`sendline()` pairs matching each literal prompt string exactly (`Enter Edge ID: `, `Enter Passcode: `, `edge-shell> `) in the order given, sending `stop legacy-sensor` then `start vision-sensor-v2` then `exit` — mismatched prompt regexes are the most common pexpect failure, so test interactively first with `python3 -i` or a small pexpect snippet before wiring it into the full script. Only append `DEPLOYMENT: SUCCESS` after `expect(pexpect.EOF)` confirms the child exited cleanly.
