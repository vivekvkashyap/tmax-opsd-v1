First isolate and fix the vendored bug in isolation: open `/app/pylitecsv-1.0.0/pylitecsv/engine.py` and find the `inner_join` implementation — it's building `{**l, **r} for l in left for r in right` with no equality check, i.e. a full Cartesian product; add the missing condition comparing `l[left_key]` and `r[right_key]` before including a pair. Install it editable (`pip install -e /app/pylitecsv-1.0.0`) and sanity-test the join directly against the two CSVs in a quick REPL before building the server, so you know the join is correct in isolation. Then build `server.py` on Python's `http.server`/`BaseHTTPRequestHandler`, parsing the query string with `urllib.parse.parse_qs` for `limit`, `offset`, `sort`, `order`, and `category` — apply category filtering first, then sort (respecting `asc`/`desc` and the right key type), then paginate with slicing, in that order, since doing pagination before sort/filter gives wrong pages. Start the server as a background process with logs redirected to `server.log`, and only write `/home/user/ready.txt` after confirming the server actually accepts a connection (e.g. a local self-test request), not immediately after spawning it.
