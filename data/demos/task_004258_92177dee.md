Before touching the model, diagnose the data loading step: print `train['device_id'].dtype` and compare a handful of raw CSV string values for `device_id` against what pandas loaded into the DataFrame — look specifically for ID values that don't round-trip exactly. This is a classic pandas pitfall: when a column has missing values, `read_csv` promotes it to `float64` even if all present values are integers, and float64 only has ~15-17 significant decimal digits of precision, so very large integer IDs get silently rounded to the nearest representable float, collapsing distinct IDs into the same value and destroying the signal. Fix it at the `pd.read_csv` call by forcing a dtype that preserves exact large-integer values through missing data (a nullable/extension integer type, or read as string and keep as string/categorical for encoding) rather than letting it default to float and later casting to string after precision is already lost. Don't touch imputation, encoding, model type, or random_state — only the dtype used when reading `device_id`. After fixing, rerun the pipeline and confirm the printed cross-validation accuracy clears the 0.95 bar before declaring success.