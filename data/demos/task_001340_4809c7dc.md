Work the forensics before writing the honeypot. Transcribe `/app/voicemail.wav` with a local speech-to-text tool (e.g. `whisper`) and read the result to identify the passphrase — normalize it yourself (lowercase, strip punctuation) as the spec requires. Compute SHA256 of every file under `/app/www/` and diff against `/app/manifest.sha256` to find the mismatched file, noting the manifest's ORIGINAL hash for it (not the current, tampered hash — that's what you serve back). Grep `/app/access.log` for a suspicious query-string payload (`UNION`, `SELECT`, stray `'`) and extract the exact injection substring. Implement the honeypot as one script running two servers concurrently on separate threads, both bound to `127.0.0.1` only: an HTTP server on 8080 checking the `X-Passphrase` header against your recovered passphrase (200 + original hash, or 403), and a raw TCP listener on 8081 replying to the literal `SEND_PAYLOAD\n` with the SQL payload + newline. Background the process so it survives your session, then test both with `curl` and `nc` before finishing.