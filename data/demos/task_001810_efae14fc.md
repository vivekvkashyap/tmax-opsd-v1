Set up a Rust binary crate at `/home/user/vibration_analyzer` with dependencies for video decoding (e.g. `ffmpeg-next`, or shell out to the `ffmpeg` CLI to dump raw frames if the crate proves painful), FFT (`rustfft`), and HDF5 output (`hdf5` crate). Decode `/app/reactor_vibration.mp4` frame-by-frame in strict order, and for each frame extract only the pixel block x∈[0,99], y∈[0,99]; convert each pixel to grayscale via `0.299*R + 0.587*G + 0.114*B` and average the 10,000 values into one f64 per frame — keep summation order consistent (e.g. simple left-to-right, top-to-bottom accumulation) across runs for determinism. Collect the per-frame means into a `time_series` vector, run a real-to-complex FFT over it, take magnitudes, and find the peak bin excluding index 0 (DC). Convert bin index to Hz using `bin * sample_rate / N` where `sample_rate = 60.0` (the video's FPS) and `N` is the number of frames/samples. Write `time_series` (1D f64 array) and `dominant_frequency` (scalar f64) to `/home/user/analysis.h5` via the `hdf5` crate. Build with `cargo build --release`, run the binary, and confirm the HDF5 file was created before considering the task done — the resulting frequency will be checked against a hidden target with a tight tolerance, so precise, unbiased frame-region averaging and correct bin-to-Hz conversion matter most.