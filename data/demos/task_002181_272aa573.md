Start by reproducing the failure: run `cargo test` in `/home/user/kepler_solver` and note which (e, m) pair fails to converge — high eccentricity is the clue that the Newton-Raphson update step itself is wrong, not just the iteration count. Kepler's equation is f(E) = E - e·sin(E) - M = 0, so the correct derivative is f'(E) = 1 - e·cos(E); inspect `src/lib.rs`'s derivative line and compare its sign against this — a sign error there causes the update `E_curr - f/f'` to diverge instead of converge for larger e. Fix only that one line (don't touch the convergence loop structure, epsilon, or test file), rerun `cargo test` until it passes cleanly, then write `/home/user/debugging_summary.txt` with exactly three lines: the original buggy line as it appeared (trimmed), your corrected line (trimmed), and the literal text `ALL TESTS PASSED` on the third line — copy the lines verbatim from the diff you made rather than retyping them from memory, since exact whitespace-trimmed string equality is what gets checked.
