Build this as a strict two-stage pipeline where bash does the row-level cleanup and C does the numeric aggregation. For dedup-keep-last-then-sort-by-timestamp, don't hand-roll a scan: use `tac raw_events.csv | awk -F, '!seen[$2]++' | tac` to keep the last occurrence per translation_id while preserving relative order, then pipe through `sort -t, -k1,1n` on the timestamp field before feeding the C program — do this all in `pipeline.sh` before invoking the compiled analyzer, and compile analyzer.c with a redirect (`gcc -O2 -o analyzer analyzer.c`) as part of the same script so `./pipeline.sh` is fully self-contained. In the C program, read lines with fgets/sscanf, compute bucket_id = timestamp/3600 (integer division), and since input is sorted by timestamp you can accumulate hourly sums/counts by detecting bucket-id changes rather than needing a hash map — buckets simply appear in increasing order, some possibly skipped (real time gaps). Keep a small ring/array of the last up-to-3 *emitted* hourly averages (only buckets actually present in the stream, not calendar-consecutive) to compute the rolling average, and apply the anomaly rule hourly_average < rolling_average - 5.0 using the rolling average computed *including* the current bucket. Print all floats with `%.2f` via printf, matching the exact `bucket_id,hourly_average,rolling_average,anomaly_flag` format with no extra whitespace, since the grader does a byte-for-byte `cmp`.