First inspect `graph.csv` by hand (`cat`/`sort -u -t, -k2,2`) to enumerate the distinct predicate strings actually present — do not assume names, read them from the data so your Go code matches the real schema (entity-type triples plus two relation predicates linking User→Role and Role→System). In Go, parse the CSV into three maps: entity type by ID, and adjacency lists for each relation. For centrality, for every User node, follow its role-assignment edges to reachable Roles, then from each Role follow its access-grant edges to Systems, collecting into a `map[string]struct{}` per user to dedupe before taking the length — this directly gives "unique systems authorized." For the compliance check, parse `logs.csv` (skip header), and for each log row check whether `system_id` is in that user's authorized-set; if not, increment a `map[[2]string]int` (or nested map) keyed by (user_id, system_id). Sort the resulting violation list first by user_id then system_id (e.g. `sort.Slice` with a composite comparator) before marshaling, and only include entries with count > 0. Use `encoding/json` with ordered map construction (Go maps don't preserve key order automatically for `centrality`, but standard json marshaling of a `map[string]int` is fine since JSON object key order isn't semantically significant here). Run `go mod init audit` first, then `go run audit.go` to produce the report.
