Start with the packet capture, not the git history. Use `tshark`/`scapy`/`tcpdump -A` on `/home/user/traffic.pcap` to reassemble the TCP stream and extract the HTTP POST JSON payload (the input coordinates) and the HTTP response JSON (the expected precise distance) — this is your ground-truth test case, don't invent one. Then write a small test/check script that runs `calc.py` (via `git show <rev>:calc.py` or checking out each commit) against those exact coordinates and compares the printed distance to the value recovered from the pcap; wire this script into `git bisect run` across the 200-commit history in `/home/user/geo_service` so bisect automatically finds the first commit where the output stops matching to full precision. Record the resulting 40-char commit hash it lands on into `/home/user/bad_commit.txt`. Finally inspect that commit's diff to see exactly how precision was lost (e.g. an intermediate value rounded/truncated via string formatting), and fix `calc.py` on `main` by removing that truncation so the full-precision Haversine formula is restored — verify by rerunning your script with the recovered coordinates and confirming the output now matches the pcap's expected distance exactly.
