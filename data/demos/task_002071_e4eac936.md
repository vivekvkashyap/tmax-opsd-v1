Break this into three independent, verifiable pieces before writing the final C program. First, compute the Pearson correlation R between `inference_time_ms` and `accuracy` from the CSV by hand (or with a quick Python/awk check) so you know what the C code should reproduce: parse rows, compute means, then R = covariance / (std_x * std_y). Second, stat the audio file's byte size with `stat -c%s /app/audio/eval_sample.wav` (or `wc -c`) to get S. Third, apply Bayes' rule symbolically: P(Optimal|evidence) = P(evidence|Optimal)*P(Optimal) / [P(evidence|Optimal)*P(Optimal) + P(evidence|~Optimal)*P(~Optimal)], picking the Large/~Large branch based on whether S > 1,000,000 — implement this exact formula in C using the given constants (prior 0.6, likelihoods 0.8/0.3), not the file's specific numbers. In the C program: parse the CSV with `fopen`/`fscanf` or manual line splitting, compute R and P_post at startup, then implement a minimal blocking TCP server (`socket`/`bind`/`listen`/`accept`) that on any connection reads the request, ignores the details beyond checking it's a GET to `/metrics`, and writes a raw HTTP/1.1 200 response with the JSON body formatted via `snprintf` using `%.4f` for the two floats and `%ld` for the integer size. Run it in the background (`./server &`) and manually curl `/metrics` to confirm formatting matches exactly before finishing.