Sequence the three phases so later steps don't depend on undone earlier ones. Phase 1: `ssh-keygen -t ed25519 -N "" -f /home/user/.ssh/id_ed25519`, `rm` the old RSA pair, overwrite `authorized_keys` with only the new `.pub` contents, then `chmod 700 ~/.ssh` and `chmod 600 authorized_keys` explicitly (don't assume `ssh-keygen` sets directory perms correctly). Phase 2: write `rotate_encryption.py` to load the old Fernet key, iterate `uploads/`, decrypt each file, compute `hashlib.sha256(plaintext).hexdigest()`, and compare against the recorded hash for that filename in `file_hashes.json` — on mismatch, append the filename to `tampered.log` and delete the encrypted file from `uploads/` without re-encrypting it; on match, re-encrypt the plaintext under a freshly generated Fernet key (`Fernet.generate_key()`, saved to `new_key.key`) and overwrite the file in place. Run the script once at the end. Phase 3: add a `@app.before_request` function in `app.py` that calls `abort(403)` when `request.remote_addr != '127.0.0.1'`, keeping the existing route logic intact.
