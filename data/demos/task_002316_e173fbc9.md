Start by reading `/app/textprep_lib-0.4.5/textprep_lib/preprocess.py` to find exactly why `citations` ends up as float: a `'None'` string is being replaced with `np.nan` (which forces the whole column to float64) instead of a real int sentinel. Fix it so missing values become `0` and the column is explicitly cast with `.astype(int)`, then reinstall the package in editable mode (`pip install -e /app/textprep_lib-0.4.5`) so your later import picks up the change — verify with a quick `python -c "import textprep_lib.preprocess as p; print(p.load_and_clean(...).dtypes)"` that `citations` is now integer before moving on. Then build the pipeline: `TfidfVectorizer(max_features=100, stop_words='english')` on `abstract`, concatenate with the (now-int) `citations` column, run 5-fold `cross_val_score` with `LogisticRegression` against `is_accepted`, write the mean accuracy as a bare float to `/home/user/cv_result.txt`. Fit the same pipeline on the full data, then stand up a Flask/FastAPI app bound exactly to `127.0.0.1:5000` with a `/predict` POST route that checks the `Authorization: Bearer ds-research-token-881` header first (reject with 401/403 before touching the model if it's missing or wrong) and otherwise returns `{"prediction": <0 or 1>}`. Leave the process running.
