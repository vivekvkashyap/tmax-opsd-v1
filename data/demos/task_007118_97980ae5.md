Build this as a single Python script rather than juggling steps in the shell. Load `metadata.csv` with `csv.DictReader` and `sensors.json` with `json.load`, then join on `ID` using a dict keyed by ID for O(1) lookup. Apply validation carefully and in order: check `ID.startswith("SUB")` first, then try `int(Age)` in a try/except (treat non-numeric or missing Age as invalid, not a crash) and range-check 18-100, then try/except `float()` conversions for Sensor_A and Sensor_B. Only rows passing all checks proceed. For inference, read `model_config.json` to get the weights dict, bias, and activation name — do not hardcode the numbers, pull them from the file so your script generalizes. Compute `z` as the dot product of features with their matching weight keys plus bias, then branch on the activation string (implement sigmoid with `math.exp`; guard for other activation names generically). Round predictions with Python's `round(x, 4)` and format with fixed decimals when writing so trailing zeros aren't dropped (e.g. `f"{val:.4f}"`). Sort final rows by ID before writing with `csv.writer` to `/home/user/predictions.csv`, header `ID,Prediction` only.
