Build the Go monitor first as a standalone, testable unit: parse the passwd file into a uid→username map (split on `:`, fields are user=0, uid=2), parse the connections CSV, filter rows where status is exactly `ESTABLISHED`, look up the uid, default to `UNKNOWN` on miss, and print `[user] local remote` for each match in input order — verify it manually against a small CSV before wiring it into deployment. For the deploy script: compile with `go build -o /home/user/netmon_v2 /home/user/netmon.go`; kill old processes with something precise like `pkill -x netmon_v1` (exact-name match, not a substring), and confirm with `pgrep` that none remain before proceeding. Parse `deploy_fstab` by skipping `#`-prefixed and blank lines and taking whitespace-separated field 2 as the target directory (mirror `/etc/fstab` parsing conventions with `awk`). For each target: `mkdir -p`, copy the binary in, `cd` into it (or use `./netmon_v2` explicitly) so it runs relative to that directory, and redirect stdout to `monitor.log`. Run `deploy.sh` yourself afterward and inspect each `monitor.log` to catch path or ordering mistakes early.
