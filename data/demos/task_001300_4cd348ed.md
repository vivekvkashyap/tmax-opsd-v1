Run `tesseract /app/equation_params.png -` to OCR the prior means/variances for alpha and beta from the image — do not guess these values. With Gaussian priors alpha~N(m_a, v_a), beta~N(m_b, v_b) and eps~N(0,1), the MAP estimate for linear regression has a closed analytical form: it is equivalent to ridge regression with per-coefficient regularization strengths 1/v_a and 1/v_b. Concretely, build the design matrix X = [x_i, 1] over the data in `/app/data.csv`, and solve the regularized normal equations (X^T X + diag(1/v_b, 1/v_a)) theta = X^T y + [m_b/v_b, m_a/v_a] for theta=[beta_map, alpha_map] using exact linear algebra (e.g. numpy.linalg.solve) — do not use MCMC or a loose numeric optimizer, since the grader checks bit-exact-ish equivalence against a reference across many random x inputs. Then write `/home/user/predict.py` so it reads argv[1] as a float, computes `beta_map*x + alpha_map`, and prints it formatted to exactly 4 decimal places (e.g. via an f-string `f"{y:.4f}"`). Test it against several hand-picked x values for sanity before finishing.
