Diagnose the SQL bug before rewriting: read `generate_report.py`'s current query and note it lists `access_logs`, `users`, and `permissions` together in the FROM clause with only a `user_id` join condition and a `!=` filter on `resource_id` — that's an unconstrained cross join against `permissions` (missing a join on `dept_id`/`resource_id` together), which is why it produces false positives. The correct logic is: an access event (user_id, resource_id) is unauthorized if there is NO row in `permissions` matching that user's `dept_id` AND that exact `resource_id` — express this as a `NOT EXISTS` (or `LEFT JOIN ... WHERE permissions.dept_id IS NULL`) subquery joining `access_logs` to `users` on `user_id`, correlated against `permissions` on both `dept_id` and `resource_id`. Once the query returns only genuine violations, sort the resulting rows by `user_id` then `resource_id` and write them as `/home/user/unauthorized_edges.csv` with header `user_id,resource_id`. Separately, group those same violation rows by the user's `dept_id` and count them, writing that as a JSON object `{dept: count, ...}` to `/home/user/dept_summary.json`. Run the corrected script and inspect both output files against your own manual trace of the data to confirm correctness before finishing.