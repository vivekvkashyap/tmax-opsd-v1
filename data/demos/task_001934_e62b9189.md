Run `tesseract /app/reference_key.png stdout` (install tesseract if missing) to extract the label text, then use a regex to pull out the floating-point number after the label — don't hardcode a guessed value, derive it from the OCR output and sanity-check it looks like a plausible decimal. For CSV cleaning: write a `normalize_text(s)` function doing `unicodedata.normalize('NFKC', s)` first (this collapses styled Unicode like Fraktur letters to plain Latin), then strip characters that aren't letters/digits/whitespace with a regex, lowercase, and `.split()` to tokenize — apply this across rows using a `multiprocessing.Pool` or `concurrent.futures.ProcessPoolExecutor` since the task requires parallelism. For `value_a`, sort by `id`, then use linear interpolation only over the missing (empty-string) entries between known numeric neighbors (pandas `interpolate()` or manual slope math) — don't just fill with a constant. Multiply `value_b` by the OCR-derived multiplier. Build the API with Python's `http.server` or Flask: check the `Authorization: Bearer <token>` header first (401 if wrong/missing), look up the id in your processed dict (404 if absent), and return the exact JSON shape requested. Run the server in the background/foreground so it stays reachable.
