Decompose this into two independent sub-problems and solve each in isolation before combining them into the validator. First, extract the SEED: use `ffmpeg` to dump the 10 frames of `/app/sync_signal.mp4` as images (e.g. `ffmpeg -i sync_signal.mp4 -vf fps=1 frame_%02d.png`), then for each frame sample a pixel (or average brightness) to classify it black (bit 0) or white (bit 1), concatenate the 10 bits MSB-first, and convert to a decimal integer — this is your SEED constant to embed in the validator, don't hardcode a guessed value. Second, build a bytecode emulator matching the spec precisely: a bounded stack (max depth 10), instruction pointer starting at line 1, and the six opcodes with their exact fail conditions (PUSH overflow, POP/ADD/SUB underflow, JMP out-of-bounds, and a 100-step execution cap before declaring an infinite loop). For validation, recompute the checksum as (sum of ASCII values of every character in the instruction lines, including each line's trailing `\n`) + SEED, and compare against the `CHECKSUM:` header value — get this exact character-counting right (including or excluding the final line's newline consistently) since it's the most common source of off-by-one bugs. Test against both `/app/corpus/clean/` and `/app/corpus/evil/` samples and iterate until every clean file yields ACCEPT and every evil file yields REJECT before finalizing.
