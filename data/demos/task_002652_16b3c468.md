Split this into a simulate-and-score function, a parallel search driver, and a post-fit analysis step. Write one function that, given (k1,k2,k3), integrates the ODE system (e.g. `scipy.integrate.odeint`) from the first CSV row's values over the CSV's time points, then returns SSE summed across all three species against the observed data — build this function first and sanity-check it on a couple of parameter guesses before parallelizing. For the grid search, generate the 1,000 (k1,k2,k3) combinations with `itertools.product` over the three linspaces and evaluate them with a real worker pool (`multiprocessing.Pool(processes>=4)` or `concurrent.futures.ProcessPoolExecutor`) rather than a plain loop — the task explicitly requires actual parallel workers, not just parallel-looking code. Track the minimum-SSE parameters as results come back. Once you have the best (k1,k2,k3), re-simulate to get the N×3 trajectory matrix and run `numpy.linalg.svd(M, compute_uv=False)` (or full SVD), taking the first (largest) singular value. Round every numeric field to exactly 4 decimals before writing the JSON, matching the schema keys precisely.
