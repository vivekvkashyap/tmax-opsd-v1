Start by OCR'ing the ticket screenshot (`tesseract /app/ticket.png -`) and carefully reading the exact auth token string printed there — don't guess a plausible-looking token, extract it verbatim including any punctuation/dashes. You can optionally poke at the legacy binary (`strings`, `objdump`, or just running it) to sanity-check the protocol, but the task description already gives you the full wire format, so treat that as authoritative. Implement the new daemon in C using a standard TCP server loop (`socket`/`bind`/`listen`/`accept` on 127.0.0.1:8888, one connection at a time or forked/threaded), reading line-delimited input with a buffered read-until-`\n` helper. On the first line, expect `AUTH <token>`, compare against the extracted token exactly, and respond `AUTH_OK\n` or `AUTH_FAIL\n` (closing the socket on failure). For subsequent `CMD <hexlen> <payload>` lines, parse the hex length with `strtol(..., 16)` into a wide-enough integer type (this is exactly where the legacy binary's integer overflow bug lived, so use a type large enough to hold 0xFFFF without wraparound), compare against the 1024 (0x0400) limit before doing anything with the payload, replying `ERR_TOO_LARGE\n` and discarding/ignoring the rest of that line if it's too big, otherwise `CMD_RECV <decimal length>\n`. Test manually with `nc 127.0.0.1 8888` for both the valid and edge-case flows before finishing, and make sure the daemon is left running in the background.
