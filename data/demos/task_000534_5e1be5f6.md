Work in the given order: exploit first, then build tooling, then patch. For step 1, inspect the running service's behavior on port 8080 (check the source or just probe it) to learn its exact URI scheme handling, then craft a `curl` request against `/login` with the `redirect` parameter pointing at the local file (e.g. `file:///home/user/vault/secret.key`) and save the raw leaked body verbatim into evidence.txt — don't add extra whitespace/newlines beyond what's returned. For step 2, write a small C++ program that opens the input file in binary mode, reads a key string from argv, and for each input byte XORs it with `key[i % key.length()]`, writing the raw output bytes (not hex/text) to the output path — get argument parsing and binary I/O modes right, then compile with g++ and run it exactly as specified to produce evidence.enc. For step 3, copy the original server logic but add an explicit allowlist check: only permit `redirect` values whose string exactly starts with `http://internal.company.local/` (use `.rfind(prefix, 0) == 0` or equivalent, not a substring search), and for anything else return the literal forbidden response text including the exact CRLFs and "Invalid redirect" message, then fetch normally for allowed requests. Compile and smoke-test both the exploit path and the patched server manually before finishing.