Treat this as fixing a broken vendored package against a known-good spec, not a rewrite from scratch. First inspect the data: parse the FASTA to get each protein's sequence length (strip header and newlines, count remaining amino-acid characters) and use h5py or `h5dump` to inspect the HDF5 structure at `/experiments/{id}/curve` to get the experimental curve array. Then diagnose `server.py` and `compute.sh` bug-by-bug: check the actual listening port against the required `127.0.0.1:8080` and fix it; check the bearer-token comparison string against the exact required token and fix it; check the FASTA-parsing logic for multi-line sequences (a naive single-line grep will truncate sequences split across multiple FASTA lines — you must concatenate all sequence lines until the next `>` header or EOF); and check the analytical formula against the definition given in the task text (`y_t = L * (1 - e^{-0.05*t})`) since the vendored script likely has a sign or operator error. Compute MSE between the analytical and experimental curves over the N experimental points, round to 2 decimals. Start the server in the background, verify both the 401-without-auth and 200-with-auth cases with curl, and write the running process's PID to the required log file.
