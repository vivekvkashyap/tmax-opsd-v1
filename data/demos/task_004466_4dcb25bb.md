Part 1: run `make` in /app/libinfer_ops-1.2.0 first to see the actual compiler error rather than guessing — a vendored C++ library failing to build under a hardcoded old `-std=` flag while using newer standard-library features (e.g. std::optional, std::variant) is a classic sign the Makefile's CXXFLAGS standard version needs bumping to match what the source actually uses; inspect the source for such features and raise the -std flag accordingly, then rebuild and run ./benchmark_infer, leaving baseline_perf.txt in place untouched. Part 2: write sanitizer.cpp to (1) parse the single-column CSV skipping the header, treating NaN or negative values as invalid, and reject the whole file (exit 1) if invalid rows exceed 20% of total rows; (2) on the remaining valid values, implement bootstrap resampling — 1000 iterations, each drawing len(valid) samples with replacement using a proper random generator (e.g. `<random>` with a uniform int distribution over indices), computing the mean each time, collecting 1000 bootstrap means; (3) sort those means and take the values at the 2.5th and 97.5th percentiles as the CI bounds; (4) reject (exit 1) if the upper bound exceeds 100.0, otherwise exit 0. Compile to /home/user/sanitizer and test it against both plausible clean data and deliberately corrupted/outlier data you construct yourself to sanity-check both rejection paths before relying on it.