Find the PID of `run_sensor.sh` (e.g. `pgrep -f run_sensor.sh` or `ps aux | grep run_sensor`), then list its open file descriptors with `ls -l /proc/<pid>/fd` and look for one pointing at a path like `/home/user/sensor_data.db (deleted)` — a deleted-but-open file is still readable through its `/proc/<pid>/fd/<N>` symlink even though it no longer has a directory entry. Recover it by copying that fd path out, e.g. `cp /proc/<pid>/fd/<N> /home/user/recovered.db` (or `cat /proc/<pid>/fd/<N> > /home/user/recovered.db`), which duplicates the underlying inode's bytes into a new, normally-linked file — do not kill or restart the process, since that would release the fd and lose the data permanently. Then query it: `sqlite3 /home/user/recovered.db "SELECT value FROM measurements;"`, sum the values, and be careful about floating-point drift — sum in a way that avoids extra precision noise (e.g. Python's `sum()` then `round(total, 2)`, or `printf '%.2f'`), and write only the two-decimal result string to `/home/user/result.txt` with no extra whitespace or newline artifacts beyond what's expected.
