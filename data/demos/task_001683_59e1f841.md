Work through the incident in dependency order since later steps need info from earlier ones. First, extract the hardcoded malicious URL from the ELF binary: run `strings /home/user/incident/login.cgi | grep -i http` (or `evil` if strings alone is noisy) to pull out the `http://<domain>/steal?token=` string and note the domain. Second, open `authorized_keys` and remove exactly the line whose trailing comment contains that domain, writing the remaining lines (in original order) to `authorized_keys.safe` — use `grep -v` on the domain string rather than manual editing to avoid accidentally dropping/reordering other keys. Third, write `safe_redirect.c` with strict prefix matching: use `strncmp(argv[1], "https://safe.example.com/", strlen("https://safe.example.com/")) == 0` (not a substring check) to decide between echoing the given URL or falling back to the `/error` URL, and cover the zero-argument case explicitly before touching `argv[1]`; compile with `gcc safe_redirect.c -o safe_redirect`. Finally, write the CSP line into `csp.txt` exactly as specified, byte-for-byte (no trailing whitespace differences), since verification does an exact string match. Test `safe_redirect` manually against a good URL, a bad URL, and no-args before considering it done.
