Diagnose the mismatch before touching code: read config.env and confirm the client's target IP variable doesn't match the server's bind IP even though both intend to use the same loopback address and port — edit config.env so CLIENT_TARGET_IP equals SERVER_BIND_IP (127.0.0.2) and both ports are 8888, without changing variable names the C++ source expects. Compile server.cpp and client.cpp with g++ into `server` and `client` binaries in place, then launch `./server &` so it's a persistent background process and confirm via the log or a quick `./client` run that connectivity now works end-to-end. For monitor.sh: check liveness with `pgrep -f server` (or similar) and exit 1 immediately if absent; if present, rotate logs by copying (not moving, so any concurrent writer isn't disrupted mid-write) server.log's current contents into archive/server_rotated.log and then truncating server.log in place (e.g. `: > server.log` or `truncate -s0`) rather than deleting/recreating the file, since the server process holds an open file handle; finally invoke ./client and append its stdout to health.log with `>>`. Make monitor.sh executable, run it once, and leave the server process running afterward.