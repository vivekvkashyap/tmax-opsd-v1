Work top-down: fix the build before writing any wrapper code, since nothing else can be tested until libseccomp compiles. Grep the `configure` script (and Makefile) for a suspicious hardcoded `CFLAGS` — look for an invalid architecture/optimization flag injected among otherwise normal flags — remove just that flag, then `./configure --prefix=/app/libs && make && make install` and confirm the library lands there. Only then write `secure_runner.cpp`: parse the ELF section headers to locate `.secpolicy` by name (via the section header string table), extract its bytes, base64-decode, and XOR every byte with `0x5A` to recover the comma-separated syscall list — exit code 255 if the section is missing or decoding fails, before touching seccomp. Wire up libseccomp with `seccomp_init(SCMP_ACT_KILL)`, resolve each syscall name (`seccomp_syscall_resolve_name`) and add an ALLOW rule per name, then `seccomp_load()` before `execve`, passing argv exactly as specified with a NULL terminator and a cleared environment. Test with a hand-built ELF containing a real `.secpolicy` section before trusting correctness.
