Build `/home/user/filter.py` to check two independent rejection conditions on the file content: (1) find every `!INCLUDE[<path>]` occurrence (regex) and reject if any path starts with `/` or contains `..` as a path segment, while allowing relative safe paths; (2) detect a leading frontmatter block delimited by `---` lines at the very top and, if present, try `json.loads()` on its contents, rejecting on a parse failure — a file with no frontmatter at all is not automatically evil. Exit 0 for clean, 1 for evil, and validate against both 50-file corpora before wiring anything up, since 100% pass/reject is required. Then edit `/app/watcher.py` to call `subprocess.run(['/home/user/filter.py', filepath])` right before its existing `r.lpush('doc_queue', filepath)` line: on nonzero return code, append the filename to `/home/user/quarantine.log` and skip the push; on zero, push as before. Don't touch `builder.py` or nginx — verify the change by dropping a safe and an evil markdown file into `/home/user/docs_src` and checking the resulting HTTP responses/quarantine log yourself before finishing.
