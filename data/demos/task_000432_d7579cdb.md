Work the three legs independently and validate each before wiring them together. (1) Video decoding: each frame of telemetry.mp4 is a solid grayscale color whose intensity equals an ASCII code — extract frames with `ffmpeg -i telemetry.mp4 frame_%04d.png` (use a lossless/rawvideo-safe extraction to avoid compression artifacts shifting pixel values), then for each frame sample a pixel's grayscale value (or average a region to reduce noise, then round) and map it via `chr()` to reconstruct the schema text in order, writing it to `/home/user/artifact.proto`; sanity-check the result is a syntactically valid `.proto` file before trusting it. (2) C parser: in parser.c, fix the missing null terminator (`malloc(payload_len + 1)` and set the last byte to `\0`), add bounds checks so it never reads/writes past `len`, and ensure `parse_header`'s error paths don't leak the payload allocation — keep the struct and function signature untouched, then compile with `-fPIC -shared` into `/home/user/libparser.so`. (3) Python glue: compile artifact.proto with `protoc --python_out=...`, load libparser.so via `ctypes.CDLL`, read all of stdin as bytes, call `parse_header`, and on success populate and serialize the generated protobuf message to stdout; on any C-side error code, exit 1 with empty stdout. Since correctness is fuzzed bit-exact against an oracle, test manually on edge cases like empty input and very short buffers (<5 bytes) to confirm your error-code handling matches the documented boundary before considering it complete.