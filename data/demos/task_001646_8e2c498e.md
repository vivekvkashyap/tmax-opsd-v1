Install the NetCDF C headers/library first (e.g. `apt-get install -y libnetcdf-dev`) so `netcdf.h` and `-lnetcdf` are available to `gcc`. In `prepare_data.c`, use `nc_open`, `nc_inq_varid`, and `nc_get_var_double` to read the 100-element `coarse_data` array from `/home/user/input.nc`. Build the length-199 `refined_data` array exactly as specified: even indices copy the coarse value directly, odd indices average adjacent coarse pairs — watch the index bounds (`i` and `i+1`) so you don't read past the array. Sum all 199 refined values, divide each by that sum to get `P`, then compute the KL divergence against the uniform `Q = 1/199` using `log()` from `<math.h>` (link with `-lm`), accumulating `P[i] * log(P[i]/Q)` over all indices. Write the result to `/home/user/result.log` using `%.6f` formatting exactly as `KL: <value>`. For output, use `nc_create`, `nc_def_dim` for `n_refined`, `nc_def_var` for both `refined_data` and `P` dependent on that dimension, `nc_enddef`, then `nc_put_var_double` for each, checking every NetCDF call's return status against `NC_NOERR` to catch mistakes early.