This is two separate correctness problems stitched together: a robust quoted-CSV parser and a rule extracted from audio — solve them independently, then combine. Transcribe `/app/audio/template_dictation.wav` with an offline tool (e.g. whisper) and listen carefully to what bracket style is dictated as valid; use that dictated rule to build your validation regex — do not assume a bracket style without confirming it from the transcript. For the CSV parser in C, do not split on commas/newlines naively: implement a proper state machine that tracks whether you're inside a double-quoted field (toggling on `"`, handling `""` as an escaped quote) so that literal newlines inside quotes are consumed as part of the field rather than treated as row terminators; treat unterminated quotes at EOF as a structural error. After parsing each row's `translation_text`, scan for template-variable-looking spans and validate against the correct bracket style; any row using the wrong bracket style, or any malformed quoting, must cause immediate rejection (exit 1). Compile with warnings enabled and test against your own crafted multi-line-quoted and malformed CSV files before trusting it against the hidden corpus.
