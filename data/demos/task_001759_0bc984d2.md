Handle the four subtasks in order and verify each output file individually before moving on. First, open `/home/user/workspace/api-client/pyproject.toml` and look carefully at the `test` optional-dependencies list for a typo in a package name (compare each entry against the real PyPI package names `pytest` and `hypothesis` character by character) — fix any misspelling so the install succeeds. Install the package with its test extras (e.g. `pip install -e '.[test]'`) and run `pytest test_client.py`, redirecting combined output to `/home/user/workspace/test_results.log`, then grep the log for "passed" to confirm success rather than assuming it worked. Second, for the unique-lines task, don't hand-roll a diff — sort both files and use `comm -13 <(sort data1.txt) <(sort data2.txt)` to get lines only in data2, writing the result to `/home/user/workspace/unique_data.txt`. Third, compute the checksum with `sha256sum unique_data.txt | awk '{print $1}' > checksum.txt` so only the raw hash (no filename, no trailing text) lands in the file — cat the file afterward to confirm there's no stray whitespace or newline issues.