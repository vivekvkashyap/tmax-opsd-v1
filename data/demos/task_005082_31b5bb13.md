Structure the script around `argparse` for the four flags, then load the CSV into an in-memory SQLite db with `sqlite3.connect(':memory:')` — read the CSV with `csv.DictReader`, `CREATE TABLE transactions (id INTEGER, timestamp TEXT, category TEXT, price REAL, user_id INTEGER)`, bulk-insert with `executemany` and parameterized `?` placeholders, then `CREATE INDEX idx_category_price ON transactions(category, price)`. Build the filter/sort/paginate query as a single parameterized statement: `WHERE category = ? AND price >= ?` (never string-format user input into SQL), `ORDER BY price DESC, id ASC`, and `LIMIT ? OFFSET ?` where offset is computed as `(page - 1) * page_size` — this offset formula is the easy place to get off-by-one wrong, so double check page 1 means offset 0. Fetch results and write them to `/home/user/output.csv` with `csv.writer`, writing the header row first then each data row in the exact column order `id,timestamp,category,price,user_id`. Run the script with the exact CLI invocation given in the task to produce the final file, and sanity-check row count/order manually against the source CSV before finishing.
