Build this as a sequence of discrete, testable stages rather than one big pipe. First, deduplicate by composite key `IP|ENDPOINT|PAYLOAD_JSON`, keeping only the chronologically-first occurrence of each key (process lines in file order and track seen keys, e.g. with `awk` using an associative array keyed on fields 2,3,6, printing only on first sight) — a naive `sort -u` would not preserve first-occurrence-by-time and could pick the wrong duplicate. Then filter out rows where RESPONSE_TIME_MS < 0. Next compute a per-ENDPOINT rolling average over the last 3 requests (fewer if not yet available), floored to an integer — this requires grouping by endpoint while preserving overall chronological order, e.g. iterate through the cleaned rows in a scripting language (awk/python) maintaining a small deque per endpoint. Be careful to floor (not round) the average. Write the 7-column CSV to `/home/user/processed_logs.csv`, keeping the embedded JSON payload's internal commas intact (only split on the delimiter fields, not blindly on every comma). Finally create `/home/user/logs.db` with the exact `metrics` schema and bulk-import the CSV via `sqlite3 ... ".mode csv" ".import"`, watching out for sqlite3's CSV import auto-detecting/skipping a header row when there isn't one.
