First OCR/read `/app/architecture.png` (`tesseract /app/architecture.png -`) to get the exact proxy port and two upstream ports — use those literal values throughout nginx.conf and your backend startup commands, don't invent ports. Fix nginx.conf to run unprivileged: set `pid`, `client_body_temp_path`, and other temp dirs under `/home/user/nginx_temp/` (create that dir), listen on the diagram's proxy port, and define an `upstream` block pointing at `127.0.0.1:<port1>` and `127.0.0.1:<port2>` from the diagram, proxying `/` to it. Fix backend.c: replace the hardcoded port with `atoi(argv[1])`, bump `listen(fd, 1)` to `listen(fd, 1024)`, and eliminate the sequential-with-sleep bottleneck by handling each accepted connection concurrently (spawn a pthread or fork per connection, or use a small non-blocking epoll loop) so many requests can be served per second — remove any `sleep()` call entirely. Have it write the exact literal HTTP response bytes given in the task. In deploy.sh, compile with `gcc -O2 -pthread backend.c -o backend`, create the temp dir, launch two `./backend <port>` instances backgrounded with `&`, then `nginx -c /home/user/nginx.conf`. After running deploy.sh, smoke-test with `curl` against the proxy port and a quick `wrk -c50 -d3s` before declaring done, since the grading threshold is throughput-based (>2000 req/s, zero non-2xx/3xx).