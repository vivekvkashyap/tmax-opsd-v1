Debug the existing `/home/user/pipeline.py` by fixing each bug in place rather than rewriting from scratch, so the random-seed-dependent bootstrap logic stays intact. (1) Matplotlib backend: change `matplotlib.use('TkAgg')` to a non-interactive backend like `'Agg'` before `import matplotlib.pyplot as plt` — this is required in a headless container. (2) Missing data: after loading the CSV, drop NaNs from the 'value' column (e.g. `df['value'].dropna().values` or `feature[~np.isnan(feature)]`) before it feeds the bootstrap sampler, since NaNs propagate through `np.mean`/`np.percentile` otherwise. (3) Normalization bug: the prediction line uses `mean_val` directly, but the model expects Z-score standardized input — compute the mean and std of the *cleaned* value column, standardize `mean_val` as `(mean_val - clean_mean) / clean_std`, then apply `3.2 * scaled + 1.5`. Keep `np.random.seed(42)` and `n_iterations=1000` exactly as given, and keep the seed placement matching original semantics (only affects bootstrap resampling, not the NaN removal). Run the script, confirm `/home/user/output_plot.png` is a valid PNG (e.g. via `file`), and confirm `/home/user/metrics.txt` matches the required 3-line format with 2-decimal rounding.