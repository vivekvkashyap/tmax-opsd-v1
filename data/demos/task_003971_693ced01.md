Handle the four pieces independently, verifying each before moving on. For the Go build, run `go build` in `/app/go-httpbin` first to see the actual compiler error (don't assume where it is), fix the specific syntax/identifier issue it reports, then rebuild to `httpbin` and confirm `./httpbin -h` exits 0. For the filter translation, read `legacy_filter.rb` closely to extract its exact validation rules (role→bitmask mapping, forbidden patterns, type checks) — implement the same logic in `gateway_filter.sh`, loading roles/permissions once into Bash associative arrays (`declare -A`) at script start so each request is checked via array lookups instead of spawning `jq`/subprocesses per rule (only use `jq` once, to parse the incoming JSON). Print exactly `ACCEPT`/`REJECT` and exit 0/1 respectively. Validate against both `/app/corpora/clean/` and `/app/corpora/evil/` in a loop, diffing your script's verdict against the expected clean/evil split until 100% match — don't stop at spot-checking a few files. For the benchmark, wrap 500 sequential invocations of your script (feeding the same clean payload) in `time { ... } 2>&1`, and append the real elapsed value to `benchmark_results.txt`.