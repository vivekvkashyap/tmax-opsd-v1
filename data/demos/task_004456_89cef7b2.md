Start by inspecting `/app/ts_filler-1.2/Makefile` for the `CFLAGS` line — look for a preprocessor macro that gates the fill strategy (e.g. `-DINTERPOLATE_MODE=0` vs `1`) and check the corresponding `#if`/`#else` branches in the C source to confirm which value enables linear interpolation instead of zero-filling; flip the macro and rebuild with `make` (clean first if objects are cached). Build `etl.sh` as a pipeline: convert encoding with `iconv -f ISO-8859-1 -t UTF-8`, strip fully empty rows (e.g. `awk 'NF'` or check for all-empty CSV fields), pipe the cleaned UTF-8 CSV into the rebuilt `ts_filler` binary, then before writing the final file scan the `value` column and `exit 1` if any parsed value is negative (e.g. with `awk` checking `$N < 0 {exit 1}` on the relevant column, applied as a checkpoint before the final `> /home/user/final.csv` write, not after). Make the script executable. Validate your result yourself against expectations for linear interpolation on a few known gaps before trusting the MSE threshold, since a wrong macro value or an unfixed encoding step will silently degrade accuracy rather than crash.