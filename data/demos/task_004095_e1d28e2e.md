Write the C++ parser to handle each `.conf` line robustly: strip anything from the first unescaped `#` onward (covering both full-line comments and inline trailing comments), skip blank/whitespace-only lines, split on the first `=`, and trim leading/trailing whitespace from both key and value with a helper function — a naive attempt often forgets inline comments after values or leaves trailing whitespace in the value, which hurts compressibility and correctness. Serialize to JSON manually or with a small helper, sorting keys alphabetically (e.g., load into a `std::map<std::string,std::string>` which is already ordered) before emitting. For atomic writes, write to `<filename>.json.tmp` in the same directory then call `rename()` (or `std::filesystem::rename`) to the final `.json` path — never write directly to the final path. After each JSON file lands, shell out (`system()`/`fork+exec`) to `/app/json_compressor <input.json> <output.jc>` and confirm the `.jc` file was produced. Drive this over every file in `/home/user/configs/`, then sanity-check the resulting global ratio yourself (sum original .conf sizes / sum .jc sizes) before assuming you're done — noise removal and key sorting are what push the ratio past 2.8, so verify comments are truly gone and keys are truly sorted.