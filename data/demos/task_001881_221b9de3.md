Build the pipeline in `/home/user/pipeline.py` exactly in the order specified, since randomness and joins are order-sensitive:
1. Load the three CSVs, left-join `users` <- `transactions` <- `labels` on `user_id`.
2. Fix dtype corruption from the join: `fillna(-1)` on `store_id` then `.astype('int64')` explicitly (pandas silently upcasts int columns with NaNs to float — this is the main trap), and `fillna(0.0)` on `amount`.
3. Build `X = df[['age','store_id','amount']]`, `y = df['churned']`, split with `train_test_split(..., test_size=0.2, random_state=42)`, fit `RandomForestClassifier(random_state=42)`, and score on the test set to get `base_accuracy`.
4. For the bootstrap CI: set `numpy.random.seed(42)` once before the loop, then for 1000 iterations resample test-set indices with replacement (`np.random.choice`) at the test set's size, compute accuracy of already-computed predictions on each resample (don't refit the model), collect all 1000 accuracies, and take `numpy.percentile` at 2.5 and 97.5 for the CI bounds. Order of operations (seeding once, then looping) matters for reproducibility.
5. Write the four required keys to `/home/user/pipeline_results.json` and run the script to actually produce the file — don't hand-write the JSON.
