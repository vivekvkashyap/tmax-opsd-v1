Write the Go program to first parse the FASTA file into a map of sequence ID -> sequence string (identify the `Reference` record and treat every other record as a target). For each sequence, count all overlapping 3-mers (windows of length 3, sliding by 1, over all `len(seq)-2` positions) restricted to the 64 combinations of A/C/G/T, then apply Laplace smoothing exactly as specified: `(count+1)/(N+64)` where `N = len(seq)-2` — compute N per-sequence, not a shared constant, since target sequences may have different lengths than the reference. Compute KL divergence per target as `sum(P(i) * log2(P(i)/Q(i)))` over all 64 k-mers, with P = target's smoothed distribution and Q = reference's smoothed distribution (order matters, don't swap P and Q). Parallelize the per-target computation with goroutines plus a `sync.WaitGroup` (and a mutex or per-goroutine result slot to avoid races when collecting results), but sort the final rows alphabetically by SequenceID before writing — goroutine completion order is nondeterministic, so don't rely on it for output ordering. Write the CSV with header `SequenceID,KLDivergence` and values formatted to exactly 4 decimal places (`%.4f`) to `/home/user/divergence_results.csv`, then `go build`/`go run` to confirm it compiles and produces output before finishing.