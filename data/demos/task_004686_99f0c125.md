Structure this as: streaming gzip decompression -> stateful line parser -> locked append, each verified in isolation before combining. Use zlib's `gzFile`/`gzgets` (or `gzread` with a manual buffer) to read `/home/user/logs/archive*.log.gz` directly without shelling out to `gunzip` or writing a temp file. Parse line-by-line tracking a small state machine: on `===LOG===` start a new record, on `Time:`/`Status:`/`Details:` capture the field after the prefix (strip the leading label and space), on `===END===` finalize the record and, only if `Status` equals exactly `CRITICAL`, format it as `Time,Status,Details` (no extra quoting) ready to append. For the shared output file, `open(..., O_WRONLY|O_CREAT|O_APPEND)`, then `flock(fd, LOCK_EX)` immediately before writing and unlock right after — do this per-invocation, not held across both file runs, since the two archives are processed as separate program executions. Compile with `g++ log_processor.cpp -lz -o log_processor`, then actually run it against both `archive1.log.gz` and `archive2.log.gz` and inspect `/home/user/critical_events.csv` to confirm only CRITICAL lines appear and non-critical details never leak in.
