Split this into a discovery step and a classification step, and don't hardcode anything you haven't actually recovered. First, install `pytesseract`/`Pillow` and run OCR on `/app/system_def.png` to recover the exact equations F1(x,y), F2(x,y) and the tolerance threshold as printed in the image — inspect the raw OCR output text carefully (print it) rather than assuming a value, since OCR can misread characters; correct any obvious OCR artifacts (e.g. confusing `**` for `*`, stray whitespace) before hardcoding the parsed equations/tolerance as constants in your script. Then write `check_convergence.py` to open the HDF5 file with `h5py`, read the last elements of `x` and `y` (`ds[-1]`), and evaluate both equations at that point; wrap the evaluation so any NaN/Inf/overflow is caught (e.g. `np.isfinite` checks or try/except) and treated as a reject (exit 1) rather than crashing. Accept (exit 0) only if both `abs(F1)` and `abs(F2)` are within the OCR'd tolerance. Validate your script by running it against every file in `/app/corpora/clean/` (all must exit 0) and `/app/corpora/evil/` (all must exit 1) before considering the task done, since the hidden test set has the same distribution.