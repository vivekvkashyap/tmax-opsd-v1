Transcribe the audio first, then build the SQL logic around what it says — don't guess a filter threshold. Run `whisper.cpp`'s `main` binary against `/app/analyst_note.wav` with the tiny.en model (or convert with `ffmpeg` to 16kHz mono WAV first if needed) and read the transcript text to extract the exact numeric threshold and comparison direction (e.g. "ignore transactions below $X" means keep `amount >= X`). Then write `generate_report.sh` so it: (1) queries sqlite3 with a LEFT JOIN from customers to transactions filtered by that rule, using `IFNULL(SUM(...),0)` and `IFNULL(AVG(...),0)` grouped by customer so a customer with zero qualifying rows still returns a row; (2) checks whether the customer exists at all (empty result -> exit 1, no output); (3) checks whether the filtered transaction count is zero (-> "No valid transactions" line) vs nonzero (-> printf with `%.2f` for both sum and avg, matching the exact required string format); (4) chmod +x the script. Test with several IDs, including ones with all-filtered transactions and nonexistent IDs, before trusting it.
