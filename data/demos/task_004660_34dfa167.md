First transcribe `/app/specs.wav` (e.g. via a local speech-to-text tool, or by playing/inspecting it if a transcription utility is available) to recover the exact port, endpoint path, and bearer token spoken in the memo — do not assume standard defaults, use exactly what the audio says, taking care to normalize any spoken digits/words (e.g. numbers or letters spelled out) into the literal string form the server and client must actually use. Scaffold the Rust project with `cargo new audit_service` under `/home/user`, add axum/tokio/rusqlite/serde deps, and implement a single GET handler at the recovered path bound to `127.0.0.1:<recovered-port>`. In the handler, check the `Authorization` header equals `Bearer <recovered-token>` exactly, returning 401 if missing/wrong before touching the DB. For the query, join `users` and `logins` on `user_id`, extract `department` from the `metadata` JSON column with SQLite's `json_extract(metadata, '$.department')` filtered to 'Engineering', count FAILED logins per user with GROUP BY/HAVING COUNT(*) > 3, and ORDER BY failed_attempts DESC, user_id ASC — do this in SQL for efficiency rather than pulling everything into Rust. Build with `cargo build --release`, then launch the binary in the background (e.g. with `nohup ... &`) so it stays running for the automated HTTP checks.