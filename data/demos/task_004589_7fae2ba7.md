Build this as three independent, testable pieces, then wire them together. For extraction, use `ffmpeg -af "astats=metadata=1:reset=1"` (or per-second `atrim`/`volumedetect` on 1-second slices via a loop) to get mean volume per 1-second window for the first 10 seconds; explicitly fill any missing/short window with the `-90.0` dB silence floor so you always emit exactly 10 rows regardless of actual audio length. For idempotency, don't append blindly — write to a temp file and atomically replace `/home/user/metrics.csv` (or dedupe by keying on timestamp with an associative array/`awk` and overwriting) so running the script twice still yields exactly 10 lines, not 20. Normalize dB with a straightforward linear map: `norm = (db - (-90)) / (0 - (-90)) * 100`, clamped to reasonable bounds. Compute the average and the Euclidean distance against the given 10-element reference array (sqrt of sum of squared differences) and emit valid JSON with `jq -n` or manual string construction to `summary.json`. For the server, use `nc -lk` in a loop or `socat TCP-LISTEN:8080,fork,reuseaddr EXEC:...` that reads the request line, checks it targets `GET /api/v1/metrics`, and responds with a correct `HTTP/1.1 200 OK` status line, a `Content-Type: application/json` header, blank line, then the raw file contents — get the header/blank-line/CRLF framing exactly right since naive `nc` scripts often omit the blank line separator. Make both scripts executable and background the server before finishing.
