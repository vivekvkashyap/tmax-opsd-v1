First isolate and fix the vendored package before touching your own script: import networkx from `/app/networkx` (e.g. `PYTHONPATH=/app/networkx python3 -c "import networkx"`) to reproduce the SyntaxError, then open `/app/networkx/networkx/classes/digraph.py` near line 20 and look for a malformed statement (e.g. a misspelled keyword/typo in an import) — fix only that minimal typo, don't rewrite the file. Re-verify the import works and that `nx.DiGraph()`, `nx.ancestors()`, `nx.descendants()` all function before writing any analyzer code. For the analyzer itself, build a `DiGraph` where each `depends_on` entry X for job Y adds edge X->Y (X runs before Y). For each node N, compute descendants = `nx.descendants(G, N) | {N}` and ancestors = `nx.ancestors(G, N) | {N}`; score = (sum of duration_minutes over descendants) * (count of ancestors, including N itself). Sort output rows lexicographically by job_id and write a plain CSV with header `job_id,criticality_score`. Ensure `/app/networkx` is prepended to `sys.path` (or exported via `PYTHONPATH`) at the top of your script so it always uses the fixed vendored copy rather than any system-installed networkx. Test against small hand-built DAGs (including diamond-shaped dependency graphs) to confirm the ancestor/descendant counting logic before trusting it on larger inputs.
