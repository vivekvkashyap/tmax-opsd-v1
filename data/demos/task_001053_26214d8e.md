Start the upstream stub APIs first (`bash /app/services/start.sh`) and confirm both `/users` and `/transactions` respond before writing your service, so you know the real shapes you're joining. Set up a Python env with FastAPI/Flask, requests/httpx, scikit-learn, pandas/numpy. In `model_service.py`: on startup (not per-request), fetch both endpoints, inner-join on `user_id`, then sort the joined rows by `user_id` ascending and take the first 80% as train / remaining 20% as test by row position (no shuffling) — this exact ordering is required for reproducibility, so don't use `train_test_split`'s default shuffle. Fit a `StandardScaler` only on the training features, then `transform` (not `fit_transform`) both train and test to avoid leakage. Fit `GaussianNB` on scaled train features vs `target`, then compute test accuracy and cache both the accuracy and the fitted scaler+model objects in memory for reuse by the endpoints. Implement `GET /metrics` to return `{"test_accuracy": round(acc, 4)}` and `POST /predict` to scale the incoming `feature_1`/`feature_2` pair with the *already-fitted* scaler and return `{"probability_class_1": round(model.predict_proba(...)[0][1], 4)}`. Bind uvicorn/flask to `0.0.0.0:8080` and run it in the background (e.g. `nohup ... &`) so it stays alive after your turn ends; verify with a local `curl` before finishing.