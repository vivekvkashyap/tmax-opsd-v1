Read `/home/user/artifact_analyzer.c` line by line and fix each bug independently before recompiling. Bug 1 (schema mismatch): the file was serialized as 64-bit `double` but the code allocates/reads a `float*` buffer with `sizeof(float)` — change the buffer type to `double*` and `fread`/`malloc` sizing to `sizeof(double)` throughout. Bug 2 (variance math): the accumulation loop does `sq_sum += (data[i] - mean)` without squaring the deviation — it must be `sq_sum += (data[i] - mean) * (data[i] - mean)` (or `pow(...,2)`) before dividing by n and taking the square root for population stddev. Leave the 95% CI margin formula (`1.96 * stddev / sqrt(n)`) as-is since it's already correct once stddev is fixed. Recompile with `gcc -o /home/user/artifact_analyzer /home/user/artifact_analyzer.c -lm` (link math explicitly), run it, and check the output JSON at `/home/user/artifact_metrics.json` — the mean should be a small number near a plausible weight-scale value (not a garbage float from misreading the byte layout), confirming the double/float fix actually took effect.