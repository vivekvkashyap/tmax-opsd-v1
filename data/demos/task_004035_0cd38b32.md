Work through the steps in order and verify each before moving on. First, `mkdir -p include src` and `git mv`/`mv` the header and .cpp files into place, then fix all `#include` paths that reference the old flat layout. Apply the patch with `patch -p0 < serialization.patch` (check the patch's path prefixes match your file locations—since you moved files into src/, you may need to apply the patch before moving files, or adjust with `-p1`/manual path edits; verify with `patch --dry-run` first). For CMakeLists.txt, the classic broken pattern is a library/executable declared without `target_include_directories` and without `target_link_libraries`—fix it by adding `target_include_directories(vm_engine PUBLIC include)` (or similar), `target_link_libraries(vm-cli PRIVATE vm_engine)`, setting `RUNTIME_OUTPUT_DIRECTORY`/`LIBRARY_OUTPUT_DIRECTORY` to the required build/bin and build/lib paths, and setting `INSTALL_RPATH`/`BUILD_RPATH` (e.g. `set_target_properties(vm-cli PROPERTIES BUILD_RPATH "$ORIGIN/../lib")`) so it resolves the .so without LD_LIBRARY_PATH. Build with `cmake -S . -B build && cmake --build build`. Then run vm-cli on prog1.txt then prog2.txt in sequence, and check result.log — if the number is wrong, suspect an unfinished serialization implementation or a stack-ordering bug in save/load rather than the CMake config.
