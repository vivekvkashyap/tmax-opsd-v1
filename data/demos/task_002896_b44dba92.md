Treat this as a merge-join problem, not a nested-loop search: since both files are already chronologically sorted per your assumption, walk trades.csv in order while maintaining, per symbol, a pointer/cache of the most recent quote seen so far for that symbol as you scan quotes.csv forward — advance the quotes pointer only as far as needed (quote.timestamp <= trade.timestamp) before processing each trade, rather than rescanning quotes.csv from the start for every trade (that's the naive O(n*m) mistake to avoid, though at this scale correctness matters more than speed). Parse ISO8601 timestamps into a comparable integer (epoch seconds via manual parsing of `YYYY-MM-DDTHH:MM:SSZ`, or `strptime`) so comparisons are exact. Skip trades with no valid preceding quote for their symbol entirely — don't count them in the VWAS sum. Accumulate `sum(spread*volume)` and `sum(volume)` per symbol in a hash map or sorted array keyed by symbol string, then emit rows sorted alphabetically by symbol with `vwas` printed via `%.4f`. Compile with `gcc -O2 -o /home/user/processor /home/user/processor.c` and run it before checking your output against the expected header format.