Start by transcribing `/app/audio/architect_memo.wav` (e.g. `whisper /app/audio/architect_memo.wav --model base` or convert with `ffmpeg` first if the format needs normalizing) — the exact banned-operator and indexed-field rules for the sanitizer, plus the window-function grouping key, live only in that audio, so don't assume defaults without listening/reading the transcript. Build `/home/user/sanitizer.py` to parse the JSON pipeline array and walk every stage dict recursively (rules may live inside `$match`/`$lookup` nested keys), printing ACCEPT/exit 0 or REJECT/exit 1 per the transcribed criteria; iterate against `/app/corpus/clean/*` and `/app/corpus/evil/*` until every file gets the right verdict, since the corpus is your ground truth for edge cases the memo didn't spell out explicitly. For the SQL, read `schema.sql` to find the real join key, rewrite `bad_query.sql`'s implicit cross join as an explicit `JOIN ... ON`, and wrap it in a CTE using `ROW_NUMBER() OVER (PARTITION BY <entity> ORDER BY <date> DESC)` filtered to `rn = 1`, per the memo's "latest per entity" requirement. For `graph_path.py`, load `referrals.json` as an adjacency list and run BFS (not DFS) from source to target to guarantee shortest path, printing "NONE" if unreachable.
