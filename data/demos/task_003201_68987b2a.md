This is a small ETL job — a quick Python or awk script beats hand-chaining `join`/`sort` for correctness. Read both TSVs, build a dict from `trans_fr.tsv` keyed by `StringID` with `TranslatedText.strip()` applied (watch for non-ASCII whitespace or the specific spacing used in the file — strip both leading and trailing), then iterate `base_en.tsv` rows sorted by `StringID` (lexicographic string sort, not numeric) doing the join, uppercasing `Category`. For the rolling average, maintain a sliding window of the last up-to-3 normalized-text lengths (character count of the *normalized/stripped* French text, mind multi-byte/accented characters — use Python string `len()` on the decoded str, not byte length) and average whatever is currently in the window (1, 2, or 3 elements), formatting to exactly 2 decimals with e.g. `f"{avg:.2f}"`. Write the 4 tab-separated columns with no header, and diff your output against the expected 6-row structure yourself before considering it done — an off-by-one in the window (using the wrong number of prior rows) is the most common bug here.
