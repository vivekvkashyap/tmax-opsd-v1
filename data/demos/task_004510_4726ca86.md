Build this as a single ordered pipeline, not ad hoc exploration. First set up a venv and install pandas, pyarrow, scikit-learn. Load `/home/user/train.parquet`, fit `LogisticRegression(random_state=42)` on features f1/f2/f3 against `label`, leaving all other hyperparameters at default. Then load `/home/user/incoming.parquet` and apply schema validation as boolean masks on the raw columns (f1 > 0, f2 is numeric/float, f3 < 100) — filter with `df[mask]`, don't try/except row by row, and keep the `id` column through every step. Run `predict` and `predict_proba` on the surviving rows to get `predicted_label` and `probability` (positive-class column of predict_proba), then add a defensive filter dropping any row where probability isn't within [0,1] even though logistic regression can't produce that. Before saving, select exactly the columns `id, f1, f2, f3, predicted_label, probability` in that order (extra or missing columns will fail validation) and write with `to_parquet` to `/home/user/processed_data.parquet`. Double check row count arithmetic makes sense given how many rows you dropped for schema violations.
