Work through the three bugs independently and verify each with `cargo test`/`cargo run` before moving on. For the blank-plot bug, find where `plotters` builds the drawing area/chart and ensure the root drawing area's `.present()` (or backend's finalize/flush) is actually called at the end — a missing `.present()` call is the classic cause of a truncated/corrupt PNG; check the code path for any early return or missing final call. For the MSE issue, don't just bump `n_components` to the max — treat it as a search: try n_components = 2, 3, etc., rerunning the pipeline/test each time, and pick the smallest value that reliably brings MSE under the 2.0 threshold (more components isn't free — the task rewards finding the minimal sufficient number, and grep-based verification will check the literal value you set). For metrics output, after computing MSE in `main`, serialize it with `serde_json` (or hand-build the string) to exactly `{"mse": <value>}` and write it to `/home/user/rust-etl/metrics.json`. After all three fixes, run `cargo build`, `cargo test`, and `cargo run` in sequence, and manually inspect `plot.png`'s file size/PNG magic bytes and the JSON content to confirm both artifacts are valid before finishing.
