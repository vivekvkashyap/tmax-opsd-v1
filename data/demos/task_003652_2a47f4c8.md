Two independent subtasks. For extraction: use `ffmpeg -i /app/dashboard.mp4 -vf fps=1 frame_%03d.png` to split the video at 1fps, then run OCR (e.g. `tesseract`) on each frame in numeric order, cleaning up whitespace/noise, and append each recognized line to `/home/user/historical_events.txt` in chronological order. For the C++ engine: represent the wait-for graph as an adjacency structure keyed by transaction id; on every `W a b` command, add the edge then run a DFS/coloring cycle check starting from the affected nodes (not the whole graph) to detect a cycle efficiently; if found, collect cycle members, sort ascending, and print them. Apply victim selection in strict order: (1) any USER type beats BACKUP, (2) among ties, highest current out-degree computed over the *entire* graph (not just the cycle), (3) highest id. After choosing, delete the victim node and all its incident edges before continuing. Write plenty of your own hand-crafted test cases (not just the extracted log) covering ties at each tie-break level, since the grader checks bit-exact behavior against many edge cases, not just this one trace.
