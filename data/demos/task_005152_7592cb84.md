This is a fuzz-equivalence task: your script's stdout must match a hidden oracle byte-for-byte across many randomized inputs, so implement each rule literally and test edge cases yourself before assuming it's done. First fix `/app/vendored/python-dateutil/setup.py` — inspect its top import line, it's been swapped to reference a module that doesn't exist; restore the standard `from setuptools import setup` pattern, then `pip install --user -e .` from that directory. In your processor: read CSV via `csv.DictReader` on stdin, parse `raw_time` with `dateutil.parser.parse`, then zero out seconds and microseconds (not floor via timedelta math, which can behave oddly near boundaries) and format back to `%Y-%m-%dT%H:%M:%SZ`. Iterate sensor columns in `sorted()` order. Use a regex to pull the float and status out of each `[VAL: x] [STATUS: y]` cell, drop the reading if status != "OK" or value < 0.0 (note: exactly-0.0 is kept, strictly-negative is dropped — check your comparison operator direction). Emit one compact JSON object per line with keys in the exact order `ts, sensor, val`. Make the script executable with the correct shebang, and self-test with `diff` against a few hand-run inputs before trusting it.