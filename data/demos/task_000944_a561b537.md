First locate the bug by reading the vendored source, not guessing: grep the package for the env var name, e.g. `grep -rn "STORAGE_DIR\|STORAG_DIR" /app/vendored/py-state-manager-1.0.0`. Expect to find a typo (like `STORAG_DIR` used somewhere instead of `STORAGE_DIR`) in the app code and possibly a wrapper/run script that hardcodes a bad path and overrides the env var — fix the source typo directly rather than papering over it with an extra env var, and check any launcher script for a hardcoded override that would win over what you set. After fixing, write `/home/user/deploy.py` to: create `/home/user/app_data/storage` with mode 0755 using `os.makedirs(..., exist_ok=True)` plus an explicit `os.chmod`, then launch the fixed app as a background subprocess (e.g. `subprocess.Popen` with `env=` merging `os.environ` and setting `HOST=127.0.0.1`, `PORT=9090`, `AUTH_TOKEN=DeployMagic99`, `STORAGE_DIR=/home/user/app_data/storage`), not disowned/killed. Before exiting, poll the port (e.g. attempt a socket connect or HTTP request in a retry loop) to confirm the service is actually listening, then print "Deployment successful" and exit 0. Do not stop the process — let it keep running after the script returns.