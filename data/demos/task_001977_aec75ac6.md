Reverse-engineer before coding: run `strings` and `objdump -d` on `/app/vuln_server` to find the cookie-parsing routine — look for a fixed-size stack buffer filled via a copy loop or `strcpy`-like call with no length check, while scanning for a `Cookie:` header and a `session_id=` key. Read the exact buffer size off the disassembly's stack allocation (don't guess) — that size is your overflow threshold: any `session_id` value strictly longer triggers corruption. Implement sanitizer.c to read the raw HTTP request file, locate the `Cookie:` header line, find `session_id=`, and measure the value's length up to the next `;`, `\r`, `\n`, or buffer end; if it exceeds the discovered threshold print `EVIL` and exit(1), else print `CLEAN` and exit(0). Treat requests with no Cookie header or no session_id key as CLEAN. Compile with the given gcc invocation, then hand-craft test files (just-over-threshold, just-under, no-cookie) and run your compiled sanitizer against them to confirm the boundary is exactly right before trusting the hidden corpus.
