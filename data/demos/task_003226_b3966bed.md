The NaNs come from the multiplicative-update denominators hitting zero (`W.T @ W @ H` and `W @ H @ H.T`) when the data has exact-zero rows and near-singular structure. The minimal, correct fix is to add `1e-9` to each denominator, not to change the seed or `np.random.rand` initialization order — reordering or reseeding changes the numeric result and fails the check. So make the two updates read `H = H * (W.T @ V) / (W.T @ W @ H + 1e-9)` and `W = W * (V @ H.T) / (W @ H @ H.T + 1e-9)`, leaving `max_iter`, `k=5`, and seeding untouched.

Then have the script compute `V_rec = W @ H`, load `/home/user/reference.csv`, and calculate MSE as `np.mean((V_rec - V_ref)**2)`. Note MSE is measured against the CLEAN reference, while factorization is fit on the NOISY input — don't accidentally compare against the noisy matrix. Write the result formatted to exactly 4 decimals (`f"{mse:.4f}"`) into `/home/user/mse.txt`, and run so no divide-by-zero warnings appear. Let the script compute the value; do not hardcode a number.
