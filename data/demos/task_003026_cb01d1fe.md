Read the script first to find both bugs before editing. For the dtype issue, after concatenating the CSVs, coerce the numeric column with `pd.to_numeric(data['response_time'], errors='coerce')` and then `dropna(subset=['response_time'])` — do this before splitting into the two server groups, otherwise the invalid row keeps the whole column as object dtype and breaks `scipy.stats.ttest_ind`. For the plotting crash, switch `matplotlib.use('TkAgg')` to a non-interactive backend like `matplotlib.use('Agg')`, and set it immediately after importing matplotlib but before importing `pyplot` or plotting anything. Keep the statistical test itself (independent t-test via `stats.ttest_ind`) unchanged — only fix data cleaning and backend selection. After editing, actually run the script (`python3 /home/user/analytics.py`) and confirm both `/home/user/results.json` (with `t_stat` and `p_value` keys) and `/home/user/report.png` are produced; inspect the JSON to sanity-check the values look like real floats, not NaN.
