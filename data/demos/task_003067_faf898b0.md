Start by extracting the policy text from `/app/policy.png` with `tesseract /app/policy.png -` (or similar OCR invocation) rather than guessing rules — read the OCR output carefully to identify the exact maximum AST depth and any forbidden function name(s) it specifies; these are the concrete constraints your sanitizer must enforce. Then rewrite `/home/user/parser.cpp`: implement a small recursive-descent parser for the `funcname(args...)` expression grammar that builds an AST, computes each node's depth (a bare literal or a call with only literal args counts as depth 1), and rejects (prints `EVIL`, exits 1) any expression that is syntactically invalid, exceeds the discovered max depth, or invokes the forbidden function name anywhere in the tree; otherwise print `CLEAN` and exit 0. Fix the memory leak by using RAII — e.g. `std::unique_ptr<Node>` for children instead of raw pointers, or by adding a proper destructor that recursively deletes children — and verify with `valgrind --leak-check=full` on a clean input. Compile with `g++ -O2 -std=c++17 parser.cpp -o sanitizer`, then validate against every file in `/home/user/corpora/clean/` (must all print CLEAN/exit 0) and `/home/user/corpora/evil/` (must all print EVIL/exit 1) before considering the task done.