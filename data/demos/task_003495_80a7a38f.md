Since your C program must exactly reproduce an existing compiled binary's behavior across many inputs, treat this as black-box reverse engineering, not spec-guessing: feed the reference binary a range of hand-crafted and randomized inputs matching the `UPTIME:<n>:CPU:<n>:MEM:<n>` format (including edge cases like 0, boundary values around 90, multi-digit numbers) and diff its stdout against your own implementation's stdout for the same input, iterating until outputs match byte-for-byte, including exact spacing and the newline. Only after your parser's output is verified equivalent should you wire up bridge.sh: use a loop with `nc` (netcat) to persistently connect to port 9001, pipe each line through your compiled metric_parser, then pipe the result into a `nc` connection to port 9002 — test the whole chain manually first (e.g. `nc 127.0.0.1 9001 | ./metric_parser | nc 127.0.0.1 9002`) before backgrounding it, since a common mistake is buffering issues where `nc` doesn't flush line-by-line. Make bridge.sh executable with chmod +x, and write "DONE" to setup_complete.log only once the bridge is confirmed working end-to-end.
