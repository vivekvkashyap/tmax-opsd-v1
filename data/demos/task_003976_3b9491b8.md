Build the pipeline in stages you can verify independently before wiring them into one script. First, run `tesseract /app/topology.png stdout` (or similar) and parse each output line matching the pattern `SOURCE -> DEST : LATENCY` with a regex — inspect the raw OCR text yourself first since OCR can introduce stray characters, and normalize/trim before parsing. Second, query `/app/nodes.db` with `sqlite3` for `node_name` where `free_space_gb >= 500`, and explicitly force-include `DB_PRIMARY` and `DB_ARCHIVE` regardless of their storage value, since eligibility is about intermediate routing capacity. Third, build the weighted directed graph only from edges where both endpoints are eligible, then compute shortest path with Dijkstra (or Bellman-Ford) from `DB_PRIMARY` to `DB_ARCHIVE` — a common bug is computing the shortest path over the full graph and only filtering afterward, which can pick an invalid route through an ineligible node. Write the node-name chain (comma-separated) as line 1 and the integer total latency as line 2 to `/home/user/best_route.txt`. Do all of this programmatically inside the Bash script itself (calling tesseract, sqlite3, and e.g. awk/python for the graph algorithm) — no hardcoded values.
