Start by reproducing the crash to confirm the root cause: run the script as-is and read the `LinAlgError: Singular matrix` traceback back to the `X.T @ X` inversion — this happens because `Length` is a linear combination of the A/C/G/T count columns (Length = A+C+G+T), making the design matrix rank-deficient. Fix it by editing the feature-extraction loop to drop `len(s)` from each row, leaving only the 4 count columns, and update the downstream feature-name list (used for printing) to match the reduced 4-column model — don't just truncate the printed names while leaving the extra column in X, and don't leave `Length` in X while trying to work around singularity some other way (e.g. pseudo-inverse), since the task explicitly wants the redundant feature removed. Re-run the (now non-singular) MLE-initialized Metropolis-Hastings sampler unchanged otherwise. After it finishes, load `/home/user/reference_weights.csv`, compute `abs(posterior_mean[feature] - reference_weight[feature])` for A, C, G, T, round to 2 decimals, and write them to `/home/user/results.log` in the exact CSV-style format requested.
