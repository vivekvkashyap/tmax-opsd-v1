Fix the two hardcoded misconfigurations in `/home/user/portal/app.py`: the Redis port constant must match the actual redis-server port (6379), and the Nginx base URL must point at the correct scheme/port/path (`http://127.0.0.1:8080/datasets/`) that `start_services.sh` actually configures nginx to serve — read that script to confirm the real listen port and location/alias before editing app.py, don't just guess the values. After fixing, run `start_services.sh` and confirm each service is actually up (check `redis-cli ping`, `curl 127.0.0.1:8080`, and the Flask process/log) before moving on. For sanitizer.py: load train.csv/test.csv with pandas, drop `id` and `target` columns before comparing rows for exact duplicates between the two frames (e.g. via a merge or set of tuples) to catch row overlap. Separately, on train.csv compute Pearson correlation (`df.corr()` or `scipy.stats.pearsonr`) between each numeric feature column and `target`, flagging leakage if `abs(corr) >= 0.99`; handle NaNs by dropping incomplete rows first. Print exactly `LEAK_DETECTED`/exit 1 or `CLEAN`/exit 0. Finally run the curl command and save its output to the log file.
