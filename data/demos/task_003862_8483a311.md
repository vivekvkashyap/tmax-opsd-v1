Work through the four objectives in order since later steps depend on earlier ones. First, transcribe `/app/voicemail.wav` (e.g. `whisper`) to recover the exact port number spoken — parse the transcript's spoken digits carefully rather than assuming a default port. Generate a self-signed cert/key with `openssl req -x509 -newkey rsa:2048 -keyout server.key -out server.crt -days 365 -nodes -subj "/CN=localhost"`, then serve `/home/user/www/index.html` (containing the exact required string) over HTTPS on the recovered port — Python's `http.server` needs its socket wrapped with `ssl.SSLContext.wrap_socket` (or `ssl_context=` in newer versions) rather than plain HTTP, since a bearer of self-signed certs will fail curl without `-k` at verification time, which is expected. For the C sanitizer, read up to 256 chars from stdin, and validate each byte is alphanumeric, `.`, `-`, or `_`; separately, after the allowed-charset check, scan the string for the literal substring `..` and reject if found — this catches the case where `.` is otherwise permitted but path-traversal-style `..` must still be explicitly blocked. Compile with `gcc -o /home/user/sanitizer /home/user/sanitizer.c`, then loop over every file in `/app/corpora/clean/` and `/app/corpora/evil/`, piping each into the compiled binary and checking `$?` against the expected exit code before considering the task done.
