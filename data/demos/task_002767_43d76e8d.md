First locate the bug in `/app/log-processor/src/main.rs`: it likely reads stdin via a line iterator (e.g. `stdin().lines()`) and feeds each line individually into a fresh `csv::Reader`, which corrupts any record whose `message` field embeds a real newline — replace that with a single `csv::ReaderBuilder::new().from_reader(std::io::stdin())` so the `csv` crate's own RFC 4180 quote/newline handling processes the whole stream as one logical CSV. Then implement the transform logic: mask `ip` by replacing everything after the last `.` with `XXX`; for each row emit up to two output rows (one per sensor with a non-skippable value) reshaping `cpu_temp`/`gpu_temp` into `sensor_type`/`temperature` columns; maintain two running "last seen" floats (one per sensor) across rows, and when a value is empty use the last seen value for that sensor, or `0.0` if none has been seen yet, updating the last-seen state whenever a real value appears. Build with `cargo build --release` inside `/app/log-processor` so the binary lands at `target/release/log-processor`. Since verification is byte-exact fuzz equivalence against a hidden oracle, test manually with crafted CSVs containing embedded newlines, emoji, and missing temperatures to confirm exact output formatting (float representation, line endings, column order) before trusting it.