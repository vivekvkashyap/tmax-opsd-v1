Build the Rust tool as a pipeline of independent stages so each is easy to test: (1) recursively walk `/home/user/system_mock/` (e.g. with `walkdir`), collecting only regular files; (2) for each, compute SHA-256 over its raw bytes and compare to `/home/user/known_hashes.txt` — parse that file line-by-line splitting on whitespace (`sha256sum` output uses hash, two spaces, then the absolute path), and only proceed if the file's path appears with a hash that matches exactly; any file missing from the list or with a mismatched hash must be silently dropped, not logged as a warning. (3) For files that survive verification, read `std::fs::metadata(...).permissions().mode()` and test bits directly: SUID is `mode & 0o4000 != 0`, world-writable is `mode & 0o002 != 0`; when both are set, classify as "SUID" per the spec. (4) Collect `{file, vulnerability}` pairs, sort the vector by the `file` string before serializing, and use `serde_json` to pretty-print (2-space indent) so it matches the example formatting, writing to `/home/user/evasion_log.json`. Common mistakes: forgetting to sort, hashing the wrong path (relative vs absolute), and misreading the two-space-separated `sha256sum` format.
