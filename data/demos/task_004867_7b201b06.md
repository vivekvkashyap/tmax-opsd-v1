Work through this as a debug-fix-verify loop. First read `libcalc.c` carefully: check the loop bounds in `sum_array` against the array length parameter — an off-by-one in a `for` loop comparison operator (`<=` vs `<`) is the classic out-of-bounds-read bug, since arrays are zero-indexed and the last valid index is `len-1`. Fix the comparison operator only. Next inspect `setup.py`: the `Extension()` call's `sources=[...]` list must include every `.c` file the extension actually needs to link against — grep the C wrapper file for `extern` declarations to see which other `.c` file provides those symbols, and add it to the sources list. For the test, use `hypothesis.strategies.lists(integers(min_value=-1000, max_value=1000))` with `@given`, import `calc_ext`, and assert `calc_ext.sum_array(lst) == sum(lst)`. Finally rebuild in-place with `python setup.py build_ext --inplace` before running pytest — a stale compiled `.so` will silently mask your C fix — then redirect `pytest test_calc.py` output to the required log file and confirm it shows a pass.
