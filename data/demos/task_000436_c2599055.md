Approach this as nested archive unpacking followed by structured extraction — don't assume file types from extensions alone. First, open `/home/user/config_backups.ccp` with Python's `tarfile` module in gzip mode (`tarfile.open(path, 'r:gz')`) despite its unusual extension, and extract its members to a working/temp directory. Parse the extracted `index.xml` with `xml.etree.ElementTree`, iterating `<version>` elements to get each id→filename mapping. For each nested archive, detect its real format (check the filename extension given in the XML, or better, sniff the file's magic bytes) and dispatch to `zipfile.ZipFile` or `tarfile.open` accordingly — don't assume all nested archives share one format. From each opened nested archive, extract `app_config.json` and parse it, then walk the nested dict to reach `database.max_connections`; separately extract `services.csv`, parse it with the `csv` module, and find the row where `service_name == "cache_service"` to get its `port`. Assemble a dict keyed by version id with `max_connections` and `cache_port`, in the order given by index.xml, then write it as JSON to the required output path — use a temp extraction directory (e.g. tempfile.mkdtemp()) so repeated runs don't collide with stale files.
