Treat this as three separable subsystems and debug each with the right tool. For the race condition, run the Go program repeatedly under `go run -race` (or build with `-race`) to get the exact goroutine/line pinpointed by Go's race detector, then fix it with proper synchronization (mutex/channel) around the shared state rather than guessing — don't just add `sync.Mutex` everywhere blindly. For the precision loss in the FFT/accumulation math, compare against a known-good FFT implementation or reference values on simple synthetic signals; look for naive summation patterns (e.g., accumulating float32 instead of float64, or repeated subtraction that causes cancellation) and switch to more numerically stable accumulation (Kahan summation or higher precision). For the clustering non-convergence, check the convergence/stopping criterion and iteration cap — a subtly wrong distance metric or centroid update formula is a common culprit; test convergence on small deterministic inputs. Build to `/home/user/audio-detector` and validate against both clean and evil fixture-style WAV files you construct yourself before trusting the hidden corpus. For the transcript, install a Whisper-based CLI tool and run it on `/app/evidence_042.wav`, then save its exact transcribed text to `/home/user/transcript.txt` — do not fabricate the text.
