Start by inspecting the actual schema and data with `sqlite3 /home/user/company.db ".schema personnel"` and a `SELECT *` to see the real `document` JSON shapes and reporting hierarchy — don't assume structure. Then compute, for every employee that has at least one report (i.e., appears as a `reports_to` value, making them a "manager" in this context) — or more simply, compute the subtree sum for every node in the tree since the required output covers all employees: build a parent-to-children adjacency map from `reports_to`, extract `compensation.base` and `compensation.bonus` from each row's JSON (defaulting missing fields to 0) via `json.loads` in Python or SQLite's `json_extract`, then recursively (or iteratively via post-order/DFS) sum each node's own cost plus all descendants' costs, and count descendants+self for `tree_size`. Write one JSON object per line to /home/user/manager_costs.jsonl in the exact key order and types specified (`total_tree_cost` as float, `tree_size` as int), sorted ascending by `manager_id`. Verify your recursive traversal correctly accumulates multi-level trees (grandchildren must roll up through their parent, not just direct children) by manually tracing the sample hierarchy before finalizing.