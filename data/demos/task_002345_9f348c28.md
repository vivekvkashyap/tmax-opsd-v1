Model this as a dependency graph problem, not a simple line-by-line eval. First decode all `.enc` files (base64) to get the raw expression strings, and for each build the set of `.enc` filenames it references as tokens within the expression. Build a directed graph of these references, then detect cycles (e.g. DFS with a recursion/visiting-stack, or Kahn's topological sort — any node left unprocessed after topo-sort is in or depends on a cycle) — a file must be moved to `cycles/` if it's part of a cycle OR if any of its dependencies (transitively) are. Only after classifying every file should you evaluate the acyclic ones: substitute each referenced filename with its already-computed numeric value and evaluate the resulting arithmetic expression respecting standard operator precedence (Python's `eval` on the substituted numeric expression is acceptable here since inputs are your own generated files, or write a small recursive-descent parser if you prefer safety), processing files in dependency order (evaluate dependencies before dependents, e.g. via topological order or memoized recursion. For the benchmark, find the acyclic file with the longest dependency chain (longest path in the DAG), then time 1000 in-memory re-evaluations of its full expression tree with `time.perf_counter()`, writing the average in microseconds in the exact specified format.