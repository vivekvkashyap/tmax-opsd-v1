Do this as three strictly sequential, deterministic stages in pure shell/awk, since exact reproducibility depends on precise operation order. First compute mean and sample standard deviation (N-1 denominator) of `/home/user/ref_data.txt` with `awk` (sum, sum-of-squares in one pass, then the variance formula). Second, implement the Metropolis sampler in a single `awk` script: call `srand(42)` exactly once in `BEGIN`, initialize `x = mu`, then loop exactly 10000 times printing the *current* state before proposing/accepting — the print-then-propose order matters so the very first line of output equals mu exactly. For each iteration compute the proposal via `rand()`, the acceptance ratio via the Gaussian density ratio (careful to compute it as an exp of a log-difference rather than two separate exponentials, to match reference numerics), and consume exactly one more `rand()` call only in the branch where alpha < 1 — calling `rand()` unconditionally when it's not needed will desynchronize your random sequence from the reference and break exact-match verification. Third, bin the 10000 saved samples into 10 fixed-width bins over [mu-3σ, mu+3σ) using floor-division indexing, discarding out-of-range samples, and write exactly 10 integer counts. Since verification is exact diff, keep everything in one gawk invocation to avoid subtle rand()-state differences between separate awk processes.
