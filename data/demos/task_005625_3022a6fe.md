Solve the root-finding step with `scipy.optimize.brentq(lambda x: x**3 - 4*x + 1, 0, 1)` (bracketed, robust, avoids picking the wrong one of the polynomial's three real roots — check the sign change at the endpoints first). Compile sum_sim.c as-is with `gcc -O2 -o sum_sim sum_sim.c -lm` and run `./sum_sim <root>` passing the root with enough decimal digits (e.g. Python's `repr()`/`%.17g` of the float) so truncation doesn't itself introduce error; parse the two printed sums. For the Python ground truth, replicate the exact same mathematical sum $\sum r/(n^2+r^2)$ for n=1..1e6 in float64 (vectorize with numpy for speed: `r/(n**2+r**2)` summed via `np.sum`, or a Kahan-style summation if you want extra precision) — this is the reference the C outputs are compared against. Then just compare `abs(c_forward - python_true)` vs `abs(c_backward - python_true)` to decide which is closer; do not assume the answer, actually compute both C results from your own compiled binary. Reasoning about why backward summation tends to be more accurate: in float32, forward order adds many small terms after the running sum has already grown large, losing their precision to rounding, while backward order accumulates small terms first when the sum is still small.