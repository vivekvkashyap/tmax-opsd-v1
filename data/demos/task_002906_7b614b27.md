This is a two-part task: signal extraction plus a bit-exact Bash arithmetic script. For part 1, use `ffmpeg -i /app/sequencer_run.mp4 -vf format=gray` piped through a filter (e.g. `signalstats` with `-f null -` and parse `YAVG`, or extract PNG/PGM frames per-frame and average pixel values) to get one intensity value per frame in output order; write one float per line to the target file, and verify the line count matches the frame count you expect from the video's duration and fps. For part 2, write the Bash script to: (1) add 1 to every integer in each input file (pseudo-count) before summing, (2) normalize each list by its own sum to get P and Q, (3) compute sum(P_i * log2(P_i/Q_i)) per line, (4) format the final result to exactly 4 decimal places with no extra output. Do the floating-point math with `awk` (has native log()) rather than `bc` — remember bc lacks log2, so compute log(x)/log(2) if you use bc, or just use awk's `log(x)/log(2)`. Test with hand-crafted small files first to sanity-check the KL formula before relying on it, and make sure the script strips trailing whitespace/blank lines from inputs so parsing doesn't break.