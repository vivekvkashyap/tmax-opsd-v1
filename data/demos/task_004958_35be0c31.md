Don't try to spot the cycle by eye alone — build the graph programmatically and run a proper cycle-detection algorithm. Read `/home/user/etl_graph.txt` into an adjacency list (a quick Python or awk script is easiest), then do a DFS with a recursion-stack/"visiting" set (the classic white/gray/black coloring) to find a back-edge; when you hit a node that's already on the current DFS path, you've found the cycle — walk back along the path from that node to reconstruct the full cycle membership rather than just the single back-edge pair. Note that `tsort` can only tell you a cycle exists (it errors out), not which nodes are in it, so it's not sufficient alone — use it as a quick sanity check but do the real work with your own traversal. Once you have the cycle's node set, sort it alphabetically and write one per line to `/home/user/cycle_tasks.txt`. Then join those node IDs against `/home/user/etl_metadata.csv` (e.g. with `awk -F,` or Python's `csv` module) to filter rows with `TaskType` exactly `Transformation`, extract their `Description` field, sort alphabetically, and write to `/home/user/deadlocked_descriptions.txt`. Double check exact string matching on TaskType (case and spelling) since silent mismatches will silently drop rows.