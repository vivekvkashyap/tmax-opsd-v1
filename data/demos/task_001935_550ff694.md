Part 1: install/use an offline transcription tool (e.g. `whisper` / `whisper.cpp`, converting the wav with `ffmpeg` if needed for sample rate) to transcribe `/app/baseline_memo.wav`, then manually convert the spoken number words in the transcript into the exact numeric literals in spoken order (including negative and decimal values) and write one per line — don't guess the numbers, read them off the actual transcription output. Part 2: in Go, build the call graph by indexing calls by `id`, then linking each to its `parent_id` (nodes with empty parent_id are roots), and compute `duration_ns / invocations` per node as a float64. Part 3: write the filter as (a) a cycle check — DFS/topological-sort over the parent_id edges, flagging any node reachable from itself; (b) a numeric check — for every node, verify `invocations != 0` and the resulting division is finite (not NaN/Inf, e.g. via `math.IsNaN`/`math.IsInf`) and positive; (c) reject any `duration_ns < 0`. Return exit 0 only if none of these trip. Before finishing, iterate the filter over every file in `/app/corpus/clean/` and `/app/corpus/evil/` and confirm each gets the expected exit code — fix mismatches by tightening the specific check that failed.
