Split this into an OCR extraction pass and a pure-logic scripting pass; do not try to guess IPs by hand. For Part 1: use `ffmpeg -i /app/tunnel_monitor.mp4 -vf fps=<N> frame_%04d.png` at a high enough sample rate to not miss short flashes, then run `tesseract` on each frame (consider cropping/thresholding to the log region and upscaling if text is small) and grep the OCR text for the literal marker `CRITICAL_DROP:` followed by an IPv4 pattern; dedupe the captured IPs and pipe through `sort -t. -k1,1n -k2,2n -k3,3n -k4,4n` (numeric-per-octet, not lexicographic `sort`) before writing to critical_nodes.txt. For Part 2 (route_filter.sh): parse each routing-table line's CIDR into network/mask, convert the target IP and network address into 32-bit integers (e.g. via arithmetic on each octet, no external tools needed), and for a candidate route it matches only if `(target_int & mask_int) == (network_int & mask_int)`; track the best match by iterating input lines in order and only replacing the current best when a strictly longer prefix is found (equal-length prefixes must NOT replace — this preserves the first-occurrence tie-break naturally). Handle /0 and /32 edge cases explicitly, and print `DROP` with a trailing newline when nothing matches. Test both parts against many synthetic examples before considering it done, since the grader fuzzes heavily.
