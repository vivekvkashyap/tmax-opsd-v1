Load the CSV with `pandas.read_csv` directly (not by splitting lines manually) — pandas' C/python parser correctly handles quoted fields containing embedded newlines, so rows won't be dropped or misaligned; a naive line-by-line reader will break on `raw_diff` values that span multiple lines. Normalize server names with `.str.lower().str.replace('-', '_')` and parse both timestamp columns with `pd.to_datetime(..., utc=True)`. Extract keys from `raw_diff` using `re.findall(r'\[(?:UPDATE|ADD)\] (\w+)=', text)` per row, and derive `change_count` as the length of that list (for the JSON source, `change_count` is the length of `changed_keys`). Concatenate both normalized frames into one schema (`timestamp`, `server`, `updated_keys`, `change_count`), set `timestamp` as a DatetimeIndex, and sort. For the rolling sum, group by `server` and apply `.rolling('3h', closed='right').sum()` on `change_count` per group (time-based window, not a fixed row count), then reassemble the results back onto the full frame before writing to Parquet with `pyarrow`. Double check closed='right' semantics match the expected boundary inclusion.