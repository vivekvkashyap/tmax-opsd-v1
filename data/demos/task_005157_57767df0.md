Chain this as a dependency graph and bring up each layer before wiring the next. First decode `/app/voicemail.wav`: it's DTMF tones, so use a DTMF decoder (e.g. `multimon-ng -a DTMF -t wav /app/voicemail.wav`, or a Python Goertzel-based decoder) to actually recover the digit sequence — don't guess it. Write and compile the Go server to listen on a Unix socket (`net.Listen("unix", "/tmp/backend.sock")`) serving `/pin` with the JSON body built from your decoded digits. Write `keepalive.sh` to check (e.g. `pgrep -f` your binary name) and relaunch it if absent, then install it into the user's crontab via `crontab -l | { cat; echo "* * * * * /home/user/keepalive.sh"; } | crontab -` (start the cron daemon if it isn't already running) and start the backend once yourself so it's live immediately. Fix `nginx.conf`'s `proxy_pass` to point at `http://unix:/tmp/backend.sock:/pin` instead of the broken path, then reload nginx so port 8080 works. Finally, set up passwordless SSH to localhost (generate a keypair, append the pubkey to `authorized_keys`, ensure the sshd is running) and background an `ssh -N -L 0.0.0.0:8888:127.0.0.1:8080 user@localhost` tunnel (or `-R`/`-g` as needed for external bind) — verify each layer with a local `curl` before moving to the next so failures are isolated.