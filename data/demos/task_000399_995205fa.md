Read the C source first to locate the exact gradient-ascent update line for `mu` — the fix is precisely as described: change the step size used in that update from the fixed `alpha` to `alpha / n` (dividing by the number of sequences), since the unscaled step size makes each gradient-ascent update overshoot proportionally to how many sequences contribute to the gradient sum. Make this one targeted edit without altering the rest of the parsing, GC-content computation, or Z-score logic. Compile with `gcc -o seq_analyzer seq_analyzer.c -lm` (linking libm is required because of `sqrt`). Run it against the given FASTA dataset and redirect stdout exactly (`./seq_analyzer > analysis_output.txt`), preserving the program's own print formatting rather than reformatting the numbers yourself. Sanity-check your fix by hand: after the correction, gradient descent should converge mu to the actual mean GC-content of the dataset (computable directly by counting G/C characters per sequence) rather than diverging to NaN/huge values — use that as your own verification signal before considering the task done.
