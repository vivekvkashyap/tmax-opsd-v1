Work through this credential-rotation chain step by step, verifying each artifact before using it. First recover the legacy key: disassemble `/home/user/auth_handler.pyc` with `uncompyle6`/`decompyle3`/`dis` module (or just `python3 -m dis` on the marshalled code object) to find the hardcoded secret string returned by the key-getter function — don't guess it. Second, write a Python script using `requests` (or `http.client` + `ssl`) that POSTs to `https://127.0.0.1:8443/rotate` with `verify='/home/user/ca.crt'` (never `verify=False`), sets `Authorization: Bearer <recovered_key>`, and sends the exact JSON body `{"new_key": "secure_new_key_2024"}`. Third, inspect the response: read the `Set-Cookie` header to pull out the `session_token=...` value (split on `;` and `=`), and separately use `ssl.get_server_certificate` or `openssl s_client -connect 127.0.0.1:8443 -showcerts` piped to `openssl x509 -noout -subject` to read the server cert's Subject CN. Finally write exactly three lines (old key, session_token value, CN) to `/home/user/rotation_summary.txt` — no extra whitespace, labels, or trailing content.
