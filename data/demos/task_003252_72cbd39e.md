Start by extracting frames from the video with ffmpeg (e.g. `ffmpeg -i /app/waf_debug.mp4 -vf fps=2 /tmp/frames/frame_%04d.png`) and read them (visually or via `tesseract` OCR) to recover the exact backend logic: the signature formula, the IDS blocked substrings, and the string-replacement rules used for normalization/evasion. Reason about the pipeline in the CORRECT order the backend applies it: signature check happens on the pre-normalization string, IDS pattern matching happens on the pre-normalization string too, and only THEN does normalization substitute your evasion markers into the traversal sequences/forbidden words — so your script must construct a payload where (a) forbidden literal substrings like directory-traversal sequences and blocked words never appear before normalization, (b) after normalization it resolves to exactly `/var/www/uploads/` + your evasion-marker path = the target absolute path, and (c) the signature is computed over the pre-normalization (evaded) string using the salt discovered in the video, not the final resolved path. Work out algebraically how many levels to traverse up from the uploads directory to reach the filesystem root. Test your script against several sample absolute paths and manually simulate the backend logic (signature check, forbidden-substring check, then substitution) to confirm it reproduces the intended target path before considering it done, since a fuzzer will test against many random paths/contents.