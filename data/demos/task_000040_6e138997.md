Build this as four separate, independently-checkable shell stages rather than one big pipeline, and inspect the intermediate CSV after each phase. For schema validation, use `awk -F,` and count fields with `NF==5`; validate CPU/RAM/DiskIO/ID with a regex like `^[0-9]+(\.[0-9]+)?$|^\?$` and Status with `$5=="OK" || $5=="FAIL"` — remember rows with extra commas (more than 5 fields) must be dropped, and non-numeric junk like `invalid` must fail the numeric check. For imputation/capping, use `awk` with `if ($2=="?") $2="50.0"` etc., and a numeric comparison (not string) for the RAM cap `>100.0`. For Load_Factor, use `awk`'s `printf "%.2f"` for 2-decimal formatting, not `bc` alone, to avoid inconsistent rounding. For the threshold search, loop T from 1 to 100 in a `for` loop, and inside each iteration use `awk` to count correct predictions in one pass over `engineered.csv` — compare `Load_Factor >= T` against actual `Status` — then track the best (T, accuracy) pair, preferring the lowest T on ties by only updating when strictly greater accuracy is found.
