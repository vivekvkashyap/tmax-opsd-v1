Break this into three independently testable stages and verify each before moving on. (1) Video: use `ffmpeg`/OpenCV to read frames from `/app/experiment.mp4` at 30 FPS, and per frame locate the brightest pixel/blob (e.g. grayscale + `argmax`, or thresholding + centroid) to get (x,y); write `frame,x,y` rows to `/home/user/trajectory.csv` — sanity-check the trajectory looks continuous, not noise. (2) MCMC: in `mcmc_sampler.cpp`, implement Metropolis-Hastings over parameters D and alpha whose likelihood comes from the fBM increment covariance structure (increments have variance ~ D * |dt|^alpha); parallelize with OpenMP either by running independent chains per thread (`#pragma omp parallel for` over chain index) or by splitting the likelihood sum across threads with reduction, then average post-burn-in samples across chains for the posterior mean; write `D=<value>\nalpha=<value>` to `/home/user/parameters.txt`. Compile with `g++ -fopenmp`. (3) Service: expose GET `/simulate?steps=N` returning `{"trajectory":[{"x":...,"y":...},...]}` of length N starting at (0,0), generated using the fitted D/alpha (simulate fBM increments, not plain Brownian motion). Start it in the background on port 8080 and confirm with `curl` before finishing.