Treat this as two mostly-independent deliverables. First, run `/app/transcribe.sh /app/telemetry.wav` just to see the shape/format of transcript text you'd be piping into the Go program — this is only for context, not for hardcoding values. Second, the graded part: write `/home/user/main.go` to match a precise statistical spec. Read all of stdin, split strictly on whitespace with `strings.Fields`, `strconv.ParseFloat` each token and silently drop failures; if fewer than 2 valid floats remain, print exactly `{"error": "insufficient data"}` and exit 0. Otherwise compute the mean, sample stddev with Bessel's correction (divide by N-1, not N — a common bug), and the 95% CI as mean ± 1.96*(stddev/sqrt(N)). Classify "CRITICAL" only if the CI lower bound is strictly greater than 10.0, else "NORMAL". Round every float to exactly 4 decimals before JSON-encoding with consistent key ordering, since the grader diffs your binary against a reference oracle byte-for-byte across thousands of fuzzed inputs — test edge cases (0, 1, many tokens; junk text; empty input) via `go run` piped from `echo` before finishing.