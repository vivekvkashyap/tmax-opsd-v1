Work the three subproblems in order, verifying each before moving on. First, diagnose the pip conflict directly: run `pip install -r requirements.txt`, read the resolver error, identify which pinned package version is incompatible with the other, and loosen the numpy pin (e.g. to a `>=` range compatible with the pinned scipy) rather than downgrading scipy. Second, before touching the Python fix, write `stress_test.sh` to launch many concurrent background invocations of the target script (using `&` and `wait`, or GNU `parallel`/xargs -P), capturing each run's stderr so you can grep it for the specific instability marker string; on any match, print the required message and exit 1 immediately, otherwise tally successful completions and exit 0 once the total run count is reached — remember to `chmod +x` it. Confirm this script currently fails (exit 1) against the buggy script, since that's your regression baseline. Third, inspect the shared-state update method for the classic read-modify-write race across threads and serialize the critical section with a `threading.Lock()` (acquire before reading/updating shared accumulators, release after) rather than just narrowing the window. Re-run the stress test afterward to confirm it now exits 0.
