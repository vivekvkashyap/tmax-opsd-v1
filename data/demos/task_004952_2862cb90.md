Confirm the target is reachable first (e.g. a quick `requests.get` or socket connect check on 127.0.0.1:9090). Build the minimal payload in memory as raw bytes starting with the 4-byte ELF magic `b'\x7fELF'` followed by a little padding — you don't need a fully valid ELF, just satisfy the server's signature check on the leading bytes. The core exploit is in the `filename` field of the multipart upload: since the server likely does naive path concatenation (e.g. `UPLOAD_DIR + filename`) rather than sanitizing it, craft a filename using `../` traversal sequences that walks from `/tmp/uploads/` back up to `/` and then down into `/home/user/success.elf` (count directory levels carefully, e.g. `../../../home/user/success.elf`, and be prepared to try slightly different depths if the first doesn't land exactly). Send this via `requests.post(url, files={'file': (crafted_filename, payload, 'application/octet-stream')})`, using the `requests` library's raw multipart construction (or manual `urllib`) so the traversal string in `filename` isn't altered/escaped by the client library. After running the script, confirm `/home/user/success.elf` exists and starts with the ELF magic bytes.
