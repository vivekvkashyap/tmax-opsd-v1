Work incrementally and verify each stage before moving on. First, `cd /app/liburldecode` and try `make` to see the actual failure — a shared-library link error almost always means missing `-fPIC` in the compile flags for the object files feeding the `.so` target; add `-fPIC` to CFLAGS (and keep `-shared` on the link line) so both `liburldecode.so` and `decode_cli` build. Next, open `urldecode.c` and find the `%XX` decode loop: the bug is indexing `str[i+1]`/`str[i+2]` without checking they exist and without validating they're hex digits before calling a hex-conversion helper — fix it by first checking `i+1` and `i+2` are within the string length AND that both characters satisfy `isxdigit()`; if either check fails, copy the `%` (and any trailing char) through to the output unchanged rather than reading past the end. Test manually with edge cases like a trailing `%`, `%2` at EOF, and `%2G` to confirm no crash and pass-through behavior before trusting it against harder fuzzing. Finally, write `webhook_api.py` with FastAPI, load the `.so` via `ctypes.CDLL`, declare `argtypes`/`restype` for the decode function, expose `POST /decode`, and run it bound to `127.0.0.1:8000` with uvicorn.