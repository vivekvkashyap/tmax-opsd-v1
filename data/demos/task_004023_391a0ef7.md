Parse the CSV line by line, strictly validating the schema: split on commas and require exactly 3 fields with the third parsing cleanly as an integer, discarding any line that doesn't conform (e.g. missing commas). Discard edges where `ExecutionCost >= 1000` before building the graph, since those are the spurious cross-join artifacts. Build an adjacency list from the surviving edges and run a standard shortest-path algorithm (Dijkstra, since costs are non-negative, or Bellman-Ford for simplicity given the small graph) from `START` to `END`, tracking predecessors to reconstruct the path. Reconstruct the path by walking predecessors backward from `END` to `START` then reversing, and write it joined with `->` exactly as specified, with no extra whitespace or characters, to the output file.
