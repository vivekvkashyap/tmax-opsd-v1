Extract frames at each integer-second mark with ffmpeg (e.g. `-vf fps=1` or explicit per-second seeking) and decode each with pyzbar's Code128 support, keeping the decoded "Source-Target-Strength" edges in the chronological order they appear in the video — this ordering matters later for the rolling average. Load the edges into SQLite with a column that preserves insertion/temporal order so you can reconstruct the sequence for queries. Build the Flask/FastAPI app to bind exactly to 127.0.0.1:8555 with a POST /analyze route. For direct_interactions, query edges where the requested node is the source, ordered by strength descending. For strongest_path_to_hub, do a BFS to find the minimum hop-count path(s) to "ProtHub", then among all paths tied for that minimum hop count, pick the one with the highest total strength sum — don't just return the first BFS path found. For rolling_avg_strength, average the strength of all edges where the node appears as either source or target, using the chronological ordering to determine which edges are in scope per the spec. Keep the server process alive after startup so it can actually serve requests.
