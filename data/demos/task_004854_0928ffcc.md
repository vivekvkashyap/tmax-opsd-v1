Start by OCR'ing `/app/pipeline_rules.png` (install `tesseract-ocr`/`libtesseract-dev` if missing, run `tesseract /app/pipeline_rules.png -`) to recover the exact filtering/transformation rules — treat the recovered text as the full spec, don't guess at rule details like which literal substrings trigger discarding/replacement or which field position is affected. Implement `/home/user/parser.cpp` to read stdin line by line, split each line on whitespace exactly as `awk` field-splitting does (collapsing runs of whitespace, ignoring leading/trailing whitespace, handling empty lines as zero fields), and apply the recovered rules in order (line-level filter first, then a fewer-than-N-fields passthrough special case, then a field substitution, then a pipe-joined field output). Since correctness is judged by exact character-for-character match against a hidden reference across thousands of fuzzed inputs, pay special attention to edge cases: empty lines, lines with exactly the field-count boundary, and lines with no trailing newline. Compile to `/home/user/log_parser` and manually test against a handful of hand-built edge-case lines before trusting the fuzz comparison.