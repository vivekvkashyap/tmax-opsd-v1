Sequence the work so each piece can be checked independently before combining. First set up the static content and TLS: create `/home/user/www/healthz` and `/www/metrics` with their literal contents, then generate a self-signed cert with `openssl req -x509 -newkey rsa:2048 -nodes -keyout key.pem -out cert.pem -days 365 -subj "/CN=localhost"` (the `-nodes` flag is required for an unencrypted key). Serve `/home/user/www` over HTTPS on 127.0.0.1:8443 in the background — Python's `http.server` doesn't do TLS natively, so wrap a `BaseHTTPServer`/`http.server` instance's socket with `ssl.wrap_socket`/`SSLContext.wrap_socket` using the generated cert/key, or use another tool that supports TLS directly; verify with `curl -k https://127.0.0.1:8443/healthz` before moving on. Second, extract URI paths from `raw_endpoints.txt` with a targeted regex/awk match on tokens starting with `/` (e.g. `grep -oE '/[a-zA-Z0-9/_.-]+'`), being careful not to also match unrelated slashes in dates or comments — write one path per line to `clean_paths.txt` in original order. Third, write `checker.c` using `libcurl`'s easy interface, setting `CURLOPT_SSL_VERIFYPEER` and `CURLOPT_SSL_VERIFYHOST` to 0/false since the cert is self-signed, looping over each path from the file, requesting `https://127.0.0.1:8443<path>`, and getting the status via `CURLINFO_RESPONSE_CODE`; compile with `-lcurl` and write `path,http_status_code` rows to the CSV, then run it.