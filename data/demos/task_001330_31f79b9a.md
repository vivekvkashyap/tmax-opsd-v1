Treat the port assignment as a constraint-satisfaction problem, not a greedy pick: read all services, sort by the size of their `allowed_ports` list (most-constrained first) or simply backtrack — try each service's candidate ports in order, recursing/backtracking when a later service runs out of options, since a naive greedy-first-available assignment can lock in a port for a loosely-constrained service that a more-constrained service strictly needs, making the problem unsolvable even though a valid global assignment exists. For serialization, re-read the spec literally: every multi-byte integer (service count, port, config-data length, and final CRC) must use big-endian struct format (`>H`, `>H`, `>I`), not the current little-endian (`<`) packing — check every `struct.pack` call. For the checksum, compute `zlib.crc32` over the exact bytes written so far (magic header through the last service's config data, before appending the checksum itself), mask with `& 0xFFFFFFFF`, and pack it big-endian as the last 4 bytes — do not include the checksum field itself in the CRC input. After fixing the logic, run the script and confirm `output.pack` round-trips correctly by manually unpacking it the way a verifier would.