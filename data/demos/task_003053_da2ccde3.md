Start by reproducing the bottleneck, not guessing at it: launch the services, queue a task, and inspect the actual bytes flowing through Redis versus what the worker decodes them as. Compare the numpy dtype string used when the API encodes the array (e.g. `astype('<f4')` little-endian) against the dtype string the worker uses in `np.frombuffer(..., dtype=...)` — an endianness mismatch (`<f4` vs `>f4`) silently reinterprets legitimate small floats as huge numbers, which is exactly why gradient descent never converges and hits max_iter. Fix the mismatch on whichever side is wrong so both use the same byte order consistently. Write `test_serialization.py`'s `test_roundtrip()` to literally perform the same base64+numpy encode/decode steps used in `api.py`/`worker.py` on the array `[1.5, -2.3, 3.14]` and assert `np.array_equal` (or `allclose`) with the original — this pins the fix as a regression test. Finally, re-run the full pipeline and time processing 100 tasks end-to-end to confirm you're well under the 3-second threshold; if still slow, check for other inefficiencies like polling intervals in the worker loop.