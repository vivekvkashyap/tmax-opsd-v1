Work this in two independent phases and verify each before combining. Phase 1 (video): extract frames from `/app/breach_recording.mp4` at 1 fps (e.g. `ffmpeg -i /app/breach_recording.mp4 frame_%02d.png`), then for each of the 32 frames sample a pixel and classify it as pure red (#FF0000 -> bit 0) or pure blue (#0000FF -> bit 1); concatenate the 32 bits in frame order (MSB first) and convert the binary string to a decimal integer — that integer is your secret salt, do not hardcode a guessed value. Phase 2 (binary): use `file`, `strings`, and a disassembler/decompiler (`objdump -d`, `radare2`, or `ghidra`) on `/app/auth_oracle` to trace what it does with argv[1] (salt), argv[2] (user_id), argv[3] (timestamp) — look for string reversal, XOR operations, string formatting (snprintf-style concatenation), and a hashing call (check for linked crypto libs like OpenSSL/SHA to identify the hash function and how many hex bytes of the digest are printed). Reimplement in Python exactly matching operation order (e.g. reverse before or after XOR matters), then empirically validate your script's output against directly running the oracle binary on several sample inputs before finalizing, since the grader will fuzz-test bit-for-bit equivalence across ~1000 random user_id/timestamp pairs.