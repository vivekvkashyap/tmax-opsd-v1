Treat this as three independent computations that feed one merged report — compute and sanity-check each separately before assembling the final JSON. For the recursive cost, write a SQLite recursive CTE that starts from the root row (`id=1`), recursively joins `components` on `parent_id`, and propagates a multiplier (product of `required_qty` down the chain) so each descendant's contribution is `unit_cost * multiplier`; sum `unit_cost` over the whole closure (root's own unit_cost plus all descendants weighted by their path's cumulative required_qty) — build and inspect the CTE's raw row output first to confirm the multiplier logic before summing. For centrality, load all `(parent_id, id)` pairs into a `networkx.DiGraph` (edge parent->child), call `nx.in_degree_centrality(G)`, then sort nodes by (centrality descending, id ascending) and take the top 2 — note ties are expected and the ID tiebreak matters. For supplier capacity, parse `/home/user/suppliers.json`, iterate every supplier's inventory array, and accumulate `qty` per `component_id` into a dict across all suppliers (use string keys to match the required output format). Write all three results into `/home/user/optimization_report.json` using the exact key names and structure given in the task.
