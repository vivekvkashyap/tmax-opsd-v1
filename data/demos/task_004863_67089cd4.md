This is a black-box reverse-engineering task: your Python script will be fuzz-compared byte-for-byte against the real binary on thousands of random inputs, so infer behavior empirically rather than guessing. Start with `strings /app/backup_decoder` to look for error messages and magic constants, then use `ltrace`/`strace` while piping known inputs (e.g. `echo -n "BKP1hello" | /app/backup_decoder`) to observe read/write syscalls and any XOR-like patterns; feed carefully crafted inputs (all-zero payloads of increasing length, empty input, wrong magic, right magic + known bytes) and diff the outputs by hand or with `xxd` to detect a rolling/incrementing key rather than a static one. Confirm the exact boundary conditions by testing minimal cases: too-short input, invalid magic, and valid-magic-with-zero-length-payload, checking both the exit code and any stderr message precisely. Once you have a hypothesis for the transform, write `/home/user/decoder.py` reading `sys.stdin.buffer`/writing `sys.stdout.buffer`, and validate it yourself by running both the original binary and your script on the same random inputs in a loop, diffing stdout, stderr, and exit codes until they match consistently before considering it done.