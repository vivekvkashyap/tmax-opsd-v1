Write this C program in clear phases and test each phase in isolation before wiring them together. Phase 1: implement a function that opens a file, sums all byte values, and returns `sum % 256` as an 8-bit checksum — verify it manually against a tiny known file. Phase 2: implement the fixed-record binary reader/writer for `/home/user/state.bin`: each record is exactly 64 bytes, bytes 0-62 hold the null-terminated/padded basename (use `strncpy` into a 63-byte buffer and be careful it's not necessarily null-terminated if the name is exactly 63 chars — always explicit-zero the buffer first), byte 63 is the raw checksum value. Read the whole file with `fread` in a loop of 64-byte chunks into a struct/array, one record per chunk. Phase 3: scan `/home/user/configs` with `opendir`/`readdir`, filter names ending in `.conf`, compute each current checksum, and build a lookup (e.g. simple array + linear search, or a hash map) keyed by basename comparing old state vs current files to classify into added/modified/deleted/unchanged — a file is "added" if it's in current but not old state, "deleted" if in old state but not current, "modified" if present in both but checksums differ, else "unchanged". Sort each of the four lists alphabetically before printing. Phase 4: emit JSON with 2-space indent matching the exact key order/format shown in the task, always including all four arrays even when empty. Phase 5: overwrite `/home/user/state.bin` with the new current-state records in the same 64-byte format. Compile with `gcc`, run it, and manually inspect both output files against your own hand-computed checksums before finishing.