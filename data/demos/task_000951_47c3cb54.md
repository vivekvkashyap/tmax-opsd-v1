Scaffold with `cargo new`, then build the pipeline in clear stages so each is verifiable independently: (1) recursive walk of `/home/user/legacy_docs` for `*.txt` (the `walkdir` crate simplifies this over manual recursion); (2) for each file, first capture its filesystem modified time via `std::fs::metadata(...).modified()` — convert to UTC with `chrono` or `time`, and do this *before* you rename or rewrite the file, since renaming doesn't change mtime but you want to read metadata cleanly once; (3) read the raw bytes and decode with `encoding_rs::WINDOWS_1252.decode(&bytes)` to get a UTF-8 `Cow<str>` — don't use `String::from_utf8` since the source bytes aren't valid UTF-8 to begin with; (4) compute the new filename as `{YYYY-MM-DD}_{original_name}` in the *same directory*, write the decoded UTF-8 content to the new path, then remove the old file (or rename first, then overwrite — either order works as long as the old path ends up gone and the new one holds UTF-8 content); (5) append a CSV line `orig_abs_path,new_abs_path` per file, no header. Use buffered readers/writers so large files don't blow memory. Run the binary and diff the resulting filenames and `migration_log.csv` against what the directory walk actually produced.