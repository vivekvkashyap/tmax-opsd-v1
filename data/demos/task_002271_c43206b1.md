Treat `/app/score_features` purely as a black-box scorer you invoke to check your work, not something to inspect or reverse-engineer. In `pipeline.py`: load `/home/user/raw_data.csv` (no header) with pandas, impute missing values with `SimpleImputer(strategy='mean')` since the data has scattered NaNs, then standardize with `StandardScaler()` — scaling before dimensionality reduction is essential because PCA is variance-sensitive and unscaled features would dominate the components. Reduce to exactly 10 components with `PCA(n_components=10)` (SelectKBest is also viable but PCA typically captures the informative signal well here), then write the transformed 1000x10 array to `/home/user/final_features.csv` with `header=False, index=False` and confirm row/column counts match exactly. Run `/app/score_features /home/user/final_features.csv` yourself and read the printed accuracy — if it's below 0.85, iterate: try scaling variants, more components temporarily to sanity-check signal, or feature selection instead of PCA, before locking in the final pipeline. Make sure the script is fully deterministic (fixed random_state) so repeated runs reproduce the same score.
