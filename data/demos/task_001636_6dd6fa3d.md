First reproduce the reference metadata deterministically: run `ffprobe -v error -select_streams v:0 -show_entries packet=pts_time,size -of csv=p=0 /app/experiment_video.mp4` to see the actual (timestamp, size) pairs available for the video, then decide whether your C program parses this at runtime via `popen`/a preprocessing step, or you bake a generated array into the C source/an auxiliary data file loaded at startup — either way the array must be sorted by timestamp before binary search is valid. Implement the core lookup as: binary search for the largest pts_time <= T (i.e., an upper-bound style search, not exact match), output `NaN` only when T is strictly less than the first timestamp, and clamp to the last frame when T is at or past the last timestamp — get these two boundary conditions exactly right since a fuzzer will hammer edge values. Match the required output format precisely: `%.4f -> %d\n` or `%.4f -> NaN\n`, and call `fflush(stdout)` after every line since the harness feeds queries interactively over stdin/reads stdout incrementally. Compile with `gcc -O3 /home/user/fast_query.c -o /home/user/fast_query`, then write your own quick differential test (compare a handful of manually reasoned edge-case queries against your understanding of the spec) before trusting the fuzz equivalence check, since you cannot see the oracle binary's exact behavior directly — infer correctness from the spec, not from probing the oracle.
