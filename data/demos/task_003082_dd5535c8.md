Build this as a straight-line pandas/sklearn/scipy pipeline, matching every stated hyperparameter exactly since a numerical mismatch cascades through every later step. Load the CSV; fit `TfidfVectorizer(stop_words='english')` (defaults otherwise) on the `text` column, then `TruncatedSVD(n_components=5, random_state=42)` on the TF-IDF matrix to get row embeddings — the random_state matters for reproducibility, don't omit it. Compute the centroid as the elementwise mean of embeddings restricted to rows where `category == "Product"`. For every row (all categories), compute cosine similarity of its embedding vector against that single centroid vector (`sklearn.metrics.pairwise.cosine_similarity`), round to 4 decimals, and write the augmented dataframe to `/home/user/processed_feedback.csv`. Filter to `category == "Support"`, sort by `sim_to_product` descending, take the top 5 `id`s, and write one per line. For the hypothesis test, run `scipy.stats.ttest_ind(product_sims, support_sims, equal_var=False)` (Welch's), then separately compute the CI for the mean difference using the Welch-Satterthwaite degrees of freedom formula (not a simple pooled-variance CI) — round all four stat values to 4 decimals in the JSON.
