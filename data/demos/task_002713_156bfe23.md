Start by OCR'ing `/app/policy_spec.png` (e.g. `tesseract /app/policy_spec.png -`) to recover the exact custom auth header name and the HS256 JWT secret key printed in the image — read the raw output carefully, since OCR can mangle punctuation/case, and cross-check the recovered strings look sane before hardcoding them into your script. Then build `/home/user/policy_validator.py` as a pure classifier: load the JSON request, check the header exists and matches `Bearer <token>` format, verify the JWT with PyJWT using `algorithms=["HS256"]` and the recovered secret (catch `InvalidSignatureError`/decode failures as BLOCK, don't let exceptions crash the script), confirm the decoded payload's `role` claim equals `uploader`, then separately check `upload_path` for `../` and its case-insensitive URL-encoded form `%2e%2e%2f` (decode or regex both raw and lowercased), and check `upload_filename` case-insensitively for `<script>` and `onload=`. Any single violation must yield BLOCK; only a request passing all checks is ALLOW. Print exactly `ALLOW` or `BLOCK` with no extra text, exit 0 always. Iterate against `/app/corpus/clean/` and `/app/corpus/evil/`, running your script over every file and diffing actual vs. expected classification until both corpora are 100% correct — this is the actual pass/fail gate.
