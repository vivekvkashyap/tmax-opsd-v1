Don't try to fix the singular 3x3 linear system in `solve_mixture.py` — the task explicitly tells you to discard the redundant motif and solve a reduced 1D problem by hand: set up the single linear equation `w_A * P(A) + (1-w_A) * P(B) = observed_GC` using the two remaining GC-content probabilities and the given observed fraction, then solve algebraically for `w_A` (simple substitution, no numerical solver needed — verify your algebra by plugging the result back in). For `simulate.sh`, take `w_A` as `$1` and loop 100,000 times in awk or bash using `$RANDOM` (scaled to [0,1) via e.g. `$RANDOM/32768`) to first pick motif A vs B with probability `w_A`, then flip a second weighted coin using that motif's GC probability to decide G/C vs A/T; tally GC count over the run and print `gc_count/100000` as the simulated fraction — awk will be far faster than a pure bash loop for 100k iterations, consider using it for the inner random draws too. Compute `abs(simulated - 0.40)` and write both required lines to `/home/user/results.txt`, then `chmod +x simulate.sh` and run it once yourself to confirm the printed difference is small and the script doesn't error.
