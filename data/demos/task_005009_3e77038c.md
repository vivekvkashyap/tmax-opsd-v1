First isolate the parser bug in isolation: grep `/app/bash-json-parser/parse.sh` for any conditional that matches a `\uXXXX`-style regex and bails out (likely an `[[ ... =~ \\u[0-9a-fA-F]{4} ]]` guard followed by `exit`/`return`); remove or neutralize just that guard without touching the surrounding extraction logic, then confirm the parser now returns success on a sample line containing `1`. Second, build `/home/user/process_configs.sh` to read stdin line by line, call into the fixed parser to pull `metric` and `value`, and skip any line where `metric` isn't exactly `memory_weight`. Third, maintain the rolling window with a bash array: push each new value, and once the array exceeds 5 elements drop the oldest (`window=("${window[@]:1}")` after appending, or slice to the last 5) — compute the average with integer arithmetic (`sum / ${#window[@]}`) using bash's native floor division, and print it immediately after each accepted event. Mark the script executable with `chmod +x`. Test against constructed inputs with and without unicode escapes and with interleaved metrics before considering it done, since a fuzzer will check exact output over many random streams.
