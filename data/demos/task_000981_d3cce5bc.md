First install/import `networkx`, load the `referrals` table into a `DiGraph` using `referrer_id -> referred_id` edges, then use BFS (e.g. `nx.single_source_shortest_path_length(G, source=1)`) filtered to distance <= 2 to get your target user set — do not hardcode the list. Next, open `etl_pipeline.py` and fix the buggy query: the comma-joined `FROM users u, transactions t` with a `WHERE u.id IN (...)` is an implicit cross join with no join condition on `t.user_id`, which inflates sums; add an explicit `JOIN transactions t ON t.user_id = u.id` (INNER JOIN, so users with zero transactions are dropped). Replace the f-string-built `IN ({user_ids_str})` with a parameterized query using one `?` placeholder per user id (e.g. `IN ({','.join('?'*len(target_users))})`) and pass `target_users` as the params tuple/list to `cursor.execute` — never interpolate values into the SQL string. Round `SUM(t.amount)` to 2 decimals in Python, sort the output list by `user_id` ascending, then write it as JSON to `/home/user/final_report.json` and run the script to confirm it executes cleanly.