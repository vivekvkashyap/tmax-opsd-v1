The core hazard is directory traversal safety, not the encoding conversion. Use `opendir`/`readdir` on `/home/user/dataset/` and, for each entry, call `lstat` (not `stat`) to check the type — only process entries where `S_ISREG(st_mode)` is true; explicitly skip anything where `lstat` reports a symlink (`S_ISLNK`) or a directory, so the self-referential symlink loop is never followed (do not use `stat` alone, which would dereference the symlink and recurse forever). Collect matching `*.dat` filenames into an array, sort them alphabetically with `qsort` and `strcmp`, then for each file: read its raw bytes, convert UTF-16LE to UTF-8 (either with `iconv()` from `<iconv.h>` or by manually decoding UTF-16LE code units), and append the result to `/home/user/merged_output.txt`. Before each chunk's write, open the output file, call `flock(fd, LOCK_EX)`, write, then `flock(fd, LOCK_UN)` and close — this must appear literally in the source using `<sys/file.h>`. Compile with gcc, run the binary once, and confirm the merged file's line order matches the alphabetical chunk order.