First diagnose the vendored package by inspecting `/app/vendor/bayes_classifier/Makefile` and `bin/extract_features.sh` line by line for hardcoded paths — look for a mistyped interpreter path (e.g. a Python binary name that isn't actually `python3`) and a wrong hardcoded path to the inference script the shell script calls. Fix both, then run `make install` inside that directory and confirm it works with a sample invocation of `./bin/bayes_score --input '...'` before moving on — don't assume the fix worked without testing it. For feature engineering, write a small script that reads `raw_datasets.csv`, computes `time_since_creation` as whole days between each row's creation date and 2023-01-01, and buckets `size_category` using the 1MB/100MB thresholds from the spec, writing the result to `engineered_datasets.csv`. For the API, write a minimal HTTP server (stdlib `http.server` is fine) bound to `127.0.0.1:8080` with a `/score` POST handler that rejects requests missing `Authorization: Bearer secret-research-token` (401), otherwise shells out to the fixed `bayes_score` CLI with the request body and returns `{"score": <float>}`. Launch it backgrounded with output redirected to `/home/user/server.log`, and verify it stays up after your shell exits (e.g. `nohup`/`disown`).
