First fix the vendored library: open `/app/vendored/artparse/parser.go` and trace the state machine's `MERGE` handling — check whether it transitions state back to idle after processing; a missing state-reset is the classic cause of an infinite loop. Add the missing transition and quickly test-parse a `MERGE` string to confirm it returns. Then build `/app/artifact-filter/main.go`: import the fixed `artparse`, parse the input, and implement two detectors — (a) cycle detection over the `MERGE` dependency graph (DFS with visiting/visited sets, catching self-loops too) and (b) for each `LINK` target, reject if it's absolute or if resolving `..` segments relative to the build directory would escape it (normalize the path rather than just string-matching `../`, since traversal can be disguised). Treat any parse failure as REJECT. Print exactly `ACCEPT` or `REJECT` with no other stdout output, and test against your own circular and path-traversal examples before relying on the adversarial corpus.
