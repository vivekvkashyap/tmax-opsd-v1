Work the three parts in order, verifying each before moving on. First, open `/app/vendored/datacleaner-1.0.0/datacleaner/core.py` and fix `prepare_and_split`: call `train_test_split` on the raw `X`/`y` first, then instantiate a fresh `StandardScaler`, `fit_transform` it only on `X_train`, and `transform` (not fit) `X_test` with that same fitted scaler — fitting the scaler before splitting leaks test-set statistics into training, which is exactly the bug to remove. Return the 5-tuple including the fitted scaler. Reinstall the package into your environment (e.g. `pip install -e /app/vendored/datacleaner-1.0.0` or reinstall from the modified source) so the fix actually takes effect, then reload/restart any Python process using it — editing the file alone won't update an already-installed copy. Second, write a training script that loads the CSV, calls the fixed function, trains `RandomForestClassifier(random_state=42)`, pickles the model and scaler to the exact paths given, and times 100 sequential predict calls over the test set to populate `metrics.json`. Third, build the Flask/FastAPI service on 127.0.0.1:8080, checking the `Authorization` header exactly before doing any scaling/prediction work, returning 401 on any mismatch, and leave the process running in the background (e.g. with nohup or a daemon start) rather than foreground-blocking your session.