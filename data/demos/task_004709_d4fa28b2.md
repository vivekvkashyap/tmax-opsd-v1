Treat this as a numerics bug plus a filter-logic bug plus a deployment step, in that order. For the variance calculation, replace the naive `E[X^2] - (E[X])^2` (which catastrophically cancels in `f32`) with either upgrading the accumulators to `f64` or, better, implementing Welford's online algorithm (track running mean and M2, update per sample as `delta = x - mean; mean += delta/n; delta2 = x - mean; M2 += delta*delta2;` variance `= M2/n`), so it never goes negative or produces NaN under sqrt — this is what the `/api/variance` endpoint must expose per-frame cumulatively. For the smoothing routine, check the update rule/learning-rate for stability (e.g. an EMA-style `smoothed = alpha*val + (1-alpha)*smoothed` with 0<alpha<1, not something that diverges). For `/api/alerts`, read the filtering code carefully: it likely uses `>=` instead of strict `>` and/or indexes into the wrong (un-smoothed) array — fix both so it returns frames where the *smoothed* metric strictly exceeds the threshold. Enforce the bearer-token check on every route before other logic, returning 401 otherwise. Extract real frames from `/app/telemetry_feed.mp4` via `ffmpeg` to test your fixes locally, then `cargo run --release &` so port 8080 stays serving in the background.