Work bottom-up: first get correctness, then throughput. In `vm.go`, find where goroutines write to a shared slice/map concurrently when handling the batch — protect it with a `sync.Mutex` or, better, have each goroutine write to its own pre-sized slot (indexed by position) so no lock is needed on the hot path; verify with `go build -race`. Export the C function correctly (`//export`, `import "C"`, build with `go build -buildmode=c-shared -o /app/lib/libvm.so`). In `ffi.py`, set `restype = ctypes.c_char_p` (or `c_void_p` if you must free it) and correct `argtypes` to match the Go signature exactly — a mismatch here is what causes segfaults. Since cgo returns a `malloc`'d C string, capture it, copy it to a Python string immediately, then call the Go-exported free function (or `libc.free`) on the pointer to avoid leaking memory under load. Fix the Nginx `location /api/` block to `proxy_pass http://127.0.0.1:5000/;`. Add a Redis cache in `app.py`: hash the batch request, check Redis first, else compute and `SETEX` with 60s TTL. Restart all three services, then run `/app/benchmark.py` and iterate — leaks or lock contention will show up directly as RPS below 800.
