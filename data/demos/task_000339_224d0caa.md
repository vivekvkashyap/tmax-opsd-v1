Start from the crash artifact, not the source tree: load `/home/user/crash.core` in gdb against the compiled `steg_decoder` to get a backtrace and pinpoint the faulting function/array access — this tells you which file in `/app/lib-steg-transform` (likely the FFT/transform code and the payload-extraction code that scales a float and casts to int for an array index) to focus on. Then do a real `git bisect` in that repo: mark a known-good early commit as good and the current HEAD as bad, building and testing at each step with the actual trigger payload until git narrows it to one commit; write that 40-char SHA to `/home/user/bad_commit.txt`. Inspect that commit's diff for a type narrowing (e.g. a variable changed from double to float) that degrades precision enough to push a rounding/cast across an array-bounds boundary — restore full double precision rather than patching the array size, since the fix must reproduce exact pre-regression floating-point behavior. Rebuild `libsteg.a` via the Makefile, then `g++` link `/home/user/steg_decoder.cpp` against it to produce `/home/user/steg_decoder_fixed`, and confirm the crash payload no longer segfaults before trusting the fuzz-equivalence check.
