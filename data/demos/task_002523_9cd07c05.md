Build the database with `sqlite3 /home/user/ecommerce.db` using `.mode csv` and `.import --skip 1 <file> <table>` for each CSV (letting sqlite infer types, or pre-create tables with explicit column types if you want price/quantity as numeric rather than text). After import, run `CREATE INDEX idx_transactions_date ON transactions(transaction_date);` and similarly for `idx_products_category` and `idx_transactions_customer`. For the report, write `analyze.sh` as a script that runs a single SQL query joining `transactions` -> `products` -> `customers`, filtering `category='Electronics'` and `transaction_date BETWEEN '2023-01-01' AND '2023-12-31'`, grouping by `segment`, summing `quantity*price` as `total_revenue`, and ordering by that sum descending. Use `sqlite3 -json /home/user/ecommerce.db "<query>"` to emit valid JSON directly rather than hand-building strings — this avoids quoting/escaping bugs and keeps numeric types as numbers (not strings). Redirect the output to `/home/user/report.json`, then `chmod +x /home/user/analyze.sh`. Double-check the JSON keys are exactly `segment` and `total_revenue`, and verify the script runs cleanly with no arguments from a fresh shell before finishing.
