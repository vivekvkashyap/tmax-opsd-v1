In `generate_restore.py`, connect to `/home/user/backups.db` with `sqlite3` and use parameterized queries (`?` placeholders) to avoid injection and correctly quote text values. First query for the backup with the given `db_name` and the maximum `ts` that is `<= target_ts` (`ORDER BY ts DESC LIMIT 1`). Then walk backward: starting from that row, repeatedly look up `parent_id` (following the chain) until you hit a row with `backup_type = 'full'` (empty `parent_id`), collecting each id/type along the way. Reverse the collected chain so the root full backup comes first, then each incremental in dependency order — this ordering is the part naive attempts get backward, since you discover the chain tail-to-head but must emit it head-to-tail. Write `/home/user/restore.sh` starting with `#!/bin/bash`, one `restore_cmd --db <db_name> --id <id> --type <backup_type>` line per backup in that order, and nothing else — no comments, echoes, or blank lines. Then `chmod +x` it, and actually run `python3 generate_restore.py db_prod 1025` as the task requires so the file gets produced.