Build this bottom-up, verifying each layer before wiring the next. First, tackle DTMF decoding in isolation: write a small Go test harness that reads the WAV's raw PCM samples, splits them into fixed-size chunks (e.g. matching standard DTMF tone/gap durations), and runs the Goertzel algorithm per chunk to detect the strongest low-frequency and high-frequency pair (697/770/852/941 Hz vs 1209/1336/1477/1633 Hz), mapping pairs to digits/letters via the standard DTMF keypad table. Do the chunk processing with goroutines + a WaitGroup (or worker pool) so segments are evaluated concurrently, then reassemble results in original chunk order — do not just decode serially and call it "concurrent". Collapse repeated/silent detections into the final symbol string, translate A/B/C/D to +/-/*//, and evaluate the arithmetic expression respecting operator semantics. Expose this as a cgo `//export EvaluateAudio` function returning a malloc'd C string; free it correctly on the Rust side via `CString::from_raw` after copying, to avoid leaks/UB across the FFI boundary. For Rust, use `build.rs` to link `-laudio` against the compiled `.so` (set rpath or `LD_LIBRARY_PATH` in build.sh), then implement the HTTP route with Bearer-token middleware (401 on missing/wrong token) and a separate gRPC service from a `.proto` definition, both calling the same FFI function. Test HTTP and gRPC independently before combining in build.sh, which must build Go, build Rust, then launch the server in the background.
