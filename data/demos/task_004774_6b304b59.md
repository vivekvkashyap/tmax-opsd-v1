Separate the build-fix, the regression hunt, and the algorithm fix into distinct steps. First get `/home/user/fxhash_repo` compiling: inspect `go.mod` for a bad `replace` directive or unresolvable module version (likely pointing at a missing local path or nonexistent version) and remove/correct it so `go build ./...` succeeds. Next, don't guess at the bug — build a small wrapper script that feeds identical random inputs to both the current `main` binary and `/app/fxhash_legacy` and diffs their stdout, then use `git bisect run <script>` (or a manual binary search across commit history) to pinpoint exactly which commit's diff changed the hashing behavior; read that commit's diff to see what changed. Given the ticket's own hint about "integer boundaries or types," scrutinize the accumulation/index arithmetic inside the hashing loop for any type that could silently truncate or wrap on large inputs (e.g. a variable narrowed to a smaller unsigned width somewhere along the pipeline) — the "breaks only for larger files" symptom is the tell that some intermediate value overflows once inputs get big enough, so test with a range of input sizes to bracket exactly where it starts diverging from the oracle. Fix the underlying type/arithmetic so it can't wrap within the tested input range, rebuild, and re-diff against the oracle across small and large inputs before finalizing. Compile the corrected code to `/home/user/fxhash_fixed`; it will be fuzz-compared against `/app/fxhash_legacy` for exact stdout equivalence.