Read `/home/user/reviews_raw.jsonl` line by line with `json.loads`, and for each record compute a normalized-comparison key by applying, in order: `unicodedata.normalize('NFC', text)`, `.strip()`, `.lower()` — group records by `(user, normalized_text)`. Within each group pick the record with the earliest `timestamp` (ISO-8601 strings with a trailing `Z` sort correctly as plain strings, but comparing via `datetime.fromisoformat` after replacing `Z` with `+00:00` is safer); break ties by the lexicographically smallest `id`. Crucially, when writing the surviving record to the output, emit its *original* unmodified `text` field, not the normalized version — the normalization is only a dedup key, never mutate the stored data. Sort the surviving records by `(timestamp, id)` ascending before writing `/home/user/reviews_clean.jsonl`, one compact JSON object per line matching the original key order/keys. Count total input lines, output lines, and `duplicates = total_in - total_out`, then write `/home/user/report.txt` matching the exact template text and field order given in the task, with no extra blank lines.