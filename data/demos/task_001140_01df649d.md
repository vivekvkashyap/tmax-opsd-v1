Write `/home/user/process_data.py` to load both CSVs with pandas, restrict/rename to columns `trial_id, signal_x, signal_y`, drop rows with any NaN via `dropna()`, and cast `trial_id` to int and the two signal columns to float. Compute sample covariance and Pearson correlation with `df['signal_x'].cov(df['signal_y'])` (pandas defaults to ddof=1, matching the "sample covariance" requirement) and `df['signal_x'].corr(df['signal_y'])` for each cleaned dataset — do not compute these before dropping NaNs, and do not use numpy's `cov`/`corrcoef` without checking their ddof/NaN handling since they differ from pandas defaults. Round each covariance/correlation to exactly 4 decimal places with Python's `round()`, count valid rows per dataset after cleaning, and write all six values into `/home/user/validation_report.json` with exactly the specified key names. Run the script (`python3 /home/user/process_data.py`) so the report file actually exists on disk, and double check `pip install pandas numpy` succeeded if not already present.
