Write `calc_llr.cpp` to read `/home/user/sequences.txt` line by line into a vector of strings first (so the OpenMP loop below can index by position safely), then parallelize over sequences with `#pragma omp parallel for`, accumulating a shared significant-count via `#pragma omp atomic` or a per-thread reduction (`reduction(+:count)` is simplest and avoids races). For each sequence, sum per-base log-likelihood-ratio terms using `std::log` of the two model probabilities per base (precompute the four log-ratio constants for A/C/G/T once outside the loop rather than recomputing `log()` per character, for speed) — do this in log-space exactly as specified, not by multiplying raw probabilities (that underflows for length-50 sequences). Compare each sequence's total LLR to `5.0` with strict `>`. Compile exactly with `g++ -O3 -fopenmp /home/user/calc_llr.cpp -o /home/user/calc_llr`, run it, and write only the final integer count (no trailing text) to `/home/user/significant_count.txt`. Double check the file has no extra newline/whitespace issues since verification does an exact diff against a precomputed expected count.
