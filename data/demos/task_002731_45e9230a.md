Split this into two independent pieces. First, transcribe `/app/target_dist.wav` (e.g. with `whisper`, `ffmpeg`+a speech-to-text tool, or any ASR available) to recover the 10 spoken digits in order, then normalize them by their sum to build the fixed target distribution Q — do this once, not inside the program. Second, build `/home/user/analyze_signal` as a script (Python is easiest) that: takes the single comma-separated-floats argument, runs `numpy.fft.fft` (unscaled, no normalization flag) on the 128 values, takes `abs()` of the first 10 output bins as M, builds a 10x10 symmetric Toeplitz matrix from M via `scipy.linalg.toeplitz(M)`, runs `numpy.linalg.svd`/`scipy.linalg.svd` to get the 10 singular values, normalizes them to P, then computes Jensen-Shannon divergence against Q using natural log. Since verification is exact fuzz-equivalence against a hidden reference, get every numeric detail right: don't scale/normalize the FFT, use `scipy.spatial.distance.jensenshannon(P, Q, base=np.e)` and remember it returns the sqrt of the divergence — you must square it to get JSD. Print only the 6-decimal-place value, no extra text. Make the file executable and test with several hand-picked and random 128-float inputs before relying on it.