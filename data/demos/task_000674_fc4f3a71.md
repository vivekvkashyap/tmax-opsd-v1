Parse `/home/user/access.log` line by line (combined log format with a quoted trailing field for User-Agent), using a regex that captures the leading IP, the HTTP status code, and the final quoted field. Filter to only lines where the status code is exactly `500`. For each match, base64-decode the User-Agent field (`base64.b64decode`) to recover PEM certificate bytes, then load it with `cryptography.x509.load_pem_x509_certificate` and pull the Common Name via `cert.subject.get_attributes_for_oid(NameOID.COMMON_NAME)[0].value`. Watch for the trap: not every log line is a 500, and not every 500 necessarily has a valid cert — only process lines that both match the 500 filter and decode/parse successfully, and preserve log order in the output. Write each result as `IP: CN` on its own line to `/home/user/extracted_cns.txt`, with no extra formatting or blank lines, then run the script to produce the file.
