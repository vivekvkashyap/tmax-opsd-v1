Work directly in SQLite via a Python script, not ad-hoc shell queries, since the deliverable is `/home/user/audit.py` producing a JSON file. First inspect the schema (`sqlite3 /home/user/audit_data.db .schema`) to confirm column names match the task description. Create indexes on the join/filter keys before running the analytical query — e.g. on `employees(emp_id)`, `access_logs(emp_id, sys_id)`, `systems(sys_id)`, and `exceptions(emp_id, sys_id)` — using `CREATE INDEX IF NOT EXISTS ...`; the verifier checks `sqlite_master` for these. Then write a single SQL query that joins `access_logs` to `employees` and `systems`, filters where `employees.department_id != systems.owning_dept_id`, and uses `NOT EXISTS`/`LEFT JOIN ... IS NULL` against `exceptions` on the exact `(emp_id, sys_id)` pair to exclude granted exceptions — a common mistake is joining exceptions on `emp_id` alone or ignoring that exceptions must match both columns. Group by `emp_id`, count violations, filter to count > 0, and sort by `violation_count DESC, emp_id ASC`. Serialize the result with `json.dump` preserving key order `emp_id, name, violation_count`, then actually execute the script so the output file exists before finishing.