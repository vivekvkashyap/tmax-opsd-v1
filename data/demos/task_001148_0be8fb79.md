Solve this entirely in SQL inside SQLite, not by post-processing in a script. Use a recursive CTE anchored at `emp_id = 1` that joins `org_chart` on `manager_id = s.emp_id` to walk down the tree and collect the full subtree (this correctly excludes siblings/other branches like a parallel VP). Join that subtree against `events`, then use SQLite's `json_extract(payload, '$.type')`, `'$.amount'`, `'$.timestamp'` to pull fields out of the JSON text column — filter strictly on `type = 'sale'` before computing anything else. For the cumulative sum, use a window function `SUM(sale_amount) OVER (PARTITION BY emp_id ORDER BY event_timestamp ASC)` rather than a manual loop; this naturally handles per-employee running totals in timestamp order. Save the query text to `/home/user/query.sql`, then execute it via `sqlite3 -header -csv /home/user/company.db < /home/user/query.sql > /home/user/report.csv` (or `.mode csv`/`.headers on` inside a `.read` session) and finally sort explicitly by `emp_id ASC, event_timestamp ASC` as the outer ORDER BY. Watch for JSON numeric fields coming back as TEXT vs numeric — cast with `+0` or `CAST(... AS INTEGER)` if needed so the SUM window function computes correctly rather than doing string concatenation.
