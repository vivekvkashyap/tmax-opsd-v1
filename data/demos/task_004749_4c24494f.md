Build and test the C converter on one file before wiring the shell orchestration. In `transformer.c`, use zlib's `gzopen`/`gzread`/`gzwrite` (or `gzgetc`/`gzprintf`) to stream directly between compressed files rather than fully decompressing to disk first. For encoding conversion, use `iconv_open("UTF-8", "ISO-8859-1")` and `iconv()` to transcode each chunk of text you read — do this on raw bytes, not on already-mis-decoded strings. For the markup rewrite, scan the decoded text for the literal substrings `<<TITLE>>` and `<</TITLE>>`, replacing the former with `# ` and simply deleting the latter (a plain string search/replace on the buffer works fine at this file scale). In `process.sh`: compile with `gcc transformer.c -lz -o transformer` (link zlib explicitly), `tar -xf old_manuals.tar` to get the individual `.txt.gz` files, loop over each with your compiled binary to produce matching `.md.gz` names (e.g. `chapter1.txt.gz` → `chapter1.md.gz`), and finally `zip` only those newly generated `.md.gz` files (not the original `.txt.gz` ones) into `/home/user/docs_project/modern_manuals.zip`. Verify by `zcat`-ing an output file and confirming both the `#` header and correct accented-character rendering.
