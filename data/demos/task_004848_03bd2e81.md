Load `/home/user/interactions.json` and build an undirected adjacency structure (e.g. a `dict` of sets), adding an edge in both directions for every `protein -> interacts_with` pair — since the JSON only lists outgoing edges per record, a naive graph that only follows the listed direction may miss valid paths, so make sure to add the reverse edge too (and merge symmetric duplicates). Run BFS from `MAPK1` to `TP53`, tracking predecessors to reconstruct the actual path (not just the distance) once `TP53` is dequeued. Build the result dict with keys `source`, `target`, `path` (list including both endpoints), and `distance` (edge count = `len(path)-1`). Install and use `jsonschema.validate()` against `/home/user/schema.json` before writing — note the schema has `"additionalProperties": false`, so include exactly those four keys and nothing else. Write the validated dict to `/home/user/shortest_path.json` and execute the script so the file is produced.
