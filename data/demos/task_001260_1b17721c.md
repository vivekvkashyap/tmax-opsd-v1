Work each evidence source independently before combining. (1) Parse `/app/auth.log` with grep/awk for the IP that appears in many consecutive "Failed password" lines followed by "Accepted password for admin" — that repeated-offender IP is the attacker IP, not just any accepted login. (2) Run `tesseract /app/evidence.png -` (or preprocess with PIL if OCR is noisy) to recover the `MASTER_KEY=...` string exactly as printed. (3) Compute sha256 of each file in `/app/bin/` and diff against `/app/manifest.sha256` to find the one mismatched binary. (4) In `/home/user/recover.py`, for each line of `/app/obfuscated_records.txt`: base64-decode, json.loads, recompute `hmac.new(key.encode(), f"{ip}|{timestamp}|{event}".encode(), hashlib.sha256).hexdigest()[:10]` and compare to the record's `signature` field — keep only matches. Tag `compromised` by comparing each record's ip to the attacker IP from step 1. Write the valid records as a JSON array to `/home/user/recovered_logs.json` with exactly the four required keys per object.