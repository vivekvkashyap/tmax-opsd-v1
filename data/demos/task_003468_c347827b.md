Structure this as a Rust CLI with two subcommands sharing CSV plumbing (the `csv`+`serde` crates), and get each subcommand fully correct against your own small test files before relying on the hidden corpora. For `validate`: OCR the rule image with `tesseract /app/anomaly_rules.png stdout`, read the extracted rule text carefully (it will state a numeric threshold and comparison, e.g. an age cutoff and an amount cutoff joined by AND) and translate it into a literal boolean condition in code — do not hardcode a rule you haven't actually verified against the OCR output. Iterate rows; if any row satisfies the poison condition, print "REJECT" and `std::process::exit(1)` immediately; otherwise after the full scan print "ACCEPT" and exit 0. For `engineer`: read all rows once, filter to `split_type == "train"` to compute mean and **sample** standard deviation (divide by N-1, matching the worked example in the task) per numeric column, then iterate over *all* rows (train and test) applying `(x - train_mean) / train_std` using those train-only statistics — the classic bug to avoid is accidentally computing statistics over the whole dataset, which leaks test-set information. Format outputs to 4 decimal places, preserve original column and row order, and write with the `csv` crate. Build with `cargo build --release` and confirm both subcommands work on your own synthetic CSVs before finishing.