Split the pipeline cleanly between shell preprocessing and C-side resampling logic. In Bash/awk: filter rows where the Status field is exactly "OK" (watch out for near-matches like "OK " or codes containing "OK"), then sort by Location then Timestamp — since Location can contain multi-byte UTF-8, use a byte-wise/C-locale sort (`LC_ALL=C sort`) so ordering matches "raw byte values" as required, not a locale-aware Unicode collation. Feed this cleaned, sorted TSV into your C program via stdin or a temp file. In C: for each location group, bucket timestamps by `(timestamp / 3600) * 3600`, accumulating sum+count per bucket to compute the hourly average; after building the sparse hourly series for a location, walk from its earliest to latest hour in 3600-second steps and forward-fill any bucket with no readings using the last known average — track "last valid value" as you iterate rather than backfilling after the fact. Format temperatures with exactly two decimals (`printf("%.2f", ...)`) and write `Location\tTimestamp\tTemperature` lines. Wrap compilation (`gcc`) and execution of the whole chain in `run_pipeline.sh`, and diff your output against your own hand-traced expectation for the sample data before trusting it.
