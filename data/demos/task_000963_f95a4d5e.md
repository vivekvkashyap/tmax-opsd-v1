Before writing the automation, manually run `python3 /home/user/mock_router.py` and type commands by hand to understand the exact prompt string (`Router> `), the login flow, and the `OK: `/`Login successful.` response text — this tells you what `pexpect.expect()` patterns to anchor on. In `/home/user/restore_tool.py`: `pip install pexpect` if missing, `pexpect.spawn('python3 /home/user/mock_router.py')`, then `expect('Router> ')` before every `sendline`, starting with `login admin admin`. Load `/home/user/backup_data.json` with the `json` module and iterate its `users`, `interfaces`, and `routes` keys, sending one command per entry using the exact command grammar given in the task (`user add <username> <group>`, `interface set <iface> <ip_cidr>`, `route add <dest> <gw>`), expecting the prompt again after each before sending the next — sending commands too fast without waiting for the prompt is the most common way this breaks. After all commands, send `dump`, capture the JSON line from `child.before`/`child.after` (strip trailing whitespace, avoid also capturing the echoed prompt), write it verbatim to `/home/user/final_state.json`, then send `exit` and call `child.wait()` to close cleanly.
