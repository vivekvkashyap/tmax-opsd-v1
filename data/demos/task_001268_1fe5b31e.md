Start by reproducing the crash: write a tiny script that reads packets out of `/app/diagnostic_capture.pcap` (e.g. with `scapy` or manual byte parsing) and feeds each payload into `custom_net_parser.parse()`, then use `pdb.set_trace()` or `python3 -m pdb` to step into the failure and inspect the exact struct-unpack/slice call that misbehaves. Compare the buggy unpack against the protocol's actual length-field width — a length byte read as 1 byte (`'>B'`) instead of 2 bytes (`'>H'`) will silently truncate large lengths and misalign later slicing/checksum reads; fix the format string and any slice offsets that depend on it, without changing unrelated parsing logic. After fixing `/app/custom_net_parser/core.py`, write `/home/user/test_regression.py` to iterate every packet in the pcap and assert `parse()` doesn't raise. Then write `/home/user/parse_runner.py` to accept one hex-string CLI arg, `bytes.fromhex()` it, call the patched `parse()`, and `json.dumps()` the result to stdout — pay close attention to exact key names, key order (if the oracle's JSON serialization is order-sensitive), number formatting, and whether byte/hex fields should render as ints vs hex strings, since the fuzz-equivalence check requires byte-for-byte identical stdout versus the oracle across thousands of random inputs, not just semantically similar output.
