Split this into three independently checkable stages so a mistake in one doesn't corrupt the others. First, parse `/home/user/manifest.raw` with `awk -F'|'` (trim whitespace around fields) to select rows whose `Status` field is exactly `NEEDS_PATCH`, writing just the artifact names to `/home/user/targets.txt` — verify the list by eyeballing it against the raw file before touching any binaries. Second, in the C patcher, `mmap` each target file with `PROT_READ|PROT_WRITE, MAP_SHARED`, then scan byte-by-byte (not via string functions, since the signature/replacement are raw bytes that could include embedded nulls) for the 8-byte marker, overwriting matches in place with 8 zero bytes; make sure your scan handles multiple non-overlapping occurrences in one file (advance the search index past each match) and that you `msync`/`munmap` so writes are flushed before the file is read again. Only operate on files listed in `targets.txt`, resolved under `/home/user/artifacts/`, never touch unlisted files. Third, for the incremental backup, reuse the *existing* snapshot file exactly as given: `tar --listed-incremental=/home/user/backup_snapshot.snar -cvf /home/user/patch_incremental.tar -C /home/user artifacts` (or equivalent), so tar detects only the mtimes/content that changed since the base snapshot; then list its contents into `/home/user/backup_contents.txt`.