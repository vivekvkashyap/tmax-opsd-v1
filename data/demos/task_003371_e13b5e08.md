Write the C program with a manual CSV line reader (fgets + strtok or manual scanning) since no external libraries are allowed, and skip the header row explicitly. For each data row, validate strictly and independently per field: id and timestamp must parse fully as positive integers (check that the entire field converts, not just a prefix — use strtol and verify the end pointer consumed the whole token), db_name must match `[A-Za-z0-9_]{1,50}` exactly (reject any hyphen or other punctuation), status must be exactly "SUCCESS" or "FAILED" (case-sensitive, exact match not substring), and size_bytes must parse as a valid integer. Any schema violation on any field means skip the whole row silently. After schema validation, apply the business filter: keep only rows with status == "SUCCESS" and size_bytes > 0 (this excludes negative sizes even though they're schema-valid integers). For each surviving row, hand-build the JSON object matching the exact static sql_param_query string and bind args as string representations of the four raw field values, escaping care not needed since inputs are simple. Write a valid JSON array (commas between objects, no trailing comma) to the output path, then compile with gcc and run it to produce the file before considering the task done.
