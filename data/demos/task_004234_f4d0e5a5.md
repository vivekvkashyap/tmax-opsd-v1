Open `/home/user/backup_v1.tar.gz` with Python's `tarfile` module and build a lookup of each archived member's name to its raw bytes (via `extractfile().read()`). Walk `/home/user/project` and for each current file, compare its bytes directly against the archived version (e.g. via `hashlib.sha256` or byte comparison) — only include a file in the new archive if it's new or its content differs; identical files must be excluded entirely, so don't rely on filesystem mtimes. Handle `app.log` specially: read the archived version's lines and the current version's lines, find where the archived content ends within the current file (current should start with the same lines, then have new ones appended), and keep only the lines after that point — write those into a new in-memory buffer added to the archive as `app_diff.log`, never adding `app.log` itself. Build the new archive with `tarfile.open(..., 'w:gz')`, adding qualifying files with `tar.add()` and the log buffer with `tar.addfile()`.
