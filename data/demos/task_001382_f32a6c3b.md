Build the Rust server with only `std::net::TcpListener`: accept connections in a loop (spawn a thread per connection), read the raw request bytes until you find the `\r\n\r\n` header/body separator plus `Content-Length` bytes of body, then run your own explicit state machine over the body characters (Idle / ExpectAdd / ExpectSub / ExpectQuery) rather than a regex — on `+`/`-`/`?` transition state and require the very next non-whitespace-skipped char to be A-Z, updating a HashMap<char,i32> tally or, for `?`, pushing the current tally into a results Vec; on any invalid character or invalid transition (including lowercase), stop processing immediately but still return whatever results were collected so far. Write the HTTP/1.1 200 response by hand with the comma-joined results as the body. For Nginx, write a minimal `server { listen 8080; location /legacy { proxy_pass http://127.0.0.1:9000; add_header X-Mock-Proxy Active always; } }` config and launch both processes in the background before finishing, verifying with a manual curl POST that headers and body look right end-to-end.
