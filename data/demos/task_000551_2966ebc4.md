Build and test the numerical pipeline as a standalone function before wrapping it in an HTTP server, since floating-point correctness is what's actually being checked. Scaffold with `cargo new` and pick a lightweight async web framework (e.g. `axum` or `warp`) plus `hound` for WAV decoding and `rustfft` for the FFT; parse the WAV once at startup or per-request via `hound::WavReader`, converting i16 samples to f64. In the `/analyze` handler: compute `segment_length = N / total_segments` (integer floor division), slice out `[segment_index*segment_length, segment_index*segment_length+segment_length)`, apply the Hanning window formula exactly as given (careful with `segment_length - 1` in the denominator, and cast types consistently to f64), run `rustfft`'s forward FFT on the windowed complex buffer, and compute magnitude-squared per bin (`re*re+im*im`). Map bin index k to frequency via `k * sample_rate / segment_length`, filter bins within `[start_freq, end_freq]`, and sum their energies using explicit Kahan summation (track a running `sum` and compensation `c`, don't just use a plain `+=` loop) rather than trusting default summation order. Return `energy` and `abs(energy - reference_val)` as `difference`. Start the server in the background, then write `/home/user/service_ready.txt` only after it's actually accepting connections, and sanity-check with a manual `curl` POST before finishing.