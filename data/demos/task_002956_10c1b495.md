Set up with `cargo new cleaner` in `/home/user/cleaner`, add `rayon` (and `regex` if helpful) to `Cargo.toml`. Parse each input line by splitting on the first ` | ` to separate `Sample_ID` from the comma-separated `key: value` list, then split that list on `,` and each pair on `:`. For value cleaning, strip whitespace and any trailing/embedded `C`/`c` character before attempting `str::parse::<f64>()`; if parsing fails, skip that single measurement silently rather than erroring or defaulting to 0 — don't let one bad token abort the whole line. Use `rayon`'s parallel iterator (e.g. `par_iter` over lines) to produce per-line `Vec<(id, measurement, value)>` results, then flatten and reduce sequentially into a `HashMap<(String,String), f64>` keeping the max value seen per `(id, measurement)` key — the parallel step should only do line parsing/cleaning; do the dedup/max-aggregation as a single-threaded reduce afterward to avoid races. Sort the final entries by `id` then `measurement` (both string ordering) before writing CSV rows, formatting values with exactly one decimal place (`format!("{:.1}", v)`, which handles the `-2.0`/`0.0` sign and rounding correctly). Run the compiled binary and diff its output against your own hand-derived expectations from the known input format before considering it done.