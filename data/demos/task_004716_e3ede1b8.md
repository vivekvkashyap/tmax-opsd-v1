First fix the build: `/home/user/math_service/setup.py` compiles `algo.c` (which calls `pow` from `<math.h>`) but doesn't link libm, so add `libraries=['m']` to the `Extension(...)` call, then run `python3 setup.py build_ext --inplace` to produce the `.so`. Next author `service.proto` with the exact package/service/message names and field types specified (package `mathapp`, service `MathService`, rpc `ComputeSeries(SeriesRequest) returns (SeriesResponse)`, fields `int32 n`/`double x` and `double result`), and generate stubs with `python3 -m grpc_tools.protoc -I. --python_out=. --grpc_python_out=. service.proto` (install `grpcio-tools` first via `pip install -r requirements.txt`). Write `server.py` importing the compiled `algo_ext` module, calling `algo_ext.compute(n, x)` inside the RPC handler, and serving on port 50051 via `grpc.server(...).add_insecure_port('[::]:50051')`. Write `benchmark.py` as a gRPC client that issues 1000 sequential `ComputeSeries(n=100, x=0.5)` calls, times the loop with `time.perf_counter()`, and dumps `{"final_result": ..., "total_time_seconds": ...}` to `benchmark_results.json`. Start the server in the background, run the benchmark, then terminate the server process cleanly.