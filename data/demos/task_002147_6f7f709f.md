This will be graded by fuzzing your binary against a hidden reference for bit-exact match, so precision and boundary handling matter as much as the algorithm. First transcribe `/app/model_parameters.wav` with the `whisper` CLI to recover the numeric values of `alpha`, `beta`, and `window_size` spoken in the recording — read the transcript carefully, since spoken numbers like "two point five" must be converted to their literal decimal/integer form. Then write `/home/user/seq_score.cpp`: map each DNA character via A=1,C=2,G=3,T=4, apply `val*alpha+beta` as a double, then for each position i compute a moving average over the window `[max(0, i-window_size+1), i]` inclusive (so early positions average over fewer elements, not zero-padding), and truncate (not round) to int for output using integer cast, matching C++'s `(int)` truncation-toward-zero semantics exactly. Output space-separated integers on one line. Compile with `g++` and test manually on a few short sequences (including sequences shorter than window_size, and length up to 10,000) to sanity-check the averaging boundary logic before considering the task done, since off-by-one window bounds are the most likely source of mismatches against the hidden oracle.