Reverse-engineer the service contract from the server code rather than guessing: read /home/user/mock_server.py's imports and generated stub usage (or the servicer class methods) to learn the exact service name, RPC method(s), and message shapes, then hand-write system.proto (proto3 syntax) matching them exactly — a mismatched service/message name will make grpcurl fail even if everything else is right. For the Nginx config, remember plain HTTP/2 gRPC proxying over cleartext requires `listen 8080 http2;` and `grpc_pass grpc://127.0.0.1:50051;` (not `proxy_pass`/`http://`, which won't speak gRPC framing), with `daemon off;` at top level so it runs in the foreground when backgrounded with `&`. For the patch, first edit a copy of check_system.sh to the desired grpcurl invocation (`grpcurl -plaintext -proto /home/user/system.proto -d '{}' localhost:8080 <Service>.<Method>`), then generate the patch with `diff -u check_system.sh.orig check_system.sh.new > /home/user/update.patch` and apply via `patch` or `git apply` — verify the patch actually applies cleanly to the original file. Start the mock server, then nginx, then run the patched script and redirect to success.log, confirming the JSON response appears before declaring done.
