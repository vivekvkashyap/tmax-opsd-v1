Build this as a straightforward signal-processing script, but be careful with two details that are easy to get subtly wrong. First, generate the time array with exactly N=1000 samples at Fs=1000 Hz (t = arange(N)/Fs), sum the three damped cosines per the given formula, then call `np.random.seed(42)` immediately before drawing the Gaussian noise (not before generating the deterministic signal) and add noise with std=0.05. Second, for the FFT: compute `np.fft.fft`, take the one-sided spectrum (bins 0 to N/2), normalize magnitude by dividing by N (and doubling non-DC/non-Nyquist bins if you want physically correct amplitudes — but what matters for peak detection is relative ordering, not absolute scale). Since Fs and N are matched, frequency bins land exactly on integers, so peak-finding (e.g. `scipy.signal.find_peaks` or simply sorting magnitudes) should recover the three injected frequencies cleanly — verify your peak-detection logic actually returns three genuinely distinct peaks rather than adjacent bins around one true peak. For "max amplitude frequency," don't assume it's the largest-amplitude component in the time domain; the FFT peak height also depends on how quickly each component decays (larger tau spreads less energy, concentrating more in its peak bin) — compute it from your actual spectrum rather than assuming which component wins. Write the JSON with the exact required schema and produce the two-panel plot with matplotlib.
