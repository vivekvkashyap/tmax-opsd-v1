Build the redaction script as a pure text-transform: read the raw log, use `re.finditer(r'\b\d{16}\b', text)` to locate each 16-digit card number, and for each match encrypt the matched substring (as bytes, e.g. `number.encode('utf-8')`) with `Fernet(key).encrypt(...)`, then `.decode('utf-8')` the token so it's printable. Rebuild the log by replacing each match with `ENC(<token>)` — do this via a single regex substitution pass (e.g. `re.sub` with a callback) rather than naive string replace, so you don't accidentally corrupt overlapping or repeated digit sequences, and so all other log text (dates, messages, punctuation) is preserved byte-for-byte. Load the Fernet key from `/home/user/fernet.key` exactly as written (bytes, not re-encoded). After producing `/home/user/safe_audit.log`, run `bandit -f json -o /home/user/bandit_report.json /home/user/redact_audit.py` to get the scan report — check bandit's exit code doesn't block you (it can be nonzero when findings exist) and still confirm the JSON file was written.