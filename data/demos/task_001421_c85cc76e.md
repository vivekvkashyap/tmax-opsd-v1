Start the given `start_services.sh` in the background first and give it a moment to bring up Redis (6379) and the mock sequencer API (8001) before writing your own service — verify with `redis-cli ping` and `curl 127.0.0.1:8001/api/reads`. Build your Flask/FastAPI app on port 8080 exposing GET `/api/v1/network-stats`: on each request, fetch the reads list, cache the raw JSON string in Redis under key `dna_raw_reads` (use `json.dumps` of the exact list you fetched, don't re-derive it), then build a `networkx.Graph()` with one node per list index. For the edge rule, precompute the set of all 4-length substrings (k-mers) for each sequence, then for every pair of nodes check if the k-mer sets intersect (use `set.intersection` for O(1) pairwise lookups rather than nested character comparisons) and add an edge if any shared k-mer exists — remember edges are unordered pairs (i != j), so don't double count or self-loop. Compute node degrees, sort descending into Y, use index positions as X, and fit with `scipy.stats.linregress(X, Y)`. Round slope/intercept to 4 decimals and return `max_degree` as an int, not numpy int64 (cast explicitly) since JSON serialization can fail or produce unexpected types otherwise. Keep the process running in the background and confirm the port stays open after your shell returns.