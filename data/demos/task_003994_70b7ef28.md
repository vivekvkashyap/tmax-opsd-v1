Write the C parser incrementally and test it in isolation before wiring up the full pipeline. Parse FASTA by reading line by line: on a `>` line, flush the previous sequence buffer (if any) and start a new one; on any other line, strip the trailing newline and append to the current sequence buffer. Track sequence count and per-sequence GC count/length as you go, then flush the last sequence at EOF (a common bug is dropping the final record). For the two variance methods, accumulate `sum`, `sum_sq` for the naive formula, and separately run Welford's update (`delta = x - mean; mean += delta/n; M2 += delta*(x-mean)`) in the same pass or a second pass over the GC-content array — use `double` throughout and format with `printf("%.8f", ...)`. Write lengths to `lengths.txt` as you parse. For the histogram script, read `lengths.txt` with a `while read -r len` loop, bucket by the three thresholds, and build the star strings with `printf '%.0s*' $(seq 1 "$count")` or a loop, writing bins in fixed order regardless of empty counts. Compile with `gcc -o /home/user/analyzer /home/user/analyzer.c -lm` and verify by rerunning your own program against small hand-crafted FASTA input first.