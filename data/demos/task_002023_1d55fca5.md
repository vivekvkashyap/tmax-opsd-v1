Treat `/app/oracle_processor` strictly as a black box: characterize its behavior empirically by feeding varied JSON lines via stdin (normal logs, logs missing `metadata` entirely, logs with `metadata` but no `status`, high-precision `sensor_reading` strings) and diffing its stdout against your assumptions — do not disassemble it. Bring up Redis and the Flask receiver, `curl` against `/ingest`, and read the worker's crash traceback to see exactly which nested field access throws. For precision loss, recognize that default `float()`/`json.loads` parsing rounds to IEEE-754 double precision, dropping digits beyond ~15-17 significant figures; instead parse with `parse_float=decimal.Decimal` (or `simplejson` with `use_decimal=True`) so readings stay exact, and serialize the same way the oracle does. Handle the missing-field case by catching the exact `KeyError` path on `log['metadata']['status']` and injecting the oracle's error field instead of crashing. Validate by piping hand-built edge cases through both your script and the oracle, diffing line by line, before trusting it against the fuzz harness.