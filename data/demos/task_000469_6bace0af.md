Treat `/app/stat_oracle` as a black box you must probe systematically, not reverse-engineer by reading its stripped binary. Generate small, controlled synthetic CSVs (e.g. varying one row's val_A or val_B at a time, including negative values, zeros, and boundary cases) and run the oracle on each, comparing outputs to hand-computed Welch's t-statistics to isolate: (a) the exact filtering predicate on val_A/val_B (test negative values, zero, and very small values separately to nail the boundary), and (b) the exact linear combination forming the derived feature (vary val_A alone, then val_B alone, and solve for the coefficients from the shift in the resulting t-statistic). Once both are pinned down, implement Welch's t-test in C++ exactly: per-group mean and sample variance (n-1 denominator) of the derived feature, then `t = (mean_treatment - mean_control) / sqrt(var_treatment/n_treatment + var_control/n_control)`. Print with enough decimal precision (e.g. `std::setprecision` + `std::fixed`, at least 6 digits) since the grader compares floats to 1e-4. Validate by running many fresh mock CSVs through both the oracle and your compiled `/home/user/repl`, diffing outputs, before considering it done — don't assume your first guess at the filter/formula is correct without empirical confirmation.
