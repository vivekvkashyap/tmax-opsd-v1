Solve this as a two-stage aggregation using awk rather than writing a custom parser. First, filter rows where column 2 (MODULE) equals `MCMC_SAMPLER`, group by column 3 (RANK), and sum column 4 (DURATION) per rank — e.g. `awk '$2=="MCMC_SAMPLER"{sum[$3]+=$4} END{for (r in sum) print r, sum[r]}' trace.log`, then find the rank with the maximum summed value (sort -k2 -nr, take top). Second, using that specific rank, filter rows where column 2 equals `MESH_REFINE` AND column 3 equals the bottleneck rank, and compute the average of column 4 with awk's running sum/count divided at END, printing with `printf "%.2f"` to get exactly two decimals (careful with awk's default float formatting, which won't round to 2 dp on its own). Write the two result lines to `/home/user/bottleneck.txt` in the exact literal format `Target Rank: <rank>` and `Average MESH_REFINE Duration: <avg>` with no extra whitespace, since verification likely does an exact/grep match on these strings.
