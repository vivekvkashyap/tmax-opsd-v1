Phase 1 (video decode): use `opencv-python` or `ffmpeg`-extracted frames to read pixel (0,0) of every frame in sequence (10fps, frame 0 first); classify each as bit 1 if it's white (255,255,255) and 0 if black, pack 8 consecutive bits MSB-first into each byte, and write the raw bytes to `/home/user/recovered_telemetry.bin` — don't assume a frame count, read until the video ends. Verify your decode is sane by checking the first 4 bytes decode as a plausible little-endian length before trusting the rest. Phase 2 (C++ tool): use `protoc` to generate C++ bindings from your `.proto` file, then write a stdin-reading loop that repeatedly reads a 4-byte LE length prefix, reads exactly that many bytes, parses them with `Telemetry::ParseFromArray`, and recomputes CRC32 of the `data` field using zlib's `crc32()` (standard IEEE 802.3 polynomial) — reject the whole message if it doesn't match the stored `crc32`. Buffer all valid messages, sort with `std::stable_sort` using a comparator on (timestamp, module_name), then print each as lowercase hex via something like `snprintf(buf, "%02x", byte)` per data byte — get spacing/brackets in `[<timestamp>] <module>: <hex>` exactly right since this will be fuzz-tested against an oracle on thousands of random streams including edge cases (empty stream, 0-length data, corrupted CRCs).