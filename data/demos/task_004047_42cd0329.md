Diagnose the build failure precisely before touching anything: the task says not to modify the Go or Python source, so the bug must be in how `go build` is invoked in `build.sh`. A plain `go build -o libprimes.so prime_worker.go` produces a normal Go executable, not a C-ABI shared object loadable via `ctypes` — Go requires the explicit `-buildmode=c-shared` flag to emit a proper `.so` (plus a companion header) that exports the `//export`-annotated C functions. Locate that `go build` line in `/home/user/math_feature/build.sh` and add `-buildmode=c-shared` to it, keeping the same output path/name so `main.py`'s `ctypes.cdll.LoadLibrary` call still finds `libprimes.so` next to it. Run the corrected script and check the resulting `output.txt` for a comma-separated factor list — verify by cross-checking that multiplying the reported factors together reproduces 1234567890, rather than assuming any output is correct. If the build still errors, check `go.mod`/module setup and CGO availability rather than second-guessing the source files you were told not to touch.
