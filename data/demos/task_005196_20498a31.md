Do this in two clean phases with a clear intermediate store. Phase 1 — cleaning: read /app/translations_dump.csv, group rows by the (locale, key) pair, and for each group keep only the row with the maximum integer `timestamp` (not the last row encountered — sort or compare timestamps explicitly since the CSV can have retried/out-of-order duplicates). Then drop any row whose locale fails a strict "exactly two lowercase ASCII letters" check (regex `^[a-z]{2}$`), applying this filter after dedup so validation doesn't discard the wrong duplicate. Phase 2 — server: write /home/user/localeserver/main.go that loads the cleaned data into an in-memory map guarded by a mutex (since it's mutated from a concurrent TCP handler), then runs an HTTP server on :8080 serving GET /translate?locale=&key= from that map (404 on miss) and, concurrently via goroutine, a TCP listener on :9000 that reads newline-delimited `locale:key:translation` lines and updates the same map under the mutex lock, ignoring lines with invalid locale. Keep both listeners in the same process/binary so map updates are visible to the HTTP handler. Start the server in the background (`&` or nohup), leave it running, and only as a final smoke test send a raw string over TCP (e.g. with `nc`) to confirm the HTTP endpoint reflects updates before applying the real audio-derived override.