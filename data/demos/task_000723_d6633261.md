Follow the pipeline stages in exact order and seed placement, since seeding at the wrong point changes every downstream random draw. Parse the log with a line filter that only processes lines containing `partial_metric`, splitting on `=` and stripping/casting the trailing token to float — count how many such lines you find. Convert the resulting 1D array to `numpy.float16` once, up front (this is what manufactures the precision-loss effect you're measuring). Call `numpy.random.seed(42)` immediately before the Monte Carlo loop, then for exactly 5000 iterations: `numpy.random.shuffle` the float16 array in place and append `numpy.sum(...)` of that shuffled array to a results list — use `numpy.sum` specifically (not a Python loop or `.sum()` on a different dtype) since the reduction algorithm affects the float16 rounding pattern. After the loop, re-seed with `numpy.random.seed(123)` right before the bootstrap step, then do 2000 resamples of the 5000 sums with replacement (`numpy.random.choice(..., replace=True)`), computing `ddof=1` std for each resample, and take the 2.5/97.5 percentiles of those 2000 stds for the CI. Cast all reported numbers to plain Python floats before writing the JSON with the exact specified keys.
