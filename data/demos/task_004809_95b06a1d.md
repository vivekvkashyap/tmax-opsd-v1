First fix `/etc/*bashrc`/global `.bashrc` so `MATH_SCALE` (the `bc` scale env var) is set to `6`, then re-source it before testing anything, since the pipeline won't run correctly otherwise. To bisect, write a good/bad test script that runs `process_audio.sh` against `/app/telemetry_sample.wav` and checks whether the extracted peak frequency has correct rounded precision (vs. truncated), then drive it with `git bisect start` / mark known-good and bad refs / `git bisect run <script>` rather than manually stepping through commits. The key insight for the regression: `bc` truncates rather than rounds by default, so a naive `echo "$a + $b" | bc` at a given scale silently drops precision instead of rounding — you must implement correct rounding yourself (e.g. add a small half-unit-in-the-last-place offset before truncating, or use `awk`'s `printf "%.6f"` which rounds properly) inside `fixed_extractor.sh`. Validate your script against several hand-computed sums before trusting it, then rerun the corrected extraction logic over the WAV file to produce the peak frequency output.
