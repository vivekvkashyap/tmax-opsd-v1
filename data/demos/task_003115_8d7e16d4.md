Break this into extraction, math, and service layers, and validate each independently before wiring them together.

1. Run OCR on `/app/gel_metadata.png` with `tesseract /app/gel_metadata.png -` (or similar) to recover the three labeled values (AUTH_TOKEN, ALPHA, BETA) as printed in the image; clean up OCR noise/whitespace and parse them into variables rather than hardcoding guesses.
2. Implement `R(t) = ALPHA * t**BETA` and its definite integral from 0 to t analytically (power rule: `ALPHA * t**(BETA+1) / (BETA+1)`), or via numerical integration (`scipy.integrate.quad`) if you prefer — either should match closely.
3. For the bootstrap CI, load `/app/mutations.csv` with pandas/numpy, set `np.random.seed(42)` exactly once before generating exactly 1000 resamples (each resample is `len(scores)` draws with replacement from the full score array), compute the mean of each resample, then take the 2.5th and 97.5th percentiles of those 1000 means. Seed placement and resample count must match exactly or the percentiles won't line up.
4. Serve both computations via an HTTP API on `127.0.0.1:8000` (Flask/FastAPI/http.server) with `/integrate?t=<float>` and `/bootstrap` endpoints, checking `Authorization: Bearer <token>` against the OCR'd token and returning 401 on failure.
5. Wrap dependency install + background server launch in an executable `/home/user/run_server.sh`.
