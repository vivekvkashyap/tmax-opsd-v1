Build this as a pipeline of joins and sorts using only coreutils/awk — plan the join keys before writing commands. First join `features.csv`, `labels.csv`, and `split.csv` on `id` (sort each by `id` numerically and use `join`, or do it all in one pass with `awk` reading three files via `getline`/associative arrays keyed by id, which is simpler than chaining multiple `join` calls with re-sorting between each). Separate the joined rows into train and test sets based on `split_name`. For each test row, scan all train rows and compute `abs(feature_value_test - feature_value_train)` in `awk` using a conditional (`x<0?-x:x`) since awk has no builtin `abs`; track the minimum distance and, on exact ties, prefer the smaller train `id` explicitly (don't just keep the first match found — your scan order might not be by id, so compare id numerically when distances tie). Emit `id,predicted_label` for each test id using the nearest train row's label. Finally sort the output rows numerically by id (`sort -t, -k1,1n`) with the header line kept first, and write to `/home/user/predictions.csv`. Manually verify a couple of predictions by hand-computing nearest neighbors before trusting the whole pipeline.