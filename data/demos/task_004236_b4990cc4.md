Scaffold with `cargo new --bin wal_check`, then implement the four clean-file conditions by reading the file's lines into a `Vec<&str>` (or iterating once while tracking first/last): first line exactly `WAL_START`, last line exactly `WAL_END`, and no line containing the substrings `../` or `EXEC=` anywhere in the file (not just first/last lines). Exit `std::process::exit(0)` when all hold, `1` otherwise — handle empty files and single-line files as edge cases so they don't panic. Build with `cargo build --release` so the binary lands at `target/release/wal_check`.

For the pipeline, rewrite `archiver.sh`'s loop (note the given script has a bug: bash needs `while true`, not `while True`) to, for each `*.wal` file in pending/: run `wal_check`, and on success add the file's path to a list and append its `sha256sum` to a `manifest.sha256`; on failure `rm` it. Generate `manifest.sha256` *before* taring, then `tar -czf archive.tar.gz -C <pending_dir> manifest.sha256 file1.wal file2.wal ...` so the manifest sits at the tar root alongside the wal files. Restart the services via `start.sh` after editing so the end-to-end loop picks up your changes.
