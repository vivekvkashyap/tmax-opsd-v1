Build this as three discrete, verifiable stages rather than one monolithic script. First, walk /home/user/data_to_backup recursively and compute SHA256 per file — easiest is to just shell out to `sha256sum` on each relative path (run from inside the source dir with `find . -type f -exec sha256sum {} +` so the path column matches the required relative-path format exactly) and redirect to /home/user/manifest.txt. Second, mirror the directory tree into /home/user/staging_backup: for each file, branch strictly on extension — `.log`/`.txt` files get `gzip -c file > staging/.../file.gz` (compressed, keep original untouched), `.bin`/`.dat` files get a byte-exact `cp` (no compression) — get the extension matching exact, since anything else should presumably be left alone or explicitly handled per spec. Third, `tar -cf /home/user/final_backup.tar -C /home/user/staging_backup .` so the archive root contains the subdirectories directly (not a wrapping staging_backup/ folder — verify with `tar -tf`), then append the manifest at the archive root with `tar -rf /home/user/final_backup.tar -C /home/user manifest.txt`. Double check binary files extract byte-for-byte identical to originals.
