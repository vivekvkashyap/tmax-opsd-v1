Fix the vendored library build first: inspect `/app/vendored/miniz-cpp-0.1.0/CMakeLists.txt` for a mismatched source filename in `add_library` (compare against the actual file on disk) and add a missing `target_include_directories` so downstream code can find the headers, then `cmake`/`make`/install it before writing any application code — confirm you can compile and link a trivial test program against it first. For `doc_filter.cpp`, parse `/home/user/doc_rules.conf` generically (split on `=`, split the extension list on commas) rather than hardcoding the example values, since rules could vary. For each `.zip` in the target directory: attempt to open/validate it with the library's integrity check (catch and reject corrupt archives rather than crashing), enumerate entry names and uncompressed sizes, and apply each rule as an independent rejection condition (disallowed extension, size sum over the MB limit, any path component starting with `.`) — reject on the first violation found. Sort output lines alphabetically by filename before printing, and self-test against a handful of zip files you construct yourself (corrupt, oversized, hidden-file, and clean) before trusting the tool.
