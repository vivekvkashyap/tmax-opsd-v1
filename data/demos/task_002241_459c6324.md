Write poll_vm.py to (1) open a raw TCP socket to 127.0.0.1:5900 with a short timeout and classify UP/DOWN based on connect success, (2) find the qemu-system-x86_64 PID via `/proc` scanning or `pgrep`-equivalent logic in Python (read `/proc/<pid>/comm` or `/proc/<pid>/cmdline`), (3) parse `/proc/<pid>/status` for the `VmRSS:` line and extract the numeric kB value, and (4) append one CSV line with a timestamp formatted exactly as specified. For deploy_job.sh, make idempotency the core design constraint: check `[ -f file ]` before creating the CSV and only write the header on first creation; for the cron entry, capture `crontab -l` output (handling the "no crontab" case), grep for the exact script path to see if it's already present, and only append+reinstall via `crontab -` if it is not — never blindly append on every run. Make both scripts executable with `chmod +x`, run deploy_job.sh once, then run poll_vm.py once manually to seed a data row, and verify by re-running deploy_job.sh a second time that no duplicate cron line or header appears.
