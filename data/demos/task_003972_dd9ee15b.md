Do not parse the CSV with naive `split(',')` or line-by-line reading — embedded newlines inside quoted `message` fields will break that. Use Python's `csv.reader` (or `csv.DictReader`) on the opened file object, which correctly handles multi-line quoted fields per RFC 4180, as long as the source data quotes fields containing newlines/commas. After loading rows, strip whitespace from `message` and filter with `re.match(r'^CRITICAL: .*', message)`. Parse the ISO timestamp with `datetime.strptime(ts, "%Y-%m-%dT%H:%M:%SZ")` (or `fromisoformat` after stripping the trailing Z) and floor it to the hour via `.replace(minute=0, second=0, microsecond=0)`, then reformat back to the same ISO string form with `Z`. Compute `difflib.SequenceMatcher(None, message, reference).ratio()` against the fixed reference string given in the task and keep only rows with ratio `>= 0.60`. Group by the floored hour string, count, and sort by the hour key (string sort works since ISO format is lexicographically chronological) before writing the two-column CSV with `csv.writer`.