Work through the setup steps in dependency order since later steps rely on earlier ones existing. Create the two directories and write the exact literal string `FAKE PNG DATA` (no trailing newline surprises — verify with `printf` rather than `echo` if you need to avoid an extra newline, though check what the verifier expects) into `shared_assets/logo.png`. Create the symlink with `ln -s /home/user/shared_assets /home/user/app_content/assets` (absolute target, not relative, to avoid resolution issues). Generate the TLS materials with `openssl req -x509 -newkey rsa:2048 -nodes -keyout server.key -out server.crt -days 365 -subj "/CN=localhost"` (the `-nodes` flag is essential for an unencrypted key, since a passphrase-protected key will hang the Python server on load). Write `run_https.py` using `http.server.HTTPServer` with `SimpleHTTPRequestHandler`, `os.chdir` into `app_content` so relative URL paths resolve there, then wrap the raw socket with an `ssl.SSLContext` configured via `load_cert_chain` before calling `serve_forever()`. Launch it backgrounded (`nohup python3 run_https.py &` or similar) so it outlives the current shell, then confirm with `curl -k https://localhost:9443/assets/logo.png` that it returns the exact file contents through the symlink.