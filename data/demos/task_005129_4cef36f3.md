This is a security-fix-then-reimplement task with fuzz/concurrency testing, so correctness under adversarial and concurrent conditions matters more than a quick patch. First inspect `/app/zstream-1.2.0/zstream/parser.py` and locate `get_safe_filename()` — the vulnerability is that it currently returns the raw embedded path unmodified; fix it to strip all directory components and return only the base filename (Python's `os.path.basename()` is the standard, robust way to do this — don't hand-roll string splitting on `/` alone, since it must also handle `..` segments, absolute paths, and Windows-style backslashes if present). After fixing the vendored package, set `PYTHONPATH` to include `/app/zstream-1.2.0` in your extraction script so it imports your patched code, not a stale copy. In safe_extract.py, read the stream via `zstream.StreamReader(sys.stdin.buffer)`, for each record compute the target path as `/home/user/extracted/admin_<safe_filename>`, decompress with `zlib.decompress`, then open the target in append-binary mode and wrap the write in `fcntl.flock(fd, fcntl.LOCK_EX)` / write / `fcntl.flock(fd, fcntl.LOCK_UN)` — since many concurrent instances may target the same filename, the lock must span open-to-close of that single write, not be skipped for "efficiency." Make the script executable (`chmod +x`) and test it manually with a hand-crafted stream before relying on the fuzzer.
