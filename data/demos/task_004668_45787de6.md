Design the C program around a per-transaction ordered list of (timestamp, resource) locks: parse each CSV line, and append each lock event to an array/list keyed by transaction_id (a hash map or simple linear scan over a fixed-size array works fine at this data size), preserving chronological order since the file is already timestamp-sorted. Then detect deadlock pairs by, for every pair of transactions T_A < T_B, checking whether there exist resources R_X < R_Y such that T_A locks R_X before R_Y, and T_B locks R_Y before R_X (i.e., look for any resource pair locked in opposite orders by the two transactions) — a double loop over each transaction's own lock sequence comparing pairs of resources it touched, then cross-checking against the other transaction's sequence, is a reasonable approach; dedupe so each (T_A,T_B,R_X,R_Y) appears once. Sort results by T_A then T_B. Implement pagination by simply skipping `offset` rows of the already-sorted results and printing up to `limit` after that, output format exactly `T_A,T_B,R_X,R_Y`. Compile with the exact `gcc` command given, then run `./detect 5 1 > /home/user/audit_report.txt` (limit 5, offset 1) as a separate shell step to produce the report file.
