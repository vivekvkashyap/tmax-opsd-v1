Write the C++ program in clear stages and test each against the given CSV before moving to the next. Parse the CSV with simple string splitting on commas (watch for the header row and for a field that is literally empty between two commas, e.g. `1.0,,1.0`, which requires checking for an empty substring, not just relying on stream extraction which would silently fail or shift columns); treat both an empty field and the literal text `NaN` as 0.0. Load the three imputed values into an `Eigen::Vector3d`, compute its `.norm()`, and only divide by the norm when it's nonzero (guard with an epsilon or exact-zero check) — otherwise output `[0,0,0]` to avoid dividing by zero producing NaN. Round each of the three normalized components to 4 decimal places (e.g. via `std::round(x*10000)/10000` and fixed-precision output) and build a JSON object mapping each row's `id` to its 3-element array; write it with correct JSON syntax (commas between entries, no trailing comma) to `/home/user/embeddings_log.json`. Compile with g++, run the executable once, then manually inspect the output file for correct rounding and structure before considering the task complete.
