Since Python/R/Perl/Node are forbidden for the optimizer itself, plan the math as closed-form linear regression (normal equations) rather than iterative gradient descent in awk, since it's much easier to get exactly right in one pass: with two features plus bias, you need sums of x1, x2, y, x1*x1, x1*x2, x2*x2, x1*y, x2*y, and n, all accumulated in a single `awk` pass over the CSV (skip the header line), then solve the resulting 3x3 linear system for w1, w2, b — either derive the closed-form algebraic solution by hand ahead of time and encode it directly in awk, or implement a small Gaussian elimination in awk on the 3x3 normal-equations matrix. Use `awk`'s native floating point (or shell out to `bc -l` for the final division steps) to avoid precision loss; avoid doing this arithmetic in integer bash. Print the result as `w1,w2,b` with enough decimal places to `/home/user/weights.csv`. Sanity-check by rerunning `optimize.sh` and confirming the numbers are stable across runs, then only trust it once it doesn't rely on any forbidden interpreter anywhere in the pipeline (check with `grep` for stray python/perl/ruby/node invocations in your own script).