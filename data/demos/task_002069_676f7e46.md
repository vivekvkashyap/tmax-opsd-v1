Use pandas for correctness and Python's multiprocessing (or a ProcessPoolExecutor) for the parallel-per-file step, since pandas' own `read_csv` already handles quoted fields with embedded newlines correctly (the previous analyst's bug was almost certainly from naive `awk`/`split`-on-newline tooling — avoid that trap yourself). For each file: read with `pd.read_csv`, derive `site_id` from the filename stem, use `pd.melt` (or `wide_to_long`) to unpivot the three sensor columns into `sensor_name`/`reading`, coerce `reading` to numeric and drop NaN/empty rows, sort by `timestamp` within each `(site, sensor_name)` group, then compute the rolling average with `groupby(...)['reading'].transform(lambda s: s.rolling(window=3, min_periods=1).mean().round(2))`. Concatenate all four files' results (order of concatenation across files doesn't need special sorting unless the checker expects a specific order — just make sure per-group chronological order is preserved), and write out with `to_csv` using default quoting so `notes` cells containing `\n` get properly double-quoted. Double check the exact header order requested. Actually execute the script rather than just writing it, and inspect the output file afterward to confirm newlines survived inside quoted fields.