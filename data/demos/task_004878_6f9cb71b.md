Use the `zip` crate to read `legacy_docs.zip` entry-by-entry rather than relying on any OS-level extraction, since the whole point is implementing the Zip Slip guard yourself. For each entry, inspect its recorded name (`ZipFile::name()`) and reject/skip it (logging `SKIPPED: <name>`) if the raw name contains the substring `..` anywhere — check the string before any path-joining, don't rely on the OS to refuse writing outside the target directory, since that's exactly the vulnerability being tested. For entries that pass, extract them under `/home/user/workspace/extracted_docs` preserving their relative subdirectory structure, log `EXTRACTED: <name>`, then transform content: convert lines starting with `[HEADER] ` to start with `# `, and globally replace `[B]`/`[/B]` with `**` (simple string replace, applied file-wide not just line-starts). Save as `.md` next to the original with the `.txt` deleted afterward. Finally walk the extracted-docs tree, collect all `.md` files, and write them into a gzip-compressed tar (`flate2` + `tar` crates) at `clean_docs.tar.gz`, using each file's path relative to `extracted_docs` as its archive entry name so `docs/intro.md` etc. is preserved. Run everything via `cargo run` from the project directory, and inspect the tar and log afterward to confirm no traversal entries leaked through.
