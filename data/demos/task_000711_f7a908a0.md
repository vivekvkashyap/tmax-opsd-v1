Process the JSONL file line-by-line (or in chunks) rather than loading it fully into memory — a simple `for line in open(file)` streaming parse with json.loads per line works, or use pandas' `chunksize` if you prefer. Load the small metadata CSV fully into a dict/DataFrame keyed by `string_id` for the join. For each telemetry record: derive `hour` by slicing the first 13 characters of the timestamp string (`YYYY-MM-DDTHH`), look up its `string_id` in metadata, skip records with no match or non-`high` priority, then increment a counter keyed by `(hour, lang, string_id, module)`. After processing all lines, materialize the counts into rows with a `missing_count` column, then sort by `hour` ascending, `missing_count` descending, `string_id` ascending (multi-key sort, be careful of tie-break order and ascending/descending directions). Write to CSV with the exact header `hour,lang,string_id,module,missing_count`. Match pandas' default CSV formatting (no extra whitespace, LF line endings, no quoting unless needed) since your output will be byte-compared against a reference generated with pandas `to_csv`.
