Set up `/home/user/venv` with `python3 -m venv` and install `numpy`, `scipy`, `h5py`, `netCDF4` into it, then run your analysis script with that venv's interpreter. In the script: open `sim_data.h5` with `h5py` and read dataset `temperature_field` (100x100); open `obs_data.nc` with `netCDF4.Dataset` and read variable `temp_obs` (200x50) — reshape it to (100,100) using default (C-order/row-major) `numpy.reshape`, since that's the standard flattening convention and matches how the data was originally stored before being reshaped to 200x50. Flatten both arrays with `.flatten()` (also C-order by default) to 1D arrays of length 10000. Compute `scipy.stats.pearsonr(sim_flat, obs_flat)` for the correlation and `scipy.stats.ttest_rel(sim_flat, obs_flat)` for the t-statistic and p-value. Round each of the three values to exactly 4 decimal places (`round(x, 4)`) and write them as a JSON object to `/home/user/analysis_results.json` with keys `correlation`, `t_statistic`, `p_value` — verify the reshape didn't scramble spatial alignment by checking the resulting correlation is reasonably strong, not near zero.