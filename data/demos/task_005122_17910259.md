First inspect the CSV headers in /home/user/graph_data/*.csv with `head` to confirm column names and order (don't assume — verify src/dst/timestamp/status column names match what you filter on). Build the pipeline as a chain of standard tools: use `awk -F,` (or `tail -n +2` per file to skip headers, then `cat` all files together) to filter rows where the status column equals `ACTIVE` and the timestamp column is numerically `>= 1700000000`, emitting just the src and dst node per valid row. From that filtered edge list, compute degree centrality by emitting one line per node touched by each edge (once for the src, once for the dst) and piping through `sort | uniq -c` to get per-node counts — this naturally sums in-degree and out-degree in one pass. Sort the counts descending by count and ascending by node name for ties (`sort -k1,1nr -k2,2` on the right fields), take the top 3 with `head -n 3`, and use `jq -n` with `--arg`/`--argjson` (or build a jq filter over piped input) to emit the array-of-objects JSON schema `[{"node_id":...,"centrality":...}]` to /home/user/top_nodes.json. Wrap all of this in the bash script and execute it once to produce the output file.