Work this incident like a real build/crash triage: first read the Makefile and notice its rules are mutually dependent (each `.o` target lists the *other* object file as a prerequisite), so `make` can't establish an order — rewrite the dependency graph so `token.o` and `crypto.o` each depend only on their own `.c` source (and maybe shared headers), with `libtoken.so` depending on both objects. Second, open `src/token.c` and replace the unbounded `strcpy(user, serialized_data)` into the 16-byte `user` buffer with a length-checked copy (e.g. `strncpy` plus explicit truncation/null-termination, or reject with return 0 if the input exceeds the buffer) so long usernames can't overflow the stack. Rebuild with `make` to regenerate `libtoken.so`, confirming no compiler warnings about implicit declarations remain from the circular-dep fix. Then run `/app/start_services.sh` (or manually background `python3 api.py` and `python3 metrics.py`) so port 8080 serves `/verify` and port 8081 serves `/stats`; test locally with a long-token POST to confirm no segfault and a 200 response before leaving both processes running in the background.