Inspect the schema first (`sqlite3 /home/user/graph.db ".schema"`) to confirm the `Nodes`/`Edges` columns before writing SQL. Express the triangle search as a single self-join of `Edges` three times: alias e1 (A->B), e2 (B->C), e3 (C->A), join on `e1.target_id = e2.source_id`, `e2.target_id = e3.source_id`, `e3.target_id = e1.source_id`, and filter every edge's `relation_type = ?` using a bound parameter (pass it via `cursor.execute(sql, (relation_type,))`, never f-string/format it into the query). To dedupe rotations of the same cycle, add the ordering constraint `e1.source_id < e2.source_id AND e1.source_id < e3.source_id` in the WHERE clause exactly as specified — this picks a canonical rotation without needing DISTINCT tricks. Join to `Nodes` to resolve names for A, B, C, collect results in Python, sort the list of triples lexicographically by (nameA, nameB, nameC), and json.dump the sorted list of 3-element lists to `/home/user/cycles.json`. Run the script yourself with the DEPENDS_ON argument and eyeball the output structure before considering it done.
