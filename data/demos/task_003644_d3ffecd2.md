Work in stages and verify each before moving on. First, create the venv (`python3 -m venv /home/user/venv`), activate it, and `pip install tokenizers numpy`. Second, write a script that loads `/home/user/raw_data.jsonl` line by line (`json.loads` per line), collects `id` and `text`, and trains a `tokenizers.Tokenizer(BPE(unk_token="[UNK]"))` with `Whitespace` pre-tokenizer via `BpeTrainer(special_tokens=["[UNK]"], vocab_size=500)` using `train_from_iterator`, then save with `tokenizer.save(...)` to `/home/user/bpe_tokenizer.json`. Third, re-encode every text with `tokenizer.encode(text).ids` and record `len(ids)` per record — do not reuse lengths captured during training, since the trainer doesn't return per-text lengths directly. Compute the 85th percentile with `numpy.percentile(lengths, 85)` over the *entire* dataset, then filter to keep only records where length is strictly less than that threshold (not `<=`). Finally, write `id,token_length` rows sorted ascending by `id` to a CSV with a header at `/home/user/filtered_dataset.csv`. A common mistake is filtering with `<=` instead of `<`, or computing percentile after already discarding rows — do the percentile computation before filtering, over all lengths.