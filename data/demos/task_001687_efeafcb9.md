Start by inspecting the CSV headers/data in `/home/user/data/` directly (`head`/`csvlook`) to infer the join keys before writing any code — don't assume column names, discover them. Once you know the schema, load all three tables into an in-memory SQLite DB (or just parse with a lightweight CSV crate) and write a join query: events (timestamp) -> confidence scores (by event id) -> categories (by class/category id), filtered by the requested category, ordered by confidence descending, taking the top row's timestamp. For the Rust server, use a minimal HTTP crate (`tiny_http` or `rouille`) bound exactly to `127.0.0.1:5050`; implement the `Authorization: Bearer <token>` check first and return 401 immediately on mismatch/missing header before doing any work. For frame extraction, shell out to `ffmpeg -ss <timestamp> -i /app/footage.mp4 -frames:v 1 -f image2 -` (or write to a temp file and read it back) to get exact-timestamp JPEG bytes, then set `Content-Type: image/jpeg` and the custom `X-Event-Timestamp` header before writing the body. Keep the server running in the background and test with `curl -v` including and excluding the auth header to confirm both code paths.
