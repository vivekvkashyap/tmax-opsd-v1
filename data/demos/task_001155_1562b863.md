Start by reverse-engineering the shared object before writing any wrapper code: run `nm -D` or `objdump -T` on /app/libsec_eval.so to confirm the exported symbol name and use `file`/`readelf` to check its calling convention; since it's stripped you may not get types, so infer the signature from the task description (three C strings in, C int out) and test it directly from a small ctypes snippet before building the full service, e.g. load with `ctypes.CDLL`, set `.argtypes = [c_char_p]*3` and `.restype = c_int`, and call it with a few sample tokens/types to observe behavior empirically — never guess the internal rules, derive them by probing. Once the FFI call is confirmed working, build the HTTP service (Flask/FastAPI/http.server) binding to 127.0.0.1:8000 with the exact POST route, and implement graph traversal (BFS/DFS) over the nodes/edges where each candidate edge is only followed if the C function call for (type_from, type_to, token) returns 1 — evaluate edges lazily during traversal, not by precomputing a fixed adjacency mask that ignores the token. Write the standalone test_fixture.py to hit the running server and assert on the JSON response, then make sure the server is left running in the background (e.g. via nohup or a background shell) so the automated verifier can reach it after your session ends.