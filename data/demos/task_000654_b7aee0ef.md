Tackle this as four separable diagnose-and-fix steps, verifying each with its own command before moving on. First, run `cargo build --release` in `/home/user/log_pipeline` and read the actual error: a missing system dependency (likely `openssl-sys` needing `pkg-config`/`libssl-dev`, or an equivalent vendored-feature toggle in `Cargo.toml`) plus a genuine compile error in `src/query.rs` that you must trace to its root cause rather than papering over. Second, in `src/parser.rs`, find where it panics on unescaped control characters inside "user_agent" values — replace the panicking path (e.g. an `.unwrap()` or `expect()` on a strict parse) with a check that detects invalid/control bytes and skips just that field/record gracefully instead of aborting. Third, treat `/app/analytics_oracle` as a black-box target for the variance calculation only: feed it the same `input.txt` your Rust code sees, diff its stdout against your `src/analytics.rs` output to 5 decimals, and fix the catastrophic-cancellation bug by switching the rolling variance to Welford's online algorithm (mean/M2 updates) instead of sum-of-squares-minus-square-of-sum. Fourth, in `src/worker.rs`, look for a Tokio task spawned without a corresponding `JoinHandle` await or cancellation-token check on the cancel path — leaked tasks usually mean a spawned future outlives its owner; ensure cancellation propagates (e.g. via `tokio::select!` against a cancellation signal) so tasks actually terminate. Finally, scaffold `/home/user/sanitiser` with `cargo new`, parse each log line's JSON, recursively track nesting depth and key lengths, and write to the output directory only lines where max key length ≤ 64 and max nesting depth ≤ 4, preserving filenames; test it against small hand-made nested/long-key fixtures of your own before trusting it on the real corpora.