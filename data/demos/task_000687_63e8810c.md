Fix the vendored library before building on top of it. In `/app/tinycsv-1.0`, inspect the `Makefile` for a bogus `CC=` value and correct it to a real compiler (e.g. `gcc`), then search `tinycsv.c`'s line-parsing function for a deliberate early-return/error branch triggered when it sees a `\u` escape sequence and remove that sabotage so those bytes are just copied through as literal characters instead of aborting the row; rebuild with `make` to produce `libtinycsv.a`/`.so` and confirm compilation succeeds. Then write a C service (POSIX sockets, no extra deps) that: accepts raw TCP lines on 127.0.0.1:8001 and parses each with your fixed tinycsv, maintains an in-memory set/hash-table keyed by `transaction_id` to drop rows whose ID was already seen, accumulates a running count and float total of `amount` for unique rows only, and separately answers HTTP `GET /stats` on 127.0.0.1:8002 with a JSON body `{"unique_count": N, "total_amount": X.XX}` (total formatted to exactly 2 decimals). Run both listeners concurrently (threads or select/poll loop) and background the compiled binary; test manually with `nc` and `curl` against both ports, including a payload containing a raw `\u` sequence and a duplicate transaction_id, before declaring done.
