Write `organize.py` as a single-pass `os.walk` over `/home/user/project_files` that, for every file, opens it in binary mode, reads the first 4 bytes to test for the ELF magic (`b'\x7fELF'`) and separately checks `.gcode` extension plus first character in `;`/`G` for GCode — apply these as independent content-based checks, not filename heuristics, since junk files may have misleading names/extensions (e.g. a `.gcode` file whose first byte is neither). Compute `hashlib.sha256(...).hexdigest()` over the full file content for each match, then `os.symlink(absolute_original_path, link_path)` under `elf/` or `gcode/` using the `<name>_<hash[:8]>` naming convention. For the manifest, build the dict in memory, then write it to `manifest.json.tmp`, call `f.flush()` and `os.fsync(f.fileno())` before closing, and use `os.rename('manifest.json.tmp', 'manifest.json')` for the atomic swap — never write directly to `manifest.json`, and make sure the `.tmp` file doesn't linger afterward. Run the script and spot-check the manifest keys/symlink targets against the source files.