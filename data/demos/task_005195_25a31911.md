Build /home/user/auditor.py as four independent, verifiable pieces. (1) Certificate check: load ca.pem and server.pem with the `cryptography` library, verify server.pem's signature was produced by the CA's public key (use the CA cert's public_key().verify(...) against server cert's signature/tbs_certificate_bytes with the correct padding/hash algorithm from server.pem itself) and check `not_valid_before <= now <= not_valid_after`; set cert_valid accordingly. (2) Log scan: read access.log line by line, and for each line first redact any `AKIA[0-9A-Z]{16}` match with `[REDACTED]` via re.sub, then test the redacted (or original — redact regardless) line case-insensitively for the substrings "union select", "etc/passwd", "../", collecting the redacted line if any match. (3) Permissions: for every file in logs/, check the "other" permission bits (stat().st_mode & 0o007); if any are set, chmod the file to 0o640 and record its absolute path in fixed_files. (4) Write the JSON report with keys cert_valid, intrusions, fixed_files exactly as specified. Run the script and confirm /home/user/audit_report.json is produced; the two easy mistakes are redacting after the substring check instead of before, and only checking read (not write/execute) bits for the "world" permission audit.