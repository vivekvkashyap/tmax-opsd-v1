First recover the exact schema and query spec from the image, then implement it as a straightforward CSV join — don't guess the requirements from the CSV files alone. Run `tesseract /app/query_spec.png stdout` (upscale/preprocess the image first if OCR output looks garbled, e.g. with `convert` to increase contrast/resolution) to get the schema and the natural-language query description, and read it carefully for the exact filter condition, which column is the CLI argument, and the exact output format (ordering, separator, case). Once you have that spec, implement `/home/user/query.py` in Python: load the CSVs with `csv.DictReader` or `pandas`, filter users by whatever country condition the image specifies, join through `posts.csv` -> `post_tags.csv` -> `tags.csv` to find users who authored a post carrying the given `sys.argv[1]` tag name, dedupe user_ids, sort them numerically ascending, and print them space-separated on one line with no extra output (no print statements for debugging, no trailing logging). Since a fuzz-equivalence check against a hidden oracle will run your script on 50 different tag names, test your own script against several tag values from `tags.csv` to sanity check the join and sort logic before finishing.