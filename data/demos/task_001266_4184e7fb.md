Read the existing `motif_nmf.py` to understand the current double-nested-loop multiplicative update rule for both H and W before touching it, so the vectorized version preserves identical math and iteration order. Replace each `for i / for j` element-wise loop with a single NumPy elementwise operation, e.g. `H *= num_H / (den_H + eps)` and `W *= num_W / (den_W + eps)`, using `eps = 1e-9` added to the denominator only (not the numerator) to avoid divide-by-zero/NaN on the near-singular input matrix, while keeping `np.random.seed(42)` and the 50-iteration loop unchanged so results stay reproducible. After the loop, add a heatmap of `W` via `matplotlib.pyplot.imshow`/`pcolormesh` plus `savefig('/home/user/W_heatmap.png')`, and keep the existing `np.save` calls for `W_out.npy`/`H_out.npy`. Then write `requirements.txt` listing numpy/matplotlib (and anything else imported), and `run.sh` that does `python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt && python3 motif_nmf.py`, then `chmod +x run.sh`. Run `run.sh` yourself once end-to-end and confirm no NaNs appear in the output arrays before finishing.