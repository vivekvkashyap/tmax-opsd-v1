Break this into decode, count, and serve stages, verifying each independently. First, decode the DTMF tones: run `multimon-ng -a DTMF -t wav /app/sequence_signal.wav` (or convert format first if needed) and capture the sequence of digit tones it reports in order; map each digit through the given table (1=A, 2=C, 3=G, 4=T) to get the DNA sequence — do not assume the sequence, read it from the actual decoder output. Second, compute transition counts by sliding a window of 2 over the recovered sequence and tallying each overlapping pair; then build the full comma-separated string covering all 16 pairs in alphabetical order (AA, AC, AG, AT, CA, CC, ...), filling zero for any pair that never occurred — construct this programmatically (e.g. iterate the sorted Cartesian product of {A,C,G,T}×{A,C,G,T}) rather than hand-writing it, so it's guaranteed complete and correctly ordered. Third, implement the HTTP server in pure Bash using `nc -l -p 9000` (or `socat TCP-LISTEN:9000,reuseaddr,fork EXEC:...`) in a loop that reads the request, ignores it, and writes a proper `HTTP/1.1 200 OK` header (with `Content-Type` and blank line) followed by the precomputed counts string; keep it looping to serve repeated requests, and test with `curl 127.0.0.1:9000` before finishing.
