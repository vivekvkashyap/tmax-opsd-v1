Write a Python script that treats each data source with the exact parsing rule it needs: split `instrument.log` on lines that are exactly `---` to get record blocks, then within each block split each line on the first `: ` to get key/value pairs (don't use `str.split(':')` blindly, since values could contain colons). Use `configparser` to read `criteria.ini`, splitting `valid_sensors` on commas and stripping whitespace from each entry, and parsing `min_value` as float. Apply the filter as strict inequality (`SensorType in valid_sensors` AND `float(Value) > min_value`) — note "strictly greater than" excludes values equal to the threshold. For incremental-backup logic, first read all existing lines of `/home/user/backup/archive.jsonl`, parse each as JSON, and collect the set of `RecordID`s already present; only records whose `RecordID` is not in that set are candidates for appending. Convert each new valid record to a JSON object with keys `RecordID`, `Date`, `SensorType` as strings and `Value` as a float, then open the archive file in append mode and write one JSON object per line (with a trailing newline), leaving existing lines completely untouched. Run the script and verify the archive file's original lines are unchanged and only the new qualifying records were appended.
