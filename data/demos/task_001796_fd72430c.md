Treat the oracle binary purely as a black-box reference for output format, not as something to inspect internally (it's stripped). Run `/app/oracle_sim`, observe its verbose per-line format (something like "Time: ... | Position: ... | Velocity: ..."), and extract just t and x with a small awk/sed/python parser into a two-column CSV. Separately, implement RK4 in C from the governing ODE: convert the 2nd-order equation into a first-order system (x' = v, v' = -γv - ω²x - αx³), and apply the classic 4-stage RK4 update to both state variables together each step, using `double` throughout and iterating from t=0 to t=20 with dt=0.01 (2001 output rows including t=0). Write `t,x,v` with a header to `/home/user/sim_results.csv`, matching exactly one row per step — an off-by-one in step count or accumulating floating error from single precision will blow the RMSE threshold. Compile with `-O2 -lm`. The orchestration script should just chain: run oracle, parse to `oracle_clean.csv`, compile and run `fast_sim`, then diff the `x` columns and print the max absolute difference — this is a sanity check for you, not the graded metric itself (the grader independently checks RMSE against its own RK4 truth).