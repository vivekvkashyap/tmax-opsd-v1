Fix the vendored C library first in isolation, then optimize, then wrap in the Bash service. For the Makefile, `make` and read the linker error — a shared object needs objects compiled with `-fPIC` and the final link step to pass `-shared`; add both flags where missing and confirm `libmatrix.so` is produced. For the memory bug, inspect the matrix multiply's loop bounds in `src/matrix_ops.c`: a `<=` where it should be `<` against `rows`/`cols` (or similar) causes an out-of-bounds heap write — run under Valgrind/ASan on a small test case to confirm before and after the fix. For performance, the naive `i, j, k` loop order thrashes cache because the inner loop strides through a column of B; reorder to `i, k, j` (accumulate into a full row of C while streaming rows of B) to get sequential memory access, then benchmark 500x500 multiplication and confirm it lands well under 0.5s. For `service.sh`: validate argument count and that both files exist and have size > 0 (`[ -s "$f" ]`) before anything else; implement rate limiting by recording a timestamp (e.g. `date +%s`) per request into a file/dir under a scratch location, pruning entries older than 10s, and rejecting once 3 remain in the window; only then exec the Python FFI caller and pass through its stdout.
