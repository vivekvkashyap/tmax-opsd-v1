For the filesystem step, use `debugfs -R "cat /etc/backend_ips.conf" /app/router_fs.ext4` (or `guestfish` if debugfs isn't available) to read the file contents without mounting — no sudo/mount needed. Write the fstab line for a read-only loopback mount (something like `/app/router_fs.ext4 /mnt/router ext4 loop,ro,users 0 0`), one line only. For the video, don't eyeball frames — extract them programmatically: use `ffmpeg -i /app/vnc_capture.mp4 frame_%04d.png` (or decode with OpenCV/Python frame-by-frame) and for each frame sample pixels in the 0,0–50,50 top-left region, classify DOWN vs UP by comparing average/dominant channel (red-dominant vs green-dominant, watch for BGR vs RGB ordering depending on your library), and write the 0-indexed frame numbers where it's red to `/home/user/down_frames.txt`, one per line — verify your detection logic on a few known frames before trusting it across all 300, since the scoring is F1 against the true down-frame set. For nginx, build an `upstream` block listing the backend IPs extracted in step 1 for round-robin (the default nginx behavior needs no explicit directive), with `daemon off;`, `pid /home/user/nginx.pid;`, error_log to `/home/user/error.log`, and `listen 127.0.0.1:8080;` proxying to that upstream.
