Write `bootstrap_ci.c` in stages and test incrementally. First, parse the CSV with a line-based reader (fgets + strtok/sscanf on comma), skip the header, and for the 3rd field explicitly check for an empty string and the literal `"NA"` before calling `atof`/`strtod` — an empty field must not silently become 0.0. Store valid temperatures in a dynamically-grown `double` array (track count N as you go, since you don't know N until parsing finishes). Implement the LCG exactly as specified — global/static `unsigned int seed = 42`, the given multiply-add-mask recurrence — and reuse it strictly in the order given: for each of the 10000 bootstraps, draw N indices in a tight loop (`seed` must never be reset between bootstraps). Sum bootstrap means into a `double` array, `qsort` it ascending, and pull indices 250 and 9750 directly (0-indexed) as your CI bounds — don't reimplement percentile via interpolation, the task wants raw index lookup. Compute grand mean directly from the original valid-value array. Print/write all three numbers with exactly 2 decimal places using `printf("%.2f", ...)` into a JSON literal matching the given key order, then compile with `gcc -o bootstrap_ci bootstrap_ci.c -lm` and run it to produce `/home/user/result.json`.
