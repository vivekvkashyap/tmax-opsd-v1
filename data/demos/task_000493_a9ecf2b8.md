Build this as a single, deterministic Python script rather than exploring interactively — the grader checks exact rounded values, so order of operations matters. Follow the pipeline literally: (1) read the CSV, impute each of the five feature columns' NaNs with that column's own median (compute median once, ignoring NaNs, before winsorizing); (2) winsorize by computing each column's 99th percentile via `Series.quantile(0.99)` and clipping values above it to that exact percentile (use `df[col].clip(upper=p99)` or `np.where`, applied per-column, not globally); (3) fit `StandardScaler` on the five cleaned columns, then `PCA(n_components=2, random_state=42)` on the scaled output — do not skip standardization, it changes the explained-variance ratios; (4) fit `GaussianNB` on the 2 PCA components against `crashed`, using the full dataset for both fit and prediction (no train/test split); (5) get `predict_proba(...)[:, 1]` and pass it with the true labels to `brier_score_loss`. Round every float to exactly 4 decimal places with Python's `round()` before writing JSON, and write only the three specified keys to `/home/user/report.json`. Avoid common pitfalls: computing the median after winsorizing (wrong order), forgetting `random_state=42` on PCA, or rounding at a different precision.
