Build and test each layer independently before wiring the full server, since this is a multi-language integration (protobuf/gRPC, C FFI, C++ property test, audio transcription). First define `build_gate.proto` with the exact service/RPC/message names and field types specified, then generate the C++ stubs with `protoc`/`grpc_cpp_plugin` and confirm they compile standalone. For the FFI wrapper, link against the provided `/app/lib/libecc.so` using its header at `/app/include/ecc_lib.h` (via `dlopen`/`dlsym` or direct linking with `-lecc -L/app/lib`), and write `EccValidator` to read a file's bytes and call `calculate_asset_crc`. For the RapidCheck test, generate arbitrary byte vectors, skip the empty-vector case, and assert the checksum changes after appending a single `0` byte — build it to `test_ecc_bin` and run it to confirm it passes before moving on. For the gRPC server, shell out to `/usr/local/bin/whisper -m /app/models/ggml-base.bin <file>` (or equivalent CLI flags — check `whisper --help` for exact usage) to get the transcript rather than guessing its output format, compute the CRC via your validator, and set `is_valid` from `checksum > 0`. Start the server on `127.0.0.1:50051` in the background, verify it's listening, then write the `SERVER READY` log line last so the automated checker doesn't poll prematurely.