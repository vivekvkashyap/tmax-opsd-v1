Treat `/app/legacy_auditor` as a black box: feed it small hand-crafted CSVs (a straight chain, a diamond with two equal-length paths, duplicate grantor->grantee edges with different timestamps/amounts, disconnected target, zero/negative amounts) and observe the exact output for each, using `strings`/`objdump`/`strace` only to confirm hypotheses. Test hypotheses one at a time: is traversal BFS (fewest hops) from `ADMIN`? On tied path lengths, is the tie-break lexicographic on node names? On duplicate edges, is the min or max timestamp kept, and does it affect the "risk score" separately from which edge is in the graph? Is the risk score the max, sum, or last `amount` along the winning path's edges? Confirm each rule with more probes before writing `/home/user/new_auditor.sh`. Implement the equivalent in `awk`: build adjacency, run BFS with lexicographic tie-breaking, then compute the score — matching the exact output format (including the "No path found" case) byte-for-byte. Diff your script's output against the binary's on many random CSVs before finishing.