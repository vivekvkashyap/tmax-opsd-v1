Start both dependency services first (`/app/log_service/start.sh` and `/app/auth_service/start.sh`), and probe their actual responses (curl `GET /logs` and `POST /validate`) rather than assuming exact JSON shapes. Build your gateway as: on each request, read the `Authorization: Bearer <token>` header, forward the token to the auth service's `/validate`, and map its 403 to your own 401 (do not just proxy the auth service's status code blindly — the spec requires a specific translation). On success, fetch `/logs`, build an adjacency map from `waiting_for` relationships, and detect cycles via DFS with a recursion/visited stack (standard "cycle detection in directed graph" — track nodes currently on the DFS path to catch back-edges). For each cycle found, extract just the node IDs in the cycle, sort them alphabetically to canonicalize, then deduplicate identical canonical cycles (since DFS from different start nodes can rediscover the same cycle). Sort the overall cycle list lexicographically by element sequence, then slice by `page`/`limit` (1-indexed) for the paginated `data` array, and return the exact JSON shape specified including `total_deadlocks` as the count of unique cycles. Launch all three services in the background (e.g. with `&` or nohup) and verify they stay up so the verifier can hit port 9000.