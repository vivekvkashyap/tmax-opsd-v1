First fix the vendored build: run `make` in `libstatscsv-1.0`, read the compiler error carefully — an implicit-function-declaration error on `malloc`/`free`/similar under `-Werror` almost always means a missing standard header (`stdlib.h`) in the `.c` source; add the include rather than removing the `-Werror` flag, then confirm `libstatscsv.a` builds. For the detector, link against it and use its tokenizer to split each row, but crucially inspect the raw token string for the "score" column BEFORE calling any numeric-conversion function — check for literal `NaN`, `N/A`, or empty-string tokens explicitly (case-sensitively as specified) and reject immediately if found, since `atoi`/`strtol` would otherwise silently coerce garbage to 0. For valid numeric datasets, implement bootstrap resampling: 1000 iterations, each drawing `n` samples with replacement (uniform random index selection) from the score column, computing the resample mean, then sort the 1000 means and take the 2.5th/97.5th percentile as the CI bounds; reject if the interval isn't fully contained in [45.0, 55.0]. Validate against both corpus directories until 100% classification.
