Run `tesseract /app/config_spec.png stdout` (or `tesseract img out` then read `out.txt`) to recover the exact backend socket path (`unix:/...`) and the numeric rate-limit threshold from the image — treat these as unknowns you must read off OCR output, not values to assume. Use the extracted socket path to build `/home/user/nginx_fixed.conf` by copying `nginx.conf` and replacing only the incorrect `server unix:/tmp/wrong.sock;` line with the correct one (`sed` or a text edit). For `harden.sh`, read `$LOG_PATH`, and per calendar-second bucket, count requests per IP — a robust bash approach is to extract the IP and the bracketed timestamp (down to the second) from each log line with `awk`/`grep -oP`, then pipe `ip,second` pairs through `sort | uniq -c` to get counts, and flag any IP whose count for any single second strictly exceeds the OCR'd threshold. Deduplicate the matching IPs (e.g. `sort -u`) before emitting one `ufw deny from <IP>` line per IP into `/home/user/firewall.sh`, then `chmod +x` it. Since scoring is F1-based, prioritize getting the second-bucketing and threshold comparison exactly right over cosmetic script style — false positives/negatives from misparsed timestamps directly hurt the score.
