Build this with networkx primitives rather than hand-rolled graph code, since correctness of cycle-finding and centrality is easy to get subtly wrong by hand. Load `transactions.csv` with pandas and construct the graph via `nx.from_pandas_edgelist(df, 'source_id', 'target_id', create_using=nx.DiGraph())`, ignoring amount. Enumerate cycles with `nx.simple_cycles(G)` and keep only those with `len(cycle) in (3, 4)`; union all nodes appearing in any kept cycle into your candidate set. Compute `nx.betweenness_centrality(G)` with default parameters over the *entire* graph (not just the candidate subgraph — centrality must reflect full graph structure), then filter to the candidate nodes, round each centrality to 4 decimals, and sort by `(-centrality, entity_id)` so ties break alphabetically ascending. Take the top 3 and write them as a list of `{"entity_id":..., "centrality":...}` dicts to `/home/user/cycle_centrality.json`, matching the key names and structure exactly.
