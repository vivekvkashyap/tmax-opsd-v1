For the race condition, read `kv_store.cpp` and compare `put()` against `get()` — if `get()` takes `mtx` via `std::lock_guard` but `put()` mutates the underlying `std::map` without acquiring the same mutex, that's the bug (concurrent `map` mutation without exclusion causes iterator/rebalancing corruption and crashes); add the missing lock in `put()` using the same mutex, sized to cover the whole critical section. For WAL recovery, don't assume the file is well-formed — write a parser that scans byte-by-byte for `0xAA`, then reads the key-length byte, key bytes, value-length byte, value bytes, and checks the very next byte is `0xBB`; only emit an entry if that full sequence is intact, and resynchronize by continuing the scan for the next `0xAA` on any mismatch rather than aborting. Sort recovered `key=value` pairs alphabetically by key before writing. For the regression test, build `stress_test` from the fixed source with your Makefile, run it (possibly multiple times or under `-fsanitize=thread` for extra confidence), and have the bash script check its exit code, exiting 0 only if the stress test itself succeeded.
