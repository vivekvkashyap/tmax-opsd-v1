After extracting the tarball, first identify each file's actual encoding empirically rather than assuming — run `file hr_data.csv` and `file finance_data.json`, or try decoding with candidate codecs and see which produces valid accented characters / valid JSON (the task description tells you hr_data.csv is Windows-1252 and finance_data.json is UTF-16, but confirm with `hexdump -C | head` if unsure, e.g. UTF-16 files typically show a BOM or null-byte-interleaved pattern). In Python, open each with `open(path, encoding=<source-encoding>)`, parse (csv.DictReader for the CSV, json.load for the JSON array), then write out with `open(out, 'w', encoding='utf-8')` and `json.dumps(obj, ensure_ascii=False)` per line so accented/currency characters survive round-trip — a common bug is leaving `ensure_ascii=True` (the default) which escapes non-ASCII instead of preserving real UTF-8 chars, or forgetting explicit `encoding='utf-8'` on the write side. Count records as you write for the manifest. Zip the normalized directory (`shutil.make_archive` or `zipfile`), then launch `python3 -m http.server 8123 --directory /home/user/final &`, capture `$!`, and write it to server.pid — verify with `curl -o /dev/null -w '%{http_code}' http://localhost:8123/backup_v2.zip` before finishing.