Split this into the config fix and the transform script, and test the script standalone with hand-crafted input before worrying about the services. For the config, edit `/home/user/emitter.json` and `/home/user/dashboard.env` exactly as specified (redis host/port/URL) — after editing, you can sanity-check the pipeline by starting the services (e.g. via any provided startup script) and hitting the dashboard's health endpoint. For `/home/user/normalize.sh`, read stdin line by line, parse with `awk -F' \\| '` or similar to split timestamp/service/operation/key=value. Convert the timestamp to epoch with `date -u -d "$ts" +%s` (must use UTC — don't rely on local timezone). Keep only CREATE/MODIFY/DELETE operations; drop any line whose config_key matches SECRET case-insensitively (`grep -iv secret` or awk `tolower()`), being careful this filter runs before output, not after. Replace the value with its character length (`${#value}` in bash, or awk `length()`), watching out for edge cases like empty values or values containing `=`. Emit CSV `epoch,service,operation,key,length` with no extra spaces, then pipe everything through `sort -t, -k1,1n -k2,2` for the required ordering. Since verification is a bit-exact fuzz comparison against a hidden reference, test with adversarial inputs (odd whitespace, multiple `=` in value, boundary dates) to make sure your parsing is exact, not just "usually right".