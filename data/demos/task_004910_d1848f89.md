Recover the real port numbers from the image yourself before writing any service code — run `tesseract` (or another OCR tool) on `/app/network_topology.png`, inspect the raw extracted text carefully (OCR can misread digits like 1/l or 0/O), and use whichever GatewayPort/AppPort/DataPort values it actually reports rather than assuming any values from the task description. Once you have the three ports, build in the wiring order requests will actually flow: first start a background TCP/HTTP listener on DataPort that replies "DATA_ACK" to any input; verify it directly with `nc`/`curl` before adding forwarding. Then start `socat TCP-LISTEN:<AppPort>,fork,reuseaddr TCP:127.0.0.1:<DataPort>` and confirm AppPort alone round-trips DATA_ACK. Only then add `socat TCP-LISTEN:<GatewayPort>,fork,reuseaddr TCP:127.0.0.1:<AppPort>` and test the full two-hop chain end-to-end with a raw socket client, since that's exactly how the verifier probes it. For `check_connectivity.sh`, send a payload to GatewayPort, check for "DATA_ACK" in the response, and append "STATUS: OK" to `/home/user/monitor.log` on success; register it with `crontab -e` (user-level, `* * * * *`). Keep all background processes (socat x2, data service) alive/detached, not tied to your shell session.
