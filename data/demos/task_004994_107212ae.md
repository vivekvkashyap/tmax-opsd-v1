Structure the Go service as a solver function you can call repeatedly with different `dt`, plus a thin HTTP handler wrapping it. The solver: build the fixed 11-point initial condition `u(x)=4x(1-x)` for `x` from 0 to 1 in steps of 0.1, then apply explicit Forward Euler updates `u[i] += alpha*dt/dx^2*(u[i-1]-2u[i]+u[i+1])` for interior points only, holding `u[0]=u[10]=0` fixed at every step, stepping until you reach `t_end` (be careful the number of steps is an exact integer — `t_end/dt` should divide evenly given the halving scheme, or round consistently). Implement convergence as a loop: solve with `dt=0.05`, then `dt/2`, compare max abs difference across all 11 points between consecutive solutions, and keep halving until that max diff is `<= 1e-4` — return the finer of the last compared pair as your final answer. Watch the stability limit `dt <= dx^2/(2*alpha)`; the loop naturally handles this by refining, but starting far above the stability limit can blow up before you even get a comparison, so make sure early iterations don't produce NaN/Inf. Push the final 11 floats as strings via `RPUSH solution:<id>` using a simple Redis client (raw RESP protocol or a small library), and return HTTP 200 only after the push succeeds. Test manually with `curl` and `redis-cli LRANGE` before considering it done.
