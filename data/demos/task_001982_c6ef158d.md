Write the Go mutator to decode JSON generically into `map[string]interface{}` (via `encoding/json`), so it handles arbitrary/malformed-shape input without panicking — always check type assertions (`v, ok := m["kind"].(string)`) rather than assuming structure, and only apply the Pod-specific transforms when `kind == "Pod"` and `spec.containers` is actually present and is a `[]interface{}`; for anything else, pass the JSON through byte-for-byte unmodified in structure (re-marshal it as-is). For each container map, check if `image` is a string ending in `:latest` (`strings.HasSuffix`) and replace just that suffix with `:staged`, then always append the fixed `qemu-vnc` container object to the containers array for Pod kind. Marshal the result with `json.Marshal` (which is already minified/compact) and write to stdout. Build with `go build -o /home/user/mutator mutator.go`, and test it manually by piping a few hand-crafted Pod/non-Pod JSON samples through it before wiring up `deployer.env` and `deployer.reload`, since correctness here is graded by fuzzing against a hidden oracle on 1000 random JSON shapes, so edge cases (missing fields, non-Pod kinds, nested unrelated JSON) matter as much as the happy path.