Treat this as three chained, independently-checkable stages. Phase 1: implement the standard second-order finite-difference discretization of u''-u=0 on a tridiagonal system (interior points: `(u[i+1] - 2u[i] + u[i-1])/h^2 - u[i] = 0`), solve it (e.g. `scipy.linalg.solve_banded` or a dense solve for small N), compare against the closed-form `sinh(1-x)/sinh(1)` pointwise, and loop doubling N from 10 until max abs error <= 1e-4 — verify your discretization is correct by checking the error decreases roughly as O(h^2) as N grows, which catches sign/indexing bugs early. Phase 2: compile `/home/user/generator.c` exactly as instructed (`gcc -O3 ... -lm`) and run it with your discovered N as the sole argument — do not hardcode N, derive it from Phase 1's actual convergence loop. Phase 3: for each of the 100 noisy rows compute MSE against the analytical solution at the same grid points, seed `numpy.random.seed(42)` immediately before the bootstrap loop (order matters for reproducibility), do exactly 10,000 resamples of size 100 with replacement, take means, then `numpy.percentile` at 2.5/97.5. Round both CI bounds to 6 decimals and write `converged_N`, `ci_lower`, `ci_upper` to `/home/user/result.json` in the exact key names shown.