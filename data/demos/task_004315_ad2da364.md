Implement the parser strictly per the binary spec, reading only from stdin with `fread`/`getchar` in binary mode — never assume text-mode line buffering. After verifying the 4-byte "DOCP" magic, read the little-endian uint32 file count, then loop that many times: read exactly 32 bytes for the filename (treat it as null-terminated but always advance the full 32 bytes regardless of embedded nulls), 1 byte type ID, and a little-endian uint32 compressed size, then read exactly that many bytes of RLE payload. Decode the RLE by walking the payload two bytes at a time, `[count][value]`, appending `value` repeated `count` times to an output buffer — watch for off-by-one loop bounds and make sure you stop consuming payload bytes exactly at `compressed_size`, not at EOF. Write each decoded file to `/home/user/extracted/<filename>`. After extraction, create the three by_type subdirectories, and for each extracted file use `symlink()` (or `ln -s` via a wrapper script) to place a same-named symlink into the correct subdirectory based on its recorded type ID — use absolute target paths so `[ -L ... ]` and dereferencing both work. Finally collect the absolute symlink paths, sort them alphabetically, and write them one per line to manifest.txt. Compile with plain gcc and no external libraries, then run your tool with `./doc_extractor < /home/user/legacy_docs.bin` to produce all outputs.
