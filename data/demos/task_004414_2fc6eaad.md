Tackle this in order since later steps depend on earlier ones. First, diagnose SSH: read `/home/user/.ssh/config` for a directive under `Host *` disabling pubkey auth (e.g. `PubkeyAuthentication no`) and fix/remove it, then verify `chmod 600` on `id_rsa` and that its public key is present in `~/.ssh/authorized_keys`; confirm with `ssh -q localhost echo OK` before moving on — silent failures usually mean a config directive or wrong permissions, so check `ssh -v` output if it still fails. Second, transcribe `/app/capacity_request.wav` (via `ffmpeg` to extract/convert audio plus a speech-to-text tool) to determine the actual target service name spoken by the engineer — don't just default to the example name given in the prompt without trying to decode it. Third, once SSH works, pull `/var/log/services/<target_service>/` logs (now that you know the real service name) and set up a simple logrotate config for `usage.log` (daily rotation) as a secondary deliverable. Finally, parse the `TIMESTAMP CPU_PERCENT MEM_PERCENT` lines and fit an ordinary least-squares line to (timestamp, CPU_PERCENT) — e.g. compute slope/intercept via the standard sum-based formulas or `numpy.polyfit` in an awk/python helper called from your bash script — and evaluate it at timestamp 100, writing just that float to `/home/user/prediction.txt`.