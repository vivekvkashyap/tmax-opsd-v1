Read `build_lookup.py` and `mobile_emulator.py` to confirm the mechanism before touching anything: `mobile_emulator` monkey-patches `math.sin` at import time only when `TARGET_ARCH=arm64`, but `build_lookup.py` currently does `from math import sin` before `import mobile_emulator`, so it binds the name `sin` to the original unpatched function — importing `mobile_emulator` afterward patches `math.sin` in the `math` module's namespace but does not retroactively change the already-bound local `sin` reference. The fix is to reorder so `import mobile_emulator` happens first (triggering the patch), then `from math import sin` (or better, call `math.sin` dynamically rather than binding a local alias, so ordering can't silently break it again). Don't touch `test_lookup.py` or `mobile_emulator.py`. After reordering, actually run `TARGET_ARCH=arm64 python build_lookup.py` to regenerate `lookup_arm64.h`, then run `TARGET_ARCH=arm64 python test_lookup.py` and confirm it prints `PASS` and exits 0 — don't hand-construct the header file's contents.