Apply classic delta debugging: instead of reading the whole file, write a small driver that calls `process_data.py`'s stddev function (or reimplements the same naive formula) on each line of `/home/user/data_inputs.txt` one at a time inside a try/except, and record the first line number where `math.sqrt` raises `ValueError` on a negative argument — write that integer alone to `/home/user/buggy_line_num.txt`. Once found, open the actual file at that line number, copy the exact numbers into a standalone `/home/user/mre.py` that hardcodes them as `data = [...]` floats, re-implements the same buggy variance formula (`sum(x**2 for x in data)/n - mean**2`, which loses precision via catastrophic cancellation when values are large and close together) and calls `math.sqrt` on it so running the file alone reproduces the identical crash with zero external file reads. Then compute the numerically stable population stddev of that same list — use a two-pass or Welford-style algorithm (mean first, then mean of squared deviations from the mean, avoiding subtracting two large nearly-equal numbers) — and write the result rounded to 4 decimal places to `/home/user/stable_result.txt`. Verify the stable method and the naive method agree on well-conditioned inputs, confirming the bug is precision loss, not a logic error.
