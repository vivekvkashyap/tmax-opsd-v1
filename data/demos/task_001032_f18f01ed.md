Treat `/app/legacy_detector` purely as a black-box oracle since it's stripped/packed — do not try to disassemble it. Instead craft small, targeted edge-list test files (e.g. a single edge, a 2-cycle, a 3-cycle A->B->C->A, a 3-node chain with no cycle, a 4-cycle) and pipe each into `/app/legacy_detector < test.txt; echo $?` to observe which structural pattern flips the exit code from 0 to 1. Vary the motif systematically (cycle length, presence of extra unrelated edges) to pin down exactly what graph property triggers "evil" classification. Once you've deduced the rule, implement equivalent detection efficiently in `/home/user/detect_papermill.sh` — loading the edge list into an in-memory SQLite table and doing a self-join query (or an awk-based adjacency check) is far more robust on large graphs than trying to mimic the oracle's fragile memory behavior. Make the script executable, ensure it takes the file path as `$1`, and exit 0/1 to match the oracle's semantics precisely, then validate against your own crafted clean/evil test cases before trusting it on the hidden corpus.
