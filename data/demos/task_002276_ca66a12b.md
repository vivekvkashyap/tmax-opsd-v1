Start by recovering BIND_PORT and AUTH_TOKEN from `/app/config_snapshot.png` with `tesseract` (or careful visual reading) — do not hardcode assumed values. Next, automate the interactive `/app/legacy_admin_cli` login with an `expect` script (spawn the binary, expect the "Username:" and "Password:" prompts, send the known admin/legacy2024 credentials, then capture all subsequent output) and parse the `key:ip_address` lines into `/home/user/routes.txt`. For the daemon, write `/home/user/start_daemon.sh` that first kills any prior `socat` processes bound to the port (e.g. `pkill -f "socat.*<port>"`), then launches `socat TCP-LISTEN:<port>,fork,reuseaddr EXEC:/path/to/handler.sh` (or a system() call to a bash request-handler script) in the background with `&`/`disown` so the script itself returns immediately rather than blocking. The handler must read the HTTP request line and headers from stdin, parse the method/path/query-string and the `X-Auth-Token` header, and write a correctly formatted HTTP response with `\r\n` line endings for `/health`, `/lookup?service=...` (403 on bad/missing token, 404 on unknown key looked up from `routes.txt`, else 200 with the IP). Write `/home/user/monitor.sh` to `curl` `/health` and exit 0/1 based on success. Test each endpoint manually with `curl` before finishing, and confirm the daemon is still listening (e.g. `ss -tlnp`) after your script returns.
