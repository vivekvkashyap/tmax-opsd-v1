Treat this as three independent numeric computations glued together by a bash entrypoint that can shell out to Python for the math. (1) MLE fit for an Exponential distribution is just the sample mean; from that, analytically invert the CDF: solving `1 - e^{-x/mean} = 0.99` for x gives `x = -mean * ln(0.01)` — implement this formula directly rather than searching/optimizing for it, and do it for both datasets. (2) Compute the 1D Wasserstein distance between the two raw (unfitted) empirical samples using `scipy.stats.wasserstein_distance(baseline, new)` — pass the raw arrays, not the fitted distributions. (3) Apply the regression rule exactly as specified (strict `>` on both p99 comparison and Wasserstein vs 2.00, AND logic) and print the four required lines with values rounded to exactly two decimals using something like Python's `f"{value:.2f}"` (careful with rounding-mode edge cases at the .005 boundary). Make the script executable with `chmod +x`, and run it standalone with no arguments to confirm the exact four-line output format before finishing.