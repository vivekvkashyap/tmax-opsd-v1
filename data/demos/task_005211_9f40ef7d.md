Structure the Go program as a clear pipeline over the JSONL lines. Parse `timestamp` as either an RFC3339 string or a raw integer (try `time.Parse(time.RFC3339, ...)` first, and if that fails treat the field as a JSON number and use `time.Unix(sec, 0).UTC()`) — decoding into `interface{}` for that field lets you detect which case you're in. Immediately drop any record with `allocation < 0 || allocation > 10000` before any aggregation. Restrict to timestamps within `2023-10-01T00:00:00Z`..`23:59:59Z`, and for each (service, hour) pick the allocation from the chronologically latest valid record in that hour — so sort or scan in time order per service and overwrite the bucket each time a later record in the same hour appears. After collecting whatever hourly buckets have real data (0-23), fill any empty bucket by carrying forward the nearest previous hour's value in ascending hour order, defaulting to 0 if nothing precedes it yet. Only emit services that had at least one valid (non-dropped) record. Compute min/max over the 24 filled buckets and avg rounded to 2 decimals (careful with Go's float rounding — use something like `math.Round(x*100)/100`), then marshal with `json.MarshalIndent` for pretty output and write to `/home/user/summary.json`. Remember Unicode service names need no special handling in Go's JSON encoder beyond leaving `SetEscapeHTML(false)` if emoji get mangled.
