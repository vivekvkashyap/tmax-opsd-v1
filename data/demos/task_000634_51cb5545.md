Start by reading `/app/vendor/urlesc/urlesc.go`'s `Unescape` function line by line — the task states the `+`-to-space conversion was "disabled or removed," so look specifically for a commented-out or deleted `strings.Replace(s, "+", " ", -1)`-style line (or an equivalent conditional that was neutered) and restore it; don't rewrite the whole unescaping logic from scratch, since other parts of the package may be relied upon as-is. For `/home/user/investigate.go`: initialize a Go module in `/home/user` (`go mod init ...`), add a `require`/`replace` directive in `go.mod` pointing `github.com/PuerkitoBio/urlesc` at the local `/app/vendor/urlesc` path so it compiles against your fixed copy rather than a network-fetched version. Read all of stdin (`io.ReadAll(os.Stdin)`), call the package's unescape function on the whole string, then parse it as a URL query string with `net/url`'s `ParseQuery` (note this itself also treats `+` as space and does its own percent-decoding — make sure your manual unescape step and `ParseQuery`'s own decoding don't double-unescape or conflict; test carefully with values containing literal `%2B` vs `+`). Extract `payload`, print it with `fmt.Print` (no newline) if present, else print exactly `ERROR` on any missing param or error condition — wrap all fallible steps in error checks that funnel to that same `ERROR` output rather than panicking. Since your binary will be diffed byte-for-byte against a hidden oracle across many fuzzed inputs, write your own varied test cases (double-encoded values, missing `payload`, embedded `+` and `%2B`, malformed percent sequences) and check exact output before considering it done.