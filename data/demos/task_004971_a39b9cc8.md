Structure the C++ program as: parse CSV, filter, then shortest-path search — keep filtering strictly separate from traversal so you don't accidentally allow a path to "complete" through a non-PII edge. When parsing `data_tags`, check for the exact substring "PII" within the field (e.g. `field.find("PII") != string::npos`) rather than exact-equality, since some tags may combine with other text, but be careful not to match an edge whose ONLY tag is something else that happens to contain similar characters — read each row's tag value precisely. Build an adjacency list containing only edges that passed the PII filter, weighted by the `latency` column parsed as a number. Run Dijkstra's algorithm (or, given a small graph, even BFS/DFS with weight comparison) from `Web` to `ProfileDB` over this filtered subgraph only — a common mistake is filtering only at the final path level instead of at graph-construction time, which can let a mixed-tag path sneak through. Track predecessor pointers to reconstruct the actual node sequence, sum edge weights along that path, then write the result to `/home/user/compliance_report.txt` matching the exact literal text format `Violation Path: Node1->Node2->..., Total Latency: X`. Compile with g++ and actually execute the binary so the report file is generated, not just left as source.