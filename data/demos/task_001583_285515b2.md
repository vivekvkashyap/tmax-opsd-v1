First understand the oracle's exact semantics by inspecting `/app/graph_oracle`'s behavior on small hand-crafted CSVs and queries (it's slow but correct) so your C implementation matches it precisely, not just plausibly. Parse `/home/user/data/edges.csv` once at startup: split each line on commas (being careful the JSON field itself may contain commas inside braces, so parse the first three comma-separated fields normally then treat the remainder as the JSON blob), extract the `"penalty"` numeric value with simple string scanning (find `"penalty":` then parse the following number — no full JSON parser needed), and compute `effective_weight = base_weight * penalty` once per edge. Build an adjacency list indexed by node id (map arbitrary node ids to dense array indices with a hash map or sorted array, since ids may not be contiguous from 0). For each query pair, run Dijkstra with a binary min-heap over the prebuilt adjacency list — do not rebuild any graph structures per query, since avoiding that O(Q*E) reparsing is exactly what gives the required speedup. Format each result to 2 decimal places, `-1.00` when unreachable, one per line. Validate correctness by diffing your output against the oracle's output on the same small inputs before trusting it on the large held-out set, then separately confirm timing headroom (aim well past 50x, since the oracle re-parses the CSV per query while your program parses once).