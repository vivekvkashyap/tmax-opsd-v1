Start by diagnosing the build failure directly rather than guessing: `cat /app/vendored/fast-tar/Makefile` — look for a hardcoded cross-compile target (e.g. `GOOS=darwin`) that doesn't match this Linux environment, and fix it to build natively (`GOOS=linux GOARCH=amd64` or simply unset GOOS/GOARCH and let Go infer the host), then `make build` or run `go build` directly inside that module to produce the `fast-tar` binary. Next, index `/home/user/raw_data/`: recursively walk the tree, compute SHA256 per file (`sha256sum` in bash, or Go's `crypto/sha256`), and record each file's path *relative to* `raw_data/` (not absolute) into the exact JSON schema specified — build this with a real JSON encoder rather than string concatenation to avoid escaping bugs. Use the fixed `fast-tar` binary to archive `raw_data/` into `backup.ftar` per its documented usage. Finally write a Go HTTP server with three explicit routes (`/ping`, `/index`, `/download`, the last with an explicit `Content-Type: application/octet-stream` header set via `w.Header().Set`), listening on `0.0.0.0:9090`, and launch it detached/backgrounded (e.g. `nohup ... &` or a systemd-style disown) so it survives the invoking shell exiting — then smoke-test each route with `curl` before finishing.