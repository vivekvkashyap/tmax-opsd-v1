Build this as a TCP server in C with distinct pipeline stages, and treat the legacy binary purely as a black-box subprocess you pipe cleaned CSV into. (1) Accept a connection, read and validate the exact literal auth line `ETL_AUTH: VALID_STR_8472\n` before doing anything else — close the socket immediately on mismatch. (2) Parse incoming CSV correctly handling RFC4180-style quoting: a record can span multiple physical lines if it's inside an unclosed double-quote, so track quote state character-by-character rather than splitting naively on `\n`. (3) Before forwarding to `/app/sensor_processor`, strip/replace embedded newlines inside the `Sensor_Notes` field (e.g. with a space) so the legacy binary's newline-per-record assumption isn't broken — but retain the original (cleaned) notes text and correlate it by timestamp so you can re-attach it later, since the legacy tool drops the notes column entirely in its wide-format anomaly output. (4) Spawn `/app/sensor_processor` via `fork`/`exec` with pipes, write cleaned CSV to its stdin, read its wide-format epoch-timestamped anomaly output from stdout. (5) Convert each Unix epoch back to ISO8601, reshape wide-to-long (one row per sensor per anomaly row), re-attach the matching cleaned notes, and stream the long-format CSV back to the client, ending with `EOF_PIPELINE\n`. Compile to `/home/user/etl_server` and run it persistently in the background bound to 127.0.0.1:9090.