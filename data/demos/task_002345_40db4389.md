First extract the numeric threshold from `/app/policy.wav` using speech-to-text (e.g. `whisper`, or any available offline ASR tool — install one if needed) rather than guessing a round number; transcribe and read off exactly what value is spoken as the "maximum allowed deviation threshold." Then write `sanitiser.c` to stream through the CSV line by line, maintaining a small rolling buffer of the last up-to-3 `metric_value`s (a simple circular buffer of size 3 works, tracking how many are filled so far for rows 1 and 2 where the window is smaller). For each row, compute the average of the values currently in the window (including the current row's own value, per the spec), take the absolute difference between the current value and that average, and compare it against the transcribed threshold using strict `>` — reject (`exit(1)`) immediately on the first violation, otherwise continue and `exit(0)` after the last row. Validate your rolling-window logic by hand against one clean and one evil example CSV before running the full corpus in `/app/corpus/clean/` and `/app/corpus/evil/`, checking every file's exit code matches expectations.