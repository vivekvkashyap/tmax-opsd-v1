Step 1: edit `/app/vendor/regex-1.10.4/Cargo.toml` and restore the package name to `name = "regex"` so the crate resolves under its real name. Step 2: `cargo new /home/user/audit_tool` (binary), and in its `Cargo.toml` add `regex = { path = "/app/vendor/regex-1.10.4" }` so the build uses the vendored crate offline. Implement `main` to read stdin line by line (`io::stdin().lock().lines()`). For each line apply the rules in this exact order so combined cases match: (1) if the line `contains("CWE-79")` or `contains("CWE-89")`, prepend the literal `"[ALERT] "`; (2) build one `Regex::new(r"(pin=)\d+")` and use `replace_all` with `"${1}XXXX"` to blank only the digits after `pin=`. Print the result with a single trailing newline (`println!`). Aim for bit-exact stdout equivalence with the legacy behavior — mind edge cases like multiple `pin=` matches per line and empty lines. Compile with `cargo build --release` so the binary lands at `/home/user/audit_tool/target/release/sanitizer`. Do not inspect or invoke the reference oracle binary; derive behavior from the spec.
