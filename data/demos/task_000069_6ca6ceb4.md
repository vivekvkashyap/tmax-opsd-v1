In `/home/user/analyzer.cpp`, replace the blind `.value()` calls on the three `std::optional<long>` fields with explicit `.has_value()` checks: if any of `time_a`, `time_b`, `time_c` is missing for a transaction, classify it as "Failed" (not converged, and don't attempt to compare timestamps). For transactions where all three exist, add the monotonicity assertion `time_a < time_b < time_c`; if it holds, count the transaction as "Converged", otherwise classify it as a "Violation" — track failed and violation transaction IDs in separate vectors as you iterate. "Total" should be the count of unique transaction IDs seen in `service_a.log` specifically (parse that file separately or track origin per-service), not the union across all three logs. After classification, sort the failed and violation ID vectors alphabetically (`std::sort`), join with `", "`, and use `"None"` when a list is empty. Write the four lines to `/home/user/report.txt` in the exact specified format, then compile with `g++ -std=c++17` and run against the provided logs to sanity check row counts before finalizing.
