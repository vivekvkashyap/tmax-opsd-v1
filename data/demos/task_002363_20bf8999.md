This is a data-leakage bug fix: the core method is to reorder the pipeline so scaling parameters are derived only from training data. First split the raw (unscaled) data into train (rows 0-7) and test (rows 8-9), then compute min/max for each feature using only the training rows. Apply `(value - train_min) / (train_max - train_min)` to both the training rows and the test rows using those same train-derived min/max values — do not recompute min/max after splitting, and do not include test rows when computing min/max. Once both sets are scaled consistently, compute Euclidean distance between the first scaled test sample and each of the 8 scaled training samples, track the minimum, and write the winning training index (0-7) plus the distance formatted to exactly 4 decimal places (e.g. via Rust's `{:.4}` format specifier) as `index,distance` to the output file. Double-check edge cases: use `f64` throughout to avoid precision loss, and verify your distance formula sums squared differences across both features before taking the square root. Recompile and rerun the Rust binary after editing `main.rs` to regenerate the output file.
