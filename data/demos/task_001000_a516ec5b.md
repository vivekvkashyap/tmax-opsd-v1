Write etl.cpp handling each requirement as a discrete parsing/filtering stage. Read the log line by line, splitting on ` | ` to get the timestamp, sensor id, temperature, and the `status="..."` field; parse the ISO8601 timestamp (e.g. with `sscanf` on the `YYYY-MM-DDTHH:MM:SSZ` pattern) and floor the seconds field to `:00` while keeping the rest unchanged to align to the minute. Parse temperature as float and drop the record if it is strictly less than -20.0 or strictly greater than 60.0 (use strict inequality, not <=/>=, matching the spec). For the status field, extract the text between the quotes after `status=`, then strip every `\uXXXX` occurrence (a literal backslash, 'u', and 4 hex digits — 6 characters total) by scanning for the `\u` prefix followed by 4 hex chars and removing that whole substring, rather than trying to decode/interpret it. Write the CSV header exactly as `timestamp,sensor,temperature,status`, then one row per surviving record with temperature formatted via `%.1f`. Compile with the exact g++ command given, run it, then diff your output against your own hand-traced expectation of the five sample log lines (mentally apply the temp filter and unicode strip) before considering it done.