Build this as a straight pandas pipeline in the exact order specified, since reordering steps changes the result. Load the CSV with `parse_dates=['timestamp']`, filter to `sensor_id == 'A101'`, then `drop_duplicates()` on the filtered rows. Set `timestamp` as the index and use `.resample('1H').mean(numeric_only=True)` on `temperature_c` to get one row per hour bucket spanning the full min-to-max range (resample automatically inserts NaN rows for empty hours — don't manually loop over hours). Apply `.ffill()` on the resampled series so any hour with no readings inherits the previous hour's mean (verify there's no leading NaN, since ffill can't fill a gap at the very first bucket). Round to 2 decimals with `.round(2)`, then reset the index and format the `timestamp` column to `YYYY-MM-DD HH:MM:SS` strings (e.g. `dt.strftime('%Y-%m-%d %H:%M:%S')`) before writing to `/home/user/hourly_summary.csv` with `to_csv(index=False)`, matching the exact lowercase header `timestamp,avg_temp`.