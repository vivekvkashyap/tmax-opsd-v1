Tackle each subsystem independently and test it standalone before wiring it into `run.sh`. For the fstab parser, use `awk`/`grep` to filter lines where the filesystem-type column is `ext4` or `xfs` (skip comments/blank lines), printing only the mount-point column to `/home/user/active_mounts.txt`. For frame extraction, `mkdir -p /home/user/data/frames` then run `ffmpeg -i /app/test_video.mp4 -vf fps=2 /home/user/data/frames/frame_%04d.jpg` — verify the frame count matches what `ffmpeg -i ... -vf fps=2 -f null -` reports before moving on, since the Rust service must report this count dynamically (not hardcoded) by counting files in that directory at request time. For the Rust service, use `cargo new`, implement a raw `TcpListener` loop parsing the request line/headers by hand (or a minimal crate), check the `X-Service-Auth` header against the required value and return 401 on mismatch, otherwise `fs::read_dir` the frames directory to count files and return the JSON body, and append one formatted log line per request (auth pass or fail) to `/home/user/logs/microservice.log`. Write the logrotate stanza with `size 1M`, `rotate 3`, `compress`, and a `create 0644 user user` directive. Finally have `run.sh` mkdir the logs dir, `cargo build --release` (or run), and background+detach the binary (`nohup ... & disown`) so it stays up after the script exits — then actually execute `run.sh` before finishing.