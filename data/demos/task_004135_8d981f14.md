Work stage by stage and verify each output before moving on. Compile `embedder.c` with `gcc -o embedder embedder.c`, then run it against `dataset_A.txt` and `dataset_B.txt` (redirecting stdout) to produce `embed_A.csv`/`embed_B.csv`, and spot-check a couple of rows by hand-counting letter frequencies in one input line to confirm the embedder behaves as documented (normalized a-z frequencies per line, ignoring non-letters). For `evaluate.c`: read all rows of `embed_A.csv` into memory, compute the column-wise mean vector (the centroid) across all rows; read `embed_B.csv` row by row, compute Euclidean distance of each row to the centroid; accumulate these distances to get the sample mean (mu) and sample standard deviation using N-1 in the denominator; compute the 95% CI as mu ± 1.96*s/sqrt(N). Compile with `gcc -o evaluate evaluate.c -lm` and run it to write `/home/user/etl/metrics.txt`. Match the output format exactly — six lines, `%.6f` precision, only Dim0 and Dim25 of the centroid printed, exact key names as specified. Careful with off-by-one errors in dimension indexing (0-indexed 'a' through 'z') and in reading CSV rows correctly (26 comma-separated floats per line).