Fix the vendored C package before writing any bash. In `/app/net-bridge-c/Makefile`, the gcc link line is missing threading support — add `-lpthread` to the compile/link command. In `src/gateway.c`, find where the listening port is hardcoded (a comment near it likely hints at the intended `getenv("BIND_PORT")` call) and replace the hardcoded value with logic that reads the `BIND_PORT` environment variable via `getenv()` and converts it with `atoi()`, falling back to the default only if the variable is unset. Run `make` and confirm the `gateway` binary is produced and that starting it manually with `BIND_PORT=8888 ./gateway` actually binds port 8888 (check with `ss`/`curl` before moving on). Next, create the required directory tree and the symlink (`ln -s conf/settings.ini /home/user/gw/active_conf`) exactly as specified. For the supervisor script, structure it as `while true; do <start gateway with CONFIG_PATH and BIND_PORT env vars, redirecting stdout/stderr to gw.log, capture $! to gw.pid>; wait $!; cp gw.log gw.log.bak.$(date +%s); done` — the key correctness point is capturing the PID immediately after backgrounding the process and using `wait` on that specific PID so the loop only restarts after a genuine exit, and copying (not moving) the log so a fresh one starts accumulating for the next run.
