Build `/home/user/processor.cpp` as a true line-at-a-time stream filter: read with `std::getline(std::cin, line)` in a loop, parse each line independently with `nlohmann::json::parse`, and immediately discard/continue on any parse error or missing-field exception rather than buffering the whole stream — memory efficiency requires no accumulation across lines. Apply validation strictly in this order: all four keys present, `value` numeric and within `[-50.0, 150.0]` inclusive, and `meta` matching `^[a-zA-Z0-9_]+$` via `std::regex` (reject on any character outside that class, including whitespace and punctuation used in injection payloads). For passing records, format `value` to exactly two decimals (e.g. `std::ostringstream` with `std::fixed << std::setprecision(2)`) and emit the exact template string, flushing per line (`std::cout << ... << "\n"` with `std::endl` or explicit flush) so downstream consumers see data promptly. Compile with g++ including `nlohmann/json.hpp`. For `/home/user/run_pipeline.sh`, chain `nc localhost 9001 | /home/user/processor | nc localhost 9002`, make it executable, and test it manually against the source/sink services running on those ports before trusting the corpus-based grading. Validate your processor standalone first with `cat corpus_file | ./processor` against both clean and evil sample lines you construct.