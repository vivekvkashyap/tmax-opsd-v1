Start by transcribing `/app/voicemail.wav` (e.g. with a local speech-to-text tool) and write down the exact structural rules it states before coding anything — get the depth-counting convention (is root depth 0 or 1?) and the substring match (case sensitivity) precisely right, since a naive detector commonly gets the depth off-by-one or misses nested arrays/objects when recursing. Implement the detector as a recursive JSON walker that tracks current depth as it descends into both objects and arrays, checking each key name against the "forbidden key at depth >= N" rule and each string value (case-insensitively) against the forbidden substring rule; return exit code 1 the moment either rule triggers, else 0 after fully walking the structure. For the proxy, since it must inspect and possibly reject before forwarding, build a small custom middleware (Python/Node) that reads the full POST body, runs the same detection logic (reuse the detector's core function/logic rather than shelling out per-request if performance matters, though shelling out to `/home/user/detector` on a temp file also works), returns 403 immediately on a match, and otherwise proxies the request/response verbatim to `127.0.0.1:9090` using an HTTP client library. Test both the CLI and the proxy against your own clean/evil JSON samples before relying on the hidden corpus.
