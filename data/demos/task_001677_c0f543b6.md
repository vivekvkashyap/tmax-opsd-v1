Handle the three parts in order, verifying each independently. (1) Zip-slip fix: in `/home/user/extractor.c`, before the `sprintf(out_path, ...)`/`fopen` call, add a check on the raw `path` string that rejects (skips writing, but keeps parsing the rest of the mmap'd buffer at the correct offset) any path starting with `/` or containing the substring `../`, so the loop continues cleanly rather than crashing or writing outside the target dir — test this by confirming files like `../evil.txt` and `/absolute_evil.txt` do NOT appear anywhere outside `/home/user/extracted/` after running it. Compile with `gcc -O2` and run against `/home/user/docs.pkg` into a pre-created `/home/user/extracted/` directory. (2) Audio: transcribe `/app/doc_notes.wav` with any available STT tool, then normalize the text yourself (lowercase, strip all punctuation, collapse to single spaces between words) rather than trusting the raw transcript's casing/punctuation. (3) Assembly: locate the safely-extracted `draft.txt`, use `sed`/`awk` to substitute the literal placeholder `[DICTATION_INSERT]` with your normalized transcription, and write the result to `/home/user/final_doc.txt`. The grader compares this file's similarity to an expected string, so get the transcription and normalization as accurate as possible.