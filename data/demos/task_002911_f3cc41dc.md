This is a graph-traversal SQL problem: use a recursive CTE anchored on `role_permissions` rows matching `resource_name = 'SWIFT_PAYMENT_GATEWAY'`, then recursively join `role_hierarchy` upward from `child_role_id` to `parent_role_id` to find every role that transitively inherits that permission (a role in `role_hierarchy.parent_role_id` inherits from `child_role_id`, so walk from the permission-holding role outward via matching `child_role_id` to collect all ancestor `parent_role_id`s, union with the direct-permission role itself). Join the resulting role set against `user_roles`/`users` to get every user holding one of those roles directly (their own assigned role, not necessarily the one with direct permission). Then use a window function — `ROW_NUMBER() OVER (ORDER BY created_at DESC)` — to assign `audit_rank`, with rank 1 being the most recently created user. Write results to the JSON list with keys `audit_rank`, `username`, `assigned_role` in that rank order, and run the script so the file is actually produced — test your recursive CTE alone first (e.g. via sqlite3 CLI) to confirm it returns the expected role set before wiring up the full script.