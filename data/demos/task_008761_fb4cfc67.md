Write this as a streaming, regex-driven Python script rather than loading the whole file or attempting a strict JSON parse (the payloads are explicitly malformed JSON). Iterate `/home/user/events.csv` line by line with `csv.reader`/`DictReader` to keep memory bounded. For each row's `payload` field, don't call `json.loads` — instead use targeted regexes to pull `"browser"\s*:\s*"([^"]*)"` and `"duration"\s*:\s*(\d+)` directly out of the raw string, since malformed `\u` escapes elsewhere in the payload would otherwise blow up a real JSON parser. When the duration regex finds no match for a row, impute `30` rather than skipping the row. Accumulate a running `{user_id: total_duration}` dict and separately track the browser value from the most recently processed row per user (so "last seen browser" naturally falls out of iterating rows in file order and overwriting per user_id each time you see them). After the full pass, find the user_id with the max total duration and look up their last-seen browser, then write the report using the exact literal template string given, with no extra whitespace or trailing newline beyond what the template implies.