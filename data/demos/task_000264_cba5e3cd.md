Build this as a small ETL: parse `/home/user/etl.log` line-by-line to collect the set of JOB_IDs whose STATUS field is exactly `RETRY`. Then read `/home/user/events.csv` and keep only rows whose `job_id` is in that retry set AND whose `amount`, once validated as parseable as an integer, is `>= 100` — reject non-numeric or too-small amounts rather than crashing. Use `multiprocessing.Pool` genuinely (e.g. map a row-validation/filter worker function over CSV rows or chunks) rather than just importing the module unused — the checker greps the script source for the literal string `multiprocessing`. Deduplicate by `txn_id` (first occurrence per job is enough since amount doesn't vary across the duplicate retried rows), then sort the surviving transactions alphabetically by `txn_id` before writing them into the exact template at `/home/user/duplicate_report.txt` (mind the literal header lines and dash-prefixed format, including `Total Retried Jobs:` counted from the retry-job set size). Separately configure Python's `logging` module to write to `/home/user/pipeline.log`, emitting at least one INFO log at start and one at finish of the run.
