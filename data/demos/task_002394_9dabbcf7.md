First OCR the query pattern out of `/app/query_pattern.png` (e.g. `tesseract /app/query_pattern.png -`) to learn the exact Cypher-like traversal (which node labels and edge relationship types to follow, and in what order/direction) — implement whatever pattern you actually read from the image rather than assuming a generic two-hop traversal. In `query_tool.cpp`, use the SQLite C API to run a parameterized SQL query that joins `nodes`/`edges` twice (start node -> intermediate via one relationship type -> target via the next relationship type), filtering on the node `label` and edge `rel` values from the OCR'd pattern, binding the CLI's node id with `sqlite3_bind_int`. Because `idx_edges_src` is corrupted, explicitly avoid it — either `DROP INDEX IF EXISTS idx_edges_src` at the start of your program, or add `INDEXED BY` a different index / `NOT INDEXED` on the `edges` table in your SQL, and confirm with `EXPLAIN QUERY PLAN` that the corrupted index isn't chosen. Print only the resulting `name` values, one per line, sorted (`ORDER BY name` in SQL, or sort in C++). Compile with `g++ -o /home/user/query_tool /home/user/query_tool.cpp -lsqlite3` and test against a few different starting ids, comparing against your own manual trace of the schema, before finishing.