For `/home/user/filter_video.py`, extract frames (e.g. `cv2.VideoCapture` reading frame-by-frame, or `ffmpeg` dumping PNGs) and for each frame crop/inspect exactly the pixel block at x:0-9, y:0-9; treat a frame as containing the artifact only if that whole 10x10 region is (close to) pure red (255,0,0) — check all pixels in the block, not just a corner pixel, and allow a small tolerance for compression noise if reading from an MP4 (lossy encoding can shift exact RGB values slightly). Stop scanning and `sys.exit(1)` on the first hit; if you exhaust all frames clean, `sys.exit(0)`; wrap file-open/frame-read in try/except so a missing or corrupt file also fails gracefully rather than crashing. Reuse this per-frame detector (track the frame index) against `/app/incident_record.mp4` to find the first frame index where the artifact appears, and write only that integer to `/home/user/incident_report.txt`. Validate your script against every file in `/app/corpus/clean/` and `/app/corpus/evil/`, checking exit codes, before finalizing. For `/home/user/proxy.conf`, configure nginx to listen on 8080, `proxy_pass http://127.0.0.1:8081;`, and inside the relevant `location`/`server` block add `allow 127.0.0.1; deny all;` so only localhost can reach it.
