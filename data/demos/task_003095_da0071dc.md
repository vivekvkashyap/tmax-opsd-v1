First recover the filter key: run OCR on `/app/receipt_sample.png` (e.g. `tesseract /app/receipt_sample.png stdout`) and parse out the value following `TAX_REGION:` — inspect the raw OCR text carefully since OCR can introduce stray whitespace/case issues, and normalize before using it as a filter. For the Rust pipeline, read the JSONL file, and for each record first repair the double-escaped unicode in the description strings (a `\\u0026`-style sequence needs one level of unescaping back to a real `&`) before doing any regex matching, since matching against the still-escaped text will miss region tags. Reshape from wide format by iterating numbered `txn_N_amt`/`txn_N_desc` field pairs dynamically (don't hardcode a fixed N) into individual records, use a regex like `Region:\s*([\w-]+)` to pull the region code from each description, and keep only records whose region matches the OCR'd code exactly. Group by `merchant_id`, sum amounts, round to 2 decimals, and write `merchant_id,total_amount` CSV rows to `/home/user/summary.csv`. Since grading is by MaxAE against a reference, double check your rounding and unescaping don't shift totals by including/excluding a transaction incorrectly.
