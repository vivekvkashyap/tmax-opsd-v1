Two independent parts. Part 1: extract frames from `/app/warehouse_traffic.mp4` (ffmpeg or OpenCV) and count how many are *entirely* black — check that every pixel equals (0,0,0), e.g. `np.all(frame == 0)`, not just that some pixels are black; derive the count by scanning, don't assume a number. Use `sqlite3`/Python to `UPDATE edges SET weight = <counted_value> WHERE source='C' AND target='D'` on `/app/warehouse.db`. Then write a recursive CTE against the `edges` table to compute shortest path from A to F — the standard SQLite pattern is `WITH RECURSIVE paths(node, path, weight) AS (SELECT source, source, 0 FROM edges WHERE source='A' UNION ALL SELECT e.target, p.path||','||e.target, p.weight+e.weight FROM paths p JOIN edges e ON e.source=p.node WHERE p.path NOT LIKE '%'||e.target||'%') SELECT path FROM paths WHERE node='F' ORDER BY weight LIMIT 1;` — run it after the weight update so the changed edge affects the result, and write the winning comma-separated path string to `/home/user/shortest_path.txt`. Part 2: write `/home/user/query_filter.py` to flag SQL as unsafe if it contains unescaped single-quote string termination patterns, `OR 1=1`/similar tautologies, or literal value interpolation instead of `?` placeholders — test against the clean/evil corpora under `/app/corpus/` until you hit 100% on both before considering it done.