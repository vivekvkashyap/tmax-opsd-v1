Build this as a short shell/awk pipeline rather than a heavy program. Parse `mesh_graph.tsv` (tab-separated `Node1 Node2 Weight`) and find the row with maximum integer weight — `sort -t$'\t' -k3,3n` (or `-nr`) and take the last/first line, or use `awk` tracking a running max, being careful to compare weights as numbers not strings (lexicographic sort would misorder e.g. "9" vs "10"). From that winning row extract the two node IDs, then look each up in `primers.tsv` to get its 10-character sequence. Compute the Hamming distance by comparing the two equal-length strings position by position and counting mismatches (in bash/awk, iterate character indices with substring extraction; in Python, `sum(a!=b for a,b in zip(s1,s2))`). Print only the resulting integer to stdout with no extra whitespace or labels. Make the script accept the two file paths as `$1`/`$2` arguments (per the required invocation), `chmod +x` it, then actually run it and redirect stdout to `/home/user/metric.txt` as instructed, confirming the file contains just the number.