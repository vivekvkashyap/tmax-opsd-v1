Build the pipeline as a per-submission state machine in Bash, processing `<id>` prefixes in sorted alphabetical order (`ls submissions/*.meta | sort` or similar) and tracking a per-user "last accepted timestamp" in an associative array (`declare -A last_ts`). For each submission: (1) parse the three `KEY=value` lines from the `.meta` file (e.g. with `source` on a sanitized copy, or `grep`/`cut`); (2) rate-limit check — if the user has a prior tracked timestamp and `TIMESTAMP - last_ts[user] < 60`, mark `RATE_LIMITED` and skip everything else without updating `last_ts`; (3) otherwise compute `sha256sum` of the `.patch` file and compare to `CHECKSUM` — on mismatch mark `CHECKSUM_FAILED`, but still update `last_ts[user] = TIMESTAMP` since this step was reached; (4) on match, copy the baseline `src/` to a fresh `mktemp -d` directory, `cd` there, and run `patch -p1 < patchfile` — on failure mark `PATCH_FAILED`; (5) on success run `./math_utils.sh test` — non-zero exit means `TEST_FAILED`, otherwise `ACCEPTED`. Append exactly one `<id> <STATUS>` line per submission to `/home/user/ci_system/results.log`, and always `rm -rf` the temp directory afterward, even on failure paths (use a trap or explicit cleanup in each branch).
