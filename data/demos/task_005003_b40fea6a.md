Diagnose the Makefile and the SQL bug separately, since they cause different failure modes. For the build: inspect the `SRCS`/object list and `LDFLAGS` in the vendored Makefile — the sqlite3 amalgamation source file is likely missing from the compiled sources list, and linking sqlite3 typically needs `-ldl -lpthread` (and often `-lm`); add whatever is missing and re-run `make` until `graph_query` links cleanly. For the query bug: locate the recursive CTE string in `graph_query.c` and trace through it by hand — a recursive CTE that joins the *target* of each edge back onto the recursion anchor's id (instead of the *source*) walks edges backwards or gets stuck, so the join condition in the recursive term must match edges whose `source_id` equals the previously-reached node, not `target_id`. After fixing both, compile, copy the binary to `/home/user/graph_query`, and sanity-test it yourself against a few known `source_id` values from `edges.csv` by manually tracing reachability, since the automated check will fuzz it against many random valid node IDs and expects exact, alphabetically-sorted output.
