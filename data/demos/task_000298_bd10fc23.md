Parse `doc_config.ini` with `configparser` and read `image_dir` from the `[Paths]` section — don't hardcode the destination path. Never trust file extensions: walk `/home/user/docs_raw` recursively (`os.walk`), open each file in binary mode, and check whether its first bytes match the real PNG signature `\x89PNG\r\n\x1a\n` (Python's `imghdr`/`filetype` libs or a manual `f.read(8)` comparison both work). Only files whose header matches are genuine PNGs — a `.png`-named file with plain text content must be excluded, and a PNG-signed file with no or a wrong extension must be included. For each genuine match, compute the destination name: if it already ends in `.png` keep it as-is, otherwise append `.png`; move (don't copy) it into the destination directory, creating that directory first if missing. Since base names are assumed unique, a simple `shutil.move` per file is fine — don't overwrite existing files. Finally collect the moved files' absolute destination paths, sort them alphabetically, and write one per line to `/home/user/png_manifest.txt`, matching exactly (no extra blank lines).
