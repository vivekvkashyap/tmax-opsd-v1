Implement MAP word cleaning precisely as specified rather than approximating: for each noisy token, iterate over every (word, prior) pair in `word_priors.csv`, compute Levenshtein distance (use the `python-Levenshtein` package's `distance()` for speed/correctness, or implement classic edit distance if unavailable), score = 0.1**distance * prior, and track the best score — when a new candidate ties the current best score exactly, only replace it if the new word is alphabetically earlier (so iterate in a stable order and apply strict tie-breaking, not just ">="). Reconstruct each line by joining the chosen words with single spaces, preserving original sentence order, and write to `/home/user/cleaned_texts.txt`. For each sentence compute replaced_count/token_count as the replacement fraction (a word "replaced" means the MAP choice differs from the original noisy token). For the confidence interval, call `np.random.seed(42)` exactly once right before the bootstrap loop, then for 10,000 iterations draw `np.random.choice(fractions, size=len(fractions), replace=True)` and record the mean, and take the 2.5/97.5 percentiles via `np.percentile` — match this exact resample size and iteration order since seeded reproducibility depends on it. Round both bounds to 4 decimals and write as `[lower, upper]` to `/home/user/bootstrap_ci.txt`.