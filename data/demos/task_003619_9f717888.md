Before touching code, read sparse_poly.py carefully and reason about what `__mul__` should do mathematically: for each pair of terms (degree k1, coeff v1) and (k2, v2) in the two operands, the product term has degree k1+k2 and coefficient v1*v2, and terms landing on the same resulting degree must be *summed together*, not overwritten — use `res[k1+k2] = res.get(k1+k2, 0) + v1*v2`. Let `_cleanup` (already correct) strip any degree whose accumulated coefficient becomes exactly 0, and verify by hand that (x+1)*(x-1) leaves no degree-1 key at all. Write the three pytest tests exactly as specified, asserting both the resulting `.coeffs` dict values and, for the cancellation test, that the degree-1 key is absent (`assert 1 not in poly.coeffs`). For run_tests.sh: `pip install pytest pytest-json-report`, then run pytest with `--json-report --json-report-file=/home/user/sparse_poly_pr/test_report.json`, `chmod +x` the script, and actually execute it to confirm the JSON report shows 3/3 passed before finishing.
