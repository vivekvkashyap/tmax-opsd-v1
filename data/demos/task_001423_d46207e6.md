Build each pipeline stage as a standalone, idempotent script before wiring up the Makefile, and test each stage manually first even though you must not run `make` yourself at the end. For normalization, use `jq` to map the nested JSON array into flat CSV rows: pull `.loc_id`, `.coordinates.latitude`, `.coordinates.longitude` per element and prepend the `id,lat,lon` header — verify by eye that decimal precision isn't truncated. For `match.py`, implement the standard haversine formula with `R = 6371.0`, converting degrees to radians; nested-loop or vectorize over every (A,B) pair, keep only pairs with distance <= 5.0 km, round to 2 decimals with Python's `round()` (careful with banker's rounding edge cases), and sort output rows by `id_A` then `id_B` before writing the CSV with header `id_A,id_B,distance_km`. For the Makefile, declare `normalize: source_B.json` producing `source_B_norm.csv`, declare `match: source_A.csv source_B_norm.csv match.py` producing `matches.csv` via `python3 match.py`, and set `all: match` as the first/default target — test dependency correctness yourself by touching `source_B.json` and running `make -n all` (dry run) to confirm it would trigger normalize, without doing a real `make` run since the task asks you not to.