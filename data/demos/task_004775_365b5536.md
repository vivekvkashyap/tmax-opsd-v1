Treat `/app/fw_inspector` as a black box you must reverse engineer, not run in production. First run `strings /app/fw_inspector` to spot magic-byte constants and JSON key names, then use `hexdump -C` on any sample `.fw` files you can find or synthesize, and confirm structural offsets with `strace -f /app/fw_inspector some.fw` to see which file offsets/read sizes it touches. From this, work out the exact layout: a fixed magic header, followed by a length-prefixed metadata block, followed by a payload — and write down the byte offsets and endianness of the length field precisely. Build `/home/user/curator.py` to parse this layout defensively in Python: check the magic bytes, read the declared metadata length, and immediately validate that `header_size + length <= file_size` before slicing — treat any violation as malformed. Parse the metadata as JSON and reject if the `filename` field contains `../` or otherwise escapes its expected directory (use path-normalization checks, not just a naive substring search, to avoid encoding tricks). Copy accepted files byte-for-byte (open in binary mode) to the output directory. For the manifest, compute SHA256 over each accepted file's bytes, build the dict in memory, then write it atomically: write to a temp file in the same directory and `os.replace()` it into place, so a crash mid-write never leaves a corrupt or partial `manifest.json`.
