Get each service working and reachable in isolation before wiring them together, since failures compound across three hops (HTTP proxy -> TCP backend -> Redis). Implement `is_valid_release`: strip an optional leading 'v', split on '.', parse major/minor/patch as integers (guard against missing parts by defaulting to 0), then compare the tuple lexicographically against (2,0,0) — `>=` on the tuple, not a naive string or single-field comparison, since e.g. "1.9.9" must be < "2.0.0" but "10.0.0" must be >= too. Fix the Makefile's compile line to include `-lhiredis` (and `-I`/`-L` flags only if the library isn't in default search paths) and run `make`. Set `config.json` to `{"listen_addr": "127.0.0.1:8080", "backend_addr": "127.0.0.1:8081"}` exactly. Start backend_server first (it must be listening before the proxy forwards to it) and confirm with `nc -zv 127.0.0.1 8081` or similar, then start proxy_server and confirm 8080 is listening, both backgrounded with nohup. Finally do an end-to-end manual test yourself — `curl -X POST -d "v2.5.0|test" http://127.0.0.1:8080` then `redis-cli LRANGE releases:valid 0 -1` — before considering it done, since silent failures in any hop (e.g. backend crashing on malformed input) won't surface otherwise.
