Use `git bisect` mechanically rather than eyeballing 200 commits: write a small bisect-test script that, for a fixed seed, runs `calc_var.py` at the checked-out commit and compares its output against `/app/ref_calc <seed>` with a numeric tolerance, exiting 0/1 accordingly, then drive it with `git bisect start`, `git bisect bad HEAD`, `git bisect good <early-commit>`, `git bisect run <script>`. Once you have the culprit hash, `git show <hash> -- calc_var.py` to see exactly what changed — expect the diff to replace a running-sum-of-squares (Welford-style) computation with a naive `sum(x^2)/n - (sum(x)/n)^2` formula, which loses precision catastrophically when the mean is huge relative to the variance (large offset + small spread). Fix HEAD's `calc_var.py` (save as fixed_calc_var.py) by restoring an online, numerically stable single-pass algorithm — Welford's algorithm computes mean and M2 incrementally without ever squaring the raw large values — rather than reaching for `decimal` or a slow two-pass approach. Verify by running your fixed script and `/app/ref_calc` on several ad hoc seeds and comparing outputs directly before trusting the automated threshold check.