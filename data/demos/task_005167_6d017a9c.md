Fix each vendored bug independently before wiring up the orchestrator, and verify each fix in isolation first. For `interpolate.sh`, the regex is greedy across `{{...}}` pairs on a line with multiple templates — replace the pattern so it matches non-greedily/bounded to a single `{{ VAR }}` at a time, e.g. `sed -E 's/\{\{[[:space:]]*([A-Za-z_][A-Za-z0-9_]*)[[:space:]]*\}\}/${\1}/g'` combined with variable substitution (source the env file, then use `envsubst` or a loop of per-variable `sed` replacements), and handle a variable that's absent from the sourced env by substituting the literal string `UNKNOWN` instead of leaving `{{ VAR }}` or an empty string. Test with a line containing two or more templates to confirm both get replaced correctly. For `server.sh`, remove the hardcoded port and make it read `PORT` from the environment with a default: `PORT=${PORT:-8080}`, and use that variable in the `socat TCP-LISTEN:${PORT}` invocation bound to `127.0.0.1`, emitting a proper `HTTP/1.1 200 OK` header before the file body. Then write `/home/user/orchestrator.sh` as a linear script: source `vars.env`, run the fixed interpolate script against the template to produce `config.json`, append the exact-format timestamp log line (use `date '+%Y-%m-%d %H:%M:%S'`) to `pipeline.log`, `pkill -f server.sh` (or similar) to stop any prior instance, then launch the fixed `server.sh` in the background with `PORT=8080` exported, serving `config.json`. Make the orchestrator executable and run it once to confirm the server actually answers on `127.0.0.1:8080`.