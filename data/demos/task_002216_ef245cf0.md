Set up the Rust project with `cargo new` and add `rusqlite` (with the `bundled` feature so you don't need system sqlite3-dev) as a dependency. In your program, open a connection to `/home/user/network_backup.db` and execute `DROP INDEX IF EXISTS idx_corrupted_source;` then `CREATE INDEX idx_edges_covering ON topology_edges(source, target, cost);` via `conn.execute`. For the shortest-path search, implement Dijkstra using a `BinaryHeap` (min-heap via `Reverse`), but critically do not load the whole edges table into memory up front — instead, inside the traversal loop, run a parameterized query like `SELECT target, cost FROM topology_edges WHERE source = ?1` each time you pop a node to expand, using `conn.prepare` once and `query_map`/`query` with the bound parameter repeatedly. Track cumulative cost and predecessor pointers to reconstruct the path once `NODE_END` is popped. Format the result exactly as `Path: <comma_separated_node_ids> | Cost: <total_cost>` and write it with `std::fs::write` to `/home/user/recovery_path.txt`. Build with `cargo build --release` and run the binary so the DB mutation and output file both actually happen — don't just leave the code uncompiled.