Diagnose the 502 by comparing the actual Unix socket path nginx's `proxy_pass http://unix:...;` expects against the path the Python app process actually binds to (check how `start.sh` invokes `app.py` — it likely passes a different/wrong socket path argument); fix whichever side is wrong so they agree, then use `start.sh restart` and confirm with `curl -i http://127.0.0.1:8080` that you get 200. For ACLs, use `setfacl -m u:nobody:rx /home/user/service/public` (not `chmod`/`chown`, which the task explicitly forbids) and verify with `getfacl`. For the backup, a plain `tar -czf /home/user/legacy_data_backup.tar.gz -C /home/user legacy_data` preserves the expected archive layout. For storage_check.py, use `shutil.disk_usage("/")` to get free bytes and branch on the exact threshold (`> 1048576` → OK, else FULL), then run it once. For tz_convert.py, parse each ISO-8601 `...Z` timestamp as UTC-aware (`datetime.fromisoformat` after replacing `Z` with `+00:00`, or `dateutil`), convert with `zoneinfo.ZoneInfo("America/New_York")` (handles DST correctly across the log's date range automatically, unlike a fixed offset), and format each as `YYYY-MM-DD HH:MM:SS` before writing, then execute it.
