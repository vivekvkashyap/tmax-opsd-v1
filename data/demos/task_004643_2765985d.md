This has two independent halves — get each working and verifiable on its own before combining. For the audio half, install/verify `whisper` (openai-whisper) and `ffmpeg`, transcribe `/app/audio/perf_data.wav` (`whisper /app/audio/perf_data.wav --model base` or similar), then parse the transcript text for spoken "time X, velocity Y" pairs (regex over words/numbers, watch for numbers-as-words if the transcription writes them out) and emit a clean `time,velocity` CSV — don't hand-type the values, derive them from the actual transcript output. For the Rust half, scaffold a new binary crate at `/home/user/kinematics/`, add `rand` and `rand_chacha` as dependencies, and read `t,v` pairs line-by-line from stdin. Compute total distance via the trapezoidal rule (`sum(0.5*(v[i]+v[i-1])*(t[i]-t[i-1]))`), and per-interval acceleration `a_i=(v_i-v_{i-1})/(t_i-t_{i-1})`. For the bootstrap CI: seed `ChaCha8Rng::seed_from_u64(42)` once, then for exactly 1000 iterations draw `len(a)` indices with replacement (sampling uniformly via the seeded RNG, in a single consistent order) to build resampled means, sort the 1000 means, and index positions 25 and 975 (0-indexed) as the CI bounds — using the wrong RNG type/seed or a different draw order will silently break bit-exact reproducibility against the fuzz oracle. Format output to exactly 4 decimals matching the given template, build with `cargo build --release`, pipe your extracted CSV through the release binary, and redirect stdout to `/home/user/audio_analysis.log`.