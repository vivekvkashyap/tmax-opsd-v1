Handle the two parts separately and verify each in isolation before combining. For the vendored package: extract the tarball, inspect the `Makefile` and the C++ source it builds — if the source uses C++17 features (e.g. threading, structured bindings) but the Makefile compiles with an older `-std=` flag and is missing a required link flag like `-lpthread`, compilation will fail or misbehave; update the standard flag to `c++17` and add the missing link flag, then `make` and confirm the binary runs (e.g. `--version` exits 0). For the sanitizer, write a self-contained C++ parser for lines of the form `[TIMESTAMP] IP:PORT STATUS MESSAGE`: extract IP, port, and message with careful string splitting (not just regex assumptions about spacing), then implement each rejection rule precisely — check the IP falls within 10.0.0.0/8 or 192.168.0.0/16 by parsing octets and comparing numerically (don't string-match prefixes, which misclassifies things like 192.168.x vs 192.1.68.x), reject if port < 1024, and reject if MESSAGE contains "SPOOF" or "MALFORMED" as a substring. Print only accepted lines verbatim to stdout. Compile exactly as specified, then self-test by running your sanitizer against sample clean/evil log lines you construct yourself before trusting it against the real corpora.
