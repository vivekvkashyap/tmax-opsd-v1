Set up a standard Rust binary crate (`cargo new sensor_processor`) and implement the pipeline as sequential, clearly separated stages rather than one tangled loop: (1) read the CSV line by line, parse each row, and skip/drop it if fields don't parse cleanly or if `value` falls outside [-50.0, 150.0] — do this filtering first, before any per-sensor counting; (2) maintain a `HashMap<String, usize>` of a running valid-row counter per sensor_id (0-based), and keep only rows where `counter % 5 == 0`, incrementing the counter for every valid row regardless of whether it's kept; (3) maintain a separate `HashMap<String, f64>` of each sensor's last *kept* value, and for each newly kept row compute `(current - previous).abs()`, flagging as anomaly only if strictly greater than 20.0 and only if a previous kept value already exists for that sensor (first kept row per sensor is never an anomaly); update the "previous" value on every kept row, not just anomalies. Format `delta` with exactly one decimal place (e.g. `format!("{:.1}", delta)`). Write matched anomaly rows to `/home/user/anomalies.csv` in the order encountered while scanning top-to-bottom. Build with `cargo build --release` and run it, then inspect the output file to confirm the header and row order match your derivation.