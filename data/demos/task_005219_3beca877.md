Open `/home/user/model_fitter/src/main.rs` and identify that the nondeterminism comes from summing per-thread partial gradients into a shared `Mutex<f64>` in whatever order threads happen to finish — floating point addition is not associative, so the accumulated order changes run to run. Fix it by removing the threading/Mutex entirely (or by having each thread return its partial sum and reducing those results in a fixed, always-identical order back on the main thread, e.g. iterating `handles` and chunks in their original index order) so the final reduction is always: sum chunk 0's contribution, then chunk 1's, then chunk 2's, etc., every single run. Keep the rest of the gradient-descent/numerical-derivative logic as-is. Build with `cargo build --release` and run the binary against `/home/user/dataset.csv`; since the data is noise-free and generated from an integral of `a*cos(t) + b*sin(t)`, the optimizer should converge to clean values once determinism is fixed — verify convergence by running it twice and confirming identical output. Round the final printed parameters to 2 decimal places and write them to `/home/user/optimized_params.txt` in exactly the `a=<value>,b=<value>` format (no spaces).