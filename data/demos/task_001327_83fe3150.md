The core trap is pandas' silent int-to-float upcast whenever a column with NaN gets a numeric dtype — fix this deliberately rather than patching symptoms: after filling missing `category_id` with -1, cast the column explicitly to a nullable/plain integer type (e.g. `.astype(int)` only works once NaNs are gone, or use pandas' `Int64` dtype) so it never renders as `10.0` when interpolated into a string. Build `hash_key` with an f-string/format call against the now-integer column so it renders as `"10_1"` not `"10.0_1"`. For target encoding, compute `groupby('category_id')['v1'].mean()` strictly on the training DataFrame, then `.map()` that Series onto both train and test `category_id` columns, and explicitly `.fillna(0.0)` afterward to handle categories unseen in training (map produces NaN for missing keys by default — don't skip this fill step). Train `LogisticRegression(random_state=42, solver='lbfgs')` on `[v1, v2, cat_mean_v1]` vs `target`, predict on the equivalently-processed test set, and write exactly the three required columns in order to the output CSV.