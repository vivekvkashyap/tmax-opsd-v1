Tackle this in two independent tracks and verify each before integrating. Track 1 (sanitizer): implement `validate_dataset` by reading the CSV with pandas using `dtype` inference disabled or checked explicitly — for each column that looks numeric, detect whether it contains NaN (`df[col].isna().any()`) or was silently upcast to float where the underlying values are actually integral; the key insight is that pandas silently converts an int column to float64 the moment any NaN is present, so check `df[col].dtype == float` combined with all non-null values being whole numbers as a signal of a "hidden" integer column with missing data — reject if any numeric column has NaNs. Test it directly against `/home/user/data_samples/` files, both clean and adversarial cases, before wiring it in. Track 2 (services): fix the port mismatches methodically — Redis must actually bind 6379 (not 6380), gunicorn must bind 5000 (not 5001), and nginx's `proxy_pass` must target `127.0.0.1:5000` to match; update `start_services.sh` so ports agree end-to-end, then actually start the stack and `curl` `http://localhost:8080/upload` with a clean and an evil sample to confirm you get 200/400 respectively and that Redis's `dataset_queue` list only grows on the clean upload.
