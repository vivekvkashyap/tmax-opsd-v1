Build /home/user/analyze_graph.sh around `sqlite3` invoked non-interactively (e.g. via a heredoc of SQL piped to `sqlite3 :memory:` or a temp db file you delete at the end), using `.mode csv` and `.import` to load nodes.csv and edges.csv, taking care that `.import` treats the first row as a header only when using the right sqlite3 flags/version — verify the imported row counts match expectations before querying. The core pattern query is a self-join: edges joined to nodes twice (once as manager, once as employee) filtered on manager.department = the script's $1 argument and employee.department != manager.department; parameterize $1 safely (avoid naive string interpolation issues with quotes). Before running the real query, issue `CREATE INDEX` statements on the join/filter columns (e.g. edges.manager_id, edges.employee_id, nodes.node_id, nodes.department), then run `EXPLAIN QUERY PLAN <your query>;` and redirect its output to /home/user/query_plan.txt — check the plan text actually mentions your index by name (SEARCH ... USING INDEX) rather than SCAN, adjusting index columns if it falls back to a table scan. Finally run the real query with `.mode csv` and `.headers off` to print ManagerName,EmployeeName,EmployeeDept rows to stdout.