Treat `/app/legacy_archiver` as a black box you must characterize experimentally, not disassemble line-by-line. Create small controlled test directories with known file contents, run the archiver on them, then diff the known input bytes against the output `.dat` bytes at the corresponding offset to spot the transformation — a byte-for-byte comparison will reveal if it's a simple operation like a fixed XOR mask (check if `input_byte XOR output_byte` is constant across all bytes). Open the generated `.csv` index in a text editor/`cat` to infer its exact column order and delimiter by matching values (filename, offset, length) against what you know about your test file's size and position in the `.dat` blob. Build the C++ server using POSIX sockets or a small header-only HTTP library; parse `/etc/backup_config.json` for paths and port at startup, load the CSV index into memory, and on `GET /file/{name}` look up the entry, `fseek`/`pread` only the needed byte range from the archive (avoid loading the whole file), apply your reverse-engineered decompression, and return 200 with the plaintext or 404 if not found. Start the compiled `/usr/local/bin/archive_server` in the background bound to `127.0.0.1:<configured port>` and confirm with `curl` before finishing.
