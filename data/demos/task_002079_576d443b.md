This is a classic train/test statistics leakage bug — locate the mean-computation loop in `etl_processor.c` and change it to only accumulate sums over the first 8000 rows (bound the summing loop at row index 8000, not `ROWS`/10000), then keep the divisor as 8000 rather than the full row count. Critically, the *subtraction* pass must still iterate over all 10,000 rows using that same training-only mean — don't accidentally also restrict the output loop. Preserve the existing read/parse logic and the `%.5f` comma-separated output format exactly, and keep the row/column shape unchanged (10000 rows x 5 cols). After editing, recompile with `gcc -o /home/user/etl_processor /home/user/etl_processor.c -lm` if needed, run it to produce `/home/user/fixed_output.csv`, and sanity-check correctness yourself by loading `data.csv` in Python/numpy, computing the train-only mean over rows 0-7999, subtracting it from all rows, and spot-comparing a test-set row (e.g. row 8000) against your C output — the test rows should retain their original offset from the training distribution rather than being pulled toward zero.