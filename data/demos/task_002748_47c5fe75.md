Treat this as two independent Rust deliverables. For the log filter, scaffold a binary crate at `/home/user/log_filter` with `cargo new`, use `serde`/`serde_json` to deserialize each JSON file into a struct with `device_id: String` and `speed_mps: f64`, then reject (skip copying) any file where `device_id` contains `<`, `>`, `{`, or `}`, or where `speed_mps < 0.0` or `speed_mps > 100.0`; copy everything else byte-for-byte to the output directory under the same filename. Verify your logic against small clean/evil examples you write yourself before trusting it — the real grading corpus is hidden, so the rule must be implemented exactly as specified, not approximated. For the video pipeline, run `ffmpeg -i /app/traffic.mp4 -vf fps=1 frame_%03d.jpg <outdir>` to extract one JPEG per second, then in Rust read the resulting files in order, compute `abs(size(frame[t]) - size(frame[t-1]))` for each t≥1, sum those differences into non-overlapping 5-second buckets (0-4, 5-9, ...), and render `/home/user/report.md` using the exact header/list format given — compute frame count and per-window sums from the actual extracted files rather than assuming a fixed count, since video length determines how many buckets exist.