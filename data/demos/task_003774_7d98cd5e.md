Before bisecting, neutralize the environment trap: check `echo $CFLAGS` / your shell rc files for a `-ffast-math` export that would make every commit look broken, and override it for your build/test commands (e.g. `env -u CFLAGS CFLAGS="" gcc ...` or explicitly pass `CFLAGS=` when compiling/running the test at each bisect step) rather than trying to bisect with the corrupted flag still active. Write a small standalone C test harness that compiles against `signal_calc.c` at each revision, calls `compute_series(10000)`, and compares the result to the expected constant within `1e-5`, exiting 0/1 accordingly — this becomes your `git bisect run` script. Use `git bisect start`, mark current `HEAD` bad and `HEAD~100` good, then `git bisect run <your_test_script>` (make sure the script recompiles fresh each time bisect checks out a new commit, and always builds without the fast-math flag). Once bisect converges, capture the reported first-bad commit's full 40-character SHA via `git rev-parse` and write it to the required output file — do not modify any repository files in the process.