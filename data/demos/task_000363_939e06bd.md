This task has two independent halves — do the simpler config fix first. Open `/app/services/config.json` and change both `producer_redis_port` and `consumer_redis_port` from their current mismatched values to `6379` (matching the running Redis), then run `/app/services/start.sh` and confirm `/home/user/pipeline_success.log` is produced before moving on. For the C program: write a state machine parser that reads stdin line by line, tracking a "current sequence" buffer and its `prior` value; on a line starting with `>`, flush any in-progress sequence's computed posterior, then parse the new ID (stop at first space) and the `prior=` float; on other lines, scan character-by-character and increment counters only for exact 'A'/'C'/'G'/'T' matches (case-sensitive, ignore everything else including whitespace). At end-of-file, flush the last sequence. Compute GC ratio as `(G+C)/(A+C+G+T)` guarding division by zero, multiply by prior, and print with `%.4f`. Because this is graded by fuzz equivalence against a hidden oracle, test your compiled binary against varied inputs including zero-length sequences, sequences with no header, and consecutive headers with no sequence lines between them, to make sure edge cases don't diverge from the natural reading of the spec.
