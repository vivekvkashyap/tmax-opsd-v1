Debug each script by running it standalone with test inputs before chaining via process.sh. In `find_threshold.sh`, the convergence check mixes `bc` and `awk` awkwardly and likely fails because `bc` output formatting (leading zero, no decimal, or a stray newline) doesn't parse cleanly as a number in the follow-up awk comparison, or because the loop variable never actually updates across iterations — do the whole diff/compare/update purely in one `awk` invocation (awk handles floats natively) to eliminate the bc/shell round-trip, and add a hard iteration cap as a safety net against non-convergence. In `extract_metrics.sh`, ensure every awk arithmetic step uses `printf "%.6f"`-style formatting (or keeps full float precision) rather than `%d`/integer casts, since intensity averages need real precision to hit the MSE bar. In `serialize.sh`, the title must be JSON-escaped before embedding — escape backslashes first, then double-quotes (`sed 's/\\/\\\\/g; s/"/\\"/g'` applied to TITLE) so any embedded quotes/backslashes don't corrupt the JSON. Validate the final output with `python3 -m json.load` (or `jq .`) to confirm it parses before declaring done.
