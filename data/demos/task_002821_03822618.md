Use `git bisect start`, mark the current `main` (or HEAD) as bad and the `v1.0` tag as good, then let git bisect walk the history; at each step run `make test` (or a script that builds and runs the failing case: a filename with spaces and no extension) and report `git bisect good`/`git bisect bad` based on whether it segfaults, until git isolates the exact introducing commit — capture that commit hash with `git rev-parse` and write the full 40-char hash to /home/user/bad_commit.txt, then `git bisect reset`. Separately, enable core dumps with `ulimit -c unlimited`, run the crashing command to produce a core file, then open it with `gdb ./data_parser core` (or `gdb -c core ./data_parser`), navigate to the `process_file` frame with `frame`/`up`, and use `print secret_key` to read the local variable's value out of the stack — write that exact string to /home/user/recovered_key.txt. Finally, on `main`, fix data_parser.c's bug (it likely calls a string function like `strlen`/`strchr` result without checking for NULL when a filename has no extension after the space), rebuild, and confirm `make test` exits 0 without modifying the Makefile's test targets.
