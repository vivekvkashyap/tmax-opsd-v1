Use `ffmpeg` to pull frames from `/app/extraction_incident.mp4` (e.g. `ffmpeg -i extraction_incident.mp4 -vf fps=1 frame_%03d.png`) and inspect/OCR the frames to read the displayed `TARGET_DIR` path rather than guessing it — the directory name is shown as on-screen text in the recording. In your Rust program, walk the target directory recursively (e.g. with `walkdir` or `std::fs::read_dir`), and for each entry check `fs::symlink_metadata` / `read_link`: canonicalize the link's resolved target and compare it against the canonicalized target directory prefix — if the resolved path does NOT start with the target dir's canonical path, delete the symlink and write a regular file at that same path containing exactly `INVALID_LINK_REMOVED`; leave symlinks that stay inside the tree untouched. For deduplication, stream-hash every regular file's content (e.g. SHA-256, reading in chunks or via `mmap` for large files) into a `HashMap<hash, first_path>`; for every subsequent file with a hash already seen, delete it and recreate it as a hard link to the first file with that hash (`std::fs::hard_link`), so identical content shares one inode and disk usage collapses (`du -sb`) rather than just visually looking deduplicated.