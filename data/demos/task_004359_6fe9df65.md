Fix the vendored library first, in isolation: open `/app/cJSON-1.7.15/Makefile`, find where CFLAGS is defined for the shared-object target, and restore `-fPIC` (it was stripped out), then run `make` and confirm `libcjson.so` is produced before writing any C. For the tracker program, parse each JSONL line with cJSON, maintain a single global sliding window (e.g. a fixed-size double array of length 10 with an index counter and a "filled count" so you use `min(count,10)` values), and for each new size: push it into the window (overwriting the oldest slot circularly), then compute mean and population stddev (divide by N, not N-1) over exactly the values currently in the window, and z = (size-mean)/std, with z=0.0 when std==0. Format the z-score with exactly 6 decimals (`%.6f`) to match the reference in a strict MSE comparison — get numeric formatting exactly right since even tiny mismatches accumulate. Compile with `gcc ... -I/app/cJSON-1.7.15 -L/app/cJSON-1.7.15 -lcjson`. In `process_logs.sh`, export `LD_LIBRARY_PATH=/app/cJSON-1.7.15:$LD_LIBRARY_PATH` before invoking the binary so the dynamic linker finds `libcjson.so` at runtime, and install the cron entry with `crontab -u user` (or as the user) containing `*/5 * * * * /home/user/process_logs.sh`. Test manually against the provided sample file before trusting the pipeline.
