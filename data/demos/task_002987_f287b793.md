This is a black-box reverse-engineering + reimplementation task. First, generate Python gRPC stubs from `/home/user/rust_auth/proto/auth.proto` using `python3 -m grpc_tools.protoc -I. --python_out=. --grpc_python_out=. auth.proto` (don't try to fix the broken Rust project — it's a decoy; only its proto file matters). Second, treat `/app/legacy_auth` purely as an oracle: run `strings /app/legacy_auth | grep -i -E 'key|secret|hmac|sha'` to look for a hardcoded key string, and separately start it and send known `(user_id, payload)` gRPC requests via a throwaway Python client to observe token outputs — compare candidate algorithms (e.g. HMAC-SHA256 of `"{user_id}|{payload}"` with the discovered key) against those observed tokens until they match exactly; do not guess the key without checking `strings` output. Third, implement `/home/user/server.py` as a grpc server binding `127.0.0.1:50051`, implementing `AuthService.GenerateToken` with the reverse-engineered algorithm using Python's `hmac`/`hashlib`. Fourth, write `/home/user/test_auth.py` with pytest that spins up gRPC clients against both port 50051 (yours) and 50050 (oracle) for 100 randomized requests, asserts token equality, and wraps the request loop in `tracemalloc.start()`/`get_traced_memory()` to assert peak usage stays under 5MB. Leave `server.py` running in the background when done.