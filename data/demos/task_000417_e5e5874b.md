Diagnose the Makefile bug-by-bug: check `CC` — if it's set to a compiler not installed in this environment (e.g. a specific clang version), switch it to `gcc`; check the link step for `libfastcalc.so` for a missing `-lm` (needed since the code likely uses math functions); and check for stray architecture flags like `-m32` on a 64-bit host, which breaks assembling/linking the `.S` file — remove it so the assembly links against the native 64-bit ABI. Re-run `make` until `libfastcalc.so` builds and can be `dlopen`ed. For the sanitizer, write plain C that scans the expression string: track paren depth with a counter (increment on `(`, decrement on `)`, reject if it ever exceeds 5), scan for disallowed characters `%`, `&`, `|`, `^`, and pattern-match literal zero divisors like `/0` or `/0.0` (including whitespace variants) — print exactly `ACCEPT` or `REJECT`. For the migration, open the SQLite DB (via `sqlite3` CLI, Python `sqlite3`, or C API), create `equations_v2`, iterate `equations_v1` rows, shell out to your compiled `/home/user/sanitizer` for each expression, and insert with the corresponding `VALID`/`INVALID` status. Test the sanitizer standalone against a few known-good/known-bad expressions before wiring it into the migration.
