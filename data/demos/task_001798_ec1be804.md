The key obstacle is the symlink loop inside the tar.gz — do not shell out to `tar -xzf` or any recursive extraction; instead use Go's `archive/tar` package to stream through the archive entry-by-entry via `tr.Next()`, and explicitly skip any entry whose `Typeflag` is `TypeSymlink` or `TypeDir` (checking the type flag, not following the link) so the reader never recurses into the loop. For each remaining regular-file entry whose name ends in `.zip`, read its bytes into memory (or a temp file) and open it with `archive/zip` using a `bytes.Reader`/`zip.NewReader`. Inside each zip, iterate entries ending in `.txt.gz`, open each via the zip file's reader wrapped in `compress/gzip`, and decode the decompressed stream line-by-line (bufio.Scanner or json.Decoder) as JSON objects. For each object, check whether the `status` field is present and its value is exactly the string `"valid"` (use `encoding/json` decoding into a map or struct — don't do fragile string matching), and if so write that line verbatim (the original raw JSON text, not a re-serialized version) to `/home/user/valid_records.jsonl`. Compile and run the program to actually produce the output file.