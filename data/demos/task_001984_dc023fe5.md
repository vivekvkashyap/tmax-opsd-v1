Process each triplet (`meta_<id>.json`, `run_<id>.gcode`, `data_<id>.csv`) per unique id found by globbing the dataset directory. For the JSON files, don't assume UTF-8: read raw bytes and detect/try encodings (e.g. use `chardet`/`charset_normalizer`, or try UTF-8 then UTF-16LE then fall back to ISO-8859-1, checking for a BOM or decode failure) before `json.loads`. For the GCode files, parse line by line, and for lines containing a `Z` parameter (in `G0`/`G1` commands) extract the numeric value with a regex like `Z(-?\d+\.?\d*)` and track the running maximum across the whole file — note some lines have multiple axis params on one line, so match `Z` specifically rather than splitting on whitespace positionally. Copy/rename each `data_<id>.csv` to `<exp_id>_<operator>.csv` in the new directory. Build the catalog dict keyed by `exp_id` with `operator`, `max_z`, and `original_prefix`, then write it atomically: dump JSON to a temp file in the same directory (e.g. `catalog.json.tmp`) and use `os.rename`/`shutil.move` to atomically replace the final `catalog.json`, rather than writing directly to the target path.