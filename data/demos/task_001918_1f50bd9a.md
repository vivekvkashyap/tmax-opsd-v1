Extract the three config values from `/app/pipeline_config.png` with `tesseract` first, parsing `STRATA_SIZE`, `WINDOW`, and `CRON` out of the recognized text (re-run OCR with different page-segmentation modes if the cron expression's spaces/asterisks look garbled, and sanity-check the parsed cron looks like a valid 5-field expression) — do not hardcode assumed values. In the data pipeline, load `raw_data.csv`, apply `unicodedata.normalize('NFKC', text)` to every `raw_text` cell (this collapses combining-character sequences like `e` + combining-acute into the single precomposed codepoint, which matters for exact-match verification), sort by `id` ascending, compute a rolling mean over `value` with the extracted `WINDOW` and `min_periods=1` into a new `rolling_avg` column, then take the first `STRATA_SIZE` rows per `category` (after sorting, so "first" means lowest `id`) — do this stratified selection *after* computing the rolling average over the full sorted series, not after truncating, so the rolling window reflects full history. Dump the result as a JSON list to `/home/user/cleaned_data.json`. For the API, use FastAPI/Flask with middleware that checks the `Authorization: Bearer ds_secret_token` header on every route and returns 401 otherwise; implement `/api/data` (serves the JSON file) and `/api/config` (serves the three extracted values with matching key names), then start the server in the background (nohup/`&` or a process manager) and leave it running. Also write the bare cron string to `/home/user/cron_schedule.txt`.
