Fix the vendored package first since the classifier depends on it. In `setup.py`, remove/replace the bogus `nonexistent-pkg==99.9.9` dependency pin so `pip install -e .` succeeds. In `async_log_parser/core.py`, the crash is `parts[1]` being accessed in the `else` branch when `len(parts) < 2` (i.e. the line has no " - " separator) — guard that branch instead of indexing blindly, e.g. skip the malformed line or store an empty message, so parsing never raises on malformed/empty lines. Once the package installs and parses cleanly, write `/home/user/classifier.py` to scan each `.log` file using the fixed parser, and for each file track, per task ID, whether a `[CANCEL] Task <ID> interrupted` event has since been followed by a matching `[CLEANUP] Task <ID> resources released` event; count task IDs whose cancel was never matched by a later cleanup in that same file. Classify a file "evil" if that count reaches 5 or more, otherwise "clean" — implement this counting logic yourself rather than eyeballing thresholds. Write the `{filename: "clean"|"evil"}` JSON to the given output path, and validate your classifier against the provided `/home/user/corpora/clean/` and `/home/user/corpora/evil/` examples before trusting it on hidden data.
