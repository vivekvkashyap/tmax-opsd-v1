This is a reverse-engineering-then-vectorize task: your Python script must reproduce the stripped binary's formula in pure Python so it scales to large files without shelling out per row. First probe `/app/fake_metric_gen` with a handful of known (timestamp, user_id) pairs — try simple candidate relationships (addition, XOR, modulo of a prime, multiplication) and compare against the binary's output until you find a formula that matches on many test points, not just one or two (a lucky coincidence on a single sample can mislead you — verify with several diverse timestamp/user_id combos, including small and large values, before trusting it). Once confident, implement the same arithmetic in Python (e.g. bitwise XOR then modulo, matching the binary's integer semantics). In `filter_logs.py`, parse the CSV, convert `timestamp_iso` to Unix seconds (UTC, using `datetime.fromisoformat` or similar with proper timezone handling), compute the expected value per row, and drop rows where `metric_value` equals it, keeping everything else with the header intact. Test against `/app/corpora/clean/` and `/app/corpora/evil/` — clean should pass through unchanged, evil should be reduced to header-only.