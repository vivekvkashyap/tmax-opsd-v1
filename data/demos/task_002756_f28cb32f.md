Solve this in two clearly separable SQL/graph stages rather than trying to do it in one query. Stage 1 (current direct access): for each `(employee_id, resource_id)` pair in `access_events`, find the row with the maximum `timestamp` — a window function like `ROW_NUMBER() OVER (PARTITION BY employee_id, resource_id ORDER BY timestamp DESC)` (SQLite supports window functions) picks the latest event per pair; keep only pairs whose latest `action = 'GRANT'`. Watch out for the "stale row" trap: don't assume events are inserted in chronological order or that a later `event_id` means a later `timestamp` — always order by `timestamp`, not `event_id` or insertion order. Stage 2 (inheritance): load `resource_hierarchy` into an adjacency structure (e.g. a Python dict or `networkx.DiGraph`) and, for each employee's set of direct-access resources, compute the full set of reachable descendants via BFS/DFS (or `networkx.descendants`) unioned with the direct set — this is the employee's full accessible-resource set. Stage 3: an employee is a violator if their full accessible set contains both `Vault_Open` and `Vault_Close`. Collect all violator employee_ids, sort them alphabetically, and write one per line to `/home/user/sod_violators.txt` with no extra blank lines or trailing whitespace issues.