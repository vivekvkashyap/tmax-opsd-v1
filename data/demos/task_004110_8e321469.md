Treat this as two independent fixes that each need direct verification, not just code inspection. For the vendored library, grep for the injected penalty rather than reading the whole codebase: `grep -rn "sleep" /app/passlib-1.7.4/passlib/handlers/md5_crypt.py` to locate the exact `time.sleep(...)` call inserted into the verify/checksum path, delete just that line (leave the surrounding crypto logic untouched), then install the patched package into the active environment (`pip install -e /app/passlib-1.7.4` or `pip install /app/passlib-1.7.4`) and confirm with `python3 -c "import passlib; print(passlib.__file__)"` that the installed copy is the patched one, not a cached wheel. Benchmark it yourself with a small `timeit` loop calling `md5_crypt.verify()` to sanity-check the speedup before moving on. For the script, replace `argparse`'s `--passwords` flag with code that opens and reads `/home/user/wordlist.txt` line by line (stripping newlines/blanks) instead — command-line arguments leak via `/proc/[pid]/cmdline`, so removing the flag entirely (not just ignoring it) is the point. Populate the wordlist file yourself with common passwords, run the modified script, and confirm `/home/user/cracked.json` maps each hash in `/home/user/legacy_hashes.txt` to its cracked password.