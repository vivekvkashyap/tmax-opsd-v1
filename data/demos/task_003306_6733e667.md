For the deleted-but-open file, don't look for it on disk — find the daemon's PID (`pgrep -f metrics_emitter.py` or similar) and its still-open file descriptor under `/proc/<pid>/fd/`; a deleted file with an open fd shows up there as a dangling symlink you can still `cat`/`cp` from (e.g. `cp /proc/<pid>/fd/<N>/../fd/<N> /home/user/recovered.dat` — identify the right fd number by listing `/proc/<pid>/fd` and checking which target says "(deleted)"). For the Rust bug, read `src/main.rs`'s buffer-sizing logic carefully: it allocates a buffer sized `len + 1` but only fills the first `len` bytes, then asserts the buffer's *capacity* equals `len` — that assertion will always fail because of the extra `+1`. Fix the allocation so the buffer is sized exactly to `len` (`vec![0u8; len]`) rather than padding it, so the read and the assertion agree. Rebuild with `cargo run --manifest-path ... -- recovered.dat`, redirecting stdout to the required output file, and confirm the parser no longer panics and produces a comma-joined string of the recovered length-prefixed records.
