Work the four sub-tasks independently and verify each before moving on. For frame extraction, put `-ss <timestamp>` before `-i /app/demo_recording.mp4` for accurate seeking, use `-frames:v 1 -q:v 2` (or similar low-compression JPEG quality flag) for each of the 5 timestamps — matching ffmpeg's own seek behavior is what the SSIM comparison rewards, so don't second-guess the timestamps or add filters. For the archive, check integrity with `gzip -t` or `tar tzf` before extracting, then inspect the actual byte encoding of each extracted `.txt` file (don't assume — use `file` or try decoding) and convert with `iconv -f ISO-8859-1 -t UTF-8` per file, writing back in place. Do the string replacement (`AcmeCorp` -> `NovaTech`) with `sed -i` after the encoding conversion, not before, so you're operating on valid UTF-8. Finally, when building the final tar.gz, `cd` into a flat staging directory (or use `tar --transform`/`-C`) so only the frame and txt files land at the archive root with no wrapping directory, since the packaging step is checked structurally.
