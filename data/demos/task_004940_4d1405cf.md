Start by reading `/app/query_pattern.png` (OCR it, e.g. with `tesseract`) to understand the exact 3-node path pattern and filter conditions the query must implement — don't guess the schema, derive it from the image text. Then inspect `/home/user/graph_analyzer.c`'s existing SQL: it likely joins `nodes`/`edges` tables via a comma-separated FROM clause but omits the join predicate linking the second edge's source to the middle node, producing an implicit cross join (duplicate/wrong rows). Rewrite it as explicit `JOIN ... ON` clauses chaining node A -> edge1 -> node B -> edge2 -> node C, applying the category/value filter conditions from the image, and compute the rolling sum of edge weights partitioned by the target's category ordered by the source's value using SQLite's window function syntax (`SUM(weight) OVER (PARTITION BY ... ORDER BY ...)`). Add `CREATE INDEX` statements on the join columns (node ids, edge source/target ids) before running the main query for performance on large fuzzed datasets. Compile with `gcc -O3 graph_analyzer.c -lsqlite3 -o graph_analyzer` and validate output formatting (`source_id,middle_id,target_id,rolling_sum`, ordered by source then target) matches exactly since a fuzzer will diff your output against a reference binary across many random CSV datasets.