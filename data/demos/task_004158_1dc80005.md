Start by compiling exactly as specified (`gcc -O2 matrix_sim.c -o /home/user/matrix_sim -lm`) and manually running it with a couple of `dt` values to confirm it prints bare `SUCCESS`/`DIVERGED` lines, so you understand the exact output format before scripting around it. In `profile.sh`, avoid a raw bash `for ((i=...))` float loop since bash arithmetic is integer-only — instead iterate over an integer counter from 10 to 100 and construct `dt` as `printf "%.3f" "$(echo "$i/1000" | bc -l)"` (or use `awk`/`seq` with formatting) to guarantee exactly 3 decimal places and avoid floating-point drift across 91 iterations. For each `dt`, run `./matrix_sim "$dt"`, capture stdout, and append `dt,status` to `/home/user/results.csv` with no header. After the sweep, don't assume divergence is monotonic without checking — scan the CSV for all `SUCCESS` rows and take the numeric max `dt` among them (e.g. `awk -F, '$2=="SUCCESS"{print $1}' results.csv | sort -n | tail -1`) and write that single value to `/home/user/optimal_dt.txt`. Make `profile.sh` executable and run it once to actually generate both output files.
