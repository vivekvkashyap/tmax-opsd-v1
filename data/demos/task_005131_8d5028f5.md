First fix `/app/Makefile`: it must invoke `protoc-c` (not `protoc --cpp_out`) to generate `trace.pb-c.c`/`.h` from the `.proto` file, and link against `-lprotobuf-c` (not `-lprotobuf`). Verify with `protoc-c --version` and check the include path matches where the generated headers land. Second, run `tesseract /app/docs/statemachine.png stdout` to OCR the state-machine rules out of the image — don't guess the transitions, read them. Third, implement `validate_trace()` in `validator.c` as a strict finite-state-machine walk over `trace->events`: track current state, on each event look up whether (current_state -> event_type) is an allowed transition per the OCR'd rules, reject immediately on any unlisted transition, and additionally require the trace start on the specified initial state and end on the specified final state (empty traces and traces ending elsewhere must be rejected). Build with `make -C /app`, then iterate against `/app/corpus/clean/*.bin` (must all exit 0) and `/app/corpus/evil/*.bin` (must all exit nonzero) until 100% pass — the evil corpus is adversarial, so check edge cases like empty event lists, wrong start/end states, disallowed self-loops, and trailing/duplicate events.