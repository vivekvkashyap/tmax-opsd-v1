Work the task in order, verifying each step before moving on. First, run OCR (e.g. `tesseract /app/target_config.png stdout`) to recover the port and secret token text from the screenshot — don't guess values. Second, edit `src/main.rs` to use the recovered port/token in the connection/print logic. Third, to shrink the release binary below the 400KB threshold, add a `[profile.release]` section to `Cargo.toml` with size-oriented settings (`opt-level = "z"`, `lto = true`, `codegen-units = 1`, `panic = "abort"`, `strip = true`), then rebuild with `cargo build --release` and check the actual size with `stat -c%s` — iterate if still too large (e.g. remove unused deps, avoid the `std` formatting machinery bloat). Fourth, fix `run_monitor.sh`: since it runs in a stripped-down cron-like shell, don't rely on relative paths or an inherited PATH/CWD — use the absolute path to the built binary and redirect output explicitly to `/home/user/logs/monitor.log` (creating the directory if needed). Finally, actually execute the script once and confirm the log file exists with the expected content before finishing. Do not use sudo/root.