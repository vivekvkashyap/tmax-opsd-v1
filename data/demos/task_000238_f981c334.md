Tackle this in dependency order: Rust math fix first (nothing else compiles/runs without it), then the Rust WebSocket server, then the Python gateway routing/WS-client/Redis logic, then the orchestration script, then measure latency last. Read `/home/user/app/reference_math.py` to understand the determinant algorithm (likely recursive cofactor expansion or LU-based), and translate it faithfully into Rust in `math.rs`, matching the function signature `calculate_determinant` that `main.rs` already calls — check `main.rs` for the exact expected argument/return types. For the gateway, replace the broken GET route with a POST route at the exact path, use an async WebSocket client (e.g. `websockets` library) to connect to `ws://localhost:8001/math`, send the payload, await the response, then use a Redis client to cache before responding. Build the Rust binary in release mode (`cargo build --release`) for the throughput requirement — debug builds will likely blow the 25ms p95 target. Write the startup script to launch redis-server, the compiled Rust binary, and `uvicorn main:app --port 8000` in the correct order with brief waits so each dependency is ready before the next starts, then leave everything backgrounded.
