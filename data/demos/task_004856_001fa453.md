Set up the Go module cleanly first: `go mod init etl_transform` then `go get gonum.org/v1/gonum/...` (mat and stat subpackages) so imports resolve before writing logic. In main.go, load the CSV with `encoding/csv` into a `mat.Dense` (100x5), mean-center each column (or use `stat.CovarianceMatrix` directly, which handles centering internally — pass weights as nil), producing the 5x5 covariance matrix. Feed that into `mat.EigenSym` with `left: false` or as required by the API, extract eigenvalues via `.Values(nil)`, then sort them in descending order yourself (gonum doesn't guarantee output order) using `sort.Sort(sort.Reverse(sort.Float64Slice(vals)))`. Read `reference_eigenvalues.txt`, split on whitespace, parse floats, and compare index-by-index with `math.Abs(a-b) <= 1e-4` — set `accuracy_passed` only if every pair passes. Marshal the final struct to JSON with `encoding/json` and write to `report.json`. Build and run with `go run main.go` (or `go build && ./etl_transform`) and inspect the output file directly to confirm formatting matches the required schema exactly.
