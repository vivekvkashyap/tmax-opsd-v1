First transcribe `/app/ticket_recording.wav` (install `whisper` or `SpeechRecognition`/`pydub` if needed) and read the transcript carefully — it names the three specific bugs and how to fix them, so let it drive your fix list rather than guessing from the code alone. Validate locally by comparing your script's behavior against the task's documented reference for varied sample lines (well-formed, missing status after `|`, corrupted/bracket-less lines, high-precision floats), checking for byte-for-byte equivalence before submitting. Concretely: (1) stop rounding voltage with `round(float(v), 2)` — format it as a fixed-precision string instead of a rounded Python float; (2) when the text after `|` is empty/missing, still emit a record with an empty status string instead of dropping the line; (3) replace the recursive `skip_corrupted` function with an iterative loop so bursts of corrupted input can't blow the recursion limit — regex-match each line against the expected `[TIMESTAMP] DEVICE: VOLTAGE | STATUS` format and emit `{"error": "corrupted_frame"}` on failure, continuing to the next line. Test broadly with your own edge cases (empty status, malformed brackets, long corrupted bursts, many decimal places) since the grader fuzzes thousands of inputs.