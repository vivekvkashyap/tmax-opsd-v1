Build this as a pandas pipeline in clear stages, checking intermediate output at each step rather than writing it all at once. Load the CSV, then for each sensor column strip the trailing " C" and replace "ERR" with `NaN` before casting to float (e.g. `pd.to_numeric(col.str.replace(' C','', regex=False).replace('ERR', pd.NA))`). Apply `.interpolate(method='linear')` per column to fill NaNs — check this worked by printing the columns before/after, since a missing value at a series edge or wrong axis will silently propagate. Reshape with `pd.melt(..., id_vars='timestamp', var_name='sensor', value_name='temperature')`. Compute the rolling mean *after* sorting within each sensor group chronologically, using `.groupby('sensor')['temperature'].rolling(window=3, min_periods=1).mean()` and carefully realign the result back onto the long dataframe (groupby-rolling returns a multi-index that needs `.reset_index(drop=True)` or careful index alignment — this is the step most likely to silently misalign rows). Sort by `sensor` then `timestamp`, format both float columns to exactly 2 decimals with `.round(2)` or string formatting, and write with `index=False`, confirming column order and a few sample rows against your own hand computation before finishing.