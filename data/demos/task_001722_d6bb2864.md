Transcribe `/app/intercepted_call.wav` first (via `whisper`, an STT library, or any available tool) and listen/read carefully for the specific salt word spoken in the recording — don't assume a generic value. In `/home/user/detector.c`, read the HTTP request file, locate the `X-Redirect-URL:` and `X-Auth-Token:` header lines (case-insensitively, e.g. with `strcasestr`), and extract their values up to the line ending, trimming whitespace/CR. Use OpenSSL's `HMAC()` (SHA256) keyed with the recovered salt over the exact URL string, hex-encode the digest in lowercase, and compare it byte-for-byte against the token — a mismatch or missing header means immediately printing `EVIL\n` and exiting 1. If the HMAC checks out, parse the URL's hostname (strip the `http://`/`https://` scheme, take everything up to the first `/`) and verify it is exactly `trusted.corp` or ends with `.trusted.corp` — be careful that a check like `strstr(host, "trusted.corp")` would wrongly accept `trusted.corp.evil.com` or `nottrusted.corp`; use proper suffix matching including the boundary dot. Print `CLEAN\n`/exit 0 or `EVIL\n`/exit 1 accordingly, compile with `gcc -o detector detector.c -lcrypto`, and validate against both `/app/corpus/clean/` and `/app/corpus/evil/` before finishing.