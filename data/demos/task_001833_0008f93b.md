Work top-down through the pipeline and validate each stage before moving on. Install dev packages via apt (libhdf5-dev, hdf5-tools, libeigen3-dev, valgrind) and pip (matplotlib, h5py) first, since later steps depend on them. In the C++ program, seed `std::mt19937` with exactly 42 and draw from `std::uniform_real_distribution<double>(0.0, 0.1)` in the same call order the spec implies (row-major, one draw per element) — small differences in generation order will shift every downstream value, so keep it simple and consistent: nested loop over rows then columns, computing `sin(j*0.05) + draw()` per element. Use the HDF5 C++ API (or C API) to write the matrix as dataset `raw_data` with dims {1000,500}, then read it back — this read-back step matters because the grader checks the file's actual on-disk structure, not just in-memory values. Feed the matrix into Eigen's `JacobiSVD` or `BDCSVD` to get singular values, take the top 5 (already descending by Eigen convention, but sort defensively), and print with 4 decimal places. Compile with `-O2 -g` linking HDF5 and Eigen, then profile with `valgrind --tool=callgrind --callgrind-out-file=/home/user/callgrind.out ./spectromark`. Finally write a small matplotlib bar-chart script reading the 5 values and saving a PNG. Check each output file exists and looks sane (e.g. `h5ls`, `wc -l` on the results) before finishing.