This task has two independent deliverables — get the Go algorithm bit-exact first, then wire up the service glue. For Part 2, write `/home/user/analyze.go` implementing the pipeline literally as specified: map each base to 1.0 (G/C) or 0.0 (A/T), convolve with the fixed 3-tap filter [0.25,0.5,0.25] treating out-of-range indices as 0.0, take the central difference of the smoothed array, and print indices 1..L-2 space-separated with `%.2f` formatting — match Go's default float formatting/rounding behavior to the oracle by testing many random ACGT strings of varying lengths against `/opt/oracle/analyze_oracle` and diffing stdout exactly (don't just eyeball a few cases; edge lengths like L=10 and very long L matter). Compile with `go build`. For Part 1, fix `/app/emitter/config.env` (host/port literally as stated in the task), change the Flask app's `app.run(port=...)` call to the required port, then write `/home/user/worker.sh` as a loop doing `redis-cli BLPOP dna_jobs 0` (or LPOP with a poll loop), splitting the popped `UUID|SEQUENCE` value, invoking your compiled `analyze` binary, and `curl -X POST` the JSON payload `{"id":..., "result":...}` to the listener's `/results` endpoint. Restart services and observe an end-to-end job actually flow through before finishing.
