For Part 1, run `valgrind --leak-check=full /app/v1_calc 10 5 +` and read the "definitely lost" line in valgrind's summary output; extract just the byte count (an integer) into `/home/user/leak_report.txt` with no extra text. For Part 2, do not shell out to the unstable legacy binary — implement RPN evaluation purely in Bash using a stack (array or space-delimited string), pushing operands and popping two on each operator token, using `$(( ))` for integer arithmetic. Before executing a `/` operation, check if the divisor is `0` and short-circuit to `ERROR: DIV0` instead of letting Bash's arithmetic error out or crash. Build the network layer with `socat TCP-LISTEN:9000,reuseaddr,fork EXEC:"your-parser-script"` (or a `nc -lk` loop) so each connection gets its own subshell — this naturally handles concurrent/sequential connections. Read each line, evaluate, and write back `RESULT: <value>\n` or `ERROR: DIV0\n`. Make `/home/user/server.sh` executable and test it manually with `echo "10 5 +" | nc localhost 9000` before relying on the verifier.