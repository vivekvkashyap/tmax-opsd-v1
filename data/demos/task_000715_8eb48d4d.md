Process the three input files strictly in filename alphabetical order (snapA.json, snapB.csv, snapC.xml) and within each file preserve top-to-bottom record order — this ordering matters because it determines which duplicate "wins". For each format use the appropriate parser (`json.load`, `csv.DictReader`, `xml.etree.ElementTree`) to normalize every record into a common dict with `server_id, service, port, status, config_string`, coercing `port` to int. Apply validation first: keep only records with `1024 <= port <= 65535` and `status` exactly `"active"` or `"inactive"` — reject everything else (including any other status string). For deduplication, build the exact string `f"{server_id}|{service}|{config_string}"` and hash it with `hashlib.md5(...).hexdigest()`; keep a seen-hashes set and only insert a record the first time its hash appears in your ordered stream, skipping later duplicates. Create the SQLite table with the exact column names/order given, and insert each surviving record plus its computed hash. Double check your md5 hashing matches the exact separator format (`|`) and field order.
