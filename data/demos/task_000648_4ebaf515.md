Tackle the three sub-problems in order since later steps depend on earlier ones compiling. First resolve `go.mod`: run `go build` in `/home/user/recovery_src/` to see the exact conflicting dependency versions, then either pin compatible versions manually or run `go mod tidy`/`go get <pkg>@<version>` to converge. Second, find the deadlock in `recovery.go` by reading how the mutex is locked relative to channel sends — look for a lock acquired twice in one goroutine, or a code path (likely an error branch) that returns/sends without unlocking; fix by using `defer mu.Unlock()` immediately after `Lock()` so every exit path releases it. Third, run `tesseract /app/wiki_snapshot.png stdout` to extract the header text, locate the `0x`-prefixed hex constant it documents, and set `MAGIC_HEADER` to that exact value (matching case) in place of the placeholder. Build the standalone binary to `/home/user/wal_sanitizer`, have it print `VALID`/exit 0 or `CORRUPTED`/exit 1 based on whether the file's header and structure parse cleanly, and validate against every file in both `/app/wal_clean/` and `/app/wal_evil/` before finishing.
