Write the script to closely mirror the exact statistical procedure implied by the spec, since verification will recompute it independently and compare floats. Load `theta` from the CSV with pandas/numpy, compute the plain sample mean first. For the bootstrap: call `numpy.random.seed(42)` exactly once, immediately before the resampling loop (not before loading data, not inside each iteration), then perform exactly 1000 iterations of `np.random.choice(theta, size=len(theta), replace=True)`, taking the mean of each resample and collecting all 1000 means into an array — order and RNG-call sequence matter for reproducibility, so don't shuffle/reorder other random calls before this. Compute the 95% CI as `np.percentile(bootstrap_means, 2.5)` and `np.percentile(bootstrap_means, 97.5)` (percentile method, not normal-approximation). Fit `scipy.stats.gaussian_kde(theta)` with default bandwidth, then evaluate `kde.logpdf(mean_val)` — note this returns an array, so extract the scalar with `[0]`. Serialize all four values into `/home/user/results.json` using `json.dump`, converting numpy floats to native Python floats (e.g. `float(x)`) to avoid JSON serialization errors. Run the script once to actually produce the output file — don't just write it.
