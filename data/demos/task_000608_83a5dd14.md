Structure the Go program in clear stages: parse the CSV into a slice of records, group by `server_id` into a map, then sort each server's slice by integer timestamp ascending. Process each server's group concurrently with a goroutine (use a `sync.WaitGroup` and per-server output slots/mutex to avoid races) — this is what the task explicitly wants demonstrated, not just correctness. For missing `cpu_temp`, since the first and last records per server are guaranteed valid, scan forward from each gap to find the next valid value and linearly interpolate between the last known and next known temps by position (not by time delta unless specified — follow the literal instruction of "nearest preceding and succeeding valid" values). For missing `latency_ms`, compute the mean of only the valid (non-empty) latency values for that server first, then fill gaps with that mean — do this after imputing temps, and compute `is_overheated` using the already-interpolated temp values. Aggregate `max_temp`, `avg_latency` (over all records post-imputation), and `overheat_events` count. Write output sorted by `server_id` alphabetically with floats to exactly 2 decimals. Compile and actually run the program against the real input file, then spot-check a couple of rows by hand to confirm your interpolation logic before finishing.