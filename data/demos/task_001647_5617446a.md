Start by reading `calc.sh` carefully to see it does `MULTIPLIER=$(cat "$CONF_DIR/multiplier.conf")` — the fix isn't in the script logic but in exporting `CONF_DIR` (pointing to `/home/user`, where `multiplier.conf` actually lives) before invoking the script, e.g. `export CONF_DIR=/home/user`. Use `strace -f -e openat ./calc.sh` if you're unsure which path it's actually trying to open. Next, sanitize each line read from `inputs.txt` before doing arithmetic: strip commas and carriage returns with something like `val=$(echo "$raw_val" | tr -d '\r' | tr -d ',')` (or `sed`), since Bash arithmetic on `1,000\r` will error out. Finally, eliminate the read-modify-write race on the shared `sum.txt`: either wrap the critical section with `flock` on a lock file, or (simpler and more robust) have each background job append its own `final_val` to a unique temp file under a temp directory, then after `wait`, sum all the temp files together (e.g. with `awk` or a loop) into the real total. Write the resulting integer alone to `/home/user/legacy_math/final_answer.txt`.