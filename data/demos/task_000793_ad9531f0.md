Approach this as two independent bugs, fix and test each in isolation before combining. For the memory leak: the original code likely buffers all decoded frames (or all raw arrays) in a list before processing — refactor to a streaming loop that reads one frame at a time from the capture, computes its per-frame intensity metric immediately, and discards the frame (let it go out of scope / do not append raw frame data to a growing list). For the filename-with-spaces bug: in the shell wrapper, any unquoted variable expansion or unquoted command substitution (e.g. `$(cmd)`, `for f in $var`) will word-split on spaces — fix by quoting all variable expansions (`"$file"`) and using arrays or a `while IFS= read -r` loop over `find -print0`/`-0` instead of naive globbing/`$(ls ...)`. In the Python script, make sure `sys.argv` values are used as-is (already whole strings) and that any subprocess calls pass the path as a single list element, not through `shell=True` string interpolation. Preserve the exact binary output format (int32 count, then float32 array, little-endian) — verify byte-for-byte against a small self-made test video with a space in its name before trusting the fix.
