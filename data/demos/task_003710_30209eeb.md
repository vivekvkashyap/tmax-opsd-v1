First fix the vendored library before writing any new code: inspect `/app/vendored/pii-masker/pii_masker/rules.py` and try `python -c "import pii_masker"` / running its tests to surface the exact SyntaxError and any regex bug — expect an unbalanced parenthesis in a `re.compile(...)` call (fix by closing it) and an SSN regex missing a trailing word boundary (`\b`) that causes it to over-match into adjacent digits of longer clean-looking numbers, which would wrongly corrupt non-PII 10-digit serials — add the missing `\b` so it matches exactly XXX-XX-XXXX and stops there. After `pip install -e /app/vendored/pii-masker`, sanity-test `mask_text` directly on a sample string with an SSN and email to confirm both get replaced with `[SSN]`/`[EMAIL]` and nothing else changes. Then write `/home/user/sanitize.py` taking `<input_csv> <output_csv>` positional args: read rows, dedupe by `user_id` keeping first occurrence (use an ordered set/dict to preserve first-seen order), pass each row's `feedback` field through the fixed masker, and write out preserving the original column order/headers. Validate end-to-end against both `/app/corpora/evil/` (PII must be masked) and `/app/corpora/clean/` (feedback text must be byte-identical to input) before considering it done.