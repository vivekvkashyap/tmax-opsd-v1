Video part: extract frames from `/app/dashboard_recording.mp4` with `ffmpeg` (e.g. `ffmpeg -i dashboard_recording.mp4 frames/f_%05d.png` at full frame rate, or use `select` filters), then programmatically check each frame for "completely, solidly red" — e.g. with Python/PIL, load each frame and verify every pixel equals (255,0,0) exactly (or within a tiny tolerance), not just "mostly red" or containing red text/UI elements. Count frames passing that strict full-frame check and write the integer to `/home/user/red_frame_count.txt`.

Deadlock detector: write `/home/user/detector.cpp` that parses the CSV (skip header), builds a directed wait-for graph where an edge A->B exists if some transaction A WAITs on a resource that some transaction B HOLDs (map resource_id -> holder tx_id from HOLD rows, then for each WAIT row draw an edge from that tx to the resource's holder). Implement cycle detection via DFS with a recursion-stack marker (visited + on-stack arrays) — a node revisited while still on the DFS stack means a cycle/deadlock. Print `REJECT` if any cycle found, else `ACCEPT`, and always exit 0. Compile with `g++ -O2 -o /home/user/detector /home/user/detector.cpp`. Validate against a handful of self-constructed clean/deadlocked CSVs before relying on the corpora.