Build the two scripts incrementally and test each against real files rather than guessing syntax. For `deploy_edge.sh`, write a single `qemu-system-x86_64` invocation with `-m 512`, `-nographic`, `-daemonize`, `-drive file=/home/user/images/edge_device.img,...`, and a `-netdev user,...,hostfwd=tcp::8080-:80,hostfwd=tcp::5901-:5900` (with matching `-device e1000,netdev=...`); do not actually run it since virtualization isn't available. For `check_quota.sh`, get the real byte size with `stat -c%s` (or `stat -f%z` on macOS-style stat, but assume Linux `stat -c%s`) on the image path, compare numerically against 104857600, and only append the ALERT line (with the actual size substituted) when strictly greater — verify this by testing with files of different sizes before finalizing, since off-by-one on ">" vs ">=" and float vs int comparisons are common naive mistakes. `chmod +x` both scripts. For the cron job, edit the user crontab (`crontab -e` non-interactively via `crontab -l | { cat; echo "*/15 * * * * ..."; } | crontab -`) so it runs every 15 minutes, then `crontab -l > /home/user/cron_backup.txt` to capture it for verification.