Build and test each piece of the pipeline separately before scheduling it. For encoding handling in C++, read `temp.csv` as raw bytes and convert UTF-16LE to a normal byte stream yourself (each ASCII char is followed by a `0x00` byte in UTF-16LE for this simple numeric/comma content — strip the null bytes, or use `<codecvt>`/iconv-equivalent logic) before parsing lines as `timestamp,temperature`; read `humidity.csv` normally as UTF-8. For the ISO 8601 humidity timestamps, parse them into a `struct tm` (matching the `YYYY-MM-DDTHH:MM:SSZ` format) and convert to Unix epoch with `timegm` (not `mktime`, which is local-time and will shift by your timezone — a very common bug here) since the trailing `Z` means UTC. Do an inner join by exact matching integer epoch, sort the joined rows by timestamp, and write UTF-8 output with the exact header requested. Wrap compilation+execution in `run_etl.sh` (`chmod +x` it), testing it manually first. Only after `merged.csv` is verified correct should you add the cron line via `crontab -e`/`(crontab -l; echo "...") | crontab -` with the exact `*/15 * * * *` schedule and full script path.
