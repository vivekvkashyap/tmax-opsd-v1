Do this in dependency order. First, transcribe `/app/incident_report.wav`: install a speech-to-text tool (e.g. `pip install SpeechRecognition pocketsphinx` or a whisper package), run it against the file, and save the exact recognized text to `/home/user/incident_text.txt` — don't guess the phrase, actually run the ASR and use its output verbatim (minor wording variance across engines is expected and acceptable). Second, write `/home/user/backend.conf` with the three exact key=value lines specified (`HTTP_PORT=8080`, `TCP_PORT=8081`, `INCIDENT_FILE=/home/user/incident_text.txt`). Third, write `/home/user/dashboard_backend.c`: parse the conf file for the port numbers and incident path, read the incident text into memory, then open two listening sockets (or use `fork()`/threads to serve both concurrently) — one on HTTP_PORT that replies to any GET with a `200 OK` header and a JSON body `{"incident": "..."}` (make sure to JSON-escape the transcribed text if it contains quotes), and one on TCP_PORT that just writes the raw text plus `\n` and closes. Fourth, write `/home/user/deploy.sh`: ensure `/home/user/backups/` exists, back up any running `backend.bin` by timestamped copy, compile the C file (exit without touching the running service if compilation fails), then kill the old process and start the new binary backgrounded with output redirected to `/home/user/backend.log`. Run `deploy.sh` last and confirm both ports respond (`curl` and `nc`) before finishing.
