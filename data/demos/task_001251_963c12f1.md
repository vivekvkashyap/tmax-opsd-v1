First identify the rogue file programmatically instead of eyeballing directories: compute `sha256sum` for every file under `/home/user/uploads/` and `/home/user/system_cache/`, and diff those hashes against the set listed in `/home/user/manifest.txt` — the one file whose hash is absent is the payload. Inspect it read-only to understand what it does before executing it. Then write `/home/user/analyze.py` to invoke it strictly inside a locked-down `bwrap` sandbox via `subprocess.run([...], capture_output=True, text=True)`, constructing the bwrap argument list with exactly the required isolation flags: `--unshare-net`, `--ro-bind / /`, `--proc /proc`, `--dev /dev`, followed by the interpreter and the path to the malicious script. Capture `stdout` from the completed process and write it verbatim to `/home/user/malware_output.txt` (use `write()`, not `print()`, to avoid adding extra newlines/formatting that could break an exact-match check). Since the payload attempts a network call as its "phone home" test, running it correctly sandboxed should make that call fail/timeout, causing the script to print a different message than if network access succeeded — trust whatever output the sandboxed execution actually produces rather than assuming the content.