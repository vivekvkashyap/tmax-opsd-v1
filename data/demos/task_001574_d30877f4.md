Treat this as four separable subtasks. (1) For the video: use `ffmpeg -i /app/incident.mp4 frame_%04d.png` (or extract frames programmatically) to dump frames, then in Python load each frame (e.g. with PIL) and check the dominant/average pixel color against pure red (#FF0000) vs green — iterate frames in order and record the first 0-indexed frame whose color is red, not just "reddish"; write only that integer, no extra text, to the output file. Don't guess a frame count from fps/duration math alone — always verify against actual extracted pixel data. (2) For Nginx: locate the `proxy_pass` line in `nginx.conf` and point it at the correct upstream port given in the task (8081), then confirm with `nginx -t`. (3) For the classifier: read the raw HTTP request file, extract the request path, and check case-insensitively for `../` as well as URL-encoded variants (`%2e%2e`, `%2E%2E`, mixed case) — normalize/decode the path or use a case-insensitive regex rather than a literal exact-case string match, print EVIL/CLEAN and exit 1/0 accordingly. (4) For the git hook: use `git diff --cached --name-only --diff-filter=ACM` to list staged `.req` files, loop and invoke your classifier on each, and `exit 1` from the hook if any file is EVIL; remember to `chmod +x .git/hooks/pre-commit`.
