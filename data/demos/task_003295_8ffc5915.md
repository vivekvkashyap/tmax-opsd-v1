Reverse-engineer the black-box binary's rejection rules empirically rather than assuming them: pipe a variety of crafted lines into `/app/net_diag_shell` one at a time (via stdin) and observe which inputs produce error/rejection output versus which get processed normally, and separately look for evidence of command-injection behavior (e.g. try classic shell metacharacters and see if side effects occur, using `strings`/`ltrace`/`strace` on the stripped+packed binary if plain interaction is ambiguous — note it may be UPX-packed, so `upx -d` or running it directly to observe behavior may be more productive than static disassembly). Once you've enumerated the blacklist (restricted subnet prefix, loopback/localhost aliases, shell metacharacters), implement `/home/user/sanitize.py` to read the input file line by line and independently apply the *same* substring/character checks — exit 1 the moment any line matches a forbidden pattern, exit 0 only if every line is clean. Since it's graded against a hidden adversarial corpus, err on checking exactly the primitives you observed (don't over- or under-generalize). Separately handle the systems config: append `export DIAG_ENV=production` to `/home/user/.profile`, write `/home/user/audit.sh` to append `date` output to `/home/user/audit.log` and `chmod +x` it, then add a `*/5 * * * *` crontab entry invoking it.