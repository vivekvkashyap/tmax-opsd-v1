Treat `/app/legacy_scorer` purely as a black-box oracle: since it's stripped, don't try to disassemble it — instead systematically probe it with crafted inputs to infer the transformation. Start with simple, isolating test CSVs (e.g. a single row with metric_a=1, metric_b=0, metric_c=0, at a fixed prior) to isolate each metric's weight, then vary each metric independently while holding others at 0 to solve for the linear weights, then vary the prior with a fixed aggregate score to characterize how the score and prior combine (test values like prior=0.5 or extreme metric values to distinguish a simple weighted average from a Bayesian-odds-style update). Once you have a formula hypothesis, verify it against dozens of additional random (prior, csv-row) combinations before committing to `/home/user/scorer.sh`. Implement the pipeline in pure `awk` (reading `--prior` and the CSV path from `$1`/`$2`, looping over CSV lines, computing the aggregate and posterior formula, and printing with `printf "%s,%.6f\n"` for exact 6-decimal-place formatting) — avoid Python/Perl per the constraints, and double check rounding/formatting matches the oracle's printf behavior exactly (e.g. rounding vs truncation at the 6th decimal).
