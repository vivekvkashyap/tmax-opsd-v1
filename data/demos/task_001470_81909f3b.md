Build and test each piece in isolation before assuming it works together. For `/home/user/start_api.exp`, use the standard expect pattern: `spawn /home/user/legacy_api.sh`, `expect "Enter SRE passcode: "`, `send "SRE-8891\r"`, `expect "Start server? (y/n): "`, `send "y\r"`, then finish with `interact` (not `expect eof`) so the spawned server process keeps running attached to the expect process instead of being killed when the script would otherwise exit. For `/home/user/haproxy.cfg`, write a minimal config with a `global`/`defaults` section that avoids `daemon` and `user`/`group` directives (so it can run unprivileged in the foreground), a `frontend` bound to `127.0.0.1:8080` with `default_backend`, and a `backend` with a `server` line pointing at `127.0.0.1:9000`; validate syntax with `haproxy -c -f /home/user/haproxy.cfg` before moving on. For `/home/user/monitor.sh`, use `curl -s -o /dev/null --max-time 2 http://127.0.0.1:8080/` and branch on `$?` to append exactly `UP` or `DOWN` to `/home/user/uptime.log`, then `chmod +x` it. Finally write the standard `*/5 * * * * /home/user/monitor.sh` line to `/home/user/crontab.txt`.
