Build the audit script incrementally and verify each check independently before combining. For world-writable files, use `find /home/user/webapp -type f -perm -o+w` (checks the "others write" bit regardless of other permission bits). For the CSP/XSS check, iterate `.html`/`.js` files, grep for the literal `<script` (case-sensitive, use `grep -F` or fixed-string match to avoid regex surprises), then separately check the same file for `nonce=` or `integrity=`; only flag when `<script` is present AND neither marker is present — a common mistake is flagging any file containing `<script` without checking for the exempting markers, or using case-insensitive grep. For the PHP check, grep `.php` files for literal `eval(`. Collect all `path,issue` lines into an array/temp file, then pipe through `sort` before writing to `/home/user/audit_report.csv` so output is deterministic — sort the whole file, not just within each check, since multiple issue types interleave alphabetically by path first. Make the script executable with `chmod +x`, then actually run it and inspect the CSV to catch quoting or globbing bugs before finishing.