Start by OCR'ing the rule, then build the validator to satisfy it exactly. Run `tesseract /app/routing_memo.png stdout` (preprocess/upscale the image first if text is faint) to recover the specific forbidden-node identifier and the maximum-cost threshold stated in the memo — use those recovered values as constants in your C++ code, don't hardcode guesses. Write `validator.cpp` using `nlohmann::json` to parse a single line of stdin inside a try/catch: any parse exception, missing field, or wrong type must exit 1, not crash — this is the naive-attempt pitfall (uncaught exceptions terminate with a nonzero signal-based status rather than a clean `exit(1)`, and some verifiers may distinguish that, so catch broadly). Validate `schema_version` is the exact string "2.0", `path_id` is a string, `nodes` is an array of strings, `total_cost` is an integer — reject type mismatches like a float where an integer is required. Then apply the two business rules from the OCR'd memo: reject if the forbidden node appears anywhere in `nodes`, and reject if `total_cost` exceeds the extracted threshold. Compile with `g++ -std=c++17` linking nlohmann-json (header-only, so usually just `-I` if needed), and self-test with a handful of hand-written valid and invalid JSON lines covering each violation type before considering it done.