Get the failure bounds from the audio yourself before writing any C++: transcribe `/app/support_voicemail.wav` (e.g. `ffmpeg` to extract/convert audio, then a speech-to-text tool such as `whisper` or `pip install SpeechRecognition`+`vosk`/Google API if available) and read off the exact strict inequality bounds it states for the angular velocity `x` — don't guess typical "epsilon" values, the bounds are specific numbers spoken in the recording. Then look at `sensor_math.cpp`'s `(1 - cos(x)) / (x*x)` to understand why values in that narrow band cause catastrophic cancellation (1 - cos(x) loses precision for tiny x because it's a difference of nearly-equal floats). Write `triage_logs.cpp` to stream the CSV line-by-line (avoid loading the whole file, since it must handle large files efficiently — use buffered `ifstream` reads, not `std::vector` of the whole file), parse each value as a double, and check strict-inequality membership in the transcribed range; print `EVIL`/exit 1 on first match found, else `CLEAN`/exit 0 after reading the whole file. Compile with `g++ -O2` and test manually against a few files with values just inside/outside the boundary before trusting the corpus run.
