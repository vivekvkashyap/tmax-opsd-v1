OCR `/app/equation_params.png` (e.g. `tesseract /app/equation_params.png stdout`) to recover the exact oscillator name, stiffness parameter, initial conditions, time span, and required number of output points — use those recovered values, don't assume the placeholders already in the draft script. The draft's core bug is using `solve_ivp(..., method='RK45', ...)` with default loose tolerances on a stiff system, which diverges/produces NaNs for large stiffness; fix it by switching to a stiff-aware implicit method (e.g. `method='Radau'` or `'BDF'` or `'LSODA'`) and tightening `atol`/`rtol` (try values like `1e-8`–`1e-9`), then do a basic convergence check by re-running at a tighter tolerance and confirming the solution barely changes. Update `mu`, `y0`, `t_span`, and the number of evaluation points to the recovered values, use `t_eval=np.linspace(t_span[0], t_span[1], n_points)` so you get exactly that many evenly-spaced rows including both endpoints, write `t,y1,y2` to `/home/user/training_data.csv`, and separately plot `y2` vs `y1` to `/home/user/phase_portrait.png` to visually confirm a stable limit cycle. The grader compares your CSV numerically to a high-tolerance reference integration, so prioritize integrator accuracy over speed.