Start by reading `legacy.rb` carefully to understand the exact ABI and encoding contract it establishes with the C function — note the function signature (`compute_hash` taking a C string) and exactly how the hex string is decoded to raw bytes (Ruby's `pack("H*")` turns hex pairs into raw byte values, which you must replicate faithfully with Python's `bytes.fromhex`). Compile the C file with `gcc -shared -fPIC -o libmathops.so math_ops.c`. In `server.py`, use `ctypes.CDLL` to load the shared library, set `.argtypes`/`.restype` explicitly matching the C function's real signature (a `c_char_p` and a `c_long` return, based on what you read in the source), decode the query string hex parameter to bytes, call the C function, and write the integer result as the plain-text response body via `http.server`. For the Nginx config, write a `server { listen 127.0.0.1:9090; location /api/ { proxy_pass http://127.0.0.1:8080/; } }` block — the trailing slash on `proxy_pass` is what strips the `/api/` prefix, a detail that's easy to get backwards — plus `pid /tmp/nginx.pid;` and log directives pointing to `/dev/null` or `/tmp` since you lack root. Start both services backgrounded, then `curl` the proxy endpoint and redirect the raw response body to the log file, confirming manually that the proxy correctly forwards before finishing.