First OCR or read /app/policy_scan.png (e.g. `tesseract policy_scan.png -` if available, or view it directly) to recover the exact exclusion rules text — do not assume rules, extract them from the image. The policy will define byte-level conditions (e.g. a magic-byte prefix check and a substring check) that your C++ filter must implement precisely: read the file's raw bytes (open in binary mode), check if it begins with the specified magic bytes at offset 0, and separately scan the full byte content for the specified exact ASCII marker string anywhere in the file (a plain byte-wise substring search works for both text and binary files — don't assume text encoding). Return exit code 1 if either condition matches, else 0. Compile with `g++ -O2 backup_filter.cpp -o /home/user/backup_filter`. Then iterate /home/user/raw_data, run your compiled filter on each file, and for files that pass (exit 0) copy them into /home/user/safe_data/ with a `.safe` suffix appended to the original filename. Finally `tar -czf /home/user/backup.tar.gz -C /home/user/safe_data .` Test your filter manually against a few crafted evil/clean sample files before trusting it on the corpora.
