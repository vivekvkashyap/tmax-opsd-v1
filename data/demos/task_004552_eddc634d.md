Work through this as a pandas pipeline in stages, verifying each stage before moving on. Load `raw_metrics.csv`, then deduplicate on `(ts, server, metric)` keeping the last row — use `drop_duplicates(subset=[...], keep='last')`, not `keep='first'`, since retries append newer corrections after earlier bad values. Pivot to wide format with `df.pivot(index=['ts','server'], columns='metric', values='value')` (or `pivot_table`), then reset the index and sort by `ts`. Handle missing values per server: group by `server`, apply `interpolate(method='linear')` along the time axis first, then `bfill()` and `ffill()` in that order to cover leading/trailing gaps that interpolation can't fill. Compute each server's mean profile across all timestamps for every metric column (`groupby('server').mean()`), then compute pairwise Euclidean distances between server mean-vectors for every distinct unordered pair — don't forget to only consider each pair once and to order `server1 < server2` alphabetically in the output. Round the distance to exactly 4 decimal places with `round(x, 4)` and write the JSON in the exact key order/format requested to `/home/user/closest_servers.json`.
