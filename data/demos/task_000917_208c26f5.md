Work through this as four independent, verifiable sub-builds rather than one monolith. (1) For the bind mount, write a `[Service]` unit whose `ExecStart` runs `bindfs /home/user/app/config_source /home/user/app/config_mount` and whose `ExecStop` runs `fusermount -u` (or `umount`) on the mount point; enable it as a `default.target` want under the user's systemd instance (`systemctl --user enable --now app-mount.service`), and remember `bindfs` needs the mount point directory to already exist. (2) For the tunnel, use `ssh -N -L 127.0.0.1:9000:127.0.0.1:6379 user@localhost` inside a systemd unit with `Restart=always`, referencing the user's existing key via `-i` or ssh config, and disable strict host checking/agent prompts so it starts non-interactively. (3) Build the Go filter as a streaming `bufio.Scanner` over stdin: unmarshal each line into a struct, check the three malicious conditions (negative metric_value, forbidden substrings in metric_name, any tag string over 50 chars), and only `fmt.Println` the original raw line (not a re-marshaled one) when none apply — test it locally by piping known-good and known-bad JSON lines before wiring it into the pipeline. (4) Only after each piece works standalone, `go build` the binary, run `start_services.sh`, confirm both systemd units are active, and do an end-to-end curl to confirm data flows through all three services.