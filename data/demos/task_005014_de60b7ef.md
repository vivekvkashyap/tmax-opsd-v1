Treat this as a graph-processing pipeline built from plain-text parsing plus a real topological sort. Steps: (1) use `grep`/`awk` to extract triples by predicate — one pass for `lastBackupStatus` to find subjects whose literal isn't `"SUCCESS"`, one for `dbSize` to build a name→size map, one for `dependsOn` to build edges restricted to the two identified endpoints. (2) Sum sizes for only the identified (non-SUCCESS) databases and write the integer to the size file. (3) Build a dependency edge list (`dependent<TAB>dependency`) filtered to only pairs where both DB names are in the identified set, then feed it to `tsort` (which expects `child parent` pairs meaning parent-before-child ordering, so double-check the argument order tsort wants versus your dependsOn semantics — this is the most common bug) to get a valid backup order; include isolated identified DBs with no filtered edges too. (4) Emit one `/usr/local/bin/backup_tool --database NAME` line per identified DB in that order, add a `#!/bin/bash` shebang, and `chmod +x` the script. Extract DB_NAME by stripping the URI prefix (`sed 's#.*/##; s/>.*//'` style) consistently everywhere.
