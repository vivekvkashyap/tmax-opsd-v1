Inspect the schema first with `sqlite3 /home/user/network.db ".schema"` to confirm column names, then build the pipeline as a sequence of plain `sqlite3` invocations inside the bash script (using heredocs or `-cmd`/`.read`, or piping SQL via `sqlite3 db "SQL;"`). Step 1: `CREATE INDEX idx_source_weight ON edges(source_id, weight DESC);`. Step 2: create `top_edges` using a window function — `ROW_NUMBER() OVER (PARTITION BY source_id ORDER BY weight DESC, target_id ASC)` inside a CTE/subquery, then `CREATE TABLE top_edges AS SELECT source_id, target_id, weight FROM (...) WHERE rn <= 2;` — get the tie-break order right (weight DESC, target_id ASC) since it changes which edges survive. Step 3: use `json_object('id', node_id, 'label', label, 'category', category)` wrapped in `json_group_array(...)` for nodes, and similarly for edges sourced from `top_edges` (not the original edges table), then combine both arrays into one top-level `json_object('nodes', ..., 'edges', ...)` via a single SQL query, writing the raw output straight to graph.json. Step 4: a separate GROUP BY query joining `edges` to `nodes` on `source_id = node_id`, summing `weight` per `nodes.category`, ordered alphabetically, formatted with `sqlite3`'s output mode or shell `printf`/`awk` into the exact "Category: X, Total Weight: Y" lines. Make the script executable and actually run it so the output files exist, then spot check graph.json with `jq` and metrics.txt by eye against your own re-derivation of the aggregates.