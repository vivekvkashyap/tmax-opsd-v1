Extract the two secret parameters from the diagram first, then reimplement mechanically — don't guess the salt/prefix format. Run `tesseract /app/arch.png stdout` and read the output text carefully for the labeled SALT and PREFIX values (they'll appear as literal strings on lines in the diagram); copy them exactly as printed, since any typo makes every fuzz case fail. Then scaffold the Rust project with `cargo new /home/user/validator`, add `md5 = "0.7.0"` (or a compatible version) to Cargo.toml, and in main.rs: read `env::args()`, check for exactly one argument (print `INVALID` and return otherwise), concatenate token + salt into one string, compute its MD5 digest, format it as lowercase hex, and check with `starts_with(prefix)` to print `VALID` or `INVALID`. Build with `cargo build --release` so the binary lands at `target/release/validator`. Before trusting it, manually diff a handful of your binary's outputs against `/app/oracle_validator` on the same test tokens (same arg-count edge cases too) to catch formatting mismatches like trailing newlines or case differences in the hex string.
