Work in three independent tracks. First, OCR the config screenshot with `tesseract /app/alert_rules_screenshot.png stdout` (try `--psm` variants like 6 or 11 if output is garbled) and manually clean up the extracted regex/threshold/severity/destination values before hardcoding them into your script — do not trust OCR blindly, sanity-check against what a log-alerting rule set should look like. Second, treat the legacy binary as a black box: run it against a handful of synthetic log lines (matching the CRITICAL/ERROR + ConnectionRefused/Timeout pattern and non-matching lines) and diff your Python script's stdout byte-for-byte against the binary's stdout, iterating until the JSON key order, spacing, and field values match exactly — this is what the fuzz harness checks, so treat divergence on any generated line as a bug to fix, not noise. Third, for the systemd unit, add `After=dummy-mail-relay.service` and `Wants=dummy-mail-relay.service` (or `Requires=`) in the `[Unit]` section so it waits on the dependency instead of racing it at boot; set the scripts executable (`chmod +x`) and grant read/execute via `setfacl` if ACLs are required.