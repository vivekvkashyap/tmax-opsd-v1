For Part 1, edit the `[Unit]` section of `/home/user/.config/systemd/user/data-analyzer.service` to add exactly one new line, `After=data-collector.service`, without touching the existing `Description=` or other lines (note: `After=` alone only orders startup — it doesn't add a hard requirement, which matches "start after" wording here). For Part 2, write `/home/user/deploy.sh` and `chmod +x` it: use `df --output=avail /home/user | tail -1` (KB units) to check free space, exit 1 with an error message if under 10000; `mkdir -p /home/user/deploy/releases/v3`; write `config.ini` with exactly `secure_mode=true` and `chmod 0400` it; then atomically repoint the `current` symlink using `ln -sfn /home/user/deploy/releases/v3 /home/user/deploy/current` (the `-n` flag is essential — without it, `ln -sf` on an existing symlink-to-directory creates a nested symlink inside the target instead of replacing it, breaking idempotency). For Part 3, run the script, then hand-build `/home/user/verification.log` with exactly three lines: the `readlink -f` of `current`, the octal mode of `config.ini` (via `stat -c %a`), and the literal `After=` line you added.