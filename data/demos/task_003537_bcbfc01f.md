Structure `evader.cpp` as four strict, sequential stages mirroring the spec. First, load `ca.crt` and `server.crt` with OpenSSL's X509 APIs (`PEM_read_X509`, build an `X509_STORE`, add the CA, create an `X509_STORE_CTX`, call `X509_verify_cert`) and `exit(1)` immediately if verification fails — this is the gate the verifier checks for by grepping the source, so use the real API rather than a shortcut string comparison. Second, read `payload.enc` as raw bytes, reverse the stated encoding order precisely: Base64-decode first (OpenSSL's `EVP_DecodeBlock` or a manual decoder), then XOR every resulting byte with `0x42` — get the order right, since decoding then XOR-ing is different from XOR-then-decode. Third, hash the decrypted bytes with `EVP_Digest`/SHA256 APIs, format as lowercase hex, and compare against the target hash string given in the task using constant-time or simple string comparison; `exit(2)` on mismatch. Fourth, on success write the decrypted bytes to `/home/user/decoded.bin` and `exit(0)`. Compile with `g++ evader.cpp -o evader -lssl -lcrypto`, run it, and check the exit code and file contents match expectations before considering it done.
