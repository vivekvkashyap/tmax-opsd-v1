Write `analyze.sh` to take `$1` as the input CSV, skip the header with `tail -n +2` or `awk 'NR>1'`, and filter valid rows using `awk -F,` logic: `cohort` must equal exactly `Control` or `Treatment`, and `amount` must match a positive-number regex (e.g. `$3 ~ /^[0-9]+(\.[0-9]+)?$/ && $3+0 > 0`) so empty strings, `NaN`, and negatives are excluded — don't rely on `awk`'s implicit numeric coercion alone since it silently treats non-numeric strings as 0, which would wrongly pass some checks and wrongly fail others. Use two separate `awk` passes (or one pass accumulating sums/counts per cohort in arrays) to compute `sum` and `count` for each cohort, then compute means as `sum/count`. Compute the absolute difference of the two means. Format all three numbers to exactly 2 decimal places — `awk`'s `printf "%.2f"` handles rounding correctly (avoid `bc` for rounding since it truncates, not rounds, unless you add 0.005 manually). Print the three lines in the exact required format, make the script executable with `chmod +x`, then run it against `/home/user/dataset.csv` and redirect stdout to `/home/user/results.txt`.