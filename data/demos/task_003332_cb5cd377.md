Write `/home/user/build_and_test.sh` as a straight-line bash script (with `set -e` style checks) that does each step and fails fast: (1) heredoc a minimal AT&T-syntax x86_64 assembly file with a `_start` label that loads syscall number 60 (`sys_exit`) into `%rax`, loads the exit code 73 into `%rdi`, and issues `syscall`; (2) assemble and link with `as calc.s -o calc.o && ld calc.o -o calc`; (3) run `./calc`, capture `$?` immediately after (don't run anything else in between that could overwrite it), and compare against 73, exiting non-zero on mismatch; (4) dump the compiled binary's raw bytes to a continuous lowercase hex string with no whitespace — `od -v -t x1 -An /home/user/calc | tr -d ' \n'` is a reliable approach, watch out for leading/trailing spaces `od` inserts; (5) loop over each character of that hex string, and for characters matching `[0-9]` add their numeric value to a running sum (skip a-f entirely, don't try to convert them); (6) write the final integer to `/home/user/math_result.txt` with no extra formatting. Make the script executable (`chmod +x`) and run it once yourself to confirm the file gets created without errors before considering the task done.
