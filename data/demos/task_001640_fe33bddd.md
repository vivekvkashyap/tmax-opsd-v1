Implement Kahn's algorithm precisely as specified, then wire up the two network layers around it. Parse each `.txt` file in `/home/user/math_nodes/` into a dependency map (module name → list of deps, empty list if the file is empty), and build the reverse edge list (module → modules that depend on it) plus an in-degree count per module. Maintain the "ready" set as a sorted structure (e.g. re-sort a list, or use a min-heap/sorted set) rather than a plain FIFO queue: repeatedly pop the alphabetically-smallest zero-in-degree module, append it to the output order, decrement the in-degree of everything that depended on it, and insert any newly-zero modules back into the sorted ready set — this tie-break is what the grader checks byte-for-byte, so don't just iterate the directory listing in filesystem order. Serve this via a small HTTP server on 127.0.0.1:8080 with `GET /build_order` returning the JSON array with `Content-Type: application/json`. Then write an nginx.conf that overrides `pid`, `error_log`, and `access_log` to paths under `/home/user/` (required since you lack root), listens on 127.0.0.1:9000, and proxies `/api/v1/build_order` to `http://127.0.0.1:8080/build_order`; start both processes in the background and confirm the proxied curl round-trip works before finishing.
