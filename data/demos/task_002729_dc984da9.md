In `/home/user/analyze_graph.py`, load both CSVs with the `csv` module and populate two tables (`nodes`, `edges`) in an in-memory `sqlite3.connect(':memory:')` database using `executemany`/`INSERT` with the appropriate columns, then create indexes if needed for clarity. Build a single SQL query that self-joins `edges` twice (as `e1` for A->B and `e2` for B->C) and joins `nodes` three times (aliased for A, B, C), filtering: A's department='Engineering' and status='active'; C's department='Finance' and status='active'; B's status='inactive'; and `e1.message_size + e2.message_size > ?` using a `?` placeholder bound via `cursor.execute(query, (threshold,))` — never interpolate the threshold into the SQL string. Select `e1.source_id AS start_node, e2.target_id AS end_node, (e1.message_size+e2.message_size) AS total_size, MAX(e1.timestamp, e2.timestamp) AS latest_timestamp`, order by `latest_timestamp DESC, total_size DESC`, and `LIMIT 5`. Read the threshold from `sys.argv[1]` and cast to int. Write results to `/home/user/results.csv` with `csv.writer`, including the header row, matching the column names/order exactly as specified.