Break this into independently-testable pieces before wiring them together. For Phase 1, use `ffmpeg` (e.g. `-vf fps=10` and a raw/PGM pipe, or extract PNGs then read pixel data) to get per-frame grayscale brightness outside Rust if easier, or shell out from Rust — the key correctness point is frame-to-timestamp mapping: frame i corresponds to i/10.0 seconds (10fps), and you must join each frame's timestamp to the *closest* telemetry timestamp, not just index-align, since telemetry timestamps might not perfectly coincide. For missing `speed` values, implement linear interpolation between the nearest valid neighbors by timestamp order (not just forward/backward-fill), and separately count how many rows you actually interpolated for the log. For Phase 2, don't hardcode against the sample corpus — instead detect the actual mathematical signature of "leakage": in a rolling/causal normalization, row j's scaled value only depends on raw values up to j, so a global-mean/std normalization will show statistical inconsistency (e.g., correlate scaled values against a rolling stat computed causally over the same raw column and check if it matches, or check whether reconstructing raw values from scaled values via a rolling estimate is self-consistent) — build and test this detector against the given `/app/corpus/clean` and `/app/corpus/evil` first, then generalize. Write `/home/user/experiment_log.json` last, deriving all three fields from your actual computed pipeline outputs, not guessed constants.
