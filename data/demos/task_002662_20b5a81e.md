Work in two clearly separated stages. First, extraction: write `safe_extract.py` using Python's `zipfile` module. For every `ZipInfo` entry in every `.zip`, resolve what the final extraction path would be (e.g. `os.path.normpath(os.path.join(extract_root, entry.filename))`) and compare it against the resolved `extract_root` using `os.path.commonpath` or a prefix check on the normalized absolute paths — never just check for a literal `"../"` substring, since normalization can hide traversal (and absolute paths in the entry name must also be rejected). Any entry that resolves outside the root gets its original archive-relative path logged to `skipped_files.log` and must not be written anywhere; only extract entries that pass the check, using `zip.extract` or manual `open()`/`write()` after the safety check, never `extractall()` blindly. Second, analysis: parse `v1/config.json` and `v3/config.json`, iterate keys present in both dicts, keep only those whose values differ, and write a CSV with header `Key,V1_Value,V3_Value`, rows sorted alphabetically by key, with Python's native `str()` representation of each value (e.g. booleans render as `True`/`False`) unless the task shows otherwise.