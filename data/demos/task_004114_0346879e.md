Start by isolating the build failure: run `pip install /app/vendored/seq_scorer-1.0.0` to see gcc's actual error, then open setup.py and look at the Extension's `extra_compile_args`/`extra_link_args` for a flag gcc rejects — remove just that flag, then reinstall and confirm `import seq_scorer; seq_scorer.score_sequence(...)` works. Next, read `/app/data/observational.h5` with h5py, load the `reads` dataset (decode bytes to str if needed), and score every sequence with the extension to get a fixed array of scores — deterministic once the build is fixed. For the bootstrap CI, seed exactly once before generating the 10,000 resamples (`np.random.seed(42)` then repeatedly `np.random.choice(scores, size=len(scores), replace=True)`, take the mean of each resample, then `np.percentile` at 2.5 and 97.5) — reseeding inside the loop or seeding after any other random call changes the result. Wire up `/ci` and `/score` as a small Flask/http.server app referencing these precomputed values, and keep it running so the verifier can hit it.