Work this as four independently-verifiable layers, and check each layer before moving to the next. (1) Build/link: inspect `setup.py`'s `Extension(...)` call — a C extension that depends on an external `.so` needs both `library_dirs` (so the linker finds it at build time) and `runtime_library_dirs` (rpath, so the loaded module finds it at runtime without needing `LD_LIBRARY_PATH` set); verify with `ldd` on the built `.so` that it resolves to the real library path, not "not found". (2) State machine: trace `parse_data` line by line against the actual contents of `data.txt` — the bug is a logic error in how/when the state transitions between IDLE and RECORDING (e.g. wrong condition order, state not updated before processing a line, or START/STOP not gating VAL processing correctly); fix so a VAL is only counted while in RECORDING state, and validate by hand-computing what the correct running total should be from the file. (3) Process management: launch app.py in the background and confirm it's actually listening (`curl`/`ss`) before configuring anything downstream. (4) Reverse proxy: write a non-root nginx.conf with its own `pid`/log paths (root nginx configs assume privileged paths) listening on 8080 and proxying to 127.0.0.1:9000, then verify end-to-end with curl before writing the result file.