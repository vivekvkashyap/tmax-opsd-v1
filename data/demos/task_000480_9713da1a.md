This is a multi-service equivalence task — fix each broken layer and verify it in isolation before chaining them, since a single misconfiguration anywhere breaks the whole request path. Start with `/home/user/app/reference.py`: read `compute_and_merge_ref` carefully (it builds two sequences via sin/cos, concatenates, sorts, and likely slices to a fixed count) and port that exact logic into `compute_and_merge` in `poly.c`, matching iteration order and output length precisely since results must match bit-for-bit. Fix the Makefile to add `-shared -fPIC` (required to build a loadable `.so`) and link `-lm` for the math functions; run `make` and confirm `libpoly.so` is produced, then sanity-test it directly with a small `ctypes` script before touching Flask. In `api.py`, load the library with `ctypes.CDLL`, declare `argtypes`/`restype` correctly (an output array arg typically needs a `ctypes.c_double` array pointer), check Redis for `poly_merge:{n}:{m}` first, and on miss compute, `json.dumps(..., separators=(',',':'))` to match the reference's compact format, `setex` with 60s TTL, then return it. Fix `nginx.conf`'s `proxy_pass` to point at `127.0.0.1:5000`, not any placeholder/wrong port. Finally run `start_services.sh` and curl `http://localhost:8080/api/merge?n=...&m=...` directly, diffing against `python3 reference.py N M` before considering it done.