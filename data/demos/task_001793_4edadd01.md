Tackle this as three independent fixes that must all interoperate at the end. First, in `audio_filter.c`, find the convolution loop indexing `input[i - j]` without bounds-checking — when `j > i` this reads before the start of the array (undefined behavior/garbage). Fix it by guarding with `if (i - j >= 0) sum += input[i-j] * filter[j];` (i.e., treat out-of-range samples as zero, which is the mathematically correct "full" convolution boundary condition), then rebuild the extension with `python3 setup.py build_ext --inplace` and confirm it imports cleanly under Python 3. Second, port `process.py` to Python 3: replace `print "..."` statements with `print(...)`, `xrange` with `range`, fix `dict.iteritems()`/`has_key()`-style calls, and any `str`/`bytes` handling around the WAV/binary data (Python 3 is strict about bytes vs str) — do not touch the filter coefficients or math. Third, write a migration script that opens `records.db`, reads each `audio_records` row, inserts filename/duration into the new `metadata` table, then unpacks `data_blob` as a sequence of little-endian 32-bit floats (`struct.unpack(f"<{n}f", blob)`) and inserts one row per sample into `samples` with sequential `sample_index` starting at 0, referencing the new `metadata.id`. Finally run the fixed Python 3 pipeline against `/app/test_signal.wav` to produce `/home/user/output_signal.wav` and populate `/home/user/migrated.db` — the MSE check against a reference `np.convolve` will fail immediately if the boundary fix above is wrong.
