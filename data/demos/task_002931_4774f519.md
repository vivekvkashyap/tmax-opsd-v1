Run OCR on the image (e.g. `tesseract /app/network_params.png stdout`) to recover the `Source -> Dest : Weight` lines, then parse that text into the adjacency weights — read the OCR output carefully before hardcoding it into Go, since misread digits/decimal points are the most likely source of error here. The reproducibility bug is that the current code iterates a Go `map` (whose iteration order is randomized) and accumulates into a shared slice via concurrent goroutines with a mutex, so floating-point addition happens in a different order each run; fix it by replacing the map-based adjacency with a fixed `[4][4]float64` matrix (or otherwise iterating over a deterministically sorted list of edges) and doing the state update in a single sequential loop with no goroutines — this guarantees the same summation order, and hence bit-identical output, on every run. Run exactly 1000 iterations from `[1,0,0,0]`, and print each of the 4 final values with high precision (at least 15 decimal places) to steady_state.txt. Before finishing, run the built binary several times and diff the outputs to confirm zero variance.
