Start by opening both CSVs and building a mental (or scratch) adjacency list: each edge_type (HAS_ROLE, ROLE_INHERITS, CAN_ACCESS) is just a directed edge for graph purposes—treat the whole thing as one directed graph regardless of type. In your C++ program, read nodes.csv into a name-to-id and id-to-name map, read edges.csv into an adjacency list keyed by source_id, then run BFS (unweighted shortest path, since all edges have equal weight) from the node whose name is "Intern_Bob" to the node whose name is "Financial_DB_Prod". Track parent pointers during BFS so you can reconstruct the path by walking backwards from the target once found, then reverse it. Watch for the two common pitfalls: (1) CSV parsing bugs from naive split-on-comma if any field could contain quotes/commas (verify by inspecting actual file content first), and (2) writing the output with a trailing space or wrong delimiter—the spec requires an exact comma-separated line with no spaces, or "NONE" if BFS never reaches the target. Compile with the exact g++ command given, run it, then verify the log file content directly.
