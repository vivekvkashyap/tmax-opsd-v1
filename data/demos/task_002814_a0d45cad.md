Work through this in the four stated steps, but pay special attention to the security check since that's where naive implementations fail. In the C updater: for each manifest line, split on ` | `, build the candidate destination by concatenating target_dir and the relative path, then resolve it safely — since the destination file may not exist yet (so `realpath` on the full path can fail), resolve the *parent directory* of the destination with `realpath`, then compare that resolved absolute path against the resolved absolute path of `<target_dir>` using `strncmp`/prefix comparison (checking the boundary lands on a `/` or exact match, not just a string prefix, to avoid e.g. `/target_evil` matching `/target`). If resolution fails or the check fails, print `SKIPPED: <path>` and move to the next line without creating anything. If it passes and the destination file already exists, copy it into backup_dir preserving the relative path structure (mkdir -p equivalent via recursive directory creation) before overwriting. Use standard `mkdir`/`stat`/`fopen` calls, creating intermediate directories as needed for both target and backup trees. After running the updater, use `find ... -exec sed -i` (or a loop) to replace `[COMPANY_NAME]` with `AcmeCorp` in all .md/.txt files under docs_target. Finally write gen_manifest.sh using `find -type f -exec sha256sum {} +` piped to `sort` by path, redirected to final_manifest.txt.
