Write `/home/user/extract.py` to load and iterate the JSON list from `/home/user/traffic.json`, filtering first to POST requests targeting the upload endpoint, then for each candidate parse the `Content-Disposition` header value to pull out the `filename="..."` field (a regex like `filename="([^"]+)"` is sufficient). The path-traversal indicator to look for is a filename value that isn't a bare name confined to the intended upload directory — i.e. one containing `../` sequences that would resolve outside the sandbox root when combined with the base upload path; a normal upload like `profile.jpg` has no such sequences and should be ignored. Once you've identified the single matching request, write its `body` field's contents to `/home/user/extracted_payload.txt` and the matched filename string to `/home/user/target_path.txt`, using exact writes (`f.write(...)` without a trailing newline unless the source content itself includes one) since the verifier checks exact byte content — avoid `print()`, which appends a newline, and avoid re-encoding or re-escaping the extracted strings.
