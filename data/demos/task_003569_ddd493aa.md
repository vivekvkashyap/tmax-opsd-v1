Write the Go program to be fully signature-driven rather than extension-driven: walk `/home/user/project_files` with `filepath.Walk` (or `WalkDir`), skip directories, open each regular file, read just the first few bytes (a handful is enough — ZIP's magic is 4 bytes `50 4B 03 04`, GZIP's is 3 bytes `1F 8B 08`), and compare those raw bytes directly (e.g. against `[]byte{0x50,0x4B,0x03,0x04}` and `[]byte{0x1F,0x8B,0x08}`) — do not use filenames or extensions to decide. Handle read errors (unreadable files) by skipping them rather than aborting the whole walk. Print each matching file's absolute path (use `filepath.Abs` if walking with a relative root) to stdout, one per line. Compile and run the program, pipe its output through `sort`, and redirect into `/home/user/found_archives.txt`. Sanity check by manually inspecting a few files with `xxd`/`hexdump` to confirm which ones truly carry the magic bytes before trusting the program's output.
