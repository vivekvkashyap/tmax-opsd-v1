Approach this as a two-part investigation: secret recovery from git history, then a binary-parsing bug fix. For the secret: run `git log --all --oneline` in `/home/user/uptime_monitor` to see all commits including ones where files were later deleted, then use `git log --all --follow -p -- config.json` (or `git show <commit>:config.json` for the commit before the deletion) to view the removed file's contents and extract the secret value from it — do not guess it. For the bug: read `process_heartbeats.py` and find where it unpacks the 4-byte timestamp field with `struct.unpack`; a signed format code (`>i`) will interpret values with the high bit set (>= 2^31) as negative, which is why large uptime values trigger the "Negative timestamp detected" error — change it to the unsigned equivalent (`>I`) so values up to 2^32-1 parse correctly. Then export the recovered secret as `HEARTBEAT_SECRET` in the environment (needed for the HMAC verification of each packet against the pcap payload) and rerun the script against `data/heartbeats.pcap`, confirming `max_timestamp.txt` is written with the correct integer.