Work this as a layered decrypt-then-analyze pipeline in Python, verifying each stage before moving to the next. First, load `private.pem` with `cryptography` (or `Crypto.PublicKey.RSA` + `PKCS1_v1_5`) and decrypt `symmetric.enc` using RSA PKCS#1 v1.5 padding — the result is a base64-encoded Fernet key; keep it as bytes, don't re-encode it. Feed that key straight into `cryptography.fernet.Fernet(key).decrypt(...)` on the raw bytes of `logs.enc`; the decrypted payload is JSON Lines, so split on newlines and `json.loads` each line, skipping blanks. For the SQLi/XSS counts, scan each entry's `payload` field for the *exact* substrings given in the task (don't build a broader regex or you'll over/under count entries like the `DROP TABLE` line that's deliberately excluded from the trigger list) — count an entry once per category even if it matches on either of the two SQLi substrings. For the sudoers file, parse line by line, skip lines starting with `#` (comments) and blank lines, and match entries that literally contain `NOPASSWD: ALL`, excluding `root` and excluding users with restricted command lists (e.g. `/bin/ls`). Write the final JSON with sorted `critical_sudo_users` to the exact output path specified.
