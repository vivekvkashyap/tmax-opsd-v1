Read `src/main.rs` closely before changing anything — there are two distinct bugs to isolate. First, the state variable's type is too narrow for the arithmetic being done on it, causing silent wraparound; widen it (e.g. from `i8` to `i32`) so `100 - x` and the running sum can't overflow, and make sure any `wrapping_*` arithmetic is replaced with normal arithmetic once the type is wide enough (or kept safe if you intentionally retain wrapping semantics but with a sufficiently large type). Second, the step is computed via integer division `(100 - x) / 2`, which truncates toward zero and can produce a step of `0` while `x` is still less than 100, causing an infinite-looking loop that only stops at the 10_000 failsafe — after computing `step`, add a check: if `step == 0 && x < 100`, force `step = 1`. Keep the `10_000` failsafe in place as a safety net, not as the primary termination condition. After fixing, `cargo run` (or build+execute the binary) redirecting stdout to `/home/user/result.txt`, and sanity-check that convergence now happens well under the failsafe limit for typical starting values before trusting the final sum.