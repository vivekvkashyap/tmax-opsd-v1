Since correctness is judged by fuzz-matching against a hidden oracle binary, spend real effort nailing exact edge-case behavior rather than a "close enough" reimplementation — you cannot inspect the oracle's source, only its observable stdout on many random inputs, so black-box probe it with varied handcrafted inputs (missing fields, wrong types, malformed JSON lines, boundary temperatures, and strings with valid/invalid `\uXXXX` escapes) to learn its exact behavior before finalizing your Rust logic. First transcribe `/app/schema_rules.wav` (use an available speech-to-text tool) to learn the precise validation rule text — don't assume standard constraints, derive them from what's actually said. Scaffold the Rust project with `cargo new`, read stdin line-by-line, parse each with `serde_json`, and drop any record failing the transcribed schema rule. For the unicode-escape requirement in `notes`, write a manual scanner over `\uXXXX` sequences (don't rely solely on serde's escape handling) that validates hex digits and correctly pairs UTF-16 surrogate pairs, substituting `U+FFFD` for anything malformed or unpaired, then re-encode to UTF-8. Build in release mode so the path in the requirement is populated, and only after your manual probing matches the oracle closely should you consider the implementation ready. Finally write the orchestration bash script piping raw data through your binary into `sqlite3 .import` (or equivalent) into the `readings` table.