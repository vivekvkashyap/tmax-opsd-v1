Start by getting the vendored package installable: `cd /app/vendor/csv-time-bucketer-1.2.0 && cat setup.py` and look closely at the import line — a single-character typo (a missing letter in `from setuptools import setup`) will break the parse before setuptools even runs; fix the syntax error, then `pip install -e .` and verify with `python -c "import csv_time_bucketer"`. For the server, build the endpoint in layers so each requirement is independently testable: (1) check `X-API-Key` header first and short-circuit 401 before touching the body; (2) parse `charset=` out of the `Content-Type` header yourself (don't assume utf-8) and decode the raw bytes with that codec before handing to `csv.reader`; (3) dedupe by keeping, per `record_id`, the row with the lexicographically greatest ISO8601 `event_time` string; (4) normalize `message` with a regex like `re.sub(r'\s+', ' ', text.lower()).strip()` — apply normalization AFTER dedup, not before; (5) call `bucket_by_hour(records, 'event_time')` and return its dict as-is via `json.dumps`. Test manually with curl using a UTF-16LE-encoded body before declaring done.