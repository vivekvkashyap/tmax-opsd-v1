First fix `/app/start_services.sh` by adding `export REDIS_HOST="127.0.0.1"` (or whatever value the redis service actually listens on) before the API processes start, then launch the services and confirm with `curl localhost:8000/...` and `curl localhost:8001/...` that they respond before doing any analysis. Fetch `/sequences` and compute the 20-bp consensus by, for each of positions 0-19, tallying the base that appears most often across the 5 sequences at that position (simple majority vote per column) — then count G/C bases in that computed consensus to get GC content as a percentage, don't guess this value. Fetch `/kinetics` for `time` and `fluorescence` arrays. Implement the ODE system with `scipy.integrate.solve_ivp`, using P(0) = your computed GC_content and A(0)=0.01, and wrap the simulate step in an objective function that returns MSE between simulated A(t) at the observed time points and the real fluorescence; minimize over k1,k2 with `scipy.optimize.minimize` (bounds [0.001,1.0], e.g., method='L-BFGS-B') or `curve_fit` with those bounds, trying a few different initial guesses if it doesn't converge well. POST `{"k1":..., "k2":..., "gc_content":...}` to `/submit`, inspect the returned MSE, and save the full JSON response verbatim to `/home/user/submission_result.json`. If MSE isn't under 0.05, refit with better optimizer settings before submitting again.