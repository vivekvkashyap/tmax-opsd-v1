First fix the vendored dependency before writing your own logic: open `/app/gjson/gjson.go`, find the `Float()` method on `Result`, and notice it's been made to always `return 0` instead of parsing `t.Str` with `strconv.ParseFloat` — restore the real parse-and-return behavior. Point your module at the local fix via a `go.mod` `replace github.com/tidwall/gjson => /app/gjson` directive so your build actually uses the patched copy. For `config_tracker.go`: parse each JSONL line with gjson for `host`, `metric`, `ts`, `val`; group by (host, metric), sort each group's samples by `ts`, and on duplicate `ts` keep the last one read (a plain map-overwrite in read order does this naturally). Build the regular 10-second grid from the group's min ts to the largest grid point <= max ts; for each grid point take the exact match if present, else linearly interpolate between the nearest surrounding samples, guarding division-by-zero when bracketing timestamps coincide. Format `val` to exactly 2 decimals, order output by host then metric then ts, and write header-less CSV to stdout. Test against varied synthetic streams (gaps, duplicate timestamps, irregular spacing) before trusting the interpolation logic.
