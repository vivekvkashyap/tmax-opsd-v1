Locate the bug by testing the vendored library directly first: run something like `python3 -c "import sys; sys.path.insert(0,'/app'); import semantic_version as sv; print(sv.SimpleSpec('>=1.5.0').match(sv.Version('1.5.0')))"` — if it returns False/wrong, grep `/app/semantic_version/semantic_version/base.py` for the comparison operators inside `SimpleSpec`'s `match`/clause-evaluation logic (search for `_cmp`, `>=`, `<=`, `Clause` classes) and find where an operator was inverted (e.g. `>=` swapped to `<`, or vice versa); fix only that inverted comparison, don't rewrite the library, and re-test with several boundary cases (`==`, `>=`, `<=` at exact version equality) to confirm correctness. Then build `/home/user/resolver.py` as a plain `http.server`-based service on `127.0.0.1:8888` with a `/resolve` route: check `Authorization: Bearer test-agent-token-99` (401 otherwise), load `/home/user/registry.json`, use your fixed `SimpleSpec(req).match(...)` filtered against `Version(...)` for each candidate to find all satisfying versions of `pkg`, pick the max via `semantic_version` version comparison, then repeat one level for its listed deps. Compute the checksum exactly as specified: format `pkgname==version` per resolved entry (including the target package itself), sort alphabetically, join with `\n`, and `hashlib.sha256(...).hexdigest()`. Keep the server running for the test suite to query.