Don't hardcode the schema — the task explicitly says column names vary between DB versions, so discover them programmatically. In `extractor.go`, open the SQLite file with `database/sql` + `github.com/mattn/go-sqlite3`, query `sqlite_master` (`SELECT name FROM sqlite_master WHERE type='table'`) to find the one table whose name has prefix `env_data_`, then use `PRAGMA table_info(<table>)` to inspect its columns and their declared types: pick the column with type `DATETIME` as the timestamp, the column whose name contains "temp" as temperature, and the column whose name contains "id" but is NOT the primary key column literally named `id` as the sensor ID. Build the query dynamically with those discovered column names: `SELECT <id_col>, <ts_col>, <temp_col> FROM <table> WHERE <temp_col> > 20.0 ORDER BY <ts_col> DESC LIMIT 50`. Stream results out as JSONL to `/home/user/anomalies.jsonl`, one `{"sensor_id":...,"reading_time":...,"temperature":...}` object per line with those exact key names and temperature as a float64. Run `go mod init`, `go get` the sqlite3 driver, build, and execute — then eyeball the output count and ordering to confirm the dynamic schema discovery actually found the right columns rather than silently matching the wrong ones.