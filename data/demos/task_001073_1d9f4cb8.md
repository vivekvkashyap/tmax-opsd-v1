Break this into clear stages and validate each with the small provided-shaped data before trusting the whole pipeline. First write a per-file worker function that: reads one CSV, parses `Raw_Time` handling both `YYYY-MM-DD HH:MM:SS` and `MM/DD/YYYY HH:MM:SS` formats (try one parser format, fall back to the other, or use a flexible date parser), floors each timestamp to the top of its hour, then melts the wide columns (everything except Raw_Time) into long rows by splitting each column name on the first underscore into `server_name`/`param_name`, dropping rows with empty/NaN values. Dispatch one worker per input file using `concurrent.futures.ProcessPoolExecutor` (or `multiprocessing.Pool`) so real parallel processing occurs, then concatenate all workers' long-format results in the main process. Group by (aligned_hour, server_name, param_name) and take the max param_value per group — this is where duplicate-hour updates collapse. Format aligned_hour back to `YYYY-MM-DD HH:00:00` strings, sort by the three key columns ascending, and write the CSV with exactly those four headers. Run the script for real and inspect the output file's first/last few rows manually against your own mental trace of the two sample batch files to confirm alignment and max-aggregation are correct before finishing.