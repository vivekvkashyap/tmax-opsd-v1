This is a straightforward graph shortest-path problem gated behind correct data loading — get the plumbing right first. In `src/main.rs`: read and parse `systems.json` with `serde_json` into a `Vec` of structs (id, name), then build a `HashMap<String, i64>` from name to id so you can look up "PublicWeb" and "SecureVault" by name. Open `access.db` with `rusqlite::Connection::open`, and run a parameterized query (`SELECT source_id, target_id FROM network_links WHERE protocol = ?1`, bound to "SSH") — never string-interpolate the protocol value even though it's a literal here, to keep the pattern correct. Build an adjacency list (`HashMap<i64, Vec<i64>>`) from the returned rows, treating edges as directed exactly as given (source -> target). Run a standard BFS from the PublicWeb id, tracking hop-distance, and stop as soon as you dequeue the SecureVault id; if the BFS exhausts without reaching it, the answer is -1. Write only the resulting integer (no extra whitespace/newline issues) to `shortest_path.txt`. Run with `cargo run` from `/home/user/audit` and inspect the output file to confirm it's just the integer.
