Read `sim_search.c` carefully around the dot-product accumulator: a variable declared as `int` where it should be `float`/`double` will silently truncate every partial product from the float embeddings to zero (since values are typically fractional and less than 1 in magnitude), producing near-zero similarities everywhere — fix the type of that accumulator (and check norm accumulators too, though those are likely already float). Recompile with `gcc -o /home/user/sim_search sim_search.c -lm`. Run it and redirect/capture its `index similarity` output lines, then in a short script (or by piping through `sort -k2 -g -r` / a small Python/awk snippet) sort by the similarity column descending and take the top 5 indices — write just the index numbers, one per line, in that descending-similarity order to `/home/user/top5.txt`. Before finalizing, sanity check by re-deriving similarity independently (e.g. a tiny Python numpy script reading the same `.bin` files with the documented dtype/shape: 1000x128 float32 vectors and one 128-float32 query) to confirm your C program's top-5 ranking is self-consistent, since the vectors are read directly as raw float32 binary with no header.