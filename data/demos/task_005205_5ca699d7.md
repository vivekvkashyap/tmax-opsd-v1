Start by recovering the hidden configuration: run OCR (e.g. `tesseract /app/system_specs.png stdout`) on the image to read out the exact sensor column list and the SALT string — don't guess or hardcode assumed values, use exactly what the OCR returns (double check ambiguous characters like O/0 or l/1 manually if the OCR output looks odd). Then design the C++ processor around explicit per-row validation: for each row, check every sensor field against a strict numeric-character set (digits, at most one `.`, optional leading `-`) and reject the whole row on any violation — do this with a dedicated validator function, not a permissive `atof`/`stod` call, since those silently accept garbage like "1.2e5" or partial parses that a naive approach would let through. For hashing, build the exact string `<timestamp>_<sensor>_<value>_<SALT>` and feed it to `std::hash<std::string>` per the spec — don't reformat numeric values (e.g. don't strip trailing zeros) since the hash must match the literal input string. For the wrapper script, use `find ... -name '*.csv' | xargs -P <n> -I{} /home/user/cleaner {} <output>` so files are processed concurrently, and test against both well-formed and deliberately malformed rows before considering it done.
