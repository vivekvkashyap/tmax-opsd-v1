Build and validate the filter logic against the provided corpora before wiring up OCR/Makefile: write `security_filter.py` to check, in order, for an AWS key pattern (`AKIA` followed by exactly 16 uppercase alphanumeric chars — use a regex like `AKIA[A-Z0-9]{16}` and be careful it's exactly 16, not "at least"), the literal case-sensitive substrings `Traceback` or `Exception`, any HTML/XML tag shape (a regex like `<[^>]+>`), and internal IPs matching `10\.\d+\.\d+\.\d+` or `192\.168\.\d+\.\d+` — exit 1 if any pattern hits, else exit 0. Run it against every file in `/app/corpus/clean/` and `/app/corpus/evil/` to confirm 100% correct classification before moving on, since grading later reuses a separate held-out corpus with the same rules. For `test_filter.py`, use `hypothesis.strategies` restricted to alphanumeric characters (e.g. `st.text(alphabet=st.characters(whitelist_categories=('Lu','Ll','Nd')))`) so generated strings can't accidentally contain the evil patterns, write them to a temp file, and assert the filter subprocess exits 0. In the `Makefile`'s `check` target, run tesseract on `/app/screenshot.png` to a text file, then invoke `python3 security_filter.py` on it and let its exit code propagate naturally (don't swallow it) so `make check` fails when a leak is detected.