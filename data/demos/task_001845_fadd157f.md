Build this as a jq/awk pipeline inside a bash script rather than reaching for a full Python rewrite. Filter `/home/user/server_logs.jsonl` to records where `.status == "OK"`, extract the `target` field from each surviving record (in-degree only counts edges pointing INTO a server, so use `target`, not `source`), and count occurrences per target — e.g. `jq -r 'select(.status=="OK") | .target' file.jsonl | sort | uniq -c`. Convert those counts into the required JSON schema, sort primarily by count descending and secondarily by server name ascending for ties (watch out for `sort -k1,1nr` alone leaving tie order unstable — chain a secondary alphabetical sort key), then take the top 3. Use `jq -n` with `--argjson`/`--slurp` or build a small jq filter to assemble `[{"server": ..., "in_degree": ...}, ...]` directly so the output is valid strict JSON rather than hand-concatenated strings. Write the whole thing into `/home/user/process_graph.sh`, `chmod +x` it, run it, and diff the resulting `/home/user/top_targets.json` (via `jq -c .`) against your own hand-computed expected ordering before finishing.