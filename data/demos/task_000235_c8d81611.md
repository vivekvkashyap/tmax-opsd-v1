Treat this as four sequential troubleshooting phases, verifying each before moving on. First fix the environment: inspect requirements.txt for the Flask/Werkzeug version pin conflict and resolve it (relax or bump one of the pins so `pip install` succeeds and the app imports cleanly), then find where app.py reads `os.environ["APP_ENV"]` and ensure it's set to `production` when you launch the service (export it in your run command, don't just hardcode it in code). Second, use `git log` to inspect the repo history in /home/user/ticket_repo, and use `git bisect` (or manually diff each commit's transformation logic) to find the commit that changed empty-value handling from dropping keys to keeping them as empty strings; revert or fix that specific logic in app.py rather than blindly reverting the whole commit, since other unrelated changes may need to stay. Third, treat /app/legacy_transformer as a black-box oracle: pipe varied test strings (including edge cases — empty values, missing colons, trailing pipes) into it via stdin and diff its stdout against your Python endpoint's output for the same input; iterate on the Python parsing logic (split on `|`, then split each pair on the FIRST `:` only) until fuzz testing shows zero differences. Finally launch the Flask/WSGI service bound to 127.0.0.1:8080 and keep it running (background process or `&`) so it's live for verification.