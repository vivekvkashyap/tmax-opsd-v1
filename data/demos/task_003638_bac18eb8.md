Work through this in dependency order. First, install `tesseract-ocr` and run it against `/app/arch_diagram.png` (`tesseract /app/arch_diagram.png -`) to recover the `CI_TOKEN: <value>` line — do not guess the token, read it from the OCR output and use that exact string everywhere a token is required. Second, write the C service: use `libmicrohttpd` (or raw sockets) bound to `127.0.0.1:8080`, parse the `Authorization: Bearer <token>` header yourself and compare against the extracted token, return 401 on mismatch/absence. For the checksum, implement or link a standard CRC32 (IEEE 802.3, i.e. same polynomial as zlib's `crc32`/Python's `binascii.crc32`) over the raw request body bytes, format as 8-char lowercase hex, and build the exact JSON body requested. Compile to `/home/user/artifact_server` and background it. Third, install and configure nginx as a reverse proxy on `127.0.0.1:8000`: restrict the `/upload` location to `POST` only (nginx `limit_except POST { deny all; }` or equivalent) so nginx itself returns 405 for other methods before hitting the C backend, then `proxy_pass` POST traffic to `127.0.0.1:8080/upload`. Finally write `upload_artifact.sh` taking one file argument, curling it as the POST body to port 8000 with the correct Authorization header. Verify both nginx and the C server are still running at the end.
