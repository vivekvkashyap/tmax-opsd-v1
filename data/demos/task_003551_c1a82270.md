Get the statistical procedure exactly right and reproducible, since results are compared byte-for-byte against a reference run with the same GSL RNG seed. Install libgsl-dev, then write bootstrap.c to: read the 100 numbers from data.txt into a fixed-size array in file order; allocate a GSL RNG with `gsl_rng_alloc(gsl_rng_mt19937)` and seed it with `gsl_rng_set(r, 42)` exactly once before the sampling loop (not per-sample); for each of 1000 bootstrap iterations, draw 100 indices via `gsl_rng_uniform_int(r, 100)` in a fixed inner loop order, sum the corresponding data values, divide by 100 for that sample's mean; after all 1000 iterations, compute the overall mean of the 1000 bootstrap means and their sample standard deviation using B-1 (1000-1) in the denominator — get the draw order and division-by-(B-1) right since these are the two easiest places to silently diverge from a reference. Print with `%.6f` formatting matching the exact two-line "Overall Mean: ...\nStdDev: ..." format. Compile linking `-lgsl -lgslcblas -lm`, run once, and save stdout to result.txt.
