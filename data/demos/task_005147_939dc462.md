Structure `/home/user/policy_tester.py` as three independent checks feeding one JSON report. For SSH: parse `/home/user/sshd_config_test` line by line, build a dict of the last non-commented value per directive (ignore lines starting with `#`), then flag `PermitRootLogin` and `PasswordAuthentication` as violations (sorted alphabetically) if missing or not exactly `no`. For the firewall: parse `/home/user/iptables_export.txt` looking for a `-P INPUT <policy>` line — if it isn't `DROP`, add the exact violation string `"INPUT policy is not DROP"`; then scan `-A INPUT ... --dport <port> ... -j ACCEPT` rules and for every port other than 22, add `"Unauthorized port <port> ACCEPT"`. For credentials: read `/home/user/shadow_test`, split each line on `:` to get username and hash field (format `$id$salt$hash`), then for each candidate word in `/home/user/wordlist.txt` recompute the hash with the same algorithm/salt (Python's `crypt` module handles SHA-512 `$6$` hashes directly — `crypt.crypt(word, existing_hash)` and compare equality) and record usernames whose password matches a wordlist entry. Assemble the three lists into the specified JSON keys, keep arrays sorted alphabetically, and write to `/home/user/audit_report.json`. Install `passlib` only if you actually need it beyond stdlib `crypt`.