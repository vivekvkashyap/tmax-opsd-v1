Work each of the four issues with the right forensic tool rather than guessing:

1. Secret recovery: a deleted file's old content still lives in git history. Run `git log --diff-filter=D --summary` (or `git log --all --full-history -- token.key`) to find the commit that deleted `token.key`, then `git show <commit>^:token.key` to print its exact contents from the parent commit, and write that verbatim into `/home/user/geo_api/token.key`.

2. Precision regression: use `git log -p -- processor.py` (or `git bisect` against a quick precision check) to find the commit that changed how coordinates are transformed before being returned. The fix is to stop truncating/rounding the coordinate values so they pass through with full float precision.

3. Vendored recursion bug: read `/app/vendored/geomath/core.py`'s recursive function carefully — infinite recursion in a recursive function almost always means a missing or wrong base case. Add the terminating condition so recursion stops at the smallest input instead of recursing forever.

4. After fixing all three, `cd /home/user/geo_api && python server.py` in the foreground so it binds 127.0.0.1:8080 and stays running as your final action — don't background it.
