Split this into two independent deliverables and test each against the provided fixtures before considering it done. For the video: use `ffmpeg -i /app/experiment_001.mp4 frame_%03d.png` (or a Python video-reading library) to pull all 50 frames, then for each frame and each pixel (y,x) with y!=x, compute the average of the RGB channels and record an edge (frame, y, x) only if that average is strictly greater than 128 — write these to `/home/user/video_graph.csv` with header `frame,source,target` in that exact column order. For the filter script at `/home/user/filter_datasets.py <input_dir> <output_dir>`: for each CSV, first validate the header is exactly frame,source,target; then for every possible window start w (based on distinct/ranged frame values present, not assuming contiguity), sum incoming+outgoing edge counts per node over frames in [w, w+5) and reject the file the moment any node's count exceeds 7 — remember gaps in frame numbers are allowed and the window boundary is on frame value not row count. Copy only files that pass every window/every node. Validate your logic by running the script against `/app/corpus/clean/` (expect 100% copied) and `/app/corpus/evil/` (expect 0% copied) before finishing.