Work stage by stage. First, OCR the blueprint: run `tesseract /app/blueprint.png stdout` (try preprocessing with e.g. ImageMagick if OCR is noisy) and carefully read off the `API_TOKEN=` and `PORT=` lines from the output — use exactly what OCR recovers, don't guess or hardcode values you haven't actually extracted. Second, for legacy migration, write a small script (Rust or a shell one-liner) that loops over `/app/legacy_configs/*.gz`, decompresses each with gzip/flate2 to read the JSON, and re-copies (not just renames) the original gzip bytes to `/home/user/active_configs/<service_name>_v<version>.json.gz` so the result stays valid gzip. Third, scaffold the Rust daemon with `cargo new config_daemon`, add an HTTP framework (axum/actix-web) plus a gzip crate (flate2) and a file-locking crate (fs3 or fd-lock); implement `POST /upload` to check the `Authorization: Bearer <token>` header first (401 if wrong/missing), then decompress the body, parse JSON for `service_name`/`version`, write the gzip stream to the active_configs path, and — critically — open `system_changelog.log`, take an exclusive OS lock, append the `UPDATED <service> TO v<version>` line, then release, so concurrent requests never interleave. Build with `cargo build --release` and run the binary in the background on the OCR'd port, verifying with `curl` that unauthenticated/misauthenticated requests get 401 and correct ones get 200.
