Compile the given C source with `gcc /app/src/calc_det.c -o /home/user/bin/calc_det` after `mkdir -p /home/user/bin`. For the threshold, run OCR on the scanned note: `tesseract /app/lab_notes_scan.png -` (or stdout) and read the recognized text to find the stated determinant threshold — do not guess a number, extract it from the OCR output, and sanity-check the OCR result since scanned text can misread characters (e.g. confirm it parses as a plausible small decimal). In `/home/user/filter_graph.sh`, take `$1` as the matrix file, run `calc_det "$1"` to get the determinant, take its absolute value (e.g. via `awk 'function abs(x){return x<0?-x:x} {print abs($1)}'` or `bc`), and compare against the OCR'd threshold using floating point comparison (`awk` or `bc -l`, not bash integer `[ ]` which can't do decimals) — if `abs(det) < threshold` exit 1, else exit 0, matching "strictly less than" precisely since the corpus determinants may sit close to the boundary. Make the script executable with `chmod +x`, and test it manually against a couple of matrices you construct with known near-zero and healthy determinants before relying on the hidden adversarial corpus.