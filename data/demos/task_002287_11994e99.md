Build the C++ pipeline stage by stage and validate each against the visible `/app/sample_corpus/{clean,evil}` files before worrying about the hidden corpus: (1) given a WAV path, derive its JSON sidecar path by swapping the extension and load `gain_factor`/`offset` with nlohmann-json; (2) decode 16-bit PCM samples with libsndfile and normalize to [-1,1] by dividing by 32768.0; (3) apply `adjusted = x * gain_factor + offset` per sample; (4) chunk the adjusted signal into non-overlapping 160-sample windows and average `|adjusted|` per window to get your envelope — this must align sample-rate-wise with the reference (10ms windows at 16kHz = 160 samples, matching the reference's 100Hz sampling); (5) load `/app/reference_sweep.csv` as a flat float vector, truncate both sequences to the shorter length, and compute the standard Pearson correlation coefficient (covariance over product of std-devs, guarding against zero-variance divide-by-zero); (6) print "EVIL"/exit 1 if correlation >= 0.80, else "CLEAN"/exit 0. Write `build.sh` to `apt-get install` libsndfile1-dev and nlohmann-json3-dev then compile with the right `-l` flags; test manually with `echo $?` after each run against every sample file before assuming correctness generalizes to the hidden corpus.
