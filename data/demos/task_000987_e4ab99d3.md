Transcribe `/app/incident_alert.wav` (using a speech-to-text tool) and carefully parse the spoken-out-loud digit sequence into the actual epoch integer threshold — listen for how the digits are grouped/spoken and reconstruct the number precisely, since getting even one digit wrong will fail every downstream check. Write `/home/user/etl_filter.py` to load the JSON payload from the given file path, validate its structure (required fields present, correct types) and its `timestamp` field against that threshold: print `ACCEPT` only if the payload is well-formed AND `timestamp >= threshold`, otherwise print `REJECT` for any malformed JSON, missing fields, or stale timestamps — wrap the JSON parsing in a try/except so malformed files fail closed to `REJECT` rather than crashing. For `graph_query.py`, inspect `/app/warehouse.db`'s schema first (`sqlite3` `.schema` or `PRAGMA table_info`), then write a parameterized SQL query (`WHERE timestamp >= ?`) filtering transactions by the same threshold, group by target user to count valid incoming edges (in-degree), and select the top 3 by count descending; write those three user_ids comma-separated on one line to `/home/user/top_users.txt`. Test both scripts against a few hand-crafted valid/stale/corrupted JSON payloads before finishing.