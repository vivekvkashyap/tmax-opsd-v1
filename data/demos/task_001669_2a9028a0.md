Work in order: first run `tesseract /app/topology_diagram.png stdout` (or similar OCR invocation) to recover the SUBNET CIDR, RESTRICTED_PORT, and QUOTA_MB values — do not guess these, read them off the OCR output carefully since OCR can mangle digits/punctuation. Then write `/home/user/classifier.py` using Python's `ipaddress` module (`ipaddress.ip_address(ip) in ipaddress.ip_network(subnet)`) for the subnet check, exact integer comparison for the port, and `<=` for the quota — get the boundary conditions exactly right (strict inequality vs inclusive) since the corpora likely include edge cases at the boundary. Validate against BOTH `/app/corpus/clean/` (must all exit 0) and `/app/corpus/evil/` (must all exit 1) before considering it done — a naive regex-based IP/subnet check will fail on edge CIDRs. For the systemd piece, create `~/.config/systemd/user/log-analyzer.service` with the exact `ExecStart` line specified, then run `systemctl --user daemon-reload && systemctl --user enable log-analyzer.service` and confirm with `systemctl --user is-enabled`. Also create the config JSON and symlink exactly as specified, using `ln -s` with an absolute target path so it resolves correctly.