Split this into config-fix and classifier-writing, and validate each independently before wiring them together. For `config.yaml`, just correct the two intentionally-wrong values so `ingress` targets the actual Redis port and `metrics_url` points at the actual metrics service's `/report` route — read `start_services.sh` to confirm which keys each service actually consumes before editing, so you match the exact key names/format the services expect. For `classifier.py`, implement it as: load the JSON array of floats, fit `scipy.stats.gaussian_kde` on the data, numerically integrate the resulting PDF callable over [-2, 2] with `scipy.integrate.quad`, and exit 1 if the integral exceeds 0.05 (anomaly/reject) else exit 0 (normal/accept) — the threshold and integration bounds are given directly in the spec, don't need to be discovered. Validate your classifier against every file in `/app/corpus/clean/` (should all exit 0) and `/app/corpus/evil/` (should all exit 1) via a simple shell loop checking `$?`, and only consider the config/classifier correct once you've run `start_services.sh` and confirmed an end-to-end POST to `localhost:8080/data` results in a report reaching the metrics service on port 9090.