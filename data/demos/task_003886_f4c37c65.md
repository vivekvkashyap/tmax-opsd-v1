Start by reading `query_processor.cpp` closely: look for any place where `std::string::find` result (which returns `std::string::npos` when not found) is used directly in arithmetic like `substr(pos + 1)` without checking for `npos` first — that's the classic pattern causing `std::out_of_range`. Confirm the crash by compiling the original and running it with an `&`-separated token that lacks an `=` (e.g. just a bare word among the tokens), then save that exact string to `/home/user/crashing_input.txt`. Fix the bug by checking whether the delimiter search returned `npos` for each token and, if so, treating the whole token as the key with an empty value, instead of blindly slicing past the missing delimiter. Recompile with `g++ -O2 -std=c++17 -o /home/user/query_processor query_processor.cpp`, and re-run against the saved crashing input to confirm no exception and correct `K:...V:...` output. Write `/home/user/regression_test.sh` to invoke the binary with the crashing input as `argv[1]`, checking `$?` and exiting 0/1 accordingly, then `chmod +x` it. Finally redirect a run of the fixed binary on the crashing input to `/home/user/diagnostics.log`.
