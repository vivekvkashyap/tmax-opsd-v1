Tackle the three deliverables independently and verify each in isolation before moving on. (1) Docker Compose: open `docker-compose.yml`, find where `log_aggregator` and `db` sit on different networks (e.g. `front_tier`/`back_tier`), and instead put both services under one shared network named `capacity_net` (define it in the top-level `networks:` block as a bridge network, and list it under each service's `networks:` key) — don't just rename one network, both services must actually share it. (2) For the interactive backup, don't try to guess flags: inspect how `/app/interactive_backup.sh` prompts (run it manually first), then drive it with `pexpect.spawn(...)` sending exactly `y`, `metrics_db`, `500` at each `expect()` for the three prompts in order, wait for EOF/exit, then `shutil.move` the resulting `backup.tar.gz` from the CWD to `/home/user/backups/latest.tar.gz`. (3) For sanitize.py, don't hand-write validation rules blind — run the black-box oracle `/app/capacity_monitor` against sample files in `/app/corpora/clean/` and `/app/corpora/evil/`, observe which inputs crash/error/misbehave vs succeed, infer the exact evil conditions from that behavioral difference, then encode those checks (parsing JSON, range/format validation) in your script, exiting 1 for evil and 0 for clean.