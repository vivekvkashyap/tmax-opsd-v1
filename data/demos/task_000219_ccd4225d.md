Work bottom-up through the dependency chain, verifying each layer before adding the next. First inspect `/home/user/backend.sh` to find the actual port it listens on, then edit `/home/user/nginx.conf`'s `proxy_pass` to match that port (not the one currently configured) and start it with `nginx -c /home/user/nginx.conf -p /home/user/`. Next write `/home/user/start_backend.exp`: `spawn /home/user/backend.sh`, `expect "Enter passphrase to unlock server: "`, `send "admin_secret_99\r"`, then `interact` (or `expect eof` if it should just run) so the process stays attached rather than exiting immediately after sending the password. Since the backend crashes after handling a couple of requests, write `/home/user/supervisor.sh` as a `while true; do /home/user/start_backend.exp; done` loop (chmod +x) and launch it detached with `nohup ... & disown` so it survives your shell session and keeps restarting the backend forever. Then start `socat TCP-LISTEN:9000,fork,reuseaddr TCP:127.0.0.1:8080 &` in the background. Only after confirming (e.g. via `curl` once manually) that the whole chain responds, run your 5 sequential `curl http://127.0.0.1:9000` calls, appending each raw response to `/home/user/verification.log`.