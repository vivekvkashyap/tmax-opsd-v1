First inspect `/home/user/legacy_analyzer.py` to confirm the exact dark-frame definition (mean of all pixel channels < 100.0) so your fast version produces an identical integer count — correctness must match exactly, only speed differs. Build `/home/user/fast_analyzer.py` by parallelizing frame decoding/analysis: e.g. use `ffmpeg` to extract frames (or seek via multiple ffmpeg subprocess calls covering disjoint time ranges) in parallel worker processes via `concurrent.futures.ProcessPoolExecutor`, each worker computing partial dark-frame counts over its chunk, then sum results — avoid Python-level per-frame overhead from a single-threaded `cv2.VideoCapture` loop. Print only the final integer to stdout. Time both scripts yourself (e.g. with `time python3 script.py`) to confirm you comfortably clear 3x before considering it done. For the SSH tunnel, set up local SSH access (ensure an SSH server and your own key/authorized_keys work locally) and run `ssh -f -N -L 9999:localhost:8080 <user>@localhost` in the background. Write `/home/user/deploy.sh` to check if the tunnel port is already listening (e.g. via `pgrep -f` or checking `/proc/net/tcp` or `ss`) before starting a new one, and to add a `crontab` entry only if an identical line isn't already present (check with `crontab -l | grep -F` before appending) so repeated runs stay idempotent.