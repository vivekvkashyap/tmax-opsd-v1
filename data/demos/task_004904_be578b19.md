Since correctness is judged by byte-exact fuzzing against the oracle, prioritize black-box characterization over guessing internals: feed `/app/obfuscator_oracle` a wide range of crafted stdin inputs (empty, plain text, strings containing `<`, `>`, `TOKEN=` at various positions/lengths including near-EOF, binary/non-printable bytes, multiple occurrences) and diff the outputs to infer the transformation rules precisely, rather than relying solely on static disassembly (`objdump -d`, `strings`, `ltrace`) which is harder on a stripped/UPX-packed binary — `upx -d` a copy first if you want to disassemble, but dynamic testing is the reliable ground truth here. Pay special attention to edge cases that naive implementations get wrong: what happens when `TOKEN=` appears with fewer than 8 characters remaining before EOF (does it mask a truncated run or skip masking?), whether masking and HTML-escaping interact (order matters — determine via testing whether escaping runs before or after masking, and whether masked `*` characters could ever coincide with `<`/`>`), and whether occurrences of `TOKEN=` are searched non-overlapping or can re-trigger inside a previous match's replaced region. Write `/home/user/replica.cpp` reading all of stdin into a buffer (not line-by-line, since input may contain embedded newlines/binary data), apply the inferred transformation, and output via a format you've confirmed byte-for-byte against several dozen manual test cases before considering it correct. Compile exactly as specified and re-verify against the oracle on fresh random inputs before finishing.