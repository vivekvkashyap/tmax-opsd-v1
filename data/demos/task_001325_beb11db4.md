Don't try to reverse-engineer the crash conditions purely by reading disassembly — cross-reference the provided clean/evil sample directories first (diff the JSON structures between `/app/data/clean_samples/` and `/app/data/evil_samples/` to spot the structural pattern that differs), then use the binary as a black-box oracle by feeding it variations (toggling one field at a time, e.g. `maxDepth` values, presence/absence of nested stages) and observing hang/crash vs normal exit, confirming with `strings`/`objdump` only to sanity-check field names referenced in the binary. Once you've pinned down the two independent unsafe patterns — a `$graphLookup` stage with `maxDepth` missing or exceeding a small bound, and a `$lookup` stage whose nested `pipeline` array contains another `$lookup` (recursive Cartesian join) — implement a Rust JSON-pipeline walker (using `serde_json`) that recursively scans every stage and every nested `pipeline` array for these two patterns, printing "EVIL" and exiting 1 on first match, or "SAFE"/exit 0 if the whole structure is clean. Set up the project with `cargo new`, build in release mode so the binary lands at `target/release/query_sanitizer`, and validate against both sample sets before trusting your rules on the hidden corpora.