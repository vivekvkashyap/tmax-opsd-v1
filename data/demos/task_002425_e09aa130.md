Three linked fixes, in order. First, fix the vendored `libtcp` build: inspect its Makefile for a bogus compiler name (a `CC` variable pointing at a nonexistent gcc variant) and correct it, then confirm `libtcp.a` gets produced. Second, fix the data-leakage bug in `train.c`: the loop computing normalization `min`/`max` iterates over the full dataset — restrict it to only the first 80 rows (the training split), then compile and rerun to regenerate `model.weights` with the corrected min/max/w/b. Third, write `server.c` against the `libtcp` API (`start_server(port, handler)`, handler writes a null-terminated response into the given buffer): load `model.weights` at startup, and in the handler parse the incoming float, apply `(X - min) / (max - min)` scaling then `w * X_scaled + b`, formatting the reply as `%.4f\n`. Compile linking `libtcp.a`, start it in the background bound to 127.0.0.1:9000, and test with `nc` sending a sample float to confirm the reply format looks sane before leaving it running.