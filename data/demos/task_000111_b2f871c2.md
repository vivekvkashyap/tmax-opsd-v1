Chain three independent recoveries and confirm each before moving to the next rather than assuming any single step is right. First, crack the `admin_sys` hash: extract the `$5$salt$hash` field from `/home/user/shadow_leak.txt`, then for each candidate in `/home/user/wordlist.txt` compute the SHA-256-crypt with that exact salt (e.g. Python's `crypt.crypt(candidate, salt_string)` or `openssl passwd -5 -salt <salt> <candidate>`) and compare against the stored hash until you find the match — don't just assume a "likely" password, verify it by actually recomputing the hash. Second, scan `/home/user/auth_events.json` line by line (each line is a separate JSON object) for a request whose `Authorization` header is `Basic <base64>`; base64-decode each candidate and check whether it decodes to `admin_sys:<the cracked password>` and whose response status indicates success — that's the correct event, not just the first admin_sys attempt you see (there may be failed attempts too). Third, from that same successful event pull the `ip_address` field and parse the `session_id` value out of the `Set-Cookie` header in `response_headers` (split on `=` and `;`). Write all three recovered values into `/home/user/investigation_report.txt` in exactly the specified three-line format.
