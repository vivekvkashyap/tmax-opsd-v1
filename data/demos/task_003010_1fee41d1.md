Split this into a one-time historical-analysis phase and a stateless streaming script, since the fuzz verifier only exercises the latter. First run the exact extraction command given to get 1-fps frames from `/app/reaction.mp4`, then for each frame open it with PIL, take the red channel, sum pixel values over the exact 10x10 region `x:100-109, y:100-109` (inclusive both ends, 100 pixels total), and classify active if sum > 12000 — join those booleans to `/app/conditions.csv` by second (frame_0001↔second 0). Compute the two regimes' posterior (alpha,beta) by literally counting: start both at (1,1), then for each historical (temp,active) pair add 1 to alpha if active else beta, routing into the High (temp>=20.0) or Low bucket — hardcode these two resulting integer pairs into `update_model.py` since they're derived from data you computed once, not something the script recomputes per call. The script itself must: read one JSON line from stdin, pick the regime by the input `temp` threshold, start from that regime's fixed posterior as prior, then add 1 per obs==1 to alpha / per obs==0 to beta, and print compact JSON `{"alpha": a, "beta": b}` with integer values — no extra whitespace, since fuzzing does exact string/structural comparison against an oracle.
