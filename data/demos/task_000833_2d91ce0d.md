Start by OCR'ing the spec image: run `tesseract /app/specs/validation_rules.png stdout` (try `--psm 6` if output is garbled, and cross-check against a couple of files in `/app/corpus/clean/` and `/app/corpus/evil/` to sanity-check the thresholds you read — e.g. confirm every clean file satisfies the numeric boundary you extracted and every evil file violates it). Note whether bounds are inclusive/exclusive exactly as OCR'd, since off-by-one boundary handling is the most common way a naive script fails against the hidden suite. Write `/home/user/filter_data.py` using Python's `csv` module, coercing `temperature`/`pressure` to float inside a try/except so blank or non-numeric cells raise and are caught (exit 1) rather than silently becoming NaN and passing a `<=`/`>` comparison. Check every row, not just the first — a single violating row anywhere in the file should reject the whole file. Test against both corpus directories in a loop, verifying exit codes with `echo $?`, before considering the task done.