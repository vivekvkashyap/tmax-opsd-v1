Start by black-box probing `/app/legacy_api` rather than guessing its port/path: run it under the required environment (`TZ=UTC LC_ALL=C /app/legacy_api &`, since a stripped binary may silently fail or exit without those set) and then use `ss -tlnp`/`lsof -i` to find which local port it bound, or `strace -f -e trace=network` while starting it to observe `bind`/`listen` calls; once you know the port, use `strings` on the binary for likely route names and confirm with `curl` against candidate paths (e.g. `/token`, `/secret`, `/status`) until you get the token response. Only after confirming the real port+path should you write the Go proxy: an HTTP handler on `:8080` for `GET /legacy-status` that does an internal HTTP GET to the legacy binary's endpoint and relays the body verbatim with 200, plus a separate TCP listener on `:8081` that writes exactly `HEALTHY\n` and closes on each connection — run these concurrently via goroutines. For `supervise.sh`, launch both processes in a loop/subshell pattern that respawns them within 2 seconds on exit (e.g. `while true; do prog; sleep 2; done &` per process), redirect both stdout/stderr with `>>` into `/home/user/logs/app.log` (after `mkdir -p`), and run a separate background loop polling the log file size every 3 seconds with `stat -c%s`, truncating it (`> file` or `truncate -s 0`) once it exceeds 500KB. Actually run `supervise.sh` and leave it running.