Work in dependency order: reorganize files into src/, src/rust_ext/, tests/ first so subsequent build paths are stable, then fix Rust, then wire up the proxy/mock, then the Python driver, then the CI script. For the Rust borrow-checker bug in `calculate_brightness`, the fix is to stop holding a reference (`&frame_data`) while separately consuming/moving `frame_data` in a `for val in frame_data` loop — either iterate `&frame_data` (borrow only, computing `.len()` from the same borrow) or fully consume the owned vector and capture its length before the loop; don't fight the borrow checker with clones unless needed. Build with `maturin develop` or `cargo build --release` plus a `.so`/`pyo3` bridge so `import fast_video_utils` works from the Python venv used by `process_video.py`. For speed, avoid frame-by-frame Python overhead: extract frames once via ffmpeg/cv2, pass raw byte buffers straight into the Rust function, and keep the mock API/proxy as lightweight synchronous HTTP servers (`http.server` is fine) so they don't become the bottleneck. Test the mock API and proxy independently with curl before wiring them into `run_ci.sh`, and make sure `run_ci.sh` waits for both background servers to be listening before running `process_video.py`, then kills them cleanly on exit.
