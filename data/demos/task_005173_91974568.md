Treat this as three separable subtasks: recover the thresholds, fix the encoding, then run correct parallel filtering. First transcribe `/app/thresholds.wav` (install/use `whisper` or another CLI ASR tool, or ffmpeg to inspect duration/format if the tool needs a specific input) and read off the three baseline numbers spoken for CPU, MEM, and DISK — don't assume typical values, actually listen to/transcribe the audio and extract the numbers precisely. Convert the CSV from UTF-16LE to UTF-8 first (e.g. `iconv -f UTF-16LE -t UTF-8` or piping through `recode`) before any further processing, since parsing UTF-16LE bytes as UTF-8 will corrupt column boundaries. Then process the anomaly rule per component: a row is anomalous if `value > baseline[component] * 1.5`, where each component (CPU/MEM/DISK) uses its own recovered baseline. For "parallel processing" with awk/split/xargs, split the file into chunks and process concurrently (e.g. `split` + `xargs -P`), then concatenate results. Write only `timestamp,sensor_id` for anomalies, no header assumed by the grader but matching the expected format exactly, and sanity-check your output count against a manual `awk` pass before finishing since the grading is F1-based, meaning both false positives and false negatives hurt.