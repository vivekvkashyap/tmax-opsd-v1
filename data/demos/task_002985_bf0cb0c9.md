Build the pipeline stage by stage, checking shapes/values at each step rather than writing it all at once. Load both files (`sensors.csv` via pandas, `maintenance.json` via `json`/`pd.read_json`) and merge on `machine_id` with an inner or left join matching the row counts you expect. For missing `temperature` values, seed `numpy.random.seed(42)` (or use a dedicated `np.random.default_rng(42)`) once, then for each missing entry draw with replacement from the array of non-null temperature values using `np.random.choice(non_null_values, size=n_missing, replace=True)` in one call — doing it as one batched call rather than looping keeps it reproducible and matches a fixed-seed sequence deterministically. Save the merged/imputed frame to `/home/user/results/cleaned_data.csv`. Train `sklearn.ensemble.RandomForestClassifier(random_state=42)` with default hyperparameters on features `temperature`/`vibration` against target `needs_repair`, then score accuracy with `model.score` (or `predict` + compare) on the same training data used to fit it — no held-out split is specified. For benchmarking, time 100 successive `model.predict(X)` calls with `time.perf_counter()`, average in milliseconds. Write the three required keys to `/home/user/results/summary.json`, making sure `avg_inference_ms` serializes as a JSON float (not numpy float type, which `json.dump` will reject) by casting with `float(...)`.