Work top-down through the four deliverables, verifying each before moving to the next. First fix the symlinks: `ls -la /home/user/app` to confirm `conf` and `logs` point at forbidden system paths, `rm` each broken/dangerous symlink, `mkdir -p` the new real target directories (`internal_conf`, `local_logs`), then recreate the symlinks pointing at those new targets with `ln -s`. Second, write `backends.json` through the `conf` symlink (writes land in `internal_conf`) with the exact JSON array given — use a heredoc or echo to avoid introducing extra whitespace/formatting that could break an exact-match grep. Third, write `ci_cd_verify.sh` with `set -e` at the top, but structure the checks so a failure doesn't just abort silently — since `set -e` exits on the first failing command, wrap each check so you can still write `PIPELINE_FAILED` on failure (e.g. use `if ! cmd; then ...; exit 1; fi` blocks, or trap on ERR) rather than letting `set -e` kill the script before it writes status; verify directories with `[ -d ... ]` after resolving symlinks (not `-L`, since you want to confirm they resolve, not that they're symlinks) and confirm `backends.json` is readable with `[ -r ... ]`. Finally write `proxy.js` using Node's built-in `http` module, read/parse `backends.json` at startup (even if unused beyond loading), listen on 127.0.0.1:8080, respond 200 `PROXY_ACTIVE` to any request, and write `process.pid` to `logs/proxy.pid` once the server's `listen` callback fires. Make both scripts executable with `chmod +x`.