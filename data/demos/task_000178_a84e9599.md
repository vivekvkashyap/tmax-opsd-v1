Fix each defect independently before wiring the pipeline. For the Go circular import between engine and config: move the shared `Rule` type definition so it lives only in `config`, and remove config's import of `engine` entirely (drop or refactor whatever artificial call it makes into engine) so dependencies flow one-way, config -> nothing, engine -> config. For the C buffer overflow in `extract_payload`: stop copying into a fixed 16-byte buffer with `strcpy`; instead `malloc(strlen(input) + 1)` and copy into that exact-sized buffer (memcpy/strcpy is now safe since it fits), which is verifiable by grepping for `strlen`. Then write /home/user/run_ci.sh with `set -e` and, in order: apply v1_init.sql then v2_update.sql to a fresh sqlite3 database at waf.db; compile parser.c into libparser.so with both `-fsanitize=address` and `-fPIC`; build the Go binary from go_src into waf_binary, passing CGO_CFLAGS/CGO_LDFLAGS so the compiler and linker find parser.h and libparser.so; finally run waf_binary with the SQL-injection-like argument, setting LD_LIBRARY_PATH so it resolves libparser.so at runtime, and redirect its stdout to test_report.txt. Make the script executable.