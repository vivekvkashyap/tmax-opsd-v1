First get `datamash` building: run `./configure && make` inside `/app/datamash-1.8`, read the failure output, and inspect `Makefile.in` (or the generated `Makefile`) for an injected bad line — e.g. a spurious `CC = /bin/false` or corrupted compiler rule — remove/correct it, re-`configure`/`make`, then `make install` (or point PATH at the built binary) so `datamash` is callable afterward; confirm with `datamash --version`. Second, build `/home/user/eval_tracker.sh` as an awk-driven pipeline: read the CSV with `awk -F,`, skip any row whose 5th field (`prediction`) is empty, accumulate per-`experiment_id` running sums of `inference_time_ms` and counts, and a correct-match counter comparing fields 4 and 5, then in the `END` block print `experiment_id,avg,accuracy` with exactly 4 decimal places (`printf "%.4f"`), piping the whole thing through `sort -t, -k1,1` for alphabetical ordering. You may lean on `datamash` for aggregation instead of raw awk math if you prefer, but the output format and rounding must match exactly. Test edge cases (all-empty predictions, single experiment, ties) since a fuzzer checks bit-exact equivalence against many random CSVs.
