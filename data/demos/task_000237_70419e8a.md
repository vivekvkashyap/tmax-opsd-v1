Work the C build first, then the server, then the benchmark. Read the Makefile carefully: a "circular dependency" error usually means a target lists a prerequisite that isn't actually needed to build it (e.g. one .o file wrongly depends on another .o file instead of just the shared header). Fix the dependency line so each .c/.o rule only depends on the headers/sources it actually includes, then run `make` and confirm `libfastparser.so` is produced. For the server, use `http.server`'s BaseHTTPRequestHandler (no Flask/FastAPI), check the Authorization header exactly, parse query params with `urllib.parse`, and implement semantic version comparison by splitting on '.' and comparing three integers numerically (not lexicographically) — reject anything non-numeric or with wrong arity as malformed (403). Load the .so via `ctypes.CDLL`, allocate a mutable output buffer (`ctypes.create_string_buffer(256)`), call `clean_path`, and decode the result. Start the server as a background process before writing bench.py, and have bench.py time 500 sequential real HTTP requests using `urllib.request` or `http.client`, writing the elapsed seconds as a bare float to the results file.
