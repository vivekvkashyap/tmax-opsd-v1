Structure `pipeline.py` around a single `process_file(path)` function that does the full per-file transform, then drive it with `concurrent.futures.ProcessPoolExecutor().map(process_file, glob_of_csvs)` — this sidesteps GIL/multiprocessing logging pitfalls versus trying to share state across processes. Inside each worker: read the CSV with pandas; drop `patient_name`; add `patient_id = hashlib.sha256(ssn.encode()).hexdigest()` and drop `ssn`; `pd.melt` the `HR_*` columns into `time_of_day`/`heart_rate` (strip the `HR_` prefix from column names, e.g. `col.replace('HR_','')`), then `dropna(subset=['heart_rate'])`. Build the UTC timestamp by combining `record_date` + `time_of_day`, localizing to each row's `tz` with pandas/pytz tz-aware parsing, then `.dt.tz_convert('UTC')`, formatted as ISO8601 ending in `Z` — watch DST offsets, don't assume a fixed UTC delta. Drop the now-redundant date/tz/time_of_day columns and write Parquet via `pyarrow`. For logging, configure `logging.basicConfig(filename='/home/user/pipeline.log', level=logging.INFO)` in the main process and have each worker return its row count so the main process logs the exact required message per file — avoids duplicate/interleaved handlers across processes.
