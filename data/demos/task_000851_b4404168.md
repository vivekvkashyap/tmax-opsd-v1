Fix the C bug at its root cause rather than patching symptoms: replace the naive `strcmp`-based check_version with logic that parses each version string into three integers (major, minor, patch) — e.g. `sscanf(version, "%d.%d.%d", &maj, &min, &pat)` for both the incoming version and "2.2.0" — then compares them numerically in order (major first, then minor, then patch), returning true only if the parsed version is >= the minimum lexicographically-by-magnitude, not by string ordering. This is exactly what makes "2.10.0" correctly beat "2.2.0" once minor is compared as an integer (10 > 2) instead of by character. Recompile with the exact gcc command given. For the Nginx config, use a location block with a regex capture on the URL path (e.g. `location ~ ^/api/(?<version>[^/]+)/(?<endpoint>.+)$`) to extract the version segment, `proxy_pass http://127.0.0.1:9090/$endpoint;`, and `proxy_set_header X-API-Version $version;` so the backend receives it in the header it already expects. Run nginx without root by pointing pid/log directives at /tmp/, start the backend, run test.sh, and check results.log matches the four expected status codes (200/426/200/200) before considering it done.
