Build the proxy as a plain Python `socketserver`/`http.server`-based script rather than relying on any heavy framework, since it must run standalone and forward raw HTTP. Structure it as: (1) a WAF check function that lower-cases the full request path+query and does substring matching against the banned patterns before any network call — reject with a hand-written `403 Forbidden` response and stop, never touching the backend; (2) a round-robin index (module-level counter, alternating 8081/8082) with a `try/except` around the upstream connection (socket errors, timeouts, `ConnectionRefusedError`) that on failure immediately retries the *other* backend once before giving up; (3) for the timestamp header, use `datetime.now(ZoneInfo("Europe/Berlin"))` (Python's zoneinfo, not manual UTC offset math) formatted with `strftime("%Y-%m-%d %H:%M:%S")`, injected into the outgoing request headers before forwarding. Launch it with `nohup python3 /home/user/proxy.py &`, capture `$!` into `/home/user/proxy.pid`, then verify with a quick `curl` against a clean and an evil URI before finishing — don't just trust that the process started.
