Parse the CSV manually (split on commas, handle the header row separately) and apply the filters in the exact order given: keep only `model_name == "BetaNet"`, then drop rows where accuracy/loss is empty or literally "NaN" (string compare, not float parse, since parsing "NaN" as a float silently succeeds as NaN and would defeat the filter), then drop rows outside the accuracy/loss numeric bounds — implement this as a straightforward sequential check per row so the valid-row order matches file order. Store valid accuracies in a `std::vector<double>` preserving insertion order, since the bootstrap indexes into this vector by position. Match the RNG spec exactly: `std::mt19937 gen(42);` and `std::uniform_int_distribution<size_t> dist(0, N-1)` reused across all 10000 iterations (do not reseed or recreate the distribution per sample) — for each of the 10000 samples draw N indices, average them, then sort all 10000 means ascending and index positions 250/9750 for the CI bounds. Format all reported numbers to 4 decimal places exactly as specified before writing `/home/user/report.txt`.