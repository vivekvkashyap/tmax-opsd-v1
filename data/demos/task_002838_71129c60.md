Build this as a straightforward Go ETL: parse the CSV with `encoding/csv`, keep events grouped in a `map[int][]Event` keyed by `timestamp_sec`. First pass: compute counts per second. Second pass: walk seconds in sorted order and flag an "anomaly second" only when count > 5 AND count >= 2 * count(T-1) (missing T-1 counts as 0) — implement this as an explicit lookup into the count map, not just comparing to the previous row in file order, since seconds can be missing or unsorted in the source file. Collect all events belonging to flagged seconds, then group by language. For ranking within each language, count Unicode code points with `[]rune(content)` (never `len(content)`, which counts bytes and breaks on multi-byte scripts), sort descending by rune length with a stable secondary ascending lexicographic tiebreak on the raw string, and take the top 2. Finally sort the whole output slice by language ascending, then rune-length descending, then content ascending, and marshal to JSON matching the exact field order/keys shown in the spec. Compile with `go build` or `go run` and confirm the output file is written before finishing.