First inspect the reference WAV with Python's `wave` module (or parse the RIFF header manually) to read its sample rate, channel count, and bit depth — do not hardcode assumed values. Write the backup script using `os.walk` to recursively traverse the input directory, computing relative paths with `os.path.relpath`. For each file, wrap header parsing in a try/except: open with `wave.open`, catch `wave.Error`/`EOFError`/malformed-header exceptions, verify the RIFF/WAVE magic bytes are present, then compare `getframerate()`, `getnchannels()`, and `getsampwidth()` against the reference exactly — reject on any mismatch or read failure. A truncated data chunk should trigger a read error when you attempt to read all frames, so validate by actually reading the full data, not just the header. Build the `.mybak` file by opening in binary mode, writing the fixed 8-byte magic, then for each accepted file writing `struct.pack('<H', len(path_bytes))`, the UTF-8 path bytes, `struct.pack('<I', filesize)`, and `zlib.compress(raw_bytes)`. Log every file (accepted and rejected) to `filter.log` as you go, using the relative path exactly as embedded in the archive.