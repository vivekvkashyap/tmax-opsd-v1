For Part 1, use ffmpeg to extract per-frame data and detect the exact count of fully-black leading frames rather than guessing visually: e.g. `ffmpeg -i /app/dataset_recording.mp4 -vf "blackdetect=d=0:pix_th=0.00" -f null -` to get black-segment timing, or extract frames and check `max pixel value == 0` per frame, then convert the detected black duration to a frame count using the video's actual fps (`ffprobe` to confirm fps) — be precise about whether the count includes/excludes the transition frame, and verify by extracting frame N and N+1 around the boundary to confirm N is black and N+1 isn't. For Part 2, treat `/app/oracle_parser` purely as a black-box you probe, not something to copy: feed it varied, self-crafted lines (valid `ID:...|TS:...|DATA:...` lines, comment lines starting with `#`, empty lines, and deliberately malformed lines with missing pipes/tags/extra fields) at several offsets and diff outputs to reverse-engineer exact transformation rules (uppercase ID, `TS+offset`, reversed DATA, silent skip for `#`/empty, `INVALID_LINE` otherwise). Once your hypothesis reproduces the oracle on many self-generated probe cases, implement `/home/user/log_parser.py` reading `sys.argv[1]` as offset and streaming stdin to stdout line-by-line, then validate again against the oracle before finishing since it will be fuzzed with thousands of lines.