Work through the pipeline strictly in order, since later steps depend on earlier ones being correct. Set up the Go module first (`go mod init ml_task` in `/home/user/`, then `go get gonum.org/v1/gonum/stat` if you use it). Read both CSVs with `encoding/csv`, join on `id` using a map keyed by id built from one file and looked up while iterating the other. For cleaning, check every field of the merged row for the literal string `"NA"` or an empty string and drop the row if any match — do this before splitting, not after. Sort by numeric `id` (parse to int, don't string-sort). Compute the 80% split with integer division exactly as specified (`total*80/100`), and never let the split boundary see test-set data. The critical leakage trap: run Pearson correlation and pick the best feature using ONLY the train slice — computing correlation on the full/cleaned dataset before splitting is the classic bug this task is designed to catch. Fit slope/intercept via least squares on train only, then compute MSE by applying that model to the held-out test rows. Round all floats to 4 decimals and write the exact JSON key names given, then actually `go run` the program to produce `results.json`.