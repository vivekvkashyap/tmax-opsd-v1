Since the source is lost and you must match a stripped binary bit-for-bit, treat this as black-box reverse engineering: systematically feed `/app/legacy_transformer` varied CSV inputs via stdin (single row, multiple metric columns, boundary values like exactly 20 and 80, negative numbers, empty cells, non-numeric strings, multiple rows) and diff the outputs to pin down exact formatting — pay attention to literal strings, field ordering, capitalization, and whitespace/blank-line placement around `<<TEMPLATE_END>>`. Don't guess the severity thresholds or template text from the task description alone if the binary's actual behavior might differ slightly — always cross-check by running the oracle. Once you've mapped the exact block format (`[TRANSFER]`, `Dest:` line, `Payload:`, template markers, and the trailing blank line) and the three-way severity rule (parse-failure → UNKNOWN, <20 → LOW, 20–80 inclusive → NORMAL, >80 → CRITICAL), implement `/home/user/new_transformer.go` reading CSV from stdin with `encoding/csv`, iterating rows then columns left-to-right and emitting one block per (host, metric) pair for every data row. Compile with `go build -o /home/user/new_transformer new_transformer.go`, then re-run both your binary and the oracle side-by-side on several fresh test CSVs and `diff` the outputs to confirm exact equivalence before finishing.