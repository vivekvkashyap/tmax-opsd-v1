Build the pieces in dependency order and match output formatting exactly, since the verifier does an exact byte comparison of the alert file. First append `EDGE_NODE_ID=IOT-992` as an export line to `/home/user/.bashrc` and create `/home/user/iot_config.ini` with the two given key=value lines plus the `/home/user/mail_spool/outbox` directory. In `monitor.cpp`: read the PID from the argv[1] file, check liveness via `kill(pid, 0) == 0` (also treat a missing/unreadable PID file as "not running"), fall back to `UNKNOWN` for the env var only if `getenv` returns null, and parse the ini file with simple line splitting on `=`. When the process is dead, construct the alert text with an actual blank line between the Subject header and body (mind trailing/leading whitespace — match the exact three-line-plus-blank-line format given) and write it verbatim to `<SpoolDir>/alert.eml`. Print exactly `OK`/exit 0 or `ALERT GENERATED`/exit 1 per the spec. Compile with `g++ -std=c++11`, then `source ~/.bashrc` before running `/home/user/run_test.sh` so the env var is visible to the monitor process.
