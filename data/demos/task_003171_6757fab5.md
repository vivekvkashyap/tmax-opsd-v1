Diagnose the linking bug by reading `build.sh` and checking what `device.c` actually calls — since it uses `<math.h>` functions, the fix is adding `-lm` to the `gcc -shared -fPIC ...` command; confirm afterward with `ldd libdevice.so | grep libm` rather than assuming. For the proxy, write a minimal Python HTTP server (e.g. `http.server.BaseHTTPRequestHandler`) listening on localhost:8080 whose `do_POST` reads the incoming body via `Content-Length`, then issues its own outbound POST to `http://localhost:9090/` carrying that exact body plus an added `X-Proxy-Routed: 1` header (use `urllib.request` or `http.client`, not a wrapper that alters the payload), then returns 200 to the original caller before or after the forward completes. Sequence execution carefully: run `build.sh` first, then start `ci_server.py` and `proxy.py` in the background (`&`) and give them a moment to bind their ports, then run `emulator.py` in the foreground so it can report success/failure. Verify `ci_results.json` was written before considering the task done — if the proxy header or body is wrong, re-check the forwarding logic rather than the backend server.
