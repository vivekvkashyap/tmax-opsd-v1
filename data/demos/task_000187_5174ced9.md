Stream the JSONL file line-by-line (never `.readlines()` the whole thing into memory) — open both output files up front and process in a single pass. For each line, first attempt `json.loads`; wrap it in try/except and on failure write the raw original line (unmodified, including any missing trailing content) to `/home/user/invalid_records.jsonl`. If it parses, validate strictly: the key set must be exactly `{tx_id, amount, status}` (no extra, none missing — use `set(obj.keys()) == {...}`), `tx_id` must be an int, `amount` numeric and `>= 0`; any violation routes the original raw line to the invalid file. For valid records, maintain a `collections.deque(maxlen=50)` of amounts as your rolling window: before appending the current amount, if the deque is already full (i.e., you have 50 prior valid records), compute `mean(deque)` and check whether the current amount is strictly greater than `3.0 * mean` — if so, append `tx_id` to `/home/user/changepoints.txt`. Only after that check, append the current amount to the deque, so the changepoint record itself still gets folded into future averages. Preserve raw invalid lines byte-for-byte, including any lack of trailing newline in malformed JSON.
