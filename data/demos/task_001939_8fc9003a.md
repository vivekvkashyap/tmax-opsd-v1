Build this as a clean pipeline: extraction, then a single reusable Go computation, then two separate consumers (batch logging + HTTP serving) of that same computation. For extraction, use `ffmpeg -i /app/video.mp4 -vf fps=1 -pix_fmt gray /tmp/frames/frame_%04d.png` — check the actual frame count and dimensions produced rather than assuming, since APD depends on real pixel data. In Go, write one function that decodes two PNGs, sums `abs(pixel difference)` over all corresponding pixels, and divides by pixel count — reuse this for both the offline threshold sweep (writing one JSON line per threshold with `threshold` and `event_count` keys to `/home/user/experiments.jsonl`) and the live HTTP handler, so results are guaranteed consistent. For the server: parse `threshold` as a float from the query string, iterate frame pairs computing APD on demand (or from a cached precomputed array), and collect frame indices (starting at 2) where APD strictly exceeds the threshold. Enforce auth by checking the `X-API-Key` header equals the expected value before doing any work, returning 401 otherwise — test this explicitly with and without the header. Keep the server running in the background listening on 127.0.0.1:9090 so verification requests can reach it.