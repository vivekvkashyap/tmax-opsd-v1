Build this as a clean pipeline of independently-testable stages. First load both CSVs with pandas/numpy and compute pairwise Euclidean distances vectorized (e.g. via broadcasting or `scipy.spatial.distance.cdist`) rather than nested Python loops, to get the global adjacency matrix using the strict `< R` threshold. For each defect, select node indices with distance strictly `< D` from the defect's coordinates, slice the adjacency matrix to that index set to get the induced subgraph, and compute density as `2E/(V(V-1))`, handling the `V<2` edge case as 0. Collect all defect densities into an array. For the bootstrap CI: call `numpy.random.seed(42)` exactly once before the resampling loop begins (not inside each iteration), then do exactly 10,000 iterations of `np.random.choice(densities, size=len(densities), replace=True)`, taking the mean each time, and finally use `np.percentile` at 2.5 and 97.5 on the collected means. Round both bounds to 4 decimal places and write them to JSON with the exact keys specified. Double check whether "distance < D" should use the defect's own coordinates (not an index into nodes.csv).