Treat this as three sequential, independently checkable phases — don't skip the architecture-inference step. First, load `/home/user/weights.pth` with `torch.load` and inspect each tensor's `.shape` (e.g. `fc1.weight`, `fc2.weight`, `fc3.weight`) to deduce the true hidden layer sizes, then edit `model.py`'s `SimpleMLP.__init__` so the `nn.Linear` dimensions exactly match what you observed — mismatched shapes will fail to load via `load_state_dict`. Use the reconstructed model in eval mode to predict only the rows where `target` is NaN, filling those in-place; count exactly how many you filled for `imputed_nan_count`. Second, after imputation, identify all rows where `target < 0` as anomalies, and fit `GridSearchCV(Ridge(), {'alpha':[0.1,1.0,10.0]}, cv=5)` using ONLY the rows that are `target >= 0` post-imputation (excluding the anomalies you're about to fix) — training on contaminated rows would bias `best_alpha`. Use the tuned model's predictions to replace the anomalous rows in Phase 3, then write the cleaned CSV preserving original column order/index, and compute `summary.json` fields precisely from your actual counts and results (round `final_target_mean` to exactly 4 decimals) rather than guessing values.