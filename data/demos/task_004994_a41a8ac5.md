Work the phases in strict order and verify at each boundary before moving on. First, `sha256sum -c checksums.sha256` in the chunks directory to confirm integrity, then concatenate the parts in lexicographic order (`part-00`, `part-01`, ...) with `cat` into `merged.tar.gz` — do not trust filesystem listing order blindly, sort explicitly. For the Rust extractor, use `tar::Archive` over a `flate2::read::GzDecoder`, and for each entry inspect `entry.path()` (or `path_bytes()`/header path raw) before extracting: reject any component equal to `..` and reject any path starting with `/`. For symlink/hardlink entries, resolve the link target relative to where the entry would land inside the destination, canonicalize conceptually (join dest+entry_dir+linkname, normalize `..`), and reject if the resolved path escapes the destination root or the linkname itself is absolute — check this even though the entry name itself may be safe. Do NOT rely on the `tar` crate's own unpacking (`archive.unpack()`), since it may silently sanitize or behave differently than required; iterate entries manually with `entries()?` and handle extraction yourself so you can log skips in the exact `SKIPPED: <path>` format using the path exactly as stored in the archive header. Build with `cargo build --release`, create the destination dir, then run the binary.
