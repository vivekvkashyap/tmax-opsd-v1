Build the detector first, using the labeled training examples to confirm your logic before touching the video. Since corruption is defined as a pure-red (255,0,0) 10x10 block somewhere in the frame, write `detector.py` to load the image (e.g. with PIL/OpenCV/numpy), scan for any pixel exactly matching (255,0,0) — or more robustly, check for a contiguous block of such pixels to avoid false positives from stray red pixels in normal scenes — and print exactly `CLEAN` or `EVIL` with no other output; validate it against every file in both `/app/corpora/train/clean/` and `/app/corpora/train/evil/` before moving on, since the hidden test corpus will be scored strictly. Extract frames from `/app/dashcam_raw.mp4` at 1 fps with `ffmpeg -i /app/dashcam_raw.mp4 -vf fps=1 frame_%04d.png` (or via OpenCV `VideoCapture` sampling at the video's actual fps), keeping frames in order and 0-indexed. Run your detector on each extracted frame, write the 0-indexed clean frame numbers sorted ascending to `video_clean_frames.txt`, then build `clean_dataset.h5` by stacking only the clean frames' raw RGB arrays (not BGR — convert if you used OpenCV) into a single `(N,H,W,3)` uint8 dataset named `images`, preserving chronological order.
