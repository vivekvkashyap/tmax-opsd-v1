Work in stages and verify each before moving on. First compile the shared library: `gcc -shared -fPIC -O2 -o /home/user/src/libnaive.so /home/user/src/naive_solver.c`. In `workflow.py`, load the CSVs with `numpy.loadtxt` or `pandas` (no header), form `A = X.T @ X` and `b = X.T @ y`, flatten `A` row-major with `numpy.ascontiguousarray`, and call the C `solve_system` via `ctypes` using `ctypes.POINTER(ctypes.c_double)` argtypes and an output buffer of length n — this naive Gaussian elimination has no pivoting, so on the near-singular (highly collinear) data it will hit a near-zero pivot and blow up; expect and report a huge/garbage residual norm rather than trying to "fix" the C code (that instability is the point being demonstrated). Separately fit `sklearn.linear_model.Ridge(alpha=0.1, fit_intercept=False)` on the full data for the stable MSE. For the bootstrap CI, seed with `numpy.random.seed(42)` once before the loop, resample N row indices with replacement each of 1000 iterations, refit Ridge fresh each iteration on the resampled data, evaluate MSE on that same resampled set, collect all 1000 MSEs, then use `numpy.percentile` at 2.5 and 97.5. Dump all four required keys to `/home/user/results.json` as native Python floats (not numpy types, which aren't JSON-serializable).