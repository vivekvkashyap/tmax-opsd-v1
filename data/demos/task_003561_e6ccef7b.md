Implement this as a straightforward simulate-then-analyze pipeline, being careful about indexing at the parabolic-interpolation step. Generate the 500 noisy realizations exactly as specified (`t = np.linspace(0, 10, 1000, endpoint=False)`, base signal `3.0*sin(2*pi*18.23*t)`, Gaussian noise mean 0 / std 5.0), compute `np.abs(np.fft.rfft(signal))**2` for each realization, and average all 500 power spectra elementwise into one averaged spectrum. Get frequency bins via `np.fft.rfftfreq(1000, d=0.01)`. Find `argmax` of the averaged spectrum to get the peak index `p`, then take the three points at indices `p-1, p, p+1` (frequency and power pairs) and fit a quadratic `P(f)=af^2+bf+c` (e.g. `np.polyfit`) to those three points; the parabola's vertex `-b/(2a)` is your refined frequency estimate — don't just report the raw bin frequency, since the whole point of the parabolic fit is sub-bin precision. Round the vertex to two decimals. Then read `/home/user/references.csv`, compute `abs(freq - candidate)` for each material row, and pick the minimum. Write the result to `/home/user/result.txt` as exactly `MaterialName, XX.XX` with no extra whitespace or trailing newline issues.