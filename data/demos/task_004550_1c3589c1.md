Work through the three pieces independently and test each before wiring them together, staying strictly in Bash/coreutils/awk. For `migrate.sh`, read `raw.csv` and use `awk -F, -v OFS=, '{print $1,$2,"GET",$3}'` (keep timestamp, ip, insert literal `GET`, keep endpoint, drop user_agent) to write `migrated_logs.csv` in the new column order `timestamp,ip,method,endpoint`. For the ABI wrapper, you must NOT touch `processor.sh` — only append to `libvalidate.sh` a new function `validate_ip_v2() { validate_ip "$@"; }` so it forwards arguments and return status transparently. For `rate_limiter.sh`, since it's called once per line, persist state across invocations using a file-based counter keyed by `ip_timestamp` (e.g. a directory of files or a single state file you grep/increment with `awk` under a lock), incrementing on each call and exiting 1 once the count for that exact (IP, timestamp) pair exceeds 2 — remember "strictly more than 2" means the 1st and 2nd requests are allowed, the 3rd+ is rate limited, so `> 2` is the check on count *after* incrementing, or check *before* incrementing whether the existing count is already 2. Make both scripts executable, then run `migrate.sh` followed by `processor.sh` and diff `/home/user/output/final_processed.txt` against the expected accept/rate-limited pattern to confirm the per-second-per-IP threshold logic actually matches before finishing.
