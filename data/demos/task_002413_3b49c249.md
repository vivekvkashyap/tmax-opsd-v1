Since `/app/policy.png` is an image, use an OCR tool (`tesseract` if available, or another local OCR method) to extract the literal policy text rather than assuming a generic CSV-validation ruleset — the exact rejection criteria (reshape rules, empty/null handling, embedded-newline rejection, duplicate-record rejection) must be read off the image, not guessed. Once you have the rules, write `/home/user/log_classifier.py` to: read the CSV robustly using Python's `csv` module (which correctly handles RFC-4180 quoting, so embedded newlines inside quoted fields don't get silently mis-parsed as separate rows — you need to detect them, not lose them), melt/reshape the wide event-type columns into long form (timestamp, user_id, event_type, details), drop rows with empty/null details, then apply the OCR'd rejection rules: reject the whole file (`sys.exit(1)`) if any surviving `details` value contains a literal `\n`/`\r`, or if any (timestamp, user_id, event_type) triple appears more than once after reshaping. Exit 0 only if no rule is violated. Validate your script against both `/app/corpora/clean/` and `/app/corpora/evil/` before finishing, since the pass criterion requires 100% correct classification.