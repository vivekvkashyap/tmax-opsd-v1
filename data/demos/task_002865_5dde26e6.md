Read the existing `train_model.py` first to see the exact join/feature logic, then restructure it so `train_test_split` is called on the raw (unscaled) feature/label frame first, and only afterward instantiate `StandardScaler()`, call `.fit(X_train)` (or `fit_transform` on train only), and `.transform(X_test)` using that fitted scaler — never fit on combined or test data. Keep `test_size=0.2, random_state=42` on the split and `random_state=42` on `LogisticRegression` so results are deterministic and reproducible. For benchmarking, wrap only the `model.predict(X_test)` call with `time.perf_counter()` before/after to get inference time, not training time. Write `/home/user/metrics.json` with keys `accuracy` and `inference_time_seconds` using `json.dump`. For predictions, build a DataFrame with exactly the `user_id` (from the test split's original rows, not scaled data — keep a reference to the original index/user_id before scaling) and `predicted_churn` columns, then `to_csv(..., index=False)`. Run the script afterward and re-open the output files to confirm they exist and have the right shape/columns before considering the task done.