Parse the JSONL file line-by-line with Python's `json` module, filter to `metrics.status == "success"`, then group by `collection`. Within each group, sort successful backups by `timestamp` ascending (ISO 8601 strings sort correctly lexically, but parsing with `datetime.fromisoformat` after stripping/handling the `Z` suffix is safer). For each collection take the last min(3, count) entries in that sorted order as the moving-average window, average their `size_bytes`, and compare the most recent entry's `size_bytes` against `1.5 * average` using a strict `>`. A common mistake is computing the average over ALL backups instead of just the trailing window, or forgetting that the "most recent" backup is itself included in the 3-backup average (not compared against the average of the 3 *preceding* it). Collect collection names that satisfy the anomaly condition, sort alphabetically with Python's default string sort, and write one name per line to `/home/user/anomalies.txt` (no trailing blank line issues, no extra whitespace).