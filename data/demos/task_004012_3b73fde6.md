Extract the archive first (`tar -xzf /home/user/storage/telemetry_dump.tar.gz -C <workdir>`) and write one analysis script that iterates over all extracted files, dispatching by extension. For `.log` files, parse as CSV with columns `timestamp,hostname,error_code,message` (watch for a header row) and flag the whole file as "critical" if ANY row has `error_code` strictly greater than 500 — count files, not rows. For `.meta` files, parse as JSON and collect the `id` field wherever `version` is strictly less than 2.0, then sort the collected ids alphabetically (string sort, not numeric) before writing them out. For `.bin` files, read exactly 8 bytes: unpack the first 4 as an unsigned 32-bit big-endian integer (magic) and the next 4 as unsigned 32-bit little-endian (version) — get the byte order right for each field independently, since mixing them up is the most common mistake here — and count files where magic equals `0xDEADC0DE` exactly and version equals exactly 3. Assemble the three results into the exact JSON structure requested (`critical_error_count`, `legacy_meta_ids`, `target_bin_count`) and write it to `/home/user/storage_report.json`.
