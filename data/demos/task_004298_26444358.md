Step 1 is OCR recovery, not guessing: run `tesseract /app/policy.png stdout` (or similar) to extract the policy text, read the output carefully, and pull out the exact quoted signature string the policy names as the log-bomb marker — verify it by re-checking the OCR text rather than assuming a value, since OCR can mangle punctuation/underscores. Step 2: write `/home/user/filter_logs.py <input_dir> <output_dir>` that iterates `.tar.gz` files in the input dir, opens each with Python's `tarfile` module (use streaming mode `r|gz` or extract members individually rather than loading everything into memory, since some archives are large), scans each contained log's text content for the recovered signature string, and copies (`shutil.copy2`, preserving the original file unchanged) any archive that does NOT contain the signature to the output dir — archives that do contain it are simply skipped, not copied. Step 3: run your script against `/app/corpus/clean` and `/app/corpus/evil` yourself first to sanity-check 100% pass/reject behavior before relying on it, output survivors to `/home/user/processed_logs/`, then generate `/home/user/retention_manifest.txt` via `sha256sum /home/user/processed_logs/*.tar.gz > /home/user/retention_manifest.txt` (or equivalent) to match the required `<sha256>  <filename>` format exactly.