Do this in two clearly separate phases. Phase 1 (bisection): run `git bisect start`, `git bisect bad master`, `git bisect good v1.0`, then at each step write a small test script that checks out won't be needed manually — instead use `git bisect run <script>` with a script that builds/runs the current `server.sh` state against a known input and exits non-zero if it exhibits the precision loss or dropped-last-reading bug (e.g. feed a fixed set of floats through the summation path being tested and check the count of values consumed and the precision of the result), letting git automate the search; capture the identified commit hash (`git rev-parse` after bisect finishes) into `/home/user/bad_commit.txt`, then `git bisect reset`. Phase 2 (fix + deploy): checkout `master`, rewrite `server.sh` to parse `DATA:` floats into an array and pass all of them (fix any off-by-one loop bound) to `/app/kahan_oracle` as command-line arguments, capturing its stdout as the precise sum. Implement the TCP protocol (auth check -> AUTH_OK/AUTH_FAIL, then DATA -> RESULT) using `socat` with a per-connection handler (e.g. `socat TCP-LISTEN:8888,fork,reuseaddr EXEC:...` invoking a script implementing the exact line protocol), and launch it backgrounded so it stays listening for the verifier.
