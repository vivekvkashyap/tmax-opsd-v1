Extract the base dependency rules programmatically rather than assuming any fixed values — decode /app/protocol_dump.mp4 frame by frame (10 frames, 1 fps), and for each frame compute the mean pixel intensity of the top half and bottom half separately, rounding each to the nearest integer to get job IDs U and V for that frame's rule (U before V); build your 10 base rules from this actual extraction so your emulator's correctness doesn't depend on hardcoded numbers. Define job.proto exactly as given and compile it with `protoc --python_out=.` for Python bindings, importing the generated module in emulator.py. In the emulator, model dependencies as a map from job -> set of prerequisite jobs, seeded with the 10 extracted base rules; process instructions in order, where ADD_DEP adds to that same structure so base and dynamic rules are checked identically; on EXECUTE, scan the pending set once, select jobs whose full prerequisite set is a subset of executed, sort the ready ones numerically, then move them from pending to executed and append to the log — critically, do this in one pass without re-checking newly executed jobs within the same EXECUTE call. Print the log as comma-joined values with no trailing separator.