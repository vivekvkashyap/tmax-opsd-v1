Treat the legacy binary as a black box to reverse-engineer via targeted probing before writing any Go code. Feed `/app/legacy_cleaner` simple, hand-picked sequences (a single value, a constant repeated sequence, a step change, a ramp) via stdin and study the output to spot patterns: a single first-output equal to some deterministic function of x_0 suggests a recursive/stateful transform (like an exponential moving average) rather than a stateless per-element function; a bounded output range for large inputs (values compressing toward ±1) suggests a squashing function like tanh applied afterward. Hypothesize the state-update formula and the final transform, implement it in `/home/user/cleaner.go` reading whitespace-separated floats from stdin and writing space-separated results with `%.6f` formatting (matching the legacy binary's exact separator and precision), then diff your output against the oracle's on both your hand-picked sequences and randomized ones across a wide value range before trusting it — floating-point formatting/rounding mismatches are the most common source of near-but-not-bit-exact failures. Build with `go build -o /home/user/go_cleaner /home/user/cleaner.go`. Separately, write `/home/user/setup_db.sh` to create the `sensor_data` Postgres database and the `cleaned_features(id SERIAL, value FLOAT8)` table via `psql`/`createdb`.
