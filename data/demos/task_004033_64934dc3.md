Two independent deliverables here; do not let Part 1 block Part 2's correctness. For Part 1: extract frames with `ffmpeg -i /app/access_logs.mp4 -vf fps=1 frame_%03d.png` (or similar), then OCR each frame with `tesseract frame_XXX.png stdout`, parse lines matching `ACCESS: [A-Za-z0-9]+ -> [A-Za-z0-9]+ [0-9]+` with a regex, and append successfully-parsed edges to `/home/user/extracted_edges.txt`; skip frames that fail to OCR cleanly rather than crashing. For Part 2, the real work: implement Dijkstra's algorithm (all weights are positive) in C, since this will be fuzz-tested for byte-exact stdout against a reference binary. Key correctness details: use a proper priority queue (binary heap) for performance at V up to 1000 and E up to 5000, do not print any debug/diagnostic text, print exactly one integer per query line with a trailing newline matching the reference's formatting, output -1 for unreachable targets, and free all allocated memory to avoid leaks under repeated fuzzing. Read stdin efficiently (scanf/fread) since inputs can be large across 100 test cases. Compile with `-O2` for both correctness and performance parity.
