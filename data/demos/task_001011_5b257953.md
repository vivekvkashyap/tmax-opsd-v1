This has two independent halves — solve them separately. Part 1: run `collect_diagnostics.py` first to see it fail, then read `/app/vendored/sh-1.14.3/sh.py` around the `RunningCommand` class and the argument-compilation code; look specifically for (a) a threading lock that is supposed to guard shared state during concurrent execution but appears commented out or missing (this causes intermittent race-condition crashes, so reproduce by running the script several times, not just once) and (b) a place where a command string with a filename is being split into args using a naive `.split(" ")` instead of something that preserves whole arguments (e.g. `shlex.split` or a proper quoting mechanism) — this breaks any file with a space in its name. Patch both in place in the vendored file (do not pip install a replacement), then re-run `collect_diagnostics.py` repeatedly until it reliably succeeds and populates `/home/user/raw_logs/`. Part 2: `sanitizer.py` must parse the numeric columns of the CSV and detect NaN, ±Infinity, and values that overflow standard 64-bit float (e.g. from scientific notation like `1e400`) — use Python's own float parsing/`math.isnan`/`math.isinf` plus a check for `float('inf')` results from out-of-range literals, exiting 1 on any hit and 0 otherwise; test it against the extracted logs from Part 1 and craft your own poisoned/clean examples to validate both exit codes before finishing, since it will be run against a large hidden corpus of both.