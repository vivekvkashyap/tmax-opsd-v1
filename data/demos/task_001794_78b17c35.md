Split this into a build-fix task and a performance-engineering task, then integrate. For `/app/supercronic`: grep the source for where it manipulates environment variables (likely an `init()` in `main.go`) — you'll find something like `os.Setenv("PATH", "/tmp/wrongpath")`; remove or fix that line so it doesn't clobber PATH, since jobs it spawns need to find system binaries. Separately check the `Makefile` for a misnamed build target (e.g. `bulid:` instead of `build:`) and correct it, then `make build` (or the fixed target) and place the resulting binary at `/home/user/bin/supercronic`. For the Go sync script, first implement TCP dial checks with `net.DialTimeout("tcp", "localhost:5900", time.Second)` and the same for `:8080`, exiting 1 on either failure. Then read the 200k-row CSV, and instead of hashing rows one at a time, fan the work out across a worker pool (goroutines reading from a channel of rows, each computing `sha256.Sum256([]byte(username+role))`, writing results to a shared pre-sized slice indexed by row number to avoid lock contention) and join with a `sync.WaitGroup` — sequential hashing will blow the 1.5s budget by ~2x. Marshal the results slice to JSON once, at the end, rather than incrementally. Finally write `/home/user/sync.cron` with a `* * * * *` line invoking `/home/user/bin/sync_users`, and test end-to-end with `/home/user/bin/supercronic -test /home/user/sync.cron` or by direct execution.
