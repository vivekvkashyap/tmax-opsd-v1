Build the pipeline as two decoupled concerns: get any reasonable transcript with timestamps first, then make the Go aggregation logic exactly correct, since the metric only requires ~70% Jaccard overlap so transcription need not be perfect. Use `ffmpeg` to inspect/convert `call_recording.wav` if needed, then run a CLI transcription tool available on the system (or build whisper.cpp) to produce an SRT/VTT with per-segment start/end timestamps and text. In the Go program, open the transcript with `bufio.Scanner` or `bufio.Reader` (stream line-by-line, avoid `ioutil.ReadFile` on the whole file) and parse each timed segment's start time in seconds; assign the segment's words to the bucket `floor(start/10)*10` to `+10` — decide precisely whether bucket assignment is per-segment-start or per-word-timestamp based on what your transcript format gives you, and be consistent. Normalize tokens by lowercasing and stripping non-alphanumeric characters (regex `[^a-z0-9]` after lowercasing, then drop empty tokens), then aggregate counts per (bucket_start, word) into a map, and stream results out as JSON lines matching the exact schema, sorted or not as required. Compile with `go build` and run it to produce `/home/user/etl/output.jsonl`, then sanity-check a few lines by hand against the audio content.
