Since the source is lost, treat `/app/calc_service_stripped` as a black box: probe it with a range of small, hand-chosen `SEQ[x,y]` inputs (e.g. varying x from 0 upward with y fixed, and vice versa) via `echo "SEQ[x,y]" | /app/calc_service_stripped` and record the outputs into a table, rather than guessing a formula from the binary's disassembly alone — this Ackermann-like family is easiest to reverse-engineer empirically because the x=0 base case and the y=0 recursion collapse quickly to simple patterns you can spot by hand or with a spreadsheet. Also probe edge cases (negative numbers, malformed strings like missing brackets) to confirm the binary crashes/segfaults on them, since your Python port must instead catch these and print `INVALID_FORMAT` with exit code 0 rather than raising. Once you have a hypothesis for the recurrence, implement it in `/home/user/clean_calc.py` using an explicit stack or memoized iterative approach (plain unbounded recursion can hit Python's recursion limit for larger x/y, unlike the C version) and cross-check it against dozens more probed `(x,y)` outputs from the real binary before trusting it — since correctness is judged by 10,000 fuzz cases of bit-exact match, verify broadly rather than on a handful of samples. Parse the `OP[arg1,arg2]` format with a strict regex so malformed input is rejected cleanly.
