Treat `/app/sensor_model` as a black-box CLI you must repeatedly invoke, not something to reverse-engineer symbolically: for each `dt`, run it via `fork`/`exec` or `popen("/app/sensor_model f0 damping dt", "r")`, parse the "t y" lines it prints from its stdout, and compute the signal energy as the trapezoidal integral of y(t)^2 over the sequence of samples returned (pair up consecutive points, don't assume a fixed sample count since it depends on dt). Implement the convergence loop exactly as specified: start dt=0.1, compute energy, then repeatedly halve dt, compute the new energy, and stop once |energy(dt) - energy(prev_dt)| < target_error, remembering the first dt=0.1 step has no prior comparison. For dominant frequency, implement a straightforward O(n^2) or O(n log n) DFT/FFT over the sampled y(t) values at the converged dt, find the magnitude peak among strictly positive frequency bins (excluding the DC/0 Hz bin), and convert bin index to Hz using the actual sample spacing dt. Build the TCP server with raw sockets (`socket`/`bind`/`listen`/`accept` in a loop so it keeps serving after each request), parse the `ANALYZE <f0> <damping> <target_error>` line per connection, and reply with `RESULT <dt> <energy> <freq>\n` where each float is formatted to exactly 4 decimal places, then close only the client socket. Compile with g++ and background it listening on 127.0.0.1:8888.