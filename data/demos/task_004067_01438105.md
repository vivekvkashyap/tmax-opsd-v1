Treat this as two separate investigations before writing the server. First, extract the key: use `ffmpeg -i /app/incident_record.mp4 -vf fps=<rate> frame_%03d.png` to pull frames around the 3-second mark (try several fps values / frame offsets near t=3.0s so you don't miss the exact frame), then read the on-screen terminal text directly from the image (visually inspect it, or OCR with `tesseract` if the frame is legible) to recover the 16-character key exactly as typed — don't guess or truncate it. Second, reverse-engineer `/app/legacy_decoder.bin` with `strings`, `objdump -d`, or `file`/`readelf` to identify its transformation: look for base64 tables/alphabets and simple loop-with-XOR patterns in the disassembly, and confirm your hypothesis by testing the binary on a few known inputs (feed it plaintext, see what comes out, or vice versa) before committing to the decrypt logic. Only after both are confirmed, build the HTTP service on 127.0.0.1:9000: base64-decode the payload, apply your derived reverse transform with the recovered key, hash the result with `hashlib.sha256`/`sha256sum` and compare hex digests to the provided checksum (return 403 on mismatch), then execute the decrypted string via subprocess with a 2s timeout, `cwd=/tmp/sandbox` (create that directory first), and return its stdout with 200 OK. Keep the server running as a background persistent process handling multiple sequential requests.