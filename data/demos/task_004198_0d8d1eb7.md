Implement and validate each piece separately before wiring them together. First write `model.c` exactly per spec: read the CSV rows (id,x1,x2,x3,label), for each compute Euclidean distance to (1,1,1), score = exp(-D)*alpha, predicted label = (score > 0.1), accuracy = correct/total, print only that float. Compile with `gcc -o model model.c -lm` and sanity-check on a tiny hand-made CSV where you know the answer by hand. Then in `tune.sh`, be precise about the fold boundaries: fold k is rows (20*(k-1)+1) to (20*k) of the 100-row file — use something like `sed -n` or `awk 'NR>=a && NR<=b'` to slice, and make sure "validation" for each fold means running the accuracy computation ONLY on that fold's 20 rows (re-read the task's cross-validation definition carefully — decide train/val split consistently, since the C program takes a whole CSV path). Loop over the five alpha values, accumulate accuracy per alpha across the 5 folds, average, then pick the alpha with the highest average using pure bash/awk comparison (careful with floating point comparisons — use awk for numeric comparisons, not bash arithmetic). On a tie, keep the first (lowest) alpha encountered. Write `alpha,avg_accuracy` (2 decimal places) to best_model.txt, then execute tune.sh yourself and inspect the file before finishing.