First extract the embedded attachment from the video: use `ffmpeg -dump_attachment:t oracle_tracker -i /app/screencast.mp4 out.null` (or similar `-dump_attachment` invocation) to pull the attached file out of the mp4 container, then `chmod +x` it and save it to `/home/user/oracle_tracker` — this is your reference for behavior, not something to just copy as your final answer. For the C++ parser, model the filesystem as a nested tree structure (e.g. a `std::map<string, variant<int, Node>>` or similar), parsing each line's TIMESTAMP/ACTION/PATH/SIZE, splitting PATH on `/`, and walking/creating intermediate directory nodes. On CREATE/MODIFY, set the leaf to the integer size; on DELETE, erase the leaf key entirely (leaving parent directories as `{}` if now empty, not removing the parent). Ignore any line whose path doesn't start with `/`. Serialize the final tree to minified JSON (a `nlohmann::json` object works naturally here) with keys in a canonical order that matches the oracle's JSON library serialization — since the grader is fuzz equivalence, write a local test harness that feeds many randomized event sequences to both your binary and the extracted oracle and diffs stdout to catch any behavioral mismatch (e.g., edge cases like deleting a file that leaves a directory empty).