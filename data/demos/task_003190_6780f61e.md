Do the three-stage filter in strict order since later stats depend on earlier cleaning. First drop rows where `raw_value` or `model_prediction` is empty string, exactly `"NaN"`, or exactly `"NULL"` — use string equality checks, not numeric NaN-parsing, since parsing "NaN" as a float silently yields a valid float NaN that later math would propagate incorrectly. On the surviving rows, compute the **population** mean and stddev of `raw_value` (divide by N, not N-1), then drop rows strictly outside `mean ± 2*stddev` using that same first-pass mean/stddev (don't recompute per-row). On the resulting clean set, count rows where `|raw_value - model_prediction| > 0.5` (strict inequality — a value exactly at 0.5 must NOT count). Recompute the population mean of `raw_value` on the clean set only, round to exactly 2 decimals. Write the four-line report to `/home/user/analysis_report.txt` with the exact field labels and order given, and no extra text/trailing content beyond what's specified, since verification is an exact text diff.