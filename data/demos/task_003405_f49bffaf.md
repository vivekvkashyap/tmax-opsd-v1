In `/home/user/resolve.py`, first parse the CLI constraint string by splitting on `", "` and then on operators `>=, <=, ==, >, <` (check the two-character operators before the single-character ones to avoid mis-parsing `>=` as `>`). Implement a proper semantic-version comparator that splits each version on `.` and compares components as integers component-by-component — never compare version strings lexicographically, since that would wrongly rank `1.10.0` below `1.2.0`. Read `/home/user/mirror/index.csv`, and for each row decode the base64 filename, then compute `hashlib.sha256(open(path,'rb').read()).hexdigest()` and compare it to the `ExpectedSHA256` column — discard any row whose computed hash doesn't match, since that's the "corrupted/tampered" file the task is designed to catch. Among rows that pass both the constraint filter and the checksum check, select the maximum version using your semver comparator, and write the file's absolute path with no trailing newline (use `sys.stdout.write` or strip the string before writing) to `/home/user/link_target.txt`. Run the script with the given constraint and confirm the output path exists on disk before finishing.