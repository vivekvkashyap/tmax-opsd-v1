Follow the pipeline exactly in the order the task describes, since later steps (median fill, stats) depend on the intermediate cleaned data, not the raw CSV. Load `/home/user/raw_data.csv` with pandas, `dropna(subset=['sensor_a','sensor_b'])` first, then compute the median of `value_c` on that already-row-dropped frame (not the original) and `fillna` it into `value_c` — order matters because dropping rows changes which values contribute to the median. Write the cleaned frame to SQLite with `sqlite3`/`df.to_sql('sensor_data', conn, index=False)`. For stats, use `scipy.stats.pearsonr` (or `df['sensor_a'].corr(df['sensor_b'])`) for correlation, and `scipy.stats.ttest_ind(treatment_values, control_values, equal_var=False)` for Welch's t-test — pass treatment as the first array and control as the second, matching the required argument order exactly, since swapping them flips the sign of the t-statistic. Round each of the three result values to exactly 4 decimal places before writing `results.json` with the three specified keys. Sanity-check by re-reading `cleaned_data.db` and confirming row counts and value ranges look plausible before finishing.
