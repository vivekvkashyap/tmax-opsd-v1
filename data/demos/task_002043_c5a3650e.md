Work sequentially since each step unlocks the next. Run `tesseract /app/diagram.png stdout` (try preprocessing with `convert`/ImageMagick — grayscale, threshold, upscale — if the raw OCR output is noisy) to recover the master password text embedded in the image; don't guess it. Use that recovered password to decrypt the AES-256 zip, e.g. `7z x -p<password> /home/user/infra.zip -o/home/user/infra/` (or `unzip -P`), and confirm `ssh_config`, `nginx.conf`, `access.log` are extracted. In `harden.py`: shell out to `ssh-keygen -t ed25519 -N "" -f /home/user/infra/id_ed25519` for the keypair; edit `ssh_config` in place, replacing/adding `PasswordAuthentication no` and `IdentityFile /home/user/infra/id_ed25519` (don't just append duplicate directives — replace existing matching lines); inject a `Content-Security-Policy` header line inside the `server { }` block of `nginx.conf` containing `default-src 'self'` and no `unsafe-inline`/`unsafe-eval`; and parse `access.log` request lines with a regex looking for `../` or its URL-encoded form `%2e%2e%2f` (case-insensitive) in the path, extracting and deduping the source IP field, one per line to `banned_ips.txt`. Re-read each output file after writing to confirm the edits actually landed.
