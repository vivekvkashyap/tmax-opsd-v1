Start the given services script first and confirm PostgreSQL/Redis are actually reachable (e.g. `psql`/`redis-cli PING`) before writing code against them. Design the SQL as a single query using a CTE that sums each employee's sales within the department and date range, then applies `RANK() OVER (ORDER BY total_sales DESC)` to get the top performers, plus a second CTE or subquery for the department-wide total — pass dept_id/start_date/end_date as bound parameters via pqxx's parameterized/prepared-statement API (never string-format them into SQL) to avoid injection and match the spec. Build the Redis cache key exactly as `dept_report:<dept_id>:<start_date>:<end_date>`, check it with hiredis before querying Postgres, and on a miss compute, print, and SETEX it with a 60-second TTL so a second identical run hits the cache and returns byte-identical output. Match the output formatting precisely — literal labels, 2-decimal amounts, omitting RANK lines when fewer than 3 employees have sales — since a fuzz harness will diff your output against a reference binary across many random dept/date combinations, so test with several edge cases (empty department, fewer than 3 sellers, boundary dates) not just one happy path.
