First recover the two policy constants from the image: run OCR (e.g. `tesseract /app/policy_memo.png stdout`) on `/app/policy_memo.png` and read off the `CRITICAL_CWE` identifier and the `TRUSTED_HASH` SHA256 string exactly as printed — do not guess or hardcode plausible-looking values, use what OCR actually returns (clean up any stray whitespace/newlines OCR introduces). Then write `/home/user/audit_parser.sh` as a bash script that reads stdin line by line (`while IFS='|' read -r filepath filehash cwe message`), being careful to also process a final line lacking a trailing newline (`|| [ -n "$filepath" ]` in the loop condition). Apply the four checks in strict precedence order — CWE-000 config error first (overrides everything), then hash mismatch, then critical-CWE match, then compliant — using exact string equality (`=`), not substring or pattern matching, for both the hash and CWE comparisons. Emit output with no extra text/headers, exactly as specified per line. Make the script executable with `chmod +x`. Test it locally against a handful of crafted lines covering each branch before considering it done, since verification is a bit-exact fuzz comparison against a reference implementation.