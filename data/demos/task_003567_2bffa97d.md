Start by diagnosing the install failure: look at `/app/lmfit-1.2.2/setup.cfg` (not setup.py) for the `install_requires` numpy pin — a version like `numpy==1.9.0` can't build on modern Python, so relax it to a compatible range (e.g. `numpy>=1.20`) and reinstall the package locally with pip before importing it. For the fit itself, build a composite lmfit model by summing three `GaussianModel` instances (each with a distinct `prefix`, e.g. `g1_`, `g2_`, `g3_`) plus a `ConstantModel` for the baseline, then use `model.guess()` or manually seed reasonable center/amplitude/sigma starting points per peak (inspect the data to place initial guesses near the visible bumps) before calling `.fit()` — bad initial guesses are the main way multi-peak fits fail to converge to the true peaks. After fitting, evaluate `result.best_fit` on the same wavelength grid, normalize it by dividing by its sum so it sums to exactly 1.0, and save with `numpy.save` to the exact required path. Verify fit quality visually or via residuals before finalizing.
