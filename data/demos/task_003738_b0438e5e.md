Recover the missing parameter before touching the math: run OCR (e.g. `tesseract /app/incident_artefacts/dashboard_snapshot.png stdout`) on the dashboard screenshot and read off the regularization `alpha` value it reports, rather than guessing or leaving it at 0.0. Separately, inspect `/app/logs/ingest.log` and `/app/logs/engine.log` to understand the timeline (note their differing timestamp formats/timezones need normalizing to compare) and confirm when erratic outputs began relative to the format change. In `/app/src/engine.cpp`, find where numeric fields are parsed from `/app/data/input.csv` — a plain `std::stod` or `atof` stops at the first non-numeric character, so values using `D` instead of `E` for the exponent (legacy Fortran-style, e.g. `2.01D-02`) get silently truncated; fix the parser to normalize `D`/`d` to `E` before parsing (e.g. a string replace) so both formats parse correctly. Replace the hardcoded `alpha = 0.0` with the OCR'd value, and confirm the formula `score = (x*y)/(x-y+alpha)` uses it. Recompile with `g++ -O3 /app/src/engine.cpp -o /app/bin/engine`, run it against `/app/data/input.csv`, and write `id,score` to `/app/output/scores.csv`. Spot-check a couple of rows by hand-computing the formula to validate before finishing, since the grader compares MSE against a private reference.
