Build this as a clean multi-stage pipeline rather than one tangled loop. First, load both files: parse the JSONL (`latency_s` -> multiply by 1000 for ms) and the CSV (`latency_ms` already in ms), tagging each record with its source service. Second, sort the combined records by `timestamp` ascending. Third, deduplicate by `req_id`: walk the sorted list and keep only the first occurrence of each `req_id`, dropping later duplicates regardless of service — do this pass before anomaly detection, not during. Fourth, replay the deduplicated, sorted list chronologically while maintaining a separate rolling window (e.g. a deque capped at length 3) of latencies per service; after appending each record's latency to its service's window, compute that window's average and check if it strictly exceeds 500.0ms — the first record where this trips is your answer. Watch the trap of computing the window average before vs. after including the current record — the check must include the current entry. Write the single first-triggering record's service/timestamp/req_id to `/home/user/anomaly.json`.