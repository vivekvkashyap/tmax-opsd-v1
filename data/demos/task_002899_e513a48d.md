First fix `/home/user/services.json` directly with the required host/port values for each service. Then write `/home/user/deploy.py` as a plain sequential script (no need for a class): load the JSON, and for each service (a) build a tarball of the directory the *current* symlink currently resolves to — use `tarfile` and pass the resolved real path so you archive the old release's contents, not the symlink itself; (b) remove the existing symlink (`os.remove`/`os.unlink`, checking `os.path.islink`) before creating the new one pointing at the `_v2` release directory, since `os.symlink` fails if the link already exists; (c) launch `python3 -m http.server <port> --bind <host> --directory <path>` with `subprocess.Popen`, redirecting stdout/stderr to files or DEVNULL so the pipe doesn't block, and deliberately do NOT call `.wait()` or use `subprocess.run` — the server must keep running after your script exits; (d) write `proc.pid` to the pid file. Do the backup step before swapping the symlink, since the backup must capture the old (v1) content. Finally, actually execute the script and confirm both servers respond on their ports before finishing.
