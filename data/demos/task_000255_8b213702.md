Tackle the four subsystems independently and verify each before integrating. (1) Fix `/app/backend/pyproject.toml` by removing the `legacy-math-ext` dependency and bumping `flask`/`redis` to current compatible versions, then confirm with `pip install .` that it succeeds cleanly in a Python 3 environment. (2) Reverse-engineer `/app/legacy_oracle` empirically, not by guessing: run it with controlled inputs where you vary only one component at a time (e.g. fix minor/patch/n and sweep major, then fix major/patch/n and sweep minor, etc.) to isolate each term's coefficient and confirm how the semver components and integer N combine (e.g. simple weighted sum) — cross-check with a few random combined inputs before finalizing. Implement the exact same arithmetic in `/home/user/math_worker.sh` using bash arithmetic (`$((...))`), parsing the semver with `IFS=. read` or a regex. (3) Edit `app.py` to replace the `import legacy_math_ext`/`legacy_math_ext.compute(...)` call with a `subprocess.run(["bash", "/home/user/math_worker.sh", v, n], capture_output=True, text=True)` call, keeping the Redis caching logic intact. (4) Add a `location /api/math { proxy_pass http://127.0.0.1:5000; }` block to `nginx.conf`, start Redis, Flask (port 5000), and Nginx (port 8080) as background processes, then manually `curl` the full chain end-to-end and diff a few outputs against direct calls to `/app/legacy_oracle` to confirm bit-exact equivalence before considering it done.
