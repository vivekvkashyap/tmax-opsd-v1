Scaffold with `cargo new route-compiler` under `/home/user/`, then structure the program as clear pipeline stages so each is separately testable: (1) a parser that reads every `.route` file in `/home/user/routes/` (skip `previous.txt`), splits each line on whitespace into method/path+query/handler/weight, splits the path on the first `?` to get base path and query string, and splits the query string on `&` then `=` to collect just the parameter keys, sorting them alphabetically; (2) a merge step keyed by `(method, base_path)` in a `HashMap`, where for each incoming route you compare against any existing entry and replace it only if the new weight is strictly higher, or equal-weight with an alphabetically-earlier handler name — process routes in a stable, deterministic file/line order so the "first wins" tie logic is consistent; (3) a final sort with a composite key `(Reverse(weight), method, base_path)` using `sort_by` or `sort_by_key`; (4) a diff against `previous.txt` (parsed with the same key format) comparing key-sets for ADDED/REMOVED and, for keys present in both, comparing handler, weight, and sorted param keys for MODIFIED, then sorting the diff output by method then base path. Write both output files with `std::fs::write`, formatting each line exactly per the spec (mind the `[]` vs `[key1, key2]` bracket format), and run the binary once to eyeball the output against your own manual trace of the sample input before declaring done.
