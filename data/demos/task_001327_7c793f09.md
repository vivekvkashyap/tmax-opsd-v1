Work through the five deliverables independently and verify each before moving on. For the DB: run `EXPLAIN QUERY PLAN` on the join-and-group-by query first to see where it's scanning instead of seeking, then add indexes on the join key (`interview_tags(interview_id, tag_id)`) and the filter column (`interviews(location)`) and re-check the plan. For the export script: write plain SQL that joins `interviews`→`interview_tags`→`tags`, filters `location='Amazonas'`, groups by `tag_name`, sums `duration_seconds`, and pipe the `sqlite3 -json` (or manual formatting) output into the target JSON array shape. For transcription: pipe `/app/pending_interview.wav` through `/app/whisper-cli` with the tiny model, capture stdout, and strip whisper's timestamp/log decoration so only the plain transcript text lands in the output file — don't just redirect raw stdout, which includes bracketed timestamps. For the HTTP server, since Python/Node are disallowed, use `socat TCP-LISTEN:8080,fork,reuseaddr EXEC:handler.sh` (or an `nc` loop) where the handler script parses the request line for the path and manually writes a well-formed HTTP response with correct `Content-Type` and `Content-Length` headers before the body, for both `/summary` and `/transcript` routes.