Structure the Go program as: FASTA parser -> concurrent GC-ratio computation -> CSV join -> OLS fit -> JSON output, and get the filtering right since that's where naive attempts fail. Parse FASTA records by splitting on `>` headers, taking the ID as the token up to the first space/newline, and concatenating subsequent lines until the next header as the sequence. For each sequence, count A/C/G/T (case-sensitive as written, or normalize case consistently) and compute `gc_ratio = (G+C)/(A+C+G+T)`; you can dispatch this per-sequence in goroutines with a WaitGroup/channel for real concurrency, but the critical correctness step is: if `A+C+G+T == 0`, skip the sequence entirely (this is what filters out the all-N corrupted reads) rather than letting a division produce NaN/Inf that would corrupt the regression. Join surviving sequences against `growth_rates.csv` by ID, keeping only sequences with a matching row, then compute the two OLS sums (`Σ(x-x̄)(y-ȳ)` and `Σ(x-x̄)²`) over exactly that filtered set to get β1 and β0. Round both to 4 decimal places and write the JSON with those exact two keys to `/home/user/model_results.json`.