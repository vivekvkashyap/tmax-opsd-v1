Bring up Neo4j with `docker run -d --name research_graph -p 7474:7474 -p 7687:7687 -e NEO4J_AUTH=none neo4j:5`, then poll (e.g. loop `curl -s http://localhost:7474` until it responds, or check container logs for "Started") before loading data — don't just sleep a fixed short time. Load `/home/user/graph_setup.cypher` via `docker exec research_graph cypher-shell -a bolt://localhost:7687 --format plain < graph_setup.cypher` (or pipe the file in), confirming no errors. For `get_dependencies.sh`, build the HTTP request body as JSON where the Cypher `statement` uses a `$pid` parameter placeholder and a separate `"parameters": {"pid": "$1"}` object — do not string-interpolate the paper ID directly into the Cypher text, since the verifier checks for real parameterization. Write the recursive Cypher using a variable-length relationship pattern like `MATCH (:Paper {id: $pid})-[:CITES*1..3]->(p:Paper) RETURN DISTINCT p.id AS id` to cap the hop depth at 3 and get distinct nodes. Parse the nested `results[0].data[].row` structure from the HTTP API response with `jq`, extract just the id strings, sort with `sort`, and write one per line to `/home/user/output.txt`. Test with `P1` and sanity-check the hop count against the graph before finalizing.