Write a straightforward C program that: opens `/home/user/quota_data.txt`, parses each line with `sscanf(line, "%[^:]:%ld:%ld", username, &used, &quota)` (or split on ':'), and for each row compares `used <= quota` using integer comparison. When within quota, build the exact tar command string with `snprintf` — `tar -czf /home/user/backups/<username>_backup.tar.gz -C /home/user/user_data <username>` — and invoke it via `system()`, checking its return value before logging success (don't log BACKUP_SUCCESS if the system() call actually failed). Append one line per user to `/home/user/provision.log` via `fopen(..., "a")`, using exactly `BACKUP_SUCCESS: <username>\n` or `QUOTA_EXCEEDED: <username>\n` with no extra formatting. Compile with `gcc -o /home/user/provision_backup /home/user/provision_backup.c`, then actually run the executable so the tarballs and log get generated — don't just compile and stop. Double-check the equal-to-quota case counts as within-quota (backed up), not exceeded, since the task specifies "less than or equal to."
