Split this into stats, a vendored-package fix, and a server, and verify each independently before combining. For tokenization: lowercase, strip only `. , ! ?`, split on whitespace, then compute mean token length per document; use `scipy.stats.ttest_ind(treatment_means, control_means, equal_var=False)` for Welch's t-test and derive the 95% CI for the mean difference using the Welch-Satterthwaite degrees of freedom (t-critical value times the standard error of the difference) — don't assume equal variances. For the plotting package, inspect `/app/research-viz-pkg/research_viz/plot_utils.py`: it likely calls `matplotlib.use('Template')` or another non-rendering backend before `plt` is imported, which silently produces near-empty PNGs — switch it to a headless-safe backend like `Agg`, then `pip install -e /app/research-viz-pkg` and confirm `generate_boxplot` produces a plot with real file size (not just a few hundred bytes). Finally build the HTTP service (stdlib `http.server` is fine) checking the `X-Research-Token` header on every route, returning 401/403 if missing/wrong, serving `/stats` as rounded-to-4-decimal JSON and `/plot` with `Content-Type: image/png`; run it as a background process so it stays reachable after your turn ends.