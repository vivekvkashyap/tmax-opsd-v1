Read `/home/user/ticket-db/src/main.rs` and trace the WAL parsing loop precisely: it reads a 4-byte length prefix and then slices that many bytes as the record body, without checking that either slice actually fits within the remaining buffer — that's exactly where a truncated/corrupted trailing entry causes an out-of-bounds panic (`data[cursor..cursor+4]` or `data[cursor..cursor+len]` exceeding `data.len()`). Fix this by adding explicit bounds checks before each slice: if fewer than 4 bytes remain for the length field, or the declared length exceeds the remaining bytes, stop parsing immediately and return the records collected so far rather than panicking or using `.unwrap()`/`.try_into().unwrap()` on data that might not be present. Add a `#[test]` function named `test_corrupted_wal_recovery` that constructs a byte vector with the magic prefix, a couple of well-formed entries, and then a corrupted trailing entry (length claims more bytes than available), and asserts the parser returns only the valid entries and does not panic. Run `cargo test` to confirm it passes alongside existing tests. Finally rebuild and run `cargo run -- export > /home/user/recovered_tickets.jsonl` against the real corrupted `data/wal.dat` to produce the recovered output, and inspect it to confirm only the valid leading entries were recovered.
