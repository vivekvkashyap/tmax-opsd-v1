Scaffold with `cargo new /home/user/log_processor` and add `regex`, `unicode-normalization`, and `csv` as dependencies. Write one regex that captures the timestamp, level, message, and the raw metrics blob in one pass (e.g. `^\[(...)\] \w+: (.*) metrics:\{(.*)\}$`), then split the metrics blob on `;` and each pair on `=` to get key/value floats — don't assume a fixed number of metrics per line. For each line, compute the L2 norm as `sqrt(sum(v*v for v in values))` and keep the line only if that norm is strictly greater than 5.0 (be careful with the boundary case where the norm equals exactly 5.0 — it must be excluded). Normalize the message text with `unicode_normalization::UnicodeNormalization::nfkc()` and trim whitespace after normalizing, since ligatures/fraktur characters expand or map to different byte lengths than the raw string. For wide-to-long reshaping, emit one CSV row per metric key/value pair per retained line, in the header order `timestamp,metric_name,metric_value,message`; use the `csv` crate's writer so quoting/escaping is handled correctly rather than hand-formatting strings. Compile with `cargo build --release` and run the binary against `/home/user/raw_logs.txt` to produce the CSV, then spot-check row counts per timestamp against your own hand math.
