Do NOT run the original `/home/user/extractor.c` as-is — read it first to understand the vulnerable line (it writes to `fopen(filename, "wb")` using the raw archive-supplied path, which can contain `../` traversal). Fix it by extracting just the basename before opening for write, e.g. in C use something like `char *base = strrchr(filename, '/'); base = base ? base + 1 : filename;` and `fopen(base, "wb")` — apply this to every filename read from the archive, not just the one you expect to be malicious. Compile the fixed version, `mkdir -p /home/user/recovery`, `cd` into it and run the extractor there so all files land inside that directory; confirm no file appears outside it. For the WAL parsing, write a small C or Python struct-based parser: read 4-byte little-endian magic (compare to `0x4C415752`), 4-byte little-endian transaction ID, 4-byte little-endian payload length N, then N raw payload bytes, and loop over consecutive records until EOF, matching on transaction ID `999`. Write that record's exact payload bytes (no extra newline) to `/home/user/recovery/flag.txt` using binary-safe writes (`open(..., 'wb')` in Python, or `fwrite` in C) since the payload may not be printable ASCII throughout.