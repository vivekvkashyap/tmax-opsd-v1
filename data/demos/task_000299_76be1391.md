Work each artifact independently, then assemble the JSON report. (1) For the JWT: grep `server.log` for the request carrying an `Authorization: Bearer <jwt>` header, split the token on `.`, base64-decode the middle (payload) segment — pad it correctly since raw JWT base64 often lacks padding — parse the resulting JSON and pull out the `user` claim. (2) For CSP tampering: read `integrity.sha256` (format is `<hash>  <path>`), for each listed file recompute `sha256sum` and compare; the file whose computed hash doesn't match the manifest entry is the altered one. Open that file as JSON and inspect the `script-src` value for a domain/URL that doesn't belong with the legitimate directives (e.g. an added external host). (3) For privilege escalation: search `/home/user/forensics/bin/` for a file with the SUID bit set (`find . -perm -4000` or check `os.stat().st_mode & stat.S_ISUID`), then compute its SHA256 hex digest. Write all three findings into `/home/user/forensics/report.json` with exactly the three specified keys and no extras.
