Reverse-engineer the scoring constants by black-box probing before writing any Rust: run `/app/seq_align_scorer A A` to get the match score, `/app/seq_align_scorer A C` (or any mismatching single base) for the mismatch penalty, and `/app/seq_align_scorer A ""` (single base vs empty string) to isolate the linear gap penalty in one step — use several short probes with known expected relationships (e.g. two identical short strings vs strings differing by one base) to cross-check each constant independently rather than trusting one sample. Confirm the algorithm is a straightforward global (Needleman-Wunsch) DP with linear (not affine) gap cost by checking that longer all-gap probes scale linearly with length. Scaffold the Rust project with `cargo new --bin rust_scorer` in `/home/user/rust_scorer`, implement the same O(len1*len2) DP recurrence over `std::env::args()` (two sequence args), print only the integer score, and build with `cargo build --release` so the binary lands at `target/release/rust_scorer`. Write `test_scorer.sh` to generate >=50 random ACGT pairs of varying lengths, run both binaries, and `diff`/compare their stdout, fixing any mismatch by re-checking your derived constants rather than special-casing outputs.