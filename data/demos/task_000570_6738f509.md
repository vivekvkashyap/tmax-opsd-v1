Split this into two Go programs (or two clear phases) so each is independently testable. First, an index builder: read `/home/user/dataset_edges.csv` with `encoding/csv`, skip the header row, and build a `map[string][]struct{Target string; Cost int}` adjacency list, then marshal it to `/home/user/graph_index.json` with `encoding/json` — verify the key structure matches the example exactly (lowercase `target`/`cost` fields). Second, a shortest-path program: load `graph_index.json` back in, and run Dijkstra's algorithm (a simple priority-queue-based implementation via `container/heap`, or since costs are small and positive, even a straightforward relaxation loop works) from `Raw_Alpha` to `Final_Omega`, tracking both cumulative cost and the predecessor chain so you can reconstruct the actual path, not just the total cost. A common bug is only tracking distances and forgetting predecessors, making path reconstruction impossible, or using BFS (unweighted) instead of respecting edge costs. Write the result to `/home/user/path_result.json` with exactly the keys `source`, `target`, `total_time_hours` (int), and `path` (ordered array of dataset names from source to target inclusive) — double check the JSON field names and types match the schema in the task description precisely.
