Diagnose before rewriting: read `calculate_metrics.sh` and note that it does `$((bytes * 10000000000000000))` and accumulates in native Bash arithmetic, which is signed 64-bit — reproduce the bug directly at the shell prompt (e.g. `echo $((922337203685477581 * 10))`) to see it go negative and confirm the overflow threshold, rather than assuming. Fix it by moving the multiplication and summation out of Bash's `$(( ))` into a tool with arbitrary/wide precision — `awk` (careful: it uses floating point, so verify precision is sufficient for the required integer magnitude) or, more robustly, `bc` or `python3 -c` for exact big-integer arithmetic — while keeping the surrounding script as Bash per the requirement (call the arbitrary-precision tool from inside the `.sh` file). Process the CSV row by row (skip the header) accumulating `bytes * 10000000000000000` in that external tool, and make sure the final total is emitted as a plain integer string with no scientific notation or trailing decimal point/zeros. Run the fixed script, capture the exact output, write it verbatim to `/home/user/final_metric.txt`, and manually recompute the expected sum by hand (sum of bytes × the scale factor) to cross-check the script's output before finishing.
