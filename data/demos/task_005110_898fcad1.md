Do not try to reverse-engineer the oracle binary's internal math from its bytes — treat it strictly as a black-box CLI validator and call it as the task specifies: for each row, run `/app/telemetry_oracle <temp_celsius> <pressure_hpa> <humidity_pct>` (e.g. via `subprocess.run`) and check the exit code, keeping the row only if it's 0. Build the pipeline in the required order: read CSV, drop duplicate `(sensor_id, timestamp)` pairs keeping the first occurrence (`pandas.drop_duplicates` or manual seen-set), then filter with the oracle calls, then compute `pressure_hpa.mean()` and sample stddev (`ddof=1`) over the surviving valid rows only, then add `pressure_zscore = round((pressure - mean) / std, 4)` per row, guarding the std==0 case to output 0 instead of dividing by zero/NaN. Write the output CSV with header, preserving column order plus the new column. Test correctness by writing a couple of files with hand-verifiable clean/evil rows (matching the oracle's exit-code behavior you observe empirically) and confirming your script's output matches expectations exactly, including that fully-evil inputs produce header-only output.