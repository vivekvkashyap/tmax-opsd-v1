Before writing the emulator, probe `/app/legacy_chksum_bin` with several hand-crafted hex payloads covering each opcode (01, 02, 03, 04, 05, FF) and edge cases (truncated trailing operand, missing FF, multiple checksum checks) to confirm your understanding of the spec matches actual behavior. In `/home/user/bash_parser.sh`, parse the hex string two characters at a time using bash arithmetic on `((16#XX))` hex-to-decimal conversion, keep the accumulator masked with `& 0xFF` after every operation (add/subtract/shift-left/xor with 0xA5), and implement each opcode exactly: 01/02/05 consume one operand byte (bail cleanly if the string runs out before the operand is available, without emitting anything for that instruction), 03 shifts left by 1 then masks to 8 bits, 04 XORs with 0xA5, 05 compares to operand and prints OK/FAIL then continues, FF prints `FINAL: ` plus zero-padded uppercase 2-digit hex and exits immediately. Match stdout and exit codes exactly since the check is exact-string fuzz-equivalence across 1000 randomized payloads. Then write `/home/user/benchmark.sh` to time both implementations over 50 random valid hex payloads and append the `Legacy: Xs | Bash: Ys` line to the results log.
