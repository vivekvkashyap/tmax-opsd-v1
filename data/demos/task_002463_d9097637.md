Implement the PRNG exactly as given — a single global `unsigned int my_seed`, updated in place by `my_rand()` — and reset `my_seed = 42` exactly once, immediately before the bootstrap loop starts (not per-iteration). Read all 100000 floats into an array first. Inside each of the 1000 iterations: build the resampled array using `data[my_rand() % N]` for each of the N slots in order; compute `mean_A` by summing the resampled array left-to-right into a `float` accumulator (not `double` — the precision-loss demonstration depends on this) and dividing by N; compute `mean_B` by sorting a copy (or the same array, since order no longer matters after mean_A is captured) by ascending absolute value using `qsort` with a comparator based on `fabsf`, then summing sequentially into a `float` accumulator; store `diff = mean_A - mean_B`. After all iterations, `qsort` the 1000 diffs ascending and read out indices 24 and 974 for the CI bounds, printing with `%f` in the exact `[lower, upper]` format. Compile via `run_analysis.sh` with `gcc -O3 -lm`, and be careful that `-O3` doesn't let the compiler fuse/reorder the float sums in a way that changes the accumulation order (keep it as a simple sequential loop).
