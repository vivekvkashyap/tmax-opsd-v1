Start by fixing the vendored cJSON build before writing any application code: inspect `/app/cJSON-1.7.15/Makefile` line by line for a mistyped tool name (compare `CC` against what's actually installed, e.g. `which gcc`), correct it, then `make` to produce the static/shared library and confirm the headers are present. For the C program, treat this as a database query plan problem: load `papers.csv` into an array indexed by id (id-to-index array works well since ids look dense), load `citations.txt` and for each edge check both endpoints' years are >= 2015 using O(1) array lookups (never rescan the CSV per edge), incrementing an in-degree counter array — this avoids the nested-loop join that would blow the 0.5s budget. After computing filtered in-degree for the 2015+ subset, do a single sort (qsort with a comparator on count desc, then id asc) and take the first 20. For keywords, parse metadata.json once with cJSON into a lookup structure (array or hash by id) rather than re-parsing per paper. Link cJSON into `analyze.c` at compile time (`-I` for headers, `-L`/`-l` or direct .a link), then measure wall time with `time` to confirm you're under threshold before finalizing output formatting.
