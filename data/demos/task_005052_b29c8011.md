Start by decoding the Morse code audio at /app/intercepted_signal.wav: load it (e.g. `scipy.io.wavfile` or `wave`), detect tone-on/tone-off segments by amplitude thresholding, classify each tone as dot or dash from its relative duration, group symbols into letters using silence-gap length, and translate via a standard Morse table — don't guess the token, derive it from the actual signal timing. Uppercase the result and write it to /home/user/token.txt. Then patch server.py: require an `X-Auth-Token` header on /upload that matches the decoded token exactly, returning 401 if missing/wrong; sanitize the incoming filename with something like `os.path.basename()` (or `werkzeug.utils.secure_filename`) so `../` path segments can never escape /home/user/uploads/, and save into that directory using only the sanitized name; immediately after `file.save(...)`, call `os.chmod(save_path, 0o600)`. Ensure /home/user/uploads/ exists, then launch the Flask app bound to 127.0.0.1:8000 as a background process so it's live when the verifier's HTTP requests arrive — test the auth-missing, auth-wrong, valid-upload, and traversal-attempt cases yourself with curl first.