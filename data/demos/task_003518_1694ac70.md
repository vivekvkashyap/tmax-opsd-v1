Append (don't overwrite) `export TZ=Pacific/Honolulu` and `export DEVICE_ID=CAM_EDGE_01` to `.bash_profile`, and write edge.conf with the two `KEY=VALUE` lines as given. For video analysis, extract frames as raw video with ffmpeg and compute per-frame whether every pixel is exactly (0,0,0) black — a robust approach is `ffmpeg -i /app/iot_feed.mp4 -f image2pipe -pix_fmt gray -vcodec rawvideo -` piped into a small script that reads each frame-sized chunk and checks `max(chunk) == 0`; record the 0-indexed frame numbers where this holds, in order, one per line. In log_processor.c, read DEVICE_ID via `getenv` (default "UNKNOWN" if NULL), parse edge.conf with simple line-prefix matching for THRESHOLD (atoi) and ADMIN_EMAIL (strip trailing newline), then loop reading `%ld %d` pairs from stdin with `scanf`, converting each timestamp with `localtime()` (relying on the TZ env var and calling `tzset()` first) and `strftime` with format `"%Y-%m-%d %H:%M:%S"`. Since correctness will be checked against a hidden oracle via many fuzzed timestamp/value lines, match the two output line formats character-for-character, including spacing and punctuation. Finally write edge_cron with a `*/15 * * * *` line running the exact log_processor command with stdin/stdout redirection as specified.
