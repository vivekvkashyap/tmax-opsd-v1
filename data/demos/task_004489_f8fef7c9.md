Sequence this as fetch, then read, then implement, then test against the corpora. Start `/app/start_server.sh` in the background, then `curl -O http://127.0.0.1:8080/data.tar.gz` and `tar -xzf data.tar.gz -C /home/user/data/` to get the clean/ and evil/ subdirectories. For the calibration constraints, run OCR on the label image (e.g. `tesseract /app/sensor_label.png stdout`), then read the extracted text yourself to find the "Calibration Epoch" ISO-8601 timestamp and the "Max RPM" float — don't hardcode guessed values, actually parse what OCR returns, and account for OCR noise/typos by inspecting the raw output before trusting it. In validate.py, parse the input JSON, require both `timestamp` and `rpm` fields to exist, parse the timestamp with `dateutil.parser.isoparse` (or similar) which handles `Z`/`+02:00`/`-05:00` offsets, convert to UTC with `.astimezone(timezone.utc)`, and compare against the epoch you extracted (use `>=`, inclusive), then check `0.0 <= rpm <= max_rpm` (also inclusive at the max boundary per the clean example that sits exactly on Max RPM). Wrap everything in try/except so any parse failure or missing field triggers `sys.exit(1)`, and run your script against every file in both clean/ and evil/ directories to confirm 100% correct classification before finishing.
