Iterate only true directories directly under /home/user/ci_builds (`for d in .../*/; do [ -d "$d" ] || continue; ...`) and measure size in bytes with `du -sb "$d" | cut -f1` (not `-sh`, which is human-readable and unusable for numeric comparison). While a directory's size exceeds 40000000, repeatedly find the single oldest regular file with `find "$d" -type f -printf '%T@ %p\n' | sort -n | head -1`, delete it, append the exact log line, and recompute the directory's total size before deciding whether to delete another — don't precompute a static sorted list up front, since deletions must be checked incrementally against the shrinking total. Restrict deletion to `-type f` only, never remove directories, and wrap each `rm` in an exit-code check so one failure doesn't abort the whole script. Make the script executable and actually execute it so quota_cleanup.log and the on-disk state reflect the cleanup, not just the script's existence.
