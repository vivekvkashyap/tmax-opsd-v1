Design the SQLite schema before writing the query. Parse `/home/user/interactions.csv` (5 columns, no header) and normalize it into a `nodes` table (unique entity name + type) and an `edges` table (source, target, relation) — dedupe nodes that appear as both a source and a target elsewhere in the file. Add indexes that actually serve the multi-hop lookup pattern you'll query, e.g. an index on `edges(relation, source)` and one on `edges(relation, target)` (or a composite covering `(source, relation, target)`), so the Drug→Gene→Disease traversal doesn't require full scans; you can confirm indexes exist afterward with `SELECT name FROM sqlite_master WHERE type='index';` or `EXPLAIN QUERY PLAN`. For the actual query, use a parameterized (`?`-placeholder) SQL statement — never string-interpolate the disease argument — that joins edges e1 (relation='targets', target type Gene) to edges e2 (relation='associated_with', source = e1.target, target = the given disease name) and selects distinct e1.source names where e1.source's node type is Drug. Sort results alphabetically and write one name per line to `/home/user/treatment_candidates.txt`. Run the script with `"Asthma"` as the argument and inspect the output file to confirm it only contains drugs reachable through the exact two-relation chain, not through the wrong relation type.