Treat this as two independent subproblems chained together. First, recover the spoken expression from `/app/equation.wav` by actually running a transcription tool over the audio (e.g. a Python script using an available speech-to-text library) rather than guessing — listen to/inspect the transcription output carefully, since spoken math often uses words like "plus", "times"/"multiplied by", and "opening/closing parenthesis" that you must map to symbols (+, *, (, )) and spoken numbers (including decimals like "point") that you must parse into digits. Don't hardcode a guessed formula; derive it from what the transcription actually says. Second, write `evaluator.cpp` implementing a real recursive-descent parser producing an AST (e.g. classes/structs for Number, BinaryOp nodes) with correct operator precedence (`*`/`/` before `+`/`-`) and parenthesis handling, then an `eval()` function that walks the tree — a naive shell-out to a calculator or direct string eval would defeat the stated requirement of implementing your own AST/parser. Compile with `g++`, run it on the recovered expression, and write the floating-point result to `/home/user/result.txt`.