Start by OCR'ing `/app/weight_params.png` (e.g. `tesseract /app/weight_params.png -` or a Python OCR library) to recover the damping factor and the year-difference decay-weight rule — do not guess these values, read them off the image output. Then inspect the SQLite schema (`sqlite3 /home/user/citation_data.db ".schema"`) before writing `extract_subgraph.sh`: use a window function like `ROW_NUMBER() OVER (PARTITION BY source_id ORDER BY target_year DESC)` joined against `papers` to keep only the top-5 most-recent outgoing citations per source paper, and dump to `subgraph.csv`. In `compute_centrality.py`, build a directed graph (e.g. networkx `DiGraph`) from that CSV, assign each edge a weight per the OCR'd decay rule based on `abs(source_year - target_year)`, and run `nx.pagerank(G, alpha=<OCR'd damping factor>, weight='weight')`. Write results as `node_id,pagerank_score` to `/home/user/pagerank_results.csv`, covering every node in the graph (isolated/no-in-edge nodes still need a row) since missing nodes will be treated as zero and inflate your MSE against the hidden reference. Chain the two scripts (bash calling the python step) so the whole pipeline runs end-to-end from one command, and double-check the PageRank weight direction/normalization matches standard networkx conventions.
