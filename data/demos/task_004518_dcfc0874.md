Diagnose before rewriting: read `/app/.env` and `/app/worker.py` side by side and look for a mismatch between the Redis connection pool size and the thread pool size — if `ThreadPoolExecutor` spawns far more worker threads than the Redis client's connection pool allows, and the Redis calls block indefinitely with no timeout, excess threads pile up waiting forever for a free connection (a starvation deadlock, not a classic lock deadlock). Fix it from both ends: raise `REDIS_MAX_CONNECTIONS` in `.env` to comfortably cover the thread pool (or shrink `max_workers` to fit the pool), and add explicit `socket_timeout`/`socket_connect_timeout` to the Redis client so threads fail fast and retry instead of hanging forever. Also check for any blocking `BRPOP` call without a timeout in the worker loop and give it one. After fixing, restart the stack via `/app/start_services.sh` and sanity-check manually with a handful of concurrent curl requests before writing `/app/verify_throughput.py`: fire 500 concurrent POSTs to `/enqueue` (e.g. `ThreadPoolExecutor`), poll a Redis counter until it reaches 500 or a timeout, compute `500/elapsed`, and write that raw float to `/home/user/throughput_result.txt`.
