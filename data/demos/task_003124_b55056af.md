Handle the three parts independently and validate each with real commands rather than assumptions. Archive: iterate the tar's members yourself (e.g. Python's `tarfile`) and, for each entry, resolve its target path relative to the extraction root and confirm the resolved absolute path still starts with that root before extracting — reject/skip anything using `../` traversal that would escape `/home/user/extracted_dataset/`; don't rely on the default extractall, which doesn't guard against this. Count only the files you actually accept. Video: use `ffprobe -select_streams v:0 -count_packets -show_entries stream=nb_read_packets` (or an equivalent frame-counting approach) against `experiment_feed.mp4` to get the real frame count — don't hardcode a guess. Filter script: parse the bracketed timestamp and the numeric status code out of the single argument (careful with `$1` containing spaces/brackets — quote properly), branch on the 200-299 / >=400 / else ranges, append a space and the frame count you obtained from ffprobe, and wrap the whole logic in a subshell using `flock -x 200 ... 200>/tmp/filter.lock` so concurrent invocations don't interleave output. Since grading fuzzes with thousands of random status codes, test your boundary conditions (199, 200, 299, 300, 399, 400) explicitly.