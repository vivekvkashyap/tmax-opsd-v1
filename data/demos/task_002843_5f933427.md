Fix this project in dependency order and validate each step before moving to the next. First compile the C source into a shared lib: `gcc -shared -fPIC -o /home/user/project/clib/libc_lib.so /home/user/project/clib/c_lib.c`. Then diagnose the Rust link failure by reading `build.rs` — it emits `cargo:rustc-link-lib=c_lib` but never tells the linker where to search, so add `println!("cargo:rustc-link-search=native=/home/user/project/clib");` before the existing line. Next inspect `get_combined_message` in `lib.rs`: it builds a `CString` and returns `c_string.as_ptr()`, but the `CString` is dropped (and its buffer freed) the moment the function returns, leaving a dangling pointer — fix this by transferring ownership to the caller with `c_string.into_raw()` instead, which intentionally leaks the buffer to C/Python (acceptable here since nothing calls a matching free). Build with `cargo build --release` and confirm `librust_wrapper.so` appears under `target/release`. For the Python test, load the `.so` via `ctypes.CDLL`, declare `restype = ctypes.c_char_p` on the function, decode the bytes, merge with the mocked `["Zebra","Apple","Mango"]` list via `unittest.mock`, sort, and diff against `/home/user/project/expected.txt` lines using `difflib.unified_diff`, writing the result to `diff.txt`.