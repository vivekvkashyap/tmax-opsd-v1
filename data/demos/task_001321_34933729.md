Build and test each phase standalone before wiring them together via the WebSocket server. For `translator.py`, implement `translate(filepath)` as a genuine line-by-line FSM: states like AWAIT_TARGET → AWAIT_BEGIN → IN_BODY → DONE, validating the strict sequence (`TARGET <lang>`, `BEGIN_PSEUDO`, statements, `END_PSEUDO`); parse each pseudo-statement type (`PRINT "<string>"` vs `PRINT <var>` vs `SET_VAR <name> <value>`) by checking for a leading/trailing quote to distinguish literal strings from variable references, and emit the correct target-language line per the given translation table, joining output lines with newlines. Test this function directly against a couple of hand-written `.polybuild` files and print the returned tuple before touching networking. For `server.py`, use `websockets.serve` on `localhost:9333`, parse incoming JSON, call `translate()`, write the translated code to a temp `.py`/`.sh` file, run it via `subprocess.run(..., capture_output=True, text=True)`, and reply with the JSON shape specified, stripping the captured stdout. Test the server manually with a small Python websocket client before finalizing. `ci_setup.sh` should `pip install websockets`, launch `python3 server.py &`, `sleep 2`, then `exit 0` — confirm the server is actually listening (e.g. via a quick connection attempt) before considering the phase done.
