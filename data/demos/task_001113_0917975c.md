Structure this as validate -> reshape -> join -> format, and use real parallelism (a `ProcessPoolExecutor`/`multiprocessing.Pool` mapping one worker per regional CSV, or `parallel` in bash) to read the three survey files concurrently rather than sequentially. For validation, apply both constraints strictly per row before keeping it: `emp_id` must match a regex like `^EMP\d{5}$` (exactly 8 chars), and every one of the four score columns must be present and parse as an integer in [1,10] inclusive — a missing/blank score, a non-integer, or an out-of-range score means drop the whole row (don't try to impute or clamp). Melt each surviving wide row into four long rows (one per question column) using the exact column name as the `question` value. Then inner-join the combined long data against `/home/user/metadata.csv` on `emp_id`, discarding survey rows with no metadata match and metadata rows with no survey match. Finally write `/home/user/clean_long_survey.csv` with header `emp_id,hire_year,office_city,question,score` and sort rows first by `emp_id` ascending, then by `question` ascending (string sort, not column-declaration order) — double-check your sort is a true two-key sort, not just relying on insertion order from the melt step.