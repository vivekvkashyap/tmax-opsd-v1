Do the scan in the order the report needs it. First find world-writable files with `find /home/user/app_audit -type f -perm -o+w` (this matches "others" write bit regardless of owner/group bits) rather than checking only mode `777`. Second, for each match compute `sha256sum <file>` (or hashlib in Python) and store the mapping of absolute path to hex digest exactly as returned — don't re-encode or truncate it. Third, load `/home/user/app_audit/network_policy.json` with a JSON parser and iterate the `csp` array, testing each string for the literal substring `default-src`; any entry lacking it goes into the insecure list, preserving original string content and order. Finally assemble the two collections into a single JSON object matching the exact key names and nesting shown in the task (`vulnerable_files` as an object, `insecure_csp` as an array) and write it with a proper JSON serializer (e.g. `json.dump`) to `/home/user/audit_report.json` so formatting/escaping is correct — avoid hand-building the JSON string, which risks quoting bugs.