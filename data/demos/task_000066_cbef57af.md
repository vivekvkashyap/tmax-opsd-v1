Diagnose the broken `Makefile` at `/app/vendored/xsv-0.13.0/Makefile` by running `make` and reading the error: check for indentation issues (recipe lines must start with a literal TAB, not spaces) and a typo in the build command (should be `cargo build --release`); fix both so `build:` compiles the binary to `target/release/xsv`. Once it builds, reproduce the crash: run `./target/release/xsv stats <file>` against each file in `/app/raw_feeds/`, note which panic (nonzero exit, `unwrap` in the trace), and isolate the offending value with delta debugging — bisect a crashing file's rows (split the CSV in half repeatedly, test each half) until you find the minimal row/field that reliably triggers the panic. Once you know the malicious pattern, write `/home/user/sanitize_feed.sh` as a filter (e.g. `grep -v`) that reads the CSV named in `$1`, streams every row except matches to stdout, and exits 1 if the file is missing or empty — preserving the header and valid CSV for the rest.
