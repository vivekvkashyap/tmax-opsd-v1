Start by reading the image: run OCR (e.g. `tesseract /app/motif_spec.png stdout`) to recover the two-state Gaussian mixture's means, standard deviations, weights, and the Wasserstein distance threshold printed in the manual snippet — parse these values out of the OCR text rather than assuming numbers. Then build the pipeline: load raw_signal.txt as a float array, form overlapping windows (size 100, stride 10) and stack them into a matrix; mean-center each window (subtract its own mean) before running truncated SVD (e.g. `numpy.linalg.svd` or `scipy.sparse.linalg.svds`), keeping only the top 3 singular components to reconstruct a denoised version of each window (then optionally add the window mean back before comparing distributions, or account for the centering consistently). For each denoised window, treat its values as an empirical sample and compute the 1-Wasserstein distance to a reference sample drawn from (or the CDF of) the theoretical two-state Gaussian mixture you extracted (e.g. `scipy.stats.wasserstein_distance` against a large synthetic sample from the mixture, or a direct sorted-CDF integral). Flag windows whose distance is strictly below the recovered threshold, collect their starting line index, dedupe/sort ascending, and write one index per line to /home/user/motif_matches.csv. Sanity-check against a few known-injected-motif regions before trusting the F1 score.