Work through this as a systems-integration release, in order:

1. Build the shared library with proper ABI versioning: compile filter.c with `gcc -shared -fPIC -Wl,-soname,libaudiofilter.so.2 -o libaudiofilter.so.2.0.1 filter.c`, place it in `/home/user/lib/`, then create the chain of symlinks (`libaudiofilter.so.2` -> the versioned file, `libaudiofilter.so` -> the soname link) so both the linker and runtime loader resolve correctly.

2. Optimize `apply_filter` by first reading the inner loop closely: it repeats a fixed number of times per element without depending on loop state across iterations, so it collapses algebraically into a single closed-form multiply-add per sample — replace the nested loop with that direct arithmetic expression, keeping bit-for-bit equivalent output.

3. Rewrite main.go to split the audio buffer into >=4 contiguous chunks, launch one goroutine per chunk that calls the cgo `apply_filter` on its own slice, and synchronize completion with a `sync.WaitGroup` (or a done-channel per worker) before writing the output WAV.

4. Compile the Go binary with cgo LDFLAGS pointing at your lib dir and an rpath of `$ORIGIN/../lib` (e.g. `-ldflags "-r $ORIGIN/../lib"` or linker `-Wl,-rpath,$ORIGIN/../lib`) so it finds the .so at runtime without LD_LIBRARY_PATH.

5. Assemble `bin/` and `lib/` into the exact tarball layout specified, then time-test locally with `./bin/audiotool` before submitting to confirm both correctness and speed.
