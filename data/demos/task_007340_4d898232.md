Work through the four sub-tasks in order since later ones depend on earlier findings. First, `chmod 755 /home/user/bin/legacy_monitor` to strip setuid/setgid while keeping owner rwx and group/other r-x. Second, find the hardcoded cert path by running `strings /home/user/bin/legacy_monitor | grep -i crt` (or objdump/readelf on the rodata section) rather than guessing — it will reveal a path under a hidden (dot-prefixed) directory; create that directory tree and generate the cert there with `openssl req -x509 -newkey rsa:2048 -keyout <path key, if needed> -out <exact path> -days 365 -nodes -subj '/CN=test'`. Third, in `jwt_auth.cpp`, parse the header JSON for the `alg` field (a simple case-insensitive substring/regex check on `"alg"` value is fine here) and return `false` when it equals `none` (any case) or is absent — then recompile exactly with the given `g++` command and test both `{"alg":"none"}` and `{"alg":"NONE"}` return INVALID. Fourth, hand-construct the forged JWT: base64url-encode (no padding, `+`→`-`, `/`→`_`) the header JSON and the payload JSON yourself, join as `header.payload.` with a trailing empty signature segment, and write that exact string with no newline to the output file.
