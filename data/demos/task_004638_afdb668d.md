Work through this in order: first fix access, then fix the Rust code, then build and measure. For permissions, use `setfacl -m u:user:r-x /app/secure_logs` and `setfacl -m u:user:r-- /app/secure_logs/router.log` (plain chmod on someone else's file won't work — ACLs are required), then `export ROUTER_LOG_PATH=/app/secure_logs/router.log` both in the current shell and appended to `/home/user/.bashrc`. In `src/main.rs`, search for a byte-by-byte read loop (e.g. `file.read(&mut buf[..1])` in a loop building a `String`) — replace it with `std::io::BufReader::new(file).lines()` for line-based buffered iteration; this is the actual bottleneck causing the multi-second runtime. Also find the `panic!` triggered on lines containing "TIMEOUT" and replace it with normal counter/logic handling instead of aborting. Confirm the code reads its path from `std::env::var("ROUTER_LOG_PATH")` rather than a hardcoded fallback. Add code to write `OK` to `/tmp/analyzer.health` on startup. Build with `cargo build --release` (release mode is required to hit the performance bar), then run the binary so it writes `/tmp/analysis.json`, and time it yourself to sanity-check it's well under the threshold before considering it done.