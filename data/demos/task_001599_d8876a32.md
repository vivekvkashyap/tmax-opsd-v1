Work this end-to-end pipeline in order: first get services up via `/app/start.sh` and inspect `/app/nginx/nginx.conf` — a common break is `proxy_pass` pointing at the wrong upstream port, so trace the path Nginx(8080) -> Flask(5000) -> Redis(6379) and fix any mismatched port/route before touching code. Second, reverse-engineer `/app/crypto_logger.pyc`: since the source is gone, use a Python bytecode decompiler (e.g. `uncompyle6`/`decompyle3`/`pycdc`, matching the 3.10 bytecode version) or disassemble with `dis`/`marshal` to recover the encryption function, then confirm the algorithm empirically by encrypting/decrypting a known plaintext against a captured Redis entry. Do not guess the key/IV scheme blind — read it out of the recovered logic. Third, write `/home/user/processor.py` to `BLPOP`/loop-pop `csp_audit_logs`, decrypt each blob with the recovered cipher parameters, parse the JSON CSP report, and pull the injected string out of `document-uri`/`blocked-uri` (these fields may be URL-encoded — decode them faithfully so payloads aren't truncated). Write one JSON object per line to `/home/user/audit_trail.jsonl` matching the exact schema, keep processing continuously, and leave all services running since accuracy is graded by comparing extracted payloads against a hidden ground-truth set (F1 >= 0.95).