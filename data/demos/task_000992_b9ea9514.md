Sequence this as: start the audit server first (`python3 /home/user/ws_server.py &`) and confirm it's listening on 8080 before testing your C program against it. In C, implement Fletcher-16 exactly as specified — iterate every byte of the file (including newlines) through `sum1 = (sum1 + b) % 255; sum2 = (sum2 + sum1) % 255`, and combine as `(sum2 << 8) | sum1`; read the file in binary mode so you don't accidentally strip or translate bytes. Compute this checksum on `/home/user/policy.conf` before patching (store as `old_checksum`). Apply the patch by shelling out to the system `patch` utility via `system()`/`popen()` against `/home/user/update.patch`, then check the command's exit status before trusting the result. Recompute the same Fletcher-16 function on the now-patched file for `new_checksum` — reuse the identical checksum routine so there's no drift between the two calls. Build the JSON message string dynamically with the two computed integers (never hardcode), and send it by piping through `popen("websocat ws://127.0.0.1:8080", "w")` (or `system("echo '...' | websocat ...")`). Verify success by checking `/home/user/audit_log.json` for your message after running.
