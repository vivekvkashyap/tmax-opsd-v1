Start with OCR: run `tesseract /app/system_specs.png stdout` (try different `--psm` values if text is garbled) to recover the ODE equations, rate constants, and the conservation law — verify what you extracted makes sense (equations should be self-consistent, e.g. dC/dt should balance the loss terms in dA/dt and dB/dt) before hardcoding them into your script. Use the provided `training_good/` and `training_corrupted/` examples to validate your evaluator as you build it, rather than guessing thresholds blindly. In `evaluate_sim.py`: (1) check the conservation law holds within tolerance at every row of the CSV; (2) use `scipy.integrate.odeint`/`solve_ivp` with the OCR'd rate constants and the CSV's first-row initial conditions to regenerate the expected trajectory at the same timestamps; (3) compute residuals (CSV value minus simulated value) per species; (4) statistically characterize the residual distribution — e.g. fit/test for zero-mean Gaussian shape with the known noise scale (Shapiro-Wilk normality test, or compare sample mean/std against expected, or a KS-test against a uniform alternative) and flag discontinuities via large single-step jumps. Reject (exit 1) if conservation drifts, residuals are non-Gaussian/biased, or jumps are detected; accept (exit 0) otherwise. Iterate thresholds against the training sets until both good and corrupted files classify correctly.
