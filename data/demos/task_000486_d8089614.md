Get the vendored package building and correct before writing any detector logic — a build you haven't validated will silently make every downstream test fail. Run `go build ./...` (or `go test ./...`) inside `/app/vendor/go-graphdoc` first to see the actual compiler/runtime error, then grep `parser.go` for suspicious environment-variable gating (e.g. `os.Getenv(...)` conditionals that short-circuit parsing) and remove/neutralize any check that blocks normal operation regardless of environment — don't just set the env var as a workaround, since the harness running your detector later won't have it set. Separately, look for a regex used to validate field/identifier names (`regexp.MustCompile(...)`) and test it against real field names from the sample queries in `/home/user/corpus/` — if legitimate field names (e.g. ones containing digits) fail to match, the character class is too restrictive and needs broadening. Once `go build` succeeds cleanly, write `detector.go` to walk the input directory's `.json` files, call the fixed package's pipeline parser on each, and apply the two rejection rules (restricted field names anywhere in projections; `$lookup`/`$graphLookup` stages with `maxDepth` absent or >5) — test it first against the labeled `corpus/clean` and `corpus/evil` directories individually to confirm 100% accuracy before generating the final combined `results.log`.