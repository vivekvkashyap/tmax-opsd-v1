Build this as a Cargo project with clearly separated steps you can test independently. For `locations.txt`, don't try to parse full sentences — use a regex over the whole file to find each `S<digit>` sensor-id token paired with the nearest following `(x.x, y.y)` coordinate pattern (e.g. capture `S\d+` and `\(-?\d+\.\d+,\s*-?\d+\.\d+\)` separately per line, or scan line-by-line since each sensor's line contains exactly one id and one coordinate pair). For `readings.csv`, build a per-sensor map from hour to temperature, then for hours 0..9 forward-fill: track the last-seen value as you iterate hours in order, carrying it forward whenever the current hour is absent (hour 0 is guaranteed present as the seed). Standardize each sensor's 10-value profile independently using population std dev (divide sum-of-squared-deviations by 10, not 9) — compute mean and std once per sensor before computing per-hour z-scores. For every alphabetical pair A<B, compute Euclidean distance between their 10-dimensional z-score vectors (profile distance) and separately between their raw (x,y) coordinates (spatial distance); track the minimum profile distance across all pairs. Round both output distances to exactly 4 decimal places with Rust's `{:.4}` formatting, and write the JSON with the exact key names and ordering shown in the task, including the two sensor ids in the pair that achieved the minimum.
