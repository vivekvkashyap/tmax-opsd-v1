Build the pipeline in the exact stage order given, checking intermediate shapes at each step. Merge each CSV (`train_metadata.csv`/`test_metadata.csv`) with its per-experiment JSON metrics on `exp_id`, then stack train+test together before doing PCA — the standardization and PCA must fit on the combined N×10 loss-curve matrix (column-wise zero-mean/unit-variance via `StandardScaler`, then `PCA(n_components=2, random_state=42)`), not train-only, since the task explicitly says "all experiments (train and test combined)". Compute pairwise cosine similarity on the resulting 2D PCA vectors, zero out or ignore the diagonal (self-similarity) before taking the argmax, and sort the winning pair's IDs alphabetically. For modeling, one-hot encode `optimizer` with `drop_first=True` (alphabetical drop), assemble the feature matrix in the specified column order, then run `GridSearchCV(Ridge(random_state=42), {'alpha':[0.1,1.0,10.0,100.0]}, cv=5)` on the training rows only, predict on test rows with the best estimator, and round predictions to 4 decimals. Write `best_alpha`, the sorted pair, and the rounded per-exp_id predictions to `/home/user/report.json` matching the exact structure shown.
