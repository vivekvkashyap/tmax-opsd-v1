Do the reshape and the numeric tool as two clean, independently-testable stages. For step 1, parse `raw_data.json` with a JSON-aware tool (Python's `json` module or `jq`), not naive text splitting — extract `pos` and `val` for every element in array order and write them as two space-separated columns, one row per position, no header. For step 2, load the 1000 values into a fixed-size array indexed by position, split into 10 bins of 100 up front, then for each bin compute the empirical distribution by normalizing the 100 raw values to sum to 1, and compute TVD against a uniform 1/100 distribution using the exact formula given — sum absolute differences, halve. Apply the >0.15 refinement test to every original bin using its own bin's TVD (not the refined children), and only ever split once (no recursion into the halves). For the trapezoidal integral, always sum over the *original unnormalized* `val` array (not the normalized probabilities), using consecutive-pair averaging with Δx=1 across the exact inclusive start/end range of each final bin — get the split-bin boundaries exactly right (a bin 0-99 splits into 0-49 and 50-99, both inclusive). Format each output line as `start end integral` with the integral fixed to 2 decimals, and preserve the ascending order of bins in the log.
