Start by OCR'ing `/app/incident_report.png` (`tesseract` or similar) to recover the PORT and Z_THRESHOLD values rather than guessing them — parse the labeled lines out of the OCR text. Build the service (Flask/FastAPI) binding to `127.0.0.1:<recovered-port>`. For `/ingest`: for each log entry, count ASCII vs non-ASCII characters in `message` (e.g. `ord(c) < 128`) to set `is_intl`, then look up the IP in `/app/meta.csv` (default `UNKNOWN` if absent) and store the enriched record (timestamp, region, is_intl) in memory. For `/anomalies`: bucket stored records into 1-minute windows per region (truncate the timestamp to the minute, e.g. `timestamp[:16]+':00'` or floor via datetime), sum `is_intl` counts per window, then for each region compute mean/stddev of its window counts across ALL windows for that region and flag windows where `count > mean + Z_THRESHOLD * stddev` — treat stddev==0 as "no anomalies" to avoid a spurious flag on constant data. Get the arithmetic right by hand-tracing a small example, then launch the service in the background (`&` / nohup) and confirm with `curl` that both endpoints respond before finishing.