First diagnose the vendored package: read `/app/awk-stat-1.1/Makefile` and notice it concatenates `src/header.awk` with a nonexistent file instead of the real `src/core.awk` present in `src/` — fix that one line, then `cd /app/awk-stat-1.1 && make` and manually test `bin/awk-stat` on a tiny sample CSV to confirm it prints `ColumnName Variance` lines before building anything on top of it. In `feature_pipeline.sh`, take `$1` as the input CSV, run it through the repaired `awk-stat` to get the per-feature variances, then rank features with `sort -k2,2nr -k1,1` (numeric descending on variance, alphabetical ascending as tiebreak) and take the top 2 with `head -n 2`. Map the selected feature names back to their original column positions (don't just take them in variance-sorted order — the output must preserve original column order among ID, Label, and the two chosen features), build a `cut -f` field list of `1,2,<idx1>,<idx2>` sorted numerically, then emit the header via `cut` on line 1 and the data rows via `tail -n +2 | sort -t, -k1,1n | cut ...`. Make the script executable with `chmod +x`, and test it against a couple of hand-built CSVs (including a variance tie) to confirm byte-exact output before trusting it, since the grader fuzz-tests for bit-exact equivalence against a hidden reference pipeline.
