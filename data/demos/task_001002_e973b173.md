Set all five thread-limiting environment variables (`OMP_NUM_THREADS`, `OPENBLAS_NUM_THREADS`, `MKL_NUM_THREADS`, `VECLIB_MAXIMUM_THREADS`, `NUMEXPR_NUM_THREADS`) via `os.environ[...] = "1"` at the very top of the script, strictly before `import numpy`/`pandas`/`sklearn` — importing first and setting env vars after has no effect since BLAS threading is fixed at import time. For each artifact's CSV, compute RMSE on the full data, then run exactly 200 bootstrap iterations using `sklearn.utils.resample(df, replace=True, random_state=i)` for `i` in `range(200)`, recomputing RMSE each time; take `rmse_mean` as the average of those 200 values and `rmse_lower`/`rmse_upper` as `numpy.percentile` at 2.5 and 97.5 with default interpolation — do not shortcut with a closed-form CI formula, the exact iteration count and random_state sequence matter for reproducibility. Join these engineered features plus `training_time` from the SQLite `experiments` table by `artifact_id`. Split rows into labeled (`is_anomalous` not null) and unlabeled; fit `LogisticRegression(random_state=42)` on the labeled subset using the four listed features, predict on the rest, and for labeled rows keep their original value verbatim (do not re-predict them). Round `rmse_lower`/`rmse_upper` to 4 decimals, sort by `artifact_id`, and write the exact column set to `/home/user/audit_results.csv`.
