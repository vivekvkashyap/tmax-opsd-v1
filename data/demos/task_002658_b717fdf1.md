Build this as a straightforward pandas pipeline, being careful about two subtleties. Load both CSVs with `pandas.read_csv`, select only numeric columns (`select_dtypes(include=[np.number])`) before differencing, since non-numeric/id columns would break elementwise subtraction. Compute `difference = run1_numeric - run2_numeric`, then `max_abs_diff = difference.abs().values.max()` and compare it strictly against `1e-5` for `is_reproducible`. For the covariance step, call `difference.cov()` to get the covariance matrix, then scan only the off-diagonal (distinct-column) entries for the maximum absolute value — don't let the diagonal (variance) entries win, since they're typically much larger than off-diagonal covariances and would give a wrong answer if not excluded. When you find the winning pair, sort the two column names alphabetically before writing them to `highest_cov_pair`, and pull the actual signed covariance value (not its absolute value) for `highest_cov_value`. Write the JSON with Python's built-in `float()`/`bool()` casts around numpy scalars so `json.dump` doesn't choke on numpy types, matching the exact schema given.
