Do not try to read or run `/app/oracle_signer` as a shortcut to the algorithm — treat it strictly as a black-box equivalence target you test against, not a source to copy from. First fix the build: inspect `pkg/signer` and `pkg/hashmath` in `/app/vendored/api-signer` to find the mutual import (one package importing a type/config from the other while being imported back) and break the cycle by moving the shared type into its own package (or into whichever of the two doesn't need to import the other), then confirm `go build ./...` succeeds. Second, fix the algorithm: this is a polynomial rolling hash of the form `hash = sum(s[i] * base^i) mod prime`; the current constants are wrong (a modulus and a base that don't match the oracle) — since you can only observe the oracle's outputs, run it on a handful of test strings and reason about the modulus/base pairing needed to reproduce a standard polynomial hash exactly, then patch the constants in `pkg/hashmath` accordingly. Rebuild the fixed binary to `/home/user/sign`, and validate by running the same test strings through both `/home/user/sign` and `/app/oracle_signer` and diffing outputs before considering it done.
