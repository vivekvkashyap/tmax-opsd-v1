Start by trying to compile `/app/vendor/csv_lib/csv.h` standalone (or via a trivial test .cpp that includes it) to see the actual compiler error — it's most likely missing standard-library includes (e.g. `<string>`, `<vector>`) that the header relies on transitively from whatever includes it first elsewhere; add the missing `#include` lines directly to the header rather than patching around it in your own file. For `etl_processor.cpp`, stream the CSV line-by-line (avoid loading huge files fully into memory) and validate UTF-8 by walking each byte: implement a proper multi-byte UTF-8 continuation-byte state machine (checking leading-byte patterns like 0xC0-0xDF, 0xE0-0xEF, 0xF0-0xF7 and verifying continuation bytes are 0x80-0xBF) plus an explicit null-byte (`\0`) check — a naive `isascii`-only check will miss valid-but-malformed multi-byte sequences the evil corpus is designed to probe. On any violation, print `REJECTED` to stderr, append the exact log line to `/home/user/pipeline.log`, and exit(1) immediately without partial stdout output. For clean files, parse the wide header to discover symbol column pairs in their original left-to-right order, and for each timestamp row emit one long-format row per symbol in that same order, maintaining a per-symbol rolling window of up to the last 3 prices to compute the moving average incrementally as you stream (don't buffer the whole file just to compute this). Compile with the exact flags specified and test against both corpora directories before considering it done.