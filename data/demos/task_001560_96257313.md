Set up a real Cargo project (`cargo new /home/user/performance_analysis`) and add `csv`, `serde`/`serde_json` (and optionally `statrs`) to `Cargo.toml` rather than hand-parsing the CSV. Parse rows into two groups by `config`, computing per-group mean and sample variance (n-1 denominator) of `latency_ms`. Implement Welch's t-test manually if `statrs` doesn't give it directly: `t = (mean1-mean2)/sqrt(var1/n1+var2/n2)`, and compute Welch-Satterthwaite degrees of freedom before getting the two-tailed p-value from a Student's t distribution (e.g. via `statrs::distribution::StudentsT`), taking `p = 2*(1-cdf(|t|, df))`. Engineer `resource_pressure = cpu_util*mem_util` per row across the full dataset, then compute the Pearson correlation between that feature and `latency_ms` using the standard covariance/std-deviation formula over all rows, not per group. Serialize the five results into `/home/user/analysis_results.json` with `serde_json`, matching the required field names exactly, then actually build and run the binary (`cargo run`) so the JSON file is produced on disk rather than left as untested code.