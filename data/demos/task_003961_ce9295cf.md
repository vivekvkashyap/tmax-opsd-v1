Write process_logs.rs to do three passes: (1) read the whole log file as a String and use the `regex` crate to match `API_KEY=[A-Za-z0-9]+` and replace with `API_KEY=REDACTED` (adding regex as a dependency, or writing a manual scanner if you avoid crates); (2) split the sanitized text into line chunks of exactly 50 lines, writing each to `/home/user/chunks/part_N.txt` sequentially starting at 1 — watch the boundary math so line 50 goes to part_1 and line 51 starts part_2, and the final partial chunk is still written; (3) for every even N, create a symlink at `/home/user/symlinks/active_N.txt` using `std::os::unix::fs::symlink`, and make sure the link target string is the *relative* path `../chunks/part_N.txt`, not an absolute path (the verifier checks `readlink` output exactly). Finally shell out (`std::process::Command` or a plain `tar` invocation you run yourself) to gzip-tar the `/home/user/chunks/` directory into `/home/user/clean_logs.tar.gz`, and leave the original project_logs.txt untouched.
