Rather than guessing at fixes, read `src/main.rs` carefully to locate exactly where it diverges from the two described failure modes, then fix each root cause rather than papering over symptoms with broad try/catch-style suppression. For the panic: any slice indexing like `&data[offset..offset+len]` must first check `offset + len <= data.len()` (and `offset + len <= end` if there's a bounded region) — if the bounds check fails, treat it as a corrupted/truncated packet and stop parsing that level gracefully (break/return) rather than unwrapping or indexing blindly. For the hang: identify where the loop or recursion advances its position by a length field that can legitimately be zero (e.g. a Container with Length=0) — ensure the outer loop's progress counter still advances (e.g. by the header size) even when payload length is zero, and that recursive descent into a zero-length container doesn't re-enter the same offset/end range. After fixing, `cargo build --release` and manually run the binary against `/home/user/data/packets.bin`, comparing output line-by-line to `/app/telemetry_oracle`'s output on the same file to catch remaining behavioral mismatches before relying on the hidden dataset.