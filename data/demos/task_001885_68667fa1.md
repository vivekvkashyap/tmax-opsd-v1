This task has two unrelated parts — solve them separately.

Part 1 (manifest sanitiser): Write `/home/user/detect_slip.sh` (executable, correct shebang) taking a manifest file path. Parse records between `[RECORD]`/`[END]` blocks separated by blank lines, extracting each `File-Path:` value. For each path, normalize it against a hypothetical extraction base directory (resolve `..` and `.` segments, e.g. via `os.path.normpath`/`realpath -m` logic or manual segment-stack resolution) and check whether the resolved path stays inside the base — reject absolute paths outright, and reject any relative path whose `..` segments walk it above the base even if intermediate segments look fine (e.g. `a/../../b` must be caught, while `a/b/../c` which stays inside must be allowed). Print exactly `CLEAN`/exit 0 or `EVIL`/exit 1. Test against varied path-traversal patterns yourself since the hidden corpus will include such edge cases.

Part 2 (video analysis): use `ffmpeg` to extract or inspect frames of `/app/backup_cam.mp4` (e.g. per-frame color stats via `ffprobe`/`select` filters) and count frames where the image is predominantly solid red (very high red channel, negligible green/blue) — derive the count from the actual video, then write that integer to `/home/user/red_frame_count.txt`.
