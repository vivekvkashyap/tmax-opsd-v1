Start by transcribing `/app/machine_limits.wav` — install/use `whisper`, `ffmpeg`+a speech API, or any available STT tool, then read the transcript carefully to pull out the four numeric limits (max X, max Y, min Z, max spindle S) exactly as stated; don't guess values. Write `/home/user/gcode_validator.py` to tokenize each non-comment line (strip anything after `;`), split on whitespace, and use a regex like `([A-Z])(-?[\d.]+)` per token so it's robust to `X10` vs `X 10` spacing variants — this is where naive line-splitting attempts usually break. For each recognized axis letter (X, Y, Z, S) compare the parsed float against the corresponding limit (X/Y must not exceed max, Z must not go below the min since it's negative, S must not exceed max); exit 1 on first violation, exit 0 if the whole file is clean. Test the validator against representative safe and unsafe GCode snippets before trusting it. Then extract `/app/legacy_projects.tar.gz` recursively, find all `*.gcode` files (ignore ELF binaries and logs), run your validator on each, and pack only the ones that exit 0 — flattened, without directory structure — into `/home/user/valid_projects.tar.gz`.