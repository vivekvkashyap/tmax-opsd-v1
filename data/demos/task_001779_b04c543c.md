Treat this as OCR-from-scrolling-video, not a single-frame read. Extract frames with `ffmpeg`, then run OCR (e.g. `tesseract`) on each frame; since the text scrolls, consecutive frames overlap heavily, so you must stitch/deduplicate by finding the longest common suffix/prefix overlap between consecutive frames' OCR text (or track new lines appearing at the bottom edge as the canonical source) rather than naively concatenating every frame's OCR output, which would duplicate most lines. Once you have the deduplicated raw text, parse it into records matching the `START_RECORD ... ACTION: APPEND|DELETE ... END_RECORD` block format using a simple state machine or regex over blocks. Replay the actions in chronological order against an in-memory list representing file lines: APPEND pushes a line at the end, DELETE removes the 1-based line number from current state (validate bounds). Write the final line list to `/home/user/server_config.ini` using an atomic write pattern: write to a temp file in the same directory then `os.rename`/`rename()` it into place. Separately, chunk the full parsed changelog text into groups of exactly 5 complete records and save them as `chunk_0001.log`, `chunk_0002.log`, etc. under `/home/user/parsed_logs/`. OCR noise near record boundaries is the main failure mode — sanity-check that ACTION values and DELETE line numbers parse as expected before trusting them.
