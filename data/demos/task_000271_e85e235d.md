Work through the four subtasks in dependency order and verify each with a direct command before moving on. (1) Makefile: a shared library needs both compile-time and link-time flags — add `-fPIC` to the compile step (`smuggler.o` from `smuggler.c`) and `-shared` (plus `-fPIC`) to the link step producing `libsmuggler.so`; confirm with `file libsmuggler.so | grep "shared object"`. (2) Test ordering bug: the module-level `import smuggler` in the test file triggers C library init before `SMUGGLER_MODE` is set in `setUp()` — fix by moving `os.environ["SMUGGLER_MODE"] = "CI"` to execute before the `import smuggler` statement at module load time (e.g. set the env var at the top of the test file prior to any smuggler import, not inside a test method), then confirm `env -u SMUGGLER_MODE python3 -m unittest discover tests` exits 0 from within the smuggler dir. (3) Go server: `cd /home/user/target && go build -o server server.go`. (4) Benchmark script: use `subprocess.Popen` for the server, sleep briefly for it to bind port 8080, loop calling `send_fuzz` while `time.time() - start < 3`, count successes, terminate the subprocess, and json.dump the three required keys to benchmark.json.