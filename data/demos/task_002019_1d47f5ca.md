First get the three-service pipeline actually wired correctly before writing the filter: check the Redis config file for a wrong/disabled `port` directive and set it to `port 6379` bound to `127.0.0.1`; check `api.py` for a hardcoded wrong Redis host/port in the `redis.Redis(...)` call and correct it to `127.0.0.1:6379`; check `worker.sh` for a command popping from the wrong queue name or using a non-blocking/incorrect Redis command, and fix it to block-pop (`BLPOP`) the correct queue name that `api.py` pushes to, then invoke `/app/filter.sh <path>` on the popped value. Start Redis, the Flask API, and the worker script all in the background, and confirm the flow end-to-end with a manual `curl -X POST -d "<path>" http://127.0.0.1:8080/ingest` before moving on. For the filter itself, implement a bootstrap confidence interval in `/app/filter.sh`: for 500 iterations, resample 1000 lines with replacement from the input file (e.g. via `shuf -r -n 1000` or `awk` random sampling) and compute each sample's mean; fan these 500 independent iterations out to `parallel` for speed; then sort the 500 bootstrap means and take the 2.5th/97.5th percentile values as your CI bounds; print `ACCEPT` if 50.0 lies within (inclusive) the interval, else `REJECT`. Test against synthetic clean/shifted files you generate yourself first.