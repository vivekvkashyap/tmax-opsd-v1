Diagnose the existing query in `/home/user/generate_report.py` first: it selects from `databases d, backup_jobs b, storage_nodes s` with only a `WHERE d.name = ...` filter and no join conditions linking `b`/`s` to `d` or each other — this is the implicit cross join, multiplying rows and corrupting the `SUM(...) OVER (...)` window result. Fix it by properly joining `backup_jobs` to `databases` on the foreign key (drop the unused/unjoined `storage_nodes` from this query, or join it correctly if genuinely needed) so the window function's partition only sums that one database's actual backup rows. For the graph traversal, add a recursive CTE over `replication_links` that starts from the storage node of the target database's most recent backup job (`ORDER BY timestamp DESC LIMIT 1`), expands `source_id -> target_id` edges while accumulating the path (e.g. as a list/string of node ids) and hop count, and stops/selects the first row reaching a node whose `region = 'archive'` — order candidates by hop count ascending to guarantee shortest path, and translate node ids to node names for the final list. Keep the database-name argument fully parameterized (`?` placeholders, never string-interpolated into SQL) to avoid injection. Write the final dict with the three required keys to `/home/user/report.json`, and do a final run against `"billing-db"` to confirm the script executes end-to-end and leaves that file in place for verification.