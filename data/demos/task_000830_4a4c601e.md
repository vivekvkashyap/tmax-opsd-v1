Start by black-box fuzzing the oracle binary rather than static analysis alone: base64-encode a few probe strings containing `%s`, `%x`, `%n`, `%p` and normal text, run `/app/token_checker <token>` on each, and observe which ones crash (segfault) versus return cleanly — this confirms the vulnerability class is a classic uncontrolled format string (the decoded payload is likely passed straight into a `printf`-family call without a `%s` format specifier). Once confirmed, don't try to pattern-match exact malicious strings; instead detect the *structural* signature: after base64-decoding argv[1] in your C detector, scan the decoded bytes for the `%` character followed by a conversion specifier (n, s, x, p, d, and similar) — any occurrence should trigger rejection (exit 1), since that's what would be interpreted by an unguarded printf. Benign tokens without stray `%<letter>` sequences should exit 0. Write this scanning logic yourself in `/home/user/detector.c` using only standard C (implement your own base64 decoder), compile with `gcc -O2 -o /home/user/detector detector.c`, and test it against several self-crafted benign/malicious base64 tokens before considering it done, since the real grading corpus is hidden and adversarial.