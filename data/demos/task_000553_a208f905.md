Work Phase 1 and Phase 2 as separate evidence-gathering steps before writing any C code. For Phase 1, use `ffmpeg -i /app/incident_record.mp4 out_%04d.png` (or similar) to dump frames, then visually inspect a handful of frames a few seconds in (the ones showing a terminal window) to read off the exact raw `Upload-Path` HTTP header value shown on screen and identify the vulnerability class as CWE-22 (Path Traversal); write those two lines verbatim to `/home/user/policy_report.txt`. For Phase 2, treat `/app/sanitizer_oracle` as a black box: feed it many crafted HTTP requests via stdin (varying the `Upload-Path` header with combinations of `../`, `..\`, URL-encoded traversal sequences, nested/overlapping traversals, and edge cases) and diff outputs to infer the exact algorithm — check whether it decodes URL-encoding before or after stripping traversal sequences, and whether it strips iteratively (looping until no more matches) or in a single pass, since these produce different results on overlapping patterns like `....//`. Only after your black-box hypothesis matches the oracle on many probes should you write `/home/user/sanitizer.c`, replicate header parsing to end at `\r`/`\n`, and compile with `gcc -O2`. Validate by scripting many randomized fuzz requests comparing your binary's stdout against the oracle's before considering it done.
