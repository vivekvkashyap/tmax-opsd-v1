Read `limiter_logic.rs` and identify the ownership bug purely by inspecting the source: a `String` (e.g. `file_name`) is moved into a `Vec` via `.push()` and then used again afterward (like a `println!` referencing the moved value) — that's a use-after-move, fixable by cloning before the move or moving the print before the push. Write one sentence into `rust_bug.txt` naming this fix and including a word like "clone", "moved", or "reference" so it's verifiable by grep. For `organizer.sh`: take `$1` as source dir, list only regular files with something like `find "$1" -maxdepth 1 -type f | sort`, then loop over them maintaining a counter of files placed in the current queue — when the count hits 5, increment the queue index and reset the counter, creating `queues/queue_N` with `mkdir -p` before each `mv`. Make it executable with `chmod +x`. For `test_organizer.sh`, use `mktemp -d`, create 12 empty files with `touch`, clear `queues/*` first, run `../organizer.sh` (or the correct relative/absolute path) on the temp dir, then assert file counts per queue with `[ "$(ls queue_N | wc -l)" -eq N ]`, exiting nonzero on any failed assertion.
