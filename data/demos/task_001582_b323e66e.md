Scaffold with `cargo new sanitizer` in `/home/user`, add `encoding_rs` (for Windows-1252 to UTF-8 decoding) and a locking crate (`fs4` or `rustix`) to `Cargo.toml`. Parse the `.cpack` file as a sequential binary stream: open it, take a shared `flock`, then loop reading fixed-size fields with a buffered reader — 4-byte magic, 2-byte little-endian filename length, filename bytes decoded via Windows-1252, 4-byte little-endian payload length, then payload bytes — stopping cleanly at EOF between entries (a partial/short read mid-entry should also be treated as malformed). Apply the sanitization checks in the order given (magic first, then traversal, then size) and print+exit immediately on the first failure rather than accumulating errors. For traversal detection, check the decoded filename string for `../`, `..\`, and a leading `/` or `\` — do this on the decoded Unicode text, not raw bytes, since Windows-1252 decoding can change byte meanings. For decompression validation, spawn `/app/chunk_decoder` as a child process, write the payload bytes to its stdin, close stdin, and wait for it — check both exit status and whether it was terminated by a signal (Rust's `ExitStatus` exposes this on Unix); treat any crash as `REJECT: DECODER CRASH`. Stream large payloads to the child rather than buffering the whole file in memory when avoidable. Test against both `/home/user/corpora/clean/` and `/home/user/corpora/evil/` to confirm exact stdout strings and exit codes before considering it done.