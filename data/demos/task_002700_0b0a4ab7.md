Treat this as reverse-engineering a black box, not guessing at a spec. First run `strace -f -e trace=file,write /app/data_ingest <sample>` (or pipe sample JSON via stdin) to observe exactly which syscalls it makes — confirm the unlink-on-input behavior yourself rather than assuming. Then feed it a batch of varied, hand-crafted JSON log lines (vary `user_id`, `timestamp`, and other fields one at a time) and diff the outputs against your inputs to isolate the precise transformation rule: look for a conditional tied to `timestamp` modulo some small number, and for a field value that gets bit-mutated (XOR) or a hidden marker key that appears only in altered lines. Once you can reliably reproduce the mutation logic yourself from observed input/output pairs, encode that exact detection rule (not a fuzzy heuristic) in `/home/user/sanitize.py`, printing exactly `CLEAN` or `EVIL` per line with no extra output. Validate against the provided clean/evil corpora before finishing, since the grader requires 100% exact classification on both corpora — partial heuristics (e.g. only checking one of the two injected signals) will fail edge cases.
