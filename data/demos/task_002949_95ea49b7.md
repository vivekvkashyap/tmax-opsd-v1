Characterize the anomaly visually and numerically before writing the classifier. Extract sample frames from `/app/simulation_run.mp4` with `ffmpeg -i /app/simulation_run.mp4 -vf fps=1 frame_%04d.png`, then inspect several frames (normal and, if any exist in the sample, glitched) to understand the pixel statistics that distinguish them — the description states the glitch is a bright, saturated red band where the red channel heavily dominates green and blue. In `/home/user/detect_glitch.py`, load the image (e.g., with `PIL.Image` or OpenCV), compute per-pixel or per-row mean R/G/B values, and threshold on a simple, robust rule such as "a horizontal band exists where mean red is high and mean green/blue are low" rather than an exact-match heuristic tuned to one frame, since the grader uses a large separate corpus. Print exactly `GLITCH` or `CLEAN` with no extra whitespace or text. Validate your thresholds by testing against several extracted frames and a few synthetic edge cases (e.g., a mostly-gray frame with a faint reddish tint) to make sure your rule doesn't over- or under-fire before trusting it on the hidden corpus.
