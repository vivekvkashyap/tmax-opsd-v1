This is a classic known-plaintext XOR attack: recover the key by XORing the known 38-byte plaintext against the ciphertext in `admin_login.bin` byte-by-byte — since the key repeats every 4 bytes, the first 4 resulting bytes (or any consistent 4-byte window) give you the key directly; write a small Go snippet to do this rather than assuming any particular key value. Verify the key by re-XORing the ciphertext with it and confirming you get back valid, readable JSON matching the known plaintext exactly. Then write a scanning program that iterates every `.bin` file in `/home/user/captures/`, decrypts each with the recovered key (`ciphertext[i] ^ key[i%4]`), parses/searches the resulting JSON for `"role":"system_backup"`, and writes the matching base filename to `/home/user/found_backup.txt`. For the exploit, take the exact target JSON string given in the task, XOR it byte-by-byte with the same recovered key, and write the raw bytes (not text) to `/home/user/exploit.bin` using binary file I/O. Compile and run everything with `go run`/`go build` rather than hand-computing XOR values, and double check byte lengths align — off-by-one indexing into the key is the most common bug here.
