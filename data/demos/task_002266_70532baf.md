Start with `git log --oneline` in `/home/user/repo` to see the full commit range, then run `git bisect start`, mark the current HEAD as bad and the very first commit (`git rev-list --max-parents=0 HEAD`) as good. At each bisect step, run `python stats.py` and check whether the printed variance is a nonzero plausible value (good) or collapses to `0.0` due to catastrophic cancellation (bad); script this check with `git bisect run` using a small shell wrapper that greps the output. Once bisect converges on the exact commit, `git show` or `git log -p` that commit to see what changed to `calc.py`/`calc.pyc` — if the source was replaced by a compiled `.pyc`, don't try to read it as text; instead check it out (`git show <commit>:calc.pyc > /tmp/calc.pyc`) and disassemble it with Python's `dis` module (e.g. `python3 -c "import dis,marshal; ..."` or `uncompyle6`/`decompile3`) to recover constants like `SECRET_TOKEN` embedded in the bytecode's co_consts. Finally, on HEAD, rewrite `get_variance` in `calc.py` using a two-pass or Welford's algorithm (never `sum(x**2) - (sum(x))**2/n`, which cancels catastrophically for large means with tiny variance) and write the three fields to `/home/user/result.json`.
