First recover the four weight parameters from `/app/experiment_parameters.wav` by actual transcription — don't assume values. Check what's installed (`pip show SpeechRecognition`, `which whisper`, or similar) and use a speech-to-text tool to transcribe the audio to text, then parse out the four spoken numbers in order to get w1..w4; if one tool fails, try an alternative (e.g., a local Whisper model, or `speech_recognition` with an offline recognizer) rather than fabricating numbers. Once you have the four weights, write `/home/user/process_features.py` as a small, self-contained script: read CSV rows from stdin via `csv.DictReader`, for each row compute `weighted_sum = w1*A + w2*B + w3*C + w4*D` and `interaction = A*B - C*D` using the recovered weights, format both to exactly 4 decimal places (e.g. `f"{val:.4f}"`), and write rows via `csv.DictWriter` with header `A,B,C,D,weighted_sum,interaction` preserving the original A/B/C/D values as given in the input. Keep the script silent — no print/debug statements to stdout besides the CSV — since it will be fuzz-tested against many random CSVs comparing your output line-for-line to an oracle.
