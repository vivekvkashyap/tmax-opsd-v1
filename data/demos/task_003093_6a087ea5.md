Build this as a straightforward pandas + sklearn pipeline, but be careful with the cleaning step: coerce `mass`, `volume`, and `class` to numeric with `pd.to_numeric(..., errors='coerce')` first so non-numeric strings become NaN, then drop NaNs, then apply the strict filters (mass > 0, volume > 0, class in {0, 1}) — doing the filters before coercion will crash on non-numeric string rows or silently misclassify them. Write the cleaned frame to `/home/user/data/organized_data.csv` with `index=True` (the task asks to preserve the original index) and the header kept. For modeling, use `GaussianNB()` wrapped in `GridSearchCV` with `param_grid={'var_smoothing': [1e-9, 1e-7, 1e-5, 1e-3, 1e-1]}` and default `cv=5` (do not shuffle or set a custom KFold). Pull `best_params_['var_smoothing']` and `best_score_` (rounded to exactly 4 decimals with `round(x, 4)`) into the JSON report along with `len(cleaned_df)`, writing valid JSON with the three exact keys requested. Run the script end-to-end and confirm all three output files exist before finishing.
