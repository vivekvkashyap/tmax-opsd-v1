Work through this as a strict pipeline of shell + Go steps, verifying each stage before moving to the next. First extract: `mkdir -p /home/user/configs && tar -xzf /home/user/raw_configs/configs.tar.gz -C /home/user/configs`. In the Go program, walk `/home/user/configs/*.json`, unmarshal each into a `map[string]interface{}` (or a struct with a catch-all extra-fields map) so you can delete the `debug_token` key if present while preserving all other keys/values including nested ones. Build the output filename as `<service>_v<version>.json` from the parsed fields. For the atomic write requirement: write to a temp file in the destination directory (e.g. via `os.CreateTemp("/home/user/processed_configs", "tmp-*")`), write+flush+close it, then `os.Rename(tmpPath, finalPath)` — renaming within the same directory/filesystem is what makes it atomic, so don't write the temp file elsewhere and move it cross-filesystem. After running the Go program, `cd /home/user/processed_configs && sha256sum * > /home/user/manifest.txt` to produce the manifest exactly as required. Finally build the archive with paths relative to `/home/user` so it contains `processed_configs/` and `manifest.txt` as top-level entries, e.g. `tar -czf /home/user/final_configs.tar.gz -C /home/user processed_configs manifest.txt`. Spot-check by extracting the final tarball and grepping a processed file to confirm the debug token is gone but other fields remain.