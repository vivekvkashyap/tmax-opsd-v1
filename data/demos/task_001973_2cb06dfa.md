Build this as two clearly separated artifacts. In scorer.c: parse weights.txt with fscanf/sscanf on the `key=value` lines (strip the prefix before `=` and atof the rest); read sensor_data.csv line-by-line with fgets+strtok/sscanf, skipping the header. Apply schema enforcement first (drop if sensor_id isn't exactly 1/2/3, drop if pressure field is empty or non-numeric — check for empty string before atof, since atof silently returns 0.0 on empty/garbage which would hide a bad row instead of dropping it), then handle temperature separately: treat empty, the literal string "NaN", or any value outside [-50.0,150.0] as missing and impute 20.0. Compute risk_score with the given linear formula and write it with `%.2f`. In pipeline.sh, compile with gcc, run the binary, then use a single `awk -F,` pass over scored_data.csv to accumulate sum/count per sensor_id in arrays, and in an END block iterate sensor_ids in numeric sort order (e.g. via `sort -n` on the printed keys) printing `id: avg` with two decimals. chmod +x pipeline.sh. Verify by running the pipeline end-to-end and diffing summary.txt against hand-computed row-by-row expectations.
