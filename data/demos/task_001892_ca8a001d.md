Don't skim the code for the bug by eye first — reproduce the crash: compile with `g++ /home/user/process_queries.cpp -o /home/user/process_queries` and run it against the full `queries.txt`, then use delta debugging (e.g. bisect with `head -n N queries.txt | ./process_queries /dev/stdin`, or binary-search by splitting the file in half repeatedly) to narrow down to the exact single line/query causing the `std::out_of_range`. Once isolated, inspect the surrounding C++ for how it turns a query's X value into an index (likely `vector::at()` or array indexing) — the bug is almost certainly that a modulo of a negative number in C++ can yield a negative result, which becomes an out-of-bounds index. Fix the formula to wrap any negative result back into `[0, 99]` using the standard trick `((x % 100) + 100) % 100`, without altering the behavior for already-positive results. Recompile, rerun specifically on the isolated failing query, and write the result to `/home/user/debug_result.txt` in exactly the required `ID: <id>, CORRECT_HASH: <value>` format — match spacing and punctuation exactly since the check is an exact string comparison. Leave `queries.txt` untouched throughout.
