Chain independent extraction steps into a forging script, verifying each intermediate value before trusting it. Extract frames from the video with `ffmpeg -i /app/capture.mp4 -vf fps=<N> frame_%04d.png` (pick a frame rate dense enough to catch the address bar clearly, or target the specific timestamp where the redirect happens), then run `tesseract` on candidate frames and inspect the OCR text for a JWT-like `header.payload.signature` string in the URL — crop/preprocess frames (grayscale, threshold, upscale) if OCR is noisy on browser chrome text. Separately, grep `/app/auth_logs.json` for entries near the token's timestamp/user and look specifically for a debug/error field that leaked something like an HMAC key — don't assume the field name, actually inspect the JSON structure. For the forging script, replicate JWT signing: base64url-encode a header and a payload containing `user=admin` and `exp = <arg> + 3600`, concatenate as `header.payload`, sign with `openssl dgst -sha256 -mac HMAC -macopt hexkey:<hex-of-leaked-key> -binary`, base64url-encode the signature, and output `header.payload.signature` with no extra text. Test your script against many timestamps locally to make sure the JWT structure/claims are consistent before relying on the 95% threshold.
