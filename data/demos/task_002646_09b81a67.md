Start by transcribing `/app/instructions.wav` accurately (e.g. `ffmpeg` to extract/convert audio plus a speech-to-text tool like `whisper`) — the exact filtering rule (which subnet and which port constitute "malicious") is spoken there, so don't assume a rule; derive it from what you hear. Once you have the rule, write `main.go` to: read the single CLI arg as a file path, read and `json.Unmarshal` it into a struct matching `{src_ip, dst_port, protocol, payload}`, and on any read/parse error immediately `os.Exit(1)` (don't panic — panics won't produce a clean exit code). To test IP membership in a CIDR block, use `net.ParseCIDR` and `.Contains(net.ParseIP(...))` rather than manual string prefix matching, which is error-prone at subnet boundaries. Combine the IP-in-subnet and exact-port match with a logical AND matching what the memo specified, then `os.Exit(1)` if malicious, `os.Exit(0)` otherwise. Write `setup_ci.sh` to `go build` the binary into `/home/user/netfilter/filter` and emit a systemd unit file with `[Unit]`, `[Service]` (including `ExecStart=`), and `[Install]` sections. Validate against a few hand-crafted clean/evil JSON samples before considering it done.