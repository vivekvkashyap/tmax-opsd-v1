Read /app/protocol.png first (OCR it with `tesseract` or view it) to recover the actual state-machine rules the parser is supposed to implement — don't assume, extract the text. Then run `leaky_parser.py` on payload.txt while watching memory (e.g. `/usr/bin/time -v` or `ps`) to confirm the leak, and apply delta debugging: bisect the token sequence in payload.txt by removing/splitting halves and re-testing until you isolate the minimal token subsequence (likely repeated tokens in a specific state) that triggers unbounded growth — inspect the parser's state machine loop for a code path that increments the buffer/loop index without ever advancing `i`, causing an infinite append. Fix that logic per the recovered protocol rules so it terminates and matches the oracle's state transitions exactly, including edge cases. Since grading requires exact output equivalence to /app/oracle_parser under fuzzing, write your own randomized fuzz harness that generates many random token sequences (mixing the protocol keywords and arbitrary junk tokens), pipes each into both your fixed_parser.py and the oracle via subprocess, and diffs stdout — iterate until outputs match on all generated cases, not just the original payload.
