Handle each dataset with the statistically exact method rather than an approximation. For dataset A, load x1, x2, y and solve OLS via the normal equations (or a library like numpy.linalg.lstsq / statsmodels) on the design matrix that includes a column of ones for the intercept — do not hand-derive slopes visually. For dataset B, compute Shannon entropy in bits: get the empirical class probabilities of `label`, then sum -p*log2(p) over all classes (use math.log2 or log(p)/log(2), never natural log). For dataset C, apply the given fixed linear model (given intercept and weights) row-by-row to compute predictions, then RMSE = sqrt(mean((pred-target)^2)). Write each result to its exact specified path with exact rounding (2 decimals for OLS coefficients as a comma-separated line, 4 decimals for entropy and RMSE) — use Python's round() or string formatting consistently, and double check trailing zeros are preserved (e.g. "3.00" not "3.0"). Verify output format matches the example exactly (no extra whitespace, no brackets) since checks likely grep for the literal formatted string.
