Structure `/home/user/validate_deployment.py` as a chain of independent checks, each of which must reject-and-exit before any filesystem mutation happens (mutations should only occur after every check passes, to satisfy "no directory modifications" on evil inputs). For port validation, parse `vnc_port` and check `5900 <= port <= 5999`, guarding against it being a non-integer type too. For directory isolation, resolve the candidate path with something like `os.path.realpath(os.path.join(base, vm_image_path))` and then verify the resolved path starts with the resolved base directory plus a path separator (use `os.path.commonpath` or a strict prefix check, not a naive string `startswith` on the base alone, which a crafted sibling directory name could defeat) — this catches `../` traversal and symlink tricks. For the cluster ID check, don't hardcode a guessed value: recover the authoritative ID by running OCR (e.g. `pytesseract`/`PIL`) on `/app/vnc_screenshot.png`, extracting the token that follows the `CLUSTER_ID:` label in the recognized text, and compare the JSON's `cluster_id` against that recovered string exactly. On any failure, print `REJECT` and `sys.exit(1)` immediately. On full success, print `ACCEPT`, `os.makedirs(..., exist_ok=True)` the deployment directory, and create the `latest` symlink pointing at the resolved image path idempotently (remove/replace if it already exists as the wrong target, or skip if already correct) before `sys.exit(0)`.