Build this as a strict pipeline, not a one-off notebook, since the wrapper script must reproduce every step on unseen data. First load `/app/data/train_raw.csv`, drop NaN rows, then compute per-feature z-scores (using the *cleaned* data's mean/std) and drop any row where any feature's |z| > 3.0 — save the fitted mean/std (or refit consistently) so the wrapper applies identical cleaning to new data rather than only working on the training set. For redundant-feature removal, compute the Pearson correlation matrix on the cleaned features, and for every pair with |corr| > 0.85 drop the higher-indexed feature, iterating carefully since dropping one column can change which pairs still apply; persist the final feature list to `/home/user/selected_features.txt`. Time the legacy oracle by running `/app/bin/legacy_oracle <in.csv> <out.csv>` on batches and log per-100-row latency — this gives you the real baseline you must beat. Train a lightweight, fast model (e.g. linear/ridge regression or a small tree ensemble) on selected features against the oracle's target column only — favor simplicity since the speed threshold (50x) is aggressive and complex models risk failing it. Finally write `/home/user/predict.sh` so it reruns the exact same cleaning + feature-selection + inference chain end-to-end on the two CLI-provided CSV paths, producing a `target` column header.