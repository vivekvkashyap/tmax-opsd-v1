Do targeted delta-debugging rather than reading the whole vendored library: since the bug is described as affecting array-length, aggregation, and `contains`-style queries, go straight to `/app/vendored/jmespath-1.0.1/jmespath/functions.py` and inspect the built-in function implementations (`_func_length`, `_func_contains`, `_func_sum`, `_func_max`/`_func_min`, sort functions, etc.) for logic that deviates from the JMESPath spec — compare each against the upstream jmespath 1.0.1 semantics you know (e.g. `length()` must return the exact count with no off-by-one, `contains()` must return a proper boolean membership test). Confirm each suspected function by writing a tiny JSON fixture and query, running it through your not-yet-fixed `query.py`, and comparing to `/app/oracle_query`'s output on the same input — a mismatch pinpoints the broken function precisely. Fix the logic in place to match spec semantics exactly (don't rewrite unrelated code, since the diff is deliberately localized to a couple of functions). Then implement `/home/user/query.py` to `sys.path.insert(0, "/app/vendored/jmespath-1.0.1")` before importing `jmespath`, parse the two positional args (json file, query string), run `jmespath.search`, and print with `json.dumps(result, separators=(',', ':'))` — no `indent`, no trailing newline surprises. Before trusting your fix, run several more spot-checks against the oracle covering `length`, `contains`, sorting, and nested traversal before relying on the full fuzz suite.