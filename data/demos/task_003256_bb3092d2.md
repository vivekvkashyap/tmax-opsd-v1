Build and test the Rust executable's file-operation logic before wiring up cron, and run it manually twice as instructed so you can literally inspect the resulting files rather than assuming correctness. In the script, take `std::env::args()` to get the version argument, copy `/home/user/backups/app_v<version>.txt` to `/home/user/active/app.txt` with `std::fs::copy`, perform log rotation by checking `Path::new(".../deploy.log").exists()` and if so `std::fs::rename` it to `deploy.log.old` (rename overwrites on most platforms including Linux, but confirm this behavior rather than assuming), and only then create/write the new `deploy.log` with `Deployed v<version>\n` via `std::fs::write` or `File::create` — order matters: rotate the OLD log before writing the NEW one, otherwise you'll rotate your own fresh content. Compile with `rustc -o /home/user/deploy_restore /home/user/deploy_restore.rs`, run it once with `1` and once with `2`, then inspect `active/app.txt`, `deploy.log`, and `deploy.log.old` to confirm the rotation happened as expected. For the cron step, write `crontab.txt` with a line matching `0 3 * * * /home/user/deploy_restore 3` (exact five-field cron time syntax for daily 3 AM) and install it with `crontab /home/user/crontab.txt`, then confirm with `crontab -l`.