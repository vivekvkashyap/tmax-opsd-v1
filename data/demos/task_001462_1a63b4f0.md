Three independent subtasks — solve and sanity-check each before moving to the next.

Task 1 (OCR): run `tesseract /app/experiment_metadata.png stdout` (or output to a text file) and parse the resulting text for the `MIN_SIMILARITY_SCORE=X.XXX` pattern with a regex; don't assume a value, actually read what tesseract returns since OCR output can have noise — print/inspect the raw text first.

Task 2 (matplotlib headless): the failure is a backend problem, not a plotting-logic problem. Either edit `plot_results.py` to call `matplotlib.use('Agg')` before importing `pyplot`, or set the `MPLBACKEND=Agg` environment variable when running the script — Agg is the standard non-interactive, file-only backend that works in CI without a display.

Task 3 (filter script): write `/home/user/filter_logs.py` to `json.load` the given path, then check the three EVIL conditions from the spec against the threshold you recovered from Task 1 (not a hardcoded guess) — `p_value` outside `[0,1]`, `ci_lower > ci_upper`, or `similarity_score < MIN_SIMILARITY_SCORE`. Use `sys.exit(1)` for evil and `sys.exit(0)` (or exit normally) for clean, matching the CLI contract `python3 filter_logs.py <file_path>` exactly since it will be tested against a hidden corpus of files, not just the 7 examples shown.