Treat `/app/legacy_math` as a black box and characterize it empirically rather than guessing — pipe several small test inputs like `echo '{"query": {"value": 1}}' | /app/legacy_math`, `value: 2`, `value: 3`, `value: 6`, `value: 27` and record the outputs. Look at how the output JSON is structured (key names and nesting) and how the numeric result scales with input to identify which well-known iterative integer sequence is being computed (a sequence that always terminates at 1, with step/iteration counts that vary non-monotonically — try a handful of inputs and compare against known sequence behaviors, e.g. by hand-computing candidate sequences for the same inputs and matching outputs). Once you're confident in both the algorithm and the exact JSON field names/nesting/formatting (check for whitespace, key order, and whether numbers are ints), implement `/home/user/math_port.sh` using `jq` to parse the input JSON and build the output JSON, with a bash arithmetic loop computing the iteration count. Before finalizing, diff your script's output against the oracle binary's output for many test values (edge cases like 1, powers of 2, and large numbers near 100000) to catch formatting mismatches (e.g. trailing newline, spacing in JSON) since the equivalence check is exact byte-for-byte (ignoring trailing newline only).