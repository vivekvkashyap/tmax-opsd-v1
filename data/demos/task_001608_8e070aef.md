Debug this pipeline stage by stage rather than rewriting blindly: first read gateway.py and worker.cpp side by side and diff the Redis list/key names each one uses — the "service disconnect" is almost certainly a string mismatch between the queue name the gateway pushes to and the name the worker reads from; pick one name and make both files agree. Second, look at how gateway.py packs bytes (likely Python `struct.pack` with a format like `<BIf` for 1-byte id + 4-byte int + 4-byte float = 9 bytes) versus how worker.cpp reads them — if worker.cpp uses a plain C struct without `#pragma pack(1)` (or `__attribute__((packed))`), the compiler will insert padding and misalign every field after the first; fix by packing the struct and reading exactly 9 raw bytes into it, not by relying on struct layout. Third, replace the two-pass/sum-based variance formula with Welford's online algorithm (running mean, M2, update per new sample, variance = M2/(n-1)) to fix cancellation on large-mean small-variance data. Build a `--file-mode <in.bin> <out.bin>` CLI path so you can fuzz-test your worker directly against arbitrary binary record files without needing Redis running, and validate against constructed inputs where you can hand-compute the expected variance before trusting the daemon mode end-to-end.