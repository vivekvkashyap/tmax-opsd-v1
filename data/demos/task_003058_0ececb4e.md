Use C++17's `std::filesystem::recursive_directory_iterator` to walk project_backups and match entries ending in `.zip`. For each one, shell out to `unzip -t <file>` (via std::system or popen) and check its exit status — zero means the archive is fully valid, nonzero means it's corrupt — rather than trying to reimplement zip validation yourself. Build the renamed filename by inserting `_verified` or `_corrupt` immediately before the `.zip` suffix (manipulate the extension carefully, e.g. via `path::stem()`/`path::extension()`, not a blind string append) and rename with `std::filesystem::rename` in the same directory, leaving non-.zip files untouched. Compile the program and actually run the resulting binary so the renames are applied to disk — writing the source alone isn't sufficient, the final directory state is what's checked.
