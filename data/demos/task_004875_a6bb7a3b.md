Split this into a fitting stage and an optimization stage, and validate each separately. For fitting: define the ODE system as a function usable by `scipy.integrate.odeint`/`solve_ivp`, wrap it in a residual function that simulates with candidate (a,b,c,d) from the given initial conditions x(0)=1.0, y(0)=2.0, and compare against the CSV's x(t), y(t) columns at the same time points — use `scipy.optimize.least_squares` or `curve_fit` with bounds [0,2] on each parameter, and check the fit visually/numerically (residual norm near zero) before trusting the recovered parameters, since a bad initial guess can trap the optimizer in a local minimum. For the second stage, write an objective that, given (x0,y0), integrates the ODE from t=0 to 10 using the *fitted* parameters, computes the integral of x(t)+y(t) via `trapz`/`simpson`, and negate it for a minimizer (or use `differential_evolution` directly which maximizes via negation) over bounds x0,y0 in [0,5]; reason about the sign/monotonicity of the dynamics to sanity-check whether the optimum should land at a bound or an interior point rather than trusting the optimizer blindly. Round all values to 3 decimals and write the exact JSON schema requested to `/home/user/results.json`.
