Build this bottom-up and sanity-check each stage against the deterministic parts before trusting the stochastic parts. First read `/home/user/sequences.txt` line by line, compute each sequence's Hamming distance to `ATGCGTAC` (compare character-by-character, count mismatches), and manually verify the distribution you get (mostly 0s with a handful of 1s) makes sense — the sample mean of these 50 distances is the deterministic quantity everything else should converge toward, so compute and print it as a sanity check during development. Second, implement Metropolis-Hastings for the Poisson-rate posterior: maintain current lambda, propose `lambda + N(0, 0.5)`, clamp/reject proposals outside [0,8], compute the Poisson log-likelihood ratio (sum of Hamming distances times log(lambda_prop/lambda_curr) minus N*(lambda_prop-lambda_curr), watch for lambda=0 edge cases in log), accept via a uniform draw, run 10000 iterations, discard the first 1000, and average the rest — it should converge near the sample mean. Third, implement bootstrap: resample the 50 distances with replacement 1000 times, take each resample's mean, sort the 1000 means, and read off the values at the 2.5th and 97.5th percentiles for the CI. Write all three numbers to `/home/user/results.txt` rounded to 3 decimals in the exact `Key: value` format specified, compile with `gcc -lm`, and run it — since verification uses tolerance bands rather than exact matches, focus on getting the algorithm logic right rather than exact RNG reproduction.