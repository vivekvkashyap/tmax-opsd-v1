Read both input CSVs in C with straightforward parsing (fscanf or fgets+sscanf with comma delimiters): skip the header line of raw_data.csv, then for each data row read id,f1,f2,f3 as int/double; read transform.csv as a plain 3x3 grid of doubles (no header). Apply the matrix multiply exactly as given — note the indexing convention in the spec is `out_c = sum_r f_r * M[r][c]` (i.e., you're multiplying by columns of M, not the usual row convention), so implement it literally as written rather than assuming a generic matmul. Write the output with the exact header `id,out1,out2,out3` and format every float with `%.4f`. For the bash test script, compile with `gcc /home/user/etl.c -o /home/user/etl_bin -lm`, run the binary, then check the output file exists and has exactly 4 lines (e.g. `wc -l`), printing "PIPELINE PASS" or "PIPELINE FAIL" accordingly and making the script executable. Finally actually run the pipeline once so `processed_data.csv` exists on disk before finishing.
