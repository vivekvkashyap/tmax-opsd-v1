Start by OCR'ing `/app/authz_spec.png` with `tesseract` to recover the exact listen port and SERVICE_ID token — don't guess these; if OCR output is noisy, try preprocessing (grayscale/threshold/upscale) and re-run tesseract until the text is clean, then use those recovered values (not any placeholder) as the port to bind and the token to compare against in your C server. Implement the C service using standard BSD sockets (`socket`/`bind`/`listen`/`accept`) on `127.0.0.1:<recovered port>`. For each connection, read exactly the fixed 4-byte magic + 2-byte big-endian length header first (using `ntohs`/manual byte-order handling since the spec says big-endian), validate the magic against `0x4155545A`, then read exactly `length` more bytes as the payload — don't assume payload arrives in one `recv()` call; loop until you have all expected bytes. Compare the payload string exactly (same length, same bytes) against the recovered SERVICE_ID before replying "OKAY" (0x4F4B4159) or "FAIL" (0x4641494C) as raw 4-byte big-endian values, then close the connection. Write `build_and_deploy.sh` to compile with `gcc -Wall -Wextra`, launch the binary in the background with `&`, capture `$!` into `/home/user/mock_authz.pid`, and confirm with `netstat`/`ss` that the port is actually listening before the script exits.