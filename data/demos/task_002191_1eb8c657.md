Start by reading the policy image carefully rather than guessing: open `/app/waf_policy.png` (view it or run OCR on it) and transcribe the flowchart into a numbered list of rules exactly as ordered — rejection checks first, then transformation steps — since order determines correctness when multiple rules could match the same payload. Implement rejections before transformations in your script, matching substrings case-insensitively (e.g. `shopt -s nocasematch` with `[[ "$input" =~ pattern ]]`, or `grep -qi`) — a naive case-sensitive `==`/`case` check will fail on mixed-case adversarial payloads. On any rejection, print exactly `BLOCKED` and `exit 1` immediately, skipping later rules. For transformations, apply each one in the stated order using parameter expansion (e.g. `${var//pattern/replacement}`) and print the result with `echo -n` to avoid an unwanted trailing newline, since the grader compares output bit-exact. Quote `"$1"` throughout to preserve spaces, quotes, and special characters. Test manually against edge cases: empty string input, strings that are only whitespace, multiple consecutive spaces, and strings containing the reject-triggers in different letter cases. Finally `chmod +x /home/user/waf.sh`.
