Diagnose the two systemd-vs-interactive-shell mismatches before rewriting anything: systemd services don't inherit your shell's `$PWD` or `$PATH`, so a script relying on relative paths or unqualified binaries behaves differently under `systemctl --user start` than when you run it by hand. Fix `backup_mailer.sh` to reference `/home/user/mailing_list` and `/home/user/backups/mailer_backup.tar.gz` as absolute paths (and use absolute paths for any binaries like `tar` if PATH is suspect), and add `set -euo pipefail` at the top so failures surface instead of silently continuing. Fix the unit file's `WorkingDirectory` (or remove reliance on it) and make sure `ExecStart` invokes the corrected absolute-path script. Since you lack root, always pass `--user` to `systemctl`/`journalctl`, and export `XDG_RUNTIME_DIR=/run/user/$(id -u)` if the user manager isn't reachable. After `systemctl --user daemon-reload` and `systemctl --user start mailer-backup.service`, check `systemctl --user status` for success before computing `md5sum` on the resulting archive and writing it into `deployment_summary.log` in the exact `BACKUP_CHECKSUM: <hash>` format — compute the checksum from the real file, don't fabricate it.