First read `transaction_processor.py` and identify the deadlock: `transfer()` always acquires `account_a.lock` then `account_b.lock` in the argument order given, but since `a, b` are chosen randomly, two concurrent transfers can lock in opposite orders (A then B vs B then A) and deadlock waiting on each other. The standard fix is lock ordering: always acquire the two accounts' locks in a consistent, deterministic order (e.g., by comparing `account_id` and acquiring the lower-ID account's lock first, then the higher-ID one) regardless of which was passed as `account_a`/`account_b`, while still using the original `account_a`/`account_b` references for the actual balance update logic. Do NOT collapse this into one global lock — the verifier explicitly checks that multiple distinct locks are still used and that ordering is based on `account_id`. Leave `main()`, thread count, and iteration count untouched. After fixing, run the script (`python3 transaction_processor.py`) and confirm it completes promptly (no hang) and that `/home/user/success.log` is written with the total balance sum unchanged from the initial total (transfers are zero-sum).