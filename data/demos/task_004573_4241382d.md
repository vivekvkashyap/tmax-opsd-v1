Implement this as a multi-pass awk/bash pipeline that strictly never touches test rows during training. Pass 1: filter `dataset.csv` to rows where `split == train`, tokenize the `text` field by whitespace, lowercase every token (`tolower()` in awk), discard tokens shorter than 4 characters, and count frequencies with an awk associative array; sort by count descending then alphabetically ascending for ties (`sort -k2,2nr -k1,1`) and take the top 10 — this becomes your fixed feature vocabulary (V=10). Pass 2: for each of the two classes (A/B), count total occurrences of each of the 10 feature words within that class's training texts only, plus the total feature-word count per class; then compute Laplace-smoothed conditional probabilities `(count(w,C)+1)/(Total_Words(C)+10)` and class priors from training-set label counts. Pass 3: for each test row, tokenize its text the same way (lowercase, but do NOT re-filter by length or recompute frequencies — just check membership in your fixed feature set), intersect with the feature list, and sum `ln(prior) + sum(ln(P(w|C)))` per class using awk's `log()`; predict the class with higher score, defaulting to A on ties. Write `id,prediction` rows to `/home/user/predictions.csv` with the exact header, then `chmod +x run_pipeline.sh`. The main pitfalls are leaking test-set vocabulary into feature selection and getting the tie-breaking/sort order wrong.
