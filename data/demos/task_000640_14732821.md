Do not skip the audio step or guess at requirements — transcribe `/app/pm_request.wav` with an actual tool (install/use `openai-whisper`, or ffmpeg + a speech-to-text model) and read the resulting transcript text yourself to extract the precise business rules (which tables to join, the specific window-function metric and its window size, and the exact filter conditions on tier/session duration) — these details are the whole point of the audio and must come from what you hear/transcribe, not assumption. Then inspect `/app/naive_report.py` and the DB schema to understand the baseline columns/output shape it (would) produce, so your optimized script's CSV output matches exactly. Write `/home/user/optimized_report.py` as a single SQL query using window functions (e.g. `AVG(...) OVER (PARTITION BY user_id ORDER BY timestamp RANGE BETWEEN ...)` for the rolling average) combined with joins/EXISTS filtering on tier and session duration directly in SQL — avoid any per-row Python loop or per-user query (that reintroduces N+1 behavior and kills your speedup). Run both naive and optimized scripts yourself, compare `report.csv` output and timing, and iterate on the SQL until it's both correct and comfortably fast before finishing.
