Fix the two bugs independently, then combine. First, in `/home/user/generate_report.py`, replace the comma-separated implicit cross join with an explicit `JOIN access_events a ON a.emp_id = e.id`, keeping the `GROUP BY department, system_name` — verify by hand-checking a couple of counts against the raw table contents so the join isn't silently duplicating rows. Second, use `rdflib` to load `/home/user/system_graph.ttl` and run a SPARQL `SELECT` for `?system WHERE { ?system a <http://example.org/ontology#HighRiskSystem> }`, extracting the local system name from each returned URI (e.g. the fragment/last path segment) rather than hardcoding which systems are high-risk — the whole point is that this list is now dynamic. For each high-risk system found, run the corrected SQL query filtered to that `system_name`, and only include department/system rows with `access_count > 0`. Write the CSV with the exact header `department,system_name,access_count`, sorted first by department then by system_name, and confirm no HighRisk systems were missed and no LowRisk ones leaked in by printing the SPARQL results before writing the CSV.
