This task has two independent deliverables — solve them separately and verify each before moving on. (1) For the log sanitizer: read `/app/legacy_parser.py` to understand its intended state machine, then reimplement it as pure Bash using a real state-tracking loop (e.g. `while IFS= read -r line` with a `state` variable), not a single regex pass — multi-line Rust borrow-checker blocks and CMake link-error blocks require tracking open/close markers across lines. Explicitly check for injected shell metacharacters/commands disguised inside otherwise normal-looking log lines, and for malformed/unbalanced multi-line error blocks; treat both as REJECT. Print exactly `CLEAN`/exit 0 or `REJECT`/exit 1 — no extra output. Since grading is against a hidden adversarial corpus, favor being strict about balanced delimiters and suspicious tokens rather than pattern-matching only the two example evil forms given. (2) For the build fix: run `tesseract /app/build_arch.png stdout` (install tesseract if missing) to OCR the diagram, locate the documented library path in the OCR text, write it verbatim to `/home/user/library_path.txt`, then patch `CMakeLists.txt`'s library search/link path to that value and fix the Rust ownership error in `lib.rs` (likely a borrow held across a move or a moved value used after move — restructure ownership, don't just add `.clone()` blindly if it changes semantics). Finally actually run the `mkdir build && cmake .. && make` sequence to confirm it succeeds.