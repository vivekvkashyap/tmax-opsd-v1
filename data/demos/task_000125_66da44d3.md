First recover `INIT_VAL` from the video: extract frames from the opening seconds of `/app/legacy/demo.mp4` with `ffmpeg -i /app/legacy/demo.mp4 -vf fps=2 frame_%03d.png` (a couple frames per second is enough since the value appears early and stays static), then run `tesseract` on the frames containing the terminal text to OCR the printed `INIT_VAL=<number>` line — check a few candidate frames since OCR can misread digits, and cross-verify visually if unsure. Then read `/app/legacy/machine.py` closely to extract the exact state-machine logic: what states exist, what characters trigger which transitions, and the precise arithmetic (multiplication, addition, subtraction, modulo) and evaluation order at each step — Python 2 integer/modulo semantics can differ subtly from Bash's, so pay attention to negative-number modulo behavior (Bash's `%` can return negative results that Python wouldn't, so explicitly re-normalize into `[0,100)` after any subtraction). Port this state machine directly into `/home/user/machine.sh` as a character-by-character loop using `${input:$i:1}` and integer arithmetic with `$(( ))`, initializing the value from your OCR'd `INIT_VAL`. Test with several input strings by hand-tracing the Python logic and comparing to your Bash script's output before considering it done, since the verifier does bit-exact fuzz testing across hundreds of strings.
