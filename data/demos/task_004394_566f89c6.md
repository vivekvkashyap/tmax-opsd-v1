Build this as a small DAG: a `Makefile` `all` target that chains a Python (or awk) step for parsing/resampling/rolling stats and a shell step for orchestration, writing intermediate CSVs between stages if that's easier to debug than one monolithic script. The core logic is best done with pandas: parse timestamps, floor to date (UTC), `groupby(['server_id','date']).agg({'config_size_bytes':'last','changes_count':'sum'})`, then per-server reindex over a full daily `date_range` spanning that server's own first-to-last date (not a global range) — this per-server range is the detail naive attempts miss. Forward-fill `config_size_bytes` and fill `changes_count` with 0 on the reindexed frame. Compute rolling mean with `window=7, min_periods=1` and rolling *sample* std with `window=7, min_periods=3` (so std is NaN, and therefore never an anomaly, until 3 days of history exist). Flag anomaly when `abs(value - rolling_mean) > 2 * rolling_std` and std is not NaN/0. Round mean/std to 1 decimal only in the final JSON, filter to anomalies only, and sort by date then server_id before writing `/home/user/detected_anomalies.json`. Validate by running `make` fresh and inspecting the exact JSON.