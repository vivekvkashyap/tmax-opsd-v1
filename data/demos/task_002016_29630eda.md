Build this as a strict linear pipeline and get each stage's output verified before moving to the next, since a small mistake early (e.g. wrong frame rate, wrong normalization) silently corrupts everything downstream. Extract frames with `ffmpeg -i /app/data.mp4 -vf fps=1 frames/frame_%04d.jpg` exactly — the frame numbering and count must line up with "seconds", since frame_0015.jpg is defined as the 15-second target. For embeddings, load resnet18 pretrained, strip the final FC layer (wrap `nn.Sequential(*list(model.children())[:-1])` or hook the avgpool output), put it in `.eval()` mode, and apply the exact given resize/normalize transform — deviating from the specified mean/std or interpolation will shift every downstream similarity score. Compute cosine similarity of every frame's embedding against the target embedding, then build the two emission-likelihood arrays exactly as specified (note state 0's likelihood is a constant, not dependent on similarity). Implement the forward-backward algorithm carefully: normalize alpha and beta at each time step to avoid underflow, use the given initial and transition probabilities precisely, and compute the final posterior as the normalized product of alpha and beta at each t. Write frame_name/posterior_prob pairs sorted alphabetically by frame_name to the CSV; wrap everything in one script/Makefile so it's reproducible end-to-end.
