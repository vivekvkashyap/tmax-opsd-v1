Design the C program around a direct-access (index) array sized to the maximum possible node ID (1..50000) so out-degree counting is O(1) per edge: read each `SOURCE TARGET` line with fscanf in a loop and simply increment `degree[SOURCE]++` (target does not affect out-degree). After counting, build a list of (node_id, degree) pairs only for nodes with degree >= MIN_DEGREE — iterate the fixed-size array once rather than tracking a dynamic set, skipping zero-degree entries. Sort this filtered list with qsort using a custom comparator that sorts descending by degree first, and ascending by node_id as the tiebreaker (compare degree first; if equal, compare id). Apply pagination by simply indexing into the sorted array starting at OFFSET and taking up to PAGE_SIZE elements, being careful to bound-check against the array length so you don't read past the end when OFFSET+PAGE_SIZE exceeds the count. Write each paginated result as `NodeID: OutDegree\n` to the specified output file. Parse argv positions 1-4 for filepath, MIN_DEGREE, OFFSET, PAGE_SIZE using atoi, then compile with gcc and execute exactly as specified so the output file is generated for verification.
