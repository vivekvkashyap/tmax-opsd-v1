Fix the vendored library before writing any new code: inspect `/app/cgraph-1.0/Makefile` line by line for a typo in the compiled object filename (the compile rule may output a differently-spelled `.o` file than the one the archive/link step expects) and correct it so `gcc -c cgraph.c -o cgraph.o` and `ar rcs libcgraph.a cgraph.o` refer to the same file, then `make` to produce `libcgraph.a`. For the detector, model each resource as holding a set of (transaction, lock-mode) pairs; on each `READ`/`WRITE` line, determine compatibility against all current holders (S-S compatible, anything with X incompatible), and if incompatible, add wait-for edges from the requesting transaction to each conflicting holder in your graph (built via the vendored `create_graph`/`add_edge`/`has_cycle` API) and check `has_cycle()` immediately — exit 1 the instant a cycle appears. If compatible, the transaction becomes a new holder instead. Enforce the "blocked transaction's later ops are ignored" rule by tracking a per-transaction blocked flag. Exit 0 only if the whole schedule file is consumed without any cycle. Compile linking `-L/app/cgraph-1.0 -lcgraph`, then test against every file in both `/app/corpora/clean/` and `/app/corpora/evil/` and confirm exit codes match expectations before considering it done.
