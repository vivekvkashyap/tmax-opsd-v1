Extract the specification before writing any SQL. Run OCR (e.g. `tesseract /app/schema_rules.png`) to recover the exact table name, column names, and the window-function definition (partition column, order column and direction, and frame bounds) described in the image — don't assume typical names, read them from the OCR text. Build the C program around `sqlite3_prepare_v2` with a parameterized query (`?` placeholders bound via `sqlite3_bind_int`) for `u_id`, `limit`, and `offset`, embedding the window function exactly as specified (matching partition/order/frame precisely, since even a slightly different frame like ROWS vs RANGE or an off-by-one in preceding rows will produce different numbers) but still ordering the final result set by the timestamp column descending with the given `LIMIT`/`OFFSET` applied after the window computation. Format each output row's floating-point fields to exactly 2 decimals with `printf("%d,%.2f,%.2f\n", ...)`. Compile with `gcc ... -lsqlite3 -o /home/user/etl_query`, then sanity-test it yourself against a few `(u_id, limit, offset)` combinations and manually cross-check a couple of rows' rolling averages by hand against the raw table data before considering it done.
