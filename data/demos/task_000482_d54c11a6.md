Build this in the given order and test each piece before wiring it together. For the Expect script, `spawn /home/user/bin/interactive_setup`, then use `expect "Enter Admin Email: "` / `send "admin@backup.local\r"`, and repeat that exact pattern for the SMTP Port and Deployment Stage prompts, ending with `expect eof` so the script doesn't exit before the tool finishes writing its config file. For deploy.sh, implement idempotency with a real check, not just file existence: `grep -q "STAGE=staging" /home/user/mailer_config.txt 2>/dev/null` combined with a file-exists test, printing the exact string "Configuration already applied." only when both conditions hold, and otherwise invoking the expect script (e.g. `expect /home/user/setup_mailer.exp`) — then actually run deploy.sh once so mailer_config.txt exists before you write check_spool.c. In the C program, parse the config file's `key=value` lines (simple `sscanf`/`strtok` on `=`), opendir/readdir over the spool directory, and for each regular file read its full contents and search for the exact substring `"To: list@backup.local"` (e.g. with `strstr`), counting matches per file not per occurrence within a file. Match the final report's format exactly — same labels, one value per line, no trailing blank line differences — since the grader will byte-compare it.