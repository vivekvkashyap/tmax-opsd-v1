Build this as a single-pass awk pipeline invoked from a bash script, not a series of separate passes that buffer the whole file repeatedly. First recover the filter rule: run OCR on the image (e.g. `tesseract /app/rules.png -`) to read which event name is banned, rather than guessing it — use that recovered string as the literal comparison value in your filter, don't hardcode a value you haven't actually read from the image. In the awk logic: skip rows whose Event column matches the banned value exactly; uppercase the Event field; format Value with `sprintf("%.2f", ...)`; track a per-UserID "best record" using an associative array keyed by UserID, replacing the stored record whenever the current row's Timestamp is greater-than-or-equal to the stored max (so later rows win ties, satisfying "keep the latest on equal timestamp"); at END print the reordered `UserID,Timestamp,Event,Value` lines. Finally pipe through `sort -t',' -k1,1` for the UserID sort. Make the script executable with `chmod +x` and ensure it prints nothing but the CSV rows — no debug echoes.