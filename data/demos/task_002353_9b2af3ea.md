Work the two parts independently and verify each with direct queries against `/home/user/logistics.db` before writing final scripts. For Part 1, write `/home/user/optimize.sql` with `CREATE INDEX` statements covering the join/filter paths the reporting query needs: an index on `deliveries(courier_id)` (and/or a composite with `created_at`) for the join to couriers and windowed ordering by time, and an index on `reviews(rating)` for filtering 5-star reviews. For Part 2, build the report as a single SQL query using CTEs and window functions rather than looping in Python: use `AVG(...) OVER (PARTITION BY courier_id ORDER BY created_at ROWS BETWEEN 4 PRECEDING AND CURRENT ROW)` to get each delivery's trailing rolling average, then pick each courier's most recent delivery's value (e.g. via `ROW_NUMBER() OVER (PARTITION BY courier_id ORDER BY created_at DESC) = 1`) as `recent_avg_time`; separately count reviews with `rating = 5` joined via `deliveries.id = reviews.delivery_id`, grouped by courier, defaulting to 0 with a LEFT JOIN/COALESCE for couriers with none; then rank couriers with `DENSE_RANK() OVER (ORDER BY recent_avg_time ASC)`. In `/home/user/report.py`, run this query via `sqlite3`, round `recent_avg_time` to 2 decimals, take the top 10 ordered by `overall_rank` then `courier_id`, and write `/home/user/metrics.csv` with the exact header given. Test with a few manual spot-checks (compute one courier's rolling average by hand) to catch window-frame or join-cardinality mistakes.