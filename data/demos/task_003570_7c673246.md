Build the script as three independent checks, each with its own recovery method. Issuer ID: don't try to parse the binary by hand — run `strings /home/user/legacy_auth_bin | grep ISSUER_ID` (or `readelf -p .rodata` on the binary) via `subprocess` from Python, then split on `=` and take everything after it as the value. Certificate chain: use the `cryptography` library or shell out to `openssl verify -CAfile ca.pem -untrusted intermediate.pem leaf.pem` and interpret the exit code/output to set `chain_valid` true/false — don't assume it's valid just because files exist. JWT audit: for each `.jwt` file in `/home/user/tokens/`, split on `.`, base64url-decode (remember to add `=` padding since JWT padding is often stripped) the first segment to get the JSON header, parse it, and flag the file if `alg` matches `none` case-insensitively (use `.lower() == "none"`). Collect flagged filenames, sort alphabetically, and write everything into the exact JSON structure requested at `/home/user/audit_report.json`, then execute the script so the file is actually produced.
