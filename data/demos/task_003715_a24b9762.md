For the Makefile, the fix is adding the flags needed to produce a Python-loadable shared object: compile with `-fPIC` and link with `-shared` (e.g. `gcc -fPIC -shared -o libparser.so parser.c`) — a plain `gcc -o libparser.so parser.c` produces an executable-like ELF that `ctypes.CDLL` can't use correctly. For `classifier.py`, read the file's full contents as one string and check for the presence of any of the literal characters `;`, `|`, `&`, `$` or the substring `sudo`; if found, `sys.exit(1)`, otherwise call the loaded library's `parse_cmd` via `ctypes.CDLL('/home/user/parser_project/libparser.so')` (set `.argtypes`/`.restype` appropriately, encode the string) purely as a proof-of-load step, then `sys.exit(0)` — keep the actual security decision entirely in Python, not delegated to C. For the DTMF task, don't guess digits: use a DTMF-decoding approach (analyze `/app/voicemail.wav` with `scipy`/`numpy` FFT per audio frame, matching detected frequency pairs against the standard DTMF frequency table for 0-9,*,#) to programmatically recover the digit sequence, then write exactly that recovered string to `/home/user/pin.txt`.