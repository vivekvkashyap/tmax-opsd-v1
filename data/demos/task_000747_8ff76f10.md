For Part 1, do not guess the port/certs path — run OCR (e.g. `tesseract /app/arch_config.png stdout`, or another available OCR tool) on the image to extract the literal text it contains, then use those recovered values (an upstream port number and a certificate directory path) when writing the Nginx config in Part 3; verify the OCR output looks sane (a plausible port number, a plausible directory path) before trusting it. For Part 2, build waf_classifier.py by parsing the raw HTTP request text into its request line, headers, and body, then check: the URI/query string for path-traversal sequences (literal `../` and URL-encoded `..%2f`/`..%2F`) and SQL-injection keywords (case-insensitive `UNION SELECT`, boolean patterns like `1=1`), and the headers/body for XSS markers (`<script>`, `javascript:`) — reject on any match, accept otherwise, printing exactly ACCEPT or REJECT with no extra output. Validate against both /app/corpus/clean and /app/corpus/evil directories yourself before finishing, since it must be 100% correct on both. For Part 3, make deploy.sh idempotent: check if an nginx process is already running with your config (e.g. via a stored PID file under /home/user/nginx_data/) and `nginx -s reload -c ...` if so, else start fresh; point ssl_certificate/ssl_certificate_key at the recovered cert dir and proxy_pass at 127.0.0.1:<recovered port>, listening on 8443.
