Run OCR on the spec image first and read it carefully before writing any code: `tesseract /app/processing_rules.png -` (or equivalent). Transcribe every rule precisely — this image is the actual specification for your script's behavior, so treat ambiguous OCR output as a signal to re-run OCR or zoom/preprocess the image rather than guessing at rule details. Then implement `/home/user/process_gcode.py` reading line-by-line from stdin: strip everything from the first `;` onward, strip leading/trailing whitespace, skip lines that become empty, and apply the specific per-command transformations the image describes (e.g. special-casing a bare command, and capping/replacing a named parameter's value when it exceeds a threshold, preserving single-space separation between tokens when you rewrite a line). Since grading is fuzz-equivalence against a hidden oracle over thousands of generated cases, be exact about edge cases: trailing whitespace before a comment, lines that are only a comment, and parameters with non-numeric or missing values (leave those untouched, don't crash). Add `#!/usr/bin/env python3` and `chmod +x` the file, and test manually by piping varied GCode samples through it before trusting the fuzz run.
