Work through this in stages and verify each before moving on. First, write an `expect` script that spawns `/app/legacy_configurator`, matches each interactive prompt (username/password/dashboard ID) with `expect` patterns, and sends the given credentials with `send`; run it and confirm `config.json` actually appears in `/home/user/`. Second, for the video analysis, use `ffmpeg` to extract frames (e.g. `-vf select` or dump all frames as PNGs), then for each frame compute the pixel at (width/2, height/2) — Pillow's `Image.open(...).getpixel(...)` is simplest — and count frames matching exactly RGB(255,0,0); do not eyeball a sample, iterate every extracted frame. Build the HTTP server (`http.server` or `flask`) to check the `Authorization: Bearer <dashboard_id>` header from the parsed config before returning the JSON. Finally, write the unit file to `~/.config/systemd/user/video-metrics.service` with `[Unit] After=network.target`, `ExecStart` pointing at your Python script with an absolute interpreter path, then `systemctl --user daemon-reload`, `enable --now`, and confirm with `systemctl --user is-active` and a real curl against `127.0.0.1:9090/metrics` before declaring done.