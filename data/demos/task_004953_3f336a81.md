For the log-pattern detection, parse `/home/user/server_logs/auth.log` line by line, extracting the source IP from each "Failed publickey"/"Accepted publickey" line for user admin, and track a per-IP state machine: maintain a running count of consecutive "Failed publickey for admin" entries for that specific IP, reset to 0 on any other log line mentioning that IP (this handles the "no intervening entries" requirement — an unrelated line for the same IP between failures and the success must break the streak), and flag the IP as compromised only when you see exactly 3 failures immediately followed (with zero other lines for that IP in between) by one "Accepted publickey for admin" from the same IP — don't just count total failures/successes per IP, order and adjacency matter. For the permission audit, walk `/home/user/home_backup/` with `os.walk`, read each file's content (or just enough bytes) to check for the two private-key header strings, and use `os.stat(path).st_mode & 0o777` to compare against `0o600` (keys) or `0o700` (any directory literally named `.ssh`) — flag anything with extra bits set for group/other. Sort all three output lists lexicographically before writing exact-schema JSON to `/home/user/audit_report.json`.
