Chain this as a strict crypto pipeline where each step's output feeds the next — verify at each stage before moving on. First validate the cert chain (e.g. `openssl verify -CAfile ca.crt dropper.crt`), then compute the SHA256 fingerprint of dropper.crt's DER encoding (not the PEM text) — in Python use `cryptography.x509.load_pem_x509_certificate` then `.fingerprint(hashes.SHA256())`, or via openssl `openssl x509 -in dropper.crt -outform DER | openssl dgst -sha256`; take the resulting hex string, lowercase, and use its first 32 characters as ASCII bytes for the AES key. For decryption: the first 16 bytes of `config.enc` are the IV, the rest is ciphertext; decrypt AES-256-CBC with PKCS7 unpadding (Python `cryptography` library's `Cipher(algorithms.AES(key), modes.CBC(iv))` plus `padding.PKCS7(128).unpadder()`). Confirm correctness by computing MD5 of the plaintext and diffing it against `config.md5` before writing output — this catches key/IV derivation mistakes early. Finally, for each IP line in the plaintext, emit exactly one `iptables -A INPUT -s <ip> -j DROP` line (no shebang, no blank lines) to `/home/user/firewall_rules.sh`, then `chmod 700` it.