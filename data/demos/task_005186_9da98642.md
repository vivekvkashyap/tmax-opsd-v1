Start by identifying the ABI bug precisely: the C function now takes/returns `double`, so the ctypes `argtypes`/`restype` in grpc_server.py must both be changed to `ctypes.c_double` (currently `ctypes.c_int`) — leaving even one side as int will corrupt the interpretation of the raw double bits and produce garbage or zero. Compile the shared library, generate the gRPC stubs with `python -m grpc_tools.protoc` (or grpcio-tools equivalent) referencing the .proto's message field types, and fill in the two TODOs in deploy_check.py: open a grpc insecure channel to localhost:50051 and call Process with 4.0, then open a websocket connection to ws://localhost:8765 and send the formatted "SUCCESS: <value>" string. When starting ws_server.py and grpc_server.py in the background, don't fire deploy_check.py immediately afterward — a naive attempt races the server startup; add a short wait or retry/poll until each port is accepting connections before running the check script. Confirm success by reading back /home/user/deploy_log.txt yourself.
