Two separate deliverables. First, fix the vendored dependency: open `/app/fast_solver/Cargo.toml`, find the invalid `serde` version constraint (something like `"1.99.0"` that doesn't exist on crates.io) and change it to a real published version (e.g. `"1.0"`), then confirm the package builds with `cargo build` inside `/app/fast_solver`. Second, build `/home/user/validator` as a new Cargo project whose binary takes a file path argument, reads one float per line, and decides ACCEPT/REJECT. The task text tells you the actual failure mode: the solver's internal metric divides by the variance of the input, so it panics/NaNs exactly when variance is `0.0` — i.e. when every value in the file is identical. You don't need to link `fast_solver` at all; just compute the sample variance yourself (mean, then average squared deviation) and print `REJECT` if it's exactly `0.0`, else `ACCEPT`. Validate against the provided example corpora (`/app/corpora/clean/`, `/app/corpora/evil/`) by running your compiled `target/release/validator` on each file and confirming clean→ACCEPT, evil→REJECT, watching especially for single-element files and floating-point tie edge cases.