Treat `/app/artifact_embedder` as a black-box feature extractor: run it against every file in train_clean/ and train_evil/, capturing the 5 floats each time into a scratch table — don't try to disassemble the binary, just characterize its output distribution empirically. Compare the two classes per-dimension (min/max/mean, and especially the sum of the 5 values) to find a threshold or centroid rule that cleanly separates clean from evil with margin, since simpler rules generalize better to a hidden test set. Implement classifier.sh to call the embedder on `$1`, parse the floats with awk, compute your chosen statistic, and compare against the threshold using awk's numeric comparison (not string comparison, which breaks on floats), exiting 0 for clean and 1 for poisoned. Validate against every training file before finishing, and leave margin in your threshold since the hidden set is drawn from the same distribution but isn't identical to training data.
