Build this as a small C pipeline plus shell glue, not a monolith. First write a WAV parser in C that reads the 16-bit PCM header to get sample rate/format, then walks samples in fixed 50ms buckets (samples_per_bucket = sample_rate * 0.05), computing RMS per bucket (sqrt(mean(sample^2))) and storing bucket_index -> RMS. For each CSV row, map its [timestamp_ms, timestamp_ms+duration_ms) window onto overlapping buckets and average their RMS to get that event's signature — write this as a second C pass (or the same program) that reads `/app/noisy_events.csv`. For deduplication, sort/group events and compare every pair (or adjacent-in-time pairs after sorting by timestamp) using both conditions simultaneously: |start_a - start_b| <= 150ms AND |sig_a - sig_b| < 5.0; when both hold, keep only the higher confidence_score record. Finally have your C program (or a thin bash wrapper calling it) emit the surviving records as SQL `INSERT` statements or a CSV, and pipe that into `sqlite3 /home/user/clean_events.db` to create the `events` table and bulk-load it. Validate against the visible beeps' approximate timestamps before trusting the F1 score.
