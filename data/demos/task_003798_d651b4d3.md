Work through the PR fixes in order, testing each in isolation before moving on. First, compile the C file with `gcc -shared -fPIC -o libfastmath.so fast_inverse_sqrt.c` and inspect the C function's parameter/return type — the classic bug here is a ctypes/C mismatch: if the C function uses `float` (32-bit), the Python wrapper must declare `argtypes`/`restype` as `ctypes.c_float`, not `ctypes.c_double`, or you'll get garbage/segfaults from misaligned byte reinterpretation. Second, fix the Flask route so the URL segment is converted to a float before reaching the math function — either use Flask's `<float:value>` converter in the route decorator, or explicitly cast the string parameter with `float()` inside the handler. Third, write the hypothesis property test using `@given(st.floats(min_value=0.1, max_value=1000.0))`, decorate it as `test_inv_sqrt_property`, call `get_inv_sqrt`, and assert `abs(1.0/(result**2) - x) / x < 0.1`. Install Flask/pytest/hypothesis via pip, run `pytest -v > test_results.log 2>&1`, and check the log shows all tests passing before considering the task done.
