Build `/home/user/pipeline.sh` as a chain of shell/CLI stages, each writing an intermediate file you can inspect independently before wiring them together. Extract frames with `ffmpeg -i /app/reaction.mp4 -vf fps=1 frame_%03d.png`, then for each frame compute mean grayscale intensity via `magick frame_XXX.png -colorspace Gray -format "%[fx:mean*255]" info:` (or `convert ... -format ...`), mapping frame N to `time_sec=N`. Clean the sensor CSV with `awk -F, 'NR==1 || ($2!="NULL" && $2!="")'` to drop bad `sensor_a` rows, then trim whitespace per field with `gsub(/^[ \t]+|[ \t]+$/,"",$i)` in an awk pass. Join intensity and cleaned sensor data on `time_sec` (a `join` command on sorted files, or an awk associative array keyed by `time_sec`, works well), then compute `anomaly_flag` as `1` only when intensity is strictly `>120` AND `sensor_a` is strictly `>45.0`, else `0` — use awk's numeric comparison, not string comparison. Sort the final rows numerically by `time_sec` (`sort -t, -k1,1n`), prepend the exact header `time_sec,sensor_a,intensity,anomaly_flag`, and write to `/home/user/results.csv`. Make the script executable and run it once to confirm it regenerates the output cleanly from scratch.