Split the work: first make the three services boot cleanly and talk to each other, then reproduce the C tool's exact byte behavior independently (never call or inspect the oracle binary itself — treat it as a black box you can only compare outputs against via your own tests). For Stage 1, edit `redis.conf` to add `requirepass artf_secure_99`, edit `nginx.conf` to change `listen 80` to `listen 8080`, set `root`/serve path to `/home/user/repo/data`, and relocate `pid`/`client_body_temp_path` etc. into `/home/user/repo/run/` (create both dirs first) so nginx can run unprivileged; update the Flask app's env vars for the new Redis password and the `http://127.0.0.1:8080` backend, then start each service backgrounded and curl `GET /health` on port 5000 to confirm. For Stage 2, write a C parser that loops reading a 4-byte magic (`ARTF`), 1-byte type, 4-byte little-endian payload size, then payload; skip/drop any chunk with type `0x02`, write everything else to a `mkstemp`-created temp file in the output's directory, then `rename()` it atomically onto the output path; validate magic/lengths and return exit code 1 on any malformed input instead of crashing. Test against your own hand-crafted chunk files (valid, truncated, bad magic) before considering it done.
