Approach this in order: (1) Fix `build.sh` first — a naive `for f in $(ls src/*.cpp)` word-splits on whitespace and breaks on any filename containing a space; instead glob directly (`for f in src/*.cpp; do g++ -c "$f"; done`, always quoting `"$f"`) or simply compile with `g++ src/*.cpp -o bin/classifier` inside a script that still quotes paths, and ensure `bin/` is created before linking. (2) OCR `/app/malware_seed.png` (e.g. with `tesseract`) to read the handwritten "THRESHOLD=XX.XX" value and set that exact number as the `#define THRESHOLD` macro in `classifier.cpp` — don't guess a value. (3) For the intermittent-scoring bug, grep the scoring function for locals declared without initializers (e.g. a `float` accumulator declared then incremented in a loop without being set to `0.0f` first) — uninitialized stack memory explains run-to-run variance; initialize every accumulator explicitly. (4) After fixing, compile and run the binary against every file in `/app/corpora/evil/` and `/app/corpora/clean/`, checking exact stdout (`EVIL`/`CLEAN`) and iterating until 100% of both corpora classify correctly — don't rely on a handful of manual spot checks.