Use `sqlite3` (via Python's built-in `sqlite3` module) to read the `sensors` and `readings` tables into pandas DataFrames, and `pandas.read_csv` for the calibration file. Merge readings with sensors on `sensor_id` to get `sensor_type`, then left-merge with the calibration CSV on `sensor_id`, filling missing `correction_factor` with 1.0 (`fillna(1.0)`) rather than dropping those rows. Compute `corrected_value = raw_value * correction_factor`. Derive the grouping date by truncating the timestamp string/column to its date part (e.g. `pd.to_datetime(timestamp).dt.date` or string-slice the first 10 chars), then `groupby(['date','sensor_type'])['corrected_value'].agg(['max','min','mean'])`, rounding each aggregate to 2 decimals with `round(2)`. Sort the resulting frame by date then sensor_type before converting to a list of dicts with the exact required keys (`date`, `sensor_type`, `max_val`, `min_val`, `avg_val`), and dump with `json.dump` to `/home/user/summary.json`. Watch for float rounding edge cases (e.g. `round()` banker's rounding) if a value lands exactly between decimals.