Do OCR on the image yourself (e.g. `tesseract /app/suspicious_login.png stdout`) rather than guessing the payload — the extracted text is itself base64, so decode it with `base64 -d` to reveal the `EXPLOIT_TOKEN:<token>|HASH:<hash>` structure, then independently compute `sha256sum` of just the token string and confirm it matches the embedded hash before trusting the token value you extracted (this catches OCR misreads). For the C TCP server, open a listening socket on `127.0.0.1:8080`, and in an accept loop (no threading needed) read the client's data into a buffer, then check whether it contains both the literal substring `GET /login?redirect=` AND the recovered token string somewhere in the same buffer (order doesn't need to be adjacent — the token can appear anywhere after that substring in the request per the spec) — respond with the exact alert string and close if both conditions hold, otherwise respond `OK\n` and close. Be careful to use a byte-safe substring search (like `memmem` or manual scanning) since the payload isn't guaranteed to be null-terminated cleanly, and make sure both response strings match byte-for-byte including the trailing newline. Compile, then manually test with `nc`/`printf | nc` against a few crafted requests before backgrounding it.
