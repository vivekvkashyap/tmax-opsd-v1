Handle audio transcription, dedup, and the HTTP service as separable pieces you can test independently. Transcribe `/app/call_recording.wav` with a CLI tool (e.g. `whisper` or `whisper.cpp`) and read the output text yourself to pull out the customer name, account ID, and reported issue — don't hardcode assumed values, parse them from whatever the transcript actually contains (account IDs spoken as "ACC hyphen nine nine two eight one" need normalizing into the `ACC-99281`-style format actually used in the CSV, so write logic to convert spelled-out digits/hyphens rather than assuming a fixed string). For dedup, implement Levenshtein distance in Go (either from scratch with a DP table or a small package) and group records where both "Reported Issue" and "Customer Name" have distance <= 3, keeping one representative per group and logging the count removed to `/home/user/pipeline.log` in the exact required format. Append the audio-derived record to the deduplicated set. For the HTTP service, use Go's `net/http` on `127.0.0.1:8080`, check the `Authorization` header equals `Bearer secret-agent-token` and return 401 otherwise, and serve `/api/records?account_id=...` as JSON — run it in the background (e.g. via `nohup`/`&`) and verify with `curl` including the auth header before finishing.
