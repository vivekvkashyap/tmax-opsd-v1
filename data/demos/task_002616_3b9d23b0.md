Build `archive.py` in clear stages: use `ffmpeg -i /app/surveillance.mp4 -vf "fps=2,scale=iw/2:ih/2" frame_%04d.png` (or an equivalent OpenCV loop) to produce the resized frame set in a temp dir, then split the resulting filenames into 4 roughly-equal chunks and spawn 4 `multiprocessing.Process` workers, each compressing its assigned frames (e.g. `zlib.compress` on the raw file bytes) and appending entries to the shared `/home/user/video_archive.bin`. The critical correctness point is the concurrent-write safety: each worker must open the archive in append/binary mode and wrap every write in `fcntl.flock(fd, LOCK_EX)` / unlock, writing the record atomically as `struct.pack('>H', len(name_bytes)) + name_bytes + struct.pack('>I', len(compressed)) + compressed` so partial writes from different workers can never interleave. For `extract.py`, read the same binary framing sequentially, and before writing each file, sanitize the stored name — reject or strip any entry whose resolved path (via `os.path.normpath`/`os.path.abspath` join against the target dir) escapes the target directory or is absolute, printing a warning and skipping it rather than raising. Keep both compression and resizing aggressive enough to land the archive under 5MB while preserving enough visual fidelity for SSIM >= 0.90 against the reference 50%-scaled frames.