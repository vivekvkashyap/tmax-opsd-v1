Locate the adaptive step-size line in `diffusion.c` and check its direction against the stated invariant: when the local error `err` exceeds tolerance `tol`, the step must shrink; when `err` is below `tol`, it should grow. Standard adaptive-Euler step control scales `dt` by the square root of the ratio between tolerance and error — verify your fixed formula produces `dt_new < dt` when `err > tol` and `dt_new > dt` when `err < tol` (sanity-check by hand with `err=2*tol` and `err=0.5*tol` before compiling). After editing, compile exactly with `gcc -O3 /home/user/diffusion.c -lm -o /home/user/diffusion`, run it, and confirm the output has no `NaN` and no "Step size too small" message, and that the 5 values sum to roughly the conserved total mass (10.0) as a sanity check that the fix is correct before writing `/home/user/final_state.txt`.
