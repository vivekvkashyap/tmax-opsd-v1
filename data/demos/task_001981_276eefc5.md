Run `tesseract /app/rules.png stdout` first and inspect the raw text output before writing any filtering logic — extract the exact target directory, min/max size bounds, extension, and the required content substring/key rather than guessing them. In `run_backup.sh`, apply all filters as a conjunction: iterate files in the target dir matching the extension, use `stat -c%s` (or `wc -c`) to check the byte-size range, and `grep -F` for the required JSON key/value pair — only files passing every check should proceed. Convert each matching JSON file to YAML (e.g. with `yq -p json -o yaml`, or a small Python `json.load` + `yaml.dump` if `yq`/`PyYAML` need installing) into a staging directory, preserving base filenames with a `.yaml` extension. Build the manifest by looping over the converted files and running `sha256sum` per file, formatting each line as `filename.yaml,hash` (comma-separated, not the default `hash  filename` order). Finally `tar -czf /home/user/incremental.tar.gz -C <staging_dir> *.yaml`. Since grading is F1 on the archive's file set, double-check the filter logic excludes borderline files (wrong size, missing key) before packaging, since both false positives and false negatives cost you.