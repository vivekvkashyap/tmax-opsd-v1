Set up the Rust project first and get a correct-but-simple implementation working, then focus on release-mode performance since the verifier both checks numerical accuracy and times a large-file run. Use `cargo new` at /home/user/audio_pipeline, add `hound` (and `serde`/`serde_json` for output) to Cargo.toml, and parse CLI args as exactly two positional paths. Read all i16 samples, chunk into non-overlapping windows of 1024 (drop any short final chunk with `chunks_exact(1024)` rather than `chunks`, which would keep a short trailing chunk), and normalize samples to floats consistently with how you'll interpret the 0.05 RMS threshold (e.g. divide i16 by 32768.0 before computing RMS, since the threshold is clearly meant for a roughly [-1,1] range). Compute RMS per frame, drop frames below the noise-gate threshold, and for kept frames compute ZCR as the fraction of adjacent-sample-pairs with opposite sign, treating exactly-zero as positive per the spec. Preserve original frame_index and temporal order in the output JSON array. Build with `cargo build --release` so the binary lands at target/release/audio_pipeline, and sanity-check runtime on a large synthetic WAV (e.g. tens of MB) to confirm it comfortably beats the 1.5s threshold — avoid unnecessary allocations or per-sample I/O in the hot loop.