Structure this as: (1) a sliding-window matching function, (2) a vectorized Monte Carlo loop, (3) JSON output — and get the MMS definition exactly right before simulating. MMS for a primer of length L against a sequence of length N is the max, over all N-L+1 starting offsets, of the count of positions where primer[i] == window[i] (not the longest common substring, not edit distance — a simple positional match count per window, maximized over window position). Implement this once and reuse it for both the real target and every random sequence. For the Monte Carlo step, generate 50,000 random sequences of the target's length with each of A/C/G/T equally likely (e.g., `numpy.random.choice(['A','C','G','T'], size=(50000, N))`), vectorize the match-counting across windows/sequences with numpy broadcasting rather than nested Python loops per-sequence (50,000 sequences x many windows in pure Python loops will be slow — prefer array comparisons), then take the empirical mean and std of the 50,000 MMS values and compute `z = (observed - mean) / std`. Because of Monte Carlo noise, don't hardcode expected numbers — just make sure your simulation code is correct and reasonably large-sample; the grader tolerates ±0.05. Write the four fields to `/home/user/alignment_stats.json` with the exact key names given.