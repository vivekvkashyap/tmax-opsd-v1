First run `tesseract /app/waf_rules.png stdout` (or similar) to OCR the bulletin text and read off the exact malicious User-Agent string and restricted URI path it names — do not guess these values, extract them verbatim from the OCR output and double check for OCR artifacts (e.g. confusable characters) since the classifier must match exactly. Then write `/home/user/classifier.sh` so it takes the request-file path as `$1`, reads the file contents, and exits 1 if the content contains the extracted User-Agent string OR the extracted URI path as a substring (e.g. `grep -qE` for either pattern, or an equivalent check in another language wrapped by the shell entry point), otherwise exits 0. Since it will be tested against a large corpus for both false positives and false negatives, match on exact substrings only — don't over-generalize with fuzzy patterns that could false-positive on similar but benign strings. `chmod +x classifier.sh`. Separately, create the dummy NIC with `sudo ip link add waf0 type dummy && sudo ip addr add 10.100.0.1/24 dev waf0 && sudo ip link set waf0 up`, and generate the self-signed cert/key pair with `openssl req -x509 -newkey rsa:2048 -nodes -keyout /home/user/certs/tls.key -out /home/user/certs/tls.crt -days 365 -subj "/CN=localhost"` after `mkdir -p /home/user/certs`.