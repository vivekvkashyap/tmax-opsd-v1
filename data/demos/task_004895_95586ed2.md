First recover the version constraint from `/app/requirements.png` using OCR (`pytesseract`/`tesseract`, installing it if missing) and parse the recovered text into a semver tuple for comparison — don't hardcode a guessed version. Fix the Makefile by inspecting its current rule for `libmathops.so`: add `-fPIC` and `-shared` to the compile/link flags (position-independent code is required for a shared object), append `-lm` since matrix/math functions need libm at link time, and add `-O3` to the optimization flags to satisfy the performance requirement. In `test_ops.py`, load the library with `ctypes.CDLL`, call `get_version()` (set `restype` correctly, likely `c_char_p`), and assert its parsed version tuple is `>=` the OCR'd requirement using tuple comparison rather than string comparison. Run the existing `hypothesis` tests to confirm the C matrix transform passes property checks. For the benchmark, time 1,000,000 calls to `matrix_transform` on a random 3x3 matrix using `time.perf_counter()`, divide by iteration count, and write the average as a float to `metric.txt` — verify the optimized build brings this well under the pass threshold before finishing.
