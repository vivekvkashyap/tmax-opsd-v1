In `/home/user/plan_route.py`, connect to `/home/user/backup_network.db` with `sqlite3`, and use a parameterized query (`WHERE backup_id = ?`) to fetch the source/dest datacenter ids for the given backup_id — never string-format the id into SQL. Query `network_links` filtered to `status = 'UP'` only (the critical gotcha here is that a shorter-hop path may include a DOWN link and must be excluded entirely from the graph, not just deprioritized), and build a weighted directed adjacency list keyed by datacenter id using `latency_ms` as edge weight. Run Dijkstra's algorithm (a `heapq`-based priority queue works well) from source to destination over only the UP edges, tracking predecessors to reconstruct the path, then map the id sequence back to datacenter `name`s via the `datacenters` table. Write `/home/user/route_<backup_id>.json` with `backup_id`, `total_latency_ms` (int), and the ordered `path` name list, matching the exact key names and structure specified. Run the script for `BKP-9001` as instructed and sanity-check the resulting JSON by tracing the path/weights against the DB contents yourself.