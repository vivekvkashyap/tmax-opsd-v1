Fix the six subtasks in dependency order, testing each with `cargo build`/`cargo test` before moving on. In src/integrator.rs, the bug is that `adaptive_step` computes `(err / tolerance).sqrt()` — a larger error should shrink dt, so the ratio must be inverted to `(tolerance / err).sqrt()`; make that single swap and re-verify against the clamp logic already present. For src/main.rs, parse init.pdb by reading its lines, finding the first line starting with "ATOM", and slicing the fixed-width PDB columns for the X coordinate (PDB format has fixed column positions — don't just split on whitespace naively without checking, though these ATOM lines are whitespace-separated enough to `split_whitespace()` and take the appropriate field index) and parsing it as f64 for initial_x. Implement calculate_kl_divergence as a straightforward loop summing `p[i] * ((p[i]+1e-9)/(q[i]+1e-9)).ln()` (or apply the epsilon exactly as specified) over matching-length slices. Run `cargo run` to produce simulated.csv, read target.csv the same way main.rs does, compute and write the KL value alone to kl_result.txt. Add a `tests/integration_test.rs` that calls into your simulation/KL logic (may need to expose functions via `pub` and a lib target) and asserts divergence < 0.05, and confirm `cargo test` passes. Finally write plot_dist.py using matplotlib to plot both CSV columns and savefig to distribution.png.
