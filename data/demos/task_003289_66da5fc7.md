Write a Python script using `sqlite3` and `json`. First, deduplicate `papers`: for each distinct `id`, parse `metadata_json` for every row sharing that id and keep only the one with the maximum `"updated_at"` value (e.g. group rows by id in Python, or use a window/subquery in SQL comparing the JSON-extracted updated_at). Second, deduplicate `citations` by taking the distinct set of `(source_id, target_id)` pairs. Third, build a directed graph only from edges where both endpoints are in the deduplicated/valid papers set. Run a standard shortest-path search (BFS is enough since edges are unweighted) from `"P-START"` to `"P-END"`; when multiple shortest paths tie in length, break ties by choosing the lexicographically smallest sequence of node IDs — this means exploring/comparing candidate paths as tuples, not just taking whichever BFS happens to find first (a naive BFS may return an arbitrary equal-length path). Once you have the path, collect the `"categories"` list from each node's (deduplicated) metadata_json along the path, union them, dedupe, and sort alphabetically. Write `{"path": [...], "categories": [...]}` to `/home/user/path_results.json` with exactly those two keys.