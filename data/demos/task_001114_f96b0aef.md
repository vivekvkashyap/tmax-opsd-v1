Treat `/app/sim_binary` as a black box you drive via its documented CLI/output contract rather than trying to inspect its stripped internals: run it manually first with a small mesh_size to confirm it accepts an integer argument and writes `sim_output.h5` (containing dataset `/covariance_matrix`) into the current working directory, and note that you must run it from (or chdir/copy the output from) a directory you control since your service needs to read that file right after invocation. Build the HTTP service (Flask/Express/etc.) listening on `127.0.0.1:8080` with a POST `/run_sim` handler that: parses `mesh_size` from the JSON body, invokes `/app/sim_binary <mesh_size>` as a subprocess and waits for it to exit, opens the resulting `sim_output.h5` with an HDF5 library (h5py in Python, or an equivalent binding) and loads `/covariance_matrix` into a 2D array, runs SVD (`numpy.linalg.svd` or your language's equivalent) and takes the largest singular value from the resulting singular-value vector, then responds with `{"dominant_singular_value": <float>}`. Handle sequential requests safely by deleting/overwriting `sim_output.h5` between runs (e.g. run each simulation in its own temp working directory, or remove the file before invoking the binary again) so concurrent/successive calls never read a stale or partially-written file.