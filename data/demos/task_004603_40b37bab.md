Structure the C++ transform as three clear phases operating on parsed CSV rows, matching the pipeline's DAG semantics exactly. Phase one: parse `raw_translations.csv`, and deduplicate by grouping on the (JobId, StringId) key — for each group keep only the row with the maximum Timestamp value (not just the last-seen row; compare numerically). Phase two: for each surviving row compute `WPM = WordCount / (DurationSeconds / 60.0)` as a double. Phase three: group the deduplicated rows by TranslatorId, sort each group's rows by Timestamp ascending, then walk each group computing a rolling average of WPM over the current record and up to the 2 immediately preceding records within that same translator's sorted sequence (so the window size grows from 1 to 3 at the start of each group, it does not borrow from other translators). For output, sort the final rows first alphabetically by TranslatorId, then chronologically by Timestamp within each translator, and format WPM/RollingAvgWPM to exactly 2 decimal places (e.g. `std::fixed << std::setprecision(2)`) when writing to `/home/user/data/rolling_stats.csv`. Write `run_dag.sh` to compile with `g++ -std=c++17 -O2` into an executable named `transform` and then run it; verify your CSV output by hand-tracing a couple of translator groups against the raw input before finishing.