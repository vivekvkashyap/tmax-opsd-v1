Split this into three verifiable stages. First, fix the vendored package: open `/app/go-humanize/times.go` and look for an obviously broken string literal or directive (e.g. an unterminated quoted string) that breaks `go build` — the fix is minimal (closing the literal correctly), don't rewrite the package logic. Confirm with `go build ./...` inside `/app/go-humanize` that it compiles cleanly before moving on. Second, the data pipeline: read `loc_events.csv`, apply Unicode NFC normalization to `translated_text` (Go's `golang.org/x/text/unicode/norm`, or handle it before/without Go if easier), then deduplicate by `(event_id, lang_code)` keeping the row with the latest `timestamp` — sort or compare timestamps as actual time values, not strings, unless the format is guaranteed lexicographically sortable (ISO8601 here is, but verify). Pivot from long to wide format with fixed columns `timestamp, event_id, en, es, fr, zh`, defaulting any missing language column to the literal string `MISSING`, then sort by timestamp then event_id and write TSV to `/home/user/processed/wide_events.tsv`. Third, the server: write `/home/user/server/main.go` importing your fixed local module (use a `replace` directive in go.mod pointing at `/app/go-humanize`), load the TSV at startup, serve `GET /timeline` requiring the exact `Authorization: Bearer loc-eng-token` header (401/403 otherwise), render via `text/template` including `humanize.Time()` for relative time, 404 on missing event/lang. Launch it backgrounded and confirm with `curl` that both header enforcement and a real render work before finishing.
