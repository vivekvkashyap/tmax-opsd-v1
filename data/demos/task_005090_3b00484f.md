First locate the deadlock: open `/app/tinydb/tinydb/storages.py` and inspect `JSONStorage.write` for lock acquire/release symmetry — the perturbation typically acquires `self._lock` unconditionally but only releases it inside a branch that never executes (e.g. `if False:`) or on just one code path, so any exception or the normal successful path never releases it, deadlocking the next concurrent write. Fix it by wrapping the critical section in a `with self._lock:` block (or ensuring release happens in a `finally`) so every code path releases the lock. Reinstall with `pip install -e /app/tinydb` after editing so your fix takes effect. Then build `/app/main.py` with FastAPI: `POST /edge` validates the src/dst/weight schema (e.g. with Pydantic) and inserts into a TinyDB instance backed by `/app/graph.json`; `GET /aggregate` reads all edges, sums weight grouped by `src`, and sorts results by `out_degree` descending then `node` ascending using a tuple sort key `(-out_degree, node)`. Launch with `uvicorn main:app --host 0.0.0.0 --port 8080 &` and load-test your own `/edge` endpoint with concurrent requests before considering it done, to confirm the deadlock is actually resolved.