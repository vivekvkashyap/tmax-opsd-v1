Treat this as three independent deliverables and validate each before combining. For Stage 1, extract frames with `ffmpeg` or `opencv` (know the fps from the video, e.g. via `ffprobe`, to compute `timestamp_sec` correctly per frame), then fix `arch.py`: load `/app/model/weights.pth` with `torch.load` and inspect the state_dict's tensor shapes (`for k,v in sd.items(): print(k, v.shape)`) to infer the missing in/out channel dimensions rather than guessing, then instantiate the model and confirm `load_state_dict` succeeds with no shape mismatch before running inference. Preprocess each frame exactly as specified (grayscale, resize 64x64, normalize to [0,1]) and write the CSV. For Stage 2, join telemetry/gps/frame_scores by nearest timestamp (e.g. merge_asof or manual nearest-match), not exact-equality join. For Stage 3, write `filter_plots.py` as a genuine image-content heuristic, not a filename heuristic: load each PNG, check pixel variance/uniqueness (near-zero variance or a single dominant color across nearly all pixels signals a blank/solid image) and/or detect the presence of non-background-colored line pixels to confirm real plotted data exists; build and test this heuristic against a handful of your own solid-color and real-plot PNGs before trusting it against the hidden corpus, since accuracy must be 100% on both sides.
