Fix the microservice chain by tracing the request path end to end rather than editing blindly: inspect `/app/start_services.sh` to see what port Redis is actually launched on versus what `REDIS_PORT` env var is exported for the API server — they must agree (the API server needs `REDIS_PORT` set to Redis's actual listening port), and check `/app/nginx/nginx.conf`'s `proxy_pass` upstream port against the port the API server (`/app/api_server`) actually listens on (stated as 9000) — fix any mismatch there too. Restart each service in order (Redis, then API server with corrected env, then nginx reload) and confirm with `curl http://localhost:8080/dataset` before piping it to `/home/user/dataset.txt`. For the Go utility, follow the spec literally since a fuzz/oracle equivalence check will compare your output against a reference implementation on many random (seed, B) pairs: seed the RNG with exactly `rand.New(rand.NewSource(seed))`, draw indices with `rand.Intn(N)` in a fixed order (N draws per bootstrap sample, B samples, no extra random calls), compute each sample's mean, sort all B means ascending, and take indices `int(0.025*float64(B))` and `int(0.975*float64(B))` verbatim (Go's int() truncates) as the CI bounds, printing with exactly 4 decimal places in the specified `CI: [x, y]` format — any deviation in RNG call order will make your output diverge from the oracle on the fuzz harness.