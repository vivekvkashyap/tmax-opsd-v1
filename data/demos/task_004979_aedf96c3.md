Implement `f(t) = 50 + 20*sin(0.1*t) + 5*cos(0.5*t)` as a small Go function and reuse it for both computations so there's no drift between them. For the Monte Carlo integral, seed `math/rand` with `rand.NewSource(seed)` / `rand.New(...)` from the provided command-line seed argument, draw exactly 1,000,000 uniform samples via `r.Float64()*100` over [0,100], average f(t) over all samples, and multiply by the interval width (100) — get the sample generation order and count exactly right since the grader recomputes this deterministically with the same seed and expects an exact match. For the derivative, use the central difference formula `(f(50+h) - f(50-h)) / (2*h)` with h=0.001 exactly as specified — do not use a different h or an analytic derivative shortcut, since rounding must match the reference within its stated precision. Round both results to 4 decimal places before writing to JSON (format as a string with %.4f then parse back to float64, or otherwise ensure trailing zeros survive JSON encoding) using Go's `encoding/json`. Make `/home/user/run_pipeline.sh` executable, have it `go build`/`go run` `profiler.go` and invoke it with seed `12345`, and run the script yourself to confirm `/home/user/profile_results.json` is produced correctly.
