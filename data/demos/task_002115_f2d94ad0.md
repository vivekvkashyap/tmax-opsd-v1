Use Python's `configparser` (or manual parsing) with `pathlib.Path(...).rglob("*.ini")` to recursively find files — do not use `os.listdir` which is not recursive. For each file, guard with `.get("Metadata", "track_changes", fallback="false")` so a missing key defaults to skip, and lower-case/strip the value before comparing to `"true"`/`"false"` strings (configparser gives you strings, not real booleans, unless you use `getboolean`). Track a `{key: (version, value)}` dict so when merging settings from multiple files you always keep the entry with the higher parsed `version` int — compare integers, not strings, to avoid lexicographic bugs (e.g. "9" > "10"). After merging, get `sorted(master.items())` by key, then chunk the resulting list into groups of 2 using slicing (`items[i:i+2]`) preserving order, and write each chunk with `json.dump(dict(chunk), f, indent=2)` to `/home/user/settings_chunks/chunk_N.json`, creating the output directory first with `os.makedirs(..., exist_ok=True)`. Double check chunk numbering starts at 1 and follows alphabetical key order, not file discovery order.