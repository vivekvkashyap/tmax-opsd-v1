Build this as two independent, single-purpose scripts glued by a shell orchestrator, and test each stage's output before wiring them together. For `/home/user/mask.py`: read the JSON array, for each record compute `hashlib.sha256(email.encode()).hexdigest()` and store it as `email_hash` (drop `email`), drop `ip_address` entirely, and write a CSV via Python's `csv` module with header exactly `id,email_hash,event_type,value` — csv module handles quoting/escaping correctly, don't hand-roll string joins. For `/home/user/extract.js`: read the CSV (split lines/commas, or use a tiny manual parser since there's no external dependency), derive `value_category` as `"HIGH"` when `value > 50` else `"LOW"` (strict greater-than, so exactly 50 is LOW), drop `value`, and write one JSON object per line to `/home/user/final_features.jsonl` with keys `id` (parsed as integer, not string), `email_hash`, `event_type`, `value_category`, preserving input row order. Wire both into `/home/user/run.sh` (`#!/bin/bash`, `set -e`, call python3 then node in sequence, `chmod +x` it), then actually run it and inspect the resulting `.jsonl` to confirm the hashes and categories look right before finishing.