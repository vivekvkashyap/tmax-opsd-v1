Part 1: extract every frame with `ffmpeg` and check each programmatically (numpy/PIL) for whether *all* pixels equal `(0,0,0)` — don't rely on a coarse `blackdetect` duration filter, since isolated single-frame markers can be missed if the window is too wide; count exact matches. Part 2: build `sanitize.py` as a real tag-balance parser, not a regex blacklist — tokenize each line with `re.finditer` for `<...>` spans, classify each as an allowed open/close tag (`b`, `i`, `color=#RRGGBB` with strict 6-hex-digit validation), and reject the whole line if any tag isn't in the allowlist, is malformed, or a stack-based check finds mismatched/unclosed tags at line end. Validate per-line: one bad tag rejects only that line. Test against the clean/evil corpora until you hit 100% on both before moving on. Part 3: write `run_pipeline.sh` chaining the timestamp, video analysis, and sanitize steps, install it via `crontab` with schedule `*/5 * * * *`, and run it once manually to confirm all three output files populate correctly.