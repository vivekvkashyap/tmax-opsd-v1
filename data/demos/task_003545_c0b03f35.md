Fix the vendored package before writing any analysis code, since nothing else can run without it: open `/app/seq_affinity-0.2.1/setup.py`, find the `Extension(...)` call, and add `extra_compile_args=['-fopenmp']` and `extra_link_args=['-fopenmp']` to it (the build fails because the C source uses `#pragma omp` without telling gcc to enable OpenMP), then `pip install .` from that directory and confirm `python3 -c "import seq_affinity"` works. For analyze.py, read sequences one per line from argv[1], call `seq_affinity.calculate_k(seq)` per sequence, then solve the given 2-ODE system with `scipy.integrate.solve_ivp` using the exact solver/tolerances specified (RK45 default, rtol=1e-6, atol=1e-8) and pull `sol.y[0, -1]` for A at t=10 — do not hand-roll your own integrator, use solve_ivp so the numerics match a hidden oracle bit-for-bit. Collect all A_10 values into a numpy array, call `numpy.random.seed(42)` immediately before the bootstrap loop (seed placement matters for reproducibility), draw exactly 10,000 resamples with `numpy.random.choice(..., replace=True)`, compute each resample's mean, and use `numpy.percentile` (default linear interpolation) for the 2.5/97.5 percentiles. Print exactly `Mean,CI_lower,CI_upper` with each value formatted to 4 decimal places, matching order and rounding exactly since this is checked via bit-exact fuzz comparison.
