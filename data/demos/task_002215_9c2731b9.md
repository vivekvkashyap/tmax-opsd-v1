Work incrementally and validate each stage before moving to the next. First run `/app/legacy_extractor` on one `.bin` file and inspect the raw stdout byte-for-byte (e.g. `xxd` or `cat -A`) to see exactly how the newline bug manifests — likely an unescaped `\n` inside a field splits one logical CSV row into two physical lines with a dangling/missing quote. Write a repair step (Python, using regex or a stateful line-joiner that tracks unbalanced quotes) that stitches broken rows back together before handing the text to a real CSV parser — don't assume `csv.reader` alone will cope. For reshaping, use pandas `melt`/`pivot_longer`-style logic to go from wide (key + one column per locale) to long (key, locale, translation). For imputation, apply the `[UNTRANSLATED] ` + en_US fallback only where the translation cell is empty. For the stratified sample, fix `random.seed(42)`, select exactly 10% of unique keys, then export all locale rows for those keys. Finally write a simple Python `http.server`-based handler (or similar) for `GET /api/v1/strings?locale=...`, launch it in the background from run_pipeline.sh, and log each pipeline stage to pipeline.log. Test the endpoint with `curl` before finishing.