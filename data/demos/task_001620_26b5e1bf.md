Handle the three deliverables independently. First, transcribe `/app/voicemail.wav` with the provided `whisper.cpp` binary (`/opt/whisper.cpp/main -m /opt/whisper.cpp/models/ggml-base.en.bin -f /app/voicemail.wav`), read the transcript to identify the absolute path spoken (it will be dictated as words, e.g. "slash etc slash ..." — convert that to real path syntax), and write only that path to `/app/target.txt`. Second, patch `handle_upload` in `server.c` to check `filename` for the substring `../` or a leading `/` and `return -1` immediately before any file-write logic runs if either condition holds; compile with `gcc -o /app/server_fixed /app/server.c` and confirm it builds cleanly. Third, write `/app/redact.sh` as a `sed`/`grep -E`/`awk` pipeline reading `$1`: one regex pass to replace 16 contiguous digits or 4 groups of 4 digits joined by hyphens with `[REDACTED_CC]`, and a separate pass to replace `password=<value>` (value ending at a space or `&`) with `password=[REDACTED]` followed by the same terminator, being careful to preserve the trailing delimiter exactly. Make it executable (`chmod +x`), then test it against sample logs and measure similarity manually before submitting — the grader tolerates >=0.95 character-level similarity, not exact match, so near-miss regex edge cases are acceptable but should still be minimized.