Solve this purely with awk/sort/join — no Python. For degree centrality: stream the CSV once with `awk -F,` accumulating `outD[$1]++`, `inD[$2]++`, and marking every value seen in either column as a node (e.g. `nodes[$1]=1; nodes[$2]=1`); at `END`, iterate over the node set and print `node,in,out` using `(arr[n]+0)` (or a ternary) to default missing counts to 0 — the key correctness trap is forgetting nodes that only ever appear as a target (or only as a source) and never initializing their counter. Pipe through `sort -t, -k1,1n` and prepend the header line. For length-2 paths (a->b->c): the CSV is small enough to do an O(n^2) self-join in awk by loading all edges into parallel arrays indexed by line number, then nested-looping to match `target[i] == source[j]`, printing `source[i],target[i],target[j]` for each match — equivalent to the Cypher `(a)-[:CITES]->(b)-[:CITES]->(c)`. Sort that output with `sort -t, -k1,1n -k2,2n -k3,3n` and prepend its header. Make the script take `$1` as the input path and `chmod +x` it.