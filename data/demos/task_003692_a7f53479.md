Since parser.py's source is gone, decompile parser.pyc rather than guessing: try `pip install decompyle3` (or uncompyle6, matching the bytecode's Python version) to get readable source, or fall back to `python3 -m dis` on the .pyc's code object to read the raw bytecode instructions for the struct.unpack format string and field layout. Look specifically at how the 8-byte header per record is unpacked — the hint about "server uptime crossing a threshold" and "timestamps going backwards" points to a signed-vs-unsigned integer overflow: a struct format using signed ints (e.g. '<ii') will wrap to negative once a timestamp exceeds the signed 32-bit max, which then fails a monotonic/positive check downstream. Write a corrected /home/user/app/parser.py that unpacks the same fields but with the unsigned equivalent format (e.g. '<II'), keeping the rest of the parsing/summary logic consistent with what you observed in the decompiled bytecode. Delete or don't shadow the old .pyc's import path — make sure Python picks up your new parser.py — then run `python3 /home/user/app/main.py` and confirm it completes without the ValueError and writes /home/user/app/summary.json, checking event_count and last_ts reflect all records in trace.dat including the previously-overflowing one.
