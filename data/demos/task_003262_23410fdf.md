Fix the build first: inspect `/app/extractor/Makefile` for tab-vs-space indentation errors (Make requires literal tabs before recipe lines — a common silent break) and missing linker flags, then look at `decoder.c`'s malloc/loop bounds for an off-by-one (e.g. allocating `n` bytes but writing `n+1`, or looping `<=` instead of `<`) that causes the segfault on certain frames; fix and confirm `make` succeeds and `./decoder` runs without crashing on all sample frames before moving on. Extract frames with `ffmpeg -i /app/deploy_capture.mp4 frame_%04d.bmp`, then run your decoder over every frame, collecting only the frames that emit valid JSON telemetry (empty output means no telemetry, not an error). Parse each `{"component":..., "version":...}` line and compare against `/app/manifest.json`'s minimum versions using proper semantic-version comparison (major.minor.patch, and pre-release handling if versions include suffixes like `-beta`) — don't do naive string or float comparison, which breaks on things like "1.4.2-beta" vs "1.5.0". Collect components whose deployed version is strictly less than the required minimum into the `outdated_components` list and write `/home/user/report.json` matching the given structure exactly.
