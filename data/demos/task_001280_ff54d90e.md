The root cause is pandas upcasting integer columns that contain NaN to float64. In `/home/user/pipeline.py`, after computing the DataFrame, convert `category_id` and `token_length` to pandas' nullable `Int64` dtype (`df['col'] = df['col'].astype('Int64')`) so missing stays NA instead of becoming a float — but note `json.dump` still can't serialize `pandas.NA`/`Int64` directly, so when building the output records, explicitly cast each non-null value with `int(x)` and emit `None` for missing ones (e.g. iterate rows and build dicts manually, or use `df.where(pd.notnull(df), None)` combined with per-cell `int()` casting) rather than relying on `df.to_dict()` which will preserve floats/NaN. For the test script, load the JSON with the `json` module and assert `type(v) is not float` for every `category_id`/`token_length` value present. For the benchmark, `import timeit` and time calling `process_data` 100 times, writing the total elapsed seconds as a plain string to `/home/user/benchmark.txt`. Run the pipeline and inspect the raw JSON text (not just Python objects) to confirm no `.0` suffixes remain.