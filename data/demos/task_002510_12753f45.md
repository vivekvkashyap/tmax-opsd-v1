Don't jump straight to `git bisect run` — first confirm the linker breakage's commit range manually (try building a few commits by hand to map out which range fails with "undefined reference" errors), then use `git bisect` with a custom test script that treats a build failure as "skip" (`git bisect skip`) rather than "bad", so bisection continues past the broken linker window instead of falsely blaming it; compare each buildable commit's output against `/app/oracle_bin` run on `/home/user/sample_data.csv` (same CSV in, same output format expected: e.g. a `Final loss:` line) to classify good/bad. Once you isolate the regression commit, read its diff for the sign/update-rule change in the gradient step and any dropped learning-rate schedule — fix `master`'s current logic to restore correct gradient descent (correct sign of the update, and reinstate any decay factor that was removed) without reverting unrelated later commits. Compile the fixed `master` tree to `/home/user/optimized_engine_fixed`, then validate it against `/app/oracle_bin` on your own held-out CSV (parsing the `Final loss:` value from both and diffing) before trusting it will pass the hidden verifier's tight tolerance.