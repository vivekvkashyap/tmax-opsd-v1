Work through this in strict phase order since later phases depend on earlier ones being correct. Phase 1: use shell globbing/`ls` to group files by base name before the `.partN.` marker, sort matches numerically by the part number (not lexicographically — `part10` must sort after `part9`), `cat` them in order into `<basename>.cfg`, then remove the originals; separately, for every `.txt` file grep for a line matching `^\s*\[.+\]\s*$` and only rename those (`mv`) to `.ini`, leaving plain `.txt` notes files untouched. Phase 2: scan `.cfg`/`.ini` files line-by-line (case-insensitive, allowing leading whitespace) for `password=` or `api_key=`, replace only the value portion with `REDACTED`, and append one line per redaction to `/home/user/redaction_audit.log` as `<filename>:<line_number>:<matched_key>` using the original-case key name and just the basename (not full path). Phase 3: in `/home/user/consolidate.py`, parse `.ini` files with `configparser` into nested section dicts and `.cfg` files as flat key=value dicts (skip blank lines and `#` comments), assemble one master dict keyed by filename, then write it to `/home/user/master_config.json.tmp` with `indent=2` and use `os.rename()` to atomically move it to `/home/user/master_config.json`.
