Use SQLite's built-in integrity checker rather than hand-rolling join logic: `sqlite3 db_backup.sqlite "PRAGMA foreign_key_check;"` lists every violating row as `table|rowid|referenced_table|fkid`, which tells you exactly which rows in `orders` and `payments` are orphaned without you needing to manually LEFT JOIN against parent tables. From that output, filter rows where the first column is `orders` (or `payments`), extract the rowid, then run a follow-up `sqlite3` query like `SELECT amount FROM orders WHERE id IN (...)` to fetch the amounts, and sum with `awk` or `bc`. Count with `wc -l`. Format sums to exactly 2 decimals with `printf "%.2f"` (awk's default float printing can drop trailing zeros or use scientific notation, so don't rely on it directly for the final string). Build the final JSON with `jq -n --argjson ... ` or a heredoc, ensuring numeric fields are numbers not strings. Make the script executable and actually run it once to produce `/home/user/integrity_report.json`, then sanity-check it with `jq .` and `cat`.