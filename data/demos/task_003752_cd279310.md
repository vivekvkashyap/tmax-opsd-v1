Work through the three services as a pipeline, not independently. Fix `nginx.conf` first with a minimal `location / { proxy_pass http://127.0.0.1:5000; }` block (also forward Host/X-Forwarded headers) and reload nginx. In the Flask app, parse the JSON-lines body by decoding the raw request bytes as UTF-8 and calling `json.loads` per line — do not rely on any custom regex/string splitting that breaks on `\uXXXX` escapes, since `json.loads` handles those natively. For dedup, key by `(minute_bucket, sha256(data_string))` stored in a Redis set per minute so repeated identical `data` values within the same minute collapse to one increment; only increment the minute's change counter the first time a given hash is seen in that bucket. For `/metrics`, iterate every minute from `start` to `end` inclusive (not just minutes with data) and default missing buckets to 0 — build this by generating the full list of minute timestamps first, then look up counts, rather than only emitting minutes found in Redis. Compute `rolling_5m_count` as a sliding-window sum over the current minute plus the 4 preceding ones, using the already-materialized per-minute counts list so out-of-range look-back before `start` is handled correctly. Start redis-server, then nginx, then the Flask app (via gunicorn or `flask run`) in the background, verifying each port is listening before moving on.
