This is a black-box reverse-engineering task, not a spec-reading task — you must infer exact behavior by probing, not assume a semver library matches bit-for-bit. Systematically run `/app/version_evaluator` on many inputs covering: simple comparisons (`>=`, `<=`, `>`, `<`, `==`, `!=`), combined expressions with `&&`/`||` and parentheses (test operator precedence carefully — try `A || B && C` and `(A || B) && C` to see if the binary treats `&&` as higher precedence), prerelease versions (`-alpha`, `-rc.1`) and their SemVer 2.0.0 precedence rules against release versions, and malformed inputs (missing components, extra dots, bad operators, unbalanced parens) to map exactly which trigger `INVALID` vs `NO_MATCH`. Record stdout and exit code for each probe. Implement `/home/user/evaluator.py` using an existing well-tested semver comparison approach (or careful manual parsing) plus a small recursive-descent or precedence-climbing boolean expression evaluator, matching the exact `MATCH`/`NO_MATCH`/`INVALID` strings and exit codes (0/0/1) you observed. Cross-check your script against the binary on your own generated random fuzz cases before considering it done, since the real verifier will fuzz far more inputs than you can enumerate by hand.
