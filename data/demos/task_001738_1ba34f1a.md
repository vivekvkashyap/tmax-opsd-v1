Implement this as a recursive quadtree, not a flat grid: represent each cell as (xmin,xmax,ymin,ymax,depth), start from the four depth-0 quadrants of [0,1]x[0,1], count points falling in a cell using the half-open interval rule specified (upper boundary inclusive only at x=1.0/y=1.0), and recurse into 4 equal children only when count>50 and depth<3 — cells that hit the depth cap become leaves regardless of count. Collect only leaf cells for the final mesh. Compute each leaf's theoretical mass via the Gaussian formula evaluated at the cell's geometric center times its area, normalize across leaves to get Q, and compute empirical P as point-count/total. Sum the KL divergence term-by-term skipping any P_i=0 entries (do not add 0*log(0) which would produce NaN). Use double precision throughout, write results with kl_divergence rounded to 4 decimals via round-half-even or standard rounding, and double check total_cells/max_depth_reached match your actual leaf list before writing the JSON. Compile with g++, install nlohmann-json via apt if using it for output.
