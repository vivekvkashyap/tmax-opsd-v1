This is a multi-artifact correlation task graded by F1 score, so prioritize precision AND recall of matched (timestamp, payload, cert-flag) tuples over exhaustive extraction. Extract video frames with `ffmpeg -i /app/incident_data/capture_session.mp4 -vf fps=2` (increase fps if text flashes briefly) and OCR or visually inspect each frame for the `GET /login?redirect=...` payload and the on-screen clock/timestamp shown at that moment — record each distinct occurrence and its second-level timestamp. For the binary, don't guess the XOR key: disassemble/inspect `auth_logger.elf` with `objdump -d`/`strings`/`lief` to find where redaction happens and read the constant it XORs against directly out of `.rodata` or the code, respecting the byte order the binary itself uses. For the pcap, use `scapy` or `tshark` to extract TLS handshake Certificate messages per connection/flow, compare each cert's fingerprint/subject against `server_cert.pem` vs `suspicious_cert.pem`, and correlate flows to the video timestamps by connection timing order (video events are sequential, so match by relative order and rough time alignment). Assemble the final JSON only from events you can support with evidence from at least two artifacts.
