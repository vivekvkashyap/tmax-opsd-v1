Split this into the two independent bugs and fix/verify each in isolation before combining. For processor.cpp: use libcurl (CURLOPT_WRITEFUNCTION with a std::string buffer) to GET http://127.0.0.1:8081/data, parse the returned JSON array of 1000 floats (a minimal hand-rolled parser splitting on commas/brackets is fine, or a small JSON lib if available), then in a SINGLE pass over the batch: first compute the mean of all values NOT exactly equal to -9999.0, then replace every -9999.0 entry with that mean, THEN clamp every value (including the just-imputed ones) to [-10.0, 10.0] — do the imputation before clamping, not after, since imputed means could themselves be near the outlier range. Write all 1000 results to cleaned_data.csv one per line at 4 decimal places (`printf("%.4f\n", v)` style), compile with the exact flags given (`g++ -O3 processor.cpp -o processor -lcurl`), and run it, then sanity-check the output file has exactly 1000 lines with no remaining -9999.0 or out-of-range values. For the reporting service, the headless-plot crash is almost always a missing Matplotlib backend selection — set `MPLBACKEND=Agg` as an exported environment variable inside start_report_service.sh before invoking the Python script (or call `matplotlib.use("Agg")` before importing pyplot), then run `./start_report_service.sh` and confirm report.png is created and non-empty.