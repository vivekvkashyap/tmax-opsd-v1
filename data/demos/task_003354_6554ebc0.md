This is a numerically-stable root-finding and distance-computation task in Python. Avoid computing exp(alpha*sqrt(x)) directly for the equation sum(exp(alpha*sqrt(x))) = 1e20 — instead work in log-space: define f(alpha) = logsumexp(alpha*sqrt(x_values)) - log(1e20) and find its root over alpha in [0,10] using a bracketing solver like scipy.optimize.brentq or root_scalar, since f is monotonic in alpha. For the theoretical distribution Q(x), again avoid raw exponentials — compute logits = alpha*sqrt(x), subtract logsumexp(logits) (or the max) before exponentiating, so Q = exp(logits - logsumexp(logits)), guaranteeing normalization and no overflow. Load the empirical CSV into P(x); compute KL divergence with scipy.stats.entropy(P, Q) (or manual sum P*log(P/Q), watching for zero-probability entries) and the Wasserstein-1 distance with scipy.stats.wasserstein_distance(x, x, u_weights=P, v_weights=Q) — do not implement your own approximate Wasserstein since exact 1D weighted computation matters for tolerance. Round all three results to 6 decimal places and write them into a JSON object at /home/user/results.json with exactly the keys alpha, kl_divergence, wasserstein_distance.
