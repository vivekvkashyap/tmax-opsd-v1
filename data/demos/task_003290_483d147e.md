First fix and install the vendored dependency: open `calculator.py` in `/app/vendored/py-rolling-stats-0.1.0/py_rolling_stats/`, find where it reads the window-size env var, and correct the misspelled name to `MAX_WINDOW_SIZE`; then `pip install -e /app/vendored/py-rolling-stats-0.1.0` and sanity-check with a quick import/add/average call. In process_configs.py, set `os.environ["MAX_WINDOW_SIZE"] = "5"` before constructing calculators, and keep one `RollingCalculator` per distinct `server_id` (a dict keyed by server_id) since the rolling average is per-server, not global. For each event, add its `diff_size`, compute the rolling average including the current event, and set `anomaly` true only if strictly greater than 50.0. Build masking as three sequential regex substitutions on `content`: an IPv4 pattern to `[IPV4]`, `AKIA[A-Z0-9]{16}` to `[AWS_KEY]`, and password patterns — handle both `password=<value>` and JSON-style `"password": "<value>"` as separate alternatives, replacing only the captured value so surrounding syntax is preserved. Process each JSONL file line-by-line, and verify against the clean/evil corpora under `/home/user/corpora/` that clean files pass through unchanged except for the added `anomaly` key.