Read `login.py` carefully to spot the vulnerability class: look for where a `next`/redirect target parameter from user input is passed straight into a redirect response together with a sensitive token, without validating it against an allowlist of trusted domains — that pattern is an unvalidated/open redirect, which maps to a specific CWE ID (look it up rather than guessing a number). Next, grep `app.log` for the malicious outbound redirect line and pull out the `token=` query-string value; it's Base64, so decode it (e.g. `base64 -d` or Python's `base64.b64decode`) to get a JSON object, then parse out the `key` field — that's the Fernet key used to encrypt `stolen_data.enc`. Use Python's `cryptography.fernet.Fernet` with that recovered key to call `.decrypt()` on the raw bytes of `stolen_data.enc` and decode the result to a string. Finally write `/home/user/forensics_report.txt` with exactly three lines in the required `CWE: ...` / `Leaked Token Key: ...` / `Decrypted Content: ...` format, using the values you derived from the log and decryption, not placeholders.