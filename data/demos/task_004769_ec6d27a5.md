Work in order and verify each stage before moving on. First `mkdir -p /home/user/bin` and compile with `gcc -o /home/user/bin/calc_energy /home/user/src/calc_energy.c`; run the provided `test_energy.sh` and confirm it prints PASS before touching the data (it exercises PATH, so export `/home/user/bin` onto PATH or ensure the script finds the binary). For step 3, don't reimplement the energy math yourself — pipe each line of `signals.txt` through your compiled `calc_energy` (e.g. `while IFS= read -r line; do echo "$line" | /home/user/bin/calc_energy; done < signals.txt`) so the energy values come from the C program, not a shell reimplementation. Pair each resulting energy (x) with the corresponding line of `targets.txt` (y) by line number using `paste` or an indexed awk loop. Then compute the regression sums (N, Σx, Σy, Σxy, Σx²) in a single `awk` pass and apply the closed-form slope/intercept formulas — awk's floating point and `printf "%.4f"` handle the required precision. Write the final line to `/home/user/regression_results.txt` matching the exact format `Slope: <m>, Intercept: <b>` with no extra whitespace or trailing text.