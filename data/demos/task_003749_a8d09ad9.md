Split this into service repair and a standalone fuzz-tested script. For the pipeline: read `nginx.conf` and note which upstream port `proxy_pass` targets versus the port the Flask API actually binds to, and correct the mismatch; do the same for `worker.env`'s Redis port versus the port `redis-server` actually listens on — inspect, don't assume. Start services and confirm end-to-end with the sample `curl` upload first. For `dataset_linker.sh`, since grading fuzzes it against a reference implementation across many generated directory trees, focus on exact semantics: split `manifest.txt` on `---` into records, parse the three `Key: value` lines per record, locate `<filename.ext>` anywhere under the source dir with `find <dir> -name <filename> -type f`, compute `sha256sum` on the found file and compare to the manifest hash, and on a match `mkdir -p` the category dir then `ln` (hard link, not copy/symlink) into it — silently skip on any hash mismatch or missing file so the script keeps processing the rest of the manifest.
