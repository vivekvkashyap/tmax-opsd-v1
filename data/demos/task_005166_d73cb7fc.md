This is a forensics-plus-bisect task; do the recovery step before the bisect step since bisect needs the poison data as input. First, safely recover the SQLite state: don't just open `metrics.db` read-only and ignore the WAL — instead open it normally (e.g. `sqlite3 /home/user/data/metrics.db "PRAGMA wal_checkpoint(FULL);"` or simply connect with the sqlite3 module, which auto-checkpoints/reads the WAL) so the uncommitted-but-WAL-logged poison row becomes visible, then `SELECT * FROM metrics ORDER BY id DESC LIMIT 1;` to get its id, name, and value. Cross-check this against the stack trace in `/home/user/crash.log` for consistency. Next, write a tiny standalone test script that imports `processor.py` from the repo and calls `process_metric(name, value)` with the poison row's actual name/value, asserting it does *not* raise (or wrapping the call so the script exits 0 on success, nonzero on the crash exception) — this becomes your bisect probe. In `/home/user/app_repo`, run `git bisect start`, mark current `HEAD` as `bad` and the first commit (`git log --reverse | head`) as `good`, then `git bisect run <your_test_script>` (make it executable and correctly return exit codes) to let git automatically walk commits and find the exact regression commit. Record the reported commit hash and the poison row's id into `/home/user/report.txt` in the exact two-line format, then `git bisect reset`.