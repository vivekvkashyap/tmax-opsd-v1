Work in clear passes rather than combining everything into one script. Pass 1: recursively find all `.log` files under `/home/user/raw_logs/` and use a regex substitution (e.g. Python `re.sub` with a pattern matching `\[(\d{2})-(\d{2})-(\d{4}) ...\]` and rearranging groups to `[YYYY-MM-DD ...]`) applied line-by-line so multi-line stack traces that don't start with `[` are left untouched — do this in place, don't reconstruct the whole file naively. Pass 2: run `/app/legacy_verifier <path>` on each file via subprocess and check the exit code; delete anything nonzero. Pass 3: for surviving files, read the *first* bracketed timestamp in the file to get its date, then `mkdir -p` and move the file into `/home/user/clean_logs/<date>/`. Only after logs are organized, build the Rust server: check the `Authorization` header on every route before touching the endpoint logic (return 401 early otherwise), stream a `tar.gz` of the date directory for `/download` using the `tar`/`flate2` crates or shelling out to `tar czf`, and shell out to the same verifier binary for `/verify`. Build and background the server (`cargo build --release && ./target/release/... &`) and confirm with `curl` that it's listening on 127.0.0.1:8080 before finishing.
