Build this as a pipeline of independently-testable pieces. First `python3 -m venv /home/user/sim_env` and `pip install numpy scipy jupyter papermill` inside it. In `solver.py`, assemble the standard second-order central-difference tridiagonal system for the N-1 interior points (e.g. via `scipy.sparse.diags` with `[-1,2,-1]/h**2`), solve with `scipy.sparse.linalg.spsolve`, and compute the discrete L2 error against `sin(2*pi*x)` exactly as the formula specifies — verify this function manually against a known N before wiring up notebooks. For the notebook, generate it programmatically with `nbformat`: create a cell whose `cell.metadata['tags'] = ['parameters']` contains `N = 10`, and a following cell that imports `solver`, calls `get_l2_error(N)`, and writes the float to `results/error_{N}.txt` — papermill only recognizes the parameters cell if that tag is set correctly. In `run_study.sh`, source the venv's `activate`, `mkdir -p results`, loop over the required N values calling `papermill template.ipynb results/out_{N}.ipynb -p N <val>`, then after all runs finish, read each `error_{N}.txt` and assemble the CSV with the exact header and row order specified.