Treat this as two separate forensic sub-problems that must both be solved before the binaries will match bit-for-bit. First, use `git bisect` in /home/user/hash_repo: write a small regression test that feeds a fixed stdin string to the built binary and compares against `/app/legacy_calc`'s output for the same input, mark early/late history good/bad, and let bisect narrow down the offending commit — expect the regression to be a type-narrowing change (e.g. a floating-point variable or a numeric literal losing precision, such as `double`→`float` or a constant losing trailing digits) rather than a logic change; fix it by restoring the wider type/constant, not by reverting the whole commit blindly if later commits depend on other parts of it. Second, since the binary is stripped, run `strings /app/legacy_calc | grep -i magic` (or similar filtering) to locate the embedded constant string used in the hash mixing step, and if `strings` alone is ambiguous, use `gdb`/`objdump` to disassemble around string references and confirm exact byte content. Update the C source with the recovered constant, rebuild to /home/user/solution_bin, then self-test with several random stdin inputs piped to both binaries and diff the outputs before considering it done.
