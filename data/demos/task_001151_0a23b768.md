Start by transcribing `/app/build_constraints.wav` with a local speech-to-text tool (e.g. `whisper` CLI if available, or another installed ASR tool) rather than guessing the rules — pipe the transcript into `/home/user/extracted_rules.txt` and read it carefully, since numbers and technical terms (versions, package names, encodings) may be spoken out in a form you need to normalize (e.g. "three point nine" -> "3.9", "base sixty four" -> "base64"). Only after you've recovered the literal rules should you implement `build_validator.py`: parse `package.json` and `requirements.txt` with the standard `json` module and simple line parsing (handle `==`/`>=` version specifiers), check the Python version constraint, check the conditional package-pair constraint, scan the `.build.sh` script text for the malicious pattern described in the transcript (build the exact regex/string check from what you transcribed, not from assumptions), and validate any checksum/hash constraint the audio specifies by recomputing it yourself (e.g. `hashlib.md5` over the relevant JSON substructure) and comparing. Exit 0 only if every rule passes, else exit 1. Before trusting the tool, iterate: run it against a handful of clean and evil subdirectories individually, inspect mismatches, and adjust regexes/parsing edge cases — then scale up to looping over all 50+50 directories in verify_all.sh, printing "CLEAN PASSED"/"EVIL REJECTED" only when every case in that corpus behaves as expected.
