First recover the policy parameters yourself: install `tesseract-ocr` and `pytesseract` (or use the `tesseract` CLI directly on `/app/policy.png`), OCR the image, and read off the rolling-window length (in days) and the severity threshold stated in the text — convert the window to seconds for use in SQL. Then rewrite the SQL in `/home/user/generate_report.py` as a proper join+window query: join `access_logs` to `employees` on `emp_uid` with an explicit `JOIN ... ON` (never a comma cross join), filter to rows where `resource_dept_id != dept_id`, then compute a rolling sum per employee using `SUM(severity) OVER (PARTITION BY emp_uid ORDER BY access_time RANGE BETWEEN <window_seconds> PRECEDING AND CURRENT ROW)`. Take `MAX()` of that rolling value per employee, keep only those strictly exceeding the threshold you OCR'd, and order by severity desc then emp_uid asc. Write the exact schema (`emp_uid`, `max_rolling_severity`) to `/home/user/compliance_report.json`. For performance, write `/home/user/optimize.py` to `CREATE INDEX` on the join and window columns (e.g. `access_logs(emp_uid, access_time)` and `employees(emp_uid)`) and run it before timing — an unindexed window scan over tens of thousands of rows will blow the 1.5s budget.
