First OCR the tag: run `tesseract /app/archive_tag.png stdout` (or similar) and pipe through `tr -d '\n' | sed 's/^[[:space:]]*//; s/[[:space:]]*$//'` to get the exact clean Archive ID string — don't hardcode a guessed value, always derive it live from the image so the script generalizes. Then write `/home/user/archive_tracker.sh` to be strictly binary-safe: get the file size with `wc -c <"$1"`, and treat any file under 4 bytes, or one whose first 4 bytes (via `head -c4 "$1" | xxd -p`) aren't `7f454c46`, as `INVALID`. For valid ELF files, read exactly the first 16 bytes with `head -c16 "$1" | xxd -p | tr -d '\n'`, then right-pad the resulting hex string with `0` characters (not spaces) up to exactly 32 hex chars using something like `printf '%-32s' "$bytes" | tr ' ' '0'` — this correctly emulates zero-padding a byte array even when the file is shorter than 16 bytes. Never touch stdin/stdout with tools that mangle nulls (avoid `cat` for parsing, prefer `head`/`xxd`), print only the final `<ARCHIVE_ID>|<RESULT>` line with no extra debug output, and `chmod +x` the script. Test against empty files, short files, and full ELF-prefixed files before finishing.