For Part 1, don't eyeball frames — extract every frame from `/app/flight_video.mp4` programmatically (e.g. `ffmpeg -i flight_video.mp4 frame_%04d.png` or decode via a Python library) and check each one algorithmically for "every pixel is exactly RGB(255,0,0)" (e.g. with PIL: `all(p == (255,0,0) for p in img.getdata())`, or check the pixel min/max), writing the matching 0-indexed frame numbers in ascending order to `/home/user/red_frames.txt`; be careful with ffmpeg's 1-indexed default output naming when converting back to your required 0-indexed frame numbers. For Part 2, scaffold with `cargo new /home/user/log_filter`, add `serde_json` and `regex` (or hand-roll a MAC-address check) to `Cargo.toml`, and stream stdin line-by-line (`BufReader::lines()`), parsing each as JSON and dropping it if parsing fails, `military_zone == true`, `device_id` matches a strict 6-group two-hex-digit colon-separated pattern (case-insensitive), or `battery_temp` is a number > 90.0 — otherwise echo the line to stdout unchanged. Build with `cargo build --release` so the binary lands at `target/release/log_filter`, then self-test by constructing your own small clean/evil JSONL samples covering each rejection rule and boundary cases (e.g. exactly 90.0 should pass) before trusting the hidden corpora.