Follow the spec's exact order of operations since the result must be bit-reproducible: filtering criteria, decomposition, then a seeded bootstrap. Load `/home/user/raw_data.npy` and, for each of the N matrices, discard it if `np.isnan(mat).any()` OR `np.max(np.abs(mat)) > 1e6` — apply both conditions per-matrix, not just one, and keep the ones that fail neither. Only after building the filtered valid-matrix array should you count `valid_count`. For each valid matrix, run `np.linalg.svd` and take the first singular value `s[0]` (SVD returns singular values in descending order already, so no extra sorting needed) into a 1D array. The critical reproducibility step: call `np.random.seed(42)` immediately before the bootstrap loop (not earlier — any other random calls before this will desync it), then run exactly 10000 iterations of `np.random.choice(sigma1_vals, size=valid_count, replace=True)` and record `np.median()` of each resample. Compute the CI with `np.percentile(medians, 2.5)` and `np.percentile(medians, 97.5)`. Round both to exactly 4 decimals with Python's `round()` before writing JSON, and match the key names precisely. Re-run the whole script once end-to-end to confirm results are stable given the fixed seed.