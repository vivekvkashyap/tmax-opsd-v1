This is a fixed-width PDB parsing task — don't split on whitespace naively, since fields can run together; use the standard PDB column positions (atom name in columns 13-16, residue name in 18-20, x/y/z in fixed 8-character fields starting around column 31) to slice each ATOM line, exactly as the PDB format specifies. Filter lines where the atom-name field strips to `CA` for all alpha carbons, and among those where residue name strips to `ALA` for the alanine subset. Compute the center of mass as the plain mean of all CA x/y/z coordinates (unweighted), then Euclidean-distance each ALA CA to that center. For the KS test use `scipy.stats.kstest(distances, 'rayleigh', args=(0, 15.0))`. For the Wasserstein distance, generate the theoretical comparison sample with the exact call given in the task (`numpy.random.default_rng(42).rayleigh(scale=15.0, size=100000)`) so your RNG state matches what the verifier expects, then use `scipy.stats.wasserstein_distance`. Round every value to exactly 4 decimal places and write the six required keys to the JSON file with no extras.
