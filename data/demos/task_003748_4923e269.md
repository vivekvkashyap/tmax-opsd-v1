For the build fix, don't guess flags blindly — compile first, read the linker error carefully (undefined reference to `pow`), then add the missing math library to the `Extension(...)` call in setup.py (the `libraries=[...]` kwarg links `-lm`), and rebuild in-place to confirm the import succeeds. For semantic version comparison, do not compare version strings lexicographically or purely numerically as a whole — split each string on `-` to separate the core version from any pre-release tag, compare core version tuples numerically, and if cores are equal, compare pre-release identifiers segment-by-segment (splitting on `.`), treating purely-numeric segments as integers when compared to each other (per semver rules, `beta.2` vs `beta.11` must compare `2` and `11` as numbers, not as strings, or you'll get the wrong winner); the `packaging` library's `Version`/comparison utilities can help but confirm they handle pre-release ordering correctly. Only after the extension imports cleanly should you run `benchmark.py`, then sort its two-line CSV-ish output by the numeric second field ascending, preserving the original line format exactly.
