Build this bottom-up and test each layer in isolation before composing them. First, manually run `/app/bin/calc_cost` interactively to observe the exact prompt text and ordering, then write `cost_wrapper.exp` using `spawn`, `expect` on each literal prompt string, `send` the region/instances arguments, and a final `expect -re {\$([0-9.]+)}` to capture just the numeric cost — print only that captured number, stripping the banner/prompts. Test the expect script standalone (`./cost_wrapper.exp us-east 10`) before building the daemon. For `cost_api.sh`, use `socat TCP-LISTEN:9000,bind=127.0.0.1,reuseaddr,fork EXEC:...` or a `nc -l` loop that reads one line, splits region/instances, calls `cost_wrapper.exp`, and echoes the number back — test with `echo "us-east 10" | nc 127.0.0.1 9000` before proceeding. `supervisor.sh` should loop: launch `cost_api.sh`, `wait $!`, and relaunch on exit, run it with `&` and `disown` so it survives your session. Last, write the bare repo's `pre-receive` hook to extract `deployment.conf` via `git show <newrev>:deployment.conf`, query the daemon over TCP (e.g. with a small `/dev/tcp` redirect or `nc`), parse the numeric reply, and exit 1 with `FinOps: Budget exceeded` on stderr if cost > 500, else exit 0. Test with real `git push` of both an over-budget and under-budget `deployment.conf` before finishing.