Solve both parts as pure SQL against `/home/user/etl_metadata.db` via the `sqlite3` CLI. For cycle detection, SQLite lacks a built-in cycle-safe graph traversal, so write a recursive CTE over `dependencies` that tracks the path (e.g. a string/list of visited job_ids) and terminates/flags when a node reappears in its own path — a job belongs in the report if it can reach itself by following one or more dependency edges; join back to `jobs` for names, dedupe, and sort alphabetically before writing `/home/user/cyclic_jobs.txt`. For hierarchy_level, use a separate recursive CTE starting from jobs with no dependency row (level 0) and taking MAX(level) when a job has multiple incoming paths, restricted to non-cyclic jobs. For avg_duration, use a window function (`ROW_NUMBER() OVER (PARTITION BY job_id ORDER BY start_time DESC)`) filtered to `status='SUCCESS'`, keep rows numbered 1-3, average `(julianday(end_time)-julianday(start_time))*86400`, `ROUND()` to nearest integer, and `COALESCE`/default to 0 for jobs with no successful runs. Join hierarchy_level and duration together, order by hierarchy_level then job_name, and write the CSV with an exact header line via `sqlite3 -header -csv`, double-checking the output matches the required column names precisely.