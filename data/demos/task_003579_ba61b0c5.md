Treat this as an end-to-end pipeline: first prove each service is reachable independently (psql to Postgres, redis-cli PING) before touching Rust. Create `/home/user/app/.env` with a Postgres connection string matching the given host/port/db/user/password and a Redis URL for `127.0.0.1:6379`, then confirm the Rust app actually loads it (e.g. via `dotenv`). For the TODO block: compute Pearson correlation (or covariance/stddev ratio) between each feature column and target across all training rows, pick the feature with the largest absolute correlation, then fit ordinary least-squares simple linear regression (slope = cov(x,y)/var(x), intercept = mean(y) - slope*mean(x)) on that single feature. Store the chosen feature name and both weights in Redis under the exact key names given. When predicting on `test_data`, apply slope*x+intercept using the stored model and write `id,predicted_target` rows to the CSV with a header matching the spec. Validate your regression against a quick sanity check (e.g., recompute MSE on training data yourself) before declaring done — the grader recomputes MSE against real target values in Postgres, so an off-by-one in slope/intercept or the wrong feature selection will fail the 2.0 threshold.