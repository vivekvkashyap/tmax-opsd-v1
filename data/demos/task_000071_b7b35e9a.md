Use `std::filesystem::recursive_directory_iterator` to walk `/home/user/artifacts`, and for each entry check the extension with a suffix comparison on the full filename (not just checking "ends with .bin") so that `data.bin.tmp` is correctly excluded — filter first on ".tmp" being absent, then require ".bin" at the very end. Open each candidate file in binary mode, read the size, and validate two things before trusting it: (1) the first 4 bytes equal the little-endian magic bytes for `0xDEADC0DE`, and (2) `(file_size - 4) % 8 == 0`. If either check fails, append the absolute path to `/home/user/corrupt.log` and skip extraction — do not partially parse. For valid files, read the remaining bytes 8 at a time with `memcpy` into a `double`, then write each value with `std::fixed << std::setprecision(4)` to a sibling `.txt` file built by replacing only the trailing `.bin` in the filename (not blindly string-replacing "bin" anywhere in the path). Compile with `g++ -std=c++17` and actually run the binary before declaring done — verify `corrupt.log` and the `.txt` files exist with a quick `find`/`cat`.