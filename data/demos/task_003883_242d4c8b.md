Build and test this pipeline stage-by-stage against a small hand-crafted CSV before wiring it into the server. Decode the raw POST body explicitly as Windows-1252 (not UTF-8) before parsing CSV rows. Group rows by `sensor_id`, then for each sensor resample to a strict 1-second grid spanning that sensor's own min/max timestamp (use a time-indexed series/dataframe reindex, e.g. pandas `resample('1S')` if using Python, or an equivalent manual loop in your chosen language) — this will introduce NaNs for skipped seconds and for originally-empty `raw_value` fields alike; fill both with linear interpolation over time. Compute a 5-second rolling average per sensor with a right-closed window and `min_periods=1` so the first few seconds average whatever's available rather than becoming NaN. For calibration, don't try to reverse the stripped binary from disassembly alone — instead call `/app/calibrator <value>` as a subprocess for a handful of known inputs, plot/tabulate input vs output, and fit the simple closed-form relationship (it's a small combination of a linear term and a bounded periodic term); once confident, you may inline that formula OR keep shelling out per row for correctness — shelling out per row is simpler and safer against fitting error, just be mindful of the subprocess overhead. Sort the final results by `sensor_id` then `timestamp`, round `calibrated_value` to 3 decimals, and set `Content-Type: application/json`. Keep the server running in the background on `127.0.0.1:8000` after finishing.
