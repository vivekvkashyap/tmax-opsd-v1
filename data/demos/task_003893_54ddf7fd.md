Write the RLE routine as a single generic byte-stream encoder so it naturally handles every character including spaces and newlines — don't special-case whitespace. For each input line (file path) read with `fgets`/`getline`: use `basename()` from `<libgen.h>` to get just the filename for the header, print `---<basename>---\n`, then read the target file fully (`fopen`/`fread` into a buffer, sized by `fseek`/`ftell` or a growing buffer — don't assume a fixed max size), and RLE-encode it: scan byte by byte, counting consecutive identical bytes, and emit `<byte><count>` for each run (a lone byte still gets a count of 1, e.g. a single trailing newline becomes `\n1`). Print a newline after each file's encoded block. Wire it up with `find /home/user/project_files -name '*.txt' | sort | ./archiver > /home/user/organized_archive.rle`. Watch two common bugs: forgetting that `basename()` may modify its input buffer (use a copy), and off-by-one errors at buffer/file boundaries when a file has no trailing newline.