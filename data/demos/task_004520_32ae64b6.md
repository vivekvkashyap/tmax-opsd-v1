Do the dependency-closure logic and the service wiring as separate, testable pieces. For `bundle.sh`: parse `/app/src/deps.txt` where each line is `filename: dep1, dep2, ...` into a map, then do a BFS/DFS closure starting from `app.py` (use a "visited" set/array in bash to avoid infinite loops on cycles, and split each dep list on `,`/trim whitespace), copying only files reached by that traversal into `/app/deploy/` — explicitly do NOT `cp -r /app/src/* /app/deploy/`, since the source directory is seeded with large decoy files specifically to bloat a naive copy past the size threshold. Verify by listing `/app/deploy/` and checking its total size with `du -sb` before moving on. Start the Flask backend backgrounded (`python3 /app/deploy/app.py &`) and confirm it answers on 127.0.0.1:5000 with curl. For nginx, write a config with `pid`, `error_log`, and `access_log` all pointing into `/home/user/` (writable without root), a `listen 127.0.0.1:8080;` server block, and `proxy_pass http://127.0.0.1:5000;`, then launch with `nginx -c /home/user/nginx.conf`. Curl `127.0.0.1:8080/health` to confirm the full chain (nginx→flask→redis) works before writing `READY` to the status log.
