Build and verify each layer independently before composing them. For `health_checker.cpp`: use `socket()`/`connect()` to `127.0.0.1:<port>` from `argv[1]`, `send()` the literal string `PING\n`, then `recv()` with a short timeout (e.g. `setsockopt(SO_RCVTIMEO)`) so a hung/non-responding port doesn't block forever — check the received bytes start with `PONG\n` before exiting 0, and exit 1 on any connect failure, timeout, or mismatched response; compile it and manually test against both the live service on 8081 and a closed port to confirm the two exit codes before moving on. For `monitor.sh`: call the compiled binary, inspect `$?` immediately after (don't let any other command run in between and clobber it), and append exactly `UP` or `DOWN` plus a newline to `/home/user/health.log` with `>>`; `chmod +x` it. For the cron job: use `crontab -l` piped into a temp file, append a line with the every-minute schedule (`* * * * *`) and the absolute path to `monitor.sh`, then `crontab` the temp file back in — don't overwrite any existing crontab entries. Test the full chain manually (kill/restart the background python service) to confirm health.log reflects both states before considering the task done.