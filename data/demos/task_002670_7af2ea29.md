First reverse-engineer the schema interactively before writing any C++: run `sqlite3 /home/user/research_data.sqlite` and use `.schema` plus `SELECT * FROM tbl_X LIMIT 5` on each table to inspect column types and sample values. Identify Authors and Papers as the two entity tables (each has an id plus descriptive text/number columns — the one with a year column is Papers), then identify the two link tables by their column shapes: the one whose values reference (Author.id, Paper.id) pairs is Authorships, and the one whose values reference (Paper.id, Paper.id) pairs is Citations. Once mapped, write `/home/user/extract.cpp` using the `sqlite3` C API (`sqlite3_open`, `sqlite3_prepare_v2`, `sqlite3_step`) to run a single SQL query that self-joins Citations to Authorships twice (once for the citing paper's author, once for the cited paper's author), filters out rows where the two resolved author IDs are equal (self-citation), groups by (source author, target author) counting rows as the weight, and orders by weight descending then source name then target name ascending. Compile with `g++ -std=c++17 ... -lsqlite3`, run the executable, and write the tab-separated, headerless result to `/home/user/author_citations.tsv`.