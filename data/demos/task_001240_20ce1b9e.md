This is a classic graph-traversal bug: a recursive/iterative function loops over edges whose `source` matches the current node, then loops over *all* nodes again without filtering to the edge's actual `target`, so it recurses into (or sums) the target once per node in the graph instead of once. Read `src/main.rs` carefully and find the inner loop over `graph.nodes` inside the edge-matching loop — check whether it actually compares `node_id == target` before recursing/summing. Fix it by either removing the redundant node loop entirely (you already have `target` from the edge, so just recurse directly with `calculate_chain_size(graph, target)` once per matching edge) or by adding the missing `node_id == target` join condition. After fixing, build with `cargo build --release` (or `cargo run`), run it against `/home/user/backup_manifest.json`, and manually trace the small graph by hand (root size + each reachable descendant's size, no double-counting) to sanity-check the printed total before writing it to `/home/user/report.txt`.
