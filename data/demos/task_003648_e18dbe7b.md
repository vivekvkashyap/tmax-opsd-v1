This is a live forensics task, not a static-file task — approach it in order. First, find the PID of the running `upstream_service.py` process (`pgrep -f upstream_service.py`), then look at its open file descriptors under `/proc/<pid>/fd/` to find the one pointing to the deleted CSV file (deleted files still show as `(deleted)` in `ls -l /proc/<pid>/fd/`); recover its exact contents by reading through that fd path (e.g. `cat /proc/<pid>/fd/<N> > /home/user/recovered_data.csv`) since the file no longer exists on disk. Second, diagnose the crash: read `/home/user/data_pipeline/process_data.sh` and notice it uses a bash associative/indexed array indexed directly by the numeric line value (`counts[$line]`) — a negative value like `-1` used as an array subscript causes a fatal bash error, which is the "statistical anomaly" causing intermittent crashes. Fix the script to skip/ignore negative numbers before using them as an index (e.g. add a check `if (( line < 0 )); then continue; fi` right after reading each line), without changing how positive values are tallied. Finally run the fixed script against the recovered CSV and redirect stdout to `/home/user/summary_report.txt`.