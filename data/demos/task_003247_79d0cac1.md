Start by running the existing `summarize.sh` to see which log file produces a negative total in `results.txt` — that pinpoints the failing file without needing to disassemble the binary. Since you don't have the binary's source, reverse-engineer the bug from behavior: knowing the tool sums per-line integer durations, a large log file producing a negative sum strongly suggests 32-bit signed integer overflow (the sum exceeded roughly 2.147 billion and wrapped negative) — confirm by independently computing that file's true total using `awk '{sum+=$3} END{print sum}'` (or similar field-aware summation matching the log format) and checking it exceeds `2^31 - 1`. Record that filename and the independently-computed correct total (not the binary's broken output) in `overflow_report.txt` in the exact `<filename> <total>` format. Then write `fixed_summarize.sh` as a full replacement for the pipeline: iterate the same log files, but sum durations with `awk` (which uses floating point / handles large sums natively) or `bc` instead of calling `metric_filter` at all, and write `results.txt` in the identical `filename: total` format the original produced, this time with correct values across every file, not just the one you diagnosed.
