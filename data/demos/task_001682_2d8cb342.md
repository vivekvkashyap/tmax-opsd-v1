Use binary/delta debugging rather than scanning 10,000 lines by eye: split `app_logs.txt` in half repeatedly (e.g. `head -n N app_logs.txt > /tmp/chunk.txt`), run the binary against each chunk, and narrow the range based on which half crashes — this converges in ~14 iterations instead of manual inspection. Once you have a candidate line, extract it with `sed -n '<n>p' app_logs.txt` and confirm it alone (or a minimal file containing just that line) reproduces the segfault. Cross-check with the source: `log_processor.c` uses `strtok` three times to split on `|` and calls `strlen(message)` — if a line is missing the third field, `strtok` returns NULL for `message` and `strlen(NULL)` segfaults; use gdb (`gdb --args ./log_processor app_logs.txt`, then `run`, then `bt`) to confirm the crash frame if unsure. Fix by checking `message != NULL` (and ideally `timestamp`/`level` too) before using it, skipping the malformed line instead of crashing, then recompile with `gcc log_processor.c -o log_processor`. Rerun on the full file, capture the printed `SUCCESS_TOKEN:` value, and write both the line number you bisected to and that exact token into `/home/user/solution.txt` in the specified two-line format.
