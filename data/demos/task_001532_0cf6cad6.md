Build the pipeline in the order data flows through it and test each stage in isolation. First inspect `/home/user/raw_sensors.csv` to see the exact malformed patterns (non-numeric strings like `ERR`/`NaN`, empty fields), then write the cleaning step in `pipeline.sh` using `awk` (skip the header with `NR>1`, then use a regex or `+0` numeric check on the value field to drop rows whose value isn't a valid float) — test the awk filter alone against the raw file and manually confirm only the corrupted rows are dropped. For `aggregate.cpp`, read comma-separated `timestamp,sensor_id,value` lines from stdin, group by `sensor_id` (e.g., `std::map<std::string, ...>`), and compute mean/variance with Welford's online algorithm (maintain running `mean` and `M2`, updating incrementally per new value, then `variance = M2/(n-1)` for the sample variance) instead of the naive sum-of-squares formula — this is essential given the large baseline values with tiny fluctuations. Format output with `std::fixed << std::setprecision(6)`, sort by `sensor_id` before printing, and drop the header. Have `pipeline.sh` compile with `g++ -std=c++17 -O3`, pipe cleaned data into the binary, redirect to `/home/user/summary.csv`, and be `chmod +x`.
