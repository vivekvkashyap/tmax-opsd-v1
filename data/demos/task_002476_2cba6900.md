Model the feed-forward-loop query as a self-join over `relations` restricted to `target` nodes: first create indexes to make the join fast, e.g. `CREATE INDEX idx_rel_src ON relations(src)`, `CREATE INDEX idx_rel_dst ON relations(dst)` (or a composite `(src,dst)`), and `CREATE INDEX idx_nodes_property ON nodes(property)` since you'll filter/join on it repeatedly. Then write SQL joining `relations AS ab` (A->B) to `relations AS bc` (B->C) on `ab.dst = bc.src`, to `relations AS ac` (A->C) on `ac.src = ab.src AND ac.dst = bc.dst`, joined against `nodes` three times (aliased) to require `property = 'target'` for all of A, B, and C, and enforce A, B, C are pairwise distinct (A<>B, B<>C, A<>C) so you don't count degenerate loops. Execute this via a Python/sqlite3 (or any scripting language) script, collect the `(A,B,C)` triples, sort them lexicographically by (A,B,C) as tuples, and dump as a JSON array-of-arrays to `/home/user/motif_results.json`. Manually trace a couple of expected motifs from the schema to sanity-check your join logic before finalizing, and dedupe if the same triple can arise from multiple edge combinations.