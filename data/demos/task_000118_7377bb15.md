Get the password first, since nothing else can be tested without it: extract frames from `/app/incident_record.mp4` with `ffmpeg -i incident_record.mp4 frame_%04d.png`, then for each frame check the pixel at (x=320, y=240) — with PIL/OpenCV, `img.getpixel((320,240))` or `frame[240,320]` — and count how many frames have that pixel exactly `(255, 0, 0)`; that count is the archive password (do this via a script, don't eyeball it). For `secure_extract.py`, the core security property is: resolve every archive member's final on-disk path (accounting for `../`, absolute paths, and symlink/hardlink targets that may point outside `<dest_dir>`) and compare it against the canonicalized `<dest_dir>` using something like `os.path.realpath` and a prefix check, *before* extracting anything — if any single member fails this check, abort the whole operation with no partial writes, print `REJECT`, exit 1. For safe archives, extract member-by-member yourself (don't use `extractall` naively, since that's exactly what causes Zip Slip) recursing into nested archives up to 3 levels, applying the same path-safety check at every level, then print `ACCEPT`, exit 0. Test against both an archive you construct with a deliberate traversal/symlink attack and a clean nested archive before trusting the implementation, since it will be scored against a large adversarial corpus requiring 100% accuracy both ways.