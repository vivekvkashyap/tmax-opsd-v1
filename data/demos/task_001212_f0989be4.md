First transcribe `/app/sysadmin_memo.wav` (e.g. with `whisper` or another STT tool on the box) and listen for a specific forbidden file extension or pattern mentioned as a business rule — this is a hidden requirement in addition to the generic path-safety checks already spelled out in the task text, so don't skip it. Implement `/home/user/sanitizer.sh` to inspect archive contents without extracting, e.g. `tar -tf "$1"` to list member paths, then for each listed path check: (a) it does not start with `/`, (b) it does not contain a `../` path-traversal component or a bare `..` path segment anywhere (splitting on `/` and checking each component, not just doing a naive substring search, avoids both false positives like `walrus.txt` and false negatives like `nested/dir/../../../escape.sh`), and (c) it does not end with the specific extension named in the audio memo (match the extension precisely, e.g. only a trailing `.wal`, not any filename merely containing "wal"). If any member fails any check, `exit 1` immediately; otherwise fall through and `exit 0`. Make the script executable (`chmod +x`) and test it against a few tar files you construct yourself — including an edge case like a filename that merely contains the forbidden substring but not as a true extension — before trusting it against the hidden corpora.