Process archives in `/home/user/incoming_artifacts/` one at a time with clear validation stages: (1) try opening each with `zipfile.ZipFile`/`tarfile.open` and catch corruption errors (e.g. `zipfile.BadZipFile`, `tarfile.ReadError`, or a failed `testzip()`/CRC check) — treat any exception as "Corrupted". (2) For archives that open cleanly, inspect every member's name/path: reject as "Path Traversal" if any entry is an absolute path or if `os.path.normpath(os.path.join(extract_root, member_name))` does not stay within `extract_root` (checking normalized paths, not just a naive `'..' in name` string search, since traversal can be disguised). (3) On rejection, move the archive file into `/home/user/quarantine/` and append the exact `[REJECTED] <filename>: <Reason>` line to `/home/user/artifact_audit.log`. (4) For safe archives, extract into a temporary directory (e.g. via `tempfile.mkdtemp` in the same filesystem as the destination) first, then use `os.rename` to atomically move it into place as `/home/user/curated_artifacts/<name>/`, and finally delete the source archive from the incoming directory. Test each archive type (safe zip, safe tar.gz, malicious, corrupted) individually to confirm correct routing before running the full batch.
