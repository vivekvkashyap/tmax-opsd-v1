Debug this as a classic native-code crash-triage workflow. First reproduce: `cd /home/user/packet_project && make` then run `./parser sample.pcap` directly (or under `gdb --args ./parser sample.pcap` then `run`) to confirm the segfault and get a backtrace (`bt`) — this tells you which function crashes and roughly why (likely a fixed-size stack buffer being overrun by an oversized copy). Cross-check by inspecting `parser.c` for any `memcpy`/`strcpy`-style copy into a fixed-size local buffer without a bounds check against the source length. Second, write `fuzz.sh`: use `tcpdump -r sample.pcap -w single_N.pcap -c 1 <skip-count-options>` or `editcap -r sample.pcap out.pcap <packet-number>` in a loop over each packet index, running `./parser` on each single-packet file and checking `$?` for a segfault exit status (128+SIGSEGV), to programmatically pinpoint the exact 1-indexed packet number without hand-inspecting hex dumps. Third, fix the vulnerable function by adding a proper bounds check (e.g. clamp the copy length to the destination buffer's capacity, or reject/truncate oversized payloads) rather than deleting the parsing logic — keep the function doing real work. Confirm `make test` now exits 0, then write `/home/user/report.txt` with the packet number on line 1 and the crashing function's exact name (as found via gdb's backtrace/source inspection) on line 2. Ensure `fuzz.sh` is executable.