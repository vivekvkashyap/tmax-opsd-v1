Build this as two linked scripts with a clearly specified interface. In the Python script, read `/home/user/data.h5` with `h5py`, and for each of the 200 rows use `scipy.stats.norm.fit(row)` to get MLE mean/std, then compute theoretical quantiles via `scipy.stats.norm.ppf(np.linspace(0.001, 0.999, 1000), loc=mu, scale=std)` and feed both the raw row and these quantiles into `scipy.stats.wasserstein_distance` — matching this exact quantile construction is what makes your distances numerically match the reference. Parallelize with `multiprocessing.Pool(processes=4)` mapping a per-row worker function (the row data must be picklable, so pass plain numpy arrays, not open file handles, into the pool). Write the 200 distances one per line to `distances.csv` (`np.savetxt` works well). For the R script, `read.csv` that file, run `t.test(d, mu=0.1)` (two-sided by default), and write `res$p.value` — a bare numeric, not scientific-notation-formatted text that a strict parser might reject — to `pvalue.txt`. Execute both scripts (`python3` then `Rscript`) so the output files actually exist before finishing.
