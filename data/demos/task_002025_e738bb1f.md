Work through this in the stated order since later steps depend on earlier investigation. First, patch `/home/user/upload_handler.sh` by wrapping the destination path in `basename "$FILENAME"` so path traversal segments are stripped before writing. Second, open `/home/user/network_capture.log` and locate the request that wrote to `.ssh/authorized_keys` via the traversal exploit — extract that exact attacker key line, then edit `/home/user/.ssh/authorized_keys` to remove only that line (e.g. with `grep -v` or a targeted `sed`), leaving the legitimate key(s) untouched. Third, find the second malicious request in the log containing a base64 blob, extract just the base64 payload text (careful with any surrounding headers/whitespace), decode it with `base64 -d` into `/home/user/extracted_payload.zip`. Fourth, since the ZIP password is exactly 3 lowercase letters, write `/home/user/crack.sh` as a brute-force loop over `{a..z}{a..z}{a..z}` calling `unzip -P "$pw" -o extracted_payload.zip -d ...` and testing exit status (or `zip -T`) to find the working password, then extract `malware.sh`. Finally compute `sha256sum /home/user/malware.sh`, strip the trailing filename/whitespace, and write only the 64-char hex digest to `/home/user/malware_hash.txt`.
