Scaffold with `cargo new /home/user/credential_manager`, then implement in three clean stages so each is easy to verify independently. Stage 1: iterate `.json` files under `/home/user/services/` with `serde_json`, and for any file where `api_key == "EXPIRED_SYS_KEY_a8f9"` overwrite just that field with `"ROTATED_SEC_KEY_b7c2"` and re-serialize the full object back in place (don't drop or reorder other keys). Stage 2: parse `/home/user/logs/api_access.log` line by line with a regex/split extracting IP, key, and result; collect the set of IPs where `result == SUCCESS` and `KEY == EXPIRED_SYS_KEY_a8f9`. Stage 3: from that IP set, exclude any IP whose string starts with `"10.5."` (internal service exception), sort the remainder lexicographically as strings, and emit one `iptables -A INPUT -s <ip> -j DROP` line per IP to `/home/user/firewall_block.sh`, then `chmod +x` it (via `std::fs::set_permissions` or a follow-up shell command) — don't execute it. Run the compiled binary once so files are actually updated on disk.
