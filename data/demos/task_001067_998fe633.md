Fix this stack layer by layer and validate each before moving on. For CMakeLists.txt: a shared library target normally needs `set_target_properties(mathops PROPERTIES POSITION_INDEPENDENT_CODE ON)` (or `-fPIC`), and the CLI executable target needs an explicit `target_link_libraries` against the mathops library — check for both being present/correct. For the hash bug in src/hash.cpp: the task already gives you a reference binary (`/home/user/oracle_hash`) whose behavior your CLI must match bit-exactly — use it as a black-box oracle by running both your compiled `compute_hash` and it on the same handful of test strings, diff the outputs, and bisect which line of the hash-update loop (shift amount, accumulation order, etc.) is off; don't just guess-and-check the whole file. For ctypes in api/app.py, set `argtypes`/`restype` to match the C function's actual signature (pointer+length in, fixed-width unsigned int out) so ctypes doesn't misinterpret memory. Wire Redis via `redis.Redis(host='localhost', port=6379)`, cache on first lookup, and start redis-server, the compiled build, and Flask (127.0.0.1:5000) all running in the background before finishing.
