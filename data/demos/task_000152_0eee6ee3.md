Work phase by phase and verify each before moving on. Phase 1: for every cert/key pair in `/home/user/clients/`, verify it against the CA with `openssl verify -CAfile /home/user/ca.pem <cert>` and check validity dates with `openssl x509 -noout -dates -in <cert>` — only the certificate that both verifies against the given CA and is currently within its validity window is the correct one; expired or wrong-CA-signed certs are decoys designed to trip up a naive "just pick one that parses" approach. Phase 2: use `curl --cacert ca.pem --cert <cert> --key <key>` against `https://127.0.0.1:8443/fetch_audit?log=...`; since the endpoint joins the `log` parameter onto a base directory, use a `../` traversal payload (URL-encode `/` as `%2F` if needed) to escape into `/home/user/service/internal_secrets.txt`. Phase 3: write a regex over the raw text matching 16 digits in either `XXXX-XXXX-XXXX-XXXX` or `XXXX XXXX XXXX XXXX` form and replace the whole match with `[REDACTED]`, leaving non-CC lines untouched. Phase 4: parse `access.log`, group by source IP, count only status-403 lines per IP, and emit `deny <ip>;` for IPs with count > 3 (strictly greater, not ≥).