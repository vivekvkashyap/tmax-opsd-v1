Structure this as clear sequential pipeline stages you can sanity-check independently. Load all three CSVs (e.g. with pandas or plain csv module) and inner-join them on `id` so every row has `x`, `y`, `prior`, `likelihood`, and `predicted_posterior`. Compute `unnormalized = prior * likelihood` per row, sum it across the *entire* joined table, then divide each row's unnormalized value by that sum to get the normalized posterior — do this with full float precision, not rounded intermediates. Flag any id where `abs(normalized_posterior - predicted_posterior) > 0.01` (strict, not >=) and collect those into a sorted list of ints. Exclude the flagged ids, then compute the Pearson correlation coefficient between `x` and `y` only over the remaining rows (e.g. `scipy.stats.pearsonr` or `numpy.corrcoef`), rounding to 4 decimal places. Write `/home/user/report.json` with exactly the keys `flagged_ids` and `correlation` in the specified format. Run the script yourself and eyeball the intermediate join/sum/flagged-id values to catch join-key mismatches or type coercion issues (e.g. ids read as strings vs ints) before trusting the final numbers.