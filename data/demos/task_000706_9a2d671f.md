First inspect the schema with `sqlite3 /app/company.db ".schema"` to confirm column names/types before writing any query. Run `tesseract /app/exclude_rule.png stdout` (converting/preprocessing the image first if OCR is noisy) to recover the excluded department ID from the note — read whatever text comes back rather than assuming a value. Implement the hierarchy traversal as a recursive CTE (`WITH RECURSIVE subdept AS (...)`) starting at the requested `dept_id`, that stops descending into the excluded department's subtree entirely (filter it out of the recursive branch, not just the final results, so its children are never even visited); then join to `employees` on `dept_id`, dedupe, and sort names alphabetically. For performance with 1000 departments and 50000 employees, add indexes on `departments(parent_id)` and `employees(dept_id)` before serving traffic — verify with `EXPLAIN QUERY PLAN` that scans became searches. Build the endpoint with Flask/FastAPI/`http.server` bound exactly to `127.0.0.1:8080`, test it locally with `curl` against a couple of dept_ids before declaring it ready, and keep the process running in the background (e.g. `nohup ... &`) so the verifier can reach it.
