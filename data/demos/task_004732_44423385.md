Treat each of the four subtasks as an independent scripted check-and-produce step. For the process scan, iterate `/home/user/proc_dump/*/cmdline`, and since each file is null-byte separated, read it with `tr '\0' '\n' < cmdline` (or `xargs -0`) to split into individual arguments, then grep each argument for lines starting exactly with `--secret-token=` and strip that prefix to get the token; pair it with the PID (the directory name), format as `PID:TOKEN`, and pipe the whole set through `sort -n` before writing the file. For encryption, run `openssl enc -aes-256-cbc -pbkdf2 -salt -pass file:/home/user/audit.key -in /home/user/audit_trail.txt -out /home/user/audit_trail.enc` — double check the passphrase file has no trailing newline issues that would change the derived key, and test round-trip decryption yourself. For integrity, use `sha256sum -c /home/user/manifest.sha256` (or manually recompute and diff) against files in `/home/user/web_app/`, treating both "hash mismatch" and "file missing" as compromised, then sort the filenames output alphabetically. For the CSP header, read `/home/user/trusted_domains.txt` line by line and join with spaces (e.g. `paste -sd' '`) into the exact header string format given, preserving file order.