Build this pipeline in dependency order and validate each stage independently before wiring the next. (1) Run `tesseract /app/config/specs.png stdout` to OCR the calibration image, then parse the extracted text for a line matching `MULTIPLIER=<number>` (use a regex, don't hardcode) to get the float multiplier. (2) For each CSV in `/app/input/`, load with pandas, parse `ts` as a datetime index, multiply `val` by the extracted multiplier, then resample with `.resample('1H').mean()` — confirm bin labels align to the start of each hour as pandas does by default. (3) Create `/app/db/timeseries.sqlite3` (making the `db` directory first), write a `hourly_data` table with columns `sensor_id`, `hour_ts` (ISO8601 string, not pandas Timestamp object — format explicitly), `avg_val`, deriving `sensor_id` from each filename's stem. (4) Write a small web server (Flask is simplest) on `127.0.0.1:8000` with `GET /data?sensor_id=...` that queries the SQLite table, checks the `Authorization: Bearer ops_token_99` header exactly and returns 401 on any mismatch/missing header, and otherwise returns the rows as a JSON array sorted by `hour_ts`. Launch the server in the background (e.g. `nohup ... &` or a daemon thread) so it stays reachable for testing, and manually curl both an authorized and unauthorized request before finishing.