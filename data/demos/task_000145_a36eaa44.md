Start by OCR'ing the image: `tesseract /app/config.png stdout` (install tesseract-ocr if missing), then parse the two lines to get WINDOW and IGNORE — don't hardcode guessed values, derive them from the OCR output (strip whitespace/noise carefully since OCR can misread digits). For the dataset, read the file as raw bytes and convert with `iconv -f ISO-8859-1 -t UTF-8` (or handle byte-by-byte in C using the fact that ISO-8859-1 maps 1:1 to Unicode code points 0-255, so each byte can be widened directly to a UTF-8 sequence) — do not just read it as ASCII/UTF-8 or you'll corrupt non-ASCII tokens. Group records into a hash map keyed by token, sort each group's entries by timestamp, then run a sliding-window average using only the last min(WINDOW, count-so-far) values, tracking the running max per token. Skip rows whose token equals the OCR'd IGNORE token entirely (don't include them in any group). For the TCP server, use a standard `socket()/bind()/listen()/accept()` loop on 127.0.0.1:9090, read until `\n`, strip it, look up the token in your max-average map (format with `%.2f`), and write `NOT_FOUND\n` for ignored/unknown tokens — keep the accept loop running for multiple sequential connections, not just one.