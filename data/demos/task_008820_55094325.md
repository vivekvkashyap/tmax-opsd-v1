Two independent bugs to find by reading the code carefully, not guessing. In the Makefile, the shared-library target is missing the flags needed to produce a proper Linux `.so`: the object file must be compiled with `-fPIC` (position-independent code) and the final link step needs `-shared` — add both, since a `.so` linked without `-shared` behaves like a regular executable and ctypes/dlopen will reject or mishandle it. In `filter.cpp`, look for a heap allocation (`new[]`) inside `moving_average` that is written to but never released with a matching `delete[]` before the function returns — free it right before the function's `return`/end of scope. After fixing both, run `make` to build `libfilter.so` and `test_runner`, then run `valgrind --leak-check=full ./test_runner 2> /home/user/pipeline/valgrind.log` and open the log to confirm it reports zero bytes definitely lost — if it still leaks, you fixed the wrong allocation or missed a path where the function returns early. Finally run `python3 benchmark.py > /home/user/pipeline/benchmark_result.txt` and confirm the file contains real timing output, not an import/load error (which would mean the .so still isn't built correctly for ctypes).