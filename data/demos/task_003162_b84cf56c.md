Split into pcap analysis and WAL parsing. For the pcap, use `scapy` or `tshark -r traffic.pcap` to inspect each TCP packet's payload/destination port and locate the one whose payload equals the literal ASCII string `POISON_PILL` on port 9000, then read its source IP straight from the packet's IP header (don't assume which packet it is) and write only that IP to `/home/user/attacker_ip.txt`. For the WAL, write `recover.cpp` as a streaming byte-level parser: loop reading records from `server.wal`, at each position read the 4-byte magic and bail/stop if it doesn't match `"KVLG"` or if fewer bytes remain than needed (that's the "unexpected EOF" case); otherwise read the little-endian uint32 record length, then Opcode/KeyLen/Key/ValLen/Value per the given layout, computing a running XOR checksum over Opcode+KeyLen+Key+ValLen+Value as you go and comparing it to the trailing checksum byte — on mismatch, drop that record and stop parsing entirely (don't skip-and-continue). Only emit records with Opcode `0x01` (SET), skip others (e.g. `0x02`) but keep parsing past them, and write surviving `Key=Value` lines to `/home/user/recovered.txt` in original file order. Test incrementally by dumping intermediate parsed fields to stderr while debugging against the actual file bytes.