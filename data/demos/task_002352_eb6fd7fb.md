Start by decompiling `/app/diag_server.pyc` (e.g. `uncompyle6`/`decompyle3` or manually reading bytecode with `dis` if decompilation tools fail on 3.10) to recover the HTTP server's routes, required `Authorization` header value, and lock usage — do not guess the API shape or reimplement from scratch. Once you have the source, find the deadlock: look for two code paths (e.g. one handler acquiring lock A then lock B, another acquiring B then A) — lock-order inversion under concurrent requests is the classic cause; fix it by always acquiring locks in the same global order (or by narrowing lock scope so only one lock is ever held at a time). For the corrupted WAV, don't rely on Python's `wave` module's header-size fields since they're zeroed/truncated — instead pipe the file through `ffmpeg` (which tolerates broken RIFF size fields) to decode/re-encode it to a clean WAV, then run transcription (e.g. `whisper`) on the ffmpeg-repaired audio. Add an assertion that the decoded audio duration is > 0 before transcribing. Wire the recovered auth token into the `/transcribe` endpoint's check, start the server on `127.0.0.1:8000` in the background, and test it yourself with concurrent curl requests to `/metrics` and `/transcribe` plus one real call against `/app/corrupt_diag.wav` before finishing.