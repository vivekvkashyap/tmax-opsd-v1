This task has two independent deliverables — do not let one block the other. For the proxy: write `/home/user/nginx.conf` with `daemon off;`, explicit `pid /home/user/nginx.pid;`, `error_log`/`access_log` directives pointing at the required paths, and a `server { listen 8080; }` block with two `location` blocks (`/login` and `/steal`, each with `proxy_pass` to the respective upstream, preserving the path since these are prefix matches not `=` matches). Confirm `auth-svc` and `token-logger` are actually running first (start them, check with `curl` or `ss -ltnp`) before pointing nginx at them, then launch nginx with `nginx -c /home/user/nginx.conf` backgrounded (e.g. via `nohup ... &` or a detached process) so your shell isn't blocked, and test the redirect chain with `curl -I`. For the analyzer: scaffold with `cargo new payload_analyzer`, add the `url` and `percent-encoding` crates, implement the exact rule order in the spec (percent-decode fully, trim whitespace, then branch on `http://`/`https://`/`//`/other), building a `http:` prefix onto bare `//host/path` inputs before calling `Url::parse` so the host parses correctly, and treat parse failures or a missing host as `INVALID`. Build with `cargo build --release` so the binary lands at `target/release/analyzer`, and manually test the three example payloads from the prompt before considering it done.
