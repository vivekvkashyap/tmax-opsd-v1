Build the C program in clear stages and test each independently. Compile the regex once with `regcomp` using the exact POSIX ERE pattern given, apply `regexec` per line, and skip (don't crash on) any line that fails to match or fails to open the file. For sensor-name cleanup, implement classic Levenshtein DP (a small 2D or rolling int matrix) comparing the raw extracted name against each of the three canonical names, take the minimum distance, and drop the row if that minimum exceeds 2 — keep the "first on tie" behavior simple since the fixture data has no ties. Deduplication must compare only against the immediately preceding *accepted* row (same timestamp string, same mapped sensor name, same value) — don't compare against all previously seen rows, and remember rejected rows (fails regex/distance) don't count as "preceding" for this check. For bucketing, truncate the timestamp string to `YYYY-MM-DDTHH:00:00` (string manipulation is simpler and safer than full date arithmetic here) and accumulate sum+count per (hour,sensor) pair, e.g. in parallel arrays or a small hash map. Sort output keys first by hour string then by sensor name alphabetically, and format each line with `%.2f` for the average, matching the template exactly. Compile with the exact gcc command given and run it before declaring done.