Do the steps in the given order so each check builds on a stable prior state. First `cp /home/user/src/collector.c /home/user/backup/collector.c.bak` to preserve the original before editing anything. Then edit `/home/user/src/collector.c`, changing the `fopen` path from the relative `"./metrics.json"` to the absolute literal `"/home/user/dashboard/metrics.json"`, and recompile with `gcc /home/user/src/collector.c -o /home/user/bin/collector`, overwriting the existing binary in place. For the port forwarder, since you lack root for iptables, use `socat` in listen/fork mode exactly as hinted: run `socat TCP4-LISTEN:9090,bind=127.0.0.1,reuseaddr,fork TCP4:127.0.0.1:8080 &` and immediately capture its PID with `echo $! > /home/user/proxy.pid` right after backgrounding it (not from a subshell, or `$!` will be wrong). Then run `/home/user/bin/collector` once manually and confirm `/home/user/dashboard/metrics.json` now exists with the expected content. Finally verify end-to-end by curling `http://127.0.0.1:9090/metrics.json` (through the socat proxy) and confirming it returns the dashboard's metrics, and check `kill -0 $(cat /home/user/proxy.pid)` succeeds to prove the proxy is still alive.