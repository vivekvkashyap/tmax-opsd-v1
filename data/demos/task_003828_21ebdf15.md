Build the pipeline as a sequence of verifiable stages rather than one monolithic script. Write `requirements.txt` (pandas, numpy, scikit-learn) and `pip install -r` it. Load `raw_data.csv`, then impute only the three feature columns with `KNNImputer(n_neighbors=3)` — fit/transform on `f1,f2,f3` together (not the target, and not column-by-column, since KNN imputation needs the joint feature space to find neighbors). For outlier removal, compute the target's median and MAD (`median(|y - median(y)|)`) yourself, derive the modified z-score `0.6745*(y-median)/MAD` per row, and drop rows where `abs(z) > 3.5` — apply this filter to the whole dataframe (features + target together) so X and y stay aligned, and do it after imputation, not before. Fit `Ridge(alpha=1.0)` on the cleaned data, predict on that same training data, and compute MSE and R² with sklearn's metrics functions rather than hand-rolling them (easy to introduce bias/df errors). Create `/home/user/artifacts`, dump the model with `joblib.dump`, and write `metrics.json` with float `mse`/`r2` keys. Sanity-check your R² is high (data is a near-linear signal) before finishing.