Chain three recovery steps, each feeding the next. First, transcribe `/app/voicemail.wav` (e.g. with `whisper` or another available speech-to-text tool) to learn the backdoor mechanism and where the key lives — do not assume the mechanism, read it off the transcript. Second, use the transcript's pointer to locate the key inside `/app/upload_handler`: since it names an ELF symbol, dump the symbol table (`nm`/`readelf -s`) to find that symbol's address, then read the bytes at that address from the binary's data section (e.g. `objdump -s -j .data` or `objdump -s -j .rodata`, matching offsets) to recover the exact key bytes — don't hardcode a guessed key. Third, write the Rust `/home/user/detector`: read the raw byte payload from the file path given as argv[1], decode it with the mechanism named in the transcript using the extracted key, then check the decoded string for `../` anywhere or a leading `/`, exiting 1 if either is found (malicious) and 0 otherwise (safe). Build with `cargo build --release` (or `rustc`) to the exact path `/home/user/detector`, and sanity-test it against a couple of self-crafted encoded clean/evil filenames before trusting the corpus run.
