Split this into two independent deliverables. For Part 1, extract frames from `/app/uptime_feed.mp4` with `ffmpeg -i uptime_feed.mp4 frame_%04d.png` (or sample per-frame average color via `ffprobe`/`ffmpeg` signalstats), then for each frame programmatically classify it as red or green by checking the dominant channel of a representative pixel/average color rather than eyeballing — build the list of red frame indices this way and confirm the count and contiguity look sane (a single contiguous red run is expected for a "down" event) before writing them one per line, ascending, to `/home/user/downtime_frames.txt`; get your frame indexing convention right (0-based, matching ffmpeg's `-show_frames` output). For Part 2, work out the parsing and checksum rules directly from the spec: extract `ID`, `UptimeSecs`, `Load_A`, `Load_B`, `Checksum` with a strict regex/awk pattern that rejects malformed lines (missing brackets/pipes/whitespace anomalies), track that IDs increase by exactly 1 across lines, and compute `(ID + UptimeSecs + floor(Load_A) + ceil(Load_B)) % 100` using correct floor/ceiling semantics for negative floats (e.g. `floor(-1.5) = -2`, not truncation toward zero) — test `/home/user/detector.sh` against hand-built edge cases covering each evil category before trusting it on the hidden corpus.
