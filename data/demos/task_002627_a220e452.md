Apply the provided patch first with `patch /home/user/interpreter.sh < /home/user/fix_globbing.patch` (or `git apply`) rather than manually re-typing the fix — inspect the patch to understand it adds `set -f` to disable globbing so tokens like `*` aren't expanded by the shell before reaching the RPN evaluator. In `test_runner.sh`: read `fixtures.csv` line by line with `IFS=, read -r id expr`, write each `expr` verbatim into `test_data/test_<id>.txt` (no extra newline needed). For concurrency, launch each interpreter invocation as a background job redirecting its stdout to a per-test temp file (e.g. `/home/user/interpreter.sh "$expr" > /tmp/out_$id.txt &`), collect all PIDs, then `wait` for all of them before assembling the report — this is the key correctness point, since writing directly into a shared report file from concurrent subshells without per-test temp files risks interleaved/lost output. After `wait`, loop over the ids again (numerically sorted) reading each temp file's result into `Test <id>: <result>` lines and write them to `test_report.log`. Make both scripts executable and run `test_runner.sh` to produce the final artifacts.