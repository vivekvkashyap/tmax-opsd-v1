Pipeline this in stages and validate each before moving to the next. First, load the irregular `time`/`displacement` CSV and use `scipy.interpolate.CubicSpline` (or `interp1d(kind='cubic')`) to resample onto a uniform grid `t=0..10` step `0.01`; this uniform series is what both the FFT and the fit target use. For frequency: run `np.fft.rfft` on the interpolated signal, compute the corresponding `np.fft.rfftfreq`, find the peak magnitude bin while explicitly excluding index 0 (DC component), convert peak frequency f to angular frequency via omega=2*pi*f. For the ODE fit: write a `simulate(c,k,F)` helper using `scipy.integrate.solve_ivp` on the state [x,v] with x'' + c x' + k x = F sin(omega t), zero initial conditions, evaluated at your uniform time grid; wrap the residual (sim - interpolated data) in `scipy.optimize.least_squares` with the given initial guesses and bounds so it stays well-conditioned. Use the FFT-derived omega as a fixed known constant in the ODE (not something you re-fit) since the objectives list it as separately determined. Save rounded JSON and a comparison plot with matplotlib; check the optimizer actually converges (inspect residual norm) rather than trusting an unconverged result.
