Handle the four parts in dependency order. First fix the CMake RPATH issue: in `/home/user/legacy_audio/CMakeLists.txt` add `set(CMAKE_BUILD_WITH_INSTALL_RPATH TRUE)` and `set(CMAKE_INSTALL_RPATH "$ORIGIN/../lib")` (or wherever `libaudio_hash.so` actually lands relative to `hasher_cli`, e.g. `$ORIGIN` if in the same dir) — rebuild with `cmake --build build` and confirm `./build/hasher_cli` runs without `LD_LIBRARY_PATH` set, checking with `ldd`/`readelf -d` that RPATH/RUNPATH is set correctly. Second, transcribe `/app/voicemail.wav` (e.g. `whisper` or `ffmpeg`+a speech tool) to recover the secret seed phrase — listen carefully/re-transcribe if unclear, since this exact phrase must match what the CLI is invoked with. Third, build the Bash HTTP server with `socat TCP-LISTEN:9090,reuseaddr,fork EXEC:handler.sh` (or nc-based loop): parse the incoming HTTP request to extract the JSON body, pull out the `transcript` field with basic JSON parsing (grep/sed or `jq` if available), call `hasher_cli` with the transcript and the recovered seed phrase, and build a proper HTTP/1.1 response with correct headers, status line, and JSON body — malformed JSON must yield 400. Fourth, write `/home/user/fuzz_test.sh` that loops 50 times generating random valid/invalid JSON and curling the endpoint, asserting each response is 200 or 400 and the connection doesn't drop. Leave the socat server running in the background at the end.