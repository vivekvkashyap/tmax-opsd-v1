Diagnose each of the three bugs independently by running `./build.sh` first and reading the actual error, rather than assuming which bug you hit. For the secret key, don't guess — recover it from git history with `git log -p -- config.json` (or `git log --all -p config.json`) to see the diff that removed it, and copy the exact original value back into `config.json`. For the build script crash, the root cause is unquoted variable expansion in the `for f in data/*.json; do python3 process.py $f; done` loop — filenames containing spaces get word-split into multiple arguments; fix by quoting `"$f"` (and consider `find ... -print0 | while read -r -d ''` for extra robustness), without renaming any files in `data/`. For the encoding issue, use `file data/*.json` or try opening each file to find the one that isn't UTF-8 (likely UTF-16), then in `process.py` detect and handle it — e.g. try reading as UTF-8 first and fall back to UTF-16 on `UnicodeDecodeError`, or use a library like `chardet`/`charset_normalizer` to detect encoding per file. Re-run `./build.sh` after each fix and inspect `output/combined.json` to confirm all 3 files' data plus the recovered `auth` key appear.
