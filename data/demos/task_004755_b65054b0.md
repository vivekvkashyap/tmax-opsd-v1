Build this as a pipeline of stages in `simulate.go`, printing intermediates while developing so each stage can be sanity-checked. Parsing: keep only lines whose first token is `ATOM`, slice columns 47-54 (1-indexed, Go `line[46:54]`) for Z, trim and `strconv.ParseFloat`. Meshing: get Z_min/Z_max, take `math.Floor`/`math.Ceil` for domain bounds, build unit-width cells across the range, then for each cell check (half-open interval, last cell's upper bound inclusive) whether any atom Z falls inside it — split into two 0.5-width cells if so, else leave at 1.0 width. Domain split: bucket cells by whether their midpoint is below or at/above `(Z_start+Z_end)/2`. Integration: sum `midpoint^2 * width` (midpoint rule) over all cells in original order. Stability: compare `dt=0.2` against `dx_min^2/2` using the smallest cell width actually present. Format each field exactly per the `results.txt` template (4 decimals where specified, integers for counts), then `go run simulate.go` and inspect the output.
