Build this as a strict shell pipeline, stage by stage, verifying each stage's output before moving on. Extract per-frame luma with `ffmpeg -i input -vf signalstats -f null -` and parse YAVG out of the stderr log lines with `grep`/`sed`/`awk` (count lines carefully to get frame_index right). Build `features.csv` with awk in a single pass, tracking the previous `luma_avg` in a variable to compute `delta_luma` (0 for the first row) — this avoids off-by-one errors from re-reading the file. Filter rows with `luma_avg >= 50.0` using numeric (not string) awk comparison before bootstrapping. For the bootstrap, load the filtered delta_luma column into a Bash array, then loop 1000 times picking N random indices with replacement (`$RANDOM`-based), computing each sample's mean with awk/bc, and accumulate a running sum of the 1000 means to divide by 1000 for the grand mean — write only that final float to the result file. Make it one executable script taking the video path as `$1`, and actually run it against the real video to confirm both output files are produced.
