Break this into extraction, schema, and algorithm phases and validate each before moving on. For extraction, use `cv2.VideoCapture('/app/etl_monitor.mp4')`, iterate frames with `cap.read()`, run each frame through `pyzbar.decode(frame)`, `json.loads` the decoded payload, and deduplicate edges (e.g. by a set keyed on the full tuple) since the same QR code likely repeats across several consecutive frames — don't assume one frame = one unique edge. For the SQLite schema at `/home/user/etl_graph.db`, create an `edges` table with columns matching the JSON fields, and critically add a composite index on `(tx_id, source)` — without it, per-tx_id lookups fall back to full table scans and the 5000-query benchmark will blow past the 1.5s threshold; with the index, filtered lookups are fast. In `analyze.py`, implement `find_deadlocks` with a per-tx_id DFS/cycle-detection pass over that tx's edges (a transaction has a deadlock iff its subgraph contains a cycle), returning sorted unique tx_ids with cycles. Implement `get_shortest_path_duration` using either a `WITH RECURSIVE` SQL query filtered by `tx_id` or by fetching that tx's edges via the indexed query and running Dijkstra/BFS with `heapq` in Python — always use parameterized queries (`?` placeholders), never string-interpolated SQL, both for correctness and to let SQLite cache the query plan across repeated calls.