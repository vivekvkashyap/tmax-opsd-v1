Diagnose this as an environment-inheritance bug: the worker script falls back to `/home/user` whenever `APP_DIR` is unset, and a scheduler-invoked process won't inherit env vars set only in an interactive shell — so the fix belongs in `trigger_worker.sh` itself (`export APP_DIR=/home/user/app_data` before launching `service_worker`), not in the worker binary. Verify by clearing `data.log` from both locations, running the trigger script standalone (simulating a stripped scheduler environment, e.g. `env -i /home/user/trigger_worker.sh`), and confirming output lands only in `app_data/`. For `monitor.sh`, compute directory size with `du -sb /home/user/app_data | awk '{print $1}'` (bytes, not blocks) and branch on the 1024-byte threshold: over quota, find the worker's PID via `pgrep -f service_worker` and kill it (`pkill -f` or `kill $(pgrep ...)`), then write the exact string `QUOTA EXCEEDED`; otherwise write `OK`. Test both branches explicitly — start the trigger, run monitor while under quota to confirm `OK`, then pad the directory past 1024 bytes (e.g. `dd`) and rerun monitor to confirm it kills the process and logs `QUOTA EXCEEDED`, checking with `pgrep` that nothing is left running afterward.