Treat this as query-plan rewriting, not reimplementation. First read `/home/user/naive_pipeline.json` closely to learn the exact stage syntax the stripped `/app/graph_engine` binary accepts ($match, $lookup, $unwind, $project, field names, operators) — don't assume standard MongoDB semantics beyond what's shown. Identify which `$match` filters (e.g. node type, region, status) currently run *after* the `$lookup` joins, and push each filter to apply to its own collection (nodes.csv) as early as possible, ideally as the very first stage(s), so the subsequent `$lookup` only joins against the already-shrunk candidate set instead of the full CSVs. Keep the shape and order of pipeline stages otherwise equivalent so the final projected fields and record shapes are unchanged — predicate pushdown must not alter which records ultimately match, only when filtering happens. After rewriting, run both pipelines through `/app/graph_engine` yourself and diff the two `results.json` outputs (sorted, since order may not be guaranteed) before worrying about timing, then time both runs to confirm the speedup margin comfortably clears the required threshold.
