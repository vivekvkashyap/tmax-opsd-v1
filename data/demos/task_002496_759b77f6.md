Debug this as three separate, verifiable fixes rather than one big rewrite. First reproduce the hang by running `python /home/user/app/run.py` and confirming it stalls on a negative input in `calculate_equilibrium` — Newton's method for sqrt oscillates forever on negative values since `next_x` never converges. Add an iteration counter capped at 100 inside the `while` loop; if it exits without converging, `raise RuntimeError("Convergence failed")`. In `worker.py`'s `process_data`, wrap the call to `calculate_equilibrium` in `try/except RuntimeError as e`, print `f"Skipped corrupted input: {value}"`, and `continue` to the next line instead of aborting. For the leak, change `seen_records` handling so after each append you trim it back to the last 50 entries (e.g. `if len(seen_records) > 50: seen_records.pop(0)`, or use `collections.deque(maxlen=50)`). After all three fixes, run `python /home/user/app/run.py` end-to-end and confirm it completes (doesn't hang), produces exactly one output line per positive input, and writes `/home/user/app/success.txt`.
