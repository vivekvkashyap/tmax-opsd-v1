Transcribe `/app/instructions.wav` first — use a speech-to-text tool (e.g. `whisper`, `speech_recognition`, or any available offline/CLI ASR) and read the transcript carefully before writing any code; do not guess the algorithm from the task description alone since the exact leakage-prevention procedure is dictated by the audio. Implement `/home/user/transform.py` to: read floats one-per-line from stdin, treat the sentinel missing-value marker described in the audio specially, compute statistics (mean/std) using ONLY the first N rows' valid (non-missing) values — this is the crux of avoiding leakage, since test-set rows must never influence the training statistics — then apply those same train-derived statistics (imputation, clipping bounds, standardization) to the entire dataset including rows beyond N. Handle the zero-std edge case as specified. Format every output value to exactly 4 decimal places, one per line. Before finishing, test your script manually against constructed stdin covering missing values, outliers near the clip bounds, and values beyond N, since the fuzzer will compare byte-for-byte against a reference implementation across many randomized inputs — small formatting or rounding differences (e.g. banker's rounding vs standard rounding) will fail the equivalence check.
