For the transcription task, don't try to hand-craft a solution — install a real ASR tool (e.g. `pip install openai-whisper` plus `ffmpeg` for decoding, or a `SpeechRecognition`+Google/Sphinx backend) and run it against `/app/ticket_urgent.wav`, then write whatever text it produces to `/home/user/voicemail_transcript.txt`; if the first model mumbles domain-specific terms, try a larger whisper model size for better accuracy on technical vocabulary. For the classifier, resist writing a generic ML model — a small hidden corpus of clean vs. evil texts suggests a rule-based approach is more reliable: build a list of injection/command markers (shell metacharacters like `rm -rf`, phrases like "ignore previous instructions", SQL destructive verbs like `DROP TABLE`, `os.system`, `import os`) and flag EVIL if any appear, otherwise CLEAN. Validate against the small sample corpus at `/app/corpus/sample/` first, but keep the rules general (pattern families, not exact strings) since the hidden corpus will contain paraphrases of the same clean topics (solver convergence, WAL recovery, linker errors) and evil intents. Ensure classifier.py takes a directory argument and prints one `<filename>: CLASSIFICATION` line per file.
