Build the pipeline as a sequence of standard bash text tools rather than one clever command. First filter to `es-ES` rows with awk on the 3rd tab-separated field. Then deduplicate by MessageID keeping the highest timestamp: sort by MessageID ascending and timestamp descending (`sort -t$'\t' -k2,2n -k1,1nr`), then use awk with a `seen[]` array keyed on MessageID to keep only the first (i.e. highest-timestamp) occurrence per ID, emitting `MessageID\tText`. Next, generate the gap-fill: iterate MessageID 1..50 in awk, defaulting any ID missing from the deduped set to the exact placeholder text (watch UTF-8 handling — make sure your locale/awk doesn't mangle the accented/special characters; setting `LC_ALL=en_US.UTF-8` or `C.UTF-8` before running helps), and outputting each present or defaulted ID in ascending numeric order. Write the final 50 lines, two tab-separated columns, no header, to `/home/user/es_final.tsv`. Verify by counting lines (`wc -l` should be 50) and spot-checking the previously-duplicated IDs picked up the most recent text.
