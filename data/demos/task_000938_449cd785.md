Break this into the SQL/graph steps and validate each incrementally in a Python REPL or scratch script against `/home/user/papers.db` before finalizing `process.py`. First write the direct-citation-count query (a `GROUP BY cited_id` count on `citations`), then wrap it with a window function `RANK() OVER (PARTITION BY year ORDER BY total_citations DESC, id ASC)` to get per-year rank, and filter to `rank = 1`. Sort those by `year DESC` and slice `[:2]` for the pagination step — don't reorder before slicing. For "indirect citations" of a target paper A, first find all direct citers B of A (`citing_id` where `cited_id = A`), then find all C where `cited_id` is in that B-set (i.e., citers of citers), collect their titles, dedupe, and sort alphabetically — be careful this is a two-hop traversal, not one-hop. Assemble the list of dicts with the exact key names and types required by `schema.json`, validate with `jsonschema.validate` against the loaded schema, and only write `results.json` (2-space indent) after validation passes — catch and surface `ValidationError` rather than writing invalid output.