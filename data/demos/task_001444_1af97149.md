Don't guess the formula — transcribe `/app/metric_dictation.wav` fully with a speech-to-text tool and write down the exact spoken rules verbatim (weight thresholds, comparison operators like "strictly less than" vs "inclusive", and the exact output formatting instruction) before writing any code, since off-by-one boundary conditions in age brackets are the easy way to fail equivalence testing. Once you have the transcript, implement it in Go: parse the three comma-separated CLI args into `[]float64, []float64, []int`, iterate index-by-index computing the squared prediction/truth difference, multiply by the age-dependent weight per the transcribed boundary rules, sum, divide by N, and print with the exact `fmt.Printf` format/precision the dictation specifies (no trailing newline or extra text unless stated). Compile to exactly `/home/user/evaluator`. Before finalizing, self-test with several hand-computed examples per age bracket (a value just under, at, and just over each boundary) to confirm your boundary logic matches your transcript, since equivalence will be checked via random fuzzing across many inputs and ages.