Treat `/app/bin/spectral_sim` purely as a black-box function you can call cheaply and repeatedly — don't try to reverse-engineer its internals; instead do global optimization. Write a Python script that: loads `/app/data/experimental_spectrum.txt` once, defines an objective that calls the binary via `subprocess.run(['/app/bin/spectral_sim', str(alpha), str(beta), str(gamma)], capture_output=True, text=True)`, parses its stdout into a 1024-length array, and computes `scipy.stats.wasserstein_distance` against the experimental spectrum. Search the bounded cube `[0,10]^3` in parallel — e.g. spawn a `concurrent.futures.ProcessPoolExecutor` or `multiprocessing.Pool` over a coarse random or grid batch of candidate triples to identify a promising region, then refine locally (finer grid, coordinate descent, or `scipy.optimize.differential_evolution`/`minimize` wrapped so each worker still shells out to the binary) until you find a triple with distance ≤ 0.15. Since each subprocess call has fixed overhead, batch and parallelize across triples rather than optimizing serially. Once a satisfying triple is found, write it as `alpha,beta,gamma` to `/home/user/best_params.csv` and rerun the same subprocess+distance check yourself to confirm it clears the threshold before finishing.
