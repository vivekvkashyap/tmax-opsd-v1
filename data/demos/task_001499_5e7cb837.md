Derive your two parameters from the calibration video before writing Rust: extract frames with ffmpeg and check each for solid red (255,0,0) or solid green (0,255,0) to get counts H and N — count what's actually there, don't assume round numbers. In the Rust project, use `serde_json::Value` so unrecognized fields pass through untouched, parse `timestamp` with a datetime library that round-trips ISO-8601 with `Z`, and add exactly H hours. Mask `user_name` with N repetitions of the U+2588 block character as a literal codepoint. For "parallel but ordered", use `rayon`'s parallel iterator over indexed lines, collecting results tagged by original index and re-sorting before printing, rather than anything that could reorder output. Pay special attention to correctly handling surrogate-pair Unicode escapes in `user_name`/`message` — verify with hand-crafted surrogate-pair test lines before building in release mode and copying the binary to the required path.