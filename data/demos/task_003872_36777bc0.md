Set up a Rust binary (cargo new cleaner) and add a CSV-reading approach (manual split on commas, or the `csv` crate if network access allows; otherwise parse lines manually). For each data row, apply all filters together: skip if error_flag == 1, skip if sensor_a or sensor_b field is empty/missing, and skip if parsing as f64 fails or the parsed value is NaN (use `.parse::<f64>()` and check `is_nan()` — a literal "NaN" string won't necessarily produce a NaN via naive parsing bugs, so explicitly check both the string case and the numeric case). Only after filtering, accumulate the valid (sensor_a, sensor_b) pairs. Compute sample covariance with the n-1 (Bessel-corrected) denominator: mean_a, mean_b over the valid rows, then sum((a_i-mean_a)*(b_i-mean_b))/(n-1). For the Bayesian update, implement the two closed-form formulas exactly as given (posterior variance from prior variance 10.0 and likelihood variance 25.0 combined via precision-weighting, then posterior mean from precision-weighted average of prior mean 50.0 and sample mean). Serialize covariance, posterior_mean, posterior_variance as f64 into a JSON object at /home/user/results.json (e.g. via manual string formatting or serde_json), and sanity-check by re-deriving the numbers with an independent scratch calculation (python or a spreadsheet) before finalizing.