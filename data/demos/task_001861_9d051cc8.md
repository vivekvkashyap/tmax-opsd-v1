Work stage by stage and validate each before moving on. First, locate executable-bit `.tar.gz` files with `find /app/raw_data -name '*.tar.gz' -perm -u+x` (or check `os.stat().st_mode & stat.S_IXUSR` in a script) and extract only those into `/home/user/extracted/` — do not extract the non-executable distractors. Next, run `/app/decoder.bin <file>` on each `.dat` file and capture stdout to see the exact multi-line record format it emits (do this manually first to understand the real structure, since binary internals may differ subtly from the task description). Then write `sanitizer.c` as a strict line-oriented state machine reading stdin: buffer exactly 5 lines at a time, validate line 1 is literally `[RECORD]`, line 2 matches `^SEQ:[0-9]+$` with no sign/leading garbage, line 3 matches `^OBS:[A-Za-z0-9 ]{1,64}$` (reject empty, reject >64 chars, reject any punctuation/shell metacharacters), line 4 parses cleanly as a float with `strtod` and no trailing garbage, and line 5 is exactly `[/RECORD]`. On any single-field failure, discard the whole record and resynchronize by scanning for the next `[RECORD]` line rather than assuming perfect 5-line alignment. Build a tiny hand-made evil/clean corpus yourself to test edge cases (negative SEQ, oversized OBS, injected `;`/`|`, missing terminator) before trusting the hidden grader. Finally pipe decoder output through your compiled sanitizer for every `.dat` file, appending to `/home/user/master_clean.log`.