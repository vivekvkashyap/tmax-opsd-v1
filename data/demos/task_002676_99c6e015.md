Treat this as a standard shortest-path problem, not a shell-scripting problem: write one Python script that (1) opens `/home/user/backups.db` with `sqlite3` and queries the `backups` table for the row matching the backup name to get `size_mb` and `current_node`; (2) loads `/home/user/network_topology.json` and builds a directed weighted graph, computing each edge's weight as `latency_ms + (size_mb / bandwidth_mbps) * 1000` using the backup's own size (not a generic size) — recompute weights per-backup since the weight formula depends on the specific transfer's size; (3) runs Dijkstra (or use `networkx.shortest_path` with `weight` callable) from the source node to `ap-northeast-1`, being careful that edges are unidirectional as stated, so don't add reverse edges; (4) assembles the result as `{"backup_name": ..., "path": [...], "total_time_ms": ...}` matching exactly the fields in `/home/user/schema.json`; (5) validates against the schema with the `jsonschema` library before writing to `/home/user/transfer_plan.json`, catching any schema violations (e.g. `additionalProperties: false` catching stray keys) early rather than after the fact.
