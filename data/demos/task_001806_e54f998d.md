Follow the incident-response steps in order and confirm each before moving on. Run `tesseract /app/device_label.png` to recover the `DEVICE_SN` and `MAGIC_KEY` values printed on the label — use whatever text OCR actually returns, don't assume values. Then compile `/app/server.c` as instructed and, per the hint, add `assert()` checks around the calculation that combines SN and key (e.g. assert the intermediate product stays within expected bounds) to surface where a signed 32-bit multiplication overflows and wraps negative — this is the classic root cause of "intermittent" crashes that only trigger for large SN*key products. Fix it by widening the relevant variables (and any intermediate expressions) to 64-bit integer types (`int64_t`/`long long`), not just the final storage, since overflow happens at multiplication time regardless of the destination type. Separately resolve the libjansson conflict by making sure your linker searches `/usr/lib` (and not `/opt/legacy/lib`, e.g. via explicit `-L/usr/lib` or ensuring `LD_LIBRARY_PATH`/rpath doesn't point at the legacy dir) before compiling. Finally run the fixed binary as a background process bound to `127.0.0.1:8080`, implement `GET /process?sn=&key=` to return the computed positive 64-bit value as plain text, and test it yourself with curl against the OCR'd values and a second arbitrary sn/key pair before leaving it running.
