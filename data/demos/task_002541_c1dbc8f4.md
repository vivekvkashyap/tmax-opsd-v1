First locate and fix the injected bug: grep `/app/networkx/networkx/classes/digraph.py` for the `add_edge` method's adjacency-assignment lines and check that both the successor (`self._succ` or `self._adj`) and predecessor structures are keyed correctly by `u_of_edge` and `v_of_edge` respectively — a subtle sabotage swaps one variable for the other, silently making edges self-loop or point to the wrong node, so diff the logic against what a correct DiGraph.add_edge should do rather than skimming. Verify the fix by building a tiny DiGraph with a known edge and checking `list(G.successors(u))` returns the right target. In `detect.py`, prepend `/app/networkx` to `sys.path` before importing networkx, parse the CSV into a DiGraph (edges tx->waiting_for), and to find the shortest cycle through `target_tx_id` compute, for every out-neighbor of the target, the shortest path back to the target (BFS/`nx.shortest_path`), pick the minimum length, then among all cycles tied at that minimum length choose the lexicographically smallest node sequence (sort candidate cycles as tuples of their node list before the closing duplicate). Print `NO_DEADLOCK` when the target is missing or has no cycle back to itself. Test against several small hand-built graphs before trusting the fuzz suite.
