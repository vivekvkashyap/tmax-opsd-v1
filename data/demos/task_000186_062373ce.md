Start services in the given order (redis-server, then the Flask API, then the Node worker), each backgrounded with output redirected to a log so you can debug independently, and confirm each is actually listening (e.g. `redis-cli ping`, `curl` the API) before moving on. Use `strings /home/user/app/diagnostics/worker_heap.dump | grep -i job` (or plain `grep`) to locate the crash-triggering job id for your own diagnostic understanding — you don't need to report it, just use it to correlate with the API log if useful. The real fix is in `worker.js`: read the recursive `calculateSum`-style function that walks `node.children`, and recognize the crash is caused by a cyclic parent/child graph in the test data causing infinite recursion. Fix it by threading a `visited` Set (of node ids) through the recursive calls and returning early (contributing 0, not re-descending) whenever a node id has already been visited — this is a graph-traversal fix, not a depth-limit hack. After restarting the worker, run `/home/user/app/benchmark.sh` (or replicate its 100-request loop) end-to-end to confirm total wall-clock time for 100 requests stays under 3.0 seconds; if it's slow, check for accidental blocking/polling delays in the Redis request/response handshake.
