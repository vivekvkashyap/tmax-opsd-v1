Parse the binary format field-by-field exactly as specified, then decompress and re-serialize. In C, `fread` the 16-byte header, verify the magic bytes equal `'C','F','G','S'` (or the little-endian uint32 `0x43464753`), and pull out the timestamp, compressed size, and uncompressed size as little-endian `uint32_t` (use `memcpy` into the struct or manual byte shifts — don't assume struct packing/alignment matches the file layout). Feed the remaining bytes (the gzip payload) to zlib's `inflate` with `windowBits = 16 + MAX_WBITS` (needed specifically for gzip-wrapped streams, plain zlib inflate init will fail on gzip headers) into a buffer sized to the uncompressed-size field from the header. Parse the resulting INI-like text line-by-line, tracking the current `[Section]` header, and only keep key=value pairs while inside `[Network]` or `[Security]`. Emit them as nested JSON objects — either build the string manually with careful escaping or shell out to `jq -n` with `--arg`/`--argjson` for guaranteed-valid formatting — and write to `/home/user/parsed_config.json`. Finally compute `sha256sum /home/user/parsed_config.json` and write `TIMESTAMP=<header timestamp>` and `SHA256=<that hash>` as the two lines of `/home/user/manifest.txt`, generating the hash fresh after the JSON file is finalized so it isn't stale.
