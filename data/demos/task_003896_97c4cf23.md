Sequence the work: OCR first, then C implementation, then wiring. Run `tesseract /app/diagram.png stdout` and parse the two percentage numbers for `service-v1`/`service-v2` out of the recognized text (don't assume standard 50/50 — read what OCR actually returns and use those numbers, or their reduced integer ratio, as the proxy weights). For environment setup, append an `export OPERATOR_BACKUP_DIR=/home/user/backup` line to `~/.bashrc` (or `~/.profile`) and `mkdir -p /home/user/backup`. In `operator.c`, read the extracted directory listing of `/home/user/manifests`, then use `system("tar -czf $OPERATOR_BACKUP_DIR/manifests_backup.tar.gz -C /home/user manifests")` (getenv the backup dir rather than hardcoding it) to produce the backup archive before writing any config. Generate `/home/user/proxy.conf` with an upstream/backend block listing `127.0.0.1:8081` and `127.0.0.1:8082`, each annotated with a `weight=<n>` (nginx) or `weight <n>` (haproxy) token whose ratio matches the OCR'd v1:v2 split — the grader parses these weight numbers with a regex and checks their ratio, so keep the format simple and standard rather than inventing custom syntax. Compile and actually run the binary so both the backup archive and `proxy.conf` exist on disk, then re-check the OCR'd percentages against the weights you wrote.