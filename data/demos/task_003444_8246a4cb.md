First inspect the schema of `backups.db` (`.schema` in sqlite3 CLI) to see the exact columns of `backups` and `backup_deps`, then open `buggy_export.py` and locate the recursive CTE's recursive term — it selects from `backups b, backup_chain bc, backup_deps bd` with no WHERE clause, which is an implicit cross join producing a combinatorial explosion. Fix it by adding explicit join predicates: the recursive step should find rows in `backup_deps` where `depends_on` equals the id of a row already in `backup_chain`, then join that to `backups` on `backups.id = backup_deps.backup_id`, so each iteration only adds direct children of nodes already in the chain. Keep the base case selecting only the `DB_PROD_FULL` row, and keep the outer query's `SUM(size) OVER (ORDER BY created_at)` window function unchanged for the running total. Uncomment/re-enable the execution and JSON-export code, run the script, and verify the resulting `fixed_metrics.json` contains only the true descendant chain (not unrelated backup entries) with monotonically increasing running totals ordered by creation time.
