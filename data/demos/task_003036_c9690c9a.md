This task has two independent discovery sub-problems that must both feed into one Rust implementation. First, extract the passphrase from `/app/voicemail.wav` using a speech-to-text tool available in the environment (e.g. `whisper`/other ASR CLI) rather than guessing — listen to/transcribe the audio programmatically and use exactly the spoken phrase as the XOR key. Second, reverse-engineer `/app/secure_logger_oracle` by feeding it varied stdin inputs (empty, plain ASCII, strings containing `&`, `<`, `>`, `"`, `'` in different positions, and raw binary bytes) and diffing outputs to infer: the exact HTML-entity substitutions applied, whether sanitization happens before or after encryption (test by encrypting known plaintext and checking if entity-encoded substrings appear encrypted), the checksum algorithm (try common 32-bit checksums like CRC32 against known outputs, checking byte order), and the exact output layout (checksum position/endianness relative to the payload). Once both are confirmed, implement in Rust: read all stdin bytes, apply the sanitization byte-by-byte, XOR-encrypt with the repeating discovered key, compute the checksum over the encrypted bytes, and write checksum-then-payload to stdout — then self-fuzz against the oracle with random inputs (including raw binary) before finalizing.
