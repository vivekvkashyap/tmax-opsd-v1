Load `/home/user/perf_logs.csv` with pandas and first drop rows where `latency_ms > 9000` (strictly greater — keep rows exactly at 9000) before fitting anything, since leaving timeout outliers in will visibly distort the regression. Define the model function `f(X, a, b, c, d) = a*C + b*D + c*C*D + d` where `X = (C, D)` is a tuple of the two feature arrays, and fit it with `scipy.optimize.curve_fit(model, (df['concurrency'], df['data_size_kb']), df['latency_ms'])` — this handles the nonlinear interaction term correctly via least squares without you needing to derive normal equations by hand. Round each of the four fitted parameters to exactly 4 decimal places with Python's `round()`, and write them to `/home/user/model_params.json` as `{"a":..., "b":..., "c":..., "d":...}` matching the key names and order given. Sanity-check by plugging the fitted parameters back into the model for a few rows and confirming residuals are small — this catches column-order mistakes (e.g. swapping which variable is C vs D) before submission, since grading requires the recovered parameters to match a reference `curve_fit` run within 0.0001.