First find the live service: scan localhost ports 9000-9010 with something like `curl` in a loop (or `nmap -p 9000-9010 localhost`) until you find the one responding to `/guest_token`. Fetch that endpoint, extract the JWT, and crack it offline by trying each candidate secret from `/home/user/weak_passwords.txt` against the token with HS256 verification (e.g. using PyJWT's `jwt.decode(token, candidate, algorithms=["HS256"])` in a loop, catching `InvalidSignatureError` until one succeeds) — do not attempt online brute force against the server. Once you have the correct secret, forge a new token with `jwt.encode({"user": "auditor", "role": "admin"}, secret, algorithm="HS256")`, send it as `Authorization: Bearer <token>` to `/secure_data`, and write the raw response body (no extra whitespace/newlines beyond what's returned) to `/home/user/audit_result.txt`. Watch for the common mistake of forgetting to strip trailing whitespace or adding quotes/formatting when saving the flag.
