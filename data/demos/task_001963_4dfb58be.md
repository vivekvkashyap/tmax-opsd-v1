Start by transcribing `/app/voicemail.wav` (use an available speech-to-text tool, or listen and transcribe manually) to learn exactly which graph metric to compute and which corrupted index name to worry about — extract both from the audio, don't assume them. Inspect `/app/ledger.db`'s schema (`transfers(src, dst, amount)`) with `sqlite3`, then write `/home/user/graph_audit.py` so it explicitly drops the stale index the voicemail names (`DROP INDEX IF EXISTS <name-from-audio>`) before querying, so SQLite scans the base table instead of returning stale results. Implement the metric with a query that follows outbound transfers from the given account to depth two: sum direct outbound amounts plus the outbound amounts of each direct recipient (a recursive CTE or two joined queries works well). Print only the integer result with no extra text — the script will be fuzz-tested against many random account IDs, so verify it yourself against a few hand-picked IDs where you can manually check the two-hop sum from the raw table.
