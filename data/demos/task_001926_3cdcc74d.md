This bisect is complicated by intermittent build breakage across the commit range, so a naive `git bisect run make && ./test` will misclassify unbuildable-but-logically-fine commits as bad. First read the linker error at `HEAD` to identify which flag is missing from the `Makefile`'s `gcc` invocation. Then write a `git bisect run` test script that, at each checked-out commit: attempts `make`; if it fails, applies that same known fix (e.g. `sed -i` to append the missing linker flag to the Makefile) and retries `make` so the build always succeeds regardless of the logic-bug commit boundary; then runs `process_logs.sh test_input.log` and compares its output count against the count you first captured from the known-good commit `HEAD~195` — exit 0 (good) if it matches, 1 (bad) if it differs. This decouples "does it compile" from "is the logic correct," which is the actual bisect question. Once bisect converges on the first bad commit, get its hash with `git rev-parse <sha>` and write exactly the 40-character hash (no extra whitespace) to `/home/user/bad_commit.txt`.
