Follow the pipeline stages in order and match the spec's exact arithmetic — this is a determinism task, so any deviation from the given formulas breaks verification. First extract frames with `ffmpeg -i /app/reaction_spectroscopy.mp4 frames/%04d.png`, then load each PNG in numeric filename order, take row index 49 as a 1D array, stack rows into matrix V (shape frames x width), and divide by 255.0. Second, implement NMF from scratch (do not use sklearn's NMF, since the spec requires exact multiplicative-update math): initialize W and H to all-0.5 with the specified shapes (k=2), then run exactly 100 iterations updating H first then W each iteration using the given formulas with the `1e-9` epsilon in the denominator to avoid division by zero — get the update order right (H before W) and use matrix operations (`@` for matmul, `*` elementwise), not loops that might introduce rounding differences. Third, stand up a lightweight HTTP server (Flask/FastAPI/http.server) bound to 127.0.0.1:9090 exposing GET /W, GET /H (each returning `.tolist()` JSON), and GET /health returning `{"status":"ok"}`; launch it detached (e.g. nohup ... &) so it stays alive for verification, and curl each endpoint yourself to confirm it responds before finishing.