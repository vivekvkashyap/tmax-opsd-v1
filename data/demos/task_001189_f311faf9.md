Structure this as three independent pieces you can test in isolation before combining. First, install `pytesseract`/`tesseract-ocr` and run OCR against `/app/policy_token.png` (e.g. `pytesseract.image_to_string(Image.open(path)).strip()`) to recover the token programmatically at server startup — never hardcode a guessed token string, derive it fresh from the image each run so it stays correct if the image changes. Build the server with Flask (or Python's built-in `http.server`) bound explicitly to `127.0.0.1:8000` with a single `POST /validate` route. In the handler, check `request.headers.get('X-Policy-Token')` against the OCR'd token first and return 401 immediately on mismatch/missing before touching the body. Then `json.loads` the body, `base64.b64decode` the `command` field (decode to a string, handling non-UTF8 bytes gracefully), and run `re.search` with the given regex against the decoded command — use exactly the provided pattern (case-insensitive char class already covers `e`/`E`) and return 403 on a match, else 200. Test all four scenarios from the spec locally with `curl` before leaving the server running in the background (e.g. via `nohup ... &`).
