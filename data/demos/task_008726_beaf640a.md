Filter and sort before computing anything, using pure awk/grep/sort (no Python allowed here). First isolate `SUCCESS` rows, sort them numerically descending by `ConditionNumber` (`awk -F, '$4=="SUCCESS"' file | sort -t, -k2,2gr` — note scientific-notation fields like `2.0e+10` need `sort -g`, not `-n`, to compare correctly), and take the top two to get their condition numbers and times; compute the slope as `(Time2-Time1)/(Cond2-Cond1)` in `awk` (which handles scientific notation natively in arithmetic) and write the raw numeric result to `slope.txt` — don't round unnecessarily, just let `awk`'s default number formatting or an explicit `%g`/`%.6e` printf produce it. Second, filter rows where `Status=="FAILED"` AND `ConditionNumber` (as a number, via `awk`'s numeric comparison, not string comparison which would mis-order scientific notation) is strictly greater than `1.0e+12`, extract just the `MatrixID` column, and sort numerically ascending (`sort -n`) into `unstable_matrices.txt`. Double check the boundary case: a FAILED row with a condition number below the `1.0e+12` threshold must be excluded, so verify your `awk` comparison operator is `>` not `>=` and is evaluating the field as a genuine float.
