Build `/home/user/analyze.sh` as three independent checks chained together. For file integrity, `cd` into `/home/user/server_data/www` and run `sha256sum -c ../hashes.txt` (or loop and compare manually) — the one file reported as FAILED (or whose computed hash differs from the recorded one) is your compromised file; extract just its basename, not the path. For the certificate, use `openssl x509 -in cert.pem -noout -issuer` and parse out the `CN=` field with `sed`/`grep -oP` (careful: extract only the CN value, not the whole issuer DN string with other fields like O/C). For the log analysis, grep `access.log` for lines that reference the compromised filename AND contain the literal substring `base64_decode(` (both conditions must hold on the same line), then extract the leading IP field (first whitespace-delimited token) from that line with `awk '{print $1}'`. Write all three findings into `/home/user/report.txt` using the exact three-line `KEY: value` format given, `chmod +x` the script, and run it standalone to confirm the output matches before finishing.
