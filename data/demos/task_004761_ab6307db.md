This is a numerical-stability fix, scoped narrowly: locate the linear-solve step in the Gauss-Newton loop in `/home/user/primer_opt/src/main.rs` (where `J^T J` is factorized, e.g. via `.cholesky()`), and add Levenberg-Marquardt damping by adding `λ·I` (λ = 1e-3) to the diagonal of `J^T J` immediately before the factorization/solve call — using `nalgebra`'s identity matrix scaled by `1e-3` added to the computed `j_t_j`. Do not touch the residual or Jacobian computation functions, only this one solve step, per the constraint. After the fix, `cargo build --release` and run the binary against `/home/user/data/sequences.txt`, confirming `/home/user/output/optimized_primers.json` is produced and contains no NaNs (since damping should eliminate the near-singularity). Then write a small script (bash/python/jq) that loads that JSON, averages the `final_score` field across all entries, rounds to 3 decimals, and writes the single number to `/home/user/output/average_score.txt`. Sanity-check by re-running the binary twice to confirm deterministic, finite output before finishing.
