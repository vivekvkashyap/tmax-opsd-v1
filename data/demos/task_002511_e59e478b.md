Treat the stripped binary as a black box and build a Python equivalent through systematic probing, not guesswork. First run `strings /app/legacy_signer` to spot embedded error/usage messages verbatim. Then fuzz it methodically: test argc variations (0 and 2+ args) to find the usage message; test length boundaries just below/at/above any apparent min and max (e.g. 7 vs 8 vs 32 vs 33 chars) to find the length-validation message and thresholds; test strings with special characters mixed into otherwise-valid-length input to find the character-validation message. Once validation boundaries and error strings are pinned down exactly (byte-for-byte, including trailing newline), turn to the signature generation: feed short, controlled inputs (e.g. all 'A's, then single differing characters at each position) to the binary via `ltrace`/`strace`/direct invocation and diff outputs to isolate a per-character, per-index transformation — check whether it depends on ASCII value and position index, and what output encoding (hex? case?) is used. Encode all findings into a single Python script mirroring exit codes, stdout text, and formatting exactly, then re-verify with a fresh batch of edge-case and random inputs compared directly against the binary's output before finalizing.