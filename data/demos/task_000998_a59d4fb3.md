Start by inspecting both CSVs' headers to infer the schema before writing any code: stations.csv gives you a station id/name/zone table, routes.csv gives you directed edges with a travel_time weight and an op_status flag — only rows where op_status is the active value should be usable as graph edges. In setup_db.py, create two tables (stations, routes) with sqlite3's Python module, load rows from the CSVs with the csv module, and write the matching CREATE TABLE / CREATE INDEX DDL into schema.sql as plain SQL text (design indexes on the routes table's source-node and destination-node columns, ideally combined with the status column, since the task explicitly asks for indexes optimizing outgoing/incoming active-route lookups). In shortest_path.py, connect to network.db, query only active routes, build a weighted directed graph (adjacency list or networkx DiGraph) filtered to active edges, and run Dijkstra (or networkx's shortest_path with weight) from the station whose name is "Alpha Hub" to the one named "Omega Terminus" — look up their ids by name rather than assuming they are 1 and N. Write solution.txt with exactly two lines: total integer cost, then comma-separated station ids along the path, both endpoints inclusive.