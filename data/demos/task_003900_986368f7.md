Build this as a straightforward parameterized SQL join, but be careful with the multi-column sort. Write `/home/user/audit_query.py` using `argparse` for `--resource`, `--ip-prefix`, `--limit`, `--offset`, then run a single `sqlite3` query joining `employees`, `access_logs`, and `auth_logs` on `emp_id`/`id`, filtering `department = 'Engineering'`, `resource = ?`, and `ip_address LIKE ? || '%'` — always use `?` placeholders, never string-format values into SQL. Sort with `ORDER BY access_timestamp DESC, auth_timestamp DESC, name ASC`, then apply `LIMIT ? OFFSET ?` in the SQL itself (not in Python) so pagination is correct relative to the full sort order. Before trusting your output, manually inspect the joined/sorted rows (e.g. print them) to verify the ordering matches expectations, since a wrong join (e.g. cross-joining auth records incorrectly) will silently produce extra or misordered rows. Write results as a JSON array to `/home/user/report.json` with exactly the five required keys per object, and test by running the script with sample arguments before finishing.