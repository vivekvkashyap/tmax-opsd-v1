Structure this as a straightforward read-validate-aggregate-sort-write C++ program. Parse each CSV line with `std::stringstream` and `std::getline(ss, field, ',')` for the three columns, converting timestamp with `stol` and temperature with `stod` inside a try/catch (or manual validation) so malformed rows don't crash the parse. Apply the three filters in one pass — prefix check via `sensor_id.rfind("SN-", 0) == 0`, `timestamp > 0`, and `-50.0 <= temperature <= 150.0` — discarding any row that fails any check. Accumulate per-sensor stats (count, running min/max, running sum) in a `std::map<std::string, Stats>`, which keeps keys sorted lexicographically for free, so you can iterate it directly at output time rather than sorting separately. When writing the CSV, use `std::fixed << std::setprecision(2)` for the three float fields and compute the average as `sum / count` at the very end (not incrementally) to avoid rounding drift. Compile with the exact `g++ -O3 -std=c++17` command given, run the binary, and confirm `/home/user/summary_stats.csv` matches the header and format exactly, including the trailing newline convention.
