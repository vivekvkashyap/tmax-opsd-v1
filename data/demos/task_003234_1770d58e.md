Read `/home/user/process.py` first to see the exact hang mechanism before editing. There are two independent bugs to fix in place: (1) the payload decode does `tcp.data.decode('utf-8')` with no fallback — wrap it so that on `UnicodeDecodeError` you retry decoding the same bytes as `utf-16le` (and only fall back to a safe default if that also fails), instead of substituting a poisoned value; (2) `compute_metric`'s Newton's-method loop `while abs(x*x - val) > 1e-5` never terminates for inputs like negative numbers where the sequence diverges or oscillates — rewrite it to loop at most 100 iterations (track an iteration counter), and if the tolerance is never satisfied within that cap, return `0.0` instead of continuing to loop or raising. Keep the iterative square-root logic itself intact, just bound it and fix ingestion. After patching, run `python3 /home/user/process.py` and confirm it terminates and writes a `%.2f`-formatted total to `/home/user/result.txt` — verify by tracing through what each of the three packets in the pcap should contribute once decoding and the iteration cap are correct.