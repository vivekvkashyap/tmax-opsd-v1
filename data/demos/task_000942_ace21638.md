Break this into the three independent checks the script must perform, and derive the magic-byte allowlist rather than guessing it. First, run OCR on `/app/magic_signatures.png` with `tesseract /app/magic_signatures.png -` (or `tesseract image.png stdout`) to read out the policy text, then parse the hex signatures it lists into your script's allowed-prefix list — compare a file's leading bytes using `xxd`/`od -An -tx1` or `head -c N | od`, matching hex prefixes exactly (mind that signatures may have different byte lengths, so check each candidate's own length). Second, for symlink safety, resolve the link with a loop-safe method: use `readlink -f` (or manually follow with a visited-set / bounded iteration count) so that a cycle like `loop1 -> loop2 -> loop1` is detected and treated as evil rather than causing your script to hang; also reject broken symlinks (target doesn't exist) and check the resolved target stays within the expected base directory. Third, for any temp-file writes during header inspection, write to a `mktemp` file and `mv` it into place atomically rather than writing in-place, to avoid partial-read races. Exit 0 only when all checks pass, exit 1 otherwise, and test your script manually against a few hand-made clean/evil examples (valid PNG bytes, an ELF header, a symlink loop) before trusting it against the hidden corpus.