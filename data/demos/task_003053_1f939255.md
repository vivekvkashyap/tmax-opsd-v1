Build and wire the pieces bottom-up, then test end-to-end before declaring done. Write the C++ monitor as a loop that re-reads and fully re-parses `/home/user/usage.csv` every 2 seconds (don't try to track deltas — sum all lines each pass), checking each `InstanceID` for the literal string `CRASH_TEST` and calling `exit(1)` immediately when found. For the SMTP send, open a raw TCP socket to 127.0.0.1:2525 and write the exact HELO/MAIL FROM/RCPT TO/DATA/body/./QUIT sequence with `\r\n` line endings — read the server's response between commands if you want robustness, but the literal byte sequence is what's graded, so double check `$<TOTAL_COST>` formatting is exactly two decimals. Set up `socat TCP-LISTEN:2525,fork,reuseaddr TCP:127.0.0.1:8025 &` before starting the C++ binary so the hardcoded port 2525 actually reaches the mock SMTP server on 8025. The supervisor script must be a `while true; do ./finops_alert; code=$?; [ "$code" -eq 0 ] && break; done` style loop, compiling the C++ source first. In `start_all.sh`, background each process with `&` (socat, mock_smtp.py, supervisor.sh) and exit 0 immediately — verify manually that a restart actually produces a new PID after a crash before finishing.