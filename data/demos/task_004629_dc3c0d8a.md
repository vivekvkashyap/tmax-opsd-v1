Write the C program to parse `/home/user/mock_passwd` line by line using colon-delimited fields (standard passwd format: username:passwd:uid:gid:gecos:homedir:shell), comparing the GID field (index 3) to `1001` as an integer, not a string, to select matching users. For each matching user, first `unlink()` the existing `app_config` symlink in their home directory (readlink/remove before recreating, since `symlink()` fails if the target path already exists) and then `symlink("/home/user/releases/v2/config.ini", "<homedir>/app_config")`. Append the exact logrotate block for that user to `/home/user/logrotate_updates.conf` using `fprintf`, matching the literal template (four lines: header with homedir, `daily`, `rotate 7`, `compress`, closing brace) with no extra blank lines or trailing whitespace — mismatched formatting is the most common way this fails verification even when the logic is correct. Compile with `gcc /home/user/rollout.c -o /home/user/rollout`, run it, then manually `readlink` each symlink and `cat` the conf file to confirm output matches before considering the task done.