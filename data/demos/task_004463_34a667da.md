Build this as a small per-file `awk` extraction feeding a bash aggregation loop, not a single monolithic script. For each `sensor_X_Y.csv`, use `awk -F, 'NR>1 && $1>=1000 && $1<=1500 {if($2>max) max=$2} END{print max}'` to get that sensor's peak intensity in the target frequency band. Iterate X,Y from 0-9, bucket each sensor into one of the four 5x5 quadrants by comparing X and Y against 5 (X<5 vs X>=5, same for Y — note the task's Y-axis convention: 0..4 is the first half), accumulate a sum and count per quadrant, then compute quadrant averages with `awk 'BEGIN{printf "%.2f", sum/count}'` or `bc -l` for 2-decimal rounding. Write the four `Q*: value` lines to `/home/user/training_mesh_features.txt` in the exact order and label format specified. For the ASCII visualization, take each average, divide by 5, truncate (not round) to an integer via `awk`'s int() or bash integer division, and print that many `*` characters after `Q*: ` into `/home/user/mesh_vis.txt`. Diff your output against the exact expected format (label, colon, single space) since the grader does an exact string comparison.
