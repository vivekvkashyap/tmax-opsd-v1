Run `tesseract /app/policy_secret.png stdout` first and read the raw output to locate the line matching `POLICY_SEED: <value>`, then extract just the value programmatically (e.g. trim after the colon, strip whitespace/newlines) — don't assume a clean single-line result, OCR can add trailing whitespace or stray characters worth stripping. Load that secret into a Go constant/variable at startup rather than embedding the literal png-derived guess. Implement `/generate` and `/validate` with `net/http`: for `/generate`, decode the JSON body's `user` field, compute `sha256.Sum256([]byte(username + secret))`, hex-encode with `encoding/hex`, and build the token as `username:signaturehex`. For `/validate`, split the incoming token on the last (or first) `:`, recompute the same hash for the parsed username, and compare hex strings with constant structure (simple `==` is fine here) to decide `valid` true/false — make sure malformed tokens or unknown usernames fail closed to `false` rather than erroring. Start the server in the background and smoke-test both endpoints with `curl` against a known username to confirm you can reproduce a consistent generate→validate round trip before finishing.
