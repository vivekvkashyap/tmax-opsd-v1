Run `tesseract /app/vnc_crash.png stdout` (or similar OCR) and search the output for the line matching `PANIC: 0x<hex_string>`, extracting just the hex string after `0x` — don't assume a fixed value, read what OCR actually returns and sanity check it looks like hex. Scaffold a real cargo project at `/home/user/sre_tool` (`cargo new`), and in it parse `/app/metrics.log` line by line as `timestamp_ms latency_ms status_code`, collecting `latency_ms` only where `status_code == 200`; to compute the true P95, sort the collected latencies and index at the standard percentile position (e.g. `ceil(0.95 * n) - 1` or `n * 0.95` rounded per your chosen convention) — implement this carefully in Rust rather than approximating, since the grader checks against the exact P95 with only 1.5 tolerance. For network setup, use `ip link add sre-mon type dummy`, `ip link set sre-mon up`, and `ip route add 10.55.0.0/24 dev sre-mon` invoked via `std::process::Command` from Rust (or shell out with sudo if required by the environment). Write the logrotate config with `daily`, `rotate 7`, `compress`, and `missingok` directives to `/home/user/sre_logrotate.conf`, and emit the final `{"panic_code": ..., "p95_latency": ...}` JSON to `/home/user/report.json`.