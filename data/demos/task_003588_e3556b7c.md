First unblock the install: open `/app/pyspectro-1.0.0/setup.py` and inspect `install_requires` closely — a missing comma between adjacent string literals in a Python list causes them to be silently concatenated into one bogus package name, which is why pip fails to resolve a dependency. Fix the punctuation, then `pip install -e /app/pyspectro-1.0.0/` and confirm `from pyspectro.voigt import fit_spectrum` imports cleanly. For the science: load the CSV, call `fit_spectrum` to get an initial best-fit model and its residuals (data minus fit), then implement residual bootstrap — for 500+ iterations, resample the residuals with replacement, add them back onto the best-fit curve to synthesize a new noisy spectrum, refit with `fit_spectrum` again, and collect the resulting peak centers per iteration. After the loop, compute the mean and the 2.5th/97.5th percentiles of each peak's bootstrap distribution (`numpy.percentile`), matching peaks across iterations by sorted wavelength order so peak identity stays consistent. Write exactly the required CSV header and three rows ordered by increasing wavelength; since scoring is MAE against hidden true centers, make sure your bootstrap refit converges reliably (good initial guesses) rather than drifting.
