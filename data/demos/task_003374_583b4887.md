Diagnose why the existing integrator diverges: a fixed-step explicit Euler method on a stiff nonlinear ODE with a coarse step size can overshoot and blow up, so replace it with `scipy.integrate.solve_ivp(..., method='BDF')` or `'Radau'`, integrating the given ODE from t=0 to t=100 with M(0)=0, and confirm it converges to a stable finite value for a range of k_syn/d combinations before moving on. For the root-finding step, wrap a function `f(d) = M_sim(k_syn, d) - target_M` and pass it to `scipy.optimize.brentq` over the given bracket [0.01, 5.0], relying on the solver to bisect/interpolate to the root rather than hand-rolling search logic — verify the bracket actually changes sign (since the system reaches steady state, sanity-check the physical relationship between d and final M before trusting the root). For batch processing, read all rows from the sequences CSV and dispatch the per-row optimization in parallel (Python `multiprocessing.Pool` or a bash loop backgrounding processes with a concurrency cap), collecting each SeqID's solved d rounded to 4 decimal places, and write the final CSV with the exact two-column header and one row per sequence, preserving input order.
