First inspect the schema with `sqlite3 /home/user/audit_logs.db ".schema"` before writing any code, so column names match exactly. In your C program, open the database with `sqlite3_open`, then run a single SQL query that joins `lock_requests` to itself: find pairs (req_w WAITING on resource R by tx T1, req_g GRANTED on resource R by tx T2) to build wait-for edges T1->T2, joining `transactions` on both tx_id's to filter `start_time > '2023-01-01 00:00:00'` (string comparison works fine for this datetime format). Then self-join the wait-for edge set with itself swapping T1/T2 to find mutual pairs (T1 waits for T2 AND T2 waits for T1), keeping only rows where T1 < T2 to avoid duplicates and to satisfy the tx1<tx2 ordering requirement. Use `ORDER BY` in SQL (or sort in C) by tx1 then tx2 ascending. Write results with a hand-built JSON string (or a tiny helper) matching the exact schema — don't add extra whitespace assumptions, just ensure it parses correctly. Compile with `-lsqlite3`, and double check you never open the DB in write mode or execute any INSERT/UPDATE.