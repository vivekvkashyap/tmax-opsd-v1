Work service-by-service and verify each is actually listening before moving to the next, since failures compound through the proxy chain. Use `find /app/messy_files -name '*.prod.bak'` and `find ... -name '*.py.bak'` to enumerate targets, then rename each with parameter substitution (e.g. `mv "$f" "${f%.prod.bak}.conf"`) rather than a blind global replace, since the two suffix patterns differ. Apply the three `sed -i 's/old/new/'` substitutions to the renamed files exactly as specified (upstream address, DB_HOST, ENV) — do these before moving files into their final service directories, or after, but not both/inconsistently. Move `nginx.conf` into `/app/nginx/`, overwriting the existing broken config, and reload with the given nginx binary invocation. Move `app.py` into `/app/api/`, find the currently-crashing Flask process (`pgrep -f app.py` or check the PID against port 5000), kill it, then restart with `python3 /app/api/app.py &` and confirm with `ss -ltn | grep 5000` before testing. Only after all three services are confirmed listening should you `curl http://127.0.0.1:8080/health` to check the final JSON.
