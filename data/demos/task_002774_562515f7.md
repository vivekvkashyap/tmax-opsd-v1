Treat every path found inside an archive as untrusted input. When extracting the outer tar and any nested zip/tar members, do not call a naive `extractall()` — for each member's path, resolve it against the target extraction directory (e.g. `os.path.realpath(os.path.join(dest, member_name))`) and verify the resolved path still starts with the destination directory before writing; skip or reject any member (like ones using `../` sequences) that would escape it. Do this recursively: after extracting the outer tar, scan for nested `.zip`/`.tar` files and apply the same safe-extraction check when unpacking them too. Once extraction is safe, find all `*.log` files under the extraction root, and for each one replace occurrences of `API_KEY: <alphanumeric>` with `API_KEY: REDACTED` using a regex substitution — write to a temp file and atomically rename over the original rather than truncating in place, to avoid partial writes. Finally, gather all the redacted `.log` files and add them to a new tar.gz using `arcname=os.path.basename(path)` for each (not the original relative path) so the archive is flat. Afterward, confirm `/home/user/system_check.txt` was never touched.
