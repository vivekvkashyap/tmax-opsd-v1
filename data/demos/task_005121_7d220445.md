Reverse engineer `/app/prober` before writing any server code: use `strings /app/prober` to spot embedded protocol literals (greeting/hello strings, "METRIC"-style keywords, ack/bye tokens), and if strings alone are ambiguous, run the binary against a throwaway `nc -l` listener on the port it connects to and observe the exact bytes it sends and in what order (use `strace`/`tcpdump`/`netcat` to log the raw exchange), then work out what reply it expects after each message before it proceeds. Once you know the exact line-based protocol (greeting → ack, metric-name/value → per-metric ack, teardown → close), implement `/home/user/alert_receiver.c` as a TCP server on 127.0.0.1:9090 using a standard accept-loop (handle sequential connections one after another, don't need concurrency), matching each incoming line and replying byte-for-byte in the format the prober expects, and appending `[ALERT_RECEIVED] <name>: <value>` to `/home/user/alerts.log` for each metric. Compile and launch it in the background. For forwarding, write `/home/user/start_tunnel.exp` that spawns `ssh -L 8080:127.0.0.1:9090 monitor_user@127.0.0.1`, expects the password prompt and sends `monitor_pass`, then keeps the session alive in the background (e.g., backgrounding after confirming the tunnel is up) so port 8080 forwards to your receiver on 9090.