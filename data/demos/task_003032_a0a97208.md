For Part 1, read `/app/perf_env/config.env` and each service's actual startup code/config (`redis.conf`, the Flask API's Redis-connection code, the load generator's target URL) to find the mismatches, then edit `config.env` so Redis's port, the value the Flask API is told to use, and the load generator's `API_URL` are all consistent with what `redis.conf` and the app code actually expect (per the task, Redis on 6380, Flask listening on 5000, load generator hitting `http://127.0.0.1:5000`) — restart via the provided `start_services.sh` and confirm `/home/user/load_gen.log` contains "Flow Complete". For Part 2, implement OLS by hand in the bash script using `awk`: given 10 args as 5 (x,y) pairs, compute `slope = (n*Σxy - Σx*Σy) / (n*Σxx - (Σx)^2)`, `intercept = (Σy - slope*Σx)/n`, then residuals `y_i - (slope*x_i+intercept)` and their population variance (divide by N=5, not N-1). Print exactly `Slope: %.4f, Intercept: %.4f, ResidualVar: %.4f` with no extra text. Since this is checked by fuzz-equivalence against a hidden oracle across many random float inputs, use double-precision arithmetic throughout (awk's native floats, not `bc` with limited scale) and test your script against several hand-computed or independently-checked (x,y) sets, including edge cases with values near 0 and 100, before finishing. Make the script executable.
