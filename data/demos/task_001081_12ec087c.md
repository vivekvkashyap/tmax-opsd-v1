Key recovery first — get it exactly right or every downstream test fails. Extract all 256 frames of `/app/capture.mp4` in order, and for each sample only the exact center pixel `(width/2, height/2)`; take its Red channel value, threshold >128 for bit `1` else `0`. Pack the 256 bits into 32 bytes, 8 bits per byte MSB-first (frame 1 of each group of 8 is the high bit) — sanity-check the packing before hardcoding it. In the C++ detector: Base64-decode the file's contents to raw bytes, XOR-decrypt against the 32-byte key (cycling it across the buffer), then regex-search the decrypted bytes for `EXFIL-[A-Z0-9]{4}` (treat as raw bytes, not assumed-UTF-8 text). Exit 1 on a match, 0 otherwise. Before trusting it, manually run the binary against several files from both `/app/corpus/evil/` and `/app/corpus/clean/`, checking `$?` matches expectations, and iterate on the key or regex handling if any sample misclassifies.