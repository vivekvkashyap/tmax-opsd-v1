Break this into three sequential sub-tasks, each depending on the previous output. First, scan `auth_module.c` line-by-line (1-indexed) for exact matches of the banned function names `gets`, `strcpy`, `sprintf` — a naive regex on `strcpy(` etc. is fine but must record the true source line number, not a match index; sort matches numerically by line and write `LINE: function` rows. Second, for the XOR-encrypted log: since you know the plaintext key length is 4 lowercase letters and the plaintext's known first 13 bytes are `SECURE_LOG_V1`, don't brute-force blindly — recover the key directly by XORing each of the first 4 ciphertext bytes with the corresponding known plaintext bytes (`key[i] = ciphertext[i] ^ plaintext[i]` for i in 0..4), then verify the recovered 4-byte key is all lowercase a-z and re-derives the rest of the known prefix; write a small C (or any language) program to do this XOR recovery and decrypt the full file by repeating the key. Third, search the decrypted text for a token matching `TOKEN-` followed by exactly 16 uppercase alphanumeric characters (regex `TOKEN-[A-Z0-9]{16}`) and write only that matched string, with no trailing newline, to the token file. Verify each output file's exact byte content before moving to the next step.