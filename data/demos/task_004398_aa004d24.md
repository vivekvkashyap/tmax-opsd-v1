Structure the script around a general linear-ODE solver rather than hardcoding per-network equations, since the network topology varies per JSON file. Build, for each network, a vector of node names and a matrix/derivative function: `dX_i/dt = -decay_i * X_i + sum over edges(j->i) rate_ji * X_j`, implemented as a closure passed to `scipy.integrate.solve_ivp` (state vector ordered consistently with your node-name list) integrated from t=0 to t=50 and evaluated at the final time. Before running on real networks, implement and run `validate_solver()` exactly as specified: solve a single isolated node (no incoming edges, decay=0.1, init=100) from 0 to 50 and compare the numeric endpoint against the closed-form `100*exp(-5)` — this catches solver setup mistakes (wrong sign, wrong time span, wrong indexing) early, so don't skip it or fake the print statement. Only after that check passes should you loop over every `.json` in `/home/user/networks/`, run the same solver per graph, collect `(network_id, node_id, concentration)` rounded to 4 decimals, sort by network_id then node_id, and write the CSV with the exact header `network_id,node_id,concentration`.