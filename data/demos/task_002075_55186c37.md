Build this incrementally and verify each stage numerically before trusting the final Rust output. First, in a scratch script or by hand, parse `/home/user/experiments.csv` and identify which rows have a valid integer `particle_count` (reject "NA", "N/A", empty, or non-numeric strings — do not silently coerce to float) — group the surviving rows by `experiment_id`, preserving original CSV row order within each group, and note each group's `temperature` sequence and length L. Implement the LCG exactly as specified (`state = (a*state + c) mod 2^31`, starting at seed 42) as a single mutable counter that persists across the entire run — process experiment_ids in sorted alphabetical order, and for each one draw `1000 * L` LCG values sequentially (one advance per bootstrap-sample-element), taking `value mod L` as the resample index each time; get the draw order right since even one misplaced advance shifts every subsequent sample. Compute each bootstrap sample's mean, sort the 1000 means, and take index 25 and 975 (0-indexed) as the 2.5th/97.5th percentile bounds. In the actual Rust project, use `cargo new`, wrap I/O and JSON with `csv`/`serde_json`, keep the LCG state as a plain `u64`/`u32` you thread through calls (not `rand` crate, per the spec), round all floats to 2 decimals only at output time, and run `cargo build --release && ./target/release/pipeline` (or `cargo run`) to produce `/home/user/summary.json`.