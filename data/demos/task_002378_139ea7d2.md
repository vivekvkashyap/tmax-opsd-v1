Chain four independent numerical steps carefully, matching shapes at each boundary. Compile the C library first: `gcc -shared -fPIC -O2 -o libspectrum.so spectrum_gen.c -lm` from inside `/home/user/sim_project`. In `simulate.py`, define the ODE system exactly as given (`dA/dt=-0.5A`, `dB/dt=0.5A-0.2B`, `dC/dt=0.2B`) and solve with `solve_ivp(..., t_eval=np.linspace(0,10,100))`; transpose `sol.y` (shape `(3,100)`) to get `C_mat` of shape `(100,3)` — a common bug is leaving it un-transposed. Load the shared lib with `ctypes.CDLL('./libspectrum.so')` (or an absolute path — relative paths depend on cwd, so be careful), declare the `get_spectrum` argtypes (`c_int, POINTER(c_double), c_int`) explicitly to avoid ctypes default-int truncation bugs on the double array, call it for `comp_id` 0/1/2 with `length=200`, and assemble `S_mat` as shape `(3,200)`. Multiply `M = C_mat @ S_mat` giving `(100,200)`, run `scipy.linalg.svd(M)`, take the first 3 singular values `s[:3]`, and write them comma-joined with `f"{v:.4f}"` formatting to `singular_values.txt`. Run the script and confirm the output file was actually written before finishing.
