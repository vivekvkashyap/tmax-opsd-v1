Compile the C source with `gcc /home/user/legacy_daemon.c -o /home/user/legacy_daemon` first, and manually run it interactively (or pipe test input) to confirm the exact prompt strings it emits before scripting against it — expect scripts are brittle if the prompt text doesn't match exactly. Write `/home/user/trigger_migration.exp` using `spawn`, then a `expect "Enter Auth Key: "` / `send "cloud_admin_99\r"` pair, repeated for the `Command:` and `Target IP:` prompts with the specified values, ending in `expect eof` so the script waits for the daemon to fully exit rather than detaching early (a common naive mistake that leaves output un-flushed to the log). Run it once with output redirected (`/usr/bin/expect /home/user/trigger_migration.exp > /home/user/migration_run.log`) and inspect the log to confirm the daemon's success message actually landed in it. Finally write `/home/user/migration.cron` with a standard 5-field cron line (minute=0, every hour) invoking the expect script with the exact absolute-path command given in the task — this file just needs to contain that line, it does not need to be installed into an actual crontab.
