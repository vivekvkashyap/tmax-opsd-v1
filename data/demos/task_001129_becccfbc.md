Start by diffing the vendored CMakeLists.txt against the fixed requirement: the task states the perturbation sets `CMAKE_CXX_STANDARD` to 11 when it must be 17 — locate that exact `set(CMAKE_CXX_STANDARD ...)` line and correct only that value, don't rewrite the rest of the file. Then write `detector.cpp`: use the vendored csv-parser's `CSVReader` to stream rows (don't buffer the whole file — construct `CSVReader` and iterate `CSVRow`s) computing running sums for mean(sensor_x), max(|sensor_y|), and sum-of-squares(sensor_z) to derive RMS in one pass. Implement the MLP forward pass literally from the given matrices: compute the 2 hidden pre-activations as dot products of `[f1,f2,f3]` with each row of W1 plus bias, apply ReLU, then the single output neuron via W2 dot the ReLU outputs plus b2, apply sigmoid, and print `EVIL` if `>= 0.5` else `CLEAN` — do this with plain arithmetic, no libraries. Build with CMake linking the fixed csv-parser and C++17 (`-std=c++17`). `run_filter.sh` should just invoke the compiled binary and translate its stdout (`EVIL`/`CLEAN`) into exit code 1/0. Validate against the clean/evil sample corpora yourself before finishing.
