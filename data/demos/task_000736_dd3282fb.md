Reverse-engineer `/app/legacy_categorizer` empirically before writing any Python: create several small synthetic `.dat` files with controlled byte patterns (e.g. all zeros, then incrementing bytes at specific offsets) in the first 16 bytes, run each through the binary, and compare outputs to isolate which byte ranges map to which output field — use `xxd`/`od` to inspect your crafted headers alongside the binary's JSON output. Watch for two things naive attempts miss: a magic-number check at the very start of the header that must match before anything else is trusted, and a bitwise XOR obfuscation applied to some fields (e.g. an encoded value is not the field directly but that value XORed with another header field) — verify this by holding one field constant while varying another and checking if the decoded output changes unexpectedly, which reveals the XOR dependency. Once the byte layout and any modulo/derived fields (like a type value computed from another field mod N) are fully confirmed against many synthetic examples (not just one), implement the parsing purely in Python using `struct.unpack` with explicit endianness — big-endian per the header conventions you observe — and avoid any dependency on the legacy binary at runtime. Test parser.py for speed and 100% accuracy on a large held-out set before wiring up organize.py.