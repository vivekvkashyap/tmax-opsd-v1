Step 1: run ffmpeg with `-vf signalstats -f null -` (or similar) on /app/telemetry.mp4 and capture stderr, since signalstats prints its per-frame stats there; use grep/sed/awk to pull out the frame index and the `YAVG` value from each matching line and reformat into `frame_number,YAVG,value` CSV lines — the exact ffmpeg log line format needs to be inspected first (run it once and look at the raw text) rather than assumed. Step 2: write detector.py to read stdin line by line; validate each line against a strict regex like `^\d+,[A-Za-z]+,\d+(\.\d+)?$` before parsing, sending non-matching lines verbatim (with the exact literal prefix) to stderr and continuing. Maintain a per-metric rolling history of up to 3 *normal* values only: only compute a mean and flag an anomaly once exactly 3 history values exist for that metric, comparing `abs(value - mean) > 15.0`; on anomaly, print the formatted ANOMALY line and do NOT push the anomalous value into history — only append non-anomalous values, and cap history at length 3 (drop oldest, FIFO) after appending. At EOF print `SUMMARY:` then each metric alphabetically with its anomaly count. Step 3: pipe your regex-parsed ffmpeg output into `python3 detector.py`, redirecting stdout to final_report.txt and stderr to pipeline_errors.log. Since this will be fuzz-checked against an oracle, test detector.py standalone against small hand-built CSV streams (including malformed lines) before wiring up ffmpeg.