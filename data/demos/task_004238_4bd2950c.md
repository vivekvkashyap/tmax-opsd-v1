Parse `/home/user/protein.fasta` by skipping the `>` header line and concatenating the remaining lines with all whitespace stripped, so newlines inside the sequence don't corrupt it. Parse `/home/user/mass_table.txt` into a `char -> double` map by reading whitespace-separated pairs. Build the mass array `W[n]` by mapping each residue through the table (fail loudly on unknown chars rather than silently skipping). Compute the integral as a plain accumulating sum, and the max abs derivative by iterating `D[n]=W[n]-W[n-1]` for n>=1 and tracking the max `fabs`.

For the DFT, install `libfftw3-dev`, link with `-lfftw3 -lm`, and use a real-to-complex plan (`fftw_plan_dft_r2c_1d`) on `W[n]`; compute power as `re*re+im*im` for each output bin, then scan only indices `1..N/2` (inclusive, skipping the DC bin at 0) for the argmax — an off-by-one here (including index 0 or going past N/2) is the easiest way to get the wrong dominant frequency. Format the three output lines exactly as specified (`std::fixed << std::setprecision(1)` for the two float values, plain integer for the index), and write only those three lines to `/home/user/analysis_result.txt` with no extra whitespace or text. Compute all values from the actual input files rather than assuming any pattern in the data.
