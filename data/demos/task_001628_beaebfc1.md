Structure the integrity script around three clearly separated phases so each is independently testable. Compute checksums with something like `find /home/user/protected_data -type f -exec md5sum {} \; | sort` so output is deterministic regardless of filesystem ordering; compare this against the baseline file to find lines whose hash differs or whose path is new (missing from baseline). Handle the first-run case explicitly: if the baseline file doesn't exist, just write current checksums to it and exit before any alert/rotation logic runs. For rotation, implement it as an unconditional pre-check that runs before appending new alerts: count lines in `alerts.log` with `wc -l`, and if >= 5, do the shifts in reverse order (`.2`->`.3`, `.1`->`.2`, current->`.1`) using `mv`, overwriting any prior `.3` — get the ordering right or you'll clobber history. Use `date '+%Y-%m-%d %H:%M:%S'` to build alert lines matching the exact required format, and always rewrite baseline.md5 at the end of a run so state carries forward. Install the schedule with `(crontab -l 2>/dev/null; echo "*/5 * * * * /home/user/monitor/integrity_monitor.sh") | crontab -`, guarding against duplicate entries if run twice. Test each phase manually (empty baseline, then modify a file, then force >=5 log lines) before trusting it.
