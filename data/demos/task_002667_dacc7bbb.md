Build this bottom-up: binary parser -> state machine -> stats -> serialization, testing incrementally. Open `/home/user/test_data.bin` in binary mode and read fixed 9-byte chunks in a loop (`fread` into a buffer or a struct with `#pragma pack(1)` to avoid alignment padding breaking the 9-byte layout); the first byte is the command (`uint8_t`), the next 8 bytes are a little-endian IEEE-754 double — reinterpret those 8 bytes directly as `double` on a little-endian host, or manually assemble them if you want to be endian-safe. Drive a simple two-state machine (`IDLE`/`ACTIVE`) as you process records in order: 0x01 sets ACTIVE, 0x03 sets IDLE, 0x02 appends its value to a running list only while ACTIVE, anything else is a no-op — get this state transition logic right since it changes which values are "collected" (some data before the second START are meant to be dropped). Once all records are consumed, compute the population variance (`sum((x-mean)^2)/N`, mean over N, not N-1) of the collected values, defaulting to 0.0 if none were collected, and write `{"variance": <value rounded to 4 decimals>}` with `printf`/`snprintf` using `%.4f`. Write the Makefile with `g++ -std=c++17 -O2` producing `/home/user/test_runner` as the default target, then `make` and run it to confirm the JSON file is produced.