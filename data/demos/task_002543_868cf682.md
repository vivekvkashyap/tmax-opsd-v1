Start with the build failure, not the DSP logic: circular header dependencies between router.h, state_machine.h, and audio_buffer.h usually mean each header directly includes the others. Fix by adding `#ifndef/#define` include guards to every header, replacing unnecessary `#include` of a peer header with a forward declaration (`typedef struct X X;` or `struct X;`) where only a pointer/opaque type is needed, and moving the full include into the .c file that actually needs the complete type. Rerun `make` and eliminate every warning (unused vars, implicit declarations, missing prototypes) — treat warnings as build failures. For the interpreter, find where the state machine already dispatches known filter tokens and add a case that matches the literal prefix `MA(`, parses the integer window `W` up to `)`, then implements a real sliding-window average over the audio samples (not a no-op) as it applies the filter. After compiling, launch `./dsp_server 8080` in the background and write a small WebSocket client (e.g. Python `websockets`) that opens the exact URL with the filter/input/output query params and waits for the server to finish before checking `/app/output.wav`.
