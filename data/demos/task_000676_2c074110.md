Work in this order: first OCR the spec image with `tesseract /app/spec.png stdout` and read off the INIT value, POLY value, and the three opcode-to-operation mappings — do not guess these, they come from the image text. Second, fix `crc.c`: the bit-extraction loop is off-by-one (iterates one index past the end of an 8-element array), so bound it correctly to 8 iterations; also make sure you're not otherwise mismanaging the CRC accumulator across calls (each call takes the *current* CRC in and returns the *new* CRC, no hidden static state). Compile with `gcc -shared -fPIC -o /app/libcrc.so crc.c`. Third, write `/app/run.py`: parse the hex string into opcode/value byte pairs, maintain an 8-bit accumulator A (wrap with `& 0xFF`) updated per the OCR'd opcode table, then call `compute_crc` via `ctypes.CDLL('/app/libcrc.so')` with correct `argtypes`/`restype` (uint16, uint8, uint16 -> uint16) each iteration, threading the running CRC value through. Print only the final CRC as 4 uppercase hex digits with no trailing text. Test against several hex strings of varying length, including edge cases like a single opcode/value pair, to sanity-check before finishing.