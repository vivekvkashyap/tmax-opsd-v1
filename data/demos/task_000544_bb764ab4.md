Work through this as three linked pieces. First, fix `/home/user/app/services/config.json`: set `webhook_url` to `http://127.0.0.1:8080/provision` and change the API bind host from `0.0.0.0` to `127.0.0.1`, then check `account_api.py`/`smtp_gateway.py` actually read these config fields at startup (not hardcoded elsewhere). Second, write `/home/user/app/filter.py` to parse the email file: extract the `Subject: Request Account: <username>` header with a strict regex (e.g. `^[a-z0-9]{3,12}$` for the username, rejecting anything with uppercase, punctuation, `/`, `..`, or shell metacharacters — validate with an allowlist regex rather than a denylist, since denylists are easy to bypass), and case-insensitively search the body for the exact acceptance phrase; exit 0 only if every rule passes, else exit 1. Third, write `/home/user/app/create_skel.sh`: first compute `du -sb /home/user/accounts/` and bail with `QUOTA_EXCEEDED` (exit 2) over the 5MB threshold, then idempotently `mkdir -p` the storage dir, create the symlink only if absent (`[ -L ... ] || ln -s`), and write `user.conf` only if it doesn't already exist. Start both Python services in the background, then send a test email via `smtplib` locally to confirm the full chain (filter -> webhook -> skel script) before trusting the corpora tests.