In the C program, read stdin line by line (e.g. `fgets`/`getline`), split each line on commas into exactly `id,email,raw_text` (no quoting/escaping needed since fields are comma-free by spec), and check `strchr(email, '@')` to decide whether to drop the row. Compute the XOR hash by folding `raw_text` byte-by-byte with `^=`, being careful to strip the trailing newline (and any `\r` if present) before folding so it doesn't pollute the hash. Print `id,xor_hash\n` per surviving row to stdout. In `run.sh`, compile with `gcc -O3 -o /home/user/process /home/user/process.c`, then pipe `/home/user/data/users.csv` through the compiled binary, wrapping that invocation with `/usr/bin/time -v ... 2> /home/user/bench.txt` (or `-o`) so timing output actually lands in the file — note the input file has a header row you likely need to skip (`tail -n +2`) since it's not `id,email,raw_text` data. Sort the hash output numerically by id (`sort -t, -k1,1n`) before `join`-ing against the already-sorted `/home/user/data/legacy_hashes.csv` on field 1 with the correct `-t,` delimiter, producing `id,xor_hash,legacy_hash` into `/home/user/final.csv`. Make `run.sh` executable and run it standalone to confirm it reproduces the whole pipeline from a clean state.