Tackle the three parts in order, verifying each before moving to the next. (1) Rust fix: run `cargo build` in `rust_graph` to see the exact borrow-checker error in `resolver.rs`'s recursive DFS — the typical cause is holding a `&Graph` reference into a recursive call while also trying to mutate shared state (e.g. a visited-set field on the same struct); fix by passing node IDs instead of long-lived references, or by separating the read-only adjacency lookup from the mutable visited/order tracking (e.g. pass `&mut HashSet` and `&mut Vec` alongside an immutable graph reference, or clone just the needed neighbor list before recursing) — don't change the traversal algorithm itself. (2) Cross-compile: `rustup target add x86_64-unknown-linux-musl`, then `cargo build --release --target x86_64-unknown-linux-musl`, and copy/symlink the resulting binary to `bin/graph_resolver`. Test it manually against a couple of hand-built DAG strings (including a cyclic one) to confirm the I/O format matches spec before writing the Go harness. (3) Go harness: use `testing/quick.Check` or manual randomized generation to build ≥5000 random DAG strings, run both binaries via `exec.Command` for each, and diff their stdout; make it a `BenchmarkGraphSolver` function, and use `b.RunParallel` (or a worker pool with goroutines) to overlap subprocess calls so throughput clears 500 ops/sec — sequential exec.Command calls will almost certainly be too slow. Run `go test -bench . -benchtime=5s` yourself to confirm before finishing.
