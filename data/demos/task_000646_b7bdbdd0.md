Structure the C program around a TCP accept loop on `127.0.0.1:8888` (socket/bind/listen/accept, then read line-by-line per connection with `fgets`-like buffering that handles partial reads until `\n`). For each JSON line, avoid pulling in a JSON library — since the schema is fixed (`{"ts": "...", "msg": "..."}`), locate the `"ts":"` and `"msg":"` substrings and extract the quoted values manually (watch for the closing quote, not the first `\` inside an escape). Parse the ISO8601 timestamp with `strptime` using format `%Y-%m-%dT%H:%M:%SZ` into a `struct tm`, zero-initialize it first, then convert with `timegm` (not `mktime`, which applies local timezone) to get a correct UNIX epoch. Decode `\uXXXX` escapes in `msg` by scanning for the literal `\u` prefix, reading the following 4 hex digits, converting to an integer code point, and since inputs are guaranteed printable ASCII, just emit that single byte — this must happen before the string reaches the analyzer, since the analyzer treats any raw `\` as a crash signal. Fork/spawn `/app/legacy_analyzer` per line (pipe to its stdin, read its stdout), and write back `<epoch>,<score>\n` to the client socket. Launch the compiled server in the background (e.g. `./stream_processor &`) and leave it running.