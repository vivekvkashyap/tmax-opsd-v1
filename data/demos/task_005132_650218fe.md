List `.dat` files in `/home/user/raw_data` first, then for each file: read its raw bytes, compress with standard zlib (e.g. Python's `zlib.compress(data)` at default level — do not use gzip headers or a different level, since the verifier decompresses with plain `zlib.decompress`), and write the compressed bytes to `/home/user/archive/<basename>.zdat`. Only after the compressed file is written and verified, delete the original `.dat` file and create a symlink at the exact same path pointing to the absolute path of the new `.zdat` file (use `os.symlink(target, link_path)` with an absolute target, not relative). Collect each `(symlink_path, target_path)` pair, sort them alphabetically by symlink path, and write them as comma-separated lines to `/home/user/space_report.csv` with no header and no trailing blank-line surprises. Process files in a script rather than manually so the compress-then-symlink ordering is atomic and repeatable, and double check `os.readlink()` returns exactly the archive path (not a relative or trailing-slash variant) before finishing.