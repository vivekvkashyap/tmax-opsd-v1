Build this as three clearly separated stages in your script so each is independently testable. Stage 1 (validation): scan each line with a regex that flags any `\u` NOT followed by exactly 4 hex digits — in grep/awk terms, a line is invalid if it matches `\\u` where the next 4 chars aren't `[0-9a-fA-F]{4}`; a reliable approach is a negative-lookahead-style check via `grep -P '\\\\u(?![0-9a-fA-F]{4})'` to catch invalid lines directly, then use `grep -v -f` or complement logic to get the valid set — preserve original line order by processing sequentially rather than sorting. Stage 2 (rolling average): once you trust `clean_logs.jsonl` is valid JSON, use `jq` to filter `path == "/api/data"` and extract `ts,time` pairs, then feed that into `awk` to maintain a sliding window of up to 3 prior values (for the first two rows, average over however many are available so far), formatting each average to 2 decimals with `printf "%.2f"`. Stage 3 (report): compute valid/invalid counts and the max time for `/api/data` paths, then substitute them into the exact markdown template with `sed` or a heredoc. Test incrementally against small synthetic examples before running on the real file.
