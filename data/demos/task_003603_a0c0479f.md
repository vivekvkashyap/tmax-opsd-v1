Handle the three sub-problems separately, then combine. Frame count: don't estimate from duration*fps by hand — extract it programmatically, e.g. `cv2.VideoCapture(path).get(cv2.CAP_PROP_FRAME_COUNT)` or `ffprobe -v error -count_frames -select_streams v:0 -show_entries stream=nb_read_frames -of csv=p=0 /app/experiment.mp4`, and use whichever value that call actually returns (cv2's CAP_PROP_FRAME_COUNT can be an estimate for some codecs, so prefer the frame-counting ffprobe mode if they disagree). Pandas bug: the root cause is that `astype(str)` on a float64 column (made float by NaN presence) stringifies as "500.0"; fix by converting valid values through `pandas.array(df['event_code'], dtype='Int64')` (nullable integer) or by applying a row-wise function that does `f"EVT_{int(x)}"` when `pd.notna(x)` else `"EVT_UNK"`, avoiding the naive astype(str) path entirely. Serving: build a small FastAPI/Flask app with a `/api/records` GET handler that returns `df.to_dict(orient='records')` (or Flask's jsonify) as JSON, bind it to 127.0.0.1:8000, and leave it running so the automated check can hit it.