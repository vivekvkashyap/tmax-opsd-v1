Work the chain of artifacts in strict dependency order, verifying each step's output before moving to the next. 1) OCR the passphrase: `tesseract /app/passphrase.png stdout` (try preprocessing like grayscale/threshold with ImageMagick if raw OCR is noisy) to recover the exact passphrase text. 2) Decrypt the key: `openssl rsa -in /app/server.key.enc -passin pass:<recovered-passphrase> -out server.key`. 3) Decrypt the pcap with the plaintext RSA key using `tshark -r /app/traffic.pcap -o "tls.keys_list:127.0.0.1,443,http,server.key" -Y http` (or equivalent Wireshark/tshark TLS-decrypt workflow) since the capture uses RSA key exchange, which only needs the private key, no session keys. 4) Locate the HTTP 200 response and extract its HTML body byte-for-byte into /home/user/index.html — use tshark's "Follow HTTP Stream"/export-object feature or manually pull the payload after decryption. 5) Parse index.html for all resource URLs (script/img/link href/src), classify each domain as safe if it contains "trusted" or "internal", otherwise malicious. 6) Write generate_csp.py to print a CSP string with `default-src 'self'` plus script-src/img-src/style-src directives whitelisting only the safe origins per resource type (never including the malicious domain or a wildcard). Run `/app/evaluate_csp.py "$(python3 generate_csp.py)"` and iterate until the printed score is >= 90.