Set up a Rust project (`cargo new`) with `serde`, `serde_json`, and `unicode-normalization` as dependencies. Read `/home/user/raw_messages.jsonl` line by line, deserializing each into a struct with user_id/timestamp/text. For normalization, apply `.nfc().collect::<String>()` from `unicode-normalization`, then `.to_lowercase()`, then collapse whitespace by splitting on any whitespace run and rejoining with single spaces (`text.split_whitespace().collect::<Vec<_>>().join(" ")` handles both collapsing and trimming in one step). Group records into a `BTreeMap<String, Vec<Record>>` keyed by user_id (BTreeMap gives you alphabetical ordering for free), and within each group use a stable sort by timestamp (`sort_by_key` is stable in Rust) to preserve original order on ties. For the rolling average, iterate each user's chronological messages and for message i compute the mean of normalized-text `.chars().count()` over the window `[max(0,i-2)..=i]`, rounding to 2 decimals (`(x * 100.0).round() / 100.0`). Write output records in the exact grouped/sorted order to `/home/user/processed_messages.jsonl`, one JSON object per line.
