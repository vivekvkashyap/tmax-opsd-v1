Create the config and mock service files exactly as specified, then focus your engineering effort on `health_monitor.py`'s logic: load and parse the JSON config, and for each service (preserving JSON array order) do a real TCP connectivity check — e.g. `socket.create_connection(('127.0.0.1', port), timeout=1)` wrapped in try/except, treating a successful connect as UP and a `ConnectionRefusedError`/timeout as down. On a down port, spawn the service's `cmd` as a detached background process (e.g. `subprocess.Popen(cmd.split(), stdout=DEVNULL, stderr=DEVNULL)`, not `subprocess.run`, so it doesn't block), sleep 1 second to let it bind, then log `RESTARTED`; otherwise log `UP`. Append (not overwrite) `service_name,state` lines to `/home/user/status.csv` for each run. Drive the required sequence yourself: run the script once (both down → both RESTARTED), sleep 2 seconds, run it again (both now up from the first run's spawned processes → both UP), and leave those background processes running rather than killing them at the end.
