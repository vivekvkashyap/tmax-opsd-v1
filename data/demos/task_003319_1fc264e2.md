Start by transcribing `/app/pi_instructions.wav` with a real tool (`whisper` CLI, or `ffmpeg` to extract/convert audio then a speech-to-text tool) rather than guessing values — read the transcript output carefully to pull out the exact diffusion coefficient D, grid point count N, and time step dt spoken by the PI, and use those variables in your code (don't hand-derive them from the task text). Then implement the 1D diffusion finite-difference solver in C: standard explicit update `u_new[i] = u[i] + D*dt/h^2 * (u[i-1] - 2*u[i] + u[i+1])` with `h = L/(N-1)`, but structure the code as true domain decomposition — split the N points into two halves, run each half's update independently each step, and explicitly exchange/synchronize the boundary value at the midpoint between the two halves every iteration (this ordering affects numerical results, so get the exchange semantics exactly right, e.g. exchange before or after the local update, consistently). Parse argv exactly as specified (T, then a comma-separated initial-value string of length N), and print the final state as comma-separated floats to 4 decimal places with no trailing artifacts. Since this will be fuzz-tested against a hidden reference across many T and initial-condition combinations, keep boundary conditions, iteration order, and floating-point precision consistent and deterministic.
