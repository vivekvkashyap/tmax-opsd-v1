Correlate the three log files by RequestID and duration: grep compute.log for "Finished computation in" lines, extract the millisecond values, and find the one request whose duration exceeds 5000ms — cross-reference its RequestID against ingest.log to inspect its Mean/Median/Variance/StdDev fields. Identify which named feature's value is exactly 0.0 for that request (compare numeric strings precisely, not just visually) and write /home/user/anomaly_report.json with keys "slow_request_id" and "anomaly_feature" holding that request id and the zero-valued feature's name. Then open math_compute.py and trace normalize_and_compute: it calls calculate_std, and when the computed std equals 0.0 it currently falls into slow_fallback_compute (which sleeps for seconds, simulating the slow path). Fix this by short-circuiting: when std == 0.0, return a list of 0.0 of the same length as data directly, without calling slow_fallback_compute at all, so the function returns near-instantly. Do not alter normalize_and_compute's signature. Finally run `python3 /home/user/verify.py` and confirm it exits successfully and writes /home/user/success.log — if it times out or asserts incorrect output, re-check that the zero-check path really bypasses the slow function entirely.
