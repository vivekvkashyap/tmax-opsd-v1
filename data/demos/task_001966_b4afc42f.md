Build the C side first and test it standalone before wiring up Python. In `counter.c`, implement a simple open-addressing or chained hash table sized by capacity that depends on `#ifdef FAST_MODE` (>=8192) vs the else branch (1024); expose `init_counter`, `add_values(const int*, int)`, and `get_count(int)` with `extern "C"`-safe plain C signatures (no name mangling issues since it's pure C). Compile with `gcc -DFAST_MODE -fPIC -shared -o libcounter.so counter.c`. In `migrate.py`, use `ctypes.CDLL` to load the library, set `.argtypes`/`.restype` on each function explicitly (especially `add_values` needing a `POINTER(c_int)` and length), read `/home/user/data.bin` fully with `open(...,'rb').read()` and unpack it as little-endian signed 32-bit ints via `struct.unpack` or `array` — don't assume a record count, derive it from `len(data)//4`. After calling `add_values`, determine the top 5 by counting distinct values in Python (e.g., via a Python `set`/`Counter` over the same input, or by querying `get_count` per unique value) and sort by (count desc, value desc) to break ties correctly — that "prioritize larger integer" tie-break is easy to get backwards. Write results one per line and the exact benchmark JSON.