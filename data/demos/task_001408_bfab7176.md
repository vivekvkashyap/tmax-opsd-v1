First get the audio transcript: use a local speech-to-text tool (e.g. `whisper` if available, or another installed ASR CLI) on `/app/audio/directive.wav` to recover the exact validation rules in plain English — do not guess the rules from the example JSON alone, since the true thresholds/field names only exist in the audio. Once transcribed, scaffold a Rust binary crate at `/home/user/query_sanitizer/` (`cargo new`) with `serde`/`serde_json` as dependencies, parse the input file's JSON into a `serde_json::Value` (so malformed/missing fields don't panic — malformed JSON should itself trigger REJECT via the parse error path), then implement each rule as an explicit check: read the pagination limit field and compare it against the threshold from the transcript, walk any joins array and confirm each join object has the required condition key, and scan the source-document string for the banned character(s) named in the audio. Treat any rule violation, parse failure, or missing expected field as REJECT; only print ACCEPT if every check passes. Build with `cargo build --release` so the binary lands at `target/release/query_sanitizer`, then self-test with a few hand-written clean/evil JSON files mirroring the rules before relying on it.
