Do not try to disassemble or reverse-engineer the stripped binary's internals directly — instead treat it as a black box and reason from input/output behavior only, since the final detector must never call the binary. Approach: (1) Run `/app/bin/feature_extractor` over every file in `/app/data/clean/` and `/app/data/evil/`, saving each JSON feature vector alongside the source filename. (2) Diff the feature vectors between clean and evil corpora to spot which dimension(s) behave anomalously (e.g. one index takes on an extreme/out-of-range value only for evil files) — this points you to the leaked feature index. (3) Once you've spotted the anomalous feature, go back to the raw text logs for evil vs clean files and look for structural differences (character frequency counts, specific character occurrences) that correlate with when that feature goes anomalous — vary counts of suspicious characters yourself and rerun the extractor to empirically nail down the exact trigger condition (thresholds and which characters). (4) Encode that exact condition (or train a simple classifier reproducing it) directly in `/home/user/detector.py`, reading the raw text and printing `CLEAN`/`EVIL` with correct exit codes. (5) Validate against both corpora until 100% accuracy is reached before finishing.