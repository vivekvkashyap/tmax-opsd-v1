First recover the policy parameters yourself with OCR — run `tesseract /app/policy_rules.png -` (or equivalent) and read the extracted text carefully to get the three numeric thresholds (imputation default memory, max CPU, max aggregated delta); don't guess these values, and double check the OCR output for digit-misreads (0/O, 1/l) before hardcoding them into your script. Then implement the validator precisely per the stated rules: iterate `.json` files in the given directory, load each state sequence, first count missing `cpu`/`memory` fields across the whole sequence (reject if >2) BEFORE imputing, then impute missing memory with the recovered default, check every state's `cpu` against the recovered max-CPU threshold, and sum `abs(memory[i]-memory[i-1])` over consecutive states after imputation, rejecting if it exceeds the recovered max-delta. Since verification is adversarial (clean files must all pass, evil files must all fail), test your script against constructed edge cases for each individual rule in isolation to make sure none of the three checks is off-by-one or applied in the wrong order (e.g. imputing before vs. after the missing-field count changes results).