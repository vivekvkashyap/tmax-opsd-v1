First recover the exact Cypher query from `/app/query.png` using OCR (`tesseract /app/query.png stdout`), and re-read the extracted text carefully — OCR on code/query text can mis-read symbols like `->`, `%`, or comparison operators, so double check the pattern (relationship type, node-property filters, and RETURN clause) makes logical sense before implementing it in C. Once you have the query's actual predicate logic (a 2-hop path pattern with modular-arithmetic filters on the endpoint IDs), implement it in C as: build an adjacency list from stdin edges, then for every node A satisfying the first filter, walk its out-edges to B, then B's out-edges to C, and only keep (A,C) pairs where C satisfies the second filter — use a hash set or sorted-dedup step to eliminate duplicate output pairs since multiple 2-hop paths can produce the same (A,C). Since the graph can be cyclic, this is fine as long as you're only ever doing an exact 2-hop expansion (not unbounded traversal) so cycles can't cause non-termination. Sort output numerically by (A,C) before printing. Since a fuzz harness will diff your output against a reference oracle bit-for-bit across many random graphs, write your own small test harness with hand-computed small graphs first to validate correctness before relying on the fuzzer's verdict.