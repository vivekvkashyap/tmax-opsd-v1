First inspect a few lines of `/home/user/data/transactions.jsonl` yourself to discover the actual field names used for source and destinations (the task deliberately doesn't name them in prose — you must read the raw JSON to find the real keys, e.g. via `head -n1` and `jq` or just `cat`). Model this in Rust using `serde` derive structs matching those discovered field names, parsing the file line-by-line (avoid loading a huge file entirely if it were large — use a `BufReader` and iterate lines). Build in-degree/out-degree counters (e.g. two `HashMap<String, i64>`), incrementing out-degree once per transaction for the source and in-degree once for each entity in the destination array — note a single transaction can create multiple edges from one source. Compute total_degree = in+out for every node seen (as either source or destination), then pick the max, breaking ties by lexicographically smallest name (sort candidates or track carefully during a fold). Write the four required keys to `/home/user/etl_output.json` via `serde_json`. Build with `cargo build --release` inside `/home/user/graph_etl` and actually run the binary so the output file exists.
