Break this into data science work plus a service fix, and verify each independently before combining. For the data: load `/home/user/data/server_metrics.csv`, impute missing `cpu_usage` with that column's median (`fillna` after computing `median()`, not mean), then drop rows where `disk_io > 1000`. Compute the Pearson correlation matrix (`df.corr()`) and pick the two non-`latency` columns with the largest `abs(corr with latency)` — don't hardcode which columns those are, derive them from the actual data. Train `sklearn.linear_model.BayesianRidge()` on just those two features with default hyperparameters, then dump `model.alpha_` and `model.lambda_` into `/home/user/experiment.json` under the exact keys `"alpha"` and `"lambda"`. Separately, fix the vendored `bottle` package at `/app/bottle-0.12.25/bottle.py`: search for an injected line inside the `route` method/decorator that raises an exception or calls `sys.exit` unconditionally — delete just that injected line, don't rewrite the surrounding routing logic, then `pip install -e /app/bottle-0.12.25`. Build a small bottle server exposing `GET /health` returning `{"status":"ok"}` and `POST /predict` that parses the JSON body, extracts the two feature values by their exact column-name keys, feeds them through your fitted model's `.predict()`, and returns `{"predicted_latency": <float>}`. Run it on `127.0.0.1:8080` and leave it running; test both endpoints yourself with `curl` before finishing.
