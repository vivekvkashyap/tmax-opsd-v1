Scaffold a real Cargo binary crate (`cargo new artifact_tracker`) inside `/home/user/mlops/`, add `serde`, `serde_json`, and `csv` (plus `serde_derive`) to `Cargo.toml`, and define a struct matching the JSON schema (`id`, `weights: [f64;4]`, `error_rate: f64`) with `#[derive(Deserialize)]`. In `main.rs`: glob-read all 50 JSON files from `/home/user/mlops/data/`, deserialize each, compute `projected_weight` as the dot product of `weights` with the fixed vector `[0.5, -0.5, 0.5, -0.5]` (element-wise multiply and sum, not a matrix library). Collect `(id, projected_weight, error_rate)` tuples, sort by `error_rate` ascending before writing the CSV with the `csv` crate (header `id,projected_weight,error_rate`). Separately, run OLS regression: compute means of X (projected_weight) and Y (error_rate) across all 50 points, then slope = Σ(x-x̄)(y-ȳ) / Σ(x-x̄)², intercept = ȳ - slope*x̄ — implement this directly in Rust rather than importing a stats crate. Format slope and intercept to exactly 4 decimal places with Rust's `{:.4}` formatter and write them to `report.txt` in the exact `Slope: <v>\nIntercept: <v>` layout. Build and run the binary to confirm both output files are generated before finishing.
