First inspect `/app/bash-conf-tracker` to find `normalize.sh` and the `Makefile`. The parsing bug is the classic `while read -r line; do ... done < "$file"` pattern that silently drops a final line lacking a trailing newline — fix it to `while read -r line || [ -n "$line" ]; do ...`. In the `Makefile`, correct the install target's destination from `/user/bin/` to `/usr/local/bin/` (verify that path is writable/on PATH), then run `make install`. For the normalization logic itself: split each line on the first `=`, lowercase and trim the key, trim whitespace and surrounding quotes from the value, and use an associative array (or ordered map) keyed by lowercased key so a later duplicate key overwrites an earlier one — the payload is only valid if a `hostname` key ends up present after this dedup, otherwise the handler must return HTTP 400. Get the `socat`-based server listening on exactly `127.0.0.1:9090`, wire `POST /upload` to invoke your normalize logic and write `/home/user/configs/<hostname>.json`, and `GET /config?host=X` to return that file (200) or 404 if absent. Test with a POST body deliberately missing a trailing newline to confirm the fix actually works end-to-end, then start the server backgrounded so it stays up for external requests.
