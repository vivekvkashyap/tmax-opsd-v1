Use pandas/numpy for this, done as a small script. Load the CSV, then for each column replace the literal string `MISSING` with NaN (e.g. `pd.to_numeric(col, errors='coerce')` or `replace('MISSING', np.nan)`), and be sure to cast the column to float explicitly afterward — pandas' NaN handling silently upcasts ints to float, and if you skip an explicit `astype(float)` step you can end up mixing dtypes or truncating values later. Compute each column's median from the non-missing values only, then fill the NaNs with that median. Next, per column, compute Q1 and Q3 with linear interpolation (`Series.quantile(0.25/0.75, interpolation='linear')`, which is pandas/numpy's default), derive IQR = Q3 - Q1, and clip values to [Q1 - 1.5*IQR, Q3 + 1.5*IQR] using `.clip(lower, upper)` — do this after imputation, not before. Then compute the sample covariance matrix with ddof=1 (`DataFrame.cov()` already uses N-1). Get eigenvalues/eigenvectors of that covariance matrix with `numpy.linalg.eigh` (since it's symmetric), pick the eigenvector for the largest eigenvalue, and flip its sign if the first component is negative so the first entry is positive. Round each of the 3 components to exactly 4 decimal places and write them comma-separated (no extra spaces) to `/home/user/eigenvector.txt`. Verify by re-reading the file and diffing against your own recomputation before finishing.
