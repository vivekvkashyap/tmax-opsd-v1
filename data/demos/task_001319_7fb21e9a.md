Work top-down through the stack and test each layer independently before wiring them together. In `backend.c`, replace the XOR routine with OpenSSL EVP AES-128-CBC encrypt/decrypt calls (link with `-lcrypto`), keep the key/IV handling consistent with what the server expects for decrypting incoming tokens, and have the handler respond with the literal header `Set-Cookie: session=<token>; Secure; HttpOnly` after successful decryption; recompile and test the raw TCP service directly (e.g. with a small client or `openssl enc`) on `127.0.0.1:8081` before touching nginx. For nginx.conf, configure an `ssl` server block on `8443` referencing the cert/key in `/home/user/app/certs/`, `proxy_pass` to `127.0.0.1:8081`, and add an `if ($http_x_auditor_auth = "")` (or map-based) check that returns 403/400 when the `X-Auditor-Auth` header is absent — test with `curl -k` both with and without the header. Fix SSH permissions with `chmod 600` on the private key (and typically `644` on the .pub) — verify via `stat -c %a`. Finally write `start.sh` to launch nginx (`nginx -c <path>`) and the backend binary in the background, then run it and confirm both ports are listening.
