Handle the three subsystems independently before wiring them together. Compile the C hashing file as a position-independent shared object: `gcc -shared -fPIC -o /home/user/app/libuserhash.so /home/user/app/userhash.c`, and confirm the exported symbol name with `nm -D` if unsure. For the schema migration, work in sqlite3 CLI or a script: create the new-shape table, `INSERT INTO new_table SELECT id, substr(full_name,1,instr(full_name,' ')-1), substr(full_name,instr(full_name,' ')+1), age FROM employees` (splitting on the first space), then drop the old table and rename the new one to `employees` — verify the split logic against sample names before committing. For the API, load the .so via your language's FFI (e.g. Python `ctypes.CDLL`, declaring `argtypes`/`restype` as `int`/`int`), query sqlite for the row by id, call `compute_hash(id)` through the FFI binding, return 404 when the row is missing, and format the JSON response exactly matching the field order/keys shown in the spec. Start the server on 127.0.0.1:8080, then use `curl` to hit the endpoint and redirect the raw response body into the results file.
