Start with OCR: run `tesseract /app/legacy_export.png stdout` and inspect the raw text output carefully — tesseract can introduce minor character errors (e.g. confusing `0`/`O` or quote characters), so sanity-check the extracted `Node:`/`Edge:` lines against expected patterns before parsing, and write a small parser (regex per line) that extracts entity type, id/name fields, and edge from/to references. Use `pip install kuzu`, create the database directory, define Node tables (e.g. `Employee(id, name)`, `Project(id, name)`) and a `WORKS_ON` REL table between them via Cypher DDL executed through the kuzu Python connection, then insert rows using parameterized `conn.execute(query, parameters={...})` calls (never string-interpolate values into the Cypher text) for every extracted node/edge. After loading, create an index on each node table's primary id property. Build the API with Flask/FastAPI: a `POST /query` handler that reads `employee_id` from the JSON body, runs a Cypher `MATCH (e:Employee {id: $eid})-[:WORKS_ON]->(p:Project) RETURN p.name` parameterized query, collects the `name` values into a list, and returns `{"projects": [...]}`. Run the service bound to `127.0.0.1:8080` in the background (e.g. `nohup ... &` or a systemd-less daemonized launch), redirecting startup output into `/home/user/api.log`, then confirm with `curl` that both endpoints respond correctly before finishing.