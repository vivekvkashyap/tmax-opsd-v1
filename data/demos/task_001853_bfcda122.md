Scaffold with `cargo new svd_profile` inside /home/user, add `serde`, `serde_json`, and `nalgebra` (0.32-compatible) to Cargo.toml, then work in stages you can check independently. First, deserialize matrices.json into `Vec<Vec<Vec<f64>>>` (array of 50 10x10 arrays) and confirm you read exactly 50 matrices of the right shape. Next, for each matrix, build an `nalgebra::DMatrix<f64>` (or fixed SMatrix<f64,10,10>) and call its SVD routine (e.g. `.svd(false, false)` or `SVD::new`) to get the singular values vector, then take its maximum — singular values from nalgebra's SVD aren't guaranteed pre-sorted in every version, so explicitly take the max rather than assuming index 0 is largest. Collect all 50 largest singular values, compute the population mean (sum/50) and population standard deviation using N (not N-1) in the denominator — this is the detail naive implementations get wrong by defaulting to sample variance. Format both to exactly 4 decimal places and write as `mean,std` to svd_results.txt. Sanity check by printing intermediate singular values for one matrix and confirming they look reasonable relative to the matrix's diagonal magnitude before trusting the aggregate.