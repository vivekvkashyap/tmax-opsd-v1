This is a hierarchical rollup problem, best solved with SQLite's recursive CTEs rather than manual Python loops, since department nesting depth is arbitrary. Load all four CSVs into an in-memory (or temp file) SQLite DB using `sqlite3` (or pandas `to_sql`). Write a recursive CTE that walks each department's `parent_dept_id` chain upward until it hits a row with a null/empty parent, giving you a `dept_id -> root_dept_id` mapping for every department (including roots mapping to themselves). Join `timesheets` to `employees` to compute `hours * hourly_rate` per line, join that to `projects` to get each timesheet's `dept_id`, then join through your root-mapping CTE to attribute cost to the root department. GROUP BY root department name, SUM the costs, and ORDER BY total_cost DESC. Watch for two common mistakes: treating empty-string parent_dept_id inconsistently with NULL (normalize both), and forgetting a project's dept_id might itself be a non-root department requiring the recursive walk. Write the final two-column CSV (`root_dept_name,total_cost`) to `/home/user/department_costs.csv` with no quoting, comma-delimited, with header, matching sort order exactly.