Split this into the two independent pieces. For the proxy fix, run `/home/user/app/start_services.sh`, inspect `/home/user/app/nginx/nginx.conf`, and add/correct a `location /api/ { proxy_pass http://localhost:5000/; }` block inside the `server { listen 8080; ... }` block (note the trailing slash on proxy_pass strips the `/api/` prefix before forwarding — get this right or Flask will 404); reload/restart nginx and confirm with `curl -X POST http://localhost:8080/api/upload` followed by `redis-cli get uploads_count` to prove the end-to-end path and Redis increment work. For the sanitizer, write `/home/user/sanitizer.sh` using `awk -F, '{print $4}'` (skip header with `NR>1`) piped through a strict integer regex check, e.g. reject if a value is empty or fails `^-?[0-9]+$` (this correctly rejects floats like `12.0`, `NaN`, `null`, and blanks while accepting `-3`); exit 0 only if every row matches. Test it directly against every file in `/home/user/data/clean/` and `/home/user/data/evil/` in a loop, capturing exit codes, and write each result as `[filename]: [ACCEPT/REJECT]` to `/home/user/verification.log`.