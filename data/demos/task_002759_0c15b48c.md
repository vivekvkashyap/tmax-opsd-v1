In C, use `opendir("/proc")`/`readdir` and filter entries whose name is entirely digits (the PIDs). For each candidate PID, read `/proc/<pid>/comm` (or the first token of `/proc/<pid>/stat`) and compare it exactly to `vuln_service` to confirm the process name. Then read `/proc/<pid>/cmdline`, which is NUL-separated argv — split on `\0` bytes (not spaces) to get each argument correctly, find the arg equal to `--secret`, and take the following arg as the value; handle the case where `--secret value` might also appear as a single `--secret=value` token defensively based on how cmdline actually looks. Collect all extracted secrets into an array, sort them alphabetically (e.g. `qsort` with `strcmp`), and write one per line to `/home/user/extracted_secrets.log`. Separately, generate the key pair with `ssh-keygen -t ed25519 -f /home/user/.ssh/ir_key -N ""`, then append to `/home/user/.ssh/authorized_keys` a line built by prepending the literal string `restrict ` to the full contents of `ir_key.pub` (type, base64 key, and comment) exactly as `ssh-keygen` produced it.
