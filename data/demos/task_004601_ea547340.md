Build and verify this in layers: apps first, then proxy, then diagnosis, then measurement. Write `/home/user/manage_services.sh` to launch each `appN.py` with `nohup python3 appN.py & echo $! > /home/user/run/appN.pid` (creating the run dir first), have `stop` read each pid file, `kill` the pid, and remove the file, and have `status` check both file existence and that the pid is a live process (e.g. `kill -0 $pid`) for all three, exiting 1 if any check fails. For HAProxy, write a config with a `frontend` binding `127.0.0.1:8080`, a `backend` in `roundrobin` mode listing the three `127.0.0.1:900{1,2,3}` servers each with `check` and `option httpchk GET /ping` so unhealthy backends get skipped automatically, then launch with the exact daemon-mode command given. Before assuming everything works, curl each app directly and via the proxy to find which one 503s — trace it to a missing dependency file the app checks for on startup/request, and create that missing file (empty is fine) so the app recovers; recheck its health status rather than assuming. Finally write `/home/user/verify_restore.sh` to loop 12 `curl -s -o /dev/null -w '%{http_code}\n'` calls against `http://127.0.0.1:8080/ping`, appending each code to `/home/user/proxy_results.log`, and run it only after confirming HAProxy's health check already reports all backends up.