First recover the actual spec: transcribe `/app/requirements.wav` with an offline tool (`whisper` if available, or another local ASR) since the pipeline logic (which columns, what order of operations, output shape) is dictated entirely by that audio — do not guess the steps, listen/transcribe carefully and re-check ambiguous parts of the transcript before coding. Once you have the steps, implement `/home/user/pipeline.py` to read a JSON array from stdin into a `pandas.DataFrame`, and replicate the described operations in the exact stated order (e.g., masking a column's negative values to NaN via a boolean-indexed assignment — note this silently upcasts an int column to float, forward-filling those NaNs, dropping any remaining NaN rows, computing a correlation, and fitting a linear regression with an intercept using something like `sklearn.linear_model.LinearRegression`), matching rounding precision and edge-case handling (insufficient rows, zero variance) precisely as described. Since this is checked by fuzz equivalence against many random inputs, test your script locally against a variety of hand-crafted edge cases (all-negative column, too-few rows after dropping, zero-variance columns) to make sure your JSON output's keys, structure, and numeric rounding match the specification exactly, and ensure no network calls are made at runtime.