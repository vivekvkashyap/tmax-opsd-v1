Do the audio transcription first since it determines the entire algorithm — install a speech-to-text tool (e.g. `pip install openai-whisper` or SpeechRecognition+pocketsphinx if network/whisper isn't available) and run it against `/app/audio/lab_notes.wav`; read the transcript carefully for the specific graph-theoretic property being described rather than assuming a rule. Once you know the rule, write `/home/user/kg_filter.cpp`: parse the edge list (`node1 node2` per line) into an adjacency list, then implement the appropriate graph-property check (e.g. a DFS-based coloring scheme with white/gray/black states to detect back-edges, or Kahn's algorithm topological sort where leftover in-degree indicates the property) and `exit(1)` if the graph has the flagged property, `exit(0)` if not — validate against sample files from both `/app/corpora/clean/` and `/app/corpora/evil/` yourself before trusting it, since 100% accuracy is required on a holdout set. For the degree-summary step, parse `/app/corpora/clean/graph_001.txt` and tally in-degree+out-degree per node, tracking the max as you go (ties broken however the spec implies, but verify there isn't one in this fixture). Finally assemble `/home/user/final_report.json` with your actual transcript text, a one-sentence restatement of the rule, and the node ID you computed — do not fabricate any of these three values, derive them from your own transcription and computation.
