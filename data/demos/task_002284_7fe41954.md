First recover the actual routing table yourself — run `tesseract /app/policy.png stdout` (installing tesseract-ocr if needed) and read off each `<prefix> -> <port>` line plus the `DEFAULT -> <port>` line from the OCR output; don't guess values. Then implement `/home/user/router.sh` so the matching is a true longest-prefix match: check the input URI against every extracted prefix with `[[ "$URI" == prefix* ]]`, collect all prefixes that match, and pick the longest one (by string length) rather than the first one you happen to test in an if/elif chain — an if/elif in the wrong order will silently misroute inputs where a shorter prefix is checked before a longer one that also matches. If nothing matches, print the DEFAULT port. Output must be exactly the port number and nothing else (no trailing text/newline noise beyond one line). Make the script executable with `chmod +x`, and sanity-check it manually against a handful of paths (e.g. an admin path, an api/v1 vs api/v2 path, an unmatched path) before considering it done.
