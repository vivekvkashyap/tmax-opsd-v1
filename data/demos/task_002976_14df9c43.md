Transcribe `/app/experiment_log.wav` first (e.g. `whisper /app/experiment_log.wav --model base` or a Python speech-recognition library) and read off the spoken Z-score threshold as a number — do not guess a common value like 3.0, the actual figure is stated in the audio and must be parsed from the transcript text (spelled-out numbers like "two point five" map to a decimal). Then implement `/home/user/pipeline.py` as a strict three-stage numpy pipeline operating per-column: (1) parse the CSV from stdin treating empty strings as NaN, impute with `np.nanmean`; (2) recompute mean/std (`ddof=1`) and use `np.clip` with bounds `mean ± Z*std` where Z is the value you transcribed; (3) recompute mean/std again and standardize. Then compute `np.cov(data, rowvar=False, ddof=1)`, round to 4 decimals with `np.round`, and print rows as comma-joined values each formatted to exactly 4 decimal places (e.g. with an f-string `:.4f`, not Python's default `str()` which drops trailing zeros) — this formatting detail is what fuzz-equivalence checks against the oracle will catch first. Separately fix `/home/user/plot_cov.py` by adding `matplotlib.use("Agg")` before importing pyplot and replacing `plt.show()` with `plt.savefig(...)`.