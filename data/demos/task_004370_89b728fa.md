Fix the vendored library before writing any of your own code, since your sanitizer links against its output. Open `/app/vendored/libcsv_etl/Makefile` and look for a compiler flag that enables a "retry duplication" simulation macro (something like `-DSIMULATE_RETRY_DUPLICATION`) guarding an `#ifdef` in the IO source that duplicates lines — remove that flag/define, then `make` to produce a clean `libcsv_etl.a`. For the sanitizer itself, process stdin line by line (skip/reuse the header line for output but not for row processing): first check `status` is exactly `SUCCESS` or `PENDING` via strict string equality, then check `tx_id` against a `std::set`/`std::unordered_set` of already-seen IDs and drop repeats, then evaluate the rolling-average rule *before* committing the record — maintain a small deque/vector of the last up-to-3 accepted amounts, compute what the average would be if this record's amount were included, and only accept (and push into the window) if that average is `<= 5000.0`; a record that fails this check must not be pushed into the window at all. Only after a record survives all three filters do you mask `card_number` (replace first 12 chars with `*`, keep last 4) and emit the line. Link with the fixed `.a`, compile `/home/user/sanitizer.cpp` to `/home/user/sanitizer`, and manually pipe a few crafted rows through it to confirm each rule fires independently before running against the corpora.
