First transcribe the audio with `whisper /app/voice_memo.wav --model base.en` (or similar) and read the output transcript carefully to derive the exact binary chunk format (header size/endianness, what the header count means — characters vs bytes — and the character encoding of the payload) rather than guessing. Design the C parser as a loop: read the fixed-size header via `fread`, checking the return value equals the expected element count — if not (EOF or short read), `exit(1)` immediately. Compute the payload byte length from the transcribed encoding's char width, `fread` that many bytes, again validating the actual bytes read against the expected count and exiting 1 on truncation. Convert the payload's encoding to UTF-8 using `iconv_open`/`iconv` (matching the source/target encodings named in the transcript) and write the converted UTF-8 bytes to stdout followed by `\n`, looping until stdin is exhausted (clean EOF at a chunk boundary means normal successful termination, not an error). Test edge cases yourself: zero-length chunks, multi-chunk streams, and deliberately truncated inputs, comparing byte-for-byte since the grader does bit-exact diffing against a hidden reference across many random streams — get the encoding/endianness convention exactly right, since subtle mismatches (e.g. treating count as bytes instead of characters) will fail most fuzz cases.