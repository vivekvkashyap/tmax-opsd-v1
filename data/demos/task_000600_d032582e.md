Break this into three independent, verifiable stages rather than trying to do it all at once. Stage 1 (OCR extraction): use `ffmpeg -i /app/db_monitor.mp4 frame_%04d.png` (or similar) to extract frames at a reasonable rate, then run `tesseract` on each frame to pull out text lines matching `Tx[ID] ACQUIRE [Resource]` or `Tx[ID] WAIT [Resource]`; since the video accumulates lines over time, dedupe across frames (e.g. sort -u) to get the final unique event set — inspect OCR output manually for noise/misreads and clean obvious artifacts before trusting it. Stage 2 (graph query): load events into an SQLite table (tx_id, event_type, resource), derive wait-for edges by joining each WAITing transaction's resource to the transaction that ACQUIREd that resource, then write a recursive CTE that walks the wait-for edges from each node and flags a cycle when a path returns to its start node — separately compute in-degree per node (count of distinct waiters pointing at it) for "blocker centrality," breaking ties alphabetically. Stage 3 (API): serve `/api/deadlock` (comma-separated, alphabetically sorted cycle members) and `/api/blocker` (single top in-degree node) on 127.0.0.1:9090 using a lightweight bash-invoked HTTP responder (e.g. a tiny Python http.server wrapper, or socat/nc loop), run it in the background, and curl-test both endpoints yourself before finishing.