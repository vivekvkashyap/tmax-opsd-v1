Write dispatcher_v2.cpp to take argv[1] as the email file path, then read all of stdin into a buffer/stream and scan lines for exactly `To: ` and `Message-ID: ` prefixes — extract the list name by taking the substring between `To: ` and `@localhost`, and the message id by taking everything after `Message-ID: `, stripping leading `<` and trailing `>` if present (don't assume they're always there). If either header is absent after scanning all of stdin, exit(1); otherwise use `symlink()` (or `std::filesystem::create_symlink`) to create `/home/user/mail_queue/lists/<list>/<msgid>.eml` pointing at argv[1] (the absolute path passed in, not a relative one), then exit(0). Compile with `g++ -std=c++17 -o /home/user/app/v2/dispatcher dispatcher_v2.cpp`, create the announce/discuss list directories, and do the symlink swap with `ln -sfn /home/user/app/v2 /home/user/app/current` (the `-n` prevents it from following an existing symlink and putting v2 inside v1's directory). Finally write process_queue.sh to loop over `*.eml` in incoming/, and for each file run `/home/user/app/current/dispatcher "$(readlink -f "$file")" < "$file"`, then execute the script.
