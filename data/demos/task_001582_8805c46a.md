Parse each `sensor_A.log` line by extracting the bracketed UTC timestamp and the lat/lon values (e.g. with `grep -oP` or `sed`/`awk` regex captures), then convert the timestamp to Unix epoch reliably with `date -u -d "YYYY-MM-DD HH:MM:SS" +%s` (the `-u` flag matters since the log states UTC explicitly — a naive `date` call without it would use local timezone and shift the epoch). Compute distance as `sqrt(lat^2+lon^2)` using `bc -l` or `awk`, rounding to exactly 2 decimals with correct rounding (not truncation) — printf's `%.2f` in awk/bc handles this. For each log epoch, scan all `sensor_B.csv` rows (skip the header) and pick the row minimizing `abs(csv_epoch - log_epoch)`, keeping the first-seen row on ties (process rows in file order and use strict `<` when updating the best match, never `<=`, so earlier ties are kept). Assemble one output line per log entry with tab separators in the exact column order `log_epoch, csv_epoch, distance, temp`, writing to `/home/user/aligned_data.tsv` with no header. Since verification is an exact byte/checksum match, be careful about trailing whitespace, tab vs space separators, and decimal formatting consistency (temp value formatting should mirror the CSV's own precision).