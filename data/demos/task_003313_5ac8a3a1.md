Tackle the four pieces independently, verifying each in isolation before wiring them together. (1) For the legacy binary, don't guess the transformation from reading disassembly alone — run `/app/legacy_processor.bin` on a handful of crafted inputs (empty string, single char, ascending letters, known bytes) and diff the outputs byte-by-byte against the input to spot patterns like byte-order reversal, offset/XOR arithmetic, or hex encoding; replicate that exact transform in Python and sanity-check against more random strings before trusting it, since the grader fuzzes thousands of cases. (2) For the corrupted SQLite file, don't try to repair metrics.db directly — instead replay the WAL against a fresh checkpoint: copy metrics.db-wal alongside a valid empty/matching db file (or use `sqlite3` recovery tools, e.g. `PRAGMA wal_checkpoint` after placing the WAL correctly, or use `.recover`) to produce a queryable metrics_recovered.db, then verify with a `SELECT` that data reads back correctly. (3) In compute_recursive_metric, the recursive call must decrement n each step (self-call without decrementing n causes infinite recursion) — fix the recursion and add assertions checking n stays a non-negative int on each call. (4) Launch Redis and Flask with the required env vars set in the process environment, then hit /process?id=1 yourself with curl to confirm a 200 JSON response before declaring done.