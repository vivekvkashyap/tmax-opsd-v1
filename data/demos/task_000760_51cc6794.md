Find the running process with `pgrep -f traffic_sim.sh` (or `ps aux | grep traffic_sim`), then inspect its open file descriptors with `ls -l /proc/<PID>/fd/` to find the entry pointing to the deleted `/home/user/service.log` (it will show as `(deleted)`). Recover the still-live data by reading directly through that fd path, e.g. `cat /proc/<PID>/fd/<N> > /tmp/recovered.log` (do not try to reopen the original path — it no longer exists on disk; the data only lives via the open descriptor). Parse the recovered content as pseudo-JSON per line — since the task warns of "minor formatting edge-cases," don't assume rigid JSON; use a tolerant approach (e.g. Python with regex extraction of `"status":\s*(\d+)` and `"latency":\s*(\d+)` and `"ip":\s*"([\d.]+)"` per line) rather than a strict json.loads that might throw on a malformed line. Filter for the single line where status==500 and latency>8000, extract its ip field, and write just that IP (no extra whitespace/newline issues) to /home/user/culprit_ip.txt.
