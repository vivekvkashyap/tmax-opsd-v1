Work through the three bugs in `/app/log_filter-1.0.0` one file at a time. In `worker.c`, look for a mutex around the shared file-processing queue that's been commented out (or otherwise disabled) and re-enable the lock/unlock pair around the queue push/pop so concurrent workers don't race. In `parser.c`, find the control-character rejection loop — if it flags every byte `< 0x20` it will also reject legitimate `\r`, `\n`, `\t`, so explicitly whitelist those three before the reject check, and add a missing `strlen(line) > 1024` check to reject overlong lines. In `main.c`, locate the `assert(init_done)` and make sure the corresponding `init_done = 1;` actually executes before it (it's likely missing or misplaced after the assert). Rebuild with the project's normal build command, place the binary at `/home/user/log_filter`, then self-test by running it against small hand-made clean and malicious sample files before trusting it against the real corpora — don't just assume the fixes compile-and-work.
