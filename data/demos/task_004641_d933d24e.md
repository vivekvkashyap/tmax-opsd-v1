This is a black-box reverse-engineering task — determine the format empirically rather than guessing, then confirm with tooling. Start with `file` and `strings /app/wal_archive_tool` to look for magic-byte hints or format clues. Create small, controlled input files (e.g. all-zero bytes, a single repeated byte, a run of >255 identical bytes, text with no repeats, an empty file) and run the tool on each, then `hexdump -C` the outputs to reverse the structure: check for a fixed-length header at the start (look for a stable 4-byte prefix across outputs), inspect how repeated byte-runs are encoded (look for count/value byte pairs and how the tool handles counts over 255 — does it split into multiple pairs?), and check the tail bytes to see if they encode the original file length (try comparing input size to the last 4 bytes as a little-endian integer). Use `strace -e trace=openat,read,write ./wal_archive_tool in out` if behavior is unclear. Once the algorithm is characterized, implement it in C++ writing byte-for-byte identical output, compile with `g++ -O2`, and self-test with `cmp` against the original tool's output on your own varied fixtures (empty file, long runs, mixed data) before considering the port correct.