Treat this as three independent, verifiable stages and check each before moving on. (1) Cleaning: use pipe-delimited parsing (`awk -F'|'`) to skip the header, drop the `ObservationID` column, and filter rows where `Intensity` is a valid number strictly greater than 0 — reject both non-numeric tokens like "NaN" and negative/zero values with a numeric regex check (e.g. `$3 ~ /^-?[0-9]+(\.[0-9]+)?$/ && $3+0 > 0`), then emit `Wavelength,Intensity` as CSV with a comma, no header, no trailing blank fields. (2) Compilation: build the Go program with `go build -o /home/user/fitter mcmc_fitter.go` and confirm the binary runs before relying on it — don't hand-reimplement the fitting logic yourself, the Go program's stdout is the source of truth for the parameter values. (3) Run `./fitter /home/user/clean_spectra.csv`, capture stdout, and extract only the four `Key: value` lines matching `Amplitude`, `Center`, `Width`, `Background` (ignore any other printed lines like iteration counts) into `/home/user/final_parameters.log` in that exact order, preserving the numeric formatting the program printed verbatim rather than reformatting or rounding it yourself.
