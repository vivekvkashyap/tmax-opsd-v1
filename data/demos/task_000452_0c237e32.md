Split this into fixing the vendored library first, then writing the consumer program, and verify each independently. To find the deadlock in `/app/vendor/go-graph-etl/builder.go`, read the edge-adding code and identify that it locks two node mutexes in a fixed order (e.g. always "from" then "to") — the classic deadlock arises when two goroutines add reciprocal edges (A→B and B→A) concurrently and each acquires the first lock then blocks waiting for the second, held by the other goroutine. Fix it by always acquiring the two node locks in a deterministic order derived from a total ordering of node IDs (e.g. lexicographic string comparison), regardless of which node is "from" vs "to", with a special case for self-edges where only one lock is needed — this eliminates circular wait entirely. Stress-test your fix with many goroutines adding bidirectional edges concurrently under a timeout before moving on. Then write `/home/user/etl_query.go`: parse the input JSON edge list, call the fixed package's concurrent builder, do a DFS/BFS enumeration of all simple paths of exactly 4 nodes (3 edges) from every starting node, filter to those with summed edge weight > 100, sort lexicographically by the rendered "A->B->C->D" string, truncate to the first 10, and print as a JSON array of strings. Build with `go build -o /home/user/etl_query /home/user/etl_query.go` and test against several hand-built graphs to confirm ordering, filtering, and pagination all match the spec precisely, since correctness is checked by exact output comparison against a reference on many random graphs.
