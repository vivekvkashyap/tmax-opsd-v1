Don't hardcode a threshold guess — recover it from the audio first. Transcribe `/app/secret_instructions.wav` (e.g. `whisper /app/secret_instructions.wav --model base --output_format txt`, or any available STT tool; ffmpeg alone won't transcribe speech, only convert format) and read the resulting text carefully for the exact amplitude threshold and the exact minimum run-length of consecutive samples that defines "divergent" — write these two numbers down before coding. Then implement `/home/user/detect_divergence.sh`: dump the wav's raw sample values to text (e.g. `sox "$1" -t dat - | tail -n +3` giving time+amplitude columns, or use `ffmpeg`/`sox` to export raw PCM and decode with `od`/`awk`), then use `awk` to scan the amplitude column, maintaining a counter of consecutive samples whose `absolute value` exceeds the threshold you transcribed, resetting to 0 whenever a sample falls back under it, and printing `EVIL` if that counter ever reaches the required run-length, else `CLEAN` after scanning the whole file. Before finalizing, loop your script over every file in `/app/data/clean/` and `/app/data/evil/` (a `for f in ...; do ... done` or GNU `parallel`) and confirm 100% correct labels — adjust off-by-one in the run-length comparison if any file misclassifies.
