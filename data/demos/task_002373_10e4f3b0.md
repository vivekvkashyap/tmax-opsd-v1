Write a straightforward C program that parses the CSV manually (skip the header line, then split each remaining line on commas, using `strtod`/`sscanf` to pull the `latency_ms` and `accuracy` fields by their column position) while accumulating the running sums needed for Pearson's formula: n, sum(x), sum(y), sum(x*y), sum(x^2), sum(y^2). Then compute `r = (n*sum_xy - sum_x*sum_y) / sqrt((n*sum_x2 - sum_x^2) * (n*sum_y2 - sum_y^2))` using `double` precision throughout (not float, to avoid rounding drift), and format the result with `printf("Correlation: %.4f\n", r)` (verify whether a trailing newline matters, and match the exact literal prefix `Correlation: `). Compile with `gcc -o analyze analyze.c -lm` and run it so it reads `/home/user/experiments.csv` and writes `/home/user/correlation_result.txt`. Sanity-check by computing the same correlation by hand or with a quick Python/numpy one-liner on the same CSV and comparing to your program's output before considering it done — this catches column-index-off-by-one or float truncation bugs early.
