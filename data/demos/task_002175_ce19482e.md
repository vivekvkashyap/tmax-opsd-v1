Structure this as three clear passes over the data rather than trying to do everything in one loop. Pass 1: parse `/home/user/transactions.csv` (skip the header) and build a map keyed by `(source, target)` pair, keeping only the maximum `amount` seen for each pair — this is your deduplication step, so use something like `std::map<std::pair<string,string>, double>` and overwrite only when a larger amount is found, ignoring `timestamp` entirely. Pass 2: collect the set of all distinct node names (union of every source and every target across the deduplicated edges) so nodes with zero incoming edges are still represented with a sum of 0 — a common mistake is only iterating over nodes that appear as edge targets and silently dropping source-only nodes. Pass 3: for each node, gather all deduplicated edges where it is the target, sort those incoming weights descending, sum the top 3 (or fewer if less than 3 exist), and write one row per node. Sort the final node list alphabetically by node_id before writing, and match the exact header `node_id,top3_in_weight_sum`. Compile with the exact command given and run it to actually produce the CSV — don't just leave the compiled logic unexecuted.
