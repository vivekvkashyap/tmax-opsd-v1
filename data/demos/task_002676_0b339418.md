Reverse-engineer methodically rather than guessing the algorithm. Start with `file` and `strings /app/legacy_checker` to see if any hints survive stripping, then use `ltrace`/`strace` on a couple of known inputs to see if library calls (like `sscanf`, `printf`) reveal the parsing structure. Since it's a stripped binary, disassemble the relevant function with `objdump -d` or `gdb`/`ghidra` if available, and trace how each hex-pair of the 12-character argument gets transformed — look for a byte-wise XOR against a repeated immediate constant, which is a common lightweight obfuscation. Once you can decode the 6 raw bytes, work out how they map to the three 16-bit fields (check byte order — try both big- and little-endian by comparing against binary output on chosen test inputs) and derive the conditional logic controlling which of the three status strings gets printed by feeding the binary crafted hex strings that isolate each branch (e.g. try mode bits that set/clear specific flag bits, and uid/gid of 0 vs nonzero). Validate your understanding by running the real binary on many random 12-hex-char inputs and diffing against your derivation before writing the Go port, so you catch edge cases (like which bit exactly triggers "world-writable" vs "group-writable" checks) before compiling `/home/user/policy_check`.
