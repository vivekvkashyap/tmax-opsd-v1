Treat the memory dump as unstructured binary containing readable text fragments — use `strings /home/user/crash-dump.bin | grep "panic: SRE_CRITICAL_ERR:"` (or `grep -a` directly on the binary file) to pull out the exact panic line without corrupting it via a text-mode read of the raw file. Then search the repo's full history, not just the current HEAD, for when that exact string first appears: `git -C /home/user/app-repo log -p --all -S "SRE_CRITICAL_ERR:" --reverse` (the `-S` pickaxe finds commits that changed the count of occurrences of the string) and take the first (oldest, due to `--reverse`) matching commit's full hash — don't assume it's the most recent commit that merely touches the file, since later commits may also contain the string without introducing it. Get the full 40-character hash with `git rev-parse <short-hash>` if needed. Write `/home/user/report.txt` with exactly two lines in the given format: `COMMIT_HASH:<hash>` and `ERROR:<message>` where the error text has the leading `panic: ` prefix stripped but is otherwise verbatim from the dump.
