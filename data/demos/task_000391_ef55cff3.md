Do not read the running process's source or memory directly for the secret — treat it as a black-box service you must probe over the network, matching real incident-response practice. First find the listening port without killing or restarting the process: use `ss -tlnp` or `lsof -i -P -n` (or inspect `/proc/<pid>/net/tcp` and cross-reference `/proc/<pid>/cmdline` for `suspicious_service.py`) to identify which port belongs to that PID. Then write a small Python script using `urllib.request` or `http.client` to GET `http://127.0.0.1:<port>/admin`, and read the `Set-Cookie` response header directly from the response object (not from a text dump) to reliably extract the cookie name/value pair, isolating just the value after `Secret-Admin-Cookie=` and before any `;`. Compute `hashlib.sha256(cookie_value.encode()).hexdigest()` and `hashlib.md5(open(path,'rb').read()).hexdigest()` for the script file itself (hash the file's bytes, not a guessed value). Write exactly two lines to `/home/user/report.txt` in the specified order, with no extra whitespace.