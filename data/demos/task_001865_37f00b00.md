Build the pieces in dependency order and verify each in isolation before wiring them together. First write `verify.c` implementing `calculate_checksum` as a simple sum of ASCII byte values over the input string, compile it to `libverify.so` with `gcc -shared -fPIC`, and confirm with a tiny standalone C or Python ctypes test that it returns the sum you expect for a known string. Compute by hand (or with a one-liner) what checksum your chosen algorithm produces for the exact verification string `id:42,result:47` — this is the value your mock API's hardcoded `checksum` field must match, so don't blindly copy a checksum from the task text; derive it from your own `calculate_checksum` implementation. Write `mock_api.py` to serve that computed checksum (along with id=42 and the given expression) on GET `/task`. For the Rust client: use `reqwest`/`ureq` for HTTP and `serde_json` for parsing, implement a small precedence-aware evaluator for `+`/`*` (evaluate all multiplications first, then sum), format the verification string exactly as `id:<ID>,result:<RESULT>`, and call the C library via `extern "C"` FFI with a `build.rs` or `#[link]` directive pointing at the `lib/` directory — set `LD_LIBRARY_PATH` (or use `rpath`/`-Wl,-rpath`) in `ci_run.sh` so the dynamic linker finds `libverify.so` at runtime. Have `ci_run.sh` start the mock API in the background, `cargo build --release`, run the client, then kill the mock API.