Start by extracting the hardcoded HMAC secret from /app/architecture.png — open the image and read the text in it (OCR or visual inspection); you need this exact string later to validate signatures, so don't guess it. Then process tokens.txt one JWT per line: base64url-decode the header and payload segments yourself (or use a JWT library) without verifying first, so you can classify by priority — check `alg` in the header for `"none"` first (alg_none takes precedence), then attempt HMAC-SHA256 verification of the token's signature using the secret you read from the image (if it verifies, mark weak_signature), then check the `exp` claim against the fixed reference time 1710000000 for expired tokens — apply these checks in that specific priority order since a token can satisfy multiple conditions. Skip any token that hits none of these categories entirely (don't write a "none" row). For the SSH fingerprint column, parse ssh_logs.txt to build a per-user history and pick the fingerprint from the most recent timestamped entry matching the token's `sub` claim, leaving it blank when there's no match. Since scoring uses macro F1 with a 0.95 threshold across ~5000 tokens, be rigorous about edge cases (missing claims, malformed base64) rather than approximate.
