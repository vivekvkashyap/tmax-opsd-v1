Do not disassemble or copy logic from `/app/path_finder` — just use it as a black-box oracle to validate your own output's correctness (run it once to see the expected JSON shape/path, and to compare against your program's result). Design your C program for the actual bottleneck: parsing 500k+ CSV rows and building a graph efficiently. Use a single-pass buffered read (avoid `fscanf` per field; use `fgets`/`strtok` or manual pointer scanning), a hash map (open-addressing or simple chaining keyed by node-name hash) to intern node names to small integer IDs instead of repeated string comparisons, and an adjacency list (not a dense matrix) for the graph so traversal is O(V+E) not O(V^2) or worse. Aggregate edge capacities into a hash map keyed by (source,target) while streaming rows — do this once, not per query. For shortest path with max-bottleneck tiebreak, run BFS layer-by-layer from A100 to get min hops, then among equal-hop paths use a DP/BFS variant that also tracks max-min-edge-capacity per node at each hop level, backtracking to reconstruct the path. Compile with `-O2`. Benchmark both binaries with `time` yourself before declaring done — the required speedup is large (10x+), so algorithmic complexity, not just micro-optimization, is what matters.