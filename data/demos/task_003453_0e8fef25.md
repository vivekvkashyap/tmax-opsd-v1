Fix the package before touching any data. Read `/app/custom_vec_eval-1.0.0/custom_vec_eval/metrics.py`: the squared-Euclidean-distance expansion `||q||^2 + ||e||^2 ± 2*q.e` should use a MINUS on the cross term (since ||q-e||^2 = ||q||^2 + ||e||^2 - 2*q·e); if you find a `+` there instead, that's the sign error to correct. Separately, check `plot.py` for a non-interactive backend call like `matplotlib.use('Template')` — a backend that produces no real output — and switch it to `matplotlib.use('Agg')`, which does write real PNGs; confirm by installing with `pip install -e /app/custom_vec_eval-1.0.0` and generating a test plot that you actually open/inspect. Only once distance and plotting are verified correct should you tackle the alignment: this is ordinary least squares — using queries[0:20] as X and their ground-truth-matched embeddings as Y, solve for a weight matrix and bias (e.g. via `np.linalg.lstsq` on the augmented system, or closed-form normal equations) that maps raw queries into embedding space, then apply that same learned transform to all 100 queries before saving. Validate on the training 20 first (recall should be near-perfect there) before checking the held-out 80, and don't let subtle transposition errors in the linear algebra silently corrupt the mapping.
