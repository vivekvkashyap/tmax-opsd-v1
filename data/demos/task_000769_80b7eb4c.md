Break this into extraction, cleaning, Go parsing, and packaging stages, verifying each before moving on. Extract every `.tar.gz` under `/home/user/docs_raw/` into `/home/user/docs_cleaned/` (loop over files with `tar -xzf ... -C /home/user/docs_cleaned/`). Strip watermarks in-place with a targeted regex, e.g. `sed -i -E 's/\[DRAFT_WATERMARK_[0-9]+\]//g' *.md`, and confirm with `grep -r DRAFT_WATERMARK` that nothing remains. Parse `/home/user/doc_rules.ini` (a simple INI) in Go — either with a tiny manual line-scanner splitting on `=` under the `[extraction]` section, or a minimal INI library — to pull out `start_marker` and `end_marker` as configurable strings rather than hardcoding them. For each `.md` file, scan lines and only capture `key: value` pairs found strictly between a `start_marker` line and the next `end_marker` line (toggle an "inside block" boolean state machine); merge all captured pairs across files into one flat map and marshal with `encoding/json` to `/home/user/compiled_config.json`. Finally zip the cleaned `.md` files plus the JSON into `/home/user/final_docs.zip` with entries at the archive root — when adding files with `archive/zip`, set each `zip.FileHeader.Name` to just the basename, not a path including the source directory.
