Break this into independently-verifiable pieces before wiring them into the Rust server. First, get the transcript: use a Python/CLI tool (e.g. `whisper` or `openai-whisper`) on `/app/engine_test_run.wav` to produce text — do this as a shell step, capture the exact transcribed string, and have the Rust server serve that recovered text (read it from a file you saved, or embed the exact recovered string) rather than trying to re-transcribe audio in Rust. Second, for signal processing: read the WAV as 16-bit PCM, normalize each sample to a float in [-1,1] by dividing by 32768.0, then reshape into non-overlapping 1024-sample rows, discarding the remainder — get the frame count right (rows = len(samples)/1024, floor division). Compute C = XᵀX (1024x1024) and run SVD (e.g. via `nalgebra`'s SVD) to get singular values; the largest singular value and the ratio of it to the sum of all singular values are the two features you must serve — do not hardcode a guessed number, compute both from your actual matrix. Plot the first 1024 samples with `plotters` to `/home/user/frame_plot.png`. Finally wire an HTTP server (any Rust HTTP crate) exposing `/transcript`, `/features`, and `/hypothesis` exactly as specified, keeping it running in the background (tmux or `&` with disown) so the verifier can query it after your turn ends.