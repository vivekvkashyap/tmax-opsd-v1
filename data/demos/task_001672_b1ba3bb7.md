Work bottom-up through the toolchain since each layer depends on the one below it working correctly. Fix the CMake link error by adding `target_link_libraries(mathwrap fastmath)` after both `add_library` calls so the wrapper .so is linked against the assembly .so at build time; run `cmake` + `make` in `build/` and confirm `libmathwrap.so` produces no unresolved-symbol errors. For the assembly bug, read the comments literally: the intended polynomial is `3x^2 + 2y`, but the code computes `y + 2` instead of `y * 2` — change `add $2, %rcx` to `imul $2, %rcx` (integer multiply by 2, not add), leaving the `3x^2` computation as-is. Verify by hand-computing `f(x,y)` for a couple of test pairs and comparing to the assembly's actual behavior after patching. For the Flask endpoint, load the .so with `ctypes.CDLL`, set `argtypes`/`restype` to `c_long` for `compute_wrapper`, parse the JSON body's `x`/`y`, call the function, and return `{"result": ...}`; start the server as a background process before your client script runs. Finally, read `inputs.json`, map `input_x/input_y` to `x/y`, POST each to `/compute`, and assemble the exact three-key output structure specified.