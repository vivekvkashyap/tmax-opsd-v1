Work through this vendored package in the order given, verifying each stage before moving to the next. First, get the package installable/importable: open `setup.py` and `legacy_sso/auth.py` and look for syntax-level defects — a malformed version pin (like a single `=` where `==` is required in a requirements spec) and a misspelled `import` statement are the kind of trivial-but-blocking errors to hunt for; fix them so `python3 -m legacy_sso.server` runs without ImportError. Second, grep the request-handling code (likely in `auth.py`) for any conditional that grants admin/bypass access based on a specific HTTP header value (a "debug bypass" style backdoor) — delete that branch entirely so the check falls through to real auth. Third, crack the MD5 hash in `config.json`: read `admin_hash`, then iterate `/app/wordlist.txt` line by line, `md5`-hashing each candidate and comparing to the stored hash (e.g. `while read w; do [ "$(echo -n "$w" | md5sum | cut -d' ' -f1)" = "$HASH" ] && echo "$w"; done < wordlist.txt`), then add the recovered plaintext under a new `admin_password_plaintext` key in `config.json` without removing the original hash field. Finally start the server on 127.0.0.1:8080 in the background and keep it running so the verifier can exercise `/admin` (with and without the removed backdoor header) and `/login`.
