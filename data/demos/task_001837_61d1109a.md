Treat `/app/citation_oracle` as a black box to reverse-engineer, not something to copy. Build small synthetic author/paper/citation datasets of increasing complexity, run the oracle against them with varied queries, and diff outputs to infer the algorithm: likely a bounded-hop (try 1, 2, 3 hops) BFS/traversal over the citation graph starting from an author's papers, with per-hop weighting, some date-based filter, a top-N cutoff, and a specific sort/format for the output JSON — confirm each hypothesis (hop count, weight decay, filter cutoff year, N, sort key) by constructing a minimal test case that isolates just that behavior. Once you're confident in the exact logic, implement it in Python: parse `authors.csv` and all paper JSON files once into in-memory indices (author->papers, paper->citations, paper->year) — e.g., dicts/adjacency lists, or load into SQLite with indices on author-paper and citation edges — then answer each query via graph traversal over the index instead of re-scanning files. Write results to `/home/user/fast_results.json` in the oracle's exact format. Benchmark both tools on the same query set to confirm output equality and the required >=10x speedup before finishing.
