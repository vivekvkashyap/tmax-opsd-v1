Before writing any Rust, reverse-engineer the crash trigger empirically. Extract both tarballs, then run `/app/legacy_doc_parser` against individual files from `clean/` (should succeed) and `evil/` (should crash/hang) to build intuition; diff the structure of paired clean vs. evil files, paying close attention to the `Signature:` header field, since RFC-822-style header folding means a header value can be split across multiple indented continuation lines — you must reconstruct the *logical* (unfolded) value length, not just measure a single physical line, to correctly detect files whose total signature content exceeds a fixed buffer size. Use `strings`/`objdump`/`gdb` on the stripped binary only to confirm your hypothesis about the buffer size threshold if the corpus alone is ambiguous — do not hardcode the specific byte threshold if the corpus itself makes it discoverable via bisection. Then build the Rust tool with `cargo new sanitizer`, implement a proper multi-line header parser (accumulate continuation lines for `Signature:` before measuring length), recursively walk `--input`, and copy only files whose reconstructed signature length is under the threshold to the mirrored path under `--output`, skipping the rest silently. Validate against your own extracted corpora before relying on the hidden test set, and build with `cargo build --release`.
