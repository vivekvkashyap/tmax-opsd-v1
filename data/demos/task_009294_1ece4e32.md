Start the Python auth service in the background and confirm it responds (`curl -i "http://127.0.0.1:8000/login?next=..."`) before writing any C++ — this tells you exactly what response format you're parsing, including header casing and line endings, without guessing. In the C++ program, build a raw socket with `socket()`/`connect()` to 127.0.0.1:8000, then write a well-formed HTTP/1.1 GET request string with `\r\n` line endings including a `Host:` header and a blank line terminator, targeting the exact vulnerable path with the attacker URL as the `next` query parameter. Read the response into a buffer, split on `\r\n`, and scan header lines for one starting with `Location:` (case-insensitively, since HTTP headers aren't case-sensitive) — extract everything after the colon and trim leading whitespace to get the redirect URL exactly as sent, not URL-decoded or otherwise transformed. Hash that exact string with OpenSSL's `SHA256()` from `<openssl/sha.h>`, hex-encode the 32-byte digest into a lowercase hex string, and write the audit line to `audit_trail.txt` in precisely the specified format. Compile with `-lcrypto`, run it, and diff your header parsing against the raw `curl -i` output you captured earlier to confirm you extracted the right substring.
