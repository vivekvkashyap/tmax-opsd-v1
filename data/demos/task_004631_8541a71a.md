When pivoting a CSV with `pandas.DataFrame.pivot`, missing cells become `NaN`, which silently upcasts int columns to float and can also turn integer column labels (`item_id`) into floats. After `fillna(0)`, explicitly cast the column index back to int (e.g. `matrix.columns = matrix.columns.astype(int)`) so downstream IDs stay integers, not `101.0`. Build the pipeline step by step: read the CSV, pivot user_id x item_id with values=rating and fillna(0), transpose so items become rows, fit `TruncatedSVD(n_components=2, random_state=42)` on the item matrix, then compute pairwise `cosine_similarity` on the reduced embeddings. Use the item-index labels (not positional row numbers) to look up item 101's similarity row, drop itself, sort descending, and take the top 3 index labels. Write them as comma-separated integers on one line to the output file — verify with `cat -A` that there's no trailing float formatting or extra whitespace/newlines beyond a single line.