Treat `/app/sim_engine` as a black-box objective function you must minimize over 3 bounded parameters, not something to reverse-engineer by disassembly. First reshape `raw_data.csv` (columns x_index, y_index, measurement) into a 100x100 array in the right (row, col) order and write it to HDF5 under the dataset name `grid_data` — verify the shape and orientation before running the optimizer, since a transposed grid will silently ruin results. Then write `optimize.py` using a global optimizer well suited to noisy, possibly multimodal black-box functions (e.g. `scipy.optimize.differential_evolution` with bounds `[-10,10]` for each of p1,p2,p3), calling `subprocess.run` on the binary and parsing its stdout as the energy value. Parallelize evaluations (e.g. `differential_evolution(..., workers=-1)` or a `multiprocessing.Pool` mapping candidate parameter sets to subprocess calls) since each binary call is slow and you need enough function evaluations to converge below the energy threshold. Run a few evaluations manually first to confirm you're parsing the binary's output format correctly before wiring up the optimizer, and write the best (p1,p2,p3,energy) to `/home/user/best_params.json` once converged.