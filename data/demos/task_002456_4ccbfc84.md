Build this pipeline in clearly separable stages and validate each independently before wiring them together. First, use `ffmpeg -i /app/data/traffic_cam.mp4 -vf fps=1,format=gray,scale=4:4 ...` to extract 1-fps grayscale 4x4 frames, then read raw pixel bytes (e.g. via `ffmpeg ... -f rawvideo -pix_fmt gray -` piped to a small script) to flatten each frame into a 16-int vector in row-major order — verify pixel ordering against a known test image before trusting it. Second, write `/home/user/infer.go` to load the 16x8 weight matrix and 8-value bias from the CSVs exactly as given (watch row/column orientation: confirm via matrix dimensions that `Input(1x16) * Weights(16x8) + Bias(1x8)` is the intended multiplication order, not its transpose), apply ReLU elementwise, and read/write via stdin/stdout JSON with `encoding/json`. Third, implement cosine similarity between consecutive embeddings in float64 with the explicit "both-zero-norm implies similarity 1.0" special case, and mark frame `i` as a scene change only when similarity is strictly `< 0.8`. Compile with `go build -o /home/user/bin/infer /home/user/infer.go`, test it standalone with hand-crafted JSON arrays covering zero vectors and negative-dot-product cases before piping real video-derived vectors through it, since the fuzz equivalence check will probe edge cases like these directly.