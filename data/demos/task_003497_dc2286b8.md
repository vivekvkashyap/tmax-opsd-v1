Write curate.sh to iterate over every file in /home/user/incoming, and for each one use `readelf -h "$f" >/dev/null 2>&1` (checking the exit status) as the ELF validity filter — this correctly rejects text files and shell scripts without needing to inspect magic bytes yourself. For files that pass, parse the `readelf -h` output: extract the value after "Machine:" with something like `sed 's/.*Machine:[ \t]*//'` and convert spaces to underscores with `tr ' ' '_'`; extract the "Type:" line and classify as EXEC or DYN based on substring match (grep -q), since e.g. "DYN (Position-Independent Executable)" and "DYN (Shared object file)" both count as DYN. Create the destination directory /home/user/repo/<Arch>/<Type>/ with `mkdir -p` before copying the file in, and compute the checksum with `sha256sum "$f" | awk '{print $1}'` on the *original* file (not the copy). Append one CSV line per ELF file to /home/user/repo/manifest.csv in the exact comma-separated order specified, being careful there's no trailing/leading whitespace around commas. Finally, actually run the script so the repo and manifest exist for verification, and spot-check a couple of entries against `readelf -h` output yourself.
