Tackle the three issues independently and verify each before moving to the next. First, recover the deleted config: loopback-mount `/app/data/partition.img` (`mount -o loop /app/data/partition.img /mnt/...`) or use `debugfs`/`ext4magic` to extract the deleted `config.json` inode, and place the recovered file at `/app/worker/config.json` — confirm its contents parse as valid JSON before moving on. Second, read `/app/worker/aggregator.py`'s batch-processing loop closely; an `IndexError` on batch windows is almost always a range/slice endpoint that goes one past the batch length (e.g. `range(len(batch)+1)` or an inclusive final index) — fix the boundary so it iterates exactly over valid indices, and test by feeding it a small in-memory batch by hand. Third, for the precision drift, replace any naive running `total += value` float accumulation with `math.fsum()` over collected values (or Python's `decimal` module) so tiny floating-point sensor readings don't lose precision under repeated addition. Only after all three fixes are verified in isolation, run `/app/start.sh`, let it run for 30 seconds so the pipeline actually processes live batches through Redis and the FastAPI simulator, then run `/app/stop.sh` and inspect `/app/worker/output/totals.csv` for sane, high-precision values.