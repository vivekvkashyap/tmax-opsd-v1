Build this in dependency order and verify each stage in isolation before wiring them together. First, OCR `/app/spec.png` (e.g. `tesseract /app/spec.png -`) to recover the exact 3x3 kernel values — don't assume a specific well-known kernel, read what the image actually says and transcribe all 9 numbers precisely, including sign. Next fix `/app/clib/filter.c`: check convolution loop bounds carefully — a common bug is `<=` instead of `<` on width/height loops causing one-past-the-end reads/writes; also verify the 3x3 neighborhood accesses clamp or skip at image edges rather than reading negative/out-of-range indices, per the edge-handling rule in the task. Fix the `Makefile` to actually produce a shared object (`-shared -fPIC`, correct output name `libfilter.so`) and confirm it compiles standalone with `make` before touching Go. Define the `.proto` with `FilterRequest`/`FilterResponse` message types carrying bytes fields, generate Go stubs, then implement the cgo bridge in `main.go` matching the C signature exactly (`void apply_filter(const unsigned char*, unsigned char*, int, int, float[9])`), passing the extracted kernel as a `[9]C.float` array. Test convolution correctness locally against a simple case (e.g. compare against `scipy.ndimage.convolve` on a small array) before running the full client/server pipeline to produce `/app/output.png`.