Implement the CHNK parser strictly from the binary spec, then validate against both provided corpora before touching the pipeline. In C++, open the file in binary mode, read exactly 4 bytes and compare to "CHNK", read a little-endian uint32 chunk count, then loop reading a little-endian uint32 size followed by that many payload bytes for each chunk — use `read()`/`fread()` return values (not just `feof`) to detect truncation, since a short read mid-chunk means the file ended early. Reject (exit 1) whenever: magic mismatches, any chunk's declared size exceeds 1048576, a read comes up short (truncated), or after parsing all declared chunks there are still unconsumed bytes left in the file (trailing garbage) — compare bytes consumed to total file size explicitly. Exit 0 only when parsing consumes the file exactly. Compile with a normal C++ compiler and iterate against `/home/user/corpus/clean` and `/home/user/corpus/evil` until every case passes, since the real verifier corpus is stricter/hidden. Only after the binary is solid, edit `[Upload]` FILTER_CMD in `/app/flask_app/config.ini` to the binary's absolute path — no service restart needed since Flask reloads config per request.