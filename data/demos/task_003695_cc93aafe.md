Do the two reconnaissance steps before writing any server code. First, run `tesseract /app/sticky_note.png stdout` (install tesseract-ocr if missing) to read the port and token off the image — don't hardcode guessed values, actually OCR it and sanity-check the extracted digits/token string look plausible. Second, inspect `/app/datasets.db` with `sqlite3 /app/datasets.db .schema` to discover the real table/column names (they won't be obviously named "authors"/"collections"/"metrics" — figure out the foreign-key relationships by looking at column names like `*_id`). Write the aggregation as a SQL query using `ROW_NUMBER() OVER (PARTITION BY <collection> ORDER BY metric_value DESC)` filtered to rank <= 2, then join back to authors/collections for names. Build the Python API (Flask/FastAPI/stdlib) binding `0.0.0.0` on the OCR'd port; implement bearer-token auth by checking the `Authorization` header equals `Bearer <token>` exactly, returning 401 otherwise. Group the ranked rows per dataset into a `top_metrics` list (highest first), sort the final `results` array alphabetically by `dataset_name`, and run the server in the background before finishing, then smoke-test both the 401 and 200 paths with `curl`.
