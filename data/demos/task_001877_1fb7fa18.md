Tackle the four sub-problems in order, since later steps depend on earlier discoveries. First transcribe `/app/alert.wav` (e.g. with `whisper` or another ASR tool) to recover the exact timezone offset in hours stated in the voicemail — don't assume a common value, read it from the audio. Second, extract the 16-character "poison" string from `/app/crash.dump`: since it starts with `POISON_`, grep the binary dump for printable strings (`strings /app/crash.dump | grep POISON_` or scan for the ASCII prefix directly) to recover the exact literal. Third, fix the build: run `make` in `/home/user/pipeline/`, and address whatever surfaces — likely a wrong/missing compiler name and missing tabs in Makefile recipe lines, plus missing `#include` headers (`stdio.h`, `string.h`) in `parser.c` causing implicit-declaration or undefined-reference errors. Fourth, implement `parser.c` to read lines from stdin: check for exact match against the poison string first (output `[DROPPED]`), then try strict parsing of `YYYY-MM-DD HH:MM:SS` (validate all fields, not just format shape) and apply the audio-derived hour offset using proper calendar arithmetic (converting to a `struct tm`, using `timegm`/`mktime`-style epoch math or manual day/month/year rollover, not naive string manipulation) before reformatting; anything else prints `[ERROR]`. Since grading is exact-match fuzzing against a hidden oracle, test with edge dates (month/year boundaries, leap years) manually before finishing.
