Build this in clearly separated stages so you can validate each before moving on. Extract 3-mers by sliding a window of size 3 across the given sequence, collect the unique set to define graph nodes, then for each 4-mer (window of size 4) increment the weighted edge from its 3-mer prefix to its 3-mer suffix — this naturally builds the transition-count matrix. Normalize each row to sum to 1 to get M, giving sink nodes (no outgoing edges) a uniform 1/N row instead of leaving it all zero. For the exact PageRank, solve the linear system directly via matrix inversion (`np.linalg.inv(I - d*M.T) @ (...)`) rather than iterating — this is your ground truth to compare against. Separately implement power iteration starting from the uniform vector, applying `v = d*M.T@v + (1-d)/N*ones` each step, and record the L-infinity error `max(abs(v_k - v_exact))` at exactly iterations 1, 5, 10, 20, 50 (use a running counter, don't reset v between recorded checkpoints). Save those five errors as strings keyed by iteration number to convergence.json, and separately plot k vs error and save to convergence_plot.png. Sanity-check that the error strictly decreases (power iteration should converge monotonically here) before finishing.
