Implement the standard Normal PDF and the Student's t PDF directly from their formulas using `std::tgamma` for the gamma function terms in the t-distribution's normalizing constant. Write the integrand as `P(x) * log((P(x)+1e-15)/(Q(x,nu)+1e-15))`, and implement composite Simpson's 1/3 rule precisely: with N=100000 (even) intervals over [-20,20], the formula is `(h/3) * (f(x0) + f(xN) + 4*sum(f(odd-index points)) + 2*sum(f(even-index interior points)))` — get the odd/even index alternation and the endpoint weights exactly right, since Simpson's rule is easy to subtly misindex. Wrap this integral as a function of nu so you can call it at 3.0, 3.01, and 2.99. Compute the derivative via central difference: `(KL(3.01) - KL(2.99)) / (2*0.01)`. Use `double` precision throughout, format both outputs with `std::fixed << std::setprecision(6)`, and write exactly two lines to `/home/user/results.txt`. Compile exactly as specified (`g++ -O3 -std=c++11 ...`) and run it to produce the file — verify your own numbers are self-consistent by checking the integral of P alone integrates to ~1 as a sanity check before trusting the KL result.