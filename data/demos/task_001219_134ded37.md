Treat this as black-box binary reverse engineering by differential testing, not disassembly. Build a harness that feeds `/app/dataset_cleaner` many crafted CSVs (small, e.g. 3-5 rows/cols, with and without "NaN", with ties and near-ties in distance) and diff the stdout against hypotheses. Work incrementally: (1) first confirm the imputation rule by giving a column with one NaN and checking whether the printed nearest-neighbor result implies the NaN was replaced by something computed from ALL rows (leak) vs. only some subset — vary which rows have NaN to isolate this; (2) once imputation is nailed down, probe the distance metric (Euclidean vs squared, vs L1) using rows with known geometric relationships; (3) probe tie-breaking by constructing exact ties in distance and checking which index wins; (4) check exact output formatting (spacing, integer/float printing, trailing newline) byte-for-byte since the verifier does exact string comparison. Implement your hypothesis in `/home/user/replicate.py` using pandas/numpy, then re-run your own fuzz harness comparing your script's output to the binary's output on fresh random CSVs before declaring done — don't stop at the first case that happens to match.