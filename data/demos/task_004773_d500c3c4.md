Start by reproducing the hang: run the existing program against `inputs.txt` and observe it stall on the negative/zero entries, confirming the root cause is that `collatzSteps`'s `for n != 1` loop never reaches 1 when n starts at 0 or negative (0 stays 0 forever under the even branch; negatives grow away from 1 or oscillate without terminating). Fix it with a guard at the top of `collatzSteps`: `if n <= 0 { return -1 }`, before the loop runs, so no infinite loop is ever entered — don't try to detect the hang mid-loop with a step counter, since that's fragile and against the spirit of the fix. In the main loop, branch on the returned steps value: when it's `-1` write `Input: <N>, Steps: Invalid`, otherwise write `Input: <N>, Steps: <S>`, using `fmt.Fprintf` to the output file exactly as specified. Rebuild with `go build` (or `go run`) and execute so `output.txt` is regenerated, then read it back to confirm every line matches the expected input/steps pairing before considering the task done.