Treat this as three independently-gradable pieces and get each fully right rather than partially covering all three. (1) `filter_telemetry.py`: parse each JSON record and validate strictly — `timestamp` must be a float >= 0.0, `frame_id` an int >= 0, `bbox` a list of exactly 4 non-negative ints, `confidence` a float in [0.0, 1.0]; wrap parsing/validation in try/except so any type mismatch or missing key causes silent discard rather than a crash, and only print valid records as JSON lines. Validate this against `/app/corpus/clean/` and `/app/corpus/evil/` (or similarly structured fixtures) until every malformed record in the evil set is dropped and every clean record survives. (2) `process_video.py`: read `/app/traffic.mp4` frame by frame (opencv or ffmpeg pipe), compute mean grayscale brightness per frame at 30fps-derived timestamps, sort by frame_id, forward-fill any gaps in frame_id sequence with the previous brightness before computing a 5-frame rolling average, and write the exact CSV columns/order requested. (3) Register the cron job for the current user with `crontab -e`/`(crontab -l; echo "0 * * * * ...") | crontab -` using minute-field `0` for hourly execution, and make both scripts executable.
