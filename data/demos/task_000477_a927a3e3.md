Diagnose before fixing: run `/home/user/project/build.sh` several times in a loop and capture stderr from the backgrounded subshells (redirect each `sqlite3 ... UPDATE` command's stderr to a file, or run with `bash -x`) to observe the actual error message the concurrent `sqlite3` invocations produce when multiple background processes hit the same SQLite file simultaneously — this is a classic "database is locked" race from concurrent writers without WAL mode or retry logic, not a logic bug in the SQL itself. Capture that exact stderr text since your report must quote it verbatim (case-insensitive match is fine, but don't rephrase it). Fix `build.sh` while preserving the `&` parallel backgrounding: either enable WAL mode (`PRAGMA journal_mode=WAL;`) plus a busy timeout (`PRAGMA busy_timeout=...` or `sqlite3 -cmd ".timeout ..."`), or add retry-on-lock logic around each UPDATE call, so concurrent writers don't collide. After fixing, re-run `./init_db.sh` then `./build.sh` repeatedly to confirm it reliably exits 0 and `SELECT count(*) FROM tasks WHERE status='pending'` is always 0. Then write the single-line `ERROR=<message>` report using the stderr text you actually captured.