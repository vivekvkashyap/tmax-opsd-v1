Write `/home/user/process_updates.py` as a sequence of clearly separated, independently-testable steps. For safe extraction, open the tar with `tarfile.open`, iterate `tar.getmembers()` and inspect each member's `.name` BEFORE extracting: skip (don't extract) any member whose name contains `../` or starts with `/` — extract only the remaining members individually (`tar.extract(member, path=...)`), never call `extractall()` blindly since that would apply no filtering. For logging, open the log file in append mode, call `fcntl.flock(f, fcntl.LOCK_EX)` before writing each extracted file's absolute destination path, then `fcntl.flock(f, fcntl.LOCK_UN)` (or let a `with` block release it) afterward — do this per-file so the lock genuinely brackets each write. For the metadata pass, walk `/home/user/docs/` recursively with `os.walk`, check each `.md` file's `os.path.getmtime()` against `time.time() - 600` (10 minutes), and only for files newer than that cutoff, read the existing content and rewrite the file with `<!-- REVIEWED -->\n` prepended — be careful not to touch files outside that recent window (like pre-existing stale docs), and don't accidentally re-stamp a file twice if your script logic reruns.