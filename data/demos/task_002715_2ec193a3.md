Split this into orchestration and algorithm work, and do the algorithm work first since it's independently testable. For Phase 3, do NOT try to disassemble `/app/oracle/integrator_reference` — instead treat it as a black-box: generate many `{N_initial, growth_rate, max_steps, tolerance}` JSON inputs across the documented ranges, run the oracle on each, and inspect its output JSON structure and values to infer the step-size-adaptive integration algorithm (the description says the bug is in "step-size adaptation," so focus your candidate implementations on adaptive-step ODE/growth integrators, e.g. halving/doubling step size based on error tolerance vs a fixed-step method). Iterate: write a candidate `/home/user/fixed_integrator.py`, run it against the same inputs as the oracle, diff outputs field-by-field, and refine until bit-exact across your generated corpus, not just a few samples. For Phase 1, inspect each service's existing config/env files under `/app/api` and `/app/notebooks` to find the specific missing/wrong keys (`REDIS_URL`, `SIMULATION_API_URL`) rather than guessing values, start each service (`gunicorn`, redis-server, `jupyter notebook`) in the background, and confirm end-to-end connectivity with a manual curl to `http://127.0.0.1:8000/simulate` before writing Phase 2's KS-test/Wasserstein comparison script and divergence report.
