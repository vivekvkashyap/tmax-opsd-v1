OCR `/app/ci_architecture.png` with `tesseract` and carefully read the extracted text for the line formatted like `CI_SECRET_SEED: <value>` — use exactly what OCR recovers, don't fabricate a plausible-looking seed. Compile the shared library with `gcc -shared -fPIC -o /app/libencoder.so /app/encoder.c`. In the Python server, load it with `ctypes.CDLL("/app/libencoder.so")`, and critically set `lib.encode_payload.restype = ctypes.c_char_p` (and argtypes to `c_char_p`) before calling it — otherwise ctypes will misinterpret the returned pointer/truncate the string. Encode Python strings to bytes (`.encode('utf-8')`) before passing to ctypes and decode the returned bytes back to str for the JSON response. Bind the server strictly to `127.0.0.1:8080`, implement the GET route reading `payload`/`platform` query params, check the `Authorization: Bearer mobile-ci-token-2024` header exactly and return 401 if missing/wrong, then call the C function with the payload, platform, and the OCR'd seed, returning `{"status":"success","result": <decoded>}`.
