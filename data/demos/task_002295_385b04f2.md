Tackle the three bugs separately and verify each in isolation before moving on. First, run `build.sh` to see the failure: look for a loop like `for file in $(ls src/*.cpp)` — unquoted command substitution and globbing breaks on filenames containing spaces (a source file has one). Fix it to iterate safely, e.g. `for file in src/*.cpp; do g++ -c "$file" ...; done`, quoting every expansion of `$file`. Second, grep the source for `std::thread` worker code touching a shared accumulator (e.g. `global_total += ...`) with no synchronization — add a `std::mutex` and lock around the update (or use an atomic/per-thread partial sums merged at the end) to eliminate the race and get deterministic totals. Third, find where a `TradeRecord*` (or similar) is heap-allocated per line inside the worker loop and never freed — either add the matching `delete`, or better, avoid the per-line heap allocation entirely (stack-allocate or reuse a buffer) to keep peak RSS low under a large synthetic dataset. After fixing, rebuild and copy the binary to the exact required output path, then sanity-test against `/home/user/mock_data/` including filenames with spaces.
