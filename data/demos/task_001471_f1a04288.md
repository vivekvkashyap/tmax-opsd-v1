Split this into the two objectives and verify each in isolation. For Nginx: edit `/app/nginx/nginx.conf`'s `server` block to `listen 127.0.0.1:8443 ssl;`, set `ssl_certificate`/`ssl_certificate_key` to the server cert/key, set `ssl_client_certificate /app/certs/ca.crt;` with `ssl_verify_client on;` to enforce mTLS, add a `location /` that `proxy_pass`es to `http://127.0.0.1:8080`, and pass the verified client CN through with `proxy_set_header X-Client-CN $ssl_client_s_dn_cn;` (enable `ssl_verify_client optional_no_ca`/CN parsing extensions if your nginx build needs them). Test with `nginx -t -c /app/nginx/nginx.conf` and then a real `curl --cacert ca.crt --cert client.crt --key client.key https://127.0.0.1:8443/...` round trip before moving on. For the Go tool, use `net/http.ReadRequest(bufio.NewReader(os.Stdin))` to parse and `req.Write(os.Stdout)` to re-serialize — this guarantees byte-accurate HTTP/1.1 formatting instead of hand-building the request text. Parse the client cert with `encoding/pem` + `crypto/x509.ParseCertificate` for the CN, then apply the header/cookie/Host-prefix rules exactly as specified in the task, mutating `req.Header`, `req.Header.Get("Cookie")`/`req.Cookies()`, and `req.URL.Path` before writing.
