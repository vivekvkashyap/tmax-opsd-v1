This is a black-box reverse-engineering task, not a from-scratch design — use the oracle binary itself to characterize the transform. Generate a synthetic HDF5 file with a `raw_features` dataset (N x 50, e.g. correlated Gaussian data via a random projection so it has real covariance structure, not pure noise) and run `/app/oracle_processor input.h5 output.h5` on several such probe files, inspecting `clean_features` (N x 5) each time. Check that each output column lies in [0,1] and looks like a uniform marginal — this confirms a per-component ECDF/quantile mapping after a linear reduction. Fit the pipeline yourself on a large held-out sample: center and reduce with `sklearn.decomposition.PCA(n_components=5)` (or TruncatedSVD), then fit a `sklearn.preprocessing.QuantileTransformer(output_distribution='uniform', n_quantiles=...)` on the 5 projected components; verify empirically by comparing your pipeline's output to the oracle's output on a probe file (compute MSE yourself) and iterate on `n_quantiles`/PCA settings until the two agree closely. Persist the fitted PCA + QuantileTransformer with `joblib.dump` so `/home/user/fast_prepare.py` loads them at runtime instead of refitting, then reads `raw_features`/writes `clean_features` via `h5py`.
