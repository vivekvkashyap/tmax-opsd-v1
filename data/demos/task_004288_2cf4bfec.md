First recover the spec from the video: extract frames with `ffmpeg -i /app/backup_spec.mp4 frame_%03d.png` (or sample a few frames) and read the on-screen text (via OCR like `tesseract`, or just visually inspect a frame since it's static terminal text) to get the exact `BASE_MTIME` integer and the `EXCLUDE` glob patterns — do not guess these values, derive them from the actual frame content. Then implement `/home/user/backup_planner.cpp`: parse the JSON array (a small hand-rolled or header-only JSON parser is fine, no network deps since it must compile standalone), build a map from path to node plus a children index (any directory not referenced as another's child via prefix path is a root — infer parent/child from path prefixes, not an explicit parent field), and DFS children in alphabetical path order. Track the active DFS path stack as a set; when following a symlink's target directory, if the resolved target is already on the stack, skip it (cycle) — this is the key subtlety naive solutions miss, along with correctly excluding matched paths (fnmatch-style glob matching, plus prefix matching for directory-style excludes like `/var/log`) before emitting, and only emitting files (not dirs/symlinks) with `mtime > BASE_MTIME`. Print `logical_path,mtime` in DFS visitation order. Compile with a C++17 g++ invocation and test against a few hand-built stdin JSON fixtures including a self-referential and mutually-cyclic symlink pair before trusting it.