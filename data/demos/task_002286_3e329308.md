Two independent sub-problems — solve transcription first, in isolation, before touching the Go code: run `/opt/whisper/main -m /opt/whisper/models/ggml-base.en.bin -f /app/call_audio.wav` (check its `-h` for exact output-format flags), read the transcript text it produces, and pull the 5-digit number out of the phrase "My customer ID is ..." with a simple regex (`\d{5}`) — verify the extracted ID looks sane before hardcoding it into anything. For the Go rewrite, replace the nested-loop join with two hash maps built in a single pass each: `agent_id -> {department, rating}` from `agents.csv`, and while scanning `tickets.csv` filter to rows matching the given `customer_id`, look up each ticket's agent in the agent map, and accumulate `total_duration` and a running (sum, count) for rating per `department` in a `map[string]*Aggregate` — this is O(N) instead of O(N^2). After the scan, compute `avg_rating` per department and marshal to the exact JSON shape shown (customer_id as a string, nested `departments` object). Compile to `/home/user/analyzer`, run it with the ID extracted from the transcript, and inspect `/home/user/result.json` before assuming it's correct.
