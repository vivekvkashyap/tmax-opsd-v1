First recover the business logic before writing any code: transcribe `/app/architect_notes.wav` with a real ASR tool (install and use `openai-whisper` or Python `speech_recognition` with `ffmpeg` for format conversion) rather than guessing. Listen carefully — the architect spells some tokens out letter by letter (e.g. interface prefixes, exact interface names), so transcribe slowly and cross-check ambiguous words; re-run transcription on a trimmed/normalized WAV if the first pass is garbled. Write down the rules as an ordered, numbered list exactly as spoken — order is critical, since the architect says "follow these rules exactly in order," implying a first-match-wins structure, not independent conditions. Implement that as a sequential `if/elif/.../else` chain in the bash script, evaluating rules in the transcribed order and falling through to a default only when nothing else matches. Separately handle robustness: if `$#` isn't exactly 3, print `ERROR` and exit before touching the rule logic; also decide what to output when the status argument isn't one of the valid values you'd expect, since the fuzz harness will pass malformed/unexpected status strings. Use integer-safe comparisons (`-eq`, `-gt`) for the latency argument, and `chmod +x` the final script.
