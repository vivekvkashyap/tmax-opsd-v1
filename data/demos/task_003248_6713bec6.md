Structure the C++ program in clear phases: stream-filter, reshape, then compute. Read `/home/user/config_stream.csv` line by line with `std::ifstream`/`getline` (don't load the whole file into memory at once since it's meant to be large) and only retain rows whose Timestamp field equals the target value `1700000000`, parsing each line by splitting on commas. For the retained rows, build a `std::map<std::string, std::set<std::string>>` mapping ServerID to a set of "ConfigKey=ConfigValue" strings — using a set naturally dedupes and enables the Jaccard computation. For each server other than server_001, compute intersection size (count of common ConfigKey=Value strings, via std::set_intersection or manual iteration) and union size (|A|+|B|-|intersection|), then Jaccard = intersection/union as a double. Track the best score seen so far, and on ties prefer the lexicographically smaller ServerID (compare candidate to current best with `<` only when strictly greater, so earlier-seen or smaller IDs win ties depending on iteration order — explicitly compare ServerID strings when scores are equal rather than relying on iteration order). Format the output score to exactly 4 decimal places (e.g. via `std::fixed << std::setprecision(4)`) and write `ServerID,SimilarityScore` to `/home/user/closest_server.txt`.