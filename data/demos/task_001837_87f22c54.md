Scaffold with `cargo new /home/user/doc_converter`, add `serde` and `serde_json` to Cargo.toml. In main.rs, read all of stdin into a String, then split it on the `==DOC==` delimiter (skip any empty leading chunk) and for each chunk parse the `Title:` and `Author:` lines, then locate the `Body:` marker and take everything after its line up to the end of that chunk as the body — strip exactly the newline right after `Body:` and trim trailing newlines before the next delimiter, but preserve internal newlines within the body. Collect these into a `Vec` of a struct with `title`/`author`/`body` fields (serde Serialize), serialize to JSON with `serde_json::to_string_pretty` or similar. For the atomic-write requirement: write the serialized bytes to `/home/user/docs_final.json.tmp` with `std::fs::write`, then call `std::fs::rename("/home/user/docs_final.json.tmp", "/home/user/docs_final.json")` — this is what the verifier checks the source for. Build with `cargo build --release`, then run it as `cat /home/user/raw_docs.txt | ./target/release/doc_converter` and inspect the resulting JSON for correctness.
