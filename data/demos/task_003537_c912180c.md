First extract the PIN from the audio: install a transcription tool (`whisper`, or `ffmpeg` plus a speech-to-text CLI) and run it against `/app/voicemail.wav`, then read the spoken digits off the transcript rather than guessing — the PIN is whatever number words the recording actually says. Next compile and run the diagnostic server as-is, then reproduce the crash exactly as described: `POST /auth` with the PIN in the body, then `GET /diagnostics`, and confirm it segfaults. Use `gdb -c core` (enable core dumps first with `ulimit -c unlimited`) or `strace`/`valgrind` to pinpoint the fault — inspect the `/diagnostics` handler code path specifically, looking for any fixed-size stack buffer being filled from a longer source string without a bounds check (a classic `strcpy`-into-small-buffer overflow). Fix it by sizing the buffer correctly or switching to `std::string`/`strncpy` with an explicit length limit, without deleting or renaming the `/auth` or `/diagnostics` routes. Recompile to `/home/user/diag_server`, restart it in the background on `0.0.0.0:9090`, and re-run the same auth-then-diagnostics sequence to confirm both endpoints now return 200 without crashing.
