Extract the outer archive first: `tar -xzf /home/user/archive.tar.gz -C /home/user/extracted/`. For each nested `.tar` file, verify integrity before trusting it — do NOT just try/extract and swallow all errors blindly; use `tar -tf <file> >/dev/null 2>&1` (or `tar -tvf`) and check the exit code to decide whether an archive is valid; a truncated/corrupted tar will make this command fail non-zero. Skip any archive that fails this check entirely — do not attempt partial extraction of a corrupted member. For each archive that passes, extract its `.log` files with `tar -xf <file> -C <some dir>`. Then process all the log files together: use `grep -v '\[DEBUG\]'` (or an equivalent filter) on each valid log's lines to drop debug lines, concatenate the surviving lines from all valid logs, and pipe the combined output through `sort` to get lexicographic ordering, writing the result to `/home/user/cleaned_logs.txt`. Double-check you're only including logs from tars that passed the integrity check, and that sorting is plain lexicographic (not numeric) using the default `sort` behavior.
