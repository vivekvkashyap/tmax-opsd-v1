This task has two independent parts. For the audio part: transcribe `/app/urgent_alert.wav` (use `ffmpeg`/`whisper`/speech-to-text tooling, installing what you need) to recover the spoken mailing-list name, convert it to the lowercase-hyphenated form implied by the example in the prompt, `mkdir -p` the corresponding `/home/user/mail/<name>` directory, then `ln -s` it as `/home/user/target_list` — do not guess the name from context, actually listen/transcribe it. For the classifier: don't write a generic "spam filter" from prior knowledge — inspect the actual samples in `/app/corpus/clean/` and `/app/corpus/evil/` directly (grep for common substrings/headers, diff structural patterns) and derive the precise discriminating rule(s) from what's really there, since the held-out test set is drawn from the same distribution and a mismatched heuristic will fail edge cases. Make `/home/user/alert_filter.py` take a filepath argv, read it, and print exactly `ACCEPT` or `REJECT` (nothing else) to stdout; `chmod +x` it and verify it runs standalone via `python3`.