Sequence the work: first parse `sync.log`'s multi-line `[START UPLOAD]`/`File:`/`Expected-Size:`/`[END UPLOAD]` blocks into a list of (filename, expected_size) records — a simple state machine or regex over the whole file works better than line-by-line parsing since records span multiple lines. For each file, verify it's a valid gzip+tar archive before chunking — actually attempt to open/read it (e.g. `tar tzf` or Rust's tar/flate2 crates) rather than trusting the extension, and skip anything that fails to parse, producing no chunks and no symlink for it. Chunk valid archives into exact 512,000-byte pieces (last chunk shorter is fine) named `<filename>.chunk.NNN` with zero-padded 3-digit numbers, and create the `/home/user/data/active/<filename>` symlink pointing at the original archive path. For the HTTP layer, build the Rust server (e.g. actix-web/warp) to answer `/health` with 200 "System Operational" on port 9000, then configure Nginx separately: `location /api/ { proxy_pass http://127.0.0.1:9000/; }` (trailing slash strips the prefix) and `location /downloads/ { alias /home/user/data/active/; }`. Start both, curl-test each route yourself, then write `/home/user/finished.txt`.
