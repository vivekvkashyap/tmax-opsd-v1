Decompose into three independently verifiable stages before wiring the HTTP service. (1) Streaming frame ingestion: launch `ffmpeg -i /app/rack_monitor.mp4 -f rawvideo -pix_fmt gray -r 10 -` via `popen`/pipe into your C++ process (never write intermediate frames to disk), read fixed-size raw grayscale buffers (width*height bytes per frame) from the pipe in a loop, and compute average pixel intensity per frame; track frame index and divide by the known 10 FPS to get each frame's timestamp. (2) Anomaly detection: flag frames with mean intensity strictly > 128.0, converting frame index to `time_sec` with one-decimal rounding. (3) CSV join with nearest-neighbor interpolation: parse `/home/user/baseline_config.csv` into (int second, config_id) pairs, then for each anomaly's fractional time_sec pick the CSV row whose integer timestamp is closest (ties broken consistently, e.g. round-half-up) — this nearest-neighbor step is the easiest part to get wrong, so unit-test it against a few known fractional seconds before trusting the pipeline. Finally implement a minimal HTTP server (e.g. cpp-httplib) on 127.0.0.1:8080 serving `GET /anomalies`, checking the `Authorization: Bearer trace-mgr-77` header, and returning the anomalies as a JSON array in chronological order. Start it backgrounded and curl-test it before finishing.
