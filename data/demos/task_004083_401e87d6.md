This task has two independent parts. For the video: extract frames with `ffmpeg -i /app/audit_log.mp4 frame_%04d.png`, then check each frame's pixel color — since frames are solid colors, sampling one pixel per frame suffices (e.g. `convert frame.png -format "%[pixel:p{0,0}]" info:` or use ImageMagick's `identify -format "%[fx:mean.r],%[fx:mean.g],%[fx:mean.b]"`), and count how many frames are pure red (255,0,0) vs other colors — write that integer count to /home/user/red_frame_count.txt. For the validator: write policy_validator.cpp that (1) reads the first line, extracts the claimed SHA256 hex, computes SHA256 of everything after the first line's newline to EOF (you can shell out to `sha256sum` via `popen`, or implement/link a SHA256 routine) and compares case-sensitively; (2) finds the `CSP: ` line and rejects if it contains "unsafe-inline" or "unsafe-eval" as a substring; (3) finds `CERT_CHAIN: `, splits on commas, and checks the first entry is exactly "RootCA", the last is exactly "LeafCA", and no entry equals "RevokedCA". Return non-zero on any missing file, missing line, or malformed input. Compile with g++, then test it against every file in /app/corpus/clean/ (must all exit 0) and /app/corpus/evil/ (must all exit non-zero) before considering it done — a single false positive/negative fails the whole check.