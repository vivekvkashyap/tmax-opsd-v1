Structure audit.go around Go's filepath.Walk (or WalkDir) over /home/user/app: for each regular file, os.Lstat/os.Stat to get its os.FileMode, check the SUID bit via `mode&os.ModeSetuid != 0` (on Linux this corresponds to the 04000 octal bit — inspect `info.Sys().(*syscall.Stat_t).Mode` or use `mode.Perm()&04000` for the raw permission bits since Go's os.FileMode SUID constant maps to it), and check world-writable via `mode.Perm()&0002 != 0`. Collect matches into two string slices, sort.Strings() each before output since the spec requires alphabetical order, and default to empty (non-nil) slices so JSON marshals `[]` not `null`. For the CSP check, do a plain `http.Get("http://localhost:8080/")` and inspect `resp.Header.Get("Content-Security-Policy")` — treat both "header absent" and "present but empty string" as false, only a non-empty header value is true. Marshal the three fields into the exact JSON key names given (use struct tags or a map) and write to /home/user/report.json with `json.MarshalIndent` for readability, then compile with `go build` and run it once to produce the file.