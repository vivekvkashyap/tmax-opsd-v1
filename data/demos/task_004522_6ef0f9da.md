Fix and verify each of the three layers independently before moving to the next, since each failure masks the next. First, `python3 setup.py build_ext --inplace` and read the actual linker error — it's almost certainly undefined references to libm symbols (`sqrt`/`pow`), so add `libraries=['m']` to the `Extension(...)` call in `setup.py`; also check the C module's init function name matches the real CPython C-API symbol (`PyModule_Create`, not a similar-looking but wrong name) since a typo there fails to build/import too. Once it builds and imports cleanly, run `python3 main.py` against `telemetry.bin` and reproduce the segfault; the cause is `fastparse.c` copying `payload_len` bytes into a fixed 16-byte stack buffer without checking `payload_len <= 16` (and that `2 + payload_len <= data_len`) — add that bounds check and `PyErr_SetString(PyExc_ValueError, ...)` + `return NULL` instead of overflowing. Rebuild and rerun; catch the resulting `ValueError` in `main.py` per-record so one bad record doesn't crash the whole run. Finally, re-derive the metric formula literally from the spec `(velocity + wind_speed) * drag_coefficient / 2.0` — check operator precedence against what's currently coded, since a misplaced grouping is the actual bug, and confirm `results.txt` is written with the corrected per-record values.
