Build `runner.sh` as a strict pipeline of four discrete stages, and read each of the four lines of the payload file precisely (key, iv, base64 ciphertext, expected sha256) rather than assuming fixed offsets in one blob. Decrypt with `openssl enc -d -aes-256-cbc -K <key> -iv <iv> -a` (the `-a`/`-base64` flag handles the base64 ciphertext directly) to get the intermediate base64 text, then `base64 -d` that to recover the raw bash script bytes — remember there are two layers of encoding/encryption here, not one. Compute `sha256sum` on the decoded raw script and compare against line 4 exactly (byte-for-byte hex string, watch for trailing newline differences); on mismatch write exactly `POLICY_VIOLATION: CHECKSUM_MISMATCH` to `/home/user/execution.log` and `exit 1` immediately without executing anything. On match, execute the script with a fully scrubbed environment and a hard timeout using `env -i timeout 5 bash -c "$decoded_script"` (or write it to a temp file and run `env -i timeout 5 bash /tmp/script.sh`), redirecting its stdout into `/home/user/execution.log`. Make the script executable and actually run it once against the provided `/home/user/payload.dat` so the log file is populated before finishing.