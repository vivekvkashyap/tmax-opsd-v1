Sequence this as a pipeline and validate each stage's output before moving to the next. Run `/app/start_services.sh`, then `curl -O http://127.0.0.1:8080/backup_pool.tar`, extract it, and recursively extract every nested `.tar.gz` so all `.log` files land flat in `/home/user/raw_logs` — check with `find` that you actually captured every log rather than missing nested archives. Dump Redis with `redis-cli HGETALL user_map` (or `HSCAN`) and pipe through `awk`/`sed` to reshape the alternating key/value output into `UID,USERNAME` lines in `/home/user/user_dict.csv` — verify a few entries manually against direct `HGET` calls. For the C program, load the CSV dictionary into an in-memory hash/array keyed by UID first, then for each `.log` file `open()` + `fstat()` for size + `mmap()` (this is a hard requirement checked by grepping the source) and scan the mapped memory for lines, parsing `[TIMESTAMP] UID ACTION`. Write each record as exactly 21 bytes: 4-byte little/native-endian uint32 timestamp, 16-byte null-padded (or truncated-and-padded) username field, 1-byte action code per the given mapping, defaulting unknown UIDs to "UNKNOWN" and unknown actions to 0xFF. After compiling and running, confirm `archive.bin`'s size is exactly `(total log lines) * 21` by counting lines in `raw_logs` yourself.
