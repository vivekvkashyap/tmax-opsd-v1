Start the two databases first (`/app/start_services.sh`), then build and test your classifier iteratively against the provided clean/evil corpora rather than writing it blind. Implement each rejection rule as its own guarded check: (1) missing `paper_id`/`author_id`/`cites` → reject immediately; (2) if `author_id` isn't a plain string (e.g. it's a dict — a NoSQL-injection shape), reject before ever touching Mongo; otherwise run a MongoDB aggregation (`$match` + `$group`/`$avg`) over the `authors` collection to compute the mean `trust_score` for that author and reject if it's under 0.5; (3) for the graph check, use parameterized Cypher (`MATCH (t:Paper {paper_id: $id})`) to confirm every id in `cites` exists, and separately query whether any cited paper already has a `:CITES` edge back to this submission's `paper_id` (a 2-cycle) — reject if so. Never interpolate user JSON directly into a Mongo filter or Cypher string. Make the script print only `ACCEPT`/`REJECT` (no debug prints to stdout — send those to stderr if needed), then loop it over every file in `/app/corpora/clean/` and `/app/corpora/evil/` to confirm 100% correct classification before finishing.