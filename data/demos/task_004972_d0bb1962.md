Work the three phases in strict order and verify each before moving on. Phase 1: search every `cmdline` file under `/home/user/proc_dump/*/cmdline` (they're NUL-separated, so use `tr '\0' ' '` or `xargs -0` when printing) for an MD5-looking hex string embedded among the arguments. Phase 2: write a small Bash loop that reads each word from `/home/user/wordlist.txt`, computes `echo -n "$word" | md5sum`, and compares the hash to the one you found, stopping once you get a match — don't hardcode a guess. Phase 3: read `runner.sh`'s filter list closely — it blocks common read commands, the literal words "secret"/"document"/"flag", and the characters `\`, `'`, `"`. Bypass this using only Bash built-ins and shell globbing: a `read` builtin can read a file's line into a variable without invoking any external command name, and filesystem glob wildcards (`?`, `*`) let you spell forbidden filenames without typing the blocked substrings literally, while `$(...)`/`$var` and double-quote-free syntax avoid the banned characters. Pass the cracked password as arg 1 and your crafted payload as arg 2 to `/home/user/runner.sh`, capturing its output into `/home/user/solution.txt` so the flag content ends up there exactly.