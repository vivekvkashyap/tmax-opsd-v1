Handle the three sub-problems in order of dependency. First, OCR the rate limit: run `tesseract /app/rate_limit.png stdout` (or output to a file) and parse out just the integer following "Limit:", writing only that digit string (no extra text/newline issues) to `/app/rate_limit.txt` — inspect the raw OCR output first since tesseract can introduce stray characters you need to strip with a regex like `\d+`. Second, fix the build: run `make` in `/app/workspace/` to see the exact linker error, which typically manifests as "undefined reference" to C functions when a C++ translation unit includes a C header without `extern "C"` linkage — check `numalgo.h` for a missing `extern "C" { ... }` guard around the function declarations and add it (guarded by `#ifdef __cplusplus`) rather than renaming symbols. Third, after `make` succeeds, start the compiled server binary in the background (e.g. with `&` or `nohup`) and confirm with `ss -ltnp` or a quick gRPC health probe that it is listening on `0.0.0.0:50051` before considering the task done, since the automated verifier will make live gRPC calls including one designed to exceed the rate limit you wrote to `/app/rate_limit.txt`.
