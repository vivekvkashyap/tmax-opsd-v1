Build bottom-up so each layer is independently testable. First implement compute_weighted_checksum in checksum.c exactly as specified: iterate bytes with `total += (unsigned long long)data[i] * (i + 1)` (be careful of integer overflow/promotion—use unsigned long long throughout, and use `(i+1)` as a size_t or unsigned long long, not int, to avoid truncation on large inputs), compile with `gcc -shared -fPIC -o libchecksum.so checksum.c`, and sanity check the exported symbol with `nm -D libchecksum.so | grep compute_weighted_checksum`. Then build the venv and install flask/pytest/requests/hypothesis. In api.py, load the .so via `ctypes.CDLL('./libchecksum.so')`, set `.argtypes`/`.restype` correctly (POINTER(c_ubyte) or c_char_p, c_size_t, and c_ulonglong return type) so results aren't silently truncated to a 32-bit int, decode the incoming base64 payload with `base64.b64decode`, and return the checksum as a plain Python int in JSON. For test_api.py, use `@given(st.binary(min_size=0, max_size=1000))` from hypothesis, compute the expected checksum in pure Python using the identical formula, and assert the live API's response matches. Start the Flask app in the background (e.g. nohup or subprocess.Popen) before running pytest, and redirect pytest -v output to test_results.log.
