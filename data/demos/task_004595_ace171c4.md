Work bottom-up: get the backend running standalone first, then wire nginx to it, then automate both in scripts. Edit nginx.conf's `upstream backend` block, changing only the `server unix:...` line from the legacy socket path to `/home/user/app.sock` — leave every other directive untouched. Write manage_service.sh with a case statement on `$1`: `start` should `export APP_SOCK=/home/user/app.sock` then launch backend.py with `nohup ... &`, capturing `$!` into app.pid; `stop` should read that pid, `kill` it, wait for it to exit, then remove both the pid file and the socket file; `restart` simply calls stop then start (add a short sleep between them so the old process and socket are fully cleared before restart). Test manage_service.sh directly first — confirm the socket file appears after start and disappears after stop — before adding nginx into the mix. Then write deploy.sh to call manage_service.sh restart, start or reload nginx with the given `-p`/`-c` flags (detect if it's already running via its pid file or `pgrep`), sleep 2s, then curl the health endpoint and append the response to migration.log. Run deploy.sh yourself and inspect migration.log to confirm the full chain actually works end to end.