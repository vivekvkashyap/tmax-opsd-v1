Run `tesseract /app/release_tag.png stdout` (upscale/preprocess with ImageMagick first if OCR is noisy) and parse the two lines with a regex matching the literal `VERSION:` and `TOKEN:` prefixes — do not hardcode any guessed value, extract both programmatically from the OCR text. Write a small C helper that opens `/app/payload.bin`, reads it byte-by-byte (or via a buffer), and XORs an accumulator starting at 0 across every byte to get the 8-bit checksum, then prints it as a decimal integer. For the HTTP gate, write raw C using socket()/bind()/listen()/accept() in an infinite loop (never exit after one client) so it keeps serving; parse the request line and headers manually to find the `Authorization: Bearer <token>` header, compare against the OCR'd token, and return a well-formed `HTTP/1.1 401 Unauthorized` or a `200 OK` with `Content-Type: application/json` and a `Content-Length` header plus the JSON body embedding the OCR'd version and computed checksum. Compile, launch it backgrounded (e.g. with `&` and `disown`), and verify with `curl -v` against both the missing-token and correct-token cases before considering it done.
