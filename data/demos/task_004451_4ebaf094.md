Build this bottom-up: create the directory tree and files first, then get each script working standalone before wiring supervisord. Create configs/, logs/, bin/ under capacity_planner, write prod.json with the exact given content, then symlink active_config to the absolute path of prod.json with `ln -s`. Write simulated_metrics.csv verbatim. For mock_smtp.py, use Python's `smtpd`/`aiosmtpd` (or a minimal raw socket SMTP handler) listening on 127.0.0.1:8025; on each received message, strip MIME/SMTP headers and append only the body payload as one line to logs/emails.log — test it standalone first by sending a manual test email via smtplib before trusting it inside supervisord. For monitor.py, resolve the config through the symlink (open the symlink path directly — Python's open() follows symlinks transparently), read the CSV skipping the header, and for each row where cpu_usage>cpu_max or mem_usage>mem_max, connect via smtplib to 127.0.0.1:8025 and send a message whose body is exactly `ALERT: timestamp=..., cpu=..., mem=...`; sleep 1s between rows and exit after the last row. Write supervisord.conf with `[program:smtp]` and `[program:monitor]` sections pointing stdout/stderr logfiles into logs/, matching autorestart flags per spec. Install supervisor via pip, launch it, wait several seconds past the CSV's total runtime, then inspect emails.log to confirm exactly the alerting rows appear with correctly formatted bodies.