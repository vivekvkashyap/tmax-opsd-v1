Build this as three independently-testable pieces before wiring them together. (1) `netmon`: std-only Rust, `TcpListener::bind("127.0.0.1:8181")`, loop accepting connections, read enough of the request to see it's a GET, then attempt `TcpStream::connect("127.0.0.1:9090")` with a short timeout — on success write the literal 200/UP response bytes, on error write the 503/DOWN bytes, then close the connection. Test this binary standalone (with and without something listening on 9090) before touching git. (2) The post-receive hook: write it as an ordinary bash script reading refs from stdin (even if unused), doing `df -k /home/user` and parsing the available-KB column, then `GIT_WORK_TREE=/home/user/deploy git checkout -f`, `cd` there and `cargo build --release`, `pkill -f netmon` (ignore failure), then `nohup ./target/release/netmon >/home/user/netmon.log 2>&1 &` with `disown` so it survives the hook process exiting. Make it executable. (3) Init the git repos, commit, add the remote, push, then start the port-9090 dummy backend and curl to capture final_status.txt.
