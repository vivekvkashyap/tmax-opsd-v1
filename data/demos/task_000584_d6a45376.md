Start by isolating the vendored bug: grep the `peewee` source at `/app/peewee` for the `JOIN` class/enum definitions and compare each string constant against upstream peewee's known values (`INNER`, `LEFT OUTER`, `RIGHT OUTER`, `FULL`, `CROSS`) — a single mutated string is the classic perturbation pattern, so look carefully for typos like a doubled letter. Fix only that constant, then sanity-check by running any `.join()` query and confirming it compiles to valid SQL against SQLite instead of raising an `OperationalError`. Next, inspect the schema of `/home/user/backup_metrics.db` with `sqlite3 .schema` to confirm the `servers`/`backups`/`restores` relationships before writing the query logic. Build the script's SQL using parameterized peewee query expressions (never string-format the datacenter or size into SQL) so it is injection-safe. Compute the four output columns per spec: latest status by max timestamp, sum of SUCCESS sizes (default 0), and restore success rate as successes/total*100 formatted to 2 decimals, or `N/A` if zero restores. Test against edge cases: datacenters with no matching servers, servers with zero restores, and unusual strings like SQL-like text passed as the datacenter argument, since the script must treat it as pure data.
