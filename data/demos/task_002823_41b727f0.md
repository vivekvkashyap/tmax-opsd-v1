Fix the build first: inspect the Makefile in /home/user/native_module and notice the final link rule compiles processor.o into libprocessor.so without the `-shared` flag (and likely without `-fPIC` propagated to the link step) — add `-shared` (and keep `-fPIC` on the compile step) so `make` actually produces a loadable shared object, then verify with `file libprocessor.so`. For the Python FFI wrapper, the critical trap called out in the task is `ctypes.c_char_p` as a restype: it auto-converts the C pointer into an immutable Python bytes object and discards the original pointer value, so you can never pass it back to `free_string` — instead set `lib.process_string.restype = ctypes.c_void_p`, capture the raw pointer, convert to a Python string via `ctypes.cast(ptr, ctypes.c_char_p).value.decode()` for your own use, but retain the original `c_void_p` pointer object to pass unchanged into `lib.free_string(ptr)` afterward. Process each line from /home/user/inputs.txt this way, write the normalized strings to /home/user/outputs.txt, then call `get_active_allocations()` last and append its integer result as the final line — it should read 0 if every allocation was freed correctly.
