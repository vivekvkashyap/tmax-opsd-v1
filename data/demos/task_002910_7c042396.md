Write the audit script to follow the described procedure exactly and step by step, since the verifier recomputes the same pipeline and diffs the JSON. For each run_XX.csv, load with pandas and check `df["text_token_id"].isna().any()` — this is how you distinguish corrupted (float column, contains NaN) from clean (integer column, no NaN) rather than checking the pandas dtype directly, since dtype alone can be ambiguous. Concatenate clean runs into one DataFrame and corrupted runs into another, then use `.corr()` (Pearson, pandas default) between `true_label` and `predicted_prob` for each combined set. For the bootstrap: call `numpy.random.seed(42)` exactly once right before the loop of 1000 iterations, sample indices with `numpy.random.choice(n, n, replace=True)` on the clean-combined dataset each iteration, compute the correlation on that resampled subset, collect all 1000 values, then take `numpy.percentile` at 2.5 and 97.5. Round every float to 5 decimal places and sort `corrupted_runs` alphabetically before writing the JSON — match key names and structure exactly, since the check is an exact-equality diff against a reference computation.