Transcribe `/app/pipeline_specs.wav` yourself before writing any code — e.g. `pip install openai-whisper` then `whisper /app/pipeline_specs.wav --model base --language English`, or use `ffmpeg` to convert/inspect it first if the format needs normalizing. Read the transcript carefully for each discrete step in order: what input byte encoding to decode with (and what to do about invalid bytes), what case-normalization to apply, what delimiter to split on, what validation/filtering rule drops empty tokens, what substring-based redaction rule replaces certain tokens with a placeholder word, and what output encoding to use when joining and writing. Implement these steps in `/home/user/processor.py` reading raw bytes from `sys.stdin.buffer` and writing raw bytes to `sys.stdout.buffer`, keeping the exact order of operations mentioned in the audio since reordering (e.g. redacting before splitting, or lowercasing after redaction-check) changes results on edge cases. Before trusting your implementation, generate random byte strings (biased toward the audio's example patterns, e.g. delimiter characters and case variants of flagged words) and diff your script's output against `/app/oracle_processor`'s output on the same input, iterating until they match exactly, including on empty input and inputs with no delimiters.
