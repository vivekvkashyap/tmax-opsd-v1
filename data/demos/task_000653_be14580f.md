Build and verify each pipeline stage independently before wiring them together. For the tar extraction in C, parse the tar header block-by-block (512-byte headers; name field is the first 100 bytes, null-terminated) rather than shelling out to `tar` blindly, since you must reject any entry whose name contains the substring `../` (classic tar-slip defense) and only extract the rest into `/home/user/extracted/`; create that directory first. For GCode parsing, scan lines starting with `G1`, and on each such line extract the numeric value following an `E` token (careful: split on whitespace and match tokens starting with `E`, not just any `E` substring) and accumulate a running float total. For the log, don't just grab the first `Status:` — scan the whole file and keep overwriting your "final status" with the value on the line immediately following each `Status:` occurrence, so the last one wins. For the video, use `ffmpeg` to extract frames (e.g. `-vf select` to pull every frame, or extract all with `-r <fps>`), convert each to grayscale, compute mean pixel intensity per frame, and count frames where the mean exceeds 200. Write `result.json` to `/home/user/web/` with the three computed fields, then serve that directory with a simple HTTP server (Python's `http.server` is fine) bound to 127.0.0.1:8080 in the background, and confirm with `curl http://127.0.0.1:8080/result.json` before finishing.