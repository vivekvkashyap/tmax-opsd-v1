Use a proper CSV parser (Python's `csv` module or `pandas.read_csv`) rather than splitting on raw newlines — a correct CSV parser will already treat embedded `\n`/`\r\n` inside quoted fields as part of that field, so a row with a multiline quoted "notes" value will parse as one logical row (not multiple). Detect "invalid" rows by checking each field's raw string value for `\n` or `\r` characters after parsing, and drop those rows entirely (don't try to repair them) — count how many are dropped. For normalization, detect the timestamp format per row (`YYYY/MM/DD` vs `MM-DD-YYYY`) by checking separator and position (e.g. try `datetime.strptime` with `%Y/%m/%d` first, fall back to `%m-%d-%Y`) and reformat to `YYYY-MM-DD`. Melt the three metric columns into long format (one row per metric per original row), producing `date,host,metric_name,metric_value`. Sort by date, then host, then metric_name (alphabetical, so `cpu_load` < `disk_io` < `memory_usage`). Write the CSV with exact header and write `/home/user/output/summary.txt` using the exact literal template with the dropped-row and valid-record counts substituted in — create `/home/user/output` first if missing. Verify your script actually counts distinct raw dropped rows, not dropped metric records.