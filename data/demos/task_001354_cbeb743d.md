Write `extractor.c` around the exact binary layout: a 16-byte null-padded filename field (it may not be null-terminated if the name fills all 16 bytes, so bound all comparisons/copies to 16 bytes), then a little-endian 32-bit unsigned size, then that many bytes of data, repeated back-to-back. `open()` the archive, `fstat` for its size, `mmap` it read-only, then walk an offset through the mapping: read the 16-byte name and 4-byte size (memcpy into a `uint32_t`), advance past the header, and if the name ends in `.md` write those `size` bytes to a new file in `/home/user/docs_extracted/`; advance the offset by `size` to reach the next entry, stopping at the end of the mapped region. After extraction, build the tarball with `tar -czf /home/user/markdown_archive.tar.gz -C /home/user/docs_extracted .` so it contains flat filenames with no leading directory components — verify with `tar -tzf` before finishing.