Process each `.log` file in `/home/user/logs/` independently: grep for lines matching the literal pattern `[ERROR] Connection to <target_service> failed:`, extracting the target service name with a regex/awk capture (e.g. `sed -E 's/.*Connection to ([^ ]+) failed:.*/\1/'`). Only create a symlink in `/home/user/alerts/active/` (via `ln -s`, using an absolute path to the source log so it resolves correctly) for files that had at least one match — skip files with none. The source service name is just the log's basename minus `.log`. Collect all (source, target) pairs across files, dedupe identical pairs (a file can have repeated errors to the same target — only emit one ALERT line per unique pair), then sort the `ALERT: <source> cannot reach <target>` lines alphabetically by source service before writing them under the fixed `To:`/`Subject:`/blank-line header into `/home/user/spool/mail/network_alerts.txt` — match the header format and blank-line spacing exactly. Finally `mkdir -p` the spool directory first, then `chmod 600` the resulting file so only the owner can read/write it.