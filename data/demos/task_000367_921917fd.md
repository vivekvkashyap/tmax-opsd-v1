Diagnose the vendored package build failure methodically before touching sanitizer logic: run `make build` in `/app/vendored/py-fast-log-parser` to see the actual error, inspect the `Makefile` for tab-vs-space indentation issues (Make requires literal tabs for recipe lines — an editor or `cat -A` will reveal spaces masquerading as tabs) and for any environment-variable gate in the recipe (e.g. checking `BUILD_ENV`) that needs the correct value exported before building. Fix the Makefile, export the correct env var, `make build`, then `pip install -e .` so `fast_log_parser` becomes importable. Only then write `/home/user/log_sanitizer/sanitizer.py`: iterate `.log` files in the input dir, parse each line as JSON (skip/guard against malformed lines so nothing crashes), and implement the safety checks as a small state machine — scan the URL for traversal sequences (both literal `../` and percent-encoded variants like `%2e%2e%2f`, case-insensitively), scan all parameter values for SQLi markers (`' OR '1'='1`, `UNION SELECT`, `--`), and scan URL/params for raw control-byte characters (0x00-0x1F). If a line is evil, drop the whole file's line; write the surviving lines to the same filename in the output dir. Test against `/app/corpora/clean/` and `/app/corpora/evil/` and iterate until clean files pass through untouched and evil files become empty.
