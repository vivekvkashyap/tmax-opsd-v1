Write organizer.sh as a line-by-line state machine: read config.org with a `while read -r cmd arg` loop (or `while IFS= read -r line`), split each line into command and argument, and maintain shell variables for the current TARGET dir and PARSE_DIR results across iterations. For SET_TARGET, `mkdir -p` the given path and remember it. For PARSE_DIR, scan `*.proto` files in the directory and, for each, extract dependencies by grepping for lines matching `import "....proto";` (e.g. `grep -oP 'import "\K[^"]+(?=\.proto")'` or sed) — build an associative structure (bash 4+ associative array, or parallel files) mapping each file to its dependency list. Implement topological sort yourself (Kahn's algorithm: repeatedly find files whose remaining unprocessed dependency count is zero, and among ties pick alphabetically smallest via `sort`), since this ordering is not something `tsort` gives you consistent alphabetical tie-breaking for by default — verify tie-breaking manually against a small example. For EMIT_PLAN, write the topological order (basenames only) one per line, then copy each source file into the SET_TARGET directory with a new first line `// ORDER: N` prepended (N = its 1-based position in the topological order) using something like `{ echo "// ORDER: $N"; cat file; } > dest`. Make the script executable and run it against config.org at the end.