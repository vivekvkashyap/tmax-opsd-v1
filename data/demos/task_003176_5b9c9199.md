Solve this as a pure shell/jq pipeline without Python. First flatten nodes into a lookup table of `node_id -> sorted/joined authors` using `jq -r '[.node_id, (.authors|join(","))] | @tsv' dataset/nodes.jsonl`, and similarly extract `src,dst` pairs from edges.jsonl with `jq -r '[.src,.dst]|@tsv'`. Then for each edge, look up both authors lists (e.g. via a bash associative array populated from the nodes TSV, or two `join` passes on sorted files keyed by node_id) and test whether the two author lists share at least one element — a clean way is to explode each side's authors onto separate lines tagged with the edge id and use `sort`/`uniq -d` or `comm`/`grep -f` per edge to detect any overlap. Iterate edges in a loop (or vectorize with joins), incrementing a counter each time the intersection is non-empty. Write only the final integer count, no extra text or newline issues, to `/home/user/self_cite_count.txt` (e.g. `printf '%d' "$count" > ...` or ensure no stray whitespace so a strict string-equality check on the file passes). Test your pipeline step by step (print intermediate joins) before trusting the final count.