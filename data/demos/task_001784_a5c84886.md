First transcribe `/app/rules.wav` accurately (play/inspect it with an audio tool or speech-to-text) to get the exact naming rule sequence — do not guess a plausible-sounding rule, since the fuzz verifier will test edge cases. Implement `/home/user/artifact_namer` as a small script/binary (any language, but make it directly executable, e.g. a shebang + chmod +x, or compile it) that takes argv[1] and applies the transformation steps in the precise order stated in the audio: strip directory components first, then handle the extension/period logic exactly as described (watch off-by-one errors like splitting on the first vs. last period), then case-fold, then character substitutions, then append the fixed suffix. Test it by hand against a handful of tricky inputs (empty string, no extension, multiple periods, leading/trailing spaces, mixed separators) before trusting it. Only after the namer is solid, extract `/home/user/artifacts.tar.gz` recursively (it contains nested/multi-part archives — keep unpacking until you hit raw files), flatten by ignoring the directory structure, rename each extracted file by piping its original name through your namer, and move the results into `/home/user/curated_binaries/`.
