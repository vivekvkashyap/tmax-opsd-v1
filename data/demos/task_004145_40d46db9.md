Build this in the given order since later pieces depend on earlier ones. First create `/home/user/secure_zone` and set its default ACL mask with `setfacl -d -m mask::r-x /home/user/secure_zone` (default ACLs only affect files created afterward, so create the directory before writing test files into it) — verify with `getfacl`. Second, write the inotify C monitor: use `inotify_init1`, `inotify_add_watch(fd, path, IN_CREATE)`, read from the fd into a buffer sized for several `struct inotify_event` plus name, iterate events checking `mask & IN_CREATE`, print `ALERT: New file %s detected\n` with the event's `name` field, and call `fflush(stdout)` after every print (this is the naive-attempt trap: without it stdout is fully buffered when not a tty and nothing appears in the log for a long time). Compile it, then launch it backgrounded with output redirected: `./fs_monitor >> /home/user/events.log 2>&1 &` (or `nohup ... &`), and confirm it's still alive with `ps`/`jobs` before finishing. Third, write `process_alerts.sh` using `grep`/`awk`/`sed` to pull just the filename token out of each alert line into `processed_files.txt`, and `chmod +x` it. Finally write `start_tunnel.sh` containing the exact `ssh -f -N -L 8888:127.0.0.1:9999 monitor-server` form and make it executable.
