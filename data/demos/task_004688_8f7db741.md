Build this pipeline incrementally and verify each artifact before moving to the next, since floating-point and grid-indexing bugs are easy to introduce in bash. Use nested loops (e.g. `seq 0.0 0.5 5.0` twice, or an integer counter with `bc`/`awk` for the actual X/Y values to avoid shell float-loop drift) and call `/home/user/sim.sh "$x" "$y"` for each of the 121 combinations, capturing Z with command substitution. Track the running maximum in shell variables (compare floats via `awk` or `bc -l`, not `[ ]` string comparison), and write the winner to `/home/user/best_params.txt` in exactly the `X=<x>, Y=<y>, Z=<z>` format. For `grid_data.txt`, be careful about axis orientation: rows must be ordered by increasing Y top-to-bottom and columns by increasing X left-to-right — build it as an 11x11 array (e.g. an awk associative array indexed by row/col) and print row-by-row with `awk`'s `printf` after the full grid is populated, don't stream values as you compute them. Verify the exact expected peak value and corner value against `sim.sh` manually before trusting your grid. Finally write `/home/user/plot.gp` with `set terminal png`, `set output '/home/user/heatmap.png'`, and a `plot ... matrix with image` (or `splot`) command reading `grid_data.txt`, then invoke `gnuplot plot.gp` as the pipeline's last step and confirm the PNG is produced.
