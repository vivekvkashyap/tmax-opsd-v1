Build and test the Rust converter in isolation before wiring it into the pipeline. Write `/home/user/converter.rs` to read stdin line by line, match each line against the pattern `[LEVEL] YYYY-MM-DD message...` (e.g. with a regex or manual split on the first two whitespace-delimited tokens after stripping brackets), emit one JSON object per matching line with keys level/date/message, and silently skip non-matching lines (don't panic or error out) — test it manually with `echo "[ERROR] 2023-10-12 Database connection failed" | ./converter` before running it over real files. Compile with `rustc /home/user/converter.rs -o /home/user/converter`. Then for each `.log.gz` in `/home/user/legacy_logs/`, pipe it through: `zcat file.log.gz | ./converter | gzip > /home/user/processed_logs/<new_name>_archive.json.gz`, deriving `<new_name>` by parsing the old app name out of the filename (`app_<old_name>_archive.log.gz`) and looking it up in `/home/user/service_mapping.conf` (a simple `old=new` per line, parseable with awk/grep or a shell associative array). Create `/home/user/processed_logs/` first if it doesn't exist. Only after all files are successfully converted and verified (e.g. `zcat` + `jq` sanity check) should you `rm -rf /home/user/legacy_logs/`.