First parse `/home/user/server/access.log` (likely with `grep`/regex or a quick script) to extract, for each `GET /login` line, the `redirect` and `token` query parameters; flag a token as leaked only when `redirect` is present and its value's domain is neither `http://internal.corp` nor `http://localhost` (mind the `localhost:8080` variant — match the scheme+host, not just prefix substring). Cross-reference flagged tokens against `/home/user/server/users.json` to get the leaked usernames. In your Rust project (`cargo new /home/user/rotator`), use a SHA-256 crate (e.g. `sha2`) to compute each leaked user's new token as `hex(sha256("ROTATED_<username>"))`, leaving non-leaked users' tokens untouched. Build the output as a `Vec` of users sorted by `username`, serialize with keys ordered alphabetically (`token` before `username`) into compact/minified JSON — this usually means manually formatting strings rather than relying on default serde map ordering, since struct field order in serde_json follows declaration order, so declare `token` before `username` in your struct. Write the result to `/home/user/server/users_rotated.json`, `chmod 600` it, then compute its SHA-256 and write only the hex digest (no trailing newline, no filename) to the `.sha256` file — verify with `sha256sum` that the file's actual hash matches what you wrote.
