Build this as a straightforward streaming/grouping pipeline in Go, but get the ordering and rolling-window semantics exactly right since those are where naive implementations diverge. Read the JSONL line by line, apply the three rejection rules first (regex-validate `lang` as exactly two lowercase letters, reject empty `new`, reject `old == new`) and count rejects as you go. For valid records, group into a map keyed by `lang+"|"+mod`, storing a slice per group; after all records are read, sort each group's slice by `ts` ascending with `id` as the tie-breaker (Go's `sort.Slice` with a two-field comparator). Then replay each group in that sorted order, maintaining a sliding window of the last up to 5 `R` values (a small ring buffer or slice you append to and truncate from the front) and recomputing the average on each step — do this only after sorting, not during the initial read, since input order need not match chronological order. Use `len([]byte(s))` for byte length, not `len([]rune(s))`. Flag ids where the average strictly exceeds 2.500, dedupe and sort them alphabetically, then write both output files with `go run` and inspect them directly afterward.