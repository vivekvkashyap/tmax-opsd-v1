This is a deterministic simulation, so get the exact update semantics right rather than approximating. In heat_sim.c, allocate two 50x50 double arrays (current and next) and update only interior cells (i,j from 1 to 48) using the given 5-point stencil with alpha=0.1, applied to a copy each iteration so you never read partially-updated values from the same step (use two buffers and swap, or write to a fresh next array each time) — updating in place is the classic bug that corrupts the diffusion. Initialize the 20..29 x 20..29 block to 100.0 and everything else (including boundary rows/cols 0 and 49) to 0.0, and re-clamp boundaries to 0.0 after every iteration since they must stay fixed. Run exactly 100 iterations. For binning, compute `bin = min(int(value // 10), 9)` handling the value==100.0 edge case explicitly so it lands in bin 9, and write "Bin X: Y" for X=0..9. Compile with `gcc -O3 -o /home/user/heat_sim /home/user/heat_sim.c`. Write evaluate.sh to run the binary, then diff the two bin count files element-by-element summing absolute differences into a single integer written to error_metric.txt; make it executable and test with `./evaluate.sh` — if temp_dist.txt doesn't match reference_profile.txt exactly, the bug is almost always in the stencil update ordering or boundary re-clamping.
