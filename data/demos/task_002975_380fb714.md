Diagnose the two vendored bugs before touching your own server code: (1) try `make` in `/app/tinyhttp` first — the link failure against pthread symbols means the Makefile's CXXFLAGS/LDFLAGS are missing `-pthread` (add it to both compile and link flags, since `std::thread` needs it at both stages); (2) inspect `parser.cpp`'s Content-Length accumulation loop character-by-character — a loop computing a decimal integer from ASCII digits should subtract `'0'` per digit, so `(c - '1')` is the tell-tale off-by-one bug to fix to `(c - '0')` (verify by testing with a POST body length that isn't all 1s/2s, since the bug would silently miscompute lengths for many other digits too). Only after `libtinyhttp.a` builds cleanly and Content-Length parsing round-trips correctly should you write your own `/home/user/verifier` project. Implement semver parsing/comparison carefully per the three operators (`=`, `^`, `~`), build the dependency graph from the payload, and on the first unmet constraint return the exact `MISSING <Pkg>` or `CONFLICT <Src> -> <Tgt>` body with the required HTTP codes. Start the server in the background bound to 127.0.0.1:9000 and leave it running.