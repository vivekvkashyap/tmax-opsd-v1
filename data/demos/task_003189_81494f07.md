Scaffold with `cargo new /home/user/model_fit`. Implement a small reusable trapezoidal-rule function `trapezoid(a, b, n_points, f)` that generates `n_points` evenly spaced x-values over `[a,b]` (so `n_points-1` intervals, step = (b-a)/(n_points-1)) and sums `(f(x_i)+f(x_i+1))/2 * step`. Call it exactly three times with the specified point counts for the left/center/right sub-domains — get the boundary points and counts exactly right (100 points for the outer domains, 1000 for the center) since the mesh refinement near the singularity is what makes the error small; a uniform mesh across the whole domain will fail the `1e-4` threshold. Sum the three sub-integrals for the numerical result. Compute the analytical value directly with `f64::asinh` (`2.0 * (1.0_f64/alpha).asinh()`) rather than hardcoding a literal, since `asinh` isn't in `std` before certain versions — verify it compiles, or derive it via `ln(x + sqrt(x*x+1))` if needed. Write `analytical`, `numerical`, and `abs(analytical-numerical)` as JSON with `serde_json` or manual string formatting to `/home/user/integration_log.json`, then `cargo run` to confirm output and error < 1e-4.