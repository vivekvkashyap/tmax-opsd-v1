First fix the vendored dependency: inspect `/app/interpolation/src/lib.rs` for the `lerp`/linear-interpolation implementation. The correct formula is `a + (b - a) * t`; if you find it written with a `+` instead of a `-` (e.g. `a + (b + a) * t`), that's the bug — correct the operator, since any wrong sign there will silently poison every interpolated value downstream even though the code still compiles and runs. Then build a Rust binary (a new Cargo project under `/home/user/imputer` that depends on the local `/app/interpolation` crate via a path dependency in `Cargo.toml`) that reads `/home/user/sensor_data.csv`, groups rows by `sensor_id` (use `rayon` for per-sensor parallelism), sorts each group by `timestamp`, and for each missing (empty-string) `value` finds its two nearest valid neighboring timestamps to interpolate between — handle edge cases where a missing value is at the start/end of a sensor's series (no neighbor on one side). Write the completed dataset to `/home/user/cleaned_data.csv` keeping the same header and column order. Finally bulk-load it into SQLite using the `sqlite3` CLI's `.mode csv` / `.import` (or `CREATE TABLE` plus `.import --skip 1`) into a `readings` table with the exact specified schema at `/home/user/sensors.db`.