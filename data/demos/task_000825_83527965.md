Write the cleaning steps in `/home/user/clean_metrics.py` in the exact order specified, since pandas dtype/NaN behavior depends on it: load `/home/user/raw_metrics.csv` with `pandas.read_csv`, compute `median()` of `memory_usage` over its non-null values and use `.fillna()` with that median on the column, then filter rows where `cpu_usage` is outside `[0.0, 100.0]` (drop `> 100.0` or `< 0.0`, using strict comparisons so exact boundary values are kept), then call `.dropna()` to remove any rows still missing a value in any column (this catches rows where `cpu_usage` itself was originally null, which the memory-median fill doesn't touch), and finally `.to_csv(..., index=False)`. Expect pandas to keep the numeric columns as float64 even for originally-integer-looking values, because the presence of NaNs upcasts the dtype — don't try to force ints back. Separately write `/home/user/run_pipeline.sh` that creates a venv at `/home/user/venv` with `python3 -m venv`, activates it, `pip install pandas`, then runs the cleaning script with that venv's interpreter; `chmod +x` it, then execute it end-to-end to produce `/home/user/clean_metrics.csv`.