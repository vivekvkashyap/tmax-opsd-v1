Start by running `make` inside `/app/libgraphdump-1.2.0/` to see the real linker/compiler error rather than guessing. A Makefile that builds a shared library (`gcc -shared -o lib....so obj.o`) but compiles the object file without `-fPIC` will fail (or silently produce a broken .so) on typical Linux toolchains — add `-fPIC` to the object-compilation rule and rebuild. Read `graphdump.h` to learn the exact `Graph`/`Edge` struct layout and the `parse_graph_dump` signature before writing `/home/user/backup_verifier.c`. Structure your C server as: call `parse_graph_dump("/home/user/backup.dat", &g)`, build an adjacency structure (e.g., adjacency list keyed by node id) treating each edge as undirected (add both directions), then implement a standard TCP server (`socket`/`bind`/`listen`/`accept`) on `127.0.0.1:8080` that reads newline-terminated lines per connection, parses `DEGREE <id>` (count of adjacency entries for that node) or `PATH <a> <b>` (BFS shortest-path edge count, `0` if `a==b`, `-1` if unreachable), and writes back the integer plus `\n`. Handle multiple sequential queries on one connection and don't exit after the first. Compile linking `-L/app/libgraphdump-1.2.0 -lgraphdump` (set `LD_LIBRARY_PATH` or use rpath so it finds the .so at runtime), run it in the background, and manually test with `nc`/`printf | nc 127.0.0.1 8080` before finishing.