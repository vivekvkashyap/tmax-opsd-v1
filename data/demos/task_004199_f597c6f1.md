Recover the PIN first since everything downstream depends on it: transcribe `/app/telemetry_pin.wav` with a speech-to-text tool (install `whisper.cpp`/`openai-whisper` or similar, or use `ffmpeg` plus another ASR you can get working offline) and convert the four spoken digit-words into the numeric 4-digit PIN string — don't guess, actually run the transcription and read its output. Write the C daemon as a plain blocking TCP server (socket/bind/listen/accept loop, one connection at a time or forked/threaded) on 127.0.0.1:9090: read a line, compare it exactly to `AUTH <PIN>\n` using the digits you recovered, reply `TELEMETRY_ACK\n` on match and just close the socket silently otherwise; log every connection attempt (matched or not) to the log file with a timestamp before deciding the outcome. Compile and start it backgrounded, then confirm it works with `nc` or `printf | nc` before touching haproxy. Configure haproxy.cfg as a plain TCP passthrough (`mode tcp`) listening on 0.0.0.0:8080 forwarding to 127.0.0.1:9090, and launch it as your own user. Finally write rotate_and_backup.sh to mkdir -p the backups dir, mv the log to a timestamped name under it, and restart/signal the daemon so it recreates a fresh log — test the whole chain end-to-end yourself (connect, rotate, connect again) before finishing.