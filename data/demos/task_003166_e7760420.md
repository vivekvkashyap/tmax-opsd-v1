Work through the four phases sequentially, verifying each before moving on. Phase 1: inspect the Makefile in `/home/user/auth-lib/` — the link step is missing `-lm -lcrypto` since the C code uses math.h and openssl/sha.h; add those flags to the gcc command, run `make`, then `python3 test_load.py` to confirm `libauth.so` loads and returns the expected value. Phase 2: write a Python script using `sqlite3` against `/home/user/db/audit.db` — since SQLite can't add FK constraints to existing tables, create a new `log_entries_new` table with the `client_ip` and `user_id` columns (with FK to `users`), copy data over setting `client_ip='0.0.0.0'` and `user_id=1`, drop the old table, rename the new one; also create and populate the `users` table first. Phase 3: extract frames from `/app/ci_test_run.mp4` with ffmpeg at a reasonable rate (e.g. `-vf fps=10` or per-frame), then use `pyzbar.pyzbar.decode` on each PIL-loaded frame image to find and decode the QR code around the 2-4 second mark — this recovered string becomes your BEARER_TOKEN, don't guess it. Phase 4: build a Flask/FastAPI app on 127.0.0.1:8000 with `/health` and an `Authorization: Bearer <token>`-gated `/api/v1/audit/logs` that queries the migrated DB and returns the exact JSON schema. Run it in the foreground.