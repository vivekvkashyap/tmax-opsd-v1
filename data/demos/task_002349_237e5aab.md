Start by reading the vendored `/app/graph_backup_store` source (not just its public API) to find the bug, since it must be fixed in place, not worked around. Look specifically at the index-building logic that maps a node id to its parent id — a common bug pattern is using the node's own id as a fallback default (e.g. `node.get('parent_id', node['id'])`) instead of `None` when a node has no parent; that self-reference is what causes infinite loops when a root node's "parent" resolves back to itself during recursive lineage traversal. Fix the fallback so root nodes correctly have no parent. Then write `/home/user/get_lineage.py`, which must import and use the fixed `graph_backup_store` package's API (not re-implement JSON parsing/graph traversal from scratch) to walk from the given backup id up through parent references until it hits a node with no parent, collecting ids along the way, and print them joined by `" -> "` starting from the given id down to the root. Test it manually on a few ids from `/home/user/backups.json` — including likely root ids — to confirm termination and exact `" -> "` formatting (no trailing/leading spaces, correct order) before considering it done, since the grader fuzzes many ids and expects bit-exact output.