First inspect the vendored Go source: `grep -rn "Salted" /app/vendor/github.com/Luzifer/go-openssl` to locate where the OpenSSL magic header constant/string comparison was tampered with, then restore it to the standard 8-byte ASCII value OpenSSL actually writes (`Salted__`). Verify the fix by writing a tiny throwaway Go test that encrypts a known string with the `openssl` CLI (`openssl enc -aes-256-cbc -pbkdf2 -a -salt -pass pass:...`) and decrypts it with the patched library to confirm round-trip works. For the decryptor program, use `go mod edit -replace` (or a local `go.mod` module path) so your `/home/user/decryptor.go` imports the patched vendored package rather than fetching from the internet. Read all of stdin (`io.ReadAll(os.Stdin)`), base64-decode, call the library's byte-decrypt function with the passphrase, and on ANY error (base64 decode failure, decrypt failure, wrong salt, corrupted ciphertext) print nothing and `os.Exit(1)` — do not print partial output or error messages to stdout. Build with `go build -o /home/user/decryptor /home/user/decryptor.go` and test manually against a few valid and garbage inputs before considering it done, since it will be fuzzed for exact behavioral parity including error paths.