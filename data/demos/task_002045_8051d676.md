Write one script that runs the full pipeline end to end so intermediate steps stay consistent. Load `/home/user/raw_data.csv` with pandas, `dropna(subset=['id'])`, then cast `id` to `int` (this undoes the float coercion). Build TF-IDF features on the `text` column of the cleaned rows with `TfidfVectorizer()` default settings, then fit `PCA(n_components=2, random_state=42)` on the dense TF-IDF matrix (`.toarray()`) to get PC1/PC2. Use `np.corrcoef` (or `scipy.stats.pearsonr`) between the cleaned `value` column and each PC, taking `abs()` of both correlations since PCA sign is arbitrary. Load `/home/user/predictions.json`, and for each cleaned id check whether `str(id)` is a key in that dict, counting misses. Round all floats to 4 decimal places and write the four fields (`clean_row_count`, `abs_correlation_pc1`, `abs_correlation_pc2`, `missing_predictions`) to `/home/user/analysis_summary.json` exactly as specified. Be careful with row order after dropna (reset_index if needed) so `value` and the PCA rows stay aligned.
