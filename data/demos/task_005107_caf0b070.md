Work in order since later steps depend on earlier state. First, tar the pristine source before touching anything: `tar czf /home/user/miniping_backup.tar.gz -C /app miniping-1.2`. Second, to find the build bug, run `make` in `/app/miniping-1.2` and read the linker error carefully — an "undefined reference to" a math function (like `log2`/`sqrt`/`pow`) means the Makefile's `LDFLAGS`/`LIBS` is missing `-lm`; add it and rebuild until `miniping` links successfully. Third, write the Expect script: use `spawn ./miniping --init` (with correct working directory), then a sequence of `expect "Set default timeout (ms):"` / `send "500\r"`, similarly for retries ("3") and debug ("n"), ending with `expect eof`. Test it manually (`./setup_miniping.exp`) and confirm the config file/state it produces is what `--check` expects — inspect the binary's behavior via `strings`/running it directly to understand expected prompts precisely rather than guessing. Fourth, the wrapper script should simply be `exec /app/miniping-1.2/miniping --check "$1"` (quote to preserve the argument exactly), then `chmod +x` all scripts. Test bit-exactness by running the wrapper against a few sample payloads and inspecting output carefully for trailing whitespace/newline differences.