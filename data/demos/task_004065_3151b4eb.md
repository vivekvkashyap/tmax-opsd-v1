First recover the query text from the image with OCR: `tesseract /app/schema_and_query.png stdout` (try preprocessing — upscaling/thresholding — if OCR output looks garbled), then sanity-check the recovered SQL by running it against /app/graph.db and confirming it executes and returns plausible rows before treating it as ground truth. Analyze why it's slow with `EXPLAIN QUERY PLAN <query>` in sqlite3 — a multi-hop self-join chain on edges.src/edges.dst without indexes forces full table scans at each join step. The fix is adding indexes on the join columns (e.g. `CREATE INDEX idx_edges_src ON edges(src)`, and likely one covering dst too, plus an index on nodes.type since it's filtered), not rewriting the query's semantics — the output rows/columns must stay identical. Put the `CREATE INDEX` statements (and any `ANALYZE`) into /home/user/setup_db.py as a script that connects to a given sqlite file and commits the schema changes. Save the (possibly unchanged, now index-accelerated) query text into /home/user/optimized.sql. Benchmark before/after yourself with `.timer on` in sqlite3 or Python's `time` module to confirm you clear the 20x speedup bar, since indexing alone on a 25k-edge, 3-hop join is usually sufficient.