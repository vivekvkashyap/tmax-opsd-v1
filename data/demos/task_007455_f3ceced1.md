Diagnose the two bugs separately before touching the optimizer. The instability is purely numerical: the hand-rolled Euler stepper uses `dt=0.5`, which is too coarse for this stiff-ish rate system — replace `euler_integrate` entirely with `scipy.integrate.solve_ivp`, passing the ODE right-hand side (`dA/dt=-k1*A, dB/dt=k1*A-k2*B, dC/dt=k2*B`) and `t_eval=t_ref` so you get values exactly at the reference sample times without needing fragile floating-point time matching. For the objective, run the integrator with trial `(k1, k2)`, extract simulated B at `t_ref`, and return the sum of squared differences against `B_ref`. Call `scipy.optimize.minimize(objective, x0=[1.0, 1.0], method='Nelder-Mead')` and pull `result.x`. Round both fitted values to 2 decimal places and write them comma-separated (no extra whitespace/newline issues) to `/home/user/kinetics/optimal_params.txt`. Sanity-check convergence by printing the fitted SSE — it should be near zero since the reference data was generated from a clean two-step kinetic model.
