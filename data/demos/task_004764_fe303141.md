Work through this in three separate, individually-testable stages: indexing, the recursive query, and the C plumbing. First, add an index that speeds up traversal from a package to its dependencies: `CREATE INDEX idx_dependencies_pkg ON dependencies(pkg_id);` (run via `sqlite3 /home/user/graph.db "..."`). Second, design the recursive CTE separately in the `sqlite3` CLI before touching C: something like `WITH RECURSIVE deps(id, depth) AS (SELECT depends_on_id, 1 FROM dependencies JOIN packages ON packages.id=dependencies.pkg_id WHERE packages.name=? UNION SELECT d.depends_on_id, deps.depth+1 FROM dependencies d JOIN deps ON d.pkg_id=deps.id) SELECT depth, name FROM deps JOIN packages ON packages.id=deps.id ORDER BY depth, name;` — use plain `UNION` (not `UNION ALL`) so repeated dependencies (e.g. diamond-shaped graphs) are deduplicated, and verify it manually with the CLI first. Only once the SQL is confirmed correct, port it into the C file's `sql` string, bind the CLI argument with `sqlite3_bind_text(stmt, 1, argv[1], -1, SQLITE_TRANSIENT)` (the placeholder currently has no binding call), and keep the existing fetch/write loop. Compile with `gcc -o extract_deps extract_deps.c -lsqlite3`, run it, and diff the output CSV format (`depth,name` per line) against your CLI-derived expectations before finishing.