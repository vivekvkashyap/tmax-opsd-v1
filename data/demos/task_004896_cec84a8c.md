Apply the patch first with `patch build.py < fix_linker.patch` (or `git apply`) and inspect the result to confirm `build.py` now accepts `--link-order` and reads the file into an ordered list before writing `build_artifact.bin`. For `deps.json`, write a small Python script: load the JSON, then for every key ending in `_stub`, overwrite its dependency list with `[]` — do this transformation before building the graph so it actually breaks the cycle the stub was causing. Then implement Kahn's topological sort: compute in-degree for each node from the (post-stub-clearing) edges, maintain a set/heap of nodes with in-degree 0, and at each step pop the alphabetically smallest ready node (use a min-heap or sort the ready set each iteration — a plain queue without alphabetical tie-breaking will produce a valid-but-wrong order), decrementing in-degrees of its dependents as you emit it. Write one module name per line to `/home/user/build_env/link_order.txt`, then run `python3 build.py --link-order link_order.txt` and confirm `build_artifact.bin` is created with the expected "SUCCESS: ..." content reflecting your computed order.
