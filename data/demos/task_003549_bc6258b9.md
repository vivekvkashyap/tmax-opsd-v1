Port the Python prototype to C++ line-for-line rather than reinventing the math, since exact numerical equivalence to NumPy's SVD-based scoring is what will be checked. Download and extract Eigen 3.4.0 locally (no root needed — it's header-only, so just add `-I` to the include path at compile time, no linking required). Load the 50x20 matrix with Eigen's `Matrix<double, Dynamic, Dynamic>`, compute SVD via `Eigen::JacobiSVD` (or BDCSVD) with thin U/V options, then replicate `compute_score` (truncate to k components, reconstruct, RMSE + 0.1*k) and `get_similar_items` (cosine similarity over truncated V^T columns, excluding the target index, top-3 by descending similarity) exactly as in the Python reference — watch for the classic C++/Eigen pitfalls: SVD sign ambiguity (doesn't affect scoring or cosine similarity magnitude here since both target and compared vectors come from the same decomposition, but double-check), and matching NumPy's argsort-descending tie-breaking behavior when sorting similarities. Write pipeline.sh to compile once, then loop k=1..15 calling your binary (or have the binary itself sweep k internally and print the optimal result) to find the minimal-score k, then compute both target items' top-3 lists and emit the exact JSON structure to /home/user/output/result.json. Validate intermediate RMSE/score values by eyeballing whether they decrease then increase as expected before trusting the final k.
