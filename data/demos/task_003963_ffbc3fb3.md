For check_port.c, open a raw TCP socket (`socket`/`connect` with a sockaddr_in for 127.0.0.1:8080), and print exactly "STATUS: OK\n" or "STATUS: FAIL\n" to stdout based on whether `connect()` succeeds (non-zero/refused connection means FAIL) — no extra output, no newline variations. For the git hook, remember that hooks run with the repo's `.git` as a working-context quirk: always reference `/home/user/check_port` and the log files by absolute path inside the hook script rather than relying on relative paths or an inherited PATH, or output will land in the wrong place. Implement the hook to append the C program's output to net_log.txt, then count lines with `wc -l`; when the count reaches exactly 3, copy net_log.txt to net_log.bak (overwrite) and truncate net_log.txt to empty (e.g. `: > net_log.txt` or `cp -f` then `> file`), otherwise leave it accumulating. Test the rotation boundary carefully — it must trigger exactly at 3 lines, not before or after, and chmod +x the hook file.
