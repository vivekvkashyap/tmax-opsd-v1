This is a multi-stage pipeline: OCR extraction, notebook-based simulation, and a live HTTP service — get each stage right before moving to the next. First, run `tesseract /app/params.png stdout` (or similar) to recover the six parameter values as text; don't hardcode them — parse them programmatically from the OCR output (e.g. with a simple regex on `key=value` lines), and sanity-check the parsed values look plausible before trusting them. Second, build the notebook so the RNG steps happen in the specified order exactly: call `np.random.seed(Seed)` once, then draw `np.random.normal(0, 1, N_paths)` for Z — any different draw order or an extra draw beforehand will silently shift every generated value. Apply the closed-form GBM formula directly (not a multi-step Euler simulation) and take the mean of S_T. Third, read `/app/historical.csv`'s Final_Price column and take its mean for reference_mean, then compute the absolute difference. Finally, stand up a Flask/FastAPI server bound to 127.0.0.1:8080 with a GET /metrics route returning the exact JSON schema requested, rounding the three computed floats to 2 decimals — start it in the background (e.g. nohup ... &) so it stays up after your response ends, and curl it yourself to confirm it responds before finishing.
