Use `git bisect` twice, once per regression, driven by scripted good/bad checks rather than manual eyeballing of ~200 commits. For the core-dump regression, write a bisect test script that checks out each commit, rebuilds/uses `filter_tool` as it exists at that commit, feeds it representative input, and reports bad if it segfaults (`git bisect run <script>`); for the race condition, write a script that runs `pipeline.sh` (perhaps repeatedly, since races can be flaky) and reports bad if `results.txt` doesn't have exactly 100 well-formed lines. Capture the two commit hashes bisect converges on and write them to `/home/user/regression_report.txt` in the exact `KEY=value` format requested. Then, working at `main` HEAD: fix the race in `pipeline.sh` by removing whatever concurrency hazard was introduced (e.g. multiple processes appending to the same file/fd simultaneously — write to separate per-job files or serialize the append) while still using background jobs. For `filter_tool`, since there's no source, treat it as a black box: try feeding it different headers/prefixes and observe with `strace`/experimentation which byte sequence avoids the crash (a "magic header"), then prepend that header to the data piped into `filter_tool` from `pipeline.sh`. Re-run `./pipeline.sh` repeatedly to confirm 100 clean lines and no crashes before finalizing.