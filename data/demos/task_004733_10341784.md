Attack the three defects independently, verifying each in isolation before combining. For the concurrency bug, inspect the thread-spawning loop in processor.cpp: any lambda that captures the loop index `i` by reference (`[&]`) races because `i` mutates before the thread reads it — fix by capturing by value (`[&, i]` or pass `i` as an explicit thread argument) so each thread gets its own copy. For the numerically unstable variance, replace the naive `sum_sq/n - mean^2` (or equivalent one-pass sum-of-squares) formula with a two-pass computation (compute mean first, then sum `(x-mean)^2`) or Welford's online algorithm, so it can't go negative or produce NaN. For the filter taps, don't guess or invent numbers — use binary tools to recover the real symbol: `nm -D /home/user/libfilter.so | grep taps_array` to find its address/section, then dump the raw bytes with `objdump -s -j .rodata /home/user/libfilter.so` (or the relevant section) and reinterpret each 4-byte little-endian chunk as an IEEE-754 float, or more reliably write a tiny C program that `dlopen`s the library, `dlsym`s `taps_array`, and prints the 8 floats directly — then hardcode exactly what you extracted. Compile with the given g++ command and run against the input WAV, comparing your output's behavior for sanity before relying on the final MSE check.