For Phase 1, read `/home/user/historical_runs.txt` into a numpy array, call `np.random.seed(42)` immediately before resampling, then vectorize the bootstrap as `resamples = np.random.choice(data, size=(10000, len(data)), replace=True)` and take `resamples.mean(axis=1)` to get 10,000 bootstrap means — using this exact vectorized call order matters for reproducing the seeded sequence, so avoid resampling in a Python for-loop with per-iteration `np.random.choice` calls, which consumes the RNG stream differently. Compute `np.percentile(boot_means, [2.5, 97.5])` for the bounds, round to 2 decimals, and write JSON keys `lower_bound`/`upper_bound` to `/home/user/bootstrap_results.json`. For Phase 2, build the initial mesh for `solve_bvp` by concatenating `np.linspace(0,0.4,11)`, `np.linspace(0.4,0.6,51)`, `np.linspace(0.6,1.0,11)` and removing the duplicate junction points (e.g. via `np.unique` or manual slicing) so you end up with 71 unique points — passing overlapping duplicate x-values will confuse the solver. Define the ODE as a first-order system (`u`, `u'`) with `f(x)=100` on `[0.4,0.6]` else `0`, initial guess zeros, boundary residuals `u(0)=0, u(1)=0`, solve, evaluate `sol.sol(0.5)[0]`, round to 4 decimals, and write it to `/home/user/bvp_result.txt`.