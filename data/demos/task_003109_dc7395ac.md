Fix the cross-compilation, then write the WebSocket test script, verifying each piece separately. First, check whether the aarch64 cross toolchain is installed (`which aarch64-linux-gnu-gcc aarch64-linux-gnu-ar`); install via `apt-get install gcc-aarch64-linux-gnu` if missing. Edit `build.sh` so the C step uses `aarch64-linux-gnu-gcc -c src/c_src/processor.c -o processor.o` and `aarch64-linux-gnu-ar rcs libprocessor.a processor.o` (matching the paths/output name Cargo's build script expects), replacing the host `gcc`/`ar` calls. Then generate `.cargo/config.toml` inside the project directory with a `[target.aarch64-unknown-linux-gnu]` section setting `linker = "aarch64-linux-gnu-gcc"`, so Cargo invokes the cross-linker instead of the host one. Run `bash build.sh` and confirm it exits 0. Separately, for `test_ws.sh`, hand-craft a WebSocket upgrade handshake with `curl -i -N` and the required headers (`Connection: Upgrade`, `Upgrade: websocket`, `Sec-WebSocket-Version: 13`, a valid base64 `Sec-WebSocket-Key`), sending the exact JSON payload to `ws://localhost:8080/stream`, redirecting all output (`> /home/user/ws_test_output.log`). Make both scripts executable and confirm build.sh's actual exit status with `echo $?`.