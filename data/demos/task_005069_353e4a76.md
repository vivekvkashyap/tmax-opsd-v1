Build each of the three artifacts to the letter of its spec, then verify end-to-end. For `~/.ssh/config`, write standard `Host` blocks with `Hostname`/`User`/`Port` directives for both aliases and set restrictive permissions (`chmod 600`) since SSH itself will refuse to use a world-readable config in some setups. For `port_fstab`, start with a `#` comment line, then one space-separated line per tunnel in exactly the `<local_port> <remote_target>:<remote_port> <ssh_alias> <extra_ssh_options>` order shown in the spec — this exact token layout is what downstream parsing depends on. For `tunnel_builder.py`: read the fstab file, skip blank/`#` lines, split each remaining line on whitespace into the four fields, then for each entry call `input()` with the prompt text built by substituting the parsed remote_target/remote_port/local_port into the exact given template (match spacing and punctuation precisely, including the trailing `(y/n): `). On a `y` response, append (don't overwrite mid-run) a line to `start_tunnels.sh` formatted as `ssh -L <local_port>:<remote_target>:<remote_port> <extra_ssh_options> <ssh_alias>`. Before writing, ensure the script starts fresh with `#!/bin/bash` as its very first line (truncate/overwrite on each full run), and call `os.chmod` to set the executable bit afterward. Do not execute the generated script.