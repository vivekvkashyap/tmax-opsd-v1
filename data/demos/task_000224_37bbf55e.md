Work this incident response task in strict order so each piece is testable independently. First, `grep` the log file for lines containing the suspicious IP, and extract the token value from the matching admin login line into `/app/revoked_tokens.txt` — don't hardcode a guess, read it out of the log. Second, generate the SSH keypair with `ssh-keygen -t ed25519 -f /home/user/.ssh/admin_key -N ""`, `chmod 600` the private key, append the `.pub` contents to `authorized_keys`, and strip out any pre-existing compromised key line (don't just append, actually remove the old entry). Third, edit `login.py` so redirect targets are validated: treat anything starting with an absolute scheme (`http://`/`https://`) as untrusted and force it to `/dashboard`, while relative paths pass through unchanged — write this as an explicit check, not a blocklist that's easy to bypass. Also change `config.py` so missing `AUTH_KEY` raises `ValueError` instead of silently defaulting. Fourth, in `start_services.sh`, generate a random 32-char hex key (e.g. `openssl rand -hex 16`), export it, save a copy to `/app/new_auth_key.txt`, configure/start sshd on port 2222, launch the vendored server on 127.0.0.1:8080, and background both processes so the script itself exits while they keep running.
