For the ETL step, load the CSV with pandas, compute `df[['sensor_A','sensor_B','sensor_C','sensor_D']].corr()`, and pick whichever of B/C/D has the largest `abs()` correlation with `sensor_A` to drop — don't assume, read the matrix. Compute the covariance determinant of the remaining three columns with `np.linalg.det(np.cov(remaining_df.values, rowvar=False))` and write both results to `tracking.json` in the exact required schema. For the vendored package: open the `Makefile` and check literal whitespace — `make` requires a real tab before recipe lines, so replace the leading spaces on the `gcc` line with an actual tab character (e.g. via an editor that shows whitespace, or `sed -i 's/^    /\t/'` on that line only); separately fix `import numpy as n` to `import numpy as np` in `inference.py` since the code calls `np.exp`. Rebuild with `make`, then `pip install -e /app/bayes_prob_lib`. For the API, check the `Authorization` header exactly against the required bearer token before calling `calculate_posterior`, returning 401 on mismatch, and leave the server running in the background listening on 127.0.0.1:8000.
