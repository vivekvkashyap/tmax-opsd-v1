Write the C++ program to open papers.db with the SQLite3 C API, first run `CREATE INDEX IF NOT EXISTS idx_citations_cited ON citations(cited_id);` (and optionally citing_id too) to speed up the recursive lookup without touching existing data. Then build a single SQL query using a recursive CTE that starts from `cited_id = 1` at depth 1, joins citations to find citing_id at each level, and recurses while incrementing depth, capping at depth <= 3 in the recursive term's WHERE clause (be careful the base case is depth 1, not 0, and that you don't accidentally include paper 1 itself). For each resulting (citing paper, depth) row, join to papers and extract `domain` from metadata_json using `json_extract(metadata_json, '$.domain')`. Aggregate with `GROUP BY depth, domain, COUNT(*)`, ordering `ORDER BY depth ASC, domain ASC`. Write results to citation_summary.csv with header `depth,domain,count`, one row per group, no trailing extras. Compile with the exact g++ command given, run it, and diff your citation_summary.csv output against what you'd expect from manually tracing a few known citation chains in the schema before considering it done — small CTE depth or dedup mistakes (e.g. double-counting a paper cited by two depth-1 papers) are the likely failure mode.
