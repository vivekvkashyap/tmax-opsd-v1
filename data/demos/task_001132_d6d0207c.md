Part 1 (video): extract frames with `ffmpeg -i /app/dataset_video.mp4 -vf fps=1 frame_%03d.png` (or similar) into a working dir, then group filenames into chunks of exactly 5 (sorted numerically) and `tar -czf` each group into `/home/user/archive/chunk_0.tar.gz`, `chunk_1.tar.gz`, etc. — use `cd` into the frame dir or `tar -C` so archive members aren't absolute paths, and handle the leftover partial chunk if frame count isn't a multiple of 5. Part 2 (classifier): first compute a reference vector by running `/app/embed.py` on every file in `/app/corpora/clean/` and averaging the resulting vectors component-wise; store that reference (e.g. in a file `classifier.sh` reads, or hardcode it after computing once) inside `/home/user/classifier.sh`. At runtime, embed the input file, compute cosine similarity to the reference, and pick a threshold that cleanly separates all of `/app/corpora/clean/` from all of `/app/corpora/evil/` — sweep threshold values programmatically (e.g., check similarity of every clean and evil training file and pick a cutoff strictly between the lowest clean score and highest evil score) rather than guessing a constant. Exit 0 for clean, 1 for evil. Validate against the training corpora before trusting the hidden set.
