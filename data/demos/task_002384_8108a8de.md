Write `process_configs.py` as a true streaming pipeline: open the log file and iterate `for line in f:` (never `.readlines()` or reading the whole file into a string) so it scales to large files. Compile a regex once, e.g. matching `\[(\d{4}-\d{2}-\d{2} \d{2}):\d{2}:\d{2}\] USER:\S+ CONFIG:(.+)=(.+)` (capture the date+hour directly, or capture full timestamp and slice to the hour with string slicing/`datetime.strptime`), to pull out the hour bucket and the `key=value` string per line. Maintain a `dict` mapping hour-bucket -> an ordered set (e.g. `dict.fromkeys` or a plain `set` plus insertion tracking) of unique `key=value` strings, adding only if not already seen for that bucket to implement first-occurrence dedup. Count every line processed (including ones that don't match, if the spec implies total lines) for the "Processed X lines successfully." log message. After processing, for each bucket sort its list of strings alphabetically (`sorted()`) before dumping to `/home/user/hourly_summary.json` with `json.dump`. Write the single pipeline log line to `/home/user/pipeline.log`. Run the script and diff the JSON output structurally against your own hand-traced expectation from the log content.