First recover the target App ID: run OCR (e.g. `tesseract /app/policy.png -`) or otherwise extract the text from the image, then locate the hex App ID it states and convert it to an integer — do not guess or hardcode a value you haven't actually derived from the image. Then write `/home/user/compact_wal.py` as a true streaming filter: read the 8-byte magic header first and pass it through unchanged, then loop reading exactly 8 bytes (Length + App ID, both little-endian uint32 via `struct.unpack('<II', ...)`), then read exactly `Length` payload bytes. If the record's App ID equals the decommissioned ID, drop the header and payload; otherwise write both unaltered to stdout. Use `sys.stdin.buffer`/`sys.stdout.buffer` for binary I/O and never read the whole input into memory. Handle the end of stream by breaking cleanly when a header read returns fewer than 8 bytes (EOF), and make sure output is flushed. `chmod +x` the script. Test with hand-crafted binary WAL streams (including zero-length payloads and records with the target ID) before trusting correctness, since payload bytes can contain arbitrary binary data including bytes that look like headers.