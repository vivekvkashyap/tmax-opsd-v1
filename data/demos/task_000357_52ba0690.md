For Phase 1, don't hand-pick a combination — enumerate all version combinations across the three components programmatically (nested loops or `itertools.product`), filter out any that violate a conflict rule from `deps.json` (check both orderings of the conflict pair), filter out any with total cost >= 90, and among what remains pick the one with maximum total cost; write `component:version` lines sorted alphabetically by component name. For Phase 2, use `subprocess.run(['gcc', ...])` twice, building the flag list conditionally: always add `-DTARGET_ARM` for the arm binary only, and add `-DCORE_V2` to both binaries' flag lists only if the resolved core version equals "2.0" — check this dynamically from your Phase-1 result, not by inspection. For Phase 3, hash each binary's raw bytes with `hashlib.sha256(data).digest()` (keep the raw bytes, not hex, for the parity step), XOR together all 64 bytes from both digests one at a time (`parity ^= byte`), format as two-hex-digit lowercase, and write hex-digest lines plus the `PARITY:` line to `manifest.txt` in the exact key order given. Run the whole script end-to-end once to produce all output files.
