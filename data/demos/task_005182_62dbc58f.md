Write `/home/user/profile.sh` to loop over thread counts `1 2 4 8`, exporting `OMP_NUM_THREADS=<n>` before each run of `python3 /home/user/sim.py --mesh 1024` and capturing its single-number stdout output as the Actual Time for that thread count — run threads=1 first and keep its Actual Time in a variable since every ideal-time calculation depends on it (`IdealTime = ActualTime_1 / n`). Compute `Efficiency = IdealTime / ActualTime` for each row using `awk` or `bc -l` for floating-point math (shell arithmetic alone can't do decimals), and format every float column to exactly 4 decimal places, e.g. `printf "%.4f"` or `awk '{printf "%.4f", $1}'`. Append rows to `/home/user/results.csv` under the exact header `Threads,ActualTime,IdealTime,Efficiency`. After collecting all four rows, compute the Mean Squared Error between the ActualTime and IdealTime columns (sum of squared differences divided by 4) with `awk`, format it to 4 decimals, and write only that value to `/home/user/mse.txt`. Make the script executable and run it end-to-end to produce both output files.
