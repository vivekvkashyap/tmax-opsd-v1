Write mail_monitor.c to open `/home/user/mail_spool` with `opendir`, loop with `readdir`, and count only regular files — check each entry's type with `stat`/`lstat` on `d_type == DT_REG` (or fall back to `stat()` if `d_type` isn't reliably populated on this filesystem) rather than just skipping `.`/`..` by name, since subdirectories must also be excluded from the count. Keep the count dynamic; never hardcode a number. If count > 5, call `system("tar -czf /home/user/mail_backup.tar.gz -C /home/user mail_spool")` exactly as specified so the tarball's internal paths start with `mail_spool/`. Then `fopen("/home/user/health_status.log", "w")` and `fprintf` exactly one of the two required status lines with the real count substituted for X — match the literal format strings precisely (e.g. `STATUS: BACKUP_TRIGGERED, EMAIL_COUNT: %d\n"`), since the verifier does exact string matching. Compile with `gcc mail_monitor.c -o /home/user/mail_monitor` and run it once so the log and (if triggered) backup actually get produced against the current spool contents, then spot-check the log file and tarball listing before finishing.
