Work through this in stages and verify each before moving on. First, convert the 5000 JSON files with `jq -r '[.timestamp, .service, .config.rate_limit, .config.timeout] | @csv' /home/user/data/raw_configs/*.json | sort -t, -k1,1n > /home/user/data/configs.csv` (sort numerically by timestamp, not lexically). Second, write `compress.py`: since timestamps are near-sequential and service names/values repeat heavily, delta-encode the timestamp column, run-length-encode the service name column, then pack everything (e.g. with `struct`) and pipe through `zlib.compress` or `bz2.compress` — test the resulting file size against the 85000-byte target and iterate on the encoding if it doesn't fit; write the exact inverse in `decompress.py` and round-trip test it against the original CSV before touching the services. Third, edit `/home/user/app/nginx.conf` to add `proxy_pass http://127.0.0.1:5000;` inside the existing (currently incomplete) location block — don't rewrite the whole file, just add the missing directive. Set `COMPRESSED_DATA_PATH` and `DECOMPRESS_SCRIPT` env vars as specified, then run `start_services.sh` and confirm with `curl http://localhost:8080/history` that the decompressed CSV comes back byte-identical to your original.
