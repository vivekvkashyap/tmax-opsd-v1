Build this as three composable numeric functions in the Rust project so each can be checked in isolation against a hand-computed example before wiring them into the final report. First, implement JS divergence per workload size: compute M = (A+B)/2 elementwise, then JS = 0.5*KL(A||M) + 0.5*KL(B||M), where KL(P||Q) = sum(p * ln(p/q)) using natural log, and explicitly special-case `p == 0.0` to contribute 0 to the sum (skip the term rather than computing `0*ln(0)` which is NaN in floating point). Second, compute Version B's expected latency per workload size as the dot product of its histogram with `bin_centers`. Third, fit a quadratic (least-squares, degree 2) to (workload_sizes, expected_latencies) — solve the 3x3 normal-equations system (Vandermonde matrix transpose times itself) with straightforward Gaussian elimination or Cramer's rule rather than pulling in an external crate, since the system is small and exactness matters. Verify your JS divergence and regression coefficients against a couple of terms you compute by hand with a calculator before trusting the full pipeline, then serialize all three sections into `/home/user/perf_report.json` matching the required key names and array ordering exactly.
