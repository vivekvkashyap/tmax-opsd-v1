Work through this in stages and verify each before moving on. (1) Threshold extraction: use `ffmpeg` (e.g. `-vf select` or extract all frames to PNGs) to pull frames from `/app/calibration_timelapse.mp4`, take the last 10 frames, compute mean grayscale pixel intensity across them, then divide by 255.0 to get threshold `T` — don't hardcode a guessed value, derive it from the actual pixel data. (2) Reference model: use the HDF5 C API (`H5Fopen`, `H5Dopen2` on dataset `/dinucleotide_probs`, `H5Dread` into a `double[4][4]`) to load the reference transition matrix; link with `-lhdf5`. (3) In your detector C program: parse FASTA sequences, build an empirical 4x4 dinucleotide transition probability matrix (counts of X→Y pairs normalized by row or total, matching however you define it consistently on both reference and empirical sides), then compute Bhattacharyya distance as `-ln(sum(sqrt(p_i*q_i)))` over matrix entries. For the bootstrap CI, resample FASTA sequences with replacement 1000 times, recompute the distance each time, sort, and take the 2.5th percentile as the CI lower bound. Compare that lower bound to `T`: reject only if lower bound > T. Write `/home/user/run_eval.sh` to compile with `-lhdf5 -lm`, loop over both corpus directories, and log `[ACCEPT|REJECT] /path/to/file.fasta` lines to `/home/user/eval_results.txt`. Test against both `/app/corpus/clean/` and `/app/corpus/evil/` and iterate on your Bhattacharyya/CI logic if any file misclassifies — the threshold and bootstrap procedure are what separate clean from evil, not arbitrary tuning.
