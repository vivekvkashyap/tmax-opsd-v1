Start by isolating the vendored library bug: grep `/app/uthash-2.3.0/src/uthash.h` for `HASH_ADD_STTR` or similar misspellings of the standard uthash macros (e.g. `HASH_ADD_STR`), and diff against what real uthash macro definitions look like (a properly parenthesized `do { ... } while(0)` block ending in a statement, no trailing semicolon issue inside the macro but callers must terminate their own statement) — rename the macro back correctly and fix the missing semicolon inside its expansion so it compiles as a valid do-while(0) block. Then build etl_dedup.c using uthash for a hash-set keyed on the normalized payload string: parse `YYYY-MM-DD HH:MM:SS` into epoch seconds with `strptime` + `timegm` (UTC, not localtime — mktime would apply local timezone and break bit-exactness), normalize the payload by lowercasing, replacing every non-alnum char with a single space, then collapsing repeated spaces and trimming ends. Only emit and hash-insert a line if its normalized payload isn't already a hash key. Compile with `-I/app/uthash-2.3.0/src`. Since the grader fuzzes thousands of cases for byte-exact stdout, test incrementally with small crafted inputs including punctuation/casing variants of the same duplicate before trusting the full pipeline. Wrap it with run_pipeline.sh doing `./etl_dedup < "$1" > "$2"`.