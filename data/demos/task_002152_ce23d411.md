First fix the vendored crate: inspect `/app/vendored/log-parser-1.0.0/Cargo.toml` closely for a typo in a section header (TOML section names must match exactly, e.g. `[dependencies]` not a misspelling) — correct it so `cargo build` succeeds there standalone. Then scaffold a new binary crate at `/home/user/detector` (`cargo new`), add `log-parser` as a path dependency pointing at the vendored crate's directory in your `Cargo.toml`, and call its exported `parse_log` function on the file contents read from the single CLI argument (`std::env::args`). For classification, iterate the returned tokens and flag "evil" if any token matches the exact credential-leak pattern `password=<nonempty>` or `--password=<nonempty>` (empty value after `=` should NOT count — watch for that edge case), or contains, case-insensitively, one of `<script>`, `javascript:`, `onerror=` as a substring. Exit with status 1 the moment any evil token is found, otherwise exit 0 after scanning everything. Build in release mode so the binary lands at `/home/user/detector/target/release/detector`, then self-test against every file in `/app/data/evil/` and `/app/data/clean/` (checking `$?` after each run) before considering it done — both corpora must classify with 100% accuracy.