Approach this as delta debugging plus source-level static analysis, not black-box guessing. First understand the packet framing (fixed-size header containing an id and a length field, followed by a variable payload) by reading `src/parser.rs`; write a small script to walk `/home/user/trace.bin` packet-by-packet using that framing, and bisect by truncating/removing packets and re-running `cargo run` (or a debug harness) until you isolate the single packet whose length field is a malformed/extreme value that causes the crash — extract exactly those header+payload bytes to `/home/user/minimal_trigger.bin`. To find the source line and function, run under `gdb` (or read a core dump with `coredumpctl`/`gdb core`) with a debug build (`cargo build`, not `--release`), trigger the crash, and use `bt`/`info line` to get the exact file:line and mangled/demangled function name for `report.txt`. Fix the parser bug by validating the length against the remaining buffer size *before* any unsafe pointer arithmetic and returning `None`/`Result::Err` on invalid lengths instead of doing raw pointer offsets. Fix the race by replacing the shared mutable counter with `std::sync::atomic::AtomicUsize` and `fetch_add(1, Ordering::SeqCst)` across threads. Rebuild and confirm `cargo build` is warning-free and `cargo run -- trace.bin` completes cleanly.
