Fix the build first: run `make` in /app/graph-sim-1.0, read the exact linker error, and diagnose it as a missing math-library link flag (functions like sqrt/pow undefined at link time mean you need `-lm` on the final link line of the Makefile) — add it and confirm `bin/simulate_graph` is produced. Next, open src/simulator.cpp and find the OpenMP `#pragma omp parallel for reduction(...)` loop that sums floating-point values; parallel reductions combine partial sums in a thread-scheduling-dependent order, which changes rounding each run, so make the summation deterministic by removing the pragma (or forcing a single thread / a fixed sequential accumulation order) rather than trying to "average out" nondeterminism. Verify determinism by running the binary several times and diffing outputs. Finally build server.py: on POST /simulate, check the X-API-Key header first and reject if missing/wrong, then subprocess-invoke the compiled binary against /app/molecule_graph.dat, parse the line-per-value stdout into floats, compute mean and population standard deviation (divide by N, not N-1) yourself, read ref_mean/ref_std from /app/reference_data.csv, compute the diffs, round every field to 4 decimals, and return that JSON. Launch the server bound to 127.0.0.1:9090 and leave it running in the background.