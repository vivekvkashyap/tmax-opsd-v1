Treat the baseline script's data leakage as the key bug to avoid repeating: always split data into train/validation before fitting any imputer, scaler, or outlier-handling transform, and only ever call `.transform()` (never `.fit_transform()`) on validation/test data — fit exclusively on the training split. Join `sensors_a.csv` and `sensors_b.csv` on `id`, then explore missingness and the outliers in `sensor3` (e.g. via `df.describe()` / quantiles) and choose a principled cap or robust-scaling approach (e.g. clipping to a percentile range or using `RobustScaler`) rather than deleting rows outright, since your `predict.py` must handle similarly messy hidden test data the same way. To get labels, query `/app/legacy_scorer s1 s2 s3 s4` across your training rows as a black box (batch/parallelize the subprocess calls), but also probe it with sweeps holding three inputs fixed and varying the fourth to infer whether the decision boundary is linear or has interaction terms — this tells you what model family will actually reach the accuracy bar. Train a model that captures the true structure you found, then build `predict.py` to load the same fitted preprocessing (persist it, e.g. via `joblib`, don't recompute from the new file) and model, handle missing values in new input consistently with training, and write `id,label` to `predictions.csv`.
