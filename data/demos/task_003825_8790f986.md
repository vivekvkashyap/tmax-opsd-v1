Read `packet_processor.py` and the crash log together: the traceback points at a list that grows on every packet (e.g. appending each packet object to a `history`/`self.packets` list) — that is the O(n) memory leak; remove any such accumulation and instead update only a few scalar accumulators per packet, so memory usage stays flat regardless of how many packets are processed. For the variance bug, look for a formula computing variance from running sums like `E[X^2] - E[X]^2` (or `sum_sq/n - mean^2`) — this is the classic numerically unstable form that suffers catastrophic cancellation for large values; replace it with Welford's online algorithm (track `n`, `mean`, and `M2`, updating incrementally as `delta = x - mean; mean += delta/n; delta2 = x - mean; M2 += delta*delta2`), then compute population variance as `M2 / n` at the end. Use a pcap-reading library (e.g. scapy's `PcapReader`, streamed rather than loaded fully into memory) to get each packet's length, run it through your fixed algorithm, round the final variance to 4 decimal places, and write only that number to `/home/user/solution.txt`.