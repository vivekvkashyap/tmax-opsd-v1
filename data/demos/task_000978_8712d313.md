Treat `/app/logistic_sim` as a black box to reverse-engineer, not to copy or wrap: run it on several chosen input combinations (small/large dt, few/many steps, edge cases like y0 near K) and compare against your own implementations of candidate integration schemes (Forward Euler, Heun/RK2, RK4, etc.) computed independently in Python to see which one's output matches to 6 decimal places across many trials. A good method is to pick a single step (n_steps=1) with a large dt first, since different schemes diverge most visibly there, and binary-search which scheme's formula for `y_{n+1}` reproduces the exact binary output. Once you've identified the correct scheme, implement it in `/home/user/py_sim.py` using the exact update formula (e.g. for RK4: compute k1..k4 from the ODE `dy/dt = r*y*(1-y/K)` at increasing intermediate points and combine with weights 1,2,2,1 over 6), applied iteratively `n_steps` times, printing the final `y` with `%.6f`-style formatting. Since grading is a large-scale fuzz equivalence test across random parameter ranges, don't stop after matching one or two examples — test against dozens of varied inputs yourself (including boundary dt/n_steps values) before concluding the scheme is correct, since a wrong-but-close scheme (e.g. RK2 instead of RK4) can pass small tests but fail broad fuzzing.