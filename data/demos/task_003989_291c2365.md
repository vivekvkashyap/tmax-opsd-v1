Break this into independent, verifiable stages rather than one monolithic script. First install an offline transcription tool (e.g. `pip install openai-whisper` or SpeechRecognition+pocketsphinx) and run it on `/app/research_data/interview_sample.wav`, then normalize the output yourself (lowercase, strip punctuation) — don't trust the raw transcript casing/punctuation. Separately parse `/app/research_data/index.csv` with the csv module and `/app/research_data/metadata.xml` with `xml.etree.ElementTree`, cross-referencing both on the filename `interview_sample.wav` to get `participant_id` and `location` — verify by printing intermediate values before writing the manifest. Copy the audio file, compute its SHA256 with `hashlib.sha256(open(path,'rb').read()).hexdigest()`, and assemble the manifest JSON with exactly the required keys. For serving, write a `http.server`-based (or similar) handler bound to 127.0.0.1:9090 that checks the `Authorization` header case-sensitively for `Bearer delta-v-2024`, returning 401 with no body leak if absent/wrong, else 200 with `Content-Type: application/json` and the manifest entry. Launch it detached (e.g. `nohup ... &` or background the process) so it keeps running after your session ends, and test both the authorized and unauthorized paths with curl before finishing.
