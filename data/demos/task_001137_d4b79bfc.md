Write `deploy_migration.sh` to read `/home/user/legacy_topology.csv` line by line with a `while IFS=, read -r ...` loop, skipping the header (e.g. `tail -n +2 file | while IFS=, read ...`), and strip trailing `\r`/whitespace from each field (e.g. `tr -d '\r'` or parameter expansion) since CSVs often carry stray carriage returns that would corrupt exact-match output. For each row: `mkdir -p /home/user/services/<ServiceName>` and write `.app_profile` with the four `export` lines interpolating CloudDBHost/NewPort exactly as specified; `mkdir -p /home/user/system_configs` once, then write `<ServiceName>_proxy.conf` with the exact `[ProxyConfig]` block. Accumulate service name/port/profile-path triples as you go (e.g. into an array or temp file), then after the loop sort them alphabetically by ServiceName and emit the JSON report with `total_services` set to the row count — build the JSON manually with printf/heredoc (or via `jq -n`) to guarantee exact key/structure match rather than relying on load order. Remember `chmod +x` before running the script, and avoid any `sudo`.
