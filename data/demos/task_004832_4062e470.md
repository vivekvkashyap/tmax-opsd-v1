Write `/home/user/analyze.go` using only the standard library: `encoding/csv` to read `/home/user/data.csv`, locating the `cpu` and `latency` column indices from the header row rather than hardcoding positions. For reproducibility, read the raw file bytes separately (e.g. `os.ReadFile`) and hash them with `crypto/sha256`, hex-encoding the digest — hash the file's actual on-disk bytes, not a re-serialized version of the parsed data, since even whitespace/newline differences would change the hash. Compute Pearson's r from scratch: means of both columns, then `sum((x-mx)*(y-my)) / sqrt(sum((x-mx)^2) * sum((y-my)^2))` using `math.Sqrt`; round to exactly 3 decimals with care (e.g. `math.Round(r*1000)/1000`) rather than truncating, and format so trailing zeros still show correctly in JSON (e.g. `0.985` not `.985` or `0.98`). Apply the threshold as strictly-less-than 0.700 for PASS, else FAIL — the boundary condition matters, don't use `<=`. Marshal `{"file_hash":..., "correlation":..., "status":...}` with `encoding/json` ensuring the correlation field stays a JSON number (not a string), run the program with `go run` to produce `/home/user/report.json`, and manually recompute the hash/correlation on paper or with a second tool to sanity-check your output before finishing.
