Transcribe `/app/dataset_dictation.wav` with the `whisper` CLI to recover the actual dictated relationship sentences — don't guess the table names, read them off the transcript text file it produces. Write `/home/user/parser.c` to scan the transcript for the pattern "<TableA> joins with <TableB>" using straightforward string scanning (e.g. `strstr` for the literal " joins with ", then trim/extract the identifier tokens immediately before and after it, handling punctuation like periods), and emit `source,target` CSV lines; compile with gcc and run it to produce edges.csv. Load that CSV into a fresh SQLite `edges` table (`sqlite3 schema.db < ... ` or `.import`). Write a recursive CTE seeded from `edges` where `source='user_logs'` (or dynamically derived), expanding one hop, then join again to reach depth exactly 2 — express this as two explicit UNION steps or track hop-depth in the recursive query so you select only nodes reached in exactly 2 hops, not 1-or-2. Sort resulting target names alphabetically, dedupe, and write one per line to 2hop_results.txt; the grader compares this set via IoU so precision on which tables surface at exactly two hops matters more than extra formatting.