First inspect the schema of compliance.db (`.schema` in sqlite3) and read the recursive CTE in audit_access.py carefully — the bug is a classic missing join predicate: the recursive step selects from `SubordinateGraph s, hierarchy h` with no `WHERE` linking them, producing a cross join instead of a graph walk. Fix it by adding the correct join condition so each recursive step only extends a path through an actual manager→subordinate edge (e.g. `JOIN hierarchy h ON h.manager_id = s.emp_id`), preserving the original `manager_id` as you recurse. After fixing, re-run the script and inspect the output for sanity (a manager should only inherit access from their real reporting chain, not from unrelated employees). Then verify the final CSV is sorted alphabetically by employee name, has the exact header `employee_name,system_name`, and contains only rows for `Project_Zeus`. Don't hand-derive who "should" have access from staring at the INSERT statements alone — run the corrected query against the actual database and let it compute the answer, since hierarchy depth/inheritance direction errors are easy to introduce even in a rewritten CTE.