Build this as a clean multi-stage pipeline and be exact about each numerical definition given. Parse the FASTA with simple line-based logic (header lines start with `>`, sequence is the next line) or `Bio.SeqIO`; for each sequence use `str.find('CGTAGCTACG')` to get the first 0-based index, and discard sequences where `find` returns -1 (do not use `in` alone, since you need the index). Load the signals CSV (e.g. with `csv` or `pandas`), group by `SeqID`, and sort each group's rows by `wavenumber` ascending before extracting the intensity list — do not assume the CSV is already sorted. For the trapezoidal area, use `numpy.trapz(intensities, wavenumbers)` or the manual composite formula (half-weight the endpoints, full-weight interior points, times the uniform spacing) — get the endpoint weighting right or the area will be off. For sharp peaks, compute the second derivative only at interior indices `k` (1..n-2) using `I[k-1] - 2*I[k] + I[k+1]`, and count strictly where this value is `< -1.5`; do not include boundary points. Finally sort surviving sequences alphabetically by `SeqID`, round `TotalArea` to 1 decimal, and write the CSV with the exact header `SeqID,PrimerIndex,TotalArea,SharpPeakCount` to `/home/user/output/training_data.csv`, creating the output directory first if needed.