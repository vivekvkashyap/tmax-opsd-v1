Begin by extracting the configuration from `/app/architecture_spec.png` (OCR it or view it directly) to recover the six required values (proxy/app1/app2/monitor ports, auth token, 5xx alert threshold) — use exactly what the image specifies, don't assume defaults. Set up the filesystem next: `mkdir -p /home/user/telemetry/logs /home/user/telemetry/config` then `ln -s /home/user/telemetry/logs /home/user/active_logs`. Build the reverse proxy as a small HTTP server on the recovered `PROXY_PORT` that, for `/app1/*` and `/app2/*`, forwards the request (stripping the matched prefix) to the corresponding backend port using a client library (e.g. `requests`/`http.client`/Node `http`), relays the backend's status/body back to the caller, and appends one log line per completed request containing the numeric status code to `/home/user/active_logs/proxy_access.log` — flush/fsync so lines are visible immediately since the verifier checks the log soon after issuing requests. Build the monitoring service on `MONITOR_PORT` exposing `GET /health`: validate the `Authorization: Bearer <token>` header exactly against the recovered token (401 otherwise), then on each call re-read the full log file, count lines whose status code falls in 500-599, and return 503 with `{"alert_status":"CRITICAL"}` if that count is strictly greater than the threshold, else 200 with `{"alert_status":"OK"}`. Launch both servers as background processes (e.g. `nohup ... &`) so they remain listening for the automated verifier's HTTP tests.