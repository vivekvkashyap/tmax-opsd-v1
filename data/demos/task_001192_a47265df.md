Load the pickle with `pickle.load(open(path, 'rb'))` — since the payload is a plain list of ints, protocol-2 unpickling works directly in Python 3 with no special encoding argument needed. Write a small `is_prime(n)` trial-division helper and check both that `sum(list) == 277` and that every element passes `is_prime`. For the legacy checksum, build the comma-joined string first (e.g. `",".join(str(n) for n in data)`), then the key pitfall: do NOT iterate the Python 3 `str` and call `ord()` on each character — instead `.encode('ascii')` the string into a `bytes` object and iterate that directly, since indexing/iterating a `bytes` object in Python 3 already yields integer byte values (matching Python 2's byte-string semantics), avoiding subtle unicode-vs-bytes mismatches. Accumulate `sum(byte_value * (index + 1) for index, byte_value in enumerate(the_bytes)) % 1024`. Finally write `{"is_valid_constraint": <bool>, "legacy_checksum": <int>}` to the JSON file with `json.dump`, using native Python `bool`/`int` types so JSON serializes them as `true`/`false` and a plain integer.
