Treat `/app/query_oracle` purely as a black box you characterize by experimentation, not by disassembly. Start small: pipe tiny hand-written inputs (a `GRAPH`/edge-list block terminated by `END`, followed by one or more `CHAIN <rel1> <rel2> ...` lines) into the binary via stdin and inspect stdout closely — note the exact header line format per chain query, the ordering of result pairs (test with inputs designed to reveal whether sorting is by start node, then end node, or something else), the exact wording/format for no-match cases, and edge cases like empty graphs, self-loops, duplicate edges, or chains of length 1. Iterate with increasingly tricky fixtures (cycles, multiple edges between the same node pair with different relations, chains longer than the graph's diameter) to pin down exact semantics before writing any Python. Implement the traversal in `/home/user/graph_query.py` using an adjacency structure indexed by relation type so each `CHAIN` query is a sequence of joins/DFS-with-memoization rather than brute-force path enumeration — this matters because the verifier fuzzes graphs up to thousands of edges and requires bit-exact stdout match without timing out. Continuously diff your script's output against the real oracle on the same random inputs as you refine it.
