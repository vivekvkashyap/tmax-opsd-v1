Work through this as a checklist, verifying each item before moving to the next. First, edit `server.py`'s `ssl_context.load_cert_chain(...)` call to point at the new cert/key paths, and add a CSP header inside the Flask response — use `response.headers['Content-Security-Policy'] = "..."` (get the exact string from the task spec) on the `make_response` object before returning it, not via `after_request` unless you also register it on the right route. Second, in `client.py`, replace `verify=False` with `verify='/path/to/new_ca.pem'` — do not just swap to `verify=True`, since the new CA is self-signed and won't be in the system trust store. Third, start the server as a background process (`&`), sleep ~2s to let it bind to the TLS socket, then run the client and redirect stdout to the log file. Before declaring success, actually `cat` the result log and confirm the CSP header key/value appears verbatim and `status_code` is 200 — a common failure is a typo in the CSP string or forgetting to kill/restart a stale server process still bound to the old certs.
