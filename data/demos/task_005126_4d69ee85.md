Build the pipeline in two clearly separated stages: a Bash-driven extraction/normalization stage that walks `/home/user/raw_data/` with `find` and converts each format (CSV via `awk`, JSON via `jq`, the custom `.log` records via `awk`/`sed` state-machine parsing between `---RECORD---`/`---END---` markers, and `.dat` files by shelling out to the undocumented binary) into one unified intermediate representation (e.g. a single NDJSON or CSV file with sensor_id, timestamp, value). For the `.dat` files, don't guess the binary's internal format — just invoke it empirically first (`/app/sensor_decoder somefile.dat`) and inspect its JSON stdout to learn the output schema, then wire that into your pipeline. Second, write a small Python HTTP server that loads the unified data once at startup (or re-reads it per request) and serves `/query?sensor=<id>`, filtering, sorting by timestamp ascending, and formatting each entry as `{"timestamp": ..., "value": ...}` — return `[]` with 200 for unknown sensors. Enforce auth by checking the incoming `Authorization` header equals exactly `Bearer RES-992` before doing any work, returning 401 otherwise. Test with `curl -H "Authorization: Bearer RES-992" "http://127.0.0.1:8080/query?sensor=..."` before leaving it backgrounded.
