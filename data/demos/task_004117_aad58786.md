Tackle each vulnerability/config change independently and verify with a targeted curl before moving on. In server.c: for the open-redirect fix, validate `redirect_url` starts with exactly one `/` (not `//`) and contains no `://`; redirect to `/home` otherwise. For XSS, write a helper that HTML-entity-encodes `<`,`>`,`&`,`"`,`'` and apply it at the point `error_msg` is printed into the response, not at parse time, so no path bypasses it. In nginx.conf, add `add_header Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self'; object-src 'none'; frame-ancestors 'none';" always;` in the relevant server/location block (the `always` flag matters for error responses too). For the IP block, parse `/home/user/app/logs/auth.log` with grep/awk to count failed attempts per IP, find the IP with count > 10, and write `deny <ip>;` into block.conf (already included by nginx.conf). In redis.conf change `bind 0.0.0.0` to `bind 127.0.0.1`. Rerun `/home/user/app/start_services.sh` to rebuild and restart everything, then curl each endpoint to confirm.