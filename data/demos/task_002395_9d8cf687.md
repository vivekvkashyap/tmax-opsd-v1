Work bottom-up: fix the Rust library first, then the FFI boundary, then the build glue. In `lib.rs`, the borrow-checker error comes from a struct holding `&'a str` tokens that reference a `String` created inside the constructor and dropped at scope end — a classic dangling-lifetime bug. Fix it by making the tokens borrow directly from the input parameter's lifetime (avoid the intermediate owned `String`; trim/split the original `&'a str` argument itself so all returned references stay tied to the caller's data). Confirm with `cargo build --release` inside the Rust project directory. Next, grep both the Rust `#[no_mangle] pub extern "C" fn ...` signature and the C file's `extern` declaration/call site — they must reference the identical exported symbol name; rename the mismatched one in the C file. Finally, rewrite the Makefile so `make` first builds the Rust crate in release mode (producing a static lib under `target/release`), then compiles/links the C source against that static lib plus the standard runtime libraries typical Rust staticlibs need (threading, dynamic loading, math). Verify end-to-end by running the built binary with the given RPN expression and check the output file it produces before declaring success.
