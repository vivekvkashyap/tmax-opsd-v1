Build this in clear layers and test each independently before wiring them together. First write `roles.conf` with the exact three lines given. In `main.go`, parse `os.Args[1:]` for exactly `user, cmd, endpoint`; for the connectivity check use `net.DialTimeout("tcp", endpoint, <short timeout>)` — on error print `ENDPOINT_UNREACHABLE` and `os.Exit(2)` immediately (do this check before touching roles.conf, since the spec requires connectivity to gate the auth check). If connected, close the conn, then read `roles.conf` line by line, split each line on `:`, and for the matching `username` check whether `cmd` appears in the comma-split `allowed_commands` list; print `ACCESS_GRANTED`/exit 0 or `ACCESS_DENIED`/exit 3 accordingly, treating unknown users as denied. In `pipeline.sh`, create `bin/`, `go build -o bin/runner main.go`, start `nc -l 8080 &` (capture its PID), add a brief `sleep` to ensure it's listening before connecting, run `./bin/runner bob restart localhost:8080 > test.log`, then `kill` the listener PID. Test the binary manually against both a live and a dead port to confirm exit codes 0/2/3 before finalizing.