Handle the three parts as independent, verifiable stages. First, inspect `/app/datamash-1.8/Makefile.in` for the build target definition (search for `bin_PROGRAMS`) — a single misspelled binary name there breaks `make install`; correct it back to the real utility name, then run `./configure --prefix=/usr/local && make && make install` and confirm the binary is on PATH and runs. Second, write `/home/user/etl.sh` to group `transactions.csv` by `user_id` and sum `amount` — the key trap is that 19-digit IDs exceed double-precision float exactness and tools like `awk`/`datamash` may print them in scientific notation or lose trailing digits, so keep `user_id` as a string throughout: use `awk`/`datamash` only to compute the numeric sum per key while echoing back the original ID string unmodified (e.g. `datamash -t, groupby 2 sum 3` on properly quoted string fields, or an `awk` associative array keyed by the literal ID string), and format output with `printf`/`OFMT` set to avoid exponential notation; emit valid JSON with the IDs as string keys. Third, build `/home/user/server.sh` as a simple accept-loop using `nc -l -p 9090` (or a `socat` TCP listener) in a loop that, on each connection, reads the request, invokes `etl.sh`, and writes back `HTTP/1.1 200 OK` + `Content-Type: application/json` headers followed by the JSON body; run it with `&`/`nohup` so it persists, and test it yourself with `curl` before finishing.