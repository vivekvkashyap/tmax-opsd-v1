Diagnose the crash mechanism before patching: ALS-NMF updates solve `H = (W^T W)^-1 W^T V` and `W^T = (H H^T)^-1 H V^T`; when the true rank of V is lower than k, `W^T W` or `H H^T` becomes singular/ill-conditioned for k=4, so `np.linalg.solve` blows up. Fix by adding Tikhonov/L2 regularization: solve against `W.T @ W + 0.1*np.eye(k)` and `H @ H.T + 0.1*np.eye(k)` instead of the raw Gram matrices — this stabilizes the inversion without changing the update structure. Keep `np.random.seed(42)` exactly where it is so results stay reproducible. Add a loss-tracking loop (`loss = np.sum((V - W@H)**2)`) that breaks early once `abs(prev_loss - loss) < 1e-4`, recording loss history per iteration for both k=3 and k=4 so you can plot both curves on one matplotlib figure and save to convergence.png. For the statistical comparison, compute per-sample squared error as a row-wise sum over genes (shape (50,) per model) — not the scalar total loss — then run `scipy.stats.ttest_rel` on the two 50-length arrays and write k3_loss, k4_loss, p_value to results.json.
