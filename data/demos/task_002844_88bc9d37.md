Work through the three phases in order since later phases depend on earlier fixes compiling/running. Phase 1: in `rust_ext/src/lib.rs`, `process_string` currently declares `-> &str` but builds and returns a locally-owned `String`, which cannot outlive the function — change the signature to `-> String` (PyO3 converts owned `String` to a Python str automatically) rather than trying to fabricate a lifetime; then build/install with `maturin develop` (or `maturin build` + `pip install`) inside the project's Python env. Phase 2: add `int32 multiplier = 2;` to the `ProcessRequest` message in `service.proto`, regenerate Python stubs with `python -m grpc_tools.protoc` (writing `service_pb2.py`/`service_pb2_grpc.py` into `/home/user/project/`), then update `server.py` so `Process` reads `request.multiplier`, treating `0` (proto3's default for unset ints) as "use 1" per the spec, and forwards it to `process_string`. Phase 3: write `test_server.py` that starts the gRPC server (e.g. in a background thread), connects a client channel, and uses `@given(st.text(), st.integers(min_value=1, max_value=10))` to assert `len(response.output_text) == len(input_string) * multiplier` for every generated case. Run `pytest test_server.py -v > /home/user/test_results.log` and confirm it shows all tests passing before finishing.