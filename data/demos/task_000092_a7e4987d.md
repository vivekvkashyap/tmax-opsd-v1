Start by running `tesseract /app/config_spec.png stdout` (or convert to a clean format first if OCR is noisy) to read off `WINDOW_SIZE` and `DRIFT_THRESHOLD` — don't guess these values, verify what OCR returns against the image content structurally (e.g. sanity-check they parse as integers). Then implement the Rust binary: parse the CSV in timestamp order, and for each row maintain/compute a sliding window over rows with `timestamp` in `[current_ts - WINDOW_SIZE + 1, current_ts]` inclusive (a two-pointer or deque approach avoids O(n^2) rescans but a straightforward filter is fine for correctness first). Only emit a report line when the window's row count is strictly greater than the threshold, formatting the average with exactly 2 decimal places (Rust's `{:.2}` format specifier) to match the expected output format precisely — off-by-one on window boundaries or rounding mode is where naive attempts fail. After compiling in release mode, verify by running the binary against the CSV before setting up the cron job entry (`* * * * *` for `/home/user/drift_monitor/target/release/drift_monitor`) via `crontab -u user`.
