Diagnose top-down along the request path rather than guessing: first check why the backend crashes on startup by running `python3 /home/user/app/server.py` directly and reading the traceback — it will point at a missing data file relative path, which you fix by creating the symlink `/home/user/app/mnt -> /home/user/storage` (not a real mount, since you have no root) so the relative path resolves. Separately inspect the source for the actual listening port it binds vs. the port Nginx proxies to — read `/home/user/nginx/nginx.conf`'s `proxy_pass` target and compare it against the port in `server.py`; whichever one is inconsistent (script bound to the wrong port, or nginx pointing at the wrong port) needs to be corrected so both agree on 9000. Deploy by copying the fixed `/home/user/app/` tree (including making the symlink work when copied, or recreating it) into `/home/user/deploy/current/`, then start the backend from that directory in the background with `&`/`nohup` and confirm with `curl 127.0.0.1:9000/...` that it responds before touching nginx. Reload nginx with the exact reload command given, then curl `http://127.0.0.1:8080/api/status` and redirect the body to `/home/user/success.log`, verifying HTTP 200 before finishing.