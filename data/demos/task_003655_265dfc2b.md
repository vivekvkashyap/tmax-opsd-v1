Load all three files into in-memory dicts first: `authors.csv` -> {author_id: name}, `papers.jsonl` -> {paper_id: (year, [author_ids])}. Then stream `citations.txt` line by line, splitting each into `citing_id cited_id`; look up both papers' years and apply the validity check exactly as specified — cited paper's year strictly before 2020, citing paper's year 2022 or later. The key detail to get right: credit for a valid citation goes to the AUTHORS OF THE CITED PAPER (the paper being cited/referenced), not the citing paper — increment a counter dict keyed by author_id (or name) for every author in the cited paper's author list. After processing all citations, resolve author_ids to names via the authors dict, filter to counts >= 1, and sort with a compound key: count descending, then name ascending (e.g. `sorted(items, key=lambda x: (-x[1], x[0]))`). Write as `Name:Count` lines to `top_authors.txt`. Stick to stdlib (`csv`, `json`) only.
