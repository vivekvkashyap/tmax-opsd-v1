Build /home/user/analyze_dynamics.sh as a sequence of discrete, independently testable stages, driven by bash but calling out to ffmpeg/python for the heavy lifting. (1) Use `ffmpeg` to extract frames from /app/membrane_vib.mp4 (e.g. to PNGs or raw frames), then for each frame compute the mean grayscale intensity of the pixel box x=[270,370), y=[190,290) — do this with a small Python/PIL or numpy snippet invoked from bash rather than trying pure ffmpeg filters, so you can verify the crop coordinates precisely; write one intensity value per line to intensity_signal.txt. (2) Zero-mean the signal, run `np.fft.rfft`, compute the frequency axis from the known 60 FPS sample rate, and take the frequency bin with peak magnitude (excluding DC) as the dominant frequency in Hz. (3) For the bootstrap, resample contiguous 30-frame blocks with replacement to reconstruct a series of the same total length, recompute the dominant frequency the same way for each of 1000 resamples, then take the 2.5th/97.5th percentiles of that distribution for the CI — reusing your exact frequency-extraction function from step 2 to keep it consistent. (4) For the Laplacian, load laplacian.csv, add 0.01*I to the diagonal (Tikhonov), invert, and take the trace. Combine all four values into results.json matching the schema exactly, and chmod +x the script so it runs end-to-end unattended.
