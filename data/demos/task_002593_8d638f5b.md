This is a targeted data-leakage bug fix, not a rewrite — read `/home/user/pipeline/src/normalize.cpp` first to find exactly where min/max are computed and confirm they're currently scanning every row regardless of the `is_train` column. Fix it by computing `global_min`/`global_max` only when updating from rows where `is_train == 1` (skip test rows during the min/max pass), but still apply the resulting scaling formula to every row (train and test) in the output — don't accidentally also filter which rows get written. Update the printed metrics line to reflect the train-only min/max in the exact required format. After editing, create the build directory, run `cmake ..` then `make` (or `cmake --build .`) inside `/home/user/pipeline/build/`, confirming the `normalize_tool` executable is produced. Then fill in `/home/user/pipeline/run_experiment.sh` to invoke the tool with the CSV input/output paths, redirecting stdout (the metrics line) to `/home/user/pipeline/output/metrics.log` and writing the transformed CSV to `/home/user/pipeline/output/normalized.csv`, making sure the output directory exists beforehand (`mkdir -p`). Run the script and manually recompute train_min/train_max and a couple of scaled values by hand from the CSV to sanity-check your output before considering it done.
