Build this as three separable Python stages, each verifiable independently before moving on. Stage 1 (cleaning): load `/home/user/data/raw_data.csv` with pandas; impute NaNs in `value_1` with `value_1.median()` computed on the original column (not post-imputation); clip `value_2` to its 5th/95th percentiles using `np.percentile`/`Series.quantile` with inclusive clipping so results land exactly on those bounds; add `seq_length` as `text.str.split().str.len()` (whitespace word count); write all original columns plus `seq_length` to `/home/user/data/processed_data.csv`. Stage 2 (model + benchmark): fit `RandomForestRegressor(random_state=42)` on features `[value_1, value_2, seq_length]` against `target`, compute training MSE, then time 10 separate `.predict()` calls over the full processed dataset (use `time.perf_counter()` around each call, average the 10 durations — don't just time one call), round both metrics to 4 decimals, and dump `{"mse": ..., "avg_inference_sec": ...}` to `/home/user/metrics.json`. Stage 3 (plot): use matplotlib with the `Agg` backend (set before importing pyplot, or via `matplotlib.use('Agg')`) to avoid needing a display, scatter `seq_length` vs `target`, and save to `/home/user/scatter.png`. Run everything end-to-end so all three output files actually exist on disk.
