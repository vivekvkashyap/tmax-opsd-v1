Start by OCR'ing the image with `tesseract /app/dataset_lineage.png stdout`, then parse each output line matching the `SOURCE -> DESTINATION` pattern; since node names are stated to be uppercase-only, use that constraint to correct OCR misreads (e.g. re-map obviously wrong characters like 0/O or 1/I to the nearest valid uppercase-letter token, or manually sanity-check the parsed edge list against what's visually plausible before trusting it). Build a directed graph (adjacency list) from the recovered edges, then implement `/upstream` and `/downstream` as graph traversals (BFS/DFS) that walk edges backward or forward respectively, collecting *all* transitively reachable nodes — not just direct neighbors — then dedupe and sort lexicographically before returning JSON; watch for cycles or repeated visits by tracking a visited-set. Serve this with Python's built-in `http.server` (or Flask) bound explicitly to `127.0.0.1:8000`, launched in the background (e.g. `nohup ... &` or as a daemon) so it keeps running after you finish interacting, and verify both endpoints with `curl` before concluding, including a node with no upstream/downstream to confirm the empty-array case works.
