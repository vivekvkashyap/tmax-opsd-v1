Use fuzz.sh/generate_fuzz.py locally to reproduce both failure modes before fixing anything — one run should crash with `math domain error`, and inspect a few decoded values to see the sign-flip bug. Fix 1: the payload is documented as a big-endian signed 32-bit int, but the parser unpacks with `struct.unpack('>I', ...)` (unsigned) — change the format code to `'>i'` (lowercase = signed) so negative sensor readings decode correctly instead of wrapping to huge positive numbers. Fix 2: the variance formula `sum_sq/count - mean*mean` is a classic catastrophic-cancellation trap when readings are large in magnitude and close together — replace it with a numerically stable approach, e.g. Welford's running-variance algorithm, or compute variance as the mean of squared deviations from the mean (`sum((x-mean)**2 for x in values)/count`) which avoids subtracting two large near-equal numbers, and additionally clamp any residual negative variance to 0 before sqrt as a defensive guard. Re-run fuzz.sh repeatedly to confirm no more crashes across many random seeds, then run `python3 /home/user/sensor_analyzer.py /home/user/test.pcap > /home/user/result.txt` and check the printed stddev matches what you'd compute by hand from the correctly-decoded signed values in test.pcap.
