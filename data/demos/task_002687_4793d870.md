Split this into the build-fix half and the routing-script half, and treat them as separate verification targets. For the Makefile.in bug: grep the file for the `install:` rule/target definitions (`grep -n "install" Makefile.in`) and look for a subtly mangled target name (a rule that should read `install:` but doesn't quite) — autotools Makefile.in files typically have `install: install-am` or similar chains, so compare each rule name character-by-character rather than assuming the bug is in the recipe body. Fix only the target name, then run `./configure --prefix=/home/user/local && make && make install` fully offline (no network calls, so avoid `autoreconf`/`./autogen.sh` which may try to fetch tools — the vendored source should already have `configure` generated). Verify with `/home/user/local/bin/msmtp --version`. Write the systemd unit's `ExecStart=` to point at that exact installed binary path. For the router script, implement each rule as a case-insensitive header check (extract `List-Id:` and `Subject:` lines, lowercase them, use pattern matching) in strict priority order — no `List-Id` match falls through to the `Subject` check, and anything unmatched falls to port 8080. Test against many synthetic header variations (mixed case, header ordering, extra whitespace) before considering it fuzz-safe, since a hidden equivalence oracle will compare byte-for-byte output.
