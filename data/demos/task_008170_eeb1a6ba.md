Structure the script in clear stages: parse `$1` as the run name and create `/home/user/runs/<run_name>/` first, so every later step can write directly into it. For schema validation, use `jq` to check each JSON file has a string `id`, string `name`, and a `tags` array with length >= 1 (missing key, wrong type, or empty array should all fail); collect failing basenames, sort them, and write to `invalid.log`. For similarity, only operate on files that passed validation — extract each valid file's `id` and `tags` set, then for every unordered pair compute `|intersection| / |union|` (0 when either set doesn't overlap at all), and for each dataset pick the single best-scoring partner, breaking ties by alphabetically smallest `id` of the candidate (not of the current dataset). Format scores to exactly two decimals (careful with rounding vs. truncation, and locale-independent formatting) and sort the final TSV by the first column before writing. Compute the three metrics.txt counts from simple totals (all json files, valid count, invalid count = total - valid) rather than recomputing separately, to avoid off-by-one drift. Make the script executable and run it once with `test_run` yourself, then inspect all three output files against the spec before finishing.
