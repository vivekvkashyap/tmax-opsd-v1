Parse `edges.csv` to confirm its three columns are source, destination, weight (inspect a handful of rows), then implement Dijkstra's algorithm in C++ using a min-heap (`std::priority_queue` with a min-comparator, or negate distances) keyed on cumulative cost from node 10, storing a predecessor map so you can reconstruct the path once you pop node 500 (or after the algorithm terminates). Build an adjacency list by streaming the CSV with `std::ifstream` and splitting each line on commas (using `std::stringstream` with `getline(ss, field, ',')`), converting fields to integers. Since the graph is directed and edges may not be symmetric, be sure you only add edges in the direction given, not both ways. After computing the shortest distance and reconstructing the path by walking predecessors backward from 500 to 10 then reversing, write the result to `result.log` in exactly the format `Cost: X\nPath: 10 -> ... -> 500`. Compile with `g++` and actually run the binary yourself, then spot-check the printed path's total edge weight against the reported cost by summing edge weights along it manually or with a small script, since any path tying the true minimum cost is accepted but the cost itself must be exact.