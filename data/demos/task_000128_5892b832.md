Work in strict pipeline order: extract features first, verify the CSV looks right, then handle modeling, then serve. Use OpenCV (cv2.VideoCapture) to read /app/experiment.mp4 frame-by-frame, convert each frame to grayscale with cv2.cvtColor, and compute mean/std with numpy (frame.mean(), frame.std()) — write frame_index starting at 0 incrementally to features.csv as you go so row order matches decode order. Merge features.csv with /app/targets.csv on frame_index (inner join, matching row counts). For the leakage fix, build `Pipeline([('scaler', StandardScaler()), ('ridge', Ridge(alpha=1.0))])` and pass that whole pipeline object into `cross_val_score(pipeline, X, y, cv=5, scoring='neg_mean_squared_error')` — never call `.fit_transform` on the scaler outside the CV loop, since that's exactly the leakage bug you're fixing. Take the mean of the 5 fold scores and dump `{"cv_nmse": <float>}` to experiment_log.json. Refit the same pipeline architecture on the full merged data, pickle/keep it in memory, then stand up a FastAPI or Flask app with `POST /predict` on 127.0.0.1:8080 that builds a 2-feature array from the JSON body and returns `{"prediction": float(pred)}`; launch it detached (e.g. `nohup ... &` or uvicorn in a background subprocess) so it survives after your script/session finishes, and confirm with a curl before ending.
