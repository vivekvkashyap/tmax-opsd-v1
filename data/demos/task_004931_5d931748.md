Read `/home/user/bayesian_model.py` first to see exactly how the leak happens (scaling before split), then restructure it as: load the CSV, slice `X_train_raw, X_test_raw = X[:80], X[80:]` and same for `y`, fit `StandardScaler()` only on `X_train_raw` via `fit_transform`, and get `X_test` by calling `scaler.transform(X_test_raw)` (never `fit_transform` on test data) — this is the core fix, order of operations matters more than anything else here. For the covariance matrix, use `np.cov(X_train, rowvar=False, ddof=1)` on the *scaled* training features (note: `StandardScaler` itself normalizes with population variance, ddof=0, so the covariance of the scaled output will not be exactly 1.0 on the diagonal — that's expected, don't "fix" it). Keep the existing Bayesian linear regression posterior-mean/predict logic unchanged, just make sure it now runs on the leakage-free `X_train`/`X_test`. Round both the covariance matrix and the prediction vector to 4 decimal places with `np.round(..., 4)` before writing `{"train_feature_covariance": ..., "test_predictions": ...}` to `/home/user/results.json` (convert numpy arrays to lists first, e.g. `.tolist()`, since numpy types aren't JSON-serializable).