For Part 1, open `/app/poly_basis/setup.py` and replace the broken `Extension("poly_basis", ["poly_basis.c"])` (which references a nonexistent .c file) with `from Cython.Build import cythonize` plus `ext_modules=cythonize("poly_basis.pyx")`, keeping `include_dirs=[np.get_include()]`; then `pip install .` (or `python setup.py build_ext --inplace`) from that directory and confirm with a quick `python -c "from poly_basis import expand; print(expand(...))"` that it imports and runs. For Part 2, implement each numbered step literally and in order — arg parsing to float64 array, `expand(x, degree=3)`, loading both `.npy` files, `scipy.linalg.cholesky(S, lower=True)` for L, `scipy.linalg.solve_triangular(L, x_poly, lower=True)` for z, then `posterior_weights @ z`. The trickiest fidelity point is the bootstrap: seed `np.random.default_rng` exactly as specified (an integer derived from `sum(abs(x))*1000`), then draw resamples in a loop calling `rng.choice(..., replace=True)` — using a different RNG call pattern (e.g. `rng.integers` for indices instead of `rng.choice`) will silently desynchronize the random stream from any reference implementation, so match the exact API calls in the spec. Format the final print with `%.4f` on all three numbers, no extra prints.
