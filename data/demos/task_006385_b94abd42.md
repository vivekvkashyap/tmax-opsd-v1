Treat each phase as independently verifiable — check your work with `grep`/`openssl` before moving on. Phase 1: write `redact.sh` using `sed`/`grep -oP` with two regex substitutions — one matching `password=<alnum>+` and replacing with `password=REDACTED`, another matching `Bearer <token chars including . - _>+` and replacing with `Bearer REDACTED` — run it against `access.log`, redirect to `access_redacted.log`, and confirm the original secrets are gone and the redacted markers are present. Phase 2: decrypt with `openssl enc -d -aes-256-cbc -pbkdf2 -pass pass:compromised_pass_123 -in config.enc -out <plaintext>`, edit only the `DB_PASSWORD=` line to the new value (leave every other line untouched) into `config_updated.txt`, then re-encrypt with the *new* password and the same `-pbkdf2` flag into `config_new.enc` — a mismatched `-pbkdf2` flag between encrypt/decrypt is the most common way this silently fails verification. Phase 3: edit `sshd_config` in place so the final file contains the lines `PermitRootLogin no` and `PasswordAuthentication no` exactly (uncomment/replace/add as needed, and remove or overwrite any conflicting `yes` lines rather than leaving both) — the verifier does exact-line grep matching so stray comments or trailing text on those lines will fail it. Generate the keypair with `ssh-keygen -t ed25519 -N "" -f /home/user/incident_response/new_keys/id_ed25519`.
