Start by running `make` in `/app/vendored/log-processor-2.0` to see the exact build error; since the source uses `pthread_create`, check the `Makefile`'s compile/link line for a missing `-pthread` (or `-lpthread`) flag and add it, then rebuild and confirm `./log-proc /app/sample_clean.log` exits 0. Study `main.c`'s record-parsing loop to find how it advances through the file (an offset field pointing to the next record) — the hang/race happens when that offset is <= the current position or beyond EOF, causing an infinite loop instead of terminating. Use the clean vs. evil sample logs (hexdump both, e.g. `xxd /app/sample_clean.log` vs `xxd /app/sample_evil.log`) to precisely determine the binary layout (header, per-record offset field size/endianness) before writing `classifier.c`. Your classifier should parse the same structure independently in C, walking record-to-record and checking at each step that the next offset strictly increases and stays within file bounds; exit 1 on any violation (including truncated/malformed headers), exit 0 only if the entire file parses cleanly to EOF. Compile with `gcc -o /home/user/classifier /home/user/classifier.c` and validate against both samples before trusting it on the corpora.
