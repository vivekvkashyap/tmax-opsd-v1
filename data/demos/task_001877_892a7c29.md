Set up the Rust project with `cargo init /home/user/storage_manager` and add `flate2` (for gzip decompression) and `serde_json` (for the report) to `Cargo.toml`. In `main.rs`, glob or read-dir over `/home/user/logs/*.gz`, wrap each file in `flate2::read::GzDecoder` then a `BufReader` to stream lines, and parse multi-line records by tracking state as you go: when you see a line matching `Caused by: DiskQuotaExceeded`, remember that the *current* record is a quota-exceeded record, and when you subsequently see `File: <path>` *within the same record* (before the next timestamped `[...]` line starts a new record), capture that path — don't just scan for "DiskQuotaExceeded" and "File:" independently anywhere in the file, since a File: line belongs to whichever record it's nested under. Rename each matched file by appending `.archived` with `std::fs::rename`. Collect the *original* paths (pre-rename) of successfully renamed files, sort them alphabetically, serialize as a JSON array with `serde_json`, write to a temp file in the same directory (e.g. `archived_report.json.tmp`), and then `std::fs::rename` it atomically onto `/home/user/archived_report.json` so no partial write is ever visible. Build with `cargo build --release` and run the binary to actually perform the renames and produce the report.
