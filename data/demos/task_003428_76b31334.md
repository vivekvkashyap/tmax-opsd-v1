Recover the nucleotide weights with `tesseract /app/reference_params.png stdout` and parse `A=`, `C=`, `G=`, `T=` out of the OCR'd text (watch for OCR mangling negative signs). Implement the pipeline in this exact order, since fuzz-equivalence checks byte-for-byte output: map sequence chars to weights, take `numpy.fft.fft`, compute `|X[k]|**2`, normalize to Q, then compute base-2 KL divergence against uniform `1/L` using `Q + 1e-9` only inside the log per the given formula (don't add epsilon anywhere else). Call `numpy.random.seed(seed)` once before the loop, then run exactly 100 bootstrap iterations resampling L indices with replacement (`numpy.random.choice(L, L, replace=True)`), rebuilding the array and recomputing D each time. Sort the 100 D values and take index 94 by rank (not `numpy.percentile`, which interpolates differently), printing it rounded to 4 decimals with no extra output. RNG call order must match a hidden reference exactly — verify against the worked example before trusting it.