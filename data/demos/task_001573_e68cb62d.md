Work the three layers independently, then wire them together. First, diagnose the service chain: curl the Flask app directly on its own port to confirm it works, then curl through Nginx on 8080 and compare — a working direct call plus a failing proxied call means the bug is in nginx.conf's `proxy_pass` target (check it points at the actual Flask port, not a typo'd one), not in the app itself; restart via the provided start script after editing. Second, for the C tool, use `apt-get install` (or equivalent) to get `libhdf5-dev` and `libfftw3-dev`, then compile with `gcc calc_spectrum.c -o /home/user/bin/calc_spectrum -lhdf5 -lfftw3 -lm` (adjust include/lib paths with `pkg-config` or `h5cc` if linking fails), and always check `nm`/`ldd` or run a smoke test against one sample HDF5 file to confirm the power-spectrum output shape looks sane before wiring it into the classifier. Third, in the Bash script, iterate `*.h5` files, pipe the C tool's CSV output into a JSON array using `jq` or manual string building for the POST body, curl the API through the Nginx URL (not directly to Flask, since that's the required path), extract `p_value` from the JSON response with `jq -r`, and apply the strict `<0.01` threshold before copying clean files.
