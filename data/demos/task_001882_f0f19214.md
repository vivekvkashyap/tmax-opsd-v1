Start by extracting ground truth from the image rather than guessing model parameters: run `tesseract /app/architecture.png stdout` (or similar OCR) and carefully read off the perceptron's weight_x, weight_y, bias, and the exact anomaly threshold/comparison it specifies — get these values from the OCR output, not assumed defaults. Then:
1. Implement `/home/user/detect_anomaly.sh` to take a JSON file path, parse `x` and `y` with `jq`, compute the linear combination using the recovered weights/bias, compare against the recovered threshold with the correct comparison operator (e.g. `>=` vs `>`), and exit 0 for clean / 1 for anomaly, with strictly no stdout output on success paths (bash `bc` or `awk` for the floating-point arithmetic works well since bash itself can't do floats).
2. Validate against `/app/corpus/clean/` and `/app/corpus/evil/` yourself, checking exit codes for every file, and fix any misclassifications before considering the detector done — 100% accuracy on both corpora is required.
3. Write `/home/user/bootstrap_bench.sh` to randomly sample 50 files with replacement from a given directory argument, time the total wall-clock across 50 invocations of `detect_anomaly.sh`, and write the total in milliseconds to `/home/user/bench.log`.
