Before automating, run `/home/user/legacy_deploy.py` manually once to see the exact prompt text and ordering, since `pexpect` matching must line up with the real strings byte-for-byte (trailing spaces/colons matter). Use `pexpect.spawn('/home/user/legacy_deploy.py')` (or `python3 /home/user/legacy_deploy.py`), then for each of the four prompts call `child.expect("Enter service name to migrate: ")` followed by `child.sendline(...)`, repeating for the version, pre-checks, and online prompts in order — a common mistake is using `expect` patterns that are substrings appearing in multiple prompts, causing pexpect to match the wrong prompt; anchor on the full distinctive text of each. After each interaction, call `child.expect(pexpect.EOF)` (or check exit status) to ensure the subprocess actually finished before moving to the next service, otherwise you can race the log file being written. Loop this over `worker-1`, `worker-2`, `worker-3` with version `v2.1`, `yes`, and `y`, then run your script and confirm `/home/user/migration_log.json` shows all three as `v2.1`/`online`.
