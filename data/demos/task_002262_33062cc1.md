Fix the four bugs one at a time and re-run after each change to confirm no crashes. First, the headless plotting failure: set `matplotlib.use('Agg')` before importing pyplot (or before any figure creation) and replace `plt.show()` with `plt.savefig('/home/user/pca_plot.png')`. Second, bootstrap sampling: use `df.sample(n=1000, replace=True, random_state=42)` — get the parameter names exactly right (n, replace, random_state) since the grader checks reproducibility. Third, dynamic component selection: fit PCA with no `n_components` cap (or `n_components=None`) so you get the full `explained_variance_ratio_` array, take its cumulative sum, and find the smallest index where cumulative variance is `>= 0.85`, then set `n_components` to that count+1 (1-indexed). Fourth, write `/home/user/tracking.json` with keys `n_components` (int) and `variance_explained` (float, the cumulative value at that chosen index) via `json.dump`. Run the whole script end to end, then inspect both output files exist and are non-trivial (png size>0, json parses) before considering it done. Do not touch `data.parquet`.
