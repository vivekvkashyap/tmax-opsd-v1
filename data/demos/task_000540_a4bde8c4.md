First transcribe the voicemail: play/inspect `/app/voicemail.wav` with a speech-to-text tool (or `ffprobe`/manual listening if transcription tooling is unavailable) to recover the four spoken words, then uppercase and hyphen-join them to form the token — do not guess a token value. Build the HTTP server with Python's stdlib `http.server` (or a minimal framework), binding strictly to `127.0.0.1:9000`. In the request handler, check the `Authorization` header equals `Bearer <TOKEN>` before doing any work; return 401 immediately otherwise. For `/build-order`, load `/home/user/mobile_graph.json` and run Kahn's algorithm for topological sort: maintain a set/list of nodes whose dependencies are all already placed, and at each step pick the alphabetically smallest ready node (use a min-heap or sort the ready set each iteration) rather than relying on dict/insertion order, which is the most common bug here. Return the resulting list as a JSON array with HTTP 200. Launch the server as a background process (e.g. `nohup python3 server.py &` or a subprocess) so the shell remains free for the verifier to issue curl requests.