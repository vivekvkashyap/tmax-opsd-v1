Fix the script in two targeted spots rather than rewriting it. First, the precision bug: `bc` defaults to `scale=0` for the expression given, so wrap the calculation with an explicit scale, e.g. `echo "scale=4; ($curr_val * 0.9) + ($val * 0.1)" | bc`, so intermediate arithmetic keeps 4 decimal places instead of truncating to integers before your `printf` formatting ever runs. Second, the corruption bug: before feeding `$val` into `bc`, validate it's a proper float (e.g. a regex check like `[[ "$val" =~ ^-?[0-9]+\.?[0-9]*$ ]]`) and `continue` the loop (skip updating state and don't log an error that halts the script) when it isn't, so one bad WAL line doesn't abort the whole run. Always rebuild state by starting fresh from `initial_state.csv` (the script already does `cp initial_state.csv current_state.csv` at the top — keep that), replay every WAL line in order applying the EMA formula sensor-by-sensor, then copy or rename the resulting state file to `final_state.csv` in `SENSOR_ID,EMA_VALUE` format. Hand-trace the WAL by calculator to sanity-check your script's output before trusting it.
