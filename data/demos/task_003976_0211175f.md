Do the extraction as plain shell first, decoupled from the C++ part: `tar -xzf /home/user/docs_incoming.tar.gz`, then unzip each `.zip` found, pulling out only `*.md` files into `/home/user/processed_docs/`. Before renaming, list the extracted files and sort by their ORIGINAL filenames alphabetically (not by discovery/extraction order, which depends on zip iteration order) — that sorted order determines the `module_NN.md` numbering. Rename sequentially, then `ln -s module_0N.md /home/user/processed_docs/current_module.md` pointing at the highest-numbered file. For the C++ program (`/home/user/make_index.cpp`), use `<filesystem>` (or `dirent.h`) to list the directory, keep only regular files matching `.md` and explicitly exclude symlinks (check `is_symlink`/`is_regular_file`, not just extension) so `current_module.md` doesn't leak into the index. Sort names, write them to `index.tmp` in the same directory, `fflush`/close the stream, then call the POSIX `rename("index.tmp", "index.txt")` to atomically publish it — this atomicity is the graded requirement, so don't write directly to `index.txt`.
