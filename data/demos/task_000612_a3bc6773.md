Two separable problems: fix a broken build, then hit a hard performance target. For the library, inspect `/app/libfastmerge/Makefile` — a shared library build needs position-independent code (`-fPIC` on the compile step) and the link step must invoke `-shared -o libfastmerge.so` rather than producing a static `.a`; fix both and confirm with `file libfastmerge.so` / `nm -D`. For the C++ organizer, parse the JSON requests, validate paths by resolving them (e.g. realpath) and rejecting anything that escapes `/home/user/project_files/` via `..`. The performance requirement (under 2s for 10,000 requests where each merge costs ~0.5ms of CPU work) cannot be hit single-threaded — dispatch the merge calls across a thread pool or via `std::async`/`std::thread` with a bounded number of workers matching available cores, and make sure I/O (reading files, writing the JSONL) doesn't serialize the whole pipeline. Link with `-L/app/libfastmerge -lfastmerge` and set rpath (`-Wl,-rpath,/app/libfastmerge`) so the binary resolves the .so without manually exporting LD_LIBRARY_PATH at runtime.
