Work top to bottom, verifying each stage before the next. (1) Read `/app/routes.txt`, use `grep '^ACTIVE: '` plus `awk`/`sed` to isolate the route path after the prefix, and write it into `/app/legacy-svc/route.h` as `#define ROUTE_PATH "<route>"` — do this with a script, not by hand-typing, so it stays correct if the file changes. (2) Try `make` first to see the actual compiler error; a "missing threading flag" bug in a Makefile using `cpp-httplib` almost always means `-pthread` is absent from `CXXFLAGS` (or the link step) causing undefined references to pthread symbols — add `-pthread` and rebuild until `server` compiles cleanly. (3) Run `./server` in the background and confirm with `curl http://127.0.0.1:8080/api/v1/system/health` (or the ACTIVE route from step 1) that it responds and appends to `/home/user/app.log`. (4) Set up `socat TCP-LISTEN:9090,fork,bind=0.0.0.0 TCP:127.0.0.1:8080` in the background, then re-verify via port 9090 externally. (5) Write `/home/user/logrotate.conf` with exactly the requested directives (daily, rotate 3, compress, missingok, size 10M) scoped to `/home/user/app.log`. Confirm both `server` and `socat` processes remain alive at the end.