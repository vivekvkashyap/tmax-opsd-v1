Don't assume one encoding for all files — detect per file. Either try decoding with utf-8, then iso-8859-1, then utf-16 in sequence (catching UnicodeDecodeError) or use `chardet`/`charset_normalizer` to sniff each file before parsing its CSV rows; validate by checking accented characters render correctly, not just that decoding didn't throw. Apply the three validity constraints in order — id is all-digits and non-empty (`str.isdigit()`), email has exactly one `@` and a `.` after it, rating parses as int in [1,5] — and route failing rows to rejected (unmasked) vs passing rows to processed (masked). For masking, split email on `@` and replace only the local part with `***`, keep the domain; for phone, keep the last 4 characters and replace everything before them with `*` per original character (not per digit), handling the "<=4 chars: unchanged" and "empty: unchanged" edge cases explicitly. Write both outputs with Python's `csv` module and `encoding='utf-8'`, and iterate input files in a deterministic order (e.g. sorted glob) so masking logic is applied consistently regardless of file layout.
