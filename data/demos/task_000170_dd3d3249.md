Work through this pipeline in dependency order: bring MongoDB up first (mkdir the dbpath, `mongod --fork`), confirm it's listening before seeding, then load `seed.json` into `analytics_db.activity_logs` (a small pymongo script with `insert_many` is more reliable than shelling out to `mongoimport` for a JSON array). Build the aggregation with `$group` on `user_id`, computing `total_actions` (count) and `latest_action_date` (max timestamp) — before running it, create an index on `user_id` with `create_index`, then call `.explain("executionStats")` on the *exact* pipeline you use and dump that dict as JSON to `/home/user/query_plan.json`. Define the Pydantic `UserStats` model with strict types so records with a missing timestamp or a non-string `user_id` are rejected by `ValidationError` and simply skipped, not crashed on — wrap each validation in try/except. For the SQLite step, the naive bug is spinning up one thread per record each opening its own connection with `BEGIN EXCLUSIVE`, which serializes threads into lock contention/deadlock; instead, do the aggregation/validation in Python, collect all valid rows, and perform a single-threaded (or single-connection) batched `executemany`/`INSERT OR REPLACE` inside one transaction. Verify by counting rows and re-running the script to confirm idempotency and no lock errors.
