Build the pipeline in stages inside your script and inspect intermediate output at each step rather than writing it all blind. First join: use `awk -F, 'NR==FNR{...}FNR>1{...}'` (or `join` after sorting both files on DeviceID) to merge device_info.csv and inference_logs.csv on DeviceID, then filter rows where MatrixSize<=0 or InferenceTimeMS<=0 with a numeric comparison in awk (avoid string comparisons). Compute GigaFLOPS as `(2*MatrixSize^3)/(InferenceTimeMS*10^6)` inside awk using `printf "%.3f"` for the 3-decimal rounding, and write the metrics.csv with the exact header order given. For the bootstrap: within awk (or a small loop calling awk), for each of 100 iterations draw N samples with replacement from the ARM64 GigaFLOPS values using `rand()`-driven random indices (seed with `srand()`), average each sample, accumulate the 100 sample-means, then divide by 100 and round to 3 decimals for bootstrap_arm64.txt. Since the grader tolerates ±1.5 from the true population mean, don't over-engineer the RNG — just make sure sampling is genuinely with replacement (index can repeat) and the sample size equals the ARM64 row count, not some other value.
