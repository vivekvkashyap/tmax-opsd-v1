Use OpenCV (`cv2.VideoCapture`) to read all 120 frames and, per frame, count pixels equal exactly to (255,255,255) — that count is one cache-miss sample; derive your dataset this way instead of assuming a distribution shape. In C, implement the standard Gamma MLE iteration: compute the sample mean and mean-of-log, then Newton-Raphson update `k` on the equation `ln(k) - digamma(k) - (mean_log - ln(mean)) = 0` with derivative `1/k - trigamma(k)` (link GSL's `gsl_sf_psi`/`gsl_sf_psi_1`), iterating until `|Δk| < 1e-6`, then set `theta = mean/k`. Have the C program print k and theta so the Python plotting code and the HTTP server both consume the *same* fitted values (e.g. read from a file the C program writes) rather than re-deriving parameters separately in each language. Build the histogram+PDF overlay in matplotlib. For the server, check the Authorization header on every route first and return 401 immediately if missing/wrong; only create server_ready.txt after the listening socket is actually up.
