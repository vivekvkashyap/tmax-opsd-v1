Build this bottom-up: proto first, then a pure-Python scoring function you can unit-test in isolation before wiring gRPC around it, since a scoring bug will be invisible once buried in RPC plumbing. Write the `.proto` exactly to spec and compile with `python -m grpc_tools.protoc` (need `-I`, `--python_out`, `--grpc_python_out` all pointing at `/home/user/`). For the scorer: process lines in order, strip whitespace, strip inline `;` comments, skip blank/comment-only lines; detect a label as a line ending in `:` and record its name plus the sequential position among *processed* (non-comment) lines — this ordering matters for the backward-jump check. For each instruction line, split on whitespace, treat the first token (lowercased) as the mnemonic and the second token (strip trailing commas) as the first operand; look up its cost from the given cost table (unlisted mnemonics cost 0, labels cost 0). Only jump mnemonics whose operand matches a label already seen earlier apply the 1.5^N multiplier — compute N as a count of such backward jumps, not a binary flag, then apply Base * 1.5**N once at the end. Wire the server on port 50051, then write a client that reads `.asm` files from `/home/user/snippets/` in sorted filename order and appends `filename: score` (rounded to 2 decimals) to `/home/user/results.log`.