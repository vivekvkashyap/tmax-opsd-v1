Build this as a real push-triggered deploy pipeline, not a one-off manual run. Steps: (1) write `/home/user/monitor.git/hooks/post-receive` as a shell script that on stdin (ignore the ref line content, just trigger on invocation) does: `mkdir -p /home/user/deploy /home/user/bin`, then `git --work-tree=/home/user/deploy --git-dir=/home/user/monitor.git checkout -f master` (or the pushed ref) to materialize the source into the work tree, then `g++ /home/user/deploy/monitor.cpp -o /home/user/bin/monitor`, then `chmod 755 /home/user/bin/monitor`; `chmod +x` the hook file itself. (2) `git clone /home/user/monitor.git /home/user/workspace`. (3) Write `monitor.cpp`: read the integer PID from `/home/user/target.pid`, build the path `/proc/<pid>/comm`, read its contents, strip trailing whitespace/newline (careful — `comm` often already ends in `\n`, trim it precisely), then open `/home/user/alerts.log` in append mode and write the exact literal format `ALERT: Process <name> (PID <pid>) detected\n`. (4) `git add`, `commit`, and `push origin master` from `/home/user/workspace` — verify the hook actually compiled and deployed by checking `/home/user/bin/monitor` exists after the push. (5) Execute `/home/user/bin/monitor` once so the log line appears.