Build and validate each layer in isolation before wiring the pipeline together. Write `analyzer.cpp` to read the CSV with no header, parsing each line's three comma-separated fields (string ID, and two floats), and print the ID only when `cpu < 5.0 && mem < 50.0` (note strict less-than, so boundary values like exactly 5.0 or 50.0 must NOT be flagged) — compile and test it manually against a small CSV before trusting it. Write `optimizer.sh` to check `[ -f /home/user/metrics.csv ] || exit 1` first (this must be the very first real check so the script exits early when the CSV is missing), then compile with `g++ /home/user/analyzer.cpp -o /home/user/analyzer`, run it on the metrics file, and pipe each output ID through a loop appending `STOPPED: <id>` lines to `/home/user/stopped_containers.log`. Make the script executable with `chmod +x`. For the cron entry, write a line matching `0 2 * * * /home/user/optimizer.sh` into `/home/user/finops.cron` (no need to install it into an actual crontab, just create the file). Test the whole chain end to end: delete any old log/binary, run `optimizer.sh`, and diff the resulting log against your expectation from the CSV thresholds; also test the missing-CSV case to confirm the exit-code-1 path.