Don't just eyeball the C source — reproduce the crash under a memory sanitizer first so you know exactly which write is out of bounds: compile with `gcc -fsanitize=address -g -o /home/user/parse_logs_asan /home/user/parse_logs.c` and run it against `/home/user/attack_logs.dat`, then read the ASan report to pinpoint the overflowing buffer and the offending write. Look closely at the fixed-size `token[]` buffer used to accumulate characters between colon delimiters in the manual state machine — one of the attack-type strings in the log is longer than that buffer, so the fix is to size the buffer generously (or make it dynamic/bounds-checked) rather than restructuring the parsing state machine itself, since the task says the state machine logic should stay intact. After patching, recompile the real binary with plain `gcc -o /home/user/parse_logs /home/user/parse_logs.c`, rerun it, and confirm it no longer crashes and `/home/user/malicious_ips.txt` is produced with one "Detected <attack_type> from <ip>" line per log entry, in the same order as the input file.