Treat this as a data-join bug, not a rewrite: keep the existing project structure and dependencies, but replace the nested loop that pairs every author with every paper with a read of the actual mapping file. Use the `csv` crate to parse `authors_papers.csv` row by row, collecting (author_id, paper_id) pairs as integers (parse the string fields with `.parse::<i64>()`, not strings) — this avoids the join being either a cross product or accidentally comparing types incorrectly. Build a `serde`-derived struct/Value for the required output shape: a top-level object with a fixed `query` string (copy it exactly as specified, including whitespace) and a `parameters.batch` array of `{author_id, paper_id}` objects. Sort the collected pairs by author_id ascending, then paper_id ascending, before serializing — do this in Rust with a `sort_by` on a tuple key rather than relying on file order. Serialize with `serde_json::to_writer_pretty` (or to a String and write) to `/home/user/graph_exporter/output.json`. Finally, run `cargo run` (or build+execute the binary) from within `/home/user/graph_exporter/` so the file is actually produced, and inspect the resulting JSON to confirm integer types and ordering before finishing.