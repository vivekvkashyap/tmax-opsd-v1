Use pandas for this: read the log line-by-line with a regex like `r'(\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2}).*metric=(\w+) val=([\d.]+)'`, dropping non-matching lines. Build a long-format DataFrame (timestamp, metric, value), parse timestamps with `pd.to_datetime`, then pivot to wide format with `df.pivot_table(index='timestamp', columns='metric', values='value')`. For resampling, first set timestamp as the index, then use `.resample('5min', label='left', closed='left')` (or equivalent) restricted to the exact bin edges 10:00 through 10:20 — construct the bin edges explicitly with `pd.date_range` and reindex/resample against them rather than trusting resample's default range, since resample will otherwise infer bin boundaries from the data's own min/max timestamp, which can shift the first or last bin. Aggregate each bin with `.mean()`, then `.ffill()`, then `.fillna(0.0)` for any leading NaNs (metrics with no reading before their first ffillable value). Round with `.round(1)`, format the timestamp index as strings in `YYYY-MM-DD HH:MM:SS`, and write columns in the exact order `timestamp,cpu,memory,temperature` to the CSV with `index=False`. Verify the row count is exactly 5 (10:00 to 10:20 inclusive, 5-min steps).