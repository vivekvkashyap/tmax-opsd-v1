Fix the vendored package first: inspect `/app/vendored/struct-stats-1.1.0/setup.py` for an unsatisfiable pin (a numpy version constraint that can't resolve on a modern system) and relax it to a normal `>=` constraint, then `pip install -e .` (or build+install) from that directory so `import struct_stats.hypothesis` works. Parse `reference.pdb` yourself with simple fixed-column slicing on `ATOM` records (columns 13-16 for atom name, 31-54 for x/y/z per the PDB spec) filtering for `CA` atom name, rather than assuming a library is available — collect the CA coordinates into an array. For the Monte Carlo step, call `numpy.random.seed(42)` exactly once, then generate the WT ensemble (10,000 draws of Gaussian noise, std=0.2, added to CA coords) before generating the Mutant ensemble (std=0.5) in the same script run without reseeding, so the two draws consume the RNG stream in sequence as specified. For each conformation compute Rg as the RMS distance of atoms from their centroid, then call the installed package's `struct_stats.hypothesis.permutation_test(wt_rg_values, mutant_rg_values, n_permutations=5000)` and write its returned statistic and p-value into the exact JSON shape at `/home/user/results.json`.
