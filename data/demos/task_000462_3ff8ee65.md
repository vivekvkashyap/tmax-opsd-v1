First locate the offending SQL in the vendored package by grepping for `INDEXED BY` under `/app/sqlite-graph-builder-0.4.5` — you'll find it forces use of a corrupted index in the recursive-CTE query used to fetch child rows. Edit that file to remove the `INDEXED BY idx_parent_stale` clause (and its variants on each SELECT it appears in) so SQLite falls back to its normal query planner, then reinstall the package editable (e.g. `pip install -e /app/sqlite-graph-builder-0.4.5`) so the fix takes effect for imports. Sanity-check the fix by running a simple query against taxonomy.db directly and confirming it no longer errors and returns all expected child rows for a node with known children.

For fetch_subtree.py, either call the fixed package's function or write your own recursive CTE (`WITH RECURSIVE ... SELECT id, name, parent_id FROM taxa WHERE id=? UNION ALL ... JOIN ON parent_id`) against taxonomy.db, then reconstruct the nested tree in Python: build a dict mapping parent_id to list of child rows, and recursively assemble `{"id":..., "name":..., "children":[...]}`, sorting each node's children list by `id` ascending before returning it. Use `json.dumps` with no extra whitespace/logging on stdout, and print nothing else — any stray `print` (e.g. debug output) will break the bit-exact comparison against the oracle.