This task has two independent deliverables driven by the same audio file, `/app/recording.wav` — transcribe it first (e.g. with `whisper` CLI) to recover the exact RAM size, disk size, and the specific SSH directive it describes (it's about rejecting key-based login, which maps to a concrete sshd_config/cloud-init setting — figure out which one from context, don't guess a different mechanism). For compression, encode with `ffmpeg`/`opusenc` to Ogg Opus using a low bitrate (e.g. well under 24kbps mono, tuned for voice/VOIP) to hit the required opus/wav size ratio while keeping speech intelligible enough for Whisper to still transcribe it accurately — too aggressive a bitrate will blow the WER budget, so test round-trip transcription yourself before finalizing. For `/home/user/setup_node.sh`: make it executable, accept the instance name as $1, export `QEMU_VM_NAME`, create a `qcow2` image of the transcribed disk size via `qemu-img create -f qcow2 <name>.qcow2 <size>`, write a cloud-init user-data file applying the transcribed SSH rule (verify the exact directive syntax), and echo the correct `qemu-system-x86_64` command line with `-m <transcribed RAM>`, the disk image, and `-nographic`.