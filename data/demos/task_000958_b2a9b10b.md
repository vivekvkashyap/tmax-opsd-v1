Split this into two independent extraction tasks and format their outputs exactly as specified. For the ELF entry point: write C that opens the file, reads (or mmaps) the first `sizeof(Elf64_Ehdr)` bytes into an `Elf64_Ehdr` struct from `<elf.h>`, and prints `e_entry` with `printf("Entry: 0x%lx\n", (unsigned long)hdr.e_entry)` — use lowercase hex with no leading zeros to match what `readelf -h`'s "Entry point address" field shows, and sanity-check your program's output against `readelf -h /home/user/server_bin` yourself before trusting it. For the certificate: run `openssl x509 -in server.crt -noout -enddate`, which prints `notAfter=<date>` — strip the `notAfter=` prefix (e.g. with `cut -d= -f2`) so you're left with just the date string, and use that exact string (do not reformat the date) after the `Expiry: ` prefix. Assemble `audit_report.txt` as exactly two lines with no extra whitespace or trailing blank lines — build it programmatically (e.g. `{ ./elf_audit server_bin; echo "Expiry: $(openssl x509 ... | cut -d= -f2)"; } > audit_report.txt`) rather than typing by hand, then diff your report's two lines against a fresh independent run of the same commands to confirm they match exactly.
