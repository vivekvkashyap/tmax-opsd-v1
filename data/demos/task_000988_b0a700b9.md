Use a real CSV parser (Python's `csv` module, pandas, etc.) rather than splitting on newlines — a naive line-by-line reader will break on the embedded newlines inside quoted `notes` fields and misalign rows. Read rows in file order (not re-sorted) to correctly track "first time a hash is seen" for deduplication, since dedup must respect chronological/original row order even though final output is sorted by timestamp. Apply the three normalization steps in the exact order given (strip whitespace, then `x`→`*`, then `**`→`^`) — order matters because normalizing `x` before collapsing `**` avoids double-mutating characters. Compute SHA-256 over the normalized string (not the raw one) and drop duplicate rows entirely rather than merging them. For the rolling average, sort surviving deduplicated rows by timestamp first, then slide a window of up to 3 preceding+current lengths — don't include dropped duplicate rows in the window. Round only at final output time to 2 decimals, using proper rounding (not truncation) to avoid off-by-hundredth errors.
