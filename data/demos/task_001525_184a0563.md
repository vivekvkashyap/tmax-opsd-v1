Diagnose the leak with tooling, don't guess: compile `parser.c` with `gcc -Wall -Wextra` first to see if any warnings hint at the issue, then run `valgrind --leak-check=full ./parser /home/user/data.txt` and read the "definitely lost" blocks it reports, which will point at the exact `malloc`/`strdup` calls whose memory is never freed. Fix the leak by freeing every dynamically allocated field before the enclosing struct is freed (e.g. free each `strdup`'d string, then free the struct itself), and re-run valgrind to confirm 0 leaks and exit code 0 with `--error-exitcode=1`. For `run_ci.sh`: read the version string from `version.txt`, split on `.` into major/minor/patch and compare numerically (e.g. with arithmetic comparisons or `sort -V` against `2.0.0`, being careful that `sort -V` lexical/version comparison handles multi-digit components correctly), exiting 1 immediately if below threshold. Then compile with `gcc`, run valgrind with the exact required flags against `/home/user/data.txt`, check `$?` from valgrind (not from the program alone), and only on success write `CI SUCCESS: <version>` — using the literal string read from `version.txt` — to `ci_report.txt`. Test the whole script end-to-end and confirm both the exit code and file contents match expectations.