First recover the transformation rules from the audio, don't guess them. Transcribe `/app/instructions.wav` (e.g. `ffmpeg` to extract/convert audio, then a speech-to-text tool such as `openai-whisper` if available, or listen carefully if you can play it) and write down precisely: the exact magic header string/bytes to prepend, the exact byte-level transformation rule (which byte ranges are altered and how), and the exact compression algorithm named. Read the transcript multiple times/re-run STT if unsure of any spelled-out word, since header bytes and the transform rule must be bit-exact.

Then implement `/home/user/archiver.py` reading `<input_file>` in binary mode (`'rb'`), applying the transformation byte-by-byte or via a translation table (e.g. `bytes.translate` with a custom map) over the raw data, prepending the header bytes to the transformed data (verify prepend order — header untransformed, then transformed body, unless the audio says otherwise), then compressing the full payload with the named algorithm's standard library module and writing raw compressed bytes to `<output_file>` with no extra newlines. Test round-trip and byte-exactness against multiple input sizes including empty input before finishing.
