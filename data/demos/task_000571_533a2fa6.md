Automate the interactive script with `expect` rather than trying to pipe answers via echo/heredoc (which is fragile against `read -p` prompts): write `/home/user/auto_deploy.exp` using `spawn /home/user/deploy.sh`, then `expect "Environment"` / `send "prod\r"`, `expect "Confirm start"` / `send "y\r"`, followed by `expect eof` (or `interact`) so the script doesn't exit before the backend actually launches in the background. Diagnose the 502 by checking what port the Python backend really bound to — use `ss -tlnp` or `lsof -i` after starting it, or read the backend source directly — rather than assuming it matches the Nginx config. Compare that against the `proxy_pass` directive in `/home/user/nginx/nginx.conf` and correct the port to match reality; do not touch the backend code or `deploy.sh`. Start/reload nginx with the exact command given, checking for a stale nginx process first (`nginx -s stop` or kill by pid file before restarting, since a second instance on the same port will itself fail to bind). Finally `curl -s http://127.0.0.1:8080/ > /home/user/resolution.log` and confirm the body matches what the backend actually returns.
