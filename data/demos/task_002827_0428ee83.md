Do this in phases, testing each before the next. Phase 1: read `/home/user/legacy/cache.cpp` — it keys a map by priority, each bucket a list with push_front/pop_front (LIFO within a priority tier); eviction targets the lowest priority key and pops the front of that bucket. Translate exactly into Python: `dict[priority] -> list` used as a LIFO stack, plus `min(keys)` for the lowest bucket, plus a `key -> priority` index so `put` on an existing key removes it from its old bucket first. Phase 2: wrap the structure in a minimal HTTP server (Flask is simplest) exposing the three specified routes/status codes, run it in the background on 127.0.0.1:8081, and verify with curl before moving on. Phase 3: write an nginx.conf that runs fully unprivileged — pid/logs/temp dirs all under a `/home/user/nginx_data/` you create first — and use an `if ($http_x_deploy_env = "green")` condition (or map block) in the 8000 server block to pick the proxy_pass target between 8080 and 8081; `nginx -t` to validate before starting. Finish by writing "DEPLOYMENT READY" to the log file.