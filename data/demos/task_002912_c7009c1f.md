Two independent parts: extract two numeric thresholds from an image, and write a bit-exact Rust CSV-stream processor. Use `tesseract /app/compliance_rule.png stdout` (or similar OCR) to read off the `MIN_IN_DEGREE` and `MIN_SUM` values printed in the image — parse them out of the OCR text rather than assuming fixed numbers, and sanity-check the extracted values look like plausible thresholds. For the Rust program: read all of stdin, split each line on commas into (sender, receiver, amount), skip malformed lines; maintain, per receiver node, a `HashSet` of distinct senders (for in-degree) and a running sum of amounts (for total inbound volume) using maps like `HashMap<String, HashSet<String>>` and `HashMap<String, i64>`. After consuming all input, flag any node whose distinct-sender count >= MIN_IN_DEGREE and whose inbound sum >= MIN_SUM, collect the flagged names, sort them alphabetically, and print one per line with no extra output. Build with `cargo build` (or `--release`) but make sure the final binary ends up at exactly `/home/user/audit_tool`, e.g. by copying it there post-build. Test manually against a few hand-built CSV inputs against your understanding of the two thresholds before trusting the fuzz comparison.