Work through the build/fix/serve chain methodically. First run `make all` in `/home/user/pipeline`, read the linker error to find which compiled object/module is missing from the link command in the Makefile, add it, then fix the `server.c` compile error the same way (read the exact compiler diagnostic — likely a missing declaration, wrong type, or unbound socket variable). Write `telemetry_mock.c` implementing `int send_telemetry(const char* event)` that opens `/home/user/pipeline/telemetry.log` in append mode, writes the event string plus a newline, and returns 0; wire it into the test target of the Makefile so tests link against this mock instead of a real telemetry sender. Transcribe `/app/voicemail.wav` with whatever speech-to-text tool is available in the environment and use the exact recovered text as the transcript payload — don't paraphrase it. In `server.c`, fix the binding bug so the server listens on exactly 127.0.0.1:9090, implement raw HTTP/1.1 request parsing for `GET /health` (always 200 OK) and `GET /transcript` (checks for header `Authorization: Bearer ci_pipeline_token`, returning 401 if absent/wrong, otherwise 200 with the transcribed text as the body). Rebuild, launch `audio_server` in the background, and manually test all three cases with `curl -i` before finishing.