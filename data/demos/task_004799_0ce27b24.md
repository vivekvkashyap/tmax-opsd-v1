Treat `/app/legacy_resolver` purely as a black-box oracle for correctness, not something to copy logic from by disassembly — run it on `/app/deps.txt` and a few hand-built small graphs (including ones with cycles) to confirm your understanding of its I/O contract (exit code 1 + "CYCLE" text vs exit code 0 + integer). Implement the algorithm yourself in C: parse edges into an adjacency list (hash small alphanumeric node names to indices), do a DFS-based topological check that simultaneously detects cycles (track an "on current recursion stack" marker per node, distinct from "fully visited"), and if acyclic compute the longest path via memoized DP over each node (`longest[node] = 1 + max(longest[child])` for its out-edges, 0 for a sink) — this avoids the legacy tool's exponential recomputation of overlapping subgraphs. Compile with static linking and aggressive optimization (e.g., `gcc -O3 -static -o /home/user/fast_resolver`) so `file` reports it as statically linked. Before trusting your speed, test correctness against the legacy binary on several graphs you construct yourself (including deep chains and diamonds) to make sure your longest-path counts match exactly, then time both against a graph you generate that's large/densely connected to confirm your DP scales roughly linearly.