Debug this as two independent, isolated bugs rather than trying to fix everything at once. For the hang: run `/app/parse_logs.py` with a timeout (`timeout 2 python3 /app/parse_logs.py "<ts with Z or negative offset>"`) to reproduce it, then use `faulthandler`/`py-spy dump`/Ctrl-\ (SIGQUIT) or simply add temporary print/pdb breakpoints inside `DateTimeParser.parse` in `/app/arrow-1.2.3/arrow/parser.py` to find the loop whose index variable isn't being advanced on certain tokens (specifically timezone-related ones like `Z` or negative offsets) — fix so the increment always happens. For the numerical bug, compare `/app/parse_logs.py`'s output against `/app/parse_logs_oracle` on inputs with varying fractional-second digit counts to see exactly how the float_timestamp diverges; then inspect the fractional/microsecond parsing helper for arithmetic that doesn't correctly convert a decimal string fragment into a fraction (e.g. multiplying by a fixed constant instead of scaling by the digit count) and correct it to compute the true base-10 fractional value. After each fix, re-run a broad set of ISO-8601 and offset-variant timestamps through both scripts and diff the outputs to confirm exact equivalence before considering it done. Do not touch `/app/parse_logs.py` itself.
