Run `tesseract /app/mapping.png stdout` (install tesseract if missing) to OCR the character→token-ID table; verify the OCR result looks like 5 lines of `<letter>: <digit>` before trusting it, since misreads on a small font are common — cross-check against the actual letters appearing in `/app/events.csv`. In your C program, load this mapping into a small lookup table, then parse each CSV row by splitting `event_sequence` on spaces and mapping each token via the lookup. For the bigram model: prepend a `<s>` state to every sequence (not a token ID, just a distinguished "previous" state, e.g. index 0), accumulate `count[prev][cur]` and `count[prev]` over the *raw* training data only, and apply the given Laplace formula with V=5 exactly. For perplexity, sum `log2(P(w_i|w_{i-1}))` over every token in `/app/test_events.csv` (using `<s>` as prev for the first token of each row), divide by total token count (excluding `<s>`), and negate/exponentiate as specified — get the sign right, a common mistake is flipping it and getting fractional instead of >1 perplexity. For the bootstrap step, resample rows with replacement 10,000 times using a seeded PRNG and average sequence lengths across all samples; this is independent of the bigram model. Write both rounded values to `/home/user/metrics.txt` in the exact two-line format.
