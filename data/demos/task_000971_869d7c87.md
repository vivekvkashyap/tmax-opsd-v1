Build the script incrementally and validate each piece with a quick manual check before combining. Parse `model_params.txt` for `weight_A`, `weight_B`, `bias` (simple `grep`/`cut` or `awk -F=`). For inference over all CSVs in `/home/user/data/`, use `awk` (skip header with `NR>1` or `FNR>1`) to compute `predicted = A*wA + B*wB + bias` per row across all files concatenated, keeping paired `(predicted, actual)` values. For covariance and Pearson correlation, either implement the standard formulas in `awk`/`bc` accumulating sums (`Σx, Σy, Σxy, Σx², Σy², n`) or shell out to a one-line Python/numpy snippet inside the bash script — either is fine as long as results are deterministic and rounded to exactly 4 decimals with the exact label format `Covariance: ...` / `Correlation: ...`. For reproducibility, literally call your own inference+stats logic twice (e.g. as a function invoked twice, writing to two temp files or the same file twice) and `md5sum` the resulting `metrics.txt`, saving that line to `/home/user/reproducibility.txt`. Test manually against a small hand-computed example before trusting the numpy-based verifier's cross-check.
