Fix the build first: `make` fails because the Makefile only compiles `decoder.cpp` but the code references symbols defined in `utils.cpp`/`utils.h` — add `utils.cpp` as a compilation unit/object in the Makefile's link step so `decoder` links cleanly. Then reproduce the crash under `gdb` before editing anything: `gdb ./decoder`, `run crash.bin`, and on the segfault use `bt`/`info locals` to find the exact faulting line and note it — this is what you'll report, so capture it from the *original* unpatched binary, not after you've already patched. Reason about the root cause: `decode_payload` reads a `short len` from attacker bytes and compares it against `total_len - 3` — if the check treats `len` as signed and the attacker supplies a negative `short` (e.g. via a length field that's `0xFFFF`), the bounds check `len > total_len - 3` can pass even though `len` is effectively huge when later used as a size in `memcpy`, causing a stack buffer overflow/segfault. Fix `decode_payload` to validate `len` is non-negative and within the actual remaining buffer bounds before the `memcpy`, returning -1 otherwise, without ever touching invalid memory. Confirm `./decoder crash.bin; echo $?` prints 255 with no crash output, then write the two-line `report.txt` with the original faulting line number and a description of the signedness/integer issue you diagnosed via gdb.
