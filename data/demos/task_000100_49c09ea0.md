Work through `/home/user/pr_review` methodically, one failure at a time, re-running `./run_ci.sh` after each fix to see the next error. (1) In `math_engine.c`, look at `calculate_stddev`'s summation loop bounds carefully — a loop that iterates `i <= len` while indexing a `len`-sized array reads one element past the end (classic off-by-one/buffer over-read); change the comparison so the loop only visits valid indices. (2) The `Makefile` builds a shared library via `gcc -shared -fPIC` but the resulting `.so` fails to load or fails at link time because it calls `sqrt`/math functions without linking the math library — add `-lm` to the compile command. (3) `schema_v2.sql` is empty but the test suite expects a `variance REAL` column on `stats` (which schema_v1 only creates with `id`/`mean`); write the `ALTER TABLE stats ADD COLUMN variance REAL;` statement (or equivalent) so it applies cleanly after schema_v1. (4) Confirm `run_ci.sh` is executable (`chmod +x` if needed) and that it runs `make`, applies both SQL files via `sqlite3`, and runs `python3 test.py` without errors — only then does it write `CI PASS` to `/home/user/ci_success.log`.
