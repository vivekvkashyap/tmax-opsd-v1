Start by reverse-engineering `/app/legacy_hash` empirically rather than guessing: create small test files (all zeros, a single 0x01 byte, a 4-byte pattern, a file with a trailing 1-3 byte remainder) and run the binary on each, then use `objdump -d` or `strings`/hexdump on the binary to corroborate. Look for a simple accumulator loop reading 4-byte little-endian words, summing with wraparound (unsigned 32-bit, so implement with `uint32_t` so overflow wraps naturally in C), then XOR-ing with a constant before printing as `%08x`. Handle the tail: zero-pad any remaining 1-3 bytes into a final 4-byte word before summing. Structure the daemon as: TCP accept loop -> parse `DEDUP <dir>\n` -> `opendir`/`readdir` regular files -> `mmap` each read-only to hash -> group by checksum -> for duplicate groups, sort names, keep lexicographically first. For replacement, always write the WAL entry (with `flock` exclusive on the WAL fd) BEFORE the rename, use `link()` to a temp path then `rename()` over the duplicate for atomicity, and unlink temp files only on failure. Track total bytes freed via `stat().st_size` of each replaced file's original size and reply `FREED <N>\n`. Test the WAL layout with a hexdump against the exact byte offsets/padding described in the spec.