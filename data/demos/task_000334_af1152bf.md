Write the health-check script to do in-place edits with precise, targeted substitutions rather than blanket text replacement — e.g. use `sed -i` (or Python line-by-line rewrite) targeting only the exact keys `AllowAutomatedRestore=false` → `AllowAutomatedRestore=true` and `Timezone=America/New_York` → `Timezone=UTC`, leaving every other line in `config.ini` byte-for-byte unchanged. For the size measurement, do not reimplement your own directory-size logic (recursive `os.path.getsize` sums can disagree with `du`'s block-based accounting) — instead shell out to exactly the specified pipeline, `du -sb /home/user/app_restore/data/ | awk '{print $1}'`, capture its stdout, and cast it to an integer. Build the JSON report with a proper serializer (`json.dumps`/`jq`) rather than hand-built strings to guarantee valid JSON, matching the required schema keys and the `"status": "ready"` / `"timezone": "UTC"` literals. Finally, actually execute the script (not just write it) so both `config.ini` and `/home/user/restore_status.json` reflect the changes on disk before finishing.