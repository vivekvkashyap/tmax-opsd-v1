Break this into clear ETL stages and verify each before moving to the next. Use `ffmpeg` to dump all 100 frames of `/app/sensor_feed.mp4` to individual images (e.g. PNG), then write the C++ program to load each frame (a lightweight image library or manual PPM/PNG decode), slice it into the 16 fixed 100x100 blocks (row-major, 4 columns x 4 rows), and compute each block's mean grayscale intensity as an integer (round, don't truncate silently) — build one 100x16 integer matrix. Detect "glitch" frames as rows where all 16 values equal 0, and forward-fill each LED's column independently from the last valid frame; keep every intermediate buffer as an integer type (`int`/`long`), since converting through `float`/`double` for the imputation or convolution step is the classic bug that introduces rounding drift and fails the tolerance check. Apply the `[1,-2,1]` convolution independently per LED column across the time axis, replicating the boundary value at index 0 and 99 (edge padding) — implement it as explicit integer arithmetic, not via a floating-point signal-processing library. Write the resulting 100x16 integer matrix to `/home/user/filtered_signals.csv`, one row per frame, comma-separated, and validate row/column counts before finishing.