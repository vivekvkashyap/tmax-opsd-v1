Treat this as a classic "latest version wins" ETL: for each of the three CSVs, group rows by their entity key (emp_id for employees, proj_id for projects, and — since works_on has no separate id — emp_id as the effective key there too, per the task's own framing) and keep only the row with the maximum `version` per key; do this with `sort -t, -k1,1 -k4,4nr <file> | sort -u -t, -k1,1` or an `awk` associative array that overwrites on higher version, whichever is easier to get right. Once you have deduplicated employees, projects, and works_on tables, join works_on -> employees (on emp_id) -> projects (on proj_id) using `join` (after sorting both sides on the join key) or an `awk` script that loads one table into memory first. Flag a row as anomalous when the employee's dept_id differs from the project's dept_id, then emit only `emp_name`/`proj_name` pairs, sort by employee name, and build strict JSON with `jq -n --slurpfile ...` or by piping structured lines through `jq -R -s` to avoid manual string-escaping bugs. Save the script to `/home/user/find_anomalies.sh`, `chmod +x` it, run it, and validate the output with `jq -S . /home/user/anomalies.json` to check field order/formatting is exactly two keys per object.
