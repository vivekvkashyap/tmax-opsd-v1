Reverse-engineer by systematic probing, don't guess. First inspect `/app/datasets.db`'s schema (`sqlite3 /app/datasets.db .schema`) to see the entities/edges tables and understand what columns exist (ids, categories, edge costs/weights). Then use the oracle binary `/app/compute_relevance <A> <B>` as a black box: run it on carefully chosen pairs — same node twice, directly connected pairs, pairs requiring 2+ hops, disconnected pairs, pairs with different edge weights — and compare the printed scores against hypotheses (e.g. inverse distance, exponential decay, some function of shortest-path cost) computed by hand or in a scratch script from the DB's edge weights. Once you find a graph-distance transform that matches the oracle's outputs across many test cases (not just one), implement it in `/home/user/batch_score.py`: load the graph into memory once (e.g. with `networkx` or a hand-rolled Dijkstra/BFS over an adjacency structure built from the SQL tables), then for each row in the input CSV compute shortest-path cost between the two ids and apply your derived transform, writing `A,B,score` per line to the output CSV with no header. Test against many oracle calls before finalizing, since the pass bar is MSE < 0.001 over 1000 hidden pairs — a wrong formula that's "close" won't be close enough.
