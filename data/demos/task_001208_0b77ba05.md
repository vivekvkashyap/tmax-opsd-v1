Fix the vendored library first: run `make` inside `/app/libfasta-parser` and read the actual compiler error rather than guessing — expect two independent issues, a toolchain mismatch (Makefile hardcodes a compiler not present on this system; point `CC` at the one that is, e.g. `gcc`) and a genuine C syntax error (search for missing punctuation like a semicolon after a struct/typedef definition, since one missing `;` after a `struct { ... }` will cascade into confusing downstream errors — fix the syntax at its true source line, not where the compiler first complains). Confirm `libfasta.so` and a header get produced before moving on. For `/home/user/sequence_sanitizer.c`, stream stdin FASTA records (a header line starting with `>` followed by one or more sequence lines) as complete units, buffering each record until you hit the next header or EOF, and only decide to print/reject after seeing the whole sequence. Apply the three checks in order: reject any character outside A/C/G/T/N (case-insensitive), reject if the concatenated sequence contains `GTCGAC` case-insensitively (check across line boundaries, not per-line), and reject unless GC% is in [35.0, 65.0] inclusive. Test against small hand-crafted clean and evil FASTA snippets covering each rule individually before trusting the whole pipeline.