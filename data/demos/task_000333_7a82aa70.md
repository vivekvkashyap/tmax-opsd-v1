Start by OCRing `/app/model_specs.png` with `tesseract` to recover the exact architecture — the hidden-layer formulas, coefficients, and combination rule for the final score — transcribe every number and sign precisely, since a fuzz-equivalence check will compare your binary's output against a hidden reference on thousands of random inputs, so any coefficient typo or sign flip fails broadly rather than on edge cases. Implement the extracted formula directly in Go: read stdin line by line, split each line into two floats (A and B), compute the hidden nodes with their activation function exactly as written in the image (apply any clamping/ReLU-style nonlinearity precisely), combine them into the final score per the extracted combination formula, and print with exactly 4 decimal places and a trailing newline per line, with zero extra stdout output (no logging/debug prints — the grader parses stdout literally). Build with `go build -o /home/user/scorer` and smoke-test it manually on a couple of hand-computed input/output pairs (compute the expected value by hand from the OCR'd formula) before trusting it against the fuzz harness.
