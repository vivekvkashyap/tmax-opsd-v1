Build this as a clear multi-stage Python script (invoked by the orchestration shell script) rather than one monolithic pass: (1) Validation stage — iterate `/home/user/raw_configs/config_<ts>.json`, try `json.loads` on each, move parse failures to `/home/user/quarantine/` with `shutil.move`, and count them for logging. (2) Imputation stage — extract the integer timestamp from each remaining filename (e.g. via regex or splitting on `_`/`.`), sort configs by that timestamp, then for `max_memory`/`timeout` do linear interpolation strictly by timestamp value (not by list index) between the nearest non-null neighbors, casting `max_memory` to `int` and `timeout` to `float`; for `log_level` forward-fill from the previous valid value in timestamp order, defaulting the very first entry to `"INFO"` if it's null. Count every individual null field imputed across all files for the log line. (3) Load stage — write reconstructed JSON to `/home/user/reconstructed_configs/` preserving filenames. Configure Python's `logging` to write to `/home/user/pipeline.log` and emit the three required log lines with exact substrings and correct counts at each stage boundary. Finally write `/home/user/run_pipeline.sh` that runs the Python script and exits 0 on success — verify end-to-end by re-running it.