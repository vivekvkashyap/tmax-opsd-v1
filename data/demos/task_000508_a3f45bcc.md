In `/home/user/find_deadlock.py`, first query `locks` with a self-join: for every pair of rows where one has `status='WAITING'` on some `resource_id` and another row has `status='GRANTED'` on that same `resource_id`, record a directed edge `waiting_process_id -> granted_process_id` (skip self-loops where they're the same process). Then load `processes.json`, build a `process_id -> owner` map, and drop any edge where either endpoint's owner is not `'user'` — do this filtering after building edges, not before, since you need both endpoints' owners. Project the surviving edges into a directed graph (a simple adjacency dict is enough, or use `networkx.DiGraph`) and run cycle detection (DFS with recursion-stack tracking, or `networkx.find_cycle`/`simple_cycles`) to locate the single cycle — be careful to only report nodes that are actually part of the cycle, not just reachable from it. Sort the cycle's `process_id`s numerically ascending and write them as a comma-separated line to `/home/user/deadlock_report.txt` with no trailing formatting surprises (no brackets, no extra whitespace/newline issues). Run the script and inspect the output file before finishing.