Start the services (`/app/start_services.sh`) and manually curl `GET /data` and `GET /truth` to see the shapes before editing C++. In `estimator.cpp`, apply Tikhonov regularization by adding `lambda = 1e-4` to each diagonal entry of the covariance matrix before every determinant/inverse computation (both for the running estimate and inside the KL formula) — this is what prevents NaN/inf on the near-singular collinear data. Implement the 2D closed-form KL divergence exactly as given (trace term, quadratic mean term, `-2`, log-determinant-ratio term, all times 1/2), and use it as the convergence test: after each iteration compare the new distribution to the previous one and stop once that KL drops below `1e-6`, rather than running a fixed iteration count. Build with `make` in `/home/user/worker/`, run the worker, then after convergence fetch `GET /truth`, compute the KL divergence between your fitted distribution and the true one using the same regularized formula, and write only that float to `/home/user/final_kl.txt`. Sanity check the number is small and non-negative before finishing.
