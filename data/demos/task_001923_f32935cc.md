Recover the token format from the audio before writing any detection logic — use a speech-to-text tool (e.g. `whisper` if installed) or an audio-analysis tool on `/app/intercept_774.wav` to get the spoken description, then translate the described prefix word, number, separator, and character-count/charset constraints into a precise regex; don't guess or reuse a format from memory. Build `/home/user/detector.sh` as a thin wrapper: `grep -qE '<derived-pattern>' "$1"` and print `EVIL` on match, `CLEAN` otherwise, ensuring exact string output with no extra whitespace/newlines beyond the trailing newline from `echo`. Before finalizing, actually run the script against every file in `/app/corpora/clean/` and `/app/corpora/evil/` and diff the results — the described format almost certainly has an exact-length constraint (not "at least N"), so watch for near-miss decoys in the clean corpus (e.g. same prefix but slightly shorter/longer suffix, or a different digit) designed to trip up a loose or unanchored regex; tighten the character class and length bound until both corpora classify 100% correctly. Make the script executable with `chmod +x`.
