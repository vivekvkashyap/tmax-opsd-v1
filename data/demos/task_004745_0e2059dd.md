Read `/home/user/stats_calc.c` and look specifically at how each bootstrap mean is computed: `sum` is declared as an integer type (`long`), and `boot_means[i] = sum / N` performs integer division before ever becoming a double, truncating all fractional precision — this is the classic C bug where an assignment target being a `double` doesn't retroactively make the RHS division floating-point. Fix it by casting before dividing: `boot_means[i] = (double)sum / N;`. Don't touch the `srand(42)` call, the sampling loop, or the percentile-index logic (`boot_means[250]`, `boot_means[9749]`) — those are already correct given the fixed-size arrays. Leave the Bayesian posterior mean formula untouched too, since it already operates purely in doubles. After fixing, compile with `gcc /home/user/stats_calc.c -o /home/user/stats_calc -lm`, run it, and redirect stdout exactly to `/home/user/results.txt` (`./stats_calc > /home/user/results.txt`) — check the CI bounds are no longer suspiciously integer-valued (e.g. ending in `.000`), which is your signal the truncation bug is actually fixed.