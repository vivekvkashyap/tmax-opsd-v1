Use `ffmpeg -i /app/crypto_test.mp4 -vf fps=1 frame_%03d.png` (or similar) to dump frames, then read the on-screen text in each frame (OCR with `tesseract`, or visual inspection if frames are few) to recover the plaintext/ciphertext hex pairs the developer typed. With at least two known-plaintext pairs of different lengths, do byte-level cryptanalysis: compute `ciphertext_byte - plaintext_byte` (mod 256) at each position and look for a pattern that grows linearly with index (suggesting an additive term keyed on position) layered on top of a short repeating XOR key — test the hypothesis "XOR with a period-4 key, then add a value proportional to the byte index mod 256" against all recovered pairs to pin down both the 4-byte key and the additive formula's coefficient. Once solved, write `/home/user/decryptor.cpp` implementing the exact inverse (undo the addition first, then XOR), compile it, and verify it round-trips your recovered pairs correctly before considering the reverse-engineering complete — the final grading fuzzes your binary against many random inputs, so the formula must be exactly right, not just approximately matching the sample frames.
