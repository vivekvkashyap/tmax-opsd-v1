Transcribe `/app/voicemail.wav` first (e.g. with `whisper`) and listen carefully for the spoken-out motif letters and the min/max values for the reference distribution — write these into your script as named constants derived from what you actually hear, not assumed defaults. Then implement `/home/user/primer_model.py` to match the specified algorithm exactly and in the exact order given, since bit-for-bit reproducibility against a hidden oracle depends on the precise sequence of `random` calls: read stdin, `random.seed(len(sequence))`, then for each of 1000 iterations build one mutated sequence by iterating character-by-character and calling `random.random()` once per character (replacing with 'A' if `< 0.05`), and only after finishing all 1000 mutated-sequence scores do you generate the 1000-element reference list via `random.randint(MIN_VAL, MAX_VAL)` in a single subsequent list comprehension — do not interleave randint calls with the mutation loop or reorder these two phases, since Python's Mersenne Twister state must advance identically to the oracle's. Count non-overlapping motif occurrences with `str.count()` for each mutated sequence. Compute `scipy.stats.wasserstein_distance(scores, ref_data)` and print it formatted to exactly 4 decimal places. Test with a few sample sequences and confirm determinism (same input twice → same output) before trusting it against the fuzz harness.
