Fix the build before writing any application code. Look at `/app/fastcov-1.2.0/Makefile` for the `LDFLAGS` used to link `libfastcov.so` — a shared object exposing `sqrt` (via `compute_stddev`) needs `-lm` on its link line; add it to `LDFLAGS` and confirm `make` produces `libfastcov.so` cleanly. Then write `/home/user/dataset_cleaner.c`: include `fastcov.h`, load the 5-column, 50,000-row CSV from `/app/data/sensor_readings.csv` into memory (e.g. an array of 5 double arrays), and implement bootstrap sampling — for each of 100 iterations, draw 2,000 row indices uniformly at random *with replacement*, build per-feature sample arrays from those indices, then use `compute_covariance`/`compute_stddev` on each feature pair to fill a 5x5 correlation matrix for that sample. Accumulate all 100 matrices element-wise and divide by 100 for the final average. Compile linking against your fixed library: `gcc dataset_cleaner.c -L/app/fastcov-1.2.0 -lfastcov -Wl,-rpath=/app/fastcov-1.2.0 -lm -o cleaner`. Write the 5x5 result to `/home/user/correlations.csv` as exactly 5 comma-separated `%.6f` lines, then sanity-check diagonal entries are close to 1.0 before finishing.