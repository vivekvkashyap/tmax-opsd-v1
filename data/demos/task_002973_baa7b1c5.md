Extract the zip programmatically (Python's `zipfile` module, not `unzip -d`) so you can sanitize each entry name yourself: for every `ZipInfo.filename`, strip leading `../` sequences and any absolute-path prefix (e.g. use `os.path.normpath` after stripping drive/leading slashes, or just take `os.path.basename` if entries are flat) and join onto `/home/user/docs_clean`, then verify the resolved path is still inside that directory before writing — reject/clean anything that resolves outside via `..`. Do not rely on `unzip` alone, since some versions still honor traversal paths. Run `tesseract /app/instructions.png` (or `tesseract instructions.png stdout`) to OCR the renaming/replacement rules rather than guessing them. For files missing extensions, open each in binary mode and check the first bytes against known magic numbers (PNG starts with `\x89PNG\r\n\x1a\n`) rather than trusting filenames. Apply text replacements to file contents first, then rename files (including newly-identified PNGs) per the OCR'd rules, keeping everything inside `/home/user/docs_clean`.