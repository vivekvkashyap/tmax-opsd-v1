Reverse-engineer the binary first: run `strings /home/user/audit_tool | grep -i -E "^[A-Z_]+$"` to spot plausible env-var names, then confirm with `ltrace -e getenv /home/user/audit_tool -f /home/user/fw_rules.txt` (or `strace -e trace=execve,getenv` won't work directly since getenv is libc-only — use `ltrace`) to see exactly which variable name it calls `getenv()` on. Do not guess a name from the description; verify it against the binary's actual behavior (e.g., diff output with and without the variable set). Once confirmed, build the wrapper script as a pipeline: `EXPORTED_VAR=10.0.0.1 /home/user/audit_tool -f /home/user/fw_rules.txt > tmp_output`. Then sanitize in two ordered passes: first redact AWS keys with a regex anchored to the exact shape (`AKIA` + 16 alnum chars, word-boundary so it doesn't over/under-match) using `sed -E 's/AKIA[A-Z0-9]{16}/REDACTED/g'`, then HTML-escape `<`/`>` with `sed -e 's/</\&lt;/g' -e 's/>/\&gt;/g'` — do the AWS redaction BEFORE the `<`/`>` escaping so you don't have to worry about escaped characters interfering with the key regex. Write the final result to `/home/user/audit_report.txt` and `chmod +x` the script.