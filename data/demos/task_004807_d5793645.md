Load the CSV with pandas and inspect `feature_A` with `df['feature_A'].unique()` first — since the column mixes numbers and the string "MISSING", pandas will load it as `object` dtype, not `int64`. Coerce valid entries to numeric (e.g. `pd.to_numeric(..., errors='coerce')`) to isolate the missing mask, compute the median of only the valid values, then floor/round it to a Python int before using it to fill. The classic mistake here is calling `.fillna(median)` with a float median or leaving the column as `object`/`float64` afterward — you must explicitly `.astype('int64')` (or equivalent) as the final step so the dtype check passes. Train `RandomForestClassifier(random_state=42)` on `feature_A`/`feature_B` vs `label`, score on the same training data. For the inference benchmark, time a plain `for` loop of 100 `model.predict(X)` calls with `time.perf_counter()`, summing elapsed seconds. When writing JSON, cast numpy scalar types (int64/float64) to native Python `int`/`float` — `json.dump` will error otherwise.
