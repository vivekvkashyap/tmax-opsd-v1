Treat this as a graph-traversal + query-optimization problem, not a black-box mimicry problem. First inspect the schema of `/app/datasets.db` and confirm the `dependencies` table has no index on `source_id`/`target_id` — create `CREATE INDEX IF NOT EXISTS idx_dep_source ON dependencies(source_id)` (and target if needed) before running any traversal, since an unindexed self-join or Python-side BFS/DFS over 25k edges will blow the 100ms budget. Implement the traversal either as a recursive CTE (`WITH RECURSIVE`) in SQLite or as an iterative BFS/DFS in Python using a visited-set to avoid revisiting nodes in the DAG (recursion without memoization risks exponential blowup on diamond-shaped dependency graphs). After collecting all downstream node IDs (excluding the root), fetch their size/created_at, filter out size < 1024, then sort by size DESC, created_at ASC, id ASC exactly — sort key order matters for matching the reference. Slice with `[(page-1)*50 : page*50]` for pagination. Benchmark your script's wall time on the actual query and tighten the index/query plan (use `EXPLAIN QUERY PLAN`) if it's not comfortably under the threshold.