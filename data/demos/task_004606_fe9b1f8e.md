Write a small Python script that iterates every `.pkl` file in `/home/user/models/`, loads each with `joblib.load`, runs `.predict(X)` on `/home/user/X.npy`, and computes `mean_squared_error(y_target, preds)` from `sklearn.metrics` against `/home/user/y_target.npy`. Track the filename with the minimum MSE as you go (don't just eyeball printed values — compare programmatically), then write exactly that bare filename (e.g. `model_xxx.pkl`, no path prefix, no extra text) to `/home/user/best_model.txt`. Double check you're evaluating every model file present in the directory, not a hardcoded subset, since the ranking must be computed rather than assumed.
