First recover the filtering rule from the image rather than guessing it: run `tesseract /app/criteria.png stdout` (or similar OCR invocation) and read the extracted text carefully to get the exact energy formula, the differencing scheme, and the numeric threshold — do not assume a formula, since the classifier must match it precisely. Once you have the rule, implement it in Go: open the NetCDF file (use a cgo binding to netcdf-c, e.g. `github.com/fhs/go-netcdf`, after installing `libnetcdf-dev`), read the `t`, `x`, `v` 1D arrays, derive `dt = t[1]-t[0]`, compute the energy array per the recovered formula, then compute the central-difference derivative for interior indices per the recovered scheme, take the max absolute value, and compare it against the recovered threshold to decide exit code 0 (clean) vs 1 (unstable). Be careful with edge handling — only compute the derivative where both neighbors exist, don't index out of bounds at the array ends. Build with `go build`/`go run` and self-test against a couple of hand-crafted small NetCDF files (one clearly stable, one with an injected perturbation) using Python's `netCDF4` to generate them, before assuming correctness against the hidden clean/evil corpora.