Triage the three bugs independently rather than guessing at all of them from the description alone — reproduce each first. Run `make` and read the actual linker error; if it complains about an undefined math symbol (e.g. `sqrt`), the Makefile's link step is missing a required library flag on the final binary target. Build, then run the parser on the sample CSV and let it crash under `gdb`/with a debug print; `strtok` returns NULL when a line has fewer delimited fields than expected, so any code that unconditionally calls `strcpy`/`strlen` on the next token without a NULL check will segfault — add an explicit check and a sane fallback for that field. Finally, compare the input and output timestamps by eye: if a large integer microsecond epoch value gets truncated/rounded when round-tripped, look for a struct field declared as `float` (which cannot exactly represent 16+ digit integers) — change it to a 64-bit unsigned integer type and update the corresponding `sscanf`/`printf` format specifiers to match. Re-run the binary end-to-end and diff the output against the input before writing the postmortem.
