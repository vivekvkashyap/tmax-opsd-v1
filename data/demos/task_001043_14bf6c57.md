Work in stages: (1) connect to the SQLite DB and pull all rows from `components` and `assembly` with plain `SELECT *` — do not touch the corrupted index. (2) Build an in-memory `rdflib.Graph()`, bind the `http://example.org/bom/` namespace, and add one triple per component for `name`, one for `category`, plus one `hasChild` triple per assembly edge, using URIRef(namespace + part_id) for entities. (3) Run a SPARQL query using the `+` property-path operator (e.g. `?root bom:hasChild+ ?dep`) to get the full transitive closure from `PROD-001` in one query rather than recursing in Python. (4) For each dependency returned, decide leaf-ness by checking whether it appears as the subject of any `hasChild` triple in the graph (ASK or a second query) — a node with zero outgoing hasChild edges is a leaf. (5) Assemble dicts with the four required keys, validate types (e.g. with pydantic or jsonschema) before writing. (6) Sort the final list by `part_id` before dumping to JSON. Watch for: including PROD-001 itself in the output (it shouldn't be, only its dependents), and components unreachable from PROD-001 (must be excluded).
