Work in stages and test each independently. First parse `/home/user/logs.csv`: for `raw_timestamp`, try `int()`/epoch parsing first, fall back to `datetime.strptime(x, "%m/%d/%Y %H:%M:%S")`, then format both paths to `YYYY-MM-DDTHH:MM:SSZ` (UTC, no timezone conversion needed since these are naive). For IP masking, split on `.` and replace each octet with `X` regardless of value — don't try to preserve any digits. For the distance score, call the black-box binary exactly as documented (`/app/log_distance "root shell attempt" "<details>"`) via subprocess for every row, capture stdout, and parse it as a float — don't try to reimplement its algorithm yourself, since it's a black box you must invoke, not reverse-engineer. Cache nothing that depends on request time; recompute per request or precompute once at startup and store all fields (timestamp, masked ip, action, distance) in memory. For the server, a simple Python `http.server` (or Flask) subclass handling GET on `/api/anomalies` is easiest — parse `max_distance` from the query string, filter rows with `distance <= max_distance`, sort by ISO timestamp string (which sorts correctly lexicographically), and return JSON. Launch it with `nohup ... &` or similar and verify with `curl` before finishing.