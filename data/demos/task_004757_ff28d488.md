Structure this as a standard Go numerical-analysis pipeline using gonum. First `go mod init sensor_analysis` and `go get gonum.org/v1/gonum/...` (stat, mat packages). When filtering rows, compare against both `-9999` and `-9999.0` representations after parsing each field as float64 — a naive string-equality check on the raw CSV cell will miss values written as `-9999.00` or similar, so filter on the *parsed* float being (very close to) -9999 rather than string match. Build column slices from the cleaned rows, compute Pearson correlation between F1 and F2 with `stat.Correlation`, build the 4x4 covariance matrix with `stat.CovarianceMatrix` (or manually via centered data and matrix multiply), then compute eigenvalues via `mat.Eigen` (or `EigenSym` since covariance matrices are symmetric) and take the maximum real eigenvalue. Round each float to exactly 4 decimal places (e.g. `math.Round(x*10000)/10000`) before marshaling to JSON, and match the exact key names/structure requested. Double check row-filtering logic against the dataset by hand before trusting the eigen output.
