Since you don't have source for /app/primer_oracle, black-box it: run it on sequences of known, easily-computed composition (all-G/C, all-A/T, and mixes) of varying lengths, and look for patterns relating output to sequence length and GC-content ratio — check whether output scales linearly with length for a fixed GC ratio, and how it shifts as the ratio changes, to narrow down the underlying model (a ratio-driven scaling factor times length is a common pattern here). Once you have a hypothesis (a GC ratio feeding an iterative/fixed-point numerical solve, then scaled by length), test it against many oracle outputs across varied lengths/GC-contents to confirm it reproduces results to high precision, adjusting iteration count or convergence details if close but not exact. Implement the confirmed formula in /home/user/my_primer_oracle.sh using `awk` or `bc -l` (both support `exp()`) for the floating-point math, format to exactly 6 decimal places, `chmod +x` it, and validate against the real oracle on fresh random ACGT sequences of length 10-50 (including boundaries) before considering it done — it's fuzz-tested for bit-exact output.