Treat `/app/feature_projector` as a black box you must characterize empirically, not something to disassemble line-by-line. Generate many random 20-dimensional vectors spanning a wide range of magnitudes and signs, pipe each through the binary, and record which inputs return NaN/crash vs. a valid projection. Compute simple summary statistics per vector (sum of elements, L2 norm, min/max, count of negatives, etc.) alongside the pass/fail outcome, then look for a clean separating boundary in one or two of those statistics — e.g. does failure correlate with the sum being negative, or with the norm exceeding some threshold? Binary-search along a boundary axis (e.g. fix all but one dimension and sweep it) to pin down the exact numeric cutoff rather than eyeballing it. Once you have a hypothesis for the constraint(s), validate it against a fresh batch of random vectors before committing to it in Go. Implement `filter.go` to read the file, parse the 20 floats, evaluate your discovered rule(s), and print exactly `CLEAN` or `EVIL` — keep it allocation-light so it runs in milliseconds. Build with `go build -o /home/user/filter filter.go`, and never shell out to the projector binary at runtime.
