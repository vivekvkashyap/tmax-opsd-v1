Follow the pipeline stages in the exact order given, since seeds and step ordering affect the final numbers: load `/home/user/telemetry.csv` with pandas, fit `StandardScaler` on all features, then fit `PCA(n_components=5, random_state=42)` on the standardized data and transform it. Inverse-transform the 5-component representation back with `pca.inverse_transform(...)` and compute MSE between that reconstruction and the *standardized* (not raw) original data using `sklearn.metrics.mean_squared_error` or an equivalent mean-of-squared-differences — round to exactly 4 decimals. Separately, fit `IsolationForest(random_state=42)` on the 5-component PCA-transformed data (not the standardized full-dimensional data), call `.predict()`, and count entries equal to -1 as anomalies. Write both results to `/home/user/metrics.json` under the exact keys `reconstruction_mse` and `anomaly_count`. A common mistake is computing MSE against the raw unscaled data or fitting IsolationForest on the wrong feature space — both scaler and PCA/model random_state must be 42 and applied in this order for the results to match the expected values within tolerance.
