Two independent deliverables. First, open `/app/vendor/cap-plan-tools-1.0.0/aggregator.py` and find the hardcoded privileged base-directory constant (a root-owned path assigned directly). Replace it with a line that reads `os.environ.get("CAP_PLAN_DIR", "/home/user/cap_plan")` — do not just chmod the existing root path, since the check runs with a custom `CAP_PLAN_DIR` env var pointed at a temp dir and expects the script to honor it. Second, write `/home/user/sanitize.py` as a streaming filter: read stdin line by line, `json.loads` each line inside a try/except (drop on `JSONDecodeError` or any parse failure), then apply the numeric bounds checks (`cpu_percent` present and in `[0,100]` inclusive per the spec's strict `<0`/`>100` wording, `memory_mb` present and `>=0`), and reject if `process_name` contains any of `; & | < >`. Print surviving lines back out as JSON (re-serializing is fine as long as structure matches) so output is line-buffered JSONL. Make it executable with `chmod +x`. Finally create `/home/user/cap_plan/logs/` so directory-existence checks pass, and manually pipe a few sample clean/malicious lines through your script to confirm the filtering logic before considering it done.
