Extract the tarball into a freshly created `/home/user/restore_area/` with `tar -xzf`. Write `rotate_and_test.sh` so it *dynamically* parses `etc/app_config.conf` at run time (e.g. `grep '^PORT=' file | cut -d= -f2`, same for `TLS_CERT`/`LOG_DIR`) rather than hardcoding values you observed by inspecting the file once — the grader may re-run your script and expects it to derive everything from the config each time. For rotation: build the real log dir as `/home/user/restore_area/$LOG_DIR`, `gzip -c access.log > access.log.1.gz`, then truncate the original with `: > access.log` (don't just delete it — the checker expects a 0-byte file to still exist). For the cert: build the path the same way and test with `[ -f "$path" ]` to set `TLS_CERT_FOUND`. Write the three-line summary to `/home/user/restore_summary.txt` in exactly the literal format given (`KEY=value`, no extra spaces). `chmod +x` the script, then actually run it once and inspect all three output files before finishing.