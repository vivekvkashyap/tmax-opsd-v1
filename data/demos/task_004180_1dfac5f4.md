First read `/home/user/setup_mounts.sh` to see the exact prompt strings it emits (via `read -p`), since your expect script's `expect` patterns must match those literal prompt texts. Write `/home/user/auto_mount.exp` starting with `#!/usr/bin/expect -f`, set an unlimited or generous timeout, `spawn` the target script, then for each prompt use `expect "<exact prompt text>"` followed by `send "<answer>\r"` — sending the four required values in the order the script asks for them (device path, mount point, filesystem type, mount options), and end with `expect eof` so the spawned process is allowed to finish and flush its output file before your script exits. Do not edit setup_mounts.sh itself. `chmod +x /home/user/auto_mount.exp`, run it once, then inspect `/home/user/my_fstab.conf` to confirm it was generated with the four values space-joined plus the trailing `0 0` fstab suffix exactly as the underlying script writes them.