Treat this as two separate sub-problems: recovering the join relationships from the image, and writing a performant C++ join. First run OCR (e.g. `tesseract /app/schema_diagram.png`) to read the schema text and determine which columns in `users.csv`, `orders.csv`, and `items.csv` act as foreign keys linking the three tables — don't assume column names, read them from the OCR output and confirm against the CSV headers. For the C++ program, avoid any nested-loop or repeated linear-scan join: read each CSV once, build `std::unordered_map<key, vector<row>>` indexes (e.g. orders keyed by user_id, items keyed by order_id) in a single pass, then iterate users once, look up their orders in O(1) average time, and for each order look up its items the same way, streaming the nested JSON as you go rather than building giant intermediate structures. Use fast I/O (avoid `std::endl`, prefer buffered `ostringstream` or manual buffer writes, avoid iostream sync overhead) since the target is hundreds of thousands of rows under 1.5 seconds. Skip users with zero orders and orders with zero items per the spec. Compile exactly as instructed and time-test your own binary before considering it done.
