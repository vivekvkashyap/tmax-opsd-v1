Build the pipeline incrementally and verify each stage against real extracted files before wiring it into one script. First extract the tarball and inspect a few files with `file` and `xxd` to confirm your detection logic actually distinguishes ELF, GCode, and SQLite WAL correctly (WAL files are identified by the 4-byte magic `37 7f 06 82/83` at offset 0 — `file` also recognizes them, so cross-check both). Use `find <dir> ! -newer <ref>` or compare `stat -c %Y`/`stat -f %m` against the epoch timestamp for 2024-01-01 to filter by modification time — get the comparison direction right (strictly before, not on-or-after). For ELF files, parse the architecture out of `file`'s output and strip spaces/parens so it fits cleanly into the filename. For GCode, grep the first 5 lines for the marker string, then extract the ModelName value with `grep`/`sed` from the line prefixed `; ModelName: `. Write each classification+rename step so you can test it standalone on one file at a time, then assemble `/home/user/organize_assets.sh` to loop over all files, rename per the schema, and finally `zip -j` (junk paths) the `organized_assets` directory into `/home/user/release_assets.zip` so files sit at the zip root. Run the script and unzip -l to confirm exactly the expected set of files is present.
