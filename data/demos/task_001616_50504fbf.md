Do the steps in order and verify each before moving to the next, since a mistake in segment selection wastes all downstream compression tuning. First `grep`/parse every `.log` file under `/app/backup_metadata/` for a record block containing `Priority: Critical`, and pull that record's `Start_Sec`/`End_Sec` (a simple state-machine or regex over `[Record Start]...[Record End]` blocks is more robust than assuming fixed line offsets, since there may be multiple log files or multiple records). In your C++ program, use OpenCV's `VideoCapture` with `CAP_PROP_POS_MSEC` (or frame index computed from FPS) to seek to `Start_Sec` and read frames up through `End_Sec`, convert each with `cvtColor(..., COLOR_BGR2GRAY)`, and resize to exactly half width/height with `cv::resize`. For the size/quality tradeoff, don't rely solely on OpenCV's VideoWriter defaults — it's usually easier and more controllable to write frames to a temp raw/lossless intermediate and then shell out via `system()` to `ffmpeg` with an explicit H.264 CRF (start around 28-32) to hit the <200KB target while keeping SSIM>=0.85; iterate the CRF value and re-check both the file size and an SSIM comparison against the reference segment locally before finalizing. Confirm the final file is at `/home/user/archived_segment.mp4` and plays (e.g. `ffprobe` it).
