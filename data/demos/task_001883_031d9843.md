First recover the actual C struct definition from the image with OCR (`tesseract /app/struct.png stdout`) rather than assuming a generic struct — read the exact field names, types, and declared order carefully, since field order and names drive the output. Then write `/app/generate_proto.py` to programmatically translate that struct into a proto3 schema: map each C type to its proto3 equivalent (`int32_t`->`int32`, `float`->`float`, `double`->`double`, fixed `char array`->`string`), preserve field order, and number fields sequentially starting at 1. Use the struct name verbatim as the `message` name. Output must start with `syntax = "proto3";` and be saved to `/app/sensor.proto`. Since grading uses a whitespace/comment-normalized `difflib.SequenceMatcher` ratio against a reference (threshold 0.90), focus on getting field names, types, order, and numbering exactly right rather than cosmetic formatting — run your script and inspect the generated file before finishing.
