Start with the image: run OCR (e.g. `tesseract /app/policy.png stdout`) on `/app/policy.png` and read out the exact CSP directive string and the SSH `AllowUsers`/`PermitRootLogin` lines verbatim — don't paraphrase or reformat what OCR gives you, since the verifier greps for an exact string match. Next, read the original `upload_handler.py` to understand its exact CLI args, decoding, and output/error conventions, then write the fixed version preserving all of that but adding a filename check before writing: reject (print exactly `CWE-22 DETECTED`, exit 1) if the decoded filename contains `/` or `..` — safest is to resolve the final path with something like `os.path.realpath` and confirm it stays inside the uploads directory prefix, catching all traversal encodings, not just a literal `../` substring check which fuzzers can bypass with variants. Because a fuzz-equivalence harness with thousands of random inputs will diff your stdout/exit-code/filesystem state against a secure oracle, be conservative: any filename that isn't a plain basename should be rejected the same way the oracle would. Put `# CSP: <extracted text>` as the literal first line of the file. Finally write apply_ssh_hardening.sh that appends the extracted AllowUsers/PermitRootLogin lines to sshd_config via `tee -a` or `>>`.
