Fix and verify each of the three phases independently before running `build.sh` end to end. Phase 1: parse `journal.dat` transaction by transaction (split on `BEGIN`/`COMMIT` markers matching txids), for each transaction sum the character lengths of all its `SET <key> <value>` values (excluding spaces/newlines — value is the text after the key, trimmed), compare against the trailing checksum on its `COMMIT` line, and keep only transactions with a matching `COMMIT` present AND a correct checksum; write the `SET` lines of valid transactions, in original file order, to `recovered.dat` (a transaction missing `COMMIT` entirely, or with a wrong checksum, must be dropped — don't just check checksum and forget to require `COMMIT` presence, or vice versa). Phase 2: implement delta debugging as a real bisection over the 500 `*.src` files — split the current candidate file set in half, run `compile.sh` on each half, recurse into whichever half fails (or narrows the fault), and keep halving until exactly one file remains that reproduces the failure alone; don't linearly test all 500 files one by one, and don't assume the poison file's position. Once found, record its name in `poison.txt` and delete it. Phase 3: replace the naive two-pass variance formula in `aggregate.awk` with a numerically stable one (Welford's running mean/M2 update, or subtract the first value as an offset before accumulating sums) so `sqrt(...)` never goes negative on closely-spaced large numbers, keeping the exact same print format. Then run `build.sh` and confirm `build_success.flag` appears.
