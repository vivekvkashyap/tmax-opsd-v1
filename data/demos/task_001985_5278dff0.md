Build this as one Go program, not a REPL exploration. First write a CSV parser that reads the 10x10 grid into a flat `[]float64` (row-major order matters for reproducibility). For each Q value, solve `alpha*exp(alpha) = Q` with Newton-Raphson: f(a) = a*e^a - Q, f'(a) = e^a + a*e^a = e^a*(1+a), starting at alpha0=1.0, updating alpha_next = alpha - f(alpha)/f'(alpha), stopping when |alpha_next - alpha| < 1e-6 (loop with a break condition checked after computing the update, not before). Then implement the bootstrap exactly as specified: create `r := rand.New(rand.NewSource(42))` ONCE before the resampling loop (not per-iteration, which would desync the sequence), and for each of 10,000 resamples draw 100 indices via `r.Intn(100)`, average the selected alphas, and store the mean. Sort the 10,000 means with `sort.Float64s`, then take the 0-indexed 250th and 9750th elements as the CI bounds (do not use a percentile library that interpolates differently). Format output as `lower,upper` with `%.6f` to `/home/user/alpha_ci.txt`, then actually run `go run` (or build+execute) so the file is produced — don't just leave source unexecuted.