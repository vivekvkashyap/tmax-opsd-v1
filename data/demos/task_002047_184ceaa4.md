Handle the two data sources separately, then join in Python (not in SQL) since one side is JSON. First, `glob` all files under `/home/user/etl_data/events/`, load each as JSON, and walk the nested structure to find the array of action records — inspect the actual keys (e.g. print one document) rather than assuming field names, and collect the set of customer identifiers that have at least one action of type `GRANT_VIP` (note some may later have a revoke-type action too — only the presence of the grant action matters per the task wording, so don't over-filter). Second, query `/home/user/etl_data/primary.db` with sqlite3, joining `orders` to `customers` and summing `amount` per customer where `status='completed'`. Third, in Python, intersect the VIP customer id set with the revenue dict, keep only entries with revenue strictly greater than 0, and sort by `(-total_revenue, customer_name)` so ties break alphabetically. Write `customer_name,total_revenue` rows with no header to `/home/user/etl_data/vip_revenue.csv`, and double check the numeric formatting is a plain float that parses correctly (not scientific notation).
