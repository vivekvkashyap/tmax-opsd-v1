Chain the stages and validate each output before trusting it downstream. First OCR the config image with `tesseract /app/config.png stdout` and parse the `alpha=` and `beta=` values out of the recognized text programmatically (regex) rather than assuming their values — inspect the raw OCR text to catch misreads before using it. Compile `/home/user/sim.c` with `gcc -o sim sim.c` and run `./sim <alpha> <beta>`, capturing stdout as your X/Y data (note the C code seeds `rand()` deterministically, so output is reproducible for given alpha/beta). Fit `Y = mX + c` via ordinary least squares (e.g. `numpy.polyfit(x, y, 1)`) to get slope `m`. For the confidence interval, call `numpy.random.seed(123)` immediately before running exactly 1000 bootstrap resamples (sample `(x,y)` pairs with replacement, refit slope each time), then take the 2.5th and 97.5th percentiles of the resampled slopes as `ci_lower`/`ci_upper` — seeding order and resample count must match exactly since it affects the result. Round all three values to 3 decimals. Finally stand up an HTTP server (Flask/FastAPI/`http.server`) on `0.0.0.0:8080` with `GET /api/result` returning `{"slope":..., "ci_lower":..., "ci_upper":...}`, and keep it running in the background (e.g. `nohup ... &`) so the verifier can query it after your shell exits.