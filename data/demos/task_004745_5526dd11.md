Treat this as two separate deliverables. For the ETL script: run whisper (CLI or python module) against `/app/audio/support_call.wav` to get a transcript, strip it to plain text (trim whitespace/newlines, keep punctuation as spoken unless whisper adds none), look up the row for `support_call.wav` in `/app/data/metadata.csv` with `awk -F,` matching on the filename column, and append `customer_id | account_tier | transcript` to `/home/user/etl_output.log` — get the exact transcript text from whisper's output rather than assuming what it says. For the filter: first actually inspect the files in `/app/corpus/clean/` and `/app/corpus/evil/` with `grep`, `wc -w`, and manual reading to find real discriminating signals (repeated bot phrasing, suspicious keywords like financial/crypto terms, URL patterns, or message length) rather than picking features blindly. Encode those signals as `grep -qiE` pattern checks or length/threshold checks in `filter.sh`, iterating and re-testing against every file in both corpus directories until every clean file exits 0 and every evil file exits 1 — treat the corpus as your own held-out test set since the real grader is adversarial and stricter. Make both scripts executable with `chmod +x`.
