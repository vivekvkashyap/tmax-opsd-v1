Work the pieces in dependency order. First open `extractor.c`: the crash is a classic fixed-size stack buffer overflow from an unbounded `strcpy` — replace it with a length-bounded copy (e.g. `strncpy` into the buffer sized to its capacity minus one, then explicitly NUL-terminate) so long inputs can't smash the stack; then rebuild with the exact `gcc -shared -fPIC` command given. Only after the .so is fixed should you start the gRPC server, since it dlopens that library via ctypes and will segfault otherwise. For the WebSocket payload, connect with the `websockets` library, parse the JSON, and compare the version strings using a real SemVer library (`packaging.version` or `semantic_version`) rather than naive string/tuple comparison — naive comparisons break on multi-digit components (e.g. "1.5.11" vs "1.5.2") and must correctly exclude any version with a pre-release suffix when picking the highest stable one. Generate the protobuf stubs from the .proto file with `grpc_tools.protoc` before importing them, build a gRPC channel/stub to localhost:50051, call the RPC, and write the returned string verbatim (no extra whitespace/newline surprises) to the result log.