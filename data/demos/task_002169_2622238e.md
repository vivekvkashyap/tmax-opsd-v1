Split this into a filtering stage and an aggregation stage you can test separately. In `run_pipeline.sh`, iterate the `.csv` files under `/home/user/raw_data/` and pipe each through `grep -E` (or `awk`) with an anchored regex matching the full valid-row shape: `^[0-9]{4}-[0-9]{2}-[0-9]{2} [0-9]{2}:[0-9]{2}:[0-9]{2},[A-Za-z0-9]+,temp,[0-9]+(\.[0-9]+)?$` — anchoring with `^...$` is what excludes log lines and non-`temp` metric rows that merely start similarly. Feed the concatenated, filtered lines into `aggregate.py` (via stdin or a temp file). In Python, parse the timestamp with `datetime.strptime`, truncate to the hour with `.replace(minute=0, second=0)` to get the tumbling-window bucket, and accumulate sum/count per `(bucket, sensor_id)`. Compute averages, round with `round(x, 2)`, and format as `f"{avg:.2f}"` to avoid dropping trailing zeros. Sort output rows by `(timestamp, sensor_id)` before writing the CSV with the required header. Make `run_pipeline.sh` executable and run it end-to-end to confirm `/home/user/hourly_averages.csv` is produced correctly.
