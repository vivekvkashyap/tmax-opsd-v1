Debug this in three separate, verifiable steps rather than rewriting everything at once. First, the stack overflow: the account hierarchy in `data.db` has a cycle (some account is its own ancestor through parent/child links), so `calculateTotal`'s recursion into sub-accounts must track visited account IDs (e.g. a set/map passed down or a package-level seen-set keyed per run) and skip recursing into an account already on the current path, instead of assuming a tree. Second, the precision bug: replace the `float32` accumulator with integer cents — when reading each `amount` from SQLite, convert dollars to an `int64` count of cents (round to nearest cent rather than truncating, since floating-point dollar values like 100.10 don't convert exactly), sum in cents, and only format back to a `%d.%02d`-style dollar string at the very end via integer division/modulo, not float formatting. Third, fix the SQL in `db.go` to add `WHERE status = 'SETTLED'` so PENDING transactions are excluded. After fixing, `cd /home/user/app && go build -o recon-service . && ./recon-service > result.txt` and confirm the file contains only the single `Total: X.YY` line with no extra output.
