Walk the tree with `os.walk` (or `pathlib.Path.rglob`) over `/home/user/configs`, treating each immediate subdirectory as one application. For each app directory, load `metadata.json` with `json.load` and check `data.get("status") == "rotating"` exactly (don't just check truthiness or substring). Only for matching apps, parse `config.xml` with `xml.etree.ElementTree`, pull the `<version>` tag's text, cast to `int`, and keep the app only if `version > 1` (strict inequality — off-by-one here silently includes or excludes borderline cases). Build the in-archive filename as `f"{app_name}_v{version}.xml"` using the directory's basename as `app_name`. Use Python's `tarfile` module in `"w:gz"` mode and add each qualifying XML file with `tar.add(src_path, arcname=new_name)` so the file lands at the archive root under the renamed name rather than preserving its original directory path — a common mistake is forgetting `arcname`, which nests the original path inside the tarball. Finally sanity-check with `tar -tf /home/user/rotated_configs.tar.gz` to confirm only the two renamed entries appear at the root.