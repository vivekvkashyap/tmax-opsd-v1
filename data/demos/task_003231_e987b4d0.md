Work in two phases. First, fuzz: write a small Python script that generates many random/edge-case byte sequences (include invalid UTF-8 continuation bytes, lone high-bit bytes like `\xff`, `\x80`, truncated multi-byte sequences), base64-encodes each, and invokes `python evaluate.py '{"expr_b64": "<b64>"}'` via subprocess, checking the return code. Keep the first payload whose exit code is non-zero on the current `HEAD` — this is likely an unhandled `UnicodeDecodeError` from strict `.decode('utf-8')`, so target bytes that are invalid UTF-8 but still valid base64. Save that exact base64 string. Second, automate `git bisect`: write a small shell script usable as the bisect run-script that checks out the current commit's `evaluate.py`, runs it with your saved payload, and exits 0 if the tool exits 0 (good) or 1 if it exits non-zero (bad) — then drive `git bisect start HEAD HEAD~200`, `git bisect run <script>`, and capture the reported first-bad commit hash. Restore the repo to `HEAD` afterward (`git bisect reset`), then write the full 40-char hash to `/home/user/bad_commit.txt` and the payload to `/home/user/crash_payload.txt`. Double check the payload cleanly exits 0 on `HEAD~200` and non-zero on `HEAD`.