Clean the CSV with a shell pipeline before touching Python: strip CRLF endings (`tr -d '\r'` or `sed 's/\r$//'`), swap the pipe delimiter for commas (`tr '|' ','` or `sed 's/|/,/g'`), and strip the `<b>`/`</b>` tags (`sed -e 's/<b>//g' -e 's/<\/b>//g'`) — chain these into a single pipeline writing to `/home/user/clean_metadata.csv`, keeping the original header row intact. In Python, read that cleaned CSV row by row, open each referenced blob file in binary mode, and compute `hashlib.sha384(data).hexdigest()`. Build the curated path as `curated_repo/<hash[:2]>/<hash>.blob`, creating directories with `os.makedirs(..., exist_ok=True)` before copying. Accumulate one JSON object per row with exactly the required keys and dump the full list to `registry.json` (use `json.dump` with indent for readability, but ensure it's valid JSON either way). Finally, walk `curated_repo` for `.blob` files, filter by `os.path.getsize(path) > 500`, sort the absolute paths alphabetically, and write one per line to `large_blobs.txt`. Double-check byte size thresholds are strict (`>`, not `>=`).