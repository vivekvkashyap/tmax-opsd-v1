Run `tesseract /app/policy_doc.png stdout` (upscale/threshold the image first if OCR is noisy) to recover the visible 12 characters of the 14-character key prefix; the final two characters are smudged but known to be uppercase A-Z, so brute-force all 26×26 combinations. For each candidate full 16-char key, decrypt `/app/test_payload.hex` per the spec (bytes 0-15 IV, 16-47 HMAC, 48+ ciphertext) using `openssl enc -d -aes-128-cbc -K <hex-key> -iv <hex-iv> -nosalt`, and accept the key only when decryption succeeds and the plaintext exactly matches the known reference string. Once the key is found, build `/home/user/verify_payload.sh` implementing the four checks in order — hex/length validation, HMAC-SHA256 comparison via `openssl dgst -sha256 -hmac` (or `-mac hmac`) using constant structure, then AES-128-CBC decryption catching padding failures — emitting the exact specified error strings and exit codes at each stage, with success printing the raw decrypted bytes with no extra newline. Test edge cases (odd-length hex, short payloads, tampered HMAC) manually before trusting the fuzzer.
