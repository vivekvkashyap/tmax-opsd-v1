Build this as a single streaming pipe chain (grep/awk/sort/awk/sort), never a bash `while read` loop — the log is large and per-line bash iteration will be far too slow. Steps: (1) `grep -v` (or awk) to drop lines whose Action field starts with `DEBUG_`; (2) `sort -t'|' -k2,2 -k1,1` to group by UserID and order each group chronologically by Timestamp (a stable multi-key sort, not per-user shell loops); (3) a single awk pass over the sorted stream that tracks the previous user and previous action, starts a new accumulated action-list on user change, and only appends an action when it differs from the immediately preceding one (consecutive-duplicate collapse, not full dedup); (4) emit `UserID\tAction1,Action2,...` per user; (5) pipe that directly into `/app/anomaly_scorer`, treating it as an opaque stdin->stdout filter — do not try to reverse engineer or bypass it; (6) `sort -t$'\t' -k2,2nr -k1,1` on its output (numeric descending score, then ascending UserID for ties); (7) `head -n 100`, convert tabs to commas, and redirect to `/home/user/top_100_anomalies.csv`. Verify correctness on a small synthetic sample before running on the full file.