Extract the ground truth from the image before writing any logic. Run `tesseract /app/abi_matrix.png stdout` (or convert/preprocess the image first if OCR is noisy — upscale, grayscale, threshold) to recover the GLIBC-version-to-CFLAGS mapping table, and manually double-check the OCR output line by line against what you'd expect a version-flags table to look like, since OCR errors on characters like `_`, `.`, or `-` are common and would break exact-match comparisons. Once you have a clean, verified mapping, hardcode it as a Python dict in `/home/user/generate_flags.py` (faster and more reliable than OCR-at-runtime for a fuzzer hammering thousands of inputs). Read stdin line by line, strip each line, look it up in the dict, and print `TARGET <version>: <flags>` on a match or `TARGET <version>: UNKNOWN` otherwise — pay attention to exact spacing (one space after the colon) since the grader does bit-for-bit comparison against a reference. Test with the known-good versions plus edge cases (trailing whitespace, unknown strings, empty lines) to confirm your formatting matches exactly before relying on the fuzzer to validate it.
