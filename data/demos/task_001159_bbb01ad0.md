Treat /app/legacy_dedup strictly as a black box and derive its behavior empirically before writing any replacement code — pipe carefully chosen probe inputs into it (mixed case, punctuation, multiple/leading/trailing spaces, empty lines, lines that normalize to the same content, tabs, non-ASCII bytes near the edges) and observe stdout for each to infer the exact normalization and dedup rules, rather than assuming a plausible algorithm. Confirm hypotheses like "non-alphanumeric becomes a single space, case is folded, runs of spaces collapse, and empty results are dropped" by testing edge cases such as a line that's entirely punctuation, or two differently-punctuated lines that should normalize to the same string, and check whether the second one is suppressed. Once you're confident in the rules, implement the C++ replacement to stream input line-by-line (avoid `getline` buffering everything, and keep only the dedup hash set in memory, not full input) and write it to /home/user/new_dedup. Before considering it done, replicate a handful of the same probe inputs you fed the oracle earlier and diff your program's output against the oracle's output byte-for-byte — this is exactly what the fuzz-equivalence check will do at scale.