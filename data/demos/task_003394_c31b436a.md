Use `csv.DictReader` (which correctly handles quoted fields containing embedded newlines, unlike naive line-splitting) and open each file with `encoding='utf-8', errors='replace'` so invalid bytes become U+FFFD instead of raising or being silently dropped — this is exactly what a naive shell pipeline (e.g. `awk`/`grep` splitting on newlines) gets wrong. For each row, classify `Action` by checking `RawMessage` case-insensitively for LOGIN/LOGOUT/TIMEOUT in that priority order, defaulting to "unknown". Compute the dedup key as `hashlib.md5(f"{Timestamp}|{User}|{Action}".encode()).hexdigest()` over all rows gathered from every `*.csv` file. Sort the full combined row list by `(Timestamp, LogID)` ascending before deduplicating, then walk the sorted list keeping only the first row per distinct hash (this makes "first occurrence" well-defined and deterministic — don't dedupe before sorting, and don't dedupe per-file). Write each kept record as one JSON object per line to `/home/user/processed_logs.jsonl` with exactly the four required keys. Double check row counts and a few hash values by hand against your understanding of the input files.
