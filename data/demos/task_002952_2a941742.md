Recover the missing spec first, then harden the code, then prove equivalence empirically. Run `tesseract /app/kernel_spec.png stdout` to extract the integer weights used in the checksum formula, and sanity-check the OCR result (weights should be small positive integers) before hardcoding them into `/home/user/project/main.c`. Fix the three named defects: (1) validate each token before converting — skip any token containing non-numeric characters instead of calling `atoi` blindly (use `strtol` with an endptr check, or manually verify all chars are digits); (2) fix the sliding-window loop bound so the final valid window is included (`i < count`, not `i < count - 1`); (3) explicitly zero-initialize the window/accumulator array so no uninitialized-memory garbage leaks into output. Compile to `/home/user/fixed_bin`. Then write a small shell fuzz-test loop that generates random mixes of digits and letters/whitespace, feeds identical input to both your binary and the reference oracle, and diffs their stdout — iterate on any mismatches found this way rather than assuming your first fix is complete, since edge cases (empty input, all-invalid tokens, exactly 3 numbers) are easy to miss by inspection alone.
