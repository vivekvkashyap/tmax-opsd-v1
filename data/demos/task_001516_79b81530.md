Split this into three independent, verifiable pieces. (1) Log archiver: in Go, walk `/app/legacy_logs/` with `filepath.Walk`, and for each file detect encoding rather than assuming — check for a UTF-16LE BOM or use `golang.org/x/text/encoding/unicode` and `golang.org/x/text/encoding/charmap` (ISO-8859-1) to decode to UTF-8, falling back sensibly if BOM is absent (e.g. try UTF-16LE decode and check for validity, else treat as Latin-1 since it's always "valid" byte-for-byte). Scan decoded lines for the exact substring `CRITICAL_BACKUP_FAILURE`, collect matches, then write to a temp file (e.g. `os.CreateTemp` in the same directory as the destination) and call `os.Rename` to the final `aggregated.log` path only after the write succeeds and is flushed/closed — this is the atomic-write requirement the verifier statically greps for (`os.Rename`/`Rename(`). (2) Video audit: use `ffmpeg -i /app/audit.mp4 -vf format=gray frame_%04d.png` (or similar) to extract grayscale frames, then compute each frame's mean pixel intensity (a short script or `convert ... -format "%[mean]"` from ImageMagick works) and count frames exceeding the threshold of 100, writing the integer to `light_frames.txt`. (3) Ensure `archive_stage/` contains exactly the two expected files, then `tar -czf /app/final_backup.tar.gz -C / app/archive_stage` (or equivalent) preserving the `app/archive_stage/...` path structure the verifier expects when it extracts the tarball.