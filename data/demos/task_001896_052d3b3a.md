Run `tesseract /app/params.png stdout` and read the raw text to find the `MULTIPLIER=` and `MODULO=` lines, then parse the two integers out of that OCR text (strip whitespace, watch for OCR digit misreads and sanity-check the values look like a multiplier/prime pair) rather than hardcoding numbers you eyeball once. Before fixing anything, run `cargo build` to capture the exact borrow-checker/lifetime error in the `RingBuffer` — typical causes are storing a `&T` field without a lifetime parameter, or a method returning a reference to a value that's dropped at the end of its scope; fix by adding an explicit lifetime parameter to the struct/impl (`struct RingBuffer<'a> { ... }`) or by changing the struct to own its data instead of borrowing, without altering the overall push/read API the rest of the code depends on. Substitute the OCR-derived constants into the existing formulas rather than reworking the hashing algorithm itself. After `cargo build --release` succeeds, manually pipe a few known byte sequences through the binary via stdin and sanity check the hex output is stable and deterministic (same input -> same output) before considering it done, since the grader will fuzz-compare against a hidden reference.
