Structure this as separable stages inside one Rust binary. (1) `cargo new dataset_organizer` at the required path, and parse `/home/user/data.csv` (a manual split on commas is fine for two simple columns, or add the `csv` crate to Cargo.toml). (2) Feature vector: for each text, lowercase it and slide a 2-character window across it counting occurrences of each of the five specified bigrams in order — be careful this is overlapping-substring counting (e.g. scan every adjacent character pair, not just word-boundary bigrams). (3) LOOCV: for each text held out in turn, compute Euclidean distance in 5-D space to every *other* text, take the k nearest, and vote on label by frequency; break ties in the vote by alphabetically-smallest label. Repeat for k=1,2,3 and compute accuracy = correct/total for each. Pick the k with highest accuracy, breaking ties by smallest k. (4) Write `/home/user/best_model.json` with `best_k` and `accuracy` (as a float) in the exact key names given. (5) `/home/user/run_pipeline.sh` should `cargo build --release` then run the resulting binary, and be `chmod +x`.
