Trace the 502 to its two independent causes before touching the script: the C server currently binds a different socket path than what nginx's `proxy_pass` expects, so both sides must agree on `/home/user/run/app.sock` exactly (edit the `strncpy`/socket path literal in `server.c`, and edit the `proxy_pass http://unix:...;` line in `nginx.conf` to match). When testing manually, remember Unix domain sockets can't be rebound while a stale file exists, so `unlink()`/`rm -f` the socket path before the C server calls `bind()` each run. For `setup.sh`, make every step idempotent: `mkdir -p` + `chmod 700` for `/home/user/run` (safe to repeat), recompile with `gcc` unconditionally (overwriting the binary is idempotent), kill previously running `server`/`nginx` processes with a pattern-based `pkill -f` guarded so a "no process found" exit code doesn't abort the script (e.g. `pkill -f ... || true`), remove any leftover socket file, then start the compiled server and `nginx -c ... -p ...` both backgrounded with `&`, `sleep 1`, and finally `curl -s http://127.0.0.1:8080/api > /home/user/success.log` capturing only the body. Run the script twice in a row yourself to confirm the idempotency requirement actually holds before considering the task done.
