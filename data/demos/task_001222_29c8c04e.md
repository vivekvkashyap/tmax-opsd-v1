Edit the notebook's JSON structure directly rather than trying to use a Jupyter GUI. Load `/home/user/simulation.ipynb` with `nbformat` (or parse the raw JSON) in a short Python script, find the cell containing the `np.linalg.cholesky(C)` call, and modify its source to regularize `C` immediately before that call (add a small multiple of the identity matrix of matching shape to `C`, e.g. compute `C.shape[0]` for the size) — insert this as a line within the same cell rather than a separate cell, so you don't disturb the sequence/order the task requires, and don't touch the RNG seed or the signal-generation/FFT cells. Save the notebook back with `nbformat.write`. Then execute it headlessly and in-place with `jupyter nbconvert --to notebook --execute --inplace /home/user/simulation.ipynb`, making sure to set `OMP_NUM_THREADS=2` in the environment of that exact command (e.g. prefix the command with the env var) since it affects BLAS threading determinism. After execution, confirm `/home/user/trace.txt` was written and that the notebook shows no error output and has execution counts on every cell — treat a missing/empty trace.txt or an error cell as a signal to inspect the modified cell's source for a syntax mistake before retrying.