Work in three stages and verify each before moving on. First, transcribe `/app/intercepted_comms.wav` (install/use `whisper`, `ffmpeg`+a speech tool, or `speechrecognition`) — read the transcript carefully to recover the exact hidden admin endpoint path and username; don't guess or hardcode a plausible-looking path. Second, read `/home/user/vulnerable_app.py` to understand how the discovered admin endpoint triggers a diagnostic subprocess; write a small Python script that hits that endpoint (with the recovered username) while concurrently polling `/proc/<pid>/cmdline` (or scanning `/proc/*/cmdline`) fast enough to catch the short-lived child process and read the leaked SSH token from its argv. Third, once you've confirmed by observation the three vulnerabilities (proc-visible token in argv, shell/command injection in how the diagnostic script builds its command line, and cookies set without `httponly`/`secure`), write `/home/user/patched_app.py`: replace shell-string command construction with an argument-list subprocess call (no `shell=True`, no string interpolation into a command), pass the sensitive token via `env=` instead of argv, and add `httponly=True, secure=True` to cookie-setting calls. Iterate by running `python3 /app/grade_security.py /home/user/patched_app.py` until it prints exactly 1.0.