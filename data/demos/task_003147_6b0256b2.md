Build this bottom-up and verify each layer before moving to the next. First write and compile the C/C++ stdev function (population stdev = sqrt(sum((x-mean)^2)/N)) as a shared object with `-shared -fPIC -o libmetric.so`, and sanity-check it standalone (e.g. a tiny test C program or `ctypes`/FFI call from a Python REPL) before wiring it into a server — a common failure is silently getting the sample-stdev formula (dividing by N-1) instead of population (divide by N). Next write the web server (any language, e.g. Python `http.server`/Flask or Node) binding to `127.0.0.1:8181`, parsing the `build_times` query param by splitting on commas and casting to floats, calling into `libmetric.so` via FFI (`ctypes.CDLL`, `dlopen`, etc.) rather than reimplementing the math in the server language, and rounding the result to exactly 2 decimals in the JSON response. Test manually with `curl` against known inputs (e.g. verify against a hand-computed stdev) before writing `test_integration.sh`. Keep the server running in the background (e.g. `nohup ... &` or `disown`) so it survives after your session ends, and make `test_integration.sh` executable and self-contained (`curl` + parse + compare + explicit exit code).