Since you must not call the binary from your final script, use it only during analysis: run `strings`/`objdump`/`gdb` (or ltrace/strace) against `/app/c2_auth_check` to see which library calls it makes (look for X.509/OpenSSL-related symbols, string comparisons on certificate fields, and any hashing calls) and black-box it by feeding it a range of self-crafted test certificates (self-signed vs CA-signed, varying CNs, varying content to shift the hash) to observe which single property flip changes its exit code — this isolates each independent check. Once you've enumerated the checks (issuer/subject relationship, a CN suffix pattern, and a hash-based proof-of-work condition on the DER bytes), reimplement each independently in pure Python using the `cryptography` library: load the PEM, compare `cert.issuer == cert.subject` for self-signed, pull the CN via `get_attributes_for_oid(NameOID.COMMON_NAME)` and check its suffix, and hash the `public_bytes(Encoding.DER)` output to test the digest condition — get the exact hash algorithm and digest-prefix rule right by testing against your generated corpus samples rather than guessing. All three conditions must hold for "C2"; any failure (or parse error) should classify as "BENIGN". Test your classifier against both corpora before finishing.