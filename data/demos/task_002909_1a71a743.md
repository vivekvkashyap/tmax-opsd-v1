Work through the four fixes independently and verify each with a direct check before moving on. (1) Backup first, before any edits: `mkdir -p /home/user/backups && tar czf /home/user/backups/config_backup.tar.gz -C /home/user config` (or equivalent) so the tarball contains app.conf. (2) Open app.conf and look closely at the `upstream_socket` value character-by-character — it's a subtle typo (a missing letter), not a wrong path structure; correct it to the exact target path given in the task. (3) In supervisor.sh, add `sleep 5` on its own line inside the `while` loop, immediately before the point where the daemon is (re)started, so a crash-restart loop is throttled. (4) For the metrics: grep only lines containing "Connection refused" from error.log, extract the `YYYY-MM-DD HH:MM` prefix (truncate/drop the seconds field, e.g. with `cut`/`awk`/`sed` on the timestamp), then group and count occurrences per distinct minute, output sorted chronologically as `[YYYY-MM-DD HH:MM] - <COUNT> errors` — a one-liner like `grep 'Connection refused' file | cut -c1-16 | sort | uniq -c` reordered into the target format works well; double check whitespace and bracket formatting matches exactly.