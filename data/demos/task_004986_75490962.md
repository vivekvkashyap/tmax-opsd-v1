Transcribe `/app/policy_brief.wav` yourself (using an available speech-to-text tool, or careful manual listening) to extract the two concrete constraints: the numeric threshold for CSP/HSTS `max-age` and the CWE identifier the ELF auditor must flag — don't assume standard/textbook values, use exactly what the briefing states. Then implement `policy_auditor.py` to: parse the single JSON CLI argument (`sys.argv[1]`), pull `csp_header` and `elf_metadata`, extract the numeric `max-age=` value from the CSP string (regex it out, handling it appearing anywhere in the string), and compare it against the transcribed threshold using the exact comparison operator implied by the briefing's wording (e.g. "strictly greater than" means `>`, not `>=`). For `elf_metadata`, split on commas and check whether any segment flag matches the vulnerable pattern described (e.g. a segment containing both write and execute bits) tied to the transcribed CWE. Print bare `1` or `0` to stdout with no extra text/newize surrounding it, since the grader does exact byte comparison against a hidden oracle across thousands of fuzzed inputs — test edge cases (boundary max-age values, multiple/absent RWE flags) manually to make sure your comparison operators match the transcribed rule precisely.
