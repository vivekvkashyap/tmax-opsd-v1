Work in order: first transcribe `/app/dba_notes.wav` (e.g. `whisper`/`ffmpeg`+speech-to-text, or inspect the WAV for embedded text/metadata if TTS-generated) to recover the target user ID and the bearer auth token from the spoken sentence — do not guess these. Start MongoDB locally with a writable dbpath, then write a small Python ingestion script that reads the CSV with the `csv` module and inserts each row into `deadlock_db.locks` via `pymongo`, keeping `timestamp` numeric for ordering. For the aggregation/deadlock logic, think in terms of graph edges: for the target user, find a resource they REQUESTED that another user already GRANTED, and a resource that other user REQUESTED that the target user already GRANTED (a 2-cycle) — this can be done with a `$match`/`$lookup`/`$group` pipeline or equivalently in plain Python after fetching from Mongo, then sort the two resource IDs alphabetically. Build the Flask/FastAPI app to parse the `Authorization: Bearer <token>` header manually and compare against the token from the audio, returning 401 on mismatch/absence; run it with `uvicorn`/Flask dev server bound to 127.0.0.1:8080 backgrounded (e.g. `nohup ... &`) so it stays alive for external checks.