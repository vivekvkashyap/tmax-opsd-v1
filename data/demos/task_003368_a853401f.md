Build the pipeline in `/home/user/src/aggregator.cpp` as clearly separated stages so each is easy to get right. Parsing: use `std::regex` to match lines like `[<ISO8601>] INFO: Server {<id>} updated config [<key>] -> <value>` capturing the four fields — be careful the value can contain characters like spaces or letters (`DEBUG`, `rotated`, `30s`), so make that capture group permissive (e.g. match to end of line) rather than assuming numeric-only. Deduplicate: insert each parsed record's 4-tuple into a `std::set` (or hash the concatenation) before counting, so exact repeats collapse to one. Join: load `server_meta.csv` into a `map<ServerID, Environment>`, and drop any log record whose ServerID isn't found in that map (this discards the unknown-server line in the fixture). Bucket: truncate each ISO8601 timestamp to the top of the hour by zeroing minutes/seconds in the string (string slicing on the fixed-width ISO format is simplest and safe here) rather than reformatting through a locale-dependent date library. Aggregate: count distinct (post-dedup) records per (hour, environment) pair, then emit a JSON array sorted first by hour, then by environment name, with 4-space indentation matching the exact key order and structure shown in the task. Compile with the exact `g++ -std=c++17 ... -o /home/user/bin/aggregator` command and run the binary to produce the output file — don't hand-write the JSON.
