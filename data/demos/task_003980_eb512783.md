First recover the secret: transcribe `/app/intercepted_comms.wav` with a local speech-to-text tool (e.g. `whisper` CLI, or any installed ASR library) rather than guessing — listen to what phrase is actually spoken and normalize it consistently (decide once whether spaces, underscores, or no separators are used, since the byte-exact string matters for HMAC). Confirm the transcription by re-listening or checking segment confidence before locking in the secret string. Then write `/home/user/token_filter.py` using PyJWT: for each file in the given directory, read the token string, call `jwt.decode(token, secret, algorithms=["HS256"])` wrapped in try/except, printing ACCEPT on success and REJECT on any `InvalidSignatureError`/`DecodeError`/other JWT exception. Do not hardcode any assumption about claims or expiry — only verify the signature; disable expiration/audience checks if they aren't part of the spec (`options={"verify_exp": False}` etc.) so tokens aren't wrongly rejected for unrelated reasons. Validate your script against both `/app/corpus/clean/` and `/app/corpus/evil/` yourself before finishing, iterating on the derived secret if you see misclassifications — a single wrong character in the transcribed secret will fail every token.
