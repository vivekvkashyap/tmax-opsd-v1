Break this into independent pieces and test each in isolation before wiring them together. First read `/home/user/new_user_config.ini` with Python's `configparser`. For the interactive CLI, install `pexpect` if missing (`pip install pexpect`), then spawn the tool and use `child.expect("Enter username: ")` / `child.sendline(...)` for each prompt in the exact order given (username, password, sandbox y/N, quota); after the last send, `child.expect(pexpect.EOF)` and read `child.before` to get the full transcript. Parse the success line with a regex like `UID (\d+)` rather than assuming a fixed offset — don't hardcode the prompt text loosely, match it exactly as specified. For the routing script, write the literal `ip route add <ip> dev <username>_veth` line to `/home/user/user_routing.sh` and `chmod 0755` it (0644 will fail verification). For the daemon, check for a running `quota_daemon.py` process (e.g. scan `/proc` or use `pgrep -f`) before starting a new one with `subprocess.Popen`, and capture its real PID — don't guess or reuse a stale PID. Finally assemble the JSON report with correct types (uid and quota_mb as integers, not strings) and write it to the exact path required.
