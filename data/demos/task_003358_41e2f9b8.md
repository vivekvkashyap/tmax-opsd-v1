Fix the build first, then the algorithm, then add the test, verifying incrementally. In `CMakeLists.txt`, add `target_link_libraries(test_app math_lib)` after the executable target — a missing link directive is the classic cause of undefined-symbol errors at link time even though both targets compile fine individually. In `math_lib.cpp`, the loop bound is off-by-one: iterating `i <= v.size()` writes/reads one element past the fixed-size array, so change it to `i < v.size()` (or equivalent strictly-bounded iteration) without altering the redistribution logic or intent. For the property-based test in `test_app.cpp`, generate 100 random `FixedVector`s (seed the RNG, values mod 20), call `solve_constraint(v, 100)` on each, and check both invariants (sum == 100, all elements >= 0) before writing `PROPERTY_TEST_PASSED` or `PROPERTY_TEST_FAILED` to the log file — write the log unconditionally based on the actual check results, don't hardcode success. Finally rebuild via `cmake .. && make` in the `build` dir and run `test_app` to confirm no segfault and the log file is correct.
