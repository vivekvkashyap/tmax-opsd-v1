Translate the Cypher-style 4-hop pattern into a single SQL query using the SQLite C API rather than trying to walk the graph manually in C — chain three self-joins across `edges`/`nodes` (USER —ASSUMES→ ROLE —INHERITS→ ROLE —CAN_MODIFY→ USER), filtering out rows where the starting and ending user id are equal. For the "highest risk path per target user" requirement, use a window function: `ROW_NUMBER() OVER (PARTITION BY target_user ORDER BY path_risk DESC, source_user ASC)` and keep only rank 1 — this cleanly handles the tie-break rule (alphabetically first source on equal risk) without extra application logic. Sum the risk_weight of all four nodes per path exactly as specified. When emitting JSON from C, don't hand-roll ad-hoc string concatenation carelessly — build each object with the exact three keys in the given order, escape nothing unexpected (ids are simple identifiers here), and sort the final array by target_user before writing. Prepare the statement with `sqlite3_prepare_v2`, step through results, and check return codes so query errors don't silently produce an empty file.
