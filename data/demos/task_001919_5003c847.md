Handle each of the three deliverables independently and verify each in isolation. First, open `/app/vendor/tunneled_smtp_client-0.9/tunneled_smtp_client/connection.py` and find the `connect()` method — grep for `22` or `ssh_port`; the bug is a hardcoded literal port overriding the instance's configured `self.ssh_port` attribute, so replace the hardcoded value with a read of `self.ssh_port`, then install the package editable with `pip install --user -e /app/vendor/tunneled_smtp_client-0.9/` and write a quick smoke test that instantiates a connection with a non-default port to confirm it's actually used, not silently ignored. Second, for `format_alert.py`, don't guess the oracle's output format — feed `/app/oracle_format_alert` several varied JSON inputs (services up/down, empty error vs message, multiple services) and diff your Python script's stdout against the oracle's stdout byte-for-byte for each, paying attention to exact spacing, section headers, ordering of "up" vs "down" listing, and trailing newlines, since the fuzzer checks bit-for-bit equivalence across randomized inputs — iterate until several hand-picked cases match exactly before trusting it broadly. Third, write `/home/user/monitoring_cron` with a line scheduling `/home/user/run_monitor.sh` every 5 minutes (`*/5 * * * * /home/user/run_monitor.sh`) and load it into the crontab with `crontab /home/user/monitoring_cron`, then confirm with `crontab -l` that it's actually installed.
