For the video task, extract per-frame average luma with ffmpeg (e.g. `ffmpeg -i /app/sync_video.mp4 -vf signalstats -f null -` to get per-frame `YAVG` stats, or extract frames and average pixel values yourself) and parse the brightness values to find every frame whose average luma exceeds 128; write the 0-indexed frame numbers sorted ascending to `/home/user/corrupted_frames.txt`, one per line — don't guess frame count/timing, derive it from the actual video (fps × duration) rather than assuming. For the C sanitizer, write a CSV row parser that reads the header, then for each `category_token` field validates strictly: reject if it contains a `.` (decimal point), reject case-insensitive `nan`/`inf`, reject non-numeric garbage, and reject values `<= 0`; use `strtod` or manual parsing plus a check that the string has no fractional part and consists only of digits (with optional leading `+`) to distinguish `"5"` from `"5.0"`. Exit 0 the moment the whole file is confirmed clean (all rows pass), exit 1 as soon as any row fails validation — test against varied edge cases (leading zeros, whitespace, empty fields) before trusting the sanitizer against the hidden adversarial corpus.