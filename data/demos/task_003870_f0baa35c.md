Write the C parser as a streaming state machine over `/home/user/backups/db.wal`: verify the `BKWAL1\n` magic, then loop reading lines until `BEGIN`, then parse the `FILE:`, `SIZE:`, `CHECKSUM:` header lines with `sscanf` (stripping trailing newlines), read exactly `SIZE` raw bytes after the `DATA\n` marker using `fread` (not line-based reading, since binary data can contain newlines/nulls), then confirm the following line is `END`. Compute the checksum as a straight `unsigned char` accumulator summing all data bytes mod 256, format as two-digit uppercase hex (`%02X`), and compare case-sensitively against the header's `CHECKSUM` value — skip the whole record (don't archive, don't log) on mismatch. For archiving, open `archive.bin` for append (`O_WRONLY|O_APPEND|O_CREAT`), and around each record's write, acquire an exclusive lock with `fcntl(fd, F_SETLKW, &lock)` where `lock.l_type = F_WRLCK` covers the whole file (or at least the region being appended), then release it with `F_UNLCK` immediately after that record's write — do this per-record, not once for the whole run. Write each field of the archive format in little-endian order using `fwrite` with explicit `uint32_t` for lengths (path length, no null terminator; then data length; then raw data). Append the `ARCHIVED <path> <size> bytes` line to `process.log` only for successfully archived records, in file order.
