Work top-down through the broken pipeline. First fix `fastdtw`: inspect `/app/fastdtw-0.3.4/setup.py` for the line causing the import error (it references a nonexistent module) and remove/comment just that bad import, then `pip install -e /app/fastdtw-0.3.4` and confirm `from fastdtw import fastdtw` works before moving on. Second, write a script that loads all 30 CSVs from `/home/user/data/`, z-score normalizes each `signal` column (subtract mean, divide by std) so DTW is scale-invariant, then computes the full pairwise DTW distance matrix using `fastdtw`. Third, feed that distance matrix into `sklearn.cluster.AgglomerativeClustering(n_clusters=3, linkage='complete', metric='precomputed')` (or `affinity='precomputed'` depending on sklearn version) and write cluster assignments to `/home/user/clusters.json` as `{"cluster_id": [filenames]}`. Fourth, open `/home/user/plot_clusters.py` and diagnose why it fails headless — it's almost certainly missing `matplotlib.use('Agg')` before importing `pyplot`; add that line at the top before any pyplot import, then run it to produce `/home/user/dendrogram.png`. Validate your clustering quality makes sense (3 visually distinct groups) before finishing, since the grading compares against hidden ground-truth cluster labels.