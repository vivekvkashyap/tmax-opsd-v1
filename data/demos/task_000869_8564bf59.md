Debug this as a real multi-service integration, verifying each hop before moving to the next. First get Redis running and confirm the `SEQ_99` diffusion coefficient D is actually stored/readable (`redis-cli GET ...` or equivalent) — do not assume a value, read it from Redis. In `app.py`, fix the Redis connection to point at `localhost`/`127.0.0.1:6379` (replace any placeholder host) and bind Flask to port 8080; make sure the subprocess invocation of the compiled worker passes D and other params correctly and captures its stdout. In `pde_worker.cpp`, the core bug is a fixed/unstable timestep — replace it with a CFL-derived timestep computed from the actual grid spacing dx and D at runtime, e.g. `dt = safety_factor * dx*dx / (2.0*D)` with safety_factor < 1, and recompute total steps from `time_end/dt` — do not leave dt hardcoded. Parallelize the spatial update loop with `#pragma omp parallel for` and compile with `-fopenmp`. For the SVD step, use Eigen's JacobiSVD/BDCSVD on the concentration-history matrix and take column 0 of `matrixU()` (the first left singular vector), fixing whatever index/transpose bug currently selects the wrong vector. After wiring everything up, curl the `/analyze` endpoint end-to-end, inspect `/home/user/output_mode_SEQ_99.txt`, and sanity-check its shape/sign against expectations for a smooth diffusion mode before considering it correct.
