Do the schema discovery first: run `sqlite3 /home/user/source.db .schema` to find the real customers/orders/order_items tables and their join columns and date column, rather than assuming names. In Phase 2, write /home/user/optimize.sql with at least three `CREATE INDEX` statements targeting the actual join keys (orders.customer_id, order_items.order_id) and the filter column (orders.order_date), then apply it with `sqlite3 /home/user/source.db < /home/user/optimize.sql`; confirm indexes exist via `PRAGMA index_list('orders');`. For Phase 3, write /home/user/etl.sh accepting `$1` as the date and pass it as a bound parameter to the sqlite3 CLI rather than string-interpolating it into the SQL text — the CLI supports positional parameters (e.g. `sqlite3 -header -csv db.db "SELECT ... WHERE order_date = ?" "$1"`), which satisfies the "pass parameters safely" constraint and avoids injection. Join customers→orders→order_items, group by customer, SUM(quantity*unit_price), format with `printf('%.2f', ...)` inside SQL for exact 2-decimal formatting, ORDER BY customer_id ASC, and redirect CSV output to /home/user/report_<DATE>.csv with header row included. `chmod +x` the script and test it against a couple of dates to confirm exact CSV formatting before finishing.