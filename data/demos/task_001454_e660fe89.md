Treat the binary strictly as a black box and reverse-engineer it empirically before writing any replicant code. Probe it with controlled inputs: vary only length (e.g. same string vs. string+padding) to isolate a length-difference term, then vary only vowel counts (feed strings with all consonants vs. strings with known vowel counts, holding length constant) to isolate a vowel-count term, and test with mixed-case and strings containing spaces to confirm case-insensitivity and that non-letters are ignored/counted consistently. Once you have a hypothesis for the formula (a linear combination of a vowel-count difference metric and a length difference metric, each with its own integer weight), validate it against many more random probe pairs by actually calling the binary and diffing predicted vs actual output before trusting it. Only then implement the confirmed formula in pure Bash/awk (bash arithmetic is fine, use `awk` for character counting since it's fast and portable) — no calls to `/app/sim_scorer` in the final script. For parallelism, split the CSV into chunks or pipe rows through `xargs -P<N> -I{}` (or GNU `parallel`) calling a per-row scoring function, but preserve original row order in the output — e.g. by tagging each row with its line number before parallel dispatch and sorting results back into order afterward, since parallel execution can complete out of order.
