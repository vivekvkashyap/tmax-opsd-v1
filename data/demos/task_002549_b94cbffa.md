Start by reproducing the build failure: `cd /app/sod-graph-checker-1.1.0 && make` and read the linker error carefully — a symbol-not-found error for a math function (e.g. `pow`) at link time almost always means a missing `-lm` on the compiler/linker invocation, not a source bug. Open the Makefile, find the link rule for `bin/check_sod`, and append `-lm` after the other libs (order matters with GNU ld: object files before libraries, and `-lm` typically goes after `-ljansson`). Re-run `make` and confirm the binary appears and runs, feeding it a small hand-crafted JSON via stdin to sanity-check its output format before wiring up the wrapper. For the wrapper script, write the SQL as two separate queries against `/var/data/hr.db`: one simple join for roles, and one using `ROW_NUMBER() OVER (PARTITION BY resource_id ORDER BY access_time DESC)` (or similar) filtered to `status='GRANTED'`, then outer-ordered by `access_time DESC` and limited to 3 distinct resources. Assemble the JSON precisely (use `jq -n` or a scripting language, not manual string concatenation, to avoid quoting bugs), then pipe it straight into the compiled binary and print only its stdout.
