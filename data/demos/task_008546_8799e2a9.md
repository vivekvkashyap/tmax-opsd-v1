Diagnose the SQL bug precisely before patching: an implicit cross join happens when a query lists two tables with a comma (`FROM servers s, backup_jobs b`) but filters instead of joining on the foreign key, producing every server × every job row and inflating SUM() by a multiplicative factor. Fix `sql_report.sh` by adding an explicit `JOIN backup_jobs b ON b.server_id = s.id` (or equivalent `WHERE s.id = b.server_id` condition) plus the `status='SUCCESS'` filter, then `GROUP BY hostname ORDER BY total_size_mb DESC`, and pipe `sqlite3 -header -csv` output straight to the target file. For the lineage chain, write a recursive CTE (`WITH RECURSIVE chain(id, parent) AS (SELECT child_id, parent_id FROM backup_lineage WHERE child_id = :start UNION ALL SELECT l.child_id, l.parent_id FROM backup_lineage l JOIN chain c ON l.child_id = c.parent) ...`) that walks up via parent_id until no parent exists, then join the ids with `->` in application logic or `group_concat`. For the NoSQL summary, use `jq` to group by `.cluster_id`, filter `.status=="SUCCESS"`, and pick the max-`.timestamp` entry per group (e.g. `group_by(.cluster_id) | map(select(...) | max_by(.timestamp))`), then sort output rows alphabetically by cluster_id before writing CSV.
