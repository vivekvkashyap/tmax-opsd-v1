Debug this systematically rather than rewriting everything at once: first run `systemctl --user status health-server.service` and `journalctl --user -u health-server.service` to see the actual failure, since the task lists multiple distinct bugs (port binding, unimplemented handler, missing cert mount) that will surface as different errors at different stages — fix and re-test one at a time. For the certs, don't just mount them manually; add `ExecStartPre` (using `archivemount /home/user/certs.zip /home/user/app/certs`) and `ExecStopPost` (using `fusermount -u /home/user/app/certs`) directives to the unit file so mounting is automatic and reversible, and make sure the mount directory exists and is empty before mounting. In `main.rs`, change the bind address/port to `127.0.0.1:8443` (binding to ports <1024 requires root, which you don't have) and replace the `/health` handler's `todo!()` with a handler returning HTTP 200 and the literal JSON body `{"status": "healthy"}` (set Content-Type appropriately). Build with `cargo build --release`, then `systemctl --user daemon-reload` and `systemctl --user start health-server.service`, confirming `active (running)` before curling `https://127.0.0.1:8443/health` with `-k` (self-signed cert) and redirecting the exact stdout to the required output file.