Start with git forensics: since config.py was deleted in a later commit, `git log --diff-filter=D --summary` or `git log -p -- config.py` will show the commit that removed it, and `git show <commit>^:config.py` (or just look at the full diff) reveals its last content — recreate that exact file at /home/user/service_repo/config.py rather than inventing a new key. For the memory leak, profile per-frame with tracemalloc snapshots or just watch RSS growth (`/usr/bin/time -v` or reading /proc/self/status) while running against /app/stream.mp4; inspect any long-lived collection that objects get appended to on every frame without ever being cleared — a classic culprit is an event/observer registry where each iteration registers a new closure that captures the current frame's data, so the list grows unboundedly and pins large per-frame buffers in memory. Fix by not accumulating unbounded per-frame state in a structure whose lifetime spans the whole run — either avoid registering a new listener per frame, or clear/reset the registry after each frame is processed — while leaving the actual intensity/statistics computation untouched so results.json output is bit-identical to before. Verify with `/usr/bin/time -v python video_processor.py /app/stream.mp4` and confirm Maximum resident set size stays under 150000 KB.
