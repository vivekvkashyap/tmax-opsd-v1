Do not try to reverse-engineer or disassemble the stripped/UPX-packed binary line by line — instead treat `/app/bin/spec_deconv` as a black box and characterize its hang behavior empirically. Run it against every file in `/app/corpus/clean/` and `/app/corpus/evil/` under a timeout (e.g. `timeout 5s`) to confirm which inputs actually hang vs. complete, then look for a statistical property that separates the two groups — since this is a nonlinear least-squares solver, the classic failure mode is instability driven by localized high-frequency variance/noise bursts in the input signal. For each candidate signal, compute a sliding-window statistic (e.g. variance over short windows of consecutive samples, or an FFT high-frequency energy check) using `awk`, `python3`, or `octave`, and find the threshold that cleanly separates all evil examples from all clean examples — don't guess a threshold, derive it from the corpora. Implement `/home/user/sanitize_wrapper.sh` to compute that statistic on its one argument, print `REJECT` and exit 1 if it crosses the threshold, otherwise pipe the file to the real binary, forward its stdout, and exit 0 — always guard the binary invocation with a timeout as a safety net even after your filter.
