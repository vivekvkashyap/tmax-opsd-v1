First recover the imputation parameters from the image: run `tesseract /app/schema_rules.png stdout` (or similar OCR) and read the extracted text carefully rather than assuming standard values — it specifies the regression approach, the Z-score to use for the confidence interval, and the rounding precision; use exactly what OCR reports. Build the Rust web service (e.g. with `axum` or `actix-web`) listening on `127.0.0.1:9090` with a `POST /clean` handler. For imputation: fit ordinary least squares on only the rows where B is non-null (compute means of A and B over that subset, then slope = covariance(A,B)/variance(A), intercept = meanB - slope*meanA), then predict missing B values from their A values using that line — do not include already-imputed values back into the regression fit. After filling all B values, compute the sample standard deviation (divide by n-1, not n) of the full column, then build the CI as `mean ± Z * stddev/sqrt(n)` using the Z-score you OCR'd. Round every output value to exactly 4 decimal places before serializing. Test your OCR extraction and your regression math against the example values in the task description before wiring up the HTTP layer, then launch the compiled binary in the background so it stays listening when the task ends.
