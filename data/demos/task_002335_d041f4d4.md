Treat /app/auth_backend as a black box to reverse-engineer empirically rather than guessing from the name alone. Start with `strings /app/auth_backend` to spot literal format fragments (prefixes/suffixes it prints), then feed it a handful of controlled inputs of increasing complexity (single char, short known string, string with repeated chars) and observe the output pattern by hand — look for whether output length/content correlates with input length, reversed order, or a character-wise transform (hex encoding is a common one to check by converting output bytes back to ASCII). Use `strace`/`ltrace` if static inspection isn't conclusive, watching for calls like printf/putc that reveal the exact byte sequence being built. Once you have a hypothesis for the transform and any fixed prefix/suffix, implement it purely in Bash using standard tools (`rev`, `xxd -p`, `tr`, `awk`) — avoid anything that adds hidden whitespace or trailing characters the original doesn't produce. Validate by running both the oracle and your script over many random alphanumeric inputs (varying length 8-16) and diffing stdout byte-for-byte before considering it done, since the grader fuzzes hundreds of cases.