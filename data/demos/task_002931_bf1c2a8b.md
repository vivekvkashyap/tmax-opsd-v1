Build the analyzer in stages and test each independently before combining. First, load every `.json` file in the pipeline_defs directory into a dict keyed by task name, then build a directed graph from `depends_on` edges and run a DFS with a recursion stack (not just a visited set) to detect back-edges, which indicate cycles — collect the exact node sequence on the stack when a back-edge is found. When formatting the cycle for the report, rotate the cycle list so it starts at the lexicographically smallest task name, then append that same name again at the end, joined by ` -> `; write an empty file if no cycle exists. For version validation, use the `packaging` library's `SpecifierSet` to parse each `requires` constraint string and `Version` to compare candidates — pick the maximum available version returned by the mocked registry and check `.contains()` against the specifier. Write the mocked `requests.get` test with `unittest.mock.patch`, returning a `Mock` whose `.json()` gives controlled version lists per tool name (route based on the URL argument), so you can independently confirm both a passing and a failing tool are classified correctly. Sort final failing tool names alphabetically before writing.
