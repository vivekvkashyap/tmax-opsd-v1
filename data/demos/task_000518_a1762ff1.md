Work phase by phase and verify intermediate results before writing the final report. For Phase 1, parse `auth_logs.json` (JSON Lines) into a Python list, group entries by `ip`, and for each IP scan for a run of exactly 15 consecutive "failed" statuses whose timestamps span at most a 60-second window, immediately followed (as the very next record for that IP) by a "success" — check this precisely (e.g. `logs_for_ip[i:i+15]` all failed and `timestamp[i+14]-timestamp[i] <= 60`, then `logs_for_ip[i+15]` is success) rather than just counting total failures per IP, since a noisy IP with no success afterward should not match. For Phase 2, use `readelf -x .malconf suspicious.elf` or `objdump -s -j .malconf` (or parse the ELF section table in Python with a library like `pyelftools`) to pull out the raw 8 bytes of that custom section — don't assume the key, extract it directly from the binary. For Phase 3, repeat that 8-byte key cyclically and XOR it against `payload.bin` byte-by-byte to recover the plaintext flag; verify the result looks like sensible ASCII before trusting it. Finally, assemble the attacker IP, the key as lowercase hex, and the decrypted flag into `/home/user/report.json` in the exact structure specified.
