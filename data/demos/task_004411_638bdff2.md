Structure this as three clean stages in your Python script: (1) query `backups.sqlite` for rows where `status = 'FAILED'` to get the failed-service set; (2) load `services.json` and build a dependency map restricted to that set — for each failed service, keep only the entries in its `depends_on` list that are also failed services, dropping edges to successful services entirely; (3) run Kahn's algorithm (BFS topological sort using in-degree counts) over this filtered subgraph, but instead of a plain queue/set for the zero-in-degree frontier, use a min-heap or sort the frontier alphabetically before each pop so that whenever multiple nodes are simultaneously ready, you always emit the alphabetically-first one — this tie-breaking rule is the part naive implementations get wrong (e.g. using an unordered set or FIFO queue silently violates it). Write the resulting order as a JSON array of strings (dependencies-first order, so a service always appears after everything it depends on) to `/home/user/restore_order.json`. Trace through your own dependency graph by hand for a small example to confirm the tie-breaking behaves as expected before finalizing.
