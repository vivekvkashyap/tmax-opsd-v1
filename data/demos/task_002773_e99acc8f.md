Do this in three clearly separated passes and validate each before moving on. First, open `/app/yq-3.2.3/setup.py` and look for a syntax error in the `install_requires` list — a missing closing bracket is the classic version of this perturbation; fix it and confirm with `python3 -c "import ast; ast.parse(open('setup.py').read())"` before installing, then `pip install --user -e /app/yq-3.2.3` and verify `yq --version` (adding `~/.local/bin` to PATH if needed) actually reports the expected version. Second, write `/home/user/pipeline.sh` to take `<input_dir> <output_dir>`, read all `.json` files in sorted filename order, and for each line (one JSON object per line) validate strictly: valid JSON (use `jq` to parse/reject, don't roll your own parser), `duration_ms` numeric and >= 0, `operation` matching `^[a-zA-Z0-9_]+$` exactly (reject if it contains spaces or shell metacharacters — test this against an injection-style value like `foo; rm -rf /`), and `status` exactly `"success"` or `"failure"`. Deduplicate by `request_id`, keeping only the first valid occurrence in file-then-line order — track seen IDs in an associative array or a temp file. Write survivors to `<output_dir>/cleaned.json`, creating the dir if absent. Third, install the cron job with `crontab -l` piped through to add the exact `*/15 * * * * /home/user/pipeline.sh /app/incoming_logs /home/user/processed_logs` line, and re-check with `crontab -l` that it landed verbatim.
