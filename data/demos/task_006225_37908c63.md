Read `/home/user/sequences.txt` line by line. For binding score, slide a length-9 window across each sequence (from index 0 to len-9 inclusive; if the sequence is shorter than 9, score is 0), compare each window character-by-character against `GATCGATCG`, count matches, and keep the max across all window positions — don't just compare from a fixed offset. For GC-content, count 'G'/'C' occurrences over the full sequence length, compare `>= 50.0` for group A vs `< 50.0` for group B (get the boundary condition right). Write the CSV with `std::fixed << std::setprecision(2)` for the gc_percentage column, one row per input sequence, matching the header exactly. Accumulate the binding scores separately per group as you go so you can compute each group's mean afterward without a second pass. Compare `abs(meanA - meanB) > 1.0` strictly, and write `stats.txt` with exactly the three required lines, values rounded to 2 decimals and the boolean lowercase (`true`/`false`). Compile with `g++ -std=c++11` and run to generate both output files.
