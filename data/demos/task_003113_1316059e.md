Treat this as three independent subsystems that must all work before the final service is live: audio intel, build fix, data logic, then the API.

1. Transcribe `/app/voicemail.wav` first (e.g. with `whisper`, `speech_recognition`, or any installed ASR tool) — you need it to recover both the missing-library hint and the exact bearer token; don't guess these values.
2. Inspect `/app/vuln_dashboard/build.rs`: it's missing the directives that tell cargo where to find and link the static lib. Add `println!("cargo:rustc-link-search=native=...")` pointing at `/app/vuln_dashboard/lib` and `println!("cargo:rustc-link-lib=static=secutils")` (matching `libsecutils.a`'s base name), then confirm `cargo build` succeeds.
3. In `src/processor.rs`, parse `/app/data/raw_vulns.json`, group entries by `cve_id`, dedupe/merge `affected_endpoints` (union, no repeats), then sort by a severity rank map (CRITICAL > HIGH > MEDIUM > LOW) with `cve_id` as the tiebreaker.
4. In `src/main.rs`, wire up an HTTP server bound to `127.0.0.1:8080` with `GET /api/v1/reports`; check the `Authorization: Bearer <token>` header against the token you transcribed, returning 401 on mismatch/missing and 200 with the sorted/merged JSON otherwise. Launch it in the background and verify with `curl` before finishing.
