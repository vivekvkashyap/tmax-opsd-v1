Work through the four objectives in dependency order since later ones need earlier ones building successfully. First fix CMakeLists.txt: add `target_link_libraries(auth_test mock_waf)` plus a `link_directories`/`find_library` pointing at /home/user/opt/mock_libs, and set the RPATH via `set_target_properties(auth_test PROPERTIES BUILD_RPATH "/home/user/opt/mock_libs")` (or INSTALL_RPATH with BUILD_WITH_INSTALL_RPATH TRUE) so the binary finds libmock_waf.so at runtime without LD_LIBRARY_PATH. Build with `cmake .. && make` and confirm it links and runs before moving on. For the memory leak, trace the `malformed` branch in process_token() — it returns early without freeing the heap buffer; add the missing `delete[]` before that return. For generate_attack_payload(), treat it as a constraint-satisfaction problem: pick any 11 printable ASCII characters after "admin" whose sum equals exactly 1000 (e.g. compute one filler value and adjust one character to make the sum land exactly right), verify the total string length is 16. Finally write ci_run.sh to create a fresh build dir, cmake with the ASan flags given, make, run auth_test redirecting stdout/stderr to the exact log path, and propagate the real exit code; run it yourself to confirm "All tests passed!" appears with no ASan reports.