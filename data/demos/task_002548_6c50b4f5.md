This is a single, well-defined signal-processing check — implement it precisely rather than approximately. Read the WAV with `scipy.io.wavfile.read` or the `wave` module to get the sample rate and raw sample array; compute the run-length of samples that are exactly `0` (not "close to zero" — the spec is exact digital silence), by scanning the array and tracking the current streak length, resetting on any nonzero sample and updating a max-streak variable. Convert the 50ms threshold to a sample count via `int(0.05 * sample_rate)` and compare the max streak against it (streak >= that count means unsafe). For stereo files, decide up front whether "sample" means a full frame (all channels zero) or per-channel, and apply it consistently — check a couple of files in `/app/corpus/clean/` vs `/app/corpus/evil/` with a quick script to confirm your logic separates them before wiring up the exit-code contract (`sys.exit(1)` for unsafe, `sys.exit(0)`/normal exit for safe). Test your detector directly against both corpus directories in a loop before considering it done, since the hidden suite uses the exact same criteria.
