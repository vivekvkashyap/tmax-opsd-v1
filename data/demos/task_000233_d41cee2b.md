Build this as a clean multi-stage pipeline where each stage's output file is inspected before moving to the next, rather than trying to do it all in one script. Stage 1: transcribe /app/telemetry_data.wav with an ASR tool (e.g. `whisper`) and read the transcript carefully to extract each "Time X, Alpha Y, Beta Z" triplet, explicitly noting which readings are spoken as "missing" — don't assume every timestep has both readings present. Stage 2: reshape into long format (time,sensor,value) with exactly one row per (time, sensor) pair across all 7 timesteps (0,10,...,60) x 2 sensors (Alpha,Beta) = 14 rows, leaving missing values blank. Stage 3: perform genuine linear interpolation for each sensor's missing values using its two nearest known neighboring timesteps (value = v1 + (v2-v1)*(t-t1)/(t2-t1)) — do this programmatically (e.g. pandas `interpolate()` grouped by sensor, or by hand in Python) rather than guessing values, and double check no timestep/sensor combination remains missing since the verifier computes MSE against the full 14-row array. Stage 4: aggregate the mean per sensor from the imputed data into summary.json with the exact key structure specified.