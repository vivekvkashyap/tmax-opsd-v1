First locate `backup.arc` with `find /home/user/projects/archive_mount/ -name backup.arc` since it's nested under an unspecified subdirectory — don't assume the path. Parse the archive by reading it as text and splitting on the `===FILENAME:`, `===CHECKSUM:`, and `===END_FILE===` delimiter lines (e.g. with a regex over the whole file, or a small state machine reading line by line) rather than assuming fixed line offsets, since hex payloads can vary in length. For each entry: hex-decode the payload with `bytes.fromhex(...)`, compute `hashlib.md5(decoded).hexdigest()`, and compare case-insensitively (or lowercase both) against the archive's checksum line. Create `/home/user/projects/restored/` first with `os.makedirs(..., exist_ok=True)`. On a match, write the decoded bytes to `restored/<filename>` in binary mode (not text, to avoid encoding surprises); on a mismatch, append just the filename (one per line) to `restored/corrupted.log`, preserving the order entries appear in the archive, and do not write the corrupted file's content at all.