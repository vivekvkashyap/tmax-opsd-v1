First recover the hardcoded salt from the stripped binary without disassembling it in depth: run `strings /app/auth_service | less` and look for a plausible secret-looking string near auth/salt context; if `strings` alone is ambiguous, use `ltrace`/`strace ./auth_service` or feed it sample stdin (per its own protocol) and observe behavior to confirm which string is the salt used in the MD5 concatenation — verify your guess by reproducing one of the example checksums the task gives you (payload hex + candidate salt, MD5'd, compared to the given checksum) before hardcoding it into your service. Then build the Python TCP server: use `socketserver` or a raw `socket.accept()` loop, read a line per connection, and apply checks in the exact required order — reject on forbidden username characters first (before even checking hash), then verify `hashlib.md5((payload_hex + salt).encode()).hexdigest() == checksum`, then only for valid payloads decode `bytes.fromhex(payload_hex)` and check if `b"admin"` is a substring. Send responses with `\n` terminator and close the connection each time. Start the server backgrounded (e.g. `nohup python3 new_auth_service.py &`) and test each protocol branch manually with `nc 127.0.0.1 8080` before considering it done.
