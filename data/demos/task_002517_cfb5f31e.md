Read the C code in `c_src/filter.c` line by line against what `apply_filter(int* pixels, int width, int height)` should do, and fix each memory bug individually: the allocation `malloc(width * height)` is missing `* sizeof(int)`, so it allocates 4x too little for an int buffer — fix the size calculation; the loop bound `i <= width * height` reads/writes one element past the end of both the pixel and temp arrays — change to strict `<`; and the `temp_buffer` allocated for the intermediate doubled values is never freed before `apply_filter` returns — add a `free(temp_buffer)` call after copying into `new_pixels`. Validate the C-level fix in isolation first by compiling `c_src/sanity_check.c` with `gcc -fsanitize=address -g` and running it — AddressSanitizer will immediately flag any remaining heap-buffer-overflow or leak, so iterate until it exits cleanly with no ASan report. Only after that passes, write `/home/user/run_validation.sh` to: cd into the project, build+run the ASan sanity check (propagating its exit code), run `cargo test` for the Rust FFI, compile `c_src/bench.c` with `-O3` and run it, parse its printed milliseconds value with `grep -oE` or similar, and write `BENCHMARK_MS=<value>` to `/home/user/benchmark_result.txt`. Make the script `chmod +x` and have it propagate a nonzero exit if any stage fails.