Treat this as a multi-service debugging task: start by reading every config/script under `/home/user/app/` (start_services.sh, config files, gateway.py, worker script, aggregator.py, reshape_data.sh, run_mpi_sim.sh, simulator.py) to build a full picture of intended data/control flow before changing anything. Fix port wiring so each service's outbound calls target the correct inbound port of the next hop (gateway->worker->aggregator), matching the ports the spec requires. In `reshape_data.sh`, parse comma-separated `time,value,error` lines (e.g. with `awk -F,`), drop any row where the error field exceeds 0.5, and emit only `time value` space-separated for the remaining rows — check the filter direction/threshold carefully. In `run_mpi_sim.sh`, source/activate the venv correctly and invoke `mpirun -n 4 python simulator.py` with correct working directory and arguments. In `simulator.py`, change the hardcoded `dt = 1.0` to `dt = 0.01` so the Euler integration is stable instead of diverging. After each fix, restart the services via `start_services.sh` and test end-to-end incrementally (first check ports are listening, then check reshape output, then check the full POST->TCP-GET_RESULTS round trip) rather than making all changes blind and testing once at the end. Leave all services running in the background when done.