Recover the passphrase with `tesseract /app/passphrase.png stdout`, then strip trailing/leading whitespace before using it — OCR often adds stray newlines. Decrypt with `openssl enc -d -aes-256-cbc -pbkdf2 -salt -pass pass:<recovered> -in /home/user/backup.enc -out /home/user/backup.tar.gz` (the `-pbkdf2` flag must match how it was encrypted). Generate the SSH key with `ssh-keygen -t ed25519 -f /home/user/.ssh/id_ed25519 -N ""`, and write `/home/user/.ssh/config` with a `Host *` block containing `PasswordAuthentication no` and `IdentityFile /home/user/.ssh/id_ed25519`. Generate the TLS cert/key in one step with `openssl req -x509 -newkey rsa:2048 -nodes -keyout /home/user/server.key -out /home/user/server.crt -days 365 -subj "/CN=internal.corp"`. For `filter_logs.sh`, read the file line by line and drop any line matching (case-insensitively where noted) `<script`, `javascript:`, `../`, `..%2F`, `UNION SELECT`, `1=1`, `$(`, a backtick, or `wget ` — build this as a single `grep -viE` pattern (remembering to escape regex-special characters like `(` and backticks) and test it against both `/app/corpus/clean/` and `/app/corpus/evil/` to confirm zero false positives/negatives before relying on it.
