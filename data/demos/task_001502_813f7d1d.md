Treat this as three independent subsystems wired together. (1) Parsing: read `/home/user/raw_logs.csv` with the correct encoding (`encoding="utf-16-le"` in Python's csv module via a decoded text stream) so multi-line quoted `Message` fields aren't split — Python's `csv` module handles embedded newlines inside quoted fields correctly by default as long as you don't read line-by-line manually. Normalize each `Timestamp`: if it's purely digits, treat it as a Unix epoch (seconds) and convert with `datetime.utcfromtimestamp`; otherwise parse as `MM/DD/YYYY HH:MM:SS` and treat as already UTC; format both to `YYYY-MM-DDTHH:MM:SSZ`. (2) Deduplication: for every record, shell out to `/app/legacy_hasher "<normalized_ts>|<UserID>"` (treat it as an opaque black-box binary — do not try to reverse-engineer or reimplement its hash, just call it) and keep only the first record per returned hash, preserving file order. (3) API: build a stdlib `http.server`-based (or Flask) service on `127.0.0.1:8080` exposing `GET /api/logs?user=<id>`, returning `{"logs":[{"timestamp":...,"message":...}]}` sorted by timestamp ascending; launch it detached with `nohup python3 /home/user/server.py &` so it stays up for the verifier. Finally write `/home/user/pipeline.cron` with a line scheduling `/home/user/pipeline.py` every 15 minutes (`*/15 * * * * /home/user/pipeline.py`).