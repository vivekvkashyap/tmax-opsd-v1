Implement and validate each stage of the C++ pipeline separately before chaining. First write a CSV reader that parses the header to map feature column names (`f1`..`f20`) to indices, then for each feature compute group means and unbiased sample variances (divide by n-1) separately for `label==0` and `label==1` rows, and the Welch t-statistic `|mean1-mean0| / sqrt(var1/n1 + var0/n0)` — compute this with double precision accumulators to avoid catastrophic cancellation on large datasets, and sort features by descending |t| to pick the top 3, breaking ties consistently. Second, for bootstrap resampling, build two filtered index arrays preserving original CSV row order (label==0 indices, then label==1 indices), seed `std::mt19937 rng(42)`, and draw exactly 1000 indices with `std::uniform_int_distribution<size_t>(0, n0-1)` from the class-0 array first, then a *fresh* 1000 draws similarly from the class-1 array (using the same rng object continuing its stream, not reseeding) — the order of operations (class 0 fully before class 1, using indices not direct row values) matters for matching expected output exactly. Third, write `prepared_data.csv` with header `id,label,<f_top1>,<f_top2>,<f_top3>` and `metrics.txt` with the three feature:tstat lines formatted to exactly 4 decimals using `printf`/`std::fixed`+`setprecision(4)`. Build with a simple Makefile using only `<random>`, `<cmath>`, `<fstream>`, `<vector>` — no Boost/Eigen.
