Chain three independent operations in one Python script, testing each in isolation before combining. First, decrypt: read the base64 key from `/home/user/secret.key`, construct `cryptography.fernet.Fernet(key)`, and call `.decrypt()` on the bytes read from `/home/user/audit_logs.enc`, then `json.loads()` the resulting plaintext into a list of dicts. Second, get the issuer CN from `/home/user/server.crt` — either with the `cryptography` library (`x509.load_pem_x509_certificate`, then `cert.issuer.get_attributes_for_oid(NameOID.COMMON_NAME)`) or by shelling out to `openssl x509 -in server.crt -noout -issuer` and parsing the `CN=` substring out of the output; don't hardcode the value, extract it programmatically. Third, redaction: write a regex that matches 16 digits either as four groups of four separated by hyphens (`\d{4}-\d{4}-\d{4}-\d{4}`) or as one contiguous run of 16 digits (`\d{16}`), and `re.sub` each match in every log's `message` field with the literal string `[REDACTED]` — apply the hyphenated pattern first (or use alternation) so both formats are caught without double-matching. Add the `cert_issuer` key to every dict, then `json.dump(..., indent=2)` to `/home/user/clean_audit.json`. Print intermediate results while developing to confirm decryption and extraction actually worked before relying on them.
