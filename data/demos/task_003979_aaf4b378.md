First recover the key from the video: extract frames in the ~12-15s window of `/app/evidence.mp4` (e.g. `ffmpeg -i /app/evidence.mp4 -ss 12 -to 15 -vf fps=5 frame_%03d.png`) and run OCR (tesseract) or visually inspect the frames to read the flashed base64 text overlay, then base64-decode it yourself to get the plaintext symmetric key — don't hardcode a guessed value, actually derive it from the frames. Inspect a sample payload from `/app/corpus/clean/` and `/app/corpus/evil/` to determine the encoding/cipher scheme empirically (the task implies base64-then-XOR): decode base64, then XOR every byte against the repeating key bytes, and check the decrypted plaintext. In `/home/user/payload_filter.cpp`, implement exactly that decode+decrypt pipeline, then scan the resulting plaintext for the literal substring `EXEC_SHELL` or `../` — if found, `return 1` (malicious); otherwise `return 0` (clean). Compile to `/home/user/filter_bin`, and explicitly `chmod 755 /home/user/filter_bin` afterward since the exact permission mode is checked. Test against a few sample files from both corpora before considering it done.
