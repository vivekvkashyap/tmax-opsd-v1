OCR the image first and extract both the auth token and the primary-key field name — run `tesseract /app/auth_schema.png stdout` and read the actual text rather than assuming a field name like "id"; the deduplication key must be whatever field name the image specifies. Build the HTTP server (stdlib `http.server` or Flask, your choice) bound exactly to 127.0.0.1:8080 with two routes. For `POST /ingest`: check the `Authorization: Bearer <token>` header against the OCR'd token and return 401 on mismatch/missing; then parse the body as JSON Lines (split on newlines, `json.loads` each line); maintain an in-memory dict keyed by the extracted primary-key field, where for each incoming record you compare its `timestamp` against the currently stored record's timestamp for that key — replace only if strictly higher, and whenever a record is discarded (lower/equal timestamp, or an incoming record that loses to an existing one), append its exact raw JSON line to `/home/user/dropped.log`. For `GET /state`, return the current dict mapped from primary-key to `config_value`. Keep the process running in the background (e.g. `nohup ... &` or run as a daemon thread) so the verifier can hit it after your script "completes."
