Start by reverse-engineering the binary, not guessing at its interface: run `strings /app/query_engine` to find literal format strings (this reveals any required environment variables and the exact scanf/printf-style query template it expects), then confirm with `strace -f` on a sample invocation to see which env vars it reads and what it writes to stdout/stderr on success vs. failure. Build a small harness to test hypotheses (e.g. set the discovered env var to point at the CSV, pipe a candidate query line into the binary via stdin, observe output) before writing any server code. For the C TCP server: use socket/bind/listen/accept on 127.0.0.1:9090, read one line per connection, parse the JSON request with straightforward string scanning (no need for a JSON library for a fixed two-key schema), translate start_node/max_hops into the binary's exact query syntax, invoke it with fork+exec or popen capturing stdout, split the newline-delimited results into a JSON array, and write it back followed by '\n'. Keep the server looping to accept multiple connections, and launch it detached/backgrounded so it stays up for verification. Test end-to-end with a manual `nc`/`curl` style TCP client before considering it done.