Install `pandas` and `mlflow` in the environment, then launch `mlflow server --host 127.0.0.1 --port 5000` (or `mlflow ui`) as a background process and confirm it's actually listening before proceeding (curl the API). Write a script that reads `/home/user/raw_data.csv`, computes `df['temperature'].median()` over non-null values only, counts how many rows are NaN before filling (that count is your `missing_imputed` metric), fills NaN with that median, then counts and drops rows where `humidity < 0` or `humidity > 100` (that drop count is `rows_removed`) — preserve original column order and header when writing `cleaned_data.csv`. In the same script, set the tracking URI to `http://127.0.0.1:5000`, create/get experiment `climate_cleaning`, start a run, `mlflow.log_metric` for both counts, and capture `run.info.run_id`, writing it verbatim to `/home/user/run_id.txt`. Verify by querying the MLflow REST API for that run_id and confirming metrics round-trip before finishing, and leave the server running.
