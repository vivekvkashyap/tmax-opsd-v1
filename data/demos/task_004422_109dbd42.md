Fix each layer independently and re-test before integrating. In `processor.c`, the segfault comes from `extract_tokens` writing into `out_tokens[token_count]` with no bound against the caller-provided array size (`MAX_TOKENS`) — add a check like `if (token_count >= MAX_TOKENS) break;` right before the write/increment so extra tokens are safely ignored, then recompile as a shared library: `gcc -shared -fPIC -o /home/user/src/libprocessor.so processor.c`. In `server.py`, change the `ctypes.CDLL(...)` path from the hardcoded `/usr/lib/libprocessor.so` to the freshly built `/home/user/src/libprocessor.so` (use an absolute or `__file__`-relative path so it works regardless of cwd). Generate the gRPC/protobuf Python bindings from `service.proto` with `python -m grpc_tools.protoc` before implementing the servicer. Implement `GetTokenDiff` by calling the C `extract_tokens` for both `input_a` and `input_b` through `ctypes`, sorting each token list lexicographically, computing the symmetric difference (e.g. `set(a) ^ set(b)`), sorting the result, and returning it in the response's `tokens` field — remember the C parser silently truncates each input to `MAX_TOKENS` tokens, so the diff must be computed on the truncated lists, not the full input strings. Start the server, run `client.py`, and inspect `/home/user/diff_result.txt` to confirm correctness before finishing.