Build the emulator first as a small state machine: keep a dict mapping var name to one of UNSET/ALIVE/DEAD, read the .lt file line by line, strip comments (`#`) and blank lines, split each remaining line on whitespace into command and operand, and implement the three transition rules exactly as specified (ALLOC on ALIVE -> exit 1, FREE on DEAD/UNSET -> exit 1, USE on DEAD/UNSET -> exit 1); fall through to exit 0 after the loop. Test the emulator directly against the unpatched test_case.lt first to confirm it correctly reports a failure, and inspect where the failure occurs. Then, for phase 2/3, do not eyeball the diffs — for each patch_N.diff in the patches directory, copy the original test_case.lt to a fresh temp file, apply the patch with `patch <tmpfile> < patches/patch_N.diff` (or `patch -p0`), run the emulator against the result, and record the exit code. Only trust the emulator's exit code, not assumptions about what a patch "looks like it does" — some patches may look plausible but still introduce a new double-alloc/free/use violation. Once you find the single patch yielding exit 0, write its filename verbatim to /home/user/success.log.
