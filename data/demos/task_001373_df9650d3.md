Break the pipeline into clear stages in your C++ program rather than one monolithic pass. Parse the wide CSV, and for each row/language pair skip it entirely if that language's latency is `-1` (don't emit a zero-latency record) — this filtering must happen before bucketing so excluded queries don't skew averages. Compute the bucket key with integer division: `(timestamp_sec / 3600) * 3600`. Aggregate per (language, bucket) into running sums for latency and expansion ratio (`target_chars / (double)src_chars`, cast to double before dividing to avoid integer truncation), then compute means per group. For Z-scores, you need each language's mean and *population* standard deviation (divide by N, not N-1) computed across that language's own bucket-level average latencies, then apply `(avg_latency - mean) / std_dev` per bucket. Output both CSVs sorted by language then ascending bucket_ts, with all floats formatted via `std::fixed << std::setprecision(4)`, and filter `anomalies.csv` to rows with z-score strictly greater than 2.0. Compile exactly as specified and run it to produce both files, then sanity-check row counts and a couple of computed values against the raw input by hand.
