Rewrite `/home/user/generate_report.py` rather than patching the buggy query in place, since the bug is structural (an implicit cross join with no join condition, plus a hardcoded path). Use a recursive CTE anchored on `manager_id IS NULL` for the CEO, with the recursive step joining `employees e JOIN cte c ON e.manager_id = c.emp_id`, and build the `path` column by string-concatenating in SQL (e.g. `c.path || '->' || CAST(e.emp_id AS TEXT)`) or by computing it in Python after fetching parent-child pairs — either way each employee's path must reflect the actual chain from the CEO down through their real manager lineage, not a constant. Join to `permissions` with an explicit `ON e.dept_id = p.dept_id` (this is what eliminates the cross-join bug — every employee must match exactly one permissions row via their own department, not all rows). Fetch results, sort by `emp_id` ascending, and dump a JSON list of `{"emp_id", "name", "resource", "path"}` dicts (matching that exact key order/naming) to `/home/user/compliance_graph.json`. Run the script yourself and sanity-check that no employee appears more than once and that deep paths (e.g. an intern several levels down) show the full `->`-joined chain.