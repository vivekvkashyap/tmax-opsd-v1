Write a single Python script that loads `network.json` into an adjacency dict and runs Dijkstra's algorithm (e.g. `heapq`-based priority queue) from "N_0" to "N_3", tracking both cumulative distance and the predecessor path so you can reconstruct the shortest path list, not just its length. Separately, load `signals.csv` with `csv`/`pandas`, extract the "N_0" and "N_3" columns as ordered numeric sequences. For each, compute `numpy.fft.fft`, take the squared magnitude (`abs(fft)**2`) as the power spectrum, then normalize by dividing by its sum so it forms a probability distribution. Use `scipy.spatial.distance.jensenshannon(p, q, base=None)` — note scipy's function returns a *distance* (already the square root of the JS divergence in nats when base is natural log / no base arg for base e... verify the base parameter behavior in scipy's docs so you match "base e" precisely) between the two normalized spectra. Compare that value to 0.5 with a strict `>` to set `is_distorted`. Round `path_length` and `js_distance` to 4 decimal places with Python's `round()`, and write the exact JSON structure with keys `shortest_path`, `path_length`, `js_distance`, `is_distorted` to `/home/user/analysis_out.json`.