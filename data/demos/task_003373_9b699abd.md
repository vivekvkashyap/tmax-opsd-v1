Build datamash from source into a user-local prefix since root is unavailable: download the tarball, extract, then `./configure --prefix=/home/user/local && make && make install`, and confirm the resulting binary at `/home/user/local/bin/datamash` runs before writing the pipeline script — add that bin dir to PATH inside the script itself so it's self-contained. For the cleaning stage, process each raw file with awk/sed: split on `|`, discard any row that doesn't have exactly 4 non-empty fields, normalize dates by detecting the format (a `/`-containing date is `MM/DD/YYYY` and needs reordering to `YYYY-MM-DD`, zero-padding month/day if needed; a `-`-containing date already matching `YYYY-MM-DD` passes through), replace commas with dots in the Value field, then validate the Value is a well-formed signed float/integer (use a regex like `^-?[0-9]+(\.[0-9]+)?$`) and drop the row otherwise (rows like "invalid" must be discarded). Convert delimiters to tabs for the cleaned output. For aggregation, sort the cleaned data by Category, pipe through `datamash -t $'\t' -g 3 sum 4 mean 4`, reorder/reformat into `Category,Total,Average` CSV with 2-decimal rounding via awk's printf, and sort alphabetically by category.
