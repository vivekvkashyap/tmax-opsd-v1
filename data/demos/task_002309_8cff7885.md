Tackle this in dependency order, verifying each layer works standalone before chaining. (1) System config: `sudo timedatectl set-timezone Europe/Helsinki`; generate the locale with `sudo locale-gen fi_FI.UTF-8` then `sudo update-locale` or `localectl set-locale`. (2) Run `tesseract /app/secret_passcode.png stdout` (installing `tesseract-ocr` via apt if missing) to recover the passcode text — strip trailing whitespace/newlines, and sanity-check the OCR result looks like a plausible alphanumeric token before hardcoding it anywhere; store it in one place (e.g. a shell variable or a constant your C++ reads) so a re-OCR is easy if it's wrong. (3) Write a minimal raw-socket C++ HTTP/1.1 server (or use a lightweight header-only lib) that parses the request line and headers manually, checks the `Authorization: Bearer <token>` header against the OCR'd value, and returns 200/403 accordingly; test it directly with curl on port 9000 first. (4) Create an SSH tunnel with `ssh -N -L 8080:127.0.0.1:9000 -i <local-keypair> localhost -o StrictHostKeyChecking=no &` after generating a passwordless key and adding it to `authorized_keys`. (5) Generate a self-signed cert with `openssl req -x509 -newkey rsa:2048 -nodes ...`, write an nginx.conf that terminates TLS on 8443 and `proxy_pass` to 8080, and run it in a rootless podman/docker container with the certs and config bind-mounted. Test the full chain end-to-end with `curl -k` before finishing.
