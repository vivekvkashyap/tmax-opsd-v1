Build and verify this pipeline stage by stage instead of writing everything then testing at the end. First confirm Redis, Flask, and Nginx are actually running and reachable on their ports (`redis-cli ping`, `curl localhost:5000`, `curl localhost:8080`) before editing configs. Fix `/home/user/nginx.conf` by adding `location /api/ { proxy_pass http://localhost:5000/; }`, reload nginx, and confirm proxying with curl. In `api.py`, read `REDIS_HOST`/`REDIS_PORT` env vars with sane defaults and test the Redis connection before wiring in embeddings. For `/api/ingest`, fit (or incrementally update) a `TfidfVectorizer` on ingested texts and persist both vectors and the fitted vectorizer state in Redis — a vectorizer refit only at search time won't share vocabulary with stored vectors, so reuse the same fitted vectorizer for both ingest and search. For `/api/search`, transform the query with that same vectorizer, compute cosine similarity against stored vectors, and return the top 5 ids. Write and run `ingest_data.py` against `/home/user/data/training_data.csv`, then sanity-check a few queries' Recall@5 before declaring done.