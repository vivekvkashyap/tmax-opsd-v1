Build the directory skeleton first (`/home/user/sec_logger/rust_lib`, `/go_cli`), then `mv` the scattered files into place (renaming `analyzer.rs` to `src/lib.rs`) rather than copying, so there's a single source of truth. Run `cargo init --lib` in `rust_lib`, edit `Cargo.toml` to set `[lib] crate-type = ["staticlib"]` (or include `rlib` too) and add `proptest` under `[dev-dependencies]`. Since the exported function takes a raw C pointer, don't property-test `analyze_payload` directly — factor the actual string-parsing logic into a private, safe Rust function that takes `&str`, have the `extern "C"` function call it, and write your `proptest!` block against arbitrary `String` inputs calling that safe wrapper, asserting no panic. In `go_cli`, run `go mod init`, wire up the CGO `#cgo LDFLAGS` pointing at the built static lib, and implement a worker-pool with goroutines + channels reading JSONL lines and dispatching the `"payload"` field to the FFI call. Finally write `ci_build.sh` with `set -e`, running `cargo test`, `cargo build --release`, `go build`, then executing the scanner against the logs and redirecting stdout to `final_report.txt`; `chmod +x` it and run it end-to-end to confirm it exits 0.
