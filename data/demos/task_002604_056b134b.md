Do not attempt to parse this CSV line-by-line with plain `awk`/`grep` — embedded newlines inside quoted fields will break naive row splitting. Use a real CSV-aware parser (Python's `csv` module is simplest to invoke from inside your bash script, or `csvkit`/`mlr`) to read records correctly even when a field spans multiple physical lines, and detect "embedded newline in Notes" by checking whether the raw field value (as parsed) contains a `\n` character, then drop those rows and count them. For deduplication, iterate rows in file order and keep the last row per `ServerID` (e.g. build a dict keyed by ServerID, overwriting on each occurrence, then preserve encounter order of first appearance or whatever order the verifier expects — check the task's row-order implications carefully). Lowercase Hostname and uppercase OS with straightforward string methods. Filter to `Status == active` and emit the exact template block per record, being precise about the literal field names (`ServerID=`, `OperatingSystem=`, `Info=`) and the `---` separator. Write the log lines in the exact given order with real computed counts, not placeholders. Make the script executable with `chmod +x`.