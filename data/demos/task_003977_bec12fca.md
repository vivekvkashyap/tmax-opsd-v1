First recover the `user_id` by transcribing `/app/intercept.wav` (a speech-to-text tool) — the alert spells out digits as words; convert them to the numeric id yourself, don't guess. Then build the SQL in `/home/user/run_audit.sh` around a recursive CTE over `access_logs`: anchor on the row(s) leaving the system named `entry.corp.local` for the given `$1` user_id, recursively join on `dest_sys_id = source_sys_id`, tracking the path (concatenated list or JSON array) and a running total of `duration_seconds`, keeping only the shortest chain per destination so the CTE doesn't explode combinatorially. The critical step: run `EXPLAIN QUERY PLAN` first to spot full-table scans, then `CREATE INDEX` on `access_logs(user_id, source_sys_id)` and `access_logs(dest_sys_id)` (and `systems(hostname)`) — without these indexes the traversal over a large table blows past the 0.5s budget. Emit the final JSON array with `path_length`, `total_duration`, `system_chain` via `sqlite3 -json` or manual formatting to stdout.
