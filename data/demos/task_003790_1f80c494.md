Start by exploring the schema interactively before writing the script: use `sqlite3 /home/user/access_logs.db ".schema"` or Python's `sqlite3` with `SELECT name FROM sqlite_master WHERE type='table'` to find the actual table and column names — don't guess or hardcode a schema you haven't confirmed. Once you know the table (columns for username, ip, timestamp, status), write the target query as `SELECT * FROM <table> WHERE status = <failure_value> ORDER BY timestamp DESC LIMIT 10` using whatever literal value the status column actually uses for failures (inspect distinct values first). Create an index that covers this query's WHERE + ORDER BY together, e.g. `CREATE INDEX idx_status_ts ON <table>(status, timestamp)`, then run `EXPLAIN QUERY PLAN <query>` via the `sqlite3` CLI or cursor and write its exact returned row(s) to `query_plan.txt` — confirm the plan actually says it's using your new index (e.g. "SEARCH ... USING INDEX") rather than a full table scan, otherwise your column order or query shape doesn't match the index. For the JSON output, fetch column names from `cursor.description` so each row becomes a dict with real column names as keys, and dump the list of 10 dicts with `json.dump`.
