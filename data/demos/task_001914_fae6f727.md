Scaffold with `cargo new /home/user/bayes_prep` and add `csv` and `serde` (with `derive`) to `Cargo.toml`; use `serde`'s `Option<u64>` (or similar) for the `successes` field so an empty CSV cell deserializes to `None` rather than erroring or silently coercing to 0 — this is the Rust equivalent of the "pandas silently makes it a float" trap the task warns about, so be deliberate about the missing-value representation. Do a first pass over all rows to accumulate total successes and total failures (trials - successes) only from rows where `successes` is `Some`, add these to the prior alpha=2.0/beta=5.0 to get the posterior, and compute posterior_mean = alpha/(alpha+beta). Do a second pass to fill missing rows: multiply that row's `trials` by posterior_mean, round to the nearest integer (Rust's `.round()`), and write it back as the imputed `successes`. Preserve original row order and column format when writing `/home/user/cleaned_data.csv`. Format all three metrics to exactly 4 decimal places (e.g. `format!("{:.4}", value)`) for `/home/user/metrics.txt`, matching the exact three-line label format specified. Build and run with `cargo run` (or build a release binary and execute it) so the output files actually exist on disk — writing the code without executing it will fail verification.
