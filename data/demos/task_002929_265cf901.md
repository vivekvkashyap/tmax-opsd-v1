Grep traffic.log for the line whose request line contains `token=` in a successful GET, and pull the first whitespace-delimited field as the source IP for malicious_ip.txt. Extract the token's value from the URL query string, then decrypt it with the standard OpenSSL PBKDF2 invocation that mirrors how it was likely encrypted: `openssl enc -d -aes-256-cbc -pbkdf2 -base64 -pass file:/home/user/secret.key` (read the password from secret.key rather than assuming its contents) — get the base64 substring exactly right (no stray newlines/whitespace) or the decrypt will fail with a bad-decrypt error. The plaintext is an HTML `<script src="https://...">` tag; use a regex (e.g. `grep -oP` or `sed`) to pull just the hostname out of the URL, not the full URL, and write only that domain to malicious_domain.txt. Write the CSP line to csp_header.txt exactly as specified (single line, three directives). For generate_block_rule.sh, just echo `deny $1;` and `chmod +x` the script.
