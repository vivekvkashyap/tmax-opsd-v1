Scaffold with `cargo new /home/user/archiver` and add `serde`, `serde_json`, `csv`, and `walkdir` as dependencies. Use `walkdir::WalkDir` to recursively traverse `/home/user/logs`; for each `.jsonl` file open a `BufReader` and process line-by-line (do not read the whole file into a String) parsing each line with `serde_json` and keeping records where `level == "CRITICAL"`; for each `.csv` file use the `csv` crate's reader in streaming mode, checking each record's `status` column for exact match `"ERROR"`, and convert the row to a JSON object keyed by header names. Wrap each kept record as `{"source": <basename>, "data": <record>}` and buffer them into groups of exactly 10. The key correctness point is atomic output: for each chunk, write to a `.tmp` file (e.g. `archive_part_001.jsonl.tmp`), flush and close it, then `std::fs::rename` it to the final `archive_part_001.jsonl` — never write directly to the final filename, since a partial write could be observed by a downstream consumer. Increment the chunk counter across both file types combined, not per-file. After coding, `cargo build --release` and run the binary, then confirm `/home/user/archive/` has no leftover `.tmp` files.