Work the four sub-problems in order, verifying each before moving on. (1) For the C-extension build failure, run the compile command directly to see the real error — a missing library flag (e.g. `-lm`) or a header typo in `setup.py`/`fast_filter` sources — then fix the linker flags and any dependency version pin conflicts in `requirements.txt` and reinstall. (2) For the asyncio leak, read `main.py`'s cancellation path: when a task is cancelled, its `CancelledError` must be caught/awaited and the task removed from any tracking set (e.g. via `try/except asyncio.CancelledError` plus `finally` cleanup, or `asyncio.gather(..., return_exceptions=True)`), not just dropped. (3) For the pcap, open it with `scapy` or `tshark` and inspect the raw UDP payload bytes across the repeating malformed packets — a single-byte XOR key will appear as a consistent leading/marker byte; don't guess a key, read it off the packets. (4) Write a small Python script that XORs every byte of `voicemail.wav` with that recovered single-byte key (note WAV headers get corrupted too, so validate the repaired file plays/parses correctly), then transcribe it with whisper and save plain lowercase text to `/home/user/transcript.txt`.