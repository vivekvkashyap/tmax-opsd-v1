Treat `/app/log_hasher` as a black box to reverse-engineer through observation, not disassembly alone: feed it controlled inputs via stdin (empty line, single characters, repeated characters, known strings) and record the 8-hex-char outputs — this differential testing tells you far more than raw disassembly. Confirm the output width (8 hex chars = 32-bit state) and look for classic rolling-hash structure: an initial constant, per-byte XOR, a bit rotation, and an additive constant, each round. Use `objdump -d` or `gdb`/`strace` on the stripped binary only to confirm the exact rotate amount, XOR order, and additive constant you infer from the input/output pairs — cross-check against your black-box observations rather than trusting either source alone. Implement the candidate algorithm in Python using 32-bit masking (`& 0xFFFFFFFF`) after every shift/add so it doesn't silently grow beyond native word size. Write `generate_test_logs.py` to produce edge cases (empty, very long lines, tabs, unicode-adjacent bytes, boundary lengths), and `test_parser.py` to pipe each through both the binary and your Python script and assert exact string equality, iterating until every case matches before finalizing `/home/user/log_parser.py`.
