Treat this as a git archaeology + build-diagnosis task, not a rewrite. First `cd /home/user/stats_project && git log --oneline --all` and `git log --diff-filter=D --summary` (or `git log -- stats.h`) to find the commit where the header was deleted, then restore it with `git show <commit>^:stats.h > stats.h` (or `git checkout <commit>^ -- stats.h`) rather than recreating the file by hand. Next inspect the Makefile's link line: it's linking both `-ladvanced` and `-llegacy`, and both static libraries define the same symbol (`compute_mean`), causing a duplicate/conflicting symbol at link time — fix it by removing `-llegacy` from the link flags so only `libadvanced.a` is used, keeping `-lm` and library search path (`-L./lib`) intact. Then `make` to build `calc_variance`, run it against `dataset.txt`, and redirect stdout with `>` (not `2>&1` or `tee`, which could alter exact byte content) to `final_result.txt`. Don't touch calc_variance.c or stats.h contents — the fix is purely git recovery + Makefile edit.