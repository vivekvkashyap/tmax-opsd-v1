Treat this as OCR-extraction plus a straightforward numeric filter, and get the OCR step right first since everything downstream depends on it. Run `tesseract /app/reference_spec.png stdout` (try `--psm 6` or similar if default segmentation garbles the numbers) and inspect the raw output carefully — parse out the 5 comma-separated floats of the reference embedding and the similarity threshold from the "REJECTION CRITERIA" text; sanity-check the parsed numbers look plausible (5 values, threshold between 0 and 1) before hardcoding them into your C source, since OCR can misread digits/signs. In `detector.c`: parse the CSV skipping the header, compute cosine similarity per row as `dot(v,ref)/(||v||*||ref||)` using `float`, and as soon as any row's similarity is `>= threshold` immediately `exit(1)` (don't wait to scan the whole file). If the loop finishes without triggering, `exit(0)`. Keep stdout/stderr completely silent in the final build — remove any debug `printf`s — since the verifier only checks the exit code. Compile with `gcc -O2 ... -lm` and test manually against a couple of synthetic near-threshold rows to confirm the boundary condition (`>=` not `>`) is exactly right.
