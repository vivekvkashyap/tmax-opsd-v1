Work this as two independent extraction steps that you then join. First, pull the C2 domain out of the binary: run `strings /home/user/implant` and grep for anything ending in `.xyz` (`strings /home/user/implant | grep -oE '[a-zA-Z0-9.-]+\.xyz'`) rather than trying to disassemble it — hardcoded domains in simple binaries usually show up directly in the strings output; if not, fall back to `objdump -s`/`readelf` on relevant sections. Once you have the exact domain, use `jq` to filter `/home/user/csp_violations.jsonl` (newline-delimited JSON, so use `jq -c` or `jq -s` appropriately) for records whose `.["csp-report"]["blocked-uri"]` contains that domain string — use a substring/contains match since the field may include a full URL path, not just the bare domain. Extract the root-level `source_ip` field from each matching record with `jq -r '.source_ip'`, then pipe through `sort -u` to get unique, alphabetically sorted IPs (note: this is lexicographic string sort, not numeric IP sort, which matters for multi-octet ordering), and write the result to `/home/user/compromised_ips.txt` with one IP per line and no extra formatting.