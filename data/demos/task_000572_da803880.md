Write the C program to take argv[1] (CSV path) and argv[2] (integer threshold T), open and read the file with fopen/fgets, skip the header line, then parse each remaining line with sscanf on the comma-delimited id,val1,val2 fields (use atoi/atof, being careful with negative numbers like -999). For each row: replace val1 or val2 with 0 if it equals -999, compute val3 = val1*val1 + val2, then clamp val3 to T if val3 > T. Accumulate a running sum and count, then print the mean (sum/count) as a float with at least 2 decimals — nothing else to stdout. In tune.sh, compile with `gcc /home/user/preprocess.c -o /home/user/preprocess`, then loop over T in the given list in ascending order, capture each run's printed mean, and stop at (write) the first T whose mean is strictly greater than 50.0 — use a numeric comparison (e.g. via `awk` or `bc`, not string comparison) since shell arithmetic doesn't handle floats natively. Write only the integer T (no newline issues, no extra text) to /home/user/best_t.txt.