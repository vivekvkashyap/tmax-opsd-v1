Write the Rust program as a straightforward line-oriented state machine, not a full parser generator. Read the log file line by line, and for each line, check with a cheap prefix test (`starts_with("[SEQ]")`) before doing any further work — skip everything else immediately. For matching lines, split on whitespace or use a small regex/manual token search to pull out the text following `State:`, `Weight:`, and `Next:` labels; parse the weight with a signed integer parser since it can be negative. Maintain `current_state` (starting at `"START"`) and `score` (starting at 0) as mutable variables outside the loop. On each parsed transition, compare the parsed current-state token against your tracked `current_state` using exact string equality; if it matches, add weight to score and advance `current_state` to the parsed next-state; if it does NOT match, break out of the loop immediately (do not process remaining lines, even other valid `[SEQ]` lines further down). After the loop, serialize the final state and score into the exact required JSON key/value shape and write it to the output path, then compile with `rustc` and execute the binary so the file is actually produced.
