Do not try to patch or wrap `/app/legacy_calc` — treat it purely as a diagnostic aid, then implement the fix yourself in `/home/user/generate_report.py`. First inspect `T1.csv`, `T2.csv`, `T3.csv` with `head`/`pandas.read_csv` to understand the schema (entities, transactions with an amount+currency, and an exchange-rate table). Run the legacy binary and compare its output row counts/sums against a manual per-entity sum to confirm the cross-join symptom: if `T3` (rates) has multiple rows per currency (e.g. rates at different timestamps), naively joining transactions to all matching rate rows multiplies each transaction's contribution by the number of duplicate rate rows for its currency. Fix this in pandas by first deduplicating T3 down to one rate per currency (take the latest by timestamp, or whichever resolution the data implies — inspect the actual duplicate pattern rather than assuming) before merging T2 to T3 on currency, then merging to T1 and grouping by entity to sum `amount * rate`. Round to 2 decimals and write `entity_id,total_value` to `/home/user/correct_totals.csv`. Validate your own output makes sense by spot-checking one entity's totals by hand against the raw CSVs.
