Handle each phase independently and verify output content exactly matches required format (no extra whitespace/newlines beyond what's asked). Phase 1: patch `upload_server.cpp`'s `handle_upload` so it checks `filename.find("..") != std::string::npos || filename.find('/') != std::string::npos` and returns `"ERROR: Invalid filename"` in that case, otherwise keeps the original concatenation logic — write the whole file to `patched_server.cpp`. Phase 2: each line in `tokens.txt` is hex-encoded ciphertext; decode hex to raw bytes, XOR every byte with `0x5A`, and you get `username:role` — do this decoding programmatically in C++ (hex-pair to byte, then XOR) rather than by hand, then scan all decoded tokens for the one whose role is exactly `admin` and write just the username portion (split on `:`) to `attacker_username.txt`. Phase 3: same idea but the pipeline is Base64-decode then XOR with `0x1F` — you may use `base64 -d` at the shell to get raw bytes into a file/pipe, but perform the XOR step in your C++ tool, writing the resulting plaintext bytes directly to `decoded_payload.txt`. Phase 4: compute `md5sum` for every file in `bin/` and diff against `hashes.md5` line by line to find the one mismatch; write just that basename to `compromised_file.txt`.