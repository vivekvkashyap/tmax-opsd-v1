Build the pipeline as clearly separated stages so each can be tested independently before chaining them. Stage 1 (masking): use `sed -E 's/[0-9]{3}-[0-9]{2}-[0-9]{4}/***-**-****/g'` (or an equivalent regex) to replace SSN digit patterns while preserving hyphens — test against a line containing a real-looking SSN to confirm the replacement format matches exactly. Stage 2 (quality gate): parse out each line's `BYTES: <number>` value (e.g. with `grep -oP 'BYTES: \K[0-9]+'` per line) and maintain a rolling sum over every 3 consecutive lines (a sliding-window sum, not a cumulative total) — as soon as any window's sum strictly exceeds 1000, the whole script must exit 1 immediately without writing anything to the output file, so check this condition before committing any output. Stage 3: pipe lines that pass the gate through `/app/legacy_filter` (a black-box binary — treat it purely as a stdin→stdout filter, don't try to reimplement its logic) to strip blacklisted diagnostics. Chain masking → gate-check → legacy_filter → write to the output path, exit 0 on success. Validate against both `/home/user/corpora/clean/` and `/home/user/corpora/evil/`, checking exit codes and, for clean files, that output content matches exactly.
