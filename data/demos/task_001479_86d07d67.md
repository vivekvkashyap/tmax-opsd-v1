Structure the C++ extractor around three independent checks applied per manifest entry, in order: (1) reject any `path` containing `..` or starting with `/` before touching the filesystem at all — do this with plain substring/prefix checks, not a canonicalization function that could itself be tricked; (2) track a running byte total and skip a file (and stop processing entirely, per spec) once adding its content would exceed `max_total_bytes`; (3) only then write. For the write itself, always create parent directories under the base dir first, write full content to `<dest>.tmp`, close the stream, then `rename()` it to the final path — never write directly to the final path, since that's what makes the write atomic. Use OpenSSL's `SHA256`/EVP API to hash each successfully-written file's content and append `relative_path,hexdigest` to the CSV as you go, using lowercase hex. Parse the simple flat JSON manifest/config by hand or with a header-only library rather than assuming complex JSON features are needed. Compile with `-lcrypto` and test manually against a manifest containing at least one safe file, one `..` path, one absolute path, and one file that would overflow the quota.
