Fix the build chain in dependency order, verifying each layer before moving to the next. In `CMakeLists.txt`, the link directory is deliberately pointed at a nonexistent path — replace it with the real build output directory of the Rust library (`rust_lib/target/release/`), and make sure the target link library name matches the crate's actual `cdylib` output name (check `Cargo.toml`'s `[lib] name` field, since the linker flag `-l<name>` must match, not the Cargo package name). Implement `evaluate_rule` as a C ABI function (`#[no_mangle] pub extern "C" fn`) that safely converts the incoming `*const c_char` (checking for null first), parses the `"MAX:<number>"` format defensively (missing prefix, non-numeric suffix, or negative number should all fall into the -1 error path rather than panicking or unwrapping), then compares `request_count` against the parsed max. For the proptest, add it as a `[dev-dependencies]` entry, generate valid `(N, request_count)` pairs with `proptest`'s ranged strategies, format the rule string inline, call the function, and assert against the same comparison logic independently reimplemented in the test (not by calling the function to check itself). Build and test incrementally: `cargo build --release` then `cargo test` before touching CMake, so failures are isolated to one layer at a time.