Split this into two independent pieces and verify each in isolation before wiring them together. (1) For the auditor: write `/home/user/path_auditor.py` as a stdin-JSONL-loop script; for each line, first attempt `base64.b64decode(..., validate=True)` in a try/except to catch invalid input as `"error"`; always compute `canonical_path` via `os.path.abspath(os.path.join("/var/uploads", filename))` regardless of decode outcome; check traversal by verifying the canonical path starts with `/var/uploads/` (or equals it exactly) — reject otherwise as `"malicious"`; then scan the raw decoded bytes for the literal 4-byte sequence `\xDE\xAD\xBE\xEF` and mark `"malicious"` if found; only if all checks pass compute `hashlib.sha256(...).hexdigest()` and mark `"clean"`; set `sha256` to `null` for non-clean/error cases. Get key ordering, exact status strings, and edge cases (absolute filenames, `../` chains, missing keys) right since fuzzing checks byte-for-byte equivalence. Test manually by piping crafted JSONL lines through it. (2) For the proxy: edit `/app/nginx.conf` to add a `location /api/ { proxy_pass http://127.0.0.1:5000/; }` block on the 8080 server, then run `/app/start_services.sh` and confirm with `curl -X POST http://127.0.0.1:8080/api/upload ...` that you get a 200 end-to-end.