Structure the C program in clear stages: (1) read the 4x4 matrix from the file with simple `fscanf` loops; (2) implement Cholesky decomposition manually — for a 4x4 symmetric positive-definite matrix, compute L column by column using the standard formulas `L[i][j] = (A[i][j] - sum_{k<j} L[i][k]*L[j][k]) / L[j][j]` for i>j, and `L[j][j] = sqrt(A[j][j] - sum_{k<j} L[j][k]^2)` on the diagonal, filling upper entries with 0; (3) generate standard normal variates via Box-Muller (`sqrt(-2*log(u1))*cos(2*pi*u2)`, seeding the RNG once), then for each of the 500,000 samples draw a 4-vector of i.i.d. standard normals and left-multiply by L to get the correlated sample (`sample = L * z`, since `L L^T = Cov` gives `Cov(Lz) = L Cov(z) L^T = L L^T = Cov`). For the HDF5 output, use the C API (`H5Fcreate`, `H5Screate_simple` with dims `{500000,4}`, `H5Dcreate2`/`H5Dwrite`) to write one dataset literally named `samples`, and link with `-lhdf5 -lm`. After running, you can sanity-check your own output's empirical mean/covariance against the input matrix before considering the task done — the verifier recomputes them independently.
