Since `sys-profiler` is a stripped, undocumented binary, reverse-engineer its interface empirically before writing the supervisor: run `strings /app/bin/sys-profiler | less` to look for argparse-style flag names and any hardcoded trigger phrases/response templates, and try `/app/bin/sys-profiler --help` if it's argparse-based (it typically prints usage even when stripped). Confirm your findings by actually launching it with a candidate `--data-dir`/`--tcp-port` combination and connecting with `nc`/a raw socket, sending candidate trigger strings until you get the expected JSON-shaped response — never hardcode a guessed trigger phrase without confirming it works against the real binary. For the Python supervisor: create `/home/user/metrics/`, chmod it (and everything under it) to `0700`, make a new `run_<timestamp>` dir each start and symlink `current` to it, then spawn the profiler in a loop/thread that respawns it whenever `subprocess.poll()` shows it exited. Implement the Flask/HTTP layer for `POST /metric` (write JSON body to a new file under `current`) and `GET /plan` (open a fresh TCP socket to the profiler's port, send the discovered trigger, relay its raw response back as the HTTP body). Before starting a new run, tar+gzip and remove any older timestamped subdirectories under `/home/user/metrics/` into `/home/user/backups/`. Run everything in the background as your own user, not root.
