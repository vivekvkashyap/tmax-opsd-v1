Work stage by stage and verify each before moving on. Stage 1: open `/app/api.py`, find the `redis.Redis(...)` call, and change its `port=` argument from whatever wrong value it has to `6379` (the default Redis port) to match the actually-running redis-server; then launch the Flask app in the background (e.g. `nohup python3 /app/api.py &`) bound to port 5000, and confirm with `curl http://127.0.0.1:5000/data/train` before proceeding. Stage 2: pull both endpoints' JSON, drop training records with null `label`, drop records whose `text` has more than 50 space-separated words, then lowercase text and strip all non-alphabetic/non-space characters (e.g. regex `[^a-z ]` removed) before splitting on spaces into tokens — apply the identical cleaning function to test text too (test records are kept even without labels). Save the cleaned datasets to disk in a simple format (CSV/JSON) under `/home/user/`. Stage 3: implement Multinomial Naive Bayes in C from your saved training tokens — build vocab and per-class token counts from Class 0 and Class 1 training rows, apply Laplace (+1) smoothing only over tokens seen in training vocabulary (skip unseen test tokens entirely, don't smooth them in), sum log-priors and log-likelihoods per document to avoid underflow, then convert back with the standard log-sum-exp / softmax-style normalization to get `P(class=1|text)`. Write `/home/user/predictions.csv` with header `id,probability_class_1`, compile with `gcc`, and run it to produce the file. Since the grader checks MSE against a hidden ground truth, double check your smoothing and normalization formulas match a textbook Multinomial NB exactly rather than approximating.
