Build and unit-test the `TimeSeriesRingBuffer` in isolation first: a fixed-capacity structure (e.g. backed by a `list` or `collections.deque(maxlen=3)` if you want to lean on stdlib, or implement your own indexable circular array) that evicts the oldest element once it holds 3 items, and exposes enough to compute SMA/variance over whatever it currently contains (1, 2, or 3 elements). Process CSV rows in chronological order (they should already be sorted by timestamp, but don't assume — sort explicitly if unsure), pushing `cpu_util` into the buffer each iteration, then compute SMA as the mean of the buffer's current contents and manually compute population variance as `sum((x-mean)**2 for x in window)/len(window)` — explicitly do NOT reach for `statistics.pvariance`/`variance` even though they'd give the same answer, since the instructions call for a from-scratch implementation. Build each JSON record with the exact nested keys (`ts`, `metrics.cpu`, `metrics.mem`, `stats.cpu_sma_3`, `stats.cpu_var_3`), rounding every float to 4 decimal places with `round(x, 4)` right before serialization (not earlier, to avoid compounding rounding error). Write the array with `json.dump` to the exact output path, and only use the Python standard library (`csv`, `json`) — no pandas/numpy.