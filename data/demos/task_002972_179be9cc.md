Build and test each stage of the pipeline in isolation before chaining them, since a mismatch anywhere breaks the final numeric comparison. In `join_data.py`, use `pandas.merge` with `how='inner'` on `id` across all three frames (join A+B first, then that result with C, or use `functools.reduce`), then `sort_values('id')` before writing — an inner join naturally handles the differing row counts across the three files. In `pca_py.py`, drop the `id` column, use `sklearn.decomposition.PCA(n_components=2)` — scikit-learn's PCA always mean-centers internally by default and does not scale, which matches the "center=TRUE, scale=FALSE" requirement in R, so no extra preprocessing is needed; just call `fit_transform` and preserve the original row order (do not re-sort after this step). In `pca_r.R`, read the same CSV, drop `id`, call `prcomp(x, center=TRUE, scale.=FALSE)`, and take `$x[,1:2]` as PC1/PC2, writing with `write.csv(..., row.names=FALSE)`. The key subtlety for `test_accuracy.py`: PCA sign is arbitrary per solver, so take `abs()` of both score matrices element-wise before diffing, not just at the end — compare the full matrices, then report the max absolute difference against the `1e-4` threshold. Finally wire everything into `run_pipeline.sh`, using `pip install --user` (no sudo) for Python deps, and run it end-to-end to confirm `PASS`.