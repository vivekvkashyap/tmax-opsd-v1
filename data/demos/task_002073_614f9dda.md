Diagnose the build failure first: run `make` in `/home/user/uptime_monitor`, read the linker error carefully — a shared library calling `sqrt()` (or other libm functions) needs the math library explicitly linked on many toolchains, so check whether the Makefile's gcc invocation is missing `-lm` and add it. Then run the Python script to see it hang/crash, and inspect the loop: it's using `while current_sla != target_sla:` which is an exact floating-point equality check that essentially never becomes true after the value is computed through floating-point arithmetic (sqrt, divisions) — replace it with a tolerance-based comparison, `while abs(current_sla - target_sla) > 1e-9:`, per the required tolerance. Rebuild with `make`, rerun `python calculate_sla.py`, and confirm it exits 0 without hitting the iteration safeguard, and that `sla_report.txt` gets written. Verify by checking the exit code explicitly (`echo $?`) rather than assuming success just because there was no traceback.