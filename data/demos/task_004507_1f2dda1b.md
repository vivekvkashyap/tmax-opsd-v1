Don't trust file extensions — identify the true file type of everything in `/home/user/uploads` with `file *` (or check magic bytes / the `\x7fELF` header) to find the one that is actually an ELF executable despite its extension. Once identified, extract embedded strings with `strings <file> | grep AUTH_TOKEN=` to pull out the full token line, then take exactly the substring after the `AUTH_TOKEN=` prefix. Compute its SHA-256 with `printf '%s' "<token-value>" | sha256sum` — use `printf`/`echo -n` (no trailing newline) since a stray newline changes the hash. Write the three required lines to `/home/user/investigation_report.txt` in the exact format given: the filename with extension as found, the full token including the `AUTH_TOKEN=` prefix, and the hash of the value only. Double check there's no extra whitespace or blank lines in the output file.
