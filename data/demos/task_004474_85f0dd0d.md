Set up the venv and install scipy/numpy/pandas/pytesseract/pillow first, then run `tesseract /app/lab_notes_scan.png stdout` (or via `pytesseract.image_to_string`) to recover the four Lotka-Volterra parameter values from the scanned text — don't hardcode guessed numbers, parse them out of the OCR output (strip whitespace, handle possible OCR noise like confusable characters). Use `scipy.integrate.solve_ivp` (or `odeint`) with the standard Lotka-Volterra equations (`dprey/dt = alpha*prey - beta*prey*predator`, `dpredator/dt = delta*prey*predator - gamma*predator`) and the given initial conditions to build your dense theoretical baseline over t in [0,50]. In `trace_filter.py`, for each CSV interpolate your baseline (e.g. `scipy.interpolate.interp1d` or `np.interp`) onto the trace's own timestamps, then check both the non-negativity condition and the MSE<5.0 threshold for prey and predator separately before accepting; copy (don't move) files into `accepted/`/`rejected/`. Validate against the provided `/app/corpora/clean/` and `/app/corpora/evil/` directories yourself, iterating on your OCR parsing and MSE logic until you get 100% correct classification on both before trusting the pipeline on hidden data.