Read the input/output spec as a strict contract, not a rough guide — a fuzz-equivalence verifier will throw many malformed and edge-case strings at your binary, so implement exactly what's specified rather than a lenient best-effort parser. In `/app/socket_resolver/src/main.rs`, replace ad-hoc splitting (e.g. splitting on `//` alone) with explicit, ordered validation: check the literal prefix `upstream://`, then require a `:` separating host from port, then a `/` separating port from path, and validate that host/port/path are each non-empty and alphanumeric-only — any deviation (missing prefix, missing separator, empty component, non-alphanumeric char) must print exactly `INVALID` and nothing else, not a partial/malformed path. On success, print `/home/user/run/upstream/<host>_<port>_<path>.sock` with a normal `println!`. Before finalizing, stress-test your own binary by piping a wide variety of inputs at it (valid URIs, truncated ones, ones with wrong delimiters, empty string, non-ASCII) via a shell loop and manually eyeballing that output matches the spec, since subtle off-by-one slicing bugs are exactly what caused the original breakage. Build with `cargo build --release` so the binary lands at the required path, then handle the supervisord/logrotate config steps as straightforward file edits.