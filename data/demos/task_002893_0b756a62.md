Work the two phases in order and verify each before moving on.

Phase 1 (build 7za): Grep the vendored source tree at `/app/p7zip_16.02` for the compiler definition (check `makefile.machine`) rather than guessing where the typo is — search for `CXX=` and compare it to the standard `g++` invocation; a doubled or extra character is the kind of typo to look for. Fix only that line, then run `make` from the package root and confirm `bin/7za` was produced before using it. Extract the multi-part archive with `7za x /home/user/project_logs.7z.001 -o/home/user/logs/` (7z auto-detects the `.002` part) and confirm files land in `/home/user/logs/`.

Phase 2 (sanitizer script): Write `/home/user/filter_records.sh` using awk (or a similar line-scanning approach) rather than a naive grep/sed pipeline — it must track record boundaries statefully: begin buffering only at a line that is exactly `[RECORD_START]`, accumulate lines until a line that is exactly `[RECORD_END]`, and only then decide whether to emit the buffered block. Check the buffered content for the raw byte sequences of the ELF (`\x7fELF`) and PNG (`\x89PNG`) magic headers using binary-safe matching (e.g. `grep -c` with `-a`/hex patterns, not text-only regex that could mangle non-UTF8 bytes) — do not rely on `grep -q` over the whole file, since matches outside record boundaries or partial/unterminated records must be silently dropped, not emitted. Test against both well-formed and deliberately malformed inputs before considering it done.
