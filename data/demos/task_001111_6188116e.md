Scaffold with `cargo new /home/user/profiler` first so you get a normal Cargo.toml/src layout, then build the pipeline in stages you can check independently. Parsing stage: read `/home/user/raw_profiling.txt` line by line, split on `|` into RunID/Algorithm/TargetSequence/PrimerSequence/ExecutionTimeNs, and compute the match score by zipping the primer's characters against the target's characters at the same index (0-based) and counting exact matches — only compare up to the primer's length, not the target's. Write the reshaped rows (`run_id,algorithm,match_score,time_ns`) to `/home/user/reshaped_data.csv` and eyeball a few rows before moving on. Stats stage: separate `time_ns` values by algorithm, compute sample mean and sample variance with N-1 (Bessel's correction) for each group, then apply Welch's t-test formulas directly for the t-statistic and the Welch–Satterthwaite degrees-of-freedom approximation; get the two-tailed p-value from a Student's t CDF (the `statrs` crate's `StudentsT` distribution works well, or implement via the incomplete beta function) rather than approximating with a normal distribution, since p-values are checked to tight tolerance. Serialize the final numbers (rounded to a handful of decimals) plus `significant_at_05 = p_value < 0.05` into `/home/user/profiling_report.json` matching the given schema, then run `cargo build`/`cargo run` end-to-end to confirm both output files are produced cleanly.