Build the three pieces independently and test each against its own fixtures before wiring them together. For `validator.py`: read the file line by line, wrap `json.loads` in try/except to catch both malformed JSON and broken unicode escapes (a raw `\u002` followed by non-hex chars will raise `json.JSONDecodeError` — catch it, don't let it crash the script), then explicitly check the required keys/types (`user_id` int, `transcript` str, `duration_sec` float-or-null and >0 if present). On any failure anywhere in the file, print "REJECTED" and `sys.exit(1)`; only after scanning every line successfully print "ACCEPTED" and exit 0. Validate this against `/app/corpus/clean/` and `/app/corpus/evil/` directly with a loop before trusting it. For `process.py`, when `duration_sec` is null, shell out to `ffprobe -show_entries format=duration` (or open the wav with the `wave` module and divide frames by framerate) and round to 2 decimals. For `run_daily.sh`, chain validator then process with proper exit-code checking (`if`/`&&`), and register the cron line with `crontab -l | { cat; echo "15 3 * * * ..."; } | crontab -` rather than editing files by hand.
