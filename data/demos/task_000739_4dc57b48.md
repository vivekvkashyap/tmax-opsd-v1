Install `mongomock` and `networkx` with pip first. Read the JSONL file line by line into a list of dicts, then deduplicate by `paper_id`: group records by `paper_id`, and for each group keep only the one with the maximum `timestamp` (use a dict keyed on `paper_id`, overwriting whenever you see a higher timestamp — do not just take the last line in file order, since order isn't guaranteed to match timestamp order). After picking the latest record per `paper_id`, drop it entirely if its `is_deleted` field is true.

Insert the surviving valid records into `mongomock.MongoClient().db.citations`. Build an aggregation pipeline with `$group` on `{author, cited_author}` computing `$sum: 1` (or count of matching docs) as `weight`, run it via `.aggregate([...])`, and iterate the cursor results.

For each aggregated group, add a directed edge `author -> cited_author` with `weight=count` to a `networkx.DiGraph()`. Run `nx.pagerank(G, alpha=0.85, weight='weight')` with default settings otherwise, and dump the resulting dict straight to `/home/user/pagerank.json` via `json.dump`. Double check your dedup logic against the raw file manually before trusting the pipeline output.
