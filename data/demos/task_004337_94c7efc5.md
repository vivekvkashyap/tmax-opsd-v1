Sequence this as: recover the grammar, fix memory safety, migrate the language version, then wire up serving — each independently checkable. First, OCR the image (`tesseract /app/grammar.png -` or similar) and inspect the recovered text for garbling before trusting it; save it to `/home/user/extracted_grammar.txt`. Second, open `c_src/fast_eval.c` and look for fixed-size buffers filled via unchecked `strcpy`/`sprintf` (use bounded copies or dynamic allocation) and AST nodes freed once but referenced/freed again elsewhere (free each node exactly once, e.g. a single post-order teardown) — recompile and stress-test with long, deeply-nested expressions to confirm no segfault. Third, port `parser.py`/`server.py` to Python 3 (print statements, integer division, dict methods, `except ... as e`) and smoke-test the server standalone on port 8080. Fourth, add an Nginx location block proxying `/api/eval` to `127.0.0.1:8080/eval`, reload, and verify with curl. Finally write `run_eval.sh` as a thin wrapper around your fixed engine printing only the numeric result, and test it against hand-picked expressions (nested parens, division) before trusting it.
