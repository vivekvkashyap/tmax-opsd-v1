Install OpenBLAS and headers via the system package manager (e.g. `apt-get install -y libopenblas-dev`) before writing code, and confirm `cblas.h` is discoverable. Write a loader helper that reads a whitespace-separated matrix of known dimensions from a text file into a flat row-major `double*` buffer (100x10 for X, 10x3 for P, 100x3 for Y_ref) using `fscanf` in a loop over rows*cols. Call `cblas_dgemm(CblasRowMajor, CblasNoTrans, CblasNoTrans, M, N, K, 1.0, X, K, P, N, 0.0, Y, N)` with M=100, K=10, N=3, and correct leading dimensions matching row-major layout (lda=K for X, ldb=N for P, ldc=N for Y) — getting the leading-dimension arguments wrong for row-major (versus BLAS's native column-major convention) is the classic mistake here. After computing Y, iterate elementwise over Y and Y_ref taking the max absolute difference, and write it to `/home/user/max_diff.txt` formatted with exactly 6 decimal places (`printf("%.6f\n", max_diff)`). Compile with `gcc /home/user/validate_pca.c -o /home/user/validate_pca -lopenblas -lm` (link flags matter), run it, and sanity-check the output looks like a small positive number consistent with the injected noise scale before finishing.