Build and test each pipeline stage independently before chaining them. First inspect the schema with `sqlite3 /home/user/system.db ".schema"` and sample rows to understand the `services`/`dependencies` shape. Write the 2-hop query as a self-join: join `dependencies` d1 (A->B) to `dependencies` d2 (B->C) on `d1.depends_on_id = d2.service_id`, join `services` twice to resolve names for A and C, then exclude pairs where a direct A->C edge already exists in `dependencies` (use `NOT EXISTS` against a third reference to the dependencies table matching A's id and C's id). Add `DISTINCT`, and `ORDER BY source, target` for determinism — do the sorting/dedup in SQL rather than relying on the Go program, since that's simpler to reason about. For the Go program, read stdin line-by-line with `bufio.Scanner`, split each line on tabs (sqlite3's default CLI separator when using `.mode tabs` or `-separator $'\t'`), build structs with `source`/`target` fields, and marshal with `encoding/json`. In `run_etl.sh`, explicitly set the sqlite3 output mode/separator to tabs so the Go parser's assumptions hold, and redirect final stdout to `/home/user/graph_projection.json`. Test the full chain end-to-end with `bash run_etl.sh` and inspect the JSON manually against your own trace of the dependency graph.
