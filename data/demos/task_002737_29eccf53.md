Work in two clean phases. First, bootstrap FFTW as an ordinary user-space build: `wget` the tarball, `tar xzf`, then `./configure --prefix=/home/user/fftw --enable-shared` (add `--enable-openmp`/`--enable-threads` only if you actually use FFTW's threaded API — plain fftw_execute in an OpenMP-parallel outer loop needs neither), `make && make install`. Verify the headers/libs actually landed under that prefix before writing any C.

For the simulation, write straightforward nested loops exactly matching the recurrence given — do not try to be clever with array indexing, since off-by-one errors in the `t` or `x` loop bounds will silently corrupt the FFT input. Parallelize the outer `s` loop with `#pragma omp parallel for reduction(+:total_kl)` (or accumulate into a per-thread array and sum after) — each simulation is independent so this is embarrassingly parallel; do not share the FFTW plan/buffers across threads without giving each thread its own arrays, or you'll get races. Use `fftw_plan_dft_1d` with `FFTW_FORWARD` and a complex array whose imaginary parts you memset to zero.

Compute the power spectrum, add the epsilon, normalize to a distribution, and sum `P*log(P/Q)` in double precision. Average across all 50 sims, print with `printf("%.6f\n", ...)` to the result file. Compile with `-I/home/user/fftw/include -L/home/user/fftw/lib -Wl,-rpath,/home/user/fftw/lib -lfftw3 -lm -fopenmp` — the rpath flag matters since the shared lib won't be on the default loader path at runtime.