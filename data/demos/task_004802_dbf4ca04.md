First isolate the vendored package bug in isolation before touching the rest of the pipeline: inspect `/app/text-cleaner-pro/text_cleaner_pro/utils.py` (and `core.py`) for anything that would make `get_embedding` return empty/crash — a common pattern is a dimension constant set to 0 or a function that returns an empty list/array — fix that constant/logic, then `pip install -e /app/text-cleaner-pro` and confirm interactively that `get_embedding("test")` returns a full-length numeric vector before writing any downstream code. Only then build the data-cleaning + Ridge-regression training script, computing train MSE on the embeddings you produced and writing `{"train_mse": <float>}` to `/home/user/experiment_results.json`. For the server, implement `POST /predict` on `127.0.0.1:8080` that first checks the `X-API-Key` header against the required value and returns 401/403 immediately if missing or wrong (test this negative case explicitly), then on success returns `cleaned_text` from your `clean_text` function and a `prediction` from the trained model applied to the request text's embedding. Run the server as a background process (e.g. `&` or nohup) so it stays listening, then curl it yourself with and without the header to confirm both paths work before finishing.
