First extract the schema from `/app/schema.png` with `tesseract /app/schema.png stdout` to get the exact table name and column names — use these literally when building your `CREATE TABLE` statement so your SQLite schema matches what the image specifies (don't guess your own column names). In `/home/user/detector.py`, load the CSV rows via `sqlite3` (use `csv.DictReader` plus parameterized `INSERT` statements, never string-format values into SQL). Then write a recursive CTE (or a bounded self-join, since the loop length is fixed at exactly 3 hops) that finds triples of distinct accounts A->B->C->A: join the events table to itself three times where each hop's target equals the next hop's source and the third hop returns to the first hop's source, and check the three account IDs (source_node values of the three hops) are pairwise distinct. For each candidate triple, verify: `max(epoch_time) - min(epoch_time) <= 60` and `sum(transfer_value) > 5000`. If any qualifying triple exists, print `SUSPICIOUS`, else `OK`. Watch the two clean-corpus edge cases carefully — a loop that exceeds the 60s window and a loop with too-low total value should NOT trigger a false positive — so make sure your SQL conditions are strict inequalities in the right direction and that you're not accidentally matching loops with repeated node visits or fewer than 3 distinct accounts.