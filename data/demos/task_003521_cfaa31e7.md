Start by reproducing the failure: `docker compose logs gateway` or a direct `curl` from inside the gateway container to `processor:5000` will show it can't resolve/reach the host because the two services sit on disjoint Docker networks. Fix `docker-compose.yml` by adding `back-tier` to the `gateway` service's `networks:` list (keep `front-tier` too, since it's the host-exposed port), then apply with `docker compose up -d` and confirm connectivity before touching the client script — don't skip this verification step, since a broken proxy will make every later test of `client.sh` fail in a way that looks like a scripting bug. For `client.sh`, build the JSON body by substituting `$1` into `{"data": "..."}` (watch for characters needing escaping if you want robustness, though the grading inputs are alphanumeric), `curl -s -X POST` with the `Content-Type: application/json` header, then extract just the `result` value from the JSON response using a targeted `grep -o` / `sed` regex (e.g. matching between `"result":"` and the next `"`) rather than a full JSON parser, and print only that value with no extra output.
