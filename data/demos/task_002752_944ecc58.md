Break this into three independent, verifiable pieces and test each before moving on. (1) Deployment layout: create both release directories, then `ln -s /home/user/deployment/releases/v2.0.0 /home/user/deployment/current` — verify with `readlink` that it resolves exactly to the v2.0.0 path, not a relative/dangling link. (2) filter.py: first run `ffprobe -v error -count_frames -select_streams v:0 -show_entries stream=nb_read_frames -of csv=p=0 /app/test_video.mp4` (or count via `-show_entries stream=nb_frames`) to get the real total frame count programmatically — do not hardcode a guessed number, since grading recomputes it from the actual video. Then read the JSON arg file, and reject (non-zero exit) if `frame > total_frames` OR the callback string contains any character outside `[A-Za-z0-9:/.?=_-]` — implement the character check with a whitelist regex like `re.fullmatch(r'[A-Za-z0-9:/.?=_-]*', callback)`, not a blacklist, since blacklists miss unlisted metacharacters. Exit 0 only when both checks pass. (3) rotate.sh: rename `access.log` to `access.log.$(date +%s)`, touch a fresh empty `access.log`, then list `access.log.*` sorted by mtime/name and delete all but the newest 3 so the count never exceeds 3 even after repeated runs. Test filter.py against a few crafted clean/evil payloads yourself before considering it done.