Work through the archive nesting without extracting everything to disk at once: open `/home/user/sensor_data/master_archive.tar` with Python's `tarfile`, iterate members, and for each `.zip` member use `tarfile.extractfile()` to get a file-like object, wrap it (e.g. `io.BytesIO(member.read())` if the zip module needs seekability, or extract just that zip to a temp dir) and open it with `zipfile.ZipFile`; then for each `.log` entry inside, use `zf.open(name)` to stream lines rather than reading the whole file into memory, processing line-by-line. For each line, split on `|`, strip whitespace from fields, and use a regex like `re.sub(r'\[ERR_NOISE_\d+\]', '', value_field)` to strip the noise tag before `float()`-parsing the value — the noise tag can appear appended directly after the number with no separator, so strip before parsing, not after. Only accumulate sum/count for lines where STATUS (after stripping) equals `OK` exactly, skipping WARN/ERROR. Aggregate into a dict keyed by SENSOR_ID, round each sum to 2 decimals with `round(x, 2)`, and write to `/home/user/results/summary.json` (creating the directory first with `os.makedirs(..., exist_ok=True)`). Actually run the script so the file exists.
