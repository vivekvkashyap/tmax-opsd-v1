Build the pipeline as a chain of `awk`/`grep` stages you can test independently on the raw log. First filter lines to `DEVICE_A` only. Then extract the minute-truncated timestamp (drop the seconds field from `[YYYY-MM-DD HH:MM:SS]`) and the `TEMP=` value; since multiple readings can land in the same minute, process the filtered stream in file order and let a later reading for the same minute overwrite an earlier one in an associative array keyed by minute (awk's natural "last write wins" behavior on a map handles this cleanly). After building the minute->temp map from actual data, generate the full minute sequence from `10:00` to `10:10` inclusive (11 minutes) and for each one either emit the recorded temperature or, if absent, carry forward the most recent previous minute's value (forward-fill) — track a "last seen" variable as you iterate the minutes in order rather than trying to look this up after the fact. Write the result as `YYYY-MM-DD HH:MM,TEMP` lines to `/home/user/clean_temperature.csv`, one per minute, and diff your output against the expected structure by eye to confirm both the last-reading-wins and forward-fill logic behaved correctly at the minutes that have no direct reading.