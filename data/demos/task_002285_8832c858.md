Start the stack with `start_services.sh`, confirm liveness via `curl http://127.0.0.1:5000/health`, then read `worker.py` end to end rather than guessing at the bug — look specifically at how it builds NumPy arrays from the timestamp fields and what dtype it uses. Millisecond epoch timestamps are on the order of 1.7e12, which overflows a signed 32-bit integer (`np.int32` max is ~2.1e9); if the array is explicitly created with `dtype=np.int32`, that's the root cause of negative/garbage latencies — fix it by widening to `np.int64` (or dropping the explicit dtype so NumPy infers int64). Verify the fix by re-running the services, pushing a few known start/end timestamp pairs to the API (or via your regression test script) and checking `/home/user/log_pipeline/output/metrics.json` shows the correct positive latency for each entry, not just that it runs without crashing. Build `regression_test.py` to actually assert on the computed values, not just that the file exists.
