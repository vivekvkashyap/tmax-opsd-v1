Start services and confirm connectivity before writing any Go code: run `/app/start_services.sh`, then set the DB credentials in `/app/backup-service/config.env` and manually connect with `psql` to confirm you can reach `backup_db`. Discover the actual foreign-key graph programmatically rather than assuming it — query `information_schema.table_constraints` joined with `key_column_usage`/`constraint_column_usage` (or `pg_constraint`) for `constraint_type = 'FOREIGN KEY'` to list every (referencing_table, referenced_table) pair, and build an undirected adjacency map from these pairs. In `validator.go`, parse the requested table list from the JSON file, restrict the graph to edges where both endpoints are in the requested set, then run a graph traversal (BFS/DFS or union-find) starting from one requested table and check that every other requested table is reached — that's the "single connected component" test. Print exactly `ACCEPT`/exit 0 or `REJECT`/exit 1 per the outcome. Before finalizing, manually run your tool against every file in `/app/corpus/clean/` and `/app/corpus/evil/` with `go run validator.go <file>` and confirm the accept/reject verdicts match expectations, paying attention to cases like `products` connected to `orders` only via the intermediate `order_items` table.
