First fix the vendored package: run `python -c "import patch_processor"` (after `pip install -e /app/vendored/patch-processor/`) to reproduce the ImportError, read the traceback to see which two modules import each other at top level (likely parser.py and models.py), and break the cycle by moving one side's import inside the function that needs it (a local import) rather than restructuring the whole package — the goal is minimal, correct changes that preserve the public `parse()` behavior. Confirm the fix in isolation first: `python -c "import patch_processor; print(patch_processor.parse('--- a/file\n+++ b/file\n'))"` should return a list of filepaths before you build anything on top of it. Then write the Flask/FastAPI app: read the raw diff text from the request body, call `patch_processor.parse()` to get modified paths, and reject (403) any path containing `../` or `..\\` or that starts with/contains `ios/Secrets/`, `android/keystores/`, or `.github/workflows/` — check substring containment, not just prefix, since evil paths can bury the sensitive directory mid-path via traversal. Launch the server backgrounded (e.g. `python3 api.py &`) and verify with `curl` against a clean and an evil sample before finishing, since it will be corpus-tested on port 8000.
