Treat this as a pipeline: clean -> impute -> load -> serve, and verify each stage before moving to the next. For text normalization, apply lowercase, strip non-alphanumeric-non-space chars (e.g. `re.sub(r'[^a-z0-9 ]', '', s)`), then `.split()`. For imputation, do NOT try to reimplement the interpolation yourself — call the provided `/app/imputer_oracle` binary per record via subprocess, passing the readings as a single space-separated argument with `NaN` for null entries, and parse its stdout back into a list of floats; run it on a couple of sample rows first to confirm the exact input/output format (delimiter, NaN casing) before wiring it into the full pipeline. Bulk-insert results into `/app/cleaned_sensors.db`'s `sensor_data` table, JSON-serializing the tokens and readings lists into the TEXT columns. Build the Flask/FastAPI service to check the `Authorization: Bearer secret_impute_token_99` header on every route (return 401/403 otherwise), implement `GET /api/record/{id}` and `POST /api/export` (writing `/app/export.csv` via SQL/pandas), then launch it with `nohup ... &` bound to `127.0.0.1:8080`, and only write "READY" to `/app/service_ready.log` after confirming the port is actually accepting connections (e.g. poll with `curl` or `socket.connect_ex` in a startup check).