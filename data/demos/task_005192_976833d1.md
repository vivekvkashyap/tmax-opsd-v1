First recover ESCALATION_SECRET yourself: extract frames from /app/audit_visual_log.mp4 with ffmpeg and, for each frame, sample the pixel at (0,0) to test whether it is exactly RGB(255,0,0); count how many frames satisfy that strict equality — that count is the secret integer, do not assume a value. Then scaffold a Rust binary with `cargo new /home/user/audit_verifier`, add the `md5` crate to Cargo.toml, and implement main.rs to: read exactly one CLI arg (print INVALID and return cleanly if argc != 2, never panic); split on ';' and require exactly 3 parts with the literal prefixes `USER:`, `ESCALATION:`, `TOKEN:`; validate username is non-empty alphanumeric and level is non-empty all-digits, treating any violation as INVALID rather than crashing (use safe indexing/checked parsing, not slicing that can panic on short strings); compute MD5 of `format!("{}{}{}", username, level, secret)` and compare hex digest to the token field, printing VALID or INVALID with a trailing newline and nothing else. Build with `cargo build --release`. Since grading is fuzz-equivalence against a hidden oracle, focus on matching edge-case behavior exactly (malformed input, non-hex tokens, wrong-length tokens) rather than just the happy path — test your binary against garbage/random strings yourself before finishing.