Work in two phases: fix the build, then script the pipeline. First run `make -C /home/user/project` and read the linker error — it complains about undefined math symbols (e.g. `sqrt`). The fix is to append `-lm` to the gcc link command in the `Makefile` (the recipe that produces `main` from `main.o`); `-lm` must come after the objects, not before. Verify `make` now succeeds. Then write `/home/user/pipeline.sh` starting with `#!/bin/bash` and execute each step in the exact order the task lists, using the literal paths given. Key points: capture the assembly line count with `wc -l < main.s` (use `<` so only the number is written, no filename); pipe `c_output.txt` into the Go `analyzer` via stdin then pipe that into `sort` before redirecting to `final_output.txt`; run `diff final_output.txt expected.txt` and branch on its exit status (`if diff ...; then echo PASS; else echo FAIL; fi`) into `status.log`. Build the Go binary with `go build -o` before invoking it. Finish with `chmod +x /home/user/pipeline.sh` and run it once to confirm PASS.
