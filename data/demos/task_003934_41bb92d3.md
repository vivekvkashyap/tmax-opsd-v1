Don't scan the logs by eye — write a small script that merges all three files' entries for TxID `SIM-9942` into one chronologically-sorted list (parse the ISO-8601 timestamp bracket, sort ascending), then walk the list computing the sum of the State array at each step. Establish the "expected" energy as the sum from the very first entry in that sorted timeline, and compare every subsequent entry's sum against it. The first entry whose sum differs from that baseline is the anomaly — record its timestamp, the service name (parse it from which log file the line came from, e.g. alpha/beta/gamma), the expected (baseline) energy, and the actual (divergent) energy. A common mistake is comparing each entry to the *previous* entry's energy instead of the original baseline, or not correctly interleaving all three logs by timestamp (the transaction bounces between alpha/beta/gamma non-sequentially in file order, so per-file scanning alone will miss the true chronological divergence point). Also make sure you filter strictly on `[SIM-9942]` and ignore the other decoy transaction IDs present in the same log files. Write exactly one line, `TIMESTAMP,SERVICE_NAME,EXPECTED_ENERGY,ACTUAL_ENERGY`, to `/home/user/bug_report.txt`.
