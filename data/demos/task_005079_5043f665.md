Build this as a single awk-driven filter piped into sort/uniq, not a hand-rolled loop. First use `awk '$1=="ATOM" && $3=="CA"'` to select the right lines (fields are whitespace-delimited, so awk's default field splitting handles it directly — no need to slice fixed PDB columns). Extract field 8 (the Z coordinate) and truncate toward zero by splitting on the decimal point (e.g. `split($8,a,"."); z=a[1]`), taking care of the classic edge case: values like `-0.500` truncate to the string `-0`, which must be normalized to `0` (compare with `awk` numeric coercion `z+0` or an explicit check) so it doesn't create a spurious duplicate bin next to `0`. Feed the truncated bins through `sort -n | uniq -c`, then reorder/reformat with a final `awk '{print $2, $1}'` to get `Z_BIN COUNT` in that column order. Finally pipe the whole thing through `sort -n` on the first column (numeric, not lexical, so negatives sort correctly) before writing to `/home/user/z_density.txt`, since the reproducibility requirement is specifically about strict numeric ascending order of Z_BIN.
