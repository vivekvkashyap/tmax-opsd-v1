Implement this as three verified stages, checking numerics at each step rather than trusting the pipeline blind. In the C solver: build the tridiagonal system from the standard central-difference stencil `(u_{i-1} - 2u_i + u_{i+1})/h^2 = f(x_i)` with `f(x_i) = -pi^2*sin(pi*x_i)`, apply Dirichlet BCs by folding known boundary values (0 here) into the RHS of the first/last equations, then solve with the Thomas algorithm (forward elimination of sub-diagonal, back-substitution) — a common bug is off-by-one in grid indexing (`x_i = i*h` for `i=1..N`, N interior points, h=1/(N+1)). Compute the discrete L2 error exactly as specified (sqrt(h * sum of squared differences)), and print with `%.6f` formatting for h and error, matching the exact `N=<N>, h=<h>, error=<error>` string format — string-format mismatches are a frequent verification failure. Write the `.dat` file excluding boundary points. In the bash pipeline, compile with `-lm` (needed for `sin`), loop over the exact N values in order, and append (not overwrite) stdout to the log. In Python, use matplotlib on a log-log scale for the convergence plot and a normal plot overlaying numeric vs exact solution for N=159, saving both PNGs.