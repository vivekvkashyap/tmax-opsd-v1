Get the vendored library importable before writing any classifier logic. Open `/app/graph-backup-lib/graph_backup_lib/__init__.py` and look for two separate defects: a syntax error (likely an unclosed parenthesis somewhere) and a spurious environment-variable guard that raises an exception under certain conditions (e.g. checking `os.environ.get("ENV")`) — remove or neutralize that check since it isn't a real requirement of the task, then fix the syntax so `python -c "import graph_backup_lib"` succeeds cleanly. Only then write `/home/user/manifest_filter.py`. Build the graph classifier as a small pipeline: parse each JSON manifest's nodes/edges, compute in-degree per node to confirm exactly one root (in-degree 0), run a cycle check (e.g. Kahn's topological sort — if not all nodes get consumed, there's a cycle) to confirm it's a DAG, and validate every node's `timestamp` against `^\d{10}$` and presence of a `checksum` field. Reject on the first violation found; only accept if all checks pass. Test your script separately against `/home/user/data/clean_manifests/` and `/home/user/data/evil_manifests/`, inspecting mismatches file-by-file rather than assuming correctness, since silently misclassifying even one file fails the whole task.
