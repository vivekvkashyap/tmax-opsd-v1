Since your Go binary will be fuzz-tested against a hidden reference implementation for exact stdout/exit-code equivalence across thousands of adversarial inputs, prioritize deriving the exact policy spec correctly over guessing at "reasonable" validation logic. Extract frames from the video with `ffmpeg -i /app/auth_policy_audit.mp4 -vf "select='between(n,45,55)'" -vsync 0 frame_%d.png` (or similar frame-range extraction), then read the hex-encoded configuration text visible in those frames — decode the hex to get the actual policy values (trusted CA fingerprint, allowed sandbox directory, max token age) rather than inventing plausible-looking ones. Design your Go program's token parser, signature/cert-chain validation, and path-traversal check (e.g. using `filepath.Clean`/`filepath.Abs` and verifying the resolved path has the sandbox dir as a prefix, rejecting any `..` component or symlink escape) to be deterministic and total — handle every malformed/truncated/adversarial input without panicking or producing ambiguous output, since any divergence from the reference on any of the 10,000 inputs fails the check. Test with clearly valid, clearly invalid, and boundary-case constructed tokens/paths before considering it done.