Model this as a graph problem in Go: parse `dataset.json` into node/edge lists, then build a map from each Paper ID to the set of Author IDs that wrote it (from the `edges` where source=Author, target=Paper). From that, build a co-authorship weight map: for every pair of authors sharing a paper, increment their pair's shared-paper count by one per paper (a pair sharing N papers contributes weight N, so iterate each paper's author set and increment every unordered pair within it). Once you have this weighted co-author graph, enumerate all triangles: for every unordered triple of authors, check that all three pairwise edges exist in the co-author map (i.e., each pair has count > 0), and if so compute the triangle's strength as the sum of the three pairwise counts. Canonicalize each triangle by sorting its three author IDs alphabetically before storing so you don't double-count permutations. Sort the final triangle list descending by strength, breaking ties by ascending (author1, author2, author3) lexicographic order. Write the CSV with the exact header `author1,author2,author3,strength` and no trailing formatting differences (watch for CRLF vs LF, and trailing newline) since verification does an exact string comparison.