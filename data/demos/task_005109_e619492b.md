Start by getting the environment wiring correct, since nothing else can be tested until it is: fix `/home/user/app/.env` so `REDIS_URL` points to the actual Redis port (6379) and `BIND_ADDR` matches the port the verifier will hit (8080), then run `startup.sh` and confirm both processes are actually listening (`ss -ltnp` or `curl`) before touching Rust code. Next, use the provided corpora as your test oracle: iterate over `/home/user/corpus/evil/*` piping each through your service via curl and note which requests panic the Rust process (check its logs/exit) — these reveal the specific malformed-JSON edge cases (bad escapes, wrong types, missing fields) your parser must handle without `.unwrap()` panics; prefer `serde_json::from_str` with proper `Result` handling and pattern matching over hand-rolled parsing. For PII stripping, after successful parsing, walk the JSON value recursively: replace any object field literally named `ssn` or `credit_card` with `"[REDACTED]"`, and separately scan all string values with a compiled regex for `\b\d{3}-\d{2}-\d{4}\b`, replacing matches. Confirm the clean corpus passes through byte-for-byte unmodified in structure. Only after local curl tests against both corpora succeed should you write `READY` to `/home/user/solution_status.txt`.