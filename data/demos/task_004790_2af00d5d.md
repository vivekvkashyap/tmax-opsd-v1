Start by running `tesseract /app/config/sensor_specs.png stdout` (try a couple of `--psm` modes if the raw output is garbled) to recover the Sensor ID validation regex from the image — read it carefully character-by-character, since OCR often confuses similar characters in regex syntax (e.g. `{}`, `\d`, hyphens), and sanity-check it against the sample rows in `/app/corpora/clean/` (should all match) and `/app/corpora/evil/` (bad IDs should not match). Build `sanitizer.py` using Python's `csv` module (which correctly handles embedded newlines inside quoted fields) rather than naive line-splitting, so you can detect "embedded newline in a field" as a distinct evil condition versus rows that just parse cleanly; validate `timestamp` with `datetime.strptime` against the strict ISO-8601 pattern (not just a loose regex), and validate `sensor_id` against your extracted regex with `re.fullmatch`. Preserve passing rows byte-for-byte (same field values, don't reformat). For the bash orchestrator, iterate `*.csv` files, invoke the sanitizer per file, and build the HTML report with a heredoc template, counting files processed and diffing file counts between the input and output directories as the checkpoint. Test end-to-end against both corpora before finishing.