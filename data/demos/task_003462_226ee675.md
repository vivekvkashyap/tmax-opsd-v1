Read `/home/user/target_app/src/main.rs` closely as a two-part reverse-engineering exercise, not something to hardcode. For `verify_admin`, the token isn't stored directly — it's derived from a known plaintext (built from the admin username and salt) run through a per-character transformation (e.g. shifting each byte's value by a fixed offset). Write `exploit.rs` to construct that plaintext and apply the same transformation programmatically (iterate chars, cast to numeric, add offset, cast back) so it computes the token rather than embedding a hand-derived literal. For `sanitize_xss`, it does simple non-recursive substring removal of the literal opening/closing script tags — so a payload where tag markers are nested inside themselves survives, because removing the inner match leaves the outer markers intact and rejoined. Construct such a self-healing payload algorithmically (splice a decoy `<script>`/`</script>` fragment inside the real tags) so that after the target's `.replace()` calls run, the leftover string equals the target payload. Compile with `rustc`, run it, and write its `TOKEN|PAYLOAD` stdout to `/home/user/final_payload.txt`.