Handle the three phases as separate, independently-verifiable deliverables. Phase 1: extract frames from `/app/e2e_test_run.mp4` with `ffmpeg -i ... -vf fps=<N> frame_%04d.png` at a high enough sampling rate to not skip the 15 flashing frames, then OCR each frame (`tesseract`) looking for a line matching `AUTH_TOKEN: XXXX-XXXX-XXXX-XXXX`; don't assume a single OCR pass is clean — check several nearby frames and take the value that appears consistently, then write that exact recovered string to `/home/user/auth_token.txt` (no extra whitespace/newline surprises). Phase 2: write `/home/user/calc_engine.cpp` with the minimal exported symbol described, then `build.sh` must invoke `g++` twice with genuinely different flag sets — debug build needs `-g -O0 -DDEBUG_TRACE=1 -shared -fPIC`, release build needs `-O3 -DPRODUCTION_MODE=1 -shared -fPIC -s` (or a separate `strip` step) so the debug `.so` retains a `.debug_info` ELF section (verify with `objdump --dwarf=info` or `readelf -S`) while the release one doesn't. Phase 3: implement `is_safe_expression` with explicit, separately-testable checks (regex/case-insensitive substring match for banned keywords, a paren-depth counter, length check, and a regex extracting numbers around `^` to compare digit counts) — then run it against every file in `/app/corpus/clean` and `/app/corpus/evil` yourself and fix any misses before finishing.
