Build this as a pure awk/sed/sort pipeline in stages, testing each stage's output before chaining the next. Stage 1 (normalize): strip leading/trailing whitespace from every field (awk's `gsub(/^[ \t]+|[ \t]+$/, "", field)` per column after splitting on commas) and uppercase the header line only (`tr '[:lower:]' '[:upper:]'` on line 1, keep data rows' values as-is except sensor names). Stage 2 (reshape wide-to-long): for each data row, split into timestamp + N sensor columns using the *normalized* header to get each sensor's name, then emit one output line per sensor column as `timestamp,SENSOR_NAME,temp` — do this in awk by looping over fields 2..NF and pairing with the corresponding uppercased header field name. Stage 3 (rolling SMA): this must be computed *per sensor group* in chronological order, not globally — track a per-sensor queue of up to the last 3 temp values (associative arrays keyed by sensor name holding recent values), and average whatever's accumulated so far (1, 2, or 3 values), formatting to 2 decimals with `printf "%.2f"`. The natural processing order is by timestamp (input order), but the *output* must then be sorted by SENSOR_NAME then TIMESTAMP — do the SMA calculation first in temporal order, buffer the results, then pipe through `sort -t, -k2,2 -k1,1` before writing the final file with the header prepended. Diff your output against the given examples in the task description to catch off-by-one windowing errors.