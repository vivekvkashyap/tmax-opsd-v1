First OCR `/app/config_image.png` (e.g. `tesseract /app/config_image.png stdout`) to recover the exact list of invalid tokens and the numeric threshold — these are the parameters your filter logic must use, and they must come from the image, not be guessed. Then write `etl_cleaner.py` as a strict stdin-to-stdout filter: for each input line, split on whitespace (`line.strip().split()` is the natural approach, treating consecutive whitespace as a single delimiter and stripping the trailing newline), drop tokens that exactly match one of the invalid tokens (case-sensitive exact match, not substring), and if zero tokens remain print `REJECTED`. Otherwise compute the average character length of the remaining tokens as a float division, and print the space-joined remaining tokens only if that average is `>=` the threshold (inclusive), else print `REJECTED`. Handle an empty/whitespace-only line the same way (no tokens -> REJECTED). Because grading is exact-match fuzzing against a hidden strict reference implementation across thousands of random strings, be precise about edge cases: exact whitespace splitting semantics, floating point comparison direction (`>=` not `>`), and not adding any extra text/formatting beyond what's specified. Add the `#!/usr/bin/env python3` shebang and `chmod +x` the script.