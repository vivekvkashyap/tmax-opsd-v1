Performance is the binding constraint: a naive O(N*window) rolling median in C may be too slow at scale, so favor an O(N log window) sliding-window approach (balanced insert/remove structure) over brute-force re-sorting; compile with `-O2`/`-O3`. Match the exact C signature and write zeros for the first `window-1` outputs. For the WS server, use `ctypes` to call the `.so`, passing/receiving `double*` buffers via `ctypes.POINTER(ctypes.c_double)` — a common bug is mis-building the ctypes array from JSON floats or off-by-one buffer sizing. For rate limiting, track per-IP request timestamps (Redis or in-memory) and close the socket with the exact custom code 4000 when a client exceeds 10 req/s — confirm your WS library actually supports a custom close code. Leave the server running and sanity-test round-trip latency yourself first, since correctness is MSE-checked against a numpy median filter.
