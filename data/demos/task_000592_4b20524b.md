Treat this as a classic graph shortest-path problem, not a tree-parent-chase. Read the CSV into a dict of emp_id -> (name, salary, manager_id), then build an undirected adjacency list by adding an edge between each employee and their manager_id (skip empty manager_id). Run BFS from EMP-012, tracking predecessors, until you reach EMP-007, then reconstruct the path by walking predecessors backward and reversing it — do not assume the path is a simple straight line up to a common ancestor and back down; with an undirected graph BFS naturally finds the true shortest path even through indirect connections. Sum the salary field for every emp_id in the reconstructed path (inclusive of both endpoints). Write the result as JSON with keys `path` (ordered list from EMP-012 to EMP-007) and `total_salary` (integer) to `/home/user/path_summary.json` using `json.dump`. Verify by printing intermediate BFS distances/predecessors before trusting the final path, and double check you didn't off-by-one the path reconstruction (missing the start or end node).