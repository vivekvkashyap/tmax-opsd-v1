First listen to (or transcribe with a speech-to-text tool, e.g. `whisper`) `/app/voice_memo.wav` to recover the exact transformation rule described — do not assume it, derive it from the audio content, and note it will describe two chained string operations. Define `/home/user/metadata.proto` with `syntax = "proto3";`, `option go_package = "./pb";`, and message `AssetInfo { string data = 1; int32 original_length = 2; }`, then generate Go bindings with `protoc --go_out=. --go_opt=paths=source_relative metadata.proto` (ensure `protoc-gen-go` is installed/on PATH). In `/home/user/main.go`: read all of stdin via `io.ReadAll(os.Stdin)`, trim exactly one trailing `\n` with `strings.TrimSuffix`, record the trimmed string's length as `original_length`, apply the transformation you recovered from the audio to produce `data`, populate and `proto.Marshal` an `AssetInfo`, and write only the raw serialized bytes to stdout (no fmt.Println or debug prints — those would corrupt the binary output stream). Init the module (`go mod init encoder`), fetch `google.golang.org/protobuf`, and build the binary to `/home/user/encoder`. Test round-trip manually by piping a known string in and unmarshaling the output to confirm the encoding matches your understanding of the audio instructions.
