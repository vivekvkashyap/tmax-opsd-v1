Work the pipeline stage by stage, verifying each before moving on. Extract frames from `/app/intercepted_session.mp4` with `ffmpeg -i ... frame_%04d.png` (or sample at the video's fps) and read off the four hex-encoded ciphertexts shown on screen — record them exactly as displayed, don't paraphrase. Next, read `/home/user/weak_cipher.py` closely to understand its substitution-permutation structure, then perform differential cryptanalysis: feed the cipher chosen plaintext pairs with controlled input differences, observe how the output differences propagate through the rounds, and use that to narrow down and recover the 16-bit key by testing candidate keys against known plaintext/ciphertext behavior — write a small brute-force/differential script rather than guessing. Once you have the key, decrypt the four captured payloads with the cipher's decrypt routine to see the actual attacker strings (path traversal and XSS patterns). Implement `sanitizer.py` to neutralize those classes generically: normalize paths (e.g., `os.path.normpath` combined with rejecting/stripping `../` sequences) and HTML-escape dangerous tags/protocols (`<script>`, `javascript:`) without over-escaping benign input, then fuzz-test it yourself against varied inputs before submitting.
