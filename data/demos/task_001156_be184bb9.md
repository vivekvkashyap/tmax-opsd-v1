Work through this in clearly separated stages and validate each before chaining them. First, get true edge data out of the SQLite DB despite the corrupted index: don't rely on a query planner that might pick the bad index — force a full table scan (e.g. `sqlite3 network.db "SELECT * FROM edges"` after `DROP INDEX idx_edges_src;`, or use `PRAGMA` to disable automatic index use) so you read straight from the table b-tree rather than stale index entries. Second, apply updates.csv as overrides: for each (source,target) pair in the CSV, replace any existing DB edge weight or add it as new. Third, reverse-engineer the oracle's protocol before scripting against it — run `strings /app/path_oracle` to find its literal command keywords (look for something like an EDGE/ROUTE/QUIT-style text protocol), and confirm the format empirically by feeding it a couple of manual commands interactively and observing stdout, rather than assuming a format. Then script feeding all edges followed by all 100 queries from queries.csv in one stdin stream, capturing stdout, and mapping results back to `source,target,distance` (use -1 for "no path" if that's how the oracle signals it — verify what its no-path output actually looks like from your probing). Write results.csv in the exact column format, and spot-check a handful of results manually (e.g. with a quick Dijkstra sanity check) before finishing.