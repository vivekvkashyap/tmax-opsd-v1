Work through the three defects in order, testing after each. First, get it compiling: add the missing standard headers (`<cstdint>`, `<string>`, and anything else `g++ -std=c++17 -Wall` complains about) — don't suppress warnings, actually fix the includes. Second, fix the resync logic: replace the `throw` on an unexpected marker byte with a loop that, on seeing a non-`0xFF` byte, keeps reading and discarding single bytes (re-checking each one) until it finds `0xFF` or hits EOF, then resumes normal 5-byte record parsing from there — this is a byte-by-byte scan, not a fixed skip count, since corrupted regions vary in length. Third, fix the average: the temperature field is deci-Celsius as a signed 16-bit int, so cast the running sum to floating point (or cast at division time) before dividing by count, then divide by 10.0 to convert deci-Celsius to Celsius — the original bug does integer division and skips the /10 scale entirely. Redirect the fixed program's output to `/home/user/ticket_4092/report.txt` in the exact two-line format with `%.2f`-equivalent formatting via `std::fixed << std::setprecision(2)`. Rebuild with `make`, run it against `sensor_data.bin`, and inspect `report.txt` directly to confirm both lines look sane before finishing.
