Fix things in dependency order and verify each stage builds before moving on. First, try `cargo build` in `/home/user/tool/rust_parser` and read the borrow-checker error carefully — a function returning `*const c_char` from a local `String` is a classic use-after-free/dangling-pointer bug because the `String` is dropped when the function returns; fix it by leaking the string intentionally (e.g., `Box::leak` or a `'static` string literal) or by using a thread-local/static buffer, so the returned pointer stays valid for the caller, while keeping the function's exported signature identical. Second, run `make` in `/home/user/tool` and read the "undefined reference" error — with static libraries, `gcc` resolves symbols left-to-right, so if `main.o` (which references the Rust symbol) appears after the `.a` archive on the link line, the reference won't be found; reorder the Makefile's link command so object files needing symbols precede the static archives that provide them (or use `-Wl,--start-group/--end-group` if there are circular needs). Confirm `http_fuzzer` is produced and runs standalone. Finally, run `bash e2e_test.sh` (it stands up a mock backend and proxy itself) and redirect all output to `/home/user/test_result.log`, then check the log contains the fuzzer's payload output and the script's completion message.
