Start by running `tesseract /app/policy.png stdout` to extract the version-constraint text — inspect the raw output carefully (trim whitespace/newlines) since OCR output often needs cleanup before it's usable as a parseable constraint string; don't hardcode what you read, have the service call tesseract dynamically each time as the spec requires. For the semver bug, open `src/semver.rs` and trace the `PartialOrd`/`cmp` implementation: look specifically for a comparison that only checks `major` before falling through to `patch`, skipping `minor` entirely — fix it to compare major, then minor, then patch in strict lexicographic order. Add a `proptest!` block that generates random `Version` triples and asserts transitivity (`if a > b && b > c { assert!(a > c) }`), then run `cargo test` to confirm both correctness and the new property test pass. For `main.rs`, wire up an async HTTP framework (axum/tiny_http) bound exactly to `127.0.0.1:8080`, parse the `POST /validate` JSON body, re-run tesseract to get the current constraint, parse it and the given version with your fixed semver module, and return `{"allowed": bool}`. Only touch `/tmp/service_ready` after the listener is confirmed live (e.g., after `bind`/`serve` succeeds).
