Build the SQL in two composable pieces before joining them. First write a recursive CTE seeded on `manager_id IS NULL` with `hierarchy_level = 0`, recursively joining `employees` to itself on `manager_id = emp_id` and incrementing the level — test this in isolation with `sqlite3 audit.db` first to confirm levels look right before adding more logic. Second, write a separate query/CTE over `access_logs` that groups by `(emp_id, system_name)` to get counts, then uses `ROW_NUMBER() OVER (PARTITION BY emp_id ORDER BY count DESC, system_name ASC)` to pick the top system per employee (the ORDER BY tiebreak on `system_name ASC` is required for deterministic ties) and filters to `rn = 1`. Third, INNER JOIN (not LEFT JOIN) this ranked-systems result to the hierarchy CTE on `emp_id` so employees with zero access logs are naturally excluded. In Python, execute the combined query, fetch rows in `employee_id` ascending order, build the exact key names (`employee_id`, `hierarchy_level`, `top_system`, `top_system_access_count`), and write with `json.dump(..., indent=2)` to the report path. Validate by manually eyeballing counts against a plain `SELECT ... GROUP BY` sanity query.
