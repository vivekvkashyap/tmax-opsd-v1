Build this as a real ETL pipeline in Rust, not a one-off script. Steps:
1. `cargo new /home/user/log_processor`, add `serde`/`serde_json` (deserialize into structs with a nested `events: Vec<Event>`), `csv` for writing, and `sha2` for hashing.
2. Read and parse `/home/user/raw_logs.json` into your session structs.
3. Flatten: for each session, iterate its `events` and emit one output record per event, carrying the parent's `client_ip`/`user_id` onto each.
4. Parse `request` by splitting on whitespace to get method and the raw path (drop the HTTP version token); if the string doesn't split into the expected parts, fall back to `"UNKNOWN"`/`"UNKNOWN"`. Normalize the path by splitting on `/` and replacing any segment that parses entirely as numeric with `{id}`, then rejoining — don't use a fragile regex that only matches trailing digits, since IDs can appear mid-path.
5. Mask the IP by splitting on `.`, replacing the 4th octet with `0`, rejoining — don't just string-replace digits.
6. Hash `user_id` with SHA-256 (sha2 crate), format as lower-case hex.
5. Sort the flattened rows by `timestamp` (ISO-8601 strings sort correctly lexicographically) before writing.
6. Write CSV with the exact header order specified, then `cargo build`/`cargo run` and confirm the output file appears at `/home/user/processed_logs.csv`.
