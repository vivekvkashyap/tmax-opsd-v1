First recover the Global Master Key by examining `/app/artifact_scan.mp4` frame by frame: extract all 8 frames (e.g. with `ffmpeg`), classify each as black or white by sampling pixel values, and assemble the 8 bits in chronological order (frame 1 = MSB, frame 8 = LSB) into a single byte — do this recovery yourself rather than guessing. Then implement `/home/user/parser.go` strictly per the binary spec: read the whole file, take an advisory shared flock (`syscall.Flock` with `LOCK_SH`) before parsing, and validate every length constraint before indexing (file too short, wrong magic, wrong version, insufficient bytes for the embedded key or the declared payload length) — on any violation print exactly `INVALID` and exit 1, with no other output. If valid, slice out the payload, and apply XOR only for the bits that are actually set: embedded-key XOR first (cyclically over K bytes, skipped if K==0), then master-key XOR second, only if the corresponding flag bit is set — order matters when both bits are set. Print the raw decrypted bytes to stdout with no added newline, exit 0. Since this will be fuzz-tested against an oracle, focus on getting every boundary check exactly right (off-by-one lengths, K=0 case, flags=0 case) rather than the happy path alone; write a few of your own edge-case test files to self-check before considering it done.
