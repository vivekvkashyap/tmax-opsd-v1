Split this into the transform script and the service wiring, and get each working independently before testing end-to-end. For `process_artifact.py`: read the gzip file fully into memory with the `gzip` module, do a plain string/bytes replace of `__MACRO_REPO_HOST__` with the target host, then re-gzip. For the atomic write, do not write directly to the original path — create the temp file with `tempfile.NamedTemporaryFile(dir=os.path.dirname(target), delete=False)` (same directory, so it's on the same filesystem), write and flush/fsync the compressed bytes to it, close it, then use `os.replace(tmp_path, target_path)`, which is atomic on POSIX — a naive `open(path,'w')` truncate-then-write is not atomic and would fail a concurrent-reader check. For the service stack: read `/app/upload_server.py` and `/app/worker.py` to understand how they hand off files (likely via a Redis list), start `redis-server` (or confirm it's already running), edit `/app/start_worker.sh` to export `PROCESSOR_SCRIPT=/home/user/process_artifact.py` before it invokes the worker, then launch the Flask app and the worker script in the background (`nohup ... &`). Validate by POSTing a small gzip test file containing the macro string to `http://127.0.0.1:5000/upload` and checking the uploaded file was rewritten and remains valid gzip.
