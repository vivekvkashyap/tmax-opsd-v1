Decode the video first since it gates everything downstream: read `/app/signal.mp4` frame by frame (e.g. with `cv2.VideoCapture` or `ffmpeg` frame extraction), classify each frame as black(0)/white(1) by checking mean pixel intensity against a midpoint threshold, concatenate the bits in frame order, and decode every 8 bits as an ASCII byte to recover the constraint text — sanity-check the result is printable text with recognizable version-constraint syntax before trusting it. Read the Go reference implementation to understand exactly how it resolves each package's highest valid version under the combined constraints (treat overlapping constraints per package as an intersection of allowed ranges, then pick the max satisfying version) — your C++ port must reproduce that same resolution logic and print `Package: Version` lines sorted alphabetically by package. For performance, avoid unnecessary I/O/allocation in the hot path and use real concurrency (e.g. `std::thread`/`std::async` with a lock-free or mutex-protected merge structure) so the compiled `-O3 -pthread -std=c++20` binary finishes well under the 0.5s threshold on the scaled dataset; profile if it's close to the limit.