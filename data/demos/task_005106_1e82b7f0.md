Read the C source fully before editing. First diagnose the leak: `strdup()` allocates a new buffer per line, but the `continue` statements on malformed/invalid rows skip the corresponding `free(record)` at the loop's end — every early-exit path must free `record` before continuing (or restructure with a single cleanup path/goto). Second diagnose the hang: the file uses `float` for `current`/`start_time`/`end_time`; once the epoch value grows large, `float`'s ~7 significant digits mean `current += 0.05f` stops changing `current` at all (mantissa exhausted), so `current < end_time` never becomes false — an infinite loop. Fix by switching the timestamp/accumulator variables (and `sscanf`/`fprintf` conversions) from `float`/`%f` to `double`/`%lf`, which has enough precision that adding 0.05 to a ~1.7e9 epoch still advances it. After fixing, recompile with `gcc -g -O0`, run under valgrind to confirm zero leaks/errors, then run against the real data file and diff the output. Keep the exact `Window: %f to %f, avg: %f\n` output format.