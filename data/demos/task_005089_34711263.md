Scaffold with `cargo new bio_divergence` in `/home/user/bio_divergence`. Parse the FASTA by skipping header lines (starting with `>`) and concatenating/iterating over sequence characters, incrementing a per-amino-acid counter only for the 20 standard letters (use a fixed set/whitelist so stray characters or gaps are silently ignored, not counted or errored on). Apply add-one smoothing by initializing every one of the 20 counters to 1 before counting (not adding 1 only to amino acids that appeared — every one of the 20 gets +1 regardless), then normalize by dividing each smoothed count by the total smoothed count to get P. Parse `background.csv` (no header row) into a map from letter to Q probability. Compute KL divergence as the sum over the 20 amino acids of `P(i) * ln(P(i)/Q(i))` using natural log (`f64::ln`), being careful to iterate over the same fixed 20-letter set on both sides so nothing is skipped due to missing keys. Format the final sum to exactly 4 decimal places and write it to `/home/user/kl_divergence.txt` with no extra whitespace or newline artifacts beyond what's expected. Run `cargo run` and inspect the output file directly to sanity-check the value looks like a small non-negative KL divergence.