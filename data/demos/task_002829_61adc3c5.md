Treat this as two unrelated deliverables. Part 1 (video OCR): extract frames from `/app/capture.mp4` at a fine enough interval to not miss a brief flash — e.g. `ffmpeg -i /app/capture.mp4 -vf fps=<high-rate> frame_%04d.png` — then OCR each frame (or a promising subset) with `tesseract` and grep the OCR text for the pattern `TOKEN:` followed by 16 alphanumeric characters; if a straight frame-rate sweep misses it, narrow down by binary-searching timestamps around where text briefly appears. Write the full matched token string, exactly as found, to `/home/user/video_secret.txt` — don't guess or truncate it. Part 2 (classifier): inspect the sample files under `/app/corpus/evil/` and `/app/corpus/clean/` to derive concrete, generalizable signatures of command-injection/reverse-shell techniques (e.g. `base64 -d | sh`/`bash`, `nc -e`, suspicious `eval` constructs, obfuscated pipes to shells) rather than hardcoding exact file contents, then write `/home/user/detector.py` (executable, single directory argv) that scans every file in that directory and prints `<filename>: EVIL` or `<filename>: CLEAN` per line. Validate against both provided corpora first, aiming for 100% correct classification, since a hidden corpus with similar techniques will be used to grade it.