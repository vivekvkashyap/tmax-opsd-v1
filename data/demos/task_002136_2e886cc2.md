In lib.rs, the borrow-checker error comes from holding a reference into a `Vec` (e.g. `let r = &results[0];`) while later mutating that same `Vec` via `.push()` — Rust forbids a live immutable borrow across a mutation that could reallocate the backing buffer. Fix it by capturing the needed value (e.g. copy `results[0]` into a plain `f64` before the loop) instead of keeping a reference, so no borrow spans the mutation; do not alter the polynomial math or the `poly_eval` C ABI signature. Build with `cargo build --release` and confirm `librust_lib.so` lands under `target/release/`. For the Go side, use `cgo` with `#cgo LDFLAGS: -L<path> -lrust_lib -Wl,-rpath,<path>` so the compiled binary can find the shared library at runtime without needing `LD_LIBRARY_PATH` set externally. Implement the rate limiter per-IP (e.g. a map of IP to a token bucket or timestamp list, guarded by a mutex) enforcing exactly 2 req/sec before hitting the Rust call; validate `coeffs` non-empty before calling the FFI to avoid passing a null/zero-length slice. Write table-driven tests exercising success, 400, and 429 paths, then `go build -o server` and `go test`.
