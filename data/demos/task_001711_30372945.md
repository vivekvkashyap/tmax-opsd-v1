Start by OCR'ing /app/motif_reference.png with `tesseract image.png stdout` (or similar) and parsing out the `<SEQUENCE>` following the `MOTIF:` prefix — trim whitespace/newlines tesseract may introduce and validate it only contains A/C/G/T before using it, retrying OCR settings (psm mode, contrast) if it misreads a character. Write /home/user/motif_scorer.c to slide the motif of length L across /home/user/target.txt (length N), scoring each of the N-L+1 positions by +1 per matching char / -1 per mismatch; parallelize with `#pragma omp parallel for` over the starting-position loop, having each thread compute a local best (score, index) and then reduce across threads picking the lowest index on ties (a naive OpenMP reduction on plain max won't preserve tie-breaking by index, so track index alongside score, e.g. via a custom reduction, per-thread locals combined in a critical section, or by encoding (score, -index) into a single comparable value). Compile with `gcc -fopenmp`. For serving, the simplest robust approach is a small C or bash/socat/nc loop listening on 127.0.0.1:8080 that, on each `/score` GET, runs your compiled scorer against target.txt and writes back a proper `HTTP/1.1 200 OK` header plus `{"max_score": <int>, "index": <int>}` JSON body, then keeps looping to accept the next connection.