Work this as a pipeline of small, verifiable jq/bash steps rather than one big command. First filter: `jq -r 'select(.service=="RustCompileAPI") | .meta.version'` (or similar) across `/home/user/api_logs/*.json`, pairing each version with its source filename (e.g. output `version filename` lines) so you can trace back to the right file later. Sort these semantic versions correctly — plain lexicographic `sort` breaks on multi-digit components (e.g. 1.10.1 vs 1.2.11) and doesn't understand pre-release suffixes like `-rc1`; use `sort -V` (version sort) or `sort -t. -k1,1n -k2,2n -k3,3n` and treat/verify how `-rc1` should rank relative to the final release by checking semver rules (pre-release < release of the same X.Y.Z), then pick the top two. For each of those two files, extract `.data.endpoints[]` with jq, pipe through `sort` to get one endpoint per line, and save each to a temp file. Finally run `diff -u --label "second_highest" --label "highest" old.txt new.txt > /home/user/endpoint_changes.diff`. Double-check by manually inspecting which two files you picked and their raw endpoint arrays before trusting the diff output.