Split this into config wiring and validation logic, and test each independently before the end-to-end check. For nginx, edit `/etc/nginx/sites-available/default` to `listen 8080;` with `proxy_pass http://127.0.0.1:5000;`, then reload/restart nginx and confirm with `curl localhost:8080` that it reaches the Flask app. For `start_pipeline.sh`, have it start nginx, `export VALIDATOR_SCRIPT=/home/user/validator.py`, then launch `python3 /app/tracking_server.py &` in the background, and make the script executable. For `validator.py`, read the CSV with the csv module or pandas, check headers match exactly `epoch,loss,accuracy` (no extras, no missing), then per-row: `epoch` parses as a non-negative int, `loss` parses as float and is strictly > 0, `accuracy` parses as float in [0,1], and no cell is empty/NaN — any parse failure or violation should be caught and treated as invalid rather than crashing. Exit 0 only if every row passes every rule, else exit 1. Test the validator directly against sample clean/evil CSVs by hand before testing the full HTTP round trip via curl POST to `/upload`.