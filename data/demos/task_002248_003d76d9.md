Fix the two bugs independently and verify each in isolation before running the full pipeline. For the C++ buffer overflow in `parse_version_core`: the fixed 16-byte stack buffer overflows when the pre-release-stripped base version plus a very long suffix is copied byte-by-byte without a bounds check — fix it by bounding the copy loop against the buffer size (or better, writing directly to `output` which the Python side already allocates as a 256-byte buffer, avoiding the intermediate fixed buffer entirely) rather than just enlarging the constant, since the caller can pass arbitrarily long strings. Rebuild with `make all` and test it directly against a long pre-release string before touching the Python. For `compare_versions`, replace the naive lexicographic `>`/`<` string comparison with logic that splits each version on `.` into integer components and compares them component-by-component numerically (so "1.2.10" > "1.2.3"). After both fixes, run `python3 build_resolver.py manifest_A.json manifest_B.json`, then manually recompute the expected highest version per key from the two manifests to confirm your merged/sorted JSON output is correct before trusting it.