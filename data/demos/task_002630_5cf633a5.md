Fix the C build first: in `/home/user/app/c_eval`, the Makefile likely fails to pass `-L. -lmatheval` when linking `eval_server` against `libmatheval.so`, and even once it links, the runtime loader won't find the `.so` unless you add `-Wl,-rpath,.` to the link flags (or export `LD_LIBRARY_PATH=.` before running) — add both the link flag and the rpath so `./eval_server` starts on port 8081 without "cannot open shared object" errors. For `sanitizer.go`'s `Sanitize(expr string) error`: validate the character set with a regex (`^[0-9+\-*/(). ]+$`) to reject variables/unknown symbols, use a simple stack-based parenthesis balance check for mismatches, and detect division by zero by either parsing the expression into an AST/recursive-descent evaluator and checking for `/ 0` denominators (including sub-expressions evaluating to 0) or at minimum textual detection of `/ 0` and `/(...)` where the parenthesized part evaluates to 0. Start `redis-server`, `./eval_server`, and `go run main.go sanitizer.go` all backgrounded. Verify end-to-end against `/home/user/app/corpora/clean` (expect HTTP 200) and `evil` (expect HTTP 400) with curl before writing `READY` to `success.log`.