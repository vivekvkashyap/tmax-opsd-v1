Build this as a standard extract-transform-join-load script using regexes and csv, and test against the sample data before trusting it. Use one regex per log line that captures the bracketed timestamp, the 4-digit user ID after "User ", and the ERR_ token together (e.g. `r'\[(\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2})\].*User (\d{4}).*?(ERR_\w+)'`), and skip (continue past) any line that doesn't match all three groups — don't error out on non-matching lines like restart/system messages. Load departments.csv into a dict keyed by the raw (unmasked) user_id string, and only emit a row if that raw ID is present in the dict — match before masking, since masking is applied only for display in the output. Mask by keeping exactly the first two characters and replacing the remaining two with '*' (assume 4-digit IDs per the spec). Before writing, sort all matched rows by timestamp string (which sorts correctly lexicographically given the fixed YYYY-MM-DD HH:MM:SS format), then write with Python's csv.writer including the exact header `timestamp,masked_user,dept,error_code` so quoting/newline conventions match what a csv.reader-based verifier expects.
