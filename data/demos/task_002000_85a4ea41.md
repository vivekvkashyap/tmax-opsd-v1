Start by treating `/app/graph_daemon` as a black box you must characterize empirically, not read source for. Launch it (checking its `--help`/usage or startup flags, and how it's told to bind `/tmp/graph.sock` and load `/home/user/data.csv` — try common patterns like `./graph_daemon --socket /tmp/graph.sock --data /home/user/data.csv` and inspect any logged errors to converge on the right invocation), then reverse-engineer the wire protocol by writing a small throwaway client (Python `socket` script is fastest) that connects to the Unix socket and sends candidate byte sequences while observing raw responses — confirm the 4-byte header pattern (command byte, depth byte, 2-byte node id) plus trailing newline by trial and error against known small queries, and specifically capture what a `0xFF` deadlock response looks like on the wire. Only after you can reliably drive the daemon manually should you write the C++ HTTP server: implement Bearer-token auth checking first (reject fast on mismatch), parse `start_node`/`depth` query params, build the query bytes per the reverse-engineered format, and wrap the socket call in a retry-with-backoff loop keyed off detecting the `0xFF` response byte before returning the JSON node array.
