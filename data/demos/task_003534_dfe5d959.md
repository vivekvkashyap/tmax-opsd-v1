Use `git log --oneline` (or `git bisect start`) in `/home/user/metric_service` to walk the commit history of `calc.py`; since only one commit is known good (the first) and HEAD is bad, bisect by checking out each candidate commit, running `python3 calc.py`, and comparing its printed "Final Average" against the reported-good baseline behavior described in the ticket — narrow down to the single commit whose diff changes the arithmetic in `calc_health` (look closely at the sign/operator between the two terms, since formula regressions are often a single flipped operator introduced alongside an innocuous-looking comment-only commit). Once you find the exact bad commit, get its full hash with `git rev-parse <commit>`. Then fix `calc.py` at HEAD by reverting just that operator to match the original good logic (don't blindly `git revert`/`checkout` the whole bad commit if later unrelated commits touched other files — edit the function directly). Re-run `calc.py` to confirm the average now matches the ticket's baseline value, then write `/home/user/resolution.txt` with `BAD_COMMIT=<hash>` and `CORRECTED_AVERAGE=<value>` rounded to 2 decimals, and finish any in-progress `git bisect` with `git bisect reset` to leave the repo clean.
