Break this into independent, testable stages before wiring up the API. First run `tesseract /app/config.png stdout` (install `tesseract-ocr` if missing) and grep the output for the `AUTH_TOKEN:` line to extract the token programmatically at startup — don't hardcode a guessed token, read it from the OCR output each time the service boots so it stays correct if the image changes. Second, build the graph with `networkx.DiGraph()`: iterate `transactions.jsonl` line by line, and when adding an edge that already exists, add the amount to the existing weight rather than overwriting it (use `G.add_edge(u, v, weight=G[u][v]['weight']+amt if G.has_edge(u,v) else amt)` or accumulate in a dict first, then build the graph). Compute `nx.pagerank(G, weight='weight')` once at startup (not per-request) and cache it. For clusters, use `nx.weakly_connected_components(G)`, and for each component pick `min(component)` as its ID (lexicographic string min, matching the task's rule) and sum the weights of all edges whose endpoints are both in that component. Implement the Flask/FastAPI routes with a decorator or middleware that checks the `Authorization: Bearer <token>` header against the OCR'd token for every route except `/health`, returning 401 on mismatch. Run the server backgrounded and manually curl each endpoint (with and without auth) to confirm behavior before finishing.