Start by running `cargo test` in `/app/math-daemon` to see the proptest failure output for `src/interpreter.rs` — it will show a panicking input where the divisor for opcode `0x04` is zero. Locate the division-handling branch and change it from an unchecked `a / b` to first check `b == 0`, returning/propagating an error value or `Result::Err("DivisionByZero".to_string())` through whatever error-handling convention the rest of the interpreter already uses (match the existing pattern rather than introducing a new one, e.g. if other opcodes return `Result<i32, String>`, do the same here) instead of letting it panic; re-run `cargo test` until it's green. Separately, in `src/main.rs`, find the `.bind("127.0.0.1:9000")` call and change the host to `0.0.0.0:9000` so the container-mapped ports can reach it. Build and run with `cargo run --release &`, then manually verify with `curl` against the `/eval` POST endpoint using a division-by-zero bytecode sequence and a normal one, confirming the JSON error/result fields behave as expected, before leaving the service running in the background.
