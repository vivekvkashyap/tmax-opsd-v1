Do not skip the OCR step or assume rules from the task text alone — the exact compliance wording lives in `/app/schema_rules.png`; run `tesseract /app/schema_rules.png stdout` and read the extracted rules carefully to confirm exact table names, syntax, and forbidden patterns before writing validation logic. Implement `validator.go` to read the whole SQL file and check two independent conditions: (1) reject if the `FROM` clause contains a bare comma between table references (an implicit cross join) rather than an explicit `JOIN` keyword — a regex on the `FROM ... WHERE/JOIN` segment works given the corpus is simple SQL; (2) reject if `tenant_id` appears compared to a literal number/value instead of a placeholder (`?` or `$N`) — match `tenant_id\s*=\s*` followed by something that isn't `?` or `$<digits>`. Print a clear error message and `os.Exit(1)` on any violation found; only `os.Exit(0)` if neither rule is violated. Compile with `go build -o /home/user/validator /home/user/validator.go`, then run it manually against every file in both `/app/corpus/clean/` and `/app/corpus/evil/`, checking exit codes match expectations before declaring it done.