Build the pipeline stage by stage and validate each before moving on. For the ODE, define `dZ/dt = (-gamma + 2j*pi*f) * Z` as a complex-valued function and call `scipy.integrate.solve_ivp` with `t_eval=np.linspace(0, 2.0, 1024)`, `method='RK45'`, `max_step=0.01` — do not let the solver pick its own step size or the FFT bins will be misaligned. Loop over the parameter grid in the exact nested order given (gamma outer, f inner) since row order in the CSV matters. For each series: take `np.real(z)`, multiply by a length-1024 Hann window (`np.hanning(1024)`), run `np.fft.rfft` on the windowed signal, and compute frequency bins with `np.fft.rfftfreq(1024, d=dt)` using `dt = 2.0/1023.0` (not `2.0/1024`, since t_eval spans 1024 points over an inclusive [0,2] interval, giving 1023 intervals). Find `f_peak` as the frequency at `argmax` of the magnitude spectrum, and expect it to land near — but not exactly on — the true `f` because of finite frequency-bin resolution; that discrepancy is the "error" you're measuring, not a bug to eliminate. Write the CSV with header `gamma,f_true,f_peak,error`, and separately round the max error across all 6 runs to 4 decimals for `max_error.txt`.
