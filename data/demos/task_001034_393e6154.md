Work through this migration in dependency order. First fix the vendored C extension: inspect `/app/waf-service/vendor/http-parser-ext/Makefile` for hardcoded `python2-config` calls and change them to `python3-config` (and check `setup.py` for any Python-2-only syntax or `python2`/`pip2` references), then rebuild it so it compiles as a Python 3 extension importable as `http_parser_ext`. Update `/app/waf-service/build.sh` to invoke `python3`/`pip3` instead of the Python 2 equivalents, then run it to install the fixed extension. For the schema migration, write `/app/waf-service/migrate_db.sh` to run `sqlite3 db/rules.db < db/schema_v1.sql` followed by an `ALTER TABLE signatures ADD COLUMN rule_hash TEXT;` against the same database, and execute it. In `server.py`, convert Python 2 idioms to Python 3: `print x` -> `print(x)`, `urllib2` -> `urllib.request`, `dict.iteritems()` -> `dict.items()`, and fix any byte/str handling needed for the C extension's I/O. Finally launch the server so it binds `127.0.0.1:8443`, responds to `GET /` with `200 OK` and body `WAF Active`, and keep it running in the background (e.g. with `nohup ... &` or a daemonizing loop) — verify with `curl http://127.0.0.1:8443/` before finishing.
