Write a single C program that: parses `/home/user/data/vectors.csv` (id + 5 floats per line) into an array; seeds with `srand(atoi(argv[1]))`; builds the 5x2 projection matrix in row-major order using exactly `(double)(rand() % 1000) / 1000.0` per element, calling `rand()` in the same nested-loop order the spec implies (outer over the 5 rows, inner over the 2 columns) so the sequence of `rand()` calls matches a reference implementation bit-for-bit — getting this call order right is the detail that silently breaks reproducibility even when the math is otherwise correct. Compute each vector's 2D projection as the 1x5 vector times the 5x2 matrix (two dot products). Find the row with `id == 42`, compute Euclidean distance in projected space to every other row, and sort ascending by `(distance, id)` — implement the tie-break explicitly in your comparator, don't rely on a stable sort alone since distances are floating point and near-ties can appear. Run the compiled binary as `./search 2024`, take the top 3, and write `/home/user/output/recommendations.txt` as three lines `id,distance` with distance formatted via `%.4f` (create `/home/user/output/` first). Double check your matrix multiplication indexing (`P[j] = sum_k V[k]*M[k][j]`) against a hand-computed example before trusting the full run.