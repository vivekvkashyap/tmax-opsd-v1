First recover ETL_SEED by running OCR (e.g. `tesseract /app/params.png stdout`) or careful visual inspection on the image — do not guess the seed. Then scaffold a Rust binary crate at `/home/user/processor` with `cargo new`. Implement the LCG exactly as specified: use `u64` (or wider) intermediates for `1103515245 * X_n + 12345` before taking `mod 2^31` to avoid overflow, and generate the matrix once at program start, in row-major order (24 total LCG calls for an 8x3 matrix), converting each raw LCG output to `f64` via `/2147483648.0*2.0-1.0`. Read stdin line-by-line, parse 8 f64 fields per line, compute the 1x8 by 8x3 matrix product for each row of the vector against the columns of P, and print each of the 3 outputs formatted to exactly 4 decimal places (Rust's `{:.4}` format specifier), space-separated. Build with `cargo build --release` so the binary lands at `target/release/processor`. Before finishing, manually pipe a test vector through the binary to sanity-check output formatting (no trailing/leading whitespace issues, correct rounding of negative-zero cases).
