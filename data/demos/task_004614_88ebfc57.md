Fix the Makefile first: it likely points to the wrong vendored sqlite path and is missing linker flags required on Linux (`-lpthread -ldl`) — correct the include/source path to `/app/vendor/sqlite3` and add those link flags, then confirm the build actually links (`ldd`/`nm` or just run it) before writing detection logic. In `audit_checker.cpp`, write a tiny uniform JSON array parser (the format is fixed: objects with tx_id/src/dst/amount) rather than pulling in a JSON library, open an in-memory SQLite DB (`sqlite3_open(":memory:", ...)`), create an edges table, and insert rows using parameterized `sqlite3_bind_*` calls. For cycle detection use a recursive CTE that walks from each edge's src, accumulating a path (e.g. as a string or via a visited-list column), and checks at each step whether the current `dst` equals the original starting `src` — if any row of the CTE result satisfies that, a cycle exists. Print exactly `EVIL` or `CLEAN` with a trailing newline. Before finishing, run your compiled binary against every file in `/app/corpus/evil/` and `/app/corpus/clean/` to confirm 100% correct classification, since the recursive CTE termination condition is the easiest part to get subtly wrong (e.g. failing to stop on self-loop vs. true cycles).