Treat `/app/astro_dsl_compiler` as a black box: don't try to disassemble/reverse it, just feed it small `.astro` files and observe the SQL it prints on stdout to learn its translation patterns (keywords, LIMIT syntax, table names) empirically. Separately inspect `/app/astro_data.db` with `sqlite3 .schema` and `.indexes` to know what indexed columns exist on `stars` and `observations`. Build the Rust classifier (`cargo new`) to: (1) shell out to the compiler binary via `std::process::Command`, capturing stdout as the SQL string; (2) open the SQLite db (use the `rusqlite` crate) and run `EXPLAIN QUERY PLAN <sql>`, collecting the plan rows; (3) apply the two independent rules — parse the SQL text (regex or simple tokenizing) to require a `LIMIT` clause with value <= 100, and scan the EXPLAIN QUERY PLAN output text for any `SCAN TABLE stars` or `SCAN TABLE observations` occurrences (reject those; `SEARCH TABLE` is fine). Fail closed: if LIMIT is missing/too high OR a full scan on either restricted table is detected, exit 1; otherwise exit 0. Build test fixtures yourself (clean vs bad `.astro` files) to validate both rules independently before trusting the tool. Build with `cargo build --release` so the binary lands at `target/release/classifier`.