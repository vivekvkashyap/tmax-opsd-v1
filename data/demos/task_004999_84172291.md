Build the classifier data-driven from the provided corpora rather than guessing a fixed rule list from scratch — read every file under `/app/corpora/clean/` and `/app/corpora/evil/` first to see the actual range of patterns (XSS tags, SQLi keywords/syntax, path traversal) you must handle, then design `is_safe_input` with clear detection rules (regexes for `<script`, `drop table`, `select ... from`, `--` comment markers combined with SQL keywords, `../` sequences, etc.) and iterate until it scores 100% on both directories — don't stop at "looks reasonable," actually loop over every corpus file and assert. Set up the venv (`python3 -m venv`, activate, `pip install pytest`) in `/home/user/voice_sec` first so tests are runnable, and write `test_sanitizer.py` to dynamically glob both corpus directories and parametrize assertions per file rather than hardcoding filenames. For the incident analysis, run `/app/bin/whisper-stub /app/incident_042.wav` to get the actual transcript text (don't assume its content), feed that exact string through your tested `is_safe_input`, and write the verdict line reflecting the real boolean result — the file must say CLEAN or EVIL based on what your function actually returns, not a guess. Run `pytest -v` to confirm the full suite passes before finishing.
