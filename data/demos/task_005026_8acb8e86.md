Build the merge and video steps before writing any server code. For merging: load the JSON as the base dict (key -> {lang: text}), then apply CSV rows and then XML `<item>` entries in order so later sources overwrite earlier ones for the same (key, lang) pair; strip whitespace and run Python's `unicodedata.normalize("NFC", text)` on every string before storing it. Parse the XML with `xml.etree.ElementTree`, not regex. For video analysis, use `ffprobe`/`ffmpeg`'s scene-detection filter (e.g. `ffmpeg -i video -filter:v "select='gt(scene,0.3)',showinfo" -f null -`) to extract cut timestamps, then sort them and prepend 0.0 to build the ordered list of scene start times; zip that list against the lines of sequence.txt in order to know which key is active at a given time (a time `t` maps to the last scene-start <= t). For the server, implement auth as middleware checked before any route logic, returning 401 immediately if the header doesn't match; keep translations and the scene->key mapping in memory, and implement `/subtitle` by looking up scene index from `time`, then the key, then falling back from the requested lang to "en" if the key/lang combo is missing. Test each endpoint with curl against a running instance before considering it done.
