Start with OCR: run `tesseract /app/legacy_config.png stdout` (try different `--psm` page-segmentation modes if the first pass garbles text, and preprocess with `convert`/ImageMagick to boost contrast if needed) to recover the `XOR_KEY` and `MOD_FACTOR` values printed in the image — read them from the OCR text, don't guess. Once you have those two integers, implement the C++ processor: for each character of the argv string, compute `(int)(unsigned char)c XOR XOR_KEY`, then add `MOD_FACTOR`, and print the integers space-separated on one line (match the surviving legacy binary's exact output format, including no trailing space and a final newline — verify this by testing a few sample inputs and comparing spacing/newline behavior carefully, since the grader does exact-output fuzz comparison across thousands of random alphanumeric strings). Compile with g++ to `/home/user/migrated_processor`. For deployment, write `/home/user/deploy.sh` (chmod +x) that backgrounds an `ssh -f -N -L 8080:localhost:9090 ...` tunnel, backgrounds a loop that checks (e.g. via `pgrep` or a port check) whether `python3 -m http.server 9090` is running and (re)starts it if not, and backgrounds a health-check loop that every 5 seconds appends a timestamp + status line to `/home/user/health.log` — use `&` and `disown`/`nohup` so these survive the script's own exit.