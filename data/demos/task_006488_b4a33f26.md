Write the C program in two clearly separated passes: a manifest parser and a binary-archive decoder. For the manifest, read `/home/user/backup_manifest.log` and split it into blank-line-delimited records; for each record parse the `Job-ID:`, `Archive:`, and `Status:` fields (simple line-prefix matching is enough) and only proceed for records where status is exactly `SUCCESS`. For each archive, open the file in binary mode, read the first 4 bytes and verify they equal `BKUP` before doing anything else (skip the file otherwise), then read the next 32 bytes as the null-padded filename and trim at the first `\0`. Decompress the remaining bytes as RLE: read 2 bytes at a time as `(count, char)` pairs and emit `count` copies of `char`, continuing until EOF — don't assume a fixed payload length. Create `/home/user/restored/` before writing (`mkdir` with a check, or `mkdir -p` semantics via `stat`+`mkdir`), write the decompressed bytes to `/home/user/restored/<filename>`, then use `symlink()` to create `/home/user/restored/job_<Job-ID>.link` pointing at that restored file. Compile with `gcc` and actually run the binary to produce the output files — don't just leave the source unbuilt.