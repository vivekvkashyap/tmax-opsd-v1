Start by statically/dynamically analyzing the stripped binary: run `strings /app/cert_validator` to look for hardcoded hex digests and hints about which OpenSSL/X.509 field is inspected, and use `objdump -d` / `ltrace` / `gdb` to see which library calls (e.g. `X509_NAME_get_text_by_NID`, `MD5`/`EVP_MD5`) are invoked and in what order — this tells you both the certificate field being read and the hash algorithm before you commit to cracking anything. Once you have the field (likely an X.509 Subject sub-field) and the hardcoded digest, crack it as an offline dictionary-hash search: iterate a wordlist (e.g. `/usr/share/dict/words` or a common wordlist) computing the same hash algorithm over each candidate and compare against the extracted digest, rather than guessing; save the exact matching plaintext to `/home/user/cracked_password.txt`. Then write `/home/user/detect_backdoor.sh` using `openssl x509 -in "$1" -noout -subject` (or `-text`) to extract that same specific field robustly (careful not to match a similarly-named but different field), compare it to the cracked plaintext, print `EVIL`/exit 1 on match or `CLEAN`/exit 0 otherwise, and `chmod +x` it. Validate your script against a few certs you generate yourself with and without the backdoor field before finishing.