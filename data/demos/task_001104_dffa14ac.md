Since the binary is stripped and undocumented, first probe it empirically rather than guessing its interface: run `/app/module_checker` with no args and with `-h`/`--help` to read its usage output, then try invoking it with a plausible flag and a sample module name to observe the exact output format and exit behavior — this tells you the real CLI contract instead of assuming one. Build the log parser as a small state machine: scan lines for an `error[...]:` marker, and when found, look at the *next* line for a `--> src/<name>.rs:...` path, extracting `<name>` as the failing module (use a regex like `--> src/(\w+)\.rs` rather than naive string splitting, since paths can have extra fields after the colon). For each extracted module, shell out to the binary with the module name as the argument you determined during probing, capture stdout, and map its status text to `"APPROVED"`/`"REJECTED"` in your response. Implement the server (Flask/http.server both fine) with a `POST /analyze` route that reads the raw text body, runs the parser, invokes the binary per module, and returns the JSON schema exactly as specified. Start it in the background bound to `127.0.0.1:8080` and confirm it accepts a connection before finishing.