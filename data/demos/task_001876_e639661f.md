Diagnose the ABI issue first: run `nm -D libratelimit.so` (or `objdump -T`) after an initial build attempt and note that the exported symbol for `allow_request` is mangled (e.g. `_Z13allow_requestjj`) because the C++ source lacks `extern "C"` — wrap the function declaration/definition in `extern "C" { ... }` in the `.cpp`/header so ctypes can find it by its plain name, then rebuild and re-check with `nm` that the symbol now appears unmangled. Fix the Makefile only if it also has real build errors; otherwise focus on the source. In `/home/user/test_api.py`, load the library with `ctypes.CDLL(path)`, set `.argtypes`/`.restype` for `allow_request` explicitly to match `(uint32, uint32) -> int`. Parse `/home/user/requests.txt` line by line: validate strictly against `CLIENT_ID:PAYLOAD` where CLIENT_ID must be all-digits and positive (use a regex like `^\d+:.*$`), silently skip non-matching lines. For valid lines, call `allow_request(client_id, 2)` and log `ACCEPTED: <line>` or `REJECTED: <line>` to `/home/user/api_results.log`, stripping only the trailing newline from the original line, preserving everything else verbatim. Remember the shared library keeps per-client state across calls within the same process run, so process all lines in one script invocation, in file order.
