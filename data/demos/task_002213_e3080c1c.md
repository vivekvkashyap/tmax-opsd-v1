For intrusion detection, grep/awk over `/home/user/auth.log` for lines matching `Failed password for (root|admin) from <ip>`, extract the IP with a regex, tally counts per (user-class, ip) — actually per IP across both usernames since the requirement is "more than 3 failed attempts for root or admin combined per IP" — then filter IPs with count > 3, dedupe, sort ascending (numeric-aware sort like `sort -t. -k1,1n -k2,2n -k3,3n -k4,4n` or just careful string sort if that suffices), and write one per line. For key permissions, iterate `/home/user/.ssh_mock/*.key`, check each file's current mode (`stat -c %a`) before changing it — only log the filename (basename, not path) if its mode was not already exactly `600`; then `chmod 600` all of them regardless, sort the logged names alphabetically. For the sshd config, copy the file and use targeted line replacement (sed on lines matching `^PermitRootLogin` and `^PasswordAuthentication`, or a stricter no-regex-injection edit) to set those two directives to `no`, leaving every other line byte-identical, and write the result to the hardened output path.
