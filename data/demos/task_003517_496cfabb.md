Write the Rust parser using only `std::fs`/`std::io` (BufReader over lines) — for each line, match the pattern `[TIMESTAMP] LEVEL - METRIC_NAME: METRIC_VALUE` with simple string splitting (find `]`, split on ` - `, then split the remainder on `: `) rather than a regex crate, since no external dependencies are allowed; only emit a CSV row when METRIC_NAME is exactly `memory_utilization`, appending (not overwriting) to the output CSV so repeated runs accumulate correctly. Compile with `rustc parser.rs -o parser` and test it against a small handmade log first. For logrotate.conf, write the block for `/home/user/telemetry.log` with `size 100k`, `rotate 4`, `compress`, `copytruncate`, and deliberately omit any `su` line (since there's no root access, an `su` directive naming a user would cause logrotate to fail without privileges). In process_metrics.sh, call the parser binary by absolute path, then invoke `logrotate -s /home/user/logrotate.state /home/user/logrotate.conf` exactly as specified (a local, non-default state file is required since you can't write to the system default under `/var/lib`). Finally add the cron line with `(crontab -l 2>/dev/null; echo '*/5 * * * * /home/user/process_metrics.sh') | crontab -`, then verify with `crontab -l`.
