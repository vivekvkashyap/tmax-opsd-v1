Build the pipeline in stages and sanity-check each before trusting the final number. First compile the given C source exactly as-is with `gcc -O2 -o /home/user/bin/wav_extract /home/user/src/wav_extract.c` (create `/home/user/bin` first) — note it skips a fixed 44-byte header, so it only works correctly on standard-format WAV files, which is fine here. Run it on `/app/signal.wav` and spot-check a few printed values look like plausible normalized samples in [-1, 1]. Then in your `analyze_noise.sh` script, pipe those samples into a Python one-liner/heredoc that loads them into a numpy array, generates an ideal Gaussian reference sample with `numpy.random.normal(0, 0.1, size=N)` where N matches the number of extracted samples (matching sample count matters for a stable Wasserstein estimate), and computes `scipy.stats.wasserstein_distance(empirical, ideal)`. Write only the resulting float to `/home/user/distance.txt`, no extra text. Since the ideal distribution is randomly sampled, use a fixed/seeded RNG within your script so reruns are stable, and verify by rerunning the script twice to confirm the output stays within a small tolerance rather than fluctuating wildly.