Reverse-engineer the binary format empirically before writing any parsing code. Run `/app/audit_compiler <name> <content>` several times with varying, controlled inputs (short/long filenames, short/long content, filenames with special chars) and diff the byte-level outputs (`xxd`) to isolate: a fixed magic-byte prefix, a fixed-size checksum/hash block whose bytes change unpredictably with content (candidate: hash of the payload — test MD5/SHA1 by computing it yourself over candidate payload byte ranges and comparing), and an encoded payload region. For the payload, try simple reversible transforms (single-byte XOR, byte rotation) against known plaintext you supplied — XOR your known filename/content bytes against the corresponding output bytes to recover a repeating key. Once decoded, inspect the leading bytes for a length-prefixed filename field (check both 1-byte and 2-byte, little vs big endian, by varying filename length near powers of two and seeing where a length byte changes). Only after the format is fully confirmed against several test vectors should you write `/home/user/analyze.py` to batch-decode `/home/user/audit_data/*`, verify the checksum before trusting a file, and flag decoded filenames containing `../` or a leading `/` as malicious in `/home/user/report.csv`.
