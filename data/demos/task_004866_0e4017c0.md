Treat the two detection criteria as independent passes over the data, then union and dedupe the resulting IP sets before writing the script. For Criterion A, iterate every `<domain>.pem` file in `/home/user/certs/` (skip `ca.pem` itself) and run `openssl verify -CAfile /home/user/certs/ca.pem /home/user/certs/<domain>.pem`; a non-zero exit or "verify failure" in output marks that domain as having a bad certificate — then grep `/home/user/traffic.log` for lines whose domain field matches that bad domain and collect the source IPs. For Criterion B, scan the `[Request_URI]` field of every log line for the three literal substrings `../`, `UNION+SELECT`, and `%3Cscript%3E` (simple substring/grep match is sufficient, no need to URL-decode first since the patterns are given in their log-encoded form) and collect those source IPs too. Union both IP sets, dedupe, and sort the resulting list alphabetically as strings (not numerically by octet — plain lexicographic `sort` matches what's required). Emit `/home/user/firewall_block.sh` starting with `#!/bin/bash` followed by one `iptables -A INPUT -s <IP> -j DROP` line per sorted IP, and `chmod +x` it without executing it.