Start by running `tesseract /app/schema_clue.png -` (or similar OCR invocation) to extract the text clue about the correct join — do not guess the schema, read it from the OCR output. The clue will point you toward a composite join key (i.e., joining on more than just a single id column) that the current buggy query in compute_metrics.sh is missing, which causes duplicate row multiplication (a fan-out/cross-join effect) when a node participates in multiple groups. Inspect the nodes/edges tables' columns with `sqlite3 /home/user/graph.db ".schema"` to see exactly what composite key is available, then rewrite the query to LEFT JOIN on all matching key columns together (not just one), GROUP BY node id, ORDER BY the count descending with node_id ascending as tiebreaker, and LIMIT 3. Modify the script to emit valid JSON (array of `{"node_id":..., "centrality":...}`) to /home/user/top_nodes.json instead of printing to console — you can format this with `sqlite3 -json` or by piping results through a small awk/jq transform. Finally write serve.sh using only nc/socat in a loop to accept connections on port 8080, respond to GET /api/top_nodes with a proper HTTP/1.1 200 response including a Content-Type: application/json header and the exact file contents as the body, run it backgrounded (&) so it stays alive for verification.
