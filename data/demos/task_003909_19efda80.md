Work through the chain in dependency order: reverse-engineer first, then build the C tool. Use `strings`, `objdump -d`, or `gdb` on `/home/user/incident/beacon.elf` to locate a hardcoded 16-byte key near crypto-initialization code — look for a printable-looking 16-character constant near any function referencing crypto/encryption, since malware authors often leave it as a readable string in a local buffer. Once you have the key, write `/home/user/decryptor.c` using OpenSSL's X509 APIs (e.g. `X509_STORE`, `X509_STORE_CTX_init`, `X509_verify_cert`) to build a trust chain from `ca.crt`, add `intermediate.crt`, and verify `leaf.crt` against it, printing `CERT_CHAIN_VALID` only on success — do this in C, not by shelling out to `openssl verify`. Only after that succeeds, use OpenSSL's EVP AES-128-CBC decryption routines with the extracted key and the given IV (hex `0102030405060708090a0b0c0d0e0f10`) to decrypt `payload.enc`, handling PKCS#7 padding removal correctly. Parse the decrypted text for the `C2_SERVER=<value>` pattern and write just the IP address to `/home/user/c2_indicator.txt` with a single trailing newline and no extra whitespace. Compile with `-lcrypto -lssl` and test each stage's output before chaining them.