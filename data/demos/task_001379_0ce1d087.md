Break this into independently testable pieces. In `analyzer.py`, first load all JSON files from `projects/` into a dict keyed by module name, then compute W(m) via memoized recursion (since it's a DAG, recurse into each dependency's W before adding `0.5*W(d)`, caching results to avoid recomputation) — a naive iterative pass in file order will produce wrong values if a module's dependency appears later in an unrelated file order, so always resolve recursively/topologically rather than assuming input order. Serialize the resulting dict to `migration_order.json` with `json.dump`. For the web service, add a lightweight HTTP handler (`http.server.BaseHTTPRequestHandler` or Flask) that on `GET /weights` reads and returns that same JSON file with `Content-Type: application/json`, then launch it detached (`nohup python3 analyzer.py &` or similar) so it survives after your shell command returns. For Nginx, write a full config with `pid`, `access_log`, `error_log`, and a `client_body_temp_path`/`proxy_temp_path` etc. all pointed inside `/home/user/workspace/nginx/` (create that directory first), a `server` block on `127.0.0.1:8080` with `location /api/weights { proxy_pass http://127.0.0.1:8000/weights; }`, then start it with `nginx -c /home/user/workspace/nginx.conf` (no root needed since it's all local paths and a high port). Verify end-to-end with `curl http://127.0.0.1:8080/api/weights` before writing tests, and capture pytest's terminal output into `test_results.log` with `pytest ... | tee test_results.log` or `> test_results.log 2>&1`.
