Build and verify each layer in isolation before chaining through Nginx. TLS: `openssl req -x509 -newkey rsa:2048 -nodes -keyout /app/certs/key.pem -out /app/certs/cert.pem -days 365 -subj '/CN=localhost'`, matching paths nginx.conf expects. Proxy (8080 -> backend 5000): derive WAF rules empirically, not by guessing — read every file in `/app/corpus/evil/` and `/app/corpus/clean/` to see the actual payload strings, then pick substring/pattern checks (case-insensitive script tags, boolean-injection idioms, UNION SELECT, etc.) that classify every evil file as 403 and every clean file as forwarded, testing directly against the corpus until 100%. For redaction, regex-match 16-digit dash-grouped card numbers in the backend's JSON body and replace with `[REDACTED]`. For auth, compute SHA256 of the given operation string yourself (e.g. `echo -n <string> | sha256sum`) to know the expected header value, and reject mismatches with 401. Write `/home/user/run_all.sh` to background-launch Flask, then the proxy, then nginx, and test the full chain with `curl -k` against port 8443 using the corpus before finishing.
