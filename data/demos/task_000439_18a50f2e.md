Use Python's csv module (not manual line splitting) to correctly handle the embedded newlines inside quoted raw_config fields — this is the whole point of the task, and naive `.split('\n')` or `readlines()` on the file will misparse rows. Read the file with `csv.DictReader`, then for each row normalize raw_config by lowercasing the entire string and stripping all whitespace characters — use a regex like `re.sub(r'\s+', '', s.lower())` rather than just `.strip()`, since whitespace can appear in the middle of the string (spaces around `=`) as well as at the edges. Hash the normalized string with `hashlib.sha256(s.encode()).hexdigest()`. Build a dict mapping hash to a list of server_ids by appending as you iterate, then sort each list alphabetically before writing out. Ensure the output directory exists (`os.makedirs(..., exist_ok=True)`) before writing the JSON with `json.dump`. Double check by re-deriving the normalized string and hash for a couple of rows by hand/print-debugging to confirm your normalization logic actually collapses the intended duplicates together (values that differ only in case, spacing, or embedded newlines around `=` should hash identically).
