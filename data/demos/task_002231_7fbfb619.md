In `/home/user/harden_vms.py`, first list `*.sh` files in `/home/user/qemu_configs` (`glob` or `os.listdir`), then for each file read its contents and check specifically for `password=off` occurring as part of a `-vnc` argument (a simple targeted string/regex replace of `password=off` -> `password=on` is fine here since it's only meant to appear in that context — but confirm by inspecting the sample files first rather than assuming). Only rewrite the file and record its basename in `vnc_fixed` if a replacement actually happened (compare before/after content, don't just check the string exists). Separately check world-writable permission using `os.stat(path).st_mode` and the `stat.S_IWOTH` bit; if set, `os.chmod(path, 0o750)` and record the basename in `permissions_fixed` — do these two checks independently per file since a file can trigger one, both, or neither. Sort both lists alphabetically before writing the final `hardening_report.json` with `json.dump`. Run the script and diff the resulting file contents/permissions against your expectations (`vm_db.sh` should remain fully untouched) before finishing.
