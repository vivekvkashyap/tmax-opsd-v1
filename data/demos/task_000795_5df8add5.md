First locate and undo the perturbation before writing any pipeline code. Open `/app/pyod-1.0.9/pyod/models/pca.py` and search the `fit` method for the `decision_scores_` computation — look for a suspicious multiplier or zeroing operation appended to the line that computes scores via `cdist` between the data and the principal components; remove exactly that perturbing modification so the score calculation returns to its natural `np.sum(cdist(...) ** 2, axis=1)` form, without altering unrelated logic. Then `pip install -e /app/pyod-1.0.9` (editable install so your fix is picked up) and sanity-check by fitting the PCA detector on a small synthetic array with an obvious outlier and confirming `decision_scores_` are nonzero and the outlier gets `labels_ == 1`. For the cleaning script, read the whole stdin as one JSON blob, convert to a float64 numpy array treating `null`→`NaN`, impute NaNs with per-column median via `np.nanmedian` (fully-NaN columns → 0.0, watch for this edge case), fit `PCA(n_components=2, contamination=0.1, random_state=42)` on the imputed matrix, keep rows where `labels_ == 0`, and print only the compact JSON result — no extra prints, no trailing debug output, since the check is bit-exact stdout comparison against the oracle.
