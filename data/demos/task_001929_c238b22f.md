Start by recovering the spoken instructions from the audio, since the graph pattern to detect isn't stated in the text task — transcribe /app/voicemail.wav (e.g. install and run `openai-whisper`, or another available speech-to-text tool) and read the transcript carefully to identify the exact structural pattern described (a specific cycle length among accounts, not a vague "suspicious loop"). Once you know the pattern, implement analyze_graph.sh as a thin wrapper around a fast structural query rather than hand-rolled bash graph traversal — loading the CSV into `sqlite3 :memory:` (or `duckdb`) and doing a self-join of the transactions table against itself the right number of times (joining target_account to source_account at each hop, and requiring the final hop's target to equal the very first source) is far more robust and fast than ad hoc awk logic, and matters because the script will be run against a large hidden corpus for both correctness and speed. Exit 1 if any qualifying row is found, exit 0 otherwise. Test your script against a few small hand-built CSVs — one containing the described pattern, one without it, one with unrelated cycles of a different length — before trusting it on the hidden corpus, and make sure it strictly matches the given header format and is executable.