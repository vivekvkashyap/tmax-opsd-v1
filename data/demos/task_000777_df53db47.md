Start by inspecting `/home/user/old_config.json` to identify the hash algorithm field and hash value (a weak/fast algorithm like MD5 is the CWE-328 red flag here). Write a small Python script that reads `/home/user/wordlist.txt` line by line, hashes each candidate with the identified algorithm, and compares against the stored hash to recover the plaintext password — don't hardcode a guessed password, actually crack it programmatically so it's robust regardless of which word matches. Separately compute the SHA256 of the wordlist file itself (`hashlib.sha256(open(path,'rb').read()).hexdigest()`) for the integrity check. In `/home/user/rotate.py`, generate a fresh 16-byte random salt with `secrets.token_bytes(16)` (or `os.urandom`), hex-encode it to get exactly 32 hex characters, and compute `hashlib.sha256((salt_hex + password).encode()).hexdigest()` — note the concatenation order is salt-then-password as string concatenation, not password-then-salt. Assemble the final JSON with the recovered password's length (as an integer, not the password itself), the new hash algorithm name, the salt, the password hash, and the wordlist checksum, writing it to `/home/user/new_config.json` without touching the original files.