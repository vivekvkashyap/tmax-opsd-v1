Build the `.plug` file byte-by-byte in a Python script using `struct.pack`, not by guessing offsets manually. Write fields in order: 4-byte magic `b'PLUG'`; then `struct.pack('<I', len(cert_bytes))` for the certificate length; then the certificate bytes themselves; then `struct.pack('<H', len(fname_bytes))` for the filename length; then the filename bytes; then the payload. Generate the self-signed certificate with the `cryptography` library (create an `rsa` private key, build an `x509.CertificateBuilder` with matching issuer/subject, sign it, and serialize with `.public_bytes(serialization.Encoding.PEM)`) — a naive placeholder string will fail the PEM parse check. For the filename, use the exact literal path given in the spec (do not re-derive it). For the payload, start with the 4 raw ELF magic bytes `b'\x7fELF'` and then embed the required marker string as bytes anywhere after that — the rest of the payload can be arbitrary filler. Concatenate all the parts and write to `/home/user/evasion.plug`, then run the script and manually re-parse the file with the same offset logic to sanity-check before finishing.
