Build the pipeline as a single idempotent bash script that mkdir -p's its output directories first, then cleans the CSV with a stream tool — an `awk -F,` one-liner is cleanest: always print the header line, and for data rows only print if the temperature field is non-empty AND numerically greater than 0 (use `NF>=3 && $3!="" && $3+0>0`, being careful that awk's implicit numeric conversion of an empty string is 0, which would wrongly fail the >0 test in some logics but here also correctly excludes it — verify your exact condition against the header-preserving requirement). Redirect the cleaned rows to `/home/user/clean_data/cleaned_sensors.csv`. Then have the bash script emit a short inline Python script (via heredoc or a separate .py file) that loads the cleaned CSV with pandas or csv, splits temperature values by `rack` column into two groups, and calls `scipy.stats.ttest_ind(groupA, groupB)` to get the p-value; format it with Python's `round(p, 4)` or an f-string `%.4f` (round-half-to-even edge cases matter less here, just match Python's own rounding) and write it as a single line to `/home/user/results/p_value.txt`. Make the script executable with `chmod +x` and actually run it once to produce the file before finishing.