Start with the index: create it on (sender, receiver) or similar to speed up the traversal lookups, and confirm with `.indices transfers` in the sqlite3 CLI. For the path search itself, use a recursive CTE (or equivalent recursive traversal in your chosen language) that starts from C-100, at each step joins to transfers where sender = current path's last account, extends the route, accumulates a running total, and stops extending a branch the moment any single transfer's amount is below the 50.0 threshold — filter at each hop, not just at the end, since a path that has one bad transfer must be discarded entirely even if later transfers are fine. Cap recursion depth at 4 hops, then filter completed paths to those ending exactly at C-999. Sort the surviving paths by total_amount descending and keep only the top 5. Serialize to /home/user/audit_report.json matching the given JSON schema exactly (top-level "paths" array, each item with "route" as an ordered account array and "total_amount" as a number). Validate your own output against the schema before finishing.