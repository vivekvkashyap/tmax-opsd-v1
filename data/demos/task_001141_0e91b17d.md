Start by port-scanning localhost (e.g. `nc -zv 127.0.0.1 1-65535` or `ss -tlnp`) to find the WAF gateway and backend ports, then read `/home/user/waf/waf.py` carefully to extract the exact regex it uses to detect `alg=none` in the base64url-decoded JWT header. The key insight: the WAF's regex is a syntactic pattern match on the raw decoded bytes (likely something like `"alg"\s*:\s*"none"`), while the backend parses the header with a real JSON parser — so any JSON-legal way of spelling the same key/value that the regex doesn't recognize will pass through. Consider JSON string escapes (e.g. representing one letter of `"alg"` or `"none"` as a `\uXXXX` unicode escape) so the literal decoded text no longer matches the regex verbatim but `JSON.parse` still yields `alg: "none"`. Build the header/payload JSON, base64url-encode each part (no padding), join header.payload. with an empty signature, and send it as `Authorization: Bearer <token>` to the WAF's `/api/admin/flag` route — do not touch the WAF or backend source or restart anything. Save whatever text comes back as the flag verbatim to `/home/user/flag.txt`.