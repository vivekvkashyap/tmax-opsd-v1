Break this into three independently verifiable pieces and check each before moving on. (1) Persist the env var by appending an `export DEPLOY_ENV=production` line to `/home/user/.bashrc` (a grep for that line is how it'll be checked — don't rely on setting it only in the current shell, since that doesn't survive to cron). (2) Write `/home/user/orchestrator.py` so its very first action reads `os.environ.get("DEPLOY_ENV")` and calls `sys.exit(0)` immediately if it's not exactly `"production"` — this must happen before creating any directories or touching the PID file, since the verifier checks that no PID file is written under a wrong env. Then ensure `/home/user/run/` exists, glob `*.py` under `/home/user/services/`, and for each one check the PID file: parse existing `name:PID` lines, use `os.kill(pid, 0)` (catching `ProcessLookupError`) to test liveness, and only launch (`subprocess.Popen(["/usr/bin/python3", path])`) and rewrite/append the PID file entry when the service is missing or dead — don't blindly relaunch running services. `chmod +x` the script. (3) Install a crontab line with `crontab -e`/`(crontab -l; echo "...") | crontab -` using `*/5 * * * *` and absolute paths to both python3 and the script.
