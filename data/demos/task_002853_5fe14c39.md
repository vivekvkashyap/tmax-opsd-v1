Work in the given order since later steps depend on earlier ones. First run `tesseract /app/calibration.png stdout` (try different `--psm` values if the raw output is garbled) and read off the labeled `ACCESS_TOKEN` and `BASE_MULTIPLIER` values from the extracted text — don't guess these, they must come from the OCR output. Second, fix the Makefile in `/app/legacy_calc`: ensure recipe lines use real tab characters (not spaces — a classic Makefile syntax break), add `-fPIC` to the compile flags, and use `-shared -o libmatrix_ops.so` so `make` produces a proper shared object; run `make` and confirm the `.so` exists before moving on. Third, write `/home/user/server.py` using only `http.server`/`json`/`ctypes` from the standard library: parse the `Authorization: Bearer <token>` header and compare it (constant-value check is fine) against the token you recovered, returning 401 on mismatch/missing; on success, load the `.so` via `ctypes.CDLL`, set `restype`/`argtypes` to `c_double` for `compute_calibrated`, call it with the request's `value` and the recovered multiplier, and return `{"status": "ok", "result": ...}` as JSON with status 200. Start the server on exactly `127.0.0.1:8000` and test it with a real `curl` POST before finishing.