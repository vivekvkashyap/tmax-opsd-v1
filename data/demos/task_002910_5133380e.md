Build this bottom-up: first get frame brightness extraction exactly right, since the C++ detector depends on it. Use `ffmpeg -i /app/video/sensor_feed.mp4 -vf fps=1` to pull one frame per second in order, then compute the mean grayscale intensity of each frame yourself (e.g. convert to grayscale and average pixel values with a small script or ImageMagick's `identify -format "%[fx:mean*255]"`), writing `frame_id,intensity` to the CSV with frame_id starting at 1 in extraction order — verify row count matches the video's frame count. Then write detector.cpp to: load the CSV into an in-memory map (frame_id -> intensity), parse the single log line's tokens by prefix (`FRAME_`, `TEMP_`, `VOLT_`), and apply the three EVIL conditions in order — any `NA` token, VOLT outside [0.0, 24.0], or |TEMP - 0.75*intensity| > 25.0 (treating a missing frame_id in the CSV as automatically anomalous); print exactly `CLEAN` or `EVIL` with a trailing newline and nothing else. Compile with g++ to the exact `/home/user/detector` path and chmod +x it, then hand-test against a few clean/evil examples you construct from the task's own rule descriptions before trusting it against the hidden corpus.