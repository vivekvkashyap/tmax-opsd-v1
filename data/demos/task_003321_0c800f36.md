First recover the target node ID by running `tesseract /app/target_node.png stdout` (try a few `--psm` values if the first pass is noisy) and trim/clean the OCR output rather than assuming a value — verify it looks like a well-formed node ID before hardcoding it into your server. Parse `/app/network.csv` (src,dst columns) into an in-memory adjacency list (HashMap<String, Vec<String>>). Implement `/reachable?depth=X` as a bounded BFS from the extracted target node: maintain a visited set seeded with the target at depth 0, and at each of X steps expand the frontier along directed edges only, adding newly reached nodes to the visited set (depth 0 returns just the target itself, not its neighbors). Return the full visited set (not just the last frontier) as a JSON array of strings, sorted lexicographically before serializing. Scaffold with `cargo new` and use a lightweight framework like `axum` or `actix-web` bound to `127.0.0.1:8080`, and launch it detached/backgrounded (e.g. `cargo run &` or build a release binary and run it with `&`) so it stays up for the verifier to query after your turn ends.
