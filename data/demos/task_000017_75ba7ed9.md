First extract the prior mean and variance from the audio: transcribe `/app/experiment_prior.wav` (e.g. with `whisper` or another local speech-to-text tool, or careful manual listening if you can play it) and parse the spoken numbers into the two constants the formula needs — don't guess or hardcode without confirming the transcript. Since your script's stdout must match a hidden oracle bit-for-bit, treat this as an equivalence problem: implement the exact conjugate-normal posterior-mean update using the prior mean/variance you extracted and the fixed observation variance of 0.01, then `round(x, 4)` in Python specifically (not manual string formatting, which can round differently at the boundary). For the similarity search, maintain a list of previously seen `(score, loss)` records in arrival order; for each new record scan the full history computing Euclidean distance, track the minimum with strict `<` comparison (so on ties the earliest-seen record wins, since it was already the incumbent), and only append the current record to history after computing distances against it. Read input line-by-line from stdin with `for line in sys.stdin`, parse each as its own JSON object, and print one JSON output line per input line immediately (flush or let the loop naturally flush) so it behaves correctly when piped. Test by hand against a few small synthetic sequences you construct yourself, checking tie-breaking and first-record ("NONE") behavior explicitly, since these edge cases are what a naive implementation gets wrong.
