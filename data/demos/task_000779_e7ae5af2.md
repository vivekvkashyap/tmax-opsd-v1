Start with the audio: transcribe `/app/compliance_audio.wav` (e.g. via `whisper`/`speech_recognition` or any available ASR tool) and carefully parse out three tuples of (server name, last-4-chars of a cert fingerprint, CWE id) — listen/re-check ambiguous spoken digits/letters (like "E4F1") since transcription errors there will cascade into wrong mappings. For certificate mapping, compute SHA-256 fingerprints of every file in `/app/certs/` (e.g. `openssl x509 -noout -fingerprint -sha256 -in certN.crt`) and match each to the server whose audio-dictated suffix matches the fingerprint's last 4 hex characters — do this programmatically, not by assumption, since the directory names under `/app/code/` are cert-named, not server-named, and you need the same mapping to look up the right code snippet. Validate each matched cert's chain against `/app/certs/rootCA.pem` with `openssl verify -CAfile /app/certs/rootCA.pem certN.crt`, recording true/false per server. For each mapped code directory, actually read the source and confirm the vulnerability class (XSS/SQLi/path traversal, etc.) is present and matches the audio's stated CWE id before trusting it. Assemble the final report keyed by the transcribed server names into `/home/user/audit_report.json` matching the exact required schema.