Start by reproducing the bug before touching code: build a tiny two-node, one-edge test case, run it through the vendored engine, and observe the output blow up or duplicate — this confirms where to look. Since the symptom is an implicit cross join during parent-child evaluation, search `/app/sql-graph-emulator` for a function pairing two lists of edges/nodes (grep for nested comprehensions/loops joining two collections) and check whether it filters on the matching key (child id == parent id) or just enumerates every combination — a join missing its `if` condition is a classic Cartesian-product bug; restore the equality filter. Install the fixed package into your Python environment (`pip install -e /app/sql-graph-emulator` or adjust `PYTHONPATH`) so `/home/user/solve.py` imports the patched version, not a cached broken one. Implement the recursive sum with memoized DFS over parent→children adjacency (each node's total = its value + sum of children's totals), output strict CSV `id,total_value` sorted by ascending id, printing nothing else to stdout. Verify against a hand-built small graph you can check by hand before trusting it on larger fuzz inputs.
