Rewrite `deploy.py` as a sequence of hardened steps rather than patching the old flow in place. Load the Fernet key by reading raw bytes from `/home/user/.secret_key` (never from `sys.argv`), and decrypt `/home/user/config.enc` with it. Immediately after decryption, compute `hashlib.sha256(plaintext_bytes).hexdigest()` and compare it exactly (case-sensitive, stripped of whitespace) against the contents of `/home/user/config.sha256`; on any mismatch, `sys.exit(1)` before writing any report — integrity must gate report generation. For the certificate, load `/home/user/server.crt` with the `cryptography` library (`x509.load_pem_x509_certificate`), then compute the SHA-256 digest of the certificate's DER bytes (`cert.fingerprint(hashes.SHA256())`) and format it as lowercase colon-separated hex pairs — this must equal what an independent OpenSSL-style fingerprint check would produce. Build the HTML report with the CSP meta tag copied verbatim into the `<head>`, and a body containing the three required lines (`Integrity: OK`, `Cert Fingerprint: ...`, `Config: ...`) each on its own line or separated by `<br>`. Run the script with no arguments and confirm `/home/user/secure_report.html` is produced with exit code 0.
