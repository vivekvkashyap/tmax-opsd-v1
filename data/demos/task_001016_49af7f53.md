Before optimizing, run `/app/legacy_calc` once to capture its exact output column order, ranking tie-break behavior, and header/no-header format — your CSV must match it byte-for-byte since correctness is checked via exact diff, not just approximate values. The performance win comes from moving the whole computation into SQLite itself rather than looping in C: first execute `CREATE INDEX idx_parent ON edges(parent_id);` so parent→child lookups are O(log n) instead of full scans. Then write a single SQL query using a `WITH RECURSIVE` CTE that walks from every node down through `edges` to accumulate total descendant weight per node (careful: recursion needs to start from every node as a root, not just true roots — union each node with its subtree contributions). Wrap that in an outer query using `RANK() OVER (PARTITION BY category_group ORDER BY total_weight DESC)` and filter `WHERE rank <= 3`. Execute this via the SQLite3 C API (`sqlite3_open`, `sqlite3_prepare_v2`, `sqlite3_step`), write results directly to `/home/user/results.csv`, and time both binaries end-to-end to confirm the speedup target.
