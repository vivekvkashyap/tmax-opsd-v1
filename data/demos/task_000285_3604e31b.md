Build the directories and the symlink first (`ln -s /home/user/actual_configs/current.conf /home/user/config/service.conf`), then write the config file with exactly the two required lines. For the C++ supervisor, parse the config with simple line-based string splitting on `=` to get `HEARTBEAT_FILE` and `TIMEOUT` (as an int). The core loop must run exactly 5 iterations, sleeping 1 second at the START of each iteration (not the end) before checking the heartbeat file's mtime via `stat()`/`std::filesystem::last_write_time`. Compare `current_time - mtime` against `TIMEOUT` using strict `>` (not `>=`). When stale, append the alert line to the log with `std::ofstream` opened in append mode (`std::ios::app`), and update the heartbeat file's mtime to "now" using `utime()`/`utimensat()` with a null/current timespec, not by truncating and rewriting the file (which would also work but stat carefully that you're only touching mtime, not deleting content). Trace through the timing by hand before running: the file is created, then the shell sleeps 3s before the supervisor starts, so the file's age going into each 1s-sleep iteration accumulates — work out exactly which iterations cross the TIMEOUT threshold rather than guessing, since off-by-one sleep placement changes the alert count.