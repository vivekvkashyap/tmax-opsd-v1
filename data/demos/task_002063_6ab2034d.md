Split this into two independent parts. First, get the calibration integer F: use `ffprobe -v error -count_frames -select_streams v:0 -show_entries stream=nb_read_frames -of default=nokey=1:noprint_wrappers=1 /app/dashboard.mp4` to get the exact frame count (don't estimate from duration*fps, since containers can be inexact — always count frames directly). Second, write the C filter with `sscanf(line, "%d,%d,%d,%d", ...)` per row after skipping the header line with a single `fgets`. Maintain a fixed `int hash[1024]` array initialized to -1 (a sentinel that can never equal a real time value if inputs are non-negative, but time could theoretically be 0 so use -1 specifically as unoccupied marker); on each row check `hash[t % 1024] == t` to drop dupes, else overwrite. Keep `last_s1/s2/s3` state variables for forward-fill, applied before the XOR step — order matters: impute first, then XOR only values that ended up non -1. Emit three CSV lines per surviving row. Compile with plain `gcc`, no extra libs, and test against your own hand-built edge cases (leading -1 with no prior value, hash collisions between different times) before trusting it.