Fix the build first: `cargo build` fails because `build.rs` never emits the `cargo:rustc-link-lib=static=tracker` directive needed to actually link `libtracker.a` against the `extern "C"` declarations in `src/main.rs` — add that line (in addition to the existing `link-search` directive) and confirm `cargo build` succeeds before touching the logs. For isolating the crashing line, don't read all 500 lines manually — use delta debugging: run `cargo run -- traffic.log`, then bisect by running the service against truncated/halved copies of the log (binary search on line ranges, e.g. with `head -n`/`sed -n` to produce candidate sub-logs) to find exactly which single line makes it crash, and write that exact line verbatim to `/home/user/poison_line.txt`. For the leaked session ID, don't guess the format — after the crash produces `heap.raw`, search the raw bytes for the literal marker `LEAKED_SESSION_ID:` (e.g. `strings heap.raw | grep LEAKED_SESSION_ID` or a Python byte-scan), then extract only the alphanumeric run immediately following that marker up to the next non-alphanumeric byte, and write just that substring (no prefix, no trailing garbage) to `/home/user/leak_id.txt`.
