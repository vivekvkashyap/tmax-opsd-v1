First diagnose why the Makefile fails: try running `make` in `/home/user/project/` and read the compiler error — a shared library needs position-independent code and the shared-object flag, so the fix is adding `-shared -fPIC` (and `-o libeval.so`) to the gcc invocation compiling `src/eval.c`. Verify with `file libeval.so` that it reports a shared object, and sanity-check symbol export with `nm -D libeval.so | grep evaluate`. For `processor.py`, load the library with `ctypes.CDLL('/home/user/project/libeval.so')`, then explicitly set `lib.evaluate.argtypes = [ctypes.c_char_p, ctypes.c_int, ctypes.c_int]` and `lib.evaluate.restype = ctypes.c_int` — skipping this is the common bug since ctypes defaults to assuming int args/return, which happens to work here but leaves the string arg unmarshalled correctly only if you encode it to bytes (`op.encode()`). Parse each `data.txt` line by splitting on whitespace into op/arg1/arg2, call `evaluate`, and format each output line exactly as `Line [N]: [OP] [ARG1] [ARG2] = [RESULT]` with 1-based line numbers, writing to `/home/user/project/output.log`. Run the script and diff the output against the given example logic to confirm correctness before finishing.
