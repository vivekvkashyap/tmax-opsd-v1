Follow the statistical spec exactly and in the exact order given, since bootstrap results are highly sensitive to RNG call order. Load `/home/user/transactions.csv` with pandas, filter to rows where `amount` is not null and `amount >= 0` (note: "strictly less than 0" is excluded, so 0 itself stays), and compute the plain mean of the cleaned column. For the bootstrap CI, call `numpy.random.seed(42)` exactly once immediately before generating the resamples — do not call any other random function first, and do not reseed per-iteration — then draw 10,000 resamples (each the same size as the cleaned data, with replacement, e.g. via `np.random.choice(cleaned, size=(10000, n), replace=True)` in one vectorized call to keep the RNG stream deterministic), take the mean of each resample row, and use `numpy.percentile` at 2.5 and 97.5 for the CI bounds. Run `scipy.stats.ttest_1samp(cleaned, 150.0)` for the t-test. Round every float to exactly 4 decimal places with Python's `round()` before dumping to JSON at `/home/user/dq_metrics.json`, matching the exact key names in the spec, then execute the script yourself to confirm it produces valid output.
