Work in three clean layers so failures are easy to isolate. First, run `tesseract /app/config.png stdout` (try a few `--psm` values if the first pass is garbled) and parse the `COEFF=<value>` line into a float — do not hardcode a guessed coefficient. Second, write `/home/user/mc_integral.c` as a straightforward Monte Carlo sampler: use `#pragma omp parallel` with each thread accumulating its own partial sum using `rand_r` seeded uniquely (e.g. seed + thread id, or combine with time) to avoid correlated/shared-state RNG bugs, reduce across threads, divide by total sample count, and print only the final float. Compile with `gcc -fopenmp -O2 -o /home/user/mc_integral mc_integral.c -lm`, and inside the program call `omp_set_num_threads()` from the `<num_threads>` argument. Third, build the HTTP server bound exactly to `127.0.0.1:8080` with a `/simulate` route that reads `samples`/`threads` query params, shells out to the compiled binary with those two args, captures stdout, and returns `{"result": <float>}` with `Content-Type: application/json`. Launch the server detached (e.g. `nohup ... &` or a background process manager) so it stays alive after your session ends — a server that dies when the shell exits will fail every check.
