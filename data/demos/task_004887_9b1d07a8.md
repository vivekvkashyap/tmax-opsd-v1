Tackle this in three independent pieces and verify each before wiring them together. (1) Systemd fix: edit `/home/user/.config/systemd/user/cost-aggregator.service`'s `[Unit]` section to add `After=billing-receiver.service` and `Requires=billing-receiver.service`, and its `[Service]` section to add `Restart=always` and `RestartSec=5`; then run `systemctl --user daemon-reload` and `systemctl --user restart` all three services, checking `systemctl --user status` for each to confirm active/running. (2) Filter script: write it to read stdin line-by-line, parse each line as JSON (use a real JSON parser, not naive string grep, since fields can be missing or malformed), and only pass through lines where `env == "prod"` AND `cost` is present, numeric, and `> 0` — treat missing/null/zero cost and any non-prod env as reject; make it executable with `chmod 700`. Test it directly against the given clean and evil corpora paths before integrating, since the pass criterion is 100% exact preservation/rejection. (3) Pipeline wiring: edit `run-aggregator.sh` to insert your filter between the `tail -F` and the `curl`/`send-to-billing` step in the pipe chain, preserving streaming behavior (avoid buffering that breaks `tail -F`'s live nature). (4) Write the exact `FILTER_CMD="..."` line to solution.env. Restart services after each config change and confirm the live end-to-end flow via `curl http://localhost:8080/metrics`.