Write secure_proxy.sh as a `while IFS= read -r line; do ... done` loop over stdin, handling each line in strict priority order: first check rate limiting, then classify the command. For rate limiting, track a window-start timestamp and a count using `date +%s`; when the current second differs from the stored window, reset the counter to 0 and update the window; increment the counter for every processed command (including PING and INVALID_REQ, per the spec) and if it would exceed 3 in the current second, emit `ERR: RATE_LIMIT` and skip further processing for that line — but still count it against the limit. Only after the rate-limit check passes do you interpret the command: exact-match `PING` prints `PONG`; a line starting with `EXEC ` needs its payload length computed via bash parameter expansion (`payload="${line#EXEC }"`, `${#payload}`) and compared strictly greater than 20 to trigger `ERR: MEM_SAFETY`, otherwise the full line is passed to `/home/user/legacy_backend "$line"` and its stdout echoed as-is; anything else prints `ERR: INVALID_REQ`. Test by feeding a crafted sequence of inputs through the script via stdin redirection and comparing output line-by-line, since all test invocations happening within the same second is the key edge case to get right. Remember `chmod +x`.