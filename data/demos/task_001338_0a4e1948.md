Work bottom-up: fix C++ first, then Go, then derive the key, then Python. In `analyzer.cpp`, look for an off-by-one loop bound (e.g. `i <= length` instead of `i < length`) causing an out-of-bounds read on the pixel buffer. In `main.go`, find the goroutine incrementing `BrightFrameCount++` without synchronization; fix with a `sync.Mutex` or `sync/atomic`, not by removing concurrency. Rebuild the Go binary and run it against `/app/test_run.mp4`; the printed count is your `calibration_key` — don't hardcode a guess, derive it. For the Python filter, port to Python 3 syntax, replace `yaml.load()` with `yaml.safe_load()` wrapped in try/except to skip malformed/unsafe/invalid-unicode files, and implement `is_clean_record` to check `signature == (event_id * calibration_key) % 99991`. Wire up `argparse` for `--input/--output/--key`, and verify end-to-end against a few hand-crafted clean and evil JSON files before finishing, since it's graded against hidden adversarial corpora requiring 100% separation.