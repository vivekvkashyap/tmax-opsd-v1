Load the CSV into an in-memory SQLite DB with Python's `sqlite3` and `csv` modules (create a table with columns `emp_id, name, manager_id, salary`, being careful that empty `manager_id` for the CEO becomes NULL rather than an empty string, since a recursive CTE's join condition depends on that). Write a `WITH RECURSIVE` CTE: the base case selects the row where `emp_id = ?` (the parameterized target), and the recursive case joins subordinates whose `manager_id` equals the previously found `emp_id`s in the growing set — always pass the target ID through `cursor.execute(query, (emp_id,))` with a `?` placeholder rather than string-formatting it into the SQL, since the task explicitly calls out injection safety. From the resulting employee set, aggregate `COUNT(*)` and `SUM(salary)` (either in SQL or in Python over the fetched rows). Write the three required keys (`manager_id`, `total_employees`, `total_salary`) as a JSON object with `manager_id` cast back to the integer type, and make sure the script prints nothing to stdout — only produces the file. Test the script against a couple of different manager IDs to sanity-check the hierarchy traversal covers multiple levels, not just direct reports.
