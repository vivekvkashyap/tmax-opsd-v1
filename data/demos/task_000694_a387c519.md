OCR `/app/reference_ui.png` with `tesseract`, then strip whitespace/newlines to isolate the uppercase-with-underscores identifier — do not assume its value, read it from the image output. Write a C++ program that opens `/app/loc_events.csv`, parses each line's `timestamp,ui_string,locale,views` (comma-split, `std::stoi`/`std::stol` for the epoch and views fields), filters rows whose `ui_string` equals the OCR'd identifier, converts each Unix timestamp to a UTC date string `YYYY-MM-DD` (use `gmtime`/`std::gmtime`, not `localtime`, to avoid timezone drift), and accumulates a `map<string,long>` of date -> summed views. Have the C++ program emit this map as JSON (either print directly with manual JSON formatting, or write to a file). Then bring up an HTTP listener on `127.0.0.1:8080` — simplest is a small C++ socket server or a thin wrapper (e.g. Python) that reads the C++-produced JSON file and serves it verbatim on GET `/metrics`. Run it in the background (`&` or nohup) and verify with `curl 127.0.0.1:8080/metrics` before finishing, since the process must remain alive for the automated check.