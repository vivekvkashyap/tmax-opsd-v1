This is a classic stack buffer-overflow exploitation exercise — approach it methodically. Read auth_checker.c carefully: `ctx.username` is a fixed 64-byte buffer inside a struct that also holds `is_admin`, and it's filled via unbounded `strcpy(ctx.username, argv[1])` with no bounds check, while the admin path is gated on `ctx.is_admin != 0`. Since C structs are typically laid out in declaration order and `is_admin` sits immediately after `username` in memory (and the binary was compiled with `-fno-stack-protector` so there's no canary to trip), overflowing username past 64 bytes should let you overwrite is_admin's bytes directly — but confirm the actual offset empirically (e.g. compile a small probe or use pattern/cyclic bytes to see exactly where the 4-byte nonzero value needs to land) rather than assuming exactly 64. Craft argv[1] as 64 filler bytes followed by 4 non-zero bytes (e.g. all 0x01) to set is_admin nonzero, avoiding the literal string "admin" as argv[1] since that's checked separately. In exploit.c, invoke the checker via execv/system with this crafted argv[1] and `/home/user/new_cert.pem` as argv[2]. Separately, generate the cert with `openssl req -x509 -newkey rsa:2048 -nodes -keyout new_key.pem -out new_cert.pem -days 365 -subj "/CN=test"` before running the exploit, since the checker hashes it on success.
