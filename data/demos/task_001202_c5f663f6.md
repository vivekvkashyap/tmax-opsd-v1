First fix the vendored dependency: search the `toml` package source for an obvious import-time typo (e.g. a misspelled standard-library module name) that would raise an `ImportError` the moment the package is imported — grep for `import` statements in the decoder module, correct the misspelling, then install it into your environment with `pip install -e /path/to/toml-package` and confirm with `python3 -c "import toml; print(toml.loads('a = 1'))"` before moving on. For `curate.py`: walk the target directory recursively (`os.walk` or `pathlib.rglob`) for `*.manifest` files; for each, attempt to decode its raw bytes trying `utf-8` first, then `utf-16le`, then `cp1252`, catching decode errors to fall through the chain rather than crashing. Parse the decoded text with `toml.loads`, pull `artifact_name`, `version`, `architecture`, `status` from `[metadata]`, and keep only entries whose `status` is exactly `"released"` (exact string match, not case-insensitive). Build a list of `{"name":..., "version":..., "arch":...}` dicts, sort by `name` ascending, and print with `json.dumps(..., separators=(',', ':'))` or equivalent compact form (no indentation) to stdout — pretty-printing will fail byte-exact comparison against the oracle.