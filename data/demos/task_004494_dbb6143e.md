OCR `/app/spec.png` with `tesseract` to recover the exact mesh dimension `N` and regularization `lambda` — don't hardcode assumed values. Build the mesh with `numpy.meshgrid(numpy.linspace(0,1,N), numpy.linspace(0,1,N))`, flatten to point pairs, and split into the four quadrants using the boundary rules given (note the `<=`/`>` split so points aren't double-counted or dropped at x=0.5/y=0.5). For each quadrant, compute the RBF kernel matrix (`exp(-||p_i-p_j||^2/0.1)`, e.g. via `scipy.spatial.distance.cdist(..., metric='sqeuclidean')`), add `lambda * I` to the diagonal for regularization, then `numpy.linalg.cholesky` to get the lower-triangular factor and sum its diagonal for the trace. Wrap this in a small HTTP service (Flask/FastAPI) on `127.0.0.1:8050` that checks the `Authorization: Bearer gp-data-token-2024` header on every request (401/403 if missing or wrong), and implements `POST /factorize` accepting `{"quadrant": "<name>"}`, returning `{"trace": <float>}` as a native JSON float (cast numpy floats with `float(...)`). Keep the process running in the background for the verifier to hit.
