Before writing any application logic, diagnose why the vendored `annoy` package is slow: open `/app/annoy-1.17.2/setup.py` and look at the `extra_compile_args` passed to the C++ extension — an intentionally weakened optimization flag (e.g. `-O0`) will make index-building/querying unacceptably slow even though it still "works" functionally. Restore proper optimization flags (like `-O3`, matching what upstream annoy normally ships), then `pip install /app/annoy-1.17.2` to rebuild the extension from source — a `pip install` without editing setup.py first will just reinstall the same slow build. For the actual task: load `/home/user/dataset.npy` (shape 10000x300), L2-normalize every row so each has unit norm, build an `AnnoyIndex` with metric `"angular"` and add all 10000 normalized vectors, build with `n_trees=50`, then for indices 0-99 call `get_nns_by_item(i, 10)` and dump the resulting dict (string keys "0".."99", list-of-int values) to `/home/user/nearest_neighbors.json`. Sanity-check recall yourself by comparing a few queries' index results against a brute-force numpy dot-product ranking before finishing, since the pass bar requires ~95% recall against exact top-10.
