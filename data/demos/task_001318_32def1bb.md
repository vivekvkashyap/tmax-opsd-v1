Treat this as reverse engineering a stripped binary via dynamic analysis rather than static disassembly alone. First extract candidate payloads: use `tshark`/`scapy` to reassemble TCP streams from `/app/traffic.pcap` and pull raw payload bytes, and parse `/app/daemon_state.wal` (an SQLite WAL) for embedded blob records — SQLite WAL frames have a fixed header structure you can scan for, or you can load the WAL via `sqlite3`/the raw page format to recover buffered payloads. Once you have candidate binary payloads, feed each to `/app/telemetry_daemon < payload.bin` under Valgrind (`--leak-check=full`) to confirm which trigger leaks, then apply delta debugging (bisect the byte stream, truncate/mutate fields) to isolate the minimal structural condition. Given the TLV framing (2-byte magic, then repeating 1-byte type + 2-byte big-endian length + value), suspect that one specific record Type mishandles the case where declared Length exceeds remaining input — confirm by constructing a minimal record of exactly that shape and checking Valgrind flags a leak only then. Implement `/home/user/filter.c` to parse the same TLV framing, detect that exact type+truncation condition, and exit 1 if present, 0 otherwise; test against self-crafted safe/unsafe samples before relying on it.
