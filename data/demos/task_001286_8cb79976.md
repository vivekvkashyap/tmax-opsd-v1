Build the Rust tool as a straightforward two-pass process: first parse every `.csv`/`.json` file in `/home/user/backups/raw`, normalize each record into `(id, date, event)` tuples, and render TSV lines with `\t` separators and no header/trailing content quirks (join with `\n`, watch for a trailing newline that could break exact-content comparisons for hardlink detection). For the destination, create `/home/user/backups/processed/<date>/` per file using `std::fs::create_dir_all`, and write the `.tsv` using the original filename stem. For hard-link dedup, keep an in-memory map from exact TSV byte-content to the path already written; when a new file's content matches, call `std::fs::hard_link(existing_path, new_path)` instead of `File::create` — do this check before writing, not after, and be careful that byte-for-byte equality (not just record equality) is what matters. Finally, after all files are processed, parse the `YYYY-MM-DD` folder names as dates, find the lexicographically/chronologically max one, and create `/home/user/backups/latest` as a symlink (`std::os::unix::fs::symlink`) pointing at that directory — remove any pre-existing symlink first so re-runs don't fail.