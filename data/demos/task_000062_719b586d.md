Fix the vendored crate first: inspect `/app/vendored/bio-spectrum-0.1.0/src/lib.rs` for its `use` statements, identify which external crate (used for the FFT planner) is missing from `Cargo.toml`'s `[dependencies]`, add it with a compatible version, and confirm `cargo build` succeeds inside that crate directory alone before depending on it elsewhere. Create your binary project at `/home/user/motif_filter` with a `path` dependency pointing at the fixed vendored crate, and call `bio_spectrum::power_spectrum` on each `.fasta` file's sequence content (strip the header line, concatenate sequence lines) for every file under the input directory. The key analytical step is calibration, not guesswork: read every file in `/home/user/data/clean/` and `/home/user/data/evil/`, compute each one's max spectral magnitude excluding index 0 (the DC component), print/log these values, and find a threshold that cleanly separates the two populations (there should be a wide gap, not a narrow one, so pick a robust midpoint rather than a boundary value). Use that computed threshold in your tool's classification logic, then copy only "clean" files (max peak below threshold) to the output directory. Build with `cargo build --release` and validate against both corpora directly before considering it done.
