Three independent components — build and test each in isolation before wiring them together. (1) For the admin audit, parse `/home/user/sysfiles/group` to find the `admin` line, extract its GID and its comma-separated supplementary member list; parse `/home/user/sysfiles/passwd` and collect any username whose 4th colon-separated field (primary GID) equals the admin GID; union that with the supplementary members, dedupe, sort alphabetically, and write one-per-line — don't forget users can qualify via either mechanism, not just one. (2) For the TCP proxy, use raw `socket` with two threads (or `select`) per connection to relay bytes bidirectionally between a client on 9999 and the upstream 8888 connection, closing both ends cleanly on EOF; test it manually against the given `legacy_service.py` before scripting the Expect flow. (3) For the Expect script, use `spawn nc 127.0.0.1 9999`, then alternate `expect "Username: "` / `send "admin\r"`, `expect "Password: "` / `send "supersecret\r"`, `expect "Access Granted"`, ending with `expect eof` or explicit `exit 0`; match prompt strings exactly including trailing space/colon. Make it executable and dry-run the whole chain yourself (start service, start proxy, run expect) to confirm exit code 0 before finishing.
