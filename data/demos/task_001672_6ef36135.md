Fix the build before writing any application code: a C-extension compile failure with missing-header errors (e.g. `ujson.h: No such file or directory`) means the extension's include path is broken — inspect `setup.py`'s `include_dirs` for the C extension and restore/add the project's internal header directories (its own `./python` and `./lib` source subfolders) so the compiler can find the vendored headers, then `pip install .` from the source tree and confirm with `python3 -c "import ujson; print(ujson.dumps({'a':1}))"`. For the processor script, implement it as a precise, deterministic string operation: hex-decode the argument (`bytes.fromhex`), decode to text permissively (errors='ignore' or similar so decode never throws), find the first `{` and last `}` via `str.find`/`str.rfind`, and only if both exist and the opening index is before the closing one, slice that inclusive substring and attempt to parse it with `ujson`; on any exception or missing/misordered brackets, print exactly `INVALID_RECORD`. On success, print `json.dumps(parsed, sort_keys=True, separators=(',', ':'))` — match this serialization exactly since a fuzz-equivalence harness will diff your output byte-for-byte against a reference oracle across hundreds of randomized inputs, so test edge cases like nested braces, garbage-only strings, and truncated JSON yourself before trusting it.