Work through this as five independent sub-deliverables, each with its own pitfall. (1) Mounting: try `mount --bind /app/backup_data /home/user/restore_mnt` first; without root it will fail, so fall back to `cp -a` to populate the mountpoint, but still append the specified fstab-style line to `/home/user/local_fstab` regardless of which method actually worked. (2) Vendored library: read the compiler error from `make` in `/app/libnetconf` carefully — a missing socket/inet-related symbol implicit-declaration error points to a missing `#include <arpa/inet.h>` in `net_parse.c`; separately inspect the `Makefile` rule that's supposed to produce `libnetconf.a` — if it's compiling/linking the `.c` file straight into an executable instead of an object file archived with `ar`, fix the rule to compile to `.o` then `ar rcs libnetconf.a`. (3) Manifest: `export LC_ALL=C.UTF-8 TZ=America/New_York` then `awk -F, '$3=="RESTORED"'`-style filtering (check the manifest's actual delimiter) into `/home/user/processed_manifest.txt`. (4) C validator: parse stdin with a strict regex/manual check for `ROUTE:####.` + one uppercase letter, exit 0 always. (5) SSH tunnel: `ssh -N -L 9090:127.0.0.1:8080 user@127.0.0.1`, saved verbatim to the specified file. Test each piece in isolation before combining.
