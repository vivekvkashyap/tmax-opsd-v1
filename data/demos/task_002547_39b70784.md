Two unrelated subsystems feeding one report — build and test them separately. Video: use OpenCV or ffmpeg to read frames from `/app/factory_lights.mp4`, slice each frame into the three 100px-wide x-ranges, and for each 10-frame (1-second) window compute the mean of the relevant single color channel (R left, G middle, B right) across all pixels/frames in that window — watch BGR vs RGB ordering in OpenCV. Threshold strictly `> 100.0` for ON. Write the long-format CSV sorted by `second_offset` then `sensor_name` (alphabetical: blue, green, red). Validator (`/home/user/validator.py`): for each `.json` file, parse it (reject on parse error), require the key set to be *exactly* `{timestamp, temp, pressure}` (reject extra/missing keys), reject non-numeric `temp`/`pressure` (a numeric-looking string must fail), enforce their inclusive ranges, and check `timestamp` against a strict `YYYY-MM-DDTHH:MM:SSZ` pattern rather than a loose ISO parser that tolerates variants. Print exactly `ACCEPT: <file>` or `REJECT: <file>` per file. Finally, tally ON-seconds per sensor from the CSV into the report template.
