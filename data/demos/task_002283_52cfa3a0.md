Structure this as four ordered phases in one script, each computing its own counts from actual data rather than hardcoding numbers: (1) stream-read the JSONL, validate each record by checking `timestamp`, `text`, and `lang` are all present and non-empty/non-null — count and skip failures, don't just try/except blindly; (2) for valid records, mask `user_email` with a regex/split on `@`: keep `local[0]`, append `***`, keep `@domain` unchanged, and replace `ip_address` wholesale with the literal string `[IP_REDACTED]`, only touching fields that exist; write these to the anonymized JSONL; (3) parse each valid record's ISO timestamp, truncate to the hour (zero out minutes/seconds, keep the `Z` suffix and `T` separator), and tally counts in a `dict[(hour_bucket, lang)] -> count`, then dump to CSV with the exact header order `hour_bucket,lang,feedback_count`; (4) write the four required log phrases to `/home/user/logs/pipeline.log` using the counts you actually computed for dropped/anonymized records — write "Pipeline started" before phase 1 and "Pipeline complete" after phase 3. Create `/home/user/processed` and `/home/user/logs` before writing into them.
