Fix the Rust ownership bug first in isolation: the compiler will complain that `prefix` is moved into `out = prefix + &l` on the first loop iteration then reused on the next — solve by building the output with `format!("{}{}", prefix, l)` or by cloning/using `&prefix` in a concatenation that borrows instead of consumes, then `cargo build --release`. For the services, check what's under `/app/services` for how to start Redis/Flask, then write an Nginx site config that proxy_passes port 8080 to `127.0.0.1:5000` and reload/restart nginx — verify with a manual curl through 8080 before writing any Python. For the orchestrator, the key performance insight is to avoid spawning the Rust binary 50,000 times: read all raw lines, join them with newlines, pipe the whole batch into the Rust binary's stdin in one `subprocess.run`, split its stdout back into lines, then send a single JSON POST (not per-line requests) to `http://localhost:8080/api/batch`. Time your script locally with `time python3 submit_telemetry.py` and profile if it's near 2.5s — likely bottlenecks are subprocess overhead or JSON serialization, not networking. Tie it together in `run_all.sh` that builds, starts services, and runs the script.