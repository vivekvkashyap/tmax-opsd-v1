For the plot bug, look for the classic ordering mistake: `plt.show()` called before `plt.savefig()` clears/closes the figure in some backends or displays an empty canvas in a non-interactive environment, so simply reorder to save first (and drop or move `show()` after), or switch to a non-interactive backend (`matplotlib.use('Agg')`) before plotting — then confirm the resulting PNG is meaningfully larger than a near-empty file. For `etl.py`, fit `TfidfVectorizer()` with all defaults on the `bio` column, get the dense/sparse array per row, and horizontally concatenate each row's TF-IDF vector with a single trailing `age/100.0` scalar (order matters: TF-IDF first, then age) before saving `id` and the combined vector list to Parquet via pandas/pyarrow. For `recommend.py`, load the parquet, extract the row for `id=101`, compute cosine similarity (dot product over norms, or `sklearn.metrics.pairwise.cosine_similarity`) against every other row's feature_vector, sort descending, and take the top 2 ids excluding 101 itself — write `{"target": 101, "similar": [id1, id2]}` to `/home/user/recommendations.json`, letting the actual computed ranking (not an assumption about which bios look similar) decide the order.
