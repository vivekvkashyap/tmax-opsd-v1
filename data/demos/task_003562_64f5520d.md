Work this in the given order since later steps depend on earlier ones. First, OCR the whiteboard photo: `tesseract /app/system_design.png stdout` (try `--psm 6` if the default layout parsing garbles lines) and read off the exact PRAGMA statements, the two `CREATE INDEX` statements, and the target concurrency/latency number — apply these verbatim rather than guessing your own indexing/pragma strategy, since the grading threshold is tied to what's specified in the image. Load `/home/user/raw_data/orders.csv` into a fresh `/home/user/analytics.db` with sqlite3 (create the table with appropriate types first), then execute the PRAGMAs (importantly `journal_mode=WAL` and `synchronous=NORMAL`, applied per-connection or persisted at file level) and the two indexes exactly as OCR'd. In `/home/user/query_api.py`, use `?`-parameterized SQL (never string-format the customer_id), and make sure every connection your API opens also sets the WAL pragma (a connection made without it can still block under concurrent writers). For pagination use `LIMIT ? OFFSET ?` with `(page-1)*page_size`. Finally write `/home/user/benchmark.py` using `multiprocessing` with concurrent readers plus a writer process inserting rows, time the reader completion, and save the float to `/home/user/execution_time.txt`; if you're near the 2.0s threshold, double check WAL mode is actually persisted (query `PRAGMA journal_mode;` after reopening) since that's the single biggest lever against locking contention.