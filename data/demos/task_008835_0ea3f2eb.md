Factor the ETL logic (read JSONL, lowercase+strip text, fit TfidfVectorizer(max_features=100, stop_words='english'), fit TruncatedSVD(n_components=10, random_state=42), save features/models) into a reusable function or module so `etl.py`, `reproducibility_test.py`, and `benchmark.py` don't diverge in behavior. For reproducibility, running the full fit-and-transform twice from scratch should give bit-identical arrays only if every source of randomness is seeded — the SVD's `random_state=42` already handles that, so `np.array_equal` on two independent runs should pass as long as you re-read the same input file both times; write "PASS"/"FAIL" based on that comparison, not a hardcoded PASS. For the benchmark script, be careful to only time `.transform()` calls on the already-fitted, loaded (`joblib.load`) vectorizer and SVD — never call `.fit()` or `.fit_transform()` inside the timed loop, or you'll be benchmarking training instead of inference. Use `time.perf_counter()` around the 50-iteration loop, divide by 50, and dump `{"num_runs": 50, "avg_time_seconds": ...}` as valid JSON. Create `/home/user/models/` with `os.makedirs(exist_ok=True)` before saving.
