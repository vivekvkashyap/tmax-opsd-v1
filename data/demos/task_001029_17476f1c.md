Transcribe `/app/field_notes.wav` first using an ASR tool (e.g. `whisper` CLI or a Python speech-recognition library) — do not guess the dictated names. Once you have the text, parse sentences matching the pattern "[Person A] influences [Person B]" with a regex/split on the word "influences", stripping punctuation, and collect them as directed edges (A -> B, influencer to influenced). Build a `networkx.DiGraph`, add exactly the edges you extracted (don't invent or omit any), then run `nx.pagerank(G, alpha=0.85)` with default settings (no weight parameter, so unweighted). Write the result to `/home/user/centrality.json` as a flat dict of `{name: float_score}`, keeping the capitalization consistent with standard English proper-noun casing. Double check the JSON is valid and that all node names transcribed from audio are spelled correctly, since spelling mistakes will create phantom nodes and skew the PageRank distribution.
