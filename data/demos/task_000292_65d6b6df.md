Work through this as three separate, independently verifiable fixes rather than one big patch. First, run `tesseract /app/specs.png stdout` to recover the exact port number and tolerance value from the image — don't guess or assume defaults; read what OCR actually returns and use those literal values. Second, fix compilation: run `make` (or `g++`) in `/app/math_server`, read the linker error carefully — a missing-object-file link error means an implementation file's `.o` isn't being passed to the final link step, so check the Makefile's link line/object list rather than the source itself. Third, diagnose the convergence bug with `git log`/`git bisect` across the repo's commit history: build and test each candidate commit's Newton-Raphson update against a known root, and compare the update-step arithmetic (`x = x - f(x)/df(x)` is the correct Newton step; a sign flip there causes divergence) to find exactly which commit and which line introduced the regression, then fix only that line. Apply the OCR'd tolerance to the convergence check, rebuild, and launch the server backgrounded (`./server &`) bound to the OCR'd port, then sanity-test it yourself with `nc`/a raw socket before declaring done.
