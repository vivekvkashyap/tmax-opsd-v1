Break this into independent, individually testable subsystems before wiring them into one server. (1) Video: extract frames with `ffmpeg`, and independently cross-check the true frame count with `ffprobe -select_streams v -count_frames -show_entries stream=nb_read_frames` so `/api/stats` reports a number you've confirmed. (2) Logs: when traversing the log directory, use `lstat`/`readlink` and track visited real (canonicalized) paths in a set so you never re-enter a directory already resolved — this prevents infinite loops from the symlink loop. Decompress each `.gz` with zlib in C++; convert the UTF-16LE log to UTF-8 before scanning for `[EVENT]` markers, and count occurrences across *unique* underlying files (dedupe by realpath so the loop doesn't double-count). (3) Serving directory: populate a temp dir with hardlinks (`link()`, not copy) to the JPEGs, then `rename()` it into place for atomicity — a plain copy-in-place would let partial state be served. (4) HTTP server: use a C++ HTTP library, wire `/api/stats` to your computed totals and `/api/frames/:id` to serve from the atomic directory with correct `image/jpeg` content type, run it in the background, and curl both endpoints yourself to confirm before finishing.
