Split this into two independent deliverables and verify each in isolation. For the video part, use `ffmpeg`/OpenCV to iterate every frame of `/app/telemetry_dashboard.mp4`, crop to the top-left 50x50 region, compute the mean of the red channel per frame, then normalize the whole series to 0-1 by dividing by the observed max (or by 255 if you confirm the raw channel range) — write `frame_index,red_intensity` to `/home/user/video_telemetry.csv` for every frame in order, and inspect the resulting values yourself (don't assume) to confirm they look like a clean blocky signal rather than noisy per-pixel variation. For the sanitizer, write a script that reads JSON-lines from stdin, is defensive when json-parsing each line (catch/skip unparseable ones after attempting a fallback unicode-escape repair, e.g. treating invalid `\uXXXX` sequences safely rather than crashing), tracks a set/hash of already-seen exact records to drop true duplicates, and tracks the previous valid record's timestamp per stream to drop records within 10ms of it. Test your sanitizer manually against both a clean, well-formed JSONL sample and a deliberately malformed one (bad unicode escapes, duplicate lines, near-duplicate timestamps) to confirm valid records pass through byte-for-byte and corrupted/noisy ones are handled without crashing. Make the script executable.
