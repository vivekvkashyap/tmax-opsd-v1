Since the source for `/app/xar_tool` is gone, reverse-engineer the XAR2 format empirically: create small, controlled test archives with the tool (single file, single dir, single symlink, nested paths) and inspect each with `xxd`, correlating offsets to what you put in — expect a magic header, an entry count, then per-entry a type byte, a length-prefixed path, and length-prefixed payload/target; vary one input at a time to isolate which bytes encode what. Once you can parse entries reliably in C++, write the sanitiser as a validator, not an extractor: reject any absolute path (starts with `/`), and detect traversal by tracking directory depth as you split path components rather than string-matching `../` (so `folder/../../etc/passwd` is caught even without a literal leading `../`). Apply the same checks to symlink targets, and separately build a graph of symlink name -> target so you can detect cycles and chains resolving deeper than 5 hops, using a visited-set and depth counter while following links. Test against both corpora and iterate until every clean archive exits 0 and every evil archive exits 1.
