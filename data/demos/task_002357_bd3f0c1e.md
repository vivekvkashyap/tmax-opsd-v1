Work the ticket in the reported order. First get the build working: run `make` on `main`, read the linker error, and inspect `git log -p Makefile` (or diff against the `v1.0` tag) to see what flag/library reference was removed or altered — restore it. Once it builds, run `make test` and read which assertion fails; inspect the solver code for an array-indexing loop that goes one element too far or starts one off — fix the loop bound rather than guessing. For the convergence/precision issue, don't just eyeball the diff: run `git bisect start`, mark current `HEAD` bad and `v1.0` good, and let bisect walk the history, testing at each step whether the numerical solver still converges correctly (rebuild and rerun the relevant test each iteration); the commit bisect lands on is your answer. A precision-loss bug in an iterative solver is often a type narrowing (e.g. an accumulator or tolerance variable declared with too little precision) — check variable types in the affected function. Record the exact fixing file and the bisected commit hash in `resolution.json` only after `make test` prints success.
