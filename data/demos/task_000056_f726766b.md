Load `/app/vibration_data.wav` (e.g. `scipy.io.wavfile.read`) to get `x1(t)` sampled at 1000 Hz over 10 s, and use its FFT to get rough initial guesses for the two coupled-mode frequencies (peaks in the spectrum) — these seed your optimizer rather than being the final answer. Define the ODE system exactly as given with `scipy.integrate.solve_ivp` (use a stiff-capable/high-accuracy method like `Radau` with tight `rtol`/`atol`, since the grader re-simulates with `Radau, rtol=1e-8, atol=1e-8` and compares MSE against the audio — sloppy solver tolerances will bias your fitted parameters even if the optimizer converges). Fit `c1, c2, k1, k2, k3` by minimizing squared error between simulated and observed `x1(t)` using `scipy.optimize.least_squares` or `minimize`; because `k1`, `k2`, `k3` are highly correlated (near-singular Jacobian), add bounds (all positive, physically reasonable ranges) and/or a small Tikhonov/L2 regularization term on the parameter vector, and consider multiple restarts from different initial guesses to avoid a bad local optimum. Verify convergence by checking that tightening solver tolerance further doesn't change your fitted parameters, then write the five floats to `/home/user/parameters.json` with the exact required keys.
