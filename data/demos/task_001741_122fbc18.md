Structure the C++ tracker around three phases: (1) directory scan — iterate `.gcode` files in the current dir with `std::filesystem`, read each file's first line and regex/parse it against `;; VERSION: <string>` (default to `UNKNOWN` if the line doesn't match that exact pattern), and compute SHA-256 by shelling out to `sha256sum <file>` via `popen`/`system` and parsing its stdout (simplest reliable approach without a crypto library); (2) load the old manifest by reading `<old_manifest_path>` line by line, splitting on commas into a map keyed by filename; (3) diff — for each current file, mark it NEW_OR_CHANGED if its filename is absent from the old map or its checksum differs, and only that subset gets bundled into the archive. Write the new manifest sorted alphabetically by filename (use `std::sort` on a vector of records before writing, don't rely on filesystem iteration order). Build the tar.gz with changed files at the archive root — that means either `cd`-ing into the source directory before invoking `tar -czf <out> file1 file2 ...`, or using `tar -czf <out> -C <dir> file1 file2 ...`, not passing full paths that would embed directory prefixes. Compile with g++ and run the tracker with the exact four arguments given.
