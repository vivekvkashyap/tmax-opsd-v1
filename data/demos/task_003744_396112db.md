Build the environment first, then the math. In `/home/user/run.sh` (chmod +x), create a venv at `/home/user/venv` (`python3 -m venv /home/user/venv`), activate it, `pip install numpy scipy pandas`, then invoke `python3 /home/user/analyze.py` from inside the activated venv so the installed packages are actually used. In `analyze.py`: load `/home/user/sim_data.csv`, compute empirical mean vector and covariance matrix of columns x,y with `df.mean()`/`df.cov()`, and regularize by adding `1e-4 * np.eye(2)` to the covariance to fix the near-singularity from collinear data — this step is essential or `scipy.stats.multivariate_normal` will fail or be unstable. Define the objective `-logpdf(x,y) + sin(x) + cos(y)` using `multivariate_normal(mean=mu, cov=cov_reg).logpdf`, and minimize it with `scipy.optimize.minimize`, seeding the initial guess with the empirical mean (not zeros or random) since that's specified and affects which local minimum is found. Format the resulting x,y to exactly 6 decimal places, comma-separated, single line, written to `/home/user/solution.txt`. Run `run.sh` end-to-end yourself and confirm `solution.txt` was actually produced before finishing.