Build this as three chained, independently-testable awk/bash scripts rather than one monolithic pass. In `1_clean.sh`, validate each row of `raw_data.csv` with awk: require exactly `NF==7` (comma-separated), require `$1` to match an integer regex, and require columns 2-7 to each match a numeric regex allowing optional sign/decimal (e.g. `^-?[0-9]+(\.[0-9]+)?$`) — this rejects rows with wrong column counts, non-numeric tokens like "NaN"/"error", and any stray text, writing survivors to `clean_data.csv`. In `2_predict.sh`, read the 6 lines of `model_weights.txt` into bias + 5 weights (via `mapfile`/`read` loop or `awk 'NR==1{b=$1} NR>1{w[NR-1]=$1}'`), then for each clean row compute `b + sum(f_i * w_i)` in awk with `printf "%d,%.4f\n"`, writing `id,prediction` pairs — watch the column offsets since `id` is column 1 and features are columns 2-6, label is column 7. In `3_evaluate.sh`, join predictions to true_label by id (matching row order or an explicit awk associative-array join) and compute population covariance `E[XY]-E[X]E[Y]` (divide by N, not N-1) with awk, printing just the rounded number to `covariance.txt`. `chmod +x` all three and run them in sequence.