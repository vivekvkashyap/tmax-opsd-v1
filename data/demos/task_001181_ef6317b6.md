Design the script around explicit state tracking of "which instances have already been updated so far," since rollback scope depends on that history, not just the current batch. Structure: split instances into batches of 2 per the given order; for each batch, first back up every instance's config.json to the backups dir (creating it if needed) and log it, then overwrite every instance's config.json with the new target JSON and log it — do backup+update for the whole batch before validating. After the batch is fully updated, call `subprocess.run(["python3", "/home/user/validate.py", instance_dir])` for each instance in the batch and check `.returncode`; log SUCCESS or FAILED per instance. If any instance in the batch fails, immediately roll back every instance touched across ALL batches so far (not just this batch) by copying each backup file back over its config.json, in exact reverse chronological order of when each instance was updated, logging each rollback line, then a final CRITICAL completion line, and stop (do not proceed to further batches). If a batch passes fully, sleep 1 second and continue to the next batch; if all batches succeed, log the final SUCCESS line. Match the log line formats exactly since verification checks them literally.
