Work through the vendored package methodically: first read the `Makefile` and `run.sh`/`config.ini`-parsing code to find the exact install-path and config-key bugs (e.g. a hardcoded system directory in `DESTDIR`, or a grep/parse looking for the wrong config key name) and fix them with minimal, targeted edits rather than rewriting from scratch — this preserves the rest of the vendored logic. For the zip-slip fix, do not just call `tar -x` directly on untrusted input; instead validate each archive member's path before extraction (reject absolute paths and any path whose resolved form escapes the `UPLOAD_DIR` root, e.g. checking `os.path.realpath` stays under the root, or using `tar --strip-components`/manual member iteration), while still allowing legitimate relative paths and symlinks that stay within the root. For concurrency, wrap the extraction critical section with `flock` (a lock file scoped to the target directory) so simultaneous uploads serialize safely, and pipe the incoming HTTP body directly into the extractor (e.g. `tar -x` reading from a pipe/fifo or streaming socket) instead of writing the whole upload to a temp file first. After fixing, run `make install`, start the service bound to `127.0.0.1:8888`, and test it yourself with both a benign tar and a `../` path-traversal tar before leaving it running in the background.