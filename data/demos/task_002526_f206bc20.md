Write the C program methodically: open both files with `open()`, get their size with `fstat`, and `mmap` them read-only (`PROT_READ, MAP_PRIVATE`) as `float*` arrays of length 2,500,000 — never use `fread` for the data itself, since the spec explicitly requires mmap and a checker greps the source for the literal string "mmap". Bracket only the comparison loop (not the mmap/open calls) with `clock_gettime(CLOCK_MONOTONIC, &start)` and `&end` immediately before/after, then compute elapsed microseconds as `(end.tv_sec-start.tv_sec)*1000000 + (end.tv_nsec-start.tv_nsec)/1000` cast to `uint64_t`. Accumulate MAE as a running sum of `fabsf(diff)` divided by count at the end (use double accumulator to avoid precision loss over 2.5M additions) and track MaxAE as a running max. For the log record, pack it exactly as a 24-byte struct matching Python's `struct.unpack('<QffQ', ...)` — that means little-endian uint64, float, float, uint64 with no padding, so either use `#pragma pack(1)` / write raw bytes with `write()` field-by-field, or build a matching packed struct — don't rely on natural struct alignment, which will introduce padding and break the 24-byte size check. Open the log file with `O_APPEND|O_CREAT`, munmap and close both input files before exiting, and print `MAE: %.6f, MaxAE: %.6f\n` exactly.
