Structure the script in the same order as the spec so each step's success can gate the next. For the interactive part, use `pexpect.spawn("python3 /home/user/mock_device_cli.py")` and drive it with `expect()`/`sendline()` pairs matching each prompt text literally (e.g. expect `"Enter Device ID:"` then sendline `EDGE-404`, and so on for the remaining two prompts) — then call `child.wait()` or check the exit status before assuming `/home/user/config.json` was written. For the backup, ensure the backup directory exists (`os.makedirs(..., exist_ok=True)`) before using Python's `tarfile` module (or `subprocess` calling `tar czf`) to compress the sensor data directory to the exact required path. For lifecycle management, find whatever process is bound to port 8080 (e.g. parse `lsof -i :8080` or scan `/proc/net/tcp`) and send it a termination signal before launching `edge_service.py` detached in the background (e.g. `subprocess.Popen` with no blocking wait). Poll the health endpoint with retries up to the 5-second budget rather than a single immediate request, since the new service needs time to bind. Populate the report booleans from actual filesystem checks, not assumptions, and copy the health body text verbatim into the report.
