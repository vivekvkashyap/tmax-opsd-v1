Work through this in three clean stages rather than trying to do everything in one pass. First, get the audio into text: use a speech-to-text tool (e.g. `whisper`/`whisper.cpp` or a Python STT library) on the wav file, install it if missing, and be careful to configure it for multilingual recognition since the log lines mix English/Spanish/Mandarin — do not assume ASCII-only output, and normalize/clean the resulting Unicode text (strip stray punctuation, unify whitespace) before parsing. Parse each transcribed line against the stated `[Language Code] [Timestamp] [RecordID] [Status]` format with a regex, tolerating minor STT noise. Second, build the dedup logic in plain Python: group by RecordID, and for duplicates keep only the record with the lexicographically/temporally earliest ISO timestamp; write the result as a JSON array with exactly the specified keys to the given output path. Third, for the query service, write a `.proto` file defining a `LogAuditor` service with the `GetRecord` RPC as specified, compile it with `grpcio-tools`/`protoc`, then implement and start a gRPC server bound to exactly `127.0.0.1:50051` that looks up records from the deduped JSON in memory, returning empty fields when a record_id is not found. Launch the server as a persistent background process (not foreground) so it survives after your script exits, and verify it responds before finishing.
