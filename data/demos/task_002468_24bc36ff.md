Rewrite the integrator to actually use the trapezoidal rule for non-uniform steps: track the previous row's time/energy as you stream through the CSV with awk, and on each new row accumulate `0.5 * (cur_energy + prev_energy) * (cur_time - prev_time)`, then update prev_time/prev_energy — skip the header row and only print the final sum at END. Test it manually on one file before batch-running. For the parallel step, generate the list of 50 CSV paths and pipe them through `xargs -P 4 -I{} /home/user/integrate.sh {}` (or `-n1 -P4`), redirecting stdout to `/home/user/all_integrals.txt` — verify the process count really hits 4 concurrent workers (e.g. watch with `ps` during a rerun), since `xargs` silently defaults to serial if `-P` is omitted or misplaced. For bootstrap.sh, use awk's `rand()`/`srand()` to draw 50 indices with replacement per resample, repeat 1000 times, compute each resample's mean, then `sort -n` all 1000 means and pick lines 25 and 975 (1-indexed) as the 2.5th/97.5th percentiles, writing `low,high` to `/home/user/ci.txt`. Make both scripts executable with `chmod +x`.