Structure the C++ program in clear stages. First parse `/home/user/audit.log` as plain text: scan for `[RECORD]`...`[END]` blocks, extract the `File:` and `Status:` fields per block (simple line-based parsing with `std::getline` and prefix stripping is enough — no need for a real grammar), and collect the filenames where `Status: CHANGED`. Second, for each such filename, recursively search `/home/user/config_repo/` for a matching basename — use `std::filesystem::recursive_directory_iterator` and compare `path.filename()`, skipping any name not found (per the setup, one changed file won't exist and must be silently omitted from output, not error). Third, when reading a located file's bytes, decode UTF-16LE to UTF-8 yourself: read the raw bytes as an array of little-endian 16-bit code units and convert each (or surrogate pairs, if any appear) to UTF-8 byte sequences — either hand-roll this per the UTF-8 encoding rules or use `<codecvt>`/iconv via `system()`, but verify the output text has no BOM artifacts or stray nulls. Finally sort the found (filename, content) pairs alphabetically by filename and write `--- <filename> ---` followed by the decoded content for each to `/home/user/summary.txt`, with nothing else in the file. Compile with `g++ -std=c++17` and diff your output against your own understanding of the input files to sanity check before finishing.