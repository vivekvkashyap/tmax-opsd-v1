Treat this as black-box reverse engineering, not decompilation. First run `file`/`strings` on `/app/deadlock_oracle` to look for clues (library names, symbols surviving stripping), but mainly probe it with small hand-crafted graphs via stdin and observe stdout: try a direct edge, a two-hop path, parallel paths with different weights, disconnected S/T, and S==T. Compare the results against known graph algorithms — the phrase "critical lock path capacity" combined with edge "weights" strongly suggests a bottleneck/widest-path style problem rather than shortest path, so test whether the answer tracks the minimum edge weight along the best path (maximized over all paths) instead of a sum of weights. Once you infer the semantics from these probes, implement it as a modified Dijkstra/Prim-like traversal using a max-priority-queue keyed on the running bottleneck value, not by summed distance. Also verify explicitly what integer the binary prints when no S→T path exists, and replicate that exact case. Write the C++ solution, compile with the exact specified g++ command, and re-run your own probe cases against both the oracle and your solver to confirm bit-exact matches before considering it done.