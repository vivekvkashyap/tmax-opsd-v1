Fix the toolkit before running any analysis: open `detrend.awk` and find the variance computation feeding a `sqrt(var)` denominator — when `var` is at or near zero (uniform sequence composition), that division blows up. Add the small epsilon to the variance itself before taking the square root (e.g. `var = var + 1e-6` right after it's computed, so every downstream `sqrt(var)` use benefits), rather than patching each call site separately. Validate the fix by running `run_spectral.sh` directly on the maximally uniform sequence in the FASTA file and confirming it now returns a finite score instead of erroring. Then compute the spectral score for every sequence in `sequences.fasta` (extract each ID/sequence pair, e.g. with `awk`/`grep`, and call `run_spectral.sh` on each), and identify the max-scoring one. For the Monte Carlo step, generate 100 independent random character-shuffles of that top sequence's string (e.g. via `awk`'s `rand()` seeded per iteration or a shell-level Fisher-Yates, avoiding reusing one fixed seed for all 100), score each through the same toolkit, and compute the p-value as the fraction of permutation scores that are `>=` the original top score (inclusive comparison). Write the three-line result file with the ID, score, and p-value exactly as specified.
