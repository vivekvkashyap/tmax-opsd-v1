Start by inspecting the schema of /home/user/research_data.db (`.schema` in sqlite3) to see the datasets/authors/join-table structure before touching the C++ code. The bug in graph_search.cpp is that the neighbor query selects from two copies of the join table without any WHERE clause linking them, producing a full cross product; fix it to find datasets that share at least one author with the current node, e.g. join the junction table to itself on matching author_id but differing dataset_id, filtered to the current dataset_id — and replace string-concatenated SQL with `sqlite3_prepare_v2` + `sqlite3_bind_int`/`sqlite3_bind_text` placeholders for all user-derived values (including the getId/getTitle lookups, not just the BFS query). For optimize.sql, add indexes that make the join and lookup fast — think about which columns are used in equality filters and joins on the junction and datasets tables — and apply it with `sqlite3 research_data.db < optimize.sql`. Compile with `g++ -O3 graph_search.cpp -o graph_search -lsqlite3`, run it with the two given titles as arguments, and redirect stdout to /home/user/path_result.txt.