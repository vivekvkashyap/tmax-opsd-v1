Fix the three bugs independently, then wire up the regression test. (1) In run_pipeline.sh, replace `for file in $(ls ...)` with a `find /home/user/logs -name '*.log' -print0 | while IFS= read -r -d '' file; do ...` pattern (or a glob with quoted `"$file"`), keeping `&` backgrounding and a trailing `wait` so files with spaces still launch concurrently. Note: piping into `while read` puts the loop in a subshell, so backgrounded jobs inside it are still waited on by the outer `wait` only if you avoid the pipe subshell (use process substitution `< <(find ...)` instead of `| while`) or `wait` inside the loop's parent shell. (2) In find_root_cause, track visited error IDs (a set) as you follow "Caused by" links; when you hit an ID already visited, stop and return the last unique ID found before the repeat, rather than recursing again. (3) For the shared report.json, wrap the read-modify-write in a `fcntl.flock` exclusive lock held for the full critical section (open the file, flock, read, append entry, seek+truncate, write, unlock) so concurrent Python processes serialize correctly. Build test_pipeline.sh to clear report.json, run the pipeline, wait, then use `python3 -c` or `jq` to assert valid JSON with exactly 5 entries, exiting 0/1 accordingly.