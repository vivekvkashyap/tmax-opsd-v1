Start by fixing the vendored dependency before writing any application code: open `/app/vendored/cxxopts/include/cxxopts.hpp`, try compiling a trivial program that includes it and uses `cxxopts::Options`, read the compiler error to confirm which standard type is undeclared, and add the missing `#include <string>` near the other standard includes at the top of the file — don't touch anything else in the vendored file. For the server, build it in layers you can test independently: (1) a raw POSIX socket TCP server that accepts connections and echoes back a fixed HTTP response, verified with `curl`; (2) a request-parsing state machine that reads until it sees the `\r\n\r\n` header terminator, extracts `Content-Length` from headers, then reads exactly that many more bytes for the body — don't assume the body arrives in one `recv()` call, loop until you've read Content-Length bytes; (3) URL-decode the `expr` and `x` params from the form body; (4) a recursive-descent or shunting-yard expression evaluator handling `+ - * / ^` with correct precedence (`^` right-associative, binds tighter than `*`/`/`) and parentheses, substituting the parsed `x` value. Build the response with a correct `Content-Length` header matching the exact byte length of the body. Test incrementally with `curl -X POST -d "expr=...&x=..." http://127.0.0.1:8080/score` against known hand-computed expression values, keep the server running in the background after building to `/home/user/artifact_scorer` and launching with `--port 8080`.
