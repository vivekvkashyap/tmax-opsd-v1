Build the wait-for graph programmatically rather than tracing it by eye: load `held_locks.csv` into a map from table_name -> holder_pid (assume one holder per table since it's "granted"), load `waiting_locks.csv` as a list of (waiter_pid, table_name) pairs, then for each waiting pair look up the table's holder to create a directed edge waiter -> holder. This gives you an adjacency map pid -> pid (each pid waits for at most one other, based on the data shape, so the wait-for graph is a simple functional graph — perfect for cycle detection via a visited-set walk from any node following the single outgoing edge until you revisit a node). Walk from an arbitrary starting pid following edges until you see a repeat; the repeated node marks the cycle start — collect from there back to it as the cycle. Once you have the cycle as a set of pids in edge order, rotate the sequence so it starts at the lowest PID in the cycle (find the min, then slice-and-wrap the list starting from that index) while preserving the wait-for edge direction. Output the comma-separated PIDs (no trailing PID repeat, no spaces) to `/home/user/deadlock.txt`. Write this as a short Python or awk script, run it, and verify the printed sequence's edges are consistent with the input CSVs before finishing.