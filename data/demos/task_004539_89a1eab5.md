Break this into independent, verifiable stages. First, extract the token: use `ffmpeg -i /app/admin_session.mp4` to dump frames around 150-160 (e.g., `ffmpeg -i input.mp4 -vf "select='between(n\,150\,160)'" -vsync 0 frame_%d.png`), then read the text off those frames (OCR or direct visual inspection) and write exactly what you find into `/home/user/extracted_token.txt` with no extra whitespace — don't guess or fabricate a token. Second, generate the cert independently: `openssl req -x509 -newkey rsa:2048 -keyout server.key -out server.crt -days 365 -nodes -subj "/CN=localhost"`. Third, write the HTTPS server using only stdlib: subclass `http.server.BaseHTTPRequestHandler`, wrap the socket with `ssl.wrap_socket`/`SSLContext.load_cert_chain`, parse the `Cookie` header for `session_id`, compare it against the token you read from `extracted_token.txt` (load it from the file at runtime rather than hardcoding), and respond 200 with the required body on match or 403 otherwise. Run the server backgrounded (`&`) and verify it yourself with `curl -k -b "session_id=<token>" https://127.0.0.1:8443/callback` before writing the payload URL.
