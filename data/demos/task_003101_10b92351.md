Work the linear-algebra recovery out on paper (or in a scratch script) before writing Rust: after filtering `logs.csv` rows to only those whose `run_id` maps to `status == "SUCCESS"` in `metadata.csv`, identify the baseline row (all of x1,x2,x3 == 0) to get the bias b = y_pred directly. For each remaining success row where exactly one of x1/x2/x3 is non-zero, solve `w_i = (y_pred - b) / x_i` for that feature — this is a one-at-a-time perturbation design, so each surviving non-baseline row isolates exactly one weight. Once you have w1, w2, w3, b, scaffold the project with `cargo new /home/user/tracker`, add `csv` and `serde`/`serde_json` to `Cargo.toml`, read all three CSVs (join logs+metadata by run_id, filter by status), derive the weights as above at runtime (don't hardcode values derived from your scratch work — compute them from the actual parsed rows so the program is correct if the fixture changes), then for each `test.csv` row compute `x_comp = x1*x2 + x3` and `y_pred = w1*x1 + w2*x2 + w3*x3 + b`. Serialize the array of `{test_id, x_comp, y_pred}` objects to `/home/user/experiments/predictions.json` with `serde_json`, run `cargo build --release` and execute the binary to actually produce the file.