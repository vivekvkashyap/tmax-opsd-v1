Write a genuine character-by-character CSV state machine in C rather than splitting on newlines: track an "inside quotes" boolean, toggle it on each unescaped `"`, and only treat `\n`/`\r\n` as a record separator when that boolean is false — this is the key fix over a naive line-based reader. For encoding, exploit that ISO-8859-1 byte values equal their Unicode code points: for each byte b read, if `b < 0x80` emit it unchanged; if `b >= 0x80` emit the two-byte UTF-8 sequence `0xC0 | (b >> 6)` followed by `0x80 | (b & 0x3F)`. Apply this conversion while emitting each field/record, not as a separate pass, so quoting and embedded newlines are preserved correctly through the transcoding. For stratified sampling, always write the header first, then keep a small in-memory count map keyed by the `Department` field value, incrementing per record and only emitting (writing) a record if that department's count is still below 2 — skip it otherwise but continue scanning so later departments are still captured. Compile with `gcc -o process process.c` and run it to produce `/home/user/output.csv`.
