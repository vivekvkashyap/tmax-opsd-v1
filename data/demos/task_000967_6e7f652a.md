Reproduce the crash first (`cd /home/user/anomaly_detector && cargo run --release -- /home/user/data/metrics.log`) and read the panic message/backtrace to confirm it originates in the value-parsing function, then fix `parse_value` to return `Option<f64>` (or `Result`) instead of calling `.unwrap()` on `str::parse` and on the exponent split — for scientific notation, check that the exponent substring is non-empty and itself parses successfully before combining; on any parse failure, return `None` and have the caller `continue`/skip that line entirely rather than propagating a panic, matching the spec that malformed rows are silently ignored. Separately, fix the Welford variance getter: guard the `self.m2 / (self.count - 1.0)` division so that when `count <= 1.0` it returns `0.0` instead of dividing by zero (which produces `NaN`/inf) — a single-sample query key must report variance 0.0, not NaN. After both fixes, rebuild (`cargo build --release`) and run the service against `/home/user/data/metrics.log`, confirming it exits cleanly and produces `/home/user/corrected_queries.csv` with no NaN values and with the malformed-value rows (scientific notation missing digits, and non-numeric text) excluded from their query's stats.
