Fix `setup.py` by adding an `ext_modules` list with a `setuptools.Extension` pointing at `src/trib_c.c` named so it builds as `math_accelerator.trib_c` (match the package/module naming the C file's `PyInit_trib_c` expects), then run `pip install -e .` and confirm `python -c "import math_accelerator.trib_c"` works before moving on. Implement `tribonacci(n)` in `trib_py.py` as a straightforward iterative accumulation from the three base cases (T(0)=0, T(1)=1, T(2)=1) forward — avoid naive recursion, which will be slow and skew the benchmark comparison unfairly. In `benchmark.py`, import both implementations, assert they agree at n=25 before benchmarking (a mismatch means one implementation is wrong, not a benchmarking issue), then use `timeit.timeit(..., number=100)` for each and compare the totals to set `c_faster_than_py`. Write the JSON with the exact two keys specified, and re-run the benchmark a couple of times if the C-vs-Python timing comparison looks unstable or unexpectedly close.
