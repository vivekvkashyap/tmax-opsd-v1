Transcribe `/app/dictation.wav` first (convert with `ffmpeg` if needed, then use whatever offline speech-to-text tool is available) to recover the exact secret root directory name — double-check spelling since it prefixes every output line. Implement the resolver as a stack-based path normalizer: for each stdin line, split on `/`, skipping empty components and `.`; for `..`, pop the stack if non-empty, or if already empty print `INVALID` and stop processing that line. For a fully-resolved line, join the remaining stack with `/`, prefixed by the root name and a trailing `/` — handle the edge case where the stack ends up empty (e.g. input `.`) by outputting just the root name plus trailing slash. Since correctness is judged by exact stdout match against thousands of fuzzed inputs, write your own test cases covering every edge case in the spec and check them manually before considering it done.
