Bring the three services up one at a time, verifying each before moving to the next: start `redis-server` and confirm with `redis-cli ping`; fix the worker's Redis connection (correct `REDIS_PORT`/host env var to match the actual running redis instance, 6379); `pip install uvicorn fastapi redis scikit-learn matplotlib` and launch `uvicorn api:app --host 127.0.0.1 --port 8000` backgrounded. Fix the blank-plot bug by forcing a non-interactive backend before importing pyplot — `import matplotlib; matplotlib.use('Agg')` must come before `import matplotlib.pyplot as plt` — and ensure `plt.savefig(...)` is actually called with data plotted (not on an empty/cleared figure) followed by `plt.close()`. Rewrite `process_batch` to accumulate incoming rows in order, fit `StandardScaler` + `PCA(n_components=2)` on only the first 1000 accumulated rows, then `.transform()` the full accumulated dataset, writing `pc1,pc2` CSV rows in original arrival order. Finally, write a small client script that POSTs every row of `raw_sensors.csv` as `{"features": [...]}` to `/ingest`, then poll/wait until the worker has drained the Redis queue (e.g. check `LLEN sensor_stream` hits 0) before treating the run as complete.
