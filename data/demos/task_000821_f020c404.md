Split this into evidence-gathering then clean-room reimplementation. First pull the seed out of the video: extract frames with `ffmpeg -i /app/debug_session.mp4 ...` (e.g. sample at a few fps or grab all frames), then `grep`/OCR the frames for the line containing `SERVER_SEED: ` and read off the hex byte that follows — that's a value you must recover from the artifact, not guess. Second, reverse the binary black-box style since it's stripped: run `/app/token_checker` with varied crafted `Cookie:` strings (valid SessionId of different lengths/hex content, missing SessionId, odd-length hex, non-hex chars, no args) and record exact stdout/stderr/exit code for each; also use `objdump -d`/`strings`/`radare2` or similar static disassembly to confirm the transformation (look for XOR against a constant, then a bit-rotate/shift pattern applied per output byte) rather than assuming the algorithm. Cross-check your derived seed value and transform against several probe outputs before writing C. Implement `/home/user/my_token_checker.c` to replicate cookie parsing (locate `SessionId=`, take chars until `;` or end, validate even length and non-empty), the per-byte transform using the seed you extracted, and match error messages/exit codes exactly for every malformed case you observed. Fuzz-test your binary against the oracle on many random cookie strings before finishing.