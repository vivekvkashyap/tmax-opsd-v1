Load all JSON files into an in-memory graph keyed by `id`, with a name lookup, before doing any traversal — don't process files ad hoc. For part 1/2, do a BFS/DFS over `depends_on` edges starting from the concept named "Quantum Computing", collecting visited node IDs (excluding the start) for the dependency list, and the full visited set (including the start) plus only the `depends_on` edges whose both endpoints are in that visited set for the subgraph; watch for a node reachable via two different paths so you don't double-count or double-traverse (use a visited-set guard). Sort names alphabetically for output, and sort edges by `(source, target)` tuple for determinism. For part 3, build a directed graph from `related_to` edges across every concept, then search specifically for 3-cycles: for each edge A→B, for each edge B→C, check whether edge C→A exists, requiring A, B, C pairwise distinct; dedupe found cycles by their sorted-name representation before writing (the same 3-cycle can be discovered multiple times starting from different nodes), then sort the resulting lines alphabetically. Write this as one Python script using only standard libraries (`json`, `os`) and run it to produce all three output files, then re-open them to sanity-check structure.
