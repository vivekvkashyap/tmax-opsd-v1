Recover the ground truth before trying to reimplement it: search the git history in /app/repo (e.g. `git log --diff-filter=D --summary` or check recent commits/reflog) to find the commit where the compiled Go helper binary existed prior to deletion, check it out into a scratch location, and run it directly on a variety of test inputs (empty string, spaces, newlines, shell metacharacters, non-ASCII bytes) to observe its exact stdout format and exit behavior — do not guess the format from the Bash replacement script, since that's the broken version you're replacing. From your observations, reverse-engineer the transformation (e.g. hash the input and combine it with an encoded form of the original string) precisely enough to reproduce it byte-for-byte. Then write fixed_format.sh using `sha256sum` (feeding the exact `$1` bytes, being careful to avoid trailing-newline differences from command substitution or extra output from sha256sum's filename suffix) plus a byte-level hex/other encoding tool (`xxd -p`/`od`) for the filename portion, always quoting `"$1"` to prevent word-splitting/glob-expansion on spaces and shell metacharacters. Test your script against the recovered binary on many edge-case inputs including newlines and control characters, diffing stdout exactly, before finalizing, since it will be fuzzed for byte-for-byte equivalence.