Before writing any code, inspect /home/user/communications.db with `sqlite3 .schema` (or `.tables` then `PRAGMA table_info(...)` per table) to discover the real table/column names for users and message links — the description deliberately withholds them since it's "undocumented." Identify which columns represent sender and receiver on the link table. In your Python script, write a SQL query that computes, per user, the count of rows where they appear as sender PLUS the count where they appear as receiver (two separate aggregations or a UNION ALL joined back to users, then summed) — this sum is the "communication score." Filter to score > 2, order by score descending then username ascending for ties, and LIMIT 3. Before writing output, load /home/user/schema.json and validate your result list against it (via the `jsonschema` package, or a manual check of required keys/types/additionalProperties if you avoid the dependency) — don't skip this validation step since the task calls it out explicitly. Write the validated array to /home/user/top_users.json with objects containing exactly `username` and `communication_score` fields matching the schema's property names and types.