Build this in layers and test each independently. First, get the vendored PyJWT working: add `/app/PyJWT-2.6.0` to `sys.path` before importing `jwt`, then try decoding a self-signed-and-verified test JWT — it will raise a signature error because `RSAAlgorithm.verify` in `/app/PyJWT-2.6.0/jwt/algorithms.py` has been sabotaged to always return False; locate that method and restore it to actually return the boolean result of the underlying `cryptography` RSA verify call (e.g. `public_key.verify(signature, msg, padding.PKCS1v15(), self.hash_alg())`, returning True on success / False on `InvalidSignature`). Second, implement certificate chain validation: verify `/app/certs/leaf.crt` was signed by `/app/certs/ca.crt` (e.g. via `openssl verify -CAfile ca.crt leaf.crt` through subprocess, or the `cryptography` library's certificate verification APIs) before trusting the leaf's public key. Third, in `/home/user/audit_filter.py`, read the null-byte-separated cmdline file, scan the joined/split arguments with a regex for three dot-separated base64url segments, and for each candidate attempt `jwt.decode` with the leaf's public key and `algorithms=["RS256"]`; print `LEAK` and exit 0 only if chain validation and signature verification both succeed, else print `CLEAN` and exit 0. Iterate against the evil/clean corpora until 100% pass.
