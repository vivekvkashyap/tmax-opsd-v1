Treat this as a two-bug fix-and-wrap task validated by fuzz-equivalence against an internal reference, so correctness on edge cases matters more than a quick patch. First locate the exact SSH-rejection regex in `/app/log-router-v1.2/src/parser.py` and compare it character-by-character against the real OpenSSH message `"Disconnecting: Too many authentication failures"` — a subtly shortened or altered pattern will silently fail to match and drop real log lines, so search the file for all `re.compile` calls near rejection/disconnect logic rather than trusting the first match you see. Second, find the timezone/UTC-offset lookup table or calculation for `fr_FR.UTF-8` and check it against the correct offset (France is UTC+1, or +2 during DST) — look for a hardcoded dict entry that's simply wrong. After fixing both, install/generate the `fr_FR.UTF-8` locale (`locale-gen fr_FR.UTF-8` or equivalent) so tests exercising that locale don't fail for unrelated reasons. Write `/home/user/process_logs.sh` as a thin wrapper invoking `python3 /app/log-router-v1.2/src/main.py "$1"`, then stress-test it yourself with varied synthetic log lines (including edge-case rejection messages and timezone strings) to catch regressions before finishing. Also configure `/home/user/logrotate.conf` for daily rotation, 7 days retention, targeting `/home/user/logs/`.
