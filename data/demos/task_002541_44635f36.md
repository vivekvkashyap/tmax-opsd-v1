For port scanning without root/nmap, write `audit_ports.sh` using a loop over 8000-8100 that tests each port with a fast TCP probe such as `(echo >/dev/tcp/127.0.0.1/$p) 2>/dev/null && echo $p` (or `nc -z -w1`), appending successes to `active_ports.log`. For integrity checking, run `sha256sum -c /home/user/baseline.sha256` from the directory the baseline's relative paths were generated in (likely `/home/user`), then parse its stderr/stdout for lines ending in `FAILED` to get the mismatched relative paths, and prepend the base dir to make them absolute before writing `modified_files.txt`. Discover the rogue port from Task 1's output rather than assuming a fixed number, then in `test_auth.sh` use `curl -s -o /dev/null -w '%{http_code}' -X POST -H 'Content-Type: application/json' -d '{"username":"admin","password":"password123"}' http://127.0.0.1:<port>/admin` and write just that status code (no extra text/newline issues) to `auth_status.txt`. Finally `block_ip.sh` should `echo 'deny 10.10.50.15' >> /home/user/app/acl.conf` and then `chmod 444` the file — do the chmod after the append, since a read-only file can't be appended to afterward. Run every script so the output files actually exist.
