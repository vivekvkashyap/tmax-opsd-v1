For phase 1, decode `/app/security_cam.mp4` frame by frame (OpenCV `cv2.VideoCapture`), and for each frame check whether the 10x10 block from (0,0) to (9,9) is solid pure red (RGB 255,0,0) — count frames matching that exact condition (use a small tolerance only if compression artifacts are present, but check first whether the source is lossless). That count C gives the passphrase `PIN:{C}`. For phase 2, derive the AES key as `hashlib.sha256(passphrase.encode()).digest()`, read the first 16 bytes of evidence.enc as the IV, and decrypt the remainder with AES-256-CBC (`cryptography` library's `Cipher(algorithms.AES(key), modes.CBC(iv))`), then strip PKCS7 padding before json.loads-ing the plaintext. For phase 3, write a minimal HTTP server (Flask or http.server) bound to 0.0.0.0:8000, implement GET /api/v1/evidence to check the `Authorization: Bearer <token>` header against the decrypted auth_token exactly, returning 401 on mismatch/missing and 200 with the recovered_data object as JSON on match; keep the process running in the background (nohup/&) so the verifier can curl it after your turn ends. Verify the video frame count logic against a handful of manually-inspected frames before trusting it, since an off-by-one there breaks the whole decryption chain.