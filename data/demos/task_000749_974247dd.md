Tackle Issue 1 by tracing the request path hop by hop rather than editing blindly: start each service, then check each config file in /home/user/app/config/ against what the previous hop actually expects — Nginx's proxy_pass target must match the Flask listen port, Flask's backend-port variable must match where the C backend daemon actually binds, and the C backend's own bind-port setting must be internally consistent; fix each mismatched port one at a time and retest with curl after each fix (`curl -s http://localhost:8080/api/fraud_status`) until you get past 502. For Issue 2, don't just eyeball the C source — build both the reference binary from fraud_aggregator.c and note the provided legacy oracle at /home/user/app/legacy/oracle_aggregator, generate small crafted binary streams of 32-bit integers, run both, and diff outputs to localize where they diverge; inspect the rolling-window (ring buffer) logic for an off-by-one in how the index wraps around the window size — a modulus applied against the wrong buffer size will corrupt reads once the window fills. Fix the modulus to match the actual window array length, recompile with the same flags to /home/user/app/bin/fraud_aggregator, re-run comparisons against the oracle on varied random inputs to confirm matching output, then leave all four services running.
