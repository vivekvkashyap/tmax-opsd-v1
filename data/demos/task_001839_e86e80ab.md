Build the script in the order the task lists so each stage is independently testable. Use `numpy.linspace(0, 10, 1_000_000)` for `t` (inclusive endpoint), compute the clean damped-oscillator signal with the given gamma/f0 formula, then call `numpy.random.seed(42)` immediately before `numpy.random.normal(0, 0.05, size=...)` and add that noise — seed placement matters for reproducibility, so don't generate any other random numbers before it. For spectral analysis, use `numpy.fft.rfft` (or full `fft` plus `fftfreq`) to get frequency bins and magnitudes, then find the peak by taking `argmax` of the magnitude array restricted to positive frequencies — this is a numerical value you compute, not something to hardcode. Save all four arrays as root-level datasets in an HDF5 file via `h5py.File(...).create_dataset(name, data=arr)`. Compute `abs(f_peak - f0)` and write it as a plain float to validation.txt. Finally wrap the whole pipeline in one function and profile it with `cProfile.run('main()', '/home/user/simulation.prof')`. Run the script end-to-end and spot-check each output file exists and loads correctly before finishing.
