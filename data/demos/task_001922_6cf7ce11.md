Part 1 (DMD): use `cv2.VideoCapture` to pull the first 50 frames, convert to grayscale, resize to 128x128, flatten each to a length-16384 column; assemble `X1` (frames 0..48) and `X2` (frames 1..49). Compute a truncated SVD of `X1` (`np.linalg.svd`, keep top r=5 singular values/vectors), build the reduced operator `A_tilde = U_r.T @ X2 @ V_r @ diag(1/S_r)`, eigendecompose it for discrete eigenvalues λ, then convert to continuous-time via `ω = log(λ)/dt` with dt=0.01 (use `numpy` complex log, watch branch-cut issues). Sort by `abs(ω)` descending, take the top 3, and write the CSV exactly as `Rank,Real,Imaginary`. Part 2 (filter): load the `.npy` matrix, check `np.isnan`/`np.isinf` first, then compute `np.linalg.cond` wrapped in a try/except for singular matrices (treat exceptions or cond > 1e8 as "evil"), and `sys.exit(0)`/`sys.exit(1)` accordingly. Build and test your own small clean/singular/Hilbert-matrix examples locally to validate the logic — don't assume a fixed threshold works without checking edge cases like exact singularity and extreme scaling.
