Set up the venv and install `pandas`/`scipy` first, then write the script incrementally, checking intermediate output at each step rather than writing everything blind. Load the CSV with pandas, group by `sensor_id`, and compute Z-scores per group using population statistics (ddof=0) — i.e. `(x - group.mean()) / group.std(ddof=0)`, not pandas' default sample std (ddof=1), since the task explicitly specifies degrees of freedom = 0. Filter to keep only `abs(z) <= 3.0`, then write the cleaned frame back out with the original columns only (drop any z-score helper column before saving) to `clean_data.csv`. For the report, compute counts with `len(df)` before/after filtering, and per-sensor max of the cleaned `reading` column with `.round(2)` — format with a fixed 2-decimal string (e.g. an f-string `:.2f`) so trailing zeros aren't dropped. Match the report line format exactly (labels, colons, spacing) as specified, then actually execute the script so both output files exist on disk — don't just leave the code unrun.