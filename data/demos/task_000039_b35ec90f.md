Start by running OCR (`tesseract /app/dev_note.png stdout`, or preprocess with imagemagick if the raw scan is noisy) to recover the two secret values written in the note — the TLS pin and the salt — and confirm you got clean text before hardcoding them into your script. Then treat `/app/ref_auth_gen` as a black-box oracle: run it with a handful of crafted inputs (normal username/password, empty strings, usernames containing `../` or `..\\`) and diff your script's output against it byte-for-byte, not just eyeballing. Implement the path-traversal check first and make it short-circuit before any hashing. For the normal path, concatenate pin + username + password + salt in that exact order, hash with `hashlib.sha256(...).hexdigest()`, and format the JSON with `json.dumps` (or an exact literal f-string) so field order and spacing match the reference — quote escaping and key ordering are common mismatch points against a compiled oracle. Iterate against the binary with a small fuzz loop (varying lengths, special characters, edge-case traversal strings like `....//`) before considering the script done.
