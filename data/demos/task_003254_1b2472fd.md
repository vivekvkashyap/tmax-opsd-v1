Fix `pipeline.sh` in the same order the steps are listed, testing after each edit rather than all at once. (1) Replace the naive `cat` with a proper filter that keeps the header line plus any row whose 4th comma-separated field is exactly `1` — `awk -F, 'NR==1 || $4==1'` is the natural tool, redirected to `/home/user/data/filtered.csv`. (2) Add `export PIPELINE_MODE=STRICT` before invoking Python so the env var is visible to the child process, not just set locally. (3) Loop `for p in 0.1 0.5 0.9; do python3 .../evaluate.py --param "$p" --input ... ; done`, capturing each line of output. (4) Parse each output line's SCORE/CI_LOWER/CI_UPPER fields (e.g. with `grep -oP` or `awk`), track the highest SCORE among only the runs where CI_LOWER is strictly greater than 0.5 (candidates failing that bound must be excluded even if their score is higher), and remember the corresponding score/CI pair. (5) Write the single winning line to `/home/user/best_model.log` in the exact `Best Param: <P>, Score: <S>, CI: [<L>, <U>]` format, matching the two-decimal formatting the Python script already emits. Do not touch `evaluate.py` or the raw CSV — only the orchestration script.
