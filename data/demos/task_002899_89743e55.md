Handle each data source independently and validate before combining. Run `tesseract lab_notes.png stdout` (or similar OCR) to read the k_deg value printed in the image, and parse it as a float from the OCR text rather than assuming a value. For GC content, parse the FASTA by sequence ID, counting 'G'/'C' over total length per record — watch for multi-line sequences under one header if any exist. For the integrator bug: the described symptom (step size grows when local error is high) means the adaptation formula has the exponent/ratio inverted — the correct adaptive-step rule shrinks `dt` when estimated error exceeds tolerance and grows it when error is small, i.e. `dt_new = dt * (tol/error)**power`, not `(error/tol)**power`; fix the direction of that ratio specifically, don't rewrite the whole integrator. After fixing, run it per sequence with that sequence's `k_syn = 5.0*GC_fraction` and the OCR'd `k_deg` up to t_end=100 to get each steady-state P(100); since this is a linear ODE, you can sanity-check numerical results against the closed-form solution `P(t) = (k_syn/k_deg)*(1-exp(-k_deg*t))`. Finally compute MLE mean/std (population std, i.e. divide by N) over the 100 concentrations and write the exact JSON structure requested.