Write the C program to use standard BSD sockets: create a TCP socket, `connect()` to `127.0.0.1:<port>` (parse the port from `argv[1]`), and branch on the connect() return value — success means open the log file in append mode and write exactly `STATUS: OK\n`, failure means write exactly `STATUS: FAIL\n`, then exit with the matching status code; make sure the log file is opened with append (`"a"` mode or `O_APPEND`) so repeated runs don't clobber prior lines, since the verifier checks the log ends with a specific status after running against both an open and a closed port. For `setup_env.sh`, chain the steps as ordinary shell commands: `gcc -o /home/user/uptime_monitor /home/user/uptime_monitor.c`, then `dd if=/dev/zero of=/home/user/vm_data.img bs=1M count=10` followed by `mkfs.ext4 /home/user/vm_data.img` (format only, do not mount). Write the fstab line and logrotate config as literal file content matching the exact field order and keywords the task specifies (device, mountpoint, fstype, options, dump, pass for fstab; daily/rotate 4/compress/missingok for logrotate). Make the script executable, run it, then manually invoke the compiled binary against port 8080 to populate the log before finishing.