Tackle each phase independently and verify intermediate state before moving on. Phase 1: write a loop that repeatedly finds `.tar`, `.tar.gz`/`.tgz`, and `.zip` files under `/home/user/extracted/` (via `find`), extracts each with the right tool (`tar xf`, `unzip`), deletes the archive, and repeats until a `find` pass turns up zero archives — a single-pass extraction misses archives nested inside other archives. Phase 2: identify ELF files by reading the first 4 bytes of every file (e.g. `head -c4 file | od -An -tx1` or `xxd`) and matching against `7f 45 4c 46`, not by relying on `file` output text matching alone or on extensions — some non-ELF files (like a PE `MZ` header) must be excluded. Phase 3: grep the first 10 lines of candidate files for `; FLAVOR:Marlin`, then extract the `; TIME:<n>` value with a regex, format as `basename: Ns`, and sort the report alphabetically by filename. Phase 4: use `split -l 500 -d --additional-suffix=.log file file_part_` style naming carefully to match the exact `_part_aa`/`_part_ab` suffix convention, then delete originals. Phase 5: in `build_index.sh`, launch one backgrounded subshell per file that reads its first line and appends via `flock -x /path/lockfile -c '...'` (or `flock` fd locking) around the `>>` write, then `wait` for all jobs — omitting `flock` here causes interleaved/corrupted lines under concurrency.