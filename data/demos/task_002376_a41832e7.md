Three separable pieces. First, fix the circular import: inspect `router` and `handlers` packages in `/app/patch-api`, find the cross-dependency (likely `router` importing `handlers` for route registration while `handlers` imports `router` for a shared global/type), and break the cycle by moving the shared symbol into a third package (e.g. `types` or `shared`) or by using dependency injection (pass handler functions into the router instead of importing them directly) — confirm with `go build ./...` before moving on. Second, implement `/api/v1/patch`: read the raw request body as a unified diff, scan lines starting with `+++` or `---`, extract the file path after the prefix (stripping the `a/`/`b/` marker if present), and reject with HTTP 400 if any path is absolute (starts with `/`) or contains a `../` traversal segment (check path components, not just a substring, to avoid false negatives/positives on legitimate filenames) — use `net/http` and standard `strings`/`path` functions only, run the server as a background process on :8080. Third, extract video frames at 1fps with `ffmpeg -i /app/incident.mp4 -vf fps=1 frame_%04d.png` into a fresh directory, `ls | wc -l` the results, and write that integer (no extra text) to `/app/frame_count.txt`.
