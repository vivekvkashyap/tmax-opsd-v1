Make three targeted edits to the existing `/home/user/run_diffusion.py` rather than rewriting it — the diffusion ODE is stiff (large negative-eigenvalue Laplacian term), so change the `solve_ivp` call's `method='RK45'` to `method='BDF'` (or LSODA/Radau) to fix both the slowness and the implicit accuracy issue. Replace the sequential `for s in seeds: ...` loop with `multiprocessing.Pool(4)` mapping `simulate_graph` over `seeds`, collecting the (key, value) tuples it returns into the results dict exactly as the sequential version did (order doesn't matter since results are keyed by seed string). Inside `simulate_graph`, after computing `final_x.mean()` and `x0.mean()`, add an `assert abs(final_x.mean() - x0.mean()) < 1e-5` — the stiff solver fix is what makes this assertion actually pass since RK45 wouldn't converge tightly enough in the time given. Leave the JSON-writing of `analytical_mean` values (i.e. `x0.mean()`, not the numerical mean) unchanged. Run the script afterward and confirm it finishes quickly and `/home/user/steady_states.json` is produced without the assertion firing.
