For `/home/user/sanitize.sh`, parse each data file with `awk`/`grep` rather than a full parser: pull `Total_Score` from the first line, and pull every `Score=` value plus every `Primer=` sequence from the `Alignment N:` lines (a regex like `Score=([^ ]+)` and `Primer=([A-Za-z]+)` per line works well in awk). Reject immediately (exit 1) if any extracted numeric field matches `NaN`, `Inf`, `-Inf`, or `inf`/`nan` case-insensitively — check this before doing arithmetic, since feeding those into `bc`/awk sums can silently propagate or error. Reject if any Primer length is <15 or >30 chars (`awk 'length($0)<15 || length($0)>30'`). Otherwise sum all individual Score values (use `bc -l` or `awk` for float precision) and compare `abs(sum - Total_Score)` against the `0.001` threshold; reject if it exceeds that, else exit 0. Test against both provided clean/evil examples before trusting it on the hidden corpus. For the video part, use `ffmpeg`'s `blackdetect` filter (e.g. `ffmpeg -i /app/data/convergence_sim.mp4 -vf blackdetect=d=0.1:pic_th=0.98 -f null -`) to find the black_start timestamp from stderr, round it to the nearest integer second, and write just that integer to `/home/user/crash_time.txt` — don't assume the crash time, measure it from the actual video.
