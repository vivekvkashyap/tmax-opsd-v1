Break this into sequential, independently-verifiable stages. First, extract frames from the video with ffmpeg (e.g. `ffmpeg -i /app/incident_screen.mp4 out_%04d.png`) and visually inspect frames around the point where a password is echoed, reading off the two visible leading digits directly from the image rather than guessing. Second, write a small C (or any language) brute-force program that iterates the remaining two unknown digits (00-99), builds the candidate string `Secure_<2 known digits><2 unknown digits>`, computes SHA-256 of each candidate, and compares against the hash stored in /home/user/intercepted.hash until it matches; write the winning candidate verbatim to /home/user/cracked_password.txt. Third, generate the TLS cert/key with `openssl req -x509 -newkey rsa:2048 -keyout server.key -out server.crt -days 365 -nodes -subj "/CN=honeypot.local"` into /home/user/certs/. Fourth, write honeypot.c: load cert/key and bind/listen via OpenSSL *before* chrooting (files must be open already, since /home/user/jail won't contain them), then chroot() to /home/user/jail and setuid/setgid to nobody (65534) — order matters: bind+load first, drop privileges second. Parse the raw HTTP request for the `Cookie: AuthToken=` header value, compare exactly against the cracked password, and return 200/"ACCESS GRANTED" or 403 accordingly. Compile with `-lssl -lcrypto`, run it backgrounded, and confirm with curl -k that both the valid and invalid cases behave as specified.