Start by reverse-engineering the encoding, don't guess: disassemble `/home/user/telemetry_lib.pyc` (e.g. with `uncompyle6`/`decompyle3`/`dis` module) to recover the exact framing and transform functions, and cross-check against the raw bytes in `/home/user/capture.pcap` (open with scapy or tcpdump -X and inspect the TCP payload bytes) — confirm things like a length-prefix header, any XOR/byte-transform, and a text-encoding layer (e.g. base64) by decoding a captured frame and checking it produces sensible ASCII sensor text. Once you understand encode/decode symmetrically, write `/home/user/proxy.py` as a TCP server on `localhost:8000` that, per connection, reads the framed message, decodes it fully to plaintext, checks for the substring `"CRASH_OVERRIDE_V1"` and drops (does not forward) any payload containing it, otherwise re-encodes the plaintext using the exact same scheme and forwards the framed bytes to a new connection to `localhost:8001`, relaying the consumer's response back if applicable. Handle multiple sequential/concurrent connections (loop accept, or thread/fork per connection) and don't crash on malformed frames. Test manually by sending a known benign and known malicious payload through your proxy before considering it done.