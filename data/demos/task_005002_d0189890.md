Work through the three bugs independently before trying to run anything. First, decompile `compute_utils.pyc` with a tool like `decompyle3`/`uncompyle6`/`pycdc` (or manually disassemble with `dis` if decompilers fail on this Python version) to recover `compute_utils.py`; inspect `calculate_convergence` for the boundary bug (likely a `>` vs `>=` loop condition that never terminates or overshoots) and rewrite it to converge correctly, adding an assertion that each iteration's delta is strictly smaller than the previous one so the fix is self-verifying. Second, in `server.py`, find the two locks used by the HTTP and TCP handlers and make both handlers acquire them in the *same* order (e.g. always A-then-B) — mismatched acquisition order across handlers is the classic deadlock pattern, so grep for every `.acquire()`/`with lock` site and normalize them all. Third, fix the Makefile's Python interpreter variable so `make run` invokes python3, not python2. After all three fixes, `make run` in the background, then manually smoke-test both the HTTP endpoint (with the bearer token) and the raw TCP socket before considering it done.
