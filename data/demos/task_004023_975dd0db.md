First get the vendored `simdjson` building at all: check its `CMakeLists.txt` for an explicitly pinned `CMAKE_CXX_STANDARD` — simdjson requires C++17+, so if it's pinned to an older standard, bump it, then `cmake`/`make`/`make install` (or build+link locally) before touching the aggregator. Only after the dependency itself compiles and its headers/libs are usable should you fill in the C++ aggregation logic: stream-parse the JSONL with simdjson, bucket each event by `ts - (ts % 3600)` per `lang`, accumulate `words` sums into a map keyed by (bucket, lang). For the rolling 3-hour average, iterate buckets in chronological order per language and sum the current bucket plus the two immediately preceding hourly buckets for that same language (treating any missing preceding bucket as zero, not skipping it) then divide by 3. Compile with `-std=c++17` and link simdjson, run it against the full JSONL file, and format output rows exactly per the header, sorted by bucket then language, with the rolling average rounded to 2 decimals.
