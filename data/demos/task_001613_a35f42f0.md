Do this in two clearly separated phases. Phase 1: fix the vendored crate first, in isolation — `cd /app/acl-manager-rs && cargo build` and read the actual compiler/panic error rather than guessing; a build.rs that calls `.unwrap()` on an unset env var is a common cause, fixed either by changing it to `.unwrap_or_else(|_| "default".into())` or by exporting the missing env var before building. Confirm the crate builds cleanly before moving on. Phase 2: `cargo new` the `provisioner` binary at `/home/user/provisioner`, add the fixed crate as a `path = "/app/acl-manager-rs"` dependency in Cargo.toml, and write strict, explicit validation for each field (username: length 1-32, charset `[a-z0-9_]` only, reject any `.`/`/`; quota_mb: integer in [1,10240]; ssh_key: must start with `ssh-rsa ` or `ssh-ed25519 ` and must not contain any of `; | & $ > <`; sync_ip: parse as strict IPv4, e.g. via `std::net::Ipv4Addr::from_str` plus your own octet-range/format checks since some parsers are lenient about leading zeros or partial addresses). Exit 0 only if every rule passes, otherwise a non-zero code. Build in release mode, then self-test against both `/app/corpora/clean/` and `/app/corpora/evil/` before considering it done — don't just eyeball the code, actually run the binary on every file in both dirs and check exit codes.
