Treat this as two independent bugs; fix and test each separately before running the full e2e. Parsing bug: aggregator.py's stream loop likely assumes every packet starts with a 2-byte length prefix, but a 4-byte heartbeat marker (0x00 0x00 0xFF 0xFF) can appear instead. Peek ahead and special-case it: if the next 4 bytes match the heartbeat pattern, consume exactly those 4 bytes and continue without treating them as a length prefix; otherwise read the 2-byte little-endian length and that many bytes of float32 data. Use a buffered/stateful reader that accumulates bytes and only consumes a full frame at a time, or TCP fragmentation will desync you. Precision bug: don't try to disassemble/patch libvariance.so — stop calling it. Replace it in aggregator.py with a numerically stable variance computed in Python (Welford's online algorithm or `statistics.variance()`), which avoids the sum-of-squares-minus-square-of-sum cancellation that blows up on large-offset data. Validate incrementally with `python3 /home/user/app/run_e2e_test.py` after each fix, checking the printed `MSE:` drops below 1e-6.
