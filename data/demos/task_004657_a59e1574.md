Work through each subtask as an isolated, verifiable step using only `jq`, `sed`/`grep`, `gcc`/`make`, and `diff`. For the JSON extraction, use `jq -r` with the correct nested paths (the fields are nested under `deployment.metadata` and `deployment.config`, not flat) to pull `service_name`, `target_environment`, and `version`, then write them into `release.env` with `printf`/heredoc in the exact `KEY=VALUE` format requested. For the protobuf, edit `service.proto` textually: insert a new `message HealthCheckResponse { bool healthy = 1; }` block and add an `rpc HealthCheck(google.protobuf.Empty) returns (HealthCheckResponse);` line inside the existing `service DeploymentService { ... }` block (the file already imports `google/protobuf/empty.proto`, so no new import is needed). For the Makefile, `cp Makefile Makefile.orig` before editing, then locate the link rule and append `-lm` to the compiler invocation that links `check_util.o` into `check_util` (the "undefined reference" symptom points at a missing math library at link time, not a compile-time issue), then run `make` to confirm the binary builds and runs correctly. Finally generate the patch with `diff -u Makefile.orig Makefile > makefile_fix.patch`.
