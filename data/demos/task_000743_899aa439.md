Since the two CSVs aren't sorted, sort them on `user_id` first before joining (`join` requires sorted input): e.g. `tail -n +2 data_x.csv | sort -t, -k1,1n > x_sorted.csv` and similarly for `data_y.csv`, then `join -t, -1 1 -2 1 x_sorted.csv y_sorted.csv` to produce `user_id,x_val,y_val` rows only for matching IDs (join naturally drops IDs present in only one file).

Filter out rows with an empty `x_val` or `y_val` field — watch for cases where the CSV has a trailing comma with nothing after it (empty field, not a missing field) — use `awk -F, '$2 != "" && $3 != ""'` to catch this correctly.

Install `datamash` (`sudo apt-get install datamash`) and pipe the filtered two-column numeric data into `datamash ppearson 1:2` (or similar column-selection syntax) to compute the Pearson correlation directly from the shell, avoiding a full python/R detour. Round the result to 4 decimal places with `printf "%.4f\n" "$value"`, then write only that value with no extra whitespace to `/home/user/correlation.txt`. Sanity check by manually verifying which user_ids ended up in the joined, filtered set matches your expectation.
