Start by exploring the unknown schema before writing any query: use `sqlite3 /home/user/analytics.db .schema` (or `.tables` then `.schema <table>`) to discover the real table and column names — don't assume names like "customers"/"orders", since this is deliberately undocumented and the actual tables/columns may be non-obvious (e.g. renamed with prefixes or abbreviations). Once you know the schema, identify the customer/client table, an orders/purchases table with an amount and status column, and an interactions/support table with a duration column, plus the foreign keys linking them back to the customer id. Build the report as a parameterized query (use `?` placeholders with the sqlite3 driver, never string-interpolate the CLI args) that: left-joins customers to filtered orders (status = arg1) and filtered interactions (duration >= arg2) separately (to avoid a fan-out cross join inflating sums when doing a single join across both), aggregates each side with `SUM(...)` and `COALESCE(..., 0)`, and keeps only customers present in at least one of the two joined sets. Sort by total_spent desc, then customer_name asc, and dump to the path given as arg3. Wrap it all in an executable shell script `/home/user/generate_report.sh` that forwards `$1 $2 $3` to your implementation.
