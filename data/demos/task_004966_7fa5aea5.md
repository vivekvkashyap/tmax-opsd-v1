Build the pipeline stage by stage and test each output before chaining. Stage 1: read every CSV in /app/data/snapshots/, melt the wide locale columns (en_US, fr_FR, es_ES, de_DE) into long form with pandas `melt(id_vars=['date','msg_id'], value_vars=[...], var_name='locale', value_name='translation')`, concatenate across files, and write reshaped_long.csv. Stage 2 is the critical part: /app/bin/eval_metric is a black-box oracle scoring a string — do not guess its threshold, discover it empirically by invoking it (e.g. `/app/bin/eval_metric "some string"`) on a range of test strings varying properties like casing, length, and punctuation, and observe how the score changes; from those probes infer both the scoring signal it's sensitive to and the cutoff value that separates known-spam-looking strings from normal ones, then encode that threshold in sanitiser.py which calls the same binary via subprocess per row (or per unique string, cached, for speed) and drops rows whose score crosses the threshold. Take `--input`/`--output` CSV args and preserve the row schema exactly. Stage 3: bulk-insert sanitized_long.csv into locales.db table translations_ts using `executemany` inside a single transaction (or `.to_sql` with a big chunksize) rather than row-by-row commits. Stage 4: in generate_templates.py, for each (msg_id, locale) pick the row with the max date via SQL window function or groupby, then emit one .po file per locale under /home/user/templates/ following the exact `msgid "..."` / `msgstr "..."` block format. Wire all four stages into pipeline.py or a Makefile so a single command runs them in order, and validate sanitiser.py separately against your own constructed clean/spam-like strings before trusting it on the hidden corpus.
