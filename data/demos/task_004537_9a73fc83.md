Write the C program to process input incrementally rather than slurping the file: use `fgets(buf, 1024, stdin)` in a loop with a running line counter, never `fread` the whole stream. For each line, scan character by character for the literal substring `\u` (backslash followed by 'u'), then check that the next four characters all satisfy `isxdigit()` — treat running off the end of the buffer/line (hitting `\0` or newline early) as invalid too, not just non-hex characters. If any `\u` occurrence fails validation, print the 1-based line number to the error log and `continue` to the next line without extracting fields. For valid lines, don't write a full JSON parser — locate the literal substrings `"event_id": ` and `"sensor_val": ` with `strstr`, then use `sscanf`/`strtol`/`strtod` starting right after each to pull the numeric value up to the next non-numeric character. Format output with `printf("%d,%f\n", event_id, sensor_val)`. Compile with `gcc -o /home/user/cleaner /home/user/cleaner.c`, then run `./cleaner < sensors.jsonl > clean_data.csv` and inspect both output files against the input to sanity-check line numbering.
