Implement textbook gradient descent with numerical (central-difference) differentiation entirely in Bash, using `bc -l` or `awk` for the floating-point arithmetic since Bash itself can't do decimals. Structure: loop 50 times; each iteration call `/usr/local/bin/run_workload -p <value>` twice (at p+h and p-h with h=0.1), capture both latency outputs, compute the gradient as (f_plus - f_minus)/(2h), then update p := p - learning_rate*gradient (learning_rate=0.05), starting p at 0.0. Do not analytically hardcode the target function — treat `run_workload` as a black box and only use the two function evaluations for the gradient at each step, since the whole point of numerical differentiation is not knowing the closed form. After the loop, round the final p to exactly 2 decimal places (e.g. via `printf "%.2f"` in awk/bc) and write only that value to `/home/user/optimal_p.txt`. A common naive mistake is truncating instead of rounding, or reusing a stale gradient/step size — make sure each of the 50 iterations does two fresh binary invocations and updates p immediately.
