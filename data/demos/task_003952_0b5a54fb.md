This is a numeric verification task best done with `awk` reading the CSV directly, since it needs floating point math per row. For each run, compute A = prior_alpha + heads, B = prior_beta + tails, then True Mean = A/(A+B) and True Variance = (A*B)/((A+B)^2*(A+B+1)) using the exact formulas given — don't approximate or round intermediate values, only the final comparison thresholds matter (abs diff <= 0.01 for mean, <= 0.001 for variance). Mark PASSED only if both conditions hold, else FAILED. Write a small `awk` script (or bash loop) that skips the CSV header, computes these per line, and emits `run_id,status` rows; make sure the header row `run_id,status` is written first and that rows are sorted by `run_id` (use `sort` afterward if the input isn't already ordered, or `sort -t, -k1,1` while preserving the header). Redirect final output to `/home/user/validation_report.csv` and diff it visually against your own hand-recomputation of at least one row to catch formula transcription errors before finishing.