First recover the hidden window size `W` from the image using OCR (`tesseract /app/window_size.png -` or similar), and sanity-check the extracted digits before hardcoding them into your program — don't guess. Then implement the C++ program stage by stage: read lines with `std::getline`, split on the first space to separate ID from message, normalize the message by lowercasing, dropping any non-alphanumeric-non-space character, collapsing runs of spaces, and trimming leading/trailing spaces. Compare the normalized message to the immediately preceding *emitted* (non-skipped) message's normalized form for deduplication — track only one previous string, not a set, since only immediate repeats are skipped. Maintain a running deque/vector of the normalized-message lengths, and for each new non-skipped event compute the mean of the last `W` (or fewer, if not yet available) as an integer-truncated average using integer division rather than floating point rounding. Print `ID normalized_message avg` per line, and test against varied inputs including consecutive duplicates and short streams before considering it done, since it will be fuzz-tested at scale.