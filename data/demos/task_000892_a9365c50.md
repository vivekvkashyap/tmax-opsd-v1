Work through the three parts in dependency order: static analysis first, then exploit, then policy tool. For the CWE, read `greeter.c` — an unbounded `gets()` into a fixed stack buffer is a classic stack buffer overflow; map that to the standard CWE family for unbounded copies (not a generic "buffer" ID) and write just the ID to `cwe.txt`. For the exploit, don't guess the offset — disassemble the binary (`objdump -d /home/user/greeter` or `readelf -s`) to find both the exact saved-return-address offset from `buffer` (accounting for the compiled stack layout, not just `sizeof(buffer)`) and the address of the hidden `admin_shell` function, since it's never called and only appears in the symbol table. Build the payload in Python as raw bytes (padding + packed little-endian address via `struct.pack("<Q", addr)`), write it to stdout with `sys.stdout.buffer.write`, and test it directly with `python3 exploit.py | ./greeter` before wiring anything else. For the policy script, parse `sshd_config` generically (split into directive/value pairs, skip commented lines) and check the three required directives are present, uncommented, and match exactly — don't special-case filenames.