Start the target Flask server in the background first (nohup/tmux) and confirm it answers on 127.0.0.1:8080 before writing the tester. Write `policy_tester.py` using `requests`: POST to `/api/login` with the given credentials, then inspect the raw `Set-Cookie` response header (via `resp.headers.get('Set-Cookie')` or `resp.raw.headers.get_all`, since `requests`' cookie jar strips attributes) to check for the literal substrings `HttpOnly` and `Secure` — don't assume `resp.cookies` exposes these flags, they only live in the raw header text. Compare `resp.headers` keys against the required security-header list and record which required names are absent, sorted alphabetically. Then reuse the session (or its `session_id` cookie) to GET `/api/users`, parse the JSON list, and replace every SSN value with the fixed string `***-**-****` via a regex or direct field overwrite while preserving name/role. Serialize everything into the exact nested JSON shape specified, write it to `/home/user/compliance_report.json`, and re-read the file to confirm booleans/arrays match what you actually observed from the live server rather than assumptions.