Iterate the log files and reduce to per-model (successes, trials) counts before computing the ratio — don't try to combine both steps in one pass with fragile string math. For each `.log` file in `/home/user/artifacts/`, use `grep -c "OUTCOME: 1"` for successes and `grep -c "OUTCOME: [01]"` (or count total lines matching either pattern) for total trials `N`; the model name is the filename with `.log` stripped (`basename "$f" .log`). Apply the given Beta(1,1) posterior-mean formula `(S+1)/(N+2)` per model using `awk` or `bc -l` for floating point (bash arithmetic alone won't do decimals), and format to exactly 4 decimal places with `printf "%.4f"`. Track the running maximum expected rate and its model name as you loop (e.g. compare with `awk 'BEGIN{if (a>b) print "yes"}'` or accumulate in an array and pick the max at the end) rather than assuming which model wins — the whole point of the Bayesian correction is that raw win-rate ranking can differ from the corrected ranking when trial counts vary a lot. Write the winner as `MODEL_NAME,EXPECTED_RATE` with no extra whitespace or trailing content, make `evaluate.sh` executable, and run it to produce `best_model.txt`.
