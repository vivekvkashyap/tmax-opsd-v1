Break this into three independent, exactly-specified transformations and verify each in isolation. First, `/home/user/audit.py`: load `/home/user/traffic.json`, keep only entries whose `headers["Cookie"]` contains the substring `admin_session` (use substring containment, not exact match, since cookie strings have other fields appended), preserve original list order, and for kept entries replace an existing `Authorization` header's value with the literal string `[REDACTED]` if present (entries without an `Authorization` key stay as-is, don't add one) — write the result to `/home/user/audit_results.json` with the same JSON structure. Second, tally `source_ip` occurrence counts among only the filtered (admin_session-containing) entries, find the IP with the strict maximum count, and write exactly one line `iptables -A INPUT -s <IP> -j DROP` to `/home/user/block_ip.sh`, then `chmod +x` it. Third, edit `/home/user/sshd_config` in place: for `PermitRootLogin` and `PasswordAuthentication`, if a line (commented or not) sets the directive, rewrite it uncommented with value `no`; if the directive is entirely absent, append a new `Directive no` line. Confirm afterward with `grep -E "^PermitRootLogin no"` and `grep -E "^PasswordAuthentication no"` that both match.
