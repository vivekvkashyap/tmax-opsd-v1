Debug this legacy script by fixing each failure in the order it manifests, verifying each step manually before moving to the next. First, inspect the config file the script reads — if `cat`-ing it shows garbled/non-plaintext content, it's likely base64-encoded; decode it (`base64 -d`) before parsing out the iteration count, rather than grepping the raw encoded blob. Second, when the C helper fails to compile, read the exact gcc error — if it references an undefined math function symbol, add the missing linker flag (`-lm`) to the compile command. Third, once it builds and runs, inspect the loop that accumulates the series: a Gregory-Leibniz-type alternating series needs its sign flipped every iteration (e.g. `SIGN=$((SIGN * -1))`) — if that line is missing, the partial sum will not converge near pi/4 at all, which is your signal that this is the remaining bug. Apply all three fixes directly in `calc_pi.sh`, then run it end-to-end and confirm `/home/user/pi_result.txt` contains a value close to pi (given the `scale=4` truncation used by `bc`).
