Handle the encoding conversion and CSV parsing carefully since the whole point of the task is mixed encodings and column-order variation. In your C program, parse `/home/user/dataset_raw/config.json` (install/use a JSON library like `libcjson-dev`) to get each `{filename, encoding}` pair, then for each file read its raw bytes and use POSIX `iconv_open(encoding, "UTF-8")`/`iconv()` to convert the buffer to UTF-8 in memory before any text parsing — parsing raw bytes as if they were already UTF-8 will corrupt multi-byte characters (e.g. accented letters) and can misalign column boundaries. After conversion, parse the CSV header row to find the index of the column literally named `Measurement` (its position varies between files — do not hardcode a column index), then sum that column's float values per file and append `filename: sum` (one decimal place, trailing newline) to `/home/user/dataset_processed/summary.txt`, creating the directory if needed. Compile with `gcc ... -lcjson` (or your chosen library) and run it. Separately, write `/home/user/backup.sh` that ensures `/home/user/backup/` exists and runs `tar` with `--listed-incremental=/home/user/backup/snapshot.snar` against `/home/user/dataset_processed/`, naming the archive `backup_$(date +%s).tar.gz`; execute it once so exactly one archive plus the snapshot file exist afterward.