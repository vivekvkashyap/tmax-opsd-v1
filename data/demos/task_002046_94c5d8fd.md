Treat this as two independent subsystems and get each fully working before wiring them together. For the sanitizer, write `/home/user/data_pipeline/sanitizer.py` (argv: input, output) that reads the CSV row by row and keeps a row only if ALL of: `price >= 0.0` (numeric parse), `id != parent_id` (skip check gracefully when parent_id is empty), `metadata` parses via `json.loads` into a dict, and `name` (case-insensitive) contains neither `DROP TABLE` nor `UNION SELECT` — since this will be fuzz/corpus tested, be conservative: treat any parse failure or type error on a field as a rejection rather than crashing the script. For the pipeline, inspect the running services first (`ps`, `curl` each port) to see what's actually broken before editing; fix `/home/user/data_pipeline/nginx.conf` so `location /api/ { proxy_pass http://127.0.0.1:5000/; }` and reload nginx; fix `/home/user/data_pipeline/.env` with the correct local ports for Redis (6379), Postgres (5432), and Mongo (27017), restart Flask/worker so they pick up the new env, then run `/home/user/data_pipeline/test_e2e.py` and iterate on whichever leg (nginx routing, Flask enqueue, worker consume, DB writes) it reports as failing.
