Work the fix in dependency order. First inspect `/home/user/app/start.sh` to see what socket path the app expects (or pick a consistent one like `/home/user/app/api.sock`), then edit the `proxy_pass` line in `/home/user/nginx/nginx.conf` to `http://unix:<that-socket>:;` and reload/restart nginx (`nginx -s reload` or restart the process) — a broken socket path is the classic cause of 502s here. Start the Flask app with `gunicorn --bind unix:<socket> api:app` as a background/daemon process from `/home/user/app`, and confirm the socket file actually appears before testing. For dynamic volume discovery, parse `/home/user/vfs/fstab` line by line in Python, splitting on whitespace to get device/mountpoint/fstype/options, and select the line where fstype=='ext4' and 'finops=true' is a token in the comma-split options field — don't just substring-match the whole line, since other ext4 lines could partially match. For `/metrics`, shell out (subprocess) to an `awk` pipeline that sums the 4th pipe-delimited field grouped by the 2nd field, then convert to a JSON dict of floats. Test end-to-end with `curl http://127.0.0.1:8080/metrics`.