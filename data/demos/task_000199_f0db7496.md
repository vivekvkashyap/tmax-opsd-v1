The actual specification is only in the audio file, not the prose task description — do not guess the transformation from the task text alone. First transcribe `/app/spec_memo.wav` (e.g. with a local speech-to-text tool, or by listening/playing it back if you have audio tooling) and write down the exact steps it describes verbatim before coding anything. Pay close attention to: the precise arithmetic formula applied to each integer, the exact condition and operation for handling negative intermediate results, the byte width/endianness/signedness used before base64-encoding each value, and the exact JSON key/value shape (what the keys are, what the values are). Implement `/home/user/serializer.py` to read a JSON array from stdin, apply the transcribed transformation per element, and print a JSON object to stdout matching the transcribed serialization format exactly — since a fuzz test compares byte-for-byte against a hidden reference, get edge cases right (empty arrays, negative inputs, zero) and match Python's default `json.dumps` formatting only if the memo doesn't specify a different one.
