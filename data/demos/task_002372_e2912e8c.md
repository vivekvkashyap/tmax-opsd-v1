Break this into four independent pieces and verify each before moving on. (1) Transcribe `/app/maintenance_recording.wav` to recover the spoken 4-digit PIN (do not guess), then write `backup.sh` that tars `/home/user/iot_configs/` and pipes/encrypts it with `openssl enc -aes-256-cbc -pbkdf2 -pass pass:<PIN>` (or -k) into `/home/user/configs_backup.tar.gz.enc` — confirm you can decrypt it back with the same PIN. (2) For the Go parser, since it will be fuzz-tested for bit-exact output against a hidden legacy binary, implement the spec literally and precisely: use Go's `encoding/base64` StdEncoding to decode, reverse the resulting byte slice in place, then lowercase-hex-encode it with `encoding/hex`, and print with `fmt.Print` (not `Println`) so there is no trailing newline; write and run your own round-trip tests (encode known bytes, reverse by hand, compare) to be sure edge cases like empty input, padding, and single-byte inputs behave exactly as specified. (3) `deploy.sh` must compile the Go file first, then loop sequentially (not in parallel/background) over node_1..node_5, copying the binary and writing `deploy.log` with the exact text `Deployment successful` (no trailing newline mismatch issues — match what verification expects). (4) For port forwarding without root, launch `socat TCP-LISTEN:8080,fork,reuseaddr TCP:localhost:8081 &` (or `disown`/nohup) so it persists in the background after the script exits; test it actually relays a payload end-to-end. Make both scripts executable.
