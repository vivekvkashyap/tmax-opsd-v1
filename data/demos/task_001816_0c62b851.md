Work this as a pipeline of independently-checkable numeric steps, since the grader diffs your final file exactly. First load points.csv with numpy (`np.loadtxt(..., delimiter=',')`), transpose to shape (2, N) for `scipy.stats.gaussian_kde(data.T)` with default bandwidth. Build the mesh with `np.linspace(-10, 10, 100)` for both axes and `np.meshgrid`, then evaluate the KDE on the flattened grid coordinates (`kde(np.vstack([X.ravel(), Y.ravel()]))`) — evaluating on raw (x,y) pairs instead of the stacked/ravel form is a common mistake. Find the argmax of the density array and map that flat index back to the corresponding (x*, y*) grid coordinates — this is the origin for quadrant splitting, using >= for one side and < for the other consistently (Top-Right = x>=x*, y>=y*, etc.) to avoid double-counting or dropping boundary points. For each quadrant's subset of the *original* points (not grid points), compute the 2x2 sample covariance with `np.cov(pts, rowvar=False)` — treat quadrants with fewer than 2 points as a zero matrix/zero trace instead of erroring. Use `np.linalg.cholesky` for L, sum its diagonal for the trace, sort the four traces ascending, and write each formatted to exactly 4 decimal places, one per line.
