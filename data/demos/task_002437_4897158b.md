First transcribe `/app/backend_instructions.wav` with an STT tool and read it precisely — since the verifier fuzz-tests bit-exact equivalence against a hidden oracle across thousands of random inputs, do not paraphrase or approximate the transformation rule; implement it literally (e.g. if it says to strip/remove a specific character class from stdin, use a precise filter like `tr -d` or a regex substitution covering exactly that class, and test manually against a few edge-case strings — empty input, input with none of the target characters, input that's entirely the target characters — to confirm bit-exact byte output including trailing newline handling). Make `/home/user/backend_processor` executable and self-contained (a shebang line + your chosen language). Separately: launch a minimal HTTP server on port 8081 returning 200, wrap it in `/home/user/supervisor.sh` using a restart loop (e.g. `while true; do ./server; sleep 1; done &`) so it respawns on crash, run it in the background, then edit `/home/user/nginx.conf` to change `proxy_pass http://127.0.0.1:9999` to `proxy_pass http://127.0.0.1:8081` and start nginx with that config file. Confirm `curl http://localhost:8080` returns 200 before finishing.