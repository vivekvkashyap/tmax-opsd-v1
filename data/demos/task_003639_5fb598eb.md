Start by diagnosing the link failure: run `ldd /app/libsolver.so` to confirm `libhelpers.so.1 => not found`, then make it resolvable without modifying system paths — either set `LD_LIBRARY_PATH=/app/legacy_libs` in the environment that launches your Python server, or `patchelf --set-rpath /app/legacy_libs /app/libsolver.so` (install `patchelf` if needed) so `ctypes.CDLL` can load it. Do not try to recompile or replace the stripped binaries. Next, treat `/app/oracle_parser` as an opaque black box: call it with `subprocess.run([...], capture_output=True)` passing the exact raw path+query string as its single argument, then `json.loads` its stdout to get `target` and `weights` — never hardcode the parsing logic yourself, let the binary do it so you match its exact behavior. Then in Python, load `libsolver.so` via `ctypes.CDLL`, set `argtypes = [c_int, POINTER(c_int), c_int]` and `restype = c_int` on `run_solver`, build a `(c_int * len(weights))(*weights)` array, and call it. Implement the HTTP server with `http.server`'s `BaseHTTPRequestHandler` (or similar) bound exactly to `127.0.0.1:8080`, handling `GET /solve?...`, returning the required JSON with correct `Content-Type: application/json` header and status 200. Run it in the background and write its PID to `/home/user/server.pid`.
