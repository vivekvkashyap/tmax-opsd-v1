Reverse-engineer `/app/sensor_filter_x64` empirically before trusting any assumption about its logic: since it's UPX-packed and stripped, either `upx -d` it and disassemble, or simply black-box it by calling it as a subprocess with many float arguments spanning different fractional parts and decimal digits, recording exit codes, to find the pattern (e.g. it may depend on a specific digit of the value) — do this rather than hardcoding a guessed formula, since the reward depends on matching its exact behavior. Once confirmed, reimplement the equivalent check natively in Rust for performance (calling the binary per-request would be slow and fragile) — express the derived rule using integer arithmetic on the float's decimal representation to avoid floating-point rounding mismatches with the oracle. Structure the service with a web framework (axum/actix-web) exposing `POST /ingest`; parse each log line with a regex capturing timestamp/sensor/msg_id/temp; dedupe by first-seen `msg_id` using an ordered `HashSet`/`IndexSet`; filter via your reimplemented anomaly check (optionally parallelized with `rayon`); bucket valid readings by truncated-to-second timestamp and average per bucket; then walk every second from min to max timestamp, carrying forward the last known average into any second with no readings, and return the JSON array. Build with `cargo build --release` and run it in the background listening on `127.0.0.1:8080`.