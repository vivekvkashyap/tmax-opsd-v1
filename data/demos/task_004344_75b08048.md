Treat `/app/graph_oracle` purely as a black-box oracle you query, not something to reverse-engineer for a shortcut — you need to learn its output format empirically. Start by running it interactively with a handful of hand-crafted queries: a deliberate implicit cross join like `SELECT * FROM nodes n1, nodes n2;`, a proper explicit join like `SELECT * FROM nodes n JOIN edges e ON n.id = e.src;`, and a recursive CTE traversal, comparing the `EXPLAIN QUERY PLAN` text each produces. Note the pattern: a Cartesian product shows multiple `SCAN TABLE` lines with no accompanying `SEARCH TABLE` (i.e., no index-based lookup connecting the tables), whereas a well-constrained join/traversal shows `SEARCH TABLE` (using an index) for at least one side. Write `/home/user/sanitizer.py` to invoke the oracle safely via `subprocess.run([...], ...)` passing the query as a separate argv element (never via `shell=True` string interpolation, to avoid injection), capture stdout, and apply your learned heuristic (e.g. count `SCAN TABLE` vs `SEARCH TABLE` occurrences) to print exactly `ACCEPT` or `REJECT`. Handle oracle errors/non-zero exit gracefully (treat as REJECT or ACCEPT per what's safest) rather than crashing. Before finishing, construct and test additional varied cross-join and clean-join phrasings yourself to make sure your heuristic generalizes, since grading uses hidden corpora you won't see.
