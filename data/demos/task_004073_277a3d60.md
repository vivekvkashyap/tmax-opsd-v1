First recover the database: `sqlite3 data.db "PRAGMA wal_checkpoint(FULL);"` (or open it once with sqlite3 and issue a checkpoint) so the `-wal` file's uncommitted inserts get merged into `data.db` and become queryable. Then reverse-engineer the stripped binary: run `strings service_bin | grep -i SELECT` to pull out the hardcoded SQL query text directly from the binary's rodata section — this avoids needing a disassembler for the query itself. For the leak function, use `nm service_bin | grep -iE 'alloc|leak|unfree'` or `objdump -d service_bin | less` and scan exported/local symbol names for one matching the task's naming hint (contains words like "alloc"/"leak"/"unfreed") — do not guess, confirm it exists as a defined function symbol, not just a string reference. Create `/home/user/report/` first. Write only the exact function name (no extra text) to `leak_func.txt`. Finally run the exact extracted SELECT query against the checkpointed `data.db` with `sqlite3 -csv -noheader data.db "<query>" > /home/user/report/query_out.csv`, ensuring comma-separated, no header row.