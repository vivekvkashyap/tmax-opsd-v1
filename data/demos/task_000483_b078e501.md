Break this into independent, verifiable stages using only the allowed tools. Extract per-frame mean luma with ffmpeg's `signalstats` filter (e.g. `ffmpeg -i /app/pulsar.mp4 -vf signalstats -f null - 2>&1 | grep YAVG` or extract via `showinfo`/`ffprobe` frame stats), redirecting the numeric YAVG values in frame order to a text file — verify frame count and value range look sane before proceeding. Write the DFT in awk yourself: for each candidate bin k from 1 to N/2, accumulate real/imag sums `sum(x_t * cos(2*pi*k*t/N))` and `sum(x_t * sin(2*pi*k*t/N))` over all frames t, compute power = real^2+imag^2, and pick the k with max power among k>=1 (excluding the DC bin k=0) — do this as a nested double loop in awk, it's slow but N is small enough. Once you have your empirically-derived mean, amplitude, and frequency (don't hardcode assumed values — derive them from the extracted signal), generate 100,000 Monte Carlo samples per the given model formula using awk's `rand()`, bin both the raw empirical frames and your simulated samples into 10 equal-width bins spanning the empirical min/max, convert counts to probabilities, and write the simulated bin probabilities (fractions, one per line) to sim_dist.txt. Compute TVD as half the sum of absolute probability differences and report it alongside your derived frequency and stats in report.txt.