Reproduce each bug before patching it — run the app, feed it problem inputs, and read the actual traceback/crash rather than guessing. For `wav_parser.py`: a naive parser that assumes chunk order is exactly RIFF/fmt/data will break on files with extra chunks (e.g. `fact`, `JUNK`) inserted before `data`; fix it to loop over chunks generically — read each chunk's ID and size, and if the ID isn't one you recognize, seek/skip forward by that chunk's declared size (with padding for odd sizes) instead of assuming a fixed layout. For `analyzer.py`'s `trim_silence`/`find_silence`: a recursive per-sample-index implementation will blow Python's recursion limit on long files — rewrite it as an iterative loop (while/for) over the sample buffer instead of recursing. For the duration bug: don't accumulate a fixed increment per chunk (that's where float drift comes from) — compute duration as `total_frames / framerate` directly from the WAV header once. Fix the multipart handling in `app.py` by reading the traceback to see which parsing call fails, then correct it. Start Flask bound to `0.0.0.0:8000`, backgrounded, then POST `/app/evidence.wav` to `/analyze` and save the JSON response to the required path.
