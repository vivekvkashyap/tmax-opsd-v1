The core hazard here is naive CSV parsing: use Python's `csv` module (or pandas `read_csv`) to read each file, never split on raw newlines yourself, since `config_diff` fields contain embedded newlines inside quoted CSV fields that a line-by-line reader would corrupt. Process the files in `/home/user/raw_data/*.csv` in parallel with `concurrent.futures.ProcessPoolExecutor` (one worker per file is a natural split), having each worker return a list/DataFrame of rows with a new `diff_line_count` column computed as `config_diff.count('\n') + 1` for non-empty strings and `0` for empty strings. Concatenate all workers' results, then sort by `timestamp` ascending and `event_id` ascending as the tiebreaker. Group by `service_name` and compute the rolling average of `diff_line_count` with `min_periods=1, window=3` (e.g. pandas `.rolling(window=3, min_periods=1).mean()` per group, preserving the sorted row order within each group), rounding to 2 decimals. Before writing output, validate: no NaNs in `diff_line_count`/`rolling_avg_diff`, and all `rolling_avg_diff >= 0`; on failure print an error and `sys.exit(1)`. Write the final CSV to `/home/user/output/final_stats.csv` with columns in the exact specified order, creating the output directory first if needed.
