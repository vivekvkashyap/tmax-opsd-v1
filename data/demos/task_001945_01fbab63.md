These are two independent deliverables; build and self-test each before relying on a hidden test suite. For `check_tar.sh`, inspect archive member paths without extracting using `tar -tf "$1"` (or `tar --list`), then check each listed path: reject if it starts with `/` (absolute) or contains a `../` component anywhere (split on `/` and check each segment equals `..`, not just a naive substring match, since something like `foo/..bar` shouldn't false-positive but `foo/../bar` must) — print exactly `REJECT`/exit 1 or `ACCEPT`/exit 0. Build your own small test corpus first: craft a tar with an absolute-path member (`tar --transform` or add a file at an absolute path) and one with `../../etc/passwd`, plus a clean tar, and confirm your script classifies all three correctly before trusting it against the hidden corpus. For the thumbnail, extract the frame with `ffmpeg -ss 00:00:02 -i /app/demo_video.mp4 -frames:v 1 -q:v 2 <tmpfile>.jpg`, writing to a temp file in the same filesystem as the destination, then atomically place it with `mv` (rename is atomic only within the same filesystem) to `/home/user/thumbnail.jpg` — verify the result opens as a valid JPEG before finishing.