Work top-down through the pipeline and verify each stage in isolation before chaining them. First, transcribe `/app/alert.wav` with `whisper --model tiny.en` (install via pip) and print the raw transcript so you can see exactly what words appear — do not assume the wording, read it. Write a regex that captures the letter+digit-word error code and the word+digit-word node name from the fixed sentence pattern, then build a small word-to-digit lookup table (one, two, three, ... nine, etc.) to normalize spoken digits into numerals — apply it to each captured digit word independently rather than hardcoding the final string. Assemble the error code as `<Letter>-<digits>` and the node name as `<word> <digit>`, then look up that exact node name string in `/home/user/nodes.csv` (match case/spacing carefully) to pull `ip_address` and `owner`. Build the JSON payload with those four keys. Finally implement the HTTP server (Flask/FastAPI/http.server all work) bound to `127.0.0.1:8080` with a `GET /alert` route returning the JSON with `Content-Type: application/json` and status 200, and launch it detached (e.g. `nohup ... &`) so it keeps serving after your shell command returns.