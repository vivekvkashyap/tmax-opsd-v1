Build this as three independently-testable pieces before wiring them together. First bring up the Python file server (`python3 -m http.server 8080 -d /app/data/`) and Redis (`redis-server --port 6379`) using the helper scripts in `/app/scripts/`, and confirm both respond (`curl` the PDB file, `redis-cli ping`) before touching Go. Then write `/home/user/pipeline/main.go`: parse PDB `ATOM` lines yourself (fixed-column PDB format — atom name at a known column range, decide which rows are alpha-carbons by checking the atom-name field equals "CA", and x/y/z coordinates in their fixed column ranges) rather than assuming whitespace-splitting works, since PDB columns can be packed together. Compute consecutive Euclidean distances between CA atoms, apply a size-3 moving average with edge padding (reuse the first/last value at the boundaries), take the mean, then bootstrap-resample the smoothed signal (with replacement) `num_bootstrap_samples` times to build the sampling distribution of the mean, using goroutines to parallelize the resampling; take the 2.5/97.5 percentiles for the CI. Enforce the Bearer token check before any work (401 if missing/wrong), cache `[lower,upper]` in Redis under `spectro_ci_{pdb_id}` via the RESP protocol or a Go redis client, and script `/home/user/start_all.sh` to launch all three services in the background in the right order.