Process the file as a stream: open with plain file iteration (not `json.load` on the whole file) and for each line wrap the parse in `try: json.loads(line) except json.JSONDecodeError:` — catching only the decode error, not a bare `except:`, so you don't silently swallow other bugs. A line only counts valid if it both parses AND the resulting dict has an `"ip"` key; increment invalid_lines for either failure mode (decode error or missing key), valid_lines otherwise. Use a `collections.Counter` keyed on the ip value from valid lines only. For "top 3", sort candidates by `(-count, ip)` so ties break alphabetically ascending — a naive `Counter.most_common(3)` alone won't guarantee alphabetical tie-breaking, so re-sort explicitly. Write the final dict to `/home/user/summary.json` with exactly the three keys `valid_lines`, `invalid_lines`, `top_ips` in the specified schema, using `json.dump`. Test your script against a handful of manually crafted malformed-unicode lines first to confirm the exception path actually triggers before running it on the full file.
