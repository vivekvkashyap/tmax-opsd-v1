Use `pexpect` to drive the telnet session as a finite state machine: spawn `telnet localhost 5000`, then chain `child.expect(...)` / `child.sendline(...)` pairs matching each prompt exactly as specified (`Username: `, `Password: `, `edge-router# `, `edge-router(config)# `, `Commit successful.`). Read `v2.conf` with plain file I/O, split into lines, and iterate: send each line then `expect` the config prompt before sending the next — sending everything at once without waiting for the prompt echo is the most common failure mode since the mock server processes one line per recv. After `commit`, wait for the exact success message before issuing `exit` then `logout`, otherwise you may race the state machine and the JSON state file won't be written yet. Only after the script exits cleanly should you update the symlink — use `os.symlink`/`ln -sfn` pointing `/home/user/deployments/active` at `/home/user/deployments/v2.conf` (remove or overwrite the existing link rather than creating a link-inside-a-directory, which happens if you forget the `-f`/`-n` semantics). Verify success by checking the resulting `/home/user/router_state.json` reflects the lines you actually sent.