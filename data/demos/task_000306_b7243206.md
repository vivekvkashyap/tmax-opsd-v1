Chain three recovery steps before writing any code. (1) Transcribe `/app/voicemail.wav` to text — install and run a speech-to-text tool (e.g. `whisper`, or `ffmpeg` plus a STT library) and read the transcript carefully; it will describe how to build the zip password from a word in `/app/wordlist.txt` plus a suffix. (2) Use that instruction to construct the password, then try it (or brute-force across the wordlist with the derived suffix using `unzip -P` or `fcrackzip`) to extract `/app/bundle.zip`. (3) Read the extracted specification file closely and implement `/home/user/jwt_processor` to match it exactly — pay attention to precise error strings, exit codes, and JSON formatting (e.g. minified output, exact key replacement) since the grader does bit-exact byte comparison against a hidden reference across many fuzzed JWTs, including ones with `alg: none` (case-insensitive) and ones with invalid signatures. Test your script by hand-crafting a signed JWT, a none-alg JWT, and a tampered JWT and diffing your output against what the spec demands. Make sure the file is executable (`chmod +x`).