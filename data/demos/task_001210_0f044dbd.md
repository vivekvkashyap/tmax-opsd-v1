Build `/home/user/policy_enforcer.py` as three independent, testable pieces. For redaction, use a regex matching exactly four groups of four digits separated by hyphens (e.g. `\b\d{4}-\d{4}-\d{4}-\d{4}\b`) and substitute the literal string `[REDACTED]`, preserving every other character and line exactly — write output line-by-line to avoid accidentally altering whitespace/newlines elsewhere. For intrusion detection, scan each line's MESSAGE portion (after the `[LEVEL]` field) case-insensitively for the two exact substrings given in the spec, tally malicious-line counts per source IP, and include only IPs with count >= 3, sorted alphabetically as strings (not numeric IP sort). For password auditing, extract MD5 hashes only from `[DEBUG]` lines matching `Auth failed: hash=<hex>`, then for each candidate word in the wordlist compute `hashlib.md5(word.encode()).hexdigest()` and compare against each extracted hash, keeping only matches that actually crack — omit any hash with no wordlist match from the output. Write the redacted log to `/home/user/redacted_logs.txt` and the JSON report (with `blocked_ips` and `weak_passwords` keys) to `/home/user/security_report.json`, then diff/inspect your outputs against the raw log by hand to catch formatting drift.