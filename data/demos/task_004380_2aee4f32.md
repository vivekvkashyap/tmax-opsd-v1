Solve this as a pipeline: analyze, encrypt, lock down permissions, then generate policy. Write a Python script that reads `/home/user/logs/login_attempts.csv` with the `csv` module, groups rows by `ip_address`, counts rows where `status == "FAILED"` per IP, and keeps only IPs with a strict count > 3 (i.e., >= 4); collect all FAILED rows (not just counts) for those flagged IPs as dicts matching the CSV header, matching the CSV's original row order. Use `cryptography.fernet.Fernet` with the exact key bytes read from `/home/user/keys/audit.key` (read the file, don't invent a key) to encrypt the JSON-encoded list, and write the ciphertext to `/home/user/secure_archive/audit_report.enc`. Then apply exact permission bits with `os.chmod` or `chmod`: `0700` on the secure_archive directory, `0400` on both the encrypted report and the key file. For the network policy, sort the flagged IPs as plain strings (ascending, not numeric/IP-aware) and emit one `iptables -A INPUT -s <IP> -j DROP` line per IP into `/home/user/network_policy/block_rules.sh`, then `chmod 744` it — do not execute it.
