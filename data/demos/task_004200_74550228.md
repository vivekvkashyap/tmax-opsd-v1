Get the parameter K first: `cd /app/decoder_src && make` (or read the Makefile to find the right target), run the resulting binary against `/app/signal.wav`, and read whatever numeric value it prints/extracts as K — do not hardcode a guessed K, use exactly what the decoder outputs. Then implement Newton-Raphson for f(x)=x^3 - K*x + 1 purely in awk (fastest path to meeting the "under a second for 1000 inputs" requirement — spawning a subprocess per line in bash would be far too slow). For each guess x0, iterate x_{n+1} = x_n - f(x_n)/f'(x_n) where f'(x) = 3x^2 - K, stopping when |x_{n+1}-x_n| < 1e-7 or after 100 iterations; explicitly guard the case f'(x)==0 and emit the literal string "NaN" for that line instead of dividing by zero. Process the whole file in one awk pass over `/app/guesses.txt`, writing one root per line, same order, to `/home/user/roots.txt`. Before finishing, spot-check a handful of your computed roots by hand-verifying f(root)≈0, and check timing with `time` on the full 1000-line run.