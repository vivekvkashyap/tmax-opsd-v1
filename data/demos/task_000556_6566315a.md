Treat this as three independently verifiable subsystems and build/test them in isolation before wiring the end-to-end flow. (1) Nginx: write a `location /api/ { proxy_pass http://127.0.0.1:5000/; ... }` block in `/etc/nginx/sites-available/default` and reload nginx; test with curl against 8080 directly. (2) Flask (`/app/flask/app.py`): add JWT Bearer validation using the given secret `redteam_hunter_99` (standard HS256 decode/verify on the `Authorization` header, reject with 401/403 on failure), and add SFTP upload logic via `paramiko` using a fresh SSH keypair you generate for a `flask_svc` user — put the private key where Flask can read it and the public key in the SFTP server's `authorized_keys` for that user, and test the SFTP leg standalone before integrating. (3) Sanitizer (`/home/user/sanitizer.py`): since it will be fuzz-tested against corpora at `/app/corpora/evil/` and `/app/corpora/clean/`, don't hand-tune it against a couple of examples — implement full-decoding path-traversal detection (URL-decode repeatedly, normalize `..\`, overlong/percent-encoded slashes like `%2e%2e%2f` and `%c0%af`, and any `../`-equivalent after normalization counts as EVIL) and run it against every file in both corpora yourself, fixing false positives/negatives until 100% pass. Call this sanitizer from `app.py` before accepting uploads (403 on EVIL). Last, write `iptables` rules restricting 5000 and 2222 to localhost-only, save to `/home/user/firewall_rules.v4`, and test the full chain (upload through 8080 with a valid JWT lands in SFTP; a malicious filename gets 403) before finishing.
