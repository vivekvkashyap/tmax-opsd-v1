Apply the patch first with `patch -p0 < /home/user/patch.diff` (or `-p1`, check which strips correctly) from inside `/home/user/waf_project`, then read the resulting `src/parser.c` closely — the patch likely adds a `strdup`/`malloc` allocation on a code path but only frees it on some returns, leaking on others; trace every `return` statement in the function and ensure the allocated pointer is freed on each one (or restructure with a single cleanup point) before returning. Implement `tests/mock_request.c` by writing `setup_mock_request` to assign the four given string literals into the `HttpRequest` fields per `include/waf.h`'s struct layout — since these are static literals, plain assignment (not strdup) is fine. Write `ci_pipeline.sh` to run the given ASan-enabled gcc build command, execute `tests/run_tests`, and check its exit code — ASan will abort with nonzero exit and a leak report if you missed a free path, which is your correctness signal; only build the tar.gz of `src` and `include` when tests pass, otherwise exit nonzero. Iterate: run the script, read ASan's leak backtrace if it fails, and fix the corresponding return path.
