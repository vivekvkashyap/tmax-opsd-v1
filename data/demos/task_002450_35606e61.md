Separate the three concerns and validate each independently before wiring them together. First, OCR `/app/config.png` (`tesseract /app/config.png -` or similar) and inspect the raw text output carefully — OCR often mangles digits/punctuation, so sanity-check the extracted `K_FOLDS=` integer and comma-separated `LAMBDA_VALS=` floats against what looks plausible, and parse them programmatically rather than hardcoding whatever you first read. Second, write `engine.c` against GSL: load the CSV columns, compute the Pearson correlation matrix with GSL's stats functions (or a manual implementation using GSL vectors), then implement k-fold cross-validation using the OCR'd `K_FOLDS` and try each OCR'd lambda, computing the dummy MSE formula given in the spec per fold and averaging to find the minimizing lambda — do this generically off the parsed values, not off assumed literals. Third, expose the two GET routes on `127.0.0.1:8080` (a small Flask wrapper calling out to the compiled C binary via subprocess and parsing its output into JSON is simplest, or a C HTTP server with libmicrohttpd) and start it in the background before finishing, confirming both endpoints respond with `curl` yourself.
