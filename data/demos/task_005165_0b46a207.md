Treat `/app/pathfinder` as a black box and derive the corruption rule empirically rather than assuming it — do not try to reverse-engineer or read the stripped binary's internals directly. Instead, systematically probe it: construct small test query CSVs with varying node-name patterns (different substrings, lengths, characters) against `/app/data/graph.csv`, run `/app/pathfinder --explain --graph /app/data/graph.csv --query <file>` on each, and grep the output for the exact strings `[WARN] INDEX_STATE: CORRUPTED` vs `[INFO] INDEX_STATE: VALID`. Vary one property at a time (e.g. test node names with/without a particular token) to isolate what triggers the WARN state, since a real routing engine bug is usually a specific pattern in the source/target node identifiers, not something size- or graph-dependent. Once you've confirmed the trigger pattern reliably via repeated experiments, write `/home/user/sanitize.sh` to: read the input CSV argument, detect and pass through the header line unchanged if present, and for every data row either call `pathfinder --explain` per-row (writing a tiny one-row query CSV to a scratch temp file, e.g. under `mktemp`/`/tmp`, and cleaning it up so no leftovers pollute stdout) or, once you're confident in the exact substring rule, filter directly with a text match — but validate any shortcut against the binary's actual `--explain` output on a range of edge cases before trusting it. Make the script executable.