Split this into the validator and the sync pipeline, and get the validator solid first since it feeds the second half. For `/home/user/validate_subs.sh`, process the file line by line (`jq -c '.' 2>/dev/null` per line is a good starting filter for basic JSON validity), then add explicit checks beyond jq's own leniency: reject lines missing `seq` or `text`, reject `seq` that isn't a positive integer, and specifically hunt for malformed `\u` escapes — a `\u` not followed by exactly 4 hex digits, or a high surrogate (`\ud800`-`\udbff`) not immediately followed by a matching low surrogate (`\udc00`-`\udfff`) — using grep/awk regex over the raw line text before trusting jq's parse (jq may silently accept or choke inconsistently on bad surrogates, so don't rely on it alone). Print only valid lines to stdout, exit non-zero if the file had any invalid line. For sync: run `ffmpeg -i /app/reference_ui.mp4 -filter:v "select='gt(scene,0.3)',showinfo" -f null -` and parse the `pts_time` values from stderr to get real scene-change timestamps (don't hardcode timing) — combine with 0.000000 as the implicit start. Filter+sort `raw_translations.jsonl` through your validator by `seq`, zip each surviving translation to a window `[change_i, change_{i+1})`, cap any window at 5.0s duration, and emit `/home/user/final_subtitles.srt` in standard numbered `HH:MM:SS,mmm --> HH:MM:SS,mmm` format.
