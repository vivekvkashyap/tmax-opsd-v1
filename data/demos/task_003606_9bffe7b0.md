Fix the race condition by giving each background `process_chunk.sh` invocation its own unique temp output (e.g. named by PID or input filename) instead of appending directly to the shared aggregated.dat, then have run_pipeline.sh `wait` for all jobs and concatenate the temp files in a deterministic order afterward — or alternatively serialize the shared-file appends with `flock`. For calculate_convergence.sh, the root cause is that `bc` defaults to `scale=0` (integer truncation), which breaks the iterative comparison; prefix every `bc` expression with `scale=6;` (e.g. `echo "scale=6; val / 1.05" | bc`) for both the division and the difference check. Do not alter the underlying formula, only add scale settings and fix the concurrency bug. Re-run the full pipeline end-to-end after both fixes and confirm the loop actually converges (exits before hitting the iteration cap) before writing the final numeric value to /home/user/convergence_result.txt.
