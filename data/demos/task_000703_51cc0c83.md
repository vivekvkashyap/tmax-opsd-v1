Stream the gzip directly into a JSON parser rather than writing an intermediate file: pipe `zcat`/`gunzip -c` into `jq` (e.g. `gunzip -c /home/user/quotas.json.gz | jq -r '.[] | select(.used > (.limit*0.9)) | .user'`) so nothing touches disk. Use a strict `>` comparison, not `>=`, since the spec says "strictly exceeding" — verify boundary users (exactly 90%) are excluded by hand-checking the arithmetic rather than assuming. For the concurrency requirement, wrap only the append step in a subshell/function that acquires the lock with `flock -x /home/user/warnings.lock -c '... >> /home/user/warnings.log'` (or `exec {fd}>/home/user/warnings.lock; flock -x "$fd"`) so parallel invocations of the script serialize their writes instead of interleaving. Make the script `#!/bin/bash`, `chmod +x` it, and actually execute it once to confirm `/home/user/warnings.log` is created with the right usernames before considering the task done — don't just eyeball the script logic.
