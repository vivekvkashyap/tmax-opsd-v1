Treat this as several distinct engineering artifacts that must each be independently correct, then wired together. Start with the .proto definition for the AudioRouter service and generate the C++ stubs via protoc/grpc plugin before writing server logic. For the router itself, don't just do string matching — parse the path into segments, validate the fixed prefix `/v3/api/audio/`, restrict {action} and {format} to the exact allowed enums, and explicitly reject any path containing `../`, `%00`, or unrecognized segments; parse query parameters after `?` into a map by splitting on `&` and `=`. Build the reject/accept logic as a single reusable function so both the gRPC server and the verify_router test binary call the same code path — that avoids drift between what's tested and what's served. For verify_router, read both corpus files line by line, run every line through the router, and count acceptances vs rejections, printing the two required lines. For transcription, don't hardcode any output — shell out to `whisper` (via `system`/`popen`/`exec`) on the actual wav file path so it works generically, and pass through its captured stdout as the result. Compile everything via a Makefile, start the server on port 50051, then run verify_router and redirect output to the log file.
