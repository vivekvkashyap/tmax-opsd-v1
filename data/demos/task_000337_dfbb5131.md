Decode `payload.txt` with `base64.b64decode` then `json.loads` to pull out `source_ip` — don't hardcode it, read it from the file each time. For certificate validation, don't just eyeball the cert fields; actually run the verification chain with OpenSSL, e.g. `openssl verify -CAfile /home/user/traffic_data/rootCA.crt /home/user/traffic_data/client.crt` via `subprocess.run(..., capture_output=True)`, and check both the return code and stderr/stdout for "OK" versus an error like "unable to get local issuer certificate" — a cert signed by a different (fake) CA will fail this check even though it looks superficially valid. Only when validation fails should you generate the firewall rule: write `/home/user/traffic_data/firewall_rule.sh` containing exactly one line in the form `iptables -A INPUT -s <source_ip> -j DROP` using the IP you extracted from the decoded payload (not a placeholder), then `chmod +x` it. Keep the script to that single command with no extra echo/comments so the exact-match check on its contents passes.