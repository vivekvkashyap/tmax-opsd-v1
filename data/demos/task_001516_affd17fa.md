Debug this like a real incident: reproduce the failure first (`cd /home/user/project && ./build.sh`), then use `strace -f python3 precompute.py 2>&1 | grep -i -E 'open|ENOENT'` to see exactly which file path the script tries to open and fails on — a common cause is a trailing newline or stray character being carried into a file path string read from a config file, so check whether the config value needs `.strip()`. Next, inspect the custom parser that extracts floats from the data file: test it against the actual data file's edge cases (trailing commas, blank/empty tokens between delimiters, mixed comma/newline separators) and make it robust to empty fields rather than crashing on `float('')`. Finally, look at the variance calculation: if it uses the naive sum-of-squares formula `E[X^2] - E[X]^2`, that will lose precision catastrophically when the data has a large common offset — replace it with a numerically stable method (subtract the mean first, i.e. two-pass, or use Welford's online algorithm, or just call `statistics.variance()`/`statistics.pvariance()` from the standard library, choosing whichever matches "sample variance" i.e. divide by n-1). Only edit `precompute.py`; rerun `./build.sh` and confirm it exits 0 with the correctly formatted 6-decimal output.