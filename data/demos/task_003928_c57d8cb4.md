Work through the pipeline in dependency order and verify each stage before moving to the next. First run `/app/tests/regression.sh` to see it fail, then open `/app/src/primer_score.c` and find the mismatch branch that adds 2 to the score instead of subtracting — change `score += 2` to `score -= 2` in the mismatch case (matches still add 1), recompile to `/app/bin/primer_score`, and rerun the regression script until it reports pass. Next bring up Redis: start `redis-server` bound to `127.0.0.1:6379` (background it), then use `redis-cli SET motif_threshold 15` and confirm with `GET`. Then edit `/app/service/app.py`: replace the hardcoded wrong binary path in the `subprocess` call with `/app/bin/primer_score`, keep the Redis lookup and JSON response shape as-is, and launch it in the background with `python3 app.py &` (or `nohup ... &`) bound to `0.0.0.0:8080`. Validate the whole chain yourself with a real `curl -X POST http://127.0.0.1:8080/score -H 'Content-Type: application/json' -d '{"target":"ATGCGTAC","primer":"ATGC"}'` before finishing, since the automated verifier will do exactly this. Leave both processes running and write the literal word `READY` into `/home/user/deployment.log` as the last step.
