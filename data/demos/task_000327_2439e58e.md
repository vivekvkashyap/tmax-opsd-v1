Work this as a layered extraction pipeline, verifying each stage's output before moving to the next. First, use `objcopy -O binary --only-section=.hidden_payload suspicious.elf <out>` (or `readelf`/`objdump` to locate the section, then dump its bytes) to pull the raw `.hidden_payload` contents — confirm it looks like base64 text before decoding. Base64-decode it, then XOR every byte of the result against `0x5A` and check that the output is printable ASCII (a JWT secret string) before using it further — if it's garbled, revisit the extraction/decoding step rather than guessing. Next, parse `access.log` line by line with a regex capturing timestamp, request path, and source IP, and separately extract any `Bearer <token>` value from the line; for each token, attempt `jwt.decode(token, secret, algorithms=["HS256"])` inside a try/except, keeping only entries where verification succeeds (reject on `InvalidSignatureError` or any decode failure). For each surviving entry, redact the IP's last octet with `XXX` and emit `{timestamp, endpoint, redacted_ip}` in log order to `/home/user/investigation/report.json` as a JSON array, and double check the JSON is syntactically valid and contains only the successfully-validated entries.