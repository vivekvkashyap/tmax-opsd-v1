Break this into three independently-testable stages. First, extract the key: use `cv2.VideoCapture` (or `ffmpeg`) to pull all 128 frames of `/app/key_feed.mp4`, convert each to grayscale, compute its mean pixel intensity, and threshold at 128 to get one bit per frame in frame order; pack every consecutive group of 8 bits (frame 0 = MSB) into one byte using bit-shifting, producing a 16-byte key — print/inspect the resulting bytes yourself to sanity-check they look like plausible key bytes (not all zero/all one) before trusting the extraction logic. Second, for each `.gcode.enc` file, XOR byte `i` with `key[i % 16]`, then feed the result through `zlib.decompress()` inside a try/except — treat any exception as corrupt, and separately reject files whose decompressed bytes don't start with `b"; BEGIN GCODE"` even if decompression succeeded. Third, build the HTTP server (Flask/http.server) to serve only the validated files at `/artifacts/<name-without-.enc>`, returning 404 for anything not in your validated set — test each endpoint with `curl` locally, including one you expect to be corrupt, before leaving the server running.