Start by extracting the protocol text from the image at /app/protocol.png — use OCR (e.g. `tesseract`, or a Python OCR/vision library) rather than guessing; read it carefully since it defines the exact graph-construction rule, the filtering step, and the statistical formula you must implement, and don't assume a standard bioinformatics pipeline. Once you have the protocol, write a C program that: parses the FASTA file manually (track sequence boundaries on lines starting with '>', concatenate sequence lines between headers), builds the graph exactly as the protocol specifies over all sequence pairs, finds the connected component described (a simple BFS/DFS over an adjacency list is sufficient, no external graph library needed), extracts the two per-node features named in the protocol for members of that component, and computes the specified correlation/statistic using the formulas given. Compile with `gcc -lm`, run against /app/data.fasta, and write only the final float to /app/result.txt. Cross-check your C output against a quick independent calculation (e.g., a short Python snippet) before finalizing, since a single indexing or edge-inclusion bug (like using < vs <=) will shift the connected component and thus the final statistic.
