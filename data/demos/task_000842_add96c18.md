Split this into a strict validator you can unit-test in isolation, then a daemon that reuses that exact logic — don't duplicate slightly-different filtering rules or the adversarial corpus tests will fail. In `sanitizer.py`: read input JSONL line by line, wrap `json.loads` in try/except so malformed JSON is silently skipped, and strictly validate all four fields (UUIDv4 via `uuid` module with version check, timestamp via `datetime.fromisoformat`, host via `re.fullmatch(r'^[a-zA-Z0-9\-]+$', host)`, cpu_usage numeric with `0.0 <= x <= 100.0`), rejecting records missing keys or with wrong types. Import this same validate function into `etl_daemon.py` rather than reimplementing it. For the daemon: poll the emitter every 1s, sanitize, then dedupe via Redis `SET NX EX 3600` keyed by record id so retried ids are skipped; for rolling stats use a per-host Redis sorted set keyed by timestamp epoch, `ZREMRANGEBYSCORE` to drop entries older than 5 minutes before averaging, POST the average to the sink, and log counts to `pipeline.log`. Start the daemon in the background, then touch `ready.flag`.