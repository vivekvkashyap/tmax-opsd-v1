Reverse-engineer the encoder before writing any Rust: since the binary is stripped, don't try to disassemble it blindly — instead treat it as a black box and probe it. Run it with a few known inputs of your choosing (e.g. `./encoder "a"`, `./encoder "ab"`, `./encoder "AA"`) and compare the hex output byte-by-byte against the ASCII codes of your input; a single fixed-XOR-key cipher will show up as a constant XOR delta between each input byte and its corresponding output byte pair (`int(hex_byte,16) ^ ord(input_char)`), and that delta should be identical across all your probes — that constant is the key. If it isn't constant, consider other simple transforms (byte addition, reversal) via more probes; only use `objdump`/`strings` as a fallback if black-box probing is ambiguous. Once decoding is confirmed, write the Rust analyzer to hex-decode each payload byte, XOR-decode it with the discovered key, join into a plaintext path, and resolve it against the sandbox root by manually normalizing `.`/`..` components (don't rely on `std::fs::canonicalize`, since these paths don't exist on disk — implement logical path resolution by walking components and popping on `..`). Compare the resolved path's prefix against `/var/www/uploads` and log only escapes in the exact specified format.
