First locate the bug: grep the vendored source under `/app/auth_proxy-1.2.3` for where it reads the incoming HTTP header used for bearer-token auth — look specifically for a misspelled header name (a common British/American spelling variant of "Authorization") passed to `self.headers.get(...)` or equivalent, and correct it to the standard header name so token extraction actually works. Verify the fix by grepping again or briefly running the server standalone against a curl request before wiring up the deployment script. For `/home/user/deploy.sh`, make every step idempotent: kill any existing `auth-proxy` process by matching its process name/pattern (check first, don't error if absent), regenerate `/home/user/users.json` unconditionally (safe to overwrite), create the venv only if it doesn't already exist (`[ -d /home/user/venv ] || python3 -m venv ...`), reinstall the package each run so fixes propagate, and always relaunch the service backgrounded via the venv's bin directory with the exact CLI args specified. For `/home/user/check_health.sh`, use `curl -H "Authorization: Bearer super-secret-99" http://127.0.0.1:9090/api/whoami`. Run `deploy.sh` and confirm the service is listening before finishing.
