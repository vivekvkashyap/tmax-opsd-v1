Build and verify each shared library independently before writing the Python glue, since ABI mismatches are easy to introduce silently. Compile each with a SONAME/output name matching the version file exactly: `gcc -shared -fPIC -o /home/user/lib/libdata.so.1 /home/user/src/v1/libdata.c` and `... -o /home/user/lib/libdata.so.2 /home/user/src/v2/libdata.c` (create `/home/user/lib/` first). In `/home/user/app.py`, load the library via `ctypes.CDLL(sys.argv[1])`, call `get_version` with `.restype = ctypes.c_char_p`, decode it, and parse the major version number from the semver string (split on `.`, take the first component as an int — don't assume it's always a single digit). Branch on major version: for major==1, set `encode_data.argtypes` to `(c_char_p, c_char_p)`, encode the input string to UTF-8 bytes, pass a `create_string_buffer(256)` as output, and decode the result as UTF-8. For major>=2, set `encode_data_wide.argtypes` to `(c_wchar_p, c_wchar_p)`, pass the Python string directly (ctypes handles wide-char marshaling), and use `ctypes.create_unicode_buffer(256)` for the output, reading `.value` afterward. Print the decoded string with a trailing newline. Run against both libraries with `"TestString123"`, redirecting stdout to `/home/user/result_v1.txt` and `/home/user/result_v2.txt`, and inspect the C source's actual per-character transform to understand what output you should expect rather than assuming.