For integrity verification, don't just eyeball the checksum file — parse `/home/user/audit/checksums.sha256` (standard `sha256sum` output format: `<hash>  <filename>`) and for each entry recompute the SHA256 of the corresponding file in logs/ (e.g. `sha256sum -c` against the checksum file, or manually hash and compare); collect the filenames whose computed hash differs, sort them alphabetically, and write one per line to compromised.txt. For the certificate, use `openssl req -x509 -newkey rsa:2048 -nodes -keyout key.pem -out cert.pem -days 30 -subj "/CN=vulnerable.local"` so the CN matches exactly and the key is unencrypted (-nodes). For the vulnerable script, scan permissions in scripts/ (e.g. `find scripts/ -perm -o+w` or check the "others" write bit of each file's mode) to identify the world-writable one — don't assume which file it is, detect it programmatically — then append the payload line with `>>` (never truncate/overwrite the file) and leave its permission bits untouched. Finally execute that script directly to confirm pwned.txt is created with the exact expected content.
