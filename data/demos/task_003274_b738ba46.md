For the CSV cleanup, don't try to parse it line-by-line naively — embedded newlines inside the text column mean a "row" can span multiple physical lines. Instead, use a proper CSV-aware tool (e.g. Python's `csv` module, or `awk`/`mlr` if you're confident in quote handling) rather than plain `sed`/`grep` splitting on `\n`, and validate row boundaries by checking that each reconstructed row starts with an integer ID and timestamp pattern as stated. Normalize the text column to lowercase, then deduplicate by hashing the normalized text (e.g. `sha256` of the string) and keeping only the first occurrence per hash, preserving original row order otherwise. For the audio, use `ffmpeg` with `-ar 16000 -ac 1` for resampling/mono-mixing, and the `silenceremove` filter with parameters that strip silences longer than 1.0s down to a 0.1s gap — study `ffmpeg -h filter=silenceremove` for the exact parameter names (stop_periods, stop_duration, stop_threshold, leave a residual via multiple silenceremove passes or window) rather than guessing; a slightly wrong threshold or duration will fail the downstream MSE comparison against the reference. Test the audio filter on a short clip and listen to / inspect duration changes before running on the full file.
