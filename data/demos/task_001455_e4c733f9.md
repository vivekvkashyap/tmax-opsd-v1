Build and verify each layer independently before wiring them together in deploy.sh. For the timezone-stamped log line, don't rely on the shell's ambient TZ inside Go — load the location explicitly with `time.LoadLocation("Europe/Copenhagen")` and format `time.Now().In(loc).Format(time.RFC3339)` so the offset is correct regardless of the process environment. For TLS, generate the self-signed cert/key with `openssl req -x509 -newkey rsa:2048 -nodes -keyout key.pem -out cert.pem -days 365 -subj "/CN=127.0.0.1"` (add `-addext "subjectAltName=IP:127.0.0.1"` if your Go TLS client/verifier checks SAN), and serve with `http.ListenAndServeTLS` bound to `127.0.0.1:9443`. For `/notify`, read and JSON-decode `settings.json` at request time (not just at startup) so the admin_email value is sourced from the file, then write the exact literal template with that email substituted — match whitespace/newlines precisely. In deploy.sh, compile with `go build -o /home/user/app/server`, `export TZ=Europe/Copenhagen` before backgrounding the server, `sleep 2`, then `curl -k` the `/notify` endpoint — check the server process actually started (e.g. poll `/health`) before curling notify to avoid a race.
