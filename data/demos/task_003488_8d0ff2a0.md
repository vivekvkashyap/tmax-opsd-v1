Do this as a short shell pipeline per model version rather than one monolithic script, and validate intermediate numbers by hand. First filter: `awk -F, 'NR>1 && $2=="A" && $3 ~ /^[0-9.]+$/ {print $3}' inference_logs.csv` (adapt the numeric check to reliably exclude the literal "NaN" string and any non-numeric rows) to get a clean list of latencies per version. Then compute N, mean, sample variance (sum of squared deviations divided by N-1, not N), sample stddev, SEM = stddev/sqrt(N), and CI = mean ± 1.96*SEM — you can do this arithmetic in `awk` or pipe values into `python3 -c` for precision, but be careful to use N-1 (sample, not population) as the task specifies. Round every output number to exactly 2 decimal places at the very end (not during intermediate math, to avoid compounding rounding error), and format them as JSON string values (quoted), matching the exact key structure given, using something like `jq -n` or a small Python snippet to assemble `/home/user/experiment_metrics.json` so the formatting is exact. Recompute by hand for one model version to sanity-check your pipeline's numbers before trusting the script for both.
