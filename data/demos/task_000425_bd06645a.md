Install what's needed first (`apt-get install -y libfftw3-dev libsndfile1-dev build-essential` or equivalent) and confirm the WAV can be read (`sndfile-info /app/data/signal.wav` or similar) before writing C++. In `decoder.cpp`, load the WAV with `libsndfile`, then slide a non-overlapping window of exactly `0.1s * 16000Hz = 1600 samples` across the signal; for each window compute an FFT (fftw3's `fftw_plan_dft_r2c_1d` or similar) and find the magnitude at the two target-frequency bins closest to 1000 Hz and 2000 Hz (bin index ≈ `freq * window_size / sample_rate`) — decide the bit by comparing which bin has more energy, don't just threshold one bin, since noise can shift absolute magnitude. Write one `0`/`1` character per line to `/home/user/decoded.txt`, one line per window, in signal order — get the window count right (total_samples / 1600) so you don't drop or duplicate the final bit. Because the signal is heavily noisy, validate your bin-selection logic against a few windows manually (print the two candidate magnitudes) rather than trusting a single naive threshold; you need ≥95% accuracy against the hidden ground truth so a robust dominant-bin comparison matters more than precise frequency estimation.
