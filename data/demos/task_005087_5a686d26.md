Build the script around `sqlite3 :memory:` (or a temp .db file) using `.mode csv` and `.import` to load `nodes.csv` and `edges.csv` as tables, then express the pattern match as a single SQL query rather than hand-rolled loops: self-join `edges e1` (A->B) to `edges e2` (B->C) on `e1.target_id = e2.source_id`, join `nodes` three times (aliased for A, B, C) to constrain `entity_type` to Retail/Intermediary/Offshore respectively, and add `e1.timestamp < e2.timestamp`. Compute `total_amount = e1.amount + e2.amount` in the SELECT, and use `RANK() OVER (PARTITION BY nA.region ORDER BY total_amount DESC) AS region_rank` as a window function over that same result set (wrap the join in a CTE/subquery so you can filter `WHERE region_rank <= 3` afterward, since window function results can't be filtered in the same SELECT they're defined in). Run this via `sqlite3 -header -separator '|' dbfile "SELECT ..."` and pipe the output through `awk -F'|' 'BEGIN{OFS=","}{$1=$1; print}'` (or `sed 's/|/,/g'`) to convert to comma-separated, replacing the header row with the exact required header string, and redirect to `/home/user/flagged_paths.csv`.
