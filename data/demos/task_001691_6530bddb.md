Root-cause the bug before patching: the naive `E[X^2] - E[X]^2` variance formula in `calculate_naive_std` can go slightly negative due to floating-point cancellation on near-identical frames, and `math.sqrt` throws on negative input — reproduce this first against a very static/low-motion frame range to confirm the exact failure. Fix it by either switching to `numpy.var(diff)` (which is more numerically stable) or explicitly clamping with `max(variance, 0.0)` right before the sqrt, and add an assertion (`assert variance >= -1e-6 and not math.isnan(variance)`, then clamp) immediately before the sqrt call as the task requires — don't just silently clamp without the validation step. Keep the rest of the endpoint logic (frame reading via OpenCV, `X-Ops-Token` header check, JSON response shape) intact. After patching, install dependencies, start uvicorn on `0.0.0.0:8080` in the background, and verify both a normal frame-range request and a deliberately static/low-motion range return 200 with a valid float instead of a 500, using `curl` with the required header.
