Fix the Nginx config first: the current `location /api/ { proxy_pass http://127.0.0.1:8080/api/; }` proxies back to itself (an infinite loop) — change the `proxy_pass` target to the sanitizer's address, `http://127.0.0.1:8081/api/`, keeping the trailing slash so the `/api/` prefix is preserved/stripped consistently. Then implement `/home/user/app/sanitizer.py` (Flask is simplest) with a `POST /api/patch` route: parse the JSON body as a list of RFC-6902 operations, and for each operation's `path`, reject (403) if the path equals `/id`, `/role`, or `/is_admin`, OR starts with one of those followed by `/` (e.g. `/role/0`) — do this with a simple prefix check per protected field rather than a naive substring match, to avoid false positives like `/roles` or `/identity`. If no operation is protected, forward the exact original JSON body via `requests.post` (or `urllib`) to `http://127.0.0.1:8082/api/patch`, and relay the upstream status code and body back verbatim. Update `/home/user/app/start.sh` to also launch your sanitizer, then run it and manually test both a clean and an evil payload through `curl http://localhost:8080/api/patch` to confirm 200 vs 403 before considering it done.
