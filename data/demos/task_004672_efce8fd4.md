Read `waf_eval.py` and `config.ini` line by line before writing anything — the token check and sandbox are both directly derivable from source, this isn't a black box. Reproduce the exact token derivation the script uses (it MD5-hashes the `Cookie` header value concatenated with the salt from config.ini's `[security]` section) so your generator computes `hashlib.md5((cookie+salt).encode()).hexdigest()` for whatever Cookie value you choose. For the sandbox, note it only nulls `__builtins__` in the globals dict passed to `exec`, which is a well-known incomplete restriction: builtins are still reachable indirectly (e.g. through an object's class hierarchy, a generator/comprehension's own scope, or by walking `().__class__.__bases__` / `__subclasses__()` to reach a class like file or subprocess-capable objects, or via exception objects' `__traceback__`/frame introspection) — use one of these standard `__builtins__=None` escape patterns to regain access to `open` (or equivalent) and write the literal string to `/home/user/pwned.txt`. Have `exploit_generator.py` assemble both pieces into `/home/user/exploit.json` matching the required schema, then test end-to-end against the real `waf_eval.py`.
