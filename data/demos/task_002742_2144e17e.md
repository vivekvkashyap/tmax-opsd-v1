Locate the ownership bug precisely: `CString::new(res).unwrap()` creates a local `CString` whose backing memory is freed when it goes out of scope at function return, so returning `c_res.as_ptr()` hands the caller a dangling pointer. The fix is to transfer ownership across the FFI boundary with `c_res.into_raw()`, which returns a raw pointer while leaking the Rust-side allocation deliberately (the C/Python caller now owns it) — since the task doesn't require a matching free function, just make the pointer valid for the caller to read via ctypes. Rebuild with `cargo build --release` inside `/home/user/engine` and confirm `target/release/libengine.so` exists.

In `api_test.py`, load the library with `ctypes.CDLL(...)`, and critically set `lib.encode_rle.argtypes = [ctypes.c_char_p]` and `lib.encode_rle.restype = ctypes.c_char_p` before calling — without correct `restype`, ctypes will misinterpret the returned pointer. Implement the pure-Python RLE straightforwardly (itertools.groupby or a manual scan) and assert both outputs are identical on the actual bytes of `data.txt` (careful with UTF-8 multi-byte characters — decide up front whether RLE runs over Unicode codepoints or raw bytes, and make sure both implementations agree on that unit). Time 1000 iterations of each with `time.perf_counter()`, write the JSON report, then wire it all together in `ci_run.sh` with explicit exit-code checks after each step (`set -e` or manual `$?` checks) so a build or test failure propagates to exit code 1.