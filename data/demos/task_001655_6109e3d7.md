Build the Git plumbing first, then the operator logic, then wire them together — test each piece before chaining. Create the bare repo (`git init --bare`) and a clone from it. Write `operator.py` so that when invoked (with no special args, since the hook calls it directly) it uses something like `git --git-dir=... archive master | tar -x -C <tmpdir>` to materialize the latest commit's tree, then globs `*.yaml` in that tree's root and parses each with a YAML library (install `pyyaml` if missing). Build the `cluster_state.json` dict keyed by each manifest's `name` field holding the full parsed dict, and write it with `json.dump`. For the profile file, iterate every deployment's `env` dict and format each entry into an `export K8S_ENV_<NAME>_<KEY>="<VALUE>"` line, uppercasing the deployment name consistently, truncating and rewriting the file each run (not appending across runs). For `apply_containers.sh`, start it with `set -eo pipefail`, then loop `replicas` times per deployment emitting the exact echo-to-log line format specified, joining env keys with commas in their original order. Make the `post-receive` hook just exec the operator script, chmod both scripts executable, then commit+push the test YAML and manually run the generated apply script.
