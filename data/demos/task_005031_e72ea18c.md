Start by OCRing the memo: run `tesseract /app/policy.png stdout` and read the output carefully to identify the three policy facts (the legacy encoding name, the mandatory filename prefix, and the exact malicious signature string) — treat these as data you extract, not something to guess or hardcode from assumption. Build backup_filter.py to open the target file in binary mode, decode leniently (or just search bytes) for the exact malware signature string, and `sys.exit(1)` if found, `sys.exit(0)` otherwise; since it will be run against a large hidden corpus of clean/evil files, make the check a simple, exact substring match with no false positives/negatives (avoid regex escaping issues since the signature contains brackets). For extraction: concatenate archive.zip.NNN parts in numeric order into one file (e.g. `cat archive.zip.* > combined.zip` after confirming the split naming matches, or use `zip -s 0 ... --out combined.zip` then unzip), extract the nested tarballs recursively with `tarfile`, and for every extracted file invoke your own filter script as a subprocess to decide keep/delete. For files that pass, decode their bytes using the exact legacy encoding name you OCR'd (Python's `codecs`/`.decode(encoding)`) and re-encode as UTF-8, rename by replacing spaces with underscores and prepending the OCR'd prefix, then write to /home/user/processed_backup/. Test backup_filter.py manually against a few crafted clean/malicious files before running it at scale.
