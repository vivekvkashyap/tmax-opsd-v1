Do the three objectives independently since they don't share state except the recovered employee ID. Objective 1: transcribe /app/whistleblower.wav (e.g. via a speech-to-text CLI tool available in the environment) and read out the specific employee ID mentioned — don't assume a value, extract it from the actual transcript. Objective 2: use that ID to query /app/trading/users.db with a SQL join between employees and departments (`SELECT ... FROM employees JOIN departments ON ...`) to get name/department_name/risk_level, and separately stream /app/trading/executions.jsonl filtering rows matching that employee_id, summing the `amount` field for total_trade_volume and counting rows where `is_flagged` is true — build this as a small script (Python's sqlite3 + json modules are natural) and write the exact schema/field names/types requested to /home/user/audit_report.json. Objective 3: for query_validator.py, load the JSON payload, walk all string values under `filters`, and check each against a set of SQLi indicator patterns (tautologies like `' OR '1'='1`, `UNION SELECT`, comment markers `--`, stacked queries `; DROP`) case-insensitively; print REJECT/exit 1 on any match, else ACCEPT/exit 0 — test against both /app/corpora/evil and /app/corpora/clean before finishing to catch false positives/negatives on legitimate complex filter values.
