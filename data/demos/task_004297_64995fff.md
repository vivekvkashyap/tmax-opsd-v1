First recover the dictated sequence: convert `/app/dictation.wav` if needed (`ffmpeg` to a whisper-friendly format/sample rate) and transcribe with `whisper` (or another ASR tool available); since the speaker is spelling individual letters, post-process the raw transcription to strip punctuation/spaces and normalize to a single uppercase run of amino-acid letter codes — inspect the raw ASR output carefully, since letter-dictation often gets transcribed with commas/periods/spaces between letters that you must join together, and verify each recovered character is one of the 20 standard amino-acid codes. Separately parse `/app/reference.fasta` (skip the header line) to get the reference sequence. For each sequence, count occurrences of each of the 20 canonical amino acids (alphabetical order A,C,D,...,Y), normalize counts to probabilities, and compute the Wasserstein distance between the two distributions with `scipy.stats.wasserstein_distance` using coordinate positions 0..19 as the sample space. Finally write a small Python `http.server`-based (or similar) service bound to `127.0.0.1:9090` that serves `GET /metrics` returning the exact-format JSON with `transcription` and `wasserstein_distance`, and launch it in the background so it stays running after your script exits.