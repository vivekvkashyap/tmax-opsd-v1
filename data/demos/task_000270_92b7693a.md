Model the pipeline as filter -> group -> reduce -> sort -> write, using serde_json to deserialize input.json into a `Vec<Record>` (derive `Deserialize`). First filter: drop records where `text` is empty or contains the char `'\u{FFFD}'` (check with `text.chars().any(|c| c == '\u{FFFD}')` rather than a naive byte/string containment check, since the replacement character is multi-byte in UTF-8). Then deduplicate: build a `HashMap<(String, String), Record>` keyed by `(key, lang)`, and when inserting an existing key compare timestamps, keeping the entry with the higher one — a single pass with `entry().and_modify()` or manual comparison works. Finally collect the surviving records into a `Vec`, sort with `sort_by(|a,b| a.key.cmp(&b.key).then(a.lang.cmp(&b.lang)))`, and write with the `csv` crate using `WriterBuilder` so quoting/escaping of the text field is handled correctly (don't hand-format CSV lines, since UTF-8 fields could need quoting). Build and generate the file with `cargo run` from `/home/user/l10n_processor`, then double check the row ordering and header line by eye before considering it done.