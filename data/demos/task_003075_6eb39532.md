This is a black-box reverse-engineering task — resist the urge to disassemble deeply; instead characterize the binary by systematic probing since its output format (fixed 6-decimal precision, deterministic per input) is exactly the kind of thing you can reconstruct empirically. Start with edge cases: empty text with a zero vector, single-character text, texts with varying word counts and lengths, and vectors with known simple values (e.g., `[1,0,0]`, `[0,1,0]`, `[0,0,1]`) to isolate how each vector component maps to the output before adding text — this lets you infer a linear transform matrix by solving for how each output component responds to a unit change in each input component. Separately vary only the text (holding the vector fixed at zero) to see how text length, word count, and character content shift the output, which suggests the tokenization features get added onto the transformed vector before some final normalization step (note if output vectors always have unit magnitude). Once you have a hypothesis, write it in Python, then fuzz-test it yourself against the binary with random inputs piped via stdin to both, diffing formatted output before declaring done — since the grader itself will fuzz with thousands of cases, exact float formatting and edge-case handling (empty text, zero vector) matter.