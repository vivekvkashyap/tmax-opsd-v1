OCR `/app/corruption_rule.png` with `tesseract` to recover the exact corruption condition (a math expression over `weight`, `source_id`, `target_id`) — read it off the image rather than guessing, and sanity-check the extracted text forms a coherent boolean expression since OCR can mis-read operators/punctuation. Implement the C filter as a streaming CSV parser: echo the header unconditionally, parse each row's four integers, evaluate the recovered condition, and write the row only if it does NOT match. Validate your binary against any provided clean/evil corpora before touching the main graph, so it drops 100% of corrupted rows and preserves clean ones byte-for-byte. For the SQL step, use `RANK() OVER (PARTITION BY source_id ORDER BY weight DESC, target_id ASC)` inside a subquery/CTE (window functions can't be filtered in the same-level WHERE), then apply `ORDER BY source_id ASC, rank ASC` with `LIMIT 15 OFFSET 10` on the outer query, and run it via `sqlite3 graph.db` in CSV/header mode after `.import`-ing the cleaned CSV.
