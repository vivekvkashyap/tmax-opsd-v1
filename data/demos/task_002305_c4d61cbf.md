First fix `/app/.env`: set `REDIS_URL=redis://127.0.0.1:6379/0` and change `PORT` from 5005 to `5000`, then run `/app/start.sh` and verify both Redis (`redis-cli ping`) and the Flask API (`curl http://127.0.0.1:5000/...`) are actually reachable before writing the processing script — a misconfigured env var is the classic naive-attempt failure here, so double check the running process picked up the new value (restart if it was cached). For `/home/user/process.sh`, read exactly one line from stdin, split it on commas into `tx_id,user_id,amount,timestamp` (e.g. with `IFS=',' read -r ...`), call the API with `curl -s http://127.0.0.1:5000/api/users/$user_id` and extract `risk_score` with `jq -r`, fetch `limit:$user_id` from Redis with `redis-cli get`, then compute `amount * risk_score` using `bc -l` (never plain shell arithmetic, since values are floats) and compare it strictly greater than the limit using another `bc -l` comparison. Print only `tx_id,REJECT` or `tx_id,APPROVE` — suppress all curl/jq/redis-cli diagnostic output so nothing extra reaches stdout, and test manually against a couple of hand-crafted CSV lines before trusting it against fuzzed input.
