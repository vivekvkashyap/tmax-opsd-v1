Recover the cipher by black-box experimentation with `/app/legacy_crypto`: run it on several known short ASCII inputs (e.g. single repeated characters, sequential characters) and compare each output hex byte to the input byte's ASCII value — try simple hypotheses in order (XOR with a constant, then add/subtract a constant, bit rotation) since legacy weak ciphers are usually one or two simple byte-wise transforms; verify your hypothesis reproduces the tool's output exactly on multiple test passwords before coding it into your server. Implement decryption as the mathematical inverse of whatever transform you found (reverse the operations in reverse order) applied per hex-byte-pair of the token to recover the 4-character password. For the HTTP server, use raw POSIX sockets to read the request line/headers/body, url-decode the `application/x-www-form-urlencoded` body to get `token` and `redirect`. For redirect validation, reject (400) any value that is absolute or protocol-relative — check for `http://`, `https://`, or the value starting with `//`, but allow single-leading-slash relative paths. On success return `200 OK` with header `Content-Security-Policy: default-src 'self';` and a body containing exactly the decrypted password. Compile with `g++`, run it in the background listening on 127.0.0.1:8080, and test with `curl -X POST -d "token=...&redirect=..."` before finishing.