Fix the infra layer before writing the classifier, and confirm each fix with a direct curl test. In `/app/services/api/app.py`, point the Redis connection string at `127.0.0.1:6379` (via env var or code edit) and restart it; confirm with a Redis client that the Flask app can connect. In `/app/services/nginx/nginx.conf`, change the `upstream` backend from its broken value to `127.0.0.1:5000` (matching wherever Flask actually binds) and ensure `listen 8080;` is set, then reload/restart nginx. Validate the whole chain with `curl -X POST http://127.0.0.1:8080/score -d '{"sequence":"ACGT"}' -H 'Content-Type: application/json'` before writing any bash. For `sequence_classifier.sh`: for each `.fasta` file, strip the `>` header line and join remaining lines into one sequence string (watch for trailing newlines/whitespace corrupting the JSON payload), POST it to the `/score` endpoint, parse the JSON response with `jq` for `.converged` and `.score`, and route the file to the output dir only if `converged == true` and `score >= 0.5`, otherwise append its basename to `/home/user/rejected.log`. Run the script against both the clean and evil corpora and manually verify the resulting output directory and log counts match expectations exactly before finishing.