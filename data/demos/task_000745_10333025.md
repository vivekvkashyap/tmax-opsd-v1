First fix the vendored package before writing anything else: open /app/genomic_dsp/genomic_dsp/transforms.py, find the commented-out import lines (the numpy/scipy imports needed by filter_signal), uncomment them, then install the package editable with `pip install -e /app/genomic_dsp` and verify `python3 -c "from genomic_dsp import filter_signal; print(filter_signal([1.0,2.0,3.0]))"` runs without NameError before writing prepare_data.py. For the script: read stdin line by line, use `json.loads` per line, use Python's `str.find` on `sequence` for the primer to get the first exact match (or print NO_MATCH if find returns -1), slice `signal[start:end+1]`, pass to `filter_signal`, and format each returned float with Python's standard rounding to 4 decimals (e.g. f"{x:.4f}") joined by ", ". Since this will be fuzz-tested against a hidden oracle across many random primer/sequence/signal combinations, be careful to: only print the required output (no debug prints to stdout), handle the primer-not-found case exactly as specified, and not swallow/alter exceptions in a way that silently produces wrong output — test manually with primer present, absent, and edge lengths.
