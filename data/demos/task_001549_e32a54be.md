Build run_analysis.sh as a sequence of discrete, independently-testable steps rather than one big pipe. Step 1: reshape raw_sensor.dat by splitting on spaces then on the underscore in each `time_value` token (e.g. `tr ' ' '\n'` then `awk -F_ '{print $1","$2}'`), prepending the `time,value` header. Step 2: call `python3 get_freq.py clean_data.csv` and capture stdout into a variable — do not modify the python script. Step 3: compare against reference.txt using `awk 'BEGIN{...}'` or `bc -l` with `if (diff < 0.5)` logic, being careful with absolute value and float comparison syntax in bash (string `<` won't work). Step 4: build noisy_data.csv by keeping the header line, then using `awk -F, 'NR==1{print;next}{print $1","($2+0.05)}'` on the data rows only, and rerun get_freq.py on it. Step 5: assemble the report with the exact label:value format shown in the task — get the DATA_POINTS count via `tail -n +2 clean_data.csv | wc -l`, and compare noisy vs clean frequency as strings/floats for the STABLE field. Make the script executable and actually run it before considering it done.