Build this as a clear multi-stage pipeline over the raw log lines, testing each stage's output before moving to the next. Extraction: use a regex per line to pull out the leading `TIMESTAMP | LEVEL |` prefix plus `Client IP: (\S+?),`, `Time taken: (-?\d+)ms`, and `ErrorCode: (\S+)`; skip (don't error on) any line where the regex fails to match all three required fields. Validation: apply an IPv4 regex like `^\d{1,3}(\.\d{1,3}){3}$` to the captured IP string (this correctly rejects a 4-digit octet like `9999` even though the raw text looks IP-shaped) and reject duration<0, writing the original raw line verbatim to rejected_logs.txt for anything failing either check. Deduplication: build the hash key from `f"{ip}_{code}_{duration}"`, hash with `hashlib.sha256`, and keep only the first record per hash using a seen-set, preserving original file order for the ones you keep. Aggregation: group the deduplicated records by ErrorCode, compute count and mean duration rounded with Python's `round(x, 2)` (watch for banker's rounding edge cases, but round() is what's expected here), and sort output rows alphabetically by ErrorCode. Write clean_events.jsonl with json.dumps per line (only the five specified keys) and stats.csv with the exact header and comma-separated rows.