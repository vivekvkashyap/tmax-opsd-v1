Use `git bisect` mechanically instead of eyeballing 200 commits: mark the very first commit as good, `main`/HEAD as bad, and write a small bisect-run script that executes `./process.sh` at each candidate commit and compares its output to the output at the very first commit (checked out once and cached), exiting 0 if they match and 1 if they differ — then run `git bisect run <script>` to let git binary-search automatically. Once bisect converges, capture the reported first-bad commit's full SHA with `git rev-parse` and write it to `/home/user/bad_commit.txt`, then run `git bisect reset` to return to a clean state. For the actual bug, inspect `process.sh`'s use of `bc`: look for a low `scale=` value truncating the intermediate arithmetic and restore/keep a high-precision scale (e.g. `scale=7`) throughout every `bc` invocation, not just the first one, since truncation can be reintroduced at any step in the sum-of-inverses loop. Checkout `main`, apply the precision fix, rerun `./process.sh`, and write its raw numeric output (no extra text) to `/home/user/fixed_output.txt`.
