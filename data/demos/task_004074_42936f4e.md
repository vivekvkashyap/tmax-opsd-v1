Start by reading both Flask app source files in /app to find their required env vars/config (e.g. one likely needs FLASK_APP or a PORT env var set explicitly, and the scorer likely needs Redis running on its default port 6379 before it will boot or respond) — launch redis first (`redis-server --daemonize yes`), verify it's up with `redis-cli ping`, then start each Flask service in the background bound to its specified port and confirm with `curl` before writing any optimization logic. For the orchestrator script, implement a coarse grid search over scale in [0.1,3.0] and shift in [-5.0,5.0] using nested loops in bash with `awk`/`bc` for the float increments (e.g. step 0.1 or 0.05), calling `curl -s -X POST .../score -d '{"signal":[...],"scale":X,"shift":Y}'` and parsing the returned mse field (via `python3 -c` or `jq`) each iteration, tracking the best (lowest) mse seen. Since the true transform is linear (raw = (true-shift)/scale form), a modest grid should get well under the 0.02 threshold — don't overcomplicate with gradient descent unless the grid is too coarse. Write scale/shift/mse of the best point found to calibration_result.txt in the exact `key=value` line format, and `chmod +x` the script.