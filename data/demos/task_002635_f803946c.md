Write the C++ program to parse both config files generically, since the grader will swap them: for fstab, split each non-comment, non-blank line on whitespace and collect column 2 (mount point) whenever column 3 equals `ext4`, preserving file order. For routes, split each data row on whitespace and find the row whose Destination column is `0.0.0.0`, then take its Gateway column value (skip the header lines). For the timestamp, set the timezone inside the program itself before formatting — e.g. `setenv("TZ", "Asia/Tokyo", 1); tzset();` then use `localtime`/`strftime` with a format string matching `%Y-%m-%d %H:%M:%S` followed by the literal `JST` suffix, so it reflects the actual current time rather than a hardcoded value. Build the comma-joined EXT4 mount list without a trailing comma (e.g. accumulate into a vector and join with `", "` — check exactly what separator/spacing the spec calls for). Compile with `g++ -o /home/user/sre_monitor /home/user/sre_monitor.cpp`, run it, and inspect the generated log file's exact formatting against the spec before finishing; re-run it once more to confirm it regenerates cleanly.
