Pull the small graph into Python rather than fighting a recursive-CTE shortest-path query in SQL — read all `nodes` and `edges` rows, build an adjacency structure keyed by node id with `latency_ms` as edge weight, then run a standard Dijkstra (or since the graph is small, even brute-force all simple paths and pick the minimum total) from the node whose name is `'API_Gateway'` to the node whose name is `'User_Master'`, looking up ids by name first. Track predecessor pointers during the search so you can reconstruct the full path of node names, not just the total cost — a common mistake is computing the correct minimal distance but forgetting to also record and backtrack the path. Convert path node ids back to names in order, including source and destination, and write the JSON with exactly the required keys (`source`, `destination`, `total_latency_ms`, `path_nodes`). Run the script once to confirm `/home/user/routing_result.json` is produced and is valid JSON before finishing.