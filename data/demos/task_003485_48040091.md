First extract the numeric spec from the image with `tesseract /app/bayes_spec.png stdout` (or similar OCR), and read off the prior, the two likelihoods, and the MISSING/RESET integer codes precisely — don't guess these from the prose. Then implement the C program as a strict line-oriented state machine reading ints from stdin via `scanf`/`fgets`+`atoi` until EOF: keep a `p_h` double initialized to the OCR'd prior and a `nan_mode` flag. On each value: if it equals RESET, reset p_h to the prior, clear nan_mode, print `RESET` (this takes priority even while in NaN mode); else if it equals MISSING or nan_mode is already set, set nan_mode and print `NaN`; else if it's 1 or 0, apply Bayes' rule with the OCR'd likelihoods and print the new posterior with exactly 4 decimals (`printf("%.4f\n", ...)`). Get the exact Bayesian update formula right — normalize by the full-probability denominator, not just multiply. Since a fuzzer will diff your binary byte-for-byte against a hidden oracle across thousands of streams, be meticulous about edge cases: negative/unexpected integers, empty input, and consistent float formatting (locale, rounding mode) — test locally by piping varied integer sequences through your compiled `/home/user/etl_transform` and eyeballing output for consistency before considering it done.
