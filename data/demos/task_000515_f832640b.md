Fix the vendored header before writing any application code: try compiling a trivial `#include "json.hpp"` translation unit first and read the compiler error carefully — a single injected malformed macro/typedef near the top of the file (search for stray `#define` lines that redefine a standard type like `std::size_t`) is a plausible culprit; remove or correct only that line rather than patching around it. For the ETL filter, build the graph per file from `source`/`target` pairs, then implement the reject rule precisely as specified: reject if every node's degree is exactly 2 AND the graph is a single connected cycle (not just "all degree 2", which could also be multiple disjoint cycles — check connectivity too), OR if self-loop edges make up more than half of total edges. Everything else must be accepted and written byte-for-byte identical to the input. Validate your logic against the given clean/evil examples by hand before running the full corpora — trace through each sample file's degree sequence and connectivity yourself. Compile with `-I /app/json-3.11.2/single_include/ -std=c++17`, then run against both `/app/data/clean/` and `/app/data/evil/`, diffing accepted output content against source to confirm exact preservation.
