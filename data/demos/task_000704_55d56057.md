Build the pipeline as a bash script that shells out to Python for the numeric/text work, keeping each stage's output file exactly as specified. For cleaning: load the CSV with pandas, drop rows where `measurement` is NaN/empty and rows where `measurement` is outside [0, 100] (use `>` / `<`, matching the strict outlier definition given), and write the cleaned CSV with header intact. For the CI: compute mean and sample std dev of the cleaned column, then use a t-distribution critical value (`scipy.stats.t.ppf`, `n-1` df) rather than a fixed `1.96` z-value, which is wrong for small n; round both bounds to 2 decimals, comma-separated, no whitespace. For retrieval: fit `TfidfVectorizer` on the cleaned `notes` column, transform the query string with the same fitted vectorizer (don't refit on the query alone), compute cosine similarity between the query vector and each row, and take the `record_id` of the argmax. Run the finished script once and inspect all three output files to confirm formats match the spec exactly before finishing.
