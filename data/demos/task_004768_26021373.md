Debug this system layer by layer, and fix the standalone math script last, since it's the part that gets fuzz-tested against the reference oracle your task describes. (1) Service wiring: inspect `/app/start.sh` and any `.env`/config for the Redis URL — it's likely pointing at a placeholder/dummy value instead of `redis://127.0.0.1:6379/0`; fix it, start redis, then the worker, then the API, and confirm `curl -X POST http://127.0.0.1:8000/compute -d '{"value": 100}'` returns something (even if numerically wrong) before moving on. (2) Memory leak: find the module-level cache dict in `worker.py` that grows unbounded on every job and either bound it (`functools.lru_cache(maxsize=...)`) or stop accumulating unnecessary state. (3) Loop boundary: look for an off-by-one in the recurrence's `range(...)` call — compare it against the intended inclusive range for the sequence index. (4) Signed overflow: the code masks to 32 bits with `& 0xFFFFFFFF` but never converts the unsigned result back to a signed two's-complement value; after masking, add the branch `if val >= 0x80000000: val -= 0x100000000`. (5) Missing coefficient: run `git log -p` (or `git log --all -p -- worker.py`) in `/app/` to find the commit that zeroed out a hex constant, and restore the original hex value from that diff. After fixing all of this in `worker.py`, factor the pure recurrence function out into `/home/user/fixed_math.py` as a CLI script (`argv[1]` -> int -> print result), then self-test it against the reference oracle across a wide range of inputs (0 to 100000, including edge values) before finishing, since exact bit-for-bit match is what's graded.