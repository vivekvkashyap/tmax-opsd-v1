First run `tesseract /app/storage_policy.png -` to recover the target encoding, the forbidden severity level, and the renaming prefix from the policy document — do not assume these values, read and sanity-check them from the OCR text. Build the script as a pure stdin-to-stdout stream filter: read all input bytes, decode using a fallback-tolerant source encoding (e.g. `latin-1`, which never raises), then split into records using a regex anchored on the timestamp header pattern `^\[\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2}\]` with a capturing lookahead so each chunk includes its header — not naive line-splitting, since multi-line messages must stay attached to their header. For each record, check whether it contains the forbidden severity marker and drop the whole record if so; keep all others verbatim. Re-encode the surviving output strictly in the target encoding before writing to stdout. Make the script executable and test it by piping varied synthetic multi-line logs through it to confirm records aren't split incorrectly at edge cases (empty input, no trailing newline, consecutive dropped blocks).
