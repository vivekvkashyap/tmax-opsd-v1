Work the three sub-problems independently, then wire them together. First, open the pcap with `tcpdump -A -r healthcheck_trace.pcap` (or tshark) to see the raw bytes of the HTTP request the script sent; compare against a valid HTTP/1.1 request to spot the missing/incorrect line terminators. Locate the file in the vendored package that builds the probe string (search for `GET`/`Host` inside the source tree with grep), and rewrite it to emit `\r\n` (not `\n`) after each header line plus a blank `\r\n\r\n` after the last header — `printf` is safer than `echo -e` for exact byte control. Second, for the race condition, find where background jobs (`&`) each write to the shared JSON state file; a naive `sed -i` from multiple processes interleaves writes and corrupts JSON. Fix by serializing writes with `flock` (a lock file or `flock fd` around the read-modify-write of the state file), not by removing parallelism. Finally, export `ADMIN_TOKEN=sre-secret-77x`, launch the dashboard bound to `127.0.0.1:8080` with `nohup ... &` or similar so it survives your shell exiting, and confirm with `curl` that `/state` returns valid JSON with all targets UP and requires the bearer token.