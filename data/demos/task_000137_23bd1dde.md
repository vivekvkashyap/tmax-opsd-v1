Recover the two hidden inputs before writing any C code. For `/app/crash.dmp`, it's a binary memory-dump-style file with plain ASCII path strings embedded among random bytes — use `strings /app/crash.dmp | grep '^/app/corpora/'` (or equivalent) to pull out the exact evil/clean corpus directory paths rather than assuming a layout. For `/app/voicemail.wav`, transcribe it with a speech-to-text tool (`whisper` or similar) to get the exact spoken precision-loss threshold value, and use that literal decimal in your C comparison — don't guess a round number. Then write `audio_sanitizer.c`: open the file in binary mode, loop reading 4-byte little-endian IEEE-754 floats (`fread` into a `float`, which matches native little-endian layout on x86/most Linux, or use `memcpy` from a 4-byte buffer to be explicit about endianness), maintaining `float float_sum` and `double double_sum` accumulators updated every iteration, and after each update check `fabs((double)float_sum - double_sum) > threshold` — exit(1) immediately on the first violation, otherwise exit(0) after the loop completes normally (handle EOF/short reads cleanly). Compile with `gcc -O0` or similar (avoid aggressive optimization that could fold the float/double arithmetic differently) and test the binary yourself against a couple of files you construct with known accumulation patterns before trusting it on the hidden corpora.