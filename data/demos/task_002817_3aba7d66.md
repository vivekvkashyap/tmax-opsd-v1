Gather the two pieces of ground truth first, then write the validator to check all four rules in the right order. Extract the archive with `tar` — note it says "nested," so you may need to unpack one archive to find another archive or directory inside before reaching the CSV of allowed keys; read that CSV to get the exact allowed key names (don't hardcode guesses). Run `tesseract /app/policy.png stdout` to recover the required environment_id string exactly as printed. In verify_config.py: first check `os.path.islink(path)` (use `os.path.islink`, not just checking if it resolves) and reject immediately if true — this must run before opening the file, since following a symlink to read it is itself the vulnerability. Then load the JSON, verify every key in the object is a member of the allowed-keys set (reject on any extra key), and verify the `environment_id` field equals the OCR'd string exactly. Exit 0 only if all checks pass, exit 1 on any failure (missing key set violation, wrong environment_id, or symlink). Test your script manually against a few hand-crafted valid and invalid JSON files, including a symlink you create yourself with `ln -s`, before assuming it's correct.
