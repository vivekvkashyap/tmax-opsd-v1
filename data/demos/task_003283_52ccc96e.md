Build this as three layered pieces, testing each independently before wiring together. First, the CSV rolling-metric logic: for a given source_node, filter matching rows, sort by timestamp ascending, then slide a window of up to 3 consecutive rows summing `amount`, tracking the running maximum — verify by hand against a small example first. Next, treat `/app/prop_scorer` as an opaque CLI oracle: invoke it via `popen`/`fork+exec` with the node id and computed metric as argv strings, capture stdout, and parse just the trailing integer from the unstructured "Risk Metric: <score>" line with a regex rather than assuming fixed column positions. Finally implement a raw POSIX-socket TCP server bound to `127.0.0.1:8080` that reads a line-terminated integer per connection, runs the pipeline, and writes back a single-line JSON object `{"node": <id>, "score": <score>}\n` before closing — test sequential connections with `nc` to confirm no leftover buffered data causes malformed responses.
