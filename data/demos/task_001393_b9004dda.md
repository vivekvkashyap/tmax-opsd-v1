Compile `/home/user/sim_project/rhs_calc.c` to a shared object with position-independent code explicitly, e.g. `gcc -shared -fPIC -o /home/user/sim_project/librhs.so rhs_calc.c` (omitting `-fPIC` is the classic mistake that breaks or silently produces a non-relocatable library). In `run_sim.py`, load it with `ctypes.CDLL`, and set `.argtypes`/`.restype` on the exported function precisely matching its C signature (a `double`, plus `double*`/pointer args for the state and derivative arrays) — mismatched ctypes argtypes is the other common source of silent corruption. Wrap the C function in a Python callable that `solve_ivp` can call as the RHS (converting between numpy arrays and ctypes pointers each call), and integrate with `method='Radau'` from t=0 to t=1000 for each `rtol` in the given list, always setting `atol = 1e-3 * rtol`. After collecting `y1(1000)` at every tolerance, compare each of the four coarser tolerances' `y1(1000)` against the `rtol=1e-6` baseline, find the largest `rtol` (excluding 1e-6) whose absolute difference is strictly `< 1e-5`, defaulting to `1e-6` if none qualify. Write both values to `/home/user/sim_project/convergence_result.json` with the exact two required keys.