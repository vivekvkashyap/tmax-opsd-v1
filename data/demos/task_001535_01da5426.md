Start the services with `/app/start_services.sh`, then reverse engineer `/app/auth_gateway` using `strings`, `objdump -d`, or `ltrace`/`strace` on it while sending probe requests with `curl` to `localhost:8080`. Look specifically for a token "type" byte that selects between a real cryptographic signature check and a weaker fallback path (mirroring the classic JWT "alg=none" bypass) — confirm by crafting a minimal token by hand and observing the gateway's response/logs, rather than assuming the format. Once you've confirmed the bypass type and the lightweight integrity check it still performs (e.g. a simple checksum over the payload bytes), write a small throwaway Python/bash test to validate your understanding before committing to C++. For the C++ generator, prioritize raw performance: build the binary token in memory with bit operations, compute the checksum locally, base64-encode with a fast lookup-table implementation (not a naive per-byte string-append), and buffer output in memory before one bulk write to `/home/user/tokens.txt` — per-token syscalls or iostream flushing will blow the 0.8s/1M budget. Validate one generated token end-to-end against `localhost:8080/flag` with curl and save the response to `/home/user/success.log`.