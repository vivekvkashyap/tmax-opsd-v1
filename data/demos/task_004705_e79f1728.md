Build this as a straight numerical pipeline in one Python script, being careful about two spots where naive attempts drift from the spec. First, generate the grid with `np.linspace(-5, 5, 25)` for both axes and `np.meshgrid`, then flatten so you have 625 (x,y) pairs in a fixed order — keep that ordering consistent through normalization and saving. Second, for each pair solve `z**3 + sin(z) - x*y = 0` with `scipy.optimize.fsolve(func, 0.0)` using an initial guess of exactly 0.0 (a different initial guess can converge to the wrong root, which will silently break the final max-Z check), and stack results into an (625,3) array of [x,y,z]. Compute column-wise mean and population std (`np.std(..., axis=0, ddof=0)`, not the sample ddof=1 default elsewhere) to z-score normalize, then save that normalized array with `np.save` to the exact path required. Separately, make a 3D scatter (`mpl_toolkits.mplot3d`) of the *unnormalized* x/y/z with axis labels 'X','Y','Z' and save as PNG. Finally find `max(z)` over the unnormalized values and write `Max Z: <value>` rounded to exactly 4 decimals to the stats file — compute this from your own fsolve results rather than assuming a value.