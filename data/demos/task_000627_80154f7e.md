Start by OCR'ing `/app/schema.png` with `tesseract /app/schema.png -` (try `--psm` variants and manual visual inspection if OCR text is noisy) to recover the actual table names, join key, JSON column names, and the target output field names — do not guess these, read them off the image, since the whole schema is only discoverable that way. Once you have the real column/table names, inspect `/home/user/data.db` with `sqlite3` to confirm they exist (`.schema`, `select * from <table> limit 5`). Write `engine.cpp` using the SQLite C API plus SQLite's JSON1 functions (`json_extract`, `json_each` for iterating array elements) to join the two tables on the recovered key and aggregate the recovered numeric field per user, compiling with `g++ engine.cpp -lsqlite3 -o /home/user/engine`. Test the compiled binary standalone against a known user_id first. Then write a thin Python HTTP wrapper (stdlib `http.server` or Flask) that parses `user_id` from the query string, shells out to `/home/user/engine <id>`, sets `Content-Type: application/json`, and returns the binary's stdout (or wraps it into the JSON shape the image specifies). Run the server as a background/daemon process bound to `127.0.0.1:8080` and curl it yourself before finishing.
