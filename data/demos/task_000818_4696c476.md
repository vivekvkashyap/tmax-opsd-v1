Don't guess the anomaly rule — deduce it empirically using `/home/user/samples/`, which has a labeled-by-directory mix of clean and evil files: run `/app/legacy_qc` against each sample, note its exit code, then diff the numeric columns between passing and failing files (e.g. inspect `sensor_beta` vs `sensor_gamma` row by row) until you find the exact relational condition that separates them — vary one row at a time in a copy of a clean file if needed to confirm the boundary precisely (strict vs non-strict inequality matters). Reimplement that exact condition natively in Python (no subprocess call to the binary — it must stay fast), and make it the very first gate in `cleaner.py`: if any row trips it, exit 1 immediately with no output file written. Only then proceed to the pandas reshape/resample: set `timestamp` as a datetime index, `melt` wide→long to `timestamp,sensor_name,value`, `resample('5min')` per sensor group, `ffill(limit=3)`, then `fillna(0.0)` for anything still missing, and write the CSV. Append the exact log line format after each success. Validate your reimplementation against many samples' exit codes before trusting it.
