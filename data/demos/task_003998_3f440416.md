Work through the pipeline stage by stage and validate each output before moving on. For the token, run `tesseract /app/release_token.png stdout` (try preprocessing with `convert`/`imagemagick` if OCR is noisy) and trim whitespace — verify the extracted string looks like a plausible token before using it. Merge the CSVs by concatenating the data rows (skip the repeated header) from all three `part*.csv` files, then sort by `ArtifactID` with `sort -t, -k1,1`, and prepend a single header line. Apply `updates.patch` with `patch merged_manifest.csv < updates.patch -o final_manifest.csv` (or `cd /app && patch -p0 ...`), and diff the result against your expectation manually. For the log script, replace the bash-array approach with a streaming pipeline like `grep -oE 'CODE_[0-9]+' release.log | sort -u > processed_logs.txt` (or `awk` equivalent) so it never loads the whole file into memory. For the server, write `server.sh` using `nc -l -k 127.0.0.1 8080` or a small loop with `socat`/`nc` that parses the request line and headers, checks for `Authorization: Bearer <token>` matching your OCR'd value exactly, and returns 200 with the manifest body or 401 otherwise — test it yourself with `curl -H "Authorization: Bearer ..." http://127.0.0.1:8080/manifest` and a request with a wrong/missing header before leaving it running in the background.