First fix and install the vendored library before writing the stream processor, since your script depends on importing it. Inspect `/app/vendored/text_cleaner_lib/text_cleaner_lib/redactor.py` for the import error (a typo'd module name causing `ModuleNotFoundError`) and correct it to the real standard-library module, then install the package locally (e.g. `pip install -e /app/vendored/text_cleaner_lib`) and sanity-check by importing it and calling `redact_emails` on a string containing an email to confirm it returns the redacted form, and `tokenize_and_normalize` on a sample string. Then write `/home/user/process_stream.py` to read stdin line by line (avoid loading everything into memory), `json.loads` each line, look up `user_id` in a dict pre-loaded once from `/app/lookup.csv` via `csv.DictReader` (default to `region="UNKNOWN"`, `risk_score=0` when absent), drop the record entirely with a `continue` if `risk_score > 90`, otherwise pass `message` through `redact_emails` then `tokenize_and_normalize` in that exact order, and print a `json.dumps` object with precisely the four required keys in a consistent structure (dict key order in the literal you build controls the output). Since your output is checked byte-for-byte against a reference across many randomized inputs, test against edge cases: messages with multiple/no emails, unknown user_ids, and risk_score exactly 90 (must NOT be dropped, only strictly greater than 90 is dropped).