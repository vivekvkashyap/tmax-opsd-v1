This task has three independent sub-problems — solve them in isolation and verify each before combining. (1) For the video: use `ffmpeg` to extract frames at the stated 10fps (e.g. `ffmpeg -i /app/diagnostic_blink.mp4 frame_%04d.png`), then for each frame check whether it's black or white by sampling pixel brightness (e.g. with Python/PIL, average pixel value near 0 vs near 255) and emit '0' or '1' per frame in order into a single string written to `/app/extracted_bits.txt` — don't assume frame count, just process however many frames ffmpeg extracts. (2) For the git secret: the key was deleted in a later commit, so it won't appear via `grep` on the working tree or `git log -p` alone always shows it — run `git log -p -- diag_tools/decode.py` (or the correct path) through the full history, or `git log --all -p | grep -i secret`, to find the commit that introduced `SECRET_KEY` before it was blanked out, and recover its exact string value from that diff. (3) For the decoder bug: read `decode.py` carefully — a common bug pattern here is slicing bit-chunks with the wrong width (e.g. 7 instead of 8 bits per byte), which shifts every character after the first. Fix the slicing so each iteration consumes a full byte, hardcode the recovered SECRET_KEY, then fuzz-test your fix against many random binary strings of varying length (including non-multiples of 8) to confirm robustness before finishing, since grading is by output equivalence to a hidden oracle across randomized inputs.
