Trace the pointer arithmetic in `parse_log_line`: after stripping the trailing newline, `len` is `strlen(message)`, and the code unconditionally checks `message[len-1]`. If the message segment (everything after the second `|`) is empty, `len == 0`, so `message[-1]` is an out-of-bounds read one byte before the buffer — this is the crash. Build `crash_input.txt` as a single line with exactly two pipes and nothing (or only a newline) after the second pipe, e.g. `TS|INFO|` — this reliably reproduces the underflow. Fix it in `fixed_processor.c` by guarding the check with `if (len > 0 && message[len-1] == '\\')` (short-circuit prevents the out-of-bounds access when len is 0), and decide sensible behavior for an empty message (e.g. just print `PROCESSED: [SEVERITY] ` or skip). Compile with `gcc -o /home/user/fixed_processor /home/user/fixed_processor.c` and confirm both: the crash input now exits cleanly (code 0) and a normal `TS|SEVERITY|message` line still prints `PROCESSED: [SEVERITY] message` unchanged.