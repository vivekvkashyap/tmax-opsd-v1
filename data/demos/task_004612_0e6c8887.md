Transcribe `/app/compliance_alert.wav` first (speech-to-text tooling) to recover the target entity name and the traversal depth — treat both as unknowns to extract, not given values. Reverse-engineer the SQLite schema with `sqlite3 /home/user/audit_records.db '.schema'` and sample rows to understand how nodes/edges are stored (look for a nodes table with names/ids and an edges table with source/target foreign keys) — check whether edges should be treated as directed or undirected for traversal, since compliance graphs like this are often meant to be traversed bidirectionally. Write a recursive CTE (`WITH RECURSIVE`) that starts from the given entity, joins across edges up to the discovered depth, tracks depth explicitly to stop at the limit, and dedupes visited entities. Expose it through a plain HTTP server (Flask/FastAPI/http.server) with no auth, on 127.0.0.1:8080, implementing `/api/alert_info` (returning the transcribed target/depth as JSON) and `/api/trace?entity=NAME` (returning a sorted, deduped JSON array of entity names within that depth, including the queried entity). Run the server backgrounded so it stays up for automated checks.