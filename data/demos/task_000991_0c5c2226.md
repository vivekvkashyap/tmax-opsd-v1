Do the mechanical setup first (move `legacy.c`, `cargo new` the evaluator project) so it's out of the way, then focus effort on the recursive-descent parser: write a small character-index-based parser that recognizes either an integer literal or one of `add/sub/mul/div` followed by `(`, a recursively-parsed argument, `,`, another recursively-parsed argument, and `)`. Use `Result`/`Option` throughout instead of `.unwrap()` or indexing that can panic — any parse failure, trailing garbage, unexpected character, or out-of-bounds index must be caught and converted into printing exactly `EVAL_ERROR` with exit code 0, never a Rust panic. Handle `div` by explicitly checking the divisor for zero before dividing and treating it as a parse/eval error rather than crashing; use truncating integer division for valid cases. Compile in release mode (`cargo build --release`) before benchmarking. For the benchmark script, invoke the compiled binary once per line of `/home/user/dataset.txt`, capture stdout, skip/ignore any line whose output is `EVAL_ERROR`, sum the rest as integers, and write the integer sum (no decimals) to `/home/user/benchmark_result.txt`. Test the div-by-zero and malformed-input cases explicitly before trusting the benchmark totals.
