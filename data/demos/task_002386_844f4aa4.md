Break this into independent artifacts before wiring the server together. Run `tesseract /app/disk_warning.png stdout` (installing tesseract if needed) to extract the volume identifier text, and strip whitespace/newlines from it in your code rather than hardcoding the OCR'd value. For the binary: `dd`/Python file I/O to read the first 32 bytes of `/app/dump.bin` into `/home/user/header.bin`, then read the remainder in a loop, writing 1,048,576-byte chunks to `/home/user/chunks/chunk_N.bin` (the last chunk will just be whatever's left, shorter than 1MB — don't pad it). Create `/home/user/active_links/link_N.bin` as a symlink (`os.symlink` or `ln -s`) to each corresponding chunk file, using absolute paths for the link targets so they resolve correctly regardless of cwd. For the server, use Python's `http.server` or a small framework: on startup, `fcntl.flock` an open handle to `/home/user/server.lock` with `LOCK_EX` before calling `serve_forever`, keeping that file handle open for the process lifetime; implement `GET /volume` returning the OCR'd (stripped) text and `GET /chunk/<id>` reading through the symlink and streaming the raw bytes. Launch it backgrounded and curl both endpoints yourself to confirm byte-exact responses before finishing.