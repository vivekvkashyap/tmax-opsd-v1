Do frame extraction first with `ffmpeg -i /app/dirty_feed.mp4 -vf fps=10 /home/user/frames/frame_%04d.jpg`, confirming the fps filter (not `-r` alone, which can behave differently for extraction) yields exactly the frame count implied by the video's duration. In the Python pipeline, process frames in strict numeric/lexicographic order (they're already zero-padded so sorted filenames work), load each with a library like Pillow or OpenCV, compute mean pixel intensity across all channels for the "black" check (`< 5.0`), and compute MAE against the immediately previous frame's raw pixel array (not the previous *classification*) for the "duplicate" check (`< 1.0`) — remember frame 1 can never be duplicate regardless of MAE. Build the summary counts and per-frame dict exactly as specified, writing to `/home/user/manifest.json`. For the server, use a plain `http.server` subclass or Flask, binding exactly to `127.0.0.1:9090`; enforce the `Authorization: Bearer SECRET_DATA_TOKEN` header on every route (return 401 if missing/wrong before doing any other logic), serve `/summary` from the manifest's summary object, and `/frame/<filename>` by looking up that key in the manifest's frames dict, returning 404 if absent. Start the server as a background process so it stays reachable after your turn ends.