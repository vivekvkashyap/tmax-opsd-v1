Structure the Go program in clear stages. First, parse `checksums.txt` into a map of filename -> expected checksum, then for each `.tar.gz`/`.clog` file in the directory compute its actual SHA256 with `crypto/sha256` over the raw file bytes and skip any file whose checksum doesn't match (do this comparison on the file's on-disk bytes before any decoding). For `.clog` files, base64-decode the entire file content first (`encoding/base64`) to reconstruct the underlying `.tar.gz` bytes, then feed that into `compress/gzip` + `archive/tar` to extract `changes.log`. Parse `changes.log` with a simple state machine or regex: scan lines, and only start capturing fields when you see `START_CHANGE`, reset/stop at `END_CHANGE`, and extract the `Author:` line within each captured block — ignore any text outside a START/END pair. Increment a `map[string]int` counter per author found across all valid archives, then marshal that map to JSON and write it to `/home/user/summary.json`. Test incrementally: verify checksum validation alone, then extraction alone, then log parsing alone, before wiring everything together, since silently mis-skipping a valid archive or double-counting is easy to miss.