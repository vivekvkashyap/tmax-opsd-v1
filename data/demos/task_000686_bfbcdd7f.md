Structure the C++ program around three clear passes: parse, align, filter/report. Parsing: read each log with a regex or manual scan matching the bracketed timestamp and the value pair for that file's specific format (A uses `X=.. Y=..`, B uses `coord: (.., ..)`); convert each `HH:MM:SS.mmm` timestamp to a single comparable unit (e.g. milliseconds since midnight) since that's what you need for nearest-neighbor comparison. Alignment: for each entry in A, scan all of B's entries and track the minimum absolute time difference (ties go to the first B entry encountered in file order); only keep the pair if that minimum difference is <= 100ms, otherwise skip the A entry entirely — don't just take B's chronologically nearest-by-index entry. Distance/filter: compute Euclidean distance for each valid pair and keep only those strictly greater than 2.0. Output: write header then rows to `/home/user/anomalies.csv`, formatting timestamps back into their original bracket string form and all numeric fields with exactly 2 decimal places (use `std::fixed << std::setprecision(2)`, not default formatting). Compile with `g++ analyze_logs.cpp -o analyze_logs` and actually run it so the CSV is produced before finishing.
