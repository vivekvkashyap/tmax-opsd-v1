Treat this as three linked subtasks: recover the leaked secret from video, then harden the Rust source, then verify against /proc. To recover the passphrase, extract frames from `/app/admin_session.mp4` with `ffmpeg -i /app/admin_session.mp4 -vf fps=1 frame_%04d.png` (increase fps if the terminal text scrolls quickly), then run `tesseract` OCR over the frames to find the command line showing the `--passphrase` argument, and write exactly the recovered string (no extra whitespace/quotes) to `/home/user/leaked_passphrase.txt`. For the Rust fix in `/home/user/rusty_scanner/src/main.rs`, find where `std::process::Command` is built with the passphrase as a plain argument (this is what leaks into `/proc/[pid]/cmdline`) and instead pass it via the child's `stdin` (using `.stdin(Stdio::piped())` and writing to the handle) or via a securely-created temp file that's read by the child and immediately removed — never place secret material in argv. Also add an explicit permissions check on the SSH key file using `std::fs::metadata(...).permissions().mode() & 0o777 == 0o600`, aborting with a non-zero exit code if it doesn't match exactly. Finally write a hardened `/home/user/.ssh/config` disabling root login and forcing pubkey auth (e.g. `PermitRootLogin no`, `PubkeyAuthentication yes`, `PasswordAuthentication no`). Rebuild and manually poll `/proc/<pid>/cmdline` during a test run to confirm no leak before considering it done.