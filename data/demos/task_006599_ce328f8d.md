Implement each of the three checks as a self-contained pipeline using coreutils only (no Python/Perl, per the constraint). For open redirects: use `awk`/`grep` to pull the `redirect=` query value from each `access.log` line, base64-decode it with `base64 -d` (note it may need padding restored, or handle unpadded input if `base64 -d` errors), then test the decoded string against `^https?://` while excluding hosts `localhost` or `127.0.0.1` — extract the domain portion (strip scheme, cut at first `/`) to compare it exactly rather than substring-matching, and collect the *request's source IP* (not the redirect target) for matches, sorted and deduplicated. For privilege escalation: `find /home/user/deploy_scripts -type f -perm -002` reliably matches world-writable regardless of other bits, then `basename` each result. For unauthorized ports: read `open_ports.txt` line by line and filter out exactly `80` and `443`, sorting the rest numerically (`sort -n`). Assemble the three comma-joined, sorted lists into the exact `KEY: v1,v2,v3` format (empty value after the colon-space when a category has no hits), and make the script executable.
