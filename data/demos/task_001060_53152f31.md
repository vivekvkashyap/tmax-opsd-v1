Build this as a bounded-concurrency FFI pipeline. In Python, load the library with `ctypes.CDLL('/home/user/libchecker.so')`, declare `check_magic.argtypes = [ctypes.c_char_p]` and `.restype = ctypes.c_int`, and call it with each file path encoded to bytes. Enforce exactly 2 concurrent workers using a `concurrent.futures.ThreadPoolExecutor(max_workers=2)` (threads are fine here since the work is I/O-bound C calls) or a `threading.Semaphore(2)` guarding the FFI call plus file I/O. For files where check_magic returns 1: read the raw bytes, slice off the first 4 bytes as the header, base64-decode the remainder (`base64.b64decode`), then reinterpret those decoded bytes as ISO-8859-1 text and re-encode/write as UTF-8 (`decoded.decode('iso-8859-1').encode('utf-8')` — Latin-1 maps every byte 1:1 to a codepoint, so this never raises, but you must do the decode/encode step explicitly rather than writing raw bytes, since some inputs contain codepoints above ASCII like accented characters). Create organized_data/ first with `os.makedirs(exist_ok=True)`. Collect successfully processed basenames in a thread-safe list/lock, sort alphabetically at the end, and write one per line to processed.log.