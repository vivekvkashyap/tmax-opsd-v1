This is a stdin-to-stdout behavioral clone of `/app/conf_oracle`; treat the oracle as a black box and validate against it via differential fuzzing rather than guessing edge cases. From the described algorithm: split each line on the first space into KEY and PAYLOAD, Unicode-normalize PAYLOAD to NFC (use a proper Unicode normalization crate, not a hand-rolled one — subtle NFC edge cases with combining marks/emoji are the main source of mismatches), then SHA-256 hash the NFC string to dedupe per-KEY (skip silently if this hash was already seen for that key). For each newly-accepted payload, count Unicode scalar values (chars, not bytes — critical for multi-byte UTF-8 payloads) and push into a per-key rolling window capped at the last 3 accepted lengths (evict oldest when exceeding 3), then print `KEY:{integer average via floor division}\n`. Initialize a Cargo project in `/home/user/tracker/`, add the needed crates (e.g. `unicode-normalization`, `sha2`), and build in release mode so the binary lands at `/home/user/tracker/target/release/tracker`. Before finishing, generate randomized multi-line UTF-8 inputs (including combining characters and emoji) and diff your binary's stdout against the oracle's stdout for many iterations, fixing any divergence.