Work bottom-up: fix the library, build against it, then wrap in scheduling. First get `/app/anonymizer_lib` to compile — run `cargo build` inside it, read the compiler error (likely a crate that source code imports but isn't declared in `Cargo.toml`'s `[dependencies]`), and add that dependency rather than rewriting library logic. Then scaffold a binary crate at `/home/user/processor` (`cargo new`) with a path dependency on the fixed library, implementing stdin-to-stdout CSV transform logic per spec: split each line on `;`, parse `KEY:value` fields, call into the anonymizer for USER/IP masking, and implement the S1/S2/S3 "NA" imputation rules exactly as stated (1 missing = avg of other two; 2 missing = copy the single valid value; 3 missing = all zero). Since correctness will be fuzz-tested against many random inputs, handle edge cases (boundary floats, exact 2-decimal formatting) carefully, not just the example. Build in release mode. Finally write and chmod the wrapper script, and add the cron file with the exact schedule/user/command specified.
