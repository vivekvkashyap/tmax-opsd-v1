Treat this as a two-bug numerical debugging task inside Welford's online algorithm, not a networking problem — the pcap/logs are just for timeline reconstruction, so parse `traffic.pcap` (scapy or tshark) and cross-reference with the two log files to confirm when rejections start behaving oddly, but the real fix is in `detector.py`. Read the `process()` method carefully and compare it line-by-line against the textbook Welford update: the mean update should use `delta = value - old_mean`, then `mean += delta/count`, then a *second* delta (`delta2 = value - new_mean`) should be used for the `M2 += delta * delta2` accumulation — using the same `delta` twice instead of computing delta2 after updating the mean is the classic subtle bug. Also check that the standard deviation is actually the square root of the variance (`M2/count`) rather than the raw variance itself — forgetting `sqrt()` is a common second bug that inflates the rejection threshold. After fixing both, recompile nothing is needed (pure Python); just rerun `python run_pipeline.py` and inspect `resolution.json` to confirm accepted/rejected counts now match a correct single-outlier scenario, without editing function signatures or class names.