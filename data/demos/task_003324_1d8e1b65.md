For Phase 1, generate the cert pair non-interactively: `openssl req -x509 -newkey rsa:2048 -nodes -keyout /home/user/audit_certs/server.key -out /home/user/audit_certs/server.crt -days 365 -subj "/CN=localhost"`. For Phase 2, don't brute-force — solve it algebraically from two known-plaintext pairs (P1,C1) and (P2,C2) from `/home/user/known_pairs.csv`: since `C = (a*P + b) mod 256`, subtracting gives `C1-C2 ≡ a*(P1-P2) mod 256`, so `a = (C1-C2) * modinv(P1-P2, 256) mod 256` (compute the modular inverse via extended Euclidean algorithm — note it only exists when P1-P2 is odd, so pick a pair where that holds), then `b = (C1 - a*P1) mod 256`; verify a,b against the other given pairs before trusting them. For Phase 3, decrypt by computing `a_inv = modinv(a, 256)` and applying `P = a_inv * (C - b) mod 256` to every byte of `/home/user/historical_traffic.enc`, writing the result as text to `/home/user/historical_audit.txt`. For Phase 4, use Python's `ssl.SSLContext(ssl.PROTOCOL_TLS_SERVER)`, `load_cert_chain(certfile, keyfile)`, wrap a listening socket bound to `('localhost', 8443)`, and for each accepted connection read until EOF, apply the same decrypt formula, append the utf-8 result plus a newline to `/home/user/live_audit.log`; run this listener as a background/detached process so it's still live afterward.
