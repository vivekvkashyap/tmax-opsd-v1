Treat `waits_for` rows as directed graph edges and find every node that lies on at least one cycle — not just nodes reachable from a cycle. The robust approach: extract all edges with `sqlite3 -csv`/`sqlite3 -json`, then in a small script (Python or awk) run Tarjan's or Kosaraju's strongly-connected-components algorithm, or simpler — for each node, do a DFS/BFS following outgoing edges and check whether you can get back to the starting node; a node belongs in the output only if it can reach itself via a nonempty path (this correctly handles self-loops as length-1 cycles and correctly excludes nodes like the one waiting on a cycle member but not part of the loop itself, since that node can reach the cycle but the cycle can never reach back to it). Union all deadlocked node IDs across all SCCs of size >1 plus any single-node SCC with a self-loop, dedupe, sort numerically, and write one per line to `deadlocked_txs.txt`. Test your logic by hand-tracing a couple of the graph's cycles and the "waiter but not in cycle" case before trusting the script output; wrap it all in the required `detect_deadlocks.sh` and `chmod +x` it.
