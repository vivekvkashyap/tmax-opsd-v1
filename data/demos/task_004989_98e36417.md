Build this as a pipeline of small, independently-testable stages rather than one monolithic function. Use `ffprobe -show_packets -of json -select_streams v:0 /app/data_video.mp4` (or `-of csv`) and stream-parse it in Go incrementally (e.g. `json.Decoder` token-by-token, or line-by-line CSV) rather than unmarshalling the whole payload into memory. For sanitizing tags, write a regex that strips non-ASCII bytes and malformed `\uXXXX` escapes — test it against a few crafted strings before trusting it. For dedup, key each packet by a composite of exact `size` and `flags` strings, keep only the first occurrence per key, and preserve original chronological order for everything else. Sort/iterate strictly by `pts_time` ascending before computing anomalies — compare each deduplicated packet's size to the *immediately preceding deduplicated* packet's size (not the raw stream), flag `size > 3.0 * prev_size`, and never flag the first packet. Expose `/api/stats` and `/api/anomalies` as JSON over `net/http` bound to `127.0.0.1:8080`, and make sure the server actually stays running (don't exit after processing) — trigger analysis on startup or lazily on first request, cache the result, and verify with `curl` locally before considering it done.
