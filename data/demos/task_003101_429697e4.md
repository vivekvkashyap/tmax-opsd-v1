Break this into its four stages and verify each output file before moving to the next. (1) Extract frames from `/app/recovery.mp4` with `ffmpeg -i /app/recovery.mp4 -vf fps=1 /tmp/frames/frame_%02d.png` (match the fps to the video's actual frame rate — inspect with `ffprobe` first if unsure), then run `zbarimg` on each frame to decode the embedded QR JSON payloads (`{"path": ..., "expr": ...}`). (2) For each payload, evaluate `expr` as an arithmetic expression — use `bc` or `awk` for the actual numeric evaluation (don't hardcode results), building a `[priority] [path]` line for each, then `sort -k1,1nr` to get strict descending order and write to `/home/user/ordered_files.txt`. (3) Extract just the paths (awk '{print $2}') in your sorted order and `diff -u` against `/home/user/legacy_order.txt`, redirecting to `/home/user/order_diff.patch`. (4) For the TCP server, use a bash `/dev/tcp`-based loop or `nc -lk -p 9000` in a while-loop that reads each line, strips the `QUERY ` prefix, evaluates the trailing expression the same way (`bc`), looks up the matching priority in your data structure (an associative array or the ordered_files.txt content) built once at startup, and replies with the path or `NOT_FOUND`; ensure the loop re-listens after each connection so it survives multiple sequential clients.