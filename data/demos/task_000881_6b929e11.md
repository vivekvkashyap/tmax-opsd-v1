Break this into three separable Bash stages inside `audit.sh` and get each right in isolation before chaining them. (1) Determine violations: use `jq` to filter `access_logs.json` for records where `target_node == "F"`, then for each such record look up the user's role in `employees.csv` with `awk -F,` and keep only rows where role is not "Admin". (2) Compute shortest path length: parse `network.txt` into an adjacency list (e.g. an associative array built with awk/bash, or feed edges to a small BFS implemented in awk) and run a breadth-first search from each violating record's `entry_node` to `F`, counting edges traversed — BFS is required here rather than a simple lookup because paths can require multiple hops and there can be shorter paths through nodes not on the "obvious" route. (3) Cross-map: for each violation, pull the user's `name` from the CSV via the `user_id`, and assemble one JSON object per violation with `jq -n` or by building JSON strings carefully with proper escaping. Sort the final array by `user_id` ascending (`jq 'sort_by(.user_id)'`) and write it to `/home/user/violations.json`, validating it parses with `jq .` before finishing. Avoid Python/Perl/Node per the constraint — do the BFS and CSV/JSON work with awk, sed, grep, and jq.