The core technique is replacing fixed `sleep`s with active readiness polling, then measuring wall-clock speedup against the reference script. Structure `/home/user/fast_provision.py` as: (1) launch `/app/start_services.sh` as a background subprocess (don't block on it); (2) poll each service in a tight retry loop with short sleeps (e.g. 0.1-0.2s) and a timeout — for Redis and the Auth API, attempt a raw `socket.connect(("localhost", port))` (or a real `redis-py`/HTTP health check) inside try/except, retrying until it succeeds; for the worker, attempt to connect to the Unix domain socket at `/tmp/worker_health.sock` with `socket.socket(AF_UNIX, SOCK_STREAM)` similarly; (3) once all three are up, use `pexpect.spawn("/app/admin_cli.py init-cluster")` and drive it with `child.expect("...prompt text...")` / `child.sendline(...)` for username, password, and confirm, then `child.expect(pexpect.EOF)` to capture full output — write that captured output (`child.before`/logfile) to `/home/user/provision.log` verbatim; (4) install the cron entry via `crontab -l` piped through to append `*/5 * * * * /app/health_check.py` and re-install with `crontab -` (careful to preserve any existing crontab lines and not overwrite them). Time your own script end-to-end during development and compare against timing `/app/slow_provision.sh` to confirm you clear the required speedup margin before finishing; make the file executable with `chmod +x`.