Parse `/home/user/project_rules.json` for allowed extensions and the hex magic-byte signature; decode the hex to raw bytes (`bytes.fromhex(...)`) and search for it as a substring in each candidate file's content — for the large file, mmap or stream in chunks rather than loading 10MB into memory. Copy only files whose extension is allowed AND whose bytes contain the magic signature anywhere into `/home/user/clean_project/`. Transcribe `/app/voice_memo.wav` with a speech-to-text tool to obtain the exact `TRANSCRIPT` text — don't hardcode a guess. Build the plain (uncompressed) tar with `tar -cf`. For the network services, run two servers concurrently (threads or `asyncio` or two processes): the HTTP one on 8080 just needs to serve the tar's bytes at `/backup.tar`; the TCP one on 9090 must normalize incoming text (lowercase, strip whitespace/punctuation) before comparing to a similarly normalized `TRANSCRIPT`, and reply with the tar file's SHA-256 hex digest + `\n` on match, else `DENIED\n`. Test both services locally with `curl` and a raw socket client before finishing, and keep the process backgrounded.
