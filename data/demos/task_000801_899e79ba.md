Pipeline this as: extract -> lookup -> decode -> verify -> filter -> sort -> write, and don't skip the verify step. First, grep `link_error.log` for lines matching exactly `cannot find -l<name>` and capture `<name>` (be careful not to match other linker error lines that mention `-l` in a different context). For each missing library name, hex-decode it (e.g. Python `bytes.fromhex(name).decode()`) and search `registry.dat` for a line whose first colon-delimited field equals that hex string exactly — an accidental substring match will pull the wrong record. Base64-decode the second field to get the candidate path, then independently compute MD5 of that decoded path string (no trailing newline) and compare it byte-for-byte against the third field; only keep the path if the hashes match, discarding any registry entry whose checksum doesn't verify (this is the deliberate integrity check the task wants exercised, not skipped). Collect the verified paths only for libraries that were actually reported missing in the log, sort them alphabetically, and write one path per line to `/home/user/ci_artifacts/recovered_paths.txt` with no extra blank lines.
