Start by running OCR on the screenshot (e.g. `tesseract /app/server_audit.png stdout`, possibly preprocessing with grayscale/thresholding via `convert` or `PIL` if raw OCR is noisy) to recover the XOR key and additive constant printed in the image — do not guess these values. Then write `decoder.c` that: takes argv[1] as an uppercase hex string, validates/parses it two hex characters at a time (e.g. with `sscanf("%2x", ...)` or manual hex-digit parsing) into raw bytes, and for each byte reverses the encoding in the correct order — first subtract the additive constant (mod 256), then XOR with the key — matching exactly what the task states ("first subtract the additive constant, then XOR with the key"). Print the resulting bytes as raw characters via `putchar` with no trailing newline and no extra formatting, since the grader does exact byte-for-byte comparison against a hidden reference binary via fuzzing. Compile with `gcc -o /home/user/decoder /home/user/decoder.c`. Before finishing, self-test manually: encode a known string with the recovered key/constant to confirm round-tripping, and test edge cases like odd-length or empty hex input, since fuzzing will explore many lengths.