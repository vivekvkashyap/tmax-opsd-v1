Split this into the two deliverables and build/test each independently. For the hostfile, just write four lines like `worker-0 slots=8` through `worker-3 slots=8` (standard MPI hostfile syntax) — no execution needed, just correct formatting. For `analyze.sh`, don't try to do everything in pure awk from scratch on the HDF5 binary; instead have the script invoke a small inline `python3 -c "..."` (or `h5dump`) to dump the `/links/source` and `/links/target` datasets as plain integers, one per line, then feed both streams together into standard Bash text tools: concatenate source and target values into one list (since the graph is undirected and degree counts appearances in either array), then `sort -n | uniq -c | sort -k1,1nr -k2,2n` to get counts per node ID with ties broken by lowest node ID (the secondary numeric sort on the ID column handles ties correctly — sorting only by count and taking the first line won't reliably break ties without it). Take the top line and format it into the exact required string `Node ID: X, Degree: Y` written to `/home/user/max_degree.txt`. Make the script executable and actually run it so the output file exists before finishing.
