Work the pipeline in dependency order, verifying each stage before moving on. First fix `requirements.txt`: don't just bump one package — resolve the whole set (Flask/Werkzeug/urllib3/requests) to mutually compatible versions, then confirm with an actual `pip install -r requirements.txt` in a clean venv rather than eyeballing version ranges. Second, write `nginx.conf` to listen on 8000 and `proxy_pass http://127.0.0.1:5000;` for all locations; since you're not root, explicitly set `pid`, `error_log`, and the `client_body_temp_path`/related temp directives to writable paths (e.g. under `/home/user/project`) or nginx will fail to start. Third, implement `/process` in Flask: read the uploaded WAV's samples, compute the current peak amplitude, derive the linear gain needed so the peak lands exactly at -3.0 dBFS relative to 32768 (dBFS = 20*log10(peak/32768)), scale all samples by that gain, clip to int16 range, and stream back a valid WAV. Finally write the pytest fixture that posts the sample file through the running Nginx (not directly to Flask) and saves the response, then actually execute the test to produce `processed_audio.wav`.
