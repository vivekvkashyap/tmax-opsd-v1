Read `/home/user/backup_logs.jsonl` line by line with `json.loads` per line (it's JSON Lines, not a single JSON array). Filter to `status == "FAILED"` first, then group by `cluster` using a dict-of-lists (or `collections.defaultdict`). For each cluster compute the mean `duration_seconds` with `round(x, 1)`. For the top-3 longest jobs, sort each cluster's failed records with a compound key: descending `duration_seconds`, then ascending `job_id` as the tiebreaker (`sorted(records, key=lambda r: (-r['duration_seconds'], r['job_id']))`), then take the first 3, padding missing slots with empty strings. Drop any cluster with zero failed jobs entirely. Sort the final cluster rows alphabetically by cluster name before writing. Write the CSV manually or with the `csv` module using the exact header `cluster,avg_failed_duration,longest_job_1,longest_job_2,longest_job_3`, formatting the average with one decimal place (e.g. `f"{avg:.1f}"`) so integers like 125 render as `125.0`. Run the script so `/home/user/failed_backups_report.csv` is actually produced, and diff it mentally against the spec before finishing.
