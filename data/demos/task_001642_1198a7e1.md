Work the three pieces of evidence in order since each later step consumes an earlier discovery. First, find the SUID binary by scanning `/home/user/bin/` for files with the setuid bit set (`find /home/user/bin -perm -4000` or check each file's mode with `stat`/`ls -l`), and write its absolute path to `suid_bin.txt`. Second, grep `/home/user/evidence/server.log` for requests to exactly `/hidden/payload` and pick the one with a `200` status code (there may be a decoy request to the same path with a different status/IP), extracting just the source IP into `attacker_ip.txt`. Third, read `/home/user/attacker_tools/crypto_spec.txt` to confirm the token's plaintext layout is `IP:<ip>:EXP:<exp>` followed by `.` and the lowercase hex HMAC-SHA256 of that plaintext under the given key; scaffold a Rust project with `cargo new`, add `hmac` and `sha2` to Cargo.toml, compute the HMAC over the plaintext built from your discovered IP and the fixed expiration `1900000000`, hex-encode it lowercase, and write the full concatenated token string to `forged_token.txt`. Double-check you're HMACing the exact plaintext string (no extra whitespace/newline) and hex-encoding without `0x` prefixes or uppercase letters.
