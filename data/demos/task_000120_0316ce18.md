Break this into three independently-verifiable stages so mistakes in one don't silently corrupt the next. (1) Run `/usr/local/bin/transcriber /app/backup_log.wav`, capture the text output, and extract timestamp/value pairs with a regex like `grep -oP 'timestamp \K[0-9]+' ` paired with `grep -oP 'value is \K[0-9]+'` (or a combined awk/sed pass) — build a timestamp->value lookup rather than assuming row order matches transcript order. (2) Impute: for each `NaN` row in `/app/raw_data.csv`, substitute the value keyed by that row's `Timestamp` from your lookup, then write the full cleaned CSV to `/home/user/clean_data.csv`. (3) Compute regression/correlation with `awk`/`datamash` using standard sum formulas (sum(x), sum(y), sum(xy), sum(x^2), sum(y^2), n) — do this in one awk pass over the cleaned data to avoid precision loss, and keep at least 4 decimal places (use `%.4f`/`%.6f` printf, not integer truncation). Double check slope/intercept algebra against the standard least-squares formulas before writing `/home/user/model_stats.txt`. (4) Apply `SensorB = m*SensorA + c` per row of `/app/eval_data.csv` in awk to produce `/home/user/predictions.csv` with the exact two-column header, no spaces. Avoid Python/R entirely — verify by re-reading your own script for banned interpreters.
