Work in `/home/user/spectro_graphs` using Python (numpy/scipy). Step 1: load `reference.csv` ids into a set and filter `molecules.json` to only those ids — do this filtering before any Wiener-index or FFT work, since unstable molecules must be fully excluded from every downstream step. Step 2: for each valid molecule's adjacency matrix, compute all-pairs shortest paths (e.g. `scipy.sparse.csgraph.shortest_path` with `unweighted=True, directed=False`) and sum the upper-triangle (i<j) distances for the Wiener Index. Step 3: for the 1024-length raw_signal, take `np.fft.fft`, zero out indices 20..1003 inclusive (keep 0-19 and 1004-1023 untouched — get this slice boundary exactly right, off-by-one errors here are the common mistake), inverse-FFT with `np.fft.ifft`, take the real part, and record its max value as peak amplitude P. Step 4: build design matrix X = [ones, W] per valid molecule in the same order as y = [P], compute `np.linalg.cond(X)` and fit via `np.linalg.lstsq(X, y)` to get the slope (second coefficient). Step 5: round both outputs to 4 decimals and write `/home/user/spectro_graphs/results.json` with exactly the keys `beta` and `condition_number`.