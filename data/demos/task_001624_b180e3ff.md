Build and verify this pipeline stage by stage rather than writing it all at once and hoping. Write `processor.c` to read stdin line by line, split each `DEVICE_ID,VALUE` line on the comma, maintain a small fixed-size table (linear search or simple hash) of running per-device sums up to 50 devices, and on EOF compute `sqrt()` of each device's total sum (link `-lm`), track the max, and `printf` the JSON with `%.2f` for the float. Write the Makefile with a default target building `processor` and linking `-lm`, plus a `clean` target. For `ci_pipeline.sh`, follow the given command hints literally and in order: download `websocat`, `chmod +x` it, `make clean && make`, write the exact 4-line `test_data.csv`, background a `websocat` listen server serving that file on port 8080, `sleep 1` to let it bind before connecting (racing the client against a not-yet-bound server is the main pitfall), pipe the client's received stream into `./processor` redirecting to `result.json`, then `grep` for the expected JSON string to decide whether to write `CI PIPELINE SUCCESS` or `CI PIPELINE FAILED` to `ci_status.log`. Run the script once manually, inspect `result.json` and `ci_status.log`, and only trust the pipeline once you've watched it succeed end-to-end.