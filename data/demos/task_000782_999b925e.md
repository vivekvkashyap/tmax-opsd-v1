Solve this purely with text-processing pipelines, building each stage on the previous file. First, project: `awk '$1 ~ /^prod-/ && $2 ~ /^prod-/' /home/user/backup_dependencies.txt > /home/user/prod_edges.txt` to keep only edges where both endpoints start with `prod-`. Second, for out-degree centrality: `awk '{print $1}' /home/user/prod_edges.txt | sort | uniq -c | sort -k1,1nr -k2,2` gives counts per upstream job sorted by count descending then name ascending for tie-breaking — take the first line's job name and write just that name (no count) to `/home/user/highest_outdegree.txt`. Third, for length-2 paths (A->B->C): self-join the edge list on B — for every edge `A B`, look up all edges whose source equals `B` to get `C`, emitting `A,B,C`. Do this either with a `join` after sorting both copies of the file on the shared column, or with `awk` reading the edge file twice (once into an array keyed by source, once to emit). Sort the final triples alphabetically with `sort` and write to `/home/user/backup_chains.txt`. Double check by hand-tracing the small dependency file to confirm which triples should appear before trusting script output.
