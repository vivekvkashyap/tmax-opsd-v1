Build the pipeline as clear, composable awk/sed/sort stages rather than one dense one-liner, so each filtering rule is easy to verify independently. Skip the header, then for each row validate with awk that `requests` and `errors` are present and match a numeric pattern (e.g. `^-?[0-9]+$`, since values like "abc" or empty fields must be dropped, and negative requests must be dropped) before doing any arithmetic — apply the rules in this order: numeric check, `requests >= 1`, `errors >= 0`, `errors <= requests`. For surviving rows, compute `(errors + alpha) / (requests + alpha + beta)` with `alpha=2, beta=10` using awk's floating point, and format to exactly 4 decimal places with `printf "%.4f"` (not %f's default 6, and be careful of rounding — the sort key must be the same precision as the display value so ties resolve correctly). Sort the result numerically descending on the posterior rate as the primary key and alphabetically ascending on `server_id` as the tiebreaker (e.g. `sort -t, -k2,2nr -k1,1`), then write the final `server_id,rate` lines with no header to `/home/user/posteriors.csv`. Make the script executable and run it yourself to confirm the output file before considering the task done.