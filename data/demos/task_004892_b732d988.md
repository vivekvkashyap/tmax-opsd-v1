Treat the legacy binary as a black box you must characterize by systematic probing before writing any Go code — don't guess at semantics from the task description alone, since it explicitly warns of quirks. Feed `/app/legacy_calc` many crafted JSON payloads via stdin and record stdout for each: simple two-operand expressions to determine operator precedence (the prompt hints precedence may not be conventional — test `2 + 3 * 4` style cases specifically to see if it's evaluated strictly left-to-right vs standard precedence), parenthesized expressions, division (check whether it's integer division and its rounding/truncation direction, and its zero-division error message/JSON), undefined variables, malformed expression syntax, and invalid/malformed JSON — capture the exact error `reason` strings and the exact compact JSON formatting (key order, absence of spaces) for both success and error cases. Once you have a confirmed grammar and precedence model from empirical testing, implement a Go expression parser/evaluator (e.g. hand-rolled recursive descent or a simple left-to-right token scanner matching the observed precedence) and marshal output with Go's `encoding/json` or manual string building to match formatting exactly. Before finishing, script a comparison harness that generates random inputs, runs both binaries, and diffs stdout byte-for-byte across many cases, including edge cases like empty context, negative numbers, and nested parens.