Start by compiling `/home/user/src/estimator.c` with `gcc -o /home/user/bin/estimator /home/user/src/estimator.c` and run it manually on a sample file to confirm its CLI contract (`--tol <value> <file>` printing a float). Then recover the calibration constants from `/app/calibration.png` — do not guess them: run `tesseract /app/calibration.png stdout` (or open/view the image) and parse out the exact `MAX_CONDITION_NUM` and `CONV_TOL` values from the recognized text; if OCR output looks noisy, cross-check by inspecting the image directly. Write `/home/user/check_spectrum.sh` as a bash script that calls the estimator with the recovered tolerance on `$1`, captures the printed condition number, and uses a numeric comparison (e.g. `awk` or `bc`, since bash can't do float comparisons natively) against the recovered max threshold: exit 0 if strictly less than the max, exit 1 if greater-or-equal. Make the script executable with `chmod +x`. Before finishing, test it against sample files in both `/app/corpora/spectra/clean/` and `/app/corpora/spectra/evil/` to confirm exit codes match expectations across the whole corpus, not just one file.