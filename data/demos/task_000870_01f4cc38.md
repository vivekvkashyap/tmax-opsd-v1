Write straightforward, careful C++: open `/home/user/raw_data.csv`, skip the header line, then parse each remaining line by splitting on commas and converting the Temperature, Humidity, and Pressure fields (columns 1-3, skipping Timestamp) to `double` with `std::stod`. Accumulate all rows into three `std::vector<double>` (or a single Nx3 structure) before computing statistics — you need the full column means first. Compute each column's mean, then the 3x3 sample covariance matrix using `Cov(i,j) = sum((x_i_k - mean_i)*(x_j_k - mean_j)) / (N-1)` for k=1..N, with the N-1 (sample, not population) denominator — this is a common off-by-one that naive implementations get wrong. Keep the row/column ordering Temperature=0, Humidity=1, Pressure=2 exactly, and note the matrix is symmetric so you can compute the upper triangle and mirror it. Print with `std::fixed << std::setprecision(4)`, three space-separated values per line, three lines total, to `/home/user/covariance.txt`. Then write `run_pipeline.sh` that does `g++ -O2 process_data.cpp -o process_data && ./process_data`, and `chmod +x` it. Run the pipeline yourself and sanity-check the output is symmetric and roughly matches the intended correlation structure (large diagonal variances, signed off-diagonal covariances) before finishing.
