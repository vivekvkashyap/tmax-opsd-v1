This task has two independent deliverables — build and test them separately. For `abi_linter.sh`: scan the given file for lines matching a ctypes C-call pattern (`lib.<funcname>(...)`, e.g. via grep/regex) that contain a plain quoted string literal argument; a line is unsafe only if it has a bare `"..."`/`'...'` string passed to such a call AND lacks a `b"`/`b'` prefix on that literal AND lacks `.encode(` anywhere on the same line — anything else (byte-prefixed literals, `.encode()` calls, or files with no `lib.func(...)` ctypes calls at all) is safe. Print exactly `ACCEPT`/exit 0 or `REJECT`/exit 1 accordingly, `chmod +x` it, and validate against every file in both `/home/user/app/corpus/clean/` and `/home/user/app/corpus/evil/` before moving on — loop over each file and check the observed verdict against which directory it came from. For the router: write `router.sh` using `socat`/`nc` (or a small loop) to listen on 8080, parse the incoming JSON body for `"version": 2` or `"version": 3` (a simple grep/sed on the raw body is fine, no need for a real JSON parser), forward the request to the matching backend port, relay its response back, and append a `[TIMESTAMP] ROUTED TO <version>` line to `/home/user/app/router_access.log` for every request. Start the multi-service stack via `start_services.sh` first and confirm each backend actually answers on its port before wiring the router to them, then launch the router backgrounded and test both version paths manually with `curl`.
