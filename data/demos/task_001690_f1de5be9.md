Step 1: edit `run_telemetry.sh` in place — explicitly `export HOME="/home/user"` and set `PATH` to include `/sbin` and `/usr/sbin` alongside the existing entries (prepend or append, either works), leaving the call to `collect.sh` untouched; never edit `collect.sh` itself, since it correctly derives its behavior from the environment it's given. `mkdir -p /home/user/logs` first, then execute the fixed wrapper and confirm the log lands in the right place with no "Network command failed" line. Step 2: write `extract_routes.sh` as an INI parser using awk/sed — track the current `[section]` header as you scan lines, and only emit `interface:gateway` when a `gateway=` key is actually found inside that section (some sections legitimately lack one and must be skipped, not defaulted). Step 3: write a standard logrotate stanza for the telemetry log path with `daily`, `rotate 4`, `compress`, and `missingok` inside the braces, then actually run the exact `logrotate -s ... -f?` test command given in the task against it and confirm it exits cleanly before calling this done.