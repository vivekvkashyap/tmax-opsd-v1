Work through the four pieces independently and verify each before moving on. (1) Add `export BACKEND1_PORT=9001` and `export BACKEND2_PORT=9002` to `/home/user/.bash_profile`, source it, then run `/app/start_services.sh` and confirm with `curl`/`ss` that something is actually listening on 127.0.0.1:9001 and :9002 — don't assume the script works just because it exits 0. (2) Write `proxy.go` as a minimal `net/http/httputil.ReverseProxy` (or manual `net.Dial` proxy) with a package-level counter/index that alternates targets on each request (guard it with a mutex if using goroutines) so 10 requests split exactly 5/5 between the two backends; compile with `go build -o proxy proxy.go` and launch it backgrounded (`nohup ./proxy &`) listening on 127.0.0.1:8080. (3) Write `filter.go` to read the file argument, check `len(data) > 10240` and `strings.Contains` for `../` or `..\` first (reject fast), then attempt `json.Valid(data)` before accepting; compile to `filter` and test manually against a big file and a file with `../` before trusting it. (4) `monitor.sh` should use `du -sb /home/user/logs/` (bytes, not `du -sh`) to get an exact byte count, compare against 50000, and print exactly `OK` or `WARNING: QUOTA EXCEEDED` with no extra text. Test each script directly with crafted inputs before declaring done.
