Transcribe `/app/dictation.wav` with an available STT tool (`whisper` or similar), catch the dictated amino-acid names, and map each full name to its standard 1-letter IUPAC code yourself (don't assume a sequence — derive it from what's actually said) before writing a proper FASTA file (`>header` line then the sequence) to `/home/user/target.fasta`. For the server, scaffold a Rust binary crate, add a lightweight HTTP framework (e.g. actix-web, axum, or even a minimal hand-rolled listener) bound to `127.0.0.1:8080`, and implement `POST /simulate` to: read+parse the FASTA at the given path, look up each residue's step length from the fixed table in the spec (default 0.0 for anything not listed), then for `iterations` runs simulate a 3D random walk — per step, draw a uniformly random unit direction (e.g. sample from a normal distribution per axis and normalize, or use spherical coordinates with uniform cosine and angle) scaled by that residue's step length, sum the displacement vector across the whole sequence, and accumulate the squared magnitude of the final displacement. Average across iterations and return `{"mean_sq_dist": <value>}` as JSON. Use `cargo build --release`, launch the binary detached/backgrounded (e.g. `nohup ... &` or `setsid`) so it survives your shell exiting, and verify with a manual `curl` POST before finishing.