Split this into two independent sub-problems and solve them in order. First, OCR the template: run `tesseract /app/watermark.png stdout` (try `--psm 6` or similar if the default garbles it) and inspect the raw output carefully — preserve exact spacing, brackets, and punctuation, since the printed record must match byte-for-byte, and the placeholders in the template correspond positionally to config_id, revision, value (in that order). Second, write the C program: read stdin line by line (getline or fgets with a generous buffer), split on commas, and store records in a hash map or array keyed by config_id (3 uppercase letters gives at most 26^3 keys — a direct-indexed array works well and avoids hashing bugs); for each incoming record, only overwrite the stored one if the new timestamp is higher. After EOF, collect the kept records, sort them alphabetically by config_id (qsort with strcmp), then print each with your extracted template via a format string — build that format string from what you actually OCR'd, not from assumptions. Compile with `gcc -O2` and test manually with crafted duplicate-timestamp CSV input before trusting it against the fuzzer. Watch for off-by-one buffer sizes given values up to 32 chars and up to 5000 lines.
