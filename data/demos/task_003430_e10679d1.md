Read `/home/user/app/server.py` first to locate the two bugs precisely, then write a fixed copy at `server_fixed.py` (keep class/method names identical). For the precision-loss bug: naive `total += delta` truncates when `delta` (1e-9) is many orders of magnitude smaller than the accumulator (1e8) due to float64's ~15-17 significant digits — fix by accumulating a compensated sum (e.g. Kahan summation, keeping a running error-correction term) or by tracking the small deltas separately and periodically merging (e.g. `math.fsum` over a buffer of pending deltas, or maintaining two floats: a coarse total and a fine remainder that gets folded in once it's large enough to matter) rather than adding into the same float repeatedly. For the task-leak bug: `handle_process_request` currently just `create_task`s a worker and awaits it — if the outer coroutine is cancelled during `await asyncio.sleep(1)`, the worker task is never cancelled. Wrap the worker await in a try/except/finally (or use `asyncio.shield`/explicit cleanup) so that on `CancelledError` you call `worker_task.cancel()` and `await worker_task` (catching its own CancelledError) before re-raising, ensuring no orphaned task remains. Then write `test_regression.py` exercising both scenarios exactly as described, exiting 0/nonzero appropriately, using only the standard library.