Build this as a clean pipeline in stages, verifying each stage before moving on. First write and test the tokenizer alone (lowercase, strip to [a-z0-9 ], split, drop empties) against a few sample rows to confirm it matches the spec exactly — token boundaries determine everything downstream. Then implement the Naive Bayes probability formula precisely as given, being careful that V (vocabulary size) is computed over the *entire* dataset, not just the training fold, and that N_class counts total token occurrences (not unique tokens) in that class. For cross-validation, use `KFold(n_splits=5, shuffle=False)` exactly as specified — do not shuffle, since fold membership must be deterministic and reproducible. For each alpha, retrain counts from scratch on the fold's training rows only, then measure accuracy on the held-out fold; pick argmax average accuracy, breaking ties toward the smallest alpha value in your iteration order. Finally, refit on the full dataset with the selected alpha, compute LLR = |log P(token|spam=1) - log P(token|spam=0)| for every vocabulary token, sort by LLR descending with alphabetical tiebreak, and take the top 5. Write results as strict JSON matching the required schema.
