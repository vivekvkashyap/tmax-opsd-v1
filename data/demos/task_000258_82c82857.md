Treat these as three independent subtasks and verify each before moving on. Task 1: open `/home/user/log_tool/Makefile` and check whitespace with `cat -A` — Make requires literal tab characters to indent recipe lines, not spaces; fix the indentation and add the missing `-o log_processor` flag to the gcc invocation, then confirm with `make` and check the binary runs. Task 2: write the Go sanitizer to read the file as bytes, validate with `utf8.Valid()` (or decode rune-by-rune to catch invalid sequences), scan for byte values 0x00-0x1F/0x7F excluding \t \n \r, and split on \n to check each line's byte length against the 2000 cap — exit 1 the moment any rule is violated, exit 0 only if the whole file passes; build a few of your own clean/malicious test files first to sanity-check before trusting it against the hidden corpus. Task 3: use `ffmpeg -i /app/test_run.mp4` to extract frames (e.g. one image per frame via `-vsync 0` or the `fps` filter), then script through them (ImageMagick `identify`/`convert` or a small script checking pixel values) to detect the single frame that is uniformly pure red, and write only its 1-based frame index to the output file — don't guess the index, compute it by inspecting the extracted frames.
