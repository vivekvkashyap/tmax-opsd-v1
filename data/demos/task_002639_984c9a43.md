This task has three layers that must each work before the next matters: fix the shared-directory wiring, implement the documented binary log format as your own C parser, then wire up a self-restarting tail pipeline. First inspect `/home/user/app/config.env` and `/home/user/app/start_services.sh` — correct `SHARED_DIR` to point at `/home/user/app/shared_logs` (create it) and fix `LOG_MODE`/env var mismatches so generator, aggregator, and sink all reference the identical path; verify all three keep running (not just exit once) after `start_services.sh` runs. For the parser, implement exactly what the task spec describes for the fixed-size binary record layout (field order, sizes, endianness) and the exact expected print format, and handle partial/invalid trailing chunks at EOF by ignoring them; write your own `/home/user/workspace/alert_parser.c` reading from stdin with `fread`/`struct`-style unpacking, then compile it — build and test your parser independently against small hand-crafted binary inputs you construct yourself to confirm each field decodes correctly before wiring it into the pipeline. For monitor.sh, use a loop like `while true; do tail -f generator.bin | ./alert_parser >> alerts.log; sleep 1; done` so a crash restarts within ~1s, and export `MONITOR_ACTIVE=1` in `~/.bashrc`.
