Reverse-engineer `/app/validator` empirically before writing any glue code: run it against small hand-crafted CSV files (correct header, wrong header, right/wrong column counts) and record the exit codes with `echo $?` — don't assume 0-means-success without confirming. For the C++ covariance program, read all CSV lines from stdin, split each on commas into 3 doubles, compute column means, then the sample covariance matrix `Cov[i][j] = sum((x_i - mean_i)*(x_j - mean_j)) / (N-1)` for all i,j (it's symmetric, so compute the upper triangle and mirror), and print exactly 3 lines of 3 comma-separated values each formatted to 4 decimal places (`std::fixed << std::setprecision(4)`). For `start_service.sh`, use `socat TCP-LISTEN:8888,reuseaddr,fork EXEC:/path/to/handler_script` (or a `nc -l` loop) so each connection is handled independently and the listener survives across connections — a common bug is a one-shot `nc` that dies after the first client. In the per-connection handler: save the incoming stream to a temp file, run `/app/validator <tempfile>`, check its exit code, and only if it succeeds strip the header line (validator requires it, but your covariance math needs pure numbers) before piping the remaining rows into `/home/user/covar`; on validator failure, write exactly `ERR_SCHEMA` back to the client instead.
