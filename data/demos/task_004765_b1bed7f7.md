Debug this stack layer by layer instead of guessing: first try to run the Rust backend directly (`cargo run` or the built binary) and read the panic message — it will point you at a missing required environment variable used for a monitoring/health primitive; supply that variable (e.g. set it in the launch command/script) rather than removing the check. Second, diff/inspect the slug-generation code path in `src/main.rs` for character filtering logic — a filter like "keep only alphabetic characters" is too strict for a slugger that's supposed to preserve digits; it should keep alphanumeric characters (letters AND digits), lowercasing and hyphenating the rest. Fix that predicate so numbers survive slugification. Third, build with `cargo build --release`, copy the resulting binary to `/home/user/backend-bin`, and launch it in the background with the env var set, bound to `127.0.0.1:8080`. Fourth, fix `/home/user/nginx.conf`'s `proxy_pass` to actually point at the now-live `127.0.0.1:8080`, and start nginx with that config (`nginx -c /home/user/nginx.conf`). Only after `curl http://127.0.0.1:9090/process` works end-to-end should you write `/home/user/health.sh` (POST "health check 1", check for exact "health-check-1" response, exit 0/1, `chmod +x`). Test each layer (raw backend, then nginx proxy, then health script) independently to isolate failures.