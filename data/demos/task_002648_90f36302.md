Treat this as building an independent test harness around an existing script, not reimplementing its logic — invoke `/home/user/src/run_etl.py` twice with `subprocess.run(..., env={**os.environ, "ETL_SEED": "42"})`, capturing/saving the output files from each run separately (e.g. copy `similarity.npy`/`posteriors.json` to two different paths between runs, or diff byte-for-byte using `np.array_equal` / raw file comparison after run 1, save copies, then rerun and compare) so you can honestly assert reproducibility rather than assuming it. Load the similarity matrix with `numpy.load` and check symmetry with `np.array_equal(A, A.T)`. For top-3-similar-to-item-0, copy row 0, mask out self-similarity (set index 0 to `-inf` or exclude it before sorting) and use `np.argsort` descending, taking the first 3 indices — get the ordering direction right (most similar first). For posteriors, load the JSON, compute `alpha/(alpha+beta)` per item, and track the max over integer-keyed items (remember JSON keys are strings — cast back to int for the report). Assemble the exact five keys requested into `/home/user/test_report.json`, rounding `highest_ctr_value` to 4 decimals with Python's `round`. Run the harness once at the end to confirm the file is actually produced.