Work through this as a strict, order-sensitive pipeline where each stage's exact semantics matter for reproducibility. First fit `TfidfVectorizer(max_features=50, stop_words='english')` once on the full `text` column so embeddings are consistent across rows. Separate row indices into "valid-target" and "missing-target" sets using the original DataFrame index (don't reset it). For imputation, for each missing row compute cosine similarity between its TF-IDF vector and all valid rows' vectors, pick the argmax (numpy's `argmax` naturally returns the first occurrence on ties, satisfying the lowest-index tie-break), and copy that neighbor's target value — do this on a copy of the target series so you don't contaminate future lookups. For correlation, compute Pearson correlation per feature column using only the originally-valid rows/targets (not the imputed ones), then pick the argmax positive correlation. Train `LogisticRegression(random_state=42)` on the full imputed dataset. For the bootstrap CI, call `np.random.seed(42)` exactly once before the loop (not inside each iteration), sample indices with replacement each iteration, score the already-fitted model on that resampled X/y, collect 1000 accuracies, then take the 2.5/97.5 percentiles with `np.percentile` and format to 3 decimals exactly as `low,high`.
