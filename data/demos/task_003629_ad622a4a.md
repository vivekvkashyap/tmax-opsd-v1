Start the WebSocket server (`python3 /home/user/ws_server.py &`) and write a small client script (Python's `websockets` library is available) that connects to `ws://127.0.0.1:9753`, receives the single text payload until the connection closes, and saves it to a working file so you can inspect the raw concatenated diffs before writing parsing logic. Split the payload into individual unified-diff blocks using the `--- a/...` line as the block delimiter (each block begins with a `--- a/` header). For each block, identify its target path from the `+++ b/...` line, and check whether the block's body contains a removed line (a `-` prefixed line) that references `native_camera` in a file whose target path is `CMakeLists.txt` — drop exactly that one diff block, keep all others intact and byte-for-byte as received. Sort the remaining blocks by their `+++ b/...` target path string (plain alphabetical sort), then concatenate them in that order preserving each block's original internal formatting exactly, and write the result to `/home/user/final_pipeline.patch`. Diff your output against the original blocks to make sure nothing was mangled during splitting/rejoining.
