Start by running `tesseract /app/customer_upload.png stdout` (or via pytesseract) to recover the exact text in the image — do not guess or transcribe it visually, OCR it so you get the precise string including punctuation. Note any apostrophe/quote characters, since those are the likely trigger for a SQL-formatting crash (an unescaped single quote breaking an f-string/query construction). Write/fix `/home/user/server.py` so the query-building code uses parameterized queries or otherwise safely escapes the OCR'd text instead of naive string interpolation. Build `/home/user/mre.py` as a small standalone script that reproduces the original crash with the extracted string, applies the same fix, and prints `SUCCESS: <extracted_text>` using the actual recovered text. For the HTTP service, use Flask (or `http.server`) bound to 127.0.0.1:8080 with a `POST /process` route: check the `Authorization` header equals exactly `Bearer diag-token-xyz` (401/403 otherwise), accept the uploaded image bytes (support both raw body and multipart), run OCR on it, strip whitespace, and return `{"extracted_text": "..."}` as JSON. Launch it with nohup/background and confirm it's listening before finishing.
