Reorder the pipeline instead of just renaming the variable: in `prepare_dataset.sh`, do the 80/20 split first (keep the existing `head`/`tail` split logic), then compute `MAX_WORDS` with awk (`split($2,a," ")` word count) using ONLY `train_raw.csv`, and apply that single `MAX_WORDS` value when normalizing both the train and test raw files — the bug is that the original script computes the max over the full file before splitting, so just moving the awk max-word computation to run after splitting and pointing it at the train file only is the fix. For `validate.sh`, stream `test_normalized.csv` (space-separated, columns id/word_count/label) through a single awk pass accumulating `sumX`, `sumY`, `sumXY`, and `N`, then in the `END` block compute `Cov = sumXY/N - (sumX/N)*(sumY/N)` and `printf "%.4f\n", cov` into covariance_result.txt. Watch the field separator: the normalized files are space-delimited, not comma-delimited like the source CSV, so don't reuse `-F','` from the original script.
