Scaffold with `cargo new /home/user/build_tools/impact_analyzer`, add `tonic`, `prost`, `tonic-build` and define the `.proto` exactly as specified in a `build.rs`. Implement `CalculateImpact`: parse `v1`/`v2` with the `semver` crate, return `Status::invalid_argument` if `v1 >= v2`; build paths `/home/user/protos/<v>/api.proto` and return `Status::not_found` if either is missing. Shell out to `diff -U0 <file1> <file2>` via `std::process::Command`, capture stdout, and count lines carefully: a line counts as an addition only if it starts with `+` AND is not the `+++` file-header line; same logic for `-`/`---` deletions — getting this header-exclusion wrong is the most common bug. Compute `score = D*D*3 + A` and return it. For `test_harness.sh`, build/start the server in the background (`cargo run &` or run the built binary), poll port 50051 (e.g. loop `nc -z 127.0.0.1 50051` or use grpcurl itself in a retry loop) before issuing requests, append both `grpcurl` JSON responses to `/home/user/report.log`, then kill the background server PID before exiting. Verify your diff-parsing logic against a few proto pairs by hand before trusting the formula.
