Work in three stages. First, OCR the notes: run `tesseract /app/params.png stdout` and parse the printed line into the target `mu` and `sigma` (do not assume values; read them from the image). If OCR is noisy, preprocess (grayscale/threshold with ImageMagick) and retry. Second, write the numerics in C++: seed a `std::mt19937` deterministically, draw 10000 target samples from `std::normal_distribution(mu,sigma)` and 10000 reference samples from N(4.5,2.0). For the 1st Wasserstein distance on equal-size 1D samples, sort both vectors and take the mean of absolute differences of the sorted pairs. For the bootstrap, repeat 1000 times: resample 10000 with replacement from each sample (index via `uniform_int_distribution`), recompute the sorted-difference distance, collect the 1000 values, sort them, and read the 2.5th and 97.5th percentiles for `ci_lower`/`ci_upper`. Keep all sorting/simulation in C++. Third, expose results as JSON `{"wasserstein","ci_lower","ci_upper"}` on `GET /metrics` at `127.0.0.1:8080` (cpp-httplib, or a Python wrapper calling the compiled binary). Naive traps: comparing unsorted samples, or resampling without replacement. Leave the server running.