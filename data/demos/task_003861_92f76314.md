Break this into the three phases the task describes and validate each before moving to the next. Parse `ingest.log` by splitting on `[START]`/`[END]` blocks (e.g. regex or manual line-buffering), extracting `Artifact-ID` and `Status` per record, and keep only `SUCCESS`/`WARNING` (drop `FAILED` outright). Cross-reference surviving IDs against `repo_index.json` to get filename/project/version, then for each candidate zip, verify integrity with Python's `zipfile.ZipFile(...).testzip()` (returns `None` if OK) or `unzip -t` — catch `zipfile.BadZipFile` for files that aren't valid zips at all, and drop those from the pipeline too. For survivors, `os.makedirs(..., exist_ok=True)` the `<project>/<version>` path and `os.link()` (hard link, not copy) the original zip into it. For the `latest` symlink per project, parse each version string into a tuple of ints and pick the max via proper semantic-version comparison (not lexical string sort, since e.g. "10.0.0" > "2.0.0" numerically but not as strings) before calling `os.symlink`. Finally emit the CSV sorted by project ascending, version descending (again using semver-aware comparison, not string sort), containing only artifacts that passed both checks.
