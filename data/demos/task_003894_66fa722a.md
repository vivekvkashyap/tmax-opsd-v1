Use `mmap()` to map `/home/user/project_data.bin` read-only, then walk it as a pointer/offset-based record parser rather than doing sequential I/O. Maintain a "visited offsets" set (e.g. a dynamic array or hash set of `size_t`) that you check and insert into at the start of processing each record's offset — before reading the 5-byte header at an offset, if that offset is already in the visited set, stop immediately (this is what breaks the cycle; a naive parser without this check will spin forever). Parse byte 0 as the type tag and bytes 1-4 as a little-endian uint32 (careful with alignment — read via `memcpy` into a `uint32_t`, don't cast the pointer directly, since the mapped bytes aren't guaranteed aligned). For `T`, copy `Length` bytes of payload starting right after the header into your output buffer, then advance the current offset to just past the payload. For `L`, set the current offset to the given absolute offset and continue there (do not advance further). Bounds-check every offset and length against the mmap'd file size before reading. Write the accumulated payload bytes verbatim (no added newlines) to `/home/user/extracted_text.txt`.