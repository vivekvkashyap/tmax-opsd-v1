Treat this as three separable fixes, verify each in isolation before chaining them. First, get the build working: `build.sh` must compile evaluator.cpp into a position-independent shared object — add `-fPIC -shared -o lib/libevaluator.so` (create the lib dir if missing) so Python's ctypes can dlopen it; confirm with `nm -D` or a trivial ctypes load before touching the C++ logic. Second, harden the C++: scan the recursive-descent expression parser for uninitialized pointers/variables (initialize all locals), array indexing into pixel buffers that isn't bounds-checked against width*height*channels, and any integer arithmetic (e.g. `int` accumulators or `/` between ints) that should be `double`/float division for per-pixel scores — cast operands before dividing. Also check operator precedence in the parser (multiplication must bind tighter than addition; a naive left-to-right eval without precedence climbing will misparse `R*0.299 + G*0.587 + B*0.114`). Third, once it builds and runs without segfaulting on the real recording, compare a few frame scores from output.json against a quick reference grayscale-weighted-average you compute independently in Python to sanity-check before relying on the full MSE threshold.
