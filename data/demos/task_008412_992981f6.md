Read `extract.go` to see exactly where the vulnerability is: it joins the archive entry's name directly onto the destination directory with `filepath.Join` and writes there with no bounds check, so a crafted entry name containing `../` sequences can resolve outside the target directory (Zip Slip). Fix it by, for each entry, computing the joined path, then resolving it to an absolute/clean path and checking it still has the destination directory's absolute path as a prefix (with a path separator boundary, not just a string prefix, to avoid `/home/user/configs-evil` false-passing against `/home/user/configs`) — skip extraction entirely for any entry that fails this check, do not error out the whole program. Do not extract manually with `unzip`; compile and run the fixed Go program itself so the safety logic is what actually produces the files. Afterward, generate the manifest by `cd`-ing into `/home/user/configs/` and running `sha256sum` over the extracted files (using a glob or `find` that covers nested paths like `sub/db.json`), then sort the output lines alphabetically by filename before writing to `/home/user/manifest.txt`. Confirm no file escaped by checking the parent/grandparent directories for anything unexpected.
