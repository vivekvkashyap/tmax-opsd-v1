Build this as three separate, testable pieces rather than one tangled script. First write `analytical.awk`: read paired X,Y values (via `paste -d, X.csv Y.csv | awk -F,`), accumulate sums to compute mean(X), mean(Y), then Cov(X,Y) = mean(X*Y) - mean(X)*mean(Y) and Var(X) = mean(X^2) - mean(X)^2 (population, not sample, formulas), derive w = Cov/Var and b = mean(Y) - w*mean(X), and print with `printf "%.4f,%.4f\n", w, b`. Second write `gd.awk` implementing batch gradient descent: initialize w=0, b=0, and for 500 epochs compute predictions for all points, the gradients (mean error terms w.r.t. w and b), and update both simultaneously using learning rate 0.5 — update w and b using the *old* values of both in each epoch's gradient computation, not partially-updated ones. Verify the two methods converge to matching values on well-scaled [0,1] data. Third, `test.sh` should run both awk scripts, parse their `w,b` output, compute `abs(w1-w2)` and `abs(b1-b2)` (use `awk` or `bc -l` for floating comparisons since bash lacks float arithmetic), and write PASS/FAIL to `/home/user/regression.log` accordingly, then actually execute it so the log exists.