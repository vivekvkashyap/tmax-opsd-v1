Handle the two subtasks as separate scripts you can verify independently. For the overhang: read `/home/user/sequence.txt`, use `str.find("GGTCTC")` (or a regex) to locate the first BsaI site, slice the 4 characters immediately after it, then reverse the string and complement each base with a simple A<->T, C<->G mapping (build a translation table, e.g. `str.maketrans('ACGT','TGCA')`, then reverse the translated string) and write the 4-char result to `overhang.txt`. For the qPCR data: load the 96x40 CSV with `numpy`/`pandas`, and for each row slice columns 5 through 15 (remember columns are 1-indexed in the spec but 0-indexed in your array, so that's indices 4:15) as `y`, with `x = np.arange(5,16)`; fit `y = A*exp(r*x)` per row via `scipy.optimize.curve_fit` seeded with `p0=[10, 0.2]`, collecting the fitted `r` for all 96 wells. Reshape the 96 r-values into an 8x12 array in row-major order (`np.reshape(rs, (8,12))`), take the column-wise max (`axis=0`), and write the 12 values comma-joined with exactly 4 decimal places (`f"{v:.4f}"`) to `col_max_r.txt`.
