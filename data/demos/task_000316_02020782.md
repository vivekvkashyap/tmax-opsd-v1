Treat `/app/tokenizer_oracle` strictly as a black box: don't try to reverse-engineer or replicate its internal algorithm — just shell out to it (`os/exec` in Go, writing the text plus newline to its stdin and reading its stdout) and parse the 20 whitespace-separated floats it returns with `strconv.ParseFloat`. Build the Go module (`go mod init data_api`) with `net/http` only (no third-party router). For `/process`: decode the JSON body, invoke the oracle per request, average indices 0-9 for D1 and 10-19 for D2, append the pair to an in-memory slice (guard with a mutex since concurrent requests are possible), and return `{"reduced_vector":[D1,D2]}`. For `/stats`: compute the sample mean and sample standard deviation (divide the variance sum by N-1, not N — a common bug) of all stored D1 values, then CI bounds as `mean ± 1.96*stddev/sqrt(N)`; special-case N<2 to return zeros for mean/CI rather than dividing by zero or NaN. Set `Content-Type: application/json` on every response and start the server bound to `127.0.0.1:9000`, leaving it running in the background (e.g. `nohup ... &`) so the verifier can hit it.
