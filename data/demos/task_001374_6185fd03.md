Build this as a straight-line Go pipeline: init the module (`go mod init cleaner`), then read `/home/user/raw_reviews.csv` with `encoding/csv` (set `FieldsPerRecord = -1` or handle quoted commas carefully, since review text can itself contain commas — a naive `strings.Split(line, ",")` will corrupt rows). Apply the filters in order (empty text, rating out of [1,5], negative price) before computing anything else. For tokenization, lowercase the ORIGINAL text into a separate variable, strip only `.` and `,`, then split on single spaces with `strings.Split` (not `Fields`, since the spec says split on single spaces) and count non-consideration of extra whitespace as tokens — but write the untouched raw `Text` column back to the CSV output, not the tokenized string. Keep parallel slices of TokenCount and Rating for the surviving rows only, then compute Pearson correlation yourself (mean-center both vectors, sum of products over sqrt of sum of squares) rather than trusting a library default without checking it matches the standard formula. Format the final float with `%.4f` and verify sign and decimal count before writing `/home/user/correlation.txt`. Run `go build` and execute the binary to confirm no runtime panics before finishing.
