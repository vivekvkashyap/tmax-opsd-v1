Push the heavy lifting into SQL and let Go just execute the query and format CSV output. Open `network.db` read-only (e.g. `sqlite3.Open("file:/home/user/network.db?mode=ro", ...)` with the mattn/go-sqlite3 driver, or simply avoid any write statements) and write a single query using CTEs and window functions: (1) a CTE that dedupes edges by `(src,dst)`, keeping only the row with `MAX(timestamp)` per pair — e.g. `ROW_NUMBER() OVER (PARTITION BY src,dst ORDER BY timestamp DESC) = 1`; (2) from the deduped edges, compute each node's total degree by `UNION ALL`-ing src and dst occurrences and grouping by node id, joining to `nodes` to get department; (3) apply `DENSE_RANK() OVER (PARTITION BY department ORDER BY total_degree DESC, id ASC)` for the tie-break rule; (4) filter to `rank <= 3`. In Go, run this query, iterate rows, and write them to `top_nodes.csv` using `encoding/csv`, first writing the exact header row, then ensuring final output ordering is `department ASC, rank ASC, node_id ASC` (add an explicit `ORDER BY` in SQL or sort in Go — don't rely on incidental row order). Test your query logic against a small manual example to confirm the dense-rank tie behavior before trusting the full dataset.
