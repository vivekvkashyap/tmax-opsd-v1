Inspect `/home/user/legacy_data/` first (`ls`, peek at a sample line) to confirm the JSONL schema before coding. In `migrate.py`, for each line: parse JSON, recompute `zlib.crc32(data.encode('utf-8')) & 0xFFFFFFFF` and format it as lowercase hex (`format(..., '08x')`) to compare against the stored `crc32` field — a record is corrupt (dropped) only when these differ, so get the hex formatting/casing exactly right or valid records will be wrongly rejected. For valid records, compute `hashlib.sha256(data.encode('utf-8')).hexdigest()` as `record_id`, and write `{"record_id": ..., "content": data}` to `/home/user/new_data/<hash[:2]>/<hash>.json`, creating the shard subdirectory as needed. Time the whole processing+I/O with `time.perf_counter()` around the loop, then write `benchmark_report.json` with `total_valid`, `total_invalid`, and `records_per_second` (valid+invalid count divided by elapsed seconds). For `ci_pipeline.sh`: run `python3 /home/user/migrate.py`, then parse the resulting JSON (e.g. with `python3 -c` or `jq`) to compare `total_invalid` against `total_valid`, exiting 1 if invalid >= valid, otherwise printing exactly `CI SUCCESS` and exiting 0. Make it executable and run it end-to-end to confirm both the sharded files and the report look right.
