Since Python/Perl are disallowed, build this as a shell pipeline where each stage handles one file's quirks before a final merge/sort. Convert each source file to UTF-8 first with `iconv -f <source-encoding> -t UTF-8` (Latin-1 for the CSV, the TSV and JSON are already UTF-8) so all downstream text processing is consistent. Parse each format with the right tool: `awk -F,`/`-F'\t'` for the CSV/TSV (skipping header rows), and `jq -r` for the JSON array, projecting each into a common `id,name,email,phone` shape per record. Apply field transforms with `awk`/`sed`/`tr`: zero-pad id to 6 digits (`printf "%06d"`), trim whitespace and uppercase name (`tr '[:lower:]' '[:upper:]'` under a UTF-8 locale so accented letters uppercase correctly), lowercase and trim email. For phone, strip all non-digit characters (`tr -cd '0-9'` or `sed 's/[^0-9]//g'`), then apply the exact rule order: if 11 digits and starts with '1', drop the leading '1'; if the result is 10 digits, format as `XXX-XXX-XXXX`; otherwise output the literal string `INVALID`. Concatenate all three sources' transformed rows, prepend the header, and pipe through `sort -t, -k1,1n` (or numeric key sort) on the id column before writing the final file — diff your output against the expected structure before finishing.