Start by fixing the vendored package: grep `setup.py` in `/app/pyzstd-0.15.9` for `extra_compile_args`, find the bogus flag injected there, delete just that flag entry (keep any legitimate flags), then `pip install .` from that directory and confirm with `python3 -c "import pyzstd; print(pyzstd.compress(b'x', level=19))"` before moving on. For file selection, walk `/home/user/raw_logs/` recursively (`os.walk` or `pathlib.rglob`) and use `os.path.getmtime()` compared against the Unix timestamps for 2021-01-01 00:00:00 and 2021-12-31 23:59:59 inclusive — don't rely on filename patterns. For each matching file, don't assume UTF-8: try decoding as UTF-8 first, and if that raises `UnicodeDecodeError`, fall back to trying UTF-16LE then ISO-8859-1 (which never fails, so it's a safe last resort) — or use a lightweight heuristic/BOM check if you prefer determinism over trial-and-error. After decoding to a Python str, split into lines and drop any line whose content starts exactly with `[DEBUG]` (str.startswith, case-sensitive), then re-join and encode as UTF-8. Concatenate all cleaned file contents in a stable order, then compress the combined bytes with `pyzstd.compress(data, level=19)` and write the raw compressed bytes (binary mode) to `/home/user/archive.zst`. Check the final file size against the stated threshold to catch filtering/compression mistakes early.
