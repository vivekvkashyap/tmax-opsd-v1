Treat this as a standard ODE parameter-fitting problem: define the two-compartment ODE system as a function for `scipy.integrate.odeint`, then use `scipy.optimize.curve_fit` or `least_squares` to fit `k1, k2` against the observed `plasma_conc`/`tissue_conc` columns in `/home/user/drug_data.csv`, constraining both to be positive (e.g. fit in log-space, or pass `bounds=(0, np.inf)` to `least_squares`, or use `curve_fit` with `bounds`). Use reasonable positive initial guesses since the optimizer can diverge or converge to a local minimum with poor starting values. Once fitted, get plasma AUC by integrating your ODE solution for plasma concentration from t=0 to t=50 with `scipy.integrate.trapz`/`simpson` on a fine time grid (or `odeint`'s own output over that range) — don't just use the noisy data points, which only run to the data's own max time. For steady state, note the two-compartment system conserves total drug mass and the compartments equilibrate proportionally to the *opposite* rate constant (the compartment with the slower outflow accumulates more) — derive `p_plasma` and `p_tissue` algebraically from your fitted `k1, k2` rather than simulating to a very large t. Finally compute KL divergence of your `[p_plasma, p_tissue]` against `[0.3, 0.7]` using the natural-log formula `sum(p_i * ln(p_i/q_i))`, and write all four values to `/home/user/results.json` with at least 4 decimal places of precision.
