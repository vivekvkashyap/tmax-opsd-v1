Build and test each piece in isolation before chaining them through a git push. First verify the IoT simulator on port 8888 is reachable (`nc -v localhost 8888` and manually walk through login/command by hand) so you understand its exact prompt strings before scripting against them. Set up `git init --bare /home/user/iot_repo.git`, `ln -s /home/user/iot_repo.git /home/user/active_repo`, and `mkdir /home/user/deploy_stage`. Write the post-receive hook as a bash script that reads the pushed ref from stdin, uses `git --work-tree=/home/user/deploy_stage --git-dir=/home/user/iot_repo.git checkout -f` to materialize files, then runs `ip route get 8.8.8.8 > /home/user/deploy_stage/route_info.txt`, then invokes the Expect script — all with absolute paths since hooks run with cwd inside the bare repo's internals. Write trigger_sync.exp using `spawn nc localhost 8888` (or telnet), `expect "Device login:"`, `send "edgeAdmin\r"`, `expect "Command>"`, `send "SYNC /home/user/deploy_stage\r"`, then let it finish/close. Chmod +x both the hook and the .exp file. Test the Expect script standalone against the running simulator first (checking `/home/user/device_sync_log.txt` appears) before wiring it into the hook, then do the full clone/commit/push cycle to trigger it end-to-end.