Implement this as one Rust binary with three clearly separated computations, then compile and actually run it — don't hand-write the JSON output. For storage, recursively walk `/home/user/model_artifacts/` (std::fs::read_dir, recursing into subdirectories if any) and sum `metadata().len()` across all files. For tokenization, read the CSV (skip header row), and for the `text_sample` column split on ASCII whitespace (`str::split_whitespace` handles this correctly, avoiding empty-token bugs from naive `split(' ')`), summing token counts across every row. For the Bayesian step, compute n and the sample mean of `latency_ms` directly from the parsed rows, then apply the two update formulas exactly as given: first compute posterior variance via `1/sigma_n^2 = 1/sigma_0^2 + n/sigma^2`, then plug into the mean formula — do the division carefully as floating point (f64), and round only the final posterior mean to 2 decimal places for the JSON output (don't round intermediate quantities, which would compound error). Use `serde_json` or manual string formatting matching the exact key names given. Build with `cargo build` (adjusting Cargo.toml only if you add crates) and run the binary so `/home/user/experiment_summary.json` is actually produced, then spot-check the numbers against a hand calculation.