Build this in layers and verify each independently before wiring them together. (1) `/home/user/setup_fs.sh`: use `mkdir -p /home/user/frame_cache` (idempotent, no error if it exists) followed by `chmod 700 /home/user/frame_cache`; run it twice yourself to confirm idempotency. (2) `/home/user/video_api.py`: a minimal Flask (or stdlib http.server) app bound to `127.0.0.1:9090` only — not `0.0.0.0`. `/health` returns `{"status":"running"}` with no auth. `/frame_hash` must check the raw `Authorization` header string equals exactly `Bearer vault-key-2024` (reject anything else, including case variants or missing header, with 401) — do the ffmpeg extraction and sha256 hashing on-demand inside the handler (using `subprocess.run` with the exact ffmpeg args given, then `hashlib.sha256` over the file bytes), and wrap in try/except returning 500 on failure. (3) `/home/user/launch.sh`: idempotent — check if something is already listening on 9090 (e.g. `curl -s 127.0.0.1:9090/health` or checking a pidfile/`lsof`) before starting a new background process with output redirected to `/home/user/api.log`. Test the full flow yourself with curl (no auth, wrong auth, correct auth) before finishing, and leave the service actually running at the end.
