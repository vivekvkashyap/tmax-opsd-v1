Load the JSONL with pandas, parse `timestamp` to datetime, and process per `server_id` group separately since each server has its own min/max range. For each group, set the timestamp as index, sort it, then `resample('1H').ffill()` to fill gaps forward from the prior record, and additionally `bfill()` afterward to backfill only the leading gap if the very first resampled slot precedes the first real record — apply this per-group, not globally, or ranges will bleed across servers. Reindex/resample using `pd.date_range(start=group_min, end=group_max, freq='1H')` explicitly if `.resample()` truncates unexpectedly. After reconcatenating all groups, mask `ip_address` with a regex replacing the last dot-segment (e.g. `re.sub(r'\.\d+$', '.XXX', ip)`), applied to every row. For the stratified sample, use `df.groupby('role', group_keys=False).sample(frac=0.25, random_state=42)` so each role contributes proportionally with a fixed seed — don't use a single global `.sample(frac=0.25)`, which isn't stratified. Format `timestamp` back to ISO 8601 strings and write both CSVs with `index=False`.