Because the output must be exactly reproducible, treat this as "replicate the exact sequence of RNG calls the spec describes," not "get a statistically similar answer." Compute sample mean and stddev (N-1 denominator) from data.csv, seed `rng := rand.New(rand.NewSource(12345))` once, then draw the 1000 initial conditions in a single sequential loop calling `rng.NormFloat64()*s+mu` in order (no goroutines, no reordering — any deviation from strict call order changes every downstream value). For each initial value, integrate the given ODE with RK4 using the exact k1..k4 formulas and dt=0.1 for 50 steps to get y(5). For the bootstrap stage, continue drawing from the same `rng` with `rng.Intn(1000)` sequentially to build each of the 2000 resampled arrays, sort each sample, take the average of indices 499/500 as the median, then sort all 2000 bootstrap medians and read off indices 50 and 1950 for the CI bounds. Format every value with 4 decimal places in the exact `Label: value` layout requested, then `go build`/`go run` to produce output.txt.
