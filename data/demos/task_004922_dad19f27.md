Scaffold a Rust binary crate at `/home/user/feature_pipeline` with `hdf5`, `ndarray`, `ndarray-linalg` (pick a linked BLAS backend feature, e.g. `openblas-static`), and `rayon` as dependencies — get a trivial "open and print shapes" build working first since HDF5/BLAS linking is the most likely source of setup friction. Read `sequences` (fixed-length byte strings — decode carefully, trimming any null padding) and the 4x4 `interaction_matrix` from `/home/user/raw_data.h5`. For each sequence, count A/C/G/T occurrences in that exact order and divide by sequence length to build vector `b`, then solve `A x = b` using `ndarray_linalg`'s `.solve()` (or LU factorization) rather than manually inverting the matrix. Parallelize the per-sequence solve with `.par_iter()` from rayon, collecting results while preserving original order (rayon's `par_iter().map().collect()` on an indexed structure keeps order). Write the resulting `(N,4)` f64 array to `training_features.h5` under dataset `features`, then compute column-wise means and write them comma-separated to `/home/user/feature_means.txt`. Sanity-check a few rows against a hand solve before trusting the pipeline.
