Treat this as two independent workstreams verified separately: a byte-exact CLI parser and a live multi-service pipeline.

For the parser, don't rewrite `parser.c` from scratch — read it carefully to locate the specific memory bugs: look for any function returning the address of a local (stack) buffer to the caller (a classic dangling-pointer bug — fix by using caller-allocated or heap-allocated/`strdup`'d memory instead), and look for a struct that gets `free()`'d more than once along some code path (fix by nulling the pointer after free or restructuring ownership so only one place frees it). After fixing, `make parser` to build `/home/user/app/build/url_parser`, then validate correctness yourself before trusting it: feed it several valid `test-env://service/method?bench=N&retry=M` strings plus deliberately malformed ones (missing slashes, non-numeric bench, empty scheme) and compare its JSON output against `/home/user/oracle/url_parser_oracle` on the same inputs using a small bash loop with `diff`, fixing any remaining discrepancies before moving on — do not just eyeball the C code and assume correctness.

For the service composition, read `gateway.json` to find the placeholder/zero port values, fix `redis_port` and `grpc_backend_port` to the given ports, `make gateway`, then start redis-server, the Python mock backend, and the gateway in that dependency order, and confirm end-to-end with a real `curl` POST to the `/benchmark` endpoint before leaving everything running.
