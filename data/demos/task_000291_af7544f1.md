Treat this as a two-hop join, not a general graph search: build an adjacency structure from `edges.csv` filtered to `relation == BUYS` (User->Order) and a second one filtered to `relation == CONTAINS` (Order->Product), then for every BUYS edge look up matching CONTAINS edges sharing the same intermediate Order id and emit (user_id, product_id) pairs — no need for generic path search since the pattern length is fixed at 2 hops. In C, parse each CSV line with `sscanf`/`strtok` respecting the stated max line length (256) and array caps (10000 nodes/edges), storing edges in simple arrays. After generating all pairs, deduplicate if the same (user,product) can arise from multiple order paths only if the spec implies it should (check whether duplicates are expected — if not, dedupe), then sort primarily by numeric user_id and secondarily by numeric product_id using `qsort` with a proper integer comparator (not string comparison, which would misorder multi-digit ids). Write the sorted pairs to `/home/user/results.csv` as `user_id,product_id` with no header, one pair per line, and verify by compiling with `gcc` and running against the actual `nodes.csv`/`edges.csv` present at runtime.
