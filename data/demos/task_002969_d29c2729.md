Treat this as a real incident: don't fix blind. First reproduce the hang by loading the vendored `dpkt` from `/app/dpkt-1.9.8` and feeding it packets from the pcap with a short timeout (e.g. run parsing in a subprocess or signal-based alarm) to confirm which packet(s) trigger it — inspect the pcap with `scapy` or `dpkt` itself (carefully, with a timeout wrapper) to see the TCP options bytes on the offending packet. Then read `dpkt/tcp.py`'s option-parsing loop: look for the `while i < len(opts)` loop and its `opt_len` boundary check — a boundary that accepts `opt_len` values too small (e.g. treating 0 or near-zero as valid) lets the index fail to advance, causing an infinite loop on malformed/truncated option bytes. Fix the check so any `opt_len` that can't guarantee forward progress causes the loop to safely raise/break rather than spin, without touching correct-packet behavior. For the detector script, don't hardcode the exact byte pattern from the one sample packet — instead reproduce the vulnerability condition itself: parse each packet's TCP options length bytes and check whether the same boundary condition that caused dpkt to hang would be triggered (i.e., detect the structural TCP options malformation independent of any single packet's payload), since the hidden test suite uses many different crafted packets exercising the same class of bug. Print exactly `EVIL`/`CLEAN` with exit code 0 always.