Diagnose by reading the Python script's own failure conditions rather than guessing — since you're told not to modify `check_health.py`, treat it as the spec: it checks for `TZ=UTC` and `LC_ALL=C` in the environment and for a readable file at `/home/user/logs/current/app.log`. First create the missing symlink with `ln -s /home/user/app_data/logs /home/user/logs/current` (a directory symlink, so `.../current/app.log` resolves through it to the real log file). Then edit `/home/user/monitor/run_monitor.sh` to `export TZ=UTC` and `export LC_ALL=C` on lines before the `python3 .../check_health.py` invocation (exporting in the same shell process so the child inherits them, not just setting them inline in a way that doesn't persist). Run the wrapper script and confirm `/home/user/monitor/status.txt` is produced with the exact expected text; if it fails, read the script's own printed FAIL message to see which specific check is still unmet rather than re-guessing blindly.
