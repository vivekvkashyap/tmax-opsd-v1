Write the C program as a straightforward single-pass CSV parser: read the header line and discard it, then read each subsequent line with fgets/sscanf into sensor_id, timestamp, metric_A, metric_B, and the raw event_log string. Track seen (sensor_id, timestamp) pairs in a hash set or simple linear array of structs (dataset is small, so an O(n^2) scan against previously-seen entries is fine) and skip any row whose pair was already recorded — check this before writing anything for that row, since a duplicate must produce zero output rows, not partial ones. For the event_log field, locate the first ';' with strchr (or use the whole string if none exists), copy only that substring, then lowercase it in place with tolower over each byte — don't lowercase the full log field, only the extracted token. Emit two output lines per unique row (metric_A then metric_B, in that column order) using `%.2f` formatting for the metric value so it always shows exactly two decimals. Compile with the exact `gcc -O3` command given, run the binary against `/home/user/raw_sensors.csv`, and diff your generated `clean_telemetry.csv` line-by-line against a hand-traced expected output before considering it done.
