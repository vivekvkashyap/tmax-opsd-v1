Build a thin stateless wrapper service — most of the work is correctly plumbing base64/temp-files/subprocess/diff, not reimplementing the solver. For `POST /resolve`: base64-decode `graph_b64` and `manifest_b64` to temp files, invoke `/app/peer_resolver <graph_path> <manifest_path>` via subprocess and capture stdout, which is a unified diff against the manifest. Apply that diff programmatically (e.g. shell out to `patch`/`git apply` against a copy of the decoded manifest, or use a Python unified-diff-apply library) rather than hand-parsing the patch text, since diff format details (context lines, hunk headers) are easy to get subtly wrong by hand. Read back the patched file's contents as a string for the response. For `GET /benchmark`, time 50 sequential invocations of `/app/peer_resolver /app/sample_graph.bin /app/sample_manifest.json` (wall-clock per call, e.g. `time.perf_counter()`), average the milliseconds, and return `{"avg_time_ms": <float>}`. Bind the server to `127.0.0.1:9090`, start it in the background, and sanity-test both endpoints — including that the patched manifest string exactly reflects the diff's insertion — before finishing.
