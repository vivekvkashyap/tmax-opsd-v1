Start by extracting the exact configuration from the image: run `tesseract /app/sensor_config.png stdout` (try different page-segmentation modes if text is misread) and carefully parse out `W_SIZE`, `THRESH_MULT`, and the `FMT` string verbatim — since your output format must match this template byte-for-byte, transcribe it precisely rather than approximating. Implement `/home/user/stream_cleaner.py` as a streaming line processor: for each stdin line, try/except a JSON parse and a required-field/float-cast check, silently `continue`ing on any failure. Maintain a `dict` of `sensor_id -> deque(maxlen=W_SIZE)` so each sensor's rolling window is independent and older values drop off naturally; compute the mean over whatever is currently in the deque (handles the "fewer than W_SIZE" case automatically). Compute `abs(value - mean) > THRESH_MULT * mean` for the ANOMALY/OK status, then `.format()` the extracted `FMT` string with the recovered field names, formatting mean to 2 decimals. Preserve output order exactly matching valid input order (natural since you process line-by-line). Finally write `/home/user/pipeline.sh` with the given `cat | python3 ... > ...` pipeline and register it in the crontab with `*/5 * * * *` via `crontab -e` or `crontab -l | { cat; echo "..."; } | crontab -`. Test your script against a handful of crafted good/bad JSONL lines before trusting it against the fuzzer.