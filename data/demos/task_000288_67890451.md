Install `sentence-transformers` and load `all-MiniLM-L6-v2`, then read `/home/user/raw_texts.txt` preserving original line order. The core dedup logic must be sequential and greedy: maintain a growing list of "kept" sentence embeddings; for each new sentence in original order, compute cosine similarity against every already-kept embedding (not against all sentences, and not pairwise-all-at-once), and discard it the moment any comparison reaches or exceeds the 0.85 threshold (use `>=`, not `>`) — a common mistake is comparing only to the immediately preceding sentence rather than to all previously-kept ones, or deduping via a single global clustering pass that ignores order-dependence. Write survivors to `/home/user/cleaned_texts.txt` in original order. For vocabulary extraction, lowercase each kept sentence and tokenize using a regex equivalent to `[a-z0-9]+` (e.g. `re.findall`) rather than naive whitespace/punctuation splitting, which would let empty strings or stray punctuation leak into the token set. Collect the union of tokens across all kept sentences, filter out any empty strings, sort alphabetically, and write one per line to `/home/user/vocab.txt`. Double check cosine similarity is computed on the raw embedding vectors (dot product over norms), not on some other distance metric.