Build `deploy.sh` step by step matching the checks in order: `set -euo pipefail`, ensure `/home/user/app_storage/` exists, then compute usage with `du -sk /home/user/app_storage | cut -f1` and compare against 5000 *before* extracting anything (so a violation aborts cleanly without side effects). Extract to a unique `tmp_extract_$$` dir, grep `run.sh` for `curl`/`wget` (case matters less than presence) and abort+cleanup+log on match. On success, derive the app name by stripping `.tar.gz` from the tarball's basename, `mv` the temp dir into place, launch `run.sh` in the background with `nohup ... > app.log 2>&1 &`, capture `$!` as the new PID, and only after that succeeds read any prior `current.pid` and `kill -15` it (order matters — kill old only once new is confirmed started) before overwriting `current.pid`. Append the exact `[SUCCESS]`/`[FAILED]` log lines to `/home/user/deploy.log` for every attempt, including the failed ones (which never reach the PID-handling stage). After writing the script, `chmod +x` it and actually execute it against all four archives in order — a working solution requires the runtime side effects (running v4 process, dead v1 process), not just the script existing.
