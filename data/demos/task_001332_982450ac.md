Process everything in-memory/streaming rather than extracting to disk, using Python's `tarfile` and `zipfile` modules together: open the tar with `tarfile.open`, iterate its members, and for each `.zip` member use `extractfile()` to get a file-like object, then wrap it directly in `zipfile.ZipFile(BytesIO(member.read()))` (or pass the file object if seekable) to read the inner zip's members without writing intermediate files. For each inner file, read its raw bytes, check the first 8 bytes equal exactly `b'TECHDOC\x00'` before treating it as valid — files with a different or missing header must be skipped entirely, not just have the header stripped. For valid files, slice off the 8-byte header, `.decode('utf-8')` the remainder, do a plain string `.replace('[COMPANY_V1]', 'OmniCorp')`, and write the result into a new zip under a name built as `f"{zip_member_name}_{inner_filename}"`. Build this new zip at a temp path (e.g. `/home/user/clean_docs_temp.zip`), close it fully, then finalize with `os.replace` to `/home/user/clean_docs.zip` for atomicity — this is a specific verification point, so make sure the rename call is literally present in your script. Count valid documents processed and append the exact summary line to the log at the end.