First recover the cutoff frequency from the image yourself — run `tesseract /app/instrument_specs.png stdout` (or `tesseract /app/instrument_specs.png out && cat out.txt`), read the OCR'd notice text, and parse out the numeric Hz value it states as the maximum stable cutoff; don't guess or hardcode a number without checking the actual OCR output, and sanity-check the OCR result since tesseract can misread digits.

Then write `/home/user/filter.go`: parse the FASTA file's description lines with a regex like `Freq:\s*(\S+)\s*Hz` per `>` header, and for each match try to parse the captured value as a number — if the regex doesn't match, or the captured text isn't a valid number (e.g. `NaN`, missing entirely), treat that as malformed and reject the whole file (`os.Exit(1)`). If all headers parse cleanly and every frequency is `<= cutoff` (using the value you OCR'd, as a named constant or variable — not by re-deriving it per run), exit `0`; if any exceeds it, exit `1`. Compile with `go build -o /home/user/filter filter.go`, then self-test by looping the binary over every file in `/app/clean/` and `/app/evil/` and confirming exit codes match expectations before considering it done.
