Structure evaluate.py to fix the leakage by calling `train_test_split` on the raw (unscaled) features/target first, then `scaler.fit_transform` only on X_train and `scaler.transform` on X_test — never fit the scaler before splitting. Train Ridge with defaults on the scaled train data, predict on scaled test data, and compute MSE with `sklearn.metrics.mean_squared_error`. For the correlation, use `numpy.corrcoef` (or `scipy.stats.pearsonr`) between the scaled test set's A and B columns specifically — not the raw or training columns. For the bootstrap standard error, call `np.random.seed(42)` immediately before the loop, then in each of 1000 iterations draw `np.random.choice(n_test, size=n_test, replace=True)` indices, index into the *already-computed* test predictions and true y values, compute MSE per sample, and take `np.std` of the 1000 MSEs — reusing the model's fixed predictions rather than refitting each iteration. For inference timing, call `model.predict()` on single-row reshaped arrays in a tight loop over up to 1000 test rows, timing each call individually (e.g. `time.perf_counter()`) and averaging in microseconds. Dump all four metrics to results.json unrounded.
