Build the cgo bridge first and confirm it links before adding the websocket layer. Write a Go `cgo` file with `#cgo LDFLAGS: -L/home/user/legacy_c -lgraph` (or equivalent) and a `#include "graph.h"` cgo preamble declaring the `get_children` signature, then write a tiny standalone test call to confirm the shared library loads correctly at runtime with `LD_LIBRARY_PATH` set to `/home/user/legacy_c` before adding networking. Implement the traversal as BFS/DFS in Go: call `get_children(node, buf, 100)` for each frontier node, collect returned children into a visited set (including the start node), and stop when no new nodes are found — sort the final set ascending before serializing. Wrap this in a `gorilla/websocket` handler on `/ws` that reads `{"target": N}` and replies `{"reachable": [...]}`; run the server in the background with `LD_LIBRARY_PATH` exported. For the Python side, use the `websockets` library (installing it if missing) to connect, send the target-2 query, and write the reachable list as a comma-joined string to `/home/user/migration_test.log`.
