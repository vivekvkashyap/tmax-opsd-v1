Work top-down through the stack, testing each layer before wiring the next. Rust: rewrite src/lib.rs's function signature to be C-ABI safe — add `#[no_mangle]`, `pub extern "C"`, take `data: *const u8, len: usize`, dereference via `std::slice::from_raw_parts(data, len)` inside an `unsafe` block, sum bytes as u32/u16 and return `u16`; build with `cargo build --release` and confirm `target/release/librust_lib.so` exists. Proto: write service.proto with `syntax = "proto3"`, `package checksum`, a `PayloadRequest{bytes data = 1;}`, `ChecksumResponse{uint32 checksum = 1;}`, and a `ChecksumService` service with rpc `Calculate`. Generate Python stubs with `python -m grpc_tools.protoc -I. --python_out=. --grpc_python_out=. service.proto`. Server: in server.py, load the .so via `ctypes.CDLL`, set `.argtypes`/`.restype` explicitly to match the C signature (pointer+size in, u16/c_ushort out) to avoid ABI mismatches, implement the generated servicer's Calculate to pass request.data through ctypes and return the checksum, and serve on localhost:50051 in the background. Client: write run_client.sh as a thin wrapper (heredoc'd inline python using the grpc stub, or grpcurl) that reads the given file's bytes, calls Calculate, and prints only the numeric checksum. Validate end-to-end with a known small test file and hand-compute the expected sum-mod-65536 to check your own pipeline before finishing.