Start by reading /home/user/app/logs/crash.log and /home/user/app/build.sh to see how the compiler is invoked — note it likely passes both `-I.../deps/v1` and `-I.../deps/v2` include paths, and the compiler picks whichever include directory is listed first when resolving `<calc_utils.h>`. Diff the two header files under deps/v1 and deps/v2 to see which one defines the divergent UPDATE_RATE. Write /home/user/app/mre.c exactly as specified (10 iterations of `x = x - UPDATE_RATE * x;` starting from x=10.0, printf with %.2f) and compile/run it first with the current (broken) build.sh include order to confirm it diverges — this proves your MRE reproduces the bug before you fix anything. Then edit build.sh so the correct v2 include path is searched before (or instead of) v1 — reorder the -I flags or drop the v1 one — rebuild, rerun ./sim, and confirm it now converges. Finally write solution.txt with the exact printed line from the fixed sim on line 1 and the name of the directory that had the faulty macro (the one you are removing/deprioritizing) on line 2.