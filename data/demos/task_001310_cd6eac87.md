Don't trust the `edges` table as-is — the task states it's a corrupted cross join, so the real filtering rule lives only in the image. Run OCR on `/app/schema_rule.png` (e.g. `tesseract`) and read the business rule it states about which node attribute must match between a researcher and an institution for an edge to be valid; use that rule literally, not an assumption. Then build the Rust service (`cargo new /home/user/graph_api`, add `rusqlite` and a web framework like `axum`): open `/app/dataset.db`, join `edges` to `nodes` twice (once for source, once for target) filtering `label='researcher'`/`label='institution'` and applying the OCR'd equality condition, group by researcher id, count valid edges, and sort by count descending then id ascending, taking the top 3. Expose `GET /top-affiliations` on `127.0.0.1:8080` returning a JSON array of `[id, count]` pairs matching the exact nested-array shape described in the task. Start the server as a background process so it stays up for testing, and sanity-check your own SQL against a couple of known-region rows before finalizing.