Start by transcribing `/app/voicemail_99.wav` (e.g. `whisper` or another available STT tool, or an `ffmpeg` pipeline into a speech-to-text model) and read the transcript carefully to pull out the three named spam keywords verbatim — don't guess or hardcode plausible-sounding words. Then build `/home/user/cleanup.sh` as a function-based pipeline: for each `*.wav` in the target dir, first validate with `ffprobe -v error -show_format -show_streams "$file"` (or check its exit code) and `rm` the file immediately if it fails or isn't real audio; otherwise transcribe it to a temp file (create with `mktemp`, and register cleanup via `trap 'rm -f "$tmpfile"' EXIT` so it's removed even on error/interrupt), then grep the transcript case-insensitively for the recovered keywords (`grep -qiE "kw1|kw2|kw3"`) and delete the file if any match. For files that survive both checks, compute `sha256sum "$file"` and append `"<hash>  <path>"` to the manifest — but wrap that append in `flock` (e.g. `( flock -x 200; echo "..." >> "$manifest" ) 200>"$manifest.lock"`) since the script will be invoked concurrently on different subdirectories and naive `>>` can interleave writes across processes. Test by running two invocations in parallel against separate dirs writing to the same manifest and confirm no corruption.