Two independent fixes are required. First, locate the unicode-escape regex inside `/app/vendored/JSON.sh-0.4.1/JSON.sh` (search for the tokenizer's escape-sequence pattern, likely something like `\\\\u[0-9]{4}`) and restore the full hex character class `[0-9a-fA-F]{4}` so valid escapes like `ꯍ` parse again — verify by feeding `{"test":"ꯍ"}` through `JSON.sh -b` and confirming it emits the expected token line rather than failing. Second, write `/home/user/detect_anomalies.sh`: for a given file, treat it as invalid/evil if any `\u` escape is followed by fewer than 4 hex digits or by a non-hex character (grep/regex over the raw text is enough, e.g. flag any `\u` NOT followed by exactly `[0-9a-fA-F]{4}`), OR if the sequence of `timestamp` field values extracted line-by-line (grep/sed/awk or your fixed JSON.sh) ever decreases compared to the previous line when compared as ISO8601 strings (safe to compare lexicographically since the format is fixed-width `YYYY-MM-DDTHH:MM:SSZ`). Exit 1 on either violation, 0 if all checks pass; chmod +x the script. Test against both provided corpora directories before considering it done.
