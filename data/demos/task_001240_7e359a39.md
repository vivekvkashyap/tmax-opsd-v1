Your Rust binary will be diffed line-by-line against a hidden reference on thousands of fuzzed WAL lines, so correctness of the path-normalization algorithm matters more than any particular library choice — implement normalization yourself (split on `/`, walk components left to right, push non-`.`/non-`..` segments onto a stack, pop on `..`, and treat an attempt to pop past the root, or an absolute path escaping `/config_root/`, as a REJECT) rather than trusting a filesystem-aware canonicalize call (which can behave oddly on non-existent paths or symlinks and diverge from a pure lexical oracle). Parse each stdin line by splitting on the first two spaces only (SEQ_ID, OPERATION, then the rest is RAW_PATH, since paths can't contain spaces per the spec but be defensive about malformed lines and just skip them). First extract the "Mandatory Asset Prefix" from `/app/scan_policy.png` with OCR (e.g. `tesseract`), verifying the extracted text is clean before hardcoding it as a constant in your program. Build with `cargo build --release` so the binary lands at `target/release/sanitizer`, then stress-test it yourself against edge cases (`../`, absolute paths, trailing dots, empty segments) before relying on it.
