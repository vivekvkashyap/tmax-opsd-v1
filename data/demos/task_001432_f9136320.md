Start with the image: run `tesseract /app/watermark_config.png stdout` to recover the master offset text and parse out the numeric value the config uses (don't hardcode a guessed number — read it). Then debug the two components separately. For the C extension in `/home/user/service/`, search for off-by-one loop bounds like `while (i <= len)` or `for (i = 0; i <= n; i++)` over a buffer of size `len`/`n` — these read/write one element past the end, which can both crash (invalid input) and infinite-loop (if the OOB read corrupts the loop's own bound variable); fix the comparison to `<`. For the Python side, look for the null-byte handling — e.g. code using `str.split('\0')` or C-string-style parsing that mishandles embedded nulls or fails to bound-check a length field taken from untrusted input; make it length-prefixed/robust rather than relying on sentinel bytes. After fixing both, wire them together in `/home/user/run_extractor.sh` so it applies the OCR'd offset consistently and prints telemetry to stdout for a given input file argument. Since grading is by fuzz equivalence against a hidden reference across ~1000 random/edge-case inputs (including 0x00, 0xFF, 0x7F-heavy and short/empty files), test your own wrapper against many synthetic edge-case files locally — especially zero-length and max-length inputs — before considering it correct.