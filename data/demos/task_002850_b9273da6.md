Work through this in strict dependency order since each step relies on the previous: create the four directories first, then write `monitor_mock.cpp` as a trivial program that just `printf`s the five fixed CSV lines exactly as given and returns 0, compile with `g++ src/monitor_mock.cpp -o bin/monitor`, then create the symlink with `ln -s /home/user/capacity_planner/bin/monitor /home/user/capacity_planner/active/current_monitor` (use the absolute target path, not a relative one, since the verifier checks `readlink` against the absolute path). For health_check.sh, pipe the symlink's output through `awk -F',' '$2>=80 || $3>=80 {print $1}'` (treating fields as numeric, not string, comparisons) to extract only the service name column for rows where CPU or Memory crosses the 80 threshold, redirecting into logs/critical_alerts.log — a common mistake is using string comparison in awk/grep which mis-sorts two-digit vs. numeric thresholds, or filtering on AND instead of OR. Make the script executable with `chmod +x`, then actually run it once so the log file exists, and manually verify its contents match the three services you'd expect from the data (backend_api, database_main, ml_worker) before finishing.