Work this incident end-to-end rather than fixing pieces in isolation, since the recovery-rate metric depends on the whole pipeline running. First, recover the lost API token: `cd /app/api && git log -p` (or `git log -S <keyword> -p`) through the repo history to find where the token was hardcoded before removal — search for terms like "TOKEN" or "SECRET" across commits. Once found, export it as `API_SECRET_TOKEN` when starting the Flask API, and patch `/app/inject.py` to send it as an `Authorization: Bearer <token>` header. Second, start services with `/app/start_services.sh`, then tail `/app/worker/logs/worker.log` to see the worker's crash traceback — identify the specific exception type and the line/function in `/app/worker/processor.py` that raises it on malformed payloads. Third, wrap the offending parsing logic in `processor.py` with a targeted `try/except` that catches that specific exception, logs/skips the bad record, and `continue`s the loop instead of letting the worker process die (a bare/broad except that also swallows valid-payload errors, or one that doesn't `continue` the loop, will undercount). Restart the worker after each code change, then run `/app/inject.py` to push all ~10,000 payloads through, and check `/app/data/processed.db` row count against the valid-payload count to confirm you're at or above 98% recovery.