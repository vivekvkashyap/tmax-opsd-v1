Tackle the Rust compile errors first in isolation: run `cargo build` in /app/rust-proxy, read each borrow-checker error, and where a function returns a reference borrowed from a locally-parsed/owned value, either change the return type to an owned `String` or thread through the actual input buffer's lifetime `'a` on the struct/function signature — don't paper over it with `.clone()` everywhere if it changes semantics elsewhere. Next, open /app/reference/checksum.py and read its exact algorithm (field selection, XOR/salt operations, bit widths) carefully, then hand-port that same arithmetic into Rust using matching integer types (watch for u8/u16/u32 wraparound differences between Python and Rust) and wire it into the frame validation path so malformed or checksum-failing frames are dropped, not forwarded. Bring up Redis, then the Flask backend (point it at Redis with correct host/port), then `cargo build` and run the proxy pointing at the backend's real port. Validate end-to-end manually with a couple of sample payloads from /app/corpora/clean and /app/corpora/evil via a WebSocket client before trusting the automated corpus run. Only once all three services are confirmed live and correct, write "READY" to /home/user/environment_ready.log.