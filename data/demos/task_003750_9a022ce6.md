Read `/home/user/telemetry.bin` with `fread` into a fixed-layout struct (or six `int32_t` reads) per record, confirming with `sizeof` and a byte-count check against file size before trusting the parse. Maintain four independent sliding-window buffers (capacity 4), one per language (FR, DE, ES, JA), each tracking the last up-to-4 widths for that language. For every record: compute the window's integer average (sum divided by count present, truncating toward zero; `0` if empty), then check anomaly conditions in order FR, DE, ES, JA using integer arithmetic for the 1.5x comparison (`width > (avg * 3) / 2`, no floats) combined with `width > 100`. Only after checking, push the current width into that language's window (evicting oldest if full) — order matters, since a value must never appear in its own comparison average. Always write the CSV header, appending one row per anomaly as you scan.
