Structure the Rust program as: connect, DDL (drop old index, create new one), one recursive query, then serialize — verify each piece against sqlite3 CLI first if unsure. Use `rusqlite` and execute `DROP INDEX IF EXISTS idx_employee_assets;` then `CREATE INDEX idx_assets_emp_val ON assets(employee_id, value DESC);` as separate statements. For the hierarchy+aggregation, write a recursive CTE that starts from `id = 1`, recursively joins `employees e ON e.manager_id = chain.id` to walk the whole subtree, then LEFT JOIN `assets` (so employees with zero assets still appear) and `GROUP BY employee` computing `SUM(value)` and `COUNT(asset id)` — use `COALESCE(SUM(value),0)` and `COUNT(assets.id)` (not `COUNT(*)`) so a no-asset employee gets 0/0 rather than 1/NULL. Sort results by total_asset_value DESC, employee_id ASC as the tiebreak, either in SQL `ORDER BY` or after mapping into Rust structs. Serialize with `serde` to the exact field names/order shown in the spec and write to `/home/user/audit_report.json`. Confirm afterward with `sqlite3 compliance.db ".indexes assets"` that the old index is gone and the new one exists.
