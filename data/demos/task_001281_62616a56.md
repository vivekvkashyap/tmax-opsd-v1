Treat this as three debugging phases done in sequence. Phase 1: read `/app/config.ini`, compare the Redis port and queue name against what `api.py`/`worker.py` actually expect (grep those files for the config keys they read), fix the mismatches, then start Redis and both services via `/app/start_services.sh` and confirm end-to-end with a curl POST to `/ingest` followed by checking `/app/processed.log`. Phase 2: in `/home/user/log_parser_repo`, run `git bisect start` with a known-good early commit and the bad HEAD, using `git bisect run` with a script that builds/imports `parser.py` and tries parsing a legacy escaped-string sample to pinpoint the breaking commit; fix `parser.py` to import the right modules and handle both plain JSON and legacy double-escaped strings (unescape before `json.loads`, or catch decode errors and retry), then reinstall the package so `worker.py` picks it up. Phase 3: write `/home/user/detector.py` with CLI `detector.py <input_dir> <output.csv>`, scanning each `.log` file for injection signatures (JNDI patterns like `${jndi:`, `<script>` tags, SQL keywords like `UNION SELECT`, path traversal like `../../`) and writing `filename,ACCEPT` or `filename,REJECT` per file — validate against your own clean/evil samples first.