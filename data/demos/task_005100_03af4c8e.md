Compile `calc_engine.cpp` with `-g -O0` first and reproduce the crash under `gdb --args ./calc_engine` (or run then `bt` after the SIGSEGV) to get the exact line and variable values at fault, rather than guessing from the source alone. You should find a pattern like: compute `metric = 1.0/(x-y)`, then cast `metric` to an `int` and use it directly as an array index without bounds checking — when `x == y`, division by zero produces `inf`/`nan`, and casting that to `int` yields an out-of-range (often huge or UB) index, causing the out-of-bounds write/segfault. Fix by special-casing the zero-difference condition before the division: if `x - y == 0.0` (or `fabs(x-y) < epsilon` if the task implies near-zero too), set `metric = 0.0` and `index = 0` directly, skipping the division and cast entirely. Recompile to the exact path `/home/user/calc_engine`, then run it against `/home/user/input_batch.dat`, redirecting/writing to `/home/user/output.txt` as the program already does internally. Double check the output file has one line per input row, including a `0` line for the identical x/y pair.
