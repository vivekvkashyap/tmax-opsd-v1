Decompose this into independent, checkable pipeline stages before training anything. First, transcribe `/app/legacy_alerts.wav` (e.g. install `openai-whisper` or another STT tool) and parse the resulting text for patterns like "server <spoken digits>, severity <spoken digit>" — convert spoken number words to digits/integers yourself (a small word-to-number mapping or a library) to build a `{server_id: severity_score}` dict; listen to/inspect the transcript rather than assuming a fixed set of servers, since the exact IDs and scores must be recovered from the audio, not guessed. Second, load and concatenate all `server_metrics_part*.csv` files with pandas, group by `server_id`, and compute mean `cpu_usage`, `memory_usage`, `disk_io`. Third, left-join the aggregated CSV features with the severity dict, filling missing severities with 0 — get the join key type consistent (int vs str `server_id`) since this is a common silent bug. Fourth, train a regression model (e.g. `sklearn.linear_model.LinearRegression` or a gradient-boosted model) on the four features to predict `network_latency`, and validate against a held-out split before trusting the MSE < 0.25 threshold. Persist the model with `joblib.dump`, and make `predict.py` reload the same features (recomputing severity/aggregation logic must not be needed at inference time if the hidden test CSV already includes `severity_score`, per the task's own note) and print one prediction per line matching row order.
