Implement this as an explicit per-connection state machine, not a stateless request handler — each accepted socket needs its own state variable (INIT/READY) and its own rolling timestamp list for rate limiting, since the spec is per-connection. Use `socketserver` (threaded) or a manual `socket.accept()` loop with `threading.Thread` per connection so multiple clients can connect, and read line-by-line by buffering received bytes until you see `\n` (a single `recv()` call may return partial or multiple commands). For `EXEC`, validate the command string against `^[A-Za-z0-9]+$` with `re.fullmatch` before checking rate limits — order matters, since the spec implies validation applies regardless of state check ordering with rate limiting. For rate limiting, keep a per-connection list of the EXEC timestamps and only count the request as consumed if it's allowed; the rule is "3rd EXEC within 1.0s of the 1st" — track it as a true rolling window (compare current time to timestamps within the last 1.0s), not a fixed-bucket counter, to match the test's precise timing (2 allowed, 3rd rejected, then allowed again after sleeping past the window). Start the server in the background (`&`) before running `test_client.py`, and iterate based on the log output if assertions fail.
