First run `tesseract /app/auditor_instructions.png` (or convert/preprocess the image if OCR is noisy) to recover the redaction username and auth token from the image text — do not guess these values, read them from the OCR output and verify the two lines match the documented `REDACT_USER:` / `AUTH_TOKEN:` formats. Generate a self-signed TLS cert/key pair for localhost with `openssl req -x509 -newkey rsa:2048 -nodes -keyout key.pem -out cert.pem -days 365 -subj "/CN=localhost"` and place them under `/home/user/certs/`. Build the HTTPS server (e.g. `http.server` wrapped with `ssl.wrap_socket`/`SSLContext.load_cert_chain`, or a small `BaseHTTPRequestHandler`) bound exactly to 127.0.0.1:8443, handling GET /audit-report: check the `Authorization: Bearer <token>` header against the token you extracted, return 401 if missing/wrong, otherwise read the raw log file, replace every occurrence of the recovered username with the literal string `[REDACTED]`, and return it as text/plain with the exact CSP header string specified. Keep the server running in the background (nohup/subprocess with Popen, not foreground blocking) so the verifier can connect afterward.