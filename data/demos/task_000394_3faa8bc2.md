Work top-down through the dependency chain since each layer blocks the next. First fix the vendored Makefile: grep for the compiler variable assignment (likely something like `CXX = ...` hardcoded to a wrong/missing path) and correct it to the real g++ path, then `make` to produce `transform_backend`. Next fix the C++ RLE logic: trace through the encoding loop for a run of identical characters that spans an internal buffer/counter boundary (e.g. a counter or output-chunking limit around 10 chars) — the bug is likely a premature reset or flush of the run-length counter; fix it so it counts a run based purely on character equality, not on position, and remove or guard the locale-based abort so it degrades gracefully instead of crashing, while still working correctly once `LANG=en_US.UTF-8` and `TZ=UTC` are exported. Since a fuzz oracle will diff your binary's stdout against a reference across many random strings, test locally with edge cases (runs >10, runs at exact multiples of 10, single chars, empty input) via `stdin`. Finally wire up the systemd user service reading `PORT=9000`, start/enable it, and confirm `curl http://127.0.0.1:8080/api -d "test"` returns 200 through Nginx instead of 502.