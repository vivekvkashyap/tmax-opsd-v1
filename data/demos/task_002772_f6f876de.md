Start by reverse-engineering the schema instead of guessing: open the SQLite file with `sqlite3 /home/user/research_data.db ".tables"` and `.schema <table>` for each table to see the actual (obfuscated) column and table names before writing any query. Identify which table holds papers (title/year), which holds authorship (paper-to-author rows), and which holds directed citation edges (from/to). Then in your Python script, load the edge list into an adjacency structure and run a standard BFS/shortest-path from the node matching "Quantum Origins" to the node matching "Macroscopic Superposition", tracking the predecessor chain so you can reconstruct the path in forward order once the target is reached — do not assume the path length or hop sequence, compute it from the actual edges in the DB. For each paper on the reconstructed path, issue a parameterized query (`?` placeholders, never string-formatted SQL) to fetch title, year, and all associated authors, sorting the authors list alphabetically before building each JSON object. Assemble the array in path order (start to end) and write it to the exact output path with `json.dump`, matching the required key names and types precisely (year as int, authors as a list of strings).
