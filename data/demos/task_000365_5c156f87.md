Set up the Go module first (`go mod init ml_pipeline` inside `/home/user/ml_pipeline`, then `go get gonum.org/v1/gonum/...` for the SVD types) before writing any logic, so imports resolve on the first build. Read the CSV into a 10x100 `float64` slice-of-slices or a `gonum/mat.Dense`. For the background curve, do not solve the ODE analytically — implement the literal Forward Euler loop: `y[0]=50.0`, then for each step `y[j+1] = y[j] + dt*(-0.1*y[j])` for dt=0.1, 99 steps, matching exactly what a naive-but-correct Euler integrator produces (not the closed-form exponential decay, which will disagree). Subtract this same 100-length vector from every row of the raw matrix elementwise to build the corrected matrix M. Wrap M in a `mat.Dense`, run `mat.SVD` (economy mode is fine), and pull the first entry of the singular values vector — SVD conventions already sort descending. Format the single float to exactly 4 decimal places (`fmt.Sprintf("%.4f", v)`) and write it, with no trailing newline issues, to `/home/user/svd_result.txt`. Build with `go build` and execute the binary to actually generate the file — don't just leave source code unbuilt.
