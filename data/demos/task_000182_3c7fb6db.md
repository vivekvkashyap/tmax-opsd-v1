Split this into a correctness pass and a performance pass, since the grader checks both. First get the ETL logically right: in etl.py, connect to Postgres and use a window function (e.g. `LEAD()` partitioned by user_id, ordered by event_timestamp) to find each event's immediate successor for the same user, filtering to pairs within 60 minutes; write these as `(:Event)-[:NEXT]->(:Event)` nodes/edges into Memgraph via the neo4j Bolt driver, using `MERGE` to avoid duplicate nodes when the same event_id/event_type recurs across edges. Then write target_query.cypher to find the most frequent 3-hop event_type sequence (chain two NEXT hops, group by the three event_type values, count, order by count descending, limit 1, aliasing the count as `frequency`) and aliasing the three columns as step1/step2/step3. Only after correctness works, address the <15ms performance requirement: without an index on `:Event(event_type)` (and likely one on the identifying property used in MERGE/lookups), the 3-hop pattern match will scan all ~50k nodes and blow the threshold — create the appropriate Memgraph index/constraint via `CREATE INDEX ON :Event(event_type);` (and similarly for event_id) before benchmarking.