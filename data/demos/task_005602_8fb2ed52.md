First inspect the schema with `sqlite3 /home/user/legacy_etl.db ".schema"` to learn the actual table/column names for jobs and their dependency edges — don't assume names, confirm them. Write `/home/user/build_indexes.sh` to `CREATE INDEX` on the foreign-key-like columns in the dependencies table (both the "upstream" and "downstream" id columns, since recursive lookups traverse from a job backward to its prerequisites) and run it. For `/home/user/get_upstream.sh`, first resolve the input `job_name` to its internal ID via a lookup query, then use a `WITH RECURSIVE` CTE that starts from that ID and repeatedly joins the dependency table to walk toward upstream (prerequisite) jobs, being careful about which direction "upstream" maps to in the edge table (test with a job you can reason about by hand). Exclude the target job itself from the recursive result set, then output alphabetically-sorted, deduplicated job names — pipe the sqlite3 query result through `jq -R -s -c 'split("\n")|map(select(length>0))|sort'` (or equivalent) to get strict JSON array-of-strings formatting. Run the script for `"Load_Fact_Sales"` and redirect stdout to `/home/user/sales_lineage.json`, then double check the JSON parses with `jq .` and the job set matches your own manual trace of the dependency chain.