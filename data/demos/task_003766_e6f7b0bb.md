Work through this in the stated order since later steps depend on earlier ones being live. First run `/app/services/start_all.sh` and confirm Redis/Flask/Nginx are actually listening (`curl`/`redis-cli ping`) before touching config. Edit `/app/services/nginx.conf` to add a `location /api/ { proxy_pass http://127.0.0.1:5000/; }` block (mind the trailing slash so the `/api/` prefix is stripped correctly), then reload nginx (`nginx -s reload` or restart) and verify with `curl http://127.0.0.1:8080/api/...`. For the Go processor: read `/home/user/process_docs.go` to understand its current sequential tar-extraction loop, then (a) add a path-safety check — resolve each tar entry's target path with `filepath.Join` + `filepath.Clean` and skip any entry whose resolved path escapes `/tmp/docs_out/` (compare via `strings.HasPrefix` on the cleaned absolute path, not just string-matching "../"); (b) parallelize extraction using goroutines + a `sync.WaitGroup` (and a bounded worker pool/semaphore if needed) instead of writing one goroutine per file naively (watch out for shared state races); (c) special-case the `history.wal` entry — parse its fixed 9-byte binary records with `encoding/binary.LittleEndian`, and for status byte `1`, shell out to `redis-cli SET doc:<id> <timestamp>` or use a Redis client. Compile to `/home/user/process_docs` and benchmark against `/app/bin/process_docs_ref` on the large tarball to confirm real speedup before considering it done — profile where time is spent if the 3x target isn't hit.