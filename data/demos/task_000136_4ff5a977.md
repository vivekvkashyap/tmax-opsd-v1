Work through the five requirements as separable diagnostics rather than one blob. Set `export DEPLOY_ENVIRONMENT=production` in `/home/user/.bash_profile`. For the full disk, first inspect what's actually mounted/filling `/home/user/deploy_data` (`df -h`, `du -sh` on subdirs) before deleting — only remove files under `logs/` matching the stated criteria (older than 30 days via `find ... -mtime +30`, or the `old_log_*.txt` naming pattern), don't blanket-wipe the directory. For the audio config, transcribe `/app/config.wav` with a speech-to-text tool (`whisper`, or `ffmpeg` + a Python ASR lib) rather than guessing — the spoken text gives you both the port number and the bearer token as literal words/digits; convert what you hear into the exact numeric port and token string the service should use, and don't hardcode a placeholder. For `server.py`, read it carefully to find why the rolling deployment logic doesn't converge to `v2` (likely a state/flag that's never flipped, or a version check that's inverted) and fix only that logic so `GET /version` ends up returning `{"version": "v2"}`. Add bearer-token auth middleware/handler logic checking the `Authorization: Bearer <token>` header against the transcribed token, returning 401 when absent/wrong. Finally launch via `start_daemon.sh` in the background and curl it yourself on the transcribed port to confirm both the 200/JSON and 401 paths work before finishing.