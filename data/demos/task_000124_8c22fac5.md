Start by reading `/home/user/app.py` carefully to find how it verifies JWT signatures — look specifically at how it branches on the `alg` field in the JWT header, since custom/home-rolled JWT verifiers commonly trust the client-supplied algorithm and special-case an `"none"` (no-signature) algorithm without re-checking that the server actually requires a signature. Confirm the exact bypass path by reading the verification function line by line rather than guessing. Once identified, install Flask and start `app.py` in the background (`python3 app.py &`), confirming it's listening on port 5000. Write `/home/user/exploit.py` to hand-construct a JWT: base64url-encode (no padding) a header claiming the vulnerable algorithm, base64url-encode a payload with whatever role claim the code checks for admin access, join them with `.` and an empty (or per-app-specific) signature segment, then send it as a Bearer token via `requests` to the flagged endpoint. Parse the JSON response for the flag field and write only the raw flag string (strip any trailing newline/whitespace) to `/home/user/flag.txt`. Verify by curling the endpoint manually with your forged token before finalizing the script.
