Start by transcribing `/app/auth_token.wav` (e.g. `whisper --model base /app/auth_token.wav` or another available STT tool) and normalize the result to lowercase with punctuation stripped — this becomes your bearer token, used only as `Authorization: Bearer <token>`, never hardcode a guessed phrase. For the server itself, don't hand-roll a fragile read-loop with `nc`; use `socat TCP-LISTEN:8080,reuseaddr,fork EXEC:./handler.sh` (or a small C/Python server) so each connection is handled in its own forked process for concurrency, and have the handler read the request line + headers safely (respecting Content-Length, not assuming a fixed header count). To avoid command injection, never interpolate the `repo`/`branch` JSON values directly into a shell command that's eval'd or passed to `system()`; parse the JSON with `jq -r` and only ever use the extracted strings inside quoted variable expansions (`"$repo"`, `"$branch"`) when building the echoed response — never pass them to `eval`, backticks, or unquoted command substitution. Validate the Authorization header before touching the body, returning a proper `HTTP/1.1 401 Unauthorized` status line with headers for missing/incorrect tokens, and `200 OK` with the exact response body format otherwise. Launch the server backgrounded (`&` plus `disown` or nohup) so it keeps listening after your turn ends.