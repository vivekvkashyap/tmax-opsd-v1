Treat this as a closed-loop calibration problem: build the ground-truth side once, then search over the simulator side to match it. Use `opencv`/`ffmpeg` to extract frames from `/app/thermal_diffusion.mp4`, convert to grayscale, normalize to [0,1], flatten each 64x64 frame to a 4096-vector, stack into matrix X, and run `np.linalg.svd` to get the first left singular vector as your `reference_mode` (normalize its L2 norm to 1 — watch for sign ambiguity in SVD, since U's sign is arbitrary, so compare using absolute dot product rather than raw dot product). For the C++ side, first change `#define N 16` to `#define N 64` in `heat_solver.cpp` and confirm it compiles and runs standalone with `make`, producing `sim_output.csv` with the right shape (T rows x 4096 cols) before wiring up any search. Then do a 1D grid or bisection-style search over D in [0.01, 0.20] at 0.01 resolution: for each candidate D, run the solver, load `sim_output.csv`, run the same SVD pipeline to extract its own primary mode, and score it by absolute dot product against `reference_mode`. Keep the D and mode achieving the best score, save that mode (L2-normalized, reshaped to 64x64) to `best_sim_mode.npy` and the D value to `best_D.txt`. Validate your own similarity score is high before finishing, since the grader recomputes the reference independently.