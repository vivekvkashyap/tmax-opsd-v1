Implement the C program as three sequential, independently-testable transforms over the row stream, then load via sqlite3. Parse the CSV with a simple line/field splitter (no header to skip). For masking: if `strlen(user_id) <= 4` leave unchanged, else replace every character except the last 4 with `*` (build a new string of `len-4` asterisks followed by the last 4 original characters). For gap-filling: track a running "last valid sensor_value" as you scan rows top to bottom; when a row's value field is blank, substitute the last valid value (the spec guarantees row 1 always has a value, so no special-casing needed there). For the rolling SMA: maintain the filled values in order and, for each row i, average the filled values from max(0,i-2) to i inclusive (i.e., current plus up to 2 previous) — this must operate on the gap-filled series, not the raw one. Format both filled_value and sma with exactly 2 decimals (`%.2f`) when writing `timestamp,anon_id,filled_value,sma` (no header) to the output CSV. Once that file is correct, create the SQLite DB and table with the exact column types given, then bulk-load with `sqlite3 telemetry.db -cmd ".mode csv" ".import /home/user/processed_telemetry.csv cleaned_data"` — verify with `.mode csv` import doesn't misinterpret the anonymized asterisks as anything special, and spot-check a `SELECT` against the expected values before finishing.
