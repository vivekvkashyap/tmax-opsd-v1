Build the baseline once, deterministically: open `/app/experiment_baseline.wav` with the `wave` module, read all frames, unpack as 16-bit signed little-endian samples (`struct.unpack(f"<{n}h", frames)`), filter to values `> 0`, and keep the first 1000 — cache/compute this the same way every run since the script must be bit-exact. In `metric_analyzer.py`, read all of stdin, parse it as CSV with a `csv.DictReader` keyed on `experiment_name,metric_value`, and silently skip rows where `metric_value` fails `float()` or is `<= 0`. If fewer than 2 valid rows survive, emit exactly the specified fallback JSON and stop. Otherwise run a two-sided Welch's t-test (`scipy.stats.ttest_ind(..., equal_var=False)`) between the valid metric values and the cached baseline distribution, round the p-value to exactly 4 decimals, and separately count occurrences of each vowel (a,e,i,o,u) case-insensitively across the concatenated valid experiment names. Print only the final JSON object (no debug output) to stdout, and make the file executable with the required shebang — since this is checked by fuzzing many random CSV inputs against a hidden reference, matching the exact filtering order and rounding behavior described is what makes outputs bit-exact.
