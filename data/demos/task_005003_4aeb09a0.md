Since you have no root, download the `jq-linux64` binary directly from the jq GitHub releases (v1.6 tag) with `curl -L`, save it to `/home/user/bin/jq`, and `chmod +x` it — then reference it by full path (or prepend `/home/user/bin` to `$PATH`) in your scripts rather than assuming a bare `jq` call resolves. For `process_metrics.sh`: skip the CSV header, then per data row check with `awk`/shell arithmetic whether `loss` is empty (impute to `0.500` if so) and whether `accuracy` is strictly outside `[0.0, 1.0]` (drop the row if so) — do imputation and filtering as two distinct passes so you don't accidentally drop rows that only need imputing. Build the JSON array incrementally, e.g. emit one JSON object per line with `jq -n --arg run "$run" --argjson loss "$loss" --argjson acc "$acc" '{run:$run, loss:$loss, acc:$acc}'`, then wrap all lines into an array with `jq -s .`. For `test_pipeline.sh`, use your local `jq` to `jq 'length' cleaned_metrics.json` and print the count in the exact required sentence format, redirecting output to `test_results.log`. Run both scripts end-to-end and inspect the actual output files afterward.
