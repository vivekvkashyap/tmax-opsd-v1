This is a precision-loss bug caused by pandas silently upcasting an integer column to float64 whenever a left join introduces NaNs, so the fix and the detection method both hinge on avoiding that upcast. Steps: (1) run `/home/user/buggy_join.py` as-is to produce `/home/user/bad_output.csv` (do not touch it, it's the "before" baseline); (2) write `/home/user/fixed_join.py` that reads `tokens.csv` with `token_id` forced to pandas' nullable `Int64` dtype (or read it as a Python/string type and only convert per-value) so the merge never routes the column through float64, then perform the identical left join (docs left join tokens on doc_id) and write `/home/user/fixed_output.csv` with `index=False`; (3) compare the two output CSVs per row: for each doc_id, treat the fixed value as ground truth and check whether the bad-output's float representation, when converted back to an integer, differs from the fixed integer value — skip rows that are legitimately missing/NaN in both; (4) write only the doc_ids where the values differ, one per line, to `/home/user/precision_loss_docs.txt`. Reason about which large token_ids are near the float64 53-bit mantissa boundary to sanity-check which rows should flag as corrupted before finalizing.