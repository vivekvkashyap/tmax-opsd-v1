Start by extracting the hidden policy rules from the scanned image with OCR: run `tesseract /app/policy_scanned.png stdout` (try preprocessing with e.g. `convert`/thresholding first if the raw OCR output is noisy) and read the recovered text carefully to identify the specific forbidden username(s), group(s), and directory path(s) it names — these become extra literal patterns your filter must block, in addition to the standard privilege-escalation checks. Then write `/home/user/audit_filter.py` to read the single command from the given file and check it against two categories of rules: (1) general escalation patterns like `sudo`, `su -`/`su root`, `chmod +s` / `chmod 4755`, `chown` to root, reading `/etc/shadow` or `/etc/passwd`, `setuid`/`find -perm -4000`, shell-spawning one-liners (e.g. `awk ... system(...)`); and (2) the specific keywords/paths you recovered via OCR. Print exactly `SAFE` or `VIOLATION` with nothing else. Before finalizing, run your script against every file in both `/home/user/corpora/clean/` and `/home/user/corpora/evil/` and iterate on your regex/substring matching until every clean command is accepted and every evil command is rejected — pay special attention to clean commands that superficially resemble blocked patterns (e.g. legitimate `cat`/`grep` usage) to avoid over-blocking.