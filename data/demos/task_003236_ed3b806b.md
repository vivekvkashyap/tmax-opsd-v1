This has two independent deliverables — build and test each against its own corpus/spec before moving on. For `/home/user/detector.py`: recursively walk the parsed JSON (dicts, lists, and nested combinations at any depth) collecting every string value encountered, and flag the config as evil (exit 1) if any string contains the literal substring `LD_PRELOAD`, or starts with exactly `/tmp/` or `/var/tmp/` — check prefixes carefully (a path merely containing `/tmp/` mid-string should not count, only literal starts-with per the spec) — otherwise exit 0; test it directly against sample clean/evil JSON files you construct locally that mirror the described patterns. For `/home/user/etl.py`: OCR `/app/system_params.png` (e.g. `tesseract`) to recover the `SALT` string and `INTERVAL` bucket size (a pandas-style offset like `30T`) — read the digits/characters carefully. Then load all input JSONs, group by `server_id`, sort by timestamp, compute `sha256(compact-sorted-keys-JSON(payload) + SALT)` per record, drop a record if its hash equals the immediately preceding kept record's hash for that server, build INTERVAL-sized buckets across the full day, and for each (bucket, server) emit the most recent hash carried forward or `EMPTY` if none seen yet, writing sorted CSV rows to `/home/user/timeline.csv`.