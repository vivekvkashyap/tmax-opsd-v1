Fix the dependency first: open `/app/vendored/pyjwt-custom/jwt/api_jwt.py`, find the misspelled method name around line 142 and correct it back to the proper PyJWT method name so the internal call sites (which reference the correct spelling) resolve; then `pip install -e /app/vendored/pyjwt-custom` and confirm with a quick `python3 -c "import jwt; ..."` round-trip that decode/verify works before writing any classifier logic. For `/home/user/payload_filter.py`, implement the two checks as separable functions and test each against the given corpora directly: (1) base64-decode `tls_cert` to PEM, load it with the `cryptography` library, and verify its issuer/signature chains to `/app/rogue_ca.pem` (load the CA cert, use its public key to verify the client cert's signature — reject on any exception or mismatched issuer); (2) for `auth_token`, iterate every line of `/app/wordlist.txt` as a candidate HMAC secret, attempt `jwt.decode(token, candidate, algorithms=["HS256"])` catching signature/verification errors, and treat the token as valid only if some candidate succeeds AND the decoded payload's `role` claim equals `operator`. Copy a file unmodified to the output directory only when both checks pass; otherwise skip it. Run your script against `/app/corpus/clean` and `/app/corpus/evil` yourself and verify all clean files appear in output and no evil files do before finishing.