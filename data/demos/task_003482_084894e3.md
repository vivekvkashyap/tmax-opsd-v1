Write extractor.c to read stdin line by line (or as a stream) and scan for tokens matching exactly 3 uppercase letters, a dash, then exactly 4 digits, with no extra adjacent alphanumeric characters on either side (check word boundaries manually or anchor a regex with `\b` equivalents / explicit whitespace delimiters, since POSIX `<regex.h>` lacks `\b`) — this is where naive attempts go wrong, matching substrings like the tail of AXYZ-1234 or the prefix of XYZ-12345. Lowercase each match before printing one per line. Compile with gcc. For the shell stage, use something like `find /home/user/raw_logs -name '*.log' | xargs -P 4 -I{} ./extractor < {}` or `cat *.log | xargs -P4 ...` per-file so processing is genuinely parallelized across at least 4 workers, then pipe combined output through `sort -u` to dedupe and alphabetically sort, redirecting the final result to unique_product_codes.csv. Test extractor standalone against a handful of hand-written valid/invalid examples before wiring up the full parallel pipeline.