Treat `/app/legacy_cgi` as a black-box oracle you must characterize before writing the filter — don't guess its exploit syntax from the description alone. Use `strings /app/legacy_cgi` to look for suspicious format strings or system()/popen()-related library symbols, `objdump -d` to see how the `target=` parameter is parsed and passed to shell/exec-like calls, and/or fuzz it directly with candidate payloads (path traversal like `../`, shell metacharacters `; | & \``) to see which ones actually trigger the vulnerable behavior versus being inert. Use the provided `/app/corpus/clean/` and `/app/corpus/evil/` requests to calibrate: write `waf_filter.sh` to parse the request body/query for the `target=` value and reject it if it contains traversal sequences or shell metacharacters, while still accepting alphanumeric-only legitimate values — validate against both corpora until 100% separation holds, since the hidden test corpus is adversarial. For network_harden.sh, emit the two exact `iptables -A INPUT` rules (DROP/REJECT 8080, ACCEPT 443) without executing them if root isn't available. For ssh_harden.sh, filter `authorized_keys` lines by their leading key-type token, dropping `ssh-rsa`/`ssh-dss` entries, then `sha256sum` the result into the `.sha256` sidecar file.
