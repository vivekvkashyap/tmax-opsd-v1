Start by inspecting the schema directly: run `sqlite3 /home/user/logistics.db ".schema"` and `.tables` to see the actual column names and foreign-key relationships between `locations`, `routes`, and `shipments` rather than assuming names — a shipment references a route, and a route references source/destination location IDs, so the edge weight (shipment count) must be derived by joining shipments to routes and grouping by route (or by source/dest pair) before aggregating per source location. Write a single SQL query (or equivalent pandas groupby) that: joins `shipments` to `routes` on route_id, counts shipments per `route_id` (or directly per `source_id`), then sums those counts grouped by `source_id` to get the weighted out-degree per location — don't accidentally count distinct routes instead of total shipments, and don't confuse in-degree with out-degree. Join the winning `source_id` back to `locations.name` to get the human-readable hub name, and write exactly that name with no trailing newline issues or extra text to `/home/user/optimal_hub.txt`. Verify by manually summing shipment counts per location from the raw tables to cross-check your script's answer.