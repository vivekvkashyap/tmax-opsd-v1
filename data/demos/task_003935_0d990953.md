Build `/home/user/generate_policy.py` to be driven entirely by scanning the target directory at runtime (it will be run against a held-out codebase later, so never hardcode filenames from the sample you see now). For SRI hashes: walk `app_code/static/*.js`, compute `hashlib.sha384(data).digest()`, base64-encode it, and prefix with `sha384-`. For CSP: walk `app_code/templates/*.html`, extract every inline `<script>...</script>` block's exact text content, SHA-256 + base64 each one prefixed `sha256-`, and build `default-src 'self'; script-src 'self' 'sha256-...' 'sha256-...';` — never emit `'unsafe-inline'` or wildcards, since the oracle explicitly penalizes those. For CWE-732: recursively walk `app_code/`, use `os.stat` on every file to check the world-writable bit (`mode & 0o002`) and the SUID bit (`mode & 0o4000`), and emit one entry per flagged file with `cwe: CWE-732` and `issue` set to `"world-writable"` or `"suid-set"` respectively — make sure precision and recall are both perfect (no false positives, no missed files) since the oracle scores exact set matching. After writing the script, iterate: run it, then run `/app/sec_eval policy.json` to see your score, and adjust based on which category seems to be losing points, without ever reading or modifying the `sec_eval` binary itself.
