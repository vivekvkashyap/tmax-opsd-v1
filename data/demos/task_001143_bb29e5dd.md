Read `/home/user/extract.cpp` and locate the line `double mean_neigh_deg = sum / deg[i];` — this divides by zero whenever a node has no outgoing edges in the adjacency list; guard it with `deg[i] == 0 ? 0.0 : sum / deg[i]` (or an if/else) before computing the mean. Compile with `g++ -O2 -o /home/user/extract /home/user/extract.cpp`, run it on `/home/user/graph.txt`, and redirect stdout to `/home/user/features.txt`. For `density.sh`, read the second column of `features.txt` with `awk` and bucket each value into 4 counters using the exact boundary rules (value==0 → bin0; 0<value<=1 → bin1; 1<value<=2 → bin2; value>2 → bin3), being careful with the `<=` vs `<` boundaries since off-by-one binning is the classic mistake here. Write the four `Bin N: <count>` lines to `/home/user/density.log` in the exact required format (no extra whitespace or trailing text), and manually recompute the expected bin counts by hand from `features.txt` to confirm your awk script's boundary logic matches before considering it done.