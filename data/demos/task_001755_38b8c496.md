Transcribe `/app/dataset_memo.wav` with a speech-to-text tool, listening carefully for each "Node X connects to Node Y" statement, and write each as a `Source,Target` line to `/home/user/graph.txt` — don't guess node names or edges you didn't hear; re-listen/re-transcribe if a name is ambiguous. For `graph_tool.go`, recognize the existing bug is a full cross-join over all edge pairs regardless of the query node — replace it with a real graph traversal: build an adjacency list from the parsed edges, then for each queried node collect its direct out-neighbors (1-hop) and their out-neighbors (2-hop), union them into a set, sort alphabetically, and join with commas, printing one line per stdin query exactly as the current buggy version does structurally (same I/O loop, just correct logic). Test it yourself against small hand-built edge lists with known 1- and 2-hop neighborhoods (including nodes with no outgoing edges and query names absent from the graph) before considering it correct, since it must match a hidden reference binary's stdout exactly across many randomized node queries.
