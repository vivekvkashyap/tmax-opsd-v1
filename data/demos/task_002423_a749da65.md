Write `/home/user/tracker.py` in clear stages: (1) parse `/home/user/rules.ini` with `configparser` to get the `gcode_format`/`elf_format` templates and the `wal_file` log path — don't hardcode these, read them from the file since the exact template strings are the task's real config surface. (2) For each `.gcode` file, open as text and scan the header comment lines for exact matches on `; MATERIAL:<name>` and `; TIME:<seconds>`, extracting the values after the colon. (3) For each `.elf` file, open in binary mode and read byte index 4 (EI_CLASS) directly from the header bytes — 1 means 32-bit, 2 means 64-bit; don't rely on file size or name heuristics. (4) Compute new filenames by substituting the extracted fields into the config's format strings (watch for placeholders beyond `{material}`/`{time}`/`{bitness}`, such as an index, if present in the actual ini content — read the file rather than assuming the format string from the task description alone). (5) Rename with `os.rename`, and for every rename append a line `[RENAME] <old> -> <new>` to the WAL path from the config. Run the script and inspect the renamed directory and WAL log afterward to confirm correctness.