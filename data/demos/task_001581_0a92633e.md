Start by fixing the vendored tool: inspect `/app/mo-3.0.2/mo` for a shell-option line near the top that was commented out (look for something like a `shopt` call being disabled) and restore it — `mo` relies on that shell option to parse Mustache-style `{{VAR}}` templates correctly, and without it templating silently breaks. Verify the fix works standalone (e.g. `export USER_ID=5; /app/mo-3.0.2/mo your_template.sql.mo`) before wiring it into your script. Then write `/home/user/query.sql.mo` as a parameterized SQL template using `{{USER_ID}}` for the user filter, joining `orders`→`order_items`→`products` grouped by category, summing `price*quantity`, formatted to 2 decimals, ordered by total spend descending then category ascending — inspect the schema/data with `sqlite3 /home/user/ecommerce.db` first to get column/table names exactly right. In `run_report.sh`, export `USER_ID="$1"`, render the template with the fixed `mo` binary, and pipe the rendered SQL into `sqlite3 -csv /home/user/ecommerce.db` (or set `.mode csv` inline) to get headerless CSV output. Make the script executable and test it against several user IDs, checking output formatting (no header row, correct decimal places, correct sort order) since it must be bit-exact against a hidden oracle across hundreds of random IDs.