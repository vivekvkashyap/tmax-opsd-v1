Do not try to reverse-engineer the timestamp obfuscation algorithm — the binary is your decoder oracle, so just shell out to `/app/bin/temporal_decoder <hex>` via subprocess for each event and parse its stdout as the epoch integer (cache repeated hex values to avoid redundant calls on large files). Build the pipeline in stages: (1) parse each JSON line and check `set(obj.keys()) == {"user_id","obfuscated_ts","event_type","ip_address"}` exactly — any missing/extra key means immediate EVIL; (2) decode every timestamp via the oracle; (3) bucket by `ts // 300 * 300`; (4) per bucket, track distinct IPs per user_id and a running count of `event_type == "LOGIN_FAILED"`. Flag EVIL the moment any bucket has a user with more than 3 distinct IPs (i.e. >=4) or total LOGIN_FAILED count strictly greater than 10 (i.e. >=11) — get the strict-inequality boundary exactly right, since off-by-one here is the classic bug. Print exactly `EVIL`/`CLEAN` and exit 1/0 accordingly. Before trusting your script, hand-craft small synthetic clean and malicious `.jsonl` fixtures yourself to exercise each rule independently.
