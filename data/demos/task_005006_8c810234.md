Handle the two parts independently. For the audio: transcribe `/app/suspicious_recording.wav` with an available speech-to-text tool (install `whisper` or similar if needed), normalize to lowercase with punctuation stripped, and write exactly that to `transcript.txt` — verify by re-checking the transcript rather than trusting one pass. For the sanitizer: write `wav_sanitizer.c` that manually parses the RIFF header (`RIFF` magic, chunk size, `WAVE` id) and walks sub-chunks (`fmt `, `data`, etc.), reading each declared chunk size as an **unsigned** 32-bit integer (not signed) — malicious files encode negative-looking sizes like `0xFFFFFF00` specifically to defeat naive `int size > MAX` checks that pass but then blow up in `malloc`. Validate that each chunk's declared size doesn't exceed remaining bytes in the file, use explicit bounds checks/assertions rather than trusting header values, and reject (exit 1) on any parse failure, truncated chunk, or overflowing size; exit 0 only for structurally sound files. Compile with a C compiler, then test the binary against both the clean/ and evil/ corpora yourself before considering it done.
