Treat this as two independent subsystems and verify each in isolation before wiring them together. For the proxy/config fix: edit `/app/nginx/nginx.conf` so the `location /ingest` block has `proxy_pass http://127.0.0.1:5000/ingest;` (check path suffix handling carefully — a trailing slash mismatch silently breaks routing), and edit `/app/.env` to set `REDIS_HOST=127.0.0.1`, then run `/app/restart_services.sh` and confirm all three ports (8080, 5000, 6379) are actually listening before moving on. For `validate_loc.py`: detect input format by extension or by attempting JSON parse then falling back to CSV (`csv.DictReader` with `key,translation` columns); collect all translation string lengths, compute mean and population/sample std-dev with the standard library (`statistics.mean`, `statistics.pstdev` or manual math — confirm which the checker expects by testing edge cases), and reject (exit 1, no output file) if any length exceeds `mean + 3*std_dev` OR any value contains `<script>` or `${`. On the boundary — is "strictly greater than" — don't reject values exactly equal to the threshold. On success, write the parsed dict as JSON to the output path and exit 0. Test against constructed clean and malicious fixtures yourself before wiring into Flask.