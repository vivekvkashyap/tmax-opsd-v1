Treat this as a targeted bug hunt, not a rewrite — read the existing `math_util.cpp` and fix each defect in place. Look specifically for: (1) a sort comparator using `<=` instead of `<` — `std::sort` requires strict-weak-ordering, and `<=` on equal elements violates it, causing crashes/UB; fix to strict `<`. (2) An allocation sized one element short of the actual point count (e.g. `new Point[n-1]` when copying `n` points) causing a buffer overflow during the `memcpy`/copy loop — size the allocation to the real count. (3) A mismatched `delete` used on an array allocated with `new[]` — must be `delete[]`. (4) An uninitialized or never-assigned distance accumulator before the sqrt computation — ensure it's cleanly assigned, not accumulated into garbage. After patching, recompile with `-O2 -std=c++17`, rerun on `data.b64`, and diff your `output.csv` against `expected.csv` — an empty diff confirms correctness. Consider running under `valgrind` to confirm no leaks or invalid reads remain.