Build this as a single awk-centric pipeline (or a short chain of grep/sed/sort/awk) rather than many fragile passes, and validate against a few lines by hand before trusting the whole run. First extract fields robustly despite inconsistent line prefixes: use grep/sed or awk with regex captures anchored on the literal labels `User: `, `email: `, `age: `, `joined: ` and the ` | ` separator, ignoring anything before `User:`. Apply validation as filters: drop rows where age (after trimming) isn't composed entirely of digits (reject things like `NaN` or a leading `-`), and drop rows where the email doesn't contain exactly one `@` (reject `invalid_email.com` with zero and `frank@double@at.com` with two). Lowercase the email with `tr '[:upper:]' '[:lower:]'`, derive `email_domain` as everything after the last `@`, and `joined_year` as the first 4 characters of the joined field. For deduplication by user_id, since input order matters only in that any one valid record per id is acceptable, use awk's associative arrays (`seen[uid]`) to keep first-seen and skip repeats — beware a raw duplicate line for user 101 in the data. Finally sort numerically by user_id (`sort -t, -k1,1n`) and prepend the exact header row before writing to /home/user/clean_features.csv; diff your output against your own manual recomputation of a couple of rows to catch mistakes.
