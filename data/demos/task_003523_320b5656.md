Break this into three deliverables and validate each against the given test corpora before wiring them together. (1) For video telemetry, run `ffprobe -select_streams v -show_entries frame=pkt_pts_time,pkt_size -of csv=p=0 /app/conveyor.mp4` (or equivalent) to dump per-frame timestamp and size, then reformat into `timestamp,size` CSV rows at `/home/user/video_ts.csv` — confirm the row count matches the video's actual frame count rather than assuming. (2) For the C filter, write `/home/user/ts_filter.c` that reads line by line, splits strictly on exactly two commas (reject lines with more or fewer fields), validates both `timestamp` and `value` parse fully as floats (e.g. use `strtod` and check the end pointer consumed the whole field, don't accept partial/garbage parses), and deduplicates by timestamp using a hash set or sorted structure sized to handle up to 10,000 entries — keep only the first occurrence per timestamp, preserving original line order for everything else. Test it directly against `/app/corpora/clean/` (output must equal input exactly) and `/app/corpora/evil/` (malformed/duplicate lines dropped) before moving on, since these corpora are your primary correctness signal. (3) Build `/home/user/pipeline.sh` (chmod +x) that runs `ts_filter` on its argument then joins the result with `video_ts.csv` on timestamp (e.g. `join -t,` after sorting both by the join key) to emit `timestamp,sensor_id,value,size`.
