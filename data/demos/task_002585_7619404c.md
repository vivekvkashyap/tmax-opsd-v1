For the PIN, read the MD5 in `/home/user/pin_hash.txt` and brute-force all 4-digit strings "0000"-"9999" (only 10,000 possibilities — a short shell or Python loop computing `hashlib.md5` per candidate is fast enough), writing just the matching digits to `cracked_pin.txt`. For the overflow: read `legacy_parser.c` to see the vulnerable `struct Config { char buffer[64]; int admin_priv; }` and the unbounded `fscanf(f, "%s", cfg.buffer)` — craft a payload in `exploit.py` that writes more than 64 bytes so the overflow spills into the adjacent `admin_priv` field, setting it to a nonzero value (this is a straightforward padding + int-sized nonzero suffix, no ASLR/shellcode needed); write it to `/home/user/malicious.cfg`. When fixing the source, bound the read to 63 chars (e.g. `fscanf(f, "%63s", ...)` or a manual loop) and add a redaction pass over the loaded string using a regex/manual scan for `PIN=` followed by exactly four digits, replacing those digits with `****` before printing. Compile with the exact `gcc -O0` command given, then generate the checksum with `sha256sum /home/user/secure_parser > /home/user/secure_parser.sha256` so the path recorded matches for `sha256sum -c` to pass.
