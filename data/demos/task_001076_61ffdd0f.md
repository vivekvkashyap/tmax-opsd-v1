First fix the vendored library, then build the filter. Search `/app/vendored/pyelftools-0.31/elftools/elf/elffile.py` for the ELF magic-number validation near the start of `ELFFile.__init__` — a junior admin likely swapped or garbled the expected 4-byte magic constant (`\x7fELF`) so valid ELF headers are rejected; diff it mentally against the real ELF spec and restore the correct bytes. Confirm the fix by running `PYTHONPATH=/app/vendored/pyelftools-0.31 python3 -c "from elftools.elf.elffile import ELFFile; ELFFile(open('/bin/ls','rb'))"` and checking it no longer raises. For `elf_filter.py`: set/import with that PYTHONPATH, walk the target directory with `os.walk`, open each file in binary mode (or mmap for large files), wrap `ELFFile(stream)` construction and section iteration in try/except so any parse failure (bad magic, corrupt offsets) marks the file `delete`; otherwise iterate `elffile.iter_sections()` and check each section's `.name` for exact string `.storage_bloat` — if present, `delete`, else `preserve`. Never let one bad file crash the whole scan. Write the path→verdict dict as JSON to `/home/user/elf_report.json`.