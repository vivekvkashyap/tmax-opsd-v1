First inspect the schema of `/home/user/audit.db` with `sqlite3 audit.db .schema` to see the exact columns of `employees`, `resources`, and `access_logs`. The bug to avoid is joining `access_logs` to itself (or to a second copy of `resources`) for the co-access calculation without properly restricting the join keys — that implicit cross join multiplies rows across every pair of log entries instead of every pair of *employees* sharing a resource, inflating counts. Structure the query as CTEs: (1) filter access_logs to HIGH-sensitivity resources only via a join to resources; (2) aggregate per employee to get `high_risk_accesses`; (3) use a window function like `AVG(...) OVER (PARTITION BY department)` computed only over employees who qualified in step 2, rounded to 2 decimals; (4) for co-access, self-join the filtered HIGH-access log table on resource_id where emp_id differs, then COUNT(DISTINCT other emp_id) per employee. Only keep employees with at least one HIGH access. Write the whole thing as a single `sqlite3 audit.db "<query>" -header -csv > /home/user/audit_report.csv` invocation inside an executable `run_audit.sh`, sorted by co_access_degree DESC, high_risk_accesses DESC, emp_id ASC. Sanity-check row-by-row against the raw access_logs before trusting the output.