Read `/home/user/app.py` to find the HMAC verification and the WAF regex. The vulnerability class where a validated redirect target is not restricted to trusted destinations is Open Redirect — write its standard CWE ID (format `CWE-XXX`) to `/home/user/cwe.txt`. Note the WAF only blocks strings matching `^https?://`; a protocol-relative URL like `//evil.com` does not start with that scheme prefix, so it slips past the regex while browsers still resolve it as `https://evil.com` when loaded from an HTTPS page — that's your evasion payload, and the task specifies it exactly as `//evil.com`. Extract the hardcoded secret key from the script's source (it's a literal `bytes` constant assigned to a variable). Then compute the HMAC-MD5 of the exact string `//evil.com` using that key (in Python: `hmac.new(key, b"//evil.com", hashlib.md5).hexdigest()`) — don't guess or hand-compute this, run the code. Finally, assemble the query string in the exact literal format `redirect_url=//evil.com&signature=<computed_hex>` with no URL-encoding and no trailing whitespace, and write it verbatim to `/home/user/exploit.txt`.
