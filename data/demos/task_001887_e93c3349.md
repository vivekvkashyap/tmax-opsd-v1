For Part 1, don't guess at the bug — reproduce it first by running `/app/bats-core-1.11.0/bin/bats` against any valid `.bats` file and reading the exact error message, then trace which script in `libexec/bats-core/` raises it (start with `bats-exec-test`, since that's where a test file gets sourced). Use `grep -n` for the failing variable name across `libexec/bats-core/*` to find where it's referenced but never set (a renamed/misspelled variable is the classic cause of "cannot source test file" style errors) and restore the correct, consistently-used name. Verify the fix by re-running bats against a clean fixture and confirming exit code 0 with expected output. For Part 2, do not rely on a single `grep -w eval` — build the linter carefully: check line 1 for an exact string match against the required shebang (not just "contains bats"), and separately scan the whole file for the token `eval` bounded by non-word characters (`grep -Pq '\beval\b'` or equivalent), since naive checks fail on things like `evaluate` (should NOT match) or `eval` embedded oddly. Validate your linter against both provided corpora (`/app/corpus/clean`, `/app/corpus/evil`) before considering it done, checking exit codes exactly.
