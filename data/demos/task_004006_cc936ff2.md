Handle encoding detection defensively per file rather than assuming: try decoding each file with `UTF-8` first, and if that raises a `UnicodeDecodeError`, fall back to `UTF-16` and then `ISO-8859-1` (or use `chardet`/inspect the byte-order-mark to pick), since each of the three files is deliberately encoded differently and reading with the wrong codec will corrupt or crash on the matrix/polynomial text. Once decoded, use `re` to find the matrix pattern like `\[\[\s*(-?\d+)\s*,\s*(-?\d+)\s*\]\s*,\s*\[\s*(-?\d+)\s*,\s*(-?\d+)\s*\]\]` capturing a,b,c,d and compute trace as `a + d` (ignore b, c). For the polynomial, extract the expression text (terms separated by `+`/`-` involving optional integer coefficients before `x^2` or `x`, plus a constant term) with a regex over signed terms, handling implicit coefficient 1 when a term is just `x^2` or `x` (and `-x^2`/`-x` meaning coefficient -1), then evaluate at x=10 by summing `coef * 10**degree` per term rather than using `eval()` on untrusted-looking text. Write results per file into the exact nested JSON structure requested (`trace`, `poly_eval_10` as integers) keyed by filename, and sanity-check your regex against all three sample files by hand before finalizing, since a single missed term silently produces a wrong integer.