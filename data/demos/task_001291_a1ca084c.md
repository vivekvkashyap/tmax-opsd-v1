Treat the closed-source `/app/legacy_mailer` as a black box to characterize empirically rather than something to patch: feed it a range of crafted `.eml` files (very long `Subject:` values around common overflow boundaries, emails missing `From:` entirely, body lines starting with a dot followed by text vs. dot-space) while watching its exit status/signal (`echo $?`, check for SIGSEGV/SIGABRT) and whether it hangs (run under `timeout`), using the provided `training_corpus/clean` and `training_corpus/evil` samples as a starting set of known-good/known-bad examples to narrow down the exact trigger conditions — tools like `strings`/`objdump -d` can help confirm buffer sizes but empirical testing is faster and more reliable here. Encode each discovered trigger as an explicit check in `/home/user/filter.c` (header length limits, presence checks, dot-stuffing pattern detection) plus a `To:` header parse that extracts the local-part before `@` and checks membership against `/etc/passwd` (e.g. via `getpwnam` or parsing the file directly), exiting 0 only if every check passes. Wire `daemon.sh` to loop over `/home/user/spool/incoming/`, branch on `filter`'s exit code to either invoke `legacy_mailer` then move to `processed/`, or move straight to `quarantine/`, and register/enable it as a `systemd --user` service so it's running persistently, not just started once.