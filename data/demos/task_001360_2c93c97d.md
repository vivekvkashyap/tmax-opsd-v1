Handle the three problems in dependency order: fix the vendored package first since the pipeline script needs it importable. Open `/app/tinygraph/setup.py` and find the typo'd dependency name in `install_requires` (a misspelling of `networkx`), correct it, then `pip install -e /app/tinygraph` and confirm `python3 -c "import tinygraph"` succeeds. Next, rewrite `/home/user/build_graph.sql` as a recursive CTE rather than a join: seed it with direct `emp_id -> manager_id` pairs from `reporting`, then recursively join the growing result's target back onto `reporting.emp_id` to walk up the chain, unioning all levels together so the final SELECT yields `(source, target)` for every employee paired with every ancestor manager (not just immediate ones, and not a cross join across a department). Test the SQL standalone against the CSVs loaded into a scratch SQLite DB before trusting `pipeline.py` to use it. Finally run `python /home/user/pipeline.py` and inspect the resulting `/home/user/pagerank.json` for sane values (rows summing near 1, no NaNs) — since the real grading compares against a hidden reference via MSE, focus on getting the graph construction exactly right rather than tuning PageRank parameters.