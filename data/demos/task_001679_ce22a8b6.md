This is a multi-service ETL + graph-analytics pipeline — get each hop working and verifiable before optimizing for the time budget. (1) Confirm Postgres and Neo4j are reachable (docker compose ps / simple connection test) and inspect the actual `entities`/`transactions` schema before coding. (2) In Rust, use `sqlx` to stream/batch-read entities and transactions from Postgres, then use `neo4rs` to write them into Neo4j as `Account` nodes and `TRANSFERRED_TO` edges with an `amount` property — batch writes (e.g. `UNWIND` Cypher queries) rather than one query per row, since 50k transactions with per-row round-trips will blow the 15s budget. (3) For betweenness centrality, prefer Neo4j's Graph Data Science (GDS) library procedures (`CALL gds.betweenness.stream/write`) if available rather than hand-rolling Brandes' algorithm in Rust, and write the result back as a `risk_score` node property. (4) Determine the top-5% `risk_score` threshold with a Cypher percentile query, then find shortest paths from `is_sanctioned = true` nodes to nodes above that threshold (Cypher `shortestPath` or GDS shortest-path procs), and emit `sanctioned_id`, `hub_id`, `path_length` triples as JSON to `/home/user/compliance_report.json`. Build with `cargo build --release` and time your run end-to-end against the 15s budget, profiling the ingestion step first since it's the likely bottleneck.