Apply the patch first (`patch -p0 < feature-factorial.patch` or `git apply`, adjusting strip level as needed) so the new `math_ops.c` and the Makefile/main.c hunks land. Inspect the resulting Makefile: the `calc` target's link recipe must list every object file it depends on — if `math_ops.o` is a prerequisite of `calc` but missing from the actual `$(CC) ... -o calc` command line, the linker won't pull in `factorial`, causing an undefined-reference error; add it to the recipe line. Then inspect `math_ops.c`'s factorial loop bound: a loop that runs `for (int i = 1; i < n; i++)` multiplies through n-1, not n, giving a result n times too small — trace through a small n by hand to see the off-by-one and correct the comparison operator. Run `make` and check it links cleanly. Finally pipe `"5 ! P"` into `./calc` (matching the existing token-processing convention shown for `+`), redirecting stdout to `/home/user/calc_out.txt`. Verify the file's contents make sense as 5 factorial before considering the task done.
