Treat `/app/pricing_oracle` as a black box you must characterize empirically, not reverse-engineer by reading its binary — from your C++ code, spawn it as a subprocess (e.g. `popen` or `fork`/`exec`) for a given `request_rate` and candidate `allocation_units`, parse the "Latency: <L> ms, Cost: $<C>" text output, and search for the minimum integer allocation (1–100) where latency stays <= 200ms. Since latency should be monotonically non-increasing as allocation increases, use binary search over the 1–100 range per endpoint instead of a linear scan, to minimize subprocess calls. Write results to the CSV with headers `endpoint_path,allocation_units,cost` exactly as found (don't hardcode any assumed formula for latency/cost — derive everything by querying the oracle). Then generate the Nginx config (port 8080, one `location` block per endpoint proxying to 127.0.0.1:9000, a custom `finops` log_format including request time/upstream response time/request path, logging to /home/user/logs/access.log) and the logrotate config (daily, rotate 7, compress). Finally write deploy.sh to compile with g++ -std=c++17, run the optimizer, and reload/start nginx with your generated config; make it executable and run it end-to-end.
