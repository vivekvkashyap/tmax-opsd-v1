Get services running first, then build the computation pipeline, then wire them together. Start Redis (default port 6379, or via `/app/redis/redis.conf`) and confirm with `redis-cli ping`; in `/app/api/app.py`, find the `redis.Redis(...)` constructor and change its `port` argument to `6379` (don't touch anything else in the Flask logic), then launch the API in the background so it binds `0.0.0.0:8080`. For the science, write `/home/user/process.sh` in bash: parse `/home/user/sequences.fasta` with `awk`/`grep` to pull out each `>id` header and its following sequence line, count G/C characters vs total length per sequence to get growth rate `r`, then simulate the logistic ODE `dP/dt = r*P*(1-P/100)` with Euler's method (`P_new = P + dt * r * P * (1 - P/100)`, dt=1, starting P=10, iterated 10 times) using `awk` or `bc` for the floating-point arithmetic — a for-loop that updates a running P variable each step. Push each sequence's final P value to Redis with `redis-cli SET <seq_id> <value>` (key = ID without the `>`). Validate by curling `http://127.0.0.1:8080/api/population?seq_id=seq1` and checking the JSON response looks sane before finishing.
