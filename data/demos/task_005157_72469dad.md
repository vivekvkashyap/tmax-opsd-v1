Work in stages: first get correlation right, then wire up JWT signing. Parse `auth.log` with a line-by-line regex/split to pull the `ip=` value only from lines containing "Failed login attempt" into a set. Parse `ufw.log` similarly, extracting `SRC=` only from lines that contain both `[UFW BLOCK]` and `DPT=8080` — watch out for `[UFW ALLOW]` lines that also have `DPT=8080`, they must be excluded. Intersect the two sets, then sort the result alphabetically as strings (not numeric IP sort). For the JWT, read the secret file exactly as-is (no trailing newline trimming surprises — read raw bytes/string), initialize a Go module (`go mod init`, `go get github.com/golang-jwt/jwt/v5`), build a claims struct/map with `role` and `audited_ips`, sign with `jwt.SigningMethodHS256` and the secret bytes, and write only the resulting compact token string to the output file with no extra newline or quoting (use something like `os.WriteFile` with the raw token bytes, not `fmt.Println`). Verify locally by decoding your own token with the same secret before finishing.