Build this incrementally and verify each stage with the sqlite3 CLI before wiring it into C++. First design the schema and load the CSV, then work out the index: since the window query needs `ORDER BY sender, timestamp` (or however the window's PARTITION BY/ORDER BY is structured) to avoid a sort, create a composite index on (sender, timestamp) matching that exact column order — test with `EXPLAIN QUERY PLAN` directly in the sqlite3 shell first and iterate on the index definition until the plan shows it using the index instead of "USE TEMP B-TREE FOR ORDER BY". Only port the working SQL into the C++ program afterward, using the C sqlite3 API (`sqlite3_open`, `sqlite3_prepare_v2`, `sqlite3_step`) to run the schema creation, CSV inserts, the EXPLAIN QUERY PLAN capture (redirect its rows to query_plan.txt), and the real window query. For each result row, format one Cypher MERGE/CREATE line exactly matching the required template, taking care with numeric formatting (integers vs decimals) since the verifier normalizes but still expects semantically correct values from your own running_total/rank computation.