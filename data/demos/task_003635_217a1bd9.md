Run OCR on `/app/sys_monitor.png` (e.g. `tesseract /app/sys_monitor.png stdout`) to recover the exact numeric `CRITICAL SYSTEM SALT` value — don't approximate it, since it's a multiplier/parameter your hash computation depends on downstream. Next, fix the build of `/app/math_parser_repo`: inspect `setup.py` for a broken compiler flag/include path (check recent git history for what changed) and correct it so `pip install -e .` succeeds. Then use `git log`/`git bisect start` with a known-good and known-bad commit (test each candidate by rebuilding and running a known query) to pinpoint exactly which commit introduced the wrong hash formula in the C extension; once found, fix or revert that specific change rather than blindly reverting everything, since later commits (like the setup.py breakage) may still be needed. Finally write `/home/user/log_classifier.py` that imports the now-correct `math_parser`, computes `math_parser.compute_hash(query_value, <salt>)` for the log's `query_value`, compares to `reported_hash`, and exits 0 on match / 1 on mismatch. Validate against both `/app/corpus/clean/` and `/app/corpus/evil/` directories and iterate until you get 100% correct classification on both before finishing.
