Apply the patch first with `patch -p0 < update.patch` (or `-p1`, adjust based on the patch header paths) run from `/home/user/build_project`, then verify `grep VULN= src/core.sh` shows the patched line, not the original. Rewrite `build.sh` to explicitly concatenate in dependency order — `cat src/utils.sh src/core.sh src/main.sh > dist/app.sh` — instead of the glob, and keep the `chmod +x dist/app.sh` step. For the leak, edit `src/main.sh` to `rm -f "$TEMP_FILE"` immediately before the function/script returns successfully (after its use, not before), then rerun `build.sh` to regenerate `dist/app.sh` from the fixed sources — editing only the compiled output would be lost on the next build. Write `fuzz.sh` as a loop of 50 iterations calling `./dist/app.sh $RANDOM`, checking `$?` is 0 and `[ -z "$(ls -A cache/)" ]` after each run, exiting 1 immediately on any failure, and only writing `FUZZING SUCCESS` (exact string, no extra whitespace/newline issues) to `report.txt` after all 50 pass.