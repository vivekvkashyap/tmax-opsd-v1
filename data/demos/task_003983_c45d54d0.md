Since the oracle is described as a "very simple linear architecture," treat this as a black-box weight-recovery problem: generate synthetic 64-float binary files and probe `/app/anomaly_oracle <file>` with its exit code to build a labeled dataset. Use targeted probes rather than random guessing — e.g. vary one feature index at a time (holding the rest at zero) and observe how far you must push that single value before the exit code flips, which reveals the per-feature weight ratio and helps separate even/odd-index behavior; also probe the all-zero vector to help estimate the bias term. Once you have enough exit-code observations, fit or directly solve for a linear decision boundary (sum(w_i * x_i) + b > 0 => evil) that's consistent with all your probes — treat it as a linear-classifier recovery problem, not curve-fitting noise. Implement `fast_filter.c` to read each `.bin` file as 64 little-endian floats, compute the dot product with your recovered weights plus bias, and copy to `<output_dir>/clean/` or `<output_dir>/evil/` accordingly, creating those subdirectories first. Time total processing with `clock_gettime` and print "Processed X files in Y seconds". Before finishing, re-verify your C binary's classifications exactly match the oracle's exit codes on a fresh batch of self-generated near-boundary test files, since the eval corpus is explicitly adversarial near the boundary.
