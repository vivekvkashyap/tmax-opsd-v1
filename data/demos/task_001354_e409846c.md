Work the four subsystems independently, testing each in isolation before wiring them together. For the C extension, the CMake fix is to add `find_package(Python3 COMPONENTS Development)` and link against `Python3::Module` instead of the legacy `PythonLibs`/`PYTHON_LIBRARIES` variables — run `cmake` and `make` directly to confirm `ctrie.so` builds before touching Python code, and fix any Python-3 C API syntax errors (e.g. module init macros) as they surface. For the backend, port `server.py` from Python 2 idioms (`print "x"`, `BaseHTTPServer`) to Python 3 (`print()`, `http.server`) and run it standalone first (`python3 server.py`) to confirm it serves on 8081. For the Go proxy, implement rate limiting with a buffered channel or ticker-based token bucket shared across goroutines (a naive per-request `time.Sleep` won't give a hard 5/sec cap under a 20-request burst), returning 429 immediately when the bucket is empty rather than blocking. Decode the JSON body to check `query` for the substring `OR 1=1` and `is_admin == true`, returning 403 before forwarding upstream. Start services via `start.sh &`, then curl both clean and evil sample payloads and fire a concurrent burst with `curl` in a loop backgrounded to confirm exactly 5 pass and 15 return 429 before declaring done.