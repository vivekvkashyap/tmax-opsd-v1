Get the infrastructure working and verified before writing the aggregation logic. Start mongod as specified, then `mongoimport` the papers file, and sanity check with `mongosh` (`db.papers.countDocuments()`, inspect a document) that the import matched the expected schema (field names `_id`, `topics`, `citations`). In the Go module, use the official driver's `bson.D`/`bson.A` (ordered) rather than `bson.M` for pipeline stages, since `$graphLookup` and aggregation pipelines are order-sensitive lists of stages — a plain map won't preserve stage order. Build the pipeline as a literal sequence: `$match` on `topics`, `$graphLookup` with `startWith: "$citations"`, `connectFromField: "citations"`, `connectToField: "_id"`, `as: "citation_network"`, `maxDepth: 1`, then `$addFields` computing `$size` of that array, `$sort` by `network_size: -1, _id: 1`, `$limit: 3`, `$project` keeping only `_id` and `network_size`. Decode the cursor results into a struct/slice matching that shape, marshal with `json.MarshalIndent`, and write to the output file. Double check the graphLookup depth semantics (maxDepth 1 includes the direct citations AND one hop further) by inspecting intermediate results before trusting the final numbers.
