Start by extracting frames from `/app/dashboard_recording.mp4` at a high enough rate to catch a brief flash (e.g. `ffmpeg -i dashboard_recording.mp4 -vf fps=10 frame_%04d.png` or even every frame) and visually inspect (or OCR with `tesseract`) the frames around the point where a `RATE_LIMIT=<value>` overlay appears, to recover the integer value — don't guess it, read it off the frames. Set up the bare repo with `git init --bare /home/user/processor.git`, then write `hooks/post-receive` (executable, `#!/bin/sh`) that reads the ref from stdin, does `git --work-tree=/home/user/deploy --git-dir=/home/user/processor.git checkout -f`, runs `gcc -o /home/user/deploy/processor /home/user/deploy/processor.c`, and restarts the supervisor (e.g. `pkill -f supervisor.sh; nohup /home/user/deploy/supervisor.sh &` or signal an existing supervisor loop). Write `processor.c` to strictly match: exact `[DEBUG]` prefix -> print nothing, exit 0; substring `PANIC` anywhere -> print nothing, exit 1; otherwise print the original string immediately followed by `_RLIMIT<value>` (the number you extracted from the video) with no extra whitespace, exit 0 — check `[DEBUG]` before `PANIC` since prefix and substring checks can overlap in fuzzed input. `supervisor.sh` should `export TZ=Etc/UTC LC_ALL=C.UTF-8`, then loop running the current `processor` binary, appending stdout/stderr to `/home/user/logs/processor.log`, re-reading/re-execing the binary path each iteration so a hook-triggered rebuild is picked up. `logrotate.conf` needs `daily`, `rotate 7`, `compress`, and `size 5M` directives for that log path. Finally commit `processor.c` and push to the bare repo to trigger and verify the whole chain, then fuzz your compiled `processor` against varied ASCII strings yourself to sanity-check exit codes and output format before finishing.