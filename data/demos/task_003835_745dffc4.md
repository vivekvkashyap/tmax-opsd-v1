Diagnose by comparing the two simulation implementations statistically before touching code: `simulate_v1` sums 100 draws from a standard normal per sample using Python's `random.gauss(0,1)`, while `simulate_v2` vectorizes with `np.random.normal`. Read the `scale`/`loc` arguments passed to the numpy normal call carefully — a subtly wrong scale (not exactly 1.0) will silently inflate or shrink the variance of the resulting sums without raising any error, and is exactly the kind of bug that only shows up in a distributional comparison, not a single-sample check. Fix the scale argument to match the intended standard normal step size. For `compare.py`, generate both series with the specified N and seed, then use `scipy.stats.wasserstein_distance` and `scipy.stats.ks_2samp` (take the `.pvalue` attribute) — write results into the JSON with exactly the two required keys. Sanity check by rerunning: if the KS p-value is very small or Wasserstein distance is large, the fix is incomplete or a seed/parameter mismatch remains; a small Wasserstein distance and non-trivial p-value indicate the two now come from matching distributions.