Build the regression test first, before touching git history: it must import `bin_parser` from the repo, feed it the bytes of `/home/user/crash_packet.bin`, and distinguish "crashed with struct.error" (exit 1) from "ran fine or raised a different/handled exception" (exit 0) — wrap the call in try/except specifically catching `struct.error`. Then drive `git bisect start`, mark the current tip `git bisect bad` and an early commit `git bisect good`, and run your script via `git bisect run python3 /home/user/test_regression.py` so git automates the search instead of you eyeballing diffs. After bisect reports the first bad commit, capture its full SHA with `git rev-parse <ref>` (bisect leaves you on a detached commit — copy the hash before doing anything else) and write it to `/home/user/bad_commit_hash.txt`. Then run `git bisect reset`, checkout `main`, and fix the actual root cause in `bin_parser.py`: guard the packet-type-1 payload-length check before the `struct.unpack` call and raise the existing `MalformedPacketError` class with precisely the message text the spec requires — an off-by-one or reworded message will fail verification even if the crash is gone.
