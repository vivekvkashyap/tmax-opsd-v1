Design the script around a worker function that does exactly three things atomically per file: stream-decompress with `gzip.open(path, 'rt')` (never write the decompressed data to disk), scan lines for the exact substring `[CRITICAL]`, and only after successfully collecting matches, acquire an `fcntl.flock(fd, LOCK_EX)` on the shared output file, write/append the matched lines, flush, then release the lock — this ordering prevents interleaved/garbled writes across processes. Rename the source file to `.archived.gz` only after the critical lines for that file have been durably written, so a crash mid-run doesn't lose data or leave a file half-processed. Use `concurrent.futures.ProcessPoolExecutor` (or `multiprocessing.Pool`) with `map`/`submit` over the list of `.log.gz` files so workers truly run in separate processes, since Python threads won't parallelize CPU-bound decompression and also since file locking semantics matter more with real processes. Open the shared output file in append-binary or append-text mode inside each worker (not once in the parent) so each process has its own file descriptor for locking. After running, verify no `.log.gz` files remain, all 20 became `.archived.gz`, and the total line count in critical_events.txt matches expectations.