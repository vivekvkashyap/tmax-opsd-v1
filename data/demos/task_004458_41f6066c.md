Work top-down through the evidence chain. First, `ffmpeg -i /app/evidence.mp4 -vsync 0 frame_%03d.png` to dump every frame, then run `zbarimg` over each frame — most frames will fail to decode (no QR present), so only keep the ones that succeed, in frame/chronological order, and concatenate their decoded text to reconstruct the PEM private key; don't assume every frame contains a code. Second, in Go use `debug/elf` to open the malware binary, locate `.tls_cert` and `.exfil_data` sections by name and pull their raw bytes with `Section.Data()`; parse the cert with `x509.ParseCertificate` and confirm its RSA public key's modulus/exponent match the recovered private key's public half (`rsa.PrivateKey.PublicKey`) before doing any redaction — only proceed if they match. Third, for redaction, read the whole original file as raw bytes yourself (Go's elf package can't rewrite files), use the section's `Offset`/`Size` from the section header to locate `.exfil_data` in that byte slice, and run regexes for the exact card format and standard emails to find match spans, overwriting only the matched bytes in place with `*` — never insert/delete bytes, since that shifts every subsequent offset and corrupts the ELF layout. Write the patched full-file bytes to `/home/user/sanitized.elf`.
