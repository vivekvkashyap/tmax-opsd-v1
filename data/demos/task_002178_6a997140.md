Write and test the SQL logic in the `sqlite3` CLI first before embedding it in C — it's much faster to iterate on the query text that way. In your C program, open the DB with `sqlite3_open`, execute `REINDEX;` via `sqlite3_exec`, then run a single query that joins `telemetry` to `devices` on `device_id = devices.id`, extracts the temperature with `json_extract(payload, '$.temp')`, and computes a moving average using a window function: `AVG(json_extract(payload,'$.temp')) OVER (PARTITION BY device_id ORDER BY ts ROWS BETWEEN 2 PRECEDING AND CURRENT ROW)` — the key detail experts get right is `ROWS BETWEEN 2 PRECEDING AND CURRENT ROW` (a 3-row trailing window), not `RANGE`, and partitioning per device so windows don't blend across devices. Order the final result by `name ASC, ts ASC` in the query itself so you don't need to re-sort in C. Step through result rows with `sqlite3_step`, format `moving_avg` with `%.2f` using `snprintf`, and write CSV rows with the exact header `device_name,timestamp,temperature,moving_avg` to `/home/user/output.csv`. Compile with the exact `g++ -O3 -std=c++17` invocation specified (note: despite being called "C code" this task expects it built with the g++ command given, so link `-lsqlite3`), then execute the binary to actually produce the file.
