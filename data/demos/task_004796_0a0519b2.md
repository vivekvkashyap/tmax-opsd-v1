Treat `/app/legacy_decoder` as a black box first: run `file`/`strings` on it for hints, then generate small handcrafted `.dat` files (varying byte patterns) and observe the JSONL it emits to infer the binary format — look for a fixed magic header at the start, then a repeating pattern of a 1-byte type tag, a length field, and a value, stopping at some sentinel byte. Test edge cases (truncated files, zero-length values, an unrecognized type byte) to see how the binary "gracefully skips" bad records, and replicate that exact skip behavior. Separately, recover the decryption key by exploring git history in `/home/user/pipeline_repo`: run `git log --all --oneline`, `git log --diff-filter=A --all`, and inspect diffs/blobs of deleted files (`git show <commit>:<path>` or `git log -p -- <path>`) to find a commit that added a key file later removed. Use the recovered key to decrypt whichever type tag maps to "encrypted"/XOR-obfuscated values. Write the pure-Python replica to `/home/user/pipeline_repo/decoder.py` matching the exact CLI signature, then validate it against the binary yourself on many generated test files in `/home/user/pipeline_repo/test_regression.py` before relying on the hidden dataset.