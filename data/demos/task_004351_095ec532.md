Work in order: first edit `/home/user/appliance_fs/auth.conf` to flip `auth_type=key` to `auth_type=password` — the simulator sources this file and silently exits when it's set to `key`, so nothing downstream will work until this is fixed. Then install `expect` if missing, and write `/home/user/auto_deploy.exp` using `spawn` on `/home/user/appliance_cli.sh` followed by `expect "Login: "` / `send "admin\r"`, `expect "Password: "` / `send "admin123\r"`, then `expect "appliance> "` before every command you send. Send the four commands (`set interface ...`, `set route ...`, `commit`, `exit`) one at a time, each followed by `expect "appliance> "` (except after `exit`, which should end the interaction — use `expect eof`). A common mistake is sending all commands without waiting for the prompt between them, causing input to be dropped or misread by the simulator's `read` loop. After the script is correct, run `expect /home/user/auto_deploy.exp` and confirm `/home/user/appliance_fs/network.conf` and `/home/user/deploy.log` were written as a result of the `commit` step.
