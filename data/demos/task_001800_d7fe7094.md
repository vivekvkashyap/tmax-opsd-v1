Structure `run.sh` as two clearly separated phases and test each independently before chaining them. Phase 1: for each subdirectory under `/home/user/raw_configs/`, launch processing in parallel (e.g. `ls raw_configs | xargs -P4 -I{} bash -c '...' _ {}`), where each worker: reads `config.txt` as-is or converts `config.json` to `KEY=VALUE` lines with `jq -r 'to_entries[] | "\(.key)=\(.value)"'`, checks whether any key matches `^ERROR_`, and if so skips writing output entirely for that server (do not emit a partial/empty file that later gets treated as valid) — otherwise sort the KEY=VALUE lines by key and write to `/home/user/processed_configs/<server>.txt`. Phase 2 must only run after all Phase 1 background jobs finish (use `wait`), then build the CSV: collect the sorted union of all keys across the processed files (`cut -d= -f1 | sort -u`), for each server determine present keys (e.g. via `join` on sorted files or an associative array per server), and for each key row emit each server's value or an empty field if absent — join on missing keys must produce truly empty CSV cells (`VAL1,,VAL3`), not the literal word "null" or a missing comma. Verify by diffing your output against the exact expected header/rows for the sample servers.