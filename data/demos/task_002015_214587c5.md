Work strictly phase by phase, verifying each extraction before moving on. Extract the outer tarball, then extract models.tar and firmware.zip from within it into their own locations. For GCode renaming: for each extracted gcode file, read its first line to pull the `<ID>` out of the `; AssetID: <ID>` comment (e.g. with `head -1 file | sed`/`grep -oP`), then look up that ID in config.json (`jq` is available and much safer than hand-parsing JSON with grep/sed) to get the `name`, substitute spaces with underscores, and move (not copy, unless you need the original) the file to the gcode output dir with the new name — do this for every file, don't hardcode based on the sample IDs. For the ELF manifest: for each ELF binary, run `readelf -h <file>` and extract the value after the `Machine:` label with sed/awk, trimming whitespace precisely (leading/trailing spaces will break an exact-match comparison), compute `sha256sum`, and append a CSV row in the exact `Filename,Machine,SHA256` header format. Finally, build the last tarball so its root entry is `titan_processed/` (use `tar -czf out.tar.gz -C /home/user titan_processed`, not `-C /home/user/titan_processed .`, which would omit the wrapping directory), and write the sha256 checksum file in the standard `hash  filename` two-space format via `sha256sum` output redirected directly.
