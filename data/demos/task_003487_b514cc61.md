Build and start a minimal Flask/http.server app first, then layer in each transform, testing with curl after each addition. For timestamp alignment, use `dateutil.parser.parse` (handles both ISO 8601 and `YYYY-MM-DD HH:MM:SS TZ` abbreviations reasonably well) or explicitly map known timezone abbreviations (EST, PST, UTC, etc.) to fixed offsets since Python's stdlib doesn't resolve those abbreviations reliably on its own — convert the parsed datetime to UTC and format as `YYYY-MM-DDTHH:MM:SSZ`. For tokenization, treat `/app/legacy_tokenizer` strictly as an opaque subprocess: pipe the raw `message` string into its stdin (with a trailing newline) via `subprocess.run(..., input=..., capture_output=True, text=True)`, and use its stdout verbatim (stripped of trailing newline) as `normalized_message` — do not try to reimplement or guess its normalization logic yourself, since the constraints in step 5 depend on its exact literal output (e.g., check whether it actually contains the token "err" or "fail" and count exact whitespace-separated tokens from what it produced, not from your own guess of what it should produce). Wire in the two validation constraints against that literal tokenizer output, assemble the three-field JSON response, and manually curl a few varied payloads (short messages, messages with "error"/"failed" vs exact "err"/"fail") to confirm behavior before considering the service done.
