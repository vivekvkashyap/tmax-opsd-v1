Start by reverse-engineering the actual schema (`.schema` in sqlite3, or `PRAGMA table_info`) rather than assuming column names — the task only tells you conceptually what the tables represent. Once you know the edges table and the node-metrics table (with node_id, category, score) columns, create the index with `CREATE INDEX idx_category_score ON <metrics_table>(category, score DESC)` so it matches the described access pattern (group by category, sort score descending) — verify it's used with `EXPLAIN QUERY PLAN` on your actual query. Write `/home/user/analyze_subgraph.py` to take `source_node` as argv, and use a parameterized query (`?` placeholder, never string-formatting the ID into SQL) to select direct targets via the edges table. For ranking, use a window function: `SELECT node_id, category, score, RANK() OVER (PARTITION BY category ORDER BY score DESC) AS category_rank FROM node_metrics WHERE node_id IN (<direct targets>)` — compute this via a subquery or CTE, not by ranking in Python, so ties are handled per SQLite's RANK() semantics. Output as a JSON array sorted by `category_rank` ascending then `node_id` ascending, and run it for source_node=100, saving stdout to `/home/user/output_100.json`.