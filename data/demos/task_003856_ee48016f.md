Split this into two independent steps. First, fix the vendored builder: open `/app/vendored_sql_builder/compiler.py`, find the method that compiles CTEs, and look for a place where a boolean parameter (e.g. `is_recursive`) is received but the code branches on a hardcoded literal instead of that parameter — replace the literal with the actual parameter so `WITH RECURSIVE` is emitted correctly when requested. Check `/app/tests` for any existing test that exercises this behavior and run it to confirm the fix. Second, write `/home/user/query_sanitizer.py`, make it executable, and have it read the SQL file passed as `sys.argv[1]`, then apply two independent checks: (a) detect a recursive CTE (case-insensitive `WITH RECURSIVE`) and verify the recursive branch has a bounding condition — a `WHERE` clause constraining depth/recursion in the recursive SELECT, or a `LIMIT`; missing that means unsafe; (b) detect an explicit `CROSS JOIN` and verify a `WHERE` clause filters the joined tables afterward; missing that means unsafe. Use `sys.exit(0)`/`sys.exit(1)` accordingly. Validate against both `/home/user/corpora/clean/` and `/home/user/corpora/evil/` before finishing, since a naive regex on `WHERE` presence anywhere in the query (rather than scoped to the relevant clause) will misclassify borderline cases.
