Approach this as two separable correctness problems: parsing the WAV header/data faithfully, and implementing the exact finite-difference recurrence with matching numeric semantics. For the WAV file, parse the RIFF/fmt/data chunks yourself or use Go's minimal WAV handling to read raw 16-bit little-endian PCM samples from the first channel (careful with stereo interleaving if the file has >1 channel — skip every other sample per channel count), then convert each int16 sample to float64 for the first 1000 samples; a naive read that ignores channel count or chunk offsets will silently desync the sample stream. For the solver, implement the leapfrog update `U[i,t+1] = 2*U[i,t] - U[i,t-1] + r*r*(U[i+1,t]-2*U[i,t]+U[i-1,t])` where `r = c*dt/dx`, initializing U(t=-1) = U(t=0) since velocity is zero, and clamping indices 0 and 999 to zero every step (Dirichlet). Use float64 throughout to match a C double-precision reference, and be precise about loop bounds (interior points 1..998) and the order of operations, since bit-for-bit-adjacent equivalence against a compiled reference will be checked. Parse the four CLI args in the exact order given, compile with `go build -o /home/user/simulator /home/user/simulator.go`, and print each final value with `fmt.Printf("%.6f\n", val)`.