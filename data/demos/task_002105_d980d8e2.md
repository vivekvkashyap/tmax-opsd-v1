Build and test this end-to-end rather than writing the hook blind. First `git init --bare /home/user/infra.git`. Write the Python `post-receive` hook to read `oldrev newrev refname` from stdin (one line per ref, but you only need to handle the single push here), then use `git --git-dir=/home/user/infra.git show <newrev>:deploy_config.json` (run via `subprocess`) to pull the pushed file's content without needing a working tree. Parse it as JSON and implement each side effect precisely as specified: for the fstab, read existing lines first and only append a mount line if no existing line already starts with `"<source> <destination> "` (idempotency check, not just avoiding exact duplicates); for the container status, load/modify/rewrite the existing JSON file, deriving the abbreviated hash via `newrev[:7]` or `git rev-parse --short`; for the mail config, write the three lines exactly as specified with the same abbreviated hash. Make the hook executable (`chmod +x`). Finally clone the bare repo, add the exact `deploy_config.json` given, commit, and `git push origin master` — then inspect all three output files yourself to confirm the hook actually fired and produced the right content before considering it done.