Use `git bisect` rather than manually scanning 200 commits: `git bisect start`, mark `main` bad and `v1.0.0` good, then automate each step with `git bisect run <script.sh>` — the script builds, starts the server, probes `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:8080/api/v1/metrics` (or checks for a crash/panic), kills the server, and exits 0/1 accordingly. Once bisect finds the culprit, capture its full hash (`git rev-parse HEAD` during bisect, or from bisect's summary) and write it to `/home/user/bad_commit.txt`, then `git bisect reset`. Check out `main`, `git show <hash>` to see the unsafe code introduced in `src/metrics.rs` (likely an unsafe null-pointer read or an `.unwrap()`/`.expect()` on a `None`/`Err` value for this route), and fix it defensively with proper error/bounds handling rather than deleting functionality. Rebuild with `cargo build --release`, confirm the endpoint returns exactly `Metrics OK` via `curl`, then start it detached (e.g. `nohup ./target/release/<bin> &`) so it stays listening after your session ends.