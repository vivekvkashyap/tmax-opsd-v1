Two independent fixes to `fit.py`, verify each with the given commands. For `compute_metric`, the non-reproducibility comes from summing floats in shuffled order with plain `+=`; fix it by building the list of signed values first (keep the shuffle and sign logic exactly as-is) and reducing with `math.fsum(values)` instead of a running float accumulator — `fsum` is order-independent to full precision, which is what makes `res1 == res2` hold exactly regardless of shuffle seed. For `save_to_netcdf`, open the HDF5 input with `h5py`, read the 2D `spectra` dataset, apply `np.abs(np.fft.rfft(row))` per row (use `axis=1` on the whole 2D array for efficiency instead of looping), then create the output NetCDF4 file with the `netCDF4` package: declare dimensions `row` and `freq` sized to match the FFT output shape, create a variable `power_spectrum` of type `f8` over those two dimensions, and assign the computed array into it. After editing, run `pytest /home/user/project/test_fit.py` and also execute `save_to_netcdf('data.h5', 'output.nc')` from within `/home/user/project` to confirm `output.nc` is actually produced with the expected shape before finishing.