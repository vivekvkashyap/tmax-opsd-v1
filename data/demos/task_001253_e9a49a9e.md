Begin by decompiling `/app/services/legacy-auth/auth_token.pyc` — use `uncompyle6`/`decompyle3`/`pycdc`, or failing that disassemble with `dis.dis` on the loaded code object via `import marshal, dis` — to recover exactly how the admin cookie is built and how logs are XOR/weak-encrypted (look for a static key string used byte-wise against the plaintext). Use that logic to mint a valid cookie, GET `http://127.0.0.1:5002/export`, and write `/home/user/decrypt_logs.py` that reimplements the same decryption to turn the encrypted payload into a decrypted JSON array at `/home/user/decrypted_logs.json`. Separately, open `/app/services/log-ingest/app.py` and find the `/search` handler building SQL via f-string/`%`/`+` concatenation of `request.args`; replace it with a parameterized query (`cursor.execute(query, (param,))`) and add basic input validation, then restart the `log-ingest` process so the fix is live (test with a `' OR 1=1 --` payload to confirm it no longer dumps all rows). Finally build `secure-audit-proxy` as its own small HTTP server on `127.0.0.1:9000` with a `/compliance?id=<id>` route that checks the `Authorization: Bearer COMPLIANCE_SECURE_TOKEN` header (401 if wrong/missing), then internally queries the patched log-ingest service, decrypts the entry with your recovered logic, and returns `{"id":..., "action":..., "user":...}`; run it in the background and keep it alive.