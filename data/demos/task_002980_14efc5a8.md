Start by trying to import the vendored package to reproduce the crash: `pip install -e /app/gq_engine-1.0.4` then `python -c "from gq_engine.planner import QueryPlanner"`. Read the traceback carefully — it will point at a config value, not a code path, so grep the package's config module (e.g. `config.py`) for suspiciously invalid constants like a negative recursion-depth limit. Fix the single bad value to a sane positive integer, reinstall, and confirm the import and a basic `QueryPlanner(...).parse()` call succeed before moving to Phase 2. For the validator script, load the JSON, build the plan via `QueryPlanner`, then evaluate the three rejection conditions independently (cost threshold via `estimate_cost()`, a cycle check that looks for any repeated label in `restricted_path` if that key exists, and a `max_depth` bound) — use `dict.get()` with defaults so missing keys never raise. Reject (exit 1) if any condition trips, otherwise accept (exit 0). Test against small handwritten clean/evil JSON files of your own before assuming it generalizes to the hidden corpora.