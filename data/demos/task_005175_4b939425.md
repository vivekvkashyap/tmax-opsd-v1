Work through the three problems in the order given, using git history for the secret before touching code. Run `git log -p` (or `git log --all -p -- calc_variance.c`) in `/home/user/risk_engine` and scan every commit's diff for a hardcoded string/macro that looks like a credential — it will appear in an early commit and be removed in a later one, so you must look at historical diffs, not just the current file. For the build failure, run `./build.sh` to see the actual compiler/linker error; a `math.h` function like `sqrt` used without linking `-lm` is a classic cause and shows up as an "undefined reference" at link time, so add `-lm` to the gcc invocation. For the numerical instability, replace the naive one-pass `sum`/`sum_sq` variance formula (which subtracts two large nearly-equal numbers, losing precision) with a two-pass approach: first compute the mean, then sum `(x - mean)^2` in a second pass, divide by `n-1`, and take the square root — this avoids catastrophic cancellation entirely. Read `data.txt`, compute correctly, and write `SECRET: ...` / `STDDEV: ...` to the log in exactly the specified two-line format, double-checking your recovered secret string against what actually appears in the git diff rather than paraphrasing it.