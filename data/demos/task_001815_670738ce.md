First recover the scoring parameters from `/app/scoring_rules.png` using OCR (`tesseract /app/scoring_rules.png stdout`, adjusting `--psm` if needed) — read off the match score and the two mismatch penalties (transition vs transversion) exactly as printed, don't guess standard bioinformatics defaults. Then write a C++ program that loads `/app/sequences.fasta`, and implements the fitness function: for a candidate 15-mer, slide it across each 200-base sequence, score each window position by summing per-base scores (match/transition/transversion per the recovered values), take the max window score per sequence, and sum across all 100 sequences. Since brute-forcing all 4^15 motifs is infeasible, implement a stochastic local search: start from one or more random or greedily-seeded motifs, and repeatedly mutate a single base, re-evaluate fitness (early-exit/prune to keep it fast, e.g. cache partial computations or restrict evaluation to a data structure of decoded sequences as integer arrays for speed), accepting improvements (hill-climbing) or using simulated-annealing acceptance to escape local optima, running many iterations/generations logged to `/home/user/optimization.log`. Because the dataset was generated from one embedded motif with mutations, search should converge toward it — take the best motif found across restarts and write it, 15 uppercase A/C/G/T characters only, to `/home/user/optimal_motif.txt`. Confirm your own scoring code reaches the required fitness threshold before finishing.