First inspect the container to find the subtitle stream index: `ffprobe /app/db_recording.mp4` (it's referenced as stream 0:1). Extract it as text with `ffmpeg -i /app/db_recording.mp4 -map 0:s:0 -c:s srt /tmp/queries.srt` (or copy codec then convert), then strip SRT numbering/timestamps to get the raw SQL lines. Parse each SQL statement with regex to reverse-engineer the schema: for `SELECT ... FROM <table>` capture the selected columns (handle qualified `alias.column` by stripping the alias prefix) and any `JOIN ... ON a.x = b.y` conditions which also reveal columns; for `INSERT INTO <table> (<cols>) VALUES (...)` capture table+columns directly. Aggregate per-table column sets, dedupe, sort alphabetically. For the HTTP server, don't try to hand-roll raw socket parsing if you can avoid it — a simple `while true; do { printf 'HTTP/1.1 200 OK\r\nContent-Type: application/json\r\n\r\n'; cat schema.json; } | nc -l -p 8080 -q1; done` loop (or Python's http.server for reliability) satisfies "listens on 0.0.0.0:8080, responds to GET /api/schema". Serve the JSON as a static pre-generated file, not recomputed per-request.
