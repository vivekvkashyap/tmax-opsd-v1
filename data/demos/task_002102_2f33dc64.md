Split this into two independent sub-problems and solve each with the right tool. First, transcribe `/app/query.wav` (e.g. with a speech-to-text CLI such as `whisper` if available, or another ASR tool present in the environment) and carefully parse the spoken numbers/words into the exact `user_id` (integer) and `region_code` (string) it names — read the transcript back and sanity-check digit-by-digit before hardcoding it into your script, since a transcription slip will silently break every downstream check. Second, for performance, avoid row-by-row Python loops over a 1M-row CSV entirely — use a vectorized library (pandas with boolean masking, or better, polars/duckdb which push filtering down efficiently) to filter, then use vectorized string operations (or a single regex pass) for lowercasing/stripping punctuation and a `Counter`/`value_counts()` over the exploded token column to find the top token, and vectorized `.sum()` for the amount. Time your script locally with `time python fast_etl.py` to confirm it beats the 2-second budget before considering it done, and write the JSON with `round(...,2)` for the amount.