Run the setup script first to stand up MongoDB and populate data before writing the analysis — verify it actually started (check the log/port 27017) since local unprivileged Mongo installs can silently fail. In `audit.py`, use a MongoDB aggregation pipeline with `$group` on `{sender, receiver}` summing `amount`, then `$match` on `total > 10000` to get the filtered edge list — don't do this filtering in Python after pulling everything, since the task explicitly wants the aggregation done in Mongo. Build a `networkx.DiGraph` from the filtered (sender, receiver) pairs as edges (ignore weights per the spec), find cycle membership via `networkx.simple_cycles` or by checking each node's strongly-connected-component size (a node is in a cycle iff its SCC has >1 node, or it has a self-loop), then compute `networkx.pagerank(G, alpha=0.85)` over the *entire* graph (not just the cycle subgraph). Intersect the cycle-node set with the full pagerank ranking, take the top 3 by pagerank descending, round to 4 decimals, and write the JSON array in that exact object-list format.
