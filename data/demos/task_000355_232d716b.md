Load both CSVs into an in-memory `sqlite3.connect(':memory:')` database: create a `routers(router_id TEXT, properties TEXT)` table and a `links(source, target, latency_ms)` table, then bulk-insert rows read via `csv.DictReader`. For the "valid router" filter, use SQLite's JSON1 functions in a parameterized query, e.g. `SELECT router_id FROM routers WHERE json_extract(properties, '$.active') = 1 AND json_extract(properties, '$.capacity') >= ?` (careful: JSON booleans come back as integer 1/0 in SQLite, not Python True) — this is the "NoSQL-style" querying the task wants, don't just filter in Python string logic alone. Collect the valid router IDs into a set, then build a `networkx.Graph()`, adding an edge only when BOTH endpoints of a `links.csv` row are in that valid set, with `weight=latency_ms`. Use `networkx.shortest_path_length(G, 'R1', 'R15', weight='weight')` (Dijkstra) to get the total latency, and write `Total Latency: <integer>` to `/home/user/answer.txt`. Make the script fully dynamic — no hardcoded router lists or path.
