First inspect `/app/artifact_feed.mp4` (duration, resolution, bitrate) with `ffprobe` so you can pick an encoding target intelligently rather than guessing. Encode with `ffmpeg` using a quality-driven approach (e.g. libx264/libx265 with a CRF sweep, or two-pass bitrate targeting) and iterate: after each attempt, check the output file size against the 2,000,000-byte hard cap and measure SSIM against the original using `ffmpeg -i orig -i optimized -lavfi ssim -f null -` (grep the "All:" value), adjusting CRF/bitrate until you satisfy both the size ceiling and SSIM >= 0.92 — start near the quality threshold and back off just enough to fit the size budget, don't over-compress. Save the final file as `/home/user/optimized_artifact.mp4`. Then parse `/app/repo_layout.conf` programmatically (don't hardcode its values) to get BASE_DIR, PRIMARY_LOCATION, HARD_LINKS, and SYM_LINKS; create the directory tree under BASE_DIR, place the optimized file at the primary location, use `ln` for each hard link (same inode) and `ln -s` with a relative path (computed via each symlink's own directory) for each symlink so the repo stays portable if moved.