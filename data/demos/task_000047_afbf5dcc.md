Treat the DAG and DROP-TABLE checks as straightforward: implement cycle detection over the `dependencies` adjacency dict (e.g. Kahn's algorithm or DFS with a recursion-stack set) and a case-insensitive substring search for `DROP TABLE` in `schema_migration`. The hard part is the URL rule set enforced by the stripped `/app/legacy_route_validator` binary — don't try to disassemble it purely by reading; combine static inspection (`strings`, `objdump -d`, `ltrace`/`strace` on a run) with systematic black-box probing: run the binary directly against many crafted URLs (with/without `../` in the path, with/without `admin=true`, with duplicate query keys, with edge cases like `?` at the end or no query string) and record its exit code for each, to empirically pin down exactly which conditions cause rejection. Only reimplement the confirmed logic in `/home/user/ci_filter.py`; the script must never invoke the binary itself. Since grading uses a hidden adversarial corpus that must be 100% correctly classified, test your reimplementation against your own probe cases before finishing, not just the example manifest.