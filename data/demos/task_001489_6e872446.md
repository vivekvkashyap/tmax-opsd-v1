Build this pandas pipeline stage by stage and print intermediate shapes/logs as you go so bugs surface early. Load the CSV, drop exact duplicate rows with `drop_duplicates()` (keep first — default behavior), drop rows with negative `speed_kmh`, and drop rows with missing/empty `battery_mv` (`dropna(subset=['battery_mv'])`, watch out for empty-string vs NaN depending on how pandas parses the CSV). Compute `battery_pct = (battery_mv - 3200) / 1000 * 100` then clip to [0, 100] with `.clip(lower=0, upper=100)`. For rolling speed, sort by `timestamp` within each `device_id` group and use `groupby('device_id')['speed_kmh'].transform(lambda s: s.rolling(window=3, min_periods=1).mean())` so the first two rows average over however many points are available. For bucketing, floor each timestamp to a 15-minute boundary (e.g. `pd.to_datetime(...).dt.floor('15min')`) then group by `(interval_start, device_id)` computing max of `temp_c`, mean of `rolling_speed`, and max-minus-min of `battery_pct`. Round the three output metrics to 2 decimals, sort by `interval_start` then `device_id`, write the CSV with the exact column order specified, and log the total dropped-row count to `/home/user/pipeline.log`.
