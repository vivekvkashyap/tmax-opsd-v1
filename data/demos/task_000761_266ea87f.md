Transcribe /app/voicemail.wav first (whisper.cpp or similar STT) to recover the exact spoken passphrase — do not guess it. Write /home/user/restore.exp using Expect's `spawn`/`expect`/`send` to run /app/secure_restore.sh, match its password prompt, and send the transcribed passphrase followed by a newline; run the expect script to populate /home/user/restored_data/. For the Go rewrite of oracle_parser: since it's a black-box binary, do NOT try to disassemble it from scratch — instead treat it as an oracle and black-box test it by piping many crafted log lines (varying level, component, message content/spacing) into it and diffing outputs to infer the exact JSON field order, key names, and any level-specific quirks (the task hints components may mutate under certain status levels — test FATAL/ERROR/WARN/INFO specifically to catch this). Once you've inferred the full grammar including edge cases, implement matching parsing/formatting logic in Go, build with `go build -o /home/user/uptime_parser /home/user/uptime_parser.go`, and re-verify by diffing your binary's output against the oracle's for the same sample inputs, including edge cases like unusual component names or short/long messages.
