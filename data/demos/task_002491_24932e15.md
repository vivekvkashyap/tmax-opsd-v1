Work through the chain in order, confirming each piece before building on it. Run `tesseract /app/admin_note.png` (or equivalent OCR) and read the output carefully for the line following "MASTER_KEY=" to recover the exact 8-character hex key — don't guess or truncate. Then open `/app/auth_module.py` and trace through `generate_token` by hand (or by running it directly with test inputs) to understand precisely how it derives a token from `master_key` and `challenge` — reproduce its exact arithmetic (bitwise ops, modulus, hex formatting) rather than approximating it, and sanity-check by calling the original function yourself with a sample challenge and comparing to your reimplementation's output. Build the HTTP server (Python's `http.server` or a small framework) so `GET /scan` reads `X-Challenge` and `X-Auth-Token` headers, recomputes the expected token using your reimplementation plus the OCR'd key, and does an exact string comparison — return `200 SCAN_AUTHORIZED` on match, `403 ACCESS_DENIED` otherwise. Test locally with `curl -H` before relying on the automated suite. Finally write `/home/user/sshd_config_hardened` containing exactly the five listed directives, nothing else, one per line.
