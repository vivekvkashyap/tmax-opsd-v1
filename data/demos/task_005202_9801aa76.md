Process `/home/user/sensor_data.csv` in a streaming/chunked fashion (e.g. Python with `csv.reader` iterating row by row, or pandas `read_csv(chunksize=...)`) rather than assuming it fits in memory, filtering to `device_id == 'dev_42'` as you go. Bucket each surviving reading into its 1-minute-aligned timestamp (truncate seconds/fractional part to `:00Z`), and average temperatures that land in the same minute — do this aggregation pass before worrying about gaps, and don't assume input rows are perfectly ordered (sort or otherwise reconcile out-of-order timestamps by minute-key rather than by arrival order). Once you have one aggregated value per occupied minute, walk the *full* span from the earliest to latest occupied minute and identify runs of missing minutes between two known points: if a run's length is between 1 and 5 minutes, forward-fill each missing minute with the temperature of the minute immediately preceding the gap; if the run exceeds 5 minutes, leave every minute in that run absent from the output entirely (don't emit a row for it). Format each output row's average to exactly two decimals, emit rows only for minutes that ended up with a value (real or filled), sort by timestamp ascending, and write the exact header `timestamp,device_id,avg_temperature` to `/home/user/cleaned_dev_42.csv`. Double check gap-length boundaries (exactly 5 vs 6 missing minutes) since that edge is where naive implementations diverge.