This is a classic train/test leakage bug in awk-based scaling — fix the *order of operations*, not just the formula. Rewrite `/home/user/etl.sh` so it first splits `/home/user/data/raw.csv` into the training block (the first 80 data rows after the header, i.e. `NR` 2–81) and the test block (`NR` 82 onward), computes min/max for each of the three columns using only the training block's values, and then applies that single train-derived min/max to scale *both* the training rows and the test rows (accept that some scaled test values may legitimately land outside [0,1] — that's expected once leakage is removed, not a bug to "fix" further). Keep the schema validation pass as-is. For the final step, apply the fixed linear regression weights (`W_age=2.5, W_income=0.5, W_score=-1.2, bias=5.0`) to each scaled test row and write exactly 20 floating-point predictions, one per line, no header, to `/home/user/predictions.txt`. Test incrementally: first print the train-only min/max to confirm they differ from the whole-dataset min/max, then verify row count and formatting of the final predictions file.