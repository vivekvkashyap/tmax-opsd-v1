Split this into extraction, measurement, and curve-fitting stages so you can sanity-check each before trusting the fit. First extract frames with `ffmpeg -i /app/reaction_spectroscopy.mp4 frame_%04d.png` (or read frames directly with OpenCV/`cv2.VideoCapture` without dumping to disk). For each frame, load it as a numpy array and find the brightest pixel's x-coordinate — e.g. convert to grayscale, take `np.unravel_index(np.argmax(gray), gray.shape)` and keep the column index; do this per frame in order to build arrays `t = [0, 1, 2, ...]` and `x = [x0, x1, ...]`. Plot or print `x` vs `t` first to confirm it looks like a decaying exponential approaching an asymptote — this catches extraction bugs before you fit. Then fit `x(t) = A*exp(-k*t) + C` with `scipy.optimize.curve_fit`, giving reasonable initial guesses (e.g. `A0 = x[0]-x[-1]`, `k0` small positive, `C0 = x[-1]`) since a bad guess can make the nonlinear fit diverge or converge to a wrong sign of k. Extract the fitted `k` parameter and write only that float (nothing else) to `/home/user/decay_constant.txt`.
