Read `/home/user/failed_backup.log` carefully: the stack-trace block identifies the looping symlink as the last path listed before "Resolves back to" — locate that exact symlink on disk (don't just guess a path) and `rm` only that symlink, leaving other symlinks intact. Before deleting, use `ls -la`/`find -type l` to confirm which entries under `/home/user/data` are symlinks, since you must dereference the *remaining* ones (e.g. one pointing to a file outside the data directory) when archiving. Build the archive with `tar -czhf /home/user/backup.tar.gz -C /home/user data` — the `-h` flag is essential to follow/dereference symlinks into their target file content rather than storing the link itself; verify with `tar -tvf` that `data/metrics.json` shows up as a regular file, not a symlink. Extract just that member (`tar -xzf backup.tar.gz data/metrics.json`), then convert its JSON array of objects to CSV with exactly the header `id,value,status` in that column order — a quick `jq -r` pipeline (headers then `.[] | [.id,.value,.status] | @csv`) is a reliable way to guarantee exact field order and formatting.
