Work the log analysis before touching the CSV. Grep `/home/user/access.log` for lines containing SQL injection indicators (`UNION`, `SELECT`, etc.) in the request URI AND filter to only those with HTTP status `200` — note that a failed/error attempt (non-200) with the same payload keywords must be excluded, and among successful attempts pick the entry representing full exfiltration (the one actually pulling the sensitive columns), recording its source IP and exact timestamp from the log line. For the CSV redaction, use targeted regex substitutions with `sed -E` (or `awk`) matching the SSN pattern `\d{3}-\d{2}-\d{4}` → `XXX-XX-XXXX` and the credit card pattern `\d{4}-\d{4}-\d{4}-\d{4}` → `XXXX-XXXX-XXXX-XXXX`, applied only to those two columns/fields so the header, id, name, and email remain byte-for-byte unchanged. Count the redacted data rows (excluding header) for the report. Build `/home/user/forensics_report.txt` matching the exact three-line key:value format requested, with the IP, timestamp string as it appears in the log, and the row count.
