Split this into three independent deliverables and verify each in isolation before moving on. (1) Video: run `ffmpeg -i /app/vnc_capture.mp4 frames/frame_%04d.png` to dump frames, then write `/home/user/analyze_frames.cpp` using a small image library (e.g. stb_image, or decode PNG via a simple loader) to scan each frame for a contiguous 20x20 block of pure red (255,0,0) pixels — don't just check a single pixel, verify the full 20x20 region to avoid false positives on partial red artifacts. Track the 0-indexed frame number of the first match and write it to `/home/user/crash_frame.txt`. Compile with g++. (2) Classifier: parse each sshd log file, extract timestamp+message for lines containing "Accepted publickey" and lines containing "Connection closed by authenticating user", and flag evil (exit 1) only when a closed-connection line's timestamp exactly matches (same second) an accepted-publickey line — don't just look for the two substrings occurring anywhere in the file, order and exact timestamp equality matter. Test against both corpora directories until 100% pass. (3) Supervisor: fork/exec `/app/mock_qemu.sh` in a loop, `wait` on its PID, check `$?`, and if nonzero, sleep well under 1s before restarting, appending timestamp + "Restarted QEMU" to the log each time; run this loop forever (e.g. `while true`) and background it properly so it doesn't block the calling shell.