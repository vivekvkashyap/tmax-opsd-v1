Split this into fixing the vendored dependency and writing the validator, verifying each independently. For `sqlite-utils`, don't just skim the code — write a tiny reproduction: load a CSV with duplicate values in a column, call `create_index([...])` on that column (non-unique), and check whether rows silently vanish or duplicates get rejected; trace into `db.py`'s index-creation SQL string and look for a place where `UNIQUE` is being hardcoded into the `CREATE INDEX` statement regardless of the caller's `unique` argument — fix it so the SQL only includes `UNIQUE` when actually requested, then rerun your repro to confirm all rows survive indexing. For the validator script, dynamically detect the id/parent columns from the CSV header (looking for substrings like "id" and "parent"/"manager" rather than assuming fixed column names), load rows into SQLite via the now-fixed `sqlite-utils`, index the parent column, and run a recursive CTE (`WITH RECURSIVE ... UNION`) that walks each row's ancestor chain and flags if it revisits a starting node — that's your cycle detector. Print exactly `CLEAN: <filename>` (exit 0) or `EVIL: <filename>` (exit 1) with no extra output, and test against both the clean and evil corpora directories before finishing.
