Start with `sqlite3 /home/user/topology.db "REINDEX;"` (or drop+recreate `idx_edges_src`) to address the corruption. Then build the query incrementally: first create a view/CTE of compute-only nodes (`role = 'compute'`) and filter `edges` to only rows where both `src` and `dst` are in that compute set. For directed-triangle detection, self-join edges three ways — e1(a->b) JOIN e2(b->c) ON e2.src=e1.dst JOIN e3(c->a) ON e3.src=e2.dst AND e3.dst=e1.src — ensuring a, b, c are three distinct nodes (add `a != b AND b != c AND a != c` guards) so you don't count degenerate 2-cycles or self-loops as triangles. Sum the three edge latencies per matched triangle, then take the global `MIN()` across all found triangles. Be careful the self-join doesn't double count the same triangle from different rotation starting points if using DISTINCT/MIN — since you only need the minimum value it likely doesn't matter, but verify by hand against a couple of triangles in the data. Write only the bare integer (no trailing text/newline issues) to `/home/user/min_latency.txt` using something like `printf '%s' "$result" > /home/user/min_latency.txt`.