First reproduce and localize the hang before fixing output: run `track.py` with a short timeout and tracing to find which frame it stalls on — likely a recursive/mean-shift loop lacking a base case or iteration cap. Fix by adding an explicit iteration limit and/or convergence-delta check so it always terminates. Separately, detect corrupted/static frames with a simple statistical test (e.g. pixel variance or frame-diff far outside normal range) before tracking them; on detection, skip tracking and interpolate the bounding box from the nearest valid neighboring frame instead of letting the optimizer spin on noise. Add a unit test in `test_tracker.py` that calls the recursive function directly with adversarial (zero-gradient/noise) input and asserts termination within 50 iterations. Finally run the fixed script over the full video to produce `/home/user/tracker/output.json` as `[x, y, w, h]` per frame, and sanity-check smooth frame-to-frame motion (a jump suggests interpolation isn't wired in) since scoring compares against hidden ground truth via IoU.
