Treat this as two separate subtasks. For the bisect: use `git bisect start`, mark `main` bad and an early commit good, and at each step actually run `worker.sh` with data that triggers a negative sensor value to decide good/bad — look for a `while` loop guarding on a sensor value comparison that never updates its loop variable when the value is negative, causing an infinite spin. Fix it by making the loop terminate correctly (add the missing increment/break, or handle the negative case with a bounded retry) rather than deleting the anomaly handling. Restart via `start_services.sh` and confirm end-to-end flow by sending a test payload to port 8080 and checking it lands in `data.db`. For the WAL forensics: SQLite WAL frames store raw page images with the literal text data still embedded as bytes — don't try to open the corrupted db normally; instead scan `data.db-wal` for readable INSERT-shaped byte patterns (sensor name + numeric value pairs) with a Python regex over the raw bytes, and bulk-insert everything parseable into a fresh `/home/user/recovered.db` with the specified schema, maximizing row count over perfect fidelity.
