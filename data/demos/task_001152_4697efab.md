Set up with `go mod init bio_sim` inside `/home/user/bio_sim`, then write a small FASTA parser: read the file line by line, treat lines starting with `>` as new headers (trim the `>` and whitespace to get StrainID), and accumulate all following non-header lines (until the next `>` or EOF) into that strain's sequence buffer — sequences can span multiple lines. For GC content, iterate the accumulated sequence, uppercase each character, count 'G'/'C' against total length, ignoring whitespace/newlines entirely (don't count them in the denominator). Implement the Euler integration as a tight, explicit loop: `P := 10.0; for i := 0; i < 100; i++ { P += 0.1 * r * P * (1.0 - P/1000.0) }` using `float64` throughout — do not use an ODE library or adaptive-step solver, since exact numeric reproducibility with a simple loop is what's being checked. Format `P` to exactly 2 decimals with `fmt.Sprintf("%.2f", P)` when writing the CSV row. For `main_test.go`, replicate the exact same loop with `r=0.5` and assert the result is within ±0.1 of the expected value using `t.Errorf` on failure — work out that expected value yourself by running the same Euler recurrence (by hand or with a throwaway script), don't hardcode a guessed number. Verify with `go build`, `go test`, and `go run main.go` against a dummy FASTA before trusting it on the real input file.
