Build the pipeline stage by stage, checking intermediate output at each step rather than writing one monolithic script. Use `ffmpeg` to extract per-frame average luma — e.g. decode to raw grayscale frames and compute per-frame mean brightness (ffmpeg's `signalstats` filter with `showinfo`, or piping raw grayscale frames to a small Python/awk reducer, both work; verify by inspecting the printed values look sinusoidal). Feed that time series (300 samples at 30fps) into a Python one-liner/heredoc that does an FFT (`numpy.fft.rfft`), finds the peak-magnitude frequency bin (excluding the DC/zero-frequency component) and converts bin index to Hz using the known sample rate (30 Hz) and signal length. Compute the trapezoidal integral over the 10-second span with `numpy.trapz(signal, dx=1/30)`. For the Monte Carlo step, write a Bash loop (e.g. `for i in $(seq 1 100)`) that each iteration calls out to Python to add N(0, 5.0) noise to the brightness array, recompute the dominant frequency the same way, collect the 100 frequency estimates, then compute their standard deviation. Assemble all three numbers into the exact JSON shape requested at `/home/user/output.json`, driven end-to-end by `/home/user/run_analysis.sh`.