Implement the interpolation logic carefully in C, then wrap it with shell orchestration. Parse both CSVs (skip header) into arrays of (timestamp, value) pairs — assume timestamps are already sorted per file. For each srv_a timestamp `t`: if `t` is before srv_b's first timestamp or after its last, skip the row entirely; if it exactly matches a srv_b timestamp, use that value directly; otherwise scan (or binary-search) for the bounding pair `(t0,v0)`,`(t1,v1)` in srv_b with `t0 < t < t1` and compute `v0 + (v1-v0)*(t-t0)/(t1-t0)`. Print each surviving row with `%d,%.2f,%.2f\n` for `timestamp,cpu,mem`. Compile with a Makefile-free `gcc -O2 -o /home/user/align_logs /home/user/align_logs.c` (or invoke gcc directly in the pipeline script). Then write `run_pipeline.sh` to `mkdir -p` the output and archive directories, run the compiled binary, and `mv` both source logs into `/home/user/logs/archive/` — run it once manually to produce the initial `aligned.csv` and confirm the values by hand for a couple of rows before trusting it. Finally add the cron entry with `(crontab -l 2>/dev/null; echo "15 * * * * /home/user/run_pipeline.sh") | crontab -` so it doesn't clobber any existing crontab.