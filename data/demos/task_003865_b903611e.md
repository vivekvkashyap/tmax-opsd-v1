Parse the PDB file by reading fixed-width columns per the PDB ATOM record spec: filter lines where the record type is `ATOM` and the atom name field (columns 13-16, i.e. index 12:16 stripped) equals `CA`, then extract the B-factor from columns 61-66 (index 60:66) as a float, preserving file order — don't rely on splitting by whitespace since PDB fields can be tightly packed. Load `signals.csv` with `pandas.read_csv(header=None)` to get matrix X, confirm its row count matches the number of CA atoms extracted. Mean-center X by subtracting the column means, then run `numpy.linalg.svd(X_centered, full_matrices=False)` to get U, S, Vt, and compute principal components as `U * S` (elementwise scaling of U's columns by S, not U @ diag(S) unless you want the same result via matmul). Fit two OLS models with `statsmodels.api.OLS` after `sm.add_constant`: one on PC1 only, one on PC1-3, then apply the classic nested-model F-test formula using each model's `.ssr` (residual sum of squares) and `.df_resid`, and get the p-value from `scipy.stats.f.sf`. Round both results to 4 decimals before writing the JSON.
