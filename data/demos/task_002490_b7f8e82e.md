Reproduce the bug first before fixing it: load `/home/user/data/raw_data.csv` with pandas and inspect `df.dtypes` to confirm the `_id` columns became `float64` because of empty-string NaNs, and identify which columns actually end in `_id` (don't assume — check the header). Edit `/home/user/process.py` so that after reading the CSV, you cast every column whose name ends with `_id` to pandas' nullable `Int64` dtype (e.g. `df[col] = df[col].astype('Int64')`), which preserves missing values as `pd.NA` instead of coercing to float — read with `dtype` on load or convert post-read, but convert before writing to Parquet either way. Run the script and verify with `pd.read_parquet(...).dtypes` that each affected column shows `Int64`, not `float64` or plain `int64`. Finally, write `/home/user/fixed_columns.txt` listing exactly the columns you converted, one per line, sorted alphabetically — derive this list from the columns actually present and matching the `_id` suffix rule rather than hardcoding names by assumption.
