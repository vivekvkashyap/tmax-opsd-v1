Build this as a real signal-processing pipeline, verifying each stage's intermediate output before trusting the next. First set up the venv (`python3 -m venv /home/user/perf_env && source it && pip install numpy scipy`). For extraction, use `ffmpeg` with a crop filter (`crop=100:100:0:0`) on `/app/ui_render_test.mp4` plus a filter that reports per-frame average luma — e.g. `-vf "crop=100:100:0:0,signalstats" -f null -` and parse the `YAVG` values from stderr, or extract raw frames and average pixel values yourself in the script; either way, write one `Frame,Luminance` row per video frame to CSV, and manually inspect the first several rows to confirm the values actually oscillate rather than being constant (a common bug is cropping the wrong region or averaging the wrong channel). For the FFT stage, load the CSV, subtract the mean (or explicitly exclude the 0 Hz bin), run `numpy.fft.rfft`, and use `numpy.fft.rfftfreq(n, d=1/60)` to map bin indices to Hz — take the frequency of the bin with maximum magnitude among the non-DC bins. Chain everything in `run_pipeline.sh` and write only the resulting float to `/home/user/dominant_frequency.txt`. Sanity-check by plotting or printing the top few FFT peaks to confirm the dominant one is stable and not a harmonic/aliasing artifact.