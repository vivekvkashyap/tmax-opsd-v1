Work the task as four independent deliverables, verified separately. First, transcribe `/app/vendor_call.wav`: convert to a clean WAV with `ffmpeg` if needed, then run it through a local ASR tool (e.g. `whisper` CLI or `openai-whisper` python package) and read the transcript text yourself to pull out the disk quota (in MB, convert to bytes with `* 1024 * 1024`) and the systemd `Restart=` policy string exactly as spoken — do not guess these values, derive them from the transcript. Second, write `/home/user/disk_monitor.py` using `os.walk`/`os.path.getsize` to sum bytes under `/home/user/data`, compare strictly greater-than the quota, print exactly `QUOTA_EXCEEDED`/exit 1 or `OK`/exit 0, and `chmod +x` it. Third, write the systemd unit with `ExecStart=` pointing at that script and `Restart=<value-from-audio>` in `[Service]`. Fourth, for `/home/user/ssh_filter.py`, parse the config file directive-by-directive (split on whitespace, normalize case), and reject only if `PubkeyAuthentication no` or `AuthorizedKeysFile /dev/null` appear (allowing whitespace/case variants) — a naive substring match will misfire on comments or quoted paths, so tokenize each non-comment line properly before comparing.
