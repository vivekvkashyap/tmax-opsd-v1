In `/home/user/evaluator.c`, read up to 100 chars from stdin (e.g. `fgets`), then decode the hex string into ASCII by processing it two hex characters at a time (`sscanf(pos, "%2hhx", &byte)` or manual hex-digit parsing) into a buffer — remember to strip any trailing newline from the input line first so it doesn't get treated as part of the hex string. Once decoded, parse the resulting expression by scanning for the first occurrence of `+`, `-`, or `*` to split it into two integers and an operator (`atoi`/`strtol` on each side), then apply the corresponding arithmetic. Print the result with `printf("{\"status\": \"success\", \"result\": %d}\n", result)` matching the exact key names and quoting shown in the spec. In `/home/user/build_and_test.sh`, compile with `gcc /home/user/evaluator.c -o /home/user/evaluator`, then `cat /home/user/payload.txt | /home/user/evaluator > /home/user/test_result.json`. Make the script executable and actually run it so the output file is produced.