First diagnose the hang: read `reconstruct_timeline.py`'s causal-resolution function — it's almost certainly a recursive walk over `parent_event_id` links with no cycle guard, so cyclic references (A's parent is B, B's parent is A) recurse forever. Fix it by adding a `visited`/`in_progress` set so any node already being resolved is treated as having no further-resolvable parent (break the cycle rather than crashing). Second, replace the naive timestamp adjustment with a real constrained-optimization approach: build the DAG (after cycle-breaking) and walk it in a valid topological/causal order, shifting a child's `corrected_ts` forward only enough to be strictly greater than its (already corrected) parent's timestamp, while leaving already-satisfied timestamps untouched — this minimizes total drift versus applying a blanket offset to every event. After regenerating `/home/user/log_pipeline/merged_timeline.json` sorted by corrected timestamp, run `/app/score_timeline` against it and read the printed score; if below 95.0, check whether remaining causality violations or excess drift dominate the penalty and iterate — do not try to read or reverse-engineer the scorer binary itself, treat it as a black box you optimize against.