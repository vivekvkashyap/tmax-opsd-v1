Take the backup before touching anything: `mkdir -p /home/user/archive && tar -czf /home/user/archive/network_diag.tar.gz -C /home/user -network_diag`-style command (i.e. tar the whole `network_diag` directory) run first, so it captures the original broken state. Then fix `diag.cpp`: it's missing the actual socket headers (`<sys/socket.h>`, `<netinet/in.h>`, `<arpa/inet.h>`) needed for `socket()`, `sockaddr_in`, `htons`, `inet_pton`, and `connect` to be declared — add those includes so it compiles. For the timestamp, use `std::gmtime` (already present) and confirm the `strftime` format string exactly matches `YYYY-MM-DD HH:MM:SS`; this alone gives UTC regardless of local `TZ`, but also honor the pipeline's `TZ=UTC` requirement when invoking the binary for consistency/defense-in-depth. Write `/home/user/run_pipeline.sh`: create the build dir, `g++ -o /home/user/build/diag_tool /home/user/network_diag/diag.cpp`, launch `nc -l 127.0.0.1 9999` in the background with output `ALL_CLEAR` sent on connect (e.g. via `echo ALL_CLEAR | nc -l -p 9999` or looping, depending on your `nc` variant — test which flags your `nc` supports first), briefly wait/sleep for the listener to be ready, then run `TZ=UTC /home/user/build/diag_tool > /home/user/pipeline.log`, and clean up the background `nc`. Run the script and check the log line matches the required regex exactly.
