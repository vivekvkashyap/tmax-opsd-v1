First extract and validate `/app/legacy_backup.tar.gz` (`tar tzf` to check integrity, then `tar xzf`) to get `admin_memo.wav` and `storage_audit.log`. The log is UTF-16LE — convert it to UTF-8 first (e.g. `iconv -f UTF-16LE -t UTF-8` from the shell, or handle the conversion inside your Rust program) before treating it as text, otherwise line-splitting and pattern matching will silently produce garbage. In `parser.rs`, split the converted text into `[EVENT]...[/EVENT]` blocks (multi-line records), scan each block for a `Type: AudioMemo` line to identify the correct record among possibly several events, and within that same block parse the `KeepStart:` and `KeepEnd:` numeric fields (seconds, can have decimals) — don't assume they're always the same line offset within the block, parse by field label. Once you have those two float values, use `ffmpeg -i admin_memo.wav -ss <KeepStart> -to <KeepEnd> -c copy /home/user/optimized_memo.wav` (or `sox` trim) to cut precisely that window; verify sample rate and channel layout are preserved. Since grading compares your trimmed audio's waveform (MSE) and duration against a hidden reference trim of the same timestamps, get the start/end values exactly right from the log rather than approximating, and confirm the output WAV plays and has the expected duration before finishing.
