Treat this as three loosely-coupled subsystems that must all work together at the end. (1) Nginx: edit `/home/user/nginx/nginx.conf` to set `listen 8080;` and `root /home/user/restore_out;` inside the server block, run nginx as the current user (not root) with `nginx -c <path>` and use `nginx -s reload` or restart with the same `-c` flag after config edits — check `nginx -t` first to catch syntax errors. (2) SMTP: after the restore completes, open a raw TCP socket from your C code to 127.0.0.1:2525 and speak minimal SMTP by hand (HELO, MAIL FROM, RCPT TO, DATA, subject header, body, `.`, QUIT) — no library needed. (3) Restore tool: parse the WAL-like chunked format (4-byte little-endian length prefix + raw bytes, repeated) and instead of naive small `read()`/`write()` calls per chunk, use a large output buffer or `mmap` for the input file and a single/few large `write()` calls (or `fwrite` with a big buffer) to hit the sub-0.2s target for 50MB — profile with `time` before assuming you're fast enough. Compile with `-O2`/`-O3`. Build and test each piece independently (curl the nginx URL, tail the mail log, time the restore tool) before verifying the full workflow end to end.
