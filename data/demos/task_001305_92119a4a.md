Parse `/home/user/sequences.fasta` (e.g. with Biopython's `SeqIO.parse` or simple header-line splitting on `>`) to get the valid ID set, then filter `/home/user/signals_raw.csv` to only those IDs and drop any others (there's a decoy ID present to test this), grouping and sorting each series by `t_index` with `t = t_index * 0.05`. For each series: run `np.fft.fft`, take magnitude of the FFT bins, find the bin with peak magnitude excluding index 0 (the DC term), and convert its bin frequency to angular frequency `omega = 2*pi*freq`, where `freq = bin_index / (n_samples * dt)` — this is your initial guess. Use `scipy.optimize.curve_fit` (or similar) with model `A*sin(omega*t+phi)+C`, initializing C=mean(v), A=max(v)-mean(v), phi=0, omega=omega_guess, and rerun the fit three times varying only the initial A (0.5x, 1.0x, 1.5x guess), keeping whichever run has lowest sum of squared residuals. Enforce A>0 and phi in [-pi,pi] by normalizing the fitted parameters afterward (flip sign of A and shift phi by pi with wraparound if the fit returns A<0). Round to 3 decimals, write `/home/user/fit_results.csv`, then serve `/home/user` on port 8080 via `python3 -m http.server 8080` in the background.
