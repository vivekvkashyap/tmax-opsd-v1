Start by running `/home/user/run_task.sh` to see the actual failure mode (it wraps execution in a stripped environment via `env -i`, so relative paths and PATH lookups will break) — this tells you exactly what to fix rather than guessing. In `monitor.py`, replace the bare `ssh_manager.sh` call with the absolute path `/home/user/supervisor/ssh_manager.sh`, redirect the error-log write to the absolute path `/home/user/logs/monitor_error.log` instead of a CWD-relative one, and on success add a call to run `/home/user/supervisor/alert.rb` before exiting. In `ssh_manager.sh`, don't attempt a real SSH connection (it would hang) — just append the literal required port-forward command string to `/home/user/logs/ssh_cmd.log` and `exit 0`. Write `alert.rb` as an executable script that writes the exact required email content (headers + blank line + body) to `/home/user/mail/alert.eml` — match the required text byte-for-byte, no extra whitespace. Finally rerun `run_task.sh`, confirm no errors and both output files are correct, then write `DONE` to `/home/user/success.txt`.