First inspect /app/vendored_bio_struct_parser/Makefile's `install` target line by eye — it invokes pip with a misspelled subcommand; fix the typo (it should read `pip install .`) rather than rewriting the target, then run `make install` (or the corrected pip command directly) inside /app/venv so `bio_struct_parser` becomes importable via /app/venv/bin/python. Use that library's `parse_pdb()` to load both PDB files, filter each result to entries with `atom_name == "CA"`, and pull `b_factor` into a numpy array per protein. Compute `scipy.stats.ks_2samp(arrA, arrB)` once and `scipy.stats.gaussian_kde(arrA)` / `gaussian_kde(arrB)` once, keeping both fitted KDE objects and the KS result in memory for the life of the server process so responses are exact and consistent rather than recomputed per request differently. Build the HTTP server (stdlib `http.server` or a minimal framework) bound to 127.0.0.1:8080, with a handler for `/ks_test` returning `{"stat": ..., "pvalue": ...}` from the cached KS result, and a handler parsing `/density/<protein_id>/<b_factor>` from the path, converting the b_factor segment to float, evaluating the corresponding protein's KDE via `kde.evaluate([x])[0]` (or `kde(x)`), and returning `{"density": ...}`. Run the server backgrounded (e.g. `nohup ... &` or inside tmux) so it stays reachable for the verifier's HTTP requests.
