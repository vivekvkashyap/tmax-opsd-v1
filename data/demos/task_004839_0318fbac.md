Build the pipeline in clearly testable stages rather than one monolithic C++ program. First transcribe the audio with a CLI speech-to-text tool (e.g. `whisper /app/field_dictation.wav --output_dir ... --model ...`) and inspect the raw transcript by hand — dictated numbers/names may come out as words or with minor recognition noise, so read it carefully before assuming a fixed template, and save it verbatim to transcript.txt. Design your C++ tokenizer around the transcript's actual recurring pattern (e.g. "Customer N is NAME from CITY, age AGE") using string search/split rather than a rigid fixed-width parser, and use `std::string`/`std::wstring`-safe UTF-8 handling (avoid `char`-by-char ASCII assumptions like `isalpha` on multi-byte sequences) so accented names aren't mangled. Apply the exact same age (18-120) and city-has-no-digits validation to both the transcript records and the legacy CSV rows, logging every rejection with its reason and raw text. Compile with `g++ -std=c++17`, run the pipeline end-to-end, and spot-check clean_merged.csv rows against what you read in the transcript/CSV before considering the F1 threshold met.
