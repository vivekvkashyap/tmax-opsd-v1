Treat this as five separable subsystems and verify each in isolation before wiring them together. (1) Nginx: add a `location /api/ { proxy_pass http://localhost:9000; }` block inside the server block on port 8080, then test with `nginx -t` and reload. (2) CGO hashing: port the exact djb2 arithmetic from hash_go.go into hash.c (`hash = hash*33 + byte`), then wrap it in hash_cgo.go with `import "C"` and cgo build tags `//go:build linux && amd64` (and a complementary `!(linux && amd64)` fallback file); to actually clear the 4x speedup bar, avoid per-call CGO overhead dominating — process the whole byte slice in a single C call, and consider loop unrolling or processing 4/8 bytes at a time in C, then benchmark with `go test -bench=BenchmarkHash` and compute the ratio yourself before trusting it. (3) rules.go: implement EvaluateRule with a small hand-written tokenizer/recursive-descent (or precedence-climbing) evaluator for `==,!=,<,>,&&,||` over string/int comparisons — don't reach for a heavy expression-parsing library. (4) limit.go: use Redis INCR + EXPIRE (or a sliding window) keyed by client IP with a 1-second window and cap of 5, returning HTTP 429 past that. (5) Build with `CGO_ENABLED=1 GOOS=linux GOARCH=amd64 go build`, then start Redis, Nginx, and the Go binary and leave them running.