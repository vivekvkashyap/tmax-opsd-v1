These two parts are independent; do them separately. For scene detection, run something like `ffmpeg -i /app/scene.mp4 -filter:v "select='gt(scene,0.4)',showinfo" -f null - 2>&1` and count how many frame lines the `showinfo` filter reports as passing the selection (grep the stderr output for the `pts_time`/frame markers), then write that single integer to `/home/user/scene_changes.txt` — don't estimate, derive the count from the actual filter output. For the Rust graph tool: `cargo new graph_projector`, read CSV rows from stdin, keep only rows with `interaction_weight > 0.5`, build an undirected adjacency structure keyed by node id, summing weights when the same unordered pair repeats (normalize pair order, e.g. `(min(u,v), max(u,v))`, when aggregating) — and separately track every node id seen in any row (even filtered-out ones) so isolated nodes still appear as size-1 components. Run a BFS/DFS or union-find over the surviving edges to get connected components, sort each component's members ascending, then sort the outer list by descending size and ascending smallest-member id as a tiebreak, and print only the resulting JSON array to stdout (no other output). Build with `cargo build --release` and confirm the binary path matches exactly what's required.
