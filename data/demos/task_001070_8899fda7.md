Build this as a two-stage pipeline and verify each stage independently before wiring them together. First scaffold the Rust crate (`cargo new`), add `rayon` and a CSV/JSON crate to Cargo.toml, and implement the histogram+JSD math exactly as specified: bucket values into 10 fixed-width bins (careful with the value==1.0 edge case landing in the last bin), add the Laplace smoothing constant to every bin before normalizing, then compute JSD pairwise using rayon's parallel iterator (e.g. `par_iter()` over pair indices) so the parallelism requirement is genuinely exercised, not just imported. Sort pairs so sensor_a < sensor_b lexicographically and write valid JSON. Test the Rust binary standalone first (`cargo run --release`) and eyeball distances.json before touching the notebook. For the notebook, write actual executable cells (shell-out cells calling `cargo build --release` and the binary, then a Python cell reading the JSON, finding the min-JSD pair, and writing the sorted pair to best_pair.txt) and execute it with `jupyter nbconvert --execute` so real outputs are embedded, not just cell source. Confirm both `/home/user/output/distances.json` and `/home/user/output/best_pair.txt` are produced by actually running the pipeline end-to-end.