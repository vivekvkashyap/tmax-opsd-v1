First get the vendored PyJWT installable: try `pip install /app/PyJWT` and read the actual error, then inspect `/app/PyJWT/pyproject.toml` for its dependency version constraints — if a dependency (e.g. `cryptography`) has an impossible minimum version pin, relax it to a real satisfiable range (or drop the constraint) so `pip install` can resolve it, and confirm `import jwt` works afterward. Then write the audit script: load `/home/user/logs/jwt_audit.json` and `/home/user/wordlists/weak_secrets.txt`, and for each of the 10,000 tokens try `jwt.decode(token, secret, algorithms=["HS256"])` against every candidate secret, catching the specific signature-verification exception (not a bare `except:` that could hide real bugs) to determine failure vs success. Since this is 10,000 tokens x up to 100 secrets, parallelize with `multiprocessing.Pool` or batch/short-circuit per token (stop trying secrets once one verifies) rather than brute-forcing serially with no early exit. For each token that verifies, record its `kid` from the decoded header (falling back to the JSON wrapper's `id` field only if the task's schema calls for that) alongside the matching secret, and write `/home/user/cracked_jwts.csv` with exactly the `id,secret` columns. Spot-check a handful of results by manually decoding a few tokens with the matched secret before trusting the full run — false positives/negatives directly hurt the F1 score.
