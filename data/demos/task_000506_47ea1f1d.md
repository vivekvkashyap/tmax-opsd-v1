Implement the pipeline as straightforward, literal steps rather than pulling in a heavy ML framework — this is just arithmetic. Load `/home/user/system_metrics.csv` with pandas, compute `cpu_mem_ratio = cpu_util / mem_util` row-wise but explicitly guard the divide-by-zero case (`mem_util == 0` → ratio 0, e.g. via `np.where` or a conditional, not a raw division that produces inf/NaN). Load `/home/user/model_weights.json` and pull the `weights` dict and `bias`, building the linear combination in the exact feature order given (`cpu_util`, `disk_io`, `cpu_mem_ratio`) — order matters only insofar as you multiply each feature by its own named weight, not a positional one, so use the JSON keys directly rather than assuming a list order. Apply the sigmoid `1/(1+exp(-z))` per row, filter to rows with probability strictly greater than 0.8, round the probability to 4 decimal places, and write only the `timestamp` and `probability` columns (in that order, that header) to `/home/user/alerts.csv` via `to_csv(..., index=False)`. Double check the threshold is strict `>` not `>=`.
