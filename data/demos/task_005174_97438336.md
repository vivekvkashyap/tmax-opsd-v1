Diagnose each of the three described failures independently by reading the actual script rather than guessing. For the precision bug, check how the sum/count accumulate — a very common Bash pitfall is piping into a `while read` loop (`cmd | while read ...`), which runs the loop in a subshell so variable updates inside it (`sum`, `count`) are lost once the loop exits; fix this with process substitution (`while read ...; do ...; done < <(cmd)`) or accumulate via a temp file, then verify `sum` is nonzero after the loop before computing the average. For the average itself, use `bc -l` or `echo "scale=4; $sum / $count" | bc -l` (not plain `bc`, which does integer division/truncated scale by default) to get exactly 4 decimal places. For the shared library, add `export LD_LIBRARY_PATH="$(dirname "$0"):$LD_LIBRARY_PATH"` (or use `-Wl,-rpath` at link time, but since you can't recompile, prefer the env-var route) before invoking `./metric_parser`. For the resource leak, add `trap 'kill $(jobs -p) 2>/dev/null; exit' SIGINT SIGTERM` near the top of the script, and test it by starting the script and sending it SIGTERM mid-run to confirm no `sleep` processes remain. Compute the expected average by hand from the CSV to sanity-check your fixed script's output.