Do the three stages separately and validate each before chaining them. First, compile the filter: `cd /home/user/spec-filter-src && make`, producing `./spec-filter`; write the raw `signal` array from `/home/user/raw_spectra.h5` (read via `h5py`) to a plain-text file with one value per line, pipe it through `./spec-filter < input.txt > smoothed.txt`, and confirm the output line count matches the input. Second, treat `/app/instrument_oracle` strictly as a black box — do not disassemble or guess its internal formula. Write the `freq` array from the HDF5 file to a text file, call it as `/app/instrument_oracle --batch input_freqs.txt output_gains.txt`, and read back the gains in the same order as your frequencies. Third, invert the measurement model elementwise: `true_signal = smoothed_signal / gain` (not the raw noisy signal — you want the denoised one divided by gain, since noise was already removed by the filter). Finally write `true_signal` as a float64 (or float32) 1D dataset named exactly `true_signal` into `/home/user/recovered_signal.h5` using `h5py`, matching the original array length, then sanity-check the MSE against your own reasoning about smoothness before finishing.