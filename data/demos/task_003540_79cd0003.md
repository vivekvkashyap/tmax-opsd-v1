Work in the order the task implies: network first, then the parser, then automation. For tap0, use `ip tuntap add dev tap0 mode tap`, `ip addr add 10.0.0.1/24 dev tap0`, `ip link set tap0 up` before starting `/app/start_services.sh`. For the C parser, read binary records from stdin with `fread`/`read` in a loop: 4 bytes big-endian uid (use `ntohl` or manual shifts), 1 byte name_len, then exactly name_len raw bytes for username, then 2 bytes little-endian perms (manual byte assembly, not `ntohs` since that's big-endian). The critical correctness trap is EOF handling: if any of the four reads returns fewer bytes than requested (including 0 bytes at a record boundary, which is a clean stream end, vs. partial reads mid-record, which must print `INVALID` and exit 0) — distinguish "no more data before starting a new record" (just stop, exit cleanly) from "ran out of data partway through a record" (print INVALID). Format perms as exactly 4 lowercase hex digits with `%04x`. Build/test your parser against arbitrary byte streams of varying lengths (including 0, and lengths that cut off mid-field) to catch off-by-one and endianness bugs before wiring up sync.sh with `nc 10.0.0.2 9999 | /home/user/parser | curl -X POST --data-binary @- http://127.0.0.1:8080/sync`.
