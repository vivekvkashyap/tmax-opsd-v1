Transcribe `/app/specs.wav` offline first (e.g. an offline speech-to-text tool such as a local whisper model) to recover the exact primer length, bootstrap iteration count, numpy random seed, and the two percentile values spoken aloud — write these down precisely as heard, don't guess plausible defaults. Then in `/home/user/fast_primer.py`: for every contiguous substring of that length, compute score = (count of G/C)*2 - (count of A/T), take the max as line 1. For line 2, seed numpy with the recovered seed exactly once, then for the recovered number of iterations draw a bootstrap resample (`np.random.choice(scores, size=len(scores), replace=True)`) and record its mean; compute the recovered lower/upper percentiles of those means with `np.percentile`, and print them as `f"{lower:.1f}, {upper:.1f}"`. Match formatting exactly (comma-space separator, one decimal place) since the verifier does bit-exact comparison against the oracle across thousands of fuzzed sequences — the RNG call order and seed placement (seed once, then loop) must mirror what you'd expect from a straightforward reference implementation, or outputs will diverge.
