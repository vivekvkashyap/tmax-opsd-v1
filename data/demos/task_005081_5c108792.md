Fix the environment loading first: add the `PATH` (prepend `/home/user/custom_bin`) and `MONITOR_LOG_DIR=/home/user/logs` exports to `/home/user/.bash_profile`, then make `monitor.sh` `source /home/user/.bash_profile` (or `. ~/.bash_profile`) as its very first executable line so it works even when invoked via `env -i /bin/bash monitor.sh` with a stripped environment — remember `.bash_profile` needs an absolute path since HOME may not resolve as expected under `env -i`. Next fix the config parsing: pipe `services.conf` through a filter like `grep -v '^#' | grep -v '^[[:space:]]*$'` (or an equivalent `awk`/`sed` filter) before the `while read` loop so comment and blank lines never reach the parsing logic. For each remaining line, call `mock_curl <endpoint>`, compare its output to the expected code, and append a line to `/home/user/uptime_report.log` in the exact `NAME | RETURNED | EXPECTED | STATUS` format — build the status as `UP`/`DOWN` via a simple string comparison, not the old broken hardcoded `UNKNOWN`. Verify by creating `/home/user/logs`, chmod'ing `mock_curl` executable, and actually running `env -i /bin/bash /home/user/monitor.sh` to confirm output.
