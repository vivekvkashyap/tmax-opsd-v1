Build `/home/user/clean_data.sh` as a pipeline over `/home/user/raw_feedback.txt` (fields `id|email|timestamp|feedback`). For dedup, process rows in original order and use an associative array (bash) or `awk '!seen[$4]++'` keyed on the feedback field (field 4 when split on `|`) to keep only first occurrences — do this before any transformation so it's exact-text matching. For anonymization, per surviving row compute `printf '%s' "$email" | md5sum` (note: no trailing newline in the hashed input, so use `printf` not `echo`), take the first 8 hex chars, split the email on `@` to get the domain, and rebuild as `<hash8>@<domain>`. For word count, use `wc -w <<< "$feedback"` or `awk '{print NF}'` on just the feedback field. Assemble output lines as `id|anon_email|timestamp|feedback|word_count` into `cleaned_feedback.txt`, being careful that feedback text itself contains no unescaped `|`. Count `N=$(wc -l < raw_feedback.txt)` and `M=$(wc -l < cleaned_feedback.txt)` and emit `report.md` with exactly the three-line template. awk (splitting on `|`, single pass tracking a `seen` array) is well suited to combine steps 1-3 in one script; verify output byte-for-byte against the described transformations before finishing.
