Start by extracting the banned signature: run `tesseract /app/security_bulletin.png stdout` (install tesseract-ocr if missing) and parse out the 23-character value following "BANNED_SIGNATURE:" — do not hardcode a guess. For the scanner, avoid the naive trap of shelling out to `gunzip` and reading the extracted file from disk; instead link against zlib and use `gzopen`/`gzread` (or zlib's inflate API) to stream-decompress the `.gz` argument entirely in memory, then run a substring search for the recovered signature over the decompressed buffer (memmem works well and handles embedded NULs better than strstr). For logging, open `/home/user/artifact_log.txt` with `O_APPEND|O_CREAT`, call `flock(fd, LOCK_EX)` before writing the `[FILENAME] - [CLEAN|REJECTED]\n` line and `flock(fd, LOCK_UN)` right after — this is the part naive solutions skip, causing corrupted interleaved log lines under concurrent invocation. Exit 1 only when the signature is found, 0 otherwise. Compile with `g++ scanner.cpp -o scanner -lz`, then self-test against your own clean/evil `.gz` fixtures (embed the signature at varying offsets, including near buffer boundaries) before relying on the hidden corpus.