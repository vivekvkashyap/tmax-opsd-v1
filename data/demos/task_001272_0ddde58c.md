Do all inspection without extracting archives, since that's the whole point of the security check. Parse `archive_directory` and `report_output` out of `/home/user/dataset_config.ini` (simple `grep`/`cut` on `key=value` lines under `[settings]`). Parse `/home/user/submission_logs.txt`'s blank-line-separated records to build a filename -> expected-SHA256 map (e.g. `awk`/`csplit` on blank lines, or a loop reading `Dataset:`/`SHA256:` lines). For each listed archive: first `sha256sum` it and compare to the expected hash — if mismatched, classify `checksum_failed` and skip further checks for that file. If the hash matches, list internal paths without extracting: use `tar -tzf <file>` for `.tar.gz` or `unzip -Z1 <file>` / `unzip -l <file>` for `.zip`, then check each listed path for a leading `/` or a `../` traversal segment (check path components, not just a substring match, to avoid both false positives and false negatives) — if found, classify `zip_slip_detected`, otherwise `valid_and_safe`. Build the three arrays, sort each alphabetically, and emit the JSON to `report_output` with exactly the three keys in that structure.
