Start by extracting the exact algorithm from the memo image itself — run `tesseract /app/legacy_policy.png -` (or open/view the image) and read off the named MAC algorithm, the literal secret salt string, and the exact data-format template (e.g. how username and role are concatenated/delimited) verbatim; don't guess or approximate any of these three values, since a single wrong character in the salt or a wrong delimiter will make every output diverge from the audit tool. Implement the script using Python's `hmac`/`hashlib` matching the named algorithm exactly, encoding the formatted string as UTF-8 before MACing, and printing only the lowercase hex digest via `hmac.new(...).hexdigest()` with no extra newline, prompts, or debug prints — use `sys.stdout.write` or `print(..., end="")` and take `username`/`role` from `sys.argv[1:3]`. Test it yourself against a couple of hand-picked username/role pairs and confirm output is a clean hex string with no leading/trailing whitespace before considering it done, since the grader will run it many times with random inputs and diff stdout exactly.
