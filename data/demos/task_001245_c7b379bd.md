First recover the three zone-base integers from the video: extract frames with `ffmpeg -i /app/cost_demo.mp4 -vf fps=1 frame_%03d.png` (or similar sampling), then OCR each frame (e.g. `tesseract`) looking for `export ZONE_A_BASE=`, `ZONE_B_BASE=`, `ZONE_C_BASE=` lines, and sanity-check the recovered numbers are plausible integers before appending exact `export ZONE_X_BASE=<value>` lines to `/home/user/.bash_profile`. Since your `lb_proxy.py` will be fuzz-tested against a hidden oracle across thousands of `req_id`/`size_mb` pairs, implement the cost formulas and tie-break order (A before B before C on exact cost ties) exactly as specified — a common mistake is using an unstable sort or comparing floats where integer arithmetic is expected, so keep everything as integers and pick the minimum with an explicit ordered comparison (or a stable sort by cost over the list `[("ZONE_A",...), ("ZONE_B",...), ("ZONE_C",...)]`) rather than a dict (which has no reliable tie order). Read the three env vars inside the script itself (don't hardcode values you observed once). For `setup_proxy.sh`, source `.bash_profile` then write the three `FORWARD PORT...` lines using the sourced variable values, and make it executable.
