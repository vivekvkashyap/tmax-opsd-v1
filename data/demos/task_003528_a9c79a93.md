Chain the steps in order and verify each before moving to the next rather than assuming success. First, in Go, open `/home/user/dropper.elf` with `debug/elf`, iterate `f.Sections`, find the one named `.keydata`, read its raw bytes (`section.Data()`), and parse out the value after `KEY_PREFIX=` (strip any trailing null bytes/whitespace the section may contain) — print it to confirm it looks like a real prefix before using it. Second, brute-force by looping suffixes "00".."99" appended to that prefix and attempting extraction, e.g. shelling out to `unzip -P "$password" -o /home/user/payload.zip -d /home/user` (or `unzip -o -P` per-attempt) inside a loop, stopping at the first suffix that extracts successfully (check the command's exit status, not just that a file appeared) — a naive brute force that only checks "did a file get created" without verifying it's non-corrupted may accept a wrong password on some zip implementations, so always cross-check with the SHA256 step next. After extraction, compute `sha256sum /home/user/payload.bin` and compare against the hash recorded in `/home/user/payload.sha256`; only once they match, write the exact password string (prefix+suffix, no newline — use `printf` not `echo`) to `/home/user/password.txt`. Keep the brute-force driver logic in the Go program or invoked by it, as required.