Start by transcribing `/app/operator_spec.wav` (convert with `ffmpeg` to a clean mono WAV, then run an offline ASR tool such as whisper) to recover the precise mathematical definition of the `@` operator — implement exactly what the transcript specifies in `evaluate_node` inside `eval.c`, giving `@` the same precedence tier as `*` in the parser/evaluator. For the CMake fix, inspect `CMakeLists.txt`: ensure the shared library target is built and that `eval_tool` uses `target_link_libraries(eval_tool PRIVATE mathast)`, and set `CMAKE_INSTALL_RPATH`/`INSTALL_RPATH` (e.g. `$ORIGIN/../lib` or the actual build lib dir) plus `SET(CMAKE_BUILD_WITH_INSTALL_RPATH TRUE)` so the binary finds `libmathast.so` without `LD_LIBRARY_PATH` — verify with `ldd build/eval_tool` and `readelf -d`. For serialization, walk the AST recursively writing a tagged binary format (node type, operator, value, then child pointers/offsets) in `serialize_ast`, and mirror the exact same walk order in `deserialize_ast` to reconstruct nodes — a common bug is mismatched byte order or forgetting to handle leaf (integer literal) nodes distinctly from operator nodes. Use `int64_t` throughout for evaluation. Build with `cmake --build` and test round-trip correctness with several hand-picked expressions including `@` before trusting fuzz input.