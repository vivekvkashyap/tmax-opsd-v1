Write `enforce.py` as a straightforward decode-redact-reencode loop: load the JSON array, for each entry `base64.b64decode(entry['b64_payload']).decode('utf-8')`, run `re.sub(r'AKIA[A-Z0-9]{16}', '[REDACTED]', text)` to replace every match (the regex must match exactly 16 trailing chars — don't be greedy/short, since AWS-style keys are fixed-length and a wrong length will silently miss or over-match adjacent characters), then `base64.b64encode(new_text.encode('utf-8')).decode('ascii')` back into the field, preserving the original `id` and structure. Write the result to `/home/user/clean_logs.json` with `json.dump`. For the isolation step, use something like `unshare --user --net --map-root-user bash -c 'cat /home/user/clean_logs.json > /home/user/final_output.log'` — note plain `--net` without `--map-root-user` (or without also using `-r`) may fail for an unprivileged user, so test the unshare invocation standalone first and confirm the resulting namespace genuinely has no route to the network (e.g. `ip link` inside shows only loopback) before wiring it into the final command. Verify `final_output.log` parses as valid JSON and matches the redacted structure.
