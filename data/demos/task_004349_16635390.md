Run `/app/start_services.sh` first and inspect the seeded `transfers` table (`psql -U etl_user -d etl_db -c "\d transfers"` and sample rows) before writing any code, so your SQL matches the real schema and data distribution. Implement query.py as three independent SQL queries per metric rather than one giant query, since each has different join semantics: (1) degree — `SELECT COUNT(DISTINCT other) FROM (SELECT dst AS other FROM transfers WHERE src=%s UNION SELECT src FROM transfers WHERE dst=%s) t`; (2) mutual_volume — sum of amounts sent by the user to counterparties who have independently sent at least one transfer back to the user (a self-join or EXISTS subquery on the reverse direction, not just symmetric-difference logic); (3) max_rolling_3 — pull all outgoing transfers ordered by `ts` into Python (or use a window function `SUM(amount) OVER (ORDER BY ts ROWS BETWEEN 2 PRECEDING AND CURRENT ROW)`), then take the max of that rolling sum, falling back to summing all rows if fewer than 3 exist, and 0 if none exist. Write indexes.sql with indexes on `src`, `dst`, and `(src, ts)`/`(dst, ts)` to keep each query well under 200ms. Validate by comparing your script's JSON output against `/app/oracle_query` for several manually chosen user IDs (including ones with zero, one, and many transfers) before relying on the fuzz harness.
