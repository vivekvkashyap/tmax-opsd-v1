Two separable pieces: repair the DB, then write the Rust analytics program. For repair, connect with `sqlite3 /home/user/graph.db` and run `REINDEX idx_edges_target;` (or `REINDEX;` for all indexes) to rebuild it from the underlying table data — don't try to hand-edit index pages. For the query, scaffold with `cargo new graph_analyzer`, add `rusqlite` (with the `bundled` feature so it doesn't need system sqlite) to `Cargo.toml`, and write a single parameterized SQL query rather than doing the aggregation in Rust: compute `SUM(weight)` grouped by target joined to `nodes` for type/name, then use `RANK() OVER (PARTITION BY type ORDER BY total_weight DESC)` in a CTE, filter `total_weight >= ?1` in an outer WHERE (or HAVING) referencing the parameter bound from `std::env::args()`, then keep only `rank = 1`, ordered by `type`. Execute with `Statement::query_map`, write results manually as CSV (header first) to avoid dependency setup, then actually run the compiled binary with `15.0` so the CSV file is produced before finishing.