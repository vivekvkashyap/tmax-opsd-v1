Scaffold a Rust binary crate at `/home/user/sim_filter` with the `hdf5` crate as a dependency (ensure `libhdf5-dev` is installed so it links). In `main.rs`, take the file path from `std::env::args()`, open the file with the hdf5 crate, and implement each check as an early-exit: (1) try to open dataset `/observational/metadata` — if the open fails, `std::process::exit(1)`; (2) read `/mc_results/energy` as a 1D float array — if reading fails or any value is negative, exit(1); (3) read `/mc_results/amplitude` similarly and compute its max — if the dataset is missing, or the max is not strictly less than the threshold, exit(1). Only exit(0) if every dataset was present and every constraint held. For the threshold, do not guess a number — actually transcribe `/app/hydrophone_metadata.wav` (e.g. via `ffmpeg`-based extraction plus a local speech-to-text tool, or manual careful listening) to recover the exact spoken threshold value, and use that recovered number as your constant. Build with `cargo build --release` so the binary lands at `target/release/sim_filter`, then iterate the check by running it against every file in `/app/sim_corpus/clean/` (expect exit 0) and `/app/sim_corpus/evil/` (expect nonzero exit) before finishing, fixing any dataset-path or comparison-boundary mistakes (note "strictly less than" — a value exactly equal to the threshold must be rejected).