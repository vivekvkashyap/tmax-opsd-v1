Structure `clean.go` as a clear pipeline of independent stages so each is easy to get right. Read the raw CSV line-by-line (skip/keep header carefully), and dedupe by hashing each row's raw string with `crypto/sha256` and tracking seen hashes in a `map[string]bool` — hash the exact row text (excluding the newline), not a re-serialized version, since formatting differences would break the "identical row string" definition. For reshaping, iterate parsed rows and emit three logical (timestamp, sensor_id, temp) records per row for loc1/loc2/loc3. For bucketing, parse timestamps with `time.Parse(time.RFC3339, ...)` and truncate to the hour with `t.Truncate(time.Hour)` (do this in UTC to match RFC3339 Z-suffixed timestamps), then accumulate sum/count per (bucket, sensor_id) key in a map, compute the average, and format with `%.2f`. Finally sort the aggregated keys by bucket ascending then sensor_id ascending before writing CSV rows with the exact header `bucket,sensor_id,avg_temp`. Run the program and diff the output against expectations only in your head — the grader does its own diff, so focus on getting formatting (RFC3339 bucket, 2-decimal temp) exactly right.