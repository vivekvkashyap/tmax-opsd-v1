Set up the environment first (`python3 -m venv /home/user/venv`, activate it, `pip install numpy scipy h5py`), then diagnose why the current integrator diverges: it's explicit (forward) Euler with a fixed step on a stiff ODE (large `r`), which is unstable for stiff systems — the fix is to replace it with `scipy.integrate.solve_ivp`, passing an implicit/stiff-capable method such as `method='BDF'`, `'Radau'`, or `'LSODA'`. Read `y0`, `r`, `m` from `/home/user/params.h5` via `h5py` (scalar datasets, so index with `[()]`), define the RHS function `dy/dt = r*y*(1-y) - m*y`, and call `solve_ivp` with `t_span=(0,10)` and `t_eval=np.linspace(0,10,1000)` so you get exactly 1000 output points matching the time grid. Save both the `t` array and the resulting `y` array as `time` and `frequency` datasets in a new `/home/user/results.h5` file via `h5py`. Sanity-check your result before finishing: this logistic-with-decay ODE has a known steady state `y* = 1 - m/r`, so the final `frequency` value should converge close to that analytical limit and contain no NaNs — if it doesn't, the solver tolerance or method choice needs adjustment.