Work through the four TODOs in `analyze.py` methodically. First install missing packages (`pip install pandas numpy scipy matplotlib`). For aggregation, replace the hardcoded fallback with a real `df.groupby('variant')` that computes `converted.sum()` (successes) and group size (trials) for each variant — read the values off the actual grouped result rather than assuming numbers. For the blank-plot bug: in a headless environment you must set the matplotlib backend to a non-interactive one (`matplotlib.use('Agg')`) before importing `pyplot`, and critically call `plt.savefig(...)` before any `plt.show()`/`plt.close()` — `show()` in a non-interactive backend can clear or no-op the figure, so drop it or reorder so save happens first. For the Bayesian step, form Beta posteriors from a Beta(1,1) prior plus the aggregated successes/failures for each variant, call `np.random.seed(42)` immediately before drawing exactly 1,000,000 samples from each posterior with `np.random.beta`, then compute the fraction of samples where B's draw exceeds A's draw as the Monte Carlo probability estimate, round to 4 decimals, and write it to `/home/user/prob_B_better.txt`. Verify the PNG file size is non-trivial (well above a blank-canvas byte count) after running.