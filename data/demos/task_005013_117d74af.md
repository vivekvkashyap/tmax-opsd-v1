Write the C program to stream-parse `edges.csv` line by line with `fscanf(fp, "%d,%d", &src, &dst)`, and maintain per-node degree counts in a hash map or a dynamically-growing array/table keyed by node id (since ids aren't guaranteed small/contiguous, don't assume a fixed-size array is safe — size it to the max id seen, or use a simple open-addressing/linked hash table). Increment the count for both `src` and `dst` on every edge (in-degree and out-degree both add to the same total-degree counter for that node). After reading all edges, select the top 3 by degree descending, breaking ties by smaller `node_id` first — implement this as a comparator in your sort (or a manual 3-pass max-scan) rather than relying on stable sort order alone. Emit the result by hand-building the JSON array string (`fprintf`) in the exact `{"node_id": X, "degree": Y}` shape per element so it's valid, parseable JSON. Compile with `gcc analyze_graph.c -o analyze_graph`, run it against the real CSV path, and check the output file with `jq` to confirm it's syntactically valid before finishing.
