Structure this as a CMake C++/OpenCV project and validate each stage against intermediate output before chaining them. Per frame: threshold to isolate the bright projectile against the dark background, then compute the centroid as the mean (x,y) of foreground pixel coordinates (or via `cv::moments` on the thresholded mask) rather than a bounding-box center, since a moments-based centroid is much closer to the true physical center used for the hidden ground truth. Apply your smoothing (e.g. a size-5 moving average) causally or centered consistently across the whole trajectory, and think carefully about how you handle the first/last few frames where a full window isn't available — dropping frames or handling edges inconsistently is a common source of MSE blowup near the boundaries. Write the CSV with header `frame,x,y` and 0-indexed frames exactly matching the number of frames you processed, then write experiment_log.json with the same `num_frames` count. Before finishing, visually or numerically sanity-check that your x/y trajectory looks like a smooth parabola matching the projectile's expected motion, not noisy or offset.
