Build this as a strict shell pipeline, verifying each stage's output before chaining to the next. First get a working CLI transcriber: compile whisper.cpp from source and fetch the tiny.en model, then run it on `/app/query.wav`; normalize the transcript yourself with `tr '[:upper:]' '[:lower:]'` and `sed`/`tr` to strip punctuation — inspect the raw transcript text before trusting it, since whisper output often includes timestamps/brackets you must strip. Second, for each normalized word, `grep`/`awk` its row out of `/app/vocab.csv` and accumulate the five embedding dimensions with `awk` (sum only over words actually found; silently skip missing ones). Third, for every row in `/app/documents.csv`, compute cosine similarity (dot product over product of L2 norms) with `awk`, again being careful with floating-point formatting (`printf "%.4f"`). Sort numerically descending (`sort -t, -k2,2 -nr`), take the first 10 with `head`, and write exactly `doc_id,similarity_score` lines with no header to `/home/user/results.csv`. Test incrementally — verify the transcription and vocab lookups look sane before trusting the final similarity ranking.
