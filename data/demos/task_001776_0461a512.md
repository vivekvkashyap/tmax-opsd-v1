Two independent Rust/SQLite fixes are needed in `src/main.rs`. For the deadlock: SQLite's default rollback journal serializes readers behind writers, so a reader thread can block indefinitely while a writer transaction is open; set `PRAGMA journal_mode = WAL;` right after opening each connection (e.g. `conn.pragma_update(None, "journal_mode", "WAL")?` in `get_db()`) so readers and writers can proceed concurrently. For the shortest-path query: implement it as a recursive CTE that walks the directed `citations` edges from `source_id`, tracking hop count, and stops at the first row where `target_id` matches the destination (`WITH RECURSIVE path(id, depth) AS (SELECT source_id, 0 UNION ALL SELECT c.target_id, p.depth+1 FROM citations c JOIN path p ON c.source_id = p.id) SELECT depth FROM path WHERE id = ? ORDER BY depth LIMIT 1`, adapted to `rusqlite` parameter binding) — guard against infinite recursion on cyclic graphs if needed. Also create the index `idx_citations_source_target` on `citations(source_id, target_id)` via a `CREATE INDEX` statement (either in Rust startup code or directly with `sqlite3`) to keep the recursive join fast. Run both `cargo run` invocations as specified and redirect stdout to the two result files.
