Implement this as an in-memory graph join in C, structured in clear passes rather than one big scan. Pass 1: read `/home/user/graph.csv` (Subject,Predicate,Object; up to 15-char alphanumeric fields, no header) into memory, e.g. an array of triple structs — use `fgets`/`sscanf` or manual comma splitting, sized generously since it must handle several thousand edges. Pass 2: build a mapping from each `?category` to the SET of distinct `?item`s it INCLUDES (only rows where Predicate=="INCLUDES"), deduplicating items per category as you insert (a simple sorted array or hash set per category works). Pass 3: for each row where Predicate=="VIEWS" (giving `user -> category`), union that category's item-set into a per-user set of distinct items, again deduplicating across multiple categories the same user views — this is the step naive implementations get wrong by double-counting items reachable via more than one viewed category. Pass 4: for each user with a non-empty item set, output `User,Count`; skip users whose distinct-item count is 0. Sort the final output alphabetically by user (e.g. `qsort` on username). Compile with `gcc -Wall`, run it, and redirect stdout to `/home/user/recommendations.csv`. Sanity-check by manually tracing one or two users' expected counts against the raw CSV before finishing.