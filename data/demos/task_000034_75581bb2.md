First diagnose the vendored build failure: run `make -C /app/stats-tools` and read the actual linker/compiler error rather than guessing — a C program using `<math.h>` functions like `sqrt`/`log` typically fails to link with "undefined reference" unless `-lm` is added to the gcc command in the Makefile; edit the Makefile to append `-lm` and rebuild until `mcmc_estimate` compiles cleanly. Next write `/home/user/analyze.sh` as a pure bash script using `awk` for floating point math (bash itself can't do decimals): compute `Chi2 = (O1-E1)^2/E1 + (O2-E2)^2/E2` inside a single `awk` call to avoid intermediate precision loss, and handle the zero-expectation and >=4.00 branch conditions with `awk` numeric comparisons rather than shell integer comparisons. Format the ACCEPT value to exactly two decimals with `printf "%.2f"` inside awk. For REJECT, invoke the fixed `/app/stats-tools/mcmc_estimate` binary with the four arguments and pass its raw stdout through unchanged after the "REJECT: " prefix. Since grading fuzzes many integer inputs and checks bit-exact output against a hidden oracle, test your script against several boundary cases (E1 or E2 = 0, Chi2 exactly at 4.00, negative differences) to confirm formatting and branch logic match expectations exactly.