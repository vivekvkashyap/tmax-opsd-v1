Load both CSVs into dictionaries: `nodes[node_id] = (base_cost, node_type)` and an adjacency list `children[parent_id] = [(child_id, multiplier), ...]` built from edges.csv. Compute cost with a memoized recursive function: `cost(node) = base_cost[node] + sum(cost(child) * multiplier for each (child, multiplier) in children[node])`, with the recursion base case being nodes with no children (their cost is just their own base_cost). Compute max depth with a separate DFS/BFS from `PRODUCT_A` that tracks edge-count-so-far, updating a running maximum only when it reaches a node whose `node_type` is `RAW_MATERIAL` — nodes with no children that aren't `RAW_MATERIAL` should not affect the max. Since it's a DAG (not necessarily a tree), a node could be reached via multiple parents with different multipliers, so don't cache blindly across different call contexts unless the recursion is purely on node identity (cost per node value is context-independent here, but paths for depth are not — enumerate all paths for depth, not just one). Write `{"total_cost": <float>, "max_depth": <int>}` to the JSON file and double check against your own hand trace of a small subgraph.
