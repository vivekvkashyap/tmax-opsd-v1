Do the assembly piece first in isolation since it's the highest-risk part. Write custom.s implementing compute_hash per the System V AMD64 ABI: the first integer argument arrives in the lower 32 bits of `edi`; XOR it with 0x5A using `xor eax, edi` after moving edi to eax (or `xor edi, 0x5A` then move), add 12, and return via `eax` (`ret`) — declare the symbol `.globl compute_hash` and use a `.text` section. Assemble and link it standalone first: `as custom.s -o custom.o && gcc -nostdlib -fPIC -shared -o libcustom.so custom.o`, then write a tiny ctypes smoke test calling compute_hash(100) and check it returns 66 by hand before trusting the pipeline — this isolates ABI mistakes (wrong register, forgetting sign/zero extension) from downstream Python/SQLite issues. Write migrate.sh as an idempotent `ALTER TABLE metrics ADD COLUMN hash_val INTEGER;` guarded so re-running doesn't error if the column already exists (e.g. check `PRAGMA table_info` first, or ignore the "duplicate column" error). Write build_and_run.sh to compile the .so, run migrate.sh, run processor.py, then dump the table exactly as specified. Chmod all scripts executable, then run build_and_run.sh end to end and compare final_dump.txt row by row against your own hand-computed XOR/add values before considering it done.
