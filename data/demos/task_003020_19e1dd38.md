Start by inspecting the schema with `sqlite3 /home/user/architecture.db ".schema"` to learn the actual table/column names for services and dependencies (do not assume names). In Python, use `sqlite3.connect` and always pass the service name as a bound parameter (`?` placeholder with a tuple), never via f-string/`%`/`.format` into the SQL text — that's the injection requirement. Implement the blast-radius search as a breadth-first traversal capped at depth 2: first query all rows where the target is the callee and the dependency is critical to get depth-1 callers; then, for each depth-1 caller, query rows where it is the callee and the dependency is critical to get depth-2 callers. Union the two sets, dedupe, drop the target itself if it reappears, sort alphabetically, and `json.dump` into `{"target": ..., "impacted": [...]}` at `/home/user/impact.json`. Finally actually invoke `python3 /home/user/analyze_impact.py "PaymentGateway"` so the output file exists — don't just leave the script unexecuted.
