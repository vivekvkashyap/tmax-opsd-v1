Read `/home/user/logs/crash.log` first to confirm the failure is a `RecursionError` in `fetch_data_with_retry` — the bug is that the retry branch recurses without decrementing the `retries` counter, so fix it to decrement (e.g., `retries=retries-1`) on each retry attempt so the base case is actually reachable. For `compute_standard_deviation`, recognize the bug as catastrophic cancellation from the naive "sum of squares minus square of sum" variance formula on large, closely-spaced floats — replace it with a numerically stable approach: either a two-pass algorithm (subtract the mean first, then sum squared deviations) or Welford's online algorithm, and use sample variance (dividing by n-1) since that's what downstream expects. After both fixes, run `python3 /home/user/service/run_production.py` without touching that file, and inspect `/home/user/metrics_output.json` to confirm it was written with valid JSON and no exceptions were printed — if the runner reports an error instead of completing, one of the two bugs is still present.
