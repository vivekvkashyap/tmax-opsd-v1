Work bottom-up through the stack since each layer depends on the one below compiling/running correctly. First fix `/app/c_src/libocr.c` and its Makefile: add missing includes, correct pointer/dereference bugs, and make sure the Makefile compiles with `-fPIC -shared` into `libocr.so` (check the target name, compiler flags, and that the rule actually links a shared object rather than a static one). Confirm it builds standalone with `make` before touching Rust. Next fix `src/main.rs`: FFI calls returning a C string (`*const c_char`) must be converted via `CStr::from_ptr(...).to_str()`/`to_owned()` into an owned Rust `String` before the C-side pointer's lifetime ends or before it's freed — the compile errors are almost certainly from trying to hold a borrowed `&str` tied to a raw pointer past its valid scope; own the data immediately. Run `cargo build` and confirm the binary prints OCR text for a test image. Then write `server.sh` using `socat TCP-LISTEN:8081,reuseaddr,fork EXEC:...` (or a `nc`-based loop) that, per connection, runs the Rust binary against `/app/test_receipt.png`, wraps stdout in a proper `HTTP/1.1 200 OK` response with `Content-Length`, and writes it to the socket. Finally write `nginx.conf` proxying `:8080` to `127.0.0.1:8081`, start nginx with `-c` pointing at it, and curl `127.0.0.1:8080/` end-to-end to confirm the OCR text round-trips through both hops before finishing.