Fix the build system and code defects methodically before chasing behavioral equivalence with the reference implementation. For the Makefile, ensure it compiles both router.c and parser.c to .o files, includes `-I.` so local headers resolve, and links all resulting .o files (not just one) into router_bin. For the header circular-dependency, add `#ifndef/#define` include guards to both router.h and parser.h, and where one header only needs a pointer/type from the other, use a forward declaration instead of a full include. For the parser.c segfault, inspect the copy loop carefully: an index variable used to write into `dest[i++]` must be initialized to 0 before the loop — an uninitialized index is the classic cause of that kind of memory corruption. Separately, derive CONSTRAINT_MODULUS by extracting frames from /app/signal.mp4 (e.g. `ffmpeg -i signal.mp4 frame_%03d.png`) and programmatically counting frames whose pixels are pure red versus black (average pixel color per frame), then compute `(count * 17) % 255` and place that literal value in parser.h. Once it builds, stress-test router_bin yourself with a wide variety of generated URL strings (including edge cases like empty segments, long query strings, and boundary-length parameters) and manually verify the routed ID and parsed variables look correct for each, before considering it done.
