Treat the audio file as the actual spec — do not guess the procedure, extract it. Install a speech-to-text tool (e.g. `pip install openai-whisper` plus `ffmpeg`, or `SpeechRecognition`) and transcribe `/app/calibration_memo.wav`; if the transcript is ambiguous, re-run transcription or convert/normalize the audio (resample, boost volume) and listen again to nail down exact numbers and wording — spoken numeric constants and index formulas are easy for STT to mangle, so cross-check them carefully before hardcoding anything. Once you have the precise procedure (a per-element windowing/weighting function of index i and total count N, a summation step, a scaling constant, and an output format), implement it in `/home/user/process_signal.py`: read whitespace/newline-separated floats from stdin, apply the transformation exactly as described, and use `math.fsum` (not built-in `sum` or a running accumulator) for the final reduction — this is the whole point, since naive summation order varies across architectures and breaks bit-exact reproducibility. Handle edge cases explicitly (empty input, single-element input) since a formula involving `N-1` will divide by zero for N=1. Finally print using the exact decimal-place formatting stated in the memo — the grader compares output byte-for-byte against a hidden oracle across many random-length fuzz inputs.
