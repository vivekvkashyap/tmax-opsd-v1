Fix the build first, then design validation rules independently — do not try to reverse-engineer exact behavior from the reference oracle binary beyond using it as a black-box sanity check on ambiguous edge cases; your classification logic must be self-contained since it's what's graded. In build.rs, use the `cc` crate to compile and statically link the vendored C source: `cc::Build::new().file("src/libexpr.c").compile("expr_c");` (add `cc` under `[build-dependencies]` in Cargo.toml if missing). For `is_safe_expression`, write a simple recursive-descent tokenizer/parser (or a paren-depth counter plus a token-adjacency scan) that tracks: (a) current parenthesis nesting depth, rejecting if it exceeds 15; (b) scans for a `/` token followed (possibly through whitespace/parens) by a literal `0`, rejecting syntactic division-by-zero; (c) checks that no two operator tokens appear consecutively without an operand between them. Wire this into main.rs so it exits 0 for safe, 1 for malicious. Before writing ci_check.sh, manually run your compiled binary against a few files from both `/app/corpora/clean/` and `/app/corpora/evil/` to catch misclassifications early, then write ci_check.sh to loop over both directories, print PASS/FAIL per file, and exit 0 only if every file's result matches its corpus's expectation.