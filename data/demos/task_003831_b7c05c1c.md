Before touching the code, work out the intended physics of adaptive step control: error estimate should shrink the step when it's above tolerance and grow it when comfortably below — so the step-scaling factor must move inversely with the error, not proportionally to it. Read `/home/user/integrator.go` looking specifically at the line recomputing `dt` after the error check; if it scales `dt` up when `errEst` is large (proportional to `errEst/tol`), that's backwards and explains the divergence on this stiff equation — invert the ratio so `dt` scales by `sqrt(tol/errEst)` instead. After fixing, compile (`go build`) and run so `/home/user/dataset.csv` is regenerated fresh — don't reuse any stale partial output from the broken version. Write `plot.gp` with `set terminal png`, `set output '/home/user/plot.png'`, `plot 'dataset.csv' using 1:2 with lines` (mind the CSV has a header row — use `skip 1` or equivalent), and run `gnuplot /home/user/plot.gp`. Finally pull the last row's y-value from `dataset.csv` into `final_y.txt` — for this stiff decay you should expect a value extremely close to zero, not NaN/Inf, which is your signal the fix actually worked.
