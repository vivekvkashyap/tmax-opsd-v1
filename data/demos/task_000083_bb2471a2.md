Build and validate the `verify` subcommand first against the provided corpora before attempting the daemon. Scaffold with `cargo new /home/user/backup_processor` and add crates for archive handling (zip, tar/flate2), sha2, and CLI parsing. Implement `verify <dir>`: recursively walk the directory, temporarily extract any nested `.zip`/`.tar.gz` archives found so their contents are also inspected, parse `backup.log` records (BEGIN FILE/CHECKSUM/END FILE blocks), and for each listed file confirm it exists and its SHA256 matches; also reject if any file (including inside nested archives) has a `.sh`/`.elf` extension or the executable bit set. Print `ACCEPT`/exit 0 or `REJECT`/exit 1 accordingly. Run it manually against every directory in `/app/corpus/clean` and `/app/corpus/evil` and fix mismatches before moving on — this is fuzz/corpus-tested so partial correctness fails. Then implement `daemon <spool> <dest>` using the `notify` crate to watch for new `.tar.gz` files, extract, call your verify logic internally, and on success read `ID.txt` for backup_id, check/set Redis membership in `processed_backups`, POST to `http://localhost:8080/register_backup`, and move the archive to `<dest_dir>`; on failure just delete it. Test end-to-end via `/app/start_services.sh` plus dropping test archives into the spool directory and observing the resulting state.