Build this as a clear multi-stage pipeline (pandas or plain Python is fine) rather than one big script: (1) load both sources, tag sensor A rows with id `A` and B rows with `B`, normalize to a common (sensor_id, timestamp, temperature) shape; (2) deduplicate by grouping on (sensor_id, timestamp) and keeping the row with the max temperature — do this before resampling, not after; (3) build the fixed 60-second timestamp grid explicitly (don't rely on pandas' resample defaults, which can silently interpolate or use the wrong fill direction) and forward-fill each sensor's series independently along that grid — the first timestamp is guaranteed present so ffill has a seed; (4) compute the 3-period SMA per sensor on the resampled grid using a simple rolling window over exactly the current and two prior points, leaving the first two rows of each sensor's series blank (empty string, not 0 or NaN literal) since there's no full window yet; (5) sort by timestamp then sensor_id, format both float columns to exactly 2 decimals with a format string (not just `round()`, which can leave trailing-zero mismatches), and write the CSV with the exact header. Double check off-by-one boundaries at the SMA window edges and at the last grid timestamp.
