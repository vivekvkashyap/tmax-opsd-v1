Open the file in binary mode and decode with `errors='replace'` (e.g. `open(path, 'rb').read().decode('utf-8', errors='replace')`), then feed the decoded text into `csv.DictReader` via `io.StringIO` — never use `readlines()` or naive line-splitting, since the `message` field contains embedded newlines that only a proper CSV parser (which respects quoted fields) will handle correctly. Deduplicate by iterating rows in file order and keeping only the first occurrence per `tx_id` (a dict keyed by tx_id, only inserting if not already present, preserves "first wins"). Drop rows with `response_time_ms < 0` after casting to int/float. For error codes, compile a regex like `ERR-\d{3}` and use `.search()` (not `.findall()`) to grab only the first match per message, storing `None` when there's no match. Parse the ISO timestamp, truncate to the hour by zeroing minutes/seconds (keep the `Z` suffix format), then group by that hour bucket with a dict of accumulators, computing count, unique user set size, mean response time rounded to 2 decimals, and a Counter of non-null error codes. Sort the final list by `hour_bucket` before writing JSON.