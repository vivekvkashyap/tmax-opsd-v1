Extract the calibration values first with `tesseract /app/calibration.png stdout` and parse the `X_SCALE:`/`Y_SCALE:` lines from the OCR text rather than assuming any value. Write the C++ binary-archive parser using `ifstream` with `ios::binary`: read and validate the 4-byte `ARTF` magic, then a little-endian `uint32_t` count, then loop that many times reading a 1-byte filename length, that many filename bytes, a little-endian `uint32_t` payload size, and that many payload bytes as the raw GCode text — use `read()` with explicit sizes, not `>>`, to avoid corrupting binary data. For each GCode payload, scan lines starting with `G0`/`G1`, parse the `X`, `Y`, `Z`, `E` tokens present on each line (carrying forward unspecified axes from the previous state if the format implies modal coordinates, or track only tokens explicitly given per your reading of the payloads), multiply X and Y by the OCR'd scales *before* updating running min/max, and track the maximum absolute E separately from bounding-box math. Compute volume as the product of the three axis ranges. Emit the CSV sorted alphabetically by filename with values formatted to exactly 4 decimal places (e.g. via `std::fixed << std::setprecision(4)`), matching the required header exactly.