Treat `/app/anomaly_scorer` as an opaque scorer you must feed correctly, not reverse-engineer — probe it first with `echo "1,2.0000,0.0000" | /app/anomaly_scorer` to confirm the exact input/output contract before building the pipeline. In your script, load `/app/server_logs.csv` with pandas, bucket rows into fixed 300-second windows anchored at the minimum timestamp (`bucket = (timestamp - min_ts) // 300`), and per bucket compute `request_count`, the 95th percentile of `latency_ms` (`np.percentile(..., 95)`), and the error rate (`status_code >= 500` mean). Format exactly as the binary expects — integer request_count, floats to 4 decimal places — then pipe rows to the binary via subprocess and parse back one score per bucket in the same order sent, since a common bug is misaligning scores to buckets. Filter buckets where the score is strictly greater than 0.85, and write the final CSV sorted by timestamp with the exact required header.