Work in stages and test each stage independently before chaining. For `translator.py`, load the JSON with `json.load`, then iterate over the mapping generating a `dataclasses`-style Python file (map `string`→`str`, `int`→`int`) and a C header with `typedef struct { ... } Name;` (map `string`→`char*`, `int`→`int`); write both files and manually inspect them for syntax validity. For `builder.py`, parse `build.json`, build an adjacency list of task→deps, and implement a proper topological sort (e.g., repeatedly pick from the set of tasks whose deps are already satisfied, and when multiple candidates are ready simultaneously, choose the alphabetically smallest one — this tie-break matters and a naive DFS-based topo sort will get it wrong). Execute each task's `cmd` via `subprocess.run(..., shell=True)` in that resolved order, appending the task name to the log file immediately after each command completes (not batched at the end, in case of partial failure). Build the C program with `gcc` as one of the tasks, not manually. Finally, after running the orchestrator, start `python3 -m http.server 8080 --directory /path/to/polybuild` in the background and confirm with `curl localhost:8080/execution_log.txt` that it serves the expected directory content.