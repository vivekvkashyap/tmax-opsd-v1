Structure the C program in clear stages and verify each against the given sequences before trusting the final output. Stage 1: for each line in sequences.txt, slide a window of 2 over the characters (positions i and i+1 for i=0..len-2) and increment a 16-entry frequency table indexed by mapping each nucleotide pair to 0-15 (e.g. via a helper that converts 'A'/'C'/'G'/'T' to 0/1/2/3 then index = first*4+second). Stage 2: for every pair of sequences (including self-pairs, giving distance 0 on the diagonal), sum the absolute differences across all 16 dimensions to get the Manhattan distance — store this in an N×N int matrix and print it as comma-separated integers, one row per line, no header. Stage 3: find the max value anywhere in the matrix, then for the PGM, write the header `P2`, then `<N> <N>`, then `255`, then each pixel computed as `(distance * 255) / max_distance` using integer division (truncating, not rounding) in row-major order matching the distance matrix. Sanity-check by hand-computing one or two distances from sequences.txt yourself and comparing against your program's matrix.csv output before finishing.