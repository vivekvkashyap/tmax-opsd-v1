Since the oracle is stated to implement an exact linear map `y = Wx` with no bias, recover `W` by algebraic probing rather than statistics: call `/app/sensor_encoder` with the standard basis vectors (all zeros except a single 1 in position `i`, for each `i` from 0 to 9) — each such call directly returns column `i` of `W` (since `W @ e_i` equals that column). Ten calls fully determine the 5x10 matrix; use more probes (e.g. scaled or summed basis vectors) only to cross-check for rounding artifacts. Reconstruct all arithmetic in float32 (e.g. `numpy.float32` dtype throughout, not float64) since the task stresses matching the legacy system's single-precision behavior — mismatched precision is the likely naive-attempt failure mode given the tight `1e-10` MSE threshold. Write `/home/user/reconstructed_model.py` to parse 10 CLI float args, cast to float32, multiply by the recovered float32 matrix, and print 5 space-separated floats formatted like the oracle's `%.6f` style. Validate by comparing your script's output to the oracle's output on many random vectors before finishing; the candidate-similarity dataset cleaning step is separate exploratory work, not required for the graded script itself.