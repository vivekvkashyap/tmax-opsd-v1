Build this as a clear pipeline inside your POST /process handler: (1) parse the JSON array and sort by `timestamp` ascending; (2) for deduplication, shell out to `/app/config_hasher <config_id>` for each record (cache repeated calls) to get its hash, then walk the sorted list keeping only the first record seen per hash value and dropping subsequent ones with the same hash — do not dedupe on `config_id` directly, the hash is the required key; (3) for imputation, treat `cpu` and `mem` as two independent series over the deduplicated, time-sorted records: for each run of `null`s bounded by known values, linearly interpolate against the timestamp deltas (not just index position), and separately handle leading nulls with backward-fill and trailing nulls with forward-fill; (4) compute the windowed moving average with a shrinking window at the start (index 0 is itself, index 1 averages 0-1, index >=2 averages the trailing 3) — implement this explicitly rather than using a library that pads at the edges. Round all four output floats to 2 decimals with normal rounding. Run the HTTP server (stdlib `http.server` or Flask) on 127.0.0.1:8080 in the background and test with `curl -X POST` against a small crafted payload including nulls before considering it done.
