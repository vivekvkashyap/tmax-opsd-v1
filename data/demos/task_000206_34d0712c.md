Chain the pipeline as discrete, testable stages rather than one monolithic script. Stage 1: convert encoding with `iconv -f UTF-16LE -t UTF-8` on experiment_01.gcode — verify with `file` that the source really is UTF-16LE before assuming so. Stage 2: filter with `grep -E '^(G0 |G1 )'` (anchored, including the trailing space) to drop comments/M-codes/other G-codes, piping the iconv output directly in. Stage 3: write transform.c to read the two offset floats from config.txt (first line X offset, second line Y offset), then read filtered lines from stdin, locate 'X' and 'Y' markers with strchr/sscanf, parse the following float, add the corresponding offset, and reprint the line with the coordinate replaced — use sprintf/printf with "%.1f" to preserve exactly one decimal place, and copy through any trailing tokens (like F3000) untouched by reconstructing the line from parsed pieces rather than doing blind string replacement. Compile with gcc to /home/user/transform. Finally chain it all: `iconv ... | grep ... | ./transform > /home/user/dataset/normalized_experiment_01.gcode`. Test on a small hand-traced example first to confirm your X/Y parsing and offset arithmetic before trusting the full file.
