These are two independent subtasks — solve them separately. For the video, open `/app/deploy_recording.mp4` with OpenCV, iterate every frame, compute the mean R, G, B across all pixels of that frame (careful: OpenCV loads frames as BGR, so map channels correctly), and count a frame as "blue" only if it satisfies mean-R<50 AND mean-G<50 AND mean-B>200 simultaneously; write the integer count to `/home/user/blue_frames.txt` with no extra whitespace/newline issues. For `dedup.py`, since it will be fuzzed for byte-exact stdout/stderr match against a hidden oracle, be precise about ordering and formatting: parse each stdin line as JSON (catch `json.JSONDecodeError` specifically and emit the exact `ERROR: Invalid JSON` message before continuing); normalize `config` by splitting on `,`, lowercasing only the key portion before `=`, then sorting the resulting `key=value` tokens alphabetically as strings (not by key alone) and rejoining with `,`; hash `host + normalized_config` with SHA-256 hex; track a `seen` set of hashes, printing new records via `json.dumps(obj, separators=(',',':'), sort_keys=True)` to stdout and `DUPLICATE: <hash>` to stderr for repeats; finally emit the exact `SUMMARY: Processed <N> valid records, <D> duplicates found.` line to stderr after EOF. Test locally with a handful of crafted lines (including malformed JSON and repeated configs with different key order/case) before trusting it against the fuzzer.