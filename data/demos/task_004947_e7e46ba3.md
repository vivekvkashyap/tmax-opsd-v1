Read `generate_limits.py` first to see exactly how `merge_specs` currently combines the JSON spec files (it likely just extends/appends without deduplication or ordering). Fix it to build a dict keyed by endpoint path where, for each path seen across files, you keep the minimum limit value seen so far (compare and replace, don't just overwrite), then when generating output, iterate the dict's items sorted by key alphabetically so the emitted Rust match arms are both deduplicated and ordered — this directly fixes the "duplicate match arms" compile failure. Add `test_generate.py` using `unittest`, exercising `merge_specs` against small in-memory or temp-file JSON inputs with overlapping keys and asserting both minimum-selection and sort order. For memory profiling, wrap the `main` logic with `tracemalloc.start()` at the top and `tracemalloc.get_traced_memory()` (take the peak/second value) right before exit, writing that integer to the specified file. After regenerating `limits.rs`, run `cargo build` to confirm it compiles, then run `diff -u specs/old_limits.rs src/limits.rs > limits.diff` to produce the required diff artifact.