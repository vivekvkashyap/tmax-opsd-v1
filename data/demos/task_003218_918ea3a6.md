Build this as a small pipeline of discrete, verifiable stages rather than one big command. First, detect each CSV's encoding (e.g. `file -i` or `iconv -l` trial, or just inspect for BOM/null-byte patterns typical of UTF-16) and convert each to UTF-8 with `iconv -f <detected> -t UTF-8`, writing to temp files — don't assume all inputs share one encoding. Next, strip each file's header, concatenate the data rows from all converted files, and sort the combined rows numerically/lexicographically by the timestamp field (ISO-8601 timestamps sort correctly as plain strings) with `sort -t, -k2`; prepend a single header line and write to `/home/user/combined.csv`. For changepoint detection, walk the sorted rows with `awk`, keeping the previous row's `response_time_ms` in a variable, and emit the current row when `current > 3 * previous` (first row has no predecessor and can't be an anomaly); write matches plus the header to `/home/user/anomalies.csv`. Finally, load `/home/user/combined.csv` into `/home/user/analytics.db` using `sqlite3 analytics.db -csv ".import combined.csv performance_logs"` (create the table or let `.import` infer it, skipping the header row correctly). Verify each output file against expectations before finishing.