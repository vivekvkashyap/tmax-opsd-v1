Implement this as a straightforward pipeline but be precise about the math and tokenization, since the verifier checks exact rounded values. Tokenize each message by lowercasing, replacing all punctuation with spaces (e.g. `re.sub(r'[^\w\s]', ' ', text)` minus underscore handling as needed, or explicitly `re.sub(r'[^a-z0-9\s]', ' ', text.lower())`), splitting on whitespace, and taking the unique set of resulting words. For the Naive-Bayes posterior: start with unnormalized scores `score_urgent = prior_urgent` and `score_normal = 1 - prior_urgent`; for each token that's a key in the config's `likelihoods` dict (ignore all others), multiply `score_urgent *= likelihoods[word]['urgent']` and `score_normal *= likelihoods[word]['normal']`; finally normalize with `posterior = score_urgent / (score_urgent + score_normal)`. Keep only rows where posterior > 0.5, round to exactly 4 decimals with Python's `round()`, and write a JSON array of `{"id":..., "posterior":...}` in the same order as the input rows. For MLflow, set the tracking URI to the exact `file://` path given, use `mlflow.set_experiment(...)` with the exact experiment name (creating it if it doesn't exist), and inside `with mlflow.start_run():` call `log_param("prior_urgent", ...)` and `log_metric("urgent_count", ...)` — log the prior as the same value/type present in the config so downstream string comparisons match.