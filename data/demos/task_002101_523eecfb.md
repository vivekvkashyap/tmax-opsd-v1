Parse `/home/user/archive_rules.conf` with Python's `configparser` to pull `source_dir`, `error_filter`, `output_file`, and `archive_name` from the `[Backup]` section rather than hardcoding paths, since the config is the single source of truth. For each `.log` file in `source_dir`, split its content on the exact three-equals-sign separator `===` to get record blocks; for each non-empty block, parse the first line with a regex like `\[(\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2})\] (\w+)` to pull timestamp and level, and join all remaining lines in the block with `\n` for the message (strip trailing/leading blank lines from the block first, since the separator-adjacent whitespace can leak into the message otherwise). Keep only blocks whose level matches `error_filter` exactly, collect them into a list of dicts in the required key order, and write with `json.dump(..., indent=2)` (or without indent — just make sure the keys and values match) to `output_file`. Finally, produce the tar.gz using `tar -czf <archive_name> -C <dir_of_output_file> <basename_of_output_file>` so the archive contains only the bare filename with no directory prefix — this `-C` plus basename trick is the key detail, since a naive `tar -czf archive.tar.gz /home/user/filtered_logs.json` would embed the full path inside the archive instead.