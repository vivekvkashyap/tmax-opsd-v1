Write `/home/user/pipeline.c` as a straightforward multi-pass C program: pass 1 reads `raw_data.csv` line by line (skip header), parsing each row's four comma-separated fields; treat `f2` as text so you can detect empty string or literal "NaN" as missing while still summing the valid ones as integers — accumulate a sum and count of valid f2 values, then compute the mean with integer (truncating) division. Pass 2: re-emit each row, substituting the computed mean wherever f2 was missing, and compute `target = 0.25*f1 + 0.75*f2` for every row, writing the CSV with `target` formatted via `%.2f`. Keep the cleaned rows in an array so you can also print the two specific summary values requested. For the benchmark, isolate the bare arithmetic (`0.25*f1[i] + 0.75*f2[i]`) inside a loop that runs it 10,000,000 times over the in-memory arrays (accumulate results into a dummy sum so the compiler doesn't optimize the loop away), timing it with `clock_gettime(CLOCK_MONOTONIC, ...)` before and after and reporting the delta in seconds to 6 decimal places. Compile with `gcc -O2 -lm` (or without -O2 if you worry about the loop vanishing) into `/home/user/pipeline`, run it, and check the generated CSV and summary.txt match the exact expected numeric values from the spec before finishing.