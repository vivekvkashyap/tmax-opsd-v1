Extract the tarball first (`tar -xzf`) into the target workspace directory, then inspect each file's actual byte encoding before parsing — do not assume UTF-8. For the XML, detect it's UTF-16LE (check the byte order mark or use `file`) and decode accordingly before feeding it to an XML parser (e.g. read the file specifying encoding `utf-16-le`, or use `iconv` to transcode to UTF-8 first, then parse with a standard XML library). For the JSON, decode as ISO-8859-1/Latin-1 (again transcode with `iconv -f ISO-8859-1 -t UTF-8` or open with that encoding) before passing to a JSON parser — naive UTF-8 parsing will throw decode errors or corrupt characters on both files. Pull the four required fields, build rows as (component, key, value), sort them alphabetically first by component then by key, and write a CSV with the exact header `component,key,value` at the specified path. Finally zip only the CSV (no directory prefix) — use `cd` into the CSV's directory before zipping, or pass just the basename to `zip`, so the archive doesn't embed a path.
