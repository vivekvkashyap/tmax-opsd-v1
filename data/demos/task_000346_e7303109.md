Treat this as three independent sub-steps that only combine at the final detector script. First, OCR `/app/parameters.png` with `tesseract /app/parameters.png stdout` (or via Python `pytesseract`) and carefully parse the printed text to recover the three numeric parameters (GAMMA, OMEGA, DIVERGENCE_THRESHOLD) — don't guess values, read what OCR actually returns and sanity-check the digits since OCR can misread characters like 0/O or 1/l. Second, compile `/app/src/rk4_core.c` into `librk4.so` with `gcc -shared -fPIC rk4_core.c -o librk4.so`; this step is largely a distractor since the detector itself only needs numpy, but it must still succeed. Third, in `/home/user/detector.py`, load the `.npy` file with `numpy.load`, extract the position/velocity columns via vectorized array slicing (not a Python loop) with `arr[:,1]` and `arr[:,2]`, compute `Energy = 0.5*v**2 + 0.5*OMEGA**2*p**2` for every row at once, take `Energy.max()`, and compare strictly (`>`) against DIVERGENCE_THRESHOLD to decide `sys.exit(1)` vs `sys.exit(0)`. Test against both a clearly clean and clearly evil synthetic array before trusting the logic, since the real grading corpus will be hidden.
