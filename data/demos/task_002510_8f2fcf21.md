Stream `/home/user/raw_config_events.jsonl` line-by-line (never `json.load` the whole file) while deduplicating on `event_id` using a set, and bucket each unique event by `server_id` as you go (e.g. writing to per-server lists or spilling to disk/dict-of-lists) rather than materializing everything before any processing. Then use `multiprocessing.Pool` to hand off each server's sorted event timeline to a worker function that, for every hour boundary between 2023-10-01T00:00:00Z and 2023-10-07T23:00:00Z inclusive, finds that server's most recent event at-or-before the hour (binary search or a running pointer over the server's own sorted events, since they're already time-ordered) and records the resulting config_hash for that hour — servers with no prior event at a given hour contribute nothing for that hour. In the main process, merge each worker's per-hour hash back into a global structure keyed by hour, and for each hour compute the count of distinct config_hash values across all servers active at that hour. Write `hour,unique_configs` rows sorted chronologically with the ISO 8601 `Z`-suffixed timestamp format, no extra whitespace, matching the header exactly.