Design the SQL query first in the `sqlite3` CLI against `/home/user/company.db` before writing any Rust, since window-function logic is much faster to iterate on there. Use a `JOIN` of `employees` to a per-manager report count derived from `reports_to` (`GROUP BY mgr_id`), then apply `ROW_NUMBER() OVER (PARTITION BY dept_id ORDER BY report_count DESC, salary DESC, emp_id ASC)` to rank within each department, filtering to `report_count >= 1` and `rn = 1`. Join in `departments` for the name and `ORDER BY dept_name` for final output. Only after you've confirmed the query returns correct rows in the CLI should you scaffold the Rust project (`cargo new manager_report`), add `rusqlite` (with the `bundled` feature to avoid needing system libsqlite3), `serde`, and `serde_json` to `Cargo.toml`, embed the validated SQL as a string, map rows into a `#[derive(Serialize)]` struct matching the exact field names/order given, and write the array with `serde_json::to_writer` (or `to_string_pretty`) to `/home/user/top_managers.json`. Double-check tie-breaking direction (DESC on reports and salary, ASC on emp_id) matches the ORDER BY clause exactly — this is the easiest place to get backwards.