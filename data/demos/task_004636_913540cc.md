Extract the exact byte range first, then decode: use `dd if=/home/user/artifacts.bin bs=1 skip=4096 count=512 2>/dev/null` to pull just the 512-byte metadata block (using `bs=1` with skip/count in bytes is slow but precise and safe for exact offsets — for large files you could instead use `bs=4096 skip=1` and `bs=512 count=1` to speed it up, but verify the offset math carefully either way). Pipe that raw block through `iconv -f UTF-16LE -t UTF-8` to decode it, then strip null padding bytes with `tr -d '\0'`. From the decoded text, filter out lines containing the literal uppercase tokens `OBSOLETE` or `REJECTED` using `grep -Ev 'OBSOLETE|REJECTED'`, also drop any blank/whitespace-only lines left from padding, then pipe through `sort` for alphabetical order. Redirect the final result to `/home/user/curated_list.txt` and inspect it with `cat -A` to confirm there are no stray control characters, extra blank lines, or trailing garbage from the decode step.