Treat this as three independent, verifiable fixes rather than one monolith. First, open `services.ini` and add `After=analytics-worker`'s dependency line under the `[analytics-worker]` section only — don't touch other sections. Second, in `analytics-worker.py`, wrap the `open()` call in a loop that catches `FileNotFoundError` specifically (not a bare `except`), sleeps 1 second, and retries up to 3 additional times (4 attempts total); if all attempts fail, call `sys.exit(1)` explicitly rather than letting the exception propagate. Third, build `run_pipeline.sh` as an idempotent wrapper: clear `data/` (e.g. `rm -rf`/`mkdir -p` or `find -delete`) before each run so stale files don't mask a real race condition, `mkdir -p` the logs directory, invoke the supervisor with `2>&1` redirected into the log file, and branch on `$?` to write the exact success/failure string to `status.txt`. Finally `chmod +x` the script. Test end-to-end by running the wrapper multiple times to confirm the ordering fix plus retry logic reliably produces `PIPELINE_SUCCESS` even though the original race condition still exists timing-wise.