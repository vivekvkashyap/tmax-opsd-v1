Write the physics/numerics core as plain, testable NumPy first, then wrap it with mpi4py — verifying the serial math independently (e.g. by printing intermediate results for one alpha) will save you from debugging MPI and finite-difference bugs simultaneously. Build the grid with `np.linspace(0,1,50)`, set the initial condition to 1.0 on indices 20-29 inclusive, and apply the FTCS update only to interior points `u[1:-1] = un[1:-1] + alpha*dt/dx**2*(un[2:]-2*un[1:-1]+un[:-2])` leaving the Dirichlet boundaries at 0. After every step check `np.any(np.isnan(u)) or np.max(np.abs(u)) > 100.0` and break immediately if so, recording distance -1.0 for that rank; do the instability math yourself (compare alpha*dt/dx^2 against the FTCS stability bound) to sanity-check which of the four alphas you expect to blow up before running. For stable runs, normalize both the final and initial profiles to sum to 1 and call `scipy.stats.wasserstein_distance(x, x, u_norm, u_init_norm)`. Assign rank i to alpha[i] via `comm.rank`, gather (alpha, distance) tuples at rank 0 with `comm.gather`, sort ascending by alpha, and write plain `alpha,distance` CSV lines with no header. Run via `mpirun -np 4 python3 diffusion_mpi.py`, installing mpi4py/openmpi first if missing.