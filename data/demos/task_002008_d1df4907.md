Work through the pipeline in dependency order, verifying each stage first. Fix the vendored library build: inspect `/app/svdlibc/Makefile` for the link flags used to build `libsvd.a` — a missing math-library flag is a classic cause of `undefined reference to 'sqrt'` errors; add it and confirm `make` produces `libsvd.a`. Read the `svdlibc` headers to understand `SMat`/`SVDRec` and the calling convention for `svdNewSMat`/`svdLAS2A` so your C driver builds the matrix correctly from the combined dataset+anchors rows. In `sanitizer.c`: parse both files into a single (N+M)x50 matrix, run SVD keeping k=3 singular vectors, project every row (dataset and anchor) via the top-3 right singular vectors, then for each dataset row compute Euclidean distance in 3D to every anchor and reject if any distance < 1.50. Write survivors in original 50-dim format, and write the JSON log with real counts and the actual top singular value from your SVD run — verify locally against `/home/user/corpora/clean` and `/evil` (100% preserved vs 100% rejected) before trusting the hidden eval.