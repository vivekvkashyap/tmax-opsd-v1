Extract the target metric prefix from the audio first, since everything downstream depends on it: transcribe `/app/incident_report.wav` with an available STT tool (e.g. `whisper`, or `SpeechRecognition` with a local/offline engine — install if needed) and read out the specific metric-name prefix the on-call engineer names; use that exact prefix to decide which fields of the `metrics` object to keep, don't guess a prefix from field names alone. For the Go pipeline: open `/app/telemetry.jsonl` with a buffered/streaming line reader (never read the whole file into memory), and for each line attempt `json.Unmarshal`; when unmarshal fails due to an invalid `\uXYZW`-style escape, sanitize the raw line (e.g. regex-replace malformed `\u` sequences with something valid, or strip them) and retry rather than dropping the line outright, since the message field's corruption shouldn't cost you a data point. For each host+metric matching the target prefix, maintain a per-(host,metric) sliding window of the last 300 seconds of readings (by timestamp, not by count) and compute the mean each time a new timestamp arrives; write rows sorted by `window_end_ts`, then `host`, then `metric_name` to the CSV with the exact header given.
