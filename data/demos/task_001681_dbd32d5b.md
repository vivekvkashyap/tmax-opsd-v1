This is a deterministic-simulation task: exact reproducibility of the RNG sequence matters more than the statistics themselves. Set up the Go module with `go mod init` and add the gonum dependency via `go get gonum.org/v1/gonum`. Load X.csv/y.csv into a `mat.Dense`/`mat.VecDense` by parsing CSV rows manually (encoding/csv + strconv.ParseFloat). Create `rng := rand.New(rand.NewSource(42))` once, outside the loop, and for each of exactly 1000 iterations draw exactly 100 indices via `rng.Intn(100)` in a fixed order — building extra intermediate structures or drawing indices in a different sequence (e.g. batch-generating all indices up front vs. per-iteration) will desync the sequence from the reference implementation, so keep index generation tight inside the loop matching the described order. Build the bootstrap X/y by row-gathering, call `SolveVec`, and on error just skip (don't panic, don't fall back to a different solver). Track successes for `success_rate`, collect w0 from successes, sort ascending, and use `stat.Quantile(p, stat.Empirical, sorted, nil)` at p=0.025/0.975 — using a different quantile estimation type will shift the CI edges outside tolerance. Read `ref_w0.txt`, check inclusive bounds, and write the exact JSON keys requested to `/home/user/result.json`.
