Treat `/app/spectra_oracle` strictly as a black-box function you call as a subprocess with three CLI floats and parse its comma-separated stdout — don't try to disassemble or reverse-engineer its internals, just probe it empirically. In your C++ server, implement a derivative-free optimizer (Nelder-Mead simplex is a solid, simple choice) that: given a target 10-float spectrum, defines an objective function which shells out to the oracle for a candidate (x,y,z), parses the returned CSV into 10 doubles, and computes mean squared error against the target; iterate the simplex updates (reflect/expand/contract/shrink) starting from a few different initial simplices spanning the `[0,10]` cube (to avoid local minima) until the MSE converges below a tight tolerance, then output the best (x,y,z) found. For the HTTP layer, use minimal raw POSIX sockets or a tiny header-only library to bind `127.0.0.1:8080`, read the `POST /optimize` body (parse `Content-Length` to know how much to read), run the optimizer synchronously per request (reusing the same process across sequential requests, spawning `spectra_oracle` fresh each objective-function evaluation), and write back `HTTP/1.1 200 OK` with the comma-separated x,y,z as plain text. Compile with `g++ -O3`, launch in the background, and sanity-check with a manual `curl -d "..."` against a spectrum you generated yourself from a known (x,y,z) via the oracle, confirming your optimizer recovers values within 0.1 before considering it done.
