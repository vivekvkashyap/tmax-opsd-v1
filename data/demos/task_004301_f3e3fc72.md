Work through the steps in order and verify each before moving on. First back up the config exactly as-is (`cp /home/user/appstack/config.json /home/user/backups/config.json.bak`) before touching it. Then append the export line to `/home/user/.bash_profile` and `source` it in your current shell so subsequent commands see `APP_SOCKET_PATH`. Edit `config.json`'s `upstream_socket` value so it is character-for-character identical to the exported path — the backend and proxy both read this path, and any mismatch (trailing slash, typo) reproduces the 502. Rewrite `manager.sh` so `start` launches `backend.py` and `proxy.py` as detached background processes (e.g. `nohup ... & echo $! > run/backend.pid`) with the environment variable exported into that subshell, and `stop` reads each PID file, `kill`s it, then removes the file — test both paths manually. For port forwarding, run `socat TCP-LISTEN:9090,fork,reuseaddr TCP:127.0.0.1:8080 &` in the background, not an SSH tunnel. Finally start the stack, confirm the socket exists and the proxy connects (no more 502), then capture `curl -s http://127.0.0.1:9090/status` output verbatim into `/home/user/final_output.log` — don't add trailing newlines or reformat it.