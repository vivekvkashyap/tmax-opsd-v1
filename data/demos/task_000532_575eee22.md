This is a startup-ordering race condition, so fix it with an explicit readiness check rather than a fixed sleep. In `src/main.rs`, replace the hardcoded port with `std::env::var("BACKEND_PORT")` and build the connection address from it; on successful `TcpStream::connect`, write the exact string `RESTORE_SUCCESS: Verified data connection` (verify it matches byte-for-byte, no extra text) to `/home/user/restore_results.log`, then `cargo build --release`. In `start_all.sh`, start `backend_service.sh` in the background redirected to `backend.log`, then poll in a loop (e.g. `while ! grep -q "Listening for connections" backend.log; do sleep 0.5; done`, or equivalently poll with `nc -z localhost $PORT` / a `curl` connect check) with a reasonable timeout before giving up, only then `export BACKEND_PORT=8085` and exec `./app/target/release/restore_verifier`. The bug a naive fix misses is polling only the log OR only the port but not handling the case where the process takes several seconds to bind — make sure the wait loop actually blocks until the service is truly ready before launching the Rust binary. Test end to end by running `./start_all.sh` and checking the log file content.
