Scaffold the Rust side first with `cargo init --lib` at the target path, then set `crate-type = ["cdylib"]` in Cargo.toml so it produces a C-compatible shared object. Implement `decode_payload` with `#[no_mangle] pub extern "C"` linkage, taking raw pointers and lengths matching the given C signature exactly (isize return type, const uint8_t* input, size_t input_len, uint8_t* output, size_t max_len) — inside, build a Rust slice from the raw input pointer, XOR every byte with 0xAA, then validate the result with `std::str::from_utf8` (return -1 on error), and only if the decoded length fits within max_len copy it into the output buffer via `std::ptr::copy_nonoverlapping` and return its length, else -1. Build with `cargo build --release` and confirm the .so exists at the expected path before touching Python. In Python, load the library with `ctypes.CDLL`, set `.argtypes`/`.restype` explicitly to avoid pointer size bugs, allocate a `ctypes.create_string_buffer(1024)` output buffer, call the function, and slice the returned length. Finally generate the patch with `diff -u legacy_process.py process.py > migration.patch` (order matters: original first, modified second) so the diff headers are `---`/`+++` correctly.
