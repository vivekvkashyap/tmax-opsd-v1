Split this into three independent sub-problems and verify each before wiring them together. First, OCR the video: use `ffmpeg -i /app/corruption_report.mp4 -vf fps=<N> frame_%04d.png` to extract frames at a high enough rate to catch every scroll position, then run `tesseract` on each frame and grep for lines mentioning "CORRUPTED" near a `BK-XXXX` token; dedupe the collected IDs across frames since scrolling text repeats between consecutive frames. Second, inspect `/app/backup_meta.db` with `sqlite3 .schema` and sample rows to understand the `servers`/`backups`/`storage_nodes` relationship — the bug is a classic multi-row-join fan-out: joining `backups` to `storage_nodes` (which can have multiple rows per backup) before aggregating inflates SUM(size_bytes). Fix it by either aggregating `size_bytes` per backup first (subquery/CTE) before joining storage_nodes, or by dropping the unnecessary join to storage_nodes entirely if it's not needed for the size calculation, so each backup contributes its size exactly once. Third, hardcode the OCR'd IDs into the SQL's IN(...) clause in `/app/server.go`, build and run the Go HTTP server bound to 127.0.0.1:8080 exposing GET /api/v1/corrupted_sizes, sorted alphabetically by hostname, and launch it in the background (e.g. `nohup ./server &`) so it stays alive for the verifier.