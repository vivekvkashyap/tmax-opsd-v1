Transcribe `/app/network_log.wav` offline first (e.g. a local `whisper` model, or another offline speech-to-text tool) and manually parse out every "X knows Y." sentence into an edge list — don't hardcode edges from guessing; verify each one against what the transcript actually says. Represent the graph as an undirected adjacency structure (in awk/bash, e.g. build 1-hop neighbor lists by scanning lines where the target appears as either column). In `/home/user/query.sh`, given `$1`: compute the 1-hop set, then union the 1-hop sets of each of those neighbors to get raw 2-hop candidates, then subtract the target node itself AND the full 1-hop set from that candidate set (a node reachable via two different 2-hop paths must only appear once — dedupe), sort alphabetically, join with spaces, and print `NONE` if the result is empty or the node isn't in the graph at all. The two most common bugs: forgetting to exclude 1-hop neighbors that are also reachable in 2 hops, and not deduplicating/sorting the final output — the fuzz-equivalence checker requires exact line-for-line match against many random names, so test with several nodes including ones with no edges.
