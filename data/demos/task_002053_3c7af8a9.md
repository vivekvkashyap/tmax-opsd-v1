Start by diagnosing the build failure, don't just retry `make`: run `make clean && make xxhsum` in `/app/vendored/xxHash-0.8.1`, read the actual compiler/linker error, then grep the `Makefile` for the flag it's tripping on (search for `DISPATCH` — a dispatch build pulls in SIMD/AVX code paths that need extra intrinsics support not present here). Flip that setting back to the disabled/non-dispatch value so the build uses the portable scalar path, then re-run `make clean && make xxhsum` and confirm the binary runs (`./xxhsum -H0 <<< test`) before moving on — don't touch anything else in the Makefile.

For the ETL script, write it in Python for reliable JSON/float handling, invoked as `/home/user/run_etl.sh <jsonl_path>`. Per line: json.loads, skip (don't error) if `user_id` or `amount` is absent; coerce `amount` to float (handle numeric-looking strings), clamp to [0.0, 1000.0], format with exactly 2 decimals. Shell out to your compiled `xxhsum -H0` with the raw `user_id` string as input, and parse only the first whitespace-separated token of its output (the hex hash) — ignore the trailing filename/stdin marker. Emit `{hash},{amount}` per valid line, no header, no trailing spaces. Test against the worked example in the task before considering it done, and pay attention to edge cases like malformed JSON lines or non-numeric amount strings, since the grader fuzzes heavily.
