Handle the three sub-problems independently, verifying each in isolation. First, transcribe `/app/outage_alert.wav` with an STT tool to recover the spoken backup port number, then overwrite `/home/user/mailer.conf` with exactly `PORT=<that number>`. Second, for the systemd user services, inspect both unit files under `~/.config/systemd/user/`: add an `After=diagnostic-logger.service` (and typically `Wants=diagnostic-logger.service`) line under `[Unit]` in `alert-sender.service` so the FIFO consumer starts before the FIFO writer — just adding `After=` without also using `systemctl --user daemon-reload` and starting `diagnostic-logger.service` first will still fail, so reload, enable, and start both in the correct order and confirm with `systemctl --user status`. Third, for the endpoint checker, replace the sequential loop in `check_endpoints.sh` with backgrounded checks (`&`) per endpoint followed by `wait`, being careful to preserve exact output ordering/format in the log — write each check's result to a per-endpoint temp file or use a construct that avoids interleaved/garbled writes to the shared log, then assemble the final log in the original order. Time the script afterward to confirm it's under 0.5s.
