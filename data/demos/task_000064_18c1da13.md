Build the script in independently-verifiable pieces. First write a pure-Bash semver comparator (split the version string on `.`, compare major/minor/patch numerically, not lexicographically, since e.g. "4.10" > "4.9") and run it against `ffprobe -version`'s reported version, exiting 1 if below 4.2.0. Second, extract per-frame packet sizes with `ffprobe -v error -select_streams v:0 -show_entries frame=pkt_size -of csv=p=0 /app/attack_capture.mp4` (redirect ffprobe's own stderr/logging away from stdout so only the size values are captured), storing them in a Bash indexed array so you can track both the frame index and its size while scanning. Third, iterate the array once to find the index of the maximum value — don't rely on `sort -n | tail -1` on a size-only stream since that discards the frame index; keep index and size paired as you scan. Fourth, use a heredoc to write `/home/user/.gitlab-ci.yml` containing an `alpine:latest` image, an `apk add --no-cache ffmpeg bash` install step, and an `analyze` job invoking this same script. Finally ensure only the integer frame number is printed to stdout (route all other diagnostics to stderr with `>&2`), and `chmod +x` the script. Test it standalone and confirm the printed number is close to where the sudden size spike actually occurs in the ffprobe output.
