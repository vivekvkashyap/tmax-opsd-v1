Model this explicitly as graph construction plus cycle detection rather than trying to spot deadlocks by inspection. Parse both CSVs, and for every blocked request row (tx_id=A, resource, requested_mode) look up all lock rows on that same resource held by a different tx_id=B, adding a directed edge A->B only when the mode pair conflicts — the conflict rule is: any edge exists unless both the held and requested modes are `SHARED` (SHARED+SHARED is the only non-conflicting pair, everything involving an `EXCLUSIVE` conflicts). Skip self-edges where A==B. Build this with `networkx.DiGraph` and use `nx.simple_cycles(G)` (or an equivalent cycle-finding routine) to enumerate all simple cycles rather than hand-rolling cycle detection, since manual DFS-based cycle enumeration is easy to get wrong on shared sub-cycles. For output, sort the tx_ids alphabetically *within* each cycle list, then dedupe cycles that are rotations of each other by converting each to its sorted-tuple form and using a set, then sort the outer list of cycles lexicographically by the sorted tuple before serializing to `/home/user/deadlocks.json`. Manually trace a couple of the wait-for edges against the CSVs to sanity-check your graph-building logic before trusting the cycle output.