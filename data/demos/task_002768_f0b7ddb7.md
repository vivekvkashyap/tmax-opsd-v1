Build a small streaming HTTP server (e.g. Python's `http.server` with a custom `BaseHTTPRequestHandler`, or Flask with `request.stream`) bound to `127.0.0.1:9090` handling `POST /api/v1/process`. Read the body incrementally and split on newlines so you never buffer the whole payload before processing — parse each JSON line, replace the `email` value with the literal string `***@***.***`, then call the external binary via `subprocess.run(["/app/legacy_hasher", user_id], capture_output=True, text=True)` and take its stdout (stripped) as `secure_token`. Only keep records whose `secure_token` starts with a lowercase letter in the range `a`-`f` (hex-style alphabetic first char) — drop everything else, including tracking counts of received vs retained rows per request. Stream the retained records back as newline-delimited JSON in the response body. After the response completes, append exactly one line `PROCESSED:<received>|RETAINED:<retained>` to `/home/user/etl_metrics.log`. Launch the server as a background process (e.g. `nohup python3 server.py &` or via a subprocess with `disown`) so it stays up after your shell command returns, and confirm it's listening with a quick local `curl` test before finishing.