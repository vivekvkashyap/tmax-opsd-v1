Start by inspecting the schema before writing any SQL: run `sqlite3 /home/user/climate_data.db ".schema"` to see the `locations`, `sensors`, and `readings` tables and how they relate (sensor→location via `location_id`, reading→sensor via `sensor_id`). Create indexes that actually match your join/filter columns, e.g. `CREATE INDEX idx_readings_sensor ON readings(sensor_id)`, `CREATE INDEX idx_sensors_location ON sensors(location_id)`, and one on `sensors(type)` or `locations(region_name)` for the filter predicates — verify with `EXPLAIN QUERY PLAN` that SQLite actually uses them. Then replace the Python script's logic entirely with one SQL query: join `readings` → `sensors` → `locations`, filter `sensors.type = 'TEMP' AND locations.region_name = 'Alpine_Zone'`, and compute the moving average with a window function: `AVG(value) OVER (PARTITION BY sensor_id ORDER BY timestamp ROWS BETWEEN 2 PRECEDING AND CURRENT ROW)`, rounding with SQL `ROUND(..., 2)` so rounding happens identically to how the checker will recompute it. Order results by `sensor_id ASC, timestamp ASC` in the SQL itself (don't rely on Python re-sorting), fetch rows, and dump them as a JSON array of objects with the exact required keys to the specified output path.
