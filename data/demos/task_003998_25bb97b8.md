Compile with `gcc -o /home/user/generate_pdb /home/user/generate_pdb.c -lm`, run it redirecting stdout to `/home/user/simulated.pdb`, and eyeball a few lines to confirm the record format before parsing. In Python, read the file line by line, keep only lines starting with `ATOM`, and slice the B-factor as `float(line[60:66])` (0-based columns 61-66) rather than splitting on whitespace, since PDB is fixed-width and adjacent numeric fields can run together. Fit both distributions with `scipy.stats.norm.fit(data)` and `scipy.stats.gamma.fit(data)`, get the log-likelihood of each fit via `dist.logpdf(data, *params).sum()`, then compute `AIC = 2*k - 2*loglik` with k=2 for Normal and k=3 for Gamma. Compare the two AIC values, pick the lower as `best_fit`, and round both to 2 decimals. Write the JSON to `/home/user/analysis_results.json` with exactly the keys `best_fit`, `aic_normal`, `aic_gamma`. Sanity-check by plotting or printing summary stats (mean/skew) of the B-factors — a right-skewed, non-negative distribution is a strong hint about which family should fit better.