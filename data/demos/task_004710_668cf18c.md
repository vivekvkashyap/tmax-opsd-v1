Run this as a real incident-response workflow, not a guess-and-patch. First mine `/app/logs/container.log` for the exact crashing frame index/arguments (grep timestamps/frame numbers around the crash), and use `ffmpeg` to extract that frame (and neighbors) from `/app/incident-stream.mp4` as raw binary (e.g. `-pix_fmt gray`/`rgb24` `-f rawvideo`) matching whatever format the log/service implies. Second, actually bisect: `git bisect start`, mark `HEAD` bad and `v1.0.0` good inside `/home/user/video_service`, and at each step build and run the current commit against the extracted crashing frame to classify good/bad (script this so bisect can auto-test), narrowing down to the exact commit — don't just eyeball the diff. Third, once you have the bad commit, build with debug symbols and reproduce the crash to get a concrete stack trace (e.g. `gdb --args ./processor frame.bin` then `run`/`bt`, or compile with ASan) pinpointing the unsafe memory operation (likely an off-by-one loop bound from the "optimize"/SIMD-chunking commit). Fix the boundary condition, not just add a bounds check that changes output. Rebuild the corrected program and place the binary at exactly `/home/user/fixed_processor`, confirming it reads one filepath arg and streams processed bytes to stdout — test against several frame sizes including sizes not divisible by 16, since that's the exact edge the bug hinges on.