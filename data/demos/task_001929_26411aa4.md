Build the ELF parser as a standalone tool first and validate it on a single known binary before scaling to the whole archive. In extractor.c, include `<elf.h>`, mmap or read the file, verify the ELF magic, then walk the 64-bit section header table (`Elf64_Ehdr.e_shoff`/`e_shnum`/`e_shstrndx`) to find the section whose name (resolved via the section-header-string-table section) equals `.exp_data`, then write its raw bytes (`sh_offset`/`sh_size`) to stdout — do not grep for the string in the raw bytes, since the spec requires proper section-header parsing and arbitrary preceding bytes could break a naive grep. Compile with gcc and test it against one binary with a known `.exp_data` section before searching the whole tree. To find candidates across `/home/user/dataset_archive`, use `find ... -type f -exec file {} \;` or check the ELF magic bytes directly, since not every file will be an ELF executable or have the section — skip failures gracefully. For each hit, parse the `[TRIAL:x;OUTCOME:y;TIME:z]` format with `sed`/`awk` (e.g. strip brackets, split on `;`, split each `key:value` on `:`) into CSV rows, then `sort -u` by the trial field before writing the `trial,outcome,time` header and rows to summary.csv. Finally `tar -czf` summary.csv into final_archive.tar.gz.