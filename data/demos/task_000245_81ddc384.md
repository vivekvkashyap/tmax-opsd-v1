Build this as a pipeline of clearly separated, independently testable stages rather than one monolithic Rust program. First use `ffmpeg -i /app/video.mp4 -vf fps=1 /tmp/frames/frame_%04d.jpg` to get numbered frames, and verify manually that the count/order look right before writing Rust. In Rust, load frames with the `image` crate, convert to grayscale, and bin into 64 buckets of 4 intensity levels each, then normalize to sum to 1.0 — do this arithmetic in `f64` throughout. Detect "corrupted" frames by mean intensity < 10.0 and mark them, but do the NaN-imputation pass only after processing every frame so you always have both a strictly-previous and strictly-next valid neighbor to average (handle edge cases where a corrupted frame borders the sequence boundary explicitly). For PCA to 4 dimensions, use `linfa`/`nalgebra`/`ndarray` on the imputed 64-d matrix — center the data before computing components, a naive skip of mean-centering will bias the results and hurt the recall metric. Compute pairwise Euclidean distances in the 4-d space, then for each query frame in {10,20,30,40,50} take the 5 nearest excluding itself. Write `/home/user/similarities.csv` with the exact header and 1-indexed frame ids ordered most- to least-similar; run the full pipeline end-to-end to confirm the file is actually produced.