Write a Python script that parses each log line of `/home/user/vendor_updates.log` with a regex capturing timestamp, LANG, KEY, SRC, TGT. The key trap is timestamp normalization: two formats (`YYYY-MM-DD HH:MM:SS` and `MM/DD/YYYY HH:MM:SS`) appear interleaved for the same (LANG,KEY) pair, so you must parse both into a single comparable `datetime` object (e.g. try `strptime` with each format pattern) rather than comparing raw strings, or "latest" will be picked wrong. Apply the anomaly filter first: compute `R = len(TGT)/len(SRC)` and discard entries with `R < 0.2` or `R > 3.0` before ranking by timestamp — an anomalous entry should never count as "most recent" even if its timestamp is later. Group survivors by (LANG, KEY), keep only the max-timestamp entry per group, and build a nested dict `{lang: {key: tgt}}`. Dump with `json.dump(..., indent=2, ensure_ascii=False)` to `/home/user/clean_translations.json` — using ensure_ascii=False matters since translations contain non-ASCII characters that should be written literally, not escaped.