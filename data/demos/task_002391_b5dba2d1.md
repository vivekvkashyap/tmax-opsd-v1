Work through this in dependency order. First OCR the token out of `/app/token.png` (e.g. `tesseract /app/token.png -`, trimming whitespace/noise from the result) and treat it as a variable you'll reuse, not something to hardcode blindly — verify it looks like a real token string before using it everywhere. Build the dashboard as a Python `http.server`/Flask app with the three routes; launch it with `TZ=Europe/Berlin LANG=de_DE.UTF-8` set in its environment (and make sure the locale is actually generated/available, e.g. via `locale -a` or `locale-gen de_DE.UTF-8`) so `/status` can read `os.environ['TZ']` and `os.environ['LANG']` directly rather than hardcoding the strings. Check the `Authorization: Bearer <token>` header exactly on `/alert` and `/alerts`, returning 401/200 as specified, and keep alerts in a module-level list. For the git piece: `git init --bare /home/user/alerts.git`, write a `post-receive` hook that reads `git diff-tree --no-commit-id --name-only -r <new-sha>` from stdin, filters `*.alert` files, `git show <sha>:<file>` to get `LEVEL:MESSAGE`, and POSTs it with the bearer token to the running dashboard. Clone the bare repo into `/home/user/alerts_local`, then write `auto_alert.exp` using `spawn`/`expect`/`send` to answer the three prompts of `/app/interactive_submit.sh` in order, and schedule it via the user's crontab at `*/5 * * * *`.