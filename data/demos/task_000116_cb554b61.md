Treat this as three independent extraction problems feeding one Rust program, and validate each independently before combining. First, integrity check: compute SHA-256 of `/app/raw_logs.txt` (Rust `sha2` crate or shell out to `sha256sum`) and compare against the hex digest in `/app/raw_logs.txt.sha256`, exiting with an error immediately on mismatch. Second, certificate parsing: use a crate like `x509-parser` or `openssl` bindings (or shell out to `openssl x509 -in server.crt -noout -subject` and parse the CN field) to pull the Subject Common Name out of `/app/server.crt`. Third, redaction rules: run OCR (`tesseract /app/evidence_photo.png stdout`) to read the note's text and derive the exact regex/replacement rules it specifies for redacting credit-card-like and SSN-like patterns from the log lines — implement those patterns precisely as described (e.g. via the `regex` crate), replacing only the digit groups as instructed rather than the whole match indiscriminately. Write `Server CN: <cn>` as line 1, then the redacted lines in original order, to `/home/user/redacted_logs.txt`. Since scoring is line-by-line exact-match accuracy against a hidden reference (threshold 95%), test your redaction against a variety of sample log lines yourself to catch edge cases like multiple matches per line or almost-matching but invalid formats.