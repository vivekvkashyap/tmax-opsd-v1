Approach this as a systematic debugging task, not a rewrite. First reproduce the crash: compile with debug symbols (`gcc -g -o calc_metrics calc_metrics.c`) and run under `gdb --args ./calc_metrics nightly.csv output_fixed.csv`, then `run` and `bt` on the SIGSEGV to see the exact line and call frame. Look specifically at two failure classes described in the ticket: (1) a record whose count field is `0` may have no third (values) field at all, so `strtok(vals_str, ",")` is called with a NULL pointer — guard against a NULL vals_str/zero count before tokenizing, and treat it as an empty value list yielding mean 0.0; (2) when the sum of reciprocals is exactly 0.0 the harmonic-mean formula divides by zero producing Inf/NaN, which then gets cast to int for histogram bucketing and indexes out of bounds — explicitly check for a zero (or non-finite) sum in the harmonic-mean function and force the result to 0.0 before it ever reaches the bucket-index cast. After patching both spots, recompile and rerun `./calc_metrics nightly.csv output_fixed.csv`, then inspect the produced CSV/histogram lines by hand to sanity-check they look plausible before considering the task done.
