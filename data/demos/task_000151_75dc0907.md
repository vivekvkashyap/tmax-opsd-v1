Write the Go pipeline as clearly separated stages: read raw_data.csv with `encoding/csv`, and for validation try `strconv.ParseFloat` on both latitude and longitude — skip the row if parsing fails (missing/non-numeric) or if the parsed value is outside [-90,90] for latitude or [-180,180] for longitude (use strict bound checks, not just non-negative). For email masking, find the `@` index with `strings.Index` and replace everything before it with `***`, keeping the `@domain` part verbatim — don't assume a fixed-length prefix. Compute Manhattan distance as `math.Abs(lat) + math.Abs(lon)`, then sort the surviving records with `sort.Slice` using a comparator that orders by distance ascending and, for ties, by `id` ascending (compare as integers, not strings, to avoid lexicographic ordering bugs). Write output with the exact header `id,masked_email,distance` and format distance with `fmt.Sprintf("%.2f", d)`. For the cron part, write setup_cron.sh so it captures the current crontab (`crontab -l 2>/dev/null`), appends the exact line `0 2 * * * cd /home/user && go run /home/user/process.go` if not already present, and reinstalls with `crontab -`, then run both the Go program once and the cron setup script.