Split this into two independent deliverables and verify each end-to-end. For Nginx: edit `/etc/nginx/nginx.conf` adding `location /auth/ { proxy_pass http://127.0.0.1:8081/; }` and `location /data/ { proxy_pass http://127.0.0.1:8082/; }` — note the trailing slash on `proxy_pass` is what strips the `/auth` or `/data` prefix before forwarding; test with `nginx -t` then reload/restart and curl both routes to confirm 200s before moving on. For the Go auditor: write it to read one line of stdin JSON, split the JWT on `.`, base64url-decode the header part using `base64.RawURLEncoding` (since padding may be absent), and unmarshal it — wrap every decode/unmarshal step in error handling that falls through to the `MALFORMED`/`DROP` path rather than panicking, since malformed tokens are explicitly fuzzed. Check `alg` case-sensitively against exactly `"none"`, `"NONE"`, `""`. For redaction, use `regexp.MustCompile` with literal `\d{3}-\d{2}-\d{4}` and `\d{4}-\d{4}-\d{4}-\d{4}` and `ReplaceAllString`, applying SSN before CC (or vice versa, non-overlapping) since order can matter if patterns could overlap. Never write to stdout except the single final JSON line — send nothing else there. Build with `go build -o /app/auditor /app/auditor.go` and manually pipe several crafted stdin lines to sanity-check exact output formatting before considering it done.
