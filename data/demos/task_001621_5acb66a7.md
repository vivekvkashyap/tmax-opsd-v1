Tackle this in build-system, algorithm, and audio-extraction pieces, then verify each independently before combining. First, fix the Makefile: the broken linking step likely omits `poly_filter.o` from the final link command and/or misses `-lm` (needed if `poly_filter.c` uses `pow`/math functions) — make sure both `main.o` and `poly_filter.o` are compiled and linked together. Add an `ARCH` conditional: default `CC=gcc` and output name `poly_tool_x86_64`, but if `ARCH=aarch64` is passed, switch `CC` to `aarch64-linux-gnu-gcc` and the output name to `poly_tool_aarch64` (use Makefile conditionals like `ifeq ($(ARCH),aarch64)` or `ARCH ?= x86_64` with variable-based naming). Second, transcribe `/app/memo.wav` (via `whisper` or similar) to recover the three spoken integer coefficients in the order they're stated, and inject them as C1, C2, C3 into `apply_filter(int x)` implementing `(C1*x^3 + C2*x^2 + C3*x) % 1000000007` using integer/modular arithmetic (avoid floating-point `pow` for exactness with large x, or take care with overflow — consider `long long`). Compile `poly_tool_x86_64` and spot-check the function against a few manual computations before considering the coefficients correct. Then write `ci_build.sh` to `apt-get install gcc-aarch64-linux-gnu` and build both architectures via `make ARCH=...`.