For the secret, search the git history of `/home/user/project` (e.g. `git log --all -p -- secret.env` or `git log --all -p | grep -i key`) since the deployment key was committed then removed in a later commit — recover the exact key string from the diff and save it to `/home/user/api_key.txt`. For the parser bug, look at `parse_config.sh`: it does `cut -d= -f2` on lines like `MULTIPLIER=1.5 # Safety margin`, leaving the trailing `# Safety margin` comment attached to the value before piping to `bc`, which breaks the arithmetic — fix it by stripping everything from `#` onward (e.g. `sed 's/#.*//'`) and trimming whitespace before the value reaches `bc`, applying the same comment-stripping to both `BASE_MEM` and `MULTIPLIER` extraction. Verify by running `build.sh` end to end after placing `api_key.txt` correctly and confirming exit code 0. For `mre.sh`, write a small standalone script that takes `$1`, strips the trailing `#...` comment and surrounding whitespace (same technique as above, e.g. via `sed`/`awk`/parameter expansion), echoes the clean numeric value, and is made executable with `chmod +x`.