First transcribe `/app/incident_report.wav` using a local speech-to-text tool (e.g. `whisper` CLI if available, or another installed ASR utility) to recover the spoken snapshot ID — listen for how the ID is spoken (letters/numbers spelled or said aloud with a connector like "dash") and convert it into the same slug format used in the Turtle file (lowercase-word-dash-number), don't hardcode a guessed value. Load `/app/backup_topology.ttl` with an RDF library (e.g. Python's `rdflib`), and build your HTTP handler so it executes a genuine parameterized SPARQL query at request time using the incoming `corrupted_id` query parameter — use a property path query like `SELECT ?dependent WHERE { ?dependent ex:dependsOn+ ex:<corrupted_id> . }` so it naturally handles chains of any depth via the `+` transitive operator, rather than writing your own manual graph traversal in Python. Strip each result URI down to its local name (the part after the last `/`) before returning it. Wire this into a simple HTTP server on `127.0.0.1:8080` at `GET /api/impact`, return `{"impacted": [...]}`, run it in the background, and confirm with `curl` using the ID you transcribed (not a value read from the graph itself) before finishing.
