Walk `/home/user/server_logs/` recursively (e.g. `os.walk` or `find`), and for each file ending in `.log` open it as text, for `.log.gz` open it via `gzip.open` in text mode — never assume all files use the same opener. Process line by line and wrap each `json.loads(line)` in a try/except that silently `continue`s on any parse error, so truncated trailing lines are dropped without crashing the whole run. Keep only records whose `level` field equals exactly `"FATAL"`, collect them all in memory, then sort by the integer `id` field before writing. Write output with `json.dumps(record, separators=(",", ":"))`-style compact serialization (no extra whitespace) one record per line to `/home/user/fatal_errors.jsonl`. Finally build `/home/user/fatal_backup.tar.gz` with Python's `tarfile` module (mode `w:gz`), adding the jsonl file with `arcname="fatal_errors.jsonl"` so it sits at the archive root regardless of its real path. Double check ordering and exact field content by re-reading the jsonl file back before packaging.