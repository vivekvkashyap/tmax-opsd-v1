First OCR the config image (`tesseract /app/config_snippet.png -`) to recover `MAGIC_SEED` and `XOR_KEY` — verify these look plausible before trusting them. Treat `/app/oracle_decoder` as a black box: feed it small, controlled inputs via stdin and observe stdout/exit codes to infer the wire format — likely a fixed-size length header followed by an encoded body; confirm header width/endianness by bisecting with inputs of known length. Hypothesize the transform (XOR with the recovered key, offset by seed) and test against the oracle on many payload sizes, especially near size boundaries, since a 32-bit signed overflow bug affects the decoding offset for large payloads. Reproduce that overflow deliberately in Python using fixed-width 32-bit signed arithmetic (mask to 32 bits, reinterpret top bit as sign, e.g. `((x + 2**31) % 2**32) - 2**31`) rather than Python's native big ints, and let this determine which XOR path is used. Iterate: run your script and the oracle on identical random byte streams and diff raw output bytes until they match exactly across the full size range.
