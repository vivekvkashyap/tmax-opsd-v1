Reverse-engineer the binary methodically rather than guessing. Use `strings /app/processor` and `objdump -d /app/processor` (or `gdb`/`radare2`) to locate the header validation routine — look for comparisons against constants near where the input buffer is read, since a stripped binary's bug is usually a mis-implemented boundary/XOR check on a small header. Cross-reference by scanning `/app/crash.dmp` for repeating byte patterns that could be a fixed-size header structure (try `xxd /app/crash.dmp | less` and look for a recurring magic value) — the dump should reveal the field layout (magic, length, checksum, etc.) and give you concrete "evil" values to compare against the disassembly's branch conditions. Once you have a hypothesis for the exact bug condition (e.g. some field relationship using XOR and a length threshold), construct minimal test files by hand and actually run `/app/processor` against them (with a timeout) to confirm which ones hang/spike CPU and which return normally — don't trust static analysis alone. Only after empirically confirming the rule should you encode it into `/home/user/filter.go`, then dry-run it against a handful of files from both corpora before assuming full correctness, since the grader checks every file in the clean and evil sets.
