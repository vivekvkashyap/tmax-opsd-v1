Use pure Bash text tools, no third-party log parsers. For the IP with the most failures, filter lines containing "Failed password", extract the IP token (it follows "from" — e.g. `grep 'Failed password' /home/user/auth.log | awk '{print $(NF-3)}'` or use a regex to pull the IPv4 pattern rather than a fixed field index, since field positions can shift with username length), then `sort | uniq -c | sort -rn` and take the top IP. For world-writable files, use `find /home/user/webroot -type f -perm -o+w` (the `-perm -o+w` form matches "others write bit set" regardless of other permission bits, unlike an exact `-perm 777` match which would miss files like mode 666) to list only files, not directories. Sort those paths alphabetically. Write `/home/user/audit_report.txt` with the IP as the first line and the sorted file paths as subsequent lines, with no extra headers, blank lines, or trailing content — the grader does an exact diff, so double check there's no stray trailing newline mismatch or extra whitespace introduced by your commands.
