First set up an isolated environment (`python3 -m venv /home/user/venv`, activate it, `pip install pandas scikit-learn numpy`) so version drift doesn't cause mismatches. Read `/home/user/etl.py` and locate the leakage: it currently calls `pca.fit_transform(X)` on the full dataset before `train_test_split`. Fix the ordering — split first into `X_train`/`X_test` with `test_size=0.2, random_state=42`, then `pca = PCA(n_components=2, random_state=42); pca.fit(X_train)`, and use `pca.transform()` (never `fit` again) on both `X_train` and `X_test` — fitting must only ever see training data. Keep the final aggregation (mean of PC1 over the test set) writing to `/home/user/etl_output.txt` exactly as before, just fed by the corrected split. Then write `/home/user/run_etl.sh` that sources the venv's activate script, exports `OMP_NUM_THREADS=1` and `OPENBLAS_NUM_THREADS=1` before invoking Python (these must be set prior to importing numpy/sklearn to take effect), and calls `python3 /home/user/etl.py`; `chmod +x` it and run it to confirm `/home/user/etl_output.txt` regenerates with a plausible single float value. Note PCA sign can flip between runs/versions — that's expected and not a bug to chase.
