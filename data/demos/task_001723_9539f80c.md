Transcribe `/app/incident_report.wav` (via `whisper`/`ffmpeg`+STT/etc.) and carefully extract the specific base IP octets and base port numbers spoken in the recording — these are the constants your router logic needs, don't guess them. Since a compiled oracle (`/app/oracle_proxy_check`) is available, use it as a black-box behavioral spec: run it on many sample 16-character hex strings (e.g. all-zero, all-f, incrementing patterns, and random ones) and observe how the output IP octets and port change as you vary specific hex substrings, to infer which byte ranges of the header map to which output fields — do not disassemble or copy the oracle's internals, just observe its input/output behavior and combine that with the audio-derived constants to reconstruct the algorithm in Python. Write `/home/user/proxy_check.py` accepting one 16-char hex argument, computing the IP/port per your inferred formula, and printing exactly `IP:PORT`. Before finishing, write a small differential test script that generates many random 16-char hex strings, runs both your script and the oracle, and diffs their outputs — fix any mismatches by refining your parsing of which hex slices map to which octets/port bits.