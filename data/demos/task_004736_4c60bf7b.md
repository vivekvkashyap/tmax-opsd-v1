Start by inspecting the database rather than assuming column names: run `sqlite3 /home/user/audit_logs.db ".schema"` and `.tables`/`SELECT * FROM <table> LIMIT 5` to discover the actual table and column names for actor, target, timestamp, risk score, and context tag. Once you know the schema, write the C++ program using the sqlite3 C-API: open the DB with `sqlite3_open`, prepare a statement with `sqlite3_prepare_v2` using a `?` placeholder for the context tag, and bind it with `sqlite3_bind_text` (never string-concatenate the tag into the SQL). Use a SQL window function to compute the rolling sum: `SUM(risk) OVER (PARTITION BY actor ORDER BY action_time ROWS BETWEEN 2 PRECEDING AND CURRENT ROW)` inside a CTE or subquery, then an outer `GROUP BY actor` taking `MAX(rolling_risk)`, ordered by that max descending and actor name ascending, limited to 5 rows. Fetch rows with `sqlite3_step`/`sqlite3_column_*`, format each line exactly as `Actor: <name>, Max Rolling Risk: <score>` with `%.2f`-style two-decimal formatting, and write to `/home/user/suspicious_users.log`. Compile with the given g++/sqlite3 flags and run with the `PROD` argument — remember fewer than 5 actors may exist for a given tag, so only emit as many lines as actually qualify.