For keyframes, use `ffprobe -v error -select_streams v:0 -show_entries frame=pkt_pts_time,pict_type -of csv=print_section=0 /app/foraging_behavior.mp4`, filter rows where picture type is `I`, and write timestamps sorted ascending to a CSV with header `timestamp` (watch for missing pts on some frames). For cycle detection, treat `parent_id` as a directed edge child->parent; for each node walk up the chain using a visited-set (or white/gray/black coloring) so cycles anywhere in the forest are caught, not just at roots — exit 0 only if no walk revisits an in-progress node, else exit 1; verify against a hand-built tiny cyclic example first. For the schema, create the composite index as `CREATE INDEX idx_keyframes_video_time ON keyframes(video_name, timestamp);` — column order matters, filter column first then sort column, so range+order-by queries can use it. For the recursive path query, write a `WITH RECURSIVE` CTE starting from rows where `parent_id IS NULL` (base case, path = name), unioning rows that join child to parent (path = parent.path || ' > ' || child.name), selecting id, name, path ordered by id.
