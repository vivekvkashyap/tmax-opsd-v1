Treat this as reverse-engineering plus safe reimplementation, not a rewrite from scratch: read legacy_auth.cpp to confirm the token format (USERID-TIMESTAMP-SIGNATURE) and the exact signature formula already given (USERID*3 + TIMESTAMP + MASTER_PIN), noting where the legacy code's unsafe parsing (e.g. raw scanf/sscanf without bounds or fixed buffers) is the actual defect to fix — your new parser should validate structure explicitly (three hyphen-separated fields, digits only, no overflow) rather than trusting scanf. Separately, run OCR (e.g. `tesseract`) on /app/admin_dashboard.png to recover the 4-digit MASTER_PIN embedded in the dashboard text — read the OCR output carefully to extract just the PIN, don't guess a placeholder value. In check_auth.cpp, parse the token with strict validation, reject anything that isn't exactly two hyphens with positive-integer USERID and TIMESTAMP fields, compute the expected signature using the recovered PIN, compare to the SIGNATURE field, and print VALID or INVALID — guard against overflow when parsing large numeric strings (e.g. use strtoll with range checks) so malformed or oversized inputs never crash, matching the legacy tool's valid-token behavior exactly while being safe on everything else. Compile to /home/user/check_auth and test with both well-formed and garbage inputs before considering it done.
