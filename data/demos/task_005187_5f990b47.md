Do this in clearly separable stages and verify each before moving on. First, run `tesseract` against the calibration image to OCR the multiplier text, then parse it out of the `CALIBRATION_MULTIPLIER=X.XX` string programmatically (or hardcode after confirming the OCR result, but don't guess it) — sanity-check the OCR output since misreads (e.g. a stray character) will silently poison every downstream calculation. Second, write the C TCP server: a normal socket/bind/listen/accept loop that keeps serving after each connection closes. On each `EVAL <N>` request, load the CSV, multiply by the calibration value, split at index N, and — the critical correctness point — standardize the *test* set using the *training* set's mean/std (never recompute test's own mean/std), which is exactly the leakage bug you're asked to fix. For the bootstrap, use GSL's RNG to resample the scaled test set with replacement 1000 times, take each resample's mean, sort the 1000 means, and read off the values at the 2.5th/97.5th percentile positions. Format both floats to exactly 4 decimals in the "CI: <lower>, <upper>\n" response, and test manually with a raw socket client before finishing.
