Build this as three independently checkable stages, not one monolithic script, so numerical mistakes don't compound silently. First implement the ODE system `y'' + 0.1y' + w^2 y = 0` as a first-order system for `solve_ivp` (state = [y, y']), integrate with `t_eval = np.linspace(0, 100, 1000)` and `rtol=1e-6, atol=1e-8`, and derive/hardcode the analytical underdamped solution `y(t) = e^{-0.05t}(cos(wd*t) + (0.05/wd) sin(wd*t))` with `wd = sqrt(w^2 - 0.05^2)` — validate at w=5.0 by comparing your numeric and analytical arrays pointwise and taking the max absolute difference; this should come out very small, confirming your integrator setup before trusting it elsewhere. Second, seed `np.random.seed(42)` exactly once, draw 50 omegas from `U(2,8)`, and for each rerun the same solver settings. Third, for spectral analysis use `np.fft.rfft` on each y(t) with matching `dt = 100/999` and `np.fft.rfftfreq(1000, d=dt)`, zero out the DC bin before taking argmax so you don't pick frequency 0, and collect the 50 dominant frequencies in Hz (not angular). Finally fit `scipy.stats.gaussian_kde` on those 50 frequencies (default bandwidth) and evaluate it at `5.0/(2*np.pi)`. Write all three floats to the specified JSON keys.
