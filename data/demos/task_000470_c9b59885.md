Build this as filter -> sort -> C-process, verifying each stage's intermediate output before moving on. Use shell tools to prefilter: `grep 'SENS_88' raw_telemetry.txt` then `sort -t';' -k1,1n` to get chronological order by timestamp (numeric sort on the semicolon-delimited first field), and feed that sorted stream into your compiled C program (via a pipe or an intermediate temp file) rather than doing all logic in C. In the C program, tokenize each line on `;` with `strtok`/`strsep`, keep a small sliding window (array or circular buffer of the last up to 3 temperatures) to compute the moving average — for the first line average over 1 value, second over 2, third onward over exactly 3 — and print each average with `printf("%.2f", avg)`. For notes cleaning, iterate the string byte-by-byte and only copy characters whose value is in [32,126] (this correctly strips multi-byte UTF-8 accented characters since their continuation bytes fall outside that ASCII range, leaving only the printable letters behind) — don't use a locale-aware or wide-char strip, since a simple byte-range filter is what's required here. Compare your CSV output line-by-line against expectations for each field before finalizing.
