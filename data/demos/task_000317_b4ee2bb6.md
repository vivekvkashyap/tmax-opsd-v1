Design the C program around a hash map keyed by bucket timestamp so you can do a single pass over stdin: for each line, parse the three space-separated fields, floor the timestamp with `ts - (ts % 60)` (integer arithmetic, watch for negative timestamps if any — floor division must round toward -infinity, though here inputs are likely positive so simple modulo works), look up or insert the bucket, and update `max` for the metric matching the code (1/2/3) only if the new value is greater — initialize all three metrics to 0.00 per bucket so missing metrics correctly default. Since the number of distinct buckets is unknown up front, use a dynamically growing array or a simple open-addressing/linked hash table rather than a fixed-size array indexed by raw timestamp. After processing all input, sort the buckets by timestamp ascending (`qsort` with a comparator) and print each line with `%ld,%.2f,%.2f,%.2f` formatting. Compile with `gcc -O2` (or similar) to `/home/user/process_logs`, pipe `raw_metrics.log` through it, and redirect to `/home/user/aggregated_metrics.csv` — verify row count and spot-check a few buckets by hand against the raw log before trusting the full output.
