Extract per-frame mean intensity with OpenCV (`cv2.VideoCapture`, loop `cap.read()` for all 100 frames, compute `frame.mean()` per frame) to reconstruct the I(k) array, then convert frame index k to frequency with the given linear relation ω=100+5k before fitting — fitting in the wrong domain (k instead of ω) will shift your peak positions. Use `scipy.optimize.curve_fit` with a double-Lorentzian-plus-baseline model function matching the formula exactly, and pick reasonable initial guesses (e.g. from visually inspecting where the intensity array peaks, or using `scipy.signal.find_peaks` on the reconstructed spectrum to seed the two center positions) since nonlinear fits are sensitive to initialization and could converge to a degenerate/single-peak solution otherwise. Enforce or check ω1<ω2 in your output regardless of fit parameter order. Serve the result via a lightweight standard-library HTTP server (or Flask/FastAPI) bound to 127.0.0.1:8000 with `GET /api/peaks` returning the two centers rounded to 1 decimal in the exact JSON key format specified, and keep the process running so the verifier can poll it with retries.
