Decode the audio signal first and confirm the recovered text independently before touching performance or the Makefile. Use `od`/`hexdump` (or write a tiny throwaway Python/C probe just to explore, even though the final deliverable must be pure Bash) to inspect the raw bytes after the 44-byte WAV header, confirm sample width/endianness, and manually verify your understanding of the block-averaging threshold and the Hamming-like parity check on a few sample blocks by hand before automating it — this is where naive off-by-one errors (block size 8, bit-order MSB-first, discarding blocks with mismatched parity, only assembling from valid nibbles pairwise) most often creep in. Once your Bash script `/home/user/fast_decode.sh` produces plausible ASCII output matching the described dependency-version format, only then worry about speed: avoid spawning subshells or external processes (`expr`, `bc`, `awk`, pipelines) inside the per-sample or per-block loop — read the whole file's bytes into a Bash array once (e.g. via `od`/`read -a`), then do all arithmetic with `$(( ))` and array indexing so the hot loop is pure builtins; time it with `time ./fast_decode.sh ...` and iterate until comfortably under 2 seconds. Only after the script's output is verified correct should you edit `/app/src/Makefile`, replacing the hardcoded wrong version-numbered library paths with the versions you decoded, then run `make` in `/app/src` to confirm `main` builds.