Recover the deleted-but-staged file via git's object store, not by guessing its content: run `git fsck --lost-found` (or `git fsck --unreachable`) inside `/home/user/project` to list dangling blobs, then `git cat-file -p <blob-sha>` each candidate until you find the JSON content matching what `build.log`'s traceback implies (a list of objects with an `id` and a `weight` field, one of which is a string causing the `TypeError` in the log) — write that exact recovered content to `/home/user/project/recovered.json`. For the fix, read `build.log` closely: the crash is `TypeError: unsupported operand type(s) for +=: 'int' and 'str'` on `item['weight']`, so in `parse_config.py` coerce/validate `weight` per-item (accept int, default to `0` if it's a string, missing, or otherwise invalid — use `.get()` plus a type check, not a bare `try/except` around the whole loop) and wrap the top-level `json.load` in a `try/except json.decoder.JSONDecodeError` that prints "Invalid JSON" and calls `sys.exit(0)`. Write `/home/user/project/fuzz.sh` as a `chmod +x` bash loop of exactly 10 iterations writing random `/dev/urandom` bytes to `test.json` and invoking `python3 parse_config.py test.json`, checking `$?` each time; run it once and only write `SUCCESS` to `fuzz_result.txt` if all 10 iterations actually exited 0.