Since the JSON schema is fixed and flat, avoid a general JSON parser in C — just scan each stdin line for the literal substrings `"event":"` and `"user":"` etc. with `strstr`/manual pointer scanning, which is both fast and robust to the malformed lines. Filter lines where the event value isn't exactly `login`. For the timestamp, parse the `YYYY-MM-DDThh:mm:ssZ` fields as integers by fixed offsets and convert to Unix epoch yourself with a UTC-only days-since-epoch calculation (accounting for leap years) rather than relying on `mktime`, which is timezone-sensitive; verify your conversion against a known date/epoch pair before trusting it. For `\uXXXX` decoding, scan for the 6-character sequence, parse the 4 hex digits, and since you're told to only handle 0x20–0x7E, just cast to `char` and emit it. In the bash orchestrator, use `split -l 5` (or similar) into `/home/user/tmp_chunks/`, then run `ls tmp_chunks/* | xargs -P 4 -I{} /home/user/parser < {}` capturing stdout, concatenate all chunk outputs, and pipe through `sort -t, -k1,1n` (numeric sort on the epoch column) before writing the final CSV — don't sort lexicographically, since epoch strings can differ in digit count edge cases.
