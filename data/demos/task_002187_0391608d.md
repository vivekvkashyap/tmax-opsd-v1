Start the extraction stage as a shell pipeline: `ffmpeg -i /app/telemetry_backup.mp4 -vf fps=2 frame_%04d.png` to pull frames, then loop `tesseract frame_XXXX.png stdout` (or output to files) concatenating all recognized text into one combined log for the Go stage to consume — don't assume OCR is clean, expect stray characters and partial lines. In the Go program, compile a regex like `\[TELEMETRY\]\s+NODE:([A-Za-z0-9]{4})\s+P:([\-0-9.]+)\s+V:([\-0-9.]+)\s+A:([\-0-9.]+)` and scan the combined text line-by-line (bufio.Scanner), silently skipping any line that doesn't match — this naturally filters OCR garbage. For each match, parse the four float/string fields, compute the pseudo-energy `E = P*V*V + math.Sqrt(A)`, and accumulate a running sum in a `map[string]float64` keyed by NODE. After processing all lines, marshal the map to JSON and write it to `/home/user/energy_totals.json`. Because grading tolerates OCR noise (MAE threshold), focus on correctness of the regex and formula rather than perfect OCR; test the regex against a few hand-typed sample lines including intentionally malformed ones before running on the full extracted text.