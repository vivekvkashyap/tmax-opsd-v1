Read `/home/user/telemetry.jsonl` line by line and wrap each `json.loads(line)` call in a try/except `json.JSONDecodeError` (or broad `except Exception`) that simply `continue`s past the bad line — do not try to repair the malformed `\u` escapes, just drop those lines entirely as instructed. Load survivors into a list of dicts, mask `user_ip` to `"***"`, sort by `ts` ascending, then compute the rolling average of `metric` with a window of 3 using only the trailing available points (i.e. `mean(metric[max(0,i-2):i+1])`) — the common bug here is using a fixed-size window that requires 3 full periods and mishandles the first two rows, or computing the average before appending the current row instead of including it. Compute `is_anomaly` as a strict `>` comparison against `rolling_avg + 20.0`, using the *already-rounded* or unrounded rolling average consistently with how the spec's example was derived (compare before rounding, then round only for the CSV output column). Write out with pandas `to_csv(index=False)` or Python's `csv` module with columns in the exact order `ts,user_ip,metric,rolling_avg,is_anomaly`, rounding `rolling_avg` to 2 decimals just before writing. Run the script and manually check the printed CSV against the arithmetic before finishing.
