Work through the three sub-problems independently before wiring them together. First, fix the header cycle: pick one direction of dependency and break the other with a forward declaration (`struct DecodedData;` / `struct EncodedData;`) plus pointer members instead of full includes — never delete either header. Second, fix the Makefile: the object-file rules must depend only on their own `.c`/`.h` inputs, not on each other's `.o` (e.g. `encoder.o` should not depend on `decoder.o`); only the final link target should depend on all three object files. Third, implement URL parsing in `main.c` by locating the literal substring `/api/encode` at the start of argv[1]; if absent, print `404 Not Found` and exit 1. Otherwise use `sscanf` or manual string splitting on `?`, `=`, and `&` to pull out `shift` as an int and `data` as the remainder string, being careful not to include the trailing newline or extra chars. In `encoder.c`, iterate each byte of `data`, compute `(unsigned char)(c + shift) % 256`, and emit two uppercase hex digits per byte via `sprintf(out, "%02X", byte)`. Rebuild with `make`, then redirect the required invocation's stdout to run.log.
