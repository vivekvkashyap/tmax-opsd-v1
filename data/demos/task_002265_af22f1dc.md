Load the graph with `rdflib.Graph().parse('/home/user/dataset.nt', format='nt')` and inspect the predicates to confirm the `foaf:name` and custom `collaboratesWith` URIs. For the parameterized query, write a SPARQL string with a variable placeholder (e.g. `?name`) and bind the actual name at execution time via `g.query(query, initBindings={'name': Literal("Dr. Alice")})` — never string-format or concatenate the name into the query text, since that's exactly the anti-pattern being tested. Remember the collaboration edge may only be stored in one direction, so query both `?s collaboratesWith ?alice` and `?alice collaboratesWith ?s` (or UNION them) to get all direct collaborators, then write the comma-joined names. For the graph traversal, build a `networkx.Graph()` (undirected, since collaboration is symmetric) by adding an edge between the two names for every `collaboratesWith` triple regardless of direction, then use `nx.shortest_path`. For the algebra step, call `rdflib.plugins.sparql.prepareQuery(query_string)` and inspect/stringify its `.algebra` attribute, writing that string to the output file — don't hand-write a fake plan string.
