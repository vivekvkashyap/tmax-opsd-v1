Write the script with `set -euo pipefail` and `export LC_ALL=C` at the top so numeric comparisons in awk are locale-independent. Use awk with `-F,` to process the CSV: keep the header line unconditionally (NR==1), then for data rows drop if `$2 < 0` (numeric comparison) or if `$3` is empty, "NaN", or "NA" (use `==` string checks, remembering CSV fields may have no surrounding whitespace here). Write matching rows to the output path given as `$2` (second script argument), reading input from `$1`. After writing the output, compute `md5sum` of both the input and output files (strip the trailing filename/whitespace `md5sum file | cut -d' ' -f1` or use `awk '{print $1}'`), count output lines with `wc -l < outfile`, and append a single formatted line `IN:<hash> OUT:<hash> LINES:<count>` to /home/user/experiments.log with `>>` (don't overwrite prior runs). Run the script once as specified and diff clean_data.csv against your own manual trace of the filtering rules to confirm correctness before considering it done.
