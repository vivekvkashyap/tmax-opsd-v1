Start by reverse engineering /app/auth_daemon with `strings` (look for a format string that builds a shell command with flags for username/password) and `objdump -d` to confirm exactly how the binary constructs and invokes the subprocess — note the literal helper binary path and flag names it uses, and how the raw user/password values get interpolated (likely single-quoted, which is what makes injection via embedded quotes possible). Then write detector.py so it does NOT hardcode filenames: for each JSON file in input_dir, load the array of command-line args, join/inspect them, and flag as evil (writing the base filename to output_log) if the joined command matches the daemon's specific invocation pattern with visible credential flags, OR if it contains an injection breakout (e.g. an unescaped single quote followed by shell metacharacters/additional commands) within that same command structure. Keep the match narrow to the specific pattern you reverse-engineered so unrelated/benign `check_user` calls and generic shell activity aren't false-flagged. Test against a few synthetic clean/evil examples you construct yourself before relying on the hidden corpus.