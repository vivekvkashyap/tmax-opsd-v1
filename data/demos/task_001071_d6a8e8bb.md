This is a classic startup-ordering/race-condition fix, not a logic rewrite. Read all three existing files first (`telemetry_server.py`, `aggregator.c`, `start.sh`) to understand the current (broken) sequencing before changing anything. In `aggregator.c`, wrap the existing `connect()` call in a retry loop: on failure, `sleep(1)` and retry, up to 10 attempts, only treating it as a fatal failure after exhausting retries; keep the read/write-to-log logic otherwise intact but update the success message format if needed, then recompile with `gcc aggregator.c -o aggregator`. In `start.sh`, the key fix is ordering plus bridging the port mismatch: start the Python server in the background, then start `socat TCP-LISTEN:8080,fork,reuseaddr TCP:127.0.0.1:9090 &` in the background so traffic aimed at 8080 reaches the server's real 9090 port, and only then invoke `./aggregator` in the foreground — the retry loop in the C code is what absorbs the remaining race between socat/server startup and the aggregator's first connection attempt. Check `socat` is installed (install via package manager if missing, no root required if using a user-writable prefix or checking it's already present). Run `start.sh` for real and confirm `/home/user/obs_stack/dashboard.log` is created with the expected content before declaring success.