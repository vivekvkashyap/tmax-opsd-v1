Start by triaging with a quick script that runs `/home/user/telemetry` against every file in `/home/user/packets/` (capturing exit status, e.g. `for f in ...; do ./telemetry "$f"; echo "$f: $?"; done`) so you know exactly which files segfault (exit 139) versus succeed, without needing gdb on all 200+ files. Then compare the first few bytes of the crashing files against the clean ones with `hexdump -C` or `xxd` to spot the distinguishing byte pattern shared only by the crashers. Cross-reference that pattern against `/home/user/telemetry.c`: look specifically at how the code branches on the first header byte and then derives a size/length from a later byte — an off-by-one or unsigned/signed underflow when that second byte is a particular value (e.g. zero) is a classic root cause of a malloc-then-null-dereference crash. Confirm your hypothesis by testing whether hand-crafted packets with that same 2-byte prefix reliably crash the binary and packets without it never do. Write the confirmed 2-byte prefix to `/home/user/root_cause.txt` as two `0xNN` tokens separated by a space.