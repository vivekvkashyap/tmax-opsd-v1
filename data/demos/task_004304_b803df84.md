Write `audit_trace.sh` to take `$1` as the access_id and pass it into sqlite3 as a bound parameter, not string-interpolated into the SQL text — use `sqlite3 -cmd ".parameter set :id $1" /home/user/audit.db "..."` or safer, build the query with `.param` binding via a heredoc, so injection/quoting issues can't occur even though this is described as an internal tool. Before the main query, run `REINDEX;` against the database to address the corrupted `idx_timestamp` index. For the recursive CTE, seed it with the row matching the given `access_id`, then recursively join `access_logs` where `parent_access_id` equals the previous row's `id`, walking the chain forward (not backward toward parents). Order the resulting chain chronologically by `timestamp`. Apply a window function `ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY timestamp)` as `user_seq` so each user's own actions within the chain are numbered independently starting at 1, not a global row count. Emit as CSV with header exactly `id,user_id,doc_id,timestamp,user_seq` using `.mode csv` and `.headers on` in sqlite3, then run the script with argument `1` and redirect stdout into `/home/user/audit_result.csv`; diff the result against your own manual trace of the parent-child chain to catch off-by-one sequencing errors.