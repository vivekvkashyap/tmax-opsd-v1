First recover the configuration parameters from `/app/sensor_config.png` with `tesseract /app/sensor_config.png -` (or convert to a higher-contrast image first if OCR is noisy) — read off the missing-character indicator, the max forward-fill length, and the fallback character exactly as printed; do not guess these values. Then write the Go program as a strict character-by-character state machine: validate the whole line matches only uppercase letters and the missing-indicator (empty input or any other character → print "INVALID", exit 1); otherwise scan left to right keeping the last-seen valid letter and a running fill counter that resets to 0 on every real letter — for each missing-indicator character, if a prior valid letter exists and the counter is below the max, emit that letter and increment the counter, otherwise emit the fallback character (this also covers gaps at the very start of the string, where there is no prior letter). Strip only a trailing newline from stdin before processing, and print the transformed string with no added newline. Since correctness is checked by byte-exact comparison against a hidden reference across thousands of random fuzzed inputs, test edge cases yourself: empty string, string of only missing-indicators, gaps longer than the max, and gaps at position 0.
