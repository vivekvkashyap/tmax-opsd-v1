Solve this in two independent phases and verify each before combining. Phase 1: extract frames from `/app/rotation_alert.mp4` (e.g. `ffmpeg -i rotation_alert.mp4 frame_%02d.png`), then run each frame through `pyzbar.decode` (installing `pyzbar`/`Pillow`/`opencv-python` as needed) to find the one frame with a QR code and read its decoded string — that recovered string is your new session token; don't hardcode any guessed token. Phase 2: write `/home/user/detector.py` to load the JSON request file and apply checks in this order for correctness: first check token validity (compare the recovered token against `headers.Authorization` stripped of the `"Bearer "` prefix, OR `cookies.session_id`; if neither key exists or neither matches, exit 1 immediately) — always guard dict lookups with `.get()` so missing keys don't throw; then, only if `X-Encoded-Payload` is present, base64-decode it (catching decode errors as a reject) and case-insensitively search the decoded text for `drop table`, `1=1`, and `<script>`, exiting 1 on any match; otherwise exit 0. Test your script by hand against a few requests you construct with and without the correct token and with each malicious payload variant before running it against the hidden clean/evil corpora.