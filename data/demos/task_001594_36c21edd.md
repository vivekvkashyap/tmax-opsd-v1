Treat `/app/sim_engine` as an opaque black box and diagnose instability empirically rather than guessing — the filter must decide based on repeated observed behavior, not any assumption about the binary's internals. Method: (1) For each parameter file in the clean and evil corpora, invoke the binary many times (e.g. 20-50 repetitions) on the same file, parsing `final_energy` from stdout each time with grep/regex. (2) Compute a bootstrap 95% confidence interval over each file's collection of observed `final_energy` values (resample with replacement from the observed runs, compute the sample mean many times, take the 2.5th/97.5th percentiles) — a naive single-run check will miss this entirely, since instability only shows up across repeated invocations. (3) Pick a CI-width threshold that cleanly separates the two corpora — validate it explicitly on both corpora before finalizing, tightening/loosening until 100% clean-preserved and 100% evil-rejected. (4) Implement this as `/home/user/filter_sims`, taking a directory argument, iterating its files, and printing only the absolute paths of files classified stable, one per line, nothing else.