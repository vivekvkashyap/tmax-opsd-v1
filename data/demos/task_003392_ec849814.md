Work in strict pipeline order and validate each stage's output before starting the next. First reassemble the split legacy tarball: `cat /home/user/legacy_split/docs.tar.gz.part_* > docs.tar.gz` (parts sorted lexically already gives correct order) then `tar -xzf docs.tar.gz -C /home/user/extracted_legacy/` and confirm the expected `legacy_docs/` subtree appears. Next write the Rust comparison tool: for each file under `current_docs/`, look up the same relative name under `extracted_legacy/legacy_docs/`; classify as new (no legacy counterpart) or modified (counterpart exists but byte content differs — compare full contents, not just size/mtime) and skip unchanged files entirely. For each new/modified file, copy it into `/home/user/diff_docs/`, renaming only files that start with `draft_` by replacing that prefix with `prod_` (preserve any subdirectory structure). Compile with `rustc` (or cargo) and run it, checking `diff_docs/` contents match expectations. Then `tar -czf diff.tar.gz -C /home/user diff_docs` so the archive root is `diff_docs/`, and finally `split -b 100 diff.tar.gz /home/user/final_archive/diff.tar.gz.chunk_`. Verify by reversing the split/tar steps yourself.
