Follow the four steps in the exact order and with the exact numeric conventions specified, since small deviations (e.g. rounding, seed placement, sort order) will silently produce a different but plausible-looking answer. For each `event_id` (iterate in ascending numeric order, not string order), pull its `current_val` list, remove exactly one instance of the max and one instance of the min (use `list.remove`, not a set-based filter, to correctly handle duplicates), average the rest, then pick the reference base whose `mean_current` is closest in absolute difference — keep this "observed event mean" list around for step 3. Join predicted bases with hyphens for the sequence. For the KS test, call `numpy.random.seed(42)` exactly once right before the sampling loop begins (not per-base), then for each predicted base in order draw exactly 100 samples via `numpy.random.normal(mean, std, 100)` and flatten them into one array — call order and seed placement determine the exact simulated values, so don't reorder or reseed per iteration. Run `scipy.stats.ks_2samp(event_means, simulated_ref)` and round the p-value to 4 decimals. Write only `sequence` and `ks_pvalue` keys to `/home/user/analysis_result.json` as valid JSON.
