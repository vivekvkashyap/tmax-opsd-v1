Build the three pieces in order and test each in isolation before wiring them together. (1) In `pipeline.py`, extract frames from `/app/video_sample.mp4` with `ffmpeg` or `opencv-python-headless`, convert each to grayscale by averaging R/G/B per pixel, compute MSE between consecutive frames (index i vs i-1, starting i=1), and dump the list of floats as JSON to `/home/user/mse_results.json`. (2) For `generate_plot.py`, the bug is calling `plt.show()` in a headless container, which can throw or leave the figure cleared before `savefig`; fix it by setting the `Agg` backend (`matplotlib.use('Agg')` before importing pyplot, or set `MPLBACKEND=Agg`) and removing/guarding the `show()` call, then verify the resulting PNG is non-trivial (check file size or pixel variance, not just existence). (3) In `server.py`, run an HTTP server (Flask or `http.server`) on 0.0.0.0:8080 for GET /stats returning `{"max_mse_frame": i, "max_mse_value": v}` computed from the JSON results, and separately a raw socket server on 0.0.0.0:9090 that reads until it sees the literal "PLOT\n", then streams the PNG file's raw bytes back and closes the socket cleanly. Launch both listeners (e.g. via threads or subprocesses) and background the whole service, then manually curl :8080/stats and use `nc`/a small python socket client against :9090 to confirm both protocols work end-to-end before finishing.
