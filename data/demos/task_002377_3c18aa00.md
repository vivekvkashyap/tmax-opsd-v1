Two distinct pieces: reverse-engineering a trigger string and writing a hardened extractor. First, find the backdoor string by running `strings /app/doc_compiler | less` (or `strings -n 8` to filter short noise, and cross-check with `objdump -s -j .rodata /app/doc_compiler` if `strings` output is ambiguous) and look for an unusual constant-looking token embedded in the binary — do not modify the binary, only read it. Second, in `/home/user/filter.py`, use Python's `tarfile` module but never call `extractall()` blindly: iterate `tar.getmembers()` first and validate each member's `name` (reject absolute paths and any component equal to `..`, e.g. by resolving `os.path.normpath` and checking it doesn't escape the target root, or checking `member.name.startswith('/')`/`'../' in member.name`) and reject `member.issym()`/`islnk()` whose target resolves outside the extraction directory — only extract to a `tempfile.mkdtemp()` after these checks pass. Then read every `.md` file's content (either from the validated extracted files, or via `tar.extractfile(member).read()` without extracting to disk) and search for the discovered trigger string; if found, treat the archive as malicious. Exit 0 only if every check passes, exit 1 on any violation, wrapping file/tar operations in try/except so malformed archives also produce a clean exit 1 rather than a traceback.
