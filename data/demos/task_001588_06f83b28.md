Decode `manifest.b32` with Python's `base64.b32decode` (mind that base32 alphabet is uppercase and padded with `=`; read the raw file bytes, don't strip characters that matter) and parse the resulting CSV with the `csv` module rather than naive string splitting, since fields could contain commas inside the expression in principle. For `version_expr`, do not use `eval()` blindly on untrusted input in a real system, but for this task you can safely evaluate the arithmetic expression respecting parentheses and standard precedence — either use Python's `eval()` restricted to a math-only context, or `ast.literal_eval`-style safe parsing via the `ast` module with a whitelist of allowed node types (Add/Sub/Mult/Div/parens), then cast to `int`. Drive compilation per row based on the `language` column: `gcc <source_file> -o <output>` for C, `go build -o <output> <source_file>` for Go, naming the output `<service_name>_<version>` and placing it directly in `/home/user/release/` (create that directory first). After each successful compile, record `service_name -> version` in a dict and dump it as JSON to `/home/user/release_log.json`. Run the script and confirm the binaries are real ELF executables (`file` command) before finishing.