Install HDF5 dev libraries and a C++ HDF5 library binding (or use the `h5cpp`/official C API) to read `/app/raw_data.h5`'s `covariances` dataset (50x10x10 float64) into memory. For each matrix, flatten it row-major into 100 space-separated floats, pipe them to `/app/oracle_dist` via stdin, and capture its stdout as the 10-float target distribution P for that index — treat the binary purely as an opaque oracle (you don't need to know or reimplement its internal algorithm) but do cache each of the 50 results at startup so the HTTP server doesn't re-invoke the binary on every request. Build the HTTP server with a header-only library like `cpp-httplib` plus `nlohmann/json`, binding exactly to `127.0.0.1:9000`, implementing `POST /metric`: check the `Authorization: Bearer secret-ml-token-99` header first (401 if missing/wrong), validate `index` is in range and `candidate` has 10 elements (400 otherwise), then compute KL divergence D_KL(P||Q) = sum(P_i * log((P_i+1e-9)/(Q_i+1e-9))) using the cached P for that index and the request's candidate as Q, returning `{"distance": ...}`. Start the server in the background, verify with a manual curl (including the auth header) that it responds correctly, then create `/tmp/server_ready` only after confirming the server actually answers requests.