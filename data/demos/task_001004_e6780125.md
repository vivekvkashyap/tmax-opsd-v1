Work the three sub-problems independently before wiring them together. For the video, extract frames at a fine enough rate to catch a single-frame flash (`ffmpeg -i /app/evidence.mp4 -vf fps=<high> frames/%04d.png`, or sample every frame) and run `zbarimg` over each frame until one decodes a QR payload of the form `KEY=<hex>` — record that hex value, you'll need it as a decoder argument. For the decoder, read the compiler/linker error first (a missing math library flag like `-lm` typically manifests as an undefined-reference error for a libm function such as `floor`) and add it to the Makefile; then run the binary under a debugger or with sanitizers to locate the crash, which is very likely a loop bound reading one element past a buffer's allocated size — fix the comparison operator, don't just enlarge the buffer. Separately inspect the source for an `getenv(...)` check that gates execution and export that exact environment variable/value before invoking the decoder. Finally, write `detector.sh` to export the env var, run `./decoder <payload> <key>`, and grep its output for the exact marker string that signals malicious content, exiting 1 if found and 0 otherwise — validate against both provided corpora before finalizing.
