Do Part 1 first and confirm it independently: `mkdir -p /home/user/frames`, run `ffmpeg -i /app/camera_01.mp4 -vf fps=1 /home/user/frames/frame_%03d.jpg`, then `tar -czf /home/user/reference_frames.tar.gz -C /home/user/frames .` (or `--transform`/relative paths) so the archive has no leading directory component — verify with `tar -tzf` that entries look like `frame_001.jpg` not `./frames/frame_001.jpg`. For Part 2, write `/home/user/verify_archive.sh` to reject on any of: (a) `tar -tvzf` failing/exiting non-zero → corrupted, treat as EVIL; (b) any entry whose type is symlink — resolve where it points (absolute path, or relative path normalized against its position in the extracted tree) and reject if that resolved path would escape the extraction root (i.e. starts with `/` or normalizes to something outside `.`); (c) any entry basename exactly `lock` or `.lock` (file or directory). Use `tar -tvzf archive` to list types/names/link-targets without actually extracting (safer, and lets you catch malicious symlinks before they touch disk). Print exactly `CLEAN`/`EVIL` and exit 0/1 accordingly. Test against every file in `/app/corpora/clean/` and `/app/corpora/evil/` before considering it done, since it must hit 100% accuracy including hidden test cases.