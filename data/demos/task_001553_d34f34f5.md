Since the schema is undocumented, start by inspecting it directly: run `sqlite3 /home/user/legacy.db ".schema"` and sample rows from t_alpha/t_beta/t_gamma to infer that t_alpha is Users, t_beta is Posts (with an author FK and a publish date), and t_gamma is a Connections/edges table (two user FK columns) — verify your inferred column names against `.schema` output rather than guessing. In etl_extract.c, use the SQLite3 C API (`sqlite3_open`, `sqlite3_exec` or `sqlite3_prepare_v2`/`sqlite3_step`) to first issue `CREATE INDEX` statements on the foreign-key columns in t_beta and t_gamma to speed the join, then run one SQL query that joins the connections table to two subqueries (or EXISTS clauses) checking each side has at least one post with `publish_date > '2022-01-01'` — be careful with date string comparison format matching what's stored. Iterate results in ascending order by User1 then User2 (`ORDER BY` in SQL, so you don't need to sort in C) and write each as `CREATE (u1:User {id: <id1>})-[:CONNECTED_ACTIVE]->(u2:User {id: <id2>})` to graph_import.cypher via C file I/O, driven entirely by query results (no hardcoded IDs). Compile with a standard `gcc ... -lsqlite3` and confirm the index and output file both exist by re-querying `sqlite_master` and diffing the cypher file against your own manual trace of the join logic on a few rows.