Treat this as two separable workstreams: get the multi-service pipeline flowing, then harden the CSV parser against adversarial input. For the pipeline, edit `/app/localization_pipeline/nginx.conf` to add a `proxy_pass http://127.0.0.1:5000;` route for the `/upload` location so requests hitting nginx on 8080 reach the FastAPI receiver on 5000; then set the empty Redis connection string in `receiver.py` to `redis://127.0.0.1:6379/0` so queued jobs actually reach the Celery worker; restart everything via `restart_services.sh` and confirm end-to-end with a curl upload, polling for `/home/user/processed_stats.json` to appear. For the filter script, use Python's `csv` module (not manual string splitting) so properly-escaped embedded newlines inside quoted fields parse correctly as single rows — manual `split('\n')` is the naive failure mode here. Normalize `updated_at` by trying multiple parsers in sequence (ISO8601, Unix epoch numeric, `MM/DD/YYYY`) and reformat to strict `...Z` UTC ISO8601. Derive `lang_primary` by splitting `locale` on `-`/`_` and taking the first segment. For malformed rows (null bytes, unbalanced quotes, injected control characters), wrap row-level processing in a try/except and structural sanity checks, silently dropping any row that fails rather than crashing the whole run, and print the exact required JSON summary counts to stdout at the end.