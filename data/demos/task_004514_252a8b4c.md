Since correctness is checked by byte-exact fuzz equivalence against a hidden oracle, follow the spec literally rather than improvising formatting — exact key order, exact JSON string shape, no stray stdout output (route all logging to stderr). Start with `cargo init` in `/home/user/rust_worker` and add `serde`, `serde_json`, `redis`, `reqwest` (blocking or async+tokio), `tokio`. Implement normalization precisely: lowercase, replace every non-ASCII-alphanumeric char with a single space (a regex or manual char-map both work), split on whitespace dropping empty tokens, truncate to the first 10 tokens, then rejoin the *truncated* token list with single spaces to form `normalized_text` — the cache key and embedding request must use this truncated/rejoined string, not the original text. Use the Redis key `embed:{normalized_text}` exactly, checking existence before deciding cache-hit vs miss; on miss, POST `{"text": normalized_text}` to `http://127.0.0.1:8000/embed`, parse the `embedding` array, then write it back to Redis as a JSON string under the same key so repeated identical inputs hit cache. Read stdin line-by-line, write one JSON object per line to stdout with fields in the order `id, tokens, embedding`, and build with `cargo build --release` so the binary lands at the required path. Test manually against the running Redis/API services before assuming correctness.
