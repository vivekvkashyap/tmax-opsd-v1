Build this as one C++ program plus a thin bash wrapper, not as separate shell scripts, since you need consistent floating-point formatting. Parsing order matters: validate ID (strictly positive integer, no leading garbage — use strtol and check the whole token consumed), Category (non-empty, every char isalpha), and Text (length >= 5) independently per line, and log 1-based line numbers of any failure to invalid_rows.log in the order encountered. Compute the 5-dim feature vector per the exact definitions given (length, vowel count, consonant count = alpha-and-not-vowel, uppercase count, space count), then L2-normalize; guard the zero-norm case explicitly (treat as invalid rather than dividing by zero). Format all vector components to exactly 4 decimal places using fixed/setprecision. For retrieval, compute cosine similarity between each valid query vector and every valid embedding (already unit vectors, so cosine similarity reduces to a dot product) and pick the best match, breaking ties by lowest ID — do this with a stable comparison, not just "first seen" iteration order. Finally write run_pipeline.sh that compiles with g++ -std=c++17 -O3, runs the binary against the two given TSVs, and chmod +x the script itself.
