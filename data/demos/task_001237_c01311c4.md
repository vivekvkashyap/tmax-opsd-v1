Treat this as a graph query over two CSVs rather than ad hoc string matching. Load `nodes.csv` and `edges.csv` (pandas or sqlite3 both work well — sqlite lets you express the multi-hop pattern as SQL joins directly). Build the pattern match in two joins: first join edges where `relation='USES'` from Company nodes to Technology nodes, then join again on edges where `relation='BELONGS_TO'` from those Technology nodes to Category nodes, and filter to rows where the Category's `name` equals `Database` — this gives you the set of qualifying company IDs. Then, for cross-query aggregation, go back to the full USES edge set (not the database-filtered subset) and, for each qualifying company, collect *every* technology it uses, not just the database ones — this is the step naive solutions often get wrong by only reporting the database-category techs. Sort the tech list per company alphabetically, sort the final company array alphabetically by name, and write to `/home/user/output/tech_summary.json` (creating the output dir first) with exactly the specified keys and integer `total_techs`. Validate your output structurally (e.g. with `jq`) before considering it done.