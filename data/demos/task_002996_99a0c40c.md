Build the exporter as a strict sequence of checks that each exit immediately with the specified code on failure, in the exact order given (owner check, then connectivity, then mount existence, then success) — don't reorder or combine checks, since the test suite probes each failure mode independently. For the owner check, use `os.stat('/home/user/observability/config.ini').st_uid` and compare to `os.getuid()`, exiting 4 on mismatch. Parse `config.ini` with `configparser` to get `target_port` (int) and `mount_device`, then parse `fstab.mock` by splitting each non-empty line on whitespace and matching the first field against `mount_device` to get the second field (mount path) — don't hardcode the mapping. For connectivity, use a raw `socket.socket` with `connect()` wrapped in try/except (or `settimeout`) against `('127.0.0.1', target_port)`, exiting 2 on any connection error. Then check `os.path.isdir(mount_path)`, exiting 3 if missing. On full success, append the exact JSON line (correct key order/types) to `/home/user/metrics.log` and exit 0. For the supervisor script, loop up to 4 attempts calling `python3 exporter.py`, capture `$?` each time, append the exact restart message to `/home/user/supervisor.log` and `sleep 1` on nonzero exit, exit 0 immediately on success, and exit 1 after the final failed attempt. Remember `chmod +x supervisor.sh`.