For the C++ filter, parse the binary manifest carefully per spec: read the 8-byte `BKP_MNFS` header, then loop reading a little-endian 2-byte path length, that many path bytes, and a fixed 32-byte checksum blob, until EOF — use streaming binary reads (`std::ifstream` in binary mode or raw `read()`), not line-based parsing. The core safety logic is the resolution check: join `<base_dir>` with the entry's relative path and resolve it fully (e.g. `realpath()` or `std::filesystem::weakly_canonical`/`canonical`), handling the case where resolution fails or loops (catch the filesystem exception or check realpath's error return, e.g. ELOOP/ENOENT) — treat failures as unsafe and drop the entry. For the containment check, canonicalize `base_dir` itself too, then verify the resolved entry path's string strictly starts with the canonical base_dir path followed by a separator (not just a prefix match, to avoid `/base` matching `/base_evil`). Write only the header plus the entries that pass both checks to the output, preserving each entry's original bytes exactly. For the pipeline script, use bash's `/dev/tcp/127.0.0.1/9001` (or `nc`) to pull the raw manifest, pipe it through `./filter_manifest /home/user/project_root /dev/stdin /dev/stdout`, and forward that filtered output into `/dev/tcp/127.0.0.1/9002` (or `nc 127.0.0.1 9002`), testing each stage against the running services before wiring them together.