Split this into an offline-testable filter and a wired-up pipeline, verifying each piece before moving on. In the Go filter, parse the JSON into 1000-length `x`/`y` slices, then slice the baseline as indices 0-49 and 950-999 combined into one 100-point set. For the drift test, fit ordinary least-squares slope `m` on those 100 (x,y) baseline pairs using the standard formula `m = (n*Σxy - Σx*Σy) / (n*Σx² - (Σx)²)`, and reject if `|m| > 0.05`. For the SNR test, compute mean and *sample* standard deviation (divide by n-1, not n) of the baseline y-values, then compare the maximum y over the *entire* 1000-point array against `mean + 5*stddev`, rejecting if not strictly greater. Exit 1 on either failure, 0 otherwise. Before touching the services, run the compiled binary directly against every file in `/app/corpora/clean/` and `/app/corpora/evil/` and confirm all exit codes match expectations — this is a fast, cheap correctness check. Only then edit `/app/services/config.yaml` to point `redis_host`/`redis_port`/`filter_bin` correctly, start Redis and the Flask ingestion API, and confirm end-to-end via a manual POST to `/ingest` followed by checking Redis's `valid_spectra` list length.