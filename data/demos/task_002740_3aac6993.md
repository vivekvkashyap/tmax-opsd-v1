Start by reading the log and heap dump to confirm the hypothesis stated in the task: the retry_queue accumulates entries whenever an EMA update jumps by more than the threshold, and it's never drained — that's the leak. Trace it back to `parse_hex`, which does `int(hex_str, 16)`, an unsigned interpretation; for 8-bit signed hex, values with the top bit set (0x80-0xff) need to be reinterpreted as negative. Fix it with two's-complement logic: parse as unsigned first, then if the result is >= 128, subtract 256 (equivalently `val = val - 256 if val & 0x80 else val`), matching the given examples ("ff"->-1, "80"->-128, "7f"->127).

Copy the script to `/home/user/fixed_daemon.py`, apply the signed-hex fix, and trace through what happens to the retry queue afterward — with correct signed parsing, EMA jumps between correctly-signed values are much smaller, but you should still check whether any case can still trigger a queued retry and confirm the queue no longer grows without bound during a normal run (do not silently drop the sanity-check logic, since the task says the parsing fix alone should prevent the leak — verify this by tracing the actual data rather than assuming). Run `python3 fixed_daemon.py test_data.txt > final_state.json` and inspect the resulting per-sensor EMA values to confirm they follow the signed-arithmetic EMA recurrence, not the buggy unsigned one.