Build and test incrementally: write the C++ program first, compile it manually to check it works, then wrap it in the bash script. In `nfs_monitor.cpp`, read the fstab-style file line by line, skip blank lines and lines starting with `#`, and for each remaining line extract the hostname as the substring before the first `:` (use `find(':')`/`substr`, not a full path parse). Call `getaddrinfo(hostname, ...)` and check its return code — nonzero means resolution failed. For each failure, create `/home/user/outbox/alert_<hostname>.txt` with the exact literal template given in the task (To/From/Subject/blank line/Error line, substituting `<hostname>`) — match whitespace and line breaks exactly, since the checker does a string comparison. In `check_mounts.sh`, use `set -e`, compile with `g++ /home/user/nfs_monitor.cpp -o /home/user/nfs_monitor`, `mkdir -p /home/user/outbox`, verify `/home/user/custom_fstab` exists (exit 1 if missing), then run the compiled binary against it. Create the sample `custom_fstab` from the prompt and run the wrapper once so the alert files actually get generated for verification — expect `localhost` and `127.0.0.1` to resolve fine, leaving only the two `.invalid` hosts as failures.
