For the infinite loop: read `calculate.sh` carefully and trace the inner `while [ $n -ne 1 ]` loop by hand for a small odd input — look specifically for whether the odd-number branch actually reassigns the loop variable `n` (a common bug is computing `3*n+1` into a differently-named temp variable and never updating `n`, which means the loop condition never becomes false). Fix so the odd branch updates the same variable the while-condition tests, then run `./calculate.sh 20 > /home/user/collatz_20.txt` and sanity check a couple of stopping times by hand (e.g. n=1 should give 0 steps, n=2 should give 1 step). For the password: don't look only at the current file — use `git log -p -- calculate.sh` (or `git log --all -p`) to view the full diff history of the script, find the commit where a `DB_PASS`/credential-looking variable was removed, and extract its literal value exactly as it appeared (no surrounding quotes) into `recovered_password.txt`. Confirm you're grabbing it from the correct git object, not a currently-live line, since the file no longer contains it.
