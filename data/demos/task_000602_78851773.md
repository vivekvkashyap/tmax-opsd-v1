Build the pipeline as real, correct data cleaning — do not try to reverse-engineer or special-case the `/app/evaluator` binary, since it re-derives everything independently from your output file's content. In `/home/user/clean_pipeline.sh`, chain the stages in strict order: (1) normalize with `tr '[:upper:]' '[:lower:]'`, then squeeze internal whitespace and trim with something like `sed -E 's/[[:space:]]+/ /g; s/^ //; s/ $//'`; (2) mask emails and phone numbers with `sed -E` regexes matching `[^@[:space:]]+@[^@[:space:]]+\.[^@[:space:]]+` -> `<EMAIL>` and `[0-9]{3}-[0-9]{3}-[0-9]{4}` -> `<PHONE>` (do masking after normalization so casing/spacing don't break the match, but be careful the phone/email patterns still match post-lowercasing); (3) deduplicate by full line content keeping first occurrence only — use `awk '!seen[$0]++'` (do this before appending any rolling-average suffix, since dedup must be on content, not the annotated line); (4) for the rolling stats, iterate the deduplicated lines in order, maintain a sliding window of the last 5 kept lines' character counts, compute the average of whatever's currently in the window (fewer than 5 at the start) rounded to 1 decimal, and append `,<avg>` to each line as it's written out — this is easiest done in a single awk pass with a small array/queue. Run `/app/evaluator /home/user/cleaned_chats.txt`, read the printed score, and iterate on whichever stage is under-scoring until >= 0.95.