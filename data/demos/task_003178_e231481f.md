Work in two independent tracks. First, port `/app/legacy_processor.py` to Python 3: replace `urllib2`→`urllib.request`/`urllib3`, `BaseHTTPServer`→`http.server`, fix the `print` statement syntax, and in the `ctypes.Structure` fix the ABI mismatch by encoding Python 3 `str` to `bytes` (e.g. `"test".encode()`) before passing to `c_char_p` fields — Python 3 strings are unicode, not bytes, so C calls will fail or corrupt without this. Second, extract the module load order and semantic versions from `/app/legacy_specs.png` using OCR (`tesseract legacy_specs.png stdout`, cropping/upscaling the image first if OCR is noisy); read the recovered order and versions carefully rather than guessing, and double check ambiguous OCR characters (0/O, 1/l, similar digits) against the image. Then implement the `http.server`-based REST API on port 8080 with a `GET /config` handler returning a JSON array of `{"module": ..., "version": ...}` in the OCR'd order, write the identical array to `/app/final_config.json`, and start the server in the background before finishing.