Work through this as a multi-service repair: first fix `/app/nginx/nginx.conf` to listen on 8080 and use an `upstream` block round-robining to `127.0.0.1:8081/8082/8083`, and fix `/app/worker/main.go` so each worker instance dials Redis at `127.0.0.1:6379` with no auth. Then implement `AnalyzeSequence` in `/app/worker/analyzer.go` precisely per spec: chunk the sequence into non-overlapping 20-base windows (dropping any trailing partial window), compute each window's GC ratio as (count of G+C)/20, then fit ordinary least squares `y = mx + c` over (window_index, gc_ratio) pairs using the standard closed-form slope/intercept formulas, and compute R² as `1 - SS_res/SS_tot` from scratch (no external stats library available). Classify "evil" when R² >= 0.60, else "clean" — implement this exactly, since off-by-one windowing or an R² sign/formula error will silently misclassify. Wire the `/analyze` POST handler in `main.go` to launch analysis in a goroutine (e.g. via a channel + WaitGroup for the single request, or worker pool) so concurrent requests aren't serialized, cache the result in Redis keyed by sequence, and respond `{"status": "clean"|"evil"}`. Start everything with `/app/start.sh` and leave it running, then self-test with a few POSTs to `http://127.0.0.1:8080/analyze` before finishing.