Use `git bisect` instead of manually inspecting commits: `git bisect start`, mark current `main` HEAD `bad`, mark `v1.0` `good`, then at each step run `run_pipeline.sh` and check `output/stats.txt` to decide good/bad (ideally via a small helper script driven by `git bisect run`). Once bisect lands on the culprit commit, `git show`/diff it against its parent to find the exact line — look at filtering/regex logic touching numeric parsing, since a skew plus "records silently dropping" points to a regex or comparison excluding negative numbers. Fix that logic on `main` (checkout main first, don't leave the fix on the detached bisect HEAD), rerun the pipeline, then cross-reference the three log files by transaction ID/timestamp to find the first negative transaction that now reaches export but previously didn't. Record findings in the required format and `git commit -am "Fix statistical anomaly"` on `main`.
