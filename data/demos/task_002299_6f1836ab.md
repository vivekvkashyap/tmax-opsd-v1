This has two independent pieces. First, fix the vendored tool: inspect the first line of `/app/bats-core/bin/bats` — a broken shebang (pointing at a nonexistent interpreter) will make execution fail immediately; change it back to a valid bash path (e.g. `#!/usr/bin/env bash`) and confirm `bats -v` reports the expected version before trusting further test runs. Second, write `/home/user/detector.sh` to grep the target file with extended regex (`grep -E`) for three independent patterns, rejecting (exit 1) if ANY matches: (1) `-----BEGIN [A-Z ]*PRIVATE KEY-----`-style pattern for private keys; (2) `AKIA` followed by exactly 16 uppercase alphanumeric characters (bound the length precisely, e.g. `AKIA[A-Z0-9]{16}` with a boundary so it doesn't match longer runs like the clean-corpus decoy `AKIB...`/near-miss strings); (3) `Bearer(%20| )` followed by at least 20 chars from the JWT-safe character class (letters, digits, `-_.`). Exit 0 only if none match. Validate against both `/app/corpus/clean/` and `/app/corpus/evil/` directories, checking exit codes for every file, paying special attention to near-miss decoys in the clean corpus that are designed to trip up sloppy regexes.
