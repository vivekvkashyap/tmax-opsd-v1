First reshape the data correctly: read `/home/user/seq_lengths.txt`, split each line on commas, strip whitespace, and flatten every line's values into one 1D list/array of numeric lengths (a common mistake is treating each line as a single value or failing to flatten nested lists). Convert to a numpy array of floats/ints for `scipy.stats`. Fit both candidate distributions with `norm.fit(data)` (returns loc, scale = mean, std) and `expon.fit(data)` (returns loc, scale), then run `kstest(data, 'norm', args=(mu, std))` and `kstest(data, 'expon', args=(loc, scale))` — pass the exact fitted parameters as `args` so the KS test compares against the fitted CDF, not a generic default distribution. Compare the two resulting p-values and pick the distribution with the higher one as the better fit; ties or edge cases should still resolve deterministically per the comparison as specified. Round the winning p-value to exactly 4 decimal places and write a single line `<distribution_name>,<p_value>` (lowercase distribution name, comma-separated, no extra whitespace or trailing content beyond what's specified) to `/home/user/best_fit.log`. Double check you're rounding, not truncating, and that the numeric formatting always shows 4 decimals even if the trailing digit is zero.
