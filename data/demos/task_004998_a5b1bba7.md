Build this as a stateless HTTP handler with a careful recursive-extraction loop, and test the two hard parts (loop safety, encoding) with synthetic archives before wiring up the server. For each request, write the body to a temp dir (e.g. `tempfile.mkdtemp`), extract the top-level tar, then repeatedly scan for `.tar`/`.zip` files, extracting each into a sibling directory named after the archive minus its extension and deleting the archive file, looping until a scan finds none left — track already-seen archive paths or a max-iteration bound so a specially crafted archive-in-archive-with-same-name can't cause infinite work. For symlink safety, when walking the tree use `os.walk` with `followlinks=False` (or manually `os.lstat`/`os.path.islink` and skip descending into symlinked directories) so a symlink pointing to `.` or a parent never causes a traversal loop — only regular files and non-symlink directories should be visited. For `.txt` files, detect encoding (e.g. `chardet`/`cchardet`, or heuristics for UTF-16 BOM vs Shift_JIS vs Latin-1) and rewrite the file in place as UTF-8, being careful you read the pre-conversion bytes before writing. Call `/app/dedup_hash <path>` via subprocess for every final regular file and build the manifest keyed by path relative to the original extraction root (post-flattening of nested dirs). Serve this on `127.0.0.1:9090` via a simple framework and leave it running (backgrounded, e.g. with `nohup ... &` or `setsid`) so the verifier's later POST requests reach it.
