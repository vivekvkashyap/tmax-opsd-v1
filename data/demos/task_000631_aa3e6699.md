Build this as three separable pieces and test each in isolation before wiring them together. (1) Flask CSV fix: in `/app/api/app.py`, switch whatever raw string-concatenation or naive `csv.writer` misuse is dropping embedded newlines to Python's `csv.writer` with `quoting=csv.QUOTE_ALL` (or ensure the writer is never bypassed by manual `join(",")` calls) so multi-line fields get properly quoted per RFC 4180. (2) C sanitizer: for RFC 4180 CSV *with embedded newlines inside quoted fields*, do not read line-by-line with `fgets`/`getline` — a quoted field can span multiple physical lines, so parse character-by-character tracking whether you're inside an open quote (`"..."`, with `""` as an escaped quote) before treating a `\n` or `,` as a delimiter. For each row, split into 6 fields, reshape into two output rows (config_A, config_B), and implement Levenshtein distance with the standard DP table (O(n*m), rows of size min(len)+1 is fine given values are short). Apply the adversarial filter — POSIX ERE `[`$|<>]` match on old_val/new_val (use `regcomp`/`regexec`, or manually scan for those literal characters) OR distance > 50 — to silently drop rows, then write survivors back out fully CSV-quoted where needed. Test your compiled binary directly against the clean/evil corpora with `cat file | ./csv_tracker` before touching the Flask/Redis wiring, since that's exactly how it will be verified. (3) Only after the binary passes, hook it into the pipe-reading loop and start the services via `start_services.sh`.