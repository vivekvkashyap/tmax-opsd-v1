Do the two phases in order and validate each before moving on. Phase 1: find every `.jsonl` file under `/home/user/sensor_data/` (`find ... -name '*.jsonl'`) and, using shell text tools, transform each into a sibling `.csv` with header `timestamp,temperature,humidity`, mapping the JSON keys `ts,t,h` to the three columns per line (a `while read` loop with `python3 -c` or `jq` per line is more reliable here than a single fragile `sed`/`awk` regex given JSON key ordering isn't guaranteed) — spot-check one converted file against its source before running it on all of them. Phase 2: write `/home/user/aggregate.c` to recursively walk the directory (`nftw`/`opendir` recursion or shell out to `find` and read the file list) for `.csv` files, parse each line, and apply the validation filter exactly as specified (drop rows with humidity outside [0,100] or temperature outside [-50,60]) while accumulating count/sum/min/max of temperature only over valid rows — get this filter right since a single mis-included outlier will throw off the average. Format the three floats to exactly two decimals in the exact XML template given. Compile with `gcc -O3` and run the binary, then diff the produced `/home/user/final_results.xml` visually against the required template for exact whitespace/structure match.
