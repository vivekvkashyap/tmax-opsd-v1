Work in three independent, verifiable stages rather than jumping straight to wiring the server. (1) Run `tesseract /app/legacy_formula.png stdout` (or similar OCR invocation) to read the `ENCODING_MULTIPLIER = <N>` line from the image — use the value the OCR actually reports, don't assume a number. (2) Open `/app/rust_math/src/lib.rs`: the bug is a classic "reference to a value that doesn't outlive the function" pattern — a `Vec` built locally is borrowed and copied into a raw output slice; fix it by writing the computed bytes directly into `out_slice` (e.g. iterate and assign `out_slice[i] = ...` or collect straight into `out_slice.copy_from_slice(&computed_vec)` where `computed_vec` itself isn't dropped before use) so it compiles under the borrow checker, then run `cargo bench` and `cargo build --release` and confirm the `.so` lands in `target/release/`. (3) Migrate `/app/server.py` off `BaseHTTPServer`/Python-2 string+bytes mixing to Python 3 (`http.server`, `bytes` for ctypes buffers, `.encode('utf-8')`/`base64.b64encode` returning bytes you must `.decode()` before JSON-serializing), load the `.so` via `ctypes.CDLL`, pass the OCR'd multiplier into `process_bytes`, enforce the exact `Authorization: Bearer migrate-token-2024` header, read `/app/config.json` on startup, and launch the server as a background process listening on `127.0.0.1:8080`.