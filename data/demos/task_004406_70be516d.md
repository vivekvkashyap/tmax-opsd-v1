Build the three signatures as independent regex/byte scans over the whole file content, not line-by-line string equality — the QEMU escape check needs you to detect the two literal bytes 0x01 0x63 on one line (they may not be printable, so read raw bytes or hex-dump the line) and then check the *next* line for `system_reset` or `quit`; the OOM check is a regex like `Out of memory: Killed process \d+ \(sshd\)`; the hex-A check means scanning for 16+ repeated `41` hex pairs as literal text in the log (not actual 0x41 bytes) — use a regex like `(41){16,}`. Validate against every file in both corpus directories before considering the classifier done, and make it exit 1/print EVIL on first match, exit 0/print CLEAN only if none match. For the forwarder, don't just `nc` once — use `tail -F` piped into `nc` so it keeps streaming as the generator appends, and launch it with `nohup ... &` so it survives your shell exiting. For logrotate, write the `prerotate` block referencing the file being rotated via logrotate's own path variable (e.g. `$1` or the rotated path passed by logrotate), run the classifier on it, and on EVIL copy it into the quarantine dir you must first `mkdir -p`. Test the forwarder end-to-end by writing a marker string into the log and confirming it appears on the aggregator port within seconds.
