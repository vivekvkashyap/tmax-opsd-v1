Determine the two independent validity checks separately before combining them. First get the target frame count from `/app/sync_feed.mp4` using `ffprobe -count_frames -select_streams v:0 -show_entries stream=nb_read_frames` (or `ffmpeg -i ... -f null -` and parse the frame counter) rather than estimating from duration*fps, since exact frame count is what must match. Second, implement Pearson correlation between `sensor_alpha` and `sensor_beta` columns yourself in Rust (parse the CSV, compute means, covariance, and standard deviations, then r = cov/(std_a*std_b)) — watch for edge cases like NaN/Infinity values in a column, which should make the correlation check fail (treat non-finite inputs as invalid rather than crashing or silently passing). A file is VALID only if row count equals the video's frame count AND |r| computed correctly is >= 0.85 (note the sign: a strong negative correlation should still fail if the threshold is on the raw signed value, so read the requirement precisely). Initialize the cargo project at `/home/user/telemetry_filter/`, keep all diagnostic/build output on stderr, and print only `VALID`/`INVALID` (no trailing whitespace) to stdout. Test with `cargo run --release -- <file>` against every file in both `/app/corpora/clean/` and `/app/corpora/evil/`, including the tricky evil cases (off-by-one row counts, negative correlation, independent/random sensors, NaN-laden data) before considering it done.
