Do the two stages in order and validate each before moving on. For the PIN: use a DTMF decoder approach — extract the audio's frequency spectrum over time (e.g. via `ffmpeg` to get raw samples plus a short Python script doing FFT/Goertzel analysis per tone segment, or an existing DTMF-decode library/tool if available) to map each detected tone pair to a digit, and reconstruct the 6-digit sequence; sanity-check that you get exactly 6 digits before using it. Use the recovered PIN to unzip the password-protected archive (`unzip -P <pin> /app/audit_logs.zip -d /app/corpus/`) into the clean/evil directories described. For the scanner: URL-decode the payload content first (implement percent-decoding, e.g. `%2F` -> `/`, `%3B` -> `;`), then check the decoded string for path-traversal sequences (`../`, `..\`) and shell metacharacters (`;`, `|`, backtick, `$(`) — treat any hit as EVIL. Print exactly `EVIL`/`CLEAN` with the matching exit code, nothing extra (no newlines-only concerns, but no debug output). Test your binary against a handful of files you construct yourself with these patterns before trusting it against the full corpora.