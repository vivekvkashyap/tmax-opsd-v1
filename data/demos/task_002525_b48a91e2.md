Start the two Python microservices first (`/app/start_services.sh &`) and manually curl them to confirm their exact JSON shapes before writing any C++. Fetch the header-only libraries (cpp-httplib and nlohmann/json single-header) into `/home/user/include` — check network access works, and if a direct download fails, pull from an already-cached pip package (`pip download` won't help here; prefer `curl`/`wget` from a raw GitHub URL) or check if the packages are already present system-wide via apt/pip source trees. In the C++ service, use `httplib::Client` to call the metadata and embedding services at startup, parse responses with nlohmann::json, and cache `{id, vector<float>}` pairs in memory. For `/search`, parse query params manually (httplib gives you `req.get_param_value`), fetch the query embedding, compute cosine similarity as dot-product over norms (embeddings from the service are already normalized, so a plain dot product works — but compute norms defensively anyway), sort descending, and take the top-k. Time the search-and-score portion only (not the query embedding fetch, if that skews numbers — but simplest is to time the whole handler) using `std::chrono::high_resolution_clock`, and append the log line in the exact required format before returning JSON. Compile with `-lcurl -pthread` and background the binary with `&`, confirming with a curl request afterward.
