Set up a cargo binary project in `/home/user/etl_processor` with `serde`, `serde_json`, `chrono`, `regex`, and `unicode-normalization` as dependencies. The trickiest part is the double-escaped unicode: the raw JSON string literally contains the characters backslash-u-XXXX (because it was double-escaped on ingestion), so after `serde_json` parses the message field you must scan the resulting Rust String with a regex like `\\u([0-9A-Fa-f]{4})` and replace each match by parsing the hex as a `u32` and converting to `char`, then run the result through `unicode_normalization`'s NFKC iterator (`.nfkc().collect::<String>()`) — do this only after decoding, not before. Parse chat timestamps with `chrono` (RFC3339/ISO8601) into Unix seconds; tx timestamps are already epoch ints. For each chat row, sum tx amounts for the same session where `chat_ts < tx_ts <= chat_ts + 60` (strict lower, inclusive upper — an off-by-one here is a common bug). Sort the output by `chat_ts` ascending with a stable sort to preserve original order on ties, then serialize each record as one JSON line to `/home/user/output.jsonl`.
