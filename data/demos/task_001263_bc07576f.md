Set up the venv and dependencies first (`python3 -m venv /home/user/venv && /home/user/venv/bin/pip install numpy scipy pandas matplotlib`), then write and run the analysis as a single script inside that venv so you're not debugging environment issues mixed with statistics issues. Load the CSV with pandas, and implement the bootstrap CI by calling `scipy.stats.bootstrap((error_array,), np.mean, method='percentile', n_resamples=10000, random_state=42)` exactly as specified — match every keyword argument precisely, since a different method or missing random_state will shift the interval bounds and fail exact/tolerance comparison against the golden solution. For the power-law fit, define an MSE objective closing over N and error arrays, minimize with `scipy.optimize.minimize(mse_fn, x0=[1e-16, 1.0], method='Nelder-Mead')`, and pull `a, b` from `result.x` — verify convergence by checking `result.success` and that the fitted curve visually tracks the scatter before trusting the numbers. Plot raw (N, error) points as a scatter, overlay the fitted curve `a*N**b` (sort N first so the line renders correctly, e.g. via `np.linspace` over N's range), and save to `/home/user/fit_plot.png`. Write the four floats to results.json using the exact key names given; since grading tolerates 1e-4 relative error, keep the computation in double precision throughout and avoid manual rounding.
