Phase 1 — recover the key from the video. Extract frames: `ffmpeg -i /app/surveillance.mp4 frame_%02d.png` (24 frames, 1 fps = 1 bit each). For each frame, sample the top-left corner (e.g. average pixel brightness in the 0,0–10,10 box) and threshold: bright = '1', dark = '0'. Concatenate the 24 bits in frame order, split into three 8-bit big-endian bytes, and convert each to its ASCII character to form the 3-char passphrase (derive it — don't assume it). Use it to decrypt: `openssl enc -d -aes-256-cbc -pbkdf2 -in /app/training_data.zip.enc -out /tmp/training_data.zip -pass pass:<recovered-key>`, then unzip to inspect `evil/` and `clean/`. Phase 2/3 — write `/home/user/mock_ids.c`: read the whole file given as argv[1], build a sanitized copy that removes every `\`, `'`, and `"` character, then check whether that sanitized buffer contains the substring `eval` or `nc` (use `strstr`). Exit `1` if either is present (evil), else exit `0` (clean). Compile with `gcc /home/user/mock_ids.c -o /home/user/mock_ids`. Validate against the decrypted corpora — 100% accuracy is required. Traps: scanning before stripping the quote/backslash characters, or thresholding the LED corner too loosely.