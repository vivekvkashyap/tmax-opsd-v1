This is the classic pandas int-to-float precision bug: a left join leaves NaN for unmatched rows, and any column mixing NaN with int64 values silently upcasts the whole column to float64, which can't exactly represent integers past 2^53 — printing the "same looking" number hides that it's actually been rounded. Fix `etl.py` by casting the integer column to the pandas nullable `"Int64"` dtype (capital I) either right after loading or right after the merge, and use `.fillna(0)` in a way that's compatible with that nullable dtype (fillna works fine on Int64 columns and keeps them integer, unlike fillna on a float64 column). Verify the fix actually worked by checking the column's dtype (`df.dtypes`) and comparing specific large values against their known integer string form, not just visual inspection. For the correlation script, use `pandas`/`numpy`'s Pearson correlation function on the two feature columns and round to exactly 4 decimals before writing only the numeric value to the text file. For scoring, simply run the provided `score.py` unmodified against your corrected `processed.csv` and capture its printed MSE into the target file. Chain all three scripts in a small `run_pipeline.sh` with `set -e` so a failure anywhere stops the pipeline, and mark it executable.