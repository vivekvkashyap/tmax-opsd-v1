Do the three fixes in dependency order since later steps need earlier ones. (1) Recovery: find the still-running process holding the deleted file with `ps aux | grep data_holder` (or similar), get its PID, then look in `/proc/<pid>/fd/` for the file descriptor pointing at the deleted `records.csv` (deleted-but-open files show as `records.csv (deleted)` in `ls -l`), and `cat /proc/<pid>/fd/<N> > /home/user/recovered_records.csv` to pull the content back before touching the process further. (2) C fix: change the multiplication in `processor.c` to widen operands to a 64-bit type (e.g. cast to `long long` or use `int64_t` for the result) before multiplying, so it no longer overflows 32-bit `int`, while keeping the existing negative-input check returning -1; recompile with gcc. (3) Python fix: in `daemon.py`, replace the loop that appends a 0 and `continue`s without advancing `i` (the actual bug — it never increments the index on negative results, causing the infinite loop/leak) with logic that skips the record and always advances the index; then add code writing the final integer total to `/home/user/final_sum.txt`. Test end-to-end by rerunning daemon.py against the recovered CSV and manually recomputing the expected sum from the recovered file's rows to sanity-check.