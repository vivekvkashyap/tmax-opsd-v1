Use `tesseract /app/experiment_notes.png stdout` (or open/inspect it visually) to recover the exact missing-value rule, the linear model formula, and the outlier threshold — do not guess these numbers, read them off the image precisely since they drive every downstream calculation. Then write `/home/user/detector.sh` taking one CSV path argument, using `awk -F,` to process rows after the header: for each row, substitute empty `feature_alpha`/`feature_beta` fields with the OCR'd default value, compute the score using the OCR'd linear formula, and compare against the OCR'd threshold — if any row's score exceeds the threshold, exit with status 1 (evil/corrupted); if the whole file completes without triggering that, exit 0. Test against a few of your own constructed CSV rows (one clearly clean, one clearly over-threshold, one with missing fields) to confirm exit codes behave correctly before relying on the hidden corpus, and remember to `chmod +x` the script.