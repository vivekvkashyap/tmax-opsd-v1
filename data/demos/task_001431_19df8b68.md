Build and calibrate the filter using the provided clean/evil corpora before trusting it against the hidden set. Steps: (1) transcribe `/app/reference_report.wav` once with `whisper` (small/base model is fine) to get the reference text — do this once at script start or cache it, not per-row; (2) load `sentence-transformers` `all-MiniLM-L6-v2` and embed the reference text plus every row's `transcript` column; (3) compute cosine similarity between each row embedding and the reference embedding (`sentence_transformers.util.cos_sim` or manual dot/norm); (4) apply a threshold to decide keep/reject. Don't just guess a threshold — run your script against `/app/corpus/clean/*.csv` and `/app/corpus/evil/*.csv`, print the similarity scores for each, and pick a threshold that cleanly separates the two clusters (there should be a visible gap since clean rows are semantically close and evil rows are unrelated); a value near the reported 0.4 is a starting guess but verify it empirically against your own corpus scores and adjust if the two distributions don't separate there. Implement the exact `--input`/`--output` argparse CLI, and after each invocation append one line to `/home/user/experiment_log.jsonl` with counts of accepted/rejected rows for that specific input file (append mode, one JSON object per line, don't overwrite prior runs).