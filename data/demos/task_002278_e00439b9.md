Sequence this as OCR-first, then a modeling function you can call both once for validation and again on-demand from the API. Run `tesseract /app/experiment_summary.png stdout` (or pytesseract) and parse out the four labeled fields (ID, seed, alpha, baseline) with simple line-based string splitting — OCR output can have stray whitespace/artifacts, so strip and validate each parsed value (e.g. confirm seed parses as int, alpha/baseline as float) rather than trusting raw text. For the dataset, load with pandas, `dropna()`, then filter to rows where `target` is strictly 0 or 1 (watch for the injected noise value of 2 in the fixture — an `isin([0,1])` filter handles it cleanly). Train `RandomForestClassifier(random_state=<extracted_seed>)` with defaults, run `cross_val_score(clf, X, y, cv=10)`, and take the mean. For the hypothesis test, use `scipy.stats.ttest_1samp(scores, baseline)` and take the right-tailed p-value (halve the two-sided p-value if the sample mean exceeds baseline, or use `alternative='greater'` if your scipy version supports it) — "reproducible" requires both p < alpha AND mean > baseline, check both conditions explicitly. Wrap the OCR'd metadata and the modeling function behind a Flask/FastAPI app on 127.0.0.1:8080, gating both endpoints on `Authorization: Bearer <extracted ID>`, running the pipeline fresh on each `/test-reproducibility` call.
