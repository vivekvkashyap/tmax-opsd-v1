Work through the three concerns (build, security, performance) as separate, independently verifiable fixes rather than one big rewrite. First run `make` in `/app/fast-pcap-extract-1.2` to see the actual link failure — a Makefile that sets `CC` to include a `-c` flag will compile objects fine but fail at the final link step since `-c` stops before linking; remove `-c` from the variable used for the link recipe (keep it only for the compile-objects rule) so `./pcap_extract` gets produced. Next, review `parser.c` for `strcpy` onto a fixed-size buffer from attacker-controlled packet data and replace it with a length-bounded copy (`strncpy`/`memcpy` capped at the buffer size, with explicit null-termination); in `writer.c`, change file/dir creation modes from `0666`/`0777` to `0600`/`0700` respectively (and consider `umask` interference — set an explicit mode via `open`/`mkdir` flags, not just relying on umask). For `matcher.c`, replace the manual O(N*M) nested-loop signature search with `memmem()` (or implement KMP if `memmem` isn't suitable) and add `-O2`/`-O3` to compile flags; measure your own before/after timing on a large PCAP to sanity-check you're well past 3x before relying on the hidden benchmark. Finally run the tool end-to-end on `/home/user/traffic.pcap` and confirm `hashes.txt` and permissions look right.