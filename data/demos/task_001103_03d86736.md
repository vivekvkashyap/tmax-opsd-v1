Fix the two flaws directly in the vendored package source before reinstalling, rather than patching around it. For the tar-slip issue in `extractor.py`, don't call `tar.extractall(path=target_dir)` blindly — iterate `tar.getmembers()`, and for each member skip (don't abort the whole extraction) any whose `member.name` is absolute (starts with `/`) or contains a `..` path-traversal segment (check path components, not just a naive substring match, so things like `foo/../../bar` are still caught); only call `tar.extract()`/`extractall` on the filtered safe members. For the O(N^2) dedup bottleneck in `dedup.py`, replace the nested byte-by-byte comparison loop with a single pass that computes `hashlib.sha256(f.read()).hexdigest()` per file, stores digest -> first-seen-path in a dict, and hardlinks (`os.link`) any file whose digest already exists instead of comparing it against every prior file. After editing, reinstall the package locally (e.g. `pip install -e /app/vendored/artifact_curator-1.2.0`) so the `artifact-curator` CLI reflects your fix, then write `/home/user/process_all.sh` to iterate `.tar` files in `/home/user/incoming/` and invoke the CLI per archive into `/home/user/processed/`, and `chmod +x` it. Time it against the 100-archive dataset to confirm it's under 2 seconds and that `/home/user/system_fake/` stays empty.