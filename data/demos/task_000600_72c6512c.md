This is a classic "keep only the latest record per key" stream pipeline — solve it with a filter, sort, and dedup-by-latest chain, not a custom parser. Steps: (1) `awk -F, '$4=="SUCCESS"'` to keep only successful rows; (2) sort those rows primarily by component_name (field 2) ascending, and secondarily by timestamp (field 1) numerically descending, so that for each component_name group the newest row comes first — e.g. `sort -t, -k2,2 -k1,1nr`; (3) pipe through an awk (or `sort -u`-style) dedup step that keeps only the first row seen per component_name (since it's now the most recent due to the sort order) — `awk -F, '!seen[$2]++ {print $2","$3}'`; (4) the component_name,config_hash pairs produced this way are already alphabetically ordered by component_name because of the primary sort key, so no extra final sort is needed, but you can add `sort -t, -k1,1` defensively. Write the final pipeline output directly to `/home/user/latest_config.csv` and diff it against your own hand-derived expectation from the input file to sanity-check before finishing.