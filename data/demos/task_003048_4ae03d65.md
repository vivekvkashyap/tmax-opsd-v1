Treat `/app/policy_engine.bin` as a black box you must characterize empirically before writing any Python. Run `strings /app/policy_engine.bin` first — this is the fastest way to spot any hardcoded secret/salt string embedded in the binary; note whatever salt string you find (don't guess it). Then systematically probe the binary via stdin with hand-picked inputs that isolate each rule independently: vary only the port (22 vs. others), only the key type (rsa/dsa/ecdsa/ed25519), only the key length relative to typical thresholds like 1024/2048, and only the IP prefix (10.x, 192.168.x, external), observing how the printed score changes each time — this lets you reconstruct the additive scoring rules and the allow/block cutoff by binary search on the threshold. Also test IPs/ports/key combos that could hit multiple rules simultaneously to check whether effects are simply additive or have interactions. For the hash field, once you know the salt string and the exact output format, verify your guessed hash-input construction (which fields, in what order and delimiter) by comparing your locally computed MD5 against the binary's TOKEN output for several test cases — iterate on the format string until it matches exactly. Implement `/home/user/policy_engine.py` to parse stdin, replicate the scoring/action/hash logic, and print output byte-for-byte in the same format (spacing, labels, newline) as the binary.