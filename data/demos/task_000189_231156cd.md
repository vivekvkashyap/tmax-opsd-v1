Build and test each layer independently before wiring them together. First scaffold the Rust binary with `cargo new /home/user/src/network_provisioner`, add a JSON dependency (e.g. `serde`/`serde_json`), and implement: read the JSON path from `env::args()`, parse the array of `{username, subnet}` objects, truncate-and-rewrite `/home/user/network/routes.batch` (open with `File::create`, which truncates, at the very start of the run — don't append across runs), then for each user append a `route add <subnet> dev dummy0` line and a `[YYYY-MM-DD HH:MM:SS] Provisioned user <username> with subnet <subnet>` line (UTC, via `chrono::Utc::now()` or `SystemTime`) to `/home/user/logs/provision.log`, creating parent directories with `create_dir_all` first. Build with `cargo build --release` and test the binary directly against a sample JSON file before touching Git. Then `git init --bare /home/user/accounts.git`, and write a `hooks/post-receive` shell script that does `git --work-tree=/home/user/accounts_worktree --git-dir=/home/user/accounts.git checkout -f master` (creating the worktree dir first), `cargo build --release` in the provisioner directory, then runs the compiled binary against `/home/user/accounts_worktree/users.json`; make the hook executable. Finally write `/home/user/logrotate.conf` targeting the log path with `daily`, `rotate 5`, `compress`, `missingok`, and `create 0644`. Test the whole chain end-to-end with a real `git push` before finishing.
