Read both JSONL files line by line into Python dicts (or a networkx graph with employee/manager edges) rather than relying on fragile string parsing. Build a lookup emp_id -> {skills, manager_id} and proj_id -> {name, members}. For each project, gather `team_skills` as the union of `skills` for each member id in `members` (dedupe with a set, then `sorted()`); for `manager_oversight_skills`, for each member look up their `manager_id` (skip if null/missing), union those managers' skills, then subtract the `team_skills` set before sorting — order matters, compute team_skills first. Build each record as a dict matching the schema exactly (`proj_id`, `proj_name`, `team_skills`, `manager_oversight_skills`, no extra keys) and validate with `jsonschema.validate` against the given schema before writing. Sort the final list of records by `proj_id` before writing one JSON object per line to `/home/user/output/enriched_projects.jsonl`. Keep any print/debug statements out of that file — write to stderr or remove them, and don't accidentally write partial/invalid records that fail schema validation.
