Split this into preprocessing and model, and get each exactly right independently since the model will be fuzz-tested bit-for-bit against a hidden oracle. For `extract.py`, use OpenCV (or ffmpeg) to sample `/app/experiment.mp4` at exactly 2 fps, convert to grayscale, resize to 16x16 (watch the interpolation and resize order — grayscale-then-resize vs resize-then-grayscale can matter, and normalize by dividing by 255.0 to get float32 in [0,1]), flatten to 256 floats, and append every frame's raw float32 bytes sequentially to `/home/user/dataset.bin` (use `numpy.tofile` or explicit struct packing — don't accidentally write as float64). For `model.cpp`, implement the exact online SGD update as specified: initialize W to 0.1 for all 256 dims, then for each frame read 256 float32 values (`fread`/`ifstream::read` in raw binary mode, not text parsing), compute the dot product prediction, error against target 1.0, and update every weight with the given learning-rate formula, processing frames strictly in file order. Print with `printf("%.6f ", ...)` per weight plus a trailing newline — precision and spacing matter for equivalence checking. Compile with g++, run it on your generated `dataset.bin`, and save output to `/home/user/signature.txt`.
