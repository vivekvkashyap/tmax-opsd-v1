First recover the rules from the audio: use a local speech-to-text tool (e.g. `whisper` if available, or `ffmpeg` to extract/inspect the waveform plus any installed ASR CLI) on /app/research_log.wav and transcribe it carefully — the memo states which node labels/collections are allowed vs forbidden, how match values must be parameterized (look for a required prefix character), and which field the output projection must always include. Do not guess these rules; verify by re-listening or re-running transcription if key details are unclear. Then write /home/user/sanitizer.c using cJSON (from /opt/cJSON) to parse the input file's JSON payload, walking the "$match" and "$project" objects to check: (1) no forbidden label/collection appears as a match value, (2) every match value that should be parameterized actually starts with the required sentinel character, (3) the required field is present in "$project". Return exit 0 only if all rules pass, else exit 1 — handle malformed/missing JSON as evil (non-zero exit), not a crash. Compile to /home/user/sanitize_query and validate manually against a few files in /app/corpus/clean and /app/corpus/evil before considering it done, since the real check runs every file in both directories and requires 100% correct classification.
