Work the three sub-problems in order since later steps depend on earlier ones. First, run `tesseract /app/ticket_screenshot.png stdout` (or similar OCR invocation) to recover the dashboard text, locate the line containing `CRITICAL_OFFSET:` and read off the integer that follows it, then hardcode that exact integer into the `CONFIG_OFFSET` variable in `fast_parser.py` — do not guess the value, extract it. Second, debug the recursion/infinite-loop bugs by tracing through the parser's nested-parenthesis handling logic: a common cause of `RecursionError` or infinite loops in hand-written recursive-descent parsers is failing to advance an index/cursor when descending into a nested group, so check every recursive call updates position state correctly. Third, achieve bit-for-bit parity: treat `/app/legacy_parser` as a black box, feed it many probe strings from stdin (varying letters, digits, and paired/unpaired parentheses, including edge cases like empty input, unbalanced parens, deep nesting) via `echo`/`printf`, diff its stdout against your Python script's stdout for the same input, and when a mismatch appears use delta debugging (bisect the failing string) to shrink it to a minimal reproducer before adjusting your parsing logic. Iterate until outputs are identical across many random samples.