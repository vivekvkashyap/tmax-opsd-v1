Treat the zip-slip defense as the load-bearing part of step 1: when extracting `payload.zip` (itself nested inside `docs.tar`), for each zip entry compute its resolved absolute path (`os.path.normpath(os.path.join(dest, entry.filename))`) and only extract it if that resolved path is still inside `/app/extracted/` (e.g. `resolved.startswith(os.path.abspath(dest) + os.sep)`); skip/reject any entry (like one using `../`) that resolves outside — do not rely on Python's `zipfile.extractall` alone, since it doesn't guard against this. For the GCode transform, use a regex scoped to lines matching `G1 X(\d+(\.\d+)?) Y(\d+(\.\d+)?)` and rewrite only those to `LINEAR_MOVE X:<num> Y:<num>`, leaving lines like `G0 Z5` and text outside `[GCODE]...[/GCODE]` untouched — don't apply the regex file-wide if it could accidentally match text outside the tags. For the multi-service piece, edit `nginx.conf` to listen on 8080 as a non-root user with `location /docs/ { proxy_pass http://127.0.0.1:5000; }`, and edit `app.py`'s Flask route `GET /docs/<filename>` to read and return the raw text from `/app/extracted/<filename>`, binding the app to `127.0.0.1:5000`. Launch both via `start.sh` and confirm with `curl http://127.0.0.1:8080/docs/doc1.txt` that the transformed content comes back, then confirm `/app/system_config.txt` is untouched.