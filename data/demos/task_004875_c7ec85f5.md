This is a fuzz-equivalence task graded against a hidden reference program you never see, so focus on deriving the exact math and format rather than guessing. First inspect `/app/archive_sample.mp4` yourself with `ffprobe` to learn its actual resolution, frame count, and frame rate — don't hardcode assumed dimensions. The RGB24 byte size for one frame is `width * height * 3`; total bytes for an inclusive frame range is `(end_frame - start_frame + 1) * bytes_per_frame`, since RGB24 is a fixed per-pixel size regardless of compression — you do not need to actually decode pixel data with ffmpeg per request once you've confirmed the frame count and resolution match this formula, but validate the formula against a real ffmpeg raw-frame extraction (`ffmpeg -i in.mp4 -vf select='between(n,S,E)' -pix_fmt rgb24 -f rawvideo - | wc -c`) for a couple of sample ranges to make sure your derived per-frame size is exact and there are no off-by-one or extra-frame issues. Then write `/home/user/segment_sizer` as an executable script (proper shebang, chmod +x) that reads the whole JSON array from stdin, computes bytes per object in input order, and prints strict CSV `start,end,bytes` with no extra whitespace or trailing blank lines, since comparison is exact-match.
