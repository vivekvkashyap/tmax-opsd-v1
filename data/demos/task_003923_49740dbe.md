Build this as a pipeline of independently-testable pieces. First launch `/app/audit_daemon` as a background subprocess and, separately, find its hardcoded auth token by static analysis of the stripped binary — since it's stripped of symbols but not of string data, `strings /app/audit_daemon | grep -i auth` (or similar patterns) is the standard way to recover embedded plaintext secrets from a stripped binary; don't try to guess it. Generate a self-signed cert/key with `openssl req -x509 -newkey rsa:2048 -nodes -keyout ... -out ... -days 365` into /home/user/certs/, then run a TLS-terminating proxy on 127.0.0.1:9443 that forwards decrypted bytes to the daemon's plaintext 127.0.0.1:9000 (Python's `ssl` module wrapping a socket forwarder, or `socat OPENSSL-LISTEN:9443,...  TCP:127.0.0.1:9000`, works). Your script's client half connects to 9443 over TLS, sends the recovered token + `\n`, then streams the response. For redaction, compile the three regexes given in the task (IPv4, hyphenated 16-digit CC, 32-hex-char token) and apply `re.sub` with `[REDACTED]` per line as you stream — keep patterns anchored with `\b` word boundaries to avoid over/under-matching, and write sanitized lines incrementally to clean_audit.log until the daemon closes the connection.