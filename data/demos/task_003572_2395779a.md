First fix the vendored library, since your detector's correctness depends on it: in `/app/networkx-source/networkx/algorithms/shortest_paths/unweighted.py`, look at `single_source_shortest_path_length` and `single_target_shortest_path_length` for a spot where `cutoff` is decremented before use (an off-by-one that makes paths at exactly the cutoff depth get missed) — remove that erroneous adjustment so the cutoff boundary is inclusive as networkx normally documents, then make sure your script imports this local package (e.g. set `PYTHONPATH=/app/networkx-source` or install in editable mode) rather than any system-installed networkx. For the detector: implement Condition A with a SQL window function (e.g. `SUM(amount) OVER (PARTITION BY source_account ORDER BY timestamp RANGE BETWEEN INTERVAL and CURRENT ROW)` in SQLite, or emulate a rolling 24h window with a self-join/correlated subquery per row) to find any account exceeding both the count and sum thresholds. Implement Condition B by building a directed graph from all transactions with networkx and using `has_path`/`shortest_path_length` with `cutoff=3` between any `SANCTIONED_*` and `CLEARED_*` node pair. Flag "EVIL" only when both conditions hold, then validate against both corpora and inspect any misclassification.
