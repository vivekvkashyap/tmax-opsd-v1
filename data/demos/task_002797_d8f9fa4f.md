First fix the vendored package: inspect `/app/signal-profiler-0.1.0/setup.py` for compiler flags passed to the C extension build (look at `extra_compile_args`) and remove any flag your compiler doesn't recognize (an unsupported `-m...` architecture flag is a classic cause of a failed `pip install -e .`); confirm the fix by successfully importing `signal_profiler` and calling `load_trace` on a sample CSV in `/app/traces/clean/`. For the detector, treat this as a two-signal classification problem rather than a single heuristic: (1) compute an FFT of each trace and look for a dominant frequency peak with energy well above the noise floor (periodic throttling signature) — e.g. compare the max non-DC spectral magnitude against the mean/median magnitude; (2) fit a KDE (or simpler: check for two separated modes via a bimodality test like Hartigan's dip test, or count local maxima in a smoothed histogram) to catch the multi-modal cache-miss case. Mark a trace "evil" if either signal fires, "clean" otherwise. Calibrate both thresholds empirically against the labeled `/app/traces/clean` and `/app/traces/evil` directories (their names tell you the ground truth) until you get 100% separation there, since the hidden test set is generated the same way, then wire up the `--input-dir`/`--output` CLI and JSON output exactly as specified.