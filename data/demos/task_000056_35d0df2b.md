Structure `audit_processor.cpp` as: read all lines from the input log into memory, process each line through detection+redaction, tally counters, then encrypt the fully-redacted buffer once at the end (never write the redacted plaintext to disk — build it as an in-memory string/vector). For intrusion detection, use simple `std::string::find` substring checks for `../` and `' OR '1'='1` per line (exact substring match, not regex, since the task specifies exact strings) and increment the intrusion counter without necessarily excluding that line from also being redaction-checked. For redaction, write two small regex passes with `<regex>`: one matching `cc=\d{16}` replacing the digits with `[REDACTED]`, another matching `token=[A-Za-z0-9]+` replacing the value with `[REDACTED]` — track whether any redaction happened per-line to increment "redacted lines" (a line can have both redactions but only counts once). For encryption, use OpenSSL's EVP API (`EVP_EncryptInit_ex` with `EVP_aes_256_cbc()`, reading the raw 32-byte key and 16-byte IV directly from `key.bin`/`iv.bin` as binary, not hex) and write the raw ciphertext bytes to the output path. Compile with the exact given g++ command and test decryption manually with the OpenSSL CLI using the same key/IV in hex to confirm round-trip correctness before considering it done.
