Load the JSON into SQLite first — e.g. with a short Python script using the `json` and `sqlite3` modules, or `jq` piped into `.import`, to create the `transactions` table with columns `tx_id, account_id, start_time, end_time, operation` matching the JSON keys exactly. Create a composite index like `CREATE INDEX idx_account_start ON transactions(account_id, start_time)` since queries partition by account and order by start_time. For the overlap detection, write a single SQL query using `LEAD(tx_id) OVER (PARTITION BY account_id ORDER BY start_time)` and `LEAD(start_time) OVER (...)` in a CTE to get, per row, the chronologically-next transaction on the same account; an overlap is where that next transaction's start_time is strictly less than the current row's end_time — filter with `WHERE next_start < end_time` in an outer query (window functions can't be referenced directly in the same `WHERE`). Order by `account_id DESC, current_tx ASC` and `LIMIT 10`. Export the three required columns to `/home/user/deadlock_risks.csv` with a header row, using `sqlite3 -header -csv` or manual formatting, matching the exact column names `account_id,current_tx,next_tx`.
