Treat the binary purely as a black-box oracle: write a small script that loops over every ID in entities.csv, calls `/app/legacy_audit_tool <id>`, and parses the `ACCESS: <id1> <id2> ...` (or `ACCESS: NONE`) line into an adjacency list, persisting the full graph to a file your C server reads at startup (e.g. a simple `id: neighbor1,neighbor2,...` text format) — do not try to disassemble or reverse-engineer the binary's internals, just harvest its outputs for all 200 nodes once. In the C server, load this adjacency list into an array-of-lists graph structure keyed by ID, then implement shortest path via plain BFS from source to target (BFS guarantees fewest hops, unlike DFS) tracking parent pointers to reconstruct the path once target is dequeued; if BFS exhausts without reaching target, respond `NONE\n`. For networking, `socket()`/`bind()`/`listen()` on 127.0.0.1:8888, `accept()` in a loop (fork or thread per connection, or handle sequentially), read until `\n`, parse `CHECK <src> <dst>`, run BFS, write the comma-joined path with trailing `\n`. Launch the compiled server with `&` or `nohup` and confirm it's listening (`ss -tlnp` or a test `nc`) before finishing.