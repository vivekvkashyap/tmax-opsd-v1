Scaffold with `cargo new analysis` inside `/home/user`, add `csv` and `serde` (with `derive` feature) to `Cargo.toml`, and implement the statistics by hand in `main.rs` rather than reaching for a heavy stats crate — Pearson correlation and its t-statistic only need sums, means, and variances/covariance computed in one or two passes over the parsed rows. Read the CSV with the `csv` crate into records, compute `R&D_Ratio = R&D_Spend / Total_Expenses` per row, then compute `r` via the standard formula `cov(x,y)/(std(x)*std(y))` and `t = r * sqrt((n-2)/(1-r^2))` exactly as given. For the "Top 3" list, sort a `Vec` of `(Company_ID, R&D_Ratio)` pairs descending by ratio (watch for ties/float ordering — use `partial_cmp`) and take the first three IDs. Format each number with 4 decimal places (Rust `{:.4}`) and write the report to `/home/user/report.txt` matching the exact three-line template, including comma-and-space separated IDs. Run `cargo run` from `/home/user/analysis` and confirm the file is produced before finishing.
