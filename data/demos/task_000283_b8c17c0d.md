Read `data.csv` with pandas, and compute Q1/Q3 with `numpy.percentile(non_nan_values, [25, 75])` (default linear interpolation, which matches typical grading conventions) rather than a manual quantile formula. Apply the outlier filter with strict inequalities exactly as specified (`< Q1 - 1.5*IQR` or `> Q3 + 1.5*IQR` removed), then count how many original NaNs there were. For imputation, call `numpy.random.RandomState(42).choice(cleaned_values, size=num_nans, replace=True)` as the *very first* random operation in your script (order matters for reproducibility — don't call any other RNG function beforehand), and append the result to the cleaned array to form `final_dataset`. For the Bayesian step, derive the closed-form Normal-Normal posterior: `posterior_var = 1/(1/alpha**2 + N/sigma2)`, `posterior_mean = posterior_var * (sum(x)/sigma2)` with `sigma2=1.0`. Implement leave-one-out CV over `alpha in [1.0, 5.0, 10.0]`: for each held-out point, refit posterior mean on the remaining N-1 points, accumulate squared error, and pick the alpha with lowest average MSE. Recompute final posterior mean/variance on the *full* final_dataset with that alpha, round to 4 decimals, and dump to JSON. Call `matplotlib.use("Agg")` before importing `pyplot` so `posterior.png` actually renders headlessly.
