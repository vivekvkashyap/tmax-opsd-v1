Write the C program in clear stages and check each with printf debugging before combining. First, parse both CSVs into small fixed-size arrays of structs (id, f1, f2) and (id, f3, f4) — do not assume matching row order between files; for each sensor1 record, linearly search sensor2 for the matching `id` (small datasets, so O(n^2) is fine) to build the joined `[f1,f2,f3,f4]` vector per id. Second, read the 4x2 PCA matrix and 2x1 model weights from their text files with `fscanf` into float arrays, checking read counts. Third, do the matrix multiply by hand: for the 1x4 * 4x2 step, each of the 2 output columns is the dot product of the feature vector with that column of the PCA matrix (row-major: element order in the file is row-by-row, 4 rows of 2 values each); then dot the resulting 1x2 vector with the 2x1 weight column to get the scalar score. Fourth, sort the results by ascending `id` before writing (e.g. simple insertion/bubble sort since n is small), and write `/home/user/predictions.csv` with header `id,score` and each score formatted to exactly one decimal place via `"%d,%.1f\n"`. Compile with `gcc -o pipeline pipeline.c` and run it to produce the file — verify by re-deriving the PCA/dot-product math by hand for one row to catch matrix-orientation mistakes (row vs column mixed up is the classic bug here).