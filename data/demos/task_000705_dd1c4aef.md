Start with git forensics: since `config.json` was committed then deleted, use `git log --all --diff-filter=D -- config.json` (or `git log --all --full-history -- config.json`) to find the commit that removed it, then `git show <commit>^:config.json` to recover its content and write it to `/home/user/async_service/config.json`. Find the culprit commit by walking `git log --oneline` (or `git bisect`) with a reproduction script that starts the server, opens+immediately-closes a socket, and checks `asyncio.all_tasks()` count afterward, rather than guessing from commit messages — confirm by diffing the suspected commit against its parent for a per-connection `asyncio.create_task` that's never cancelled. Fix `server.py` by cancelling and awaiting the background task in a `finally` block on disconnect/cancellation, then add an assertion after cleanup that the count of that connection's background tasks is 0. Finally emit `resolution_report.json` with the recovered secret and the full (not abbreviated) commit hash from git log.
