Two separable pieces: audio-driven config, and a hand-rolled validator. For the audio, transcribe `/app/qa_instructions.wav` (install/use `whisper` or another STT tool, or `ffmpeg` plus a speech-to-text pipeline) and read off the exact environment variable name and value it states, then write `/home/user/env_setup.sh` containing `export <NAME>=<VALUE>` matching precisely what was said — do not guess or invent a plausible-sounding variable. For `/home/user/math_filter.sh`, resist the urge to use a single regex; implement a real character-by-character scan in Bash (loop over `${#str}` indices with `${str:i:1}`) tracking: (a) a running paren-depth counter that must never go negative and must end at zero, (b) a "last character was an operator" flag to reject consecutive operators, and (c) reject immediately on any character outside the allowed set `0-9 + - * / ( )` — this is what blocks injection payloads like `$(...)`, backticks, or `;`. Only if the whole string passes all checks should you compute and print the result (e.g. via `bc`) and exit 0; any violation must exit 1 with no output. Validate against your own small clean/evil examples before trusting the corpus, and chmod +x the script.
