Inspect the `nodes`/`edges` schema first (`sqlite3 /home/user/research_data.db .schema`) to confirm `edges.source_id` depends on `edges.target_id`. In `analyze.sh`, accept `$1` as the target dataset name, then pipe a single SQL script into `sqlite3 -header -csv /home/user/research_data.db` with `.mode csv` and `.headers on` set inside the script (or via CLI flags) so output is emitted as clean CSV directly to stdout. Write a recursive CTE that starts from the row(s) in `edges` whose `target_id` resolves to the target dataset's `id` (looked up by name), recursively joins backward following `source_id -> target_id` chains, and accumulates `depth` by incrementing 1 each recursion step — take the minimum depth per dependent if multiple paths exist, since the task wants shortest path length. Then wrap that recursive result in an outer `SELECT` using `RANK() OVER (PARTITION BY category ORDER BY depth ASC, name ASC)` as `category_rank`, excluding the target dataset itself, and order the final output however matches the expected row ordering. Test by running the script and diffing your output against the CSV schema/column names character-for-character, since the verifier does an exact string comparison.
