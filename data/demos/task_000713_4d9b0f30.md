Treat the oracle as a black box you must characterize empirically, not guess at. Use `protoc` with `/app/schema/deps.proto` to generate protobuf bindings, then write small test-graph generators (a Python or C++ helper) that build serialized `Graph` messages for controlled cases: a single node, a simple linear chain, a diamond (multiple valid orderings) to reveal tie-breaking rules, a graph with several independent roots, and graphs containing a cycle (simple 2-node cycle, a cycle plus disconnected acyclic nodes) to reveal the cycle-report format. Feed each to `/app/bin/dep_solver_oracle <file>` and diff stdout carefully — capture exact whitespace, separators, prefixes, and ordering. From these observations, infer the underlying algorithm (this is a classic dependency/build-order problem, so consider standard topological-sort variants and how ties over ready-nodes get broken) and the precise success/cycle output formats. Only after your hypothesis matches every probe should you implement `solve_dependencies` in C++, compile it into `libdepsolver.so`, and build the `dep_solver` CLI wrapper that calls it and prints the buffer verbatim. Re-verify against fresh random graphs before finishing.
