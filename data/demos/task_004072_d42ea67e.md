Treat this as several independent subsystems to build and verify one at a time before wiring the full chain. For video analysis, extract frames from `/app/incident_record.mp4` at 1fps with `ffmpeg -i incident_record.mp4 -vf fps=1 frame_%03d.png`, then for each frame check if it's fully black by computing pixel statistics (e.g. `convert frame.png -format "%[fx:mean]" info:` with ImageMagick, or read pixel data in Python with PIL and check max/mean intensity is near zero) rather than assuming — count how many frames qualify and write that integer to `/home/user/black_frame_count.txt`. For the sanitizer, write `/home/user/sanitizer.c` reading all of stdin into a buffer, then check in order: does it contain the literal substring `DROP TABLE`, does it contain a run of >1000 consecutive `A` characters, and are its `{`/`}` brackets balanced (simple counter, not a full JSON parser) — if any check trips, exit nonzero with no output; otherwise echo the input verbatim to stdout and exit 0. Validate your sanitizer directly against every file in `/app/corpora/clean/` and `/app/corpora/evil/` yourself before moving on. For supervision, write `watchdog.sh` as a `while true; do /app/backend/telemetry_daemon; done` loop (so it restarts on any exit/crash) run in the background. Finally fix Nginx's config to proxy `/telemetry` to port 8080, gating requests through your sanitizer (e.g. an `nc`-based bridge script invoked so only sanitizer-approved payloads reach the daemon), reload Nginx, and confirm `curl localhost:80/telemetry` with a clean payload returns 200.