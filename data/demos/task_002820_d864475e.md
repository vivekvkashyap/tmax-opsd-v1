Don't fuzz /app/chunk_reader blindly to find the crash — first do static analysis: `file`, `strings`, and `objdump -d` (or a decompiler) to locate the header-parsing routine, since crashes in a stripped reader are almost always an unchecked read/seek past the file's actual size. Diff the sample files byte-for-byte with `xxd` across /home/user/samples/clean/ vs /home/user/samples/evil/ to identify the fixed-size header fields (magic bytes, then likely little-endian 32-bit integers) and hypothesize what each represents (e.g. a count, an offset) by correlating field values against each file's actual byte length (`stat -c %s`). Test your hypothesis: predict clean/evil for each sample from the field values vs. file size, and confirm it matches all 10 samples. Confirm further with `gdb`/`strace` on a clean and an evil sample to see exactly where/why the crash occurs. Once validated, encode the same bounds check in chunk_filter.py: unpack the header with `struct.unpack`, compare against `os.path.getsize`, and exit 1 if it would overflow, else exit 0.
