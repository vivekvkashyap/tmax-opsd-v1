Treat `/app/legacy_resolver` as a black box you must characterize empirically before writing any C code: feed it small, controlled inputs via stdin (a single edge, a diamond DAG, several nodes with no constraints between them, a 2-node cycle, a longer cycle) and diff the outputs to pin down (a) the exact success output format for a topological order, (b) the exact `CYCLE: ...` output format and which cycle it reports when several exist, and (c) the deterministic tie-breaking rule it uses among simultaneously-available nodes — don't assume, verify with cases where multiple nodes have no unresolved dependencies at once. Once you've nailed the behavior, implement it in C with O(V+E) data structures: adjacency lists, in-degree counts, and a min-heap/sorted structure for Kahn's algorithm to realize whatever tie-break order you observed; for cycle cases, implement whatever exact cycle-reporting behavior you measured. Then write the Go fuzzer to generate many random graphs (including cyclic ones), run both binaries, and assert byte-identical stdout — use this to catch edge cases you missed in manual testing, then iterate the C code against failures the fuzzer finds. Benchmark against the legacy binary only after correctness is airtight.
