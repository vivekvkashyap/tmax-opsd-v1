In Go, parse `fstab.conf` line by line, split each non-comment line on whitespace with `strings.Fields`, and match the row whose first field equals exactly `/dev/vdb1`; take its second field as the mount point. Open `<mountpoint>/network.log` and count occurrences of the literal substring `ERROR: CONNECTIVITY_LOST` (use `strings.Count` on the full content, or count per-line matches — same result either way). Parse `qemu.opts` by finding the token after a `-vnc` flag, splitting on `:` to get the display number, and computing port as `5900 + display`. If the connectivity-lost count is greater than 0: build the mailspool directory if needed, write `/home/user/mailspool/alert.eml` with the exact template (blank line between headers and body, `<count>` and `<port>` substituted), overwriting any existing file, and `os.Exit(1)`; otherwise `os.Exit(0)` without writing the alert. Compile with `go build -o /home/user/net_troubleshooter net_troubleshooter.go`. For supervision, hand-write the systemd unit at the given path with `[Unit]`, `[Service]` (`ExecStart=/home/user/net_troubleshooter`, `Restart=on-failure`, `RestartSec=5`), and `[Install]` (`WantedBy=default.target`) sections — do not attempt to start it, just place the file.
