In C, for each of the four files, read all values as `double` and compute the empirical mean, then the empirical *population* standard deviation using the given formula with denominator M=10000 exactly — using the sample (N-1) formula here is the classic mistake and will throw off every downstream number. Compute the squared 2-Wasserstein distance D per file with the closed-form Gaussian formula given (squared mean difference plus squared std difference against target μ=2.0, σ=2.0). Then perform ordinary least-squares linear regression of the four points (ln(dt), ln(D)) where dt = 1/N, using the standard closed-form slope formula `m = (n*Σxy - Σx*Σy) / (n*Σx² - (Σx)²)` — don't reach for a library, just accumulate the five sums (n, Σx, Σy, Σxy, Σx²) in a loop. Use `double` throughout, not `float`, to avoid precision drift versus a reference computation. Print/write the slope formatted to exactly three decimal places (e.g. via `printf("%.3f", m)`) into the output file with no extra whitespace or trailing content. Compile with `gcc -o convergence convergence.c -lm` and run it once to produce the file.
