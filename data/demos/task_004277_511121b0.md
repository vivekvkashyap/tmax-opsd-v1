Reverse-engineer `/app/legacy_oracle` by isolating one transformation at a time rather than guessing the whole pipeline at once. Feed it a payload of a single repeated character (e.g. all 'a's) to make any sorting step trivially visible or invisible; feed an empty-ish/short payload to see if a checksum-like 8-hex-digit field appears (a strong signal of a CRC/hash) and test whether it matches a standard CRC32 by comparing against a known CRC32 implementation on the same payload; feed alphabetic payloads and diff against transformations like Caesar/ROT ciphers or hex-encodings of the input to spot character-level substitution before any sorting/encoding step. Vary the version argument only after the payload logic is nailed down, to confirm the oracle's output format string. Use `strings`/`objdump -d` on the stripped binary to corroborate (e.g. spotting a CRC polynomial constant or a lookup table). Once you can reproduce `< 2.0.0` outputs bit-for-bit in your head/scratch script, implement both branches (legacy replica and the specified >=2.0.0 CRC32+Base32 format) in the Rust `/process` handler, then wire up `build_and_run.sh` and confirm the C integration client actually runs against the live service.
