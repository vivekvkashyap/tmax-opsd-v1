Split this into three independent pieces and verify each before wiring them together. (1) Correlation: load `/app/dataset.csv`, filter strictly to rows where `split == "train"` (exact string match, not substring), compute Pearson correlation between `featureA` and `featureB` with a standard stats library, and round to 4 decimal places only at the final formatting step, not mid-calculation. (2) Token recovery: run `tesseract /app/auth_token.png stdout` (or similar) to OCR the single line of text, then strip trailing whitespace/newlines — don't hardcode a guess, actually read the OCR output back into your server code/config so it stays in sync if the image changes. (3) Server: implement a minimal HTTP server bound to `127.0.0.1:8000` exposing `GET /api/v1/correlation`; check the `Authorization` header equals exactly `Bearer <token>` before doing any work, return 401 on missing/wrong token, and 200 with `{"correlation": <value>}` on success. Launch the server as a background/daemon process (e.g. `nohup ... &` or a systemd-less detach) so it stays alive after your shell command returns, and confirm with a local curl test before finishing.
