Write the bash script to drive `sqlite3` non-interactively (e.g. via `sqlite3 db_file <<'SQL' ... SQL` heredocs or `.import`). Import each CSV with `.mode csv` and `.import --skip 1 file.csv table` (or create tables explicitly first so types are right) since the files have headers. The key constraint is the query plan: create indexes on the join/filter columns actually used — `board_members(person_id)`, `board_members(company_id)`, `investments(investor_company_id)`, `investments(target_company_id)` — so SQLite's planner can do index `SEARCH` instead of a full `SCAN` on those tables. For the conflict logic, self-join `board_members` to itself on `person_id` (aliasing as bm1/bm2 for company A and company B respectively, with `bm1.company_id != bm2.company_id`), then join `investments` where `investor_company_id = bm1.company_id AND target_company_id = bm2.company_id`, joining back to `persons`/`companies` for names. Order results by the three name columns, write with `.headers on` `.mode csv` to `conflicts.csv`, and separately run `EXPLAIN QUERY PLAN` on the exact same SELECT, redirecting its output to `plan.txt` — inspect that output yourself to confirm it says `SEARCH ... USING INDEX` for board_members/investments, not `SCAN TABLE`.
