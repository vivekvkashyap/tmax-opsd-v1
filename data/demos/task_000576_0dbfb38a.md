Diagnose the two separate performance problems before rewriting: the deadlocks come from `SELECT ... FOR UPDATE` row locks colliding with the background traffic_generator's concurrent UPDATEs — since this script only reads and projects data, drop all locking entirely (plain SELECTs, or explicit `READ COMMITTED`/no-lock reads) rather than trying to tune lock timeouts. The slowness comes from N+1 querying per author; replace it with a single SQL query using a CTE/window function that joins authors to citations and computes each author's average score in one pass (e.g. `WITH scored AS (SELECT author_id, avg(score) ... GROUP BY author_id) SELECT ... FROM scored JOIN citations ...`), filtering to score > 50.0 server-side so you don't pull unnecessary rows. Consider adding an index on the citation table's author_id/cited_author_id columns via psql if it speeds up the join. For Redis writes, batch everything through a single `redis.pipeline()` (set score strings and sadd cites sets) instead of one round-trip per author, since network round-trips are what will blow the 2.5s budget. Time your script end-to-end locally with `python /app/extract_graph.py` and `time` before considering it done, and re-run it a few times to confirm it's robust to concurrent background writes, not just fast once.