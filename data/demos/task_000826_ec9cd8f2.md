Build the C filter first and test it in isolation before wiring up the pipeline. In `align_hash.c`, read lines with `fgets` into a buffer sized comfortably above 256 chars, split on commas (watch for the trailing newline on the payload field — strip it before hashing or you'll get wrong hashes). For timestamp alignment, don't do string math on the seconds — just find the last two chars before the trailing `Z` (positions 17-18 in `YYYY-MM-DDTHH:MM:SSZ`) and overwrite them with `'0','0'`. Implement DJB2 exactly as specified (`hash = 5381`, loop `hash = ((hash<<5)+hash)+c`) over `unsigned long`, and print with `%lu` — do not use a different hash or a cast that truncates. Compile with `gcc -o align_hash align_hash.c` and test manually with a `printf | ./align_hash` before trusting it. In the bash script, concatenate all `*.csv` in the data dir (skip header lines if any exist — this task's fixtures have none), pipe through the compiled binary, then dedupe by full line (`sort -u` is simplest and also gives you the final alphabetical sort in one step) and redirect to `final_output.csv`. Verify row count and column shapes manually before finishing.