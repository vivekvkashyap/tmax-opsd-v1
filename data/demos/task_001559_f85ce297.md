Diagnose the 502 methodically: a 502 from Nginx proxying to a unix socket almost always means Nginx's `proxy_pass` socket path doesn't match where Gunicorn is actually bound, or the socket directory doesn't exist/isn't writable. Read `/home/user/nginx/nginx.conf` to see which socket path it expects, create that directory with correct permissions if missing, and start Gunicorn bound to exactly that same socket path (e.g. `gunicorn --bind unix:<path> server:app` run from `/home/user/app`), then reload/restart Nginx and confirm `curl http://127.0.0.1:8080/...` no longer 502s before touching the transformation script. For `/home/user/processor.py`, treat `/app/oracle_processor` as a black box: feed it a battery of test inputs via stdin (empty string, single chars, mixed-case letters, digits, punctuation, long strings) and diff outputs against inputs to infer the transformation rule — implement whatever behavior you observe in Python reading all of stdin and writing to stdout, then re-verify by piping the same test inputs through both the oracle and your script and diffing outputs before wiring it into the Flask app's subprocess call. Finally confirm the full chain end-to-end with a POST to `http://127.0.0.1:8080/process` and leave all services running in the background.