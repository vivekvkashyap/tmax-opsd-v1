In `/home/user/audit_analyzer.cpp`, read `audit_logs.csv` line by line, splitting on comma, and build two structures: an adjacency list `map<string, set<string>>` mapping each source to its set of direct targets (for O(1) `A->C` edge existence checks), and reuse it as the "out-neighbors" list for both hops. For each edge `A->B` (iterate every stored edge), iterate B's out-neighbors `C`; for each such `C`, check if `A` also has a direct edge to `C` in the adjacency set — if so, that's one triangle violation, so increment a per-A violation counter (use a `map<string,int>` so you don't miss zero-violation nodes and can iterate deterministically). After processing all edges, find the max violation count; among ties, pick the alphabetically first source (sort candidate names or compare directly). Out-degree centrality of the winner is simply the size of its adjacency set (or count of edges with that node as source, including duplicates if any exist in the CSV — decide whether to dedupe edges, and check the CSV for duplicate rows). Write the report with exact literal labels `Highest Risk Entity:`, `Violation Count:`, `Total Out-Degree:` each on its own line, then compile with g++ and run to confirm the file matches the required format exactly.