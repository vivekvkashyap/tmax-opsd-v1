Treat this as three diagnostics feeding one pipeline; validate each stage before moving on. (1) Vendored build: read `/app/log-query-1.0/Makefile` for two defects — a missing link flag for a threading library and a wrong path baked into a `-D` macro — fix both and confirm `make` produces the binary. (2) Core dump: rather than parsing ELF structures, use a string-scan (`strings /app/authd.core | grep AUTHKEY-` or equivalent) to locate the fixed-format token and write the key to the output file. (3) The C recovery tool has two bugs: an O(N^2) leak from repeated small reallocations (fix with chunked reads into a geometrically-growing buffer, doubling capacity) and a boundary bug where the newline search stops early when a match straddles a read-buffer edge (fix by carrying a small overlap window across chunks instead of resetting state per chunk). Test correctness and timing on a large generated image before running the final `AUTH_KEY`-gated `log-query parse` command.
