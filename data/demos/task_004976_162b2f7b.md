Stream-decompress each `.gz` file directly with Python's `gzip.open(path, 'rb')` and `.read()` rather than shelling out to extract to disk first, then check the first 4 bytes for the ELF magic `\x7fELF` before treating it as valid — skip non-matching files entirely. For valid files, compute `hashlib.sha256(data).hexdigest()`, and maintain a dict mapping checksum -> first-written filename as you iterate in a deterministic order (e.g. sorted filenames) so duplicates are detected consistently. Write each unique file's decompressed bytes to `/home/user/curated/<name-without-.gz>`, and for repeat checksums use `os.link(first_path, new_path)` to create a real hard link (not a copy or symlink) — the grader checks inode equality via `os.stat().st_ino`, so a plain copy will fail. Build the manifest dict of {filename: checksum} for every extracted-or-linked file (duplicates included, pointing to the same checksum) and dump it as JSON. Finally, tar+gzip the whole curated directory with Python's `tarfile` module (or `tar czf`) into `/home/user/curated_archive.tar.gz`. Double check with `os.stat().st_nlink` that a unique file (no duplicates) still shows nlink==1.
