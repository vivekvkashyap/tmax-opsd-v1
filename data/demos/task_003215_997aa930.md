Design `/home/user/analyze_backups.sh` around a single recursive SQL query rather than shell-side graph walking. Use a recursive CTE against the `dependencies` table starting from the target database's id (looked up by name via a parameter), walking `parent_id -> child_id` edges upward (i.e., from the target, find its parents, then their parents, etc.) while tracking a `depth` column that increments by 1 each hop and starts at 0 for the target itself; union in all discovered ancestor rows plus the target row itself, deduplicating by database id (watch for diamond dependencies producing multiple paths to the same ancestor — take the max depth per database). Order the resulting database set by `depth DESC, name ASC` to get the restoration order. For each database in that order, correlate against `backups` with a `WHERE status='SUCCESS' AND backup_timestamp < :cutoff ORDER BY backup_timestamp DESC LIMIT 1` (or an equivalent MAX-with-subquery), skipping any database with no qualifying backup. Sum `size_bytes` across the selected backups for the total. Drive all of this from bash by building the SQL as a heredoc or `-cmd` string passed to `sqlite3 -separator '|' /home/user/db_metadata.sqlite`, parsing pipe-delimited output with `awk`/`read`, and formatting the final numbered list plus `Total Size:` line into `/home/user/restore_plan.txt` exactly as specified — treat the two CLI args generically since the grader will invoke the script with different target/cutoff values than shown.