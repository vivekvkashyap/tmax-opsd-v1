Start by reading `ref.py` carefully and translating its loop/state exactly into Go (same modulus, same iteration bounds, same accumulation order) — a single off-by-one in the loop range or accumulation step will silently diverge from the reference. For the CGO integration, remember the function is defined in a `.s` assembly file, not C source: you cannot just `#include` it — you need a matching C declaration (from `fast_math.h`) in your cgo preamble, and you must get the assembly object linked into the final Go binary. The most reliable route is to assemble `fast_math.s` into `fast_math.o` with `gcc` (or `as`), then pass it to the Go build via cgo LDFLAGS (e.g. `#cgo LDFLAGS: fast_math.o` in the Go file's C comment, or explicitly link the object in build.sh) — don't assume `go build` will find a loose `.s` file automatically. Write `build.sh` to assemble first, then `go build`, producing `/home/user/math_util/math_runner`, and make it executable. Finally run the built binary and redirect stdout to `result.txt`; cross-check by also running `ref.py` yourself locally to confirm your Go/assembly pipeline produces the identical integer before considering it done.