Split this into the two independent parts and verify each in isolation before combining. For the nginx piece, edit `/app/nginx.conf` adding two `location` blocks under the port-8080 server: `location /api/ { proxy_pass http://localhost:5000/; }` and `location /cache/ { proxy_pass http://localhost:7379/; }` — note the trailing slash on proxy_pass matters, it strips the location prefix before forwarding. Restart/reload nginx via the provided start script and confirm with curl against both routes before moving on. For the Go spectral tool, implement it as pure, deterministic math: map A/C/G/T to the given numeric values, compute a DFT (roll your own O(n^2) sum-of-complex-exponentials if you don't want a dependency — sequence lengths are small so speed doesn't matter), take squared magnitudes as the power spectrum, normalize to sum to 1 (falling back to uniform 1/N if the sum is exactly 0), then compute Euclidean distance to the uniform distribution 1/N and print with `%.6f`. Since you'll be checked by fuzz-equivalence against a hidden oracle on many random ACGT strings, test your program against several inputs by hand-deriving the DFT math carefully — getting the exact normalization and rounding right matters more than performance.
