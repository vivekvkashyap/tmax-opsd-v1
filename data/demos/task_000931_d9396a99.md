Tackle this service-by-service, verifying each in isolation before wiring the pipeline together. First fix the Rust ownership bug in `src/main.rs`: a value like `app_name` is moved (e.g. into `format!`) and then used again afterward — reorder so the borrow used in `hash(&app)` happens before or independent of the move, or clone the string where cheap; confirm with `cargo build` until it compiles clean. Second, generate Python protobuf/grpc bindings from `proto/manifest.proto` using `python -m grpc_tools.protoc -I proto --python_out=... --grpc_python_out=...` targeting the `python_gateway` directory, and check the generated `_pb2.py`/`_pb2_grpc.py` imports resolve. Third, write `sanitizer.py`'s `is_safe` as a recursive walk over dicts/lists/strings — for dict values recurse into nested dicts and lists, for list items recurse per element, and for string leaves check each restricted substring (`&&`, `|`, `;`, `$`, `../`) with plain `in` checks; a naive version that only checks top-level string values will miss nested injections in the corpora. Fourth, implement `/validate` in `app.py` to call `is_safe` before touching gRPC, build a protobuf request from the JSON fields, call the Rust service via a grpc stub, cache `signature` in Redis keyed by `app_name`, and return the JSON contract exactly. Finally get `start.sh` backgrounding all three processes in the right order (Redis, then Rust gRPC, then gateway) and test end-to-end with `curl` before trusting the corpus run.