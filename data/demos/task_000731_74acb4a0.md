Work through the Python 2→3 migration methodically, not with a blind rewrite: (1) Compile the C shim first with `gcc -shared -fPIC -o lib_calc.so lib_calc.c` so ctypes has something to load. (2) Break the `vm.py`/`checksum.py` circular import — since `checksum.py` imports `MAGIC_CONSTANT` from `vm.py` while `vm.py` imports `checksum`, move the shared constant to a separate module (or import `vm` lazily inside the function that needs it, or restructure so `checksum.py` doesn't need to import `vm` at module load time) rather than reordering imports superficially. (3) Fix the ctypes FFI bug: `ctypes.c_char_p` needs actual `bytes`, so encode the Python 3 `str` with `.encode('utf-8')` before passing it into `lib.compute_checksum`, and pass the byte length. (4) Run `python3 main.py` and confirm no `ImportError`/`TypeError`, then inspect `output.txt`. (5) Write `ci_test.sh` that compiles the .so, runs `python3 main.py`, checks its exit status, and exits 0 only on success — test it standalone with `./ci_test.sh; echo $?`. Do not alter the checksum algorithm itself, only the language-compatibility issues.