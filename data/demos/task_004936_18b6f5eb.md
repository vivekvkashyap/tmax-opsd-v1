Work in two clean phases: fix the library, then write the client. First, read `Packer.cpp`'s directory-walking code in `/app/libdata-packer-1.0` — find the `recursive_directory_iterator` call using `follow_directory_symlink`. Add a `std::set<std::pair<dev_t,ino_t>>` (or a set of `std::filesystem::canonical()` paths) that you populate as you descend and check before recursing into a symlinked directory; if the canonical target/inode is already in the set, skip it instead of recursing — this breaks cycles while still following non-cyclic symlinks. Rebuild the static archive with `ar rcs libdatapacker.a *.o`. Then write `parser.cpp`: walk `/workspace/project_files` with the fixed traversal, and for each regular file use `mmap`/`mmap`-backed reading (not `ifstream`) to check the 4-byte `"DATA"` magic, read the little-endian `uint32_t` length at offset 4, and slice `S` bytes starting at offset 8, adding each chunk to the archive under its relative path. Compile and link against your fixed static lib into `/home/user/fast_parser`. The verifier times execution and checks output correctness, so avoid buffered stream I/O entirely on the hot path.