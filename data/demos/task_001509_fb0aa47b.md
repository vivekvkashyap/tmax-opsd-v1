Diagnose 502 errors by working backward through the proxy chain, not by guessing: check `/home/user/nginx/nginx.conf` to see which upstream port Nginx's `proxy_pass` targets, then check `/home/user/app/server.py` to see which port the Python backend actually listens on — a 502 from Nginx almost always means nothing is listening where it expects. Compare the two ports directly; if they differ, fix the mismatch on the backend side (the task says not to change Nginx's listening port 8080, so adjust `server.py` instead of the proxy_pass target). After editing, use `/home/user/cicd/deploy.sh` to restart the backend in the background (check it kills any stale process first) and start Nginx with `nginx -c /home/user/nginx/nginx.conf`. Verify manually with `curl http://127.0.0.1:8080` before writing `/home/user/verify.py` — have it perform an HTTP GET (e.g. via `urllib.request` or `requests`) against that URL and write the response body exactly as received to `/home/user/success.log`. Run everything end-to-end once to confirm the log file is populated correctly.