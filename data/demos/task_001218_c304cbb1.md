Start by OCR'ing `/app/scaling_rules.png` (e.g. `tesseract /app/scaling_rules.png stdout`) to recover the exact linear transformation formulas for `Area` and `Age` — read the extracted text precisely rather than assuming typical scaling conventions, since the coefficients/offsets are specific to this image. Build the ETL so the fixed image-derived transforms are applied to Area/Age unconditionally (they don't depend on the split), but be strict about leakage for `Income`: compute the z-score mean/std only from the training partition after the 80/20 split, then apply that same mean/std to both train and test. Fit a `LinearRegression` on the transformed features to predict `Price`, and persist the learned model coefficients plus the Income mean/std (e.g. to a JSON or pickle file) so the serving process can reload them without retraining. Implement the `/predict` POST endpoint (Flask/FastAPI) to apply the identical transform pipeline — same Area/Age formulas, same stored Income mean/std — before calling the model, returning `{"predicted_price": <float>}`. Start the server backgrounded (e.g. `nohup ... &`) bound to `127.0.0.1:5000`, write its PID to `/app/api.pid`, and manually `curl -X POST` a sample payload to confirm you get a well-formed 200 JSON response before finishing.