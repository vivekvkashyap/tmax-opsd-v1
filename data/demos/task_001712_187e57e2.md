Work entirely with in-memory byte streams, never touching disk for intermediate extraction. In Python: open the tar.gz with `tarfile.open(path, 'r:gz')`, extract the `project.zip` member via `extractfile()` into a `BytesIO`, then open that with `zipfile.ZipFile(...)`. List its namelist(), filter to entries ending in `.txt`, and sort those names alphabetically (string sort on the full in-zip path, not just basename) before reading — order matters for the final concatenation. For each sorted entry, read via `zf.read(name)` and `.decode('utf-8')`, joining pieces with a single `\n` between files (not after the last one, and not doubling newlines already inside file content). For the atomic write requirement, write the concatenated text to a temp file in the same directory (e.g. via `tempfile.NamedTemporaryFile(dir=..., delete=False)` or a fixed `.tmp` path), close/flush it, then call `os.replace()` or `os.rename()` to move it to /home/user/final_doc.md — this is what makes the write atomic, not a direct `open(...,'w')`. Print `len()` of the final concatenated string to stdout, then run the script with `python3 build_docs.py > /home/user/char_count.txt` so the count lands in that file.