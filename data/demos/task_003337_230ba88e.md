Merge the two dataframes on `id` (inner join is fine since both cover the same ids), then compute cosine similarity per row as `dot(truth_vec, model_vec) / (norm(truth_vec) * norm(model_vec))` — vectorize this over the merged rows rather than looping in pure Python. Group by `category`, take the mean similarity, and when writing the final CSV format each value with a fixed 4-decimal string (e.g. `f"{x:.4f}"` or pandas `.round(4)` combined with explicit string formatting) so values like exactly `1.0` render as `1.0000`, not `1.0` — plain `round()` alone can drop trailing zeros when the CSV is written. Sort by `category` alphabetically, write with `header=True, index=False`, exactly two columns (`category,mean_similarity`), then compress with `gzip` (or `pandas.to_csv(..., compression='gzip')`) to produce `/home/user/category_metrics.csv.gz`. Double check by gunzipping and catting the result before finishing.
