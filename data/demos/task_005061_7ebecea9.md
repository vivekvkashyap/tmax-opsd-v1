Work each sub-task independently, then assemble the JSON last. (1) Extract frames from `/app/breach_capture.mp4` with `ffmpeg -i breach_capture.mp4 -vf fps=<n> frame_%03d.png` (or seek directly to ~5s with `-ss 5`) and read the on-screen text to recover the exact dropped filename — don't guess it. (2) Find the ELF under `/var/www/uploads/` (path traversal means it may not be in the expected images subfolder — use `find /var/www -type f`), then use `readelf -x .bk_port <file>` or `objdump -s -j .bk_port` to dump the raw bytes of that section and interpret them as a little/big-endian 16-bit integer to get the port — check both endiannesses against sanity. Cross-check `/app/manifest.sha256` with `sha256sum -c` and separately `find /var/www -perm -0777` (or `stat`) to find the one file with anomalous 0777 permissions. (3) Parse the cert with `openssl x509 -in /etc/ssl/certs/rogue.pem -noout -issuer` to get the Issuer CN. (4) For the sanitizer, do NOT read or run `/app/oracle_sanitizer` as a shortcut for producing bit-exact output ahead of time — instead feed it many varied traversal-heavy strings (`../`, `..\\`, `./`, `//`, URL-encoded `%2e%2e%2f`, nested combos) via stdin, diff your script's output against the oracle's, and iteratively refine your Python logic (a naive "strip ../ globally" often differs from an oracle that only strips left-to-right in a single pass) until outputs match across your generated fuzz corpus.
