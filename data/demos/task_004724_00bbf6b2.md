Scaffold with `cargo new build_broker`, add `axum`, `tokio` (full features), `serde`/`serde_json`, and `tokio-tungstenite`/`futures-util` to Cargo.toml, and build incrementally, testing each feature with `curl` before moving on. Set up an axum router with `POST /api/build/:target` bound to `127.0.0.1:8080`; inside the handler, validate `:target` is exactly `rust` or `cpp`, returning `StatusCode::BAD_REQUEST` otherwise. For rate limiting, keep a shared `Arc<Mutex<HashMap<String, Instant>>>` (or per-target atomics) in app state, recording the last accepted request time per target; on each request compare `Instant::now()` against the stored time and reject with `StatusCode::TOO_MANY_REQUESTS` if under 1 second has elapsed, otherwise update the stored time and proceed. Parse the request body line-by-line for `CMD <string>`, `ENV <key>=<value>` (accumulate into a map), skip blank lines, and stop parsing as soon as `RUN` appears — if `RUN` never appears, just return 200 without dispatching. On `RUN`, build the JSON object matching the required shape, open a fresh websocket connection to `ws://127.0.0.1:9000/dispatch`, send it as a single text frame, then close and return 200. Run the server persistently (background process or tmux) and redirect output to broker.log so the verifier's HTTP test sequence can reach it.
