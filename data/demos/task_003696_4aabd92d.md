This is a strict binary stdin/stdout C program — no video decoding needed, the video file is just narrative context; your program only ever sees the 16-byte float records described. Read the first 4 bytes as a little-endian (native) `uint32_t N` with `fread`, then read all remaining 16-byte records (`float q1,q2,q3,q4`) into a growable buffer until EOF, keeping the original stream order/indices. Project each record to 2D as `(q1+q2, q3+q4)` (top two vs bottom two quadrants). Compute the mean of dim1 and dim2 using ONLY the first N records (this is the leakage-avoidance step — do not include query records in the mean), then subtract that mean from every record's projection, both training and query. For each query record (index N, N+1, ...), linear-scan the N training projections and find the minimum squared Euclidean distance, breaking ties by lowest index, and write its `uint32_t` index to stdout with `fwrite` (binary, not text). Read all of stdin with a loop that handles partial reads/short reads correctly since input can be piped, and be careful with `float` vs `double` precision consistency across the mean/distance math to match a reference implementation exactly.
