Start by exploring the schema interactively before writing the pipeline: open `/home/user/telemetry.db` with `sqlite3` (or Python's `sqlite3` module) and run `.schema` / `SELECT * FROM <table> LIMIT 5` to learn the real table/column names (they won't necessarily match the task's plain-English description). Once you know the table, write the extraction as a single SQL query using a window function — `LAG(created_at) OVER (PARTITION BY uid ORDER BY created_at)` to get each user's previous event timestamp, then compute the difference in seconds against the current event's timestamp (parse both as datetimes, e.g. via `julianday()` diff * 86400, or parse in Python after fetching) — but only emit rows where the current row's action is the checkout event type, and let the first-event-per-user case naturally come out NULL from the window function. Load `/home/user/schema.json` and validate each candidate record dict with `jsonschema.validate` inside a try/except, silently skipping (not crashing on) records that fail — this is what filters out the type-mismatched rows. Write surviving records as one JSON object per line to `transformed_events.jsonl`, keeping only the four required keys in the exact types the schema demands (cast `uid`/`event_id` to `int`, not numpy or string types).
