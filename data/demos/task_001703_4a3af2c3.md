Treat `/app/primer_match` as a black-box oracle: probe it with small, controlled single-base sequences (e.g. reference="A", primer="A" vs reference="A", primer="C", etc., and short windows isolating one position at a time) to determine the score contributed by every base pair (A-A, A-C, A-G, A-T, C-C, C-G, C-T, ..., 16 combinations, likely symmetric) — vary one pair at a time so each probe isolates a single (ref_base, primer_base) score, and build up the full 4x4 matrix this way rather than guessing. Confirm your inferred matrix reproduces the oracle's output exactly on a handful of longer multi-window test cases before trusting it. Then implement the Rust program: read the reference and primer list, and for each primer slide it across every valid offset in the reference (no gaps/indels), sum the matrix scores for that window using your byte-indexed lookup table, and keep the maximum per primer — this is an O(reference_len * primer_len) inner loop per primer, so use byte slices (`&[u8]`) and a flat `[i32; 16]` or `[[i32;4];4]` lookup indexed by base rather than string comparisons or HashMaps for speed. Compile with `rustc -O` (or `cargo build --release`) to `/home/user/fast_primer_match`, and verify output order in `scores.txt` matches `primers.txt` line order exactly.