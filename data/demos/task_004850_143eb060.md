In `/home/user/tracker.py`, walk `configs_v2` with `os.walk`, compute each file's relative path with `os.path.relpath`, and hash file contents with `hashlib.sha256` reading in binary mode (chunked reads to be safe on large files). For each file, check whether the same relative path exists under `configs_v1`; if not, classify NEW, else compare SHA-256 hashes and classify MODIFIED only if they differ (identical files must be skipped entirely — don't copy or log unchanged files). For each NEW/MODIFIED file, build the destination path by taking the relative directory unchanged and renaming just the basename: split off the extension with `os.path.splitext`, insert `_<first-8-hex-chars-of-hash>` before it, and `os.makedirs(..., exist_ok=True)` the destination subdirectory before `shutil.copy2`. Collect `(status, relpath, new_relpath)` tuples, sort them alphabetically by the original relative path, and write `/home/user/changes.log` lines in exactly the format `[STATUS] rel/path -> rel/path_hash.ext`. Run the script so both `/home/user/incremental_backup/` and `/home/user/changes.log` get populated.
