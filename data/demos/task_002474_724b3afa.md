Write `smoother.c` to read the input CSV line by line, maintaining a small ring buffer (or just the last up-to-2 previous values) so each row's smoothed value is the mean of itself plus up to 2 prior values (1 value for row 1, 2 for row 2, 3 thereafter) — print `timestamp,%.2f\n` per row, matching the average exactly by using floating point division by the actual count of values seen, not always dividing by 3. In `pipeline.sh`: compile with `gcc smoother.c -o smoother`, `mkdir -p /home/user/clean_data`, then launch the 4 smoother invocations as background jobs (`smoother in out &` for each of the 4 files) and immediately follow with `wait` so all finish before merging — this parallel-then-wait pattern is the key mechanic naive sequential scripts miss. Finally concatenate the 4 `clean_*.csv` outputs and sort numerically by the first (timestamp) column, e.g. `sort -t, -k1,1n -o /home/user/final_clean.csv <(cat /home/user/clean_data/clean_*.csv)`. Test the full pipeline end-to-end and eyeball a few rows' arithmetic by hand to confirm the windowing and formatting are correct before finishing.