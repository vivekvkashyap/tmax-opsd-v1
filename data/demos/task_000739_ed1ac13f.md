This task has two independent parts — tackle them separately.

For the video: use `ffmpeg` to extract frames from `/app/db_monitor.mp4` (e.g. `ffmpeg -i db_monitor.mp4 frames/frame_%04d.png`), then scan frames in order — either visually/programmatically with OCR (e.g. `tesseract` or a Python OCR library) or by diffing consecutive frames for a sudden text change — to find the exact frame where the alert text and transaction ID first appear. Recover the frame number and transaction ID directly from what you observe in the video; don't guess a value. Write them to `/home/user/video_analysis.json` in the exact required schema, using ffmpeg's frame indexing convention (verify whether it's 0- or 1-indexed relative to how the checker counts frames).

For the deadlock detector: parse the JSON, build a directed "waits-for" graph where transaction A points to transaction B if A is waiting for a resource that B currently holds (cross-reference `waiting_for_locks_on` against every other transaction's `holds_locks_on`). Detect cycles with a standard DFS using a recursion/visiting stack (three-color marking: unvisited/in-progress/done) rather than just tracking visited nodes, so you correctly catch cycles including self-loops. Print exactly the required JSON status string, nothing else, to stdout. Test against self-referential and deeply chained cases before trusting it.
