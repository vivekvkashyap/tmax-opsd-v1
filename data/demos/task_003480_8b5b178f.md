Build this in three clearly separated stages and check each before moving to the next. First, write a normalization pass over raw_backups.jsonl: for each line, use `dict.get` fallbacks (`database` or `db`, `status` or `state` with "ok"→"success", `size` or nested `metrics.bytes`, `ts` or `time`) to build a uniform record, then load these into a `backup_runs` SQLite table with columns for database_name, status, size_bytes, timestamp. Second, compute the rolling average per database using either SQL window functions (`AVG(...) OVER (PARTITION BY database_name ORDER BY timestamp ROWS BETWEEN 3 PRECEDING AND 1 PRECEDING)` restricted to prior successful rows) or, if window-function semantics get fiddly with the "successful only, current excluded" rule, compute it iteratively in Python by maintaining a per-database queue of up to the last 3 successful sizes seen so far, comparing each new successful backup against 1.2× that average before pushing it in. Anomalies need a non-null (i.e., at least 1 prior successful) average. Third, sort qualifying anomalies by timestamp descending, slice the first page_size=2 rows, round rolling_avg to 1 decimal, and write the exact JSON shape requested including the true total_anomalies count across all pages.