This is a black-box hash reverse-engineering task: don't try to disassemble the stripped binary first — instead treat `/app/manifest_hash_oracle` purely as an input/output oracle and probe it. Feed it known small inputs (empty string, single bytes like `a`, `ab`, `abc`, sequences of repeated bytes) and record the 8-hex-char outputs; compare against common rolling-hash families (DJB2 variants with different multipliers/seeds, FNV, simple polynomial hashes) computed by hand or in a scratch script for the same inputs until one matches exactly across all your probes — verify on several more random byte strings, including non-ASCII/binary bytes, before committing to it. Implement the matching algorithm in your `/home/user/manifest_tool` reading raw bytes from stdin (open stdin in binary mode) so it handles arbitrary binary input, not just text. For the watcher, use `inotifywait` (or a polling loop) on `/home/user/project/`, and make manifest updates atomic by writing to a temp file in the same directory then `mv`-ing it over `/home/user/manifest.txt` (same filesystem, so `mv`/`rename` is atomic) rather than editing the manifest in place.
