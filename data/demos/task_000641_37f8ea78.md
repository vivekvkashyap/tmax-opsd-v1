Extract the rule from the image before writing any filtering logic — run OCR (`tesseract /app/threshold.png stdout`, installing tesseract/imagemagick if needed) and read the resulting text carefully; it describes both a nucleotide-frequency filtering condition and an equation you must solve algebraically by hand (a simple cubic) to get the numeric threshold — solve it yourself from the extracted equation rather than assuming a round number. Then write `/home/user/filter_fasta.sh` to: strip header lines (starting with `>`), concatenate remaining sequence lines while stripping newlines/whitespace and normalizing case, count occurrences of A/C/G/T only (excluding N or other ambiguity codes from the denominator, per "counting only A, C, G, T to find the total"), compute each of the four frequencies as count/total, and REJECT if any single nucleotide's frequency strictly exceeds the threshold you derived, else ACCEPT. Validate against every file in both `/app/clean/` and `/app/evil/` — loop over the directories and check each file's classification matches expectations, adjusting the OCR'd threshold interpretation if any clean/evil file misclassifies, since OCR text can be noisy.
