Write this as a single awk-driven pipeline reading the CSV with `-F,` and skipping the header (`NR==1{next}`). For cleaning, test `$2==""` to impute age=35 and `$3<0` to clip income to 0, applying these before computing the LTV formula so downstream math uses the cleaned values, not the raw ones. Compute `predicted_LTV` with the exact linear combination given and print with `printf "%s,%.2f\n"` to get two decimals (awk's rounding is fine — no need to hand-implement banker's rounding). For nearest-neighbor, do a second pass (or store U001's score1/score2 in a first pass) and for every other user compute `(s1-s1_u1)^2+(s2-s2_u1)^2`, tracking the minimum in an awk variable, then print just the winning `user_id` with no extra whitespace to `/home/user/nearest_to_U001.txt`. Chain both passes inside `/home/user/pipeline.sh`, make it executable, and avoid needing Python — everything here is expressible in awk/bash alone.
