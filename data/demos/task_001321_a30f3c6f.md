Build the pipeline as a strict streaming filter, avoiding any tool (like naive pandas/awk) that silently coerces integers to floats. In `pipeline.sh`, take the video path as `$1`, then read stdin line by line (skip the header row), split each line on the comma into `event_id` and `frame_idx`; if `frame_idx` is empty (or non-numeric), skip the row entirely — do not print anything for it. For a value that looks like a float (e.g. contains a decimal point), normalize it to a plain integer string before using it (e.g. via `printf "%.0f"` or trimming), since the spec requires frame_idx be treated strictly as an integer with no trailing `.0`. For each valid row, extract the exact frame at that index with ffmpeg's `select` filter keyed on `n` equal to the frame index and `-vframes 1`, writing to a temp PNG (use a unique/scratch filename so concurrent/fuzz runs don't collide), then run the exact ImageMagick `convert ... -colorspace Gray -format "%[fx:mean]" info:` command to get brightness. Print the header once, then one CSV line per valid row with no trailing whitespace or extra debug output — since verification is byte-for-byte against a reference, test your script manually against `/app/experiment_video.mp4` with a small crafted CSV (including an empty frame_idx and a "N.0" float case) and diff the output carefully for stray spaces or newlines.
