Work through this as a standard three-part security remediation, fixing each script in place without changing its overall architecture. In `server_setup.sh`, find the line that writes `db_secret.key` and add an explicit `chmod 600 /home/user/www/config/db_secret.key` immediately after it (don't rely on umask). For the HTTP response, locate the `socat ... SYSTEM:"echo HTTP/1.1 200 OK; echo Content-Type: ..."` string and insert additional `echo` statements for `X-Content-Type-Options: nosniff` and `X-Frame-Options: DENY` into that same header block before the blank line that separates headers from body. In `greet.sh`, the vulnerability is the `eval "echo Hello, $NAME"` pattern — remove `eval` entirely and instead sanitize `NAME` by stripping shell-metacharacters with something like `NAME=$(printf '%s' "$NAME" | tr -d ';&|$`')` and then simply `echo`/`printf` it directly (no eval, no command substitution of the input) so injected shell syntax can't execute. Finally, write `audit_report.json` matching the exact schema given, listing the three fixed vulnerability categories, the permission value, and the two added header names as literal strings.
