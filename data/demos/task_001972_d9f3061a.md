Start by running `go mod init mlops` inside `/home/user/mlops`, then `go get gonum.org/v1/gonum/mat` so the dependency is recorded in go.mod before you write logic. Parse the CSV with `encoding/csv`, keeping the header row separate. For each row build the 5-dim embedding by counting `a,e,i,o,u` case-insensitively over the Description field (iterate runes, lowercase, compare against a small set). Use `gonum/mat.NewVecDense` to wrap each embedding and compute cosine similarity as `mat.Dot(a,b) / (norm(a)*norm(b))`, guarding against a zero-norm baseline vector. Apply the two filters (Latency_ms < 50 AND cosine similarity >= 0.85) together, then sort the surviving IDs with `sort.Strings` before writing them one-per-line — do not rely on CSV row order, since the expected result is alphabetical. For the benchmark, isolate ONLY the dot-product/cosine math in the timed loop (no parsing, no `mat.NewVecDense` allocation inside the loop) using `time.Now()`/`time.Since` around a `for i := 0; i < 100000; i++` loop, then write the elapsed milliseconds (as a plain number) to the result file. Run `go build`/`go run` to confirm it compiles and actually produces both output files before finishing.
