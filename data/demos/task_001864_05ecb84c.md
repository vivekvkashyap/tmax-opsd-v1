Before touching the bootstrap logic, isolate the parsing bug: write a tiny test program that calls `fastcsv` on your 5-column CSV and prints every field per row — you'll see the `amount` column (4th, index 3) always reads as zero/blank. Grep the library headers/source for a fixed-size column limit (a `#define MAX_COLUMNS <n>` or a statically sized array smaller than 5) and bump it to accommodate all 5 columns, then rerun `make`/`make install` and re-verify with your test program that all 5 fields, including `amount`, now parse correctly with nonzero values — do not proceed to the C bootstrap code until this is confirmed working. Then write `bootstrap.c`: read all ~50k rows via the fixed library, extract `amount` into a double array, seed with `srand(42)`, and perform exactly 10,000 resamples of size N with replacement (sample with `rand() % n` or similar), computing the mean of each resample; compute the standard error as the sample standard deviation of the 10,000 bootstrap means (or equivalently the std of resample means). Also compute and report the plain sample mean of the original data. Write both values to `/home/user/bootstrap_results.txt` in the exact two-line format requested, and sanity-check the numbers are in a plausible range (the values are drawn from a lognormal distribution) before finishing.