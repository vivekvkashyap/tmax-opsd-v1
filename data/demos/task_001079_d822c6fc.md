This is black-box system identification, not curve-fitting by eye. Generate a large batch (e.g. thousands) of random `{f1,f2,f3,f4}` JSON inputs spanning a wide numeric range including negatives and near-zero, call `/app/legacy_score` on each via subprocess, and collect the printed score. Build a design matrix with every degree-≤2 monomial in the four features: the four linear terms, the four squared terms, and all six pairwise products (15 terms total plus intercept) — since the team confirmed it's a pure 2nd-degree polynomial with no higher terms or nonlinear transforms, an ordinary least-squares fit (`numpy.linalg.lstsq`) over this basis will recover the true coefficients with near-zero residual; treat any term whose fitted coefficient is ~0 as absent. Sanity-check by holding out fresh random samples and confirming your formula reproduces the oracle's output to within floating rounding before hardcoding the coefficients as literals in `/home/user/etl_scorer.py`. Implement strict schema validation first (exactly the four numeric keys, else print `SCHEMA_ERROR` and exit 1) before computing/rounding the score to 4 decimal places.