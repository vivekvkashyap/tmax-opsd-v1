Start by building and installing the libdist C-extension (`cd /app/libdist && pip install .`) so `compute_distance` is importable — the detector depends on it per the task constraints. Then characterize the fixture empirically: run /app/integrator_bin on several files from /app/data/clean/ and /app/data/evil/, inspect the output trajectory CSVs, and compute summary statistics (variance over time windows, tail behavior) to confirm the described signal — evil runs show variance/heavy-tailedness growing especially toward the end of the trajectory, while clean runs stay close to N(0,1). Build detector.py to: invoke the binary with a temp output path, load the trajectory, compute a variance-based statistic with bootstrap resampling to get a confidence interval, and use libdist's compute_distance against samples drawn from a standard normal as a second signal; combine both into a threshold decision rather than relying on either alone, since a single naive metric (e.g. just checking for NaN/overflow) will miss "silent" divergence that stays finite but heavy-tailed. Calibrate your threshold against the clean/evil examples in /app/data/ until you get 100% separation on both corpora, then keep the same logic for hidden data.
