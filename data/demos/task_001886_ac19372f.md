Build `/home/user/etl_pipeline.py` in clear stages, using only stdlib (`sqlite3`, `json`, `csv`):
1. Stream `/home/user/data/events.jsonl` line by line, group `level_complete` events by `user_id`, sort each user's scores descending, keep only the top 3 (fewer if the user has fewer), and sum them into `user_top3_score`. Users with zero `level_complete` events should not appear in this aggregate at all.
2. Load this per-user aggregate into `/home/user/data/users.db` as a temporary table (e.g. `CREATE TEMP TABLE`) and join it against the existing `users` table on `user_id` — this naturally excludes users with no qualifying events, and also means users not in the aggregate won't need score computation.
3. Use SQL window functions on the joined result: `RANK() OVER (PARTITION BY region ORDER BY user_top3_score DESC)` for `region_rank`, and `SUM(user_top3_score) OVER (PARTITION BY region)` for `region_total_score` — let SQLite compute these rather than doing it manually in Python, to avoid tie-handling bugs.
4. Export to `/home/user/output/region_leaderboard.csv` with the exact column order specified, rows sorted by region ascending then region_rank ascending. Compute `top_region` as the region with the highest `region_total_score` and write the summary JSON with the exact three keys specified. Create `/home/user/output/` first, then actually run the script to produce both output files.
