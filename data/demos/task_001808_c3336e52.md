First transcribe `/app/network_topology.wav` with a speech-to-text tool (e.g. `whisper` if available, or another installed ASR CLI) and carefully write down every "X to Y" edge mentioned — build the directed edge list from that transcript, not from guessing. Then write `/home/user/graph_query.py` using `networkx`: build a `DiGraph` from exactly those edges, compute `nx.pagerank(G, alpha=0.85)` and in-degree once at startup (recomputing per query is fine too but precomputing avoids drift). Loop reading lines from `sys.stdin` with `for line in sys.stdin`, `json.loads` each, dispatch on `action`, and `print(json.dumps(...)); sys.stdout.flush()` after every response — never buffer or add extra prints. Handle missing nodes/unreachable targets by returning `null` for `result` rather than raising. For `shortest_path` use `nx.shortest_path` (unweighted, directed) wrapped in a try/except for `NetworkXNoPath`/`NodeNotFound`. For `top_in_degree`, sort by `(-in_degree, node_name)` so ties break alphabetically ascending, then slice to `limit`. Round pagerank output to exactly 4 decimals with `round(x, 4)`. Test manually by piping a few JSON lines into the script before relying on the fuzz verifier.