Do the OCR step first and treat its output as ground truth for the rest of the task — run `tesseract /app/config_image.png stdout` (try preprocessing with `convert`/ImageMagick if the raw OCR is noisy) to recover the two config lines, then read them carefully to get the actual PORT number and the MASK_REGEX pattern rather than assuming a generic email regex; use exactly what the image specifies. Build the log processing as a pipeline: `sed`/`grep -E` or `awk` with the recovered regex to replace any email-shaped token in each line with `[REDACTED]` (do this to the whole line/field, not just print matches), then use `awk` to bucket each entry by the `HH` from its timestamp (truncate minutes/seconds), summing `LATENCY_MS` and counts per hour, and finally divide sum by count per hour using integer/floor division (e.g. `awk` with `int(sum/count)`) since the output must be rounded down. Precompute this aggregated text into a variable or temp file. Then write a bash loop like `while true; do echo -e "..." | nc -l -p <recovered-port>; done` (or use `nc` in a persistent listen mode appropriate to your `nc` variant) that, on receiving exactly `FETCH_STATS`, replies with the precomputed `YYYY-MM-DD HH:00 -> AVG_LATENCY` lines; background this process so it keeps serving repeated verifier connections.