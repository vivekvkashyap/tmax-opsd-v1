First get the vendored library importable: try `python3 -c "import sys; sys.path.insert(0,'/app/vendor/sqlglot'); import sqlglot"` and read the traceback — a broken top-level import (a typo'd module name) in `__init__.py` will fail immediately; open that file, locate the bad import line, and correct the misspelled standard-library module name so it imports cleanly, without altering any other logic. Then build the classifier around `sqlglot.parse_one(query)`: reject anything that isn't a `SELECT`/`WITH...SELECT` statement type at the AST root (check the parsed expression's type, not just string prefix matching, since comments/whitespace tricks can fool naive string checks). For the sensitive-table rule, walk the full AST recursively with `parsed.find_all(sqlglot.exp.Table)` so you catch tables referenced anywhere — inside CTEs, subqueries, joins, or under aliases — and reject if any resolved table name (ignoring alias, case-insensitive) matches the sensitive names. Wrap parsing in a try/except so unparseable or malformed queries also result in REJECT rather than crashing. Validate iteratively against every file in `/app/corpus/clean/` (must all print ACCEPT) and `/app/corpus/evil/` (must all print REJECT) before finishing — don't just eyeball a couple examples.