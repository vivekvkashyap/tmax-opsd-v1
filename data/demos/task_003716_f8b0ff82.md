Fix `setup.py`'s Extension sources list first (it's missing a comma between source filenames and/or references a nonexistent file — list only `src/processor.c`), then get `pip install -e .` compiling cleanly. In `processor.c`, replace the fixed-size `long arr[10]` stack buffer with a dynamically sized allocation (`malloc`/`PyMem_Malloc` sized to `size`) so arrays larger than 10 elements don't overflow the stack — that's the segfault source. Replace the O(n^2) bubble sort with an O(n log n) algorithm: either implement quicksort/mergesort by hand, or simply call the C library's `qsort()` with a comparator — either satisfies the requirement. Rebuild the extension after each fix and test with a >10-element list before moving on. For the audio task, transcribe `/app/transmission.wav` with a speech-to-text tool (e.g. `openai-whisper` CLI/Python API, or `SpeechRecognition` with an offline engine) rather than guessing numbers by ear/metadata; parse the transcript text for the spoken integers (watch for words like "seventeen" vs digits) into a Python list. Feed that list through the compiled `fast_dsp.sort_and_merge`, then write the sorted result as a JSON array to `/home/user/final_output.json`; the WebSocket client code can be a simple connect-and-send stub since no server is required to be running.