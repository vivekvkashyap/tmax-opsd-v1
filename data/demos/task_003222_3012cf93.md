Get the plumbing working first, then write pure logic against it. Edit `/home/user/nginx.conf` to add an `http { server { listen 8000; location /user/ { proxy_pass http://127.0.0.1:8081; } location /item/ { proxy_pass http://127.0.0.1:8082; } } }` block (keep the existing `events {}`), start both Flask services in the background, then start/reload nginx with `-c /home/user/nginx.conf`, and confirm with `curl` that `127.0.0.1:8000/user/<id>` and `/item/<id>` proxy correctly before writing any Python. In `/home/user/feature_extractor.py`, parse the single JSON CLI argument, fetch both endpoints through the nginx port (not the backend ports directly), and assemble the 10-d vector exactly as specified: 5 embedding floats, then a one-hot over the alphabetically-ordered categories `['book','clothing','electronics','home']` (all-zero if the category isn't in that list — note the services may return categories outside this vocabulary), then `log10(price + 1.0)`. Read `/home/user/projection_matrix.csv` as a 10x3 matrix and do a straightforward 1x10 · 10x3 matrix multiply. Print exactly three space-separated floats at 4 decimal places with no other output — extraneous prints will break the fuzz-equivalence comparison against the hidden oracle, so keep stdout clean and make float formatting deterministic (`f"{v:.4f}"`).