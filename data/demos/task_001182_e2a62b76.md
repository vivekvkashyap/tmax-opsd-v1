Start by reading `/app/rk4-solver/src/lib.rs` carefully and compare each stage of the classic RK4 update against the textbook formula: k1 uses (t, y); k2 uses (t + dt/2, y + k1*dt/2); k3 must also be evaluated at the half-step (t + dt/2, y + k2*dt/2) — check specifically whether k3's time or state argument has been swapped to use the full dt instead of the half step, since that's the classic subtle RK4 bug, and fix only that line, preserving the rest of the integrator's structure. Once fixed, build the fisher_sim Rust crate as a local path dependency on rk4-solver, discretize the 1D domain into a mesh, implement the Laplacian via central finite differences with ghost/mirrored boundary nodes for the zero-flux (Neumann) condition, and pack the reaction term into your ODE right-hand side function passed to the RK4 stepper. Parse observations.jsonl, and treat mesh resolution (dx) and integration step (dt) as parameters to sweep/refine — finer dx and smaller dt should converge toward the reference; use the MSE against observations (interpolated at your own mesh points if needed) as your own feedback signal to pick resolution before finalizing. Write final_state.csv with header `x,u` and include the observation x-coordinates among your output rows.
