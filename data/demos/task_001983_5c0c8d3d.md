First isolate the vendored bug by reading `/app/vendored_bio_sim/sim.py` and finding the `solve_ivp(...)` call — look for any explicit `first_step=` (or similarly hardcoded step-control) keyword argument that overrides scipy's adaptive stepping; removing that argument (letting scipy pick its own initial step) is the fix, not touching the ODE system or tolerances. Verify the fix works in isolation first (e.g. run the vendored `run_simulation` function directly against the PDB file and confirm it returns without raising and yields finite values) before building the server on top of it. For the HTTP server, bind exactly to `127.0.0.1:8888`, implement `GET /simulate` with manual header inspection for `Authorization: Bearer sim-token-42` (return 401 on missing/mismatched header before doing any simulation work), then on success call the fixed vendored functions to get initial and final X-coordinates, compute `scipy.stats.wasserstein_distance(initial_x, final_x)`, and return `{"distance": <float>}` as JSON with a 200 status. Run the server as a background process and smoke-test both the auth-rejection and success paths with `curl` before finishing.