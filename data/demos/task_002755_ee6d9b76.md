Work top to bottom: fix compilation first, since nothing else can be tested until `make` succeeds. Run `make` in `/home/user/protocol_backend/`, read the "multiple definition" linker error carefully — it points at a non-`extern`/non-`static` variable or function defined (not just declared) inside a header that's included by more than one translation unit. Fix it in the header/source by turning the shared header's definition into a proper `extern` declaration and moving the actual definition into exactly one `.c` file (or mark it `static`/`inline` if it should stay per-file) — don't just delete functionality. Re-run `make` until `libproto.so` and the server binary build cleanly with no warnings. Next, edit `nginx.conf`'s `server` block to `listen 8080;` and add a `location / { proxy_pass http://127.0.0.1:9090; }` directive so it proxies to the C backend. Start the backend (`./server &`), start nginx with your config, then write `prop_test.sh`: generate 100 random alphanumeric strings of length 1-64 (e.g. via `/dev/urandom` + `tr -dc`), POST each with `curl -s -X POST --data "$str" http://127.0.0.1:8080/`, compare the response body byte-for-byte to the input, printing `FAIL: <input>` and exiting 1 on any mismatch, or `PASS: ALL` with exit 0 if all pass. Run the full chain yourself end-to-end and leave server/nginx running with `prop_test.sh` passing before finishing.