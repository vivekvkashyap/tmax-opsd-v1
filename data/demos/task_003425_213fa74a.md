Use OpenCV to iterate all 100 frames of `/app/network_traffic.mp4` and `pyzbar.decode()` each frame to read the QR payload string, splitting it on commas into source, dest, bytes — write one row per frame to `/home/user/traffic.csv` (frame_id, source, destination, bytes), checking decode succeeded for every frame since a missed decode will silently shift your rolling sums. For the rolling aggregation, build a per-destination array indexed by frame (0 when that destination received nothing in a given frame) rather than trying to filter/group the raw rows directly — this makes windowing trivial: for each row where a real transmission occurred, sum that destination's per-frame array over frames `[frame_id-2, frame_id]` clamped at 0 (i.e., include zeros for missing frames within the window, not skip them). Only emit rows in `/home/user/results.csv` for frames where an actual transmission happened at that destination, matching the frame_id/destination pairs from your extracted CSV. Since the verifier requires an exact MSE-of-0 match, double check off-by-one window boundaries (window is current frame plus the 2 immediately preceding, not 3 preceding). Also write the equivalent Cypher import + `SUM(...) OVER (... ROWS BETWEEN 2 PRECEDING AND CURRENT ROW)` window query to `/home/user/rolling_query.cypher`.