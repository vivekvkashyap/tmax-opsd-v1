Do this as a short awk/grep/sort pipeline, no external tools needed. First build a fast corruption-membership check: read `/home/user/corrupted.txt` into an awk array (e.g. via `-v` file arg passed as first ARGV, populating `bad[$0]=1` while `NR==FNR`), then process `backup_deps.csv` line by line, skipping any row where either the dependent or base backup ID is a key in `bad[]` — do this filtering in a single awk pass rather than multiple greps to avoid partial-string false matches (exact field match, not substring). From the surviving edges, tally out-degree per `base_backup_id` (count of rows where it appears in the base column) using an associative array. Sort the results by count descending, then by backup_id ascending for ties — `sort -t, -k2,2nr -k1,1` on a `id,count` intermediate stream handles this correctly, but double check ties resolve alphabetically not by insertion order. Take the top 3 and hand-format them into the required JSON array (`{"backup_id": "...", "deps": N}` objects) — you can build this with printf/awk directly since jq isn't required, just be careful with commas and quoting. Write the result to `/home/user/top_backups.json` and validate it parses with `jq -c '.'` before finishing, even though jq isn't used to generate it.
