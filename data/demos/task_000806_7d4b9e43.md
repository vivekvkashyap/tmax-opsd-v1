Build this as a pipeline of independent, individually-verifiable stages rather than one monolithic program. (1) Write run_pipeline.sh to call ffmpeg with `-vf format=gray -r 30` to dump frames as grayscale PNGs into /home/user/frames/, then invoke the Go program. (2) In Go, init a module and `go get gonum.org/v1/gonum/...`; load each frame into a flattened pixel-intensity vector and stack them into a matrix M (frames x pixels). Use gonum's SVD (`mat.SVD`) on M: the background is essentially constant across frames, so it dominates the top singular component — reconstruct/subtract that component (or simply subtract the per-pixel mean/median frame) to isolate the moving object, then threshold the residual and compute the centroid (weighted mean of x,y) per frame. (3) Convert frame index to time using the known 30 FPS, and pixel Y to meters via the stated 100 px/m scale — do this conversion explicitly, not by guessing units. (4) Fit y(t) = y0 + vy0*t + 0.5*g*t^2 by least squares to get a starting estimate, then refine g with your own gradient-descent loop on squared error, tracking loss between iterations and stopping when the change drops below 1e-6 — count and report the iterations taken. Validate against the equation's known physical form, not by hardcoding.
