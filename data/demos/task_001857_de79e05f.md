Tackle the three defects independently and verify each before moving to the next. First fix the Makefile: run `make` in `/app/smhasher`, read the linker error carefully — a missing library flag (like `-lrt`) or a mismatched object-file dependency name for `parallel_hash.o` is the likely culprit, so correct the flags/target names rather than rewriting the whole build. Second, use `git log --oneline` and `git log -p` (or `git show`) in `/app/smhasher` to find the reverted commit fixing "numerical instability in finalization" — inspect its diff to see exactly which variable was changed from a signed to unsigned type (or vice versa) around the `k1 *= c1` multiplication in Murmur3's tail processing, and reapply that specific type fix (don't alter the algorithm's logic otherwise). Third, use `gdb` to attach to a hung run of `parallel_hash.c`, inspect each thread's backtrace to see it blocked in `pthread_mutex_lock`, confirm the two threads lock `mutex_A`/`mutex_B` in reversed order, and fix it by enforcing a single consistent lock-acquisition order across all threads. Compile the final binary to `/home/user/fixed_hash`, reading from stdin and printing the 32-bit hex hash, then self-test by piping varied random byte streams through it and checking for consistent, non-hanging output before considering it done — since your output must match a hidden reference implementation bit-for-bit, do not deviate from the standard Murmur3 algorithm.