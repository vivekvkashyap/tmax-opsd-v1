Structure the C++17 pipeline as three clearly separated passes over the data rather than one tangled loop, since ordering matters for correctness: Pass 1 — parse the CSV into an in-memory struct per row (handle empty temperature/humidity fields as "missing" via a sentinel like NaN or optional<double>), and while parsing, immediately drop+log any row whose temperature is present and outside (-50.0, 100.0] bounds to `/home/user/pipeline.log` using the exact WARN format given, before any interpolation touches the data. Pass 2 — group remaining rows by sensor_id preserving chronological order, then for each sensor and each metric independently, walk the sequence tracking the last-seen valid (timestamp,value); when a gap is hit, look ahead to the next valid value and apply the given linear interpolation formula using the two surrounding timestamps. Pass 3 — bucket each row by `timestamp_ms / 900000 * 900000` (integer division, use int64), group by (bucket, sensor_id), average temp and humidity, then emit rows sorted by bucket ascending then sensor_id alphabetically, with values formatted to exactly 2 decimals via stream manipulators. Finally append the INFO completion line to the log. Compile with the exact `g++ -std=c++17` command given and run the binary so both output files are actually generated on disk.
