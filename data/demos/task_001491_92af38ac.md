Treat Part 1 as a protocol/state-machine parser and Part 2 as service wiring — build and test each independently. For `decoder.c`: write a small explicit parser that walks the input string checking for the literal `patch://` prefix, then reads a single uppercase letter, then a `/`, then the remainder as the encoded patch (don't rely on `sscanf` with loose format specifiers — validate each transition explicitly so malformed input is rejected cleanly). URL-decode `%XX` escapes yourself (a simple loop replacing `%` + 2 hex digits), then Base64-decode the result (implement a standard base64 decode table, or link against a small embedded decoder since you can't assume external libs are available) to get the raw 2-byte command stream. Apply commands sequentially to a growable buffer: `I<c>` appends, `D` pops the last char if non-empty, `R<c>` overwrites the last char if non-empty — then print `TARGET=<id> RESULT=<buffer>` exactly. Write a `Makefile` with a default target producing `decoder`. Since correctness is checked by fuzz-equivalence against a hidden oracle across many generated `patch://X/...` URLs, test your decoder by hand-constructing a few base64/url-encoded patch strings and tracing the buffer state yourself. For Part 2, fix nginx's `location /process/` block to `proxy_pass http://127.0.0.1:5000/process/;`, then in Flask's route handler build the `patch://` URL from the path parameter, invoke `/home/user/decoder` via `subprocess.run(..., capture_output=True)`, store the captured stdout string verbatim into Redis under `latest_patch`, and return it as the response body.
