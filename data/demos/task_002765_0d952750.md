Build this as a small standard graph pipeline. In `graph_solver.cpp`: read argv[1] (TSV path), argv[2] (start), argv[3] (end); parse the TSV skipping the header row, split each line on tabs, and only keep edges whose middle column is exactly `depends_on`, adding a directed adjacency entry `source -> target` in an `unordered_map<string, vector<string>>`. Run BFS from the start node, tracking a `parent`/`visited` map, and stop as soon as the end node is dequeued. Reconstruct the path by walking parents backward from end to start and reversing it; the edge count (`Length`) is `path.size() - 1`. Write the exact two-line format `Length: N` / `Path: A -> B -> ...` to `/home/user/output/shortest_path.txt`, or `No path found.` if BFS never reaches the target — get whitespace and arrow spacing exact since verification is a byte-for-byte string comparison. In `pipeline.sh`, `cd` into the etl directory (or use absolute paths), compile with `g++ -std=c++17 graph_solver.cpp -o graph_solver`, then invoke it with the three required arguments; `chmod +x pipeline.sh` afterward.