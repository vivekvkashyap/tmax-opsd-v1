Build and validate each pipeline phase against the sample data before chaining them. For `extract.py`, use one regex per line that anchors on the fixed markers in the log format: a leading `YYYY-MM-DD HH:MM:SS` timestamp, `Mach-\d+` for machine_id, `ErrCode:(\S+)` for the error code, and `(\d+)\s+minutes downtime` for downtime — test the regex against each sample line individually since free-text logs are easy to mis-anchor. Write the extracted rows to `extracted_logs.csv` preserving input line order. For `process.py`, parse both CSVs with pandas, convert timestamp columns with `pd.to_datetime`, and for each maintenance event filter `sensor_telemetry` rows for the same `machine_id` where `event_ts - pd.Timedelta(hours=3) <= sensor_ts < event_ts` (note the strict upper bound), then average `temperature`/`vibration` over that filtered set — when the filtered set is empty, emit JSON `null` (not NaN or 0) by explicitly checking `if group.empty`. Round non-null averages to 2 decimals with Python's `round()`, and emit fields in the exact key order/names specified with `json.dumps` per line to build the `.jsonl`. Finally write `run_pipeline.sh` to call `python3 extract.py && python3 process.py` and `chmod +x` it, then run it and diff the output against the sample events manually before finishing.