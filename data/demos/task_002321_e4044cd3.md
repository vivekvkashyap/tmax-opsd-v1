Write the orchestrator as a sequence of clearly separated steps in `/home/user/orchestrate_pipeline.sh`. Step 1: execute the notebook in place with `jupyter nbconvert --to notebook --execute --inplace /home/user/generate_scores.ipynb` (or `papermill`), which populates `/home/user/raw_scores.txt`. Step 2: pipe that file into `awk` and compute population variance with a two-pass, mean-centered approach — first pass accumulates sum and count to get the mean, second pass accumulates sum of squared deviations from that mean, then divide by N — rather than the naive `sum(x^2)/N - mean^2` formula, which loses precision here because the scores are large (~1e9) and nearly identical, so squaring them first causes catastrophic cancellation. Format the result to exactly 6 decimal places with `printf "%.6f\n"` and write it to `/home/user/calculated_variance.txt`. Step 3: read both that value and `/home/user/reference_variance.txt`, compute the absolute difference (again in `awk` or bash arithmetic), and write literally `PASS` or `FAIL` to `/home/user/pipeline_status.log` based on whether the difference is under `0.0001`. Make the script executable and run it end-to-end to confirm all three output files appear correctly.