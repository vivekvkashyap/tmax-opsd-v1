Write `/home/user/fix_audio.py` to read `/app/suspicious.wav` (e.g. `scipy.io.wavfile.read` or `soundfile.read`), preserving the original sample rate and converting to a working float array if needed. Design the filter with `scipy.signal.butter(5, 4000/(0.5*sample_rate), btype='low')` and apply it with `scipy.signal.filtfilt` (zero-phase, matching how a reference implementation would typically be built for exact comparison — a causal `lfilter` would introduce phase/MSE differences) rather than a hand-rolled filter. Handle stereo input by matching however the reference likely reduces channels (mono/mean) if the source has multiple channels — check the input first. After filtering, rescale/clip carefully back to 16-bit PCM range (e.g. normalize by max absolute value then scale to int16, or clip if you don't normalize) and write out with `scipy.io.wavfile.write` at the same sample rate to `/home/user/clean.wav`. Since grading is MSE-based against a hidden reference filter implementation, keep your normalization/scaling approach as standard as possible (avoid unusual custom scaling) to minimize divergence, and sanity-check by listening to or plotting the spectrum of your output to confirm high frequencies above ~4kHz are attenuated.
