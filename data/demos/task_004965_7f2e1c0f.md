Fix the C++ bug first and in isolation: replace the hardcoded `long available_space = 1000000;` with a call to `std::filesystem::space("/home/user/app_data")`, taking `.available` from the returned `space_info` struct — compile with `-std=c++17` (filesystem needs it) and test one instance manually with curl before scaling out, confirming the returned available_bytes roughly matches `df` output for that filesystem. Launch three backgrounded copies of the compiled binary, one per port (8081/8082/8083), each verified independently reachable. Write haproxy.cfg with a frontend bound to 127.0.0.1:8080 and a backend in `balance roundrobin` mode listing all three servers by their 127.0.0.1:port addresses, then start haproxy with `haproxy -f /home/user/haproxy.cfg &` and confirm round-robin behavior by hitting port 8080 repeatedly and observing the values differ if agents were configured differently (or just confirm connectivity). Set up the SSH tunnel non-interactively and backgrounded: `ssh -N -f -L 9090:127.0.0.1:8080 user@localhost` (passwordless auth is already configured — verify it works before scripting around it). Finally write poll_metrics.sh as a simple loop issuing exactly 6 `curl -s http://localhost:9090` calls appended to dashboard_metrics.log with a 1-second sleep between each, then execute it and inspect the log to confirm 6 valid JSON lines landed.
