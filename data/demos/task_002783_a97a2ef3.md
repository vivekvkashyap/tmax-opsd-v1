Reverse-engineer the stripped binary black-box first: run `/app/legacy_auth_gen` on several chosen inputs (0, 1, small values, powers of two, max uint32) and look for patterns consistent with a linear congruential generator combined with a bit-shift XOR — common legacy "hash" constructions are of the form `(x * A + B) ^ (x >> k)`. Confirm with `objdump -d` / `gdb` disassembly of the stripped binary to read the actual multiply/add/xor/shift immediates and shift amount rather than guessing from output alone; cross-check by computing your candidate formula in a small test program and diffing against binary output over many random inputs before trusting it. Once the transform is nailed down, implement it identically in C inside your TCP server (use `uint32_t` throughout to get correct wraparound behavior — a common bug is using `int` and losing overflow semantics). Build the server with a simple accept loop, parse the exact request line with sscanf, do a strict string compare on the secret, and reply as specified. For the DB migration, open the legacy sqlite file, read each row, apply the same transform function to `input_val`, and insert into the new schema with the fixed migration_date string.