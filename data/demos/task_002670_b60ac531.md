This has four independent deliverables — tackle them separately and verify each in isolation. (1) Video extraction: open `/app/dashboard_feed.mp4` frame-by-frame with OpenCV (`cv2.VideoCapture`), and for each frame vectorize the red-pixel test (R>150 and G<50 and B<50 per pixel, careful with BGR vs RGB channel order in OpenCV) using numpy boolean masking rather than per-pixel Python loops for speed; write `frame,red_pct` rows to `/home/user/extracted_metrics.csv` with frame indices starting at 0 and red_pct as a percentage of total pixels. (2) `monitor_service.py` should tail/read `extracted_metrics.csv` and append each row as a line to `/home/user/logs/monitor.log`, using `datetime.now(timezone.utc)` (or setting `TZ=Etc/UTC` in the environment) for timestamps so logs are UTC. (3) Write a `supervisord.conf` with a `[program:...]` section pointing at the python interpreter and script path, `autorestart=true`, and run supervisord in user mode (e.g. `-c /home/user/supervisord.conf`, non-root ports/paths). (4) Write `logrotate.conf` targeting `/home/user/logs/monitor.log` with `daily`, `rotate 3`, `compress`, and `missingok` directives.