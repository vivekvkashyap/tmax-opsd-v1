Write this as a single awk program embedded in the bash script, and get determinism right first since everything downstream depends on it: call `srand(42)` exactly once at the very start of the `BEGIN` block (not per-reading), then for each of 1000 readings sum 100 calls to `(rand()*2)-1` for the uniform `[-1,1)` step, writing each final reading to `mc_results.txt` as you go. Compute running sums of value and value² in the same awk pass to get mean and (sample, N-1 denominator) variance without a second pass, printf'ing the variance to 2 decimals into `variance.txt`, then branch on that value (as a shell-side numeric comparison via awk/bc, not string comparison) to write `STABLE` or `UNSTABLE`. For the histogram, do a second awk pass over `mc_results.txt` that buckets each value into one of the 8 fixed `[lo,hi)` ranges (use `>=` and `<`, and treat the very last bin's upper bound as inclusive since the task says so), accumulate per-bin counts, then for each bin printf the center (bin width 5, so center = lo+2.5) with one decimal, a space, `|`, and `int(count/10)` asterisks (integer division, not rounding) built with a loop or `sprintf`. Keep bin order fixed from -17.5 to 17.5 regardless of which bins end up empty.
