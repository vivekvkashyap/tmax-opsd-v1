Write the PDB parser carefully to the spec: since atom name lives at fixed columns 13-16 of each `ATOM` record, parse by column position (substring) rather than naive whitespace-splitting, so you don't confuse a CA in the residue name with the atom-name field, and trim/compare it exactly to `"CA"`. Extract x/y/z from their fixed columns too (standard PDB columns 31-38, 39-46, 47-54) using `sscanf`/manual parsing, store all CA coordinates in a dynamic array, then compute Euclidean distance for every unique pair with a double nested loop (j > i) to avoid double counting or self-pairs. Bin each distance into `floor(distance)` if it's in [0,100), incrementing a 100-element histogram array and a separate total-pairs-within-range counter; distances ≥100 increment neither. Normalize each bin count by that total to get P(i), then sum `P(i)*log(P(i)/0.01)` only over bins with P(i)>0, using `log()` from `<math.h>` (link with `-lm`). Write the final double to the output file using a format specifier that guarantees exactly 6 decimal places (e.g. `%.6f`), and double check with a hand-traceable small example that your binning boundaries (`[k, k+1)`) match the spec before trusting the full-file result.
