Work through this as a real crash-diagnosis flow, not a guess. First enable core dumps (`ulimit -c unlimited`, and check/set `/proc/sys/kernel/core_pattern` if needed) and run `/app/telemetry_parser` to produce a core file; load it with `gdb /app/telemetry_parser core` (or the dumped path) and run `bt`/`info registers` to see where it faults — since the binary is stripped, focus on syscalls/libc frames (e.g. `fopen`/`strcpy` on a config path) visible in the backtrace or use `strace -f /app/telemetry_parser` as a faster complement to spot the missing file path it's trying to open. Create that missing directory/config file so the binary prints "Config loaded" and exits cleanly. Next, compile the plugin with `gcc -shared -fPIC -o /home/user/plugin/parser_plugin.so /home/user/plugin/parser_plugin.c -lm`, fixing whatever concrete compiler/linker error appears first (missing `-fPIC` for shared-object relocations, missing `-lm` for math symbols) and re-running until it links; set `PLUGIN_PATH` when invoking the parser. For the wrapper script, don't process the 1000 `.dat` files sequentially — pipe filenames through `xargs -P <n> -n1` (or GNU `parallel`) to run many parser invocations concurrently, sized to available cores, and confirm wall-clock time with `time ./run_diagnostics.sh /app/data` stays under 5 seconds before finalizing; make the script executable.