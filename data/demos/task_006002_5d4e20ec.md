Build and test each phase in isolation before composing them. Translate `/home/user/legacy/scorer.js`'s `ReadinessScorer` to Python line-by-line, preserving circular-buffer semantics: subtract the value being overwritten from `total` before overwriting it, advance `head` modulo `window_size`, and use integer floor division for `get_score` (`self.total // self.count`, matching JS `Math.floor`) — plain float division would silently diverge from the JS original. Test `add_metric`/`get_score` with a known sequence to confirm parity before wiring it into the server. Define `deployment.proto` exactly as specified (message/field/service/RPC names) since the client stub depends on exact generated symbols, then compile with `python -m grpc_tools.protoc -I. --python_out=. --grpc_python_out=. deployment.proto` from `/home/user/`. In `server.py`, run the gRPC server and a `websockets` server concurrently, sharing one `ReadinessScorer(window_size=3)` instance; on each `SendMetric` call feed the value in, compute the score, and if it drops below 50 broadcast the exact JSON alert to all connected websocket clients before returning `approved=False`. Write `run_e2e.sh` to background the server, sleep 2s, background the listener, run the client to completion, then cleanly kill both background processes and exit 0 — verify against `/home/user/alerts.log` after a manual run.