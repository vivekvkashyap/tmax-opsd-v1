Work through the four subtasks independently but verify each with the actual tool that will check it. (1) For the systemd unit, edit the `[Unit]` section of `manifest-operator.service` to add `After=k8s-mock-api.service` and `Requires=k8s-mock-api.service`, then run `systemctl --user daemon-reload` followed by `systemctl --user enable --now k8s-mock-api.service manifest-operator.service` (note: user services may need `XDG_RUNTIME_DIR`/`loginctl enable-linger` set correctly in a container — check `systemctl --user status` for errors). (2) The post-receive hook receives lines of `<oldrev> <newrev> <refname>` on stdin — read with `while read oldrev newrev refname; do echo "HOOK_TRIGGERED: $newrev" >> /home/user/operator-logs/hook.log; done`, then `chmod +x` it; ensure the log directory exists first. (3) Write the expect script to `spawn /home/user/push-manifests.sh`, `expect "Deploy Passphrase: "`, `send "k8s-oper-2024\r"`, then `expect eof` and let exit status propagate; test it manually and confirm the hook actually fires (check hook.log after). (4) The logrotate config is a plain text block: path glob, then `{ daily\n rotate 3\n compress\n }`. Finally `chown -R user:user` everything you create.