Recursively unzip everything under `/app/legacy_backups/` — note archives can be nested (a zip containing another zip), so after each `unzip`, re-scan the extraction output for more `.zip` files and extract those too until no zips remain. For every `chunk_NNN.bin`, compute its MD5 (`md5sum`) and compare against the paired `chunk_NNN.md5` file's contents; keep only chunks whose hash matches. Order the surviving chunks numerically by NNN and concatenate their raw bytes directly into `/home/user/recovered.wav` — when a chunk is missing or failed the check, just skip it and continue with the next valid one (don't pad or reinsert placeholder bytes). Transcribe with `pip install openai-whisper` then in Python `whisper.load_model(...).transcribe(path)["text"]`, writing the raw text to `/home/user/firewall_rules.txt`; since a chunk is corrupted the audio will have a gap, so maximize accuracy (e.g. a larger whisper model) rather than assuming a small one clears the 0.75 similarity threshold. Separately, write `/home/user/watcher.py` using `watchdog`'s `Observer`/`FileSystemEventHandler` with `on_modified` triggering a split-zip (e.g. `zip -s 500k -r`, or manual chunking) of `/app/active_config/` into `/app/backup_staging/` — it only needs to be written correctly, not run as a daemon.
