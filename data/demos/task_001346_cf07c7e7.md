First recover the XOR key by transcribing `/app/reference_memo.wav` — convert it with `ffmpeg` to a format a speech-to-text tool accepts, run a transcription tool (e.g. `whisper.cpp` or any installed ASR) over it, and read the spoken number out of the transcript rather than assuming a value; if the transcription is garbled, re-listen/re-run with different model settings until the number is clear. Once you have the numeric key, write the Go classifier to, for each `.bin` file: read all bytes, XOR every byte with the recovered key, feed the result to `compress/gzip` to decompress, then split the payload into the first 64 bytes (header) and the rest, and search for `---END_LOG---` to separate the log text from the trailing artifact data. Classify EVIL if the header's first 4 bytes equal `0xDE 0xAD 0xBE 0xEF` OR the log text contains `AUTHORIZATION: BYPASS`; otherwise CLEAN. Print `[FILENAME]: CLEAN`/`[FILENAME]: EVIL` per file to stdout, then rename each file in place (`os.Rename`) by appending `.clean` or `.evil`. Test against both directories before finalizing, since a single misclassification anywhere fails the corpus check. Build with `go build -o /home/user/artifact_filter`.
