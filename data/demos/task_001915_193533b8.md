Start by recovering the salt with OCR rather than assuming a value: run `tesseract /app/arch_notes.png stdout` (try `--psm` variants if the first pass mangles characters) and extract the salt string exactly as printed, verifying it by hand-computing one token yourself (`echo -n "<salt><username><pin>" | sha256sum`) against a known-format sample before trusting it in the script. Then implement the filter as a brute-force search: parse `USERNAME:TOKEN` from the single input file, loop PIN from `0000` to `9999` (zero-padded, so use `printf "%04d" $i`), concatenate `<SALT><USERNAME><PIN>` with no separators, hash with `sha256sum`, and compare (case-insensitively is safe since sha256sum is lowercase hex) against the token — exit 0 the moment a match is found, exit 1 if the full 10000-PIN space is exhausted with no match. Common naive-attempt failures: forgetting to strip trailing newlines/whitespace from the parsed username or token before comparing, and using `[[ $var == $hash ]]` without quoting (fine for hex but be careful with other stray characters). Make the script executable and test it against both the clean and evil corpora directories before finishing.
