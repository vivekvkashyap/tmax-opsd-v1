Work top-down: first author the `.proto` schema exactly matching the field names/numbers/enum values given, then compile it with `protoc --python_out=. build_artifact.proto` (install `protobuf-compiler` or `pip install grpcio-tools` and use `python -m grpc_tools.protoc` if `protoc` isn't on PATH) before writing any Python that imports the generated `_pb2` module. In `migrate.py`, implement the parser as an explicit state machine: iterate lines, on `BEGIN_ARTIFACT` start a new record, accumulate `DEPENDS` lines in order into a list, track the *last* `STATE` seen (overwrite, don't append) since final_state must be the last one before END_ARTIFACT, and on `END_ARTIFACT` construct the protobuf message, serialize with `.SerializeToString()`, and write it to `/home/user/artifacts/out/<name>.bin`. A common naive bug is using the first STATE instead of the last, or forgetting DEPENDS ordering — verify against the sample log's expected per-artifact fields before moving on. Write `test_migrate.py` with pytest testing the parser on a mock multi-artifact string. Finally, after generating all `.bin` files, compute `sha256sum` for each and write sorted `<name>.bin: <hash>` lines to `completion.log`.