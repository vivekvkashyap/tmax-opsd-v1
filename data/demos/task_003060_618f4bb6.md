Clean the CSV with a single shell pipeline before touching the C++ program: strip the header (`tail -n +2`), then filter out rows containing 'NaN' and rows where the first field is negative — e.g. `tail -n +2 raw_data.csv | grep -v NaN | awk -F, '$1+0 >= 0'` redirected into cleaned_data.csv. Watch for the awk-numeric-comparison trap: comparing the X field as a string won't catch negatives correctly, so force numeric context (`$1+0`) or use a proper `awk '$1 !~ /NaN/ && $1 >= 0'` guard. Verify the row count matches expectations before moving on. Then compile with `g++ train.cpp -o train_model`, run `./train_model cleaned_data.csv > model_output.txt`, and confirm model_output.txt contains only the program's stdout (no extra echoed commands or stderr mixed in — don't use `2>&1` here).