Two independent recovery steps feed into one analysis. First, OCR `/app/calibration_matrix.png` (e.g. `tesseract /app/calibration_matrix.png stdout`) to recover the per-sensor calibration multipliers — visually double-check the OCR text against the image for digit misreads before using the numbers. Second, recover the journal: open `/home/user/sensor.journal` in binary/text mode and process it line by line (or byte-scan for `\n`-delimited chunks), attempting `json.loads` on each candidate line inside a try/except, and additionally validate the resulting dict strictly matches the schema (`timestamp` is int, `sensor` is str, `raw_value` is float) before accepting it — reject anything that fails to parse or has extra/missing/wrong-typed fields, since null bytes and truncation will produce garbage that superficially looks JSON-ish but fails strict validation. Multiply each valid `raw_value` by its sensor's OCR'd calibration factor to get `corrected_value`. Group by sensor, compute population mean/std (divide by N, not N-1) per sensor, then flag readings with `abs(Z) > 2.5`. Write the flagged rows, sorted by timestamp, with `corrected_value` rounded to 2 decimals, to `/home/user/anomalies.csv` with the exact header given.
