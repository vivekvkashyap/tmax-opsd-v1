Diagnose the two independent problems separately before touching the running service. For concurrency: inspect `run_api.sh` in `/app/bash-sqlite-graph-api` — if it invokes `socat TCP4-LISTEN:8888 EXEC:./handler.sh` without `fork`, socat only ever services one connection and later connections queue/hang; add `fork` (and typically `reuseaddr`) to the listen address so each incoming connection spawns a fresh handler process. For performance: open the vendored `handler.sh` (or wherever the SQL lives) to see the recursive CTE traversing `edges` by joining on `parent_id`; since SQLite has no index on that column the recursive step does a full table scan per level — add `CREATE INDEX idx_parent ON edges(parent_id);` via `sqlite3 /app/data/graph.db` to fix the query plan (verify with `EXPLAIN QUERY PLAN` that it now uses the index). After both fixes, start `run_api.sh` in the background bound to `127.0.0.1:8888`, then manually test with two simultaneous `nc`/socket connections sending a node id to confirm neither blocks the other before considering the task done.