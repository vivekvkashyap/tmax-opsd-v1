This task has two independent parts — fix the service wiring first, then build the classifier script, and verify each in isolation. Phase 1: inspect `/app/services/nginx.conf`, find the `location /api/` block, and correct its `proxy_pass` target to point at the actual Flask port (127.0.0.1:5000) rather than whatever wrong port it currently has; then run `/app/services/start.sh` and confirm with `curl http://localhost:8080/api/vector` that it returns the calibration JSON before moving on — do not assume the proxy works without testing it directly. Phase 2: in `/home/user/filter.sh`, `curl` the calibration vector once, parse `v_x`/`v_y`/`v_z` out of the JSON (e.g. with `jq` or grep/sed), then for each CSV row (no header) compute the dot product `x*v_x + y*v_y + z*v_z` using `awk` or `bc` for floating-point precision (avoid integer-truncating shell arithmetic). Track whether ANY row's absolute dot product exceeds 10.000 strictly; if so, `redis-cli SADD rejected_files <basename>` and `exit 1`, otherwise `redis-cli SADD accepted_files <basename>` and `exit 0`. Test against a few hand-computed rows before running the full corpora, and `chmod +x` the script.