Read `/home/user/pca_cleaner/src/main.rs` fully before editing — both bugs are in the parsing/projection logic, not somewhere hidden. For schema enforcement: currently each field is parsed with `.parse::<f64>().unwrap_or(NAN)`, silently substituting NaN for bad values like "ERR"/"N/A"; instead, parse each of the 4 feature columns with a `Result`, and if *any* column in a row fails to parse, skip that entire row (don't add it to the working dataset at all — it must not influence the mean either). For the numerical-accuracy bug: currently rows are multiplied by the projection matrix directly; instead, first compute the per-feature mean across only the valid/kept rows, then subtract that column mean from each valid row's corresponding feature (mean-centering) *before* applying the projection matrix — order matters, meaning must be computed from valid rows only, and centering must happen before, not after, projection. Keep the `id` field from each surviving row unchanged. After fixing, rebuild (`cargo build`) and run (`cargo run`) to regenerate `/home/user/projected.csv` with header `id,p1,p2` and `p1`/`p2` formatted to exactly 4 decimal places via Rust's `{:.4}` format specifier. Manually recompute one row's centered/projected values by hand to confirm your logic before considering it done.
