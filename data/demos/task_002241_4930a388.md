This task has two independent deliverables — solve them separately and verify each in isolation before combining outputs.

Part 1 (video): extract frames from `/app/file_events.mp4` with `ffmpeg` (e.g. `ffmpeg -i file_events.mp4 frame_%05d.png` or process frames in a loop), then for each frame inspect only the top-left 10x10 pixel block (crop with ffmpeg's `crop` filter or read pixel values with a tool like Python/PIL or ImageMagick's `convert ... txt:`) and test whether that block is predominantly pure green (0,255,0) — allow for minor compression/encoding noise by checking a tolerance around the target RGB rather than requiring exact equality on every pixel. Count matching frames and write the integer count as the first line of the summary log.

Part 2 (sanitizer): write a small recursive-descent parser/evaluator in C++ for the `file()/concat()/parent()` expression grammar, extract each JSON's `depends_on` string, evaluate it to a final path, then normalize/resolve that path (handle `..` segments and absolute paths) and reject (exit 1, print REJECTED) anything that escapes the working directory or is absolute; otherwise accept (exit 0, print ACCEPTED). Test iteratively against every file in `/app/corpora/clean/` and `/app/corpora/evil/` until both pass 100%, then append the sanitizer's executable path as line 2 of the summary log.
