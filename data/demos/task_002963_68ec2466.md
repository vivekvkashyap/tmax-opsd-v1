Load `/home/user/transactions.csv` into a directed graph structure (dict of sender -> set of receivers) and enumerate all directed 3-cycles: for each edge A->B, for each edge B->C, check whether an edge C->A exists (dedupe cycles that are rotations of the same triple, e.g. A->B->C->A and B->C->A->B are the same cycle — normalize by sorting the three account names before treating a cycle as unique). For each unique 3-cycle, filter the original transaction rows to only those where both sender and receiver are in that 3-account set. Then compute the maximum rolling 7-day sum: sort those filtered transactions by timestamp, and for every transaction's date D as a window start, sum all transaction amounts with timestamp in [D, D+6 days], tracking the max across all windows (a sliding-window/two-pointer approach over sorted dates is more efficient than recomputing per date but either works for small data). Flag cycles where that max sum is strictly greater than 10000. Write flagged cycles to `/home/user/flagged_cycles.csv` with no header, each row as the three account names sorted alphabetically followed by the integer max sum, and sort the output rows by the first account name.