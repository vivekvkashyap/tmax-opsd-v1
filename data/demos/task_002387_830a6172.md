Reproduce before fixing: start all three services (db.py on 7070, worker.py on 9090, gateway.py on 8080) and POST a large tx_id like 9007199254740993 to `/process`, then read `/tmp/worker.log` and the response to see the corruption/crash firsthand rather than assuming where the bug is. Trace the data path: the gateway packs `tx_id` for the socket send to the worker, and the worker unpacks it — inspect both `struct.pack`/`struct.unpack` format strings and buffer sizes on each side; a 32-bit float format code (`'f'`, 4 bytes) cannot represent 53+ bit integers exactly, causing precision loss, and mismatched byte counts on `recv()` cause further corruption. Fix by switching both sides to an 8-byte integer format (`struct.pack('q', tx_id)` / `struct.unpack('q', data)` with `conn.recv(8)`) so the full 64-bit value round-trips exactly — keep both ends consistent. Add an assertion in worker.py right after unpacking, e.g. `assert type(tx_id) is int`, to catch future silent corruption. Restart all three services in the background and re-test the same large tx_id end-to-end, confirming the JSON response's `processed_id` matches the request's `tx_id` exactly (not just approximately).