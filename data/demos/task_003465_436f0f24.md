Build the pipeline in `analyze.py` in strict order, fitting the TF-IDF vectorizer only on `golden.csv`'s text column (`vectorizer.fit_transform(golden_df['text'])`), then use `.transform()` (never `.fit_transform()`) on `exp_A`/`exp_B` text so their vector spaces align with golden's vocabulary — this is the most common mistake (accidentally re-fitting on each experiment's own text, which breaks comparability). For each experiment row, compute `cosine_similarity(exp_vecs, golden_vecs)` and take `.max(axis=1)` per row to get the validation score array. Run `scipy.stats.ttest_ind(scores_A, scores_B, equal_var=False)` for Welch's t-test to get t_stat/p_value. For the Bayesian step, count successes in experiment A as `sum(scores_A > 0.5)`, failures as the remainder, and the Beta(1,1) posterior is simply `alpha_post = 1 + successes`, `beta_post = 1 + failures` (conjugate Binomial-Beta update — no need for MCMC or scipy.stats.beta fitting). Write exactly the four required keys to `/home/user/report.json` as valid JSON, then re-open and print it to sanity-check types (float vs int) before finishing. Install any missing packages with `pip install pandas scikit-learn scipy` first.