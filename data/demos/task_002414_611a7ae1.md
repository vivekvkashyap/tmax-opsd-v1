Start by transcribing `/app/transmission.wav` (via `whisper` CLI or similar) to recover the exact integer modulus the audio specifies — don't reuse whatever hardcoded value the PR author left, since that's explicitly called out as wrong. In `/home/user/aud-eval`, rewrite `parser.go` as a proper recursive-descent (or Pratt) expression parser producing an AST that correctly handles nested parentheses and operator precedence for `+ - * /`, then an evaluator that walks that AST. For checksum validation, parse each `<expression> | <checksum>` line, sum the ASCII codes of all non-space characters in the expression, and reject (skip) any line whose computed sum doesn't match the stored checksum. Refactor `main.go` to fan out work across a worker-pool of goroutines reading from an input channel and writing results to an output channel (buffered appropriately), so the 500k-1M line file is processed in parallel rather than one line at a time — this is what gets you the required ~4x+ speedup. Apply the transcribed modulus to every evaluated result before writing `<expression>,<result>` lines to `results.csv`. Build with `go build -o bin/aud-eval` and benchmark against a large input to confirm both runtime and 100% accuracy before finishing.