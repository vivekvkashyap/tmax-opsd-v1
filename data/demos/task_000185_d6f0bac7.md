Reverse-engineer `/app/config_unpacker`'s expected archive layout before writing any packing code: run `strings /app/config_unpacker | grep -i -E "magic|CFG|error"` and `objdump -d /app/config_unpacker` (or `xxd` on a tiny hand-crafted candidate file, then rerun the binary against it and watch where it errors) to pin down the magic bytes, integer field widths/endianness, and per-file header layout (path length, path, original size, compressed size, then payload). Treat any exit code / stderr message as a strong signal about which field you got wrong — iterate by constructing minimal 1-file archives and testing extraction against the binary rather than guessing the whole format at once. Once the format is confirmed, write `/home/user/pack.go` to: read `/home/user/backup_rules.json`, `filepath.Walk` `/home/user/configs/` applying the extension/age/size filters exactly, then for each surviving file write the header fields with `binary.Write(buf, binary.LittleEndian, ...)` and compress the payload using Go's `compress/flate` — critically, use `flate.NewWriter(w, flate.BestCompression)` (level 9), since default compression won't clear the 45,000-byte target. Verify your output actually round-trips through `/app/config_unpacker` before declaring done.
