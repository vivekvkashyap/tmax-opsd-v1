Break this into three independent sub-problems and verify each before combining. (1) Consensus primer: parse the FASTA with Biopython or manual line parsing, take the first 20 characters of every sequence record, and at each of the 20 positions pick the majority nucleotide across all records (a simple `Counter.most_common(1)` per column) — don't just copy one sequence, since some columns may have minority variants that must be outvoted. (2) Curve fit: load the CSV with pandas, set `F0` to the fluorescence value at `time==0` from the data (not a guessed constant), then use `scipy.optimize.curve_fit` on the logistic form `K/(1+(K/F0-1)*exp(-r*t))` with `F0` fixed (wrap it in a lambda or partial so curve_fit only estimates `r` and `K`), and pick positive initial guesses (e.g. r≈0.1-1, K≈max(fluorescence)*1.1) so the optimizer converges to the true basin. (3) Integrate the fitted `F(t)` from 0 to 30 with `scipy.integrate.quad`, reusing the exact fitted `r`, `K`, and the same `F0`. Round each numeric output to 3 decimals per the spec and write the JSON with the exact key names to `/home/user/analysis_output.json`.