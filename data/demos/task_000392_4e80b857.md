For Phase 1, don't just call `tar.extractall()` — that's the Zip Slip vulnerability itself. Rewrite `extract.py` to iterate `tar.getmembers()`, for each member compute the resolved absolute destination path (`os.path.realpath(os.path.join(dest_dir, member.name))`), and only extract that member if the resolved path is still inside `os.path.realpath(dest_dir)` (e.g. `resolved.startswith(dest_dir + os.sep)`); skip (don't raise on) any member that escapes. Run the fixed script against the tar and confirm no files landed outside `/home/user/docs_target`. For Phase 2, locate ELF files by checking the `\x7fELF` magic bytes and size, then get the entry point via `readelf -h <file> | grep 'Entry point'` (or parse the e_entry field directly) and format it as `0x...`; for `.gcode` files, regex-extract the numeric value after `E` on `G0`/`G1` lines and track the running maximum per file; for `compile.log`, use a regex anchored on lines starting with `[YYYY-MM-DD]` to split records, and keep only records whose first line contains `FATAL`, preserving all continuation lines until the next timestamp. Assemble Phase 3's markdown with exact formatting via a script, not by hand, to avoid transcription errors.