Start by black-box probing `/app/oracle_graph_parser`: feed it small, hand-crafted binary inputs (use `printf`/`xxd -r` to build byte sequences) representing tiny graphs and observe stdout to infer the header layout (magic bytes, vertex/edge counts, endianness, field widths) and the output format (separator, ordering). Cross-check your hypothesis with `strings` and `objdump -d`/`file` on the binary to spot format constants or library calls that hint at the encoding. Once the wire format and the topological-sort tie-breaking rule (test with graphs that have multiple valid orderings to see which vertex it prefers) are confirmed by matching oracle outputs on several inputs, port the logic into `resolver.cpp`: parse the buffer manually (no dynamic library dependency on the oracle), implement Kahn's algorithm with a min-heap for deterministic tie-breaking, and manage all heap allocations with RAII/smart pointers or matching new/delete to avoid leaks — verify with `valgrind --leak-check=full` after building. Fix `setup.py` by ensuring it declares the extension module name exactly as `_graph_resolver`, points to `resolver.cpp`, and includes any needed C++ standard flag. Finally write `runner.py` to read `sys.stdin.buffer`, call your extension, and print the exact string — diff its output against the oracle on the same inputs you used to reverse-engineer the format.
