Write `evaluate.rs` as a simple stdin line reader: split each line on whitespace, parse fields 2 and 3 as `f64` (skip/ignore malformed lines defensively), and print field 1 only when `(pred - actual).abs() < 1.0`. Compile with `rustc -O /home/user/evaluate.rs -o /home/user/evaluate`. Since both preds.txt and truth.txt are already sorted by numeric id and space-separated, use `join /home/user/preds.txt /home/user/truth.txt` directly (default join key is the first whitespace-delimited field) and pipe its output straight into `./evaluate`, redirecting to `/home/user/valid_ids.txt`. Watch out for `join`'s requirement that both inputs be sorted the same way (they already are per the spec) and that the joined line order is `id pred actual` matching what your Rust parser expects. Count lines with `wc -l < /home/user/valid_ids.txt` and append `Run 1: <count> valid models` to `/home/user/experiment_log.txt` using that count, not a hardcoded number.
