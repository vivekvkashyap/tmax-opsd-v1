Start by diffing the vendored library against expectations rather than guessing: grep `/app/kissfft/kiss_fft.h` for the `kiss_fft_scalar` type definition — kissfft is meant to default to `float`, so if you find it typedef'd/defined to `int` (or any non-floating type), that's the injected perturbation; restore it to `float` and recompile the library and your program against the fixed header. For the C filter, write `/home/user/filter.c` to: read 1024 whitespace-separated floats from the input file into a real-valued buffer, call `kiss_fft_alloc`/`kiss_fftr` (or complex kiss_fft with zero imaginary parts) to get 1024 complex bins, compute magnitude-squared (`re*re + im*im`) for each bin, then scan only indices 400–450 inclusive for any value exceeding 50.0 — if found, `return 1`; otherwise `return 0`. Compile with `gcc filter.c /app/kissfft/kiss_fft.c -I/app/kissfft -lm -o /home/user/filter`. Validate against both `/app/corpus/clean/` (must all exit 0) and `/app/corpus/evil/` (must all exit 1) before considering it done — if results look inverted or truncated, suspect the scalar type wasn't actually fixed or a stale object file is being linked.
