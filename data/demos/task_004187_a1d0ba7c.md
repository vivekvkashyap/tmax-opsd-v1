Debug this like a real 502: check each hop independently before assuming the fix. First try building/running the Go backend standalone and see what it actually panics on — read `main.go` for a hardcoded directory value and a conditional panic that fires precisely when that value is anything other than itself (i.e., it's designed to break the moment someone "fixes" the directory naively); you need to both correct the served directory to the required path and remove/neutralize the panic condition, not just one or the other. Rebuild, run the server as a background process, and confirm with `curl localhost:9000/...` that it serves files directly before touching nginx. Then inspect `nginx.conf` for the `proxy_pass` target port and correct it to match the port the Go server actually listens on. Start nginx with the given config, then re-verify with `curl` through port 8080 before trusting the load test. Finally write the tunnel command (local 8080 -> remote 9999 on localhost, via `ssh -L`) to the required file without executing it, and drop the completion log line once the full chain is confirmed serving correctly end-to-end.
