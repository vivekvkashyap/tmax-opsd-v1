Diagnose the vendored cJSON build failure by actually compiling `/app/cJSON` first and reading the compiler error precisely (e.g. `make` inside that directory) — the failure will point at whichever header include is broken; fix that single include line in `cJSON.c` rather than rewriting library code, then confirm `libcjson.so` builds and is linkable. For `/app/abc_parser.c`, parse the header fields in the exact byte order and widths given (4-byte magic, 1-byte version, 1-byte flags, 2-byte LE metadata length, 4-byte LE payload length), and validate strictly: wrong magic, wrong version, or a file that ends before metadata/payload are fully present must all produce exactly `{"error": "invalid format"}` on stdout and exit 1 — check remaining file length against declared lengths before reading, don't just let `fread` short-reads pass silently. Parse metadata by splitting on `;` then `:` per pair, tolerating trailing semicolons, missing values, and empty metadata without crashing. If flag bit 0 is set, XOR every payload byte with `0x5A` before computing the 8-bit (mod 256) checksum. Build the final JSON with cJSON's API (not manual string concatenation) to guarantee valid escaping, matching field names/types exactly. Since a fuzzer diffs your binary byte-for-byte against a reference across ~1000 malformed/valid inputs, test your parser yourself against hand-crafted truncated and malformed files (not just the happy path) before considering it done.