Write `/home/user/curate.sh` as a loop over `.meta` files in `/home/user/incoming`, since each archive has one companion `.meta` file. For each, use `grep`/`sed` (e.g. `sed -n 's:.*<status>\(.*\)</status>.*:\1:p'`) to pull out `<name>`, `<version>`, `<arch>`, `<status>` — skip the archive entirely unless status is exactly `production`. Determine the archive's extension (`.tar.gz` vs `.zip`) from the filename next to the `.meta` file, then extract just `build.txt` to a temp location (`tar -xzf ... build.txt` or `unzip -p ... build.txt`) and pull the value after `ID=` with `awk -F= '{print $2}'` or `cut`. Lowercase the name (`tr '[:upper:]' '[:lower:]'` or bash `${var,,}`) to build the destination filename `<lowercase_name>-<version>.<ext>`, `mkdir -p` the per-arch subdirectory under `/home/user/repo`, and `cp` the original archive there (not the extracted contents). Accumulate one JSON object per curated artifact and use `jq -n --slurpfile` or build a jq array incrementally to emit valid `/home/user/repo/manifest.json` — avoid hand-built JSON string concatenation, which breaks easily on escaping.
