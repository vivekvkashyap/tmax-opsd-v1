Recover the policy values from the image first rather than assuming defaults: run `tesseract /app/policy.png stdout` (trying different `--psm` modes if the first pass is noisy) and parse the `KEY=VALUE` lines for `LOG_DIR`, `ARCHIVE_DIR`, and `CHUNK_SIZE`, printing what you extracted to sanity-check it looks like real paths and a numeric byte count before hardcoding it into your script. In `archiver.py`, create both directories if missing, then for each `.log` file in `LOG_DIR`: open it, acquire an exclusive `fcntl.flock(fd, fcntl.LOCK_EX)` before reading, then stream-read in `CHUNK_SIZE`-byte binary chunks (the last chunk naturally shorter), compress each chunk with Python's `gzip` module (writing directly with `gzip.open(...,'wb')` rather than shelling out, so you control the compression level — use a high level like 9 to maximize the ratio, which matters since the data is highly repetitive and must compress well under the size threshold) and name outputs `<filename>.chunk<idx>.gz` in `ARCHIVE_DIR`. Release the lock, then delete the original file only after all chunks are confirmed written. Run the script once and check the total size of `ARCHIVE_DIR` yourself (e.g. `du -sb`) to confirm it comfortably clears the target before considering the task done.
