This is a fuzz-equivalence task: your script's stdout must match a hidden reference byte-for-byte across many random CSVs, so favor a straightforward, literal implementation of the spec over cleverness that might diverge on edge cases. First extract the 10 initial lock records from the video: sample one frame per second (t=0..9) from `/app/historical_locks.mp4` (e.g. with `ffmpeg -i ... -vf fps=1`) and OCR the center text overlay on each frame (e.g. `tesseract`) to recover each `TransactionID,ResourceID` pair — don't hardcode values, actually read them from the video since that's the state your pipeline initializes from. Model the wait-for graph explicitly: a resource is "free" or "held by exactly one transaction"; a REQUEST either acquires a free resource (edge Resource→Transaction) or queues the transaction as waiting (edge Transaction→Resource); a RELEASE hands the resource to the longest-waiting queued requester (order by original request Timestamp) or frees it if none are waiting. After each event, or at the end, detect cycles in the transaction-wait graph via DFS to find deadlocks; report each cycle's distinct transaction IDs sorted ascending, and sort the list of cycles lexicographically. Track "longest waiting, not yet granted" transaction using the minimum timestamp among currently-pending REQUESTs. Implement as `/home/user/etl_pipeline.sh <csv>` printing only the final strict JSON to stdout — test against several self-authored CSVs including cycles and releases before considering it done.