First localize and fix the packaging bug before writing any pipeline logic: inspect `bin/graph_tool` in `/app/graph_tools-1.0.0` — it's a wrapper script that likely hardcodes a python interpreter variable pointing at `python2`; change it to invoke `python3`, then run `make install` and confirm `graph_tool --input <file>` works standalone on a small test CSV before building anything on top of it. For the bootstrap script, implement the resampling with explicit row sampling with replacement (e.g., using `awk` with `rand()` or `shuf -r -n <rows>` on the data lines only, always re-prepending the original header before invoking the tool), write each sampled file to a temp path, capture the single float `graph_tool --input` prints, and accumulate all N values into an array/file. Sort the N collected values numerically (`sort -n`), then pick the 2.5th/97.5th percentile entries using the exact 1-based rank formula given in the task (e.g. for N=500 that's specific fixed ranks — derive the general formula `ceil(0.025*N)` and `floor(0.975*N)` or similar rather than hardcoding for one N, since correctness is judged against a reference bootstrap). Emit the two bounds as the specified JSON keys with reasonable precision, then run the full script once with N=500 against the real signals.csv and sanity-check the output values look plausible before finishing.