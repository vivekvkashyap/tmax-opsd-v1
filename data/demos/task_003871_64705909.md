Start by inspecting the broken SQL: a three-table comma join (`employees, transfers, messages`) without a join condition tying `transfers` to `messages` creates a Cartesian product — each transfer gets duplicated once per matching urgent message from that sender, inflating SUM(amount). Fix it by first finding the *set* of employees who sent at least one 'urgent' message (e.g. a subquery/CTE like `SELECT DISTINCT sender_id FROM messages WHERE content LIKE '%urgent%'`), then join that set only against `transfers` (not `messages`) and GROUP BY employee to sum `amount`, so each transfer row is counted exactly once. Save this corrected query to fixed_query.sql and run it via sqlite3 to get accurate totals. For the graph analytics part, query transfers filtered to Trading-department employees, group by sender_id, count rows (COUNT(*), not SUM(amount)) to get out-degree, and take the max. Cross-check both results independently (e.g. compute by hand from the query outputs) before writing audit_summary.json with the exact two required keys, then validate the JSON parses correctly (e.g. `python3 -m json.tool`).