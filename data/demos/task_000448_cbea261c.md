Treat this as three independent, chained fixes that you should verify at each layer before moving to the next. First, fix `firewall.py`: find the conditional that inspects the `X-Forensics` header and drops/blocks the request when it's present, and invert or remove that check so requests carrying `X-Forensics: true` are forwarded to Nginx instead of rejected — test with `curl -H "X-Forensics: true" http://127.0.0.1:8000/evidence` and confirm it no longer gets blocked at the firewall layer. Second, fix `nginx.conf`: locate the directive that clears the `Cookie` header (e.g. `proxy_set_header Cookie "";`) and change it to forward the incoming cookie unmodified (`proxy_set_header Cookie $http_cookie;`), then reload/restart nginx with the config. Third, fill in `decrypt.sh` with the openssl decrypt invocation matching how the evidence was originally encrypted — `aes-256-cbc` with `-pbkdf2` and the given passphrase, decrypting `-in` the evidence file `-d` to stdout (no `-out` flag, so it prints directly). After each fix, restart all three services via `start_services.sh` and test the full chain end-to-end with a curl request through port 8000 carrying both the header and the `Auth-Token=admin_access` cookie, confirming the decrypted plaintext comes back with HTTP 200. Leave all services running in the background afterward.
