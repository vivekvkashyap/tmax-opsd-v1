Read `legacy_tester.js` and `server.py` fully first to understand the exact contract before writing Python — don't guess at endpoint behavior. Build `test_pipeline.py` in stages: (1) `os.makedirs('artifacts', exist_ok=True)` then `subprocess.run(['g++', 'src/main.cpp', '-o', 'artifacts/cpp_artifact'])` and `subprocess.run(['rustc', 'src/main.rs', '-o', 'artifacts/rust_artifact'])`, checking each return code; (2) launch `server.py` as a background subprocess (`subprocess.Popen`) and poll/sleep briefly until port 8080 accepts connections before sending requests; (3) for the missing-header test, POST the compiled cpp artifact bytes to `/upload` without the `X-Artifact-Type` header using `requests` or `urllib`, and assert status 400; (4) for rate limiting, fire 3 POSTs back-to-back with a valid header (`cpp` or `rust`) with no sleep between them — since the server tracks a rolling 1-second window, the first two should return 200 and the third 429; use threads or rapid sequential calls, not delayed ones, or the window will reset. Finally write the four boolean results to `/home/user/workspace/test_results.json` with the exact key names specified, and make sure the server process is left running or cleaned up appropriately.