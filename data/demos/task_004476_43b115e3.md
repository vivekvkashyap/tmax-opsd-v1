Treat this as black-box behavioral cloning: implement the algorithm from the description first (in-degree-0 nodes are primaries; for every other node compute shortest-path `transfer_time` distance from the nearest primary via Dijkstra/BFS-with-weights; unreachable or primary nodes get `min_transfer_time=0`; score = `size*(1+min_transfer_time)`; output node IDs sorted by score descending as a comma-separated list). Since tie-breaking and edge-case handling (cycles, multiple components, disconnected nodes) aren't fully specified, generate a variety of small synthetic JSON graphs yourself (including graphs with equal-score ties, cycles among non-root nodes, and isolated nodes) and run each through `/app/backup_path_oracle <file.json>`, comparing byte-for-byte against your own script's output on the same input. Iterate on your tie-breaking rule and edge-case logic (e.g. try sorting equal scores by ascending node ID, then verify) until your outputs match the oracle exactly across your generated test corpus, then finalize `/home/user/backup_planner.py` reading the path from `sys.argv[1]`.