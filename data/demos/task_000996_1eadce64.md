Build this in clear stages and test each before moving on. First use `ffmpeg -i traffic_feed.mp4 frame_%05d.jpg` to dump frames, then get each JPEG's byte size (e.g. `stat -c%s` or `ls -l`) and group frames into 1-second buckets of 24 consecutive frames, taking the max size per bucket — do this with a script or precompute it into a simple file your C program can read, since only the joining/inference/serving logic must be in C. In C, parse the radar CSV (skip header, split on commas) and join each row's timestamp_sec to its corresponding max-JPEG-size bucket. Implement the Bayesian update carefully: compute `evidence = likelihood_speeding*prior + likelihood_not_speeding*(1-prior)` then `posterior = likelihood_speeding*prior/evidence`, and append the experiment log line for every row processed. For the HTTP server, use raw sockets (`socket`/`bind`/`listen`/`accept`) on 127.0.0.1:8080, parse the request line for the `sec` query parameter and manually scan headers for `X-Auth-Token: etl-agent-secret` (case-sensitive header line match), returning 401 if absent/wrong and 200 with the posterior formatted via `%.4f` otherwise. Run the server backgrounded (e.g. `./server &`) so it stays alive after your turn ends, and verify with `curl` yourself before finishing.