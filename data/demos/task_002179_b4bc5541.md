Read `src/main.rs` closely and identify exactly where statistics are computed over `data` as a whole rather than over `data[..split_idx]` — the fix is purely about which slice feeds the mean/variance calculation, not about the normalization formula itself. Split the array explicitly into a training slice (`&data[..split_idx]`) and a test slice (`&data[split_idx..]`), compute the population mean and population standard deviation using only the training slice, then apply that same (train-derived) mean/std to normalize the test slice values before writing them out — do not also renormalize or recompute anything using test data, and don't touch the positivity schema check that runs earlier. Keep the output JSON shape/keys the same so `/home/user/benchmark_metrics.json` is still produced by the existing `File::create`/`write_all` call. After editing, run `cargo run --release` yourself and sanity-check that the two normalized test values look like a reasonable z-score given training-set statistics (not near-zero, which would suggest you accidentally normalized against the whole dataset's mean instead of the training mean) before considering it done.
