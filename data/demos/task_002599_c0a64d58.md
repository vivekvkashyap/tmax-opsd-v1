Structure the C++ program in the three stages the task describes, and validate each independently before wiring them together. (1) FASTA parsing: read both files line by line, skip lines starting with `>`, and concatenate the rest into a single uppercase sequence string per file. (2) Primer extraction: forward primer = first 20 chars of target; reverse primer = reverse-complement (A<->T, C<->G, then reverse the string) of the *last* 20 chars of target — implement the complement mapping carefully and reverse the string itself, not just complement in place. Search for each primer as an exact substring in the reference sequence and count all occurrences (including overlapping ones, using a sliding-window `find`/`substr` scan advancing by 1 each match) — if either count != 1, print the exact error string and stop before doing any ODE work. (3) ODE simulation: implement the Euler update `A += r*A*(1-A/K)*delta_c` in a loop incrementing `c` by `delta_c` each iteration, and record the first `c` (formatted to 2 decimals) at which `A` reaches or exceeds the 1.0e7 threshold — check the threshold *after* each update, not before. Compile with `g++ -O2` and format the results file exactly as specified.
