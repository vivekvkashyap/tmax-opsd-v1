This task has three independent pieces — solve them separately then wire together. (1) For the video, run `ffmpeg` to extract frames at roughly 1-second intervals (e.g. `-vf fps=1`), then OCR each frame (e.g. `tesseract`) to recover the "CRASH_PAYLOAD: <hex>" lines in order; write just the hex values, one per line, to the specified output file — don't guess the values, derive them from the frames. (2) For the Rust bug: a PyO3 function taking `payload: String` forces an owned-string allocation/clone on every call from Python; change the signature to borrow a `&str` (via `&PyString` or PyO3's zero-copy string extraction) so no clone occurs, which is what actually fixes both the borrow-checker friction and the performance problem — then `cargo build --release` and confirm the `.so` lands where `scanner.py` expects it. (3) For the Python import bug, reorder `scanner.py` so `ssl` and `requests` (or equivalent standard-library/third-party modules touched by CI's monkey-patching) are imported and initialized before `import fast_fuzzer`. Finally write `benchmark.py` to loop 10,000 times over the 5 extracted payloads, time it with `time.perf_counter()`, and print exactly `Throughput: <float> seconds`. Measure before/after to confirm you actually crossed the performance threshold, not just that it compiles.