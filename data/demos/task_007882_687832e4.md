Fix permissions first and simply: `chmod 700 /home/user/.ssh && chmod 600 /home/user/.ssh/id_rsa`. Generate the new keypair with `ssh-keygen -t ed25519 -f /home/user/.ssh/deploy_key -N ""` (empty passphrase via `-N ""`, non-interactive). For the payload, read `/home/user/legacy_auth.py` to understand its XOR key and base64 scheme rather than assuming a value, then write a small decode routine that reverses it: base64-decode `/home/user/admin_payload.b64`, XOR every byte with the same key found in the library, and decode as text to get `username:password` — split on the first `:` to isolate the password. Reuse (import or replicate) `encode_payload` from `legacy_auth.py` itself to generate the line-3 token for `system_admin`, rather than reimplementing the XOR logic separately, to guarantee it matches exactly. Assemble `/home/user/rotation_summary.txt` as exactly three lines in order: recovered password, the full contents of `deploy_key.pub`, then the newly generated token — no extra blank lines or labels.
