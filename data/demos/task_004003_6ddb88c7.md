Set up a normal Rust binary crate (or a single-file `rustc` build) that you compile and place at exactly `/home/user/metrics-dashboard.git/hooks/post-receive`, then `chmod 755` it — Git hooks must be directly executable, not scripts requiring an interpreter line unless you add a proper shebang for a script wrapper, so compiling a native binary and pointing the hook path directly at it is simplest. In `main`, read stdin line by line; for each line, split on whitespace and require exactly 3 fields — if not, write "Invalid input" to stderr and `std::process::exit(1)` immediately (test this path first with `echo "bad data" | ./post-receive`, since a malformed single line should fail before any output is produced). For valid lines, build the JSON manually with `format!` (no serde) matching the exact key order and literal field values from the ref/new-value, ensure `/home/user/dashboard_data/` exists via `std::fs::create_dir_all`, append the line to `push_events.jsonl`, then explicitly `set_permissions` on the file to mode `0o644` after each write using `std::os::unix::fs::PermissionsExt`. Finally verify end-to-end with a real `git clone` + commit + `git push` against the bare repo to confirm the hook fires correctly during a real Git invocation, not just when piped manually.