Reproduce the crash first before touching code: build `log_ingestor` as-is per the given `gcc` command and run it against each file in `/home/user/logs/` individually (not all at once) to isolate exactly which file/line segfaults. Then reverse-engineer the black-box `libfilter.so` without source: use `objdump -d` or `gdb` (run under gdb, catch the SIGSEGV, inspect the backtrace/registers) to see it's a fixed-size stack buffer overflow inside `filter_log`, and confirm the exact trigger by writing a small Python fuzzer that calls the ingestor (or a tiny C harness linking the .so) with a field of increasing length until it crashes, binary-searching to find the precise safe/unsafe boundary length rather than guessing. Once you know that boundary, edit `log_ingestor.c` to extract the vulnerable field from each line and truncate it to the safe length before calling `filter_log`, then add an `assert()` immediately before that call checking the field's length is within the safe bound as a defensive invariant. Recompile with the exact linking command given (don't touch the Makefile or `libfilter.so`), rerun against all logs to confirm no crash, and write the crashing filename plus the discovered safe-length integer as the two lines of `/home/user/solution.txt`.