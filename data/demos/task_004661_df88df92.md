Start by reading `/app/vendored/netprobe-1.0.0/netprobe/client.py` and locate the artificial perturbation inside the async fetch path — look specifically for a synchronous/blocking call like `time.sleep(...)` sitting inside an `async def`, which stalls the whole event loop and throttles throughput regardless of concurrency; remove or replace it with `await asyncio.sleep(0)`/nothing, then reinstall the package locally (e.g. `pip install -e /app/vendored/netprobe-1.0.0`) so `python3 -m netprobe` picks up the fix. Separately build the infra: create `/home/user/services/{logs,run}` and symlink `/home/user/logs -> /home/user/services/logs`; write `start_backends.py` to spawn three `http.server`-based processes on 9001-9003 (e.g. via `subprocess.Popen`) and record each PID to a file under `services/run/`; write `proxy.py` as an async reverse proxy (e.g. using `asyncio.start_server` or `aiohttp`) on port 8080 that round-robins each incoming request to one of the three backends and streams the response back — keep it non-blocking throughout, since a slow proxy will bottleneck the RPS metric just like the netprobe bug did. Launch backends and proxy in the background, then run the netprobe command exactly as specified and confirm `requests_per_second` in the resulting JSON clears the required threshold before finishing.