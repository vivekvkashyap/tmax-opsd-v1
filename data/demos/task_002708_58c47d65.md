Write the .proto file first with the exact package, service, and message field names/tags specified, then wire up build.rs to invoke tonic-build/prost-build against it and to emit cargo linker directives (`cargo:rustc-link-lib=math_core`, `cargo:rustc-link-search=...`) pointing at the legacy library directory. In main.rs, declare the FFI signature for compute_transform inside an `extern "C"` block matching the C header's types exactly (uint64_t maps to u64), and wrap the unsafe call in a safe helper. Implement the generated service trait, calling the FFI function inside the RPC handler and returning the result field. Build with `cargo build --release`, then launch the binary in the background with `LD_LIBRARY_PATH` set to the legacy library directory so the dynamic loader can resolve libmath_core.so — verify with `ldd` on the binary if the server fails to start. For benchmark.sh, use `grpcurl -plaintext -d '{"value": N}' 127.0.0.1:50051 <package>.<Service>/<Method>` to call the endpoint, parse the JSON result field out of its response (e.g., with jq) for the answer file, then loop the same call 50 times timing the whole loop and append elapsed seconds to the results file.
