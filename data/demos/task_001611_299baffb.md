Work through this as diagnose -> fix -> verify -> automate, in that order. Run `/home/user/deploy.sh` first and read its actual failure output to confirm it's the simulated `/home/user/app/data` quota check failing (inspect `service-runner`/whatever it calls to see the threshold logic). Then rewrite `cleanup.sh` as a real text-processing pipeline: parse `registry.txt` lines (`YYYY-MM-DD | filename | STATUS`, pipe-delimited with spaces) using `awk -F '|'` or similar, trimming whitespace from each field; for every line whose status trims to `PROCESSED`, delete the corresponding file from `/home/user/app/data/` and drop that line from the registry (e.g., build a filtered version and overwrite the original), leaving non-PROCESSED lines untouched. Before deleting, note which of the PROCESSED files is largest (`ls -S` or `du` comparison) so you can report its full path later. After cleanup runs, re-run `deploy.sh` and confirm it exits 0. Install the cron job with `crontab -e` or `(crontab -l; echo "0 * * * * /home/user/bin/cleanup.sh") | crontab -` for exactly-top-of-the-hour scheduling, then write the two-line resolution file with the largest deleted file's path and the exact cron expression you used, matching the format required exactly.