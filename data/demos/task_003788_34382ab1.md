Build this as a strict pipeline of independently-testable stages: parse, clean, reshape, roll, serve. Spawn `/app/sensor_gen 5000` via `subprocess` capturing stdout as bytes, then parse it with `csv` or `pandas.read_csv` (both handle embedded newlines inside quoted fields correctly — don't split on `\n` yourself, that's exactly the trap the messy `metadata` column sets). Deduplicate on `id` keeping the first occurrence (`drop_duplicates(subset='id', keep='first')` if using pandas), then drop rows where any of `sensor_A/B/C` is empty/NaN. Melt the wide frame into long format with `id, timestamp, sensor, value`, then sort by `timestamp` ascending — do the rolling computation *per sensor group*, not globally, since window state must not mix sensors. For each sensor's sorted series, compute a trailing window of the previous 50 values (excluding the current row) starting at the 51st row, using sample standard deviation (ddof=1); flag anomalies where `abs(value - rolling_mean) > 3.0 * rolling_std`. Serve results with a minimal `http.server` (or Flask) on 127.0.0.1:8080, doing all data processing synchronously before entering the listen loop, and format `/anomalies` output sorted by timestamp then sensor name exactly as specified.
