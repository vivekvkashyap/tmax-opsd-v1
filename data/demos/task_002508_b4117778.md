Read `/home/user/pipeline.c` closely to find where the mean is computed and change the accumulation loop so it sums/counts `feature_val` only over the first 800 rows (skip -999.0 sentinels within that subset), then apply that single training-derived mean to fill -999.0 wherever it appears in the full 1000 rows, but only emit rows 801-1000 to `test_imputed.csv` with two-decimal formatting. Treat `/app/outlier_scorer` as an opaque black box: run it exactly as specified (`/app/outlier_scorer /home/user/test_imputed.csv`) and pair its stdout lines positionally with the CSV rows (same order, one score per line) rather than trying to reverse-engineer its internal scoring logic. Filter out rows whose score is strictly greater than 0.80, write the remainder to `final_test.csv`, then serve the containing directory over HTTP on 127.0.0.1:8080 (e.g. `python3 -m http.server 8080 --directory /home/user --bind 127.0.0.1 &` if final_test.csv lives there, or use Python's http.server module directly) so `GET /final_test.csv` returns the file, and leave that process running in the background rather than exiting.