For the credential leak, don't grep process listings for the secret directly — find the PID with `pgrep -f data_processor.py` (or `ps aux | grep`), then read `/proc/<pid>/cmdline`, which stores arguments NUL-separated (use `tr '\0' ' '` or `cat -A`/`xargs -0` to make it readable), and extract the value that follows the `--api-key` token; write exactly that string (no extra whitespace/newline issues) to `/home/user/compromised_key.txt`. For permissions, `chmod 600 /home/user/private.pem` gives owner read/write only with no execute and no group/other access, matching "no one else has any access." For integrity verification, don't manually recompute and string-compare — use the provided checksum file's own mechanism: `cd /home/user && sha256sum -c backup.zip.sha256` (or `sha256sum --check`), check its exit code/output for "OK", and write `VALID` or `INVALID` to `/home/user/integrity_status.txt` accordingly. Do each check independently and confirm each output file's exact contents before moving on.
