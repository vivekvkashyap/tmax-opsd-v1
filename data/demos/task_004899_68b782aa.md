Treat this as a debug-then-run task, not a rewrite: locate the exact subprocess spawn in the vendored package's scheduler code first, since the fix must be surgical. Search the source tree (e.g. `grep -rn "subprocess" /app/vendored_ci_runner-1.2.0`) to find where the deployment shell script is launched — look specifically for a `Popen`/`run` call passing an explicit `env=` dict (an empty or broken PATH like `{"PATH": ""}` or `{"PATH": "/usr/local/broken"}`) and no `cwd`. Fix it by inheriting the real environment (`env=os.environ.copy()`, optionally overriding PATH to include standard bin dirs) and setting `cwd="/home/user/deploy_logs/"` so relative log writes and shell built-ins (`echo`, `date`, etc.) resolve correctly. Create `/home/user/deploy_logs/` with writable permissions for the process's user before starting the service. Start the service bound to `127.0.0.1:8080` as a background/daemon process so it survives your shell exiting (e.g. `nohup ... &` or a proper daemonizing launcher), and confirm it's listening (`curl`/`ss`) before writing `SERVICE_READY` to `/home/user/status.txt`. Verify end-to-end yourself: POST to `/api/v1/deploy` with header `Authorization: Bearer DeployerToken-99x` and confirm a log file actually appears under `/home/user/deploy_logs/` before declaring done.