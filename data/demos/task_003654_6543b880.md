Scaffold with `cargo new log_processor` in `/home/user/log_processor` and add `serde`, `serde_json`, `sha2` to `Cargo.toml`. Read `raw_logs.jsonl` line by line preserving order; for each entry compute SHA-256 over the exact concatenation `{action}-{status}-{email}` (build the string first, then hash), and use a `HashSet<String>` (or `HashMap` of hex digest) to detect and skip repeats, keeping only the first occurrence per hash — this is what "hash-based dedup, first occurrence wins" means in code. On the retained records, mask fields via string manipulation: split IP on `.` and replace the first three octets with `XXX`, and split email on `@` replacing everything before it with exactly `***`. Write masked, deduped entries to `clean_logs.jsonl` preserving original field order/types. Track counts as you go (total lines read, unique kept, frequency per `action` among unique) so you can compute the top action with a simple max-by-count scan, then load `report_template.html` and do straightforward `.replace("{{TOTAL_RAW}}", ...)` style substitutions for all three placeholders before writing `summary.html`.
