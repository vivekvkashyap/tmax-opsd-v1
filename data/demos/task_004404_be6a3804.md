First recover the incident code: extract candidate frames around where it flashes with `ffmpeg -i server_feed.mp4 -vf fps=<rate> frames/frame_%03d.png` (a higher fps than 1 helps catch a brief flash), then run `tesseract` on each frame and grep the OCR text for the `INC-XXXX` pattern to find the exact code — don't guess it. For the script, use `jq -c 'select(.level=="CRITICAL")'` to filter stdin, then per line pull `.log_id`, `.ip`, `.message` with `jq -r` (which naturally decodes `\uXXXX` escapes into literal characters). Mask the IP by replacing only the first two dot-separated octets with `XXX.XXX` using a targeted regex/sed substitution (`sed -E 's/^[0-9]+\.[0-9]+/XXX.XXX/'`) rather than a naive split that could break on malformed IPs. Assemble the exact template `[<CODE>] ID:<log_id> | IP:<ip> | MSG:<message>` per surviving line, writing only that to stdout, nothing else, and make sure the script is `chmod +x`.
