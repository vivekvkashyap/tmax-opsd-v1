Build `/home/user/enforcer.py` in stages and verify each before moving to the next. First, generate the JWT with `jwt.encode({"sub": "policy_scanner", "role": "auditor"}, "devsecops_secret_2024", algorithm="HS256")` from PyJWT — install with pip if missing. Second, fetch the server's TLS certificate for `127.0.0.1:8443` (e.g. `ssl.get_server_certificate` or opening a raw SSL socket and calling `getpeercert`) and parse the Issuer field to pull out just the CN component — don't hardcode it, extract it from the actual cert data structure/DN string. Third, call `GET https://127.0.0.1:8443/api/health` with `requests`, setting `Authorization: Bearer <token>` and `verify=False` (self-signed cert), capture the status code, and compare the response headers (case-insensitively) against the required set `{X-Content-Type-Options, Strict-Transport-Security, Content-Security-Policy}` — any of the three that are absent from the actual response go into `policy_violations` (only truly-missing ones, order doesn't matter). Finally write all four fields as a JSON object to `/home/user/report.json` in exactly the specified key structure, then re-open and print the file to confirm it's valid JSON with the right shape before finishing.