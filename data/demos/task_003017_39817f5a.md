Build and test the two scripts independently before chaining them. First write monitor.py: parse fstab_mock as whitespace-separated fields per line (mount_point, fs_type, port), create each mount_point directory if missing (`os.makedirs(path, exist_ok=True)`), and for connectivity use a raw `socket.socket()` with `settimeout(1)` and `connect_ex(('localhost', port))` — treat a zero return as reachable and any nonzero/exception as unreachable, wrapping in try/except so an unreachable port doesn't crash the script. Read `LOG_DIR` via `os.environ.get('LOG_DIR', '/tmp')`, ensure that directory exists, and write the status dict as JSON with exactly the keys `fs_type` and `backup_reachable` per mount point — match the schema precisely (booleans, not strings). Then write run_monitor.sh: it must `export LOG_DIR=/home/user/migration/logs`, `mkdir -p` that directory, then invoke `python3 /home/user/migration/monitor.py` — the whole point of the wrapper is to fix the cron-style environment problem, so set LOG_DIR before the python call, not after. Make run_monitor.sh executable and actually run it, then inspect the generated status.json against the schema to confirm both mount dirs were created and the two ports show the expected reachable/unreachable status.