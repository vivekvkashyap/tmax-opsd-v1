OCR `/app/network_map.png` (e.g. `tesseract`) and read the extracted text carefully to identify the exact target router hostname — don't assume it matches any router name you happen to see in the CSVs without confirming. Import `/app/data/routers.csv` and `/app/data/links.csv` into `/app/network.db` with `sqlite3 -csv` `.import`, then create indexes on the join/traversal columns (`links.source_id`, `links.target_id`) to speed up the recursive CTE. Write a recursive CTE that starts from `ALPHA-1`'s id, recursively follows directed edges in `links` accumulating total latency and path, and select the minimum-latency path reaching the OCR'd target hostname's id — join back to `routers` to map ids to hostnames for the final path string, joined with `->`. Export exactly two columns (`path`, `total_latency`) with a header row to `/app/www/path.csv` using `sqlite3 -header -csv`. Finally serve `/app/www/` on port 8080 in the background (e.g. `python3 -m http.server 8080 --directory /app/www &`) and confirm with `curl http://127.0.0.1:8080/path.csv` that it returns 200 and the expected CSV before finishing, since the verifier polls that exact endpoint.