Implement this as an exact, deterministic reproduction of the specified algorithm — any deviation from the stated seeding/ordering/RNG-call-sequence will produce a different MAP estimate than expected. In `/home/user/run_mcmc.py`, get `rank`/`size` from `MPI.COMM_WORLD`, seed with `np.random.seed(rank * 100)` before generating any random numbers on that rank, then run the Metropolis-Hastings loop exactly as specified: propose via `np.random.normal(0, 0.5)` added to the current lambda, reject non-positive proposals immediately (without consuming a uniform draw), otherwise compute the log-posterior difference and compare against `np.log(np.random.uniform())` — keep this call order (normal first, then uniform only if lambda_prop>0) since it affects the RNG stream. Store every iteration's current lambda (post-accept/reject) so you have exactly 5000 samples, then slice `[1000:]` for the 4000 post-burn-in samples. Use `comm.gather(samples, root=0)` so rank 0 receives a list ordered by rank, then flatten/concatenate in that same rank order before running `scipy.stats.gaussian_kde` with default settings, evaluating on `np.linspace(0, 20, 1000)`, and taking argmax for the MAP. Save samples with `np.save` and the MAP formatted to exactly 2 decimals with `f"{val:.2f}"`. Run via `mpirun -np 4 python run_mcmc.py` to test.
