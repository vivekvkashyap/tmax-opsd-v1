Parse each line with a strict format check (e.g. `sscanf` with a format string matching `[%d-%d-%d %d:%d:%d.%d] INFO duration=%d`, checking the return count equals the number of fields, or a hand-written character-by-character validator) — don't use a loose match that would accept lines missing the brackets or wrong spacing. After a line validates, drop the millisecond component to get a second-granularity timestamp key, and keep a data structure (e.g. an array/hash of the last-seen truncated timestamp) so that if the same truncated second repeats you skip subsequent entries for it, keeping only the first. Maintain a small ring buffer or index-based array of at most the last 3 kept durations; for the Nth kept entry, average over `min(N, 3)` of the most recent values (sum divided by count, not always divided by 3). Format each output line as `timestamp,avg` with the average printed via `printf("%.2f", ...)`. Compile with `gcc -o process_logs process_logs.c`, run it against `raw_logs.txt`, and diff-check `rolling_stats.csv` line by line against your own hand traced expectations before considering it done.
