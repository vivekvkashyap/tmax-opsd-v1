Extract the two hidden parameters from audio before writing any Go code: transcribe `/app/memo.wav` with a local STT tool (install `whisper.cpp` or similar if needed) and listen/read carefully for the exact CIDR subnet and the exact recovery key string as spoken — numbers and letters may be spelled out phonetically (e.g. "one seven two dot one six..."), so convert them precisely rather than approximating, and note whether the key uses hyphens or spaces as it will need to be reproduced verbatim in output. Then write `/home/user/telemetry_filter.go` to read stdin line by line, split each line on commas into DeviceID/IPAddress/Status, use `net.ParseCIDR` once at startup for the discovered subnet, and per line use `net.ParseIP` + `ipNet.Contains(ip)` — treating a `ParseIP` failure as "not matching" rather than crashing, since malformed IPs are an explicit test case. Emit `<DeviceID> RECOVER <key>` when Status is exactly "OFFLINE" and the IP is in-subnet, else `<DeviceID> IGNORE`. Compile with `go build -o /home/user/telemetry_filter telemetry_filter.go`, then self-test with a handful of crafted lines (in-subnet+offline, out-of-subnet, malformed IP, other statuses) piped through the binary to confirm exact output formatting before considering it done.