Install pandas and jinja2, then load metrics.csv with `pd.read_csv(parse_dates=['timestamp'])` and sort by timestamp globally first. Derive time_of_day from the hour with a simple bucket function (careful with the wraparound bucket 18:00-05:59 spanning midnight — handle it as an OR condition, not a single range). Compute the rolling stats per server: `df.groupby('server_id')['cpu_usage'].rolling(window=4, min_periods=1)` then `.mean()` and `.std(ddof=1)` — note that pandas' rolling std returns NaN when there's only 1 point in the window, so explicitly fillna(0.0) for windows with fewer than 2 points as the spec requires, rather than leaving NaN (which would break your anomaly comparison). Reset the index carefully after a groupby-rolling operation so you don't misalign rows back onto the original chronological ordering. The anomaly condition is a strict double inequality on the same row's cpu_usage against its own rolling stats — compute it row-wise after the rolling columns are attached. Finally render the Markdown via a Jinja2 Template string matching the exact header/table format given, iterating anomalies sorted chronologically, rounding cpu_usage to 1 decimal with Python round() or a Jinja filter, and write the rendered string to anomaly_report.md. Run the script and diff the actual output against the required table format character-for-character (pipe spacing, header text).
