Build `/home/user/craft_payload.sh` as a pipeline of discrete, testable stages rather than one giant one-liner: (1) at the top of the script, `chmod 700 "$0"` so it self-secures; (2) redact `/home/user/loot.txt` with `sed`/`grep -E` regexes — one pattern for IPv4 (`([0-9]{1,3}\.){3}[0-9]{1,3}`) replaced with the literal string `[REDACTED_IP]`, and one for SSNs (`[0-9]{3}-[0-9]{2}-[0-9]{4}`) replaced with `[REDACTED_SSN]` — apply IP redaction and SSN redaction as separate sed passes so patterns don't collide; (3) pipe the redacted text through `openssl enc -aes-256-cbc -pbkdf2 -pass pass:'Evasion2024!' -base64 -A` (the `-A` flag is what forces single-line, non-wrapped Base64 — a common mistake is omitting it and getting multi-line output that breaks strict format checks); (4) compute `echo -n "$B64_DATA" | openssl dgst -sha256 -hmac 'RedTeamC2Secret' -r | awk '{print $1}'` — note the HMAC must be over the exact Base64 string bytes, not the raw ciphertext, and `echo -n` avoids a stray trailing newline being hashed; (5) write output with `printf 'HMAC:%s\nDATA:%s\n' "$hmac" "$b64"` to `/home/user/payload.out`. Test round-trip decryption yourself with the reverse `openssl enc -d` command before considering it done.