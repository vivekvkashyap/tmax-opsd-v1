Build this as a pipeline of separately-testable stages rather than one monolithic program. First, extract exactly 1 frame/second from `/app/sensor_feed.mp4` with `ffmpeg -i /app/sensor_feed.mp4 -vf fps=1 frame_%03d.png` (shell out from Go via `os/exec` is fine), then OCR each frame with `tesseract <frame> stdout` (also via `os/exec`), capturing raw text per frame index. In Go, parse each OCR'd text block with a regex for the ISO8601 `Timestamp:` line and the numeric `Pressure: <float> kPa` line; on frames where OCR garbled the text (missing/unparseable Timestamp or Pressure), simply skip parsing that frame rather than failing the whole pipeline — this is expected and by design. Once you have the sparse set of (timestamp, pressure) pairs actually recovered, determine the min/max valid timestamps, build a complete second-by-second timeline between them, and for any second missing a value, linearly interpolate pressure using the nearest valid points before and after that gap (standard `y = y0 + (y1-y0)*(t-t0)/(t1-t0)`); don't just carry-forward or drop gaps. Write the full timeline to `/home/user/interpolated_pressure.csv` with headers `timestamp,pressure`, ISO8601 timestamps, and pressure formatted to exactly 1 decimal place. Validate against a few frames you inspect manually before trusting the MSE will be low.
