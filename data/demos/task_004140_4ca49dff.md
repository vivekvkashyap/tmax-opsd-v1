Treat the "corrupted index" instruction literally: every SQL query against `jobs` must include `FROM jobs NOT INDEXED` (SQLite syntax) so the query planner can't use `idx_parent`. Approach: (1) connect to `/home/user/backups.db` with Python's `sqlite3`; (2) find all root jobs via `SELECT * FROM jobs NOT INDEXED WHERE parent_id IS NULL`; (3) for each root, recursively (or iteratively via BFS/DFS, or a recursive CTE that also specifies NOT INDEXED on the jobs reference) collect all descendant jobs whose parent chain traces back to that root — a plain `parent_id = X` filter is one hop, so you need to follow the chain transitively, not just direct children; (4) sum `size_bytes` over the root plus all descendants for `total_size`, and count them for `job_count`; (5) filter out chains where `total_size <= 5000`; (6) join to `servers` to get `hostname`, group chains by hostname into lists sorted ascending by `root_job_id`; (7) write the nested dict to `/home/user/chain_report.json` matching the exact schema (keys `root_job_id`, `total_size`, `job_count`). Print/inspect the raw table contents first to sanity-check your chain-building logic against the schema before trusting the output.