Scaffold with `cargo new etl_pipeline` at `/home/user/`, then implement in stages you can test independently: (1) a line parser — split each line on the literal delimiters (`[`, `]`, whitespace, `-`) or use a regex/manual splitting to pull out endpoint, status code, and response time (strip the trailing `ms` and parse as f64); skip any record whose status code is 500. (2) Group into a `HashMap<String, Vec<f64>>` keyed by endpoint. (3) For each group compute n, mean, and *sample* standard deviation (divide by n-1, not n — this is the most common mistake and will silently produce wrong CIs), then the 95% CI as mean ± 1.96*s/sqrt(n), with the special case n<=1 meaning both bounds equal the mean. (4) Sort endpoints alphabetically before writing. (5) Format every float with exactly two decimals (Rust's `{:.2}` format specifier) and write the exact header `endpoint,count,mean,ci_lower,ci_upper` to `/home/user/data_warehouse/aggregated_stats.csv` (create the directory first). Build and run with `cargo run --release` and inspect the CSV by hand against a couple of endpoints computed with a calculator to catch rounding or sample-vs-population variance errors before trusting the full output.
