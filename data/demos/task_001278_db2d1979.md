Work the pipeline in order. First, transcribe `/app/reference_recording.wav`: install `ffmpeg` plus a local speech-to-text tool (e.g. `pip install openai-whisper`, or `whisper.cpp`), run it, then normalize the raw transcript yourself with `tr '[:lower:]' '[:upper:]'` and strip punctuation (e.g. `tr -d '[:punct:]'`) to derive DATASET_SIGNATURE — do not guess the phrase. Verify and extract the archive with `tar -tzf` then `tar -xzf /app/incoming_data.tar.gz -C /app/staging`. For the filter script, read `/app/dataset.conf`, split on commas into a required-keys array, and for each `.txt` file check every required key appears as a `Key:` line (e.g. with `grep -q "^Key:"` in a loop) — reject on any miss. For sanitization, grep the whole file content for the literal characters `;`, `|`, `&`, `$`, and backtick (careful with shell quoting/escaping each metacharacter in the regex) and reject on any match — test against files that contain these chars in values, not just filenames. Only after both checks pass, copy the file and append `Signature: <DATASET_SIGNATURE>` as the last line. Test against small handmade clean/evil examples before trusting your logic on the hidden corpus.