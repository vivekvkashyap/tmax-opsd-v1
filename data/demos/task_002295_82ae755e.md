First, read a sample of `/home/user/system_locks.jsonl` to catalogue every distinct schema shape present (different key names for process id and for held/wanted resources, possibly nested under different sub-object names). Write a normalization function that tries each known schema pattern in turn and produces a uniform `(pid, held_resources, waiting_resources)` tuple per line, skipping/logging anything unrecognized rather than crashing. Build a directed wait-for graph: for each process P waiting on resource R, find the process(es) currently holding R and add an edge P -> holder. Then run a BFS (not DFS) from `P_COMPLIANCE_AUDIT` to guarantee you find the *shortest* cycle back to it — track predecessor chains, and when you first reach `P_COMPLIANCE_AUDIT` again via a valid edge, reconstruct the path from start to that point. If multiple shortest cycles exist, generate all candidates at the shortest length and pick the lexicographically smallest when serialized as the comma-joined string. Write the result as a single line, comma-separated, both starting and ending with `P_COMPLIANCE_AUDIT`, to the exact output path required.
