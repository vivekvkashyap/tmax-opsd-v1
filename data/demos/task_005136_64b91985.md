First OCR the scoring parameters from `/app/scoring_rules.png` with `tesseract /app/scoring_rules.png stdout` (or `tesseract image.png output.txt`) and parse out the match reward, mismatch penalty, and (linear) gap penalty as signed integers — don't guess typical Needleman-Wunsch defaults, read the actual values from the image. Then implement standard global alignment DP in C++: an (n+1)x(m+1) score matrix where row 0 and column 0 are initialized as cumulative gap penalties (i*gap, j*gap), and each cell = max(diag + match_or_mismatch(a[i-1],b[j-1]), up + gap, left + gap); the final answer is the bottom-right cell. Read stdin line by line, split each line on the comma to get the two sequences, compute the score, and print just the integer — watch for trailing newline/whitespace handling and empty-sequence edge cases. Compile with `g++ -O3 -std=c++17 -o /home/user/aligner /home/user/aligner.cpp`, then run it on `/app/sample_pairs.txt` and save output to `/home/user/sample_scores.txt`, spot-checking a couple of alignment scores by hand against your extracted parameters before trusting the hidden-test accuracy.