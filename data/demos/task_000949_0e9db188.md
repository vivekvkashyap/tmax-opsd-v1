Debug this by comparing each computation against its statistically correct counterpart, not by guessing at syntax errors alone. Read the existing script fully before editing: identify (1) what statistic is actually being computed for "correlation" vs. what Pearson correlation requires — use `scipy.stats.pearsonr` on the full, undownsampled data for each engine, not covariance or a metric computed after downsampling; (2) whether the downsampling call passes a fixed `random_state=42` to `DataFrame.sample()` — irreproducibility almost always traces to an omitted or randomized seed; (3) whether the t-test call passes `equal_var=False` to `scipy.stats.ttest_ind` (Welch's), since the default assumes equal variances. Only downsample the larger dataframe to match the smaller one's row count, leaving the smaller untouched. Convert numpy scalar types to native Python floats before `json.dump` (numpy floats/int64 aren't natively JSON-serializable and silently succeed under some pandas versions but can break strict checks). Run the fixed script and inspect `metrics.json` to sanity-check magnitudes (correlations near the underlying linear relationship, a t-statistic and p-value consistent with a real effect) before finishing.