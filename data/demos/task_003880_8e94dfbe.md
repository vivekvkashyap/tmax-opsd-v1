Make the smallest possible diff to `worker.c`: replace the hardcoded `int threshold = 50;` line with logic that reads `getenv("ALERT_THRESHOLD")`, and if it's NULL uses default 80, otherwise parses it with `atoi` (or `strtol` for stricter validation) — do not touch the `printf` format strings, since the log format must stay byte-identical. Recompile with `gcc worker.c -o /home/user/worker`. Append `export ALERT_THRESHOLD=85` to `/home/user/.bashrc` (don't overwrite the file — append). For `run_monitor.sh`, remember that `source ~/.bashrc` inside a non-interactive script can be a no-op or error if `.bashrc` has an early "if not interactive, return" guard near the top — check for that and, if present, either strip/skip that guard or export the variable directly before sourcing, otherwise the threshold silently falls back to 80 and your CRITICAL lines won't match. Pipe worker's stdout through something like `grep '^CRITICAL:'` then `sed 's/^CRITICAL:/[ALERT]/'` (or an equivalent awk one-liner) redirected into `alerts.log`. `chmod +x run_monitor.sh` and actually execute it once to produce the final `alerts.log`, then diff it against the expected four ALERT lines mentally to confirm formatting matches exactly.
