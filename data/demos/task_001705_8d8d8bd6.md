Write the C parser as a straight sequential state machine over the packed binary records — do not try to mmap the whole struct layout at once since fields aren't padded/aligned. For each record: read the 8-byte LE u64 timestamp, 1-byte encoding, 2-byte LE u16 length, then the payload; for encoding==1 (UTF-16LE with ASCII-only content) convert by dropping every second byte rather than doing a real UTF-16 decode. Track the most-recently-accepted message string to implement adjacent-duplicate dedup (compare only against the last accepted record, not all previous ones). Derive Event_Type by scanning for the first space (or end of string). For resampling, first pass over records to get T_start from the very first record's timestamp, then walk fixed 60-second buckets from T_start through the bucket containing the last record; for each bucket, emit the first accepted record falling in it (with bucket-start timestamp) or a `GAP,MISSING` row if none. Write the pipeline script to `gcc` compile then run the binary, and keep the crontab line to the standard 5-field `*/5 * * * *` syntax pointing at the script's absolute path.