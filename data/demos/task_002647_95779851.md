Work this as a chained decode pipeline, verifying each stage before moving to the next. First extract frames from `/app/intercept.mp4` at a reasonably high rate (e.g. `ffmpeg -i intercept.mp4 -vf fps=5 frame_%04d.png`) and scan them with `zbarimg` (or similar) until you find the frame containing the QR code — don't assume a single frame number, iterate over all extracted frames. The decoded QR text gives you a SHA-256 hash of a 4-digit PIN and a base64 ciphertext; recover the PIN by brute-forcing all 10000 four-digit combinations (`0000`-`9999`), hashing each with `sha256sum` or a script, and comparing to the given hash — don't guess a PIN. Once found, decrypt with `openssl enc -d -aes-256-cbc -pbkdf2 -a -salt -pass pass:<pin> -in <ciphertext_file>` to get the `PORT=...;TOKEN=...;PAYLOAD=...` string, then parse those three fields (e.g. with shell parameter expansion or a small script). Finally write a minimal HTTP server (Python's `http.server` with a custom handler, or Flask) binding `0.0.0.0:<PORT>`, implementing `GET /download` that checks the `X-Auth` header against the extracted token exactly, returning 200 + payload body on match and 403 otherwise, and leave it running in the background (e.g. `nohup ... &`) so the verifier can hit it.