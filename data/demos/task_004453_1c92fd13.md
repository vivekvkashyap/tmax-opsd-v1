Reverse-engineer the oracle's formula empirically before writing code: run it on small, hand-chosen inputs (e.g., identical chains to force W→0, chains with known means/variances) and compare against manually computed candidate statistics to pin down the exact convergence diagnostic variant in use — there are several common formulations (with/without the (N-1)/N weighting, different denominators for between-chain variance B), so don't assume the textbook Gelman-Rubin formula is exactly what's implemented. Once you've matched several test cases bit-for-bit, implement it in C++: compute per-chain means, the overall mean, between-chain variance B (scaled by N and divided by M-1), within-chain variance W (average of each chain's sample variance using N-1 denominator), then V_hat as a weighted combination of W and B, and R_hat = sqrt(V_hat/W) with a guard for W near zero. Match the output formatting exactly — `fixed`/`setprecision(6)` with a trailing newline via `std::cout << fixed << setprecision(6) << R_hat << endl`. Test edge cases like very small W to confirm your zero-guard threshold matches the oracle's behavior, since the fuzzer will include such cases.