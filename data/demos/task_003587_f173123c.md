Build this as a multi-pass pipeline, not a single loop, since dedup depends on all files first being normalized. Pass 1: for each file in the directories listed in `config.yaml`, open it, `fcntl.flock(fd, fcntl.LOCK_EX)` before reading, detect its encoding (e.g. via `chardet`/BOM sniffing, don't assume UTF-8), decode it, and if it's CSV convert to a JSON array of objects keyed by the header row using `csv.DictReader`, writing a new `.json` file and deleting the original `.csv`; if it's already JSON just re-save as UTF-8. Release the lock (or let the `with` context do it) before moving to the next file. Pass 2: after all files are normalized, hash or directly compare the final content of every `.json` file to find exact duplicate groups; for each group, sort paths alphabetically, keep the first untouched, and replace every other member with `os.link(kept_path, dup_path)` after removing the original (a hard link, not a copy or symlink — verify with matching inode via `os.stat().st_ino`). Track counts of files processed, CSV conversions, and links created as you go, then dump them into `optimizer_report.json` in the exact required shape.
