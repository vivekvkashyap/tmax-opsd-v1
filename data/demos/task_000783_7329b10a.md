Build this as a streaming C validator plus a bash orchestrator that fans work out by region and joins with `wait`. For `validate.c`: read the input CSV line-by-line with `getline`/`fgets` (never load the whole file into memory), split each line on commas into exactly 6 fields, and check: field 1 parses as an integer strictly greater than 1600000000 (reject non-numeric or malformed values), field 2 (user) is non-empty, field 3 (region) is exactly 2 characters, field 5 (action) is exactly `ADD`, `MOD`, or `DEL` — write the whole original line to the valid or invalid output file accordingly. Compile with `gcc -O3 -o /home/user/validate /home/user/validate.c` inside `pipeline.sh`. For the split phase, use `awk -F, '{print > ("split_" $3 ".csv")}'` (or similar) to fan out by region into temp files. For phase 3, launch `./validate split_X.csv valid_X.csv invalid_X.csv &` for each region file in a loop, then call a single `wait` after the loop so all finish before phase 4. Concatenate all `valid_*.csv` into `processed_valid.csv` and all `invalid_*.csv` into `processed_invalid.csv`, count lines with `wc -l`, and write `stats.txt` in the exact `Total Valid: N` / `Total Invalid: N` format. Make the script executable and run it end-to-end, then verify counts manually against the source CSV.
