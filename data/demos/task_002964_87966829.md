Read `/home/user/dropper.go` carefully to find the race: it splits `payload.dat`'s decoded bytes into fixed-size chunks, XOR-decodes each chunk concurrently in a goroutine, and stores results in a `map[int][]byte` keyed by start offset — then reassembles the output by ranging over that map. Go's map iteration order is randomized by design, so `for _, chunk := range chunks { out = append(out, chunk...) }` appends chunks in random order every run, explaining the "changes randomly every time" symptom. Fix it deterministically: after `wg.Wait()`, collect the map's keys, sort them numerically (ascending offset), and append chunks in that sorted key order — don't rely on channel ordering or removing the goroutines (both work, but explicit key-sort is the minimal fix that preserves the concurrent-decode structure). Rerun `go run dropper.go` repeatedly to confirm `/home/user/decoded.dat` is now byte-identical across runs, inspect the decoded text (`cat` or `strings`), then extract the IPv4 address (dotted-quad pattern, e.g. via `grep -oE '[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+'`) mentioned in it and write only that address, no trailing newline or whitespace, to `/home/user/result.txt` (use `printf` not `echo` to avoid a trailing newline).