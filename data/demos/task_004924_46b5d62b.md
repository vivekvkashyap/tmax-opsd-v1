Build this as a straightforward pandas (or pure-csv) pipeline in clear stages, and print intermediate counts while developing so you can sanity-check against the raw CSVs. Load `activity.csv`, apply the validity filter (duration >= 0 AND action non-empty/non-null) while tallying dropped rows separately — do this filter before any join so the drop count reflects only activity-level invalidity, not join mismatches. Inner-join the valid rows to `users.csv` on `user_id` (rows with no matching user simply vanish per inner-join semantics — don't double count them as "invalid"). Parse the ISO8601 timestamp with a robust parser (e.g. `datetime.fromisoformat` after stripping a trailing `Z`, or `pandas.to_datetime`) and truncate to `YYYY-MM-DD`. Group by (date, country, tier) and aggregate sum(duration) and count(actions); watch numeric types — keep integers as integers in the JSON rather than floats. Sort the output list by date, then country, then tier before serializing with `json.dump`, and create `/home/user/output/` with `os.makedirs(exist_ok=True)` before writing either file. Finally actually execute the script so the two JSON files exist on disk.