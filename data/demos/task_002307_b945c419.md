Build this as a straightforward multi-format ETL: iterate every file in `/home/user/raw_configs/`, branch on extension (`.json` -> `json.load`, list of dicts; `.csv` -> `csv.DictReader`), and map each record's fields per the given schema table into a single normalized dict with exactly `server`, `timestamp`, `key`, `value`. Coerce `value` with `str(...)` unconditionally (so ints become strings). For timestamps, try parsing with `datetime.strptime` against both known input formats (`%Y-%m-%dT%H:%M:%SZ` and `%Y/%m/%d %H:%M:%S`) and re-emit in the first format — don't assume only one format appears. Count total raw records read (N) before deduplication. Deduplicate using a set of the 4-tuple (server, timestamp, key, value) — insertion order into the set doesn't matter since you sort afterward. Sort the final list with a compound key `(timestamp, server, key)`. Write the JSON array, then append (not overwrite) the exact log line to `/home/user/pipeline.log` with the real N and M counts you computed, not hardcoded numbers.
