Use an OCR tool (e.g. `tesseract /app/assay_protocol.png`) to read off the exact `TARGET_SEQUENCE`, `MC_ITERATIONS`, and `TEMP_THRESHOLD` printed in the screenshot — don't guess or approximate the sequence, copy it character-for-character since exact substring matching depends on it. Then build `/home/user/primer_filter.py` with argparse flags `--input --output --target --mc-iters --thresh`. For each FASTA record: (a) reject if the primer sequence is not an exact substring of `--target`; (b) otherwise run the Monte Carlo self-dimer simulation for `--mc-iters` iterations — each iteration pick a random overlap length uniformly in [4, len(primer)], align the primer against itself with that overlap, score complementary base pairs (A-T, C-G count as 1) over the overlapping region, divide by primer length, and average this fraction across iterations; reject if the average is >= `--thresh`. Only primers passing both checks are written to the output FASTA, preserving standard FASTA format. Validate against the given `/app/corpora/clean/` and `/app/corpora/evil/` directories, iterating on your dimer-scoring logic (especially how you define "complementary" for overlapping offsets) until you get 100% correct accept/reject on both corpora before considering the task done.
