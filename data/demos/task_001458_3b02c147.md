Fix the two bugs independently, verifying each in isolation before running the full pipeline. First run `python3 /app/setup_test_db.py` to materialize `/home/user/backup.db`. Second, open the vendored `Makefile` and check whether the `validate` target actually forwards the `DB_PATH` make variable to the python invocation (e.g. via `--db $(DB_PATH)`) rather than a hardcoded path — fix it to pass through both a `--db` argument sourced from `$(DB_PATH)` and an output path. Third, inspect the SQL in `validator.py`: if it lists multiple tables in the `FROM` clause separated by commas but only constrains some of the join keys in `WHERE`, that's an implicit cross join — every unconstrained table multiplies the row count combinatorially, which both inflates aggregates and can make the query hang on non-trivial data. Rewrite it using explicit `JOIN ... ON` clauses that properly connect customers to orders (by customer_id) and orders to items (by order_id), aggregating `SUM(amount)` and `COUNT(item_id)` per customer with `GROUP BY`. Run `make validate DB_PATH=/home/user/backup.db` and confirm it finishes quickly and produces plausible (not wildly large) numbers before considering it done — sanity-check a couple of customers' totals by hand-querying the DB with `sqlite3` to build confidence the join is correct.
