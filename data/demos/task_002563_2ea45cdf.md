Work in three passes. First, run `tesseract /app/evidence.png -` (or similar OCR invocation) to extract the SYNC_SEED multiplier text from the image — read whatever numeric value OCR actually recovers, don't guess it. Second, open `/home/user/timeline.go` and find the custom sort routine: look for a recursive quicksort/mergesort whose partition or recursion base case is off-by-one (e.g. recursing on the full slice instead of a strictly smaller sub-slice), causing infinite recursion/stack overflow on small or already-sorted inputs — fix the base case so it always shrinks the problem. Also find where epoch timestamps like `seconds.microseconds` are parsed and summed using `float32` (or repeated float addition) — replace that with `float64` arithmetic or Go's `time.Time`/integer-nanosecond math to eliminate cumulative drift when applying timezone offsets. Third, write `/home/user/detector.go` reusing the corrected parsing/sorting logic: for each consecutive pair of corrected timestamps, multiply the drift by the extracted SYNC_SEED and flag `EVIL` if the corrected sequence ever moves backward by more than 500ms, otherwise print `CLEAN`. Test against every file in `/app/corpus/clean/` and `/app/corpus/evil/` before finishing — all must classify correctly.