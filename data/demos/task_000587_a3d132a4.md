Treat this as a straightforward multi-variable least-squares fit, not an approximate/iterative guess if you can avoid it: read the CSV in your C++ program, build the normal-equations matrix X^T X and vector X^T y for the three features (CPU, Mem, IO) with no intercept term (since the model has no bias), then solve the resulting 3x3 linear system directly (Cramer's rule or Gaussian elimination) rather than relying on gradient descent, which risks not converging exactly and failing the exact-match check on `/home/user/weights.txt`. Print the weights with `std::fixed << std::setprecision(2)` in the exact `CPU=X.XX MEM=Y.YY IO=Z.ZZ` format with a single space between fields and no trailing newline issues. Compile with `g++ -O2 -o optimizer optimizer.cpp` and run it to generate `weights.txt`. For `visualize.sh`, parse the CSV with `awk`, read the three weights out of `weights.txt` (e.g. via `grep -oP` or `cut`), compute `predicted = w_cpu*CPU + w_mem*Mem + w_io*IO` per row with `awk`'s printf `%.2f`, and print `*` characters using `floor(Actual/10)` via a loop or `printf '%.0s*' $(seq 1 N)`. Make the script executable with `chmod +x`.
