Reconstruct the transaction timeline by parsing all three logs into a common structure (transaction_id, timestamp, value), concatenating them, then sorting each transaction's entries by timestamp to recover the original input order — don't assume the logs are individually complete or pre-sorted, since the values are deliberately fragmented and interleaved across the three files. Write the reconstructed sequences to `/home/user/reconstructed_inputs.csv` in the required wide format. To find the root cause, feed a reconstructed sequence into `/app/legacy_aggregator` via stdin and compare its printed sum against a high-precision sum computed independently (e.g. Python's built-in float/Decimal sum of the same values); a large discrepancy combined with the data containing both very large and very many very small values points to accumulation error from limited-precision (float32-style) summation, where repeatedly adding tiny numbers to a much larger running total loses low-order bits. The standard fix without touching the binary is to reorder the values before piping them in — sort by absolute magnitude ascending so small values accumulate together before being added to large ones, which substantially reduces cancellation/rounding error. Implement `/home/user/safe_aggregator.sh` to read the input file, sort by absolute value, pipe the reordered sequence into the legacy binary, and print only its output. Test it against a synthetic skewed dataset (one huge value plus many tiny ones) and compare against a Python-computed high-precision sum to confirm the error is now small before finishing.