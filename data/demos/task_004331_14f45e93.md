For the C++ monitor: iterate `/proc/<pid>/comm` (or parse `/proc/<pid>/stat`) for every numeric entry in `/proc`, collect matches against the given process name, and pick the numerically lowest PID — don't rely on directory listing order. Open the log file in append mode, write the exact status line, then close it and reopen/count lines to decide rotation (do line-counting as a separate pass after the write, not during). Apply `chmod(path, 0600)` via `chmod()`/`fchmod()` in the C++ code itself after writing, not via a shell command outside the program — the spec requires it done "programmatically." Handle rotation with `rename()`/`std::filesystem::rename`, overwriting any prior `.1` file. Test by launching a long-running dummy process (e.g. `sleep 120 &`), running `./monitor sleep`, and checking the log line and permissions before moving on. For the crontab line and fstab line, match the exact field syntax requested (five cron fields plus command; six fstab fields) rather than approximating.
