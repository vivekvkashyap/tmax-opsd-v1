First derive the analytic gradient of f(x) = x^4 - 3x^3 + 2 yourself (df/dx = 4x^3 - 9x^2) and confirm where it equals zero — that root is the stable fixed point gradient descent should settle at. Edit the Go file to insert gradient clipping right where the gradient is computed, before the update step: compute `grad`, then clamp it into [-5.0, 5.0] (e.g. `if grad > 5.0 { grad = 5.0 } else if grad < -5.0 { grad = -5.0 }`), then apply `x = x - lr*grad` as before. Leave the initial x=4.0, lr=0.1, and 1000 iterations untouched — the task only wants the clip added. Compile and run with `go run test_stability.go` (or build then execute) and read the printed final x value. Round that value to exactly two decimal places yourself using normal rounding rules, and write only that number (no extra text, no trailing label) to /home/user/solution.txt. Watch for the common naive mistake: clipping the update term (`lr*grad`) instead of the raw gradient produces a different trajectory — clip `grad` itself as specified.