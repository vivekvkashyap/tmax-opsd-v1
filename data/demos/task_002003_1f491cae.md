Start by extracting the spec from the image with OCR (`tesseract /app/deployment_spec.png stdout`, falling back to viewing the image directly if OCR is noisy) to recover the exact port, endpoint path, and target-ms threshold — use whatever values you actually read, don't assume defaults. For the C++ side, fix the Makefile by adding `-fPIC` to compilation and `-shared` (plus `-O3`) to the link step so it produces a proper `.so`; for the performance bottleneck, look for an obviously redundant inner loop (e.g. an inner loop that recomputes the same accumulation thousands of times per element without changing the result) and simplify it while keeping the mathematically identical output — verify by comparing outputs on a few inputs before/after your change. For the Go side, use `cgo` with `#cgo LDFLAGS: -L<path> -lcompute` and `import "C"` to call into the shared library, set `LD_LIBRARY_PATH` before running so the dynamic loader finds `libcompute.so`, and use a goroutine-safe handler (each request spawning its own C call is fine since the C function is stateless) for concurrency. Start the service, write `/home/user/status.txt` containing READY only once it's actually accepting connections, then load-test locally yourself against the extracted target before declaring done.
