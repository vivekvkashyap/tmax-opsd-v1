Read `det_calc.c` carefully to locate the two flagged bugs rather than rewriting from scratch: the row-swap block only swaps the row pointers but never negates `det`, so add `det = -det;` right after the swap inside `if (pivot != i)`. The memory leak is the missing cleanup — before `return 0;`, add a loop freeing each `matrix[i]` followed by freeing `matrix` itself. For the Makefile, compile with static linking and math library explicitly (`gcc -static -o det_calc det_calc.c -lm`) since the container is minimal and dynamic linking will fail there; verify afterwards with `file det_calc` that it reports "statically linked". Finally run `python3 verify_props.py` yourself first to confirm it passes (it uses `hypothesis` to fuzz-test the multiplicative determinant property and runs `valgrind --leak-check=full --error-exitcode=1` to confirm the leak is truly gone) — only a clean pass writes `SUCCESS` to `verification.log`. If valgrind still reports leaks, double check every `malloc` has a matching `free` on every code path, not just the success path.
