Do Stage 1 first and in isolation: transcribe `/app/voicemail.wav` (e.g. with `whisper-cli` or any available speech-to-text tool) to recover the spoken `INC-XXXX` incident ID — treat this as data you must extract from the audio, not something to guess. For Stage 2, the core challenge is correctly distinguishing a properly RFC4180-quoted embedded newline (an odd number of unescaped double-quotes opened before that newline, i.e. still "inside a quoted field") from an injected bare newline used to forge a fake row — a line-based `grep`/`sed` approach will silently break on real quoted newlines, so instead track quote parity across the stream (e.g. an `awk` program that accumulates a record buffer and counts `"` characters per line, only emitting/finalizing a record once the running quote count is even), and reject any record that doesn't end up with exactly 4 well-formed CSV fields. Test `/home/user/detector.sh` against both `/app/corpus/clean/` and `/app/corpus/evil/` samples if present, confirming clean rows pass through unchanged and evil rows are dropped, before trusting it on the real data. For Stage 3, pipe `/app/raw_logs.csv` through the detector, `grep` for the discovered incident ID, then use `awk` to forward-fill empty column-3 values from the last non-empty value seen and maintain a sliding window of up to the last 3 response_times to compute the rolling average, printing with `printf "%.2f"`. Make `/home/user/detector.sh` executable.
