Connect to the SQLite DB with sqlite3 and issue a recursive CTE that starts from the row(s) where manager_id IS NULL at depth 0, then repeatedly joins employees to the growing hierarchy on manager_id = parent emp_id, incrementing depth by 1 each level — e.g. `WITH RECURSIVE hierarchy(emp_id, name, depth) AS (SELECT emp_id, name, 0 FROM employees WHERE manager_id IS NULL UNION ALL SELECT e.emp_id, e.name, h.depth+1 FROM employees e JOIN hierarchy h ON e.manager_id = h.emp_id) SELECT name FROM hierarchy WHERE depth = ?`. Bind the target depth as a query parameter (using `?` placeholders, not string interpolation) to keep it parameterized/injection-safe. Read the depth from sys.argv[1] as an int. Sort the resulting employee names alphabetically before building the output dict `{"target_depth": depth, "employees": [...]}` and writing it with json.dump to `/home/user/output_depth_<depth>.json`. Finally invoke the script with argument 2 to produce the required output file.
