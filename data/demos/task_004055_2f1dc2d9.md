Build and verify this bottom-up: get the C++ TCP server correct and log-writing correct before wiring up systemd/symlinks/scripts. In the C++ program, read `MIGRATION_PORT`/`MIGRATION_LOG` via `getenv`, create a socket, `bind`/`listen` on `127.0.0.1:<port>`, and only after `listen()` succeeds append `"SERVICE_UP\n"` to the log file (open in append mode) — then loop forever (e.g. `accept()` in a loop, or a `while(true) sleep()`) so the process doesn't exit and the port stays bound. Compile to `/home/user/app/bin/migrator`, then create the symlink with `ln -s /home/user/app/bin/migrator /home/user/app/active/migrator` (verify with `readlink`). For the systemd unit, just write a plain `.ini`-style file by hand with `[Service]`, `ExecStart=...`, and two separate `Environment=KEY=VALUE` lines (systemd requires one `Environment=` per variable or space-separated on one line — use separate lines to be safe) — you don't need systemd actually running since a fallback shell script is required anyway. In `start.sh`, export the two env vars, launch the symlinked binary with `&`, capture `$!` immediately into the PID file, and `chmod +x` the script. Finally, actually execute `start.sh` and confirm with `ss -tln | grep 9090` that the port is listening before finishing.