Load both CSVs with `numpy.loadtxt`, then fit `PCA(n_components=2, random_state=42)` only on `outputs_run1` with `.fit_transform`, and reuse that same fitted model's `.transform` on `outputs_run2` — never fit a second PCA on run2, since the point is measuring reproducibility in a shared basis. Compute the covariance of the run1-reduced array with `np.cov(reduced1, rowvar=False)` and sum its diagonal with `np.trace`. Compute the Pearson correlation between the first columns (PC1) of the two reduced arrays with `np.corrcoef(reduced1[:,0], reduced2[:,0])[0,1]`. Format both numbers to exactly 4 decimal places with Python string formatting (`f"{value:.4f}"`), not raw `round()`, so trailing zeros are preserved, and write them to `/home/user/report.txt` matching the required two-line label format exactly (including the literal text before each value). Run the script once and diff the output file against the spec to confirm formatting matches character-for-character.
