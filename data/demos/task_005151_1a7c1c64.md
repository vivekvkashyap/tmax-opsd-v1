Build the pipeline in clear stages inside `/home/user/etl_pipeline.py`. Load both CSVs (pandas or csv module), then construct a directed weighted graph with `networkx.DiGraph()` where each distinct `(sender_id, receiver_id)` pair becomes one edge whose weight is the count of matching rows in `messages.csv` (aggregate via groupby before adding edges, don't add duplicate parallel edges). Compute influencer scores with `nx.pagerank(G, alpha=0.85, weight='weight')`, and make sure every employee from `employees.csv` appears in the output even if they have zero edges (add isolated nodes to the graph first, or default their score appropriately) since the influencers table must join cleanly against all employee metadata. For triangles, don't reuse `nx.triangles` (that's for undirected graphs) — instead explicitly search for directed 3-cycles: for each edge A->B, for each edge B->C, check if edge C->A exists; normalize/dedupe each found triangle by sorting the three ids ascending (`emp_a<emp_b<emp_c`) into a set before final output. Write both `influencers` and `triangles` tables to a SQLite DB at `/home/user/company_analytics.db` via `sqlite3`, matching the specified column names/types, then run the script to confirm the DB is populated correctly.