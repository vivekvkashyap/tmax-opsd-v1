Build this as a small pipeline of Unix tools chained together, testing each stage's output before piping into the next. Stage 1: use `jq` to filter `/home/user/slow_queries.json` for objects where `exec_time_ms` is strictly greater than 1000, and extract just the `qid` values as a plain list (e.g. `jq -r '.[] | select(.exec_time_ms > 1000) | .qid'`) — verify this list by eyeballing the input file first. Stage 2: use that qid list to filter `/home/user/table_locks.csv` down to only rows whose `query_id` is in the slow set (e.g. with `grep -F -f` against a file of slow qids, or `awk` with an associative array built from the qid list), keeping only the `table_name` column and deduplicating per (query_id, table_name) pair so a query locking a table via multiple lock types isn't double-counted. Stage 3: count occurrences per table with `sort | uniq -c`, then sort by count descending and table name ascending as tiebreaker (`sort -k1,1nr -k2,2`) to pick the top table deterministically. Write only that table name (no extra whitespace/newline issues) to `/home/user/bottleneck_table.txt`. Sanity-check your final answer by manually tracing which slow queries touch which tables from the source files.