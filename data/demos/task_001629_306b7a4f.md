Treat this as two independent deliverables and validate each in isolation before wiring end-to-end. For the telemetry stack, trace the request path manually: check what port/upstream nginx.conf currently proxies to under `/api/` (likely wrong, e.g. port 80) and correct it to point at the Flask app's actual port 5000; check the Flask app's Redis client config and ensure `.env` sets the host/port Flask actually reads (e.g. `REDIS_HOST=127.0.0.1`, `REDIS_PORT=6379`); make sure `start.sh` actually launches redis-server, the Flask app, and nginx in the right order/backgrounded. Verify with a manual `curl -X POST http://localhost:8080/api/record` and check Redis directly (`redis-cli`) before moving on. For the cleanup script, implement the "wasteful" test as: parse the date embedded in the filename (regex on `YYYY-MM-DD`) and compare against 2024-05-15 minus 30 days, OR grep the file content for `DEBUG_LEVEL=`; but always check for `AUDIT_TRAIL=true` first and short-circuit to "keep" if found, since compliance overrides both other conditions — get this precedence right or you'll fail the adversarial corpus. Test your script directly against files you construct that hit each rule combination before running it for real. Finally add the cron line via `crontab -l | { cat; echo "0 2 * * * ..."; } | crontab -`.
