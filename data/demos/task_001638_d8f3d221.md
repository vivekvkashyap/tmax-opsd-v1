Work this as three independent, verifiable stages rather than one monolithic script. First, transcribe `/app/pi_instructions.wav` (install/use `whisper` or another ASR tool available in the environment) to recover the graph metric name, the bootstrap resample count, and the confidence level — do not guess these, read them off the transcript text you produce. Second, build `/home/user/classifier.py` as a pure function of valency rules: parse `atoms` and `bonds`, compute each atom's degree by counting bonds referencing its id, and reject (exit 1) if any atom's degree exceeds its element's max (H:1, O:2, C:4, N:3); accept (exit 0) otherwise. Test it against edge cases (isolated atoms, atoms at exactly the limit) since it will be run against a strict adversarial clean/evil corpus at `/app/verifier_corpus/`. Third, in the notebook, walk `/app/corpus/` recursively, shell out to your classifier to filter clean files, compute the transcribed metric per graph with networkx, then bootstrap the mean using the exact resample count and confidence level from the audio (e.g. resample with replacement N times, take the percentile interval), and write `metric`, `ci_lower`, `ci_upper`, `num_clean_graphs` to `/home/user/final_result.json`.
