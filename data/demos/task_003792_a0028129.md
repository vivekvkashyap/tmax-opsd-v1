This task has two independent deliverables — solve them separately and verify each in isolation. For Part 1, first read `/app/auth-redirect-v1.0.0/auth.py` to locate the host-extraction function that splits on `:`; a common backport bug is returning the wrong half of the split (e.g. the port instead of the host) when a port is present — trace through both the "host:port" and bare "host" cases by hand before fixing the indexing. Then inspect the `Makefile`'s `test` target: if it invokes pytest without properly invoking the interpreter/module (e.g. missing `python3 -m` or PATH setup), fix the invocation so `make test` actually runs and exits 0 — run it yourself to confirm. For Part 2, build `/home/user/validate_redirect` as a pure, deterministic URL classifier: use a real URL-parsing library (don't hand-roll regex string splitting) to distinguish relative paths (single leading `/`, reject `//...`) from absolute URLs; for absolute URLs enforce scheme==https and hostname equal to or a proper subdomain of the allowed base domain (careful: "notexample.com" must NOT match "example.com" — check for an exact match or that the hostname ends with "." + base domain); also reject any path containing `../` or its percent-encoded form case-insensitively. Since this is fuzz-tested against a hidden oracle, enumerate edge cases yourself (malformed URLs, missing scheme, encoded traversal, mixed case) and make sure parse failures always yield `INVALID` rather than throwing. Mark the script executable.