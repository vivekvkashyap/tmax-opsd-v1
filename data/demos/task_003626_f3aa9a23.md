Work the pipeline in the order the tasks are listed, validating each stage before moving on. First recover the deleted file from `/app/data.img`: mount it (or use `debugfs -R "ls -d" /app/data.img` to enumerate deleted directory entries and find the freed inode for `latency_data.log`), then extract its contents with `debugfs -R "dump <inode> /home/user/recovered_latency.log" /app/data.img`. Second, clean the recovered log: its lines mix delimiters (commas, pipes, spaces) and trailing garbage, so use a permissive regex (e.g. `grep -oE` for a float pattern per line, or `awk`/`sed` normalizing all delimiters to one) to extract just the numeric latency values into a clean one-number-per-line file. Third, pipe that cleaned data into `/app/variance_calc` and observe it returning a wrong/negative variance — this confirms catastrophic cancellation from a naive sum-of-squares formula on large clustered values, don't try to fix the binary. Fourth, write `/home/user/stable_variance.sh` implementing a numerically stable variance (subtract the mean before squaring, or Welford's running-update algorithm) in awk/bash, printing only the sample variance to 4 decimal places. Finally write `/home/user/regression.sh` that generates a small large-magnitude/low-variance dataset, runs it through both `/app/variance_calc` and your script, and exits 0 only when your script's result differs from (and is more accurate than) the naive binary's.
