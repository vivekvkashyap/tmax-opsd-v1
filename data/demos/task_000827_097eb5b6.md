Work the three subtasks in order since each feeds the next. First, extract the payload signature from the memory dump: don't try to parse it as structured memory — just run `strings /app/dumps/crash.bin | grep -i` for suspicious keywords (e.g. "flow", "trigger", "malformed") to locate the human-readable signature string embedded in the noise; note its exact bytes for later use in the detector. Second, fix the vendored dpkt: grep the vendored source under `/app/vendored/dpkt-*` for `nan` or `float(` to find the deliberately-injected line that overwrites a parsed integer field (likely a checksum/length in the IP header parser) with `float('nan')`; delete that injected line so the field keeps its normal integer value, then `pip install -e /app/vendored/dpkt-<version>` (or equivalent) so the fix is actually the one Python imports — verify with `python3 -c "import dpkt; print(dpkt.__file__)"`. Third, write `/home/user/detector.py` using the fixed dpkt to iterate packets in a pcap and flag any whose payload bytes contain the signature string you extracted; print EVIL/exit 1 or CLEAN/exit 0 accordingly. Test against a few self-made clean/evil pcaps before assuming correctness, since the real corpora are hidden.