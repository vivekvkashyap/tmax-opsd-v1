Diagnose by compiling first (`go build`) to surface the actual compile errors, then fix them one at a time rather than guessing. The core logic bug is that `isGreater` does a raw string comparison of version strings, which breaks on cases like "1.10.0" vs "1.2.0" — replace it with real SemVer comparison: split each version on `.`, parse each component with `strconv.Atoi`, and compare major, then minor, then patch numerically in order (return as soon as one differs). Also fix the missing `:` in the `ListenAndServe` port argument (must be `":8080"`, not `"8080"`), and after building the `updates` slice, sort it with `sort.Slice` comparing `updates[i].Package < updates[j].Package` before encoding — the current code has no sort at all. After fixing, run the server in the background, `curl -s http://localhost:8080/updates > /home/user/updates.json`, and inspect the output to confirm it's alphabetically ordered and only includes packages where wanted > current numerically, then stop the server.