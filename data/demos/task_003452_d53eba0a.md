Diagnose before writing any code. First find where the backend actually creates its Unix socket: read `/home/user/backend/server.sh` to see the exact socket path it binds (likely under `/home/user/run/`), then compare that against what `/home/user/proxy/config.env` currently points to via `/home/user/proxy/health_check.sh` — fix the mismatch in `config.env` so the two paths agree. Test the health check manually (start the backend, then run `health_check.sh`) to confirm it now reports success before building the rest. For the storage monitor, write a simple `while true; do ... ; sleep 2; done` loop in bash that uses `stat -c%s` (or equivalent) to check `/home/user/logs/backend.log`'s size, and truncate it in place with `: > file` or `truncate -s 0 file` (not `rm`, which would break the backend's open file handle) whenever it exceeds 50000 bytes. For `deploy.sh`, background the backend and monitor with `&`, `sleep 5` to let them initialize, then run `health_check.sh` with output redirected via `>` to `deployment_result.log`. Make both scripts executable, run `deploy.sh`, and leave the backgrounded processes running rather than letting the deploy script exit and reap them.
