Write process_artifacts.sh to read artifact_manifest.csv line by line (e.g. `while IFS=, read -r name size mode`), and for each row: create the fixture with `truncate -s "$size" "/home/user/mock_artifacts/$name"` then `chmod "$mode" "..."` (creating the mock_artifacts dir first with `mkdir -p`). For scoring, compute `DecimalMode` by converting the octal permission string to base-10 — in bash use arithmetic context `$((8#$mode))` (e.g. `$((8#0644))`) rather than trying to parse digits manually, since that correctly interprets leading-zero octal literals. Then compute the score with bash's built-in XOR and modulo: `score=$(( (size ^ decimal_mode) * 13 % 97 ))` — watch operator precedence, XOR before multiply before modulo, or just parenthesize explicitly as the formula states. Accumulate results and emit compact JSON array output (build it manually with printf/string concatenation, comma-joining `{"artifact":"...","score":N}` entries, or pipe through `jq -c -n` if available) to security_report.json. Make the script executable and run it once so both output artifacts exist before finishing; spot-check one or two computed scores by hand against the manifest values.