Determine the valid client cert programmatically rather than guessing: for each of the three client certs, run `openssl verify -CAfile /home/user/ca.crt <clientN>.crt` and pick the one that reports `OK` (the others will be self-signed or signed by a different CA and fail verification). Then connect with the winning cert/key pair and mTLS, e.g. `curl --cert clientN.crt --key clientN.key --cacert /home/user/ca.crt https://localhost:8443/payload`, and base64-decode the response body (`base64 -d`). Parse the decoded `IP:...;PORT:...;FLAG:...` string with a simple split on `;` and `:` to pull out the three fields — do not hardcode any of them, since they must come from the live decode. Write the recovered flag value (only) to `/home/user/flag.txt`. Build `/home/user/block_vuln.sh` containing exactly one `iptables -A OUTPUT -p tcp -d <recovered-ip> --dport <recovered-port> -j DROP` line using the IP/port you actually extracted — you don't need root to run it, just create the file with correct content.
