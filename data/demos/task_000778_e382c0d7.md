Work top to bottom, verifying each stage's output before moving to the next. Import `raw_logs.csv` into SQLite (`sqlite3 /home/user/logs.db` with `.mode csv` + `.import`, or Python's `csv`/`sqlite3` modules) into a table named exactly `access_logs`. In Python, run a GROUP BY `user_id` query (or use pandas `.groupby`) computing `COUNT(*)` as total_requests, `COUNT(DISTINCT source_ip)` as unique_ips, and `AVG(bytes_sent)` as avg_bytes — keep the three features in that exact order per user. For each user's vector, compute cosine similarity against the fixed baseline `[50, 1, 2500]` using `dot(v, baseline) / (norm(v) * norm(baseline))`; don't normalize baseline separately from the vector or you'll get a wrong denominator. Sort ascending by similarity (lowest = most anomalous), take the bottom/lowest 5, round the score to 4 decimals, and write them to `/home/user/top_anomalies.csv` with the exact column names and ascending sort order specified. Finally render `/home/user/anomaly_report.html` with Jinja2, iterating the same top-5 list and matching the literal `<li>User {user_id} with score {similarity_score}</li>` format exactly — check whitespace/formatting of the rendered score matches what you wrote to the CSV.