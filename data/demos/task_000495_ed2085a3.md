Solve this as two independent extraction steps that you then join, using plain shell text tools — no need for a real database. Step 1, build the foreign-key graph: for every CSV in `/home/user/etl_data/tables/`, read only the header line (`head -1`), split on commas, and for each column matching `<name>_id` record an edge from the current table to `<name>` (the table named exactly `<name>.csv`, ignoring self-references or references to non-existent tables). Step 2, compute in-degree per table as the count of distinct *other* tables with an edge pointing to it — a table referencing itself twice or the same target from two columns should still count as one. Step 3, extract scanned tables from `/home/user/etl_data/query_plan.txt` by grepping the pattern `Seq Scan on "<table>"` (use `grep -oP` or `sed` to pull the quoted name). Step 4, intersect the scanned-table set with your in-degree map, and pick the max in-degree; break ties alphabetically (`sort` handles this naturally if you sort by degree desc then name asc). Write only the bare table name (no quotes, no trailing whitespace) to `/home/user/bottleneck_analysis.txt`. Double check you're computing in-degree (who points AT this table), not out-degree.
