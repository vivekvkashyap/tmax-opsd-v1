Work through this in dependency order since later steps require earlier ones to be live. First, diff the port the backend actually binds (read `server.py`'s `HTTPServer((...), port)` call) against the `proxy_pass` target in `nginx.conf`, and correct the mismatch. Second, write `manage_auth.sh` to take `<user> <pass>` positional args and append an htpasswd-format line to `.htpasswd` — generate the hash with something like `openssl passwd -apr1 "$pass"` and write `"$user:$(openssl passwd -apr1 "$pass")"`, then invoke it once for `sysadmin`/`supersecurepass`. Third, add `auth_basic "Restricted Area";` and `auth_basic_user_file <path-to-.htpasswd>;` inside the `location /` block of `nginx.conf`. Fourth, start the Python backend in the background, then start nginx unprivileged with the exact `-c`/`-p` flags given (watch for nginx needing writable log/temp paths under the app_stack directory, already prepared, and for a stale pid file if nginx was started before). Finally write `test_connection.sh` to `curl -s -o /dev/null -w ...`-style capture both status and body with `-u sysadmin:supersecurepass`, and format the single output line exactly as `HTTP_STATUS: <code>, BODY: <body>` into `final_result.log`.