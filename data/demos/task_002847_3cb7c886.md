First reproduce the slowness: run the existing ingestion path against the CSVs and observe it either hang or take very long — this confirms the deadlock is real before you start editing. Search the `csv_graph_mapper` source for the concurrent-ingestion code path (likely a threaded/batched loader touching a SQLite connection) and look specifically for a per-row `commit()` call inside a loop that runs across multiple threads/connections — that pattern is what causes "database is locked" contention in SQLite. Fix it by batching: use `executemany()` for the insert and commit once per batch (or once for the whole transaction) instead of committing every row; also confirm you're not opening a separate SQLite connection per thread without proper `isolation_level`/WAL settings, which is the other common cause. After the fix, benchmark by timing your ingestion script directly (`time python3 ...`) to confirm it's well under the 3-second budget, not just "faster than before". Use the package's own recursive CTE query support to compute max dependency depth — don't hand-roll a Python BFS/DFS if the tool already provides SQL-based traversal, since that's what the task is asking you to fix and use. Validate node/edge counts against the CSVs before writing output.json.