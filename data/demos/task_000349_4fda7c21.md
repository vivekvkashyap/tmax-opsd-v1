Build and verify each layer in isolation before wiring them together with supervisord. First, OCR `/app/archived_record.png` (`tesseract`) and carefully read the extracted text to recover the exact `ADMIN_TOKEN` value used for auth — verify the string you extracted rather than guessing, since OCR can mis-scan alphanumeric tokens. Write `backend.py` accepting `port` and `token` as argv, binding only to `127.0.0.1:<port>`; implement `/status` (200 + `{"status":"ok"}`) and `/users` checking the `X-Admin-Token` header against the passed-in token (401 if missing/wrong, 200 + user list if correct); set `X-Backend-Port` on every response so the proxy's load-balancing can be verified externally. Test each backend instance directly with curl on 9001/9002 before touching nginx. Configure `nginx.conf` with an `upstream` block listing `127.0.0.1:9001` and `127.0.0.1:9002` (default round-robin), `listen 127.0.0.1:8080`, custom `access_log`/`error_log` paths, `pid /home/user/nginx.pid`, and `daemon off` — run nginx standalone first (`nginx -c ... -p ...`) to confirm it proxies correctly without root. Only then write `supervisord.conf` defining `backend1`, `backend2`, `proxy` as separate `[program:]` sections pointing at your script/binary with correct args, with supervisord's own logs/pidfile under `/home/user/`. Start it and curl port 8080 repeatedly to confirm round-robin alternation and auth behavior before declaring done.
