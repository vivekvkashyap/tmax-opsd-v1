For bootstrap sampling with replacement in pure bash, read the 100 data rows into an array (`mapfile -t rows < <(tail -n +2 dataset.csv)`), then loop 50 times picking `${rows[$RANDOM % ${#rows[@]}]}` and appending to the output — do not use `shuf` alone, since plain `shuf -n 50` samples *without* replacement and will violate the "with replacement" requirement. For 5-fold CV, since folds must be exact consecutive 20-row blocks (not random), use `sed -n` or `awk 'NR==a,NR==b'` on the data rows (post-header) to slice fold `i`'s 20 lines as test, and everything else (`sed` with an inverted range or concatenating the other four blocks) as the 80-row train set; write each to a temp file per iteration. Call `python3 model.py --train ... --test ... --pca`, capture stdout, and extract the number after "Accuracy: " with `grep -oP` or `cut`/`awk`. Accumulate the 5 values and average with `awk` or `bc -l`, formatting to exactly 2 decimals for `final_metric.txt`. Make the script executable and run it end-to-end once before finishing to confirm `bootstrap_sample.csv` has exactly 50 lines.
