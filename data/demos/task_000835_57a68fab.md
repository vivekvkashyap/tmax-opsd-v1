Parse `/home/user/logs/gateway.log` with a regex that captures the client IP, the request line's path+query, and the status code (standard combined/common log format: `IP - - [date] "METHOD /path?query HTTP/1.1" STATUS size`). For each line, only consider it a match if the path is `/login`, status is exactly `302`, and the `next` query parameter's value (use `urllib.parse.parse_qs` or a regex on `next=...` up to the next `&`/space, then unquote it) starts with `http://` or `https://` — relative paths and non-302 responses must be excluded even if they look suspicious. Collect matching IPs into a set (dedupe), sort lexicographically as plain strings (not by numeric octets), and write one `DenyUsers *@<IP>` line per IP to `/home/user/ssh_deny.conf`. For the CSP, read `/home/user/csp.txt`, split on `;`, strip each directive, and either replace the existing `form-action ...` directive's value with `'self'` in place or append `; form-action 'self'` if absent, then rejoin preserving the other directives' order and write to `/home/user/hardened_csp.txt`. Run the script yourself and diff your output against the expected shape before finishing.