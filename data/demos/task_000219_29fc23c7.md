Start by extracting the hidden rules from the audio before writing any code: transcribe `/app/incident_memo.wav` (e.g. `whisper /app/incident_memo.wav --model base --output_format txt`, or another local STT tool) and read the transcript carefully — it specifies the exact validation constraints (acyclicity, which node must be the root, and a max hop-count from the root) that your classifier must enforce; do not assume generic "DAG" rules, use the specific values stated in the memo. Then implement `/home/user/validate_backup.py` (chmod +x, proper shebang) to: parse the JSON's nodes/edges, build an adjacency structure, detect cycles (e.g. DFS with a recursion-stack / topological sort check), identify the true root (the node with zero outgoing edges per the memo's definition — verify its id matches what the memo specifies), confirm the graph is fully connected to that root, and compute each node's shortest hop distance to the root (BFS) to enforce the max-distance rule; exit 0 only if every rule holds, else exit 1. Test against a few hand-written valid and invalid graphs before trusting it. For `/home/user/backup_schema.sql`, model nodes/edges as tables (e.g. an edges adjacency-list table with an index on source and target) and include a recursive CTE example for shortest-path-to-root queries.