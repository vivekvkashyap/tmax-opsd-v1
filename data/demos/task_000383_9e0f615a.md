Derive the AES key exactly as specified rather than guessing: extract the public key from `/home/user/audit/server.crt` in DER form and SHA-256 it in one openssl pipeline, e.g. `openssl x509 -in server.crt -pubkey -noout | openssl rsa -pubin -outform DER | openssl dgst -sha256 -binary`, then take the first 16 bytes of that digest as your AES-128 key (keep it as raw bytes, not hex, when feeding it to the AES routine). In the Rust project, parse `access.log` for `Authorization: Bearer <token>` values, split each JWT on `.`, base64url-decode (no padding) the header and payload segments, and filter for header containing `"alg":"none"` and payload containing `"role":"admin"` — do not attempt any signature verification, that's intentionally absent. For each match, hex-decode the payload's `encrypted_action` field and AES-128-ECB-decrypt it with your derived key (16-byte blocks, no padding needed since the plaintext actions are exactly 16 bytes) to recover the plaintext action string. Write the decrypted actions to `/home/user/audit/compromised_actions.txt`, one per line, in the order the matching log entries appear, with no extra formatting. Test with `cargo run` and confirm the key derivation independently (e.g. a small script) if results look wrong before assuming the AES logic is broken.