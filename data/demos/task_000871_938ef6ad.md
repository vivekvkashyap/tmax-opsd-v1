Build this in clear stages and validate each before layering the next. Stage 1: run `ffmpeg -i /app/source_video.mp4 -vf fps=1 /home/user/frames/frame_%04d.png` to extract one PNG per second with the required 4-digit zero-padded naming; confirm the files exist and are readable images before moving on. Stage 2: in the Rust project, use the `image` crate to load consecutive frame pairs in numeric order, convert each to 8-bit grayscale (`to_luma8()` or similar), then compute MAD as the sum of per-pixel absolute differences divided by total pixel count — make sure both frames have identical dimensions and iterate in matching (x,y) order; this is the core numeric part the verifier cross-checks with OpenCV/Pillow within a tolerance, so get grayscale conversion and averaging exactly right rather than approximating. Write results to `/home/user/distances.csv` with header `start_frame,end_frame,mad`, integers for frame numbers and mad formatted to 2 decimal places. Stage 3: reuse the same computed data (don't recompute per-request) to serve `axum`/similar HTTP routes on `127.0.0.1:8000`: `/api/distances` returning the JSON array matching the CSV data, and `/api/frames/<id>` streaming the raw PNG bytes for `frame_%04d.png` with `Content-Type: image/png`. Keep the server running (foreground or backgrounded) so it can be curled, and manually test both routes before finishing.
