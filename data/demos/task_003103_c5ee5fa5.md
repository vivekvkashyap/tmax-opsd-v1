Reconstruct the MLP exactly as specified rather than guessing an architecture: build a small PyTorch module (or even do it with plain NumPy matrix ops) with `hidden = relu(x @ W1 + b1)` then `output = hidden @ W2 + b2`, loading `W1, b1, W2, b2` directly from `/home/user/model_weights.npz` via `np.load` — match the matrix-multiply orientation given in the prompt (`x @ W1 + b1`, so weights are stored input-major, not the transposed convention some frameworks default to). Iterate over all 50 `.npy` files in `/home/user/dataset/`, load each as a length-10 vector, run it through the reconstructed forward pass to get a scalar logit, then route the file with `shutil.move` to `/home/user/positive_cases/` if the logit is strictly > 0, else `/home/user/negative_cases/` — create both destination directories first with `os.makedirs(exist_ok=True)`. Tally the counts as you go (don't recount by listing directories afterward, in case of prior partial runs) and write `Positive: N` / `Negative: M` to `/home/user/summary.txt` in the exact two-line format. Since the source data and weights are seeded deterministically, run your script and let the actual computed logits — not an assumption — decide each file's bucket.