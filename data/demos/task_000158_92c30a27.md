Write the C program in two clear passes: a parser and a writer. For parsing, recursively walk `/home/user/log_backups` (e.g. `nftw` or `opendir`/recursion) collecting only `.txt` files, then scan each file's text for `---START---`/`---END---` blocks — ignore anything outside these markers (like stray garbage lines). Inside a block, parse `Time:` as an integer, `Severity:` mapped to the INFO=0/WARN=1/CRIT=2 enum, and treat everything after `Message:` up to `---END---` as the message body, which may itself span multiple lines — join those lines with the newline characters as they literally appear, then strip only leading/trailing whitespace from the whole message before measuring its byte length. Collect all parsed records into a dynamic array/struct list before writing anything, since the spec requires a stable sort by timestamp (ascending, ties broken by original discovery order — use a stable sort algorithm, not `qsort`'s unstable default, or add an index tiebreaker to the comparator). For the binary writer, write the 4-byte `BKP1` magic first, then per record: an 8-byte little-endian `uint64_t` timestamp, 1-byte severity, a 2-byte little-endian `uint16_t` message length, then the raw message bytes with no null terminator — be careful with endianness and struct padding if you use a struct instead of manual byte writes. Compile with a strict warnings flag and run it, then hexdump the output to sanity-check the header and one record's layout before trusting the whole file.