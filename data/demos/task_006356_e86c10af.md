In the C library, define `MathBuffer` with `unsigned char* data` then `int length` (field order/types must match what you'll mirror in the FFI struct), `malloc` the struct and the data array inside `encode_data`, and compute each byte as `(input[i] * key + i) % 256` using unsigned/int arithmetic exactly as specified. Compile with `gcc -shared -fPIC -o libmath_encode.so libmath_encode.c`. In your FFI script, define a matching `ctypes.Structure` (fields `("data", ctypes.POINTER(ctypes.c_ubyte))`, `("length", ctypes.c_int)`), and set `lib.encode_data.restype = ctypes.POINTER(MathBuffer)` plus correct `argtypes` — the most common naive-attempt bug is leaving `restype` as the default `int`, which truncates/corrupts the pointer. Read the bytes with something like `bytes(ctypes.cast(buf.contents.data, ctypes.POINTER(ctypes.c_ubyte * buf.contents.length)).contents)` then `.hex()` for a lowercase string. For the URL, use `urllib.parse.urlsplit`/`parse_qs` (or Ruby/Node equivalents) to pull `data` and `key` robustly rather than manual string splitting. Write only the hex string (no newline/extra text) to `/home/user/result.txt`.
