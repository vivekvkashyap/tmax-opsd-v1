Write `/home/user/pipeline.cpp` using only the standard library. Parse `/home/user/sensor_data.csv` by reading the header to get column indices, then splitting each subsequent line on commas and parsing floats into `std::vector<double>` per column (F1..F4, T). For each feature compute the Pearson correlation with T using the standard formula: `sum((x-mean_x)*(y-mean_y)) / sqrt(sum((x-mean_x)^2) * sum((y-mean_y)^2))`. Track the feature with the largest `fabs(correlation)` as selected. Fit univariate OLS on the selected feature using the closed form `w = cov(x,y)/var(x)`, `b = mean_y - w*mean_x` (equivalent to `np.polyfit` degree 1). Write the four output lines to `/home/user/etl_model_output.txt` using `std::ofstream` with `std::fixed << std::setprecision(4)` for every numeric value, matching the exact label format `SelectedFeature: ...`, `Correlation: ...`, `Weight: ...`, `Bias: ...` with no trailing blank line beyond what's specified. Compile with `g++ -O3 -std=c++17 /home/user/pipeline.cpp -o /home/user/pipeline` and run it. Sanity-check your result independently (e.g. eyeball with a quick numpy one-liner) since the grader recomputes the exact expected feature/correlation/weight/bias from the CSV and does a strict string comparison — small rounding or formula differences (e.g. population vs sample variance canceling out, but sign/precision mistakes) will fail the exact match.