Treat this as a classic wide-to-long reshape plus priority-based dedup, and get the precedence rule exactly right since it drives correctness. Load `app_strings.json` as-is (already long: key/language/translation). Melt `web_strings.csv` from wide (one column per language) to long form with `pandas.melt`, then drop rows with null/empty translation values — an empty CSV cell must not become a bogus record. Concatenate the two long frames, tagging or ordering them so you can enforce "app_strings wins on collision": compute `hash_id = sha256(f"{key}_{language}")` for every row, then deduplicate on `hash_id` in a way that prefers app-sourced rows — e.g. concatenate with app rows first and use `drop_duplicates(subset='hash_id', keep='first')`, or explicitly split into app/web sets and use app to override any overlapping keys in web. Reorder columns to exactly `hash_id, key, language, translation`, sort by `key` then `language`, then write both CSV (no index) and Parquet to the pre-created `/home/user/locales_processed/` directory. Spot-check a couple of known-overlapping keys by hand against the two source files to confirm the override direction is correct before finishing.