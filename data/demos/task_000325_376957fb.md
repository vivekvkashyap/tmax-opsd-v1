Write `validate.sh` to check the header line with an exact string comparison (`head -1 "$1"`) and then check every line's field count with `awk -F',' '{print NF}'` (or `awk -F',' 'NF!=3{exit 1}'`), exiting 0 only if both the header matches and all lines have exactly 3 fields, else exit 1 — test it against both a good file and a deliberately malformed one before moving on. In `bootstrap.py`, call `numpy.random.seed(42)` once at the top before any sampling, then for each of 1000 iterations draw `numpy.random.choice(amounts, size=len(amounts), replace=True)` and record its mean; compute the 2.5th/97.5th percentiles with `numpy.percentile` and write them rounded to 2 decimals as `lower,upper` to `ci.txt`. Critically, put `import matplotlib; matplotlib.use('Agg')` before `import matplotlib.pyplot as plt` (order matters — importing pyplot first locks in a GUI backend that fails headlessly), then histogram the means and `savefig` to `plot.png`. Have `pipeline.sh` call `validate.sh`, check its exit code with `if [ $? -eq 0 ]`, run `python3 bootstrap.py` on success, else print the failure message and exit 1. Just write the scripts; don't execute the pipeline.
