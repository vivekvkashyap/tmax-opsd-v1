Write a small Python script to do the binary parsing rather than trying to hand-roll it in bash. For `.artf` extraction: open in binary mode, verify the 4-byte `ARTF` magic, read the file count as `<H`, then loop reading a 1-byte name length, the name bytes, `<I` decompressed size, `<I` compressed size, then that many compressed bytes; decompress by reading the compressed bytes two at a time as `(count, value)` pairs and emitting `value` repeated `count` times — write each decompressed file under `/home/user/extracted_artifacts/`. For the `.cfg` text transforms, iterate only files ending in `.cfg` and apply the three rules in order: substring-replace `auth_protocol=v1` -> `auth_protocol=v3`, substring-replace the mirror URL, and drop any line whose content (after stripping) starts with `deprecated_flag=` — do this with plain Python string/line operations for precision rather than complex sed regex. For `index.bin`, parse it as consecutive fixed 24-byte records (16 bytes name padded with nulls + `<I` checksum + `<I` timestamp), strip trailing `\x00` from the name with `.rstrip(b'\\x00')`, and build the JSON dict keyed by filename. Finally write the summary log counting total extracted files and total `.cfg` files actually modified, in the exact two-line format specified.
