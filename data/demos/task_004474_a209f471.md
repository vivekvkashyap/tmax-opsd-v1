First reproduce the crash: build the existing `solver.c` and call `solve_knapsack` with a `capacity` above the hardcoded stack-array bound to confirm the stack-buffer-overflow. Fix it by replacing the fixed-size `int dp[50]` with a heap-allocated buffer sized to the actual `capacity` (`malloc(sizeof(int)*(capacity+1))`, zero-initialize, and `free` before returning) rather than just bumping the constant — the algorithm (0/1 knapsack DP recurrence) itself must stay unchanged. Compile with `gcc -shared -fPIC -o libsolver.so solver.c` and verify with a small C or Python `ctypes` test at capacity=1000 before moving to Go. In the Go server, use `cgo` (`#cgo LDFLAGS: -L/home/user/legacy -lsolver`) and set `LD_LIBRARY_PATH` in `start.sh` so the dynamic linker finds `libsolver.so` at runtime — this is the step naive attempts miss, causing "cannot open shared object file" at start. Validate request bounds (capacity 1-1000, items 1-100) before calling into C. Implement rate limiting with a simple token bucket or `golang.org/x/time/rate.Limiter` set to 5 req/s globally, returning 429 when exhausted. Have `start.sh` background the server and write `$!` to `server.pid`.