Build this incrementally and test each piece before wiring them together. Scaffold with `cargo new --bin data_organizer`, then add `notify`, `tar`, and `flate2` to `Cargo.toml` (check crates.io for current API — `notify` v6's watcher API differs from v4/v5, so pick one and match its event-handling signature). Implement the pipeline in clear stages inside `main.rs`: (1) set up a `notify` watcher on `incoming/` with a channel/callback; (2) on a create event for a `.txt` file, read it fully and chunk it into groups of 50 lines using `lines().collect::<Vec<_>>().chunks(50)`, writing each chunk with `File::create`; (3) after chunks exist, create hard links with `std::fs::hard_link` (not copies) into `debug_chunks/`, and a symlink with `std::os::unix::fs::symlink` pointing at the *original* dropped file's path (absolute path, not relative); (4) build the tar.gz by wrapping a `File` in `flate2::write::GzEncoder` then `tar::Builder`, appending each chunk file, and finishing/dropping in the right order so the file is flushed; (5) only after the archive write succeeds, write "SUCCESS" to the status log. Test the watcher timing carefully — file-system events can fire before the writer has finished flushing, so read files defensively (e.g. retry-on-empty or watch for the close-write event) rather than assuming the drop event means the data is ready.
