First fix and install the vendored `yq`: inspect `/app/yq/yq/__init__.py` for an obviously misspelled stdlib import (compare against real `import argparse`) and correct the typo, then `pip install -e /app/yq` and confirm `yq --version` runs cleanly before moving on — don't skip this verification step. Then design `process_configs.sh` around a generated Makefile rather than a shell loop: for each input file, emit a Makefile rule/target (e.g., named after a sanitized version of the filename) whose recipe does the per-file work, then invoke `make -j4 -f <generated-makefile>` so tasks run in parallel. For dedup, compute `sha256sum` per file, group filenames sharing a hash, and only keep the lexicographically first name in each group — do this as a precomputation pass before generating Makefile rules, not inside each parallel job (to avoid races deciding "first"). For the two rejection regexes, use `grep -E` with patterns matching `evil_directive:\S+` and `password:\s*[a-zA-Z0-9]+$` anchored appropriately; reject if either matches anywhere in the file. Only files passing both the dedup and regex checks get `cp`'d to `<output_dir>`. Test against both `/app/corpus/clean/` and `/app/corpus/evil/` directly to confirm 100% pass/reject before considering it done.