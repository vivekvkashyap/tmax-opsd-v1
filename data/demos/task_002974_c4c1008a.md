First reverse-engineer the ABI: run `nm -D /app/liblegacy_math.so` or `objdump -T /app/liblegacy_math.so` to list the exported dynamic symbol(s) — the library is stripped of debug info but dynamic symbols must remain for linking to work, so the exact function name is discoverable there rather than assumed. Declare that function's prototype (`int iterations, double value` -> `double`) via `extern` in server.c and call it from your `/zeta` handler after parsing `iter`/`val` from the query string with Mongoose's query-parsing helpers; format the JSON reply with a fixed/consistent float precision. For the Makefile, the key fixes are adding `-L/app -llegacy_math` to link against the library and `-Wl,-rpath=/app` (or setting `LD_LIBRARY_PATH` at runtime, but rpath is more robust) so the resulting binary finds the .so without manual environment tweaks at execution time — a naive `gcc server.c vendor/mongoose.c -I vendor -o server` will compile but fail to link/run. Build with `make`, then launch `./server &` and confirm with `curl` that port 9090 responds before finishing.