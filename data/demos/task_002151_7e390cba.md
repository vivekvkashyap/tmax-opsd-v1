Start by transcribing `/app/requirements.wav` (e.g. with a local speech-to-text tool/library) to recover two spec values spoken aloud: the exact API token string and the rolling-window day count — listen carefully for how spelled-out words like "dash" or number words should be reassembled into the literal token/number, don't assume a format. Load the three CSVs into SQLite with pandas'/sqlite3's ingestion, then create indexes on `customer_id` in each table and on `tx_date`/`call_date` to support efficient joins and ordering. Write the analytical query using window functions: for each customer's transactions, compute a rolling sum of `amount` over the trailing N days (the day count from the audio) relative to their most recent transaction date, and separately pull each customer's most recent call's `duration_seconds` via a correlated subquery or `ROW_NUMBER()` partitioned by customer ordered by `call_date DESC`; inner-join so customers with no transactions or no calls are excluded entirely. Stand up a FastAPI/Flask app bound to `127.0.0.1:9090` with a `GET /insights` route that requires header `X-API-Token` to exactly equal the recovered token (401/403 otherwise), returning the query result as JSON matching the exact key names and types specified. Launch it as a background process so it stays reachable for testing.