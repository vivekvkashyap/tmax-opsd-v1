Write the C scanner as a byte-stream state machine reading from stdin in a loop (fread into a buffer, process byte-by-byte or use memmem-style scanning across buffer boundaries carefully so a magic number split across two reads isn't missed — track a small carry-over of the last 3 bytes between reads). When you match the 4-byte little-endian magic `0xDEADBEEF` (careful with byte order: the low byte 0xEF appears first in the stream), read the next 2 bytes as sensor ID, 4 bytes as timestamp, 2 bytes as payload length (all little-endian uint16/uint32 — use bit-shifting from raw bytes rather than casting a pointer, to avoid alignment/endianness bugs across platforms), then skip exactly that many payload bytes (read and discard, or fseek-equivalent via stdin consumption) before resuming the magic-number scan. Print `Timestamp,SensorID,PayloadLength` per record found. Separately, find qualifying files with `find /home/user/sensor_archives -type f -name '*.log' -size +4096c`, concatenate their contents in one pipeline into your compiled extractor, then numerically sort the CSV output by the first field (`sort -t, -k1,1n`) before writing to the final path — verify no header row leaks in and the file ordering from `find` doesn't affect the final result since you sort afterward.
