Structure the script in the same order as the requirements. For traversal, don't use a naive `os.walk` that blindly follows symlinks (it will hang on the loop) — instead walk manually, and whenever you encounter a directory entry that is a symlink, resolve it with `os.path.realpath`/`os.stat` and track visited real (device, inode) pairs in a set; skip any directory whose (dev, inode) you've already visited, which both allows the valid symlink into `batch_valid` and safely terminates the self-referential loop. For each `.csv.gz` match, stream-read with `gzip.open(path, 'rt')` + `csv.reader`/`csv.DictReader` rather than extracting to disk. Filter rows to `status == 'VALID'`, keep `record_id` and `measurement`, and compute `float(measurement) * 100.0`. Collect all rows in memory, sort by `record_id` numerically (cast to int for the sort key, not string sort), then write the header plus rows to a temp file (e.g. `/home/user/clean_dataset.tmp`) and finish with `os.rename()` to `/home/user/clean_dataset.csv` for atomicity. Watch float formatting — match Python's default `str(float)` rendering (e.g. `123.0`, not `123.00`) so output matches expected precision exactly.
