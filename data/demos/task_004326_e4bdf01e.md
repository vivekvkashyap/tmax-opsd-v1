Break this into three verifiable stages before wiring them together in Rust. First, locate the key: read `guard.bin` as raw bytes (e.g. via `std::fs::read` and a byte-slice search, or use the `strings`/`grep -a` CLI first to sanity-check by eye) to find the literal marker `KEY_START:` and take the 16 bytes immediately following it as the XOR key — don't try to parse ELF sections for this, a simple substring/byte scan over the whole file content is sufficient and more robust. Second, decrypt `encrypted.dat` by XOR-ing each byte with `key[i % 16]` (repeating key, not a stream cipher) and confirm the result looks like a valid bash script (starts with `#!/bin/bash` or similar) before proceeding. Third, when spawning the decrypted script, use `std::process::Command` with `.env_clear()` and then `.env("ISOLATION_MODE", "active")` so absolutely no other host environment variables leak through, capture stdout via `.output()`, and write it verbatim to `/home/user/exfil.txt`. Verify each stage manually (print the recovered key, print the decrypted script, then run it) before trusting the final automated output.
