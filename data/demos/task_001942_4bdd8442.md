First reverse-engineer the schema instead of guessing: run `sqlite3 /home/user/network.db ".schema"` and sample rows from `routers` and `links` to confirm actual column names and how edges are directed/stored. For the cost oracle, don't try to disassemble it — treat it purely as a black-box CLI: shell out to `/app/link_cost_oracle <src> <dst>` for each edge you need and capture stdout as an integer; cache results per (src,dst) pair to avoid redundant subprocess calls when running Dijkstra. Build the graph adjacency from the `links` table, then run a standard shortest-path algorithm (Dijkstra, since oracle costs are positive integers) computing edge weights lazily via the oracle, and return the actual path plus summed cost — verify by hand-computing the oracle's cost formula behavior on a couple of edges via direct CLI calls before trusting your path search. For the REST layer, implement both endpoints with the auth check (`Authorization: Bearer ADMIN_TOKEN_99`, else 401) applied uniformly first; get pagination/filtering exactly right (filter by tier, sort by `id` descending, then apply limit/offset) since off-by-one errors in ordering are the most common mistake here. Bind the server to 127.0.0.1:9000 and leave it running in the background.