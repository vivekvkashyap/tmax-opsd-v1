Work through the pipeline stage by stage, confirming each piece functions before layering the next on top, since the final check is just an end-to-end curl. Reorganize files into the required directory tree first (`C_src/`, `Python_src/`, `build/`, `config/`) and move the two source files accordingly. Compile the C file with `gcc -shared -fPIC -o build/libprime.so C_src/prime_c.c` and sanity-check with `nm` or a tiny ctypes call that `count_primes` is exported and returns a sane value for a small n before writing the full benchmark. In `benchmark.py`, load the shared library via `ctypes.CDLL('../build/libprime.so')` (set `.argtypes`/`.restype` on the function), import `count_primes` from `prime_py.py` directly, time 5 calls of `count_primes(50000)` for each implementation with `time.perf_counter()`, average them, and write `report.txt` in exactly the three-line `Key: value` format specified (Speedup = Py_avg/C_avg). Run the benchmark script yourself to actually produce the file. Finally serve `build/` with `python3 -m http.server 9000` in the background, then write an HAProxy config with a frontend on `127.0.0.1:8080` and a backend forwarding to `127.0.0.1:9000`, and launch `haproxy -f config/haproxy.cfg` in the background — verify with `curl http://127.0.0.1:8080/report.txt` yourself before finishing.
