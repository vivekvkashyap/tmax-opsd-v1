Work top-down: first derive the dynamic version number `K`, then build the parser, then validate against the corpora. Use `ffprobe -select_streams v -show_frames -show_entries frame=pict_type -of csv /app/config_session.mp4 | grep -c I` (or `ffprobe -show_entries frame=key_frame`) to count I-frames in `/app/config_session.mp4` — do not guess the count, derive it from the actual video. For the C program: open the file with zlib's `gzFile`/`gzopen`/`gzread` (or `zlib.h` stream API) rather than shelling out to `gzip`, since headers must be parsed from the decompressed byte stream directly. Read the 8-byte header with explicit little-endian field extraction (don't rely on struct packing/alignment — read individual bytes and shift). For UTF-16LE conversion, iterate 2 bytes at a time taking the low byte as ASCII. Check magic, then version against your discovered `K`, then scan the decoded payload for the literal substring `DROP_TABLE` using strstr-style logic, rejecting on any mismatch or read error. Finally, loop over every file in `/app/corpus/clean/` and `/app/corpus/evil/`, running your compiled binary on each and confirming exit codes match expectations before declaring done.
