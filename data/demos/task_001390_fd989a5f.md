Start by inspecting the vendored source at `/app/storage-monitor-1.0`: check the `Makefile` for how `CXXFLAGS`/environment handling is wired — look for a hardcoded flag (like a fixed `-DENV=PROD`) that should instead be derived from the `STORAGE_ENV` environment variable at build time (e.g. `CXXFLAGS=-DENV=$(STORAGE_ENV)`), and fix it so building with `STORAGE_ENV=STAGING make` actually threads that value through. Then read `monitor.cpp` carefully around the threshold comparisons: the boundary conditions in the spec ("below 1024" -> OK, "between 1024 and 2048" -> WARNING, "over 2048" -> CRITICAL) imply the CRITICAL comparison should be inclusive at 2048, so check whether the code uses a strict `>` where it needs `>=`. Also make sure the program flushes stdout after each line (e.g. `std::endl` or explicit `flush`) so output isn't buffered and lost/delayed. After fixing, compile with `STORAGE_ENV=STAGING` to produce `/home/user/bin/storage-daemon`, and write `/home/user/deploy.sh` to export the env var and launch it. Manually pipe a range of integers (0, 1023, 1024, 2047, 2048, 2049, 5000) through the daemon and check each line's classification against the stated rule before finishing.