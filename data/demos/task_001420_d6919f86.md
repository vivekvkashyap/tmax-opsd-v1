Build this as a bash-only pipeline in clear stages, since Python/Perl/Ruby are disallowed. First run `tesseract` on `/app/metrics_chart.png` to OCR the "Scaling Factor: X.XX" text, then extract the numeric value with `grep -oP` or `sed`/`awk` (don't hardcode a guessed factor — read whatever OCR actually returns, trimming whitespace/noise). Next, normalize each format in `/home/user/data/` into a common `timestamp,sensor_id,temperature` tuple stream: CSV via `awk -F,` skipping the header, JSON-lines via `jq -r '[.timestamp,.sensor,.temp] | @csv'`, and XML via `sed`/`grep` with a regex pulling out `<ts>`/`<id>`/`<val>` content per line — concatenate all three into one normalized stream. For deduplication, compute `echo -n "$ts$id$temp" | md5sum` per record and use `awk '!seen[$hash]++'` (or sort -u on hash+record) to keep only first occurrences by hash. Sort the deduplicated stream by timestamp, then per `sensor_id` compute a 3-period trailing rolling average with `awk` (maintain a small sliding window array per sensor, averaging over min(3, readings-so-far)), multiply each average by the OCR'd scaling factor, and round to 2 decimals with `awk '{printf "%.2f", $1}'`. Write the final `timestamp,sensor_id,scaled_rolling_avg` rows to `/home/user/output.csv` with header, and spot-check a few rows by hand against the source files to confirm the rolling-window and rounding logic are correct.