Treat this as two separate investigations: a profiling task and a git-forensics task. For the memory leak, build the service, run it under `go tool pprof` (heap profile) or instrument with `runtime.ReadMemStats` at intervals while processing a video, and look for a slice or map that grows unbounded across the per-frame processing loop — a classic pattern is a global/outer-scope slice that gets `append`-ed to on every frame instead of being reused or windowed; fix it by keeping only a rolling window (e.g. size 1) of frame state, not accumulating history. For the corrupted serialization, use `git log -p` / `git log --oneline` on the repo at `/app/math-vid-service` to find the commit that changed the binary-encoding logic, diff it against the prior commit to see what the correct `encoding/binary` usage looked like (correct type/width, e.g. float64 vs an incorrect int32 cast), and restore that logic rather than guessing a new encoding. After fixing both issues, build and run the binary against `/app/input_video.mp4`, writing output to `/home/user/metrics.bin`, then verify peak RSS with `/usr/bin/time -f "%M"` stays low and confirm output correctness makes sense structurally (right dtype, right element count) before finishing.