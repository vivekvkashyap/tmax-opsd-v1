Diagnose the two regressions separately before touching code: profile a baseline build first so you have before/after numbers, then inspect `/app/libsvd_fast/CMakeLists.txt` for the compiler optimization flags (a build set to `-O0` will be dramatically slower than `-O2`/`-O3` — check `CMAKE_CXX_FLAGS` and change the level, keeping other flags like `-fPIC` intact) — this addresses the speed regression. Separately, inspect the actual SVD/decomposition algorithm's convergence or tolerance parameters in the library's source for anything that looks like it was deliberately loosened (an overly large epsilon/tolerance constant causes the iterative solver to stop too early, producing large reconstruction error) — tighten it back to a numerically sound value like `1e-9`. Rebuild the library from scratch after each fix (don't rely on a stale build cache) and install headers plus the shared library under `/home/user/local/`. Compile `/home/user/regression_test.cpp` against your fixed install (set include/lib paths and rpath correctly so it finds your `.so` at runtime), run it to produce `/home/user/run_test` and `/home/user/metrics.json`, and verify both the timing and reconstruction-error numbers before considering the task done.
