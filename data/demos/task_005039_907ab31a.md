Secret recovery: run `git -C /app/service log --all --oneline` and `git log -p -- config.py` (or `git show <old-commit>:config.py`) to find an earlier commit where the real secret was present before it got overwritten with a placeholder — deleted/overwritten history is still reachable via `git log`/`git show` on prior commits, so walk the file's history rather than trusting the current working tree.

Vendored package fixes: open `/app/vendored/telemetry-decoder/decoder.py`. For the memory leak, find the global unbounded cache dict that stores failed serialization attempts and either remove the caching of failures entirely or bound/clear it (e.g., don't persist per-request state in a module-level dict at all). For the precision bug, find where fields ending in `_coord` are parsed and switch from `float(val)` to `decimal.Decimal(val)` so exact precision survives serialization/deserialization. For crash safety, wrap the decode step so `UnicodeDecodeError` (and similar decode exceptions) on corrupted UTF-8 input is caught and handled gracefully (e.g., skip/replace) instead of propagating and crashing the request handler.

Then start `python /app/service/server.py` in the background and verify it's actually listening on 127.0.0.1:8080 (e.g. `curl` a test POST to `/ingest` with the recovered secret in the `Authorization: Bearer` header) before considering the task done.