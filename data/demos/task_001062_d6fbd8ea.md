Break this into a clean per-stage pipeline in analysis.py. Video tracking: open /app/experiment.mp4 with OpenCV, for each frame threshold/find the brightest region (e.g. `cv2.minMaxLoc` on a grayscale frame, or thresholding + `cv2.findContours`/centroid of the largest bright blob) and record its x-coordinate per frame index, converting frame index to time via `t = frame_idx / 30.0`. Model fitting: define `x(t) = A*exp(-gamma*t)*cos(omega*t+phi) + x0` and use `emcee` for MCMC — set up a log-likelihood assuming Gaussian noise around the model, uniform log-priors over physically reasonable bounds for each of the 5 parameters (informed by the rough scale of your extracted A, oscillation period, and offset from the raw trajectory, not the true underlying values which you don't know), run enough walkers/steps with a burn-in you discard, then take posterior means for gamma and omega specifically (the metric that's checked). Residual/bootstrap: compute residuals between observed and best-fit model, resample them with replacement 1000 times computing the variance each time, and take the 2.5/97.5 percentiles as the CI bounds. Write all four required keys to /home/user/results.json with the exact key names given.