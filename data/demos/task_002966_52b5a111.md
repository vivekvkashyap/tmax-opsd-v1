Structure `/home/user/curate_audio.sh` to take `$1` (input dir) and `$2` (output tar.gz) as required arguments. For transcription, install a CLI tool such as `openai-whisper` (`pip install openai-whisper`, requires `ffmpeg`) and run it per-file (e.g. `whisper "$file" --model base --output_format txt --output_dir <tmpdir>`) rather than assuming any fixed transcript — the actual spoken content is only known at verification time against a hidden corpus, so your rejection logic must genuinely run transcription and case-insensitively grep the output text for "CONFIDENTIAL" or "RESTRICTED" (`grep -iE 'confidential|restricted'`) to decide accept/reject. Use a temp working directory to hold per-file transcripts and only copy accepted `.wav` files into a staging directory. For each accepted file compute `sha256sum` and `stat -c%s` (or equivalent) for size, and build `manifest.json` with `jq -n` or manual string construction listing filename/sha256/size objects under a `"files"` key. Finally `tar -czf "$2" -C <staging_dir> .` so the wav files and manifest.json sit at the archive root with no directory prefix. Test against `/app/sample_corpus/` before relying on the hidden adversarial corpus, and make sure the script degrades gracefully (doesn't crash) on any input file.