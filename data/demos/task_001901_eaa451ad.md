Diagnose before editing: run `cargo build` in `/app/bayesian_tracker-1.0.0/` first and read the actual compiler error rather than assuming — the task says the perturbation is in `Cargo.toml`, so check it for something simple like a malformed string/missing quote or delimiter and fix only that minimal syntax issue, not the whole file. Once it compiles, grep the Rust source (`src/main.rs` or wherever env vars are read, e.g. `std::env::var(...)`) to find the *actual* environment variable name the code expects for the auth token — do not assume it matches whatever `run.sh` sets, since the task states they're mismatched; fix or bypass the run script's wrong variable name and export the correct one when launching. Confirm the code binds to `127.0.0.1:9090` (check for hardcoded address vs. a CLI arg/env var it reads) and supply that if needed. Build in release or debug mode, then launch the binary in the background with output redirected to `/home/user/service.log` using `nohup ... &` (or `setsid`) so it isn't killed when your shell session ends, and leave it running rather than exiting the process.
