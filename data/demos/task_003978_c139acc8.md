Start by trying `make` in `/app/vendored/libdag-1.0.0/` and reading the actual linker error — it will point at the `libcore.so` rule. The bug is that the Makefile links `libcore.so` against `-lcore` (itself) instead of `-lutils`, and/or the library search path is wrong; fix the recipe so `libcore.so` builds from `src/core.c` linking `-L. -lutils` (utils must be built first and be the actual dependency, not core linking itself). After `make` succeeds, `pip install .` (or `python setup.py install`) to make `libdag` importable, and confirm with `python3 -c "from libdag import topological_sort"`. For the API, implement `/resolve` and `/filter_versions` as described, requiring the exact `Authorization: Bearer ci-deploy-token-8842` header on both (reject others with 401/403). Implement rate limiting with an in-memory sliding window (e.g. a deque of request timestamps per token, evicting entries older than 60s) — cap at 5 requests per 60s window and return 429 on the 6th, not by counting per-minute buckets which can incorrectly allow bursts across boundaries. Log every request (path, status) to `/home/user/service.log`, and leave the server running in the background (e.g. `nohup ... &`) before finishing.
