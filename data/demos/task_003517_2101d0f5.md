Before writing any Go code, reverse-engineer the stripped validator binary: run `file`, `strings /app/repo-validator` to look for usage/error text or format hints, and `objdump -d /app/repo-validator` to trace how argv is consumed — pay attention to whether it expects a numeric checksum argument and how that checksum might be derived from a file's contents (e.g. sum of character codes) since the manifest text is available to compute it against. Confirm your hypothesis empirically by running the binary directly against a test git dir and manifest with different trial argument values and observing exit codes/output rather than assuming. In the Go service, implement `POST /restore` to: decode the JSON body, extract the tarball into `/home/user/restored/<name>` (`archive/tar` + `compress/gzip`, or shell out to `tar`), parse `manifest.txt` for the `owner=` line (simple string split, not hardcoded to one repo name), invoke `exec.Command` with the validator and your derived argument(s), and capture stdout to decide success/failure and build the JSON response. Also generate the `pre-receive` hook script dynamically per restored repo, embedding the allowed users read from manifest.txt, and `chmod +x` it. Start the server with `go run` or a background `go build && ./binary &`, and test the full flow yourself with `curl` before considering it done.
