Follow the proto-first workflow rather than writing Go structs by hand. Write `migration.proto` exactly matching the spec (syntax, package, go_package option, enum with explicit `= 0/1/2` values, messages, service+rpc), then generate code with `protoc --go_out=. --go_opt=paths=source_relative --go-grpc_out=. --go-grpc_opt=paths=source_relative migration.proto`, checking the output lands under `/home/user/workspace/migration/` matching the import path `client.go` expects (`workspace/migration`). Implement `TextBuffer` as a small struct with an `[]string` slice; `Append` does `b.lines = append(b.lines, line)` and `Render` does `strings.Join(b.lines, "\n")`. In the gRPC server's `Apply` method, iterate `req.GetChanges()` and switch on `change.GetOp()`: for `KEEP` and `ADD` call `buf.Append(change.GetLine())`, for `DELETE` skip — this mirrors how a real diff/patch is applied line by line. Have `main` register the server on `localhost:50051` and block. Run the server in the background (`go run server.go &`), then `go run client.go`, and inspect `patched.py` to confirm every KEEP/ADD line appears in original order with DELETE lines omitted.
