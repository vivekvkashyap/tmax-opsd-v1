Work phase by phase and verify each output before moving on. Mount the squashfs read-only (`mount -o loop /home/user/docs_data.sqfs /home/user/mnt`, or `unsquashfs` if you lack mount permissions in the sandbox) and locate the `.ldoc` files, then run each through `/app/ldoc_decoder <in.ldoc> <out.html>` exactly as documented (two positional args) to get raw HTML — don't try to reimplement the decoder yourself, the binary is the given tool for this step. For Phase 2, parse each HTML with `beautifulsoup4`, walking tags in document order and emitting Markdown equivalents (headings -> `#`, paragraphs -> plain text, links -> `[text](href)`), rather than a blind regex strip, since structure matters for a faithful conversion. For Phase 3, read `build_config.json` and only keep files whose filename is listed, using its target logical path as the archive key. For Phase 4, write `pack_docs.py` implementing the exact byte layout: `DOCZ` + little-endian uint32 JSON-index length + UTF-8 JSON index (`{path: [offset, length]}` into the *uncompressed* concatenated payload) + `zlib.compress(payload, 9)` of the single concatenated block (not per-file compression) — compressing per-file instead of the whole block is the most common way to blow the size threshold, so concatenate first, then compress once.