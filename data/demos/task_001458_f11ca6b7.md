Set up the environment and script deliberately, matching the verifier's exact reproduction steps rather than approximating. Create the venv with `python3 -m venv /home/user/scipy_env` and install packages via that venv's pip so `/home/user/scipy_env/bin/python` exists and has numpy/scipy/matplotlib. For the "true" integral, the key fix versus the junior colleague's broken attempt is passing `limit=1000` to `scipy.integrate.quad` so the adaptive quadrature subdivides enough to resolve the `sin(100x)` oscillation instead of diverging with the default limit; round the result to exactly 6 decimals when writing it to the txt file. For Monte Carlo, call `numpy.random.seed(42)` exactly once at the very start of the MC section (not per-N), then draw samples for each N in the given order `[10000, 50000, 100000, 500000]` using `numpy.random.uniform(0, 5, N)` — sampling order and count matter for reproducing the verifier's exact expected values, since each draw consumes from the shared RNG stream sequentially. Estimate is `mean(f(samples)) * (interval width = 5)`; error is `abs(estimate - quad_value)`. Write the CSV with header `N,Estimate,Error` and then produce a log-log `Error` vs `N` plot saved to `convergence.png` using the venv's matplotlib (non-interactive `Agg` backend since there's no display).
