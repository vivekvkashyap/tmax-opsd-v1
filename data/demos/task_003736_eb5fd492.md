Write this as a single-pass C program with clear separated stages, and validate each stage's math against a hand-check before trusting the output. First read both CSVs into arrays keyed by ID (since row order differs between files, you must join by ID, not by line position — e.g. load one file into a fixed-size array indexed by ID or a simple sorted structure, then look up matching IDs from the second file). Compute Pearson's r with the standard formula `r = (n*Sum(xy) - Sum(x)*Sum(y)) / sqrt((n*Sum(x^2)-Sum(x)^2)*(n*Sum(y^2)-Sum(y)^2))`, accumulating sums in a single pass over the joined pairs, and write it to `/home/user/correlation.txt` with exactly 4 decimal places (`%.4f`). Fit OLS slope/intercept using the standard closed-form (`m = (n*Sum(xy)-Sum(x)Sum(y))/(n*Sum(x^2)-Sum(x)^2)`, `c = mean(y) - m*mean(x)`), then for every joined row compute the residual `|y - (m*x+c)|` and keep only rows with residual strictly less than 1.5. Sort the kept rows by ID ascending (a simple insertion or qsort by ID works fine at 100 rows) before writing `/home/user/cleaned.csv` with header `ID,X,Y` and `%.4f` formatting for both floats. Compile with `gcc /home/user/processor.c -lm -o /home/user/processor` and run it, then sanity-check your correlation.txt value looks plausible (should be strongly positive given the linear relationship in the data) before finishing.
