Do the data extraction and hyperparameter discovery first, independently, before writing the MCMC C code. Transcribe `/app/lab_note.wav` (install a whisper-style CLI/python tool) and read the transcript carefully to pull out four numbers: the uniform prior's lower/upper bounds for kappa, the Gaussian proposal's standard deviation, the total iteration count, and the burn-in count — convert any spoken-out numbers to digits yourself, don't hardcode assumed values. Separately, read `/app/alignments.h5`'s `/sequences` dataset with the HDF5 C API (or first inspect it with `h5dump`/`h5py` in Python to sanity-check dimensions), and compute transition/transversion counts by comparing each adjacent row pair position-by-position: classify A<->G and C<->T substitutions as transitions, all other differing pairs as transversions, skipping identical positions. Implement Metropolis-Hastings in C: propose `kappa_new ~ N(kappa_current, sigma^2)`, reject immediately if outside the transcribed prior range, otherwise accept with probability `min(1, L(new)/L(current))` using the given likelihood formula (working in log-space avoids overflow), starting the chain at kappa=2.0. After running the chain for the transcribed total iteration count, discard the transcribed burn-in count of leading samples, average the remainder, and write the value rounded to 3 decimals to `/home/user/kappa_estimate.txt`. Compile linking `-lhdf5` and `-lm`.