Build and test each stage of the pipeline independently before wiring the Makefile. For `extract.sh`, write a regex (grep -oP or awk match) that captures the bracketed timestamp, the endpoint, and latency_ms, but apply the endpoint filter precisely: `^/api/v[1-3]/[a-z_]+$` — note this must anchor to the full endpoint so `/api/v2/users123` (trailing digits) and `/api/v2/USERS` (uppercase) are correctly excluded; test the regex against the tricky near-miss lines in the log, not just the obvious ones. For `transform.sh`, bucket timestamps by flooring the minute to the nearest multiple of 5 (parse HH:MM:SS, compute `minute - minute % 5`), group by (bucket, endpoint) with awk associative arrays accumulating sum and count, and floor the average with integer division. Sort output chronologically before writing. For `load.sh`, convert each aggregated row to a JSON object with jq or manual string building, matching the exact key names (`bucket`, `endpoint`, `request_count`, `avg_latency`). Wire the Makefile with proper prerequisite chains (`extracted.csv: raw_server.log`, etc.) so `make all` reruns only what's stale, and give `clean` a `rm -f` of all generated artifacts. Run `make all` and diff your JSON (via `jq -Sc`) against manually recomputed expectations before finishing.
