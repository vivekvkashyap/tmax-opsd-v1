Design the parser around independent regexes for each required field rather than one giant pattern, then require all four to match before counting a line as successful. Match the date with an alternation covering both `\[\d{4}/\d{2}/\d{2}\]` and `\[\d{2}-\d{2}-\d{4}\]`, the unit with `Unit-[A-Za-z]+`, the temperature with something like `(\d+\.?\d*)\s*([FC])\b` (allowing preceding words like "Temp:" to be ignored since you only capture the number+unit), and pressure similarly for `psi`/`kPa`. Normalize dates to `YYYY-MM-DD` by checking which of the two bracket formats matched and rearranging components accordingly. Convert F→C with `(F-32)*5/9` and psi→kPa with `psi*6.89476`, rounding each final value to exactly 1 decimal with Python's `round()` (be aware of banker's rounding on exact .05 boundaries — verify against a couple of hand-computed cases). Preserve input line order when writing rows to the CSV. Count every line read for `total_lines_processed`, increment `successful_parses` only when all four elements matched, and set `failed_parses` as the remainder — write both `/home/user/clean_data.csv` and `/home/user/pipeline.json` from the same single pass over the file so the counts stay consistent.