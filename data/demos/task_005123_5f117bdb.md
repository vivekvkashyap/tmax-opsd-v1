Start by reading checksum_ref.py to nail down the exact Fletcher-16-style algorithm (modulus, byte-order of the two accumulators, how they combine into a 16-bit value) and the JSON payload shape it writes — you'll match this exactly in C++. Then read fast_checksum.cpp looking specifically for a loop bound that reads one element past the buffer (an off-by-one like `i <= len` instead of `i < len`, causing out-of-bounds reads / segfaults on some inputs) and for any `new[]` allocation without a matching `delete[]` (memory leak) — fix both without changing the checksum math. Recompile with `g++ -std=c++17` and diff your output against running checksum_ref.py on the same /home/user/project_sync/data directory to confirm the JSON values match exactly (same key ordering/alphabetical file iteration, same integer values). Finally validate memory safety yourself before trusting it: run `valgrind --leak-check=full --error-exitcode=1 ./fast_checksum` and iterate until it reports zero leaks and zero errors — this is exactly how the automated grader checks your work, so don't skip this local verification step.