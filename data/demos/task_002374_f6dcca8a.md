Implement this in Go as straight linear algebra on small 2x2/1x1 systems, so you don't need a heavy matrix library — direct formulas are simplest and least error-prone. For each dataset, build the 2-column design matrix X (columns x1, x2) and target Y, then form X^T X (a 2x2 matrix), add lambda*I (lambda=0.05) to the diagonal, invert that 2x2 analytically (`inv = 1/det * [[d,-b],[-c,a]]`), and multiply by X^T Y to get theta — this ridge term is what keeps the MUT dataset's near-collinear x1/x2 columns numerically stable, so apply it to both WT and MUT, not just MUT. For the reduced H0 model on WT, redo the same ridge solve but with a 1-column design matrix (just x1), i.e. a scalar solve `theta1 = sum(x1*y)/(sum(x1*x1)+lambda)`. Compute SSE for both H0 and H1 as sum of squared residuals over all N points, then the F-statistic per the given formula with denominator (N-2). For the ODE part, compute y(10) as theta1*x1(10)+theta2*x2(10) using the fitted MUT theta, then dy/dt as specified (note it reuses the same predicted y(10), so simplifies algebraically — implement it exactly as the formula states rather than assuming cancellation). Round every output field to 4 decimals and marshal to `/home/user/results.json` with the exact key names given, then run the program to produce the file.
