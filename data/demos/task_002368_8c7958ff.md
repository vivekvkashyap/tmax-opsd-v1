This is a classic single-source shortest-path-per-query problem at scale, so the method matters more than clever tricks: parse `edges.csv` into a CSR-style or array-of-linked-lists adjacency list indexed by node id (allocate arrays sized to max node id + 1, avoid any O(V) or O(E) linear scan per query). For each query pair, run Dijkstra's algorithm with a binary min-heap (implement a simple array-based heap in C, or a bucket queue since weights are bounded/positive) rather than a naive O(V^2) relaxation scan — with up to ~5000 nodes and 2000 queries, an unoptimized approach risks exceeding the 2-second budget. Handle same-source-target queries as distance 0 directly. After computing all distances, filter out unreachable pairs and any distance > 5000, then sort the surviving results with a single `qsort` using a three-key comparator (distance, then source, then target, all ascending), and apply the offset/limit (skip 10, take 20) by simple array indexing into the sorted result — write exactly those rows as `source,target,distance` with no trailing extras. Compile with `-O3` and time your run to confirm it's well under 2 seconds before finalizing.
