Build and verify each pipeline stage independently before chaining them. Start with the C++ HTTP server: include `httplib.h` from `/home/user/cpp-httplib/httplib.h`, bind a POST handler on `/ingest` at port 9000, and write a CSV parser that respects RFC-4180 quoting — do not split naively on newlines, since `sensor_dump` fields contain embedded newlines inside double quotes; a char-by-char state machine tracking "inside quotes" is the safe approach. Use `std::regex` to pull out `Temp: <value>C`, `Load: <value>%`, and `Fan: <value>RPM` patterns from each `sensor_dump`, emitting one long-format JSON object per matched metric (machine_id, timestamp, metric, value). Push each JSON string onto the Redis list `pipeline_metrics` — either shell out to `redis-cli RPUSH` or speak the RESP protocol directly over a TCP socket to 127.0.0.1:6379. Respond 200 with the count of metrics extracted. Separately, verify `start_services.sh` actually starts Nginx (proxying 8080->9000) and Redis before testing through the proxy. For scheduling, write `/home/user/flush_metrics.sh` to atomically drain the list (e.g. `LPOP` in a loop or `LRANGE`+`DEL`) and append each item as a line to `/home/user/data/metrics_archive.jsonl`, then register it with `crontab -e`/`crontab -l` using a `* * * * *` line, and test it works when triggered manually before relying on the timer.
