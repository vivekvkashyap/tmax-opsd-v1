Start by analyzing `/app/reference_signal.wav` with `numpy`/`scipy`: load it (`scipy.io.wavfile.read` or `librosa`), compute an FFT or `scipy.signal.welch` power spectral density, and find the frequency bin with peak power — that's your anomaly signature frequency (don't hardcode a guessed frequency; derive it from the actual signal). Plot it with matplotlib and save to `/home/user/psd.png`. Then for every file in `/app/corpus/clean/` and `/app/corpus/evil/`, compute the same PSD and extract the power magnitude at (a window around) that peak frequency as a single scalar feature. Sort/inspect the clean vs evil feature values and pick a threshold that cleanly separates them (e.g. midpoint between max-clean and min-evil, or exact optimization if provided) — write this selection logic into your build script so it's not a magic number you typed by eye. Implement `/home/user/classifier` (Python script with shebang + chmod +x, or compiled C++) that repeats the same feature-extraction and thresholding on the input WAV path, exiting 1 if power at that frequency exceeds the threshold, else 0. Validate against a few held-out samples for generalization, not just the training corpus, to avoid overfitting.
