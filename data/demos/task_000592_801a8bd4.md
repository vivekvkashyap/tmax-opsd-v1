Work the three parts independently and test each in isolation before wiring them together. First, implement `/home/user/sanitize.py` reading stdin fully and writing stdout, applying the four transforms in the exact order specified: (1) strip lines starting with `X-Internal-Trace-ID: ` — do this via line-based filtering, not a naive substring replace, so partial matches mid-line aren't touched; (2) find the `Subject: ` line and insert `X-Processed-By: SecRelay-v1` as the very next line, or prepend it as the first line if no Subject line exists — handle both cases explicitly; (3) do a plain string replace of `http://insecure.local/` with `https://secure.local/`, order matters relative to step 1/2 since headers could contain the URL too; (4) normalize `\r\n` to `\n` — but decide carefully whether normalization happens before or after the other steps change line boundaries, since the fuzz oracle will catch order-sensitivity bugs. Test sanitize.py manually with `echo -e | python3 sanitize.py` cases covering CRLF, missing Subject, and repeated Trace-ID headers before moving on. Second, write `relay_daemon.sh` as a simple socat/nc/python listener on 10026 that reads the full payload, pipes it through `sanitize.py`, and forwards to 10027 — test it standalone with `nc` before integrating. Third, in `setup_env.sh`, start `/app/start_services.sh`, then idempotently launch the daemon (check if something is already listening on 10026 first, e.g. via `lsof`/`fuser`/pgrep, and kill+restart or skip accordingly) so re-running the script doesn't break things, then send a test payload to port 10025 to validate end-to-end flow, checking `/app/egress_out.log` if available.