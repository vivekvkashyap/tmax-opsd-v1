First extract the business rules from the image with OCR: run `tesseract /app/etl_rules.png stdout` and read the text carefully rather than assuming standard rules — the exact validity conditions (amount sign, allowed departments, forbidden characters) live only in that image. Then write `/home/user/filter.cpp` to stream-parse the CSV, buffering rows in memory while validating each against the OCR'd rules — if any row fails, write nothing to output and `exit(1)` (don't write partial output before finding a later bad row). If all rows pass, deduplicate by `TransactionID` using an `unordered_set` of seen IDs, keeping the first occurrence, appending `Duplicate Dropped: <TransactionID>` to the log for each drop. Write the deduplicated rows plus header to the output CSV and `exit(0)`. Compile, then test against every file in `/app/corpus/clean/` (expect exit 0) and `/app/corpus/evil/` (expect exit 1) before considering it done.