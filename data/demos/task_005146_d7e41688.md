Build the four files under `/home/user/polyglot` in order. In `libseq.c`, the leak is the `temp` buffer that's malloc'd but never freed before `compute_sequence` returns — add `free(temp);` right before the `return result;` line. Write `test_mem.c` to `#include "libseq.c"` (or forward-declare and link), call `compute_sequence(100)`, `free_sequence` on the result, and return 0 cleanly so valgrind sees a clean run. Write `verify.py` with `ctypes.CDLL('./libseq.so')`, set `restype`/`argtypes` appropriately (pointer to int, then convert with e.g. `ctypes.cast(ptr, ctypes.POINTER(ctypes.c_int))[:5]`), assert equality to the expected sequence, call `free_sequence`, and print `VERIFICATION_PASSED` only on success (else non-zero exit). In `build.sh`, branch on `$BUILD_MODE` with an if/elif/else defaulting to DEBUG, compile the `.so` with the specified flags, compile+link `test_mem`, run valgrind with `--error-exitcode=1 --leak-check=full` and let `set -e` (or explicit exit-code checks) stop the script if it fails, then run `python3 verify.py` and only write `build_report.txt` with `ALL_SYSTEMS_GO` if every prior step succeeded. `chmod +x build.sh`.