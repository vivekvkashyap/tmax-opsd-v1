Split this into a transcription/extraction stage (any language) and a C-implemented stats+server stage, as required. Transcribe `/app/lab_log_12.wav` with whisper or similar, then parse the spoken text observation-by-observation, pulling out the alpha/beta numeric pair for each; apply the schema rule literally — drop any observation where a value is missing, non-numeric, or the word "invalid"/"error" appears for that field, and feed only the surviving pairs into the C program. In `analyzer.c`, implement OLS slope calculation directly from the standard formula (covariance/variance over the sample), and implement bootstrap by repeatedly resampling the data pairs with replacement (rand()-based indices), refitting the slope each time, and taking the 2.5th/97.5th percentiles of the resulting slope distribution for the CI — 1000+ resamples as specified. For the HTTP server, implement raw socket handling (bind/listen/accept) or a minimal HTTP parser in C: check the `Authorization` header on every request before touching the route, return 401 immediately if it doesn't exactly match, and only reach `/data` or `/stats` handling afterward. Build the JSON responses by hand with sprintf/snprintf. Run the compiled server in the background (e.g. `./analyzer &`) so it stays reachable for verification, and test both endpoints with curl (with and without the header) yourself first.
