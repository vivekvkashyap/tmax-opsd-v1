Start by extracting the hidden config, since correctness depends entirely on it: transcribe `/app/loc_config.wav` (e.g. `whisper`/any available speech-to-text tool, or listen and note it manually if you have audio playback) to recover which region codes are valid and their integer second offsets (positive or negative) — do not guess or hardcode assumed offsets. Then write `/home/user/src/main.rs`: read stdin line-by-line, split each line on commas into `(timestamp: i64, region: String, key: String, word_count: i32)`, look up the region in a small map built from the transcribed offsets, skip the line entirely if the region isn't in that map, otherwise compute `local_timestamp = timestamp + offset`. Buffer all valid records in a `Vec`, then sort with a composite key: region ascending, local_timestamp descending, translation_key ascending (use `sort_by` with a `then_with` chain, being careful to reverse only the timestamp comparison). Print each record as `region,local_timestamp,translation_key,word_count` with no header. Build with `cargo build --release` and copy the resulting binary to exactly `/home/user/loc_processor` (e.g. `cp target/release/<binname> /home/user/loc_processor`). Test with a handful of crafted CSV lines covering valid and filtered-out regions before considering it done, and confirm performance is reasonable for large inputs by using buffered I/O (`BufReader`/`BufWriter`) rather than line-by-line unbuffered prints.
