Treat this as three unrelated sub-tasks and finish each independently before moving on. For the symver script: list `/home/user/libs/*.so.*`, strip the `libcatalan.so.` prefix from each filename to get a version string, split on `-` to separate any pre-release suffix (anything with `-rc`, `-alpha`, `-beta` etc. must be excluded from consideration entirely), then compare the remaining `X.Y.Z` triples numerically field-by-field (not as strings, since e.g. "1.10.0" > "1.9.0" numerically but not lexically) to find the max; symlink `libcatalan.so` to that file and echo its version string into `version.log`. For the Catalan translation: read the C recursive formula in `catalan.c` literally and reimplement it in Bash using an array to memoize `catalan(i)` for i=0..n (plain recursive Bash would be painfully slow and error-prone), using only `((...))` integer arithmetic — mirror the C recurrence exactly rather than substituting the closed-form binomial-coefficient formula, since subtle off-by-one differences would produce wrong results for larger n. For Nginx: write the `limit_req_zone` directive in the `http` block and `limit_req zone=mylimit;` (no `burst`) inside the `location /` block, along with `pid`, log paths, and `proxy_pass` exactly as specified, with no `user` directive since you lack root.
