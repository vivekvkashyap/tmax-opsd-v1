Parse `/home/user/sequences.fasta` by tracking sequence boundaries via `>` header lines, treating each header-to-next-header block as one sequence (sequences may span multiple lines), so you can correctly select "the first N sequences" rather than the first N lines. Maintain a running count of the 20 standard amino acids as you consume sequences in file order; at each checkpoint N in {100,500,1000,2000}, compute `P(i) = count(i)/total_count` from the counts accumulated over exactly the first N sequences, and compute KL divergence with `Q(i)=0.05` fixed, using `std::log` (natural log) and explicitly skipping terms where `count(i)==0` (treat as contributing 0, since `0*log(0/Q)` is undefined numerically). Accumulate counts incrementally across checkpoints rather than re-scanning from the start each time, for efficiency and correctness. Write the four CSV rows with `std::fixed << std::setprecision(6)`. For the hypothesis test, reuse the N=2000 KL value already computed and compare strictly with `>` against 0.005, writing only `H0` or `H1` (no newline issues, no trailing text) to hypothesis.txt. Compile with the exact g++ command given and run the binary once to produce both output files before considering the task done.
