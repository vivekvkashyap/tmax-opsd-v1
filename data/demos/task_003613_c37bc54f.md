Break this into independent pieces and verify each in isolation before wiring them together. First, run `tesseract /app/auth_code.png stdout` (installing tesseract-ocr via apt if missing) to recover the 6-digit passcode — don't hardcode a guess. Next, write the C++ file separately: use `std::thread`/`std::async` per input file for parallelism, and detect UTF-16LE files by checking for a BOM (`0xFF 0xFE`) or by trying UTF-8 decode and falling back; convert UTF-16LE to UTF-8 (e.g. with `<codecvt>` or manual byte-pair conversion) before parsing. For the wide-to-long reshape, treat row 1 as headers (ServerName + ConfigKeys) and row 2 as the single data row's values, emitting one (ServerName, ConfigKey, ConfigValue) tuple per key column. Apply the anonymization check case-insensitively on the key substring. Sort the combined long-format rows by ServerName then ConfigKey before serving. For the TCP server, use raw sockets (bind/listen/accept on 127.0.0.1:8888), read until `\n`, compare the received line against the OCR'd passcode, and either close the socket (wrong) or write the CSV then close (correct). Compile with g++, launch it backgrounded (e.g. `nohup ./config_svc &`), and confirm with `curl` or a manual socket test that it's actually listening before finishing.
