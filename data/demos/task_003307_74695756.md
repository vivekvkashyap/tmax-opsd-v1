Locate the Euler-integration loop in `simulator.cpp` and notice the fixed `dt` is a compile-time constant unrelated to the matrix's actual rates — for an explicit Euler step on a CTMC, stability requires `dt` smaller than the reciprocal of the largest-magnitude diagonal rate in Q (since diagonal entries represent the fastest local rate of change); a `dt` too close to or larger than that bound causes the update to overshoot and can drive probabilities negative, propagating to NaNs after normalization. Replace the fixed `dt` with a value computed at runtime from `Q`'s diagonal (e.g., take the max absolute diagonal entry and pick `dt` as some safety-margin fraction of `1/that value`, comfortably below the stated stability bound), keeping `steps = T/dt` consistent so total simulated time stays `T=10`. Leave the SVD-based stationary-distribution and KL-divergence logic untouched — only the step-size derivation is broken. Recompile with the given g++ flags, run the binary, and confirm `results.txt` shows no `nan` and a KL divergence very close to zero, indicating the empirical distribution converged to the theoretical one.
