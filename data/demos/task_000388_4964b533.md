Treat this as three layered problems: fix a concurrency bug, understand a data model, then build a server on top. For the deadlock, read `transaction.go`'s `AddEdge`: it locks `n1` then `n2` unconditionally, so two goroutines calling `AddEdge(A,B)` and `AddEdge(B,A)` concurrently can deadlock (classic lock-ordering inversion) — fix it by always acquiring locks in a consistent global order, e.g. compare `n1.ID` and `n2.ID` lexicographically and lock the smaller-ID node first, handling the edge case where they're equal. For the data model, read through `dump.nt`'s triples yourself to see which predicates link employees to departments, managers to departments, and employees to salaries, rather than assuming a fixed schema — your aggregation logic in `/summary` must walk those actual relationships. Set up the Go module with `go mod init`, add a `replace graph-etl-engine => /app/vendored/graph-etl-engine` directive so your local fix is used, and load the N-Triples data concurrently via goroutines (e.g. one per entity type) to actually exercise and prove the deadlock fix works. Enforce the exact bearer-token header check before routing, and compute manager/employee_count/total_salary per department query, testing your fixed engine's concurrent load with `go test -race` or repeated runs before considering it solid.
