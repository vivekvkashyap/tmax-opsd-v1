Read `/home/user/etl_pipeline.py` fully before editing to isolate exactly two bugs. Bug 1: `df['batch_id']` has NaNs, so pandas stores it as float64; `.astype(str)` on a float produces `"1.0"` which won't match the string keys in `meta.json`. Fix by filling first (`df['batch_id'] = df['batch_id'].fillna(999)`) then casting to int then to str (`.astype(int).astype(str)`) before the `.map(meta)` lookup, so `1.0` becomes `"1"` not `"1.0"`. Bug 2: `Y = X * W` is element-wise; replace with true matrix multiplication `Y = X @ W` (or `np.matmul(X, W)`) to match the intended linear transform. Keep the rest of the scaling/output logic (multiply by per-row scaling_factors, save as `sample_id,out1,out2,out3` to `/home/user/output.csv`) intact. After fixing, manually trace one row by hand to sanity-check the output matches expected matrix-multiply results before trusting the script. Then write `/home/user/test_reproducibility.sh` (chmod +x) that runs the python script 3 times, computing `md5sum /home/user/output.csv` after each run and appending each full md5sum output line to `/home/user/reproducibility_log.txt` — run it to actually produce the log.