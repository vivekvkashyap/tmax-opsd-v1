Split this into the video-analysis part and the bit-exact tool part, and treat the tool as a fuzz-equivalence problem against an unseen oracle — so match the spec literally rather than approximating. For Part 1, extract frames from `/app/spectroscopy.mp4` with `opencv-python` (`cv2.VideoCapture`) or `ffmpeg`, convert each frame to grayscale, sum pixel intensities per frame, and write the 0-based index of the max-intensity frame to `/home/user/peak_frame.txt` — don't hardcode an index, compute it from the actual video. For Part 2, implement `/home/user/signal_analyzer` (Python with a shebang + `chmod +x` is simplest) that: reads one comma-separated line of floats from stdin; computes the length-3 moving average exactly as specified (`S_i = (X_i+X_{i+1}+X_{i+2})/3`, length N-2) — using `numpy.convolve` with `mode='valid'` and a `[1/3,1/3,1/3]` kernel reproduces this; sets `numpy.random.seed(42)` then does exactly 1000 iterations of `numpy.random.choice(smoothed, size=len(smoothed), replace=True)` to build a distribution of resampled means; takes the 2.5th/97.5th percentiles via `numpy.percentile`; and prints `CI: [lower, upper]` with each value formatted to exactly 4 decimal places. Since the grader compares your output bit-for-bit against a hidden oracle across many random inputs, keep the RNG call order and algorithm identical to this spec rather than an equivalent-but-different implementation (e.g. don't reorder the seed call or change resample size).