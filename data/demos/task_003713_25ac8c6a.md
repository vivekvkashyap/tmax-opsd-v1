Parse the inventory CSV with `awk`/shell, skip the header, then sort/iterate rows numerically by version (don't trust file order). For each file, look up its encoding column and convert to UTF-8 with `iconv -f <encoding> -t UTF-8 infile -o outfile` before diffing — comparing raw bytes across different encodings (e.g. UTF-16LE vs UTF-8) will give bogus diffs. For each consecutive pair of converted files, run `diff -U0 fileA fileB` and count only lines starting with a single `+` or `-` — explicitly exclude the `+++`/`---` header lines (e.g. with `grep -E '^[+-][^+-]'` or checking the line doesn't start with `+++`/`---`). Accumulate these counts into an array/list as you go, and for the rolling average maintain a sliding window of the last 2 transition values (for the first transition, the "average" is just that single value; from the second transition onward, average the current and previous one). Format `rolling_avg` with `printf "%.1f"` or `awk` to guarantee exactly one decimal place, and build the `transition` label as `v{X}-v{Y}` from the version numbers themselves, not hardcoded. Write the CSV with the exact header `transition,changes,rolling_avg`.