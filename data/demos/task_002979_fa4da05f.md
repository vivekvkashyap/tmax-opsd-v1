Work bottom-up: first repair the Makefile in `/app/c_worker/` so `make` produces both `transformer` (from `main_cli.c` + `transform.c`) and `worker_daemon` (from `daemon.c` + `transform.c`) — check for missing object-file rules, wrong source lists, or missing linker flags. Then debug `transform.c` against the documented wire format (4-byte little-endian length prefix, then that many ASCII bytes) and fix parsing/buffer-management bugs (e.g. reading the wrong byte order, off-by-one buffer sizing, or not bounding the read to the declared length) so ROT-13 is applied correctly to letters only and non-letters pass through unchanged. Validate your fix empirically: run both your `transformer` and the reference `/app/reference/transformer_oracle` on the same crafted stdin payloads (vary length and content) and diff their stdout byte-for-byte until they always match — this is faster and more reliable than reading the oracle's binary. Finally, set `/app/gateway/.env` to point the Go gateway at port 8080 and the worker daemon address `127.0.0.1:9000` exactly as specified, since the automated harness will boot the services itself using your built binaries and this config file.