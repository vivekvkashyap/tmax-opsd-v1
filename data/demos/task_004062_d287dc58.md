Fix the build issues before touching logic. For the circular import between `streamer` and `stateparser`, extract the shared type(s) they both need (e.g. an `Event`/state struct) into a third neutral package (or move the dependency direction so only one imports the other) — Go forbids cycles, so one of the two must stop importing the other. For the cgo ABI mismatch, check color.h's exact signature (`GetColorState(unsigned char, unsigned char, unsigned char)`) and make sure the Go call site casts to `C.uchar` (not plain `C.int` or Go `int`) for each argument, matching width and signedness exactly. For frame analysis, extract frames from /app/ui_test.mp4 with ffmpeg (e.g. to raw images or a pipe), and for each frame sample the pixel at the exact center coordinate, passing its R/G/B through your fixed cgo binding to get the 0/1/2 state. Build the run-length grouping by scanning frames in order and starting a new Event whenever the state changes, recording start_frame/end_frame inclusive. Serve the JSON as a single WebSocket text message on connect at ws://127.0.0.1:8080/ws, then close gracefully, and run the server backgrounded so it stays up for the accuracy check (IoU-style frame overlap against ground truth, threshold 0.95).