First transcribe the audio memo (ffmpeg to extract/convert audio, then whisper.cpp or another STT tool) to recover the three parameters it states: the character encoding of the binary file, the rolling-window size N, and the TCP port. Do not guess these — read them off the transcript. Then load `/app/system_metrics.bin` using the recovered encoding (Python or C++ can decode it; note it's just comma-separated text despite the `.bin` extension), and melt/pivot the wide columns (`zone_alpha`, `zone_beta`, `zone_gamma`) into long rows of `(timestamp, zone_name, value)`, sorted chronologically per zone. Implement the rolling average as a trailing window: for each index i include indices max(0, i-N+1)..i (fewer than N values at the start is fine, just average what's available) — don't center the window or require a full window before emitting a value. Write this logic natively in C++17, embedding `httplib.h` to serve `GET /rolling?zone=<name>` on `127.0.0.1:<recovered port>`, returning `text/csv` with header `timestamp,value,rolling_avg` and values rounded to 2 decimals. Compile, launch the server in the background (e.g. with `&` or `nohup`), and verify with `curl` that it's listening before finishing.