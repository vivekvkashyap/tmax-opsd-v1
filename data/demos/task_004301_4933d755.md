Rewrite `filter_paths.c` to check each non-empty line against two conditions independently, not just a naive substring search: (1) absolute path — the line starts with `/`; (2) directory traversal — split the path on `/` into segments and check if any *whole segment* equals exactly `..` (do not just search for the substring `..` anywhere, since a safe relative path like `foo/..bar` or `my..file` must NOT be flagged — only a standalone `..` path component is malicious). Read the file line-by-line, trim trailing newline/whitespace, skip blank lines, and print `MALICIOUS` and exit as soon as any line fails either check; only print `SAFE` if every line passes both checks. Compile exactly with `gcc -o /home/user/app/filter_paths /home/user/app/filter_paths.c`. After that, don't forget the pipeline plumbing: restart `watcher.sh` (kill the old background instance using its stored PID if present, then relaunch it) so it picks up the recompiled binary, since the FTP-upload-triggers-inotify-triggers-filter chain is what the verifier actually exercises end-to-end, not just the binary alone.