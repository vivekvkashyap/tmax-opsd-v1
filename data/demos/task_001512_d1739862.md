Use pandas and implement the pipeline exactly as specified, stage by stage, checking intermediate output at each step rather than writing one monolithic script. Load `/home/user/raw_sales.csv`, then apply `unicodedata.normalize('NFKC', str(x))` to `Product_Name_Local` to create `Normalized_Name` — this is required because the source column mixes composed/decomposed and full-width Unicode forms that must be canonicalized before grouping, otherwise visually-identical products will be treated as distinct. Reshape wide-to-long with `pd.wide_to_long` using stubnames `Sales`/`Temp`, index columns `Date`, `Product_ID`, `Normalized_Name`, and suffix pattern matching the region codes (NA/EU/AS) — verify the resulting `Region` column values look right. Sort by `Region` then chronological `Date`, and interpolate missing `Temp` values per-region using `groupby('Region')['Temp'].transform(lambda g: g.interpolate(method='linear'))`, then round to 2 decimals — do this before aggregation, not after. Aggregate by `Region`/`Product_ID`/`Normalized_Name` computing sum of `Sales` and mean of interpolated `Temp` (rounded to 2 decimals), then sort by `Region` ascending, `Total_Sales` descending, `Product_ID` ascending as tiebreaker. Write to `/home/user/summary.csv` with exactly the specified column order and names.