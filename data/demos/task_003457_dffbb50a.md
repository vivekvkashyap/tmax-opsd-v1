Split this into a two-phase pipeline. Phase 1 (OCR recovery): extract frames from the video with `ffmpeg`, run `tesseract` on each, and regex-match `INSERT_EDGE: (\d+) -> (\d+)` — discard non-matching noise/partial lines, dedupe, and write `u,v` pairs to `/home/user/edges.csv`. Try multiple `--psm` modes or contrast/threshold preprocessing if accuracy is poor, since exact edge recovery is graded. Phase 2 (algorithmic optimization): rewrite the C program to build an adjacency list (dynamic vectors or CSR-style arrays) from `edges.csv` in O(V+E), then compute 2-hop path counts per source node by walking each node's neighbors and their neighbors via the adjacency list instead of nested O(E^2) scans over a flat edge array — that's the core fix. Keep the exact same rolling-sum output format. Compile to `/home/user/query_engine_opt`; since the verifier times it against a much larger hidden dataset, benchmark it yourself on a synthetic large edge list first. Document the before/after Big-O in `/home/user/optimization_report.txt`.