Parse `/home/user/protein.pdb` line by line, keeping only lines starting with `ATOM` where the atom-name field (fixed-width columns 13-16, 1-indexed, i.e. Python slice `line[12:16]`) strips to exactly `CA`. From each matching line, slice the fixed-width coordinate columns precisely as specified — X at columns 31-38 (`line[30:38]`), Y at 39-46 (`line[38:46]`), Z at 47-54 (`line[46:54]`) — and `float()` each substring; do not rely on splitting by whitespace, since PDB columns can abut with no space between negative numbers and neighbors. Write the header `X,Y,Z`, then one row per CA atom formatted to exactly 3 decimal places (e.g. `f"{x:.3f}"`), preserving file order. After all atom rows, append one final row containing the arithmetic mean of all collected X, Y, and Z values respectively, also formatted to 3 decimals — compute the mean only after collecting every CA atom, don't average incrementally in a way that introduces rounding drift. Save to `/home/user/features.csv`.
