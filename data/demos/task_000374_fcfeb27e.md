Build the bash script as a per-line parsing pipeline over the combined log format, being precise about field boundaries: the quoted request field is `"METHOD URL PROTOCOL"`, so extract METHOD and URL by splitting on whitespace inside the quotes (careful that URLs themselves contain no spaces here, so this is safe), and STATUS is the field right after the closing quote. Compute url_len as the literal character count of the URL substring (use e.g. `${#url}` or `wc -c` minus newline), param_count by counting literal `=` occurrences (e.g. `grep -o '=' <<<"$url" | wc -l`), and suspicious via a case-insensitive substring test against eval/base64/script (e.g. `shopt -s nocasematch` or `grep -iE`). For tokenized_url, replace every character outside `[A-Za-z0-9]` with a space using `sed -E 's/[^A-Za-z0-9]/ /g'`, then squeeze repeated spaces (`tr -s ' '`), trim leading/trailing spaces, and lowercase the result — apply these transformations in that exact order to avoid double-spacing artifacts. Write the CSV header first, append one row per log line preserving field order, then separately append/create the experiment_log.csv tracking row using counts computed during the same pass. Make the script executable and run it once with `run_baseline` before finishing.
