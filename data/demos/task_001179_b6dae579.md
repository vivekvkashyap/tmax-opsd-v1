Fix the C code with the actual limits in mind, then build the integration layer, then the service. In evaluator.c, replace the fixed `int stack[10]` with a heap-allocated or sufficiently large stack (size proportional to max tokens, e.g. 2001) and replace the fixed `char buffer[256]` with a buffer sized to the actual input length (`strlen(expr)+1`, heap-allocated or a large stack array) instead of a hardcoded copy target — also guard against popping an empty stack. Compile it as a shared library (`gcc -shared -fPIC -o libevaluator.so evaluator.c`) and load it from Python with `ctypes.CDLL('./libevaluator.so')`, declaring `argtypes`/`restype` explicitly (char pointer in, int out) so ctypes marshals the Python string correctly. For the web service, use Flask/FastAPI: POST /evaluate parses the JSON body and calls your ctypes wrapper directly, returning `{"result": ...}`; GET /image runs `pytesseract.image_to_string('/app/equation.png')` (try alternate `--psm` values if the default OCR output is noisy/has stray characters), cleans up the extracted text (strip whitespace, drop non RPN-token characters) before feeding it to the same evaluator, and returns the result the same way. Bind to 0.0.0.0:8080, no auth, and test both endpoints with curl (including a large synthetic 1000+ token expression) to confirm the overflow fixes actually hold before finishing.
