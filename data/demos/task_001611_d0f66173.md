Treat this as four independent sub-tasks that only combine at the final `make run` step — validate each before wiring them together. (1) Passphrase recovery: the key is AES-encrypted with a known-format 4-digit PIN, so brute force it — script a loop over 0000-9999 calling `openssl rsa -in server.key -passin pass:$pin -check -noout` (or `openssl pkey`), stopping at the pin that succeeds without a decrypt error, and write that value to the required output path. (2) Certificate chain: concatenate leaf, intermediate, and root certs in the correct order (server -> intermediate, root typically excluded or appended per convention — check with `openssl verify -CAfile root.pem -untrusted intermediate.crt server.crt` to confirm the chain validates), writing the result to `fullchain.pem`. (3) Makefile: fix the `pythn3` typo to `python3` and add `export JWT_SECRET=super-secret-dev-key` before the run command. (4) Code fix: in `auth.py`, change the `jwt.decode(...)` call to pass `algorithms=["HS256"]` only (removing any `"none"` option or `verify_signature: False`), which is what makes the alg=none forged-token test return 401 instead of 200. Only after each piece is individually confirmed, decrypt the key using your recovered passphrase, run `make run` in the background, and curl both the HTTP and HTTPS endpoints yourself with no-auth, valid-JWT, and forged alg=none tokens to confirm the expected 401/200/401 pattern before finishing.