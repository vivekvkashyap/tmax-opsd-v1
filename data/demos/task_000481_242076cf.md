Work this in stages and verify each before moving on. First fix `/home/user/nginx.conf`: inside the `location /artifacts/` block, set `proxy_pass http://localhost:8081;` (note: no trailing path segment after the port, so the `/artifacts/` prefix is stripped and requests reach the upstream server's root) — then start/reload nginx and sanity-check with `curl -I http://localhost:8080/artifacts/bundle.zip` before trusting anything downstream. Download the bundle, then unwrap it layer by layer (`unzip` then `tar xzf`) — don't assume the structure, inspect with `unzip -l` / `tar tzf` first. For the WAL parser, this is a binary format: loop reading a 4-byte little-endian length (`uint32_t`, e.g. via `memcpy`/`reinterpret_cast` or bit-shifting from a byte buffer), then read that many bytes as a UTF-16LE string and convert to UTF-8 (pair each 2-byte LE code unit into a UTF-16 codepoint, then UTF-8 re-encode manually or with `<codecvt>`/ICU if available — avoid shelling out per-record, that will blow the throughput budget). To validate a path is an ELF binary, `open()`/`read()` just its first 4 bytes and compare against `\x7F\x45\x4C\x46` — do this efficiently (e.g. mmap the WAL file itself with `mmap()` for zero-copy record scanning, use buffered I/O, avoid per-line `std::endl`/repeated small syscalls) since the 50MB benchmark file must be processed in under 0.5s.