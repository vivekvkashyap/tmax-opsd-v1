Structure the Go program in clear stages: (1) open `/home/user/data.db` via `database/sql` with `mattn/go-sqlite3`, immediately execute `DROP INDEX IF EXISTS idx_active;` before any SELECT against `departments` — this must run every time the program executes, not just once, since it's the given workaround for the corrupted index; (2) load all department rows into memory and build a parent→children adjacency map; (3) load and parse `/home/user/sales.json`, summing `items[].price` per `dept_id` into a `dept_id -> own_sales` map. For validity, do a top-down traversal from root departments: a department is valid only if `is_active = 1` AND its parent (if any) is valid — recurse and prune whole subtrees the moment an inactive ancestor is hit, rather than checking `is_active` independently per row. Then do a bottom-up (post-order) recursive sum: `total_sales(dept) = own_sales(dept) + sum(total_sales(child) for child in dept.children if child is valid)`. Finally emit only valid departments as `{dept_id, total_sales}` sorted ascending by `dept_id` to `/home/user/summary.json`, matching the exact JSON key names.
