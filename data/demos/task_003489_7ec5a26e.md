Design the C++ detector around a fixed-size sliding window of the last 10 parsed log entries (e.g. a `std::deque` or ring buffer), reading stdin line-by-line so it never loads the whole file into memory. For each new line, parse the JSON to pull out `status` and `bytes` (a minimal hand-rolled or lightweight JSON extraction is fine — no need for a full parser), push it into the window, evict the oldest entry once the window exceeds 10, and only evaluate the anomaly condition once you have a full window of 10. Track a running sum of bytes and a running count of status>=400 as you slide the window (update incrementally on push/evict) rather than rescanning all 10 entries each time, for efficiency on large streams. The moment average bytes > 10000 AND count(status>=400) > 3 simultaneously hold, exit(1) immediately without reading further input. If the stream ends without ever triggering, exit(0). Validate against every file in `/app/corpus/clean/` and `/app/corpus/evil/` by piping each through the compiled binary and checking exit codes match expectations before moving on. For monitoring, write `monitor.sh` to tail the last 100 log lines, pipe into the detector, and append "ALERT DETECTED\n" to the alerts log on exit code 1; install it via `crontab` for user `user` with a `* * * * *` entry.