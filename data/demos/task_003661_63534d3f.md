Start by extracting the ground-truth mapping from the image yourself — run OCR (e.g., `tesseract /app/topology.png stdout`, or open it and read manually if OCR is unreliable) to recover the Service/VNC-Port/Base-Storage-Path table; do not guess these values. Encode that recovered mapping as a small lookup table (dict/associative array) inside `/home/user/validate_vm`. For each rule: (1) parse the JSON config, look up the service, and compare `vnc_port` for an exact match; (2) for the path check, the critical technique is to canonicalize BOTH the `host_mount` and the service's base path with `realpath`/`os.path.realpath` (which resolves `..` and symlinks) before comparing, then check the resolved host_mount starts with the resolved base path plus a path separator (to avoid a bug where `/home/user/vms/auth_data2` wrongly matches prefix `/home/user/vms/auth_data`). Exit 0 only if every rule passes, else exit 1 — wrap all lookups/parsing in error handling that also exits non-zero (e.g., invalid JSON, unknown service). Validate iteratively against the provided `/app/configs/clean/` and `/app/configs/evil/` samples (especially the symlink-trap and traversal cases) before trusting the script against the hidden corpus, since those samples are designed to catch prefix-matching and symlink-resolution mistakes specifically.