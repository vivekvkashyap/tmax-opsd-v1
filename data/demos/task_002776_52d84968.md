Because your encoder will be strictly fuzz-compared byte-for-byte against a hidden reference implementation, prioritize extracting the exact algorithm precisely rather than guessing. Use OCR (e.g. `tesseract`) on the spec image to read the magic header bytes and the described bitwise transform rule, but don't trust OCR blindly — digits and hex characters are easy to misread, so cross-check the recovered rule against any other analysis tools available to you (disassembly/string extraction on the legacy artifact mentioned in the task) until the two sources agree on one consistent transform. Implement the C++ program to: write the magic header bytes first, then for each subsequent input byte apply exactly the confirmed transform (an XOR step and a bit-rotation step, in the confirmed order) before emitting it, using unbuffered/binary-safe I/O (`std::ios::binary`, never `std::cout <<` text formatting) so every byte value from 0x00 to 0xFF round-trips correctly. Gate the WebSocket "BUILD_START" send strictly behind `WS_PORT` being set, and ensure absolutely nothing else (library logs, etc.) touches stdout, since the fuzz comparison is byte-exact. Before trusting your implementation, manually test it against a handful of your own random binary inputs of varying length, including empty input, checking that the header and transform are self-consistent and reversible where expected.
