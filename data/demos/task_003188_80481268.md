First read `/app/metrics-exporter/server.cpp` carefully rather than guessing — find the hardcoded bind-address constant and change it from the wrong loopback variant to `127.0.0.1`, and separately search the request-handling loop for any artificial delay (e.g. a `sleep`/`this_thread::sleep_for` call) that throttles throughput; removing it is what unlocks high requests/sec, not micro-optimizing the rest of the code. Build with the provided `Makefile` to produce `exporter`, copy it to `/home/user/bin/`, and write a systemd **user** unit file (`~/.config/systemd/user/exporter.service`) with `ExecStart=/home/user/bin/exporter`, `Restart=always`, and `RestartSec=2`, then `systemctl --user daemon-reload && systemctl --user enable --now exporter.service`. Write `/home/user/deploy.sh` to take `$1` as the version, write it to `/home/user/version.txt`, and call `systemctl --user restart exporter.service`, then `chmod +x` it. Before declaring done, actually hammer `127.0.0.1:8080/metrics` with a quick load test to confirm throughput clears 1000 req/s — if it doesn't, re-inspect the loop for remaining blocking I/O or thread-per-connection overhead.