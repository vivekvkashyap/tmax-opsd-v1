Build the operator script as a strict sequence of steps in the order the spec implies, and test each step's output before moving to the next. Parse `operator.ini` with `configparser` to get `timezone`. Get the current time in that zone with `zoneinfo.ZoneInfo("Asia/Tokyo")` applied to `datetime.now(tz)` — do not use naive local time or UTC, since the annotation and filenames must reflect Tokyo time specifically. Back up `active/`'s *current* contents (i.e., before any new files are written into it) into a tar.gz named `backup_<that timestamp>.tar.gz` in `backups/`, adding files directly (use `tar.add(filepath, arcname=filename)` for each file in `active/`, not the directory itself, so no nested `active/` folder appears inside the archive). Only after the backup is written, process each `.yaml` in `incoming/`: inject `operator.k8s.io/processed-at: "YYYY-MM-DD HH:MM:SS +0900"` under `metadata.annotations`, creating that block if absent — since this is simple text/YAML editing, either use `pyyaml` (load/dump preserving other keys) or careful line-based insertion matching the `metadata:` block indentation. Write each processed file into `active/`, overwriting same-named files. Then delete everything from `incoming/`, and finally append one log line to `operator.log` matching the exact bracketed-timestamp SUCCESS format with the backup filename and count of manifests processed. Run the script and diff the resulting files/log against the spec's exact formats before finishing.