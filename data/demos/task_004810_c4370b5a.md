Extract the required auth token by running `tesseract /app/auth_badge.png stdout` (installing `tesseract-ocr` if missing) and stripping whitespace/newlines from the result — don't hardcode a guessed token, verify OCR output carefully since misreads (0 vs O, 1 vs I) are common. Build the service (Flask or `http.server`) so `/policy-check` first checks the `Authorization: Bearer <token>` header against the OCR'd value and returns 401 on any mismatch or absence before doing any policy logic. For the privilege-escalation check, parse `/app/auth.log` line-by-line looking for entries containing "command not allowed" where the requesting user matches the `"user"` field from the JSON body — use a substring/regex match on the log line, not a fixed column parser, since log formats vary. For the integrity check, compute `hashlib.sha256` over the raw bytes of `/app/scripts/<script_name>` (handle FileNotFoundError) and compare against the hash-to-filename mapping parsed from `/app/manifest.sha256`, treating any missing file, missing manifest entry, or hash mismatch identically as `checksum_mismatch`. Run policies in the specified order and keep the server running in the background.
