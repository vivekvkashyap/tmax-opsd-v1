Start by running `make` in `/app/cJSON-1.7.15` and reading the actual linker error — a shared library build that fails with a relocation error against a symbol almost always means object files were compiled without position-independent code; find where `CFLAGS` is set in the Makefile and ensure `-fPIC` is present for the objects that go into `libcjson.so`, then confirm `make` produces `libcjson.so` cleanly. Capture that exact change as a unified diff with `diff -u` (or `git diff` if you keep the original around) and save it to `/home/user/cjson-fix.patch`. For the leak, don't guess — build `semver_parser` with `-g` and run it under `valgrind --leak-check=full --show-leak-kinds=definite`; the log will point at the exact allocation sites (e.g., unfreed `cJSON` objects after parsing, and struct nodes allocated per array element in the state machine that are never freed once superseded as the running max). Add the missing `cJSON_Delete()`/`free()` calls at every place an object goes out of scope, including error paths, then re-run valgrind to confirm "definitely lost" reaches exactly 0. Compile `semver_parser` linking against your rebuilt `libcjson.so`, set `LD_LIBRARY_PATH` so it resolves at runtime, run it against `versions.json`, and write the highest version string it reports to `/home/user/highest_version.txt`.
