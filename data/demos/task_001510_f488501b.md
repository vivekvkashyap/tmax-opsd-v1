First get the vendored library building: inspect `/app/libsecauth-1.0/Makefile` for its `LDFLAGS`/link line and check whether the OpenSSL crypto library flag is present — if missing, add it and rerun `make` until `libsecauth.so` compiles cleanly. Then audit the token-parsing source (e.g. `token_parse.c`) for how it decides an algorithm is verified; this is modeled on the real "JWT alg=none" bug, so look specifically for a code path that special-cases an "none"/unsigned algorithm string and returns success without ever calling the OpenSSL signature-verification routine — patch that branch so unsigned/none-alg tokens are rejected (return failure) instead of auto-accepted, while genuine signed tokens still verify correctly through OpenSSL. Rebuild the patched `.so`. Then write `/home/user/detector.c`, include `/app/libsecauth-1.0/include/secauth.h`, call `secauth_validate_token(filepath)`, and print exactly `ACCEPT` or `REJECT` based on its return value; compile it linking against the fixed shared library (set rpath or `LD_LIBRARY_PATH` so it resolves at runtime) to `/home/user/detector`. Test manually against sample tokens in `/app/corpus/clean/` and `/app/corpus/evil/` before finishing, confirming clean all ACCEPT and evil all REJECT.