Read `corpus` and `queries` from `/home/user/data/embeddings.h5` with `h5py`, keeping full float precision (do not round intermediate values). L2-normalize each row of both arrays by dividing by `np.linalg.norm(..., axis=1, keepdims=True)` so every vector has norm 1. Because vectors are normalized, cosine similarity is just the dot product, so compute `queries_norm @ corpus_norm.T` to get a (50, 10000) similarity matrix. For each query, take the top-5 largest values (e.g., `np.sort(row)[-5:]` or `np.argpartition`), and flatten all 50×5 = 250 scores into one array — double check you end up with exactly 250 values. Run `scipy.stats.ttest_1samp(scores, 0.20)` for the two-sided p-value, and use `scipy.stats.t.interval` (or manually with `scipy.stats.sem` and the t critical value) for the 95% CI of the mean. Only round to 4 decimal places at the very last step when writing the JSON to `/home/user/pipeline_results.json`, matching the exact key names required.