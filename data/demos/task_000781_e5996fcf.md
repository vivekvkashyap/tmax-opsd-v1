Build the scanner as four independent checks, each producing a rule-id string on failure, then assemble and sort the results. For the certificate: parse `Not After` and the signature algorithm using `openssl x509 -in <cert> -noout -enddate -text` (via `subprocess`), or Python's `cryptography`/`OpenSSL` bindings if available — compare the parsed expiry against `datetime.now()` (mind timezone-awareness) for `TLS_EXPIRED`, and check the signature algorithm string for `sha1` or `md5` substrings for `TLS_WEAK_SIG`. For headers: load the JSON file, check for a `Content-Security-Policy` key (case-sensitive per the header name as given) for `CSP_MISSING`; if present, lowercase the value and search for `unsafe-inline` or `unsafe-eval` for `CSP_UNSAFE` — note CSP_MISSING and CSP_UNSAFE are mutually exclusive per input. Collect violated rule ids into a list, sort alphabetically, and write `{"violations": [...]}` to `/home/user/scan_report.json` with `json.dump`. Take the cert and headers paths as argv[1] and argv[2], not hardcoded. Test each rule individually against the actual provided `/home/user/cert.pem` and `/home/user/headers.json` before finalizing — don't assume which violations apply without inspecting the real files.
