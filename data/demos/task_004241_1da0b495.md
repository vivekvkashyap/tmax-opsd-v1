First inspect `/home/user/interactive_tool.sh` to see the exact order and wording of its prompts — Expect scripts must `spawn` the real script and use `expect` patterns that match those prompts (or generic patterns like `-re ".*: $"`) followed by `send` of the answer plus `\r`, in the same sequence the script asks (source dir, then destination archive, then confirm). End the Expect script with `expect eof` so it waits for the child to actually finish before exiting, otherwise the C program may check for the archive before it's written. In the C program, use `system("/usr/bin/expect /home/user/automate.exp")` and check its return, then use `access()`/`stat()` on the backup path to decide which log line to append (open the log with `O_APPEND`, don't overwrite it). Compile with `gcc -o /home/user/backup_manager backup_manager.c` and actually execute it once so the backup and log line are produced before finishing. For the fstab line, remember the canonical 6-field format is `UUID=<uuid> <mountpoint> <fstype> <options> <dump> <pass>` separated by whitespace — write exactly one line matching that field order into `/home/user/fstab.out`.