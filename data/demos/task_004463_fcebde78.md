Because correctness is checked by fuzz-equivalence against a hidden oracle across 200 random inputs, focus on making the fix bit-exact rather than merely "close." Read `/app/netcost-1.2.0/processor.cpp` carefully — the loop bound is the bug (an off-by-one that skips the last character of stdin input), so fix the loop condition to iterate over the full string length, rebuild with the provided Makefile/`g++`, and copy/install the resulting binary to `/home/user/bin/netcost` (mkdir -p first). Confirm the program reads a full line from stdin (`std::getline`) and prints a single integer cost to stdout, matching the exact behavior implied by the task description, by testing it yourself against a few known strings and manually summing character codes to sanity-check. For the pipeline script, replace the bare `netcost` invocation with the absolute path `/home/user/bin/netcost` so it works with an empty `PATH`, and change the relative `reports/cost.out` to the absolute `/home/user/reports/cost.out` so output location doesn't depend on cwd. Finally create the symlink `/tmp/qemu-vms/ -> /home/user/vm-logs/` (or the reverse direction implied by which one is the real data — check which directory actually contains the logs before linking) so the script's hardcoded log path resolves.
