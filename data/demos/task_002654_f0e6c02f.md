Treat the four sub-problems as independent and verify each in isolation before wiring them together. (1) To recover the API key from the core dump, use `strings /app/worker.core | grep API_KEY=` (or a Python byte-scan) to locate the `API_KEY=` marker and extract the fixed-length token that follows it — don't hardcode a guess, derive it from the dump. (2) For the deadlock in `audio_processor.py`, read `process_batch()` to find two locks acquired in inconsistent order across threads (classic lock-order inversion); fix it by enforcing a single global acquisition order (or using a single lock / `with` statements acquiring both locks in the same order everywhere). (3) For the Newton-Raphson convergence failure in `math_filter.py`, guard the update step so a near-zero derivative doesn't blow up — e.g. check `abs(f_prime(x)) < epsilon` and fall back (perturb x, switch to bisection, or return best-effort) instead of dividing by ~0. (4) For anomaly detection, run an FFT (e.g. `numpy.fft`) over the decrypted, filtered audio and compare spectral content between a few known `/app/clean/` and `/app/evil/` samples to discover the distinguishing feature yourself (a distinct frequency-domain artifact present only in evil samples); build `detect_anomaly.py` to threshold on that discovered feature, then validate it classifies 100% of both corpora correctly.
