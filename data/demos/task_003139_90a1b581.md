Work in three clearly separated stages and verify each before moving on. (1) OCR: run `tesseract /app/transit_costs.png stdout` (try `--psm 6` if line breaks get mangled) and inspect the raw output carefully — OCR often confuses characters like `$`/`S`, `0`/`O`, or drops the `/GB` suffix, so eyeball the result against the expected `Region: <name>, Cost: $<value>/GB` pattern before trusting it. (2) Parse robustly with a regex such as `r'Region:\s*([\w-]+),\s*Cost:\s*\$([\d.]+)/GB'` applied line-by-line, and store results in a dict keyed by region name. (3) Build the API in a language you're comfortable with (e.g. Python's `http.server` or Flask) bound to `127.0.0.1:8080`, launched detached/backgrounded (`nohup ... &` or a systemd-less background process) so it stays up after your shell command returns. Implement `/health` returning plain `OK` with status 200, and `/cost` that first checks the `Authorization: Bearer FINOPS_SECRET_99X` header (exact match, else 401) before looking up the region (404 if absent, else plain numeric cost text). Test each endpoint yourself with `curl` before finishing, including the auth-failure and not-found cases.
