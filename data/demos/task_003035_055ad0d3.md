Build this as a linear extract-decrypt-exploit pipeline in Bash, verifying each stage's output before chaining to the next. Use `tesseract <image> stdout` (or via a temp file) to OCR the image, then use `grep -oP`/regex to pull the 32-hex-char key after `KEY: ` and the base64 blob after `PAYLOAD: ` — be careful with anchoring so you don't accidentally capture trailing whitespace or newlines from OCR noise (trim with `tr -d`/`sed`). Decrypt with OpenSSL: `openssl enc -aes-128-ecb -d -K <hex_key> -in <(echo "$payload_b64") -base64 -A` (or pipe via `base64 -d` then `openssl enc -d -aes-128-ecb -K <hex_key> -nopad`/with padding depending on how it was encrypted — test both to see which yields printable ASCII). Once you recover the plaintext, build the JSON body with `jq -n --arg q "$plaintext" '{query:$q}'` to avoid shell-escaping bugs, then POST it with `curl -s -X POST -H 'Content-Type: application/json' -d "$body" http://localhost:8080/vulnerable_endpoint` and print the raw response body exactly (no extra echo text). Test end-to-end against the given sample image first, and design the script generically (no hardcoded key/payload) so it generalizes to the hidden test images, since accuracy must be 100%.
