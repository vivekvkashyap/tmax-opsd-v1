Write the C parser carefully around CSV edge cases: split each line on commas, and for each of the three sensor fields treat both an empty string and any string that fails full numeric parsing (e.g. via `strtod` where the end pointer doesn't advance, or leftover non-numeric characters) as invalid — discard the whole row if any of the three sensor fields is invalid, not just sensor1. Compute `gsl_stats_mean`/`gsl_stats_sd` (sample, ddof=1 semantics, which is GSL's default) for sensor1 over the clean rows first — these are the values you report BEFORE outlier removal. Then apply the outlier filter with a strict `>` comparison (`|sensor1 - mean| > 2.0*sd` means reject) and compute sensor2/sensor3 means only over what remains, reporting valid_rows as that post-filter count. Format every float to exactly 4 decimal places in the two-line CSV output. Write the Makefile linking `-lgsl -lgslcblas -lm` and a run.sh that builds then executes; test end-to-end against the actual sensor_data.csv and eyeball that valid_rows and the means look sane before finishing.
