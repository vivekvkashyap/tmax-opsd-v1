Do the filesystem setup first and verify each permission with `stat -c "%a" <path>` after `chmod`, since typos (e.g. `700` vs `0700` string vs actual octal mode) are the easy way to fail this task — write the JSON content with `printf` (no `echo`, which may add a newline) to avoid a trailing newline. For the C checker, use `stat()` on both paths, mask the returned `st_mode` with `S_IRWXU|S_IRWXG|S_IRWXO` (or `0777`) to get just the permission bits, and compare against `0700` and `0400` respectively — also handle the case where `stat()` fails (ENOENT) by printing `INSECURE`, not crashing. Print exactly `SECURE\n` or `INSECURE\n` with no extra output. Compile with `gcc -o /home/user/check_perms /home/user/check_perms.c`. Test it yourself by temporarily chmod'ing the file/dir to something else and confirming it reports `INSECURE`, then restore the correct permissions before finishing. For `verify.sh`, export `TZ=UTC` and `LC_ALL=C` (or set them inline on the command) before invoking `/home/user/check_perms > /home/user/security_status.txt`, then actually run it so the output file exists.
