Start by extracting the threshold from the image with `tesseract /app/profile_spec.png -` (or `stdout`), then parse the line matching `THRESHOLD:` (or similarly-named key) to pull the numeric value programmatically rather than hardcoding a guessed number — read whatever OCR actually returns and sanity-check it looks like a plausible float before using it in your script. Build the classifier pipeline exactly as specified and in order: load the CSV, apply `scipy.signal.savgol_filter(counts, window_length=11, polyorder=3)` and clip negatives to zero, integrate with `numpy.trapz(smoothed, latency_ms)` and divide by that integral to get a proper PDF (area = 1), build the ideal Normal(120, 20) PDF on the same grid (`scipy.stats.norm.pdf`), then compute the Wasserstein distance between the two densities — using `scipy.stats.wasserstein_distance` with the grid values as `u_values`/`v_values` and the normalized densities as `u_weights`/`v_weights` is the correct call signature (not simply comparing raw sample arrays). Compare against the OCR'd threshold and `sys.exit(1)` if strictly greater, else `sys.exit(0)`. Test your script against a couple of synthetic clean/shifted profiles you construct yourself to confirm behavior before trusting the corpus run.
