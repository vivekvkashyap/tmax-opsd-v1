Write `/home/user/build_system.py` as a small parser+codegen+test-harness, in this order: (1) read `/home/user/formulas.txt` line by line and evaluate `ALPHA` and `BETA` using Python's own arithmetic evaluator (e.g. `eval()` on the RHS after substituting previously-resolved constant names, or a simple recursive-descent parser) — do this generically from the file's expressions rather than assuming the arithmetic result, since the grader recomputes ALPHA/BETA from the same formulas independently; (2) for the `compute(x) = ...` line, substitute the resolved integer ALPHA/BETA values textually into the expression to generate both a C implementation (`impl.c`, reading `argv[1]` via `atoi`, printing the integer result) and a Python implementation (`impl.py` with `def compute(x): return ...`) — keep the polynomial structure identical in both; (3) compile with `gcc /home/user/impl.c -o /home/user/impl_bin`; (4) loop x=1..5, run `impl_bin` as a subprocess capturing stdout and also call `impl.compute(x)`, collecting both result arrays; (5) write `/home/user/validation.json` with keys `ALPHA`, `BETA`, `c_results`, `py_results`, `match` (true only if every element pairs up exactly). Run the script and confirm the JSON is valid and `match` is true — if not, the bug is almost certainly in operator precedence or integer truncation during substitution.
