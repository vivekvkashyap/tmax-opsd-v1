Follow the pipeline steps in order and watch for pandas dtype pitfalls, since the grader checks both the parquet schema and exact JSON values. Load `/home/user/data.csv`, add `Ratio = Feature_A / Feature_B` (this will produce `inf` for the zero-`Feature_B` rows), then `df.replace([np.inf, -np.inf], np.nan)` followed by `df.dropna()` to remove all NaN-containing rows including the ones seeded with missing `Target`. After dropping, explicitly cast `ID` back to `int64` with `.astype('int64')` — pandas silently upcasts int columns to float once NaNs have passed through the frame, so skipping this cast is the most common way this task fails even though the data itself has no NaNs left; also explicitly cast the feature/ratio/target columns to `float64`. Save with `df.to_parquet(...)` and verify the schema with `pd.read_parquet(...).dtypes` before moving on. For the modeling step, build a `Ridge` + `GridSearchCV(cv=5, scoring='neg_mean_squared_error')` over `alpha in [0.1, 1.0, 10.0]` using features `['Feature_A','Feature_B','Ratio']`, extract `best_params_['alpha']` and the positive (negated) mean CV MSE rounded to 4 decimals, and write both to `/home/user/experiment.json`.