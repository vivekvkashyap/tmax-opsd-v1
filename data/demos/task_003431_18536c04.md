Tackle this in layers, testing each independently before wiring them together. First build `sanitize_chunk(filepath)` in `/home/user/archiver/sanitizer.py` purely as a binary/JSON parser: open the file in binary mode, check the first 4 bytes equal `b'BKP1'`, read the next 4 bytes as a little-endian unsigned int (`struct.unpack('<I', ...)`) for header length and reject if it exceeds 1024, read exactly that many bytes and `json.loads` them (reject on any parse error), check `source` key exists and is not `"unknown"`/`"test"`, then read the remaining bytes as payload and compare `hashlib.sha256(payload).hexdigest()` against the header's `checksum` field (reject on missing or mismatch). Validate this in isolation against `/home/user/archiver/corpus/clean/` and `/home/user/archiver/corpus/evil/` before touching the services. Only then edit `api.py` and `worker.py` to use `redis.Redis(unix_socket_path=...)` instead of TCP defaults, and have the worker call `sanitize_chunk` on each pulled chunk, appending valid payloads to `master_archive.bin` and logging invalid filenames to `rejected.log`. Start Redis, then the Flask app, then the worker, each in the background, and confirm with a manual curl POST before considering it done.