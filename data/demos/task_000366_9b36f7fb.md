Fetch and extract Eigen 3.4.0 into `/home/user/eigen_src` first (it's header-only, so compiling just needs `-I` pointed at the extracted `Eigen/` directory) and confirm a trivial Eigen program compiles before writing the real logic. Parse the PDB by filtering lines where the atom-name field (columns 13-16, fixed-width) equals `CA`, extracting the x/y/z floats from their fixed columns (don't split on whitespace alone — PDB is column-based and can have no spaces between fields). Parse the FASTA sequence length and assert it matches the CA count, erroring out otherwise. Build the NxN Euclidean distance matrix, load it into an `Eigen::MatrixXd`, and run `Eigen::JacobiSVD` (or BDCSVD) to get singular values, which Eigen already returns sorted descending — take the top 3 and format to exactly 4 decimals. For the KNN graph, for each residue sort all other residues by distance ascending, breaking exact ties by smaller index (stable sort by (distance, index) pairs), and take the first 3. Write singular values as the first CSV line, then one line per residue in PDB order as `index,nn1,nn2,nn3`. Compile with a C++ compiler pointing `-I` at the Eigen headers, run the binary, and confirm `/home/user/output/features.csv` is produced (creating the output directory beforehand).
