Start the stack with `/app/start_services.sh` and connect to `research_db` (via `psql` or a quick `psycopg2` script) to inspect the actual query patterns in `/app/corpus/clean/` and `/app/corpus/evil/` before creating indexes — the clean corpus uses a recursive CTE walking `citations` and a window function partitioned/ordered over `papers`, so build indexes that let Postgres avoid sequential scans for those specific access patterns: an index on the join/filter column(s) used in the recursive step of `citations`, and a composite index on `papers` matching the columns used in the `WHERE`/`PARTITION BY`/`ORDER BY` of the window query. After creating indexes, run `EXPLAIN` yourself on each clean query to confirm no `Seq Scan` remains and total cost is comfortably under the threshold; also run it on the evil queries to confirm they still show a `Seq Scan` or high cost (since they use `LIKE '%...%'` or a cross join that no index can fix). For `/home/user/query_filter.py`, read the SQL from the given file, run `EXPLAIN (FORMAT JSON) <query>` via psycopg2, recursively walk the returned plan-node tree looking for any `Node Type` equal to `Seq Scan` on the `papers` or `citations` tables, and check the top-level `Total Cost` against 50000 — print `REJECT` if either condition triggers, else `ACCEPT`. Test it directly against both corpora before finishing.