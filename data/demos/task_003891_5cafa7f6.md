First fix the vendored dependency before writing any pipeline code: grep the `category_encoders` source tree (especially `category_encoders/target_encoder.py`) for a broken import — a typo like an extra letter on `import numpy as np` is a classic injected bug that produces a `NameError` only when the class is instantiated, not at file-parse time, so actually try `from category_encoders import TargetEncoder; TargetEncoder()` to surface it rather than just eyeballing the file. Fix the typo, then `pip install -e /app/category_encoders` and re-verify the import works before touching the ML script. For the pipeline: load train/test CSVs, fit `TargetEncoder` on `job_title`/`city` using the target column (fit only on train, transform both), impute or drop missing values, and train a tree-based regressor with cross-validation. Log hyperparameters and CV scores to the required JSON. To hit the R² bar reliably, use k-fold CV to tune a couple of hyperparameters (e.g. depth/leaf count) rather than accepting defaults blindly, and double check `predictions.csv` has exactly the required column name and row order matching `test.csv`.