Approach this as classic ddmin (delta debugging), not manual guessing. Write a script that: given a candidate subset of lines (preserving original order), writes it to a temp file, runs `/home/user/vuln_bin <tempfile>`, and checks whether the process died from SIGSEGV (in bash, `$?` will be 139 = 128+11; in Python use `subprocess.run` and check `returncode == -11`). Implement the standard ddmin algorithm: start with the full line list, repeatedly try removing each of N equal chunks (start with N=2, doubling N on success, halving granularity on failure) and keep the crash-preserving reduction, iterating until no single line can be removed without losing the crash — because this bug is a race condition (a background thread frees a buffer that another thread still writes to), re-run each candidate a few times before declaring it "does not crash", since removing a helper line may only reduce the crash probability rather than eliminate it. Save the final minimal, order-preserved lines to minimal.txt. Once minimized, reproduce the crash under `gdb -batch -ex run -ex bt --args /home/user/vuln_bin minimal.txt` and read the backtrace's innermost user function name (not a libc frame) into crash_func.txt. For poc.py, spawn the binary via subprocess, check the negative-signal return code, and exit 0 only on that specific crash signature.
