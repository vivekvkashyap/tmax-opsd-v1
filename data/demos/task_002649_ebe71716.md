Run `tesseract /app/config_spec.png stdout` (install `tesseract-ocr` if needed) to recover the base directory and hash salt text exactly as printed — copy them character-for-character into your Go constants, since a typo breaks every hash the fuzzer checks. Implement the manifest parser to match Go's `path` semantics precisely: read stdin with `bufio.Scanner`, `strings.TrimSpace` each line, skip empty lines and lines starting with `#`, and only treat lines starting with exactly `"+ "` or `"- "` as directives. Resolve relative paths with `path.Join(baseDir, p)` and always run the result through `path.Clean` (for absolute and resolved-relative paths alike) so `.`/`..` normalize identically to the reference. Use a `map[string]bool` keyed by cleaned absolute path: `+` inserts, `-` deletes the key entirely, so later re-inclusion just re-adds it. At the end, sort the map keys with `sort.Strings`, and for each print `hex(sha256(path+salt))` then a space then the path (`%x` formatting). Build with `go build -o /home/user/doc_builder`, then hand-test with manifests containing comments, blank lines, and mixed relative/absolute include/exclude lines before trusting the fuzz suite.