Build this as three independent, testable stages rather than one monolith. First, write `/home/user/api.py` (Flask/FastAPI) that loads `/home/user/vectors.npy` once at startup, and on `POST /search` normalizes the query vector and the matrix rows and computes cosine similarity via a single vectorized dot product (`(V @ q) / (norms * ||q||)`), then returns the indices of the 5 largest similarities using `argsort`/`argpartition` — sanity-check by querying with a vector copied exactly from the matrix and confirming its own index comes back first. Second, write `benchmark.sh` so it starts `api.py` with `OMP_NUM_THREADS=1 python3 api.py &`, polls the port (e.g. curl loop or `wait-for-it`) until it accepts connections before sending requests, times each of 200 sequential POSTs individually (e.g. via `time` per request or Python's `time.perf_counter` in a small client loop), kills the server, then repeats with `OMP_NUM_THREADS=4`. The common naive mistake is firing requests before the server is ready or running requests concurrently instead of sequentially, which corrupts the latency measurements. Third, `analyze.py` should use `scipy.stats.ttest_ind(..., equal_var=False)` for Welch's t-test and derive the CI from the same Welch degrees-of-freedom/standard-error formula, writing JSON with native Python floats/bool (not numpy types, which aren't JSON-serializable).
