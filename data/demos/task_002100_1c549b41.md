Reverse-engineer the `.parc` format empirically before writing any extraction code: run `xxd`/`strings` on a sample archive to spot a fixed magic-byte header, and use `ltrace`/`strace` on `/app/unpacker` while it processes one small file to watch the sizes/offsets of its `read()`/`malloc()`/`write()` calls — this reveals the record layout (a length-prefixed filename followed by a length-prefixed compressed blob per entry) and hints at the compression scheme's structure (look for small, regular chunk sizes in the reads, which is characteristic of a simple byte-oriented encoding rather than a real compressor). Confirm your hypothesis by decoding one known small file by hand and diffing the result against what `/app/unpacker` extracts for that same file before committing to the format in code. In your C++ extractor, the critical security requirement is zip-slip prevention: when writing each extracted file, strip all directory components from the stored path (e.g., `std::filesystem::path(name).filename()`) and write only into `/home/user/output/` — never concatenate the raw stored path onto the output directory. For performance, parse archives concurrently with a thread pool (one thread per file or per archive, bounded to hardware concurrency) and use buffered/mmap'd I/O; since duplicate base filenames should overwrite (last-writer-wins), guard concurrent writes to the same output path with a per-filename lock or a global write mutex to avoid corrupted partial writes. Compile with optimizations (`-O2`) and benchmark against the sequential bash+unpacker baseline to confirm you're meaningfully faster.