Tackle this in the order given since later steps depend on earlier ones running correctly. First, inspect the core dump at `/app/core/core.aggregator` with `strings` (or `gdb` if you want the exact crash frame) to locate the malformed telemetry string that triggered the crash, and write it verbatim to `/home/user/poison_pill.txt`. Second, read `server.cpp` for the concurrency bug: look for a mutex that is locked but not guaranteed to unlock on every exit path (e.g. manual `lock()`/`unlock()` instead of RAII) — convert it to a scope-based lock (`std::lock_guard`/`std::unique_lock`) so it always releases, and move any blocking file I/O (e.g. `fsync`/synchronous `write`) outside the critical section so concurrent ingestion doesn't serialize on disk access. Third, edit `config.ini` and `config.json` so the ports match the task's specification exactly (Redis 6379, aggregator ingest 8080 / query 8081, dashboard 9000), keeping the dashboard's aggregator address consistent with the aggregator's query port. Then `make` the aggregator, run `start_all.sh`, and manually smoke-test by opening a few concurrent TCP connections to 8080 and confirming an HTTP GET to `/api/stats` returns consistent, non-deadlocked aggregation before considering the fix done.