Start by recovering the password with OCR (`tesseract /app/emergency_password.png`) — don't hardcode or guess the string, read it off the actual OCR output and use it verbatim in the `unzip -P` command to extract the legacy source. Review that source to find the concrete CWEs: unsafe fixed-size buffer functions (e.g. `gets()`/`strcpy()`) that need bounds-checked replacements, and a missing or incorrect permission check on the input credentials stream. Implement your rewrite strictly from the stated command-line contract: two args (`username`, `new_password_hash`), a passwd-like file read from stdin. Before touching stdin, `fstat` `STDIN_FILENO` and abort with a clear error and non-zero exit if group/other permission bits are set on the underlying file, since the spec requires the tool to refuse insecure permissions. Parse the input line by line into fields, update only the hash field for the matching username, leave other lines untouched, and re-emit the file in the same format including trailing newline. Since your program will be fuzzed for exact byte-for-byte equivalence against a hidden reference implementation, be disciplined about matching conventional Unix tool behavior precisely (error message wording, exit code values, whitespace/newline handling) rather than guessing — build a battery of your own edge-case tests (empty file, unknown user, malformed hash, boundary permission bits) to shake out ambiguity in your own implementation before considering it done.
