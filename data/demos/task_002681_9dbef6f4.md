Start by reading `audio_filter.c` line by line looking specifically for the classic causal-filter off-by-one: computing `y[n] = x[n] + x[n-1] + x[n-2]` without guarding `n-1`/`n-2` against negative indices for the first two samples, plus any uninitialized accumulator or missing bounds check on the sample count read from the WAV header. Fix it so out-of-range prior samples are treated as 0, matching the causal moving-average definition exactly (careful with integer truncation on the /3 — match it consistently to how the reference would round). For the build script, compile twice: once with `-fsanitize=address,undefined -g` for the test pass, and only after that binary survives your generated random-size fixtures (with a minimal but valid WAV header wrapped around `/dev/urandom` bytes) with no sanitizer output or nonzero exit, recompile with `-O3` for the release binary. Make every step check its own exit status and `exit 1` on failure so the script overall fails fast. Finally run the release binary against `/app/test_speech.wav` to produce `/home/user/filtered.wav`, keeping the same WAV header/format/length as the input — only the sample data changes.
