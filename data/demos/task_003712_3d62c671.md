Build the C HTTP server with a minimal blocking socket loop (bind/listen/accept on 0.0.0.0:9000), parse just enough of the request line and headers to get Content-Length, then read exactly that many body bytes — don't rely on a single recv() returning everything. For CSV parsing, implement a proper quote-aware state machine: track whether you're inside a quoted field so that a bare `\n` while inside quotes is treated as an embedded newline (mark that whole logical row for dropping) rather than a row terminator; a naive line-by-line split will misparse these rows and also corrupt the row after them. After identifying valid rows, melt each `timestamp,sensor1,sensor2,sensor3` row into three `timestamp,sensor_name,value` lines, preserving row order for the eventual response ordering. Pipe this data to `/app/legacy_scorer` via a child process (fork/exec with pipes, or popen), writing to its stdin and reading its stdout line by line — don't just call it once per request without proper stream synchronization. Match returned scores back to their originating rows by position/order, build alerts only for `score == 1` using the exact template, and write the HTTP response with correct Content-Length and Content-Type headers. Test incrementally with curl before wiring in the scorer.