Work the three failures independently and verify each in isolation before wiring the system together. (1) Linking: run `make` in `/home/user/app/backend`, read the linker error carefully — a common cause is placing `-lcrypto_custom` before the object files that reference its symbols on the link line; reorder so objects/sources come before the `-l` flags (GCC/G++ resolves libraries left-to-right) and re-run `make` until `backend_server` is produced. (2) Algorithm equivalence: don't guess the transformation — treat `/opt/oracle/backend_server_oracle --cli <input>` as a black box, feed it varied strings (short/long/special chars/unicode-adjacent bytes) via the CLI mode, and diff outputs against hypotheses systematically (try common reversible transforms: base64, XOR with single-byte keys, bit rotations, combinations) until you find a transform that reproduces the oracle's output byte-for-byte across many probes; implement exactly that in `transform_string` in `algo.cpp`, then fuzz-test your own binary against the oracle CLI with a script before trusting it. (3) Config: edit `/home/user/app/gateway/config.json`, change the backend URL's port to 9090 and the rate limit from 0 to 100. Finally start `./backend_server` in the backend dir and `go run main.go` in the gateway dir, and confirm end-to-end with a curl through port 8080.
