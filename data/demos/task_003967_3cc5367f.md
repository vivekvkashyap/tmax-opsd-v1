First diagnose the vendored struccalc build failure by running `make` and reading the error — Makefiles require literal tab characters to indent recipe lines, so if the `all:`/`install:` targets use spaces the build fails with a "missing separator" error; fix by replacing the leading whitespace on those recipe lines with real tabs. Next inspect matrix_solve.c's `factorize_covariance` function: it inverts a 3x3 covariance matrix directly, which blows up (NaN/div-by-zero) when the matrix is near-singular (e.g. from flat/degenerate structures); patch it to add the Tikhonov term 1e-5 to the three diagonal entries (mat[0][0], mat[1][1], mat[2][2]) before performing the inversion/factorization, then rebuild and `make install` to /home/user/local. Then write filter_pdb.c to link against the fixed library, parse each .pdb file, call `compute_wasserstein_dist` against /app/reference/ideal.pdb, and print STABLE if distance <= 2.5 Å and parsing succeeds, else UNSTABLE (including on any parse failure). Validate 100% correct classification against both /app/data/clean and /app/data/evil before finishing.
