Work this in strict stages. First, extract the patch: run `ffprobe -show_format /app/security_feed.mp4` to read the `title` metadata tag, base64-decode its contents to a `.patch` file, then `git apply` (or `patch -p1`) it against `/home/user/secure_router`. Second, fix the resulting Rust build: the patch converts `Router`/`Route` fields from `String` to `&str`, so every struct, impl block, and function signature touching them needs an explicit lifetime parameter (e.g. `struct Router<'a> { ... }`) that ties the borrowed slices back to the original input URL string's lifetime — iterate `cargo build` error-by-error rather than guessing lifetimes upfront, since the compiler's suggestions are usually close to correct here. Third, open `/home/user/secure_router/asm/validator_x64.asm` and translate it instruction-by-instruction into Rust in `src/validator.rs`: identify the loop over string bytes, the rolling-checksum register operations (shifts, XORs, adds), and the final comparison/return condition, and reproduce that arithmetic exactly (matching integer widths and wraparound behavior) rather than approximating it. Finally `cargo build --release` and manually test `url_processor` against a range of hand-crafted URLs — valid tokens, invalid tokens, edge-case lengths — to sanity-check ACCEPT/REJECT and path normalization before relying on the fuzz-equivalence check.