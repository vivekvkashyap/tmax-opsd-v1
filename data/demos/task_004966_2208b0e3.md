Start by running mock-cron.sh as-is to see it fail in the sanitized `env -i` environment (no PATH, no MANIFEST_DIR) — that confirms the failure mode you're fixing. Create /home/user/.operator_profile that does `export MANIFEST_DIR=/home/user/operator-data` and `export PATH=/home/user/dummy-bin:$PATH`, then edit operator-sync.sh so its very first executable line (right after the shebang) is `source /home/user/.operator_profile` — leave every other line of that script untouched since the task forbids changing its logic. Build the directory skeleton `mkdir -p /home/user/operator-data/inputs` (outputs/ gets created by the operator script itself), then symlink rather than copy: `ln -s /home/user/legacy-manifests /home/user/operator-data/inputs/legacy` — the operator script globs `$MANIFEST_DIR/inputs/legacy/*.yaml`, so the symlink must resolve to a directory containing those files. Write check_metrics.sh to test 127.0.0.1:8080 (curl -sf or nc -z), and on success both print `METRICS_UP=1` to stdout and append that exact line to /home/user/operator-data/outputs/status.log; chmod +x it. Run mock-cron.sh again and confirm compiled.yaml and operator.log now appear before running check_metrics.sh, then verify every required output path exists with the expected content.
