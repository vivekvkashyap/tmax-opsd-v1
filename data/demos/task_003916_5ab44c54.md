Break this into independent, individually-testable pieces before wiring them together. First, `tesseract /app/intercepted_hash.png stdout` to recover the leaked hash string (try different `--psm` values if the first pass is noisy); once you have a crypt-format hash (e.g. `$1$salt$...`), brute-force the 4-digit space (0000-9999) by hashing each candidate with the matching scheme (e.g. `openssl passwd -1 -salt <salt> <candidate>`) and comparing to the recovered hash — do this as a tight loop, not by inspection. Second, write `/home/user/generate_csp.sh` to take `$1` (webroot) and `$2` (header file): iterate immediate subdirectories with a `for dir in "$1"/*/` loop, use `stat -c "%a"` or `find ... -perm -0002` to detect world-writable dirs (append `script-src-elem 'none' /<dirname>;`) and `find ... -perm -1000` to detect the sticky bit (append `object-src 'none';`), building up the CSP value starting from `default-src 'self';`. Third, use `awk` with CRLF-aware record separators to inject the `Content-Security-Policy` and `X-Admin-Pin: <cracked_pin>` header lines immediately before the blank line separating headers from body in `$2`, preserving all other lines and CRLF endings exactly. Test against a few hand-built header files and directory trees (including a world-writable and a sticky dir) before trusting the script; `chmod +x` it when done.