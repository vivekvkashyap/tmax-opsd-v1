First, actually get the instructions out of `/app/instructions.wav` — inspect it with `ffprobe`/`file` to confirm it's audio, then run it through a speech-to-text tool available in your environment (or a Python STT library) to produce a transcript; don't guess the instructions from the task description alone, since the real split sizes, chunk names, and merge order live only in the audio. Turn whatever the transcript specifies into a simple, unambiguous `config.ini` (e.g. `[split] file=..., chunk_size=..., prefix=...` and `[merge] files=..., output=...`) that your C program can parse without a real INI library — hand-rolled line parsing with `fgets`/`sscanf` is fine. In `doc_manager.c`, implement the split as sequential fixed-size reads/writes (careful with the final, shorter chunk) and the merge as a simple concatenation in the specified order, then for every output file shell out via `popen("sha256sum <file>", "r")` (or link libcrypto) to get its checksum, and build the JSON manifest array `[{"filename":...,"hash":...}, ...]` by hand or with a lightweight JSON writer. Compile to `/home/user/doc_manager`, run it, and diff the actual byte sizes/count of each output file against what the transcript said before trusting the manifest.
