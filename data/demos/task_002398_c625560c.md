Tackle this in three independent stages and verify each before moving to the next. First, the vendored zlib build fails because of an injected bogus flag in its Makefile — inspect `/app/vendored/zlib-1.3.1/Makefile`'s CFLAGS line for anything that doesn't belong there, remove it, then run `make` to confirm `libz.a` is produced. Second, diff the C++ fingerprint logic against the Python reference line by line rather than guessing: the checksum call must use zlib's proper two-argument seeded-crc pattern; watch particularly for a narrow integer type used to accumulate the running polynomial value across the modulus multiplication — Python ints never overflow, so any narrower C++ accumulator type will silently wrap and diverge from the reference once values get large, so match Python's unbounded-int behavior with a wide-enough integer type throughout the modular multiply-accumulate loop. Validate by computing fingerprints for several byte strings both via `reference.py` and your compiled logic and diffing them directly. Third, build and link `server.cpp` against your freshly built `libz.a`, and run a small manual HTTP POST test against `127.0.0.1:9090/compute` before leaving it running in the background for the grader.
