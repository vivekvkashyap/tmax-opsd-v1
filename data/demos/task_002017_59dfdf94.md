Start by reading `/app/ssh_log_parser/parser.py` end to end rather than guessing: locate the regex/constant that whitelists SSH key types and any status-check function. A common bug pattern is a regex that accepts a deprecated algorithm (e.g. `ssh-dss`) while failing to match modern ones (e.g. `ssh-ed25519`, `ecdsa-sha2-nistp256`) — fix the pattern so it matches the modern key type strings and excludes deprecated ones. Also look for control-flow bugs like an indentation mistake that makes a validation function return `False` (or `True`) unconditionally regardless of the actual check — trace it line by line rather than trusting the surface logic. Do not touch test files; only patch the library. Once the library is fixed, write `/home/user/ssh_monitor.py` as a CLI that takes one directory argument, iterates `*.log` files, imports and uses `ssh_log_parser` to classify each as clean or malicious (deprecated algorithms, brute-force patterns, malformed signatures), and prints `CLEAN: <file>` or `EVIL: <file>`. Make it executable. Then author `/home/user/supervisord.conf` with a `[program:ssh_monitor_daemon]` block invoking the script against the incoming-logs directory, with autorestart enabled and stdout/stderr logged to the specified log file. Test against sample clean and malicious logs you construct yourself before trusting the classifier.