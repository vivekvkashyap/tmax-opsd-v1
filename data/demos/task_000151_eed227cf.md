Work in stages and validate each independently before wiring them together. First, transcribe `/app/config.wav` (ffmpeg to extract audio if needed, then a speech-to-text tool such as `whisper`/`vosk`/`speech_recognition`, or careful manual listening) to recover the integer window size spoken in the clip — do not guess a number. Second, build the C filter as a real JSON parser (or use jansson) that fully unescapes `\uXXXX` sequences in string values recursively before substring-matching `EXFIL`; a naive approach that only checks the raw bytes for "EXFIL" will miss split/obfuscated encodings like `EXFIL`, so decode first, then compare. Handle malformed escapes without crashing (skip or treat literally). Test the compiled `filter_bin` by hand against a few crafted lines with mixed plain/escaped keywords before trusting it. Third, write `pipeline.sh` to pipe the filter's stdout into an awk/python rolling-average step keyed on the discovered window size W, using min(available_lines, W) for the denominator on early lines, and emit exactly the required CSV header and columns.