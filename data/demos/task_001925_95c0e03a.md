The linker error is a missing search path: `build.rs` declares `cargo:rustc-link-lib=static=telemetry` but never tells cargo where to find `libtelemetry.a` — add a `println!("cargo:rustc-link-search=native=<path-to-lib-dir>");` pointing at the workspace's `lib` directory, then confirm `cargo build` succeeds. To diagnose the crash, run the built binary with `RUST_BACKTRACE=1` and read the panic message/location in `main.rs` — it will indicate roughly which stage (payload deserialization) fails; since the code increments a packet counter before processing each packet, the printed panic context combined with that counter (or a quick added `eprintln!` before the failing call) tells you the 1-based packet index. To get the exact raw UDP payload bytes without hand-computing header offsets, parse the pcap independently and reliably (e.g. Python + scapy: `rdpcap`, iterate packets, pull the `UDP` layer's payload, hex-encode with `.hex()`), cross-checking against the packet index you found from the crash. Write both values into `/home/user/report.txt` in the exact specified two-line format.
