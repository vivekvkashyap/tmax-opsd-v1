Treat this as three independent components joined by a CI script. (1) C fix: the segfault comes from `wav_parser.c` casting `buffer + 4000` on a 44-byte stack buffer — an out-of-bounds pointer dereference. Fix it to read the actual WAV header fields within the 44 bytes it read (e.g., the ChunkSize field at the correct offset, bytes 4-7, little-endian) instead of an arbitrary out-of-range offset, and add a check that `fread` actually returned 44 bytes before dereferencing. Compile with `gcc` and test against the real WAV file to confirm no segfault. (2) Metadata extraction: use `ffprobe -show_entries format_tags=comment -of default=nw=1:nk=1 /app/incident_001.wav` (or `ffmpeg -i ... 2>&1 | grep comment`) to pull the comment tag verbatim into `incident_transcript.txt` — don't retype or guess the string, capture it exactly as extracted, since it's designed to look like a prompt-injection payload that your sanitizer must reject, not obey. (3) Go sanitizer: read the file, uppercase/normalize and check for banned substrings (SHUTDOWN, DROP TABLE, IGNORE ALL PREVIOUS INSTRUCTIONS, BASH, and similar SQLi/injection patterns) — print REJECT if any match, else ACCEPT; test against the clean/evil corpora under `/app/corpus/` before finalizing, since the CI harness runs the compiled binary against a hidden set with similar patterns. Finally, chain everything in `run_ci.sh`: compile C and Go, run the parser on the wav, run the sanitizer on the transcript, redirect its stdout to `incident_result.log`.