Use `ffmpeg -i /app/dashcam.mp4 -vf fps=1 -q:v 2 frame_%04d.jpg` (or similar) to extract frames at exactly 1fps; verify the frame count matches the video duration before proceeding. For each output frame, get its byte size with `stat -c%s` or `wc -c`, and write `frame_num,size` lines to `/home/user/raw_features.csv`, sorted numerically by frame number (the ffmpeg naming already zero-pads to 4 digits if you request it, but re-sort with `sort -t, -k1,1` to be safe). For `center_features.sh`, the critical requirement is data-leakage prevention: compute the training mean using ONLY the first `<N>` rows of the input CSV (e.g. `head -n "$N" "$input" | awk -F, '{s+=$2} END{print s}'`), then do integer/floor division of that sum by N using bash arithmetic `$(( sum / N ))` (awk's `int()` on negative results differs from bash truncation, so prefer native bash integer math or match its truncation-toward-zero behavior exactly). Then apply that single scalar mean to every row (train and test) of the full input file with a second awk pass, printing `frame_num,centered_size`. Make the script executable with `chmod +x`.