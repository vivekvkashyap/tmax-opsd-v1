Extract frames densely enough to catch brief flashes: `ffmpeg -i /app/evidence.mp4 -vf fps=10 frames/frame_%04d.png` (increase fps if codes are very brief), then run `zbarimg` over each frame (or the whole directory) to decode any QR codes present, collecting unique decoded strings into `/home/user/recovered_payloads.txt`. Inspect what you actually recovered rather than assuming — the payloads tell you which attack families to defend against (XSS via script/event-handler injection, tag-based injection, SQL tautology injection), but you should generalize the filter beyond the literal recovered strings to cover common evasion variants: case-insensitive matching (`grep -iE`), URL-decoding the input before matching (`%3C` etc.), and normalizing whitespace. Build `/home/user/detector.sh` as a set of case-insensitive extended-regex checks (e.g. `<script`, `on\w+\s*=`, `<svg`, `javascript:`, SQL tautologies like `or\s+1\s*=\s*1`, `'\s*or\s*'`) — exit 1 the moment any pattern matches, exit 0 otherwise. Test it directly against both `/app/corpus/evil/` and `/app/corpus/clean/` with a loop before considering it done, since 100% accuracy on each is required and false positives on clean text fail the task just as badly as missed attacks.
