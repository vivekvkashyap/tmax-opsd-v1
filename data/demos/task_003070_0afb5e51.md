Structure the Go program as four clear stages: fetch, decode, normalize, interpolate. Fetch raw bytes from `http://127.0.0.1:8080/raw.log` with `net/http`, then split into lines and parse each with a regex/manual split on `[` `]` to separate TIMESTAMP from MESSAGE_CONTENT. For encoding: try `utf8.Valid(messageBytes)` first — if invalid, decode the raw bytes as Windows-1252 using `golang.org/x/text/encoding/charmap.Windows1252.NewDecoder()` before treating it as a string; do this per-message, not per-line, since only the message portion may be mis-encoded. Regardless of source encoding, run every message through NFC normalization via `golang.org/x/text/unicode/norm.NFC.String(msg)` — this is what fixes a decomposed character (base+combining accent) into its single composed codepoint. For gap-filling: collect all line indices, note which have valid ISO-8601 timestamps vs `???`; for each contiguous run of `???` between two known timestamps, linearly interpolate by dividing the total duration by (gap length + 1) and assigning equal-spaced increments — parse/format with `time.Parse`/`.Format` using RFC3339Nano truncated/rounded to milliseconds, being careful to keep the `Z` suffix and exactly 3 fractional digits. Marshal each record as `{"timestamp":...,"message":...}` with `encoding/json`, one per line, into `/home/user/processed_logs.jsonl`.