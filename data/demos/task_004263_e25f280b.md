Before optimizing, nail down the I/O contract exactly by reading small chunks of the binary files (e.g. with `xxd` or `struct.unpack` in Python) to confirm float32 little-endian, 128-dim, row-major layout, and by running /app/ref_search on a tiny synthetic dataset to see its exact output formatting (index ordering, tie-breaking, separators) — your C++ program must reproduce this output textually identical, not just numerically close, since the verifier does a strict string comparison. For performance, avoid a naive triple-nested loop: exploit the algebraic identity ||x-y||^2 = ||x||^2 + ||y||^2 - 2x^Ty so the bulk of the work becomes a single dense matrix multiply (queries × datasetᵀ) via Eigen, which vectorizes far better than manual loops; precompute the squared norms once. After computing distances, replicate the reference's exact top-10 selection and sort semantics (including its tie-breaking rule for equal distances) to guarantee identical index lists. Compile with `-O3 -march=native`. For the benchmark script, time 15 runs of each binary, compute per-run ratios time_ref/time_fast, and derive a 95% CI for the mean ratio (e.g. via the sample mean ± t-critical × standard error) rather than a single-point speedup.
