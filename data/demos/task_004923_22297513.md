Process `/home/user/transactions.csv` in strict timestamp order, maintaining a running per-sender (count, sum) so you can compute "average of all preceding transactions from that sender" incrementally as you scan — a sender's very first transaction has no history and can never be an anomaly. For each row, before updating that sender's running stats, check if `amount > 2.0 * running_avg_so_far` (only evaluable when count > 0); flag it as anomalous if so, then update the sender's running sum/count with the current transaction regardless. Doing this in a single ordered pass (e.g. a Python script using a dict of {sender: (count,sum)}, or an awk script keyed on sender) avoids the common bug of computing average over ALL transactions (including future ones) instead of just prior ones. For each flagged transaction, in chronological order, emit the exact 3-line Cypher MERGE/MERGE/CREATE block (fill in sender_id, receiver_id, tx_id, amount) followed by a blank line, and write the concatenated result to `/home/user/anomalies.cypher`. Double check exact whitespace/formatting (semicolon placement, blank line between blocks) since the verifier does a strict text match.
