Reproduce first: run `/home/user/run_tests.sh` repeatedly and note it fails intermittently with NaN — this points at nondeterministic I/O rather than the math alone. Inspect how `cluster.c` reads from stdin: a single `read()` syscall on a pipe is not guaranteed to return all requested bytes in one call (especially when the test harness feeds data in slow chunks), so replace the one-shot `read` with a loop that keeps calling `read`/accumulating into the buffer until either the requested byte count is reached or EOF, handling `EINTR`/short reads correctly — this is the root cause of "intermittent data loss." Separately, in the centroid-update step, guard the division by each cluster's point count: if a cluster's count is 0 for an iteration, leave its centroid unchanged instead of dividing by zero (which produces NaN and then poisons future distance comparisons). Add assertions (e.g. `assert(counts[i] >= 0)`, finite-value checks) around the update to catch regressions early. Compile to `/home/user/clustering/cluster_fixed`, rerun `run_tests.sh` until it's 100% green, then produce `/home/user/final_output.txt` via the exact command given, and confirm output has three finite `Centroid` lines with no NaN.