Work top-down: fix the build first, since nothing else can be tested until the shared library exists. Inspect the Makefile's CFLAGS — a shared library built from object code that isn't position-independent fails to link/load correctly on x86_64; add `-fPIC` to CFLAGS and rerun `make`, confirming `libmatrix.so` is produced and `nm -D libmatrix.so | grep calculate_determinant` shows the symbol exported. Next, write ffi_wrapper.py using `ctypes.CDLL` to load the .so, setting `.argtypes` to `(ctypes.POINTER(ctypes.c_double), ctypes.c_int)` and `.restype` to `ctypes.c_double`, converting your Python flat list to a `(ctypes.c_double * len(data))(*data)` array before calling — get the ctypes signature exactly right or you'll get garbage/segfaults. Then build sanitizer.py to iterate every JSON file in the input directory, load `n` and `data`, call your wrapper to get the determinant, and classify: reject if `abs(det) < 1e-5`, else accept — write results keyed by basename to the output JSON. Test against the given clean/evil corpus paths directly before considering it complete, since the pass bar is 100% exact classification on both; also double check your determinant formula path only needs to handle the n values actually present in the corpus (the C code here only implements n=2 and n=3).