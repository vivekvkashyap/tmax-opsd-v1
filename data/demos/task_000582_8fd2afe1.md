Start by extracting the hidden project code from the audio: transcribe `/app/voicemail.wav` (e.g. with a local speech-to-text tool such as `whisper`/`vosk`, or by listening if a playback path is available) rather than assuming a project code — the whole calculation is worthless if you guess wrong here, so confirm the transcription carefully before proceeding. Once you have the actual spoken project code (a `<recovered-value>` you must derive, not one to hardcode from guesswork), write a Python script that walks `/home/user/storage_meta/`, branching on file extension: use `csv.DictReader` for `.csv` files and `json.load` for `.json` files (which may be a list of objects), normalizing both into a common record shape with `file_id`, `filename`, `project_code`, `size_bytes` (cast size to int). Sum `size_bytes` only for records whose `project_code` matches the recovered code exactly, and write that integer sum to `/home/user/reclaimed_bytes.txt`. Separately collect every record whose project_code does NOT match, sorted/compiled into `/home/user/active_manifest.csv` with header `file_id,filename,project_code,size_bytes` via Python's `csv` writer (more robust than raw `sed`/`awk` for JSON sources). Finally, track which original files (by path) contained at least one matching record during your scan, and run `sha256sum` on exactly those files, redirecting output to `/home/user/modified_files_checksums.txt` in standard `sha256sum` format.
