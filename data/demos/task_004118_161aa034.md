Structure the C++ program around a reusable `parse_weights()` that skips lines starting with `#` and reads the 3x4 W1 matrix, 4-element b1 vector, 4x1 W2 vector, and scalar b2 in that fixed order using a stream that just reads whitespace-separated doubles and ignores comment lines. Implement the forward pass as a plain function taking (v1,v2,v3) -> computes x1=v1*v1, x2=v1*v2, x3=log(v3), then for each of the 4 hidden neurons sums x_i*W1[i][j]+b1[j] and applies ReLU (max(0,.)), then the output neuron sums hidden[j]*W2[j]+b2 and applies sigmoid (1/(1+exp(-z))) — double check matrix orientation matches the file's stated layout (row i = feature i's weights into each hidden neuron). Read data.csv with a simple CSV line split on commas, skip its header, run the forward pass per row for the initial predictions.csv (id,pred with `std::fixed << std::setprecision(4)`). For benchmarking, wrap only the feature-engineering+forward-pass loop (no I/O, no string parsing) in an outer loop of 1000 iterations over the whole in-memory dataset, timing with `std::chrono::high_resolution_clock` before/after, converting total duration to microseconds for benchmark.txt. Compile via run_pipeline.sh with `g++ -O3 -o etl_inference etl_inference.cpp` then execute the binary.