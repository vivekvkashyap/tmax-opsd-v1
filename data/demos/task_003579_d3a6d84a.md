Build this as a small parser + formatter + concurrency-safe writer, in that order. First write a parser that scans each `.txt` file in `/home/user/raw_notes/` for repeated `Title:` / `Date:` / `Body:` ... `===END===` blocks (the body can span multiple lines, so read until the `===END===` sentinel rather than assuming a fixed number of lines). Reformat each parsed record into the exact `## title` / `*date*` / blank line / body markdown shape requested. Sort records by parsed date ascending using a stable sort (Python's `sorted` is stable, so as long as you preserve file/record read order before sorting, ties keep original order) — do not re-sort within-file order arbitrarily. Join records with exactly one blank line between them (be careful not to add extra blank lines from string concatenation). For the atomic write: acquire an `fcntl.flock(LOCK_EX)` on `/home/user/docs.lock`, write the whole compiled string to a `tempfile` created in `/home/user/`, then `os.replace()` the temp file onto `/home/user/compiled_docs.md` before releasing the lock — the ordering (lock → temp write → atomic rename → unlock) is what the verifier checks for, not just the final content. Make the script executable and run it once to produce the output file.