This is three separable pieces — solve them independently and test each before wiring them together. First, transcribe `/app/sequence_audio.wav` (via a speech-to-text tool) to recover the dictated DNA letters, keeping only A/T/G/C and discarding spacing/punctuation — this recovered string is an input, not something to guess. Second, implement `/home/user/score.py` from the algorithm the task text already fully describes (overlapping 3-mers of seq1, +3 per 3-mer found as a substring anywhere in seq2, then subtract `abs(len(seq1)-len(seq2))`) — implement exactly that spec rather than trying to reverse-engineer the compiled binary, and test against a few hand-computed small examples (watch for off-by-one in the sliding window, or overlapping vs. non-overlapping k-mer mistakes). Third, write `process_data.sh` to read the CSV, call score.py per row with the recovered reference as seq1 and each row's sequence as seq2, then pivot from long (sample_id, read_id, score) into a wide matrix (pandas `pivot_table` with `fill_value=0` fits well), sorted by sample_id, into `/home/user/features.csv`.
