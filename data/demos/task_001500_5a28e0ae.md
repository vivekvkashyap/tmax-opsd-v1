The exact filtering threshold is spoken, not written in the description — transcribe `/app/dictation.wav` first (e.g. with `whisper` or another offline speech-to-text tool available in the environment) and use the dictated rule verbatim as your rejection condition; do not guess a common default like "±2 sigma" without confirming it against the transcript. Implement the Go pipeline in stages: (1) parse each JSONL line into a struct with sensor_id, timestamp, signal; (2) deduplicate by sensor_id keeping the record with the max timestamp — use a map keyed by sensor_id, only overwriting when timestamp is strictly greater; (3) compute mean and population standard deviation (divide by N, not N-1) over the deduplicated signal values; (4) apply the dictated z-score threshold to flag outliers, and log `STATS: mean=%.2f stddev=%.2f rejected=%d` to stderr; (5) print surviving deduplicated records as JSONL to stdout. Test against both `/app/corpus/clean/` and `/app/corpus/evil/` — the evil corpus deliberately mixes duplicate sensor IDs with stale wild values and extreme outliers, so verify your dedup logic runs before z-score filtering, not after.