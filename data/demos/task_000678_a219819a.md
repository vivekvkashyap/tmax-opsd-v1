Diagnose before editing: run `make` in `/app/etl-worker` first and read the linker errors — they'll point at undefined references to pthread/thread symbols, confirming the `Makefile`'s `LDFLAGS` is missing the `-pthread` flag (add it, not just `-lpthread`, since C++11 `std::thread` needs the compiler-level pthread flag too for both compile and link stages). Next open `src/extractor.cpp` and inspect the existing `std::regex` pattern against the required ID format `TXN-<4 uppercase letters>-<5 digits>`; a broken pattern will have the wrong quantifiers (e.g. `{3}`/`{4}` instead of `{4}`/`{5}`), so correct the character classes and counts precisely, and confirm the code uses `std::regex_search` in a loop (not `regex_match`) so it can find IDs embedded anywhere in a multi-line/whitespace-varied buffer — pass `std::regex::multiline` behavior isn't required since `.` doesn't need to match newlines here, just ensure the search iterates using `sregex_iterator` across the whole payload. Rebuild with `make`, then start `./etl_server 127.0.0.1 8080` in the background. Validate with `curl -X POST --data-binary $'log\\nTXN-ABCD-12345\\nmore' http://127.0.0.1:8080/extract` and confirm the JSON array contains the exact ID before finishing.