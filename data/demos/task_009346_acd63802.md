Implement the C function state machine carefully, since the three checks (rate limit, decode, validate) must run in the right order and with the right side effects. Use a `static int call_count` inside `validate_and_fetch`, incremented on every call; check `call_count > 3` (i.e., strictly after 3 successful/attempted calls) first and return `-2` immediately without touching `output_buffer` — get the off-by-one boundary right by testing calls 1-5 in a fresh process. For percent-decoding, scan the input for `%XX` sequences, parse the two hex digits, and substitute the decoded byte — decode fully before validating, so encoded variants of the allowed prefix (like `%2E` for `.`) are correctly normalized to `https://internal.build.corp/...` before the prefix check. Validate with a strict `strncmp`-style prefix check against `"https://internal.build.corp/"` after decoding, returning `-1` (buffer untouched) on mismatch, otherwise copy the decoded string into `output_buffer` and return `0`. Compile with `gcc -shared -fPIC -o libfetch.so libfetch.c`. In the Python driver, load via `ctypes.CDLL`, declare `argtypes`/`restype` correctly (a mutable `ctypes.create_string_buffer(256)` for the output param), call the 5 URLs in the exact given order, and log each line in the exact specified format, leaving `Output:` blank (not `None` or `null`) on failure.