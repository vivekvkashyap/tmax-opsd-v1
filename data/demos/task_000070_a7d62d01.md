Debug the ctypes ABI mismatch in `/app/pr_workspace/wrapper.py` methodically: since `mask_payload` mutates the buffer in place, passing an immutable Python `bytes`/`c_char_p` in is unsafe (it may crash or silently not mutate) — instead allocate a mutable buffer with `ctypes.create_string_buffer(data, len(data))` and pass that pointer to the C function, then read back `.raw[:len(data)]` afterward. Set `argtypes`/`restype` to match the real C signature `int mask_payload(unsigned char*, int, unsigned char)` — use `ctypes.c_char_p` or a `c_char` buffer for the pointer, `c_int` for length, `c_ubyte` for the key, and `c_int` for the return value — and verify by running `pytest /app/pr_workspace/test_wrapper.py` (install `pytest`/`hypothesis` first) until the round-trip property test passes. Then implement the FastAPI `/mask` POST handler in `server.py`: base64-decode the `payload` field, call `apply_mask(data, key)`, base64-encode the result, and return it under `masked_base64`. Launch the server bound to `127.0.0.1:8080` in the background (e.g. `nohup python server.py > /app/pr_workspace/server.log 2>&1 &` or `uvicorn` invoked similarly) and leave the process running for the verifier to hit.
