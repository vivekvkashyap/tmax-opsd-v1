Load the CSV into plain C arrays (100 rows x 4 doubles) with `fscanf` or manual parsing, then standardize only the first 3 columns: compute each feature's mean and population std (divide sum-of-squared-deviations by N, not N-1) across all 100 rows, and transform in place. Implement KNN by, for each test point, computing Euclidean distance to every training point on the standardized features, then finding the K smallest distances (a simple partial selection-sort over an array of distance/index pairs is fine — no need for a heap for N=100) and averaging their raw (unstandardized) Y values. For 5-fold CV, use the fixed sequential blocks the task specifies (rows 0-19, 20-39, ...) rather than random splits, train on the other 80 rows each fold, and accumulate MAE per K across all folds before averaging. Loop K in {1..5}, track the K with lowest mean MAE, round to 2 decimals, and write `{"best_k": ..., "best_mae": ...}` to `/home/user/best_model.json` using plain `fprintf`. Compile with gcc and `-lm`, then run and inspect the output.
