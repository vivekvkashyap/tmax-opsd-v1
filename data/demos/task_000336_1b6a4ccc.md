Diagnose the memory leak before optimizing blindly: read `eval_server.py` and look for any global dict/cache that grows unboundedly per unique expression (storing full parsed ASTs forever is the classic culprit) — confirm by running the server standalone and watching RSS with `ps -o rss=` while sending repeated varied requests. Fix it by replacing the unbounded in-process cache with the provided Redis instance (`REDIS_HOST`/`REDIS_PORT` env vars) so evaluated results are stored externally with a TTL or bounded eviction instead of accumulating in the Python heap; only keep small, short-lived working state in-process. Compile the protobuf first with `python -m grpc_tools.protoc -I/app/proto --python_out=/app/ --grpc_python_out=/app/ /app/proto/expression.proto` so the generated stubs exist before the server can import them. Validate correctness against `/app/tests/test_fixtures.py` after each change so the optimization doesn't alter evaluation results. Finally write `/home/user/run_services.sh` to start `redis-server`, then the gRPC server, wait until port 50051 is accepting connections (poll with a small loop, not a fixed sleep), then launch `load_tester.py`, and measure peak RSS of the server process throughout to confirm it stays well under the threshold.