Work stage by stage. (1) For the dependency conflict, don't just pin blindly: check which pydantic major version each fastapi/starlette release actually requires (`pip index versions` or the package changelogs), then choose a mutually-compatible trio (e.g. an older fastapi/starlette pair built for pydantic v1, or a modern trio built for pydantic v2) and rewrite requirements.txt before creating the venv. (2) Transcribe `/app/instruction.wav` with a tool like `ffmpeg` + a speech-to-text library (e.g. openai-whisper) and save the raw transcript verbatim to transcript.txt — read it carefully, since it names the exact keywords and structural pattern the sanitizer must block. (3) Build the GoLite interpreter with asyncio: model each channel as an `asyncio.Queue`, run SPAWN blocks as tasks via `asyncio.create_task`, and wrap the whole script execution in `asyncio.wait_for(..., timeout=2)`. (4) In sanitizer.py, implement checks derived directly from what the transcript told you to block (keyword scan plus a structural check for one SPAWN nested inside another, e.g. by tracking block/indent depth or matching SPAWN...SPAWN patterns). Validate the sanitizer against both corpora before wiring it into the `/execute` endpoint to return HTTP 400 for evil scripts.
