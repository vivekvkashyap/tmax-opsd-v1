This task has two independent halves — solve them separately and verify each on its own before moving on. For Part 1, install the vendored package editable (`pip install -e /app/mcmc_graph_solver`), then run the failing test to see the flakiness; the root cause of non-reproducibility with Python `set()` on strings is hash randomization changing iteration order across runs — search `core.py` for where node identifiers are collected into a `set` and converted to a list, and make that ordering deterministic (e.g., sort the list) so the transition/adjacency matrix is built the same way every invocation. Re-run the pytest file several times (not just once) to confirm the flake is actually gone, since a single passing run can still be luck. For Part 2, ergodicity of a Markov chain built from a directed adjacency list means the underlying digraph must be strongly connected (every node reachable from every other, no absorbing/sink nodes with out-degree 0) — implement this with a standard strong-connectivity check (e.g., run BFS/DFS from an arbitrary node and again on the reversed graph, or use Tarjan/Kosaraju) rather than approximate heuristics, then wire exit codes 0/1 and test your script exhaustively against every file in both provided corpora before trusting it.