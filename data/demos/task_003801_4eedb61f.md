Three-part build. First, find and fix the broken vendored cJSON build: inspect the Makefile at `/app/vendored/cJSON-1.7.15` for a bad `CC =` assignment (a typo'd/nonexistent compiler name is a common way these builds get sabotaged) and correct it to `gcc`, then run `make` to produce `libcjson.a`. Second, write `sanitizer.c`: parse the input file by splitting on lines exactly matching `---END_RECORD---` (read the whole file, split into blocks preserving each block's original raw text so you can echo it back byte-for-byte), then for each block call `cJSON_Parse`; if parsing fails, discard. Otherwise check `cJSON_GetObjectItem(obj, "type")` is a string equal to `"sensor_reading"`, and if a `calibration_error` field exists, check it's numeric and `<= 5.0`; only if all checks pass, write the original raw block text plus the delimiter to output. Compile with static linking against your fixed `libcjson.a` (e.g. `gcc sanitizer.c -I<cjson-include-dir> -L<cjson-lib-dir> -lcjson -static -o sanitizer` or link the .a directly). Third, write `pipeline.sh` using `find <input_dir> -name '*.log'` to recurse, computing each file's relative path to reconstruct the same structure under `<output_dir>`, creating parent directories with `mkdir -p` before invoking the sanitizer on each file. Self-test against small hand-made clean/evil log files before trusting the hidden corpora.
