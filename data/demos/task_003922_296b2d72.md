Get the secret token first since everything downstream depends on it: transcribe /app/instructions.wav using a local speech-to-text tool or library available in the environment (e.g. whisper/vosk/speech_recognition) — don't guess the word, actually run recognition on the audio and treat its output as SECRET_TOKEN. Next, build translations.db: `sqlite3 /home/user/translations.db "CREATE TABLE dict(source TEXT, target TEXT);"` then bulk-load terms.csv via `.mode csv` + `.import` (skip the header row) or via a small script using Go's database/sql + mattn/go-sqlite3. For process.go, load the entire dict table into an in-memory Go map at startup for speed, since it'll be invoked repeatedly by the fuzz harness. Read stdin line by line preserving the exact line terminators, apply `norm.NFC` from golang.org/x/text/unicode/norm to each line, then scan character by character (not by splitting on whitespace) to identify maximal runs of ASCII letters as "words" while copying every non-letter rune through unchanged — replace each word with dict[word] if present, else with SECRET_TOKEN if the word is literally "SECRET", else leave it unchanged. Test with inputs containing emojis and punctuation adjacent to words to confirm byte-exact preservation before trusting the fuzz comparison against the oracle.