Write the Go program to process the CSV in a single clean pass: parse each row with `encoding/csv`, attempt `strconv.ParseFloat` on temperature and pressure and skip the row entirely (don't error out) if either fails to parse; then apply the strict range filters (temperature in [-50,50], pressure in [900,1100], both endpoints inclusive per the "strictly less/greater than" wording — reread the boundary conditions carefully since off-by-one inequality direction is the most common naive mistake here). Accumulate valid rows into two float64 slices for statistics. Compute mean, sample variance/covariance/stddev using N-1 (Bessel's correction) manually with `math` package functions — no external stats libs allowed. Pearson correlation is covariance divided by the product of the two sample standard deviations. For the 95% CI, use exactly `mean ± 1.96 * (sampleStdDev / sqrt(N))` as specified, not a t-distribution critical value. Round every float output to 4 decimal places (e.g. via `math.Round(x*10000)/10000`) before marshaling to JSON, and keep `valid_records` as a plain integer. Build and run the program, then diff the produced `/home/user/analysis_results.json` mentally against a from-scratch recomputation to catch parsing/rounding bugs before finishing.
