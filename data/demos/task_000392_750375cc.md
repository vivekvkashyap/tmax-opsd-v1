First locate and fix the vendored framework bug: grep `/app/vendored_microframe/` for where it splits the raw query string (likely `split(';')`) and change it to split on `&` (then split each pair on `=` as before) so `?category=Electronics&limit=3` parses correctly — verify with a quick standalone test of just that parsing function before wiring up the endpoint. Next, implement the server (Python is simplest given the framework is Python) that imports the fixed framework, binds `127.0.0.1:8080`, and on `/api/top_customers` runs a single parameterized SQL query joining `order_items -> orders -> customers` and `order_items -> products` filtered by `category`, grouped by customer, summing `quantity * price`, ordered descending, limited by the `limit` param — round the spend server-side with `round(x, 2)`. For the optimization step, run `CREATE INDEX idx_product_category ON products(category);` and `CREATE INDEX idx_order_items_product ON order_items(product_id);` via `sqlite3 /home/user/ecommerce.db` or a Python `sqlite3` connection, and sanity-check with `EXPLAIN QUERY PLAN` that scans use the index rather than SCAN TABLE. Finally write `READY` to `/home/user/server_ready` only after the listener is actually up, and keep the server process running.