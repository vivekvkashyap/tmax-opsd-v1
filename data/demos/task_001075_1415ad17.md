Build and test each pipeline stage as a standalone script before wiring them into Snakemake. Write `filter.py` to open the log with a normal line-by-line `for line in f:` loop (no `.readlines()` or loading the whole file), parse the status code out of each line (e.g. with a regex matching the combined log format's quoted request and trailing status/size), and write through only lines whose status is >= 400 — run it directly on `/home/user/access.log` first and eyeball a few output lines. Write `extract.py` to re-parse each filtered line with a similar regex to pull IP, method, and endpoint, and emit a CSV with header `ip,method,endpoint` using the `csv` module (not manual string joining, to avoid quoting bugs). Write `aggregate.py` to read that CSV, tally row counts per IP with a `Counter` or dict, and `json.dump` the result. Only after each script works standalone should you write the `Snakefile` with three `rule` blocks whose `input`/`output` chain matches these paths exactly and whose `shell`/`run` directives call your scripts; install snakemake via pip if not present, then run `snakemake -c 1` from `/home/user/` and confirm all three output files (`filtered.log`, `features.csv`, `threats.json`) are actually regenerated, not stale from manual testing.