Work through the four pieces in dependency order since later steps need earlier ones compiling. First `cd /app/rust-processor && patch -p1 < ../patches/redis-support.patch`, then run `cargo build` and read the compiler errors literally: a missing-trait-method error on redis calls usually means you need `use redis::AsyncCommands;` in `src/cache.rs`, and a type-mismatch error on a `&str`/`String` usually means adding `.to_string()`/`.to_owned()` or adjusting a function signature — fix only what the compiler flags, don't rewrite unrelated code. Next, implement `evaluate_semver_ast` in `src/evaluator.rs` as a recursive match/enum handler: `CONSTRAINT` nodes parse their `value` string with `semver::VersionReq::parse` and test the input `semver::Version` with `.matches()`; `AND`/`OR` nodes recursively evaluate their children and combine with boolean `&&`/`||`. Create `tests/fixtures/mock_ast.json` as a valid AST (e.g. a CONSTRAINT node) that you've checked by hand evaluates true for `1.4.5`, then run `cargo test` until green. Finally start services in dependency order — `redis-server &`, then `python3 /app/legacy-engine/app.py &`, then `REDIS_URL=redis://127.0.0.1:6379 LEGACY_ENGINE_URL=http://127.0.0.1:8081 cargo run --release &` — and smoke-test with a `curl POST /evaluate` before declaring done.