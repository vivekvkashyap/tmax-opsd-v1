Split the work into three independent pieces you can test in isolation before wiring them together. First, write a small Python normalizer for `/app/telemetry_stream.jsonl`: read line by line, use a regex to repair malformed `\u` escapes (e.g. sequences with fewer than 4 hex digits or stray backslashes) before calling `json.loads`, wrap the parse in try/except, and drop any line that still fails — keep only records exposing integer `frame_index` and string `sensor_reading`. Second, use `ffmpeg` to extract frames from `/app/dashboard_recording.mp4` (e.g. `-vf format=gray` piped to raw output, or extract PNGs then load with PIL/numpy), compute each frame's mean pixel intensity, and record the frame index wherever brightness is strictly greater than 200.0 — be careful that frame indices line up with the telemetry's `frame_index` numbering (0-based, matching the extraction order). Third, join flash-event frame indices to the parsed telemetry by `frame_index`, sort ascending, and build the exact `flash_events` payload shape shown in the spec. Implement the HTTP server (Flask/FastAPI/http.server) on `127.0.0.1:8080` with a `GET /api/v1/flash-telemetry` handler that checks the `Authorization: Bearer <token>` header against the required token, returning 401 on mismatch/absence, and serving the precomputed JSON otherwise. Launch it as a background process (e.g. `nohup ... &`) so it stays reachable for the automated test suite, and verify with a manual `curl` before declaring done.