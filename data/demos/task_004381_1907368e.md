Since `/app/auth_tool` will be deleted before grading, your Python script must contain a native reimplementation, not a subprocess call — build and verify that reimplementation now while the binary is still available. Reverse-engineer the hash by treating the tool as an oracle: run it on several controlled inputs (empty file, single byte, known short strings) and use static analysis (`objdump -d`/`strings`/`ghidra`/`radare2` if available) or differential testing to infer the algorithm's structure — look for common patterns like a rolling hash with XOR, bit-rotation, and a multiplier/seed constant, and confirm your Python port produces identical hex output to the binary on every test input before trusting it. Separately, for CSP violation detection, parse each HTML file's `<script>` and `<object>` tags (an HTML parser like `html.parser` or regex carefully scoped to tags) and check `src` attributes against the allowed origins (`self`/relative paths and the one trusted CDN domain); any inline script, disallowed script origin, or any `<object>` tag counts as a violation. Only compute your hash for violating files, write `filename,hash` rows sorted alphabetically by filename with no header, and test against the sample files in `/app/evidence/` before the binary disappears.
