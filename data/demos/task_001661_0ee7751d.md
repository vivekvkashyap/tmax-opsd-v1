Treat this as an exact reproduction task: since verification will byte-compare your CSV outputs against a golden implementation, follow the spec literally rather than taking shortcuts, and build the script in the same order as the steps listed. Read the CSV with pandas, compute `feature_1 = var_a*var_b` and `feature_2 = var_a**2+var_b**2`, then compute `category_c_encoded` as a target-mean encoding using `df.groupby('category_c')['target'].mean()` mapped back onto every row (mean over the whole dataset, not leave-one-out). Load the JSON weights and compute the linear combination then sigmoid exactly as given — use `numpy` for `exp` to avoid overflow issues. Select and order columns exactly as specified, round all floats to 4 decimals, sort by `id`, then write CSV with header and no index. For the bootstrap step, the exact call order matters for reproducibility: set `np.random.seed(42)` once, then loop 100 times calling `np.random.choice(N, size=N, replace=True)` per iteration (don't reseed inside the loop), average `baseline_prob` for each sample, then sort and round the 100 means before writing them one-per-line with the `mean_prob` header. Small ordering deviations (e.g. rounding before vs after sorting, or filtering before computing features) will produce mismatched output — mirror the described sequence precisely.
