First fix the vendored crate: open `/app/vendored/log-chrono-0.1.5/Cargo.toml`, find the `[build-dependencies]` entry pointing at a nonexistent local path, and replace it with a normal registry version (or drop the `path` key) so `cargo build` on that crate succeeds standalone before you depend on it. Then scaffold a new binary crate at `/home/user/log_sanitizer` with `cargo new`, and add the vendored crate as a `path` dependency in its `Cargo.toml` pointing at `/app/vendored/log-chrono-0.1.5`. In `main.rs`, take `input` and `output` as CLI args (`std::env::args`), read the input line by line, parse each line as JSON, call `log_chrono::parse_ts` on the `timestamp` field and drop the line if it errors, then case-insensitively check the `message` field for each injection pattern (`union select`, `drop table`, `or 1=1`, `--`) and drop the line if any match — otherwise write the original line unchanged (don't re-serialize the JSON, to guarantee byte-exact preservation). Build with `cargo build --release`, then write `/home/user/run_filter.sh` as a thin wrapper invoking the compiled binary with the two args, and `chmod +x` both the script and confirm the binary path it calls is correct. Test manually against the evil/clean corpora before finishing.
