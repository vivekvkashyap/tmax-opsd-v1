Fix the build first: inspect the daemon's `Makefile` for stale references to the old mock (`-I../lib/mock_include`, `-L../lib -lpcap-mock`) and replace them with the real system `libpcap` (`-lpcap`, and `pkg-config --cflags --libs libpcap` if available), removing any now-conflicting include paths that shadow the real headers. Confirm `make` produces `dpi_daemon` cleanly before touching the parsing logic.

For the hang, read `parser.c`'s recursive TLV parser closely: it reads a 16-bit length into a signed `short`, so a crafted length like `0xFFFE` becomes a negative `declared_len` after the cast, which then passes the `declared_len > remaining` bounds check (a negative number is never greater than a positive `remaining`) and causes `remaining - declared_len` to grow and the recursion to either infinite-loop or process garbage. Fix it by parsing the length into an unsigned type (`uint16_t`/`int`, not `short`) so it can't go negative, and add explicit bounds/sanity checks (e.g. `assert` or an early return of an error code) that reject any length that would make `remaining` negative or exceed the buffer — treat an invalid length as "skip this malformed TLV" rather than crashing or looping. Rebuild, start emitter/aggregator/daemon in sequence per the task's instructions, and confirm the parsed-TLV log count clears the required threshold within the time window rather than stalling at the malicious packet's position.