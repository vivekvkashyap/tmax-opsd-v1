First fix the build: open `Cargo.toml`, find the `polars` dependency, and add the missing feature flags needed for lazy/window operations (check Polars docs for which features enable `.lazy()` and `rolling_mean`) — a bare `polars = { version = "..." }` line without extra features will fail to compile against code using lazy frames or rolling windows. Then run `cargo build --release` to confirm it compiles before touching logic.

Next, profile why `src/main.rs` is slow: look for nested loops that re-scan the full dataset per row (O(N^2)) to compute a rolling average per `cluster_id`. Replace this with either (a) a Polars lazy pipeline: read JSONL into a DataFrame, `.lazy().group_by("cluster_id")` then use a rolling/window expression over `duration_seconds` sorted by timestamp, or (b) a manual O(N) pass: group rows by `cluster_id` into a `HashMap<String, VecDeque<f64>>`, maintain a running sum as you push/pop the last 7 values, and flag when the current duration exceeds 2.5x the rolling average.

Finally, write matching rows sorted by timestamp descending into a CSV with the required columns. Rebuild in release mode and time the binary — target well under the 1.5s threshold.
