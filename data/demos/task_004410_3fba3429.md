Confirm the oracle's algorithm empirically before implementing: run `/app/oracle_extractor` on a couple of small hand-built edge-list graphs (e.g. a triangle, a path) and check the printed values against singular values you compute independently, to validate the "top-5 SVD singular values of the adjacency matrix, zero-padded if N<5" hypothesis from the hint. In your Rust project, build the N×N adjacency matrix from the edge list (N = max node id + 1, symmetric 0/1 entries), then compute its SVD with `ndarray-linalg` (or another linear algebra crate), sort singular values descending, and pad with zeros to length 5. Watch two correctness traps: node indexing must span 0..N even if some intermediate node ids never appear as isolated nodes (they still count toward N and contribute all-zero rows/columns), and output CSV rows must be sorted alphabetically by filename with floats precise enough that the MSE against the reference stays under 1e-5 (use f64 throughout, not f32). Process all files in `/app/dataset/`, write the header and rows to `/home/user/embeddings.csv`, and spot-check a few outputs against the oracle directly before considering it done.
