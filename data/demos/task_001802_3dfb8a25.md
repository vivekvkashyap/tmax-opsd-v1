Treat this as a classic ETL pipeline built from standard Linux text tools, and get each stage right before chaining them. Merge: concatenate base.csv and update.csv, then for each (msg_id,language_code) key keep the last-seen row — `sort -t, -k1,1 -k2,2 -s` combined with `awk` keyed on the first two fields (processing update.csv's rows after base.csv's, or using an associative array keyed by "id,lang" that update.csv overwrites) is the reliable way, not a naive `sort -u` which picks arbitrarily. Filter: apply a strict regex like `^ERR_[0-9]{3}$` against msg_id only (grep/awk), dropping everything else including malformed IDs like `ERR_9999`. Gap-fill: after filtering, enumerate the *distinct* surviving msg_ids and cross them with the three fixed languages (en,es,fr); for any (id,lang) combination absent from your data, synthesize a row with `UNTRANSLATED`. Do this fill with an awk script that tracks which (id,lang) pairs it has seen, then emits the missing ones. Sort final.csv by msg_id then language_code (`sort -t, -k1,1 -k2,2`). For review.csv, group final.csv rows by language_code and take the first 2 per language (already alphabetical by msg_id within each language due to the sort), then re-sort by language_code then msg_id. Diff your outputs against your own hand-traced expectations before finishing.
