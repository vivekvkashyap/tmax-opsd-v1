Work in stages with intermediate files so each transform is checkable. First `cp /var/tmp/etl_drop/server_metrics.log /home/user/workspace/raw.log` (creating the workspace dir first). Deduplicate exact duplicate lines with `sort -u` (or `awk '!seen[$0]++'`) before any aggregation — do this before extracting fields, not after, since retried lines are byte-identical. Use `grep -oE` or `awk` with a regex/field split on `[`, `]`, `=`, `%` to pull the `YYYY-MM-DD HH:MM` prefix (drop seconds) and the CPU_USAGE numeric value from each line. Group by minute and average with awk associative arrays (`sum[minute]+=cpu; cnt[minute]++`). For the continuous 10:00–10:10 timeline, iterate minute-by-minute in a bash loop, and when a minute is absent from your aggregated map, carry forward the last seen average (forward-fill) rather than skipping it — build this as an ordered array so rolling stats can index by position. For the 3-minute rolling average, sum the current and up to two prior minutes' averages and divide by however many are actually available (1, 2, or 3), not always by 3. Use `printf` with `%.1f` for rounding to exactly one decimal, and construct the CSV header/rows exactly as specified.
