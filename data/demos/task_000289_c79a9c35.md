Build the pipeline as clearly separated stages so each is easy to verify independently. Load the CSV with pandas (or csv module), group by `ip`, and drop any IP with strictly fewer than 5 total rows before doing anything else. For each remaining IP, bucket its rows into 10 fixed one-minute bins anchored at `2023-10-01T10:00:00` (bin index = minutes since that anchor, valid range 0-9) and average `latency_ms` within each bin — use the bin boundary consistently (e.g. `(timestamp - anchor).total_seconds() // 60`). For gap-filling, apply forward-fill across the 10-length array in order, then back-fill any still-empty leading bins from the first available value — implement this explicitly (e.g. a manual loop or `pandas.Series.ffill().bfill()`) rather than assuming pandas defaults handle both directions correctly. For normalization, use sample standard deviation (ddof=1, i.e. divide by N-1), and specifically handle the zero-std-dev case by setting all ten normalized values to `0.0` instead of producing NaN/inf from a division by zero. Then compute pairwise Euclidean distance across all valid-IP pairs, pick the minimum, and write `ip1`/`ip2` sorted lexicographically (not by which was "first") with `distance` rounded to 4 decimal places to `/home/user/closest_ips.json`.