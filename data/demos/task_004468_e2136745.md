The bug is that normalize.sh's awk pass computes min/max over the entire embeddings.csv (both passes read the same file with `NR==FNR`), which leaks test-set statistics into the training normalization. Fix it by restricting the min/max accumulation to only the first 80 rows of the *first* file pass — since both passes read the same file, use `FNR<=80` (not `NR<=80`) as the guard around the min/max update block inside the `NR==FNR` branch, leaving the second pass (the actual normalization/output pass) untouched so it still transforms and prints all 100 rows using those training-only bounds. Keep the printf formatting at exactly 4 decimal places and the head -n 80 / tail -n 20 split into train.csv/test.csv unchanged. After fixing, run `./normalize.sh` (chmod +x if needed) to regenerate train.csv and test.csv, then run `python3 evaluate.py` without modifying it, and confirm test_log_probs.txt is produced. Sanity-check your fix by manually computing the min/max of the first 80 rows' three feature columns (e.g. with awk or pandas) and spot-checking that the first row of test.csv matches those bounds rather than the full-dataset bounds.