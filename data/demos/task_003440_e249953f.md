First inspect existing indexes on `edges` (e.g. `SELECT name, sql FROM sqlite_master WHERE type='index' AND tbl_name='edges'`) and drop each one with `DROP INDEX`, then create a single composite index `CREATE INDEX idx_edges_source_target ON edges(source_id, target_id)` — leading column must be `source_id` since that's the traversal filter/join key. For the path query, write a `WITH RECURSIVE` CTE that starts from the node named `Router_START`, joins `edges` on `source_id`, joins `nodes` on the target to check `status = 'active'`, and recursively extends the path while accumulating a running weight sum and hop count and a delimiter-joined path-so-far (e.g. via `||','||`) — guard against cycles/unbounded recursion by carrying the depth in the CTE and filtering `WHERE depth < 7` (or similar) in the recursive term, and terminate/select only rows where the last node's name is `Router_END`. In the outer query, `ORDER BY total_weight ASC LIMIT 3`. Parse the SQL result rows in Python and serialize to JSON with exactly the keys `path_names`, `total_weight`, `hop_count`, writing to `/home/user/top_paths.json`. Sanity-check by hand-tracing a couple of paths through the schema to confirm your recursive join direction (source->target) and active-node filtering are correct before trusting the query output.
