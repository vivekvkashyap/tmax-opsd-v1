Work in two clean stages. First, decode the signal: use `ffmpeg -i /app/config_signal.mp4 out_%02d.png` to dump all 32 frames, then for each frame check whether it is white or black (e.g. `convert frame.png -format "%[fx:mean]" info:` or sample a pixel with ImageMagick/PIL — a mean near 1 is white/bit=1, near 0 is black/bit=0). Assemble the 32 bits in frame order (frame 0 = MSB, since the spec says big-endian) into an unsigned 32-bit integer S; do not guess the value, derive it from the actual frames. Second, write a small C program that opens `/app/base_toolpath.gcode`, and for each line strictly matching `G1 X<val> Y<val>` (skip everything else, including lines with only X or only Y) parses the two floats, applies the scale rule (multiply by S if x<0, else by S/2.0) to get the new point, accumulates Euclidean distance from the previous transformed point (start at (0,0)), and updates the running "current point" to the new transformed point (not the original). Compile with gcc, run it, and write the final distance as a plain float to /home/user/total_distance.txt. Double-check the frame ordering/bit-endianness and the "only lines with both X and Y" filter — these are the two easiest places to get wrong.