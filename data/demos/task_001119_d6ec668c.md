Fix the service in layers and test each layer before wiring the pipeline around it. First generate a self-signed cert/key pair with `openssl req -x509 -newkey rsa:2048 -nodes -keyout /home/user/certs/key.pem -out /home/user/certs/cert.pem ...` and create `/home/user/logs/`. In `webhook_service.py`, wrap the HTTP server's socket with TLS using the generated cert/key (e.g. `ssl.SSLContext`/`wrap_socket`) so it actually serves HTTPS, and swap the logging setup from a basic file handler to `logging.handlers.RotatingFileHandler('/home/user/logs/webhook.log', maxBytes=1024, backupCount=3)` attached to the logger. Start the service manually first and confirm `curl -k https://localhost:8443/` returns the expected body before scripting anything. Then write `/home/user/ci_pipeline.sh`: launch the supervisor in the background, sleep briefly, curl with `-k` (or `--insecure`) and check the response body for the exact required string with grep, print `CI SUCCESS`/`CI FAILED` accordingly, kill the background supervisor process (and its child) in both success and failure paths, and exit with the matching status code. Make the script executable and run it end-to-end to confirm exit code 0.
