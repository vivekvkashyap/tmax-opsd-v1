Treat this as two independent subtasks. For Part 1, read `/home/user/data/genome.fasta`, strip the `>` header line(s), concatenate all remaining sequence lines into one contiguous string (no newlines between them), and find the 0-based index of the first occurrence of `ATGCGTACGTTAG` using simple string search (e.g. Python `str.find` or `grep -b` on the concatenated sequence) — write just the integer to `/home/user/promoter_index.txt`. For Part 2, implement the ODE model exactly as specified: solve `dp/dt = k_prod - k_deg*p` with `p(0)=0` (note it has a closed-form analytic solution `p(t) = (k_prod/k_deg)*(1-exp(-k_deg*t))`, which you can use directly instead of numerical integration for speed/stability) inside a Gaussian log-likelihood with fixed `sigma=0.5`, plus uniform(0,10) log-priors returning `-inf` outside bounds. Build the log-posterior, run `emcee.EnsembleSampler` with 32 walkers and 2000 steps, seeding `np.random.seed(42)` and initializing walkers as a tight Gaussian ball (std 0.1) around `[1.0, 1.0]` — match these hyperparameters exactly, since they affect the final estimate. Discard the first 500 steps per walker as burn-in, flatten, take the posterior means of both parameters, round each to 1 decimal, and write `k_prod_mean,k_deg_mean` to `/home/user/params.txt`.
