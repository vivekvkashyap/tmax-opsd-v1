Keep the timing scope tight and the binary parsing correct. Read `/home/user/data/features.bin` as raw bytes and reinterpret as little-endian f32 values (e.g. in Rust, read the whole file into a `Vec<u8>` then use `f32::from_le_bytes` on each 4-byte chunk, or use the `byteorder` crate) — do this file read BEFORE starting the timer, since I/O time must be excluded from the benchmark. Start a `std::time::Instant::now()` timer immediately before computing the L2 norm (sum of squares, then sqrt) over all 1,000,000 values, stop it right after, and convert the elapsed duration to microseconds as an integer via `.as_micros()`. Write the result to `/home/user/metrics.json` with exactly the two keys `l2_norm` (float) and `compute_time_us` (integer) — build the JSON with a small manual string format or `serde_json` if available, matching the key names and types exactly since the verifier checks both value correctness (l2_norm within tolerance) and that compute_time_us is a JSON integer, not a float. Compile with `rustc` or `cargo build --release` and actually run the resulting binary so the JSON file is produced on disk, not just printed.
