Start by extracting frames from the video with ffmpeg (e.g. `ffmpeg -i /app/intercept_leak.mp4 -vf fps=<N> frame_%04d.png`), then scan the frames (visually or via OCR) for the flashed configuration text to recover the three values: the allowed ingress port, the AuthToken cookie prefix, and the CSP whitelist domain — do not guess these, read them directly off the extracted frames. Once recovered, implement `/home/user/waf_simulator.rs` using `serde`/`serde_json` to parse the request JSON, then apply three independent checks: exact port match, cookie parsing (split the `Cookie` header on `;`, find `AuthToken=`, verify the value starts with the recovered prefix), and CSP parsing (find the `script-src` token, split its space-separated source list, and reject if any entry is not `'self'` or the recovered whitelist domain). Any failing check should short-circuit to exit code 1; only exit 0 if all pass. Before finishing, self-test by hand-writing a few clean and evil JSON fixtures that mirror the corpus format and confirming your binary's exit codes match expectations, then compile with `rustc waf_simulator.rs -o waf_simulator` (or cargo) so the binary is directly executable at the required path.