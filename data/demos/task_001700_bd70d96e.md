Use Python's `csv` module (or pandas with proper quoting) rather than naive line-splitting, since `review_text` contains embedded newlines inside quoted fields — a shell-based `split('\n')` approach will corrupt rows, which is exactly the bug you're fixing. Pipeline order matters: (1) parse rows respecting CSV quoting; (2) drop rows where `rating` can't be parsed as an int; (3) replace `\r\n` and `\n` inside `review_text` with a single space; (4) apply `unicodedata.normalize('NFKC', text)` to `review_text` — this changes character counts for decomposed Unicode sequences, so always measure `len()` after normalization, not before; (5) group by `user_id`, parse `timestamp` with `datetime.fromisoformat` (strip trailing `Z` or replace with `+00:00`), and keep only the row with the max timestamp per user; (6) write each surviving record as one JSON object per line to the `.jsonl` output, with `rating` coerced to `int`. For the monthly aggregation, bucket by `timestamp[:7]` (`YYYY-MM`), compute count, mean rating, and mean of the (already-cleaned) `review_text` length, round both to 2 decimals, and write a header row plus rows sorted chronologically by month key.