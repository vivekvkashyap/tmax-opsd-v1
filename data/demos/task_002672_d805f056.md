Profile first to confirm the bottleneck before touching code: run the unfixed `analyze.go` and observe it takes far longer than the 1-second budget, which for a Dijkstra-style shortest path over ~50k nodes strongly implicates `GetEdges` being called on every relaxation — check `/app/graphdb/store.go` for how edges are stored and retrieved. If `AddEdge` only appends to a flat slice and `GetEdges` linearly scans that whole slice per call, that's O(V*E) instead of O(E) total. Fix it by making `AddEdge` also append into the existing-but-unused `edgeIndex map[string][]Edge` keyed by source node, and making `GetEdges` simply return `s.edgeIndex[nodeID]` (an O(1) map lookup) instead of scanning. For `/home/user/analyze.go`: set up a Go module with a `replace` directive in `go.mod` pointing `github.com/custom/graphdb` at `/app/graphdb`, load `edges.csv`, call `ShortestPath("START_NODE","END_NODE")`, then walk the returned path accumulating a running (cumulative) sum of edge weights, writing each `{"node":..., "cumulative_weight":...}` entry to `/home/user/result.json`. Re-time the full `go run` after the fix to confirm it's now well under 1.0s.