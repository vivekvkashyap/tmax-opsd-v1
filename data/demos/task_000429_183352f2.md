Treat this as two separable subproblems that must each be exact, since the grader fuzzes many matrices against a golden oracle and any drift fails. First, spectral analysis: load the WAV with `scipy.io.wavfile` (or `wave`+`numpy`), run `numpy.fft.rfft` over the full signal, find the bin index of maximum magnitude (skip/ignore the DC bin at index 0 since noise floor there is irrelevant), convert bin index to Hz via `freq = bin_index * sample_rate / N`, and round to the nearest integer — do this once, since the WAV is fixed, and reuse that dominant frequency for every fuzz input rather than recomputing incorrectly per matrix. Second, simulate the ODE with literal Euler integration exactly as specified: precompute `k = M00*M11`, `c = M01+M10`, iterate `t` from 0 to 2.0 in steps of 0.001 (2000 steps), updating `v_new = v + dt*(-k*x - c*v + A*sin(2*pi*f_d*t))` and `x_new = x + dt*v` using the *old* v and x each step (not updated-in-place order swaps, which silently changes results), track `max(abs(x))` across all steps, and print it rounded to exactly 4 decimals with `f"{val:.4f}"`. Match argument parsing precisely: two CLI args, second is a comma-separated 4-float string.