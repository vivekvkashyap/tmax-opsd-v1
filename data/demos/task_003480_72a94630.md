Profile before rewriting — use `cargo build --release` plus a timer around each of the three stages (video extraction, ODE solve, alignment) to find which one dominates the 45s, since optimizing the wrong stage wastes time. For video extraction, check src/video.rs: spawning ffmpeg once per frame is expensive — read the video via a single ffmpeg pipe/process producing raw frames, or decode once and iterate in memory, rather than N subprocess spawns. For alignment, check src/alignment.rs for a naive Smith-Waterman that reallocates vectors inside the inner loop or recomputes without memoization — replace inner-loop allocations with a pre-allocated DP matrix reused across calls, and consider `rayon` for row/column parallelism if still too slow. For the ODE solver in src/ode.rs, look at the fixed timestep `dt`; an absurdly tiny step size multiplies iteration count for no accuracy benefit — increase it to a still-stable value and confirm the resulting concentration curve doesn't diverge or change materially versus the original tiny-step output. After each optimization, re-run and diff results.json values against your own pre-optimization baseline to confirm numerical equivalence, not just speed.