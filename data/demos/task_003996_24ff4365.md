Use `git bisect start`, mark `HEAD` bad and `v1.0` good, then drive it with `git bisect run <script>` rather than manually. Your test script must, at each checked-out commit: attempt the normal build first; if the build fails (grep the build output for the undefined-reference/linker error signature), don't just skip — instead patch the Makefile on the fly (e.g. `sed -i 's/gcc sim.c -o sim/gcc sim.c -lm -o sim/' Makefile` or append `-lm` directly) and rebuild, so every commit can actually be evaluated for the runtime behavior rather than being reported as untestable. Only exit 125 (skip) if the source itself is unbuildable for reasons unrelated to the missing flag. After a successful build, run `./sim 10000`; check the exit status — a segfault shows as exit code 128+SIGSEGV (typically 139) — and exit 1 (bad) or 0 (good) accordingly, ignoring separate compile failures. This ensures bisect narrows to the exact commit that introduced the crash, not the unrelated Makefile breakage. Write the resulting 40-char hash from `git bisect view`/`git rev-parse` to `/home/user/bad_commit.txt`, then `git bisect reset`.