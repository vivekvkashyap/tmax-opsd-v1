Fix the vendored C library before touching data: first run `make` in `/app/fastagg-1.0` and read the linker error — a missing `-lm` in `LDFLAGS` is a common cause when `sqrt()` (or similar libm calls) is undefined at link time; add it and confirm the build succeeds. Then inspect the standard-deviation routine in `src/agg.c` — look specifically at whether the variance/std-dev sum is divided by `n` or `n - 1`; sample standard deviation requires Bessel's correction (`n - 1`), so if you see a plain `n` in that denominator, that's the numerical bug degrading accuracy. Fix it, rebuild, and verify by hand-checking one small window's std-dev against a quick Python/numpy `ddof=1` calculation before trusting the full run. Only then execute `./fastagg /app/data/raw_features.csv /home/user/processed_features.csv` and inspect the output CSV's `std_dev` column for sane values (no NaNs, no obviously population-style underestimates) before considering the task done — the verifier scores MSE against a reference, so precision matters more than getting output in any shape.
