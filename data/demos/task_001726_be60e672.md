Start by transcribing /app/guidelines.wav yourself (e.g. `whisper /app/guidelines.wav --model base` or Python `speech_recognition`/`openai-whisper`) — do not assume you know the rules; read the actual transcript output carefully, including any corrections the speaker makes mid-sentence (voice memos often self-correct, and the LATEST stated version of a rule is the one that counts, not an earlier draft phrasing). From the transcript, extract each concrete condition (missing-value checks, exact-value checks, and any statistical/bootstrap procedure with its sample count and random seed) precisely as stated — get the exact column names, thresholds, and seed value from the audio, not from guesswork. Implement `/home/user/filter_dataset.py` so it reads the CSV robustly (strip whitespace from column names), applies each rule in order, and for any bootstrap-based threshold, set the random seed exactly as specified before sampling so results are reproducible. Print only `ACCEPT` or `REJECT` with no extra output. Test against the sample datasets in /home/user/samples/ first, then reason about edge cases (NaNs, boundary values) since the hidden adversarial corpus will include files that narrowly trigger or narrowly avoid each rule.