Start by extracting the archive with `tar -xzf /home/user/configs.tar.gz -C /home/user/configs/` (create the directory first). Then write the C program: open `/home/user/configs/` with `opendir`/`readdir`, skip non-regular files (check with `stat`/`lstat` and `S_ISREG`), and for each file `fopen` it in binary mode, `fread` the first 4 bytes into a buffer. Compare the buffer against the ELF magic (`0x7F 'E' 'L' 'F'`) and both WAL magic variants (`0x37 0x7F 0x06 0x82` and `0x37 0x7F 0x06 0x83`) byte-by-byte — do not assume a fixed byte order, check both WAL patterns explicitly. Close the file handle before calling `rename` or `remove`, since renaming/removing an open file can behave unexpectedly if buffers aren't flushed/closed first. Build the new filename by concatenating the original path with `.elf` or `.wal` and call `rename(oldpath, newpath)`; for unmatched files call `remove(path)`. Compile with `gcc -o /home/user/process_configs /home/user/process_configs.c`, run it, then verify by listing the directory that only `.elf`/`.wal` files remain before creating the final tarball. For the final archive, `cd /home/user/configs/` first so tar entries don't include directory path prefixes, then `tar -czf /home/user/updated_configs.tar.gz *.elf *.wal`.
