Work run-by-run with `awk`/`grep` rather than a script that guesses: for each `run_*/predictions.csv`, skip the header and check that every `prediction` field (2nd comma-separated column) is strictly `0` or `1` for all data rows — a single float like `1.0` or `NaN` anywhere disqualifies the whole run, so use something like `awk -F',' 'NR>1 && $2!~/^[01]$/{bad=1} END{exit !bad}'` per file to flag invalid runs, then only `cat` the data rows (no headers) of the valid runs into `/home/user/pooled_preds.csv`. For the bootstrap step, you must use `shuf` with replacement — `shuf` alone samples without replacement, so instead do something like looping `shuf -n 1 pooled_preds.csv` 500 times, or use `shuf -r -n 500 pooled_preds.csv` if your `shuf` supports `-r` (repeat/with-replacement mode), and write exactly 500 lines to `/home/user/bootstrap_sample.csv`. Compute accuracy with `awk -F',' '$1==$2{c++} END{print c/NR}'` over the bootstrap file and write that decimal exactly (no rounding beyond what awk naturally produces) to `/home/user/report.txt`, since the verifier recomputes accuracy from your own bootstrap file and compares it against your reported value.