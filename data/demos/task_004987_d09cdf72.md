This is a brute-force + multi-service integration task. For the C++ cracker, since the pin is a 6-digit numeric string (000000-999999), write a tight loop that formats each candidate with zero-padding, computes SHA-256 (use a compact public-domain/self-contained implementation or an available crypto library — avoid shelling out to `sha256sum` per candidate, that's what makes naive solutions too slow), compares the hex digest against the input hash from stdin, and prints only the matching pin the instant it's found; compile with `-O3` as the verifier will. Reading the hash from stdin, iterating a million candidates, and hashing in-process (not via subprocess) is the key to hitting the tight time budget across 100 hashes. Once you recover the pin from `/home/user/app/legacy_hash.txt`'s hash, generate a new random 16-char alphanumeric password, and update `redis.conf` (`requirepass <new>`), `flask/.env` (`REDIS_PASSWORD=<new>`), keeping both in sync. For nginx, add a `location /auth-debug/ { return 403; }` block ahead of/instead of any proxying for that path, while leaving `/api/` and `/auth/` proxy_pass directives intact. Restart all services and manually curl each endpoint to confirm expected status codes before writing `rotation_report.txt` (old pin, then new password, one per line).
