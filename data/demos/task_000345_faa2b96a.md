Build the benchmark in two layers: first get the C++ program correct and deterministic, then wrap it with the orchestration script. In `benchmark.cpp`, allocate a flat 1D array (size N*N) so you can address it as `arr[row*N+col]`; the row-major loop should increment the inner index contiguously (`for row: for col: arr[row*N+col]`) while the column-major loop swaps the loop order so the inner index jumps by N each step — this is what produces the cache-miss gap. Seed `std::mt19937` with 42 exactly once, before either measured run, so both modes are compared on the same data. Time only the summation loop with `steady_clock`, not the allocation/init. Print the sum to stderr (never stdout) so the pipeline's stdout capture stays clean, and print only the integer microsecond count to stdout. Compile with `-O0` — with any optimization, the compiler may still vectorize or reorder the column-major loop and erase the expected slowdown. In `pipeline.sh`, loop 10 times per mode capturing each run's stdout into an array or file, then average with `awk`/shell arithmetic before writing the two-line report in the exact `Row-major mean: <value> us` format. Actually run the script at the end to produce `results.txt`.
