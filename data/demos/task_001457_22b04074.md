Fix and verify the two components separately before wiring them together. For Nginx, add a `location / { proxy_pass http://127.0.0.1:9000; }` block (or equivalent) inside the port-8080 server block — the given config is missing `proxy_pass` entirely, so requests currently have nowhere to go; reload/start nginx with the config and confirm with a direct `curl` to port 8080 that it forwards (even before the C bug is fixed, you should see *some* response reach the backend). For the C backend, locate `normalize_embedding` and look for a variable declared with an integer type (e.g. `int norm = sqrt(sum);`) used to hold the L2 norm before dividing each component by it — truncating a norm like 3.74 down to an int (3 or worse, 0 if sum<1) causes either precision loss or division-by-invalid-value zeroing. Change that variable's type to `float`/`double` so the division preserves precision, keeping the JSON response schema and all other logic untouched. Recompile with `gcc /app/server.c -lm -o /app/server`, run it in the background on port 9000, then confirm end-to-end via `curl "http://127.0.0.1:8080/embed?id=5"` that the returned array's L2 norm is ~1.0 and no components are exactly 0.0.
