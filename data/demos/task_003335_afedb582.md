Write a Python script (pandas/numpy) that follows the pipeline literally and precisely, since small deviations change the final answer. Load `eval.csv`, compute `f1_z = (f1 - f1.mean()) / f1.std(ddof=0)` — using pandas' default `ddof=1` here is the classic mistake, so explicitly pass `ddof=0` (or use `numpy.std` which defaults to population std). Filter to rows with `f1_z > 0` (strictly greater, not >=) to get the evaluation slice. For each `preds_<model_id>.csv` file discovered in `/home/user/artifacts/` (glob for the pattern and extract `model_id` from the filename), inner-join its predictions to the slice on `id`, compute MSE with `((y_true - y_pred)**2).mean()` over only the slice rows, read `num_features` for that model from `metadata.json`, and compute `penalized_score = MSE + 0.5 * num_features`. Track the minimum penalized_score across all models, then write `/home/user/best_model.txt` as a single line `Model: <id>, Score: <score>` with the score formatted to exactly 4 decimals (e.g. Python f-string `f"{score:.4f}"`). Print intermediate MSE/score values while developing so you can sanity-check the pipeline logic against the raw CSV numbers by hand.
