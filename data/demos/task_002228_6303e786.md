Run `tesseract /app/auth_token_schema.png stdout` and parse the token out of the recognized text programmatically, since OCR output may have noise; sanity-check it against a couple of known-clean lines in `/app/corpus/clean/` before relying on it. Build `/home/user/wal_classifier.sh` around a single strict anchored regex applied per-line (e.g. `[[ $line =~ ^...$ ]]`), matching the whole line: date/time, `TX-` plus exactly 4 digits, the extracted token, one of `SET|UPDATE|DELETE`, an uppercase/underscore key, and an alphanumeric/underscore value, separated by single spaces with no trailing whitespace. Read the file line-by-line carefully (watch `read` stripping vs `IFS=`, and check the file isn't empty and its final line isn't truncated/partial) and exit 1 on the first non-matching line, else exit 0. Validate against every file in both `/app/corpus/clean/` and `/app/corpus/evil/`. Finally build the manifest with `find /app/corpus/clean/ -type f -exec sha256sum {} \;` redirected to `/home/user/clean_manifest.txt`, matching `sha256sum`'s exact output format.
