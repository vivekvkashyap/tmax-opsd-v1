This is a multi-stage pipeline: transcribe audio, use the recovered values to drive a file-archival job, then serve a token-gated report. First, transcribe `/app/billing_voicemail.wav` with a local speech-to-text tool (e.g. a Python `speech_recognition`/whisper-style script) — do not guess the age threshold or auth token, read them off the actual transcript output. Second, walk `/home/user/cloud_data/` recursively, compare each file's mtime against "now minus <recovered threshold> days" (use `find -mtime` or `os.stat().st_mtime`), and for every match: mirror its relative path under `/home/user/archive_staging/` (`os.makedirs` for parent dirs) and move it there while accumulating count and total byte size; then `tar czf /home/user/cold_storage.tar.gz` the staged tree. Third, write the exact fstab line format `<remote>:<path> /mnt/archive nfs defaults,ro 0 0` to `/home/user/fstab_addition`. Fourth, implement a small HTTP server (Python's http.server or a bash nc/socat loop) bound to 127.0.0.1:8080 that parses the `Authorization: Bearer <token>` header, returns 401 on mismatch/missing, and on match returns exact JSON with the counted files/bytes — run it backgrounded and verify it's still listening before finishing.
