Work through this in order. First, try `make` in `/app/cJSON-1.7.15` and read the exact error — a static-archive build step likely uses the wrong archiver invocation (check the Makefile line that's supposed to invoke `ar` to create `libcjson.a`); fix that line so the archive command is correct, then confirm `libcjson.a` actually gets produced. Next build `/app/uptime-analyzer` against that library and resolve any remaining link errors (correct include/library paths). Then inspect `analyzer.c`'s percentage calculation: a classic bug here is performing integer division (e.g. `(pings - missed) / pings`) before casting to double, which truncates to 0 or 1 before multiplying by 100.0 — cast one operand to double (or the whole expression) before dividing so the division is floating-point. Rebuild and manually test against a few sample JSON payloads to confirm output like `Server X: NN.NN%` matches expected math with two decimal places, then place the final binary at `/home/user/uptime-analyzer-fixed`.
