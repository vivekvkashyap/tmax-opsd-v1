Run `cargo build --release` in `/home/user/rust_data_ext` first to see the actual borrow-checker error rather than guessing at the fix; the compiler will point to a location where an earlier immutable/shared borrow of the slice is kept alive across a later mutable iteration — restructure the code (e.g., read the value you need from that borrow before starting the mutable loop, or drop/scope the borrow) so the immutable and mutable borrows don't overlap, while preserving the function's behavior (compute sum and max first, then normalize in place, return original sum). After it compiles, copy the built `.so` to `/home/user/python_processor/libdataparser.so`. In `wrapper.py`, match the Rust C ABI exactly: `argtypes = [ctypes.POINTER(ctypes.c_double), ctypes.c_size_t]` and `restype = ctypes.c_double` (the existing code uses `c_float`/`c_int`, which is wrong for `*mut f64`/`usize` -> `f64`). For the tests, define a `sample_data` pytest fixture returning the given 5 floats, patch `app.report_analytics` with `unittest.mock.patch` (patch it where it's looked up, i.e. in the `app` module namespace), call `process_data`, and assert the returned sum, the in-place normalized values, and that the mock was called once with the sum. Finally run pytest with `-v`, redirecting output to `/home/user/test_results.log`, and check it shows a pass.