Do the two parts in either order but keep them decoupled: build/test the resolver against hand-crafted stdin input first, independent of the video, so bugs in graph logic don't hide behind video-extraction issues. For the video, use ffprobe to list streams and ffmpeg to extract the subtitle track (e.g. `ffmpeg -i /app/architecture.mp4 -map 0:s:0 out.srt`), then strip SRT timestamps/indices to get the plain `Source:Target` lines. For the resolver, implement Kahn's algorithm: build an adjacency list and in-degree count from `Source:Target\n` lines read until EOF, repeatedly pick the alphabetically-smallest zero-in-degree unvisited node, append it, and decrement in-degrees of its neighbors — if you run out of zero-in-degree nodes before all nodes are output, print exactly `{"error": "cycle_detected"}` instead. Print only the final compact JSON array (`["A", "B", ...]`, comma-space separated, matching a plain reference implementation's formatting) to stdout with nothing else, since your binary is fuzz-compared byte-for-byte against a reference. Provide a Makefile at /home/user/Makefile building resolver.c into /home/user/resolver. Finally pipe the extracted dependency lines into `/home/user/resolver` and save stdout to /home/user/video_graph_result.json.