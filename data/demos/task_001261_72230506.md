For the GPAK format: reverse-engineer by binary-diffing, not guessing. Pack several small, controlled test directories (single file, empty file, multiple files, files with names that sort differently) using `/app/bin/gpak-ref`, then hexdump the outputs and compare against each other/known-plaintext lengths to infer the header/field layout (magic bytes, counts, per-entry lengths, endianness, whether the payload is zlib-compressed — try `zlib.decompress` on suspected payload offsets to confirm). Iterate: change one variable at a time (add a file, rename a file, change file size) and see which bytes shift, to pin down field widths and ordering (note whether entries are alphabetically sorted). Implement `/home/user/gpak.py` to match byte-for-byte, then verify with `diff <(xxd ref.gpak) <(xxd yours.gpak)` on several fuzz cases before trusting it. For the pipeline: fix `/etc/nginx/sites-available/default` to proxy_pass `/api/` to the Flask/Gunicorn upstream on port 5000, fix Redis host in `/app/api/app.py` (likely wrong host/port), call your own gpak.py's unpack logic (import it or subprocess it) on uploaded files, count lines whose stripped text starts with `G0` or `G1` across all `.gcode` files, store the sum in Redis under the specified key, then restart nginx and the Flask/gunicorn process.