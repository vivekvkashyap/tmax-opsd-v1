Approach this as a set membership / cross-reference problem across three heterogeneous sources, plus a separate indexing question. In audit_backup.py: load relational.db with sqlite3 and collect the set of existing tx_id values from the transactions table; load documents.json and graph.csv, collecting every tx_id referenced (as last_tx_id in documents, and as tx_id in graph edges) into a combined set; missing_tx_ids is that combined referenced set minus the set that actually exists in the DB, sorted alphabetically (string sort, not numeric) — be careful to include a tx_id even if it's referenced by only one of the two sources. For invalid_doc_user_ids, validate each document strictly against the schema: user_id must be an int, preferences must be a dict (not a string or missing), and last_tx_id must be a string matching the pattern `tx` followed by only digits (use a regex like `^tx\d+$`) — any single violation makes that user_id invalid; sort the result ascending as integers. Write both lists into the exact JSON key names requested. Separately, for the slow query filtering on `user_id` and `status` with an ORDER BY on `created_at`, write a composite index over exactly those three columns in that order (equality columns first, then the sort column, matching the query's WHERE+ORDER BY) into index_strategy.sql, named idx_tx_user_status_created.