Write the script to dynamically enumerate `/home/user/intercepts/*.pem` (never hardcode filenames). For each file, determine RSA key size without letting openssl touch the network: run something like `unshare -n openssl rsa -in "$f" -noout -text` (or `-modulus`) and parse the reported bit length (look for "Private-Key: (N bit"); treat exactly 1024 as weak. For every weak file, `cp` it into `/home/user/vuln_redacted/` under the same name, then redact it in place: use `awk`/`sed` to find the line matching `-----BEGIN (RSA )?PRIVATE KEY-----` and the line matching the corresponding `-----END ... PRIVATE KEY-----`, and replace everything strictly between those two markers with a single literal line `[REDACTED]`, leaving the BEGIN/END lines and any certificate block elsewhere in the file untouched (a naive sed range delete can accidentally eat the markers themselves or clobber a certificate block that appears before/after in the same PEM bundle — test on a file with both a cert and a key to be sure only the private-key body is touched). Make the script executable with `chmod +x`, and test end-to-end against the four generated certs (two 1024-bit, one 2048, one 4096) to confirm only the weak ones land in vuln_redacted/.
