Two independent deliverables — tackle separately. First, recover the hidden seed: run OCR on the evidence image (e.g. `tesseract /app/evidence.png stdout`) to read the embedded text, then extract just the seed value and write it to `/home/user/extracted_seed.txt` — don't fabricate or guess it. Second, build the classifier by actually studying both corpora rather than hardcoding filenames: read files in `/app/corpus/evil/` and `/app/corpus/clean/` as raw bytes and look for concrete signal patterns that differ between the two sets — e.g. embedded ELF magic bytes (`\x7fELF`) inside non-executable image data, path-traversal substrings like `../`, or script/XSS-like tags (`<script`) appended or hidden in custom chunks. Write `/home/user/filter.py` to scan a given file's bytes for these anomaly signatures and `sys.exit(1)` if any is found, else `sys.exit(0)`. Validate your logic empirically: loop over every file in both corpus directories, run your script against each, and confirm exit codes match expectations (1 for evil, 0 for clean) before considering the task done — iterate on your detection heuristics based on any misclassifications you observe.