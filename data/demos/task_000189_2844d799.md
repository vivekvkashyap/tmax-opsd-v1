Work the incident in stages, verifying each before moving on. First, transcribe `/app/incident_voicemail.wav` with a speech-to-text tool (e.g. `whisper` or any available STT CLI/library) to recover the tenant ID spoken in the audio — do not guess it. Second, do Git forensics on `/app/uptime_repo`: check `git log --all`, `git reflog`, dangling commits (`git fsck --unreachable`), and `git log -p --all -- <file>` or `git grep` across all commits/blobs to find the removed `X-API-Key` string, since a plain working-tree search will miss history-only content. Third, in `server.py` find `calculate_ema` and rewrite it to strictly implement `S_new = alpha*V + (1-alpha)*S` — check current code for swapped terms or wrong operator precedence. Fourth, in the `/record_ping/{tenant_id}` handler, wrap the read-sleep-write critical section in an `asyncio.Lock` (per-tenant or global) so concurrent requests can't interleave and lose updates — a naive fix that only locks the write, not the read+write span, will still race. Finally, launch the service bound to `127.0.0.1:9090` and leave it running so the verifier's concurrent client can hit it.
