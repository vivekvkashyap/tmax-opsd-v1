Inspect `/app/sqlite-src/build.sh` first — look for compiler defines that quietly disable a feature you need; a `-DSQLITE_OMIT_...` flag related to CTE/recursive-query support is the kind of thing that silently breaks `WITH RECURSIVE` at compile time even though the code compiles fine. Remove that define, rerun the build to produce `libsqlite3.a`, and sanity-check the built library actually supports recursive CTEs before writing C against it. In `checker.c`, open the DB with `sqlite3_open`, then use `sqlite3_prepare_v2`/`sqlite3_step` to run a `WITH RECURSIVE` query over the `citations(source_id, target_id)` table that tracks the path taken so far and detects revisiting a node (a proper cycle-detection recursive CTE, not just reachability) — printing `EVIL`/exit 1 the moment a repeated node reappears in a path, else `CLEAN`/exit 0 once all paths are exhausted. Link against the fixed static library (`-L/app/sqlite-src -lsqlite3 -ldl -lpthread`), compile to `/home/user/checker`, and run it over every `.db` file in both `corpora/clean` and `corpora/evil` to confirm 100% correct classification before declaring done.
