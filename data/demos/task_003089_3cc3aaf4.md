Iterate over `/home/user/manifests/*.yaml` in alphabetical order (e.g. `for f in $(ls /home/user/manifests/*.yaml | sort)`), and for each file extract the `podCIDR:` value from the `spec:` block using `grep`/`awk`/`sed` (e.g. `grep 'podCIDR:' "$f" | awk '{print $2}'`) rather than a full YAML parser, since the structure is simple and consistent. For each file: append `ip route add <CIDR> via 10.96.0.1` to `/home/user/apply_routes.sh` (create it with a shebang and `chmod +x` if it doesn't exist yet — don't overwrite it on each run, append), append `PROCESSED <filename> <CIDR>` to `/home/user/operator.log`, then `mv` the file into `/home/user/processed_manifests/` and `chmod 0400` the moved copy (chmod after the move, targeting the destination path). Process files strictly in sorted order since the log/route order is checked. After writing `/home/user/k8s-operator.sh`, run it once so the existing manifests actually get processed and the output files exist before you finish.