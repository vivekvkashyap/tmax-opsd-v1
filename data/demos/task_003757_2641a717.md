Process this in clear stages so corrupted inputs never abort the pipeline. First, for each `.zip` in `/home/user/backups/`, run integrity verification (e.g. `unzip -t file.zip` or Python `zipfile.testzip()`/catching `BadZipFile`) and only proceed to extract the ones that pass — wrap extraction in a try/except (or check exit status) so a corrupt archive is simply skipped, not fatal. Extract valid archives into their own temp subdirectories to avoid filename collisions before flattening. Second, gather every `.json` candidate — both loose files directly in `backups/` and everything just extracted — and validate each with `json.loads` inside a try/except, keeping only files that parse cleanly; a JSON file with a trailing comma or missing comma should be silently dropped, not crash the script. Third, copy only the surviving valid JSON files by basename into a flat staging directory (watch for the archives possibly nesting subdirectories — you need just the basename at the tar root), then build `clean_configs.tar.gz` from that staging directory's contents (e.g. `tar -czf ... -C staging_dir .`) so no path prefixes leak in. Finally write the sorted basenames, one per line, to `summary.txt` using `sort`.
