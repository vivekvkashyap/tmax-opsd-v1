Reverse-engineer the header format by diffing clean vs evil samples with `xxd` on the first ~128 bytes: note whether the file starts with the `FF FE` BOM (UTF-16LE header, read first 128 bytes) or not (UTF-8 header, read first 64 bytes). Build extraction as a pipeline: `dd bs=1 count=<64 or 128> if="$file"` to grab the header window, `iconv -f UTF-16LE -t UTF-8` only when the BOM is present, then `tr -d '\0'` to strip padding nulls, then `jq -r '.data_size'` to pull the declared size. Compare that declared size against the real file size via `stat -c%s`/`stat -f%z`; if the declared size meaningfully exceeds the actual remaining bytes, this is the exploit signature — exit 1. Do NOT invoke `/app/artifact_reader` directly on evil samples inside your gatekeeper logic (it can crash or hang the process) — only run it cautiously and separately, e.g. under `timeout`, to study clean-file behavior while developing. The gatekeeper itself should be pure static parsing, never executing the reader.
