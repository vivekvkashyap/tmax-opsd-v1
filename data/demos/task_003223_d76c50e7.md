Start by reproducing the failure: run `python3 /home/user/scripts/build_report.py /home/user/data/query_logs.csv` and read the actual traceback to see exactly which exception (e.g. a `ValueError` from casting `ExecutionTime` to float, or a `csv`/encoding error from an embedded null byte) and roughly which row triggers it, rather than assuming. Then scan the CSV programmatically (e.g. a small Python or awk pass over each row) checking specifically for the conditions the script depends on — that `ExecutionTime` parses as a float and that the row contains no null bytes/control characters — and collect every row that fails either check, not just the first one, since there may be more than one corrupt row. Write the passing rows (with header) to `/home/user/data/query_logs_fixed.csv`, and write the sorted `QueryID` values of the failing rows, one per line, to `/home/user/bad_queries.log`. Do not touch `build_report.py`. Finally rerun the build script against the fixed CSV and confirm it exits 0 and prints a successful report — that's your proof the isolation was complete and correct.