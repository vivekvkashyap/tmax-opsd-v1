Separate the two requirements — baseline policy matching and metacharacter blocking — into independent checks, since both must hold for exit 0. First, OCR `/app/policy_baseline.png` with `tesseract` and carefully read the required key-value pairs (re-run with different settings if ambiguous) — these become hardcoded constants your validator checks against, not something derived from the input file. In `validate_sshd.py`, parse the config line by line, splitting each non-comment line on the first run of whitespace into key/value, normalize keys to lowercase, and build a dict; confirm every required baseline key is present with a value matching exactly (after stripping whitespace) what you read from the image. Separately, scan the raw file content (including comment lines, since the spec says "anywhere in the file") for the literal characters `;`, `|`, `&`, or backtick, and fail immediately if found. Exit 0 only if both checks pass, exit 1 otherwise. Test manually against a few hand-written sample configs — one clean, one per violation type — before finishing.
