Work through the three broken pieces independently and verify each before moving to the next. First fix `build.sh`: run it, read the actual error (bad package name/version pin or venv creation issue), and correct it so the virtualenv and dependencies install cleanly. Second, in `detector.py`, locate the line-crossing detection math — a 2D line/segment intersection typically uses a determinant-based formula (cross products of direction vectors); check the sign conventions and denominator terms against the standard line-intersection formula, since a flipped sign or wrong term is what's producing negative counts and missed crossings. After fixing, run the engine against `/app/traffic_cam_04.mp4` and sanity-check the resulting counts in `/home/user/data/traffic.db` look plausible (no negatives) before trusting them. Third, in `server.js`, inspect the SQL query's `GROUP BY`/aggregation — a common bug is grouping by the wrong column or double-counting/undercounting via a misapplied DISTINCT; fix it to correctly group and sum by direction and hour. Start the Node service on `0.0.0.0:8080`, then hit `GET /api/stats?auth=vision2024` with curl yourself to confirm the JSON shape and that missing/wrong auth returns 401, before leaving it running.