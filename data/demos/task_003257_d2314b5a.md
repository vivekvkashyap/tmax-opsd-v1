For Part 1, extract frames from `/app/deployment_monitor.mp4` with `ffmpeg -i deployment_monitor.mp4 frames/frame_%04d.png` (or per-frame extraction), then scan each frame for the "Node-3" panel showing "KEY_REJECTED" — either OCR each frame with `tesseract` and grep for the phrase near the Node-3 label, or crop the Node-3 region and detect the red-flash/overlay programmatically. Map ffmpeg's 1-indexed output filenames to actual frame numbers carefully (verify against total frame count via `ffprobe`), and only record frames where the label is specifically "Node-3", not other nodes. For Part 2, write the parser to be robust under fuzzing: use a single non-greedy regex like `(?:Connection closed by |Disconnecting from )(\d{1,3}(?:\.\d{1,3}){3})` via `re.findall`, then validate each captured group is a real IPv4 (each octet 0-255) before keeping it — reject malformed digit sequences. Deduplicate and sort the IP list, count `publickey` occurrences as a plain substring count (not word-boundary), and emit compact JSON via `json.dumps` with exactly the two keys in that order. Handle missing argv gracefully rather than crashing, since the fuzzer will pass edge-case and empty strings.
