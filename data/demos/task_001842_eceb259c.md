Split this into two passes meeting only at the dedup step. First, use `cv2.VideoCapture` to iterate all 1800 frames, and for each compute the mean of the top-left 10x10 block (`frame[0:10, 0:10]`) per channel (OpenCV is BGR, index accordingly), flagging red if R>200, G<50, B<50; a second S (0–59) is a "retry second" if *any* of its 30 frames (`30*S`..`30*S+29`) is flagged. Second, stream `config_events.jsonl` line by line in file order while maintaining `dict[service_id] -> set(seen_hashes)`; for each event, check membership *before* updating the set — it's a duplicate to drop only if its second is a retry second AND its `(service_id, config_hash)` pair was already seen earlier in the stream. After filtering, compute the 5-second rolling sum: for each integer second T in 0..59, sum `payload_size` over kept events with `T-4 < timestamp <= T` (half-open, strict `>` lower bound), defaulting to 0 for empty windows, and write the two-column CSV.
