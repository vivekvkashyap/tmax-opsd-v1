Read `auth_backend.cpp` carefully to identify the exact cipher: it's a linear congruential generator producing a keystream byte-by-byte (`state = (state * A + C) % 256` starting from a fixed seed), where each plaintext byte is XOR'd with the current LCG state to produce one ciphertext byte per two hex characters. Locate the admin line in `proc_logs.txt` and extract its hex token. Since XOR is self-inverse and the LCG is fully deterministic from the same seed/multiplier/increment found in the source, write `decrypt.cpp` that: parses the hex string into bytes, re-runs the identical LCG state update for each byte position (same seed and recurrence as the encryption function), and XORs each ciphertext byte with the corresponding keystream byte to recover the plaintext character — do not attempt to brute-force; simply mirror the forward algorithm since it's symmetric. Compile with `g++`, run it against the extracted admin token, and write only the recovered plaintext (no trailing newline or extra text) to `/home/user/admin_pass.txt`. Double check by comparing against the shorter guest/testuser tokens in the log if you want a sanity check on your LCG parameters.