Run `tesseract /app/rule.png stdout` first to recover the exact `EXTENSION` and `KEYWORD` values from the image — do not assume or hardcode plausible-looking values without OCR'ing them, and double check the OCR output for stray whitespace/case issues before embedding it in code. Write the Go program to take `os.Args[1]` as the root directory, walk it with `filepath.WalkDir`, and for each regular file whose name ends with the recovered extension (use `strings.HasSuffix`, exact match including the dot), open it and wrap the reader with `compress/gzip.NewReader` — wrap the gzip read in error handling so a corrupt/non-gzip file is skipped rather than crashing the whole scan, since the spec requires graceful handling of unreadable files. Stream-read the decompressed content line by line (`bufio.NewScanner` over the gzip reader) rather than buffering the whole decompressed content in a big string, and increment a global counter for every line that contains the exact keyword as a substring (`strings.Contains`, case-sensitive — do not lowercase either side). After the full walk, print only the integer count followed by a newline via `fmt.Println(count)`. Compile with `go build -o /home/user/archive_scanner` exactly at that path, and test it manually against a small self-made nested/gzip fixture with known matching and non-matching lines before considering it done, since the grader will fuzz-compare byte-for-byte against a hidden oracle.
