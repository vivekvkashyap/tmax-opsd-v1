Fix the C server first: wrap the existing `accept()`/`write()`/`close()` sequence in a `while(1)` loop so the process keeps listening after each connection instead of exiting; leave the response string and socket setup (bind/listen) untouched, and recompile it to `/home/user/health_server`. For the supervisor script, write a loop that: writes `$$` to `/home/user/supervisor.pid` once at the top; then in each iteration launches `health_server` with output redirected to `/home/user/server.log` (`>> ... 2>&1`), captures its PID with `$!` and writes it to `/home/user/server.pid` immediately, then `wait`s on that PID before looping again with a `sleep 1` — this way a restart naturally updates server.pid each time. For deploy.sh: compile to a temp file (e.g. `mktemp`), `mv` it over `/home/user/health_server` (mv is atomic on the same filesystem, unlike direct overwrite via recompilation), then read the PID from `/home/user/server.pid` and `kill -TERM` it — do not touch supervisor.pid, since the supervisor process itself must keep running and detect the child's exit to restart it. Remember `chmod +x` on both scripts.