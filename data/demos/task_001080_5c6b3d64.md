Diagnose before touching code. Run `strace -f -e trace=open,openat python3 server.py` (or similar) to see the silent exit is caused by a failed `open()` on a config file path the server expects but that doesn't exist — note the exact path it tried. Separately, inspect `traffic.pcap` (e.g. `tcpdump -nr traffic.pcap` or a quick `scapy` script reading `UDP` layers) to find the destination port the recorded packets target, and write a config file at the deduced path containing that port so the server can bind correctly. For the concurrency bug: replay traffic against the running server and reproduce the crash/wrong totals, then read `server.py`'s packet-handling code — look for a shared dict/counter being read-modified-written across multiple handler threads without protection; wrap that critical section (read current value, update, store) with a single `threading.Lock` (`with lock:`) so increments can't interleave and lose updates. Then start the fixed server in the background, run `python3 replay.py <port>`, wait for the `SHUTDOWN` packet to trigger the final write, and confirm `/home/user/metrics_out.json` reflects fully-summed, non-dropped counts.