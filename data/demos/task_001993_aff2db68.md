This is a black-box equivalence task, not a design task — your Python script's behavior must match the compiled oracle byte-for-byte, so derive everything from observation rather than assumption. First, transcribe `/app/voicemail.wav` (e.g. with a local speech-to-text tool or by listening) to recover the spoken 6-digit numeric key; don't guess it. Second, probe `/app/token_oracle` with a range of hand-crafted Cookie strings (missing token, odd-length hex, invalid hex chars, valid hex decrypting to various domains) and record its exact stdout for each — this tells you the precise regex used to locate `redirect_token=`, the error-message strings, and the exact allow/deny condition, without needing to disassemble the binary. Implement `/home/user/policy_enforcer.py` to: extract the token value with a regex tolerant of surrounding cookie pairs; validate hex parity and characters; XOR-decrypt the bytes against the recovered key's ASCII repeating cyclically; attempt ASCII decoding; and print `ALLOW: <url>` only if the URL starts exactly with the trusted prefix, else the matching DENY/ERROR string. Match error message text and formatting exactly, including edge cases like decrypted bytes that aren't valid ASCII.