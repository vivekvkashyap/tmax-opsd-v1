First extract frames from the video with ffmpeg (e.g. `ffmpeg -i /app/intercept_debug.mp4 -vf fps=30 frame_%03d.png`) and inspect frames within the first 5 seconds for a terminal window showing two exported shell variables — a numeric XOR key and a string auth prefix. Read the values directly off the image rather than guessing. Then implement /home/user/auth_analyzer.py following the spec's exact order: hex-decode the argument, then UTF-8-decode it (print ERR_DECODE and exit on failure); split on \r\n and parse the Cookie header's `session=` value and the X-Cert-Chain header (ERR_MISSING if either is absent); require the cert value contain `CN=Admin` AND its MD5 hex digest end in `f` (else ERR_CERT); base64-decode the session cookie (ERR_B64 on failure); XOR every decoded byte with the numeric key you read from the video; UTF-8-decode the result (ERR_AUTH on failure); and check it starts with the recovered prefix, printing `SUCCESS:<string>` or `ERR_AUTH`. Match every error string and exit code exactly, since a fuzz-equivalence harness will diff your output against a reference implementation across thousands of malformed and well-formed inputs.
