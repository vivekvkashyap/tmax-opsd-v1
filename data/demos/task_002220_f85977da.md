Since Python/R are unavailable, do every numeric step with awk (for floating point) and coreutils. Phase 1: for each of Feature_A/B/C, pass over raw_data.csv once with awk to find min/max, then again to compute `(value-min)/(max-min)`, formatted with `printf "%.4f"` — write ID, scaled values, and integer Target to engineered.csv with the exact header. Phase 2: compute Pearson correlation between each Norm_X and Target directly in awk (sum of products minus n*mean*mean, normalized by both standard deviations), accumulating sums in one pass, formatting each line as `Norm_A: <value>` with 4 decimals. Phase 3: take REF_001's three normalized values, compute Euclidean distance to every other row in awk/bc, sort numerically ascending with ID as alphabetical tiebreaker (`sort -t, -k2,2g -k1,1`), and write only the top 3 IDs to similar.txt. Chain each phase off the previous phase's rounded output, not raw high-precision intermediates, since that's what the spec implies, and sanity-check a few rows by hand before finishing.