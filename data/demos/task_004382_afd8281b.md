Scaffold a real Cargo project (`cargo new` in `/home/user/etl_pipeline`) and add the `csv`, `serde` (with `derive`), and `serde_json` crates to `Cargo.toml` rather than hand-rolling CSV parsing — the `csv` crate's `Reader` correctly handles quoted fields with embedded newlines, which naive line-splitting cannot. Read `/home/user/raw/data.csv` with `csv::Reader`, and process rows in file order: track seen `id`s in a `HashSet`/`HashMap` and skip any row whose `id` was already kept (first occurrence wins). For each remaining row, trim and lowercase `email`, drop the row if the result lacks `@`; validate `dob` against `YYYY-MM-DD` with a regex or manual digit/hyphen-position check, dropping non-matches; mask `ssn` by keeping only the last four digits and prefixing `***-**-` (don't just slice blindly — locate the last hyphen). Serialize each surviving record as one JSON object per line (a `serde_json::to_writer` plus a manual newline, not `serde_json::to_writer_pretty`) preserving all six keys, and write to `/home/user/processed/output.jsonl`. Build and run with `cargo run`, then inspect the output file to confirm row count and field values match your manual trace of the input before finishing.
