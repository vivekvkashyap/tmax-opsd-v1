Treat this as three independent bug classes plus one new component, and verify each in isolation before wiring the pipeline end-to-end. For the C extension build failure, inspect `setup.py`'s `Extension(...)` call — a link failure for math functions (e.g. `sqrt`, `pow`) usually means the `libraries` argument is missing the `m` math library; add it and rebuild with `python setup.py build_ext --inplace` (or equivalent) to confirm it compiles cleanly. For the precision bug in `inference.py`, grep for any explicit `astype(np.float32)` or similar downcast between JSON parsing and the call into the C extension, and remove it so values stay float64 throughout. For the Redis connectivity bug, diff the `REDIS_HOST`/`REDIS_PORT` (or equivalent env vars) set in `start_services.sh` against the actual port Redis is listening on, and correct any mismatch. For the sanitiser, write `clean_payload` to explicitly validate each numeric field: reject non-finite values (NaN/Inf via `math.isfinite`), reject values outside [-1e10, 1e10], and attempt-parse string inputs before checking bounds, raising `ValueError` on any failure. Start all services and smoke-test the full request path (8001 -> 8002 -> 6379) before writing the final completion log.
