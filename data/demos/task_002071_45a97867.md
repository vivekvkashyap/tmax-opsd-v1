Treat this like resolving a dependency graph by symbol inspection rather than guesswork. For each static library in `/home/user/ws_proxy/packages/`, run `nm -A` (or `nm` per file) to list defined (T) and undefined (U) symbols. Start from what `server.c` needs (`ws_init_secure`) — find which `libws` version defines it, then note that version's undefined references; find the `libauth` version that defines that missing symbol, note its own undefined references in turn, and find the `libfilter` version that satisfies those with no further undefined refs — this chains to exactly one consistent triple. Update the Makefile's `-l` flags (and library search path order — GCC resolves left-to-right, so list them in dependency order: the library needing a symbol before the library providing it, or use `-Wl,--start-group ... --end-group` if link order is finicky) to that triple, then run `make`. Once `proxy_server` builds, launch it in the background (`./proxy_server &`), then act as a WebSocket client (e.g. a small Python script using the `websockets` library, or `websocat` if available) to connect to `ws://127.0.0.1:8080`, send the exact JSON message specified, capture the raw response text, and write only that response string into `/home/user/ws_response.log`.