Read pipeline.log and the code together to locate both bugs before editing. For the encoding bug, don't hardcode a single encoding — detect it per file, e.g. read the file's raw bytes and check for a UTF-16 BOM (`\xff\xfe` or `\xfe\xff`) or attempt `open(..., encoding='utf-8')` inside a try/except UnicodeDecodeError and fall back to `encoding='utf-16'`; a library like `chardet`/`charset-normalizer` is optional but a manual BOM/try-except check is enough and keeps behavior deterministic. For the overflow bug, don't just wrap `math.exp` in a try/except — replace `compute_activation` with the numerically stable piecewise sigmoid: for x >= 0 use `1/(1+exp(-x))`, and for x < 0 use `exp(x)/(1+exp(x))`, which avoids ever computing exp() of a large positive number while remaining mathematically identical to the standard sigmoid for all inputs. After fixing both, run processor.py directly (no args) and inspect output.json to confirm every sensor key from all three files is present and no exception was thrown, rather than assuming success from a clean exit code alone.
