Process the JSONL line by line, validating and cleaning each record independently before deciding to keep or drop it. For `product_id`, require it present and matching exactly 8 alphanumeric characters (e.g. a regex like `^[A-Za-z0-9]{8}$`); anything else drops the row. For `price`, handle all three input shapes: if it's already numeric use it directly, if it's a string strip out everything except digits/`.`/`-` (regex extraction of the first numeric substring handles forms like "$12.99" or "12.99 USD"), convert to float, and drop the row if extraction fails or the result is <= 0. For `category`, keep it only if it exactly matches one of the four allowed values; anything missing or different becomes `'other'` — don't silently keep unexpected categories. Compute `discounted` by checking `original_price` exists, is itself a valid number (guard against non-numeric junk), and is strictly greater than the cleaned price. Bucket price into low/medium/high using the given cutoffs. Finally sort surviving rows by `product_id` ascending and write the CSV with the exact column order, letting Python's `csv` module handle proper True/False boolean stringification.
