OCR `/app/system_specs.png` (crop/upscale/threshold with ImageMagick first if the handwriting is faint) to recover the damping coefficient (ζ) and natural frequency (ω0) — treat these as unknowns to extract, don't guess typical textbook values. Compile `/home/user/integrator.c` normally, but wire `DAMPING_COEF`/`NATURAL_FREQ` as environment variables read via `getenv` inside the program (or in a wrapper shell script that `export`s them before invoking the integrator) rather than baking them into the C source as literals, so the stabilization is driven by the OCR'd values. In `analyze.sh`, pipe the integrator's displacement time series through a Python FFT (`numpy.fft.rfft` on evenly sampled output) to find the dominant frequency bin, then wrap a small search loop (grid search over candidate H_MAX values, or a simple bisection/gradient step) that reruns the integrator at each candidate step size and picks the H_MAX minimizing `|simulated_dominant_freq - ω0|`. Finally expose it via a minimal HTTP server (Python's `http.server` with a custom handler, or similar) on 127.0.0.1:8080 that checks the `Authorization: Bearer perf-eng-token-2024` header (401 if missing/wrong), parses the JSON body's `duration`, runs the optimized integrator for that long, reruns the FFT analysis, and returns `{"h_max":..., "dominant_frequency":...}` as JSON.