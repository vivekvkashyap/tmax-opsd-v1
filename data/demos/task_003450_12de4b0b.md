Work this like a real incident: fix the build before touching the crash. Run `make` in `/home/user/aero_service`, read the error, and check the Makefile/library search paths (e.g. `LD_LIBRARY_PATH`, `-L`/`-I` flags, or a moved shared library) against what actually exists on disk — correct the path rather than reinstalling anything. Once it builds, reproduce the segfault by running the binary on `/app/blackbox_audio.wav`; enable core dumps (`ulimit -c unlimited`) and use `gdb <binary> core` (or run under gdb directly) to get a backtrace pinpointing the crashing function and line. Because the crash is triggered only by specific frames, apply delta-debugging: bisect or instrument the code to find the minimal audio offset/frame that reproduces the fault, which will point you at the exact buffer read/write causing corruption. Fix the root cause in the C++ source — most likely a missing bounds check before writing/reading a buffer during decode/serialize — not just by catching the crash. Recompile, rerun on the real input to produce `/home/user/output_reconstructed.wav`, and confirm it's a valid, playable WAV (correct header, sane sample count) before considering the fix complete, since the eventual grading compares its waveform against a clean reference via MSE.
