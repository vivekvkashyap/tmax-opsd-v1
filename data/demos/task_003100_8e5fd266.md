First locate and fix the vendored bug: open `/app/data_purify/data_purify/cleaner.py` and find the `filter_valid` function — a naive read might miss a subtle logical short-circuit like `... and False` appended to the filter condition, which unconditionally empties the list regardless of the category check; remove only the erroneous clause so the function correctly keeps records where `category != "unknown"`. Reinstall the package (`pip install -e /app/data_purify` or equivalent) so your script picks up the fix, and verify with a quick interactive import before moving on.

For `/home/user/process.py`: read all of stdin as JSON, call the fixed `filter_valid`, then extract paired (x, y) lists from the surviving records. Implement Pearson correlation and OLS manually using population (N-divisor, not N-1) formulas exactly as specified — do not rely on library defaults like numpy's `corrcoef`/`polyfit` without checking they match population semantics, and numpy's ddof=0 var/cov are population by default so that's a safe match. Guard against fewer than 2 valid records or zero variance in x or y (would divide by zero) — in those cases and when correlation <= 0.5, output `0.0000`. Format the final print with `f"{value:.4f}"` and print nothing else to stdout.