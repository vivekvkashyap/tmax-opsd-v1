Write the parser as a small, careful state machine rather than relying on `sscanf` alone, since malformed inputs must be distinguished precisely. First check the literal prefix `SEC-PROTO V` with `strncmp`; if it doesn't match, print the malformed-prefix error and exit 1 immediately. Otherwise manually split the remainder on `.` and validate each of the three components is non-empty and consists only of digits (reject anything with letters or missing parts) before calling `atoi`/`strtol` — this is where naively using `sscanf("%d.%d.%d", ...)` goes wrong, since it silently accepts a garbage token like "a" as 0 instead of flagging invalid format. Compare the parsed (major,minor,patch) tuple against (1,5,2) using standard lexicographic version comparison (major first, then minor, then patch) to decide ACCEPTED (>=) vs REJECTED (<), matching the exact required exit codes 0/1/2 and output strings. Guard the `printf("Listening...\n")` and the outer read-one-line-vs-loop-until-EOF behavior with `#ifdef FUZZ_MODE`/`#else` so the same source compiles to both `sec_server` (single line) and `sec_fuzzer` (loop over stdin ignoring exit codes) via `-DFUZZ_MODE`. Build inputs.txt exactly as specified, run the fuzzer against it, and redirect stdout to qa_report.log — double check line-by-line against the version comparisons before finishing.