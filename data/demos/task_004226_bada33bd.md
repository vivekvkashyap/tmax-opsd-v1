Build the pipeline in layers and test each independently before combining. First, `process_logs.sh`: use `sed`/`awk`/regex to replace the *values* of `api_key=` and `card=` query params with `REDACTED` while leaving other params and the surrounding request line untouched (e.g. `sed -E 's/(api_key|card)=[^& ]*/\1=REDACTED/g'` applied to the log content) — write the redacted result to a temp file, then pipe it through `openssl enc -aes-256-cbc -pbkdf2 -salt -in <tmpfile> -pass file:/home/user/certs/audit_key.pem -out /home/user/audit_trail/secure_audit.log.enc`, and clean up the temp file. Verify decryption round-trips correctly with `openssl enc -d` using the same flags before moving on. Second, `run_sandbox.sh`: construct a `bwrap` invocation with `--unshare-net`, `--ro-bind / /`, `--dev /dev`, `--ro-bind /home/user/raw_logs /home/user/raw_logs`, `--ro-bind /home/user/certs /home/user/certs`, `--bind /home/user/audit_trail /home/user/audit_trail`, ending with the command to run `/home/user/process_logs.sh` inside. Make both scripts executable with `chmod +x`, then run `run_sandbox.sh` and confirm the `.enc` file appears and decrypts to show redacted-but-otherwise-intact log lines.
