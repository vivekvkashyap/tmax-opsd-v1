Build this pipeline exactly as sequenced to avoid the data-leakage bug: split BEFORE any dimensionality reduction, and fit PCA only on the training partition. In `/home/user/pipeline.sh`, export `OPENBLAS_NUM_THREADS=1` and `OMP_NUM_THREADS=1` first (this pins down floating-point reduction order so results are reproducible run to run), then use `head -n 800 data.csv > train.csv` and `tail -n 200 data.csv > test.csv` to split without a header row to worry about. In `/home/user/pca.py`, load each CSV with pandas/numpy (no header), slice columns 0-9 as features and column 10 as labels, call `sklearn.decomposition.PCA(n_components=5, svd_solver='full').fit(train_features)` — fit exclusively on train — then `.transform()` both train and test features, and write each transformed-features-plus-label matrix to CSV with no header/index. In `/home/user/train.py`, fit `LogisticRegression(random_state=42)` on the PCA'd training data, predict on PCA'd test data, and write only the predicted labels one per line. Finally in the bash script, use `paste`/`awk` to compare `predictions.txt` against the last column of `test.csv` and compute correct/total as a float into `/home/user/accuracy.txt`. Run the whole script end-to-end and confirm each intermediate file (train.csv, test.csv, train_pca.csv, test_pca.csv, predictions.txt) actually has the expected row counts before trusting the final accuracy.