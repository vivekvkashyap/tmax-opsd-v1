First run OCR (`tesseract /app/hub_graph.png stdout` or similar) to extract the list of valid directed edges from the image; inspect the raw OCR output carefully and clean up any misreads (OCR often confuses similar characters) before trusting the edge list — cross-check node names for consistency across lines. Represent the extracted edges as a set of `"Source->Dest"` strings (or a source->set-of-targets map) either hardcoded into your Go program or loaded from a text file at runtime. Write `/home/user/filter.go` to read the input file line by line, split each route on commas to get the node sequence, then walk consecutive pairs checking each transition against your valid-edge set — remember edges are directed, so `(A,B)` valid does not imply `(B,A)` valid. If every transition in every route is valid, exit 0; if any single route has any single invalid transition, exit 1 immediately (or track a global failure flag). Compile with `go build -o /home/user/filter filter.go` and test it against sample valid and invalid route files before relying on it, since off-by-one errors in the pairwise walk are the most common bug here.