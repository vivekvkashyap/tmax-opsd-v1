Start by reading `/app/backup_graph_lib-1.0.0/setup.py` to find the deliberate typo in `install_requires` — a misspelled dependency name that should map to a real, already-available package (e.g. a graph library). Fix only that string, then `pip install -e /app/backup_graph_lib-1.0.0/` and confirm import succeeds before writing any server code. For the HTTP service, pick one framework (Flask/FastAPI/http.server) and implement routes incrementally, testing each with `curl` as you go: `/health` first (no auth), then wrap the rest with Bearer-token middleware that reads the token from `/app/config/auth_token.txt` at startup and returns 401 on mismatch or missing header. For `/critical_nodes`, build a directed graph from the `edges` list in `/app/data/backups.json` and rank nodes by in-degree (networkx `in_degree()` or a manual dict counter) — take the top 3. For `/window_top_backups`, group nodes by `region`, sort each group by `size_mb` descending, and slice the top 2 IDs per group. For `/aggregate`, implement `$match`-style filtering on the given field/value then sum the requested numeric field, returning an integer. Launch the server backgrounded (e.g. `nohup ... &`) redirecting stdout/stderr to `/home/user/service.log`, then verify all four endpoints with `curl` before finishing.
