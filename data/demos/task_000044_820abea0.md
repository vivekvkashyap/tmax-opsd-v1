Process files defensively in stages rather than assuming every file is a well-formed archive. First pass: open each file in `/home/user/project_assets` in binary mode and read only the first 4 bytes to check against the ZIP magic `b'\x50\x4b\x03\x04'` — skip non-matches immediately (e.g. the PNG-header file). Second pass, only on magic-matched files: try `zipfile.ZipFile(path)` and call `.testzip()` (or catch `zipfile.BadZipFile`/`zipfile.LargeZipFile` around opening/reading) to detect truncated/corrupted archives, and skip any that fail — don't let one bad file crash the whole scan. Third pass, only on valid archives: check the namelist for an entry named exactly `metadata.json` (not a path ending in it), read and `json.loads` it, and pull out `asset_id`, `type`, `version`. Collect `(filename, asset_id, type, version)` tuples, sort by filename, and write with Python's `csv.writer` (not manual string joins) including the exact header row, so quoting/formatting matches expectations.
