Break this into independent, verifiable stages rather than trying to write the C++ program first. (1) Transcribe `/app/data/lab_dictation.wav` with an available speech-to-text tool (e.g. `whisper` if installed, or `ffmpeg` plus a local ASR library); listen/inspect the raw output carefully since dictated letters (A, C, G, U) and numbers are easy to mis-transcribe — normalize the recovered text into a clean RNA sequence string and a time value, and write exactly what you recover to `/home/user/transcript.txt`. (2) Design the C++ program's math independently from first principles: build a directed graph of structural states from the sequence, define transition rates, then implement RK4 integration of the CTMC's ODE system for state probabilities at t (using the time value you transcribed), and finally derive/normalize a steady-state distribution, printing space-separated floats to 4 decimals. (3) Since you must match a hidden reference bit-for-bit under fuzzing, be rigorous about numeric formatting (printf with `%.4f`), argument parsing (exactly one CLI arg, the sequence), and deterministic algorithm choices — ambiguity in graph construction or integration order is the most common source of mismatch, so keep the model as simple and well-specified as possible and test it yourself across several sequence lengths (10-50) for stability before finishing.
