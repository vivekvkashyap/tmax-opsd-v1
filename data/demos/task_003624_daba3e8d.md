Build this bottom-up and test each layer before wiring the next. First write `operator.c` as a plain blocking TCP server (socket/bind/listen/accept loop on `127.0.0.1:<port>`) that, on each accepted connection, reads the request (you can ignore its contents), scans `/home/user/manifests/` for `*.yaml` files, and for each writes `/home/user/mail_spool/<name>.processed` containing exactly `Processed <name>` (no trailing newline unless you intend one — match the literal string precisely), then writes the minimal HTTP response and loops back to accept() rather than exiting. Compile with gcc, launch two instances in the background on ports 8081 and 8082, and curl each directly to confirm the spool files appear before touching HAProxy. Then write a minimal `haproxy.cfg` with a `frontend` bound to `127.0.0.1:8080` and a `backend` in roundrobin mode listing both `127.0.0.1:8081`/`8082` as servers, start it with `haproxy -f ... -D`, and curl port 8080 to confirm it proxies through. Only then write `cicd.sh` to create `deployment.yaml`, curl the load balancer, and chmod +x it. Verify end-to-end by rerunning `cicd.sh` and checking the exact spool file content and that both operator ports plus 8080 are listening.
