Build the solver in layers, not all at once: first write a `residual(U)` function returning the vector of interior-point residuals `(u_{i-1} - 2u_i + u_{i+1})/h^2 - u_i^2 - f(x_i)` where `u_full` is the interior unknowns padded with the Dirichlet boundary values `u(0)=1, u(1)=e`; call `scipy.optimize.fsolve` on this with an initial guess of all-ones, then reassemble the full grid including boundaries. Compute f(x) analytically from the given u(x)=e^x target (f(x) = e^x - e^(2x)) — do not skip re-deriving this, it must match exactly for the residual to be correct. Loop over N = 10, 20, 40, 80, computing `h = 1/N`, solving, and taking `max(abs(u_numeric - exp(x)))` as the L-infinity error at grid points x = linspace(0,1,N+1). Format each error in scientific notation (Python's `f"{err:e}"` gives the right form) and write the exact CSV header `N,MaxError` followed by one row per N, in increasing N order. A common mistake is off-by-one indexing between interior unknowns and the full padded array — double check that `u_full[i]` lines up with `x[i]` before computing residuals or errors. Actually run the script so the log file is produced, not just written.
