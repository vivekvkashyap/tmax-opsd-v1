Build this as a standard Rust binary crate with `cargo new vowel_embedder` in `/home/user/vowel_embedder`, add `csv`, `rusqlite` (with bundled sqlite feature to avoid linking issues), and `clap` to Cargo.toml. Structure the program so it always (a) reads dataset.csv, computes the 5-dim vowel-count vector per row (lowercase the text first, count a/e/i/o/u), and upserts rows into the `embeddings` table at /home/user/embeddings.db; and (b) when `--query <id>` is passed, loads all embeddings, computes squared Euclidean distance from the query row to every other row, and prints only the id of the minimum-distance row (ties broken by smallest id) with no extra output. Write /home/user/run_pipeline.sh to `cargo build --release`, run the binary once to populate the DB, then run it again with `--query 42` redirecting stdout to /home/user/closest.txt, and chmod +x the script. Common pitfalls: recomputing/duplicating embeddings on every run (use INSERT OR REPLACE or clear the table first), off-by-one tie-breaking, and printing debug output alongside the id which corrupts closest.txt. Verify by inspecting the actual dataset.csv contents and computing vowel counts by hand or with a quick Python one-liner before trusting the Rust output.