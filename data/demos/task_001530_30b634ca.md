Start by extracting the required text from `/app/schema_mapping.png` using OCR (e.g., `tesseract`) or careful visual inspection — do not guess the schema or pattern; read the image to learn the exact node/edge field names, the definition of an "active" row, and the exact subgraph pattern (which node types connect via which edge types, and any property filter) you must match. Since the index is stated to be corrupted/misleading, write your SQL to explicitly filter on the underlying boolean column yourself (e.g., `WHERE is_deleted = 0`) rather than relying on any existing index, and consider issuing `PRAGMA` hints or just avoiding the index entirely via a full predicate scan. In your C++ program, load matching nodes/edges into in-memory adjacency structures (e.g., hash maps keyed by node id) and do a straightforward 3-hop path traversal (or general BFS/DFS pattern match) matching the exact chain and property condition described in the image — build your own indexing (e.g., a map from source_id to outgoing edges) for efficiency rather than nested O(n^2) scans. Write matches as a JSON array of objects with the exact key names shown in the task's example schema to `/app/matches.json`, and sanity-check a couple of rows by hand against the raw SQLite tables before finishing.
