Build and test each stage independently before chaining them. First write `redact.c` as a simple stdin-to-stdout line filter: read each line, search for the literal substring `PASSWORD=`, and if found, scan forward from just after it to the next whitespace/newline/EOF, replacing that captured token with the literal text `[REDACTED]` while leaving the rest of the line untouched — then compile with `gcc -o /home/user/redact redact.c` and sanity-test it manually with a sample line before wiring it into the pipeline. Then write `archive.sh`: create `/home/user/clean_logs`, iterate over `/home/user/raw_logs/*.log` with a loop (e.g. `for f in /home/user/raw_logs/*.log`), pipe each through `./redact < "$f" > /home/user/clean_logs/$(basename "$f")`, then `cd` into `clean_logs` and run `sha256sum *.log > manifest.sha256` so the manifest lists bare filenames (not full paths), then `cd` back out and `tar -czf /home/user/backup.tar.gz -C /home/user clean_logs`. Verify the final tarball by extracting it elsewhere and checking no plaintext passwords remain and `sha256sum -c manifest.sha256` passes.
