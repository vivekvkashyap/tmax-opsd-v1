Use the standard-library `sqlite3` module with parameterized placeholders (`?`) for `start_year`/`end_year` rather than string-formatting SQL, both for safety and correctness. Deduplicate citations with a window function in a CTE, e.g. `SELECT source_id, target_id, citation_date FROM (SELECT *, ROW_NUMBER() OVER (PARTITION BY source_id, target_id ORDER BY citation_date DESC) rn FROM citations) WHERE rn = 1` — this keeps only the latest-dated row per (source_id, target_id) pair. Join that deduplicated edge set against `papers` twice (once for source, once for target), filtering both endpoints' `year` to be within `[start_year, end_year]` inclusive. Build the node set as the union of all source_ids and target_ids appearing in the filtered edges (so isolated papers in range but with no valid edge are excluded), then look up each node's title/year. Sort nodes by `id` ascending and edges by `(source, target)` ascending before serializing with `json.dump` to `/home/user/graph.json` using the exact key names `nodes`, `edges`, `id`, `title`, `year`, `source`, `target`, `citation_date`. Run the script with the CLI args to confirm it produces valid, correctly filtered JSON before considering the task done.