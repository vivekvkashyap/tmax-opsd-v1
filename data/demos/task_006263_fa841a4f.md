Build the C program with a single argv[1] dispatch to three subcommands. For parse_csv, skip the header line and convert the ISO8601 timestamp to epoch using timegm (not mktime) so you don't accidentally apply the local timezone offset. For parse_json, since the input is a small fixed array-of-objects shape, a lightweight manual scanner (strstr/sscanf per object) is sufficient — no need for a full JSON library. For rolling_stats, read TSV lines from stdin and maintain a small circular buffer of the last 3 Length values seen so far in the (already sorted) stream, printing the average to 2 decimal places with printf. In the Makefile, the `pipeline` target must merge fr.tsv and de.tsv with a numeric sort on the first column (`sort -k1,1n`), not a plain concatenation, before piping into `./loc_processor rolling_stats > final_stats.tsv`. Compile cleanly with `make all`, then `make pipeline`, and confirm final_stats.tsv is produced with correctly ordered, correctly averaged rows.
