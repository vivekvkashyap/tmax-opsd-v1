Do the video analysis before touching any C code, since the C logic depends entirely on what you measure. Extract frames from `/app/calibration.mp4` (ffmpeg or OpenCV `VideoCapture`), threshold each frame to isolate the white region, and compute its centroid per frame (e.g. `cv2.moments` or the mean of thresholded-pixel coordinates) to build a `(t, x, y)` table across all frames. Since the trajectory is an exact integer polynomial, fit low-degree polynomials to x(t) and y(t) separately with `numpy.polyfit`, trying degree 1 then degree 2 and checking residuals are essentially zero with coefficients rounding cleanly to integers — don't assume the degree, verify it from the fit. For the build, the failure mode is a missing link plus a missing rpath: add `target_link_libraries(predictor traj_lib)` and set `INSTALL_RPATH`/`BUILD_WITH_INSTALL_RPATH` (or `$ORIGIN`) on the executable target so it finds the shared library without `LD_LIBRARY_PATH`. Implement `get_position` in `traj.c` with your derived formulas, rebuild with `cmake --build`, and spot-check several `t` values against your own formula to confirm the binary is self-consistent before finishing.
