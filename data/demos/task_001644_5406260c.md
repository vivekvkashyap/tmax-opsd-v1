Fix the build before touching crypto: inspect the Makefile in `/app/libmalcrypt-0.5` for the compilation rule that builds `.o` files feeding into `gcc -shared`, and add the missing `-fPIC` flag to CFLAGS (shared-library object files must be position-independent) — then confirm `make clean && make && ./test_runner` succeeds. Next, read `cipher.c` closely to understand the block cipher: identify it's a small Feistel network (rounds, XOR, bit rotations) and work out its exact inverse (Feistel decryption typically just runs the same round function with round keys/operations in reverse order) — write out the round structure on paper before coding. In `/home/user/decoder.c`, read all of stdin as a byte buffer, apply your inverse-cipher routine, then treat the plaintext as URL-encoded `key=value&key=value` pairs: split on `&`, split each on the first `=`, URL-decode the value for the `payload` key, and print only that decoded value to stdout with no extra formatting. Detect decryption/integrity failure however the cipher exposes it (e.g. malformed structure, invalid padding) and print `INVALID` with exit code 1 in that case. Since correctness is checked by fuzzing against a reference oracle across many random inputs including malformed ones, test your decoder yourself on edge cases like empty input, non-multiple-of-block-size lengths, and inputs with no `payload=` field.
