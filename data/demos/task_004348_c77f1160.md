Start by OCR'ing the image to recover the exact leakage rule text (e.g. `tesseract /app/leak_rule.png stdout`), then read it carefully to understand exactly which operation-ordering condition defines "evil" — don't assume a generic rule, base your logic strictly on the recovered text. Once you have the rule, implement the detector to read the log file line by line, and check the relative ordering of the operations the rule refers to (e.g. scan for the first occurrence of the split operation and check whether any disallowed "fit"-type operation appears before it, using substring matching since operation names vary like `fit_scaler` vs `fit_transform_tokenizer`). Exit 1 immediately on violation, exit 0 if the log is clean or the disallowed pattern never appears before the split (or never appears at all). Before finalizing, test your script against every file in `/app/samples/evil/` (expect exit 1) and `/app/samples/clean/` (expect exit 0) using a loop that checks `$?` after each invocation, since the real verifier uses a held-out corpus and any edge case you miss (e.g. multiple split operations, fit appearing only after) will fail there. Make the script executable.
