Use pandas' time-series tools rather than hand-rolling the resampling: load the CSV with `pd.read_csv(..., parse_dates=['timestamp'])`, set `timestamp` as the index, then `.resample('1H').asfreq()` between the min and max timestamps to materialize the hourly grid including gap hours. Apply `.interpolate(method='linear')` on the `value` column to fill numeric gaps, and separately `.fillna("")` on `status_msg` (do these as distinct column operations, not a blanket fillna, since they need different fill strategies). Filter rows where `value > 80.0` strictly. For counting non-ASCII characters in `status_msg`, iterate each character and test `ord(ch) > 127` — be careful that some emoji are represented as UTF-16 surrogate pairs in the source but as single Python codepoints once decoded, so count actual Python string characters, not raw bytes. Build the output list of dicts with the timestamp formatted via `.strftime("%Y-%m-%d %H:%M:%S")`, `value` rounded via `round(v, 2)`, and the non-ASCII count, then write with `json.dump` to `/home/user/anomalies.json`. Verify by manually inspecting each anomalous row's original vs interpolated value.