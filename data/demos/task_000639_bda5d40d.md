Split this into three independently verifiable pieces before wiring up the server. (1) Decode the ROOT_ID: use a video/frame library (e.g. OpenCV or ffmpeg to extract frames) to read the first 16 frames of `/app/deploy_sequence.mp4`, classify each as black(0)/white(1) by average pixel brightness against a threshold, and pack the 16 bits into a big-endian uint16 — verify by printing the frame-by-frame bit sequence you detected so you can sanity check it, then compute the integer programmatically rather than assuming a value. (2) Write a binary parser for the `.bin` format per the exact byte-offset spec given (little-endian ints, magic check, N deps array, then M-byte XML manifest) and test it against one artifact file directly, printing the parsed fields, before building the HTTP layer. (3) For `/dependencies/<id>`, do a graph traversal (BFS/DFS) with a visited-set keyed by artifact ID to avoid infinite recursion on cycles — this is the core trap given the deliberately circular dependency data — collecting all reachable IDs except the root, then dedupe and sort ascending. Enforce the `Authorization: Bearer <ROOT_ID>` check (computed from the video, not hardcoded) on every endpoint, returning 401 on missing/wrong tokens. Start the server on 127.0.0.1:8080 and manually curl each endpoint with correct/incorrect auth before finishing.
