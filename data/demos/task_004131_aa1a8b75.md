Parse `/home/user/proteins.fasta` preserving file order (e.g. with `Bio.SeqIO.parse` or a simple manual FASTA parser splitting on `>` headers) — order matters because output weights must align with sequence order. For each sequence, reconstruct its synthetic spectrum exactly per the given formula: `x = np.linspace(0, 100, 1000)`, then for each character sum `np.exp(-((x - ord(aa))**2) / (2 * 2.0**2))` across all residues. Stack the 5 resulting spectra as columns of a design matrix `A` (shape 1000x5), load the experimental spectrum from `/home/user/exp_spectrum.npy`, and call `scipy.optimize.nnls(A, exp_spectrum)` to get non-negative weights directly — don't use ordinary least squares, since the true concentrations are non-negative and some are exactly zero. Compute `best_fit = A @ weights` and `residuals = exp_spectrum - best_fit`, then fit `scipy.stats.norm.fit(residuals)` to get (mean, std). Write `weights` (list of 5 floats, in FASTA order), `residual_mean`, and `residual_std` to `/home/user/results.json` with exactly those key names, then run the script so the file is actually produced.
