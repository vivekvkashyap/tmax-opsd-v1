Split this into a strict C parser plus a shell pipeline, and get the edge cases right in the C code rather than trying to patch them in shell. In the C program: read line by line, use `strchr` (or manual scan) to find only the *first* colon so later colons stay part of the value; trim leading/trailing spaces, tabs, and the newline from both key and value with a small trim helper; validate the key with a character-by-character check against `[A-Z_]+` (reject empty keys, lowercase, hyphens, or anything else) and silently skip invalid/colon-less lines; print `KEY=VALUE` (value may be empty). Compile with `gcc -o /home/user/loc_cleaner /home/user/loc_cleaner.c`. For the pipeline, remember "keep last occurrence, then sort" is not the same as "sort, then dedupe first" — get the order right: feed the raw file through the cleaner, then use `tac` (or equivalent reverse) before deduping by key so the last original occurrence wins, then sort alphabetically by key, all via pipes with no intermediate temp files, redirecting final output to `/home/user/clean_locales.ini`.