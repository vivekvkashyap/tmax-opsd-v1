Structure the ETL as one function-per-file that returns a normalized DataFrame (columns: sensor_id, timestamp_utc as a proper datetime, temperature), then process each of the three files with its own encoding/format handling before merging: read `sensor_alpha.csv` as UTF-8 with ISO8601 `Z`-suffixed timestamps (parse as UTC), `sensor_beta.csv` as UTF-16 (pass `encoding='utf-16'` to your reader) with Unix epoch integers (convert with `pd.to_datetime(..., unit='s', utc=True)`), and `sensor_gamma.csv` as Latin-1/ISO-8859-1 with `DD-MM-YYYY HH:MM:SS` format (parse explicitly with that format string, assuming UTC). Dispatch the three per-file parse+clean functions via `concurrent.futures.ProcessPoolExecutor`/`ThreadPoolExecutor` to satisfy the parallelism requirement. After parsing, drop exact duplicate `(sensor_id, timestamp)` pairs (`drop_duplicates`), then sort chronologically per sensor and compute a rolling average with `window=3, min_periods=1` (so early rows average over however many prior records exist, not NaN) over `temperature`. Concatenate all sensors, format `timestamp_utc` with `strftime('%Y-%m-%d %H:%M:%S')`, round the rolling average to 2 decimals, sort by `sensor_id` then `timestamp_utc`, and write to `/home/user/output/clean_aggregates.csv` (create the output dir first) with exactly the three required column names.
