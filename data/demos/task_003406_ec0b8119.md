Set up in this order: `git init /home/user/manifest-repo` (non-bare), then write and compile `/home/user/operator.cpp` to a standalone `/home/user/operator` binary, then create the `post-commit` hook, then create/commit the manifest — because the hook only fires on the final commit and you want the compiled operator ready before that commit runs. The operator must read `network-manifest.conf` from its current working directory (git hooks run with CWD at the repo root), extract the value after `default_route=`, and branch: if it's exactly `192.168.1.254`, overwrite `/home/user/active_network.log` with `ROUTE_ACCEPTED\n`; otherwise append `ALERT: Invalid route <VALUE> rejected\n` to `/home/user/mail_spool.txt` — always `exit(0)` in both branches, and also on a missing file, so the hook never blocks the commit. For the hook script (`/home/user/manifest-repo/.git/hooks/post-commit`), just call `/home/user/operator` and `chmod +x` it. Since the manifest you commit has `default_route=10.0.0.1` (not the accepted IP), the commit should silently populate the mail spool with the rejection alert rather than failing. Test the operator standalone against both a valid-IP and invalid-IP manifest before wiring up the hook.