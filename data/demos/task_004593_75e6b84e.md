Break this into four verifiable stages and test each before proceeding. First, write the archive parser in Python: open the file in binary mode, confirm the first 4 bytes equal `DCAR`, then loop reading fixed-size fields with `struct.unpack` using big-endian format codes (`>H` for the 2-byte file ID, `>I` for the 4-byte size), read exactly 128 bytes for the path and strip trailing null bytes before decoding as ASCII, then read exactly `File Size` bytes of data — recreate the directory structure with `os.makedirs(..., exist_ok=True)` before writing each file, and loop until EOF (stop when fewer bytes remain than a header needs). Second, for indexing, since `/app/doc_indexer` is a stripped binary, don't try to disassemble it — just run it on each extracted file and capture its stdout, splitting on the colon to get `legacy_hash` and `doc_type`; also independently compute the MD5 of each file yourself in Python. Third, assemble the manifest dict keyed by file ID (as a string) with the four required fields. Fourth, build the HTTP server (stdlib http.server is simplest for background persistence): implement Bearer token auth checking the `Authorization` header exactly, serve `/manifest` from the JSON file, and serve `/doc/<id>` by looking up the id in your manifest and streaming the corresponding extracted file's raw bytes with `application/octet-stream`, returning 404 for unknown ids. Start it backgrounded and confirm with a curl request that both endpoints work before finishing.