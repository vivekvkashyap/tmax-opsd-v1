Work in stages: first get the project building, then fix correctness, then wire up the server. (1) Diagnose the CMake error by running `cmake` and reading the actual error message rather than guessing — likely a missing `find_package(Threads REQUIRED)` or a bad target_link_libraries line; fix it minimally. (2) In solver.cpp, the Newton's method loop is almost certainly using `float` instead of `double` for accumulation, causing the residual to oscillate near but never below the 1e-7 tolerance — switch all solver arithmetic to `double`. Also add a hard iteration cap (e.g. 100-1000 iterations) as a safety net so no input can hang the HTTP handler, and add a check for near-zero derivative (Newton's method divide-by-zero) that perturbs or reseeds x0. (3) Rebuild after each fix and test with several coefficient sets covering repeated roots, roots near zero, and large-magnitude coefficients. (4) Use the black-box reference solver mentioned in your task only as an external comparison tool by feeding it the same coefficients and diffing its printed root against your endpoint's JSON response within tolerance — never inspect, disassemble, or copy its internals, treat it purely as an oracle for input/output comparison. (5) Start the server bound to 127.0.0.1:8080 in the background and confirm with a curl POST before finishing.
