First run `tesseract /app/invoice_summary.png stdout` to OCR the policy text, then parse out the APPROVED_FAMILIES list and MAX_DEPLOYMENT_COST integer from the recognized text programmatically (don't hardcode assumed values — read what OCR actually returns and handle whitespace/comma variations robustly). Implement `/home/user/finops_filter.py` to load the JSON config, check every `instances[].type` against the approved family prefixes using something like `type.startswith(family)` for each approved family, sum `count * monthly_cost_per_unit` across all instances, and exit 0 only if all types match an approved prefix AND total cost <= the extracted max; exit 1 otherwise. Since a hidden adversarial corpus will test this, make the parsing defensive (handle missing keys, non-numeric costs) so it fails closed rather than crashing. For the dashboard, generate a self-signed cert with `openssl req -x509 -newkey rsa:2048 -nodes ...`, configure nginx to listen on 8443 with `ssl_certificate`/`ssl_certificate_key` pointing at it and `root /home/user/www;`, then start nginx. Write `/home/user/deploy.sh` to call the filter on its argument, abort on nonzero exit, otherwise create `/home/user/www/<app_name>-<timestamp>/config.json` and atomically repoint the `<app_name>-live` symlink (`ln -sfn`) to that new directory.