Write `/home/user/analyze_graph.c` using the SQLite3 C API (`sqlite3_open`, `sqlite3_exec`/`sqlite3_prepare_v2`+`sqlite3_step`), and structure the SQL in two logical stages: first execute `CREATE INDEX IF NOT EXISTS idx_target_id ON citations(target_id);` so the index persists in the database file afterward. Then compute in-degree centrality per paper as a subquery (`SELECT target_id, COUNT(*) AS cnt FROM citations GROUP BY target_id`), join that to `papers` on `id`, and aggregate by `category` to get total citations per category — separately determine the top-cited paper per category by ordering citation counts descending within each category group (e.g. a window function `ROW_NUMBER() OVER (PARTITION BY category ORDER BY cnt DESC)` if your SQLite build supports it, or an equivalent correlated-subquery/max-join approach if it doesn't). Iterate the result rows in your C code, write `category,total_category_citations,top_paper_id` lines to `/home/user/summary.csv`, and make sure you order by `category` alphabetically (either via `ORDER BY category` in SQL or by sorting rows in C) before writing. Compile with `gcc analyze_graph.c -lsqlite3 -o analyze_graph` and run it, then confirm the index survived by querying `sqlite_master` afterward.
