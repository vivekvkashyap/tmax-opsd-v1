Solve this with a single `awk` pass rather than chaining many shell tools, since group-by-mean plus overall-min is naturally an associative-array problem. Use `awk -F,` with `NR==1{next}` to skip the header, then for each row test `$4==""` to skip rows with missing `validation_loss` before accumulating (don't rely on `$4+0` treating empty as zero silently — explicitly guard). Key a running sum and count by the composite key `$1","$2` (or a two-dimensional associative array indexed by both fields) so each `(learning_rate, batch_size)` pair aggregates independently. In an `END` block, iterate the keys, compute `sum/count` per key, and track the minimum average and its key as you go (or store all pairs and pipe through `sort -t, -k3 -g | head -1` on a temp stream) rather than sorting text lexically, which would misorder decimal loss values. Format the final line with `printf "%s,%.4f\n"` so the loss is rounded to exactly 4 decimal places, and write only that single line to `/home/user/best_config.txt` — no header, no trailing blank lines.
