Build this as a clear pipeline of independent, testable steps. First write a `normalize_timestamp(value)` helper that detects which of the three formats a given field is (try: is it purely numeric/numeric-string → treat as Unix epoch via `datetime.utcfromtimestamp`; else does it match `YYYY/MM/DD HH:MM:SS` → `strptime` with that format; else assume ISO8601 → `datetime.fromisoformat` after handling the trailing `Z`), and always format the result back out as `strftime('%Y-%m-%dT%H:%M:%SZ')` so all three formats converge to one canonical string. Second, iterate records in original order, compute `hashlib.sha256(f"{user}|{action}|{normalized_ts}".encode()).hexdigest()`, and keep only the first record per unique hash (a seen-set of hashes) — do this dedup before sorting, using original array order to decide which occurrence is "first". Third, sort the deduped records by normalized timestamp and write JSONL with exactly the required four keys per line, dropping ingestion_time. Finally compute the four summary counts (total, unique, duplicates=total-unique, and most-active user via a Counter over unique records with alphabetical tiebreak) and render them into the report template exactly, matching whitespace/wording precisely.
