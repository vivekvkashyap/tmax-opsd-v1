Structure the Go program in clear stages: query all distinct (locale, category) pairs, then for each pair query its rows ordered by `id ASC`. For gap-filling, build the 3-element sample by indexing into the sorted slice with `sorted[i % len(sorted)]` for i in 0,1,2 — this naturally reproduces "A,B,A" for a 2-row group and "C,C,C" for a 1-row group, and just takes the first 3 in order when there are 3+ rows. For anonymization, split each email on the last `@` and replace everything before it with the literal string `***`, preserving the domain unchanged. Collect all sampled rows across every (locale, category) group, then sort the final row list by locale ascending, then category ascending, keeping the original per-group sample order stable within each group (use a stable sort, e.g. `sort.SliceStable`) — don't re-sort within the group by id, since repeated ids must stay in their cyclic order. Write the CSV with Go's `encoding/csv` writer (it handles quoting fields containing commas automatically) including the header row, run the program, and check the output file's row count and ordering against the source data logic by hand for one or two groups.
