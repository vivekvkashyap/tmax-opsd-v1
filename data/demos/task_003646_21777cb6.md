Implement this as a strict two-file pipeline and run both. In `/home/user/generate_data.py`: load the 3x3 covariance matrix from `/home/user/target_cov.json` with `json.load`, call `np.random.seed(42)` immediately before drawing the base standard-normal samples (order matters — seed right before the `randn` call, not before other unrelated random calls), compute `L = np.linalg.cholesky(cov)`, draw `Z = np.random.randn(100000, 3)`, and form correlated samples as `X = Z @ L.T` (get the orientation right: multiplying by `L.T` on the right of row-vectors is equivalent to `L @ z` per-column). Save with `np.savetxt(..., delimiter=',', header='f1,f2,f3', comments='', fmt='%.6f')` so there's no stray `#` before the header. In `/home/user/test_generator.py`, write a pytest test that loads the generated CSV (or regenerates), computes the empirical covariance with `np.cov(X, rowvar=False)`, loads the target matrix, and asserts every element differs by less than 0.05 from target using `np.allclose(..., atol=0.05)` or elementwise comparison. Run the generator, then run `pytest /home/user/test_generator.py > /home/user/test_results.log` and confirm it reports passing.