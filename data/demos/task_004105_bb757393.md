Structure the script as: iterate `.csv` files in `/home/user/datasets/` sorted by name, validate each with `awk`/`grep` before computing anything. For validation, use a regex-based `awk` pass that rejects a file if any line doesn't match exactly two fields where each field is either `NA` or an optionally-signed integer (e.g. per-line pattern like `^(-?[0-9]+|NA),(-?[0-9]+|NA)$`) — reject on the first non-conforming line (wrong column count, non-integer, float, stray letters) rather than only checking the first row, since a bad row can appear anywhere. Only for files that pass validation, run a second `awk` pass that maps `NA` to `0` per field and accumulates the running sum of `col1*col2` across rows to get the dot product, keeping it as an integer accumulator (don't let awk coerce to float notation in output). Emit `filename:result` or `filename:INVALID_SCHEMA` per file, collect all lines, and pipe the final list through `sort` (on the filename) before writing to `/home/user/experiment_log.txt`. Make the script executable and actually invoke it once so the log file is generated, then eyeball the output for stray whitespace or scientific notation in the numeric results.