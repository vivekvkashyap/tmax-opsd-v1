Diagnose the cross-join first: the buggy query selects ?emp and ?dept as independent, unconnected triple patterns, so SPARQL's default join behaves like a Cartesian product. Fix it by adding the actual graph edge connecting an employee to its department (the predicate that links ex:Emp* to ex:Dept*) inside the same WHERE block so ?dept is bound relative to ?emp, not free-floating. Layer the remaining requirements on top in this order: add a FILTER comparing salary against a variable you bind via rdflib's `initBindings` parameter (never string-format the threshold into the query text — that's the insecure pattern you're asked to avoid), add GROUP BY on the department name with an AVG() aggregate in the SELECT, add ORDER BY DESC on that aggregate, and add LIMIT 2. Test incrementally — run the query after each addition and eyeball the intermediate row counts/values to confirm the join, filter, and aggregation are each behaving before adding the next clause. Finally write results as two bare CSV columns (name, integer-rounded average) with no header row, matching the exact output path.