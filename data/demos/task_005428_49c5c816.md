Start by extracting the matrix reliably: run `tesseract /app/substitution_matrix.png` (try `--psm 6` or similar table-friendly page-segmentation modes if default OCR misreads the grid), then manually verify the recovered 4x4 grid is symmetric and internally consistent (substitution matrices like this are typically symmetric with a fixed diagonal) before hardcoding it into your script — OCR misreads on digits/minus-signs are the most common way this task silently fails. Index the matrix by nucleotide so lookups are order-independent given the stated A,C,G,T ordering. In `prep_features.py`, compute the substitution score by summing matrix lookups per aligned position; compute GC-content as `(count('G')+count('C'))/len(seq)` for each sequence; compute the identity-match proportion as matching positions over length; build the 2x2 matrix exactly as specified (note `M[1][0]` normalizes by `length * max(matrix)`, not by the score's own max); add `0.01` to both diagonal entries for regularization; and compute `numpy.linalg.det`. Since correctness is checked by fuzzing against a hidden reference implementation, match formatting exactly — 4 decimal places on the determinant, and reproduce the exact `"Score: <score>, Det: <det>"` string layout.
