Write the C program with a clear separation of concerns: a tokenizer that scans each line character-by-character, building a token whenever it hits an alphanumeric run (`isalnum`) and lowercasing each char (`tolower`) as you go, emitting a token at each transition to a non-alphanumeric character or end of line; store each line's tokens in a small unique-set structure (e.g. a sorted array or simple hash set with dedup) since duplicates within a line must collapse to one. For Jaccard similarity between two token sets, compute intersection size by checking membership of every token in the smaller set against the other set, and union size as `|A| + |B| - |intersection|`; treat line 1's "previous" set as empty explicitly (don't special-case division by zero incorrectly — union with an empty set is just the non-empty set's size, or 0/0 which per the spec should print as `0.0000` when both are empty, but note the first line is never both-empty in this input). For the rolling average, maintain a small circular buffer/queue of the last up-to-3 Jaccard scores and average whatever's currently in it (1, 2, or 3 values) — don't hardcode divide-by-3 for early lines. Format every float with `%.4f` and match the CSV header/row format exactly, then compile with `gcc /home/user/log_processor.c -o /home/user/log_processor` and run it against the given input before checking output.