Use `strings /home/user/service_dump.bin | sort | uniq -c | sort -rn | head` (or similar) to surface the most frequently repeated string in the dump — the leaked username will stand out as an extreme outlier in repetition count compared to normal one-off entries, and specifically it will appear as `USER_LOGIN: <name>` without a trailing `;`, distinguishing it from well-formed valid entries. Confirm by grepping the dump for `USER_LOGIN:` occurrences and checking which value dominates. Write exactly that recovered username (no extra text) to `/home/user/leaked_username.txt`. Then open `parser.py` and find where `process_log` currently appends unterminated entries to `leaked_records` when no `;` is present in the text after `USER_LOGIN:` — replace that append-and-return-False branch with `raise ValueError("Missing semicolon")`, leaving the valid-line parsing path (which finds `;` and returns True) unchanged. Finally write `test_parser.py` that imports `process_log`, constructs a log line using the same malformed pattern you recovered (i.e. `"USER_LOGIN: <leaked-username>"` with no semicolon), asserts via `pytest.raises`/`try-except` that `ValueError` is raised, and exits 0 on success — run it yourself to confirm before finishing.