Sequence this as fix-package, then clean-data, then model, then serve — each stage depends on the previous succeeding. Fix `/app/fast-corr-py/setup.py` by importing numpy and passing `include_dirs=[numpy.get_include()]` into the Extension() call (the build fails because the C extension can't find `<numpy/arrayobject.h>` without that), then `pip install .` and confirm `import fast_corr` works before writing any data code. For cleaning: impute NaNs per-column with that column's median (computed before outlier removal), then compute z-scores per feature and drop any row where any feature's |z| > 3.0 — do this after imputation. Use `fast_corr.compute(x, y)` (not pandas' own corr) per feature against target, rank by absolute value, and keep the top 2 feature names — you'll need these exact column names as the JSON keys your API expects. For modeling, use sklearn's Ridge with GridSearchCV/cross_val_score over alpha in [0.1, 1.0, 10.0] and 5 folds, then refit on the full cleaned data with the best alpha. For serving, run two listeners concurrently in one process (e.g. threads): an HTTP server on 8080 checking the exact Bearer token before predicting, and a raw TCP socket on 8081 that replies PONG\n to a PING\n line and closes the connection.