Two independent sub-problems: extract ground truth from an image, then implement exact-spec bash logic. Run `tesseract /app/memo.png stdout` to recover the approved domain text, isolate just the domain substring, and verify the OCR output by eye before trusting it (OCR can introduce stray characters). For the script, implement steps in exact order: (1) check ELF class via `readelf -h` or byte offset 4 of `/home/user/server.bin` (0x02 = ELF64), exit 1 if not 64-bit; (2) `base64 -d` the argument, check `$?` explicitly, print "DECODE ERROR" and exit 0 on failure; (3) verify the decoded string starts with `https://` via a prefix check; (4) extract the domain as everything between that prefix and the first `/` (or end of string) using parameter expansion or `cut`/`awk`; (5) compare against the OCR-recovered domain and print the exact SAFE/OPEN REDIRECT message with no extra output. Self-test against crafted base64 payloads (missing path, wrong protocol, garbage base64, empty string) before finalizing, since a fuzz harness will exercise those edge cases.
