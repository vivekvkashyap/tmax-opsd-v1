Split this into the two scripts and get each working independently before chaining. For `analyze_logs.sh`, use `grep` for lines containing `FAILED` or `refused` (case-sensitive, so don't add `-i`), then pull out the port from an `IP:PORT` pattern with `grep -oE ':[0-9]+' | tr -d ':'` (or an `awk`/`sed` equivalent), and dedupe with `sort -u` — the trap is including the IP address digits in your port extraction or missing that "refused" can appear without the word "FAILED" on the same line. For `auto_recover.py`, shell out to run the bash script and capture its stdout lines as the failed-port list, then for each port read the PID from `/home/user/services/pids/<port>.pid`, send a kill signal (verify the process actually dies, e.g. retry or check `/proc/<pid>` or catch `ProcessLookupError`), call `start_single.sh <port>` to relaunch, and re-read the freshly overwritten `.pid` file to get the new PID. For idempotent JSON updates, always load the existing file into a dict first (defaulting to `{}` if missing/absent), mutate only the recovered ports' keys, and rewrite the whole file — never append raw JSON fragments, which would corrupt the structure on repeated runs.