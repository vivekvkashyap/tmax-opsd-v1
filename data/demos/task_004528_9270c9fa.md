First reproduce the deadlock to understand it, then fix the root cause rather than papering over it with a single coarse lock (that would fail the throughput requirement). Read `add_edge` in the vendored `graphetl-lib` graph module: if it acquires two per-node locks in the order the arguments are given, two threads inserting the same pair in opposite order (A,B) and (B,A) can each hold one lock and wait on the other — classic lock-ordering deadlock. Fix it by always acquiring locks in a fixed, deterministic order regardless of call order — e.g. sort the two node identifiers and lock the smaller one first — so no two threads can ever wait on each other's held lock; keep per-node (fine-grained) locks, don't collapse to a global lock. Validate with a quick concurrent stress test of your own (many threads inserting overlapping edge pairs) to confirm it no longer hangs and throughput scales. For the second half, write `analyze_graph.py` that loads `/app/data/transactions.csv`, builds an adjacency structure using the fixed library's insertion API, and runs a standard BFS (not DFS) from `CUST_739` tracking parent pointers to reconstruct the shortest hop path to `CUST_882`, then write that path as a comma-joined string to the output file.
