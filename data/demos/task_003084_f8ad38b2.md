First inspect the schema with `sqlite3 /home/user/dataset/collaborations.db ".schema"` to confirm table/column names (authors, coauthors with author1/author2). Read the buggy recursive CTE in `/home/user/generate_report.py` closely: the recursive term does `FROM author_paths, coauthors` with no join predicate, which is an implicit cross join producing a combinatorial explosion of bogus paths (hence the `LIMIT` band-aid and inflated distances). Fix it by adding an explicit `JOIN coauthors ON author_paths.id = coauthors.author1` (or equivalent WHERE join condition) so each recursive step only follows real edges from the current node. Also guard against infinite recursion on the cyclic graph by bounding depth (e.g. `WHERE author_paths.distance < <reasonable-bound>`) and by using `GROUP BY id` with `MIN(distance)` (already present) to collapse duplicate/cyclic paths to the true shortest distance — SQLite recursive CTEs don't dedupe visited nodes automatically, so without a depth bound or visited-set the query can run indefinitely on cycles. Test with `time python3 /home/user/generate_report.py` to confirm it finishes well under 1 second and inspect `/home/user/distances.json` for sane (small, non-inflated) distance values before finishing.