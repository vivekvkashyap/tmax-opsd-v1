Build this as a strict pipeline and verify each stage before moving to the next, since a mistake early (e.g. wrong tie-break order in the window function) silently propagates into the PageRank numbers. Start services, create the `graph_analytics` database, and load the CSV with `psycopg2`/`pandas.to_sql` or `COPY` into `raw_interactions`, checking row counts match the source file. Write the SQL window function exactly as specified — `ROW_NUMBER() OVER (PARTITION BY source_id ORDER BY interaction_weight DESC, timestamp ASC, target_id ASC)` filtered to `rn <= 3` — and sanity-check a few source_ids manually to confirm at most 3 rows each. Build the directed graph with `networkx.DiGraph()`, adding edges with `weight=interaction_weight` as the attribute name matching what you'll pass to `pagerank`, then call `nx.pagerank(G, alpha=0.85, weight='interaction_weight')` (do not use the default weight key name if your edge attribute differs). Push all node scores into a Redis hash `node_pageranks` via `HSET`, converting both key and value to strings. Sort by score descending then node_id ascending for the top 20, and write `/home/user/top_nodes.csv` with exactly the `node_id,pagerank_score` header — the grader compares your scores to a reference with MAE ≤ 1e-4, so use full float precision, not rounded strings.
