Build this bottom-up and test each layer before wiring them together. First create and chmod the dummy `app_service.sh`, then `git init --bare /home/user/service.git`. Write monitor.rs to: set `TZ=Asia/Tokyo` (via `std::env::set_var` before spawning the date command, or by setting it directly in the `Command` you spawn so it's inherited by that child), invoke `date` with the exact format string via `Command::new("date").arg("+%Y-%m-%d %H:%M:%S %z")` and capture stdout, then use `Command::new("pgrep").args(["-f","app_service.sh"])` and check its exit status to decide down-vs-active. To start the service so it survives the Rust process exiting, spawn via a shell (`sh -c "nohup /home/user/app_service.sh > /dev/null 2>&1 &"`) rather than directly forking the child, since directly-spawned children can be reaped or tied to the parent's process group. Append (not overwrite) the exact log line with `OpenOptions::new().append(true).create(true)`. Test monitor_bin standalone first (both with the service killed and running) before hooking it into post-receive, then verify the hook fires end-to-end by cloning the bare repo and pushing a commit, checking /home/user/alert.log after each push. Use absolute paths everywhere in the hook script.