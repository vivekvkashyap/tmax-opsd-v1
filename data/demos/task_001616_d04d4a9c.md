Build and test each phase independently before chaining them. First create the two input files exactly as given. In `/home/user/process_sensor.py`, parse the CSV with the stdlib `csv` module, convert `timestamp` to a comparable form (e.g. `datetime.fromisoformat`), and for each missing temperature/humidity value do linear interpolation using the actual elapsed time between the nearest valid points before and after (not just row-index averaging, though in this sample the intervals happen to be equal) — this matters for correctness even though the sample data has uniform 5-minute spacing. Compute min/max/avg over the fully-interpolated series, format every numeric substitution to exactly two decimals with `f"{val:.2f}"`, and do simple string replacement of the `{{PLACEHOLDER}}` tokens in the template, writing to `/home/user/output/report.txt`. In `/home/user/run_pipeline.sh`, `mkdir -p /home/user/output` then invoke the Python script (e.g. `python3 /home/user/process_sensor.py`), and `chmod +x` the wrapper. For cron, install with `crontab -l | { cat; echo "*/15 * * * * /home/user/run_pipeline.sh"; } | crontab -` (or edit a crontab file) rather than assuming a cron daemon config file location. Finally run the wrapper once manually so the report file exists for verification, and check `crontab -l` shows the schedule.
