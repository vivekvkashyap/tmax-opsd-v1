Start by running OCR on the diagram: `tesseract /app/arch_diagram.png stdout` (install tesseract if missing) and read the extracted text carefully to find the labeled port number and vault token string — do not guess or hardcode a plausible-looking port/token, use exactly what OCR recovers (re-crop or preprocess the image with ImageMagick if OCR output is noisy). Write the daemon in C using standard POSIX sockets (socket/bind/listen/accept), parsing each connection's line up to `\n` and matching it against the literal format `TOKEN:<token>|PAYLOAD:<data>` with strict string comparison — reject anything that doesn't parse cleanly or whose token doesn't match exactly, sending `ERR: Unauthorized\n`. On success write `<data>` to a new file under `/home/user/backups/` and send `OK\n`, then close the socket. Handle concurrent clients (fork or a simple accept loop is fine) so the verifier's sequential probes work reliably. In `manage_backup.sh`, compile with gcc, mkdir -p the backups dir, launch the daemon with `&` and capture `$!` to the pid file for start; for stop, read the pid file, `kill` it, then remove the file. Finally actually invoke `./manage_backup.sh start` and leave it running.