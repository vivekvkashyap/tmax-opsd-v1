Debug this as a layered pipeline failure, checking each hop independently before re-triggering the whole deploy. First inspect the post-receive hook to see exactly what socket path gunicorn binds to, then compare that against the `upstream backend` block in the nginx config — a 502 from nginx with a working backend almost always means the proxy's unix-socket path string doesn't match the one the app process actually created; fix the nginx config to point at the same socket path the hook starts gunicorn on. Next, separately test why the Python process itself might crash on start: try launching the app manually with and without TZ/LANG set to reveal it raises on missing locale/timezone env vars, then edit the hook so the gunicorn invocation line explicitly exports `TZ=UTC LANG=en_US.UTF-8` in front of the command (inline env-var prefix or `export` before it) rather than assuming the shell hook inherits those from the caller's environment, which it typically won't during a git push from another shell. Only after both fixes are verified independently should you make a trivial commit in the workspace clone and `git push origin master` to retrigger the hook end-to-end, then confirm with `curl -s http://127.0.0.1:8080/health`.