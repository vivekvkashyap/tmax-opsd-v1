Read `/home/user/generate_cypher.py` and locate the nested loop building the Cypher output — it currently pairs every user with every purchase (a Cartesian product) because the inner loop over purchases has no filter tying it back to the outer user. Fix it by only emitting a query when the purchase's `user_id` matches the current user's `user_id` (e.g. add an `if p['user_id'] == u['user_id']:` guard inside the nested loop, or restructure by grouping purchases by `user_id` first for efficiency) — remember CSV `DictReader` values are strings, so compare them as strings (or cast consistently) rather than mixing str/int. Do not touch the CSV files. After fixing, run the script and inspect `/home/user/relationships.cypher` to confirm it now has one line per actual user-purchase pair matching the input data, in the exact `MATCH (u:User {user_id: <id>}), (p:Purchase {purchase_id: <id>}) CREATE (u)-[:BOUGHT]->(p);` format, with no leftover cross-join entries.
