First load `/home/user/schema.json` and filter `data.csv` to only the schema's columns before doing anything else — dropping the extra `metadata` column matters for reproducing the leak faithfully. For the "leaky" pipeline, replicate `train.py`'s exact bug: fit `StandardScaler` on the *entire* feature matrix X (before splitting), then call `train_test_split(X_scaled, y, test_size=0.2, random_state=42)`, fit `Ridge(alpha=1.0)`, predict on the test set, and take `|y_test - preds|` as the leaky absolute errors. For the fixed pipeline, split raw X/y first with the same `test_size=0.2, random_state=42`, fit the scaler only on X_train, transform train and test separately, then fit a fresh `Ridge(alpha=1.0)` and get its own absolute errors on its own test split. Do NOT reuse one model's predictions for both — each pipeline needs its own split/fit/predict pass, and both use the identical random_state so the same rows end up in the test set. Compute MSE for each, then run `scipy.stats.ttest_rel` on the two absolute-error arrays (paired, two-sided) to get the p-value. Write unrounded floats to `/home/user/report.json` with keys `leaky_mse`, `fixed_mse`, `p_value`.
