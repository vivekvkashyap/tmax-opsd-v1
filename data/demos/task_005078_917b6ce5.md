Get the numerical computation exactly right in isolation first, then wrap it in the HTTP handler. Use `numpy.random.default_rng(seed=42)` to generate the 1000x1000 float32 array, cast to float64 before `np.sum(..., axis=1)` (the order matters for reproducibility — do the cast before summing, not after), normalize the resulting 1000-length vector to sum to 1.0 as your P distribution, build Q as a uniform 1/1000 vector, and compute KL divergence as `np.sum(P * np.log(P / Q))` — verify this snippet standalone in a Python REPL/script and sanity-check the magnitude before wiring it into the server. Separately, transcribe the spoken number in `/app/data/target_metric.wav` using a speech-recognition tool or library (don't assume the value — actually run transcription and read back the words, converting the spoken digits to a float) and use that as `expected_metric` in the response, not the KL value itself. Implement the server with Python's built-in `http.server` or `Flask`/`FastAPI`, exposing `GET /api/validate` on `127.0.0.1:8000` that recomputes the KL divergence (or serves a precomputed cached value using the same seed) and returns the JSON with both keys, then leave the process running so it stays reachable for external requests.
