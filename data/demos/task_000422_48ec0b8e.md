Grep `/home/user/process_dump.log` for the process line containing `--auth-token=` and `--target=` (e.g. `grep -oP '(?<=--auth-token=)\S+'` and `grep -oP '(?<=--target=)\S+'`) to extract the token and payload path exactly as they appear — don't retype or normalize them. Confirm the target path exists and is an ELF (`file <path>`), then compute `sha256sum <path>` and run `readelf -h <path>` (or `file`) to get the "Machine:" field, which is your architecture string. Write `/home/user/generate_report.py` as a self-contained script with no CLI args: it should re-derive all four values itself (parse the log for the token/path via regex, then shell out to or reimplement sha256 and readelf/file parsing for the arch string) rather than hardcoding the values you found manually, since the report must be reproducible. Have the script write the JSON with exactly the keys `leaked_token`, `payload_path`, `payload_sha256`, `elf_architecture` to `/home/user/incident_report.json`, then execute it and confirm the output file contents match what you derived from raw analysis (payload_path should be the absolute path from the log, and elf_architecture should match readelf's "Machine:" wording).
