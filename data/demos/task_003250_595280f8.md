Structure the Go program as three clearly separated stages: parsing/validation, statistics, and reporting. Read `/home/user/sensor_data.csv` with `encoding/csv`, and for each row apply all four validation rules together — a row must be dropped (not partially used) if timestamp is empty, sensor_id fails to parse as an int in [1,10], or temperature/humidity fail to parse as floats in their respective ranges; use `strconv.Atoi`/`strconv.ParseFloat` and treat any parse error as invalid rather than only checking range on already-valid-looking strings, since malformed values like "bad" must also be excluded. Accumulate valid temperatures into a slice, then compute sample mean and sample standard deviation using the n-1 (Bessel's correction) denominator for variance, not n, since this is the "sample" standard deviation. Compute the 95% CI margin as `1.96 * stddev / sqrt(n)` exactly as specified (don't substitute a t-distribution). Check whether 21.5 falls within [ci_lower, ci_upper] inclusive for model_validated. Round each float to exactly 4 decimal places before marshaling to JSON (e.g. via `math.Round(x*10000)/10000`), write to `/home/user/report.json`, then `go run`/compile and execute it.