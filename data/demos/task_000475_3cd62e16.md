Approach this as a diff-against-oracle debugging task: first build and run the existing `/app/audio_filter` on `/app/sample_recording.wav`, then run `/app/ref_filter.py` on the same input, and diff the two outputs (`cmp`/`xxd`) to see where divergence starts — this localizes the precision bug before you read code. For the memory leak, read `/app/src/audio_filter.c` looking specifically at any loop that processes audio in chunks; find the `malloc` for a per-chunk temporary buffer that is missing a matching `free` at the end of each iteration (not just at program exit), and confirm with `valgrind --leak-check=full` that "definitely lost" drops to 0 bytes. For the precision issue, look for an accumulator variable declared as `float` inside the filter's inner loop (e.g. FIR/IIR summation) — Python's float is double precision, so a C `float` accumulator loses bits on longer sums; change it to `double` and re-diff outputs to confirm byte-for-byte match. After fixing both issues, compile to exactly `/home/user/audio_filter_fixed` and re-verify with several different WAV files (varying length/content), not just the one sample, since the grader fuzzes many WAVs and checks sha256 equality plus a clean valgrind run.