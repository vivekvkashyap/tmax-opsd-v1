Build this as a pure in-memory pipeline in Go, keeping each stage separate so bugs are easy to isolate: (1) load users.csv into a map[int]struct{Name string}; (2) load friends.csv and tally out-degree by incrementing a counter keyed on source_id (do not count in-edges); (3) load transactions.csv and track a running max amount per user_id, defaulting missing users to 0.0; (4) join the three maps into one slice of records; (5) filter to max_amount > 50.0 strictly (watch off-by-one: equal to 50.0 must be excluded); (6) sort with a multi-key comparator — max_amount descending, then out_degree descending, then user_id ascending — using sort.Slice with a tie-break chain, not a single-field sort; (7) paginate by slicing sorted[(page-1)*pageSize : page*pageSize], being careful with the definition of "page 2" (items 3 and 4, i.e. index 2:4 for page size 2). Marshal the page slice to JSON matching the exact field names/order shown, then write to the output path. Compile and actually run the program (not just write source) so /home/user/output.json is produced, and spot-check it against your own hand-traced expectation of degree/max/sort before finishing.
