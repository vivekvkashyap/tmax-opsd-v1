Fix the Rust library first: inspect /app/pow_shield/Cargo.toml and note that `cargo build --release` only produces an rlib without a `[lib]` section specifying `crate-type = ["cdylib"]` — add that so libpow_shield.so actually gets built (this is why the Makefile's `cp` step fails). Then inspect src/lib.rs for the borrow-checker/lifetime error: look for a pattern where a `Vec` is created, a reference to it is taken, and the vec is then dropped/moved while the reference is still used afterward — fix it by using the original `slice` (already valid for the FFI call's lifetime) directly in the loop instead of creating a temporary owned Vec and dropping it. Run `make` and confirm libpow_shield.so exists. For the Python side: create a venv, install Flask, load the .so via ctypes with the exact C signature (const uint8_t*, size_t, uint64_t, uint32_t) -> bool, and wire /submit to call it with the UTF-8 bytes of `data`. Implement the EMA limiter as literal running state (module-level variables, not per-request), updating strictly in the given order: compute diff_ms since last request timestamp, update EMA with the 0.3/0.7 weights, then check EMA < 100.0 for rejection — get the order and the "skip EMA update on the very first request" detail right, then test the 403/429/200 paths manually before backgrounding the server.
