Treat this as three layered problems: environment/service wiring, classifier logic, and end-to-end verification. First inspect `/app/dna_pipeline/config.yaml`, `start.sh`, `api.py`, and `worker.py` to understand the intended Redis/Flask/Worker topology; fix the config's host/port mismatches so Redis is on 6379, Flask on 5000, and the worker points at the same Redis instance, then launch all three with `start.sh` (or manually) and confirm with `redis-cli ping`, `curl localhost:5000`, and process checks that everything actually stays up in the background (use nohup/disown or a supervisor loop, not a foreground blocking call). Second, implement `evaluate_sequence` in `classifier.py` exactly per spec: check the literal 8-bp prefix first and reject immediately if absent; otherwise strip the prefix, map bases to {A:1,C:2,G:-1,T:-2}, chunk into non-overlapping length-5 blocks (drop remainder), bootstrap-resample blocks with replacement 1000 times using numpy's RNG, run `np.fft.fft` on each resampled sequence, take squared magnitude at index `len(sample)//5`, and compute the 2.5/97.5 percentiles across the 1000 trials with `np.percentile` — classify "evil" only if the lower CI bound exceeds 200.0. Third, test locally against both corpora in `/app/corpus/` via `curl -X POST .../classify` before declaring done, since the verifier requires 100% accuracy on both.
