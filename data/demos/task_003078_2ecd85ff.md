Build and validate each layer before stacking the next. First confirm you can call `/app/embed_oracle` correctly: pipe a test string via stdin and inspect the raw stdout bytes (`xxd` or Python `struct.unpack('<64f', data)`) to confirm it's little-endian float32 x64 with no extra framing, since a byte-order or count mistake here silently corrupts every downstream embedding. Create the SQLite schema exactly as specified (store the embedding as raw bytes via `sqlite3.Binary`/BLOB), then loop over `/home/user/raw_texts.json`, calling the oracle once per text and inserting rows. For the stats step, load all embeddings back as numpy arrays, compute the full pairwise cosine similarity matrix (excluding self-pairs), and run `scipy.stats.ttest_1samp` against 0.0 plus a 95% CI (t-distribution based, using sample mean/stderr) — don't substitute ad hoc formulas. Build the Flask/FastAPI service last, exposing `/stats` (serve precomputed or on-demand stats) and `/search` (embed the query via the same oracle call, compute cosine similarity against all stored vectors, return top_k sorted by score). Launch it as a background process bound to 127.0.0.1:8080 and confirm both endpoints respond correctly with `curl` before finishing.