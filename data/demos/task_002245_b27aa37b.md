Use Python's `zipfile` module rather than hand-parsing headers: wrap each file in a `try`/`except` around `zipfile.ZipFile(path)` (and call `.testzip()` or catch `BadZipFile`) to detect and skip corrupted or non-zip files without raising — this handles both the "not a zip at all" and "PK magic bytes but garbage contents" corruption cases in the fixture. For each valid archive, iterate `zf.infolist()` and take the max of `.file_size` (the uncompressed size) across all entries — do not use `.compress_size`, and do not actually extract/write any file contents to disk. Print `<absolute_path>: <max_size>` per valid archive to stdout, then in bash pipe your script's output through `sort -t: -k2 -n -r` (colon-delimited, numeric, reverse) to get largest-first ordering, redirecting the final result to `/home/user/largest_backups.txt`. Double check the sort flags handle the `path: number` format correctly (the numeric field is after the colon-space), and confirm corrupted/fake zip entries never appear in the final file.
