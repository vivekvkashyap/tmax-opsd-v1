Build a small, correct FASTA GC-content pipeline rather than eyeballing values. Compile with `g++ -O3 fasta_stats.cpp -o /home/user/fasta_stats`, making sure the parser correctly resets per-sequence counters at each `>` header line and only counts alphabetic bases (G/C case-insensitive) toward the total so percentages aren't skewed by stray characters. Run the tool separately on `groupA.fasta` and `groupB.fasta`, redirecting each `SequenceID\tGC_Percentage` output to a temp file or piping directly into `awk`. Use `awk -F'\t' '{sum+=$2; n++} END{printf "%.2f", sum/n}'` (or equivalent) to compute each group's mean — do this from the tool's actual printed output, not by hand-deriving GC counts. Then compute the absolute difference and determine which group is higher, formatting every number to exactly 2 decimal places with `printf "%.2f"` so the final `/home/user/analysis_report.txt` matches the required `Mean_GroupA:`/`Mean_GroupB:`/`Absolute_Difference:`/`Higher_Group:` format exactly, including capitalization of "GroupA"/"GroupB".