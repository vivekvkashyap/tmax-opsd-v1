Do this entirely with `awk`/`bc` since no Python/R is allowed. First compute Pearson correlation for each of sensor_A, sensor_B, sensor_C against target in one `awk` pass per column: accumulate sum(x), sum(y), sum(x*y), sum(x^2), sum(y^2), n, then apply `r = (n*sumxy - sumx*sumy) / sqrt((n*sumx2 - sumx^2)*(n*sumy2 - sumy^2))` — compare absolute values across the three to pick the best feature. Reuse the same accumulated sums (for the winning feature) to compute OLS slope `m = (n*sumxy - sumx*sumy)/(n*sumx2 - sumx^2)` and intercept `b = (sumy - m*sumx)/n`, rounding each to 2 decimals with `printf "%.2f"` in awk (careful: awk's default rounding truncates, so use a proper round-half-up helper, e.g. `int(x*100+0.5)/100` for positive values). Compute MSE in a second pass over all rows using the *rounded* m and b as the reference truth expects, summing `(y - (m*x+b))^2` then dividing by n and rounding. Write `/home/user/predict.sh` as a one-line awk wrapper that takes `$1`, applies the stored m/b, and prints the rounded prediction; `chmod +x` both scripts. Assemble `/home/user/report.txt` with a heredoc matching the exact template, substituting the computed Best Feature/Slope/Intercept/MSE.
