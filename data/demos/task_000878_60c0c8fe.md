Split this into two clean sub-problems and verify each independently. First, get the exact frame count from the video with a precise, non-lossy method: `ffprobe -v error -count_frames -select_streams v:0 -show_entries stream=nb_read_frames -of csv=p=0 experiment.mp4` (avoid estimating from duration*fps, which can be off by a frame due to rounding — always count actual decoded frames). Store that integer as N and use it literally as the mesh dimension; do not hardcode any other value. Second, write the Go program carefully matching the spec exactly: parse the three CLI args as float64 via strconv.ParseFloat, compute dx=dy=2R/N, loop i,j from 0 to N-1, evaluate midpoints -R+(i+0.5)*dx (do the arithmetic in float64, not integer division), sum 1/sqrt((x-cx)^2+(y-cy)^2+1e-8) * (dx*dy), and print with fmt.Printf("%.6f\n", sum) with nothing else on stdout. Since correctness is checked by fuzzing against a hidden oracle across many random inputs, get the loop bounds, midpoint formula, and epsilon term bit-for-bit correct rather than approximating — small formula deviations will fail most fuzz cases. Compile with `go build -o /home/user/mesh_solver /home/user/mesh_solver.go` and test manually with a few sample invocations before finishing.