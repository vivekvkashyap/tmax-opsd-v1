Use `jq -c '.deployments[]'` to iterate manifest entries as compact JSON objects, extracting `id` and `path` from each with `jq -r '.id'` / `.path`. For each path, determine ELF-ness reliably rather than guessing by extension — e.g. check the first 4 bytes for the ELF magic number (`\x7fELF`) via `head -c4 "$path" | xxd` or simply run `readelf -h "$path" >/dev/null 2>&1` and check its exit status; skip the file silently if it isn't ELF. For files that pass, run `readelf -l "$path"` and grep the `program interpreter` line (format is `[Requesting program interpreter: /path/to/ld]`), then extract just the path with `awk`/`sed` (strip the brackets and label text). Append one CSV line `id,path,interpreter` per valid ELF to `/home/user/elf_deps.csv`, with no header row and no entries for non-ELF files. Run the script once and manually inspect the CSV against `readelf -l` output for each ELF file to confirm the interpreter strings match exactly.
