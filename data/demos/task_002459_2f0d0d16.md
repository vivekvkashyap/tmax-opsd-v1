Treat this as four independent subsystems and verify each in isolation before declaring done. (1) Cron runs in a minimal environment without your interactive shell's PATH, so edit `/home/user/edge_deploy/extract.sh` to use an absolute path to `ffmpeg` (find it via `which ffmpeg`) or explicitly `export PATH=...` at the top of the script, and change the output target to the absolute path `/home/user/frames/frame_%d.jpg` (mkdir -p it first) rather than a relative `frames/` dir that resolves against cron's unexpected cwd; update the crontab entry with `crontab -e`/`crontab -l | ... | crontab -` accordingly, then trigger it manually or wait a minute and confirm exactly the expected frame count appears. (2) Write `sanitizer.py` to `json.load` the file inside try/except, and check the field set is *exactly* `{sensor_id, timestamp, status}` (reject extras), `sensor_id` is a string starting with `CAM-`, `timestamp` is an `int` (not a numeric string), and `status == "OK"` exactly — exit 0 or 1, print nothing. (3) Launch two `python3 -m http.server` instances bound to `127.0.0.1:9001` and `:9002` serving `/home/user/frames/`, backgrounded with `&`/`nohup`. (4) Write a minimal round-robin `nginx`/`haproxy` config as an unprivileged user listening on `127.0.0.1:8080` upstream to both backend ports, and start it, then confirm with `curl` that requests get routed successfully.