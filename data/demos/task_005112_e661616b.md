Debug the 502 by tracing the actual socket path each side uses, not by guessing: read `proxy_pass` in `/app/nginx/capacity.conf` and read the Gunicorn bind argument (in the app's Makefile/startup script/README) — a 502 from nginx almost always means the upstream socket path or permissions don't match what nginx expects. Pick one shared path both processes can read/write (e.g. a socket under `/tmp`), update whichever side is wrong, start Gunicorn as a background daemon bound to that socket, then start nginx with the config and confirm `curl http://localhost:8080/analyze` returns 200 before optimizing anything. For caching, add a `proxy_cache_path ... keys_zone=<name>:10m;` directive in the nginx `http {}` block (main config, since the site conf's location block can't declare it), then in the `/analyze` location add `proxy_cache <name>;` and `proxy_cache_valid 200 10s;`. Verify caching actually engages by checking `$upstream_cache_status` (add it as a header) or by timing two consecutive curls — the second must be much faster. Only after confirming the cache works should you write `/home/user/verify_capacity.py` using threads/asyncio to fire 50 concurrent requests and assert status 200, exiting 0 on success.