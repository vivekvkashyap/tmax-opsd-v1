First get the plumbing right in isolation: edit `/app/gateway/nginx.conf` to add `location /meta/ { proxy_pass http://127.0.0.1:5001/; }` and `location /records/ { proxy_pass http://127.0.0.1:5002/; }` (note trailing slashes so the prefix is stripped correctly), run `/app/start_services.sh`, then sanity-check with `curl http://localhost:8080/meta/experiments` before writing any aggregation logic. In `aggregate.sh`, chain `curl -s` + `jq` calls: fetch the experiment ID list, loop over each with `jq -r '.[]'`, fetch its schema and its records, then use `jq` to rename fields per the schema mapping (build a `jq` filter dynamically from the schema JSON, e.g. with `--argjson` or by constructing a `map(...)` expression from the schema's key-value pairs) and drop any record whose status is "corrupted" or "invalid" with a `select()`. Accumulate all experiments' filtered/mapped records into one JSON array (e.g. via `jq -s 'add'`) and write it to `/home/user/processed_dataset.json`. Test incrementally against one experiment ID first, comparing shapes to the example in the task description, since the grader requires near-exact (Jaccard >= 0.99) field-for-field matches including the numeric types.
