Work strictly with awk/grep/sort/find pipelines. First extract CA-atom coordinates: filter lines where field 1 is `ATOM` and field 3 (atom name) is `CA`, then print fields 6,7,8 space-separated with awk — verify column indices against the actual PDB whitespace-separated layout before trusting them. Run `mc_sim.sh` once ca_coords.txt exists so it populates `/home/user/results/*.score`. For the reduce script, the bug is reduction order: instead of `find ... -exec cat {} + | awk '{sum+=$1}'`, first collect all values with `cat`, pipe through `sort -n` to sort numerically ascending, then feed that sorted stream into `awk '{sum+=$1} END{printf "%.15f\n", sum}'` — do not sort lexicographically (plain `sort` will misorder negative numbers and scientific-notation-free floats incorrectly) and do not skip the intermediate `cat`/`find` step needed to normalize file boundaries before sorting. Redirect the fixed script's stdout to final_sum.txt. For the histogram, iterate every score file's value and bucket with awk comparisons matching the exact interval boundaries given (note strict vs inclusive edges at 0.0, 10.0, 100.0), writing counts in the exact key:value format requested.