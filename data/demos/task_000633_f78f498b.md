Use `seq 1 1000 | xargs -P 4 -I{} python3 /home/user/factorize.py {} >> /home/user/all_runs.txt` (or an equivalent xargs invocation with `-n1`) to run all seeds with 4-way parallelism, being careful that concurrent appends to the same file don't interleave/corrupt partial lines — xargs invoking a single command per line whose full stdout is one short print is safe, but if you build this differently (e.g. shell function per invocation) redirect each run's output atomically rather than using a shared `>>` inside a race-prone subshell. In `/home/user/distance.py`, parse each line of `all_runs.txt` by splitting on whitespace into a status token (`SUCCESS`/`FAIL`) and a float condition number, bucket floats into two lists accordingly, then call `scipy.stats.wasserstein_distance(success_list, fail_list)` directly — do not attempt to reimplement Wasserstein distance by hand, since subtle order-of-arguments or binning choices will produce a mismatching value. Round the result with Python's `round(x, 2)` and write only that number to `/home/user/result.txt`. Before trusting the final number, sanity check that `all_runs.txt` has exactly 1000 lines and that parsing correctly separates the two classes (e.g. print counts of each).