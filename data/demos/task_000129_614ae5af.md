Do the audio step first since it defines the sanitizer's actual rule set — transcribe /app/voicemail_backup.wav with a local speech-to-text tool (e.g. whisper-cli or ffmpeg piped to a transcription model) and read the spoken instructions carefully; listen for both the reject conditions (specific literal substrings that make a file evil) and the accept condition (a required literal substring/keyword that must be present), including the precedence between them if the speaker gives one (e.g. "reject if a bad string is present, otherwise accept only if the required keyword is present, otherwise reject"). Implement sanitizer.cpp as a simple substring scan of file contents against the exact strings you transcribed, returning exit 0/1 per the rule, then compile with g++ and validate it against every file in /app/corpus/clean and /app/corpus/evil before moving on, fixing your rule logic if any file misclassifies. For the VM piece, write vm_supervisor.sh to exec the given qemu-system-x86_64 command verbatim and chmod +x it. For scheduling, write unit files under `~/.config/systemd/user/vm-restore.service` (ExecStart pointing at the supervisor script) and `vm-restore.timer` (OnCalendar or OnUnitActiveSec=15min plus WantedBy=timers.target), then `systemctl --user daemon-reload`, `enable --now vm-restore.timer`, and confirm with `systemctl --user is-active vm-restore.timer`.
