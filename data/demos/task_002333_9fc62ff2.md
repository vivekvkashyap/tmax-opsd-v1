Design the script around idempotent merge-then-write, not append: on each invocation, load the new input (branch on `.csv` vs `.jsonl` extension using pandas `read_csv`/`read_json(lines=True)`), and if the output parquet already exists, read it back into a DataFrame and concatenate it with the new rows before deduplicating — this is what makes retries safe. Deduplicate by `user_id`, keeping only the row with the max `timestamp` per group (sort by timestamp then `drop_duplicates(subset='user_id', keep='last')`, or use `groupby('user_id').apply` picking the max); do this before or after masking but be consistent, and mask only for the final write, not for the merge comparison (so partially-masked emails from a prior run don't get double-masked). Implement email masking as `'***@' + email.split('@', 1)[1]` and phone masking as replacing the last 4 characters with `XXXX`, but special-case phone numbers of length <= 4 to become exactly `XXXX` in full. Finally overwrite the parquet at the given output path, preserving the five-column schema. Test manually with the sample files in /home/user/data/ across multiple sequential runs to confirm dedup and masking both hold up under retries.