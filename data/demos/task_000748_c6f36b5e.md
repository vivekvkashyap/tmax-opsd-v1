Fix the vendored library first: open /app/vendor/yaml.v3/yaml.go, find the `Unmarshal` function around line 95, remove the injected `panic("TODO: implement")` line, and uncomment/restore the original `return unmarshal(in, out, false)` call (or equivalent) so the package builds — confirm with `go build ./...` inside the vendor path or a small throwaway test program before moving on, since your routing script depends entirely on this working. For lb_decision.go, set up go.mod with a `replace` directive pointing `gopkg.in/yaml.v3` at the local `/app/vendor/yaml.v3` path so the build uses your fixed copy, not any network-fetched version. Define structs matching the YAML shape (request.path/method, vms as a slice of {id, status, cpu}), decode stdin with `yaml.NewDecoder(os.Stdin).Decode(&payload)`, then filter to status=="online" AND cpu<80.0, and if the filtered slice is empty print exactly `503 Service Unavailable`; otherwise sort/scan for minimum cpu breaking ties by lexicographically smallest id, and print `ROUTE: <id>`. Test manually with hand-crafted YAML covering zero VMs, all-filtered-out, and tie-breaking cases, and ensure no stray debug output goes to stdout since only the final line is expected.
