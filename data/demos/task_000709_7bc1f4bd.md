Break this into independently-verifiable stages and test each before moving on. Extract frames with `ffmpeg -i /app/web_traffic_capture.mp4 frame_%04d.png`, then for each frame read the pixel at (10,10) (e.g. with Pillow: `Image.open(f).getpixel((10,10))`) and take the grayscale/R value as one byte, writing bytes in frame order to `/home/user/extracted_payload.bin` — verify the file's length matches the frame count before trusting it. For the C decoder, design a lookup structure (array/hash map indexed by chunk ID, with bounds checking against the "total chunks" field) that accumulates 4-byte fragments per chunk ID, then reconstructs each complete run of chunks in ascending chunk-ID order into hex output, one line per group; handle malformed/truncated input defensively (partial chunks, out-of-range totals) since it's fuzzed against a reference implementation, so favor well-defined edge-case behavior over crashing. Compile both `decoder_cli` and `libdecoder.so` from the same source, load the `.so` from Python via `ctypes.CDLL`, and wire the Flask/FastAPI `/requests` endpoint on port 8080 to call into it and return JSON. Write the Makefile with build and `start-api` targets, then manually exercise the decoder on a few hand-crafted binary inputs (valid and malformed) to sanity-check parsing before relying on it.
