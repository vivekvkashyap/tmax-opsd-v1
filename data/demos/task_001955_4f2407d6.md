Extract exact-per-frame pixel coordinates from /app/movement_log.mp4 programmatically (e.g. ffmpeg to dump each of the 5 frames as an image, then with PIL/OpenCV scan each frame for the single pixel that is pure white (255,255,255) and record its (x,y) — don't guess coordinates by eye). Write the raw x/y arrays to /home/user/extracted_path.json exactly as specified. Implement Z-score normalization as a shared function: `(value - mean) / sample_std` where sample_std uses ddof=1 (numpy's `np.std(arr, ddof=1)`, not population std) — reuse this exact function both for building the reference path server-side and for normalizing incoming x_path/y_path in the endpoint, since a mismatched ddof will make even identical paths fail the "distance ~0" check. Stand up the Flask/FastAPI app bound to 127.0.0.1:9090, guard /analyze by comparing the "auth" field to the required token and returning 401 on mismatch before doing any computation, then compute per-timestep Euclidean distance between the two normalized paths and average across the 5 points, applying the <=0.5 anomaly threshold. Run the server in the background (e.g. nohup/&) so it stays up for external testing, and curl-test both the matching and mismatching cases yourself before finishing.