Build this as a Python data-processing script invoked by the bash orchestrator, following pandas' documented semantics precisely rather than approximating. Order matters: first drop rows with any missing `length`/`width`/`height` (do this before computing medians/means so the imputation stats aren't polluted by rows you're about to discard anyway — check which order the spec implies), then impute `price` with `df['price'].median()` and `weight` with `df['weight'].mean()` on the remaining rows. Compute `volume = length*width*height` and `density = weight/volume`. For outlier removal, compute the z-score using sample standard deviation (pandas' `.std()` default ddof=1) as `(density - density.mean())/density.std()` and keep only rows with `abs(z) <= 2.0`. For embeddings, `pip install sentence-transformers`, load `all-MiniLM-L6-v2`, encode the `product_name` column and the fixed query string separately, then compute cosine similarity (e.g. via `sklearn.metrics.pairwise.cosine_similarity`) between the query vector and each product vector, and take the argmax to get the best-matching `id`. Write the cleaned dataframe (all columns except the embedding array) to JSONL with `df.to_json(orient='records', lines=True)`, and write only the integer id (no extra formatting) to `best_match.txt`.
