Work this as a forensic chain where each step's output feeds the next — verify each link before proceeding. First, grep/scan `/home/user/logs/process.log` for command lines under `/home/user/app/` that pass a secret-looking argument (like `--key`) directly on the command line; that identifies both the offending script's filename and the literal leaked key value present in the log line — read it out of the log rather than guessing. Second, open that script in `/home/user/app/` and refactor it: remove the CLI argument for the key and instead read it via `os.environ.get("BACKUP_KEY")` (or `os.getenv`), keeping the rest of its behavior intact, and set `BACKUP_KEY` in the environment when you later invoke it, if needed. Third, decrypt `/home/user/data/secret.enc` using the exact key you extracted from the log with `openssl enc -aes-256-cbc -pbkdf2 -d -in ... -pass pass:<key>` — the recovered plaintext is your task's actual secret, don't assume its content. Fourth, hash the decrypted text with `sha256sum`, being careful to strip any trailing newline before hashing (e.g. `printf '%s'` instead of `echo`, or `-n`). Finally assemble the four-line report exactly as specified, with no extra blank lines or labels.