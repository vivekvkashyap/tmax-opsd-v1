Build this bottom-up and test each layer in isolation before wiring them together. First get `processor.go` compiling as a C-shared library: `go build -buildmode=c-shared -o libprocessor.so processor.go` (put this in `build.sh`); confirm it produces both the `.so` and a header, and check the exported symbol name matches `ProcessData`. Before writing the WebSocket server, write a tiny throwaway Python snippet that loads `libprocessor.so` via `ctypes.CDLL`, sets `argtypes = [ctypes.c_char_p]` and `restype = ctypes.c_char_p`, and calls `ProcessData` directly on a test string — verify the output matches the expected reverse-and-capitalize-each-word transform (compute it yourself: uppercase then reverse each word's characters, preserving word order) before trusting the library boundary. Only then build `server.py` with `websockets.serve`, decoding ctypes return bytes to str for the client. For `test_server.py`, launch `server.py` as a subprocess, give it a moment to bind the port (poll rather than blind-sleep), run the assertion, then make sure you terminate the subprocess cleanly in a `finally`/fixture teardown so the port isn't left bound across test runs.