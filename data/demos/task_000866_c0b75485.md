Build this as a three-stage pipeline and verify each stage's output before moving to the next. Stage 1: clone/build whisper.cpp, fetch the ggml-base.en.bin model, and run it on the wav with a flag that emits timestamps (VTT or CSV output) — inspect the raw output file to confirm segment start times and text before parsing. Stage 2: parse each segment's start time and text; normalize by lowercasing and stripping punctuation (keep only letters/digits/spaces, collapse extra whitespace); bucket by `floor(start_time/10)*10`; within a bucket, deduplicate identical normalized phrases before joining with single spaces (the transcript audio is designed so one bucket contains a repeated phrase — if you don't dedupe you'll produce a doubled string that fails verification). Stage 3: write a C TCP server binding to 127.0.0.1:9090 that accepts a connection, reads a newline-terminated bucket-start integer, looks up the cleaned bucketed text (or reports EMPTY if absent/out of range), and responds with exactly `"<text> [LOC-EN]\n"` (note the single leading space before the tag) before closing the connection — get the exact byte-for-byte response format right, including for the EMPTY case. Compile with gcc, launch it detached (e.g. with `&` or nohup) so it keeps serving, and test with `nc`/a socket script for buckets 0, 10, 20, and an out-of-range bucket before finishing.
