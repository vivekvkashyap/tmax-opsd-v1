Parse `/home/user/edges.csv` and build a directed adjacency structure, but filter rows to `relation_type == 'reports_to'` at load time — this is the key fix over the intern's script, which effectively ignored relation_type and treated every edge as traversable, causing cross-join blowup and cycles. With only `reports_to` edges the graph should be a clean DAG (a tree rooted near the CEO), so run BFS/shortest-path from each employee node to `E001` following only those edges (a simple BFS per source, or one reverse-BFS from E001 over reversed `reports_to` edges to get shortest paths to all nodes at once is more efficient). Count hops as number of edges in the path (path node count minus 1), keep only employees with hops >= 2, sort the resulting list by `employee_id` ascending, and serialize each entry as `{"employee_id", "hops", "path"}` with path listed from the employee to E001. Write the JSON array to `/home/user/verified_paths.json` with `json.dump`, and sanity-check by hand-tracing one or two employees' `reports_to` chains against the CSV to make sure only that relation type was followed.
