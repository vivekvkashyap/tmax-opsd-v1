Tackle this in three layers and validate each independently before combining. First, understand why `/app/bin/polycalc` fails: run `strings` and `ltrace`/`strace` on it to see it's trying to dynamically resolve a license-check symbol (a function returning a boolean) from a missing shared object named `liblicense.so`; write a minimal C stub exporting that exact function name returning 1, compile it as a shared object with `gcc -shared -fPIC -o /home/user/liblicense.so lic.c`, and get the binary to run cleanly via `LD_PRELOAD=/home/user/liblicense.so /app/bin/polycalc <x> <y>` — confirm it prints a plain integer for known inputs before building anything on top of it. Second, write `server.sh` as a `while true` loop around `nc -l` (or `socat`) that reads the raw HTTP request, parses the request line/body, and only proceeds for `POST /api/v1/math/poly`. Third, use `jq` to pull `x` and `y` out of the JSON body, call `polycalc` with `LD_PRELOAD` set, capture its stdout, and hand-construct the HTTP response with correct headers (`HTTP/1.1 200 OK`, `Content-Type: application/json`, blank line) and body `{"status": "success", "result": <value>}`. Test with several sequential `curl` POSTs to confirm the loop doesn't die after one request, then leave the server backgrounded (`nohup ... &`) and running.
