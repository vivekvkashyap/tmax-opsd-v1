Implement your own directory walker instead of relying on `os.walk(followlinks=True)` blindly, since you need per-directory loop detection: maintain a `set()` of `os.path.realpath()` values for directories already visited; before recursing into a subdirectory (symlink or not), resolve its real path — if already in the visited set, it's a loop, so append the *symlink's absolute path* (not the resolved target) to the skip log and don't descend into it; otherwise add the real path to the visited set and recurse. For each `.json` file found, look for a sibling `.bin` file sharing the same base name in that directory, read it in binary mode, and hash it with `hashlib.sha256(data).hexdigest()`. Extract `name`/`version` from the parsed JSON. Before writing the final manifest array, open the output file, call `fcntl.flock(fd, fcntl.LOCK_EX)` to guard the write, write the JSON array, then let the lock release on close — this is a stated grading requirement, not optional. Run the script once and confirm both output files are produced and non-empty.
