Start by inspecting the schema before writing any query logic: open the SQLite file with `sqlite3 /home/user/graph.db ".schema"` (or `.tables` then `PRAGMA table_info(<table>)`) to discover the actual table and column names, since they are deliberately non-obvious — don't assume names like `edges`/`src`/`dst`. Once you know the table storing directed edges and the relationship-type column, write the Python script to load only rows where that column equals `'friend'` into memory (or query with a `WHERE rel = 'friend'` clause), build a directed adjacency structure from source to destination, and search for 3-cycles: for each edge A->B, for each edge B->C, check whether an edge C->A exists in the same filtered friend-only edge set. Deduplicate found triangles by converting each triple to a sorted tuple (since rotations of the same cycle should collapse to one entry), collect into a set, then convert to a list of 3-element lists sorted first by node values within each triangle and then lexicographically across the outer list. Dump with `json.dump` to `/home/user/triangles.json` using the exact nested-list format shown, then execute the script and confirm the file was written.
