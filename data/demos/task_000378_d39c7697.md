Use `git bisect` properly rather than eyeballing diffs: `git bisect start`, mark `HEAD` bad and the first commit good, then at each step run `python3 analyzer.py U15` (or another known user) against `sqlite3 transactions.db` to compare the computed balance to what you independently derive from summing the raw rows (purchases add, refunds subtract) for that user, and mark the commit good/bad based on whether the logic matches the correct subtract-refunds semantics — not just whether the script runs without error, since the bug is a silent logic change (e.g. a query filter or missing subtraction), not a crash. Once `git bisect` converges, capture the commit hash it reports before running `git bisect reset`. Then check out `main`, patch `analyzer.py` so refunds are subtracted from purchases (matching the original correct logic), rerun for `U15`, and record both the bisected hash and the fixed balance into `/home/user/solution.txt` on two separate lines, exactly as specified.