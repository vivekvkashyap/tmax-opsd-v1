Treat this as two separable subproblems that happen to point at the same underlying cycle. For Phase 1, parse deps.json into an adjacency map and run a standard cycle-detection algorithm (DFS with recursion-stack coloring, or Tarjan's SCC) that can report the actual cycle path, not just a yes/no; among all cycles found, pick the shortest one, sort its member module names alphabetically, and write them comma-separated with no extra whitespace/newline issues. For Phase 2, once you see which two Go packages cycle, break the mutual import by extracting the shared types (the structs each package currently imports from the other) into a brand-new package under the same module, updating both original packages' imports and usages accordingly, and confirm with `go build ./...` that no cycle remains. Add the unit test asserting the specific Solve() call and expected field values, and run `go test ./...` to confirm it passes. Finally chmod +x run_ci.sh, make sure it truly chains the cycle-detection run and the Go build/test steps with proper error propagation (`set -e` or explicit exit-code checks), and run it end-to-end yourself to confirm it exits 0.
