Fix `benchmark.py` methodically against each stated defect rather than rewriting from scratch: add `argparse` with `--batch_size` and `--seed`; call `matplotlib.use('Agg')` immediately after importing matplotlib and before any `pyplot` import (import order matters — importing pyplot first before setting the backend will still try to initialize a display backend); call `np.random.seed(args.seed)` right before `np.random.rand(N, N)` so the matrix generation is reproducible; time only the `A @ A.T` and `np.trace(...)` calls (start the timer right before the matmul, stop right after the trace, excluding matrix generation and I/O); replace `plt.show()` with `os.makedirs('plots', exist_ok=True)` then `plt.savefig(f'plots/plot_{batch_size}.png')`; and print exactly one JSON line with the four required keys (build it via `json.dumps` to guarantee valid JSON, and make sure nothing else is printed to stdout). Then write `run_experiments.sh` to `mkdir -p /home/user/results`, loop over batch sizes 100/200/300 with seed 123, capture each run's stdout (the JSON line) via `$(python3 benchmark.py --batch_size N --seed 123)` and append it to `/home/user/results/tracking.jsonl`. Run the pipeline and confirm the three plot PNGs and the JSONL file exist.
