Work the three stages independently and verify each before moving on. For Stage 1, don't guess-fix compile errors line by line — first check `Cargo.toml` for anything obviously wrong (edition, dependency versions) since a single misconfigured `edition` field can cascade into dozens of unrelated-looking syntax errors (e.g. `?` operator, module path resolution). Fix the root cause, then rerun `cargo build` inside the vendored crate directory to confirm exit code 0 before touching Stage 2. For Stage 2, use `openssl req -x509 -newkey rsa:2048 -nodes -keyout ... -out ...` for an unencrypted key/cert pair, then `chmod 400` the key and verify with `stat -c "%a"`. For Stage 3, implement the decoder as a literal transliteration of the spec: read one stdin line, base64-decode it (treat decode failure as invalid), XOR every byte with 0x5A, check length >= 4 and that the first four XORed bytes equal the ELF magic bytes when XORed back (i.e. compare decoded[0..4] to the raw ELF magic after your own XOR step) — get the byte order and which value gets XORed against which exactly right, since a fuzzer will catch any off-by-one or reversed-XOR mistake. Add goblin as a path dependency to satisfy the "must depend on fixed goblin" requirement even if unused, build with `cargo build --release`, and manually test both valid-ELF and invalid inputs against your own binary before considering it done.