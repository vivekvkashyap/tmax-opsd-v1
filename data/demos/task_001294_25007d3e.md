This task has three independent parts — tackle them separately and verify each before moving on. (1) Build fix: inspect `/app/vendor/libsectoken-1.2.0/Makefile` for tab-vs-space issues in recipe lines (Make requires literal tabs, not spaces, before commands) and check the include flag points at the package's own `include/` directory rather than a bogus path; fix both, then confirm `make` and `pip install .` succeed and `import libsectoken` works. While auditing `src/validate.c`, look specifically for a `strcmp`-style comparison used on secret/token data — that's a timing side-channel (CWE-208); replace it with a constant-time comparison (XOR-accumulate over all bytes regardless of early mismatch, or use a crypto library's constant-time compare) so runtime doesn't depend on where the mismatch occurs. (2) For `rotate_tokens.py`, since it must be bit-exact equivalent to a reference binary under fuzzing, don't guess the transformation — write a Python program using your fixed `libsectoken`, then iteratively test many seed/user_id combinations against `/opt/legacy/rotate_tokens_ref` and adjust your hashing/escaping/formatting logic until outputs match exactly, since fuzz equivalence testing has zero tolerance for formatting differences. (3) For the port audit, iterate ports 1000-5000, attempt an HTTP GET (with a short timeout) on each, and grep the response body for the literal endpoint string before logging the port.