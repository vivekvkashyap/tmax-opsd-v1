Build the pipeline in Go as a strict sequential transform, matching the spec's operation order exactly since the grader recomputes ground truth with the same steps. Use `ffmpeg` (via `os/exec`) to extract frames from `/app/data_feed.mp4` at `fps=10`, converting each to grayscale and computing mean pixel intensity per frame (you can shell out to ffmpeg's `signalstats` filter to get per-frame average, or decode frames to raw grayscale and average pixels yourself in Go for full control — either is fine as long as the numbers match a straightforward frame-average). Process frames in Go for the rest: always keep frame index 0; for each subsequent frame compare its raw intensity to the *last kept* frame's raw intensity and only keep it if the absolute difference is strictly greater than 1.0 — do the freeze-detection comparison on raw intensity, not normalized, before normalizing. Normalize kept intensities by dividing by 255.0. Compute the rolling SMA over kept (normalized) values with a window of 5, using a shorter window for the first 4 points (average of however many kept points exist so far) — don't pad with zeros. Compute `sma_diff` as the absolute difference to the previous kept point's SMA, `0.0` for the first. Write the CSV with the exact header and columns specified, then run the Go program to produce `/home/user/processed_signal.csv`.