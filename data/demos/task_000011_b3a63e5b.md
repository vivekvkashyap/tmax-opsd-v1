Split this into a data-recovery phase and a from-scratch statistics phase. First, transcribe `/app/experiment_targets.wav` using a speech-to-text tool (e.g. `openai-whisper` via pip, or another ASR you can install) to get the 50 dictated numbers in spoken order — parse the transcript carefully into 50 floats (watch for words like "point", "negative", spoken-number formatting) and pair them positionally with the 50 X values from `features.csv` in file order; verify you got exactly 50 values before proceeding, since a wrong count silently corrupts everything downstream. Second, in the C++ program implement Pearson correlation manually from the standard formula (covariance over product of standard deviations) using only `<cmath>`/`<vector>`. For the bootstrap: seed `std::mt19937 gen(42)` once, then for each of 10000 iterations draw N indices with replacement using a uniform_int_distribution over [0, N-1], build the resampled (X,Y) pairs, and recompute Pearson r for that resample, storing all 10000 r values. Sort that vector of r values and take the elements at the 2.5th and 97.5th percentile positions (use consistent, documented index rounding, e.g. `floor(0.025 * (B-1))` and `floor(0.975 * (B-1))`) as `ci_lower`/`ci_upper`. Write these two floats to the exact JSON path/format required.
