Diagnose the race condition first: read backend.py and kv_store.py to confirm exactly which port each binds/checks and why simultaneous startup causes the backend's connection attempt to fail before kv_store is ready. Then modify only /app/start_all.sh to add explicit readiness gates between each service launch — start a service in the background, then busy-wait/poll until its port is actually accepting connections (e.g. a loop using `nc -z 127.0.0.1 <port>` or retrying a socket connect, with `sleep` between attempts) before starting the next service; do this between kv_store→backend and again between backend→frontend. Leave all three `&`-launched processes backgrounded so the script itself returns while they keep running. For health_monitor.py, make a plain HTTP GET to the frontend's proxy endpoint, parse the JSON response, and write the exact JSON text (not a Python repr) to /home/user/status.log. After editing, actually run start_all.sh fresh (kill any stale processes first) and confirm with `pgrep`/curl that all three ports are live and the end-to-end proxy chain returns 200 before considering it done.
