Treat this as black-box protocol reverse engineering, not guesswork. First read the binary statically: `file`, `strings`, and `objdump -d` / `radare2`/`ghidra` on `/app/alert-router` to spot string literals (section names, URL prefixes, error messages) and comparison logic. Then do differential black-box testing: take a known-good config and mutate it one field at a time (toggle `[MailingList]` presence, swap `https://`/`http://`, insert `localhost`, vary line lengths) while running the binary each time and recording its exit code. Pay special attention to boundary conditions on line length — stripped C binaries with fixed-size stack buffers often crash (segfault) at one specific length due to an off-by-one or missing bounds check; sweep line lengths systematically (e.g. 1..128 bytes per line) to find the exact crashing length rather than assuming it. Cross-reference findings against the example configs in `/home/user/example_configs/` to confirm your hypothesis before finalizing rules. Only after you have empirically confirmed each rule (section requirement, URL scheme/substring checks, and the crash-triggering line length) should you encode them into `/home/user/config_linter.py`, which must replicate all checks purely in Python without invoking the binary, and exit 0 only when every rule passes.
