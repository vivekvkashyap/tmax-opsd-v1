Write one Python script that handles each log format with its own parser, then merges. For `/home/user/logs/auth.log`, parse lines with a regex like `\[([\d\-T:]+)Z\] (\S+) (.*)`, filter where the message contains `timeout` case-insensitively, and normalize the timestamp by appending `.000Z` after stripping the trailing `Z`. For `/home/user/logs/payment.log`, open with `encoding='utf-8'` (it's already UTF-8/Japanese, no special decoding needed), parse `EPOCH_MS [LEVEL] message` with a regex, filter for the exact Japanese substring the task specifies, and convert epoch milliseconds to ISO 8601 UTC with milliseconds using `datetime.fromtimestamp(ms/1000, tz=timezone.utc)` formatted to `YYYY-MM-DDThh:mm:ss.sssZ` — watch out for locale/timezone bugs, always use UTC explicitly. Merge both lists of dicts, sort by the ISO timestamp string (which sorts correctly lexicographically since it's zero-padded ISO 8601), and write to `/home/user/unified_timeouts.json` as a JSON array with keys `timestamp`, `service`, `message` in that structure. Count timeouts per service and append the exact `[SUCCESS] Processed logs: auth timeouts: <X>, payment timeouts: <Y>` line to `/home/user/pipeline.log`. Run the script and check the JSON is valid and properly UTF-8 encoded (use `ensure_ascii=False` when dumping so Japanese text is human-readable).