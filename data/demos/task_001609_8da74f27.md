Implement this as an ordered sequence of pandas operations, applying each rule exactly as specified rather than combining/reordering steps. Load the CSV, then: (1) drop rows with missing/NaN timestamp; (2) convert `temperature_c` values strictly outside (-30.0, 80.0) to NaN (do this before imputation, treating them the same as originally-missing values); (3) group by `sensor_id` and compute the mean of *valid* (non-NaN, post-outlier-removal) temperature per group, rounded to 2 decimals, then fill each group's remaining temperature NaNs with that group's mean — do the analogous thing for `humidity` using median rounded to 1 decimal, computed independently per sensor group (use `groupby().transform` or a per-group fillna, being careful the mean/median used for imputation excludes the values you're about to fill); (4) derive `temp_f` from the now-clean `temperature_c` (not the raw values) rounded to 2 decimals, and `hour_of_day` as an integer via `pd.to_datetime(...).dt.hour`; (5) sort by timestamp then sensor_id, reorder/select the exact required columns, and write with `index=False`. After running, spot-check a couple of rows by hand against the raw CSV to confirm the outlier and imputation logic produced sane values before trusting the full diff-based verification.