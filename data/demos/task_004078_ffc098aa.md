Inspect /home/user/iot_init.sh first (cat it) to see its exact prompt strings and what environment variables it checks — match the prompts character-for-character in your pexpect `expect()` calls, since a trailing space or colon mismatch will hang the spawn. Use `pexpect.spawn('/home/user/iot_init.sh', env=env_dict)` where env_dict is built by copying `os.environ` (via `dict(os.environ)`) and then overriding only `TZ` and `LC_TIME` with the required values — passing a fresh env dict to spawn's `env=` param scopes the vars to the child process without touching the actual shell/session environment, which is exactly what's required. Chain `child.expect("Configuration ready? \\[y/N\\]: ")` then `child.sendline("y")`, then `child.expect("Device ID: ")` then `child.sendline("EDGE-99X")`, and finally `child.expect(pexpect.EOF)` plus check `child.exitstatus == 0` to confirm success rather than assuming it worked. After running your script, `cat /home/user/iot_deploy.log` to confirm the script's own success-path message appears rather than its failure-path message.