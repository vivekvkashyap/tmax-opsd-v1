Locate the regression with git history tools before touching code: run `git -C /home/user/geoparse-repo log --oneline -- parser.py` (or the relevant file) and `git log -p -- <file>` to find the commit that introduced a `round(x, 4)` call and removed a `.strip()` (or similar whitespace-sanitization) step — `git bisect` or `git blame` on the current `parse_line` function works too if the log is large. Once you've identified the offending diff, port the fix (both restoring full precision by not rounding, and restoring whitespace stripping) into the currently-deployed `/app/geoparse/parser.py`, since that vendored copy is what's actually broken, not the repo itself. For robust parsing in `fixed_parser.py`: split on the first comma, `.strip()` each half (handles tabs/spaces), and rely on Python's native `float()` which already accepts forms like `-.987` — wrap the whole parse in try/except and print `INVALID` on any failure (not exactly two comma-separated numeric fields). Print the tuple with enough precision (e.g. avoid default `str()` truncation) to preserve up to 9 decimal places, and test manually against a range of well-formed, corrupted, and garbage inputs before considering it done.
