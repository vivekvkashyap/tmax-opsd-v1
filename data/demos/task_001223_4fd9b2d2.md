Handle this as three independent deliverables, testing each before assembling the HTTP layer. (1) Linking: look at `/home/user/src/build.sh` and note it only compiles `analyzer.c` without referencing `libcore.so` — fix it to add the library search path and link flag (`-L./lib -lcore`) AND bake in a runtime search path via `-Wl,-rpath,<absolute-path-to-lib>` (or export `LD_LIBRARY_PATH` before running) since a binary that links but can't find the .so at runtime will still fail; verify by actually running `./analyzer` standalone afterward, not just checking the compile succeeded. (2) Frames: run `ffmpeg -i /app/test_stream.mp4 -vframes 30 /home/user/frames/frame_%02d.png` and confirm the output filenames are exactly zero-padded 2-digit (ffmpeg's `%02d` gives this) matching frame_01.png..frame_30.png. (3) Server: write `server.sh` using a loop around `nc -l -p 8080` (or `socat TCP-LISTEN:8080,reuseaddr,fork`) that reads the request line, extracts the `frame=<N>` query parameter with basic string tools (`grep -oP`/`sed`), invokes the analyzer binary on the matching frame file, captures its stdout, computes `Content-Length` from the actual byte length of that output, and writes a well-formed HTTP/1.1 response (correct header/blank-line/body structure, using `\r\n` line endings) back on the same connection. Test the server manually with `curl` against a known frame before backgrounding it and leaving it running.