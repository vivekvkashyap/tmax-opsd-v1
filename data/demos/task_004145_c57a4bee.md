First recover the token: transcribe `/app/voicemail.wav` (a speech-to-text tool or transcription utility) to get the four spoken digits, and embed that literal string in your C source as the expected token — don't guess it. Then write a TCP server in C: `socket`/`bind`/`listen` on `0.0.0.0:8080`, `accept` in a loop (fork/thread per connection), and read each connection into a fixed 512-byte stack buffer via `read`/`recv` with an explicit length bound — never `gets()` or unbounded `scanf`/`strcpy`, and leave room for the null terminator. Parse the `TOKEN: <t> PAYLOAD: <b64>\n` line (bounded `sscanf` or manual pointer scanning), compare the parsed token to the expected one, and only on match base64-decode the payload and append it plus `\n` to `/home/user/audit.log`, replying `SUCCESS\n`; otherwise reply `AUTH_FAILED\n` and drop the connection without touching the log. Test with `nc` including an oversized payload to confirm the 512-byte cap prevents crashes, then compile and leave the server running in the background, confirming it stays listening on 8080.
