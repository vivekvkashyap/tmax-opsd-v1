Parse the CSV into fixed-size arrays keyed by emp_id (or a small hash map) since emp_id/manager_id are small integers — don't reinvent a general CSV library. For subordinate_count, build a manager->children adjacency and do a DFS/BFS from each employee counting all reachable descendants (careful: this is total descendants in the whole subtree, not just direct reports). For dept_avg_salary, first pass to sum salary and count per department, then a second pass to assign the average (formatted with `%.2f`) to every employee in that department. For dept_salary_rank, within each department sort employees by (salary DESC, emp_id ASC) and assign rank 1..n in that sorted order — the tie-break on smaller emp_id is easy to miss, so write a comparator that checks salary first, then emp_id. Treat "NULL" manager_id as no-parent (top-level) rather than parsing it as a number. Finally sort the output rows by emp_id ascending before writing, `mkdir -p` the output directory, compile with `gcc -o /home/user/process_csv /home/user/process_csv.c`, run it, and diff the generated CSV against the exact expected header/format.
