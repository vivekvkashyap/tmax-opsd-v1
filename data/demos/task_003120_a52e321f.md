Break this into its three phases and verify each in isolation before wiring them together. Phase 1: parse `net_status.txt` to get the set of REACHABLE vm names, parse `qemu_procs.txt` to extract `-name`, `-m`, `-smp` per process (a regex or field-split per line works), then intersect the two sets before summing RAM/CPU — a VM must be both reachable AND have a running qemu process to count; write the totals to `active_capacity.txt` in exactly the specified two-line format (no extra whitespace/labels). Phase 2: the git `pre-receive` hook receives `<old> <new> <ref>` lines on stdin — use `git show <new>:vms.json` (or a temp checkout) to read the incoming file's content without needing a working tree, sum its cpu/ram, add to the current `active_capacity.txt` totals, and reject (non-zero exit, "CAPACITY EXCEEDED" on stderr) only if the combined total crosses either limit; remember to `chmod +x` the hook and give it a Python shebang. Test it by actually pushing a small over-limit and under-limit commit to `infra.git` from a scratch clone, not just by inspecting the code. Phase 3: `roll_deploy.py` just needs to create the two stage directories and write one `<vm>.conf` file per entry keyed off its `env` field — test it directly against a small sample JSON.