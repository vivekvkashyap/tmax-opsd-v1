Build this deployment pipeline piece by piece and verify each piece is actually running (not just written) before moving to the next. Init the bare repo with `git init --bare /home/user/repo/app.git`; write the post-receive hook in Python reading pushed refs from stdin, using `git --work-tree=... --git-dir=... checkout -f` to materialize files into `/home/user/deployments/app`, then atomically updating the `current_app` symlink (write to a temp symlink and `os.rename` over it), killing any existing server.py (e.g. via pgrep/pkill or a saved PID file) before respawning it with stdout/stderr redirected into app.log using `subprocess.Popen` with `start_new_session=True` so it survives the hook's exit. For proxy.py, use a simple threaded socket accept-loop that opens a matching connection to 9090 for each inbound 8080 connection and pipes bytes both directions. For monitor.py, loop with `time.sleep(5)`, check via pgrep/psutil whether server.py is alive, restart if not and if the target file exists. Write PID files immediately after backgrounding each script with `disown`/`&`, and confirm with `kill -0 $PID` that they're truly running before finishing.