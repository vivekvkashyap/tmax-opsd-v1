Tackle the three deliverables independently and verify each before moving on. For the vendored package: try `pip install --user .` inside `/app/vendored/yaml-patcher-2.1.0` first to see the actual traceback rather than guessing — a `NameError` on `os.environ` in `setup.py` means a missing `import os` at the top of that file; add it, reinstall, then confirm with `~/.local/bin/yaml-patcher --version`. For the manifest processor: write a script (Python with a YAML library is easiest for reliable round-tripping) that reads Deployment YAML from stdin, walks `spec.template.spec.containers`, and for each container appends a `volumeMounts` entry named `audit-vault` at `/var/log/audit` only if an entry with that exact name isn't already present (idempotency = check-before-append, not blind append), then does the same check-and-append for the pod-level `spec.template.spec.volumes` entry with `emptyDir: {}`; test manually with a manifest that already has the mount to confirm it doesn't duplicate. For systemd: create the unit file with `[Service]` `ExecStart=/home/user/watch.sh` (making watch.sh empty and executable first), then run `systemctl --user enable manifest-watcher.service` and confirm with `systemctl --user is-enabled`.
