Treat /app/provtool as a black box and derive its rules empirically rather than guessing: run `strings /app/provtool` to spot key names/paths/thresholds it references, then diff the clean vs evil example configs in /home/user/examples/ line-by-line to see which fields differ, and use `strace`/black-box test runs (feed it crafted configs varying one field at a time, e.g. quota values near suspected boundaries, path prefixes with/without `../`) to pin down exact thresholds and crash triggers — don't stop at the given examples alone since the hidden test corpus will probe edge cases the examples don't show. Once you've inferred the rules (e.g. path traversal or disallowed prefixes for Target-Dir, out-of-range or overflow-triggering Disk-Quota values, a specific Ssh-Key-Auth value that silently disables auth), implement equivalent checks in Go at /home/user/filter.go: parse the `Key: Value` lines yourself (don't shell out to the vulnerable provtool binary), print "SAFE"/exit 0 or "REJECT"/exit 1 accordingly, and handle a missing/unreadable file as REJECT. Before finishing, re-test your compiled /home/user/filter against all given clean and evil examples to confirm 100% agreement, since the hidden corpus is scored on exact accuracy.