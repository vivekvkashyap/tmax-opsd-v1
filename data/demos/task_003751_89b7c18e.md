Isolate the leak first: the current code folds bootstrap samples into the dataset before computing mean/stddev, so scaling parameters see resampled duplicates from the "future." Refactor so mean and standard deviation (population or sample — be consistent) are computed strictly from the original input rows column-wise, stored separately, and then reused unchanged to standardize both the original rows and each of the 5 bootstrap-resampled row sets — guard the division with `if std == 0.0 { 0.0 } else { (x - mean) / std }` per column. For tracking, after computing the scaled original array, take its element-wise absolute value and reduce to a single max, then use the `redis` crate to `SET experiment:latest:max_abs_val <value>` against `127.0.0.1:6379` — start `redis-server --port 6379 &` before your Actix app binds to `127.0.0.1:3000`. Keep the response shape exactly `{"original": [...], "bootstraps": [[...], ...]}` with nested arrays matching the original row/column shape. Test manually with `curl -X POST` and `redis-cli GET` before considering it done, since both an HTTP and a Redis check will run against your live services.
