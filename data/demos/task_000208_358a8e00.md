Write this as a state-machine line reader in C rather than trying to regex the whole file at once, since Text can span arbitrarily many lines. Read raw_dataset.txt line by line: on seeing "---RECORD---", finalize/emit the previous record buffer (if any) and start a new one; for each subsequent line, check if it starts with "ID: ", "Date: ", or "Text: " to begin a field, and if a line matches none of those prefixes while you're inside a Text field, treat it as a continuation of Text (append with a single space separator, tracking whether you've already started so you don't add a leading space, and not appending a trailing space when the record ends). A record is valid only if all three of ID, Date, and non-empty Text got set — otherwise skip it and append the exact literal error line to pipeline.log. For valid records, compute the cleaned text length and update a running average (running_sum/count) formatted to exactly 2 decimal places, then emit `ID,Date,Text,RollingAvgLen` rows to cleaned.csv with a header. Compile to /home/user/processor and run it, then diff your cleaned.csv/pipeline.log against your own hand-traced expectations on a couple of small constructed inputs before trusting the full run.
