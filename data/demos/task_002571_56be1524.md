Fix the C bug first, in isolation, by comparing against the intended semantics: the `+` branch is silently doing subtraction, so correct it to actually add the two operands, then rebuild and manually test the binary on a couple of inputs before touching the Python layer. For the Python 2-to-3 migration, the two failure points are the `print` statement syntax (must become a function call) and the fact that `subprocess.check_output` returns `bytes` in Python 3 — a naive `print(out.strip())` on bytes yields a `b'...'` repr in the output, which is wrong; decode the subprocess output (e.g. `.decode()`) before concatenating so the final printed lines are clean text like `Result: <value>`, matching the original intended format exactly. Write `ci_build.sh` as a straightforward sequential script: compile with `gcc calc_interp.c -o calc_interp`, then invoke `python3 run_data.py data.csv`, redirecting stdout to `ci_output.log` with `>`. Make it executable, then actually run `./ci_build.sh` yourself and inspect `ci_output.log` byte-for-byte against what you expect from the CSV's three operations before declaring done — don't just trust that the script "should" work.