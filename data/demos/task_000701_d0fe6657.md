For the recursion fix, add a "visited" guard that persists across the whole run (e.g. a file like `/home/user/project/visited.txt`, or better, refactor to a single top-level script that maintains an associative array/set in one process) so each dependency name is expanded and appended to `resolved.txt` at most once — check membership before recursing and before echoing, and skip entirely (don't recurse, don't re-append) if already visited; this breaks the cycle in deps.txt (crypto -> network_base -> network -> crypto) without needing to detect the cycle explicitly, since the second visit to any already-resolved node is simply a no-op. For the anomaly, parse `download.log` and pull out the numeric ms value from each line (e.g. with `grep -oP` or `awk`), filter for values greater than 5000, and write the corresponding dependency name (not the whole line) to anomaly.txt. For the timeline, concatenate all files in `logs/` and sort with `sort` using the leading `[YYYY-MM-DDTHH:MM:SS]` prefix as the sort key (plain lexicographic `sort` works since ISO-8601 timestamps sort correctly as strings) — write the full original lines, unmodified, in sorted order to timeline.txt.