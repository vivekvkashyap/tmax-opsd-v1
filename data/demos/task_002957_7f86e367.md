Treat this as three layered fixes plus an orchestration step — verify each layer before moving to the next. First, inspect `mc_integrator.cpp`'s RK4 loop closely: a common seeded bug is that the `k3` stage is computed using the `k1`-half-step state instead of the `k2`-half-step state (i.e. `k3 = dt*f(x + k1/2, ...)` when it should be `k3 = dt*f(x + k2/2, ...)`); compare each `k1`..`k4` computation against the textbook RK4 definition for the coupled system `dx/dt=y, dy/dt=-k*x-c*y` and fix any stage that reuses the wrong intermediate state, then rebuild with `-fopenmp` to `/home/user/workspace/mc_worker`. Sanity-test it manually with a few hand-picked `(k, c, x0, y0, N_mc)` inputs and check the output is exactly two space-separated floats on stdout, since it will be fuzz-compared against a hidden oracle for exact equivalence — get the numerics right, not just "close". Second, in `api_server.py`, replace the placeholder Redis host with `127.0.0.1:6379` and set the worker invocation to launch your compiled binary via `mpirun` (e.g. `mpirun -np 2 /home/user/workspace/mc_worker ...`), then start Redis, the Flask API on port 5000, and Jupyter on port 8888. Finally use the notebook or a direct `POST` to the API to trigger generation and confirm `training_data.h5` is created with the expected dataset structure by opening it in Python.