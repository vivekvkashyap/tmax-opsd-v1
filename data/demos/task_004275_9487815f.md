Process the JSONL as a true stream: open the file and iterate line-by-line with `json.loads` per line, never `readlines()`/`json.load()` on the whole file. Maintain the rolling window with `collections.deque(maxlen=10)`, appending each line's `len(es)/len(en)` ratio and computing the average over whatever is currently in the deque (so early lines average over fewer than 10 items). Check "average strictly exceeds 2.0" after updating the deque for the current line, and stop collecting once exactly 5 flagged records have been gathered — you can break out of the loop early. Serialize the 5 records as a JSON array with `json.dump`/`json.dumps` to `/home/user/flagged.json`. For the upload, read that file's raw bytes and POST them as the request body verbatim (e.g. `requests.post(url, data=open(path,'rb').read())` or `curl --data-binary @/home/user/flagged.json`) rather than re-serializing a Python object with `json=` — the server must receive the exact same bytes you wrote to disk, since it saves and later compares them.
