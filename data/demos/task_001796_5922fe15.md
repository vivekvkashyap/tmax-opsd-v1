Work through this as five independent sub-steps, verifying each before moving on. First load both CSVs with pandas and merge on `user_id` (inner join). For the bootstrap CI: extract the two `session_duration` arrays for group A and B, call `numpy.random.seed(42)` exactly once right before the loop, then run 1000 iterations using `numpy.random.choice(arr, size=len(arr), replace=True)` on each group separately per iteration, storing `mean(sample_B) - mean(sample_A)`; take `numpy.percentile` at 2.5 and 97.5 over the collected diffs — seed placement and using choice (not `sample`/`randint`) matters for reproducibility. For the Bayesian step, count Group B successes/trials and apply the Beta(1,1) posterior-mean formula directly. For retrieval, filter to Group B converters only, fit `TfidfVectorizer` solely on their feedback texts (not the whole corpus), transform the query with the same fitted vectorizer, and use `cosine_similarity` + `argmax` (ties resolved by first occurrence naturally). For the broken plot script, diagnose why it's blank — check for `plt.show()` before `savefig()` clearing the figure, or a non-interactive backend not being set — fix the script in place and rerun it rather than rewriting the plotting from scratch. Round the three floats to 4 decimals; keep the user_id as int.