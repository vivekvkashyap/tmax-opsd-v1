First transcribe `/app/voice_memo.wav` (extract/convert with `ffmpeg` if needed, then run it through an available speech-to-text tool) and write down the exact rule it describes before coding — do not assume you know the anomaly criterion in advance. Once you have the rule in hand, implement `/home/user/log_analyzer.py` to read lines from `sys.stdin` one at a time (don't buffer the whole input — it will be tested with millions of lines), decode/handle them as UTF-8 robustly, and apply the transcribed rule using a proper Unicode-aware library for any counting it calls for (e.g. Python's `emoji` package, since ad hoc character-range checks miss many emoji code points and multi-codepoint sequences) while carrying state (like a previous-line value) across iterations. Print qualifying lines to stdout exactly as received, unmodified, and make the script executable with a `#!/usr/bin/env python3` shebang plus `chmod +x`. Test it against a handful of hand-crafted lines with known emoji counts to confirm your interpretation of the audio rule matches before considering it done, since a misheard or misinterpreted detail (e.g. the threshold or which direction of difference counts) will silently fail the whole grading run.
