Implement this as a straightforward Kahn's-algorithm topological sort with a deterministic tie-break, not a recursive DFS (DFS ordering makes the "alphabetical among ready services" rule awkward to enforce). Parse mesh.dsl into a dependency map (service -> list of things it depends on, ignoring "NONE" as a real node) and derive each service's remaining in-degree in the *reversed* sense: a service is "ready" once all the services it depends on have already been assigned. Maintain a ready set/slice, and at each step sort the currently-ready services alphabetically before assigning them the next sequential ports (8000, 8001, ...) in that sorted order — this is the detail a naive queue-based topo sort gets wrong, since a plain FIFO queue won't reorder same-round-ready nodes alphabetically. After assigning a batch, remove those nodes and recompute which newly-ready nodes appear (their remaining dependencies are now all resolved), sort that new batch alphabetically, and continue until all services are assigned. Build the output map service->"http://localhost:<port>" and marshal to proxy_config.json. Test your Go program against the example DSL in the task text to confirm your ordering logic reproduces its example output before trusting it on the actual /home/user/mesh.dsl contents (do not hardcode the example — the real file differs).
