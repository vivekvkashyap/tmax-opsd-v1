Build this in layers and test each independently before wiring them together. For frame analysis, shell out to `ffmpeg` from Go (e.g. extract frames as raw/PNG images to a temp dir, or pipe raw RGB frames via `-f rawvideo` and read them in Go) and for each frame compute the average R, G, B across all pixels, counting it as a "CRITICAL INODE" frame only when the average satisfies R>200, G<50, B<50 simultaneously — verify your counting logic against a couple of frames you inspect manually before trusting the total. For the HTTP server, read `OBS_PORT`/`OBS_TOKEN` from the environment at startup, implement `/health` unconditionally returning 200, and implement `/metrics` checking the exact `Authorization: Bearer <OBS_TOKEN>` header before responding with the Prometheus-style `critical_inode_frames_total <count>` line — reject with 401/403 when the header is missing or wrong. Wrap execution in `start.sh`: export the two env vars, `go build` the exporter, then loop `while true; do ./exporter_binary; done` so it restarts on crash, redirecting all output to the log file, and launch that script backgrounded. Finally add the logrotate config with the four required directives and register an hourly cron entry (`crontab -l`/`crontab -e` style, or writing to the user's crontab) invoking `logrotate` on that config file.
