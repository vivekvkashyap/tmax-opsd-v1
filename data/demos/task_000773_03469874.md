Break this into three independent stages you can verify separately before wiring them together. First, run OCR on the image (`tesseract /app/topology.png stdout` or similar) and inspect the raw text output carefully — OCR often mangles arrows/colons, so manually sanity-check each parsed edge line (`source -> dest : weight`) against what you'd expect before trusting it in code. Second, load `/app/backups.json` for per-node sizes. Third, in your Rust project implement a standard weighted shortest-path algorithm (Dijkstra, since weights look non-negative) computed independently from every node as source, since you need shortest_path_distance(X,Y) for all pairs, not just one root — a naive attempt only computing paths from one node and reusing them for all X is a common bug. For each X, sum size_gb(X) plus size_gb(Y)/distance(X,Y) over all reachable Y (skip unreachable Y, contributing 0 — don't crash on infinite distance or self-loop division). Use f64 throughout. Sort descending by score, take the top 5, and serialize to `/home/user/top_backups.json` matching the exact key names and array-of-objects shape required. Double check node ordering and tie-breaking against the description before finalizing.