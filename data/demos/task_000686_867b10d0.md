Treat this as three independent deliverables and tackle them in order. Phase 1 (reverse engineering): use `strings`/`objdump -d`/a debugger on `/app/bin/auth_module` to locate the hardcoded MD5 hash literal being compared via strcmp, then crack it against a common wordlist (e.g. `rockyou.txt` if present, or common weak-password lists via `hashcat`/`john`, or simply hash candidate weak passwords with `md5sum` and compare) — write the recovered plaintext to `/app/config/admin_pass.txt`. Phase 2 (TLS + proxy): generate a self-signed cert for `rotation.local` with `openssl req -x509 -newkey rsa:2048 -nodes -keyout /app/certs/key.pem -out /app/certs/cert.pem -days 365 -subj "/CN=rotation.local"`, then edit `/app/nginx/nginx.conf` to `listen 8443 ssl;` with `ssl_certificate`/`ssl_certificate_key` pointing at those files and `proxy_pass http://127.0.0.1:9000;`, then run `/app/start.sh` and confirm both processes are up. Phase 3 (sanitizer): write `/home/user/sanitizer.c` that rejects any filename containing `../`, `..\`, URL-encoded traversal sequences (decode percent-encoding before checking), null bytes, or a leading `/`, and only accepts filenames matching a strict alphanumeric+dash+underscore+single-extension-dot pattern; test it yourself against tricky obfuscated payloads (double-encoding, mixed slashes) before considering it done, since the hidden evil corpus will specifically probe those edge cases.
