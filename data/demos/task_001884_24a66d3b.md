Two independent fixes, handle in this order so the benchmark script can call the fixed plotter at the end:
1. `plot_artifacts.py` fails in a headless environment because it uses an interactive backend (`TkAgg`) and calls `plt.show()` instead of saving to disk. Fix by setting a non-interactive backend (`matplotlib.use('Agg')`) before importing `pyplot`, and replacing `plt.show()` with `plt.savefig('artifact_plot.png')` (plus `plt.close()`) — leave the data-loading/plotting logic itself untouched.
2. Write `run_experiments.sh`: create the `logs/` directory, loop over batch sizes 1 2 4 8 16, run `python3 generate_embeddings.py --batch_size <n>` redirecting stdout to `logs/run_<n>.log` for each, then parse the `Latency: <X> ms` line out of each log (e.g. with `grep`/`awk`/`sed`) to build `latency_summary.csv` with header `batch_size,latency_ms` sorted numerically by batch size, and finally invoke `python3 plot_artifacts.py`. Make the script executable and actually run it end-to-end to produce both `latency_summary.csv` and a valid non-empty `artifact_plot.png`, then double check the CSV values against what each log actually printed rather than assuming the formula.
