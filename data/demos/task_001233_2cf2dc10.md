Fix each layer independently and verify before moving to the next. First recover the database: run `sqlite3 /home/user/profiler/logs.db "PRAGMA wal_checkpoint(FULL); PRAGMA journal_mode=DELETE;"` and confirm `logs.db-wal` is gone afterward. Then fix `process.py`'s two bugs, don't conflate them: the `multiprocessing.Value('i', 0)` uses a 32-bit signed int typecode — change both `total_metric` and `processed_count` to `'q'` (64-bit signed) since aggregated totals exceed the 32-bit range. Separately, `total_metric.value += local_metric` from multiple processes is a non-atomic read-modify-write race — wrap each update in `with total_metric.get_lock(): total_metric.value += local_metric` (and same for `processed_count`), not a single shared lock guessed ad hoc; `multiprocessing.Value` already provides its own lock via `get_lock()`, use that rather than inventing a separate `Lock()`. For tracing, inside the per-row loop in `process_chunk`, when `row_id % 100 == 0`, open `/home/user/profiler/trace.log` in append mode and write the id plus newline — simplicity here (open/close per write) is fine and matches the spec exactly, don't over-engineer with buffered handles across processes since that risks interleaved/corrupted writes. Run `process.py` to completion and check `summary.json`, `trace.log` (sorted content, 10 lines), and that `process.py` contains `get_lock()` as required by verification.