For Part 1, don't guess the transcript — extract/convert the audio (ffmpeg can pull raw PCM from the WAV, or feed the file directly) into whatever your speech-to-text tool/library needs, run it, and write the recognized text verbatim to `/home/user/transcript.txt`; verification is fuzzy-matched so focus on getting the words right rather than exact casing/punctuation. For Part 2, write the C indexer with correct binary parsing and locking discipline: open the file, call `fcntl(fd, F_SETLK, &lock)` with `l_type = F_RDLCK` before any read and exit(1) on failure to open or lock; then read the 44-byte canonical RIFF/WAVE header as raw bytes (not via buffered text I/O) — ChunkSize is a little-endian uint32 at byte offset 4, NumChannels is a little-endian uint16 at offset 22, SampleRate is a little-endian uint32 at offset 24. Total file size to print is ChunkSize + 8. Since your binary is fuzz-tested byte-for-byte against a hidden oracle across many randomized headers, be exact about the print format `SIZE: <n> | CH: <n> | SR: <n>\n` and about unlocking (`F_UNLCK`) and closing before a clean `exit(0)`.