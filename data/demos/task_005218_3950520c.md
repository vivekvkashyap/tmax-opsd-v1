Use `cargo new audit_tool` and add the `rusqlite` crate (with the `bundled` feature so it doesn't need a system SQLite) plus `serde`/`serde_json` for output. Write a single SQL query rather than post-processing in Rust: a CTE (or subquery) that does `SELECT emp_id, SUM(amount) AS total_spent FROM expenses GROUP BY emp_id`, joined to `employees` to get `name` and `manager_id`, wrapped in an outer query that applies `RANK() OVER (PARTITION BY manager_id ORDER BY total_spent DESC) AS team_rank`, then filter in an outer-outer query (or `WHERE` after a subquery, since window functions can't be filtered directly in the same SELECT) for `team_rank <= 2 AND total_spent > 10000`. Only include employees who actually have a manager (skip the CEO row, since the report is about teams under a manager) — check whether `manager_id IS NOT NULL` should gate inclusion. Order the final result set by `manager_id ASC, total_spent DESC` directly in SQL rather than sorting in Rust, to avoid mismatches. Serialize each row to the exact JSON schema given (field order doesn't matter but types do — `total_spent` must serialize as a float like `15000.0`, not an integer). Run the compiled binary so `suspicious_report.json` is actually written to disk.