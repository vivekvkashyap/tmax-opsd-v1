Start by recovering the leaked token sequence from the audio itself rather than assuming a phrase — run the pre-compiled whisper binary against the wav file, e.g. `/opt/whisper/main -m /opt/whisper/models/ggml-base.en.bin -f /app/audio/leakage_report.wav`, read the transcript output, and pick out the specific 3-word sequence it says was leaked (listen for phrasing like "the exact token sequence: <word> <word> <word>"). Use that recovered sequence (lowercased for comparison) as the pattern your C++ program searches for. In artifact_filter.cpp, parse the CSV properly (header + experiment_id,timestamp,train_features, being careful the train_features field itself is space-separated so don't further split on commas within it), tokenize train_features on whitespace, lowercase each token, and slide a 3-token window across the sequence checking for consecutive equality against your recovered phrase, across every row of the file. If any row matches, print "STATUS: LEAKED" and exit 1; otherwise print "STATUS: CLEAN" and exit 0. Compile to /home/user/artifact_filter and validate it against every file in both /app/corpora/clean and /app/corpora/evil, fixing any misses before considering the task done.
