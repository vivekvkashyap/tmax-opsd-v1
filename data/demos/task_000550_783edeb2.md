Implement and unit-test each phase of the C program independently against hand-computed examples before wiring them together. For token validation, split the `<auth_token>` on `:` into user_id/timestamp/signature, sum the raw ASCII byte values of the user_id string and separately of the timestamp string (note the timestamp is compared as its literal characters, not its numeric value — sum digit characters' ASCII codes), add the two sums, mod 256, and format as two uppercase hex digits (zero-padded, e.g. `sprintf(buf, "%02X", val)`); compare case-sensitively against the provided signature. For hex decoding, validate the input length is even and every character is a valid hex digit before decoding pairs into bytes — reject with the DECODE_FAIL path on either failure. For path traversal, scan the decoded filename bytes for `..`, `/`, or `\` using simple substring/byte scans (not just checking the start of the string). Use `fopen(path, "ab")` for audit.log so entries append across invocations, and format each log line with the exact bracketed prefixes and trailing `\n` given in the spec — mismatched punctuation or missing newlines will fail exact-match verification. Test all four documented cases manually via the shell, checking both exit codes and `cat audit.log`, before considering it done.