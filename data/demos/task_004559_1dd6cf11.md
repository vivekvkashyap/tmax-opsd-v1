For the video component: use `ffmpeg -i /app/experiment_run.mp4 frame_%d.pgm` (or similar) to dump raw P5 PGM frames, then write a C program that opens each PGM, skips the P5 header (magic number, width, height, maxval — watch for comment lines starting with `#`), reads the binary pixel bytes, sums them, and divides by pixel count using integer division (truncation, not rounding). Loop over frames in chronological order (1-indexed) and emit `frame_number,avg_intensity` rows to `/home/user/video_metrics.csv` with the exact header line first. Because ffmpeg/libx264 encoding can shift raw gray values slightly, don't hardcode expected values — just compute faithfully from what ffmpeg actually extracts.

For the validator: write `/home/user/meta_validator.c` to open the given path (fail if it can't), parse each `key=value` line by splitting on the first `=`, and track which of the three required keys (`learning_rate`, `batch_size`, `cv_folds`) were seen. Reject on: unknown key, duplicate/missing key, non-numeric value, learning_rate outside (0.0, 1.0], batch_size not in {8,16,32,64,128,256}, or cv_folds outside [2,10]. Use `strtod`/`strtol` with endpointer checks (not `atoi`/`atof`) to catch malformed numbers, and only succeed (exit 0) if every rule passes. Compile with `gcc -o /home/user/meta_validator /home/user/meta_validator.c` and test it manually against a few valid/invalid configs you construct yourself before relying on it.