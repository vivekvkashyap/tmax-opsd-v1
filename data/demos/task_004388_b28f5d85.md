Work in dependency order. First fix the vendored library build: inspect `/app/vendor/libarchive_custom/CMakeLists.txt` and source files for the breakage — a `target_link_libraries` referencing a misspelled/nonexistent target name should be corrected to the actual bundled zlib target, and any source file using exceptions or `std::runtime_error`-style types needs its missing standard header included; then `cmake`/`make`/`make install` it into `/app/vendor/install`. Second, read `/home/user/repo_config.json` for `target_dir` and `expected_magic`, and study `/home/user/reference_scanner.py` closely since your C++ output must match its exact status semantics (VALID/BAD_HEADER/CORRUPT_ARCHIVE) file-for-file. Write `artifact_scanner.cpp` using `std::filesystem` for traversal, memory-mapping (`mmap`) each `.art` file to check the 16-byte header's first 4 bytes against `expected_magic`, and feed the bytes from offset 16 onward into the fixed archive library to validate ZIP integrity. To hit the required 3x+ speedup over the single-threaded Python reference, parallelize file processing across a thread pool (e.g. `std::thread`/`std::async` with hardware_concurrency), and benchmark iteratively — measure wall-clock for both tools yourself before assuming you've met the threshold.
