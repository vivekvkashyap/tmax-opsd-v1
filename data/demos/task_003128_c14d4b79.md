Do the pieces in dependency order. First, recover the passphrase: transcribe `/app/voicemail.wav` with a speech-to-text tool (install `openai-whisper` or use `SpeechRecognition`/`ffmpeg` as needed), then normalize the transcript by lowercasing it and stripping all punctuation — this normalized string is the exact auth token your server must compare against the `X-Vault-Auth` header, so derive it programmatically rather than hardcoding a guess. Second, build the filesystem layout: create `/home/user/vault/data` with `chunk1.dat`/`chunk2.dat`/`chunk3.dat` at exactly 1024/2048/4096 bytes (e.g. `head -c <n> /dev/urandom > file`), create the `active_data` symlink pointing at `data` (not an absolute resolved path elsewhere), then chmod the vault directory to `0700` and the ACL file to `0400` — set permissions last, after all writes, since `0400`/`0700` will block further edits. Third, write the app-level ACL line into `app_users.conf` exactly as specified before locking it down. Finally, implement the HTTP server (Python's `http.server` or Node's `http` module both work) so `GET /metrics` requires the exact-match header, returns `401` on mismatch/missing header, and otherwise walks the symlinked directory to sum file sizes and returns the specified JSON shape; launch it detached (e.g. with `nohup ... &` or `setsid`) so it survives your shell exiting, and confirm it's listening on 127.0.0.1:8123 with a local curl before finishing.
