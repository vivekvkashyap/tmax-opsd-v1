Start by extracting the hidden parameters from the whiteboard image with OCR: run `tesseract /app/config.png stdout` (try preprocessing with ImageMagick if the raw OCR is noisy — upscaling, thresholding) and read off the W, D, and target-script values from the recognized text rather than guessing; verify what you recovered looks sane (a window size, a float distance threshold, a script name) before hardcoding it into the Rust source. Structure the Cargo project at `/home/user/bot_detector` with `strsim` for Levenshtein distance. Parse the JSONL file line by line, maintaining a `VecDeque` (or Vec used as a ring buffer) of the last W message texts. For each new message: if the deque already has W entries, compute Levenshtein distance from the new text to each of the W buffered texts, average them, and check both conditions — average distance strictly less than the OCR'd D, and the new text containing at least one character in the OCR'd target Unicode script (use Rust `char` Unicode range checks against the correct code-point range for whatever script name you actually recovered from the image — look up its standard Unicode block). If both hold, print/exit immediately with `std::process::exit(1)`. Otherwise push the message into the window (evicting the oldest if over W) and continue; if the file ends with no anomaly, exit 0. Build with `cargo build --release` so the binary lands at `target/release/bot_detector`.
