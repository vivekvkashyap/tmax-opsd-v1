Treat `/app/csv_processor_oracle` purely as a black-box: feed it a handful of hand-crafted CSV lines one at a time via stdin (varying name lengths, email domains, phone numbers, including short/edge-case values) and diff the outputs to infer the transformation rules — don't try to disassemble it first, black-box testing is faster and safer. Specifically probe: does it reorder fields (compare output field order to input), how does it mask the email (try emails with 1-char and 2-char prefixes to find the exact prefix length kept), how does it mask the phone (try different lengths to find how many trailing digits survive), and whether a hash appears (its length/charset — a 64-char hex string strongly suggests SHA-256, and lowercase hex with no dashes narrows the exact digest function). Once the exact output grammar is nailed down from enough probes, reproduce it faithfully in C++, being byte-exact about field order, punctuation, separators, and hash casing/format (uppercase vs lowercase hex) since the grader does byte-for-byte comparison. Link OpenSSL's crypto lib if you use SHA-256 (`-lcrypto`), compile exactly as instructed, and fuzz-test your own binary against the oracle on many random inputs before finishing to catch edge cases like short strings.