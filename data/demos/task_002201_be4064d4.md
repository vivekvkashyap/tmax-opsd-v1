This is a two-stage numerical task: fix a summation algorithm in C, then do linear algebra in Python. First, open `/home/user/data_gen.c` and find the `compute_feature` function's integration loop that does `sum += y * dt;` — this naive running sum loses precision in `float` because small increments get swallowed by a large accumulator. Replace it with Kahan summation using a compensation variable `c`, following exactly this update order each iteration: `y_val = (y*dt) - c; t = sum + y_val; c = (t - sum) - y_val; sum = t;` — keep everything as `float`, not `double`, since the task is about fixing the reduction algorithm, not the precision type. Compile with `gcc` and run the binary to regenerate `/home/user/matrix.txt`. Then, in Python (numpy/scipy), read that matrix and compute its SVD (`numpy.linalg.svd`), take the largest singular value `S[0]`, format it to exactly 4 decimal places, and write only that string to `/home/user/result.txt`. Match the exact update-order and float32 semantics precisely, since bit-level drift in the summation will change the singular value your grader compares against.
