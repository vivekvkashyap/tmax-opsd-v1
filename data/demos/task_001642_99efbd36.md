Recover the config before writing any classification logic, since the archive target paths come from the video, not from guessing. Use `ffprobe -v quiet -show_entries format_tags=comment -of json /app/project_screencast.mp4` (or similar), pull the `comment` tag with `jq`, base64-decode it, and save the decoded `TYPE:ARCHIVE_PATH` lines to `/home/user/packaging.conf` — parse this file at runtime rather than hardcoding paths in your script. For file identification, don't trust extensions: read each file's actual bytes. ELF files start with the magic `\x7fELF`; SQLite WAL files start with magic bytes `0x37 0x7f 0x06 0x82` or `...0x83`; GCODE files are text containing both the substring `M109` and at least one line matching a `G1` movement command (a file with only `G1` or only `M109` should be excluded — check both conditions, not just one). Recurse into subdirectories of `/home/user/messy_project/`. When archiving, `cd` into each file's directory or use `tar --transform`/`zip -j` so files are stored at the archive root without their original path prefix. Finally serve the archives' containing directory with a simple HTTP server backgrounded on 127.0.0.1:8080, and verify with curl that each archive path from packaging.conf actually downloads before finishing.
