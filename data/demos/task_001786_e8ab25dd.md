Split the work into the two independent phases. For certs, write `/home/user/gen_certs.sh` using `openssl req -x509 -newkey rsa:2048 -nodes -days 30 -subj "/CN=policy.local" -keyout .../server.key -out .../server.crt`, `mkdir -p` the certs dir first, then execute it. For the C auditor, handle each requirement with the right tool rather than hand-rolling everything: (1) redaction — scan the log text for exactly-16-consecutive-digit runs with a manual digit-counting scan (careful not to match 15 or 17+ digit runs) and replace each matched run with 16 `*` characters, writing the rest of the line unchanged; (2) SUID scan — `opendir`/`readdir` non-recursively over `jail/bin`, `stat` each entry, and check `st_mode & S_ISUID`; (3) ports — read each line of `open_ports.txt`, parse as int, and flag anything not equal to 22 or 443; (4) build the JSON by hand with correct field order/indentation, sorting `suid_files` alphabetically and `unauthorized_ports` numerically ascending before printing. Compile with `gcc -o /home/user/auditor auditor.c` and run it once to produce both output files.
