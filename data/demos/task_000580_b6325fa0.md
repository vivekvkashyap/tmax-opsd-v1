Break this into independently verifiable pieces and test each before moving on. (1) Launch `socat TCP-LISTEN:8080,fork,reuseaddr TCP:127.0.0.1:5000 &`, capture `$!` right after backgrounding it, and write that exact PID to `/home/user/proxy.pid` (verify with `ps -p $(cat proxy.pid)` that it's really socat). (2) Create `/home/user/logs/` and write a logrotate stanza scoped to `/home/user/logs/telemetry.log` including `daily`, `rotate 5`, `compress`, and `missingok` directives — these keywords are checked literally, so use them verbatim. (3) `git init --bare /home/user/telemetry.git`, clone to `/home/user/workspace`, then write `hooks/post-receive` (executable, `chmod +x`) that on push to master does a `git --work-tree=/home/user/deploy --git-dir=/home/user/telemetry.git checkout -f master`, runs `python3 /home/user/deploy/analyze.py /app/telemetry.mp4`, and appends its stdout to `/home/user/logs/telemetry.log` (create `/home/user/deploy` first). (4) For `analyze.py`, read the video with `cv2.VideoCapture`, slice each frame's top-left 50x50 region, and use vectorized numpy (`frame[:50,:50,2].mean()` etc. per BGR channel) rather than pixel loops — avoid per-pixel Python iteration since the 1.5s budget requires full vectorization; print just the integer count of matching frames. Finally `git add`, commit, and `git push` from `/home/user/workspace` to trigger the hook and produce the log entry.
