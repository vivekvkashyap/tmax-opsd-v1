Since there's no root, do everything user-space under `/home/user`, and build/verify each layer (binary, log rotation, cron, CI script) independently.

1. Write `/home/user/src/operator.c` to open/read `/home/user/manifests` (e.g. `opendir`/`readdir`), filter entries whose name ends in `.yaml`, and for each append a line `[SUCCESS] Deployed <filename>\n` to `/home/user/logs/operator.log` (open in append mode, create the file/dirs as needed). Compile with `gcc /home/user/src/operator.c -o /home/user/bin/operator`.
2. Write `/home/user/logrotate.conf` targeting `/home/user/logs/operator.log` with `daily`, `rotate 3`, `compress`, and `create` directives inside the block for that path (standard logrotate stanza syntax).
3. Write `/home/user/operator.cron` with a line scheduling `/home/user/bin/operator` every 5 minutes (`*/5 * * * * /home/user/bin/operator`), then actually apply it via `crontab /home/user/operator.cron` so `crontab -l` reflects it.
4. Write an executable `/home/user/build_and_test.sh` that compiles the operator, creates a dummy `/home/user/manifests/ci-test.yaml`, runs the operator, greps the log for the expected success line for that file, and prints exactly `CI PASSED`/exit 0 or `CI FAILED`/exit 1 accordingly. Test each piece manually (run the binary, check the log, run the script) before considering it done.
