This has two independent halves — do the nginx fix first since it's quick, then focus most effort on the Rust detector. For Phase 1, edit `/app/nginx.conf` adding `location /api/auth { proxy_pass http://127.0.0.1:8081; }` and `location /api/audit { proxy_pass http://127.0.0.1:8082; }` blocks with `proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;`, then restart via `/app/start.sh` and confirm with curl against both routes. For Phase 2, first recover the HMAC secret by running `strings /app/bin/auth_svc | grep -i secret` (or similar; look for a plausible key-shaped string in the binary's strings output) rather than guessing it — this recovered value is what your Rust JWT validator must use. Build the detector with `cargo init` in `/home/user/detector`, pull in a JWT/HMAC crate (or implement HS256 manually with `hmac`+`sha2`), and for each corpus file: extract the bearer token, reject as EVIL if the header alg is `none`, reject as EVIL if HMAC-SHA256 verification against the recovered secret fails, and reject as EVIL if decoded payload claims (`username`/`role`) contain injection markers like `' OR 1=1`, `--`, `<script>`, `javascript:` via substring checks — otherwise print CLEAN. Test against both known corpora before considering it done, since scoring requires 100% accuracy on both.
