Tackle this in the given order: sanitizer correctness first (offline, against the corpora), then service wiring second — don't try to debug both at once. For the C++ sanitizer, parse the ELF64 header/section header table manually (e_shoff, e_shnum, e_shentsize, plus the section-header-string-table index) to locate a section literally named `.cnc_gcode`; if found, read its raw bytes as text and scan line by line for `G0`/`G1` commands, extracting the Z parameter (regex or manual token scan for `Z` followed by a signed number) and flagging evil if any parsed Z value is negative. Recurse the given directory for `*.elf` files, exit 1 if any evil file is found else exit 0. Validate against `/home/user/corpora/clean` and `/home/user/corpora/evil` directly via the CLI before touching the Flask app, since a false positive/negative here cascades into every downstream failure. Only once that's 100% correct, edit `/home/user/services/api/app.py` to shell out to your sanitizer binary against the staging directory after upload and before the Redis write, returning HTTP 403 and deleting the file on exit code 1, then restart Flask/Nginx and confirm one clean and one evil upload behave correctly end-to-end through port 8080.
