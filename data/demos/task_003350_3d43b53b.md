Treat this as a "last event wins" reduction problem solvable purely with coreutils/awk. First pass: read the CSV (skip header) and use `awk -F, '{mgr[$3]=$4; typ[$3]=$5}'` style logic to keep overwriting a per-employee associative array with each row's manager and event_type as you stream through in chronological order — since rows are already time-ordered, the last write for a given employee naturally becomes the current state. After the single pass, employees whose final event_type is DELETE should be excluded/treated as having no valid manager. Build the current employee->manager map only from employees whose latest event was UPDATE. Then do the upward walk: starting at EMP_404, repeatedly look up the map for the current node's manager and append it, stopping once you hit the literal string CEO (CEO itself is not a key needing further lookup). Use a simple bash while-loop calling out to awk (or a single awk script with an array) to perform the chain lookup, then join the collected names with commas via `paste -sd,` or manual string concatenation, writing the result with a trailing newline suppressed as specified to /home/user/management_chain.txt. Watch out for reprocessing DELETE rows as if they set a manager, and for off-by-one errors in stopping exactly at CEO.
