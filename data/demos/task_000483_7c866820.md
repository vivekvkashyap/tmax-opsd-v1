Structure the pandas pipeline as clearly separated stages so each requirement can be checked independently: (1) decode `device_hex` per row with a helper that does `bytes.fromhex(h).decode('utf-8')` wrapped in try/except, falling back to `.decode('latin-1')` on `UnicodeDecodeError` — latin-1 never raises, so it's a safe fallback of last resort; (2) parse `timestamp` to UTC datetimes, then drop exact duplicate `(timestamp, device)` pairs with `drop_duplicates(subset=['timestamp','device'], keep='first')`; (3) do resampling *per device*: group by decoded device name, set the datetime as index, and call `.resample('1H').ffill()` (or equivalent asfreq+ffill) separately for each group, then concatenate results back together — a single global resample across all devices mixed together will corrupt the per-device time series, so this per-group split is the step naive attempts skip; (4) after resampling, flag `value > 100.0` as anomalies; (5) write both output CSVs with exactly the columns `timestamp,device,value`, sorted by device then timestamp. Double check the forward-fill only fills gaps *within* a device's own series, never carrying values across different devices.