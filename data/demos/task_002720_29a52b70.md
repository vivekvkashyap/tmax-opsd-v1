Write `/home/user/organizer.cpp` to parse `/home/user/backup_history.log` by splitting on blank lines into records, then within each record extracting the `Version:` and `Archive:` fields (simple line-prefix parsing, e.g. `std::getline` + `substr`/`find(": ")`). For each record, test archive integrity using `std::system("unzip -t <path> > /dev/null 2>&1")` and check the returned exit status (remember `std::system`'s return value must be interpreted with `WEXITSTATUS` on POSIX, since raw return isn't the shell's exit code) — only treat a clean/zero exit as valid. Create `/home/user/valid_backups/` first (via `std::system("mkdir -p ...")` or `mkdir` syscall) before linking. For valid archives, create a *symbolic* link (not a hard link or copy) named `<version>_link.zip` pointing at the original absolute archive path, using `std::system("ln -s <archive> <link>")` or the `symlink()` syscall — do not create a link at all for the corrupted archive. Compile with `g++ -std=c++17 -o /home/user/organizer /home/user/organizer.cpp` and run it to produce the final filesystem state.
