Build this as a short pipeline of well-defined stages inside your bash script (mixing `sort`, `awk`, and a small inline `python3 -` snippet is fine, since bash alone makes forward-fill and rounding awkward). Stage 1: sort `/home/user/etl_data.csv` (excluding header) by timestamp — use `sort -t, -k1,1` on the data rows only. Stage 2: deduplicate by `record_id`, keeping the first chronological occurrence — a stable pass that tracks "seen" record_ids (an awk associative array works well: `!seen[$2]++`) does this cleanly on the already-sorted stream. Stage 3: forward-fill empty `sensor_value` fields by carrying forward the last non-empty value seen in order — again a simple sequential awk/python pass with a "last valid" variable, not a lookup after the fact. Stage 4: compute the count of values > 100.0 (anomalies) and the mean of all final values, rounding the mean to exactly 2 decimals (printf "%.2f" or Python round). Stage 5: use `sed` (or a small python replace) to substitute the literal placeholders `{{AVG_VALUE}}` and `{{ANOMALY_COUNT}}` in `/home/user/report_template.txt`, writing the result to `/home/user/final_report.txt`. Make the script executable and run it end-to-end to confirm the output file is produced correctly.
