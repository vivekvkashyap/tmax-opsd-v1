This is a multi-stage extraction-then-computation pipeline — validate each stage before moving to the next. Extract frames from `/app/dashboard_leak.mp4` with `ffmpeg` (one frame is enough per distinct on-screen text event, e.g. sample at the video's frame rate), then OCR each frame (install `tesseract` if unavailable) to recover lines of the form `ACCESS: [EmpID] -> [RoomID]` — inspect the OCR output for garbling and clean it up (e.g. via regex matching the known `ACCESS: X -> Y` pattern) rather than trusting raw OCR text blindly. Query `employees` and `rooms` from the SQLite DB to get each employee's `ClearanceLevel` and each room's `RequiredClearance`. For each employee, iterate their *distinct* rooms accessed (dedupe repeated accesses to the same room) and score 1.0 if clearance < required, else 0.5 — sum per employee, starting everyone (including employees who never appear in the video log) at 0. Write the CSV with header `EmpID,RiskScore`, one row per employee in the `employees` table, sorted by EmpID ascending. Since grading is by MAE <= 0.15 rather than exact match, double check your OCR-extracted access events are complete and not missing any lines — a single dropped access event event will materially shift a score.