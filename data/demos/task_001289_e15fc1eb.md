Work this like a real audit: first read `auth_server.c` fully to understand the token format `HEX(payload).signature`, how the payload bytes are derived (an XOR-style stream cipher against some key — locate exactly how the key is derived/stored in the source, since it's a fixed value in the code you can read directly rather than needing to reverse-engineer a cert), and how `verify_sig` decides pass/fail. Look specifically for a bypass pattern reminiscent of JWT's "alg=none" vulnerability — a special-cased signature string (e.g. a literal like `"NONE"`) that short-circuits to a valid result regardless of the actual signature. Once you have the key and the encryption scheme, write a small C (or Python) helper that takes your target plaintext `user=auditor&role=admin`, XORs it byte-by-byte with the key (repeating the key as needed), hex-encodes the result, and appends the discovered bypass signature string after a `.`. Save that exact forged token string to `/home/user/workspace/forged_token.txt`, then run `./auth_check <forged_token>` and capture its stdout into `/home/user/workspace/flag.txt`. Verify by re-running the binary yourself and confirming the flag line appears before considering it done.