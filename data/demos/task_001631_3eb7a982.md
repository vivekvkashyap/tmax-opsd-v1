Write a single Python script that does the full pipeline in order, since later steps depend on exact earlier values. Parse the PDB with simple fixed-column or whitespace-aware slicing, filtering lines where the record is `ATOM` and the atom-name field equals exactly `CA` (watch for column alignment — use the PDB column spec or split carefully, not a naive substring match, since other atoms like `CA` in different residues must still match on name field only). Compute the center as the mean of X/Y/Z, then compute each D_i as the Euclidean distance from that center. For the mesh refinement, implement it literally as specified: keep the mesh sorted, and in each of the 4 iterations compute ALL midpoints to insert from the CURRENT mesh state before inserting any of them (don't insert-and-immediately-recurse within the same iteration, that changes the point count). Define g(r) with numpy vectorized sum of exp(-(r-D_i)^2/2) over all D_i. For root-finding use `scipy.optimize.brentq` on `lambda r: g(r) - 2.0` over `[R_max/2, R_max]`; verify the function actually changes sign on that interval before calling brentq. Round final scalars to 4 decimals only at output time, not during intermediate computation, to avoid compounding rounding error.
