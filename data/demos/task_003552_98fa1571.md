Start by inspecting the schema directly (e.g. `sqlite3 research_data.db ".schema"` and browsing table contents) rather than trusting the task description's table names — the description explicitly warns of a denormalized "cache" table with stale, impossible data mixed in among the real tables; identify it by its shape (e.g. it stores pre-aggregated author-level scores directly rather than normalized paper/author/citation relationships) and exclude it entirely from your computation. Build the citation graph from the true relational tables only (authors, papers, and a citations/references join table), compute each paper's in-degree by counting incoming citation edges, then join papers to authors to sum in-degrees per author for the impact score. Sort authors by descending impact score with alphabetical tie-breaking, take the top 3, and write them to /home/user/top_authors.json in the exact structure implied by /home/user/schema.json — read that schema file first and match its field names and nesting precisely (e.g. a top-level key wrapping an array of {name, impact_score} objects) rather than guessing the output shape, then validate your JSON against it programmatically (e.g. with the `jsonschema` package) before finishing.
