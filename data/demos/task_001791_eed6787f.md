Structure this as three independent, verifiable pieces. First create `/home/user/network_tools/check_conn.py`, make it executable (`chmod +x`, with a `#!/usr/bin/env python3` shebang), and symlink `/home/user/active_tool -> /home/user/network_tools/check_conn.py` using `ln -s` with the target as an absolute path (verify with `readlink`). Second, write the script so it attempts an HTTP GET to `http://127.0.0.1:8080` wrapped in a try/except catching connection errors and timeouts (use `urllib.request` or `requests` with a short timeout), then append one line to `/home/user/conn.log`: get the current time in the `Asia/Tokyo` zone (use `zoneinfo.ZoneInfo("Asia/Tokyo")` or `pytz`, not just a manual UTC+9 offset, to be DST/format safe), format it exactly as `[YYYY-MM-DD HH:MM:SS JST]`, then append ` OK` if status==200 else ` FAIL`. Test both branches locally: run once with nothing listening on 8080 (expect FAIL) and once after starting `python3 -m http.server 8080 --bind 127.0.0.1 &` (expect OK). Third, install a crontab with exactly one line scheduling `/home/user/active_tool` every 5 minutes (`*/5 * * * * /home/user/active_tool`) via `crontab -l` piped/appended and `crontab -` — don't leave any default/example cron entries in place.
