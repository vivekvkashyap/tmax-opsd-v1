Handle the two goals as separate, independently testable pieces. For reconfiguration, run `/app/start_services.sh`, then edit `/home/user/config.json` filling in the given connection strings (`bolt://localhost:7687` with user/password `neo4j`/`password`, and `redis://localhost:6379/0`) — do not touch the startup script itself. Restart or re-invoke the Flask app as needed so it picks up the new config, then verify with `curl http://localhost:5000/health` until you get exactly `{"status": "ok"}`; if it fails, check the Flask app's logs for connection errors rather than guessing at config syntax. For the sanitizer, write sanitizer.py to take one CLI argument and scan it case-appropriately for the disallowed Cypher keywords/patterns listed in the task (CALL, YIELD, DELETE, REMOVE, SET, MERGE, CREATE, and the `//` comment marker) — match as substrings/word patterns since payloads embed these mid-string, and print EVIL if any is found, else SAFE. Before trusting it, run it against every line of `/home/user/corpora/clean.csv` (expect all SAFE) and `/home/user/corpora/evil.csv` (expect all EVIL) in a loop, since the held-out verifier corpus will likely include similar keyword/case variants. Make sanitizer.py executable.
