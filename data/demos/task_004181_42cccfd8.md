Implement the curator as an event loop, not a one-shot pass: on startup, glob `/home/user/dropzone` for existing `*.zip` and `*.tar.gz` files and process them, then loop polling the directory (e.g. every ~0.5-1s, or use inotify/watchdog if available) for new arrivals, checking for a `SHUTDOWN` marker file each iteration. For integrity checks, use `zipfile.ZipFile(path).testzip()` (or `unzip -t`) for zips and `tarfile.open(path, 'r:gz')` opened successfully / `gzip.open` read-through for tar.gz — catching exceptions as corruption signals. For valid archives, lowercase the filename, replace spaces with underscores, prepend `verified_`; to satisfy the atomicity requirement, first copy/move the file into `/home/user/curated/` under a `.tmp` name, then use `os.rename` to the final extension-correct name (rename within the same filesystem is atomic, unlike a partial write). Invalid archives move straight to `/home/user/quarantine/` under their original name. On seeing SHUTDOWN, finish processing whatever remains in dropzone first, then delete the SHUTDOWN file and exit. Launch the script backgrounded (`&`), then run `/home/user/simulate_drops.sh`, and wait on the background PID before inspecting final directory contents.