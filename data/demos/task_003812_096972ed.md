Build this as three independently testable layers, verifying each before moving on. (1) Implement the Rust validator as an explicit state machine (enum of states: ExpectApp -> ExpectVersion -> ExpectDeps -> ExpectDepLineOrEnd -> Done) that reads lines one at a time and rejects (exit 1) on any unexpected line, wrong order, or trailing garbage after END — don't use a lenient regex-per-line-anywhere approach, since the spec requires strict sequential ordering. Parse semver strictly as three integers; for `^X.Y.Z` compute the upper bound explicitly (X+1 unless X==0, in which case bump Y instead) rather than approximating with string comparison. (2) Before touching the gateway, run your compiled binary against every file in `/home/user/corpora/clean/` and `/home/user/corpora/evil/` in a loop and confirm exit codes match expectations 100% — fix any mismatches first. (3) Only then wire up `/home/user/app/gateway/index.js`: write the request body to a temp file, invoke the verifier synchronously (e.g. `child_process.execFileSync`, catching its non-zero exit as invalid), and on success `lpush` the raw manifest text to Redis list `valid_builds` before responding 200; respond 400 on failure. Start Redis, then the gateway, and confirm it's listening on 8080 before finishing.
