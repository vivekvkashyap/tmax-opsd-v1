Break the script into clearly ordered stages and verify each before adding the next. First parse `/home/user/incoming/builds.log`: use `awk` to buffer lines between each `--- BUILD <ID> ---` marker and its terminating `STATUS:` line, emitting the ID only when the terminator is `STATUS: SUCCESS` (a state machine keyed on the marker regex and the status regex, not a fixed line-count assumption, since failure/success blocks may differ in length). For each surviving ID, `mkdir -p curated/build_<ID>` and `tar -xf incoming/archives/build_<ID>.tar -C curated/build_<ID>`. For deduplication, compute `md5sum` of every regular file under `/home/user/curated`, sort by hash, group identical hashes, and within each group `ln -f` every file after the first onto the first (so they share an inode) — implement the grouping with `awk` keyed on the hash column after sorting `find ... -type f -exec md5sum {} +` output. Determine the max successful ID numerically (`sort -n`, not lexicographic) for the `latest` symlink target (a relative name like `build_105`, created with `ln -s` from inside `curated/`). Finally run `tar --listed-incremental=/home/user/backup.snar -czf /home/user/curated_backup.tar.gz -C /home/user curated` reusing the existing `.snar` state file. Chmod the script +x and execute it once to produce final state.
