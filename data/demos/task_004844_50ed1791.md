Set up a Go module and add a SQLite driver (e.g. `github.com/mattn/go-sqlite3`, `go mod init` + `go get`). Query `assets` for all rows with `is_corrupted = 1` to get your BFS/DFS seed set. Build an adjacency map from `links` filtering strictly on `status = 'active'` — explicitly exclude `'stale'` rows in your SQL WHERE clause or in-memory filter, since including them will pull in extra unreachable nodes and fail verification. From each corrupted seed node, traverse forward along active edges (standard graph traversal, tracking visited to avoid infinite loops if cycles exist) and collect every reached node whose `type = 'storage'`. Deduplicate names across all corrupted seeds (a storage node reachable from two different corrupted assets should appear once), sort alphabetically, and write one name per line to `/home/user/compromised_backups.txt` with no extra blank lines or trailing whitespace differences from what a straightforward newline-joined sorted list would produce.
