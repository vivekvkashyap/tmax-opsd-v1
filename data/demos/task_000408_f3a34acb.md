Handle the two objectives separately. For the vendored tool, don't rewrite it from scratch — diff its behavior against expectation: run `/app/bash-archive-tool-1.0.0/archive.sh <out> <dir>` and observe the exact error (a "command not found" style failure points straight at a misspelled command name in the script); open `archive.sh`, find the misspelled archiving command, and correct it to the real `tar` invocation with its existing arguments/flags intact, then re-verify it actually produces a valid `.tar.gz` you can list with `tar -tzf`. For the classifier, write `/home/user/classifier.sh` using `grep -E` (or two greps) against the file: one pattern for a hyphen-separated run of four 4-digit groups (`[0-9]{4}-[0-9]{4}-[0-9]{4}-[0-9]{4}`), one for the exact literal marker string — if either matches anywhere in the file, print `EVIL` and `exit 1`; otherwise print `CLEAN` and `exit 0`. Validate against every file in both `/app/corpus/clean/` and `/app/corpus/evil/` individually (not just a couple) to catch edge cases like multi-line evil files or CLEAN files that contain non-dashed long digit runs that shouldn't match.
