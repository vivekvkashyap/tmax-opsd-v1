Iterate all 50 JSON files in `/home/user/logs/`, parsing the nested `metrics.training_time` and `metrics.val_loss` fields for each (don't assume flat top-level keys — inspect one file's structure first with `python3 -m json.tool` or similar to confirm the exact nesting). Compute the Pearson correlation with a standard library function (e.g. `scipy.stats.pearsonr` or `numpy.corrcoef`) rather than a hand-rolled formula, to avoid precision bugs, then round to exactly 4 decimal places and write to `/home/user/correlation.txt` with no extra formatting/quotes. For the plot, since this is a headless server, set the matplotlib backend to a non-interactive one (`matplotlib.use('Agg')`) before importing `pyplot` — doing this after import or skipping it entirely is the classic mistake that crashes headless plotting or produces a blank/broken file. Build a simple `plt.scatter(x, y)` with axis labels and save via `plt.savefig('/home/user/scatter.png')`, then verify the file exists and has a reasonable size (not near-zero bytes) afterward as your own sanity check before finishing.