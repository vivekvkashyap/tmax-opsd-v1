Ignore the `log_decoder` binary — it decodes raw logs into the plaintext your redactor consumes on stdin, so your program only needs to handle already-decoded text lines. Write `redactor.c` reading one line at a time with `fgets`/`getline`, and use POSIX `<regex.h>` with `regcomp`/`regexec` for both patterns; since POSIX ERE doesn't support non-greedy `.*?`, compile the pattern to match the *shortest* span manually (anchor on the literal `-----BEGIN PRIVATE KEY-----` and `-----END PRIVATE KEY-----`/`</script>` delimiters and find the nearest closing delimiter via `strstr` after the opening one, rather than relying on a naive greedy regex that would over-match to the last occurrence in the line). Replace each matched span with the exact literal replacement text, preserving everything before and after it unchanged. After redaction, compute `SHA256()` from `<openssl/sha.h>` over the resulting string (excluding any trailing newline), hex-encode the digest lowercase, and append ` | SHA256: <hex>\n`. Compile with `gcc -O2 redactor.c -o /home/user/redactor -lcrypto`, then pipe every file in both `/app/corpora/clean/` and `/app/corpora/evil/` through it and manually confirm the transformation is correct before finishing.
