First repair the vendored networkx: open `/app/vendored/networkx-2.8.8/networkx/algorithms/dag.py`, locate the `lexicographical_topological_sort` definition and remove/delete the bogus line injected right after it that breaks import/execution, then confirm the fix by running the function directly with `PYTHONPATH=/app/vendored/networkx-2.8.8 python3 -c "import networkx as nx; ..."` before building anything on top of it. For the Flask server, implement `/plan` to: parse the `data` JSON string, build a DiGraph from `dependencies`, and call `nx.lexicographical_topological_sort(G, key=...)` (or implement equivalent tie-breaking: among currently-ready/zero-indegree tasks, always pick alphabetically smallest) to get the full order — this must match networkx's algorithm exactly since the oracle likely relies on the same library. Then do a single linear pass batching: accumulate costs into the current batch, and only when adding the next task's cost would exceed max_cost_per_batch, close the batch and start a new one (a single task whose own cost exceeds the max still gets its own batch). Run the server as a background process, then write request_planner.py to POST `{"data": <arg>}` to `http://localhost:5000/plan` and print only the raw JSON array response, matching the oracle byte-for-byte under fuzzing across many random DAGs.