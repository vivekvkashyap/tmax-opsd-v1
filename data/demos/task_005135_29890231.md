Write `/home/user/deploy_vm.py` using `argparse` for `--version` (int) and `--vnc-port` (int). For filesystem work: `os.makedirs(f'/home/user/deployments/v{version}', exist_ok=True)`, then `os.symlink('/home/user/images/base.img', f'.../v{version}/disk.img')` — remember symlink target must be the absolute base image path, not relative. For the `current` symlink, since it may already exist from a prior run, remove any existing link/file at that path first (`os.path.lexists` + `os.remove`) before calling `os.symlink(f'/home/user/deployments/v{version}', '/home/user/deployments/current')`, otherwise `FileExistsError` will occur. Write `run.sh` with the exact two-line content (shebang + qemu command with the vnc-port substituted), then `os.chmod(path, 0o755)` or equivalent to make it executable. For logging, configure a `logging.handlers.RotatingFileHandler('/home/user/deployments/deploy.log', maxBytes=500, backupCount=2)` with formatter `'%(levelname)s - %(message)s'`, attach it to a logger set to INFO level, log the deployment message, then loop 1..20 logging the padding message each iteration — with maxBytes=500 this should force at least two rotations, producing `deploy.log`, `.log.1`, and `.log.2`; if rotation doesn't trigger, check that your padding lines are long enough and that you're actually flushing/emitting through the handler. Finally run the script with `--version 42 --vnc-port 5`.