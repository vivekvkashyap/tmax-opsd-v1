Treat this as three separable subsystems and verify each with a real request/response, not just config inspection. First trace the request path end-to-end: nginx on 8080 must proxy_pass `/api/` to the Flask backend's actual listening port (check `backend.env` for the real port Flask binds, since nginx.conf's upstream may currently point at the wrong port), Flask must read REDIS_HOST/REDIS_PORT from its env file to reach Redis on 6379, and the env file must also set LANG=en_US.UTF-8 and TZ=UTC — after editing, restart the services via `start_services.sh` and hit `curl http://localhost:8080/api/health` to confirm a 200 with redis reported connected before moving on. Second, write the timezone filter as a strict single-line validator: read the whole file, reject if it isn't exactly one line, reject any character outside `[A-Za-z0-9_/-]`, and don't rely on shell `case`/glob patterns alone — anchor a regex to the full line so injected `;`, `..`, or trailing garbage cannot slip through; test it against both benign and malicious-looking timezone strings you construct yourself. Third, in `app_fstab` use the `bind` mount option syntax exactly, and make `deploy.sh` poll/wait for Redis (e.g. loop on a ping/connect check) before restarting the API so there's no request-dropping gap.