Run `pytesseract.image_to_string` on `/app/config_schematic.png`, then parse the returned text with a regex like `r'(\w+)=(\S+)'` per line to pull out the `MAX_CONNECTIONS` and `TIMEOUT` key-value pairs rather than hardcoding — OCR output can have stray whitespace/newlines, so strip each line before matching. For the nested archive, open the outer tar with `tarfile.open(..., 'r:gz')`, for each member read its bytes into `io.BytesIO`, open that as a `zipfile.ZipFile`, and for each `.conf` entry inside, read its text, then for each OCR'd key either regex-substitute an existing `KEY=value` line or append `KEY=value\n` if the key is absent — do this in memory, don't write to disk per file (that's slow and the task hints at streaming). Rebuild each modified zip in a fresh `io.BytesIO` with `zipfile.ZipFile(..., 'w', zipfile.ZIP_DEFLATED)`, then write all rebuilt zips into a new tar opened with `tarfile.open(..., 'w:xz', preset=9 | lzma.PRESET_EXTREME)` — the `PRESET_EXTREME` flag combined with the deflate-compressed inner zips is what gets you under the tight size threshold; test the final size with `os.path.getsize` and iterate on compression settings if it's still too large (e.g., check whether pre-compressing the zips is actually helping vs. storing them uncompressed inside so xz alone does the compression).
