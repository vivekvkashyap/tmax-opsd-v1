Write the C++ program in clear stages and validate each independently before combining. Parse the CSV line-by-line: split on the first comma only, then strictly validate the id field with something like `std::from_chars` or a manual check that the entire substring (no leading/trailing junk) parses as an integer — any row that fails this check (including malformed rows with no comma) must be skipped entirely, not partially processed. For valid rows, split the text field on single spaces (careful with `std::stringstream >>` since it also splits on other whitespace types — prefer explicit index scanning to match "split on single space characters" precisely) and record the length of each token into one combined vector across all rows. From that vector compute sample mean, sample variance dividing by n-1, standard error = stddev/sqrt(n), and the 95% CI as mean ± 1.96*SE. Print with exactly 4 decimal places using `std::fixed << std::setprecision(4)` in the exact two-line format specified. Compile with `g++ -O3 etl_stats.cpp -o etl_stats`, run it, and check the output file matches the required format before finishing.
