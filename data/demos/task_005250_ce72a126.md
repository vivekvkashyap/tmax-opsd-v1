Enumerate `/home/user/monitored_links/` with `os.ReadDir`, and for each entry use `os.Lstat` to confirm it's a symlink, then `os.Readlink` plus `filepath.EvalSymlinks` (or `os.Stat` on the resolved target) to test whether the target actually exists — a failing `Stat`/`EvalSymlinks` means a broken link, logged with the exact given format using the symlink's own name (not the broken target path). For a valid link, sum file sizes non-recursively: `os.ReadDir(target)`, skip subdirectories, and add `entry.Info().Size()` for each regular file. Track a single package-level (or function-scoped) boolean for whether you've already logged the email-server-unreachable warning, so it's only appended once even if multiple directories exceed quota — test connectivity with `net.DialTimeout("tcp", "127.0.0.1:9025", someTimeout)` and check that dial *before* writing any alert files, not per-alert. For each over-quota directory, create `/home/user/mail_spool/alert_<symlink_name>.txt` with the exact three-line body (To/Subject/Size) using the symlink's name and the computed byte total. Run with `go run monitor.go` and inspect `monitor.log` and the spool directory afterward to sanity-check line-for-line.
