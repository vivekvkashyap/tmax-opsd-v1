Treat this as four independent sub-investigations and verify each before moving to the next. (1) Grep/scan access.log for `POST /upload` lines with HTTP status 200, extract the `path=` query parameter value and the source IP from the matching line — a traversal payload looks like repeated `../` segments, so pick the entry whose path clearly escapes the web root, and write both fields into the two-key JSON exactly as named. (2) Read auth_service.py's `generate_token` line by line — it base64-encodes the username, then does a repeating-key XOR against a hardcoded key, then base64-encodes the XOR result again — reproduce that exact same transformation yourself (in Python or by hand) for the username `system_admin` and write only the raw resulting token string to admin_token.txt, no extra formatting. (3) For each certificate under certs/, use `openssl verify -CAfile rootCA.pem <cert>` (or manually check `openssl x509 -in <cert> -noout -issuer -subject` and signature) to find which single cert fails verification against the root CA; write just its filename. (4) Correlate the attacker's IP/timestamp from step 1 to identify which authorized_keys line was attacker-injected, remove only that line, and write the hardened sshd config with exactly the three specified directives.