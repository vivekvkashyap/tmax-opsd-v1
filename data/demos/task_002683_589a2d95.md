Treat `/app/kg_oracle` purely as a black box you probe, never call from your service. Load `nodes.csv`/`edges.csv` and compute basic per-node graph statistics yourself (in-degree, out-degree, and any other candidate features like total degree). Then systematically query the oracle for a spread of nodes — isolated nodes, high in-degree, high out-degree, mixed — and tabulate `oracle_score` alongside your computed in/out-degree for each; look for a simple linear combination (small integer coefficients are typical for this kind of exercise) that reproduces every observed score exactly, and verify the fitted formula against several held-out nodes before hardcoding it natively in C. For the HTTP server, hand-rolling a minimal raw-socket GET-only parser is usually simpler and more reliable than pulling in a new library — read the request line, extract the query string, URL-decode parameters, and write a valid HTTP/1.1 response with correct `Content-Length` and `Content-Type: application/json`. Implement `/pattern` by scanning edges for matching `relation_type` and looking up each endpoint's `node_type` in your loaded node map. Print `SERVER READY` to the log only after both CSVs are loaded and the listening socket is bound, and background the process so the verifier can hit it.
