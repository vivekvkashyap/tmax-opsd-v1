Run `/app/data_generator` in `/home/user` first to produce a fresh, unindexed `graph.db` you can develop and time against. The naive query (self-join Nodes/Edges/Edges/Nodes filtered by type) will be slow on an unindexed 250k-row database mainly due to full table scans on the join columns, so before querying, issue `CREATE INDEX` on `Edges(source)`, `Edges(target)`, and `Nodes(type, id)` (a composite covering the type filter plus id) via your C++ program using the sqlite3 C API (`sqlite3_exec`), and consider `PRAGMA journal_mode=OFF`/`synchronous=OFF` and `ANALYZE` since this is a read-then-discard workload. Write the aggregation as a single SQL query with two chained JOINs (AUTHOR -> Edges -> intermediate -> Edges -> ARTICLE) and `GROUP BY`/`ORDER BY` pushed into SQLite rather than pulling rows into C++ and aggregating manually — let the query planner use your new indexes. Use `sqlite3_prepare_v2`/`sqlite3_step` with a compiled statement, format each score to 4 decimal places exactly as you write CSV rows, and cap output at 100 rows with `LIMIT 100` in SQL rather than truncating in code. Compile exactly with the specified `g++ -O3 ... -lsqlite3` command and time your own runs before submitting to check you're comfortably under the threshold.
