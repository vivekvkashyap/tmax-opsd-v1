This is a classic "someone left a debug delay in the hot path" bug: grep the vendored package for artificial slowdowns before assuming the bottleneck is architectural — check `/app/fast-forwarder/fast_forwarder/server.py`'s async request-handling function for a stray `asyncio.sleep(...)` call (often left with a TODO comment) and remove it. After fixing, reinstall the package into the venv (`pip install -e /app/fast-forwarder` or equivalent) so the running proxy picks up the change. For `deploy.sh`, make every step idempotent: `mkdir -p` for dirs, generate the self-signed cert with `openssl req -x509 -newkey rsa:2048 -nodes ... -subj "/CN=localhost"` only if the files don't already exist, create/reuse the venv, and for both the background HTTP server and the fast-forwarder daemon, kill any prior instance by matching its process (`pkill -f` on a distinguishing command-line substring) before starting a fresh one so re-running the script doesn't spawn duplicates or crash on "address already in use". Verify both ports are listening and the proxy actually forwards traffic before finishing.