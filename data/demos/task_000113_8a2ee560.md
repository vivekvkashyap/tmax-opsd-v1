Build this as a Bash-orchestrated pipeline that calls out to helper scripts, in clear stages, verifying each stage's output before moving to the next. (1) Use `ffmpeg -i drop_test.mp4 frame_%04d.png` to extract frames, then a small Python/OpenCV helper (`cv2.threshold` + `cv2.findContours` or simple centroid-of-bright-pixels on each frame) to find the vertical center of the white circle per frame; write `frame_index,y_pixel` rows to `/home/user/trajectory.csv`. (2) Convert frame index to time via `t = frame_index / 30.0`, convert pixel Y to meters via `y_m = y_pixel * 0.005`, then fit both a degree-1 and degree-2 polynomial (e.g. `numpy.polyfit`) to (t, y_pixel or y_m — be consistent with units when computing RSS), computing residual sum of squares for each to compare hypotheses. Recover acceleration from the quadratic coefficient `a` in `y = a*t^2 + b*t + c` as `g = 2*a` (already in m/s^2 if you fit against meters and seconds). (3) Compute velocity via `numpy.gradient` or finite differences on position vs time, then integrate with `numpy.trapz` to get total displacement. (4) Stand up a lightweight HTTP server (Flask or `http.server`) bound to `0.0.0.0:9090` serving `GET /analysis` with the four rounded (3 decimal) float fields, launched in the background so it stays alive for testing. Track pixel-vs-meter unit conversions carefully at each step — mixing them is the most common source of a wrong acceleration value.