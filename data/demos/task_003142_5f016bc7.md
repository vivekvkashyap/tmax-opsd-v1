Split the work into the C fix, the sanitizer script, and corpus testing, verifying each independently. (1) For the vendored `inotify-tools` build failure, don't assume the bug is a missing dependency — actually run the build (`./configure --prefix=/home/user/local && make`) and read the compiler/linker error carefully; a linker error about an undefined `main` symbol is a strong hint the entrypoint function name itself was tampered with in the source (search `src/inotifywait.c` for the function signature near `int main(`/similar and compare against a normal C program's expected entry point). Fix, then `make install` and confirm `/home/user/local/bin/inotifywait --help` exits 0. (2) For the sanitizer script, first detect encoding per-file (check for a UTF-16LE BOM `\xff\xfe`, or fall back — if not valid UTF-16LE, try Windows-1252) and convert with `iconv` before parsing, since you cannot assume a fixed encoding per file. Parse the manifest's multi-line record structure with care for the required blank-line-terminated Description field. Validate ArtifactName against `^[A-Za-z0-9_-]+$`, Path must not start with `/` and must not contain `../` (check path components after normalizing, not just a naive substring match, since encodings like `..%2f` or double slashes can evade a weak check), and Hash must match `^[0-9a-f]{64}$` exactly. On any violation, print nothing and exit non-zero; on success, print the converted UTF-8 text and exit 0. (3) Manually craft a few clean and malicious sample manifests yourself to test both encodings and each rejection rule before trusting the hidden corpus.
