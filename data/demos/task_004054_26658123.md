Work this as a pipeline of small, independently-verifiable scripts rather than one monolithic script. First, write a consolidation script that globs `sim_0.csv`..`sim_49.csv` in numeric order (not lexicographic — sort by the integer index to avoid sim_10 sorting before sim_2), reads each with pandas `read_csv(header=None)`, concatenates in that exact order, assigns `feature_0`..`feature_19` as column names, and writes to Parquet via `to_parquet` (pyarrow engine) after `mkdir -p` on the output directories. Second, reconstruct the MLP by hand rather than trying to guess a PyTorch class shape: load the JSON weights, build `nn.Linear(20,32)` and `nn.Linear(32,1)`, and load each weight tensor with `torch.tensor(...)` respecting PyTorch's (out_features, in_features) weight orientation — do not transpose incorrectly. Third, run inference vectorized over all 500k rows at once (not row-by-row) for speed and numerical consistency. Fourth, compute Pearson correlation per feature with `numpy.corrcoef` or `scipy.stats.pearsonr`, round to 4 decimals, and dump as JSON with the exact `feature_i` keys. Finally, factor the inference+correlation logic into a reusable function so `verify_pipeline.py` can just re-run it standalone and exit 0/1 based on success — this reproducibility script is graded separately, so don't skip it.