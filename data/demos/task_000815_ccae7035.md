Start by running `tesseract /app/config.png -` (or via a Python wrapper) to OCR the config screenshot; read the extracted text carefully to identify the model type, the K value, and the distance metric it specifies — don't hardcode assumed KNN defaults, use exactly what the image says. Then write `pipeline.c`: parse both CSVs (train rows have 4 floats + trailing int label, test rows have just 4 floats) into arrays/structs. Implement KNN from scratch: for a query point, compute distance to every training point using the metric from the image, partial-sort or scan to find the K nearest, then take a majority vote over their labels, breaking ties by choosing the smaller label value. For the validation step, manually partition the training set into 5 contiguous or interleaved folds, train-on-4/test-on-1 in rotation, average the per-fold accuracy, and print it. Compile with `gcc -O2 -lm`, run the 5-fold CV as a sanity check, then retrain on the full training set and classify every test row, writing one integer prediction per line (matching test row order) to `/home/user/predictions.csv`.
