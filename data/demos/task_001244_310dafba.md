Start by fixing the vendored parser: open `/app/vendored/jsonparse-1.2/src/parser.cpp`, locate the `parse_string` function's `\u` escape handling, and find the loop that reads hex digits — check its bound carefully (it likely reads only 3 hex digits instead of the required 4, corrupting subsequent parsing/memory). Fix the loop bound, then confirm it compiles per the given g++ command. For the filter program itself, build validation as explicit, independent checks per the spec (valid JSON parse; all four fields present; `source_ip` regex/split-and-range-check on four dot-separated octets 0-255; `bytes_transferred` normalized whether it arrives as JSON string or number, then checked `>= 0`) and print the original raw input line verbatim (not a re-serialized version) for lines that pass every check — re-serializing risks subtle formatting differences that could fail strict output comparisons even though the task allows it here. Before trusting your binary against the real corpora, manually construct a few edge cases (`256.0.0.1`, negative bytes, missing fields, malformed JSON, valid ` `/`©` escapes) and confirm accept/reject behavior line by line.
