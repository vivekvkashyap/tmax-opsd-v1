Write `/home/user/validate.go` using only `encoding/csv`, `os`, `strconv`, `math`, `fmt`. Read both CSVs into maps/slices keyed by `id` (e.g. `map[string]float64` for predictions, and a struct for truth rows holding `actual_value` and `category`), then join by iterating one map and looking up the other by `id` — skip ids present in only one file. Apply the validity filter `predicted_value >= 0.0 && predicted_value <= 100.0` (inclusive both ends) and drop rows failing it. Compute `math.Abs(predicted - actual)` and format with `strconv.FormatFloat(err, 'f', 2, 64)` or `fmt.Sprintf("%.2f", err)` to guarantee exactly two decimal places (watch out for values like `0` printing as `0.00`, not `0`). Write the header `id,category,abs_error` followed by one row per valid joined record — preserve the join order (e.g. iterate by ascending id) so output is deterministic. Run `go run /home/user/validate.go` (or build it) so `/home/user/cleaned_metrics.csv` is actually produced, not just written as source.