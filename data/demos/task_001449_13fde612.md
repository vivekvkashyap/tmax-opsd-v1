You must reverse-engineer `/app/legacy_audit_processor`'s exact behavior by black-box testing, then reimplement it in Python — don't guess the format. Feed it a battery of crafted inputs via stdin (e.g. `echo -n "..." | /app/legacy_audit_processor`) and diff the outputs to pin down each of the three transformations independently: (a) plain text with no SSN/traversal to see the baseline format and delimiter; (b) text containing a 9-digit SSN pattern like `123-45-6789` to see the exact redaction replacement string; (c) text containing `../` or `..\` to find the exact alert tag string and where it's placed (prefix vs suffix); (d) combine both to confirm ordering of the two transforms. Once you have the modified string, try common hash algorithms (md5, sha1, sha256) on candidate byte encodings (with/without trailing newline) and compare hex digests against the binary's trailing token to identify the exact algorithm and normalization. Pay attention to whether the binary strips a trailing newline from stdin before processing — test with and without one. Once all three behaviors are pinned down precisely, write `/home/user/audit_processor.py` to reproduce them and re-verify against several more black-box probes before finishing.