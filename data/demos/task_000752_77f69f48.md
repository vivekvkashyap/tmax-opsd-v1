First OCR /app/admin_notes.png (install pytesseract/tesseract-ocr) to recover the exact API_PORT and AUTH_TOKEN values — parse them out of the OCR text rather than assuming values. Then write a binary parser for legacy_backup.bin: read the 4-byte 'BKUP' magic, then loop reading a little-endian u16 path length, the path bytes, a little-endian u32 data length, and the data bytes, until EOF. For each entry, before writing anything to disk, validate the path: reject (skip) any entry whose path contains '..' or starts with '/' — this is the zip-slip check, so validate the raw path string itself, not just the resolved/joined path (which could still be manipulated). Write safe entries under /home/user/extracted_safe/, creating subdirectories as needed. Merge logs/server.log.part* files by sorting filenames alphanumerically (e.g. natural/lexical sort of "part1", "part2", ...) and concatenating into logs/server_complete.log, then tar+gzip the logs/ directory. Finally implement a simple Python http.server (or similar) on 0.0.0.0:<API_PORT> handling GET /download/<path>, checking the Authorization: Bearer <AUTH_TOKEN> header exactly, returning 401/404/200 as specified, and make sure it stays running (background it, e.g. with nohup/& or a persistent process) so the verifier's curl requests can reach it.