Start by locating the bug: read `/app/vendored/qemu-net-builder/qemu_net_builder/utils.py` and find the TypeError (a string being concatenated with a non-string) — fix it so the function returns a valid string, then `pip install -e .` from that package directory (in a venv or user install) and confirm `python3 -c "from qemu_net_builder.utils import get_subnet; get_subnet()"` runs without error before moving on. For the path-traversal validator, the key technique is: join the storage base with the untrusted `host_mount`, then canonicalize with something that resolves symlinks and `..` (Python's `os.path.realpath` or `pathlib.Path.resolve()`), and check the result is equal to or a subpath of the realpath'd base directory — never do a naive string prefix check on the unresolved path, since that misses symlink-based escapes. Test your script manually against a `../`-traversal case and a symlink-escape case before trusting it against the full corpora, iterating until both `/home/user/corpora/clean/` and `/home/user/corpora/evil/` classify correctly via exit codes 0/1. Finally create the systemd unit file with the exact `Description`, `ExecStart`, and `Restart` lines specified.