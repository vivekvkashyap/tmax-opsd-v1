Treat the oracle as a black box to query, not to disassemble — build small, controlled edge-list test graphs (isolated star patterns, chains, graphs varying only the target's in-degree, then graphs varying 2-hop predecessor structure) and run `/app/graph_oracle.bin edgelist.txt target` on each, changing one structural feature at a time to isolate its contribution to the score. Check for linear relationships: does adding a direct incoming edge change the score by a fixed amount? Does an edge two hops away change it by a different fixed amount? Once you have a formula hypothesis in terms of the target's in-degree and its predecessors' in-degrees, confirm it against more random graphs before implementing. Write the C++ solution using a hash map from node to predecessor list (reverse adjacency) built in one pass over the edge list, then compute the target's score in O(in-degree) lookups. Match the integer output format exactly (single integer + newline) and compile with `g++ -O3`.