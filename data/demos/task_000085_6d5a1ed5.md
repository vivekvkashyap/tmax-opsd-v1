Start by reading `gateway_bin`'s behavior indirectly: run `strings gateway_bin` or inspect the pre-populated `backend.py` to confirm ports and the dlopen contract before writing any C. Implement `process_request(const char* ip, const char* path)` in `plugin.c` with `strncmp`/`strncmp` prefix check on `/api/v1/` first (anything else is 403 immediately, cheaper than rate-limiting). For rate limiting, keep a small fixed-size static array (up to 10 IP slots), each holding an IP string and a small ring/array of the last few request timestamps (use `time(NULL)` or better `clock_gettime(CLOCK_MONOTONIC, ...)` for sub-second precision since the window is only 2 seconds — `time()`'s 1-second granularity is a common naive mistake that breaks the 2-second rolling window test). On each request to `/api/v1/data`, prune timestamps older than 2 seconds from the IP's history, count what remains, and reject with 429 if it would exceed 3. Compile with `gcc -shared -fPIC -o libgateway_plugins.so plugin.c`. Write `test_plugin.c` to call `process_request` directly (not via HTTP) for fast, deterministic tests including firing 4 rapid calls for the same IP/path. Only after tests print `ALL TESTS PASSED` to the log, launch `backend.py` and `gateway_bin` in the background from `start_services.sh`, run it, and verify both ports are listening with `curl` before finishing.