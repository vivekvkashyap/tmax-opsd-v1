Avoid N+1 queries entirely: write one SQL query in `export_backup.py` that joins `customers`, `orders`, and `items`, filtering `orders.status = 'completed'` (an inner join naturally drops customers with no completed orders), and order the result by `customer_id, order_id, product_name` so rows arrive pre-sorted for grouping. Fetch all rows with a single `cursor.execute(...)` + `fetchall()` call (the task requires exactly one query), then in Python group the flat rows into the nested customer -> orders -> items structure using something like `itertools.groupby` or manual dict-building keyed by customer_id then order_id, accumulating each item's price into a running `total_spent` per customer as you go. Round or coerce `total_spent` and item `price` to `float`. Serialize with `json.dump(data, f, indent=2)` to `/home/user/backup_data.json`, opening the SQLite connection read-only (or simply issuing no INSERT/UPDATE) so the database file itself is left unmodified. Double check the nested sort orders (orders by order_id, items by product_name alphabetically) match exactly since the verifier compares against an exact golden JSON.
