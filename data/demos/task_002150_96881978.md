First recover the expected high-precision value: the memory dump is mostly binary noise, so scan `/home/user/memory.dump` as raw bytes for the ASCII marker `EXPECTED_METRIC_VALUE=` and extract the string that follows (e.g. with `strings /home/user/memory.dump | grep EXPECTED_METRIC_VALUE` or a small Python byte-scan) — use exactly what's embedded there, don't assume a value. Write a small Python test script that imports/executes `calculate_metrics()` from `math_ops.py` at the current checkout, formats the result as a string, and compares it against the recovered expected value to at least 15 decimal places (e.g. compare the leading N characters, or compare as high-precision decimals) — this is your bisection predicate, exit 0 if it matches ("good"), exit 1 if it deviates ("bad"). In `/home/user/repo`, run `git bisect start`, mark `HEAD` bad and the very first commit good, then `git bisect run <your_test_script>` to let git converge on the offending commit automatically rather than checking commits out by hand. After bisect finishes, capture the identified commit's full 40-character SHA (`git rev-parse`) and write it to `/home/user/bad_commit.txt`, then run `git bisect reset` to leave the repo clean.