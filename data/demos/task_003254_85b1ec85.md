Work through this incrementally and verify each stage before moving to the next, since later stages depend on earlier ones compiling/running. First fix the build: run `cargo build` in `/home/user/video_service`, read the compiler errors carefully — add the missing dependency (likely `tonic`/`prost` given the gRPC requirement) to `Cargo.toml` with a version compatible with the existing `build.rs`/proto setup, and fix the syntax error(s) in `src/main.rs` one at a time until it compiles cleanly. Second, recover the DB: use `sqlite3 /home/user/video_service/data/metrics.db ".recover"` or `.dump` (corrupted header bytes need `.recover` mode, not plain `.dump`, since the file may not open as a valid DB at all) to extract the intact `video_stats` table into a fresh `recovered.db`. Third, fix `src/processor.rs`: locate the loop that counts frames — check for an off-by-one in the loop bound and a condition that never terminates or terminates one iteration early/late; consider replacing manual frame-by-frame counting with a single `ffprobe -count_packets -show_entries stream=nb_read_packets` call on `/app/incident.mp4` for a correct, fast count rather than looping yourself. Finally, launch the service with `SERVICE_TOKEN=secret-uptime-token-99` set and backgrounded (e.g. `nohup ... &`), then manually curl `/health` and `/metrics` (with and without the bearer token) and test the gRPC method with `grpcurl` before declaring done.