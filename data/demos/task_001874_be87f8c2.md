Start by extracting the algorithm and parameter from `/app/dataset_note.wav` — transcribe it with a speech-to-text tool (e.g. `openai-whisper`, or ffmpeg to convert then a Python ASR library) rather than guessing; the transcript will name a smoothing algorithm and give a numeric parameter, which you must read off precisely rather than assume. Once you have the algorithm and parameter, implement it in `/home/user/ema_filter.cpp` using single-precision `float` throughout (avoid accidentally promoting to `double`, which would cause fuzz-equivalence mismatches against the hidden oracle). Read raw floats from stdin with `std::cin.read` in a loop until it fails/EOF, handle the very first sample as a direct passthrough (`y_0 = x_0`), and write each output float directly via `std::cout.write` — do not print any text, whitespace, or newlines, and disable stdio sync/buffering artifacts that might interleave incorrectly. Compile with `g++ -O3 -o /home/user/ema_filter /home/user/ema_filter.cpp`. Before finishing, sanity-test manually by piping a small handcrafted binary float stream through the binary and checking the output length and values make sense, since the grader will fuzz-test byte-for-byte against a hidden reference implementation.
