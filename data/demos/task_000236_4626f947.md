Structure analyze.go in clear steps: read and parse spectra.csv (skip header), filter rows to 400.0 <= wavelength <= 500.0 inclusive, and compute N as the count of filtered rows. Compute the baseline intensity-weighted center of mass with the given formula so you have a sanity value to compare bootstrap results against. For the bootstrap, spin up several goroutines (e.g. 4-8) each handling a slice of the 10,000 total iterations, with each iteration sampling N points with replacement (use `math/rand` with an index draw per point, not sampling without replacement) from the filtered slice and computing that sample's CoM; use a `sync.WaitGroup` to wait for all workers and write results into a pre-allocated shared slice indexed by iteration number (or per-worker slices concatenated afterward) to avoid data races — avoid appending to a shared slice concurrently without synchronization, which is a common goroutine bug. After collecting all 10,000 CoM values, sort them, take the values at 0-indexed positions 250 and 9750 as the 95% CI bounds, and compute the mean of all 10,000. Marshal mean_com/ci_lower/ci_upper as JSON numbers (not strings) to peak_ci.json, then build and run the program to actually produce the file.