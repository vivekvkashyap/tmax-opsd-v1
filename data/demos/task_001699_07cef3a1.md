Write `pipeline.py` as a straightforward discover-measure-run-aggregate loop: use `glob.glob` or `os.listdir` filtered to `*.bin` under `/home/user/models/`, get each file's size via `os.path.getsize`. For each model, invoke the benchmark with `subprocess.run(['python3', '/home/user/benchmark.py', abs_path], capture_output=True, text=True)` — pass the absolute path since the script requires it — then `json.loads(result.stdout)` to pull out `latency_ms` and `throughput`. Build a dict keyed by `os.path.basename(model_path)` (not the full path, per spec) with a nested object combining `size_bytes` plus the two benchmark fields, preserving whatever numeric precision the benchmark script returns rather than re-rounding. Write the combined dict to `/home/user/artifact_metrics.json` with `json.dump`, then actually execute `python3 /home/user/pipeline.py` so the output file exists on disk for verification, and spot check it against a couple of the benchmark script's raw outputs to confirm the merge logic is correct.
