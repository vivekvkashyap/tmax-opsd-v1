Use `git bisect` mechanically rather than eyeballing the diff of 200 commits: write a tiny test harness (C++ program or script) that serializes and deserializes a known high-precision double via the library's `Telemetry::serialize()`/`deserialize()` and exits nonzero if the round-tripped value doesn't exactly match the input, then run `git bisect start`, mark `HEAD` bad and `v1.1.0` good, and drive it with `git bisect run <your-script-that-builds-and-tests>` so bisection is automated and exact. Once you land on the offending commit, inspect its diff (`git show`) to see how it changed the float/double packing — expect something like a truncating cast during (de)serialization — and fix the underlying source so the full 8-byte double precision is preserved end-to-end. Rebuild with `make` and confirm `make check` passes before moving on. For the detector, don't hand-tune against just the given corpora: inspect several clean and evil samples with a hex viewer to understand the header/UTF-8/float encoding, then write `/home/user/detector.cpp` to validate UTF-8 header bytes strictly and check embedded IEEE-754 doubles for NaN/subnormal patterns, returning exit 0/1 accordingly. Compile and run it against every file in both `/app/corpora/clean/` and `/app/corpora/evil/` in a loop yourself to confirm 100% accuracy before finishing, iterating on any misclassifications you find.