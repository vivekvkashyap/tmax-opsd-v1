Split this into two independent deliverables and don't let one block the other. For Step 1: transcribe `/app/last_request.wav` (install/use `whisper` or another STT tool, or `ffmpeg` plus a speech-to-text model) to recover the spoken magic word — read the transcript output carefully rather than assuming a value. Then grep/search the binary `/app/service.core` for that exact uppercase word as an ASCII substring (e.g. `strings /app/service.core | grep WORD` or a Python byte-scan), and take the 16 hex characters immediately following it in the raw byte stream. Write `MAGIC_WORD:PAYLOAD` to the specified file exactly. For Step 2/3: don't try to reverse-engineer any hidden oracle — instead diff the byte-level headers of files in `/app/corpus/clean/` vs `/app/corpus/evil/` yourself (e.g. `xxd` on the first ~100 bytes of a few examples from each) to find the discriminating field. The RIFF/WAV format stores chunk sizes as little-endian uint32 right after each 4-byte chunk ID; write a C++ program that walks the chunks from byte 12 onward, reads each declared chunk size, and flags the file as evil (exit 1) if any declared chunk size is absurdly larger than what remains in the actual file (or exceeds a large sanity threshold like 50MB); otherwise exit 0. Validate your detector against both provided corpora directly before finishing.