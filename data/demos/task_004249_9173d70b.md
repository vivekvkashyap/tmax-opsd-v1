Run `tesseract /app/memo.png stdout` first and read the extracted text closely to identify the exact rejection rules (a forbidden token, a case-insensitive forbidden phrase, and a dedup requirement) — don't assume rule wording, use what OCR actually recovers, and account for likely OCR noise (odd capitalization/spacing) by testing your grep patterns against edge cases. Implement `/home/user/filter.sh` as a streaming pipeline of coreutils only — chain `grep -v` for the exact-token rule, `grep -vi` for the case-insensitive phrase rule, and `uniq` for collapsing consecutive duplicate lines — avoid buffering the whole input in a shell array or `$(cat ...)`, which breaks the "efficient streaming for huge input" requirement; a pipe of grep/uniq processes each line as it streams through. Send the rejected-line count to stderr only after the stream ends (e.g., accumulate a count via a final `wc -l` diff, or tee through `grep -c` on a side channel) without polluting stdout. Test locally: craft small evil lines containing the forbidden token/phrase in mixed case and confirm they're dropped, and craft clean already-deduped lines and confirm they pass through byte-for-byte.
