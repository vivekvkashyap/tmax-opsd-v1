Set up the Go module first (`go mod init` in /home/user, `go get golang.org/x/text/encoding/...` for the charmap/unicode packages needed to decode Windows-1252 and UTF-16LE). Use `filepath.WalkDir` for recursive traversal, and for each `.txt` file, split the filename on `.` to read the second-to-last segment as the encoding tag (win1252/utf16le/utf8), then pick the matching `golang.org/x/text/encoding` transformer (`charmap.Windows1252.NewDecoder()`, `unicode.UTF16(unicode.LittleEndian, unicode.IgnoreBOM).NewDecoder()`, or pass through for utf8) wrapped with `transform.NewReader` to decode into UTF-8 bytes. Mirror the relative directory structure by computing the path relative to raw_data root and recreating it under clean_data with `os.MkdirAll`. For atomic writes: write to a uniquely-named file inside `clean_data/.tmp/` via `os.CreateTemp`, flush/close it, then `os.Rename` it directly into the final destination path (rename within the same filesystem is atomic; don't try to rename across filesystems). Track a running file counter and byte counter as you go, and write them as two lines to `/home/user/summary.txt` at the end.
