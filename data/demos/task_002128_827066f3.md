Build the pipeline as a chain of small, testable Bash stages before wiring them together. First run `ffmpeg -i /app/telemetry.mp4 -map 0:s:0 -f srt -` to confirm you can pull the subtitle track and see raw SRT text (index numbers, timestamps, blank lines, and payloads), then filter with `grep '^{'` to keep only JSON lines. Inspect a few lines to find the malformed unicode escape in the status field, then write a `sed` regex that matches the broken pattern and substitutes the correct escaped space — test it against sample lines in isolation first. For reshaping, build the `jq` filter incrementally: confirm single-line parsing works, then emit two rows per record (temperature and humidity), applying the default when `h` is null; do the C-to-F conversion in `jq` or a following `awk` pass — verify the arithmetic against a hand-computed example. Only once the full pipe (ffmpeg | grep | sed | jq | awk) reliably produces correct CSV should you redirect it to `/home/user/clean_telemetry.csv`. Finally implement the one-shot HTTP responder with `nc -l -p 8080` (or `socat`) that prints a status line and blank line, then the CSV contents — verify with `curl` locally before finishing.
