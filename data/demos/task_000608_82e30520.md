Handle the three parts independently and verify each before moving on. Part 1: use `ffmpeg -i /app/spectroscopy_run.mp4 frame_%04d.png` (or pipe raw frames) to extract frames in order, then compute average grayscale brightness per frame (convert to grayscale, average all pixel values) with a tool like ImageMagick `identify -format "%[mean]"` or Python/PIL, writing `frame_index,average_brightness` to 1 decimal place. Part 2: grep/parse `/app/target.pdb` lines starting with `HETATM` and count them into `/home/user/hetatm_count.txt` — this K value feeds Part 3. Part 3 is the core challenge: replicate the oracle's LCG bit-exactly in awk — seed X0 = sum(ASCII of sequence chars) + K, iterate `X = (1103515245*X + 12345) mod 2^31` for 2*N steps (N = len(seq)*K), pairing consecutive X values as (x,y) normalized by dividing by 2^31, and count pairs where x^2+y^2 <= 1. Use awk's arbitrary-precision-safe integer arithmetic carefully (bitwise AND with 2^31-1 instead of mod, or careful mod handling, since intermediate products can exceed doubles' exact-integer range around 2^53 — verify 1103515245*X stays representable, or use a language/technique that keeps 64-bit integer precision). Since this will be fuzz-tested for bit-exact equivalence across thousands of random DNA strings and K values, manually hand-trace the LCG sequence and collision test for a tiny input (e.g. a 2-3 character sequence) to confirm your seed computation, iteration order, and comparison threshold exactly match the specification before finishing.