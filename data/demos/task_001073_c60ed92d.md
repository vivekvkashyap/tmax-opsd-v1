The security rules are embedded in an audio recording, not documentation — recover them by transcription rather than guessing plausible-sounding rules. Install a transcription tool (e.g. `pip install openai-whisper` plus `ffmpeg`, or any local STT you can get running) and run it against `/app/intercepted_dev_meeting.wav`; read the resulting transcript carefully and enumerate every constraint it states for `target_file` and `body` as a checklist before coding, since the classifier's pass rate depends on catching all of them precisely (allowed file extensions, traversal patterns, disallowed path prefixes, and each XSS-related substring/attribute check). Implement `/home/user/sanitiser.py` to: read the JSON path argument, wrap parsing in try/except returning exit code 1 on any error (bad JSON, missing keys, wrong types), then apply each transcribed rule as an explicit substring/pattern check on `target_file` and `body`, exiting 0 only if every check passes and 1 the moment any fails. Build your own small clean and evil test payloads mirroring the described attack classes (traversal, disallowed extension, script tags, javascript: URIs, inline event handlers) and run your script against them manually, checking exit codes with `echo $?`, before trusting it against the full corpora.