Separate the graph/checksum logic from the web/Redis plumbing so it's independently testable, as the task itself asks for. Write a pure `resolver.py` module with a function that, given a way to fetch a node's deps+checksum, recursively walks the dependency graph, detects cycles (e.g. via DFS with a recursion-stack set — return/raise a distinguishable error rather than looping forever), and produces a topological order (Kahn's algorithm or DFS post-order reversed) with the root's own checksum appended last, not first. Compute the aggregated checksum by concatenating each node's checksum string in that exact topological order and hashing the concatenation with SHA-256 — get the order and inclusion of the root exactly right, since these are the easiest places to introduce an off-by-one. In `api.py`, have the Flask/FastAPI route parse Redis keys (`artifact:<name>:<version>:deps` as JSON, `:checksum` as a string), call into `resolver.py`, and translate cycle detection into a 400 with the exact error JSON shape. For the property-based tests, use `hypothesis.strategies` to generate random small DAGs (e.g. lists of edges guaranteed acyclic by construction) and assert your topo-sort respects all edge orderings, plus a separate test that mutates one node's checksum and asserts the aggregated checksum changes. Finally make sure `start_services.sh` actually launches Redis and the API in the background and doesn't block.