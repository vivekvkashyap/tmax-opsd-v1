Fix the config first, then build the proxy, then automate verification, in that order so each step can be tested against a working system. Edit `/home/user/microservices/config.json` to change only `email_port` from 9025 to 8025, leaving `api_ports` and `proxy_port` untouched. Write `/home/user/router.py` using only `http.server`/`urllib.request`: maintain a module-level counter (or itertools.cycle) over `[8081, 8082]` so consecutive `/api` requests alternate strictly round-robin starting at 8081, and route `/email` straight to port 8025; for each proxied request, forward the backend's exact response body back with a 200 status — use `urllib.request.urlopen` to hit `http://localhost:<port>/` and relay `.read()` verbatim, not a wrapped/altered message. Start `/home/user/microservices/start_services.py` in the background first, confirm the three backend ports are listening (e.g. `curl` each directly) before starting `router.py` in the background. Finally write `/home/user/verify.sh` issuing the three curls in the exact specified order, appending raw output with no extra newlines, and run it to produce `/home/user/routing.log`.