Treat the legacy binary as a black-box oracle: probe it with small hand-crafted CSVs (including empty age/amount, huge user_ids near int64 max, and extreme amount values) and diff its stdout against your evolving script before trusting the stated logic. When implementing in pandas, the critical pitfall is that a left join or any operation introducing NaNs silently upcasts integer columns to float64, which loses precision for 19-digit user_id and amount values — avoid this by using pandas nullable integer dtype (`Int64`) for `user_id` and `amount` throughout, or by imputing missing values as strings/ints before any join/groupby rather than relying on default float NaN handling. Apply the outlier filter (amount > 1000000 or < -1000000) to transactions before aggregation, impute missing age with 99 and missing amount with 0, group by user_id summing amount and counting valid transactions (0/0 for users with none), and format the final CSV so integers never render with `.0` or scientific notation. Track imputed-age and dropped-outlier counts as you process, and write them to the `--track` JSON path when that flag is given. Validate bit-exact equivalence against the oracle across many randomized fuzz inputs, not just one example, before considering the script done.