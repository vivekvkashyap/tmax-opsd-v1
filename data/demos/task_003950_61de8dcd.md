First copy the vendored Go package to the working directory as instructed, then locate the scoring/posterior calculation function (likely in a file under a `scorer` package) and read it carefully term-by-term against the stated formula `(Prior * Likelihood) / Marginal`. A classic injected bug swaps an operator (e.g. `+` where `*` belongs) — diff the code's arithmetic expression against the spec rather than assuming it's already correct. After fixing, run `go test -bench . ./...` from the package root and confirm all tests pass, not just that it compiles. Check `main.go` or config/env handling to see how the listen address/port is set (often via an env var or flag default) and start the binary so it binds to `127.0.0.1:9090` specifically — verify with `curl` or `ss`/`lsof` that something is actually listening before finishing. Run the server as a background/daemonized process (e.g. `nohup ... &` or `setsid`) so it keeps running after your shell session ends, and do not kill it. Finally sanity-check the `/score` endpoint yourself with a GET request including `prior`, `likelihood`, `marginal` query params to confirm the JSON response shape before declaring done.