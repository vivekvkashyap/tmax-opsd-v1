Read the existing `/home/user/generate_report.sh` to see the current SQLite heredoc and its buggy `FROM employees e, departments d` cross join. Fix the join first by adding an explicit `JOIN departments d ON e.dept_id = d.dept_id`. Then replace the flat SELECT with a `WITH RECURSIVE` CTE: the base case selects employees whose `manager_id` is empty/null (top of hierarchy) with `manager_name` empty and `management_chain` = their own name; the recursive case joins `employees e` to the CTE on `e.manager_id = cte.emp_id`, sets `manager_name` to the parent's name, and builds `management_chain` by concatenating the parent's chain with `'->' || e.name`. Watch for how empty `manager_id` values are represented in the imported CSV (likely empty string, not NULL, since `.import` treats blanks as empty strings) — your base-case WHERE clause must match that representation. Keep the `.mode csv` / `.headers on` / `.out` directives so the output has commas and a header row, then execute the script and confirm `/home/user/org_chart.csv` matches the expected columns and hierarchy for every employee.