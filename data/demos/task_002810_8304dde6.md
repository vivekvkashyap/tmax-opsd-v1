Build the pipeline in clear stages and verify each stage's output before moving to the next. First use `ffmpeg -i /app/biosensor_timelapse.mp4 -vf fps=1 ...` (or explicit `-ss`/frame-select flags) to extract exactly 11 frames at t=0..10s, saving as image files; confirm you got 11 distinct frames. Second, per frame, load with `PIL`/`cv2`, convert to grayscale, average pixel intensity down each column (axis=0) to get a 1D profile across the channel's x-extent, then normalize it to a probability density by dividing by its sum times `dx` (so it integrates to 1) — this gives you `C_obs(x,t)` at each timepoint. Third, implement a finite-difference solver for the 1D no-flux diffusion PDE (e.g. explicit or Crank-Nicolson finite differences with ghost-point or one-sided-difference Neumann boundaries) starting from the t=0 empirical profile as initial condition; implement mesh refinement by solving at increasing N (e.g. 20, 40, 80...) and stopping once consecutive solutions agree within a small tolerance, to avoid reporting a diffusion coefficient biased by discretization error. Fourth, wrap the solver in a 1D scalar optimization (e.g. `scipy.optimize.minimize_scalar` or a simple grid+refine search) over D that minimizes MSE between the converged PDE solution and the observed profiles across all 10 nonzero timepoints. Write the best-fit D as a single float to `/home/user/diffusion_result.txt`, and make `/home/user/run_analysis.sh` (executable, calling your Python helpers) run this whole pipeline non-interactively end to end.