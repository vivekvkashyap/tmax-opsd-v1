For Part 1, first read /app/src/sanitizer.py carefully to extract its exact algorithm (character filtering rule, case normalization, and the precise checksum formula/encoding), since your C++ port will be fuzz-tested byte-for-byte against a hidden oracle across thousands of random strings including edge cases (empty input, non-printable bytes, full 8-bit range). Read stdin as raw bytes (not just until EOF on whitespace), replicate the Python logic exactly including any modulo/overflow semantics, and test your binary against the Python reference locally on varied inputs (empty string, all symbols, unicode-looking byte sequences) before considering it done — mismatches on obscure inputs are the most common failure. Compile with the exact specified g++ command to the exact output path. For Part 2, edit the nginx.conf skeleton: add a `location /api/ { proxy_pass http://127.0.0.1:5000; proxy_set_header Host $host; }` block and a `location /stream/ { proxy_pass http://127.0.0.1:5001; proxy_set_header Host $host; proxy_set_header Upgrade $http_upgrade; proxy_set_header Connection "upgrade"; }` block, with the server listening on 8080. Validate the config syntax if nginx is available (`nginx -t`).
