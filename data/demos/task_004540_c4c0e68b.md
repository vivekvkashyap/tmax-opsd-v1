Bring up the chain from the backend outward, testing each hop before adding the next. Start `/app/backend/server.py` and confirm it's reachable on 8081 directly. Fill in `inspect_and_modify_http` in `proxy.cpp`: check the raw request string for the literal substrings `UNION SELECT` and `<script>` (simple `std::string::find`, no need for regex) and return the fixed 403 response without connecting to the backend when found; for redaction, locate the `Cookie:` header line, then use `std::regex` (or manual scanning) to find `session_id=` followed by alphanumeric characters and replace just that value with `REDACTED`, preserving the rest of the cookie string exactly (other cookies like `user=admin` must survive untouched). Compile and start the proxy on 8080, and test it directly with `curl` before adding TLS. Generate the cert with `openssl req -x509 -newkey rsa:2048 ... -subj "/CN=localhost"`, fill in the Nginx `ssl_certificate`/`ssl_certificate_key`/`listen 8443 ssl`/`proxy_pass http://127.0.0.1:8080` directives, and point `pid`/`error_log`/`access_log` inside `/home/user/nginx_setup/` so `nginx -c` can run unprivileged. Finally test the full chain with `curl -k` for the redaction, SQLi, and XSS cases described.
