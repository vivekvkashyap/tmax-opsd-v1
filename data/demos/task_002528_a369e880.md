First get the vendored C extension building: inspect `/app/fast_log_tokenizer-0.1/setup.py` and the directory listing — when a build fails with a missing-source error, the fix is almost always a filename/path mismatch between what `setup.py`'s `Extension(sources=[...])` lists and what's actually on disk, so `ls` the package dir, compare to the `sources` list, and either rename the .c file to match or edit `setup.py` to point at the real filename, then `pip install -e /app/fast_log_tokenizer-0.1` and confirm with a Python import + a test call. For the ETL script: handle each timestamp format defensively — try parsing as a float first (distinguish epoch-seconds vs epoch-milliseconds by magnitude, e.g. values much larger than ~2e9 are milliseconds), then fall back to explicit string format parsing (`'%Y/%m/%d %H:%M:%S'`, ISO8601), converting everything to UTC and formatting as `%Y-%m-%dT%H:%M:%SZ`. Tokenize with the fixed module, lowercase each token, and drop empty/whitespace-only tokens. For stratified sampling to exactly 1000 records matching the full dataset's language proportions, compute each language's fraction in the full file, allocate per-language sample counts proportionally (rounding carefully so counts sum to exactly 1000), and sample that many records per language (e.g. via `pandas.groupby` + `sample`), rather than a single global random sample which won't guarantee the distribution match.
