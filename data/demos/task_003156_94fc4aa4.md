Transcribe `/app/audio_instructions.wav` first (whisper or similar) to learn the extra filtering rule verbatim from the lead engineer's memo — implement exactly what it says rather than guessing a plausible rule. In the Rust project at `/home/user/loc_etl`, write a streaming stdin JSONL parser (serde_json line-by-line) that for each record: drops it if `text` contains the Unicode replacement character (`'\u{FFFD}'`) or matches the audio-derived rule; otherwise tracks a `HashMap<(key, lang), (timestamp, text, order_index)>`, replacing the stored entry whenever a new record has a strictly higher timestamp, OR an equal timestamp but a later stream position (so always overwrite on `timestamp >= stored_timestamp`, since equal-timestamp ties should keep whichever appears later — just always overwrite on `>=` as you scan in order, that naturally keeps the last one on ties). After processing all lines, build a nested `BTreeMap<String, BTreeMap<String, String>>` (BTreeMap gives you sorted keys for free) from the surviving winners, drop any outer key whose inner map ends up empty, then serialize with a compact/minified JSON writer to stdout. Build with `cargo build --release` and diff your output against hand-crafted small JSONL inputs before trusting it against the fuzzer.