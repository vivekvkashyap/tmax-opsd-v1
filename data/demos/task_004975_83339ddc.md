Install Eigen3 first (e.g. `apt-get install -y libeigen-dev`) so the C++ compile step will succeed. Implement Bayesian linear regression by hand-coding the unnormalized log-posterior: log p(beta|data) ∝ -0.5/sigma^2 * ||Y - X*beta||^2 - 0.5*alpha*||beta||^2 (using the given sigma^2=1.0 and alpha=10.0), since you only need relative log-density ratios for Metropolis-Hastings acceptance, not the full normalized posterior. Implement Random Walk MH: at each iteration, propose beta' = beta + N(0, 0.01*I) (sample 3 independent N(0,0.01) perturbations), compute the log-posterior ratio, and accept with probability min(1, exp(log_ratio)) using a uniform random draw — get the RNG state and seeding right so behavior is reproducible and correct. Run exactly 15,000 iterations, discard the first 5,000 as burn-in, and write out the remaining 10,000 accepted-chain states (not just accepted proposals — record the current state every iteration whether or not the proposal was accepted) as CSV rows with 3 comma-separated floats. Sanity-check your own implementation by comparing the sample mean of beta to the closed-form ridge-regularized posterior mean (X^T X + alpha*I)^-1 X^T Y before considering the task done.
