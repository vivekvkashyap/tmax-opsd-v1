Two distinct sub-problems: recover the valid locale list from the image, then implement exact streaming logic matching the spec. For the image, don't guess locale codes — actually extract the text from `/app/locales.png` (e.g. install and run an OCR tool like `tesseract`, or open/inspect the image directly if you have vision capability) and use whatever locale codes you find; don't hardcode a plausible-looking list from memory. For the processing script, implement precisely in this order: parse each stdin line as JSON, skip on any parse error or missing field, validate `loc` against the OCR'd locale set, group by locale, sort by ts, average duplicate timestamps together first, then walk consecutive deduped points doing linear interpolation to insert points at every missing 60s step strictly between them (careful with `k * 60 < ts_b - ts_a`, not `<=`), drop locales with fewer than 2 distinct original timestamps, and finally average all points (original + imputed) then floor to 2 decimal places using `math.floor(avg*100)/100.0` (not naive rounding). Build a handful of your own small JSONL test cases (with gaps, duplicates, invalid locales, malformed lines) and manually hand-trace the expected numbers to sanity-check your script before considering it done.