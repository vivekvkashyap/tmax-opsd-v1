Work through the four sub-problems in order, verifying each before moving on. (1) Open recovery.sql and look at the tail — a crash usually leaves a truncated/garbled statement; fix just the broken line(s) rather than rewriting the file, then load with `sqlite3 sensor.db < recovery.sql` and confirm sane rows via `SELECT * FROM readings;`. (2) Run `make` and read the actual compiler error — likely an invalid/unsupported flag in the Makefile; remove or correct it rather than guessing new ones. (3) The naive variance formula `(sum_sq - sum*sum/n)/(n-1)` loses precision when values share a large common offset because sum_sq and sum*sum/n nearly cancel; replace it with a two-pass approach (mean first, then sum of (x-mean)^2) or Welford's algorithm, keeping `printf("%.6f\n", var)` as the sole output. (4) For the test script, pull values with `sqlite3 sensor.db "SELECT value FROM readings;"`, pipe into ./variance, and compare against an independently-computed expected variance (e.g. awk using the same stable method) to decide exit 0/1. Finally pipe the recovered values through fixed ./variance and save the number into final_result.txt.