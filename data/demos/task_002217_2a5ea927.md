Begin by inspecting the schema before writing any queries: run `sqlite3 /home/user/network.db ".schema"` (or `.tables` then `.schema <table>`) to see how authors, papers, and authorship links are modeled — don't assume column names. Once you know the join table connecting authors to papers, build a co-authorship graph in your script by querying pairs of authors that share a paper_id, then run a standard BFS (not DFS, to guarantee shortest path and match the "exactly one unique shortest path" guarantee) from "Alice Smith" to "Bob Jones", tracking parent pointers to reconstruct the path. For the aggregation step, once you have the ordered list of authors on that path, query all paper IDs authored by any of them and take the set union (not sum, to correctly dedupe papers with multiple path-authors) to get total_unique_papers. Capture the actual SQL query you use for the co-authorship join/lookup, prefix it with `EXPLAIN QUERY PLAN`, run it, and write the raw output to /home/user/query_plan.txt (it should mention SCAN or SEARCH). Finally serialize shortest_path and total_unique_papers into /home/user/summary.json with exactly the two specified keys, and validate with `jq` before finishing.