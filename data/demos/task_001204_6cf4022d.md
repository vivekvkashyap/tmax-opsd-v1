Use the MongoDB Go driver's aggregation pipeline builder (`bson.A{bson.D{{...}}}`) rather than implementing traversal or aggregation logic in Go application code, since the task explicitly forbids Go-level graph walking. For `/api/reachable`: build a `$graphLookup` stage with `startWith: "$to"`, `connectFromField: "to"`, `connectToField: "from"`, `maxDepth: 2`, `as: "reachable"`, matching on `from: <account>`, then flatten and dedupe the `to` values across the matched documents plus the graphLookup results — the "up to 3 hops" spec with `maxDepth: 2` (0-indexed) is the key detail to get exactly right; test manually against the seeded data to make sure a 3rd-hop neighbor is included and a 4th-hop one is not. For `/api/volume`: use `$facet` or two `$group` stages unioned via `$unionWith`/`$group` summing amounts where the account is `from` and separately where it's `to`, merge into one total per account (a `$group` with `$sum` over a projected/unwound "from" and "to" pair is a clean approach), then `$sort` by volume descending and account ascending for ties, and apply `$skip`/`$limit` for pagination inside the pipeline (not in application code). Validate query parameters up front and return 400 on missing/invalid ones. Run the server backgrounded and confirm both endpoints with `curl` against the seeded data before finishing.