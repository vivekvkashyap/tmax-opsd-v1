In Go, open the tar with `archive/tar.NewReader` and iterate `Next()` entries manually rather than using a one-shot "extract all" helper — for each header, sanitize the path by rejecting (not extracting) any entry whose `Name` contains `..` as a path segment or is absolute (starts with `/`); use `filepath.Clean`/`filepath.IsAbs` checks, then also verify the cleaned join of restore-dir + name still has the restore-dir as a prefix (defense against symlink/traversal tricks) before writing. Log each rejected entry's original archive path (as stored in the header, not the cleaned version) to `/home/user/rejected.log`, one per line. For accepted entries, write the raw bytes to `/home/user/restored/<path>` preserving directory structure. Then do a second pass over every extracted file: detect encoding (a UTF-16LE BOM `FF FE` at the start is a strong signal; otherwise treat as Windows-1252) and transcode to UTF-8 — Go's `golang.org/x/text/encoding` package (or shelling out to `iconv`) handles this; strip BOMs after conversion. Normalize CRLF to LF with a simple byte replace (`\r\n` -> `\n`), and do a plain string replace of `SERVER_LEGACY` -> `SERVER_MODERN`. Do the encoding/EOL/replace work in whatever order avoids corrupting multi-byte sequences — convert to UTF-8 first, then do line-ending and string transforms on the UTF-8 text.