Chain standard CLI tools in the exact order the spec implies, since order affects the reproducible sample. First filter for schema validity with a strict regex anchored on both ends, e.g. `grep -E '^[^,]+,[0-9.]+,[0-9.]+,[0-9.]+$' /home/user/artifacts.csv` — this naturally drops the header (has non-numeric column names), rows with "N/A"/"error" text, and rows with fewer than 4 fields (missing trailing commas won't match). Pipe that into `shuf -n 30 --random-source=/home/user/random_seed` exactly as specified — using a different random source or a different filter order will desync your sample from the graded reference, so don't substitute `sort -R` or omit `--random-source`. Finally pipe the 30 sampled lines into `awk -F',' '{a+=$2; l+=$3} END {printf "Mean Accuracy: %.4f, Mean Loss: %.4f\n", a/NR, l/NR}'` and redirect straight to `/home/user/evaluation_summary.txt`, being careful the file contains only that one line with no extra echoes or blank lines. Verify by rerunning the same pipeline and diffing.