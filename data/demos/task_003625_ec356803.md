Treat this as three independent filters whose intersection is the answer, and implement each with a script you can spot-check before combining. (1) Symlink-loop detection: for each `/home/user/artifact_store/art_<ID>/` directory, walk it without following the same loop forever — use `find <dir> -L` with a depth/visited-inode guard, or Python's `os.walk(followlinks=False)` combined with explicit `os.path.realpath`/inode tracking to detect a cycle, rather than naively following symlinks (which will hang). (2) Status parsing: the log is multi-line stanzas per artifact separated by `---`; parse it block-by-block (e.g. split on `---`, then regex/extract `Artifact:` and `Status:` from each block) rather than line-by-line regex across the whole file, and exclude any `Status: DEPRECATED`. (3) Frame analysis: use `ffmpeg` to extract all 600 frames (or `cv2.VideoCapture` to iterate them) mapping frame index i to `art_<i:03d>`, crop the top-left 50x50 pixel block of each, and compute the channel-wise mean; flag as corrupted only if R>200 and G<50 and B<50 by the stated averaging rule. Intersect the three "clean" sets, sort, and write one ID per line to `/home/user/valid_artifacts.txt`. Validate each filter on a handful of manually-inspected examples before running the full 600-artifact pass, since your F1 threshold is strict.
