For the Rust piece, use `std::process::Command::new("ffmpeg").arg("-i").arg("/app/input.wav")...` to build the exact argument list (include `-y` to allow overwrite, `-b:a 32k`, `-ac 1`) and use `.status()` or `.output()` checking success rather than firing-and-forgetting the process. For `deploy.sh`, make every step idempotent: `mkdir -p` for dirs; before generating the TLS cert with `openssl req -x509 -newkey rsa:2048 -nodes -days 365 -keyout key.pem -out cert.pem ...`, guard with `if [ ! -f /home/user/certs/cert.pem ]` so reruns don't regenerate it; write the Nginx server block to its own config file and only add an `include`/symlink to sites-enabled if not already present (grep before appending) rather than duplicating; run `setfacl -R -m u:www-data:rx /home/user/srv/media` plus a default ACL so newly-added files inherit read access, then `nginx -t && systemctl reload nginx || systemctl restart nginx`. Only disable the plaintext `listen 80` server block, keep TLS on 4433. Verify end-to-end yourself with `curl -k https://127.0.0.1:4433/output.mp3 -o /tmp/check.mp3` before considering it done — the grader measures PSNR against the original, so don't over-compress beyond the specified 32k/mono settings.
