Begin by extracting the rules from the image with `tesseract /app/guidelines.png stdout` (try a couple of `--psm` modes if the first pass looks garbled) — read the OCR output carefully to identify the per-column outlier thresholds, the imputation strategy (e.g. mean/median), and the target PCA dimensionality; don't assume these values, confirm them from the actual OCR text. Then set up a Go module in `/home/user/workspace` (`go mod init ...`, `go get gonum.org/v1/gonum/...`), and implement the pipeline in clear stages: (1) parse the CSV, (2) drop rows violating the extracted outlier thresholds, (3) impute remaining missing values per-column using the extracted strategy computed only over the post-outlier-removal valid values, (4) mean-center the cleaned/imputed matrix and compute its covariance matrix, (5) eigendecompose the covariance matrix (gonum's `mat.EigenSym` for a symmetric matrix) and take the top-k eigenvectors matching the target dimension count, (6) project data onto those components and reconstruct by projecting back and adding the column means. Double-check sign/ordering conventions of eigenvectors don't matter for reconstruction (projection then back-projection is sign-invariant), but eigenvalue ordering (largest first) does matter — sort by descending eigenvalue before selecting components. Write the reconstructed 4-column data, one row per cleaned input row in original order, each value formatted to exactly 6 decimals, comma-separated, no header, to `/home/user/reconstructed.csv`.