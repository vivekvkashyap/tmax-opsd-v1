Work through this as four separable fixes. First get `/app/src/server.cpp` compiling: read the compiler/linker errors carefully — a common cause is a missing threading header plus a missing `-lpthread` link flag, so add the needed `#include` and pass `-pthread` (or `-lpthread`) to `g++`. Second, recover the auth token by actually opening `/app/auth_spec.png` (view it as an image, or OCR it with `tesseract auth_spec.png -` if available) and reading the token text out of the design doc — do not guess a token; use exactly what the image shows in the server's `AUTH <token>` comparison. Third, analyze `/app/traffic.pcap` with `tcpdump -A -r traffic.pcap` or `tshark -r traffic.pcap -x` to see the exact byte sequence of the payload that follows `DATA` in the crashing packet, then find the unsafe parsing call in the C++ code (likely an unbounded `strcpy`/`memcpy` or an unchecked length assumption) and replace it with bounds-checked copying that returns `ERROR\n` on malformed input instead of crashing. Fourth, find where a per-request heap buffer is allocated with `new`/`new[]` and never freed on the success path, and add the matching `delete`/`delete[]` before returning `OK\n`. Recompile, then start the server backgrounded and listening on `127.0.0.1:9000`, and manually test all three response cases with a raw TCP client (e.g. `nc` or a small script) before finishing.
