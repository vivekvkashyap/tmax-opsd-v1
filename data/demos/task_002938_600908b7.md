Build this as two cooperating pieces and wire them together carefully. First set up an unprivileged Nginx: write nginx.conf with `pid /home/user/nginx/nginx.pid;`, error/access logs under /home/user/nginx/logs/, a server block on port 8080 proxying to an `upstream` name, and `include` the separate upstream.conf file (write it with a placeholder server first so nginx can start) — create all directories before launching, and start nginx with `-c /home/user/nginx/nginx.conf` (no sudo needed since ports/paths are unprivileged). Then write the C++ monitor: loop every 2s, use libcurl to GET `http://127.0.0.1:<port>/health` for each of 9001-9003, treat any non-200 or connection failure as DOWN. Regenerate upstream.conf's `server 127.0.0.1:<port>;` lines only for UP backends each cycle, and only send SIGHUP to the pid read from nginx.pid when the healthy-set actually changed from the previous check (track previous state). Append one line per check to uptime.log in the exact specified format. Compile with g++ linking -lcurl, launch nginx first, then start the monitor in the background (nohup/disown), and verify end-to-end with curl against 8080 before finishing.
