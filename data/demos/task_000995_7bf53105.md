Reverse-engineer the emulator black-box first: run `strings /app/schema_emulator` (it may be UPX-packed — try `upx -d` on a copy first, or fall back to strings/black-box testing if unpacking fails) looking for suspicious tokens beyond the expected `TABLE`/`COLUMN`/`INDEX` schema keywords — search specifically for anything resembling exec/shell/hook/curl-style directives. Confirm your hypothesis by crafting small `.mig` test files containing candidate keywords (with bracketed payloads) and observing whether the emulator's behavior changes (e.g., side effects, output, exit behavior) versus a clean file — this validates the exact trigger syntax rather than guessing from strings alone. Once you've confirmed the unsafe directive keyword(s) and their bracketed-argument syntax, write `/home/user/classifier.py` to scan each `.mig` file's raw text for those exact marker patterns (handle the fact that the injection may be embedded anywhere in a line, not just at line start, and be case-sensitive to what you observed) and copy matching files to the unsafe output dir, everything else to the safe output dir, using `shutil.copy` (never delete originals). Test your classifier against your own held-out safe/unsafe samples — not just the given `raw_migrations/` — since the hidden eval corpus will include obfuscated variants of the same directive.
