First locate the injected slowdown: grep the vendored source for suspicious sleeps, e.g. `grep -rn "sleep" /app/huey-src/huey/` — a debugging `time.sleep(...)` call inside the consumer's polling/task-processing loop (likely `huey/consumer.py`) is throttling every task. Remove that line, then install the fixed package into the environment your app/consumer actually uses with `pip install -e /app/huey-src` (editable install, so the fix takes effect without copying files). Next write `/home/user/app.py` defining a `SqliteHuey` named `huey` pointed at `/home/user/huey.db`, with a `@huey.task()`-decorated `process_data(n)` that hashes `str(n)` with SHA256 iteratively 100 times and returns the final hex digest. For supervision, write a systemd user unit at `~/.config/systemd/user/huey-worker.service` whose `ExecStart` runs `huey_consumer.py app.huey` with `-w 4` (4 workers), `Restart=always`, `RestartSec=3`; reload the daemon, then `enable --now` it under `systemctl --user`, ensuring linger/user session is active so it keeps running. Finally run `/app/benchmark.py`, redirecting stdout to `/home/user/benchmark_result.txt`, and confirm the reported time is under the 4.0s threshold — if not, re-check for other throttling code paths.
