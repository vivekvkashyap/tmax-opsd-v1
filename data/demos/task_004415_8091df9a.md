Split this into infra fix, model training, and streaming glue. First, `grep proxy_pass /app/services/nginx.conf` to find the upstream port Nginx forwards to and correct it to match the Flask/gunicorn port actually bound in `start.sh` (they must agree — read `start.sh` to see which port Flask really listens on), then rerun `start.sh` (or reload nginx) and confirm with `curl -X POST http://127.0.0.1:8080/ingest` that traffic reaches Flask instead of getting connection-refused/404. Second, for `filter.py`, load `/home/user/data/train.csv`, build a text classification pipeline (TF-IDF vectorizer + a linear classifier like Logistic Regression or LinearSVC is a strong default for keyword-driven adversarial text), and use cross-validation/grid search over regularization strength to pick the best-generalizing model rather than just fitting once — inspect which n-grams/tokens the model weights most heavily to sanity-check it's picking up on genuine injection/attack signals, not spurious correlations, since it will be graded on a held-out adversarial corpus requiring 100% separation. Expose `is_clean(text: str) -> bool` wrapping `vectorizer.transform` + `model.predict`. Third, write `etl_worker.py` as a simple blocking loop: `BLPOP chat_queue` from Redis, `json.loads` the payload, call `is_clean`, and `requests.post` clean items to the fixed Nginx endpoint, silently dropping evil ones — test manually with `redis-cli LPUSH` before leaving it running.