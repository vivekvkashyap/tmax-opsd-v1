Since the schema is undocumented, start by introspecting rather than guessing: run `sqlite3 /home/user/company.db ".tables"` and `.schema <table>` (or query `sqlite_master`) to find the real (obfuscated) table/column names, and use `PRAGMA foreign_key_list(<table>)` to confirm which column is the self-referencing manager pointer. Once you know the columns, compute descendant counts with a graph traversal rather than a single flat SQL query: load all employee rows into Python, build a manager -> [direct reports] adjacency map, then for every employee do a BFS/DFS over that map counting all reachable nodes (their full subtree, not just direct reports) — a recursive CTE also works if you prefer pure SQL, but be careful it counts indirect reports too. Join against the department table to filter to 'Engineering', and pick the max by descendant count (verify there's a single unambiguous max, and settle ties by the department/business rule of "highest" if none). To build `chain_to_ceo`, walk upward from that employee via the manager pointer until it's NULL, collecting names in reverse then flipping the list so the CEO (root) comes first and the target employee is last. Write the JSON with the exact key names and types specified, then reload it yourself to confirm structure before finishing.
