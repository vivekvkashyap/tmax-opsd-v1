Read `/home/user/prepare_data.py` and locate the exact ordering of operations: it currently fits `StandardScaler` on the full merged dataframe and only afterward calls `train_test_split`. This is the leak — statistics from the held-out rows influence the scaling of the training rows. Fix by reordering: merge the CSVs, call `train_test_split(df, test_size=0.2, random_state=42)` first (unchanged params, so row membership stays identical), then instantiate `StandardScaler()`, call `.fit_transform()` only on `train[['age','income']]`, and use the already-fitted scaler's `.transform()` (not `fit_transform`) on `test[['age','income']]`. Round both scaled columns to 4 decimal places with `.round(4)` before writing. Save with `to_csv(..., index=False)` to `/home/user/train_clean.csv` and `/home/user/test_clean.csv`. Sanity-check yourself the way the grader will: after fixing, the training set's scaled `age`/`income` columns should average to ~0 (since they were the fitting set), while the test set's average will generally NOT be exactly 0, because it was only transformed, not fit. Run the script and inspect both output files before finishing.