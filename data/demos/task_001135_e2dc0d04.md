Fix the C bug first: in `classifier.c`, after tokenizing each line, add an explicit string check on the feature token (e.g. `strcmp(feat_str, "NaN") == 0`) and `continue`/skip that row entirely before calling `atof()`, in both the training-mean computation and the test/accuracy loop — do this everywhere `atof` is called on the feature field, not just once. Recompile with `gcc -o /home/user/classifier /home/user/classifier.c`. For the CV script: strip the CSV header with `tail -n +2`, use `split -l 20 -d` (or similar) with prefix `fold_` to make 5 files of 20 rows each, then loop i=1..5, using `cat` to concatenate the other four fold files into a temp training file and the current fold as the test file, run `./classifier <train> <test>` capturing its printed float, sum the 5 accuracies in bash with `awk`/`bc` (since bash arithmetic is integer-only), divide by 5, and format the result to exactly 2 decimal places with `printf "%.2f"` into `/home/user/final_accuracy.txt`. Verify the script is idempotent and cleans/regenerates fold files each run.
