Implement the trapezoidal-rule TVD as a single awk pass over the paired density columns rather than trying to do it in bc alone. First confirm the GC_Fraction columns in both TSVs are row-aligned (same x-values in the same order) — if so, you can use `paste` to merge the two files' Density columns line-by-line after stripping headers. In awk, for each pair of consecutive rows i and i+1, compute the absolute difference |P-Q| at both points, then accumulate `(diff[i+1]+diff[i])/2 * (x[i+1]-x[i])` into a running sum — this is the trapezoidal formula applied directly to the absolute-difference curve, which is mathematically equivalent to (but simpler than) integrating P and Q separately and subtracting. Multiply the final accumulated sum by 0.5 to get the TVD. Be careful with awk field separators (tab-delimited, skip the header line with `NR>1`), and format the final number to exactly 4 decimal places using `printf "%.4f\n"`. Write only that formatted number, with no extra text or trailing content, to `/home/user/tvd_result.txt`.