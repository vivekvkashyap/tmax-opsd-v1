Treat this as two independent, precisely-specified components since the C program is fuzz-tested bit-for-bit against a hidden oracle. First, extract per-frame mean luminance from the 64x64 grayscale video: decode raw frames with ffmpeg (e.g. `-pix_fmt gray -f rawvideo`) in chronological order, average each frame's 4096 pixel bytes, and write one decimal value per line to frame_means.txt. Second, implement analyze.c to read all whitespace-separated doubles from stdin into an array (x-index = position), then: (a) compute ordinary least-squares slope/intercept using the standard sum formulas (sum_x, sum_y, sum_xy, sum_xx over N points); (b) implement the exact specified linear congruential generator (`state=42`, update `state = (state*1103515245+12345) & 0x7FFFFFFF`) and use `next_rand() % N` to draw N bootstrap indices per iteration, in order, for exactly 1000 iterations, recomputing the slope on each resample (falling back to 0.0 when the resampled x-variance is zero); (c) sort the 1000 bootstrap slopes with qsort and take the values at indices 24 and 974 as the CI bounds. Match the printf format string exactly, character for character, since output is diffed against the oracle. Compile, then run `./analyze < frame_means.txt > video_analysis.txt`.
