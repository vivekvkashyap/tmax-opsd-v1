Do this in pandas, stepping through each transformation explicitly rather than chaining blindly. Load `/home/user/raw_data.csv`, then `df['upvotes'] = df['upvotes'].fillna(0)`, and convert both `review_id` and `upvotes` to strict ints — note that a column with NaN gets silently upcast to float64 by pandas, so after filling you must explicitly `.astype(int)` (or `.astype('Int64')` then cast) on both columns, including handling string-typed review_id values like `"103.0"` (cast via float first, e.g. `df['review_id'].astype(float).astype(int)`). For tokenization: lowercase the text, strip all non-alphanumeric/non-whitespace characters with a regex substitution (e.g. `re.sub(r'[^a-z0-9\s]', '', text)`), then `.split()` on whitespace to get tokens — compute `token_count` as the list length and `avg_token_length` as mean character length per token, explicitly guarding the zero-token case to output `0.0` instead of dividing by zero. Compute `df[['user_score','upvotes','token_count','avg_token_length']].corr(method='pearson').round(4)` and write it out via `.to_dict()` to JSON. Save the cleaned frame with `to_csv(..., index=False)` in the exact column order specified.