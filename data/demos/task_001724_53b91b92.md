Build the pipeline in stages and test each independently before wiring them together. First, `go mod init` in the workspace and add the sqlite3 driver dependency; write a tiny standalone check that opens the DB and runs a parameterized `SELECT name FROM nodes WHERE id = ?` (never string-concatenate the ID into SQL). Second, parse edges.csv into an adjacency list (map[int][]int, remembering edges are undirected so add both directions) and run a plain BFS from `--source`, tracking parent pointers to reconstruct the shortest path to `--target` once found — BFS guarantees shortest edge-count, unlike DFS. Third, load topics.json into a map[string][]string (keys are strings even though IDs are ints, so convert accordingly) and default to an empty slice, not nil, when a node ID is absent so it serializes as `[]` rather than `null`. Finally build the ordered `path` array by walking your BFS parent chain from source to target, look up each node's name and topics, and marshal to the exact output schema before writing to /home/user/output/path.json. Run the compiled binary with the required flags and inspect the file for valid JSON and the expected node ordering.