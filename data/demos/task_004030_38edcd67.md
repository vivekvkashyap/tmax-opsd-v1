Run `tesseract /app/model_params.png stdout` (or similar OCR invocation) first and read its output carefully to extract the three numeric parameters (imputation value, outlier threshold, and the two weights) exactly as printed — don't assume typical/round values, use what OCR actually returns, and sanity-check the text for OCR misreads (e.g. confusing 0/O, 1/l) before hardcoding them into the script. Then write `/home/user/pipeline.sh` to: split the single CSV-string argument on commas into `id`, `col2`, `col3`; test `col2` for emptiness or the literal string `NaN` and substitute the imputation value; use `awk` or `bc` for the `col3 > threshold` comparison (shell `[ ]` can't do floats) and clamp if needed; compute `final_score = w2*col2 + w3*col3` with `bc -l` or `awk`, rounding to exactly 2 decimals with `printf "%.2f"`; print the four fields comma-joined. Make it executable and test it manually against a handful of edge cases (empty col2, "NaN" col2, col3 above/below/equal to threshold) before assuming it matches the hidden oracle across the full fuzz distribution.
