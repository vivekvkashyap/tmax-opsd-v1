Begin by exploring the schema: run `sqlite3 /home/user/audit_system.db ".schema"` to discover the actual table and column names (do not assume names — read them from the database). Check existing indices with `.indices <access_log_table>`; if the foreign-key column linking access logs to employees isn't indexed, create it with `CREATE INDEX idx_emp_access ON <table>(<fk_column>);` via a bash/sqlite3 command. Then run `EXPLAIN QUERY PLAN SELECT ...` for the join query and redirect its exact output to `/home/user/query_plan.txt`. For the C program, either link against `libsqlite3-dev` and query via the SQLite C API, or shell out with `sqlite3 -separator '|' db "SELECT ..."` and parse the pipe-delimited output in C — the latter is simpler for JSON string handling. To parse the `projects` array inside each clearance JSON blob, avoid a full JSON parser: do a substring search for `"<ProjectName>"` inside the clearance text field for each access-log project name; if not found, it's a violation. Collect violations, sort them by employee name then project name (e.g. via `qsort` with a comparator, or pipe through `sort` in the C-invoked shell), and write lines exactly as `[Employee Name] unauthorized access to [Project Name]` to `/home/user/violations.log`. Compile with `gcc ... -lsqlite3` and run it.
