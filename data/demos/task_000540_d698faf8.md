Write a small INI-style parser (simple line splitting on `=` and `[section]` headers is enough) to load `/home/user/pipeline.conf` into key/value pairs. For file selection, use `std::filesystem` to iterate the input directory, filter by suffix match on `Extension`, and check `last_write_time` converted to a `year <= MaxYear` boundary (build a comparison timestamp for Dec 31 of MaxYear, e.g. via `std::chrono` or `mktime`) — sort the matching filenames lexicographically before merging so output is deterministic. For encoding conversion, use `iconv_open("UTF-8", "WINDOWS-1252")` then `iconv()` in a loop over each file's bytes, watching for buffer-size/leftover-byte edge cases. Concatenate all converted file contents' lines in file order, then chunk them: buffer lines and flush every `LinesPerChunk` lines to a new file named `<Prefix><NNN>.txt` with zero-padded 3-digit index (use `std::setw(3) << std::setfill('0')`), flushing the final partial chunk too. Compile with `g++ -liconv` (or link iconv as needed on the platform), then run the binary against the existing `pipeline.conf` and inspect `/home/user/processed_data/` to confirm chunk count, line counts, and UTF-8 encoding via `file`/`grep`.