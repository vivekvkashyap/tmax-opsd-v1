Do the OCR step first and confirm the recovered value before writing any validation logic around it: run `tesseract /app/build_architecture.png stdout` (or similar OCR tool), inspect the raw extracted text, and pull out the value following `BUILD_TOKEN=` — don't hardcode a guessed token, derive it from the actual OCR output and print it during development to confirm it looks like a clean token before wiring it into your script. For the orchestrator, parse the `build://action?query` URL with Python's `urllib.parse` (`urlparse` + `parse_qs`) rather than regex/string-splitting, since query parsing needs to handle `&`-separated key=value pairs robustly. Validate strictly and in this order: token must match exactly, action must be one of the three allowed literals, target must match `^[A-Za-z0-9_]+$` (reject any slash, dot, or path-traversal attempt), and flags must match `^[A-Za-z0-9\- ]+$` (reject `;`, `&`, `|`, `$`, `>`, `<`, backticks — treat any character outside the whitelist as reject, don't blacklist-only). On any failure print exactly `REJECT`; on success print `ACCEPT: [{action}] built {target} with {flags}`. Test against every file in `/app/corpus/clean/` and `/app/corpus/evil/` directly and iterate on your regex until all pass, since a single overlooked injection character is the typical failure mode.
