Work through the four requirements in dependency order and verify each before moving to the next. First fix the Makefile: inspect it for the classic breakages — recipe lines must use a literal TAB (not spaces) for indentation, and building a shared object needs `-shared -fPIC` (and `-o libmathops.so`); run `make` and confirm the `.so` exists and is a valid ELF (`file libmathops.so`). Next write `test_mathops.py` using `ctypes.CDLL('./libmathops.so')`, declaring `argtypes`/`restype` for `add_safe`/`multiply_safe`, and use `hypothesis.given(st.integers(-1000,1000), st.integers(-1000,1000))` to assert the C wrapper matches native `+`/`*` for both functions — run via `pytest -v > pytest_results.log` and check it reports both tests passing. Finally for `check_version.sh`, call `get_version()` through a small inline `python3 -c "..."` ctypes snippet, capture the returned string in a bash variable, then compare it to `2.0.0` using `printf '%s\n%s\n' "$v" "2.0.0" | sort -V | head -1` (if the lower value is `2.0.0`, extracted version is >=) to decide between writing `VERSION_OK` or `VERSION_TOO_OLD` to `version.log`, then execute the script so the log file is actually produced.
