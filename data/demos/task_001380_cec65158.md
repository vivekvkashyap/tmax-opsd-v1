Work through the four layers independently and verify each before moving to the next. For Step 1, run `EXPLAIN QUERY PLAN` on the given join query against `/home/user/backups.db` to see it's doing full table scans on `deps`; then `CREATE INDEX` on `deps(source)` and `deps(target)` (both, since the query joins on each) and re-run `EXPLAIN QUERY PLAN` to confirm SQLite now uses the index (look for "USING INDEX" in the plan output) rather than assuming it worked. For Step 2, pull the optimized query's rows with Python's `sqlite3` module, build a `networkx.DiGraph` by adding an edge `source -> target` per row, run `nx.pagerank(G, alpha=0.85, max_iter=100)`, and sort by score descending to grab the top 5 node IDs. For Step 3, treat `/app/key_deriver` as a black box: call it via `subprocess.run(["/app/key_deriver", job_id], capture_output=True, text=True)` for each of the top-5 IDs and capture stdout as the key — don't try to reverse-engineer its internals, just invoke it directly since the task only requires calling it correctly. For Step 4, serve the resulting `{job_id: key}` dict as JSON from a `GET /critical_keys` handler bound to `0.0.0.0:8000`, launched in the background (e.g. `nohup python3 server.py &`) so the verifier can query it after your commands return; test with `curl` yourself before finishing.
