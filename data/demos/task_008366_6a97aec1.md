Install OpenBLAS dev headers first (`apt-get install libopenblas-dev` or equivalent) and confirm `cblas.h` is discoverable before writing C. Parse each CSV line manually, splitting on commas and counting fields — reject/skip any users.csv line that doesn't have exactly 6 fields (ID + 5 floats) or items.csv line that doesn't have exactly 4 fields (ID + 3 floats); use `strtod`/`sscanf` and check conversion succeeded rather than trusting malformed numeric strings silently parse to 0. Load weights.csv into a 3x5 row-major double array. For the BLAS call, `cblas_dgemv(CblasRowMajor, CblasNoTrans, 3, 5, 1.0, W, 5, u, 1, 0.0, p, 1)` computes `p = W*u` correctly for a row-major 3x5 matrix — get the leading dimension (5, the column count) and `M`/`N` order right, since a common bug is transposing rows/cols or using the wrong leading dimension. For similarity search, brute-force compute squared Euclidean distance from `p` to each valid item's 3D embedding and track the minimum (ties: whichever comes first in file order, unless the task implies otherwise — check by hand-computing the example data). Preserve users.csv row order in output, compile with `gcc ... -lopenblas -lm`, and actually run the binary to produce `/home/user/recommendations.csv`.