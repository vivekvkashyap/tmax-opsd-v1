Diagnose first: run `systemctl --user status voicemail-transcriber.service` and `journalctl --user -u voicemail-transcriber.service -e` to see the actual failure (likely a restart-loop from `StartLimitBurst` being hit because `RestartSec` is 0/too low, or `Type=` missing so systemd never sees a clean success, or the unit writing to a directory the user can't write). Fix the unit file: set an explicit `Type=` matching how the script exits (e.g. `oneshot` if it runs once), `Restart=on-failure`, `RestartSec=5`, and raise/adjust `StartLimitIntervalSec`/`StartLimitBurst` so legitimate retries aren't blocked. Create `/home/user/logs/transcriber/` and `/home/user/processed/` with the user as owner and write permissions, and route stdout/stderr with `StandardOutput=append:/home/user/logs/transcriber/service.log` (not a root path). Fix `transcribe.sh` to write to these correct paths and to call the available transcription tool (`whisper-cli` or the python `openai-whisper` equivalent) on `/app/voicemail.wav`, stripping timestamp markers/segment numbers from the tool's output so only plain text lands in `/home/user/processed/transcript.txt`. Add a `/home/user/logrotate.conf` stanza for that log file with `daily`, `rotate 7`, `compress`. Reload, enable, start, and confirm the run actually completes and produces an accurate transcript.
