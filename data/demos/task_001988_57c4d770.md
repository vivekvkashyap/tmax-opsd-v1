Structure this as: bring up infra, implement graph logic in Rust, then validate against the corpus. Start the compose stack (`docker-compose up -d` in `/home/user/services`) and confirm Postgres (`monitoring` db, user `admin`/`secret`, port 5432) and Redis (port 6379) are reachable before writing any Rust. Scaffold with `cargo new deadlock_detector`, add a CSV reader, and build a directed graph where edges go `waiting_for_tx_id -> tx_id`. Detect cycles via DFS with recursion-stack tracking (a node revisited while still on the stack is a cycle), not just a visited-set, which would miss or over-report cycles. If a cycle exists, restrict centrality to cycle members: compute in-degree centrality (transactions transitively waiting on each node) among them, pick the max, break ties by lowest tx_id. Look up the query via `HGET queries <query_hash>` in Redis, then `INSERT INTO deadlock_reports` via a Postgres crate. Exit 1 on cycle found, exit 0 on clean DAG. Before finishing, run your binary against several files in both `/home/user/corpora/clean/` and `/home/user/corpora/evil/` to sanity-check exit codes.