Build the pieces in the order they're checked: create `/home/user/storage/net_logs`, then a symlink `/home/user/logs -> /home/user/storage/net_logs` (`ln -s`), then `/home/user/mail`, then write the exact fstab line verbatim into `/home/user/my_fstab` (no extra whitespace changes — it will be grep-matched literally). For `net_alert.sh`, parse each log line by extracting the value after `IP=` and after `STATE=` (e.g. with `awk` field splitting or `grep -oP`), and for every line where STATE is DOWN append a line of the exact form `ALERT: Host <ip> is DOWN` to `/home/user/mail/alerts.txt` — get the wording and spacing exactly right since it will be diffed. After processing, rotate by moving the input file to `<path>.bak` (overwrite any existing one) and then creating a fresh empty file at the original path — do the rotation as the last step so the read/parse happens against the original content first. Make the script executable with `chmod +x`. Test it manually against a small multi-line sample log before considering it done.
