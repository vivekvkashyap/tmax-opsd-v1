Work through this in order. First, integrity check: compute `sha256sum /home/user/app.py`, compare it against the hash text stored in `/home/user/manifest.txt`, and write exactly `VALID` or `INVALID` to `/home/user/integrity.log` — do this with a shell one-liner, not by eyeballing. Second, read `app.py` to find the JWT verification logic; if it accepts `alg: "none"` without checking a signature, you can forge a token yourself: base64url-encode a header `{"alg":"none","typ":"JWT"}` and a payload containing `"role":"admin"`, join them with dots and leave the signature segment empty (JWT with `alg=none` still needs the trailing dot, e.g. `header.payload.`). Start the app in the background (`python3 /home/user/app.py &`), wait for the port to open, then curl `/api/reports` with `Authorization: Bearer <forged token>`. Finally, parse the returned JSON structure exactly as received, and regex-replace every SSN-shaped substring (`\d{3}-\d{2}-\d{4}`) inside string values with `[REDACTED]` while preserving all other structure, then write the modified JSON to `/home/user/redacted_reports.json`. Do not touch `app.py` itself.