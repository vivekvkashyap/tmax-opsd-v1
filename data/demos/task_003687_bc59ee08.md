Read the panic message and the existing variance code in src/main.rs first — the naive formula computes variance as (sum_of_squares - sum^2/n)/n, which loses precision to catastrophic cancellation when values share a huge common offset (small variance riding on a large magnitude), occasionally producing a tiny negative number that trips an assert. Don't just clamp or abs() the result — that hides bad math and won't be numerically correct for other inputs. Instead replace the algorithm with a numerically stable one: either a two-pass approach (compute the mean first, then average the squared deviations from that mean) or Welford's online algorithm (running mean/M2 update per element, variance = M2/n at the end). Apply this per CSV line (each line is one batch of comma-separated floats), keep population variance (divide by n, not n-1). Rebuild with `cargo build --release`, run the binary against /home/user/sensor_data.csv, and redirect stdout to /home/user/fixed_output.txt with one variance value per line, verifying none are negative and each is close to what a manual two-pass calculation by hand would give.
