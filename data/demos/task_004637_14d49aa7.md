Fix the C leak first: the `malloc`'d result pointer must be `free`'d after it's used (right after `printf`, before `return`), on every code path. Add `-g` to the `Makefile`'s gcc invocation for debug symbols. Then write `ci_test.sh` to: run `make` to rebuild, then read `tests.txt` line by line (`while IFS=, read -r id a b op`), invoke `valgrind --leak-check=full ./calc "$a" "$b" "$op"` capturing both the program's stdout (the numeric result) and valgrind's stderr output separately — e.g. redirect valgrind's diagnostics to a temp file or capture with `2>&1` and separate streams carefully so you can parse the numeric result cleanly. Apply the constraint: if the numeric result is strictly greater than 50, skip that test case entirely (don't write any report line for it). For tests that pass the constraint, grep the captured valgrind output for the line containing "definitely lost:" and check whether the byte count is exactly 0 to decide PASS vs LEAK. Append formatted lines to `ci_report.txt` in the exact `[id] result=<value> status=<PASS|LEAK>` format, then run the script once to populate the report.