Parse `/home/user/structure.pdb` using fixed-column slicing exactly as PDB format dictates — in 0-indexed Python that's `line[12:16].strip()` for the atom name (1-based columns 13-16) and `line[60:66]` for the B-factor (1-based columns 61-66); do not split on whitespace, since PDB fields are fixed-width and can abut. Only process lines starting with `ATOM`. Accumulate `CA` and `CB` B-factors into separate lists as floats. For the mean, use `math.fsum(ca_list) / len(ca_list)` rather than naive summation/numpy `.mean()` to satisfy the precision-preserving requirement, and format with Python's `f"{value:.6f}"` before writing to `/home/user/ca_mean.txt`. For the hypothesis test, use `scipy.stats.ttest_ind(ca_list, cb_list, equal_var=False)` (Welch's t-test, two-sided by default) and write the p-value formatted the same way to `/home/user/p_value.txt`. Since the p-value will likely be extremely small, verify how `.6f` formatting rounds such values (it may legitimately format to all zeros) rather than assuming something went wrong.
