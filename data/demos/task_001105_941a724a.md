Build this as a pipeline of independently-verifiable stages. First, extract frames with ffmpeg using video/pixel-format filters that force exact 64x64 grayscale raw output, e.g. `ffmpeg -i experiment.mp4 -vf scale=64:64 -pix_fmt gray -f rawvideo frames/frame_%03d.raw` — check the numbering matches the required 1-indexed, zero-padded `frame_001.raw` pattern exactly. In the C program, read each raw file as a flat 64*64 byte buffer, then compute the 8x8 reduced feature vector by averaging each non-overlapping 8x8 pixel block using integer division (be consistent about truncation vs rounding, and use it consistently for both storage and the confidence-interval pixel sum). For similarity, compute squared Euclidean distance in the 64-dim reduced space between the query frame and every other frame, keep a running top-3 via a small sorted array/partial selection rather than sorting everything if you want speed. For the CI, compute the global mean over all raw pixel values across all frames, and per the spec compute standard error as population std-dev of the per-frame means divided by sqrt(N=number of frames), then multiply by 1.96 for the interval — get this formula right since the verifier recomputes it independently. Implement a simple blocking or forking TCP server on 127.0.0.1:9090 that parses the 3-digit index, replies with the exact `SIMILAR=%03d,%03d,%03d CI=[%.2f,%.2f]\n` format, and closes the connection. Record your exact compile+launch commands in `run_pipeline.sh` and leave the server running.