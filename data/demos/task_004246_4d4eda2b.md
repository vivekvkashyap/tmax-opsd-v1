For the WAL parser, use Python's `struct` module to read the fixed binary header per record — `>Q` (8-byte timestamp), `>B` (1-byte type), `>I` (4-byte payload length) — then read exactly that many payload bytes, looping until EOF; keep only records whose type byte equals `0x03` and reassemble them (header+payload bytes, unchanged) into the output buffer so the filtered file is byte-identical to a re-serialization of just the CRITICAL records. Satisfy the atomic-write requirement by writing to a temp file inside `/home/user/` (e.g. via `tempfile.mkstemp(dir="/home/user")`), then `os.replace()`/`shutil.move()` it to `/home/user/filtered.wal`. For frame extraction, use `ffmpeg -ss <second> -i /app/server_room_cam.mp4 -frames:v 1 -y /home/user/frames/frame_<second>.jpg` for each of seconds 10-15, placing `-ss` before `-i` for fast/accurate seeking. Build the HTTP server (Flask/FastAPI/`http.server` with a custom handler) so every route first checks the `Authorization` header equals exactly `Bearer archiver_token_x7z`, returning 401 otherwise; `/api/wal` streams the filtered file bytes with `Content-Type: application/octet-stream`, and `/api/frame/<second>` maps the path parameter to `frames/frame_<second>.jpg`, returning 404 if that file doesn't exist. Run the server backgrounded and confirm it's reachable before finishing.