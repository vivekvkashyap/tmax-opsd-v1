This is a straightforward extract-and-join task, so build it as a short pipeline rather than a monolithic script. For each file in `/home/user/dataset/binaries/`, run `readelf -h <file>` and parse the `Machine:` and `Entry point address:` lines directly from its output (grep/awk/sed the exact text after the colon, trimming whitespace) so the values match exactly what the parsing tool emits — do not hardcode or guess architecture names. Load `/home/user/dataset/metadata.csv` with `awk -F,` or a small script keyed by `filename` so you can look up `experiment_id` and `temperature` for each binary's basename. Combine both sources per binary into a JSON object with the five required keys, and use `jq -n` or a heredoc plus `jq -c` to ensure each line is valid, compact JSON. Iterate over all binaries (e.g., with a `for f in /home/user/dataset/binaries/*; do ... done` loop), appending one line per binary to `/home/user/dataset_summary.jsonl`, and spot-check the file with `jq .` afterward to confirm every line parses and the readelf-derived fields aren't truncated or reformatted.
