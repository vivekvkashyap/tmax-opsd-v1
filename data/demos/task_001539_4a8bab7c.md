First inspect `/home/user/network_events.csv` with `head`/`cat` to confirm the column order and header before writing any parsing code — don't assume field order matches the description exactly. In your C++ program, read all rows, normalize each edge by sorting the pair of node IDs (e.g. store as `min(a,b), max(a,b)`) so A-B and B-A collapse to the same key, and for each normalized edge track only the event with the maximum timestamp seen so far (a hash map from edge-key to `{timestamp, action}`), updating it as you scan — since the file is unordered you cannot assume the last line for an edge is chronologically latest, you must compare timestamps explicitly. After processing all rows, an edge is active only if its stored latest action is a connect. Build degree counts by iterating the active-edge map and incrementing both endpoints. To get the top 5, sort nodes by `(degree descending, node_id ascending)` as a tie-breaker rather than degree alone — a plain descending sort leaves tied node order undefined and will not match the expected deterministic output. Write exactly 5 lines as `<node_id>,<degree>` to `/home/user/top_nodes.txt`. Compile with `g++` and verify by hand-tracing 2-3 edges against your program's output.