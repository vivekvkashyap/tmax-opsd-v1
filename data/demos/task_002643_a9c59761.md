Build this as a real Cargo project, not a single script — `cargo new sensor_pipeline` inside `/home/user/`, add `csv` and `serde_json` (or manual JSON parsing) as dependencies in `Cargo.toml`. In `main.rs`: parse `/home/user/data/raw_sensor.csv` (no header, first column integer id, next 10 columns floats) and `/home/user/data/model_weights.json` (10x2 matrix) exactly as specified. For each row compute the 1x2 projection `Z = X · W` by summing `X[i] * W[i][j]` over the 10 features for each of the 2 output dims — get the row/column indexing right, a transposed multiply will silently produce wrong-but-plausible numbers. Then compute the two log-likelihoods against the fixed means `[0,0]` and `[2,-2]` using the exact formula given (`-0.5 * sum of squared diffs`, no other constant), pick the class with the higher value, and format `max_ll` to exactly 4 decimal places with Rust's `{:.4}` formatter to match expected output precision. Write `id,class,max_ll` rows to `/home/user/data/classified_results.csv`. Wrap the whole thing in `/home/user/run_pipeline.sh` (`chmod +x`) that runs `cargo build --release` then executes the resulting binary, and actually run that script at the end so the output CSV exists on disk.
