The core risk here is precision: value can be up to a full 64-bit signed range, so parse it as `int64_t` (e.g. with `strtoll`) and track running max/min in `int64_t` — never route through `double`/`float`/`int`, which would silently lose precision or overflow. Read the CSV line by line (a simple `fgets`/`fscanf` loop splitting on the comma is fine given the fixed two-column format), skip the header, and for each row treat the value field as invalid if it's empty (nothing after the comma before the newline) or the literal string "NaN" — check these conditions explicitly before calling any numeric parse function, since blindly calling atoi/strtoll on an empty string will silently yield 0 rather than being skipped. Initialize max to INT64_MIN and min to INT64_MAX, only updating them for accepted rows, and count valid rows separately. Compile exactly as specified with `gcc -O3 -Wall -o /home/user/etl_parser /home/user/process_data.c` (heed any warnings — they often reveal implicit-conversion precision bugs), run it and redirect stdout to /home/user/etl_result.txt, and make sure the three output lines match the required `Valid:`/`Max:`/`Min:` format exactly, including no trailing formatting differences.