Treat this as three chained subsystems: video analysis, SQL recovery, then an authenticated HTTP server — validate each independently before wiring them together. For the video, extract each of the 10 one-per-second frames (e.g. via `ffmpeg -vf fps=1` or `cv2`) and check whether the whole frame is solid red (roughly RGB 255,0,0 — remember OpenCV reads BGR, so check `(0,0,255)` per-channel if using cv2 directly); collect the integer second indices where this holds, then compute epoch timestamps as `1730000000 + T`. For the database, do not trust a filtered query that relies on the `idx_time` index — force a full table scan with `SELECT * FROM access_logs NOT INDEXED WHERE ...` or drop/rebuild the index first, and cross-check row counts against a naive Python filter over all rows to catch any silently dropped/duplicated results. Match `access_logs` rows for resource `MAIN_VAULT` at the breach timestamps, then for each employee, walk `manager_id` up the `employees` table recursively (via SQL recursive CTE or Python loop) collecting names in order until `manager_id IS NULL` (the CEO), building the `chain` list starting from the employee. Finally, implement a minimal HTTP server (e.g. Python `http.server` or Flask) on `127.0.0.1:9090` with `GET /breaches` that checks the `Authorization: Bearer AUDIT_TOKEN_2024` header exactly, returns 401 otherwise, and otherwise returns the breach list JSON sorted by timestamp descending — test with `curl` yourself before leaving it running in the background.
