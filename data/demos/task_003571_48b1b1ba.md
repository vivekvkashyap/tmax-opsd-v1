Treat the black-box decoder as something to characterize empirically: feed it crafted stdin payloads (varying length, varying leading bytes) and observe which cause a crash vs. success, to find the exact fail conditions (check payload size limits and suspicious leading-byte patterns). Encode that as a pre-check in the Go pipeline so poison-pill records are skipped before spawning the subprocess, counting/dropping them instead of aborting the batch. The "shared data structure written by goroutines" description is a classic Go map data race — protect writes with a `sync.Mutex` around the map, or better, have each worker send results over a channel to a single collector goroutine that owns the map, rather than writing directly from multiple goroutines. Use a bounded worker pool (goroutines + channels + `sync.WaitGroup`) to keep decoder calls concurrent for speed. Marshal the final map to `output.json` with the exact required shape, then time a full run and cross-check the record count against your own accounting of filtered vs. processed records.
