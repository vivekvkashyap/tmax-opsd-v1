Parse the CSV, generate the canonical set of 8 timestamps by stepping 15 minutes from 10:00:00Z to 11:45:00Z, and build a lookup from the file's actual rows keyed by timestamp. For any timestamp missing from the file, or any cell that's an empty string in an existing row, treat that app's raw value as the literal string `NONE` — apply this per-cell, not per-row (a row can have some apps present and others missing). Reshape wide-to-long by iterating each of the 8 timestamps x 3 apps. Normalize each raw string: lowercase, replace every non-alphanumeric rune with a space (regex `[^a-z0-9]` on the lowercased string), collapse repeated spaces and trim — implement this with Go's `regexp`/`strings` packages carefully since punctuation patterns vary (`-`, `_`, `:`, `%`). If the normalized result is empty or equals `none`, use `no_action` instead. Tally counts per app into a nested map, then marshal with `json.MarshalIndent(data, "", "  ")` (2-space indent) to get deterministic, sorted-key output — Go's map JSON marshaling already sorts keys alphabetically, which matches the required format.
