Work the two parts independently and verify each end-to-end before moving on. For Part 1: edit nginx.conf to add an `upstream` block listing `127.0.0.1:5001;` and `127.0.0.1:5002;`, have the `server` block `listen 8080` and `proxy_pass` to that upstream (round-robin is nginx's default, no extra directive needed); in api.py, connect with `redis.Redis(host='127.0.0.1', port=6379)` and `lpush` the measured latency float onto the list `latency_metrics` inside the `/ping` handler. Start everything via the provided script, then sanity-check manually with `curl http://127.0.0.1:8080/ping` several times and `redis-cli LLEN latency_metrics` to confirm the count grows. For Part 2, don't hand-tune thresholds by eyeballing a few files — compute a real statistic (e.g. KS-test/Wasserstein distance against a fitted exponential, plus a bootstrap CI on the 95th/99th percentile) so the classifier generalizes across all 100 hidden files rather than overfitting to samples you glance at; keep it fast (well under 2s per file) by avoiding excessive bootstrap iterations or O(n^2) work. Test your script against as many files in /home/user/data/clean and /home/user/data/evil as you can before considering it done.
