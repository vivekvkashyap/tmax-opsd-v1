Set up the environment first: `python3 -m venv /home/user/venv`, activate it, then `pip install pandas numpy` inside it — run all subsequent steps using that venv's interpreter, not the system Python. For the matching logic, avoid a naive double for-loop over every (user, item) pair in Python; instead load both CSVs into numpy arrays/DataFrames, extract the `f1..f4` feature columns as matrices, and compute all dot products at once via matrix multiplication (`users_matrix @ items_matrix.T`), which is both faster and less error-prone. For each row of the resulting score matrix, take `argmax` to find the best-matching item's column index, map it back to `item_id`, and round the corresponding score to exactly 4 decimal places (use Python's `round()` or format string, watching for floating-point display quirks). Sort the final DataFrame by `user_id` ascending before writing, and write with `to_csv(..., index=False)` so the header is exactly `user_id,item_id,score`.
