For `backup_and_convert.sh`, use `mkdir -p` for idempotent directory creation, `cp -f` for the backup, then parse `legacy_proxy.conf` line by line (split on `=`) and emit JSON with a manual heredoc/printf loop or `jq -n --argjson` construction rather than hand-string-concatenating — get the exact nested structure (`routes` array of `{name, port}` objects with port as a JSON number, not a string) right on the first try. For the Rust service, `cargo new proxy_service`, add `serde`/`serde_json` to `Cargo.toml`, read and deserialize `new_proxy.json` into a struct, loop over routes binding `std::net::TcpListener::bind(("127.0.0.1", port))`, and on each successful bind append (not overwrite) `Started {name} on {port}\n` to `/home/user/proxy.log` — then keep the process alive (e.g., `loop { std::thread::sleep(...) }` or hold the listeners and `accept()` in threads) so the ports stay bound. For `deploy.sh`, `cargo build --release`, launch the binary with `&` and `disown`, then poll the log file in a loop (`grep -c` for each route name) until all three appear before writing `status=MIGRATED`. Confirm bound ports afterward with `ss -tln`.
