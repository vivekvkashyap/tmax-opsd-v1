Automate the interactive prompts with a real `expect` script rather than trying to pipe answers via plain bash (a naive `echo -e "a\nb\nc" | python3 disk_reporter.py` may not work reliably if the tool uses `input()` with TTY-dependent buffering, but `expect` handles it robustly). In `/home/user/run_analysis.sh`, use `expect -c` or a heredoc-based expect script that spawns `python3 /home/user/disk_reporter.py`, uses `expect "Enter directory path: "` then `send "<dir>\r"`, then `expect "Enter size threshold in MB: "` / `send "10\r"`, then `expect "Output format (csv/json/text): "` / `send "csv\r"`, and captures stdout (redirect the spawned process output to a file, or use `expect`'s logging) — run this once per zone (zoneA, zoneB), saving each CSV output separately. Then parse each captured CSV with `awk -F, 'NR>1{count++; sum+=$2} END{print count, sum}'`, accumulate counts and sums across both zones' outputs, and format the final sum to exactly two decimals with `printf "%.2f"`. Write the two-line result to `/home/user/capacity_summary.txt` in the exact `Total Files: <n>` / `Total Size: <x.xx> MB` format. Make `run_analysis.sh` executable with `chmod +x` and actually run it so `capacity_summary.txt` is produced.