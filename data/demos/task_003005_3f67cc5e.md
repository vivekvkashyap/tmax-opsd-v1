For the video key: extract the target frame with `ffmpeg -i /app/surveillance.mp4 -vf "select=eq(n\,149)" -vframes 1 out.png` (frame 150 at 30fps ≈ timestamp 5s), then either read the overlaid hex text visually/via OCR (`tesseract`) — don't guess it. Write exactly the 32 hex characters to `/home/user/staging_key.txt` with no trailing newline (use `printf` not `echo`, or `echo -n`).

For the sanitizer: don't hand-guess signatures. Write a first-pass C classifier (e.g. reject on any non-printable-heavy byte run), compile it, then run it against every file in `/app/corpus/clean/` and `/app/corpus/evil/`, diffing misclassifications. Use `xxd`/`hexdump` and `diff` on paired clean/evil samples to spot byte sequences (NOP-sled-like repeats, ASCII markers, malformed length fields) that only appear in evil files. Encode each distinguishing pattern as a byte-sequence memchr/memmem scan in C, iterate until both corpora are 100% correct, then confirm the exit-code contract (0=clean,1=evil) via a small shell loop over both directories.