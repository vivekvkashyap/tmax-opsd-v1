Treat this as three loosely-coupled services wired by Redis, and get each one working standalone before wiring `start_services.sh`. First inspect the existing `api.py` and `rust_worker/` skeleton and the sample graphs in `/home/user/corpus/{clean,evil}/` to see the exact JSON shape (`nodes`, `edges`). In the Rust worker, implement clustering coefficient and max-degree per node using `rayon`'s parallel iterators (e.g. `par_iter()` over nodes to compute each node's local clustering coefficient concurrently), then average across nodes. Apply the classification rule exactly as given: CLEAN only if max_degree <= 4 AND avg_clustering_coefficient > 0.0, else EVIL — implement this as a straightforward threshold check, not a heuristic guess, since the corpora are specifically designed to separate on these two properties. Have the worker loop: `BLPOP graph_queue` from Redis, parse JSON, classify, append a `graph_id,STATUS` line to `/home/user/classification_results.csv`. Write `start_services.sh` to launch `redis-server --daemonize yes`, then the Python API (`python3 api.py &`) with `REDIS_URL` exported, then `cargo run --release &` for the worker from its directory. Test end-to-end yourself with `curl -X POST ... http://localhost:5000/process` against one clean and one evil sample before considering it done.