First run `sqlite3 /app/graph.db "PRAGMA integrity_check;"` to see the actual failure, then repair it with `REINDEX;` (or drop and recreate the specific corrupted index) rather than deleting/recreating the whole database — corruption localized to an index doesn't require touching table data. Re-run `PRAGMA integrity_check;` until it reports `ok`. Second, don't guess the scoring formula from the task text — actually listen to or transcribe `/app/requirements.wav` (e.g. with `ffmpeg`/`whisper`/any available speech-to-text tool, or play it and listen) to extract the precise scoring formula and sort order it specifies; write down the transcript before coding so you can cite it. Then write the Go program using `database/sql` with a sqlite3 driver, computing each node's score as specified from `edges` (in-degree/out-degree style aggregation via correlated subqueries or joins+GROUP BY), filtering by the `min_score` argument, sorting exactly per the transcript's tie-break rule, and applying LIMIT/OFFSET in SQL or in Go. Test with several limit/offset/min_score combinations and confirm nodes with zero edges are handled without crashing (score simply computes to 0).
