Implement sequence_filter.c as a straightforward two-check classifier, testing each check independently against the provided clean/evil corpora as you go. Parsing: read the FASTA file line by line, skip any line starting with '>', and concatenate the remaining lines (stripping newlines) into one uppercase nucleotide buffer. Check 1 (G-quadruplex): scan the buffer for the pattern `GGGG` followed by 1-3 arbitrary characters followed by `GGGG` — a simple sliding-window loop checking `buf[i..i+4]=="GGGG"` then trying gap lengths 1,2,3 before requiring another "GGGG" is enough; no regex library needed. Check 2 (KL divergence): build a 16-bucket histogram of overlapping dinucleotides (positions i,i+1 for all i), add a pseudocount of 1 to every bucket before normalizing to probabilities, then compute `sum(p_i * log(p_i / (1.0/16)))` in nats (natural log) and compare strictly greater than 0.20. If either check trips, `exit(1)`; otherwise `exit(0)`. Compile with gcc, then loop your binary over every file in both corpus directories and check exit codes match expectations before considering it done.