Write a Python script that processes `/home/user/upload_logs.txt` line by line, splitting on ` | ` into TOKEN and ENCODED_FILENAME. Base64-decode the filename (standard base64) and check for the traversal markers `../` or `..%2f` case-insensitively — use `.lower()` before matching, and check for the literal substring, not a regex that could miss encoded variants. For malicious lines, split TOKEN on the last `.` into payload and mac parts, base64url-decode the payload (add back `=` padding as needed since it's unpadded) to get the JSON and extract `user`. Recompute the expected MAC yourself: `hmac.new(open('/home/user/secret.key').read().strip().encode(), payload_str.encode(), hashlib.sha256)`, base64url-encode the digest (strip padding), and compare against the token's mac component using `hmac.compare_digest` — do not just trust a field in the token claiming validity. Build the output list preserving original log order, and write it as JSON with keys `user`, `malicious_payload`, `signature_valid` to `/home/user/audit_trail.json`.