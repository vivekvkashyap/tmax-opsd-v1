Build the exploit script in the same order the attack chain happens, testing each stage against the running local services before moving to the next. First start the environment (`/app/start.sh`) and use `scapy` or `pyshark`/`dpkt` to parse `/home/user/traffic.pcap`: filter TCP payloads to `/login_v1` and extract the JSON body's `user` and `hash` fields (reassemble HTTP if it spans multiple TCP segments). Second, crack each hash offline by iterating the wordlist and comparing `hashlib.md5(f"{username}:{candidate}".encode()).hexdigest()` against the captured hash — this is a dictionary attack, not something to hardcode. Third, POST the recovered `{user, password}` JSON to `/login` and capture the returned session token/cookie. Fourth, exploit the path traversal on `/download?file=...` — since secrets live in `/app/secrets/<user>.txt`, try a payload like `../secrets/<user>.txt` (adjust `../` depth to match the server's static-file base directory, which you should determine empirically against the live service). Write the `{user: content}` mapping to the given output path. Parameterize pcap/wordlist/output paths from `sys.argv`, since the verifier reruns your script against a different held-out pcap.