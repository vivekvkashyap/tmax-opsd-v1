First fix the vendored library: inspect `/app/libcsv_ext-1.0.0/Makefile` line by line against the actual source filenames in that directory (`ls` it) — a build failure from a missing/renamed `.o` target almost always means the Makefile references an object file name that doesn't match the corresponding `.c` file (e.g. transposed letters); correct the target name so `make` in that directory succeeds and produces `libcsv_ext.a`. Then write `/home/user/processor.c` as a line-oriented stdin reader: read line by line (e.g. `getline` in a loop until EOF), split on commas into ID (int), Value (float), Category (char), Model_Score (float) using something robust like `sscanf`. Apply the rules in order: skip rows with ID < 0 entirely (no output line), else square Value, map category via a simple if/else ('A'->1,'B'->2,'C'->3, else 0), and classify Model_Score > 0.5 strictly as VALID else INVALID. Print exactly `ID,Value_squared,Encoded_Category,Validation_Status` with `%f` for the float field, one line per non-skipped row, matching the given example format exactly (field order, comma separators, no extra whitespace). Compile to `/home/user/processor` and test it against varied/edge-case CSV lines (negative IDs, unexpected characters, whitespace) before considering it done, since it will be fuzzed against a hidden reference implementation.
