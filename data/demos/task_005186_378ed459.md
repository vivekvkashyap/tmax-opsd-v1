Work in three independently-testable stages. First extract frames with ffmpeg at 1fps into sequentially numbered jpgs. Second, write the C++ hasher: read the directory, sort filenames alphabetically (this matters — it must match the Python baseline's `sorted()` order exactly), initialize with SHA-256("init"), and fold in each frame's raw bytes iteratively using a fast crypto library (e.g. OpenSSL's EVP SHA256 API) compiled with optimizations (-O2/-O3) — avoid unnecessary buffering/copying so it clearly beats the slow Python baseline. Verify correctness first by comparing your hasher's output directly against `baseline.py`'s output before worrying about speed. Third, wrap execution in a bwrap sandbox: `--unshare-net`, read-only binds for the frames directory and needed system library paths (/usr, /lib, /lib64, and the dynamic linker), and a writable bind only for the audit trail output path — bwrap sandboxes commonly fail silently or error on missing library paths, so run the wrapped command manually first and confirm it actually produces the hash file before considering the task done, rather than assuming the bwrap invocation is correct on the first try.
