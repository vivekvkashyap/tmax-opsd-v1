This task chains three separate recovery steps — do each in order and confirm success before moving on, rather than trying to shortcut to the detector. First, OCR the password out of /app/clue.png (e.g. `tesseract /app/clue.png stdout`), then use it to unzip /app/corpus.zip (`unzip -P <recovered-password> corpus.zip`) into its clean/ and evil/ directories — verify the extraction succeeded and inspect a few sample files from each to understand the binary format before writing any detection code. Second, use `git bisect start`, marking the current HEAD as bad and `HEAD~200` as good, inside /app/parser_repo; when bisect lands on a commit that fails to compile, use `git bisect skip` rather than treating it as bad or good, and keep narrowing until you find the exact regression commit — then diff that commit against its parent to see exactly what safety check was removed. Use `strace -f` on a build running against an evil sample to observe which syscalls (e.g. `openat`) reveal the exploited path-traversal behavior, confirming your understanding of the vulnerable pattern. Only then write the Rust detector: parse just the type byte and filename length/bytes per the format, and exit non-zero whenever the type or filename indicates the unsafe traversal pattern you identified, exit 0 otherwise. Compile to exactly /home/user/detector and test against sample files from both corpus directories before trusting it on the full set.