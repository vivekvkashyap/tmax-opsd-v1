Work bottom-up: first fix the vendored crate before writing any new code. Open `/app/spectro_filter/src/lib.rs` and find the boundary bug in the smoothing loop — it almost certainly iterates one index too far and dereferences `data[i+1]` when `i` is the last valid middle index, causing an out-of-bounds panic; correct the loop's upper bound so the first and last elements are copied unchanged and only true interior indices are averaged over their left/right neighbors, and sanity-test it against short arrays (length 0, 1, 2, 3) with `cargo test` or a quick manual `cargo run` before moving on. Then scaffold `/home/user/spectro_cli` as a new binary crate with a `path` dependency on `/app/spectro_filter` in its Cargo.toml (no crates.io deps allowed). In `main.rs`, read one line from stdin, split on whitespace and parse to `f64`, branch on length < 3 to print inputs unmodified at `.4` precision, otherwise call `apply_filter`, find the minimum of the smoothed vector and subtract it from every element, then print space-joined `.4`-formatted values. Build with `cargo build --release` and confirm the binary lands exactly at `/home/user/spectro_cli/target/release/spectro_cli`, matching stdin/stdout behavior expected by an external fuzz-equivalence check.