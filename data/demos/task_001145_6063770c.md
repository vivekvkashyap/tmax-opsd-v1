Compile `/home/user/smooth.c` into a shared library with `gcc -shared -fPIC -o /home/user/libsmooth.so /home/user/smooth.c`. In `analyze.py`, load it via `ctypes.CDLL`, set `argtypes` to `[POINTER(c_double), POINTER(c_double), c_int]` on the `smooth` function, and call it once per row of the loaded `spectra.csv`, passing each row's `.ctypes.data_as(...)` pointer for input and a same-shape output array's pointer — don't reuse the input buffer as output since the C function reads neighboring elements. Then loop `alpha` from 0.0 to 1.0 in 0.1 steps (use integer loop `for i in range(11): alpha=i/10` to avoid floating point drift), constructing `NMF(n_components=2, init='random', random_state=42, max_iter=500, solver='cd', alpha_W=alpha, alpha_H=alpha, l1_ratio=1.0)` exactly with those fixed parameters, fitting on the smoothed data, and checking `nmf.n_iter_ < 500`; stop at the first alpha (ascending) that converges and write `alpha=X.X, iter=Y` to `/home/user/result.txt`. Because this is checked against a canonical run with the same seed and data, keep every parameter and the alpha ordering exactly as specified — any deviation changes the deterministic result.