Start by extracting the whiteboard content: run `tesseract /app/relevance_formula.png stdout` and read the OCR output carefully to recover both the scoring formula and the exact required output JSON schema (field names, key order, types) — don't assume a formula, derive it from the OCR text, and if OCR is noisy, crop/preprocess the image (e.g. with `convert`/ImageMagick to upscale or threshold) and re-run tesseract until the text is legible. Then scaffold a Rust binary crate (`cargo new`), parse stdin JSON with `serde_json` into structs for `nodes`/`edges`, compute per-node in-degree and out-degree by counting edges where the node is `dst`/`src` respectively, apply the formula you recovered against each node's weight and degrees, sort results alphabetically by node id, and serialize to the exact schema/field names found in the image with `serde_json` (use `to_string` for compact output, no extra logging to stdout). Build with `cargo build --release` and copy the resulting binary to `/home/user/relevance_calc`, then `chmod +x` it. Test manually by piping a small sample JSON in before relying on the fuzzer.
