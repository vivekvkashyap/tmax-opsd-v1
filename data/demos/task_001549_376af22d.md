This is a bit-exact reimplementation task, so start by black-box probing the reference binary with varied inputs (empty metric, missing metric key, valid/invalid `\uXXXX` sequences, single-line streams, exactly-window-size streams) and diff your Python's output against it after each change — don't trust the prose spec alone for edge cases like where forward-fill starts or how the first window elements behave. Implement JSON-lines parsing carefully: for the `msg` field, scan for `\uXXXX` escapes yourself (don't rely on the standard json module's decoder, which will raise on malformed hex) and substitute `?` when the four characters aren't valid hex. Maintain running state across lines for forward-fill and a trailing deque of size 5 for the rolling mean/std (use sample std with N-1, guarding against a single-element window). Format floats with `%.4f` and empty string for None — mismatched float formatting (e.g. `-0.0000` vs `0.0000`, or Python `round()` vs C-style truncation) is a common silent divergence, so specifically test negative-near-zero values and boundary anomaly cases where |diff| equals exactly 2.0*std.