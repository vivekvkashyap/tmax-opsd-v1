Write and test the SQL query directly in the `sqlite3` CLI before embedding it in C++ — this avoids debugging SQL syntax and C API code simultaneously. Build the recursive CTE first: seed with `id=1` (CEO) at depth 0, recursively join `employees e JOIN cte ON e.manager_id = cte.id` incrementing depth, which gives you the full hierarchy. Left-join this against a per-employee aggregate of `access_logs` filtered to `status='DENIED'` (use `LEFT JOIN` plus `COALESCE(count,0)` so employees with zero denials still appear). Apply `RANK() OVER (PARTITION BY department ORDER BY denied_count DESC, id ASC)` for `dept_rank`, matching the tie-break rule exactly. Once the query returns correct rows in the CLI, port it into the C++ program using `sqlite3_prepare_v2`/`sqlite3_step`, and before running the main query execute `CREATE INDEX idx_emp_status ON access_logs(emp_id, status);` (check the return code with `sqlite3_exec`/`rc == SQLITE_OK`, don't ignore errors). Write CSV rows in the exact column order requested to `/home/user/audit_report.csv`, compile with `g++ ... -lsqlite3`, and run the binary to generate the file before finishing.
