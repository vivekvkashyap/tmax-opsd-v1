Fix the C library build first and in isolation: run `make library` in `/app/vendored/http-parser-2.9.4`, read the actual compiler error (it will be a `-Wimplicit-fallthrough` warning promoted to an error by `-Werror`), and make the smallest targeted fix to the `Makefile` — either drop `-Werror` from the `CFLAGS` line or append `-Wno-implicit-fallthrough` — rather than stripping out other flags; confirm with a clean `make clean && make library` that `libhttp_parser.a` is produced, then generate the patch with `git diff` (or `diff -u` against a saved original) into `/home/user/http-parser-makefile.patch`. For the Rust side, scaffold with `cargo new --bin http_analytics`, and in `build.rs` emit `cargo:rustc-link-search` pointing at the vendored lib dir and `cargo:rustc-link-lib=static=http_parser`; declare `extern "C"` bindings for `http_parser_init`/`http_parser_execute`/`http_parser_settings` matching the C header layout. Split the input file on the literal boundary string, feed each request's bytes through the parser with an `on_url` callback capturing the URL into a buffer, then manually scan the query string for `score=` and parse the trailing digits as an integer. Sort with a comparator: descending by score, then ascending by full URL string as a tiebreaker. For the 1.5s budget, avoid per-request heap churn — reuse buffers, avoid unnecessary `String` clones, and always build with `--release`.