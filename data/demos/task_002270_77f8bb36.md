Inspect `/app/data/graph.db` schema and existing indexes with `sqlite3 /app/data/graph.db ".schema edges"` and `.indexes edges` first; drop any suspicious/duplicate index on `edges` and recreate a clean composite index with `CREATE INDEX idx_edges_src_tgt ON edges(source, target);`. In `/app/etl/compute_metrics.sh`, compute out-degree centrality with a single `sqlite3` invocation using aggregation/window functions, e.g. `SELECT source, COUNT(*) AS out_degree, RANK() OVER (ORDER BY COUNT(*) DESC) AS rank FROM edges GROUP BY source;`, then pipe each `(id, rank)` pair — including nodes with zero out-edges, which need a LEFT JOIN against `nodes` to appear with rank set appropriately — through `redis-cli SET node:<id>:rank <rank>` in a bash loop (`while read -r id rank; do redis-cli set "node:$id:rank" "$rank"; done`). Fix `query_server.sh` so that for each TCP connection it reads the incoming node id line, does `redis-cli get "node:$id:rank"`, and writes back exactly `Rank: <rank>\n`; run it persistently in the background via `socat TCP-LISTEN:8080,fork,reuseaddr EXEC:/app/service/query_server.sh &` (one script invocation per connection) so the verifier's fuzzed TCP queries against many random node ids all get answered correctly and consistently with the reference oracle.
