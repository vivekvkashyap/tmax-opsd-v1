Diagnose the 502 before editing configs: check what port/socket the Gunicorn/Flask process is actually bound to (`ss -tlnp` or `ps aux`), then open `/home/user/deployment/nginx/nginx.conf` and compare its `proxy_pass` target against that real binding — fix the mismatch (wrong port, stale socket path, etc.), then reload Nginx (`nginx -s reload` or restart the process pointed at that config file) and re-curl the `/api/process` endpoint to confirm the 502 is gone before moving to the processor logic. For `processor.py`, treat `/app/oracle_processor` purely as a black box: feed it a range of crafted single-purpose inputs via stdin (empty string, single characters, repeated characters, mixed case, strings with vowels/consonants, digits) one at a time and diff outputs to isolate each transformation step (e.g. does it reverse order? change case? substitute specific characters? collapse repeats?), then compose your Python implementation from the deduced steps and re-verify against the oracle on many random strings before trusting it. Ensure the script is executable and reads/writes only stdin/stdout, since the Flask app pipes to it via `subprocess.run`.
