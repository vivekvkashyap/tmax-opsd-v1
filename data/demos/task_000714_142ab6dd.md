Don't guess the filtering rule — transcribe `/app/filter_memo.wav` yourself using `/opt/whisper.cpp/main -m /opt/whisper.cpp/models/ggml-base.en.bin -f /app/filter_memo.wav` (or similar flags) and read the transcript carefully to extract the exact condition (which severities qualify, the numeric threshold and comparison direction) and the exact output fields/format requested. Separately, unpack `/app/sensor_logs.tar.gz` recursively (`tar`, `unzip`, `zcat` as needed) to see real record samples and confirm the field-line prefixes (`EventID: `, `Severity: `, `Temperature: `, etc.) and that records are delimited by a line that is exactly `---`. Then write `parser.cpp` reading stdin line-by-line, accumulating fields for the current record, and on hitting the `---` delimiter (and also handling a final record with no trailing delimiter), evaluate the rule you transcribed and print only the requested fields, joined however the memo specified, to stdout with nothing else. Be strict about `---` matching exactly, field prefix parsing, and numeric type conversion, since it will be fuzzed heavily against a hidden reference.
