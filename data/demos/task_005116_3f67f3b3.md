Work the three phases independently and confirm each before combining. First, transcribe `/app/deployment_voicemail.wav` — install a speech-to-text tool (e.g. `pip install openai-whisper` plus ffmpeg) and run it rather than guessing; read the transcript to find the spoken port number, convert spelled-out digits to numerals, and write just the digits to `/home/user/deploy_port.txt`. Second, open the SSH tunnel in the background with `ssh -L 8080:localhost:<discovered-port> -N -f user@localhost`, and write `worker_wrapper.sh` that calls `ulimit -f 51200` (50MB in 512-byte blocks) before `exec "$@"` so wrapped commands inherit the file-size limit — test with `./worker_wrapper.sh sh -c 'ulimit -f'`. Third, for the WAV validator, parse the RIFF header manually in C++: read the 4-byte "RIFF" magic, skip 4 bytes for size, verify "WAVE", then loop reading each chunk's 4-byte ID and 4-byte little-endian size, rejecting (exit 1) if invalid or any chunk exceeds 100000000 bytes, else exit 0. Compile with `g++`, then loop the validator over `/app/corpus/clean` and `/app/corpus/evil`, checking exit codes before declaring done.