Confirm the header layout experimentally (`xxd`/`strings` on sample `.cpk`-like data and observing `/app/cpk_tool`'s stdin/stdout behavior) before coding, then parse the little-endian timestamp with your language's struct/pack module (e.g. Python `struct.unpack('<I', data[4:8])`) rather than manual byte math, to avoid endianness bugs. For the watcher, avoid reading files mid-write: use inotify/watchdog with a stability check (file size unchanged across polls, or a close-write event) instead of reacting to raw create events. Pipe each stable file's bytes to `/app/cpk_tool` as stdin via subprocess and capture stdout/exit code to detect corruption. For the HTTP server, enforce the exact `Authorization: Bearer conf-sync-secret` header on every route, track the most recent valid file by parsed timestamp (not filesystem mtime) for `GET /latest`, and for `POST /upload` write to a temp/hidden filename then atomically rename into the spool dir so the watcher never sees a partial file. Write the ready log only once the server is actually bound and accepting connections.
