First try `make linux` in `/app/vendored/lua-5.4.6` to see the actual link error — it will fail because a system library flag was tampered with in `src/Makefile`. Diff or grep the `SYSLIBS`/link-flags line in that Makefile for anything that isn't a real system library (an unusual `-l<name>` that doesn't correspond to a library on the system) and restore it to the standard `-lm` math library flag; don't touch unrelated lines. Rebuild with `make linux` and confirm `liblua.a` is produced. For the server, write `server.cpp` in `/home/user/workspace` that opens a TCP listen socket on `127.0.0.1:8888` (standard `socket`/`bind`/`listen`/`accept` loop), reads one line per connection, calls `luaL_dostring`/`lua_pcall` on a wrapped `return <expr>` via the Lua C API, pulls the numeric result off the stack with `lua_tonumber`, writes it back as a string plus `\n`, then closes that connection and loops to accept the next one (sequential, not concurrent). Write a `Makefile` linking against the vendored `liblua.a` plus `-ldl -lm`, build `lua_server`, and launch it in the background so it's listening when you finish.
