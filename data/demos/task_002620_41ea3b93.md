First reproduce the bug: run the existing `generate_summary.sh` and confirm the `FROM departments, employees` produces an implicit cross join (every department paired with every employee row), which is why counts are inflated — fix it with an explicit `JOIN employees ON employees.dept_id = departments.dept_id` plus `GROUP BY`. For management depth, write a recursive CTE seeded with rows where `manager_id IS NULL` at depth 0, recursively joining employees to their manager's row and incrementing depth by 1 — test this CTE alone first against the small dataset and manually check a couple of depths against the reporting chain before combining it with anything else. Chain the two computations by joining the per-employee depth result to `employees`/`departments`, then aggregate with `COUNT(*)` and `AVG(depth)` grouped by department, ordered alphabetically. To produce JSON from sqlite3 output, either use `sqlite3`'s `.mode json` (if available) or pipe delimited output through `jq -R -s` / `awk` to construct the array of objects — validate the final JSON structure (field names, types, ordering) against the spec before finishing, and actually execute the script so `/home/user/dept_metrics.json` is written to disk.