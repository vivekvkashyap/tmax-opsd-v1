Break this into three independently-checkable stages. Stage 1: find pure-white frames in `/app/config_blinks.mp4` — decode frame-by-frame (e.g. `ffmpeg` piping raw RGB frames to a small script, or a Python/OpenCV loop) and test each frame for every pixel equal to (255,255,255) rather than trusting brightness/average heuristics, since near-white frames could otherwise false-positive; record the exact 0-indexed frame numbers as commit IDs. Stage 2: write the C redactor using POSIX `regex.h` with a pattern anchored to require exactly `AKIA` immediately followed by 16 characters from `[A-Z0-9]{16}` — be careful the regex doesn't also match if a 17th alphanumeric char follows (word-boundary/length check) and doesn't match lowercase variants; process input character-by-character or line-by-line via `regexec`, replacing only the captured 16-char group with 16 `*` while copying all other bytes unchanged, then validate against a few of your own crafted clean/evil samples (not just the provided corpus) before compiling with `gcc`. Stage 3: filter `/app/audit_log.csv` (careful with CSV quoting/embedded commas — don't split naively on every comma) to rows whose commit_id matches your Stage-1 frame numbers, pipe each `config_snippet` through your compiled filter binary, sort by `service_name`, and emit the markdown using the exact literal template (headers, dashes, blank lines) shown in the task.