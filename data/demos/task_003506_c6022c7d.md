Before writing any detection logic, spend time black-box probing `/app/legacy_graph_oracle`: pipe small, hand-crafted CSVs into it via stdin (e.g. `echo "A,A,TRANSFER,100" | /app/legacy_graph_oracle`) and vary one property at a time — self-loops, cycles, unusual transaction-type strings (very long, containing special characters like `;` or quotes), duplicate edges, disconnected components — while checking the exit code (`$?`) and whether it segfaults (exit 139) or hangs. Build up a list of the specific conditions that cause a crash versus normal exit purely from this empirical testing, rather than guessing from the general description. Once you've identified the concrete trigger conditions, implement `/home/user/graph_sanitizer.py` to parse the CSV, build the graph (e.g. with a dict of adjacency lists, or `networkx` if available, for any cycle/topology checks you need), and check each row/structural property against exactly the conditions you empirically confirmed — print "ACCEPT"/exit 0 only when none of them are present, otherwise "REJECT"/exit 1. Validate your script against both crafted-safe and crafted-unsafe CSVs you construct yourself, re-running the real binary on the same inputs to confirm your script's verdict matches the binary's actual crash/no-crash behavior, before considering it done.