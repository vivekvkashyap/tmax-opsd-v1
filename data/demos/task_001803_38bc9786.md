Debug this in strict order: parsing bug first, then build errors, then runtime. For `get_libs()` in setup.py, first write `mre_parse.py` that just imports and calls it against the real `libs.conf` and prints the list — run it and observe exactly how comments, trailing whitespace, or line-continuation backslashes corrupt entries (e.g. a naive `line.split()` leaves inline `#comment` text attached to a token). Fix the parser to strip inline comments before splitting, strip whitespace per token, and join continuation lines (lines ending in `\`) before tokenizing, then confirm your mre_parse.py prints a clean list like `avformat, avcodec, avutil, swscale`. Next run `python setup.py build_ext --inplace` and read compiler/linker errors carefully: C++ including FFmpeg's C headers typically needs `extern "C" { #include <libavformat/avformat.h> ... }` wrapping (missing this causes name-mangling linker errors), and the link line/order matters — list dependent libraries (avformat before avcodec before avutil, swscale last or per actual dependency graph) and ensure they match the entries your fixed `get_libs()` now returns. Once the .so builds, run `run_pipeline.py` against `/app/test_video.mp4`, output to `/workspace/output_frames/`, and sanity-check the jpgs visually before considering it done — the real check compares them via SSIM against ffmpeg-extracted reference frames.
