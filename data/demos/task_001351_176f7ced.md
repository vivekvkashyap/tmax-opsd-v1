Handle the semver selection carefully in bash before writing any C. List `/home/user/libs/libmathops-*.so`, extract the `X.Y.Z` triple from each filename, and compare versions numerically (not lexicographically — e.g. `1.12.3` must be recognized as greater than `1.4.2`, which a plain string `sort` would get wrong). Use `sort -t. -k1,1n -k2,2n -k3,3n` on the extracted version numbers, filter to those with major version `< 2`, and take the highest; pass its full absolute path as argv[1] to your compiled `process` binary along with the CSV path as argv[2]. In `process.c`, `dlopen(argv[1], RTLD_NOW)` then `dlsym` for `process_value`, checking both calls for NULL/errors. Parse the CSV line by line (skip/ignore the timestamp field), keep a small array indexed by `user_id` (1-100) tracking how many valid requests each user has had; only call `process_value` and emit a JSON line when `value > 0` AND that user's count is still below 2, incrementing the count on acceptance. Write each output as `{"uid": <user_id>, "result": <computed_value>}\n` to `/home/user/output.jsonl`, and make sure `run.sh` compiles `process.c` with `-ldl` and is executable.