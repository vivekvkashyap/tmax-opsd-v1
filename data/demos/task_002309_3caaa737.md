Work through this as a strict pipeline, checking output after each step before moving to the next. Extract with `tar -xzf incoming_project.tar.gz -C project_workspace` (create the dir first). Find `.txt` files with `find ... -name '*.txt'` and convert each in place with `iconv -f ISO-8859-1 -t UTF-8 file.txt -o file.txt.tmp && mv file.txt.tmp file.txt` (never iconv to the same path directly, which can truncate the file mid-read). For duplicates, compute a content hash (e.g. `sha256sum`) per file, group by hash, and within each group sort the paths alphabetically — hard-link every non-first path to the first with `ln -f <first> <other>` (this replaces the file with a hard link, so the same inode appears at both paths). Generate the manifest by `cd`-ing into `project_workspace` and running `find . -type f -exec sha256sum {} \;`, then sort the output lines by path (the second column) before writing `/home/user/manifest.sha256`. Finally, tar the workspace's contents without a wrapper directory using `tar -czf ../clean_project.tar.gz -C project_workspace .` (or run tar from inside the directory with `.` as the target).
