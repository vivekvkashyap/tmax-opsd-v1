Follow the spec fields exactly since correctness here is about precise syntax, not discovery. For the fstab line, use the canonical six whitespace-separated fields in order: `device mountpoint fstype options dump pass` — match the exact device, mountpoint, filesystem, and option string given, with both trailing numbers `0 0`. For ACLs, use `setfacl -m u:2048:r-x /home/user/metrics_data` for the explicit entry and `setfacl -d -m u:2048:r-x /home/user/metrics_data` for the default (inherited) entry — verify with `getfacl` afterward that both a `user:2048:r-x` line and a `default:user:2048:r-x` line appear, since these are two separate ACL entries that are easy to conflate. Write the INI file with the exact section/key names given (`[Dashboard]`, `theme`, `refresh_rate`, `[Metrics]`, `source`). In `build_dash.py`, parse with `configparser`, cast `refresh_rate` to `int` explicitly (configparser reads strings by default), build the JSON with the exact key names/nesting shown in the task, and actually execute the script so `dashboard.json` exists on disk before finishing.
