Treat /app/obfuscator as a black-box oracle to characterize empirically rather than guessing: feed it small controlled inputs (single characters, short ASCII strings, strings with a known repeated character) and study how the output changes byte-by-byte — look for signs of a fixed-length output growth pattern (suggesting hex or base-N encoding of each input byte), check whether reversing your input string flips the output in a way consistent with string reversal, and check whether XOR-style bit patterns emerge by comparing outputs for inputs differing in one bit. Cross-check any hypothesis with `strings`/`objdump -d` on the binary to see literal constants or transform instructions it references. Validate your reconstructed algorithm by writing a harness that runs the binary and your Python guess over many random inputs and diffs results until they always agree. Only then build /home/user/detector.py: since obfuscated payloads follow a recognizable structural signature (fixed prefix, fixed alphabet/length pattern for the rest), scan each input line for a substring matching that structure and classify EVIL/CLEAN accordingly, printing exactly one token per line with nothing else.