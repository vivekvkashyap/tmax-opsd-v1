Transcribe `/app/lab_recording.wav` yourself with a real ASR tool (`whisper`/`openai-whisper` CLI, or `ffmpeg` to extract audio plus a speech-to-text model) — don't guess at its content. Listen closely for exact numeric details: the time-step count/range, how `t` is normalized, which trig/summation formula is used per amino acid frequency, and — since the task is framed around non-reproducible floating-point *reduction order* — pay special attention to any instruction about summation technique (order of addition, compensated/Kahan-style summation, etc.), since that is precisely the kind of detail that determines bit-exact equivalence under fuzzing. Load `/app/freqs.json` for the per-amino-acid frequency mapping. Implement `simulate.py` to follow the transcribed recipe literally and deterministically (avoid numpy vectorized reductions if the instructions call for a specific scalar summation order, since vectorized sums can reduce in a different order than a loop). After computing the signal array, apply the requested frequency-domain transform and print the requested scalar output exactly as specified (format matters for exact-match fuzz testing). Also produce the requested plot for the specific sequence mentioned in the audio, saved to the exact path stated. Test your script against several different random amino-acid sequences and check outputs are self-consistent and stable across repeated runs before finishing.