In C, parse each CSV line's timestamp with `strptime` (format `%Y-%m-%d %H:%M:%S`) into a `struct tm`, convert to a comparable form (e.g. `mktime` or just compare the struct fields / an epoch-like integer), and skip a row if it is an exact duplicate of the immediately preceding row (same timestamp and state) before using it. Build a sorted-in-memory array of (timestamp, state) events from the deduplicated input, since the file is already chronological this is just a filtered copy. Then generate the fixed hourly grid from `2023-10-01 00:00:00` to `23:00:00` (24 points) by looping and adding 3600 seconds each iteration (or incrementing the hour field), and for each grid hour scan forward through the event list to find the last event whose timestamp is `<=` the grid hour — carry that state forward (this is standard "as-of" forward-fill, not nearest-neighbor); if no event qualifies yet, use `OFFLINE`. Format output timestamps with `strftime` using the same `%Y-%m-%d %H:%M:%S` pattern so they match exactly. Compile with `gcc -o /home/user/process /home/user/process.c` and run it to produce the CSV.
