Write the C chunker to open `/home/user/archive.dat` in binary mode, read in a loop using `fread` with a 4096-byte buffer, and for each read chunk (the last one naturally being shorter if the file size isn't a multiple of 4096 — don't pad it) write it to `/home/user/chunks/chunk_%04d.dat` with a zero-padded sequential index. Compute the XOR checksum as a single running `unsigned char` accumulator initialized to 0, XORed with every byte actually read into that chunk, then format it as exactly 2 lowercase hex digits (`%02x`) — get the width and case right since the grader does exact string matching. Append one manifest line per chunk to `/home/user/chunks/manifest.txt` in the format `chunk_NNNN.dat <size> <checksum>`, written in chunk order as you process them (not after the fact) so line order matches file order. After compiling and running, sanity check yourself: concatenate all chunk files back together and diff against the original `archive.dat` to confirm byte-for-byte reconstruction, and manually recompute the last chunk's checksum by hand or with a one-off script to catch any off-by-one in the byte count on the final partial chunk.