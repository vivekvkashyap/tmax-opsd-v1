First run `tesseract /app/schema.png stdout` (try different `--psm` values if the default output is noisy) to recover the two node IDs and the join instructions embedded in the image — do not guess these values, read them from the OCR output. Then inspect `/app/network.db` with `sqlite3` to find the raw source tables the image told you to join (e.g. link/cost tables) and understand their columns. Materialize a clean `edges(src, dst, cost)` table into `/home/user/optimized.db` via a `CREATE TABLE ... AS SELECT` join, and add indexes on `src` (and `dst` if needed) — the whole point of "optimized" here is avoiding repeated full scans/joins at query time. For the shortest-path computation, do not reimplement a slow O(V^2) Dijkstra in the shell script the same way the naive reference likely does; use an efficient priority-queue-based Dijkstra (e.g. a short Python script with `heapq`, or a recursive CTE if you're comfortable with SQL) reading only from the pre-indexed table, and print just the integer total cost with no extra text. Benchmark your script against `/app/ref_slow.py`'s wall-clock time yourself before considering the task done, since the 5x speedup threshold is measured end-to-end.
