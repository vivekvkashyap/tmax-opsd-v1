Write the awk logic as a self-contained, testable unit before wrapping it in the bash entry script. Use `FS=","` and skip the header with `NR>1`, filter rows where `$4 > 50.0` (column 4, cpu_utilization) before accumulating any sums — filtering must happen before the correlation accumulation, not after, or you'll compute the wrong statistic. Accumulate `n`, `sum_x`, `sum_y`, `sum_xy`, `sum_x2`, `sum_y2` for columns 2 (inference_time_ms) and 3 (accuracy) using the standard Pearson formula `r = (n*sum_xy - sum_x*sum_y) / sqrt((n*sum_x2-sum_x^2)*(n*sum_y2-sum_y^2))`, guard the denominator against zero, and print with `printf "%.4f\n"` for exactly 4 decimal places. Make `/home/user/analyze.sh` take the CSV path as `$1` (not hardcoded) so it's reusable, invoke the awk script against that argument, and write the single resulting number to `/home/user/result.txt` with no extra text. Validate your awk script against a small hand-computed example first (e.g. 3-4 rows with known correlation) to catch formula/quoting bugs before trusting it on the full dataset — do not reach for python/R, only bash+awk+coreutils are allowed.
