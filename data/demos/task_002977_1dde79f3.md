Work the pipeline stage by stage and verify each before moving on. First, concatenate the three `.cst.*` parts in their alphabetical/lexical order (`cat sensor_dump.cst.aa sensor_dump.cst.ab sensor_dump.cst.ac`), then reverse the XOR by piping through a small script that XORs every byte with `0xAA` (Python: `bytes(b ^ 0xAA for b in data)`, or an equivalent C/`xxd`+`tr` trick), then `gzip -d` the result and `tar -xf` it into `/home/user/extracted_v1/` — check `file` output after each stage to confirm you actually have valid gzip/tar data before proceeding, since a mistake in ordering (XOR before concat, or decompressing before un-XORing) will silently produce garbage. Second, for `measurements.bin`, use Python's `mmap` module to map the file read-only and iterate in 16-byte strides, unpacking each record with `struct.unpack_from('<IIII', mm, offset)` to avoid loading the whole file into RAM; track the max `value` only where `status == 1`. Third, for backups, first do a plain recursive copy to `v1/`, add `update.log`, then create `v2/` using `cp -al` (hardlink-all) or `rsync -a --link-dest=/home/user/backups/v1 /home/user/extracted_v1/ /home/user/backups/v2/` so unchanged files share inodes — confirm with `stat -c '%i'` on matching files in v1 and v2.