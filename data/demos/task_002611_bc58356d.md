Start by running `tesseract /app/schema.png -` (or similar OCR invocation) to recover the actual mapping of `t1`/`t2`/`t3`/`t4` to Researcher nodes, KNOWS edges, LIKES edges, and Concept nodes — don't assume the mapping, read it off the OCR output, since the join logic depends entirely on it. Once you know which table is which, write a single `sqlite3` query (or a small shell function calling `sqlite3 -noheader -csv /app/graph_data.db "..."`) that: finds the researcher's id by name, joins to the KNOWS-edges table to get the ids of people they know, joins those ids to the LIKES-edges table, joins to the Concepts table for names, dedupes, sorts alphabetically, and joins with commas. Wrap that query in a handler script invoked per-connection, and drive the TCP listener with `socat TCP-LISTEN:8080,reuseaddr,fork EXEC:handler.sh` (or an `nc -l` loop) so each connection is handled independently and the server keeps accepting new ones — launch it with `&`/`nohup` and disown so it survives in the background. Test manually with `printf 'Alice\n' | nc 127.0.0.1 8080` before considering it done.