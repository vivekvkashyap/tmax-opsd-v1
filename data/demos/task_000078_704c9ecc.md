Set up an isolated environment first: `python3 -m venv /home/user/sim_env && /home/user/sim_env/bin/pip install scipy`, and run your analysis script with that venv's Python. In `/home/user/analyze_primers.py`, write a simple FASTA reader (skip `>` header lines, concatenate/track sequence per record) for both files, then for each sequence search for the forward primer `ATGCGT` and the reverse-complement-of-reverse-primer `TGCGTA` as literal substrings (e.g. with `str.find`/regex), checking every forward-primer occurrence against every candidate `TGCGTA` occurrence to find one where the second motif's start index falls 10-100 inclusive after the forward primer's end index — don't just check global presence of both substrings anywhere in the sequence, the positional gap constraint is the easy part to get wrong. Count amplified vs non-amplified per file, build the 2x2 contingency table `[[ref_amp, ref_not], [mut_amp, mut_not]]`, and call `scipy.stats.fisher_exact(table)` (two-sided, the default) to get the p-value — write it unrounded as a Python float. Dump the results dict to `/home/user/pcr_results.json` with `json.dump`, matching the exact key names given.