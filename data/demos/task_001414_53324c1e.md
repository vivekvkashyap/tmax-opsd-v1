Treat this as a graph-merge-then-longest-path problem. First load `papers.db` with `sqlite3`/Python's `sqlite3` module and pull only `citations_raw` rows where `is_valid = 1` — do not silently include invalid rows, that's the main trap. Separately parse `updates.jsonl` line by line, and for each `{"paper_id": X, "missing_citations": [...]}` add edges `X -> each id`. Merge both edge sets into one adjacency dict keyed by source id, deduplicating and sorting target lists ascending; only emit keys for papers that actually cite something, then write to `/home/user/output/graph.json` (create the output directory first). For the longest chain, since the graph is guaranteed a DAG, do a topological sort (or memoized DFS) computing the longest path starting at each node; when comparing candidate longest chains of equal length, prefer the one whose first node has the smallest id — so don't just take the first-found max, explicitly iterate nodes in ascending order and compare lengths carefully. Write the winning chain as a JSON array of ints to `/home/user/output/longest_chain.json`.