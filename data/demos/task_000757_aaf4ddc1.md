Work through this as a layered debugging exercise rather than rewriting from scratch. First fix the Makefile: it's likely invoking the wrong compiler/flags for a .cpp file (e.g. using `gcc` instead of `g++`, or missing `-g` for debug symbols) — correct it so `make` builds cleanly with debug symbols enabled. Second, run the binary and read the assertion failure message; inspect the CSV-parsing loop in the C++ source — a common bug pattern here is that the header row gets parsed into the record count/list alongside data rows, throwing off an assumed fixed row count used in an assert; use gdb (`gdb ./process_data`, `run`, `bt` on the abort) to confirm exactly where and why the assertion fires, then fix the parsing logic so it correctly skips or accounts for the header. Third, after the assertion issue is resolved, run again under gdb or with fprintf debugging to find the remaining crash — look for an off-by-one bug such as an inclusive `<=` loop bound that indexes one past the end of a records container, and fix the bound. Only after both fixes should you compute the expected average by hand from data.csv (score>50 filter) to sanity-check the final result.txt content before declaring success.