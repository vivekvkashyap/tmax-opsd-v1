Work this in three independent phases and verify each before moving on. Phase 1: rewrite startup.sh so it runs `init_routing.sh` synchronously (foreground, `wait`ed or not backgrounded) and only starts background services after it exits successfully; make it idempotent by guarding re-creatable state (e.g. check-before-create, or `mkdir -p`) so re-running doesn't error or duplicate processes; export TZ and LC_ALL with `export TZ=UTC LC_ALL=C`; and `chmod 700` the routing_rules directory. Phase 2: extract frames with `ffmpeg -i /app/legacy_dashboard.mp4 -vf fps=1 frame_%03d.png` (or similar), then for each frame sample the pixel at exactly x=320,y=240 (e.g. with Python/PIL or ImageMagick's `convert frame.png -crop 1x1+320+240 txt:-`) and read off the Red channel value — build your own ACTIVE/INACTIVE table from this per-frame measurement rather than guessing. Phase 3: in Rust, parse each stdin line as an IPv4 string, split on '.', parse the second octet as the zone, look up your derived active/inactive table (embed it as a const array you built from Phase 2's actual measurements), and format output using the runtime `TZ`/`LC_ALL` env vars (not hardcoded strings) — match the FORWARD/DROP logic and bracket format exactly, and compile with `cargo build --release`.