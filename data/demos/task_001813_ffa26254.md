Transcribe `/app/voice_memo.wav` with an available speech-to-text tool (e.g. `whisper`) and listen closely for how the sensitive code format is spelled out verbally (a prefix word, digit count, separators, a trailing letter sequence) — derive the exact regex pattern from what's said, don't assume a generic format. Write `/home/user/doc_filter.sh` (or `.py`, executable via `chmod +x`) that takes a file path argument and does `grep -qE '<derived-pattern>' "$1"` — exit 1 if the pattern matches (evil), exit 0 otherwise (clean); test this against a few sample strings you construct yourself before trusting it against the hidden corpus. For the compilation step: find recent `.md` files with `find /home/user/drafts -name '*.md' -mtime -7`, run each through your filter, and for clean ones append them to `/home/user/compiled_docs.md` — but since multiple writers may run this concurrently, wrap the append in `flock`, e.g. `flock /home/user/compiled_docs.md.lock -c "cat '$file' >> /home/user/compiled_docs.md"` (or use `flock -x 200; ... ; 200>>lockfile` inside the script), not a plain `>>` which is not safe under concurrent writers. Verify the final compiled file only contains content from clean, recently-modified drafts.