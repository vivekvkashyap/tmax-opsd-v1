Run `tesseract /app/historical_trend.png -` (or with `--psm 6` if columns get jumbled) to OCR the Year/Value table, then manually clean up any OCR noise (misread digits, stray commas) by eyeballing the raw text before trusting it. Once you have clean (x=Year, y=Value) pairs, compute the least-squares line entirely with awk: accumulate n, sum(x), sum(y), sum(x*y), sum(x^2) in one pass, then apply m = (n*sum_xy - sum_x*sum_y) / (n*sum_x2 - sum_x^2) and c = (sum_y - m*sum_x) / n using awk's floating point (or pipe the sums into `bc -l` for precision). Double-check the OCR'd numbers against sane expectations (e.g., values should look roughly linear) since a single misread digit will silently throw off the whole regression. Finally compute m*year + c for years 6 through 10 and write exactly 5 lines (one decimal value each, ascending year order) to /home/user/forecast.txt — no headers, no extra whitespace/blank lines, since the grader parses each line as a float and computes MSE against the exact analytical fit.