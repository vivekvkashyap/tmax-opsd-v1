Use `git log -p -- aggregate.py` (or `git log --oneline` then `git show <sha> -- aggregate.py`) to walk the history of `/home/user/repo/aggregate.py` and find the single commit where the summation implementation changed from a numerically-stable method to a naive running-sum loop — note the commit hash and diff, since your detector needs both the pre-regression logic (checkout that file at the parent commit, or reimplement the stable summation, e.g. Kahan/compensated summation or `math.fsum`-equivalent) and HEAD's current buggy logic. Separately, OCR `/app/alert.png` with `tesseract` to recover the exact tolerance threshold value — don't hardcode a guessed number. In detector.py, load the JSON float list, compute both sums, take `abs(correct - buggy)`, and print `EVIL` only if that absolute difference is strictly greater than the recovered threshold, else `CLEAN`, with no other output. Validate against the corpus directories described in the task by hand-running a few evil and clean cases to confirm your two summation implementations actually diverge/converge as expected before trusting the automated grading.