Build the script in clearly separated stages and validate each before moving on. First, load the Q0 matrix from CSV and implement `f(t) = exp(-0.1*t) * cos(t)**2`; write a helper `S(T)` that integrates f from 0 to T with `scipy.integrate.quad` (handle T=0 as a special case returning 0 to avoid a degenerate integral), scales Q0 element-wise by that integral, applies `scipy.linalg.expm`, and returns the Frobenius norm of the result minus the identity. Call `numpy.random.seed(42)` before any random draws, and only then start the Metropolis-Hastings loop: fix the log-likelihood as Normal(S_obs, sigma=0.1) with a hard reject (probability 0) outside [0,10], propose via `np.random.normal(current, 0.5)` each of exactly 5000 steps, and accept using `log(uniform(0,1)) < ll_prop - ll_current` — match this exact call order (propose, then draw uniform) since RNG state matters for reproducibility. Discard the first 1000 of 5001 stored states as burn-in, keep 4000. For the CI, do exactly 1000 resamples of size 4000 with replacement, take medians, then `np.percentile` at [2.5, 97.5]. Round both to 3 decimals and dump to JSON with the exact key names.
