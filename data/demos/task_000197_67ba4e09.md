This task has three independent stages — handle them separately and verify each in isolation.

Stage 1 (video): use `ffmpeg` to extract frames from `/app/dashboard_alert.mp4` (e.g. `ffmpeg -i in.mp4 frame_%04d.png`), then script a check per frame for pure red — read each frame's pixel values (e.g. via `ffmpeg`'s signalstats filter, or a quick Python/PIL loop) and count frames where every pixel is exactly RGB(255,0,0). Write only the integer count to `/home/user/red_frame_count.txt`.

Stage 2 (symlink loops): write `/home/user/sanitizer.sh` that walks the directory tree tracking visited inodes/paths (e.g. `readlink -f` each symlink and detect cycles, or use `find -L` with a depth/error check) rather than naive recursion, since naive recursive traversal will itself infinite-loop on cyclic symlinks. Exit 1 on any loop found, 0 otherwise. Test against varied structures before trusting it.

Stage 3 (log parsing): parse the multi-line block format with `awk`/`csplit` splitting on `--`, extract `Target`/`Status` pairs, filter `Status: FAILED`, then use `find -mtime -7 -name '*.dat'` under each surviving target, and `sha256sum` the results sorted by path into `/home/user/failed_dat_manifest.txt`.
