Before writing any query, inspect the schema live with `sqlite3 /home/user/data.db ".schema"` so column names match exactly. In your Python script, open the connection and immediately execute `REINDEX idx_manager;` (or `REINDEX;`) before any SELECT — this is a stated precondition, not optional cleanup. Build the recursive CTE bottom-up: the base case selects the employee row where `id = 845` with level 0; the recursive case joins `employees e JOIN cte c ON e.id = c.manager_id`, incrementing level each step, terminating naturally when a row has `manager_id IS NULL` (the CEO) since the join then produces no further match. Join `departments` on `dept_id` in the final SELECT (not inside the recursive step) to pull `department_name`. When building the JSON array, ensure `manager_id` serializes as JSON `null` (not the string "None") for the CEO row — use `json.dump` on a list of dicts built from cursor rows, converting Python `None` naturally. Execute the script and re-read `/home/user/audit_chain.json` to confirm ordering (level 0 first, ascending) and key names match the spec exactly before finishing.