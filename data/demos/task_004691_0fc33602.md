First reproduce the crash: `ulimit -c unlimited`, compile `/app/processor.cpp` with debug symbols (`g++ -g -O0 -o processor processor.cpp`), run it on `/app/transmission.wav`, then load the resulting core file in `gdb ./processor core` (`bt` for the crashing frame, `info locals`/`x/s` around global/heap addresses) to locate the in-memory session-ID string — search memory near global variables for a 16-character ASCII run and write it verbatim to `/app/session_id.txt`. For the crash fix, find the delta-encoding step that indexes or dereferences based on an unclamped difference between successive energy values; instead of crashing on a huge diff, clamp the encoded value into its valid range (e.g. int8 bounds) without touching the encoding formula for normal samples. For the performance bottleneck, replace the O(N*W) `compute_rolling_energy` nested loop with an O(N) sliding-window sum of squares: maintain a running `sum_sq`, add the newly entering sample's square and subtract the one leaving the window each step, taking care to match the original window/edge-count behavior exactly so floating-point results stay identical. Save the fixed file as `/app/processor_fixed.cpp`, compile with optimizations (`-O2`) to `/app/processor_fixed`, and time `./processor_fixed /app/transmission.wav /app/output.bin` to confirm it's under the threshold.
