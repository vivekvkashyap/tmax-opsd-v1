Start by trying to build the vendored library as-is to see the exact undefined-symbol errors, which will point to `exp`/`fabs`-style math functions — fix the `Makefile`'s link rule by appending `-lm` after the object files in the `gcc -shared ...` command (link order matters: `-lm` must come after the objects that use it). Next inspect `integrator.c`'s adaptive step-size logic: locate the line scaling `dt` by `(error / tolerance)` — this is inverted, since a larger error should shrink the step, not grow it; change it to scale by `(tolerance / error)` instead. Rebuild the shared library after both fixes and confirm no undefined symbols remain (e.g. `ldd`/`nm -D`). Then write `/home/user/prepare_data.c`, which parses argv[1] as a double, calls `integrate_density(x)` from the library's header, and prints the result with exactly `%.6f\n`. Compile and link against `libsim_model.so`, and make sure the resulting binary can find the shared library at runtime — set `-Wl,-rpath,<lib_dir>` at link time or export `LD_LIBRARY_PATH` before running. Test manually with a few sample x values across the 0.1–5.0 range to confirm output is a finite, sane number (not NaN/Inf) before considering it done.
