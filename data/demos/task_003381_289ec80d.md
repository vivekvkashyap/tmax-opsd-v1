Write `/home/user/process_sensors.py` as a clear four-stage pipeline and test each stage's intermediate output before moving to the next. (1) List all `shard_*.csv` files and read them in parallel with `concurrent.futures.ProcessPoolExecutor` (or multiprocessing.Pool) mapping a per-file read function, then concatenate the resulting DataFrames. (2) For deduplication, build the hash key per row as `md5(f"{event_time}{sensor_id}{temperature:.1f}".encode()).hexdigest()` — note the temperature must be formatted to exactly 1 decimal place before hashing, matching float values that may already have differing precision — then for rows sharing a hash keep only the one with the maximum `ingested_at` (sort by ingested_at descending and drop_duplicates on the hash, or groupby+idxmax). (3) Derive a plain date string from `event_time` and compute `groupby(['sensor_id','date'])['temperature'].mean()` for the daily average. (4) For the rolling average, set date as a DatetimeIndex per sensor, sort ascending, and use pandas' time-aware rolling window `.rolling('3D').mean()` (not a fixed row-count window) so that gaps in dates are handled correctly — this only averages days that actually exist within the trailing 3-day span rather than always averaging exactly 3 data points. Round to 2 decimals and nest the result into a dict-of-dicts JSON keyed by sensor_id then date string, and actually run the script to produce the output file.