For the video part, run `ffprobe -v error -show_entries format=duration -of default=noprint_wrappers=1:nokey=1 /app/test_video.mp4` and write just that numeric value (no extra text) to `/home/user/video_duration.txt`. For the filter, first diff/inspect several files in `/app/corpus/clean/` versus `/app/corpus/evil/` directly to spot the structural anomaly (the description hints at nested-bracket-style syntax) rather than assuming — the discriminator must be derived from the actual corpus, not guessed abstractly. Implement the C++ checker as an iterative (not naive unbounded-recursion) scanner over the file content — e.g. track bracket/nesting depth with a stack or counter and detect unmatched/negative-depth or unterminated structures — so it can't stack-overflow or infinite-loop on adversarial input; read the whole file safely and bound all loops by file length. Exit 0 for clean, 1 for evil. Compile with `g++`, then batch-test your binary against every file in both corpus directories (checking exit codes) and run under `valgrind --leak-check=full` on a few samples to confirm no leaks, plus time each run to confirm sub-second completion, before considering it done.
