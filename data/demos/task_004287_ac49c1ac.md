The core bug is a train/test leak: the original script computes normalization gain from the whole file, letting test-set statistics influence training. Fix the pipeline ordering rather than just tweaking a number: (1) split `interview.wav` into 10s segments with `ffmpeg -f segment -segment_time 10`; (2) count total segments, compute `train_count = floor(0.8 * total)` and partition by segment index (first N = train, rest = test) — do this purely from segment filenames/timestamps, never touch the full file's volume stats; (3) run `ffmpeg -af volumedetect` (or equivalent) only over the concatenated/aggregated train segments to get `max_volume`; (4) apply that single gain value via `ffmpeg -af volume=<gain>dB` independently to each train and each test segment (same gain, applied per-file, not recomputed per-segment); (5) run `/app/tools/transcribe` then `/app/tools/text2vec` on the normalized test segments only; (6) compute cosine similarity between each test embedding and its corresponding file in `/app/experiment/ground_truth/` (match by segment index/filename), average them; (7) write `Test_Similarity: <score>` to `/home/user/metrics.txt`. Keep the whole thing scripted in `/home/user/pipeline.sh` so it's reproducible end-to-end via `bash /home/user/pipeline.sh`, and actually run it to produce the file.