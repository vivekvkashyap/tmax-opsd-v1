Write curate_repo.sh to parse the ini dynamically rather than hardcoding the prefixes: use something like `grep '^OldPrefix=' /home/user/repo_config.ini | cut -d= -f2-` (and similarly for NewPrefix) to capture the values into shell variables, since the values contain backslashes and could change — never embed the literal `C:\OldSystem\Binaries\` string in your replacement logic. Find all `.meta` files with `find /home/user/artifact_repo -name '*.meta'`, and for each: convert with `iconv -f UTF-16LE -t UTF-8 "$f"` piped or redirected into a variable/temp step, then do the text substitution — since the old prefix contains backslashes, use a tool that treats them literally, e.g. pass the raw prefix into `sed` with backslash-escaping (`printf '%s\n' "$old" | sed 's/[.[\*^$/]/\\&/g'` to auto-escape sed metacharacters and backslashes) or use `awk`/`python3 -c` with plain string replace to sidestep escaping headaches entirely. Write the fully converted+substituted UTF-8 text to `"$f.tmp"`, then `mv "$f.tmp" "$f"` for atomicity (never write directly to `$f` in place). Make the script executable with `chmod +x`, then run it once and confirm with `file -i` that resulting files report utf-8/us-ascii and `grep` shows the new Linux paths.