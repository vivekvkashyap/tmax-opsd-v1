Write `/home/user/dataset_tool.cpp` to: open `/app/dataset_stream.raw` once with `open()`+`fstat()`+`mmap(..., PROT_READ, MAP_PRIVATE, fd, 0)` at startup (not per-instruction, to actually get the mmap benefit), then loop reading lines from stdin with `std::getline(std::cin, line)` until EOF. Parse each `extract <offset> <length> <scale> <output_path>` line, treat the mmap'd region as `int16_t*` indexed by `offset/2`, iterate `length/2` samples, multiply each by the float scale, round (match standard rounding — `std::round`) and clamp to [-32768, 32767] before casting back to int16_t, then write the raw bytes to a temp file in the *same directory* as `output_path` (compute the directory via string manipulation, don't assume cwd) using a unique suffix (e.g. include `getpid()`), and `rename()` it into place for atomicity. After each successful write, print `DONE <output_path>` and flush stdout immediately (`std::cout << ... << std::endl` or explicit `flush()`) so a caller pipelining requests can detect completion per-line. Since grading is bit-exact fuzz equivalence against a reference binary across ~1000 random offset/length/scale requests, pay close attention to endianness (little-endian, matching x86) and exact rounding/clamping order — test manually with a few crafted extract commands and compare byte-for-byte against a hand-computed expectation before considering it correct. Compile with something like `g++ -O2` to `/home/user/dataset_tool`.
