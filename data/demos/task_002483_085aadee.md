Write a small parser/script for `/app/project_archive.bin`: read the 6-byte header and validate the "PROJ" magic plus version bytes, then loop reading a little-endian uint16 path-length, the path string, a little-endian uint32 data-length, and the raw bytes, writing each out under `/home/user/extracted/<path>` (create parent directories as needed since paths may contain `/`) until EOF. Use Python's `struct` module for the little-endian unpacking to avoid endianness bugs. Separately, transcribe `/app/voice_memo_001.wav` with an offline STT tool (whisper.cpp/whisper), read the transcript text carefully to recover the spoken project code (a word plus a number), then normalize it to uppercase with punctuation/spaces stripped as the task specifies for the header value — try both the with-space and no-space forms if the transcript is ambiguous, since the auth check must match what the grader expects. Build the tarball atomically: `tar czf /home/user/backup/.latest.tar.gz.tmp -C /home/user extracted && mv /home/user/backup/.latest.tar.gz.tmp /home/user/backup/latest.tar.gz`, and generate `manifest.txt` with `md5sum`-style `MD5(path) = hash` lines for each extracted file. Finally implement the Flask/stdlib HTTP server enforcing the `X-Project-Token` header on every route before returning the three JSON endpoints, and leave it running.