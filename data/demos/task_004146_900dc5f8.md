Read `/home/user/train_model.cpp` closely looking for a struct/variable whose declared type is narrower than the floating-point value being assigned to it — a silent truncation bug typically looks like storing a `double` computation into an `int` (or `float`) field before it's printed, so the fractional part is dropped even though the print statement itself uses `setprecision`. Fix it by changing that field's type to `double` (do not touch the arithmetic or the print formatting, which are already correct) and recompile with `g++ -std=c++11 train_model.cpp -o model_trainer`. Then write `run_experiments.sh`: loop over the three learning rates literally, invoke `./model_trainer <lr>`, capture stdout, extract the number after `FINAL_LOSS=` with `grep -oP`/`sed`/`awk`, and append `lr,loss` lines to `experiments.csv`. Before finishing, run the trainer manually for one learning rate and confirm the printed loss has real decimal digits (not `.0000`) — that's the signal the struct fix actually worked — then run the shell script to regenerate the full CSV.
