Fix the package first: inspect `/app/custom_session_lib/setup.py` for the deliberate perturbation — look for a misspelled import and unbalanced parentheses in the `setup(...)` call — correct both, then `pip install -e /app/custom_session_lib` (or equivalent) so `import custom_session_lib` works. Next, recover the XOR key via known-plaintext: read the hex token from `/home/user/traffic.txt`, hex-decode it to raw bytes, and XOR those bytes against the known plaintext `username:guest_user,role:guest` byte-for-byte — this recovers the repeating key material (verify it actually repeats to confirm you found the true key length rather than a longer coincidental match). Build `forge.py` to construct plaintext `username:<arg>,role:admin`, XOR-encrypt it with the recovered repeating key, hex-encode, then validate the result by spawning a fresh subprocess that imports the library and calls `verify_token(token)`, capturing its exit status/exception rather than trusting an in-process call (since the library can segfault on bad input and you don't want that to kill `forge.py` itself). Only print the hex token on success; exit non-zero and print nothing extra on failure.
