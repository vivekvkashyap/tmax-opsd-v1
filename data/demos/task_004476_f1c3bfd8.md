Follow the pipeline steps in exact order since the verifier checks values with tight tolerance: (1) load the CSV, impute `memory_usage` NaNs with `df['memory_usage'].fillna(df['memory_usage'].median())` computed on the column including NaNs (median ignores NaN by default in pandas) — do this before touching `response_time`; (2) compute the 99th percentile via `df['response_time'].quantile(0.99)` and keep only rows `<= threshold` (strictly greater than are removed); (3) split features/target and call `train_test_split(..., test_size=0.2, random_state=42)`; (4) fit `GridSearchCV(RandomForestRegressor(random_state=42), {'n_estimators':[10,50,100],'max_depth':[3,5,None]}, cv=3)` on the training set only; (5) predict on the test set with `best_estimator_`, then bootstrap using `np.random.default_rng(42)` and, for each of 1000 iterations, draw `rng.integers(0, n, n)` indices to resample the paired (true, predicted) arrays and compute MSE, matching this exact resampling scheme since a different RNG call pattern will produce different numbers even with the same seed. Take `np.percentile(mse_scores, [2.5, 97.5])` for the CI bounds and dump `best_params_`, lower, and upper into the specified JSON structure at `/home/user/artifacts/experiment_summary.json`.