Structure this as a C daemon built around `inotify_init1`/`inotify_add_watch` on `/home/user/spool` for `IN_CLOSE_WRITE|IN_MOVED_TO`, with a blocking `read()` loop parsing `struct inotify_event` buffers — test the watch loop alone first before adding mmap/parsing. On a `.map` file, `mmap` it read-only and split lines on `:` into `SOURCE_FILENAME`/`TARGET_RELATIVE_PATH`. The core security logic: since the target need not exist yet, `realpath()` alone won't work — logically resolve `..`/`.` components yourself (e.g. push/pop path segments onto a stack) against a base of `/home/user/vault/`, then verify the resolved absolute path's prefix is exactly `/home/user/vault/` (with a trailing-separator check so `vault-evil` can't prefix-match `vault`); also reject any target starting with `/`. For allowed paths, recursively create missing parent dirs (mode 0755) then `rename()` from staging into vault. Append `[MOVED]`/`[BLOCKED]` lines to `security.log`, `unlink()` the processed `.map` file, and have the loop watch for a `SHUTDOWN` file to break and exit cleanly.