Build and validate the sanitizer script standalone before touching the multi-service plumbing. In `/home/user/validate.sh`, use `jq` to inspect the *type* of each field precisely, not just presence: `jq -e 'type=="object" and (.age|type)=="number" and (.age == (.age|floor)) and (.clicks|type)=="number" and (.clicks == (.clicks|floor)) and ((.amount|type)=="number")'` (or equivalent) — the key trap is that JSON has no separate "integer" type, so you must check the numeric value has no fractional part (e.g. compare against `floor`) to reject floats like `30.5` while accepting `30`, and separately reject values where the field is a string, null, or missing entirely (`jq` type checks will naturally fail on those). Exit 0 only when age and clicks are both whole numbers and amount is any number; exit 1 otherwise. Test it directly against the given clean/evil examples in the task before wiring it up. For the services, edit `/home/user/app/config.env` to set `REDIS_PORT=6379` (matching Redis's real default) and `VALIDATOR_SCRIPT=/home/user/validate.sh`, then run `/home/user/app/start_services.sh` and confirm with `curl` that valid payloads land in the SQLite `events` table (`sqlite3 /home/user/app/db/transactions.db 'select * from events'`) while malformed ones are silently dropped, matching what your validator rejects.