First recover the two hidden configuration parameters (the ESCAPE character and MIN_RUN integer) by OCRing `/app/config.png` (e.g. `tesseract /app/config.png -` or an equivalent OCR tool); inspect the extracted text carefully since OCR can misread punctuation/digits, and sanity-check the values make sense before hardcoding them into your program. Then implement the RLE compressor as a single-pass character scanner (do not use naive regex substitution, since escaping and run-detection interact): walk the input left to right, and for each position, count the length of the run of the identical current character. If that run length is >= MIN_RUN, emit `ESCAPE + char + length + ESCAPE` regardless of whether char is the escape character itself (this covers rule 3 automatically since escaping the char once still yields the correct doubled-escape form when char==ESCAPE). If the run is shorter than MIN_RUN, emit each character literally, but double any literal occurrence of ESCAPE. Handle edge cases explicitly: empty input, input entirely of escape chars, and runs at buffer boundaries. Write this as a stream-processing filter reading stdin and writing stdout (avoid loading everything with Python-level string concatenation in a loop — use a list buffer or similar for O(n) performance since it will be fuzzed with inputs up to 50000 chars). Save to `/home/user/compress_rotate` and `chmod +x` it.
