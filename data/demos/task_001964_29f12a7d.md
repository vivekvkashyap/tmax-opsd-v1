Fix the vendored package first: run `make` in `/app/vendored_data_parser` and read the linker error — it's likely a stale/wrong library flag in `LDFLAGS`/`LIBS` (a hardcoded old library name); find and swap in the correct one, `make clean && make`, then confirm `import vendored_data_parser` and its parse function work on a test hex string. For semver comparison, parse `major.minor.patch` from both strings and implement compatibility as: majors must match exactly, then available's (minor, patch) must be >= requested's — reject any lower major outright. Write property-based tests with `hypothesis` asserting that a strictly lower available major always yields `False`; capture the run's output to `/home/user/test_results.log`. For the API, implement `POST /validate` requiring the exact Bearer token (401 if missing/wrong), check `schema_version` against the fixed internal version via `is_compatible` (400 if incompatible), then call the vendored parser's hex function and return JSON on 200. Start the service, confirm each case behaves correctly, and write its PID to `/home/user/service.pid`.
