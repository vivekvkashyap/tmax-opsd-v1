First recover the two numeric thresholds from the image by running `tesseract /app/spec_chart.png stdout` (or similar OCR) and reading off the printed correlation and p-value threshold text — do not guess values, extract them directly from the OCR output and hardcode only what you actually read. Then implement filter.py to load the .npy array with `np.load`, compute the pairwise Pearson correlation matrix via `np.corrcoef(data, rowvar=False)`, and check the off-diagonal absolute values against the OCR'd correlation threshold (mask the diagonal so self-correlation of 1.0 doesn't trigger a false reject). For the mean check, run `scipy.stats.ttest_1samp(data[:, j], popmean=0.0)` per column and compare each p-value against the OCR'd p-value threshold using strict-less-than as specified. Use vectorized numpy/scipy calls (avoid Python-level loops over samples) to stay under the 200ms budget — `np.corrcoef` and `scipy.stats.ttest_1samp` with axis support already vectorize across all D features. Exit 0 only if both checks pass everywhere, else exit 1; verify against a few of your own synthetic clean/correlated/mean-shifted arrays before trusting the corpus evaluation.