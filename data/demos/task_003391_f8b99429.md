This has two independent pieces: OCR extraction and QCOW2 analysis — build and validate each separately before combining. For the threshold, use `pytesseract.image_to_string(Image.open('/app/dashboard.png'))` to OCR the dashboard text, then extract the "Max Utilization Threshold" percentage from the OCR output with a regex (something like matching a number immediately before a `%` on the relevant line) rather than hard-coding any value — print/inspect the raw OCR text first to confirm what pattern actually appears, since OCR can introduce noise. For the QCOW2 analysis, run `qemu-img info --output=json <file>` per file, parse the JSON, and compute `utilization = (actual-size / virtual-size) * 100`; be careful to read the exact JSON keys `qemu-img` emits (they use hyphens, e.g. `actual-size`, `virtual-size`) via `json.loads`, not brittle string parsing. Process `.qcow2` files sorted alphabetically by filename, only emitting an fstab line for those with utilization strictly less than the OCR'd threshold, assigning `/dev/nbd<X>` with X incrementing only for included images (not skipped ones). Match the fstab line format exactly, including the `<filename_without_ext>` mount path.
