First OCR `/app/localization_rules.png` (e.g. `tesseract /app/localization_rules.png stdout`) to recover the exact rolling window size, variance threshold, and multiplier the ETL config specifies — do not assume typical defaults, read them from the image and use those recovered numbers in your logic. Then implement the core normalizer (Python is easiest for JSON/Unicode/decimal handling, wrapped by `/home/user/normalize_stream.sh`): for each stdin line, `json.loads` it (which naturally decodes `\uXXXX` escapes), then parse `raw_value` by detecting the locale's decimal/thousands separators (e.g. strip non-breaking/narrow spaces used as thousands separators, then decide whether `,` is a thousands separator or decimal comma based on position/locale) into a float. Maintain a per-`metric` sliding window (deque of the recovered window size) to compute the rolling average over only the values seen so far when the window isn't full yet. Compare the current normalized value's deviation from the rolling average against the recovered variance threshold (in units of that metric's rolling standard deviation) and cap it if exceeded, setting the outlier flag accordingly; then apply the recovered multiplier. Emit CSV with values rounded to 4 decimal places, matching float formatting exactly since the grader diffs byte-for-byte against a reference across thousands of fuzzed lines — test incrementally against small hand-built cases before trusting bit-exactness.