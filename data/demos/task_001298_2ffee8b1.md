Rewrite parseArtifact as a proper state machine that scans for the literal marker `name:` in the message, then collects characters into the name buffer until hitting a delimiter like space or `|` — trace through the exact message format `[ARTIFACT] name:<name> | deps:<deps>;` character by character to get the start/stop conditions right, rather than patching the existing broken state transitions (the current code has a bug where it breaks immediately after entering the name-capture state without ever appending characters). For concurrency, the deadlock comes from sending to an unbuffered channel with no concurrent reader draining it while the main goroutine is still blocked reading websocket messages in the same loop — fix this either by making the channel buffered large enough, or (cleaner) by launching a separate goroutine that ranges over the channel and prints results concurrently with the read loop, closing the channel only after `wg.Wait()` completes in yet another goroutine so the print-loop doesn't block forever. Build and manually run the client against the server to confirm `Processed: core/utils/app` are printed before writing e2e.sh, which should background the node server, sleep, run the Go client capturing output.log, then kill the server process by PID and exit 0.