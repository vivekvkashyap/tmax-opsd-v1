Model this as a directed graph where each package points to its direct dependencies, and you need the sum of vulnerability_score over the set of all distinct nodes reachable from each package (excluding itself's own contribution being double counted, but including itself once). Load the three CSVs, build an adjacency list keyed by pkg_id from dependencies.csv, and build a vulnerability_score lookup from vulnerabilities.csv defaulting missing pkg_ids to 0. For each package, run a graph traversal (DFS/BFS, or memoized recursion) from that package, collecting the set of unique reachable pkg_ids (including the start node itself), then sum their scores — using a set is essential so diamond-shaped dependency paths don't double-count a shared transitive dependency. Memoize per-node reachable-sets bottom-up (topological order or recursion with caching) rather than recomputing full traversals for every package, since dependency graphs can share large subgraphs. After computing total_risk per pkg_id, join back to pkg_name, sort by total_risk descending then pkg_name ascending for ties, and write the JSON array with exactly the two required keys to /home/user/package_risk.json, then run the script to confirm output.
