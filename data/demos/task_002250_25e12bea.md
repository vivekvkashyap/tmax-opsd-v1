Design the data structures before coding: you need (a) a "seen" set covering every integer ever encountered (a hash set, or if you can bound the input range, a large enough boolean array — but prefer a general hash-set approach since input range isn't guaranteed small), and (b) a separate fixed-size circular buffer of the last 4 *unique* values used only for the rolling average, kept distinct from the seen-set. Process the stream line by line: for each new integer, first check global seen-set membership and skip if already seen; otherwise, if the window already holds 4 unique values, compute the float average of the current window's 4 values and compare the new value against `average + 10.0` using strict `>` before updating the window — do the anomaly check BEFORE inserting the new value, using the old window, not after (a common bug is checking against a window that's already been updated to include the new value). Then slide the window (drop oldest, append newest) and mark the value as seen. Print `Anomaly: %d\n` for each flag, in encounter order. Compile with plain `gcc`, run against `/home/user/stream.txt`, and trace through the first several values by hand to confirm your window/average logic matches before trusting the full output.