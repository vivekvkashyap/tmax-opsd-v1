Implement this as straightforward C: read `/home/user/experiments.csv` line by line with `fscanf`/`strtok`, parsing `exp_id` as a char and the rest as doubles/ints. Maintain a small in-memory table (array or simple hash) keyed by `exp_id`, accumulating success/failure counts as you go: for each row compute `prediction = f1*w1 + f2*w2 + f3*w3`, compare `fabs(prediction - target) < 0.1`, and increment the per-`exp_id` success or failure counter (also store `prior_alpha`/`prior_beta` the first time you see each `exp_id`, since they're constant per group). After processing all rows, iterate `exp_id`s in alphabetical order (sort them, e.g. via a fixed-size sorted array since IDs are single chars), compute `posterior_alpha = prior_alpha + successes`, `posterior_beta = prior_beta + failures`, and `expected_value = posterior_alpha / (double)(posterior_alpha + posterior_beta)`. Write the output CSV with `fprintf` using `%.4f` for the expected value, matching the header exactly. Compile with `gcc -o analyze analyze.c -lm`, run it, and diff `/home/user/results.csv` against your own hand-computed expectation for a couple of rows to catch off-by-one or grouping bugs.