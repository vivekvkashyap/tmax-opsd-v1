Start by extracting the routing rules from the image: run `tesseract /app/routing_rules.png stdout` and read the recovered rule text carefully — note the exact ordered conditions (prefix/suffix/substring checks and their priority) and the exact target directory strings; don't guess at directory names or rule order, they must match the image precisely. Implement `router.py` as a straightforward top-to-bottom if/elif chain over `sys.argv[1]` mirroring that exact rule order, printing only the matched path with no trailing newline discrepancies (use `print(path, end='')` or strip before printing) since it will be fuzz-compared byte-for-byte against a legacy oracle binary. For the Go tester, spin up goroutines that send the same filename to both `python3 router.py <name>` and `/app/legacy_router <name>` via `os/exec`, collect results over channels, and diff stdout. Cross-compile with `GOOS=linux GOARCH=amd64 go build -o /home/user/bin/tester_amd64` and the `arm64` equivalent. Pitfall: since the real pass/fail is the fuzz test against `/app/legacy_router`, verify your OCR'd rule order and exact target strings against a handful of manually-tested filenames covering each rule branch before trusting the implementation.
