Decode the video like a signal, not a picture: extract every frame of `/app/dataset_transmission.mp4` in order (ffmpeg/opencv), convert to grayscale, and compute mean pixel intensity; threshold at 128 for one bit per frame (>128 → 1, else 0). Group bits into bytes 8-at-a-time, MSB-first, decode as ASCII — it should parse as a JSON array of `{source, target, weight}` edges; if not, recheck frame ordering/bit-grouping before doubting the threshold. Design a normalized SQLite schema (`nodes` plus `edges` with FKs and a `CHECK(weight > 0)`), inserting via parameterized `?` placeholders, never string-formatted SQL. For the API, build a Flask/FastAPI app on `0.0.0.0:8080` with `POST /shortest_path`: validate the `Authorization: Bearer RESEARCH_TOKEN_99` header first, returning 401 on mismatch, then run shortest-path (e.g. `networkx.shortest_path`, `weight='weight'`) over edges loaded from the DB, returning path+cost or 404 if disconnected. Leave the server running for the verifier.