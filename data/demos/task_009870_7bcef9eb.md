Create the symlink with `os.symlink('/home/user/dataset.tar', '/home/user/latest_dataset')` (source, then link name — easy to swap the argument order by mistake). Open the archive through the symlink with `tarfile.open`, iterate `tar.getmembers()`, and for each member: skip it unless `member.isfile()` (skip directories/symlinks inside the archive too), and treat it as unsafe if `'..' in member.name` or `member.name.startswith('/')` — reject those instead of extracting them, this is the zip-slip/path-traversal defense the task is testing. For safe members, extract only that one member (`tar.extractfile(member)`) into `/home/user/extracted_safe/` using just the basename, read the first 4 bytes of the extracted content, and format them as `''.join(f'{b:02X}' for b in header_bytes)` for an 8-character uppercase hex string. Collect (basename, hex) pairs, sort alphabetically by filename, and write space-separated lines to `/home/user/headers.txt`. Double-check nothing gets written outside `extracted_safe/` — don't call the archive's blanket `.extractall()` anywhere.
