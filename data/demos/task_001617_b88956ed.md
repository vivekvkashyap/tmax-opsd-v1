Recover the inputs from the image first, then apply the comparison and math logic, then wire up the server. Run `pytesseract.image_to_string` on `/app/artifact_info.png` and print the raw output to confirm exactly how the `BUILD_VERSION:` line and the two rows of the `COEFFICIENTS` matrix are formatted before writing a parser for it — don't assume a fixed character offset, split on the label and use a regex to pull the version string and the four integers robustly. Compare the extracted version against baseline `"2.0.0"` using proper semantic-version comparison (split into major.minor.patch integers and compare tuple-wise; a naive string comparison will misorder something like "2.10.0" vs "2.9.0"). For the 2x2 matrix `[[a,b],[c,d]]`, the determinant is `a*d - b*c`. Stand up a small HTTP server (stdlib `http.server`, Flask, or FastAPI+uvicorn) bound to `127.0.0.1:8080` with `GET /api/artifact` that returns `{"version": <extracted>, "determinant": <calc if version > baseline else 0>}` as JSON, and make sure the process is actually listening (test it yourself with curl) before you finish, since the verifier connects to that exact host:port.
