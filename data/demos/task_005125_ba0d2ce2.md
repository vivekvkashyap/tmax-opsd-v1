Treat the legacy_decoder binary as an opaque filter: pipe the raw file through it (`/app/legacy_decoder < /home/user/raw_sensor_logs.txt > decoded.csv`) rather than trying to reimplement its decoding logic yourself — you don't need to reverse-engineer it, just use it as intended via stdin/stdout. For cleaning, build the pipeline as a single chain of standard tools operating on the decoded CSV: `awk -F, '$3!="ERROR" && $3+0>=0 {print}'` to drop bad/negative rows, then uppercase the sensor_id field (e.g. with `awk` or `tr` on that specific column, not the whole line), then `sort -u` or `sort | uniq` for dedup, then a final `sort -t, -k1,1` on the timestamp field for chronological order — get the field order and separators right since column position determines what gets normalized. Compute [COUNT] via `wc -l cleaned_data.csv` and [SENSOR_COUNT] via `cut -d, -f2 cleaned_data.csv | sort -u | wc -l`, then substitute into the HTML template exactly. For the server, write a bash `while true; do` loop around `nc -l -p 9090` (or `socat TCP-LISTEN:9090,reuseaddr,fork EXEC:...`) that reads the request line, checks the path, and echoes a proper `HTTP/1.1 200 OK\r\nContent-Length: ...\r\n\r\n` header before the file body — get Content-Length right or clients will hang; background it with `&` and disown.
