Tackle the four subsystems independently and verify each before moving on. For the vendored library, diff-read /app/bash-ini-parser/bash-ini-parser.sh line by line (or run `bash -n` on it) to find the unbalanced bracket/quote syntax error, fix it in place, then copy the corrected file to ~/.local/lib/bash-ini-parser.sh and confirm it sources cleanly with no errors. For the filesystem: `dd`/`truncate` a 100MB sparse file, `mkfs.ext4` it, create the mount directory, then mount with `fuse2fs` (since you lack root, this must be the FUSE-based ext4 driver, not the kernel mount command) and confirm with `mount | grep`. For the systemd unit, write do_restore.sh, create `~/.config/systemd/user/restore-test.service` with an `Environment=PATH=/home/user/.local/bin:$PATH` (or similar) line in the `[Service]` section — this is the step naive attempts skip, since default systemd --user PATH is minimal — then `systemctl --user daemon-reload && enable --now`. For analyze_restore.sh, extract job id, restored count, error count, and the ordered list of failed file paths from stdin using targeted grep/sed patterns per log line prefix (e.g. `[INFO] Starting restore job`, `[ERROR] Failed to extract:`), joining failures with commas or printing NONE.