Work the four sub-problems in order, validating each before moving on. First, recover the deleted header from the ext4 image: mount or inspect `/home/user/ext4_dump.img` read-only (e.g. `debugfs /home/user/ext4_dump.img` and use `lsdel`/`logdump`/`icat` on the recovered deleted inode, or try `extundelete`) to pull back `expr_parser.h` content into `/home/user/src/`. Second, get `math_evaluator.cpp` compiling: address the specific classes of errors named in the task (missing includes for math functions, missing `-lm` link flag) rather than guessing — read each compiler/linker error message and fix its exact cause. Third, do NOT try to reverse-engineer `/app/math_oracle`'s internals — treat it strictly as a black-box reference: write a fuzzer that generates random valid arithmetic expressions with nesting and unary minus (e.g. `5 * -3`, `(2+(3))*4`), run both your compiled evaluator and `/app/math_oracle` on each, and diff the numeric outputs to surface parser bugs (especially around consecutive operators and parenthesis depth tracking) — fix your parser's tokenizer/recursive-descent logic, not the oracle. Finally build `/home/user/final_evaluator` and re-run your fuzzer at larger scale to confirm the match rate is comfortably above 99% before finishing.