First fix the vendored build: open `/app/vendor/telemetry-parser/Makefile` and look for anything disabling cgo (e.g. `export CGO_ENABLED=0`) — since the package's stats module uses `import "C"` for a cgo call, cgo must be enabled; flip it to `1` and confirm `make build` succeeds. For the classifier itself, create the Go module under `/home/user/prof_classifier/`, add `gonum.org/v1/gonum` as a dependency (`go get gonum.org/v1/gonum/dsp/fourier` or similar), parse the CSV's `latency_ms` column into a `[]float64`, and compute its FFT/power spectrum. The key detection logic: look at the magnitude spectrum excluding the DC (0Hz) bin, find the peak magnitude, and compare it against the noise floor (e.g. mean or median of the remaining spectrum) — a synthetic sinusoidal artifact will show one bin whose magnitude is many multiples (pick and tune a clear threshold, e.g. several standard deviations or a large ratio) above the rest, while authentic log-normal noise has a roughly flat spectrum with no such outlier. Print exactly `EVIL` or `CLEAN` per the decision. Tune your threshold empirically: build the tool, then run it against every file in both `/home/user/corpora/clean/` and `/home/user/corpora/evil/`, adjusting the detection threshold until all 50+50 classify correctly, before finalizing the compiled `trace_filter` binary in `/home/user/prof_classifier/`.
