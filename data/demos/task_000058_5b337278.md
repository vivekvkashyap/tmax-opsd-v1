Work in two clearly separated phases. First, read `/app/legacy/process_v2.py` closely and port the exact arithmetic to Go: split each `ID|Timestamp|v1,v2,...` line, average the values, and round to 3 decimals using the same rounding convention Python uses (Python's `round()` is banker's rounding on ties, not simple half-up) — mismatches here are the most common source of fuzz failures. Sort output by numeric Timestamp ascending, then by ID string ascending, and preserve exact formatting (no trailing zeros mismatch, no scientific notation) in the printed float. Build the binary at `/home/user/migrated/processor` reading stdin/writing stdout, and self-test it by generating random inputs and diffing against `python2 process_v2.py` directly before touching the services. Second, wire the pipeline end to end: generate Go and Python protobuf/gRPC stubs from `/app/services/proto/data.proto` with `protoc`, implement a gRPC server on port 50051 that reuses your ported averaging logic, fix the Flask gateway to build a gRPC stub/channel to localhost:50051 and call it in the `/api/process` handler, and add an nginx `location /api/process { proxy_pass http://localhost:5000; }` block. Start all three services and verify the full chain with a manual curl before finishing.
