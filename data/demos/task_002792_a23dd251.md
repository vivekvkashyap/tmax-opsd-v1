Write a recursive CTE in SQLite that starts with `SELECT id, parent_id, 0 AS depth FROM tasks WHERE id='ROOT'` as the base case, then unions with a recursive step joining `tasks t ON t.parent_id = cte.id` incrementing depth by 1 — critically, add `AND t.status != 'FAILED'` in the recursive join condition itself (not just a WHERE filter after the fact), so that a FAILED node is excluded AND none of its descendants are ever reached by the recursion in the first place (pruning, not just filtering). Then apply the exact ORDER BY the task specifies: `depth ASC, exec_time DESC, id ASC`. Use SQL `LIMIT 5 OFFSET 5` to get page 2 with page size 5 rather than slicing in Python, so the ordering semantics match exactly. Use only `sqlite3` and `csv` from the standard library: open the CSV writer with `newline=''`, write rows as `(id, depth, parent_id)` with no header, matching the requested column order exactly. After running, diff your CSV against your own hand-traced expectation of which nodes survive pruning and their depths before finishing.