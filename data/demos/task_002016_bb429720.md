Write this as a small, defensive Python script rather than a one-off regex hack, since correctness on exact matching and robustness on missing/empty files are both graded. Parse each log line with a precise regex or split that pulls out the level, message, IP, and port fields from the fixed format — match on `ERROR`, message containing `Port forwarding failed`, and `PORT:8080` exactly (not just any port, and not partial message matches); extract the IP that follows `IP:` on that same line. Collect matching IPs into a set to dedupe, then write them to alerts.txt sorted with plain lexicographic string sort (not numeric/IP-aware sort) since the spec asks for alphabetical order. Wrap the file read in a check/try-except: if `/home/user/fw_logs.txt` doesn't exist or is empty, don't let an exception propagate — catch it, write an empty alerts.txt, and exit 0 cleanly. Test both paths yourself: run the script once against the real log file to produce the expected alerts, and separately simulate the missing-file case to confirm it doesn't crash and produces an empty output file with exit code 0.
