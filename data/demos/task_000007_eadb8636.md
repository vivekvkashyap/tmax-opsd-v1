This is a black-box reverse-engineering task: treat `/app/cleaner_oracle` as an opaque binary you must characterize by observation, not read the source of. First inspect it statically — run `file`, `strings`, and if available a disassembler/`objdump -d` on it to look for hints (float constants, comparison immediates) even though it's stripped; stripped binaries still often leak literal constants in `.rodata`. Then probe it dynamically and systematically: pipe crafted `int32_t` sequences into it via a small Python script using `struct.pack('<i', ...)` for input and `struct.unpack('<f', ...)` to read each output float, and binary-search / sweep candidate sentinel values (e.g. common missing-value markers like -1, 0, INT32_MIN, -9999, -999999) to find which single input value maps to a NaN output — check the NaN's raw bit pattern too (quiet vs signaling) via `struct.pack('<f', out)`. For non-sentinel inputs, feed a range of known integers and solve for the linear scale factor from `output/input` ratios (use exact/fractional inputs like powers of two to get an exact multiplier, not a rounded guess). Once you've nailed the sentinel and multiplier exactly, write the C program, mimicking IEEE754 NaN by punning the exact bit pattern you observed, and compile with the given command, then fuzz-test your own binary against the oracle before finishing.
