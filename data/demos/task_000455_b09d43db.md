This is a classic train/test data-leakage bug: read `normalize.c` and notice the mean/std loops iterate over `count` (all rows) instead of `train_size` (the first 80%). Fix it by computing `sum`/`mean` and the squared-deviation sum/`std` only over indices `0..train_size-1`, using population variance (divide by `train_size`, not `train_size-1`), then apply that same mean/std to normalize both the training rows and the test rows in the existing write loops — don't touch the split boundary, output paths, or the `%.4f` formatting. Recompile with `gcc -o normalize normalize.c -lm` and run it against `/home/user/data.csv` to regenerate both CSVs. Before finishing, sanity-check by hand-computing the training mean/std from the first 8 rows of `data.csv` and confirming the normalized test values look consistent with those training statistics (not with statistics that include the test rows) — that's the signature that the leak is actually fixed.
