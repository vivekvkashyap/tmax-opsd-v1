Handle embedded-newline CSV correctly first, since naive line-based tools (raw `awk`/`grep` on lines) will mis-split quoted multi-line fields — use a proper CSV-aware pass (e.g. a small `python3 -c` snippet using the `csv` module invoked from within the bash script, or `csvkit`) to read records respecting quoting, then join any newline inside a quoted field into a single space, re-emitting well-formed CSV rows before doing any further line-oriented processing. For deduplication, compute `md5sum` of the concatenation of `timestamp`+`sensor_id` per row (e.g. `printf '%s' "$ts$id" | md5sum`) and keep only first-seen hashes, preserving row order and the header. For normalization, do two passes over the data: first find global min/max of `value` (via `sort -n`/`awk` aggregation), then a second pass computing `(value-min)/(max-min)` with `awk` using `printf "%.4f"`. For anomaly detection, group by `sensor_id`, sort each group by `timestamp` ascending, and compute the absolute difference in `norm_value` between consecutive rows per sensor (track previous value in an awk associative array keyed by sensor), flagging distances `> 0.5`; write anomalies sorted by timestamp with 4-decimal distances. Test your script end-to-end against small crafted CSVs with embedded newlines and duplicates to confirm exact output formatting before trusting it on the real input.
