The Go program already computes `mean`, sample `stdDev` (N-1 denominator), and `n` — you only need to add the missing confidence-interval logic exactly per the given formula: `margin := 1.96 * stdDev / math.Sqrt(n)`, `lowerBound := mean - margin`, `upperBound := mean + margin`, then print exactly `PASS` if `target > lowerBound && target < upperBound` (strictly inside, matching "strictly inside the interval" in the spec) else print exactly `FAIL`, with no extra text or newline variations since the report format requires exact string matching. Build/run with `go run check_accuracy.go <file>` (or `go build` then execute) once per CSV in `/home/user/datasets/`, capturing each program's stdout. Iterate over the datasets sorted alphabetically by filename (`exp1.csv`, `exp2.csv`, `exp3.csv` glob-sorts correctly already, but sort explicitly rather than relying on directory listing order) and write `/home/user/report.txt` with one `<filename>: <RESULT>` line per dataset, verifying the file has no trailing/leading blank lines beyond what's expected.