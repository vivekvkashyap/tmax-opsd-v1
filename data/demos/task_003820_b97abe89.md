Start by transcribing `/app/alert.wav` (e.g. with a speech-to-text CLI/library) to recover the phonetically-spoken backup node ID — map spoken letters/numbers (like "alpha", "delta", "niner") to the ID format used in `backups.json` (letter-dash-number); don't guess the ID from the task text. For the C++ service: parse `/home/user/backups.json` with `nlohmann::json`, build a parent->children adjacency map, then do a DFS/BFS from the recovered target ID to collect ALL descendants (not just direct children), filter to `status == "SUCCESS"`, and sort by `timestamp` descending. Use `httplib::Server` to bind strictly to `127.0.0.1:8080`, implement `GET /audit` reading `page` and `limit` query params (default sensibly if absent, but tests will always pass both), slice the sorted descendant list for that page, and serialize the exact schema `{"data":[{"id":...,"timestamp":...}], "total_descendants":..., "page":...}` — field order and types matter for strict JSON schema validation. Compile with `-std=c++17 -lpthread`, launch the server in the background (e.g. `nohup ./server &` or via `&` and verifying with `curl`), and confirm it stays alive and responds before finishing.
