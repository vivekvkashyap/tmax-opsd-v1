Clean the project tree with `find /home/user/projects -name '*.bak' -delete` and `find /home/user/projects -name settings.conf -exec sed -i 's/^SECRET_TOKEN=.*/SECRET_TOKEN=REDACTED/' {} +` (anchor the sed pattern to line-start so you only touch the token line, not other content). Archive with `tar -czf /home/user/www/clean_projects.tar.gz -C /home/user/projects .` — the `-C` flag is what makes the tarball's internal paths relative instead of embedding the absolute /home/user/projects prefix. For the vendored server, inspect serve.sh's listener line: it invokes `nc` with an invalid flag that makes it fail to bind; remove the bad flag (or rewrite the invocation to a plain valid `nc -l -p $PORT` / equivalent) so the loop actually listens. Then launch it backgrounded (e.g. `nohup ./serve.sh /home/user/www &`) bound to 127.0.0.1:8080, and confirm with `curl -sI http://127.0.0.1:8080/clean_projects.tar.gz` that it serves the archive before considering the task done — the verifier connects to that exact port and path.
