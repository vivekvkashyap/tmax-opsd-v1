Define `ci_analyzer.proto` exactly per spec (package `ci_video`, service `CIAnalyzer`, RPC `RunEmulator`, message fields with the given field numbers), then generate Python stubs with `python -m grpc_tools.protoc -I. --python_out=. --grpc_python_out=. ci_analyzer.proto`. For video analysis, use `ffmpeg`/`opencv-python` to sample the center pixel at the midpoint of each 1-second window (0.5s, 1.5s, ...) — compute frame indices from the video's actual FPS rather than assuming a fixed frame rate, and classify each sampled RGB pixel against the six color categories using the >200/<50 channel thresholds given. Implement the stack VM exactly as specified (PUSH, ADD, MUL popping two and pushing result, POP_OUT appending to an output list, HALT stopping immediately) and keep sampling frames until you hit HALT or run out of video. In `server.py`, implement the gRPC servicer's `RunEmulator`, and at the top of the handler inspect `context.invocation_metadata()` for an `authorization` key equal to `Bearer secret-token-123`, calling `context.abort(grpc.StatusCode.UNAUTHENTICATED, ...)` if absent/wrong before doing any work. Bind the server to `0.0.0.0:50051`, log startup to `/home/user/server.log`, and leave it running in the background for the verifier to connect to.