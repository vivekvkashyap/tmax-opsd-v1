Split this into the two independent parts and verify each in isolation before combining. For Part 1, first read `app_server.py` to confirm exactly when/how it creates the `active_data` symlink, then rewrite `start_all.sh` so `cost_analyzer.py` is only launched after that symlink exists — poll for the symlink's existence in a loop (e.g. `while [ ! -L /home/user/app_data/active_data ]; do sleep 0.5; done`) before backgrounding the analyzer, while still starting `app_server.py` in the background immediately; both processes must be launched with `&` (or `nohup ... &`) so the script returns while they keep running, and the script itself must not block forever. Test by running the script and using `pgrep` after a few seconds to confirm both processes are alive. For Part 2, implement the classifier by checking the rules in the exact order given — extension checks first (case matters), then read only the first line of the file, and wrap file reads in try/except so any unreadable/empty file falls through to the default `KEEP`. Since the script will be run against a large hidden corpus, don't special-case example filenames; implement the general rule logic and test it against a few files you construct yourself matching each rule branch.
