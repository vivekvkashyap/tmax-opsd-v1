Scaffold with `cargo new /home/user/archiver` and add `serde`, `serde_json`, and `memmap2` to `Cargo.toml`. Structure the program in clear stages: (1) deserialize `/home/user/backup_config.json` into a struct with `source_dir`, `archive_dir`, and `expected_magic` (a `Vec<u8>` or `[u8;8]`); (2) parse `/home/user/data/sync.log` by splitting on `[TRANSACTION]`/`[/TRANSACTION]` blocks (or scanning line-by-line, resetting state at each block start) and collecting the `File:` value only when the paired `Status:` line equals exactly `SUCCESS` — a naive line-by-line grep for "SUCCESS" without correlating it to the same block's `File:` line is the common mistake; (3) for each candidate file, `File::open` it in `source_dir`, `memmap2::Mmap::map` it, and compare the first 8 bytes against `expected_magic` byte-for-byte; (4) only when both the log status and the magic-byte check pass, create `archive_dir` (if missing) and use `std::fs::hard_link` (not copy) into it under the same filename; (5) collect the archived filenames, sort them alphabetically, and write one per line to `/home/user/archive_report.txt`. Run the compiled binary once so the final filesystem state (hardlinks + report) exists on disk.
