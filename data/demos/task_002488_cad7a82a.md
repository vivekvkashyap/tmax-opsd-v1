Write the validator as a straightforward C++ program: open the file with `std::ifstream`, use `std::getline` in a loop, skip the first line, then for each subsequent line split on commas (a simple manual split is fine since the fixture has no quoted/escaped commas) and grab the field at the given index. Validate the field with a tight character-by-character check: reject empty strings, allow at most one leading `-`, and require every remaining character to be a digit — any `.`, letter, or stray character (including a lone `-` with no digits after it) makes it invalid. Track the current line number starting at 1 for the header and increment per line, printing the 1-based line number of any row whose field fails validation. Compile with `g++ -O3 -o /home/user/validate_int /home/user/validate_int.cpp`, then run it against `/home/user/dataset.csv` with column index 2, redirecting stdout to `/home/user/invalid_rows.txt`. Before trusting the output, manually eyeball each row's target column value in the CSV and confirm your line-number list matches your own hand analysis — a common bug is off-by-one on the header line or column indexing starting at 1 instead of 0.
