Work top-down, verifying each stage before consuming it in the next. Combine the split archive parts in order (`cat legacy_docs.tar.gz.001 ... > combined.tar.gz`) and extract, then unzip the nested `internal_files.zip` into `/home/user/extracted_docs/`. For encoding, use `file -i` (or `chardet`) per `.txt` file, then `iconv -f <detected> -t UTF-8` — don't assume one blanket encoding, the files differ. Apply `[[TITLE:...]]`→`<h1>...</h1>` and `[[BOLD:...]]`→`<strong>...</strong>` with `perl -pe` (non-greedy) or careful `sed -E`, since plain `sed` regex is greedy across `]]`. Transcribe `/app/writer_notes.wav` with any available tool (ffmpeg + speech-to-text, or `whisper`) to recover the actual auth token, port number, and sign-off phrase — these must come from the transcription, not be guessed. Append the recovered phrase to every normalized file. Finally, implement the HTTP server in bash with `socat`/`nc` in a loop: parse the request line and `Authorization: Bearer` header, check it against the recovered token, and serve file contents from `/home/user/extracted_docs/` on the recovered port; run it backgrounded and leave it running.