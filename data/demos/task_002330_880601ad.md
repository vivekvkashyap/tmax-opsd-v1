Start `/app/start_services.sh` first and confirm Postgres, Redis, and Flask are actually reachable before touching detector.py. The core SQL bug to find and fix is an implicit cross join inside the recursive CTE — look for a `FROM edges, current_path` (or similar comma-join) pattern and replace it with an explicit `JOIN edges ON current_path.target = edges.source` (plus a cycle-back-to-origin condition and a depth limit up to 4) so the recursion only follows real graph edges instead of the full cartesian product. For the 7-day moving average, use a proper SQL window function (`AVG(amount) OVER (PARTITION BY sender ORDER BY tx_date RANGE BETWEEN INTERVAL '7 days' PRECEDING AND CURRENT ROW)` or equivalent) rather than a Python-side loop. For the risk-score lookup, before calling the Flask endpoint check Redis for a cached score keyed by account_id, and on a miss call the API then `SET` the result in Redis so repeated lookups don't hit Flask again. Wire the three criteria together with OR logic (reject if any one is true), and validate your fix against the provided `clean`/`evil` sample CSVs — clean files must pass through completely unchanged, evil files must end up with zero rows in the output — before trusting the held-out corpora.
