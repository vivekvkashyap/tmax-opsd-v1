Treat this as a black-box reverse-engineering problem first, then an optimization problem second — don't guess at the algorithm. Start with static analysis: run `strings /app/legacy_token_validator` to look for hardcoded secret material, and `file`/`objdump -d`/`nm` to see if it's dynamically linked against a crypto library (e.g. OpenSSL) which hints at the hash/MAC construction and digest length used. Cross-check with dynamic/differential testing: call the validator with the same `device_id_hex` and `timestamp_int` but a token you control, brute-forcing small known inputs isn't feasible for a keyed MAC, so instead reason about digest truncation — count the hex characters accepted in a valid token to infer how many bytes of the underlying digest are used, and infer the message construction (is it concatenation of the two arguments, with what encoding/order?) by testing how changing each argument changes acceptance. Once you've nailed down the algorithm, key, message layout, and truncation length, implement it in C++ using a fast crypto library (e.g. OpenSSL's HMAC/EVP API, not a naive from-scratch implementation) and write directly to file with buffered I/O in a single pass over `requests.csv`, avoiding any per-line subprocess calls or string-stream overhead, to hit the 500k-lines-under-0.5s throughput bar.