Load `/app/signal.wav` with `scipy.io.wavfile.read` or `soundfile`, apply `np.fft.rfft` over the full signal, compute the magnitude spectrum and corresponding frequency bins via `np.fft.rfftfreq(len(signal), 1/sample_rate)`, then find the three highest, well-separated peaks (e.g. using `scipy.signal.find_peaks` on the magnitude spectrum, sorted by peak height) and round each to the nearest integer Hz — a naive "top-3 magnitude bins" approach can pick multiple bins from the same spectral peak, so add a minimum-distance/prominence constraint between selected peaks. For the KDE, load `/home/user/samples.csv` and fit `scipy.stats.gaussian_kde(samples, bw_method='scott')` (Scott's rule is scipy's default but state it explicitly), then evaluate at exactly [10,20,...,90]. Build the HTTP service (Flask, http.server, or similar) bound to `127.0.0.1:8080` with `/audio_peaks` and `/mesh_density` GET handlers returning the JSON shapes specified, and start it in the background before finishing — test with `curl` yourself. Write test_api.py to hit both endpoints and assert status 200 and key presence (it doesn't need to be run by you, just valid). Write plot_data.py as a straightforward two-panel matplotlib figure (waveform on top, KDE curve x=0..100 on bottom), remembering to use a non-interactive backend since this is headless.