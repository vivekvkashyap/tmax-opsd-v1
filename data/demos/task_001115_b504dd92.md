Treat this as forensics-then-repair. For the key: don't just grep the working tree — walk full git history (`git log -p -- parser.c`, or `git log --all -S<pattern>`) since the key was committed once and later scrubbed; diffing an early commit against a later one reveals the removed line. Save only the bare key string, no extra text. For the hang: read the TLV-parsing loop by hand (or step through with a debugger) and find the chunk type/length combination whose branch never advances the read cursor — that's the infinite loop and leak source. Fix it so any degenerate or malformed chunk is skipped by advancing the cursor, or causes the loop to break, but never loops without progress. Rebuild via the Makefile, run the binary against the capture file passing the recovered key as an argument (check `-h`/`main()` for exact ordering), and redirect stdout to the output file. Confirm the run terminates and decodes every well-formed chunk.
