Do this in Python for reliability rather than trying to hand-roll it purely in jq/awk, since the malformed-line filtering needs precise regex control before JSON parsing is even attempted. Step 1: read the file line by line and, BEFORE calling any JSON parser, scan the raw text for `\u` occurrences and check that each is followed by exactly 4 hex digits (regex like `\\u(?![0-9a-fA-F]{4})` catches violations); discard the whole line if any bad escape is found — do this pre-parse since a strict JSON parser will simply throw/crash rather than let you inspect the bad escape. Step 2: for surviving lines, parse JSON, take `timestamp`, truncate/format to `YYYY-MM-DDTHH` and tally counts per bucket across the full observed hour range (0-23), including hours with zero valid logs so the rolling window is complete. Step 3: for each hour with logs, compute the rolling average from the counts of the strictly preceding 1-3 hours (available ones only, 0.0 if none), rounding to 1 decimal. Step 4: flag anomalies where `count > 2.0 * rolling_avg` AND `count >= 10`. Step 5: write matching rows to `/home/user/anomalies.csv` with header `hour,count,rolling_avg`, verifying exact string match against expectations by re-reading your own output.