First recover the matrix from the video: extract per-second frames from `/app/matrix.mp4` (`ffmpeg -i matrix.mp4 -vf fps=1 frame_%02d.png`, or use `ffmpeg`'s `signalstats`/`blackframe` filters) and compute each frame's average grayscale intensity (0-255) — since frames are uniform solid color, any pixel or the mean over the frame gives the same value. Map the 16 intensities in order into a 4x4 row-major matrix M (rows/cols = A,C,G,T), dividing each entry by 255.0. In the C program, count A/C/G/T occurrences in argv[1] into vector b, then form the 4x4 normal-equations matrix `A = M^T M + 0.05*I` and right-hand side `c = M^T b`, and solve `A x = c` with straightforward Gaussian elimination with partial pivoting (a 4x4 system, no external linear-algebra library needed). Print exactly `x[0] x[1] x[2] x[3]` space-separated with `%.6f` each, newline-terminated, and nothing else to stdout. Since correctness is graded by matching behavior across many random ACGT sequences of varying length, test your compiled binary on several different sequences (including edge cases like all-same-nucleotide and length 10 vs 50) and confirm the output format never deviates.