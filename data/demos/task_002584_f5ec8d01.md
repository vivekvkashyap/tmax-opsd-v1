Fix the build first: open `/app/vendor/miniscript/Makefile`, correct the misspelled `-Wno-unnused` flag to `-Wno-unused` (or drop it), and append `-lm` to the linker line since the code calls math functions like `pow`; run `make` and confirm `libminiscript.so` exists before writing any Python. For the benchmark, load the library with `ctypes.CDLL`, declare `evaluate_rule.argtypes`/`restype` explicitly (char pointers, int) to avoid marshalling bugs, time a loop of 100,000 calls with `time.perf_counter()`, and write the elapsed seconds as a bare float to `/app/bench_result.txt`. For the server, run both listeners in one process: use `threading` to start an `http.server`/`socketserver` HTTP handler on 8080 checking the exact `Authorization: Bearer sec-token-999` header (401 on mismatch) and a separate blocking TCP socket server on 8081 in another thread that reads newline-terminated `RULE|PAYLOAD` lines, splits on the first `|`, calls `evaluate_rule` via ctypes, and writes back `<result>\n`. Launch `server.py` with `nohup ... &` or as a background process so it stays alive after your session ends, then verify both ports respond with `curl`/`nc` before finishing.
