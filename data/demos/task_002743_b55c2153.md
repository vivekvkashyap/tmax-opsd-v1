Scaffold the Rust project with `cargo new citation_query`, add `oxigraph` and `serde_json` (plus `oxigraph`'s SPARQL support) to `Cargo.toml`. In `main.rs`, load the store by parsing `/home/user/research_data.ttl` as Turtle into an in-memory `oxigraph::store::Store` (bulk-load the RDF via the store's Turtle loader with the right base IRI/prefixes so `ex:`, `dc:`, `foaf:` resolve).

Write one SPARQL query that does the heavy lifting rather than hand-rolling the joins in Rust: select articles with `dc:date > 2018`, count incoming `ex:cites` edges per article via a subquery or `COUNT(DISTINCT ?citer)` with a `GROUP BY`, and separately aggregate author names — since SPARQL's `GROUP_CONCAT` doesn't sort inputs, either fetch authors per article as a separate query/loop and sort them in Rust before joining with `", "`, or use `GROUP_CONCAT(?name; separator=", ")` over an ORDER BY subquery (behavior varies by engine, so verify oxigraph actually respects ordering inside GROUP_CONCAT — if unsure, do the author sort in Rust code instead of relying on SPARQL). After getting counts, filter out zero-citation articles, sort by citations descending then title ascending as a tiebreak, take the top 3, and serialize with `serde_json` matching the field names and order exactly (`title`, `year`, `authors`, `citations`). Run the binary and diff the output against your own manual trace of the citation counts to sanity-check before finishing.