Fix the two components independently, then wire them together and test end-to-end with curl. For nginx.conf, the bug is the proxy target: change `proxy_pass` under `location /api/predict` (or `/api/` with a rewrite) to point at `http://127.0.0.1:8080/predict` so the path and port both match the Rust service, not the data server on 8000. For main.rs, the bug is in the sufficient-statistics accumulation: it must sum `x*x/noise_var` (not `x/noise_var`) for the posterior precision term — re-derive the formulas from the stated Bayesian linear regression equations (`V_n^-1 = V_0^-1 + sum(x_i^2/sigma^2)`, `w_n = V_n * sum(x_i*y_i/sigma^2)`, predictive variance `x_*^2 * V_n + sigma^2`) and check your fix against a hand computation using the four (x,y) pairs served by data_server.py before trusting the running service. Bring the stack up in dependency order: python data server first, then `cargo run` for the Rust service (it fetches data.csv on startup), then nginx last (`nginx -c /app/nginx.conf`), each backgrounded. Finally confirm with `curl 'http://127.0.0.1:80/api/predict?x=3.0'` and sanity-check the returned mean/variance against your own recomputed values, not just that JSON comes back.