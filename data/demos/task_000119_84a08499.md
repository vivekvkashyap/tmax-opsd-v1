Fix the three components in dependency order: Makefile, then C source, then the wrapper script, testing after each. For the Makefile, replace the broken rule with one that actually links and names the output binary, e.g. `gcc -Wall manifest_parser.c -o manifest_parser`, and confirm `make` produces a `manifest_parser` executable with no warnings suppressed. For the C fix, change the `value` buffer to `char value[128]` and constrain whatever scan/copy mechanism reads into it so it can never write more than 127 chars plus a null terminator — e.g. if using `fscanf`, use a width-limited specifier like `%127s`/`%127[^\n]` instead of an unbounded `%s`; if using `strcpy`/`sscanf` elsewhere, switch to `strncpy`/bounded equivalents. Verify manually with a very long crafted `value` field that the program no longer segfaults and truncates/handles it safely per spec, then re-run against the real `payload.dat` to confirm the malicious injected string round-trips correctly since it's within the new 128-byte limit. Finally write `process.sh` to run `make`, execute `./manifest_parser payload.dat`, redirect stdout exactly to `/home/user/pipeline/output.json`, exit 0, and `chmod +x` the script — run it yourself and diff the produced JSON lines against the expected key-value structure before finishing.