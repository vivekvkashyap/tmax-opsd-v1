Avoid symlink-loop infinite recursion by tracking visited real directories: use `os.walk(root, followlinks=True)` but before descending into any symlinked directory, resolve it with `os.path.realpath()` and skip it if you've already visited that real path (keep a `set()` of realpaths seen so far) — this is the key trick naive recursive walkers miss, causing `RecursionError` or hangs. As you walk, for every entry ending in `.blob` whose `os.path.getsize()` is strictly greater than 100000, open it in `'rb'` mode, `f.read(32)` (exactly 32 bytes, not the whole file), and convert with `.hex()` to get a lowercase hex string. Collect `(abs_path, hex_string)` pairs, sort them alphabetically by absolute path with `sorted()`, and write each as `f"{path}: {hexstr}\n"` to `/home/user/curated_index.txt` — match the exact `path: hex` format and ordering, since the verifier does an exact-content (MD5) comparison, so trailing newlines, extra spacing, or wrong sort order will fail it. Do not modify or delete the symlinks themselves. Run the script and inspect the output file manually before considering it done.