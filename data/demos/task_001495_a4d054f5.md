Work through the four bugs systematically: use `git log -p main.go` to find the commit that removed the hardcoded secret token, and export it as `SECRET_TOKEN` before running. Fix the SQL query with `WHERE active = 1` so inactive rows are excluded. Fix the concurrent map write by protecting the shared `state` map with a `sync.Mutex` around each write in the goroutine, rather than removing concurrency. For convergence, note the loop currently sets `converged = false` for any non-empty state regardless of whether it actually matches the desired state — replace that with a real comparison (or recognize convergence once the query/write cycle completes without change). Build, run with the recovered token, confirm converged_state.json contains only active=1 keys, and write the token to the required file.