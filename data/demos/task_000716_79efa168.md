Follow the four steps in exact order since each depends on the previous, and be meticulous about when each `numpy.random.seed(42)` call happens (it must be called immediately before the corresponding random draw, not once globally). Step 1: read the 1000 observations, compute the sample mean, then seed, and generate exactly 10,000 bootstrap resamples via `numpy.random.choice(data, size=len(data), replace=True)` in a loop, taking the mean of each, then use `numpy.percentile` at the 2.5 and 97.5 percentiles for the CI bounds. Step 2: invert the analytical exponential-decay solution `C(t)=C0*exp(-k*t)` to solve for `k` given `C(0)=100` and `C(5)=` your bootstrap sample mean (not the CI bounds). Step 3: reseed(42), draw 100,000 `numpy.random.uniform(90,110)` initial concentrations, apply the same analytical formula with your fitted k, and average. Step 4: implement Forward Euler (`C += -k*C*dt` iterated) for dt=0.5 and dt=0.01 separately from t=0 to 5, and take the absolute difference of the two final values. Round every output field to exactly 4 decimals and write the JSON with the specified keys.
