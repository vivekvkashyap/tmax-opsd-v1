Work through the four phases in order and validate each before moving on. First, fix `/app/vendored/mongoose/Makefile`: add a `static` target that compiles the mongoose sources to object files and archives them with `ar rcs libmongoose.a mongoose.o` (don't just tweak the existing `-shared` rule — add a genuinely separate static-archive target so `make static` produces a `.a` file). Second, write the C++ WebSocket firewall using the mongoose event-handler pattern: on a WS message event, check the payload buffer for the exact substrings `<script>` and `DROP TABLE` (simple substring search is sufficient, no need for regex) and send back `REJECT` or `ACK` accordingly via the mongoose send function, listening on port 8080. Third, compile with static linking flags (e.g. `g++ -static ws_firewall.cpp libmongoose.a -o /home/user/ws_firewall_bin`) and confirm with `file` that it reports "statically linked" — if `-static` fails due to missing static system libs, install the needed static libc/libstdc++ packages rather than falling back to dynamic linking. Fourth, start the binary in the background (`&`), confirm it's listening on 8080 (e.g. `ss -tlnp`), then run `/app/bench_tool.sh > /home/user/bench_results.txt`, and leave the server running for the adversarial corpus checks.