Treat this as a "read without blocking writers" problem, not a normal transactional query. Open a psycopg2 connection and set the isolation level to something that avoids row/table locks against concurrent writers (e.g. `ISOLATION_LEVEL_REPEATABLE_READ`/serializable snapshot or `SET TRANSACTION ISOLATION LEVEL ...`), and keep each transaction short-lived — never hold one open across the whole traversal. Implement the graph walk as BFS from `--start-node` up to `--max-depth` hops using `edges(source_id, target_id, weight)`, but fetch edges for each frontier level in batches of 100 rows (use `LIMIT/OFFSET` or a keyset/server-side cursor per batch) instead of pulling the whole table — this is what prevents the deadlocks the naive script has. Join to `nodes` to drop any node with `type = 'transient'`. Track each node's shortest hop-distance from the start (first time it's discovered in BFS), then sort final results by (distance, id). Push the ordered ID list as JSON to Redis under `backup:path:<start-node>` and print the same JSON to stdout — since correctness is checked by exact stdout match against a hidden reference, get tie-breaking and JSON formatting (no extra whitespace/keys) exactly right.
