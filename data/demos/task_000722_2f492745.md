Recover the banned project code first: transcribe `/app/intercepted_comms.wav` with a CLI STT tool (install whisper.cpp or similar) and read the transcript carefully to get the exact code string, since your later check must match it precisely. Then build the validator script defensively, checking things in a safe order: (1) list the tar's contents with `tar -tvf` or `tar -tf` before extracting, and reject if any entry is an absolute path, contains a `../` traversal component, or is a symlink whose target escapes the extraction root — do this scan before extracting into a fresh temp directory (`mktemp -d`) that you clean up afterward regardless of outcome; (2) after safely extracting, read `manifest.sha256` and for every listed file recompute `sha256sum` and compare against the manifest entry, rejecting on any missing file or mismatch; (3) extract `metadata.txt` and convert it from Shift_JIS to UTF-8 with `iconv -f SHIFT_JIS -t UTF-8` (handle conversion failures as reject-worthy too), then grep the converted text case-sensitively for the recovered project code. Exit 0 only if every stage passes; exit 1 the moment any stage fails. Test against both corpora, paying attention to filenames with spaces.
