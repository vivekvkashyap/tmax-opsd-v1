Split this into service-repair and deduplication phases and validate each with a direct check. For the services, don't assume the fix — read `/app/nginx/nginx.conf`'s upstream port against Flask's actual bind port and correct the `proxy_pass` target, and read the Flask config/`.env` to see it's using a unix-socket-style Redis URL when Redis is actually listening on TCP, then change it to `redis://127.0.0.1:6379/0`; after restarting via `/app/start.sh`, confirm yourself with `curl http://127.0.0.1:8080/health` before moving on. For deduplication, write `/home/user/curate.py` to walk `/app/data/artifacts/` recursively, compute SHA-256 per file by reading in fixed-size chunks (avoid loading huge files fully into memory), group files by hash, and for each duplicate group pick the file with the oldest mtime as master, then replace every other duplicate with `os.link(master, path)` after removing the original (hardlinks share the same inode/blocks, which is what makes `du` report reduced usage). Also write `/app/data/backup_index.json` mapping every relative file path (including the master files) to its hash. After running the script, check `du -sk /app/data/artifacts/` yourself to confirm the disk usage actually dropped before declaring done, and leave the services running.
