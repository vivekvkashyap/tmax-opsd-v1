Structure the Go program as clearly separated stages so each is independently testable. First, OCR the calibration image with `tesseract /app/calibration.png stdout` and parse `OFFSET:`, `SCALE:`, `WINDOW:` lines with a regex — OCR can misread digits/punctuation, so print the raw OCR text while developing and sanity-check the parsed numbers look reasonable before trusting them. Second, timestamp parsing needs three separate code paths tried in order: (a) if the field is all-digits, treat as Unix epoch seconds; (b) try `time.RFC3339` / `time.Parse` with layout `"2006-01-02T15:04:05Z"`; (c) try the custom layout matching `"02 Jan 06 15:04 MST"` (Go's reference-time format). After parsing, convert everything to UTC and sort all records by time before computing anything — do the timestamp normalization pass fully before the rolling-window pass, since window order depends on it. Apply `normalized_value = (raw_value + OFFSET) * SCALE` per row, then compute the SMA over the trailing WINDOW records inclusive of the current one (using min(available, WINDOW) elements for the first WINDOW-1 rows, not zero-padding). Write `time,smoothed_value` with the value formatted to exactly 4 decimals, and verify a few of your own computed rows by hand against the formula before considering it done — the grader checks MSE against a hidden reference so small windowing or rounding mistakes will fail it.
