Work bottom-up: get each service healthy in isolation before wiring them together. Start `redis-server` and confirm with `redis-cli ping`. Read `/app/service/app.py` line by line and diff its behavior against the spec — look for a wrong Redis port/host, a missing `/tmp/builds` directory creation (`os.makedirs(..., exist_ok=True)`), and a wrong bind address/port; fix each bug individually and restart Flask, testing directly against `127.0.0.1:5000` before touching Nginx. Write the Nginx site file with a `server { listen 8080; location / { proxy_pass http://127.0.0.1:5000; } }` block, symlink/place it under sites-enabled, then reload and curl `127.0.0.1:8080` to confirm the proxy chain. For the WAF, don't just grep for banned words as substrings of arbitrary text — parse the C source with word-boundary-aware matching (regex `\b(system|popen|execve|fork)\s*\(`) so legitimate identifiers containing those substrings aren't false-flagged, and test against both corpora before finalizing. Build `ci_cd.sh` to loop `for f in "$dir"/*.json`, invoke the WAF, check `$?`, and only `curl -X POST -H 'Content-Type: application/json' --data @"$f"` on success, appending status+body to the log.
