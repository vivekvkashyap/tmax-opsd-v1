Start by OCR'ing `/app/evidence/ransom_note.png` with `tesseract` (preprocess with `convert -resize`/threshold if text is unclear) to recover the `MASTER_KEY` hex string and the `SALT` string — never hardcode a guessed key/salt, extract them from the image. Design `/home/user/payload_decoder` to read all of stdin as one raw HTTP request, split headers from body on a blank line, and parse the `Cookie:` header for a `Session-ID=...` value and the `X-Ransom-Token:` header separately, plus the body as the hex ciphertext — if any of the three is missing, print exactly `MALFORMED_REQUEST` and stop. Compute `expected_token = md5(session_id + SALT).hexdigest()` and compare case-insensitively to the received token; on mismatch print exactly `INVALID_TOKEN`. For decryption, derive the AES-128 key by hex-decoding `MASTER_KEY` to 16 raw bytes, derive the IV as `md5(session_id).digest()` (16 raw bytes, not hex), hex-decode the body to ciphertext, run AES-128-CBC decrypt, strip PKCS#7 padding, and print the plaintext with no trailing newline. Handle decrypt/unpad exceptions gracefully. Since grading is bit-exact fuzz comparison against a hidden reference, match whitespace/newline handling precisely and test with several hand-crafted requests covering each of the three outcome branches before considering it done.
