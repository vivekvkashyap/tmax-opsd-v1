OCR the spec first: `tesseract /app/config_rules.png stdout` (try `--psm 6` if lines merge), and read it carefully for three distinct pieces — the exact regex pattern with named groups, the gap-fill cadence (per-second interpolation between start/end), and the exact output line format/delimiter. Don't hand-guess the regex from the task description; the image is the source of truth and small details (bracket placement, literal "TO", field order) matter for bit-exact matching against a hidden oracle. In `process_line.py`, use `re.match` (anchored) against the whole stripped input line; on no match print exactly `INVALID_FORMAT` and exit 0. On match, parse the timestamps with `datetime.strptime` in the matched format, validate end >= start (reject with INVALID_FORMAT if malformed or end < start), then loop from start to end inclusive incrementing by `timedelta(seconds=1)`, emitting one line per second in the exact format from the image. Watch for off-by-one at the boundary (inclusive end) and exact field ordering/separators — these are the two places bit-exact fuzzing will catch you. Also write the trivial `run_pipeline.sh` wrapper that streams a file line-by-line into your Python script and appends to `/home/user/pipeline.log`.