Diagnose the build failure first: run `cmake -S . -B build && cmake --build build` and read the linker error — it's a missing `target_link_libraries(envelope_cli envelope)` (or similarly named) directive in `CMakeLists.txt` tying the CLI target to the shared library target. Separately, because the project builds with `-fvisibility=hidden`, `compute_envelope` in `src/envelope.c` needs an explicit `__attribute__((visibility("default")))` (or a matching export macro) on its declaration/definition or the symbol won't be visible to the linker/dynamic loader even after linking is fixed. After a clean build in `build/`, fix `api/cgi-bin/process.sh` to reference the actual compiled binary path under `build/` (not a bare `envelope_cli` relying on PATH) and export `LD_LIBRARY_PATH` pointing at the directory containing `libenvelope.so` before invoking it, since CGI scripts run in a minimal environment. Start `api/server.sh` in the background, poll until it accepts connections, then in `run_test.sh` `curl -s --data-binary @/app/test_audio.wav http://localhost:8080/cgi-bin/process.sh` and save stdout to `/home/user/output.json`, then kill the server. Validate the JSON is a flat float array (`python3 -c "import json;json.load(open(...))"`) before finishing.
