Do the index work directly in `sqlite3 /home/user/backups.db`: `DROP INDEX idx_edges_bad;` then `CREATE INDEX idx_edges_cov ON edges(source, target, weight);` — column order matters for a covering index, put it exactly as specified. For `process_neighbors` in `recovery.c`, fill in the TODO using the parameterized-query pattern already sketched in the comments: `sqlite3_prepare_v2(db, sql, -1, &stmt, NULL)`, then `sqlite3_bind_text(stmt, 1, current_node, -1, SQLITE_TRANSIENT)`, loop `while (sqlite3_step(stmt) == SQLITE_ROW)` pulling `target`/`weight` via `sqlite3_column_text`/`sqlite3_column_int`, updating `states[target_idx].dist`/`.prev` when you find a shorter path (standard Dijkstra relaxation), and call `sqlite3_finalize(stmt)` when done — never build the SQL string with sprintf/concatenation, that's the injection vector being tested. Compile with `gcc /home/user/recovery.c -lsqlite3 -o /home/user/recovery`, run it with `Start Recovery` as arguments, and redirect its single-line `->`-joined path output to `/home/user/recovery_path.txt`. Trace the edge weights by hand to sanity-check which path is actually shortest before trusting the program's output.
