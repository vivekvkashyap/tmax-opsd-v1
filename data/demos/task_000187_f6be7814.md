Part 1: extract frames from `/app/dashboard_record.mp4` (e.g. `ffmpeg -i ... -vf fps=2 frames/%03d.png` at a few fps, or scrub with OpenCV) and read the on-screen terminal text to locate the exact "FATAL: Shell parser exception at <timestamp>" moment — use OCR (`tesseract`) or visually inspect extracted frames rather than guessing; that recovered timestamp is what you match against the recovered log later. For the deleted file, mount `/app/log_volume.img` read-only (`mount -o loop,ro`) and if the file isn't visible via normal listing, use a raw-recovery approach appropriate to ext4 (`extundelete`, `debugfs` with `lsdel`/`catfile`, or scanning unallocated blocks with `strings`/`grep` on the raw image for log-line patterns) to reconstruct `/home/user/recovered_system.log`. Grep the recovered log for the line whose timestamp matches what you read from the video, and write that line's file-path payload verbatim to `/home/user/crash_cause.txt`. Part 2: write `sanitizer.cpp` that reads one line from stdin and rejects (non-zero exit) on any shell metacharacter, unescaped space, or control character (check byte-by-byte against an explicit denylist/allowlist rather than trying to regex-match "looks safe"), accepting only `[A-Za-z0-9._-]+`; compile with g++ and validate against your own constructed edge cases (embedded `$()`, newlines, semicolons) before considering it done.
