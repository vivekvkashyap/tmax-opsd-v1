Fix the vendored dependency completely before writing any pipeline code against it, since the ETL depends on it compiling correctly. In `/app/go-nlp-utils`, run `go build ./...` and `go vet ./...` first to surface all issues rather than fixing one and re-running blindly: check `go.mod`'s `module` line matches what your ETL's `go.mod` `replace` directive expects (a one-letter typo in the module path is easy to miss — read both `go.mod` files side by side), check `tokenizer.go` for use of an identifier (e.g. from an unused/missing import) without the corresponding `import` line, and run `go get <missing-package>` plus `go mod tidy` to pull in any transitive dependency the code references but `go.mod` doesn't declare. Confirm with `go test ./...` passing before moving on. In `/home/user/etl.go`, set up `go.mod` with `replace github.com/dataeng/go-nlp-utils => /app/go-nlp-utils` matching the corrected module name exactly. For the worker pool, preserve input order by tagging each row with its original index/id, dispatching to workers via a channel, collecting results into a slice sized by row count (or a map keyed by id), and writing them out only after sorting/indexing by `id` — never write directly from worker goroutines, since that races on output order. Implement forward-fill and CSV quoting using Go's `encoding/csv` writer (which handles quoting automatically) rather than hand-formatting strings.