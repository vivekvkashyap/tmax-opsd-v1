Fix the three layers of this C project in order, verifying each with a build/run before moving on. First, `make` in `/home/user/build_telemetry` and read the linker error — it's almost certainly a missing `-lcurl` on the final link line in the Makefile's `build_reporter` target; add it after the object files. Second, once it links, run it and it will segfault in `cmap.c`'s insert function — inspect how the key string is stored on the node: if the code does `strcpy(node->key, key)` without first allocating memory for `node->key` (e.g. via `malloc(strlen(key)+1)` or `strdup(key)`), that's writing through an uninitialized/garbage pointer, so allocate before copying. Rebuild and rerun to confirm no more crash. Third, before running `./build_reporter`, start the telemetry server in the background (`python3 server.py &`) and give it a moment to bind its port; then run `./build_reporter` and check `/home/user/build_telemetry/telemetry_success.json` was written with the expected metric keys — if it's missing, check the server log/stdout for POST errors (wrong port, malformed JSON) rather than re-guessing at the C code blindly.