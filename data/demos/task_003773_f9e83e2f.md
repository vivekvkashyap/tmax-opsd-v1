Fix the vendored server first: grep `/app/bashttpd/bashttpd` for the variable that's supposed to hold the HTTP status line — a single-character typo in a variable name (assignment vs. later reference not matching) is a classic subtle bug; find the mismatched names and make them consistent so the response line is actually emitted. Then do the file/config steps in order: tar up `config_template/` into the backup archive before modifying anything in it, copy `bashttpd.conf` to `/home/user/`, and edit the copy (not the template) to point at `/home/user/www/`. For TLS, generate the self-signed cert/key pair with `openssl req -x509 -newkey rsa:2048 -keyout key.pem -out cert.pem -days 365 -passout pass:HardenedPass123`, confirming the key is actually password-protected (encrypted PEM header) not plaintext. Write the Expect script to launch `bashttpd` listening on 8080 (e.g. via a simple loop with `socat`/`nc`, or however the server is normally driven) and a TLS-terminating proxy on 8443 forwarding to it, using `expect`/`send` to answer the private-key passphrase prompt automatically rather than hardcoding the password as a CLI arg. Test with `curl -k https://127.0.0.1:8443/index.html` yourself and confirm it returns the real file content before finishing, and make sure the proxy process is left running.
