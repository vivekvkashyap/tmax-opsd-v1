Write a script (Python or bash) that parses `/home/user/micro_fstab` line by line into (source_dir, mount_symlink, process_name) triples. To check if a named process is running, use something like `pgrep -x <process_name>` (or scan `/proc/*/comm`/`/proc/*/cmdline`) rather than `ps aux | grep`, which can false-positive-match its own grep invocation or substrings of other process names — use exact-name matching. For each entry: if the process is running, `mkdir -p` the source directory, then remove any existing file/symlink at the mount point and recreate it as a fresh symlink to the source dir (use `os.symlink` after `os.path.lexists` check, or `ln -sfn`); if not running, remove the mount point if it exists (including stale symlinks pointing nowhere) via `os.remove`/`os.unlink` guarded by a lexists check, without touching the source directory's existence either way. Accumulate a dict of process_name -> bool(is_running) and dump it as JSON to `/home/user/service_report.json`. Save the script to `/home/user/sync_mounts` (or `.py`/`.sh` variant) and actually execute it so the filesystem state matches before finishing.