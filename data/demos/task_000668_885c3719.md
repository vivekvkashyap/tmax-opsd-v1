Fix the Makefile first: a shared object needs `gcc -fPIC -shared -o libencoder.so encoder.c` (compile object files with `-fPIC`, then link with `-shared`, not the default executable link rule). Drop any rule expecting a `main` symbol. Verify with `file libencoder.so` and `nm -D libencoder.so | grep process_data`. For the Python side, load the library with `ctypes.CDLL("/home/user/artifact_encoder/libencoder.so")`, set `.argtypes`/`.restype` on `process_data` explicitly (e.g. `POINTER(c_ubyte)`/`c_char_p`, `c_int`, `POINTER(c_ubyte)`), and allocate the output buffer with `ctypes.create_string_buffer(length)` sized to the input length — under-sizing it is the classic ctypes bug here. Read each raw artifact in binary mode, call the function, slice the returned buffer to the byte count the function reports, and write it with the `.enc` suffix. Accumulate total input bytes across files as you go and write the exact log line format requested at the end. Iterate the raw_artifacts directory rather than hardcoding filenames so it generalizes.
