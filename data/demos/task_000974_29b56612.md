Recover the exact detection rule by listening to (or transcribing) `/app/security_memo.wav` rather than guessing what makes a file "compromised" — the memo specifies precise, checkable conditions (a checksum-based rule and a content-based rule); note both conditions exactly as stated, since a partial or approximate match will fail the adversarial corpus test. In `/home/user/filter.sh`, accept `$1` as the target directory, `mkdir -p "$1/.quarantine"`, then iterate only regular files directly in `$1` (e.g. `find "$1" -maxdepth 1 -type f`, excluding files already in `.quarantine`). For each file, compute its SHA256 with `sha256sum` and check the prefix condition from the memo, and separately check whether `head -n 1` of the file matches the exact signature string from the memo — if either condition holds, `mv` it into `.quarantine`; otherwise leave it untouched. Test against a few files you construct yourself (one matching the hash-prefix rule, one matching the signature-line rule, one clean) to confirm both detection paths work and that clean files are never moved, before assuming it will pass the full evil/clean corpus.
