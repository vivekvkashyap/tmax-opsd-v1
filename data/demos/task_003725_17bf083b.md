Connect to the SQLite DB with the stdlib `sqlite3` module and execute the pipeline as explicit SQL statements in order: `DROP INDEX IF EXISTS idx_telemetry_node;` then `CREATE INDEX idx_telemetry_optimal ON telemetry(node_id, ts, metric);`, and commit. For the extraction, join `telemetry` to `nodes` on `node_id` and filter `nodes.status = 'ACTIVE' AND nodes.region = 'us-east'` — select telemetry's `id` (map to `telemetry_id`), `node_id`, `ts` (map to `timestamp`), and `metric` (map to `metric_value`), keeping the DB's natural row order rather than re-sorting. Build each row as a dict matching the required schema exactly (correct key names and types — cast id/node_id to int, metric to float, ts stays a string), validate types before appending, then `json.dump(records, f, indent=2)` to `/home/user/clean_backup.json`. Pitfall: forgetting the index rename means dropping the old index without creating the new covering index (or creating it with wrong column order) — verify both with a query against `sqlite_master` after your script runs, and double check the JSON key names/order match the task's schema exactly since it will be compared structurally.
