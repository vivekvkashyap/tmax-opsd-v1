Fix the build bottom-up: C library first, then Rust FFI boundary, then run the pipeline, then write tests. For the Makefile, add `-fPIC` to the compile step and use `-shared` (not plain `gcc -o`) when linking the `.o` into `libintensity.so` — a missing `-fPIC` or missing `-shared` is why it currently fails to produce a valid shared object. For `src/constants.rs`, detect its actual encoding (e.g. `file src/constants.rs`) and re-save as UTF-8 (e.g. `iconv -f ISO-8859-1 -t UTF-8`) without altering its logical content. For the FFI mismatch, compare the Rust `extern "C"` declaration's parameter types against the actual C signature in intensity.c and fix any type mismatch (e.g. C `int` length must be `i32` in Rust, not `f64` — ABI mismatches cause wrong results even if it compiles). After `cargo build --release`, extract frames with ffmpeg at exactly 5fps, scale=320:240, single 8-bit grayscale raw output, feed the frames directory to the binary, and sanity-check `output_intensities.json` before finishing. Finally write the hypothesis property test asserting `calculate_intensity` returns a float in [0.0, 255.0] for any 320x240-sized bytes input.