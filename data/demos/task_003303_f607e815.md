Implement the C parser and RLE encoder carefully, then layer the filesystem operations on top with plain shell. In `process.c`, read `/home/user/raw_sequences.txt` line by line, split each on the first `-` and `:` to get YEAR, REGION_ID, and SEQUENCE (don't assume fixed-width fields — use `strchr`/`sscanf` robustly). RLE-encode SEQUENCE by scanning for runs of identical consecutive characters, emitting `<count><char>` for each run (a run of 1 still emits `1<char>`, not just the character). For output routing, before writing, ensure `/home/user/processed/` exists (`mkdir -p` from your program or beforehand), then open `year_<YEAR>.rle` in append mode (`"a"`) so multiple lines for the same year accumulate correctly across separate lines in the input, writing each RLE result followed by `\n`. After compiling (`gcc -o /home/user/process /home/user/process.c`) and running it once to generate all `.rle` files, use shell (`mkdir -p /home/user/backup && for f in /home/user/processed/*.rle; do ln "$f" /home/user/backup/$(basename "$f"); done`) to create true hard links (verified later by matching inode numbers via `stat -c %i`), and use `ln -s` to point `/home/user/latest_year.sym` at the `.rle` file whose YEAR is numerically largest among those generated — determine that year from the actual filenames you produced, not by assumption.