Break this into three separable pieces of C code so each can be tested independently: (1) a URL parser that scans the query-string portion after `?`, splits on `&`, and finds the key exactly matching `asset=`, extracting everything up to the next `&` or end-of-string — do this for both the requesting and target URL fields on each line, being careful that `asset` may appear anywhere among other query params, not necessarily first; (2) a line reader that tokenizes each row of access_graph.txt into requester, target, weight (use `sscanf` or manual whitespace splitting, `atof` for the weight); (3) an aggregation step that maintains a small dynamic (or fixed-size, given the stated max of 1000 assets) table mapping asset name -> summed incoming weight, incrementing the target asset's accumulator by the edge weight for every line, while also registering both requester and target asset names even if an asset only appears as a source (score defaults to 0.15 with no incoming sum). After parsing all lines, compute `0.15 + 0.85 * incoming_sum` per asset, sort the asset name array alphabetically (`qsort` with `strcmp`), and print each as `name,score` with `%.4f` formatting to scores.csv. Compile with `gcc -o scorer scorer.c` and run it with no arguments so it reads the fixed input/output paths.
