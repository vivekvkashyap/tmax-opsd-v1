Use `expect` to drive the interactive binary: `spawn /home/user/legacy_sensor_bin`, then `expect "Device Name? "` / `send "edge-node-01\r"`, `expect "Mode? "` / `send "telemetry\r"`, followed by `interact` (or an `expect eof`/infinite wait) so the process keeps running rather than the script exiting immediately after sending answers — a naive script that sends both replies and exits will kill the spawned child too. Route all spawned output to the log with `log_file -a /home/user/telemetry.log` (set before/after spawn) rather than shell redirection, since Expect's own redirection captures the pty output cleanly. For the watchdog, use `pgrep -f legacy_sensor_bin` to check liveness, and launch the expect script fully detached (`nohup ... &`, or `setsid`, with disown) so it survives the watchdog's own exit — a bare `&` inside a script that then exits its shell can still work but verify with `disown`/`nohup` to be safe. Write the logrotate stanza with `size 100`, `rotate 2`, `nocompress`, `create`, and test it manually with `logrotate -s <statefile> <conf>` exactly as instructed, then confirm the daemon is still alive and a fresh log file is accumulating new lines after rotation.
