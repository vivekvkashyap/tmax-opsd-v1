Separate feature extraction (do it once, upfront) from serving (read precomputed results at request time) rather than recomputing per-request. For frames: use `ffmpeg`/OpenCV to grab the frame nearest each integer second 0..9 (30fps video, so frame index = sec*30), reshape each to an Nx3 array of RGB pixels, compute the sample covariance with `np.cov(pixels.T, ddof=1)` (ddof=1 is critical — the population/ddof=0 default gives wrong numbers), flatten row-major, and round each entry to 2 decimals; cache these 10 matrices (e.g. in memory or a small JSON file) so both servers read from the same precomputed source instead of drifting. For tokens: lowercase each annotation line, strip everything outside `[a-z0-9 ]` with a regex substitution, split on whitespace, and drop empty tokens. For serving, write one Python script that spawns both a lightweight HTTP server (stdlib `http.server`/Flask on 127.0.0.1:8080 serving `GET /data?sec=X`) and a raw TCP socket server (127.0.0.1:8081, reading a newline-terminated integer per connection, replying with the 9 comma-joined floats, then closing) — run both as background processes/threads from `/home/user/start_services.sh`. Test both endpoints yourself with `curl` and `nc`/a small socket client for at least sec=3 and sec=7 before declaring done.