Work through the script top to bottom, fixing each bug in place rather than rewriting from scratch. For the blank plot: matplotlib bugs like this are almost always ordering issues — `plt.show()` clears the current figure, so any `savefig()` after it writes an empty canvas; call `savefig()` before `show()` (or drop `show()` entirely since this runs headless), and consider a non-interactive backend (e.g. `matplotlib.use('Agg')`) for scripted runs. For `filter_features`, compute `df.corr().abs()`, iterate over column pairs preserving original column order, and when a pair's correlation exceeds the threshold mark the later-indexed column for removal — build a drop set first, then drop it once at the end so you don't mutate the matrix mid-iteration. For `bootstrap_balance`, find each class's row count via `value_counts()`, use `sklearn.utils.resample` with `replace=True`, `n_samples` set to the majority count, and `random_state=42` on the minority subset only, then concatenate with the untouched majority rows. For `train_evaluate`, fit `RandomForestClassifier(random_state=42)` on the train split, predict on the held-out test split, compute `f1_score`, and write just the numeric value (no extra text) to the metrics file. After editing, actually execute the script and inspect each output file to confirm it was regenerated, not stale.
