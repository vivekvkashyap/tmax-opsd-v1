Break this into extraction, decoding, and service stages, validating output at each boundary before moving on. First use `ffmpeg -i /app/exfiltration_feed.mp4 -vf "select='not(mod(n\,30))'" -vsync vfr frame_%03d.png` (or select specific frame numbers directly) to pull out every 30th frame, then run each through `tesseract` (or a Python OpenCV/Pillow+pytesseract script if OCR quality needs preprocessing like thresholding/upscaling) to recover each frame's text; concatenate the recovered strings in frame order — do not guess the content, actually read it off the extracted images since OCR errors will break the subsequent base64 decode. Once you base64-decode the concatenated string into JSON, independently recompute SHA256 over the decoded certificate bytes and compare it to the `sha256_checksum` field to catch OCR/decoding corruption before trusting the extracted cookie or cert; on mismatch, write the integrity-failure log and stop rather than proceeding with bad data. For the honeypot, build a Python server using `http.server`/`ssl` with an `ssl.SSLContext` wrapping a socket on 8443 using the recovered cert (and a freshly generated throwaway private key, since only the cert was provided) and a plain HTTP listener on 8080, both routing `GET /callback` through the same handler logic: compare the `Cookie` header byte-for-byte against the token extracted from the video, respond 200/403 with the exact bodies specified, and append `User-Agent` to the access log only on the branches described. Run both listeners persistently (background processes/threads) so the verifier can hit them after your script exits.