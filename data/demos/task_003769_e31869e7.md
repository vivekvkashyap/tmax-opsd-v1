Work through the four sub-tasks independently, creating `/home/user/audit/` first. (1) Certificate check: verify the key/cert pair matches (e.g. compare `openssl x509 -noout -modulus -in server.crt | openssl md5` against `openssl rsa -noout -modulus -in server.key | openssl md5`), then run `openssl x509 -noout -fingerprint -sha256 -in server.crt` and redirect its exact output (including the `SHA256 Fingerprint=` prefix) to `cert_fingerprint.txt` — do not reformat it. (2) Integrity: use `sha256sum -c checksums.txt` from inside `web_root/` (or manually recompute and compare) to find lines reporting FAILED, then extract just the base filenames of the mismatches into `tampered_files.txt`. (3) Redaction script: write `redact.sh` using `sed` with a regex targeting each sensitive query-param name followed by `=` and capturing everything up to the next `&` or end-of-quote, replacing the captured value only (keep the param name and delimiters) — build one sed expression per param name (`password`, `token`, `ssn`) and chain them; run it to produce `clean_access.log`. (4) Network policy: parse `access.log` for lines with a ` 404 ` status, extract the leading IP with `awk`/`cut`, tally counts per IP (`sort | uniq -c | sort -rn`), take the top 3, and hand-write the three `iptables -A INPUT -s <IP> -j DROP` lines into `block_ips.sh` in that descending-frequency order — verify the tally by eye before writing the file.