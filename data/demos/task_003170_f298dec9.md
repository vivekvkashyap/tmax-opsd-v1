Build the two phases so they interact correctly at runtime rather than testing them in isolation only. Phase 1: write `/home/user/vault_monitor.py` as a daemon that (a) writes its own PID to `/home/user/monitor.pid` immediately on startup, (b) binds a TCP socket strictly to 127.0.0.1:8443 and, on receiving "STATUS", replies "OK\n" or "LOCKED\n" based on current quota state — run the socket accept loop and the polling loop concurrently (e.g. via threading), (c) every second, sum file sizes under `/home/user/vault/` with `os.path.getsize` over `os.listdir`/`os.walk`, and if the total strictly exceeds 10,485,760 bytes, write `/home/user/vault_state.log` with the exact line `LOCKED: <total_bytes>` and find+kill (SIGTERM) any process whose cmdline contains `interactive_uploader.py` (e.g. scan `/proc` or use `psutil`/`pgrep -f`). Phase 2: write `/home/user/simulate_uploads.exp` using `spawn`, sequential `expect "<exact prompt text>"` / `send "<value>\r"` pairs matching the uploader's literal prompts, ending in an `expect eof` or indefinite wait so the script naturally terminates once the monitor kills the uploader process. Start the monitor first, then run the expect script, and confirm the log content and killed process afterward.