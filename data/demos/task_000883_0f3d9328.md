Work through `pipeline.py` function by function rather than rewriting from scratch. In `clean_data`, impute `temperature` and `vibration` nulls with each column's own median (`df[col].fillna(df[col].median())`), one-hot encode `sensor_type` with `pd.get_dummies(df, columns=['sensor_type'])` (this both drops the original column and adds the new ones automatically), and leave `failure` untouched — return the resulting DataFrame. For the headless plotting bug, add `import matplotlib; matplotlib.use('Agg')` before `pyplot` is imported anywhere (must happen before the first `import matplotlib.pyplot as plt` executes, ideally at the very top of the file) and replace `plt.show()` with `plt.savefig('/home/user/importance.png')`. In `train_and_evaluate`, compute `accuracy_score(y_test, y_pred)` and `f1_score(y_test, y_pred, average='macro')`, then `json.dump({"accuracy": ..., "f1_score": ...}, f)` to `/home/user/metrics.json`. Write `test_pipeline.py` with a small hand-built DataFrame containing NaNs in the numeric columns and a couple of category strings, call your fixed `clean_data`, and assert `result.isnull().sum().sum() == 0` plus that the categorical column's original name is gone and new dummy columns exist. Run `python pipeline.py` then `pytest test_pipeline.py` and fix any remaining errors before declaring done — don't skip actually executing both.