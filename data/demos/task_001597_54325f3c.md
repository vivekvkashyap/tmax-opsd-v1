Treat `/app/payload_encoder` as a black-box oracle you can query for equivalence testing, and mine `/app/evidence.mp4` for the algorithm's parameters rather than guessing. Method: (1) Extract frames from the video with `ffmpeg -i /app/evidence.mp4 frames/frame_%03d.png`, then read through the frames (visually or via OCR) to find the terminal text — look specifically for compiler/linker error messages that reveal a hardcoded constant (an IV or key), and for any debugger/race-condition discussion that describes how even-indexed vs odd-indexed bytes are treated differently. (2) Formulate a hypothesis for the per-index transform (e.g. an XOR keyed by a small repeating array, with parity-dependent key selection) based on exactly what the video states. (3) Validate the hypothesis by running `/app/payload_encoder` on a handful of your own test strings and comparing byte-by-byte against your hypothesis computed by hand/script — adjust the hypothesis until every test case matches exactly, since a slightly-off constant or off-by-one on the parity/index logic will diverge quickly on longer inputs. (4) Implement the confirmed transform in `/home/user/reconstruct.py`, taking one CLI argument and printing only the uppercase hex digest, and re-verify against the oracle on several more random strings before finishing.