Tackle this in dependency order since later stages need earlier ones working. First fix the Go circular import between `graph` and `models` in `/home/user/videoproc/analyzer`: identify which package should own the shared type/interface causing the cycle, extract it into a third, lower-level package (or have one package depend on an interface instead of the concrete type) so the dependency arrow only points one way, then `go build -o /home/user/bin/analyzer` — don't rewrite the analysis logic, just restructure the imports. Compile the C++ extractor with something like `g++ -O2 -o /home/user/bin/extractor main.cpp` (check its actual dependencies, likely shelling out to `ffmpeg`). For asset organization, parse `dependencies.json` as a DAG, do a topological/DFS traversal from roots, and mirror parent/child relationships as nested directories under `/home/user/videoproc/organized_assets/`, copying (not moving) each file into its resolved path. For the pipeline, spawn the analyzer as a subprocess, open a WebSocket client (e.g. `websockets` library) to `ws://localhost:8080`, invoke the extractor to pull exactly the first 100 frames from `/app/input.mp4`, base64-encode each frame and send it, collect the returned motion scores in frame order, and write them as a JSON float array to `/home/user/output_metrics.json`. Your scores are graded by MSE against a hidden reference, so keep frame ordering and the extraction/encoding pipeline faithful to a standard, deterministic approach rather than approximating.