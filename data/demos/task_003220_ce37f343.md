Break this into data prep, OCR, modeling, and serving. Load and join `/app/data/features.csv` and `/app/data/targets.csv` on `customer_id` (pandas is fine), sorted ascending. Run OCR on `/app/data/specs.png` with `pytesseract.image_to_string` (install `pytesseract` + the `tesseract-ocr` binary) and parse the printed text yourself to pull out the numeric ALPHA value stated in the image — don't hardcode a guessed significance level. Build the design matrix with an intercept column of ones prepended to `x1,x2,x3`, and fit OLS via the normal equations or `statsmodels.OLS` to get coefficients, residual variance, and the `(X^T X)^-1` matrix needed for standard errors. Compute each coefficient's confidence interval as `coef ± t_crit * se`, where `t_crit` comes from the t-distribution's inverse CDF at `1 - ALPHA/2` with `n - p` degrees of freedom (use `scipy.stats.t.ppf`, not a hardcoded z-score) — using the extracted ALPHA is what makes the CI correct. Stand up a FastAPI/Flask app on `127.0.0.1:8000` exposing `POST /predict` (apply the fitted coefficients to the incoming x1/x2/x3) and `GET /ci` (return the four computed intervals as specified), and leave it running so requests can be tested end-to-end.