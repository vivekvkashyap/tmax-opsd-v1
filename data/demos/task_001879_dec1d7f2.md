First edit `/home/user/simulate_allele.sh` to change only the default `dt` value from `0.5` to `0.01`, leaving the ODE update logic (`dw = -m*w + b*w*(1-w)`) and integration method (explicit Euler) untouched — verify by running the script once manually with a known `m` and confirming it no longer produces negative/diverging values. Then write `/home/user/optimize_m.sh` as an executable Bash script that loops `m` from `1.00` to `2.50` in `0.01` steps (use `seq 1.00 0.01 2.50` or an `awk`-generated sequence to avoid floating-point drift from raw bash arithmetic), calling `./simulate_allele.sh <m> 0.01` for each and capturing the printed final `W`. Compare each result numerically (with `bc` or `awk`, not string comparison) against the threshold `0.1000`, and since you're scanning in increasing order, keep the *first* `m` where `W < 0.1000` — that's your smallest/optimal value, so you can stop the search there. Write the result to `/home/user/result.txt` in exactly the format `m=<value>, W=<value>`, matching the decimal formatting your simulate script prints for W. Run `chmod +x optimize_m.sh` and execute it once fully to confirm `result.txt` is produced with a sensible value before finishing.
