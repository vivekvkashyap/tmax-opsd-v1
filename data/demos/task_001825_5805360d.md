Treat this as two independent, verifiable sub-tasks. First, connect to `/home/user/graph_backup.db` with `sqlite3` and run `CREATE INDEX idx_edges_target ON edges(target_id);` — check it took with `SELECT name FROM sqlite_master WHERE type='index'`. Second, write `extract.py` to accept `sys.argv[1]` (relation) and `sys.argv[2]` (target_node_type) as parameters, and build the query as a three-way join (source nodes -> edges -> target nodes) filtered by both parameters — bind them with `?` placeholders in `cursor.execute(query, (relation, target_type))` rather than string-formatting them into the SQL, since the task explicitly requires parameterization against injection. Deduplicate results (e.g. via `SELECT DISTINCT` or a Python set/dict keyed by source name) before sorting alphabetically by `source_name`, and emit a JSON list of `{"source_name":..., "source_status":...}` objects with `json.dump`. Run `python3 extract.py depends_on storage` as the final step and inspect `/home/user/results.json` to confirm it's valid JSON matching the required shape before finishing.
