This is a data-leakage bug in cross-validation, not a math bug in the model itself — the fix must move normalization inside the fold loop. First install missing deps: `apt-get install -y libgsl-dev ffmpeg` (or equivalent). Read through `pipeline.c` to find where it currently computes the global mean/stddev of the brightness feature over the *entire* dataset before any fold splitting, then applies that single normalization to all folds — this leaks test-fold statistics into training. Restructure the loop so that, for each of the 5 folds, you first partition indices into train/validation for that fold, compute mean and standard deviation using ONLY the training subset's feature values, then apply `(x - train_mean)/train_std` to both the training points AND the validation points of that fold (never recompute stats from validation data). Keep everything else (frame extraction via `ffmpeg`/`popen`, brightness computation, linear regression fit, MSE accumulation across folds) unchanged. Compile with `gcc -O2 pipeline.c -o pipeline -lgsl -lgslcblas -lm`, run it, and write the printed overall MSE to `/home/user/final_mse.txt`. Sanity-check by confirming fold-level MSE values are now higher (less optimistic) than before your fix, which is the expected signature of removing a data leak.