First recover the threshold: run `ffprobe -v quiet -show_entries format_tags=comment -of default=noprint_wrappers=1:nokey=1 /app/audio/researcher_note.wav` (also check the `title` tag if comment is empty) to dump the embedded metadata text, then read the sentence it contains to find the maximum-duration number — use that discovered number as your threshold, either read dynamically in the script or hardcoded after you've confirmed it. For etl_filter.sh, iterate `*.csv` in the input dir; for each file, skip the header line and validate every remaining row with awk: check the row has exactly 4 comma-separated fields with none empty, `duration` is a number satisfying `0 < duration <= THRESHOLD`, `loss` is a number `>= 0`, and `wer` is a number in `[0,100]` — use awk's numeric comparison (`+0` coercion or regex to first reject non-numeric strings, since awk treats non-numeric fields as 0 silently). If any row fails any rule, discard the whole file (don't copy); only copy files where every row passes. Make the script executable with `chmod +x`, and test it yourself against a few hand-made clean/evil CSVs (missing fields, zero/negative duration, out-of-range wer) before trusting it, since silent awk numeric coercion is the most common source of false passes here.