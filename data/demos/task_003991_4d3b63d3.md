Follow the pipeline stages in strict order and verify each one numerically before moving to the next, since later steps (grid search) depend heavily on earlier ones (feature selection). Load the CSV with pandas, compute `df.drop(columns=['yield']).corr()` and scan the upper triangle for pairs with `abs(corr) > 0.85`; for each such pair compare `df[col].var()` for both columns and drop whichever has the lower variance (verify your correlation and variance numbers by printing them — don't assume which column "should" be dropped). Build `temp_humid_idx = df['temperature'] * df['humidity']` and assemble your final feature set from temperature, humidity, temp_humid_idx, plus whatever survived the correlation-pruning step. Use `sklearn.model_selection.KFold(n_splits=5, shuffle=False)` (not the default `cv=5` which may behave differently across versions) combined with `GridSearchCV(Ridge(), param_grid={'alpha':[0.1,1.0,10.0]}, cv=kfold, scoring='r2')` fit on the full feature/target arrays. Pull `best_params_['alpha']` and `best_score_` (rounded to 4 decimals) from the fitted search, and write the JSON with exactly the three required keys — double check the dropped_features list matches what you actually removed.
