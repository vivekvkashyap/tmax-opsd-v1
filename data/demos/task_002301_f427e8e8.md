For the video: decode `/app/build_signal.mp4` frame by frame at its stated 10fps rate (e.g. via `ffmpeg -i build_signal.mp4 -vf fps=10 frame_%04d.png` or reading pixel (100,100) per frame with OpenCV/PIL), classify each frame's center pixel as pure black (bit 0) or pure white (bit 1), then group the resulting bitstream into 8-bit chunks MSB-first and convert each byte to its ASCII character to recover the configuration string — don't guess the string, derive it from the actual pixel data. For the Makefile: the circular failure comes from each shared library trying to link against the other library it hasn't been built yet, so compile all object files first with `-fPIC -c`, then link each `.so` from its objects without requiring the other library's symbols to resolve at link time (e.g. `-Wl,--allow-shlib-undefined`, or defer that linking until both `.so` files exist), and only then link `pipeline_test` against both. Don't touch the `.c` file logic — only Makefile/header/linker flags. For the stream processor: implement a single-pass state machine over stdin bytes that toggles an "in-code" boolean on each backtick (printing the backtick unchanged either way) and only HTML-escapes `<`, `>`, `&` when that boolean is false; stream character-by-character so it scales to large inputs, make it executable, and test it manually against edge cases (leading/trailing backticks, odd backtick counts, empty input) before trusting it against the fuzzer.
