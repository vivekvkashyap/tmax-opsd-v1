Set up the venv first (`python3 -m venv /home/user/env && source /home/user/env/bin/activate && pip install numpy scipy pandas matplotlib`) so all subsequent work runs inside it. In `augment.py`, read `experimental.csv`, define the SIR ODE system for `scipy.integrate.odeint` exactly as specified (N=1000, S0=990, I0=10, R0=0), then wrap it in a `fit_func(t, beta, gamma)` that returns only the `I` column so `scipy.optimize.curve_fit` can fit against the data's `t` and `I` columns — pass `p0=[0.5, 0.2]` and bounds `([0.001,0.001],[1.0,1.0])` exactly as specified, since curve_fit's `popt`/`pcov` are the "posterior" mean/covariance you'll sample from. Call `np.random.seed(42)` immediately before `np.random.multivariate_normal(popt, pcov, 5)` — seeding order and the exact fitted popt/pcov both affect the samples, so don't reorder other random calls before this. For each of the 5 (beta, gamma) draws, re-solve the ODE over `np.linspace(0, 50, 51)` and take the I column, round to 4 decimals, and assemble the CSV with columns `t, I_0, ..., I_4` in draw order. Finally create the output directory before writing, and plot the noisy data as a scatter plus the 5 trajectories as lines to `trajectories.png`.