Start by transcribing `/app/voicemail.wav` with a speech-to-text tool (local Whisper or similar), normalizing the output to lowercase with single spacing and no punctuation to get the passphrase — don't guess a phrase, derive it from the actual audio. Use that exact string as the password to `unzip -P "<phrase>" /app/evidence.zip -d /app/evidence/`. In the extracted logs/auth.log, grep for successful root authentication lines (e.g. "Accepted password for root from ...") and pull the source IP from the first/only such match, ignoring failed attempts from other IPs. For the ELF, run `strings backdoor_bin | grep LISTEN_PORT` (or objdump/readelf on .rodata) to recover the `LISTEN_PORT=<port>` string and parse out the port number — don't assume a default port. Then write a Python HTTP server bound to 127.0.0.1 on that extracted port exposing GET /report with no auth required, returning JSON with exactly the keys attacker_ip and transcript populated from what you recovered in the earlier steps, and leave it running in the background (nohup/& or a persistent session) so it can be queried after your turn ends.