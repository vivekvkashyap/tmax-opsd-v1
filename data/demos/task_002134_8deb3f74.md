Read graph.c carefully looking for two classic C memory bugs: (1) any `malloc` sized from `strlen(x)` without the `+1` for the null terminator — this corrupts the heap on `strcpy`; fix every such allocation site, not just the first one you spot. (2) any array declared as a local (stack) variable inside a function that is then returned as a pointer — once the function returns, that memory is invalid and callers get garbage/undefined behavior; convert it to a `malloc`'d heap array sized to the actual result count. After patching, compile with `gcc -shared -fPIC -o libgraph.so graph.c` (or similar) and test with valgrind/ASan first to confirm no more invalid reads/writes before wiring up ctypes. In the Python wrapper, explicitly set `restype`/`argtypes` on both `get_build_order` and `free_build_order` (char pointers as `ctypes.POINTER(ctypes.c_char_p)`, ints as `ctypes.c_int` or `POINTER(c_int)`), decode each returned C string, write them to the output file, then call the free function before the process exits to avoid leaks. Do not change the topological-sort algorithm itself — only the memory-safety issues.
