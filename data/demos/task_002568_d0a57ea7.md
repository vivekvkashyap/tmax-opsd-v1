Load both CSVs into an in-memory SQLite database using Python's `sqlite3` (create `employees(id, name, manager_id)` and `sales(id, amount)` tables, then bulk-insert via `csv.reader`). Compute hierarchy sales with a single `WITH RECURSIVE` CTE: start from each employee as a root, recursively join `employees` on `manager_id` to walk down the org chart collecting all descendant ids (including the employee itself), then join to `sales` and `SUM(amount)` grouped by the root employee id — this correctly captures indirect reports at any depth, unlike a manual Python loop which is easy to get wrong for multi-level trees. Filter the aggregated results to `total_sales >= 5000`, `ORDER BY total_sales DESC, id ASC` for tie-breaking. Parse `sys.argv[1]` and `sys.argv[2]` as `limit` and `offset` and apply them with SQL `LIMIT ? OFFSET ?` on the already-sorted, already-filtered result set (not before sorting/filtering). Write `/home/user/output.csv` with header `id,name,total_sales` and only the paginated rows. Verify by running with a specific limit/offset and inspecting the output makes sense against your own by-hand check of the hierarchy sums.