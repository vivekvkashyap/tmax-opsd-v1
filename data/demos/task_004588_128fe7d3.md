Don't guess the fraud signature — derive it empirically by querying both databases for every `paper_id` in `/app/corpus/clean/` and `/app/corpus/evil/` and diffing what's structurally different. Connect to Postgres (`psycopg2`) and, for each ID, traverse outgoing edges in the `citations` table (a recursive CTE with `WITH RECURSIVE` or manual BFS/DFS in Python via `networkx`) to check whether the citation path loops back to the starting node within a small fixed number of hops, and whether it has any citations reaching the broader graph — compare hop counts and connectivity between clean and evil sets to find the discriminating pattern rather than assuming a specific hop count. Connect to Mongo (`pymongo`) and pull each paper's full metadata document, then diff the clean vs evil documents field-by-field (including nested arrays like `affiliations`) looking for a categorical field value that only appears in evil docs. Once you've identified the graph pattern and/or metadata field from this empirical comparison, implement `paper_filter.py` to check for it (combining both signals is safer than relying on just one, in case either alone has edge cases in the holdout set) and exit 0/1 accordingly. Validate against the full training corpora before considering it done.
