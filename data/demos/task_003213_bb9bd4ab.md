Fix the C parser first: locate the loop in `parse_logs.c` that scans `msg_start` for `\u` sequences and jumps `i += 5` — this reads past the string end when the `\u` marker appears near the string's tail (no bounds check against `strlen`), causing the segfault. Add a length check before advancing (e.g. only treat it as a unicode escape if at least 4 more hex-digit characters remain, else copy the backslash literally) and ensure the output buffer index never exceeds its capacity. Recompile with `gcc -o parse_logs parse_logs.c`. For `process.sh`, pipe `input.jsonl` through `./parse_logs` to get TSV, then use `awk` to bucket each row's epoch timestamp into `ts/3600` (integer division) and count occurrences per hour, printing `hour*3600,count`; to fill the gaps, compute min/max hour in the script (via `sort`/`awk` on the bucketed values) and loop from min to max emitting `0` for any hour absent from the counts before writing `hourly_counts.csv` sorted ascending. For the stratified sample, sort the TSV by locale then timestamp and use `awk '!seen[$2]++'` (locale is field 2) to keep only the first row per locale, preserving original field order, into `stratified_sample.tsv`. Finally install the cron job with `(crontab -l 2>/dev/null; echo "0 * * * * /home/user/l10n/process.sh") | crontab -u user -` (or edit the user's crontab directly) so it fires at minute 0 of every hour.