Extract the archive first (`tar xzf /home/user/raw_dataset.tar.gz`) and inspect `manifest.csv` to see the declared encoding per chunk file — don't assume they're all UTF-8. In your C program, parse the manifest line by line (filename, encoding), and for each chunk open an `iconv_t` conversion descriptor via `iconv_open("UTF-8", <source_encoding>)`, then run the chunk's raw bytes through `iconv()` before treating it as text/CSV — remember `iconv` requires you to manage input/output buffer pointers and remaining-length counters carefully across multiple calls if the buffer is exhausted mid-conversion. After converting each chunk to UTF-8, concatenate them in manifest order, but only take the header line from the first chunk (subsequent chunks are data-only). Parse CSV rows and keep only those where `Sensor` is exactly `TEMP_01` and `Value` (parsed as a float) is strictly greater than 25.0 — watch for exact string equality vs. substring matches. Write the header plus filtered rows to `filtered_dataset.csv`, count the filtered data rows (excluding header) for `summary.txt` in the exact `Filtered rows: <N>` format, then compile with `gcc -o process_data process_data.c -liconv` (or as needed on your platform), run it, and finally `tar czf` the two output files at the tarball root (`cd` into the directory containing them first, or use `-C`, so no leading path is embedded).
