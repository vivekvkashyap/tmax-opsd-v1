First, transcribe `/app/audio_sample.wav` yourself (e.g. with a local speech-to-text tool, or by careful listening/analysis if you have audio-processing tooling available) to recover the exact text-cleaning rule it describes — do not assume what it says. Once you have the rule in hand, implement `/home/user/tokenizer.sh` to: split the input string on whitespace (Bash's unquoted `for word in $1` word-splitting is the standard approach and matches how a shell reference implementation would behave), then for each token apply the rule you transcribed, printing one token per line. Pay special attention to edge cases that a naive implementation misses: an empty input string should produce no output at all (handle `$1` being unset or empty explicitly), tokens that are purely numeric integers need special handling per the rule, and multiple consecutive spaces should collapse naturally under default word-splitting. Since correctness is judged by bit-exact matching against a hidden reference across many random fuzzed inputs, test your script manually against varied inputs (empty string, numbers, the special keyword, mixed punctuation) before finishing, and `chmod +x` the script.
