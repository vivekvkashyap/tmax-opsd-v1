Write `/home/user/analyze_backups.py` to drive everything through `sqlite3`. First execute `CREATE INDEX idx_jobs_parent ON jobs(parent_id);` — the index name must match exactly since it's checked directly. Then build one SQL query combining a recursive CTE and a window function: the recursive CTE should start with the anchor row `WHERE id = 'BKP-001'` and recursively join `jobs c ON c.parent_id = cte.id` to walk down the tree (not up), unioning all descendants including the root itself. Wrap that CTE's result with a `SUM(size_bytes) OVER (ORDER BY start_time ASC ROWS UNBOUNDED PRECEDING)` (or the default frame, which is equivalent for a running total) aliased as `cumulative_size_bytes`. Verify manually that jobs unrelated to BKP-001 (e.g., one rooted at a different, nonexistent parent) are excluded from the recursion. Fetch rows as dicts (`sqlite3.Row` + `dict(row)` or `cursor.description`), and before writing, validate the list against the given JSON Schema with the `jsonschema` library (installing it if missing) — only write to `/home/user/lineage_report.json` if validation succeeds; otherwise raise/log the schema error and fix your query logic.
