Fix the C library first: `cd /home/user/libtrajectory && make`, read the linker error — a shared-library "linking error" is usually a missing `-fPIC` on the compile step and/or a misordered link line (object files must precede the `-l` libraries that use them). Fix the Makefile, rebuild, and check exported symbols (`nm -D libtrajectory.so`) for correct ABI visibility (static/visibility-hidden for internals, exported public API). Use `ffprobe -show_frames`/`ffmpeg -i camera_feed.mp4` to inspect frame count/fps and measure the actual max per-frame pixel displacement yourself rather than assuming a number — that measured value is your velocity constraint. Write `detector.go` with `cgo` (`#cgo LDFLAGS: -L. -ltrajectory`) to call into the library and validate parsed (X,Y) sequences against that per-frame delta bound; use goroutines/a worker pool if processing benefits from concurrency. `run_detector.sh` should run the Go binary on the single given file path and propagate its exit status directly (0 = clean, nonzero = evil).
