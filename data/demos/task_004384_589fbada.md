Follow the manifest as the source of truth for ordering: read `/home/user/artifact_repo/manifest.json`, find the entry with `id == "model_weights_v2"`, and concatenate its listed chunk files in the exact order given (don't sort filenames alphabetically or by mtime — use the manifest's array order) into one bytes blob. Reverse the obfuscation pipeline in the correct order: first XOR every byte with `0x42`, then feed the result to `zlib.decompress()` — doing these steps out of order will fail or silently corrupt the result, so verify decompression succeeds without error before proceeding. Interpret the decompressed bytes as a packed sequence of little-endian 32-bit floats (`struct.unpack` with `<f`, or read via `array`/`numpy` with `dtype='<f4'`), and per the task's explicit requirement use Python's `mmap` module to memory-map a temp file containing this binary data and scan through it to find the maximum value rather than just doing it in a plain list. Format the maximum to exactly 4 decimal places (Python's `f"{val:.4f}"`) and write only that string, no trailing text, to `/home/user/result.txt`.
