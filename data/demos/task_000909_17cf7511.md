Start with the audio: transcribe `/app/meeting.wav` (e.g. with `whisper-cli`) to recover the specific restricted numeric `user_id` values mentioned — these are data you must extract, not something to assume or invent. In your C++ sanitizer, use a real JSON parser (pull in a header-only library like `nlohmann/json.hpp` if not already vendored) rather than string matching, since pipeline stages are JSON objects nested inside an array and values can appear at different nesting depths (e.g. inside a `$in` array). Walk the parsed pipeline array and for each stage object check: if it has a `$match` key, recursively search its value for a `user_id` field whose value equals (or, if it's an object with `$in`, whose array contains) any restricted ID; if it has a `$graphLookup` key, check whether `startWith` equals a restricted ID; if it has `$setWindowFields`, check that both `partitionBy` and `sortBy` keys are present — missing either is a rejection regardless of restricted IDs. Reject on the first violation found across all stages; otherwise accept. Print exactly `ACCEPT` or `REJECT` with no extra output. Before finishing, run your compiled `/home/user/query_sanitizer` against every file in `/app/corpus/clean/` and `/app/corpus/evil/` and confirm the verdicts match expectations (clean -> ACCEPT, evil -> REJECT).
