Run `tesseract /app/schema.png stdout` (or via `pytesseract`) to extract the validation-rule text first, and print/inspect the raw OCR output before hardcoding filters — OCR can misread symbols like `<=`/`>=`/brackets, so sanity-check each extracted rule against what makes sense for a numeric schema. Write the cleaning script to load `/app/raw_data.csv` with pandas, apply each extracted constraint as a boolean mask (combine with `&`), and save the filtered rows to `/app/clean_data.csv`, printing `len(df)` before and after so you can cross-check the numbers the API will report. For the API, bind explicitly to host `127.0.0.1` port `8080` (not `0.0.0.0`), implement `GET /stats` returning `{"initial_rows": ..., "cleaned_rows": ...}` and `GET /data` returning the cleaned records as JSON, and launch it as a background/daemon process so it stays up for external requests — verify with a local `curl` to both endpoints before finishing. Keep the constraint logic in one place so `/stats` and `/data` are guaranteed consistent with the values actually written to `clean_data.csv`.