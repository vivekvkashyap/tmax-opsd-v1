Combine visual OCR/inspection with black-box fuzzing before writing any C code. Open or run OCR on `/app/vnc_screenshot.png` first (e.g. `tesseract` or view it directly) to extract the documented parsing rules: the expected comma-delimited input field order, the trimming behavior, and the specific default values applied when a field is empty or invalid (including how the core-count field's validity is defined). Then independently probe `/app/legacy_parser` with varied inputs — empty fields, fields with leading/trailing spaces, missing trailing commas, extra commas, non-numeric or negative core counts, and completely empty strings — to confirm the screenshot's rules actually match observed behavior (screenshots can be incomplete about corner cases like extra/missing commas or numeric edge cases like zero or negative numbers). Reconcile any discrepancies by trusting the oracle's actual output. Only then implement `/home/user/parser.c`: split on commas, trim whitespace per field with a small helper, apply the documented defaults exactly, and format the output string precisely as specified. Compile with `gcc -o /home/user/parser parser.c`, then re-fuzz test-by-test against the real oracle before finishing.