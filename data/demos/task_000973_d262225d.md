Build the archive format exactly to spec byte-for-byte, since the verifier does a bit-exact comparison against a hidden oracle plus corruption/verify tests — ambiguity in the spec should be resolved by re-reading it, not by guessing. For traversal, use `os.walk` with `followlinks=True` but maintain a `set()` of `os.path.realpath()`-resolved directory paths already visited, skipping any directory whose resolved path is already in the set (this is what prevents infinite loops from the symlink cycles in the fixture). Collect `.wav` files, compute relative paths with `os.path.relpath` normalized to forward slashes, and sort lexicographically before processing. Implement RLE carefully: iterate the raw bytes, count runs, and split any run longer than 255 into multiple `[255][byte]` chunks followed by the remainder — verify against the given `"AAAAABBB"` example. Pack integers with `struct.pack('<H', ...)` / `('<I', ...)` for little-endian sizes exactly as specified, and compute `zlib.crc32` over everything written so far, appended as a final little-endian 4-byte footer. For `verify`, recompute CRC32 over all bytes except the last 4 and compare exactly, printing precisely `Archive OK`/`Archive Corrupted` with matching exit codes. Test pack+verify round-trips on your own constructed symlink-loop fixture before running against `/app/dataset`.
