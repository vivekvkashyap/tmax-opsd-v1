This task has two independent deliverables — tackle them separately and verify each in isolation before combining. For the TLS/proxy part: generate a self-signed cert with `openssl req -x509 -newkey rsa:2048 -nodes -keyout /home/user/certs/tls.key -out /home/user/certs/tls.crt -days 365 -subj '/CN=localhost'`, write an nginx.conf with a `server { listen 8443 ssl; ssl_certificate ...; ssl_certificate_key ...; location / { proxy_pass http://127.0.0.1:5000; } }` block (remember nginx.conf needs a valid top-level `events {}` and `http {}` wrapper if you're not using an include-style snippet), start the Flask backend (`python3 /app/receiver.py &`) before nginx, then start nginx with your config and test end-to-end with `curl -k https://127.0.0.1:8443/...` to confirm it actually proxies (don't just check nginx starts without error). For the HMAC verifier script: read stdin line by line, split each line on the FIRST colon only (the raw message itself may contain colons), recompute `hmac.new(b'COMPLIANCE_KEY_2024', message.encode(), hashlib.sha256).hexdigest()` and compare using a constant-time comparison (`hmac.compare_digest`), printing `[VERIFIED] <message>` or `[TAMPERED] <message>` — get the exact prefix strings and spacing right since a fuzz-equivalence check will compare byte-for-byte against many random inputs, including malformed lines.