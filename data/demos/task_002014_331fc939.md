Do each subtask independently and verify it before writing the health check script, since the script's job is just to re-check your own work. Archive: `tar -czf /home/user/archive/legacy_service.tar.gz -C /home/user legacy_service` (create the archive/ dir first), then sanity check with `tar -tzf`. Symlink: create the shared_logs directory, then `ln -s /home/user/shared_logs/cloud_service /home/user/cloud_service/logs` using the absolute target path exactly (not a relative path), and confirm with `readlink`. Systemd unit: edit the `[Unit]` section of cloud-worker.service to add both `After=cloud-db.service` and `Requires=cloud-db.service` lines — these are separate directives and both are required by the check, not just one. For health_check.py, write straightforward checks: `os.path.isfile` for the tarball, `os.path.islink` plus `os.readlink` compared exactly to the target path for the symlink, and a substring/regex check on the unit file text for both directive lines; only if all three checks pass should it write the literal string `MIGRATION_READY` (no extra whitespace/newline issues) to the status.log path inside the now-existing logs symlink target. Run the script afterward and confirm the log file's content matches exactly.
