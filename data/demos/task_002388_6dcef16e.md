Implement the watcher as a simple polling loop using `<filesystem>` (C++17) to list `/home/user/staging` every second, since that's robust and easier to get right than inotify. For each `.md` file found: open it, read exactly the first two lines, and parse them by splitting on the first `": "` after the `Author:`/`Topic:` prefix (strip the prefix, then trim whitespace) rather than assuming fixed offsets. Replace spaces with underscores in both extracted fields before building the new filename `<Topic>_<Author>.md`, and use `std::filesystem::rename` (or copy+remove) to move the file into `/home/user/organized`. Check for a file literally named `HALT` each poll iteration and `break`/`return` cleanly when seen — do this check before trying to parse it as markdown. After writing the three sample `.md` files, sleep briefly (a couple seconds) to let the polling loop catch them before dropping `HALT`. Finally use `ls /home/user/organized | sort > /home/user/final_listing.log` to produce the exact expected listing, and confirm the watcher process has actually exited before checking results.