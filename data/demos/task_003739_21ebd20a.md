Diagnose the Makefile failure precisely before touching anything else: run `cat -A Makefile` (or `sed -n l`) to reveal whether the recipe line under `ws_sanitizer:` is indented with spaces instead of a literal tab — `make` silently rejects space-indented recipe lines with a "missing separator" error, and the fix is to replace that leading whitespace with an actual tab character, not just re-typing what looks the same visually. After `make` succeeds, follow the pipeline exactly as specified: pipe `raw_payloads.json` into `./ws_sanitizer` redirecting to `sanitized.json`, start `python3 ws_server.py sanitized.json` in the background, then run `python3 pbt_test.py` in the foreground. Do not modify `ws_server.py` or `pbt_test.py` — they're the fixed contract your build must satisfy. If the PBT step fails, the bug is in `ws_sanitizer.c`'s sanitization logic (compare its handling of `<`, `>`, and `alert` against what `pbt_test.py`'s assertions require), not in the test scripts. Confirm `success.txt` is created with the expected content before finishing.