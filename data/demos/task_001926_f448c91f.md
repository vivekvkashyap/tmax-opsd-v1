Work service-by-service, then wire them together. In compute.cpp, use Eigen: load A and b from the JSON body (nlohmann::json), compute `A.determinant()`, and if `std::abs(det) < 1e-5` add `1e-6` to each diagonal entry (`A(i,i) += 1e-6`) before solving — do this check unconditionally, since a naive fix that only regularizes when the solve visibly fails will miss exactly-singular cases like a 2x2 all-ones matrix. Replace any `A.inverse() * b` with `A.colPivHouseholderQr().solve(b)`, which stays stable even when A is only mildly conditioned. Rebuild via the provided Makefile and smoke-test `/solve` directly with curl against a known-singular 2x2 matrix before touching anything else. For start_services.sh, launch redis-server, the compiled engine, and the Flask app (with `COMPUTE_URL` exported) each backgrounded with `&`, and add short `sleep`/health-poll waits so later steps don't race the bind. Only after `/health` returns 200 and `/solve` returns finite numbers should you write visualize.py: POST the given payload, reshape the returned `state` array to 10x10 with numpy, and save a matplotlib heatmap to the exact output path. Keep all three processes alive in the background for verification.