First fully unpack the nested archive: `tar xzf /home/user/artifacts.tar.gz` extracts the outer level (giving you inner `.tar` files), then `tar xf` each of those to get the flat `.bin` files — don't assume a single extraction step is enough, inspect what each `tar tf` lists before writing your processing loop. Write a small C program that opens each `.bin` file in binary mode, reads the first 4 bytes and compares them byte-for-byte against `"ARTI"` (reject/skip anything else, including files with the wrong magic — no report row for those), then reads the next 2 bytes as a little-endian uint16 version and the following 2 bytes as a little-endian uint16 description length, then reads exactly that many bytes as the description and prints it WITHOUT relying on a null terminator (use `fwrite`/`printf("%.*s", len, buf)` since the string isn't null-terminated in the file) — a common bug here is over-reading past the description into the trailing payload bytes. Have the C program (or a wrapper shell script driving it over all extracted `.bin` files) emit CSV rows, then sort the collected rows alphabetically by filename (e.g. pipe through `sort`) before writing the final `filename,version,description` header plus rows to `/home/user/artifact_report.csv`.