Design the pipeline as: parallel per-file extraction stage, then a serial merge/aggregate stage. For each of the four logs, launch a background job (`&`) or use `xargs -P`/`parallel` to run an awk/sed extraction that, per line: buckets the timestamp down to the nearest 10 minutes (parse minute field, integer-divide by 10, multiply back, zero-pad, keep seconds as `00`), masks the IPv4 by replacing the last octet with `000` (split on `.`, rebuild), and normalizes the path (lowercase, then strip everything from `?` onward using `${var%%\?*}` or `sed 's/?.*//'`, applied after lowercasing so case-insensitive query removal isn't an issue). Write each file's intermediate rows to its own temp file, `wait` for all background jobs, then `cat` the intermediates, run them through `sort | uniq -c` (or `awk` associative-array counting) to aggregate identical `(bucket, ip, path)` triples, and finally sort the result by timestamp, then IP, then path before emitting the CSV with header `Timestamp,IP,Path,Count`. Make the script executable and actually run it to produce `/home/user/final_report.csv`.
