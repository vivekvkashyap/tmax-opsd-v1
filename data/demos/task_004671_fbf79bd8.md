Treat this as two layered problems. First replicate the reference validator's exact decision order in `/home/user/verify_token.py`: split the raw token on '.', base64url-decode (with padding correction) the header/payload and reject malformed structure first; parse the header JSON and require `alg` to be exactly `RS256` (reject `none`, missing, or anything else) before even looking at the signature; verify the RS256 signature against `/app/certs/public.pem` using PyJWT/cryptography with `algorithms=["RS256"]` and signature verification enabled; only after a valid signature, check the payload's `file_hash` claim against a freshly computed `hashlib.sha256` hex digest of `/app/data/critical_config.json`, treating a missing/mismatched hash as INTEGRITY_FAIL; otherwise print VALID. Since equivalence is judged by fuzzing thousands of cases against a hidden oracle, get the check ordering and edge cases (empty parts, bad base64, non-JSON header) precise rather than "close." Second, write `auth_service.py` as a minimal HTTP server on 127.0.0.1:8081 that pipes the Bearer token to your script's stdin and maps VALID→200/else→401, then wire nginx.conf's `auth_request` directive on `/` at 127.0.0.1:8080 to call it before proxy_pass to 127.0.0.1:9000, leaving both processes running.
