Fix the pipeline bug precisely rather than rewriting the merge logic: pandas left-merges introduce NaN for unmatched keys, which silently upcasts an int column to float64 (or nullable Int64 if you cast early). In pipeline.py, after the merge, explicitly do `df['category_id'] = df['category_id'].fillna(-1).astype('int64')` — fill first, cast second, and use the plain numpy `int64` dtype (not pandas' capital `Int64`) so `df['category_id'].dtype` prints exactly `int64`. Run the script and confirm `/home/user/processed.csv` has no NaNs and -1 in the expected gap rows before moving on. For benchmark.py, compute cosine similarity across the 5 feature columns for all documents (e.g. `sklearn.metrics.pairwise.cosine_similarity` or manual dot-product/norm), exclude the query row itself, and break ties on smaller doc_id — a stable sort by (-similarity, doc_id) handles this correctly, plain `argsort` on ties may not. For the benchmark, time each of the 1000 per-doc_id searches individually (e.g. `time.perf_counter()` around each call), then compute a Student's t-based 95% CI (`scipy.stats.t.interval` or the t-formula with `n-1` degrees of freedom) over those 1000 timings, not a normal-distribution approximation. Write report.json with the exact keys and confirm `ci_lower < mean_time_sec < ci_upper` holds.