First inspect /home/user/cloud-manager's behavior manually (run `login`, `list`, `terminate <id>` by hand) to see exactly what prompts it emits and in what order, rather than assuming a format. Then automate the interactive prompts with `expect` scripts (or heredocs piped via `expect -c "spawn ...; expect \"Username: \"; send \"...\\r\"; ..."`), one for login and one reusable per-container for terminate, sending `y` to the confirmation prompt. In the main bash script: run the expect-login step first, capture `list`'s CSV output, then parse it (skip the header) with `awk -F,` to filter rows where field 2 (CostPerHr) > 2.00 AND field 3 (CPU_Percent) < 5 AND field 4 (Mem_Percent) < 10 — use awk's numeric comparisons, not string comparisons, since these are decimal/integer values. For each matching ContainerID, invoke the expect-terminate step, check its output/exit status for "Terminated <id>" to confirm success, and only then append the ID to /home/user/terminated_containers.txt (one per line). Make cost_pipeline.sh executable with `chmod +x`, run it, and inspect both the output file and the tool's own state to confirm exactly the wasteful containers were terminated — not more, not fewer.