Build this as a clear multi-pass pipeline. Pass 1 (dedup): group rows by `id`, pick the earliest timestamp per group — but first normalize every `ts` to a comparable form (detect unix-epoch int vs ISO8601 string, convert both to a common UTC epoch/datetime) before comparing, or you'll wrongly compare an int to a string. Pass 2: convert each winning timestamp to `YYYY-MM-DDTHH:MM:SSZ`. Pass 3 (normalization): lowercase `msg`, then regex-replace IPv4 patterns with `<IP>` and hex-only tokens of length >= 6 with `<HEX>` — do IP replacement first, and match the hex pattern as a whole token (`[0-9a-f]{6,}`) so you don't clip substrings inside already-lowercased words. Pass 4 (anomaly detection): bucket deduplicated ERROR rows by hour; for any hour with at least 2 preceding hours present, flag it if its count exceeds 2.0 * the average ERROR count of the immediately preceding two hours (missing/zero hours count as 0, not skipped). Emit the exact JSON shape requested, and keep scratch files under `/tmp`.
