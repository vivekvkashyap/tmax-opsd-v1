Work in order: first wire the services, then exploit, then patch. For Nginx, edit `/app/nginx.conf` to add a `server` block listening on 8080 with `proxy_pass http://127.0.0.1:5000;`, then start nginx, the Flask app, and the daemon as background processes and confirm each is listening with `ss -ltnp` or `curl`. For the exploit, do not guess — reverse engineer `/app/auth-daemon` with `objdump -d` / `strings` / `readelf` to find how it parses the incoming TCP message (look for a fixed-size buffer read, e.g. a `recv`/`read` into a stack array followed by a comparison against a flag/state variable placed right after it in memory — that adjacency is the overflow target). Then inspect `/app/app.py` to see exactly how it packs the `username` field into the wire message (delimiters, struct packing, lack of length checks) so your payload survives the Flask-to-daemon hop unmangled. Craft a payload of filler bytes sized to reach past the buffer plus the bytes needed to flip the admin flag, and write only that raw payload string to `/home/user/exploit_payload.txt`. Finally patch `/app/app.py` to validate `username` is alphanumeric and ≤16 chars, returning HTTP 400 otherwise — validate before any packing/forwarding happens.
