Tackle the three objectives in dependency order since the video analysis needs the OCR tool working first. For Objective 1, inspect `setup.py`'s `Extension(...)` call: a C-extension failing to link with an undefined-reference-to-math-function error is almost always a missing `libraries=['m']`, and a bad `include_dirs` pointing at a nonexistent path needs to be corrected (likely to `numpy.get_include()` if numpy headers are used) — fix both, then `pip install -e .` until it succeeds. For Objective 2, extract frames from the video with `ffmpeg`, then loop `python -m ocr_extractor <frame>` over them in order, watching for the OCR'd text to contain the phrase "goroutine leak triggered at" — record the first frame index where it appears and pull the hex address that follows it directly from the OCR output (don't guess it), writing both into the exact JSON structure requested. For Objective 3, study a few sample evil vs clean logs by hand to find the discriminating pattern (the hint text suggests: cancellation followed by a goroutine blocked on channel receive with no corresponding WaitGroup completion, vs. cancellation with proper cleanup or unrelated panics), then write `detector.py` to check for that specific structural pattern rather than a single keyword — test it against a broad set of both benign and malicious variations before finishing, since it will be evaluated against a hidden corpus requiring 100% accuracy.
