Debug by comparing the code line-by-line against the canonical Haversine formula rather than guessing: `dLat = (lat2-lat1)*pi/180`, `dLon = (lon2-lon1)*pi/180`, `a = sin²(dLat/2) + cos(lat1_rad)*cos(lat2_rad)*sin²(dLon/2)`, `c = 2*atan2(sqrt(a), sqrt(1-a))`, `distance = R*c`. Check specifically whether the longitude delta is actually converted to radians before use — a missing `*pi/180` on `dLon` (while `dLat` has it) is a classic bug that inflates distances hugely and is easy to miss on a quick read. Separately, audit every place a coordinate or accumulator is declared or parsed: any `float` type for latitude/longitude storage or for the running total distance, or any use of `std::stof` instead of `std::stod`, will lose precision once you sum >100k small distances — change all of these to `double`. After both fixes, recompile with `g++ -std=c++17`, rerun against `/home/user/route.csv`, and sanity-check the resulting `total_distance_km` in `/home/user/fixed_analysis.json` against a rough independent estimate (e.g. total displacement bound) before trusting it.