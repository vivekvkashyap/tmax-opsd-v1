Build this bottom-up and verify each layer before moving on. First create the directory and empty log file, then set the ACL with `setfacl -m u:nobody:r /home/user/capacity_data/metrics.log` and confirm with `getfacl` (a plain `chmod` will not satisfy the `user:nobody:r--` check). Write the C++ program to read `/proc/loadavg`, parse the first field, and format each line with `strftime`/`localtime` (not UTC) so the timestamp reflects whatever `TZ` the process inherits — do not hardcode a timezone in the C++ code itself; let the shell's `TZ=Asia/Tokyo` env var flow through. Compile with g++. In the supervisor script, discover the interface/gateway dynamically via `ip route` parsing (e.g. `ip route | awk '/default/ {print $5; print $3}'`) rather than hardcoding values, since the verifier recomputes them independently and compares. The supervisor must be a `while true; do ./cap_monitor "$IFACE" "$GW"; done` style loop so that killing the child process causes an automatic respawn — test this yourself with `pkill -f cap_monitor` and confirm `pgrep` finds a new PID within a couple seconds. Launch the supervisor with `nohup ... &` and disown it so it survives, then tail the log to confirm lines are accumulating before finishing.
