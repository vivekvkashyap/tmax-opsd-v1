Model this as three separate, verifiable stages in your Go program rather than one tangled pass. First, parse both CSVs (using `encoding/csv`) into an employee map (`id -> {name, department}`) and a directed edge set — dedupe edges with a `map[[2]int]bool` keyed by `(sender, receiver)` so repeated emails between the same pair count once. Second, compute out-degree per node by counting unique outgoing edges per sender; track the max, breaking ties by preferring the lower id (iterate ids in ascending order and only replace the current best on strictly greater degree). Third, for triangles: iterate over all unique unordered triples `{A,B,C}` with the same department (grouping employees by department first shrinks the search a lot), and for each triple check whether the three directed edges forming a cycle exist in *some* rotation (A->B->C->A or A->C->B->A) — since it's a directed graph, a triangle counts if there's a closed 3-cycle among the trio, not just any 3 mutual edges. Use a set keyed by the sorted triple to avoid counting the same 3 nodes twice. Write results to `/home/user/graph_results.txt` in the exact two-line format specified, and print your intermediate degree/triangle computations to stdout while developing so you can sanity-check them against the raw CSV by hand.
