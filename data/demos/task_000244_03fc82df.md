Build this as a straightforward Rust pipeline with clear stages, not one tangled loop. First, copy every `.json` file from `/home/user/remote_configs/` into `/home/user/local_configs/` (std::fs::copy over a directory read). Then parse each file with serde into a struct matching the wide schema, and validate: reject if `max_conn <= 0.0` or `timeout` falls outside `[10.0, 300.0]` (inclusive bounds — use `<` / `>`, not strict exclusion mistakes). Collect invalid file paths, sort them alphabetically, and write one per line to `/home/user/invalid_files.txt`. For valid records, group by `server_id`, sort each group by `timestamp` ascending, then for each metric compare consecutive valid entries only (skipping any timestamps you rejected — the "previous" reading is the last valid one, not necessarily the immediately prior timestamp in the file). Compute `|new-old|/old` and flag as anomaly when strictly greater than 0.5. Write anomalies to `/home/user/anomalies.csv` with the exact header given, floats formatted to exactly 1 decimal place (use `format!("{:.1}", v)`), and rows sorted by server_id, then timestamp ascending, then metric_name. Compile and actually run the binary to produce both output files.