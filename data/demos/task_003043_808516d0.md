Fix the CMake link failure by pointing the linker at the library's actual directory — add `target_link_directories(vm_emulator PRIVATE ${CMAKE_SOURCE_DIR}/lib)` (or `find_library(VMLOG_LIB vmlog PATHS ${CMAKE_SOURCE_DIR}/lib)` and link that) alongside the existing `target_link_libraries(vm_emulator vmlog)`; also ensure the runtime loader can find `libvmlog.so` at execution time (e.g. set `RPATH`/`LD_LIBRARY_PATH`, or copy it beside the binary) since it's a shared lib, not just a link-time dependency. Transcribe `/app/spec_memo.wav` with a speech-to-text tool to recover the exact accumulator rules (character-class dispatch, update operation per class, bit width, output format) rather than guessing — implement `vm_execute` precisely as dictated, defensively handling all byte values including non-ASCII/control bytes so it never crashes on adversarial fuzz input. Build with CMake into `/home/user/emulator_project/build/vm_emulator`, then self-test by running your binary against varied strings (empty, all-digit, mixed, punctuation-only, long) and sanity-checking outputs are always exactly two uppercase hex characters before considering the task done — the hidden fuzzer will compare byte-for-byte against a reference binary on 10k random inputs.