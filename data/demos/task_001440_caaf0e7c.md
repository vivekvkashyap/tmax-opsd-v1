Build this as a clear multi-pass pipeline in Rust rather than one big loop: (1) read and parse each JSONL line into a struct, handling `timestamp` as an untagged/either field — try integer epoch seconds first, then fall back to RFC 2822 parsing (chrono's `DateTime::parse_from_rfc2822`), converting both to UTC and formatting as `YYYY-MM-DDTHH:MM:SSZ`. (2) Apply validation filters: count `description` length in Unicode *characters* (`.chars().count()`, not `.len()` which counts bytes — this matters for multi-byte UTF-8 like CJK text) and reject if over 40; separately try `serde_json::from_str` on `config_payload` and reject if it doesn't parse as a JSON object. (3) For deduplication, hash `description + config_payload` with SHA-256 (sha2 crate), group entries by hash, and within each group keep only the one with the earliest parsed timestamp (ties broken by original input order — track original index). (4) Sort the surviving entries by their new ISO string ascending and write JSONL. Test incrementally: parse-only, then validate, then dedupe, then sort — verifying each stage against the input file before moving on.