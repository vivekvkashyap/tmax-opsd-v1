Work top-down through the four pieces, verifying each before wiring them together. First create the required directories. Write monitor.c to read `DATA_DIR` and `MONITOR_OUTPUT_PATH` via `getenv()`; if `MONITOR_OUTPUT_PATH` is NULL, fall back to the hardcoded default path — do this substitution in C, not by relying on the shell, since the whole point of the task is that the C binary must be self-sufficient when env vars are missing. If `DATA_DIR` is NULL, write to stderr and `exit(1)` immediately, before calling stat(). Use `struct stat` with `stat()` and check `S_ISDIR(st.st_mode)` to decide OK vs ERROR, opening the output file in append mode (`fopen(path, "a")`) so repeated runs accumulate rather than clobber. Compile with `gcc -o /home/user/bin/monitor monitor.c`. Then write run_monitor.sh with `set -euo pipefail` at the top, `mkdir -p /home/user/logs`, explicit `export DATA_DIR=...` and `export MONITOR_OUTPUT_PATH=...` before invoking the binary — this is the crux of the task: the wrapper must set these itself rather than assume they're inherited, since cron won't provide them. Write health_check.sh to grab the last line with `tail -n 1` and check it with a `case`/glob or `[[ "$line" == STATUS:\ OK* ]]`. Finally append the exact `.bash_profile` line with `>>`, not `>`, to avoid clobbering existing profile content. Make all three scripts executable.
