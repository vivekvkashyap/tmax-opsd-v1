Implement the decode pipeline in the exact reverse order of encoding, and verify it on one line by hand before trusting the rest. In analyze_payloads.cpp: read /home/user/intercepted_payloads.txt line by line, hex-decode each line into raw bytes (handle both upper/lowercase hex robustly, two chars per byte), then XOR every byte with the single key `0x2B` to recover the ASCII plaintext — print/inspect one decoded line first to confirm it matches the expected `CMD:...|TARGET_IP:...|USER:...|PASS:...|ACTION:...` delimiter structure. For redaction, parse the plaintext by splitting on `|` and then on `:` within each field so you only replace the *value* of TARGET_IP, USER, and PASS with the literal string `[REDACTED]`, leaving field names, delimiters, and other fields (CMD, ACTION) untouched — a naive regex/substring replace risks corrupting values that happen to contain similar substrings, so prefer explicit field-name matching. Write one redacted line per input line, preserving original order, to /home/user/redacted_payloads.log. Compile with g++ and actually run the binary against the real input file, then diff/inspect the output log for exact formatting (no extra whitespace, correct field order) before finishing.
