Tackle this phase-by-phase, verifying each before proceeding. Phase 1: load `/app/signal.wav` with `scipy.io.wavfile` or similar, split it into three roughly equal-duration segments (since it's three sequential tones), and run an FFT (`numpy.fft.rfft`) on each segment to find the frequency bin with peak magnitude, converting bin index to Hz via `bin * sample_rate / n_samples` and rounding to the nearest integer — don't hardcode frequency guesses, derive them from the actual FFT peaks per segment. Phase 2: in `artifact_parser.cpp`, replace the `__MAGIC_1__`, `__MAGIC_2__`, `__MAX_BUF__` placeholders with the three frequencies you measured (assign the highest of the three to MAX_BUFFER_SIZE, the other two to the magic headers, matching how the code uses each placeholder), fix the unbounded `strcpy` to a `strncpy` bounded by the buffer size, and pair every `malloc` with a corresponding `free` on all exit paths; then write a Makefile compiling with `-fPIC -shared` to `/app/build/libartifact.so`. Phase 3: in `classifier.py`, load the `.so` via `ctypes`, set `argtypes`/`restype` correctly, read the file's raw bytes, and print exactly `CLEAN` or `EVIL` based on the C parser's verdict plus a direct Python-side check for the literal substring `EXEC_PAYLOAD`. Test against `/app/corpora/clean/` and `/app/corpora/evil/` directly for 100% accuracy before writing the Phase 4 benchmark script.