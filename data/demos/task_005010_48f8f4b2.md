Treat this as three sequential fixes plus a wrapper. First read `setup.py` in `/app/semver_fast-1.0.0` and check the `sources` list against the actual `.c` filename on disk — a typo'd filename there is a common vendoring bug; correct it so `sources` points at the real source file. Second, open the C comparison function and look for fixed-size stack buffers (`char buf[16]`) combined with `strcpy`/`sprintf` of caller-supplied version strings — replace the fixed buffers with heap allocation sized to the actual input length (`malloc`/`strdup` plus `free`) or otherwise bounds-check, so long pre-release strings can't overflow. Rebuild and `pip install -e` the package, then confirm it imports and a long string doesn't crash the interpreter before moving on. Third, implement `/sort` and `/diff` on a stdlib or Flask HTTP server bound to 127.0.0.1:8888, using `semver_fast` (not `packaging`) for all comparisons/sorting so the server actually exercises your C fix — sort ascending by semantic precedence including pre-release ordering, and for `/diff` return sorted `target` entries absent from `base`. Write the hypothesis-based property test into `/home/user/test_semver.py` (must import hypothesis) as a saved file — you don't need to execute it. Start the HTTP server in the background and leave it running.
