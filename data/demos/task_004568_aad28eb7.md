Before writing any SQL, run `sqlite3 /home/user/audit.db .schema` to see the real column names/types for the employee, resource, and access-log tables rather than assuming names from the prose. Build a recursive CTE that starts from each employee and walks `manager_id` upward (UNION ALL self-join on `manager_id = employees.id`) to materialize each employee's full reporting chain including department at every level. Join that against access_logs and resources filtered to `sensitivity_level = 3`, and flag a row as a violation only when the employee's own department is not 'Compliance' AND no manager anywhere in their upward chain has department 'Compliance' either — this needs an EXISTS/aggregate check over the whole recursive chain, not just the direct manager. Execute this as one query (recursive CTE + joins) from a Python script using the `sqlite3` module, fetch rows ordered by log id ascending, and write them to violations.json as a JSON array with exactly the three required keys via `json.dump`.
