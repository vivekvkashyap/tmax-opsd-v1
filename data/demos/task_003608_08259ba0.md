Treat `/app/storage_decode` as an untrusted black box: pipe a small known input through it (`echo -n test | /app/storage_decode | xxd`) and compare input/output bytes to deduce its transformation (it's a simple byte-wise cipher) before writing your Go server, rather than assuming a specific algorithm. In your Go HTTP handler for `POST /api/v1/inspect`, read the target file's raw bytes, pipe them through `/app/storage_decode` via `exec.Command` with the file contents written to the subprocess's Stdin and its Stdout captured (`cmd.StdinPipe`/`cmd.Output()`), then parse the decompressed buffer per the strict binary layout: first 4 bytes as a big-endian magic number (verify it equals the expected constant as a sanity check), next 4 bytes as a big-endian length N, then read exactly N bytes as the JSON payload (`encoding/binary.BigEndian.Uint32`). Parse that JSON, compute `usage_percent = used/(used+free)*100` rounded to 2 decimals, and return it in the exact response shape given. For the audit log, use `os.OpenFile` with `O_APPEND|O_CREATE|O_WRONLY` plus a `sync.Mutex` (or per-write file lock) around each write so concurrent requests don't interleave — append-mode alone is not sufficient for larger writes under concurrency in all filesystems, so serialize writes explicitly. Write the header row only if the file doesn't already exist. Start the server and `curl` it yourself to confirm the full round-trip before finishing.