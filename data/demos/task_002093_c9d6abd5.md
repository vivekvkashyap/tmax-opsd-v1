First diagnose the linking failure: build with cgo LDFLAGS pointing at the lib directory (e.g. `#cgo LDFLAGS: -L${SRCDIR}/lib -lvidproc`) and fix the runtime lookup by setting rpath (`-Wl,-rpath,$ORIGIN/lib` or exporting LD_LIBRARY_PATH) so the built binary finds libvidproc.so at run time, not just link time. For the ABI mismatch, read vidproc.h carefully: it uses `#pragma pack(push,1)`, meaning no padding between struct fields, so the Go struct mirroring MetadataPayload must NOT let Go's default alignment insert padding — use exact field types/order (uint32, uint8, [32]byte, uint16) and either add explicit padding removal or use cgo's `C.MetadataPayload` type directly rather than hand-redeclaring a mismatched Go struct. Extract frames with ffmpeg (`-vf fps=<rate>` or all frames to PNG/rgb24), feed each into process_frame via cgo, and collect the decoded semver strings. Implement SemVer comparison per spec: compare major.minor.patch numerically, and treat any version with a pre-release suffix (`-alpha`, `-rc.1`, etc.) as *less than* the same version without one, with pre-release identifiers compared per dot-separated component (numeric ids compared numerically, alphanumeric lexically). Write the sorted list as a flat JSON array to sorted_versions.json. Spot-check your decode+sort pipeline against several frames manually before trusting the 95% accuracy threshold.