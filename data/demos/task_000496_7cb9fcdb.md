Treat this as two separable problems: fix the packaging bug first, then automate the interactive CLI. For the package at `/app/py-restore-util-1.2.0`, inspect `pyproject.toml`'s `[project.scripts]` block closely for a malformed string (e.g. a missing closing quote) that breaks TOML parsing/install — fix the syntax, then also grep the source (likely `restore_util/cli.py`) for an environment-variable gate such as `REQUIRE_SSH_KEY` that raises on startup; remove or bypass that check so the CLI can run without real SSH keys. Install with `pip install --user /app/py-restore-util-1.2.0` and confirm the `restore-util` command runs and reaches its interactive password prompt before writing any automation. For `/home/user/auto_restore.py`, spawn `restore-util extract <BACKUP_ID>` as a subprocess with a PTY (via `pexpect` or `pty`/`subprocess` with a pseudo-terminal, since plain pipes often won't trigger the same buffering as an interactive prompt), wait for the literal prompt text `Archive Password: `, send `OperatorDeploy99!\n`, then capture the remaining stdout, parse out the file listing, and print strict JSON `{"restored_files": [...]}` with the list sorted and no other output. Test end-to-end against several different backup IDs before trusting it, since it will be fuzzed with many random IDs.
