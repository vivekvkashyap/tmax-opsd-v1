Implement this as a single streaming pass with an explicit sliding window, and be precise about off-by-one and Unicode handling since those are the likely failure points. Read app_logs.jsonl line by line, parsing each JSON object and appending its processing_time_ms to a list; maintain a window of exactly the previous 30 entries (not including the current one) — e.g. once you've processed indices 0..29 you have a full window, so the check begins at index 30. At each qualifying index, compute the mean and *sample* standard deviation (divide by n-1, i.e. `statistics.stdev`, not `pstdev`) of the current window before appending the new value to it. Flag the first entry where its processing_time_ms strictly exceeds mean + 5*stdev, then stop scanning immediately (don't keep looking for a "worse" one later). For the flagged entry's message field, compute char length as Python's `len(message)` (counts Unicode code points correctly since Python str is already code-point based) and byte length as `len(message.encode('utf-8'))` — do not assume ASCII or use raw file byte offsets. Write index, timestamp, message_char_len, message_byte_len to anomaly.json in the exact schema given, using only stdlib (json, statistics).