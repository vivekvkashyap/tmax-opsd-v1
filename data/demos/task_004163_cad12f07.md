Do frame extraction and deletion first, and confirm the on-disk frame count/naming matches spec before writing any server code. Use an ffmpeg filter chain like `-vf "scale=64:64,format=gray"` with `-pix_fmt gray` and output to `/home/user/frames/frame_%03d.pgm` — verify a resulting file actually is a valid binary P5 PGM (magic number, dimensions, maxval, then raw 64*64 grayscale bytes) by inspecting the header bytes yourself. Then delete every frame whose numeric ID (parsed from the filename, not the loop counter) is a multiple of 5. Build `serve_targets.c` as a standard blocking TCP server (`socket`/`bind`/`listen`/`accept` loop, one connection at a time is fine): read a line, parse the integer frame ID, try to `fopen` the corresponding PGM path — on failure send exactly `NaN\n`; on success skip the PGM header (read past the three header lines/whitespace correctly, don't assume a fixed byte offset since header line lengths can vary) then read all 4096 pixel bytes, average them, and plug that average into the given closed-form Gaussian posterior-mean formula using the stated prior/observation variances, printing the result with `%.2f\n`. Keep the accept loop running in the background so the server stays reachable for repeated test queries.