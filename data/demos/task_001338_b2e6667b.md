Parse the multi-line log with `awk` or a state-machine loop, splitting records on `---`: extract each `Ticket:` value, collect lines under `Files:` starting with `- ` until the next field, and check `Status:` — skip the record entirely if status is `Rejected`. For binary header validation, read exactly the first 4 bytes without loading the whole file (`head -c 4 "$file" | xxd -p`), compare (case-insensitive hex) against `cffaedfe`; use `Missing` if the file doesn't exist, `Corrupt` if bytes mismatch, `Valid` otherwise — check existence first with `[ -e "$file" ]`. Accumulate `Ticket,Filename,Status` rows for every file under non-Rejected tickets, then sort with `sort -t, -k1,1 -k2,2` before writing, prepending the exact header. Make the script executable and actually run it to produce the CSV, verifying missing/duplicate filenames render correctly.