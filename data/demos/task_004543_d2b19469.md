Process the video frame-by-frame with OpenCV (`cv2.VideoCapture`), computing the mean of R/G/B across the *entire* frame (not a sub-region, unlike similar LED-detection tasks) for each 0-indexed frame, flagging it as an alert when avg-R > 150 and avg-G < 50 and avg-B < 50 — remember OpenCV frames are BGR, so index channels carefully. Append `ALERT: Frame <N>` lines to the log as you go. Don't hardcode the aggregator's port — actively discover it at runtime with `ss -ltnp` or `netstat -tlnp` (or `lsof -i -P -n | grep LISTEN`) filtered to localhost, parse out the port number, then POST `{"frame": N}` as JSON to `http://127.0.0.1:<port>/alert` for each detected alert (via `requests` or `urllib`). Write `watchdog.sh` as a `while` loop that runs `python3 monitor.py`, checks `$?`, and only breaks the loop when the exit code is 0, restarting on any nonzero code (simulating crash recovery). Set `/home/user/alerts.log` to mode `600` — do this as a final `chmod 600` after monitor.py finishes writing (or have monitor.py `os.chmod` it after opening), not before, so writes aren't blocked.
