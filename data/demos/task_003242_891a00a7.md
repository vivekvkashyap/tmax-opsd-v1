Process the log file line by line with `json.loads` per line, and pull the token out of `auth_header` by splitting on the literal `"Bearer "` prefix, then splitting the remaining JWT string on `.` into exactly three parts — wrap this in a try/except since some lines will have malformed tokens (like `invalid_token_format`) that must simply be skipped, not crash the script. For the header part, base64url-decode manually: since JWTs strip padding, pad the string back out before decoding (e.g. add `=` characters until length is a multiple of 4, or use `base64.urlsafe_b64decode(s + '=' * (-len(s) % 4))`), then `json.loads` the decoded bytes and check `header.get("alg", "").lower() == "none"` for case-insensitivity. Only for matches, decode the payload segment the same padded way, parse its JSON, and pull out `user_id`. Append `(timestamp, ip_address, user_id)` tuples in file order (no sorting) and write them with Python's `csv` module using the exact header `timestamp,ip_address,user_id`. Run the script and diff your CSV against a hand-check of which log lines you expect to trigger before considering it done.
