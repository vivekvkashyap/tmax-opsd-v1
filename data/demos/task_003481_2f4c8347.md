Fix the build first, since nothing else can be tested until it links. In CMakeLists.txt add `find_package(OpenSSL REQUIRED)` and link the validator target against `OpenSSL::Crypto` (jwt-cpp's HS256 support needs libcrypto for HMAC) — confirm with `cmake -B build && cmake --build build` producing no linker errors before touching main.cpp. Then implement validation in main.cpp using jwt-cpp: decode the token, and explicitly check the algorithm header equals `HS256` (reject anything else including `none` before even attempting verification — don't just let a mismatched-algorithm token fail open), verify the signature against the exact secret `ssh-audit-secret-2024`, check the `exp` claim against current time, and check the payload's `role` claim equals exactly `auditor` (exact string match, not substring/case-insensitive). Print exactly `VALID` or `INVALID` with a trailing newline and nothing else. Test incrementally against a couple of hand-crafted tokens before running the corpora loop, and make sure every one of the four security checks is independently enforced — corpora will include tokens crafted to fail exactly one check while passing all others.