Read the existing buggy script fully before editing so you preserve the scaling/PCA calls exactly. Fix issues in this order: (1) after loading the CSV with pandas, impute NaNs using each column's mean computed from the non-null values — `df.fillna(df.mean())` — and do this before the `StandardScaler`/PCA steps, not after; (2) switch the matplotlib backend to a non-interactive one by calling `matplotlib.use('Agg')` before importing `pyplot` (or set it via `matplotlib.pyplot.switch_backend`), and replace `plt.show()` with `plt.savefig('/home/user/pca_plot.png')`; (3) after computing `components` from `pca.fit_transform`, build a DataFrame with columns exactly named `PC1` and `PC2`, round the values with `.round(4)`, and write with `to_csv(..., index=False)` so the header line reads `PC1,PC2`. Do not alter the `random_state=42` on PCA or the StandardScaler logic already present — those must stay identical to get matching output values. Run the script and confirm both output files are created, output.csv has exactly 50 rows with no NaNs, and the values look consistent with mean-imputed, scaled PCA.