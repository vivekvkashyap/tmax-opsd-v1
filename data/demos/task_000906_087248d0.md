Treat this as a graph-memoization problem: parse each line of `/home/user/build_graph.txt` by splitting on `:` to get the target name, then on `|` to separate the dependency list from the command (the command is everything after the single space following `|`, verbatim — do not trim trailing content). Build a map from target name to (deps, command). Write a recursive function `compute_key(target)` that: resolves each dependency's key first (recursing, and memoizing results to avoid recomputation), sorts the dependency names alphabetically, concatenates the raw `COMMAND` string followed by each dependency's already-computed hex hash in that sorted order (no separators), and SHA-256-hashes the resulting string, hex-encoding lowercase. The most common mistakes are: hashing dependency names instead of their computed hashes, forgetting to sort dependencies alphabetically (order matters for the hash), or using the command text with incorrect whitespace. Use the `sha2` crate (or implement SHA-256) in a proper `cargo new` project at `/home/user/cache_builder`, run it, and write only the 64-char lowercase hex digest for `release_bin` to `/home/user/cache_key.txt` with no trailing content beyond a possible newline.
