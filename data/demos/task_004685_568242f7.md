Build this as a straightforward, testable logging script rather than guessing rotation math. Write `/home/user/vnc_monitor.py` using `socket.socket(...).connect_ex(('127.0.0.1', 5900))` (or a try/connect with `settimeout(2)`) to test connectivity, and configure `logging.getLogger('vnc_logger')` with a `logging.handlers.RotatingFileHandler('/home/user/vnc_logs/monitor.log', maxBytes=60, backupCount=3)` and formatter string exactly `%(asctime)s - VNC_STATUS - %(message)s`. Log the literal message `SUCCESS` or `FAILED` (no extra text) based on the connection result. Before wiring rotation, manually verify one log line's byte length so you understand how many runs are needed to cross the 60-byte threshold multiple times — the task expects rotation to actually fire, producing `.log`, `.log.1`, `.log.2`, `.log.3`. Guard against duplicate handlers being attached if the module were re-imported (check `logger.handlers` before adding a new one) even though here it just runs as `__main__` each time. Finally, run the script exactly 5 times in a simple `for i in $(seq 1 5); do python3 /home/user/vnc_monitor.py; done` loop and confirm all four log files exist with lines matching the expected timestamp/status format.
