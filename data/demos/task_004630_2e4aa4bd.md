Work each phase to completion before the next, since later phases depend on earlier findings. Phase 1: transcribe `/app/intercepted_call.wav` (speech-to-text, or `ffmpeg` to convert format first) and extract the literal passphrase spoken in context — don't guess. Phase 2: try that passphrase against each cert/key in `/home/user/audit/crypto_assets/certs/` (e.g. `openssl` to see which key it unlocks and which chain it signs), and separately `sha256sum` the passphrase yourself and grep for that digest in the comment fields of `.pub` files under `.../ssh/` to find the rogue key. Phase 3: read the Flask backend and Node frontend for a file-serving route that concatenates user input into a filesystem path without sanitizing `../` — name that vulnerability class. Phase 4: build `filter.py` detecting that exact payload pattern (path-traversal sequences, URL-encoded variants) against the adversarial corpora, testing both evil/ and clean/ examples. Phase 5: assemble the JSON report strictly from what you discovered in phases 1-3, not assumed values.