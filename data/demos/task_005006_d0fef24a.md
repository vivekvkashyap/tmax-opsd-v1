Replace the failing Cholesky path in `main.go` with an SVD-based regularization instead of trying to patch Cholesky directly. Use `gonum.org/v1/gonum/mat`'s `SVD` type: call `Factorize` on the loaded matrix (make sure it's treated as symmetric since it's a covariance matrix), then extract the singular values with `.Values(nil)` and the U/V factor matrices with `.UTo`/`.VTo`. Iterate over the singular values and clamp any value strictly less than 0.1 up to exactly 0.1 (leave larger values untouched). Reconstruct the regularized matrix as U * Σ' * Vᵀ using `mat.Dense` matrix multiplication (build a diagonal matrix from the modified singular values, multiply U by it, then by Vᵀ). Compute the trace by summing the diagonal entries of the reconstructed matrix (`mat.Trace` or manual loop over `.At(i,i)`). Format the result to exactly 4 decimal places with `fmt.Sprintf("%.4f", trace)` and write it to `/home/user/trace.txt`. Run `go run main.go` afterward and confirm the file was actually written before finishing.
