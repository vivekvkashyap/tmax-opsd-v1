Build this as three composable, independently-testable pieces in C++ with Eigen. First, implement Random-Walk Metropolis-Hastings for the given 2D Gaussian target: at each step propose `x_new = x_old + N(0, Sigma_prop)`, compute the log-density ratio using the target's Cholesky factor (Eigen's `LLT`) rather than `.inverse()`/`.determinant()` — solve `L*y = (x-mu)` via forward substitution to get `Sigma^-1(x-mu)` cheaply and stably, and get `log|Sigma|` as `2*sum(log(diag(L)))`; accept/reject via the standard MH ratio. Generate every normal random variate through the exact Box-Muller transform on `std::uniform_real_distribution<double>(0,1)` fed by a single `std::mt19937(42)` engine, used consistently for both the proposal noise and the initial state, so runs are bit-reproducible. Run 10,000 burn-in iterations you discard, then 100,000 sampling iterations. For the mean/covariance of the kept samples, update running estimates with Welford's algorithm (single-pass, no naive E[X^2]-E[X]^2 and no need to store all samples) — extend the standard 1D Welford update to the outer-product form for the covariance matrix. Finally compute the closed-form multivariate-normal KL divergence formula given in the task using your empirical (mu_hat, Sigma_hat) versus the true (mu, Sigma), and dump both plus the KL value as JSON to `/home/user/results.json`.