Reverse-engineer the binary format empirically rather than guessing: generate a handful of your own small binary files with known byte layouts, run `/app/legacy_parser <file>` on each, and compare the JSON output to the bytes you wrote to pin down field order, sizes, and endianness (magic bytes, a count field, then per-entry filename-length + filename + a little-endian size field, etc.) — also use `xxd`/`hexdump` on any provided clean corpus files in `/app/corpus/clean/` to cross-check your inferred layout. Once the format is nailed down, write `/home/user/converter.go` to read the whole input file, validate strictly before trusting any offset: check magic bytes first, then for each entry ensure declared lengths don't run past the actual file size (bounds-check every read), and reject any filename containing `..`, `/`, or `\` (path traversal). On any validation failure, exit non-zero and make sure no output file is ever created (don't create-then-delete; validate fully before writing). On success, build the JSON array matching the oracle's exact field names/order and write it atomically: write to a temp file in the same directory as the target (e.g. `<output>.tmp`), then `os.Rename` it into place, so a crash mid-write never leaves a partial `<output_json_file>`. Test against both `/app/corpus/clean/` and `/app/corpus/evil/` before finishing.
