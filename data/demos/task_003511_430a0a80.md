First inspect a sample of the `.log` files under `/home/user/app_logs/` to confirm the exact `Token: <alphanumeric>` pattern before writing your regex — use something like `re.compile(r'Token: [A-Za-z0-9]+')` and replace with `Token: [REDACTED]`, counting each match as you go so you can total redactions across all files (don't recompute the count separately from the replacement pass, or you risk a mismatch). Iterate every `*.log` file in the source dir, write the redacted content to a same-named file under `/home/user/secure_audit/` (create that dir first). After writing all files, set permissions explicitly with `os.chmod`/`chmod`: `700` on the `secure_audit` directory itself and `600` on each redacted log file inside it — do this as a final step so file creation doesn't reset directory permissions in an unexpected order. Sum the per-file redaction counts and write a single line `Total Redactions: <N>` to `/home/user/audit_report.txt`, with no extra whitespace or trailing content. Double check files with zero tokens are still copied over unchanged.