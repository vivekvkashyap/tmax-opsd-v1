Build this as a straightforward, self-checking numerical script rather than trying to be clever. For each N, construct the Hilbert matrix directly from its closed-form entry `1/(i+j+1)` (0-indexed) using `numpy.zeros`/broadcasting, rather than trusting an external constructor which may use a different indexing convention. Fix `x_true` as a ones-vector and derive `b = H @ x_true` — never hardcode `b` — so the "true" solution is exactly known and error is computed the same way the verifier will. Use `numpy.linalg.cond(H)` for the condition number and `numpy.linalg.solve(H, b)` for the solve; do not use a manual Gaussian elimination or `inv(H) @ b`, since that can introduce different rounding than what a reference implementation expects. Compute relative L2 error as `norm(x - x_true) / norm(x_true)` using `numpy.linalg.norm`. Serialize the JSON with plain Python floats (cast with `float(...)`, since raw numpy scalars aren't JSON-serializable) keyed by the string of N. For the plot, use `plt.subplots(1, 2)`, set `yscale('log')` on both axes, label them clearly, and save with `savefig` before exiting — run the script yourself and sanity-check that condition numbers grow roughly exponentially with N, which is the expected Hilbert-matrix signature.
