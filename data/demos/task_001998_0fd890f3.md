Follow the pipeline stages in order, since each output feeds the next. Load the JSONL with one `json.loads` per line, drop records whose `description` is missing/null/empty-after-strip. Clean text with straightforward normalization: lowercase, replace punctuation characters with spaces (e.g. `re.sub(r'[^\w\s]', ' ', text)`), then collapse repeated whitespace with `re.sub(r'\s+', ' ', text).strip()`. Fit a single `TfidfVectorizer` across all cleaned descriptions together (not per-product) so vectors are comparable, then compute pairwise cosine similarity with `sklearn.metrics.pairwise.cosine_similarity` on the full TF-IDF matrix. For each target product ID, look up its row, sort all other products by similarity descending, exclude the product itself, take the top 3 by similarity score, then re-sort those 3 IDs alphabetically for the final output (sorting must happen after selecting top-3-by-similarity, not before). Write the result as a JSON object mapping each target ID to its 3-element sorted list to `/home/user/recommendations.json`, and double check the keys match the exact target IDs requested.