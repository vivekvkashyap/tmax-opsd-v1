First get `patool` installable: search `/app/patool-1.12/patoolib/util.py` for a syntax error (likely a function signature with a stray trailing token like `->` and no body/return type) that would raise `SyntaxError` on import — fix the signature so it parses, then `pip install -e /app/patool-1.12`. For the converter, don't just call the extraction function and hope it's safe — inspect the archive's member list *before* extracting anything: for a zip, iterate `zipfile.ZipFile(path).namelist()` and reject (print `MALICIOUS`, exit 1, write nothing) if any entry is an absolute path (starts with `/`) or, after `os.path.normpath`, resolves outside the target directory (e.g. starts with `..` or contains `../` that escapes the root) — checking for the raw substring `"../"` is a good first filter but normalize the path too since traversal can be encoded differently. Only after confirming every member is safe should you extract to a temp dir, concatenate each file's text under a `--- <basename> ---` header, and write via a temp file + `os.rename` (atomic) into `<output_dir>/converted.txt`, then exit 0. Test against both `/app/corpora/evil/` and `/app/corpora/clean/` to confirm no output files leak on malicious input.
