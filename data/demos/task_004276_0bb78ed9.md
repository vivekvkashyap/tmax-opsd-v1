Break this into three independently testable stages. Stage 1: extract frames with `ffmpeg -i /app/audit_log.mp4 -vf fps=1 frame_%04d.png`, then for each frame read only the top-left 10x10 pixel block (e.g. with Python/PIL `img.crop((0,0,10,10))` or ImageMagick) and test that all pixels equal pure white (255,255,255) — don't approximate with "mostly white". Map ffmpeg's 1-indexed frame filenames to 0-indexed event indices carefully, and write them to `/home/user/event_indices.txt` sorted numerically, one per line. Stage 2: parse the TSV DAG into an adjacency structure and do a proper topological sort (Kahn's algorithm or DFS) before resolving each triggered node's dependency order; then write a small C++ program that reads `/app/template.conf` and substitutes each placeholder in dependency-resolved order. Stage 3: write `config_sanitizer` to decode input as UTF-8 codepoints (not bytes) so you can detect the U+202E RLO character correctly, and scan for `$`, backtick, `|`, `;` while tracking single/double-quote state so metacharacters inside quotes are ignored; exit precisely 0/"ACCEPT" or 1/"REJECT". Test both stages against your own constructed clean/malicious sample files before trusting it.
