Install `libhdf5-dev` and `libfftw3-dev` (plus `h5py`, `numpy`, `matplotlib` for Python) before compiling. In the C program, read the `(1000, 1024)` float32 dataset via the HDF5 C API in one call, then for each signal in strict order 0..999: apply the Hanning window `0.5*(1-cos(2*pi*n/1023))` elementwise, run `fftwf_plan_dft_r2c_1d` (create the plan once outside the loop and reuse it with `fftwf_execute`, only swapping the input buffer, for correctness and speed), and compute power per bin `k=0..512` as `re²+im²`. The critical correctness point is the accumulation: use Kahan summation with `double` compensation variables (`sum`, `c`), and accumulate in the exact nested order specified — bin 0..512 within each signal, then signal 0..999 — since Kahan summation's result depends on summation order and the verifier recomputes the same order in Python/NumPy for comparison. Print/write the final total formatted to exactly 6 decimal places matching `Total Power: <value>`. Separately, write the Python plotting script using the same Hanning window and `np.fft.rfft`, average power across all 1000 signals, and save a plot to the given PNG path.