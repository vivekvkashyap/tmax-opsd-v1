Handle the three sub-tasks independently. For the vendored package, open `/app/vendored/auth_parser-1.2.0/auth_parser/core.py` and look for a boolean-context expression using a bitwise operator where a logical one is needed (`&` between boolean-like operands raises TypeErrors / wrong short-circuiting vs `and`); fix the operator, add any missing stdlib import (e.g. `hashlib`), then run `python3 -m unittest discover` until the suite passes. For the password, extract the MD5 hash from `/home/user/audit_logs/access.log` and crack it by hashing candidate 5-letter lowercase English words until one matches the hash (don't hardcode a guess — compute and verify), then append it to `/home/user/audit_logs/cracked_pass.txt`. For the evaluator, since the oracle's logic is opaque, treat `/opt/oracle/perm_eval_oracle` as a black box: feed it many varied byte-stream input pairs, capture outputs, and infer the algorithm empirically — implement your own script from that inferred rule and fuzz-test it against the oracle on fresh random inputs to confirm byte-for-byte matches before submitting.