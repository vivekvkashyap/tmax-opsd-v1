Work through this in three independent, individually-testable stages. First, fix the vendored package: open `py_auditor/hasher.py`, swap the `hashlib.md5` call for `hashlib.sha256` while keeping the same "open file, read bytes, hexdigest" shape, then `pip install -e /app/vendored/py-auditor-1.2.0` (or equivalent) and confirm `import py_auditor; py_auditor.get_file_hash(...)` returns a 64-character hex string on a test file. Second, build `server.py`: check file permissions with `os.stat(path).st_mode & 0o777` and compare strictly to `0o600` before doing anything else — return 403 with the exact error JSON on mismatch; only on exact 0600 read the file, hash it via the fixed package function, then AES-GCM encrypt the hex string using the `cryptography` library's `AESGCM` class with the key loaded from `/app/aes_key.bin` and a fixed 12-byte zero IV — note `AESGCM.encrypt` already appends the tag to the ciphertext, so just base64-encode its return value directly (don't append the tag yourself again). Run the server backgrounded and curl-test both permission cases manually before moving on. Third, configure `sshd`: generate host keys if missing, write a config forcing `PasswordAuthentication no` and `Port 2222`/listen on 127.0.0.1, append `/app/auditor_id_rsa.pub`'s contents to the `user` account's `~/.ssh/authorized_keys` with correct 600/700 permissions on the `.ssh` dir and file, then start `sshd` with that config and test locally with `ssh -i /app/auditor_id_rsa -p 2222 user@127.0.0.1` to confirm key auth works and password auth is refused before finishing.
