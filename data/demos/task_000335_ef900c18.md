Work through this as three sequential fixes. First, run `make` in `/app/artifact_pca` to see the actual linker error — a math-function link failure means the `Makefile`'s gcc invocation is missing `-lm`; add it to the link step (order matters: `-lm` should come after the object files). Second, once it compiles, feed it a sample `EMBEDDING:...` line and observe it silently failing to parse floats — inspect `reducer.c`'s `scanf` format string; float conversions require `%f` specifiers with `float`-typed destination variables, not `%d` (which is for integers and will corrupt/mis-parse). Fix the format string and re-run to confirm valid embeddings are now accepted end-to-end. Third, wrap the now-working `reducer` binary as a persistent network service using `socat TCP-LISTEN:8888,fork,reuseaddr EXEC:/app/artifact_pca/reducer &` (the `fork` option is essential so each connection gets its own process instance, and backgrounding/`nohup`/`disown` keeps it alive after your shell session ends). Test the full pipeline yourself with `echo "EMBEDDING:1,2,3,4" | nc localhost 8888` (or similar) before considering the task done, confirming the projection output matches the tool's own computed logic.