Fix the C bug first: the code writes `result[num_nodes] = -1` as a sentinel but only allocates `num_nodes * sizeof(int)`, so allocate `(num_nodes + 1) * sizeof(int)` instead — then recompile with the exact `gcc -shared -o libgraph.so -fPIC libgraph.c` command and confirm it no longer segfaults on a larger synthetic graph before wiring up Python. In the Python integration script, read all JSON files from the API directory, collect the *union* of every `id` and every string appearing in any `requires` list (some IDs may only appear as dependencies), sort that set alphabetically to build the id→int mapping, and be careful about edge direction: if task V "requires" U, the edge must be U→V (U executes first), so `edge_u` holds dependency sources and `edge_v` holds the dependents. Load the `.so` with `ctypes.CDLL`, declare `restype = ctypes.POINTER(ctypes.c_int)` and correct `argtypes` (ints and `ctypes.POINTER(ctypes.c_int)` arrays via `(ctypes.c_int * n)(*data)`), then iterate the returned pointer until you hit `-1`, mapping ints back to task-id strings, and join with commas into the log file.
