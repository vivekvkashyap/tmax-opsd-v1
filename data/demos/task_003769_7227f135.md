Tackle the four parts in dependency order since the server needs both the recovered key and the recovered DB to pass verification. First, OCR `/app/ticket_screenshot.png` with `tesseract` and read the printed sentence carefully to find the `API_KEY=...` token exactly as written — don't truncate or alter its characters. Second, recover the SQLite data: since `metrics.db.corrupt` is damaged but `metrics.db-wal` still holds the committed pages, place both files together with the correct base name (SQLite pairs a `.db` and its `-wal` file by matching stem) and try `sqlite3` recovery approaches — e.g. copy the corrupt file to a fresh name, ensure the WAL sits alongside it, open it and force a checkpoint (`PRAGMA wal_checkpoint`), or use `.recover`/`.dump` and re-import into a clean new database file at `/app/data/metrics_recovered.db`; if direct open fails, grep/strings the WAL file for the literal row data as a fallback. Third, debug the intermittent crash by actually reproducing it — hit the server with several sequential requests (a loop of `curl` calls) and observe which request number crashes it, then read the handler code around request counting/history tracking for an off-by-one or out-of-range index bug and fix it properly rather than papering over the symptom. Finally wire the recovered API key and DB path into the server, start it bound to `0.0.0.0:8080`, and verify both endpoints (with and without the Authorization header) respond as specified before leaving it running.