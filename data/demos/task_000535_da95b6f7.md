Debug the three named bugs one at a time and validate each independently before wiring them together. For the awk precision bug, don't rely on default float printing/accumulation in awk for the comparison; instead accumulate durations using a higher-precision approach (e.g. `printf("%.6f", sum)` throughout, or drive the cumulative sum and threshold comparison in a tool with better precision control) and confirm against manually summing the `ffprobe`-extracted `pkt_duration_time` values which sum exceeds 5.500000 and at which record. For the indexing bug, remember `awk`'s `NR` is 1-based while the database's `frame_idx` is 0-based — the critical frame index used downstream must be `NR - 1` of the frame where the cumulative sum first strictly exceeds the threshold. For the SQL bug, use `frame_idx > <critical_index>` (strictly greater, not `>=`) against the `events` table, and make sure your WHERE clause and any date/string handling isn't accidentally excluding rows — inspect actual `timestamp_utc` values in the DB rather than assuming a format. Use `sqlite3 -json` or `jq` to produce a clean JSON array for incident_data.json. For the server, use `python3 -m http.server`-style custom handler or `socat`/a tiny Python HTTP server checking the `Authorization` header exactly, serving the JSON file's exact bytes, and leave it running with `&`/nohup on 0.0.0.0:8080.