This is black-box reverse engineering plus reimplementation, not decompilation. Use `strings`, `file`, `ldd`, and `objdump`/`nm` on `/app/query_oracle` to see what it links against (look for `libsqlite3`) and any embedded SQL text or format strings — SQL queries and printf format strings often show up verbatim in `strings` output. Confirm behavior empirically: generate several small SQLite databases matching the given `sensors`/`events` schema with varied data (including edge cases like a single event per sensor, ties in `ts`, and negative/zero metrics) and run the oracle against each to observe exact output formatting (delimiter, decimal places, line endings, column order). From the observed query pattern (a windowed rolling average partitioned by sensor, ordered by ts) and the printf-style output, write `/home/user/solution.c` using `libsqlite3` to run the equivalent SQL (or replicate the window function's rolling-average logic in C if window functions aren't directly available in the linked sqlite version) and format output identically. Compile with `gcc -O2 solution.c -lsqlite3 -o solution`, then re-run both the oracle and your solution against several freshly generated random databases and diff the outputs byte-for-byte before considering it done — exact floating point formatting (%.2f) and anomaly-flag boundary conditions are the most common source of mismatches.
