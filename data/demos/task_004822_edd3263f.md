This is a BFS shortest-path problem that must be solved without any general-purpose scripting language — only bash builtins, awk, sed, grep, and coreutils. The standard approach: use `awk` to build an adjacency structure from the TSV (associative array keyed by citing paper, mapping to a list of cited papers), then implement BFS in `awk` itself (awk supports loops, arrays, and queues via indexed arrays) starting from P001, tracking a `visited` array and a `parent` array (predecessor pointer for each discovered node) rather than storing full paths per node — this keeps memory/logic simple. Stop as soon as P099 is dequeued/discovered. Reconstruct the path by walking backward from P099 through the `parent` array until you reach P001, then reverse it. Finally, format the reversed node list as JSON manually (e.g. build a comma-separated quoted list and wrap it in `{"path": [...]}`) using `printf`/`awk` string concatenation, and write it to `/home/user/path.json`. Double check there are no trailing commas or unescaped characters in the hand-built JSON.