Work through this as a real restore-and-debug exercise. Extract the archive first (`tar xzf /home/user/backup_archive.tar.gz -C /home/user/restore/`), then grep the extracted `.cpp` for the hardcoded old socket path and replace every occurrence with the new one — check both the `bind`/`connect` string literal and any comments/constants so nothing stale remains, since a later check greps the source for the new path only. Compile with `g++` into the exact required binary name/location. For log rotation, write a logrotate stanza scoped to the specific log file path with `hourly`, `rotate 5`, `compress`, and `missingok` each as separate directives inside the block. For the bridge, use `socat TCP-LISTEN:9999,reuseaddr,fork UNIX-CONNECT:/home/user/app.sock` running in the background, and start the daemon in the background first so the socket exists before socat binds to it. Run the test script only after both background processes are confirmed listening (a short sleep after launch, or check with `ss`/`lsof`), then sleep again after the test so the daemon has time to flush its write before you kill both processes and drop the final marker file.
