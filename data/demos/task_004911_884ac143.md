Build this as three separable pieces and verify each independently before wiring them together. First, write `forecast.py` against the CSV alone: load `/home/user/planner/data/history.csv`, fit a simple linear trend (e.g. least-squares on day vs cpu_load) — the historical data already has weekend effects baked in, so don't try to "de-weekend" it, just fit the raw trend. Second, handle the image separately: run OCR (pytesseract/Pillow) on `/app/dashboard_multipliers.png`, print the raw extracted text first so you can see exactly how the Growth Factor and Weekend Discount are phrased, then parse those two numbers out with a regex rather than hardcoding guesses — OCR text can have noisy spacing/punctuation, so make the regex tolerant. Third, apply the two extracted multipliers only to the future days (101-130): multiply the extrapolated trend by the growth factor for every future day, then additionally multiply by the weekend discount specifically for days where `day % 7` is 6 or 0 (day 1 = Monday). Write output with exact columns `day,predicted_load` to `/home/user/planner/output/forecast.csv`. Finally wrap venv creation, env vars, pip installs, and script execution into `deploy.sh` with `set -euo pipefail` so any failure aborts the pipeline, and create all needed directories up front with correct permissions.
