Scaffold with `cargo new tracker` inside `/home/user`, then build the traversal carefully around the two correctness hazards. For symlink-loop safety, walk the directory tree while tracking visited paths by canonicalized path or `(dev, inode)` pair (use `std::fs::symlink_metadata`/`canonicalize` and a `HashSet`) so that following the loop-creating symlink back into an already-visited directory is detected and skipped rather than recursed into infinitely. For incremental filtering, only process `.conf` files whose modification time (via `metadata().modified()`, converted to Unix seconds) is strictly greater than the `<timestamp_seconds>` CLI argument — parse that argument as the third positional arg. For redaction, process line-by-line and replace only lines that start exactly with the literal `PASSWORD=` prefix, rewriting them to `PASSWORD=REDACTED` and leaving every other line byte-for-byte unchanged. When writing output, mirror the relative path under `<dest_dir>` and create intermediate directories with `create_dir_all`. Build with `cargo build`, then actually run the binary against the given source/dest/timestamp arguments and inspect the resulting backup directory to confirm only newer files were redacted and copied, and older files were skipped.