Debug the script methodically rather than rewriting from scratch: first run it as-is to see the actual crash traceback, which will point you to the `json.loads` call failing on a malformed metadata string. Inspect the raw `metadata` column values directly (`sqlite3 catalog.db "SELECT id, metadata FROM products"`) to see exactly what's stored — you'll find at least one row uses Python-repr-style single quotes instead of valid JSON double quotes, and possibly a row with genuinely truncated/corrupted JSON. Fix the SQL query to filter `WHERE status = 'active'` at the database level (don't filter in Python after the fact) so deleted/inactive rows never even reach the parsing step. For the metadata parsing, add a fallback: try `json.loads` first, and if it raises, attempt a safe repair — e.g. `ast.literal_eval` handles Python-dict-style single-quoted strings correctly — but only apply this fallback to rows that are actually active and genuinely malformed in the single-quote way; don't silently swallow real corruption. Run the fixed script once and inspect `/home/user/catalog_output.json` to confirm only active products appear and each `meta` field is a proper nested object, not a string.
