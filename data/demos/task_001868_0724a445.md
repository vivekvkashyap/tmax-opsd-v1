Work in stages and verify each before moving on. First reorganize: `mkdir -p cmd/secapi cmd/cli pkg/secsum bin`, `git mv`/`mv` `api.go`→`cmd/secapi/main.go`, `secsum.go`→`pkg/secsum/secsum.go`, `cli.go`→`cmd/cli/main.go`; update `package` declarations and import paths (use a `go.mod` module name and import `<module>/pkg/secsum` from both mains) so `go build ./...` succeeds. Second, fix the checksum algorithm: don't try to compile/run the Rust file (it doesn't build) — instead read it purely as a reference for the *math* (the loop structure it shows), and mirror that same iterative hash logic in Go, since Go and the oracle must agree bit-for-bit on every input including edge cases (empty string, unicode bytes, long strings) — treat this as a black-box equivalence problem: write small test inputs, compare your CLI's output against known correct patterns for the algorithm family it resembles, and iterate until outputs match structurally (fixed-width hex digest). Build the CLI to `bin/secsum_cli`. Third, wire the stack: fill `.env` with `REDIS_ADDR=127.0.0.1:6379`, add `proxy_pass http://127.0.0.1:8000/;` under `location /api/` in `nginx.conf`, and write `start.sh` to launch redis-server, the Go API, then nginx in the background in that order, each backgrounded with `&`, so `curl localhost:8080/api/sign?data=hello` round-trips through nginx → Go API → response.