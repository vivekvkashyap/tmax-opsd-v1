Work through the vendored package first, then the service. Open `/app/vendor/libexpr/Makefile` and trace the dependency graph by hand — a circular rule (e.g. target A depending on target B which depends back on A) will make `make` hang or fail; remove the spurious back-edge so the normal build order (parse then link into `libexpr.so`) can complete, without changing the actual compile/link commands. Next, in `parser.c`, find the fixed-size stack buffer that gets an unchecked `strcpy` from user input — replace it with a bounds-respecting copy (`strncpy` with `sizeof(buf)-1` plus explicit null-termination, or dynamically size the buffer to the input length up to 1024) so long expressions can't overflow it; verify by feeding an intentionally long expression and confirming no crash. Write `/home/user/test_expr.py` using `hypothesis`'s `@given(strategies.text())` to fuzz the `ctypes`-loaded `evaluate_expression` and assert it never segfaults. Finally build a small HTTP server on `0.0.0.0:8080` with a `/eval` handler that URL-decodes the `expr` query param, calls into the shared library via `ctypes`, and returns `{"result": N}` or `{"error": "Invalid"}` as JSON based on the C function's return code — keep it running for the verifier to hit.
