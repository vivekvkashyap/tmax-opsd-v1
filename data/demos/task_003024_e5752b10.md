First get the vendored library building: open `/app/libwelford-1.0.0/Makefile` and look closely at the rule that builds `libwelford.a` — a static archive must be built with the `ar` archiver (`ar rcs libwelford.a welford.o`), not any other archiving tool; fix that one line, then `make clean && make` and confirm `libwelford.a` exists and `ar t libwelford.a` lists the object file. Then write `/home/user/streaming_scaler.c`: include `welford.h`, initialize one `WelfordState` with `welford_init` before the read loop, read stdin with `fgets` (cap at 64 bytes), skip blank/malformed lines silently, and use `sscanf` to match either `"FIT %lf"` or `"TRANSFORM %lf"` (reject anything else, including e.g. `INVALID`). On FIT: call `welford_update` then print `FIT_OK\n`. On TRANSFORM: read current variance via `welford_variance`; if it's exactly `0.0` print `0.000000\n`, otherwise print `(val-mean)/sqrt(variance)` formatted with `%.6f\n` using the mean/variance *at that point in the stream*, not recomputed after any later updates. Compile with `gcc -o /home/user/streaming_scaler /home/user/streaming_scaler.c -L/app/libwelford-1.0.0 -lwelford -lm -I/app/libwelford-1.0.0`, then self-test by piping varied FIT/TRANSFORM/garbage sequences through it and checking outputs behave sensibly before considering it done — exact match against any hidden oracle depends on getting the streaming order and formatting precise.
