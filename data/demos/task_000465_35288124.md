In Go, use `encoding/json` with a `bufio.Scanner` reading raw_embeddings.jsonl line by line, unmarshaling each into a struct with `Id string` and `Vector [4]float64` (or `[]float64`, then validate length). Read projection_matrix.csv with `encoding/csv`, parsing each of the 4 rows into 2 float64 columns to build a 4x2 matrix (e.g. `[4][2]float64`). For each embedding, compute the 1x4 * 4x2 = 1x2 matrix product manually: `v1 = sum(vector[i]*matrix[i][0])`, `v2 = sum(vector[i]*matrix[i][1])`. Compute the Euclidean norm `sqrt(v1*v1 + v2*v2)` and set valid=false only when the norm is strictly greater than 4.0 (use `>`, not `>=`, and watch floating point edge cases at the boundary).

Write output with `fmt.Fprintf` building the TSV line by line: header `id\tv1\tv2\tvalid`, then for each row format v1/v2 with `%.2f` (careful with negative zero — normalize `-0.00` to `0.00` if it can occur) and valid as literal `true`/`false` strings, joined with single tab characters, one row per embedding line in the same order as the input file. Write directly to /home/user/experiment_log.tsv, and diff your output against your own manual computation of the sample data before considering it correct, rather than assuming your matrix multiply orientation (row vs column) is right on the first try.