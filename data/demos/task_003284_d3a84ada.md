Fix each broken layer bottom-up and verify at each step before moving on. In `/app/libfastdir/Makefile`, add `-fPIC` to CFLAGS (required since the Rust FFI build links against it) and check the archive/clean rules for typos in tool invocation (e.g. flag order like `ar rcs` vs a scrambled variant) — run `make` and confirm `libfastdir.a` is actually produced. In `/home/user/analyzer/build.rs`, check what directory it passes to `println!("cargo:rustc-link-search=...")` and point it at the real location of `libfastdir.a` (`/app/libfastdir`), then confirm `cargo build --release` gets past the linker stage. For the Rust borrow-checker errors in `src/main.rs`/`src/graph.rs`, read each compiler error carefully — graph-building code commonly fails because it tries to hold a mutable borrow of the graph while also holding an immutable reference into it (e.g. iterating neighbors while inserting nodes); resolve by cloning small keys (like filename strings) or restructuring to collect indices first, then mutate. Implement topological sort with Kahn's algorithm, sorting ties lexicographically, and detect cycles by checking if any nodes remain with nonzero in-degree after processing, printing "CYCLE DETECTED" and exiting 0 in that case.