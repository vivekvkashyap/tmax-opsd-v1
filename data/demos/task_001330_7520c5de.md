Separate the pipeline into extraction, then estimation, then output, and validate each stage before trusting the next. First, use `ffmpeg` to dump frames (e.g. to raw or PNG) and compute, per frame, the mean pixel intensity over exactly the specified 16x16 sub-region (x:24-39, y:24-39) — a script (Python/awk/small C++ helper) that averages that window per frame gives you the 1D luminance time series; sanity-check it visually (e.g. print min/max) to confirm it bimodally clusters near two levels rather than being noise-only, which would indicate a wrong crop region. Second, in C++, implement state inference plus MCMC: since you know the emission model (N(50,10) for state 0, N(200,10) for state 1) you can get an initial state guess per frame by simple thresholding/Bayes classification, then run a Metropolis-Hastings sampler over p01 and p10 that proposes small perturbations and accepts based on the Markov-chain likelihood of the inferred (or jointly sampled) state sequence, keeping the best/MAP estimate. Third, compute the log-likelihood ratio of your fitted transition model against the null (p01=p10=0.5) evaluated on the same inferred state sequence, then write exactly the three-line `P01,...` / `P10,...` / `LLR,...` format to `/home/user/results.csv`. Iterate on sampler length/proposal width until estimates stabilize.