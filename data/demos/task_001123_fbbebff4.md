Fix each layer bottom-up and verify at each step before moving to the next. First, the C shared library: a Makefile invoking `gcc -shared -o libchecksum.so checksum.c` without `-fPIC` fails to build correctly on most platforms — add `-fPIC` to the compile step, run `make`, and confirm libchecksum.so exists. Second, the Rust build script: an empty/missing build.rs won't tell the linker where the library lives, so add `println!("cargo:rustc-link-search=native=<path-to-legacy_c>")` and `println!("cargo:rustc-link-lib=dylib=checksum")` in build.rs, then run `cargo build --release` and confirm the binary links (use `ldd` to check it references libchecksum.so). Third, edit /home/user/project/services/.env to set VALIDATOR_BIN to the release binary path and LD_LIBRARY_PATH to the legacy_c directory so the dynamic loader finds the .so at runtime — test this manually by running the binary directly with LD_LIBRARY_PATH exported first. Only after each piece works standalone, run start.sh and the pytest end-to-end flow.