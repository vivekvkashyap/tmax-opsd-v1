First run `/home/user/init_server.sh` manually to see the exact prompt text and order, then write the Expect script with `spawn ./init_server.sh`, followed by `expect "Enter Server Name: "` / `send "AutoProvisionedWeb\r"` pairs for each of the three prompts in order, ending with `expect eof` so the script waits for the child to finish before exiting — a common mistake is forgetting `expect eof`, which lets automate_init.exp exit before the config file is actually written. Set `chmod +x` on both scripts and run the expect script to confirm `server.conf` and the webroot directory get created. For `watchdog.sh`, `source /home/user/server.conf` to load `$PORT`/`$DIR`, launch the http.server with `&` and capture `$!` as the PID to track, then loop with `sleep 1` checking `kill -0 $PID 2>/dev/null` (or `pgrep -f` matching the exact command) — on failure, relaunch the server, capture the new PID, and append the exact alert string to `watchdog.log`. Launch watchdog.sh itself with `&` at the end so it keeps supervising after your commands finish; verify by manually killing the http.server PID and confirming both auto-restart and log line appear within a few seconds.
