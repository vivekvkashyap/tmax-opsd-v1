First profile empirically rather than guessing: `time` each of step1/step2/step3 individually (or add timestamps inside orchestrator.sh) to confirm which script dominates runtime, then write that script's exact filename to slowest_step.txt. Inspect the slow script's logic carefully before rewriting — the O(N^2) double-loop pattern of comparing every sequence's suffix against every other sequence's prefix (via nested `while read` loops, which is also slow in bash for other reasons) can be replaced with a single-pass awk approach: build a hash map keyed by 4-character prefix -> list of sequences sharing that prefix, then for each sequence look up whether its 4-character suffix exists as a key in that map and emit an edge for each match, explicitly skipping the case where a sequence would edge to itself. Preserve the original script's exact selection semantics (e.g. which input file it reads, whether it's filtered sequences or raw sequences) so output format and edge set stay identical to the original slow version — only the algorithm/implementation changes, not the input/output contract. After rewriting, chmod +x it, run `./orchestrator.sh` end-to-end, verify graph_output.txt is produced, and time the whole run to confirm it finishes in under 1 second.