Read the existing `/home/user/pipeline.py` first to see exactly where the leak occurs: `fit_transform` is called on the scaler and PCA over the full `X` before the train/test split, so both fitted transformers have "seen" test-set statistics. Fix this by performing `train_test_split` on the raw feature matrix `X` (and labels) FIRST — keeping `test_size=0.25, random_state=42` and the given label-generation logic untouched — then build a `sklearn.pipeline.Pipeline([('scaler', StandardScaler()), ('pca', PCA(n_components=...)), ('svc', SVC(...))])` and call `.fit(X_train, y_train)` only; evaluate with `.score(X_test, y_test)` or `.predict` + `accuracy_score`, never touching X_test during fitting. If accuracy falls short of 0.75, tune `n_components` and SVC hyperparameters (kernel, C, gamma) via cross-validation on the training set only (e.g. GridSearchCV with a CV split, not the held-out test set) until the held-out score clears the threshold. Log `n_components` and `test_accuracy` with `mlflow.log_param`/`log_metric` inside an `mlflow.start_run()` block, then `joblib.dump(pipeline, '/home/user/final_model.pkl')` so the saved object is the full fitted Pipeline, not just the classifier.
