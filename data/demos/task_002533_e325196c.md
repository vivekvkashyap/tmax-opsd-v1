Build and test each piece of this pipeline in isolation before chaining them via the git hook. Write `/home/user/.microservice_profile` with plain `export VAR=value` lines. Write `deployer.go` to read env vars with `os.Getenv`, attempt `net.DialTimeout("tcp", "127.0.0.1:9090", 2*time.Second)`, and on success/failure append the exact required log line (RFC3339 timestamp via `time.Now().Format(time.RFC3339)`) to `$LOG_DIR/deploy.log`, exiting 1 on failure — build it with `go build -o /home/user/bin/deployer` and run it manually first with the env sourced to confirm the log line format matches the verifier's regex exactly (including literal `Env=staging, Port=8080`). Then `git init --bare /home/user/microservice.git`, write `hooks/post-receive` as a shell script that sources the profile and execs the deployer binary, and `chmod +x` it. Test the hook directly by cloning the bare repo locally, committing a dummy file, and `git push` — check `deploy.log` afterward for the success line while the mock service on 9090 is up, since the task requires the log entry to exist by the time you finish. Finally add `/home/user/logrotate.conf` with `daily`, `rotate 3`, `compress`, `missingok` directives targeting `/home/user/logs/deploy.log`.