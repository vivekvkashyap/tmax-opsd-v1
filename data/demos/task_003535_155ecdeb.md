For Phase 1, open `generate_spectra.py`, find where the per-chunk energies list is reduced with plain `sum(energies)`, and replace it with `math.fsum(energies)` (import `math`) — this is the key fix, since `fsum` uses compensated summation so the result is identical regardless of the randomized chunk order, unlike built-in `sum`. Run the corrected script and capture the exact printed total energy value as your `stable_energy` (don't hardcode a number — read it from the script's own output). For Phase 2, write `bootstrap_ci.py`: load the `intensity` column with pandas/csv, call `numpy.random.seed(42)` immediately before the resampling loop, draw exactly 10,000 resamples with `numpy.random.choice(data, size=len(data), replace=True)`, compute the mean of each resample, and take `numpy.percentile` at 2.5 and 97.5 of that distribution for the CI bounds — seed placement matters, so set it right before the loop starts, not earlier in the script. For Phase 3, write `results.json` with `stable_energy` as the raw float from Phase 1 and `bootstrap_ci_lower`/`bootstrap_ci_upper` rounded to exactly 3 decimals.
