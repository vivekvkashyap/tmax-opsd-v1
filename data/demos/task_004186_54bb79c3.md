Work through this as a real credential-hardening fix, not a patch that just satisfies the letter of the spec. First, `chmod 0400` the vault file so only the owner can read it — verify with `stat`/`ls -l` that no group/other bits remain. Then rewrite the C++ source: drop the `argv[1]` password intake entirely, open and read the vault file directly, strip the trailing newline (e.g., with a check on the last character or a regex/trim), and compute SHA-256 over the exact stripped password bytes using OpenSSL's EVP or legacy SHA256 API — do not hash a string containing extra whitespace or a null terminator artifact, since this changes the output. Base64-encode the raw 32-byte digest (not a hex string) using OpenSSL's BIO/base64 or `EVP_EncodeBlock`. Write only that base64 string (no trailing text) to the log path. Compile with `g++ ... -lcrypto`, run the binary once to produce the log, then double-check your own computed hash by independently reproducing the same base64(SHA256(password)) value via `openssl dgst -sha256 -binary | base64` on the password you read from the vault, and confirm it matches your program's log output exactly.
