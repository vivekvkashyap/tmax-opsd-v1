Write `dedup.rs` to read `/home/user/raw_translations.txt` line by line, split each line on `|` into (timestamp, locale, text), and truncate the timestamp string to its first 13 characters (`YYYY-MM-DDTHH`) rather than doing full date-time arithmetic — this is a plain string slice, but do it on Unicode-safe boundaries (the timestamp itself is ASCII so byte-slicing is fine there, even though the text field may contain multi-byte UTF-8 which you must not slice into — keep it as an intact `String`/`&str` and let Rust's String handle UTF-8 correctly rather than indexing by byte offset). Deduplicate using a `HashSet<(String, String, String)>` keyed on (hour, locale, text), then collect the unique records, sort them (Rust's default string ordering sorts a `Vec<String>` or tuple lexicographically, matching what's needed), and write pipe-joined lines to `/home/user/hourly_unique_translations.txt`. Compile with `rustc /home/user/dedup.rs -o /home/user/dedup`, run it once to produce the output file, then add the cron line via `(crontab -l 2>/dev/null; echo "0 * * * * /home/user/dedup") | crontab -u user -` so the existing crontab isn't clobbered.