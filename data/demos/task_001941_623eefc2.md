Work this as a three-stage exploit: derive the auth secret, then craft a malformed ELF, then trigger the traversal. First, read `vuln_server.c` to see exactly how it validates uploads — it likely extracts a named ELF section and compares it (as a hash) against a value derived from the certificate's Subject/Common Name; use `openssl x509 -in /home/user/auth_cert.pem -noout -subject` to pull the CN, then compute the hash the C code actually uses (check the algorithm and whether it hashes the raw CN string or something formatted differently — read the comparison code precisely). Second, build a minimal valid ELF containing custom sections (e.g. `.auth_token` and `.upload_path`) with the required data using `objcopy --add-section`/`--set-section-flags` on a stub object, or write a tiny C/asm source and add sections post-compile — validate with `readelf -S` that section names and contents match exactly what the server parses. Third, exploit the path-traversal: set the `.upload_path` section's content to a relative path with `../` sequences (or similar) so the server's file-write logic escapes the intended uploads directory and lands the payload at exactly `/home/user/pwned.log`. Finally confirm `readelf -h /home/user/pwned.log` succeeds, since the target file must remain a valid ELF.