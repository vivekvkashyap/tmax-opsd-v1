Start by diagnosing the vendored build: run `make` in `/app/cookie-extractor-1.0`, read the error, then open the `Makefile` and compare the source filename it compiles against the `.c` file actually present in the directory (`ls *.c`) — fix the mismatched filename reference so `make` succeeds and produces `cookie-extractor`. Confirm the built tool's behavior by piping a sample `Cookie: ...` header into it and checking it prints just the `CommandToken` hex value, handling tokens that appear anywhere among other cookie fields (not necessarily first or last). For `/home/user/generate_auth.sh`, read one line from stdin, pipe/pass it to the compiled extractor to get the hex token, then decrypt it by XOR-ing each byte with `0x5A` (convert hex pairs to bytes, XOR, convert back to ASCII characters) to recover the plaintext command. Emit exactly one line: `restrict,command="<decrypted>" ssh-ed25519 <the-given-static-key> dummy@key`, matching the example format precisely (no extra whitespace/newlines). Test your script against multiple synthetic cookies with different key orderings and different embedded commands to make sure extraction and decryption are order-independent and byte-exact before considering it correct.
