Use OpenCV (or ffmpeg + a Python image lib) to read `/app/experiment.mp4` frame by frame; for each frame find the max pixel intensity and its (row) location via something like `cv2.minMaxLoc` on the grayscale frame, recording the row (y) as NaN whenever the max intensity is < 100. Load the resulting per-frame series into a `pandas.Series`, call `.interpolate(method='linear')` to fill NaNs, then compute `.rolling(window=5, center=True).mean()` for the rolling average — do not hand-roll a custom window average, since pandas' centered rolling window has specific edge-NaN behavior the verifier expects. Write frame/y_imputed/y_rolling_avg rows into a SQLite table `trajectory` with the exact schema given, using `sqlite3` directly or `pandas.to_sql`. Then stand up a lightweight HTTP server (Flask/FastAPI/http.server) bound to 127.0.0.1:8000 exposing `GET /api/stats?frame=N` that queries the DB for that row and returns the JSON exactly as specified, or a 404 if the frame is absent (e.g., because rolling average is NaN at the edges — decide what "not found" means and be consistent). Launch the server as a background/daemon process so it stays reachable after your script finishes, and smoke-test a couple of `curl` calls yourself before finishing.
