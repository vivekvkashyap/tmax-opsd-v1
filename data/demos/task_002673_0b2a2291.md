Parse the custom binary format field-by-field from stdin: read 2 bytes little-endian for filename length, read that many bytes as the filename, read 4 bytes little-endian for file size, then read that many raw bytes as file content — use `std::cin.read()` with explicit byte counts (not text-mode line reading, since payloads are binary) and loop until EOF/short read. For each filename, before writing anything, canonicalize the intended output path: reject it as malicious if it starts with `/` or `\`, contains any `..` path component after splitting on `/` and `\`, or if resolving it against the target directory (e.g. via `std::filesystem::weakly_canonical` and checking the result still has the target directory as a prefix) escapes the target directory — don't just string-match `"../"` since that misses tricks like absolute paths or embedded traversal in later path segments. Since malicious archives can appear anywhere in the entry sequence, buffer/track every path you've already written to disk during the current run so that on detecting *any* malicious entry you can delete all of them (rollback) before printing `MALICIOUS` and exiting 1; only write files to their final location incrementally when the whole run stays clean, printing `CLEAN` and exiting 0 at the very end.