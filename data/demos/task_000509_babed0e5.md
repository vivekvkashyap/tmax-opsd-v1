In Go, first parse the FASTA by skipping any line starting with `>` and concatenating the rest into one string — don't assume a fixed line width. Slice the sequence into consecutive 500-base bins (the final bin may be shorter if the length isn't a multiple of 500); compute each bin's GC fraction as (count of 'G'+'C')/bin length. The critical correctness point: compute the neighbor-difference refinement decision for ALL bins using the ORIGINAL unsplit GC values first, storing a boolean "split" flag per bin, and only afterward go back and actually split the flagged bins — if you split as you go, later comparisons will use already-modified bins and get the wrong answer. A bin is flagged if its GC differs from either immediate neighbor (boundary bins only have one neighbor) by strictly more than 0.15. For flagged bins, cut exactly at the midpoint into two 250-base bins; recompute GC for every final bin (split or not) fresh from the sequence. Midpoint of a bin is `start + length/2` in 0-indexed bases. Fit OLS slope/intercept by hand with the standard sum formulas (no external stats libs) over (midpoint, GC) pairs across all final bins, round both to 6 decimals, and write the JSON with sequence_length, initial_bins, refined_bins, slope, intercept to `/home/user/results.json`.