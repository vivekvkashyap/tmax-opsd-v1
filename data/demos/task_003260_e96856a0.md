Load both files with pandas, then group `api_logs.csv` by `node_id` to get `total_requests` and failure counts per node, and merge in `datacenter` from `node_info.json`. For the Bayesian step, apply the given closed-form posterior mean formula `(alpha + k) / (alpha + beta + n)` directly with alpha=2, beta=10 — don't refit a model, it's just arithmetic per node. For the latency test, filter to `status == 'success'` rows per node, and explicitly guard the `len < 2` edge case (assign p-value 1.0) before calling `scipy.stats.ttest_1samp` against 120.0, since scipy will error or return NaN on too-few samples otherwise. Compute `is_anomalous` exactly per the compound OR condition given (bayes rate > 0.15, or p<0.05 AND mean latency > 120), applying it per node using the *filtered success-only* mean latency, not the overall mean. Round the two float columns to 4 decimals with `round()` before writing, sort by `node_id` alphabetically, and write with `to_csv(..., index=False)` in the exact column order specified. Spot-check one or two nodes by hand to make sure the pipeline logic matches the spec before finishing.
