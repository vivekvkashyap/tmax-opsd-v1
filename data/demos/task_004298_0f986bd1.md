Build this pipeline bottom-up and test each piece independently before wiring it together. First write `/home/user/workspace/safepipe.c`: read stdin line by line (e.g. `fgets`/`getline`), for each line check if it contains the substring `../` anywhere or starts with `/` — if so, append it (not echo) to `/home/user/logs/alerts.log` (open in append mode, flush/fsync so it's visible promptly since this runs inside a streaming pipeline); otherwise print it to stdout. Compile with `gcc -o /home/user/workspace/safepipe safepipe.c`. Test it standalone first: `printf '../../etc/passwd\nsafe.txt\n' | ./safepipe` and confirm alerts.log and stdout split correctly. Then write `/home/user/workspace/monitor.sh`: use `inotifywait -e close_write,moved_to /home/user/incoming` in a loop, filter for the event filename being `dump.tar.ab`, then on trigger run `cat /home/user/incoming/dump.tar.aa /home/user/incoming/dump.tar.ab | tar -x -C /home/user/workspace/`, then `tar -tzf /home/user/workspace/payload.tar.gz | ./safepipe >> /home/user/logs/safe.log`. Create the three required directories first, start monitor.sh in the background (`&`), then copy the staged `dump.tar.aa` then `dump.tar.ab` from `/tmp/staging/` into `/home/user/incoming/` in that order to trigger it, and give the async pipeline a moment to finish before checking the log files.