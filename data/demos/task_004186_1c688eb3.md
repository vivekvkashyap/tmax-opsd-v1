This will be checked against a hidden reference implementation run on multiple randomly generated videos, so build a script that generically matches the specified pipeline rather than hardcoding anything from one sample file. Use `ffmpeg` to decode the input at exactly 1 fps to raw grayscale frames (e.g., `-vf fps=1 -pix_fmt gray -f image2pipe -vcodec rawvideo -`), reading the stream frame-by-frame using the video's actual width/height (query them with `ffprobe` rather than assuming a fixed resolution). For each frame compute mean pixel brightness and normalize to [0,1] by dividing by 255. Group frames into consecutive non-overlapping windows of 5 frames (indices 0-4, 5-9, ...), average their normalized brightness per window, and round to exactly 3 decimals. Emit one SQL line per window using the exact required `INSERT ... ON CONFLICT ... DO UPDATE` template, substituting window start/end frame indices and the rounded average — match the literal string format precisely since it will be diffed byte-for-byte against oracle output across several synthetic test videos. Handle a trailing partial window (fewer than 5 leftover frames) the same way the described windowing logic implies.
