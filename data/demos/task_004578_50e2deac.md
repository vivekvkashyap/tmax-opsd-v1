Fix the vendored package's two breakages first, each verifiable in isolation: in `launch.sh`, correct the typo'd command name to the real `uvicorn` invocation; in `config.py`, change the hardcoded invalid paths to point at the real data files under `/home/user/data/` (`backup_metadata.json` and `db_dependencies.json`). Then implement `/prioritize-backups` in `api.py`: load both JSON files, filter out any metadata entries with `tier == 3`, then build the in-degree count for each remaining database by scanning every edge in the dependency graph and incrementing the target's counter — note the task says to count in-degree from ALL edges targeting a remaining database, even edges whose *source* is a filtered-out (tier-3) database, so don't also filter edges by source tier. Compute `score = (in_degree + 1) * (1000 / size_gb)` for each remaining database using its `size_gb` from the metadata, sort descending by score, and return the top 5 names as `{"priority_backups": [...]}` via FastAPI's JSON response. Run `launch.sh` (backgrounded) and confirm the fix with a direct `curl http://127.0.0.1:8080/prioritize-backups` before finishing, checking that the server actually binds to 127.0.0.1:8080 and stays running.
