Break this into independent sub-problems and validate each in isolation before chaining them. (1) Audio: transcribe `/app/intercepted_comms.wav` with an available STT tool (e.g. `whisper`, `SpeechRecognition`+`pocketsphinx`, or any installed CLI) — listen for a short spoken phrase naming a password, then normalize it exactly as instructed by context (e.g. strip spaces/punctuation) since zip passwords are usually a single compact token; try a couple of normalization variants against the archive if the first guess fails. (2) Use the recovered password with `unzip -P <password> /app/vault.zip -d /home/user/` to extract `fw_policy_oracle`, then `chmod +x` it. (3) Reverse-engineering the oracle: since you cannot ship the binary, treat it as a black box — feed it many varied `IP:PORT;IP:PORT...` strings (single pair, multiple pairs, edge ports, 10.x vs 192.168.x vs other ranges, ports below/above 1024) and record stdout exactly, looking for distinct output rules per condition (e.g. by IP prefix or port threshold). (4) Write `/home/user/evasion_payload.py` in pure Python reproducing those exact rules and line/newline formatting, splitting input on `;` and parsing each `IP:PORT` token yourself. Before finishing, fuzz your own script against the oracle across many random inputs and diff outputs byte-for-byte, since the grader will do the same.