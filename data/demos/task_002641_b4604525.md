Treat this as fix-the-build then fix-the-bug then black-box-match-the-oracle. First get `maturin build`/`develop` to run inside `/app/vendored/rust_url_parser` and read the actual error — the `pyproject.toml`'s `[tool.maturin]` block has an invalid feature flag, so compare it against a known-good maturin config and remove/correct the bad key rather than guessing broadly. Once it compiles, look for the `unsafe` block in `src/lib.rs` doing raw pointer dereference on a string slice — replace it with safe, bounds-checked Rust (e.g. use `str`/byte-slice indexing with explicit length checks, or safe UTF-8-aware APIs) so it doesn't panic/UB on malformed parameter layouts; build and `pip install`/`maturin develop` the fixed extension into the environment your script will run in. Then write `/home/user/run_parser.py` to read one raw URL per stdin line, call the extension's parse function, and emit one JSON object per line with route/protocol/hex-encoded params. Since correctness is judged by exact byte-for-byte match against a hidden oracle binary across thousands of fuzzed inputs (including malformed URLs and complex UTF-8), don't guess field semantics — construct your own varied test URLs (empty, missing scheme, percent-encoded, multi-byte UTF-8 in params) and inspect your script's output for consistency/edge-case handling before considering it done, paying special attention to how encoding errors and missing components are represented.
