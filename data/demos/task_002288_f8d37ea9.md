Scaffold with `cargo new graph_analyzer --bin`, then edit `Cargo.toml` to pin `rusqlite = { version = "0.31.0", features = ["bundled"] }` (the bundled feature avoids needing system SQLite dev headers). The core of the task is a single recursive CTE in SQL, not Rust logic — write it directly and let `rusqlite` execute it: base case selects the row for `name = 'Root_Alpha'` with `path_length = 0` and a boolean/string flag `has_filter` initialized to false; recursive case joins `derivations` to extend the path by one hop, increments `path_length`, and ORs in whether `method = 'filter'` on the traversed edge into `has_filter`. Because BFS-style unconstrained recursion can revisit nodes via multiple paths, be careful about which path each row represents — use `path_length` growth to guarantee you get the shortest path per node (e.g. take the MIN path_length per node in an outer query, or track visited nodes to avoid infinite loops if the graph could cycle). Layer `DENSE_RANK() OVER (PARTITION BY domain ORDER BY size_mb DESC)` on top, then filter to `has_filter = 1` in the final SELECT. Use the `csv` crate or manual `write!` to produce the exact header and 4-decimal-free integer columns, sorted by `name`. Run `cargo build --release && cargo run --release` and confirm the output file is written before finishing.
