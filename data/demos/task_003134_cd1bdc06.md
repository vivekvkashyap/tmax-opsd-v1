Treat this as two separate C build problems. First, inspect `/app/email_quota_monitor-1.2.0/Makefile` for the syntax error preventing `make` from succeeding — a very common cause is that a recipe line under a target uses spaces instead of a literal tab character; fix that indentation issue (and any other syntax problems) so `make` produces `build/eqm_util`. Then inspect the C source for the environment-variable lookup — search for the `getenv()` call handling the quota warning level and correct the variable name it queries so it matches the required `QUOTA_WARN_LEVEL` exactly (a subtly misspelled/mismatched env var name is the classic bug here). Rebuild and confirm by running `QUOTA_WARN_LEVEL=85 /app/email_quota_monitor-1.2.0/build/eqm_util` and checking the output reflects that value. Second, write `/home/user/quota_wrapper.c` as a simple loop reading lines from stdin via `fgets`/`scanf` until EOF, parsing each as an integer byte count, comparing against the 1048576-byte threshold, and printing `EXCEEDED` or `OK` per line; compile it and test with boundary values (below, at, and above the threshold) to make sure the comparison operator (`>` vs `>=`) matches expected boundary behavior exactly, since a hidden equivalence check will fuzz many inputs including edge cases.
