Design the pipeline as two independent, testable pieces. First write `process.sh`: use `grep`/`awk` to pull only lines containing `:WAIT_FOR` from `/home/user/transactions.graph`, then reformat each to `Source,Target` (strip the `:WAIT_FOR` token and surrounding whitespace, keep column order as source-then-target), and pipe that into the compiled detector, redirecting stdout to `/home/user/deadlock_report.json`. Second, in `deadlock_detector.cpp`, parse `Source,Target` lines from stdin into an adjacency list, then run cycle detection over the *whole* directed graph (e.g. DFS with white/gray/black coloring, or Tarjan's SCC algorithm) rather than a single fixed traversal — a graph can have multiple disjoint components and only nodes on an actual cycle (SCC of size >1, or a self-loop) count as deadlocked, not just anything reachable from a cycle. Collect the union of all nodes appearing in any cycle, sort lexicographically, and emit JSON matching the exact schema (`deadlock_detected` boolean, `deadlocked_transactions` array, empty array when none). Compile to `/home/user/detector`, `chmod +x process.sh`, then run it and inspect the resulting JSON with a JSON parser (not just eyeballing) to confirm exact structure.
