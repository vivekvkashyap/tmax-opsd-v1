Treat this as three independent subtasks. (1) Secret recovery: the API key was committed then removed, so `git log -p` won't show it on the current tree — search full history instead, e.g. `git log -p --all -- '**/constants.py'` or `git log --all -p | grep -i api_key`, find the commit where it was still present, extract the exact string value, and write only that string to /home/user/recovered_key.txt. (2) Build fix: run `pip install -e /home/user/legacy_pipeline`, read the actual traceback carefully — it's likely a small bug in setup.py (e.g. a typo in a setuptools import/function name) rather than a missing dependency — fix it minimally and reinstall until the command succeeds. (3) Crash MRE: after install, write a loop/fuzzer script that reads each line of /home/user/sample_data.jsonl, invokes the installed `process_data` CLI on it (capturing exit code/stderr), and stops at the first line whose invocation crashes (non-zero exit / traceback) rather than scanning visually; once found, write that exact JSON line, unmodified, to /home/user/mre.json. Verify all three artifacts exist with exact required content before finishing.
