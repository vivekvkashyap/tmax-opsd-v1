Start by installing scapy and inspecting the pcap in a Python REPL/script rather than guessing: use `rdpcap()`, iterate packets, and filter to only those whose transport layer is UDP with destination port 9000 (ignore any other protocol/port noise packets you find, and don't count them toward the index) — decode the Raw payload bytes as UTF-8 and json.loads it to recover each {"x","y","z"} dict, keeping a running 0-based counter over just the matching UDP:9000 packets. Feed each payload in order into math_engine.compute_trajectory() inside a try/except and record the index and payload of the one that raises (a ZeroDivisionError or similar) — that pinpoints the crash-triggering geometry (look at the denominator expression in the source to understand what combination of x,y,z zeroes it out). Write that index and payload into bug_report.json in the exact required schema. Then patch math_engine.py by adding `1e-9` to the denominator right before the division (don't change the numerator or restructure the formula) so it returns a large-but-finite float instead of raising, and re-run the offending payload through the patched function to confirm no exception and a valid float result.