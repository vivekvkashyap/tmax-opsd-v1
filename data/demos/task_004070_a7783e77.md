Handle the two parts independently. For frame extraction, first check the video's duration with `ffprobe`, then run `ffmpeg -i /app/experiment.mp4 -vf fps=1 -start_number 1 /home/user/dataset/frames/frame_%04d.jpg` so the numbering and rate match the spec exactly — verify the resulting file count against the video's duration afterward. For the backup filter, the core trick is to detect symlink cycles without infinite-looping: walk the tree yourself (e.g. Python `os.walk` with `followlinks=False`, or manual recursion) and for each symlink resolve it with `os.path.realpath()`/`readlink -f`, then reject it if the resolved path lies outside the base directory (compare via `os.path.commonpath` or a prefix check on the canonicalized base) or if resolving it raises an error / doesn't terminate (guard with a max-depth counter or catch `OSError` from `os.path.realpath` on circular links) — never call something like unbounded `os.path.realpath` in a loop without a cycle guard. Print only safe files/dirs/symlinks. Test against both `/app/corpora/clean/` and `/app/corpora/evil/` before considering it done, confirming it exits quickly and cleanly on the evil corpus.
