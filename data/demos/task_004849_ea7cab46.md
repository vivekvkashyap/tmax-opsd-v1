This is checked by bit-exact fuzz comparison against a hidden reference implementation, so precision and determinism matter more than "close enough" math. First fix `Cargo.toml`'s dependency conflict (likely a pinned/incompatible `rayon` version clashing with another crate) so `cargo build --release` succeeds — check the exact version constraints causing the resolver error and relax or align them. Second, OCR `/app/kernel.png` (e.g. `tesseract`) and read the extracted text yourself to get the kernel coefficients and bias — do not hardcode guessed numbers, derive them from the image. Third, replace the racy threading (raw unsafe pointers/improper mutex writes to a shared vector) with a safe, deterministic approach — either single-threaded windowed iteration (`data.windows(3)`) or a properly synchronized parallel scheme where each output index is written by exactly one thread, so results don't vary run to run. Fourth, switch all arithmetic from `f32` to `f64`, and use a fixed, straightforward left-to-right accumulation order (avoid reordering or fusing sums) since floating-point results depend on operation order. Format output as space-separated `f64` values to 6 decimal places. Build with `cargo build --release` and manually sanity-check your binary's output on a few hand-crafted stdin sequences (e.g. an impulse or all-zero input) to confirm the kernel/bias and math are applied correctly before considering it done.
