Write the C program defensively around parsing, since schema-enforcement is the part most naive solutions get wrong. Read the file line by line (e.g. `fgets`), skip the header, and for each data line split on commas while counting fields — if the count isn't exactly 4, skip the row. For the coordinate fields, don't just check for non-empty: use `strtod` and verify via its `endptr` that the entire field was consumed as a valid float (rejects rows like `bad_row,a,b,c`) and also reject genuinely empty fields (rejects rows like `id 5` with a trailing empty z field) — this is the detail a naive `sscanf("%f,%f,%f", ...)` approach often gets wrong because it silently succeeds on partial/garbage input or leaves uninitialized floats. Apply the given linear weights and bias exactly as specified to compute the score, write `id,class` for every accepted row to `/home/user/results.csv`, and have `pipeline.sh` compile with `gcc classifier.c -o classifier` then run `./classifier`, with execute permission set via `chmod +x pipeline.sh`.