Do Phase 1 first since Phase 2 depends on it. In `/app/joblib-1.3.2`, grep the packaging metadata (`setup.py`/`setup.cfg`/`pyproject.toml`) for the dependency list — look for a misspelled package name (a typo of a common dependency, likely `cloudpickle`) and correct only the spelling, then `pip install -e .` from that directory so `joblib` resolves to the patched vendored source. For Phase 2, write `/home/user/analyze.py` to read CSV from stdin with pandas, melt the wide `metric_a/b/c` columns into long form (`variable`,`value`), group by `sensor_id` and dispatch each group's processing to `joblib.Parallel(n_jobs=...)(joblib.delayed(fn)(group) ...)` — this is a required, checked usage, not optional. Inside each group's worker, sort by timestamp, then for each `variable` subgroup compute a rolling window-3 mean of `value` (using pandas `.rolling(3).mean()` or manual logic), rendering the first two positions per group as an empty string rather than a number, formatted to exactly 2 decimals otherwise. Concatenate all processed groups, sort globally by (sensor_id, variable, timestamp), and emit CSV to stdout with the exact header. Since grading is bit-exact fuzz equivalence, be careful with float formatting and empty-window edge cases for groups smaller than 3.
