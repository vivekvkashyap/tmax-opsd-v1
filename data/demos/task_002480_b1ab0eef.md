Install an offline STT tool (e.g. `whisper.cpp` with the tiny/base model, or `openai-whisper` if available) and transcribe `/app/config_update.wav` to `/home/user/transcript.txt`; read the raw transcript text yourself to identify which three configuration keys are being changed and to what values (numeric or spoken-word values need normalizing, e.g. words to digits/strings) — don't assume the words map literally without checking the transcript. Then write `/home/user/config_updater.c` to load `/home/user/base_config.json`, apply the three updates you determined from the transcript, and for any key whose name contains "password" or "secret" replace its value with the literal string `***` regardless of what was dictated. Implement Levenshtein distance between the original and updated unformatted JSON strings (classic DP edit-distance table) and write that integer to `/home/user/drift.txt`. Keep the JSON writer simple/flat since the base config is flat — string manipulation is fine, no need for a full JSON library. Verify output types (ints vs strings) match what a JSON parser would expect. Compile and run the program to produce both output files.