Write `/home/user/mapper.sh` as an awk-driven filter since the transformation is per-line and stateful across a small window — parse each line's `ID=`, `VALUE=`, `OP=`, `IP=` fields (split on spaces and `=`, e.g. `awk -F'[ =]'` picking the right positional fields), mask the IP by keeping only the first two octets and appending literal `.X.X` (split on `.`), and decode `OP` by literal string comparison against the three unicode-escape forms `+`/`-`/`*` (careful with backslash escaping in awk/bash — the input literally contains the two characters `\` and `u002B`, not an actual `+`; you must match the escaped string, not try to interpret it as unicode). Compute `LOCAL_RESULT = VALUE <op> ID` per the decoded operator, and maintain a rolling window of the last 3 results using shift-register variables (w1,w2,w3) that carry across awk record iterations (persist state between lines, not per-line only) — for the first two lines the sum is just of however many results have been seen so far. Emit `[<MASKED_IP>] ROLLING_SUM=<sum>` per line, then `chmod +x mapper.sh` and launch the background pipeline exactly as specified with `nohup bash -c 'nc 127.0.0.1 9001 | /home/user/mapper.sh | nc 127.0.0.1 9002' > /home/user/pipeline.log 2>&1 &`. Test your script standalone first by piping sample lines through it before wiring the network pipeline, since a bit-exact fuzz comparison against a hidden oracle will exercise many operator/IP combinations.