Start by recovering the target directory from the image: run OCR (e.g. `tesseract /app/recovery_instructions.png -`) on `/app/recovery_instructions.png` to read the handwritten note, and treat whatever absolute path it reveals as the extraction destination for your later run — don't hardcode a guessed path. For the extractor itself, write `/home/user/safe_extractor.py` to take `<tar.gz path> <output dir>` as argv, open the tarball, find nested `.zip` members (extract or stream them from the tar), and for each zip, iterate entries and for every entry compute the fully resolved destination path (`os.path.normpath(os.path.join(output_dir, entry_name))`) — the core safety check is to verify that resolved path is still inside `output_dir` (e.g. `os.path.commonpath([resolved, output_dir]) == output_dir` after both are made absolute); reject/skip any entry whose name is absolute or whose resolved path escapes the output directory (this is exactly the Zip Slip pattern: `../../` traversal or absolute paths like `/etc/...` in the zip entry name), and only then extract the safe ones. Test locally by hand-crafting a small zip with one safe entry and one `../../evil.txt`-style entry to confirm your extractor keeps the safe file and silently skips the malicious one before running it against the real `/app/sample_backup.tar.gz`.