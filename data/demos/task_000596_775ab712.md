Tackle the four sub-problems in dependency order, verifying each before moving on. (1) Recover the deleted schema: find the daemon's PID with `pgrep -f legacy_daemon.py`, list `/proc/<pid>/fd/` to find the fd pointing at the unlinked `/tmp/schema.json` (it'll show as `schema.json (deleted)` via `ls -la`), then `cat /proc/<pid>/fd/<N> > /home/user/schema.json` to recover its exact bytes — do not try to reconstruct the schema by guessing its contents. (2) Fix the vendored package: edit `/app/log-cruncher-0.5/setup.py` to relax the pinned `python-dateutil==2.1.0` constraint (e.g. to `>=2.1.0` or match the installed version), then `pip install -e /app/log-cruncher-0.5/`. (3) Read `/app/crash_traceback.log` to find the exact failing line/exception in `cruncher/parser.py`, and fix it defensively — guard the `parts[1]` index access for short lines and fall back to a default level (matching what the traceback/oracle implies) instead of raising, so malformed log levels don't crash. (4) Write `/home/user/parse_logs.py` to take the raw log string as argv[1], parse it with the fixed library and the recovered schema, and print JSON with the required keys. Validate correctness incrementally by comparing your output against `/app/oracle_parser` on a handful of hand-picked and edge-case (malformed level, minimal spacing) inputs before trusting it against the full fuzz suite.