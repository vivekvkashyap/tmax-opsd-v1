Do not attempt concurrent writes at all — the fix is architectural: replace the failing update-based approach with a single read-only aggregation query. First, inspect the schema yourself with `sqlite3 /home/user/dataset.db .schema` to see the actual table/column names for researchers, publications, an authorship join table, and a citation/reference table (don't assume names — read them). Then write a Go program using `database/sql` with the sqlite3 driver that opens the DB read-only (e.g. `?mode=ro` or just avoid any write statement) and issues one query joining researchers to publications via the authorship table (counting distinct publications per researcher) and joining publications to a citations/references table (counting how many times each of a researcher's publications appears as a cited/target id), using `GROUP BY` and `SUM`/`COUNT` aggregates plus a `LEFT JOIN` so researchers with zero citations still appear correctly. Order by total citations descending, then full name ascending as a tiebreaker, and apply `LIMIT 5 OFFSET 0` in SQL rather than truncating in Go. Marshal the resulting rows to the specified JSON shape with `encoding/json` and write to `/home/user/top_researchers.json`. Initialize the Go module and fetch the sqlite driver with `go mod init` / `go get github.com/mattn/go-sqlite3` before building; verify by actually running the program and inspecting the JSON, cross-checking a couple of counts by hand against the raw table contents.