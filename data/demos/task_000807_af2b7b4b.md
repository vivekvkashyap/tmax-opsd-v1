Work the two parts independently, then verify end to end. For service wiring: read `/app/services/api/config.py` and `/app/services/worker/settings.py` first, identify the Redis connection string each expects (host/port/db), and make them consistent — since API, worker, and Redis run in the same container, use `localhost`/`127.0.0.1:6379`. Start everything with `start_all.sh`, then confirm connectivity by posting to `/process` and polling the job status before writing any classifier code — a broken pipeline will silently invalidate later testing. For the sanitizer: load the edge list into a `networkx.Graph()` (undirected), then for every node check `G.degree(n) >= 5` AND `nx.clustering(G, n) == 0.0` — a star-shaped hub whose neighbors are mutually unconnected. Any single node matching both conditions means reject (`sys.exit(1)`); otherwise accept (`sys.exit(0)`). Don't just threshold on degree alone or on average clustering — the anomaly is defined per-node, on the *local* clustering coefficient of the hub specifically. Test against every file in both `/home/user/corpus/clean/` and `/home/user/corpus/evil/` before considering it done, iterating on edge cases like nodes with degree exactly 5.
