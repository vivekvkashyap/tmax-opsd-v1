Read the whole pipeline before touching code: the bug is that vocabulary and mean/std are computed over `rows` (all data) instead of only rows where `is_train == "true"`. Fix by first partitioning rows into train/test, then building the BTreeSet vocabulary and computing mean/variance (dividing by N-1 for sample std dev) strictly from the training partition. When transforming, apply the train-derived mean/std and train vocabulary to every row (train and test alike); for test-set text, only count words that already exist in the train vocabulary — do not add new columns for unseen words, and keep vocabulary columns sorted alphabetically in the header and per-row counts. A naive fix might filter the wrong set (e.g. still scanning all rows for stats but only skipping vocabulary) or forget that std dev must use the training row count (not total) as the denominator. After editing `main.rs`, rebuild with `cargo run` inside `/home/user/etl_pipeline/` and inspect `output_test.csv` to confirm scaled values now differ from what the leaky version produced.