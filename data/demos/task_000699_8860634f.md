First recover the actual rule set from the audio rather than assuming generic XSS rules: use `ffmpeg -i /app/bug_report.wav -ar 16000 -ac 1 out.wav` to normalize it, then transcribe with whatever tool is available (e.g. `whisper`, or another installed speech-to-text utility); listen/re-check ambiguous parts (spelled-out characters, case sensitivity mentions) carefully since the sanitizer's exact accept/reject rules come only from this transcript. Then fix `sanitizer.cpp`/`Makefile` incrementally: get it to compile first (add missing includes, fix syntax, flesh out `main` to read the argv[1] file fully into a buffer including embedded null bytes — use file size via `fseek`/`ftell` or `stat`, not a C-string read which would stop at `\0`), then implement each rejection rule as an independent substring/byte check over the raw buffer, case-insensitive where the transcript specifies it. For the "print exact contents" success path, write the buffer to stdout with `fwrite` using the exact byte count, not `printf`/`cout <<` with a C-string, to avoid truncating at embedded nulls or altering binary-safe content. Validate against both corpora before finalizing, since 100% pass is required on each.