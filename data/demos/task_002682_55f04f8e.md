Work through this in dependency order since later steps rely on earlier ones being correct. First locate the Flask API's env/config file under `/app/services/api` and set the Redis host variable to point at the local Redis instance (127.0.0.1), then restart the services via the provided startup script so the change takes effect — verify connectivity (e.g. a simple curl or redis-cli check) before moving on. Second, compile the C sources in `/app/libthermal/` into a shared library with `-shared -fPIC` producing `libthermal.so`. Third, write `mesh_step.c` to link against it (`-L/app/libthermal -lthermal`, plus rpath or `LD_LIBRARY_PATH` so it resolves at runtime): read N and N integers from stdin exactly as specified, compute `get_diffusion_rate()` once, apply the interior-node update with plain C integer division (truncates toward zero — match this precisely since your binary is compared bit-for-bit against a hidden oracle via randomized fuzzing), leave boundary nodes untouched, and print space-separated integers. Test it against hand-computed small cases before trusting it. Finally write `fit.sh`: GET `/init`, parse `N` plus N ints, pipe the array through `/app/mesh_step` five times sequentially (each output feeding the next input), and POST the raw final array to `/submit`.
