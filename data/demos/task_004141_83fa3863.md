Build this as a strict binary-format parser followed by a chunk/compress/manifest pipeline — get the byte offsets and endianness exact, since any off-by-one there corrupts everything downstream. Steps: (1) `tar xzf /home/user/artifacts.tar.gz` to extract the `.bin` files; (2) in Rust, parse each file per the spec: verify the 4-byte `ARTF` magic and version byte 0x01, read the 2-byte little-endian name length `N`, read `N` bytes as UTF-8 name, read the 8-byte little-endian payload length `P` (use `u16`/`u64::from_le_bytes`), then read exactly `P` payload bytes; (3) split the payload into 1024-byte chunks (last chunk may be shorter — use `chunks(1024)`); (4) gzip-compress each chunk (the `flate2` crate's `GzEncoder` is the standard choice — add it to `Cargo.toml`) and write to `/home/user/repo_out/<artifact_name>/chunk_<index>.gz`, creating directories as needed; (5) after writing each compressed chunk file, re-read those exact bytes from disk and SHA-256 them (rather than hashing your in-memory buffer) so the manifest hash is guaranteed to match the file on disk; (6) build the nested manifest JSON keyed by artifact name (not the original `.bin` filename) with chunks listed in order, and write it to `/home/user/repo_out/manifest.json`. Build with `cargo build --release` and run the binary before considering the task done.