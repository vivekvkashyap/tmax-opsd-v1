Fix the build first, then build the pipeline against a graded similarity metric (aim for robustness, not one lucky pattern).
1. In `/app/dos2unix-7.4.3/Makefile`, find the line adding a bogus/nonexistent library to the linker flags and remove it, then `make` (and `make install` or just use the built binary in place) so `dos2unix` runs cleanly.
2. In `/home/user/pipeline.sh`: run the compiled `dos2unix` on `/home/user/raw_subs.txt` first to strip `\r`. Then, per line, try multiple timestamp regexes in sequence (bracketed `HH:MM:SS`, `HH:MM:SS AM/PM`, ISO `THH:MM:SSZ`) and normalize whichever matches to 24-hour `HH:MM:SS` (convert 12-hour AM/PM math yourself), discarding any date portion.
3. Normalize the remaining text: lowercase, trim whitespace, and strip comma/period/`!`/`?` (e.g. via `tr -d` or `sed`) before writing `HH:MM:SS<TAB>text` to the tsv.
4. Append `[YYYY-MM-DD HH:MM:SS] Started processing` / `Completed dos2unix` (use `date` command substitution) to the log at each stage.
5. Run the script and eyeball a sample of output lines against the raw input to catch format edge cases before considering it done — the grader only needs ~90% line match, so focus effort on the most common timestamp/text patterns.
