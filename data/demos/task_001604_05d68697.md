For Phase 1, extract frames from `/app/breach_record.mp4` with `ffmpeg -i breach_record.mp4 frame_%03d.png` (or read frames directly via a library like OpenCV/PIL after ffmpeg extraction), compute each frame's mean pixel luminance in order, threshold at 128 to get a bit (bright=1, dark=0), then pack every consecutive 8 bits MSB-first into one byte and convert to ASCII to recover the 8-character signature — derive it programmatically, don't hardcode a guess. For Phase 2, `policy_checker.py` must: read the target `.pyc` file's bytes, compute `hashlib.sha256(...).hexdigest()`, and check membership against the lines of `/app/manifest.txt`; if absent, exit 1 immediately without inspecting bytecode. If present, strip the `.pyc` header (version-dependent number of bytes — Python 3.7+ uses a 16-byte header) and `marshal.loads()` the remainder to get the code object, then walk `co_consts` (and nested code objects' consts, since functions may be nested) looking for the recovered signature as a whole string, and also check every adjacent pair of string constants concatenated together (in both stored order and reversed, since bytecode constant order isn't guaranteed) to catch the split-payload obfuscation trick. Exit 1 if found either way, else exit 0. Validate your script against both provided corpora before considering it correct, since the real grading corpus is stricter/larger.