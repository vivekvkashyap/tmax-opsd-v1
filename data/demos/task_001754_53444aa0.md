Start by extracting the exact rule text from `/app/audit_rules.wav` — transcribe it with an available speech-to-text tool (or careful manual listening) and write down the precise substrings/conditions and output strings it dictates verbatim, since the grading is against exact string matching, not paraphrase. Then implement `validator.sh` using only bash builtins and `base64`: decode `$1` with `base64 -d`, and critically, suppress/handle decode errors (redirect stderr, check exit status) so an invalid base64 payload degrades to an empty string rather than crashing or leaving stale output in the variable. Check the decoded string for the forbidden substrings using bash's `[[ "$x" == *pattern* ]]` glob matching (safe with spaces/special characters, unlike unquoted comparisons), then validate the path argument's prefix and reject any occurrence of `..`. Test manually with crafted inputs — spaces in arguments, malformed base64, paths with `..` embedded mid-string — before trusting it, since the task explicitly stresses these edge cases and the grader fuzzes them at scale.
