First locate the bug: in `/app/gonomaly-1.0.0/pca.go` find the eigenvalue-sorting comparator (a `Less`/`sort.Slice` closure) and check its inequality — if it currently keeps ascending order it will discard the high-variance components instead of the noise, so flip the comparator direction to descending. Don't touch anything else in the file; a minimal single-operator fix is expected. Build the Go app in `/home/user/pipeline` with a `go.mod` that declares your module and adds a `replace github.com/ds-tools/gonomaly => /app/gonomaly-1.0.0` directive, then `go get`/`go mod tidy` and import normally. Implement the HTTP `/clean` handler to decode `{"data": [[float64]]}`, call `gonomaly.CleanDataset(data, 2, 0.95)`, and re-encode as `{"cleaned_data": ...}`. Run the TCP listener on 8081 in a separate goroutine reading exactly the line `STATUS auth_token=secret_123\n` and replying `OK\n`. Start the binary in the background (e.g. `nohup ./pipeline &`) and confirm both ports are listening before finishing.
