Start by reading src/main.rs in /app/cypher_analyzer to understand the current (broken) parsing approach — it hardcodes fixed character offsets assuming single-digit IDs, so any multi-digit id breaks it. Replace that with robust extraction: either the `regex` crate to match the pattern `id: (\d+)` twice per line, or manual parsing via `.split(...)` on known delimiters (e.g. split on "id: " then take digits up to the next non-digit char) — the key fix is scanning for the run of digits rather than assuming a fixed column. Add the missing `serde` and `serde_json` dependencies to Cargo.toml (with the `derive` feature for serde if you use structs) so it compiles. For each MATCH line, increment `out` for n1's id and `in` for n2's id in a `HashMap<String, (u32,u32)>` (or a small struct), reading stdin until EOF (`io::stdin().lines()` or similar), then serialize to compact JSON with serde_json — verify against a couple of hand-computed examples that in/out counts match before trusting it. Build with `cargo build --release` and copy/symlink the resulting binary to /home/user/cypher_analyzer_fixed, then sanity-test with multi-digit and large IDs since the fuzz harness draws from [1, 9999].
