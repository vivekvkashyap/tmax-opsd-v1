Write a Python script that processes `/home/user/datasets/*.zip` one at a time. Use `zipfile.ZipFile` and call `.testzip()` (or wrap open/read in try/except `zipfile.BadZipFile`) to detect corruption — a zip that fails to open or whose `testzip()` returns a non-None bad-file name should be skipped entirely, not included in the output at all (not even with an empty list). For each valid archive, extract `measurements.bin` into a directory created with `tempfile.TemporaryDirectory()` (which auto-cleans on exit) or `tempfile.mkdtemp()` plus explicit `shutil.rmtree` in a `finally`. Open the extracted file, `mmap.mmap(fd, 0, access=mmap.ACCESS_READ)`, and scan for every occurrence of `b'ANOMALY_778'` using repeated `.find(needle, start)` calls, collecting the starting offset each time and advancing `start` past each match (advance by 1 or by len(needle) — decide based on whether overlapping matches are plausible here, they aren't). Build the `{filename: [offsets...]}` dict only from valid archives. Write it atomically: dump JSON to a temp file in the same directory via `tempfile.NamedTemporaryFile(delete=False, dir=...)`, then `os.replace(tmp_path, "/home/user/anomaly_report.json")`.