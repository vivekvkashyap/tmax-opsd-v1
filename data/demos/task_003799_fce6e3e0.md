Two independent subtasks. First, fix the vendored networkx package: inspect `/app/networkx-2.8.8/networkx/classes/digraph.py` around the `add_node` method signature for a syntax error — a common perturbation is a missing colon at the end of the `def` line — fix it, then run `pip install -e /app/networkx-2.8.8` and sanity-check with `python3 -c "import networkx as nx; g=nx.DiGraph(); g.add_node(1); print(g.nodes())"` before moving on. Second, write `validate_backup.py`: load the JSON edge list, build a `networkx.DiGraph()` by adding each `(source, target)` edge, then call `nx.is_directed_acyclic_graph(g)` (do not roll your own cycle detector — networkx's DFS-based check is the reliable, exact primitive here). On a cycle, print "REJECTED" and `sys.exit(1)`; on a valid DAG, print "ACCEPTED", `sys.exit(0)`, and only then open/create a SQLite connection to `/home/user/backups.db`, `CREATE TABLE IF NOT EXISTS edges (source TEXT, target TEXT)`, and insert the edges, committing before exit. Test against small hand-built cyclic and acyclic JSON samples yourself before trusting the hidden corpora — edge cases like self-loops (A->A) and disconnected components must both be handled correctly by the DAG check.
