Handle the two phases independently. For Phase 1, write `/home/user/nginx_alert.conf` with a `server` block listening on `127.0.0.1:8080` that `proxy_pass`es to `127.0.0.1:3000`, then launch nginx directly with an explicit config/prefix (e.g. `nginx -c /home/user/nginx_alert.conf -p <writable-prefix>`) since you have no root/systemd — you don't need the backend actually running yet, just the proxy listening and configured correctly. For Phase 2, treat `/app/legacy_alerter` as a black box: write a small script that calls it with many combinations of the 5 integer args (0-100 each) and records input->output pairs, starting with simple sweeps (vary one argument at a time while holding others at 0) to isolate how each metric affects the CRITICAL/WARNING/OK outcome, then look for threshold behavior (a weighted sum crossing cutoffs) rather than assuming independent per-field rules. Use `strings`/`objdump -d` on the binary to look for comparison constants if black-box sweeping doesn't fully pin down the boundaries. Once your hypothesis reproduces every sweep result, implement it in `/home/user/alerter.py` reading 5 CLI args and printing the matching severity string, then validate by comparing its output against the real binary across a large batch of random inputs (not just your sweep set) before finishing, since the verifier will fuzz thousands of cases and any single mismatch fails.