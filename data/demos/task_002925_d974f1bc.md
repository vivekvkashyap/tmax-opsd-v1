Profile first, don't guess: run `python -m cProfile -s cumulative mcmc_mutations.py` (or `line_profiler`) to confirm the O(n²) pure-Python nested loop in `compute_distances` dominates runtime, then write that exact function name to bottleneck.txt. Vectorize it with NumPy by converting each sequence to a byte/char array and computing pairwise Hamming distances via broadcasting (e.g. compare `seqs[:,None,:] != seqs[None,:,:]` and sum over the last axis) instead of Python double loops. For the underflow bug, replace `likelihood` with a `log_likelihood` that sums `d*log(mu) + (L-d)*log(1-mu)` over sequence pairs, and change the MCMC accept step to compare `log(uniform(0,1)) < (ll_proposal - ll_current)` rather than dividing raw probabilities — this is the standard log-space Metropolis-Hastings fix and avoids exact `0.0` ratios. Leave the random seed, prior bounds, proposal distribution, and iteration count untouched so the sample path stays identical to the intended reference. After fixing both issues, run the script once, take `mean(samples)`, round to 4 decimals, and write it to posterior_mean.txt.
