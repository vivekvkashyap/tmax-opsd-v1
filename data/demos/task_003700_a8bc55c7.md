Work the pipeline in stages and verify each before moving on. First, run `tesseract /app/evidence.png stdout` to recover the password prefix text, cleaning any stray whitespace/newlines. Then brute-force `/home/user/vault.zip` by looping candidate passwords of the form `<prefix>_00` through `<prefix>_99` (e.g. with `unzip -P "$cand" -o vault.zip secret_data.txt` in a loop, checking exit status) rather than guessing manually. After extraction, confirm integrity with `sha256sum secret_data.txt` compared against `/home/user/hash.txt` before trusting the file. For the server: generate a self-signed cert/key with `openssl req -x509 -newkey rsa -nodes -keyout key.pem -out cert.pem -subj "/CN=localhost" -days 365`, then launch `socat openssl-listen:8443,cert=cert.pem,key=key.pem,verify=0,fork,bind=0.0.0.0 EXEC:handler.sh` in the background. In the Bash handler, read the raw HTTP request, extract the Bearer token, split on `.`, and base64-decode the header/payload segments — remember JWT uses base64url (translate `-`/`_` to `+`/`/` and pad with `=` before decoding). Strip whitespace before checking for `"alg":"none"` and `"role":"admin"`, then emit the correct status line and body accordingly.
