Locate the exact failing line in `/app/vendor/biomatrix-0.1.0/biomatrix/solver.py` — it's a direct `np.linalg.cholesky(matrix)` call. Apply Tikhonov regularization by adding `1e-5` to the diagonal before decomposing, e.g. `L = np.linalg.cholesky(matrix + np.eye(matrix.shape[0]) * 1e-5)` — do this via a proper diagonal-add, not an ad hoc epsilon elsewhere, so the fix generalizes to any matrix size. Reinstall the package (`pip install -e /app/vendor/biomatrix-0.1.0` or equivalent) so the fix takes effect in your Python environment, then write a script that iterates `seq_0..seq_49` in `/app/data/spectra.h5` via `h5py`, calls `factorize_signal` on each, sums `np.trace(L)`, and appends `seq_N: <trace>` lines to `/home/user/results.txt`. For the HTTP service, implement a minimal server (e.g. Python's `http.server` with a custom handler, or `flask`/`fastapi`) bound to `127.0.0.1:9090` that parses `GET /trace/<seq_id>`, checks the `Authorization: Bearer secret-bio-token` header exactly, returns the precomputed trace as plain text on success or a `401` otherwise. Wire everything into `/home/user/run_pipeline.sh` which installs the package, runs the processing step, then backgrounds the server (`&`) and returns while it stays alive — confirm with `curl` before finishing.