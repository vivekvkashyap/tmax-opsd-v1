Recover the credential first, in isolation: run `tesseract /app/evidence.png stdout` (try `--psm` variants if handwriting confuses default segmentation) and manually verify the extracted string looks like a plausible password before hardcoding it into server logic. For the C server, the simplest robust approach is a minimal blocking HTTP server over raw sockets (bind/listen/accept on 127.0.0.1:8080) that reads the request line and headers, parses the path and method, and for `POST /login` reads the `Content-Length` body and parses `username=...&password=...` form-encoding, comparing against `"admin"` and the OCR'd password string exactly. On success, construct a JWT-looking string (`base64url(header).base64url(payload).signature`) and set it in an `Authorization` response header. For `GET /flag`, parse the incoming `Authorization` header, split on `.`, base64url-decode the first segment, and check whether it contains `"alg":"none"` (a simple substring check on the decoded header JSON is sufficient and matches the deliberately-vulnerable spec) — if so, skip any signature verification entirely and respond with body `ACCESS_GRANTED`. Compile with `gcc -o /home/user/auth_server auth_server.c`, launch it in the background (e.g. `nohup ... &`), and test both endpoints manually with `curl` before considering it done.