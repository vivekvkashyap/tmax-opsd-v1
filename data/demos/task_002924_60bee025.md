First read `/home/user/graph_etl.c` carefully to locate the edge-loading loop; the bug is that after computing `u` and `v_actual` for a parsed edge, the code loops `for (int v = 0; v < node_count; v++) adj[u][v] = weight;` instead of assigning only `adj[u][v_actual] = weight;` — fix it to a single direct assignment (no loop over all nodes) so each edge only connects its actual source and target. Be careful: since `node_count` grows as new nodes are discovered, the bug's effect also depends on how many nodes were known at the time each edge line was parsed, so don't assume you can predict its broken output by hand — just fix the loop structure and confirm Dijkstra now produces sane, monotonically-consistent distances from the start node. After fixing, compile with gcc, run `./graph_etl /home/user/edges.txt A`, and pipe its `Node\tDistance` stdout through `sort -k2,2n -k1,1` to get distance-ascending order with alphabetical tiebreak, saving to `/home/user/output.tsv`. Verify by manually tracing Dijkstra on the small edge list to sanity-check a couple of the resulting distances before finalizing.
