Start by reading the Nginx `upstream` block to confirm it points at the wrong socket, then edit it in place to reference the actual backend socket path given in the task. For the log-parsing script, don't try to write one giant regex — process the file line by line, first filtering with `grep` for lines containing all three required markers, then use `awk`/`sed` to pull out the leading date+time (first two whitespace-separated fields), the client IP (text following `client:` up to the next comma), and the upstream value (text inside the quotes following `upstream:`). Build the output line by concatenating these into the exact required format `[DATE TIME] Client: IP - Upstream: PATH` — pay close attention to matching brackets, spacing, and the literal `- Upstream:` separator since the grader likely does an exact string/line comparison. Test the script against the sample log line shown in the task description before running it against the real target log, and remember to `chmod +x` the script since it will be invoked directly as an executable with one argument.
