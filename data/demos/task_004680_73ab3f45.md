Implement the pipeline as three clear stages in the Go program. First, read the CSV and filter rows in memory, keeping only rows with `sensor_status == "OK"` and `altitude_m >= 0` (do not accidentally exclude altitude exactly 0). Second, transform to the linearized model by taking `y = ln(pressure_hpa)` and `x = altitude_m`, then compute an ordinary least-squares fit using the standard closed-form slope/intercept formulas (`slope = (n*Σxy - Σx*Σy)/(n*Σx² - (Σx)²)`, `intercept = (Σy - slope*Σx)/n`) over the filtered points — do this with a numerically straightforward summation loop rather than a library that might round differently. Recover `H = -1/slope` and `P0 = exp(intercept)`, and round both to exactly 2 decimal places for the JSON output, matching the key names and format given. Third, sort the filtered rows ascending by `altitude_m`, and for each row compute `predicted_pressure = P0 * exp(-altitude_m/H)` using your fitted (unrounded, full-precision) P0/H internally, formatting both pressure columns to 2 decimal places in the output CSV with the exact header specified. Run the program once and inspect both output files against your own hand recomputation before finishing.