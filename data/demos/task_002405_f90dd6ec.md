Sequence the script exactly as specified since fuzzing will check ordering and edge cases. First, `mkdir -p "$DEST"` then `exec 9>"$DEST/.config.lock"` and `flock 9` (blocking, held for the script's whole lifetime via the open fd). Second, compute the deployment id N by running ffmpeg's scene-detection filter against `/app/deploy_logs.mp4`, e.g. `ffmpeg -i in.mp4 -vf "select='gt(scene,0.4)',showinfo" -f null - 2>&1 | grep -c showinfo` (or parse the `pts_time` lines from `showinfo`), and use that counted integer as N — do not hardcode a guessed value, derive it from the actual video each run. Third, iterate `tar -tf "$ARCHIVE"` entries; for each, normalize the path (resolve `..` and check for a leading `/`) to decide safe vs quarantine destination, extract with `tar -xOf` piped to a file write at the computed path, lowercase the basename, replace spaces with underscores, and prefix with `deploy_${N}_`. Finally, for any resulting `.conf` file, `iconv -f ISO-8859-1 -t UTF-8` and strip `^\s*#` lines in place with `sed -i`.