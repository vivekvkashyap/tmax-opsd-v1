Build and start each layer independently, verifying it works in isolation, before wiring them together. First, write a minimal Python HTTP server backend that on `POST /upload` writes the body to a uniquely-named file in its node directory and returns 200; launch three instances on 8081/8082/8083 and confirm each with a manual curl POST. Second, configure nginx as a reverse proxy on port 8080 with an `upstream` block listing all three backends, confirming round-robin via repeated curls before adding eviction logic. Third, write `/home/user/operator.py` to loop every 1-2 seconds: read `/home/user/fstab_sim` for directory/port/quota mappings, measure each node directory's size (`du -sb` or summed file sizes), and once usage exceeds 80% of quota, rewrite the nginx upstream config to drop that server and run `nginx -s reload` — a graceful reload, not a restart, since a hard restart is the likeliest cause of dropped connections under load. Poll often enough that no directory can cross 100% between checks given expected write rates. Test manually with curl bursts to confirm eviction and reload behave correctly before running the full adversarial load test.
