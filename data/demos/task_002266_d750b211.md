First run OCR on `/app/audit_policy.png` (e.g. `tesseract /app/audit_policy.png stdout`, upscaling/thresholding the image first if OCR is noisy) to recover the exact Minimum Transfer Amount and Maximum Traversal Depth values — read the extracted text carefully rather than guessing, since the image may contain distractor/noise text you must ignore. In your C++ program, connect via `sqlite3_open` and issue traversal queries with `SELECT target FROM transfers NOT INDEXED WHERE source = ? AND amount >= ?` (or explicitly `DROP INDEX idx_transfers_source` at startup) so the corrupted index is never used — verify this matters by comparing query results with and without `NOT INDEXED`. Implement BFS layer-by-layer up to the recovered max depth, using a `visited` set keyed by account id so nodes aren't revisited, and exclude the start id from the final result set. Sort the collected unique ids in descending order, then apply `offset`/`limit` slicing (returning `[]` if offset exceeds the count) before emitting the exact JSON schema with `nlohmann::json` or manual string building — match field names, types, and ordering precisely, and print nothing else to stdout since your binary is fuzz-tested against a hidden oracle across many random inputs.
