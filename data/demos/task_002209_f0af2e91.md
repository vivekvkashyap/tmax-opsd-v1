Read the raw TSV byte-by-byte (not as UTF-8 text) since the Metadata column can contain non-ASCII/Latin-1 bytes that would break naive string parsing in C++ — treat each byte and replace any value outside the printable ASCII range (>127 or <32, keep tab/newline delimiters themselves) with '?'. Parse hour/value/metadata into a struct, then implement gap-filling: iterate every integer hour from the min to max hour seen, and for hours not present in the input, carry forward the previous hour's value and metadata (a simple "last seen" running state as you iterate the sorted rows works well). Accumulate min/max/mean over the gap-filled series, and format doubles with exactly 2 decimal places using `std::fixed << std::setprecision(2)` (or printf `%.2f`) — the last record's cleaned metadata goes in the report verbatim. Compile with the exact `g++ -O2 ... -o process_data` command given, run it once to produce `/home/user/report.txt`, then add the crontab entry with `crontab -l | { cat; echo "15 3 * * * /home/user/process_data"; } | crontab -` and verify with `crontab -l`.
