Work through each requirement as a discrete, independently-checkable step. Extract with `mkdir -p /home/user/deployments && tar -xzf /home/user/backup.tar.gz -C /home/user/deployments`, then create the symlink with `ln -s /home/user/deployments/app-v2 /home/user/deployments/live` (use an absolute target path, and verify with `readlink`). For storage monitoring, use `find /home/user/deployments -type f -size +100k -exec realpath {} \;` (note: `find -size` uses 512-byte blocks by default, so use the `k` suffix for kilobytes, and double check the >100KB boundary against `ls -la` on borderline files) and redirect to large_files.log, one absolute path per line. For credentials, use `openssl passwd -apr1 'staging_pass_2024'` to generate the MD5-APR1 hash and write `backup_admin:<hash>` to proxy_users.txt (avoid htpasswd if it's not installed; openssl is more likely available without root). For the proxy script, write a bash script using `ncat -l 8080 -k -c "ncat localhost 9090"` (or an equivalent socat TCP-LISTEN with fork, or a while-true nc loop) so it keeps accepting new connections, then `chmod +x` it — but do not execute/start it, only leave it ready. Double check each output file's exact path and format against the task text before finishing.