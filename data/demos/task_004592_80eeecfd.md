Work through each subtask methodically and verify before moving on. For the hash: this is a plain MD5 crack against a small provided wordlist, so hash each candidate word from the wordlist with `md5sum` (or python's hashlib) and compare to the target hash in admin.hash rather than trying any brute force — write only the matching plaintext (no extra whitespace/newline framing beyond what's asked) to the output file. For the redirect fix, rewrite `get_redirect_url` to explicitly reject anything that isn't a single-leading-slash relative path: check the string starts with `/` but does not start with `//`, and handle `None`/empty by falling through to the `'/home'` default — write it as explicit conditionals rather than a regex you're not sure covers protocol-relative URLs. For permissions, use `chmod 600` on the config file so only the owner can read/write. Finally, build the audit log by reading back your own artifacts in order (cracked password, then `stat -c '%a'` or equivalent for the octal permission bits of config.json, then `sha256sum` of the modified login.py, taking just the hex digest) — verify each value programmatically rather than retyping it by hand to avoid transcription errors.