Build this as a clear pipeline of stages, verifying each with print-debugging against the small sample data before trusting it on larger inputs. Parse both CSVs (a `csv`-aware crate or manual split is fine) and convert bus timestamps to Unix seconds via `chrono::DateTime::parse_from_rfc3339` or similar. Find `T_min`/`T_max` from the bus data, then generate the sequence of 10-second-aligned timestamps within `[T_min, T_max]` — align to the nearest interval boundary at or after `T_min` (i.e., round up to the next multiple of 10 relative to epoch, unless `T_min` already falls on one) and stop at or before `T_max`. For each aligned `T`, binary-search or linear-scan the sorted bus pings to find the bracketing `T_prev <= T <= T_next` and apply the given linear interpolation formula to both x and y independently; handle the exact-match case explicitly to avoid division by zero when `T_prev == T_next`. For each interpolated point, compute Euclidean distance to every target, keep only those `<= 50.0` (not `< 50.0` — read the filter direction carefully, since the task says "strictly greater than 50.0" is excluded, meaning exactly 50.0 is kept), round to 2 decimals, and emit SQL insert lines in chronological, deterministic order. Run the binary and diff the output file's sorted contents against your own hand-derived expectation for the sample data before considering it correct.