Sequence this as restore-infra, then exploit, then reverse-engineer — verify each phase independently. Phase 1: run `/app/start_services.sh`, generate a self-signed cert with `openssl req -x509 -newkey rsa:2048 -nodes -keyout /app/key.pem -out /app/cert.pem -days 365 -subj '/CN=localhost'`, edit `/app/nginx.conf` to `listen 8443 ssl;` with the cert/key paths, `proxy_pass http://localhost:5000;`, and `add_header Content-Security-Policy "default-src 'self';";`, then start nginx and confirm with `curl -k -I https://localhost:8443/` that both the status and CSP header appear. Phase 2: probe `/render?tmpl=` for SSTI (Jinja2-style `{{7*7}}` to confirm evaluation, then a payload that reads `/app/secret_seed.txt`, e.g. via `{{ self.__init__.__globals__... }}` or `open()`/`config` tricks depending on what's exposed) to recover the secret. Phase 3: do NOT try to disassemble the oracle binary — treat it as a black box and reverse-engineer by feeding it known hex inputs via stdin and diffing outputs bit-by-bit against your hypothesis (XOR with the seed bytes cyclically, then a left bitwise rotate) until your Python reimplementation matches exactly across several probe inputs; only then trust it against the hidden fuzz corpus. Make your script executable and read the seed file the same way the oracle does.
