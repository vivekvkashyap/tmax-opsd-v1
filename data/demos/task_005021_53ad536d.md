Start by carefully reading `/home/user/auth_parser.c` line by line, tracing exactly how it splits the token on `:` with `strtok` and where it checks the algorithm, signature, and role fields — look specifically for any algorithm value that causes the signature comparison branch to be skipped entirely (a classic "alg=none" style bypass), and separately note that the username is printed straight into the log with no escaping. Construct the forged token string using that bypass algorithm value, an `admin` role, and the required username payload placed in the correct field position per the `ALG:USERNAME:ROLE:SIGNATURE` format (trailing signature field can be empty). Write `/home/user/forge.c` to simply `printf` that exact literal string with no newline, compile with `gcc -o /home/user/forge /home/user/forge.c`, then verify by running `./forge | /home/user/auth_parser` and confirming the "Access Granted" line appears. Compute the checksum with `printf '%s' "$TOKEN" | sha256sum` (use `printf`, not `echo`, to avoid an extra newline affecting the hash), and assemble the three-line report exactly as specified.
