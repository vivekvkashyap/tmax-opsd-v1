Start by reading `/home/user/project/logs/service_a.log` — the change note there tells you the new serialization rule: the 1-byte length prefix now counts UTF-8 *characters*, not bytes. That is the whole bug. The current `decoder.cpp` reads exactly `len` bytes, so any multi-byte character (e.g. an accented letter encoded as two bytes) leaves leftover bytes in the stream, which get misread as the next record's length prefix and desync everything. Fix: after reading the 1-byte length `n`, consume `n` full UTF-8 *code points*, not `n` bytes. Do this by inspecting each lead byte and appending its continuation bytes: 0xxxxxxx = 1 byte, 110xxxxx = 2, 1110xxxx = 3, 11110xxx = 4; loop until you have collected `n` characters, appending all their bytes to the output string. Then print `Read: <string>` per record. Verify with `make test` in `/home/user/project/` and check `/home/user/project/output.txt` has one clean line per record with no garbage. Trap: treating the length as a byte count, or hardcoding a single 2-byte assumption instead of decoding the UTF-8 lead byte properly.