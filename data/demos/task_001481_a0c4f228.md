Start the workload generator in the background first (`python3 /home/user/trigger_db.py &`), then identify the active WAL by watching file modification times/sizes in `/home/user/wals` over a short interval (e.g. snapshot `os.stat().st_mtime` or size for each `.wal` file, sleep a couple seconds, snapshot again, and see which one changed) rather than guessing from content — this is a live-appended file so its size/hash will visibly grow. For the remaining (inactive) files, open each with Python's `mmap` module read-only and manually walk the binary layout per the spec: skip the 32-byte file header, then read the big-endian uint32 at header offset 8 as the page size, and loop over frames — each is a 24-byte frame header (whose first 4 bytes, big-endian uint32, is the page number) followed by exactly `page_size` bytes of page data — advancing your offset by `24 + page_size` each iteration until you hit EOF. Flag a file as a purge candidate if any frame's page number equals 999. Collect qualifying filenames, sort them alphabetically, and write the exact JSON shape requested to `/home/user/report.json`. Use `struct.unpack_from(">I", mm, offset)` for the big-endian reads rather than manual byte arithmetic.
