Tackle the three sub-problems independently and verify each before moving on. For the dependency conflict, run `pip install -r requirements.txt` (or `pip-compile`) to see the actual resolver error, then loosen or align the pinned versions (e.g. the pin forcing `urllib3<1.26` conflicts with a `requests` version that needs `urllib3>=1.26`) so `pytest` can install and run cleanly — don't just delete requirements, fix the version constraint that's incompatible. For the arithmetic divergence, compare `processor.py` against the legacy binary's behavior on a few sample values via `echo "<val>" | /app/legacy_oracle`: the mismatch is C's 32-bit signed integer overflow, which Python's arbitrary-precision ints don't reproduce. Fix this by masking every intermediate result to 32 bits and re-interpreting as signed (`x & 0xFFFFFFFF`, then subtract `2**32` if the top bit is set) after each arithmetic op that could overflow, not just at the end. For `regression.py`, generate 10,000 random ints in the 32-bit signed range, run them through both your `process_signal` and `subprocess.run(["/app/legacy_oracle"], input=..., capture_output=True, text=True)`, and assert list equality — use this script yourself to iterate until outputs match exactly before considering the fix complete.