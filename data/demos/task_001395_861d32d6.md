Work the three phases independently and verify each before moving on. For the vendored `pexpect` package: open `setup.py` and look for an injected guard near the top (an `if`/`raise` checking an environment variable like an install token) — either remove that conditional raise or export the environment variable it checks, then `python -m venv /home/user/venv && /home/user/venv/bin/pip install /app/pexpect-4.8.0` fully offline (no `--index-url`, no network calls), and confirm with `/home/user/venv/bin/python -c "import pexpect; print(pexpect.__version__)"`. For the sanitiser, implement each rejection rule as an explicit, independent check (regex for username charset/length, path-traversal substring and prefix check for home_dir, exact-match allowlist for shell) rather than a single loose regex, since the grading corpus is adversarial and will probe edge cases like `..\\` variants and boundary lengths — test locally against a few handmade clean/evil JSON files before trusting it. For the cron fix, never rely on inherited shell state: use fully-qualified paths for the venv Python and the log file inside the cron line itself, create the log directory with correct permissions first, then load it with `crontab <file>` and confirm via `crontab -l`.