Build this as a strict pipeline where each stage's output feeds the next unchanged. Use `cv2.VideoCapture` to iterate frames in order, convert each with `cv2.cvtColor(frame, cv2.COLOR_BGR2GRAY)`, and take `.mean()` for intensity, pairing it with a zero-based frame index. Populate an `rdflib.Graph` with a `Namespace("http://example.org/video#")`, adding `hasIndex`/`hasIntensity` literal triples (typed as `XSDInteger`/`XSDFloat`) and `hasNext` links between consecutive frame resources — this is a required intermediate representation, so don't skip straight to a DataFrame. Run an actual SPARQL `SELECT` query via `graph.query()` to pull `frame_index`/`intensity` pairs back out, then load results into a `pandas.DataFrame`, sort by `frame_index`, and compute `.rolling(window=5).var()` — note pandas' rolling `.var()` defaults to sample variance (ddof=1), so don't substitute a manually-written population-variance formula. Drop rows where the rolling variance is NaN (the first 4), and write only `frame_index` (int) and `rolling_variance` (float) columns to `/home/user/results.csv`. Since grading compares against a reference via MSE, match column types and row count precisely rather than approximating.
