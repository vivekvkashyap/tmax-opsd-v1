Define the ABI in the header exactly as specified — `RecordV1{int id; char name[20];}` and `RecordV2{int id; char name[20]; float score;}` — since Python `ctypes` will mirror this layout and any padding/order mismatch breaks interop; implement `migrate_v1_to_v2` as a straight field copy (`memcpy` or direct assignment for `id`/`name`, `out->score = 0.0f`). Compile as a shared library with position-independent code: `gcc -shared -fPIC -o /home/user/libmigrate.so migrate.c`. In the Python test, load it with `ctypes.CDLL(...)`, define matching `ctypes.Structure` subclasses field-for-field, and set `argtypes`/`restype` on the function explicitly so ctypes marshals correctly. For the property-based test, use `hypothesis.strategies.integers()` for `id` and `st.binary(min_size=0, max_size=19)` for `name` (leave room for the implicit null terminator within the 20-byte buffer), construct a `RecordV1`, call the function, and assert the output `id`/`name` round-trip exactly and `score == 0.0`. Run `python3 -m pytest test_migrate.py > /home/user/test_result.log` and confirm the log shows a passing run before finishing.
