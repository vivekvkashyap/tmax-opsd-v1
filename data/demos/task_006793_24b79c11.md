Break this into independently testable layers and verify each before moving to the next. First read `legacy_emulator.py`, port its postfix-stack `evaluate()` to Python 3 (the only real Py2→3 gotcha here is `print` statement vs function), and wrap it in a minimal `http.server`-based POST handler at 127.0.0.1:9090 that reads the JSON body, calls evaluate, and returns `{"result": <int>}`; start it in the background and curl it directly first to confirm it works before touching nginx. For nginx, since you're unprivileged, point `pid`, `client_body_temp_path`, `proxy_temp_path`, etc. all under `/tmp/`, and add a `proxy_pass http://127.0.0.1:9090;` location block for `/execute` listening on 127.0.0.1:8080 — launch with `nginx -c /home/user/nginx.conf` and check `nginx -t` first to catch config errors before starting. Write the `.proto` file matching the exact message/service/field-number spec given (proto3 syntax, package emulator, ExecuteProgram rpc). Finally write `test_system.sh` to curl the nginx endpoint with the specified payload and redirect the response body to the log file, then actually run it once so the log exists.
