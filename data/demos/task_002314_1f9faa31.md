Write `/home/user/orchestrator.py` to first walk `/home/user/monorepo` and load every `manifest.json` it finds into a dict keyed by project name (don't hardcode names). Build a dependency graph from the `deps` lists and compute a topological order yourself (e.g. repeated pass picking nodes whose deps are all already scheduled, or Kahn's algorithm with an in-degree count) rather than assuming any particular directory-listing order is correct — this is the step naive attempts skip and it silently breaks when a project's deps aren't alphabetically or lexically earlier. For each project in that order, `subprocess.run` each of its `build_steps` with `cwd` set to that project's own subdirectory (critical — the build steps use relative paths like `../other_project/output.json`), and check each command's return code before moving to the next project. After all builds succeed, read each project's generated `output.json` and assemble `/home/user/final_report.json` as `{project_name: parsed_output_dict, ...}` for every project. Run the script and confirm the final JSON has one key per manifest found.
