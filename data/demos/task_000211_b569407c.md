Work this in two independent tracks: (1) get the exploit working in any language first so you know the ground-truth checksum, then (2) reimplement the whole pipeline in Rust for speed. For extraction, use `ffmpeg` to dump each frame as a raw image, crop the exact center 10x10 pixel block of each frame, threshold each pixel as black=1/white=0 in row-major order, concatenate all frames' 100-bit blocks in frame order, and convert the resulting bitstream to ASCII to recover the JWT — do this analysis once manually/in a scratch script to confirm the token structure (three base64url segments) before hardcoding any extraction logic. For the forgery, decode the JWT's header and payload, change `"alg"` to `"none"`, change the role claim to `admin`, base64url-encode both segments, and join them with dots leaving the signature segment empty (trailing dot, no signature bytes) — many `alg:none` bypass implementations fail because they still append a colon or non-empty third segment. Send the forged token as `Authorization: Bearer <token>` to `/evidence` and capture the returned checksum. Only after confirming correctness should you port frame extraction, JWT crafting, and the HTTP call into a Rust binary at `/home/user/fast_exploit`, using `cargo build --release` and lightweight crates (e.g. minimal HTTP client, no heavy image libs) to beat the Python baseline's runtime — avoid spawning `ffmpeg` per-frame if you can decode once and slice in memory, since subprocess overhead is often the real bottleneck.
