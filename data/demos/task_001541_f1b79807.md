Build this incrementally and test each stage before wiring them together. First, write the WAV parsing in C: read the RIFF/fmt/data chunk headers to locate the PCM sample data (don't hardcode a fixed header offset — walk the chunk IDs since header size can vary), then read 16-bit signed samples in blocks of 16,000 (one second at 16kHz) and compute RMS as sqrt(mean(sample^2)) per window, handling a final partial window correctly or per the spec. Write `window_index,rms_value` to `/home/user/rms_features.csv` and sanity-check a few values by hand against a quick Python/numpy reference computed from the same WAV file. Second, parse `/app/data/labels.csv`, join on `window_index` with your RMS features, and implement Pearson correlation manually in C using the standard formula (sums of x, y, xy, x², y²) rather than assuming a library exists — validate this against `scipy.stats.pearsonr` or `numpy.corrcoef` on the same paired data as a cross-check. Third, implement the TCP server with raw sockets (`socket`/`bind`/`listen`/`accept`), read the auth line and compare it byte-for-byte against the exact required token before reading the command line, and format the final response with `%.4f` matching `RESULT: 0.7654\n` exactly. Compile with `gcc pipeline.c -o pipeline -lm`, run it in the background, and test with a manual `nc localhost 9000` session before considering it done.