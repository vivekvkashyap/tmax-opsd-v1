Treat the buggy binary as a black box you must wrap, not fix — you cannot recompile it. Use `socat TCP-LISTEN:8000,bind=127.0.0.1,reuseaddr,fork SYSTEM:/home/user/handler.sh` (or similar) so each connection is handled independently and half-close/EOF from the client is respected. In the handler, read stdin line by line, drop any line containing `CORRUPT` before it ever reaches the binary (this is the key defense against the infinite-loop bug), then batch the remaining lines into groups of at most 10, invoking a fresh instance of `/app/log_parser` per batch (feeding its stdin and closing it so it sees EOF, since the binary crashes past 20 lines cumulative — batching resets that internal state each time). Concatenate each batch's stdout in order and write it back to the socket, then let the connection close cleanly. Test incrementally: first confirm the binary's line-in/line-out behavior manually with a small batch, then verify your batching+filtering logic with a payload that deliberately mixes corrupt lines and enough clean lines to exceed 20 total, exactly as the grader will.
