This is a fully deterministic simulation, so exact adherence to the stated order of operations matters more than algorithmic cleverness. In your Cargo project, add `rand`, `rand_chacha`, `rand_distr` as pinned to the versions given, seed `ChaCha8Rng::seed_from_u64(42)` once at program start, and build a single `Normal::new(500.0, 20.0)` distribution reused across draws — reseeding or recreating the RNG mid-loop will desync the sequence from what the grader expects. Run Euler's method exactly as specified: compute `decay_A`/`decay_B` from the *current* A/B before updating either (don't update A in place then use the new A for B's decay), for t=0..59 (60 steps). Each step, draw `floor(decay_B * 0.1)` photon energies *in a fixed inner loop right after computing decay_B for that step* (order across steps matters for the RNG stream), bin only those within [400,600) into 5keV-wide bins via `((energy-400.0)/5.0).floor() as usize`, and write all 40 bins (including zeros) as `bin_start,count` lines in ascending order. Compile and actually run the binary to produce `spectrum.csv` — don't hand-derive the numbers.
