Implement this as a straightforward linear-algebra-in-C pipeline, and validate each stage's shapes before moving to the next. Parse `input.csv` (100x10), `weights.csv` (10x3), and `bias.csv` (1x3) with simple CSV-splitting in C (fgets + strtok/strtod), storing them in flat arrays or 2D arrays. For each row, compute `z = x*W + b` by looping over the 3 output dims and summing the 10 products (double-check matrix orientation: x is 1x10, W is 10x3, so z_j = sum_i x_i * W[i][j] + b[j]) then apply `if (z<0) z=0` for ReLU to get the 3-d embedding. Store all 100 embeddings, then compute Euclidean distance from embedding 0 to every other embedding (skip index 0 itself), and find the 5 smallest distances — use a simple approach like storing (index, distance) pairs and sorting, or a partial selection algorithm, being careful with tie-breaking and float precision to match a numpy-based reference implementation. Compile with `gcc -lm`, run it, and write exactly 5 lines of 0-based indices (closest first) to `/home/user/top5.txt`.
