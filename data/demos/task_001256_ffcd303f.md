For Part 1, extract frames from `/app/server_monitor.mp4` at 1 fps with `ffmpeg -i server_monitor.mp4 -vf fps=1 frame_%03d.png`, then for each frame compute the mean R, G, B channel values (ImageMagick's `convert frame.png -format "%[fx:mean.r*255] %[fx:mean.g*255] %[fx:mean.b*255]" info:`, or a quick Python/PIL/numpy average) and count frames satisfying R>150 and G<50 and B<50; write that integer count to `/home/user/red_alert_count.txt`. For Part 2, write `/home/user/validate_configs.sh` in pure bash/awk/sed/grep (no Python/R): parse `KEY=VALUE` lines, detect duplicate keys via an associative array (`declare -A seen`) or by comparing sorted-unique keys against all keys with `awk`; detect the forbidden Unicode code points (U+200B zero-width space, U+202E/U+202D bidi overrides) by grepping for their raw UTF-8 byte sequences (e.g. `grep -P '\xe2\x80\x8b|\xe2\x80\xae|\xe2\x80\xad'` if `grep -P` is available, or matching hex via `od`/`iconv`); reject unescaped shell metacharacters like backticks or `$(` in values with a regex; log each verdict via `date` timestamp appended to `/home/user/pipeline.log`. Validate both detection paths thoroughly against hand-crafted edge cases before running against the hidden clean/evil corpora, since the pass bar is 100% on both sides.