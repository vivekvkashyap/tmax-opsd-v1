Handle the four sub-tasks in order, verifying each before moving to the next. For the rogue service: `ss -tlnp` (or `netstat -tlnp`) filtered to the 8000-9000 range to find the listening PID, then `kill` it. For the SUID binary: `find /var/tmp -perm -4000 -type f` (add `-name '.*'` or check hidden files explicitly since it's described as hidden) and `chmod -s` it. For SSH: edit `/etc/ssh/sshd_config`, find the `PermitRootLogin` line currently allowing passwordless root, and change its value to `prohibit-password` or `no`. For the decryptor, OCR `/app/ransom_note.png` (e.g. `tesseract ransom_note.png -`) to recover the exact byte-level encryption steps and their order and operand values — do not assume or invent the operations. Once you have the encryption sequence (a chain of ADD/SUB/XOR-style byte ops), build the decryptor as the exact mathematical inverse: reverse the order of operations and invert each one (ADD becomes SUB, XOR stays XOR since it's self-inverse, applied in reverse sequence). Implement it in C++ reading raw bytes from stdin one at a time via `cin.get()`/`std::cin.read()`, casting to `unsigned char` so arithmetic wraps modulo 256 naturally, and writing each transformed byte to stdout — avoid `signed char` or `int` without masking, which breaks wraparound. Compile to `/home/user/decryptor` and sanity-test by encrypting then decrypting a known short byte string yourself before trusting it against the fuzz harness.
