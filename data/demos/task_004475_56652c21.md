Work through the artifacts in order since later steps depend on earlier ones. Decode the payload with `base64 -d /home/user/evidence/payload.b64` and parse the resulting JSON to pull out the injected `key` field — that's your malicious SSH key string. Audit `src/main.rs`: the vulnerability is that it sets file permissions to `0o777` after creation, which is CWE-732 (Incorrect Permission Assignment for Critical Resource); fix it by opening the file with `OpenOptions::new().write(true).create(true).mode(0o600).open(...)` from `std::os::unix::fs::OpenOptionsExt` instead of creating-then-chmodding, then confirm `cargo build` succeeds. For key management, compute `sha256sum` over each line of `authorized_keys` using `echo -n "<line>" | sha256sum` (note the `-n`, no trailing newline) and compare each against the hashes in `valid_hashes.txt`; any line whose hash isn't listed is the malicious one to remove. Apply `chmod 700` to `mock_ssh` and `chmod 600` to `authorized_keys`. Finally write the report with the recovered key on line 1, `CWE-732` on line 2, and the sha256 of the malicious key (again via `echo -n ... | sha256sum`, extracting just the hex value) on line 3.