Break this into independently-testable stages. First, extract frames with `ffmpeg -i /app/data/surveillance.mp4 -vf fps=1 /app/processed/frames/frame_%d.jpg` then rename/renumber to match the required `frame_0.jpg, frame_1.jpg, ...` zero-based convention if ffmpeg's `%d` starts at 1. In Go, write the embedding function to convert an image to grayscale (using `image` + luminance formula), split it into a 4x4 grid of cells, average pixel intensity per cell, and divide by 255.0 — verify grid boundaries handle non-evenly-divisible dimensions consistently. Join frame-second to `events.csv` rows by exact integer second match, and store timestamp/event_name/camera_id/embedding (e.g. serialize the 16 floats as JSON or a blob) into SQLite via a Go driver like `mattn/go-sqlite3`. Write the benchmark in a `_test.go` file using Go's `testing.B` convention and capture `go test -bench=BenchmarkEmbedding` output to `/app/processed/benchmark.txt`. For the API, implement bearer-token middleware that checks the `Authorization` header equals exactly `Bearer ds-api-token` before any other endpoint logic, return 401 immediately if not; for `/search`, parse the comma-separated vector, compute cosine similarity against every stored embedding, and return the top match as the exact JSON shape specified. Test each piece (frame count, DB row count, curl with/without token) before wiring it all together.