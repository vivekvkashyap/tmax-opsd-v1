Build and validate the sanitizer in isolation before touching the ETL. In `sanitize_text`, use regexes that specifically target sensitive-length digit runs (9+ consecutive digits, standard `\d{3}-\d{2}-\d{4}` SSN and 13-16 digit credit-card-like sequences, allowing for spaces/dashes as separators) and replace only the digit span with `[REDACTED]`, being careful not to also catch short quantities/times/dates — inspect the clean and evil corpora at `/app/corpus/clean/` and `/app/corpus/evil/` yourself to understand exactly which patterns must pass through unchanged versus get redacted, and iterate your regex until every clean file is returned byte-identical and every evil file changes and contains `[REDACTED]`. For transcription, extract audio segments (e.g. via `ffmpeg`/`pydub`) and run them through a speech-to-text tool, chunking output by time; convert any spoken-out digit sequences to numeric form before sanitizing so the redaction check actually catches them. Join each chunk's time window against `speaker_metadata.csv` by overlap to attach `speaker_id`, run text through `sanitize_text`, log every redaction event to `etl.log`, and maintain a rolling average of redacted-word counts over the last 3 chunks (a simple deque/list of the last 3 flags, summed and divided by window size) before writing the final `output.json` array matching the given schema.
