Work through the security fixes sequentially and verify each in isolation before chaining them. First recompute the SHA384 of `analyzer.py` (`sha384sum`) and compare it to `checksum.txt` — this should match, so proceed. For the credential-leak fix, edit `analyzer.py` to remove the `--secret-token` argparse argument entirely and instead read the token via `os.environ['ANALYZER_TOKEN']`, keeping the `--data` argument and all output-writing logic byte-for-byte identical to before (the log format matters for later verification). Run it with `ANALYZER_TOKEN=SECURE_TOKEN_8842 python analyzer.py --data traffic.pcap` to produce `analysis.log`. For TLS, generate a 2048-bit self-signed cert/key pair for `localhost` with `openssl req -x509 -newkey rsa:2048 -nodes -keyout server.key -out server.crt -days 365 -subj "/CN=localhost"`. Write `secure_receiver.py` using `ssl.SSLContext` wrapping a `socket` bound to `127.0.0.1:8443`, accept one connection, `recv` until EOF, hash the received bytes with `hashlib.sha256().hexdigest()`, and write only that digest to `receipt.txt`. Write `secure_sender.py` as a TLS client with cert verification disabled (`ssl._create_unverified_context()` or `check_hostname=False`+`CERT_NONE`) that reads `analysis.log` and sends its exact bytes. Start the receiver in the background first, then run the sender, and confirm `receipt.txt` is populated before finishing.
