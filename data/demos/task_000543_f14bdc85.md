Write `/home/user/enforce.sh` as an ordered pipeline. First redact: use `sed -E` (or `awk`) with a regex matching IPv4 octet groups (`([0-9]{1,3}\.){3}[0-9]{3}`-style pattern, properly anchored) to replace every IP in `/home/user/raw.log` with the literal `[REDACTED]`, preserving all other text exactly. Second, isolate execution of the untrusted `/home/user/encrypt.py`: pipe the redacted text into it via `env -i unshare -n python3 /home/user/encrypt.py`, capturing stdout to `/home/user/encrypted.log` — remember `env -i` strips PATH too, so invoke `python3` with an absolute path or ensure PATH is set inside the isolated call. Third, for the differential check, run the exact same isolated invocation twice, once piping in the literal string `BLOCK_A` and once `BLOCK_B`, capturing each hex ciphertext. Since XOR of ciphertext equals XOR of plaintext for a stream cipher, compute the byte-wise XOR of the two ciphertext hex strings (equal length, pad if needed) using a small loop (bash arithmetic on hex pairs, or `python3 -c` inline) and write the lowercase, zero-padded, no-`0x`-prefix result to `/home/user/diff_analysis.txt`. Make the script executable and run it end to end before checking output files.