This is a black-box reverse-engineering task: don't guess the mapping, derive it empirically. Run `/app/legacy_graph_backup /home/user/prod_backup.sqlite <tmpfile>` and inspect the resulting JSON alongside the source rows in `users`, `devices`, and `logins` (`sqlite3 prod_backup.sqlite "SELECT * FROM ..."`) to work out exactly how each row becomes a node/edge object — field names, ID prefixing scheme, key ordering inside each object, and any implicit sort order (try inspecting output ordering directly rather than assuming it's insertion order). Cross-check a few rows by hand until the mapping is unambiguous, including edge cases like a device with no logins or logins referencing a device that has no owning user. Then write `/home/user/fast_backup.py` using `sqlite3` with straightforward SQL joins/queries (avoid Python-side nested-loop joins) to build the same node/edge lists, sort them exactly like the binary does before serializing, and dump with `json.dumps`. Validate correctness first with a byte-for-byte or dict-equality diff against the legacy binary's output on the same sample DB, then only worry about speed — the naive `sqlite3` + dict-comprehension approach is already fast enough; don't over-engineer.
