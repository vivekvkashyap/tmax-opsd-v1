Build and validate each stage in isolation before chaining them. First, OCR `/app/observational_data.png` (tesseract) and print the raw text to see the two-column Time/Concentration table layout, then parse it into rows and write `/home/user/extracted_data.csv` — sanity-check the parsed numbers look like a decay curve before trusting them downstream. Second, implement Metropolis-Hastings MCMC in Go to estimate `k` for `dC/dt = -k*C`: propose new `k` values, compute a Gaussian log-likelihood (sigma=1.0) of the model's predicted concentrations (via analytic exponential decay or Euler integration) against your extracted data, accept/reject per the standard MH ratio, restrict to a Uniform(0,1) prior by rejecting proposals outside [0,1], run 100,000 steps discarding the first 10,000 as burn-in, and sanity-check the posterior mean of `k` looks reasonable (should be near the decay rate implied by the data) before proceeding. Third, for forward projection: take the last 1,000 post-burn-in samples, Euler-integrate each (dt=0.1) to time T, then — this is the key correctness requirement — sort the 1,000 resulting floats ascending before summing and averaging, to make the reduction deterministic. Finally wire an HTTP server on 0.0.0.0:8080 with `POST /predict` that checks the exact header `Authorization: Bearer chem-sim-2024` (reject with 401/403 if missing/wrong) and returns `{"expected_concentration": ...}` from the sorted-sum projection; leave it running.
