First reproduce the bug so you can confirm your fix, then repair it methodically. Note that `sqrt(2*(1-cos(theta)))` loses precision for tiny theta because `1-cos(theta)` suffers catastrophic cancellation (cos(theta) rounds to 1.0 in floating point) — verify this by evaluating the existing function at values like `1e-9` and observing it collapses to 0.0. Derive an equivalent expression using the half-angle identity that avoids subtracting two nearly-equal numbers — rewrite `1 - cos(theta)` in terms of `sin(theta/2)` so the formula becomes a direct trig evaluation rather than a difference of near-equal quantities, then implement that in `compute_chord_length`. Add the exact two assertion lines specified (type check then non-negativity check) at the top of the function, matching the messages verbatim. For `fuzzer.py`, generate 10,000 random floats uniformly in `[1e-10, 1e-7]`, call the function, and immediately print `BUG FOUND: <theta>` and `sys.exit(1)` the moment you see a `0.0` result — this doubles as your regression check once the fix is in place (it should then run clean). Finally run `python3 /home/user/geo_project/verify.py` and let it write `success.txt`; don't hand-edit that file yourself.