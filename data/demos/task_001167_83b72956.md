This is a two-part diagnosis: environment configuration discovery, then a numeric bug fix. First, inspect `sensor_dummy` statically and dynamically to find the hidden config flag: run `strings sensor_dummy` to look for environment-variable names embedded in the binary, and/or trace its logic with `objdump -d` or by running it under `strace -e trace=execve,getenv` (or simply try setting candidate env vars and diffing stdout) to confirm which variable switches it from binary garbage to plaintext integers, and what value it expects. Once you have plaintext output, read through `agent.cpp` carefully for arithmetic type bugs — specifically look at variables that accumulate squared values: if a running sum-of-squares is declared as a narrow integer type (e.g. `int`), large sensor readings can overflow it when squared and summed, corrupting the RMS calculation. Fix the type (use `long long` or `double`) rather than just changing intermediate rounding. After fixing both issues, set the discovered environment variable, recompile with `g++ agent.cpp -o agent_fixed`, run `agent_fixed` with the variable exported, and redirect stdout to `/home/user/telemetry_diag/diagnostic_report.txt`. Verify the report contains a plausible non-overflowed RMS value before finishing.
