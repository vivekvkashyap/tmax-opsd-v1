Break this into independently testable pieces before wiring them into the git hook. First write and test `optimizer.go` standalone: parse each CIDR line into a (network, prefix-length) pair, then perform proper CIDR aggregation — sort blocks, and repeatedly merge two adjacent same-prefix-length blocks into a single one-shorter-prefix block when they form an exact aligned pair (i.e., their combined range is itself a valid CIDR, not just "adjacent" numerically); a naive approach that merely sorts and drops full duplicates/subsets without doing real sibling-pair aggregation will fail to hit the minimal count. Test the optimizer directly against `/home/user/raw_routes.txt` and inspect the line count of its output before trusting it inside the hook. Next, write the `post-receive` hook in `/home/user/router_deploy.git/hooks/post-receive` (checkout main to `/home/user/deployed_router`, `go build optimizer.go`, run it against raw_routes.txt to produce `routes.conf`, then call `start_daemon.sh`), and make it executable. For `start_daemon.sh`, use `unshare --net --user --map-root-user` wrapping a sequence that creates `dummy0` (`ip link add dummy0 type dummy`), assigns `10.99.0.1/24`, brings it up, adds the `192.168.100.0/24 dev dummy0` route, then launches `/app/router_daemon <routes.conf>` backgrounded with its PID captured via `$!` into `/home/user/daemon.pid`. Finally commit and `git push` to the bare repo to trigger the hook end-to-end.