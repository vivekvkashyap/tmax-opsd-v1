Start by running the existing baseline pipeline to see it actually fail/be slow, so your fixes are grounded in observed behavior rather than guesswork. For the numerical instability in deconvolve.py: locate the direct `np.linalg.inv(A.T @ A)` (or similar) call and replace it with a regularized solve — either Tikhonov/Ridge (`np.linalg.solve(A.T@A + lambda*I, A.T@y)` with a small lambda) or `np.linalg.pinv` / `np.linalg.lstsq`, which won't throw on near-singular matrices; additionally special-case fully-black frames (all-zero input) to short-circuit and output a zero array rather than attempting the factorization at all. For the performance requirement, profile first (e.g. `time` the baseline) to see where the cost is — likely per-frame Python subprocess overhead and serial ffmpeg extraction — then parallelize frame processing (GNU `parallel`, background jobs with `wait`, or Python multiprocessing) and/or batch the ffmpeg frame extraction instead of one-process-per-frame. Wire everything into run_optimized.sh as the single entry point, and validate correctness by comparing your final_spectra.csv peak intensities against a stable reference run, and validate speed by timing your full script end-to-end against the original baseline before declaring success — both the 3x speedup and the MSE threshold are hard gates.