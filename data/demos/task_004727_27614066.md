First recover the salt by analyzing `/app/network_status.mp4` yourself: extract frames (e.g. with `ffmpeg -i network_status.mp4 frame_%03d.png` or read frames via OpenCV/PIL in Python) and count how many are exactly solid red (RGB 255,0,0 across the entire frame — check all pixels, not just a sample point, to avoid false positives from near-red frames). Use that count `X` to form the salt `RED_X`. Then write `/home/user/normalize.py`: split the single argument on `&`, split each pair on the first `=`, and for each value try to evaluate it as an integer arithmetic expression restricted to digits/+/-/* (e.g. a small recursive-descent parser or `eval` guarded by a regex whitelist — never use raw `eval` without validating the input only contains digits and +-*, since arbitrary code execution would be a security bug); if it's not a valid math expression, keep it as-is. Sort pairs by key using default string ordering, join as `key=val` with `|`, and prefix with `<SALT>:`. Print only this final line, no extra output, since the fuzz-equivalence check does exact stdout comparison against a compiled oracle across 100 random inputs.
