Build this as a bash script with clearly separated, testable steps rather than one dense pipeline. For leaked-ID detection, strip headers from both CSVs, extract just the `id` column (`cut -d, -f1`), and use `comm -12` on two sorted, deduped ID lists (or `sort | uniq -d` on the concatenation) to get IDs present in both files; sort the result numerically (`sort -n`) and write one per line to `/home/user/leaked_ids.txt`. For the join, filter `source_A.csv` and `source_B.csv` to only the leaked IDs (e.g. with `awk` using an associative array loaded from `leaked_ids.txt` as a filter, or `join` after sorting both files by id), then join on `id` producing `id,featA,featB` sorted numerically — `join -t, ` requires both inputs sorted the same way, so sort by id first. For benchmarking, invoke `time -p python3 /home/user/inference_batch.py /home/user/leaked_ids.txt > /home/user/leak_predictions.txt 2> /home/user/benchmark.txt` — remember `time` writes its `real/user/sys` lines to stderr, not stdout, so redirect stderr specifically to the benchmark file. Make the script executable and confirm all four output files match the expected format after a dry run.
