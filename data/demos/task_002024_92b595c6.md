Two facts must be recovered from evidence before you can write the sanitizer, not assumed: (1) use `ffmpeg -i /app/attack_recording.mp4 -vf fps=1 frame_%03d.png` (or similar) to extract frames, then read the frames (visually or via OCR) to find the exact `--dport` value in the `iptables ... DROP` command shown on screen; (2) run `openssl x509 -in /app/trusted_roots.pem -noout -subject` (or split the PEM bundle and inspect the first cert specifically, since the file may contain multiple concatenated certs) to get the CN of the first certificate. Once you have both recovered values, write `/home/user/redactor.py` reading stdin line-by-line and applying rules IN ORDER: drop lines containing the discovered port string entirely (skip output), then regex-substitute `--password \S+`, `Cookie: \S+`, and `Authorization: Bearer \S+` with their REDACTED forms using non-greedy/whitespace-bounded patterns, then if the line contains the discovered CN string append exactly ` [TRUSTED]` before the trailing newline. Since verification is bit-exact fuzz equivalence, be precise about regex boundaries (use `\S+` not `.*`) and preserve all unmodified text and whitespace exactly.
