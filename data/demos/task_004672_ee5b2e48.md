Start by extracting the ground truth from the image, not guessing it: run `tesseract /app/lab_notes.png stdout` (try `--psm` variants and check the raw output carefully for OCR errors in numbers/operators before trusting it) to recover the two nonlinear equations and the instruction that the precision matrix is their Jacobian at the root. Solve the system symbolically/numerically (sympy.nsolve, scipy.optimize.fsolve, or by hand if simple) restricted to the first quadrant to get μ=(x,y). Build the Jacobian matrix of partial derivatives of the two equations, evaluate it at that root to get the precision matrix, then invert it (numpy.linalg.inv, or Cholesky/SVD-based inversion for numerical robustness) to get the target covariance Σ. Verify your (μ, Σ) make sense (symmetric positive-definite Σ) against a few sample trace CSVs before finalizing. Then write `/home/user/classifier.py` to load a CSV of x,y samples, compute `np.mean`/`np.cov`, and compare element-wise to your derived μ and Σ with the stated tolerance (≤0.4 abs diff), printing exactly CLEAN or EVIL and exiting 0 either way.
