Treat this as four independently testable fixes plus one integration. Fix `/app/generate_report.sh` first by checking the real delimiter and field layout of `/app/passwd` (standard `/etc/passwd`-style colon-separated fields — the home directory is field 6), replacing the broken `awk -F';'` field-2 extraction with a correct colon-delimited field-6 extraction, and confirm output matches `User <username> has home <homedir>` exactly via manual testing before touching the C code. Fix `/app/cron_backup.sh` by hardcoding/resolving `/app/backups/` directly (e.g. `DEST=/app/backups/backup_$(date +%s).tar.gz`) instead of relying on an environment variable that may be unset when run via `env -i`. Write `/app/rotate_logs.sh` to `mv "$1" "$1.1"` then recreate `$1` empty with `install -m 644 /dev/null "$1"` or `touch` + `chmod 644`. For the C daemon, implement a TCP server on 127.0.0.1:8888 that reads a line, parses `REPORT <username>`, calls `popen("/app/generate_report.sh <username>", "r")` to capture its output, then opens its own TCP connection to 127.0.0.1:2525 and speaks minimal SMTP (HELO/MAIL FROM/RCPT TO/DATA/body/.) with sender `daemon@local.site`, recipient `admin@local.site`, and body equal to the captured report output; compile and run it in the background, then test end-to-end with `printf 'REPORT charlie\n' | nc 127.0.0.1 8888` and check `/app/mail_spool/`.
