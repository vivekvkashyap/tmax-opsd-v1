Build the `/stats` handler as a pure function you can test against the log file directly before worrying about the server framework or the nginx layer in front of it. Parse each line with a regex capturing the level and the `latency=XXXms` field; a line is valid only if the captured latency parses as a non-negative integer — explicitly reject non-numeric (e.g. "NaN") and negative values rather than letting `int()` throw or silently accepting negatives. Process lines in file order (top to bottom = chronological), keeping a running list/deque of valid latencies for the rolling average of the last 3, and a dict keyed by log level that gets overwritten with the raw line text every time a new valid line of that level is seen (so it naturally ends up holding the latest one). Serve this via Flask/FastAPI/http.server bound to `127.0.0.1:8080` with a `GET /stats` route returning the exact JSON schema (rolling_avg_3, latest_by_level), and start it as a background/daemon process (e.g. `nohup ... &` or a detached subprocess) so it keeps running after your turn ends — verify it responds correctly through the nginx proxy at `127.0.0.1:8000/api/stats`, not just directly on 8080.
