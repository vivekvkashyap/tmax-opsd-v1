Build the pipeline in exactly the order the spec implies, since each stage's math must match a hidden reference within a tight tolerance. Read all frames from the video with OpenCV (`cv2.VideoCapture`), converting to grayscale only if a frame comes back as 3-channel (OpenCV can return BGR even for grayscale-encoded video). Stack frames into an array and compute the per-pixel median across the frame axis (`np.median(frames, axis=0)`), then subtract that median frame from every frame — do this in float, not uint8, to avoid clipping/wraparound artifacts that would diverge from the reference. Flatten each cleaned frame in row-major order and multiply by a fixed random projection matrix generated with `np.random.seed(42)` immediately before `np.random.randn(W*H, 10)` — seed placement matters, don't seed earlier or generate other random numbers first. Rebuild the exact `nn.Sequential(Linear(10,16), ReLU(), Linear(16,1))` architecture, `load_state_dict` from `/app/model.pth`, run in `eval()` mode with `torch.no_grad()`, and write `frame_idx,score` rows to CSV. Since the check is max-absolute-error against a hidden oracle, get every arithmetic detail (float dtype, median axis, flatten order) exactly right rather than approximately.