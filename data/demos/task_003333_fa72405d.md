First fix the Makefile in `/home/user/build_env`: inspect it for missing/incorrect recipe tabs (Makefile recipe lines must start with an actual tab character, not spaces) and any wrong filenames (e.g. a target referencing a source file that doesn't match `algo.c`, or an output name that doesn't match the required `collatz` executable). Rewrite the `all` target so `gcc -o collatz algo.c` (or equivalent) runs with a real tab indent, then confirm `make` succeeds and `./collatz` runs. Next, read `algo.c` to see its exact Collatz-length loop logic (even → divide by 2, odd → 3n+1, count steps until n reaches 1) and port that identical logic to a Python function in `/home/user/ws_handler.py` — be careful to replicate the loop precisely (e.g., does it count the final step to reach 1, does it use integer division) so results match for any N, not just 837799. Compute the steps for N=837799 by actually running your Python function (don't hardcode a guessed number), then write the JSON to `/home/user/build_env/ws_payload.json` matching the schema exactly, with `steps` set to your computed integer.
