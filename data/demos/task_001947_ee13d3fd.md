Build one unified `networkx.DiGraph()` by adding edges from both sources: iterate `access_logs.json` and add an edge from each `user` to its `accessed_system`, then iterate `system_topology.csv` and add an edge from `source_system` to `target_system` — all nodes (users and systems) end up in the same graph since betweenness centrality needs the full path structure, including the user-entry edges, to be correct. Run `nx.betweenness_centrality(G)` with default (unweighted, directed) parameters on the whole graph. Only after computing centrality should you filter the results dictionary down to system nodes — build the set of user names from the access log and exclude them from the output, keeping systems even if their centrality is 0. Sort the remaining systems by centrality descending, take the top 3, round each score to exactly 4 decimal places, and write the JSON array to `/home/user/audit_results.json` in the exact `{"system":..., "centrality":...}` shape requested — mismatched key names, ordering, or rounding will fail the exact-match check.
