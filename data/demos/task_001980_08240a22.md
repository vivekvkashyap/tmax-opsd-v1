Start by reading the vendored package's regex in `/app/vendored/i18n_ts_parser/extractor.py` to find exactly what's hardcoded to ASCII-only (`[0-9]` character classes and literal `[`/`]` brackets). Patch the pattern to accept both bracket styles (standard `[...]` and full-width `【...】`) using an alternation or character class, and widen the digit classes to include Unicode digit ranges (e.g. Arabic-Indic `٠-٩`, full-width `０-９`) rather than only `[0-9]` — test the patched regex directly against a few multilingual sample strings before building the pipeline on top of it. In `parse_metrics.py`, after extracting the raw timestamp/value strings via the fixed extractor: normalize digits using Python's `str.translate`/`unicodedata` digit-value lookup (map each Unicode digit codepoint to its ASCII equivalent) rather than a fixed substitution table, since the fuzzer may use different Unicode digit blocks; parse the normalized timestamp into a `datetime` and reformat as strict `YYYY-MM-DDThh:mm:ssZ`; and skip/drop lines that don't match rather than crashing. Since grading is bit-exact fuzz equivalence over 500 random lines, test your script locally against several hand-crafted edge cases (missing Z, extra noise suffixes, both bracket types) before considering it done.