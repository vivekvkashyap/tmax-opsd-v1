This is a two-stage task: first recover a hidden config from a video, then write a converter to spec. To extract the config from `/app/archive_feed.mp4`, don't assume the values — use `ffmpeg` to pull the subtitle/metadata stream (e.g. `ffmpeg -i /app/archive_feed.mp4 -map 0:s:0 out.srt`, or `ffprobe` to enumerate streams first if you're unsure which stream index holds subtitles) and read the extracted text to get the actual `CHUNK_SIZE` and `ENCODING` values — plug whatever you find into your script, don't hardcode guessed values. For the converter itself: read all of stdin, `json.loads` it; use Python's `csv` module (via `io.StringIO` + `csv.writer`) rather than manual string joining so that special characters (commas, quotes) in filenames are escaped correctly per RFC 4180; chunk records into groups of the extracted `CHUNK_SIZE`, writing the header row at the start of every chunk; join chunks with a line containing exactly `---SPLIT---`; handle the empty-array case explicitly by still emitting the header alone; finally encode the assembled text string with the extracted `ENCODING` name and write raw bytes via `sys.stdout.buffer.write()` (not `print`, which would re-encode as UTF-8 and break non-UTF8 encodings). Test locally with a small sample plus an empty-array case and a filename containing a comma before considering it done.