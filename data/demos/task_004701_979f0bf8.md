Transcribe /app/dataset_recording.wav with a real ASR model (e.g. `whisper` on the audio file) rather than guessing the content — the grading compares your cleaned_text to a hidden human transcript via Word Error Rate, so transcription quality directly drives your score; pick a reasonably accurate model size given time constraints. Apply the cleaning rules in the exact stated order: lowercase, strip all punctuation (regex on non-alphanumeric/non-space), then remove standalone filler words "um"/"uh"/"like" using word-boundary matching (so "like" the word is removed but "likely" is untouched — use `\bword\b` regex, not naive substring replace), then collapse repeated whitespace. For tokenization, load the actual `bert-base-uncased` tokenizer via HuggingFace `transformers` (don't approximate with `.split()`), tokenize the cleaned string, record the total token count, then chunk into non-overlapping groups of exactly 15, dropping any incomplete trailing group, and count those chunks. Write raw_transcript, cleaned_text, total_tokens, and chunk_count into /home/user/experiment_log.json exactly as specified, then sanity-check your cleaned_text reads as a coherent transcript before finishing, since WER <= 0.15 is required to pass.
