Start by running `sqlite3 /home/user/compliance.db ".schema"` to see the actual column names and foreign-key relationships between `users`, `user_roles`, `role_systems`, and `systems` — don't assume names. The bug in the existing script is a comma-separated (implicit cross) join that only constrains `users` to `user_roles` and leaves `systems`/`role_systems` unconstrained, producing a near-full cartesian product. Rewrite the query as explicit `INNER JOIN`s that chain the real graph edges: users → user_roles (on user id) → role_systems (on role id) → systems (on system id), selecting only the two display columns. Add `ORDER BY user_name, system_name` directly in SQL rather than post-sorting in bash. Use `sqlite3 -csv -noheader` (or an equivalent flag combo) so no header row leaks into the output, and redirect straight to `/home/user/fixed_audit_report.csv`. Sanity-check the row count before finishing — it should equal the number of distinct (user, accessible-system) pairs, far smaller than the original cross-join output.
