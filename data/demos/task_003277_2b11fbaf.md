Work strictly in order: (1) load the CSV with pandas, fit `TfidfVectorizer()` with default settings on the `text` column; (2) compute the full cosine-similarity matrix with `sklearn.metrics.pairwise.cosine_similarity`, zero out the diagonal (self-similarity) with `np.fill_diagonal`, then repeatedly take the argmax, record the pair, zero out both symmetric entries, and repeat 3 times — this greedy top-3 extraction avoids the common bug of just sorting the flattened upper triangle without excluding already-picked rows/cols. Sort each pair's two doc_ids ascending before writing, and order the 3 pairs by descending similarity. Write the JSON as a plain list of two-element lists. For cleaning, drop the row with the larger doc_id from each pair (3 rows total) — do this by boolean-mask on doc_id, not by index position. Then refit TF-IDF on the *remaining* text only (not the original matrix subsetted), and run `TruncatedSVD(n_components=2, random_state=42)` on that refit matrix to get pca_1/pca_2 — reusing the original full-corpus TF-IDF matrix instead of refitting is a common mistake. Write the final CSV with pandas `to_csv(..., index=False)`.
