Split this into two independently testable pieces and get the Rust CLI correct on its own before wiring it into Flask. For the CLI: parse the JSON with `serde_json`, check `missing_ratio > 0.05` and scan both float arrays for `NaN`/`Infinity` using `f64::is_finite()` (a naive `== f64::NAN` check always fails — use `is_nan()`/`is_finite()`), reject immediately if any check trips before doing the more expensive bootstrap. For the bootstrap: sample `N/2` elements with replacement from `loss_values` 100 times (use the `rand` crate's `gen_range` in a loop, not sampling without replacement), take each sample's mean, sort the 100 means, and read off the 2.5th/97.5th percentile by index (e.g. index 2 and index 97 of a sorted 100-length array, or interpolate) — compare the span to 50.0. Finally sum `gradient_norms` and compare to 1e5. Print exactly `ACCEPT`/`REJECT` and exit 0/1 accordingly; test it directly against sample JSON files before integrating. For the service side, get Redis and Flask talking on localhost first, verify Nginx's `proxy_pass` target and `listen` directive match the actual Flask port, then have Flask write the incoming payload to a temp file, invoke the compiled `target/release/artifact_filter <path>` via `subprocess.run`, check the return code, and return 406 on rejection before any Redis write.
