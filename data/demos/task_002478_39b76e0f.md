Work through the four objectives in order since each depends on the last. First, diff-check `/app/file_vault/setup.py`'s `install_requires` — look for a misspelled dependency name (a common vendoring-package perturbation is a one-letter-off package name like a mistyped "flask") and correct it, then `pip install -e /app/file_vault` to confirm it installs cleanly. Second, read `/home/user/evidence/encryptor.py` to understand the XOR scheme (byte-wise XOR against a fixed key), then write a small decrypt script that reads the hex bytes in `/home/user/evidence/token.hex`, XORs each byte against the same key used to encrypt (brute-force the single byte key 0-255 if it isn't obviously stated, checking which key produces printable ASCII), and write the recovered plaintext token exactly to `/home/user/evidence/recovered_token.txt`. Third, patch `/app/file_vault/file_vault/server.py`'s `/upload` handler: reject any filename containing `../` or `/` with HTTP 400, and require an `Authorization: Bearer <token>` header matching exactly the recovered token, returning 401 otherwise. Finally, `mkdir -p /app/uploads`, start the patched server bound to `127.0.0.1:9090` in the background, and smoke-test all three cases (valid upload, wrong token, traversal filename) with `curl` before finishing.