Treat key recovery and algorithm reverse-engineering as two separate, verifiable steps before writing any C++. First, transcribe `/app/intercepted_comms.wav` (install/use `whisper` or another ASR tool) and listen/read carefully — the speech will spell out a key using a phonetic alphabet (like "Alpha", "Bravo", "Foxtrot"...), so map each spoken word to its corresponding letter/digit to reconstruct the literal key string rather than guessing. Second, analyze `/app/oracle_decoder` with `strings`, `objdump -d`, and `ltrace`/`strace` to spot library calls (a SHA-256 or similar hash call is a strong signal), then run the oracle yourself repeatedly with the recovered key and various chosen hex payloads (including edge cases like all-zero bytes, single bytes, and known patterns) to empirically deduce how the key is combined with any hardcoded salt/constant to derive an XOR keystream — don't stop until your hypothesis reproduces the oracle's output exactly across several handpicked test payloads, including varying payload lengths. Once verified, implement it in `/home/user/my_decoder.cpp` (matching stream length exactly per byte, uppercase hex output), compile it, and diff its output against the oracle on the same test payloads again before considering it complete.
