Start services with `bash /app/services/start.sh` and give them a few seconds, then probe the API and Redis manually (`curl http://127.0.0.1:8080/logs | head`, `redis-cli keys 'user:*'`) to see the real log/key shapes before writing the pipeline. Build `/home/user/pipeline.sh` as a sequence of clearly separated stages: (1) fetch exactly 10000 lines from `/logs` (curl + head, or loop until you have the count) and save raw JSONL; (2) use `jq` to derive `hour` from the `timestamp` substring (characters at the hour position of an ISO8601 string) and `event_clean` by lowercasing, trimming, and stripping punctuation (a `sed`/`tr` pass removing everything except letters/digits/spaces after lowercasing/trimming, applied consistently) — process each record independently so partial failures don't corrupt the whole stream; (3) for enrichment, batch or loop `redis-cli get user:<id>`, defaulting to `"unknown"` when the key is absent (check `redis-cli`'s nil response, not just empty string); (4) do stratified sampling by splitting records into three streams by `user_type` and taking exactly the first (or randomly chosen) 100 of each — verify counts before writing; (5) emit the CSV with the exact header column order given. Make the script executable, run it once to produce the CSV, then add a cron entry with `crontab -l | { cat; echo "*/5 * * * * /home/user/pipeline.sh"; } | crontab -`.
