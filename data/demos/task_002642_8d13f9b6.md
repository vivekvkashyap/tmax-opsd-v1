Build the bash pipeline as four clearly separable stages chained with intermediate files, since mixing them in one awk pass invites bugs. (1) Encoding: `iconv -f ISO-8859-1 -t UTF-8 /home/user/translations.csv > /tmp/converted.csv` first — do all downstream processing on this converted file, never the raw one. (2) Wide-to-long: use `awk -F,` skipping the header, and for each data row emit 4 lines (key,en,val)(key,fr,val)(key,de,val)(key,es,val) in that column order, writing `key,locale,translation` as the header to `/home/user/long_format.csv`. (3) Anomaly detection: for each row compute character length with `length()` in awk (be careful this measures the UTF-8-converted string, not byte length, if using awk's default locale-aware length works or adjust); compare each of fr/de/es length against `0.3 * en_length` and `3.0 * en_length` using strict `<`/`>`, and only write rows that violate one bound to `/home/user/anomalies.csv` — skip `en` itself. (4) Stratified sample: get the sorted-unique list of keys, take the first 2 alphabetically, filter the long-format data to just those keys, then `sort` the result by locale then by key (e.g. `sort -t, -k2,2 -k1,1`) into `/home/user/sample.csv`. Test each stage's output against your own manual trace of the small fixture before chaining them, since a single delimiter or field-order mistake propagates through every later stage.
