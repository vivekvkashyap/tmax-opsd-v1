Parse defensively rather than assuming every line is well-formed. Use a strict filter — e.g. `awk` or a regex — that requires exactly four space-separated fields where field 1 matches `[TIMESTAMP]` bracket syntax, field 2 is an integer PID, field 3 is a bare component name, and field 4 is an integer memory value; anything that doesn't match all four (stack traces, "Kernel panic" lines, binary garbage, partial writes) must be silently dropped rather than causing a crash or a bogus partial parse. Once you have only clean rows, group them by PID preserving their original chronological order (the file is already time-ordered, so first-seen and last-seen per PID give you the boundary values), and compute last-minus-first memory for each PID. Pick the PID with the largest positive delta — don't just take the first or largest value seen. Write the single-line result to `/home/user/leak_report.txt` in the exact format `PID: <PID>, LEAK_MB: <DELTA>` with no extra whitespace or trailing content, since the check is a strict pattern match. Sanity check your parsing by manually counting valid lines per PID against the raw file before trusting the computed delta.
