Do the data prep with simple CLI filtering first, verifying row counts before touching the C code. Use something like `grep -v NA raw_data.csv` (excluding the header, or grep the header out separately) to drop malformed rows, then `head`/`tail` (or `sed`/`awk` with line numbers) to split the first 100 valid rows into `train.csv` and the rest into `test.csv` — confirm exact counts with `wc -l` before proceeding. Then open `pipeline.c` and locate the mean/std computation: it currently accumulates over both `train_data` and `test_data` and divides by `train_rows + test_rows`. The fix is to compute mean and std using only the training loop and divide only by `train_rows`, then apply those same fixed statistics to normalize both training arrays (if used) and the test array before running inference — do not touch the weights, bias, or the prediction loop itself. Recompile with `gcc pipeline.c -lm -o pipeline`, run it, and redirect stdout to `test_predictions.txt`, confirming the output has exactly one 4-decimal float per test row with no extra formatting.