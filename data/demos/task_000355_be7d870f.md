Work the four sub-problems separately before wiring them together. (1) Transcribe `/app/voicemail.wav` with a tool like `whisper` (or `ffmpeg`-based ASR if available) to recover the spoken decimal calibration delta — don't guess it, listen/transcribe and parse the spoken words into a float. (2) Read `/app/recovery_service.py` to find the naive floating-point accumulation (`sum([delta]*10000)` compared with `!=` against `delta*10000`) — fix it by replacing the strict `!=` equality check with `math.isclose(a, b)`, or better, replace the summation itself with `math.fsum(...)` for exactness, so accumulated rounding error no longer trips the assertion. (3) Recover the corrupted SQLite DB: try `PRAGMA wal_checkpoint(TRUNCATE);` first, and if the WAL itself is damaged, fall back to `sqlite3 sensor_data.db ".recover" | sqlite3 recovered.db` and point the service at the recovered file. (4) Once both fixes are in place, run the modified `recovery_service.py` in the background so it binds `127.0.0.1:8080` and serves `/api/data` with the highest-ID row's calibrated value (raw value + calibration adjustment) as JSON.
