Don't guess the token format — derive it empirically. Run `xxd`/`hexdump` on several files in `/app/corpus/clean/` to spot a fixed byte prefix common to all of them (a magic header), then diff the byte layout of clean vs. evil samples field-by-field to infer a type/length/value record structure. Feed individual clean tokens into `/app/legacy_decoder` via stdin to confirm your parsing model reproduces its accepted JSON output. To find the actual crash conditions, run the binary under `gdb -ex run -ex bt --args /app/legacy_decoder < evil_sample` (or `valgrind --track-origins=yes`) on each evil sample and read the backtrace/crash address to identify which field and which malformed value (e.g. an oversized length, or a length exceeding remaining buffer) triggers the fault — correlate this back to the byte offsets you found from hex dumps. Once you have the header signature, per-record type/length/value grammar, and the specific length bounds that overflow fixed buffers, reimplement that validation purely in Python (no subprocess calls to the binary) in `prefilter.py`, and test it against both corpora before submitting.
