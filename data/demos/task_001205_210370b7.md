Read `uploader.c` end to end and trace the data flow from file bytes to the final `fopen` path. Notice the `sanitize` function that strips `"../"` sequences from the filename — check exactly how many times it removes such a sequence (look at any `break`/loop-exit logic) versus how attacker input could contain overlapping or nested traversal sequences that only become `"../"` *after* one pass of removal (e.g. a string like `X../` where removing an inner `../` collapses two segments together into a new `../`). This class of bug — insufficient/single-pass sanitization enabling directory traversal — corresponds to a specific, well-known CWE; identify it precisely and write only that identifier to the required file. To build the exploit, work out by hand: (1) the plaintext bundle layout (1-byte name length + filename bytes + file contents), (2) craft a filename string that, after exactly one pass of `"../"` removal, results in a path escaping the `uploads/` directory to land at the target file, (3) apply the program's decrypt/encrypt transform (note it's a symmetric single-byte XOR, so encrypting is the same operation as decrypting) to the whole plaintext buffer, and (4) write the resulting bytes to the payload file. Compile nothing new — just run the existing `uploader` binary against your crafted file and confirm the target file appears with the exact required contents.