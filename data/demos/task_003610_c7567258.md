This is a multi-service integration task — bring each component up and verify it in isolation before wiring the whole chain, and rerun the corpus test as its own gate. Start with the sanitizer since it's independently graded: implement `is_safe_payload` in `sanitizer.py` to recursively walk the parsed JSON AST tracking depth (reject if it exceeds 10) and check any `matrix`-typed node's declared dimensions against the 100x100 limit; run it directly against every file in `/home/user/app/corpora/clean/` and `/home/user/app/corpora/evil/` yourself and print pass/fail per file until you hit 100% on both before moving on — don't just eyeball a few examples. Next complete the C-FFI in `engine.py`: use `ctypes.CDLL` to load `libpoly.so`, set `argtypes`/`restype` correctly for `double evaluate_polynomial(double*, int, double)`, and unit-test `/compute` directly with `curl localhost:5001` before touching the gateway. Then wire `app.py`'s route to call the sanitizer, forward safe payloads to the engine, and return 400 on unsafe ones — test with curl directly against port 5000. Only last, configure and reload nginx (symlink into sites-enabled, `nginx -t` before reload) and test the full path through port 8080, including one deliberately evil payload to confirm the 400 path works end-to-end.