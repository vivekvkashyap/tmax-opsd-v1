Write `/home/user/process_storage.py` using `os.walk('/home/user/storage_pool', followlinks=False)` — this alone prevents descending into directory symlinks, but you must also explicitly filter `dirnames` in-place (e.g. `dirnames[:] = [d for d in dirnames if not os.path.islink(os.path.join(root, d))]`) so os.walk doesn't even list them as subdirectories to recurse into, guaranteeing no infinite loop regardless of walk order. For each `.tar.gz` file found, verify integrity by attempting to open it with `tarfile.open(path, 'r:gz')` and fully reading its members (or calling `.getmembers()`/checking `tarfile.is_tarfile(path)` combined with a try/except around extraction) — catch exceptions to flag corrupt archives, collecting absolute paths and sorting them alphabetically. For each `.gcode` file, process line-by-line: strip everything from the first `;` onward, then strip trailing whitespace, and skip/omit any line that becomes blank; write the cleaned lines (each ending in a single newline, no trailing blank lines) to `/home/user/cleaned_gcode/<original_filename>`. Finally emit the JSON report with the gcode count and sorted corrupt-archive list to `/home/user/storage_report.json`.
