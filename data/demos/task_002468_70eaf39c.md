Diagnose bottom-up through the stack rather than guessing: first fix `docker-compose.yml` by changing each backend's `expose: - "80"` to `ports: - "8001:80"` (and 8002/8003 for the others), then `docker compose up -d` (or restart) and confirm each is reachable with `curl 127.0.0.1:8001`. Next, read `nginx.conf` line by line for syntax errors — check the `upstream` block carefully for a missing trailing semicolon on one of the `server` lines, and validate with `nginx -t -c <path>` before relying on systemd to catch it. Then inspect the systemd unit: nginx forks into a master + worker processes by default, so `Type=simple` will make systemd think the service exited immediately; either change to `Type=forking` or add `-g "daemon off;"` to `ExecStart` so nginx stays foreground and matches `Type=simple`. Reload the user daemon (`systemctl --user daemon-reload`) and start the service, checking `systemctl --user status local-proxy.service` for a clean active state. Only after confirming the proxy actually round-robins across all three backends (test with a few manual curls checking varying response bodies), run `/app/lb_bench` and redirect its stdout to `/home/user/bench_results.json`.