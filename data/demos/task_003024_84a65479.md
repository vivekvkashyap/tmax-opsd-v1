Handle the corruption at the text level before JSON-parsing: read the file line by line, and for each line use a regex to find `\uXXXX`-style escapes where the four characters after `\u` are not valid hex, then strip or neutralize just that broken escape (e.g. replace with empty string or a placeholder) so `encoding/json` in Go can unmarshal the rest of the line — do this per-line so one corrupt field doesn't lose the whole record. Parse `timestamp` with `time.Parse(time.RFC3339, ...)`, group records into a `map[string][]Record` keyed by `sensor_id`, and sort each sensor's slice by timestamp. For resampling, iterate hours 0-23 for `2023-10-01`, bucket readings whose timestamp falls within `[hour:00:00, hour+1:00:00)`, average `temp` for that bucket; if a bucket is empty, copy the previous bucket's value (or `0.0` if it's the very first bucket with no prior data) — do this gap-fill in a single forward pass. Compute mean/max/min over exactly the 24 filled bucket values (not the raw readings). Write the CSV with `sensor_id,mean_temp,max_temp,min_temp` header, rows sorted alphabetically by `sensor_id`, and all floats formatted with `fmt.Sprintf("%.2f", v)`. Never write to the original `.jsonl` file — only read it.
