Write `/home/user/rotate.go` to compute the directory's total size dynamically — walk `/home/user/app_logs` with `filepath.Walk`/`os.ReadDir`, summing `info.Size()` for every regular file found, rather than hardcoding any number. Only proceed with rotation if that computed total is strictly greater than 10240 bytes. When rotating, process the shifts in the correct order to avoid overwriting files prematurely: first remove/overwrite `app.log.3` if `app.log.2` exists, rename `app.log.2` to `app.log.3`, then rename `app.log.1` to `app.log.2`, then rename `app.log` to `app.log.1` (do the shift from highest number down to lowest, i.e. `.2`->`.3` before `.1`->`.2`, so you don't clobber a file before reading it), then create a fresh empty `app.log`. Only handle files that actually exist — don't error if `app.log.2` or `app.log.3` are absent initially. Append the status line to `/home/user/rotation_status.log` using the exact computed total-size value (not the post-rotation size) in the message format specified. Run with `go run` and verify sizes with `stat` afterward.
