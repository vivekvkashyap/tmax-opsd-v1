Use `git log --oneline` and `git log -p` (or `git bisect`/`git show` on suspect commits) in `/home/user/wrapper_repo` to find the commit that changed serialization or removed an environment/config setting — diff each candidate commit's changes to the wrapper's encoding function and to any env-var/flag setup code, since regressions are often introduced together with a masking "cleanup." For the crash: inspect how the wrapper currently converts Python strings to bytes before sending to the engine — a custom/incorrect encoder can emit invalid byte values that make the binary abort; replace it with a spec-correct encoding (e.g. proper UTF-8 encoding via Python's built-in `str.encode`) so no forbidden byte value is ever produced, including for non-ASCII input. For the performance issue: search git history (`git log -p -- '*.py' '*.md' | grep -i env` or similar) for a deleted environment variable or fast-path flag that used to be set before invoking `/app/record_engine`, and restore setting it via `os.environ` or `subprocess` env kwarg in `driver_fixed.py`. Time the whole run with `time.time()` around the engine invocation, write the elapsed seconds to `/home/user/metrics.log`, and test against `/app/eval_data.json` to confirm no crash and sub-1.5s runtime.
