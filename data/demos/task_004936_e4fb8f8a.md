Approach this as a classic environment-then-bisect debugging task. First inspect `build.sh` and the way `generator` is invoked — it links against a shared library under `./libmath` but the script never exports `LD_LIBRARY_PATH` (or equivalent rpath), so the dynamic linker can't find it at runtime; add `export LD_LIBRARY_PATH=./libmath:$LD_LIBRARY_PATH` (or similar) before invoking the binary. Once that's fixed, the script will crash partway through processing `fuzz_corpus.txt`. Do a binary/linear search over the corpus rather than staring at the whole file: split the file in half, feed each half through the pipeline (or loop line-by-line with a short per-line timeout using `timeout`), and narrow down to the exact failing line number using standard bisection — e.g. `head -n N fuzz_corpus.txt | generator` and check the exit status, halving the range until you isolate one line. Record that 1-based line number in `/home/user/line_number.txt`. Then base64-decode just that line (`echo "$LINE" | base64 -d`) and write the exact decoded plaintext to `/home/user/bug_report.txt`. Do not touch the C++ source.