Work the three objectives in order since later steps depend on earlier ones. First, grep `/app/logs/system.log` for the SHA-256 hash string (look for an "Auth hash:" or similar line). Since the task states it's a 4-digit PIN, brute-force it: loop PIN candidates 0000-9999 (zero-padded), compute `sha256sum` of each candidate string, and compare against the extracted hash until you find a match — write only the matching plaintext PIN to `/app/cracked_pin.txt`. Second, open `/app/evidence_api/src/main.rs` and find the `/record` handler building a SQL string via `format!`/string concatenation with the `id` query param spliced directly into it — replace this with a parameterized query using the SQL driver's bind-parameter syntax (e.g., `?1` placeholder passed as a separate argument) so injected SQL like `1' OR '1'='1` is treated as a literal value, not code. Third, `cargo build --release` both services, start `audit_logger` on 127.0.0.1:9000 first, then start `evidence_api` on 127.0.0.1:8000 with `ADMIN_TOKEN` set to the cracked PIN, both backgrounded. Manually test both a normal id=1 request and an injection payload against port 8000 before finishing.
