Read `generate_key.py` and extract the exact linear congruential formula `key = (a*seed + c) mod m`. For the CWE, recognize this is the classic "use of a non-cryptographic / predictable PRNG for security-sensitive values" weakness and cite the specific weak-PRNG CWE identifier (not the generic "insufficient entropy" one). To recover the seed, don't brute-force blindly first — check whether `m` is prime (many textbook LCGs use a Mersenne-prime modulus), and if `gcd(a, m) == 1`, compute the modular inverse of `a` mod `m` via the extended Euclidean algorithm (Python's `pow(a, -1, m)` works on 3.8+), then solve `seed = (key - c) * inv(a) mod m` directly — this is exact and instant, versus looping billions of times. Only fall back to brute force if the modular-inverse approach fails to reproduce the target key (verify by re-running `generate_key(seed)` and comparing to the file). For the certificate chain, split `bundle.pem` into leaf/intermediate/root, verify signatures with `openssl verify -CAfile root.crt -untrusted int.crt leaf.crt`, then pull the CN with `openssl x509 -noout -subject -in leaf.crt`. Write the three fields into the JSON report with correct types (seed as int).
