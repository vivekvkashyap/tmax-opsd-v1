Implement evaluate_model.py to exactly match the specified sklearn pipeline call-for-call, since reproducibility here depends on determinism, not on tuning: pandas.read_csv the dataset, CountVectorizer().fit_transform on the review column, train_test_split(X, y, test_size=0.25, random_state=42), LogisticRegression(random_state=42).fit, accuracy_score on the test split, and write the accuracy formatted to exactly 4 decimal places (use f"{acc:.4f}") to accuracy.txt with no extra text. Keep every random_state and hyperparameter fixed exactly as specified — any deviation (different test_size, missing random_state, different solver) breaks bit-for-bit reproducibility. For test_pipeline.py, use subprocess.run to invoke `python3 /home/user/evaluate_model.py` five times as separate clean processes (not by importing and reusing in-process state, which could mask non-determinism), reading accuracy.txt fresh after each run and comparing all five string contents for exact equality; if they all match, touch an empty /home/user/test_pass.log. Run test_pipeline.py yourself at the end to confirm both output files actually get created.
