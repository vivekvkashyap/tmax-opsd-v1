Work phase by phase, verifying each output before moving on. Phase 1: read `upload_handler.c` and spot the unbounded `strcpy` into a fixed stack buffer — classify it with the specific CWE for stack-based buffer overflow (not the generic buffer-overflow CWE), write just that ID to `/home/user/evidence/cwe.txt`. Phase 2: for each `implant_X.pem` in `/home/user/forensics/certs/`, verify against the chain with `openssl verify -CAfile ca_chain.pem implant_X.pem`; only the one that returns OK is the real C2 cert — extract its CN via `openssl x509 -in <file> -noout -subject` and write just the CN value (no other subject fields) to `c2_domain.txt`. Phase 3: write a small C (or use it to drive) program that base64-decodes `payload.enc` and XORs every byte with `0x42`, writing the raw bytes to `implant.bin` — compile and actually run it, don't just write the source. Phase 4: search the decoded binary for the pattern `SIG_` followed by exactly 8 uppercase hex chars (use `grep -aoE 'SIG_[0-9A-F]{8}'` on the binary to discover the actual signature present, don't assume one), embed that discovered literal string as an ASCII match in a YARA rule named `Implant_Rule`, run `yara -r implant.yar /home/user/forensics/system_files/` and save matching absolute paths to `infected.txt`.