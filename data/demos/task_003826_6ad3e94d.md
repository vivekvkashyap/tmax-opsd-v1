Read `aggregate.sh` line by line rather than assuming the bug's location. The infinite loop is inside the inner `while [ $cleaned -eq 0 ]` cleaning block: trace what sets `cleaned` in each branch — one branch (the 99.9-to-100.0 capping case) reassigns `cpu` but never sets `cleaned=1`, so it loops forever; fix it by setting `cleaned=1` after applying the cap. For the precision loss, find where the final averages are computed — look for plain arithmetic like `$(( total / count ))` (bash integer division, which truncates) or a `bc` call missing a scale setting; fix both by using `echo "scale=3; total_cpu / count" | bc` (and similarly for mem), and confirm the result string actually has 3 decimal digits — `bc` can drop trailing zeros or omit a leading zero, so pad/format the output if needed (e.g. with `printf "%.3f"`). Also verify the capping logic changes the running total, not just a display value — re-check that `total_cpu` accumulates the capped 100.0, not the raw 99.9. Run the fixed script and confirm `/home/user/results.txt` matches the required `AvgCPU: ...` / `AvgMem: ...` format with exactly 3 decimals.