Load the `seqs` dataset from the HDF5 file with h5py, decoding each byte string to a Python str. Partition into Group A (contains the exact substring `GATTACA`) and Group B (does not) using simple `in` checks. For each sequence compute GC content as `(count('G') + count('C')) / len(seq)` — make sure you're counting characters, not doing a case-sensitive mismatch if the data has lowercase (verify by inspecting a few sequences first). Run Welch's t-test with `scipy.stats.ttest_ind(group_a, group_b, equal_var=False)`, which is the correct call since population variances shouldn't be assumed equal — using the default `equal_var=True` (Student's t-test) will produce different numbers and fail verification. Format the output line exactly as `T-statistic: [t_val], P-value: [p_val]` with both values rounded to 4 decimal places (Python's `round()` or an f-string with `:.4f`), and write only that single line to results.txt with no extra whitespace or trailing content.