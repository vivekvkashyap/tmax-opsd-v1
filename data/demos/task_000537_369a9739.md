Start by reading the vendored package's source under `/app/prot_integrator/prot_integrator/` to find the step-size adaptation logic (look specifically for where `dt` is rescaled when the error tolerance is exceeded) — a step size that *grows* instead of *shrinks* on error is what causes runaway divergence/NaNs, so trace the sign/direction of that adjustment rather than guessing at unrelated code. After fixing it, reinstall the package into your Python environment (`pip install -e /app/prot_integrator` or equivalent) so `import prot_integrator` picks up the fix, and confirm `simulate()` on the provided PDB no longer returns NaNs. Then compute, per PDB, a bootstrap 95% CI: repeatedly (1000 times) resample the flattened pairwise-fluctuation array with replacement, compute the mean, collect all 1000 bootstrap means, and take the 2.5th/97.5th percentiles as `ci_lower`/`ci_upper`. Build a minimal HTTP server (Flask/http.server) on 127.0.0.1:8080 that parses `pdb` from the query string, checks the `Authorization: Bearer dev_token_99x` header exactly (401 on mismatch/missing), and returns the JSON shape specified. Also produce the documentation notebook as asked. Leave the server running in the background and curl-test both auth outcomes yourself before finishing.