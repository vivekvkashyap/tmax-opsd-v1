Since this will be checked by fuzzing against the real `/app/bio_sim` binary across many random inputs, prioritize reverse-engineering its exact numeric behavior over guessing a "reasonable" ODE integrator. Disassemble with `objdump -d`/`gdb`/`ghidra`, and also run differential black-box testing: call the binary with varied `<P0> <r> <K> <Tmax>` and observe outputs to infer the update rule (a logistic-growth ODE with adaptive step size — pay close attention to how `dt` is chosen from the derivative magnitude each iteration, the comparison thresholds, how the final partial step is clamped to not overshoot `Tmax`, and any early-exit/divergence bound). Reproduce that logic bit-for-bit in `/home/user/emulator.py` using `sys.argv[1:5]` as floats, printing the final population to exactly 5 decimal places matching the C `%.5f` output. Once it matches on spot-checked inputs, read `/home/user/raw_inputs.nc` with `netCDF4`, loop the 50 rows through your emulator's core function, and write the results to `/home/user/processed_data.h5` under dataset name `final_states` via `h5py`.
