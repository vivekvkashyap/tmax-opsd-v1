Write the C program to read `/home/user/execution_times.bin` as raw IEEE-754 doubles using `fread` into a `double` array (or streamed accumulation), not as text — a common mistake is treating the binary file as ASCII. Determine the array length from the file size (`size / sizeof(double)`) since it should be 1,000,000 values but derive that from the file rather than hardcoding. Compute the MLE rate as `lambda = 1 / mean` where mean is the simple arithmetic average. Compute the empirical sample variance with the unbiased estimator: sum of squared deviations from the mean divided by `(N-1)`, not `N` — this is a common bug to avoid. Compute the analytical variance as `1 / (lambda*lambda)` directly from your estimated lambda (do not use a separately assumed lambda). Format each of the three output lines with exactly 5 decimal places (`%.5f` in C) and match the exact label text and line order given in the task. Compile with `gcc /home/user/profiler_stats.c -o /home/user/profiler_stats -lm` (link math library since you likely need `sqrt` or similar), then actually run the binary so it writes `/home/user/stats_output.txt`, and double check the file has exactly three lines with no extra whitespace.