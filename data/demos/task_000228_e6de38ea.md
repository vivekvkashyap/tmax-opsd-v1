Break this into three independent validation stages applied to each CSV row, and reject on the first failure — order matters for correctness but not for output. (1) Regex-check `SensorID` with `std::regex` against the exact anchored pattern given; a partial/unanchored match will wrongly accept malformed IDs. (2) UTF-8-validate `SensorDescription` yourself byte-by-byte (implement a small UTF-8 decoder checking continuation-byte structure and valid code point ranges) rather than trusting `std::regex` or locale-based checks, since invalid sequences like a lone `0xFF` byte must be caught explicitly. (3) For rows with `IsInterpolated=1`, you must find, per `SensorID`, the nearest preceding and following raw (`IsInterpolated=0`) rows — this requires either two passes over the data (first pass to index raw rows per sensor by timestamp, second pass to validate interpolated rows) or buffering, since stdin is a single stream; don't assume rows arrive pre-sorted by sensor. Call `/app/interp_oracle <t_prior> <v_prior> <t_next> <v_next> <t_target>` as a subprocess, parse its float, and accept the row only if it matches the row's `Value` within ~0.0001. Always print the header line first if present. Test your binary against small hand-built CSVs covering each rejection rule individually before trusting it on the hidden corpora.
