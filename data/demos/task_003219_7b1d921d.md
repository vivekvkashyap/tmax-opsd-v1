Start by transcribing `/app/architect_notes.wav` yourself — install `openai-whisper` or `SpeechRecognition` with a suitable backend, run it on the file, and read the actual transcript text to extract the precise path-sanitization rules (don't assume standard rules; the recording states the exact conditions to reject). Separately, fix the Rust borrow-checker error in `/home/user/project/fast_path/src/main.rs`: run `cargo build`, read the compiler's diagnostic (it will usually point to a reference outliving the value it borrows, such as returning `&str` referencing a local `String`), and resolve it by returning an owned `String` or restructuring lifetimes rather than suppressing the error. Then write `/home/user/classifier.py` to load the JSON argument, iterate every path in `"paths"`, and apply exactly the rules transcribed from the audio; exit `1` as soon as any path is invalid and `0` only if every path passes — test it manually against a few obviously-bad and obviously-good paths before relying on it. Add `/home/user/test_classifier.py` using `hypothesis` to generate random path-like strings and simply assert the classifier function/subprocess call never raises/crashes, without asserting specific accept/reject outcomes. Do not alter files under any `/app/corpora/` directories.