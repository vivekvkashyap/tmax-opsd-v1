Parse each log independently into a keyed structure before correlating, rather than trying to line-match by position. For `access.log`, extract the correlation ID and the raw `Headers` JSON blob per line — a regex like `X-Correlation-ID": "(\w+)"` plus capturing the leading IP token, and separately parse the `Cookie` header string (split on `; ` then `=`) to pull out just the `session_id` value regardless of where it appears among other cookies. For `app.log`, extract `[CorrID: (\w+)]` and the event type (`LOGIN_SUCCESS`/`LOGIN_FAILED`). Build one dict keyed by correlation ID from each file, then join on that key: for every `LOGIN_SUCCESS` corrID, look up the matching access.log entry's session_id cookie value and collect it (skip/ignore empty session_id values from failed attempts, which use a different cookie shape). For every `LOGIN_FAILED` corrID, look up the IP from access.log and tally a counter per IP; after processing all lines, flag IPs whose failure count is strictly greater than 2. Sort both output lists alphabetically before writing the JSON with `json.dump(..., indent=2)` to `/home/user/audit_report.json`.
