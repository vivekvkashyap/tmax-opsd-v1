Start with OCR: run `tesseract /app/waf_schematic.png` and carefully read the extracted text for a line matching the `DENY IF <json_path> MATCHES <regex_pattern>` DSL pattern — the JSON path and regex are what you must recover and hardcode into your interpreter's rule (don't invent your own rule). Note the path uses JSONPath-like dot notation (e.g. `$.user.input`) so you'll need to walk the nested JSON object using the header at `/app/include/json.hpp` to extract the field the rule targets, then apply `std::regex` (with case-insensitive flag if the pattern encodes one, e.g. `(?i)`) against that field's string value. Build the C++ REST service with `httplib.h`, listening on `localhost:8080`, POST `/api/v1/evaluate`: parse JSON defensively (wrap in try/catch, return 400 on parse failure), evaluate the rule, return 403 on match else 200. Write `test_waf.sh` to curl the endpoint with `-X POST -d @<file>` and inspect the HTTP status to decide exit 0/1. Validate against every file in `/app/corpus/clean/` and `/app/corpus/evil/` before finishing — any misclassification means the regex or JSON-path extraction is off.