Build `archiver.py`'s `sanitize_and_archive` as a pure, testable function before wiring it into the pipeline — this is what the hidden corpus tests directly. Read the input file in binary first to check for corrupted/non-text binary headers (e.g. attempt a UTF-8 decode or check for control-byte signatures like PNG magic bytes) and return `False` immediately if it looks binary, without writing any archive. For text logs, apply two independent regex substitutions: one matching 16-digit credit-card numbers allowing optional dashes (e.g. `\b(?:\d[ -]?){16}\b` normalized) replaced with `[REDACTED_CC]`, and one matching multi-line RSA private key blocks (`-----BEGIN RSA PRIVATE KEY-----` through `-----END RSA PRIVATE KEY-----`, using `re.DOTALL`) replaced with `[REDACTED_KEY]` — test both patterns against varied formatting so you don't under- or over-match. Write the sanitized text into a `.tar.gz` via Python's `tarfile` module, add the CLI entry point with `argparse`, then handle Part 2: fix `config.ini`'s `archiver_cmd`, and write `start_pipeline.sh` to launch docker-compose in the background, sleep 5s, then loop on `inotifywait -m -e close_write` over the raw logs directory, invoking the archiver per new file and pushing the resulting archive name to Redis with `redis-cli LPUSH`. Manually trigger a test file through the running pipeline to confirm the end-to-end timing before finishing.