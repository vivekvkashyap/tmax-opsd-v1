Build the orchestration script step by step and make process cleanup robust, since that's where naive attempts fail. Create the venv with `python3 -m venv /home/user/venv`, then `/home/user/venv/bin/pip install Flask`. Launch each server with `&` and capture its PID (`legacy_pid=$!` etc.) right after backgrounding it — don't rely on `pgrep` later, since that can match the wrong process. Use plain `python3` (system) for the legacy server and `/home/user/venv/bin/python` for the Flask one, matching the two invocation commands given. After starting both, `sleep` a couple seconds (well under the 3s budget) to let sockets bind before curling. Fetch both responses with `curl -s` into shell variables, compare them with a plain string equality test (`[ "$a" == "$b" ]`), and write `PASS` or `FAIL` accordingly — write the file without a trailing newline mismatch issue if the verifier does exact matching. Critically, kill both background PIDs (`kill $legacy_pid $new_pid`) before the script exits, in a way that runs even if the comparison fails (e.g. do the kill unconditionally after comparison, not only in a success branch), so no leftover process holds the ports.