Handle the two encodings separately before joining. Read `users.csv` as plain UTF-8, but read `scores.csv` as UTF-16LE — either decode it inside Rust (open as bytes, transcode with an encoding crate, or manually pair little-endian byte pairs into UTF-16 code units) or preprocess it up front with `iconv -f UTF-16LE -t UTF-8 scores.csv > scores_utf8.csv` and have your Rust code parse the converted file; either is acceptable per the task. Parse both CSVs into maps/structs keyed by `user_id`, then join. Compute `final_score = sqrt(score1^2+score2^2+score3^2) * multiplier` using `f64`, and format with exactly two decimals (Rust's `{:.2}` handles standard rounding correctly — verify it matches expected half-up behavior on your test values). Sort the joined rows by `final_score` descending before writing. Write the CSV with header `username,final_score` and no extra whitespace or trailing commas. Compile with `rustc process.rs`, run the binary, then diff-check `/home/user/result.csv` yourself against your hand-computed expectations before considering it done — a common failure is misreading the UTF-16 BOM/byte order or losing precision from premature rounding during joins.