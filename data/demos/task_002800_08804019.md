Start by reading `/app/libtokenparse-1.0/tokenparse.c` closely to find the vulnerability class: look at how a length field read from untrusted input is used to size a `memcpy` into a fixed-size stack buffer — a classic missing-bounds-check leading to a stack buffer overflow. Fix it by validating the length against the buffer's capacity before copying (reject/truncate/error out rather than crash) without changing the program's expected CLI behavior. Separately, the `Makefile` may look fine visually but builds may fail due to whitespace: recipe lines under a target must be indented with a literal TAB, not spaces — check with `cat -A Makefile` or `sed -n l` and fix any space-indented recipe lines. Run `make` to confirm `verify_token` compiles cleanly. Then write `/home/user/detect.sh` so it invokes `verify_token <file>` and translates the tool's exit status/crash behavior into your own exit codes: 0 for safe tokens, 1 for malicious ones — since a fixed parser should now reject the oversized payload gracefully instead of segfaulting, base your exit-code mapping on `verify_token`'s own success/failure signal (and treat any crash as malicious too, defensively). Test against both provided corpora before considering it done.
