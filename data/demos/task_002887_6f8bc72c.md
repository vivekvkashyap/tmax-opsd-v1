Break this into three independently testable pieces, then wire them with a Makefile DAG.

1. Video decoding: use `ffmpeg` to extract each frame of `/app/transmission.mp4` (e.g. `-vf fps=1` into numbered PNGs), then for each frame in time order compute average pixel brightness (Python + Pillow/numpy, or `ffmpeg`'s `signalstats`/`blackframe` filters). Threshold: brightness > 128 → bit `1`, else `0`. Concatenate bits in frame order into a 12-character binary string, then `int(bits, 2)` to get MULTIPLIER — never hardcode a guessed value, derive it from the actual pixels.

2. Safe expression filter (`/home/user/filter.py`): the naive mistake is calling Python's `eval()`/`exec()` directly on the formula string — that executes injected code. Instead, parse with `ast.parse(expr, mode='eval')`, walk the resulting AST, and only allow a strict whitelist of node types (Constant/Num, BinOp, UnaryOp, and the specific operators +-*/%** etc., plus maybe `abs`/`Call` to a tiny allowed-function set) — reject anything else (Name, Attribute, Call to unknown funcs, Import, Subscript, comprehensions, strings). Recursively evaluate only the whitelisted AST yourself; catch parse/type errors and malformed JSON, and silently drop that log rather than crashing.

3. Makefile: chain steps as targets with real file dependencies — decode video → write MULTIPLIER to a file/var, run filter.py on raw logs → intermediate JSON, multiply metric_value by MULTIPLIER, join on `id` with the CSV (e.g. via `csv`/`pandas` or `jq`+`join`), dedupe by id, sort alphanumerically, write the final CSV with the exact required header.
