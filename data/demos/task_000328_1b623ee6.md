Build and verify each subsystem independently before wiring them together in `deploy.sh`. For the Go monitor: use a simple infinite `for` loop with `time.Sleep(1*time.Second)`, `os.ReadFile` the status file, `strings.TrimSpace` and compare exactly to `CRITICAL`, then write the alert file with `os.WriteFile` using the exact multi-line RFC5322 text (get the blank line between headers and body and trailing newline right, since the verifier does an exact string comparison), and immediately after writing the alert, overwrite the status file with `OK` so it doesn't re-fire. For ACLs: `setfacl -d -m u::rw-,g::r--,o::--- /home/user/app/mail_spool` sets *default* (inherited) ACLs — verify with `getfacl` that the `default:` entries show exactly those permissions, since setting a regular (non-default) ACL won't satisfy the "new files inherit" requirement. In `deploy.sh`, create directories first, apply the ACL, initialize the status file, `go build -o bin/monitor monitor.go`, launch it with `bin/monitor &`, capture `$!` into `monitor.pid`, then make the script executable and actually run it before finishing — test the CRITICAL->alert->OK cycle manually to confirm timing works within the verifier's ~3 second window.