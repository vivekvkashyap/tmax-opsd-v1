Treat this as three independent extraction sub-problems that feed one JSON report. For the domain, search `/home/user/investigation/mem_dump.bin` for readable ASCII substrings ending in `.malware.local` — e.g. use the `strings` command (or a Python regex over the raw bytes matching something like `[A-Za-z0-9_.\-]+\.malware\.local`) since it's embedded among random binary noise, and extract the full matching hostname, not just the `.malware.local` suffix. For the timeline, parse both `logs/api.log` and `logs/db.log` as `<ISO8601 timestamp> <LEVEL> <message>` lines, combine all entries into one list, and sort by timestamp (Python's `datetime.fromisoformat` or plain lexicographic sort works since ISO8601 timestamps sort correctly as strings) to get the true chronological order across both files — the "last event before crash" is the one immediately preceding the entry whose level is `CRITICAL`, regardless of which log file it came from. For the missing env var, find the `ERROR` line mentioning `EnvironmentError` and pull out the variable name it names. Write `/home/user/investigate.py` to perform all three extractions programmatically (don't hand-transcribe values) and emit `/home/user/report.json` matching the exact schema given, then run the script to produce the file.