Split this into an isolated parser task and a service-wiring task, and verify each independently. For the C extractor: implement a stateless byte-stream parser — read the 5-byte header and bail with "INVALID" to stderr / exit 1 on mismatch, then loop reading 1-byte type + 2-byte little-endian length + payload, writing only type 0x01 payloads verbatim to stdout with no added delimiters, and stop cleanly on type 0xFF. Be careful with byte order (little-endian length parsing) and unsigned char handling to avoid sign-extension bugs; use `fread`/`getchar` on raw binary stdin, not text-mode functions. Build small synthetic DocWAL byte streams yourself (varying types, lengths, truncated/corrupt headers) and test your compiled binary's behavior against them directly before wiring it into Flask — do this without any reference implementation. For the service side: fix nginx.conf's `location /api/` block to add `proxy_pass http://127.0.0.1:5000;`, update flask_app.py's subprocess call to invoke `/home/user/extractor` (piping the uploaded binary payload to its stdin and capturing stdout, then caching in Redis), start redis-server with the given conf, then flask, then nginx as specified, and do an end-to-end curl test through nginx's port 8080 before declaring done.
