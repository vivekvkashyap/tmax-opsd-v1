Write the hook as a Python script at `/home/user/operator-repo/.git/hooks/post-commit` with a `#!/usr/bin/env python3` shebang, then `chmod +x` it — git silently no-ops non-executable hooks, so this step is easy to forget and produces a confusing "nothing happened" failure. Inside the hook, read `manifest.yaml` from the current working directory (git invokes hooks with cwd at the repo root, so a relative path is fine) and pull the replica count with a plain regex/string parse, e.g. matching a line like `  replicas: 8` and extracting the trailing integer — don't reach for pyyaml since the task forbids it and the format is guaranteed simple. Always append `DEPLOY: replicas=<X>` to `/home/user/operator.log` (append mode, one line per commit, not overwrite), and additionally append `ALERT: High replica count detected (<X>)` to `/home/user/alerts.mail` only when the extracted integer is strictly greater than 5. Test your hook manually by editing `manifest.yaml` and committing a couple of times with different replica counts (one <=5, one >5) and inspecting both output files to confirm the conditional alert logic and exact string formatting are correct before considering it done.