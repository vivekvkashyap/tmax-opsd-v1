Work through the four artifacts in order, verifying each before moving on. Run `tesseract /app/ransom.png stdout` to OCR the ransom text and read the 4-digit port number directly out of the recognized text (double-check OCR didn't confuse digits). For the binary, run `strings /app/upload_handler.bin | grep -i /` (or objdump/hexdump) to locate the hardcoded upload directory string and copy it byte-for-byte into vuln_dir.txt. For log redaction, write a bash script using `sed -E 's/(session_token=)[A-Za-z0-9]+/\1[REDACTED]/g'` or an equivalent regex against the `Cookie:` header lines, applied to the whole file, and save to clean_logs.txt — verify the rest of each line is untouched. For the honeypot, build a bash+nc/socat loop that accepts a connection, reads the request line, and branches: if it's exactly `GET /logs HTTP/1.1`, respond `200 OK` with clean_logs.txt's exact bytes as the body and correct Content-Length; if the raw request URI contains the literal substring `..`, respond `403 Forbidden` with no body; otherwise `404 Not Found` — and in every branch, include the `X-Incident-Response: Active` header. Test each of the three cases manually with curl/nc against the running server on the OCR'd port before finishing, since header/body formatting mistakes are the most common failure here.