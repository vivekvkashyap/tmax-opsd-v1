Parse the `.ini` file with a standard config parser (Python `configparser`) to get each device's log path, format, and encoding as data, not hardcoded assumptions — do not assume all logs share one encoding. For each device, open its log file in binary mode and decode using the exact `encoding` value from the ini section (a naive `open(path)` with default encoding will corrupt non-UTF-8 files like ISO-8859-1 or UTF-16LE). Then dispatch to the right parser based on `format`: use an XML library for `xml` (extract `param`/`value`/`time` children), `csv.DictReader` for `csv`, and `json.load` for `json` (mapping `p`/`v`/`t` keys). For each device, group updates by parameter name and keep only the entry with the maximum `time`/`timestamp`, being careful to cast timestamps to `int` before comparing (they may arrive as strings). Build a nested dict keyed by the ini section name, then write it as UTF-8 JSON to the exact output path with `json.dump`, ensuring value types match what the spec implies (typically stringified values). Test each format/encoding path independently before combining.
