Start by extracting the configuration rather than assuming paths: run OCR (e.g., `tesseract /app/policy.png stdout`) on the policy image and read its output carefully to discover the actual target directory and output manifest path specified in the policy text — use those recovered values, don't hardcode guesses. For the C program, do a recursive directory walk (`nftw`/`fts` or manual `opendir`/`readdir` + `lstat`) that follows symlinks but tracks the `(st_dev, st_ino)` pair of every directory already visited in a growing set/list; before descending into a directory (including one reached via a symlink), check whether its dev/inode pair is already in that visited set and skip it if so, which is what prevents the A<->B symlink loop from looping forever. For every regular file, compute MD5 via OpenSSL's `libcrypto` (link with `-lcrypto`), and write `MD5_HEX FILE_SIZE RELATIVE_PATH` lines to the manifest exactly as specified, using a path relative to the target directory root. Compile, run against the recovered target/output paths, and sanity-check the manifest lists every real file exactly once (not duplicated via the two symlinks) before considering it done — the final score depends on file coverage precision/recall, so avoid both missed files and phantom duplicate entries.
