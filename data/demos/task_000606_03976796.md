Run `tesseract /app/alert_config.png -` (upscale/threshold the image first with ImageMagick if OCR is garbled) to recover the `PORT` and `TOKEN` values — parse them programmatically rather than assuming a value. For the interactive password step, write an `expect` script that spawns `/app/secure_quota_check.sh`, matches the `Enter monitoring password: ` prompt, sends the given password `mon_alert_99` followed by a newline, and then captures/passes through the script's remaining stdout (the `DISK_USAGE=<number>` line) so your web server can parse it with a regex. Build the web server (any language) bound to `127.0.0.1` on the extracted port: `/poll` should shell out to the expect script, extract the integer after `DISK_USAGE=`, and return the JSON; `/alert` must check the `Authorization: Bearer <TOKEN>` header exactly, reject with 401 if missing/wrong, and otherwise append the raw request body as one line to `/home/user/alerts.log` and return 200. Launch the server detached (e.g. `nohup ... &` or systemd-style backgrounding) and verify with `curl` against both endpoints before finishing. Never print the token/port in your final message.