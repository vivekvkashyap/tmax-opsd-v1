Do all four phases with awk/sed/sqlite3 only, and set `LC_ALL=C.UTF-8` (or `en_US.UTF-8`) in your script so awk's `length()` and character-class matching count Unicode codepoints instead of raw bytes — this is the main place naive attempts break, since under the "C" locale multi-byte UTF-8 symbols like ∑ get counted as multiple bytes/characters. For Phase 1, use awk with `FS=","` (or a proper CSV-aware split, being careful the header has no embedded commas here so plain field-splitting is fine) to emit one `msg_id,lang,translation` row per input column pinned to its language name, skipping the header. For Phase 2, compute `char_len` via awk `length($0)` under the UTF-8 locale, and count math symbols by iterating substrings or using `gsub` with a bracket-expression containing the five literal Unicode symbols (ensure the script file itself is saved as UTF-8). For Phase 3, group by `lang` with awk associative arrays, sum char_len and math_sym_count, divide for the average, and format to 2 decimals with `printf "%.2f"`; sort output alphabetically by lang. For Phase 4, create the two tables in `sqlite3` and import each CSV with `.mode csv` + `.import <file> <table> --skip 1` (skip the header row) or an equivalent import invocation.
