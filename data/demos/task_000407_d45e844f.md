Build the pipeline in stages and validate each before moving to the next. Extract frames from `/app/ui_test_run.mp4` with `cv2.VideoCapture` (or `imageio`/`ffmpeg` to a frame sequence), and for each frame compute the mean of the Red channel across all pixels — be careful of channel order (OpenCV reads BGR, so index correctly) and process frames in their natural temporal order. Compute the 95% bootstrap CI with `scipy.stats.bootstrap((data,), statistic=np.mean, n_resamples=10000, method='percentile', random_state=42)` and separately fit a Normal via sample mean/std (or `scipy.stats.norm.fit`), writing both to the specified files at 4-decimal precision. For `classifier.py`, cache/recompute the same baseline array, load the JSON trace, run `scipy.stats.ks_2samp(trace, baseline)`, and branch strictly on `pvalue < 0.05` for the exit code — since this is checked against a whole corpus of clean/evil traces, test your classifier against several of your own baseline-like and shifted synthetic arrays first to confirm the threshold logic and exit codes are exactly right before relying on it.
