Start by transcribing `/app/project_memo.wav` (e.g. `ffmpeg` + an STT tool) — it states a late-breaking exception rule not in the written spec; implement it exactly as spoken, since the hidden reference parser encodes it too and fuzz testing will catch any line type you mishandle. Write `/home/user/parser.sh` as a line-by-line Bash reader (`while IFS= read -r line || [ -n "$line" ]; do ... done < "$1"`) that: (1) detects JSON lines matching `^\{.*\}$` and extracts `.message` via `jq -r '.message // "NULL"'`, falling back to `JSON_MSG: NULL` on parse failure; (2) detects GCode lines matching `^([GM][0-9]+)` at the start and outputs only the matched command prefix (ignore trailing params) as `GCODE: <command>`, applying the audio's special-case rule to that command; (3) otherwise uppercases the whole line via `tr '[:lower:]' '[:upper:]'` and prints `TEXT: <line>`. Make the script executable with `chmod +x`. Test against varied synthetic lines (valid/invalid JSON, GCode with different letters/numbers, plain text) to match byte-for-byte before relying on the fuzzer.
