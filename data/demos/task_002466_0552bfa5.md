For the schema-enforcement filter, validate `login_count` and `days_inactive` with a strict digits-only regex (e.g. awk `$2 ~ /^[0-9]+$/ && $3 ~ /^[0-9]+$/`) rather than a numeric comparison like `-ge 0`, since the latter would either error out or wrongly accept non-numeric/malformed values like `not_a_num` or `empty`. For the join, sort both filtered `logins.csv` and `subs.csv` by `user_id` (required for `join` to work correctly) and use `join -t, -1 1 -2 1` to keep only users present in both files. Then transform with awk to reorder into `user_id,plan_type,login_count,churn_risk`, computing `churn_risk` as `HighRisk` when `login_count < 5` else `LowRisk`. Sort the final result by `user_id` before writing to `/home/user/processed.csv`. Make `etl.sh` executable and ensure each run fully overwrites `processed.csv` (redirect with `>`, not append) so repeated runs are deterministic and idempotent.
