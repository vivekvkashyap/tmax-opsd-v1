Locate `calculate_variance` in `spectral_analyzer.cpp` and recognize the bug pattern: it's computing variance as `E[X^2] - E[X]^2` (accumulating `sum` and `sum_sq` in one pass), which loses precision catastrophically when values are large but close together, since `sum_sq/n` and `mean*mean` are both huge numbers whose difference is tiny — subtracting two nearly-equal large doubles wipes out the significant digits and can even go negative. Replace it with a numerically stable two-pass approach: first compute the mean in one pass, then compute `sum((x - mean)^2) / n` in a second pass over the data (or implement Welford's incremental algorithm, which never squares raw values) — both avoid cancellation because they operate on already-centered, small differences. Keep the function signature/output usage the same so the rest of the pipeline (the calibration solver) is unaffected once variance is no longer erroneously negative/NaN. Recompile exactly as specified with `g++ -O3 -std=c++11`, run the binary on `dataset.txt`, and redirect stdout to `results.log`, matching the required "Mean:/Variance:/Calibration Coefficient:" format with 4 decimal places (`std::fixed << std::setprecision(4)`).
