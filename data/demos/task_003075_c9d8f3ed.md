Break this into three independently testable pieces. First, walk `/home/user/raw_reports/`, parse each `cost_report_YYYY_MM.html` filename with a regex/glob, `mkdir -p` the `YYYY/MM` subdirectory under `/home/user/finops_web/`, and `ln -s` (absolute or correctly relative path — verify with `readlink`) each raw file to `index.html` there. Second, since you're barred from `-subj`/batch mode, write an `expect` script that spawns exactly the given `openssl req -x509 ...` command and matches each interactive prompt in sequence (use `expect "Country Name"` etc., matching on the actual substring openssl prints, followed by `send "value\r"`) ending with `interact` or `expect eof`; run it yourself and confirm with `openssl x509 -in cert.pem -noout -subject` that every field landed correctly before moving on — mismatched prompt patterns are the most common way expect scripts silently hang or skip fields. Third, write `start_server.sh` to launch an HTTPS static file server (e.g. Python's `http.server` wrapped with `ssl` context, or `openssl s_server`, or a simple background python snippet loading cert/key) bound to port 8443 serving `/home/user/finops_web/`, backgrounding it and capturing `$!` into `/home/user/server.pid`; verify with `curl -k https://localhost:8443/...` that content is actually served before finishing.