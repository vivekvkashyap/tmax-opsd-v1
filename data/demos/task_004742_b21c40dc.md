Treat this as a small init-system substitute since there's no systemd/root access. First write the static config file `/home/user/deploy_fstab` with the six standard fstab fields on one line: device (`nfs.internal.net:/vol/data`), mount point (`/home/user/worker_data`), fs type (`nfs`), options (`defaults,ro`), dump, and pass — get the field order and spacing exactly right since it's checked literally. Create `/home/user/mail_spool`. For `/home/user/manager.sh`, use a bounded loop (`for i in 1 2 3` or a counter capped at 3, not a `while true`) that each iteration runs `/home/user/worker.sh`, captures `$?` immediately after, and if non-zero appends `ALERT: Worker crashed with code $?` to `/home/user/mail_spool/alerts.txt` — build the alert line dynamically from the actual captured exit code rather than assuming a fixed number, since the task wants you to discover worker.sh's real behavior. After the loop runs exactly 3 times regardless of outcomes, the script must `exit 0`. `chmod +x` it and run it once so `alerts.txt` is populated as a side effect.
