Tackle the five subtasks independently and verify each before moving to the next. For the systemd unit, add `After=stub-net.service` (and likely `Wants=` or ensure ordering) under `[Unit]` in the service file, then `systemctl --user daemon-reload` and `systemctl --user start profile-backend.service`, checking status/logs to confirm it actually comes up. For the port, run `tesseract /app/backend_port.png stdout` (or install/verify tesseract works first with a trivial test) and extract just the digits following "Port:" from the OCR text — don't guess the number, read it from the image. Substitute that extracted port into nginx.conf wherever `{{BACKEND_PORT}}` appears, then reload/start nginx and `curl localhost` (or the relevant path) to confirm the proxy actually reaches the backend before moving on. For the filter script, per JSON file use `jq -r '.username'` (or equivalent) to pull the username, and classify it as evil if it contains any of `; & | $ backtick`, else clean if it matches `^[A-Za-z0-9_]+$` — anything not clearly clean should be treated conservatively as rejected; use `ln -s` for clean files into the output dir and test against small hand-built evil/clean examples of your own before trusting it. Finally define a `.timer` (OnCalendar or OnUnitActiveSec=15min) plus matching `.service` that runs the filter script with the specified input/output directories, then enable and start both units and confirm with `systemctl --user list-timers`.