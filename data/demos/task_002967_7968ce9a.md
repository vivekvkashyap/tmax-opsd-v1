Break this into independent, verifiable stages before wiring the HTTP layer on top. First extract per-frame center-region mean intensities: decode the video with OpenCV (`cv2.VideoCapture`), convert each frame to grayscale, slice `[150:250, 150:250]`, and take the mean — store as an array indexed by frame number t=0..49. Second, fit `I(t) = Vmax*t/(Km+t)` with `scipy.optimize.curve_fit`, giving it a sane initial guess (e.g. p0=[max(I), 10]) since Michaelis-Menten fits are sensitive to initialization and can diverge otherwise. Third, pull the raw flattened pixel arrays (not the means) for frame 0 and frame 49's center region and pass both to `scipy.stats.wasserstein_distance`. Fourth, parse the FASTA manually or with a lightweight parser (split on `>` headers, concatenate sequence lines per record) and compute GC% as `100*(count(G)+count(C))/len(sequence)`. Then build the plot (scatter of raw points + line of the fitted curve) and save to the exact path. Finally wrap all four results behind a Flask/FastAPI app bound to 127.0.0.1:8000 with the three GET routes, rounding as specified in each response, and launch it detached (e.g. `nohup ... &` or `setsid`) so it survives after your shell command returns — verify with `curl` against each endpoint before finishing.