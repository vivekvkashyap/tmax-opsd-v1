Install `libgsl-dev` first (`sudo apt-get update && sudo apt-get install -y libgsl-dev`) and confirm `gsl/gsl_multifit.h` is findable before writing code against it. In train.c, parse the CSV manually line-by-line rather than trusting scanf to silently coerce blanks: split each line on commas, and for each of the three fields check it is non-empty and fully numeric (reject if it's empty, contains only whitespace, or matches "NaN"/"N/A" case-sensitively as given) — use something like `strtod` with an end-pointer check to detect trailing garbage, and skip the entire row if any field fails. For the regression itself, build the design matrix with an explicit leading 1.0 column plus the two features, and solve via GSL's `gsl_multifit_linear` (or normal equations via `gsl_linalg_QR_solve`) to get least-squares coefficients — don't hand-roll matrix inversion, which is numerically fragile. Compile with `gcc train.c -o train -lgsl -lgslcblas -lm`, run it, and format the three output lines to exactly 4 decimal places using `%.4f`, matching the label text precisely.
