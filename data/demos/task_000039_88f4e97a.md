Work in stages and verify each before moving on. First, load `edges.csv` into a `networkx.Graph()` (undirected, ignore `cost`), then run `nx.pagerank(G)` with defaults and `nx.algorithms.community.greedy_modularity_communities(G)`; assign `community_id` by the index of each node's set in the returned list (0-based). Merge these onto the `nodes.csv` rows in pandas before loading into SQLite so every node row carries `node_id, node_type, pagerank, community_id`. Build the SQL query incrementally: filter `node_type = 'Distribution Center' AND pagerank > 0.01`, `ORDER BY pagerank DESC, node_id ASC`, then `LIMIT 5 OFFSET 5` for page 2 — test the un-paginated query first to sanity-check row counts before adding LIMIT/OFFSET. Capture the plan with `EXPLAIN QUERY PLAN <same query>` and write raw output to the plan file. When exporting to JSON, watch the strict schema: since `additionalProperties: false`, only emit exactly the four required keys, and cast `community_id` to a native Python `int` (not numpy/SQLite float) so it serializes as a JSON integer. Finally write `validate.py` so it loads both files, calls `jsonschema.validate`, and only exits nonzero on failure.
