Build the server with `http.server` (or similar), binding `0.0.0.0:8000`, handling `POST /process_config` by reading `Content-Length` bytes from the request body. Per request: write the body to a temp file, open it with `zipfile.ZipFile` and extract into a fresh `tempfile.mkdtemp()` workspace — use a new temp dir per request so concurrent/sequential calls don't collide. Read `update.wal`, split each non-empty line on `|` with maxsplit=2 to get COMMAND, FILENAME, PAYLOAD (payload may itself contain `|` in theory, but base64 won't, so plain split is fine). For CREATE, `base64.b64decode(payload)` and write bytes (mode `wb`, creating parent dirs if the filename has subdirs); for APPEND, only append if the file already exists; for DELETE, remove the file if present, ignoring errors otherwise. After replaying all ops, delete `update.wal`, then walk the workspace, collect every remaining file's path relative to the workspace root, sort those relative paths alphabetically, and for each call `subprocess.run(["/app/config_hasher", abs_path], capture_output=True, text=True)` — treat the binary as opaque, just pass the absolute path and capture stdout, stripping trailing newline before writing `rel: hash` lines to `hashes.txt` at the workspace root. Zip up all remaining files (including the new `hashes.txt`) with archive paths relative to workspace root, and return the zip bytes with `Content-Type: application/zip` and status 200. Clean up the temp workspace after responding.