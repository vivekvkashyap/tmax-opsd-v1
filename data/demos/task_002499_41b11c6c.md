Don't over-invest in "repairing" the index — first confirm with `sqlite3 /home/user/backup_topology.db "PRAGMA integrity_check;"` and a plain `SELECT * FROM edges` whether the index is actually returning bad data or the query planner is just using it; if in doubt, safely force correctness with `REINDEX edges;` (or `DROP INDEX idx_edges_source; CREATE INDEX idx_edges_source ON edges(source_id);`) before relying on any indexed lookup. Then do the graph traversal: either write a SQLite recursive CTE (`WITH RECURSIVE reach(id) AS (SELECT id FROM nodes WHERE hostname='main-db-server' UNION SELECT e.target_id FROM edges e JOIN reach r ON e.source_id=r.id) SELECT hostname FROM nodes WHERE id IN reach AND hostname != 'main-db-server'`), or do the traversal in Python (read all edges/nodes, BFS/DFS from the main-db-server node id, e.g. with NetworkX's `descendants()` or a manual visited-set walk) — either way make sure you find the starting node's id by hostname first, don't hardcode it. Collect the reachable hostnames (excluding the start node itself), sort them alphabetically, and write them as a JSON array of strings to /home/user/downstream_targets.json using `json.dump`, ensuring valid JSON syntax (no trailing commas, correct quoting).