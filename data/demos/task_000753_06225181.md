First fix the vendored build: inspect /app/vendored/cypher-parser/Makefile and note it's missing a required C++ standard flag needed for its modern C++ constructs — add the correct `-std=c++11` (or whatever the source needs) to CXXFLAGS, then rebuild and install so `import cypher_parser` works in Python (check for a setup.py/pip install step or a Python binding build target). Verify with a quick `cypher_parser.parse("MATCH (n) RETURN n")` smoke test before moving on. Then write /home/user/query_filter.py to read all .cypher files in --input-dir and classify each: REJECT if it contains mutating keywords (CREATE, MERGE, SET, DELETE, REMOVE, DROP — match as whole Cypher clause keywords, not substrings of identifiers), unbounded variable-length path syntax like `[*]` or `[*..]`, or multiple disconnected MATCH patterns lacking a relationship/explicit WHERE join (a naive Cartesian-product detector: multiple MATCH clauses whose node variables never overlap and are never joined). Otherwise ACCEPT. Write one `<filename>: <ACCEPT|REJECT>` line per file to --output-log. Before finishing, run your script against both /app/corpora/clean and /app/corpora/evil and confirm every clean file is ACCEPT and every evil file is REJECT — 100% is required — then save those runs to /home/user/clean_results.log and /home/user/evil_results.log.