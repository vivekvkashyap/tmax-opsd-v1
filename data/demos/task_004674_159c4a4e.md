Do this entirely with awk for the numeric work since sequences span multiple FASTA lines. Parse `/home/user/sequences.fasta` in one awk pass: on a `>` line flush the previous accumulated sequence (id, concatenated bases with only A/C/G/T counted — strip/ignore anything else, note case if any), then at the end flush the last record; for each record compute length and `gc_count/length*100` with `%.4f`, writing `id\tlength\tgc` to gc_content.tsv. For the regression, read gc_content.tsv back (or keep arrays from the same pass) and compute sums of X, Y, X*Y, X*X across all N sequences, then apply the closed-form OLS slope `(N*ΣXY - ΣXΣY)/(N*ΣXX - (ΣX)^2)` and intercept `meanY - slope*meanX` in awk (or bc) with `%.4f` formatting, writing `slope intercept` to regression.log — double check the exact formula against a couple of records by hand before trusting the loop. For the histogram, bucket each gc value into `floor(gc/10)*10` except gc==100 which stays in bin 90, tally counts in an array indexed 0,10,...,90, and print all ten bins in ascending order (including zero counts) to histogram.log.
