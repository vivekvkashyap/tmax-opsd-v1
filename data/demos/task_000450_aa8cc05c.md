Diagnose the two failures independently before touching code. For `setup_env.sh`: run it once to see it fails on `mkdir` because a directory already exists — the general fix is to make every directory-creation idempotent, e.g. replace bare `mkdir <dir>` calls with `mkdir -p <dir>` (which succeeds whether or not the directory exists) rather than special-casing the one directory you observe failing, since the grader may test with a range of pre-existing states. For `monitor.c`: read the crash with `gdb` or by reasoning through the code — it likely calls `strtok`/`atoi` on a line and then dereferences/converts a token that can be `NULL` when a line is blank (empty lines produce no tokens), causing a NULL-pointer segfault in `atoi` or a printf `%s`. Fix by checking each parsed token for `NULL` and `continue`-ing (skipping) the line before using it, rather than assuming every line matches the expected format. Recompile with `gcc -o /home/user/monitor /home/user/monitor.c`, then run the wrapper script, redirecting both stdout and stderr to the log file, and confirm it exits 0 and the log ends with the daemon's completion message.
