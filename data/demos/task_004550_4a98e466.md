Start with extraction: `ffmpeg -i /app/telemetry_stream.mp4 -map 0:s:0 /home/user/extracted_logs.srt` pulls the subtitle stream; then strip SRT numbering/timestamps/blank lines with something like `grep -vE '^[0-9]+$|-->|^$' extracted_logs.srt > raw_telemetry.txt` and inspect the result to confirm only pipe-delimited data lines remain before building the processor. Write `process_telemetry.sh` as an `awk` (or awk-driven bash) pipeline reading stdin: for normalization, convert epoch-ms to seconds with integer division (`int(epoch/1000)`) and shell out to `date -u -d @<sec> +%Y-%m-%dT%H:%M:%SZ` per line via `getline` from a `date` command (or build the same formatting in pure awk with `strftime`/`systime` if available) — truncate, don't round, the milliseconds. For masking, only replace the letters when the plate is *exactly* 3 uppercase letters + 3 digits (anchor the regex `^[A-Z]{3}[0-9]{3}$`), leaving anything else (e.g. `A1B2C3`, `UNKNOWN`) untouched. For deduplication, key on `EventType|LicensePlate` (the *unmasked* plate) in an associative array storing the last-seen timestamp for that key, and drop a record if `abs(epoch - last_seen[key]) <= 3000`, otherwise process it and update `last_seen[key]` — do this dedup check on the raw timestamp before masking, keeping only the first occurrence in stream order. Make the script executable and test it against several hand-built streams including near-boundary (2999ms vs 3001ms) duplicate cases, since grading is bit-exact fuzz comparison against a hidden reference.
