Two data-recovery steps precede the analytics: (1) query `/app/company.db` with `SELECT ... FROM employees WHERE status='active'` but force it to bypass the corrupted `idx_status` index, e.g. `SELECT * FROM employees NOT INDEXED WHERE status='active'` (or `DROP INDEX idx_status` first) — a plain indexed query will silently return stale/incorrect rows, so verify your query plan with `EXPLAIN QUERY PLAN` to confirm no index is used; (2) transcribe `/app/overrides.wav` (ffmpeg + a speech-to-text tool) to recover the extra directed edge(s) the HR director dictated — parse the spoken "from X to Y" pattern into (source, target) pairs. Build the graph only from `employee_edges` rows whose source and target are both in your true-active set, plus the audio-derived edges (also filtered to active nodes). Implement standard iterative PageRank (damping 0.85, uniform initial ranks, iterate to convergence) over this graph. Then stand up a minimal HTTP server (Flask/FastAPI/etc.) on 127.0.0.1:8000 exposing `GET /api/centrality?node=<id>` returning `{"node": <id>, "centrality": <float>}` computed from your PageRank result, and leave it running in the background (e.g. via `nohup ... &`).