Work through the pipeline in dependency order: fix the app first, then wire the hook, then commit. (1) `check_quota.sh`: use `stat -c%s` or `wc -c <` on `users.db`, compare to 1024, print the right message and exit code, then `chmod +x`. (2) Read `auth.py` to find the actual port it binds (`HTTPServer(('127.0.0.1', <port>), ...)`), then edit `config.json`'s `auth_port` to match that exact value — don't guess, the mismatch is the whole bug, and `api.py`'s crash is your signal you got it wrong. (3) Write `.git/hooks/pre-commit` as a shell script that runs `./check_quota.sh`, checks `$?`, exits nonzero (aborting the commit) if it failed, then runs `python3 run_tests.py` the same way; `chmod +x` it. (4) Stage the changed files and `git commit -m "Fix pipeline"` — this triggers the hook, which starts `auth.py` in the background, runs `api.py` against it, and on success writes the pass marker to `/home/user/test_results.log`. If the commit is rejected, read the hook's stderr to see which stage failed rather than guessing.