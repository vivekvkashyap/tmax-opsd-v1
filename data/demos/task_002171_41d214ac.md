The task description already tells you the binary's algorithm (tokenize on spaces, look up each token's 3D vector in `/app/weights.csv` with `[0.5,0.5,0.5]` as fallback, sum vectors, take the L2 norm, multiply by 0.85) — implement that directly in `scorer.py` reading `/app/weights.csv` at runtime (don't hardcode its rows, since the grader may swap the file) rather than reverse-engineering from scratch. Don't trust the hint blindly, though: since the grader fuzz-tests equivalence against the real binary, run `/app/legacy_scorer` yourself on a handful of crafted inputs (known tokens, unknown tokens, mixed, single-token, empty-ish) and diff against your Python script's output on the same inputs to catch formatting or edge-case mismatches (e.g. `%.4f` rounding, trailing newline handling) before trusting the reimplementation. For the plotting bug, the classic cause is calling `plt.show()` before `plt.savefig()` (which clears the figure) or missing a non-interactive backend — fix by calling `matplotlib.use('Agg')` before importing `pyplot` and ensuring `savefig()` runs before (or instead of) `show()`.
