Build the classifier as three independent pattern checks over the log text, then OR them together: (1) regex-match GCode commands `M8000`-`M8999` as whole tokens (careful with word boundaries so `M80001` doesn't false-match and `M799` doesn't false-match either); (2) search for the proprietary ELF section name as a literal substring — you must first recover this name yourself by extracting a frame from `/app/tutorial.mp4` around the 2-second mark (e.g. `ffmpeg -ss 2 -i /app/tutorial.mp4 -frames:v 1 frame.png`) and running `tesseract` OCR on that frame to read the terminal text it shows, then use whatever section name you actually read; (3) search for the literal string `PROPRIETARY_TX`. Exit 1 if any marker is found, else exit 0. For the audit log, open the file in append+binary or text mode, call `fcntl.flock(fd, fcntl.LOCK_EX)` before writing your single formatted line and release it after — this is the part naive attempts skip, causing interleaved/corrupted lines under concurrent execution. Test manually by running many parallel invocations against dummy files and checking the log has no garbled lines.