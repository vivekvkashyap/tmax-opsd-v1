Build `/home/user/etl_pipeline.sh` as a BFS over the JSON node files using `jq` for parsing and bash associative arrays for visited/distance tracking — a naive DFS or unindexed re-scan per hop gives wrong shortest-hop answers. Queue-based BFS: start with START_NODE at hop 0, pop from queue, if unvisited mark visited/record hop count/add weight to running total, push its `linked_nodes` (read via `jq -r`) with hop+1. Track the first hop count at which END_NODE is dequeued as the shortest path (or -1 if the queue empties without reaching it); handle START_NODE==END_NODE as hop 0 upfront. Sum weight only once per node on first visit, not per edge. For line 3, after BFS finishes, filter reachable nodes to weight>=20, sort with `sort -t, -k2,2nr -k1,1` (numeric descending weight, then id ascending) via a temp `weight,id` list, take head -3, join with ", ". Quote all jq/node-id variables to avoid word-splitting, and write all three lines to `/home/user/etl_report.txt`.