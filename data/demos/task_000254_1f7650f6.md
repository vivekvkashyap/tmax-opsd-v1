Investigate before editing: start the daemon as-is, send a sequence of normal `POST /submit?value=` calls and check `GET /stats` matches simple arithmetic, then vary request properties (headers, query params, repeated values) one at a time to see which input flips the average away from the expected value — this isolates the backdoor condition instead of guessing at the source. Once you've confirmed the trigger via live requests, go read the corresponding HTTP handler / stats-recording code in the vendored source at `/app/analytics-daemon-1.2.3` to find the conditional branch that alters the recorded value (e.g. transforming it, such as squaring, only when a specific header or param is present) and remove that branch entirely so the handler always records the value exactly as submitted, for every request regardless of headers. After patching, rebuild the Go binary, restart it bound to `127.0.0.1:8080` in the background, and re-run your earlier probing requests (including the one with the special header) to confirm `/stats` now reflects the true simple average/count with no special-case behavior — don't just trust that the code looks fixed, prove it via live HTTP calls.
