Break this into independent pieces and verify each before wiring them into the API. For the video: use ffmpeg (or cv2 VideoCapture) to extract frames, locate the brightest pixel's x-coordinate per frame with numpy (`np.unravel_index(np.argmax(frame), frame.shape)`), build the time series, then run `np.fft.rfft` with the correct sample spacing (1/60s), take the magnitude spectrum, and pick the peak bin excluding index 0 (DC) — convert bin index to Hz using `np.fft.rfftfreq`. Save a matplotlib plot of frequency vs magnitude to the required path. For the simulation bug: the root cause is that `multiprocessing.Pool.imap_unordered` returns results in completion order, so summing as results arrive makes the reduction order (and thus floating-point rounding) nondeterministic. Fix by collecting `(index, value)` pairs, sorting by index, and summing in that fixed order (consider `math.fsum` for extra precision) so the total is bit-for-bit identical across runs — verify by running the fixed script twice and diffing the exact output. Finally wrap both pieces behind a Flask/FastAPI app on 127.0.0.1:8080 with the two exact endpoints, launch it as a background process, and confirm it stays reachable after your shell session moves on.