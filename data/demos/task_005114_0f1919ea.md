Treat the legacy binary as an opaque black box: don't try to guess its algorithm, discover it empirically by piping a few known small strings into `/home/user/etl_project/bin/legacy_norm` via stdin and observing stdout, so your aggregation logic operates on whatever normalized tokens it actually emits. Build the pipeline as clear stages: (1) extract — for each `.txt` file, `cat file | ./legacy_norm` and collect tokens, logging a JSON line with the record count after this step; (2) aggregate — count token frequencies with a dict/Counter, log another JSON line; (3) merge — load `reference.csv` into a word->category map, sum counts per category (defaulting unmapped words to `"UNKNOWN"`), log a third JSON line. Persist the category totals (dict or SQLite) so the HTTP server can read them without recomputing. For serving, use Python's built-in `http.server` or Flask bound exactly to `127.0.0.1:9000`, parse the `category` query param from the URL, return the exact JSON shape requested, and respond 404 when the category key is absent from your aggregated map. Run the full pipeline once, confirm `etl.log` has three well-formed JSON log lines, then launch the server as a background process so it stays up for verification.