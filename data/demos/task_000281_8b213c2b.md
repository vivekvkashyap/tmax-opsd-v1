First recover the policy from the video: extract frames with `ffmpeg -i /app/schema_meeting.mp4 -vf fps=5 frame_%03d.png` (or similar), then run `tesseract` OCR over the frames (or inspect a few candidate frames visually if OCR is unreliable) to locate the one showing `MAX_DEPTH=<N>` and the `ALLOWED SCHEMA:` list of `(Label)-[:REL]->(Label2)` triples — record these as your program's constants, derived from what you actually read, not assumed. In query_filter.c, scan each query text for `[*` followed by digits, `..`, digits, `]` to find variable-length path bounds (use `sscanf`/manual parsing to grab the upper bound), and reject if it exceeds the discovered max depth. Separately scan for `(Label1)-[:REL_TYPE]->(Label2)` patterns with `strstr`/manual tokenizing, and reject if any such explicit triple isn't a member of the whitelist set you built from the video (order matters — a reversed relationship like swapping Label1/Label2 is a distinct, disallowed triple). Iterate: compile, run against every file in `/app/corpus/clean/` and `/app/corpus/evil/`, and refine parsing until all clean queries exit 0 and all evil queries exit 1.
