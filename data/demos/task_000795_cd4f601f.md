Build the pipeline in three verifiable stages. First, create `/home/user/dataset/graph.db` with `sqlite3`, define `citations(source INTEGER, target INTEGER)`, and bulk-load via `.mode csv` + `.import` (or Python's `csv`/`sqlite3` module) — then add indices that actually match the two access patterns called out: an index on `source` (and ideally a composite `(source, target)` index, which also lets SQLite check pair-existence via an index-only lookup) using `CREATE INDEX`. Verify indices landed with `sqlite3 graph.db ".indices citations"`. Second, in Python, connect to the SQLite file (not the raw CSV — the task wants you reading from the DB you built) and build a `networkx.DiGraph` by iterating `SELECT source, target FROM citations`. Third, use `networkx.shortest_path(G, source=10, target=99)` for a plain BFS shortest path (unweighted, since no weight column is mentioned), then join the resulting node list with commas and write it to `/home/user/dataset/shortest_path.txt` with no extra formatting. Double check node 10 and 99 actually exist as connected in the graph before trusting the result.
