Compile the given C function into a real shared object first: `gcc -shared -fPIC -o /home/user/py_telemetry/libmathops.so /home/user/legacy/math_ops.c`. In Python, load it with `ctypes.CDLL(...)`, and explicitly set `lib.calculate_score.argtypes = [c_double]*3 + [c_long]*2` and `.restype = c_double` — skipping this is the most common way this kind of ctypes call silently returns garbage on 64-bit systems. Parse `loadavg` by splitting on whitespace and taking the first three fields as floats; parse `meminfo` by scanning lines for `MemTotal:`/`MemFree:` and taking the numeric field before `kB` as a Python int (map to `c_long` when calling). Call the library function with these five values, round the returned score to 2 decimals, and classify status as `"CRITICAL"` if score < 50.0 else `"OK"`. Assemble the exact nested JSON structure requested (types matter: floats stay floats, mem fields stay integers) and write it to `/home/user/telemetry_out.json`.
