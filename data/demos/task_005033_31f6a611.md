For the video part: use `ffmpeg -i /app/dashboard.mp4 -vf fps=<N> frames/out_%04d.png` to extract frames at a rate dense enough to catch every on-screen change without duplicating too much, then run `tesseract` on each frame (or batch via a small script) to get text. Do not just count OCR hits per frame — dedupe consecutive frames that show the same alert into one "distinct segment" (track state transitions: alert-absent -> alert-present counts as one occurrence), since the alert persists across many frames. Write the resulting integer to `/home/user/backup_count.txt` with no extra text.

For the Rust filter: start a `cargo new` project at `/home/user/query_filter/`, build it with `cargo build --release` so the binary lands at `target/release/query_filter`. Read the corpus in `/app/corpus/clean/` and `/app/corpus/evil/` to derive robust detection rules rather than hardcoding filenames: check destination paths against an allowlist prefix (`/mnt/backups/`) rejecting anything else (`/tmp/`, `/var/www/`, etc.), reject `UNION SELECT`, and reject `COPY ... PROGRAM` (and similar shell-exec constructs), case-insensitively. Exit 0 only if a file has zero malicious patterns across all lines; exit 1 if any line matches a malicious pattern. Test against the local corpus first, then generalize the regex/keyword rules since the hidden test set is larger.