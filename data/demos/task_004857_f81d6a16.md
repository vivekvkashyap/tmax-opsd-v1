For `setup_proxy.py`, build the desired nginx.conf content as a string first (with all required directives: pid path, error/access log paths, events block, http block with an `upstream backend` containing both 127.0.0.1:8081 and :8082, and a server on 8080 with `location / { proxy_pass http://backend; }`), then implement idempotency by reading the existing file (if any) and comparing its content to the desired string — only write and print "CONFIG_UPDATED" if different or missing, otherwise print "CONFIG_UNCHANGED" without touching the file (this matters because the verifier re-runs the script and checks stdout both when unchanged and after an external edit). For `backup.py`, sum file sizes under `/home/user/storage/` with `os.path.getsize`/`os.walk`, compare to the 50MB threshold, and branch: over quota writes exactly `QUOTA_EXCEEDED` to `backup.log` and skips tar creation; at or under quota creates `/home/user/backups/storage_backup.tar.gz` with Python's `tarfile` module (mode `"w:gz"`) and writes exactly `BACKUP_SUCCESS`. Create the 10MB dummy file with `dd if=/dev/zero of=/home/user/storage/dummy.txt bs=1M count=10`, then run both scripts once to leave the final state described.