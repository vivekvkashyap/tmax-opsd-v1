Build and smoke-test each layer before stacking the next on top. First write the Flask app with the three POST routes; implement `/api/merge` and `/api/diff` using Python `set`/`sorted()` semantics exactly as named (merge = union of both lists as a sorted combined array — note it says "containing all elements from both lists" so decide whether duplicates are kept by testing your understanding against the requirement text carefully; diff = symmetric difference, i.e. elements in exactly one of the two sets, sorted). Package it with a `setup.py` that declares `entry_points={'console_scripts': ['run-math-api=math_ops.cli:main']}` (or similar), then `pip install -e /home/user/math_ops` and run `run-math-api` in the background, confirming with `curl localhost:5000/api/sort` directly before touching Nginx. Only then write `nginx.conf` with all temp-path directives redirected under `/home/user/` (required since you lack root — Nginx's default paths need root-owned dirs) and `pid /home/user/nginx.pid;`, start it with `nginx -c /home/user/nginx.conf`, and finally issue the three curl POSTs through port 8080, appending each raw response body as one line to `/home/user/test_results.log`.