Use `git bisect start`, mark current `HEAD` as `git bisect bad` and the earliest commit (`git log --reverse` to find it, or `git rev-list --max-parents=0 HEAD`) as `git bisect good <hash>`, then at each step run `./build.sh` and report `git bisect good`/`bad` based on its exit code — automate this with `git bisect run ./build.sh` if `build.sh`'s exit code reliably reflects pass/fail. Once bisect identifies the culprit commit, save its full 40-char hash (`git rev-parse <commit>`) to `/home/user/bad_commit.txt`. To diagnose, inspect that commit's diff (`git show <commit>`) for the loop/array boundary change; if it's not obvious from the diff alone, compile with `gcc -g` and step through with `gdb --args ./process data.csv`, watching the loop index versus array bounds (arrays sized for `count` entries but a loop iterating `<= count` reads/writes one past the end — a classic off-by-one). After `git bisect reset`, fix the C source at the tip so the loop condition doesn't exceed valid array bounds, then rerun `./build.sh` and confirm it exits 0 with the expected pass message before finishing.
