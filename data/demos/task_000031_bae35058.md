Approach this as log-diagnosis first, then reproduce, then fix — in that order, since the fix must make the regression test you write actually pass. For Phase 1, grep every `*.log` file in `/home/user/logs/` for lines containing `ERROR`, extract the file path token from each matching line (the path segment after "file:" or similar, per the log's exact wording), and write them as a JSON array to `/home/user/diagnostics/failed_files.json` (create the directory first). Look closely at the failing paths — they'll reveal a filename-content pattern (e.g. embedded spaces) that explains the failures, which is your root cause hint for Phase 3. For Phase 2, append a `#[test] fn test_filename_with_spaces_regression()` to the bottom of `src/processor.rs` that creates a temp file whose name contains a space (e.g. via `std::env::temp_dir()` + a manually chosen filename with a space, writing it with `std::fs::write`), calls `execute_worker_job` on its absolute path, and asserts `Ok(())`. For Phase 3, find where `execute_worker_job` builds a shell command by string-concatenating the filepath and replace that with `Command::new(script_path).arg(filepath)` (or equivalent), which passes the argument directly without shell interpretation, avoiding word-splitting on spaces. Run `cargo test` to confirm.
