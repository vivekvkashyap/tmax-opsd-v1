First unblock the vendored crate: inspect `/app/vendor/gap_filler/build.rs`, note it panics unless env var `GAP_FILL_BUILD_TOKEN` is set (the environment only provides `BUILD_TOKEN`), and fix it by either editing build.rs to read the variable name that's actually available, or supplying `GAP_FILL_BUILD_TOKEN` yourself via `.cargo/config.toml` `[env]` or by exporting it in your build invocation — confirm with `cargo build --release` inside the package before moving on. Then scaffold `/home/user/etl_pipeline` with `cargo new`, add `gap_filler` as a `path = "/app/vendor/gap_filler"` dependency in Cargo.toml, and implement `src/main.rs` to: read stdin CSV lines, drop rows with value outside [0,1000], group by sensor_id, compute per-sensor min/max timestamp floored to 60s buckets, for each bucket take the max value of rows falling in `[bucket, bucket+59]` or else carry forward the previous bucket's value, and print the required `REPORT: ...` lines sorted by sensor_id then bucket_timestamp. Since grading is byte-for-byte fuzz equivalence against a hidden oracle, get boundary conditions (bucket edges, empty groups, carry-forward on the very first bucket) exactly right and test with `cargo build --release` + manually crafted CSV edge cases.
