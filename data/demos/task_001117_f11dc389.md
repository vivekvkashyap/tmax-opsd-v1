Debug the script incrementally rather than rewriting it blind. First run `/app/process_alert.sh` as-is (or its `ffmpeg`/`awk` extraction step alone) to see the raw RMS levels emitted for `/app/alert_log.wav`, and confirm that silent frames really do emit the literal string `-inf` — that's what breaks `bc`. Fix this by substituting `-inf` with `-100.0` (e.g. via `sed`/awk or a bash conditional) before any arithmetic touches that value. Next, inspect the moving-average loop: it always divides by the fixed `window_size` (5) even when fewer than 5 frames have been seen — change the divisor to the actual number of elements currently in the rolling history array (`${#history[@]}`), not a constant, so the first four outputs are simple averages of 1..4 samples. Keep the sliding-window trimming logic (drop the oldest element once more than 5 accumulate) intact. After fixing both bugs, rerun the script end-to-end against the unmodified `/app/alert_log.wav` path and confirm it writes one numeric line per frame to `/home/user/smoothed_levels.txt` without errors.
