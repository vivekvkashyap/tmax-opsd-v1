First recover the required struct layout and exported function name by reading `/app/struct_spec.png` with an OCR/vision approach (e.g. open and read the image directly, or run an OCR tool) — do not guess field names/types, since `fastnode.c`, the ctypes bindings, and the build must exactly match what the image specifies. Fix `setup.py` by adding the missing `from setuptools import setup, Extension` import and pointing the Extension's sources at the actual `.c` file (not `.cpp`) with correct extra compile args, then confirm `pip install -e .` succeeds. Inspect `fastnode.c` for buffer/array bounds: a common bug pattern is an off-by-one loop like `for (i = 0; i <= N; i++)` writing one past the end of a fixed-size buffer — fix any `<=` that should be `<` and audit all array accesses against the struct's declared sizes to eliminate the segfault. After it builds cleanly, run `pytest test_prop.py` to confirm the hypothesis fuzz tests pass with zero segfaults, then run `python benchmark.py`, and only after confirming it prints a real timing number under the 0.5s threshold, copy that exact printed value into `metric.txt` (don't fabricate a number — if it's over threshold, optimize the C code, e.g. reduce unnecessary copying/allocation, and re-benchmark).
