This task has two independent halves — get the multi-service pipeline flowing end-to-end, and separately build a byte-exact Python transformer. For Nginx: edit `/home/user/services/nginx/nginx.conf` to add a `log_format json_log '{...}'` directive building a valid single-line JSON object with keys timestamp ($msec), user_id ($http_x_user_id), endpoint ($uri), latency ($request_time) — remember to escape embedded quotes properly in nginx config syntax — and point `access_log /home/user/services/logs/access.log json_log;` in the server block. For Vector, configure a `file` source tailing that access log, a `remap`/`json` transform to parse each line as JSON, and a `redis` sink pointed at 127.0.0.1:6379 pushing to list key `live_access_logs`; restart everything via `/home/user/manage_services.sh restart` and validate with `curl` + `redis-cli LPOP live_access_logs`. For `/home/user/log_transform.py`, read the JSONL, group by user_id, use `multiprocessing`/`concurrent.futures` per-user, sort by timestamp, compute a strict trailing 3-event SMA of latency and abs latency diff from the prior event (0.0000 for the first), then merge and globally sort by (timestamp, user_id) before emitting exact-format CSV (4 decimal places, no extra spaces) to stdout — since grading is bit-exact fuzz equivalence, match formatting and sort order precisely.
