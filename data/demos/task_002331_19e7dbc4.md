Set up the CMake project skeleton first, since the dependency wiring (zlib link + RapidCheck FetchContent) has to be correct before any code compiles against them: `find_package(ZLIB REQUIRED)` and link it to your executables, and use `FetchContent_Declare`/`FetchContent_MakeAvailable` for RapidCheck from the given GitHub URL, installing `zlib1g-dev` via apt-get if `find_package` fails. Implement TMR encode as a simple 3x byte replication, and decode as per-3-byte-block majority vote (compare all three bytes pairwise and return whichever value appears at least twice — if all three differ, any deterministic tiebreak is fine since the property test never produces that case when at most one byte per block is corrupted). Write the RapidCheck property test using an arbitrary `vector<uint8_t>` generator, corrupt at most one byte per encoded 3-byte block (e.g. flip it to a random different value with some probability, leaving many blocks uncorrupted), decode, and assert equality with the original — redirect the test binary's run output to `/home/user/test_results.log`. For tmr_process, walk `.dat` files recursively (e.g. `std::filesystem::recursive_directory_iterator`), compute CRC32 via zlib's `crc32()` over the full file contents, write the TMR-encoded sibling with `.tmr` appended, and append one CSV row per file using the path relative to the data_lake root and the checksum formatted as lowercase 8-hex-digit (zero-padded). Build with `cmake -B build && cmake --build build`, run the test executable, then run tmr_process and inspect report.csv and a sample .tmr file's size/content to sanity-check before finishing.
