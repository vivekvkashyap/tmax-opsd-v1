Do not parse the CSV line-by-line with naive `getline`/split-on-comma — the `text` field can contain embedded commas and newlines inside quotes, so implement a proper quoted-field state machine (or use a small CSV parsing library) that: reads character by character, toggles an "inside quotes" flag on `"`, treats a doubled `""` as a literal escaped quote, and only treats a comma/newline as a delimiter when outside quotes. For the timestamp, parse the fixed `YYYY/MM/DD-HH:MM:SS` layout with `sscanf` (or manual substring slicing since it's fixed-width) and re-emit as `YYYY-MM-DDTHH:MM:SSZ` — this is a straightforward reformat, not a timezone conversion. When emitting each JSON line, escape embedded real newline characters in `text` as the two-character sequence `\n` and existing double quotes as `\"`, building the JSON string manually (or via a minimal JSON string-escaping helper) since you're not using a JSON library. Compile with `g++ -o /home/user/parser /home/user/parser.cpp`, then run it against `/home/user/locales.csv` to produce `/home/user/processed_locales.jsonl`, diffing your output against your own hand-traced expectation for each row before moving on. Finally, write `setup_cron.sh` to append a crontab entry via `(crontab -l 2>/dev/null; echo "30 2 * * * /home/user/parser /home/user/locales.csv /home/user/processed_locales.jsonl") | crontab -`, and execute it so `crontab -l` shows the exact line.
