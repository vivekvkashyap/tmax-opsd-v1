Start with OCR since it drives the rest of the logic: run `tesseract /app/legacy_docs.png stdout` (preprocessing with `convert`/`ffmpeg` if the scan is noisy) to recover the exact C function signature exported by `libcert.so` and the precise peer-dependency security rules — read the recovered text carefully rather than guessing, since your filter's ACCEPT/REJECT logic must implement those rules exactly. Define `manifest.proto` with the four fields/tags exactly as specified, then compile it with `protoc --python_out=. manifest.proto` to get a usable Python module. In `pipeline_guard.py`, parse the CLI argument as a file path, deserialize it into your generated `Manifest` protobuf message, then load `libcert.so` via `ctypes.CDLL`, set `.argtypes`/`.restype` matching the OCR'd signature (likely two C strings in, an int return), and call it with `package_name`/`version` encoded to bytes. Combine that call's result with the other OCR'd rules (checking `peer_deps` list membership and `package_name` prefix conditions) to decide accept/reject, then `sys.exit(0)` or `sys.exit(1)` accordingly. Validate against the sample files in `/home/user/test_manifests/` before trusting it, since grading runs your script against a larger hidden clean/evil corpus requiring 100% correct classification.