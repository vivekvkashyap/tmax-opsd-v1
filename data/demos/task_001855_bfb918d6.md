Build this as a two-pass streaming C program rather than loading everything into a regular buffer. Pass 1: `mmap` the file read-only, treat it as a `double*` array of length `file_size/sizeof(double)`, and accumulate sum and sum-of-squares in a single pass to get population mean and stddev (mu = sum/n; sigma = sqrt(sum_sq/n - mu*mu), or use Welford's algorithm for better numerical stability on 10M+ values). Pass 2: iterate the same mmap'd array again, timing only this loop with `clock_gettime(CLOCK_MONOTONIC, ...)` before/after, write any value outside `[mu-3*sigma, mu+3*sigma]` directly to the output file via buffered `fwrite` of raw 8-byte doubles (not text) so the anomalies.bin stays binary-compatible. Print the report with `%.6f` format specifiers for the three float fields and an integer count, matching the exact `Key: value` line format requested. Compile with `gcc -O3 -o /home/user/clean_data /home/user/clean_data.c` (may need `-lm` for `sqrt`). Sanity-check by comparing your program's mean/stddev/anomaly count against a quick Python `numpy` computation on the same file before considering it done.