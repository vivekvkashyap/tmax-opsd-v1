Build this as a classic awk/sort validate-then-dedupe pipeline rather than trying to do everything in one pass. First, filter with awk using `-F,` and per-field regex/equality checks: Locale must match `^[a-z]{2}-[A-Z]{2}$`, TranslatedText (field 3) must be non-empty, and State (field 5) must be exactly `APPROVED` or `DRAFT` — reject the row if any check fails. Second, for deduplication, sort the valid rows by key (TranslationKey, Locale) then by Timestamp descending, and use `awk` (or `sort -k1,2 -k4,4nr | awk '!seen[$1,$2]++'`) to keep only the first row seen per (key, locale) pair, i.e., the one with the highest timestamp — watch out for numeric vs lexicographic sort on the timestamp field (`sort -n` on the right column). Third, write the deduped rows to the output CSV, then count rows where field 2 == `en-US` and field 5 == `APPROVED` with a simple awk sum, writing that integer to the metrics file. Finally, use an `if [ "$count" -lt 3 ]; then exit 1; else exit 0; fi` gate at the end of the script, chmod it executable, and actually run it once so the output files exist for verification.