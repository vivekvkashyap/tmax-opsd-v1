Treat this as four independent sub-tasks that must each be verified in isolation before moving on. (1) Parse `/home/user/auth.log` line by line looking for the authentication-outcome keyword (e.g. "Accepted" vs "Failed") tied to `service_account` — only a successful auth line gives you the attacker IP; write just that IP (no extra text/newline issues) to `/home/user/evidence/attacker_ip.txt`. (2) Lock down permissions with `chmod 700` on the evidence directory and `chmod 600` on every file inside it — do this after all files are written, since later writes could reset modes. (3) For the vendored `darkhttpd-1.14` build, diff the Makefile against what a normal darkhttpd Makefile expects — a bogus `CC` value is a common sabotage pattern, so check the compiler variable first. Then grep `darkhttpd.c` for header-string comparisons (e.g. searching for a custom header name) inside the request-handling function — that conditional bypassing path checks is the backdoor; delete it entirely rather than just neutering the condition, then rebuild with `make`. (4) Launch the fixed binary bound to 127.0.0.1:8080 serving the evidence directory, backgrounded and left running for the automated checks.
