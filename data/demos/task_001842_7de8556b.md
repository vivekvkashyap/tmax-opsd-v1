Diagnose before fixing: read `merge_logs.sh` to see it takes two file args and acquires a lock on the first then the second via `flock`, with a deliberate sleep between the two lock acquisitions — this is the classic circular-wait deadlock setup. Scan `jobs.txt` for a pair of lines where the same two files appear as arguments in opposite order (e.g. "A B" on one line and "B A" on another) — that reversed-order pair is the deadlock cause; save exactly one of those two lines verbatim to `mre.txt`. To fix `merge_logs.sh`, before acquiring any locks, compare the two file path arguments lexicographically (e.g. `if [[ "$f1" > "$f2" ]]; then swap them`) so locks are always taken in the same alphabetical order regardless of invocation order — this breaks the cycle. Add a bash assertion right before the locking section (e.g. `[[ "$f1" < "$f2" || "$f1" == "$f2" ]] || exit 1` after sorting) to guard the invariant. Do not touch `run_all.sh` or `jobs.txt`. Finally run `run_all.sh` and confirm `success.log` is created, meaning all jobs completed without hanging.