Do the encoding conversion first, independent of the package: read `/home/user/legacy_docs.json` as bytes, decode with `windows-1252`, re-encode/write as UTF-8 to `/home/user/docs_utf8.json` (e.g. `iconv -f WINDOWS-1252 -t UTF-8` or Python's `open(..., encoding='cp1252')` then write with `encoding='utf-8'`). Then debug the vendored package: open `/app/vendor/pydocorg-1.2.0/pydocorg/linker.py` and find where it builds the relative symlink target from inside `tags/<tag>/` back to `articles/`; reason about the actual directory depth — a symlink placed at `tags/<tag>/<file>.md` needs to climb out of both `<tag>/` and `tags/` before descending into `articles/`, so a single `../articles/...` is one level short. Fix the path traversal (or better, compute it robustly with `os.path.relpath` from the symlink's directory to the articles directory) rather than hardcoding a fixed number of `../`. Reinstall with `pip install -e /app/vendor/pydocorg-1.2.0`, run the `pydocorg build` CLI against the UTF-8 file, and verify by walking `/home/user/dist/tags` and checking `os.path.realpath` resolves every symlink to an existing file in `articles/` before considering the task done.