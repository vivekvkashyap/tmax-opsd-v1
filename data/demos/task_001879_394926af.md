Work the chain step by step — each stage unlocks the next. First extract frames from `/app/key_video.mp4` (`ffmpeg -i key_video.mp4 -vf fps=<video_fps> frames/frame_%03d.png`) and decode each frame's QR code in order with `zbarimg` (part of `zbar-tools`), then concatenate the decoded text chunks in chronological frame order to reconstruct the SSH key passphrase — do not guess the fps or skip frames, since QR chunks must be assembled in the correct sequence. Second, use that passphrase to unlock `/app/admin_key` (e.g. `ssh-add` with the passphrase, or `chmod 600` + `ssh -i /app/admin_key -p 2222 admin@localhost`), then `scp -i /app/admin_key -P 2222 admin@localhost:/opt/vulnerable_server /home/user/vulnerable_server`. Third, write `/home/user/patcher.go` to read the ELF binary as raw bytes, search for all byte-exact occurrences of the 35-byte string `http://super-secret-backend.local/`, and overwrite each occurrence in place with the same-length replacement `http://redacted-secure-domain.com/` (lengths match exactly so no offsets shift and the ELF structure stays valid) — do a raw `bytes.Replace`/manual scan across the whole file, not just specific sections, since string patching must not corrupt ELF headers or symbol tables. Write the result to `/home/user/patched_server`, `chmod +x` it, and compute+write its SHA256 to `/home/user/checksum.txt`. Finally run the patched binary and manually test a `/redirect?path=x` request to confirm the Location header now shows the redacted domain before considering it done.
