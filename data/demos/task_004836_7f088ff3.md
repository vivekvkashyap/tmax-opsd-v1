Do not try to fuzz the crash randomly first — reverse-engineer the binary's expected input format from the pcaps/logs and from static/dynamic analysis of `/app/processor_bin` (e.g. `strings`, `objdump -d`, or running it under `gdb`/with `ulimit -c unlimited` to get a core dump on crash and inspect registers/backtrace at the fault). Extract several payloads from the pcap captures that are known to have triggered the crash, and several that didn't, then compare their byte layout to spot the discriminating field (likely a length/count field near the header that doesn't match the actual remaining bytes — a classic buffer over-read). Confirm your hypothesis empirically: construct a handful of synthetic payloads that vary only the suspected field and rerun them against `/app/processor_bin` (checking exit status/signal) to prove the boundary condition precisely, rather than assuming. Only then implement `/home/user/detector.cpp` to parse the header structurally and compare the declared length against actual file size, compiling with `g++` and testing it manually against both crash and non-crash samples before considering it done.
