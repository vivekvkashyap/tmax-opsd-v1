Build this as a short pipeline of shell commands (a script is fine). Steps: (1) strip the CSV header and the id/rating columns, keep only review_text per row, tracking which text belongs to which id (e.g. iterate row by row rather than flattening everything together, since you need per-row counts later); (2) for cleaning, lowercase with `tr 'A-Z' 'a-z'` then replace any character that isn't a-z or space with a space (e.g. `sed 's/[^a-z ]/ /g'` after lowering), then split on whitespace; (3) build a global word-frequency count across all rows excluding any word present in stopwords.txt (use `grep -vxFf stopwords.txt` or an associative check), then rank by frequency descending with ties broken alphabetically (`sort -k2,2nr -k1,1` type approach, or awk with careful tie-break) and take the top 3, then re-sort those 3 alphabetically for the column order; (4) for each id, count occurrences of each of the 3 chosen words in that row's cleaned text and emit `id,count1,count2,count3` with no header spaces. Watch for: quoting/CSV fields with embedded commas or quotes in review_text (parse the CSV properly, don't just split on comma blindly), and getting the tie-break order exactly right since it changes which words become columns. Validate your bow_features.csv against manual counts for one row before finishing.