Generate the dataset first with `/app/data_generator 10000 > /home/user/dataset.csv`, then load it with pandas. The critical pitfall is data leakage: never call `fit`/`fit_transform` (StandardScaler, correlation ranking) on the full dataset before splitting. Instead, split into train/validation first (e.g. `train_test_split`), then build a scikit-learn `Pipeline`/`ColumnTransformer` where scaling and feature selection are fit ONLY on the training fold and applied (`transform`) to validation. For correlation-based feature selection, either use `SelectKBest(score_func=lambda X,y: abs(correlation with y), k=5)` or write a custom `TransformerMixin` that computes Pearson correlation with the target strictly inside `fit()` using only the data passed to it — never reference the full unsplit dataframe when selecting features. Wrap scaler, selector, and classifier in one `Pipeline` so `pipeline.fit(X_train, y_train)` performs every step correctly in sequence, then evaluate on a held-out split before saving with `joblib.dump(pipeline, "/home/user/model.joblib")`. Since the generator makes the first 5 features informative and the rest noise, a correct leak-free pipeline should comfortably clear the accuracy bar; use cross-validation on the training data to sanity check before finalizing.