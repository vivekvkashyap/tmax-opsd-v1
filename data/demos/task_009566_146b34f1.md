Write `router.cpp` with a plain POSIX accept loop: for each connection, read until you have two full `\n`-terminated lines (buffer partial reads — a single `recv` may not contain a whole line), parse line 1 as either `CRASH` or `ROUTE <email>`. On `CRASH`, write `BYE\n`, close the socket, and `exit(1)` — this is meant to kill the process so supervisord's `autorestart=true` gets exercised. On `ROUTE`, append line 2 + `\n` to `/home/user/mail_spool/<email>.log` (fopen "a"), reply `DELIVERED\n`, close, then loop back to `accept()` for the next connection — one process must serve many connections sequentially. Configure `supervisord.conf` with three `[program:...]` blocks pointing at the compiled binary on ports 9001-9003 with `autorestart=true`, and launch `supervisord -c ...` in the background. Write `haproxy.cfg` in `mode tcp` with a frontend on `127.0.0.1:8080` and a backend using `balance roundrobin` across the three ports; run `haproxy -f ... -D`. Before finishing, manually test with `nc`: route two messages, send a `CRASH`, wait ~2s, then route again and confirm both the spool files and continued availability through 8080.