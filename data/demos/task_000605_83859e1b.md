Write the FASTA parser carefully: a record's sequence may span multiple lines, so accumulate lines until the next `>` header or EOF, then concatenate before mapping characters to the given float values. Implement the trapezoidal integral and forward-difference derivative exactly as specified — note that the derivative array has length N-1, and the "variation" integral is the trapezoidal integral of the absolute derivative values (length N-2 pairs summed), not of the raw derivative. Be careful with floating point summation order matching the given formulas, and format output with exactly 2 decimal places (watch for `-0.00` vs `0.00` — normalize signed-zero results). Process records in the order they appear in the file and write the CSV with header `ID,I,DI`. After writing the Go program, actually `go build`/`go run` it against the real input file to produce `/home/user/results.csv` — verify by hand-computing one or two small sequences (e.g. a 4-base sequence) to sanity-check your formulas before trusting the full output.