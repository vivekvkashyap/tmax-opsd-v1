Use `git bisect` properly rather than manually eyeballing commits: pick a known-good early commit and HEAD (or a known-bad commit) as bounds, run `git bisect start`, `git bisect bad <bad>`, `git bisect good <good>`, then at each step test the current checkout by running `uptime_calc.sh` with sample inputs (e.g. `999 1000`) and checking whether the output shows precision loss (an integer/truncated result instead of a proper decimal) — mark `git bisect good`/`bad` accordingly until git identifies the culprit commit, and capture its full 40-char SHA into `/home/user/bad_commit.txt` (use `git rev-parse` to get the full hash, not the abbreviated one bisect prints). Run `git bisect reset` when done to return to `main`. For the fix, replace whatever integer-only bash arithmetic is truncating the division with a `bc -l` or `awk` computation using `printf "%.2f"` to preserve two decimal places. For `fuzz_test.sh`, loop generating random `total`/`successful` pairs with `successful < total` (e.g. via `$RANDOM`), call `uptime_calc.sh`, and `exit 1` immediately if any output is exactly `0` or `0.00` (since with random positive inputs a truncating bug would show this), otherwise `exit 0`; make it executable.