Build this as a sequential ETL script, testing each stage independently before chaining them. (1) Parse `/app/sensor_logs.jsonl` line-by-line, not with a single `json.loads` on the whole file — wrap each line's `json.loads` in try/except so malformed-Unicode-escape lines are caught and skipped rather than crashing the whole parse; count and log how many valid records you kept. (2) For the WAV, read raw samples (e.g. with `wave`/`scipy.io.wavfile`/`numpy`), detect tone-vs-silence by thresholding amplitude (or short-time energy) sample-by-sample or over small windows, then measure the duration of each on/off run to classify dots vs dashes vs the various inter-symbol/inter-letter/inter-word silence gaps — the base unit length is not given, so estimate it from the shortest tone/silence runs in the audio rather than assuming a fixed value. Map the resulting dot/dash sequences back to characters via the standard Morse table, producing a `<HHMM> <VALUE>` formatted string, then parse that into timestamp/value pairs for date `2023-10-01`. (3) Merge both sources, bucket into 5-minute intervals via mean, then build a full 12:00–13:00 5-min-interval index and use pandas linear interpolation (`Series.interpolate(method='linear')`) to fill gaps. (4) Write the required `[INFO]` log lines to `/home/user/pipeline.log` exactly as specified. (5) Align your series to `/app/reference.csv` by timestamp, compute MSE with numpy, and write the single float to `/home/user/mse_result.txt`. Double-check bucket alignment and interpolation edges — off-by-one bucketing is the most common source of a wrong MSE.
