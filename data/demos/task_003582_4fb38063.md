Get the service mesh working end-to-end before touching the C indexer, since the indexer depends on it. Edit `/home/user/services/docker-compose.yml` to bind NGINX to host port 8080, Flask to 5000, and Redis to 6379, and set the Flask `.env` so its Redis URL and upstream-NGINX URL point at those same ports on localhost; bring the stack up and manually curl `http://127.0.0.1:5000/docs.tar` to confirm the proxy chain (Flask → Redis check → NGINX → tar bytes) actually works before writing any C. In `indexer.c`, fetch the tar via libcurl or raw sockets, then: (1) verify integrity by reading the whole file, XOR-ing all 512-byte blocks except the trailing 32-byte checksum, and comparing against that checksum; (2) parse the POSIX ustar header format (name, typeflag, size, linkname fields) block by block; (3) when you hit a symlink entry, resolve it but track visited canonical paths in a set/hash so a cycle (e.g. `latest -> ../v1/latest` looping back) is detected and skipped rather than followed infinitely; (4) for each regular file entry, canonicalize its path against archive root `/` and append to `/home/user/valid_files.txt`. Cross-check your output list against a quick manual `tar -tvf` inspection of the archive to catch missed or duplicated entries before relying on the F1 grader.