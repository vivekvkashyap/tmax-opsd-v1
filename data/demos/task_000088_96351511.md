Do the path cleanup first with a targeted `sed` in-place edit, e.g. `sed -i 's#C:\\dataset\\#/home/user/dataset/#g; s#\\\\#/#g' /home/user/dataset/files.txt` — apply the specific-prefix substitution before the generic backslash-to-slash conversion so you don't accidentally mangle the prefix replacement, and verify the result with `cat` afterward. For the C++ parser: read `config.ini` with a simple line scan for a line starting with `MAGIC_BYTES=` and extract the substring after `=`. For each cleaned file path, open the `.dat` file in binary mode and read the 16-byte header with explicit little-endian field extraction (don't rely on `struct` padding/alignment assumptions — read raw bytes and manually assemble the uint32/uint64 with shifts, or use `memcpy` into a packed struct, since compiler padding between the 4-byte fields and the 8-byte timestamp can silently misalign fields). Compare the 4 magic bytes exactly (not null-terminated string compare) against the config value; skip mismatches. Shell out to `sha256sum <path>` via `popen()` and parse its first whitespace-delimited token as the checksum. Before each write to `manifest.csv`, `open()` it (append mode), call `flock(fd, LOCK_EX)`, write the line, then `flock(fd, LOCK_UN)` and close — do the locking around each individual write, not once for the whole program, since the requirement is per-write exclusivity.