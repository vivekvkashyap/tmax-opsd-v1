This spans four connected steps — fix the C bug first and verify it in isolation before wiring up Go/cgo. (1) In `/app/c_src/extractor.c`, find where a length field read from the file header is used to `memcpy` into a fixed 128-byte buffer without checking bounds; add a check that rejects (returns -1) whenever the declared payload length is negative, exceeds the buffer capacity, or exceeds the actual remaining input length — do this check before the memcpy, not after. (2) Compile it as a shared library into `/app/lib/libextractor.so` with correct visibility/calling convention for cgo (extern "C" if using C++, matching function signature). (3) Write `/app/filter.go` using `import "C"` cgo bindings to call the fixed function on the target file's bytes, exiting 0 on success (return 0) and exiting 1 on failure (return -1) — test this manually first against a hand-crafted oversized file to confirm it actually returns -1 rather than crashing. (4) Separately handle the `--extract-image` flag: shell out to `tesseract /app/sample.png stdout` via `os/exec`, capture stdout, and write it verbatim to `/app/recovered_text.txt`, then exit 0. Validate the corpus-filtering behavior against both clean and evil corpora directories before considering the fix complete.