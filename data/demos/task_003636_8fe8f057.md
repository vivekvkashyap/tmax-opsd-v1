First reproduce the bug: feed the naive Python script a list of large, close-together floats (e.g. values near 1,000,000 with tiny variation) and observe the variance go wrong/negative — this confirms catastrophic cancellation from computing `sum(x^2) - n*mean^2`. To deduce the legacy binary's exact algorithm, probe `/app/legacy_calc` with the same crafted inputs via stdin and compare its stable output to hand-computed expectations; a numerically stable running-variance approach for exactly this failure mode is Welford's online algorithm (track `n`, `mean`, `M2`, updating `delta = x - mean; mean += delta/n; M2 += delta*(x - mean)` per sample, then `variance = M2/(n-1)`). Rewrite `/home/user/calc_metrics.py` to use this streaming algorithm instead of the naive two-sum formula, keeping the same input/output format (`Mean: %.6f` / `Variance: %.6f`, defaulting to zeros for fewer than 2 values). Since grading requires bit-for-bit identical output to the C binary across thousands of fuzzed inputs, match the floating-point operation order precisely (same accumulation sequence, same rounding via Python's `.6f` formatting) rather than using library shortcuts like `statistics.variance` that may compute internally in a different order.
