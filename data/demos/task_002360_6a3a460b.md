Extract first, then process each `.log` file independently with an awk script that buffers one record at a time between `[START_RECORD]` and `[END_RECORD]` markers (e.g. accumulate lines into a buffer on seeing START, and on seeing END decide whether to print the whole buffer). Drop the buffered record entirely if any line matches `Server: DEPRECATED_SRV`; otherwise, if no buffered line starts with `ErrorCode: `, print the buffer's lines up to (not including) `[END_RECORD]`, then print `ErrorCode: 0000`, then `[END_RECORD]`. Write awk's output to a `.tmp` file and `mv` it over the original to satisfy the atomic-replace requirement — never redirect awk's stdout directly onto the file it's reading. Count total surviving `[START_RECORD]` occurrences across all processed files with `grep -c` (summed) for `summary.txt`. Finally re-tar the processed directory preserving the original relative paths (`tar -czf clean_logs.tar.gz -C /home/user/processing <files>`), and sanity-check by extracting to a scratch dir and grepping for the removed server and the inserted error codes.
