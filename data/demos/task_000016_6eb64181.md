Fix the ordering bug first, since everything else depends on it: change `start_services.sh` from launching both services in parallel to launching Service A, then polling/waiting (e.g. a loop that `sleep`s until `/home/user/config/passcode.txt` exists and is non-empty) before starting Service B — don't just add a fixed sleep, since that's fragile; wait on the actual file condition. For the ACL requirement, use `setfacl -m u:<user>:r /home/user/config` and `setfacl -d -m u:<user>:r /home/user/config` (default ACL so files created later inherit read access) rather than changing the base chmod bits, since the task explicitly forbids touching standard owner/group permissions. Run the worker to transcribe `/app/auth_audio.wav` (whisper-cli or similar) and let it write the passcode itself rather than hardcoding a guessed value. Confirm Service B is actually listening on 127.0.0.1:8080 and correctly reads the passcode file as the bearer token before considering the task done — test locally with curl using the transcribed passcode. Leave both services running in the background and populate `/home/user/migration.log` with real status lines from each service's own execution, not fabricated ones.
