Handle Step 1 and Step 2 independently, then combine. For keyframes, run `ffprobe -select_streams v -show_frames -show_entries frame=pict_type,best_effort_timestamp_time -of csv /app/evidence.mp4` (or `-skip_frame nokey`), filter for `pict_type=I`, extract the timestamps, write one per line (no header) to `/home/user/frames.csv`, and count the lines for `total_keyframes`. For the backup service, write a Python HTTP server (stdlib `http.server` or Flask) on `0.0.0.0:8333` that persists cross-request state (e.g. module-level dict or a small state file) tracking, per log file, the byte offset/line count already read and the inode of `active.jsonl` so you can detect rotation. On each `POST /backup`: stat `active.jsonl`'s inode; if it differs from what you last saw for the "current" file, that means the old file was renamed to `.1` — first drain any unread lines from `active.jsonl.1` using your previously stored offset for that inode, then start reading the new `active.jsonl` from offset 0. Write newly-read lines to a temp file, then build the archive by piping through `subprocess` (e.g. `tar -cf - <files> | gzip > backup_N.tar.gz`, or `subprocess.run` with a pipe), including both the temp log-lines file and `frames.csv`. Accumulate a running total of backed-up lines across calls for `GET /stats`. Create `/home/user/archive/` and any needed directories up front, and start the server so it keeps running (background it) for the verifier to hit.