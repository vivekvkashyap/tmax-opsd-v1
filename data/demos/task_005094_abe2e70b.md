Reverse-engineer this like a black-box signal-processing puzzle, using controlled probes rather than guessing. First isolate the per-base signal levels: call the oracle with single-character sequences and a single signal value near each expected level, or better, use sequences whose filter contributes a known combination, to solve for A/C/G/T theoretical levels algebraically (try simple sequences like a single-letter "AAAA"-style repeat with all-zero-ish raw signal to expose the base level directly, since with i=0 the filter reduces to filtered[0]=raw[0]). Then determine the causal filter coefficients by feeding sequences of length 2 and 3 with one raw sample nonzero at a time (unit impulses) and reading how the score changes — this reveals how filtered[1] and filtered[2]+ depend on current and past raw samples separately (expect different weighting for the first, second, and steady-state positions). Finally infer the distance metric (Euclidean/RMS vs sum-of-absolute-differences) by comparing outputs for known filtered-vs-expected differences of a controlled magnitude. Write and compile `open_scorer.c` implementing exactly what you deduced, printing with `%.4f\n`, then validate bit-exact equivalence yourself with many random fuzz inputs before finishing.