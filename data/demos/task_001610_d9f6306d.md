Do not guess the WAL binary format — run `/app/config_writer` once first, capture the `.gz` files it produces in `/home/user/wal_archive`, gunzip one, and hexdump it (`xxd`) to confirm the record layout (8-byte little-endian timestamp, 1-byte opcode, 4-byte little-endian key length, raw key bytes, optional 4-byte little-endian value for SET) against what's described, adjusting your parser if reality differs from any assumption. Build the Rust app in stages: a decompression+parsing module (using `flate2::read::GzDecoder`) that reads one WAL file into a Vec of typed records, applying SET/DELETE ops in file order (and in timestamp/append order within a file) into a shared `HashMap<String, i32>` (or a `Mutex`/`RwLock`-wrapped map if shared with the HTTP server); a directory watcher/poller that picks up newly rotated `wal_NNNN.gz` files as `config_writer` creates them and increments a processed-file counter; and an HTTP layer (axum or similar) exposing `GET /config/:key` (200+JSON if present, 404 if absent or deleted) and `GET /status` (files_processed count). Run `config_writer` in the background, let it finish rotating, then start your server and manually curl both endpoints to confirm behavior before considering it complete.