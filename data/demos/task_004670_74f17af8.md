Build the single-image detector first, calibrating against the given corpora. Load each image with PIL/cv2/numpy, convert to grayscale, and compute cheap linear-algebra signals: variance/std of pixel values, rank or singular-value spread of the matrix (via numpy.linalg.svd or matrix_rank), and edge/gradient energy. Solid-color or near-blank images collapse to near-zero variance and a highly skewed/low-rank singular spectrum; real plots have high variance and a spread-out set of singular values. Print debug stats on the clean vs evil corpora to pick thresholds that cleanly separate the two classes with margin, not just barely — the hidden set is drawn from the same distribution but isn't identical, so overfit thresholds to your 40 samples will fail. Wire this logic into `/home/user/detect_corruption.py` with correct exit codes (0 clean, 1 evil). For Phase 2, extract frames deterministically with a fixed ffmpeg command (e.g. `-vsync 0` and a fixed image2 naming pattern, no re-encoding surprises) so frame count/order is stable, run each frame through the same detector, collect 0-indexed corrupted frame numbers, and dump them as a JSON array to `/home/user/corrupted_frames.json`.
