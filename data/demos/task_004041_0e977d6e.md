Work the three phases independently and verify each before moving on. (1) Backup: `tar czf /home/user/history_backup.tar.gz -C /app/data -- history` (or equivalent) — confirm the tarball is non-empty and extracts back to the same tree. (2) Pipeline: inspect `nginx.conf` for the `location /ingest` block and add/fix a `proxy_pass` pointing at the Flask API's host:port so POSTs to `127.0.0.1:8080/ingest` reach it; edit the Flask `app.run(...)` call so `host` is `127.0.0.1` (not `0.0.0.0`) to satisfy the firewall requirement; then run `/app/services/start_all.sh` and check each process/port is actually listening (`ss -ltnp` or `curl`) before trusting it works — test end-to-end with a curl POST to `/ingest` and confirm the payload lands in the Redis queue (e.g. `redis-cli LPOP capacity_queue`). (3) Sanitizer: write `/home/user/sanitizer.py` to read stdin line-by-line, `json.loads` each line inside a try/except (skip on failure), then explicitly check key-set equality (exactly `host`, `cpu_percent`, `mem_bytes`), a regex for `host` (alnum/hyphen, length 3–64), numeric-type and range checks for `cpu_percent` (0–100 inclusive, accept int or float but reject bools/strings), and `mem_bytes` as a strictly-positive int. Test your sanitizer against both corpora under `/app/corpora/` before considering it done.
