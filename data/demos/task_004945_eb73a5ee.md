Start by diffing/reading `/app/vendored/polib-1.1.1/polib.py` around its encoding-detection/fallback logic to find where it returns a restrictive encoding (like ascii) instead of utf-8 as a default, fix that single value, then `pip install -e` the package so your pipeline imports the patched version. Build `run_pipeline.py` as an explicit DAG: represent each stage (read PO files, clean encodings, aggregate stats, save) as nodes with clear dependencies, executed in topological order — a simple ordered dict of stage-name -> function with explicit dependency list is sufficient; the point is to make the dependency structure explicit in code, not just call functions sequentially. For encoding repair, detect the mojibake pattern (UTF-8 bytes that were decoded as Latin-1) and reverse it programmatically per string: `text.encode('latin-1').decode('utf-8')`, wrapped in a try/except to skip strings that aren't actually corrupted this way. Compute per-language stats (count of non-empty msgstr, average length rounded to 2 decimals) grouped by filename stem, write to the exact summary path, and save corrected PO files preserving original filenames. Validate your fix on a few known corrupted strings by hand before trusting it across the whole corpus.