Treat this as two independent workstreams and verify each in isolation before wiring them together. For the infra: read `/app/nginx.conf` and add/fix the `proxy_pass` directive so requests to `/submit` are forwarded to the local `api_server` port; fix `/app/worker.env` so its Redis connection variable actually points at the running redis-server's host/port; then start redis-server, the Python api_server, the worker, and reload/start nginx, and confirm end-to-end with a manual `curl -X POST http://localhost:8080/submit` before moving on. For the filter: don't hand-tune thresholds — actually diff the `/app/corpus/clean/` and `/app/corpus/evil/` FASTA sequences (e.g. `grep -oE '[GC]+' file | awk '{print length}' | sort -n` on each corpus) to find the discriminating property (a run-length pattern in the base composition), then encode that as a fast regex-based check in `/home/user/filter.sh` using pure `grep`/`awk`, exiting 0 for clean and 1 for evil. Test the script against every file in both corpora directly before trusting the pipeline, since the hidden hold-out set exercises the same discriminator, not just what's visible.
