Write the script so each requirement maps to one verifiable checkpoint, since the grader tests them independently. Use `read -p "Enter deployment tier: " tier` (no trailing newline in the prompt, matches `-p`'s behavior) to capture input, working even when stdin is piped (`echo "staging" | bash script.sh`). Generate the cert non-interactively and silently: `openssl req -x509 -nodes -newkey rsa:2048 -keyout /home/user/certs/key.pem -out /home/user/certs/cert.pem -days 365 -subj "/CN=localhost"` after `mkdir -p /home/user/certs`. Build the nginx config as a heredoc written to `/home/user/proxy.conf` with an `events {}` block and an `http { server { ... } }` block containing `pid /home/user/nginx.pid;`, `error_log`/`access_log` paths, `listen 127.0.0.1:8443 ssl;`, `ssl_certificate`/`ssl_certificate_key` pointing at the generated files, and `location / { proxy_pass http://127.0.0.1:8080; }` — omit any `user` directive so it runs unprivileged. Validate with `nginx -t -c /home/user/proxy.conf` and check its exit code. Compute the fingerprint with `openssl x509 -noout -fingerprint -sha256 -in /home/user/certs/cert.pem`, strip everything up to and including `=` to keep just the colon-separated hex, and append `Tier: $tier, Cert SHA256: $fp` to `/home/user/deploy.log`. Finish with `chmod +x`.