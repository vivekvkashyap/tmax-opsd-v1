First fix the build: open `/app/vendored-sso-auth-1.2.0/Makefile` and find where `LD_FLAGS` is set with `=` (a hard assignment that clobbers/overrides any inherited environment value) — change it to a conditional assignment (`?=`) or point it at the correct system library path so the linker can find the crypto libs, then run `make` in that directory and confirm the `sso-decrypt` binary now builds without missing-symbol/library errors. Don't touch unrelated Makefile logic. For the classifier script, treat `/app/vendored-sso-auth-1.2.0/sso-decrypt` as a black box you invoke per file (e.g. `./sso-decrypt "$file"`) to get the decrypted URL on stdout, then classify it with pattern matching: safe = starts with a single `/` and NOT followed by another `/` (to exclude protocol-relative `//host` payloads), evil = absolute URLs with a scheme (`http://`, `https://`), protocol-relative (`//`), or `javascript:` URIs — use anchored regexes (`^/[^/]` for safe vs `^(https?:|//|javascript:)` for evil) rather than loose substring checks, since edge cases like `//attacker.com` can slip past a naive "starts with /" check. Iterate over the corpora and check exit/label correctness before finalizing, and `chmod +x` the script.
