Treat this as three independent recovery steps that feed a final service. First, extract the spoken passcode from /app/intercepted.wav: use a speech-to-text tool (e.g. `whisper`, or any offline ASR you can install/run) or listen via an audio-to-text pipeline, normalize what you hear to lowercase with spaces removed to form the token used for header matching — don't hardcode a guess, actually transcribe. Second, reverse-engineer /app/validator.elf with `strings`, `objdump`, or `file`/`nm` to recover the exact custom auth header name and cookie name it checks — string literals are usually visible directly in the binary. Third, build the honeypot: an HTTP handler on 127.0.0.1:8080 for GET /admin_check that reads the discovered header and cookie names from the request, compares the header value against the transcribed passcode exactly, checks only that the cookie is present (any value), and returns the specified JSON body with 200 or a 403 otherwise; plus a raw TCP listener on 127.0.0.1:8081 that replies PONG\n to an exact PING\n line and keeps or closes the connection cleanly. Launch both listeners as background processes so they stay up after your task ends.