Build this bottom-up: first get the uptime math and fstab parsing right in isolation, then wire it into a git hook, then handle the tunnel. For fstab parsing, treat each non-comment line as whitespace-separated fields (device, mountpoint, fstype, options, dump, pass) and match on the device column exactly equal to the target device string — don't assume field order or column widths, split on whitespace robustly. For uptime, count lines in the heartbeat log matching status "UP" versus total lines, compute the percentage with integer/floor division (not rounding) to match `(up_count * 100) / total_count` semantics. Write the C++ source as a real post-receive hook: it must run with no arguments (git invokes hooks that way), read the fixed heartbeat log path, read fstab.mock to resolve the mountpoint at hook-execution time (not hardcode it), and write the formatted output string to `<mountpoint>/latest.txt`. Compile with `g++ -std=c++17` directly to the hooks path and `chmod +x` it — then trigger it with an actual commit+push into the bare repo to confirm it fires and produces the right file, rather than trusting the code without exercising it. For the tunnel script, use the exact SSH local-forwarding flags requested and make the script executable.