Implement this as a precise, deterministic simulation — correctness depends on matching the spec exactly, not on general randomness. Build the adjacency list from graph.txt by reading each undirected edge and inserting both directions, then sort each node's neighbor list ascending. Implement the LCG exactly as given (same seed, same multiplier/increment/mask, same state update order) rather than substituting `rand()` or a different RNG, since the expected output depends bit-for-bit on this generator's sequence. Walk 10000 steps from node 0, at each step computing `lcg_rand() % degree(current)` to index into the sorted neighbor array and moving there, incrementing a visit counter for the destination node (not the start) each step. After the walk, compute empirical P(i) = visits[i]/10000 and theoretical Q(i) = degree(i)/(2*num_edges), then sum P(i)*ln(P(i)/Q(i)) over nodes with P(i)>0 for the KL divergence. Print all five values with exactly 6 decimal places (printf `%.6f`) in the exact `P(i)=...` / `KL=...` line format. Write the Makefile with `default` (compile with `-lm`) and `run` (execute and redirect stdout to results.txt) targets, then run `make && make run` and diff the output format against the spec before finishing.