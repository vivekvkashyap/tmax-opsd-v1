Work through this in strict dependency order. First fix the vendored package: open its Makefile and look carefully at the install target for a typo in the pip subcommand, then `pip install -e .` (or equivalent) from that directory to confirm it installs cleanly. Next open `core.py`'s merge function — a plain `pd.merge` on an outer/left join introduces NaN for unmatched rows, and pandas silently upcasts any int column containing NaN to float64, which loses precision for integers beyond 2^53; cast the id/hash column to the pandas nullable `"Int64"` dtype on both frames before merging so NaN can coexist with true integers. Verify precision survives by comparing the merged value against the original string/int representation, not just printing it (floats will look "right" until you check exact equality). For the pipeline script, use `scipy.stats.ttest_ind(..., equal_var=False)` for Welch's t-test and get the CI via the t-distribution's interval method or by manually combining the pooled standard error with the appropriate degrees of freedom; compute the first principal component of the centered feature matrix with `numpy.linalg.svd` (first right-singular vector) rather than hand-rolled eigen-decomposition. Finally wrap results in a FastAPI/Flask app, add authentication middleware that checks the exact Bearer header before any endpoint logic runs, round outputs to 4 decimals, and launch it as a background/daemon process bound to 127.0.0.1:8080 so it stays reachable after your shell returns.