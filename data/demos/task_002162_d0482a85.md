Build and test each layer independently before wiring them together. Write `heartbeat.c` to read `HUB_PORT` with `getenv`, attempt a TCP `connect()` to 127.0.0.1 on that port, and on failure print exactly `CONNECTION_FAILED\n` and `exit(1)` — on success, loop exactly 5 times printing `HEARTBEAT_SENT\n`, `send()`-ing `PING\n`, and `sleep(1)` each iteration, then close the socket and `exit(2)`; verify this manually with `nc -l -p <port>` listening vs. not listening before moving on. Add the `export HUB_PORT=8888` line to `.bashrc` exactly. For `supervisor.sh`, `mkdir -p` the logs dir, `source ~/.bashrc` (or the equivalent) to get `HUB_PORT` into scope, then an infinite loop that runs heartbeat with stdout appended (`>>`) to `edge.log`, and after each exit — regardless of the exit code, so check unconditionally, not just on failure — stat the file size and if it's strictly greater than 100 bytes, `mv` it to `edge.log.old` (overwriting), then `sleep 1` before the next iteration. `health_check.sh` should `grep -q HEARTBEAT_SENT` across both log files (guarding for missing files) and print/exit accordingly. `chmod +x` both scripts but do not start or background the supervisor yourself.