First do the bulk rename: for every `*.zlog` file in `/home/user/logs/`, strip the extension and append `.legacy.z` (e.g. via a loop with `mv "$f" "${f%.zlog}.legacy.z"`), and confirm no `.zlog` files remain afterward. For the server, write a C TCP daemon that `socket()/bind()/listen()` on port 9090, `accept()` in a loop (fork or thread per connection so it can serve repeatedly), reads a line from the client up to `\n` to get the file path, then spawns `/app/bin/zdecoder <path>` via `popen`/`pipe`+`fork`+`exec` and reads its stdout as a stream — treat zdecoder purely as an opaque decompressor producing raw UTF-16LE bytes, you don't need to know its internal encoding scheme. Convert the stream from UTF-16LE to UTF-8 incrementally in fixed-size chunks (e.g. with `iconv()` or manual 2-byte-codeunit decoding) rather than buffering the whole file, to keep memory bounded on large logs. Write the converted bytes to the client socket, then close the connection cleanly. Launch the compiled server with `&`/`nohup` so it keeps running in the background for testing.
