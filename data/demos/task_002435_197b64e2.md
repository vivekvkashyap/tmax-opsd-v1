Build the pipeline as a sequence of discrete, verifiable stages inside `/home/user/clean_dataset.sh`, using absolute paths throughout so it works regardless of invocation directory. 1) `tar -xzf /home/user/raw_data.tar.gz -C /home/user/extracted` (create the dir first). 2) Delete symlinks specifically with `find /home/user/extracted -type l -delete` — do NOT try to traverse them first, since the archive contains a mutual symlink loop (dirA<->dirB) that will hang any recursive copy/du/find that follows links; find with `-type l` matches the link itself without following it, so this is safe. 3) Gather `.dat` files with `find ... -name '*.dat' -type f | sort` (sort guarantees deterministic alphabetical-by-path order) then `cat` them in that order into `merged.dat`. 4) Use `grep -v -E '^(ERROR|DEBUG)'` combined with `sed -E 's#([0-9]{4})/([0-9]{2})/([0-9]{2})#\1-\2-\3#g'` to strip and transform lines into `cleaned.dat`. 5) `split -l 50 -d... ` won't give `aa/ab` suffixes — use `split -l 50 cleaned.dat /home/user/chunks/chunk_` with default alphabetic suffixes, then rename/add `.txt` if needed, or use `split -l 50 -a 2 cleaned.dat chunk_` and append `.txt`. 6) `tar -czf /home/user/processed_dataset.tar.gz -C /home/user chunks`. Make the script executable and actually run it to confirm no hang and correct output before finishing.