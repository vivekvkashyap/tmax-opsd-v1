Pipeline this as a shell chain, not a single monolithic script. (1) Chunk: `mkdir -p /home/user/chunks && split -l 15 -d /home/user/archive.txt /home/user/chunks/piece_` (check the exact flags your `split` supports for numeric suffixes). (2) Filter: use `grep -l '\[VALID_BLOCK\]' /home/user/chunks/piece_*` to list only chunk files containing that exact marker string — files without it must be excluded entirely, even if they contain what looks like valid `>>> PATH:` blocks, because unverified chunks are untrusted. (3) Merge in filename order: since `grep -l` output order can vary, explicitly `sort` the matched filenames before concatenating (`sort | xargs cat`) to guarantee alphabetical order. (4) Write `/home/user/extractor.py` reading stdin line by line, tracking a "currently inside a file" state toggled by the `>>> PATH: ... <<<` and `>>> END <<<` markers, extracting the path from the marker line, joining it under `/home/user/recovered_project/` with `os.path.join`, creating parent dirs via `os.makedirs(..., exist_ok=True)`, and writing content lines only while inside a block. Discard all lines outside markers, including stray `[VALID_BLOCK]` text.
