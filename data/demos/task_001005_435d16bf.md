Diagnose the index corruption first: query `users` filtering on `status` and compare row counts/results against a query that avoids the index (e.g. force a table scan or add a redundant condition on another column) — a mismatch confirms the `idx_status` B-tree is stale/corrupt. The safe, minimal repair in SQLite is `REINDEX idx_status;` (or `DROP INDEX idx_status; CREATE INDEX idx_status ON users(status);`) — do not alter the `users` table schema itself. For the C tool, use `sqlite3_prepare_v2` with `?` placeholders for all four inputs (status, min_priority, limit, offset) and bind them with `sqlite3_bind_text`/`sqlite3_bind_int` — never concatenate user input into the SQL string, since injection-safety is part of the spec. Write the `ORDER BY created_at DESC, id ASC` clause literally into the prepared statement (not as a bound parameter, SQL doesn't allow binding column/order clauses), and bind `LIMIT`/`OFFSET` as `?` params. Print each result row as `id,name,status,priority,created_at` with no header line. Compile with `-lsqlite3`, run with the specified arguments (`ACTIVE 50 500 0`), and redirect stdout to `/home/user/results.csv`. Verify correctness by comparing your tool's output against a plain `sqlite3` CLI query with the same predicate before finalizing.
