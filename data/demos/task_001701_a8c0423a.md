Structure this as three separate stages in your Go program: (1) build an in-memory graph from `datacenters` and `network_links`, then run Dijkstra's algorithm (or since latencies are all positive, any standard shortest-path routine) from the node named `DC-Alpha` to the node named `DC-Omega`, keeping track of the actual sequence of edges/hops taken, not just the total distance; (2) for each consecutive hop `(source_id, dest_id)` on that path, run a SQL query against `transfer_logs` that filters `status = 'SUCCESS'` and that source/dest pair, orders by `timestamp DESC`, and limits to 3 rows — compute the average of `bytes_transferred / duration_ms` (do this arithmetic as floating point, not integer division) over whatever rows come back, which may be fewer than 3; (3) look up datacenter names for the ids and marshal the ordered hops into JSON with keys `source`, `dest`, `avg_speed_bytes_per_ms` (rounded to 2 decimals), writing to `/home/user/routing_plan.json`. Use `github.com/mattn/go-sqlite3` with `go mod init` + `go mod tidy` in `/home/user/` to fetch it, and open the DB read-only or simply avoid any write statements so the source database is untouched. Test the query logic against a couple of hops manually with `sqlite3` CLI before trusting the Go output.