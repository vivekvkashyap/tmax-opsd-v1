Work service-by-service, verifying each in isolation with curl before wiring them together. First implement `split_mesh(N, k)`: compute `base = N // k`, `remainder = N % k`, then walk k subdomains where the first `remainder` subdomains get `base+1` elements and the rest get `base`, tracking a running `start_idx` so ranges are contiguous and non-overlapping (e.g. for N=105, k=4 you should get sizes 27,26,26,26 in order). Start `data_server.py`, both `worker.py` instances (pass each a distinct port via env var or CLI arg), and `coordinator.py` from `start_services.sh`, redirecting each process's stdout/stderr to its own file under `/home/user/service_logs/` and backgrounding them with `&`; confirm each port is listening (e.g. `curl` or `ss -tlnp`) before declaring the script done. In the coordinator's `/run_sim` handler, call `split_mesh`, POST each `(start,end)` range to the corresponding worker's `/compute`, and concatenate the returned arrays in worker order to form the combined result. For the regression script, POST to the coordinator, GET the reference JSON, compare arrays elementwise using max absolute difference against a 1e-5 threshold, and print exactly `REGRESSION_PASSED` or `REGRESSION_FAILED`.