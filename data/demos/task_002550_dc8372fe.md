Build this as two small, testable pieces. For migrate.py: read the JSON list and the secret text (strip trailing newline/whitespace from the secret file — a stray `\n` will silently change every hash), then for each record compute `hmac.new(secret.encode(), legacy_key.encode(), hashlib.sha256).hexdigest()` (hexdigest is already lowercase hex, so don't re-lowercase incorrectly or use `.digest()` by mistake). Create the SQLite table with `CREATE TABLE api_credentials (user_id INTEGER PRIMARY KEY, hmac_hash TEXT, migrated_at TEXT)`, insert one row per record with the hardcoded timestamp string, and commit/close the connection. For test_api_auth.py: reconnect to the same DB path, recompute the HMAC for "alpha123" independently inside the test (reading the same secret file the same way), query user_id=1, and assertEqual against the freshly computed hash — this way the test validates the pipeline's logic rather than hardcoding an expected hash. Run migrate.py first, then run the test file directly and confirm exit code 0 before finishing.
