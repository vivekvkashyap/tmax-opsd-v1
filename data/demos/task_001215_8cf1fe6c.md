First recover the actual policy: run `tesseract /app/curation_rules.png stdout` (or similar OCR) and read the extracted text carefully rather than guessing rules — it will specify prefix rename pairs, an archive-file suffix, and how many header lines to strip from manifests; do not assume any specific prefixes or suffix, derive them from the OCR output. Extract `repo.tar` into `/app/repo/`, then implement the pipeline defensively for special characters: use `find ... -print0 | while IFS= read -r -d '' f` (or bash globbing with `nullglob`) rather than naive `for f in $(find ...)` word-splitting, which breaks on spaces. Apply prefix renames via a loop that computes the new basename and `mv`s, preserving directory structure. For `.manifest` files, use `sed -i` to apply the same text substitutions and `sed -i '1,5d'` (or `tail -n +6`) to strip the header, processing them with streaming tools rather than loading whole files into memory. Move files matching the archive suffix into `/app/archived/`, replicating their relative path under that dir (e.g. via `--parents` with cp/mv or manual `mkdir -p $(dirname ...)`), then `tar cf archived.tar` from inside `/app/archived`. Finally `tar cf - -C /app repo | xz -9 > /app/curated_repo.tar.xz` (or `tar cJf`) for the remaining curated tree, and spot-check a few renamed files and manifests by hand against the OCR'd rules before finishing.