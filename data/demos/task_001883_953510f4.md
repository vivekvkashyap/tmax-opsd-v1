Two independent bugs to fix — do not conflate them:
1. In `/home/user/graph_solver.c`, `solve_graph()` returns a pointer to a stack-local array (`int sorted_nodes[26]`), which is undefined behavior once the function returns. Fix by allocating it with `malloc` (and having the caller free it) or declaring it `static`, without touching the Kahn's-algorithm logic itself (queue/in-degree processing stays the same).
2. In `/home/user/pipeline.py`, port from Python 2 to Python 3: `subprocess.Popen(...).communicate(input=...)` needs bytes input in Py3, so encode the string before passing it in; the captured `stdout` (`out`) comes back as bytes, so decode it before calling `.split('\n')`; and replace the bare `print line` / `print "..."` statements with `print(...)` function calls.
3. After fixing both, run `/home/user/build.sh` to recompile the C binary, then run `python3 /home/user/pipeline.py /home/user/graph.txt`, redirecting stdout to `/home/user/execution_order.txt`. Verify the result is a valid topological order (a node only appears after all its dependencies) rather than assuming any single fixed ordering, since Kahn's algorithm can produce multiple valid orders when there are ties.
