Work through this in the stated order since later steps depend on earlier ones being exactly right. First fix the Forward-Euler heat-equation solver: update the mesh point count and time step to the specified values, and note that CFL stability for this scheme requires `alpha*dt/dx^2` stay below a bound — verify your dx (recomputed from the new Nx) and dt combination doesn't blow up, and confirm no NaNs appear before trusting the saved profile. Second, load the observational CSV and sort its rows by the `raw_x` column ascending (do not assume file row order) so `raw_temp` values line up index-for-index with your simulation's `linspace(0,1,Nx)` grid points — this alignment is what makes the MSE comparison meaningful. Third, fit a degree-2 polynomial to your simulated profile using something like `np.polyfit(x, u_sim, 2)`, which returns coefficients highest-power-first — keep that order for the output. Fourth, compute MSE as the mean of squared differences between your simulated profile and the sorted observational temperatures (same-length, index-aligned arrays), then write both results into the JSON with exactly the required keys and as native floats/lists (not numpy types, which aren't JSON-serializable).
