Start by OCR'ing the policy image rather than guessing rules: run `tesseract /app/artifact_policy.png stdout` (or similar) and read the extracted text carefully to get the exact numeric thresholds and required fields — don't hardcode assumed rule values without confirming them from the OCR output, and manually re-read the raw text for OCR misreads (e.g. digit confusion) before encoding thresholds in code. Implement `/home/user/validator.cpp` to: take a `.json.gz` path, decompress with zlib (`gzopen`/`gzread` or `zlib.h` inflate, compiled with `-lz`), parse the JSON (write a small hand-rolled parser or vendor a header-only lib like nlohmann/json), then check each rule extracted from the image — presence of required fields, numeric threshold comparisons (careful with `<` vs `<=` boundaries matching the OCR'd wording precisely), and the exact size of the `chunks` array and exact string match on `compression_format`. Return exit 0 only if every rule passes, exit 1 if any fails or a required field is missing (guard against missing-field crashes, e.g. check key existence before accessing). Compile to `/home/user/validator`, then test manually against a few crafted clean and evil gzipped JSON manifests before trusting the automated corpus grading.