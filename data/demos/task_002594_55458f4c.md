Build this as a sequential, deterministic pipeline and match every specified parameter exactly, since the grader compares against a fixed reference computation with only small numeric tolerance. Load and join the 200 abstract files with `metadata.csv` on `file_id`, making sure the join preserves a consistent, deterministic row order (e.g. sort by `file_id`) since embedding indices must map back to the right rows. Fit `TfidfVectorizer(lowercase=True, stop_words='english', max_features=500)` on all abstracts, then `TruncatedSVD(n_components=10, random_state=42)` on the TF-IDF matrix — use exactly these parameter values and this exact library (scikit-learn) since different implementations/defaults will shift the embedding and downstream numbers. Compute cosine similarity of `math_001`'s embedding against all others, exclude `math_001` itself, and take the top 20 by similarity (careful with tie-breaking and off-by-one in your top-k slicing). Run Welch's t-test (`scipy.stats.ttest_ind(..., equal_var=False)`) comparing the top-20 group's citations to the remaining 179, and compute the 95% CI for the mean difference using the Welch-Satterthwaite degrees of freedom formula (don't substitute a simple pooled-variance CI). Round every value to 4 decimals and write the JSON with exactly the specified keys.
