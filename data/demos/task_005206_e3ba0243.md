Build the pieces bottom-up and test each in isolation before chaining them. Create the directory/payload file first (exact JSON content, no extra whitespace beyond what's specified). Write `manager.py` so `start` checks for `cert.pem`/`key.pem` and only generates them if missing, using `subprocess.run(["openssl", "req", "-x509", "-newkey", "rsa:2048", "-nodes", "-keyout", ..., "-out", ..., "-days", "365", "-subj", "/CN=localhost"])` — get the `-subj` syntax right or openssl will hang waiting for interactive prompts. Launch the HTTPS server as a genuine background OS process (e.g. `subprocess.Popen` with stdout/stderr redirected to the log file, then write `.pid` from `proc.pid`) rather than something that blocks manager.py itself, since the script must return control so the lifecycle steps (curl, then stop) can run afterward. For `stop`, read the PID, send a clean terminate signal, wait briefly, then remove the pid file — and verify port 9443 is actually released afterward (`ss -tuln`), not just that the process object was killed. Use `curl -sk` to bypass the self-signed cert warning when fetching the payload.
