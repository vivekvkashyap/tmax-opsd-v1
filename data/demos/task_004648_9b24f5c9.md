Start by diagnosing the vendored package rather than rewriting it: `cd /app/secscanner-1.0.4`, inspect `setup.py` for a bogus/nonexistent pinned dependency and remove or correct it so `pip install -e .` succeeds, then open `secscanner/redactor.py` and look for the Base64 call — if it uses the wrong base-N decode function (e.g. base32 instead of base64), fix it to the correct `base64.b64decode` call and confirm with a quick manual `python3 -c` test that decoding a known base64 string now works. Once the library imports cleanly, build `/home/user/policy_service.py` around it: run an HTTP server (e.g. `http.server`/Flask) on 127.0.0.1:8080 with a `/scan` POST handler, and a gRPC server on 127.0.0.1:50051 generated from `/home/user/scanner.proto` via `python -m grpc_tools.protoc` — implement both handlers so they share one core function that: (1) `os.stat(filepath).st_mode` to check world-readable permission bits and reject with 403/PERMISSION_DENIED if set; (2) otherwise decode the payload with the fixed library, redact the `AKIA[0-9A-Z]{16}` pattern via `re.sub`, write the redacted text to `filepath`, and return the success message. Test both endpoints against both a `chmod 600` and `chmod 644` file before leaving the service running in the background.
