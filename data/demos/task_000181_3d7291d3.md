Break this into signal-processing, orchestration, and serving layers and get each working independently before combining. For the C++ detector, first get raw PCM samples out of /app/data/raw_audio.wav (either parse the WAV header yourself — it's a standard RIFF/PCM format — or shell out to `ffmpeg` to dump raw float/int PCM), then implement the described two-level mesh: slide a coarse 100ms window across the signal, estimate energy concentrated near 4000 Hz per window (e.g. via a Goertzel filter or simple bandpass energy test rather than a full FFT), and for windows exceeding your threshold, refine the event boundary by re-scanning at 10ms resolution to tighten start/end times; emit events.json with times rounded to two decimals. Write pipeline.py to compile the C++ with g++, produce/convert the PCM input, and invoke the compiled binary to regenerate events.json — treat this as a reproducible, rerunnable step, not a one-off. Finally add an HTTP server bound to 127.0.0.1:8080 (Python's http.server or similar) that serves GET /api/events by reading and returning events.json's exact contents with the correct Content-Type header, and leave it running as a background process so the verifier can query it after your task ends.