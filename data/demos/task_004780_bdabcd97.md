Build this as one Python script with clearly separated steps, and get each step's output byte-correct in isolation before combining. Open `/home/user/artifacts/raw_data.bin` in binary mode, call `fcntl.flock(f.fileno(), fcntl.LOCK_EX)` before reading the full contents with `f.read()`, then explicitly `fcntl.flock(f.fileno(), fcntl.LOCK_UN)` (or close the file) to release it. For the RLE encoder, iterate the byte string and group runs of identical consecutive bytes; for each run, emit `(count, value)` pairs where count is capped at 255 per pair — for a run longer than 255, split it into as many full 255-count chunks as needed followed by a remainder chunk, and even single-byte runs must be encoded as `count=1` (never skip the count byte). Write the encoded bytes to `/home/user/artifacts/compressed.bin` in binary mode. For metadata, open `/home/user/artifacts/metadata.txt` in binary mode and `.decode('utf-16le')` explicitly since the default text mode won't handle this encoding correctly. Compute SHA256 over the exact bytes of `compressed.bin` after it's fully written (read it back from disk rather than hashing an in-memory buffer, to catch any write bugs), and build the manifest dict with `metadata` and `checksum` keys exactly as specified, writing valid JSON to `final_manifest.json`.
