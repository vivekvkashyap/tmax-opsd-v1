Work the three stages in order. First, treat the libyaml build failure as a config-perturbation hunt: run `./configure --prefix=/home/user/local` and `make` inside `/app/libyaml-0.2.5`, read the exact compiler error, then grep `configure` for the offending `CFLAGS` assignment (an injected `-Werror=...` flag turning a harmless warning into a hard failure) and strip just that flag rather than rewriting the whole build system; then `make && make install` so `lib/` and `include/` land under `/home/user/local`. Second, for the C validator, compile/link against your fresh libyaml (`-I/home/user/local/include -L/home/user/local/lib -lyaml`, and set `LD_LIBRARY_PATH` or use rpath so the runtime linker finds it), drive the libyaml event/token API (not naive string scanning) to detect a scalar key `hostNetwork` immediately followed by a scalar value `true` at any nesting depth, and print exactly `ACCEPT`/`REJECT` with matching exit codes. Third, generate the self-signed cert+key concatenated into one PEM, write a user systemd unit that runs `socat` on port 8443 with TLS (verify=0) forking your validator per connection wired to stdin/stdout, then `systemctl --user daemon-reload`, `enable --now`. Test end-to-end by piping known-good and known-bad manifests through `openssl s_client` or `socat` against localhost:8443.