Build and unit-test `loc_filter.py` in isolation first, independent of the Redis/Node plumbing, since it's the piece that gets fuzzed bit-exactly. Implement it as a true streaming filter: read `sys.stdin` line by line in a for-loop (never `.read()` the whole stream), `json.loads` each line, and `print(json.dumps(...), flush=True)` immediately per output line so nothing buffers. Track two pieces of state across iterations: the previous actual event's timestamp/lang, and a fixed-size sliding window (e.g. `collections.deque(maxlen=5)`) of prior actual-event ratios — only append to the window *after* evaluating the current event's anomaly flag against the window's current contents, never before. For gaps, walk `t = round(prev_ts + 1.0, 1)` upward while `t < current_ts`, emitting one synthetic event per step with the previous event's `lang`. Match the field order/values in the given example exactly. Only after the filter passes its own manual tests should you write `start_pipeline.sh`: start `redis-server`, `python3 /app/emitter.py`, and `node /app/dashboard.js` in the background, then a loop that `BLPOP`s `loc_raw_stream`, pipes the value through `loc_filter.py`, and `RPUSH`es each output line to `loc_processed_stream`.
