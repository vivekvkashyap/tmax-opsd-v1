Work through this as a chain of dependent fixes, testing after each. First build the IPC layer bottom-up: create `/home/user/app/ipc/v2/` with `mkfifo` for `backend.fifo` and `db.fifo`, chmod the fifos 0600 and the v2 dir 0700, then create `/home/user/app/ipc/v1/` and symlink each v1 name to its v2 counterpart with `ln -s` (use absolute target paths so the symlink resolves regardless of cwd). Next automate the interactive DB init: write an `expect` script that spawns `python3 /home/user/app/db_init_cli.py`, uses `expect "Enter DB Admin PIN:"` then `send` the PIN + newline, then `expect "Confirm PIN:"` and sends it again, followed by `interact` or `expect eof`. For the sanitizer, write a Bash function that lowercases the file content (or uses grep -i) and checks for each forbidden substring with `grep -qi` — reject on any match, otherwise accept; test it manually against a few crafted strings before trusting it on the corpora. Only after each piece works standalone should you run `start_services.sh` to confirm gateway->backend->db connectivity end-to-end, then write the two required absolute paths to `completion.log`.