Start by extracting the penalty value from the image with `tesseract /app/policy.png -` (or similar OCR call), reading the printed text carefully to get the exact integer rather than assuming a value — this constant must be added once per intermediate hub (not per edge, and not to the endpoints) when computing path cost. Load the three CSVs into in-memory graph structures (adjacency list keyed by hub_id, plus a cargo lookup keyed by the src,dst edge pair) inside a fresh `cargo new --bin route_engine` project. Implement shortest path with Dijkstra (or BFS+relaxation) over edge weights equal to `base_distance`, but when relaxing through a node that is not the source, add the penalty to the cost of arriving via that node — get the off-by-one right by testing a 3-hop path by hand first. For each query, reconstruct the path node sequence, then sum tonnage for cargo rows whose (src,dst) matches each consecutive edge pair exactly (not path-wide aggregation). Match the required output string format exactly, including the `NONE`/`0`/`0.0` case for unreachable pairs, and round tonnage to 1 decimal with proper rounding (not truncation). Because a fuzz-equivalence verifier will hammer this with random hub pairs, build with `cargo build --release` and confirm the binary reads stdin line-by-line without buffering/blocking issues, keeping output flushed per line.
