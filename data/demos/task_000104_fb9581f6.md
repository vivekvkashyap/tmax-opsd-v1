Extract the schema rules from `/app/pathway_schema.png` yourself using OCR (e.g. `tesseract /app/pathway_schema.png -` or a Python OCR library) rather than assuming a plausible-looking transition set — read the actual recognized text to get the exact list of valid `SourceType -> TargetType` transitions and the exact maximum path length integer; if OCR output is noisy, inspect the image and cross-check against the literal decoded string. Then write `/home/user/graph_filter.cpp` to parse each line as `<SrcType> <SrcID> <DstType> <DstID>`, build a directed graph (skip/tolerate malformed lines rather than crashing), and check two things: (1) every edge's `(SrcType, DstType)` pair must appear in your extracted valid-transitions set — reject immediately if not; (2) run a BFS/shortest-path search (by hop count, not edge weight) from every node of type GENE to any node of type PATHWAY, and accept only if some such path exists with length (number of edges) not exceeding the extracted maximum. Print exactly `ACCEPT`/`REJECT` with matching exit codes 0/1. Compile with the exact given `g++ -O3 -std=c++17` command, then test against both `/app/data/clean/` and `/app/data/evil/` corpora — a file is only correct if every clean file is accepted and every evil file is rejected.
