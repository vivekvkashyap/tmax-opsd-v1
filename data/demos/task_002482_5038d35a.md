Read `/home/user/project/processor.cpp`, `Makefile`, and `server.py` first to locate the actual bugs rather than rewriting from scratch. The C++ fix: wrap the exported function in `extern "C"` so it has C linkage callable via ctypes, change its signature to accept and return `const char*` (build the result into a `std::string` internally, then return `strdup(result.c_str())` so the pointer survives after the function returns — a raw pointer to a local std::string's buffer would dangle). Ensure the sort logic actually parses/sorts by the JSON `id` field rather than being hardcoded. Rebuild with `make`. In `server.py`, add `lib.sort_items.restype = ctypes.c_char_p` before it's called so ctypes correctly marshals the returned pointer into a Python bytes/str instead of misinterpreting it as an int. Start the server (`python3 server.py &`), then set up `socat TCP-LISTEN:8000,fork,reuseaddr TCP:127.0.0.1:8080 &` as the reverse proxy. Write `test_endpoint.sh` to `curl -s -X POST -d '<payload>' http://127.0.0.1:8000` and redirect output to `/home/user/project/final_output.json`, then run it and verify the file's exact byte content matches the sorted JSON.