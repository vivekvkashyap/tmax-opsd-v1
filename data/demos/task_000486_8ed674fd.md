Do the pieces in dependency order and confirm each works standalone before chaining. Parse `config.fstab` yourself (e.g. `awk '$1=="accounts_mount"{print $2}' /home/user/config.fstab`) to get the accounts directory, then `chmod 700` exactly that directory. Generate the TLS material with `openssl req -x509 -newkey rsa:2048 -nodes -keyout key.pem -out cert.pem -days 365 -subj "/CN=localhost"` (the `-nodes` flag is what keeps the key unencrypted, easy to forget). For server.py, use `http.server.HTTPServer` wrapped with `ssl.SSLContext`/`wrap_socket` (or `ssl_context.load_cert_chain`) bound to 127.0.0.1:8443, with `os.chdir()` (or a custom handler's `directory=` kwarg) set to the directory read dynamically from the fstab file at startup — don't hardcode the path, re-parse the fstab so the server logic matches the task's "dynamically read" requirement. For supervisor.py, use `subprocess.Popen` in a loop: `while True: proc = Popen([...]); proc.wait(); append "[RESTART]" to the log; relaunch` — the log write and relaunch must happen every time the child exits, including on the very first crash induced by `kill -9`. Launch the supervisor with `nohup ... &` and confirm with `curl -k` and a manual `pgrep -f server.py; kill -9 <pid>` test that it actually respawns before finishing.