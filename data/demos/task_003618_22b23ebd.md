Model this as a graph problem, not a linear scan: load `/home/user/backups_metadata.json`, build a directed graph where each backup node has an edge from its `parent_id` (or is a root if `type == "full"` and `parent_id` is null), and only include nodes with `status == "ok"` — critically, if a node is corrupted, every descendant reachable only through it must be excluded too, since restoring on top of a corrupted backup is invalid (a simple filter-then-keep-parent-links approach without checking ancestor validity will wrongly include unreachable chains). For each valid root (`full` backup), enumerate all root-to-node paths (or do a BFS/DFS tracking path length and max timestamp) to find, among nodes with `timestamp <= 1600003500`, the one with the highest timestamp; among chains reaching that exact best timestamp, pick the one with the fewest nodes (shortest path — plain BFS from each full root naturally finds shortest paths by depth). You may use `networkx` (build a DiGraph, then `nx.shortest_path` or manual BFS) or just write the traversal by hand. Output the chosen chain's `file` values in restoration order (root first) as a JSON array to `/home/user/restore_plan.json`, then run the script to confirm the file is produced.
