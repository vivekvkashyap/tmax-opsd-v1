Fix `/app/test-util/setup.py` first — find the actual syntax error (e.g. a missing quote/comma in a list literal) and correct only that, then confirm `pip install .` succeeds. Run `tesseract /app/server_config.png stdout` to recover the listening port and auth token, visually cross-checking the OCR text against the image since a misread character breaks everything downstream. Implement `/app/test-util/server.sh` as a loop using `nc -l -p <PORT> -q1` (or `socat TCP-LISTEN:<PORT>,reuseaddr,fork -`) that accepts one connection at a time, reads the raw HTTP request, checks for the exact header line `Authorization: Bearer <TOKEN>`, and replies with `200 OK` + body or `401 Unauthorized` as appropriate. Compute the body as the SHA256 hex digest of the token, e.g. `printf '%s' "$TOKEN" | sha256sum | awk '{print $1}'` — use `printf`, not `echo`, to avoid a trailing newline changing the hash. Launch the loop with `nohup bash server.sh &` (or similar) so it keeps listening in the background, and manually curl it to confirm both the 200 and 401 paths before finishing.
