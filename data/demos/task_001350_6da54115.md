Treat this as three independent sub-problems that converge on one script. First, run `tesseract /app/equation.png stdout` (try preprocessing with `convert`/ImageMagick if OCR output is noisy) to recover the exact polynomial text — read it carefully, don't guess coefficients. Second, fix `/home/user/math_service/`'s build: read `setup.py` for syntax errors and check for missing system packages (e.g. `python3-dev`, a C compiler) needed to build the extension, fixing one issue at a time and re-running the build until it succeeds. Third, inspect `core.dump` with `gdb -c core.dump <binary>` (or `gdb python3 core.dump` if it's a Python-extension crash) and use `bt`/`info registers` to understand where the buffer overflow occurs (look for array writes indexed by input value with no bounds check) — this tells you why the C-extension is unsafe, motivating a from-scratch pure-Python reimplementation rather than reusing the buggy C code. Finally write `/home/user/evaluate.py` that reads `x` from `sys.argv`, evaluates the polynomial you OCR'd using plain Python arithmetic (which has no fixed-width overflow, unlike the C extension), and prints the integer/float result. Sanity-check it against a few hand-computed values before considering it done.