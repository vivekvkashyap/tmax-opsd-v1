Follow the pipeline exactly as specified since the whole task is deterministic and reproducible — any deviation in order of operations will make your results.json diverge from the graders. Compile with `gcc /home/user/sim/sim.c -o /home/user/sim/sim -lm` (the `-lm` is required for the math functions). Run `./sim 42 1000 > /home/user/coarse.txt` and `./sim 42 2000 > /home/user/fine.txt` exactly as specified — same seed, different N, redirecting stdout. In your analysis script, load both files with numpy, compute `scipy.stats.wasserstein_distance(coarse, fine)` first, then call `np.random.seed(42)` exactly once immediately before starting the 1000-iteration bootstrap loop (not once per iteration, and not before the initial distance calculation) — each iteration must draw `np.random.choice(coarse, size=1000, replace=True)` and `np.random.choice(fine, size=2000, replace=True)` in that order (coarse-then-fine draw order matters for reproducing the exact RNG stream) and compute the Wasserstein distance between the two bootstrap samples. Take `np.percentile` of the collected distances at 2.5 and 97.5 for the CI bounds. Round all three outputs to exactly 4 decimal places with Python's `round()` and dump as JSON with keys `distance`, `ci_lower`, `ci_upper`.