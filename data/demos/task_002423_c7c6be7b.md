Two-part task. First, fix the vendored package before touching your own code: inspect `/app/py_kmer_counter-1.0/setup.py` for the install-blocking bug — a typo'd import is the classic cause of a "no module named X" failure at build time; correct it, then `pip install .` from that directory and verify with a quick `python3 -c "import py_kmer_counter; ..."` sanity check that `count_3mers` returns a dict, rather than assuming the fix worked. Second, write `/home/user/solve.py` to read a DNA sequence from stdin, call `py_kmer_counter.count_3mers`, then build the 64 counts into an 8x8 numpy matrix in *row-major lexicographic order of 3-mers* — generate the ordered list of 3-mers programmatically (e.g. `itertools.product('ACGT', repeat=3)` sorted) rather than typing it out by hand, since a single ordering mistake will fail bit-exact comparison against a hidden oracle across many fuzzed inputs. Run `numpy.linalg.svd` on the matrix, take the largest singular value, and print it with exactly 4 decimal places using an f-string like `f"{val:.4f}"`. Test locally on a few sequences of varying length to sanity-check output format and stability before considering it done.