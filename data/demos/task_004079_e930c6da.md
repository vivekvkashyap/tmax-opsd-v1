Fix the vendored pcre2 build before writing any application logic: search /app/pcre2-10.42/src/pcre2_compile.c around lines 150-200 for a syntax error (e.g. an incomplete statement/stray token that breaks compilation), remove or correct just that line, then run the standard autotools flow (`./autogen.sh` if needed, `./configure`, `make`, `sudo make install`, then `ldconfig`) so `-lpcre2-8` and `<pcre2.h>` resolve correctly. Confirm the fix by compiling a trivial pcre2 test program before touching loc_aggregator.c. In the C program: read the CSV line by line, use `pcre2_compile`/`pcre2_match` with pattern `^[a-z]{2}-[A-Z]{2}$` to validate lang_code and drop non-matches, validate the text field's UTF-8 validity yourself (e.g. a manual UTF-8 byte-sequence decoder, since pcre2 alone won't validate encoding) dropping invalid rows, then deduplicate by tracking a set/hash of (timestamp, lang_code, text) triples seen so far — a hash table or sorted+uniq approach both work, but must catch duplicates even if not adjacent, not just consecutive ones. Aggregate valid unique rows by (date extracted from timestamp, lang_code) into counts, then emit summary.csv sorted first by date then by lang_code, matching the header exactly.