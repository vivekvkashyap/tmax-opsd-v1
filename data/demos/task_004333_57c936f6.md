Build this as three cleanly separated C stages so each is independently testable. Stage 1: parse the WAV header manually or via a small helper to get to raw PCM samples, then read them in fixed chunks of 1600 `int16_t` samples per frame, computing RMS as `sqrt(sum(x_i^2)/1600)` using `double` accumulation to avoid overflow. Stage 2: implement the recursive Bayesian filter exactly as specified — for each frame do a predict step (mix previous posterior through the 0.95/0.05 transition matrix) then an update step multiplying by the Gaussian likelihood `N(rms; mu, sigma)` for each state (`(1/(sigma*sqrt(2*pi)))*exp(-0.5*((x-mu)/sigma)^2)`), then renormalize so the two posteriors sum to 1 — track probabilities frame-by-frame in an array so you can also write them out. Stage 3: instrument only the predict+update loop (not RMS or I/O) with `clock_gettime(CLOCK_MONOTONIC, ...)` before/after, sum elapsed time across frames, divide by frame count *1e6 for microseconds, and print in the exact required format. Write the CSV with header `frame_index,prob_active` and one row per frame at 6 decimal places. Compile with `-O3 -lm` and sanity-check against the known state pattern in the audio (idle/active/idle) before finishing.
