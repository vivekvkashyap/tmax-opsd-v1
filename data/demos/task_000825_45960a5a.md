Treat this as ground-truth extraction from video, then threshold search, then a classifier that generalizes to hidden data — don't hardcode against just the training file. Extract frames from `/app/machine_feed.mp4` with `ffmpeg` (e.g. `-vf fps=10`) and for each frame crop/inspect only the top-left 10x10 region (`convert`/`ffmpeg -vf crop=10:10:0:0` or ImageMagick pixel sampling) to detect pure red (`#FF0000`); record the timestamps where this holds. Join those timestamps against `/app/training_log.csv` rows and, per sensor column, use awk to compare values at anomalous vs. normal timestamps — look for the sensor and integer threshold `N` where "value > N" cleanly separates every anomalous row from every normal row with no overlap (sweep candidate N values with awk/sort rather than eyeballing). Once you've identified the correct sensor column and threshold, write `/home/user/classify.sh` to take a CSV path, awk over all data rows (skip header) checking that column against the threshold, and print exactly `EVIL` if any row exceeds it or `CLEAN` otherwise, exiting 0. Validate against both `/app/corpus/clean/` and `/app/corpus/evil/` to confirm 100% correct classification before finishing, since the hidden grading corpus follows the same generation logic.