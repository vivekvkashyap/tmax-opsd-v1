Work through the three fixes independently, testing each in isolation before moving on. For redaction, use `sed -E` or `awk` with a regex that matches `pwd=<value>` up to the next `&` or space (e.g. `s/pwd=[^&[:space:]]*/pwd=[REDACTED]/g`) and redirect to the new file — don't touch anything else on the line. For the C code: replace the `system()` call in `log_access` with `fopen(path, "a")` + `fprintf` + `fclose` so a username containing shell metacharacters can never execute anything (test by passing a username with `;` or backticks and confirming no side effects, just a literal log line). For the open-redirect fix, only emit the `Location:` header when the redirect string's first character is `/` AND its second character is not also `/` (this blocks `//evil.com` protocol-relative bypasses) AND it doesn't start with `http://`/`https://` — write this as an explicit conditional, not a partial string match. Insert the CSP header line right before the blank line separating HTTP headers from body. Compile with plain `gcc file.c -o /home/user/auth_cgi` and verify by setting `QUERY_STRING` env var and running the binary directly to inspect stdout.