Diagnose `process.sh` line by line by running each pipe stage individually and inspecting intermediate output (`tail -n +2 data.tsv | cut -f3 | ...` then stop and print) — the bug is a `sed`/`tr`/regex step whose character class is inverted or wrong, e.g. a substitution meant to strip non-letters that instead strips the letters themselves, or wrong field delimiter, leaving blank/garbage tokens. Fix the offending stage so it correctly keeps only lowercase alphabetic words after lowercasing and stripping punctuation/digits.

For the frequency count and sort: after tokenizing into one word per line, use `sort | uniq -c | sort -k1,1nr -k2,2` to sort by count descending with alphabetical tiebreak ascending (plain `sort -nr` alone won't guarantee correct tie order). Reformat `uniq -c` output (which has leading spaces and count-then-word with variable spacing) into the exact `[count] [word]` single-space format using `awk '{print $1, $2}'` or similar, then take the top 5 with `head -n 5` and write to `top_words.txt`.

For `verify.sh`, read `top_words.txt`, assert exactly 5 lines, and compare its content exactly (string compare or checksum) against what a freshly-rerun pipeline produces, exiting 0/1 accordingly.
