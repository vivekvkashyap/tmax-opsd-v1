Work top-down through the pipeline: patch the C code first, then wire up FFI, then write the property test, then run it. Apply the patch with `cd /home/user/release_prep && patch -p0 < security_fix.patch` (or `-p1` depending on path prefixes in the patch header — check `route_parser.c` afterward to confirm `strncpy` replaced `strcpy`). For the FFI binding in `lib.rs`, declare an `extern "C"` block matching the C signature exactly (`fn extract_item_id(url: *const c_char, out_buf: *mut c_char, out_len: c_int) -> c_int`), and write a safe Rust wrapper that allocates a fixed-size buffer (e.g. `[0u8; 32]`), converts the input `&str` to a `CString`, calls the C function via raw pointers, and converts the result back to a Rust `String` only up to the returned data's null terminator. For the proptest, use `proptest!` macro with a strategy like `"[a-zA-Z0-9]{1,20}"` to generate ids, build the URL string `/api/item/<id>`, call your wrapper, and assert return code 0 and exact string equality with the generated id. Run `cargo test 2>&1 | tee /home/user/release_test.log` from `rust_wrapper/` so the exact output (including "test result: ok") is captured.