Don't try to fix the broken JSON with `json.loads` and exception-swallowing — since the corruption is specifically in `\uXXXX` escapes inside the `Message` field, extract only the two fields you actually need (`Timestamp`, `EventType`) with a targeted regex per line (e.g. `"Timestamp":\s*"([^"]+)"` and `"EventType":\s*"([^"]+)"`), which sidesteps the broken-escape problem entirely since you never need to fully parse `Message`. Read `metrics.csv` with the `csv` module keyed by `Timestamp`. Group event counts by `Timestamp` + `EventType == "ERROR"`, then join with the metrics on `Timestamp` and scan for the single row where `CPU_Load > 80.0` and that minute's ERROR count is `> 5` (both strict inequalities, matching exactly). Write the result to `/home/user/anomaly_report.json` using `json.dump` with the exact three keys `timestamp`, `cpu_load`, `error_count`, making sure `cpu_load` is emitted as a JSON number (float) not a string.
