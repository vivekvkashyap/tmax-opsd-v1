Recover the salt first: run `tesseract /app/l10n_secret.png stdout` (or equivalent OCR) and strip whitespace/newlines from the extracted text to get the value that follows `SALT:` — treat this as a variable you compute, not something to hardcode from a guess, since OCR output must be parsed carefully (trim trailing newline, check for stray characters tesseract sometimes injects). In `/home/user/validate_l10n.py`, load the JSON array, check for duplicate `msgid`s by building a set/Counter and detecting any count > 1 (fail immediately if found), and for every object recompute `hashlib.md5((msgid + msgstr + salt).encode()).hexdigest()` and compare it exactly (case-sensitive) against the stored `hash` field — a file is only clean if ALL objects pass AND there are zero duplicates. Exit 0 only if both conditions hold across the whole file, exit 1 the moment either check fails. Test your script against `/app/corpus/clean/` (all should exit 0) and `/app/corpus/evil/` (all should exit 1) before finishing — if a clean file wrongly fails, double check the exact string concatenation order (msgid, then msgstr, then salt, no separators) and that you extracted the salt without extra whitespace.