Treat `/app/spectro_sim` purely as a black-box oracle: run it on small hand-crafted inputs (N=2 or N=3 with simple masses and a single bond) and compare outputs against known closed-form mass-spring frequencies to hypothesize the underlying model, rather than trying to disassemble the stripped binary. The hint in the task (graph Laplacians, generalized eigenvalue problem, mass matrix) points to: build the weighted graph Laplacian K from the bond/adjacency matrix (off-diagonal = -weight, diagonal = row sum of weights), build diagonal mass matrix M from the atom masses, and solve the generalized eigenvalue problem K v = λ M v (e.g. via `scipy.linalg.eigh(K, M)`), then output sqrt of the (non-negative, clipped) eigenvalues sorted ascending. Validate your hypothesis by running both your Python script and the binary on several increasingly complex self-generated test graphs and comparing outputs numerically before finalizing. Use vectorized numpy/scipy operations (not naive loops) so your script is comfortably faster than the slow binary, and clip tiny negative eigenvalues (from floating-point noise) to zero before taking the square root to avoid NaNs.