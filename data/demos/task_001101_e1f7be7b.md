Structure the C program around a small buffered RLE encoder: maintain a "current byte" and "run count" state; for each byte read from the FIFO, if it matches the current byte and count < 255, increment count, else flush the pending (count, byte) pair to the output buffer and start a new run — this naturally handles runs over 255 bytes by emitting multiple `0xFF` pairs plus a remainder pair. Install `signal()`/`sigaction()` handlers for `SIGUSR1` and `SIGTERM` that just set a flag (avoid doing file I/O directly inside a signal handler; check the flag in your main read loop instead) so that on the next loop iteration you flush any pending run, write the `0x00 0x00` EOF marker, `fclose` the current archive, and then either `rename()` it to `repo.N.dat` with an incrementing counter (SIGUSR1) and open a fresh `repo.dat`, or just exit cleanly (SIGTERM) without renaming. Open the FIFO for reading with `mkfifo()` if it doesn't exist, and use a blocking read loop (`read()` on the fd) so the signal can interrupt it (handle `EINTR`). Test manually by writing known byte sequences into the FIFO and sending signals via `kill -USR1`/`kill -TERM`, then inspect the resulting files with `xxd` to confirm the exact byte layout matches your RLE encoding rules before considering it done.