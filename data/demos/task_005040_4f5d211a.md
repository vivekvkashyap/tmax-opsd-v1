Start by inspecting the undocumented schema before writing any C: run `sqlite3 /home/user/backup_mgr.db ".schema"` (and `.tables`) to discover the actual table/column names — do not assume standard names like `jobs`/`dependencies`, this DB is deliberately obfuscated. Identify which table holds job names (with a name-to-id mapping) and which holds directed edges (source id -> dependent id). In the C program, first resolve the given job name argv[1] to its internal id via a parameterized SELECT (sqlite3_prepare_v2 + sqlite3_bind_text), then recursively query outgoing edges for each id using sqlite3_bind_int — never string-concatenate values into SQL. Build the nested JSON structure recursively as you traverse (a job node with a "dependencies" array of child job nodes, recursing until a node has no outgoing edges), writing/serializing with fprintf or a small JSON builder — watch for graph nodes referenced by multiple parents (the JSON should re-expand the subtree per parent, not dedupe/collapse into a DAG). Compile with `-lsqlite3`, run `./export_graph "app_server_backup"`, and sanity-check the output with `jq` before finishing.