Start by exploring /app/data/audit_logs.db with `sqlite3 .schema` to reverse-engineer table names/columns and how user, transaction, and access-event tables relate via a shared user id column — don't guess column names. Since the task explicitly warns an unindexed cross-table query takes 10x+ too long, add `CREATE INDEX` statements on the foreign-key/join columns (the user-id columns in each table) before running your aggregation query, and use GROUP BY aggregation (COUNT, COUNT DISTINCT) in a single SQL query rather than looping per-user in Python — that's what actually kills runtime. Treat /app/legacy_risk_scorer as an opaque CLI: pipe `user_id,total_access_events,distinct_ips` CSV lines to its stdin and capture stdout CSV of `user_id,risk_score` — do not try to reverse-engineer or reimplement its internal scoring formula, just call it as a subprocess. Filter users whose returned risk_score > 0.80, then run one more aggregate SQL query (or join) to sum `total_tx_amount` per flagged user, rounding each to 2 decimals. Write the result as a JSON object mapping user_id (string) to that float in /home/user/flagged_users.json, and time your own script end-to-end with `time python3 audit_aggregator.py` to confirm it's comfortably under 3 seconds before considering it done.