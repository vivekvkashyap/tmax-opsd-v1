Use `sklearn.decomposition.PCA` fit once on the full dataset (no `n_components` argument) to get all explained-variance ratios, take `np.cumsum` of `pca.explained_variance_ratio_`, and find the smallest component count where the cumulative ratio is `>= 0.95` via `np.argmax(cum_var >= 0.95) + 1` (argmax on a boolean array returns the first True index, which is exactly the "minimum count satisfying the threshold" — a naive loop counting until you exceed 0.95 with `>` instead of `>=` would be off by the boundary case, so match the operator precisely). Refit a fresh `PCA(n_components=optimal_components)` on the same data, transform it, and use `pca.inverse_transform` on the transformed data to reconstruct, then compute `np.max(np.abs(original - reconstructed))` for the max absolute error — compute this from the actual refit transformer, not the first exploratory fit's partial components. Save the transformed array under dataset key `features` in the HDF5 file with `h5py`, and write the JSON log with exactly the two required keys, using Python native `int`/`float` (not numpy types) so JSON serialization doesn't choke.