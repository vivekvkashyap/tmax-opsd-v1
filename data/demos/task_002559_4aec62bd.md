First transcribe `/app/voicemail.wav` (e.g. with `whisper` or `ffmpeg`+an ASR tool) to recover the two secrets you need: the 5-digit emergency token and the allowed internal domain name — don't guess these, extract them from the audio and treat them as configuration constants in your code once recovered. Then build the Go proxy: (1) load `/app/ca.crt` into an `x509.CertPool` and set it as `RootCAs` in a `tls.Config` used by your outbound `http.Transport`/`http.Client` — never set `InsecureSkipVerify: true`. (2) On each incoming request, check the `Authorization: Bearer <token>` header against the recovered token and return 401 if missing/wrong. (3) Parse the `next` query parameter: if it has no scheme/host (a relative path), allow it; if it's an absolute URL or a scheme-relative URL (starting with `//`), parse it with `net/url` and require the resulting Host to exactly equal the recovered allowed domain, otherwise return 403 — pay special attention to `//host/path` forms, which browsers treat as absolute but naive string checks often miss. (4) Only after these checks pass, reverse-proxy to `https://127.0.0.1:8443` using the validated TLS client. Build, run it in the background bound to `127.0.0.1:9090`, and manually curl a few next= variants to confirm the 200/401/403 behavior before finishing.