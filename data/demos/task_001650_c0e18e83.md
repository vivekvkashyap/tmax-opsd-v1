Step 1, service discovery: scan local ports 8000-9000 for open TLS listeners (e.g. `nmap -p 8000-9000 localhost` or a loop of `openssl s_client -connect localhost:$p </dev/null` for each candidate port), then for each responding service fetch its certificate (`openssl s_client -connect localhost:PORT -showcerts </dev/null 2>/dev/null | openssl x509 -noout -text`) and validate it against the trusted CA with `openssl verify -CAfile /home/user/evidence/ca.crt cert.pem`; only the certificate that verifies successfully is the real target — read the SHA-256 hash out of its Organization (`O`) subject field with `openssl x509 -noout -subject`. Step 2, extract the salt: run `strings /home/user/evidence/locker | less` (and/or `objdump -d`) to locate the plaintext salt string the binary appends to the PIN before hashing — look for anything printed or referenced near hashing logic. Step 3, crack: write a Rust program under `/home/user/cracker/` that loops PIN candidates `00000`-`99999`, formats each as a zero-padded 5-digit string, concatenates the recovered salt, computes SHA-256 (e.g. via the `sha2` crate), and compares against the target hash from the valid certificate; stop at the first match and write only the 5 digits to `/home/user/recovered_pin.txt`. Do everything without sudo.