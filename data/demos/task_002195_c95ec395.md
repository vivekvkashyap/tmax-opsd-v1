Build and validate this pipeline in isolated stages before combining them, since each external tool (ffmpeg, whisper.cpp) can silently misbehave. First, manually run ffmpeg on one segment from segments.csv to confirm the -ss/-to (or -ss/-t) extraction produces a correct-duration 16kHz WAV chunk — get the seek arguments right (use accurate seeking, not just fast keyframe seeking, since segment boundaries are arbitrary timestamps). Second, clone and build whisper.cpp, download the tiny.en model, and run its main binary on one extracted chunk standalone to confirm transcription works and to learn its stdout output format so you can reliably parse just the transcript text (strip timestamps/metadata whisper.cpp prints). Only then write the Go orchestration: parse the CSV, launch workers via a bounded worker pool (e.g. a buffered channel/semaphore sized 4) so at most 4 segments extract+transcribe concurrently, collect (SegmentID, SpeakerID, transcript) per segment, then group by SpeakerID and concatenate transcripts in ascending SegmentID order (not completion order, since concurrency reorders results) joined by a single space with whitespace trimmed. Compute word_count by splitting the aggregated text on whitespace. Clean up temp WAV files after use, and write summary.json with the exact required schema.