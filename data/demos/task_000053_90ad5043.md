Structure `/home/user/organizer.py` in clear stages. First, parse `checksums.txt` (format `<hash>  <filename>`) into a dict, then for each artifact file in `/home/user/artifacts/` compute `hashlib.sha256(...).hexdigest()` over its bytes and compare against the recorded hash — discard (skip moving) any mismatch. For filenames matching `worker_v<semver>_<os>_<arch>.bin`, extract the semver/os/arch with a regex (don't assume fixed-width fields since os/arch/version lengths vary), then use `shutil.move` (creating destination dirs with `os.makedirs(..., exist_ok=True)`) into `/home/user/organized/<major>.<minor>/<os>/<arch>/`. For "highest stable version", parse each valid semver into (major, minor, patch) tuples for comparison and explicitly exclude any version string containing a `-` (pre-release identifier) from that comparison, even though pre-release files still get organized normally. Use `packaging.version.Version` if available for robust comparison, but manually filter out `.is_prerelease` versions before taking the max. Finally write the mock fixture JSON next to the winning binary and the plain version string to `/home/user/latest_version.txt`.
