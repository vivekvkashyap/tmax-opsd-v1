Build the standalone algorithm first, verify it against the oracle, then wire up the services around it — don't debug both layers at once. For the k-mer distance script: for each sequence, slide a window of length 3 across all overlapping positions (i.e., `len(seq)-2` windows) and count each of the 64 possible AAA-TTT trinucleotides, add a pseudocount of 1 to every one of the 64 bins (even unseen ones), divide the vector by its sum to get an L1-normalized distribution, then compute the Euclidean (L2) norm of the difference between the two sequences' normalized vectors, and print rounded to 6 decimal places. Test this script directly against `/app/legacy_kmer_dist` on many random ACGT sequences of varying lengths before touching the services. For the multi-service half: fix the Redis broker URL in `gateway/app.py` to point at the redis service (`redis://localhost:6379/0` or the compose service name), implement `worker/tasks.py`'s Celery task to shell out to `python3 /home/user/kmer_dist.py <seq1> <seq2>` and capture stdout as the result, then start redis/gateway/worker (via docker-compose or manually) and confirm the full HTTP POST -> Celery -> subprocess -> HTTP response round trip returns `{"distance": <float>}` on port 8000.
