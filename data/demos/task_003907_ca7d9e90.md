Start by recovering the actual compliance rule from audio, don't guess it — install/use a transcription tool (`ffmpeg` to convert if needed, plus something like `whisper-cli` or an offline Python speech-to-text library) on `/app/voicemail.wav`, and read the transcript carefully for the precise thresholds and conditions it states (a size limit and a filename/extension condition). Then implement `/home/user/cleanup.py` so it processes each `.tar.gz` in the given directory without extracting to disk: open the archive as a compressed stream (`tarfile.open(path, 'r:gz')`), iterate `TarInfo` members to inspect each entry's name and uncompressed `.size` metadata directly from the tar headers, and evaluate the rule you transcribed (sum of uncompressed sizes vs. threshold, plus any forbidden extension) purely from that metadata. Delete files that violate the rule (`os.remove`), and for compliant files rename in place by prepending `approved_` (`os.rename`) — do this after fully closing the tarfile handle to avoid file-lock issues. Test your classifier against a mix of your own small/large and clean/flagged-extension synthetic archives to confirm both branches work before trusting it on the hidden corpus.