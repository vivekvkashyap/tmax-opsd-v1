Prefer sqlite3 with a recursive CTE over hand-rolled BFS in awk, since it naturally handles both "shortest path" and "lexicographically-first tiebreak" if you build the CTE carefully. Load both CSVs into temporary sqlite tables (`.mode csv`, `.import`), then write a recursive CTE that grows paths edge-by-edge from START_ID, tracking the path as a comma-joined string and its node count; at each recursion step only keep, for a given (current node, depth), the lexicographically smallest path-so-far — or simpler, generate all paths reaching END_ID, group by path length, take the minimum length, then take the single row with the smallest path string via `ORDER BY length, path LIMIT 1`. A naive attempt often finds *a* shortest path but not the lexicographically-first one when ties exist, or forgets to cap recursion depth (which can loop forever on cyclic citation graphs) — add a depth bound and `LIMIT` to guard against runaway recursion. Once you have the winning path's node list, split it, join each ID against `papers.csv` for title/year, and emit a JSON array with `step` (1-indexed), `id`, `title`, `year` in path order — build this with `jq -n` from CSV-derived JSON lines rather than string-concatenating JSON by hand, to keep quoting/escaping correct. Make the script `chmod +x`, ensure only the JSON hits stdout (send any diagnostics to stderr), and test it against a couple of source/target pairs by eye before considering it done.