First find all `.info` files recursively with `find /home/user/artifact_repo -name '*.info'`. For each file, clean it before parsing: strip null bytes (`tr -d '\000'`) and normalize line endings (`tr -d '\r'` or `dos2unix`) so `Key: Value` lines parse reliably regardless of Windows/Linux endings. Parse each cleaned file's lines case-insensitively — match keys against `id`, `version`, `size` ignoring case (e.g. lowercase the key before comparing, using `awk` with `tolower()` or a shell case statement) since the same field appears as `Id`, `ID`, `VER`, `SIZE`, etc. across files, and note the three keys can appear in any order within a file, so extract by matched key name, not by line position. Aggregate one `id,version,size` row per file into `/home/user/registry_index.csv` with that exact header first, then sort the data rows by `size` descending numerically (`sort -t, -k3,3nr`), breaking ties alphabetically by `id` ascending (`-k1,1`). Since the verifier checks an exact sha256 of the file content, be precise about trailing newline and field formatting (no extra whitespace, size as a plain integer).