Scaffold dependencies in `Cargo.toml` (`csv`, `serde`/`serde_json`, `rustfft`, `num-complex`, `rand`) and read both input files: parse `network.json` into a map from node_id to its neighbor list (degree = list length) with `serde_json`, and load `signals.csv` grouped by `node_id` preserving time order (128 samples each) with the `csv` crate. For the FFT feature, build a `rustfft` planner, run a forward FFT on each node's 128 real samples (cast to `Complex<f32>` with zero imaginary part), take the magnitude (`.norm()`) of each output bin, then scan magnitudes from index 1 through N/2 inclusive (explicitly skipping index 0, the DC term) to find the index of the maximum. For the bootstrap CI, be exact about reproducibility: for each node create `StdRng::seed_from_u64(node_id as u64)`, then perform exactly 1000 resampling iterations, each drawing N indices via `rng.gen_range(0..N)` in a fixed, sequential order (don't parallelize or reorder, since that changes the RNG stream) to build a resampled mean; collect all 1000 means, sort ascending, and read off index 24 and 974 for the lower/upper bounds. Write results sorted by ascending `node_id` to `/home/user/features.csv` with the exact header and column order given, then `cargo build --release && ./target/release/feature_extractor` (or `cargo run`) to regenerate the output.
