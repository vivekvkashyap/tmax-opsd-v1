Treat the binary as a black-box forward model and solve an inverse problem via optimization rather than trying to disassemble it. First characterize `/app/signal_gen` empirically: run it with several chosen (amplitude, decay, frequency) triples and inspect how the 100 output values respond, confirming it looks like a damped oscillation (exponential envelope times a periodic term) so you can pick a sane parametric form and reasonable bounds (positive amplitude, positive decay time, non-negative frequency) for your fit — this avoids divergence when a naive optimizer wanders into negative decay times. In the Flask/FastAPI service's `/fit` handler, check the `Authorization: Bearer LabSecret99` header first and return 401 before doing any work if it's missing or wrong. Use `scipy.optimize.curve_fit` or `least_squares` with bounds, calling your own Python re-implementation of the signal model (not shelling out to the binary per-iteration, which would be far too slow) to minimize squared error against the posted noisy data, then multi-start or use a coarse grid over frequency to escape local minima (periodic signals are classic multi-modal-in-frequency problems). Return the fitted `amplitude`, `decay`, `frequency` as JSON and background the service so it's reachable on 127.0.0.1:8080 for testing with `curl -X POST` before finishing.
