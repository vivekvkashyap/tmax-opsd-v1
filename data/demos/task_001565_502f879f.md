Implement the Go cleaner as a pure stdin-to-stdout filter so it composes with any CSV producer, including the ffprobe pipeline in part 2. Parse each `timestamp,size,type,retry_id` line, round the timestamp to the nearest 0.5 using `math.Round(timestamp*2)/2` (not naive truncation, which mishandles the `.25`/`.75` boundary cases), lowercase the `type` field, and key deduplication on `(aligned_time, type)` — when a later record collides with an existing key, replace it only if its `retry_id` is strictly higher, or on a `retry_id` tie only if its `size` is strictly higher, otherwise keep the first one seen (so use `>` comparisons, not `>=`, to preserve first-seen-wins on true ties). Format `aligned_time` with exactly one decimal (`%.1f`) and sort the deduplicated output by `aligned_time` ascending then `type` ascending before printing `aligned_time|type|size|retry_id` lines. Build with `go build -o /home/user/etl_cleaner`. For part 2, extract `pkt_pts_time,pkt_size,pict_type` per frame from `/app/etl_stream.mp4` with `ffprobe -select_streams v:0 -show_entries frame=pkt_pts_time,pkt_size,pict_type -of csv=p=0`, append a constant `retry_id=1` column to each row (matching the exact 4-field CSV order your cleaner expects), pipe that into your compiled binary, and save stdout to `/home/user/cleaned_video_data.txt`.