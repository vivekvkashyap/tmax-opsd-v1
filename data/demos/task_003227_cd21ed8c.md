Treat this as three separable stages with a clear interface boundary between them. First, inspect `/home/user/data.db` with `sqlite3 .schema` (or `PRAGMA table_info`) to discover the actual table/column names rather than assuming — the description names customers/items/transactions/feedbacks conceptually but you must confirm exact schema. Write `/home/user/etl.py` to connect via `sqlite3`, iterate each table, and emit `nodes.jsonl` (customers prefixed `c_`, items prefixed `i_`, each `{"id":..., "type":..., "properties": {...other columns...}}`) and `edges.jsonl` (transactions → `PURCHASED` edges src=customer dst=item, feedbacks/reviews → `RATED` edges, with remaining columns like rating/date under `properties`). Second, write `/home/user/query.py` to load only the two JSONL files (never reopen the DB) into in-memory Python dicts/adjacency structures keyed by node id. Third, implement the graph pattern match precisely: for each `PURCHASED` edge (A -> X), check whether any other customer B (B != A) has a `RATED` edge to X with rating==5, AND whether A itself has any `RATED` edge with rating==1 to any item Y — collect all A satisfying both, resolve their `c_id` back to `name` via the node properties, dedupe, sort alphabetically, and write one name per line to `/home/user/target_customers.txt`. Run both scripts end-to-end and inspect the intermediate JSONL before trusting the final output.
