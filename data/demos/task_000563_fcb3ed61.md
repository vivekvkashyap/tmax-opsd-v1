Build this as a standard sklearn-style pipeline, being careful about the cleaning step order. Load the CSV, then for each of the six required columns coerce to numeric (e.g. `pd.to_numeric(col, errors='coerce')`) so non-numeric strings like 'ERR' become NaN, and only after coercing all relevant columns, drop any row with a NaN in any of them — coercing and dropping per-column up front, rather than filtering before coercion, avoids accidentally keeping malformed rows that look numeric in one column but not another. Extract the five sensor feature columns, standardize with mean/std scaling (e.g. `StandardScaler`), then fit PCA with exactly 1 component on the standardized features to get PC1. Fit a plain `LinearRegression` with PC1 as the sole predictor of `target`, compute predictions on the same data, and score with `r2_score` (or the model's built-in `.score`) — do not use a train/test split since the task wants the in-sample R². Round to exactly 4 decimal places and write only that number (no extra text/newline padding beyond what's natural) to the output path.
