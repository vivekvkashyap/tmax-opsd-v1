Build the Go cleaner as a true streaming line processor: use `bufio.Scanner` on stdin, split each CSV line into timestamp_sec/sensor_id/value, and maintain two pieces of state across the whole stream — the SHA-256 hash of the last *emitted* record's `sensor_id:value` string (compare each new record's hash before deciding to drop it, then update this state only when you actually emit), and the last-seen timestamp (global, not per-sensor). Before emitting a new valid (non-duplicate) record, check if its timestamp exceeds `last_timestamp + 10`; if so, loop emitting synthetic `{"t": last_timestamp+10, "s": "FILL", "v": 0}`, then `+20`, etc., strictly while the next synthetic timestamp is `< new_record_timestamp`, updating last_timestamp and the running sum/count for each one emitted. On EOF, emit the summary line with counts/sum accumulated over every JSON line actually written (including synthetics, excluding the summary itself). Compile with `go build -o /home/user/cleaner`. For the pipeline script, chain `nc localhost 8080 | head -n 500 | /home/user/cleaner | while read -r line; do redis-cli RPUSH sensor_data_cleaned "$line"; done` (or `xargs`), then register it via `crontab -e`/`crontab -l | { cat; echo '* * * * * /home/user/pipeline.sh'; } | crontab -` for the `user` account.
