Read `/home/user/sim_project/simulator.cpp` closely and identify every variable that participates in the position accumulation chain, not just the obvious one: the accumulator itself and any intermediate variables used per-step in the accumulation loop (e.g. velocity/timestep locals multiplied together each iteration) all need to be promoted from `float` to `double`, since a single remaining `float` in the chain will reintroduce precision loss. Make the minimal type change only — don't touch the parsing logic, the CHECK comparison tolerance, or the overall control flow. Rebuild with `make` (or the given `Makefile` target) inside `/home/user/sim_project/`, then run `./simulator large_input.txt`, inspect its actual stdout, and confirm it reports success (no mismatch errors) rather than assuming the value in the task's error checks. Take the final `FINAL: <value>` line exactly as printed by the program and write it verbatim to `/home/user/result.txt` — don't manually recompute or reformat the number yourself, since the printed precision must match what the fixed binary emits.
