Implement this in precise, testable stages rather than one big loop. Parse `data.txt` into a 10x10 grid, then flatten to 100 `(X1, X2, Y)` tuples using the row index as X1 and column index as X2 exactly as specified — get this indexing right first, since swapping row/col will silently corrupt everything downstream. Implement gradient descent for `Y = θ1*X1^2 + θ2*X2^2` as a standalone function first and sanity-check it converges reasonably on the full (non-bootstrapped) dataset before wrapping it in the bootstrap loop — this isolates optimization bugs from resampling bugs. For bootstrapping, seed `std::mt19937` with exactly 42 once, and draw all 1000*100 indices from a single `std::uniform_int_distribution<int>(0,99)` bound to that one generator, in the same order/loop structure the spec implies (outer loop over B=1000 iterations, inner loop drawing 100 indices each) — the seed sequence order matters for reproducing the deterministic expected output. After each bootstrap fit, store θ1; once all 1000 are collected, sort them and extract mean, and the values at indices 25 and 974 for the CI. Write `Mean_Theta1 CI_Lower CI_Upper` space-separated to `result.txt`. Compile and actually run the program to produce real output before considering the task done.