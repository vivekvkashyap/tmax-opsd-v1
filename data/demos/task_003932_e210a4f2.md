First recover the ground-truth passphrase from the audio: use a speech-to-text tool available in the terminal (e.g. `whisper /app/auth_sample.wav` or convert with `ffmpeg` first if format conversion is needed) and transcribe carefully — the exact wording matters because it becomes a hardcoded comparison string in your filter, so double check the transcription by listening to segment boundaries or re-running with different model settings if the result looks ambiguous. Then design `filter.c` as a strict allow-list validator, not a blocklist patched reactively: for each stdin key-value block, (1) reject if `file_path` doesn't end in `.wav`, (2) reject if `file_path` contains any of `; | & $` or a `..` path-traversal sequence, (3) reject unless `old_passphrase` exactly equals the transcribed phrase (compare full string, not prefix), (4) reject unless `file_sha256` is exactly 64 hex characters (check length and that every char is `[0-9a-fA-F]`). Only when a request passes all four checks should it exit 0; any single failure means exit 1. Before finishing, mentally walk through injection payloads like `/app/audio.wav; rm -rf /` or `../../etc/passwd.wav` to confirm your character checks actually catch them, then compile with `gcc -O2 /home/user/filter.c -o /home/user/filter` and test by piping sample requests into it.
