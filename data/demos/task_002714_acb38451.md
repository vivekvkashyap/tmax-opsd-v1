Treat recovery and the numerical-stability fix as two separate problems. For recovery: since the main `.db` file's header is damaged but a valid `-wal` file sits beside it, don't try to repair the header directly — instead attempt a WAL replay. Try opening the database normally with sqlite3 (or the `sqlite3` CLI) pointed at the original path so SQLite's own WAL-checkpoint logic replays `sensor.db-wal` into a fresh copy; if the corrupted header blocks that, extract/replay the WAL frames yourself (SQLite's WAL format has a documented frame layout: 32-byte file header then per-frame headers you can parse) or use `.recover`/`.dump` style tooling to pull surviving rows, then load everything into a brand-new, cleanly created database at `/home/user/recovered.db` with the same `measurements` schema. Verify recovery by running a basic `SELECT count(*)` before moving on. For the variance bug: in `/home/user/mre.py`, first reproduce the failure by computing variance the naive `(sum(x^2) - sum(x)^2/N)/(N-1)` way on the large near-constant values and show it goes to ~0 or negative due to catastrophic cancellation from subtracting two huge close floats; then compute it again with a numerically stable one-pass method (Welford's algorithm, or Python's `statistics.variance`) and show they diverge. Finally run the stable method over all rows in `recovered.db`, round to exactly 4 decimals, and write only that number to `/home/user/final_variance.txt`.
