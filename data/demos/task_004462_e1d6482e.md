Use `os.walk` with care around symlinks: pass `followlinks=False` (the default) so directory symlinks aren't descended into, or if you must follow them, track visited real paths (`os.path.realpath`) in a set and skip any directory whose real path was already seen, to guarantee termination despite the loops in the fixture. For each `.gcode` file, read its lines and search for a line matching exactly `; filament_used_mm: <value>` (a regex like `r'^; filament_used_mm: ([\d.]+)$'` per line, not a substring search anywhere in the file) — skip files where no line matches. Rename in place with `os.rename(old, f"{root}/{base}_{value}mm.gcode")`, keeping the original directory, and record the new absolute path mapped to the float value as you go (parse the captured value with `float()` but keep its original string form for the filename so `142.5` doesn't become `142.50`). Build the summary dict incrementally, then write it with `json.dump` to `/home/user/printing_summary.json.tmp`, close/flush, and `os.rename` it to the final `/home/user/printing_summary.json` — this rename-based atomicity is the point of the task, so don't write directly to the final path.
