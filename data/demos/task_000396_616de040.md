Write the C program to read stdin line by line with `fgets`, parse each hex string with `strtoul(line, NULL, 16)` (handles the `0x` prefix natively), collect all values into a dynamic/large-enough array, then sort ascending with `qsort` and a comparator for `unsigned int`/`uint32_t`. Implement the sliding-window check as a simple loop: for each index i starting at 3, compare `t[i] - t[i-3] <= 10` and print `t[i]` with `%u\n` if it violates — do the subtraction in an unsigned-safe way (values are sorted ascending so t[i] >= t[i-3], no underflow risk). Compile with `gcc -o rate_check rate_check.c`. Then chain the shell steps exactly as specified: `./rate_check < raw_requests.txt > violations.log` (or `cat raw_requests.txt | ./rate_check > violations.log`), then `diff violations.log expected_violations.log > diff.log`. Common naives to avoid: off-by-one on the window index (must be `i >= 3`, comparing against the 3rd-preceding element, not the immediately preceding one), and forgetting that the diff must be run against the pre-existing baseline file already present in the environment rather than one you invent yourself.
