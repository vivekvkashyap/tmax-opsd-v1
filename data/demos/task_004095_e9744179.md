Scaffold with `cargo init /home/user/audit_tool` and add the needed crates (a regex or manual parser for the log line format, plus a SHA-256 crate such as `sha2`). Parse each log line by splitting on whitespace and extracting the `filename=` query parameter (URL-decode if needed) from the path segment, keeping only lines where method is POST, path starts with `/api/upload`, and status is exactly `200` — do not accidentally include other statuses or methods. For each candidate filename, resolve it relative to the safe base directory `/home/user/server_root/uploads/` using canonicalization (e.g., `std::fs::canonicalize` combined with joining the path, handling `..` traversal), and only keep entries whose canonical resolved path lies strictly outside that base directory AND which actually exist on disk — both conditions matter, since a traversal that resolves outside but points to a nonexistent file must be excluded. Compute SHA-256 over the raw file bytes for each qualifying finding. Preserve the original log order (already chronological) when building the output array, and emit exactly the five required keys per object, writing valid JSON to `/home/user/audit_report.json`. Build with `cargo build --release` and actually run the binary to produce the file.