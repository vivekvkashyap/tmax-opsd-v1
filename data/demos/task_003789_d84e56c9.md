Build this as a streaming binary parser in Go, reading each `.wal` file with a small fixed-size buffer rather than loading the whole file — use `os.Open` plus `io.ReadFull` on a buffer for just the header bytes you need, since the task emphasizes not loading large files fully into memory. First read config.json with `encoding/json` to get `cutoff_time`, `backup_dir`, `report_file`. For each `.wal` file: read the first 4 bytes and check they equal ASCII `WALB` exactly (reject/skip otherwise), then read the next 4 bytes and decode as a big-endian uint32 timestamp via `binary.BigEndian.Uint32` — compare with `<` (strictly less than) against cutoff_time. For files identified as expired, seek to byte offset 30 from the start of the `.wal` file (not offset 30 within the ELF) and read 2 bytes, decoding as little-endian uint16 via `binary.LittleEndian.Uint16` to get `e_machine`; format it as lowercase hex without a leading `0x` in your fmt verb but prepend `0x` in the string yourself, matching the exact output format `<filename>: <timestamp>, arch: 0x<hex>`. Append each matching line to the report file, then `os.Remove` the expired file — double check you never delete files with timestamp >= cutoff_time.
