Read `audio_analyzer.c` carefully before touching it — locate the header-length parsing (bytes 4-7 little-endian) and the naive variance formula (likely `sum_sq/N - mean^2` in `float`). Fix the length parsing so a value of `0xFFFFFFFF` (i.e., signed `-1`) is detected explicitly and triggers "read until EOF" instead of feeding that huge unsigned value into `malloc`. Replace the variance computation with Welford's online algorithm (maintain running mean and M2, update incrementally per sample, then `variance = M2/n`) or a genuine two-pass method using `double` precision, since the one-pass sum-of-squares approach loses precision badly when there's a large DC offset. Compile to `/home/user/audio_analyzer_fixed`. For the diagnostic step, use `ffmpeg` to convert the WAV to headerless raw 16-bit PCM, then prepend your own 32-byte header (magic "DIAG", 0xFFFFFFFF length field little-endian, 24 zero bytes) with a small script before concatenating — don't hand-edit bytes manually. Run the fixed binary on the assembled input and capture stdout exactly as printed into `/home/user/diagnostic_output.txt`. Also write a regression script that recompiles, generates a synthetic large-DC-offset signal, and asserts variance is positive and stable.
