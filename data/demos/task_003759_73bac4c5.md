Read the HDF5 datasets first (inspect shapes/dtypes with `h5dump` or a quick Python check) so your C++ loops match `x`, `t`, `u` dimensions exactly before writing the grid search. Use the HDF5 C++ API (install `libhdf5-dev` via apt if not present, then link with `-lhdf5_cpp -lhdf5`) to read all three datasets into flat arrays. Build the grid search as nested loops over A (101 points, step 0.04) and D (91 points, step 0.01), computing MSE = mean((A/sqrt(t) * exp(-x^2/(4*D*t)) - u)^2) across every (x,t) pair — precompute exp/sqrt carefully to avoid overflow/NaN at small t. Parallelize the outer grid loop with `#pragma omp parallel for`, using a reduction or per-thread local best-tracking (with a critical section) to safely find the global minimum MSE without races, then compile with `-fopenmp`. After finding the best (A, D, MSE), write exactly one line to `/home/user/best_parameters.txt` formatted to 4 decimal places (e.g. via `printf("%.4f %.4f %.4f\n", ...)`), matching the required "A D MSE" format exactly. Compile and run the program yourself to confirm the output file is produced correctly before finishing.
