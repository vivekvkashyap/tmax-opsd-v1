Fix the vendored crypto library first: inspect `/app/fastpbkdf2/Makefile`, find the invalid compiler flag in `CFLAGS` (something like a bogus optimization level) and correct it to a valid flag, then run `make` and confirm `libfastpbkdf2.a` builds cleanly before writing any C of your own. For the sanitizer, write a line-oriented state machine: maintain a boolean "inside private key block" flag that is set when a line matches `-----BEGIN PRIVATE KEY-----` or `-----BEGIN RSA PRIVATE KEY-----` and cleared (without emitting that END line either) when the matching `-----END ... KEY-----` line is seen — while the flag is set, drop every line, including the BEGIN/END markers themselves. CERTIFICATE blocks need no special handling; just pass them through like any other line. For AUTH lines, parse `user:`, `hash:`, `salt:` fields with `sscanf`/manual parsing, then for each of the known weak passwords call `fastpbkdf2_hmac_sha256` with the extracted salt and iteration parameters, hex-encode the digest, and compare against the logged hash; on a match print `AUTH user:<username> REDACTED`, otherwise print the original line unchanged. Compile linking against the built static library (`gcc sanitizer.c -L/app/fastpbkdf2 -lfastpbkdf2 -o sanitizer`), then test manually against a hand-built log containing one cracked and one uncracked hash before running the full corpora.
