Fix the vendored library before writing any application code, and verify the fix with the package's own test/build commands rather than assuming it's correct. In `/app/vendored/log-archiver-lib-1.2.0/Makefile`, find the `LOCK_DIR` (or similar) variable pointing at a path the current user can't write to and repoint it at a writable location such as `/tmp/...`; run `make test` and `make install` and iterate until both succeed cleanly in your virtual environment. In `flock_manager.py`, locate the `fcntl.flock(..., LOCK_EX | LOCK_NB)` call and wrap it in a retry loop that catches `BlockingIOError`/`OSError` (errno EAGAIN) and sleeps briefly before retrying, rather than letting it propagate — this is what lets concurrent archive tasks wait instead of crashing; test it by spawning two processes that both try to lock the same directory simultaneously. Only then write `/home/user/dedup_archive.py`: parse multi-line records by detecting `[YYYY-MM-DD HH:MM:SS]` lines as record boundaries (read from stdin when the arg is `-`), hash each record body with SHA-256, use the patched `FlockManager` around all writes into `/home/user/backup_repo/objects/<hash>`, append `<timestamp> -> <hash>` lines to `index.txt` in the target dir in encounter order, and create hard links (`os.link`, not copies) named `record_<hash>.log` pointing at the object files. Test with your own multi-line log fixtures, including duplicate bodies (should hash-dedupe and share the same object/inode) and concurrent invocations, before trusting it against the hidden oracle comparison.