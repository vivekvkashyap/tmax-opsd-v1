Start by inspecting the vendored package's build system rather than the source logic first: `grep -R` the Makefile/CMake files under `/app/librouteconf-2.1.0` for suspicious flags in `CXXFLAGS`/`CFLAGS` (e.g. anything guarding a code path with `-D`), since a common perturbation is a build-time macro that disables or breaks correct parsing behavior (such as whitespace trimming) rather than the C++ logic itself being wrong. Once you find and remove/fix the offending flag, do a clean rebuild (`make clean && make && make install` or equivalent) and confirm the shared/static library artifact is refreshed (check its mtime). Then get `log-router.service` to actually start and check `systemctl status`/journal logs to confirm the crash is gone. Finally write `route_canonicalizer.cpp`, calling `RouteConf::parse_and_format(const std::string&)`, reading up to 1024 chars of stdin in one shot (e.g. `std::getline` or a bounded read), printing the result to stdout, and returning 0/1 based on success — compile it explicitly linking against your fixed library (correct `-I`/`-L`/`-l` flags), then sanity-test manually with a few valid and malformed strings via `echo ... | ./route_canonicalizer; echo $?` before considering it done.