Set up the Rust project with `cargo new graph_analyzer` in `/home/user/`, add `serde`/`serde_json` (with derive) to Cargo.toml, and parse each JSONL file line-by-line (don't assume `serde_json::from_reader` works on multi-object JSONL — use one `from_str` call per line). Build two lookup structures: a map from node id to its `(type, field)`, and an adjacency map of CITES edges (source -> set of destinations) for O(1) edge lookups — this matters because the "no CITES edge X->Z" check is easy to get wrong if you scan the whole edge list each time instead of using a set/hashmap. The core logic: for every CITES edge X->Y and every CITES edge Y->Z where X, Y, Z are pairwise distinct Author nodes sharing the same `field`, check that no CITES edge exists from X to Z directly; if none exists, record (X,Y,Z) as a gap. Deduplicate if the same triple could arise more than once. Sort results by x, then y, then z (string/lexicographic ordering), serialize as an array of `{"x":...,"y":...,"z":...}` objects, create `/home/user/results/` if needed, and write to `/home/user/results/gaps.json`. Build with `cargo build --release` and run the release binary to actually produce the file — don't leave it only compiled.
