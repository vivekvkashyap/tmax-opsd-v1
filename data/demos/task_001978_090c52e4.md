First trace the cycle by hand: api.proto -> auth.proto -> models.proto -> api.proto, closed because models.proto references `ApiResponse` which lives in api.proto. Break it by extracting the `ApiResponse` message (with its own `syntax = "proto3";` line) into a new base.proto, then repoint models.proto's import from api.proto to base.proto, and have api.proto import base.proto instead of (or in addition to) redefining ApiResponse — end state should have base.proto depend on nothing, models.proto depend only on base.proto, auth.proto depend only on models.proto, and api.proto depend on auth.proto/models.proto/base.proto, with no file importing something that (transitively) imports it back. For toposort.py, parse each .proto file's `import "X.proto";` lines with a regex (skipping any `google/protobuf/...` imports), build an adjacency map of local dependencies only, and implement Kahn's algorithm (in-degree counting with a queue) by hand — at each step where multiple nodes have zero remaining in-degree, pick the alphabetically first filename before continuing, rather than relying on dict/set iteration order which isn't guaranteed to be alphabetical. Write one filename per line to build_order.log and re-run to confirm output is deterministic across repeated runs.
