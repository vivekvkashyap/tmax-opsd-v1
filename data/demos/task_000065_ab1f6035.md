Use OpenCV (`cv2.VideoCapture`) to stream `/app/sensor_feed.mp4` frame-by-frame rather than loading the whole video into memory — call `.read()` in a loop until it returns False, tracking a frame index counter. For each frame, convert to grayscale (`cv2.cvtColor(frame, cv2.COLOR_BGR2GRAY)`), crop the exact center 64x64 region using the frame's width/height midpoints, and compute `numpy.mean()` over that crop as the signal value. Stream `/app/metadata.jsonl` line-by-line (avoid `json.load` on the whole file) building a dict keyed by `frame_index`, then join each frame's computed signal to its timestamp by that key as you go, or after both passes complete. Buffer merged rows and write them with `pyarrow`/`pandas.to_parquet` to `/home/user/telemetry.parquet` with the exact schema `frame_index:int, timestamp:string, signal:float`. Append log lines to `/home/user/pipeline.log` for pipeline start, every 100th frame processed, and completion. Since the metric is MSE against a hidden ground truth, focus on getting the exact crop region and grayscale conversion right — off-by-one center cropping is the most common source of error.
