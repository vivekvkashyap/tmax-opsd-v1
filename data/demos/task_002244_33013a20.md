Treat `/app/legacy_aggregator` as a black box: don't try to disassemble it, instead probe its behavior empirically. Run it on `/home/user/sample.log` and carefully diff its output CSV against small hand-crafted/edited variants of the input (e.g., add a malformed line, an out-of-range reading value, a diagnostic code that doesn't start with OK/WARN, entries spanning an hour boundary) to infer: the regex/field format it extracts, the numeric/string validation constraints it silently applies, how it buckets timestamps into aggregation windows, and the exact column ordering/sorting of the wide-format output — including how it represents missing sensor values in a window. Reimplement this logic in `/home/user/fast_aggregator.py` using vectorized pandas or polars operations (regex extraction with `str.extract`, boolean-mask filtering, `pivot_table` or `groupby().mean().unstack()` for the long-to-wide reshape) rather than a Python per-line loop, since the binary's slowness comes from an artificial per-line delay that any vectorized batch approach will trivially beat. Validate correctness first on the small sample (byte-for-byte CSV diff against the binary's output), then separately confirm speed, since the grader requires both exact-match accuracy and a real measured 20x+ runtime speedup on a much larger hidden log.
