This is a fuzz-equivalence task against a hidden reference implementation, so exact binary-format correctness matters more than approach elegance. First patch the vendored `dirsync` package: look in `/app/dirsync-2.2.5/dirsync/sync.py` for the file-traversal logic and reintroduce inode/path tracking (e.g. a `visited` set keyed by `(st_dev, st_ino)` via `os.stat`, or resolved realpath) so that when the walker encounters a symlink pointing back to an already-visited directory it skips it instead of recursing forever — verify this fix directly by creating a self-referential symlink and calling `dirsync.sync()` on it, confirming no `RecursionError`/hang. Then install the patched package (`pip install -e /app/dirsync-2.2.5` or equivalent) before importing it from `archiver.py`. For the archiver itself, read `backup_config.json` from the target directory, walk the tree safely (reusing your loop-safe traversal), filter by extension and `mtime > min_mtime`, sort matched relative paths alphabetically, and write the binary format exactly as specified using `struct.pack('>H', ...)` for the path length, raw UTF-8 bytes, `struct.pack('>Q', ...)` for mtime, `struct.pack('>I', ...)` for size, then file bytes — pay close attention to big-endian byte order and exact field widths since the verifier does a byte-for-byte diff against an oracle binary across many randomized fuzz trees. Self-test on a directory with a symlink loop before finishing.
