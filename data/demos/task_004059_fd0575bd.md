Work top-down: analyze evidence before touching the fix. Run `strings /home/user/dumps/mem_core.dump | sort | uniq -c | sort -rn | head` (or just grep for repeated tokens) to spot which pattern dominates the dump — look for a recurring keyword/prefix that hints at what's being hoarded (e.g. a marker like "TYPE:ORPHAN" alongside a payload field). Then grep the same pattern across `/home/user/logs/*.log`, sort matches by timestamp, and take the first (earliest) occurrence's TRACE_ID as the leak's origin point — don't assume the first line in the file is chronologically first if multiple log files are involved; merge/sort by timestamp field. For the fix, read `sensor_aggregator.sh` looking for a variable that only ever grows (an array or string being appended to, e.g. `var+=(...)` or `var+="..."`, inside the main read loop with no cap, reset, or scope limiting it to one iteration) — that's the leak; fix it by not persisting the accumulator across iterations (e.g. drop it, or reset/cap it) while keeping the script's visible output behavior unchanged. Write `verify_fix.sh` to start the aggregator in the background against its named pipe, write ~500 synthetic lines into the pipe, then poll `ps -o rss= -p <pid>` and assert (Bash `[[ ]]`) the RSS stays under 10MB, exiting 0/1 accordingly. Finally write the two-line report with the exact variable name and trace ID you discovered.