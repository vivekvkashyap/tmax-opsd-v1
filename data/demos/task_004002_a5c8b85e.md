Read `/home/user/slow_backup.py` first to understand the exact manifest/tarball semantics you must replicate (renaming scheme, JSON shape) before optimizing in C++. Design for speed from the start: use `std::filesystem` to list `/home/user/raw_data/`, compute SHA-256 per file with OpenSSL's EVP API (avoid the deprecated one-shot calls but don't over-engineer), and parallelize hashing/reading across threads (e.g. a thread pool or `std::async`) since 10,000 files in under 2s likely requires concurrent I/O+hashing rather than a serial loop. Build the tar archive with `libarchive`'s streaming write API directly (or manual POSIX tar header writing) rather than shelling out to `tar`, writing each file under its `<sha256>_<original>` name. Use `hiredis` to connect to `127.0.0.1:6379` and issue a single `SET backup:manifest <json>` — build the JSON string manually or with minimal formatting matching the required schema exactly. Compile with `-O2`/`-O3` and link against `-lssl -lcrypto -larchive -lhiredis -lpthread`. Iterate: time your binary directly with `/usr/bin/time -f "%e" ./fast_backup` against the 10,000-file fixture before relying on the `/validate` endpoint, and only trust the endpoint's `{"status":"success"}` as a correctness check, not a speed check.