Start by extracting the passphrase from the audio: use a local transcription tool (`whisper.cpp`, `vosk`, or the `openai-whisper` Python package if available) on `/app/voicemail.wav`, read the transcript text, and pull out the password phrase from context (it will be introduced by a phrase like "the password is ..."). Strip whitespace/punctuation from just that phrase and prepend `BKP_` to build the expected end-of-file signature — do this derivation in code from the CLI `--password` argument (which the tester passes as a spaced phrase) rather than hardcoding the concatenated string, since the verifier calls your script with `--password "..."` and your script must normalize it itself. For signature checking, `mmap` the file or seek to `size - len(signature)` and read only the tail — don't load the whole archive into memory. For tar-slip detection, iterate `tarfile.open(...).getmembers()` without extracting, and reject if any member's `.name` starts with `/`, contains a `..` path segment (after normalization), or is a symlink/hardlink whose `.linkname` resolves (via `os.path.normpath` of the joined path) outside the archive's root. Only if both checks pass, use `os.link()` (a real hard link, not `shutil.copy`) into the output directory. Test against both provided corpora directories and diff your output against what should and shouldn't be linked before trusting your logic on hidden test sets.