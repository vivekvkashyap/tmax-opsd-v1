Treat this as a build-and-run debugging task, not a rewrite. First inspect `/app/go-numcalc-v1.2.3/build.sh` for the actual syntax faults — likely unbalanced quoting and a bash array/strict-mode assignment that isn't valid syntax — fix them minimally so `bash build.sh` runs cleanly under `set -e`/`set -u` if present, and add a `go mod tidy` before `go build` if module deps aren't resolved. Verify by running the script directly and confirming the `numcalc` binary appears in that directory (don't just eyeball the diff). Then check `run.sh`: it launches the binary but sets/exports the wrong AUTH_TOKEN value — find the literal it currently exports and correct it to match what the service source code expects (grep the Go source for where it reads the auth env var to confirm the exact token string and env var name), then `export` it properly before invoking the binary. Start the fixed `run.sh` in the background (e.g. with `nohup ... &` or `run.sh &`) so it survives your session, and confirm both the HTTP health endpoint on 8081 and the TCP protocol on 8080 respond as expected — send a raw TCP AUTH/EVAL sequence with a tool like `nc` to sanity-check before declaring done, rather than assuming the fix works from source inspection alone.