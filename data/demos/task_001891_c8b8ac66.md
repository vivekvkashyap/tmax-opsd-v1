Start by OCR'ing the topology diagram to recover the two required values before touching any config: run `tesseract /app/topology.png stdout` and read off the socket path and timezone it specifies (inspect the raw text carefully, since handwritten-style images can OCR imperfectly — sanity check the socket path looks like a plausible filesystem path and the timezone looks like a valid IANA zone name). Update `/home/user/router.conf`'s upstream socket line to that exact path, and update `server.cpp`'s `strcpy(addr.sun_path, ...)` (or equivalent) to bind the same path, keeping router and server in sync. Separately fix the performance bug: `process_metric` currently takes `std::string data` by value and does a naive O(n²) string-concatenation loop — change the parameter to `const std::string&` and build the padding via `std::string(1000, ' ')` or reserve+append instead of repeated `result = result + " "` in a loop, since that's what actually blows the latency budget. Apply the timezone by exporting `TZ=<recovered-value>` in the environment before launching the compiled server (or setting it in code via `setenv`/`tzset`). Compile with `g++ -O3 server.cpp -o server`, launch it in the background bound to the correct socket, regenerate `502_report.txt` via `grep`/`awk` on `router_error.log`, chmod it to 0400, then run `/home/user/benchmark.sh` and confirm the printed timing meets the required threshold before finishing.
