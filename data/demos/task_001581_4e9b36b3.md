Reverse engineer `/home/user/legacy_server` with static string extraction first (`strings /home/user/legacy_server | grep -i api` or similar) to find the hidden endpoint path embedded in the binary as a literal string — this is far faster and more reliable than dynamic probing/fuzzing of routes. Confirm the finding dynamically: start the server, `curl -i http://127.0.0.1:8888/<discovered_path>` and inspect the raw response headers to read off the cookie name from the `Set-Cookie:` header (use `curl -sI` or `-D -` and grep/cut to extract just the name before the `=`). Then write `audit_server.sh` to, in order: apply `ulimit -v 50000` in the current shell, launch the server in the background (`&`), `sleep` briefly (up to 2s) for it to bind its port, `curl` the discovered endpoint and capture headers, extract the cookie name programmatically (don't hardcode what you already found manually — derive it from the curl output at runtime so the script is self-contained and re-runnable), kill the background server process (e.g. `kill` its PID), and finally write the report to `/home/user/audit_results.txt` matching the exact three-line format given, including the CSP line verbatim. Make the script executable and run it to confirm the output file is produced correctly.