Start with the video: run `ffmpeg -i /app/network_glitch.mp4 -vf fps=1 frame_%03d.png` (or similar) into a scratch dir, then either view the frames or run `tesseract frame_XXX.png stdout` on each to OCR the on-screen text; scan across all extracted frames since the dashboard may show different node states at different timestamps, and record only the ports explicitly labeled STABLE, then sort them numerically ascending — don't guess or hardcode. For the Expect script, use `spawn /app/legacy_router_cli` then sequential `expect "Username: "; send "admin\r"`, `expect "Password: "; send "netops\r"`, `expect "Enter STABLE ports...:"; send "<port1> <port2> <port3>\r"`, ending with `expect eof`, and test it actually produces `/home/user/router.cfg`. For the gateway daemon, use `nc -lk` or a small loop with `socat`/coproc listening on 8888 for HTTP (parse the request line for `/status`, respond with a proper `HTTP/1.1 200 OK` header block plus the router.cfg contents as body) and on 8889 for raw TCP (reply `SYNC_ACK\n` on any input); background it, capture `$!` into the PID file, and confirm both ports respond via manual `curl`/`nc` tests before finishing.
