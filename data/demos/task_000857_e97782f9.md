Use `git bisect` properly rather than manually eyeballing commits: `cd /home/user/math-solver && git bisect start && git bisect bad main && git bisect good v1.0`, then at each step write a small bisect-run test (or run `go test ./...` manually) that fails when `FindRoot(2.0, 1e-9)` returns NaN, marking `git bisect good`/`bad` accordingly until git identifies the exact offending commit — capture its full 40-char hash with `git rev-parse`. Once found, diagnose by reading that commit's diff: look for an intermediate calculation that was narrowed to a lower-precision type (e.g. a cast through `float32`) inside the Newton's-method-style update step, which causes catastrophic cancellation and prevents `math.Abs(delta) < epsilon` from ever being satisfied at tight tolerances. Fix by keeping that intermediate computation in `float64` throughout, without altering `FindRoot`'s signature or algorithm structure. Then edit `solver_test.go` to delete every test function except `TestFindRoot`, run `go test` to confirm it passes, compute `FindRoot(2.0, 1e-9)` and round to exactly 6 decimals, and write the bisected commit hash and rounded root as the two lines of `/home/user/resolution.txt`.