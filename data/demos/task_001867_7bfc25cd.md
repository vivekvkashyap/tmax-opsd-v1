First run OCR on `/app/config.png` (e.g. `tesseract /app/config.png -`) to recover the Target Reference floats, the Max Euclidean Distance, and the Reject Token — don't guess these values, read them off the image output carefully since OCR can misread digits/punctuation. Then write the Go classifier using `encoding/csv` (Go's csv.Reader natively handles RFC 4180 quoted fields with embedded newlines, so don't roll your own line-splitter or you'll break on multiline quoted `notes`). For each row: trim whitespace from `notes` and do a case-sensitive substring check against the Reject Token; separately collect the first 5 valid rows' `value` floats and compute Euclidean distance to the reference vector (sqrt of sum of squared diffs). Reject (exit 1) if the token is found OR distance > threshold OR fewer than 5 valid rows exist; otherwise exit 0. Compile with `go build -o /home/user/classifier /home/user/classifier.go` and test against a few hand-made clean/evil CSVs (including one with an embedded newline inside a quoted field) before trusting it against the hidden corpus.