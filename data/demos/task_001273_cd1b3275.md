First transcribe `/app/intercept.wav` (e.g. with a speech-to-text tool) and parse the spoken "Time X, Sensor value, Sensor value..." sentences into JSONL records matching the schema used by historical_logs.jsonl — don't hardcode any recovered numbers, derive them from what the transcription actually yields. For the processing script, build it as a clear staged pipeline: (1) read all JSONL lines from stdin, flatten each `{time, sensors:{name:val}}` into (sensor, time, value) tuples; (2) group by sensor, and when multiple values share the same (sensor,time) average them before proceeding; (3) sort each sensor's series by time, then resample to a dense 1-second grid from that sensor's own min to max time, filling gaps with exact linear interpolation between the nearest known points on both sides (not nearest-neighbor or forward-fill); (4) emit CSV sorted by sensor then time, with values rounded to 4 decimal places using proper half-even/round-normal semantics matching a reference implementation, since this will be fuzz-tested for bit-exact stdout equivalence against many random inputs — pay special attention to edge cases like a sensor with only one timestamp, or duplicate-timestamp averaging happening before sorting/interpolation.