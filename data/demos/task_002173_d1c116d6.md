Reproduce the precision-loss bug first (compile the original naive version and confirm shuffled vs unshuffled sums actually differ) so you can verify your fix genuinely resolves it, not just assume it does. Replace the naive `sum += val` loop with Kahan summation: maintain a running `sum` and a compensation term `c` (both `double`, not `float`, for extra safety even though inputs are floats), and on each addition do `y = val - c; t = sum + y; c = (t - sum) - y; sum = t` — this is what cancels the rounding error that a plain float accumulator loses on large-magnitude-plus-tiny-value additions. Keep the `printf("%.6f\n", sum)` format unchanged. In `orchestrate.sh`, compile with `gcc`, use `shuf` to write `data_shuffled.txt` from `data.txt`, run the compiled binary on both files, and compare the two stdout strings with a literal string/byte comparison (not floating tolerance) — write `REPRODUCIBLE` only if they're exactly equal, else `NON-REPRODUCIBLE`. Make the script executable and actually run it so `result.log` gets produced, then double check by rerunning with a different shuffle seed.
