Work through each forensic step independently, confirming each finding before using it downstream. Extract text from `/app/screenshot.png` with `tesseract screenshot.png stdout` (or visually inspect it) to recover the backdoor port number mentioned in the terminal text — read the OCR output carefully rather than assuming a value. Use that port to grep `/home/user/netstat.log` for the `ESTABLISHED` line on that local port and pull out the foreign/remote IPv4 address (the field before the port in the Foreign Address column), writing just the bare IP to `/home/user/attacker_ip.txt`. For SSH hardening, copy `/home/user/sshd_config_compromised` to `sshd_config_fixed` and use `sed` to replace existing `PermitRootLogin`/`PasswordAuthentication`/`X11Forwarding`/`Protocol` directives with the required secure values (or append them if a directive is absent) — check each directive is present exactly once afterward, not duplicated. For the scored redaction script, since it's graded on line-by-line accuracy against a large hidden dataset, use `sed`/`grep -E` with precise anchored regexes and word boundaries: match `\b[0-9]{3}-[0-9]{2}-[0-9]{4}\b` for SSNs, `\b[0-9]{4}-[0-9]{4}-[0-9]{4}-[0-9]{4}\b` for dashed cards, and `\b[0-9]{16}\b` for bare 16-digit cards, replacing each match with `[REDACTED]` — test against constructed edge cases (15/17-digit numbers, IDs embedded in longer alphanumeric tokens, timestamps) to make sure you aren't over- or under-matching before relying on the automated accuracy check.