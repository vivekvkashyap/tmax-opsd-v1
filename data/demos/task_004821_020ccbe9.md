Start by extracting the calibration table from `/app/calibration_specs.png` programmatically with `pytesseract` (e.g. convert to grayscale/threshold first if OCR is noisy, then run `image_to_string` and parse rows by whitespace/column splitting) — do not guess the sensor bounds/multipliers by hand; recover them from the OCR text and validate the parsed numbers look sane (e.g. re-run OCR with different `--psm` settings if columns misalign). Then build `/home/user/process_sensors.py` with argparse for `--input/--output/--reject`, and for each file: detect format by extension or content-sniffing, load into a uniform structure, validate `Sensor_ID` against the OCR'd spec dict and reject on unknown sensor, malformed schema (e.g. JSON array instead of object with expected keys), or any reading outside that sensor's (min,max). Only for accepted files: apply the sensor's multiplier to every numeric value, and fill missing readings using linear interpolation ordered by timestamp (pandas' `interpolate()` works well after sorting). Write accepted output as normalized CSV with exactly columns `timestamp,Sensor_ID,value`, same basename with `.csv` extension, and move/copy rejected raw files to the reject directory. Test against both corpora before finishing.