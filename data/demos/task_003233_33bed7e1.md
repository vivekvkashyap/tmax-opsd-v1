Write `/home/user/archiver.cpp` using `<filesystem>` recursive directory iteration over `/home/user/repo`, collecting the absolute path of every entry whose filename ends in `.dat` but is NOT exactly `active.dat` (compare the basename, not a substring match, so you don't accidentally exclude something like `my_active.dat`). Sort the collected paths lexicographically (default `std::string` `<` ordering on the path strings matches standard alphabetical order). Then open each file in binary mode and write its raw bytes to `stdout` using unformatted binary writes (e.g. `std::cout.write` on a `std::ifstream` opened with `std::ios::binary`) — never use `std::cout <<` on the data itself, and never print any log/debug text, since that would corrupt the byte stream. Compile with `g++ -std=c++17`. Run `./archiver | gzip > /home/user/curated_archive.gz`. Finally verify by decompressing (`gunzip -c curated_archive.gz`) and piping through `sha256sum`, writing only the 64-hex-character digest (no filename suffix) to `/home/user/checksum.txt`.