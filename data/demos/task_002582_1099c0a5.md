Build and wire each layer independently before integrating. Compile `/app/extractor.c` with `gcc -O3 -o /app/bin/extractor /app/extractor.c` and run it manually against `/app/ci_diagnostic.wav` first to see its exact key-value output format (e.g. `DURATION=N|TRANSCRIPT=...`) so your parser matches it precisely — split on `|` then on `=` for each field. Write `/app/config/pipeline.json` with the exact specified structure and load/deserialize it in your Python service at startup (e.g. `json.load`), using `max_duration_seconds` from it as the threshold. In the `POST /analyze` handler, invoke `subprocess.run(["/app/bin/extractor", "/app/ci_diagnostic.wav"], capture_output=True, text=True)`, parse its stdout into duration (int) and transcript (str), compare duration against the configured max, and return the appropriate JSON body with correct status codes (200 on success, 400 with the specified error body when duration exceeds the limit). Start this server backgrounded on `127.0.0.1:8080` (e.g. `nohup python3 server.py &`) and confirm with `curl` that `/analyze` responds correctly before writing `/app/benchmark.py`, which should time 50 sequential `POST` requests (e.g. using `time.perf_counter()` around each `requests.post` call), average the latencies in milliseconds, and dump `{"average_latency_ms": <float>}` to `/app/benchmark_results.json`. Leave the server process running when you finish.