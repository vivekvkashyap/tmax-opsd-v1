Decode the video programmatically rather than eyeballing it: open `/app/attack_recording.mp4` with OpenCV (`cv2.VideoCapture`) or ffmpeg frame extraction, iterate frames in order, and classify each as BLUE/WHITE/BLACK/other by checking its mean/dominant channel values against simple thresholds (near-pure blue vs. near-white vs. near-black — don't require an exact pixel match since encoding can introduce compression noise). Find the first BLUE frame, then take the next 256 frames as the bitstream: WHITE -> `1`, BLACK -> `0`. Group those 256 bits into 32 bytes of 8 bits each, convert each byte to a character with `chr(int(byte_str, 2))`, concatenate into the recovered text, split on the single `|` to get KEY and PAYLOAD, and write KEY to `/home/user/compromised_key.txt`. Separately, generate a fresh 16-char random alphanumeric string (`secrets.choice` over letters+digits) for `/home/user/new_key.txt` — this is unrelated to the decoded key. For the CSP, write a single header string combining `default-src 'self'`, `script-src 'self' https://scripts.trusted.internal` (no `unsafe-inline`/`unsafe-eval`), and `object-src 'none'` to `/home/user/strict_csp.txt`.
