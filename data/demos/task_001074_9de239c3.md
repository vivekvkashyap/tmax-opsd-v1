Structure the Go program in the same order the task describes and test each stage against the actual archive before moving to the next. First extract `/home/user/raw_data/dataset.zip` into a fresh directory made with `os.MkdirTemp`, using Go's `archive/zip` package; then walk the tree (`filepath.WalkDir`) looking for `*.tar.gz` files and extract each with `archive/tar` + `compress/gzip` into its own containing directory; walk again to collect all `*.txt` file paths. For the concurrent phase, launch one goroutine per txt file (track completion with a `sync.WaitGroup`), each reading its integer and appending `filename:value\n` to the shared log — but guard the actual write with `syscall.Flock(fd, syscall.LOCK_EX)` before writing and unlock after, on the opened file descriptor, since concurrent unguarded appends can interleave/corrupt partial writes even with O_APPEND. After all goroutines finish (`wg.Wait()`), read the full log back in the main goroutine, split into lines, sort them lexicographically with `sort.Strings`, write to a new temp file in the same directory as the final destination, then `os.Rename` it into place at `/home/user/final_summary.txt` for atomicity. Build and run the program for real, then diff the output against your own manual trace of what the four sensor files should contain, rather than assuming the extraction/concurrency logic worked.