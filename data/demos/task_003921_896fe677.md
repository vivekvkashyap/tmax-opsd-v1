Work through the checks in the order they'll be verified. Create the nested directory with `mkdir -p /home/user/iot_gateway/telemetry/node_01`, then a symlink with `ln -s /home/user/iot_gateway/telemetry/node_01 /home/user/iot_gateway/current_node` (use the absolute target path so `readlink` returns exactly that path). For port forwarding, since 9443 has no listener yet, start something bound there first if needed, then background socat: `socat TCP-LISTEN:8443,fork,reuseaddr TCP:127.0.0.1:9443 &` (or `nohup ... &` so it survives), and confirm with `ss -tln | grep 8443` and `pgrep -x socat` before moving on — these are checked directly. Write diagnostics.sh using `/dev/tcp` for a dependency-free check, e.g. testing `exec 3<>/dev/tcp/127.0.0.1/8443` in a subshell/timeout and appending `OK_FORWARDING` to health.log only on success; `chmod +x` it, run it once to populate health.log, then `chmod 644 /home/user/iot_gateway/health.log` as the very last step (setting it earlier and then re-running the script could change mtime/perms unexpectedly, but 644 still allows the owner to write so order is flexible — just make sure it's 644 at the end).