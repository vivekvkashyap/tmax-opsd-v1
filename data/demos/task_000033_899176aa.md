Start by transcribing `/app/spec_dictation.wav` yourself: install a speech-to-text tool (e.g. `pip install openai-whisper` plus `ffmpeg`, or any available offline ASR) and run it on the audio to get the spoken specification text — do not assume the algorithm, derive it from what you hear. Read the transcript carefully for the exact byte-level operations described (e.g. an XOR mask value and a run-length pair ordering) and note them precisely, since off-by-one errors in operation order are the most common mistake. Implement the decoder in Rust reading raw bytes from stdin and writing to stdout with `std::io::{Read, Write}`, applying the operations in the order the dictation specifies. Validate against the provided `/app/samples/*.bin` -> `*.elf` pairs by piping each sample through your compiled binary and diffing (`cmp`) against the expected `.elf` output before considering it done. Pay special attention to edge cases: zero-length runs, the full byte range 0x00-0xFF, and very short/long inputs, since the final grading fuzzes with random binary blobs across a wide size range. Compile to exactly `/home/user/decompressor` and confirm it takes no arguments.