Install rdflib (`pip install rdflib`) and load the graph with `Graph().parse('/home/user/org_architecture.ttl', format='turtle')`. Write two separate SPARQL queries against the `org:` namespace: one that finds users having both `org:hasRole org:Developer` and `org:hasRole org:Auditor` (SoD violation), and one that joins `?user org:hasRole org:Developer`, `?user org:hasAccess ?a`, `?a org:targetResource org:Production_DB`, `?a org:accessType "WRITE"` (LP violation) — remember blank-node access entries mean you must traverse through the intermediate `?a` node rather than assuming a direct triple. Run each query, collect violating user URIs into a dict keyed by user, appending the appropriate violation tag string ("SoD_Dev_Auditor" or "LP_Dev_Write_Prod") to each user's list — a user can appear in both queries and should accumulate both tags. Sort the violations list alphabetically per user, sort the high_risk_users list alphabetically by user_uri, and set total_violations to the sum of all individual violation tags across all users (not the count of users). Skip users with zero violations entirely, and write the final structure as JSON exactly matching the required schema to /home/user/compliance_audit_report.json.