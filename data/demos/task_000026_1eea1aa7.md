Load the CSV with `pandas.read_csv` (not manual line splitting) so embedded newlines inside quoted fields and Unicode text are handled correctly by the CSV dialect parser. Melt the wide `msg_en/msg_es/msg_zh/msg_ru` columns into long form with `pandas.melt`, deriving the `language` value by stripping the `msg_` prefix from each original column name, then drop rows where `message` is empty string or NaN. Parse `timestamp` with `pd.to_datetime`. For resampling: compute the global min and max timestamp across the WHOLE dataset first (not per-language), floor the min down and the max up to 15-minute boundaries to build one shared set of interval buckets, then for each language group use `.resample('15min')` (or `pd.Grouper`) counting messages per bucket and `.reindex()` against the full shared interval range filling missing buckets with 0 — this is the step naive solutions skip, leaving gaps instead of explicit zero rows. Compute the rolling average per language with `.rolling(window=3, min_periods=1).mean()` on the count series, round to 2 decimals. Concatenate all languages, sort by `language` then `timestamp`, format timestamps as `YYYY-MM-DD HH:MM:SS` strings, and write the four required columns to `/home/user/rolling_metrics.csv`.