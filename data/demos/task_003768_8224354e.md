Sequence this task in dependency order since each step's output feeds the next. Compile `analyzer.cpp` with `g++ -o /home/user/analyzer analyzer.cpp` first — it already contains the logic to check whether `/home/user/cloud_repo` exists and write the report, so don't modify it. Rewrite `start_service.sh` as a proper guard clause: `if [ ! -d /home/user/cloud_repo ]; then tar -xzf /home/user/cloud_backup.tar.gz -C /home/user/ || exit 2; fi; /home/user/analyzer`, then run it once to materialize the repo. For the Git hook, write `.git/hooks/post-commit` as a shell script that appends `HOOK_FIRED` to `/home/user/hook_log.txt` (e.g. `echo HOOK_FIRED >> /home/user/hook_log.txt`) and then invokes `/home/user/analyzer`, and remember hooks need a shebang and `chmod +x` to be executed by Git — a hook file without the execute bit is silently skipped. Finally `cd /home/user/cloud_repo`, create `new_resource.json`, `git add` it, and `git commit -m "Add new resource"` — this fires the hook, which should populate both `hook_log.txt` and `cost_report.txt` as a side effect; check both files afterward to confirm the hook actually ran rather than assuming it did.
