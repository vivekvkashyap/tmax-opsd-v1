Do the intelligence-gathering steps first and treat their outputs as inputs to the code, not things to hardcode from guesses. Find the backend port with `strings /app/legacy_api | grep -i listen` (or `objdump -d` if strings is ambiguous) — look for a "Listening on :<port>" style string. Recover the admin key by running `tesseract /app/sticky_note.png stdout` (try `-psm` variants and manually eyeball/clean the OCR text if it's noisy) and use that recovered string verbatim as the `X-Admin-Key` value in your code — don't retype or "correct" it from memory. For the proxy in Go: use a JWT library (or hand-parse) to base64-decode just the header segment of the incoming token first and check its `alg` claim case-insensitively for `"none"` — reject with 401 before ever attempting signature verification, since that's the classic bypass. For tokens claiming HS256, verify the signature against the secret `proxy-secret-2024` using `crypto/hmac`+`crypto/sha256`; only forward to the backend port from step 1 on a valid signature. Log rejections to `/app/blocked_reqs.log`, creating it with `os.OpenFile(..., 0600)` and writing text containing "Blocked JWT attempt". Run both binaries in the background and manually curl through the proxy with valid/invalid tokens before finishing.
