Build `/home/user/benchmark.py` as a straightforward, careful benchmarking harness rather than guessing at numbers. Import `compute_true` and `compute_approx` from the two given modules (add `/home/user/models` to `sys.path` or import via file path). Generate `x = np.linspace(0.0, 100.0, 1_000_000, dtype=np.float64)` — use `linspace` with `dtype=np.float64` explicitly so both endpoints are inclusive and the count is exact. Run both functions once, check `.shape == x.shape` and `.dtype == np.float64` for each output, and AND those checks into `valid_shape_dtype`. Compute `mse = np.mean((true_out - approx_out)**2)` and `max_abs_error = np.max(np.abs(true_out - approx_out))`, casting both to plain Python `float` for JSON serialization. For timing, call each function 10 times in a loop using `time.perf_counter()` around each call (not wall-clock of the whole loop divided naively — but a simple average of 10 individual timings is fine), convert to milliseconds, and average. Compute `speedup = true_time_ms / approx_time_ms`. Write exactly the six specified keys to `/home/user/experiment_log.json` via `json.dump`, then actually execute the script so the file exists on disk.
