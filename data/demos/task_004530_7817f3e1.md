Start by running `tesseract /app/policy_note.png stdout` to OCR the sticky note and recover the ALLOWED_MOUNT_PREFIX string exactly (watch for OCR noise like trailing whitespace or misread characters — sanity-check it against expected path conventions). Scaffold the Rust project with `cargo new archiver_cleanup` at `/home/user/archiver_cleanup`, then implement a streaming line-by-line parser (BufReader) that tracks state across `[ARCHIVE_RECORD]` ... `[END_RECORD]` blocks: require ID, MOUNT, SIZE lines in the exact order, reject any stray/garbage lines outside a record, and reject any record left unclosed at EOF. For each MOUNT value, check it strictly starts with the extracted prefix AND contains no `/../` component or trailing `/..` — do this via path-segment splitting rather than naive substring matching to avoid false positives (e.g. a mount path that merely shares the prefix's characters as a longer sibling directory name should NOT count as matching unless it's the prefix itself or the prefix followed by `/`). Treat the file as Evil the moment any single violation is found (short-circuit is fine, but still exit 1). Build with `cargo build --release` and verify the binary path matches exactly, then self-test against a few hand-crafted clean/evil log files before trusting it against the hidden corpora.