Design the C program around two data structures built from `schema.txt`: a set of valid node type names, and a set of valid directed triples `(SourceType, RelType, TargetType)` — parse the `Nodes:` line by splitting on commas, and each `Edges:` line by splitting on `->` and trimming whitespace, since naive parsing often leaves stray spaces that break exact string comparisons. Then stream `dataset.csv` line by line (do not sort or reorder — output order must match input order exactly), split each line on commas into the 5 fields, and validate: both types must be in the node set, and the exact triple `(SourceType, RelType, TargetType)` must be in the edge set (validate direction — don't just check the relationship name matches regardless of type positions). Write valid rows immediately to `queries.cypher` in the exact `MERGE (a:Type {id: 'ID'})-[:Rel]->(b:Type {id: 'ID'});` format, and invalid rows immediately (raw, unmodified) to `invalid_rows.log`, both preserving CSV order via sequential single-pass writes. Compile with `gcc -o /home/user/graph_parser /home/user/graph_parser.c`, run it with the dataset and schema paths as arguments, and diff your two output files against your own manual trace of which rows should pass/fail before considering it done.