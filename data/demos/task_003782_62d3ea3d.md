Build this as a bash pipeline in clear stages, testing each against the actual log file before chaining. First, write a validation regex that enforces the full line shape strictly, e.g. with `grep -E` or `awk` matching `^[0-9]{4}-[0-9]{2}-[0-9]{2} [0-9]{2}:[0-9]{2}:[0-9]{2},[A-Za-z0-9_]+,[A-Z]+,(SUCCESS|FAIL)$` — anchor both ends so extra/garbled fields fail. Separate the file into matching and non-matching lines (e.g. `grep -Ev` for invalid, `grep -E` for valid) and write invalid ones verbatim to `/home/user/invalid_lines.log`; count them and if `> 5`, print "QUALITY GATE FAILED", exit 2, and skip bucketing entirely — check this gate before doing any aggregation work. For bucketing: from the valid lines, filter to the `2024-05-01 00:00:00`–`05:59:59` window, extract the hour, count `SUCCESS` lines per hour (e.g. with `awk` field extraction and an associative array), then explicitly loop hours 00–05 and emit a row for each — even ones with zero matches — rather than relying on `awk`'s associative array to naturally produce all keys, since gap-filling requires you to force-emit the missing hours as `0`. Remember `chmod +x` on the script itself.
