Set up the Go module first (`go mod init` inside `/home/user/ml_prep/`) so `go run analyzer.go` works standalone. Parse `/home/user/data.csv` with `encoding/csv`. For tokenization, lowercase the text, then replace every rune that is not `a-z` or `0-9` with a space using a regex like `[^a-z0-9]+` (compile once), split on whitespace, and drop empty tokens — be careful that consecutive non-alphanumeric characters collapse correctly and don't produce spurious empty tokens. Track token_count per row and whether the literal token `"ai"` appears (exact token match, not substring, so "email" doesn't count). Compute the sample Pearson correlation between token_count and engagement_score over the full dataset using the N-1 formula (mean-center both vectors, sum of products over sqrt of sum of squares, or implement the standard covariance/stddev ratio with ddof=1) — do this manually or via gonum's `stat.Correlation`. Then filter to rows with token_count > 5, and on that subset compute sample covariance (ddof=1, e.g. `stat.Covariance` or manual `sum((x-xbar)(y-ybar))/(n-1)`) between the ai-presence indicator and engagement_score. Marshal the three fields to `/home/user/stats.json` with `encoding/json`, then actually run the program to produce the file.
