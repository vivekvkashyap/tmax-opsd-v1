Fix, wrap, then measure — in that order. In processor.cpp, locate the branch that checks for "POISON_PILL" in file content and currently calls `std::abort()`; replace the abort with logging `"WARNING: Poison pill skipped in " + filename` to std::cerr and then `continue`-ing the file loop (skip writing to out/, but still consider removing/moving the poisoned input so the daemon doesn't loop on it forever) — the rest of the processing loop for other files must be untouched. Recompile with `g++ -std=c++17` to the exact binary path required. For monitor.sh, keep it simple and robust: launch the processor in the background, capture `$!` to the pid file, then loop with `sleep 2`, and each iteration check `kill -0 $(cat pidfile) 2>/dev/null` (process alive?) and check the log's most recent HEARTBEAT timestamp isn't stale — if either check fails, relaunch the daemon and overwrite the pid file. For summary.sh, count "SUCCESS: Processed" lines in the log with `grep -c` (or `awk '/SUCCESS: Processed/{n++} END{print n+0}'`) and make sure it prints only the bare integer, nothing else. Run the monitor in the background, let the daemon process the input directory fully, then capture summary.sh's output into summary_result.txt.
