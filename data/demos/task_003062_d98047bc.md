For step 1, write extract.c to parse the WAV header to locate the PCM data chunk, then read 16-bit little-endian samples sequentially, taking the LSB of each sample as one bit of the hidden message (8 samples build one byte, in the bit order the spec describes), assembling bytes into characters and stopping at a null terminator; write the recovered text to recovered_traffic.log and check the byte-order/bit-order details carefully since a reversed bit order will produce garbage. For step 2, grep the recovered log for the `Cookie:` header line associated with the admin/privileged request and copy that exact header string to malicious_cookie.txt — don't fabricate a cookie, extract the literal one present in the decoded traffic. For step 3, write sandbox.c that forks, and in the child installs a seccomp-bpf filter (via `libseccomp` or raw `SECCOMP_SET_MODE_FILTER` with BPF instructions) allowlisting only read/write/exit/exit_group/brk/mmap/munmap/execve with SECCOMP_RET_KILL as default, then execve's /app/backdoor_probe; in the parent, capture the child's stdout (pipe + dup2) to sandbox_output.txt. Compile both with gcc and test end-to-end; measure your extraction against the required similarity threshold by comparing recovered_traffic.log content structurally to what a correct LSB decode should produce.