Work this as three independent sub-problems, verifying each in isolation before wiring them together. (1) Architecture: change `/app/api.py` to push the token via `redis.rpush('auth_tasks', token)` instead of spawning a subprocess with `--token`; rewrite `/app/worker.py` as a loop that calls `redis.blpop('auth_tasks', ...)` and processes whatever it pops — restart nginx/flask/redis/worker and confirm with `ps aux | grep worker.py` that no token ever appears as an argument. (2) SSH: edit `/app/sshd_config` to set `PasswordAuthentication no` and `PubkeyAuthentication yes`, generate the keypair with `ssh-keygen -t ed25519 -f /home/user/.ssh/admin_key -N ""`, append the `.pub` to the configured `AuthorizedKeysFile`, then restart sshd and test both a password login (must fail) and a key login (must succeed) before moving on. (3) Log audit: grep/regex over `/app/historical_syslogs.txt` for JWT-shaped (base64 header.payload.signature) and `SEC-<hex>` patterns, then import and call `is_valid_token()` from `/app/token_validator.py` on every candidate to drop corrupted/expired matches — write only tokens that pass validation, one per line, to `/app/compromised_tokens.txt`. Precision matters as much as recall since the grader scores F1.
