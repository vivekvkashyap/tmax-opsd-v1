First extract the two hyperparameters from `/app/data/trace_params.wav` by actually transcribing it — run it through a speech-to-text tool (e.g. `whisper-cli`/`openai-whisper`, or any available offline ASR) and read the transcript to get the stated `burn_in` sample count and `thinning` factor; don't guess plausible-sounding numbers, confirm them from the transcription output. Then write `/home/user/trace_processor.go` reading `C` and `L` from the first stdin line, then `C` subsequent lines of `L` integers each. Launch one goroutine per chain (use a `sync.WaitGroup` and a results slice/channel indexed by chain number, not a shared counter, to avoid races), where each goroutine slices off the first `burn_in` samples, then walks the remainder with a stride of `thinning` starting at offset 0 of the post-burn-in slice (i.e. indices `burn_in`, `burn_in+thinning`, `burn_in+2*thinning`, ... in the original chain), summing the kept values as it goes. After `wg.Wait()`, sum all per-chain totals into one grand total and print only that integer plus a newline — no other stdout output, since the harness diffs byte-for-byte against an oracle across many fuzzed random chain inputs including edge cases like `L` barely larger than `burn_in`.
