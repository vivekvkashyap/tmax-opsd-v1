Extract the exact rule from the audio rather than assuming typical values — run `/usr/local/bin/transcribe /app/compliance_memo.wav` and read the transcript literally to pull out the bucket size (seconds) and the maximum allowable sum; don't hardcode numbers you haven't confirmed from the transcription output. Implement bucketing in awk/bash as `bucket = timestamp / bucket_size` using integer division (careful: shell/awk integer division must truncate toward zero the same way `timestamp // N` does for positive timestamps), accumulate `measurement_value` per bucket, then compare each bucket's sum against the limit using a strict greater-than check (the spec says "strictly exceeds", so a sum exactly equal to the limit must pass) — this boundary condition is the most common place naive scripts get the pass/fail criterion backwards. Exit 1 as soon as any bucket violates, otherwise exit 0 after scanning the whole file. Make the script executable and test it against both `/app/clean/` and `/app/evil/` corpora directly, checking `$?` for every file, since 100% accuracy on both is required.