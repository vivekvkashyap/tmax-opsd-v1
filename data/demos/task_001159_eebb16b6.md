Break this into three clean C++ passes over the data rather than one tangled loop. First, parse network.csv and group rows by `source`, sort each source's rows by `time_seq` ascending, and compute a running cumulative sum of `amount` per source as you iterate in that order — this is the per-partition window-function behavior, so make sure you reset the running sum for each new source and don't accumulate globally. Second, for each transaction, decide inclusion by checking whether the source's cumulative sum *at that transaction* (i.e., including the current row's amount, per the SQL window semantics) is strictly greater than 150.0; if so, add an undirected edge between source and target (use a set/map keyed by an unordered pair, or just push both directions). Third, run a union-find or BFS/DFS over only the kept edges to get connected components, track each component's node set, and pick the largest by node count — if there's a tie in size, compare each tied component's lexicographically smallest node and keep the component whose minimum node is smallest. Output the node count on line 1 and the sorted (lexicographic) comma-joined node list on line 2 to component_result.txt. Compile with the exact g++ flags given and run the binary to produce the file, then manually trace through a couple of edges by hand to confirm your cumulative-sum and component logic match before finishing.