Build the script in the exact stated order and validate each stage's intermediate output before moving on. For sanitization, use a regex that matches `\u` NOT followed by exactly 4 hex digits (e.g. `\\u(?![0-9a-fA-F]{4})`) and strips only the `\u` itself, leaving any following characters (including partial hex digits) in place — test this against each raw line manually before trusting `json.loads()` to succeed. Configure `logging` to a file handler pointed at `/home/user/pipeline.log` with start/end log lines. Use `pandas.melt` (or manual reshape) to go from wide (`term,es,fr,de`) to long (`term,lang,translation`), then `merge` with `categories.csv` on `term` (inner join drops unmatched terms). Normalize translations with `.str.lower().str.strip()`. Sort by `category`, then `term`, then `lang`, and compute the rolling average of string length per `category` group with `groupby('category')['translation'].apply(lambda s: s.str.len().rolling(2, min_periods=1).mean())` — verify the sort order is applied before the rolling computation, since rolling is order-dependent. Write the final CSV with headers, no index.
