Build this in layers and validate each independently before wiring them together. First, treat the reference parsing binary strictly as a black-box differential-testing tool, never as something to copy or bundle: write a fuzz harness that generates many random Cisco/Juniper-like config strings (mixing whitespace, `.:-_{}`, newlines, and edge cases like empty strings, no delimiters, and repeated keys), feeds each through the reference binary via stdin, and captures its exact stdout. Use those input/output pairs to reverse-engineer the transformation rules empirically (how whitespace is handled, how tokens/names get normalized or abbreviated, how output is ordered) rather than guessing — then implement equivalent Python logic of your own and re-run the same fuzz corpus against it, diffing byte-for-byte until you reach full equivalence across the whole corpus. Second, make `setup.sh` idempotent: check-before-create for directories, back up any existing `/app/webhook.py` with a timestamped filename before patching it to authenticate to Redis with the configured password and bind Flask to 0.0.0.0:8080, then launch both Flask (via waitress/gunicorn) and your daemon in the background, capturing PIDs to the specified files. Third, have the daemon block-pop from `config_queue`, run it through your validated parser, and append to `processed.log` — test end-to-end with a real POST before declaring done.
