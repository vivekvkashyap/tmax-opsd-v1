This is a strict size-and-correctness target (archive under 25KB, CSV must have exactly 5001 lines), so design the pipeline for both correctness and maximal compression, not just "make it work." Extract `/home/user/incoming/spool.tar` (recursively handle any nested tar/dirs inside it), then parse each `=== RECORD START === ... === RECORD END ===` block with a line-oriented tool (awk/sed/perl) pulling only the `Timestamp:` value, `Severity:` value, and the first line following `Message:` — discard the bloat-padding entirely, since that's the source of the size problem, not the compression algorithm. Write these three fields as a strict CSV (`Time,Level,Error` header, proper quoting if any field could contain a comma) to `summary.csv`. Compress with maximum settings, e.g. `tar -cf - summary.csv | xz -9e > /home/user/processed/final_archive.tar.xz` (or `tar --xz -cf` with `XZ_OPT=-9e`), since default compression levels alone likely won't hit the size threshold. Make the whole thing one executable, idempotent bash script and run it once to produce the final artifact, verifying line count and archive size yourself before finishing.
