Start by transcribing `/app/dataset_notes.wav` yourself — use an installed/installable speech-to-text tool (e.g. `whisper`, `vosk`, or any CLI STT you can get running) rather than guessing the format; listen carefully for the exact magic bytes, field byte-widths/endianness, and — critically — the exact output prefix strings and their trailing formatting, since the fuzz verifier does byte-for-byte comparison against a reference implementation. Once you have the spec, implement the Go parser: read the magic, then loop reading a little-endian uint16 path-length, the path bytes, a little-endian uint32 data-size, and skip that many data bytes (do not extract to disk — this is a dry-run parser). For each entry, sanitize the path using `filepath.Join(targetDir, entryPath)` followed by `filepath.Clean`, then check with `strings.HasPrefix` (plus a separator boundary check to avoid prefix-collision false negatives, e.g. `/mnt/data2` matching prefix `/mnt/data`) whether the cleaned path still lives under the cleaned target dir; print the transcript's two exact prefixes (`SAFE:`/`WARN:`) with the corresponding path per the audio's rules. Test manually against hand-crafted archives with both safe and `../`-escaping paths before relying on the fuzz suite.
