First get the app importable: `ctypes.CDLL("libmatheval.so")` fails because the loader won't search the CWD by default — load it with an explicit path, e.g. `ctypes.CDLL(os.path.abspath("libmatheval.so"))` or `ctypes.CDLL("./libmatheval.so")`, from the same directory the .so was built in. Set `argtypes`/`restype` correctly (`c_char_p` in, `c_double` out) before calling. For the encoding bug: the C side expects the ASCII bytes of the expression re-encoded as a hex string (each byte -> two hex chars), not the raw UTF-8 bytes themselves — convert with something like `req.expression.encode().hex()` and pass that resulting hex string's bytes to the C function, not `req.expression.encode('utf-8')` directly. Verify by hand with a tiny script that loads the .so and calls the function on a known hex string before wiring up the endpoint. For the property test, use `hypothesis.strategies` to generate lists of small non-negative integers, join them with `+` to build valid expressions, POST each via `TestClient`, and assert `status_code == 200`; run `pytest -v test_api.py > test_results.log 2>&1` and confirm it reports a pass.
