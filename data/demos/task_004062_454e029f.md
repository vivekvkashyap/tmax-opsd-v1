Build process.sh as a strict awk pipeline, since embedded-newline records and gap-filling both need careful field counting, not naive line-by-line CSV parsing. Step 1: filter with awk by counting commas per raw physical line (`gsub(/,/,",",$0)` trick or `n=split($0,a,",")` and checking `n==4` fields / 3 commas) — this naturally drops the header, drops fragment lines from multi-line quoted comments, and drops any line whose comma count differs, exactly as specified (don't try to reassemble the multi-line records; the task wants them dropped). Step 2: in the same or a second awk pass, track a running "last valid temperature" variable and substitute it whenever field 3 is empty. Step 3: extract the hour prefix (first 13 chars of the timestamp, or split on `T` and take HH) as the group key and accumulate sum/count per hour with an associative array, computing the mean at the end. Step 4: after all hourly means are known, do a second pass (or buffer results) to compute global min/max and apply `(mean-min)/(max-min)`, defaulting to 0.0000 when max==min. Use `printf "%.4f"` for both numeric columns, sort by hour, and write to /home/user/processed_hourly.csv with no header. Test against the given raw_sensors.csv before trusting the script on new data.