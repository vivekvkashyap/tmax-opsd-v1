Parse `backup.dat` as fixed pairs of lines (`PATH:` then `B64DATA:`) — read sequentially with a loop (e.g. `while read -r line`) rather than assuming blank-line separators, since there are none. For each entry, classify strictly per the spec: malicious only if the path starts with `/` OR contains the literal substring `../` — be careful with edge cases like a filename containing `..` but not `../` (e.g. `safe_but_tricky..txt`), which must NOT be flagged, so test the exact substring match rather than a looser regex. Malicious paths get appended verbatim to quarantine.log; safe paths get `mkdir -p` for their directory under `/home/user/safe_restore/` then `base64 -d` the content into the target file. After the loop, `cd /home/user/safe_restore && find . -type f | sed 's|^\./||' | sort | xargs sha256sum` (or equivalent) to get relative paths sorted alphabetically, and redirect that into restore_checksums.txt. Finally, write "EXTRACTION COMPLETE" to a temp file and `mv` it atomically into place at restore_status.txt rather than writing the target file directly.
