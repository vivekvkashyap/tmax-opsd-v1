Transcribe `/app/secret_prior.wav` (e.g. convert with `ffmpeg` then run an STT tool) to recover the single spoken threshold integer $M$ — do not guess it. Then write `/home/user/mc_estimator.cpp` implementing exactly the specified deterministic PRNG: read all doubles from stdin into a vector $X$ of length $N$, initialize `uint64_t seed = 12345`, and for each of the 10,000 outer resamples run an inner loop of $N$ draws where you update `seed = (1103515245 * seed + 12345) & 0x7FFFFFFF` BEFORE each draw and index with `seed % N` — get the loop order and the "update-seed-before-index" step exactly right, since fuzz/bit-exact comparison against a hidden oracle will fail on any deviation (e.g., updating seed after indexing, or reseeding per resample). Sum drawn values per resample, divide by N for the mean, compare strictly greater than $M$, count, and print only the final integer count plus newline. Compile with `g++` and place the binary at `/home/user/mc_estimator`. Test with small hand-computed inputs before relying on the fuzzer, and use `uint64_t` arithmetic exactly as specified to avoid overflow/precision mismatches with the oracle.
