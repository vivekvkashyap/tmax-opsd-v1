Work top-down: first extract `legacy_dump.tar.gz` with `tar xzf`, then write a small loop/script that repeatedly scans `/app/unpacked/` for anything that looks like an archive (check both extension and, more reliably, `file`/magic bytes, since nested archives may not carry a `.tar`/`.zip` extension consistently) and extracts it in place with `tar` or `unzip` as appropriate, deleting the archive file afterward; repeat until no archive files remain — a single extraction pass will miss deeper nesting levels, so loop until a scan finds nothing left to extract. For renaming, use `find /app/unpacked -type f` combined with a shell loop that lowercases only the extension (e.g. split on the last `.` and reconstruct) rather than the whole filename. Build `/app/audio_links/` with `find /app/unpacked -iname '*.wav' -exec ln -s {} /app/audio_links/ \;` (careful with name collisions). For the passphrase, install `sox`/`ffmpeg` plus a speech-to-text tool (e.g. `whisper`, or convert and use any available offline ASR) to transcribe `/app/secret_voicemail.wav` — do not guess the phrase. Build the Rust service (actix-web or axum) with `/stats` counting files under `unpacked/` and symlinks under `audio_links/` live via `fs::read_dir`, and `/unlock` normalizing both the stored transcript and the request payload (lowercase, strip punctuation) before comparing, returning 200/403 accordingly; run it in the background (e.g. `&` or `nohup`) bound to `127.0.0.1:8080` before finishing.
