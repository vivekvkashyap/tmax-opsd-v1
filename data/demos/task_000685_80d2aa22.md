This is a two-phase task; don't skip recovering the hidden parameters before writing the processor. Phase 1: transcribe `/app/audio/config_memo.wav` using an available speech-to-text tool (e.g. `whisper` or similar ASR CLI) — listen for both the integer minute offset and the exact Unicode place-name spoken (it may be given partly in Spanish); write down what you actually hear rather than guessing, since these are the two parameters your script depends on. Phase 2: in `/home/user/stream_processor.py`, read `/app/data/sensor_catalog.csv` with explicit `encoding='utf-16le'` (a naive open() will mangle the Unicode text), parse stdin line-by-line as JSON, apply the recovered offset (in minutes) to each ISO-8601 timestamp, then floor/truncate to the start of its 15-minute bucket. Join readings to the catalog on `sensor_id`, filter to rows whose `location_name` exactly equals the Unicode string recovered in Phase 1 (compare after normalization, e.g. Python's `unicodedata.normalize`, to avoid encoding mismatches), then group by (bucket, sensor_id) computing count/mean/max (rounded to 2 decimals). Emit a JSON array sorted by bucket then sensor_id to stdout, and test end-to-end by piping sample JSON lines through the script before finishing.
