First fix the vendored library: open `/app/fuzzywuzzy/fuzzywuzzy/fuzz.py`, find `def ratio(s1, s2):`, and remove the injected early `return 0` line so the function falls through to its real Levenshtein-ratio implementation — test with a quick `python3 -c "from fuzzywuzzy import fuzz; print(fuzz.ratio('a','a'))"` to confirm it no longer returns 0, then `pip install -e /app/fuzzywuzzy` (or add it to your path) so your pipeline imports the fixed version. Next, load `/home/user/model_predictions.csv`, filter to rows where `predicted_match == 1`, and compute `fuzz.ratio(item_A, item_B)` for each — you don't need the full cross-product from data_A/data_B for the CI, only the predicted-match pairs matter for step 5. For the bootstrap: set `numpy.random.seed(42)` exactly once before resampling, draw exactly 10,000 resamples with replacement (`np.random.choice(scores, size=len(scores), replace=True)`) of the score array in that same order, take the mean of each resample, then report the 2.5th and 97.5th percentiles of the 10,000 resampled means via `np.percentile`. Write `lower,upper` (comma-separated, no extra text) to `/home/user/ci_output.txt`. Getting the seed timing and resample count exactly right is what makes your interval match the reference.
