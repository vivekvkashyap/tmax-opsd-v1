Don't guess the validation rule — black-box test `/app/sensor_parser` systematically first. Construct many small single-record test files varying one thing at a time (different marker lines present/absent, different orderings, repeated lines, lines appearing before/after each other), run the binary on each, and record its exit code. Form a hypothesis about which line(s) or line-ordering make a record "evil" versus "clean", then design further test files specifically to falsify or confirm that hypothesis (e.g., does a later line override an earlier one? does position within the record matter?) before committing to the logic in your C reimplementation. Only after you're confident the rule reproduces the binary's behavior on your own test corpus should you hardcode it into `--verify` mode. For `--build` mode, recursively find `.log.gz` files, decompress each (zlib or piping to `zcat`), split on `BEGIN_RECORD`/`END_RECORD` boundaries, apply your verified filter, and process files concurrently with threads or `fork()`. Because multiple workers append to one output file, wrap each full record's write in an `flock`/`fcntl(F_SETLKW)` exclusive lock so records are never interleaved, even under heavy concurrency.
