Build the pipeline as a sequence of discrete, testable awk/grep passes rather than one giant one-liner, since the leakage rule is the trickiest part to get right. Steps: (1) strip the header, then filter rows with empty val_x/val_y, val_y equal to "NA"/"NULL", or val_x < 0, keeping the header separately for the final output. (2) From the cleaned rows, select only dataset_split=="train" and compute population mean and population standard deviation (divide by N, not N-1) for val_x and val_y using awk accumulators — do this in a dedicated pass so test rows never influence these statistics. (3) In a final pass over ALL cleaned rows (train and test), apply z=(x-mean)/stddev using the train-derived mean/stddev captured from step 2 (pass them in as awk variables via `-v`), formatting to exactly 4 decimal places with `printf "%.4f"`. (4) Sort the output rows numerically by record_id (`sort -t, -k1,1n`), prepend the required header, and write to `/home/user/processed_data.csv`. Double-check sign conventions and rounding (e.g. `-0.0000` should normalize to `0.0000`) since exact-match grading is strict.