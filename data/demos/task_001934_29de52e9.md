For Phase 1, don't bisect manually — write a small test script that checks out isn't needed since `git bisect run` handles checkouts: the test script should run `calc_stats.sh` against a dataset that reveals the regression (a case where the "refactored" mean calculation diverges from the correct one) and exit 0 if the output matches the known-good value, 1 otherwise. Then run `git bisect start`, mark current `HEAD` (or a late commit) as bad and `v1.0` as good, then `git bisect run <script>`; capture the identified commit's SHA via `git rev-parse` into the target file, then `git bisect reset`. For Phase 2, the race is a classic read-modify-write on `global_sum.txt` across backgrounded subshells — fix it by wrapping the read-update-write critical section with `flock` on a dedicated lock file descriptor (e.g. `exec 200>lockfile; flock 200; ...; flock -u 200`) inside each backgrounded job, not by removing parallelism. Verify the fixed script's output against an independently computed sum of all chunk files. For Phase 3, write a loop that generates small random-integer files (including edge cases like all-identical values) and runs `calc_stats.sh` on each, checking the exit status / stderr for a division-by-zero error; save the first crashing input verbatim.
