Write a recursive CTE in `/home/user/analyze_org.py` that starts from the employee named "Evelyn" (look up her id by name, don't hardcode it) and recursively joins `employees` on `manager_id` to pull in all direct and indirect subordinates in one query, e.g. `WITH RECURSIVE sub(id) AS (SELECT id FROM employees WHERE name='Evelyn' UNION ALL SELECT e.id FROM employees e JOIN sub ON e.manager_id = sub.id) SELECT ...` joined against `salaries` for the total. Print only the integer sum to stdout with nothing else on that line. Then build the nested JSON tree in Python: after fetching all employees/salaries into memory, recursively construct each node as `{name, salary, reports}` by finding children whose `manager_id` equals the current node's id, sorting each `reports` list alphabetically by name, and giving leaf nodes an empty list rather than omitting the key. Serialize with `json.dump(..., indent=2)` to `/home/user/evelyn_org.json`. Run the script yourself afterward and diff the printed total and the JSON structure against what you'd compute by hand from the table contents to catch any subtree or sibling-ordering mistakes.
