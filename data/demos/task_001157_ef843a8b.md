Build this in three independently testable pieces. First, append the two `export VAR=value` lines to the end of .bashrc (don't touch existing content) and confirm with `grep` that they're present in the exported form expected by a check like `export METRICS_HOST=...`. Second, scaffold the Rust binary with `cargo new`, then implement main.rs by reading env vars with `std::env::var`, parsing `/proc/loadavg` (first whitespace-separated field is the 1-minute load average) and `/proc/meminfo` (grep the `MemAvailable:` line and parse its numeric kB value), attempting `TcpStream::connect_timeout` against the METRICS_HOST address with a 2-second Duration and treating any Err as unreachable (do not let it panic/exit before writing the log), then serialize the JSON object by hand or with a minimal approach (e.g. `format!` with proper quoting, or add `serde_json` if you prefer) and append it as one line to capacity_log.jsonl. Build with `cargo build --release` and manually run the binary once with the env vars set to confirm a well-formed JSON line appears. Third, write planner.cron with a `*/5 * * * *` line pointing at the release binary's absolute path and load it via `crontab planner.cron`, then confirm with `crontab -l`.