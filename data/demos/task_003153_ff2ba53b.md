Set up first: `python3 -m venv /home/user/venv && source /home/user/venv/bin/activate && pip install pandas scipy matplotlib`, then run everything through that venv's interpreter. In `/home/user/generate_report.py`, the crash comes from `plt.show()` requiring a display; fix it by setting a non-interactive backend before any pyplot import/use — either `matplotlib.use("Agg")` right after importing matplotlib (before `import matplotlib.pyplot as plt`) or export `MPLBACKEND=Agg` in the environment — and replace `plt.show()` with `plt.savefig('/home/user/artifacts/correlation_plot.png')`, creating `/home/user/artifacts` with `os.makedirs(..., exist_ok=True)` first. Fill in the TODOs using `scipy.stats.pearsonr(df['learning_rate'], df['val_loss'])` for correlation, and `scipy.stats.ttest_ind(df[df.architecture=='Arch_A'].val_loss, df[df.architecture=='Arch_B'].val_loss, equal_var=True)` for the t-test — read the actual column/value names straight from `/home/user/experiments.csv` rather than assuming. Round every float with `round(x, 4)` before dumping into `/home/user/report.json` with exactly the four required keys. Run the script end-to-end and confirm both the PNG and JSON exist with sane, non-zero values.