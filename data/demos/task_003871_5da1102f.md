Treat this as a classic "avoid the cross join" graph problem. Load nodes.jsonl and edges.jsonl once, building: (1) an adjacency set (map[string]map[string]bool or sorted slice) for KNOWS edges, inserting both directions since KNOWS must be treated as undirected; (2) a map from product -> list of buyer user IDs, built from BOUGHT edges. For each product, instead of comparing every pair of buyers (O(N^2)), only enumerate pairs where you can confirm they know each other in O(1) via the KNOWS adjacency set — e.g., iterate buyers of a product, and for each buyer look up its KNOWS-neighbors, checking (via a small hash set membership test) whether that neighbor also bought the same product; this keeps total work proportional to edges touched, not to squared buyer counts. Emit each triangle once with User1 < User2 lexicographically (sort the pair before printing, and dedupe with a seen-set keyed by (product,u1,u2)). Sort final rows by Product, then User1, then User2 before writing the CSV header and rows. Validate output against `/app/ref_engine` on the given dataset for exact correctness first, then benchmark with repeated timed runs (e.g. `time` or Go's own timing) to confirm the required speedup — profile and eliminate any accidental O(N^2) hot loop (like scanning a slice instead of a hash set) if the speedup target isn't met.