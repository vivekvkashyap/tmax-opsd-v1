Treat this as a pipeline of independent, verifiable stages driven by a bash script — compile once, then loop. Compile with `gcc -o /home/user/bin/seq_density /home/user/src/seq_density.c -lm` (the math library link is required for the entropy log/pow calls). To extract the first FASTA sequence, read the file and concatenate all lines after the first `>` header up to (but not including) the next `>` line or EOF, stripping newlines so the tool gets one continuous string on stdin. For each candidate window size (5, 10, 20, 50), pipe that sequence into the compiled tool, capture its per-line float outputs, and compute the population variance yourself (e.g. with `awk` or a small Python/bc snippet) — don't eyeball it. Pick the smallest window size whose variance is strictly below the threshold, scanning the list in ascending order and stopping at the first hit. Then rerun the tool with that window on every sequence in the FASTA file (same extraction technique per record), average each sequence's output line, and compare against `/home/user/data/reference.tsv` by absolute difference to find the largest-diverging SeqID. Write both findings to the report file in the exact required two-line format.
