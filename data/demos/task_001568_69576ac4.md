Work the evidence sources in order rather than guessing at the code. First inspect the pcap with `tshark`/`tcpdump -A` or `strings traffic.pcap` to find the HTTP POST body sent to `/verify` — note that the interesting detail is a value containing a space, which is the trigger condition. Second, run `strings crash.core | grep -i key` (or search for common env-var naming patterns) to recover the API key text embedded in the dump — do not hardcode a guessed key. Third, probe `/app/bin/auth_oracle` directly from the shell with varying argument counts/spacing (e.g. call it with 1, 2, and 3 args) to observe how it behaves, since it's a stripped binary you should treat as a black box, not disassemble. Once you see it needs exactly two arguments, look at how `server.py` invokes it — check for `subprocess.Popen(f"... {var} ...", shell=True)`-style string interpolation, which silently splits on spaces inside `var` and breaks argument count. Fix it by switching to a list-based subprocess call (or by using `shlex.quote`) so values containing spaces stay as one argument. Restart the service bound to `127.0.0.1:8888` in the background and verify with curl using the recovered key and a payload containing a space before finishing.