Work through this as three separate forensic checks, then assemble the JSON. (1) Integrity: run `sha256sum -c /home/user/app_manifest.sha256` from the right directory (or compute `sha256sum` per file listed in the manifest and diff against recorded hashes) to get the exact list of files that FAILED — those are your `compromised_files`, sorted. (2) Attribution: grep `/home/user/logs/access.log` for path-traversal sequences (`../`) in the request line, and cross-reference which IP's traversal requests target the filenames you found compromised in step 1 — that's your `attacker_ip`, not just any IP with `../` in its log line. (3) CSP: open each compromised file and look for injected `<script src=...>` tags or `fetch`/XHR calls to external hosts; extract just the hostname (strip scheme and path) from each URL, dedupe and sort them into `extracted_malicious_domains`. The `recommended_csp` field is fixed text regardless of what you find — write it exactly as specified in the task. Build `/home/user/report.json` matching the exact schema, with both arrays sorted.
