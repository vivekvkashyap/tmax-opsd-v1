Write straightforward C: open and read both CSVs line by line with `fgets`/`fscanf`, skip the header line of each, and parse `user_id,age` and `user_id,hours_active` into two parallel arrays (or a small fixed-size struct array), matching by row order as the task allows. Compute Pearson's r directly from the definition: means of x (age) and y (hours_active), then `r = sum((x_i-mean_x)*(y_i-mean_y)) / sqrt(sum((x_i-mean_x)^2) * sum((y_i-mean_y)^2))` — implement this as a single pass computing the three sums after a first pass for the means (or use the sum-of-products shortcut formula, being careful about floating-point precision). Print/write the result with `fprintf` using `"Correlation: %.4f\n"` so it rounds to exactly 4 decimal places, matching the required output format exactly (no extra whitespace or lines). Compile with `gcc -o compute_corr compute_corr.c -lm` since you'll use `sqrt`, then run the binary so `/home/user/correlation_result.txt` is actually produced — don't just leave the source compiled but unexecuted.