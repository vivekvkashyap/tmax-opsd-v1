Build and start each service independently, verifying it works in isolation before wiring the pipeline together. Set up the venv and install flask/redis, then confirm redis-server is actually running and reachable on 127.0.0.1:6379 (`redis-cli ping`) before writing app code against it. Write `ingest.py` to check the `Authorization` header equals exactly `Bearer rel-mgr-token-992` (return 401 otherwise), parse the JSON body, and `RPUSH`/`LPUSH` the raw JSON string onto the Redis list `telemetry_queue`, returning `{"status": "queued"}` with 200. Write `worker.py` as a loop using a blocking pop (`BLPOP`) on `telemetry_queue`, parse each JSON payload, format it as `DEVICE:<device_id>,METRIC:<metric>` (match Python's default float-to-string formatting for the metric value, since the verifier checks an exact string), and append with a trailing newline to `processed_telemetry.log`. Start redis, then the worker, then the API, each backgrounded (e.g. `nohup ... &`) so they persist after your shell session ends. Before finishing, manually POST a test payload with curl and confirm the log line appears and the auth rejection path returns 401 for a bad token — the automated test replicates this exact flow.
