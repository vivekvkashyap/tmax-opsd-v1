Write `/home/user/profile_regression.py` in two clearly separated parts. Part 1: define the Lotka-Volterra RHS function with parameters `alpha=1.5, beta=1.0, delta=1.0, gamma=3.0`, initial state `[10.0, 5.0]`, and call `scipy.integrate.solve_ivp` over `t_span=(0,10)` with `t_eval=np.linspace(0, 10, 200)` and the default RK45 method (do not override the method) — this must exactly match how the baseline was generated, so avoid changing solver tolerances or t_eval spacing. Load `/home/user/baseline.csv` with pandas and compute `np.max(np.abs(your_x - baseline_x))`; since the method matches, this should come out essentially zero — a large error means your solve_ivp call configuration diverges from spec, not that the baseline is wrong. Part 2: loop `i` from 0 to 199, set `alpha = 1.5 + 0.1*np.sin(i)` each iteration, resolve the ODE the same way, and record `max(x)` per run into a 200-length array. Call `np.random.seed(42)` immediately before the bootstrap loop, then do exactly 1000 resamples of size 200 with replacement (`np.random.choice(..., size=200, replace=True)`), taking the mean of each resample, and use `np.percentile` at 2.5 and 97.5 for the CI bounds. Write `max_abs_error_x`, `ci_lower`, `ci_upper` to `/home/user/report.json` and run the script to confirm it executes cleanly.
