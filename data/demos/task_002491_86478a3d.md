Fix the pipeline before adding the monitor. Use `sed -i` (or `awk`) to patch the socket path string in `/home/user/services/producer.py` from `/tmp/app.sock` to `/home/user/services/sockets/app.sock` — a targeted in-place substitution, not a full rewrite. Start `consumer.py` first (it binds the socket) then `producer.py`, both backgrounded with `&` or `nohup`, and tail `processing.log` briefly to confirm messages are actually flowing before moving on. For `/home/user/services/monitor.py`, don't poll-and-reread the whole file — open it, seek to the end, and loop reading new lines as they're appended (classic `tail -f` pattern: read, sleep briefly if no new data, repeat), matching each line against the exact error string. On each match, compute directory size by summing `os.path.getsize()` over every file in `/home/user/services/data_store/` (use `os.walk` or `os.scandir`, and be careful to sum only regular files, not directory entries themselves), then append one JSON line per event to `alerts.json` with the exact key names and an integer byte count. Run `monitor.py` in the background too, and manually trigger a wait cycle to confirm at least one alert line gets written with a plausible size before finishing.
