Work through this as a staged migration, verifying each stage before moving to the next. First fix the Python 2 idioms: replace `cmp(a, b)` in `utils.py` with a manual three-way comparison (e.g. `(a > b) - (a < b)` or explicit if/elif on the parsed version tuples), replace any `xrange` with `range`, and replace `dict.has_key(k)` calls with `k in dict`. Second, regenerate the gRPC stubs — run `python -m grpc_tools.protoc -I./proto --python_out=./src --grpc_python_out=./src ./proto/vm_service.proto` from the package root so `vm_service_pb2.py` and `vm_service_pb2_grpc.py` land in `src/` where `server.py`/`benchmark.py` import them. Third, run `pytest tests/test_vm.py` and iterate until it's green without touching the test file itself. Fourth, profile the VM's hot loop in `vm.py`: the bug is that `program_string.split('\n')` runs on every iteration of the execution `while` loop — hoist that split (and any other per-line parsing/tokenizing) outside the loop so it happens once before execution begins, while preserving identical control-flow semantics (jump targets, register behavior). Finally run `benchmark.py`, which starts the server and times an RPC call, and confirm `benchmark_result.txt` reports well under the 0.5s threshold — if it's still slow, the split-in-loop fix is the most likely remaining culprit.