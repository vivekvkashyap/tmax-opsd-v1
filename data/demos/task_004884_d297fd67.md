Start by reading the existing `/app/waf/waf.py`, `/app/proxy/main.go`, and `/app/backend/server.py` to see the actual bugs rather than rewriting from scratch. Port the WAF's HTTP server from `BaseHTTPServer` to `http.server`/`http.server.BaseHTTPRequestHandler`, and fix every `bytes`/`str` mismatch introduced by Python 3's stricter typing (e.g. `base64.b64decode()` returns `bytes` in Py3, so `.encode('hex')` — a Py2-only codec — must become `binascii.hexlify(...).decode()` or similar; `json.loads` needs a `str`, so decode bytes first). Don't hand-craft detection rules from memory — inspect real samples in `/app/corpus/clean/` and `/app/corpus/evil/` (they may be base64/hex-encoded, per the WAF's own decoding logic) to see what patterns (SQLi, XSS, deserialization markers) actually appear, and iterate your regex/logic against the corpus until 100% of evil returns 403 and 100% of clean returns 200 via direct `curl -X POST` tests to port 5000. Only after the WAF is solid, verify the Go proxy on 8000 and backend on 9000 chain correctly end-to-end (`curl` through 8000). Finally, `/home/user/classify.py` should import your WAF's actual classification function (not duplicate its logic) and iterate both corpus directories, writing `{filename: "clean"|"evil"}` to `/home/user/classification.json`. Launch all three services in the background before finishing.