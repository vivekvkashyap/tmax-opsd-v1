Build this as a clear sequential ETL and check each stage's intermediate output before moving on. Load the CSV with pandas, drop rows where `timestamp` or `element_id` is null/empty (`dropna` on those two columns specifically), then drop exact duplicate rows across all three columns (`drop_duplicates()`), and normalize `timestamp` to a date-only value (`pd.to_datetime(...).dt.date`) for day-bucketing. Group by `(date, element_id)` to get daily counts, then pivot/reindex so every `element_id` has an explicit row (count 0) for every day in the date range spanning the data — a naive `rolling()` on a sparse groupby will silently skip missing days instead of counting them as 0, which changes the 3-day average. Compute the 3-day rolling average per `element_id` over that dense daily series with `min_periods=1`, then for the target date `2023-10-15` sort by (rolling_avg descending, element_id ascending) and take the top 3, mapping each `element_id` through `mapping.json` to get `loc_key`. Read `request_template.txt` verbatim and do a literal string replace of the `{KEYS}` placeholder with the comma-joined ordered loc_keys, preserving the rest of the template exactly, then write the result to `/home/user/translation_requests.md`.