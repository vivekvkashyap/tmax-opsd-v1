Work this as a layered diagnosis: first use dynamic tracing to find which file crashes, then use static/fuzz analysis to understand why, then trace the specific values. Run `strace -f -e trace=openat,read /home/user/math-worker run /home/user/data/` (or trace all syscalls and grep for file opens) and watch the sequence of file opens up to the point the process exits/crashes — since the worker processes files in sorted-name order, the crash file is whichever file's open/read is the last one before the trace ends or an exit/signal appears; write just its basename to crash_file.txt. Read calc.go carefully to spot the missing bounds check on the `*` operator branch (it indexes data[i+1] without checking i+1 < len(data)). Write `func FuzzCalculate(f *testing.F) { ... f.Fuzz(func(t *testing.T, data []byte) { Calculate(data) }) }` seeding with corpus examples including trailing operators, and run with `go test -fuzz=FuzzCalculate` to confirm it finds a panic (index out of range) — this validates your understanding of the bug independent of the specific crash file. Finally, to get the accumulator's exact pre-panic value, manually trace `Calculate` byte-by-byte over the identified crashing file's actual contents (or add a temporary print/recover in a small driver) rather than guessing arithmetic in your head.