Solve this in two independent halves and cross-check them. First derive the analytical posterior by hand: with a Beta(α,β) prior and Binomial(N,k) likelihood, the posterior is Beta(α+k, β+N-k) by conjugacy — compute its mean as a plain fraction, don't approximate. Second, implement Metropolis-Hastings from scratch: maintain a current state p, at each iteration propose p' ~ Normal(p, sigma=0.05), reject immediately (keep old state, still count the iteration) if p' is outside (0,1), otherwise compute the acceptance ratio using the unnormalized posterior density (prior_pdf(p') * likelihood(p') vs same for p, e.g. via log-probabilities to avoid underflow with N=100) and accept with probability min(1, ratio) using a uniform random draw. Run exactly 110,000 iterations, discard the first 10,000 as burn-in, and average the remaining 100,000 samples. A common bug is looping fewer times than specified or discarding burn-in incorrectly (off-by-one on indices) — count carefully. Before writing the JSON, assert the two means agree within 0.005; if they don't, your MH implementation (proposal, acceptance test, or burn-in slicing) has a bug, not the assertion.
