Part 1: extract frames from `/app/experiment_video.mp4` at its native 10fps (e.g. via `ffmpeg` piping raw frames, or OpenCV/PIL in Python), crop each frame to the top-left 50x50 region (x:0-49, y:0-49), convert to grayscale and average pixel intensity, and write `time,video_val` rows where time = frame_index / fps starting at 0.0 — verify the frame count and fps match expectations before trusting the signal. Part 2: build `fusion_pipeline.py` as a strict, order-sensitive pipeline since a fuzz harness will diff your output byte-for-byte against a hidden oracle across thousands of random inputs, so implement each step literally as specified rather than "close enough": outer-join on rounded/aligned `time`, sort ascending; fill `telemetry_val` with ffill then bfill for leading NaNs; fill `video_val` with linear interpolation only; then for outlier rejection compute a *centered* rolling median (window size is odd, so center index has window//2 on each side) but explicitly skip replacement for the first/last window//2 rows where a full centered window doesn't exist — do not let pandas' `min_periods` silently do a partial-window median there, since the spec requires leaving those raw. Count every actual replacement for `outliers_replaced`. Finally compute Pearson correlation on the cleaned columns, and write both output files with exact formatting (4-decimal CSV floats; JSON with only the two named keys, correlation rounded to 4 decimals).