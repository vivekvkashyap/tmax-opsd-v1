First fix the naming mismatch: grep the `Makefile` for `DEST_DIR` and rename it to `ARCHIVE_DIR` everywhere it's defined/referenced so it lines up with what `archive.sh` expects (check `archive.sh` for how it reads the env var, e.g. `${ARCHIVE_DIR}`). The real performance fix is replacing the `while IFS= read -r line; do ...; done < "$file"` filtering loop — that pattern is slow because it forks/processes per-line — with a single-pass stream filter: `grep -v '\[DEBUG\]' "$file" > "$tmp_file"` (or an equivalent `awk '!/\[DEBUG\]/'` / `sed`), which processes the whole file in one native pass and will drop a 50MB file's runtime from tens of seconds to well under a second. Keep the atomic-write pattern intact: write to a temp file (ideally via `mktemp` in the same directory as the destination) and only `mv` it into place after the filter completes, so readers never see a partially-written archive. After fixing both files, run the archival via the corrected `make` target (passing `ARCHIVE_DIR=/data/archive` etc.) against `/data/logs/`, confirming `.log` files older than 7 days end up filtered, renamed to `.archive`, and moved into `/data/archive/`. Benchmark your `archive.sh` directly against a large synthetic log file to confirm it's well under the time threshold before considering the task done.