Before writing any pipeline logic, determine which CSV is nodes and which is edges empirically: load both `graph_x.csv` and `graph_y.csv` and inspect column dtypes/cardinality — the nodes file will have a column of strings drawn from a small role vocabulary (containing values like Engineer/Manager/Director) plus a department string, while the edges file will have two integer ID-like columns whose values are a subset of the node IDs, plus a large integer that looks like an epoch timestamp; also determine the edge columns' order (which integer column is sender vs receiver vs timestamp) by checking magnitude (epoch timestamps are much larger than IDs) and by cross-referencing which column's values map cleanly to node IDs in both positions. Once the schema is confirmed, build a node ID→Role lookup, then for every edge where the sender's role is Engineer and receiver's role is Manager, search for a second edge where sender=that same Manager ID and receiver's role is Director, satisfying `0 < ts2 - ts1 <= 3600`; count how many such valid 2-hop paths each Manager mediates. Sort managers by count descending, breaking ties by ascending Manager ID, and write only the top 3 as `ManagerID,PathCount` lines (no header) to `/home/user/influencers.csv`. Validate your join logic on a few paths by hand before trusting the aggregate counts.