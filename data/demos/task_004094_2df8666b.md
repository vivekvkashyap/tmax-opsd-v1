Work purely in Python's `sqlite3` module: first inspect the schema (`PRAGMA table_info`) and row counts to understand the data before writing logic. Find stale edges with a single query using `NOT EXISTS` (or a `LEFT JOIN ... WHERE nodes.id IS NULL`) checked against source and target independently, unioned together — a common mistake is only checking one side of the edge. For the orphaned-Person subset, join edges to nodes on `source` (must be label='Person') and left-join/anti-join on `target` against nodes to find WORKS_FOR edges whose target is missing; extract distinct source ids. Sort descending alphabetically in SQL (`ORDER BY source DESC`) or Python, then slice/LIMIT to exactly 5 for the pagination requirement — be careful that "first 5 after sorting descending" is not the same as "last 5 ascending" if there are duplicates or if you sort the wrong list. Write one id per line to `/home/user/stale_persons.txt` with no trailing extra blank line issues. Finally, run a DELETE against `edges` using the same stale-detection condition (both source and target, not just WORKS_FOR), commit the connection, and close it so changes persist to disk.