Read `validate_csv.sh` closely: it prints an empty line for every valid 3-column row instead of the row itself — the fix is a single-token change from printing an empty string to printing the actual record (`$0`), while still using field-count `NF==3` as the schema gate and preserving the header line. For the Bayesian filter, precompute all conditional probabilities from `/app/data/train.csv` once (priors `P(bot)`/`P(human)` from class counts, and per-feature likelihoods for `action` and `geo` with Laplace smoothing — add 1 to each feature-value count and add the number of distinct values for that feature to the denominator) rather than recomputing per row; store them as awk/bash lookup tables or associative arrays. Pipe the input through the fixed `validate_csv.sh` first, then for each surviving row compute the (unnormalized) posterior products for bot vs human using the precomputed conditionals and reject rows where the bot product strictly exceeds the human product. Keep everything in `awk`/`bash` per the constraints — no Python/R. Write the header plus surviving rows to the output path.
