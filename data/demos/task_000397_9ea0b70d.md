Split this into independent phases and verify each before moving on. For the video, extract frames (ffmpeg) and OCR/visually inspect the terminal text shown to see exactly how the attacker base64-encodes the "none" algorithm JWT header and what the injected XSS payload looks like — use what you observe to build your detection patterns; don't guess the pattern. For log parsing, write `/home/user/analyze.sh` using grep/awk: an IP is malicious only if it appears in a request line matching an XSS pattern (`<script>`, `javascript:`, `onerror=`, and their URL-encoded forms like `%3Cscript%3E`) AND appears in a request line carrying an `Authorization: Bearer` token whose header segment, when base64url-decoded, is `{"alg":"none"...}` — decode the actual token segment rather than string-matching a fixed substring, since encodings can vary. Output the sorted-unique intersection of those two IP sets. For the forged JWT, build header/payload JSON, base64url-encode each (no padding, `+`→`-`, `/`→`_`), join with `.` and a trailing empty signature (`header.payload.`). For the TLS chain, concatenate/verify ca.crt + intermediate.crt with `openssl verify -CAfile` before using them with curl's `--cacert`/`--capath`. Save curl's response body verbatim to flag.txt — the graded metric is F1 on the IP list plus an exact substring match in the flag.
