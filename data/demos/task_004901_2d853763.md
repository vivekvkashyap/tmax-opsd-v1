Diagnose before rewriting: read `get_next_midnight` in `/home/user/process_pcap_logs.py` closely — the bug is a naive `dt.day + 1` target that never matches a valid `.day` value when the next month has fewer days than expected (e.g. Jan 31 -> "day 32" never occurs, causing the `while current.day != target_day` loop to spin, and it's silently capped at 1000 iterations producing a fake "hang" error instead of correct output). Replace the manual hour-stepping loop entirely with a direct, month/year-aware calculation: take the date part of the timestamp, add one day using `datetime.timedelta(days=1)` (which correctly rolls over month/year boundaries), then set the time to `00:00:00` and keep the original UTC offset via `.replace(hour=0, minute=0, second=0, microsecond=0)`. Preserve timezone-aware parsing (`fromisoformat` after replacing trailing `Z` with `+00:00`) and format via `.isoformat()` so the output matches `YYYY-MM-DDT00:00:00+00:00`. After fixing, run the script unmodified against `/home/user/pcap_logs.csv` (don't touch the CSV) and confirm `/home/user/binned_stats.json` is produced with entries grouped/counted by each record's correctly-computed next-midnight key, paying special attention to the Jan-31 and Feb-29 rows since those are exactly the cases that used to hang.