Work in stages: first make it compile, then make it correct, then make it deterministic. (1) Read the compiler errors carefully — missing includes (`<thread>`, `<mutex>`) and a missing `-pthread`/`-lpthread` linker flag are common causes of build failure in multithreaded C++; add both the includes and the link flag in the Makefile. (2) Once it builds, reason about what shared state each worker thread touches; a global accumulator written from multiple threads without synchronization is a classic data race that produces intermittent wrong answers under contention — protect it with a `std::mutex`/lock_guard around the update, or better, have each thread accumulate into a local variable and merge results after `join()` to avoid lock contention entirely. (3) Do NOT guess the exact transformation logic from the description alone — treat the oracle binary as a black box and empirically determine its behavior (feed it small, controlled inputs like single integers, positives/negatives, evens/odds, and observe outputs) so your fixed implementation matches its semantics exactly, since your grade depends on byte-for-byte output agreement across many random fuzzed inputs.