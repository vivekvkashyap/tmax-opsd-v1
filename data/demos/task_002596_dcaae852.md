Write the corpus file first exactly as specified, then build the script as a clear pipeline: (1) a tokenize function usable on both a line and the target phrase — lowercase with `tr '[:upper:]' '[:lower:]'`, strip non a-z/space chars with `tr -cd 'a-z '` or `sed 's/[^a-z ]//g'`, then collapse repeated spaces with `tr -s ' '`; (2) tokenize the target phrase once into a set of *unique* words (dedupe, e.g. via `tr ' ' '\n' | sort -u`); (3) for each original line, tokenize it and count how many of the target's unique words appear anywhere in that line's token list (a word-presence check, not a count of occurrences) — this is the similarity score; (4) pair each score with its original line number and line text, then sort by score descending while using a stable sort keyed secondarily on line number to preserve ties in original order (`sort -t$'\t' -k1,1nr -k2,2n` on a numbered/scored intermediate file works well, or just track order carefully since bash `sort` alone isn't stable across all scores); (5) take the top 3 and print the *original* unmodified lines, not the tokenized versions.
