First compile with `gcc sim_model.c -o /home/user/sim_model -lm` (a math-library call is likely present, so watch for a missing `-lm` linker error) and confirm it fails with exit code 1 against the given `config.txt` run from its directory. For delta debugging, don't hand-guess — automate ddmin: write a script that, given the current line set, tries removing each half (then progressively smaller chunks) and re-runs `./sim_model` with the trimmed file written as `config.txt` in a scratch working directory, keeping a reduction only if the program still exits 1; repeat until no single line can be removed without losing the failure, and write the surviving minimal set to `min_config.txt`. Because config lines are order-independent `KEY=value` pairs, treat them as a set for the reduction, not fixed offsets. Once you know which parameters actually matter (likely revealed by which lines survive minimization), copy the original 10-line `config.txt` to `fixed_config.txt` and edit only the `BETA=` line to `0.5`, leaving every other line unchanged, then rerun to confirm exit code 0.