Install scikit-learn/pandas/numpy, then load `/home/user/data.csv` and build embeddings with `TfidfVectorizer(lowercase=True, stop_words='english', max_features=1000)` — matching these exact parameters matters because the verifier recomputes a reference matrix with the same settings and compares results. For the benchmark, fit `NearestNeighbors(n_neighbors=10, metric='cosine', algorithm='brute')` once on the full TF-IDF matrix, then time (with `time.perf_counter()`) only the loop that queries each of the first 100 documents individually (one `.kneighbors()` call per document, not batched) — sum or measure total elapsed wall-clock seconds and write that float to `/home/user/benchmark_time.txt`. For duplicate detection, compute the full pairwise cosine similarity matrix (or use NearestNeighbors radius search) and collect every pair (i, j) with i<j whose similarity is >= 0.85; map matrix indices back to the `doc_id` column values, order each pair as (min_id, max_id), deduplicate, then sort rows by id1 then id2 before writing `/home/user/duplicates.csv` with headers `id1,id2`. Avoid off-by-one errors comparing indices vs doc_id values.
