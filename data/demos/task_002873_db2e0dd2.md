Start by reproducing the build failure with `go build ./...` inside `/home/user/sec-eval` to see the circular-import error between `parser` and `eval`. Resolve it by merging both packages' source files into a single new package, e.g. `/home/user/sec-eval/engine/engine.go`, keeping all existing function names and logic (including the panic-on-"PANIC" behavior and the exit-code semantics in `main.go`) unchanged — only the package structure changes, not behavior, since exit codes must stay identical. Update `main.go`'s imports and calls accordingly, then confirm `go build` succeeds. For cross-compilation, use `GOOS`/`GOARCH` env vars: `GOOS=linux GOARCH=amd64 go build -o /home/user/dist/sec-linux` and `GOOS=windows GOARCH=amd64 go build -o /home/user/dist/sec-win.exe`, creating `/home/user/dist` first. For the fuzz test, `pip install hypothesis`, then write a script using `@given(strategies.text())` that runs `/home/user/dist/sec-linux <generated-string>` via `subprocess.run` and asserts the return code is never `2` (panic). Run the script with `pytest` or as a plain script (hypothesis works standalone via `@settings`/direct call too); only if it completes without an AssertionError should you write `TESTS_PASSED` to `/home/user/test_result.log`.
