Build `run_pipeline.py` as a sequence of discrete, verifiable OS operations rather than one opaque block. Use `os.makedirs(..., exist_ok=True)` for `/home/user/deploy/` and `/home/user/deploy/logs/`, then `shutil.copy` the legacy script into `/home/user/deploy/active_batch.py`. Set exact permissions with `os.chmod(path, 0o500)` — note this must happen after the copy, since copy preserves/creates its own default mode. For the ACL, shell out via `subprocess.run(["setfacl", ...])` with a default ACL on the logs directory so new files inherit `rw-` for owner and no access for group/other (default ACLs use the `d:` prefix, e.g. `setfacl -d -m u::rw-,g::---,o::---`), and confirm the flag syntax matches what `getfacl` will show afterward. When launching the legacy script, build a copy of `os.environ`, inject `TZ=Pacific/Fiji` and `MIGRATION_LOCALE=C.UTF-8` into that copy only, and pass it via `subprocess.run([...], env=env_copy)` — never call `os.environ[...] =` directly, since that would leak into the parent process. After running your pipeline script once, confirm `/home/user/deploy/logs/execution.log` was actually written, which tells you the legacy script's own internal checks (path, perms, env vars) all passed.