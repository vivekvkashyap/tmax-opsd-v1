Discover the schema first, don't assume table/column names: `sqlite3 /home/user/research_data.sqlite ".schema"` and redirect that output verbatim to `/home/user/schema_info.txt`. For the citation depth, write a recursive CTE that starts from the citing paper and recursively follows citation edges, tracking path depth, guarding against cycles (e.g. carry a visited-path list or cap depth) since citation graphs can have cross-links; take `MAX(depth)` over all reachable paths from paper 10, output just that integer to longest_chain.txt. For the ranking task, build it in layered SQL/CTEs: first compute citations-received per paper (count of citing rows per cited paper), then join to paper_authors and papers to attribute each paper's citation total to its author(s) and its publication year, then `SUM` per (year, author), then use `RANK() OVER (PARTITION BY year ORDER BY total_citations DESC, author_name ASC)` as the window function, and finally filter to `rank <= 2`. Double-check the tie-break is alphabetical on name when citation totals match — this is easy to get backwards. Export the final query to CSV with the exact header `Year,AuthorName,TotalCitations,Rank` using `sqlite3 -header -csv` or Python's csv module.