Handle the three parts independently, then wire them together. First, recursively unpack `/home/user/docs_archive.tar`: extract it, then repeatedly detect and extract any `.zip`/`.tar.gz` found inside the extracted tree (a loop or recursive function checking file extensions/magic bytes) until no nested archives remain, then `find` all `.txt` files and move/copy them flat into `/home/user/organized_docs/` (watch for filename collisions across nested archives — none expected here, but don't overwrite silently). Second, transcribe `/app/dictation.wav` with an ASR tool to recover the spoken codename, and remember the TCP protocol compares it in uppercase. Third, write `/home/user/serve_docs.py` running two servers concurrently (e.g. one in a background thread, one in the main thread, or both via `threading`): an HTTP server via `http.server.ThreadingHTTPServer` with `SimpleHTTPRequestHandler` rooted at `organized_docs` bound to 127.0.0.1:8080, and a raw TCP socket server on 127.0.0.1:9090 that reads newline-terminated commands per connection, checks `AUTH <word>` against the uppercased transcribed codename (respond `OK\n`/`ERROR\n` and close on mismatch), and after successful auth responds to `COUNT\n` with `FILES: <n>\n` where n is a live count of `.txt` files in `organized_docs`. Test both ports manually (curl and a raw socket client) before finishing.