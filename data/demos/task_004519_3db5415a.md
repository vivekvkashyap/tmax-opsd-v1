Build this as a clear multi-stage awk/sort pipeline inside the bash script, testing each stage's intermediate output before chaining them. Stage 1 (union+filter): concatenate the three CSVs skipping repeated headers (`tail -n +2` on all but the first, or `awk 'FNR==1 && NR!=1{next}'`), then filter with awk requiring `amount > 0` and `tx_id` matching `^[A-Za-z0-9]+$` (reject anything with hyphens/underscores/spaces). Stage 2 (dedup): sort the filtered rows by `tx_id`, then by `timestamp` descending, then by `retry_flag` descending (`sort -t, -k1,1 -k4,4nr -k5,5nr`), and keep only the first row per `tx_id` with an awk that tracks the last-seen key (`$1!=prev{print; prev=$1}`). Stage 3 (rolling sum): sort the deduped rows by `user_id` ascending then `timestamp` ascending, then use awk to walk the stream maintaining a per-`user_id` "previous amount" associative array, emitting `amount + prev[user_id]` (treating missing prev as 0) and updating prev after each row. Write the final header plus rows to `/home/user/output/clean_rolling_tx.csv`, `mkdir -p` the output dir first, and `chmod +x` the script.
