First recover the reference phrase yourself: install a transcription tool (e.g. `pip install openai-whisper` or use `SpeechRecognition`/`pocketsphinx`) and run it against `/app/reference_audio.wav`, then sanity-check the output text — don't hardcode a guessed phrase, use whatever the tool actually transcribes as your query string. Then build `/home/user/filter_records.py` with `argparse` for `--input-dir`/`--output-dir`, creating the output dir if missing. For each `.txt` file, fit a fresh `TfidfVectorizer(stop_words='english')` on exactly two documents — the reference query and that single file's text — then compute `cosine_similarity` between their two TF-IDF vectors (not against a corpus-wide fit, since the task requires per-document fitting). Copy the file to the output directory only when similarity is strictly greater than 0.05. Test your script manually against a couple of clearly on-topic and off-topic text snippets to confirm the threshold behaves as expected before relying on the automated adversarial corpus check, since TfidfVectorizer's stop-word list and per-pair fitting can behave differently than a corpus-wide fit.
