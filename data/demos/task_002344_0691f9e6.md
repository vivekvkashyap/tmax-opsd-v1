Build the Go program in discrete steps and check each against the sample data by hand before trusting the final output. First parse `/home/user/raw_data.jsonl` line by line with `encoding/json` into a generic map or struct with optional fields, skipping any line that fails to unmarshal or is missing `id`/`description` as strings — don't panic on bad lines, just `continue`. For the embedding, lowercase the description and use `strings.Count` for each of the five fixed keywords in the given order to build a `[5]float64` per document. For cosine similarity, compute dot product and vector magnitudes (`sqrt(sum of squares)`) directly, guarding division by zero (magnitude 0 => similarity 0) as specified. Iterate all documents other than the target, track the best score plus first-seen tie-break (compare using `>`, not `>=`, so an earlier document keeps priority on ties), and round the final score to exactly 3 decimals (e.g. `math.Round(score*1000)/1000`) before writing the JSON with `target_id`, `similar_id`, `score`. Run it with `go run /home/user/embed.go doc4` and confirm `/home/user/result.json` was written before finishing.