Work the three subsystems independently and verify each before wiring them together. For `filter.c`/Makefile: try `make` first, read the compiler error (likely a missing `#include <math.h>` for a math function, plus the linker needing `-lm`), fix the include and add `-lm` to the Makefile's link line, then confirm `./filter` builds and runs on a sample file. For the Python 2->3 port: rewrite `BaseHTTPServer` imports to `http.server`, convert `print` statements to function calls, and fix any byte/str handling in request/response bodies. Wire the HTTP handler for `GET /api/v1/transcribe` to: run `./filter /app/data/message.wav out.wav` as a subprocess, then transcribe `out.wav` (install `SpeechRecognition` + a backend, or `openai-whisper`, via pip) — don't hardcode or guess the transcript text, actually run recognition on the produced audio and lowercase the result. Return exactly `{"version": "v2", "data": {"transcript": <text>, "status": "success"}}` as JSON with correct Content-Type. Start the server in the background (e.g. `nohup python3 server.py > /home/user/server.log 2>&1 &`) bound to 127.0.0.1:8888, then curl the endpoint yourself to confirm the JSON shape and that the transcript is non-empty before considering it done.
