This is a two-part forensics task; treat them independently. For the bisection, first write a small wrapper that runs `timeout 2 python main.py; echo $?` (or checks wall-clock time) so a hang can be detected automatically, then use `git bisect start`, mark `v2.0` bad and `v1.0` good, and `git bisect run <your-wrapper-script>` to let Git binary-search the ~200 commits for you rather than checking commits by hand — this is far faster and less error-prone than manual inspection. Record the exact 40-char hash Git reports as the first bad commit, then `git bisect reset`. For the memory dump, don't try to load `/home/user/core.dmp` as structured data — treat it as raw bytes and grep for the known token pattern, e.g. `strings /home/user/core.dmp | grep -oE 'SECRET_TOKEN-[A-Za-z0-9]{16}'` (or `grep -a -oE` directly on the binary file if `strings` output ordering is inconvenient), which will surface the exact token since it's embedded as ASCII text somewhere in the dump. Write both recovered values into `/home/user/investigation.json` in the exact required schema.