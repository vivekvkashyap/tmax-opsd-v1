Treat this as a stateful stream-parsing problem best solved in `awk`, called from `process.sh`. Read stdin line by line while tracking an "inside a quoted/broken record" flag and a buffered set of lines belonging to the current logical row: for each incoming physical line, count the double-quote characters; if you're not currently inside an open quote and the line has an odd number of quotes, that logical row is broken — start dropping this line and all subsequent lines until you see another line whose quote count makes the running total even again (i.e. the quote closes), then drop that closing line too and resume normal processing. Only lines that are complete standalone rows (even quote count, not part of a spanning multi-line record) should be collected. Feed the valid rows through `sort -t, -k1,1n` to order by numeric timestamp, then use a second `awk` pass to compute the rolling sum of column 3 over a window of the last 3 values (for row 1 just itself, row 2 sum of first two, row 3+ sum of prior 3), and build the JSON with the exact `valid_rows`/`rolling_sums` structure. For the services: fix `api.sh` to prepend proper `HTTP/1.1 200 OK` + `Content-Type: application/json` headers with `\r\n\r\n` before dumping the stats file content through `nc`, keep ports 8080/8081 unchanged, then launch both via `/app/pipeline/start.sh` and confirm end-to-end with a real POST to port 8080 followed by a GET to port 8081.
