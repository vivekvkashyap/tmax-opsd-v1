This is a recursive-hierarchy rollup in SQL, not something you can do with a flat GROUP BY. Write a recursive CTE that, for every category, enumerates all of its descendants (including itself) by walking `parent_id` links downward: start the anchor with each category joined to itself, then recursively join children where `parent_id` matches the previous level's `cat_id`, propagating the original ancestor's id/name alongside each descendant found. Join that descendant-closure table to `files` on `cat_id`, group by the original ancestor category, and `SUM(size_bytes)` to get the cumulative total (categories with no files anywhere in their subtree naturally get excluded once you filter `SUM > 0`, or use a HAVING clause). Apply `RANK() OVER (ORDER BY cumulative_total_bytes DESC)` in an outer query over that grouped result. Execute this as one query via `sqlite3 /home/user/research_data.db "<query>"` with `-csv` mode (or `.mode csv` in a heredoc) and headers off, redirecting output straight to `/home/user/category_metrics.csv`, matching the strict no-space comma format exactly.
