Start by reading the Rust source in `/app/diffusion_sim` to find where the spectral derivative multiplier is applied to each Fourier mode for the diffusion equation ∂u/∂t = ν∂²u/∂x². The correct linear operator in Fourier space for the second spatial derivative is proportional to `-ν·k²` (since differentiating twice in space multiplies by `(ik)²` = `-k²`); a common bug is applying only `-ν·k` (missing the square), which under- or over-damps modes and can diverge. Locate that multiplier and fix it to square the wavenumber before scaling by `-ν`. Remember wavenumbers must be computed correctly for an FFT of size N over domain length L=2π (standard FFT frequency ordering, e.g. via `k = 2π·freq_index/L` with proper wrap-around for negative frequencies in the second half of the array). After fixing, add code to serialize the final real-space state `u(x,1.0)` to `/home/user/final_state.json` as required. For the analyzer, build a separate Rust project that reads that JSON, computes a forward FFT with `rustfft`, takes `|coefficient|²/N²` per bin as PSD, and writes it to `/home/user/spectrum.json`. Validate your own pipeline by checking energy is concentrated near k=1 and k=3 and decays with time, consistent with the analytical exponential decay `exp(-2ν k² t)` for the PSD.
