Two separate fixes are needed in /app/vendored/ziputil before you can build on top of it: first, inspect go.mod for a malformed module path/directive and correct it (and update any import path references in your own code to match) so `go build`/`go get` can resolve it locally, likely via a `replace` directive in your service's go.mod pointing to the local vendored path. Second, in ziputil.go's Extract function, after computing each entry's destination path with filepath.Join, validate it against the cleaned destination root using filepath.Clean plus a prefix check (e.g. `!strings.HasPrefix(destPath, filepath.Clean(dest)+string(os.PathSeparator))`) and silently `continue` past any entry that fails — this is the Zip Slip fix. For the service itself, use fsnotify or a polling ticker over /home/user/incoming to detect new .zip files, extract with the fixed package, parse manifest.json, do a plain string.ReplaceAll on the source markdown for __VERSION__, then os.MkdirAll the parent dirs under /home/user/www and os.Symlink the transformed file at published_path, removing the source zip only after all steps succeed. Serve /home/user/www via http.StripPrefix("/docs/", http.FileServer(...)) mounted at /docs/, and keep the process running in the foreground/background on 127.0.0.1:8080 without exiting.
