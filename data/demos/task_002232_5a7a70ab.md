First recover the audio backup: use a speech-to-text tool available on the system (e.g. an ASR CLI or a Python speech-recognition library) to transcribe `/app/retry_audio.wav`, then parse the spoken "Name/SSN/Amount" fields out of the transcript text into proper CSV rows before appending them to `/app/historical_transactions.csv` — don't assume the transcript is perfectly formatted, normalize it yourself. For `/home/user/clean_data.c`, the performance requirement is the crux: split the file into N roughly-equal line-range chunks and process them concurrently with OpenMP (`#pragma omp parallel for`) or pthreads, each thread handling a chunk independently, then merge results — a naive single global mutex-protected hash-set per line will kill your speedup, so prefer per-thread local dedup structures merged at the end, or a partitioned/sharded hash approach. When masking SSNs, operate on the ASCII digit pattern only (regex-like scanning for `\d{3}-\d{2}-\d{4}`) so you don't corrupt adjacent multi-byte UTF-8 name bytes. Compile with `gcc -O3 -fopenmp` and benchmark against a naive single-threaded version yourself to confirm you clear the required speedup margin before finishing.
