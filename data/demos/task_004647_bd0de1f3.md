Process each `.log` file independently and in a fixed order (sort filenames alphabetically up front so the summary report is naturally ordered). For each file, read all lines, and only act if `len(lines) > 10000` — files at or under the threshold must be skipped entirely and left untouched. When a file qualifies: split it into `older = lines[:-1000]` and `tail = lines[-1000:]`; overwrite the original file with `"### LOG ROTATED ###\n"` followed by `tail` (1001 lines total). Filter `older` by dropping any line where `"[IGNORE]" in line`, count what remains, gzip-compress the filtered lines to `/home/user/archive/<filename>.gz` (create the archive directory first with `os.makedirs(exist_ok=True)`), and record `<filename>: <count>` for the summary. Be careful about ordering of operations — compute the archived line count from the filtered list itself (not from a re-read of the file) so the summary and the actual gzip contents can never drift apart. Write `/home/user/summary.txt` only after processing all files, sorted by filename, then execute the script and spot-check line counts with `zcat file.gz | wc -l` against the summary.
