Phase 1: extract every frame with `ffmpeg`, run `tesseract` OCR on each, and count frames whose OCR text contains the exact string `BREACH_DETECTED` — check every frame programmatically rather than sampling. Phase 2: use `find /app/storage_metadata -name '*.json' -size +10k` to shortlist by size, then grep those candidates for the literal `"status": "compromised"` substring, writing absolute paths. Phase 3 is the hard part: build the C daemon as an accept-loop TCP server requiring `AUTH <token>\n` first (close immediately on wrong token or out-of-order commands), then dispatch `VIDEO_COUNT`, `INSPECT <path>`, `WATCH <dir>`. For tar parsing, read raw 512-byte headers yourself — filename is the first 100 bytes; flag `MALICIOUS` if it starts with `/` or contains `../`; use the octal size field (offset 124) to skip each entry's data (rounded to the next 512-byte boundary), stopping at two zero blocks. For `WATCH`, use `inotify_init`/`inotify_add_watch` with `IN_CLOSE_WRITE`, re-running the same tar-inspection on each new file and appending to the log. Launch the daemon in the background and test each command manually with `nc` before finishing.