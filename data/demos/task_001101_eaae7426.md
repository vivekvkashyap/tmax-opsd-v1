Extract a 1-second-resolution brightness series from the video with ffmpeg (e.g. `signalstats` filter or sampling one frame per second and averaging pixel intensity per frame), and separately bucket `/app/sensor.csv` into 1-second bins by flooring `timestamp_ms/1000` and averaging `luminosity_raw` per bucket. Min-max normalize both series to [0,1] independently. To find the alignment, don't assume the offset — search integer shifts from -15 to +15 seconds, shifting the sensor (or video) series by each candidate, computing Pearson correlation over the overlapping window (`np.corrcoef` or manual formula) for each shift, and picking the shift that maximizes correlation; this brute-force grid search is the correct/expected approach given the small bounded search space. After finding the best shift, build `/home/user/aligned.csv` using only overlapping timestamps post-shift, with the exact header `timestamp_sec,normalized_brightness,normalized_sensor`. Finally, write `/home/user/crontab.txt` with a standard 5-field cron line scheduling `/home/user/process_latest.sh` at 15 minutes past every hour (minute field `15`, hour/day/month/weekday as `*`). Validate your final correlation numerically before finishing since the grader requires >= 0.90.