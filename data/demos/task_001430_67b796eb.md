Read `/home/user/analyzer/main.c` looking specifically for two classic concurrency bugs: (1) multiple worker threads calling `fprintf`/`fputs` on the same shared `FILE*` without a mutex around each write — interleaved writes from different threads corrupt/garble lines; (2) threads reading/writing a shared frame buffer or shared index counter without synchronization, causing missing or duplicated frames. Fix by adding a `pthread_mutex_t` guarding the output-write critical section (lock immediately before the fprintf call, unlock immediately after, per-frame, not around the whole thread body), and either give each thread its own private buffer plus an atomic/mutex-protected frame index, or accumulate all results in memory (indexed by frame number, not append-order) and write them out sequentially at the end sorted by frame number — the latter is simpler and avoids write-ordering races entirely. Do not touch the actual intensity math, only the synchronization/output paths. Compile with `-lpthread`, run against `/app/test_run.mp4` via the existing ffmpeg pipe logic, and confirm you get exactly 300 lines in `output.csv` with strictly increasing frame numbers 0..299 before considering it done.