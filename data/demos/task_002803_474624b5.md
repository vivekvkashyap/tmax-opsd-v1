Before writing any orchestration script, locate and fix the vendored tool's config: inspect `/app/bash-json-reshaper-1.1/config.env` (or similar config file) for a flag controlling unicode escape decoding that's disabled by default — flip it to enabled so `reshape.sh` correctly unescapes `\uXXXX` sequences into literal UTF-8 characters in its TSV output. Then write `/home/user/run_pipeline.sh` as a true streaming pipe: read stdin, pipe it directly into the fixed `reshape.sh` (don't buffer the whole input into a variable/file first — use process substitution or a straight pipe so large inputs stream through), then read the reshaper's tab-separated `id, metric_name, metric_value, text` fields line by line (e.g. with a `while IFS=$'\t' read -r ...` loop) and print each in the exact required template format. Since the grader does bit-exact fuzz comparison against a hidden reference, pay close attention to: exact spacing/punctuation in the template, correct field order, and passing unicode text through untouched (no accidental re-escaping or truncation). Make the script executable and test it manually with a small multi-line, multi-metric JSON-lines sample containing a unicode escape before considering it done.
