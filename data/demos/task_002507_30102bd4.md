Use `find /home/user/data_volume -type f -newermt ... ` won't directly give "strictly before a date" — instead use `find ... ! -newermt '2023-01-01 00:00:00' -size +10000c` (or compare each file's `stat -c %Y` against the epoch cutoff and `stat -c %s` against 10000 in a loop) to get the exact candidate set, keeping paths relative to `data_volume` (use `find data_volume -type f ... ` from the parent dir, or strip the prefix). Write the RLE compressor in Python operating on raw bytes (open in `'rb'`, iterate with a counter, cap each run at 255 before emitting a new `[count][byte]` pair, and handle runs that split across the 255 boundary). Compress `.txt` files to `name.txt.rle` bytes and non-`.txt` files with Python's `gzip` module to `name.gz`, then add both into a single tar via `tarfile` with `arcname` set to the relative path (not host-absolute) so the archive doesn't embed `/home/user/...`. Compute each original file's SHA-256 before compression for the manifest, sort lines by path. For verify.py, read tar members into memory (`tarfile.extractfile`), decompress per-extension (write your own inverse of the RLE scheme, and `gzip.decompress` for `.gz`), hash, and compare exactly against manifest.txt entries; only print `INTEGRITY_PASSED` if every hash matches.