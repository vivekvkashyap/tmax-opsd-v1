Start with the filesystem: `mkdir -p /app/dataset && mount -o loop /app/cnc_data.ext4 /app/dataset` (use `sudo` if required), then reconstruct the dataset by extracting `base.tar` into `/app/restored` first and then extracting `update.tar` on top of it (later tar overwrites earlier files) so the final `job.gcode` reflects the differential update, not just the base. For the server, write it in plain C with POSIX sockets (`socket`/`bind`/`listen`/`accept` in a loop so it serves multiple sequential clients without exiting), parsing each line as `EXTRACT Z=<float>`. Search `job.gcode` for a `; SYNC T=<hh:mm:ss> Z=<float>` comment whose Z matches — compare the Z as a string or with a tolerant float compare so formatting differences (e.g. `2.50` vs `2.5`) don't cause false NOT_FOUND. When found, shell out via `popen`/`system` to `ffmpeg -ss <T> -i /app/recording.mp4 -frames:v 1 -y /app/frames/frame_<float>.jpg` (mkdir `/app/frames` first), then `stat`/`fstat` the resulting file for its byte size and reply `SUCCESS T=<T> BYTES=<size>\n`; otherwise reply `ERROR NOT_FOUND\n`. Run the server in the background (`&`/`nohup`) bound to `0.0.0.0:7777` and leave it running.
