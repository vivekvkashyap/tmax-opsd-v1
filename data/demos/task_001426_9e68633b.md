Start by inspecting both corpora directly (`cat`/`jq` through a few files in `/app/corpus/clean/` and `/app/corpus/evil/`) to empirically discover what distinguishes them, rather than assuming — you'll likely observe the clean set has a plausible characters-per-second ratio (text length vs duration) and normal word variety, while the evil set contains cases like extreme chars-per-second ratios, strings of pure consonants/gibberish, or heavy word repetition (a stuck-pipeline pattern). Optionally transcribe the sample wav (with `whisper` if available, or inspect it however you can) purely to confirm the repeated-phrase anomaly type you should be detecting, not to hardcode its transcript. Implement `/home/user/detector.py` with a few composable checks: (1) a length-to-duration ratio bound derived from what you observed in the clean corpus (reject if far outside that band), (2) a repetition/changepoint check — e.g. split into words and reject if any single token or short n-gram makes up too large a fraction of the text, or use consecutive-duplicate detection, (3) a character-class sanity check (reject strings dominated by consonant runs with no vowels, which indicates gibberish). Log every file's decision and the reason to `/home/user/pipeline.log`, and print only the clean filenames to stdout. Before finishing, run your script against both corpora directories and confirm it achieves 100% accuracy on each before tuning thresholds further.