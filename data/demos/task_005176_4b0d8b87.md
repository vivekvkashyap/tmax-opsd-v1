Build and unit-test the filter script in isolation against the provided corpora before wiring it into the running services. Write `/home/user/filter.sh` to validate, per row (skipping the header): header line matches exactly `id,feature_A,feature_B,target`; `id` matches an integer regex; `feature_A` and `feature_B` are non-empty and match a float/integer regex (reject blank fields explicitly, don't let empty-string comparisons silently pass); `feature_A` numerically within [-1000, 1000] inclusive (use `awk` or `bc` for numeric comparison, not string comparison); `target` is exactly `0` or `1` and non-missing. Use `awk` for the bulk of this since it's well-suited to CSV field validation with numeric checks, and make the script `exit 0` only if every row passes every check, `exit 1` on the first failure. Test it directly against every file in `/home/user/corpora/clean/` and `/home/user/corpora/evil/` with a loop checking `$?`, iterating until 100%/0% pass, before editing `PRE_FILTER_CMD` in `/home/user/app/config.ini` to point to the script's absolute path and running `start_services.sh`. Finally confirm end-to-end by POSTing a clean and an evil file to the running API and checking Redis only received the clean payload.