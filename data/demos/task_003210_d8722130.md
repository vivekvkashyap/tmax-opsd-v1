Before writing the Python script, use shell tools to investigate the live service so you understand its actual behavior. Discover the listening HTTPS port on `127.0.0.1` (e.g. scan the 8000-9000 range with a quick loop of `nc -zv` or check `ss -tlnp`/`lsof` for the owning process), then confirm with `curl -k -v https://127.0.0.1:<port>/`. In your `/home/user/audit_service.py`, since `requests` is disallowed, use `ssl.SSLContext` with `CERT_NONE` plus `socket`/`http.client` (`HTTPSConnection`) to connect: grab the peer certificate via `SSLSocket.getpeercert()` (note this requires `cert_reqs`/verification enabled in some form to populate fields — if using `getpeercert()` returns empty under `CERT_NONE`, fetch the cert with `ssl.get_server_certificate` and parse it, e.g. via the `cryptography`/`OpenSSL` stdlib-adjacent tools or `subprocess` calling `openssl x509 -noout -subject`) to get the Subject string. Fetch `/` and parse the `Content-Security-Policy` response header, checking specifically whether the `script-src` directive's value contains `'unsafe-inline'`. If it does, GET `/submit` with `q=<script>alert(1)</script>` URL-encoded, check for HTTP 200 and that the exact payload string appears in the response body. Assemble all four fields into the exact required JSON keys and write to `/home/user/audit_report.json`.