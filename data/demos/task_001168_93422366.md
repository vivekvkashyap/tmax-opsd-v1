Build this in two clearly separated stages and test each independently before combining them. Stage 1 (signal processing): load `/app/system_noise.wav` with `scipy.io.wavfile.read`, compute `scipy.signal.stft(signal, nperseg=256, noverlap=128)` and take the magnitude of the complex STFT matrix, then run `numpy.linalg.svd` on that magnitude matrix. Reconstruct the rank-5 approximation by keeping only the top 5 singular values/vectors (`U[:, :5] @ diag(S[:5]) @ Vt[:5, :]`), split the resulting matrix's time axis exactly at `N//2` using integer division, average each half over time per frequency bin, add `1e-9` to both vectors, normalize each to sum to 1, and compute KL divergence with `numpy.sum(P * numpy.log(P / Q))` — verify these intermediate arrays by hand/print before wiring them into the API so you're not debugging both stages at once. Stage 2 (API): use Flask or FastAPI bound explicitly to host `127.0.0.1` port `8080`; implement Bearer-token auth as middleware/dependency checked on every route (return 401 on missing/invalid token) before implementing `/metrics` and `/compare` (the latter using `scipy.stats.wasserstein_distance` directly on the provided lists). Run the server as a long-lived background process so it stays reachable for testing.
