Use `git bisect` rather than manually scanning 200 commits. Write a small test script that: starts the server via `./server.sh <port>` in the background at a given commit, sends an HTTP request containing `X-Debug-Trace: true`, waits with a short timeout, and reports good/bad based on whether a response arrives in time (a hang = bad, a prompt 200 = good) — remember to kill any leftover server/background processes between bisect steps so stale listeners on the port don't produce false results. Run `git bisect start`, mark current `HEAD` bad and `HEAD~199` good, then either bisect manually or via `git bisect run <script>` to land on the exact offending commit; write its full 40-char hash (via `git rev-parse`) to `/app/buggy_commit.txt`. To fix the bug, inspect the identified commit's diff for the header-parsing loop — look for a loop condition/counter that's never actually decremented or updated on the path taken when `X-Debug-Trace: true` is seen, and correct it so the loop terminates and the handler proceeds to send the 200 OK response for both traced and untraced headers. Finally start the fixed server on 127.0.0.1:8080 in the background and confirm with curl (including the Authorization header) that it responds "OK" quickly.
