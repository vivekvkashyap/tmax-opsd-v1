Build the four pieces independently and verify each in isolation before wiring them together. For the SMTP server, write a small asyncio/socket-based server implementing the minimal SMTP dialogue (accept HELO/EHLO, MAIL FROM, RCPT TO, DATA-until-`.`-on-its-own-line, QUIT), always replying with success codes, and append the raw DATA payload plus headers to the maillog file on every message; test it manually with `nc` or `swaks` before trusting it. For the git hook, remember `post-receive` reads ref updates from stdin — use `git --work-tree=/home/user/app_code --git-dir=/home/user/deploy.git checkout -f <ref>` to populate the working tree, then have the hook script speak raw SMTP over a socket (e.g. Python `smtplib` or `curl`/`bash` with `/dev/tcp`) to 127.0.0.1:2525 to send the notification, and append to the deploy log; make the hook executable. For transcription, install a lightweight ASR tool (e.g. `whisper` or `vosk`) to actually transcribe `/app/voicemail.wav` rather than guessing the text, and serve it from a simple HTTP server, logging each request. For logrotate, write a conf block matching `/home/user/logs/*.log { daily rotate 5 compress missingok }` with no `su` line, and sanity-check it via `logrotate -d`. Start both servers in the background and confirm they're listening with `ss -ltnp` before finishing.