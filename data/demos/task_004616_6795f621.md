Build this as a deterministic pipeline, matching every stated detail exactly since reproducibility is the whole point. Create the venv (`python -m venv /home/user/workspace/venv`), activate it, `pip install numpy scipy`. For step 1, define the objective function exactly as specified (tanh-distorted 3-tone sum, single-sided FFT amplitude at bins 5/15/30 via `(2/N)*abs(fft[k])`), and call `scipy.optimize.minimize(fn, [1.0,1.0,1.0], method='Nelder-Mead')` — do not add extra options that change convergence. For step 2, seed exactly `np.random.default_rng(42)` once, draw the full `(1000,100)` noise matrix in one call, and for each row's sum-of-squares use `math.fsum(row.tolist())` (converting to a plain Python list is required — passing the numpy array directly changes accumulation order/precision). For step 3, seed `np.random.default_rng(99)`, draw a single `(5000,1000)` index matrix via `.choice(1000, size=(5000,1000), replace=True)`, compute per-row means, then `np.percentile` at 2.5/97.5. Write the five floats to `results.json` with the exact key names given. Run the script once and let it produce the file — do not hand-derive the numeric answers yourself.