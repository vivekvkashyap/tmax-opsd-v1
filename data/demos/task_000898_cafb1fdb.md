Build the pipeline in pure awk/bash without ever loading the full file into a shell array. Compute magnitude per row with `awk` using `sqrt(x^2+y^2+z^2)` and keep it in a temp file alongside the row. For the systematic sample, use `awk 'NR>1 && (NR-1)%7==0'` on the magnitude-augmented data (this selects data rows 7, 14, 21…, remembering NR includes the header so subtract 1 first). Compute sample mean and population standard deviation (divide by N, not N-1) from just those sampled magnitudes — do the arithmetic in `awk` or `bc -l` and round with `printf "%.4f"` rather than truncating. Then re-scan the *entire* original dataset, recompute magnitude per row, and keep only rows where mean-1.5*std < magnitude < mean+1.5*std (strict inequality, not inclusive). Write the header with the appended `magnitude` column, then the surviving rows, then a final `# STATS: mean=..., std=...` line using the exact sample-derived values. Common pitfalls: using sample standard deviation formula (N-1) instead of population (N), applying the filter only to the sample instead of the full dataset, or off-by-one errors in picking "every 7th row" relative to the header.
