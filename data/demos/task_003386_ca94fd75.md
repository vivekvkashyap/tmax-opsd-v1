Start by OCR'ing /app/api_spec.png with `tesseract /app/api_spec.png -` (or Python's pytesseract) and read off the port, endpoint path, Bearer auth token, and JSON response shape it specifies — use those recovered values verbatim as your server's bind port, route, and expected Authorization header rather than guessing typical defaults. Load /home/user/data/records.csv and /home/user/data/reference.json, join each record's row on `source_id` to its prior `alpha`/`beta`. For each id compute the Beta-Binomial posterior: `posterior_alpha = prior_alpha + successes`, `posterior_beta = prior_beta + (trials - successes)`, `posterior_mean = posterior_alpha / (posterior_alpha + posterior_beta)` — this is the standard conjugate-update formula, so derive it rather than hardcoding output numbers. Build an HTTP server (stdlib `http.server` or similar) bound to 127.0.0.1 on the OCR'd port, serving the OCR'd endpoint path with `id` as a query parameter: return 401 if the `Authorization` header doesn't exactly match the OCR'd token, 404 if the id isn't in the dataset, and otherwise 200 with JSON in the exact shape read from the image, using the computed posterior_mean. Keep the server running in the background (e.g. `nohup ... &`) so the verifier's HTTP requests can reach it.
