Implement this as a literal pipeline of discrete stages in your C program, mirroring the NoSQL aggregation stages, rather than one tangled loop — it's far easier to get each stage right in isolation and print intermediate state while debugging. Stage order matters: (1) parse orders.jsonl with cJSON and keep only rows where status=="completed" and total>100; (2) for each surviving order, look up the matching user in users.jsonl (load users into an array/hashmap once, keyed by user_id, to avoid re-scanning the file per order); (3) accumulate a running total_spent per user_id, carrying along that user's region; (4) sort the grouped array with `qsort` using a comparator that sorts descending by total_spent and, on ties, ascending by user_id string comparison — get the tie-break direction right since it's easy to invert; (5) apply skip=1/limit=3 as a simple index slice over the sorted array. Write the CSV with the exact header row, then diff your program's output against the pipeline logic worked out by hand from the raw JSONL to catch off-by-one errors in the pagination before considering it done.