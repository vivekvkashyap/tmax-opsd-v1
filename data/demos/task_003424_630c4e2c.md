Parse the three CSVs in `/home/user/data/` in C++ (plain comma-split is fine), build an adjacency list from `routes.csv` but drop edges outright where `risk_score >= 8` before running any pathfinding. Determine eligible targets from `inventory.csv`: warehouses with `item_id == "ITEM-999" && quantity >= 50`. Run Dijkstra with a priority queue from `W001` over the filtered graph (weights are real distances, not unit hops), computing shortest distance to every node, then pick the minimum-distance node among all eligible targets — there may be several, so compare across all of them rather than stopping at the first reached. Track predecessors to reconstruct the path, then write it comma-joined on line 1 and the total distance as a plain integer on line 2 to `/home/user/result.txt`, no trailing text. Compile with `g++` and actually run the binary to produce the file. Trace the given routes by hand first to confirm which edges the risk filter excludes before trusting your output.