Parse `backup_targets.ini` for the relative target paths (skip section headers like `[targets]`), then use `cp --parents -r <target> /home/user/staging/` from inside `/home/user/sys_root` so the relative subdirectory structure is preserved automatically. For the text migration, only touch files under `/home/user/staging` matching `*.conf` and replace the key `server_ip=` with `bind_addr=` (e.g. `sed -i 's/^server_ip=/bind_addr=/' file` or a Python regex anchored on the key, not a blind global substring replace that could also hit values). For the C program: recursively walk the staging directory (`nftw`/`opendir`+recursion), filter by `.conf` suffix, compute each file's SHA-256 via OpenSSL's EVP digest API (link with `-lcrypto`), collect `(relative_path, hex_digest)` pairs, sort by relative path with `qsort`/`strcmp`, and write them as `<hex> <path>` lines to `/home/user/manifest.txt`. After writing the manifest, have the C program `system()` a tar command run *from inside* the staging directory (e.g. `tar -czf /home/user/config_backup.tar.gz -C /home/user/staging .` or `cd`+`tar czf ... *`) so the archive's entries don't include a leading `staging/` wrapper. Leave `/home/user/sys_root` untouched throughout.
