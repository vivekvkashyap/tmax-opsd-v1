Parse each log line with a `regexp` like `^\[.*?\] \[.*?\] Processed request in ([\d.]+)ms with (\d+) retries\.$`, using `MatchString`/`FindStringSubmatch` and skipping (not erroring on) any line that doesn't match or where the captured groups fail to parse as float64/int — this is how non-request lines like a "Database backup completed" entry get correctly excluded. Compute `severity_score = latency * (retries + 1.5)` per valid line into a slice. For the bootstrap: loop 10,000 times, each time drawing `len(scores)` indices uniformly at random with `rand.Intn(len(scores))` (replacement allowed, so don't dedupe), average the sampled scores, and store that mean. Sort the 10,000 bootstrap means, then take the values at indices corresponding to the 2.5th and 97.5th percentiles (e.g. `index = percentile/100 * (n-1)`, rounded or interpolated — either reasonable convention should satisfy the wide verifier tolerances) as `ci_lower`/`ci_upper`. Compute the exact mean directly from the original scores (not from the bootstrap distribution). Set `reject_null = ci_lower > 600`. Marshal all fields to `/home/user/results.json` with `encoding/json`.
