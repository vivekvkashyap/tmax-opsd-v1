Use Go's standard `archive/zip` and `archive/tar` packages to read only the header/entry names — never call `Extract` or write file contents to disk, since the entries are hostile by design. Walk `/home/user/uploaded_archives/` with `filepath.Walk`, and for each `.zip`/`.tar` file open it and check each entry name: flag the archive malicious if any entry path has `filepath.IsAbs(name)` true (or simply starts with `/`) or contains the traversal sequence `../` (or `..\`) as a path component — do proper path segment checking rather than a naive substring match, so you don't miss encoded variants but also don't false-positive on legitimate names containing those characters mid-token. Collect malicious archive absolute paths, sort them with `sort.Strings`, then write results by first writing to a temp file in the same directory (`os.CreateTemp` in `/home/user/`) and calling `os.Rename` to atomically replace `/home/user/malicious_archives.log` — apply the same create-temp-then-rename pattern for `/home/user/cleanup.sh`, whose lines are `rm -f '<path>'` with a `#!/bin/bash` shebang, sorted the same way. Run the compiled program once to produce both output files; do not execute cleanup.sh.