Tackle this in three independent tracks and integrate at the end: config fixes, the C++ sanitizer, and the supervisor script. First fix the two port mismatches — `/home/user/app/nginx/nginx.conf` proxy_pass to `127.0.0.1:9090` and `/home/user/app/backend/config.json` Redis connection to `127.0.0.1:6379` — then kill any stale processes before restarting via `start.sh` so old listeners don't hold the ports. For the sanitizer, write `/home/user/app/filter/sanitizer.cpp` as a simple CLI: read the file path from argv[1], slurp its contents into a string, and do straightforward substring search (`std::string::find`) for each malicious pattern (`../`, `<script>`, `DROP TABLE`, `$(whoami)`, `; rm -rf`) — case-sensitive, exact substrings, exit 1 on any match else exit 0. Compile with `g++ -o /home/user/app/filter/sanitizer sanitizer.cpp` to that exact path. Test the binary standalone against a few crafted clean/dirty files before wiring it into the backend, since the backend shells out to it and a bug there silently breaks the whole request flow. Finally write `health_monitor.sh` using `pgrep`/`ps` checks for `nginx`, `redis-server`, and `backend.py`, re-running `start.sh` if any is absent. Validate end-to-end with a curl POST through nginx and a `redis-cli GET` to confirm the full chain works, then kill nginx and confirm the health monitor revives it.
