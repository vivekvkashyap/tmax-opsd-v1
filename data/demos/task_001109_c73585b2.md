Work through `adaptive_integrate` line by line before touching `refine_mesh`. The adaptive step-size logic compares a full step of size dt against two half-steps of dt/2; when the estimated error exceeds tolerance, the step must be *shrunk* (halved), not grown — trace the `if error > tol:` branch and check the direction of the dt update, since a step-size adaptation that grows on failure is guaranteed to diverge/overflow. Fix only that arithmetic, don't rewrite the Euler stepping method itself. For `refine_mesh`, iterate over a copy of the original edges (mutating the list you're iterating breaks traversal), assign new node IDs starting at `max(nodes)+1` and incrementing per edge, average the two endpoint temperatures for the midpoint's initial value, and rebuild `nodes`/`edges`/`initial_temps` fresh rather than mutating in place. Finally wire up the `__main__` block: call `refine_mesh` then `adaptive_integrate` up to T=0.5, dump the resulting temperature dict (keys as strings) to `/home/user/final_temperatures.json` via `json.dump`, and use matplotlib to scatter/plot node id vs temperature and save to `/home/user/thermal_plot.png`. Run the script end-to-end and confirm no OverflowError and the JSON has one entry per node after refinement.