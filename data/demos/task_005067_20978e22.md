Fix the integrator bug first, in isolation: for a successful adaptive RK45 step, the new step size must shrink when error is large and grow when error is small relative to tolerance, so the scaling factor should be `(tolerance/error)^0.2`, not `(error/tolerance)^0.2` — check both the success and failure branches use the same correct ratio direction (only invert exponent/branch as needed) and re-derive from the classical embedded RK45 step-control formula rather than guessing. Install FFTW3 via the system package manager (e.g. `apt-get install libfftw3-dev`) and link with `-lfftw3 -lm` when compiling. For the simulation enhancement, write the full repressilator ODE integration loop with your fixed adaptive stepper from t=0 to t=1000, storing (t, p1) pairs; discard samples where t<100; then linearly interpolate p1 onto a uniform grid from t=100 to t=1000 with step 0.1 (for each grid point, find the bracketing adaptive-step samples and interpolate). Feed this uniform real-valued array into `fftw_plan_dft_r2c_1d`, run it, then scan the resulting complex magnitude spectrum (skipping the DC/zero-frequency bin) for the peak magnitude; convert that bin index to a frequency using `bin_index / (N * dt)`. Write the frequency to the result file with exactly 4 decimal places.