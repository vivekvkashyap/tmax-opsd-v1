Diagnose the two bug classes independently before combining fixes. For the concurrency bug: read main.py's frame-processing path — if multiple threads/processes each independently `open(path, 'a')` and write a line, interleaved writes and lost updates are expected; fix it by having each worker return its (frame_index, entropy) result to the main thread (e.g. via `concurrent.futures` `as_completed`/`map` return values or a `queue.Queue`) and have a single thread perform all file writes, or protsect writes with a `threading.Lock`/write to per-frame temp files then merge; also make sure exceptions in worker threads aren't silently swallowed (a bare except that drops frames explains "missing records"). For the numerical crash: locate the entropy calculation (likely Shannon entropy over pixel-intensity histograms) and find where a probability of 0 hits `math.log(0)` or a division by a zero-sum bin — fix with an epsilon guard (skip zero-probability bins, or clip with `max(p, 1e-12)`) without altering the entropy formula itself, matching frames that are pure black/white. After fixing both, rerun the full pipeline on `/app/experiment.mp4`, confirm every frame index appears exactly once in `/home/user/final_output.csv`, and verify entries are sorted by frame_index ascending before finishing.