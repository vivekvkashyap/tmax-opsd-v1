In the C program, parse each CSV line manually (e.g. with `strtok`/`sscanf` on the comma) rather than `atoi` on the whole line, so you can explicitly detect "nothing after the comma" as missing rather than silently getting 0 — check if the substring after the comma is empty/whitespace-only before attempting to parse it as an integer. Maintain running posterior mean/variance state (`mu`, `sigma2`) initialized to the given prior, and for each present value first test the outlier rule using the *current* posterior (`|x-mu| > 3*sqrt(sigma2+10.0)`) before updating; only update mu/sigma2 with the conjugate-prior formulas for valid (non-missing, non-outlier) points, in file order. Print counts and final mean with `%.2f` in the exact format given. For the bash script, compile with `gcc -O2 -lm -o process_data process_data.c`, iterate `.csv` files in `/home/user/data/` sorted alphabetically (e.g. `for f in $(ls /home/user/data/*.csv | sort)`), run the binary on each, and append `File: <name> | <output>` lines to `/home/user/results.log`. Verify by hand-tracing one small file's arithmetic before trusting the full run.