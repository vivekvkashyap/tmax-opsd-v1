Treat this as a two-pass graph resolution problem in Go. Parse `/home/user/graph_backup.csv` line by line into `subject,predicate,object,timestamp` records (timestamp as int). Pass 1: build a map from subject -> best `current_status` edge, keeping the one with the strictly highest timestamp seen so far (compare on each new row, replace if higher). Pass 2: iterate all `manages` edges and look up the object's resolved status from the map built in pass 1; keep the edge only if that status's object value is exactly `ACTIVE`. Emit the union of resolved `current_status` edges and kept `manages` edges as CSV lines. Sort the final lines with Go's `sort.Strings` (plain lexicographic string comparison over the whole line, not a numeric/field-aware sort) before writing, since the spec requires literal string ordering. Common mistakes: comparing timestamps as strings instead of ints, forgetting nodes with no status edge should silently exclude any `manages` edge pointing at them, and writing a trailing newline mismatch — write each line followed by `\n` and verify final file content matches exactly via diff-style check.