Build this as pure awk-driven text pipelines, not shell loops calling bc per row (too slow/fragile). Normalize with `tr '[:upper:]' '[:lower:]'` then `sed`/`tr` to replace non-alpha chars with spaces, then feed whitespace-tokenized lines into a single awk pass that: streams consecutive word pairs per line to accumulate unigram counts C(w1) into one associative array and bigram counts C(w1,w2) into a second (key them as "w1 SUBSEP w2" or "w1\tw2"), tracks the unigram set size for vocabulary V, then in an END block emits `w1 \t w2 \t (C(w1,w2)+1)/(C(w1)+V)` for every bigram actually seen, formatted with `%.4f`, and finally pipe through `sort -k1,1 -k2,2` for the required ordering. For benchmark.sh, reuse the same normalization/tokenization, look up each test bigram in a loaded model.tsv (awk associative array), and apply the three fallback cases from the spec (known bigram / known w1 only / unknown w1) with `log()` in awk, summing per line and writing `LineNumber\tLogProb`. Wrap the scoring awk invocation with `time` (redirected to time.log) and chmod +x both scripts.
