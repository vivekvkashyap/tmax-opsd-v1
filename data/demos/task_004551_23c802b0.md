The real bottleneck is the blocking 2-second TCP connect timeout to the unreachable telemetry IP — attack that first: add a local route (`ip route add local 10.254.254.10/32 dev lo`) plus a fast listener on the target port (`socat TCP-LISTEN:8080,fork,reuseaddr - &` or similar), or an `iptables -t nat OUTPUT ... REDIRECT` rule with a matching listener. Verify with a manual `time` run of one manifest that connect latency drops to near-zero before touching parallelism. Next, since the binary is interactively prompt-driven (`Manifest path? > `, waits for a line, prints `Done.`), write a small `expect` script that spawns it, matches the prompt, sends the absolute file path, and matches `Done.` to exit cleanly — test on one file first. Only once both pieces work individually should you generate the 500 dummy YAMLs and fan the expect wrapper out with `find ... | xargs -P <N> -I{} ./wrapper.exp {}`, tuning `-P` until total wall time clears the 10s bar; clean up the listener/iptables rule and background jobs afterward.
