Work this as a real black-box audit, not a code read — the service is already running, so discover it live. First find the process and its command line: `ps auxww | grep -i python` (or `ps -eo pid,cmd`) to spot the server script and the port/token passed as arguments; the token being visible in `ps` output is exactly the vulnerability you're demonstrating. Note the port from the listen socket (cross-check with `ss -tlnp` or `netstat -tlnp` if the arg doesn't make it obvious). Then use `curl -i -s --cookie "auth_token=<recovered-token>" http://127.0.0.1:<port>/` to hit the root endpoint authenticated — inspect the full response headers for one starting with `X-Missing-` (the header name itself is part of the response, don't guess it) and read the response body for a download path. Fetch that path with the same cookie and save the binary output (`curl ... -o module.pyc`), then extract embedded strings with `strings module.pyc | grep -i flag` (or disassemble with Python's `dis`/`marshal` modules if strings alone doesn't reveal it, since string constants in .pyc bytecode are usually visible via `strings`). Write the four discovered values, one per line, in the exact order requested to `/home/user/audit_report.txt`.
