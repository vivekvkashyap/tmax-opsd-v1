Write the C program as a strict, explicit-state-machine CSV parser rather than relying on `scanf`/`strtok` shortcuts that silently coerce empty fields. Split each line on commas, and for the `machine_id` field check for an empty string explicitly (skip the row) rather than calling `atoi("")` and trusting the result; likewise compare `flag` with `strcmp(flag, "VALID") == 0` exactly, not a substring check. Accumulate per-machine sums/counts as you filter, storing all valid measurement values for machine 1 in a dynamically-growable array (you'll need them again for bootstrapping) while machine 2 only needs a running mean. For the bootstrap: call `srand(42)` exactly once right before the resampling loop (not per-sample, not before parsing), then for each of the 10,000 iterations draw N indices via `rand() % N` with replacement, average the sampled values, and store 10,000 means; sort that array (`qsort`) and read off index 250 and 9750 as the CI bounds — the exact numeric bounds are implementation/libc-dependent, so just get the algorithm and index conventions exactly right per the spec. Format all four output lines to exactly 4 decimal places (`%.4f`) and write them to `/home/user/report.txt` in the specified order.
