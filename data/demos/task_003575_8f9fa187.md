Reproduce the failure first: `cd /home/user/app-src && git push deploy master` and read the hook's stderr output carefully — the checkout step likely succeeds but the subsequent service-start step fails because the invoked script enforces its own environment preconditions (e.g. it exits nonzero unless specific `TZ`/`LC_ALL` values are set) and those aren't set in the hook's shell. Open `/home/user/deploy.git/hooks/post-receive` and `start-service.sh` to confirm exactly which variables and values are required. Fix the hook by exporting `TZ=UTC` and `LC_ALL=C.UTF-8` before invoking the service script, and add `set -e` (or explicit error checks) so any step failing aborts the hook loudly rather than silently. Since git hooks run in a minimal, non-interactive shell environment, don't assume any variables are inherited from your interactive session — set them explicitly inside the hook itself. After editing, re-run the push from `app-src` and confirm it completes without the remote rejecting it, then check that the service wrote its ready-status line to the expected log file.