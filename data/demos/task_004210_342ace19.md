Diagnose the SQL bug first: inspect `etl.py`'s query against `/app/data.db` — an implicit cross join (comma-joined tables with no or incomplete WHERE/ON conditions) is exploding row counts, so rewrite it as explicit `JOIN ... ON` clauses linking `employees` -> `employee_departments` -> `departments` on their key columns. Add a window function (`RANK() OVER (PARTITION BY department_id ORDER BY salary DESC)`) to get `salary_rank` per department, verifying manually against the known small dataset that ranks look sane before moving on. For the department names, run `tesseract` (or equivalent OCR) on `/app/schema_clue.png`, parse the `MAPPING: {...}` JSON-like text out of the OCR output, and build a dict from the recovered mapping — don't hardcode names without extracting them. Separately, stream-parse `/app/events.json` as JSONL and tally login counts per employee_id with a dict/Counter, defaulting missing employees to 0. Finally join all three sources in Python, sort by `employee_id` ascending, and serve via Flask/http.server on `127.0.0.1:8080` at `GET /api/employees`, keeping the process alive in the background for the verifier to query.
