Work through the five sub-tasks in dependency order. For the passphrase, write a small bash or Python loop that tries `openssl enc -d -aes-256-cbc -pbkdf2 -in deploy_bundle.enc -pass pass:Deploy2023_XXXX` for all 4-digit suffixes (0000-9999) and treat a zero exit code (or successful gzip test with `tar -tzf`) as the hit — don't try to guess it, brute force is the intended method since the search space is only 10,000. Once decrypted, extract with `tar -xzf` into `/home/user/deploy`. For the cert, use one `openssl req -x509 -newkey rsa:2048 -nodes -keyout ... -out ... -days 365 -subj "/CN=secure.internal.dev"` command. For the CSP fix, use `sed` or a Python string replace to swap the exact old line for the exact new line — match the string precisely, don't reformat whitespace. For permissions, use `find /home/user/deploy/app -type d -exec chmod 0755 {} \;` and the equivalent for files with `0644`, then apply the `0400` override to `config.json` last so it isn't clobbered by the bulk file pass. Finally assemble the audit log by reading back the actual computed values (passphrase you found, `sha256sum` of the cert, the literal new CSP line from the file, and `stat -c %a` of config.json) rather than hardcoding them.
