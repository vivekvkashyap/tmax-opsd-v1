Do Phase 1 first since Phase 2 depends on a working `doc-unpack` binary. Diff the vendored package's Makefile and `bin/unpack.sh` against expectations: check the Makefile for mixed tabs/spaces in recipe lines (Make requires literal tab indentation for recipes — spaces cause "missing separator" errors, fix by replacing leading whitespace on recipe lines with real tabs), and check `unpack.sh` for an environment variable pointing to a wrong/broken binary path (e.g. it should resolve the real extraction tool via something like `$(which tar)` rather than being hardcoded to a no-op like `/bin/false`) — fix both, then `make install` to place the binary at `/usr/local/bin/doc-unpack` and smoke-test it on a small nested archive you construct yourself. For Phase 2, extract each corpus archive with the fixed tool into a fresh temp dir per bundle, then apply the four evil checks precisely: use `find -type l` plus readlink resolution to detect symlinks pointing outside the extraction root or to absolute system paths; use `find` with permission tests for `*.md`/`*.txt` files that have any execute bit set; validate `metadata.json` with a real JSON parser (`jq empty` or `python3 -m json.tool`) rather than regex; and scan `changelog.log` for `Author:` lines containing `$(` or backticks. Only copy the bundle to the output dir if none of the four checks trip; otherwise skip it entirely.
