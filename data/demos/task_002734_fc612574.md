Treat `/app/legacy_partitioner` purely as a correctness oracle to diff against — run it on `/home/user/mesh_data.h5` and keep its output for `h5diff` comparisons. Write `/home/user/fast_partitioner.c` reading `/coordinates` (N x 3 doubles) via the HDF5 C API in one bulk `H5Dread` call (not element-by-element). Implement recursive coordinate bisection: at each level compute per-axis min/max over the current index subset, pick the axis with largest spread, find the true median via an O(N) selection algorithm (quickselect-style) rather than a full sort, split the index set at that median, and recurse 4 levels deep (16 partitions) assigning IDs 0-15 in the same order convention the legacy tool uses — check by diffing on the sample file first. Write `/partition_ids` as an integer dataset with one `H5Dwrite` call. Compile with `-O3`, link `-lhdf5`, verify with `h5diff`, and measure wall-clock speedup with `time` before assuming it'll hit 3x on the hidden larger dataset — the win comes from bulk I/O plus O(N) median-finding.