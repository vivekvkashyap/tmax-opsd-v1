Start the mock server first (`python3 /home/user/mock_ci_server.py &`) so you have a live target to iterate against, then fix the Rust client's two missing pieces in tandem since they interact. For the state machine, track an explicit "next expected state" enum (START -> BUILD -> TEST -> DEPLOY -> END -> back to START) and only accept an event whose `event` field matches the expected state, advancing the pointer on acceptance and silently ignoring (not erroring on) anything out of order. For rate limiting, track the timestamp of the last *accepted* event (not the last received event) and reject any incoming event whose `timestamp_ms` is within 50ms of it — the key subtlety is that rejected events must not update either the state pointer or the last-accepted timestamp, only truly accepted events advance both. Accumulate `compute_cost` only for accepted events, and only write `/home/user/final_cost.txt` (formatted to 2 decimals) after the WebSocket stream closes cleanly. Run against the mock server repeatedly while iterating and manually trace a few events through your logic to confirm the ordering and timing rules interact correctly before trusting the final number.