Write the validation and bootstrap steps to match the spec exactly, since the verifier recomputes the same procedure and compares numerically. Load `/home/user/experiment_data.csv` (pandas is convenient), then filter rows using a strict regex for `predicted_class` (exactly `[A-Z]{3}`, full match not partial), a numeric parse of `confidence_score` with a try/except and range check `0.0 <= score <= 1.0`, and a numeric parse of `inference_time_ms` requiring a valid positive float (reject NaN/negative/non-numeric) — wrap each field parse in its own try/except so one malformed field doesn't silently corrupt another. Collect the surviving `inference_time_ms` values into a numpy array, call `numpy.random.seed(42)` exactly once right before the bootstrap loop (not before filtering, and not reseeded inside the loop), then run exactly 10,000 iterations of `numpy.random.choice(valid_times, size=len(valid_times), replace=True)` computing `numpy.percentile(sample, 90)` each time, collect all 10,000 values, and take the 2.5th/97.5th percentiles of that distribution — round both bounds to 2 decimals with Python's `round()`. Write `valid_count`, `p90_ci_lower`, `p90_ci_upper` to `/home/user/report.json` with exactly those key names.