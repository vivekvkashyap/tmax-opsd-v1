Solve the video-decoding step first and validate it in isolation before writing the classifier. Use `ffmpeg` (e.g. `ffmpeg -i evidence.mp4 -vf select='between(n\,0\,15)' frame_%02d.png`) or a Python video library (`opencv-python`/`imageio`) to extract frames 0-15, then for each frame read the pixel at exact coordinates (x=640, y=360) and take its Red channel value — treat each value as an ASCII code and concatenate the 16 characters in frame order to recover the secret key; print/verify it looks like a plausible readable string before trusting it. For the classifier, parse the raw HTTP request text into headers and body (split on blank line), extract the `Authorization: Bearer <token>` value if present, and use a JWT library (e.g. `PyJWT`) to verify the signature against your recovered secret with `algorithms=["HS256"]` — treat verification failure as EVIL, then separately decode the payload (only if signature is valid) to check for `"admin": true`, and finally check the `User-Agent` header for the literal substring `${jndi:` independent of JWT validity. Exit 1 if any condition fires, else 0. Write a handful of your own hand-crafted valid/invalid/admin/log4j sample requests and hypothesis-style edge cases to sanity check exit codes before trusting the hidden corpus.