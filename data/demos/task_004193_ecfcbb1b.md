Build this as four independent, chainable stages, each writing its own output so you can debug them separately. For `validate.sh`, loop over `/home/user/raw_data/*.csv` and filter each line with a single `awk -F,` (or `grep -E`) that anchors all four column regexes at once — positive integer id (`^[0-9]+$`), strict ISO timestamp (`^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$`), a float allowing optional sign/decimal (`^-?[0-9]+(\.[0-9]+)?$`), and label exactly `0` or `1` — appending matches to `combined.csv`; test this against known-bad rows (NaN, malformed timestamp, id=abc, label=2) to make sure they're actually rejected. Partition into gzip files per label with `awk` writing two temp files then `gzip`, or `awk | gzip` per branch. Compute per-label means with `awk` accumulating sum/count for label 0 and 1 in one pass, then the absolute difference formatted to 4 decimals with `printf`. For `kfold.sh`, `shuf` the combined file then use `split -n l/5` or a round-robin `awk 'NR%5'` to produce 5 files whose total line count matches the source exactly, with no duplicated or dropped rows.
