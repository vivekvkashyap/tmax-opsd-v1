This is a two-part fix: porting a Python-2 C extension to Python 3, and correcting a weak signal-extraction algorithm — do them separately and verify each in isolation before combining. For the C extension, replace `Py_InitModule`/`initsteg` with the Python 3 module-init pattern (`PyModuleDef` + `PyInit_steg`), and swap any `PyString_*` calls for `PyBytes_*` equivalents; build with `setuptools` (`pip install -e .`) rather than the bare `distutils` setup.py, since modern Python may lack distutils. In process.py, read raw bytes from stdin via `sys.stdin.buffer.read(...)`, not `sys.stdin.read(...)`, or grayscale pixel bytes will get mangled by text-mode decoding. For the algorithm, don't just look at `frame[0]`'s LSB — compute the mean pixel value over the top-left 10x10 block and return 1 if that average is strictly greater than 127, else 0; this is robust to the per-pixel noise that H.264 compression introduces. Pipe frames with `ffmpeg -i /app/evidence.mp4 -vf fps=10 -pix_fmt gray -f rawvideo -` into process.py, sized for 640x480 per frame, and write the concatenated 0/1 output to /home/user/output.txt with no trailing newline. Test on a couple of known frames first to confirm bit extraction before running the full pipeline.