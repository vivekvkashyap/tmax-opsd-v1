Build this in two clear stages and test each independently before combining. First, replicate the PIN algorithm: write a tiny C snippet that calls `srand(time(NULL)/60)` then `rand() % 1000000`, and confirm it matches what the server expects by reasoning about the spec (current UNIX time divided by integer 60 gives the "current minute" seed) — compute this at request time, not once at compile time, since the minute can roll over. Format it as needed for the cookie value. Second, for the self-hash: don't hash your source file or a cached copy — read `/proc/self/exe` (the running binary's own compiled bytes) at runtime and compute SHA-256 over it, since the file must be re-read each time in case you recompile. Use libssl's EVP SHA-256 API (or shell out to `sha256sum` on `/proc/self/exe` as a fallback) and print the digest as lowercase hex. Use libcurl to set the `Cookie` and `X-Integrity-Check` headers on a GET to `http://127.0.0.1:8000/flag`, write the raw response body to `/home/user/flag.txt`. Compile with `-lcurl -lssl -lcrypto`, and after linking, actually run the compiled binary (not a stale build) so the hash matches what you just produced.
