This is graded against a large hidden corpus, so prioritize correctness over guessing. Understand `/app/sig_check`'s checksum by treating it as a black box: probe it with handcrafted `(timestamp, sensor_id, measurement)` triples (zeros, small values, wrap-around values), hypothesize a simple formula, and confirm it matches the binary's output across many probes before hardcoding it for speed — keep the binary as a fallback check while developing. Implement validation (drop signature mismatches) and first-occurrence deduplication by signature, preserving file order. Compute the rolling average per-sensor over that sensor's own sequence only, using up to the current row and two preceding rows for that sensor. Parallelize per-sensor computation genuinely (e.g. a process/thread pool keyed by sensor_id). Implement the anomaly kill switch precisely: if any rolling average anywhere exceeds 500.0, exit non-zero and produce no output file — test this against `/home/user/samples/sample_evil.csv` and `sample_clean.csv` explicitly first.
