Treat this as three independent sub-problems. (1) Diagnose the 502 by checking what socket the real backend Python process is actually bound to — use `ss -xlp` or `lsof -U` (or inspect the running backend's source/args) rather than guessing, then edit `nginx.conf` in `/home/user/app-config` to point `upstream`/`proxy_pass` at that exact discovered socket path. (2) Set up the deploy pipeline: `git init --bare /home/user/deploy.git`, add it as a remote from `/home/user/app-config`, write an executable `post-receive` hook in `deploy.git/hooks/` that checks out the pushed tree (e.g. via `git --work-tree=... checkout -f`) to copy `nginx.conf` into `/home/user/nginx/nginx.conf`, then runs `nginx -c /home/user/nginx/nginx.conf -s reload`; commit your fix and push to trigger it, then curl the Nginx port to confirm it now proxies successfully. (3) For the token generator, treat `/app/token_generator` purely as a black-box oracle: run it against a range of hand-picked and randomized strings (short, long, with repeated chars, edge lengths) and look for a hashing pattern in the output length/format (hex digest length strongly suggests a specific hash algorithm) combined with simple string transforms (reversal, fixed suffix/prefix) — form a hypothesis, test it against the binary's output on new inputs, and only lock in `token_generator.py` once it reproduces the binary's output exactly across your own broad test set, since grading will fuzz with hundreds of random strings.
