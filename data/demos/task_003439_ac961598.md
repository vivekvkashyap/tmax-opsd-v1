Write two independent counting passes in the C++ program. For the FASTA: skip lines beginning with `>`, concatenate the remaining sequence lines, and tally only characters that are one of the 20 standard uppercase amino-acid letters (use a lookup set/array), ignoring anything else (whitespace, lowercase, X, *, etc.); divide each count by the total tally to get P. For the PDB: only process lines whose record type is `ATOM` and whose atom-name field (fixed columns 13-16, 1-indexed, so substr(12,4) trimmed in 0-indexed C++) is exactly `CA`; from those lines extract the 3-letter residue name at columns 18-20 (substr(17,3)), map it through a hardcoded 3-letter-to-1-letter table (the reference table given in the task), and skip/ignore any code not in that table — this is what produces the correct denominator for Q. Be strict about PDB column offsets since PDB format is fixed-width, not whitespace-delimited, so don't just split on spaces. Once both 20-length probability vectors are built, compute TVD as `0.5 * sum(|P_i - Q_i|)` over all 20 amino acids (including ones with zero count in either distribution) and print it with exactly 4 decimal places (`std::setprecision(4) << std::fixed`) and nothing else to `/home/user/result.txt`.
