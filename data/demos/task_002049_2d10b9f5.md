Get each service working in isolation first, then wire them together and validate against both corpora. Check what's actually running (`ps aux`, `curl localhost:5000`, `redis-cli ping`) before assuming which piece is broken; likely the nginx config points `/upload` at the wrong upstream (port/host) — fix `/home/user/app/nginx.conf` to proxy to `127.0.0.1:5000` and reload nginx. Write `/home/user/app/detector.py` to take a CSV path argv, standardize features (zero mean/unit variance, e.g. `StandardScaler`), fit `PCA` on the scaled matrix, check `explained_variance_ratio_[0]`, and `sys.exit(1)` if it's `> 0.95` else `sys.exit(0)` — test it directly against a few files from `/home/user/data/evil/` and `/home/user/data/clean/` first to confirm the threshold behaves as expected before integrating. Then edit `/home/user/app/app.py`'s `/upload` handler to save the uploaded file to a temp path, run `detector.py` via `subprocess.run` and check `returncode`, returning 400 on 1 and otherwise falling through to the existing Redis logic and 200. Restart flask/nginx/redis, then iterate by posting real files from both corpora through `http://127.0.0.1:8080/upload` and confirming clean gets 200 and evil gets 400 for every file, not just a sample.
