Write `/home/user/etl.sh` using `awk -F,` to skip the header (`NR>1`), accumulate a sum and count per `server_group` in associative arrays, and in the `END` block `printf` each group's average with `%.2f`, emitting the `server_group,avg_latency` header first. For the Python script, first diagnose why plotting hangs/fails headless: `matplotlib`'s default backend needs a display, so set a non-interactive backend (`matplotlib.use('Agg')`) before importing `pyplot`, and remove or never call `plt.show()` — call `plt.savefig(...)` instead (note the original script calls `savefig` after `show`, which is unreachable/broken in headless mode, so reorder it). Compute the t-test with `scipy.stats.ttest_ind(group_a, group_b)`, write `round(p_val, 4)` to `/home/user/p_value.txt` as plain text, and save the boxplot to `/home/user/latency_plot.png` before the script exits. Finally write `/home/user/run_pipeline.sh`, make it executable, and have it call `./etl.sh` then `python3 analyze_and_plot.py` in sequence (using absolute paths or `cd` into `/home/user` first so relative paths resolve); run it once to confirm all three output files are produced correctly.