Implement the pipeline as one script and follow the exact order given, since each stage's formatting/rounding must match precisely for a string-equality check. Steps: (1) normalize each row of train/test by dividing by its own row sum; (2) compute Group 0 and Group 1 mean distributions from the *normalized training* rows only, then `scipy.spatial.distance.jensenshannon(mean0, mean1)` and write it formatted with `f"{val:.4f}"` (not `round()`, which can drop trailing zeros) to `/home/user/js_distance.txt`; (3) subtract the *training* column means from both normalized train and test data (reuse the same training means for both — do not recompute test means), run `numpy.linalg.svd` on the centered training matrix, take the first 5 rows of `Vt` and project both centered matrices via `centered @ V_top5.T`; (4) fit `LogisticRegression(random_state=42)` on the 5D training projection; (5) get `predict_proba` column-1 probabilities for the *test* set, split by true test labels, run `scipy.stats.ttest_ind(probs_group1, probs_group0, equal_var=False)`, and write the p-value with `f"{pval:.2e}"` to `/home/user/ttest_pvalue.txt`. Watch the ttest argument order (group1 first) and don't reuse test means when centering.
