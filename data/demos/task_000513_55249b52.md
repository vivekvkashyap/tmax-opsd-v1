Part 1: extract frames from `/app/dashboard_capture.mp4` (e.g. `ffmpeg -i dashboard_capture.mp4 frames/frame_%04d.png`) and scan them for the one containing overlaid text — since it's only present for a few frames, either sample frames at intervals and look for a visual change/text presence, or extract all frames and OCR each (e.g. `tesseract`) to find the one describing an ELF analysis; read off the exact port number, TLS certificate Common Name, and 16-byte hex knock payload from that frame's text rather than assuming values, and write them into `/home/user/extracted_intel.json` in the given schema. Part 2: in Rust, generate a self-signed cert with the extracted CN (e.g. via `rcgen`), then build an async listener (`tokio`) on `0.0.0.0:<extracted port>` that inspects each new connection's first bytes before deciding protocol: if the raw bytes exactly match the 16-byte binary knock payload (decode the hex string into actual bytes, don't compare against the ASCII hex text), respond with the literal ASCII `ACK_KNOCK` and close; otherwise treat the connection as TLS (via `rustls`/`tokio-rustls`) and, if it's an HTTPS GET to `/submit_elf`, respond 200 with body `SUBMISSION_ACCEPTED` (a lightweight `hyper` or hand-rolled HTTP response is fine). Compile in release mode, launch it in the background, and manually test both the raw-TCP knock and an HTTPS request with `curl -k` before leaving it running.