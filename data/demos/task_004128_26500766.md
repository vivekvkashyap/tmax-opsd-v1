Scaffold with `cargo new /home/user/scanner` and add `serde`, `serde_json`, and crates for URL-decoding and base64 (e.g. `percent-encoding` or `urlencoding`, and `base64`) to `Cargo.toml`. Parse `/home/user/logs.json` into structs matching the given shape. For each entry: extract the `redirect` query-parameter value from `url` (parse it out manually or with a query-string helper — don't just substring-search the raw encoded text) and fully percent-decode it before testing prefixes/substrings, since the whole point is that payloads are hidden behind encoding — check the open_redirect and xss conditions against the *decoded* string, case-insensitively for the xss substrings. For privesc, strip the `Basic ` prefix from the `Authorization` header if present, base64-decode the remainder, and search the decoded UTF-8 text for the literal `role=admin`; handle decode failures gracefully (skip, don't crash). Collect flags per entry into a `Vec<String>`, sort each entry's flags alphabetically, keep only entries with ≥1 flag, sort the overall array by `id`, and serialize with `serde_json` to `/home/user/findings.json`. Build and run with `cargo run` so the output file actually gets produced, then spot-check it against the input logs by hand.
