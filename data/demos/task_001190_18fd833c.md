Don't try to reverse-engineer the stripped binary's exact math from disassembly alone — use it as a labeled-data generator instead. Run `/app/sim_generator` with many different arguments/seeds to produce a large local corpus, and cross-reference against the provided `/home/user/data/clean/` and `/home/user/data/evil/` samples to find a statistical discriminator: compute, per file, something like the max |x| (or max amplitude/energy) over the first half of the time series versus the second half using `awk` (split rows by line count, track max of `$2` column per half). If the second-half max exceeds the first-half max, the trajectory is diverging (evil); if it's smaller/comparable, it's damped (clean). Validate this rule against every file in both provided sample directories before finalizing — check for edge cases near the split point. Implement `filter.sh` purely in bash+awk (no Python/Perl), reading the CSV, skipping the header, and using `exit 0`/`exit 1` based on the computed comparison. Since the hidden holdout must be 100% correct, stress-test your rule against a wide range of self-generated `sim_generator` outputs (varying seeds) to make sure the discriminator generalizes, not just fits the visible samples.
