Transcribe the audio precisely before writing any code — play or run it through speech-to-text (e.g. `whisper`/`ffmpeg` + an ASR tool) and re-listen to confirm exact numeric parameters (bandwidth, confidence multiplier, degrees-of-freedom setting, rounding precision) since a fuzzed bit-exact comparison against a hidden oracle will fail on any small numeric or formatting deviation. Once you have the parameters, implement the Gaussian KDE at x=0 as the mean over all sample points of `exp(-0.5*((0-x)/h)^2) / (h*sqrt(2*pi))`, and the confidence interval as `mean ± z * sample_std/sqrt(n)` using the sample standard deviation with ddof=1 (not population std, which is a common off-by-one mistake). Match the output format exactly — comma-separated, each value rounded to the specified decimal places using standard rounding, one line to stdout. Before finalizing, generate your own varied test inputs (including edge cases with very few points) and sanity check your script's numeric formulas make sense, since you don't have access to the real internal oracle to diff against.