Run `tesseract /app/loc_rules.png -` (or pytesseract) and read the extracted rules text closely — it will specify exactly how to gap-fill "MISSING" numeric values (e.g. an interpolation formula using neighboring valid values), the numeric threshold that triggers rejection, and the exact forbidden substring to search for in the translation field; do not assume generic rules, use precisely what the image states. Implement the C program to: parse CSV lines split on commas into Timestamp, NumericValue, TranslationString; track the last seen valid numeric value while scanning, and when hitting "MISSING", look ahead to the next valid value to compute the interpolation per the discovered formula, substituting that computed value for the threshold check; scan every TranslationString for the forbidden substring using simple substring search (e.g. `strstr`); if any interpolated value violates the threshold or any string contains the forbidden pattern, exit 1, otherwise exit 0 after processing the whole file. Be careful with edge cases: MISSING at the start/end of file (no preceding or following value), consecutive MISSING values, and trailing newline/whitespace parsing — since the hidden adversarial corpus will exercise boundary conditions precisely at the threshold value.