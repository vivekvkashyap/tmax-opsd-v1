This task has two independent sub-problems: recovering two parameters from audio, and a simulation program that must be numerically exact against a corpus. First, transcribe `/app/lab_dictation.wav` — try `ffmpeg` to convert it to a clean wav/format a speech-recognition tool accepts, or use a Python STT library (e.g. SpeechRecognition with a local/offline engine, or whisper if available); listen to / inspect the transcript carefully since it will spell out a DNA letter sequence and a decimal decay constant that you must extract verbatim rather than guess. Once you have the primer string and k value, write `detector.c` to: read the FASTA file (skip header lines starting with `>`, concatenate sequence lines), then walk the sequence index by index checking with `strncmp`/direct comparison whether the primer matches starting at that position, apply the Euler update `P += S - k*P` each step, and exit(1) as soon as P exceeds 1.5 (you can also just track a max and decide at the end, but early-exit is fine since anomalous is anomalous). Watch the off-by-one edge case at the end of the sequence where the primer can't fully fit — don't index out of bounds. Compile to `/home/user/detector`, then loop it over every file in `/app/corpora/clean/` and `/app/corpora/evil/`, checking exit codes match expectations before finishing.
