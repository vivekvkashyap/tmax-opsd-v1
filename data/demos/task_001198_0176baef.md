Stream the file with a plain `for line in open(path):` loop (never `.readlines()` or loading into a list) so memory stays flat regardless of file size. Set up `logging.basicConfig(filename='/home/user/pipeline.log', level=logging.INFO)` (or a FileHandler) at the top, log the start message, then log `Processed X lines` every time your running line counter is a multiple of 5000, and log the finish message after the loop ends (including a final count message once more if the total isn't itself a multiple of 5000 — re-read the spec's exact expected lines). For the rolling window, use `collections.deque(maxlen=500)` of response times; only after the deque has reached length 500 (i.e., starting from line 501) compute `round(sum(window)/len(window), 2)` for each subsequent line, and append a CSV row when that value is strictly greater than 800.0 — write the CSV header once before the loop starts, not conditionally. Track per-IP totals (`requests`, `errors` where status>=400, `sum_response_time`) in a `dict`/`defaultdict` while streaming, then after the loop compute each IP's average and bulk-insert all rows into a fresh SQLite `ip_stats` table in one `executemany` call rather than row-by-row inserts.
