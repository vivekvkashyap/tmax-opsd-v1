Diagnose the two named ABI/encoding bugs methodically rather than rewriting blindly. First, confirm the encoding chain: base64-decode a payload and inspect the raw bytes (e.g. `xxd`) — you should see two-byte-per-character patterns typical of UTF-16-LE, so decode with `bytes.decode('utf-16-le')` in Python to recover the real string, then re-encode it with `.encode('utf-8')` before it ever reaches the C library, since the library expects a UTF-8 null-terminated C string. Second, fix the ctypes ABI: explicitly set `lib.check_valid_api.argtypes = [ctypes.c_char_p]` and `lib.check_valid_api.restype = ctypes.c_int`, and pass the UTF-8-encoded bytes object (ctypes will null-terminate automatically) rather than raw base64-decoded bytes — passing the wrong bytes or omitting argtypes/restype is exactly the segfault/undefined-behavior source described. Test the fix incrementally: decode one payload by hand, verify the resulting UTF-8 string looks correct, call the library function directly to confirm it returns the expected 0/1, then run the full script over data.json and inspect result.json before declaring done. Do not touch data.json or libfilter.so.