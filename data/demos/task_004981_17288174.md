Build `pipeline.sh` as a thin wrapper that calls into Python (Bash alone struggles with float validation and JSON formatting reliably). Parse each `value` with a strict float parser (e.g. Python's `float()` in a try/except) rather than a naive regex — this correctly rejects garbage tokens while accepting valid scientific notation. After parsing, explicitly check `math.isnan(v)` and `math.isinf(v)` and drop those rows, then apply the inclusive-vs-exclusive bound check exactly as specified (drop when value < -100.0 or > 100.0, strictly). Preserve original row order and the header when writing `cleaned_measurements.csv`. Group survivors by `category`, compute mean and *population* variance (divide by N, not N-1 — do not use a library default that assumes sample variance without checking), then round to exactly 4 decimal places and serialize to `stats.json` with categories sorted alphabetically (Python's `json.dump` on a dict built by iterating `sorted(groups)` preserves key order). Make the script accept `$1` (input) and `$2` (output dir), create the output dir with `mkdir -p`, and chmod +x the script. Run it once at the end to produce both output files for verification.
