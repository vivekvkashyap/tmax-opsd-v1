Implement `/home/user/vm.cpp` as a simple line-by-line interpreter: maintain two integer registers, parse each line by splitting on whitespace and dispatching on the opcode (LOAD/ADD/SUB/OUT), and — critically — maintain an executed-instruction counter that increments once per instruction actually executed; before executing what would be the 11th instruction, stop, print `QUOTA_EXCEEDED` to stderr, and `exit(42)` (use `std::cerr` and `std::exit`, and make sure the counter check happens before execution, not after, so exactly 10 instructions can run successfully). Write `test_success.asm` with <=10 instructions that LOAD values and ADD them to total 15, ending with `OUT R0` (or whichever register holds the result). Write `test_limit.asm` with 12 `LOAD R0 1` lines to trigger the quota. In `run_ci.sh`, compile with `g++ -std=c++17 -o /home/user/vm vm.cpp`, capture the success test's stdout via command substitution and the limit test's exit code via `$?` immediately after invocation, then write the two-line summary in the exact literal format specified. Make the script executable with `chmod +x` and run it yourself, then inspect `ci_summary.txt` to confirm both lines match exactly.