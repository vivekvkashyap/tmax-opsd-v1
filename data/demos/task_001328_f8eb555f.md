Build the Python script as three independent, testable functions rather than one monolithic block. For the fstab parser, read the file line by line, split on whitespace, and filter rows whose filesystem-type field equals `nfs`; collect the mount-point field, sort alphabetically, and write one per line — don't forget a trailing newline convention consistent with a simple `write` per line. For the firewall generator, `json.load` the file, iterate `blocked_ips` in original list order (do not sort), and write `#!/bin/bash` followed by one `iptables -A INPUT -s <ip> -j DROP` line per IP. For the supervisor, use `subprocess.run` in a loop bounded by the restart limit: after each execution check `returncode`; if it is 0, stop immediately without logging or restarting further; if nonzero, append `Crash detected. Exit code: <code>` to the log and loop again, but stop once you've used up the maximum restart attempts even if the last run still failed (don't log a line for a run that never happened). Watch the off-by-one: "3 restarts" means up to 4 total subprocess executions, and the log should only contain crash lines for runs that actually crashed, not for successful or unattempted runs. After writing the script, actually execute it so the three output files exist before finishing.