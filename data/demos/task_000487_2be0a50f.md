Solve this as a graph-join pipeline in pure Bash, not a general programming problem. First build an edge list of paper->paper citations from `citations.txt` (split on " -> "). To find 3-cycles, for each edge A->B, look up all edges B->C, then check whether an edge C->A exists (use associative lookups via `awk` arrays keyed on "from SPACE to", or precompute a sorted edge file and use `join`/`grep -F`). For each cycle found, dedupe: multiple rotations of the same 3-cycle (A->B->C->A vs B->C->A->B) refer to the same set of three papers, so collapse by the sorted trio of paper IDs and only emit each unique triple once. Build `cycle_id` by sorting the three paper IDs lexicographically and joining with `-`. Map each paper_id to its author via `papers.csv` (skip the header) and to author name via `authors.jsonl` (parse with `jq -r` or careful `awk`/`sed`), then sort the three author names alphabetically for the `authors` array. Use `jq -n --arg`/`--argjson` to emit strictly valid JSON per line rather than hand-building strings, and finally pipe all output lines through `sort` on `cycle_id` before writing to `/home/user/circular_citations.jsonl`. Remember `chmod +x` the script.
