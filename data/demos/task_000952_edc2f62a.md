First inspect the database's schema (`sqlite_master`) to enumerate table names dynamically rather than hardcoding shard names — filter for names with prefix `sales_` using a `LIKE 'sales\_%' ESCAPE '\'` query or a Python-side string check, so the script generalizes if shards are added. Load target user IDs from the JSON file. For each shard table, build a query with `SELECT SUM(amount), COUNT(*) FROM {table} NOT INDEXED WHERE user_id = ? AND status = 'COMPLETED'` — the `NOT INDEXED` clause is a hard requirement per the prompt (the premise is that indices are corrupted/misleading), and since table names can't be parameterized, use Python string formatting only for the trusted, schema-derived table name while still parameterizing the `user_id` value to avoid injection on user-controlled input. Accumulate `total_amount` and `transaction_count` per user across all shards (COALESCE nulls to 0 when a shard has no matching rows), including target users with zero matches. Emit the final dict keyed by user_id-as-string with `total_amount` (float) and `transaction_count` (int), and write it with `json.dump`. Run the script and manually recompute one user's totals from the raw table contents to sanity-check your aggregation before finishing.