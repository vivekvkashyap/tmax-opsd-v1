Start by fixing the vendored tool in isolation and testing it standalone: open `/app/log-archiver-1.0/bin/unarchive.sh`, find the reference to an undefined variable used as the decompression command, and either define it (e.g. `GZ_BIN=gzip`) near the top or replace the variable reference with the literal `gzip` command directly — then test it against a manually gzipped file to confirm decompression works before touching the wrapper script. For the archive format, parse the fixed 8-byte ASCII-hex length prefix with something like `head -c 8` and `printf '%d' "0x$hex"`, then extract exactly that many bytes of JSON metadata (skip the header + newline correctly — read the spec's byte offsets precisely), then pipe/redirect the remaining bytes (`tail -c +N`) to a temp file and decompress via your fixed `unarchive.sh`. For log parsing, use `awk` or a state machine that recognizes lines beginning with `[YYYY-MM-DD HH:MM:SS] [SEVERITY]` as record starts, buffering subsequent lines into the current record until the next timestamp line, and only emit records whose severity equals `$2`; join multiple matching records with a single blank line, matching output byte-for-byte since this will be fuzz-tested against many generated archives — test against several hand-crafted or synthetic archives covering edge cases (last record, no matches, multi-line messages) before considering it done.
