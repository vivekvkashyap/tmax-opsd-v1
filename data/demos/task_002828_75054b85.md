Build this as a single pipeline of shell filters rather than a loop with conditionals. Read `/home/user/incremental_manifest.txt` line by line and reject a line if it starts with `/` (absolute path) OR contains a traversal component — specifically the literal substrings `../` or `/..`, or the line is exactly `..`. Be precise about what counts as traversal: a filename like `sneaky..file.txt` contains `..` but not as a path-separator-bounded component, so it must NOT be rejected — match on `\.\.(/|$)` or `(^|/)\.\.` patterns (e.g. with `grep -vE`) rather than a naive `grep -v '\.\.'`, which would wrongly drop that safe file. Pipe the filtered lines into a temp file first — `grep -vE '...' /home/user/incremental_manifest.txt > /home/user/safe_manifest.tmp` — then atomically rename it into place with `mv /home/user/safe_manifest.tmp /home/user/safe_manifest.txt` so a partially-written file is never visible at the final path. Preserve original line order; don't sort.