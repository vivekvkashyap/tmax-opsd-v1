Start with OCR: run `tesseract /app/legacy_api_formula.png stdout` (or similar) and read the extracted text carefully — it describes a precise per-byte numerical transform (a modular arithmetic formula with a conditional branch). Re-derive the formula yourself from the OCR text rather than assuming a common byte-transform pattern; OCR can introduce small errors (misread digits/operators), so sanity-check the extracted formula against a couple of known byte values by reasoning through it manually if possible. Implement payload_transform.c to read stdin byte-by-byte with `fgetc` until EOF, apply the formula exactly as extracted (be careful with operator precedence and mod semantics — C's `%` on non-negative ints matches typical mod here), and print each result as lowercase 2-digit hex with `printf("%02x", ...)` and no separators. For the Makefile: an `all` target building `/app/payload_transform` and a `benchmark` target building `/app/payload_benchmark` from the same source with `-DBENCHMARK_MODE`, both using `-O3`; guard the benchmark-only code (generate 10M random bytes, time the transform, print only milliseconds) behind `#ifdef BENCHMARK_MODE`. Since a fuzzing verifier checks bit-exactness across many random inputs, test your binary against several manually-computed byte values from the recovered formula before considering it done.
