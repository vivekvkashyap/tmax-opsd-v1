Start by getting the vendored C tool to build: open the `Makefile` and check whether the recipe lines under each target use a literal TAB character — `make` silently fails ("missing separator") if spaces were substituted, so retype those lines with a real tab. Next run `bayes_score.sh` and trace the exact binary name it invokes versus the name `make` actually produces; a mismatched filename is a common injected bug, so fix the wrapper to call the compiled binary that Make actually emits. Verify with a quick manual test (`make && ./bayes_score.sh somefile.txt`) before moving on. For the cleaner script, iterate `*.txt` files in the input dir, lowercase and strip punctuation (e.g. with `tr` or `sed`) before scoring — do not score raw text, since the classifier is keyword-based and case/punctuation-sensitive matches will break both directions of the adversarial test (clean docs wrongly dropped, evil docs wrongly kept). Copy files scoring <=50 to the output dir, and log every processed file's KEEP/DROP decision and score to the log file in the required format. Test explicitly against both benign and toxic sample text before considering it done.