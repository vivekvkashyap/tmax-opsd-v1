First recover the secret: install a speech-to-text tool (e.g. `pip install openai-whisper` plus `ffmpeg`, or `pip install SpeechRecognition`) and transcribe `/app/incident_audio.wav`; read the transcript carefully to find the HMAC secret key phrase. Then build the validator as a pure function you can unit-test against both corpora before wiring in the CLI: split the token on `.`, recompute `SHA256(payload_b64url + secret)` and compare hex digests with a constant-time-safe `==`, base64url-decode (with padding correction) and JSON-parse the payload, and check `exp > 1710000000` and `user_id.isalnum()`. Wrap every parsing/decoding step in try/except so malformed or tampered tokens fail safely to INVALID rather than crashing (crashing must also count as reject, i.e. exit 1). Iterate: run your script over every file in `/app/corpus/clean/` expecting exit 0 and every file in `/app/corpus/evil/` expecting exit 1, fixing edge cases (bad signature, expired, non-alphanumeric id, wrong secret) until both corpora pass 100%.
