OCR `/app/config.png` (e.g. `tesseract /app/config.png -`) to read the exact `REF_VECTOR` (x,y,z) and `THRESHOLD` values — don't hardcode a guess. In the C program, parse the CSV skipping the header line, then for each data row parse id,x,y,z as int/float (watch for trailing whitespace/newline issues), and compute cosine similarity as `dot(v, ref) / (||v|| * ||ref||)`. Before dividing, explicitly check whether `||v||` (the row vector's magnitude) is zero — treat that row as automatically rejecting the file (exit 1) since the similarity is undefined, rather than letting a divide-by-zero produce NaN/inf that might accidentally still compare true. Track a single boolean across all rows: if a single row fails the threshold or has a zero vector, the whole file is rejected — but keep parsing (or short-circuit) consistently. Compile with `gcc /home/user/validator.c -o /home/user/validator -lm` (link `-lm` for `sqrt`/`pow`). Test the binary manually against a few hand-crafted clean and evil CSVs (including a zero-vector row and a just-below-threshold row) before running it against the full `/app/corpora/clean/` and `/app/corpora/evil/` directories and checking every exit code.