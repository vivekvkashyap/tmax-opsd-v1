Treat this as building a security filter around a black-box binary, not reverse-engineering it — probe `/app/doc_builder`'s I/O behavior empirically (feed it small known input via stdin, observe stdout, note its ~2KB buffer crash threshold) rather than trying to disassemble it. Design `/home/user/sanitize.py --input <file> --outdir <dir>` around three checks that must all pass before anything is piped to the binary: (1) parse `[[INCLUDE_ASSET: ...]]` directives and resolve each referenced path with `os.path.realpath`/`os.readlink` loop-safe resolution — reject (exit 1, no output, stderr message) if resolution never terminates (symlink loop), if the resolved real path escapes the configured `AllowedAssetDir` from `docs_config.ini` (path traversal or hardlink escape), or if any include is otherwise unreadable; (2) inline the asset content in place of each include directive before sizing anything; (3) chunk the fully-inlined text into <=2048-byte pieces, pipe each through `subprocess` stdin/stdout to `/app/doc_builder`, and concatenate the outputs in order. Write the merged HTML atomically — write to a temp file in the same directory (`tempfile.NamedTemporaryFile(delete=False, dir=outdir)`) then `os.replace()`/`os.rename()` it to `<outdir>/<filename>.html`; never open the final path directly for writing. Validate by running your script over every file in `/home/user/corpus/clean/` and `/home/user/corpus/evil/` and confirm exit codes and output presence match expectations for both.