Since /app/graph_oracle is slow and black-box, don't call it per-node for the final computation — instead probe it on a handful of sample node IDs to deduce its exact algorithm (its output for a given node as you vary which incoming edges/timestamps exist), forming and testing a hypothesis (e.g. "sum of weights of incoming edges within some fixed time window ending at that node's latest activity") until your predictions match the oracle's output on several test node IDs. Once you're confident in the formula, implement it purely in SQL/DuckDB over network.csv rather than reusing the oracle at all: for each target node, find its max relevant timestamp, define a window ending there, and sum matching incoming edge weights — this is naturally expressible with a window/self-join query per node, or precomputed via `duckdb.sql` reading the CSV directly for speed across all nodes at once (e.g. join edges to a per-node max-timestamp table, filter by the time window, group and sum). Export node_id,impact_score to /home/user/results.csv for every unique target node. Validate your derived formula against several more oracle queries on distinct node IDs — not just the ones used to form the hypothesis — before finalizing, since the verifier scores by MSE against a hidden golden file and any subtle off-by-one in the time window will show up there.