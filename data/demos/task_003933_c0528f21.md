Build this in clear stages and check each before moving on. First, parse `/home/user/config.txt` as simple `KEY=value` lines to get `ARCHIVE_DIR` and `MAGIC_HEADER` — don't hardcode the paths from any example you've seen, read them from the config so the program generalizes. Second, for each `.rle` file in `ARCHIVE_DIR`, read line 1 and compare it exactly to `MAGIC_HEADER`; skip the file entirely (no `.txt` output) if it doesn't match. For matching files, decompress line 2 by scanning digit-runs followed by one character (accumulate digits into an integer count, then repeat the next char that many times), and write the result to `<basename>.txt` in the same directory. Third, for deduplication: after producing all `.txt` files, group them by file content (e.g. hash each file's bytes), and within each group of size > 1, keep the alphabetically-first filename as the canonical file and replace every other file in that group with a hard link to it using `link()`/`std::filesystem::create_hard_link` (remove the duplicate first, then link) — verify success by checking inode numbers match (`stat`) afterward. Count every replaced duplicate across all groups and write that single integer to `/home/user/link_count.log`. Test with a small self-made RLE pair before trusting the run on the real archive directory.
