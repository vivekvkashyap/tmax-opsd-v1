Structure `apply_update.py` as sequential, fail-fast stages. First recompute `sha256sum update.zip` in Python (`hashlib.sha256`) and compare against the trimmed contents of `update.zip.sha256`; exit(1) with an error message on mismatch before touching the archive. Extract with `zipfile` into a `tempfile.mkdtemp()`. Parse `changes.wal` by splitting each line on `:` (careful: `SET` lines have 4 parts, `DELETE` lines have 3 — don't assume a fixed field count), collect `(txn_id, action, key, value)` tuples, then sort by `int(txn_id)` ascending before applying in order so out-of-order lines in the file don't corrupt the result. Apply SET/DELETE to the config dict loaded from `/home/user/config.json`, then write atomically: dump to a `NamedTemporaryFile` in the same directory as the target, flush/fsync, then `os.replace()` onto `config.json` (never write the target path directly). For ELF metadata, use `pyelftools`'s `ELFFile` to read `header['e_entry']` (format as hex string with `hex()`) and the `.text` section's `sh_size`. Finally sort the resulting config's keys alphabetically and emit the YAML summary with PyYAML, matching the exact structure specified.
