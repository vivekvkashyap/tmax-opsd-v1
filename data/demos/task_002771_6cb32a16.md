Build this as a true single-pass streaming C program: open the file, read with `fgets`/`getline` in a loop (never `fread` the whole file or use `getc` byte buffering into a big buffer), and process one line at a time so memory stays flat regardless of file size. For sanitization, iterate each raw line's bytes and copy through only bytes in [0x20,0x7E] plus '\n' and '\r' into a fixed small buffer before any further parsing — do this before you try to match `] ERROR ` or extract the timestamp, since the injected garbage bytes would otherwise break substring/int parsing. For anomaly detection, maintain a circular buffer (or simple array with modulo indexing) of the last 100 booleans (is-this-line-an-error) plus a running count of errors currently in the window; when a new line pushes the window's error count to exactly 20 for the first time, record that line's timestamp (parse the integer between `[` and `]`) and never overwrite it afterward. Separately keep a total error counter across the whole file, incremented independent of the window. At the end, read the template file fully, do a plain string replace of the two placeholder tokens with the timestamp (or `NONE`) and total, and write the result to the report path. Compile with the exact `gcc -O2` command given and run it before considering the task done.
