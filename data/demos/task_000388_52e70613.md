Process the three gzip chunks as a single logical stream, not independently. Since the input was split by raw compressed-byte boundaries rather than at message boundaries, concatenate the three `.gz` files' raw bytes together first (e.g. `chunk_a.gz + chunk_b.gz + chunk_c.gz` in alphabetical order) and decompress the concatenated blob as one gzip stream — decompressing each chunk file separately will fail or silently truncate, since a mid-stream split isn't a valid standalone gzip file. Use Python's `gzip.decompress()` (or a `GzipFile` reading from a `BytesIO` of the joined bytes) to get the raw `cp1252`-encoded bytes, then `.decode('cp1252')` and re-encode as UTF-8 when writing output — verify a few known accented characters round-trip correctly. Split the decoded text by lines, and write out fixed-size chunks of exactly 500 lines each into `/home/user/organized/clean_log_XXX.txt` (zero-padded 3-digit index), with the final file holding the remainder (which may be less than 500). Create the `organized` directory first if missing, and check line counts on the output files after running to confirm the chunking landed exactly on 500/500/remainder.
