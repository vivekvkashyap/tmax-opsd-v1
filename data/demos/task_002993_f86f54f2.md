First recover the target distribution: open/inspect `/app/formula.png` (view it or OCR with `tesseract`) to read off the unnormalized PDF expression, then derive its log-density by hand so your Metropolis-Hastings acceptance ratio is numerically stable (work in log-space: accept if log(u) < logp(proposal) - logp(current)). Structure the Go project with `go mod init`, then implement a single-chain sampler function (random-walk proposal with a tuned step size, e.g. Gaussian jitter) and launch 4 of them concurrently via goroutines, each writing its post-burn-in samples to its own slice/channel — don't have goroutines write to a single shared slice or map without synchronization, since concurrent append/writes are a data race. For combining chains to compute variance/covariance, avoid naive floating-point summation in an order that depends on goroutine completion order or scheduling; instead, collect each chain's partial sums (or the full sample sets) into a fixed, chain-indexed array/slice and do the final reduction sequentially in a deterministic fixed order (e.g. chain 0,1,2,3, then point-by-point) so repeated runs are bit-for-bit reproducible. Use `sync.WaitGroup` to join chains before reducing. Compute var_x, var_y, cov_xy over the pooled samples and write them to `/home/user/mcmc/cov.json` with those exact keys.