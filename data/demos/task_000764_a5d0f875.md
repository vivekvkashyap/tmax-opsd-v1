Read /home/user/src/smoother.c to confirm its exact CLI contract (window size arg, input path arg, output to stdout) before compiling with `gcc -O2 /home/user/src/smoother.c -o /home/user/bin/smoother` (create /home/user/bin first). Run it as `smoother 5 /home/user/data/raw/spectrum.csv > /home/user/data/processed/spectrum_smoothed.csv`. For optimize.sh, write nested bash `for` loops over mu1 in {120..130} and mu2 in {340..350}, calling `python3 /home/user/src/eval_mse.py <smoothed_csv> $mu1 $mu2` each iteration and capturing its single float output; track the running minimum using bash-friendly float comparison (bash can't compare floats natively — pipe through `awk` or `bc -l`, e.g. `awk -v a="$mse" -v b="$best" 'BEGIN{exit !(a<b)}'`) rather than naive `[ ]` integer comparisons which will silently misbehave on decimals. Keep the MSE string exactly as printed by the python script (don't reformat/round it) when writing the winner. Write the final line to /home/user/best_fit.txt in the exact format `mu1=X,mu2=Y,mse=Z` with no extra spaces, and make optimize.sh executable (`chmod +x`).
