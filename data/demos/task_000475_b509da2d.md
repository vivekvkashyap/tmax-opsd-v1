Build this in the order: PKI, then validator, then firewall spec, testing each piece before moving on. For crypto: use `openssl genrsa -out ca.key 2048` and `openssl req -x509 -new -key ca.key ... -out ca.crt` to make a self-signed CA, then `openssl genrsa -out server.key 2048` plus `openssl req -new` + `openssl x509 -req -CA ca.crt -CAkey ca.key -CAcreateserial` to sign the leaf cert; verify the chain with `openssl verify -CAfile ca.crt server.crt`. Immediately `chmod 400 /home/user/app/keys/server.key` and confirm with `stat -c "%a"`. For the validator: use PyJWT's `jwt.decode(token, public_key, algorithms=["RS256"])`, extracted via `cryptography` from `server.crt` — the critical mistake to avoid is passing a permissive `algorithms` list or omitting it; PyJWT's algorithm-confusion protection only works if you hard-code `algorithms=["RS256"]` so tokens signed with `none` or HS256 (which would otherwise let an attacker reuse the public key as an HMAC secret) are rejected. Wrap decode in try/except and print exactly `VALID` or `INVALID`. Test locally by forging both an `alg=none` token and a properly RS256-signed one using the real private key. Finally write the two `iptables` rules to `firewall.sh` exactly as specified, ACCEPT rule before DROP rule.