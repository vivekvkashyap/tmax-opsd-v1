Set up the venv and installs first (`python3 -m venv /home/user/venv`, then pip install pytesseract Pillow pytest; also ensure `tesseract` the CLI/OCR engine is available, installing via apt if missing). Run `tesseract /app/system_blueprint.png stdout` (or use pytesseract.image_to_string) to recover the embedded environment ID text — do not guess it, actually run OCR and inspect the output, trimming whitespace/newlines. For the sanitizer, build a state-machine scanner rather than a single regex: walk the string character-by-character (or byte-by-byte, decoding with error handling so malformed UTF-8 itself triggers rejection) tracking state for things like path-traversal sequences (`../`, absolute paths to sensitive dirs), shell metacharacters/command substitution (`$(`, backticks, `;`, `|`, `&`), and invalid encoding — reject on any hit. Validate against both corpora directories directly (glob every file in clean/ and evil/) rather than hardcoding a handful of examples, since there are 50 files each. In the pytest suite, mock the OCR call (e.g. monkeypatch or a fake reader function) to return the ID you already extracted, and assert it matches. Redirect `pytest -v` output with `tee`/`>` to /home/user/test_report.log and confirm it shows 0 failures before finishing.
