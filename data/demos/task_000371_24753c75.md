Build the pipeline stage by stage and validate each before moving on. Use `ffmpeg -i /app/traffic.mp4 -vf fps=1 frame_%04d.png` (or similar) to extract frames at exactly 1 fps into an ordered sequence, then for each frame load it with a library like OpenCV/PIL, convert to grayscale, and take the mean pixel value as brightness — keep frame index as an integer counter starting at 1, matching extraction order exactly, since correlation is sensitive to index-brightness pairing. Before computing statistics, explicitly check for and drop/coerce any NaN values introduced by a faulty merge step (e.g. mismatched frame/index lengths from off-by-one extraction), and ensure the index column stays integer type rather than being upcast to float during a join. Compute Pearson correlation between frame index and brightness with `scipy.stats.pearsonr`, which also gives you the p-value for the two-sided t-test of correlation significance directly — prefer this over hand-rolling the t-test to match the verifier's reference computation exactly. Then stand up a lightweight HTTP server (e.g. Python's `http.server` subclassed, or Flask) bound to `127.0.0.1:8080` that serves `GET /stats` returning the exact JSON shape `{"correlation": <float>, "p_value": <float>, "reproducible": true}`, and leave it running as the final step.
