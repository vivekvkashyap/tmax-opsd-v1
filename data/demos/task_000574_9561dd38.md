Work through this as four separable subsystems and verify each in isolation before wiring them together. First fix nginx.conf: check the `proxy_pass` upstream port against the port the Flask app actually binds (5000) and correct any mismatch, then reload nginx. Fix the Flask backend's Redis connection string/env var to point at 127.0.0.1:6379 with the right DB index. Second, read backend/app.py's /process handler for two-lock acquisition: if one code path locks lock1 then lock2, and another locks lock2 then lock1 under concurrent requests, that's a classic lock-ordering deadlock — fix it by enforcing a single, consistent global lock acquisition order everywhere (or replacing with a single lock / lock-free structure), then load-test with concurrent curl requests to confirm no hang. Third, recover exfiltrated.db using its WAL file (e.g. `sqlite3 exfiltrated.db "PRAGMA wal_checkpoint(FULL);"` or open normally so SQLite auto-replays the -wal), then query the payload_rules table for the actual checksum formula — do not assume a formula, read what's stored. Finally implement detector.py using that recovered formula plus careful binary header/length-prefix parsing (validate lengths against actual buffer size, handle malformed/truncated headers as non-matching) to print EVIL/exit 1 or CLEAN/exit 0, and test it against both corpora before considering it done.