Start by reading `/home/user/app/auth_backend.py` end-to-end to understand the token format: how `authenticate()` builds a token and what field `get_admin_data()` checks. Write a small helper Python script that imports the module directly (not by re-implementing it) and calls `authenticate("guest", "guest")` to see the real token shape — decode it to see it is just a base64-encoded JSON object with a `role` field. Since nothing cryptographically signs or validates the token's origin, you can forge an admin session by base64-encoding a hand-crafted JSON object with `"role": "admin"` yourself, then pass that forged token into `get_admin_data()` to retrieve the protected secret. Map the two weaknesses to CWE IDs: the missing verification that the token actually came from a trusted `authenticate()` call is CWE-287 (Improper Authentication), and trusting a client-controlled `role` field for authorization is CWE-269 (Improper Privilege Management). Finally, write `/home/user/audit_report.json` with exactly the three required keys, using the CWE IDs given in the task and the secret string your forged call actually returned — don't guess it, print it from your script.
