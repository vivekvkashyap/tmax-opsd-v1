Write the migration as a bash script that queries v1_artifacts (e.g. `sqlite3 -separator '|' /home/user/artifacts.db "SELECT id, webhook_url FROM v1_artifacts"`) and processes each row in a loop. Parse the path portion with a regex or `sed`/parameter expansion to pull `<project_name>` and `<version>` out of the URL segments after `/artifacts/`, and extract the `file` and `encoded_meta` query-string values (split on `?` then `&`, then on `=`). Critically, URL-decode `encoded_meta` before base64-decoding it — it's percent-encoded first (e.g. `%3D` is a literal `=` needed for valid base64 padding), so decode percent-escapes with something like `printf '%b' "${val//%/\\x}"` or `python3 -c 'import urllib.parse,sys; print(urllib.parse.unquote(sys.argv[1]))'`, then pipe that through `base64 -d` to get the `branch=...,commit=...` string, and split on comma/equals to get the two values. Insert each parsed row into v2_artifacts with the same id using `sqlite3 ... "INSERT INTO ..."`, being careful to shell-quote extracted values properly to avoid breaking the INSERT on special characters. Verify by re-querying v2_artifacts and diffing against the source rows by hand for at least one entry.
