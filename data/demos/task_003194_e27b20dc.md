Build this bottom-up: parser, then similarity graph, then connectivity, then the wrapping harness. In `sequence_graph.cpp`, write a minimal FASTA parser that reads `/home/user/input.fasta` by splitting on `>` header lines and concatenating subsequent lines into each sequence (case-sensitive, no normalization). For each sequence, precompute its set of contiguous 3-mers (all length-3 substrings). Build the pairwise adjacency with OpenMP (`#pragma omp parallel for`, being careful with shared-state writes — e.g. accumulate per-thread results or use critical sections when writing edges) by checking, for each pair A != B, whether their 3-mer sets intersect. Then run BFS or DFS over the resulting adjacency to label connected components, tracking each component's size, and write `Components: <N>` / `Largest_Component_Size: <M>` to `/home/user/graph_stats.txt` in exactly that format. In `run_pipeline.sh`, compile with `g++ -fopenmp -O3`, run the binary, `diff` the generated stats file against `/home/user/expected_stats.txt`, print `PASS`/exit 0 on exact match or `FAIL`/exit 1 otherwise, and `chmod +x` the script. Run `./run_pipeline.sh` yourself to confirm PASS before finishing.
