Build the pieces independently and test each before wiring them together. For the FastAPI backend, install `capstone`, decode the incoming hex string to raw bytes, and disassemble with `Cs(CS_ARCH_X86, CS_MODE_64)`, iterating over `md.disasm(code, 0)` and checking each instruction's `.mnemonic` against the banned set (`syscall`, `int`, `int3`, `sysenter`) — match on mnemonic name exactly (case-insensitively), not substring search on raw bytes, since that would both over- and under-match. Return 403 with the exact error JSON on any match, else 200 with the exact safe JSON; make sure malformed/undecodable hex doesn't crash the endpoint. For nginx, edit `/home/user/app/nginx/nginx.conf` to `listen 8080;` and add a `location /api/ { proxy_pass http://127.0.0.1:5000; }` block (preserve the `/api/` prefix or adjust proxy_pass path matching consistently), then start `uvicorn` on 5000 and `nginx` pointed at this conf, both backgrounded. Before declaring done, manually curl every file in `/home/user/app/corpora/evil/` and `/home/user/app/corpora/clean/` through `http://127.0.0.1:8080/api/execute` and confirm 403/200 respectively for 100% of them — a single misclassified sample fails the task.