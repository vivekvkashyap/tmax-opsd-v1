Before writing the server, learn the scorer binary's exact CLI contract interactively: run `strings /app/scorer` to spot usage text, then invoke it directly with sample args (`/app/scorer "Hello" "Hola"`) and inspect stdout to see the exact output format you need to parse (e.g. a line like `Score: 0.XX`) — extract the float with a regex rather than assuming a fixed column position. Vendor `cpp-httplib` (single header) into your workspace and build the server around it: a `POST /translations` handler that parses the JSON array (a small header-only JSON lib, or hand-roll minimal parsing), then dispatches one `scorer` invocation per entry concurrently (e.g. `std::async`/thread pool) using `popen`/`fork+exec` with `argv` passed as separate arguments (not shell-concatenated, to safely handle Unicode/spaces) and captures its stdout. Maintain a `std::mutex`-guarded map from language to a fixed-size deque (max 5) for rolling averages, and a separate mutex-guarded map keyed by language for the most recent sub-0.5 sample, writing `/home/user/low_quality_samples.json` atomically (write to a temp file, then `rename()`) whenever it updates. Implement `/stats` to report per-language averages and `/flush` to clear the rolling stats structure. Finally write `flush_pipeline.sh` to curl `POST /flush`, `chmod +x` it, and register it in the crontab to run every minute before leaving the compiled server running in the background.