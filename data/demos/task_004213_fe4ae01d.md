This has two independent halves — get the config recovered first, then build and fuzz-test the script against edge cases yourself. For recovery: use `ffmpeg` to extract frames from `/app/config_scroll.mp4` at the stated intervals, OCR each frame with `tesseract`, concatenate the five recovered base64 fragments in chronological (timestamp) order, base64-decode the combined string, and `tar xzf` the result to obtain `base.ini`; copy it to `/home/user/base.ini`. Watch for OCR noise (stray characters, wrong-case letters) — validate by checking the decoded bytes actually form a legitimate gzip/tar stream before trusting the extraction, and diff against valid base64 alphabet if `tar` fails. For `apply_config.sh`, use `awk`/`sed` to maintain an in-memory or temp-file representation of sections and key-value pairs, applying ADD/MOD/DEL per line from stdin exactly per the stated semantics (MOD/DEL on missing keys are no-ops; empty sections persist), then emit sections and keys each sorted alphabetically in the exact `[Section]`/`Key=Value` format shown. Use `trap ... EXIT` to clean up any temp files, and stress-test manually with varied ADD/MOD/DEL orderings — including duplicate adds and deletes of nonexistent keys — before considering it done, since it will be fuzzed against many random changelogs.
