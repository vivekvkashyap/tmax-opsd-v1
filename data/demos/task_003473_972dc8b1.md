Tackle this in the order the pieces unblock each other. First transcribe `/app/sys_audio.wav` with whatever speech-to-text tool is available (whisper CLI, python whisper, or ffmpeg-piped recognition) and read the transcript carefully to identify each stated "X to Y" dependency edge, writing each as `NODE_A->NODE_B` (uppercase/lowercase and exact node names matter — use what's spoken, normalized consistently) into `missing_edges.txt`. Second, fix `rust_backend/src/lib.rs`: the borrow-checker error is almost certainly a function returning a reference/pointer to a String that's dropped at end of scope — instead, own the validation result as a `bool`/`c_int` return value (or write into a caller-provided buffer) rather than returning borrowed/owned string data across the FFI boundary; keep the extern "C" signature the C side expects, then `cargo build --release` to produce `librust_backend.so`. Third, fix the Makefile's link line so libraries are ordered with the object files first and dependencies later (`gcc main.o -L. -lgraph_utils -lrust_backend` after ensuring `-L` paths point at the built .so locations, plus rpath/LD_LIBRARY_PATH so runtime linking works), then `make all`. Fourth, implement `validate_file`: call the Rust FFI to check strict ASCII/safe-char encoding, parse the file into an edge list merged with `missing_edges.txt`, and run cycle detection via DFS with a recursion/visiting-set (white/gray/black coloring) to catch back-edges; return exit code 0 only if both encoding is valid and no cycle exists. Batch-test against every file in `/app/corpus/clean/` (expect 0) and `/app/corpus/evil/` (expect nonzero) with a shell loop before writing the summary log, fixing any misclassifications by checking whether the bug is in the encoding check or the cycle check.