Work this in strict order since each stage feeds the next. First transcribe `/app/incident_voicemail.wav` with a local speech-to-text tool (e.g. `whisper-cli` or `whisper`) and read the transcript carefully to pull out the three IPs and the error-threshold percentage — do not assume values, extract them from the actual transcription output. Second, diagnose the SSH failure methodically: check `ls -la /home/user/.ssh/` — sshd commonly refuses key auth when `.ssh` or `authorized_keys`/private key have group/world-writable permissions, so fix with `chmod 700 ~/.ssh`, `chmod 600 ~/.ssh/authorized_keys ~/.ssh/id_rsa`, and verify ownership; then test with `ssh -i /home/user/.ssh/id_rsa -p 2222 -o PasswordAuthentication=no user@127.0.0.1` before adding `-L 8080:127.0.0.1:9090` for tunneling, and curl the log through the tunnel. Third, in your bash script, filter raw_telemetry.log to just the three extracted IPs, truncate timestamps to the hour, average RESPONSE_TIME_MS per hour with awk/datamash, and write the CSV. Fourth, compute the 5xx rate over the filtered traffic and compare against the extracted threshold to decide whether to emit the MIME alert file. Finally write the logrotate.conf and validate with `logrotate -d /home/user/logrotate.conf`.
