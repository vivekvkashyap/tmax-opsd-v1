Write the C++ program to stream through sensor_data.csv line by line, keeping per-sensor state in a map keyed by the *normalized* sensor_id (trim whitespace, uppercase). For each row: try to parse value as a float (skip row if parse fails or value < 0), then check the row's timestamp against that sensor's last-accepted-timestamp — only accept if strictly greater (this is what naturally discards exact duplicates and stale retries, so don't add separate dedup logic). Maintain a small fixed-size rolling window (e.g. a deque capped at 3) of previously accepted values per sensor; compute the average of that window *before* pushing the current value in, flag an anomaly if current value > 2.0 * that average (skip the anomaly check entirely when the window is empty), then push the current value and evict the oldest if the window exceeds 3. Write anomalies to anomalies.csv with value/rolling_avg formatted to exactly 2 decimals (e.g. `std::fixed << std::setprecision(2)`), including the header. Trace through the sample data by hand to confirm your window/ordering logic before compiling with g++ and running.