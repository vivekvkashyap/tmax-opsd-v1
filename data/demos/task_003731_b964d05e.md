Reverse-engineer the legacy binary's flaw before writing the detector — run `strings /app/auth_redirector | grep -i internal` and disassemble the relevant comparison with `objdump -d` to confirm it does a raw suffix check on the URL text (e.g. `endsWith("internal-app.local")`) rather than proper host parsing, which is why domains like `attacker-internal-app.local` (no dot boundary) or userinfo tricks like `attacker.com\@internal-app.local` slip through. Build your `/home/user/detector` to actually parse the URL properly: extract the true authority/host component using a real URL parser (or careful manual splitting that correctly identifies the host is what appears after the last unescaped `@` and before the first `/`,`:`, or `?`), then require the host to be exactly `internal-app.local` or end with `.internal-app.local` (checking for the preceding dot, not just the bare substring) — reject anything else, including credential-embedding tricks. Base64-decode the single CLI argument first, locate the `redirect_url=` parameter after splitting on `&`, then apply this strict host check; exit 0 for safe, 1 for malicious/tampered/privilege-escalation payloads. Validate against every file in `/app/corpora/clean/` and `/app/corpora/evil/` directly before considering the detector done.