Build the pipeline stage by stage, testing each before wiring together. First get the raw frame stream working: `ffmpeg -i /app/video.mp4 -vf fps=10 -pix_fmt gray -f rawvideo -` piped into your C program; probe dimensions with `ffprobe -show_streams` so the program knows the byte size of one frame. In C, read frame-sized chunks from stdin in a loop; every 30 frames (or at EOF) close the current `gzFile` (opened via `gzopen(path, "wb9")` for max compression) and open the next `chunk_NNN.gz`. Whenever you open a new chunk, `unlink()` the old `/home/user/chunks/latest.gz` and `symlink()` it to the newly-opened chunk's path, so it always tracks the chunk being written or last completed. Track each chunk's frame count and compressed size (`gzoffset`/`stat` after close) to build `index.csv` with the required header, then convert with `iconv -f UTF-8 -t UTF-16LE index.csv -o index_utf16.csv`. Validate with `gzip -t` on each chunk and sum sizes against the threshold before finishing.