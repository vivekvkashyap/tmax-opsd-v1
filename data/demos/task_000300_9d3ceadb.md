First recover the actual prior parameters from the screenshot — don't guess or leave placeholders. Run OCR (`tesseract /app/legacy_report.png stdout`, or crop/upscale the image first if OCR misreads digits) and manually sanity-check the extracted mean vector and covariance matrix are plausible (e.g. covariance matrix should be symmetric and positive-definite, since it will need a Cholesky factorization to sample from). Then scaffold a Go module (`go mod init`, `go get gonum.org/v1/gonum/...`) and write `main.go` using `distmv.NewNormal(mean, covMatrix, source)` (gonum handles the Cholesky decomposition internally) to draw exactly 50,000 samples. Stream samples to `synthetic_data.csv` with header `f1,f2,f3`. Log the OCR'd parameters plus a generation timestamp to `experiment_log.json`. Since grading compares the empirical mean/covariance of your generated data against the true (OCR'd) values with a tight tolerance, verify your extracted numbers are exactly right before generating — a small OCR misread (e.g. sign or digit error) will fail the metric even with correct sampling code.
