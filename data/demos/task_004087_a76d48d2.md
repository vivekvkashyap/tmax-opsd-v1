First understand the buggy query's actual problem: it's an implicit cross join across all four tables with no join conditions linking Nodes to Edges, producing a Cartesian explosion. Rewrite it as an explicit self-join on Edges: join Edges E1 (A->B) to Edges E2 (B->C) ON E1.dst = E2.src, both filtered to type='friend', then apply `WHERE E1.src != E2.dst` (exclude A=C) and a `NOT EXISTS` (or `LEFT JOIN ... IS NULL`) subquery checking no direct friend edge exists between E1.src and E2.dst — this correctly excludes A-C pairs that already have a direct edge, not just any edge. Add `ORDER BY E1.src ASC, E2.dst ASC`. For pagination, don't load results into a C++ vector at once — bind an OFFSET parameter, execute with `LIMIT 5 OFFSET :offset`, loop incrementing offset by 5 until a fetch returns zero rows, appending each page to the output CSV as you go. Save your exact final query text to fixed_query.sql, then separately run `sqlite3 graph.db "EXPLAIN QUERY PLAN <query>"` and redirect output to query_plan.txt to confirm it uses indexed SEARCH/SCAN rather than a nested-loop cross product. Trace through the sample data manually to sanity-check which A->B->C paths survive both filters before trusting the compiled output.