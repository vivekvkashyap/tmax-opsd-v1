Build the detector as a standalone, deterministic numerical check, not a heuristic. In `/home/user/detector.py`, open the HDF5 file with `h5py`, read `matrix_X`, compute `X.T @ X`, and get its condition number with `numpy.linalg.cond(...)` (or `np.linalg.svd` and take max/min singular value ratio) rather than trying to invert the matrix yourself — inversion is exactly what crashes on singular inputs, so wrap any linear-algebra call in a try/except and treat exceptions or infinite condition numbers as EVIL. Compare against the threshold 1e5 with strict `<` for CLEAN, printing exactly `CLEAN` or `EVIL` with no extra text. Validate against both `/app/corpora/clean/` and `/app/corpora/evil/` by looping over every file and checking the printed output before touching the API. For the Flask integration, use `subprocess.run([...], capture_output=True, text=True)` to call the detector synchronously after saving the upload but before pushing to Redis, branch on the stripped stdout, and only then restart the Flask process (kill and re-launch via the provided start/stop scripts, don't just reload the module) so the new code path is actually live when the verifier hits `/upload`.
