Diagnose before rewriting: read `nginx.conf`'s `upstream`/`proxy_pass` block to see what port it expects the backend on, then read `backend.c` to find where its `bind()` call uses a different port — fix the C source's port constant to match nginx's expectation (don't change nginx's config to match the buggy backend). Separately, scan `backend.c`'s request-handling loop for any blocking delay (e.g. a `sleep()` call) sitting in the hot path per-connection — that's the throughput bottleneck under load; remove it entirely. Cross-compile for ARM64 exactly as specified (`aarch64-linux-gnu-gcc -static ...`) since the binary must run under `qemu-aarch64-static`, then start the backend under QEMU in the background, followed by nginx pointed at the fixed config. Confirm end-to-end health with a plain `curl` before running any load test yourself. For `monitor.sh`, poll `curl -o /dev/null -w '%{http_code}'` against the proxy on an interval, and on a 502 or curl failure, `pkill -f backend_arm` (or track the PID) then relaunch `qemu-aarch64-static backend_arm` — keep the polling loop simple (a `while true; do ... ; sleep N; done`) since the grader just needs recovery to work, not sophistication.