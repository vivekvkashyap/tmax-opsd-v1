Implement and unit-test the shared sanitization logic once, then reuse it in both the standalone script and the worker to avoid drift. For each line: attempt `json.loads`, catching decode errors (including malformed unicode escapes — Python raises on unpaired surrogates during decode/encode round-tripping, so explicitly try encoding strings to utf-8 with strict errors to catch them); recursively walk all string values checking for Unicode bidi control characters (codepoints like U+202A-U+202E, U+2066-U+2069) and reject if found; and if a `weights` key exists, verify every value is finite (`math.isfinite`) and `abs(sum(weights) - 1.0) <= 1e-6`. Drop (skip) any line failing these checks; otherwise print it. For the worker, wrap the same validation function, then apply `unicodedata.normalize('NFKC', service_name)`, build a canonical form (sorted-key JSON with NFKC-normalized strings and weights rounded to 4 decimals via `json.dumps(..., sort_keys=True)`), hash it with `hashlib.sha256`, and track seen hashes in memory to dedupe before POSTing to the Store API with `Content-Type: application/json`. Test the worker live: push a payload into Redis with `LPUSH raw_configs`, confirm it reaches the Store API, and confirm a resubmitted equivalent (differently formatted but same canonical form) is dropped.