Treat `/app/query_embedder` as a black-box CLI oracle: for each row in `data.csv`, pipe the `text` field to it (e.g. `echo "$text" | /app/query_embedder`) and capture the comma-separated 64-float output — don't try to reverse-engineer or reimplement its internal algorithm. Build a small Python step (pandas + sklearn) that loads all embeddings into a matrix, fits `PCA(n_components=2)`, transforms it, and writes `id,pca1,pca2` rows to `/home/user/reduced.csv`, keeping row order aligned with `data.csv`. Append the exact `Run complete: N records processed` line to `/home/user/experiment.log` where N is the row count (excluding header) you actually processed. For the server, write `/home/user/serve.sh` as a loop that binds a TCP listener on `0.0.0.0:8888`, and for each accepted connection reads one line, shells out to `/app/query_embedder` with that line as stdin, writes the raw output back to the socket, then closes that connection and loops to accept the next one (don't exit after one client) — launch it with `nohup ... &` or similar so it stays alive in the background, and confirm with `nc`/`ss` that it's actually listening before finishing.