Work top-down through the chain since each part depends on the previous one working. First diagnose the SSH auth failure by reading `/app/ssh/sshd_config` closely — look for a directive that silently disables key auth (e.g. an `AuthorizedKeysFile` pointing somewhere invalid like `/dev/null` instead of the real authorized_keys path under `/app/ssh/`), fix it, then restart via the provided `manage_services.sh restart sshd` and confirm you can actually authenticate with `deploy_key` (e.g. `ssh -i /app/ssh/deploy_key -p 2222 user@127.0.0.1 -o BatchMode=yes echo ok`) before moving on. Second, open the local forward tunnel in the background with something like `ssh -i deploy_key -p 2222 -N -f -L 8081:127.0.0.1:9090 user@127.0.0.1` (the `-N -f` combination avoids needing an interactive shell), and verify port 8081 actually proxies to the backend with a manual curl. Third, edit `/app/nginx/nginx.conf` to add the missing `proxy_pass http://127.0.0.1:8081;` in the location block for port 8080, then restart nginx via the service script and curl 8080 directly to confirm the full chain works. Fourth, write `/app/logrotate.conf` targeting `/app/logs/backend.log` with `size 100k`, `rotate 3`, `compress`, no `su` directive, and validate syntax with the exact `logrotate -s ... -d` style dry run before the real run. Only after confirming each hop manually should you run `/app/scripts/benchmark.sh` and check the resulting `success_rate` in `/app/benchmark_results.json` meets the 0.95 threshold.
