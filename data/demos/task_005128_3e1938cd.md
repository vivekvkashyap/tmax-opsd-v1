Start by getting the vendored package installable: open `/app/vendored/fsmeta-utils/fsmeta/validators.py`, find the syntax problem (a Python function definition missing its trailing colon is a common vendored-package perturbation), fix just that syntax issue without rewriting the logic, then run `pip install --user -e /app/vendored/fsmeta-utils` and confirm it imports cleanly. Export `TZ=Etc/UTC` in your shell/script before running any timezone validation so IANA lookups are consistent. For validate_mounts.py, implement each rule defensively against adversarial input: resolve `mount_path` with `os.path.realpath`/`pathlib.Path.resolve()` and check the resolved path's parents include exactly `/data` (string-prefix checks alone are insufficient — `/data-evil` must not pass, and `..` traversal must be neutralized by resolution, not by rejecting the literal substring `..`); validate `quota_bytes` is an int, `>0`, and `<= 100*1024**3`; validate `log_timezone` by attempting `zoneinfo.ZoneInfo(tz)` inside a try/except and treating any exception as invalid. Process every `.json` file in the given directory: on full pass, copy the file byte-for-byte into `/home/user/accepted_mounts/` (create it if missing); on any failure, append just the basename to `/home/user/rejected.log`. Test against a few hand-built clean/evil examples before trusting the corpus run.
