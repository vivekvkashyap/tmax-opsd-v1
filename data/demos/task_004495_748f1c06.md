Tackle the four subsystems independently before wiring them together. For WAL recovery, write a C++ reader that walks records sequentially: check the 4-byte magic `0xDEADBEEF` (little-endian), read the 8-byte double, verify its trailing 4-byte checksum equals the XOR of the float's two 4-byte halves; stop at the first record that fails any check rather than resyncing, printing surviving floats to `recovered.csv` at 6 decimals. For the backend, reproduce the hang first (POST huge close-valued arrays to `/compute`) — the likely cause is a variance loop that never terminates on NaN or catastrophic cancellation; switch to Welford's online variance and add a NaN/negative-variance guard. For the thread leak, ensure per-connection threads are properly joined/detached and cleaned up when a client disconnects mid-request (RAII/connection-guard). Write a libFuzzer harness calling the fixed variance function on fuzzer bytes reinterpreted as doubles, add build rules to the Makefile, then run `start.sh` so Nginx (8080) proxies to the backend (8081) and both stay up.
