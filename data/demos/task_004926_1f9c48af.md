Start the environment with `/app/start_services.sh` and confirm Redis, the streamer (8080), and validation API (8081) are actually up before writing code. Build `process.py` as a streaming consumer: use `requests.get(url, stream=True)` and iterate `response.iter_lines()` (never `.text` or `.content`, which buffer the whole 2M-row/100MB body in memory) piped through `csv.reader` on the fly. For validation, drop rows where status_code != 'OK' or temperature fails `float()` conversion, and count only the retained ones. Normalize each timestamp format explicitly: try ISO8601 parsing, then a pure-numeric string as epoch seconds, then `"%Y-%m-%d %H:%M:%S"` — don't assume one format for all rows. For the rolling 60s average per sensor, keep a small per-sensor deque of (timestamp, value) pairs, evict entries older than `current_ts - 60` before computing the mean on each new event (event-time windowing, not a fixed-size buffer) — this keeps memory bounded even at 2M rows. After each valid row, `redis.set(f"sensor:{sensor_id}:avg", f"{avg:.2f}")`. Track total valid rows and max timestamp seen, then write both to `/home/user/processing_summary.json` once the stream ends.