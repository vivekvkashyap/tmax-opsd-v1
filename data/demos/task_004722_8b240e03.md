Diagnose before rewriting: run the existing calc_stats.sh manually on the latencies file and reproduce the crash (it's the classic naive-sum variance formula `(sum_sq - sum^2/n)/(n-1)` — with large numbers like 1e8, `sum*sum` and `sum_sq` are huge and nearly equal, so their difference suffers catastrophic cancellation, and in floating point via awk this can go slightly negative, making `sqrt(var)` fail/error). Confirm this is the mechanism by inspecting the awk block's arithmetic rather than guessing. Fix it by replacing the two-pass sum-of-squares formula with Welford's single-pass online algorithm: maintain running `mean` and `M2`, updating per value as `delta = x - mean; mean += delta/n; delta2 = x - mean; M2 += delta*delta2`, then `variance = M2/(n-1)` at the end — this avoids ever subtracting two large near-equal numbers. Implement this inside the awk (or bc) portion still driven by the bash script, format Mean and StdDev to exactly 3 decimal places with `printf`, and write both lines in the exact `Mean: <value>` / `StdDev: <value>` format to stats_output.txt. Test on the given latencies file and sanity-check the numbers are small, non-crazy values before finishing.
