Treat this as four independent subsystems and verify each before moving on. (1) Video: run `ffmpeg -i /app/pendulum_experiment.mp4 -vf fps=5 /home/user/project/frames_raw/frame_%04d.jpg`, then a shell loop that strips the numeric part of each filename with parameter expansion or `sed`, tests `$(( num % 2 ))`, and copies/moves into `frames/even/` or `frames/odd/` accordingly — check the actual frame count produced rather than assuming a fixed number. (2) Rust: read the borrow-checker errors from `cargo build` output carefully — a common pattern here is holding an immutable borrow of a Vec element while later indexing/mutating the same Vec, which you fix by copying the needed scalar out (`let x = data[0];` instead of `let x = &data[0];`) rather than restructuring the algorithm, since the task forbids changing the math or I/O contract. (3) Cross-compile: build the native release binary with `cargo build --release`, add the `wasm32-wasip1` (or `wasm32-wasi`) target via `rustup target add`, then `cargo build --release --target wasm32-wasip1`, and copy both artifacts to the exact required filenames under `/home/user/project/bin/`. (4) Nginx: configure a server block on port 8080 with `location /assets/even/ { alias .../frames/even/; }` and similarly for odd (note `alias`, not `root`, so the URL prefix is stripped correctly), then start nginx and curl-test both routes.