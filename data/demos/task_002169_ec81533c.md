Treat this as: get the services healthy first, then build/validate a classifier offline, then wire it into the shell pipeline. Run `/app/start_services.sh` (or start Flask/Redis/Node yourself) and confirm each is reachable with a quick `curl`/`redis-cli PING` before writing any pipeline logic. Write a small Python helper that, given text, checks Redis for a cached embedding (key = text) and otherwise POSTs to `http://127.0.0.1:5000/embed` and caches the JSON array result back into Redis. Use `/home/user/data/train.csv` to fetch embeddings for all labeled examples, then fit a simple linear classifier (e.g. sklearn `LogisticRegression`) on the embeddings vs `label`, and persist it (pickle/joblib) to disk. `filter.sh` should, for each row of the input CSV, call a small Python script that does cache-or-fetch embedding lookup then loads the saved model to predict clean/evil, writing only clean rows (plus header) to the output. Since the grader requires 100% separation, verify your classifier gets zero training-set errors before trusting it on the hidden corpora — if it doesn't, the embeddings/labels are the place to debug, not the threshold.
