Work in clear stages and verify each before moving on. First, parse `/home/user/baseline.txt` into a dict of `{filename: hash}`, then walk `/home/user/data/` computing SHA256 per file (`hashlib.sha256(open(f,'rb').read()).hexdigest()`); a file is "changed" if it's absent from the baseline dict or its hash differs. Build the tar with only those relative paths (e.g. `tar.add(path, arcname=relpath)` in Python's `tarfile`, or `tar -cf - -C /home/user/data file2.txt file3.txt`), then gzip it in memory or as a temp file. For chunking, read the gzip bytes in 51200-byte slices; for each slice write to `part-XXX.tmp`, flush and `os.fsync` the file descriptor before closing, then `os.rename` to `part-XXX.bck` (rename is what makes it atomic — never write directly to the final name), and append the exact log line after each successful rename. Zero-pad indices to 3 digits starting at 000, and don't emit a stray empty final chunk if the size divides evenly. Finally rehash all current files and write the new baseline via the same tmp-then-rename pattern.