Do not guess the bug from the description alone — treat it as a black-box equivalence problem. First read `minidiff/core.py`'s `_pure_python_delta` (or equivalent fallback function) closely, focusing on how it slices/strips input before hashing or chunking; the description explicitly says the bug is in "chunk boundary" handling, so look for any `.rstrip()`, trailing-newline trimming, or off-by-one slicing applied to a chunk before its checksum/delta is computed. Write small differential test cases yourself (pairs of strings that differ only in trailing newlines, mixed \r\n vs \n endings, empty-file edge cases) and compare your current implementation's output against what the correct delta semantics should logically produce for those cases — this exposes exactly which boundary condition is mishandled without needing to consult any external reference. Once you identify and fix the stripping/boundary logic, write `/home/user/run_minidiff.py` as a thin CLI that reads both file paths, calls the fixed `minidiff.core.generate_delta`, and prints the result verbatim to stdout with no extra formatting, since the fuzzer will diff byte-for-byte across thousands of randomized ASCII/line-ending combinations.