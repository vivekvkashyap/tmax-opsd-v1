Set up a Rust binary project (`cargo new`) in `/home/user/doc_extractor` and add `tar`, `flate2`, `sha2` (plus `hex` if useful) as dependencies. Open the archive with `flate2::read::GzDecoder` wrapped by `tar::Archive`, then iterate entries via `.entries()?` — for each entry, inspect its path (`entry.path()?`) before extracting anything yourself (don't use the crate's built-in `unpack_in`/`unpack` which may not guard against escapes): if the path is absolute or any component equals `..`, print `WARNING: Zip Slip detected in <path>` to stderr and skip it entirely, without writing anything. For paths that pass, check the extension is `.md` or `.txt`; otherwise silently skip. For valid files, read the entry's contents into memory, create parent directories under `/home/user/docs_safe/` with `fs::create_dir_all`, write to `<name>.tmp`, then `fs::rename` the temp file to the final name so the write is atomic. While processing, compute each file's SHA-256 (`sha2::Sha256`) and store `(relative_path, hex_digest)` pairs; after all entries are processed, sort pairs by relative path and write `/home/user/docs_safe/manifest.txt` with lines formatted exactly like `sha256sum` output: `<hex_digest>  <relative_path>` (two spaces). Build with `cargo build --release` and run it against the real input tarball, then inspect the output tree and manifest for correctness.