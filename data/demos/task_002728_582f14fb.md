Build the pipeline purely with `awk`/`bc`/coreutils, no Python. Compute the Pearson correlation between `f1` and `f2` in a single `awk` pass over the data rows (skip the header with `NR>1`): accumulate sums of x, y, x*y, x^2, y^2 and n, then compute `r = (n*sumxy - sumx*sumy) / sqrt((n*sumx2 - sumx^2)*(n*sumy2 - sumy^2))` — awk's `sqrt` works fine here, and use `printf "%.3f"` to round to exactly 3 decimals for `/home/user/correlation.txt`. Compare that rounded (or raw) value against 0.85 in bash using `awk 'BEGIN{exit !(r>=0.85)}'` or similar, since bash arithmetic can't do floats directly. For `clean_data.csv`, do a second `awk` pass to first compute per-column means (of `f1` and `f3`, whichever columns survive), then a further pass (or store rows in an array in the same awk invocation) to subtract the mean and `printf "%.3f"` format each float, leaving `id` and `label` as plain integers; conditionally emit the `f2` column and its header based on the correlation check. Make the script executable with `chmod +x` and ensure it contains no `python`/`perl` substring anywhere (including in comments) before running it.