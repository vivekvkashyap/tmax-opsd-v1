Start by hunting for the leaked key the way a real recon pass would: search `/home/user/app` for backup/config artifacts (`find . -name '*.bak' -o -name '*.old' -o -name '*~'`, `grep -r SECRET_KEY`) rather than assuming its location — a `.bak` file sitting next to the real source is the classic leak pattern. Read `app.py` to confirm the exact crypto scheme: AES-CBC with a base64(iv + ciphertext) envelope and PKCS7 padding, and note the field name(s) in the JSON payload (e.g. a `role` key) that gate admin access. Grab the captured token from `/home/user/logs/auth.log`, base64-decode it, split off the first 16 bytes as IV, decrypt the rest with the recovered key using `pycryptodome`, unpad, parse the JSON, flip the role field to `admin`, re-pad, re-encrypt with a fresh random IV (prepended to the ciphertext, then base64-encoded) using the same key — don't reuse the original IV. Send the forged value as the `auth_token` cookie via `curl -b` or `requests` to `/admin_dashboard` and save the raw response body to `/home/user/admin_flag.txt`. Separately, add an `@app.after_request` hook in `app.py` that sets the `Content-Security-Policy` header to the exact required value on every response, then kill and restart the Flask process so it's listening on 127.0.0.1:8080 with the new code active.
