This is a black-box reverse-engineering task — do not guess the format, derive it empirically through controlled experiments against `/app/log_packer`, then reimplement independently in Rust. Method: (1) feed minimal, controlled inputs like a single record (`echo -n "hello===\n" | /app/log_packer | xxd`), two identical records, two records sharing a known prefix, and two records with zero shared prefix, comparing byte-for-byte outputs each time to isolate exactly how record boundaries, prefix lengths, and remainders are encoded (e.g. does a length field appear, is it big/little-endian, how many bytes wide, is there a per-record terminator byte); (2) test edge cases — empty input, empty record, record with `\n` inside it, whether the very first record is diffed against an empty "previous" record — to pin down initialization behavior; (3) only once the byte layout is fully confirmed by multiple matching experiments, `cargo new /home/user/log_packer`, implement `src/main.rs` reading all of stdin, splitting on the exact `===\n` delimiter, and emitting the discovered format per record; (4) `cargo build --release`; (5) validate your own binary against the oracle yourself using the same kind of diff tests (`diff <(cmd1) <(cmd2)`) across many random multi-line inputs before considering it done, since the real grader will fuzz-compare outputs byte-for-byte.