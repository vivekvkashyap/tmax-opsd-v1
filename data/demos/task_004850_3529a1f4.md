This is checked by fuzzing your script against a hidden oracle across many CPU_Load values, so the math must match exactly, not just look plausible. First OCR `/app/system_specs.png` with `tesseract` and read the output yourself to get the exact `Alpha` and `Beta` constants — don't guess them. Second, extract CPU_Load/Response_Latency pairs from `/app/raw_metrics.log`: use `grep`/`awk` to keep only lines containing `[METRIC]` and excluding any line with `ERROR` or `TIMEOUT` (a line can have `[METRIC]` and still be a TIMEOUT variant with no Response_Latency field, so filter by both tag presence and the absence of those keywords, and only keep rows where both CPU_Load and Response_Latency actually appear). Compute the least-squares slope `m` and intercept `c` with the standard formulas (`Sum XY`, `Sum X`, `Sum Y`, `Sum X^2`, `n`) via `awk`, rounding each to exactly 2 decimals using half-up rounding (careful: `printf "%.2f"` in awk/bc uses round-half-to-even in some implementations, so verify boundary cases like `x.xx5` round up as required). Hardcode Alpha, Beta, m, c into `/home/user/predictor.sh`, and inside the script use `awk` to compute `Alpha*(m*CPU_Load+c)+Beta` on the large integer input (awk uses doubles so this avoids bash's 64-bit integer overflow), printing only the result to exactly 3 decimal places. Make the script executable and test it against a few log rows before finishing.
