Work the three stages independently and verify each before moving on.

Stage 1: write `/home/user/start_infrastructure.sh` that backgrounds redis-server, `python3 /app/services/sensor.py`, and `python3 /app/services/c2_server.py`, then confirm all three ports are actually listening before declaring success. Build `/home/user/bridge_config.json` with the host/port fields the bot expects for sensor (127.0.0.1:9001), cache (127.0.0.1:6379), and c2 (127.0.0.1:9002) — get the exact key names from how the bridge bot reads its config, not by guessing.

Stage 2: write a driver script that runs the bot repeatedly (looping many iterations, since the crash is intermittent) to reliably reproduce the segfault, checking exit codes/core dumps. Then run `git bisect start`, mark `main` bad and `v1.0` good, and script the bisect run against your reproducer so it repeats each candidate commit enough times to avoid a false "good" from luck. Write the resulting 40-char commit hash to `/home/user/bad_commit.txt`.

Stage 3: treat `/app/oracle_decoder` as a black box — never copy or wrap it as your answer. Feed it controlled inputs (all-zero buffers, single-byte-at-a-time, sequential/incrementing patterns, repeated fixed values at different offsets) and diff input vs output byte-by-byte to isolate whether the transform depends on byte value alone, position alone, or both, and whether it's linear (XOR-style) or a substitution. Once you have a hypothesis, verify it holds across offsets and long buffers, then implement it in `decoder.cpp`, compile to `decoder.bin`, and fuzz-test your binary against the oracle on random inputs of varying lengths before finishing.
