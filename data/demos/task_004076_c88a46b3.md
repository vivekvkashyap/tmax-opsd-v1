Diff the authorized_keys against the known-good list to find the rogue key: split /home/user/.ssh/authorized_keys into individual key lines and compare each against the lines in /home/user/evidence/valid_keys.pub (e.g. `grep -vxFf valid_keys.pub authorized_keys` or a Python set difference) to isolate the one line that's not in the valid set. Write that single rogue key line to a temp file, then run `ssh-keygen -l -E md5 -f <temp_rogue_key_file>` and capture its exact stdout — don't hand-format the fingerprint yourself, use the tool's literal output since the grader string-compares against ssh-keygen's own output. For the payload, base64-decode /home/user/evidence/payload.b64 (`base64 -d`) to get a comma-separated URL list, then in analyze.py parse each URL's hostname (e.g. via urllib.parse.urlparse) and check if it ends in ".xyz" or ".ru" per the stated policy — pick the one URL that violates it (there should be exactly one). Write forensics_report.txt with exactly two lines: the raw ssh-keygen fingerprint output on line 1, and the violating URL on line 2, no extra blank lines.