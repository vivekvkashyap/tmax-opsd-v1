Start by reading legacy_filter.py closely and porting its logic line-for-line into C rather than reinventing it, since the adversarial corpus will catch any semantic drift. Implement URL decoding manually: scan the input buffer, convert `+` to space, and for each `%XX` sequence parse the two following hex digits into a byte (validate they are hex digits; if malformed, decide how to handle gracefully, e.g. pass through literally, matching Python's `unquote_plus` tolerance). After decoding, scan the resulting buffer (using its actual decoded length, not `strlen`, since decoded bytes can include an embedded NUL that would truncate a naive C-string scan) for `<`, `>`, the substring `../`, or any embedded NUL byte before the logical end — flag as evil if found. Print exactly `REJECTED`/exit 1 or `ACCEPTED: <decoded>`/exit 0. Compile with gcc to /home/user/workspace/sanitizer, test it directly via stdin with crafted strings (double-encoded, `%00`, `%2e%2e%2f`) before touching the services. Then edit config.env's FILTER_BIN line, run start_services.sh, and iterate using run_corpus_tests.sh's pass/fail feedback until both corpora hit 100%, finishing with generate_report.sh.