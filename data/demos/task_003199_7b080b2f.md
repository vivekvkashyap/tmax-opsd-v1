Tackle the four objectives in an order that lets you verify each independently. For the CMake fix, first reproduce the failure (`cmake . && make`) to see the linker error, then add the missing directory hint — `link_directories(/opt/qa_libs)` (before the target) or `find_library(QA_UTILS qa_utils PATHS /opt/qa_libs)` plus `target_link_libraries`, without touching any `.cpp`/`.h` source. For the C++→Python translation, read `rules.cpp` carefully and reimplement each substring/regex check faithfully in `analyze_payload(filepath: str) -> bool` (True = safe/clean, False = malicious) — then broaden your own test cases beyond the literal C++ rules since the hidden verifier corpus will probe edge cases (case sensitivity, partial matches, whitespace) the C++ logic implies but doesn't spell out. For the image, run OCR (`tesseract`) on `/app/whiteboard.png` and use whatever port numbers and header name/value it reveals — don't assume typical defaults, extract them. Build `proxy.py` as an `http.server`-based reverse proxy that reads the POST body, calls `analyze_payload` on a temp file, returns 403 on failure, and otherwise forwards the request upstream while injecting the OCR'd auth header. Make all scripts executable and smoke-test the proxy end-to-end with `curl` before finishing.
