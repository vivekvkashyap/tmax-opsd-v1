Transcribe `/app/primer_memo.wav` (via `whisper` CLI or similar) to recover the spoken DNA primer letters — it will just be a short A/C/G/T sequence, read it out carefully since TTS letter-by-letter speech is easy to mis-transcribe. Search `/app/reference.fasta` for that exact substring (strip whitespace/newlines from the FASTA body first) to get its 0-indexed start position P; this P becomes the initial phase-shift parameter for the model fit in `/home/user/fitter.cpp`. For the numerical-stability fix, don't just tweak constants — audit the reduction loop: replace `float` accumulators with `double`, and replace the naive `#pragma omp critical`-style parallel sum (which is non-associative and order-dependent) with either a strictly sequential reduction or Kahan summation so results are bit-for-bit reproducible across runs. Compile and run against `/app/measurements.csv`, fitting the nonlinear model with gradient descent seeded at phase=P, and write the final `theta0, theta1, theta2` comma-separated to `/home/user/model_weights.txt`. Run it multiple times to confirm zero variance before finishing, since determinism is explicitly graded.