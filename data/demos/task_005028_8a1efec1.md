Design disk_nav.c around three independent correctness requirements, and get each one right in isolation before combining them: (1) atomicity — create the temp file with `mkstemp()` in the same directory as the final output (so rename() stays on one filesystem), have every writer (parent and forked children) append to that same temp path, and only `rename()` it to the final output path once in the parent after `wait()`/`waitpid()` has reaped every child; (2) concurrency-safe writes — every process must open the temp file itself and wrap each write with an exclusive lock, e.g. `flock(fd, LOCK_EX)` before the `write()` and `flock(fd, LOCK_UN)` after, so concurrent children never interleave partial lines; (3) traversal — read only the top-level entries of target_dir with `opendir`/`readdir`, `fork()` one child per top-level subdirectory (each child does its own recursive `nftw`-style or manual recursive walk internal to itself, no further forking required unless you want it), and have the parent handle top-level regular files directly and only compare `st_size` against min_size_bytes via `stat()`. Build the test directory with `dd if=/dev/zero of=file bs=1 count=N` (or `head -c N /dev/urandom`) for exact sizes, then compile and run exactly as specified, and manually diff the output before considering it correct.
