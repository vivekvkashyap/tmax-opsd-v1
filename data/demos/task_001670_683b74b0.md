Debug the C++ hang with standard techniques before touching anything else: read `parse_wal_entry()` for the infinite-recursion (look for a recursive call with no shrinking base case, e.g. it re-parses the same offset/entry instead of advancing) and read `main()`'s loop for a missing increment of its index variable `i` — fix both, then compile with `g++ /app/src/db_parser.cpp -o /app/bin/db_parser` and run it standalone on `/app/metadata.wal` first to confirm it terminates and prints events before wiring it into anything else. For the timeline, run `ffprobe -show_frames -select_streams v -of csv /app/debug_run.mp4` (or similar) to get real PTS values from the actual video rather than assuming a framerate, and match each frame timestamp to the nearest db_parser event timestamp only if the absolute difference is <= 0.05s, dropping unmatched frames. For the filter, implement the two literal rules stated — reject if the log contains the substring "DROP TABLE", or if any `SEQ: <n>` value is not strictly greater than the previous one seen — and exit 1 on violation, 0 otherwise; compile to `/app/bin/filter` and test manually against a couple of corpus files before trusting the full pass rate.
