Tackle this as three layered fixes, each testable independently before wiring them together. First, in `calc.py`'s `solve_steady_state`, regularize before inverting: build `L_reg = L + 1e-6 * np.eye(n)` and invert that instead of the raw (near-singular) Laplacian — use exactly `1e-6`, not an approximate value, since the verifier checks this. Second, in `compute_diffusion`, integrate `dP/dt = -L_reg @ P` from t=0 to 5.0 with `scipy.integrate.solve_ivp` (or `odeint`), then wrap it in a bootstrap: resample the initial-state indices with replacement 500 times, rerun the integration each time, collect the final vectors, and take `np.percentile(..., [2.5, 97.5], axis=0)` per node for the 95% CI. Third, for orchestration: start redis with `redis-server /home/user/molecule_sim/redis.conf` (it daemonizes per the conf), export `REDIS_URL`, then launch the Flask app with something like `nohup python3 api.py &` so it survives — verify both ports are actually listening (`redis-cli ping`, `curl` on 8080) before finishing, since the task requires services left running, not just code written. Have `/simulate` store the CI as a JSON string under Redis key `latest_bounds` and return it in the response.
