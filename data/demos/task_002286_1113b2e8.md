The core difficulty is timezone normalization, not string parsing — get all three services onto one common clock (UTC) before correlating anything: parse API Gateway timestamps as ISO-8601 UTC directly; parse Auth Service epoch floats with `datetime.fromtimestamp(x, tz=timezone.utc)`; parse Payment Backend timestamps as naive local time and explicitly add 4 hours (EDT = UTC-4) to convert to UTC — don't rely on any timezone library auto-detecting this, the log format has no tz marker so you must hardcode the fixed +4h offset per the stated spec. Build one dict keyed by the shared ID (`req_id`/`tx_id`/`trace` — same string across all three files) holding: max `retry_count` seen in the payment log for that ID, and whether the gateway log shows `HTTP 503` for that ID. A request "failed" only if retry_count reached >=3 for that ID AND that same ID also has an HTTP 503 gateway entry — don't require a specific ordering between the two if both conditions are just presence-based per the spec, but do keep them scoped strictly to matching IDs. Sort the resulting failed IDs alphabetically and dump as a JSON array of strings to `/home/user/failed_traces.json`.
