Start by transcribing `/app/voice_memo.wav` (e.g. with `whisper` or another local speech-to-text tool) and read the transcript carefully — the exact wording of the conditional text-replacement rule (what triggers the replacement, what gets replaced with what, and what's left untouched) is the spec you must implement precisely; do not guess at plausible rules. Build `/home/user/project_tool.py` in three independent, testable pieces: (1) default stdin/stdout mode applying the transcribed rule line-by-line while preserving original line endings exactly (read with `sys.stdin`, don't strip then re-add newlines carelessly); (2) `--lockfile PATH` support using `fcntl.flock(fd, fcntl.LOCK_EX)` acquired before reading stdin and released via `LOCK_UN` before exit, wrapped so it always releases even on error (try/finally); (3) `--search DIR` that walks recursively (`os.walk` or `Path.rglob`) for `*.log` files modified within the last 48 hours (compare `st_mtime` to `time.time() - 48*3600`), printing absolute paths one per line sorted alphabetically. Test each mode in isolation with hand-crafted inputs before trusting the whole tool, since the verifier fuzzes the transformation logic for bit-exact equivalence against many random inputs.
