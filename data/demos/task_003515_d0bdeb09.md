Extract layer by layer: `tar -xzf project_artifacts.tar.gz -C /home/user/extracted` to get the `.zip` files, then before extracting each zip run `unzip -t` to check integrity, and only then `unzip -o *.zip -d /home/user/extracted` (flattening into the same directory, not per-zip subfolders, since the required symlink targets point at `/home/user/extracted/<name>`). In the Python script, walk the extracted directory and open each file in binary mode, reading just the first 4 bytes to check for the `b'\x7fELF'` magic before doing anything else — skip non-ELF files entirely. For ELF files, seek to byte offset 18, read 2 bytes, and unpack as little-endian unsigned short (`struct.unpack('<H', data)`) to get `e_machine`; map 0x28 to `ARM` and 0x3E to `x86_64`, and be prepared to skip/report unknown values. Use `os.makedirs(..., exist_ok=True)` for each architecture subdirectory, then `os.symlink(os.path.abspath(extracted_path), link_path)` so the link target is absolute, using `os.path.basename` for the link name. Collect every symlink path created, sort alphabetically, and write one per line to the log file. Run the script and confirm the symlinks resolve correctly with `readlink -f`.
