Split this into two independent fixes and verify each in isolation before combining. (1) For the interactive script: write `/opt/auto_init.exp` using standard `expect` idioms — `spawn /opt/init_backup.sh`, `expect "Initialize backup directory?*"`, `send "y\r"`, then `interact` or `expect eof` so the wrapped script's exit status propagates. Update the service's `ExecStartPre=` to invoke the expect script (with correct interpreter path, e.g. `/usr/bin/expect /opt/auto_init.exp`), then `systemctl daemon-reload` and start the service, checking `systemctl status`/`journalctl` if it still fails. (2) For the performance bug: don't guess — grep the `programs/` directory for the I/O buffer size definition/macro and inspect its current value directly; a buffer forced down to a tiny size (e.g. 1 byte) causes massive syscall overhead. Restore it to a normal size (64KB), then check the `Makefile`'s `CFLAGS` for a wrong optimization flag (e.g. `-O0`) and fix it to `-O3`. Rebuild with `make clean && make`, install the resulting binary to `/usr/local/bin/lz4`, and benchmark it against a large (~50MB) file with `time` to confirm the fix actually restores throughput before declaring success.