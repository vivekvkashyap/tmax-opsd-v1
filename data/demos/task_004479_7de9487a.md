First recover the threshold: run `tesseract /app/system_notes.png -` (or with preprocessing if OCR is noisy) and read off the `DEADLOCK_THRESHOLD_MS` value in the recovered text — use that exact number as your edge-activity cutoff. Then build `/home/user/detector.sh` to: read the CSV of `(tx_id, waits_on_tx_id, wait_duration_ms, tx_priority)` from stdin, keep only edges where `wait_duration_ms > threshold`, and treat this as a graph where each node has at most one outgoing active edge — cycle detection reduces to walking each node's chain and noting when you revisit a node already on the current path (that segment is the cycle). For each cycle, pick the victim by sorting members on `(tx_priority asc, wait_duration_ms asc, tx_id desc)` and taking the first. Dedupe victims and print `tx_id`s sorted ascending, nothing else. Since correctness is judged by exact match against a hidden reference across many fuzzed graphs, test against hand-constructed edge cases (self-loops, ties, multiple disjoint cycles).
