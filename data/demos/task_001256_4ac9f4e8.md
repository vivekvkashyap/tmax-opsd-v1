Fix `extractor.c` first: after reading each `[filename] [size]` header, check the filename for the substring `../` or a leading `/` before opening any file — if unsafe, still read and discard exactly `size` bytes from stdin (e.g. loop `getchar()` that many times, or `fseek`/read-and-drop) so the archive stream stays aligned for the next entry, but skip the `fopen`/write entirely for that entry. Compile with `gcc -o extractor extractor.c`. Then run `gunzip -c archive.gz | ./extractor` from inside `/home/user/project` so relative paths resolve correctly and the traversal attempt can't touch `/home/user/secret.txt`. After extraction, read `backup.conf` line by line, and for each listed filename that exists in the project directory, create a hard link into `/home/user/project/backup_dir/` with `ln <file> backup_dir/<file>` (not `cp`, and not a symlink) — hard links are what the verifier checks via matching inode numbers (`stat -c %i`). Finish by writing the literal word `COMPLETED` to `/home/user/project/success.log`.