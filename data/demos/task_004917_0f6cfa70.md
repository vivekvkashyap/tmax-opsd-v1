Build this in the order the spec implies: validation, then correlation, then formatting. First write a chronological-order check that parses each file's own timestamp format (ISO8601 for auth.jsonl/api.csv, the `[DD/MMM/YYYY:HH:MM:SS +0000]` bracket format for db.log) and confirms timestamps are non-decreasing within that file; on the first violation write exactly `ERROR: INVALID_ORDER <filename>` to `/home/user/error.log` and `exit(1)` immediately — don't continue processing after a failure. If all three files pass, move to correlation: for each `login_failed` event in auth.jsonl, convert its timestamp to epoch seconds and scan api.csv for same-IP rows within a closed [-5s, +5s] window (inclusive on both ends — an off-by-one here is a common bug), counting matches; separately scan db.log for lines whose message contains the literal substring `user <username>` and whose timestamp falls in the same inclusive window, capturing the `<LEVEL>: <Message>` portion. Then render the exact Markdown template per incident (sorted by auth_timestamp), using `- None` when no DB logs matched, with a single blank line separating incident sections — match the template's spacing and headers exactly since grading is likely string-exact.
