Transcribe `/app/voicemail.wav` first (e.g. with `whisper`) to recover the exact date and time the engineer mentioned — don't assume a value, derive it from the audio, converting any spoken-word time/date into a concrete timestamp. Search `/app/logs/auth.log` for a failed-login line whose timestamp falls in that exact minute to identify the attacker's IP (grep for "Failed password" near that minute). With the IP in hand, grep it across every file in `/app/logs/` (`auth.log`, `syslog`, `access.log`), since each uses a different timestamp format — `auth.log`/`syslog` use `Mon DD HH:MM:SS` without a year (infer the year from `access.log`'s bracketed timestamps, which do include one), while `access.log` uses `[DD/Mon/YYYY:HH:MM:SS +0000]`. For each matched line, convert its timestamp to Unix epoch with `date -d "..." +%s` (or `awk`/`perl` equivalents), strip the original timestamp/hostname prefix so only the epoch plus the process-name-and-message remainder remains, then `sort -n` the combined set of normalized lines by epoch and write them to `/home/user/timeline.txt`. Build this as a bash pipeline using `awk`/`sed`/`grep`/`date`; since grading tolerates minor formatting variance via string-similarity scoring, focus on getting timestamps, ordering, and the IP-matching correct rather than exact punctuation.