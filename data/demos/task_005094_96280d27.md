Work in stages and verify each before moving on. First, run `nm -D /home/user/project/libcalc.so` yourself to see the real output format, then write `parser.py` as a genuine state machine (e.g. states for "scanning symbol lines" vs "found match") that scans line by line for a token containing the substring `_asm_calc_` rather than hardcoding a guessed name — the symbol name is something you must discover from the binary's export table, not assume. Second, in `server.py`, use `ctypes.CDLL` to load the `.so`, set `.argtypes`/`.restype` to `c_int` for the discovered symbol, and implement `http.server.BaseHTTPRequestHandler` to call it with a=5, b=7 on GET `/`, writing the plain integer as the body; launch it with `&` or `nohup ... &`. Third, edit the Nginx config (a server block with `listen 127.0.0.1:8080; location / { proxy_pass http://127.0.0.1:9000; }`), then `nginx -s reload` or start it fresh, checking `nginx -t` first to catch syntax errors. Finally, curl `127.0.0.1:8080/` yourself to confirm the proxied result before writing `result.json` with the discovered symbol name and computed integer.