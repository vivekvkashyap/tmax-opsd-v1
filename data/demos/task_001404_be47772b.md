First OCR `/app/memo.png` (`tesseract /app/memo.png -`) to recover the exact rolling-window spec, port, endpoint path, and required auth header/value from the handwritten notes — read the OCR output carefully rather than assuming standard defaults, since the port/endpoint/header name and value are all part of what you must extract correctly. Implement the analytic step as a SQL window function against `/app/sales.db` (a `SUM(...) OVER (PARTITION BY store_id ORDER BY date ROWS BETWEEN 2 PRECEDING AND CURRENT ROW)`-style rolling sum, matching whatever window size the memo specifies), run it via `sqlite3 -json` or Python's `sqlite3` module, and shape the rows into a JSON array. For serving, write a small HTTP server (Python `http.server`/Flask, or similar) bound to the host/port from the memo, that on the extracted endpoint checks the required custom header (e.g. via `self.headers.get(...)`) and returns 401 if absent/wrong, otherwise 200 with the JSON body. Launch it in the background (`&`/nohup) so it stays up for the automated `curl -H "<header>: <value>" http://host:port/endpoint` check, and manually curl it yourself first to confirm both the authorized and unauthorized paths behave correctly.