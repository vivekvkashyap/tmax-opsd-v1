Run OCR on `/app/spec.png` (e.g. `tesseract /app/spec.png stdout`) to recover the exact Profiling Index formula rather than guessing it — read the recognized text carefully since OCR can mis-render operators or parentheses, and cross-check the formula's structure makes dimensional sense before coding it. Implement `/home/user/calc_pi.sh` reading `T_seq`, `T_par`, `N`, `C_penalty` as `$1`-`$4` and compute using Bash's `$(( ))` integer arithmetic, matching the formula's exact order of operations (multiplication/division grouping matters for integer truncation — Bash truncates toward zero on division, so replicate the parenthesization precisely as OCR'd, don't simplify algebraically). Print only the final integer with `echo`, no extra labels or whitespace. `chmod +x` the script. Since verification is a large randomized fuzz test against a hidden oracle, write a handful of your own test cases spanning small/large values and edge cases (e.g. N near boundaries) to confirm your truncation behavior matches integer semantics before considering the task done.
