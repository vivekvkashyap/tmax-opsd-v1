Start by reading the actual proxy_pass target from /home/user/nginx/nginx.conf with grep/awk instead of assuming a port — this is the port your Rust service must bind to. Then inspect /app/upstream-service-1.2.0/src/main.rs for the hardcoded TCP listener address; a naive fix that only changes the port in one place but forgets it must match exactly what proxy_pass expects will still 502. Build with `cargo build --release`, then follow the staged-deployment convention already in place: create /home/user/deploy/releases/v2/, copy the release binary there, and repoint the /home/user/deploy/current symlink (remove the old symlink first, don't nest it inside the old target) to the new v2 directory. Launch the binary from the `current` path in the background (e.g. with nohup/disown or setsid) so it survives your shell exiting, and confirm with `curl -i http://localhost:8080` that you get a 200, not just that the process is running — NGINX may still be misrouting even if the binary is up. Only after confirming 200 should you run /home/user/test_load.py, redirecting stdout to /home/user/metrics.txt exactly as specified, since the grader reads success_rate from that file.