Attack the three bugs in this vendored C project independently, verifying each fix before moving on. For the parser hang in `src/query.c`: locate `parse_expr` and trace how it advances its cursor past `(` and `)` — the infinite loop/recursion is almost certainly because the closing-paren case fails to advance the pointer past `)` before returning/recursing, so add the missing pointer increment there and re-test with a nested-parens query to confirm it terminates instead of spinning. For the filtering bug in `src/db.c`: find `match_log` and check how it compares log severity strings — if it's using a bitwise/numeric comparison (`&`, `==` on raw enum ints) instead of an actual string comparison on the level field, replace it with `strcmp(log->level, filter->level) == 0` (or the correct enum equality if levels are already parsed to enums) so only exact-level matches pass. For the auth token: since there's no source for `lib/libauth.so`, treat it as a black box — run `strings lib/libauth.so | grep -i token` (or nm/objdump/gdb) to recover the plaintext token compared inside `validate_token`, write that exact string to `/home/user/admin_token.txt`, and do not patch or bypass the .so itself. Rebuild with `make`, then start `minilogd` backgrounded so it stays alive, and manually test both the TCP ingest (port 8080) and the authenticated HTTP query with a nested-paren ERROR-level query (port 8081) before finishing.