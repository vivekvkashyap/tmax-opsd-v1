Do this in strict compile-generate-fit-test order. Compile `gcc /home/user/simulate_decay.c -o /home/user/simulate_decay -lm` and run it once, redirecting stdout to `/home/user/data.csv` — don't re-derive the data yourself, the C program's fixed seed makes it deterministic. In the Python fitter, use `scipy.optimize.curve_fit` (or similar) for both the single-exponential null model and the double-exponential alternative model; for the double-exponential fit, don't trust a single arbitrary initial guess — the objective is known to have multiple local minima, so loop over several different starting points for (A, lambda1, B, lambda2) (e.g. varying lambda scales across orders of magnitude, or swapping which exponential is "fast"/"slow"), keep the fit with lowest RSS, and treat that as convergence. Compute RSS0 and RSS1 directly as sum of squared residuals from each fit's best parameters. Build the F-statistic with df1 = 2 (extra params: 4-2) and df2 = N-4 using the standard nested-model F-test formula, then get the p-value from `scipy.stats.f.sf(F, df1, df2)`. Write the four lines to `/home/user/stats_result.txt` exactly as specified, each value rounded to 4 decimals.
