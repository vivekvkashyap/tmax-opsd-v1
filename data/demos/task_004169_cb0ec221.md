Write `/home/user/analyze.c` to parse the CSV manually with `fgets`/`strtok` or `sscanf`, splitting each line on the first two commas only and treating the remainder as `review_text` (since the text itself is guaranteed comma-free, but you should still be careful not to assume fixed field widths). Filter to rows where `rating == 5`, then tokenize each matching review's text using `strtok` (or a manual scanner) with the full delimiter set given (space, tab, newline, `.`, `,`, `!`, `?`, `;`, `:`), skipping zero-length tokens that arise from consecutive delimiters, and record the length of each token. Compute the sample mean, then the sample standard deviation using the N-1 (Bessel-corrected) denominator, and the 95% CI as `mean ± 1.96 * (s / sqrt(N))` — implement this with `double` precision and `sqrt` from `math.h`, remembering to link `-lm` when compiling. Format every numeric output to exactly 4 decimal places with `%.4f`, matching the required output block's wording and line structure exactly (including labels and punctuation), and write it to `/home/user/analysis_result.txt`. Create `/home/user/run_pipeline.sh` that compiles with `gcc analyze.c -o analyze -lm` and then executes the resulting binary, and `chmod +x` the script.