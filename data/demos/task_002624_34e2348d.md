Two independent subtasks. For the video: extract frames with `ffmpeg -i /app/dashboard_monitor.mp4 frame_%04d.png` (or read directly with OpenCV/`cv2.VideoCapture` to avoid disk I/O), then iterate frames checking `np.any(np.all(frame == [255,0,0], axis=-1))` (mind BGR vs RGB channel order depending on library) to find the single frame containing a pure-red pixel; write its 0-indexed frame number as a bare integer to `/home/user/breach_frame.txt` — don't hardcode a guessed number, derive it by scanning. For the tunnel detector: parse each line of the `ps -eo args` dump, find tokens where the command basename is `ssh` (handle full paths like `/usr/bin/ssh`), then check the remaining arguments for `-L`, `-R`, `-D` — including when they're glued into a combined short-option cluster like `-NL` or `-fNL` (so check each character after a `-` in short-opt groups, not just first char), not just as standalone args. Any ssh line with one of those flags means evil (exit 1); otherwise if all ssh lines are plain connections, exit 0. Wrap file reading in try/except so malformed or missing files don't crash the script uncontrolled — make sure exit codes stay well-defined for the grader.