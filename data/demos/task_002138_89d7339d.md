Reverse-engineer `/app/legacy_checker`'s constraints empirically by binary search: generate many `.cfg` files varying one field at a time (`Timeout` sweeping integers, `MaxRetries` sweeping integers, `NodeName` trying different prefixes/lengths/digit counts) and observe the exit code for each, to nail down the exact inclusive bounds for `Timeout`/`MaxRetries` and the exact structural pattern for `NodeName` (prefix choices, letter-count range, digit-count). Use the sample files in `/app/samples/` as a sanity check and starting point, not as the full spec — the hidden corpus will probe edges you won't see in the samples. Implement the discovered constraints natively in `/home/user/config_gate.py` as plain integer/regex checks (do not shell out to the legacy binary — that fails the performance and security goals). Separately, build the `ActionCmd` allowlist regex to match only `[A-Za-z0-9 _./-]+` for the *entire* field (anchor with `^...$`), rejecting anything containing shell metacharacters like `;`, `&`, `$`, backticks, or pipes — a common mistake is only checking for a few known-bad characters instead of anchoring a strict allowlist. Normalize whitespace when parsing `Key: Value` lines, then exit 0 only if every field passes every check.
