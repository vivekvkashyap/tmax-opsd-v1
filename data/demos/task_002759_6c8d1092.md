Run `tesseract /app/schema_info.png stdout` (or extract to a file first) to recover the table schemas, foreign-key join columns, and the business rule text — read it carefully rather than assuming a schema, since OCR text can be noisy around punctuation; cross-check by inspecting the actual DB with `sqlite3 /home/user/research_data.db .schema` to confirm table/column names match what OCR reported. The "implicit cross join" bug the user describes means the old script likely selected from multiple tables without proper `JOIN ... ON` conditions; your fixed query must explicitly join researchers → experiments → measurements on their id/foreign-key columns as discovered from the schema. Use `argparse` for `--researcher` and `--min-val`, and pass both into the SQL via parameterized placeholders (`?`) — never string-format them into the query. Apply the business rule(s) extracted from the image (e.g., excluding certain statuses) as WHERE clauses, filter by the minimum value threshold, then `ORDER BY measurement_date ASC, experiment_name ASC`. Emit `json.dumps` of a list of dicts with exactly the keys `experiment_name`, `measurement_date`, `value` to stdout.
