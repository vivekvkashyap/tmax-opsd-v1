Two independent things need fixing in the C++ before it's correct: the join logic and a missing filter. Read `get_coauthors` closely — it iterates the *entire* dataset for every matching row of the target author instead of only rows sharing the same `paper_id`, which is the Cartesian-product bug; fix it by building an adjacency structure (e.g. map from author name to the set of paper_ids they wrote, and map from paper_id to the set of (author,year) entries on that paper), then for the target author walk only their own papers and collect co-authors on those specific papers. Separately, OCR the sticky note at `/app/filter_rule.png` (e.g. `tesseract /app/filter_rule.png -`) to recover the exact year-filtering condition, and apply that condition to `PaperYear` when deciding whether a paper's co-authors count — get the comparison operator and threshold exactly as OCR'd rather than guessing. Deduplicate the coauthors set, exclude the queried author from their own coauthor list, and emit the JSON with the two required keys in that shape. Fix the Makefile/compile command as given, run the server, and curl the endpoint yourself to sanity-check the output before leaving it running.