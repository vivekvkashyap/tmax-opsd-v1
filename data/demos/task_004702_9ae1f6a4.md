Keep the extraction and the math in separate stages so the "no Python for differentiation" constraint is easy to satisfy: write a tiny Python one-liner/script that only reads the `time` and `T` datasets from the given HDF5 file (via `h5py`) and dumps them as two aligned columns to a temp CSV/text file — do no math in Python. Then in compute_deriv.sh, use `awk` to read consecutive rows, compute `(T[i+1]-T[i])/(time[i+1]-time[i])` per adjacent pair, take the absolute value, track a running max, and print the final max formatted to exactly 4 decimal places (awk's printf handles this natively — avoid `bc` unless you handle its lack of built-in abs/negative-format quirks). Make both compute_deriv.sh and test_pipeline.sh executable (`chmod +x`). test_pipeline.sh should run compute_deriv.sh on baseline.h5, compare its stdout against /home/user/baseline_expected.txt with an exact string comparison, and print PASS/FAIL with the corresponding exit code — get this passing first as your correctness check before trusting the script on sim_data.h5. Finally run it on sim_data.h5 and redirect to /home/user/sim_max_deriv.txt, confirming the output format (rounding, trailing newline) matches what test_pipeline.sh expects for baseline.
