Implement this as a small, testable numerical pipeline rather than one big function. (1) Parse each PDB file by reading fixed-width or whitespace-split ATOM records and filtering strictly to rows where the atom-name field equals `CA` (PDB atom name field has fixed columns — splitting on whitespace works for these simple files, but be careful not to match `CA` as a substring of something else); collect the (x,y,z) coordinates in file order. (2) Build the Kirchhoff/Laplacian matrix with `scipy.spatial.distance.cdist` (or pairwise loops) to get the NxN distance matrix, set off-diagonal `Gamma[i,j] = -1` where `0 < dist <= 8.0` (exclude i==j from the cutoff comparison), then set each diagonal entry to the negative sum of its row's off-diagonal entries so rows sum to zero — this is the standard GNM construction and gets the "singular matrix" property by design (translational invariance gives a zero eigenvalue). (3) Instead of `numpy.linalg.inv` (which will fail/error on this singular matrix), use `numpy.linalg.pinv` (Moore-Penrose pseudo-inverse) — this is the key fix mentioned in the task. (4) Take `numpy.trace` of each pseudo-inverse, compute `abs(trace1 - trace2)`, and write it rounded to exactly 4 decimal places (e.g. via Python's format spec `f"{value:.4f}"`) to `/home/user/fluctuation_diff.txt` with no extra whitespace.
