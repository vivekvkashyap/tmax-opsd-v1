Reproduce the Python test failure first before fixing anything: run `python3 -m unittest discover -s tests` from `/home/user/polyglot-eval` and confirm it fails, then run each test file alone to confirm they pass individually — this proves shared mutable module-level state (the `settings` dict in `src/config.py`) is leaking between test cases based on run order. Fix it by isolating state per test rather than deleting any test: e.g. have each test class save the original config values in `setUp` and restore them in `tearDown`, or have `test_b_config.py`'s test explicitly reset `settings["strict_mode"]` back to its original value at the end (in a `tearDown`), so execution order no longer matters. Confirm the fix by rerunning discovery in both file orders. For `build.sh`, use `jq -r '.go_bench_cmd'` and `jq -r '.py_test_cmd'` to extract the two commands from `ci_config.json`, run them sequentially with `bash -c "$cmd"`, and redirect combined stdout+stderr of the whole script (not per-command) to `/home/user/build_out.log` using `exec > log 2>&1` at the top or a trailing `> log 2>&1` on the invocation. Make the script executable, run it, and grep the log for the Go benchmark name and "OK" with no "FAILED" lines.
