The core challenge is recovering the exact custom-compression rule from audio before you can implement anything — don't guess at it. Transcribe `/app/memo.wav` (e.g. with `whisper` or another ASR tool available in the environment, or convert with `ffmpeg` and listen/transcribe manually) and write down the precise algorithm it describes in your own notes before coding; implement exactly what the memo specifies, since the verifier checks output equivalence, not indicated intent. Build `process_doc.sh` as a strict pipeline in this order: (1) dictionary substitution — read `/home/user/dict.csv` and do exact-string replacement of column 1 with column 2, e.g. via a `sed` script generated from the CSV or an `awk` pass; (2) apply the transcribed compression rule per line, likely a regex-based run-detection over repeated characters — test it against hand-crafted inputs with runs of 3+, 4+, and non-runs to confirm edge cases before trusting it; (3) zero-pad a 3-digit line counter starting at 001 with `: ` separator, prefixed onto each output line. Read from stdin, write to stdout only, and `chmod +x` the script. Validate against varied ASCII inputs including runs at line boundaries and dictionary terms adjacent to compressible runs.
