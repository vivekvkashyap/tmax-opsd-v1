Read `/home/user/experiment.py` and trace the exact order of operations: it currently calls `scaler.fit_transform(X)` on the full dataset before `train_test_split`, meaning the scaler's mean/variance are computed using statistics from what will become the test set — that's the leakage. Fix it by reordering: split `X` (unscaled) into train/test first with the same `train_test_split` call and parameters (keep `test_size`, `random_state`, `shuffle` identical so the split itself is unchanged), then fit the scaler only on `X_train` via `scaler.fit_transform(X_train)`, and transform the test set separately with `scaler.transform(X_test)` (never fit on test data). Keep the rest of the pipeline (KNN model, same `n_neighbors`, same accuracy metric and print formatting) identical so only the leakage is fixed. Save this corrected version to `/home/user/experiment_fixed.py`, run it, and redirect/save its printed accuracy (rounded to 4 decimals, matching the original script's print format) into `/home/user/fixed_metric.txt` with no extra text. Confirm the fixed accuracy differs from the original leaky run's output, since the shifted-outlier structure in the synthetic data means leakage vs. no-leakage should produce noticeably different scores.