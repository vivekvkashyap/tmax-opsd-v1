Load the CSV with pandas and handle the two metrics separately before merging. For the rolling average: sort each sender's transactions by `timestamp`, group by `sender`, then use `.rolling(window=3, min_periods=1).mean()` on `amount` and take the max of that series per sender — min_periods=1 matters so early transactions (fewer than 3 prior) still produce a valid average rather than NaN. For PageRank: build a `networkx.DiGraph`, but first aggregate the CSV by `(sender, receiver)` pairs summing `amount` into a single edge weight (don't add duplicate edges) before calling `nx.pagerank(G, alpha=0.85, weight='weight')`. Then take the union of all senders and receivers as your node set — nodes that only ever appear as a receiver must still get an entry with `max_rolling_3_avg` = 0.0. Merge both metrics per node, round every float to exactly 4 decimal places with Python's `round()`, sort the final list alphabetically by `node`, and write as a JSON array to `/home/user/analyzed_nodes.json` matching the schema exactly (key names, types, structure).
