Start by isolating the compile error: `cd /app/pii-masker && go build` will show `undefined: strings` — the fix is adding `"strings"` to the import block in `masker.go`, nothing else needs to change since the redaction logic (email regex + replace) is already correct. Verify with `go build` succeeding before moving on. Next scaffold `/home/user/server` with `go mod init`, add a `require example.com/pii-masker v0.0.0` plus a `replace example.com/pii-masker => /app/pii-masker` line in `go.mod` so the local fixed package resolves, and run `go mod tidy`. In `main.go`, implement the HTTP handler on 127.0.0.1:8080: check the `Authorization` header equals the exact given token string and return 401 otherwise; decode the JSON batch payload; process each item's content concurrently with goroutines feeding a `sync.WaitGroup` or channel to collect results in original order (order matters for correct output even though processing is parallel) by calling `piimasker.MaskText`; then hand-build (or use `encoding/xml` with matching struct tags) the exact nested XML response structure with `Content-Type: application/xml`. Compile with `go build`/`go run`, launch it backgrounded, and curl it yourself with a sample payload to confirm the XML shape matches before leaving it running.