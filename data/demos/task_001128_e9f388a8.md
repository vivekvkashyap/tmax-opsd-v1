Build this bottom-up and test each layer independently before wiring the pipeline together. First write `detector.c`: implement `compute_mse` as a simple loop over `size` bytes, casting each byte pair to `double`, summing squared differences, dividing by `size` — match `double` precision throughout to avoid drift versus a NumPy float64 reference. Compile it as a shared library with `gcc -shared -fPIC -o libdetector.so detector.c` (put this exact invocation in `build.sh`). In `wrapper.py`, use `ctypes.CDLL('./libdetector.so')` (or absolute path), set `.argtypes`/`.restype` explicitly (`POINTER(c_ubyte)*2, c_int` -> `c_double`) so ctypes doesn't silently truncate the return value, read both raw files as bytes, and print the float with enough precision (e.g. `print(mse)`, not rounded) since it's graded on MAE against ground truth. Test `wrapper.py` standalone on two known raw files before touching `analyze.sh`. For `analyze.sh`, parse the URI with a regex/parameter expansion, use `ffmpeg -i <path> -vf fps=<f> -s <w>x<h> -pix_fmt rgb24 <tmpdir>/frame_%04d.raw`, then iterate consecutive frame pairs writing `frame_N frame_N+1 MSE` lines to `/home/user/mse_log.txt`.
