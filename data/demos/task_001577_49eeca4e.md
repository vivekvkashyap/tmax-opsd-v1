Sequence this as: get the build working, then black-box the oracle, then implement, then validate against the local corpus before trusting the secret one. First fix the Makefile — if `make` fails with "missing separator", the recipe lines are using spaces instead of a leading tab; re-indent the build command with an actual tab character. Second, read `old_logic.c` to see the partial rule it already implements (a substring check), then feed `/app/legacy_filter` a range of hand-crafted lines via stdin (`echo "test line" | /app/legacy_filter; echo $?`) — systematically test lines containing suspicious keywords (error levels, credential-like patterns, other severity markers) individually to isolate additional substrings the binary rejects beyond what `old_logic.c` already covers; don't assume the local corpus samples are exhaustive, but they're a good starting hypothesis to test against the binary's exit codes. Third, port the full rule set (original + newly discovered substrings) into `main.cpp`, reading stdin line-by-line with `std::getline`, printing unmodified lines that pass all checks and silently skipping ones that match any evil substring. Compile with `make` and validate against `/home/user/corpus/clean/` and `/home/user/corpus/evil/` (every clean line printed, every evil line dropped) before considering it done.
