For Phase 1, use `grep '\[METRIC\]'` piped through `awk`/`sed` to isolate matching lines, then extract fields with regex/field-splitting on `key=value` tokens (timestamp is the leading ISO field before `[METRIC]`; endpoint/latency/status come from `key=value` pairs — strip the trailing `ms` from latency with a substitution), writing the four comma-joined fields per line to the CSV with no header. For Phase 2, group the parsed rows by endpoint in Python, sort each endpoint's latency list ascending, and implement nearest-rank P90 exactly as specified: index = ceil(0.9 * n) with 1-based rank converted to a 0-based array index (don't use a library percentile function that defaults to linear interpolation — it will disagree with nearest-rank on tie cases). Configure `logging.handlers.RotatingFileHandler` with the exact maxBytes/backupCount given and a bare `%(message)s` formatter, then log one INFO line per endpoint in sorted alphabetical order — verify afterward that rotation actually produced multiple `.log`/`.log.1` files given the tiny maxBytes threshold. Finally dump the same per-endpoint integer P90 values to the JSON summary.
