Start by running `tesseract /app/schema_info.png stdout` (or similar) and read the raw text carefully to recover the ALLOWED_KEYS list — don't guess key names. Then write `/home/user/consolidate.sh` as a state machine: iterate the WAL chunk files in `/home/user/wal_logs/` sorted alphabetically (`ls | sort`, or glob naturally sorts if names are zero-padded), and for each line use an associative array (bash `declare -A`) keyed by the log key — `SET k v` sets `arr[k]=v`, `DELETE k` does `unset arr[k]`. After processing all chunks, filter the surviving keys against the OCR'd allowed-key list before emitting JSON — keys deleted or not present in the schema must not appear even if they were SET. Build strictly minified JSON manually (no jq pretty printing) as `{"k1":"v1","k2":"v2"}` with no spaces; treat all values as strings unless the spec implies otherwise. For atomic write: write to `/home/user/output/.latest_config.json.tmp` then `mv` it onto `/home/user/output/latest_config.json` (mv on the same filesystem is atomic; a plain write-then-rename avoids partial reads). Finally `ln -sf /home/user/output/latest_config.json /home/user/current_config`. Keep the JSON compact — every extra byte counts against the size threshold.
