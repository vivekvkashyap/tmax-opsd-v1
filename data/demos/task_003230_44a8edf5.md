Confirm the on-disk format empirically against the oracle before writing your decoder — don't assume RLE parameters without checking. Use `xxd`/`hexdump` on a sample `.artb` file (or feed crafted minimal byte sequences to `/app/decode_artb` via stdin and observe stdout) to verify: the 4-byte magic `ARTB`, a little-endian u32 chunk count, then per chunk a little-endian u32 original size and u32 compressed size followed by that many bytes of compressed payload, and that the compressed payload is (count_byte, value_byte) pairs where count_byte is 1-255 and the run of `count` copies of `value` is emitted. Test edge cases by hand (e.g. a single chunk with one RLE pair, or a chunk with a run of exactly 255) and check the oracle's output length matches the declared original size to confirm your understanding, since off-by-one errors in the header sizes vs. read loop are the easy way to fail the fuzz-equivalence check. Implement `/home/user/decode_artb.py` reading strictly from `sys.stdin.buffer` and writing to `sys.stdout.buffer` (never open real files or hardcode a path), then diff its output against the oracle on several hand-built `.artb` byte strings piped via stdin before considering it correct.
