Do this as a clean multi-step shell/Python pipeline rather than one big script, so you can inspect intermediate output. Step 1: concatenate the `.txt` files from `/home/user/logs` in alphabetical (glob-sorted) order — do not rely on directory listing order. Step 2: filter out any line containing the literal substring `DEBUG` (simple line-level filter, not just log-level prefix matching). Step 3: mask IPv4-looking sequences (four dot-separated number groups) with the literal string `[REDACTED]` using a regex like `([0-9]{1,3}\.){3}[0-9]{1,3}` applied per line — be careful your regex doesn't accidentally require exactly 3-digit octets or leave partial matches. Step 4: split the resulting cleaned lines into files of exactly 50 lines each, naming them `chunk_00.txt`, `chunk_01.txt`, ... in `/home/user/archive` (zero-padded, 2-digit index; if line count isn't a multiple of 50, the last chunk is short and that's expected). Step 5: write a small Python script that hashes each chunk file's bytes with `hashlib.sha256`, and dump a JSON dict mapping chunk filename -> hex digest to `/home/user/archive/manifest.json`. Verify by re-reading the archive directory and re-hashing to confirm the manifest matches what's actually on disk, and grep the chunks to confirm no DEBUG lines or raw IPs remain.
