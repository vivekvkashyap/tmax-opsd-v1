Do not guess the archive format — derive it empirically. Create a small test directory with a couple of `artifact_X.Y.Z.bin` files of known content, run `/app/legacy_packer <dir> <min_semver> <out>`, and hexdump the output (`xxd out | head`) to work out the header magic, integer field widths/endianness, per-entry layout (name length, name, file size, raw data), and whatever trailing integrity value follows each entry — try re-deriving that trailing value with common checksum algorithms (CRC-32 is a good first guess) over the raw file bytes and confirm it matches across multiple test files before trusting it. Also confirm empirically whether the semver filter is strictly-greater-than by testing a file exactly equal to min_semver. Once the format is nailed down, write `/home/user/src/fast_packer.c` implementing it with efficient I/O (buffered reads/writes, avoid per-byte syscalls, minimize allocations) and a `Makefile` with an `all` target using `gcc -O3`. Validate correctness by comparing MD5 of your tool's output against the legacy tool's output on identical inputs before worrying about speed; log both MD5s to /home/user/verification.log. Only after correctness matches, profile and eliminate remaining overhead (e.g., avoid unnecessary copies, use mmap or large buffered reads) to clear the 3x speedup bar on large datasets.