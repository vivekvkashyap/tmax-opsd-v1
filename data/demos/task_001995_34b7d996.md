First identify which background process is the Node.js service and which local port it's bound to — use `ps aux | grep node` combined with `ss -tlnp` (or `lsof -i -P -n`) to map the node PID to its listening port; don't assume, confirm both the process type and port from live system state. Second, parse `/home/user/traffic.log` with a line-oriented tool (awk/grep on the `-> 127.0.0.1:PORT` field) filtering only lines whose destination port matches the node service, then tally counts per source IP (the part before the colon in `SRC_IP:SRC_PORT`) and pick the IP with the highest count — write only that IP, no extra whitespace, to rogue_ip.txt. Third, build secure_run.sh as a thin wrapper that execs `bwrap` with `--bind / /` (plus `--dev /dev --proc /proc` so basic tools like `ip` work) and the network-isolation flag `--unshare-net`, then passes through `"$@"` as the command to run inside the sandbox; chmod it executable. Validate by running `secure_run.sh ip link` yourself and confirming only `lo` appears, not eth0 or any host interface.