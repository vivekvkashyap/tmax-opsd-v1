Run `tesseract /app/projection_matrix.png stdout` (or extract to a temp text file first) to OCR the two lines of the matrix — inspect the raw output carefully, since tesseract can misread `-` signs or merge decimal points, and cross-check that you got exactly two lines of three space-separated floats before hardcoding them into your Go source. Parse the recovered numbers into a fixed 2x3 `[6]float64` array (or similar) at program start. For the stdin loop, use `bufio.NewScanner(os.Stdin)`, trim each line, and `continue` on empty lines rather than erroring out. Split each non-blank line on `,`, parse three `float64` values with `strconv.ParseFloat`, then compute `y1`/`y2` via the matrix-vector dot products exactly as specified, printing with `fmt.Printf("%.4f,%.4f\n", y1, y2)` and nothing else to stdout (route any debug output to stderr). Since your binary will be fuzz-tested against thousands of random input lines and compared byte-for-byte against a reference, get the float parsing/formatting precision exactly right and exit cleanly (code 0) on EOF.
