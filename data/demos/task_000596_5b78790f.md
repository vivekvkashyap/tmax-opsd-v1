Set up the Rust project first (`cargo new /home/user/etl`, add `rusqlite` with the `bundled` feature so no system sqlite dependency issues arise). Query `events` joined with `users`, ordered per-user by `event_date` — do this with SQL `ORDER BY user_id, event_date` rather than relying on table insertion order, since the rolling average must respect chronological order strictly. Group rows by `user_id` in Rust (e.g. into a `HashMap<i64, Vec<Row>>` after fetching all rows sorted), then for each user's ordered event list compute a rolling window of up to the last 3 scores (current plus up to 2 preceding) and average them — for the first and second events in a user's sequence, average only the events available so far, not a fixed window of 3. Round each average to exactly 2 decimal places (e.g. `(x * 100.0).round() / 100.0`) before inserting. Since `comment` may contain embedded newlines, avoid any shell/CSV-based round trip for the join/ordering step that could corrupt row boundaries — read/write directly via `rusqlite` parameterized queries, or if exporting via `sqlite3 -csv`, make sure you use a real CSV parser (the `csv` crate) which correctly handles quoted embedded newlines rather than splitting on raw `\n`. Create `rolling_stats` with `CREATE TABLE IF NOT EXISTS` (or drop-and-recreate) and insert rows, then run the program and manually spot-check a few rows with `sqlite3 data.db "SELECT ..."`.