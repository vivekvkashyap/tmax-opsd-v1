Since you must bypass a potentially corrupted index, do the full scan and all pattern-finding in your own C code, not in SQL: `SELECT source, target, weight FROM edges NOT INDEXED` and `SELECT id, label FROM nodes`, load both into in-memory arrays/hash maps (e.g. adjacency list keyed by source node, plus an edge-weight lookup). To find triangles, iterate every edge (A->B), then every edge from B (B->C), then check whether an edge C->A exists in your adjacency structure — sum the three weights and keep the triangle if the sum exceeds 100, being careful to only count each distinct 3-node cycle once (e.g. by canonicalizing the triple, since your traversal from each edge would otherwise revisit the same cycle multiple times). Collect the set of unique node IDs appearing in any heavy triangle, and for each compute `out_degree` as the count of edges with that node as source over the *entire* edge list (also `NOT INDEXED`), not just edges within triangles. Write the CSV sorted ascending by `node_id`, matching the header exactly, and diff your output against your own hand-computation of a couple of rows before trusting the whole file.
