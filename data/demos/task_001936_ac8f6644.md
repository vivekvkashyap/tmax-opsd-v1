Run OCR on `/app/manifest_scan.png` (e.g. `tesseract`) and parse the recovered text as JSON to get the file list and the hex-sequence-to-token dictionary — don't assume the mapping, read it from the OCR output and validate it parses as valid JSON with sane hex keys. Read `snapshot.csv` and compare each listed file's actual on-disk mtime (`os.path.getmtime` / `stat`) against the `last_modified` value in the CSV — only include files whose current mtime is newer than the snapshot's recorded timestamp (this excludes the unchanged file, e.g. an older one, from the archive). For compression: first apply the dictionary substitution by scanning each included file's bytes and replacing every occurrence of each hex-decoded byte sequence with its corresponding single-byte token (do longer/more specific sequences first to avoid partial overlaps), then run the substituted bytes through a standard general-purpose compressor (e.g. Python's `zlib`/`gzip`) before writing to `/home/user/backup.bin` — dictionary substitution alone likely won't clear the 30% threshold on its own for all inputs, so layering general compression on top is the safe bet. Verify by computing raw-vs-compressed size ratio yourself before finishing.
