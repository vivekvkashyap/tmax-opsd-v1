Start with the video: use `ffmpeg` to extract frames (e.g. `ffmpeg -i /app/dashboard_sync.mp4 out_%05d.png` or use `select`/`blackframe`/pixel-stat filters) and programmatically test each frame for being pure white (all pixels 255,255,255) rather than eyeballing — a quick Python/PIL or ImageMagick `identify -format "%[fx:mean]"` loop over frames works well; record the 0-indexed frame numbers that qualify. Cross-reference those frame numbers against the `sync_id` column in `raw_events.csv` to build your clean dataset, discarding ghost rows whose `sync_id` isn't in that set. For the Go program, design your data structures so lookups by `vehicle_id` are efficient (e.g. a map from vehicle_id to a sorted-by-timestamp slice of checkpoint records, built once at load time) since it will be fuzzed with thousands of calls — don't do linear file re-scans per invocation if avoidable, and always print `NOT_FOUND` for unknown IDs rather than crashing or leaving blank output. Before finishing, manually trace a couple of known valid vehicle IDs through your cleaned data to confirm the chronological ordering logic is correct.
