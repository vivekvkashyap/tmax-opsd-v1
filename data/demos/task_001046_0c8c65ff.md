Start by OCR'ing `/app/schema_info.png` with `tesseract` (e.g. `tesseract schema_info.png stdout`) to recover the two table definitions — read the extracted text carefully to get exact table/column names, don't guess them. Build `/home/user/graph.db` with a `CREATE TABLE` for each recovered schema, then walk the nested JSON (groups -> nodes -> incoming_links) inserting rows into the group/link tables accordingly, keyed by node_id and group_id. For the IWRS query in `graph_analyzer.py`, write a single parameterized SQL query using a CTE: join links to a self-join of the group table on both source and target node_id, filter to same-group pairs and the given target, then apply `DENSE_RANK() OVER (ORDER BY weight DESC, source_id ASC)`, and sum `weight * rank` in an outer query — use `COALESCE(..., 0.0)` so nodes with no matching links yield 0.0 instead of NULL. Execute this once per node_id from stdin, preserving input order (including duplicates) in the output array — don't dedupe or sort the ids. Test against handmade cases before trusting the fuzzer.
