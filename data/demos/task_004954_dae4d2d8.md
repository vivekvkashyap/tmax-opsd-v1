Write the C program to parse `ATOM  ` lines by splitting each line on whitespace and reading the 6th, 7th, 8th tokens (1-indexed) as X, Y, Z (use strtok/sscanf carefully — don't assume fixed-column offsets since the task explicitly overrides PDB's real fixed-width format). First pass: find global X_min/X_max across all atoms, compute X_mid. Second pass: bucket each atom into Left (X < X_mid) or Right (X >= X_mid), storing all three coordinates per atom (dynamic array or two passes with counting then allocation). For each domain, compute the mean of X, Y, Z, then the population covariance matrix (divide by N, not N-1): Cov[i][j] = (1/N) * sum((x_i - mean_i)(x_j - mean_j)) for i,j in {X,Y,Z}. Compute the 3x3 determinant via cofactor expansion. Watch for the classic bug of using sample covariance (N-1) instead of population covariance — the spec explicitly requires N. Format the determinant with `%.6f` and compare strictly `< 0.1` for SINGULAR vs OK. Open the output file in append mode ("a") since results are appended, and match the exact line format including the "Domain Left:" / "Domain Right:" labels. Compile with `gcc ... -lm` since determinant/sqrt-free calculations may still need libm for other operations, and test against the given input.pdb before trusting the numbers.