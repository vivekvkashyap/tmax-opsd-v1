Reverse-engineer `/app/legacy_decode` by black-box probing rather than disassembling: feed it small, controlled byte sequences via stdin (e.g. single bytes, a byte followed by simple repeats, sequences containing 0xAA) and observe stdout to infer the algorithm, since it decodes with an XOR key plus an escape-based RLE scheme. Confirm hypotheses like "first byte is a key that XORs everything else" and "an escape byte introduces a repeat-count and value" by testing edge cases (escape byte at start/end, escape value colliding with the key, zero-length input) until your C++ reimplementation in `new_decode.cpp` reproduces every observed output byte-for-byte; treat any input as a raw byte stream (read via `std::cin` in binary mode / `freopen` or unformatted `read`), not as text, to avoid newline-translation corruption. Compile to `/home/user/new_decode`. For the wrapper script, create `.backup_profile` with the two exports, then in `restore_wrapper.sh` do `source ~/.backup_profile`, iterate `.chk` files in `$1`, pipe each through `new_decode`, count output bytes (e.g. `wc -c`), and append a log line with `date '+%Y-%m-%d %H:%M:%S'` in the exact specified format. Write `logrotate.conf` targeting `/home/user/restore.log` with `daily`, `rotate 7`, `compress`, and `size 10M` (or `maxsize 10M`) all present, and validate it with `logrotate -d`.
