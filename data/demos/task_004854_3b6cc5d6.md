This is a shortest-path problem with a non-standard edge/node cost mix and a tie-breaking rule, so implement it as a variant of Dijkstra (or Bellman-Ford if costs could be negative, though here they aren't) rather than plain edge-weight shortest path. Model each edge's traversal cost as `transit_cost` plus the destination node's `processing_delay`, EXCEPT do not add the delay for the path's final destination when it's the true end_node (only intermediate nodes incur delay) — the cleanest way is to add the target node's delay when relaxing an edge, then at the end subtract the end_node's own delay contribution if you added it, or equivalently only add delay when the node is not the query's end_node. Track total cost as primary key, edge-count as secondary tiebreak, and path lexicographic order as tertiary tiebreak — when relaxing, compare tuples `(cost, edge_count, path)` rather than cost alone so ties resolve correctly. Parse CSVs, run one shortest-path search per query, and emit the exact JSON schema given, sorted/formatted precisely. Compile with the exact g++ command given and run it to produce `/home/user/results.json`.