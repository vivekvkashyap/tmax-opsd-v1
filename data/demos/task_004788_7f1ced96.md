In `/home/user/clean_data.go`, use `encoding/csv` to read `/home/user/data/events.csv`, keep the header, and classify each row by the raw `user_id` string field: if it's empty, skip the row entirely (don't write to either output); otherwise try `strconv.Atoi` (or `ParseInt` base 10) on it — if parsing succeeds cleanly as a plain base-10 integer (this rejects floats like `1003.0`, scientific notation like `1.2e4`, and non-numeric strings like `NaN`/`null` since `Atoi` fails on all of those), write the full row to `/home/user/data/cleaned.csv`; if parsing fails, write just that row's `event_id` to `/home/user/anomalies.txt`, one per line, in file order. Be careful not to use a looser numeric parser (like `ParseFloat`) for the valid-integer check, since that would wrongly accept `1003.0` and `1.2e4` as valid — the whole point is distinguishing strict integers from float-like anomalies. Write `/home/user/data/cleaned.csv` with the original header row first, then compile and run the program to produce both output files, and diff them against your own hand-derived expectation from the input CSV.
