First recover the XOR key from the video: extract the exact frame at 00:00:05 with `ffmpeg -ss 00:00:05 -i telemetry_feed.mp4 -frames:v 1 frame.png`, then read the burnt-in overlay text (visually or via OCR) to obtain the 4-byte hex key — don't guess it. Then build `pipeline_decoder.py` as a streaming binary parser over `sys.stdin.buffer`: read 2 magic bytes, 1 encoding-flag byte, 2 big-endian length bytes, `L` payload bytes, then 1 checksum byte, looping until EOF. For each frame: validate magic bytes first; XOR-decrypt the payload by repeating the 4-byte key cyclically; compute the XOR of the *decrypted* bytes and compare to the checksum byte (order matters — decrypt before checksumming). Decode with the encoding named by the flag (`cp1252`/`shift_jis`/`utf-8`) inside try/except, catching decode errors specifically. On any failure, append the matching error JSON to `/home/user/pipeline.log`; on success print the success JSON to stdout. Test hand-crafted frames covering each failure path, since the script gets fuzzed heavily for bit-exact behavior including malformed/truncated trailing data.