Use `git bisect` methodically rather than reading 200 commits by eye: pick a known-good early commit and `main` as bad, write a small reproduction script that runs the WAL processor's concurrent path against a fixed multi-file input directory several times and flags nondeterministic/out-of-order output (race conditions often need repeated runs to manifest), then `git bisect run` that script across the range. Once you land on the offending commit, read its diff specifically for classic Python concurrency footguns — the prime suspect pattern here is a mutable default argument (e.g. `def f(x, events=[])`) or a module-level/shared list being appended to from multiple worker-pool threads without synchronization, which silently accumulates and reorders state across calls. Fix it by giving each call/thread its own fresh local state (e.g. `events=None` defaulting to `events = []` inside the function body, or passing a per-thread list explicitly) rather than adding locks around a fundamentally shared object. After fixing on main, wrap the corrected logic in `/home/user/fixed_processor.py`, taking the WAL directory as argv[1] and printing the final recovered JSON state to stdout with no other output, then run it repeatedly yourself against a generated fixture directory to confirm it's now deterministic run-over-run before assuming it matches any external reference.
