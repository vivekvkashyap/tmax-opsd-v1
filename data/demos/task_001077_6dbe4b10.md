Use `os.walk` (or `Path.rglob('*.md')`) to recursively find markdown files under `/home/user/raw_notes`, filtering strictly on the `.md` suffix so sibling non-markdown files are skipped. For each file, read the full text once; scan its lines for the first one that starts exactly with `"# "` to derive the title (strip the prefix and whitespace), defaulting to `"Unknown"` if none is found — don't confuse a `##` or a mid-file `# ` with a true first H1. Compute `words` via `content.split()` (not line-by-line) so counting matches Python's whitespace-splitting semantics exactly. To mirror the directory tree in `/home/user/processed_notes`, compute each file's path relative to `raw_notes` with `os.path.relpath`, join it onto the destination root, swap the `.md` extension for `.json`, and `os.makedirs(..., exist_ok=True)` on the parent before writing — don't flatten all outputs into one directory. After running the script over the whole tree, glob all resulting `.json` paths, sort them alphabetically, and write one absolute path per line to `/home/user/files_processed.log`.