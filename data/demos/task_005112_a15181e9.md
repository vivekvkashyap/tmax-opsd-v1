Work through this as ordered, verifiable sub-tasks. First create the output directory and set its mode explicitly with `mkdir -p` then `chmod 750` — verify with `stat -c "%a"` afterward since umask can produce unexpected bits. For the systemd unit, write the `.service` file with explicit `[Unit]`, `[Service]`, `[Install]` sections; put each environment variable on its own `Environment="KEY=VALUE"` line rather than combined, since the verifier greps for each var separately — set `ExecStart` to the exact worker path. For the timer, use `OnCalendar=*:0/5` under `[Timer]` and point `Unit=` at the service if names don't match by convention. Reload with `systemctl --user daemon-reload` before enabling both units with `systemctl --user enable`. For the health check script, write `if [ -f ... ]; then ... else ... fi` logic appending the exact status strings, then `chmod +x` it. Finally edit the crontab (`crontab -l | { cat; echo ...; } | crontab -`) to add a `* * * * *` line setting `WORKER_DATA_DIR=...` inline before invoking the script — don't rely on systemd's environment leaking into cron, they are separate managers.