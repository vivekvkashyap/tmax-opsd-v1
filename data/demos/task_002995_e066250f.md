Do not try to run or reverse-engineer `/app/query_gen` directly as a shortcut for the answer — instead treat this as pattern classification: diff the two corpora yourself. Write a quick script (or use `grep`/`diff`) to scan every file in `/home/user/corpus/clean/` and `/home/user/corpus/evil/` and look for structural markers that are present in evil but absent in clean — the description hints at things like an explicit `INDEXED BY <name>` clause forcing a specific (corrupted) index, or a nested subquery pattern selecting from the same table on a particular column (e.g. `type`) inside a `WHERE ... IN (SELECT ... FROM edge_list ...)`. Confirm your hypothesis systematically: grep each corpus for your candidate pattern and check it perfectly separates clean vs evil (100% on both, not just most). Implement `/home/user/sanitizer.py` to read the full query from stdin, apply a robust check (case-insensitive, whitespace-tolerant — use regex or a lightweight SQL tokenizer rather than naive substring match so minor formatting differences in the hidden test suite don't slip through), print the original query unchanged and exit 0 if clean, or print `REJECTED` and exit 1 if it matches the dangerous pattern. Test against both provided corpora before considering it done, since a hidden corpus will apply the same underlying pattern with different surface details.