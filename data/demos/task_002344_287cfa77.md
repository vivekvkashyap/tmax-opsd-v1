Split this into the two independently-verifiable pieces and confirm each works before combining. For TLS, generate a self-signed cert/key with `openssl req -x509 -newkey rsa:2048 -keyout /home/user/key.pem -out /home/user/cert.pem -days 365 -nodes -subj "/CN=localhost"`, then edit `/home/user/nginx.conf`'s `server` block on port 8443 to add `ssl on;`/`listen 8443 ssl;`, `ssl_certificate`/`ssl_certificate_key` pointing at those files, and a `location /health { proxy_pass http://127.0.0.1:5000; }` block; validate syntax with `nginx -t -c /home/user/nginx.conf` before (re)starting it, then confirm end-to-end with `curl -sk https://127.0.0.1:8443/health`. For the metrics parser, write a small script that reads one stdin line, splits on `,` then on the first `:` per pair, trims whitespace from both key and value with something like `strip()`, always stores values as strings, sorts keys alphabetically before building the JSON, and emits it minified (no extra spaces) wrapped under a top-level `"data"` object — test manually against the example input/output pair given in the task before considering it done, since the grader fuzzes many random key/value/whitespace combinations and expects byte-exact output.