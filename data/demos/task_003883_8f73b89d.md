Sequence this as recover -> rotate -> redact, verifying each stage's output before moving to the next. For the cracker, write `/home/user/cracker.c` that iterates all 26^4 combinations of 4 lowercase letters, computes SHA256 of each candidate with OpenSSL's `EVP_Digest`/`SHA256` API, converts the digest to lowercase hex, and compares against the hash read from `/home/user/pass_hash.txt`; compile with `gcc cracker.c -o cracker -lcrypto` and print the match once found (26^4 ≈ 457k is trivially fast, don't overthink performance). Use the recovered passphrase to test-decrypt `old_key.pem` (e.g. `ssh-keygen -y -f old_key.pem -P '<recovered>'` or `openssl rsa`) purely as a sanity check that you got the right value — you don't need to persist the decrypted key. Generate the replacement with `ssh-keygen -t ed25519 -N "" -f /home/user/new_key`. For redaction, do the private-key block removal first with a tool that handles multi-line spans correctly (e.g. `sed '/-----BEGIN ENCRYPTED PRIVATE KEY-----/,/-----END ENCRYPTED PRIVATE KEY-----/c\[REDACTED_KEY]'`), then a separate pass replacing the literal passphrase string with `[REDACTED_PASS]` — order matters since the key block also contains base64 text that could coincidentally overlap patterns. Finally write `rotation_summary.txt` with exactly two lines: the recovered passphrase and the verbatim contents of `new_key.pub`.
