First inspect the corrupted index: `PRAGMA index_list('tasks')` and `SELECT sql FROM sqlite_master WHERE type='index'` will show its definition looks wrong (indexed on the wrong column) even though it's named `idx_parent`. Drop it with `DROP INDEX idx_parent;` then create the correct one with `CREATE INDEX idx_parent_id ON tasks(parent_id);`. For the critical path, write a `WITH RECURSIVE` CTE that starts from `id = 1` with an accumulated running total column, joins children via `parent_id`, and at each step adds `duration`; select `MAX(total)` from that CTE over only leaf rows (rows whose `id` never appears as a `parent_id`) — or simply take the max total across all rows reached, since leaves will hold the largest cumulative sums along their branch. Run this query and capture output with `sqlite3 -noheader -csv` or similar, then pipe through `sort -n`/`awk` to isolate the maximum, formatting into `Critical Path Length: <MAX_DURATION>`. Separately run `EXPLAIN QUERY PLAN` on the same CTE query and redirect to `/home/user/plan.txt`, confirming it references the new index name. Make the driving script executable with `chmod +x`.