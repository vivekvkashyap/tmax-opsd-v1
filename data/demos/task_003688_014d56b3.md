Build /home/user/filter_vm_requests.py to check each condition independently and exit non-zero on the first failure (fail closed). For user validation, use the `pwd`/`grp` modules or shell out to `id <username>` — catch the case where the user doesn't exist (treat as malicious, don't crash), and check group membership via the user's primary gid (pwd.getpwnam) plus supplementary groups (grp.getgrall() or `groups <user>`) against the sudo/wheel group names. For vnc_display, validate it's an int type (not a numeric string) strictly within [1,99]. For env, scan all keys for the two forbidden substrings case-appropriately. For the token, invoke subprocess.run(["/app/check_token", token], ...) and use its returncode directly — since it's a stripped binary you can't read its logic from symbols, so treat it strictly as a black box and rely solely on the exit code, testing empirically with a range of tokens against the sample corpora in /home/user/corpora/ to build confidence before trusting your integration. Make the script executable with a python3 shebang, and test it against every file in both sample corpora to confirm correct classification before assuming it generalizes to the hidden eval corpora.
