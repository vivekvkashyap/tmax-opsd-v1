Write the Go parser as a line-by-line filter with three concerns handled separately: (1) level+user filtering — use a regex to match `ERROR|WARN` and a separate regex for `\[USR-\d+\]`, and skip the line entirely if either is missing (INFO lines and SYS-prefixed IDs must never appear in output); (2) timestamp parsing — since the log mixes three distinct layouts (`YYYY/MM/DD-HH:MM:SS`, `MM-DD-YYYY HH:MM:SS`, `YYYY.MM.DD HH:MM:SS`), try Go's `time.Parse` against each of the three corresponding layout strings in turn (using Go's reference-date layout syntax) and use whichever succeeds, then format the result with `time.Format(time.RFC3339)` treating the parsed time as UTC; (3) message extraction — after locating the user-ID bracket, take everything after it and `strings.TrimSpace` it before emitting. Build the output object with keys in the exact order `time, level, user, msg` and marshal with `encoding/json` (or hand-build the string) writing one JSON object per line to `/home/user/normalized_alerts.jsonl`, preserving original line order. Run `go run parser.go` (or build+run) so the file actually lands on disk, and manually check a few lines against the raw log to confirm alignment.