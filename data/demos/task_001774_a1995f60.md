Break this into independent stages and validate each before combining. Stage 1: compile `calc_peaks.c` with `-lm`, run it on `molecule.edges`, and take the 3 largest printed wavenumbers as the theoretical peaks — these come directly from program output, don't hardcode. Stage 2: load `spectrum.csv`, apply a proper 5-point centered moving average (index i uses i-2..i+2, leaving edges as NaN/dropped — a common mistake is using a trailing/lagging window instead of centered, which shifts peak locations), then scan for strict local maxima requiring the smoothed value to exceed its 2 neighbors on each side, and take the 3 highest-intensity peaks' Wavenumber values (pandas `.rolling(5, center=True).mean()` is a clean way to get this right). Stage 3: sort both peak lists ascending and compute MSE termwise. Stage 4: write `results.json` with peaks rounded to 3 decimals and mse, and produce `spectrum_plot.png` with raw intensity (points/light line), smoothed intensity (solid line), and vertical dashed lines at the 3 theoretical peak positions — use matplotlib and save directly to the exact path. Double check your moving-average/peak-detection logic against a few points by hand before trusting the final numbers.
