First transcribe `/app/instruction.wav` with a speech-to-text tool (`whisper`, `pocketsphinx`, etc.) to determine whether the spoken instruction says to add or subtract a specific number, and note the sign/magnitude carefully — the direction (add vs. subtract) matters as much as the value. Then write `/home/user/pipeline.py` (with `#!/usr/bin/env python3` shebang, `chmod +x`'d) that reads a CSV from stdin via pandas, computes `floor(mean)` of the existing non-null `value` entries, fills missing values with that floor before doing anything else, then applies the extracted numeric adjustment to every value, and finally casts the whole `value` column back to a native integer dtype (e.g. `.astype(int)`) before writing to stdout with `to_csv(sys.stdout, index=False)` — the key pitfall is that once you introduce NaNs pandas silently upcasts the column to float64, so you must fill-then-cast in that exact order, not the reverse, or your integers will leak decimals into the output. Since this is checked by fuzz-testing against a hidden reference across many random CSVs, test your script locally against several synthetic CSVs with random missing values and various value ranges to confirm rounding/casting edge cases match expectations before considering it done.