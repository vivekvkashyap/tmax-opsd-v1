Start by inspecting the Makefile's compile/link line for `cosine_sim`: since `main.c` calls `sqrt`/math functions, a missing `-lm` linker flag is the classic cause of "undefined reference to `sqrt`" — add `-lm` at the end of the link command (order matters, put it after the object/source files) and re-run `make` until `bin/cosine_sim` exists and runs on a sample `a:b:c a:b:c` input. For the pipeline script, read the CSV line-by-line with `IFS=, read -r id vecA vecB` fed via input redirection (`< "$1"`, not a pipe) so you don't lose state across iterations, call the compiled tool for each row, and compare the returned float against the 0.5000 threshold using `awk` or `bc -l` (careful with `>=` vs `>`, and with scientific notation edge cases in bc). Preserve input row order exactly, print only `id,similarity` for passing rows, and print nothing else to stdout. Test against several hand-built CSVs and diff your output against manual calculations before trusting it, since the real verifier does exact-match comparison against a reference implementation.
