Reverse-engineer /app/logger_bin empirically before writing any decoder: pipe known sample inputs into it via stdin (e.g. `printf 'AB' | /app/logger_bin | xxd`) and compare input vs output bytes to spot the transform — pay special attention to how a literal space character is handled versus other characters, per the hint, since encoders often special-case whitespace (e.g. mapping it to a null byte) before or after a bitwise operation like XOR with a fixed key. Once you've derived the exact byte-level rule and confirmed it round-trips correctly on several test strings (including ones containing spaces), write a Rust program at /home/user/decoder.rs that reads /home/user/incident.log.enc as raw bytes, reverses the transform per byte (undo the XOR, then map the resulting null byte back to a space if that's part of the scheme), and writes plaintext to /home/user/recovered.log. Compile with `rustc -O /home/user/decoder.rs -o /home/user/decoder` and run it, then manually eyeball recovered.log to confirm it looks like sane log lines (no garbage bytes) before considering the task done, since the grader needs >=0.99 character similarity to the original.