Build this as a sequenced pandas pipeline, verifying each stage's output before moving to the next. First handle masking: drop `UserName`, and compute `UserHash` as `hashlib.md5(str(UserID).encode()).hexdigest()` per row. Second, for `NormalizedAction`, treat `/app/normalize_action` as a black box you must call per distinct `Action` value — pipe each string to its stdin and capture stdout (e.g. via `subprocess.run(..., input=action_str, capture_output=True, text=True)`), caching results by unique action string for efficiency; do not assume you know its transformation logic — verify by testing a couple of sample inputs/outputs first. Third, reshape wide-to-long with `pandas.melt` on the four metric columns into `MetricName`/`MetricValue`, keeping `Timestamp`, `UserHash`, `NormalizedAction` as id vars. Fourth, sort by `(UserHash, MetricName, Timestamp)` before computing the rolling average — grouping by `UserHash` and `MetricName` and applying `.rolling(window=3, min_periods=1).mean()` on `MetricValue`, then round to 2 decimals; sorting order here is critical since an unsorted or wrongly-grouped rolling window silently corrupts everything downstream. Finally re-sort the whole frame by `Timestamp, UserHash, MetricName` for the final CSV write, matching column order exactly, and confirm your row-match percentage against your own manual spot-checks before trusting the 98% threshold.