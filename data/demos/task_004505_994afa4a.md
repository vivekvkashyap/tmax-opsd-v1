For Part 1, extract frames from `/app/audit_session.mp4` (e.g. `ffmpeg -i audit_session.mp4 -vf fps=<rate> frame_%03d.png`), then run OCR (`tesseract`) on the frames around where the scrolling log would show a DROP TABLE event, scanning multiple frames since text may be mid-scroll or partially cropped in any single frame — look specifically for a line referencing the `compliance_records` table and its associated SESSION_ID token, and write just that ID string to `/home/user/malicious_session.txt`. For Part 2, don't guess the compliance rules — diff the JSON pipelines in `/app/corpus/clean/` against `/app/corpus/evil/` with `jq` to see structurally what differs: check whether the first pipeline stage is `$match`, whether that stage's object contains a specific isolation key, and whether any `$lookup` stage's `from` collection touches sensitive/restricted collections. Encode exactly those checks (and only those) in `/home/user/query_auditor.sh` using `jq` queries on the argument file, returning exit 0 for compliant and exit 1 for any violation, and make the script executable.
