Build this as three separable stages that each produce a verifiable artifact, so you can debug them independently. Stage 1: extract frames with `ffmpeg -i /app/experiment_record.mp4 frame_%04d.png` (or read frames directly via OpenCV in Python), and for each frame compute the pixel-intensity standard deviation across all channels (e.g. `np.std(frame)`), counting a frame as blank when that value is below 2.0; keep exact integer counts of blank vs total frames — don't estimate. Stage 2: in R, compute the Wilson score interval for a binomial proportion using the standard closed-form formula (not the naive normal-approximation interval) with z=1.96 for 95% confidence, taking blank_frames/total_frames from Stage 1's output, and round both bounds to 4 decimal places when writing the JSON with the exact required keys. Stage 3: for the transcript filter, compute a punctuation-density-style anomaly signal per file (punctuation character count / total character count, plus checks for unusual repeated characters or non-printable/invisible Unicode) and pick a threshold using the labeled `/app/training_corpus/clean` vs `/app/training_corpus/evil` examples to calibrate what separates them, since the held-out test corpus follows the same generating distribution — copy only files scored clean to the output directory. Finally write run_pipeline.sh chaining all steps with correct exec permissions (`chmod +x`), and test it end-to-end before finishing.
