Use `ffmpeg` to dump frames from `/app/archive_scroll.mp4` (e.g. one frame per second or per detected scene change) into a temp directory, then run `tesseract` over the frames in parallel (e.g. `xargs -P $(nproc)` or background jobs + `wait`) to speed up OCR instead of processing serially. Concatenate all OCR text, then filter with a regex that matches full semantic versions starting with `v` (major.minor.patch, optionally with a `-prerelease` suffix) — discard OCR noise/partial matches. Deduplicate, then sort in true semver order (not lexicographic string sort): compare major/minor/patch numerically, and remember that a pre-release version (e.g. `-rc.1`) sorts *before* its corresponding release version of the same X.Y.Z. Write one version per line to `/home/user/project_files/extracted_versions.txt`. For the API, pick a lightweight framework (Flask/http.server/Go net/http), implement `/api/versions/latest` and `/api/versions/newer_than?v=`, doing the greater-than comparison with the same semver-aware logic, and launch it with `nohup ... &` or similar so it stays alive in the background on 127.0.0.1:8080.
