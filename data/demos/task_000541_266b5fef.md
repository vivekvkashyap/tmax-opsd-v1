Start by reverse-engineering the schema: open `/home/user/system_graph.db` with `sqlite3` and run `.schema` to see the `nodes`/`edges` tables and any restriction flag column. Then treat `/app/path_verifier` as a black-box oracle: probe it with varied `(source, target)` pairs — pairs with short direct paths, pairs requiring 2-3 hops through plain nodes, and pairs whose only short path crosses a node you suspect is restricted — and record exit codes (`echo $?`) to form hypotheses about the compliance rule (e.g. a hop-count threshold, a restricted-node exclusion). Vary one factor at a time (hop count vs. restricted-node presence) to isolate which condition flips the result, rather than accepting the first hypothesis that fits a few samples. Once you have a rule that matches the oracle on all your probes, implement it in `/home/user/audit_filter.py`: connect read-only to the SQLite DB, build an adjacency structure, run BFS from `source` to find shortest-path hop count and check any restricted-node flags along that shortest path, then print exactly `ACCEPT` or `REJECT` per the rule. Before finalizing, re-test edge cases (equal to the threshold, paths that are tied for shortest) against the oracle to catch off-by-one errors.