Work this as a live recon-then-crack pipeline, not a lookup. First find the hidden service: scan localhost ports 8000-9000 (e.g. with `ss -ltnp`, `netstat`, or a quick loop of `curl` probes) to identify which one answers HTTP on 127.0.0.1. Query its `/` path with `curl -i` to capture the response headers and Set-Cookie line, reading off the exact 4-character salt from the custom header and the token value from the cookie — do not assume these values, read them from the live response. Then write /home/user/bruteforce.cpp to brute force the 6-digit PIN: for each zero-padded candidate from 000000 to 999999, concatenate it with the observed salt exactly as specified (PIN then salt), compute MD5 with OpenSSL's `<openssl/md5.h>` API, and compare the resulting hex digest against the observed cookie token, stopping at the first match. Compile with g++, link `-lssl -lcrypto` (or `-lcrypto` alone depending on how you call MD5), run it to recover the PIN, write only the 6-digit PIN to /home/user/recovered_pin.txt, and finally `chmod 400` that file so only the owner has read access.