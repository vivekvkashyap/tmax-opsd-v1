Start by running `tesseract /app/config_spec.png stdout` (try preprocessing with `convert` to grayscale/threshold if OCR output is noisy) and parse the `PORT:` and `TOKEN:` lines from the result — don't hardcode guessed values, derive them from the actual OCR output. Build `provision.sh` so idempotency is handled explicitly: at the top, check if `/home/user/service_data/server.pid` exists and if the PID in it is a live process (e.g. `kill -0 $PID`); if alive, exit 0 immediately without touching anything; if the PID file is stale, remove it and proceed to (re)start. The Python server itself should use `http.server`/`BaseHTTPRequestHandler`, binding `0.0.0.0:<extracted port>`, checking the `Authorization` header equals `Bearer <extracted token>` exactly, returning 200/"Authorized" or 401 accordingly. Launch it with `nohup ... >> access.log 2>&1 & echo $! > server.pid`, then have `provision.sh` itself exit while the server keeps running in the background. Finally emit a `logrotate.conf` with `daily`, `rotate 3`, `compress`, and `create 0644` directives targeting the log path, and actually execute `provision.sh` once at the end so the server is live for verification.
