Fix the two build issues independently, verifying each compiles clean before moving to integration. In `backend_rust/src/main.rs`, the bug is a genuine move-then-use error: `response_data` is moved into `_moved` and then used again in the struct literal — the fix is simply to remove the pointless `let _moved = response_data;` line (or clone before moving) so `response_data` is used directly and only once; run `cargo build` and read the borrow-checker diagnostic carefully to confirm the exact line before editing. In `client_cpp/CMakeLists.txt`, the `add_executable` exists but nothing links gRPC/Protobuf into it — add `target_link_libraries(feature_test PRIVATE gRPC::grpc++ protobuf::libprotobuf)` after the `add_executable` call, then rerun `cmake ..` and `make` in a fresh `build/` directory to confirm linking succeeds. Start the Rust server in the background and confirm with `ss -ltnp` or a quick `grpcurl`/log check that it's actually bound to `127.0.0.1:50051` before running the client. Finally invoke `feature_test 778899` and redirect its stdout into `/home/user/project/test_results.log`, checking the captured content matches what the server actually returned (don't hand-write the expected string into the log).
