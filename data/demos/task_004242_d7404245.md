In `job_hasher.c`, the fix is a Python-C-API concern, not an algorithm change: replace the call that extracts a C string from a Unicode object — `PyBytes_AsString` only works on `bytes` objects, so on Python 3 `str` objects you must use `PyUnicode_AsUTF8(strObj)` (or `PyUnicode_AsUTF8AndSize`) instead; check for a NULL return and clear the error rather than crashing. Rebuild with `python3 setup.py build_ext --inplace` (or `install`) so the compiled extension is importable, and verify by `python3 -c "import job_hasher"` before wiring it into the server. For the scheduling endpoint, implement classic activity selection: sort jobs by `end` ascending, greedily keep a job whose `start >= last_end` (equality allowed, per the spec), track the picked ids in order, then pass that id list into `job_hasher.hash_jobs(ids)` to compute the hash — don't recompute the hash yourself in Python. Run the Flask/FastAPI app bound to `127.0.0.1:8080` in the background (e.g. `nohup ... &` or `uvicorn/flask run &`), confirm it's listening with a quick `curl` health check, then issue the exact specified `curl -X POST ... -d '{...}'` and redirect its raw stdout into `/home/user/scheduler_service/result.json`.