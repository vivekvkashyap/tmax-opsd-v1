Parse the CSV manually in C with `fgets` + `strtok`/`sscanf` on `,` since a missing `engagement_score` shows up as two consecutive commas (an empty field) — check for that explicitly (e.g. `line[pos] == ','` immediately after the previous comma, or an empty token string) rather than trying to `sscanf` a float out of nothing and hoping it fails cleanly. Apply the imputation (0.50) and the outlier cap (session_duration > 1000.0 → 1000.0) to a working copy of the row's fields before computing distance, so the distance calculation always uses the cleaned values, not the raw ones. Compute Euclidean distance with `sqrt(pow(e-0.80,2) + pow(s-300.0,2) + pow(b-0.20,2))` (or manual multiplication instead of `pow` for speed) using `math.h`, and write every numeric output field with `%.2f` formatting including the distance. `run_pipeline.sh` should simply be `gcc /home/user/prepare_data.c -o /home/user/prepare_data -lm && /home/user/prepare_data`, and should be re-runnable idempotently (regenerating `clean_data.csv` fresh each run) so the verifier can invoke it directly.
