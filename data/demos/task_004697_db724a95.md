Start by fixing the environment issue that blocks `tests/test_apogee.py` from running (likely a Python path/import problem since the test does `from calc import calculate_apogee` but calc.py lives at the repo root — run it from the repo root, or set PYTHONPATH, or use `cd /home/user/orbital_calc && python3 tests/test_apogee.py`) — verify it runs and fails/passes appropriately before bisecting. Then write a small wrapper script that runs the test and exits 0 on precision-pass, nonzero on precision-fail (the test script itself already does `sys.exit(0/1)`, so a one-liner invoking it suffices). Use `git bisect start`, mark the current `HEAD` as bad and the `v1.0` tag as good, then run `git bisect run <your-wrapper>` so git automatically walks the ~200-commit range via binary search. Do not manually eyeball diffs across all commits — bisect is far more efficient. Once bisect reports the first bad commit, capture its full 40-character SHA with `git rev-parse <commit>` and write only that hash to `/home/user/bad_commit.txt`. Finish with `git bisect reset` to restore the working tree, and don't rewrite history or delete branches/tags.
