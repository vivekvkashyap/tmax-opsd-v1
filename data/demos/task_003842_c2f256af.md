Phase 1 relies on Linux's "deleted but still open" semantics: find the daemon's PID (`pgrep suspicious_daemon` or `ps aux`), then inspect its open file descriptors under `/proc/<pid>/fd/` — even though `secret_key.txt` no longer has a directory entry, the kernel keeps the inode alive as long as the process holds it open, so you can read the content straight from the fd symlink (`cat /proc/<pid>/fd/<N>` for whichever fd matches the deleted path, found via `ls -la /proc/<pid>/fd/`) and redirect it to `/home/user/recovered_key.txt`. Do not kill the process before doing this. Phase 2: read `daemon_src`'s current `auth.rs` to see the unsafe static-global pattern, then write a small Rust test binary that spawns two threads calling `authenticate` concurrently — one with the correct (recovered) key, one with a deliberately wrong key — in a tight loop with no synchronization, checking whether the wrong-key thread ever observes `true`; repeat many iterations since races are probabilistic. Phase 3: wrap that race-trigger test as the bisect run script and use `git bisect start`/`bad`/`good` with `git bisect run <script>` to walk the linear history automatically, then write the resulting bad commit's full SHA to `/home/user/bad_commit.txt` with no trailing whitespace.