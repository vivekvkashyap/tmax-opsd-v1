Parse `/home/user/backup_data/fstab.mock` by splitting each non-comment, non-blank line on whitespace and filtering rows where the third field equals `ext4`, keeping the second field (mount point) — skip lines starting with `#` and empty lines explicitly. For each qualifying mount point, derive the log filename by replacing every `/` with `_` and set up `logging.handlers.RotatingFileHandler(path, maxBytes=60, backupCount=2)` with a formatter whose format string is just `%(message)s` (no timestamp/level), attached to a fresh logger/handler per mount point (reuse across mount points will leak handlers onto the wrong logger — use a distinct logger name or remove/close handlers between iterations). Call `logger.info(f"VERIFIED: {mount_point}")` exactly 12 times per mount point, then let Python's rotation naturally produce `.log`, `.log.1`, `.log.2` files (rotation kicks in based on cumulative byte size, so don't try to compute counts manually — just glob for `<base>.log*` files after writing and count them). Finally write the mount_point -> count mapping as JSON via `json.dump` to `/home/user/summary.json`.
