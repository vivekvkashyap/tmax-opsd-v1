Parse the FASTA-like file with `awk`: track the current header (line starting with `>`, strip the `>`) and, on the following data line, split on whitespace into fields. For each model, check whether all trial values are numerically identical — do this by comparing every field against the first field as a number (`$i != first+0` style forces numeric comparison in awk) so equivalent representations like `0.5` and `0.50` are correctly treated as equal, not filtered differently by a naive string comparison. Skip (exclude) any model where all values are identical (zero-variance / non-convergent); for the rest, sum and divide by count to get the mean, then print with `printf "%s\t%.3f\n"`. Collect all qualifying lines and pipe through `sort` (alphabetical on model ID, which is the default field-1 string sort) before writing to `/home/user/valid_models.tsv`. Validate your logic against a couple of models by hand-computing the mean before trusting the full pipeline, since off-by-one errors in field indexing (header line vs data line) are the easiest way to silently misalign IDs and values.
