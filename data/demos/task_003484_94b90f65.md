Work this like a real incident: find evidence first, then fix the code, then prove the fix. Grep `/home/user/logs/*.log` for "PANIC" or "panic" to find the node with the crash trace; the panic line and the line right after it will show the exact hex payload that triggered the slice-bounds error — copy it exactly (no extra whitespace/newline) into `/home/user/crash_payload.txt`. Then read `parser.go`'s TLV parsing function: it reads a length field and slices `data[offset:offset+length]` without checking that `offset+length <= len(data)`, which panics on truncated/malformed input. Fix it by adding a bounds check before the slice operation and returning `errors.New("invalid length")` instead of panicking when the declared length exceeds the remaining bytes. Don't just clamp the length silently — the spec requires returning that specific error. After editing, run `go test -fuzz=FuzzParsePacket -fuzztime=2s` from `/home/user/project`; if it still crashes, look for other unchecked slice operations nearby (e.g. reading the length field itself) that fuzzing may uncover beyond the original reported payload.
