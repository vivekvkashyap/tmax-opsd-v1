Treat the two steps as fully independent deliverables. For Step 1, install a speech-to-text tool (e.g. `pip install openai-whisper`, plus `ffmpeg` if needed for decoding), transcribe `/app/speech.wav`, then lowercase and strip punctuation before writing to `/home/user/transcript.txt` — don't hand-guess the words, let the model output drive it, and sanity-listen/inspect if unsure. For Step 2, since your output must be bit-exact vs a hidden oracle at `/app/oracle_extractor`, implement each stage precisely per spec and cross-check against the oracle constantly during development rather than only at the end: (a) unwindowed FFT of the full input via a real FFT crate (e.g. `rustfft`), taking magnitudes of bins 0-15; (b) load the 5x16 matrix from `/app/projection_matrix.txt` and do straightforward matrix-vector multiplication; (c) chunk the signal into non-overlapping 128-sample frames, compute each frame's sum-of-squares energy, then implement the bootstrap CI using the EXACT LCG given (X starts at 42, updates as `X = (X*1103515245+12345) % 2147483648`, index = `X % F`) — implement this with 64-bit integer arithmetic to avoid overflow, and be careful about state ordering (each of the 1000 iterations draws F fresh indices sequentially, advancing the single shared LCG state throughout, not resetting per iteration). Sort the 1000 bootstrap means and take indices 25 and 975. Format both output lines to exactly 4 decimals and diff against the oracle across many random-length, random-value inputs before considering it done.
