Work in two independent stages. Stage 1: use `ffmpeg -i /app/session.mp4 -vf fps=<rate> frame_%04d.png` (or similar) to dump frames, then visually inspect/OCR them to find the terminal command showing an `openssl enc -aes-256-cbc -k "..."` invocation — read the exact key off the screen text yourself rather than guessing; don't hardcode a key you haven't actually observed. Stage 2: build `sanitizer.cpp` with three independent checks, ANDed by "any check fails => EVIL": (a) scan FILENAME for `../`, `..\\`, and URL-decoded traversal sequences like `%2e%2e%2f` (decode percent-escapes before checking, or check both raw and decoded forms); (b) base64-decode PAYLOAD, SHA256 it (OpenSSL's `EVP_sha256`/`SHA256`) and compare hex to the SHA256 field; (c) AES-256-CBC decrypt the decoded payload with the recovered key and an all-zero 16-byte IV (OpenSSL EVP API), verify PKCS7 padding is valid, and check for `REDTEAM_STRIKE` in the plaintext — a decrypt failure or bad padding means this check simply doesn't flag EVIL (fall through to CLEAN if others pass). Print exactly `EVIL`/`CLEAN` and set exit codes 1/0. Compile, then loop over both corpus directories yourself and confirm every file classifies correctly before finishing — don't assume it works.
