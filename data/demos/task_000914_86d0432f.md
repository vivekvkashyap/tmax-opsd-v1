First reproduce the crash by running `python3 process.py` and note exactly which row/iteration triggers `math domain error` — this tells you the running sum/sum_sq state at failure. The bug is catastrophic cancellation in the naive two-pass variance formula `(sum_sq - n*mean^2)/(n-1)`: when values are large but variance is tiny, floating-point rounding makes this go slightly negative, and `sqrt()` of a negative raises the domain error. For the MRE, don't just replay the whole CSV — construct the smallest hardcoded list of floats (feed them one by one via `update()`) that reproduces the same failure mode (e.g., a few large-magnitude values with tiny spread), then call `get_stddev()` and print the caught exception. Fix stats.py properly with Welford's online algorithm (track `mean` and `M2` incrementally, update via `delta = x - mean; mean += delta/n; delta2 = x - mean; M2 += delta*delta2`, then `variance = M2/(n-1)`) rather than just clamping negative variance to zero, since clamping masks the real precision bug. Finally rerun `python3 process.py > /home/user/success.txt` and confirm it completes without exceptions across the full dataset.
