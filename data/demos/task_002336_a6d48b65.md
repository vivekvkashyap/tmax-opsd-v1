Diagnose why the root-finder fails before changing code: a near-singular Jacobian at the symmetric initial guess `[0.5, 0.0, 0.0]` typically happens because swapping mu1/mu2 with w->1-w leaves the moment equations invariant, so the Jacobian is degenerate at that exact symmetric point. Fix by choosing a less-degenerate initial guess (e.g. estimate the two modes from the data — split the sample into halves by sign/median or use a quick k-means-like split to get rough starting means) and/or switch to a more robust method like `'lm'` or `'broyden1'` in `scipy.optimize.root`; after solving, enforce `0<=w<=1` and swap (w,mu1,mu2)->(1-w,mu2,mu1) if mu1>=mu2 so the convention holds. For the Wasserstein check, draw 100,000 samples from the fitted two-component Gaussian mixture (sample component membership via w, then draw from N(mu1,1) or N(mu2,1) per draw) and compare against the raw data.txt values with `scipy.stats.wasserstein_distance`. Write params.txt and distance.txt in the exact specified formats, then write test_fit.py as an independent script that reloads both files and re-derives the same distance, asserting it's below 0.1.