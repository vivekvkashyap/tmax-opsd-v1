For the C parser, use `open()`, `flock(fd, LOCK_SH)`, then `read`/`pread` (not `fopen`) to grab the first 64 bytes, check the `\x7FELF` magic (bytes 0-3) and the EI_CLASS byte (offset 4 == 2 for 64-bit) before treating it as `Elf64_Ehdr`, then read the 8-byte little-endian `e_entry` at offset `0x18` and print it as `0x%lx`; release the lock with `flock(fd, LOCK_UN)` before exiting. For the bash traversal, use `find /home/user/artifacts -follow ... ` is dangerous with loops — instead walk without `-follow`, and for each entry (including symlinks) resolve the real target with `realpath` inside a subshell/loop, wrapping the resolution so a broken/looping symlink (`realpath` failing or `readlink -f` hitting `ELOOP`) is caught and skipped rather than hanging or crashing the script (e.g. `realpath -q` returning nonzero, or capping traversal depth). Track already-processed real paths in an associative array or a temp file so each unique regular file is fed to `/home/user/elf_parser` exactly once. Append only lines where the parser's stdout isn't `NOT_ELF`, using the real (resolved) path in the report.