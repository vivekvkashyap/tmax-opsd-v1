Start by inspecting /app/vendor/libcsvquery/Makefile and the source files — the build failure is a compiler-standard mismatch: the Makefile pins an old C++ standard flag while the source uses newer C++ language features (check for things like structured bindings, string_view, etc. in the .cpp/.h files). Fix by bumping the CXXFLAGS standard flag to match what the code actually requires, then confirm `make -C /app/vendor/libcsvquery` exits 0 and produces libcsvquery.a — don't touch the source logic itself. For Part 2, write /home/user/sanitizer.cpp to parse the JSON pipeline file (a lightweight hand-rolled or single-header JSON parser is fine) and reject (print EVIL) if: any collection/$lookup target contains '.' or '/', any $lookup/$match target starts with 'private_' or 'system_', or the raw JSON contains '$where' or '$javascript' anywhere (search recursively through nested objects/arrays, not just top-level keys). Otherwise print CLEAN. Compile linking against the built libcsvquery.a. Test against both /app/corpus/clean and /app/corpus/evil directories yourself before finishing — the verifier requires 100% correct classification on both, so write a quick loop that runs your binary over every file in each folder and checks the expected verdict.