Structure the Rust program in stages: recursively walk `/home/user/sys_config/` (manual `fs::read_dir` or `walkdir`), filtering by `.cfg` extension. Implement RLE by iterating bytes with a running `(current_byte, count: u8)` pair — flush `[count, byte]` when the next byte differs or count reaches 255, and don't forget to flush the final run at end-of-buffer. For atomic writes, mirror the relative path under `/home/user/snapshots/` with `.rle` appended, `fs::create_dir_all` the parent dirs, write to a `<name>.cfg.rle.tmp` file first, then `std::fs::rename` it to the final `<name>.cfg.rle` — never write directly to the final path. Track each relative path and its compressed size, sort the entries alphabetically by path before writing the log, and format each line exactly as `<relative_path> -> <size> bytes`. Compile and actually run the binary so the snapshots and log file exist on disk, not just compile cleanly.