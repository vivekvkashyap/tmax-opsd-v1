Locate the join logic first: grep the vendored package (`grep -rn "pd.merge\|how=" /app/text_feature_toolkit`) to find where the metadata join happens. The bug is a classic pandas pitfall — a `how="left"` merge preserves every row from the main dataframe, injecting NaN for unmatched metadata keys, which silently upcasts an integer ID column to float64. Change it to `how="inner"` so rows without matching metadata are dropped entirely rather than padded, keeping the ID column's dtype as int64. After editing the source, reinstall the package into the environment (e.g. `pip install -e /app/text_feature_toolkit` or equivalent) so the fix actually takes effect at import time — editing the file alone isn't enough if it's already installed elsewhere. Then write `/home/user/process_stream.py` to read one line of JSON from stdin, extract `text` and `meta_id`, look up `meta_id` against `/app/reference_meta.csv` via the fixed `text_feature_toolkit.processor` module, and print the resulting integer token list comma-separated with no brackets, or `INVALID` if the meta_id has no match. Test it manually against several `meta_id` values (both present and absent in the CSV) via `echo '...' | python3 process_stream.py` before finishing, since the grader fuzzes thousands of random inputs and needs bit-exact behavior.