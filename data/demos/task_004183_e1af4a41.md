Since two different timestamp formats appear in the same file, parse each row's timestamp field by first detecting which format it matches (contains a literal " UTC" suffix vs. ISO 8601 with trailing "Z" and a "T" separator) and convert each to epoch seconds accordingly — in bash this is easiest via `date -u -d "<value>" +%s` after normalizing the string (GNU date parses both formats once the "UTC"/"Z" markers are handled), or awk/sed to strip formatting differences before feeding to `date`. Trim and uppercase the Sensor ID field with `tr` and parameter expansion or `sed 's/^ *//;s/ *$//'` plus `tr a-z A-Z`. For remarks, strip everything except letters/digits/spaces (e.g. `tr -cd 'A-Za-z0-9 '` or a sed character-class substitution), uppercase, and trim ends without collapsing internal double spaces. For the rolling average, process rows sequentially in an array/loop, at each row average the current value with up to the two immediately preceding values (window shrinks to 1 or 2 for the first rows), and format with `printf "%.2f"`. Assemble all five fields comma-joined into `/home/user/cleaned_data.csv` with no header, then run process.sh and diff the output against your hand-computed expectation for the first few rows to catch off-by-one window or timezone errors early.