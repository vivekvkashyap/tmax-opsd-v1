Break this into transcription, extraction, database, and API layers, and test each independently before wiring them together. First transcribe `/app/dictation.wav` with a real ASR library (e.g. `openai-whisper` or `SpeechRecognition`+`pocketsphinx`) — do not guess or hardcode text, run the model and capture its actual output string. Parse the researcher name and city from the transcript using a regex matching the stated format `Subject: <name>. Location: <city>.`, tolerating minor casing/punctuation differences since the ASR output may not be perfectly clean. For the database step, write parameterized SQL (never string-format values into the query) that joins `researchers` -> `researcher_project` -> `projects` <- `facility_project` <- `facilities`, filtering on the extracted name and city, using an intersection (e.g. `INTERSECT` or a join on shared `project_id`) so you only get projects linked to BOTH the researcher and the facility. Sum `budget` over that result set. Sort project titles alphabetically before building the JSON response. Finally implement a `GET /api/result` handler with Python's `http.server` bound to `127.0.0.1:8080`, launch it as a background/daemon process so it stays alive for external verification, and manually curl the endpoint yourself to confirm the JSON shape matches exactly before finishing.
