First recover the threshold value from `/app/threshold.png` using `tesseract` (crop/preprocess with ImageMagick if OCR text is garbled), and parse out the numeric `T` from whatever "THRESHOLD = ..." style text it contains — don't hardcode a guessed number. Scaffold the Rust project with `cargo new sim_filter` inside `/home/user/`, add a JSON dependency (`serde`/`serde_json`) in `Cargo.toml`, and parse the input file's `{"time": [...], "values": [...]}` structure. Compute the population standard deviation of `values` (divide the sum of squared deviations by `n`, not `n-1` — using sample stddev here is a common bug that will misclassify borderline trajectories) and the mean, then compute `|value - mean| / std_dev` for every element and compare each to `T`. Exit with `std::process::exit(1)` if any Z-score exceeds `T`, else `std::process::exit(0)` — handle the zero-stddev edge case explicitly so you don't divide by zero. Build with `cargo build --release` (the release path is required by the entry point) and manually test against a couple of hand-crafted stable/unstable JSON files before trusting it against the full corpus.