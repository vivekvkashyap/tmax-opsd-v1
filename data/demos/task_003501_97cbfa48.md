Start by reading `server.py` to learn the exact metadata/download JSON shapes and the rate-limit behavior, then design the C++ proxy around three pieces: (1) an in-memory tree/map keyed by artifact id storing reconstructed integer arrays, so a request for an already-resolved id or an already-fetched parent never re-hits the backend; (2) a recursive/iterative resolver that, on a cache miss, calls `GET /metadata/<id>` to find the parent, recursively resolves the parent (from cache if possible), fetches the diff via `GET /download/<id>`, computes `artifact[i] = parent[i] + diff[i]`, verifies `sum(artifact) % 256 == checksum`, and only then caches and returns it; (3) a self-imposed client-side rate limiter/token-bucket around outbound calls to `127.0.0.1:8081` capped at 5 req/s, since the naive "forward everything" approach will blow through the limit on cache misses and get 429'd. Use `cpp-httplib` for both the inbound and outbound HTTP, and make sure concurrent requests to the same not-yet-cached ancestor don't each trigger duplicate backend calls (use a mutex/lock per-id or a global lock around the cache). Build with CMake into `/home/user/app/proxy/build`, launch both services, then run `benchmark.py` and check its printed throughput.