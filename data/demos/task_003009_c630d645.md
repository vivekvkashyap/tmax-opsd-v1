Verify the cert chain with `openssl verify -CAfile /home/user/certs/ca.crt /home/user/certs/server.crt`; write `CERT_STATUS: VALID` or `INVALID` as line 1 of the report based on that command's actual result, not an assumption. Install flask (`pip install flask`), then start `/home/user/target_env/server.py` in the background (`python3 server.py &`) and confirm it's listening on 127.0.0.1:8080 before probing it. Read `server.py` to find the vulnerability: the `/auth` endpoint builds a raw SQL string by directly interpolating `username`/`password` into a query, which is classic SQL injection — do not try to guess the admin password. Exploit it with a curl/Python POST to `/auth` using a username payload that comments out the password check (e.g. an OR/`--`-style injection targeting the `username='...' AND password='...'` pattern) and an empty or arbitrary password; parse the JSON response for the `token` field. Append `TOKEN: <that value>` as line 2 of `/home/user/pentest_report.txt`, ensuring the file is exactly two lines with no extra blank lines.