First recover the hidden parameters from the video: use ffmpeg to extract frames around the 2-3 second mark (e.g. `ffmpeg -i /app/rotation_capture.mp4 -vf fps=30 frame_%03d.png` or seek directly with -ss) and read the on-screen "PARAMS: CHUNK=<size>, ALGO=<algo>" text off the extracted frame(s) — try a couple of nearby frames if one is blurry, and use OCR (tesseract) or visual inspection since you can view images. Hardcode the exact size and algorithm you read into the Rust program, don't guess common defaults. In /home/user/archiver, use the `flock` crate or raw libc flock() to hold an exclusive lock on /tmp/artifact.lock for the duration of processing (open/create the file first). Read all of stdin into a buffer, split into fixed-size chunks (last one may be short), compute the checksum algorithm you extracted per chunk, and write the exact binary layout described (little-endian u16/u16/u32 headers per segment, ASCII header/footer markers, u32 total length) using byteorder or manual to_le_bytes(). Build with `cargo build --release` to the exact target path, and manually round-trip a few known byte sequences through your implementation to sanity-check checksum values before trusting it against the fuzz harness.
