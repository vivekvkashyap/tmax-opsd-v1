First bring up the legacy service (`python /app/legacy_system/server.py`) and probe all three endpoints (`/api/entities`, `/api/accounts`, `/api/transactions`) to infer the schema empirically rather than assuming field names — note which fields link entities to accounts (an ownership foreign key) and which link accounts to accounts (a transfer with an amount). Build an in-memory undirected graph where entity and account IDs are nodes, ownership edges connect entity<->account, and each transaction adds/reinforces an account<->account edge; since `/shortest_path` treats all edges as undirected, use plain BFS from `src_entity` to `dst_entity` over this graph to get the node-id path. For `/exposure`, be precise about "1 transaction hop": from the entity's own accounts, follow only account-to-account transaction edges (not ownership edges) one step out, and for each resulting neighbor account sum the transaction amounts in both directions (from-entity-account to it, and it to entity-account) as the sort key; sort by that volume descending, tie-break alphabetically by account id, then slice with `limit`/`offset`. Implement the API with Flask/FastAPI on `127.0.0.1:8080`, keep it running in the background, and manually curl both endpoints against a couple of known entity pairs to sanity check the path and ordering logic before finishing.