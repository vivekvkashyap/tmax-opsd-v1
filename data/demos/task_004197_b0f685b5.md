First recover the hidden spec from the image: run OCR (e.g. `tesseract /app/backup_rules.png -` after installing tesseract, or open/inspect the image another way) to read out the exact hashing algorithm, the string-concatenation format, and the secret salt text embedded in the picture — do not guess these values, they must come from what the image actually says. Once you have them, write `/home/user/backup_name_gen.go` implementing exactly that formula (concatenate the pieces in the stated order, hash with the stated algorithm, print the hex digest with no trailing newline), then `go build -o /home/user/backup_name_gen backup_name_gen.go`. Test it yourself against a couple of hand-picked (service, date) pairs and confirm the output is a fixed-length hex string with no stray whitespace, since a fuzz-style equivalence check will call your binary with many random argument pairs and diff byte-for-byte against a hidden oracle — any formatting difference (extra newline, wrong case, wrong field order) fails everything. Separately, write the Expect script to match each interactive prompt substring in order and send the right reply, and write the Go reverse proxy using `net/http/httputil.NewSingleHostReverseProxy` pointed at the backend, listening on the specified address, run persistently in the background.