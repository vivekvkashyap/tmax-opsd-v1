Build and validate each stage against the given corpus before generalizing to the hidden one. In `classifier.c`, read raw stdin as an array of `int16_t` samples (watch endianness — the format is little-endian, which matches typical x86 hosts but read via `fread` into a signed 16-bit buffer explicitly rather than assuming char layout); compute MAA as the mean of `abs(sample)` over all samples, and ZCR as the count of adjacent pairs where one sample is strictly positive and the other strictly negative (samples exactly zero don't count toward a "crossing") divided by the number of adjacent pairs. Exit 0 only if `MAA > maa_threshold && ZCR < zcr_threshold`, else exit 1. For `tune.sh`, do an actual grid search (nested loops over candidate threshold values) running the compiled classifier against every file in `/app/corpus/clean` and `/app/corpus/evil`, scoring each combination, and pick the one with 100% separation — don't hand-pick thresholds by eyeballing typical values, since the hidden corpus must also separate cleanly at whatever you choose. `filter.sh` just calls the classifier with the tuned constants. For `process_source.sh`, use `ffmpeg`/`sox` to convert `source.wav` to raw PCM, `split` or `dd` it into exact 1-second (16000-byte) chunks, filter each through `filter.sh`, concatenate survivors in order, and wrap the result back into a WAV with correct header (`sox -r 8000 -e signed -b 16 -c 1` raw-to-wav conversion).
