Work through the three failures in order since each blocks the next. First, write `build.rs` using the `cc` crate's builder API (`cc::Build::new().file("src/c_src/fast_parse.c").compile("fast_parse")`) so Cargo actually compiles and statically links the C file during the build step — an empty or missing build.rs is why nothing links. Second, once it compiles, run `cargo test` and trace the FFI call in `src/lib.rs`: the C function takes a buffer and a length, but the Rust side is passing the *capacity* of the String rather than its actual populated length, so the C loop reads past the written bytes into uninitialized/garbage memory — fix it to pass the correct length accessor for the string's actual content. Third, add a minimal `.github/workflows/ci.yml` with a job on `ubuntu-latest` that checks out the repo and runs `cargo test`. Finally verify `cargo test` passes cleanly end to end before writing the completion marker file with the exact required text.