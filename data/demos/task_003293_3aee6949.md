Implement this as a straightforward vectorized numerics pipeline in NumPy/SciPy, matching the spec exactly since precision and RNG state matter for reproducibility. Seed with `numpy.random.default_rng(42).normal(0, 1, (1000, 2))` exactly as given to get initial `(x0,y0)` for 1000 particles — do not reseed per-run or reuse the generator across different dt runs (each dt run should start from the same fresh seeded initial state). For each dt, compute `steps = round(5.0/dt)` and vectorize the Forward Euler update across the whole (1000,2) array each iteration (no Python-level particle loop): compute `r2 = x**2+y**2`, then `dx`, `dy` per the given ODE, then update x and y simultaneously (use the old x,y for both derivatives, not sequential coupling). After all dt runs, take the `dt=0.001` run's final x-array as the reference distribution, and for each other dt compute `scipy.stats.wasserstein_distance` against it. Scan the dt list from largest to smallest (or evaluate all and pick), find the largest dt (excluding 0.001) whose distance is strictly below 0.05, and write just that float to `/home/user/perf_profile/optimal_dt.txt` after `mkdir -p`'ing the directory first.