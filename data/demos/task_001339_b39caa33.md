First recover the operational parameters by running OCR on `/app/pipeline_specs.png` (`tesseract /app/pipeline_specs.png -`, retrying with different `--psm` modes if garbled), then read the extracted text to identify the deduplication key field name, the sort field name, and the numeric `MAX_STEP_DELTA` threshold — derive these from the OCR output, don't guess. Implement the pipeline as: (1) read all JSONL lines; (2) deduplicate by keeping only the record for each key that appears LAST in file order (a dict keyed by the dedup key, overwritten on each occurrence, achieves this); (3) sort deduplicated records ascending by the extracted sort field; (4) keep the first record unconditionally, then for each subsequent record compare its `value` against the *last accepted* record's value (not the previous list entry) — drop if the absolute difference exceeds the extracted threshold, else accept and update "last accepted". Print only clean JSONL to stdout, no debug output. Since this is fuzz-tested for bit-exact equivalence, write your own random JSONL cases with duplicates and spikes and trace through expected behavior before trusting your implementation.