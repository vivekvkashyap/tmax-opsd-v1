Treat `/app/legacy_audit_tool` as a black box you learn from by probing, not reading — feed it small hand-crafted CSVs via stdin (including edge cases: ties in vulnerability index, severity exactly at the 5 threshold, fewer than 10 targets, an actor appearing under multiple targets) and diff its stdout byte-for-byte to discover exact JSON formatting (key order, presence/absence of spaces, no trailing newline vs one, etc.) — do not guess formatting, since the grader requires bit-exact output. Implement `/home/user/audit_processor.py` in pure Python using `csv`/`collections`: parse stdin, drop `SeverityScore < 5`, accumulate `actor_risk[actor] += severity`, track the *set* of unique actors per target, then compute `vulnerability_index[target] = sum(actor_risk[a] for a in that set)` (sum unique actors' total risk, not raw severities again). Sort targets by `(-vulnerability_index, target)` and take the top 10. Emit JSON with `json.dumps(result, separators=(',', ':'))` for the exact "target"/"vulnerability_index" schema with no extra whitespace, and print nothing else to stdout. Validate by running both the legacy binary and your script on the same fuzzed inputs and diffing outputs before finishing.