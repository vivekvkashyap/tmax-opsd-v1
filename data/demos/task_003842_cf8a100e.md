First transcribe `/app/schema_instructions.wav` (ffmpeg to extract audio if needed, then a speech-to-text tool such as `whisper`) to recover the exact damping factor and iteration count spoken in the memo — use those transcribed values, not assumed defaults like 0.85/100. Then write `pagerank.c` to: read `edges.csv` (source,target pairs), build a node ID mapping (nodes aren't necessarily 0..N-1 contiguous, so build a set/sorted-unique-array of all IDs seen as either source or target and map them to dense indices), construct CSR-style adjacency (an outgoing-edge array plus per-node out-degree) for O(E) iteration performance, and separately track which nodes have out-degree 0 (dangling). Initialize PR(i) = 1/N for all nodes. Each iteration: compute the total dangling PR mass first (sum PR over dangling nodes), then for every node compute the formula given — teleport term (1-d)/N, plus d times the sum of PR(j)/L(j) over incoming edges, plus d times danglingMass/N — using a fresh array so all nodes update simultaneously (don't overwrite PR values while other computations in the same iteration still need the old ones). Run for the transcribed iteration count, then write `node_id,pagerank_value` sorted by node_id ascending with `%.9f`. Compile with `-O3` and confirm runtime is fast since the graph has tens of thousands of edges.