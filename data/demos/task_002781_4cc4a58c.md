Fix the algorithm first, prove it with the patch, then build the harness.
1. In `/home/user/polymath/src/integrate.cpp`, Simpson's 1/3 rule requires odd-indexed interior points weighted 4 and even-indexed weighted 2 — swap the current ternary's `4.0`/`2.0` assignment to match that rule (check against the known result for integrating x^3 over a simple range to confirm correctness before moving on).
2. Copy the original buggy file aside, apply your fix, and generate `simpson.patch` with `diff -u original.cpp integrate.cpp > /home/user/polymath/simpson.patch` (or equivalent) so it's a real unified diff of just that change.
3. Write `/home/user/polymath/Makefile` from scratch with `build` (mkdir -p bin && g++ ... -o bin/integrate), `test` (depends on `build`; runs `pytest tests/test_integration.py`), and `clean` (`rm -rf bin`) targets.
4. Run `make test` end-to-end and confirm pytest passes and the binary's output is numerically close to the analytic integral — fix any remaining off-by-factor or precision issues before finishing.
