Implement the script's steps in the exact order specified, since rotation must happen before writing the new log. In Python: list regular files (skip symlinks, e.g. `os.path.isfile(p) and not os.path.islink(p)`) in `/home/user/logs`, sum their sizes, and while the total is >= 100 bytes, delete the file with the oldest `mtime` and re-check the total — do this as a loop, not a one-shot check, since one deletion may not be enough. Read `/home/user/ports.txt`, and for each port attempt `socket.create_connection(("127.0.0.1", port), timeout=1)` in a try/except to classify UP/DOWN, then sort ports numerically before writing lines `PORT <port>: UP`/`DOWN` to a new file named with the current integer epoch (`int(time.time())`). Update the `latest.log` symlink using a relative target (just the filename, not full path) via `os.symlink(filename, tmp_link); os.replace(tmp_link, final_link)` to atomically swap it. For scheduling, add a crontab line `* * * * * python3 /home/user/net_monitor.py` via `crontab -l` piped into a new crontab (preserving any existing entries). Finally, run the script manually 4 times with `sleep 1`+ between runs to produce distinct timestamps and observe rotation actually deleting old files, confirming the final directory has at most 3 log files.
