Extract the AES key from the certificate's Subject rather than guessing it: use `openssl x509 -in /home/user/cert.pem -noout -subject` (or the Python `cryptography`/`pyOpenSSL` API) and pull out exactly the `O=` (Organization Name) attribute value — be careful with the ordering/format `openssl` prints subject fields in, and strip any surrounding whitespace so you get the raw key bytes/string correctly. Then for each entry in `traffic.json` that has an `X-Telemetry-Data` header: base64-decode it, split off the first 16 bytes as the IV, treat the remainder as AES-128-CBC ciphertext, decrypt with the extracted key and IV, and unpad using PKCS7 (Python's `cryptography` `padding.PKCS7(128).unpadder()`, or `pycryptodome`'s `unpad`) — decrypting without correctly stripping PKCS7 padding will corrupt/garble the tail of your plaintext and can cause you to miss or misjudge the signature. Search the decrypted plaintext for the literal marker `[C2_EXFIL_START]`; for every request where it's present, collect that request's `src_ip`. Deduplicate the IPs, sort them in ascending order (note: IP-string sort isn't the same as numeric octet sort — parse each octet as an integer to sort correctly), and write one IP per line to `/home/user/malicious_ips.txt`.
