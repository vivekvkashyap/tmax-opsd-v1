Phase 1: don't rewrite the vendored package — locate the exact build error first by running `pip install -e /app/vendored/ts_distance` (or `python setup.py install`) and reading the traceback; it will point at a bad import/typo in `setup.py` (e.g. an import of a nonexistent module name similar to `setuptools`). Fix only that line, then reinstall and confirm `from ts_distance import compute_dtw` works with a quick sanity call. Phase 2: build `/home/user/filter.py` defensively — wrap the whole thing in try/except so any decode/parse/key error falls through to exit code 1. Detect encoding by attempting UTF-8 decode first and falling back to UTF-16-LE (or use a BOM/heuristic check) rather than assuming one; do strict linear interpolation only for interior `null`s using the nearest non-null neighbors on each side. Build the comma-separated one-decimal string exactly as specified before hashing with `hashlib.sha256(...).hexdigest()` and compare against the given malicious hash string. Call `compute_dtw` on your imputed list vs the baseline `[0.0..9.0]` and threshold at <15.0. Validate against every file under `/app/data/clean` and `/app/data/evil` before finishing, since the real grading corpus is hidden and adversarial.
