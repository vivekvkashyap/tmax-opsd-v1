Work phase by phase and verify each output before moving on. Phase 1: fit `TfidfVectorizer(stop_words='english')` once on the full `text` column, then process rows sequentially — for each row after the first, compute cosine similarity against the TF-IDF vectors of only the rows already kept (not all prior rows, and not the full dataset), discarding if the max similarity is >= 0.80; get this exact sequential logic right since it's stateful and easy to implement as a static, unordered comparison by mistake. Phase 2: build a `Pipeline([TfidfVectorizer, LogisticRegression(...)])` and wrap it in `GridSearchCV` with `param_grid={'logisticregression__C': [0.1, 1.0, 10.0]}` (or the equivalent named step), `cv=3`, `scoring='accuracy'`; after fitting, pull `grid.best_params_` and `grid.best_estimator_`, saving the C value as a plain float text file and the full pipeline via `joblib.dump`. Phase 3: load the joblib model in a minimal Flask app, expose `POST /predict` calling `model.predict([text])[0]`, and run it in the background (e.g. `nohup python3 app.py &`) before benchmarking. For the benchmark script, use `date +%s.%N` around a loop of 50 `curl` calls and write the elapsed seconds as a plain number — confirm the server is actually up (curl a test request) before timing.
