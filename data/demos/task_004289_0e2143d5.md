Structure the Rust binary as three clear passes rather than one tangled walk. Pass 1 (classify): use `walkdir` (or manual `fs::read_dir` recursion) over `/home/user/raw_data/`, open each file and read its first bytes into a buffer to check magic signatures — compare exact byte prefixes for ELF (`0x7F 45 4C 46`), the two SQLite WAL variants, and the literal ASCII prefix `; FLAVOR:` for gcode; skip files that read too short to contain the signature. Pass 2 (organize): for each match, canonicalize the absolute source path, compute SHA-256 of that path string (the `sha2` crate) and hex-encode it, then create a symlink at `/home/user/categorized/<type>/<hash>_<original_filename>` pointing at the absolute source path (`std::os::unix::fs::symlink`), creating the type subdirectories first. Pass 3 (archive): after ALL symlinks exist, `read_dir` `/home/user/categorized/` recursively, collect relative paths (e.g. `elf/hash_x.elf`), sort them lexicographically, then for each: write `path_len` (u16 LE), the UTF-8 path bytes, `uncompressed_size` (u64 LE) of the *target file's* real content, then RLE-encode that content — walk the byte buffer, count consecutive equal bytes, emit `[min(count,255)][byte]` pairs and continue the run for any remainder past 255 rather than resetting to a new byte. Write the `CPACK\0\0\0` header first. Build with `cargo build --release` and actually run the binary so the symlinks and `.cpack` file exist on disk before finishing.