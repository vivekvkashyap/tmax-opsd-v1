This is a stiff-ODE parameter estimation pipeline; build it stage by stage and validate each stage independently before chaining them. First write `clean_data.sh`: strip comment lines and inconsistent delimiters/whitespace from `raw_experiment.dat` with `sed`/`awk`, parse the remaining numeric rows, then interpolate (e.g. with a small Python/awk helper using linear interpolation) onto exactly 100 evenly spaced time points from t=0 to t=100, emitting `time,A,B,C` to the clean CSV. Second, fix `integrate.py`: the given RK4 diverges because the reaction is numerically stiff (this looks like a Robertson-type system with very different rate-constant magnitudes), so replace the fixed-step explicit integrator with `scipy.integrate.solve_ivp` using `method='BDF'` or `'Radau'` and reasonable `rtol`/`atol`, keeping the ODE right-hand side and initial conditions exactly as given. Third, write the Metropolis-Hastings sampler (propose new k1/k2/k3 via a small random walk, accept/reject using a Gaussian log-likelihood with sigma=0.05 comparing simulated vs. clean_experiment.csv trajectories) and drive it from `run_mcmc.sh`, collecting >=5000 post-burn-in samples (drop the first 20%). Since correctness is judged by feeding your posterior means back into the black-box reactor and comparing MSE against ground truth, focus on getting the stiff integration and likelihood right rather than the exact sampler implementation details — a working, converging sampler on a correct likelihood is what matters. Write the final `k1,k2,k3` posterior means as a single comma-separated line to `/home/user/pipeline/results/posterior_means.csv`.
