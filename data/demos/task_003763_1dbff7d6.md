Start by transcribing `/app/compliance_briefing.wav` (e.g. with `whisper`) and reading the transcript carefully to extract the exact banned node label and exact banned relationship type as literally stated — do not guess plausible-sounding names, the audio is the ground truth. Note Cypher conventions: node labels appear as `:LabelName` (CamelCase) inside parentheses, relationship types appear as `:REL_TYPE` (UPPER_SNAKE_CASE) inside brackets — so parse or regex-match the query text for `:<BannedLabel>` and `:<BANNED_REL>` occurrences case-sensitively, rather than naive substring search which could false-positive on partial word matches (anchor on the `:` prefix and a word boundary). In `/home/user/audit_query.py`, read the file path from argv, load the query text, check for either banned token, print `REJECT`/exit 1 or `ACCEPT`/exit 0 accordingly. Before finalizing, test your script against the example clean and evil query patterns shown in the task description, and think through edge cases (label/rel type appearing as a substring of another identifier) since the hidden corpora are graded for 100% separation.
