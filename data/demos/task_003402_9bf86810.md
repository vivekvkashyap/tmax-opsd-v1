Start by running `/app/start_services.sh` and confirm both fixtures respond (`curl 127.0.0.1:8081/bulk_data`, `psql` to etl_db). Then `go mod init` in `/home/user/etl_pipeline`, add `github.com/lib/pq`, and build one binary that both creates the table and serves on `127.0.0.1:9090`. Structure the pipeline as distinct stages and get each stage's ordering right — that is where naive attempts fail:

1. Extract the JSON array, then group by `sensor_id` and sort each group by `timestamp` ascending BEFORE any filtering.
2. Filter to even timestamps only. Do the rolling average over the *sampled* (even) sequence, not the raw data.
3. Rolling avg = mean of the previous up-to-2 sampled values, excluding the current one; 0 when no prior values exist. Flag an anomaly only when there is at least one prior value AND current value is strictly `> 1.5 * rolling_avg`. Record the `rolling_avg` used at detection time.
4. On each `/run_pipeline`, TRUNCATE/DELETE the table first for idempotency, then bulk insert.

For `GET /anomalies`, query by `sensor_id`, order by `timestamp` ASC, and emit floats (not ints) for `value`/`rolling_avg`. Compile and launch it in the background so it keeps listening.
