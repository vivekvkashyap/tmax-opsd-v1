In `/home/user/pdb_feature_extractor/src/main.rs`, change `cov.cholesky().unwrap()` to a fallible pattern: call `cov.clone().cholesky()`, and if it returns `None`, add exactly `1e-5` to each of the three diagonal elements (`cov[(0,0)]`, `cov[(1,1)]`, `cov[(2,2)]`) and retry the Cholesky decomposition on the jittered matrix before extracting `L` — keep the original (non-jittered) matrix used elsewhere if needed, and only jitter when the first attempt fails, so well-conditioned inputs are unaffected. Write `test_planar_jitter` as a `#[test]` function inside the same file (or a `mod tests` block) that constructs a rank-deficient 3x3 matrix by hand, runs your jitter-and-retry logic, and asserts the resulting Cholesky succeeds (e.g. `.is_some()` or that `.cholesky()` no longer panics). For the trace computation, sum the three diagonal entries of `L` after the (possibly jittered) decomposition and write that single float to `/home/user/feature_trace.txt`. Validate incrementally: `cargo test` for the regression test, then `cargo run` against `/home/user/input.pdb` to confirm no panic and a sensible trace value, computing the covariance matrix from the parsed atom coordinates exactly as the existing code does (mean-centered outer products divided by N).