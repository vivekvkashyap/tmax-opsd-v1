For `/home/user/chain.cypher`, write a Cypher `MATCH` path pattern starting at the node with `id = 'bkp_105'`, traversing `-[:DEPENDS_ON*]->` (variable-length, outgoing direction) to a node with `type = 'FULL'`, and `RETURN` the `id` property of all nodes in the matched path — this is a static query file, it need not execute. For the C pipeline, write `/home/user/pipeline.c` that reads backup IDs line-by-line from stdin into a list, then builds a parameterized SQL query against `/home/user/backups.db` using `sqlite3_prepare_v2`/`sqlite3_bind_text` for each ID (e.g. `WHERE id IN (?, ?, ...)`), computing a window function `SUM(size_mb) OVER (ORDER BY timestamp ASC)` so you get a running total per row — then take the *last* row's cumulative value (the one after ORDER BY timestamp, not just SUM(size_mb) which would also work but the running-total approach is what's required) as the final restore cost. Write that single integer plus a newline to `/home/user/restore_cost.txt`. Compile with `gcc /home/user/pipeline.c -o /home/user/pipeline -lsqlite3` and test it manually by piping the known chain IDs into it before finishing.