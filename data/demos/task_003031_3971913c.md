Work through the four requirements in order since later ones depend on earlier ones. First mount the squashfs backup without root using `squashfuse /app/backup.sqsh /app/data` (create `/app/data` if needed), and append the documented line `/app/backup.sqsh /app/data squashfs defaults 0 0` to `/app/restore-env/fstab`. Bring the compose stack up, then diagnose the 502 by reading `/app/restore-env/nginx/nginx.conf`'s `upstream`/`proxy_pass` directive against what socket/port `main.go` actually listens on (e.g. nginx expects a Unix socket but the Go app binds a TCP port, or vice versa, or the port numbers just don't match) — align them, then restart the affected containers and confirm `curl http://localhost:8080` succeeds. For the latency requirement, find the regex being compiled inside the request-handling loop in `main.go` (`regexp.MustCompile` called per-line/per-request instead of once at startup) — hoist compilation to a package-level `var` or `init()` so it runs once, rebuild the Go binary, restart `go-app`, and time `curl http://localhost:8080/stats` to confirm it's consistently under 250ms. Finally, open a background SSH tunnel through the bastion (`ssh -f -N -L 9090:go-app:9090 admin@localhost -p 2222`, password `adminpass`, likely needs `sshpass` or an expect script since it's non-interactive) and verify with `curl http://localhost:9090/...`. Only after confirming all four checks pass, write `SUCCESS` to `/app/done.txt`.
