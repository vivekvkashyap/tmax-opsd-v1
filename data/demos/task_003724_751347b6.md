Parse `/home/user/transactions.jsonl` line by line with `serde_json`, deserializing each into a struct matching the `{tx_id, sender, receiver, amount}` schema (only `receiver` is needed for the metric). Accumulate in-degree counts per receiver in a `HashMap<String, u64>`. To produce the top 3 with the specified tie-break (descending in-degree, then ascending alphabetical node_id), collect the map into a `Vec`, then sort with a custom comparator that compares in-degree descending first and falls back to node_id ascending — do not rely on HashMap iteration order or a single-key sort, since ties must be broken deterministically. Take the first 3 entries and write them as `node_id,in_degree` lines with no header and no trailing extra whitespace to `/home/user/top_nodes.csv`. Build/run via `cargo run` (or `cargo build --release && ./target/release/graph_etl`) from `/home/user/graph_etl`. Pitfall: sorting only by count and letting ties fall to arbitrary HashMap order will nondeterministically pick the wrong top-3 or wrong order among tied nodes.
