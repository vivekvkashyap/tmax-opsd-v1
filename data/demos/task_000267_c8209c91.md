Implement this as a strict phase-by-phase pipeline, matching each formula exactly since the final number must match a precise reference computation. Start the service (`python3 /home/user/sensor_service.py &`) and confirm it responds before scripting against it. Phase A: query x = 0.0..1.0 step 0.1 (11 points), stack the 5-vector responses into an 11x5 matrix M, run `numpy.linalg.svd`, zero out all singular values past the top 2, reconstruct M_denoised = U @ diag(S_truncated) @ Vt, and compute S(x) = sum of squares of each row. Phase B: repeatedly scan adjacent x pairs for |S diff| > 0.5, and for each violation query the midpoint from the API, project its raw 5-vector onto the SAME top-2 right singular vectors (Vt[:2]) computed in Phase A (don't recompute SVD), get its S value, insert it into the mesh in sorted order, and loop until no adjacent pair violates the threshold — this converges around the sharp peak the coarse mesh misses. Phase C: trapezoidal-integrate S over the final non-uniform mesh for area A, form P=S/A, build the cumulative trapezoidal CDF C(x), then sum the exact discrete formula given in the spec comparing C(x) to x. Round to 6 decimals and write only that number to `/home/user/result.txt`.
