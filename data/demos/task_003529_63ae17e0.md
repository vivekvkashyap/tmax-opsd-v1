Treat this as three sequenced subsystems that all feed one integration test, so verify each in isolation before wiring the server. (1) Extract `docs.tar.gz` to `/app/docs/`, then for every `.txt` file do a global string replace of `v1.0` -> `v2.0` and prepend the literal `[INTERNAL DRAFT]\n` as the very first line (careful: prepend, don't append, and use a real newline, not the two-character sequence `\n` inside the file). (2) Run `tesseract /app/diagram.png stdout` (or via file output) to recover the diagram's text, write it to `/app/docs/diagram.txt`, and prepend the same `[INTERNAL DRAFT]` marker — check the OCR output for stray whitespace/newlines that could break an exact-substring check downstream. (3) Write the C++ server to parse `/app/route_config.txt` line-by-line splitting on the first `=` into route and file path, store them in a map, and on each GET request look up the exact path, read the file fresh (don't cache stale content) and return 200 with `Content-Type: text/plain`, or 404 for anything unmapped. Bind strictly to `127.0.0.1:8080`, compile, launch in the background, and confirm with `curl -s http://127.0.0.1:8080/intro` that it responds correctly before finishing, since the verifier will poll all configured routes plus an unknown one.