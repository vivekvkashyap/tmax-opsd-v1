Treat the stripped binary as a black box: don't try to disassemble it, just probe it with small, hand-crafted CSV inputs where you control category counts and target values, then algebraically solve for the smoothing weight m in E = (n*x̄_c + m*x̄_g)/(n+m). A clean way: make a tiny CSV with one category having a known count n and mean x̄_c that differs from the global mean x̄_g, run the oracle, read the encoded output, and solve the single linear equation for m (try n=1 with an extreme target value for maximum sensitivity, or run several trials with different n to confirm m is consistent and looks like a round integer). Once m is fixed, write fast_etl.cpp to compute train statistics (global mean, per-category count+mean) using a hash map in a single pass over train.csv, then encode both train and test rows using only those train-derived statistics — never recompute stats from test.csv, and fall back to the train global mean for categories unseen in training. Match the CSV parsing style (comma-split, header line skipped) and output exactly `id,encoded_target` with full double precision so the max-abs-error threshold of 1e-4 is met. Compile and test against your own synthetic train/test split before trusting it against the hidden dataset.