Treat token extraction as an OCR pipeline you must tune, not a one-shot command. Extract frames from `/app/traffic_capture.mp4` at a reasonably dense sample rate with `ffmpeg` (e.g. a few fps, since cookie text scrolls), run `tesseract` on each frame, and grep the combined OCR text for the exact pattern `auth_session=[A-Za-z0-9]{16}` (anchored to 16 chars, not more/fewer) to avoid pulling in partial or merged matches from adjacent frames; dedupe the collected values before writing one per line to `/home/user/leaked_tokens.txt`. Since scoring is F1 between your extracted set and the true set, prioritize precision as well as recall — don't over-extract noisy near-matches, and don't under-extract by missing frames (try multiple frame rates/preprocessing if the first pass yields too few tokens). For phase 2/3, write `/home/user/audit_and_rotate.sh` to loop over ports 8080-8090, check if each is open (e.g. via `curl`/`/dev/tcp`), then for open ports send an HTTPS GET to `/admin` with `--cookie "auth_session=<token>"` and `-k` (ignore cert errors) for each leaked token, checking for HTTP 200. For each vulnerable port, generate a new 2048-bit RSA key/self-signed cert valid 14 days with CN `service.internal` via `openssl req -x509 -newkey rsa:2048 -days 14 -subj "/CN=service.internal" ...`, writing to the exact `/home/user/certs/port_<PORT>_{key,cert}.pem` paths, creating the directory first if needed.