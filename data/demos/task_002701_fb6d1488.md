Build the parser by decoding the binary format field-by-field exactly as specified (2-byte LE entry count, then per entry: 1-byte name length, name bytes, 4-byte LE compressed length, payload bytes) using Python's `struct` module, and be defensive: wrap parsing in try/except so malformed or truncated input never crashes the script. Before writing any decoding logic, determine the secret XOR key by listening to or transcribing `/app/voice_memo.wav` (use available audio/speech tooling, e.g. `ffmpeg`+ a speech-to-text approach, or careful manual listening) — do not hardcode a guessed number. Implement decompression as: XOR every payload byte with the recovered key, decode the result as UTF-16LE, then re-encode to UTF-8 for output. Enforce the zip-slip check on the filename (`../` substring or leading `/`) and silently skip such entries entirely, emitting nothing for them. Implement the locking requirement precisely: open the lock file, attempt `fcntl.flock(f, fcntl.LOCK_EX | fcntl.LOCK_NB)`, and on any exception print exactly `LOCKED` and exit 0 without processing; otherwise keep the file open while processing and streaming decoded content to stdout with no extra separators. Since this is checked by fuzzing against a hidden oracle, prioritize exact byte-for-byte behavioral matching over convenient shortcuts, including on edge cases like zero entries, truncated headers, and non-UTF16 payloads.
