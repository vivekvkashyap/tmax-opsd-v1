Write a single self-contained bash script that avoids Python/R entirely. Use `awk -F,` to do a two-pass (or single-pass accumulating) computation: first pass sums each of the 3 columns and counts rows to compute the three means, then a second `awk` pass (or the same invocation storing rows in memory) subtracts each column's mean from every value and prints with `printf "%.2f,%.2f,%.2f\n"` to guarantee exactly 2 decimal places and comma separation — plain arithmetic in awk can produce scientific notation or trailing floats if you don't explicitly format with printf. Compute the means as awk variables, then build the `Col1_mean=X.XX, Col2_mean=Y.YY, Col3_mean=Z.ZZ` log line (also via `printf`) and append it to the log file with `>>`. Watch two common failure points: (a) integer vs floating-point division in awk works fine since awk uses floating point natively, but forgetting `printf "%.2f"` and using plain `print` will give wrong formatting; (b) make sure negative zero (`-0.00`) doesn't appear — check your rounding. After writing the script, `chmod +x /home/user/preprocess.sh` and execute it, then inspect the two output files to confirm formatting matches exactly.
