Implement the pipeline exactly as specified rather than reaching for a library shortcut like scikit-learn's default vectorizer, since the exact tokenization (lowercase, split only on whitespace, keep punctuation attached) and per-row union vocabulary determine the score precisely. For each row: tokenize both strings, build frequency-count dictionaries restricted to the union of tokens appearing in that pair, compute dot product and L2 norms over that shared vocabulary, and handle the zero-vector edge case by returning similarity 0.0 instead of dividing by zero. Average the per-row similarities per file, then compare across all CSV files in the artifacts directory to find the maximum. Round the winning score to exactly 4 decimal places using standard rounding, and write the single output line in the precise `filename: score` format requested, with no extra text or trailing content.
