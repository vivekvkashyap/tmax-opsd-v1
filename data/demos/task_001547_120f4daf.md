Solve this as two independent passes over `/home/user/server.log`: one for redaction, one for detection. For redaction, use a regex substitution (e.g. `sed -E` or Python `re.sub`) that matches `password=` followed by everything up to the next `&` or end-of-quote, and replaces just the value with `[REDACTED]`, leaving every other character in the line — including the `password=` key itself and the surrounding `redirect_url` — untouched; apply this line-by-line to produce `/home/user/compliance_clean.log`. For the open-redirect detection, extract the `redirect_url` query parameter value from each line and check two conditions together: it must start with `http://` or `https://`, AND its host must not be `internal.company.local` — relative paths and same-domain absolute URLs are safe and must be excluded, so don't just flag every line with an external-looking scheme without checking the host, and don't flag relative paths. Collect the distinct IPs meeting both conditions, sort them as strings in ascending order, and write `/home/user/firewall_block.sh` with a `#!/bin/bash` shebang followed by one `iptables -A INPUT -s <ip> -j DROP` line per malicious IP in that sorted order, then `chmod +x` the script.