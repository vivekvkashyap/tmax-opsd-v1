Build four small, independently testable bash scripts, then a thin orchestrator — verify each in isolation first. For analyze.sh: loop over `/home/user/logs/deploy_*.log`, grep each for the exact string `Connection closed by authenticating user deploy`; for matches, extract hostname/IP from the `Connecting to host <name> [<ip>] port 22.` line via regex, output `hostname,ip` sorted with `sort -t, -k1,1` into failed_servers.txt. For supervisor.sh: loop invoking deploy_agent.sh, capture `$?`, append WARN and retry on nonzero, but stop after the 3rd failed attempt with ERROR and exit 1; on success append INFO and exit 0 — get the boundary right (3rd failure, not 3 retries after a failure). For tunnel.sh: launch `socat TCP-LISTEN:8080,fork,reuseaddr TCP:127.0.0.1:9090 &` then `echo $! > tunnel.pid`. For pipeline.sh: truncate supervisor.log, run the three scripts in order, always read tunnel.pid and kill that PID after supervisor.sh finishes regardless of its exit code, then write PIPELINE SUCCESS/FAILURE based on that exit status. chmod +x every script.