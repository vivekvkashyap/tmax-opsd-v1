Solve the secret-recovery step first, in isolation, before writing any server code. Listen to `/app/instructions.wav` (transcribe it, e.g. with a speech-to-text tool or by playing/inspecting it) to learn what cracking technique and target file the senior analyst describes — follow those spoken instructions exactly (e.g. if told to crack a hash of a short numeric PIN, brute-force candidate values and compare their hash against the referenced file in `/app/`) to recover the JWT secret, then persist it to `/home/user/jwt_secret.txt` and have your server read it from there rather than hardcoding it. For the server, generate a self-signed cert/key pair with `openssl req -x509 -newkey rsa:2048 -nodes ...`, then implement the `/audit/log` endpoint so it: decodes the JWT header first, accepts unconditionally if `alg` is `none`, otherwise verifies the HS256 signature against the recovered secret and returns 401 on mismatch. For redaction, replace all but the last 4 digits of the 16-digit `cc_number` with `****-****-****-` followed by the last 4, and append `[username] event - redacted_cc` to `/home/user/audit.log` on every success. Run the server backgrounded and confirm it's actually listening on 8443 before finishing.
