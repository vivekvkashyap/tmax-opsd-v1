Implement the pipeline as discrete, independently-testable stages in the exact order specified, since later stages depend on earlier ones' output. Load the CSV with a simple parser (skip header, split on commas), treating literal `NaN` strings specially. Compute sensor1's mean and *population* std dev (divide by N, not N-1) over ALL rows before removing anything, then filter out rows where `|z| > 3.0` strictly — do this filtering as a single pass producing a new in-memory dataset, and count removed rows for the output. Only after outlier removal, compute sensor2's mean over its non-NaN entries in the *cleaned* dataset and fill NaNs with that mean — order matters here (impute after outlier removal, using post-removal statistics). For Ridge Regression, build X with an intercept column of 1s, and implement the closed form `w = (X^T X + alpha*I)^{-1} X^T y` using Eigen's `MatrixXd`/`.inverse()` or a solver; construct 5 contiguous folds (rows 0..N/5-1, N/5..2N/5-1, etc. — not shuffled), train on the other 4 folds' rows concatenated together, predict/compute MSE on the held-out fold, and average across folds per alpha in {0.1, 1.0, 10.0}, picking the alpha with lowest mean CV MSE. Write all four values to `/home/user/pipeline_results.json` with exactly 4 decimal places using `printf`/`std::fixed << std::setprecision(4)`, matching the required key names and JSON structure exactly. Compile with Eigen included and run before finalizing.