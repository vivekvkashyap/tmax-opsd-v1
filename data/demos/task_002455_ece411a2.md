Do not trust the description of the bugs alone — treat `/app/legacy_detector` as ground truth and empirically diff its behavior against `/home/user/detector.py` on `/app/sample_audio.wav`. Steps: (1) Run both on the same fixture and compare outputs token by token to see where divergence starts — this tells you if it's a windowing/count mismatch (different number of output values) or a value-precision mismatch (same count, different numbers). (2) For windowing, recompute by hand how many windows a sliding window of size 256 with step 128 should produce for a known sample count, and check whether the script pads the tail or drops a partial trailing window versus what the binary does — verify against the binary's actual window count, don't assume. (3) For per-window energy, replace any floating-point accumulation with pure integer arithmetic that mimics fixed-point truncation: right-shift each sample by the same bit count before squaring/summing, since integer right-shift and squaring produces different truncation than float scaling and rounding. Iterate: after each fix, rerun both binaries on progressively larger/varied WAV fixtures you generate yourself and diff exactly, since fuzzing will use many random lengths.