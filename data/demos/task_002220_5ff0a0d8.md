Break this into three connections wired together in C++, and be deliberate about the corrupted-index workaround. For MongoDB, use libmongocxx to do an iterative BFS over `employees` starting from `manager_id == "E042"`, repeatedly querying subordinates-of-subordinates level by level (no single recursive query in vanilla MongoDB) until no new emp_ids appear — collect the full transitive set. For SQLite, the described index corruption means a plain indexed `WHERE emp_id = ?` lookup can silently miss rows, so force a full scan instead: use `NOT INDEXED` in the SQL, or just `SELECT *` everything and filter emp_id membership against your subordinate set in C++, rather than trusting SQLite's own filtering on that column. For Redis, use hiredis to `GET risk:<emp_id>` per matching emp_id, defaulting to "1.0" on a miss. Write the CSV with the exact header and no spaces after commas. Since grading is F1 against a ground-truth log_id set, prioritize not missing any true EXPORT logs for E042's subtree over minor performance concerns.