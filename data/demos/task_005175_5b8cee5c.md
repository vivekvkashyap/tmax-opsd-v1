Solve the two parts independently and validate each before combining. For Part 1, extract frames from `/app/calibration.mp4` (e.g. `ffmpeg -i calibration.mp4 frame_%04d.png`) and programmatically check each frame for being fully solid white — don't eyeball it; compute something like the mean/min/max pixel value per frame (e.g. with PIL/numpy) and count frames where all pixels equal 255, since a frame that's "mostly white" doesn't count. Use that count as W. For Part 2, write the stdin-to-stdout script to: extract all substrings matching `\[DATA\] (ISO8601) v=(-?\d+)` with a regex applied to the whole input text (matches can appear anywhere, including multiple per line, so search the full buffer rather than splitting by line first), sort the extracted (timestamp, value) pairs lexicographically by the ISO8601 string (which sorts chronologically for that format), then compute a trailing simple moving average of window size W where the first k<W points average only the points seen so far (not zero-padded). Round each average to exactly 1 decimal with `round(x, 1)` and emit a JSON array with keys `"t"` and `"v_smooth"`. Since grading is via fuzzed equivalence against a hidden oracle, test your script manually on a crafted input with overlapping/adjacent timestamps and verify the windowing boundary (start = max(0, i-W+1)) matches your own hand computation before considering it done.