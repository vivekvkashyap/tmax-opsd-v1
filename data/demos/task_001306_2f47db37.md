Start by OCR'ing `/app/graph_schema.png` (e.g. `tesseract /app/graph_schema.png -`) to recover the authoritative list of allowed `source.label -> relationship -> target.label` edge triples and the constraint that `target.id` must be a scalar string — do not assume or invent the schema, read it from the image. Then write `/home/user/filter_payload.py` (or `.js`/`.sh`) to: load the JSON payload argument, check that `(source.label, relationship, target.label)` exactly matches one of the OCR'd allowed triples, and reject (non-zero exit) if `target.id` is a list/array or the literal wildcard `*` rather than a plain string — this is the specific "cartesian explosion" pattern to guard against, analogous to a SQL implicit cross join. Exit 0 only when every field is well-typed and the edge triple is on the allow-list; exit non-zero for any mismatch, missing field, malformed JSON, or wildcard/array target id. Validate iteratively against `/app/corpus/clean/` and `/app/corpus/evil/` until you hit 100% accuracy on both — don't special-case the sample files themselves, since the real corpus used for grading is separate.
