Work the two phases independently and verify each before moving on. Phase 1: `mkdir -p /home/user/backup && cp /home/user/nginx_setup/nginx.conf /home/user/backup/nginx.conf.bak`, then use `sed -i 's/127.0.0.1:9999/127.0.0.1:8080/' /home/user/nginx_setup/nginx.conf` (do the backup copy BEFORE editing, not after) and grep the file afterward to confirm no `9999` remains and the `proxy_pass` line now reads port 8080. Phase 2: `cd /app/log-sanitizer`, inspect `Cargo.toml` and `src/main.rs` — the code likely won't even compile if it references a crate that isn't declared, and separately there is a deliberate `panic!()` triggered on some substring match unrelated to the actual filtering task; strip that panic entirely and instead implement the real logic: for each line from stdin, check with plain `.contains()` (no extra crate needed) whether it contains `"../"`, `"UNION SELECT"`, or `"<script>"`, and only `println!` the line if none match — drop it silently otherwise. Build with `cargo build --release`, then `mkdir -p /home/user/bin && cp target/release/log-sanitizer /home/user/bin/`. Self-test by piping `/app/corpora/clean/*` (all lines must survive) and `/app/corpora/evil/*` (all lines must be dropped) through the binary before finishing.