Build this as a standard scikit-learn model-selection pipeline, not a one-off script. Load train.csv and test.csv with pandas, then construct a `Pipeline`/`make_pipeline` of `PolynomialFeatures(degree=d)` -> `Ridge(alpha=a)`. Loop over the full degree x alpha grid, and for each combination run `cross_val_score` with `KFold(n_splits=5, shuffle=True, random_state=42)` and `scoring='neg_mean_squared_error'` — remember to negate and average the scores to get the mean MSE (a common mistake is forgetting the sign flip or averaging the wrong axis). Collect results into a DataFrame with columns degree, alpha, mean_mse, round mean_mse to 4 decimals, and sort ascending by mean_mse before writing to the CSV — order matters for verification. Pick the top row as the best hyperparameters, refit that exact pipeline on the entire training set (not just one fold), predict on test.csv, and write one prediction per line to the output file formatted with exactly 4 decimal places (e.g. via an f-string `f"{p:.4f}"`). Create the /home/user/experiments directory first if it doesn't exist, and install pandas/scikit-learn before running.