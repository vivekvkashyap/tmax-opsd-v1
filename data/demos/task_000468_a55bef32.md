Build this as a small event-driven pipeline, then wire a persistent server around it. Use `watchdog` (or a simple polling loop) on `/home/user/incoming_archives` to detect new `.zip` files, extract with `zipfile`, and locate audio members by extension/mimetype. Standardize with `ffmpeg -i <in> -ar 16000 -ac 1 <out.wav>` (16kHz mono is explicitly required and will be checked with ffprobe), then compute the MD5 of the *standardized* output file's bytes (not the original) to name it `field_record_<md5>.wav` in `/home/user/processed_audio`. Transcribe with `whisper` using the `tiny.en` model via the `openai-whisper` package, and keep an in-memory (or small persisted) dict mapping filename → transcript that a Flask/http.server thread serves at `GET /api/dataset` on `0.0.0.0:8000`. Package `/app/field_note.wav` into `/home/user/incoming_archives/test_batch.zip` to trigger the pipeline, and make sure the watcher/server process is left running in the background (not just executed once) since the verifier will poll the live API afterward.
