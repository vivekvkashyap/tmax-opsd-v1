Start by extracting the exact rules text with `tesseract /app/doc_rules.png stdout`, read it carefully, and re-verify by eye since OCR on rule specs can misread punctuation/casing that matters for bit-exact output — do not paraphrase the rules, implement precisely what the image states about line prefixes, dropped lines, and any trailing checksum/manifest line. Implement `/home/user/stream_processor.py` as a true streaming line-processor over `sys.stdin`/`sys.stdout` (or read the full input once if a whole-input checksum is required per the rules, since a checksum needs the complete original bytes) — process line by line applying the transform/drop conditions exactly as extracted, preserving original line terminators except where the rules explicitly say to alter them. Since the grading is bit-exact fuzz equivalence against a hidden reference implementation, test your own script against varied inputs (empty input, no trailing newline, lines matching each rule, mixed cases) and check the output byte-for-byte for edge cases like trailing newline handling before finishing — an off-by-one on whether a final newline or checksum line gets an extra `\n` is the most common way this class of task fails. Finish with `chmod +x /home/user/stream_processor.py`.
