Set TZ in `.bashrc` as a plain `export TZ=Etc/UTC` line (append, don't overwrite the file). For the video analysis, write the C program to read raw bytes from stdin in a loop, treating each consecutive 640*480-byte chunk as one grayscale frame; for each frame, only sum pixel values within the sub-rectangle rows 190-289 and columns 270-369 (don't average the whole frame — that's the common mistake), accumulating a running sum and pixel count across every frame until EOF (check the return value of `fread`/`read` for 0 or short reads to detect end of stream). After all frames, compute `(total_sum / total_pixels_summed) / 255.0 * 100` and print it with enough decimal precision to `average_load.txt`. Test the pipeline manually first: `ffmpeg -i /app/server_load.mp4 -f image2pipe -pix_fmt gray -vcodec rawvideo - | ./analyzer > /home/user/average_load.txt`, confirming the byte counts consumed are exact multiples of the frame size (no partial-frame truncation). Then write `monitor.sh` containing that exact same pipeline invocation (with `#!/bin/bash`, `chmod +x`), and write `monitor.cron` with a standard `30 2 * * * /home/user/monitor.sh` line pointing at that script.
