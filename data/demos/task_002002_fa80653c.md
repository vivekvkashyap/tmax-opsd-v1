Do not guess the linear weights from the task description in isolation — treat `/app/node_scorer` as a black-box oracle you must probe. Generate several small synthetic edge-list CSVs (varying graph shapes: a triangle, a star, a path, a graph with an isolated high-clustering node) matching the `author_A,author_B,collaborations` schema, run each through `/app/node_scorer dummy.csv`, and independently compute PageRank (alpha=0.85, weighted by `collaborations`) and clustering coefficient for the same graphs with `networkx`. Since the task tells you the score is a linear combination of these two metrics, set up a small linear system or least-squares fit (e.g. `numpy.linalg.lstsq`) using several probe graphs' (PageRank, clustering) pairs against the binary's observed outputs per node to solve for the two weights precisely — don't eyeball it from one example, since PageRank and clustering can be highly correlated on simple graphs and mask the true weights; use graphs specifically designed to decorrelate the two metrics (e.g. a node with high clustering but low PageRank, and vice versa). Once confirmed, apply the exact same formula in `fast_ranker.py` over the real dataset, sort descending, and emit only the top 50 in the specified JSON schema.
