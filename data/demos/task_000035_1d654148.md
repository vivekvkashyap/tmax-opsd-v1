First recover the authorization token by transcribing `/app/authorization_memo.wav` (install/use an ASR tool such as whisper or ffmpeg-assisted transcription) — listen for the token stated near the end of the recording and use that exact recovered string in your validator, don't hardcode a guess. Then write `/home/user/filter.sh` to parse the payload with `jq` and apply each rule as a distinct, testable check: (1) exact string match on `auth_token` against the transcribed token; (2) validate `branch` with a strict anchored regex like `^[A-Za-z0-9_-]+$` — reject anything containing `/`, `%`, or other characters, since URL-encoded traversal sequences like `%2e%2e%2f` won't match this whitelist; (3) iterate every value inside `build_env` (use `jq -r '.build_env[]'`) and reject if any value matches a grep for the injection metacharacters `$ ; | & < > `` (backtick)`. Exit 1 on the first rule violation and exit 0 only if all checks pass. Before finalizing, run your script against every file in `/app/corpus/clean/` (must all exit 0) and `/app/corpus/evil/` (must all exit nonzero) in a loop, printing failures, and fix any rule whose regex/matching is too loose or too strict until you hit 100% on both corpora.