Start with OCR: run `tesseract /app/lab_notes.png stdout` (install tesseract if missing) and parse out the `Alpha:` and `Beta:` numeric values from the recognized text — inspect the raw OCR output carefully since digits/decimals can be misread, and sanity-check by re-cropping/rerunning if the result looks implausible; never hardcode a guessed value. For the FASTA parsing, skip lines beginning with `>` and count sequence length and 'C' occurrences across all remaining (possibly multi-line) sequence lines for a given record. For the C server: implement a standard POSIX TCP listener (`socket`/`bind`/`listen`/`accept`) on 127.0.0.1:8333, read newline-terminated commands per connection, and keep the server looping to accept new connections after each client's `QUIT`. For SOLVE, implement Newton-Raphson (or bisection) on f(x) = x^3 + (alpha*N_c)*x - (beta*L), starting from a reasonable positive guess, iterating until convergence, then format with `%.4f` on the unique positive real root (the cubic here has only one positive real root since the polynomial is strictly increasing for x>0). Compile with `gcc -lm` and launch it backgrounded with `&` before finishing, using the OCR-extracted alpha/beta and the fasta path as argv.
