Approach this as: capture the transient secret, then use it. First launch the simulated admin process as instructed. In Python, poll `/proc/*/cmdline` in a tight loop (e.g. every 5-20ms) — for each numeric PID directory, read `/proc/<pid>/cmdline` (null-byte-separated), and check if it contains `data_backup_worker` and `--ssh-pass`; since the worker is short-lived, you must poll fast and continuously rather than doing a single one-shot scan, and handle the race where the PID directory disappears between listing and reading (wrap the read in a try/except for `FileNotFoundError`/`ProcessLookupError`). Once you capture the password argument following `--ssh-pass`, use it immediately: shell out to `openssl rsa -in /home/user/target_key -out /home/user/decrypted_key.pem -passin pass:<captured_password>` (or Python's `cryptography`/`paramiko` to load and re-serialize the key unencrypted) to produce a decrypted PEM. Set its permissions to `0600` with `os.chmod`. Compute `hashlib.sha256(open(path,'rb').read()).hexdigest()` and write just that hex string (no trailing newline, no extra text) to key_hash.txt. Verify the decrypted key parses as a valid unencrypted RSA key before finishing.
