Split this into the two independent parts and get each exactly right, since the extractor will be fuzz-tested for byte-identical output against a reference. For Part 1, use ffmpeg to seek to `00:00:05.000` in `/app/evidence.mp4` and dump a single frame as PNG (e.g. `-ss` before `-i` for fast seek, `-vframes 1`), then `sha256sum` that PNG file to get your SALT string — never hardcode a hash. For Part 2, parse the `.sbk` binary format precisely: verify the 4-byte `SBK1` magic, read the next 4 bytes as an unsigned little-endian uint32 for manifest length `L` (use `struct.unpack('<I', ...)`), parse `L` bytes of UTF-8 JSON, then treat everything after the manifest as the data section, using each entry's `offset`/`size` to slice out file bytes. For Zip Slip protection, resolve each `path` with something like `os.path.normpath(os.path.join(target_root, path))` and reject (silently skip) anything that isn't a descendant of the resolved target root, or is absolute, or contains `..` escapes — compare with `os.path.commonpath` or a prefix check on absolute paths, not a naive string check. Verify checksum as `SHA256(data + salt.encode())` before writing, silently skip mismatches, create parent dirs recursively, and wrap the whole write phase in an `fcntl.flock` on `<output_dir>/.extraction_lock`, releasing it right before exit. Only print on fatal errors (bad magic) and exit non-zero then; otherwise produce no stdout.