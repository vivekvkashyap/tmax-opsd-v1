Build `/home/user/run_pipeline.sh` as an ordered bash pipeline and make each step's success verifiable before moving to the next. Tokenization: read `/home/user/data/raw.txt`, lowercase it, strip punctuation but keep spaces (e.g. `tr '[:upper:]' '[:lower:]' | tr -d '[:punct:]'` or a `sed`/`tr` combo that preserves word boundaries), then split on whitespace onto one line per token (`tr -s ' ' '\n'`) into `/home/user/data/tokens.txt`, filtering blank lines. Environment: `python3 -m venv /home/user/venv`, `source` its `activate`, then `pip install numpy matplotlib`. Headless fix: the key gotcha is that setting `MPLBACKEND=Agg` as an environment variable *before* invoking python (`export MPLBACKEND=Agg` in the bash script, or prefix the python call with `MPLBACKEND=Agg python3 ...`) is what the provided `analyze.py` actually checks for — don't just add `matplotlib.use()` inside the script since you're not supposed to edit it, set the env var from the orchestration script instead. Create `/home/user/artifacts` with `mkdir -p` before running the script since it writes `plot.png` and `eigen_metrics.txt` there. End the script with `echo "Pipeline completed successfully"` and `chmod +x` the script itself.