Load `/home/user/spatial_data.csv` with pandas, then partition rows into the four quadrants using the exact boundary conditions given (note `>=` vs `<` — get the boundary direction right or points land in the wrong quadrant). For each quadrant, define an MSE objective function closing over that quadrant's `x, y, z` arrays: for Model A, `pred = theta[0]*x + theta[1]*y + theta[2]`; for Model B, `pred = theta[0]*x**2 + theta[1]*y**2 + theta[2]*x*y + theta[3]`; MSE is `np.mean((pred - z)**2)`. Call `scipy.optimize.minimize(objective, x0=[0.0]*n_params, method='Nelder-Mead')` for each model (initial guess must be all zeros, default tolerances — don't add custom `xatol`/`fatol` or you'll diverge from the expected optimum), take `.fun` as the minimized MSE, and pick whichever model has the strictly lower MSE. Round the winning MSE to 4 decimal places and write the required nested JSON structure with `best_model` and `mse` per quadrant to `/home/user/results.json`. Since Nelder-Mead is somewhat stochastic in path but deterministic given fixed x0 and data, running the script once should reproduce consistent results — double check by rerunning.
