Work through each of the four checks literally against what the verifier will grep/parse, since exact strings matter. Append a line like `export APP_SECURE_MODE=strict` to `/home/user/.bashrc` (don't overwrite the file). Write `/home/user/app_config.ini` as a standard INI parseable by Python's `configparser`, with `[security]` containing `tls_enabled = true` and `max_connections = 50`, and `[logging]` containing `path = /home/user/app_logs/service.log` — key names and section headers must match exactly. For `/home/user/logrotate.conf`, write a standard logrotate stanza targeting `/home/user/app_logs/service.log { daily rotate 7 compress create 0600 ... }` — the verifier greps for the literal substrings `daily`, `rotate 7`, `compress`, and `create 0600` (or `create 600`), so don't abbreviate or reorder those keywords, and make sure `/home/user/app_logs/` exists. For `/home/user/monitor.sh` (chmod +x it), implement clear branching logic: if the log file is missing OR contains the literal string "CRITICAL_ERROR", write exactly `STATUS: UNHEALTHY` to `/home/user/health.txt`; otherwise write exactly `STATUS: HEALTHY`. Test the script manually against all three scenarios (missing file, file with error, clean file) before finishing.