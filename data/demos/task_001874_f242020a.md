Start by loading `/home/user/backup_config.json` and parsing `target_magic`, which contains literal backslash-escaped hex sequences (e.g. `\x00`) inside a JSON string — decode this into actual bytes carefully (e.g. using `codecs.decode(s, 'unicode_escape').encode('latin-1')` or manual parsing), not just comparing the raw JSON string. Walk `/home/user/wal_backups` recursively with `os.walk`. For each file, read only the first 8 bytes of the *decompressed* stream: for `.gz` use `gzip.open(path, 'rb').read(8)`, for `.bz2` use `bz2.open(path, 'rb').read(8)`, and for plain files just open and read 8 bytes directly — do not fully decompress large files into memory. Compare those 8 bytes exactly against the decoded target magic. For matches, add the *original on-disk file* (still compressed if it was) to a tar.gz archive using `tarfile.open(output_archive, 'w:gz')` and `tar.add(filepath, arcname=os.path.basename(filepath))` so the directory structure is stripped. Track matched basenames, sort them alphabetically, and write them one per line to `/home/user/archive_log.txt`. Double check bz2/gzip files that are too short to have 8 bytes don't crash your script — handle short reads gracefully.
