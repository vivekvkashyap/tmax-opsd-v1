Split into transcription and scripting, and verify each independently before combining. For transcription, first normalize the audio (`ffmpeg -i /app/voicemail.wav -ar 16000 -ac 1 clean.wav`) since sample rate/channel mismatches degrade ASR accuracy, run a speech-to-text tool (e.g. `whisper`) on it, then post-process the raw output to strip any timestamps/segment markers the tool emits, keeping only the plain transcribed sentence(s), and write that to the transcripts path — accuracy is graded by character error rate against a hidden ground truth, so prioritize getting the wording right over polishing punctuation. For archive.sh: first create some dummy `.log` files with spaces and `[DEBUG]` lines in syslogs/ to test against; the rename step should iterate matched files and use something like `mv "$f" "${f// /_}"` (or `rename`) — do it via a loop, not a naive glob assumption about filenames; then for each renamed file, stream through `sed '/\[DEBUG\]/d'` piped directly into `gzip -c > archive_dir/<base>.gz`, never writing an intermediate uncompressed file to disk. Confirm resulting .gz files decompress cleanly and contain no `[DEBUG]` lines but retain other lines (e.g. `[ERROR]`).