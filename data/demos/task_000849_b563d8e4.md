In your C program, first parse categories.csv into an in-memory table of (id, parent_id, name) — a simple array indexed or hashed by id is fine given small scale — then for every category id precompute its ultimate root by walking parent_id pointers upward until parent_id is empty, memoizing results to avoid repeated walks (the naive mistake is only handling one level of nesting instead of following the chain to the true root, e.g. treating a grandchild's immediate parent as the root). Second, parse papers.jsonl line by line with sscanf/strstr to pull out the integer category_id and citations fields (safe here since each paper is single-line strict JSON) and accumulate citations into a running total keyed by root category name (use the precomputed id->root-name map). Finally, iterate your accumulator, and before printing, sort entries alphabetically by root category name (e.g. qsort with strcmp) rather than by id or insertion order, then write "Name: Total" lines to /home/user/report.txt. Compile with the exact command given and run it to actually generate the file.
