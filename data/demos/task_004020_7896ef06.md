Start by OCR'ing the config image (`tesseract /app/alert_config.png -`) and read off the Region/Timezone, Max Load threshold, and Process name directly from the recognized text — don't hardcode assumed values, and sanity-check the OCR output since fonts/noise can misread digits or punctuation. For the supervisor, use Python's `subprocess` to first run `init_env.sh` synchronously and check its return code before ever launching `generate_logs.py`; then launch `generate_logs.py` with its stdout piped, read it line-by-line, and wrap the whole launch in a loop that restarts the process only when it exits non-zero (exit 0 means done, stop looping — don't restart on success). For each line, split on `|`, filter by the exact process name from OCR, use a proper timezone library (`zoneinfo`/`pytz`) to convert the UTC timestamp to the extracted region, and only emit a CSV row when the load strictly exceeds the extracted threshold. Write rows exactly as `timestamp,process,load` with no header, and make sure the script exits once the log generator's exit-0 termination has been observed and fully drained.
