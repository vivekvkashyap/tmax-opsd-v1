Process `/home/user/app_logs.jsonl` line by line rather than loading it as one JSON blob, since some lines are individually malformed: wrap each `json.loads(line)` call in a try/except for `json.JSONDecodeError`, and on failure write that exact raw line (unmodified) to `/home/user/quarantine.jsonl`, then continue to the next line without stopping. For deduplication, key a seen-set by a canonical, order-independent serialization of only the `payload` field (e.g., `json.dumps(payload, sort_keys=True)`) — ignore `event_id`, `timestamp`, and `response_time` entirely when deciding duplicates, and keep only the first occurrence of each unique payload. When parsing `timestamp`, handle both an ISO8601 string and a raw UNIX epoch integer/number, normalizing both to UTC (e.g., check the type/format before choosing `datetime.fromisoformat` vs `datetime.utcfromtimestamp`). Bucket the surviving, deduplicated events by the UTC hour (truncate minutes/seconds to zero), and for each hour compute the event count and the mean of `response_time` rounded to exactly 2 decimals. Write the result as CSV to `/home/user/hourly_stats.csv` with the exact header and sorted ascending by hour.