Start by running `tesseract /app/config_baseline.png stdout` (or via a temp output file) to dump the raw OCR text, then inspect it manually before writing any parsing code — OCR output often has stray whitespace or line-break quirks, so parse it defensively (e.g. split on lines, then on the first `:`, trim each field) rather than assuming a rigid format. Store the four baseline values as package-level variables populated at program startup from the OCR text, never hardcoded literals. For the Go service: use `net/http` with a middleware/handler that checks the `Authorization` header first and returns 401 before touching the body; decode JSON into a struct, validate `cores`/`memory_gb` and return 400 with the exact `{"status":"invalid"}` payload on failure. Implement Levenshtein distance yourself (standard DP matrix) rather than assuming a library is available, since `go get` may be unreliable in this environment — a 20-line iterative implementation is safer and easier to verify. Compute resource and storage drift per the spec, sum them, then run the binary in the background and confirm with `curl` that all four described scenarios (missing auth, invalid payload, zero drift, computed drift) return the right status/body before declaring done.
