Write the C parser to read `/home/user/raw_data.csv` line by line (e.g. `fgets`/`getline`), split each line on commas, and validate strictly: parse column 1 with `strtol`/`sscanf` and check both that conversion fully consumed the field (no trailing garbage) and that the integer is > 0; compare column 2 exactly against "ACTIVE" or "INACTIVE" (reject anything else, including case variants); parse column 3 with `strtod`/`sscanf("%f")` and verify the whole field was consumed as a valid float. Route rows that pass all three checks to `clean_data.csv` formatted with `%d,%s,%.2f\n`; route the untouched original line (with its original newline, not reformatted) to `rejected.log` for any parse or validation failure — a naive approach that reformats rejected rows or drops the newline will fail exact-match verification. Handle EOF cleanly without a trailing empty write. In the Makefile use `gcc -O2 -Wall` for the `all` target and remove the binary plus generated CSV/log outputs in `clean`. For the reproducibility test script, run the full clean+build+execute cycle twice, hash `clean_data.csv` with `md5sum` after each run, compare, and print "REPRODUCIBLE"/exit accordingly; `chmod +x` the script.