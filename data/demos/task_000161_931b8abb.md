Handle the three parts independently and verify each with a direct check before moving on. For the SSH config audit, grep `/home/user/audit/sshd_config` for the exact directive lines (case as they appear) and check their values: `PermitRootLogin` counts as weak unless it's explicitly `no`/`prohibit-password`, and `PasswordAuthentication` is weak when `yes` — write one `FAILED: <Directive>` line per weak setting found, using the directive name exactly as it appears in the config. For the key audit, iterate files in `/home/user/audit/keys/` matching the `id_rsa*` prefix and check the "others" permission bit — `stat -c '%a'` or `ls -l` and check if the last octal digit/character group grants read (e.g. mode `644` has world-read, `600` does not) — write the full absolute paths of any matches. For the auth bypass, read `auth_service.py` closely rather than guessing: it decodes base64 into JSON and checks two specific field values, so build a minimal JSON object containing exactly those required key/value pairs (extra fields are fine if the script doesn't reject them — check whether it does), base64-encode it, and save the encoded string as-is (no trailing modifications) to the payload file. Test your payload by actually running `python3 auth_service.py /home/user/payload.txt` and confirming it prints success before considering the task done.