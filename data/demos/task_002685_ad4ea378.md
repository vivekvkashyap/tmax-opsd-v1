Build and tune the classifier against the labeled corpora before ever touching the real video — that's the fast, cheap feedback loop. Write a small, correct PGM parser first: handle the `P5` magic number, skip `#` comment lines, parse width/height/maxval from whitespace-separated header tokens (careful with mixed whitespace/newlines), then read the binary pixel data (1 byte per pixel if maxval<256). Compute simple statistics per frame — fraction of pixels at or near 0, and/or mean brightness — and pick threshold(s) that separate the two corpora; don't guess a single hardcoded number, instead loop over both `/app/corpus/clean/` and `/app/corpus/evil/`, run your detector against every file, and iterate your thresholds until you hit 100% on both sides (a script or manual sweep over threshold values is faster than guessing). Only once verified, extract frames with `ffmpeg -i /app/droplet_experiment.mp4 -vf fps=1 -pix_fmt gray %04d.pgm` (confirm the actual output format matches your parser's assumptions), run the verified detector over every extracted frame, tally exit-code-0 results, and write the integer count to `/home/user/clean_count.txt`.
