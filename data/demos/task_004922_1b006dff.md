Transcribe `/app/voicemail.wav` first (e.g. `whisper` or another STT tool) and listen closely for the specific keyword the security engineer names to block — that keyword goes into your filter logic, not a guessed word. For the polyglot piece, scaffold a Rust binary crate at `/home/user/waf_filter`, add a `build.rs` that uses the `cc` crate to compile `/app/libnormalize/normalize.c` into a static lib linked into the binary, and declare the FFI signature `extern "C" { fn normalize_text(input: *mut c_char); }` — pass an owned, null-terminated `CString`/mutable buffer since the C function mutates in place, then convert back to a Rust `&str` for matching. In `main.rs`, read the file argument, normalize it via FFI, then check (case-insensitively, since the C normalizer already lowercases) for the exact substring `or 1=1` and for the recovered secret keyword; exit 1 if either matches, else exit 0. Build in release mode (`cargo build --release`) so the binary lands at `target/release/waf_filter`, and validate against both `/app/corpus/clean/` and `/app/corpus/evil/` before considering it done, since the hidden grading corpus will test the same rules more exhaustively.
