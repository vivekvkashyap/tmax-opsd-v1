First inspect the SQL: an implicit cross join like `FROM locations l1, locations l2, connections c WHERE c.active=1` produces every location pair regardless of the actual edge, so replace it with explicit joins that tie the connection's `loc1_id`/`loc2_id` to the corresponding `locations.id` (`FROM connections c JOIN locations l1 ON c.loc1_id = l1.id JOIN locations l2 ON c.loc2_id = l2.id WHERE c.active = 1`), remembering edges are directed (source -> dest only, do not add a reverse edge). Once you have clean `(source, dest, distance)` rows, build an adjacency structure and run a shortest-path algorithm suited to weighted graphs (Dijkstra, since distances are positive) from 'Alpha' to 'Omega', reconstructing the path via predecessor tracking rather than just the total cost. Sum edge weights along the actual path taken, and write the JSON to `/home/user/shortest_path.json` matching the required `{"path": [...], "total_distance": N}` structure exactly, then execute the script and confirm the output file was written correctly.
