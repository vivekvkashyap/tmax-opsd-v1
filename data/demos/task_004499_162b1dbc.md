Implement the C++ filter exactly per the numerical spec, then validate against both corpora before wiring in the microservices. Read the sequence file, count G/C and A/T occurrences to get r and d (normalize by total length), then run a straightforward Forward Euler loop: y=0.1, dt=0.01, for t from 0 to 10 step dt update y += dt*(r*y - d*y*y + sin(t)), checking after every step whether y exceeds 10.0 — if so, stop early and exit 1 (evil); otherwise finish the loop and exit 0 (clean). Compile with g++, then run it directly against every file in `/app/corpora/clean/` and `/app/corpora/evil/` to confirm the exit codes match before trusting the logic — this ODE is sensitive to loop bounds/off-by-one in t, so double check the simulation actually reaches t=10.0. For integration, edit `/app/flask_app.py` to write the incoming POST body to a temp file, call `subprocess.run(['/home/user/filter', tmp_path])`, check `.returncode`, and only forward to the PDE backend on 0 (return its response/status), else return 400 with the required rejection text directly — no need to touch nginx or the backend stub. Restart everything via `/app/start_services.sh` and confirm end-to-end with curl for both a clean and an evil sequence.