Work stage by stage and validate each before moving on. Extract the tarball first and inspect the resulting binaries' ELF headers yourself (read bytes 18-19, the `e_machine` field, either via `readelf -h` or by parsing the raw header in Rust) rather than assuming naming conventions — map the machine value to `x86_64` or `aarch64`. Parse the build log with a simple state machine over the `--- BUILD RECORD ---` blocks, keeping only entries where `Status: SUCCESS`, and correlate `Artifact` name with the extracted binary of the same name. For each valid binary, write to a `.tmp` file in the destination directory and use `std::fs::rename` (same filesystem) to get atomicity — never write directly to the final `.elf` path. Build `all_binaries/` links with `std::fs::hard_link` (same filesystem as the arch dir) so inode identity is verifiable, and derive `-latest.elf` symlinks by comparing versions with proper semver ordering (numeric segment comparison, not lexicographic string comparison) via `std::os::unix::fs::symlink`. Finally, `tar czf` the `repo` directory from its parent so symlinks/hardlinks are preserved by default (avoid `-h`/dereference flags, which would break them).
