Write and unit-check `evaluate.py` in isolation on one CSV before wrapping it in the orchestration script. Load the CSV, fill NaNs in the three feature columns with each column's own median (compute medians after loading, per-column), then compute the 1st and 99th percentiles of `target` and drop rows strictly at or beyond those bounds (`<=` low percentile or `>=` high percentile, matching the spec exactly). For the model, use `sklearn.model_selection.KFold(n_splits=3, shuffle=False)` (or just `cv=3` which defaults to no shuffling) inside `GridSearchCV(Ridge(), {'alpha':[0.1,1.0,10.0]}, scoring='neg_mean_absolute_error', cv=3)`, fit, then take `-grid.best_score_` and round it to 3 decimals to get a positive MAE, and print `f"{alpha},{mae:.3f}"` with no extra whitespace since the log format is exact. Test this script manually on one dataset file first and read the printed line to sanity-check formatting. Then write `run_experiments.sh` to first overwrite `/home/user/experiment_log.csv` with just the header, then loop over `/home/user/datasets/*.csv` sorted alphabetically (globs are already sorted, but confirm), calling `evaluate.py "$f"` and appending `$(basename "$f"),$(python3 evaluate.py "$f")` as a CSV row. Run the bash script yourself and inspect the resulting log file.