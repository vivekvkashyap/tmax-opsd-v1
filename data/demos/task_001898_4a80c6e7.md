Use pandas for the analytics: load the CSV, then compute the rolling average correctly by first sorting by `src_node` then `timestamp` (grouping-then-rolling requires the frame to already be ordered chronologically within each group), using `df.groupby('src_node')['amount'].transform(lambda x: x.rolling(window=3, min_periods=1).mean())` so the rolling average includes the current row and up to the previous two, with fewer available at the start of a group rather than NaN. Filter to rows where `amount > rolling_avg` strictly. The key detail the naive approach misses: the *output ordering* of `commands.txt` must itself be sorted by `(src_node, timestamp)` — not by original CSV row order — since the downstream binary drops out-of-order edges per `src_node`; sort the filtered frame by those two columns before writing lines in the exact `ADD EDGE <src> <dst> <amount>` format. After generating, do a local sanity test by piping your file into `/app/graph_builder` yourself and reading its `committed_edges` count to gauge whether ordering actually mattered, before finalizing the script as a single autonomous `pipeline.py` that can be re-run end-to-end.
