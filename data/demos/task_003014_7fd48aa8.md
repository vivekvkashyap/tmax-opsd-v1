Build this as a single Python script with clearly separated stages, matching the exact numerical settings specified rather than approximating them. Load the CSV, then filter: `scipy.signal.butter(N=2, Wn=0.2)` returns (b, a) coefficients, apply them with `scipy.signal.filtfilt(b, a, signal)` — do not use `lfilter`, which introduces phase lag and will throw off the SSE comparison. For the ODE step, define the two RHS functions exactly as given and call `scipy.integrate.solve_ivp` per model with `method='RK45', rtol=1e-6, atol=1e-9, t_eval=<csv time array>` so the numeric solution lines up sample-for-sample with your smoothed signal — mismatched t_eval is the most common cause of downstream SSE errors. Compute the closed-form logistic solution directly from the formula given, and take `max(abs(numerical_B - analytical_B))` for max_diff — this validates your solver rather than being solved a different way. For AIC use the exact formula `n*ln(SSE/n) + 2k` with k=1 (Model A) and k=2 (Model B), and pick the model with the lower AIC as best_model. Round every reported number to exactly 4 decimal places before dumping the JSON.