Start by recovering the exact classification rule from the audio — transcribe `/app/lab_notes.mp3` (e.g. with `whisper`) and read it carefully for: which atoms to extract, what matrix to build from them, what decomposition to run, how many components to keep, what curve to fit them to, and the exact numeric threshold and comparison direction that separates decoys from natural proteins. Do not assume standard defaults; use precisely what the transcript specifies. Then implement `/home/user/protein_classifier.py`: parse ATOM records for CA (alpha-carbon) atoms from the PDB file yourself (columns are fixed-width; a light manual parser is fine), build the pairwise distance matrix, run an SVD (numpy/scipy), take the top singular values, fit them with `scipy.optimize.curve_fit` to the exponential form named in the transcript, and compare the fitted decay parameter against the recovered threshold to choose exit code 0 or 1. Wrap parsing/fitting in try/except so malformed or too-small PDBs cleanly exit 2. Test manually against a hand-made valid PDB before trusting it, since the corpus check needs 100% accuracy on both classes.