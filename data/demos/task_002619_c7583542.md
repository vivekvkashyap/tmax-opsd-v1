Triage this as three separate, verifiable fixes before wiring anything together. For the deadlock, read `server.cpp` closely for two endpoints that acquire the same two mutexes in opposite orders (a classic lock-inversion bug) and fix it by establishing a single consistent global lock ordering everywhere (or replacing with a single coarser mutex / `std::scoped_lock` that locks both atomically) — verify by stress-testing with concurrent curl requests before moving on. For the corrupted WAL, don't manually eyeball 100+ lines; use delta debugging (bisect the file, e.g. binary-search by splitting the file in half, running the server's load path against each half, and narrowing to the single malformed line) or just try loading progressively larger prefixes until the crash reproduces, then remove exactly that line. For the token, run `tesseract /app/token.png` (or preprocess with `convert`/`sox`-style upscaling first if OCR is noisy) to recover the exact admin token text — inspect the raw OCR output carefully for stray whitespace or misread characters before hardcoding it into your auth check. Finally compile with the exact specified `g++` flags, start the server on `127.0.0.1:8080`, and confirm concurrent `/query` and `/update` requests both return 200 without hanging before leaving it running in the background.