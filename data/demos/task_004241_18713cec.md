Read `anomaly_filter.cpp` looking for exactly three bug classes: a memory leak (something allocated, e.g. via `new`, inside the per-frame loop that's never freed — replace with a stack-allocated fixed-size container or a `std::deque`/ring buffer reused across iterations), an off-by-one in the sliding-window boundary (confirm the window holds exactly the *last 10* frames, not 9 or 11 — check the index arithmetic where old elements drop out), and precision loss from computing variance via `sum_of_squares - mean*mean` (or similar) in `float` — switch all accumulation to `double` and prefer a numerically stable formula (e.g. compute the mean first, then sum squared deviations from it) to avoid catastrophic cancellation. Compile the fix to `/home/user/anomaly_filter`.

Since your binary must be bit-exact vs. `/app/oracle_filter`, don't just eyeball correctness: generate many random streams of floating-point numbers (varying lengths, including short streams under 10 elements) and diff your binary's output against the oracle's output line-for-line, iterating on your fix until every stream matches exactly. Only then extract per-frame mean brightness from `/app/camera_feed.mp4` — e.g. `ffmpeg -i /app/camera_feed.mp4 -vf signalstats -f null - 2>&1 | grep -oP 'YAVG:\K[0-9.]+'` — feed that sequence into your fixed binary, and write its output verbatim to `/home/user/brightness_anomalies.txt`.
