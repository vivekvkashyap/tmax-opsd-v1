First recover the formula: run `tesseract /app/business_rules.png stdout` (or a quick Python/PIL + OCR script) and read the extracted text carefully to identify the exact arithmetic formula for `customer_value_score` — don't assume a formula, derive it from the OCR output, correcting any OCR misreads by cross-checking operator symbols and coefficients against a second look at the image if the text looks garbled. Then build the aggregation pipeline: load both CSVs (pandas or csv module), group `orders` by `customer_id` to get `total_spent` (sum of `total_amount`), `order_count` (row count), and the most recent `order_date`; compute `days_since_last_order` as the integer day difference between that max date and the fixed reference date `2023-12-31` (parse dates properly, don't string-diff). Apply the OCR-recovered formula to these three aggregates to get `customer_value_score`, matching operator precedence exactly as printed (e.g., parenthesized terms). Emit one JSON object per customer as a line in `/home/user/customers_export.jsonl` with the exact field names/types specified. Before finalizing, sanity-check your formula application on one customer by hand to confirm your OCR read the coefficients correctly, since the MSE threshold of 0.1 leaves little room for a misread coefficient.
