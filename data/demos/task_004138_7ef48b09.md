Treat this as a three-layer pipeline (Flask -> Redis -> Go worker) and validate each hop independently before wiring them together. First, discover the classification rule empirically: load `/app/labeled_sensor_data.csv`, and since the task says a *linear combination* of `v1,v2,v3` separates label 0 from label 1, fit/solve for coefficients and a threshold (e.g. via simple linear regression, a small brute-force search over small integer/decimal coefficients, or by inspecting the separating margin directly) rather than guessing — verify your rule gets 100% accuracy on the labeled set before trusting it. Second, start Redis, then inspect `/app/api.py` for the injected bug — check both the Redis port it connects to and the exact list name it pushes to (`processing_queue`) against what the worker expects; fix and restart Flask. Third, write the Go worker: use a blocking pop (`BLPOP`) on the same queue/port, parse the CSV with the `encoding/csv` package, apply your threshold rule per row, write only header + surviving rows to `/home/user/output/<basename>`, compile it, and run it as a background daemon (nohup/setsid), confirming it stays alive after your shell exits. Test end-to-end with a manual POST before finishing.