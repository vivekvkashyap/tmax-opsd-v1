Fix and build this multi-service pipeline in stages, testing each stage independently. (1) Nginx: edit `/app/nginx.conf`'s `proxy_pass` directive for the `/upload` location block to point at `http://127.0.0.1:5000` (matching the Flask app's actual bind address/port), then restart nginx with the config and confirm with `curl localhost:8080/upload` that it reaches Flask rather than erroring. (2) Validation script: write `/home/user/validate.sh` to load the headerless 3-column CSV into an in-memory SQLite table (`sqlite3 :memory:` with `.mode csv` and `.import`), add an index on `manager_id` for performance, then run a recursive CTE that walks the manager chain from each employee and detects if any employee's own id reappears in its own ancestor path (a cycle) — track the traversal path per row (e.g. as a comma-joined string) and check via `instr()` whether the current node's manager_id is already in that path. Exit 1 if any cycle is found, exit 0 otherwise. Test the script standalone against small hand-built clean/cyclic CSVs before wiring it to Flask, since correctness of the SQL recursion is the crux of the task — an off-by-one in cycle detection (e.g. only checking direct self-reference vs. any ancestor) will fail the hidden corpus.