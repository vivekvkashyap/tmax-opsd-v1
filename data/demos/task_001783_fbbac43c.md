This is a straightforward merge-and-serve task; get the merge logic right before worrying about the server. Glob all `*.json` files in `/home/user/manifests`, load each `deps` dict, and merge them into one dict where, for any package appearing in multiple files, you keep whichever version string is greater under plain lexicographic (string) comparison — not semantic-version comparison — exactly as instructed, then sort the merged dict's keys alphabetically before serializing (Python's `dict` preserves insertion order, so build it by inserting in sorted-key order, or rely on `json.dumps(..., sort_keys=True)`). Write `/home/user/api.py` using `http.server`'s `BaseHTTPRequestHandler` (or Flask if available) to serve `GET /manifest` returning `{"dependencies": {...}}` with correct `Content-Type: application/json`. Launch it in the background bound to port 8123 on `0.0.0.0` or `localhost`, then verify with `curl http://localhost:8123/manifest` that the JSON body exactly matches your computed merge before finishing — recheck the lexicographic (not numeric) version comparison, since that's the detail most likely to be implemented wrong.
