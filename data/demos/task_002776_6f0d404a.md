Implement the numerical pipeline in Rust exactly as specified, stage by stage, and validate against a hand-checkable example before trusting the full run. Set up a `Cargo.toml` pulling in a linear algebra crate (e.g. `nalgebra`) so you get a tested Cholesky decomposition rather than writing your own (a hand-rolled Cholesky is a common source of subtle bugs, especially for near-singular inputs). Parse each CSV row's 25 floats into a 5x5 matrix in row-major order, then build the gradient matrix with row 0 all zeros and each subsequent row being the difference from the row above — index carefully (row-major access can easily be transposed by accident). Compute $C = G^T G$, then add $\alpha I$ with $\alpha = 0.01$ before factorizing — this regularization is exactly what keeps near-singular/constant-signal rows from crashing the decomposition, so don't skip it or apply it after the fact. Take the Cholesky factor $L$ and sum its diagonal for the trace, formatting to exactly 4 decimal places on output. Process rows in file order and write one trace per line to `/home/user/features.txt`; spot-check your first few outputs by computing the same steps in Python/numpy for one row to confirm your Rust arithmetic and indexing match before running the full dataset.
