Tackle the three phases independently and verify each in isolation before integrating. Phase 1: run `make` in `/app/jq-1.6`, read the linker error for undefined symbols like `pow`/`exp`/`atan2` — these come from libm, so check the Makefile for the `LIBS`/`LDFLAGS` variable and ensure `-lm` is present (a prior "optimization" likely stripped it); re-run `make` until `jq` links cleanly, then sanity-test it on a sample JSON file. Phase 2: don't guess the formula — dynamically observe it. Use `ltrace /usr/local/bin/token_validator <probe_id> <token>` to see arithmetic/library calls, or brute-force by running the binary across a probe_id with token values 0..99999 in a loop to find which token makes it exit 0, then repeat for a couple more probe_ids and look for a linear/modular relationship between probe_id and the valid token (e.g. token = f(probe_id) mod N). Confirm the formula holds for at least 3 different probe_ids before hardcoding it in Python. Phase 3: implement all four validation conditions directly (numeric field presence, total_sec > 0, 0 <= uptime_sec <= total_sec, token formula match) with exit(0)/exit(1) and no external calls to the validator binary; test against `/app/corpus/clean` and `/app/corpus/evil` before considering it done, and handle JSON parse errors as REJECT.