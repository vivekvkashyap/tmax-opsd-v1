Do the three phases in order, verifying each before moving on. First, fix `/app/feature_server_pkg-1.2.0`: run `make` or `pip install .` there, read the actual error, and check both the `Makefile` (it may invoke a specific Python version binary that doesn't exist on this system — point it at the available `python3`) and `setup.py` (look for a syntax error in the `install_requires` list, such as a stray/missing quote or comma) — fix both, then confirm `import feature_server_pkg` works, or fall back to plain Flask/FastAPI for phase 3 if the package is just a thin wrapper. Second, write `/home/user/etl_pipeline.py`: load `/app/data/raw_features.csv`, standardize with `sklearn.preprocessing.StandardScaler`, fit `sklearn.decomposition.PCA(n_components=5)`, save the fitted scaler and PCA model (both needed for consistent inference later) via `joblib`/`pickle` to `/home/user/pca_model.pkl`, and write the transformed columns named `pc_1`..`pc_5` to `/home/user/processed_features.csv`. Third, write `/home/user/serve_features.py` serving on `127.0.0.1:8080`: `GET /health` returns `{"status":"ok"}`, `POST /transform` loads the saved scaler+PCA, applies the identical transform to the posted 50-float array, and returns the 5 reduced values; enforce the `Authorization: Bearer secret_ml_token_99X` header on every route, returning 401 otherwise. Start the server backgrounded with output redirected to `/home/user/server.log`, then hit `/health` and `/transform` yourself with `curl` to confirm both correct and unauthorized cases behave properly before finishing.
