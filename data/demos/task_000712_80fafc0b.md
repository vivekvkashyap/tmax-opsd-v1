Parse the FASTA file yourself (Biopython's `SeqIO`, or simple manual parsing on `>` headers) to get each sequence ID and sequence string; compute GC fraction as `(count('G')+count('C'))/len(sequence)` — this is your per-strain growth rate `r`. For the logistic ODE, either solve analytically (`P(t) = K / (1 + ((K-P0)/P0) * exp(-r*t))` with K=1000, P0=10) or integrate numerically with `scipy.integrate.solve_ivp` over t in [0,10] — both should agree closely, use one to sanity-check the other. Plot all trajectories on one matplotlib figure with a legend labeling each strain by its sequence ID, save to the exact PNG path. Round final t=10 populations to 1 decimal and dump as `{sequence_id: value}` JSON. Write all of this as cells in `/home/user/workflow.ipynb`, then actually execute it headlessly with `jupyter nbconvert --to notebook --execute` (or `papermill`) so the output files are truly generated — don't just leave code unexecuted in the notebook.
