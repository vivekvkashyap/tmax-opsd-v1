Reverse-engineer the oracle empirically rather than guessing: write several small dependency-graph JSON files (a simple chain, a diamond, a graph with a tie between two independent roots, and a graph with an intentional cycle) and run `/app/sec_linker_oracle <file>` on each, recording exact stdout. From the tie-break cases, deduce the ordering rule among nodes that could be processed next — a classic topological sort has ties whenever multiple nodes currently have zero remaining dependencies, so vary which names you use and see whether alphabetically-first or -last is chosen to determine the direction. From the cycle case, capture the exact error string verbatim (character-for-character, since your API/CLI must reproduce it exactly). Implement the same logic in Python: build in-degree counts and adjacency from the "libraries" depends-on lists, repeatedly pick the eligible node per your discovered tie-break rule (a heap or sorted selection works), and format output to match the oracle's exact spacing/arrows. Build the Flask/FastAPI server around this function, then the CLI script that POSTs the file's JSON and prints only the `output` field. Validate by diffing your CLI's output against the oracle's on several more fresh test graphs, including edge cases (single node, empty graph) before trusting it against the hidden fuzzer.
