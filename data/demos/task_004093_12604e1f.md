Diagnose the 502 systematically before writing automation: check nginx.conf's upstream block against the socket path server.py actually binds to (/home/user/app/backend.sock) — a mismatched socket name is a classic cause of 502 from a Unix-socket upstream, so grep the conf for the wrong path and correct it to match exactly. Separately, manually run server.py once by hand to see the interactive `Enter startup key:` prompt and confirm the exact key it needs, so you understand what the expect script must automate. Write start_backend.exp using `spawn`, `expect "Enter startup key: "`, `send "DeployKey2023!\r"`, then use `interact` or background it appropriately (e.g. `expect eof &` pattern or `spawn -noecho`) so the backend process keeps running after the expect script itself would otherwise exit — a common failure is the process getting killed when the parent expect script exits, so make sure it detaches/persists. In deploy.py, shell out to start_backend.exp, then start nginx with the given `-c/-p` flags, then poll `/health` with retries and exponential/backoff sleep using `requests` or `urllib`, checking for status 200 and body "OK" before writing the exact success string to the log — treat persistent 502s after N retries as a failure path that exits cleanly rather than hanging.