Fix the build first, then implement HTTP by hand at the socket level. In `CMakeLists.txt`, add `set(CMAKE_CXX_STANDARD 17)` (with `CMAKE_CXX_STANDARD_REQUIRED ON`), `find_package(Threads REQUIRED)`, and `target_link_libraries(bf_server PRIVATE Threads::Threads)`. In `server.cpp`, parse the raw request line yourself (no HTTP library available): extract everything between `code=` and the next whitespace/`\r\n` in the request line, treating it as the raw (non-URL-decoded) BF program per the spec. Validate every character against the exact 8-symbol whitelist before calling `execute_bf` — any other character (including things like URL-encoding artifacts) must trigger the literal 400 response with the exact status line and headers given in the spec; a match means the exact 200 response format. Always `close()` the socket after writing the response. Write the GitHub Actions workflow with a `cmake`+`build` job targeting `bf_server` on `ubuntu-latest`, validate its YAML syntax. Build, run the server in the background, run `benchmark.py` redirecting stdout to the log path, then terminate the server process.
