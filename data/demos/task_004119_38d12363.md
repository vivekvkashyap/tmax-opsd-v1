Work this as a linear chain of discovery steps, verifying each before moving on. First locate the live service: scan TCP ports 8000-8050 on localhost (e.g. loop with `curl -s --max-time 1 http://127.0.0.1:$p/config` or check `ss -tlnp`) until you get a JSON response containing `admin_hash`. Then write a small Python script that reads `/home/user/legacy_passwords.txt` line by line, computes `hashlib.sha256(word.encode()).hexdigest()` for each, and compares against the extracted hash to recover the plaintext password — do not guess or hardcode a password, derive it from the dictionary match. Once cracked, invoke the provided `/home/user/rotate_ssh.py --password <cracked>` exactly as given; do not modify that script. After it runs, compute the SHA256 of the newly generated `/home/user/.ssh/id_ed25519` with `sha256sum`, reading the actual file rather than assuming a value, since key generation is randomized each run. Finally write `/home/user/rotation_summary.json` with the cracked password and that freshly-computed checksum, matching the exact key names specified.