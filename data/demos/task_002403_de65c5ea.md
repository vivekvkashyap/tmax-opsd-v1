Determinism is the whole point here, so match the RNG call pattern exactly, not just the math. Write `calc_slope` computing OLS via the standard formula `slope = sum((x-mean_x)*(y-mean_y)) / sum((x-mean_x)^2)`. Load the 8 rows from the CSV (skip header) into fixed-size arrays. Call `srand(42)` exactly once at the very start of `main`, before anything else consumes `rand()`. For each of the 10000 bootstrap iterations, build a resampled array by calling `rand() % 8` sequentially 8 times (in a tight inner loop, in that exact order — any extra/missing `rand()` call anywhere before or during this shifts every subsequent draw and desyncs from the expected sequence), recompute the slope on the resample, and store it. After all iterations, `qsort` the slope array ascending, then read the lower/upper CI bounds directly from indices 250 and 9749 (not percentile interpolation). Format output with `%.2f` matching the exact string template given, write to the results file, and compile with `-lm`. Double check float vs double consistency throughout to avoid subtle drift.