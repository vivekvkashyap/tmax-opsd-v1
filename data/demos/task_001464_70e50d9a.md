Use Go's `database/sql` with a sqlite3 driver (e.g. `mattn/go-sqlite3` or a pure-Go driver) to query `chunks` and `dependencies` with parameterized joins, loading the full DAG into memory as an adjacency list plus per-node `size_mb`/`priority_score`. Compute a standard topological order (Kahn's algorithm using in-degree counts, or DFS-based) to guarantee validity — this satisfies the hard constraint that no chunk precedes its dependency. Since a valid topo order alone won't necessarily hit the cost threshold, use the topo sort as a base but break ties among ready nodes with a heuristic that favors the evaluator's known cost model: since the eval binary applies a per-chunk cost of `size_mb / priority_score` with a 50% discount when a chunk immediately follows something it directly depends on (LRU cache-hit bonus), among all currently-schedulable nodes (in-degree zero) prefer one that is a direct dependent of the chunk you just placed, and among remaining ties prefer lower `size_mb / priority_score`. Write the resulting IDs one per line to `/home/user/optimal_plan.txt`, then iterate: run `/app/restore_eval /home/user/optimal_plan.txt` to see the printed cost, and refine your heuristic/ordering if it's not comfortably under 150000 — treat the eval binary as a black-box scorer you can query repeatedly, not something to reverse-engineer exactly.