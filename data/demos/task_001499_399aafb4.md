Build the pipeline as a straightforward, faithful implementation of the ISM method rather than approximating it: parse the FASTA by skipping lines starting with `>` and concatenating the remaining sequence lines into one string, then map each nucleotide to its EIIP value using the exact table given (any unexpected character should be handled deliberately, e.g. by raising or skipping, so it doesn't silently corrupt the signal). Compute the FFT of the numeric signal with `numpy.fft.fft`, the power spectrum as the squared magnitude, and the frequency bins with `numpy.fft.fftfreq(N)` (not a custom formula) so the frequency-to-index correspondence matches the standard convention. Apply the mask `0.1 <= f <= 0.4` to the frequency array, find the index of maximum power within that masked range, and read off both the corresponding frequency and its power value at that same index — round both to exactly 4 decimal places using Python's `round()` before serializing to JSON with the two required keys. Double check you're taking `abs(fft)**2`, not `abs(fft**2)`, and that rounding happens only at final output, not on intermediate values.