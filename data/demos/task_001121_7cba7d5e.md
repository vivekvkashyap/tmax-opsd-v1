Split this into two independently testable pieces: audio baseline and JSON sanitization. For the audio, read `/app/diagnostic_signal.wav` with a Rust WAV crate (e.g. `hound`), take the first 500ms of samples, detect any gaps/dropped-sample markers and fill them via linear interpolation between the surrounding valid samples before computing the mean absolute amplitude over that window; round to 2 decimals and write it to `/home/user/audio_baseline.txt` — verify by recomputing the same value with a quick independent script and comparing. For sanitization, iterate the input directory's JSON files, and for each record track the first-seen version per `id`; treat a later record with the same `id` as an adversarial duplicate if it differs only after Unicode normalization (compare NFC-normalized forms) or contains invisible characters (e.g. zero-width space) not present in the original — reject those, but pass through records that are genuinely distinct or the same in normalized form. Write survivors unmodified into a `sanitized/` subdirectory. Test against both a clean corpus (everything should pass) and an adversarial one (everything should be rejected) to confirm your normalization/anomaly check isn't over- or under-triggering.
