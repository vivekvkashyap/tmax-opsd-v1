Parse `/home/user/network_fstab` as whitespace-delimited fields per standard fstab format (device, mount-point, fstype, options, dump, pass), skip comment/blank lines, and match the row whose device field is exactly `//192.168.1.100/net_backups` — don't just grep for the IP substring, since a similar-looking share (`//10.0.0.5/shared`) also exists as a decoy and matching too loosely would grab the wrong mount point; take that row's second field as the mount point. Sum file sizes with `os.path.getsize` (or `os.stat().st_size`) over only files directly in `/home/user/router_configs/` whose name ends in `.cfg` — use `os.listdir` + extension filter rather than `glob('**/*.cfg', recursive=True)`, since the spec says "directly inside", not recursive, and there's a non-.cfg decoy file to exclude. Get Tokyo local time with `datetime.now(ZoneInfo("Asia/Tokyo"))` (Python 3.9+ zoneinfo, no external tz data needed on most Linux) and format with `strftime("%Y-%m-%d_%H-%M-%S")`. Build the tarball with `tarfile.open(path, "w:gz")`, adding only the matched `.cfg` files (use `arcname` to avoid embedding full source paths), saving it at `<mount_point>/backup_<time>_tokyo.tar.gz`. Write the three-line log with the exact `Mount:`/`Size:`/`Archive:` prefixes and absolute paths, then chmod +x and actually run the script so the log and archive exist on disk.