Build this as a small orchestration server: your C++ code does its own graph computation and separately shells out to the oracle binary, then merges both results into one JSON response — don't try to reimplement or guess what the oracle computes internally, just call it. Steps: (1) include `/app/httplib.h` and set up an `httplib::Server` listening on `127.0.0.1:8080` with a `Post("/process", ...)` handler; (2) parse the incoming JSON body — since the format is guaranteed simple (`{"nodes": N, "edges": [[a,b],...]}` with no nesting/extra spaces), a lightweight manual parser (scan for `"nodes":` then the number, then extract the `[[...]]` edges array and split on brackets/commas) is sufficient, no need for a full JSON library; (3) build an adjacency structure from the parsed edges and count triangles with a straightforward O(N^3) or adjacency-set triple-loop check over node triples, since graphs are small; (4) format the graph in the oracle's required plaintext stdin format (`N M` then `M` lines of `u v`) and invoke `/app/mol_oracle` as a subprocess (e.g. `popen` with a pipe, writing to its stdin and reading its stdout) to get the oracle's float score; (5) respond with `{"triangles": <int>, "oracle_score": <float>}`. Compile with the exact g++ command given, run it backgrounded, and test both example requests from the description with `curl` before finishing.