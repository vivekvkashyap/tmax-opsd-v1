Build `generate_row.sh` as three independent pipeline stages joined by shell variables, not one giant pipeline. For the video metric, use `ffprobe -v error -select_streams v:0 -show_entries packet=pts_time,size -of csv=p=0 dashcam.mp4` piped into `awk` filtering on `$1 >= T && $1 < T+1` and summing `$2`, defaulting to 0 with `END{print sum+0}` when nothing matched (avoids printing a blank line for empty ranges). For the sensor and weather lookups, be careful with the field separators: `sensor_data.txt` is pipe-separated with surrounding spaces (`awk -F' \| '`), while `weather.csv` is comma-separated (`awk -F,`); match `time_sec` exactly as an integer, not a substring. Assemble the final line with a single `echo "$T,$SUM,$SPEED,$TEMP"` — no extra whitespace, since exact stdout equivalence against a hidden oracle is the pass criterion. For `benchmark.sh`, wrap the six sequential calls with `time`, capturing only the `real` component (e.g. via `TIMEFORMAT` or parsing `time -p`'s output), discard each call's stdout with `>/dev/null`, and write only the numeric seconds to `benchmark.log`. Test with a few known timestamps by hand before assuming correctness, since your output must match the oracle's byte-for-byte across many random T values.