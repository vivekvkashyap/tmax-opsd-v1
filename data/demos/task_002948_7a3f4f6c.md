Start by parsing the log with a regex or line-scan to extract, for each `ERROR ... failed` entry, the `job_id` and the comma-separated `Inputs:` list of floats — only failed jobs matter, ignore INFO/completed lines entirely. The core fix is numerical: don't recompute variance via `mean(x^2) - mean(x)^2` (that's what caused the negative-variance bug from catastrophic cancellation on large-magnitude, low-spread values). Instead use a numerically stable one-pass method like Welford's algorithm, or simply shift the data first (subtract the sample mean from each value before squaring and summing) so the numbers you square are small. Compute **sample** variance with n-1 in the denominator, not population variance with n. For each extracted job, run the stable calculation, round to exactly 6 decimal places, and write `job_id,variance` lines to `/home/user/fixed_variances.csv` with no header, in the order the failed jobs appear (or sorted, whichever the log implies is natural) — then manually verify one case by hand-computing the shifted-sum variance to confirm your script's rounding and stability logic are correct.
