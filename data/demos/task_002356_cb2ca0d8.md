Break this into independently-verifiable pieces. For the WAF, write `detector.sh` in pure Bash: extract the `X-API-Version` header value with grep/sed, split it into major.minor.patch integers, and compare numerically (not lexicographically — e.g. `1.10.0` > `1.5.0`) against `1.5.0`; treat a missing header as evil. Separately check the request line for `../` and, case-insensitively, `%2e%2e%2f` (use `grep -i`). Test both rules against every file in `/home/user/corpus/clean/` and `/home/user/corpus/evil/` and iterate until 100% match. For services, cross-compile the Go binary with `GOOS=linux GOARCH=amd64 go build` and `GOARCH=arm64` into the two named files. Edit `nginx.conf`'s `location /api/` and `location /auth/` blocks to add `proxy_pass` directives to `127.0.0.1:8081` and `127.0.0.1:8082` respectively, keeping trailing slashes consistent so path prefixes aren't double-appended. In `start_services.sh`, launch Redis, Flask, the Go binary, and nginx in that order, backgrounding each, then curl each service to confirm reachability before declaring success.
