Start by running `cargo build --release` in `/home/user/artifact_server` to see the actual borrow-checker error rather than guessing; the likely root cause is a struct field typed to hold borrowed/`'static` references (e.g. `HashMap<&'static str, String>`) being populated from data parsed out of a transient per-request buffer — the fix is to change the map's key (and any similarly-scoped fields) to owned `String`s and call `.to_string()`/`.to_owned()` at insertion points, not to add lifetime annotations that fight the buffer's real scope. Keep the parsing/routing logic itself unchanged. After it compiles, launch it in the background and confirm with a manual `curl` that it answers on `127.0.0.1:8080` before writing the benchmark. For `benchmark.py`, issue the 500 GET requests sequentially or with simple threading, record `time.perf_counter()` deltas in milliseconds, then implement the percentile by hand: sort the latency list ascending and index it at `int(0.95 * len(latencies))` (careful with the boundary/off-by-one per the exact spec), then write the value rounded to 2 decimals to `/home/user/p95_result.txt`. Avoid numpy/statistics as required.