Keep the language split strict, since that's explicitly graded: all math (FFT filtering, Hankel matrix construction, SVD) belongs in `compute.py`, and all looping/comparison/argmin logic belongs in `optimize.sh` as real Bash (a `for` loop calling `compute.py` per iteration and tracking the best result with shell arithmetic, e.g. `bc` or `awk` for floating point comparisons — Bash itself can't do floats natively). For the FFT low-pass filter, compute frequency bins with `np.fft.fftfreq(n, d=dt)` and zero out FFT coefficients where `abs(freq) > f_c`, remembering this affects both positive and negative frequency bins symmetrically (numpy's fftfreq already returns negative frequencies in the back half, so a simple abs() comparison over the whole array is correct — don't special-case only the first half). Build the Hankel/trajectory matrix by sliding a window of length 50 over the filtered signal (row i = filtered[i:i+50]), run `np.linalg.svd` and print only the largest singular value as a float with no extra text (since the bash script parses this stdout value directly). In `optimize.sh`, loop f_c from 1 to 20, capture `compute.py`'s stdout, compute the absolute difference from the target with `awk` or `bc -l`, keep the f_c with the smallest difference, and write just that integer to `/home/user/best_fc.txt`.