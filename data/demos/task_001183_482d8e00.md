Do the recovery step first, since it unlocks the fixtures you need to test against, then build the general-purpose script — do not hardcode anything specific to the sample fixture inside wal_applier.py, since it will be fuzz-tested against a hidden oracle on fresh random inputs. To recover the passphrase, transcribe `/app/passphrase.wav` with a speech-to-text tool (e.g. `whisper` or any available ASR CLI/library), normalize the transcript to lowercase words with no punctuation; use that recovered phrase both as the zip password and to extract the multi-part archive (reassemble the split zip parts, e.g. `zip -FF` or `cat state.z01 state.z02 state.zip > combined.zip` then extract, or use a library that supports multi-volume + encrypted zips) into the target directory. For the CLI script, compare the passphrase argument against the transcript you recovered and exit 42 immediately on mismatch. Parse the WAL line by line, skipping blanks/`#` comments, and implement each command literally per the spec: RENAME_EXT with `os.rename`/glob over the extension, CHUNK_FILE by reading bytes and slicing into fixed-size parts (last part gets the remainder) then deleting the original, and CONVERT_KV_JSON by parsing `key=value` lines (ignoring non-matching lines) and rewriting with `json.dump(..., indent=2)`. Match file naming, ordering, and byte-for-byte output exactly, since equivalence is checked via checksums against a reference binary.
