Two independent, easily-verified deliverables — check each in isolation. Open `/home/user/nginx.conf` and locate the `proxy_pass` directive; it currently points at the wrong upstream port, so change it to `http://127.0.0.1:8080;` exactly (keep the trailing semicolon and existing block structure intact) — grep the file afterward to confirm the change stuck. Then write `/home/user/monitor.py` as a self-contained script: use `shutil.disk_usage('/home/user')` to compute the used-percentage float, use `zoneinfo.ZoneInfo("Asia/Tokyo")` with `datetime.now(tz)` to get a correctly offset ISO 8601 timestamp (Tokyo is UTC+9, so confirm your formatted string ends in `+09:00`), and issue an HTTP GET to `http://127.0.0.1:8081` with `urllib.request` or `requests` to capture the status code — wrap the request in error handling since the grader spins up its own server on that port. Assemble the four fields into the exact JSON schema given and write it to `/home/user/status.json`, then `chmod +x monitor.py`. Test the script yourself by starting a throwaway listener on 8081 first, so you're not submitting untested code.
