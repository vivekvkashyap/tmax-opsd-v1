Debug this as two independent bugs, not one. First, the ODE integrator: the code fixes `dt := 1.0` for a stiff-ish nonlinear decay, causing it to overshoot and diverge (or hit negative/NaN values) — replace it with a small fixed step (e.g. 0.01) taken repeatedly between each pair of observation timestamps (the existing inner `for current_t < t[i]` loop structure can stay, just shrink dt and don't let it grow back). Second, the MCMC acceptance rule: search for how the acceptance probability is computed from the log-likelihood ratio — a common bug is comparing raw log-likelihoods directly against a uniform random draw instead of exponentiating the log-ratio (accept if `log(uniform()) < logL_proposal - logL_current`, or equivalently `uniform() < exp(logL_proposal - logL_current)`), or a sign/direction error in that comparison, which causes near-0% acceptance. After fixing both, run >=10,000 MCMC iterations with a reasonable proposal step size for theta, discard the first 2,000 as burn-in, average the remaining samples, and write mean_theta and acceptance_rate to /home/user/posterior.json. Validate acceptance_rate lands in [0.15, 0.50] by tuning the proposal's step size — too large means you'll reject almost everything, too small means the chain won't mix.