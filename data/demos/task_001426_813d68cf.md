Work phase by phase and validate each output file before moving to the next. In the C evaluator, parse each line of formulas_v2.txt by splitting on `:` then `|` to isolate Module, Expression, and the provided Checksum integer; compute your own checksum by XOR-ing the raw ASCII byte value of every character in the Expression substring exactly as it appears (including any spaces) — compare to the provided value and skip the line silently if they differ, since this is the mechanism that intentionally filters out the "Payment" formula. For lines that pass, implement a small recursive-descent (or shunting-yard) expression evaluator supporting `+`, `*`, and parentheses with correct precedence — don't just eval left-to-right. Print `Module: Cost` per valid line, redirect that into costs_v2.txt. For phase 2, sort both cost files by module name (`sort` command or Python) and produce the diff with plain `diff -u sorted_v1 sorted_v2 > cost_diff.txt`, checking the hunk content (not just that a file exists) matches what you'd expect given which modules changed value and which was dropped. For phase 3, run a simple `python3 -m http.server 9000 --directory /home/user` in the background, then write proxy.py as a minimal HTTP server on 8080 that, only for GET /ci/diff, makes an internal request (e.g. `urllib.request` or `http.client`) to `http://localhost:9000/cost_diff.txt` and streams its body back with 200; every other path must return 404. Verify end-to-end with `curl http://localhost:8080/ci/diff` and a bad path before leaving both processes running.