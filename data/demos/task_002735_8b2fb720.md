Start by transcribing `/app/voicemail.wav` (e.g. with an ASR tool) to extract the two hardcoded rules it states — likely a banned exact phrase and a banned IP subnet prefix; write those down precisely as they're spoken. Separately, do actual data analysis on `/app/train/transcripts/`, `/app/train/metadata.csv`, and `/app/train/labels.csv`: for each `evil`-labeled transcript, look for a text token/pattern that appears consistently across evil transcripts but never in clean ones (e.g. grep for suspicious all-caps tokens or unusual substrings and check which ones perfectly separate the two classes) — don't guess, verify the pattern has zero false positives/negatives on the training set. Then write `/home/user/filter.sh` taking `<transcript_path> <metadata_csv>`, deriving `transcript_id` from the transcript's basename (strip `.txt`), looking up its metadata row from the CSV (awk/grep on the matching `transcript_id` field), and exiting 1 if the audio-derived phrase rule matches, or the discovered text pattern matches, or the IP field matches the audio-derived subnet prefix — otherwise exit 0. Test it manually against several training examples per class before finishing, since the verifier requires 100% accuracy on both corpora.