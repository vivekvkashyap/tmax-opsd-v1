Transcribe the audio with the `whisper` CLI (e.g. `whisper /app/deployment_weights.wav --model base --output_format txt`) and read the resulting transcript carefully — spoken numbers will come out as words or digits depending on model settings, so parse the transcript text to pull out the integer weight for each route in the order they're mentioned. Don't hardcode weights; derive them from what whisper actually outputs, and sanity-check the count of numbers matches the number of routes mentioned. Once you have the list of integer weights, normalize them by dividing each by their sum to get probabilities, then compute Shannon entropy in nats: `H = -sum(p * ln(p) for p in probs)` using Python's `math.log` (natural log, not log2/log10). Write only the resulting float to `/home/user/entropy_result.txt` with no extra text — the grader allows some tolerance (absolute error ≤ 0.05) but a wrong route count or misread digit will blow that margin, so re-listen or cross-check the transcript if the numbers look implausible.