This is an audio-transcription-then-extraction task: first install/verify a speech-to-text tool (e.g. `openai-whisper` or `SpeechRecognition`+`ffmpeg`) and run it on `/app/noc_report.wav` to get a transcript — do not guess the IPs from the task description, they aren't given there. Inspect the transcript carefully: the speaker likely states IPs both in dotted-digit form and by spelling them out in words (e.g. "ten dot five dot twelve dot four"), so after transcribing, write logic (regex for dotted-quad patterns, plus a words-to-digits normalizer for phrases like "dot", "hundred", number words) to recover every IP mentioned, then dedupe. Write each recovered IP on its own line to `/home/user/extracted_ips.txt`. Since grading is an F1 score against the true IP set with a 0.8 threshold, prioritize precision as well as recall — don't dump every digit-like token, and re-listen/re-check ambiguous transcriptions (e.g. whisper mishearing "one ninety two" as separate numbers) before finalizing. The firewall-script-generation part (`generate_fw.py` producing an idempotent `apply_firewall.sh` using `iptables -C ... || iptables -A ...` style checks) is comparatively mechanical — get the IP extraction right first since that's what's scored.