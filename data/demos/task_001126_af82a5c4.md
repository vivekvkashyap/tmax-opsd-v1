Do not hardcode a guessed learning rate or iteration cap — transcribe /app/field_notes.wav (e.g. run it through a speech-to-text tool available on the system, or convert/listen and note the spoken numeric values carefully, including "point" decimals and word-form numbers) to recover the exact learning rate and max-iteration values the task requires, and use those parsed values as constants in your Rust program. Implement batch gradient descent over the whole CSV each epoch: compute predictions, MSE, and gradients w.r.t. m and c using the full dataset, update both weights simultaneously, and track MSE across epochs. Converge when `abs(mse_prev - mse_curr) < 1e-5`; classify EVIL if you hit the iteration cap first, or if MSE becomes NaN/Infinity at any point (check with `is_nan()`/`is_infinite()` in Rust) — check divergence *during* the loop, not only at the end, since values can blow up before the cap. Print exactly `CLEAN` or `EVIL` plus newline, nothing else to stdout. Build with `cargo build --release` and test against a few sample CSVs from both corpora before trusting it.