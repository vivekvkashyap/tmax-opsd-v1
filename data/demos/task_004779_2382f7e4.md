Extract frames first, then decode: use `ffmpeg -i /app/network_scans.mp4 frames/frame_%04d.png` to dump every frame, then run `zbarimg --raw -q frames/*.png` (or loop per-file) to decode each QR code's payload; consolidate the decoded `Source,Destination,Weight` lines into a single edge-list file, de-duplicating and validating the CSV-like format as you go since OCR/QR decoding can occasionally miss or duplicate frames — sanity-check the count and look for parse failures before trusting the dataset. Inspect the consolidated edges to find the max node ID actually present so your C program's arrays/adjacency structures are sized correctly (don't hardcode an assumed node count). Write `/home/user/graph_query.c` to read this edge file at startup (hardcoded path, not a CLI arg), build an adjacency list (array of edge lists or CSR-style), and implement Dijkstra with a binary heap/priority queue for each `stdin` query line (`Source Target`), printing only the integer distance (or `-1` if unreachable) per line, looping until EOF. Compile with `gcc -O2 -o /home/user/graph_query graph_query.c`. Before finishing, test it manually against a few known query pairs derived from your own edge file to confirm correctness and that no extraneous output is printed, since the grader does an exact byte-for-byte stdout comparison against a reference implementation.
