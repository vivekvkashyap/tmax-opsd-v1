Build the pipeline in clear stages: parse, group/sort, dedupe, report, schedule. Read `/home/user/etl_data.tsv` (tab-separated, no header) with a script (inline Python is easiest for correctness of Levenshtein distance and stable grouping), group records by the Category column, and within each group sort by Timestamp ascending. Walk each group's sorted records maintaining a "kept" list per category; for each candidate, compute Levenshtein distance from its Message against every already-kept Message in the same category (not just the previous record — "any already kept record"), and drop the candidate if the minimum distance is ≤ 3, otherwise keep it and append to the output in sorted order. Use a standard Levenshtein implementation (e.g. `python -c` with a DP table, or `pip install python-Levenshtein` if available) rather than approximating with string equality. Write the kept rows to `/home/user/cleaned_data.tsv` in the original tab-separated format, then compute N (total input lines), M (kept count), K=N-M, and emit `/home/user/report.txt` matching the template exactly, including the border lines. Finally add a crontab entry with `(crontab -l 2>/dev/null; echo "15 3 * * * /home/user/clean_pipeline.sh") | crontab -` and `chmod +x` the script.
