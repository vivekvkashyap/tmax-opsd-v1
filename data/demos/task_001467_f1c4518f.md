First locate the exact line performing the unstable reduction — `data_sum = sum(data)` inside the likelihood function — and change only that call to `math.fsum(data)` (add the `import math` if needed), leaving the random seed, iteration count, and every other line untouched, since any incidental change will shift the RNG stream and desync the comparison. Run `python3 /home/user/mcmc_sim.py` to regenerate `/home/user/posterior.txt`. Then write a small separate Python script that loads both `/home/user/posterior.txt` and `/home/user/ground_truth.txt` (e.g. with `numpy.loadtxt`), runs `scipy.stats.ks_2samp(posterior, ground_truth)`, and formats the resulting p-value to exactly 4 decimal places with an f-string like `f"{pvalue:.4f}"` — do not use Python's `round()` plus default `str()`, since that can drop trailing zeros. Write only that formatted number (no extra whitespace or newline issues) to `/home/user/p_value.txt`.
