For Phase 1, fix the C++ tool by replacing `strcpy` into a fixed 10-byte stack buffer with a bounds-safe copy (e.g. size the buffer to the input, or use `strncpy`/`std::string`), and free (or better, avoid) the leaked `new int[100]` — then compile with `g++` and confirm the binary runs without crashing or leaking (check with a sanitizer or valgrind if available). For Phase 2, write the `.proto` exactly matching the field names and types given (repeated string, map<string,int32>, double) so any codegen you run produces compatible stubs. For Phase 3, treat the constraint problem as a small brute-force search over the given finite domains (Core in {1,2,3}, UI in {1,2}, Network in {2,3}, Database in {1,2,3}) rather than hand-deriving it — enumerate all combinations, filter by the UI==2⇒Core>=2 rule, the Network==Database rule, and the sum==9 rule, and use whichever satisfying combination you find (the task only requires one valid combination, not a specific one) to compute the score with the given weighted formula. Implement the gRPC server for real (listening on localhost:50051) and a real client that calls it, then have the client write the JSON output — don't hardcode the JSON without actually running the RPC round-trip, since the pipeline's functionality is part of what's being built.
