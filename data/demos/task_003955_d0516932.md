Write a Python script that processes both sources with careful validation rather than assuming clean data. When reading `customers.csv`, validate each row strictly: `id` must parse as an integer (reject non-numeric like `"invalid"`), `age` must parse as an integer AND be >= 18 (reject non-numeric like `"NaN"` and reject under-18 rows), and `status` must be exactly `"ACTIVE"` or `"INACTIVE"` (reject anything else, e.g. `"UNKNOWN"`) — use try/except around `int()` conversion rather than a regex, since malformed numeric strings should simply exclude the row, not crash the script. For `logs.jsonl`, parse each JSON line and compute `log_tokens` as `len(text.split())` (which naturally ignores leading/trailing/extra whitespace, unlike splitting on a single space). Perform an inner join on `id == user_id` in Python (e.g. build a dict keyed by id from the filtered customers, then iterate logs matching against it), emit each joined record with exactly the four keys as native `int`/`str` JSON types, sort the resulting list by `id` ascending, and write one JSON object per line to `/home/user/processed_data.jsonl`. Manually trace 2-3 rows against the raw input by hand to confirm your filtering and join logic before finishing.