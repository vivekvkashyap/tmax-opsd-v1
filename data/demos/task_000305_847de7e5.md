Work in three passes. First, fix the C bug: read `semver_parse`/`parse_int` in `/app/semver.c-1.0.0/semver.c` looking for a fixed-size stack buffer (e.g. `char buf[12]`) that a `strcpy`/`memcpy` fills from an unbounded input string — long pre-release/build-metadata substrings overflow it and segfault. Fix by sizing the buffer to the actual token length (or using pointer arithmetic / `strncpy` with the real bound) rather than just enlarging the constant, since the dataset intentionally contains very long tags. Rebuild with `gcc -O3 -shared -fPIC -o libsemver.so semver.c` (or however the source builds), and reproduce the crash on a long string first to confirm your fix before moving on. Second, lay out `/home/user/project/{src,lib,data}` exactly as specified and move files there. Third, write `sort_versions.py` using `ctypes.CDLL` to load `libsemver.so`, wrap the comparison function via `functools.cmp_to_key` (or similar) for a descending sort, and read/write files with buffered I/O — avoid per-line Python overhead or subprocess calls, since 100k comparisons must finish under 2.5s. Test correctness against a handful of known orderings before trusting the full run.