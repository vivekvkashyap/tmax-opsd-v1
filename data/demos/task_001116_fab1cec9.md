Fix the numerics before building anything on top of them. Open `/home/user/simulate.py` and locate the local-error estimate in `adaptive_euler_heun`; summing signed differences across state components lets errors of opposite sign cancel, which is why the step size can blow up — replace it with the maximum absolute component-wise difference between the Heun and Euler estimates so the adaptive step controller actually rejects large errors. Verify the fix by running the integrator and confirming the trajectory stays bounded (a Van der Pol limit cycle) rather than diverging. For `fit_mu.py`: after integrating, interpolate the non-uniform output onto the required uniform grid with `interp1d(kind='linear')`, take the rFFT with no windowing, zero out or skip the DC bin, and pick the frequency bin with the largest magnitude as the dominant frequency — don't smooth or window, since the exact bin location is what determines the answer. Wrap this pipeline as a function of `mu`, define the residual against the target frequency, and root-find with `brentq` over the given bracket (confirm the residual changes sign at the interval endpoints first). Round the root to exactly 3 decimals when writing the output file.
