Build the pipeline as clearly separated stages so each is independently testable. Use `concurrent.futures.ProcessPoolExecutor` (or `multiprocessing.Pool`) to map a per-file worker function over the `.jsonl` files in `/home/user/raw_data/`, where each worker reads its file line by line, parses JSON, and yields cleaned records. Normalize `text` with a single regex pass: lowercase it, then `re.sub(r'[^a-z0-9 ]', '', text)` to strip everything but alphanumerics and spaces, then `re.sub(r'\s+', ' ', text).strip()` to collapse whitespace — do these in that order so punctuation removal doesn't leave irregular spacing that the collapse step then needs to fix anyway. Split on space for tokens and drop any record with fewer than 3 tokens. Group survivors by `category` into lists, sort each group's records by `id` (plain string sort, since IDs like "A02" < "A03" compare correctly lexically), and take the first 5 per category (fewer if the category has fewer than 5 valid records — don't pad or error). Write exactly the four required keys per line to `/home/user/cleaned_sampled.jsonl`, and self-check by counting output lines and manually recomputing normalized text/token counts for a couple of records against the raw input before finishing.
