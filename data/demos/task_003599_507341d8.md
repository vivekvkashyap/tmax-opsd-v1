Two independent bugs, fix and verify separately. (1) Precision: find where the summation loop accumulates in `f32` — summing large arrays with mixed magnitudes in f32 loses precision to catastrophic cancellation. Switch the accumulator to `f64` (or implement Kahan/compensated summation) and format the JSON response accordingly; validate by differential-testing your service's output against the legacy reference oracle described in the task on the same tricky inputs (e.g. large-magnitude values summed with tiny ones) until they match exactly, not just "close enough". (2) Task leak: find the `tokio::spawn` that does background aggregation per request — if it reads from a channel or loop with no way to observe the client going away, it keeps running after disconnect. Wrap the spawned work with `tokio::select!` against a cancellation signal tied to the response future/body being dropped (axum/hyper drop the connection future on client disconnect), or check the incoming stream for closure and abort the task. After fixing both, `cargo build`, run the binary bound to 127.0.0.1:8080, and confirm `/health` returns 200 and memory/task count doesn't grow after simulated aborted connections.