Start with the memory dump: search `/app/memory.core` for the ASCII marker (e.g. `strings /app/memory.core | grep CALIB_KEY=` or grep with `-a -o`) and extract the 16 characters immediately following the `=` — don't guess the key, read it out of the binary dump programmatically since it's embedded at a random offset. Next, read `nlms_filter.c` closely: the main loop condition `i <= num_samples` runs one iteration past the allocated buffers, writing out of bounds — fix it to strict `<`. Then address the divergence bug: the current update rule `w += mu * e * x` is plain LMS with a fixed step size, which explodes on high-amplitude/noisy input; replace it with the normalized form `w += mu * e * x / (x*x + 1e-6)` (or equivalently scale `mu` by that denominator) so the update size adapts to input power instead of blowing up. Recompile with `gcc -O2 -o /app/nlms_filter nlms_filter.c`, then run it with the input file, output path, and the calibration key you extracted (must match exactly, including length) as the three arguments, and sanity-check the output audio isn't clipped/static before considering it converged.
