Extract the tar carefully and write your own traversal (don't use `find -L` or naive recursive `os.walk(followlinks=True)`, which will hang on the self-referential symlink) — track visited real paths via `os.path.realpath` and skip any symlink whose resolved target you've already visited or that points outside/above the extraction root. Recursively find and extract nested `.zip`/`.tar.gz` archives you discover during that walk (extract into a subdirectory, then continue traversing into it so archives-within-archives are also handled). OCR `/app/note.png` with tesseract to recover the numeric XOR key stated on the sticky note, then decode each `.enc` file by XOR-ing every byte with that key (as an int) and write the result as a sibling `.txt` file. Collect all `.txt` files (both originals and decoded), sort by base filename alphabetically, then merge their contents while deduplicating at the line level — keep a `seen` set of lines and only write lines not already seen, preserving first-occurrence order across the whole merge. Finally `gzip -9` (or Python's `gzip.GzipFile(compresslevel=9)`) the merged file to `/home/user/clean.gz`; success is measured purely by the resulting file size being small, which only happens if dedup and decoding both worked correctly.