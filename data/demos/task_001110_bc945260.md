Diagnose the 502 by comparing the two sides of the socket, not by guessing: grep the socket path used in `proxy_pass http://unix:...` inside `/home/user/restore/nginx.conf` against the `SOCKET_PATH`/bind() path hardcoded in `/home/user/restore/src/backend.c` — they must match exactly, and the task requires the shared path to be `/home/user/restore/run/backend.sock`, so edit whichever side is wrong (likely both need to point to that exact path, and ensure the `run/` directory exists before the daemon binds). While in the C file, add a startup step that gets the daemon's own PID via `getpid()` and writes it as text to `/home/user/restore/run/backend.pid` right after the socket is created and before entering the accept loop. Compile with `gcc -o /home/user/restore/bin/backend /home/user/restore/src/backend.c`, run it in the background, confirm the socket file appears at the expected path with `ls -la` and `file`, then start nginx exactly as instructed (`nginx -c ... -p ...`) and check it's actually listening on 8080 (`ss -tlnp` or `curl -v`) before testing the endpoint. Finally curl `/api` and redirect the raw output into `/home/user/restore/success.txt`, then diff it against what the backend's hardcoded HTTP response body actually is.