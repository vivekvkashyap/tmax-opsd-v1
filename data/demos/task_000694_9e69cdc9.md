Load `/home/user/locks.ttl` with `rdflib.Graph().parse(..., format="turtle")`, then build a "waits-for-transaction" relation: for each pair (A waitsFor R) and (B holds R) with the same resource R, A effectively waits for B. You can compute this either with a SPARQL query joining `ex:waitsFor` and `ex:holds` on the shared resource, or by building the derived edge set in Python after a simpler SPARQL SELECT that pulls all holds/waitsFor triples — either is fine as long as the derived transaction-to-transaction graph is correct. Once you have that directed graph, detect cycles of length 2 (A->B->A) and length 3 (A->B->C->A) specifically — check all pairs and triples of transactions for a closed loop, or run a general cycle-detection (e.g. DFS with recursion stack) and filter to cycles of size 2 or 3. Collect every transaction URI that participates in at least one such short cycle, strip the namespace prefix to get the local name (the part after the last `/` or `#`), dedupe, sort alphabetically, and write as a JSON array to `/home/user/deadlocks.json`.