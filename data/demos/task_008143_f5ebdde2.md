Copy (never move) everything from `/home/user/repo/binaries/` into `/home/user/repo/staging/` first, preserving the originals untouched. Write `/home/user/renamer.cpp` using `<filesystem>` and `<fstream>`: parse `mapping.csv` line by line splitting on the comma into (hashed_name, semantic_name), then `std::filesystem::rename` (or copy+remove) each matching staged file to its semantic name in place. Compile and run it. For the backup step, the key detail is using GNU tar's `-C` flag to change directory into staging before adding files, so the archive stores bare filenames instead of the full `home/user/repo/staging/...` path: `tar --create --gzip --listed-incremental=/home/user/backup/snapshot.snar -C /home/user/repo/staging -f /home/user/backup/incremental.tar.gz .` — reuse the existing snapshot file rather than deleting/recreating it, since that's what makes it an incremental backup. Verify with `tar -tf incremental.tar.gz` that entries are relative (no leading path) before finishing.
