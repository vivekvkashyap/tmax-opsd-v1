Load the three CSVs into SQLite (via `sqlite3 .import` or Python's `csv`+`sqlite3`), then think about the query plan before adding indexes: you'll join/filter on `products.category_id`, `categories.parent_category_id`, and `sales.product_id`+`sale_date`, so index those columns and record the `CREATE INDEX` statements in indexes.sql. In analyze.py, write one query using a recursive CTE that starts from the row where `category_name = 'Electronics'` and walks `parent_category_id` downward to collect all descendant category_ids — don't hardcode category IDs. Join products/sales restricted to that category set, aggregate `quantity*price` per (product_id, sale_date) as daily_revenue, then compute the trailing window with `SUM(daily_revenue) OVER (PARTITION BY product_id ORDER BY sale_date ROWS BETWEEN 6 PRECEDING AND CURRENT ROW)`. Critical pitfall: apply the October date filter only in the final SELECT, after the window function has run over the full ordered history — filtering rows before windowing will make the trailing sums wrong. Order final output by product_id, then sale_date, and write via Python's csv writer for exact formatting.
