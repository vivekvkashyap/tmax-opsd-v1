Build this as one deterministic Python script, keeping generation order exact since ordering and RNG state affect the final numbers. Steps: (1) build `t_eval = np.linspace(0, 10, 1000)`; (2) define the ODE as a first-order system `[x, v] -> [v, -(c*v+k*x+alpha*x**3)/m]` and integrate with `scipy.integrate.solve_ivp(..., t_eval=t_eval, rtol=1e-6, atol=1e-8)`; (3) loop `x0` in the outer loop over `numpy.linspace(-2,2,10)` and `v0` in the inner loop over `numpy.linspace(-2,2,5)` — outer/inner order matters for row placement in the 50x1000 matrix; (4) for each trajectory recompute acceleration directly from the ODE formula (not by differentiating v numerically) using the simulated x(t), v(t) arrays, then get jerk via `numpy.gradient(accel, t_eval)` and track the running max of `abs(jerk)` across all trajectories; (5) stack all 50 position arrays into a 50x1000 matrix in loop order and run `numpy.linalg.svd`, keeping the first 3 singular values from `S`. Write the JSON with unrounded floats (use `float()` casts, `.tolist()` for the array) to `/home/user/ml_data_summary.json` in the exact schema requested. Double check integration tolerances and evenly-spaced endpoint inclusion, since small deviations compound through the SVD and gradient calculations.