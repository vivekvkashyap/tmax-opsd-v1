Build this pipeline stage by stage and verify each before moving on. First set up the plumbing: `git init --bare /home/user/operator.git`, `mkdir -p /home/user/staging /home/user/webroot`. Second, reverse-engineer `/app/crd_parser` — run `strings /app/crd_parser` to find embedded usage text/flags and any hardcoded secret-looking string, and confirm argument parsing with `objdump -d` or `ltrace ./crd_parser ...` if strings alone is ambiguous; verify your hypothesis by actually invoking it with a candidate file and key and checking for success vs "Invalid key" style errors. Third, write the post-receive hook in C: it should read the pushed refs/pack from stdin (or simply run `git archive`/`git checkout-index` against the just-updated bare repo to extract `deployment.crd` into staging — the standard approach is `git --work-tree=/home/user/staging --git-dir=/home/user/operator.git checkout -f`), locate `deployment.crd`, `exec`/`system()` the discovered `crd_parser` invocation with output redirected to `webroot/deployment.json`, `chmod(...,0600)` that file, then `symlink()`/`unlink+symlink` `latest.json` to it. Fourth, write a minimal blocking HTTP/1.1 server in C with a raw socket + `bind`/`listen`/`accept` loop that parses only the request line for `GET /api/v1/manifest`, reads `latest.json`, and writes a proper `HTTP/1.1 200 OK` response with `Content-Type: application/json` and correct `Content-Length`; run it detached (`&` or double-fork daemonize) bound to 127.0.0.1:8080. Finally write `health.sh` using `curl -s -o /dev/null -w '%{http_code}\n' ...` appended to the log, and add it via `(crontab -l; echo '* * * * * /home/user/health.sh') | crontab -`. Test the whole chain end-to-end with a real `git push` before finishing.
