Start by scanning localhost ports 8000-8100 (e.g. `nmap -p8000-8100 127.0.0.1` or a simple loop with `curl`/`nc`) to find the running HTTP audit service. Hit its root endpoint first — inspect response headers/cookies carefully (e.g. `curl -i`), since the service will hint at what session cookie or role it expects for access rather than just returning data outright. Once you have the right `Cookie` header, request the logs endpoint and you'll get an encoded payload — check for stacked encodings (e.g. base64 wrapping a plain string) and decode fully to recover the exact start/end sample indices for the sensitive audio segment; don't hardcode indices without deriving them from the service response. Then write a C program that opens `/app/intercept.wav`, reads and preserves the standard 44-byte RIFF/WAV header verbatim, treats the remaining data as 16-bit PCM samples, zeroes out every sample whose index falls within [start, end] inclusive, and writes the full result (header + modified samples) to `/home/user/clean_audio.wav`. Be precise about sample-vs-byte indexing (each sample is 2 bytes) and inclusive/exclusive boundaries, since grading compares MSE against a reference redaction and any off-by-one in the range will fail the <0.1 MSE threshold.
