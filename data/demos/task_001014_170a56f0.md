Break this into three verifiable stages and check each before moving to the next. Stage 1 — fuzzy substring match: for each sequence in each day's FASTA, slide a window of length 15 (the signature length) across it and count a match if the Hamming distance between the window and the signature is ≤1 (i.e. at most one differing character, same length, no indels); count sequences (not occurrences) containing at least one such window per file, giving you 10 daily counts — print these out and sanity check they're roughly increasing before trusting the rest of the pipeline. Stage 2 — ODE fit: implement Forward Euler explicitly (`V += dt * r * V * (1 - V/K)`) stepping from t=0 to t=9 with dt=0.1 (90 steps), initializing V(0) to your actual Day-1 count, then sample the simulated V only at the integer-day checkpoints (t=0,1,...,9) to compare against actual counts. Do a brute-force grid search over r from 0.00 to 1.00 in steps of 0.01, computing MSE at each r, and keep the smallest r on ties — don't use an optimizer library, the exact grid + tie-break behavior matters for matching the expected r. Stage 3 — write r_value.txt (2 decimals) and results.csv (2-decimal predictions), compile with the exact g++ command given, run it, then write and run the small plotting script last.