Treat this as adversarial-corpus-driven development, not a fixed spec: write a first-pass C filter, then iterate against the sample corpora until it's 100% correct on both, since the hidden grading set follows the same generation rules.
1. Read a handful of files from `/app/corpus/clean/` and `/app/corpus/evil/` to see concretely what "safe complex join / window function" vs "cartesian bomb / recursion bomb" look like in this dataset before writing detection logic.
2. In `/home/user/filter.c`, read all of stdin into a buffer, then scan case-insensitively for: (a) `CROSS JOIN` or comma-separated table lists in `FROM` that lack a following `WHERE`/`LIMIT` clause tying tables together, and (b) `WITH RECURSIVE` nesting depth greater than 3 — track nesting by counting `WITH RECURSIVE` occurrences relative to enclosing parentheses/CTE scopes, not just a flat count of the keyword.
3. Be careful not to flag legitimate joins that use proper `ON`/`WHERE` conditions or window functions (`OVER (PARTITION BY ...)`) — these must still print `ACCEPT`.
4. Compile with `gcc`, run your binary against every file in both corpus directories (`filter < file`), and check output correctness before considering the implementation done; the reference validator only checks basic SELECT/DROP/DELETE syntax, so don't rely on it for the cartesian/recursion-bomb detection — that logic must be your own.
