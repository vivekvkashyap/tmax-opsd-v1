Work bottom-up: get the vendored Rust engine compiling and correct before building the server on top of it. Locate the recursion/depth-bound handling in the vendored crate's source under `/app/vendored/graph-engine-0.4.2/` — recursive graph traversal bugs in Rust engines are commonly an off-by-one or artificially low depth guard (e.g., a hardcoded panic or early-return condition meant for testing that was left in); grep the source for `panic!`, `depth`, or bound-check comparisons to find it, then loosen/correct the condition so it matches the intended `max_depth` semantics, and confirm with `cargo build`/a small local test that recursion to arbitrary depth no longer crashes. Then build the HTTP server (std-lib `TcpListener` is sufficient) bound exactly to `127.0.0.1:9090`, parsing raw HTTP requests, checking the `Authorization` header for the exact required bearer value before doing any work (401 otherwise), parsing the JSON body for `start_node`/`max_depth`, loading the two CSVs into the fixed engine, and summing downstream edge weights via the engine's recursive query up to the given depth. Leave the process running in the background and redirect its output to the specified log file. Validate with `curl` against the documented endpoint/auth before considering it done.