Fix the linking bug first: shared libraries linked from Rust require position-independent code, so the Makefile in `/home/user/fec_lib` needs `-fPIC` added to the gcc invocation, and `build.rs` needs `println!("cargo:rustc-link-search=native=/home/user/fec_lib")` (or wherever the compiled `.so` lands) alongside the existing `rustc-link-lib=fec` — a bare `rustc-link-lib` without a search path won't find the library. For the throughput fix, the real bottleneck is doing one HTTP round-trip per frame against a service capped at 20 req/s; instead, accumulate all extracted-frame checksums into a `Vec` of a struct you design, chunk them into groups sized to respect the `/batch` endpoint's 5 req/s cap, and POST each chunk as JSON `{"batches": [...]}` — with enough frames per batch you can send everything in well under a second's worth of calls. Also avoid unnecessary buffering/copies when reading raw frames from the ffmpeg pipe. Build with `cargo build --release`, then write `/home/user/run_pipeline.sh` to simply exec the release binary so the verifier can time it end-to-end; confirm all frames land in the validator by checking its results file before declaring done.
