Start by recovering the hidden criteria from the audio before writing any filter logic — use `ffmpeg` to convert `/app/briefing.wav` to a format a speech-to-text tool can consume (or inspect it directly if a transcription tool is available on the box), and transcribe it carefully; the exact rule for "evil" requests (which path, which field, which prefix) lives only in that recording, so don't guess it from the description. Once you have the transcript, encode the rule literally in `sanitizer.py`: load the JSON argument, check `request["path"]` against the exact path from the transcript and check whether the relevant body field starts with the exact prefix mentioned, print `EVIL`/exit 1 or `CLEAN`/exit 0 accordingly. Validate against every file in both `/app/corpora/evil/` and `/app/corpora/clean/` before considering it done — a single false positive/negative fails the check. For the Go-to-Python translation, read `requester.go` to identify the channel/worker-pool pattern, then mirror it with `asyncio.gather` over an `async def process_file` coroutine reading each JSON file in the given directory and printing its path, preserving concurrent (not sequential) execution semantics.
