Work through this compliance hardening task phase by phase, verifying each phase independently before moving on. Phase 1: in `audit_runner.py`, change `subprocess.run(["/home/user/scanner.sh", args.token], ...)` to pass the token via an environment dict instead — e.g. `env = os.environ.copy(); env["SCANNER_TOKEN"] = args.token; subprocess.run(["/home/user/scanner.sh"], env=env, ...)` — then before writing `result.stdout` to `/home/user/audit_log.txt`, do `content.replace(args.token, "[REDACTED]")`; run the script with the given token and check the log for both the redaction marker and the absence of the raw token. Phase 2: `base64 -d /home/user/payload.b64 > /home/user/payload.txt`. Phase 3: `ssh-keygen -t ed25519 -N "" -f /home/user/.ssh/audit_key`, then write an `~/.ssh/config` block for `Host audit-target` with the specified HostName/User/IdentityFile/PasswordAuthentication fields, and `chmod 700 /home/user/.ssh` plus `chmod 600` on the private key. Phase 4: `openssl req -x509 -newkey rsa:2048 -nodes -keyout /home/user/certs/audit_server.key -out /home/user/certs/audit_server.crt -days 365 -subj "/CN=audit.local"`. After each phase, grep/stat the resulting files to confirm they match the exact required paths, permissions, and content before considering the task done.