Work this in the natural exploit-chain order. First stand up the target with `/app/start.sh` and inspect `/app/nginx/nginx.conf` to confirm it requires mTLS and where the CA lives. Since the CA private key is exposed in `/app/ca/`, generate your own client key + CSR with `openssl req` and sign it with the CA key (`openssl x509 -req -CA ... -CAkey ... -CAcreateserial`) — this is the standard way to forge trusted client certs when a CA key leaks. Next, reverse the compiled `auth_logic.pyc`: use `uncompyle6`/`decompyle3` or fall back to `dis.dis` on the loaded code object to see what deserialization primitive it calls on the decoded payload. Once you know it's an unsafe deserializer, the real constraint is the byte budget — a normal `__reduce__`-based payload from `pickle.dumps` is too large, so hand-craft the pickle opcode stream directly (e.g. `GLOBAL`, `TUPLE1`/`MARK`, `REDUCE`) to invoke a shell/system call with a minimal command string, keeping raw bytes ≤ 65. Verify locally by unpickling your crafted bytes before sending. Finally write `/home/user/exploit.py` using `requests` with `cert=`/`verify=` pointing at your forged cert and the CA cert, POSTing the base64-encoded payload to `/escalate`, and confirm `/tmp/admin_achieved.txt` appears.
