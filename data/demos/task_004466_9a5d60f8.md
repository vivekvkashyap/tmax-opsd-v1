Build this as a Bash wrapper around sqlite3, not raw SQL typed inline without structure. In the script: (1) drop and recreate the nodes/edges tables each run for idempotency, then use sqlite3's `.mode csv` and `.import` (or `.import --csv --skip 1`) to load nodes.csv and edges.csv; (2) write a query that filters `WHERE is_stale = 0` (or `!= 1`) on edges before doing any traversal — filtering after aggregation is a common mistake that lets stale edges leak into results; (3) implement the traversal as a Recursive CTE that starts from the node whose node_name matches the first script argument, unions in one more hop per recursion accumulating total weight and hop count, and stops recursing once hop count reaches the second argument (MAX_HOPS); (4) since multiple paths can reach the same node, group/window over the recursive results to keep only the MIN(total_weight) per destination, then rank ascending by that minimum weight (`DENSE_RANK() OVER (ORDER BY min_weight)` or similar) — exclude the start node from final output unless a cycle brings you back to it; (5) pass `$1`/`$2` into sqlite3 safely (e.g. via `.parameter` binding or careful quoting, not naive string concatenation prone to injection issues) and print final results as plain CSV with the exact header `destination_name,min_total_weight,rank` and no extra whitespace.