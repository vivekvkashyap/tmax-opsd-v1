In C, compile a POSIX regex like `^([A-Za-z0-9_]+)="([^"]*)"$` with `regcomp`/`regexec` to pull `KEY` and the English string per line of `untranslated.txt`, skipping non-matching lines entirely (don't crash or partially parse them). Stream `tm.tsv` with `fgets` (or `getline`) in a loop — never `malloc` the whole file — splitting each line on the tab character to get `English_TM` and `Spanish_TM`. Implement classic Levenshtein distance yourself with a DP table (two rolling rows of length `len+1` is enough memory, no need for the full matrix), case-sensitive, comparing the extracted English string against each `English_TM` in turn; track the minimum distance and its Spanish translation, updating only on strictly `<` (never `<=`) so the first-seen match wins ties. After streaming the entire TM file for one key, write that key's best result immediately (or buffer results and write in input order) to `/home/user/updated_translations.tsv` in the exact tab-separated format `KEY\tOriginal_English\tBest_Match_Spanish\tDistance`. Compile with the exact `gcc` invocation specified and run it to produce the file — validate a couple of distances by hand before trusting the output.
