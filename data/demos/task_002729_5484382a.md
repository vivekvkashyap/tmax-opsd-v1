First fix and install the vendored diffing library: inspect `/app/bsdiff4-1.2.4/setup.py` for the `Extension(...)` `sources` list — it likely references a nonexistent/renamed C source filename instead of the real one present in the package's source tree; list the actual files in that directory, correct the filename reference in `setup.py`, then `pip install -e /app/bsdiff4-1.2.4` and confirm `import bsdiff4` works before writing the backup script. In `/home/user/backup.py`, walk `/home/user/project/`; for each file, if no counterpart exists in `/home/user/backup_dir/`, copy it verbatim; if a counterpart exists, open the old (backup_dir) and new (project) files, read their bytes while holding a shared `fcntl.flock(fd, fcntl.LOCK_SH)` on each read (release with `LOCK_UN` afterward) to avoid tearing on concurrent writers, call `bsdiff4.diff(old_bytes, new_bytes)`, and write the result to `backup_dir/<filename>.patch` — leave the original base file untouched, and do not also copy the full new file (that would defeat the storage-efficiency check). Run the script and confirm `.patch` files appear and are small relative to the originals.