Split this into two independently testable pieces. For `run_etl.sh`: use `ffmpeg -i /app/data/source.mp4 -vf fps=1 /home/user/processed/frames/frame_%04d.jpg` (create the directory first) to get exactly-numbered frames at 1fps, then loop over the sorted frame list calling `/app/bin/get_embedding <frame>` and appending `frame_XXXX.jpg, <output>` lines to `embeddings.csv` — capture the tool's stdout exactly, don't reformat or trim it beyond what's needed to fit the line format. For `serve.sh`: implement a TCP listener on `127.0.0.1:8888` that handles requests sequentially and doesn't exit after one connection — `socat TCP-LISTEN:8888,bind=127.0.0.1,reuseaddr,fork EXEC:lookup.sh` (with `fork` for repeat connections) or an `nc`-based accept loop both work; the handler should read the 4-digit input, strip the newline, look up the matching `frame_XXXX.jpg` row in `embeddings.csv` (e.g. `grep`/`awk`), and write back only the embedding portion after the comma plus a trailing newline, trimming any leading space. Run the ETL first so the CSV exists before starting the server, then launch the server in the background and confirm with `nc -z 127.0.0.1 8888` that it's actually listening before finishing.