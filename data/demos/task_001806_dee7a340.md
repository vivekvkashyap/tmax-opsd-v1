Work top-down through the stack: schema, then server, then client. Add `double weight = 3;` to the `DataPoint` message in `data_service.proto`, keeping field numbers stable for existing fields. Regenerate the C++ protobuf/gRPC stubs (via the CMake custom command or manually with `protoc --cpp_out --grpc_out`) before touching server.cpp. In `server.cpp`'s `ProcessData`, change `processed_value` to `request->value() * request->weight()`, and separately find the memory leak — look for a heap allocation (e.g. `new int[...]`) that's never `delete[]`d in that method; either remove the unnecessary allocation or free it, and confirm with `valgrind --leak-check=full` showing "definitely lost: 0 bytes". Build via CMake into `/home/user/backend/build/server`. Only after the server builds and runs cleanly, write `/home/user/client/client_v3.py` in Python 3 (generate stubs with `python3 -m grpc_tools.protoc`), sending 1000 sequential requests with id 1..1000, value=10.0, weight=2.5, timing the whole loop with `time.perf_counter()` to get average latency, verifying every `processed_value == 25.0`, and writing the two required lines to `/home/user/benchmark.log` in the exact specified format.
