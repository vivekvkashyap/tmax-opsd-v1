Treat the video decode and the guard logic as separate, independently testable pieces. For the video: extract frames (`ffmpeg -i /app/ci_telemetry.mp4 frames/frame_%04d.png`) and scan them with `zbarimg` (install via apt if missing) rather than assuming which frames hold QR codes — iterate all extracted frames, decode whichever contain a QR payload, and concatenate the decoded fragments in frame order to reconstruct the full `CRC_POLYNOMIAL=...;TEST_MODE=...` string; verify the reconstructed string parses cleanly before wiring it into the guard. For the guard script itself, implement two independent checks per project: (1) CRC32 checksum — for every `.py` file, strip the first line, compute CRC32 of the remainder (Python's `zlib.crc32` or an equivalent CLI/awk implementation) as hex, and compare to the value in the `# CHECKSUM: <hex>` header; any mismatch anywhere fails the whole project. (2) Import check — create a throwaway venv, `pip install -r requirements.txt` if present, then attempt to import every `.py` module (e.g. via `python -c "import module"` per file) and fail the project on any `ImportError` (including circular imports) or failed dependency resolution. Exit 0 only if both checks pass for every file; exit 1 otherwise. Validate against both `/app/corpus/clean/` and `/app/corpus/evil/` before finishing.
