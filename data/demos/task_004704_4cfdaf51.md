Work through this as a leakage-audit, not a rewrite-from-scratch: first split the raw (unscaled) DataFrame into train/test with `train_test_split(..., test_size=0.2, random_state=42)` *before* touching any scaler, so scaling parameters are only ever learned from the training fold. Build a custom sklearn-compatible transformer (`fit`/`transform`) that computes the correlation matrix on the training slice it receives, and for each pair with |corr| > 0.85 drops whichever column comes later in the original column order — do this fit-time only, never using test data statistics. Wire this transformer plus `StandardScaler` plus `Ridge` into one `sklearn.pipeline.Pipeline` so every bootstrap iteration refits feature-selection and scaling from scratch on that iteration's resampled training data (bootstrap via `X_train.sample(frac=1.0, replace=True, random_state=i)` for i in range(100), keeping y aligned by index) and always evaluates on the single untouched test set. Collect the 100 MSEs, compute mean/2.5th/97.5th percentiles with numpy, dump the JSON with the exact three keys requested, and separately fit one more pipeline instance on the full (non-bootstrapped) training set to save via `joblib.dump` for the pickle artifact. Create the artifacts directory first if missing.