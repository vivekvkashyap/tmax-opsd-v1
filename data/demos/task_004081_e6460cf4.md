Run `tesseract /app/wal_spec.png -` (try `--psm 6` if layout confuses it) to recover the exact binary WAL format — magic bytes, header length, per-record field order/widths/endianness — and re-read the OCR output carefully, since a misread byte value or width breaks every parse. Implement the C++ reader consuming stdin as a raw byte stream (`std::cin.read`/`fread` in binary mode, not line-based I/O): validate the magic+version header first, printing "INVALID FORMAT\n" and exiting 1 on mismatch; then loop reading one record at a time, checking for EOF/truncation *before* trusting any length-prefixed field (silently stop and print current state on truncation); treat unrecognized opcodes as an immediate stop-and-print too, per the spec's distinction between truncation and bad-opcode handling. Maintain state in a map keyed by the string key, applying ADD (insert/overwrite), DEL (erase if present), MOD (overwrite only if key exists), then print sorted "KEY:VALUE\n" lines. Compile with `g++ -O2 wal_tracker.cpp -o wal_tracker` and test against small hand-crafted binary streams (built with `printf`/`xxd`) covering truncation, bad opcodes, and repeated keys — it's graded by bit-exact equivalence against thousands of fuzzed streams.