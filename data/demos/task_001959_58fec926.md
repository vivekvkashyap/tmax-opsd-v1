Parse `SafeDirectory` from `config.ini` with `configparser`. Open the tar file's underlying file object, get its file descriptor with `.fileno()`, and call `fcntl.flock(fd, fcntl.LOCK_SH)` before you start iterating — do this explicitly, since the verifier checks for the actual `flock` call in your script. Stream members with `tarfile.open(fileobj=f, mode='r|')` (a streaming mode) and iterate without ever calling `extract`/`extractall`. For each member that is a symlink or hardlink, resolve where it would actually land: the link's own file lives at `SafeDirectory/<member.name>`, so its target must be resolved relative to `SafeDirectory/os.path.dirname(member.name)` (not relative to `SafeDirectory` itself) using `os.path.normpath(os.path.join(...))` for both absolute and relative `linkname` values. Flag it unsafe if the normalized resolved path does not start with `SafeDirectory` plus a path separator (avoid a naive `startswith` that would wrongly accept a sibling directory sharing a prefix, e.g. `/safe-dir-evil`). Write the flagged member *names* (not resolved paths), sorted alphabetically, one per line.
