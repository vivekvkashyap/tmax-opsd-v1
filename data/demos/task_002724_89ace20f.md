Load `/home/user/graph.ttl` with `rdflib.Graph().parse(..., format="turtle")` and write a SPARQL query rather than hand-rolling graph traversal in Python — it's less error-prone for this kind of aggregation. Select papers with `?paper ex:year ?y` filtered by `FILTER(?y >= 2010)`, then separately count in-degree via `?citer ex:cites ?paper` (note direction: the *citing* paper is the subject, the *cited* paper — whose in-degree you want — is the object), grouping by `?paper` and using `COUNT(?citer)`. Be careful that the in-degree count should include incoming edges from *any* paper (even ones with year < 2010, since it's the target's year filter that matters, not the source's) — don't accidentally filter citing papers by year too. Order by count descending then by URI ascending as a tiebreaker (`ORDER BY DESC(?count) ?paper`), `LIMIT 3`. Write results to `/home/user/top_papers.csv` with a `uri,in_degree` header, using the full URI string for each paper.
