Run `/app/start_services.sh` first and confirm both Postgres and Redis are reachable before writing any code. Scaffold with `cargo new backup_validator` at the required path and add `postgres`/`tokio-postgres`, `redis`, and `serde_json` crates. Parse the input file's JSON to extract `backup_id`, treating any JSON/schema error (missing or wrong field name) as an immediate REJECT. For graph traversal, write a recursive SQL query (`WITH RECURSIVE`) walking from the given id up through `parent_id` toward the root, but guard against cycles explicitly — e.g. track visited ids in the recursive CTE (an array column checked with `NOT id = ANY(path)`) or cap iteration depth and treat exceeding it as a cycle, since a naive recursive CTE with a true cycle in the data will hang or error rather than just misbehave. After traversal, the walk must have terminated at a node with `backup_type = 'FULL'`; anything else (dead end without FULL, or cycle detected) means REJECT. Separately query Redis for `manifest:<backup_id>`, parse its JSON value, and require `status == "AVAILABLE"` exactly. Only print `ACCEPT`/exit 0 if every check above passed; otherwise print `REJECT`/exit 1 — make sure error paths (DB connection failure, missing key, malformed JSON) also fall through to REJECT rather than panicking. Build with `cargo build --release` and iterate over both corpora directories checking exit codes before declaring done.
