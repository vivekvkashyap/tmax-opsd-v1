Start by diffing the Makefile against a stock cJSON Makefile (or just inspect the `CC =` line) — the perturbation is a single corrupted compiler variable; restore it to a real compiler (`gcc`/`cc`) before running `make`. Confirm `libcjson.a` actually gets produced (check `ls`/build output, don't assume). For the verifier, write `verifier.c` that: opens the file with standard C I/O, reads its full contents into a buffer, calls `cJSON_Parse`; on NULL parse result or fopen failure, print `REJECT` and `exit(1)` immediately. Otherwise recursively walk the parsed tree — for each JSON object, iterate its children and check if any key exactly equals `"exec_cmd"` (using `cJSON_HasObjectItem` or manual string compare), and recurse into object/array children regardless of nesting depth or position (inside arrays, deeply nested, etc). If found anywhere, `REJECT`/exit 1; otherwise `ACCEPT`/exit 0. Compile with `gcc -I/app/cJSON-1.7.17 verifier.c /app/cJSON-1.7.17/libcjson.a -o /home/user/verifier`. Test against both malformed and well-formed-but-forbidden JSON samples you construct yourself before trusting it.
