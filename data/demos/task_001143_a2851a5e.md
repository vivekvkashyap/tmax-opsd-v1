First transcribe `/app/experiment_dictation.wav` (e.g. via a speech-to-text tool/library available in your environment, or by listening/converting to text) to recover the spoken amino-acid names, then map each full name to its standard 1-letter code (only Methionine=M, Alanine=A, Serine=S, Glycine=G, Tyrosine=Y are relevant here) to build the sequence string — don't guess the sequence, derive it from the audio. Write the C++ engine to take that sequence as argv[1], compute E_orig by summing `|mass_i-mass_{i+1}|*(hydro_i+hydro_{i+1})` over adjacent pairs using the given (Mass, Hydrophobicity) table, then run 1,000,000 random permutations in an OpenMP-parallel loop (`#pragma omp parallel for` with a per-thread RNG to avoid data races, and a reduction or atomic counter for `perm_energy <= E_orig`), and print `p_value = count/1000000`. Compile with `g++ -fopenmp -O2`. Wrap the binary in a small HTTP server (Flask or `http.server`) listening on 127.0.0.1:8000 that on POST /analyze extracts `sequence` from the JSON body, invokes the compiled binary via subprocess, and returns its JSON stdout unchanged; start it backgrounded and verify with a local curl before finishing.