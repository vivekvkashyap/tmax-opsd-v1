Work through the three sub-problems independently before wiring them into one C program. First, extract the custom `.audit_config` ELF section: use `objdump -s -j .audit_config /home/user/app/legacy_endpoint` (via `popen()` from your C code, or parse the section with `readelf`/libelf directly) and pull out the raw bytes, then split the semicolon-delimited `AUTH_TOKEN=...;IDS_PATTERN=...` string on `;` and `=` to get the token and the regex pattern — do not hardcode either value, read them from the binary at runtime. Second, validate the cert chain by shelling out to `openssl verify -CAfile /home/user/app/ca.crt /home/user/app/server.crt` via `popen()` and checking whether its output/exit status indicates success, mapping that to VALID/INVALID. Third, compile the extracted `IDS_PATTERN` with `regcomp()` (POSIX extended regex, `REG_EXTENDED`) and use `regexec()` to test each line of `/home/user/app/traffic.log`, counting matches — remember the pattern is anchored/alternation-based so compile it exactly as extracted rather than transforming it. Finally `fprintf` the four-line report to `/home/user/audit_trail.txt` in the exact literal format specified, compile your source with gcc, and actually run the resulting binary so the report file exists on disk.