Treat this as: bring up the stack, drive load against it, sample metrics concurrently, then tear everything down cleanly — order matters for the cleanup check. Append `export CAPACITY_TARGET_PORT=8080` to `/home/user/.bashrc`. Start `target_app.py` in the background, then start a forwarder (e.g. `socat TCP-LISTEN:8080,fork,reuseaddr TCP:127.0.0.1:9090 &`) so port 8080 relays to 9090. In `capacity_planner.py`: read the port from `os.environ`, find the target PID via `pgrep -f target_app.py` (or scan `/proc`), spawn a background thread/loop that repeatedly reads `/proc/<pid>/status` (VmRSS) or uses `psutil.Process(pid).memory_info().rss` while the main loop fires 20 sequential `requests.get(...)` calls to `/compute`, timing each to compute average latency in ms, and track the peak RSS seen across all samples. Write the exact JSON keys (`total_requests`, `avg_latency_ms`, `peak_rss_bytes`) to the report path. Finally kill both the app process and the forwarder (e.g. `kill <pid>`, `pkill -f 'socat.*8080'`) and confirm with `pgrep`/`lsof` that nothing on 8080/9090 remains before finishing.
