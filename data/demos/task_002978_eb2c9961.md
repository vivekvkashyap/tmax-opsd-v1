Write a single `sqlite3` query using a self-join of `wire_transfers` against itself three times (aliases t1, t2, t3) matching `t1.receiver = t2.sender`, `t2.receiver = t3.sender`, `t3.receiver = t1.sender`, with `t1.timestamp < t2.timestamp AND t2.timestamp < t3.timestamp` to enforce chronological order, and `t1.sender, t2.sender, t3.sender` (equivalently the three accounts) pairwise distinct. To dedupe rotations of the same cycle, add a `WHERE t1.sender < t2.sender AND t1.sender < t3.sender` condition (or equivalently `t1.sender = MIN(...)`) so only the alphabetically-smallest starting account is emitted as `acct_A`. Compute `cycle_total` as the sum of the three transfer amounts, then wrap the whole query as a CTE/subquery and apply `RANK() OVER (ORDER BY cycle_total DESC)` as `risk_rank` in an outer SELECT. Export with `sqlite3 -header -csv financial_audit.db "<query>" > /home/user/circular_flows.csv`, and double check column order/naming matches exactly (`acct_A,acct_B,acct_C,cycle_total,risk_rank`).