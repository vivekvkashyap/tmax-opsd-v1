Write a Go program that processes `/home/user/raw_data.jsonl` line by line using `encoding/json` with a struct where `series` is `[]*float64` (or `[]interface{}`) so you can detect `null` entries distinctly from real values. For interpolation: scan the series, and for each maximal run of consecutive nulls bounded by known values `v_left` at index `i` and `v_right` at index `j`, fill each null at position `k` with `v_left + (v_right - v_left) * (k - i) / (j - i)` (linear interpolation evenly spaced across the gap) — since the first and last entries are guaranteed non-null, you never need to extrapolate at the edges. For the Levenshtein distance, convert both `text_a` and `text_b` to `[]rune` first (NOT operate on the raw string/bytes), then run the standard O(n*m) dynamic-programming edit-distance algorithm over the rune slices — this is the critical detail since multi-byte UTF-8 characters (emoji, CJK, Cyrillic) would give wrong distances if you iterated over bytes or used `len(string)` instead of `len([]rune(string))`. Marshal each output record (ensuring `series` is now a plain `[]float64` with no nulls) as one JSON line to `/home/user/processed_data.jsonl`, preserving `id`, `text_a`, `text_b` unchanged and adding `text_distance`. Compile/run with `go run process.go` and spot-check a couple of output lines against manual interpolation arithmetic before finishing.