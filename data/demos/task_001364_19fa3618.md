Fill in the C++ TODOs one at a time and test each independently before wiring them together: use `std::filesystem::recursive_directory_iterator` over `/home/user/data`, summing `fs::file_size(entry)` for regular files only (skip directories/symlinks), and print the total standalone first to sanity check it against `du -sb /home/user/data`. For the socket code, open a TCP client socket (`socket`/`connect` to 127.0.0.1:9090), and explicitly check the `connect()` return value — if it fails, `return 1` (or exit non-zero) immediately rather than proceeding; then `send()` the exact string `"CAPACITY:" + std::to_string(total_size) + "\n"`. The core bug in supervisor.sh is a missing readiness wait: instead of launching the C++ binary right after backgrounding the Python server, poll for the port with something like `until bash -c "echo > /dev/tcp/127.0.0.1/9090" 2>/dev/null; do sleep 0.2; done` before starting `/home/user/capacity_planner`, and wrap the binary launch in a retry loop (track exit status, relaunch up to 3 times on nonzero exit). For ci_pipeline.sh, compile, run the supervisor in the background, then `tail -f`/poll `server.log` in a loop with a 15-second deadline, printing SUCCESS/TIMEOUT and killing child processes (e.g. via recorded PIDs or `pkill -P`) before exiting.