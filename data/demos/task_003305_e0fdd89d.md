Since real JSON parsers are off-limits, treat each line as text and extract fields with targeted regex via `grep -oP` or `sed`/`awk` (e.g. `grep -oP '"config_key":\s*"\K[^"]+'` and similarly for `config_value`), rather than trying to fully parse JSON structure. Filter to lines where the extracted `config_key` starts with `network.` before further processing. To decode `\uXXXX` escapes without a real JSON library, use `sed` with a loop or `awk`'s `printf "%c"` conversion on the hex codepoint (recognizing common ones like `:`→`:`, `/`→`/`, `=`→`=`, `,`→`,`) — write a small awk/sed routine that converts any `\uXXXX` pattern generically by hex-to-char conversion rather than hardcoding only the examples shown, since other files may use other codes. Build each `key=value` string, pipe it through `md5sum`, and capture the hash. For parallelism use `xargs -P` or GNU `parallel` across the `.jsonl` files, but do deduplication and final sort as one combined step at the end (e.g. `sort -u` on the `key=value` line before hashing, or dedupe post-hash) so parallel workers don't produce duplicate hash lines; finally sort the output by hash column alphabetically before writing.
