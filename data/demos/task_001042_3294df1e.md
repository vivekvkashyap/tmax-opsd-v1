First recover the secret: run `tesseract /app/admin_badge.png stdout` (or equivalent OCR) to extract the printed base64 string, trim whitespace/newlines from the OCR output carefully (OCR can add stray characters), then base64-decode it in Python to get the plaintext admin key — verify the decode succeeds cleanly and looks like a plausible token before hardcoding it anywhere. Then write a Python TCP server bound to `localhost:9090` using `socket`/`socketserver` (or asyncio) that accepts a connection, reads until it sees a `\n` delimiter, strips the newline, base64-decodes the payload (handling decode errors as a non-match rather than crashing), and compares the decoded bytes to your recovered plaintext key using an exact string comparison — respond `ACCESS_GRANTED\n` on match, `ACCESS_DENIED\n` otherwise, then close that connection while continuing to accept new ones in a loop (a per-connection handler or thread, not exiting after one request). Test with `nc localhost 9090` or a small Python client sending both a correct and incorrect base64 payload before considering it done.