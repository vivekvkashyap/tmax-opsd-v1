Build this as a validate-sort-sample-enrich-write pipeline, and be strict about type checks, not just presence checks: a record is invalid if `id` is a JSON string that merely looks numeric (e.g. `"2"`), if `label` isn't exactly `POSITIVE` or `NEGATIVE` (reject `NEUTRAL` and anything else), or if `text` isn't a JSON string type (a record with a numeric `text` field must be dropped even though the field exists). Use `jq` to filter with explicit type checks (`.id|type=="number"`, `.text|type=="string"`, `.label|IN("POSITIVE","NEGATIVE")`), then `sort_by(.id)` and take the first 5. For each surviving record, shell out to `python3 /opt/embedder.py "$text"` passing the text exactly as-is (watch quoting/escaping for texts with special characters) and parse its JSON array output into the `embedding` field of your output object, preserving field order (`id`, `text`, `label`, `embedding`) and compact JSON formatting per line. Make the script executable and actually run it end-to-end to produce `/home/user/training_data.jsonl` before considering the task done — don't just eyeball the logic.
