First transcribe /app/project_dictation.wav (e.g. with `whisper` if available, or another local STT tool) to recover the XOR passphrase and the file-organization rules spoken in it — do not guess these, listen to/transcribe the actual audio. Then concatenate the chunks in numeric order: `cat /home/user/project_parts/archive.dat.part1 archive.dat.part2 ... > combined.dat` (use a glob with proper numeric sort, e.g. `ls archive.dat.part* | sort -V`, since lexicographic sort breaks past part9). XOR-decrypt byte-by-byte in Python by repeating the recovered passphrase bytes across the ciphertext (`bytes(b ^ key[i % len(key)] for i, b in enumerate(data))`), then verify the result starts with the gzip magic bytes `1f 8b` before proceeding — if not, recheck the transcribed passphrase. Extract with `tarfile` into /home/user/extracted_project/. Apply the organization rules exactly as transcribed (move by extension into named subfolders per the instructions, leave the rest at the root) into /home/user/organized_project/. Finally serve it with something like `python3 -m http.server 8080 --bind 127.0.0.1 --directory /home/user/organized_project/`, backgrounded with `&` or `nohup ... &` so it stays up for the HTTP checks.
