First recover the two parameters from the image before writing any classifier logic: run `tesseract /app/model_spec.png stdout` (or convert/preprocess with `convert`/`imagemagick` first if OCR is noisy) to extract the target mean degree and max KS threshold text, and parse those two numeric values programmatically rather than eyeballing and hardcoding a possibly-misread number — sanity check them by printing what was parsed. Then scaffold the Rust binary (`cargo new /home/user/graph_filter`) so it takes a directory argument, iterates `.txt` files, and for each: parse the edge list (space-separated integer pairs, undirected, N=100 nodes) into a degree count array, form the empirical CDF of degrees over 0..N, and build the theoretical Poisson(lambda=parsed_mean) CDF over the same support (compute Poisson PMF via `e^-λ λ^k / k!`, careful with floating overflow for larger k — use log-space or iterative accumulation). Compute the KS statistic as the max absolute difference between the two CDFs at every integer degree value, then print `ACCEPT filename.txt` if KS <= parsed threshold else `REJECT filename.txt`. Validate against both provided corpora locally with `cargo run --release -- <dir>` before considering it done, since the grader repeats that exact invocation on hidden data from the same distributions.