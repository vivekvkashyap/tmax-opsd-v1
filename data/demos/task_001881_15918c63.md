This is a fuzz-equivalence porting task: your compiled `/home/user/bin/cleaner` must match a hidden reference implementation byte-for-byte on arbitrary stdin, including empty input and up to 1MB of random/edge-case bytes (all zeros, all ones). Approach:
1. Read `/home/user/src/audio_cleaner.c` carefully and identify every source of undefined behavior — uninitialized pointers/variables, off-by-one loop bounds, unchecked buffer writes, missing bounds checks on any moving-average/windowed filter, and unfreed allocations. Fix each while preserving the algorithm's intended arithmetic exactly (don't just silence a crash — get the same output as an already-correct build would produce for all sizes, including tiny/zero-length inputs).
2. Build with a Makefile producing exactly `/home/user/bin/cleaner`, using `-O2 -Wall`; treat any warnings as bugs to fix, not suppress.
3. Test extensively yourself with varied byte streams (empty, all zero, all 0xFF, random, sizes near your buffer boundaries) piped through the binary before trusting it — this is what the automated fuzzer will do.
4. Write `ci_build.sh` to build, verify the binary exists, and smoke-test it against `/dev/urandom`.
5. For the transcription step, run your cleaner on `/app/voicemail.wav`, then use an available speech-to-text tool (e.g. whisper if installed) to transcribe the cleaned audio, and write the lowercase, punctuation-stripped transcript to `/home/user/transcript.txt` — listen to/inspect the actual recovered audio rather than guessing its content.
