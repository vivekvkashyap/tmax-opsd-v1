Start by trying `make` and reading the linker error — it will name a missing symbol from a library version mismatch. Inspect `libs/` (`ls libs/*.so`) and the `.cpp` source's `extern "C"` declarations to see which library version actually exports the function the code calls, then fix the `-l` flag (or `LDFLAGS`) in the Makefile to link the matching `.so`. Rebuild, run `./sim` to produce `output.csv`, then write a short script (Python/awk) that reads both CSVs row by row and reports the first iteration where `abs(output.value - baseline.value) > 0.01` — don't guess this number, compute it directly from the buggy run's actual output. Only then edit `simulate.cpp`: search for any `float` cast or `float` variable in the compute loop and replace it with `double` so no precision is lost between steps, since single-precision rounding accumulates over iterative multiplication. Recompile, rerun, and diff the new `output.csv` against `baseline.csv` yourself (max abs diff across all rows) to confirm it's now under 1e-5 before writing `diagnostic.log` with the divergence iteration you measured and the exact `.so` filename you linked.
