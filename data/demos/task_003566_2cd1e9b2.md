The core risk here is Rust/polars silently upcasting an integer column to float when it contains nulls — guard against that explicitly rather than trusting the default join/read behavior. Initialize the project with `cargo new pipeline` inside `/home/user/pipeline` and add the `polars` crate (with `csv`, `lazy` features as needed) to `Cargo.toml`. Read both CSVs with `CsvReader`, join on `user_id` (inner or left join, then filter), and immediately after dropping null-label rows (`df.drop_nulls(Some(&["label"]))` or a filter on non-null), explicitly cast the `label` column to `Int32` (e.g. `.with_column(col("label").cast(DataType::Int32))` or `Series::cast`) so it can never remain `Float64` from the earlier nullable read — verify the resulting dtype with a debug print if unsure. Filter rows with `feature_a > 0.5`, then count how many surviving rows have `label == 1` (successes) vs `label == 0` (failures) — a simple `filter().height()` on each is enough, no polars aggregation trickery required. Apply the Beta(1,1) conjugate update: posterior alpha = 1 + successes, posterior beta = 1 + failures. Serialize `{"alpha": ..., "beta": ...}` as integers (not floats) to `/home/user/posterior.json` — use `serde_json` or hand-build the string — then `cargo run` to generate the file and confirm it's valid, integer-typed JSON.