Treat this as a three-part investigation: characterize the oracle, bisect the regression, then fix and harden the reimplementation. First, run `/app/math_oracle` for several small integers (e.g. 0-10) to observe the true output sequence and infer the recurrence relation it implements — do not assume a specific known sequence without confirming against the oracle's actual outputs. Next, in `/home/user/math_server`, use `git bisect start`, mark a known-good early commit and the broken `HEAD`, then at each bisect step build/run the candidate commit's logic against a couple of oracle-matched inputs (e.g. n=5, n=10) to mark good/bad — this pins down exactly which commit changed the formula, and its full hash goes in `/home/user/bad_commit.txt`. Separately, inspect the current `server.cpp` for the crash: look for a recursive implementation missing or mishandling its base case(s) (check both `n==0` and `n==1` boundaries) causing infinite recursion/stack overflow — prefer converting to an iterative loop for both correctness and performance. Finally fix the formula to match the oracle-observed recurrence, rebuild, and start the TCP server on 127.0.0.1:8080, testing with `nc` or a raw socket client that it responds correctly and persists across multiple queries on one connection.
