Build this as a pipeline and check each stage's intermediate output before moving on. In C++, open an in-memory SQLite DB (`sqlite3_open(":memory:", &db)`), CREATE TABLE, then read the CSV line-by-line (split on commas, watch for the exact column order given) and INSERT each row via a prepared statement. Write the window-function query directly in SQL — `SUM(amount) OVER (PARTITION BY user_id ORDER BY timestamp)` for `running_total`, and `ROW_NUMBER() OVER (PARTITION BY user_id, category ORDER BY amount DESC)` for `category_rank` — rather than reimplementing the window logic manually in C++, since SQLite supports these natively and it's far less error-prone. Iterate the result set with `sqlite3_step`, and as you go, accumulate per-`user_id` state: track the max `running_total` seen (this is `total_spent`, not a re-summed total) and collect distinct categories where `category_rank == 1`. Sort users alphabetically and sort each user's `top_categories` list alphabetically before serializing. Use a JSON library (e.g. vendor `nlohmann/json` as a single header) to emit exact key names and structure; validate your output against your own hand-computed expectations for the given CSV before trusting the pipeline end-to-end.