Read `math_service.py` and `traffic_gen.py` together, then actually run the server and fire the traffic generator at it first to observe the real failures before touching code — don't fix blind. Address each bug at its root: (1) before calling `float(v)`, strip thousands separators (e.g. `str(v).replace(',', '')`) so values like `"1,234.56"` parse correctly; (2) replace the naive `sum_sq/n - mean^2` variance formula (which suffers catastrophic cancellation for large-magnitude/low-variance data and can go slightly negative, breaking `math.sqrt`) with a numerically stable incremental algorithm such as Welford's method (track count, mean, and M2 incrementally per value) — and still clamp/guard against tiny negative variance from floating-point noise before taking the square root; (3) protect all reads/writes to the shared global `state` dict with a `threading.Lock` (acquire around the entire read-modify-write critical section, not just individual statements) so concurrent requests can't interleave and corrupt the running totals. Save the corrected server as `/home/user/fixed_math_service.py`, then re-run `traffic_gen.py`-style concurrent and edge-case requests against it to confirm all responses are 200 with sane standard deviations before finishing.