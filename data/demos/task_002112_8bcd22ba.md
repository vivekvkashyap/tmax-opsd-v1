Split this into schema/index design, C+SQLite querying, and formatted output. First write the indexing SQL — `CREATE INDEX` statements on `communications(sender_id)` and `communications(receiver_id)` (composite indexes are fine too) — into `/home/user/indexes.sql`, and actually apply them to `/home/user/audit.db` so the C program benefits. In `audit_analyzer.c`, open the DB with `sqlite3_open`, and compute out-degree as *unique* receivers per sender — use `SELECT sender_id, COUNT(DISTINCT receiver_id), SUM(byte_size) FROM communications GROUP BY sender_id` (counting distinct, not total, rows sent is the key correctness point) joined against `employees` filtered to `department = 'Contractor'`, then filter in your query or in C for out-degree `> 3`. Iterate results with `sqlite3_step`/`sqlite3_column_*`, checking each `sqlite3_*` call's return code and calling `sqlite3_finalize`/`sqlite3_close` to handle errors gracefully rather than crashing. Compile with `gcc ... -lsqlite3`, run it, and write the flagged rows as exact `EmployeeID,Name,OutDegree,TotalBytesSent` CSV lines to `flagged_contractors.txt`, plus the two-line count/max summary to `audit_summary.txt` in the exact specified wording.