Start by trying `make` inside `/app/libloc-0.1.0` and reading the failure — vendored build breakages here are typically a wrong compiler name in the Makefile's `CC=` variable; inspect the Makefile, fix `CC` to point at a real compiler on the system (e.g. `gcc`), then confirm `make` produces `libloc.a` and that `loc.h` declares the whitespace-normalization function you'll link against. For the C ETL tool (`localize.c`), load base_en.csv and updates_fr.csv as simple Key,String CSV rows (split on first comma), and parse tm_fr.txt with POSIX `regcomp`/`regexec` against a pattern matching `\[\[([^]]+)\]\] === \{\{([^}]+)\}\}` to extract key/translation pairs into a lookup table. Run every loaded string through the library's whitespace-normalize function so multiple spaces collapse to one and leading/trailing spaces are stripped — this matters because base_en.csv and updates_fr.csv are deliberately padded with irregular spacing. For each base key: use updates_fr.csv's translation if present, else fall back to the TM lookup, else leave empty; write `Key,Translation` rows to final_fr.csv. Then write `run_etl.sh` to compile-if-needed and run the C binary, and add a crontab line to `cron.txt` scheduling 2:00 AM Mon–Fri (`0 2 * * 1-5 /home/user/run_etl.sh`). Validate your merge logic by hand-tracing a couple of keys against the input files before trusting the accuracy metric.
