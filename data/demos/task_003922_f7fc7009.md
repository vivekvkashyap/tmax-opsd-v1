Work through the three bugs in order, verifying each fix builds/runs before moving to the next. (1) CGO linker error: check decode.c's function name against what decode.h declares and what extractor.go's `import "C"` block calls (`C.xor_decode(...)`) — a typo'd C function name (e.g. an extra digit or letter swapped in) will compile decode.c fine standalone but fail to link when Go's cgo tries to resolve the symbol; grep both files for the exact identifier and make them match. (2) Timeline reconstruction: read how entries are sorted before concatenating payload chunks — sorting by an unrelated field (like payload string length) instead of the log timestamp will scramble chunk order; fix the `sort.Slice` comparator to parse and compare the actual timestamp field chronologically. (3) After decoding, the assertion checks the first decoded byte against an expected signature character — if it panics, your timeline/XOR key logic is still wrong upstream, not the assertion itself; fix root causes rather than removing the check. Once `go build` succeeds and the assertion passes, run the binary to produce payload.bin, then `sha256sum payload.bin` and write only the lowercase hex digest (strip the filename/whitespace) to flag.txt.