Get the two services running and correctly wired before implementing protocol logic in depth. Fix `/app/nginx.conf`'s `proxy_pass` to point at `127.0.0.1:9000` (it's currently misdirected) and start nginx with that config in the background. Write `c2_server.py` so it runs two listeners concurrently — e.g. spawn the raw TCP server on 9001 in a background thread and run the HTTP server (Flask, or `http.server`) on 9000 in the main thread, or use `socketserver`/`asyncio` for both. For `/analyze_payload`, check the `filename` field for `../` or `..\` substrings and respond with the CWE-22 JSON shape only when found, otherwise the safe response — match the exact key sets in each case. For `/verify_integrity`, read the file at `filepath`, compute `hashlib.sha256(...).hexdigest()`, compare against `expected_sha256`, and return 404 if the file is missing. For the TCP log receiver, parse each newline-delimited line with a regex matching `[<timestamp>] <ip> <action> <target>`, and append one JSON object per line (JSONL, not a JSON array) to `/app/parsed_logs.json`, using `open(..., 'a')` so lines accumulate across connections.
