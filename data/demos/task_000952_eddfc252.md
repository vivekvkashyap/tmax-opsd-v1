Work top-down through the dependency chain: OCR the image first since later logic depends on it, then fix the C build, then the Go import cycle, then wire it together. Run `tesseract /app/version_rules.png stdout` (or similar) and read the extracted text carefully to derive the exact semver rule — it will describe a blocked range (e.g., versions below some floor and pre-release versions at/above some ceiling); implement this with a real semver comparator (major/minor/patch numeric comparison, treating any pre-release suffix as ordering before its base release) rather than naive string comparison. For the C library, diagnose the Makefile failure (missing `-fPIC -shared` for a `.so`, or a bad include/lib path) and fix it minimally; verify with `nm`/`ldd` that `libscanner.so` exports the expected symbol and is discoverable (copy to a standard lib dir or set `LD_LIBRARY_PATH` in your build/run scripts). For the Go cycle, trace the actual import statements in `config.go`/`scanner.go`, then break the cycle by introducing a small interface package or moving the shared type out — don't just delete functionality. Implement `IsAllowed` using your OCR'd rule, `go build` the CLI, then run it against both corpora and diff the resulting classification maps against the actual directory listing before finishing.