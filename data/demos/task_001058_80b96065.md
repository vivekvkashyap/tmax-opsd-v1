Work through the five pieces in dependency order so each stage is independently testable. First, transcribe /app/audio_notes.wav: convert it to a speech-recognition-friendly format with ffmpeg if needed (e.g. mono 16kHz WAV), then run a speech-to-text tool (e.g. Python `speech_recognition` with a local/offline recognizer, or `whisper` if available) and parse the spoken numbers for mass, damping, and stiffness out of the transcript text — don't guess the values, derive them from what the audio actually says. Compile mc_sampler.c with `gcc -shared -fPIC -o sampler.so mc_sampler.c -lm` and call it from Python via `ctypes.CDLL`, passing a seed and preallocated numpy buffer. Replace the naive `sum()` reduction in simulate.py with `math.fsum()` (or manual Kahan summation) so total energy is bit-reproducible regardless of chunking/order. Use `scipy.optimize.fsolve` to solve `k*x**3 + c*x - F_avg = 0` for equilibrium displacement, seeding fsolve with a sane initial guess like `F_avg/c`. Finally wrap it all in a Flask/FastAPI POST /simulate endpoint on 127.0.0.1:8080 that checks the exact bearer token, runs the sampler+fsum+fsolve pipeline per request, and returns the JSON response — start it as a background process so it stays reachable for external verification.