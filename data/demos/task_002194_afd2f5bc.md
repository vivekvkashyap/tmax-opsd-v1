Break this into three independently verifiable stages and test each before wiring them together. (1) Video->initial state: use ffmpeg to pull frame 1 of the mp4 as a grayscale PNG/PGM, then write a small script to read pixel values and map 0-255 to 0.0-1.0, saving a 64x64 space-separated float grid; sanity-check the file has exactly 64 lines of 64 numbers. (2) C simulation: implement the standard 2D explicit finite-difference heat equation update with periodic boundary conditions (wrap indices mod 64 instead of clamping). The critical fix for the "diverges to NaN" bug is the time-step: an explicit scheme is only stable when dt does not exceed the CFL bound dx^2*dy^2 / (2*alpha*(dx^2+dy^2)) — derive this bound from alpha=0.2, dx=dy=1.0 and pick a dt comfortably under it (e.g. via a stability-margin fraction), then step forward in a loop from t=0 to the requested T, printing the final grid sum. Test the binary standalone first (./simulate <T>) and confirm the printed sum stays close to the initial sum regardless of T, since periodic BCs conserve mass for this PDE — if it doesn't, your dt is still too large or your Laplacian stencil/indexing is wrong. (3) Only after the C binary is verified stable, wrap it in a minimal Flask/FastAPI app on 0.0.0.0:8080 that shells out to the compiled binary for POST /simulate and returns its stdout as JSON; run it in the background and confirm with a local curl before finishing.