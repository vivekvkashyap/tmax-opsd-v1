Work the three steps independently, then wire them into one C server. First, transcribe `/app/voicemail.wav` — play/inspect it (e.g. `ffmpeg`/`sox` to check format, then a speech-to-text tool such as `whisper` or any available offline STT) to recover the single NATO phonetic-alphabet code word; verify by re-listening rather than guessing. Second, iterate `/app/inventory.csv`, and for each archive test integrity with the right tool per extension (`unzip -t` for `.zip`, `tar -tzf` or `gzip -t` for `.tar.gz`) — corrupted files will fail these checks, not just "look wrong". For each archive that passes, extract `manifest.xml`, convert it from UTF-16LE to UTF-8 with `iconv -f UTF-16LE -t UTF-8`, then parse out the integer inside `<bytes>...</bytes>` (a simple regex or XML parser) and accumulate the sum only over valid archives. Third, write a small C TCP server (socket/bind/listen/accept loop, handling one connection at a time or forking) that reads a newline-terminated command and replies exactly per spec — cache the three computed values (auth word uppercased, sorted corrupted filenames comma-joined, byte total) at startup so each request is answered from precomputed state. Compile with gcc, then launch it detached (e.g. `nohup ./server &`) so it keeps listening on 127.0.0.1:8888 after your shell session ends.
