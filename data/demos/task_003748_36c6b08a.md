Work through this as four independent fixes, verifying each with a direct test before moving on. (1) SSH auth: edit `/home/user/sshd_config` to set `PubkeyAuthentication yes` and an explicit `AuthorizedKeysFile /home/user/.ssh/authorized_keys` (check current permissions on `.ssh` and the key files too — sshd silently rejects overly-permissive dirs/keys). (2) Git hook: create an executable `post-receive` in the bare repo's `hooks/` dir that runs a `curl -X POST -d "event=pushed" $METRICS_URL/push`, testing it manually first with a fake push. (3) Env propagation: since `post-receive` runs in a non-interactive SSH session, `.bashrc` won't be sourced — instead set `PermitUserEnvironment yes` in sshd_config and define `METRICS_URL` in `/home/user/.ssh/environment` (or export it in the hook script directly). (4) Routing: add the missing route/address on the dummy interface (e.g. `ip addr add ... dev metric0` or an `ip route add`) so `10.0.0.5:8080` becomes reachable — verify with `curl` before considering it fixed. Finally restart sshd with the corrected config and do an end-to-end test push yourself to confirm the metrics endpoint receives the POST.
