Explore the vendored Rust crate at `/app/linreg_pipeline`: locate the struct(s) used with `serde` to deserialize `/app/data/features.csv`, paying close attention to any integer field. A common silent-corruption bug is an integer field typed as `i32` (or `Option<i32>`) annotated with `#[serde(default)]` (or a manual default), which makes serde substitute `0` for empty CSV cells instead of erroring or leaving them absent — this quietly injects fake zeros into the regression rather than treating them as missing. Fix this by changing the field to a plain `Option<i32>` without a default so `serde` naturally maps empty strings to `None`, then in the join/preprocessing code explicitly filter out rows where that field is `None` (don't just `.unwrap_or(0)` downstream, which reintroduces the same bug). After the fix, rebuild the pipeline (`cargo build --release` or similar) and run it end-to-end so it joins features and targets, trains the linear regression on the cleaned rows, and writes predictions for the held-out test rows to `/app/linreg_pipeline/predictions.csv`. Validate your own MSE informally against a held-out portion of `/app/data/targets.csv` before considering it done, since the task requires MSE <= 0.5.