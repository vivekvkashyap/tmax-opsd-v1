Do the service plumbing first since the pipeline depends on it. In `/home/user/services/api/.env`, set `REDIS_URL` to point at the Redis instance on port 6380 (not the Flask default), and in `/home/user/services/nginx/nginx.conf` set `proxy_pass` to the Flask API's actual host:port (127.0.0.1:5000). Run `start_stack.sh`, then confirm with `curl http://localhost:8080/api/features` that you get a JSON array back before writing any Python — debugging the pipeline against a broken proxy wastes time. In the pipeline script: pull the JSON into a DataFrame, read the historical parquet, inner-join on `user_id`. Impute `support_tickets` NaNs with the column median (fit before or after the join doesn't matter here since it's the same column). Drop `transaction_volume` rows beyond 3 standard deviations from its own mean — compute mean/std on the joined column, not a subset. Drop `click_rate` entirely, then standardize the remaining 3 features (e.g. StandardScaler) before fitting PCA to exactly 2 components — never run PCA on unstandardized features when columns have different scales, since that skews component loadings. Assemble the final `pca_1, pca_2, churn` DataFrame in that exact column order and write it to model_input.parquet, then sanity-check by running a quick logistic regression yourself to see it's meaningfully separable.