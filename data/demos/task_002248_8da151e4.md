Do not guess the filename rules — extract them from the image first. Run `tesseract /app/legacy_rule.png stdout` (try adding `--psm 6` if default segmentation garbles it) and read the OCR'd text carefully, since OCR can misread numbers/words; sanity-check any numeric boundary it reports by re-reading the image description context, and treat the recovered rule as the sole source of truth for your sanitizer's logic — don't assume a "common sense" filename policy. Once you have the exact boundary condition and forbidden-character rule, write `/home/user/sanitizer.py` to: strip the input to its basename only (use `os.path.basename`), then check the recovered length boundary (get the comparison operator, strict vs. inclusive, right) and the recovered forbidden-character rule, exiting 0 only if both pass and 1 otherwise. Before finishing, test your script manually against edge cases at the exact boundary (e.g., a filename exactly at the limit, one character over, one under) and against filenames containing the forbidden character, since the hidden adversarial corpus will specifically probe those boundaries — an off-by-one here is the most common failure mode.