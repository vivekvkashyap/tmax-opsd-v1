Break this into three verifiable pieces before writing Rust. First, transcribe `/app/handover.wav` (ffmpeg plus a local speech-to-text tool) purely to sanity-check your own restore_planner manually — the verifier fuzzes with random db/timestamp pairs, so don't hardcode anything from the audio into the program. Second, explore `/app/catalog.db`'s schema and data with `sqlite3` (`.schema`, sample SELECTs) to understand the dependency DAG and backup rows before coding. Third, implement `/home/user/restore_planner` in Rust (e.g. `rusqlite`) to: (a) recursively resolve all upstream dependencies of the target db via the `dependencies` table, collecting the full closure; (b) topologically sort that set so dependencies precede dependents, tie-breaking alphabetically by db name; (c) for each db in that order, find the latest 'F' backup with timestamp<=target, then all 'I' backups for that db with timestamp in (F.timestamp, target], ordered ascending, and emit their filepaths. Test with several handcrafted (db, timestamp) pairs against your own manual SQL trace before finishing, since the verifier bit-compares stdout against a hidden oracle across 100 fuzz inputs.