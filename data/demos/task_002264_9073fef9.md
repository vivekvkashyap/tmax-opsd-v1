Solve the video-analysis and simulator halves independently, then combine. For the video, use OpenCV to read each frame of `/app/particle.mp4`, threshold pixels with intensity > 128, and take the mean column index of matching pixels as that frame's x-coordinate — do this for every frame, not just a sample. Compute frame-to-frame differences (`dx_t = x_t - x_{t-1}`), then the *sample* variance (divide by N-1, not N) of those differences, and round to the nearest integer to get K — print/inspect this intermediate value yourself rather than assuming a round number. For the simulator, implement the LCG update exactly as specified (`state = (1103515245*state + 12345) % 2**31`), and for each of the K microscopic collisions per macroscopic step, advance the LCG once, compute `(V % 3) - 1`, and add it to position — get the order of operations (advance-then-use vs use-then-advance) and the modulo arithmetic exactly right, since a fuzz-equivalence check against a hidden oracle over many seeds means even a single off-by-one in state advancement will fail every case. Use Python's arbitrary-precision ints so modulo never wraps incorrectly.
