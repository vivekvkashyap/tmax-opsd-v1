Write filter.cpp using Eigen: load the 64x64 matrix by reading whitespace-separated floats into an `Eigen::MatrixXf`/`MatrixXd`, run `Eigen::JacobiSVD` (or BDCSVD) to get singular values, compute condition number as max(singular values)/min(singular values), and print exactly "REJECT" if that ratio exceeds 10000 or the smallest singular value is near machine-epsilon-relative-zero, else "ACCEPT" — print nothing else to stdout so exact-match checks pass. Compile with `g++ -O2 -I/path/to/eigen filter.cpp -o /home/user/filter`, then validate by looping over every file in /app/corpus/clean and /app/corpus/evil, running the filter on each, and confirming 100% accuracy before moving on — fix any threshold/epsilon issues here first since the video step depends on the same binary being correct. For frame processing, use ffmpeg to extract at 10fps and force 64x64 grayscale in one pass (`-vf fps=10,scale=64:64,format=gray`), then dump each frame's raw grayscale bytes to a space-separated text file of floats (0-255) — a Python or awk helper can convert raw pgm/bytes to space-separated text. Run the compiled filter over every frame file in chronological order, log 1-based frame numbers of REJECTs sorted ascending to rejected_frames.txt, and separately record each frame's condition number (have the C++ program optionally print it to stderr, or compute it again in Python) to drive the matplotlib line plot saved as cond_plot.png.