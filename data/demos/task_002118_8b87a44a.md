Treat the binary purely as a black-box data source — run `/app/oracle_sim 500000`, capture stdout, and parse each line as a float into a numpy array; don't try to disassemble or guess its internal RNG algorithm, since only the empirical sample distribution matters for the KDE approach the task asks for. Use `scipy.stats.gaussian_kde(samples, bw_method="scott")` to build the estimator (this is what "Scott's Rule" refers to — it's the default bw_method, but pass it explicitly for clarity). Then integrate with `scipy.integrate.quad(lambda x: kde.evaluate(x)[0] * x**2 * math.cos(x), 1.0, 4.0)` and write just the resulting float (e.g. `str(result)`) to `/home/user/integral_result.txt` with no extra text. Before running the full pipeline, sanity-check by printing summary stats (mean/std) of the sampled array to make sure the subprocess call and parsing actually worked, and confirm `numpy`/`scipy` are installed (pip install if missing) since the environment may be minimal. Wrap the subprocess call with `subprocess.run([...], capture_output=True, text=True, check=True)` and handle non-numeric lines defensively.