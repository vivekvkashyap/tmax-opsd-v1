Treat this as a log-correlation forensics task before touching any fix. Cross-reference `/home/user/logs/app.log`, `/home/user/logs/syslog`, and `/home/user/logs/archiver.log` chronologically around 14:00-14:10: look for an app.log entry noting a filename generation anomaly (e.g. a WARN about failed title/name extraction falling back to something unexpected) immediately followed by a "Generated: <value>" line whose value looks like a shell glob/wildcard rather than a real filename, and confirm the corresponding archiver.log run shows that same value being "archived." Write that exact literal string, and nothing else, on one line to `/home/user/root_cause.txt`. For the MRE, write a small bash script that sets a variable to that same string and reproduces the destructive expansion pattern found in `archiver.sh` (unquoted `$file` inside a `for file in $(cat queue)`-style loop feeding into `rm`), so `bash -x mre.sh` visibly shows the glob expanding to every file in a directory instead of the literal string. Then fix `/home/user/archiver.sh` itself: replace the `for file in $(cat "$QUEUE")` loop with `while IFS= read -r file; do ... done < "$QUEUE"`, and quote every use of `"$PROCESSED_DIR/$file"`/`"$ARCHIVE_DIR/"` in `cp`/`rm` calls so spaces, globs, and leading dashes in filenames can't be reinterpreted by the shell. Keep `chmod +x` on the script.