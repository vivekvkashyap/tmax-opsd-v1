This is a two-phase task: recover the shared decoding key empirically, then build a classifier from the decoded corpora — don't try to reverse-engineer the stripped binary's internals directly.

1. Brute-force the password: since decoded clean payloads must start with `AUDIT_ENTRY:`, write a small script that tries every 5-character lowercase alphabetical combination (26^5, feasible), calling `/app/legacy_decoder <candidate> <clean_file>` for a sample clean file each time, and stop when the output starts with the known prefix. Confirm it consistently decodes all clean files correctly.
2. Once you have the password, decode every file in `/app/corpus/clean/` and `/app/corpus/evil/` (via `popen`/`system` calls to the binary, or by reimplementing its decode algorithm once you've inferred it from consistent behavior) and diff the decoded outputs to find what pattern(s) appear only in evil payloads (e.g. injected strings/signatures).
3. Write `/home/user/audit_filter.c` to take one encoded-file path, decode it, scan for the signature(s) you found, and `exit(0)` if clean or `exit(1)` if any matches — compile to `/home/user/audit_filter`. Since the verifier uses an unseen holdout corpus, make sure your signature detection generalizes (e.g. match the pattern class, not one exact random JSON instance) rather than overfitting to the 100 given samples.
4. Generate `/home/user/audit_trail.log` by running your filter over all 100 known payloads and writing one `[CLEAN] <path>` or `[EVIL] <path>` line per file, in the exact format shown.
