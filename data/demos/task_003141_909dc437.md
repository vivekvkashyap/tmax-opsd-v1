Bring up the service chain first, then build and iterate on the sanitizer against real traffic through it. Fix the two known bugs precisely: raise `client_max_body_size` to `20m;` in nginx.conf, and change the Redis host in `intake.py` to `127.0.0.1` (leave the port at 6379). Start Redis (`redis-server --daemonize yes`), Nginx pointed at your local config file, and the Flask app in the background, then confirm with `redis-cli ping` and a test `curl` upload through port 8080 before writing the sanitizer. For `sanitizer.sh`, loop with `redis-cli blpop intake_queue 0` (or similar) to block-pop paths so you don't busy-poll. For each popped directory, validate strictly and in order: metadata.json exists and is valid per `jq empty metadata.json`; `jq -e '.[".backup_type"] == "system_state"'` matches; payload.dat exists; check the real gzip magic bytes with `xxd -p -l 2 payload.dat` (must equal `1f8b`) rather than trusting a file extension; and check size with `stat -c%s` against 10485760. On any failure, `rm -rf` the staging dir. On success, append the metadata line under `flock` on the index file and `mv` the payload into `/home/user/archive/blobs/<uuid>.gz`, using the directory's UUID for naming. Leave the worker running in the background (`&`) so the verifier's traffic gets processed continuously.
