Build the script in clear stages and test each before moving on. Stage 1: open/create `/home/user/research.db`, create the two tables with `CREATE TABLE IF NOT EXISTS`, then parse `metadata.csv` with the `csv` module and `research_data.jsonl` line-by-line with `json.loads`, inserting rows using `cursor.execute(..., (param1, param2))` placeholders — never f-string/format values into SQL. Stage 2: create an index on `experiments.researcher_id` (the join/filter column) with `CREATE INDEX`, then run `EXPLAIN QUERY PLAN <query>` with the researcher name bound as a parameter, and capture the plan rows' detail text as a single string. Stage 3: to force a genuine SQLite `database is locked` error rather than just catching a hypothetical one, open two separate `sqlite3.connect(..., timeout=0.1)` connections, start explicit transactions on each (`BEGIN IMMEDIATE` or an `UPDATE` on different tables) from two threads, then have each thread attempt to `UPDATE` the table the other thread already holds a write lock on — with the short timeout this reliably raises `sqlite3.OperationalError`; wrap that specific write in try/except to catch it. Finally write exactly two lines to `/home/user/result.log`: the `PLAN: ` line and, only if the deadlock exception was actually caught, `DEADLOCK_ACHIEVED`.
