Parse the WAL with `awk '$1=="COMMIT"{print $3}' /home/user/data/sync.wal > /home/user/data/committed_files.txt` (adjust field index to match the actual WAL column layout) to keep only COMMIT lines' paths. For each committed `.gcode` file, detect/convert encoding in place with `iconv -f ISO-8859-1 -t UTF-8 file > file.tmp && mv file.tmp file` — do this as a shell step before invoking Go, not inside Go. In the Go program (`go mod init` inside `/home/user/parser`), read the committed-files list; for `.gcode` files scan each line for tokens starting with `G0`/`G1` and extract the numeric value following a `Z` token (regex like `Z(-?[0-9.]+)`), tracking the running maximum across the whole file. For `.elf` files, open with `debug/elf.Open`, read `f.Entry` for the entry point and `f.Machine.String()` for the machine field. Build the required JSON array with `encoding/json`, marshal to `/home/user/data/summary.json`. Finally create the two symlinks in `/home/user/latest_configs/` with `os.Symlink` pointing at the absolute source paths, named exactly `latest_cnc.gcode` and `latest_firmware.elf`.