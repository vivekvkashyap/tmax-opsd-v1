Write the C++ ETL as a clear sequence of stages so each is easy to verify independently. First, parse both CSVs into maps keyed by id (std::map keeps it id-sorted for free, satisfying the final ordering requirement). Join on id, then for each joined record compute calculated_volume = length*width*height and compare against stated_volume using the relative tolerance: `fabs(calc - stated) <= 0.03 * calc` — implement this exactly as specified (percentage of the calculated volume, not the stated one) since an inverted tolerance direction silently changes which rows pass. For the price string, don't assume single-byte currency symbols: €, ¥, £ are each multi-byte in UTF-8, so strip the correct number of leading bytes for whichever symbol appears (detect by the string's leading byte(s), e.g. check the UTF-8 byte sequences of the three known symbols) before calling strtod/atof on the remainder — the `name` field with multi-byte UTF-8 text should just be copied through untouched. Format both floats with exactly two decimal places using stringstream or snprintf with `%.2f`, and write valid JSON objects one per line in id-ascending order. Sanity-check your output manually against a couple of records you compute by hand before considering it done.