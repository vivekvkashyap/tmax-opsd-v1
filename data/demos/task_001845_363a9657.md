First transcribe `/app/experiment_readout.wav` with an ASR tool to recover the stated baseline mean inference time as a float — do not guess or hardcode a plausible-looking number, actually run the transcription and read the value out. Then write `/home/user/artifact_filter.py` to: (1) load the JSON, treat missing/empty `inference_times` or any non-numeric entries as EVIL immediately (validate every element can be cast to float before proceeding); (2) otherwise run `scipy.stats.ttest_1samp(inference_times, <transcribed_baseline>)` and flag EVIL if the two-tailed p-value is strictly less than 0.01, else CLEAN. Print exactly `CLEAN` or `EVIL` with no extra whitespace/newlines/debug text, since the verifier does exact string matching on stdout. Before trusting your implementation, run it against every file in `/app/corpora/clean/` and `/app/corpora/evil/` and confirm 100% correct classification — if some evil files (the malformed ones) aren't being caught, check that your numeric-validation step actually runs before the t-test rather than crashing or defaulting silently.