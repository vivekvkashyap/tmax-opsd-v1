This is bit-exact black-box reverse engineering, quirks included — do not try to write "correct" chain logic, replicate observed behavior. Generate several small hand-crafted SQLite test databases matching the given schema, including cases with simple linear chains, null parent_ids, and branching (two backups sharing the same parent_id), then run `/app/legacy_chain_builder <db>` on each and record the exact stdout line-by-line, including ordering. Compare against what a naive "correct" recursive traversal would produce to identify the specific quirk (e.g., whether it double-counts branches, what its sort key is — total size, start id, end id — and in what direction). Once you've inferred the precise recursive-path logic (a recursive CTE-style expansion joining chain.end_id to backups.parent_id) and exact output formatting (`start_id -> end_id -> path -> total_size` style, check separators and null handling), reimplement it in `/home/user/chain_builder.py` using Python's `sqlite3` module — you can literally issue an equivalent recursive SQL query yourself rather than hand-rolling graph code, since replicating the same join semantics (including any missing DISTINCT) is what makes it bit-exact. Test against your own databases and diff outputs before trusting the fuzz harness.
