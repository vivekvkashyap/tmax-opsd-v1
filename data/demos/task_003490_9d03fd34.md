Start MongoDB (e.g. via Docker) and confirm `libmongoc`/`libbson` are installed and linkable before writing any C code. Extract frame data with the exact ffmpeg pipe specified (`-f image2pipe -pix_fmt gray -vcodec rawvideo -`), reading the raw grayscale byte stream 320*240 bytes at a time per frame; for each frame count bytes strictly > 200 and insert `{frame, bright_pixels}` docs into `research.frames` via `mongoimport`/a small ingestion script/`mongosh` — get the frame indexing and byte-counting exactly right since a downstream oracle will independently recompute the same counts from the video and compare query results byte-for-byte in structure. In `query_frames.c`, use `mongoc_client_new`, build an aggregation pipeline with `$match` (range on `frame`, `$gte` on `bright_pixels`), `$sort` by `frame` ascending, and `$project` to drop `_id` — construct this pipeline as BSON directly (e.g. with `bson_new_from_json` or `BCON_NEW`) rather than string-concatenating JSON, to avoid escaping bugs. Run `mongoc_collection_aggregate` and iterate the cursor, printing each result as JSON and assembling them into a single top-level JSON array (`[ {...}, {...} ]`, or `[]` when empty) via `bson_as_relaxed_extended_json`. Test with several `<min_frame> <max_frame> <min_bright>` combinations across the frame range before considering it done, since a fuzz-equivalence check will vary these arguments.