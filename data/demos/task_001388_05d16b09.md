Process the three stages in a strict sequential loop that stops on first failure — don't pre-scan all stages before deciding. For each stage, read `/home/user/monitoring.log`, filter lines containing that stage's tag (e.g. `[stage1]`), and check whether any of those lines contain `CRITICAL`; if so, record it as the failed stage, break out of the loop immediately, and skip modifying that stage's manifest (and all later ones). If healthy, update the manifest's image line in place — a simple string replace of the old image tag with the new one (via Python file rewrite or `sed -i`) is safer than templated YAML editing since you must not disturb the rest of the file. Track successful stage names in a list as you go. After the loop (whether it completed all three or aborted early), write deployment_email.eml using the exact template given, joining successful stages with `, ` and using the literal string `None` when nothing failed — build this string with an f-string/format call rather than typing it by hand to avoid whitespace mistakes.
