Do not try to reverse-engineer the stripped/UPX-packed binary's internals by disassembly — treat it strictly as a black-box oracle. The method: for a given database, copy it to a scratch/temp file first (the binary mutates in place), run `/app/graph_indexer` on the copy to regenerate a fresh `index_stats` table from the real `nodes`/`edges` data, then compare that regenerated table against the original database's `index_stats` table using `sqlite3` queries (e.g., dump both tables sorted and diff, or run a SQL query checking equality row-by-row including row count). If they match exactly, print `CLEAN` and exit 0; if any row differs, is missing, or is extra, print `CORRUPTED` and exit 1. Use a unique temp filename per invocation to avoid collisions. Test iteratively against both `/app/corpus/clean/` and `/app/corpus/evil/` directories in a loop, checking your script's exit code and stdout against the expected classification for each file, before declaring the detector done — don't assume correctness from a handful of manual spot checks.
