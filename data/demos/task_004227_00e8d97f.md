Install `rdflib` first, then load the graph with `Graph().parse('/home/user/access_graph.ttl', format='turtle')` and inspect a few triples (e.g. print subjects/predicates) to confirm the actual predicate names (`ex:department`, `ex:hasAccessTo`, `foaf:name`) rather than assuming them. Write a SPARQL query using `graph.query(...)` that selects employees together with their department and the set of systems they access — e.g. group by employee/department and use `GROUP_CONCAT` on accessed systems, or simply pull all `(employee, system)` access triples plus `(employee, name, department)` triples and post-process in Python: for each employee, collect the set of accessed system URIs, check that both the FinancialSystem and HRSystem URIs are in that set, and that the department string is not "Executive". Sort the resulting violators by their EmployeeID URI string (not by name), and write a CSV to `/home/user/violations.csv` with header `EmployeeID,Name,Department` using Python's `csv` module so quoting/escaping is handled correctly. Double check the department comparison is case- and string-exact against how it's stored in the TTL.
