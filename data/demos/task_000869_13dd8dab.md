Follow the pipeline steps in exact order since order affects the numeric result: load the CSV with pandas, first drop rows where target > 1000 (this removes the injected outlier), THEN compute the median of `f1` only over the remaining (post-outlier-removal) rows, and use that median to fill NaNs in `f1` — computing the median before dropping outliers, or filling before dropping, will silently shift the answer. Install `pandas` and `scikit-learn` via pip if not present. Fit `sklearn.linear_model.RidgeCV(alphas=[0.1, 1.0, 10.0])` (leave other params default, which uses efficient leave-one-out CV) on features `f1, f2` against `target`, using the cleaned dataframe. Read off `model.best_alpha_` and write it, and nothing else, to `/home/user/best_alpha.txt` — write the value as returned by scikit-learn (e.g. via `print(model.best_alpha_, file=f)` or `str()`), don't hand-round it. Double check by re-running the script and confirming the same alpha is picked deterministically (the data generation is seeded, so results should be stable across runs as long as your preprocessing order matches the spec).
