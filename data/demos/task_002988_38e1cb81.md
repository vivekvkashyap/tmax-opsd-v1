Work through this in three independent, verifiable stages. First, read `legacy_parser.js` carefully to understand its precedence/associativity rules for `+ - * /`, then port it to Python as a real tokenizer + recursive-descent (or shunting-yard) parser evaluated over an AST — never call `eval`/`exec` on the raw string, since the grader explicitly forbids it and may test malicious expressions; only allow tokens matching numbers, `R`/`G`/`B`, and `+-*/`. Split this into `app/parser.py` (parsing/eval), `app/video.py` (frame extraction), `app/main.py` (HTTP wiring). Second, for video extraction, since every frame is a uniform solid color, use `ffmpeg` or `opencv`/`PIL` via `ffmpeg -i /app/color_math.mp4 -vf fps=10 frame_%03d.png` (or read frames directly with `cv2.VideoCapture`) and sample one pixel (e.g. `[0,0]`) per frame to get that frame's R/G/B — don't average the whole frame unnecessarily, uniformity means any pixel works. Third, build the Flask/FastAPI server on `127.0.0.1:8080`: check the `Authorization: Bearer MATH-TOKEN-2024` header first (401 if wrong/missing), validate frame bounds and expression token whitelist (400 on failure), then evaluate via your parser using that frame's cached R/G/B. For rate limiting, track request timestamps per client IP in a sliding 1-second window (e.g. a deque per IP, evict entries older than 1s, reject with 429 if count already at 5) — a naive fixed-counter-reset-every-second approach will misbehave at window boundaries, so prefer a sliding window or token-bucket.