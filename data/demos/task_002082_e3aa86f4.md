Structure the Rust program to physically materialize each DAG phase's intermediate files, not just compute in memory, since the verifier checks for their existence. Phase 1 (decode): read each input file's raw bytes and decode with the correct codec per source — US is already UTF-8 (pass through), DE needs `encoding_rs::WINDOWS_1252` or ISO-8859-1 decoding, JP needs `encoding_rs::SHIFT_JIS` — write each result as UTF-8 to its `decoded_*.csv`. Phase 2 (align): parse each row's timestamp with `chrono` using the source's exact format string (12-hour with AM/PM for US, dotted DD.MM.YYYY for DE, slashed YYYY/MM/DD for JP), attach each source's fixed UTC offset (`FixedOffset::west/east`), convert to UTC, and format as RFC3339 with a `Z` suffix — write to `aligned_*.csv`. Phase 3 (merge): read all three aligned files, parse each into a struct, sort the combined vec by the UTC timestamp field, then serialize with `serde_json` with no pretty-printing (compact/minified) into `final_output.json`. Build and actually run the binary (`cargo run`) at the end so all four output files exist, and spot-check the JSON ordering and one timezone conversion by hand.