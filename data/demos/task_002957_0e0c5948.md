Start by running `pytest /home/user/optimization_lib -v` and reading the actual traceback/`ConvergenceError` to see which test fails and why, rather than guessing. Open `solver.py` and trace the Newton-Raphson update line by hand against the standard formula `x_{n+1} = x_n - f(x_n)/f'(x_n)` — a single sign flip (`x = x + fx/dfx` instead of `x = x - fx/dfx`) makes the iteration walk away from the root instead of toward it, causing divergence/non-convergence; correct just that operator. Re-run the existing test suite to confirm it now passes before adding anything new. Then create `tests/test_regression.py` importing `newton_root_finder` from the package, define `f = lambda x: x**2 - 9` and `df = lambda x: 2*x`, call the finder with `x0=5.0`, and assert the returned root is within `1e-4` of 3.0 and the iteration count is strictly less than 10 — write these as real assertions against the function's return values, not hardcoded expected literals unrelated to the actual call. Finish by running `pytest /home/user/optimization_lib` one more time and confirming a fully green, zero-exit-code result.