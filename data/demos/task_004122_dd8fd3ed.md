Work through the four phases in order, verifying each with a real command before moving to the next rather than writing all files blind. Phase 1: create the directory tree, then write the fstab.mock line with exactly 6 whitespace-separated fields in the standard fstab column order (device/dir/type/options/dump/pass) using the literal source and target paths given, `none` and `bind` for type/options, `0 0` for dump/pass; create `storage/index.html` with exactly the required text. Phase 2: write proxy.c as a straightforward single-shot TCP relay — `socket`/`bind`/`listen`/`accept` on 8080, then `connect` a second socket to 9090, `read`/`write` once in each direction (up to 1024 bytes), close both sockets, and let `main` return so the process exits after one client — compile with `gcc` and confirm it's an ELF binary. Phase 3: the start_stack.sh script must actually run things in the right order — copy storage/* into container_data/, launch `python3 -m http.server 9090 --directory .../container_data &`, launch the compiled proxy_server & (which will block waiting for a connection), `sleep 2`, then `curl` port 8080's index.html into the log file; chmod +x it and execute it yourself to confirm test_request.log contains the expected content before moving on. Phase 4: write the .eml file with byte-exact headers/body as specified, including the blank line separating headers from body.