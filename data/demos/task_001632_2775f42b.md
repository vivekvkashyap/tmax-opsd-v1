Start by trying `make` inside `/app/spectral-ode-sim-1.0` and reading the actual compiler error rather than guessing — a missing-symbol/undefined-reference error on math functions (sin/cos/pow) at link time almost always means the Makefile's gcc invocation is missing `-lm`; grep the Makefile for the gcc/cc command line and check flag order (linker flags like `-lm` must come after the source files). Fix only that line, rebuild, and confirm `./ode_sim` runs and produces a two-column time/amplitude CSV before moving on. For step 3, write the FFT/dominant-frequency logic as a reusable Python snippet (invoked via `python3 -c` or a heredoc from Bash) that reads a two-column CSV, computes `np.fft.rfft`, takes magnitude, and finds the argmax frequency bin while explicitly excluding index 0 (DC). Apply this same function to both your generated sim CSV and to `/home/user/reference.csv`, then compare with `abs(f1-f2) < 0.05`. For the server, don't just print JSON — implement a real HTTP listener (e.g. Python `http.server`/`BaseHTTPRequestHandler` subclass, or a raw socket loop) bound to `0.0.0.0:8333` that parses the request line, checks the path is `/status`, and writes proper `HTTP/1.1 200 OK` + `Content-Type: application/json` headers followed by a blank line and the JSON body; launch it with `nohup ... &` (or backgrounded inside the script) so it stays alive after the script "returns".
