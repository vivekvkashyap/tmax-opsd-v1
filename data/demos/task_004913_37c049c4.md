Separate parsing/validation from the math. First write the CSV row filter precisely per the spec: require exactly 3 fields, `sensor_id` non-empty, `total_readings` a strictly-positive integer, `anomalies` a non-negative integer, `anomalies <= total_readings`, and drop the row on any numeric parse failure (e.g. non-numeric fields) — test this filtering logic against the given `sensor_data.csv` by hand-tracing which rows survive before trusting the aggregate. Sum `anomalies` and `(total_readings - anomalies)` only over surviving rows to get the Beta-Binomial posterior: `alpha_post = 1 + sum_anomalies`, `beta_post = 1 + sum_normal`, then `mean = alpha_post/(alpha_post+beta_post)` and `variance = alpha_post*beta_post / ((alpha_post+beta_post)^2 * (alpha_post+beta_post+1))`. For the rounding requirement, don't rely on plain C++ `printf`/stream rounding which can be round-half-to-even for some cases — implement explicit half-up rounding (e.g. `floor(x * 10^n + 0.5) / 10^n`) before formatting to exactly 6 (mean) and 8 (variance) decimal places. Write the CSV with the exact header and metric ordering shown in the spec, then compile with `g++ -O2` and actually run the binary to produce `/home/user/results.csv`.
