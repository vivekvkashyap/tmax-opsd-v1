Split this into evidence recovery and mitigation, and validate each independently. For recovery, transcribe `/app/evidence/payload.wav` with an available speech-to-text tool (convert with `ffmpeg` first if needed), lowercase the result exactly as spoken, then grep `/app/logs/voice_assistant.log` for the log line whose quoted command matches (or closely matches) your transcription and pull the IP address token from that line — don't hardcode an IP, derive it from the matching log entry. Write both fields into `/home/user/forensics_report.json` with the exact key names given. For mitigation, build `/home/user/sanitizer` as an executable script (proper shebang, `chmod +x`) that reads the single text-file argument's content and classifies it: flag as `BLOCK` if it contains shell metacharacters or injection patterns — pipes `|`, redirects `>`/`<`, backticks, `$(...)`, command separators `;`/`&`, or known dangerous commands like `curl`, `wget`, `rm -rf`, `nc`, `reboot` — otherwise print `ALLOW`. Before trusting it, hand-test the sanitizer against a few transcripts you write yourself covering both benign phrasing and injection attempts, then run it over every file in `/app/corpora/clean/` and `/app/corpora/evil/` to confirm 100% correct classification before considering the task done.