Split this into ingestion then query-serving, and get each stage verified independently before moving on. First confirm MongoDB is reachable on localhost:27017 (start it if not running). Second, run `ffprobe -v error -select_streams v:0 -show_entries frame=pict_type,pkt_size,coded_picture_number -of json /app/telemetry.mp4`, parse the JSON, filter frames where `pict_type == "I"`, and insert one document per I-frame into `etl.iframes` with fields exactly `{"frame_idx": <coded_picture_number as int>, "pkt_size": <int>}` — double check field types are ints, not strings, since Mongo comparisons in $match/$sort are type-sensitive. Third, in build_query.py, parse `sys.argv[1]` as JSON, connect via pymongo, and build the aggregation pipeline stages in the exact order specified: $match (inclusive bounds on pkt_size), $sort (by pkt_size with given sort_order, then frame_idx ascending as tiebreak), $limit, $group (_id:null, $avg into average_size, $push frame_idx into frames array), $project (exclude _id). Print with `json.dumps(list(cursor), separators=(',', ':'))` for exact compact formatting, and test manually with a few hand-picked parameter JSONs to confirm stage order and tie-break behavior before relying on the fuzzer.
