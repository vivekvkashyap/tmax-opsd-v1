Work in three clean stages instead of trying to patch things blindly. First, open the Rust struct definition in the data_prep project and find the field that lazily uses a float/Option<f64> to model missing counts — change the field type to a signed integer, and in the deserialize/transform step explicitly map empty or unparseable values to -1 rather than relying on serde's default NaN/blank behavior; make sure the CSV writer emits it as a bare integer, not `15.0` (a common naive mistake is leaving the struct as f64 and only formatting the string, which still round-trips through float parsing and can produce `15` sometimes and `15.0` other times, or crash on truly empty cells). Run `cargo build --release` and fix any compile errors from the type change propagating elsewhere. Second, run the built binary against the raw CSV to produce the cleaned CSV, and diff a few rows by eye to confirm no `.0` suffixes and no blanks remain. Third, compute statistics directly from the cleaned `response_time` column (not raw) using the standard formulas: sample mean, sample standard deviation (N-1 denominator), and a 95% CI as mean ± 1.96*(stddev/sqrt(N)); round to 3 decimals and write the exact JSON keys requested to experiment.json. Never touch the raw input file.
