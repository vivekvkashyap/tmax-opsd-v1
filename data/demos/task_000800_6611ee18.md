Treat this as black-box rule inference via differential testing, not code reading (the binary is stripped). Run `/app/query_engine` against every file in `/app/corpus/clean/` and `/app/corpus/evil/`, recording exit codes, then diff the JSON structures between the two sets to hypothesize which fields correlate with pass/fail — look specifically at `limit`, any `traverse` block's `depth` and `shortest_path` flag, and any `cross_mapping` block's `indexed` flag. Formulate candidate rules (e.g. "limit must exist and be <= some threshold", "depth is capped lower unless shortest_path is true", "cross_mapping needs an indexed flag"), then re-derive the exact thresholds by binary-searching: construct minimal synthetic query files that vary just one field at a time and rerun the binary to find the boundary values, rather than guessing. Only after your rules perfectly reproduce the binary's exit codes on the whole corpus should you encode them in `/home/user/sanitizer.py` as pure JSON/Python logic (no `subprocess` calls to the binary). Make the script executable, accept one positional file-path argument, and exit 0 for safe/1 for unsafe. Validate by rerunning your script over both corpora and confirming its exit codes match the binary's on every file before finishing.
