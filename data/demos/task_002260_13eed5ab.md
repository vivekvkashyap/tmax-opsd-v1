Work the three subproblems independently and verify each before chaining them. (1) To recover a deleted file from the ext4 image, mount it read-only or use a recovery tool (`debugfs -R 'lsdel' /app/backup.img`, or `extundelete`/`photorec` on a loop-mounted copy) rather than assuming standard `cp` will find it — deleted inodes need a recovery tool, not a normal file listing. (2) For OCR, extract frames with `ffmpeg -i /app/crash_vid.mp4 frame_%03d.png`, then run `tesseract` on each frame (upscaling/thresholding the image first improves accuracy on terminal-style text), concatenate the recognized lines in frame order, and manually inspect the result for OCR noise before saving to raw_logs.txt. (3) Before "fixing" the buggy parser, write a tiny bash fuzzer that generates malformed/edge-case lines (missing `=`, missing spaces, empty lines) and feeds them to `parser.sh` with a timeout to find which inputs hang; the known bug is an inner `while` loop that never terminates when a required token is absent — replace unbounded loops with bounded per-line processing (e.g. `IFS=' ' read -r` field parsing) and add `set -e`/validation guards so malformed lines are skipped, not looped on.
