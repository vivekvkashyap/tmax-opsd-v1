Extract frames from `/app/backup_monitor.mp4` with OpenCV (`cv2.VideoCapture`), reading one frame per second in order; for each frame convert to grayscale and compute the mean pixel intensity (`cv2.cvtColor(..., COLOR_BGR2GRAY).mean()`), classifying `status = 1` if the mean is `> 128` else `0`. Assign timestamps sequentially starting at `1700000000` and incrementing by 1 per frame, matching frame index to timestamp exactly — an off-by-one in frame ordering will desync every downstream row. Create the SQLite table with the exact schema given and bulk-insert the recovered (timestamp, status) rows. For `backup_analyzer.py`, take the timestamp argument, and instead of computing streaks in Python, use a SQL window-function approach: select failed rows (`status = 0`) before `T`, use `timestamp - ROW_NUMBER() OVER (ORDER BY timestamp)` as a grouping key to identify contiguous runs, group by that key, count rows per group, and take the max (defaulting to 0 if none) — this "gaps and islands" technique is the standard way to get consecutive-streak lengths in SQL. Print only the resulting integer. Since this is checked against a hidden reference implementation across many random timestamps, get the boundary condition (`timestamp < T`, not `<=`) exactly right.
