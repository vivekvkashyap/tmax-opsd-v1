Build the pipeline in pandas, processing each sensor_id group independently. First compute each group's mean/std of `value` ignoring NaNs, then mask outliers (|value - mean| > 3*std) to NaN — do this before sorting/interpolating, since outliers must not distort the interpolation. Sort by timestamp, then per sensor set timestamp as the index and use pandas' `interpolate(method='index')` (or `'time'` if using a datetime index) rather than the default linear-by-position, since numeric timestamp values may not be evenly spaced and index-aware interpolation is what the task specifies. After interpolation, apply `ffill()` then `bfill()` per sensor group to cover any remaining leading/trailing NaNs interpolation can't fill. For the integral, use `numpy.trapz(values, x=timestamps)` (or `scipy.integrate.trapezoid`) per sensor on the cleaned series, sorted by timestamp. Write results sorted alphabetically by sensor_id to `integration_results.csv`, then find the max `total_area` row and write `sensor_id,rounded_area` (2 decimal places) to `max_sensor.txt`. Sanity-check your area values against a rough manual estimate for at least one sensor (e.g. mean value times time span) to catch integration-order or NaN-handling mistakes before finishing.