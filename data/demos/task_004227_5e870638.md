Don't try to trace instructions inside the sandboxed legacy binary directly — instead do black-box behavioral analysis: pipe a handful of known/simple base64 seeds (including easy cases like an empty string or a single 'A' repeated) into `/app/credgen_legacy` and record the outputs, then form a hypothesis about the transform (decode base64 → combine with some fixed value → hash) and test it against more sample seeds until every prediction matches exactly. Useful tools for hypothesis generation: `strings`/`file` on the binary to spot embedded constants (a salt/suffix string), and comparing output hex length (64 hex chars strongly suggests SHA-256). Once you can reproduce the legacy tool's output byte-for-byte on your test seeds, implement the same logic in C using OpenSSL's EVP/SHA256 API for speed, reading the input file line-by-line with buffered I/O (avoid `system()` calls or per-line process spawns) and writing hex output efficiently (e.g. batched `fwrite` rather than many small `printf` calls) to hit the sub-0.25s target over 100k lines. Compile with `-O2` and link `-lcrypto`. Validate correctness against your own captured legacy-binary samples before running the full `/app/seeds.txt` batch, then benchmark with `time` to confirm you're comfortably under threshold.
