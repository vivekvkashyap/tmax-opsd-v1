Break this into independent stages and verify each before moving on. First, extract frames from `/app/system_limits.mp4` (e.g. `ffmpeg -i system_limits.mp4 frame_%04d.png`), then programmatically inspect the center pixel of each frame (Python + PIL/OpenCV) and count frames whose center pixel is exactly RGB(255,0,0) vs exactly RGB(0,0,255) — these counts become `MAX_SYMLINK_DEPTH` and `MAX_FILES_PER_ARCHIVE` respectively; do not eyeball the video, compute it. Use `sed`/`awk` to substitute the discovered values into the template placeholders and write `/home/user/config.json`. For the filter script, the key constraint is "no extraction to disk" — use `zipfile.ZipFile` and `tarfile.open` to read member metadata (`infolist()`/`getmembers()`) directly from the archive without calling `.extractall()`; check for absolute paths, `../` segments, and file-count limits from metadata alone. For symlinks, build a graph of link targets from the archive's own metadata (don't touch the real filesystem) and walk it to detect cycles or chains exceeding `max_depth`, and reject any target resolving outside the archive root. Test iteratively against both `/app/corpora/clean/` and `/app/corpora/evil/`, fixing whichever cases fail until 100% pass on both.
