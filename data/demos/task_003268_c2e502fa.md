Decode the fixture first, methodically: use ffmpeg or cv2 to extract every frame from `/app/suspicious_upload.mp4`, compute each frame's average R/G/B, and classify each frame as `1` (red: R>200, G<50, B<50) or `0` (blue: B>200, R<50, G<50) — group frames into consecutive chunks of 8 and pack each chunk big-endian into a byte to reconstruct the raw ELF bytes, then write them to `/home/user/extracted_payload.elf` and confirm it starts with the ELF magic (`\x7fELF`). Inspect it with `strings`/`readelf` to see the embedded HTTP request and path-traversal pattern before designing your classifier's detection rule. For `classifier.py`, reuse the exact same frame-decode-to-bytes pipeline as a library function, then scan the recovered bytes (or extracted strings) for path-traversal indicators — `../`, `/etc/passwd`, `/etc/shadow`, or similar sensitive-path patterns — printing `EVIL`/`CLEAN` accordingly and always exiting 0. Validate against `/app/corpus/evil/` and `/app/corpus/clean/` before finalizing: since the corpus is adversarial, make sure your ELF-vs-non-ELF and frame-color thresholds are robust (e.g. handle videos that decode to garbage/non-ELF data as CLEAN) rather than tuned to only the one fixture video.