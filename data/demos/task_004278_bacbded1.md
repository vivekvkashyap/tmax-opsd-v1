Copy the `.log` files into the workspace first, then confirm the encoding yourself (e.g. `file server_0.log` or a hexdump) before parsing — UTF-16LE means every ASCII character is followed by a `0x00` byte, so read raw bytes and either decode properly or stride 2 bytes at a time taking the low byte for pure-ASCII lines. In the C program, spawn one pthread per input file so files parse concurrently, each thread splitting lines on `|` and truncating the timestamp to `YYYY-MM-DD HH:MM` (drop seconds) while accumulating a sum/count of `RESPONSE_TIME_MS` per minute-bucket, guarding shared aggregation with a mutex (or merging per-thread local maps after `pthread_join`). Then compute each minute's average and scan minutes in chronological order (the timestamp string sorts correctly as text) to find the *first* minute whose aggregate average is strictly greater than 500. Write exactly one line to `/home/user/anomaly.txt` in the required format, no trailing carriage return.
