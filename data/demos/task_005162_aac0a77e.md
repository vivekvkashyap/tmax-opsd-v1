Implement `/home/user/analyzer.py` as a true streaming loop over `sys.stdin` (use `csv.reader`/`csv.writer` on stdin/stdout, calling `sys.stdout.flush()` after every output row) rather than reading all input first, since the pipeline tails a live file. For each line: lowercase `raw_text`, replace any char outside `[a-z0-9 ]` with a space (a simple regex substitution is fine here, it's not the state-machine task), collapse repeated spaces, strip, split on whitespace to get `token_count`. Maintain a fixed rolling window of the previous 20 token counts (append after scoring, not before, and pop the oldest once size exceeds 20) — output `0.0000` until the window has exactly 20 prior values, otherwise compute population std (divide by 20, not 19) of the window and `abs(count-mean)/std`, guarding div-by-zero as `0.0000`, formatted to 4 decimals. For the orchestration script `/home/user/start_pipeline.sh`, ensure `/tmp/input_stream.csv` and `/tmp/output_stream.csv` exist first (`touch`), then background-launch the API server, a `tail -F /tmp/input_stream.csv | python3 /home/user/analyzer.py >> /tmp/output_stream.csv` pipeline, and the redis forwarder — use `tail -F` (capital F) so it survives file rotation/creation, and make sure each piece stays running (not exiting) after the script returns.