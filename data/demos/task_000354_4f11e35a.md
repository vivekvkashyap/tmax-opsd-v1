Scaffold with `cargo new schema_extractor` in `/home/user/schema_extractor`, add `serde_json` (and `regex` only if you find it easier than manual splitting) to `Cargo.toml`. Parse the N-Triples file line by line: each line is `<subject> <predicate> <object-or-literal> .` — split on whitespace but be careful that literal objects can contain spaces/quotes, so only the first two tokens (subject, predicate) are reliably delimiter-safe; strip the trailing ` .` and angle brackets. Two-pass approach: first pass, build a map from subject -> its rdf-type URI (predicate == `.../22-rdf-syntax-ns#type`); second pass, for every triple, if the subject has a known type, insert the triple's predicate into a `BTreeSet<String>` keyed by that type (BTreeSet gives you free sorting and dedup). Convert the `HashMap<String, BTreeSet<String>>` into a `serde_json::Map` with the sets turned into sorted `Vec<String>`, and write it with `serde_json::to_writer_pretty` or `to_string` to `/home/user/schema_output.json`. Build with `cargo build --release` and run the binary so the output file is produced on disk.
