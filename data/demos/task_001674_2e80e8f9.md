Treat this as a constraint-satisfaction filtering problem, not a string-matching one. First write a small parser that scans `/home/user/asm_trace.s` line by line, using a regex to match comment lines of the form `; REQUIRE <KEY> <OP> <VALUE>` and `; CONFLICT <KEY> <VALUE>`, capturing key/op/value into a list of constraint tuples (note VALUE may be numeric or a string token like a CPU extension name). Load `/home/user/build_targets.json` into a list of dicts. For each target, evaluate every REQUIRE constraint (numeric comparisons like `>=` against the target's field, or membership checks when the field is a list) and every CONFLICT constraint (must NOT be present in the list field) — a target only survives if it satisfies ALL requires and violates NO conflicts. Common naive mistakes: forgetting a target must pass every constraint (not just one), mishandling `>=` vs `>` for numeric ops, and not distinguishing list-membership constraints (CPU_EXT) from scalar comparisons (MEM). After filtering, sort survivor names alphabetically and write them into the exact fixture template given in the task description, then actually execute the script so `/home/user/test_fixture.py` is produced on disk.