Don't guess at the trigger condition — derive it empirically and, ideally, confirm it by inspecting the binary. Diff the clean vs. evil corpora (`diff`, `grep`, or manual inspection) to spot which substrings/tags appear only in evil files; also run `strings /app/config_differ` and try `objdump -d` to look for comparison strings baked into the binary, and run the binary against sample files from both corpora to observe pass/fail behavior directly. Be careful about a common trap in this kind of task: the trigger is often a conjunction (BOTH a specific XML tag/value AND a specific JSON key/value must be present together) rather than either one alone — test that hypothesis by constructing synthetic files that have only one of the two candidate markers and confirming the binary treats them as clean. Once confirmed, implement `/home/user/detector.c` using `regex.h`/string search, reading the file with a bounded buffer via `fgets` in a loop (never loading the whole file at once) so it scales to large inputs, setting two flags as it streams, and after EOF printing `EVIL`+exit 1 only if both flags are set, else `CLEAN`+exit 0. Compile with `gcc -O2 -o /home/user/detector detector.c` and validate against both provided corpora before considering it done.