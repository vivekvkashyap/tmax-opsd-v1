Use Python's `zipfile` module strictly in-memory/stream mode — never call `extractall()` or write archive members to disk, since that's exactly the zip-slip vector you're asked to avoid; instead iterate `zf.infolist()` and open each member with `zf.open(name)` to get a stream, reading bytes without ever resolving them against the filesystem path implied by the entry name. For `.elf` files, read the stream's bytes, seek/slice to offset 0x18, and unpack 8 bytes as a little-endian unsigned 64-bit integer with `struct.unpack('<Q', data[0x18:0x20])[0]`, then format as `hex()` (verify it matches the `"0x..."` style expected). For `.gcode` files, decode the stream as text and scan lines for one starting exactly with `"; TargetMachine: "`, taking the remainder as the machine name (strip trailing whitespace/newline). Build the JSON record per archive and write it with `fcntl.flock(fileobj, fcntl.LOCK_EX)` held around the open-append-write-close sequence on `/home/user/doc_index.jsonl`, releasing the lock (or letting the `with` block close the fd) after each write so concurrent invocations don't interleave writes. Run the script once per zip file found in /home/user/docs_incoming/.