Work through this as a chain of independently-verifiable steps. First try `make` inside /app/vendored/microserver and read the linker error — a C++ program using `std::thread` (as an httplib-based server does) fails to link without `-pthread`; open the Makefile and add `-pthread` to the `LDFLAGS` line, then rebuild and copy/`cp` the resulting binary to /home/user/microserver_bin. Create /home/user/public_html with index.html and data.txt containing exactly the specified strings (no trailing content beyond what's asked — use `printf` or `echo -n` to avoid stray newlines mattering, though check if trailing newline is tolerated). Write start_server.sh exporting `TZ=Pacific/Auckland` and `LC_ALL=en_NZ.UTF-8` before launching the binary with `&` and args `9090 /home/user/public_html`, with a check like `[ -x /home/user/microserver_bin ] || exit 1` before starting. For port forwarding, launch `socat TCP-LISTEN:8080,fork,reuseaddr TCP:127.0.0.1:9090 &`. For monitor.sh, loop forever with `sleep 2`, `curl -s -o /dev/null -w '%{http_code}'` against the health endpoint, and overwrite status.log with `UP` on 200. Launch all three background processes yourself (don't just write scripts) so they're live when your turn ends — verify each with `curl` and `ps` before finishing.