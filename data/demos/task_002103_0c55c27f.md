This is a black-box distillation task, not a rule-guessing task — do not try to hand-derive the oracle's scoring formula. (1) Run `/app/risk_evaluator` on `/home/user/logs.csv` to get labeled `(message, risk_score)` pairs; since the training set is small, consider synthesizing many more probe inputs (short messages combining various words/keywords in different counts and combinations) and running them through the oracle too, to get a richer training signal. (2) Since the task states the oracle is a "deterministic tokenization + linear scoring algorithm based on word occurrences," featurize messages with something like a bag-of-words / CountVectorizer and fit a linear model (Ridge/LinearRegression) — a linear model over word-count features can recover an additive keyword-weight scheme like this essentially exactly, so check your training MSE is near zero before finalizing. (3) Package prediction logic identically in `/home/user/surrogate.py`, reusing the exact fitted vectorizer+model at inference time. (4) For the confidence interval, compute sample mean, sample std (ddof=1), and use `scipy.stats.t.interval` (or the manual t-formula) with n-1 degrees of freedom on the oracle's true scores over `/home/user/logs.csv`, writing `[lower, upper]` rounded to 4 decimals.