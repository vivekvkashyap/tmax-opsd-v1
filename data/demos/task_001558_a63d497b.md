Build the three commands as independent handlers in a single-threaded (or per-connection) TCP server on port 8888 that reads a line, dispatches, writes the raw response, and closes the socket. For symlink-loop-safe traversal in the BACKUP handler, use `lstat`/`readlink` (not `stat`, which follows symlinks blindly) and track visited real paths (via `realpath`) in a set as you recurse, or bound recursion depth, so that a self-referential symlink under `/app/configs/modules/` is detected and that path is skipped rather than causing infinite recursion — only collect `.json`/`.txt` files with paths relative to `/app/configs/`. Pack each file per the exact binary layout given (2-byte LE path-length, path bytes, 4-byte LE file-size, file bytes) into an in-memory buffer, then zlib-compress the whole buffer with `compress()`/`deflate()` and write the compressed bytes straight to the socket. For HISTORY, open `/app/config_history.mp4`, search the byte stream for the literal 4-byte sequence `CFGB` (reading the whole file into memory and using `memmem` or a manual scan is simplest here since the file is small), then zlib-decompress everything following it and return the resulting JSON text. For FRAME_HASH, shell out via `popen`/`system` to the exact ffmpeg command given with `N` substituted, pipe its PPM stdout into a SHA-256 implementation (OpenSSL's `EVP_Digest` or `sha256sum` via a pipe), and return the hex digest. Test each command manually with `nc localhost 8888` before finishing, and make sure the server is left running in the background.