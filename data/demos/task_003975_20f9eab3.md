Write a C++ program with three clearly separated phases so bugs don't compound. First, parse `/home/user/system_metrics.csv` and reshape each row into three long-format records (timestamp, "cpu"/"memory"/"disk", value) — use a struct/vector, not string hacks, for the long table. Second, apply the validation gate: drop any record with `value < 0.0` BEFORE any statistics are computed — a common mistake is computing mean/stddev over all rows and only filtering the anomaly output afterward, which corrupts the mean itself. Third, for each metric independently, compute the mean and the POPULATION standard deviation (divide the sum of squared deviations by N, not N-1 — mixing this up is the other classic error here). Flag any valid record with `|value - mean| > 2*stddev` as an anomaly. When writing `/home/user/anomalies.csv`, sort by timestamp ascending then metric name alphabetically, and format the value with exactly one decimal place using `std::fixed << std::setprecision(1)`. Compile with g++ and run it to actually produce the file before considering the task done.
