Build this as a `find` + `awk` pipeline, not a custom script language: use `find /home/user/raw_data -name '*.csv' -exec tail -n +2 {} \;` (or equivalent) to stream every data row from every nested CSV while skipping headers, then pipe into a single `awk -F,` program that computes `gross = quantity*unit_price`, `discount = gross*discount_percentage/100`, `net = gross-discount` per line and accumulates running sums into associative arrays keyed by `item_category` (`net_sum[cat]+=net; disc_sum[cat]+=discount`). In the `END` block, iterate the arrays (awk's array order is unspecified, so pipe the awk output through `sort` afterward rather than relying on awk to sort), format each line with `printf "%s\t%.2f\t%.2f\n"` for correct 2-decimal rounding, and write to `category_summary.tsv`. Watch two common pitfalls: awk's default `%.2f`-style formatting can be affected by locale/rounding mode, and floating point summation order can shift the last digit — verify your totals by hand against a couple of categories. After the TSV is written and confirmed correct, `tar czf /home/user/archive/raw_data_backup.tar.gz -C /home/user raw_data`, verify the tarball is non-empty/valid (e.g. `tar tzf`), and only then `rm -rf /home/user/raw_data`.
