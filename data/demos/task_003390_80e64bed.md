Fix the Makefile's `app` target so it both depends on the shared library and links against it in the current directory — add `-L. -ldataparser` to the link command (the missing `-L.` is why the linker can't find libdataparser.so) and make `app` depend on the `lib` target so build order is correct. The memory leak fix is simply calling the already-defined `free_parsed_data(data);` right before `return 0;` in main.c — no other logic should change. For run_tests.sh, export `LD_LIBRARY_PATH` to include the script's own directory before invoking `./app` so the dynamic loader can resolve libdataparser.so at runtime, run the app under `valgrind --leak-check=full --error-exitcode=1 ./app "$1" > output.log`, and finish with `exit $?` so the wrapper's exit code exactly mirrors valgrind's. Test end-to-end with a sample payload to confirm you get exit code 0 and the expected parsed lines in output.log.
