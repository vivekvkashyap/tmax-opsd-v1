Treat this as a checklist of independent, verifiable fixes rather than one big change. First inspect the current symlink with `readlink -f /home/user/active-deployment`, then remove and recreate it pointing at the new release directory with `ln -sfn /home/user/releases/v2 /home/user/active-deployment` — confirm with `readlink` afterward. Second, check current hook permissions with `ls -l`, then `chmod +x` the post-receive hook so Git's daemon can exec it (a hook without the execute bit is silently skipped by Git, which is the actual root cause of "not triggering"). Third, edit the unit file's `[Unit]` section to add `After=git-daemon.service` (append to any existing After= line rather than clobbering it), then run `systemctl --user daemon-reload`, and use `systemctl --user enable --now deploy-worker.service` so it's both started and enabled for boot. After each step, verify independently with the corresponding read command (`readlink`, `ls -l`, `systemctl --user show ... -p After`, `systemctl --user is-active`/`is-enabled`) rather than assuming success — everything must run under `--user`, no sudo.