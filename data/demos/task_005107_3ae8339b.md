Treat this as a small ETL/scoring pipeline: read weights.csv first to build a lookup of feature->weight and the bias term (don't hardcode which features exist — parse them generically). Then process data.csv row by row: (1) impute empty strings to 0.0 before any other step, (2) clip each imputed value into [-10.0, 10.0] using min/max, (3) compute prediction = sum(feature_i * weight_i) + bias using the clipped values. Only after computing should you filter rows where prediction > 0.0. Write results sorted ascending by id, no header, format `id,prediction` — be careful with float formatting (avoid unnecessary trailing zeros or scientific notation mismatches versus a simple `%.1f`/`%g`-style representation, matching how the numbers would naturally render, e.g. `4.5` not `4.500000`). A short Python or awk script is easiest since it handles CSV parsing, clipping, and sorting straightforwardly — validate by manually recomputing 1-2 rows by hand before trusting the script's output.