Treat this as a pipeline where an early misread cascades into total failure, so verify each stage's intermediate output before proceeding. First, transcribe the audio: convert or feed `/app/audio/client_request.wav` to a speech-to-text tool (check what's installed, e.g. `whisper`, or a CLI ASR utility) and read the transcript carefully to identify (a) which column-name prefix to drop and (b) the numeric target user ID spoken aloud — write these down explicitly before coding, since getting either wrong silently ruins the whole similarity computation. Second, in pandas: drop columns matching that prefix, standardize remaining numeric columns with sklearn's StandardScaler, fit PCA(n_components=10), then compute cosine similarity between the target user's row and all others (sklearn's cosine_similarity), exclude the target itself, and take the top 100 by score descending — save user_id,similarity_score to the CSV with header. Sanity-check by confirming your target user's ID actually exists as a row before running the full pipeline. Third, for the plot bug: `plt.show()` before `plt.savefig()` clears the figure in a headless Agg backend — remove the `plt.show()` call (or reorder so savefig comes first) and rerun to confirm a non-blank PNG is produced.
