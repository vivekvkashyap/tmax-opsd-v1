Reverse-engineer the linear model by probing, not by disassembling. First check word tokenization behavior: feed the binary single-word files (just "cat", just "dog", etc., and an empty file) to isolate the bias/intercept term from an empty or animal-free input, then vary the count of each animal word one at a time while holding others at zero to isolate each coefficient via simple differencing (score(count=1) - score(count=0) = weight). Confirm linearity by testing count=2 and count=3 for each word. Also test mixed-case and punctuation-adjacent forms ("Cat!", "dog.") to confirm the tokenizer's normalization matches the stated rules (lowercase, strip .,!? , whole-word match) — a naive attempt often fails by using substring matching instead of whole-word matching, or by not stripping trailing punctuation before comparison, causing systematic tokenization mismatches on real text. Once you have all four names and weights plus bias, implement in Bash: normalize text with `tr` for lowercase and `sed`/`tr -d` to strip punctuation, tokenize on whitespace, count each target word with `grep -ow` or `awk`, then compute the weighted sum with `bc` or `awk`, printing only the final number with two decimals via `printf`.