Work top-down through the pipeline and verify each stage before moving to the next. First fix `/app/api.py`: it connects to Redis using a docker-compose-style hostname (e.g. `redis`) that doesn't resolve locally — change it to `localhost`/`127.0.0.1` to match the Redis instance `start_services.sh` starts on this host, then run the script and confirm `curl http://localhost:8000/data.csv` actually returns data before proceeding; save it to `/home/user/train.csv`. For the tokenizer, implement exactly: lowercase, strip non-alphanumeric-non-space characters (e.g. via regex), split on whitespace — apply this identically in both `train_model.py` and `predict.py` (factor it into a shared function or duplicate it verbatim) since any drift between training and inference tokenization will blow up the MSE. Compute `num_tokens`, `num_unique`, `char_per_token` (guard the empty-token case), then log Pearson correlations of these features vs `resolution_time` to the MLflow server at `http://localhost:5000` under experiment "Support_Analysis". Train `Ridge()` with default params on just those three features, save with `joblib.dump`, and write `predict.py` to reload the model, regenerate features the same way, and print `round(mse, 2)` as the final line only. Manually run `predict.py` on your own train.csv to sanity-check the MSE is reasonably low before considering the task done.