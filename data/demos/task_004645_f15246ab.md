Build and test the C sanitizer in isolation before wiring up the live pipeline. Use POSIX `<regex.h>` with tight anchored patterns so you don't over-match: SSNs need exactly `"ssn": "DDD-DD-DDDD"` (require the surrounding quotes and key literal in the match, not just three digit groups, so fields like `"product_code": "123-45-6789"` are left untouched) — replace only the value with `XXX-XX-XXXX`. For email, match `"email": "local@domain"` and replace the local part before `@` with `***`, keeping everything from `@` onward, so a `message` field mentioning an address is not touched since its key isn't `email`. For the timestamp, parse `YYYY-MM-DDTHH:MM:SSZ` with `sscanf` and insert `"time_bucket": "YYYY-MM-DDTHH:MM"` right before the final `}` on that line (use string manipulation, not a full JSON parser, since input is one object per line). Validate against both corpora directly (`./sanitizer < clean_logs.txt` and `< evil_logs.txt`) and diff against hand-checked expectations before touching the network side. For the pipeline script, chain `nc localhost 9001 | /home/user/sanitizer | ...` and push each output line to Redis individually (e.g. via a `while read` loop calling `redis-cli rpush cleaned_logs "$line"`, since `-x rpush` reads only one value from stdin per invocation, not one per line).
