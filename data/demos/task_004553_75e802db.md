Build this as five independent, individually-testable artifacts rather than one big script. First write `telemetry_generator.py`: read the version once at startup (default "v0.0" if `version.txt` is absent), then loop forever appending one JSON line per 0.1s to `logs/telemetry.log`, calling `flush()` (and ideally `os.fsync`) after each write so rotation sees fresh data. Write the systemd unit with an absolute `ExecStart=/usr/bin/python3 /home/user/observability/telemetry_generator.py`, `WorkingDirectory=`, and `Restart=on-failure` (or `always`) under `[Service]`, saved at the exact user-unit path given. For `logrotate.conf`, target the log by its literal path and set `size 1k`, `rotate 3`, `compress`, and avoid directives needing root (no `su`/`create` as another user). For `deploy.py`, iterate the three versions in order and for each: overwrite `version.txt`, `pkill -f` the generator, relaunch it as a background subprocess (e.g. `subprocess.Popen` with no wait), sleep 2s so the log grows past the rotation threshold, then invoke `logrotate -f -s <state file> <conf>` as a subprocess and check its return code. Finally actually run `deploy.py` and confirm `.gz` rotated files appear before finishing.
