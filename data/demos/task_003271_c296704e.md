Start by reading `/app/baseline.py` carefully to understand its exact query semantics before touching performance — note which side of the join (`source` vs `target` nodes) currently gets filtered by `type != 'bot'` and which does not; this asymmetry is the kind of subtle bug that's easy to copy forward unnoticed. Run `tesseract /app/whiteboard.png` (or another OCR tool) to extract the business-logic note the developer left, and treat what it says as a correctness requirement that must be reflected in your query, not just documentation to skim. Rewrite the logic as a single SQL query (or a small number of set-based queries) that joins `nodes` and `edges` with all required filters applied to both endpoints of each edge, and aggregates `weight * target_value` per region directly in SQLite using `GROUP BY` and `SUM`, rather than looping row-by-row in Python issuing one query per node (the N+1 pattern that makes the baseline slow). Validate your output JSON structure matches the baseline's schema (region -> total) before optimizing further, then benchmark `/app/optimized.py` end-to-end to confirm it is dramatically faster than the baseline's per-row query pattern.
