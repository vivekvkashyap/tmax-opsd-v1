Do Part 1 first since Part 2 depends on the compiled tool: inspect the Makefile in /app/string_utils-1.2.0, change `CC=clang` to `CC=gcc` (or whatever compiler is actually installed), and add `-lm` to the link line since str_tool.c uses math.h/pow() — verify with `make` and confirm `str_tool encode "test"` runs without a linker error before moving on. For the DSL interpreter, treat it as a small state machine: split the input on ` | `, iterate commands, and maintain variables in an associative array (bash `declare -A`). Handle quoting carefully for SET/APPEND values that may be quoted in the input. Implement SORT and MERGE by turning the comma-separated string into a real list (`tr ',' '\n'`), sorting, and for MERGE deduplicating (`sort -u`) before rejoining with commas. For URLENCODE, shell out to the compiled str_tool. For JSON, emit keys in sorted order (`sort` on variable names) and build valid JSON manually or via `jq -n --arg` to guarantee proper escaping — don't hand-roll string concatenation for JSON output since it will break on special characters the fuzzer is likely to generate. Test against many hand-constructed DSL strings covering each command and command-chaining before considering it done, since correctness is judged by exact output equivalence across many random programs.