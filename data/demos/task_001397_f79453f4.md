Parse the log line by line as JSON, extract `headers.Authorization`, and strip the `Bearer ` prefix before splitting the JWT on `.` into its three parts. Decode only the header segment (first part) using base64url with padding restored as needed (Python's `base64.urlsafe_b64decode` requires the input length to be a multiple of 4 — pad with `=` before decoding, or use a helper that tolerates missing padding), then JSON-parse it and check the `alg` field case-insensitively against `"none"` (lowercase the value before comparing) — do not attempt to verify any signature. For every token where `alg` matches, decode the payload segment the same way and pull out `username`; accumulate these into a set to dedupe, then sort alphabetically and join with commas to build the exact string to encrypt. Load the Fernet key bytes from `/home/user/rotation_key.key` exactly as stored (don't re-encode or strip whitespace), construct `Fernet(key)`, call `.encrypt()` on the UTF-8 bytes of your comma-joined string, and write the raw encrypted bytes to `/home/user/revocation_list.enc` in binary mode with nothing else appended.