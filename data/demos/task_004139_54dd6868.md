Build this as a straightforward two-vector program, but get the tie-break and rounding rules exactly right since those are the parts naive attempts miss. Steps: (1) install gcc/make; (2) in C, read `vocab.txt` into an ordered array of words (order defines the vector dimensions); (3) parse `dataset.csv` (skip the header), and for each row tokenize `text` by spaces, building a term-frequency count vector indexed by vocab position (words not in vocab are ignored); (4) compute cosine similarity of doc id=1's vector against every other doc's vector using dot-product / (||v1||*||v2||), guarding against division by zero for all-zero vectors (similarity should be 0 in that case, not NaN); (5) sort candidates by similarity descending, and for exact ties sort by ascending numeric `id` (not string comparison) — implement this as a proper two-key comparator in `qsort`, not just relying on stable order; (6) take the top 3 excluding id=1, format each similarity with exactly 4 decimal places (e.g. `printf("%.4f")`), write header `id,similarity` plus rows to `/home/user/output/results.csv`, creating the output dir first with `mkdir -p`. Compile with the exact specified gcc command and run it.