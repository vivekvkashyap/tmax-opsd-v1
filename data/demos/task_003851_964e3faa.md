Design this around explicit polling/locking rather than trusting fsnotify events alone, since the executable-bit toggle and lock timing are the actual correctness signals. Initialize a Go module in /home/user, add github.com/fsnotify/fsnotify, and set up a watcher on /home/user/incoming_data/ for create/write events — but on every event (or periodically as a fallback), re-check the file's current permission bits via os.Stat rather than assuming the event itself proves readiness, since the simulator flips the owner-executable bit off only once fully written; only consider a .csv file a candidate when that bit is clear. For each candidate, open it and call syscall.Flock(fd, syscall.LOCK_EX|syscall.LOCK_NB) — if it returns EWOULDBLOCK/error, skip and retry later rather than blocking, since the simulator may still hold its own lock on a file that already has -x briefly set. Once locked, read the file line by line, sum the integers, unlock/close, append `<basename>:<sum>\n` to processed_sums.log, then remove the file — do the append+delete only after confirming the sum was fully computed to avoid partial/duplicate processing. Compile to /home/user/aggregator, start it in the background before running simulator.sh, and after the simulator's ~10s finishes, confirm processed_sums.log has all three expected lines and incoming_data/ is empty; if files linger, your polling/backoff interval is likely too slow or the lock retry logic isn't re-triggering.