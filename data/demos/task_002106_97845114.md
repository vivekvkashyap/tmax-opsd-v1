Build this as a real pipeline, validating each stage before the next. Start services via `/app/start_services.sh` and confirm Postgres (5432) and the Flask API (5000) are up. Extract with `curl`/`jq`, following `next_page` until `null` — don't assume a fixed page count. Load rows into a Postgres table (`employees(id, manager_id, salary)`) in `org_db`. For "Total Reporting Salary," write a recursive CTE that walks each employee down through all descendants and sums salaries — e.g. build `(ancestor_id, descendant_id)` pairs recursively, then group by ancestor and sum salaries of the subtree including the employee itself. Export ordered by `id` ascending, no header, two columns, to `/home/user/total_salaries.csv`. Wrap extract→load→transform→export in `/home/user/etl.sh` and run it, then spot-check a few rows by hand against the org tree to catch subtree-sum errors (e.g. excluding the employee's own salary, or missing deep descendants) before trusting the 100%-accuracy requirement.