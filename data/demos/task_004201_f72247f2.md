Diagnose the vendored server before touching anything else: read `/app/tiny-embed-api`'s source for an obviously misspelled stdlib import and a hardcoded host/port that won't be reachable externally — fix the import, change the bind host to 0.0.0.0 and the port to 8080, then start it and curl `/embed` with a sample text to confirm you get back a 3-float list before moving on (do not reimplement the embedding logic yourself; the fixed vendored code already computes it deterministically from the input text). Write a small client script that reads `/app/data/corpus.txt` line by line, POSTs each to your running `/embed`, and collects the N×3 embedding matrix — then compute the Pearson correlation matrix of the 3 columns with `numpy.corrcoef` (transpose appropriately) rounding to 4 decimals. For benchmarking, time 100 sequential POSTs to `/embed` with the first sentence and average the elapsed seconds. Finally stand up a second stdlib HTTP server on 0.0.0.0:8081 whose `/results` GET handler returns the exact JSON schema requested, embedding your precomputed correlation matrix and latency number. Leave both servers backgrounded and curl both ports yourself to confirm they answer correctly before finishing.