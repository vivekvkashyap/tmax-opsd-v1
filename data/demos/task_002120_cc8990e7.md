Work out the SQL logic carefully before writing indexes — get the metric right first. Resolve `omega_node`'s `user_id` from the `users` table, then find direct followers via `SELECT follower_id FROM follows WHERE followee_id = ?`. For level-2 followers, join `follows` again to find everyone who follows any direct follower (`follower_id` of rows whose `followee_id` is in the direct-follower set), then explicitly exclude both the target user's own id and anyone already in the direct-follower set using `NOT IN` or a `LEFT JOIN ... WHERE IS NULL`, and use `DISTINCT`/set semantics since a person could follow multiple direct followers but should only count once. Compute `direct_count * 1.0 + level2_count * 0.5` and write just that number (verify it prints as `4.5`, not `4.500000` or `4.5\n\n`) to `/home/user/reach.txt`. For optimization, since every query filters/joins on `follows.follower_id` and `follows.followee_id`, create indexes on both columns (e.g. `CREATE INDEX idx_follows_followee ON follows(followee_id); CREATE INDEX idx_follows_follower ON follows(follower_id);`), run them against the live `network.db` (not just write the SQL to a file), and also save the exact statements to `/home/user/indexes.sql`. Confirm creation with `SELECT name FROM sqlite_master WHERE type='index'`.