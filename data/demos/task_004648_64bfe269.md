Implement this as an interpreter loop over a byte array decoded from each job's hex string. Maintain an `int32_t stack[16]` and a separate `api_calls` counter reset per job. Switch on the opcode byte: for `PUSH` (0x01), read the *next* byte and sign-extend it as `int8_t` (not `unsigned char`, or negative pushes like `0xFB` come out wrong) before pushing. Check stack bounds (overflow at 16, underflow at 0) before every push/pop. Implement integer sqrt yourself (binary search or Newton's method), halting ERROR immediately on a negative input. For CHECK_API, increment the counter first — exceeding 3 halts RATE_LIMIT before the push; otherwise push 1 or 0 by sign. Unknown opcodes halt ERROR. After each job reaches a terminal state, write `JobID: <top-of-stack, or ERROR, or RATE_LIMIT>` (0 if stack empty on clean HALT), then reset VM state for the next job. Compile, run against the real input, and manually trace a couple of jobs by hand to confirm sign handling and the off-by-one on the API limit before trusting the output.
