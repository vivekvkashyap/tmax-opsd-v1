Start by writing a minimal WAV reader (Go's standard library has no WAV decoder, so parse the RIFF/fmt/data chunks yourself or use a small dependency) and confirm you can read the correct total sample count and that it divides evenly into 800-sample (0.1s) windows. For each window, run an FFT (via `go mod init` + `go get github.com/mjibson/go-dsp/fft`) and find the magnitude peak in the frequency bins that correspond to the audible range you care about — convert bin index to Hz using `bin * sampleRate / windowSize`, and be careful to only search bins below the Nyquist frequency and to ignore the DC (0 Hz) bin, which is a common source of a completely wrong answer. Map the detected peak frequency to the nearest of the four known base frequencies (A/C/G/T) by minimum absolute difference — this tolerates the noise mentioned in the setup better than exact matching. Use goroutines with a `sync.WaitGroup` (or channel-based fan-in) to process windows concurrently, but make sure you reassemble the per-window results in the correct original order before writing the FASTA output, since concurrent processing can complete out of order. Write the header `>sequence_1` followed by the sequence to `/home/user/decoded.fasta`.
