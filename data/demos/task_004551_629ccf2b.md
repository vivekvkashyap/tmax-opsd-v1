Process `/home/user/raw_reviews.csv` row by row with a simple CSV line split (comma-separated, three fixed columns, no embedded commas expected). For cleaning: lowercase the text, filter to keep only [a-z0-9 ], then collapse runs of spaces to one and trim leading/trailing space — do this in that order or you'll leave doubled spaces where punctuation was removed. Track the previous *valid* row's user_id in a variable (not the previous raw row) and skip+count any row whose user_id matches it before doing any further processing on that row. Count words in the cleaned text by splitting on single spaces (empty string = 0 words). Maintain a small fixed-size queue (e.g. a `std::deque` capped at 3) of word counts from valid rows only, and compute its average with `std::fixed << std::setprecision(2)` each time you emit a JSONL line. Build each output line by hand-concatenating the JSON fields in the exact key order/spacing shown in the spec — don't rely on an external JSON library. At the end, write the single `[INFO] Processed <X> valid records. Skipped <Y> duplicate records.` line to the log with the final counts.
