Do this as a straightforward pandas pipeline, applying each rule in the specified order and checking intermediate state along the way. Load both CSVs, merge on `timestamp` (inner or outer as appropriate — both share the same timestamps here) and sort ascending. For outlier handling, mask `temp` values where `temp > 100.0` or `temp < -50.0` to `NaN` using boolean indexing (`df.loc[mask, 'temp'] = np.nan`) — do this only on `temp`, not the other columns. Then apply forward-fill (`df[cols].ffill()`) across all four metric columns to propagate the last valid value into any NaN slot created by the outlier step (or pre-existing missing values). Compute the Pearson correlation matrix (`df[metric_cols].corr()`) over the cleaned data, then find the pair of *distinct* columns with the highest *absolute* value off the diagonal — iterate over the upper triangle of the matrix rather than the full matrix to avoid comparing a column with itself or double-counting symmetric pairs. Sort that pair's two names alphabetically before writing `var1,var2,0.XXX` (3 decimals) to the output file. Finally, `mkdir -p` the two required directories, `tar -czf` the raw_data folder into the archive path, and only then `rm -rf` the original raw_data directory — do the deletion last, after the archive is confirmed written.