Parse the 128-byte header record by record: read exactly 128 bytes with `std::cin.read`, stop cleanly (break, don't error) when fewer than 128 bytes remain (end of stream). Extract the path as a C-string from bytes 0-99 (stop at the first `\0`, don't assume it fills the field), and the little-endian uint64 size from bytes 100-107 with `memcpy` or manual byte-shifting — do not assume host endianness matches, build it explicitly byte-by-byte to be safe. Before extracting each entry, check whether its path contains the substring `"shared/shared/"`; if so, stop the loop entirely without writing that file or anything after it — this is what breaks the circular-symlink-style infinite growth. For each accepted entry, create parent directories with `std::filesystem::create_directories` before opening the output file, then read exactly `size` bytes of payload and write them out. When appending to the log, open the log with the process-level fd, then wrap the write in `fcntl(fd, F_SETLK/F_SETLKW, ...)` or `flock(fd, LOCK_EX)` before writing the `path: size bytes\n` line, and release the lock immediately after — do this per-entry, not once for the whole run, to correctly emulate concurrent-safe appends.