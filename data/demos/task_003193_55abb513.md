Implement this as a strict sequential pipeline in Python using scipy, matching each spec detail precisely since the grader checks numeric values to 4 decimals. (1) Fit `scipy.stats.gaussian_kde(reference_values)` with default bandwidth (don't override `bw_method`). (2) The KDE has no closed-form CDF, so define a CDF via `kde.integrate_box_1d(-inf, x)` and use a root finder (`scipy.optimize.root_scalar` or `brentq`) to solve `cdf(x) - 0.90 = 0` over a bracket wide enough to contain the root (e.g. based on the data's min/max range) — don't approximate this with `np.percentile` on raw samples, that's not the KDE's percentile. (3) Filter `batch.csv` values `<= threshold`. (4) Compute the mean of the filtered data, then get a 95% CI via `scipy.stats.bootstrap` with `method='BCa'`, `n_resamples=1000`, and `random_state=42` exactly as specified — parameter mismatches will shift the interval and fail tolerance checks. Round all four outputs to 4 decimal places and write them to `/home/user/metrics.json` under the exact keys requested. Run the script and inspect the JSON before finishing; don't touch the original CSVs.
