Run `tesseract /app/backup_policy.png -` (try `--psm 6` if output is garbled) to OCR the corporate path policy, then transcribe the numbered rules verbatim before coding — don't guess at wording. Encode each rule as an explicit, separately-testable check in C rather than one big regex: (1) prefix match on the required directory, (2) `strlen()` bound, (3) exact suffix match against the allowed extensions (careful: match the extension itself, not just "contains"), (4) reject any occurrence of `..` anywhere in the string, (5) walk the filename segment (between the last `/` and the extension) and reject on any character that isn't alnum or `_`. Treat argv[1] as untrusted: check length before indexing, don't use system()/popen() on it, and reject embedded NUL-adjacent shell metacharacters as part of rule 5 rather than a special case. Validate locally against `/app/corpus/clean.txt` (must all ACCEPT) and `/app/corpus/evil.txt` (must all REJECT) using your test_restores.sh, but remember the hidden adversarial set will include trickier edge cases (borderline lengths, near-miss extensions, traversal buried mid-path) — so implement each rule exactly and independently rather than tuning to pass only the visible samples.