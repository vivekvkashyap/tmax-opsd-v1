Your real deliverable is a Python script that behaves identically to `/app/oracle_bin` under fuzzing, not necessarily a repair of the legacy code — you may use the legacy files as a starting point or discard them entirely. First get *some* working baseline by black-box probing the oracle binary: run `/app/oracle_bin` on a handful of crafted strings (single chars, then short sequences) and note the outputs, since that's the ground truth you must match, not the legacy Python's behavior. When examining `/home/user/legacy/scorer.py`, notice the recursion has no proper base case for `idx >= len(s)` (it only bails past `len(s)*2`, and mishandles the IndexError by recursing without advancing idx — that's the infinite-loop/RecursionError bug) and that it also applies a `% 10000` reduction that's suspicious versus a rounder modulus; test candidate moduli (10000 vs other nearby round numbers) against oracle outputs on strings long enough to make the modulus matter. Rewrite cleanly in `/home/user/solution.py` as an iterative (not recursive) loop over characters with proper even/odd branching and the correct modulus discovered from your probing, then validate it yourself against `/app/oracle_bin` on many random uppercase-alphanumeric strings before considering it done, since the real test fuzzes hundreds of them.
