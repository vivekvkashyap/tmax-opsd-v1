Decompose this into independently testable stages before wiring the TCP server on top. (1) Use `ffmpeg` from the shell to extract frames from `/app/signal.mp4` to `/tmp` (e.g. one PNG per second), then write/verify a small C++ routine that reads pixel data (a lightweight PNG/PPM decode, or convert frames to raw grayscale with ffmpeg to simplify parsing) and computes the mean pixel value per frame — print these intermediate averages to confirm they look sane before moving on. (2) Inspect `/app/reference.db` with the `sqlite3` CLI (`.schema`, `.tables`, `SELECT * FROM ...`) to reverse-engineer the existing table that maps brightness ranges to signal names — do not assume its structure, read it directly. Then have your C++ program (via the sqlite3 C API) create `frame_data(frame, raw_brightness)` and insert your computed values. (3) Write the moving-average query using SQLite's window function syntax `AVG(x) OVER (ORDER BY frame ROWS BETWEEN 1 PRECEDING AND 1 FOLLOWING)`, which naturally handles the boundary frames by shrinking the window — no special-casing needed. Join the smoothed value against the brightness-range table using a BETWEEN condition. (4) Only after the query is verified correct via manual sqlite3 checks, add the POSIX-socket TCP server that listens on 0.0.0.0:8000, waits for the literal `REPORT\n`, replies with the JSON array (round smoothed_brightness to 2 decimals), and closes the connection.
