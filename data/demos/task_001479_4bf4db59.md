Extract the hidden text from `/app/leak.png` first (OCR with `tesseract`, or view the image directly) to find the hex-encoded passphrase embedded in the fake process command line; decode that hex string to ASCII yourself (e.g. `python3 -c "print(bytes.fromhex('...'))"`) rather than guessing. Use the recovered passphrase to decrypt `/app/id_rsa` (e.g. `openssl rsa -in id_rsa -passin pass:<recovered> -out /home/user/unlocked_rsa`, or `ssh-keygen -p`), then `chmod 600` the unlocked key so ssh will accept it. For the payload, do not write a full SSH client library call — invoke the system `ssh` binary via `os.system`/`subprocess`, passing `-i <key>` and `-o StrictHostKeyChecking=no` (or the shorthand `-oStrictHostKeyChecking=no`) plus the remote command, and aggressively shorten variable names/imports/whitespace to minimize byte count. Verify the script actually prints the flag contents before golfing further, since a broken minimal script scores worse than a slightly longer working one.
