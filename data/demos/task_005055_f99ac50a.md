Work in three independent phases. First fix the C validator's Makefile: inspect it for syntax/linking errors (missing tabs, wrong flags, missing `-o` target, or a missing library link) with `make` output as your guide, then build so `/workspace/validator_src/validator` exists and can check a `.pak` file's structure. Second, reverse-engineer the PAK1 format empirically rather than guessing: run `/app/packer_oracle <dir> <out.pak>` on a small test directory you create, then hexdump the output (`xxd`) and cross-check every field against the spec given (4-byte magic, LE uint32 count, per-file LE uint16 path-len + path + LE uint32 size + LE uint32 Fletcher-32 checksum + data, entries sorted alphabetically by path) — implement Fletcher-32 exactly as specified (sum1/sum2 init 0xFFFF, 16-bit LE words, pad odd trailing byte with 0x00, mod 65535, result = sum2<<16 | sum1) and unit-test it against a small known input before trusting it. Third, write `/workspace/fast_packer.go` mirroring the oracle's CLI but parallelizing file reads and checksum computation with goroutines + a WaitGroup or worker pool (collect results, then write the header/records sequentially in sorted order to guarantee byte-identical output). Validate correctness first with diff/sha256sum against the oracle's output on the same input directory, then benchmark with `time` on a larger synthetic dataset to confirm the 2.5x speedup margin before considering it done.