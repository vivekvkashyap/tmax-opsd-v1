First recover the two hidden parameters from the audio: convert `/app/directive.wav` with `ffmpeg` to 16kHz mono WAV and run a speech-to-text tool (e.g. `whisper.cpp`, `openai-whisper`, or any offline ASR) on it, then read the transcript to find the auth token phrase and the chunk size in bytes — do not guess these values. For the daemon itself, write it in C using `socket`/`bind`/`listen`/`accept`, parsing the `AUTH: <token>\n` and `BACKUP\n` lines with a small line-buffered read loop. The trickiest part is directory traversal: use `nftw` or manual recursion with `lstat`, and detect symlink loops by resolving each symlink with `realpath`/`readlink` and checking the resolved path is a regular file that lies inside (or a valid target of) the tree — track visited inode/device pairs (st_dev+st_ino) in a set to skip anything already seen instead of naively following every symlink, which is what causes the infinite loop. Acquire the file lock on `/tmp/backup.lock` with `flock()` before streaming, write the binary format (2-byte LE path length, path bytes, 4-byte LE size, file bytes) in send() calls sized to the discovered chunk size, then send the terminating `0x00 0x00`, unlock, and keep the daemon looping to `accept()` again.