Build this bottom-up: first get the C→shared-library FFI path working standalone before wiring it into Bash. Compile `sec_check.c` with `gcc -shared -fPIC -o libseccheck.so sec_check.c`, then write a tiny bridge (a Python ctypes script that loads the .so via `ctypes.CDLL` and exposes `check_vulnerability(pkg, version)` as a callable from the command line, e.g. `python3 bridge.py pkgName version` printing 1/0) — this is far simpler than writing a C caller. Test the bridge directly against a known package/version before touching the lockfile parser. For the Bash state machine, track a `current_block` variable that's set on every `BEGIN [...]` line and cleared on `END [...]`; only call the bridge for lines encountered while `current_block == "Web-Components"` — this is where naive `grep`-based approaches fail, since they'd also match packages under `[Dev-Tools]`/`[System]` or a package name that happens to match a vulnerable one outside the Web-Components block. Collect flagged `pkg version` pairs, format as `pkg@version`, sort alphabetically, write to vuln.log. For package.json, locate the peer dependency conflict (the framework version specified doesn't satisfy the peer requirement) and bump it to the compatible/secure version specified in the task, saving as a separate resolved file without mutating the original.