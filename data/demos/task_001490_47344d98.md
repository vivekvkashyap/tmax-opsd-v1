Before optimizing, read the code carefully to find the structural bottleneck rather than guessing at micro-optimizations: look for any function called on every iteration of the hot MCMC loop that does I/O or heavy recomputation of something invariant across iterations — in particular, check whether the data-loading routine (e.g. reading the HDF5 file) is being invoked from inside the per-step likelihood evaluation instead of once up front. Fix it by loading the HDF5 dataset a single time before the loop starts (hoist it into `main` or a global/static, then pass it by reference into the likelihood function) — this must not change any of the numerical logic, RNG calls, random seed, iteration counts, or gradient-descent parameters, since those determine the exact deterministic trajectory. Compile with `g++ ... -lhdf5_cpp -lhdf5` (check `pkg-config --cflags --libs hdf5-serial` or similar if linking fails), run the resulting `/home/user/mcmc_opt`, and redirect its single stdout line into `/home/user/output.txt`. Confirm the output value is unchanged from before your edit (only speed should differ) by comparing a run before and after the fix if time permits.
