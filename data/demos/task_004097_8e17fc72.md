For Part 1, write `/home/user/detector.py` to check each cmdline argument list against three independent conditions and OR them: (a) an argument that matches `^AKIA[A-Z0-9]{16}$` exactly (anchor the regex, don't just search substring loosely); (b) an argument starting with the literal prefix `--password=` or `--secret=` (careful: `--password-file=` must NOT match, so check the exact prefix including the `=`, not just `startswith('--password')`); (c) an argument exactly equal to `-p` or `-s` where the *next* argument exists, is non-empty, and does not start with `-`. Read/write JSONL line by line, preserving `id` and adding a boolean `leak`. Test against constructed clean/evil examples covering each edge case (e.g. `-p` with nothing after it, `-s` followed by another flag) before trusting it. For Part 2, change `worker.py` to read `os.environ.get('SECRET_TOKEN')` instead of an argparse flag, and change `api.py` to pass the secret via `env={**os.environ, 'SECRET_TOKEN': secret}` in `subprocess.run` with a cmdline that no longer contains the secret; also change `app.run(host=...)` to bind `127.0.0.1` instead of `0.0.0.0`, keeping `nginx.conf`'s `proxy_pass http://127.0.0.1:5000` as the external entry point. Start services via `start.sh` and manually curl `http://127.0.0.1:8080/process` to confirm end-to-end behavior and that the worker's command line no longer contains the secret (check via `ps` during the request).