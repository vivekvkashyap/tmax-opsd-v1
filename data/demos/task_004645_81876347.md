Write the C program to process stdin one line at a time with `fgets`/`getline` in a loop — never read the whole file into a buffer, since the point is streaming. Parse each line by splitting on commas with `strtok`/`sscanf` into four fields, and skip (continue) any line that doesn't yield exactly four well-formed fields or where the timestamp/render_time fail to parse as integers (use `strtol` and check `errno`/endptr rather than blindly trusting `atoi`). Compile the POSIX regex `^menu_[a-z]+_[0-9]+$` once with `regcomp` before the loop (not inside it) and reuse it with `regexec` per line for efficiency. For the locale filter, compare the field with `strcmp(loc, "fr_FR") == 0`. Floor the timestamp to the hour with integer division: `(ts / 3600) * 3600`. Classify render_time with a simple threshold (`< 100` vs `>= 100`). Print each surviving record with `printf` in the exact `timestamp_hour,translation_key,performance_category` format. After compiling with `gcc -O2`, pipe the real log file through the binary and redirect to the output CSV, then spot-check row counts against a quick independent filter in a shell one-liner or Python to catch off-by-one regex/threshold bugs before finalizing.
