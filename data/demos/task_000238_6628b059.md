Start by exploring the unknown schema: use the `sqlite3` CLI or Python's `sqlite3` module to run `.tables`/`SELECT name FROM sqlite_master WHERE type='table'`, then `PRAGMA table_info(<table>)` on each candidate to find the one with user id, amount, and timestamp columns. Once identified, write the SQL using a window function — `SUM(tx_amount) OVER (PARTITION BY user_id ORDER BY tx_ts ROWS BETWEEN 2 PRECEDING AND CURRENT ROW)` gives the rolling sum over the current row and its two immediate predecessors in timestamp order, which is exactly "any 3 consecutive transactions." A common mistake is using a centered or trailing/leading window incorrectly, or forgetting `ORDER BY tx_ts` inside the window clause (SQLite window ordering must be explicit). Filter for `rolling_sum > 50000`, then take `DISTINCT` user_ids from the filtered rows. In Python, execute this via `sqlite3.connect`, fetch results, sort the distinct user list alphabetically, and dump to JSON with the exact `{"flagged_users": [...]}` structure — don't hardcode any user IDs, let the query drive the output.
