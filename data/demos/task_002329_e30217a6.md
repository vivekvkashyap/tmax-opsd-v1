Separate this cleanly into video-to-matrix extraction and a C query engine, and get the bit-exact semantics of "union across frames" right first. Extract frames from `/app/optical_graph.mp4` (e.g. `ffmpeg -i ... -pix_fmt gray frame_%04d.png`, or decode directly in Python/OpenCV as grayscale), and for each 128x128 frame set `adj[y][x] = 1` whenever that pixel's intensity is strictly greater than 128 in ANY frame — accumulate with a logical OR across all frames, don't just use the last frame or an average. Write the resulting 128x128 matrix as raw bytes in row-major order (source-major, i.e. byte at offset `src*128+dst`) to a file of exactly 16384 bytes — verify the byte count with `wc -c` or `stat`. For the C query engine, `fread` the whole file into a `[128][128]` buffer (matching the same row-major layout), loop reading integers from stdin with `scanf("%d", &n)` until EOF, bounds-check `n` (print `ERROR` for out-of-range) and otherwise sum row `n` for out-degree and column `n` for in-degree, printing `n|in|out`. Since the grader fuzzes thousands of queries against a reference implementation, be precise about which axis is source vs destination and test the engine manually with a few hand-computed queries before finishing.
