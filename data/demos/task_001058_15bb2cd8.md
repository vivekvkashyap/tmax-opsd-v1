Write init_service.sh so every step is idempotent and safe to re-run: use `mkdir -p` for both directories; before touching the passwd file, check `[ -f /home/user/service_data/passwd ]` and if so `cp` it to /home/user/backup/passwd.bak (preserving its original content, not overwriting an existing backup unexpectedly — a straight cp is fine since the task only requires the latest pre-change snapshot). For the idempotent user line, `grep -q '^appuser:' /home/user/service_data/passwd` first (create the file if absent, e.g. via `touch`), and only append the exact appuser line if that grep fails — never append unconditionally, or reruns will duplicate the entry. Generate verify.py as a separate small Python script using a try/except around the file open, parsing colon-separated fields per line, matching the first field against "appuser", and printing `UID: <field3>`; on FileNotFoundError print exactly `Error: passwd file missing`. After chmod +x and running the shell script, run verify.py and redirect stdout with `>` into result.log, then double check the log's exact content and trailing newline.