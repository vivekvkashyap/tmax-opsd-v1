Start by extracting the actual policy text: run OCR (e.g. `tesseract /app/policy.png stdout`, or open and read the image) to recover exactly what compliance rule is being described — do not assume a specific rule, read it directly from the image. Separately, inspect a few files in `/app/corpus/clean/` and `/app/corpus/evil/` to confirm the JSON schema (array of `{action, source, target, role}` objects) and to sanity-check your understanding of what distinguishes clean from evil examples. Once you have the policy text, design `/home/user/audit.sh` around it: for each GRANT event with the role the policy names, build a directed edge `source -> target`, and detect whether that edge set (restricted to that role) contains a cycle — a standard approach is topological sort (`tsort` will fail/report a cycle) or a DFS-based cycle detector over adjacency built with `jq`. Parse the input JSON with `jq` to filter matching `action`/`role` events and emit edges, feed them into your cycle-detection method, and print `EVIL` if a cycle exists among the relevant edges, else `CLEAN`. Make the script take exactly one path argument and be executable. Validate your script against both provided corpora directories yourself (loop over each file, check output) before finishing, since grading requires 100% accuracy on both.