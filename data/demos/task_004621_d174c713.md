Build this purely with awk/shuf/bc since Python/R are unavailable. Structure: (1) load the CSV data rows (skip header) into a working array/file with `tail -n +2`; (2) for each of N iterations, use `shuf -n <numrows> -r --input-range=1-<numrows>` (or `shuf` on line numbers with replacement via repeated random draws) to sample row indices with replacement equal to the original row count, then pull those rows via `awk 'NR==FNR{...}'` or `sed -n` into a temp bootstrap sample; (3) compute Pearson's r for that sample in a single `awk` pass — accumulate sum(x), sum(y), sum(xy), sum(x^2), sum(y^2), n, then apply the correlation formula with awk's built-in `sqrt()`, printing one r value per iteration to a results stream; (4) after collecting all N r values, compute their mean and *population* standard deviation (divide by N, not N-1) in a final `awk` aggregation pass. Format both to exactly 4 decimals with `printf "%.4f"` in awk, and write the two lines in the exact `Mean:`/`SD:` layout to `/home/user/result.txt`. Make the script executable and actually run it with N=500 to produce the final file, watching for integer-division pitfalls (always route arithmetic through awk or `bc -l`, never bash's `$(( ))` for anything non-integer).
