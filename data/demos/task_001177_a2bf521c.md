Tackle the three pieces separately and verify each before integrating. Run `tesseract /app/secret_config.png stdout` (try `--psm 6` if segmentation misreads it), then parse out the value following "API_KEY:" and print it to confirm it looks right before hardcoding it into the server's auth check. For `url_decode`, a `%`-escape overflow usually means it doesn't bound-check the output against input length, or reads two hex digits after `%` without checking they exist/are valid — fix by sizing the output buffer to the input length (decoding only shrinks) and validating hex digits before consuming them. For `base64_encode`, the classic off-by-one is under-allocating output — base64 size is `4 * ceil(n/3) + 1` for the null terminator, so recompute the malloc with that formula. Write a small standalone test harness calling both functions with edge cases (empty string, truncated `%`, lengths not divisible by 3) before wiring them into the socket server, then compile with `gcc -o /app/server /app/server.c`, bind to 127.0.0.1:8080, and confirm auth rejects missing/mismatching Bearer tokens before finishing.
