Implement this as a straightforward numerical simulation in C++, mirroring the spec step by step rather than trying to be clever. Parse the 3x3 matrix from `cov_matrix.txt` and add `1e-5` to each of the three diagonal entries to regularize it before decomposing — do this addition first, not after computing Cholesky. Implement Cholesky decomposition manually for a 3x3 lower-triangular matrix (a few closed-form formulas suffice) or link Eigen3 if available; verify `L*L^T` reproduces the regularized matrix by hand-checking one case. Read the 1000 rows of `z_samples.txt` in order, and for each row compute `theta = mu + L*z` using ordinary matrix-vector multiplication (mu = [0.5, 1.0, 2.0]). Simulate the ODE with a plain forward-Euler loop: `y_{i+1} = y_i + dt*(-theta0*y_i + theta1*sin(theta2*t_i))`, using `t_i = i*dt` for exactly 200 steps starting from y=5.0, and collect y at t=2.0 for each sample. Average the 1000 final values and print with exactly 4 decimal places (`printf("%.4f", avg)` or `std::fixed << std::setprecision(4)`) to `result.txt`. Compile with `g++` and run it to actually produce the file — don't just leave source unbuilt.