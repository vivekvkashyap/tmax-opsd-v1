Fix the service wiring before writing any C++. Read `/app/services/flask_service/config.json` and `/app/start_services.sh` to find the port mismatch between where Redis actually listens and where the Flask config expects it, correct the config's `redis_port`, seed the key with `redis-cli -p <port> SET threshold 0.85`, then run the startup script and verify `curl http://127.0.0.1:5000/threshold` returns the value before touching the C++ side. For `/home/user/csv_filter.cpp`: parse the CSV by header name (don't assume fixed column order beyond what's given), extract `val_X`/`val_Y` as doubles, and compute Pearson's r manually — accumulate sums of x, y, x*y, x^2, y^2 in one pass (or compute mean first, then covariance/variances) using double precision throughout to avoid catastrophic cancellation on large corpora. Fetch the threshold via a libcurl GET to `http://127.0.0.1:5000/threshold` at runtime (don't hardcode 0.85) and compare `fabs(r) > threshold` strictly greater. Append `<filename>: EVIL` or `<filename>: CLEAN` to `/home/user/experiment_log.txt` and exit 1/0 accordingly. Before trusting it, run your compiled binary over every file in both `/app/corpus/clean/` and `/app/corpus/evil/` yourself and check exit codes match expectations, fixing any correlation-formula or off-by-boundary issues you find.