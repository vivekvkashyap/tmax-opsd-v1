Do the pieces in order and verify each in isolation before wiring them together. First, run `tesseract /app/reference_code.png stdout` (installing tesseract-ocr if missing) and strip whitespace from the result — this recovered string is your auth token; don't hardcode a guessed value, use exactly what OCR returns. Second, compile the C file with `gcc -shared -fPIC -o /app/libecc.so /app/ecc.c` and sanity-check the symbol is exported (`nm -D libecc.so | grep compute_ecc`). Third, build the Flask/FastAPI server: load the library via `ctypes.CDLL('/app/libecc.so')`, declare `argtypes`/`restype` on `compute_ecc` explicitly (it takes a `const char*` and `int` length, returns `uint32_t`) so ctypes doesn't mis-marshal the return value, and pass the string as `data.encode()` with its byte length. In the `/process` handler, compare the `Authorization: Bearer <token>` header against the OCR-derived token exactly, returning 401 on mismatch before touching ctypes. Bind and run the server on 127.0.0.1:9090 in the background, then smoke-test with `curl` against both a bad and a good token to confirm the 401/200 paths work before finishing.
