For TLS, generate the key and self-signed cert in one step with `openssl req -x509 -newkey rsa:2048 -keyout /home/user/pki/server.key -out /home/user/pki/server.crt -days 365 -nodes -subj "/CN=localhost"` (create the pki directory first). For the C++ vulnerability, read validate_token carefully: it currently returns true whenever the token contains "|sig:none" (an auth-bypass), so remove that branch entirely and keep only the check that the token contains/ends with exactly "|sig:VALID_MAC" — verify with g++ compile then manual test runs on both token strings and their exit codes before moving on. For run_secure.sh, invoke bwrap with the three required flags (`--unshare-net`, `--ro-bind / /`, `--ro-bind /home/user/pki /home/user/pki`) followed by the compiled server binary and its args — read bwrap's man page/help if the flag ordering or `--` separator before the command trips you up. For the SUID audit, use `find /home/user/test_env -perm -4000 -type f` to locate files with the setuid bit (not just chmod 755 executables), and write only the matching absolute path(s) to suid_audit.txt, one per line.