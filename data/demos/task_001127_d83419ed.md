First recover the secret: run `strings /home/user/legacy_worker | grep COMPLIANCE_KEY_` (or grep raw bytes) to pull out the printable secret string embedded in the binary — do not try to disassemble it. Then write the Python script: read each line of the log with a regex like `--token (\S+)` to find candidates, split each candidate on `.` and confirm it has exactly 3 parts. Base64url-decode the header and payload parts (remember to pad with `=` to a multiple of 4 before decoding — `s += '=' * (-len(s) % 4)`), and treat decode/JSON-parse failure as "not a valid JWT" (leave untouched). To validate, recompute `hmac.new(secret.encode(), f"{header_b64}.{payload_b64}".encode(), hashlib.sha256).digest()`, base64url-encode it, strip `=` padding, and compare against the token's third segment using `hmac.compare_digest` (constant-time, but plain `==` also works functionally). Only replace the exact matched token substring with `[REDACTED]` in-place on the line, leaving everything else (flags, other args) untouched. Write the transformed lines to `/home/user/clean_audit.log` and actually execute the script so the output file exists.
