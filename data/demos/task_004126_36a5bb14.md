Use `scipy.stats.wasserstein_distance` on the two 1-D arrays loaded via `numpy.loadtxt` from the given CSV files — do not hand-roll an EMD approximation, since the grader recomputes the exact value with scipy and compares against your rounded string. Round with Python's `round(dist, 4)` or an f-string like `f"{dist:.4f}"` and write that exact string to `/home/user/wasserstein.txt` with no extra whitespace/newline mismatches expected beyond what a simple write produces. For the plot, use `matplotlib.pyplot.hist` twice on the same axes with `bins=50`, `alpha=0.5`, and add labels/legend as specified, then `savefig('/home/user/distributions.png')`. Put all of this logic in `/home/user/analyze_sim.py` as a standalone, re-runnable script (no hardcoded interactive state), then actually execute it with `python3 /home/user/analyze_sim.py` so the three deliverables (script, txt, png) all exist on disk — a script that would produce correct output if run is not sufficient, it must have been run.
