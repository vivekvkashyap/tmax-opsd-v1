Build this as a careful pipeline rather than one giant one-liner, and use `-print0`/`while IFS= read -r -d ''` (or bash arrays with null-delimited find) throughout so filenames with spaces/special characters survive. Steps: (1) `find /home/user/data_volume -name '*.json' -mtime +30` to get candidates by mtime; (2) for each candidate, skip it if a sibling file named `<same-path>.lock` exists in the same directory; (3) parse remaining files' JSON (use `jq` or python) and keep only those where `.status == "archivable"`; (4) for kept files, rewrite the status to `"archived"` in place while preserving other JSON content (e.g. `jq '.status="archived"' file > tmp && mv tmp file`, careful to keep it valid JSON); (5) tar them into `/home/user/cold_storage.tar.gz` using paths relative to `/home/user/data_volume` (`tar -czf ... -C /home/user/data_volume <relpath1> <relpath2> ...`), then delete the originals only after confirming they were added to the archive; (6) build the CSV with header `filepath,new_status`, relative paths, sorted alphabetically by filepath (`sort`), and write it to `/home/user/archive_report.csv`.