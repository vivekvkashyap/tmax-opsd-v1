Follow the pipeline stages strictly in order and in the exact commands the spec implies, since determinism depends on it. Strip the header with `tail -n +2`, get `N` via `wc -l`, compute `train_n=$(( N * 70 / 100 ))` using bash integer arithmetic, then `head -n "$train_n"` for the initial train rows and `tail -n "$test_n"` for the test rows. Bootstrap the training set with exactly `shuf --random-source="$2" -r -n "$train_n" train_init.csv > train_boot.csv` — matching the flag order/form matters for reproducibility against a reference implementation. Compute the means of `Feature_X`/`Feature_Y` using awk over `train_boot.csv` only — never touch the test set when computing means, since that would leak information from test into the centering step. Subtract those exact training means from both the bootstrapped train set and the (untouched) test set. Finally compute sample covariance on the *test* set's centered values as `sum(Xc*Yc) / (test_n - 1)` and print it with `printf "%.4f\n"`. Make the script executable and idempotent — each run should overwrite outputs cleanly.
