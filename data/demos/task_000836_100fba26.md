OCR `/app/lab_specs.png` with `tesseract /app/lab_specs.png stdout` to recover the required motif and the MIN_GC/MAX_GC bounds — don't guess these, and re-check the OCR output against a couple of known clean/evil files in `/app/corpus/` (e.g. a clean sample's GC% should land inside your extracted range, an evil one should fail at least one check) to catch OCR misreads like a stray digit in a percentage. In `/home/user/analyze_primer.sh`, read the FASTA body by skipping the `>` header line and concatenating remaining lines into one sequence string (handles multi-line FASTA), then count G/C and A/T/G/C totals with `grep -o` or parameter expansion, compute GC% with `bc -l` or `awk` for float precision, and check motif presence with a plain substring/`grep -q` test. Apply the pass condition as: motif present AND MIN_GC <= gc <= MAX_GC (inclusive both ends) — exit 0 only then, else exit 1. Iterate over every file in both corpus directories, checking `$?` after each invocation, to confirm 100% separation before declaring done; a common bug is off-by-one on the inclusive boundary or forgetting to strip trailing newlines/whitespace before concatenating sequence lines.