Start by reproducing the bug empirically rather than guessing: build/install `research_packer` as-is, pack a small known text sample, and check the output size against the raw input size — a compression ratio near 1:1 (or worse) confirms the compressor is barely compressing. Then read through `research_packer`'s core compression module (likely where it reads input in chunks and calls into zlib/gzip) looking for a suspiciously small buffer/chunk-size constant — a tiny read-chunk size forces the compressor to flush constantly, which destroys the ratio even though the algorithm itself is correct. Fix that constant to a realistic size (tens of KB or more), reinstall the package (`pip install /app/research_packer` or `pip install -e .`), and re-verify the ratio improves dramatically on your test sample before touching the pipeline. For `process_data.py`, walk `/home/user/incoming_data/*.tar.gz`, extract each to a temp dir (or stream in-memory with `tarfile`/`zipfile`), recursively look inside for nested `.zip` archives and extract those too, find all `*.csv` files, and for each one open `research_packer pack --append /home/user/dataset.rp` as a subprocess with the CSV bytes piped to its stdin. Never modify files under `/home/user/incoming_data/`.