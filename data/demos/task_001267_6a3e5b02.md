Do this per-file, in a fixed order (patch, then hash, then update manifest, then CSV) so nothing gets hashed before patching. For the binary patch, don't use text-based `sed`/`awk` on binary files — use a byte-safe tool: `xxd` piped through `sed` on the hex dump then `xxd -r` back, or a small `perl -pe 's/\xDE\xAD\xBE\xEF\x00\x00/\xCA\xFE\xBA\xBE\x00\x00/g' -i` with the binary-safe flag, so you replace every occurrence in place without corrupting surrounding bytes or truncating on embedded NUL/newline bytes (this is the classic naive-attempt failure — treating binary content as text). Verify each replacement count matches what you expect by grepping the hex pattern before/after. After patching, run `sha256sum` on each `.bin` file to get the new digest. Update `/home/user/artifacts/manifest.json` in place with `jq` (e.g. `jq --arg name ... --arg hash ...`) so the JSON structure and key order/other fields stay intact — don't hand-edit with sed, which risks malformed JSON. Finally, iterate the `.bin` files sorted alphabetically and write `/home/user/artifacts/summary.csv` with the exact header `artifact_name,new_sha256` and one row per file using the freshly computed hashes.
