In parser.c, the buffer allocation is one byte too small for the terminator: change `malloc(len)` to `malloc(len + 1)`, and after the `fread` set `buf[len] = '\0'` before printing, so the string is properly null-terminated (fixes the UB in printf). Remove the second, duplicate `free(buf)` call — only free once, right after the string is no longer needed. In verifier.rs, `compute_checksum(metadata)` takes ownership of `metadata` by value, but `metadata.len()` is used afterward — that's the move-then-use borrow error. Fix by changing `compute_checksum` to accept `&str` (or `&String`) and pass `&metadata` at the call site, so `metadata` remains valid for the later `.len()` call. Do not alter the checksum algorithm itself. Compile the C program with gcc and the Rust program with rustc to the specified bin paths. Finally, chain them: run the parser on the binary artifact, extract the metadata text after "METADATA: " from its stdout, pass it as the verifier's argument, and write both programs' stdout lines (in order) into the two-line report file.
