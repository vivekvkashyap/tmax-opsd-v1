Combine the two data sources in Python before touching graph math: read `employees`/`departments` from `/home/user/employees.db` via `sqlite3`, and load `/home/user/transfers.json`, building a directed graph (e.g. with `networkx.DiGraph`) where an edge source->dest exists if any transfer record has that pair (dedupe multi-transfers into one edge). "Network Reach" for a node is the count of distinct other nodes reachable via any directed path — this is exactly `len(nx.descendants(G, node))` for each node (0 if the node isn't in the graph or has no outgoing paths); compute it for every employee id, including ones absent from the graph entirely (reach 0). Write results into a fresh `reach_metrics(emp_id INTEGER PRIMARY KEY, reach INTEGER)` table in the same SQLite DB. Then run a single SQL query joining `employees`, `departments`, `reach_metrics` using `DENSE_RANK() OVER (PARTITION BY department_id ORDER BY reach DESC, emp_id ASC)`, filter to rank <= 2 per department in Python or SQL, and sort final output by department_name then rank before dumping to `/home/user/audit_report.json` with exactly the five specified keys per object — trace through the transfer edges by hand for a couple of departments to sanity check reach values before trusting the pipeline.