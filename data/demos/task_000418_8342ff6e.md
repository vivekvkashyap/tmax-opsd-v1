The algorithm is fully specified in the task (per-token length-based likelihoods, sequential Bayesian update, %.4f posterior) — the real risk is losing bit-for-bit equivalence to floating-point and formatting details, so implement it precisely rather than approximately. Split `text_data` on `|`, and for each token apply `l_pos = clamp(len(token)/100.0, 0.01, 0.99)`, `l_neg = 1 - l_pos`, then update `p` with the standard Bayesian formula `p = (p*l_pos) / (p*l_pos + (1-p)*l_neg)` sequentially across tokens (guard the zero-denominator edge case by leaving `p` unchanged, matching typical C behavior). Reproduce the original numeric columns exactly as they appeared in the input (don't reformat `base_prior`, since the oracle likely echoes it via a generic float format like `%g`) and append `,posterior` with exactly 4 decimals. Before finalizing, generate small test CSVs, run them through `/app/risk_oracle` and your Python script side by side, and `diff` the outputs byte-for-byte — pay attention to trailing newlines, header handling, and float representation (e.g. `%g` vs plain `str()`) since these are common silent mismatches.
