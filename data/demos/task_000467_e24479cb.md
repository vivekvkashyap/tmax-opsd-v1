Build this as a strict pipeline and test each stage in isolation before wiring it together. (1) Load `telemetry`/`reference` from the NetCDF file with `netCDF4` or `xarray`; for each array fit `scipy.stats.gaussian_kde` on the data, evaluate it over `np.linspace(min, max, 1000)`, take the grid point with peak density as the "mode", then normalize as `(x - mode) / std`. Don't confuse KDE-derived mode with `np.mean` or `scipy.stats.mode` — those give different values. (2) Design a small objective function that: builds a Butterworth bandpass (`scipy.signal.butter(4, [low, high], btype='bandpass', fs=1000)`), applies it with `scipy.signal.lfilter` (not `filtfilt` unless specified) to normalized telemetry, and returns MSE vs normalized reference; pass it to `scipy.optimize.minimize(method='Nelder-Mead')` with the given initial guess, and clamp results into the bounds yourself since Nelder-Mead doesn't enforce them natively (reject/penalize out-of-bound trials). (3) Save the filtered array to HDF5 via `h5py` under the exact dataset name required. (4) For the audio secret, transcribe the WAV with a speech-to-text tool (e.g. `whisper`, or a locally available ASR) rather than guessing — extract the spoken digits verbatim. Then implement the Flask/FastAPI POST endpoint checking that exact auth string, returning rounded cutoffs on success and 403 otherwise, and keep the process running in the background (e.g. `nohup ... &`) so it stays reachable for verification.
