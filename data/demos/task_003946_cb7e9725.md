Treat this as a diff-against-upstream problem: fetch/inspect a clean copy of cJSON 1.7.15's `parse_array`/`parse_object` (or reason from the vendored source's structure) and diff it mentally against `/app/cJSON-1.7.15/cJSON.c` to spot the injected fault, rather than rewriting the parser from scratch — the bug is localized to error-path cleanup (allocated nodes not freed when parsing bails out partway through nested/anomalous input) and to a shared static error buffer being used instead of thread-local storage under concurrent parsing. Fix the leak by ensuring every `cJSON_Delete`/free call still happens along the early-return error paths in those two functions, and fix the race by making the error state thread-local (e.g. `__thread` or a per-call buffer instead of a file-scope static). Separately, edit the `Makefile`: it currently links `-lcjson` against the system package instead of compiling and linking the vendored `cJSON.c` — change the build rule to compile `/app/cJSON-1.7.15/cJSON.c` directly into the target and drop the system link flag. After fixing both, build `/home/user/fixed_processor` and validate before trusting it: run it under Valgrind/ASAN on a batch of deeply nested and statistically anomalous JSON inputs fed via stdin to confirm zero leaks are reported, and re-run it under concurrent/threaded load to confirm no crashes or corrupted output from the shared error-buffer race.