Start by extracting the exact binary format from the audio: transcribe `/app/format_spec.wav` (e.g. install and run `openai-whisper` or `SpeechRecognition` against the file) and read the transcript carefully for every detail — magic bytes, integer widths, byte order (endianness), and field ordering, since bit-exact fuzz testing means any mis-sized field or wrong endianness fails every case. Implement `pack_project.py` in Python using the `struct` module with an explicit endianness prefix (`<` or `>`) matching what you transcribed for every packed integer, writing raw bytes with `'wb'` mode throughout — never text mode, which would corrupt binary content. Walk the input directory with `os.walk`, compute each file's path relative to the input dir using forward slashes for portability, and sort all discovered relative paths with plain Python string sort (`sorted()`), which is already case-sensitive lexicographic, before packing so ordering matches spec exactly. Write the magic bytes, version integer, then for each file: path-length, UTF-8 encoded path, file-size as specified integer width, then raw file bytes — write everything to a `<output>.tmp` file first, flush and close it, then `os.rename` to the final output path only after the entire loop completes successfully, so a crash mid-write never leaves a partial "final" archive.