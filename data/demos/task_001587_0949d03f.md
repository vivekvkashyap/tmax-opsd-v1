First recover the scoring formula from `/app/formula_spec.png` using `tesseract` (OCR the image, then sanity-check the extracted formula by hand-computing it against one file's metadata to make sure OCR didn't mangle an operator or number). Then build `analyze.sh` as a single-pass streaming pipeline, not a per-file bash loop: use `find` to list files, then feed them through `awk` (a single invocation processing all files, using `awk 'FNR==2{...}FNR==3{...}FNR==4{...} ENDFILE{print FILENAME","score}'` style logic with `FNR`/`ENDFILE`, or `grep -h` plus `paste`/`xargs` batching) so metadata extraction and arithmetic happen in one awk process rather than forking `grep`/`cut`/`bc` per file — that forking overhead is exactly what blows the 3-second budget. Compute the formula's modulo/arithmetic in awk itself. Finally sort output by filename (`sort -t, -k1,1`) and write to `/home/user/complexity_scores.csv`; verify correctness on a handful of files by manually recomputing the score, and verify speed with `time ./analyze.sh` on the full 10,000-file directory before considering it done.