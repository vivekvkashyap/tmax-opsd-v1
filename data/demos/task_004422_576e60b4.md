Don't guess the IOC values — recover them programmatically from the video. Sample frames from `/app/evidence_041.mp4` at a fine enough rate (e.g. `ffmpeg -i /app/evidence_041.mp4 -vf fps=1 frame_%03d.png`, matching the video's actual frame rate) and OCR each frame (e.g. `tesseract`) or otherwise inspect them to find the flashed text lines revealing the SHA-256 hash and the injected CSP string; write those two extracted values verbatim into `/home/user/extracted_iocs.json` under `binary_sha256` and `injected_csp`. Compute `sha256sum /home/user/suspicious_daemon` yourself and cross-check it matches the hash you extracted from the video before trusting either. To find the hidden C2 port, don't assume a format — run `strings`/`grep` over the binary for patterns like `PORT=` or similar and confirm the recovered integer looks like a real port; write it to `/home/user/c2_port.txt`. For the CSP header, compute the exact character-by-character reverse of the injected CSP string in code (e.g. Python slicing `s[::-1]`) rather than typing it by hand, to avoid transcription errors. Then build the Python HTTP server on `127.0.0.1:8443` with `GET /status` (200, JSON status, CSP header on every response) and `POST /report` that parses the JSON body and returns 403 if `ioc_hash` equals the extracted hash, else 200 — start it in the background and verify both endpoints with curl before finishing.