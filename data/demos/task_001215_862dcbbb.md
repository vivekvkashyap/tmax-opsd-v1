Break this into independent stages and verify each before moving on. Stage 1: `grep -l '\[SECRET\]' /home/user/docs_system/drafts/*.md` to find matching files, then `sed -i 's/\[SECRET\]/[REDACTED]/g'` only on those files — keep the list of modified filenames, since step 2 needs exactly that set in alphabetical order for the merge (use `sort` on the filename list, then `cat` them into `merged_drafts.md`). Stage 2, the C++ splitter: read the whole file, split on lines exactly equal to `---SPLIT---` (accumulate lines into a buffer, flush a new chunk whenever the delimiter line is seen, skipping the delimiter itself), then RLE-encode each chunk character-by-character — count consecutive identical characters (newlines and spaces included) and emit `<count><char>`; test this against a tiny hand-computed example first (e.g. `"AABBB\n\nC"` -> `"2A3B2\n1C"`) before trusting it on real chunks. Write chunks as `chunk_N.rle` (1-indexed) into `chunks/`, creating the directory if missing. Stage 3: run `sha1sum chunks/*.rle` but strip directory prefixes from the filename column, sort lines by filename, and write to `manifest.txt` in that exact `sha1sum` format.