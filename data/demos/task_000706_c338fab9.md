Start by inspecting the schema (`sqlite3 /home/user/graph.db ".schema"`) and running `EXPLAIN QUERY PLAN` on the exact contents of `/home/user/query.sql` (e.g. `sqlite3 graph.db "EXPLAIN QUERY PLAN $(cat query.sql)"`) to see which tables show `SCAN TABLE` — these are the joins/filters that need supporting indexes. Since the recursive CTE filters `relations` by `source_id` and joins `entities` by `id`, add indexes covering those exact predicate columns (e.g. `CREATE INDEX ... ON relations(source_id)` and one on `entities(id)`, adding the ANALYZE step if needed) directly into the `.db` file with `sqlite3 graph.db "CREATE INDEX ..."` — don't touch `query.sql` itself. Re-run `EXPLAIN QUERY PLAN` after indexing to confirm every entry now says `SEARCH` not `SCAN` before moving on. Execute the unmodified query against the database, appending `ORDER BY` only on the command line (not editing the file) to sort alphabetically, and redirect just the name column, one per line, into `/home/user/impact.txt`. Finally write `verify_plan.sh` as a one-line wrapper that prints the same `EXPLAIN QUERY PLAN` output.
