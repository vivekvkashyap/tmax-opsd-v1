This task has two independent parts — do them separately and verify each before moving on. For the sandbox build, run `make` in `/app/sandbox-runner-1.2.0` first to see the actual compiler error; since the hint is "strict warnings," look for `-Werror` in the Makefile combined with an unused-variable or similar warning in the C source, and fix it minimally (either remove the offending unused code or drop `-Werror`/add the specific `-Wno-*` flag) rather than rewriting the build system. After it compiles, copy/install the resulting binary into `/usr/local/bin/` and sanity-check it runs. For the decoder, reverse-engineer `/app/oracle/malware_decoder` as a black box: feed it known inputs via stdin and observe stdout, and use `strings`/`ltrace` if available to confirm it's doing AES-CTR — the task spec already tells you the key derivation (MD5 of a fixed ASCII string) and that the first 16 bytes of stdin are the CTR nonce, so implement `/home/user/decode_trace.py` using Python's `cryptography` (or `pycryptodome`) AES-CTR primitives exactly per that spec, reading/writing via `sys.stdin.buffer`/`sys.stdout.buffer` for correct binary I/O. Validate by generating random binary inputs of varying lengths (>=16 bytes) and diffing your script's output against the oracle binary's output byte-for-byte before finishing.
