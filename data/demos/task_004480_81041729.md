First localize and fix the vendored package bug before writing any pipeline code: open `/app/spec_smooth/spec_smooth/core.py` and check every import line character-by-character — a common seeded fault is a misspelled module name (e.g. `nump` instead of `numpy`) that raises `ModuleNotFoundError` on import, not a logic bug. Fix the typo, then reinstall/editable-install the package (`pip install -e /app/spec_smooth`) so your script's `import spec_smooth` picks up the fix. In the processing script, load both `.npy` arrays, iterate row-by-row calling `spec_smooth.smooth(signal, window_length=15, polyorder=3)`, then normalize both the smoothed and theoretical arrays to sum to 1 (divide by their own sum, not a shared constant) before computing `scipy.stats.wasserstein_distance` pairwise. Average the N distances and write only the raw float (no extra text/newline formatting issues) to `/home/user/mean_distance.txt`. Actually run the script end-to-end and check the printed/saved value against the 0.005 threshold rather than assuming the smoothing parameters are sufficient.
