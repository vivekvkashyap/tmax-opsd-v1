Read the existing SQL query in `generate_report.py` carefully — it lists `sales, customers, products` in the `FROM` clause but only joins `sales` to `customers` in the `WHERE` clause, so `products` is left as an unconstrained cross join, multiplying every sales row by the number of product rows and inflating (and duplicating) the totals. Fix it by adding the missing join predicate `AND s.product_id = p.product_id` (or rewrite using explicit `JOIN ... ON` syntax) so each sale is matched to exactly one product. Add `CREATE INDEX` statements on the `sales` table's foreign key columns (`customer_id` and `product_id`) before running the aggregation query — this is required by the check, not merely a performance nicety. After fixing the query, run `cursor.execute("EXPLAIN QUERY PLAN " + query)` (or `EXPLAIN QUERY PLAN` prefixed) and write the raw rows to `/home/user/query_plan.txt`. Replace the `print(row)` loop with code that builds a list of `{"region":..., "category":..., "total_sales":...}` dicts and dumps it via `json.dump` to `/home/user/regional_sales_report.json`, keeping `total_sales` as a float. Run the fixed script once and spot-check the JSON against manual totals per region/category before finishing.