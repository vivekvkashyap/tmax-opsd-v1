Start by running `make && make test` in /app/specfit-2.1.0 to see the regression failures, then inspect trapz_integrate in src/integrate.c and compare each term of its summation loop against the standard trapezoidal rule — check specifically whether each `(y[i]+y[i+1])/2` term is being multiplied by the local step size before being added to the running sum; a missing step-size factor makes the integral ignore the x-axis scale entirely. Restore that multiplier, rebuild, and confirm `make test` passes before moving on. Bind to libspecfit.so via ctypes/cffi and call the fixed trapz_integrate on each time row's intensity values restricted to the 550-650nm wavelength slice. Fit A0*exp(-k*t)+C with scipy.optimize.curve_fit using a reasonable initial guess so it converges. For the bootstrap CI on k, resample the fit's residuals (not raw data) with replacement 2000 times, add each resampled residual set back onto the fitted curve, refit each time, collect the resulting k values, and take their 2.5/97.5 percentiles as the CI bounds.
