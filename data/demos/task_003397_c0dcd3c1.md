For the expect script, drive `/home/user/init_fs` with `spawn`, then chain `expect "Enter log directory path: "` / `send "/home/user/custom_logs\r"` for each of the three prompts in order (log directory, size limit, rotation count), ending with `expect eof` so the process is allowed to exit and flush its output file before your script finishes — a common mistake is not waiting for eof, causing the config file write to race with script exit. Run the .exp file and confirm `/home/user/provision_config.txt` was actually created with the three KEY=value lines before moving on. For the C utility, fopen and read provision_config.txt, locate the `LOG_DIR=` line (e.g. strncmp + sscanf or manual parsing), then call mkdir() with mode 0755 and check errno == EEXIST as the only acceptable "already exists" case — any other errno should print an error and exit non-zero. After mkdir succeeds, fopen/fclose an empty setup.log inside it. Finally hand-write the logrotate conf with the literal directives: size limit, `rotate 3`, `missingok`, `nomail`, and a glob path targeting `*.log` under the created directory.
