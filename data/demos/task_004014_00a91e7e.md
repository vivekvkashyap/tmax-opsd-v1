Diagnose before editing: curl to confirm the 502, then check nginx's error log to see it's dialing the wrong unix socket path — fix the `proxy_pass unix:...` line in /home/user/nginx/nginx.conf to point at /home/user/run/app.sock (the actual path the backend binds), not backend.sock. For the supervisor, write a simple bash loop, e.g. `while true; do /home/user/backend.py & pid=$!; wait $pid; echo "[CRASH] restarted backend" >> /home/user/supervise.log; done` — the key subtlety is the log line must only be appended on *restarts*, not on the very first launch, so structure the loop so the log-append happens after the first `wait` returns (loop body: start, wait, log, repeat) rather than logging before starting. Launch supervise.sh with `&` (or nohup) so it survives, confirm the backend's socket file appears before starting nginx (a race can cause nginx to fail if it starts before the socket exists), then start nginx via `nginx -c /home/user/nginx/nginx.conf` in the background. Test end-to-end: curl the proxy, then `kill -9` the backend pid and confirm both a new pid appears and the log line was appended.
