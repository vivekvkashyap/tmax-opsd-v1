Fix the ETL type bug explicitly rather than hoping the merge behaves: after `df1.merge(df2, on='id', how='left')`, cast `df['id'] = df['id'].astype('Int64')` (pandas nullable integer type) — and when later serializing a row to JSON in the API, explicitly convert with `int(row['id'])`, since numpy/pandas integer types often don't serialize as plain Python `int` by default and the grader checks `type(...) == int` exactly. For audio, use `librosa.load(path, sr=None)` to keep the native sample rate, compute `librosa.feature.mfcc(y=y, sr=sr, n_mfcc=13)`, and take the mean across the time axis to get one 13-dim vector — the same vector is appended to every row's `feature_val` to form 14-dim feature rows. Tokenize `text_description` with `.split()` for `token_count`. Fit one `sklearn.decomposition.PCA(n_components=2)` on the whole 14-dim matrix, keeping the fitted model in memory for both endpoints. Build a minimal Flask/FastAPI app bound to `127.0.0.1:8000`: `/record` looks up a row and returns the plain-int id plus pca coords; `/benchmark` calls `pca.transform()` 1000 times timed with `time.perf_counter()` and returns the average in ms. Run it in the background and curl-test both endpoints before finishing.