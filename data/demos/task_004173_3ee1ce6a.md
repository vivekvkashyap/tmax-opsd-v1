Build the pipeline bottom-up and test each script standalone before wiring them together. `migrate_config.py`: use Python's `configparser` to read `/home/user/legacy/app.conf`, pull the `user` value directly from the `[database]` section (don't hardcode it), map `db.local:3306`→`localhost:33060` and `redis.local:6379`→`localhost:63790` per the given rules, add `"cloud_region": "us-east-1"`, create `/home/user/cloud/` with `os.makedirs(..., exist_ok=True)`, and write the JSON deterministically (same key order/formatting) each run so re-running produces byte-identical output — that's what "idempotent" is graded on. `check_endpoints.py`: read the generated JSON, and for each of database/cache use `socket.create_connection((host, port), timeout=<short>)` wrapped in try/except, printing an error and `sys.exit(1)` on any failure, else `sys.exit(0)` after both succeed. `pipeline.sh`: run the migration script, validate the JSON is parseable (e.g., `python3 -c "import json,sys; json.load(open(sys.argv[1]))"` or `jq . file >/dev/null`), run the connectivity check, and only if every step's exit code is 0 write the literal string `DEPLOYMENT_READY` to `/home/user/deploy_success.log`; use `set -e` or explicit `||` exits so failures short-circuit and never touch the log file. `chmod +x` all three scripts, then run `./pipeline.sh` and confirm the log and JSON look right.