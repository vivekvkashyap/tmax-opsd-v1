Work through the checklist in dependency order since each step's output feeds the next. First apply `fix.patch` to `mathops.c` with `patch < fix.patch` (or `git apply`) from inside `/home/user/workspace` — verify with a diff/grep that the arithmetic bug is actually fixed in the file afterward, don't just trust the patch exited 0. Next inspect `setup.py`: a broken C-extension setup script commonly points `sources=[...]` at the wrong filename — correct it to reference the real `mathops.c` so `python3 setup.py build_ext --inplace` actually compiles and produces an importable `mathops` module (test with `python3 -c "import mathops; print(mathops.add(2,3))"` before moving on). Then write `test_mathops.py` using `from hypothesis import given` and `hypothesis.strategies as st`, decorating `test_add` with `@given(st.integers(), st.integers())` and asserting `mathops.add(a, b) == a + b`. Finally write `ci_run.sh` that runs the build command, then `python3 -m pytest test_mathops.py`, propagating pytest's exit code (avoid swallowing failures with `|| true`), and `chmod +x` it. Run `./ci_run.sh` yourself end-to-end and confirm exit code 0 before considering the task done.
