Since the verifier does byte-for-byte comparison against a reference oracle, prioritize exact formatting over cleverness. First recover the three hourly rates from `/app/rates.png` — run `tesseract /app/rates.png` (OCR is installed) or view the image directly, and cross-check the extracted numbers make sense (rates as decimals like 0.NN); don't guess or round them. Write `/home/user/billing_alert` to read all of stdin line-by-line, split each on whitespace, skip malformed/blank lines, accumulate `hours * rate[region]` only for lines whose region matches one of the three known keys, then print the fixed five-line email template with the total formatted to exactly two decimals (e.g. Python f-string `:.2f`, not manual rounding) — the common failure mode is a floating-point formatting mismatch or an extra/missing blank line/trailing newline versus the exact template. Make the script executable with a proper shebang and `chmod +x`. Before finishing, self-test with a handful of synthetic usage logs and manually recompute expected totals to confirm your rounding and summation match what `:.2f` on a running float total would produce.