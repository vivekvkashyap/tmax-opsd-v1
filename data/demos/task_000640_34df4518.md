Write `/home/user/run_regression.sh` as a straightforward orchestration pipeline, and get each command's exact stdout format right rather than assuming it. Step 1: loop 50 times running `python3 /home/user/simulate.py`, appending its single-line float output to a fresh `/home/user/current_runs.txt` (truncate/recreate the file first so reruns don't accumulate). Step 2: call `Rscript /home/user/bootstrap_ci.R /home/user/current_runs.txt` and capture its stdout — it prints two space-separated floats, so read them into two shell variables (e.g. `read lower upper <<< "$(Rscript ...)"`). Step 3: read the single float from `/home/user/baseline_mean.txt`. Step 4: compare with `awk`/`bc -l` using strict inequality on both sides (`baseline > lower && baseline < upper`, not `>=`/`<=`, since the spec requires the boundary case to FAIL) and write exactly `PASS` or `FAIL` (no extra whitespace/newline issues) to `/home/user/regression_result.log`. Make the script executable with chmod +x, and actually run it once yourself to confirm current_runs.txt has 50 lines and the log contains a clean PASS/FAIL before considering the task complete — but per the instructions, leave the final delivered script correct and idempotent rather than relying on your test run's leftover state.
