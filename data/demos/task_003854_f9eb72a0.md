Work through this as a sequence of verifiable stages rather than one big script. First extract with `mkdir -p /home/user/staging && tar -xzf /home/user/backups/archive.tar.gz -C /home/user/staging`, then inspect `etc/app.conf` to see its current `listen_port` and `db_path` values before editing — use `sed -i` (e.g. `sed -i 's/^listen_port=.*/listen_port=8080/'`) rather than rewriting the whole file, so other keys like `max_connections` are untouched. In `manager.sh`, launch the binary with `nohup ... &` and immediately capture `$!` to the PID file; for `stop`, kill that PID and remove the file; for `status`, use `kill -0 $(cat pidfile) 2>/dev/null` to test liveness without actually signaling the process, and set the exit code accordingly (0 running, 1 not). Use absolute paths everywhere (compute the config path from the staging dir, don't rely on a relative cwd) since the binary and manager may be invoked from anywhere. For `health_check.sh`, parse the two config values out of the (now-modified) `etc/app.conf` with grep/cut, call `manager.sh status` to decide RUNNING vs not, and write the report lines in the exact literal format requested — match spacing and bracket labels precisely, then start the service before running the health check so the report reflects a live process.
