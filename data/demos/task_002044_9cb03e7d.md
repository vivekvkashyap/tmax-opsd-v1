Process all `.jsonl` files under `/home/user/loc_logs/` line by line, counting every line read (including ones that turn out to be duplicates) toward `TOTAL_LINES`. For each JSON object, fix the `text` field's escaping robustly: some entries are properly-escaped Unicode already decoded by `json.loads`, others contain a literal backslash sequence like `é` still present as text (double-escaped) — detect and unescape those with something like `text.encode().decode('unicode_escape')` only when a literal `\u` pattern remains, so both forms normalize to the same real Unicode string. Build the dedup key as `f"{timestamp}|{loc_key}|{unescaped_text}"`, MD5-hash it, and keep only first occurrences, tracking how many were dropped for `DUPLICATES_REMOVED`. Derive the hour bucket via string slicing (`timestamp[:13]`) since the format is fixed, then use a `Counter`/dict to tally unique events per hour, and write the CSV sorted by hour with exact headers `hour,unique_events`. Write the four log lines to `/home/user/pipeline.log` in the exact order and format specified, appending (not overwriting) if the log could be run multiple times — but since this is a single run, opening in write mode is fine as long as content matches exactly.
