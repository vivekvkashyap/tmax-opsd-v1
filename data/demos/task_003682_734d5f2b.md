First locate the backdoor in the vendored `/app/pyjwt` source: search the JWS/JWT signature-verification code path (likely in `api_jws.py`) for a special-cased check that returns success when the header's `alg` is `"none"`, and remove that bypass so verification always requires the caller-specified algorithm and a valid signature. Reinstall/point your script at this fixed copy (e.g. `sys.path.insert(0, "/app/pyjwt")` or `pip install -e /app/pyjwt`) rather than any system-installed pyjwt, and confirm with a quick manual test that an `alg=none` token is now rejected. In `jwt_scanner.py`, call `jwt.decode(token, "vulnerability-scan-2024", algorithms=["HS256"])` inside a try/except catching all `jwt.exceptions.*` (invalid signature, invalid algorithm, expired, decode errors) and exit 1 immediately on any exception. Only if decoding succeeds, check the `csp_directive` claim for the substrings `unsafe-inline`/`unsafe-eval`, and base64-decode `b64_payload` (handle padding errors as evil too) checking for `<script>`; exit 1 if any check trips, else exit 0. Test both an `alg=none` token and a validly-signed malicious-claim token by hand before relying on the hidden corpora.
