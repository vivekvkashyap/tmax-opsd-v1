Start by exploring the schema interactively with `sqlite3 /home/user/legacy_system.db ".schema"` and `.tables`, then sample rows from each table to infer which generic columns are foreign keys (an employee table likely has an id, a manager/self-referencing id, and a department code; a transactions table likely references an employee id and an amount). Once the hierarchy is clear, write the recursive rollup as a SQL `WITH RECURSIVE` CTE inside your bash script (invoked via `sqlite3 -csv` or `sqlite3 -header -csv`) that: for each employee, recursively collects all descendants (direct and indirect reports) via the self-referencing manager column, sums personal sales (COALESCE unmatched employees to 0 sales) across the employee plus all descendants, and joins back to get employee name and department name. Order results by total team sales descending, then employee name ascending, and emit exactly the header `Employee Name,Department Name,Personal Sales,Total Team Sales` with integer values (cast/round sums explicitly since SQLite sums can come back as floats). Make the script executable with `chmod +x`, and validate by manually tracing one or two employees' hierarchies against your own sums before trusting the script's output.