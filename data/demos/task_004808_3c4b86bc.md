Use OpenCV (`cv2.VideoCapture`) to read each of the 100 frames; since the dot is the only bright object on black, threshold the grayscale frame and compute the centroid via `cv2.moments` or the mean of nonzero pixel coordinates (`cv2.findNonZero`) rather than trying to fit a circle — this is far more robust than manual pixel scanning. Store the (frame, x, y) list, e.g. as a CSV/text file the C program can read. In C, represent the trajectory as a 2D array; fit X(t) linearly with a closed-form least-squares formula (no iteration needed). For Y(t) = Y0 + Vy*t + A*sin(w*t), do a coarse grid/random search over omega in [0.05, 0.2], and at each candidate omega solve the remaining linear parameters (Y0, Vy, A) via least squares, keeping the omega that minimizes SSE. Compute SSE for the linear-only fit (A=0) vs the full nonlinear fit and derive the F-statistic from their difference. Wrap the compiled C binary's output in a small Python (or C) HTTP server bound to 127.0.0.1:8080 exposing the three JSON endpoints, and launch it in the background so it stays reachable for testing.
