Work in stages and verify each before moving on. First, run `tesseract /app/lab_note.png stdout` (try different `--psm` values if OCR output is garbled) to recover the 4-byte magic header hex and the AUTH_TOKEN string exactly as written — do not guess these values. Second, write a small C++ scanner that reads `/home/user/raw_sensor.bin` as raw bytes, scans for the magic header (mind byte order — the header may appear little-endian in the file), then reads the next 4 bytes as a little-endian uint32 payload_size, and appends exactly that many following bytes to your output buffer; write the concatenation to `/home/user/recovered.tar.gz`. Test with `tar -tzf` before trusting it. Third, `tar -xzf` into `/home/user/dataset/` and run a scripted find+sed/perl in-place substitution across all text files for the two exact strings (careful with bracket characters needing escaping in sed/regex). Finally write a minimal C++ HTTP server (raw sockets or a small header-only lib) binding 127.0.0.1:8000, parsing the Authorization header exactly, returning 401 on missing/wrong token and 200+file contents on match; launch it with `&` or `nohup` and confirm with `curl` before finishing.
