Spot the same implicit-cross-join anti-pattern seen in comma-joined `FROM users u, purchases p` queries: it multiplies rows instead of relating them, so first fix `fixed_etl.py` to explicitly `JOIN purchases p ON p.user_id = u.id`, and simultaneously convert the f-string-built SQL to a parameterized query using `?` placeholders with values passed via `cursor.execute(query, (signup_date, purchase_date))` — both fixes are required, not just one. Keep the same CLI argument contract (`sys.argv[1]`, `sys.argv[2]`) and CSV header/output path unchanged so the verifier's invocation still works. For indexes, think about what the fixed query filters/joins on — an index on `purchases(user_id)` supports the join, and one on `users(signup_date)` or `purchases(purchase_date)` supports the WHERE filters; write these as plain `CREATE INDEX` DDL statements in `indexes.sql` and test them by piping the file into `sqlite3 /home/user/etl_data.db` to catch syntax errors early. Finally, apply the indexes for real, then run `EXPLAIN QUERY PLAN <your fixed, parameterized query with sample bound values>` via sqlite3 and redirect its raw output to `query_plan.txt` — verify the plan actually shows index usage (e.g. "USING INDEX") rather than a full table scan.
