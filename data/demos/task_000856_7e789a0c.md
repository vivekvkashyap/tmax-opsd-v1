Build and test the sanitizer as a pure, easily-unit-testable function before wiring it into the worker daemon. In `process_and_sanitize`, wrap JSON decoding (bytes -> str -> `json.loads`) in a try/except that catches `UnicodeDecodeError`, `json.JSONDecodeError`, and any broken-escape errors, returning `None` on failure rather than raising. Explicitly check for presence and correct type of every required key (`user_id`, `email`, `ssn`, `age`, `event_type`) and validate the `age` numeric bounds (0-120 inclusive) before doing anything else. For `event_type`, use a strict allowlist regex like `^[A-Za-z0-9_]+$` and reject on any other character (this is your defense against injection payloads in the adversarial corpus). Only after all validation passes, replace `ssn` with the fixed masked literal and rewrite `email` by splitting on `@` and replacing the local part with `hidden`. For the worker, use `redis.Redis().blpop('data_queue')` in a loop, call the sanitizer on the raw bytes, and if non-None, append a row to `/home/user/processed_logs.csv` using Python's `csv` module in append mode with header-only-once logic (check file existence/emptiness first), flushing/`os.fsync` after each write so results are visible immediately. Start the worker as a background process and confirm it stays alive.