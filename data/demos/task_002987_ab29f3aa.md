Start by reading `/home/user/buggy_query.sql` and `sqlite3 /home/user/network.db .schema` to confirm the implicit cross join is missing a join condition between `servers` and `links`. Fix it with explicit `JOIN` clauses on `source_id`/`target_id` matching `servers.id`, then apply a window function (`ROW_NUMBER() OVER (PARTITION BY source ORDER BY bandwidth DESC, target ASC)`) filtered to rank <= 2 via a subquery/CTE (SQLite doesn't allow filtering directly on a window function in the same SELECT). Save this as `/home/user/fixed_query.sql`. Run it against the DB and serialize rows to JSON matching `{"source":..., "target":..., "bandwidth":...}` — `sqlite3 -json` or a small Python script with `json.dump` both work; write to `/home/user/graph.json`. For Part 3, write `/home/user/solve_path.py` that loads the JSON, builds a directed weighted graph where edge cost = `10000.0 / bandwidth`, and runs Dijkstra (e.g. via `heapq`, or `networkx.shortest_path` with weight computed per edge) from `'Start'` to `'End'`. Write the resulting node-list path and total accumulated cost to `/home/user/result.json` in the exact specified schema. Double-check the window-function filtering doesn't silently keep all rows (a naive `WHERE rn <= 2` on the raw window-function column fails in SQLite — wrap it in a CTE).