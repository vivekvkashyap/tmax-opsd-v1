In C, first build a frequency count over node IDs by reading `raw_edges.txt` and incrementing a counter for BOTH the source and target integer on every line (the "interaction degree" is just total appearance count across both columns, not distinct-neighbor count — don't build an adjacency structure, a simple hash map or sized array keyed by node ID is enough). After parsing, filter out any node with degree < 3, then sort the remaining (id, degree) pairs by degree descending, and for ties by node ID ascending — implement this exactly in your comparator since it determines which records land on which page. Pagination then just means: with page size 5, page 2 is 0-indexed records [5,10) of that sorted list (skip the first 5, take the next 5, and note the list may be shorter than 10, in which case output whatever remains). Write each surviving (id, degree) pair on its own line as `<NodeID> <Degree>` to `/home/user/page2_results.txt`. Compile with `gcc /home/user/process_graph.c -o /home/user/process_graph`, watch for compiler warnings about unused variables or format mismatches, then actually run the binary to produce the output file rather than just compiling it.
