Build and validate each pipeline stage independently before wiring them into the API service. Extract frames with `ffmpeg -i /app/traffic.mp4 -vf fps=1 frame_%d.png` (or use OpenCV/PyAV directly) so each frame's index maps cleanly to an integer timestamp in seconds (frame N corresponds to second N-1 or N depending on your indexing — pick one and be consistent when joining to sensor_logs.csv's `time_sec`). Reconstruct the exact nn.Module architecture described (matching layer types, kernel/stride/padding, and dimensions precisely, since `load_state_dict` will fail or silently mismatch tensor shapes otherwise) before calling `torch.load` + `load_state_dict`; resize/normalize each frame to `(3,64,64)` the same way for every frame. Compute embeddings for all frames, join by exact-match `time_sec`, then compute the 4x3 Pearson correlation matrix (e.g. `numpy.corrcoef` or `pandas.DataFrame.corr()` on the combined columns). Fix plot_correlations.py by adding `import matplotlib; matplotlib.use('Agg')` before any pyplot import (must happen before `import matplotlib.pyplot`) so it renders headlessly, and confirm `/app/output/correlation_heatmap.png` is a real non-blank file (check file size/pixel variance). For the API, precompute cosine similarity between all embedding pairs once at startup rather than per-request, and implement each endpoint per the exact spec; start it as a background/daemonized process and verify with curl against each route before finishing.