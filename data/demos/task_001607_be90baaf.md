This is a black-box reverse-engineering task: don't guess the normalization formula, derive it empirically by probing the oracle binary with small, hand-crafted graphs where you can compute several candidate formulas by hand. Start with the simplest possible input (e.g., 2 nodes, 1 edge, simple feature values) and run `/app/gcn_propagate edges.csv features.csv`, then compute by hand what symmetric-normalized-with-self-loops, random-walk-normalized, and unnormalized-sum-of-neighbors formulas would each produce for that node — compare against the binary's actual output to identify which formula matches. Repeat with a slightly bigger graph (a node with degree 3+, an isolated node, a node connected to itself via a duplicate edge if the format allows) to pin down edge cases like whether self-loops are added, how degree is computed (with or without the self-loop), and whether isolated nodes are handled specially — the binary's behavior on these edge cases, not assumptions, must drive your implementation. Once the formula is fully pinned down from your own probing, implement it in your chosen language, matching stdout formatting exactly (six decimal places, comma-separated, sorted by node_id, using `%.6f`/`std::fixed`-equivalent rounding), and validate your implementation against the oracle on several more random graphs of varying size before considering it done, since the real grading will fuzz against the oracle on many random inputs.