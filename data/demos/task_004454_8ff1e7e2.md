Use `git bisect start`, mark current `main` as bad and an early commit as good, then at each step build/run a small reproduction script that feeds a partially-written (truncated) WAL file to the server's startup replay path and checks whether it panics — automate the bisect with `git bisect run <script>` if possible. Once you find the offending commit, read its diff to `wal.go` to see what error-handling branch was removed; the underlying fix is to restore proper handling of `io.ErrUnexpectedEOF`/`io.EOF` when `io.ReadFull` hits a torn/partial record during replay, breaking the loop instead of indexing past the buffer — apply this directly on `main`, don't just `git revert` the regression commit since later commits likely depend on surrounding changes. For the token, run OCR (`tesseract /app/auth_token.png stdout`) to recover the exact admin token string. Build the server (`go build` or `go run`), export `ADMIN_TOKEN=<ocr-recovered-token>`, and launch it backgrounded so it binds `127.0.0.1:9000` and stays alive, then sanity-check with `curl` that `/health` and authenticated `/set`/`/get` calls behave correctly using the `Authorization: Bearer <token>` header.