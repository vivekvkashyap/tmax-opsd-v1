Write a Python script that opens the log with a plain `for line in f:` loop (no `.readlines()`) so it stays streaming. Parse each line with a regex or slicing to pull the `YYYY-MM-DD HH:MM` prefix (drop seconds) and the message text after the level marker.

For normalization, apply steps in order: lowercase, then strip non-alphanumeric characters while preserving spaces (e.g. `re.sub(r'[^a-z0-9 ]', '', s)`), then collapse repeated spaces (`re.sub(r' +', ' ', s)`), then `.strip()`. Hash the normalized string with `hashlib.sha256(...).hexdigest()` and use a `dict[minute] = set(hashes)` to count unique event types per minute.

Once you have unique counts per minute, you must build a complete, gap-free minute timeline from the first minute seen to the last (using `datetime`/`timedelta` to step minute-by-minute), filling missing minutes with count 0 — this is the step naive attempts skip, causing wrong rolling averages when a minute has zero events. Compute the 3-minute trailing rolling average (current + prior two minutes, using 0 where a window minute doesn't exist before the first minute). Round to 2 decimals, keep only averages strictly greater than 3.0, and write the list of `{minute, rolling_avg}` objects to the output JSON in chronological order.
