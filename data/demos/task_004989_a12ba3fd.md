Two separable pieces: an index and a correct windowed query. For the index, inspect the query you're about to write in Part 2 — it filters by `status`, partitions by `server_id`, and orders by `timestamp` — so a composite index on `backups(server_id, status, timestamp)` (or at minimum `server_id, timestamp`) eliminates the full scan; write this as `CREATE INDEX ...` in `/home/user/optimize.sql` and apply it with `sqlite3 /home/user/backups_meta.db < /home/user/optimize.sql`. For the analysis, use a single SQL query with `SUM(size_bytes) OVER (PARTITION BY server_id ORDER BY timestamp ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)` restricted to `status = 'SUCCESS'`, then in Python filter down to, per server, the first row where that running total strictly exceeds 500,000,000 — window functions computed on a `WHERE status='SUCCESS'` subquery, not on all rows, since a FAILED backup shouldn't affect the running sum or be a candidate crossing row. Join to `servers` for hostname, sort the final list alphabetically by hostname, and emit exactly the three required JSON keys per object. Double check ordering: rows must be time-ordered *before* the cumulative sum is computed, since unclustered insert order will produce wrong sums otherwise.
