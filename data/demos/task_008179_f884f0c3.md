Migrate incrementally and run `python3 dep_resolver.py` after each fix rather than rewriting the whole file at once — that way each syntax/runtime error points you straight at the next fix. Expect, in order: (1) a `print "..."` statement-form print → convert to `print(...)` function call; (2) `except Exception, e:` old-style tuple syntax → convert to `except Exception as e:`; (3) `StandardError` doesn't exist in Python 3 → replace with a built-in exception like `Exception` or `ValueError`; (4) `hashlib.md5().update(str)` will raise `TypeError: Unicode-objects must be encoded` because Python 3's hashlib needs bytes, not str — encode every string passed to `.update()` with `.encode('utf-8')`, including both the node's own `data` field and each dependency's stored checksum. Leave the graph traversal order (sorted DFS) and the checksum accumulation logic (concatenating dep hashes in sorted order per node) completely untouched — only fix language-level syntax/typing issues. After it runs clean, confirm `checksum.txt` contains a 32-character hex MD5 string, not an error message.
