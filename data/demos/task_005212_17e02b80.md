Start with delta-debugging, not implementation. Diff the paired clean/evil JSON files (structurally, not just textually — e.g. write a small script that walks each JSON tree computing max nesting depth per key and scans string values for suspicious byte sequences) to find what actually differs between the two corpora; don't assume the exact trigger from the description alone, confirm it against the fixtures. Once you've isolated the real discriminating feature(s) (nesting depth under a specific key past a threshold, and/or an unusual literal byte run inside a string value), implement `/home/user/validator.py` to walk the parsed JSON recursively tracking depth and scan raw string values for the byte pattern, printing ACCEPT/exit 0 or REJECT/exit 1 accordingly — test it against every file in both provided corpora before moving on, since the hidden test corpus will use the same rule. Then edit `/app/api/app.py` so each incoming request body is first written to a temp file and passed to `python3 /home/user/validator.py <path>` via subprocess; on non-zero exit, return 400 and never push to Redis, otherwise proceed with the existing Redis push. Finally start all three services via `/app/start_services.sh` (fixing ports/config if needed) and confirm end-to-end with curl against both a clean and an evil fixture, checking the Ruby worker doesn't crash and `/tmp/processed_logs.txt` only gets clean entries.
