Build this as a small Unix pipeline plus one C helper, not a monolithic script. First write and compile the C program: read lines from stdin in a loop with `fgets`/`getline` into a fixed buffer sized for up to 255 chars plus newline, strip the trailing newline, and since the input is pre-sorted, do a linear scan comparing each line to the previous one (`strcmp`) to count consecutive runs — this avoids needing a hash map. Track the running max count and the node name that achieves it, keeping the first-seen name on ties (only update on strictly greater count). On EOF (`fgets` returns NULL / `getline` returns -1), flush the last run, then print `MAX_NODE=<name> COUNT=<count>` to stdout. Compile with `gcc -o /home/user/max_degree /home/user/max_degree.c`. Then build the shell pipeline: `grep '^\[DEPENDENCY\]' /home/user/pipeline.log` to isolate dependency lines, use `awk '{print $2}'` to pull just the source_stage token (careful with field positions given the `[DEPENDENCY]` prefix), `sort` those names so equal names are adjacent, then pipe into `/home/user/max_degree`, redirecting the whole pipeline's output to `/home/user/result.txt`. Verify by manually tallying source stage counts from the log before trusting the program's output.
