Do not run the vulnerable C parser directly against /app/suspect_audio.wav or the evil corpus — you're building a pre-filter, so analyze structurally instead. First read /app/audio_pipeline/src/parser.c to understand how it walks WAV chunks (it likely loops reading a 4-byte chunk ID and a 4-byte little-endian chunk size, then mallocs/reads that many bytes). Then do a binary diff: use `xxd`/`od` to dump a clean file and an evil file side by side and diff them, and also inspect /app/suspect_audio.wav the same way, looking specifically for a non-standard chunk ID (something other than the expected RIFF/fmt /data chunks) paired with a suspicious size field — a value of 0xFFFFFFFF (or another huge/negative-when-signed value) is the classic integer-overflow-into-malloc trigger. Once you've identified the exact byte signature (chunk tag + size bytes), write /home/user/detector.sh to scan the file's raw bytes for that hex signature (e.g. via `xxd -p file | tr -d '\n' | grep -qi <hex-pattern>`) and exit 1 if found, 0 otherwise. Validate against all 100 corpus files (clean + evil) with a small loop checking exit codes before declaring done, and confirm the same signature is present in /app/suspect_audio.wav.