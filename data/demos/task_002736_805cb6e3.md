Write `/home/user/etl_processor.c` to read `/home/user/raw_data.log` line by line, parsing `LOG_TIME:... | SID:... | VAL:...` with `sscanf`. Discard any record with VAL < 0.0 or > 100.0. Compute each record's 15-minute bucket by rounding the timestamp's minute field down to the nearest multiple of 15 (keep year/month/day/hour, zero the seconds), building a bucket key string `YYYY-MM-DDTHH:MM:00Z`. Aggregate sum/count per (bucket, SID) pair — a simple array or hashmap keyed by the pair is fine given the small dataset — then compute averages formatted with `%.2f`. Sort entries chronologically by bucket start, then alphabetically by SID within a bucket (`qsort` with a custom comparator), and write to `/home/user/summary.csv`, overwriting any existing file. Compile with gcc, run it once, and check the output against the format spec (field order, decimal places, ordering) by hand. Finally add a cron entry via `crontab -e` or `(crontab -l; echo "...") | crontab -` with minute field `0,15,30,45` (or `*/15`) and the compiled binary's absolute path — confirm with `crontab -l`.