Run `/app/start_services.sh` first and give it a few seconds so the worker populates Redis's `raw_traffic` list before you try to read it — check `redis-cli llen raw_traffic` to confirm data is flowing (expect at least 1000 entries) rather than assuming it worked. In `/home/user/prepare_data.py`, connect to Redis db 0, pull all list items, `json.loads` each one, and group latency observations by the `(src, dst)` directed pair. For each edge, bootstrap the mean latency: resample the edge's latency observations with replacement; the task explicitly wants a convergence check (run at B=500 then B=1000) but only the B=1000 resamples' 2.5th/97.5th percentiles should be reported as `ci_lower`/`ci_upper`. Collect each edge's bootstrap mean latency, standardize the whole set of edge means (subtract their overall mean, divide by their overall standard deviation), then use `scipy.stats.wasserstein_distance` between that standardized array and a large sample drawn from `numpy.random.normal(0, 1, size=...)` (or the standard normal CDF) to get `wasserstein_distance`. Write `/home/user/processed_graph.json` matching the given schema exactly (top-level `wasserstein_distance` plus an `edges` array of `{src, dst, mean_latency, ci_lower, ci_upper}`). Since grading allows error ≤ 0.05 versus an independently recomputed reference, get the standardization and bootstrap procedure right rather than chasing exact digits.