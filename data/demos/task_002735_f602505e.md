Write `/home/user/provision.c` as a retry-supervisor loop, not a one-shot runner. At startup, `getenv("VM_QUOTA_DIR")` and exit(2) immediately if NULL; otherwise call `statvfs()` on that path and exit(2) if `f_bavail * f_frsize < 1024`. Then loop up to 4 total attempts: each iteration, run `/home/user/flaky_qemu.sh` via `fork`+`execve`/`execl` and `waitpid` (or `system()` if you extract the real exit status with `WEXITSTATUS`), and append the appropriate line to `/home/user/provision.log` using an append-mode `fopen(path, "a")` (or `open(O_APPEND|O_CREAT)`) each time so nothing gets truncated between runs — flush/close after each write to be safe. On exit status 0, log "Provisioning Success\n" and `exit(0)` immediately without further attempts. On nonzero, log "Provisioning Restart\n" and continue the loop unless this was the 4th attempt, in which case log "Provisioning Failed\n" and `exit(1)` instead. Compile with `gcc -o /home/user/provision_bin`, export `VM_QUOTA_DIR=/home/user`, delete any stale log/state files, then run the binary once and inspect `/home/user/provision.log` line-by-line to confirm the exact expected sequence for this particular flaky script's behavior.