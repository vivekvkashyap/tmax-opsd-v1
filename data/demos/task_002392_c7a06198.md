Work through the three bugs in order, each blocking verification of the next. (1) ABI: if `validator.h` lacks `extern "C"`, `g++` mangles the symbol name `router.cpp` expects while the `gcc`-compiled `.so` exports the unmangled C name, causing "undefined reference"; wrap the declaration in `extern "C"` guards. (2) Makefile: the link rule for `fast_router` needs `-L. -lvalidator -Wl,-rpath,.` so the binary can find `libvalidator.so` at runtime without external `LD_LIBRARY_PATH`; verify with `ldd` and by running from another directory. (3) Logic: find where `router.cpp`'s query-string parser computes the end offset of `payload=` — an off-by-one using `length() - 1` instead of `length()` when the parameter is last (no trailing `&`) truncates its final character; fix the boundary and test a URL where `payload=` is last. After `make` succeeds, copy the executable and `.so` to `/home/user/`, and fuzz-test varied URLs against your own understanding of correct behavior to catch remaining discrepancies.