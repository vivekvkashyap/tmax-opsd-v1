Diagnose the non-determinism by inspecting the OpenMP reduction: an `#pragma omp parallel for` loop combined with `#pragma omp atomic` on float additions means threads race to add partial sums in an unpredictable order, and floating-point addition is not associative, so the mean spectrum (and downstream SVD) varies run to run. Fix it by making the reduction order fixed: either drop the OpenMP pragmas entirely and sum sequentially in a simple nested loop, or use a deterministic reduction pattern (e.g., accumulate per-thread partial sums into a fixed-size array indexed by thread id, then combine those partial sums in a fixed sequential order after the parallel region — never use `omp atomic` on floats for an accumulation you need to reproduce). After fixing, recompile with the given g++ command, run the binary, capture the "Leading Singular Value" line it prints, and write just that number (rounded to exactly 5 decimal places) to `/home/user/svd_result.txt` with no extra text. Verify reproducibility yourself by running the binary twice and diffing the outputs before finishing.
