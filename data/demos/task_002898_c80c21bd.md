Start by getting the audio's exact instructions in text form — run a speech-to-text tool (install `openai-whisper` or use any available ASR) on `/app/instructions.wav` and read the transcript carefully rather than guessing at "typical" SSH/WAF filtering rules; the precise conditions (which auth method is excluded, which exact substrings trigger WAF exclusion, sort order/field) are stated in the recording and must be followed literally, since your output is checked byte-for-byte against a hidden oracle across many randomized fuzz inputs — any deviation (e.g. wrong sort key, case-sensitivity mismatch, substring vs exact-match confusion, wrong JSON indentation) will fail equivalence. After transcribing, write the filter as a straightforward single pass: sort events by the field the audio specifies, then apply the exclusion rule per service type it describes, keeping everything else untouched (including services not mentioned). Serialize with `json.dump(..., indent=2)` exactly, since indentation width affects byte-exact comparison. Test your script against a few hand-crafted inputs (including edge cases like missing fields) before trusting it against the fuzzer.