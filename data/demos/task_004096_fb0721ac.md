First fix the schema filter in the Rust CSV loader: parse `id` with `str::parse::<i64>()` and drop the row on error, drop rows with empty `text`, and drop rows with missing/empty `label` — do this before anything else touches the data. Rewrite `embed()` in `src/embedding.rs` to compute each of the five components explicitly by iterating the text's characters (use `is_ascii_alphabetic`/vowel-set membership/`is_whitespace`/summing `as u32 as f32` then `% 100.0`) rather than reusing any placeholder logic, and do not normalize the resulting vector. For cross-validation, preserve the cleaned rows' original relative order and split into exactly 5 equal-size sequential chunks (verify the valid-row count divides evenly by 5 as the task promises); for each fold use the other four as training. Implement KNN with squared/plain Euclidean distance over the 5-d embeddings, and when breaking ties among the K nearest labels, sort candidate labels alphabetically and take the first — implement this tie-break explicitly rather than relying on incidental HashMap iteration order, which is a common silent bug. Compute mean accuracy per K in {1,3,5}, pick the best (ties go to smaller K), and write `/home/user/tuning_results.json` via `serde_json`. Build with `cargo build --release` and run the binary to actually generate the file.