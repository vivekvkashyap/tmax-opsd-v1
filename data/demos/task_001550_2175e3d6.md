Structure etl_processor.cpp as a strict per-row validation pipeline: split each line on commas, check the field count is exactly 32 before parsing anything, validate sensor_id as a strict integer (reject trailing garbage, e.g. use `strtol` and check the end pointer consumed the whole token) and the timestamp as present, then parse each of the 30 readings with `strtod`/`std::stod` inside a try/catch or by checking the strtod end pointer — any single parse failure means skip-and-log the *original raw line text*, not a reconstructed one. For valid rows compute dim1 (mean of cols 3-12), dim2 (max of cols 13-22), dim3 (min of cols 23-32) using plain loops, then check each dim with `std::isnan`/`std::isinf` before clamping to [-100, 100] — a row that produces NaN/Inf must still be logged as invalid with its original text, appended after the schema-check invalids, preserving overall input order per the two failure classes. Format output floats with `std::fixed << std::setprecision(4)`. Compile with `g++ -O3` exactly as specified and run it, then diff your output files against your own hand-computed expectations for a couple of rows before trusting the full run.