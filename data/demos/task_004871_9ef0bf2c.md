Fix the three issues in `fit_model.py` independently, then rerun the whole pipeline for both mesh sizes. For the non-determinism bug, find where the load-vector contributions are grouped by node index using a `set` (or any unordered structure) before summing — iterating a `set` does not preserve insertion order and can even reorder across runs, which changes floating-point summation order and thus the result. Replace that with accumulation directly in the original file-read order: iterate the CSV rows once in sequence and do `b[idx] += weight` immediately, with no intermediate set/dict-based regrouping. For the solver, replace the generic `np.linalg.solve` call with `scipy.linalg.cho_factor(A)` followed by `scipy.linalg.cho_solve` on the factor, since $A$ is symmetric positive-definite. Parameterize the assembly/solve function by $N$ and call it for $N=10$ and $N=20$ separately (don't just rerun the old hardcoded $N=15$ version). For each $N$, compute `np.linalg.cond(A)` and `np.linalg.norm(x)` and assemble them into the exact nested JSON structure requested, writing floats (not strings) to `/home/user/stability_report.json`.
