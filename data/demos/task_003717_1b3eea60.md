Use pandas. Load both CSVs with `pd.read_csv`, `pd.concat([df1, df2], ignore_index=True)` preserving the given order, then drop rows where `RawText.str.strip()` is empty (`df[df['RawText'].str.strip().astype(bool)]`, handling NaN too). Deduplicate with `drop_duplicates(subset=['UserID','RawText'], keep='first')` computed on the concatenated order before any text cleaning, so the "first occurrence" semantics match the original file order exactly. Sort by `ReviewID` ascending, then interpolate `Timestamp`: convert to numeric/NaN, use `df['Timestamp'].interpolate(method='linear')` on the ReviewID-sorted frame, then apply `.bfill()` for any remaining leading NaNs and `.ffill()` for trailing ones (interpolate alone won't fill edges), then round to int. For `CleanedText`, lowercase, use `re.sub(r'[^a-z0-9]+', ' ', text)` to collapse non-alphanumerics to single spaces, split on whitespace, filter out the exact stop-word list, and rejoin with single spaces — do this per-row via `.apply`. Finally select/reorder columns to `ReviewID, UserID, Timestamp, CleanedText`, ensure `Timestamp` is int dtype, and write with `to_csv(..., index=False)` after `mkdir -p /home/user/output`.