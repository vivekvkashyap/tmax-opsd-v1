First confirm all needed libraries import cleanly (opencv-python, pandas, pyarrow, scipy, numpy), installing any missing via pip. Process the video frame-by-frame with `cv2.VideoCapture`, converting each frame to grayscale (`cv2.cvtColor(..., COLOR_BGR2GRAY)`) and taking `np.mean` of the grayscale array — read frames in a `while cap.read()` loop until it returns False so you don't miss or duplicate any, then release the capture. Write the intensities as a single-column `intensity` Parquet file via pandas/pyarrow. For the bootstrap CI, set `np.random.seed(42)` exactly once before the resampling loop (not per-iteration), draw 10,000 resamples of the full intensity array with replacement (`np.random.choice(..., replace=True)`), compute the mean of each resample, and take the 2.5th/97.5th percentiles of that distribution as the CI bounds — this ordering and exact seed placement matters for matching the reference implementation within tolerance. Round all three JSON values to 4 decimal places before writing metrics.json.