Diagnose before editing: start the app and nginx manually to reproduce the 502, then grep `/home/user/nginx/nginx.conf` for the `proxy_pass` line — it points at a Unix socket path that doesn't match where `app.py` actually binds (`/home/user/app/app.sock`); correct only that path so it reads `proxy_pass http://unix:/home/user/app/app.sock;`. Confirm the fix by starting `app.py`, starting nginx with `nginx -p /home/user/nginx -c /home/user/nginx/nginx.conf`, and curling `127.0.0.1:8080` for a 200. Next write `/home/user/supervisord.conf` with `[program:app]` (command `python3 /home/user/app/app.py`) and `[program:nginx]` (command running nginx in foreground with `-g 'daemon off;'`), both with `autostart=true` and `autorestart=true`, parseable by Python's `configparser`. Finally write `/home/user/monitor.py` as a loop polling `http://127.0.0.1:8080/` every 2 seconds (catch connection errors and treat as status `000`); whenever status != 200, write `/home/user/alerts/alert_<unix_epoch>.eml` with the exact To/From/Subject/body template given, substituting the real status code in both the Subject and body lines. Test end-to-end: launch everything via supervisord, run monitor.py in background, `pkill -f app.py` to force a 502, wait a few seconds, and confirm an alert file with correct content appears.