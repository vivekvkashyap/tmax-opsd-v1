Start by inspecting `/app/vendored/seqmotif-0.4.2/setup.py` to see how the C extension is declared — a C extension that calls math functions like `exp`/`log` needs the extension's `libraries` list to include `'m'` (the math library) or the linker will fail/produce an import-time undefined-symbol error even though compilation succeeds; add that before doing anything else. Next, grep the C source (likely in a `src/` subdirectory) for a variable literally named something like `pseudocount` initialized to `0.0` — this is a regularization term that prevents a singular/zero-count transition matrix when a nucleotide never appears in the input sequence; change its initial value to `0.01` per the task spec. Rebuild and install with `pip install .` (or `python3 setup.py install`) from within the vendored directory, and verify the import works with a quick `python3 -c "from seqmotif import MCMCSampler; print(MCMCSampler().fit_predict('ACGT'))"` before writing the wrapper script. For `analyze_motif.py`, take `sys.argv[1]` as the sequence, call `.fit_predict()`, and format each float to exactly 4 decimals joined by commas after the literal prefix `Posterior: ` with nothing else printed — extra whitespace, newlines, or logging output will break the bit-for-bit oracle comparison.