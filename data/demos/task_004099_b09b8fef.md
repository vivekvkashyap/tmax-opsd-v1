Treat `/app/kinetics_fitter` purely as a probe: run it against a handful of files copied from `/home/user/data/clean/` and `/home/user/data/evil/` (it takes one file argument) and observe differences in exit code, runtime (watch for hangs — use `timeout`), and stdout to characterize what separates clean from evil at the oracle level, then generalize that signal into your own detection logic in `/home/user/filter.py` rather than shelling out to the binary at grading time (the grader tests your script standalone, and evil inputs may hang the oracle). Since the underlying data represents decay vs. growth curves, fit or estimate the curves yourself with `scipy.optimize.curve_fit` against an exponential model (or use a cheaper signal like the sign of a linear/log-linear regression slope on the trace, which correlates with growth vs decay) to decide clean vs evil without needing the oracle's exact internals. Read each `.csv` in `<input_dir>`, classify it, and `shutil.copy` only the clean ones to `<output_dir>`, preserving filenames. Validate your classifier against both provided corpora directories yourself (compare your accept/reject decision to which folder each file actually lives in) before finalizing, since silent misclassification of even one file fails the 100% criterion.