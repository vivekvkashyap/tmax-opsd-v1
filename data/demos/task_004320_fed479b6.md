Implement the script as a strict, ordered pipeline and be careful about reproducibility, since the grader will diff exact output. Load the CSV with pandas, compute `df[['F1'..'F5']].corrwith(df['Target'])` (or equivalent per-column `.corr`), take absolute value, and select the top-2 by magnitude — sort those two selected names alphabetically only for the output line, but use them in their natural pairing to build the `Interact` product feature. Set `np.random.seed(42)` immediately before the bootstrap loop (not earlier), and inside the loop use `df.sample(n=len(df), replace=True)` (letting pandas draw its own random state from the global numpy seed) so the sequence of draws matches a reference implementation exactly — any deviation in sampling method (e.g. using `np.random.choice` on indices directly instead of `DataFrame.sample`) will silently produce different bootstrap coefficients. Fit `LinearRegression(fit_intercept=False)` on the `Interact` column each iteration, collect the 1000 coefficients, average them, and round only the final mean to 4 decimals. Write the two-line output file exactly as specified (feature names comma-separated on line 1, rounded mean on line 2) and actually execute the script to produce `/home/user/etl_output.txt`.