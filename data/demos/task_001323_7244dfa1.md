First list archive entries without extracting (`tar -tvf` or `tar -tf`) so you can classify each path independently of tar's own extraction-time sanitization (GNU tar silently strips leading `/` and rewrites `../` sequences on extract, which would hide the very paths you need to detect and log). For each raw path string, apply the classification yourself: absolute paths start with `/`; for relative paths, logically normalize the path (resolve `.`/`..` segments purely as string/path arithmetic, e.g. Python's `posixpath.normpath` or manual segment-stack logic) and flag it unsafe only if the normalized result would climb above the base directory (starts with `../` or is empty pointing outside). Write the exact original (unnormalized) unsafe path strings, sorted alphabetically, to the log file — don't log the normalized form. For extraction, do not just run `tar -x` and hope for the best; extract members individually (e.g. iterate with a tar library or `tar -x --occurrence` per-file) so only the paths you deemed safe land in the output directory, preserving their given relative structure. Finally, grep/sed/awk over `*.log` files under the extraction dir for lines beginning exactly with `ERROR` (anchor the match, case-sensitive) into the summary file.