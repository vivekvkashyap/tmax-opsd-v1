Structure `/home/user/fix_and_bench.py` as a sequential pipeline, verifying each stage before moving to the next. For version resolution, parse the semver strings from versions.json as (major, minor, patch) integer tuples so comparison is correct (not lexicographic string sort), filter for the >=1.1.0,<2.0.0 range, and take the max. For checksum verification, compute `hashlib.sha256(open(patch_path,'rb').read()).hexdigest()` and compare it against the line in checksums.txt matching that version — abort loudly if they don't match rather than silently continuing. Apply the patch with `subprocess.run(["patch","-p1","-i",patch_path], cwd="/home/user/rust_processor")` and check the return code. Before compiling, spawn the mock HTTP server (e.g. with `http.server`/`socketserver` or a small `BaseHTTPRequestHandler` subclass) in a background thread bound to 127.0.0.1:8080 that returns exactly the specified JSON body on GET /data, and track a flag/counter that flips to true when a request lands, so `mock_hit` is only true if the Rust binary actually hit it — start the server before `cargo build --release` runs the binary. Time each of the 5 runs with `time.perf_counter()` around `subprocess.run`, average in milliseconds, and write the report.json with exactly the four specified keys.
