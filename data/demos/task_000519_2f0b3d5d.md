Implement each numbered step as a separate, checkable block in `analyze.py` rather than one long pass, since a mistake in reshape order or reduction dtype will silently corrupt everything downstream. Load with `numpy.load` and reshape `(100,50,50)` to `(100,2500)` using the default C-order `.reshape(100, -1)` (this keeps the first axis intact and flattens the last two together, matching "keeping the first axis intact"). For the instability test, iterate columns, accumulate the unstable sum with an explicit Python loop using a `numpy.float32` accumulator (a vectorized `.sum()` would use pairwise summation and not reproduce the same absorption error — you must add elements one at a time in that dtype), compute `math.fsum` on the same column (cast to Python floats), and track the maximum absolute difference. For SVD, call `numpy.linalg.svd(data2d, full_matrices=False)`, take `Vt[0]`, and sum absolute values. For the bootstrap, set the numpy seed to 42 immediately before the loop (not earlier — any other random call beforehand shifts the sequence), then do exactly 1000 iterations of `numpy.random.choice(errors, size=len(errors), replace=True)` and collect the sample means, taking the 2.5/97.5 percentiles via `numpy.percentile`. Dump all four floats to `/home/user/profile_results.json` under the exact key names given.
