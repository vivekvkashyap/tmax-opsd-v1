Start by diffing/inspecting `/app/bash-log-utils/bin/stratify_errors.sh` for the deliberate bug: look for an early-exit condition inside its awk/loop logic (e.g. an `NR > N { exit }` guard) that truncates processing after a handful of lines — remove or relax that condition so it scans the entire input stream while still capping output at exactly 2 sampled rows per endpoint whose status column is >= 400 (use an associative array keyed by endpoint, incrementing a counter and only printing while counter <= 2). Also check the `Makefile`'s install target/PREFIX — if it targets a system path you can't write without root, override it (`make install PREFIX=/home/user` or edit the Makefile) so `make install` succeeds and lands the tool at `/home/user/bin/stratify_errors`. For Part 2, compute top-5 error endpoints with awk/sort/uniq -c on column 5 (status) and column 4 (endpoint), sorted by frequency descending, and feed the full log through your fixed tool for the stratified sample. For Part 3, implement the HTTP server using a loop of `nc -l -p 9090` (or `/dev/tcp` in a `while true` accept loop) that parses the request line for the path and writes a proper `HTTP/1.1 200 OK` response with `Content-Length` and a blank line before the body, looping to keep accepting subsequent connections — a common mistake is a server that only answers once and exits.
