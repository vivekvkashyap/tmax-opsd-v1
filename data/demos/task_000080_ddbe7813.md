Design the algorithm on paper before coding: for each integer second t from 1 to 15, you need to (a) find temperature via linear interpolation between the nearest known timestamps at or before/after t (compute slope = (v2-v1)/(t2-t1), value = v1 + slope*(t-t1)), and (b) find state via forward-fill (carry the last seen state forward, using the very first row's state before any change occurs). In your C program, parse both CSVs into arrays of (timestamp, value) pairs first, then iterate t=1..15 using two pointers/indices to track the current interpolation bracket and the current forward-fill state, advancing each pointer only when t passes the next known timestamp — don't naively re-scan the whole array each iteration if avoidable, but correctness matters more than speed here. Format each output line exactly as `timestamp,temperature,state` with `%.1f` for temperature and no header, no trailing spaces. Compile with `gcc /home/user/processor.c -o /home/user/processor`, run it, then diff `/home/user/merged.csv` against your own manual hand-trace of a few rows to catch off-by-one bracket errors at the interpolation boundaries.