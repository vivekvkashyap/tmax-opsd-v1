Work in this order: first use `ffmpeg -i /app/camera_01.mp4 -vf blackdetect=d=0:pix_th=0.00 -f null -` (or the `blackframe` filter) and parse stderr for detected black-frame/blackdetect events to count exact black frames — don't guess, count what the filter reports (adjust pix_th/d if it misses single-frame blackouts). Second, lock down the file: `chmod 600` then use `setfacl -m u::rw,g::---,o::---` (and remove any extra ACL entries with `setfacl -b` first) so `getfacl` shows exactly `user::rw-`, `group::---`, `other::---`. Third, write a minimal HTTP server (Python's http.server/BaseHTTPRequestHandler or Flask) bound to 127.0.0.1:9042 that checks the `Authorization` header equals `Bearer sec-token-8819` exactly, returning 401 on mismatch/missing, else the JSON with the count you measured. Finally write `/home/user/deploy.sh`: it should find and kill any process bound to port 9042 (e.g. `fuser -k 9042/tcp` or `lsof -ti:9042 | xargs kill`), reapply the ACL command, then launch the server with `nohup ... &` or `disown` so it survives the script exiting, and chmod +x the script itself.