Handle build/organization and the validator as separate milestones. For the build: move `.c` files to `src/`, `.h` files to `include/`, then update `CMakeLists.txt`'s source paths and add `target_include_directories`; to link the vendored mongoose library, use `find_library(MONGOOSE_LIB mongoose PATHS /app/deps)` plus `target_include_directories(dsp_server PRIVATE /app/deps)` and `target_link_libraries(dsp_server ${MONGOOSE_LIB})`, then confirm `cmake . && make` succeeds cleanly. For the validator, don't guess the rules — actually transcribe `/app/voicenote.wav`: install `ffmpeg`/a speech-to-text tool (e.g. convert to a compatible format and run a Python `speech_recognition`/Whisper-style pipeline, or any available offline STT) and read the literal spoken text to extract the concrete numeric opcode values, byte sequence, and length limit it specifies — these are the actual thresholds your validator must enforce, do not assume typical values. Implement `validate_bytecode` in `src/validator.c` checking length against the discovered max, scanning bytes for the discovered forbidden single opcode, and scanning for the discovered two-byte forbidden adjacent sequence; return 1 only if none of the rules are violated. Test it directly against files in `/app/corpus/clean/` and `/app/corpus/evil/` (write a tiny test harness that calls the function on each file's bytes) before considering it done.
