Build this as a straightforward parse-compute-test pipeline and lean on `scipy.stats` rather than hand-rolling statistics. Parse each log line in `/home/user/experiment_logs/model_A.log` and `model_B.log` by splitting on the literal markers `Latency: `/`ms` and `Output: ` to extract the latency (ms) and generated text robustly (don't rely on fixed-width slicing, since text length varies); count tokens as `len(output.split())` (whitespace-split, matching the spec exactly), and compute `TPS = tokens / (latency_ms / 1000)` per run. Collect TPS arrays for both models, then run `scipy.stats.ttest_ind(b_tps, a_tps, equal_var=True)` — note the argument order matters, since the task wants the statistic to represent B minus A, so B must be the first argument. Compute the 95% CI for the mean difference using the pooled-variance formula: pooled variance from both samples' `ddof=1` variances weighted by degrees of freedom, standard error from pooled variance and sample sizes, and the t-critical value from `scipy.stats.t.ppf(0.975, df)` where `df = nA + nB - 2`; margin = t_crit * se, applied around `mean_B - mean_A`. Round every value to exactly 4 decimal places before writing the JSON with the exact required keys.
