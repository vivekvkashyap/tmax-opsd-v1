Work through the vendored Rust package layer by layer: first make it compile, then make it correct, then make it fast. Run `cargo build` in `/app/graph_engine_rs` to see the exact Cargo.toml error (likely a malformed or incomplete `[dependencies]` table) and fix the manifest so `serde`, `serde_json`, `tokio`, `axum`, and `csv` resolve with the features the code actually uses. Next inspect `src/graph.rs`'s `build_adjacency`-style logic: a nested loop over every node times every edge without a `source == node.id` filter creates an implicit cross join, attaching all edges to all nodes. Replace it with a single pass over edges only — `for e in edges { adj.entry(e.source).or_default().push(e.target); }` — which is both correct (each edge belongs only to its source node) and O(E) instead of O(V*E). After fixing, run the server against `/home/user/data/nodes.csv` and `edges.csv`, confirm it binds `0.0.0.0:8080`, and manually curl both `/shortest_path` and `/centrality` endpoints to sanity-check the adjacency is no longer duplicated before leaving it running in the background.
