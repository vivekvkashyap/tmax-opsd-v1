Diagnose before editing: read `/app/query_api/app.py` to find where it selects from `routes_cache`, then replace it with a SQLite recursive CTE (`WITH RECURSIVE paths(node, cost, path) AS (base case UNION ALL recursive step joining routes) SELECT ...`) that walks the live `routes` table from `source` to `destination`, tracking cumulative cost and guarding against revisiting nodes to avoid infinite recursion. Recursive CTEs over an unindexed edge table are slow, so create a covering index (e.g. on `routes(source, destination, cost)`) against `/app/data/routes.db` with `sqlite3`, and use `EXPLAIN QUERY PLAN` to confirm it's used. Restart the query service (`systemctl --user restart query-api`, or check the actual unit name with `systemctl --user list-units`) so the code change takes effect. For `analyze.py`, keep it fast: query the API concurrently (thread pool or async HTTP) rather than serially, one call per `STORE_*` destination, collect total costs, sort ascending, assign rank starting at 1, and dump the required JSON shape — profile with `time python3 analyze.py` to confirm it's under the 2-second budget.
