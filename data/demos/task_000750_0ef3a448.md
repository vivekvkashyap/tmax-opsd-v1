First inspect the schema with `sqlite3 /home/user/network.db ".schema"` to see the actual table/column names, then read /home/user/slow_query.sql to see it self-joins the connections/edges table on target=source — use `EXPLAIN QUERY PLAN` on that query to confirm it's doing full table scans, then add indexes on the join columns (the source and target columns of the edges table) with `CREATE INDEX ... ON edges(source)` / `CREATE INDEX ... ON edges(target)`, re-run EXPLAIN QUERY PLAN to confirm the indexes are used, and do not alter any row data. For analyze.py, connect via sqlite3, SELECT all edges (source, target, weight) and node names, build a `networkx.DiGraph` adding edges with `weight=` set from the weight column, run `nx.pagerank(G, weight='weight')`, sort nodes by pagerank descending, take the top 3, round each score to 4 decimals, map node ids back to node_name, and write the JSON array in the exact specified key order/format to /home/user/report.json. Run the script and manually sanity-check that the highest-pagerank node is the one receiving the most/heaviest inbound edges, matching graph-theory intuition, before finishing.
