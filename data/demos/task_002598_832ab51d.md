Model this as a longest-path-on-a-DAG problem solved via a recursive CTE in SQLite. Write a recursive CTE that starts from the row in `jobs` where `name = 'extract_sales_data'`, and recursively joins through `dependencies` (parent_id -> child_id) to walk every path forward, accumulating a running sum of `base_cost` (including both the root and the current node) at each step. Then, from all rows reached, filter down to only those job ids that never appear as a `parent_id` in `dependencies` (these are the leaves), and for each leaf take the MAX of the accumulated path cost across every path that reaches it (since a DAG can have multiple paths converging on one leaf via different parents). Finally sort leaves by that max cost descending and emit `{"leaf_job": ..., "total_path_cost": ...}` objects to `/home/user/pipeline_plan.json`, matching the exact key names and structure shown in the task. Double check your CTE doesn't silently drop the root's own base_cost or double-count it.
