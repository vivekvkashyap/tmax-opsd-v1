Implement the Metropolis-Hastings sampler literally to spec rather than relying on a library shortcut — the exact update rule matters for the acceptance test. Set the RNG seed to 42 immediately before the sampling loop begins (not at script start if other random calls happen earlier). At each of the 50,000 iterations: propose `lambda_new ~ Normal(lambda_current, 0.5)`; if it falls outside [0,10], reject immediately (prior density is 0, so acceptance probability is 0 — don't compute a likelihood ratio for it); otherwise compute the Poisson log-likelihood `sum(y_i * log(lambda*t_i) - lambda*t_i)` for both current and proposed lambda (drop the constant factorial term since it cancels in the ratio), take the log acceptance ratio, and accept if `log(uniform(0,1)) < ratio` or if ratio >= 0. Store every lambda (post-acceptance-or-rejection) in the chain, discard the first 10,000 as burn-in, and average the remaining 40,000. Read the reference float from `/home/user/reference.txt` verbatim (don't hardcode it), round your computed mean to 2 decimals, compare via `abs(computed - reference) <= 0.05`, and write the JSON report with the exact three keys specified.
