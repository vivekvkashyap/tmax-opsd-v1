Build this pipeline in discrete, independently-testable pieces. First inspect `/home/user/bin/legacy_billing` by running it manually to see its exact prompt text and confirm it reads a hidden password (likely via `read -s`). Write `/home/user/fetch_costs.exp` using `expect` primitives: `spawn`, `expect "Username:"`, `send "finops\r"`, `expect "Password:"`, `send "CostCut2024!\r"`, then capture remaining output (e.g. redirect the spawned process's stdout, or use `log_user`/`interact` carefully) and write only the JSON payload to `/home/user/staging/report.json` — strip any prompt echo/leading whitespace so the file is valid JSON. Next, in `/home/user/deploy.sh`, invoke the expect script, then use `jq -r .total_cost` (or similar) to extract the numeric value and compare with a numeric comparison against 5000 (careful with float comparison in bash — use `awk` or `bc` since bash arithmetic is integer-only) to decide whether to `cp` into `/home/user/prod/report.json`. Make the script executable and actually run it. Then start `python3 -m http.server 8080 --directory /home/user/prod &` (backgrounded, persists). Finally send the email via raw SMTP commands (e.g. `curl smtp://127.0.0.1:1025 --mail-from ... --mail-rcpt ... -T -` or a short Python `smtplib` snippet) with the exact sender/recipient/subject/body given.