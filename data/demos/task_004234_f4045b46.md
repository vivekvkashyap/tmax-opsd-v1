For duration extraction, run `ffprobe -v error -show_entries format=duration -of default=noprint_wrappers=1:nokey=1 /app/interview.wav` and write its raw output (unmodified, including full decimal precision) straight to `/home/user/audio_duration.txt` — don't round or reformat it, since the verifier expects ffprobe's exact string. For the validator script, use `jq` to pull `.speaker`, `.duration`, and `.transcript` from the JSON argument, then check all EVIL conditions explicitly: speaker missing/empty/null (`jq -e '.speaker == null or .speaker == ""'` or check jq's `has()`), duration not present/non-numeric/`< 1.0`/`> 100.0` (use `jq`'s numeric comparisons, guarding against non-numeric duration causing jq errors), and transcript containing the banned phrases case-insensitively (lowercase both sides before substring match, e.g. with `grep -iq`). Any single EVIL condition should short-circuit to `exit 1`; only exit 0 if none apply. Suppress all stdout/stderr from `jq`/`grep` (redirect to `/dev/null` or use `-e`/`-q` flags) since the script must be silent. Test against a few hand-written clean and evil JSON samples locally before relying on the hidden corpus, and remember `chmod +x` the script.
