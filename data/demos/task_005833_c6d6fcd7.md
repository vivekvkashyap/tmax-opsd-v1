Diagnose before editing: the symptom (last-decimal-place fluctuation across runs despite a fixed seed) is a classic signature of a parallel/OpenMP reduction, since floating-point addition is not associative and thread scheduling changes the summation order each run — grep `sim_core.c` for `#pragma omp` to confirm. Fix it by removing the `parallel for reduction` pragma (or forcing `omp_set_num_threads(1)`) so the loop accumulates strictly left-to-right in a single thread — this sacrifices speed but guarantees a fixed operation order and thus a fixed result. Rebuild and reinstall the extension with `python3 setup.py build_ext --inplace install` (or `pip install .`) into the same environment the notebook uses. Execute the notebook non-interactively with `jupyter nbconvert --to notebook --execute --inplace simulation.ipynb` (or `papermill`), then capture its printed output and confirm determinism by running it two or three times in a row and diffing the results before writing the final value to `reproducible_energy.txt` — do not accept a single run as proof of the fix.
