Transcribe `/app/k8s_policy.wav` first (e.g. with `whisper` or `whisper-cpp`) and read the transcript carefully to pin down the exact field and condition being restricted — don't assume a generic Kubernetes security rule, derive it from what's actually said. Once you know the policy, scaffold a Rust binary crate at `/home/user/validator` with `cargo new`, add a YAML crate (`serde_yaml` or similar) as a dependency, and read the manifest file given as `argv[1]`. Because K8s files can contain multiple `---`-separated documents, use a multi-document deserializer (e.g. `serde_yaml::Deserializer::from_str` iterated, not a single `from_str` call which only reads the first document) and check every document independently. For each document, look for the flagged field at both plausible locations a manifest could put it (e.g. directly under `spec`, and under `spec.template.spec` for pod-template-based resources like Deployments) since the policy could appear in either shape depending on manifest kind. If it's set to the violating value in ANY document, `std::process::exit` with a non-zero code immediately; only exit 0 after all documents pass. Build with `cargo build --release` so the binary lands at `target/release/validator`, then run it against every file in `/app/corpora/clean/` and `/app/corpora/evil/` and confirm exit codes match expectations before considering it done.