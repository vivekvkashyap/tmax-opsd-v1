In `/home/user/analyze_telemetry.py`, read `/home/user/translation_telemetry.csv` with the built-in `csv` module row-by-row (or `pandas.read_csv(chunksize=...)`) instead of loading the whole file into a list, to keep memory bounded. For each row apply the three validity checks in order — `latency_ms <= 0`, `words_translated <= 0`, and a regex like `^[a-z]{2}-[A-Z]{2}$` for `language_code` — counting and skipping any row failing at least one, then write the total invalid count as a plain integer to `/home/user/invalid_count.txt`. For valid rows, parse the ISO timestamp to extract the hour (0-23) and accumulate `(language, hour) -> (sum, count)`; after the full pass, compute the 24-element average-latency vector per language, filling `0.0` for hours with no data. Compute Euclidean distances from `es-ES`'s vector to every other language's vector, but do the distance computation via `multiprocessing.Pool.map` over the list of other languages (each worker takes a language's vector plus the baseline vector and returns the distance) to satisfy the parallelism requirement. Round each distance to 4 decimal places and dump the dict (excluding `es-ES`) as JSON to `/home/user/distances.json`, then actually execute the script and inspect both output files.