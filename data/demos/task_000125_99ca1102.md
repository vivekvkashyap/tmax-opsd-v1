Build this as a straightforward line-by-line ETL, not a regex-only one-shot. First create the two output directories with `mkdir -p`. Then in Python, read the raw file, and for each line: strip and lowercase it, then validate with a whitelist regex like `^[a-z0-9. +\-*/=()]*$` (reject the line entirely if it fails — do not silently drop bad characters). For tokenization on valid lines, use a single regex with alternation via `re.findall`, e.g. a pattern matching `\d+\.?\d*` (digits with an optional single decimal point) OR `[a-z]+` (letters) OR one of the single symbols `+-*/=()`, applied left-to-right so `2x` naturally splits into `2` and `x` and spaces are simply skipped (not matched by any alternative). Accumulate a running total-line counter, a valid-line counter, and a set() of all tokens seen across valid lines for vocab_size. Write one JSON object per valid line to the .jsonl file with `json.dumps({"tokens": [...]})`, and write the three metrics as a single JSON object to the run_log path. Double check the token regex order: put the numeric pattern before the letter pattern so digits and letters are never merged into one token, and be careful that the period in the character-whitelist and in the numeric-token regex are treated consistently.
