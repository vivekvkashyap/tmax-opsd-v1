Write a Python script rather than doing this by hand. Load the coordinates with `numpy.loadtxt`, then fit `scipy.stats.gaussian_kde(positions)` using its default bandwidth (do not override it). To find the coordinate X where the cumulative KDE mass reaches 0.90, define a function `f(x) = kde.integrate_box_1d(-np.inf, x) - 0.90` and pass it to `scipy.optimize.root_scalar` with a bracket, e.g. `bracket=[0, 150000]` as given in the task, using a bracketing method like Brent's (the default for `root_scalar` with a bracket). Do not try to invert the KDE analytically or approximate the percentile from a histogram/sorted array — that will not match the continuous KDE integral the task defines, and small numerical differences will fail an exact-match check. After solving, round the root to exactly 2 decimal places with Python's `round()` or an f-string format spec (`f"{x:.2f}"`), and write only that string (no extra whitespace or newline issues beyond a trailing newline) to the specified output file. Double-check you're reading from and writing to the exact paths given in the task.
