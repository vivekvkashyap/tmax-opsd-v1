Build the script as a strict sequence of checks, not a single curl chain. First install deps and launch `upload_server.py` in the background, giving it a moment to bind port 8080. Decrypt the token with `openssl enc -aes-256-cbc -d -pbkdf2 -in /home/user/credentials/token.enc -pass file:/home/user/credentials/pass.txt` and capture it into a shell variable — match the exact cipher/kdf flags used to encrypt it or decryption will silently fail. Use `curl -s -o /dev/null -w '%{http_code}'` to capture status codes for the no-auth and with-auth upload attempts separately. For the path-traversal check, inspect the server's upload handling: since it takes filename from a form field independent of the actual uploaded file, craft a `filename` value using `../` segments that resolves to the absolute path of `critical_config.txt` relative to wherever the server saves uploads, with a small text file body containing "PWNED". Hash the target file with `sha256sum` both before and after the upload attempt, compare them, and assemble the JSON report with `jq -n` or a heredoc, making sure the boolean is unquoted `true`/`false`, not a string.
