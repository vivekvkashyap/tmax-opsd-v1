Implement this as three clearly separated pieces in the C program. First, the analytical normal-normal conjugate update: compute prior precision `1/sigma0^2`, data precision `n/sigma^2`, posterior precision as their sum, posterior variance as its reciprocal, and posterior mean as the precision-weighted average of the prior mean and sample mean — derive these from the trace file's actual values rather than hardcoding anything. Second, implement Metropolis-Hastings exactly as specified: start the chain at mu=100.0, propose from `N(current, 5.0)` each step, accept/reject using the ratio of (prior * likelihood) at proposed vs. current (working in log-space avoids overflow), run 100,000 iterations, discard the first 10,000 as burn-in, then compute empirical mean/variance of the remainder. Third, count the fraction of retained samples strictly greater than 105 for the posterior probability. Use `drand48`/`rand` with a seeded RNG, format each output value with `%.4f`, and sanity-check that your MCMC mean/variance land close to your analytical values — a persistent large mismatch signals a bug in the acceptance ratio, not just noise.
