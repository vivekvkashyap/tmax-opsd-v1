Parse `/home/user/dataset_rules.conf` as simple key=value lines to get `ARCHIVE_DIR` and `MAGIC_BYTES_COUNT` rather than hardcoding them. For each `*.tar.gz` in that directory, first check integrity non-destructively (e.g. `tar -tzf file.tar.gz >/dev/null 2>&1` or `gzip -t`) and skip any archive that fails, so corrupted files never appear in output. For valid archives, list contents and filter names ending in `.bin`. To read just the header bytes of an internal file without extracting to disk, stream it out via a pipe, e.g. `tar -xzOf archive.tar.gz path/inside/archive | head -c "$MAGIC_BYTES_COUNT" | xxd -p | tr -d '\n'` (or use Python's `tarfile` module to open a member as a file object and `.read(n)`), then lowercase and join the hex bytes with no separators. Build each output line as `archive_filename:internal_file_name:hex_string`, collect all lines, sort them alphabetically, and write the final sorted list to `/home/user/magic_summary.txt`. Verify the corrupted archive contributes no lines.