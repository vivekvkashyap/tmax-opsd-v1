Extract in the exact two-step nesting the task describes: `unzip legacy_docs.zip -d /home/user/workspace`, then find the resulting `docs_archive.tar.gz` inside and extract it with `tar -xzf docs_archive.tar.gz -C /home/user/workspace` so the `docs/` directory lands directly under workspace (don't double-nest by extracting into a subdirectory). Use `grep -rl '\[DEPRECATED_MACRO\]' --include='*.md' /home/user/workspace/docs` to get the exact set of files to modify — this is the gate that determines which files get touched, so compute it once and reuse it for both the edit step and the log step rather than re-deriving it differently each time (which risks including files that only contain `OldCorp` without the macro, or `.txt` files). For each matched file, apply both substitutions in place (e.g. `sed -i 's/\[DEPRECATED_MACRO\]/[UPDATED_MACRO]/g; s/OldCorp/NewCorp/g' file`), being careful to escape the brackets as literal characters in the regex. Collect the same file list, sort it alphabetically, and write absolute paths one per line to `/home/user/migration.log`. Double check untouched files (no macro, `.txt` extension) still contain their original text afterward.
