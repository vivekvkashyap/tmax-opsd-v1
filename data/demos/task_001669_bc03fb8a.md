Handle corruption at the text level before JSON parsing: invalid `\uXXXX` escapes make `json.loads` throw, so pre-scan each raw line with a regex for `\u` followed by fewer/invalid hex chars and replace or strip those escape sequences (being careful not to mangle valid `\uXXXX` escapes) before parsing — do this per-line so one bad line doesn't sink the whole file. Compute the dedup key as SHA-256 of `f"{server_id}_{timestamp}"` and keep only the first record per hash, discarding later duplicates. Do the parse+dedupe step with `multiprocessing` (e.g. a `Pool.map` over line chunks) rather than a plain loop. For imputation, group by `server_id`, sort each group by timestamp, then run true linear interpolation over the null gaps (e.g. pandas `Series.interpolate(method='linear')`) for `temp_celsius` and `cpu_load` separately, followed by `bfill()` for any remaining leading nulls and `ffill()` for trailing nulls — order matters, interpolate first, then fill the edges. Bulk-insert into SQLite with the exact column types specified, then `GROUP BY server_id` with `AVG()`, round to 2 decimals, and write the CSV with the exact header given.