Build and test each pipeline stage independently before wiring them into a Makefile. First compile and run `schema_gen.c` manually to see what it actually prints, then decode that Base64 output by hand (`base64 -d`) to inspect the pipe-separated records and confirm the header format matches what the task describes — don't assume the field order, verify it. Write your migration script (Python is simplest) to: read the base64 file, decode it, split on `\n` then `|`, skip the header row, and map each record's three fields to `id` (cast to int), `username`, `joined_date`, plus a literal `active: true`, emitting a JSON array — use a real JSON serializer (e.g. `json.dumps`) rather than string concatenation to avoid quoting/escaping bugs. Then write the Makefile's `all` target as three sequential recipe lines: compile schema_gen.c with `gcc`, run it redirecting stdout to legacy.b64, then invoke your script against legacy.b64 producing migrated_schema.json. Run `make` yourself and inspect the resulting JSON to confirm it parses and the field types (integer id, boolean active) are correct, not just string-shaped.