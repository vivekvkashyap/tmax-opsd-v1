Fix and validate each broken layer independently before wiring the orchestration script, so failures are easy to localize. First patch `/app/graph_factor_lib/setup.py`: add `import numpy as np` and pass `include_dirs=[np.get_include()]` to the C-extension's `Extension(...)` definition, then run `pip install -e /app/graph_factor_lib` and confirm the C-extension actually builds (watch for the `numpy/arrayobject.h` error disappearing) before touching anything else. Next patch `graph_factor_lib/solver.py`'s `solve_laplacian` to regularize before inversion (`np.linalg.inv(L + 1e-5 * np.eye(L.shape[0]))`) — re-import the package fresh in a throwaway `python3 -c` smoke test on one `.edges` file to confirm no more `LinAlgError` before moving to orchestration. Only then write `run_pipeline.sh`: loop over `/home/user/data/*.edges`, derive each `{basename}` , and invoke `papermill /home/user/evaluate_model.ipynb /home/user/notebooks_out/{basename}_out.ipynb -p input_graph <path>`, checking each run's exit code. After all notebooks finish, the `*_metrics.txt` files should exist next to their inputs (or wherever the notebook writes them) — locate them, extract the numeric KL value from each (e.g. with `grep -oE` or `awk`), and compute the mean with `awk '{sum+=$1; n++} END {print sum/n}'`, writing only that bare float to `final_mean_kl.txt`.