Separate the two concerns cleanly: correct SQL aggregation and correct graph shortest-path, then combine. First inspect the schema (`nodes`, `backups`, `links`) directly with `sqlite3 /home/user/backup_meta.db .schema` and sample rows to understand cardinality — the described bug ("massively inflated numbers") is a classic implicit cross join from joining `backups` to `links` (or similar) without a proper join key, so write your per-node backup total as its own isolated, parameterized query: `SELECT SUM(size_gb) FROM backups WHERE node_id = ? AND status = 'COMPLETED'`, never joining tables that aren't directly related. Separately, load `links` (directional edges with `latency_ms` weights) into an adjacency structure in Go and run Dijkstra's algorithm from the source node name to the target node name, resolving names to ids via the `nodes` table first. For each node on the resulting shortest path, run the isolated backup-sum query and add them up. Use the `mattn/go-sqlite3` driver, `go mod init` + `go get` it, and always use `?` placeholders rather than string-formatting values into SQL. Write the final JSON with `path`, `total_latency_ms`, and `total_backup_gb` fields exactly, and run the program with the two required CLI args before checking the output file.