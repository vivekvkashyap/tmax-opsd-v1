Reproduce the failure first: build the shared library, run `python3 tests/run_all.py` (which loads the lib once and calls init/process/cleanup across two tests in the same process) and confirm it segfaults, then run each test file individually to confirm they pass alone — this isolates the bug to state carried across calls within one process, exactly as the report says. Read `src/processor.c` looking for any function that frees or invalidates a resource (heap allocation, static/global pointer, file handle) without resetting the associated pointer/flag afterward — a classic use-after-free pattern is a cleanup routine calling `free()` on a static pointer but leaving that pointer non-NULL, so the next init/use path assumes it's already allocated and writes through the dangling pointer. Fix it by nulling the pointer (or otherwise resetting state) immediately after freeing, so subsequent init calls correctly reallocate. Do not touch the Python tests or `ci.sh`. Rebuild and rerun `./ci.sh`, confirming it prints `CI PASS` and creates `ci_success.log`, and also re-verify by running the full test sequence directly to make sure the crash is truly gone, not just hidden by build caching.