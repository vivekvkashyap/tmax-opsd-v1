First recover the nesting-depth threshold from the audio: extract/convert `/app/audio/security_policy.wav` and transcribe it (e.g. via an offline STT tool, or extract the audio's raw waveform and listen/inspect it through available terminal tooling) — the spoken sentence states the maximum allowed depth as a number; use that recovered integer as your constant, don't guess a round number. For the Rust bug, the issue is `get_status` returning `&'a str` borrowed from a locally-owned `String` that's dropped at function end — fix by changing the return type to an owned `String` (and update/remove callers accordingly) rather than fighting the borrow checker with lifetimes; then `cargo build --release` with `crate-type = ["staticlib"]` already in Cargo.toml to produce `librust_engine.a`. In main.go, validate Rule A with a character-class check (reject anything outside `0-9 + - * / ( ) space`), and Rule B by tracking a running paren-depth counter across the string, rejecting if it ever exceeds your recovered max. Only on success, use `cgo` (`// #cgo LDFLAGS: -L<path> -lrust_engine`, `// #include` a small header declaring `int evaluate_safe();`) to call the Rust function, then print `CLEAN`/exit 0 or `EVIL`/exit 1. Test iteratively against both corpus directories.
