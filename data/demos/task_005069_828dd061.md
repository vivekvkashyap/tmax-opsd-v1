Split this into an extraction step and a transform step, and get the transform bit-exact first since it's fuzz-tested against a hidden oracle. For trajectory extraction, read the video frame by frame (e.g. via OpenCV or ffmpeg piping raw frames), and per frame find all pixels that are pure/near-pure red (255,0,0) on black, then compute their centroid as the simple mean of matching pixel (x,y) coordinates; append `[x,y]` in frame order to a JSON array. For `transform.py`, implement exactly three sequential passes with no shortcuts: (1) causal smoothing where point i is the average of the *raw* points from `max(0,i-2)` to i inclusive (careful — average over available window, not always divided by 3, especially for i=0,1); (2) compute the single global mean over the smoothed points and subtract it from every smoothed point; (3) classify each centered point into A/B/C/D by its sign quadrant using the exact `>=`/`<` boundary conditions given (note x>=0,y<0 falls to D by exclusion, so the four branches must be checked in the stated order). Concatenate tokens into one string per input. For the plot, explicitly set a non-interactive backend before importing pyplot (e.g. `matplotlib.use('Agg')`) since headless servers otherwise fail or produce blank images — do this before any other matplotlib import.