Attack this service-by-service, verifying each piece standalone before wiring the full stack. First inspect the existing `corpus/clean/` and `corpus/evil/` JSON files directly (`cat` them) to understand exactly which edge cases must pass/fail — let the fixtures teach you the boundary conditions rather than guessing. Implement `ValidateTelemetry(a, b []int)` in two clear passes: (1) walk each of `a` and `b` checking `a[i] > a[i-1]` strictly (reject on any `<=`), then (2) merge `a` and `b` into one sorted slice (e.g. `sort.Ints` on a concatenation, or a merge-step since both are already sorted) and check every adjacent gap is `<= 10`. Return `false` on any violation. Wire the HTTP handler to return 400 on `false`, 200 on `true`. For `start_services.sh`, diff it against what's needed: compile `worker.cpp` with `g++ -o worker worker.cpp -lpthread` (check its socket includes) and `main.go`+`validator.go` with `go build -o backend`, then launch both with `&` before starting nginx via `nginx -c $(pwd)/nginx.conf`. For `nginx.conf`, use `daemon off;` if run in foreground and a `location /api/ { proxy_pass http://127.0.0.1:9000; }` block. For `ci.sh`, loop `curl -s -o /dev/null -w "%{http_code}"` over each corpus directory and compare codes; only print `CI PASS` if every single check matches expectations.
