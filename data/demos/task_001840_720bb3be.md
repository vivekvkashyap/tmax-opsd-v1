Break this into independent extraction steps and verify each in isolation before combining. For the audio, transcribe `/app/experiment_log.wav` with a speech-to-text tool (e.g. `whisper` or any available ASR CLI) and read off the cooling rate as a float. For the PDB file, parse `/app/molecule.pdb` line by line, filtering `ATOM`/`HETATM` records and checking the element symbol (rightmost column, not the atom-name column, since names like `CA` are Carbon-alpha but the element column is authoritative) to count Carbon atoms. For the HDF5 integration, load `trials` with h5py, cast each row to `float64` BEFORE summing/integrating — never sum in float32, since accumulating 50,000 single-precision values causes reduction-order-dependent rounding drift that fails the tight tolerance. Use `numpy.trapz(data.astype(np.float64), dx=0.002, axis=1)`. For the bootstrap CI, seed with `np.random.seed(42)` exactly once before resampling, draw 10,000 resamples of the 100 integrals with replacement, compute the mean of each resample, then take the overall mean and the 2.5/97.5 percentiles. Write all six fields to `/home/user/summary.json` with the exact keys specified, double-checking JSON validity with a parser before finishing.