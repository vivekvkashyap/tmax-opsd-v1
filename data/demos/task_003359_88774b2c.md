Reverse-engineer the oracle behaviorally before writing any Rust: run `/app/oracle_sanitizer <base_dir> <input>` against a systematic battery of probes — plain safe filenames, `../` and multi-level traversal, URL-encoded variants (`%2e%2e%2f`, `%00`), literal null bytes, and strings containing `<`/`>` — and record both stdout/stderr and exit codes for each category to infer the exact precedence of checks (e.g. does it check null bytes before or after traversal? does XSS detection fire even on otherwise-valid paths?) and the exact error token strings emitted. Then fix `Cargo.toml` by adding a real `[dependencies]` section (something to percent-decode input, e.g. `percent-encoding` or `url`) since it was stripped out entirely. In `main.rs`, decode percent-encoding before any checks, add explicit substring checks for null bytes and for `<`/`>` emitting the exact discovered error strings, and — the key missing security check — after calling `std::fs::canonicalize`, verify the resulting path actually `starts_with` the canonicalized base directory before treating it as safe; reject otherwise. Build with `cargo build` and re-run your probe battery against both binaries diffing outputs until they match exactly before trusting the fuzzer.
