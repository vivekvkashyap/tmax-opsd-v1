Build the pipeline as discrete, testable stages. In `pipeline.sh`, use `ffmpeg -i /app/interview.wav -f segment -segment_time 15 -c copy /home/user/chunks/out%03d.wav` (create the output dir first) to get exact 15s chunks named `out000.wav`, `out001.wav`, etc. Install whisper (`pip install openai-whisper`) and loop over chunks in sorted filename order, running the `tiny` model on each (`whisper <chunk> --model tiny --output_format txt --output_dir ...` or the Python API) and append `filename,transcribed_text` rows (no header) to `/home/user/dataset.csv`, being careful to CSV-escape any commas/quotes in the transcript text. For the plotting bug, the root cause is `matplotlib.use('TkAgg')` on a headless box and calling `plt.show()` before `plt.savefig()` — fix by setting `matplotlib.use('Agg')` at the top (before importing pyplot) and removing/reordering the `plt.show()` call so `savefig` actually runs. Since grading measures Word Error Rate against a hidden ground-truth transcript, prioritize transcription completeness/accuracy over chunk boundary precision — don't drop trailing partial chunks and make sure every chunk actually gets transcribed (check for empty outputs).