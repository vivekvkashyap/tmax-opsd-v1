Structure the C program in clear phases: load `accounts.csv` into an id->risk_level map and `transfers.csv` into an adjacency list keyed by source_id (storing target_id and amount per edge). Then run a depth-first search from `A100`, tracking the current path (array of account ids) and a visited-in-this-path set to prevent cycles, and bounding recursion depth at 3 hops (i.e. stop extending once the path already has 3 transfers). At every node you visit after the start, check if it's `HIGH` risk and if so record the full path string, joined with `->`, and the summed amount along it as a complete flagged path — but keep exploring further from that node too if depth allows, since a HIGH node can also be an intermediary toward another HIGH node further down. After enumerating all valid paths, group them by destination_id, sort each group by total_amount descending (ties broken alphabetically by path_string) and assign ranks 1..n within each group. Finally sort all output rows by destination_id ascending, then rank ascending, and write them with no header to `/home/user/flagged_paths.csv` in the exact `destination_id,path_string,total_amount,rank` format. Compile with `gcc` and test against the given CSVs, checking your path enumeration and aggregation by hand for a couple of destinations before trusting the output.