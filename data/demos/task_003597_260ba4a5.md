Parse stdin exactly per the spec: first line V and E, then V node-definition lines (id, op, optional value — INPUT only), then E "from to" edge lines. Build, for each node, its list of incoming source-node ids in the order the edges were read (this order matters for ADD/MUL/MAX accumulation). Evaluate not with an arbitrary topological sort but by repeatedly scanning node ids 0..V-1 and evaluating any node whose incoming nodes are all already evaluated, looping until every node is done — this reproduces the same evaluation/accumulation order a naive reference implementation would use, so floating-point sums/products come out bit-identical rather than merely numerically close. Apply the stated defaults for nodes with zero incoming edges (ADD=0.0, MUL=1.0, MAX=-999999.0). Print every node id in ascending order with its value formatted to exactly 4 decimal places using standard rounding (Go's `%.4f`), one "<id> <value>" line each. Build with `go build -o /home/user/engine/bin`. Before finishing, fuzz-test your binary yourself on random small DAGs and sanity-check output formatting on edge cases (empty incoming lists, negative/zero values).
