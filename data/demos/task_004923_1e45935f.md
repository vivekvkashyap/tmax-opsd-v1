Transcribe `/app/intercepted_call.wav` (e.g. with `whisper`/`ffmpeg`) and listen for the three project codenames spoken — those exact names (case-insensitive) become part of your evil-detection rules, so extract them from the transcript rather than guessing. Then write `/home/user/classifier.py` as a layered checker on the file's text content: (1) case-insensitive substring search for each of the three recovered project names, (2) search for SSH private key headers like `-----BEGIN OPENSSH PRIVATE KEY-----` / `-----BEGIN RSA PRIVATE KEY-----`, (3) search for injection/reverse-shell indicators `nc -e`, `/bin/bash -i`, and `wget http` combined with execution context. Read the file defensively (open with `errors='replace'` or try utf-8 then fall back to latin-1) so odd encodings don't crash the script — a crash should not silently pass as "clean". Exit 1 if any rule matches, else exit 0. Validate against `/home/user/sample_corpus/clean/` and `/home/user/sample_corpus/evil/` first, understanding these are just examples—harden your patterns (case, spacing) since the hidden grading corpus will be larger and more varied.
