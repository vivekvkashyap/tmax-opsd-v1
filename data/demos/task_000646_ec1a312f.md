Parse the log with a regex or simple `split` per line rather than fragile string slicing: extract `IP:` up to the next space, `status:` up to the next space, and `resp_time:` up to `ms`. Build a dict counting, per IP, how many lines satisfy `status == "failed" AND resp_time > 300` (strictly greater — 300 itself does not count, only values above it). After counting, sort the resulting `(ip, count)` pairs primarily by count descending, and for ties sort by the IP string ascending — Python's `sorted(items, key=lambda kv: (-kv[1], kv[0]))` gives this in one pass, so don't rely on dict insertion order or a single-key sort which would get tie-breaking wrong. Take the first 5 entries and write them to `/home/user/top_anomalies.csv` with `csv.writer`, header `IP,AnomalyCount` first. Before finishing, manually spot-check a couple of IPs' anomaly counts against the raw log with `grep`/`awk` to make sure your threshold comparison (`>` not `>=`) and boolean AND logic are both correct.