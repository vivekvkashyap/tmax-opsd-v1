Work the three parts in sequence, testing each before moving to the next. First, run `python /home/user/app/build.py` to see the actual error — it will likely be a `NameError`/typo on a built-in like `print`; fix the literal typo so it exits 0 and prints exactly `Build complete`. Second, extract the crash ID from the binary `/home/user/app/memory.dmp`: don't assume the value, actually search the file for the literal marker text `CRASH_REF: ` and capture the alphanumeric token immediately following it, e.g. `strings /home/user/app/memory.dmp | grep -o 'CRASH_REF: [A-Za-z0-9-]*'` or `grep -a -o 'CRASH_REF: [A-Za-z0-9-]*' memory.dmp`, then strip the prefix and write only the bare ID (no trailing newline/spaces issues) to `/home/user/crash_ref.txt`. Third, read `/home/user/app/processor.py` to see exactly which input triggers `RuntimeError`, then write `/home/user/app/test_regression.py` with a `unittest.TestCase` that imports `process` and asserts `RuntimeError` is raised when called with the ID you extracted (not a hardcoded guess — use what you actually found in the dump). Run `python -m unittest test_regression.py` from `/home/user/app` to confirm it passes.