Structure the C++ program in the exact order of the spec so each stage is independently testable. Read the 1000 numbers from data.txt into a std::vector, then implement a `cvm(lambda, sorted_data)` function following the formula literally: `1/(12n) + sum((F(x_i) - (2i-1)/(2n))^2)` where i runs 1..n over the *sorted* sample and `F(x) = 1 - exp(-lambda*x)`. For the grid search, iterate lambda from 0.1 to 5.0 in steps of 0.01 (use integer loop counters like `for(int k=0;k<=490;k++) lambda=0.1+k*0.01` to avoid float accumulation drift) and track the minimizer. For bootstrap, seed `std::mt19937 rng(42)` once and reuse it across all 500 resamples (do not reseed per resample) with `std::uniform_int_distribution<int>(0,n-1)` to draw indices with replacement, rerun the same grid search on each resample, collect all 500 lambda estimates, sort them, and take the values at the 2.5th/97.5th percentile positions (be consistent about interpolation vs nearest-index). For numerical stability, directly compute `fabs((1.0 - exp(-lambda*x)) - (-expm1(-lambda*x)))` at x=1e-8 using the optimal lambda from step 3. Write all four values to results.json as plain floats with the exact key names given.