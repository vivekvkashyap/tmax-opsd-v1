Structure the Rust program as clear sequential passes so each is easy to verify independently. Pass 1 (parse+validate): read `sensor_data.csv` line by line, split on commas, skip lines that don't have exactly 3 fields or whose fields don't parse as expected types, and discard records whose temperature falls outside [-20.0, 60.0] — keep valid records sorted by timestamp for the next step. Pass 2 (resample): generate the 25 target timestamps from 1710000000 to 1710086400 stepping by 3600, and for each one do a forward-fill lookup — the most efficient correct approach is to walk the sorted valid records with a pointer that only advances, taking the last record whose timestamp is `<=` the target (default 0.0 if none exists yet); don't accidentally use `<` or nearest-neighbor, it must be strictly "most recent at or before". Pass 3 (stats): compute min/max/mean over the 25 resampled values, formatting mean with `format!("{:.2}", mean)` and min/max with `format!("{:.1}", v)`. Pass 4 (template): read `report_template.md`, do literal `.replace("{{MIN}}", ...)` etc. (order doesn't matter since placeholders are distinct strings), and write to `daily_report.md`. Compile with `rustc` or `cargo run` and confirm the output file exists with the substitutions applied before finishing.
