Work in two independent phases. First, fix the build: `mkdir -p src include`, `git mv`/`mv` the `.c` files into `src/` and `.h` files into `include/`, then rewrite the Makefile to add `-Iinclude -fPIC -shared` and compile into `libhelpers.so` (test with `make` until it succeeds cleanly). Second, write `router.sh` as a strict whitelist parser, not a blacklist patch: read stdin, isolate the JSON body line (e.g. with `grep`/`sed` for the line after the blank line), extract the base64 value between the `"payload": "` quotes, decode it with `base64 -d`, and validate the decoded bytes byte-by-byte — reject anything with a byte outside 0x20-0x7E OR matching backtick/'/"/backslash. Do this validation on the raw decoded bytes (e.g. iterate codepoints in a language like `xxd`/`od`/awk, not just a `grep -P` regex that can mis-handle binary/multibyte edges). Only on full validation success, invoke `/app/bin/query_engine` with the *original* base64 string as `argv[1]` and print `HTTP/1.1 200 OK` then its stdout; otherwise print exactly `HTTP/1.1 400 Bad Request` and nothing else. Chmod +x the script and test against both benign and malicious/control-character payloads before finishing.