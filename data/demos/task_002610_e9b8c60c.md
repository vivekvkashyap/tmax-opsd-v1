Work through this as a staged fix rather than a rewrite: (1) run `make` first to see the actual link error, then fix the Makefile's object list/link rule so `ds.o` and `math_utils.o` are both compiled in and linked, and add `-lm` since you'll need libm for the variance calculation; (2) in `main.c`, replace the unsafe fixed-size copy with one that bounds the copy length to the destination buffer size (e.g. cap at 128 chars, use a length-limited copy, and always explicitly null-terminate) rather than trusting the parsed header length; (3) implement `compute_token_variance` as population variance: mean of ASCII byte values, then mean of squared deviations, guarding the empty-string case to avoid divide-by-zero; (4) implement the hash set with the specified hash (sum of ASCII mod 256) and linear probing for both lookup and insertion — test membership by walking probes until an empty slot or a match; (5) fill in the nginx `location /api/auth` block with `proxy_pass http://127.0.0.1:9000;` and start nginx with the exact `-c`/`-p` flags given. Finally start the backend, send the same token twice via curl/HTTP POST, and confirm `auth.log` shows replay=0 then replay=1 with a consistent variance value.