This has two independent deliverables — tackle them separately. For Ticket 1, install a CLI speech-to-text tool (e.g. `whisper.cpp` or `openai-whisper` via pip, or `ffmpeg` plus a local model) and transcribe `/app/voicemail.wav`; listen for how the code is spoken (letters/digits may be spelled phonetically, e.g. NATO-style or spoken digit-by-digit) and reconstruct the literal 4-character alphanumeric code from what you hear rather than guessing — write it uppercase, with no extra whitespace, to `/home/user/voicemail_code.txt`. For Ticket 2, write `/home/user/validate_filename.sh` that takes `$1`, and use a strict anchored regex match (e.g. `[[ "$1" =~ ^[A-Za-z0-9._-]+$ ]]`) to decide exit code 0/1 — always quote `"$1"` and use `[[ ... ]]` rather than `[ ... ]` or unquoted expansion, since adversarial inputs like `-e`, `$(...)`, embedded newlines, or glob characters can otherwise be misinterpreted as flags or trigger command/glob expansion. Test explicitly against inputs starting with `-`, containing `$()`, spaces, and embedded newlines before considering it done. `chmod +x` the script.
