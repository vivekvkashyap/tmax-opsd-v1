Do the pieces in dependency order since later steps need the ports from step 1. First, extract frames from the video (e.g. `ffmpeg -i /app/k8s_ops.mp4 frame_%03d.png`) and run a QR decoder such as `zbarimg` (install if missing) against the frames to recover the JSON payload containing `git_port`, `http_port`, and `tunnel_port` — do not hardcode ports, parse them from whatever the decoder outputs. Second, `git init --bare /home/user/manifests.git`, then write `hooks/post-receive` as a bash script that does something like `git --work-tree=/home/user/manifests_served --git-dir=/home/user/manifests.git checkout -f main` inside a `while read` loop over the ref updates, and `chmod +x` it. Third, write `supervisor.sh` that launches `git daemon --base-path=... --export-all --enable=receive-pack --port=<git_port> ...` and `python3 -m http.server <http_port> --directory /home/user/manifests_served`, then loop checking each PID (e.g. via `kill -0`) and relaunching on death, running the whole supervisor with `&`/`nohup` in the background. Fourth, set up passwordless localhost SSH (generate a keypair, add to authorized_keys) and run `ssh -f -N -L <tunnel_port>:localhost:<http_port> localhost`; separately add a distinct `Host k8s-cluster.local` block in `~/.ssh/config` with `PubkeyAuthentication no` to force password auth for that unrelated mock host. Finally write the logrotate.conf with `daily`, `rotate 3`, `compress` directives for the log path.