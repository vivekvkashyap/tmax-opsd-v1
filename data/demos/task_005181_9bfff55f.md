Use `git bisect start`, mark the current `HEAD` bad and the "Initial commit" good, then drive it with `git bisect run` against a script that invokes `runner.sh` under a timeout (e.g. `timeout 2 ./runner.sh`) and exits nonzero on hang/failure — this is far more reliable than eyeballing commit messages, especially since the bug is an infinite-recursion hang rather than a compile error. Capture the identified commit's full SHA with `git rev-parse` and write it to `/home/user/bad_commit.txt`, then `git bisect reset`. Diff that bad commit against its parent to see exactly what changed in the recursive `process_jobs` function in `runner.sh` — infinite recursion from a missing base-case update is a classic pattern (e.g. a `shift` or counter decrement got dropped so the argument list never shrinks). On the current HEAD, restore the missing step so each recursive call reduces the remaining task list until it hits zero, while keeping the function recursive rather than converting it to a loop. Make `runner.sh` executable and run it with no arguments to confirm it prints each of the three default tasks in order and exits promptly.
