Run `go build` first to see the actual compiler error message rather than guessing — it's a type mismatch where an int is being assigned to a string variable (or vice versa); fix by using the correct type or a proper conversion (e.g. `strconv.Itoa`) so the value going into the format string matches its verb. For the `ChunkData` panic, trace through what happens on the final, possibly-short chunk: the bug is that the end-of-slice bound is computed incorrectly (off by one relative to the true length), so instead of clamping to `len(data)` it goes one element past, causing "slice bounds out of range". Fix the bound to clamp exactly at `len(data)` when `i+chunkSize` would overflow. Add `main_test.go` with `func TestChunkData(t *testing.T)` calling `ChunkData([]string{"A","B","C","D"}, 3)` and asserting on the resulting shape (two chunks, first of length 3, second of length 1) without triggering a panic. Validate with `go test ./...`, then build the final binary explicitly to the required path with `go build -o /home/user/logchunker/chunker`.
