Treat this as a debugging task: run calculator.py first to see the actual traceback rather than guessing at bugs. The crash comes from unconditionally casting the status field to int — special-case the literal string `TIMEOUT` (and route it through the same branch as a 5xx) before attempting the int conversion. Fix the error classification so only status codes in the 5xx range (or TIMEOUT) increment the error counter — 4xx codes must be parsed/counted toward total_requests but excluded from error_requests. Fix the burn-rate formula to `error_rate / allowed_error_budget` where the allowed budget is the SLO's error tolerance (0.1% i.e. 0.001), not its inverse — re-derive the constant from the stated 99.9% SLO rather than reusing whatever divisor is already in the code. Keep the output JSON's key names and structure exactly as originally written; only correct the values feeding them. After fixing, actually execute the script from /home/user/uptime_monitor/ so final_metrics.json is regenerated, and sanity-check total_requests equals the log's line count.