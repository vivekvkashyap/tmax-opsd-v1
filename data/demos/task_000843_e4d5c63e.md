This is an OCR-then-compute task: don't hardcode guessed edge weights — actually extract the text from the image first, then verify what you read before hardcoding it into the C program. Run `tesseract /app/schema_graph.png stdout` (or convert to a higher-res/thresholded image first with ImageMagick if OCR output looks noisy) to recover the `NodeA NodeB Cost` lines; sanity-check the recovered text looks like a well-formed small edge list (few nodes, small integer weights) before trusting it, and re-run OCR with different `--psm` page-segmentation modes if the first pass garbles characters. Once you have the confirmed edge list, write `analyze_graph.c` implementing all-pairs shortest paths on the (small, undirected) weighted graph — Floyd-Warshall is simplest for a handful of nodes — treating each `NodeA NodeB Cost` line as a bidirectional edge with that weight. Then compute closeness centrality per node as (N-1) / (sum of shortest-path distances to all other nodes), format each line as `NodeName: 0.XXXX` rounded to 4 decimals, sort lines alphabetically by node name, and write to `/home/user/centralities.txt`. Compile and run the program so the file is actually produced; recheck your OCR'd weights against the formula's sensitivity since the verifier requires MSE <= 0.001, which leaves little room for a misread digit.