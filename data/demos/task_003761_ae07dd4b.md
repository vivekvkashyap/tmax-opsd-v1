Scaffold with `cargo new --lib mobile_sensor`, then edit `Cargo.toml`: set `[lib] crate-type = ["cdylib"]`, add `libc` as a dependency, and declare `[features] android = [] ios = []`. In `src/lib.rs`, implement `#[no_mangle] pub extern "C" fn process_sensor_data(data: *const f32, len: usize) -> i32` — check `data.is_null()` first and return -1 immediately, then build a slice via `std::slice::from_raw_parts` inside an `unsafe` block, sum it as f64/f32, and use `#[cfg(feature = "android")]`/`#[cfg(feature = "ios")]` (each as a separate compiled variant of the return logic, or an if/else guarded by `cfg!` macros) to multiply by 2.0 or 3.0 and truncate (`as i32`) before returning, defaulting to -1 when neither feature is set. For `verify.py`, load the `.so` via `ctypes.CDLL(sys.argv[1])`, set `argtypes = [ctypes.POINTER(ctypes.c_float), ctypes.c_size_t]` and `restype = ctypes.c_int32`, build the float array with `(ctypes.c_float * 3)(1.5, 2.5, 3.5)`, call the function, and print only the integer. In `run_all_tests.sh`, remember cargo overwrites the same `target/release/libmobile_sensor.so` each build, so `cp` it to a uniquely named file in `/home/user/libs/` immediately after each of the three `cargo build --release[--features ...]` invocations, before the next build overwrites it — then run `verify.py` against each and format the log exactly as specified.
