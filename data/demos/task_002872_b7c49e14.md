This is a classic denormal-float performance stall: a recursive IIR filter with a decaying feedback loop (`y_prev = x*(1-alpha) + y_prev*alpha`) that, once the input goes silent, decays y_prev exponentially toward zero. On x86, once the value gets small enough to fall into the subnormal/denormal range, hardware FPUs fall back to a microcoded slow path, making each multiply-add dramatically slower — this is what causes an otherwise fast loop to blow up in wall-clock time. Confirm the hypothesis by inspecting the input WAV (an impulse followed by silence, which forces a long denormal-decay tail) and by timing the unmodified binary. Fix it at the C level without changing the filter's audible behavior, using one of two standard techniques: (1) set the FTZ/DAZ MXCSR flags via `_MM_SET_FLUSH_ZERO_MODE`/`_MM_SET_DENORMALS_ZERO_MODE` from `<xmmintrin.h>`/`<pmmintrin.h>` at program start, or (2) add and immediately subtract a tiny constant (e.g. 1e-18f) inside the recursive update to keep the value out of the denormal range. Recompile with `gcc -O3` to `/home/user/dsp_processor_fixed`, then time it against the suspicious WAV to confirm it now runs well under the threshold and still produces valid, unchanged-sounding audio.
