Write the Go program to process the tarball in three clearly separated steps rather than one monolithic pass. First, open `/home/user/dataset_incoming/robotics_data.tar.gz` with `archive/tar` and `compress/gzip`, iterating tar entries; for each inner `.gcode.gz` entry, wrap its reader in a `gzip.NewReader` and check the returned error (and also drain/verify the stream, e.g. read fully and check for `io.ErrUnexpectedEOF`/CRC errors) — corrupted entries must be caught here and skipped, not crash the whole run. Second, for valid entries, scan the decompressed GCode line by line with `bufio.Scanner`, and for each line starting with `G1` that contains an `E<number>` token, parse the numeric value and track the running maximum. Use a proper tokenizer (split on whitespace, check prefix `E`) rather than a fragile regex that might match `E` inside other parameters. Third, write the raw decompressed GCode text (unchanged) to `/home/user/dataset_processed/<basename>_E<int(maxE)>.gcode`, truncating (not rounding) the max E value to an integer. Create the output directory with `os.MkdirAll` before writing, and only emit output for files that decompressed successfully.