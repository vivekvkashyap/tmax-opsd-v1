Inspect `/home/user/extract_graph.sql` first: the bug is an implicit cross join (multiple tables listed in `FROM` with the join condition missing or incomplete), which explodes edge counts. Rewrite it to select only from `edges` filtered by a bound `backup_id` parameter (use `?` placeholders bound via your language's DB driver, e.g. sqlite3 parameterized queries — never string-format the id in). Add `CREATE INDEX ... ON nodes(backup_id)` and `CREATE INDEX ... ON edges(backup_id)` to `/home/user/backup.db` so lookups are efficient. For the service, use a lightweight framework (FastAPI/Flask/Express) bound to `127.0.0.1:8080` with a `POST /validate` route: read `backup_id` from the JSON body, run your fixed parameterized query to pull nodes/edges, build an undirected graph (e.g. `networkx.Graph()`), find the max-degree node breaking ties by sorting candidates on `(-degree, id)` to get the lexicographically smallest id among ties, compute `nx.average_clustering(graph)` rounded to 4 decimals, and return exactly the specified JSON shape. Start the server backgrounded and curl it yourself to confirm the response shape before finishing.
