First recover the bytecode spec from the image: run OCR (e.g. `tesseract /app/spec.png stdout`) and read the extracted text carefully to get the exact hex opcode values for `OP_PUSH`, `OP_ADD`, `OP_SUB`, `OP_MUL`, `OP_POP`, and the `CONST_MOD` value — if OCR output is noisy, try preprocessing (upscaling, thresholding with `convert`/`imagemagick`) or a different tesseract page-segmentation mode until the digits/hex are legible, and double check against typical hex-format conventions rather than guessing. Then implement `/home/user/math_vm.c`: read stdin fully as raw bytes, walk the byte stream, and for `OP_PUSH` consume exactly the next 4 bytes as a little-endian signed 32-bit int (careful with byte order and sign extension). For each arithmetic op pop two, combine in the order `second_from_top OP top`, reduce modulo the constant ensuring the result is non-negative (e.g. `((a % m) + m) % m` in a way that also applies after the arithmetic op, not just to inputs). Handle stack underflow by silently ignoring the operation. Since your output must match `/app/oracle_vm` byte-for-byte across many random fuzz inputs, write your own quick differential test harness (generate random byte strings, run both binaries, diff outputs) rather than relying on a handful of manual cases.