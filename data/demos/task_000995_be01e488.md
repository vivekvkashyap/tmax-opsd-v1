Start by writing a small C struct matching the header exactly: 4-byte magic "CBND" (compare with memcmp, not strcmp, since it has no terminator), then a little-endian uint32 entry count — read raw bytes and assemble with bit shifts or use fixed-width types with `fread` into a packed struct (watch for compiler struct padding; read fields individually or use `#pragma pack(1)`). For each of N index entries (64-byte filename + three little-endian uint32 fields: comp_size, uncomp_size, absolute offset), `fseek` to the given offset, `fread` comp_size compressed bytes, then decompress with zlib's `uncompress()` (link with `-lz`), providing an output buffer sized to uncomp_size. Write the decompressed bytes to `<output_dir>/<filename>` (filename may need null-termination handling since it's padded, not necessarily null-terminated if it fills all 64 bytes). After extraction, recurse through the repo directory to find every `.cbnd` file (e.g. `find /home/user/artifact_repo -name '*.cbnd'`) and run your compiled extractor on each. Finally compute SHA-256 of each extracted file (e.g. `sha256sum`), format as `<hash>  <basename>`, and sort lines alphabetically by filename before writing the manifest.