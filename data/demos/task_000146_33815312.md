OCR the image first: `tesseract /app/alert_config.png stdout` (install tesseract-ocr if not present), then parse out the LISTEN_PORT and AUTH_TOKEN values from the recognized text rather than guessing them. Build the server with Python's `http.server`/`BaseHTTPRequestHandler` (or a lightweight framework), binding only to `127.0.0.1` on the OCR'd port. In `do_GET`, check the path is exactly `/alert`; parse the `Authorization` header and require it to equal `Bearer <token>` exactly, returning 401 with `send_response(401)` for anything missing/incorrect. On success: use `os.makedirs(path, exist_ok=True)` for idempotency, then explicitly `os.chmod(path, 0o700)` afterward (makedirs' mode argument is affected by umask, so an explicit chmod call is the reliable way to guarantee exactly 700), append `"Alert received\n"` to alerts.txt, and respond 200 with body `Alert Logged`. Start the server in the background with `python3 /home/user/webhook.py &` and verify with `curl` that it's actually listening on the given port before finishing, testing both an authorized and unauthorized request.