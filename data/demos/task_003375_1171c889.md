Locate the three specific defects in the vendored archiver by reading its source before editing: find where directory listing happens (likely a shallow `os.listdir` that never recurses into subdirectories or skips entries where `os.path.isdir` is true) and replace it with a recursive walk (e.g. `os.walk`) so nested files are included; find the read/chunking loop and check its buffer size constant — if it reads one byte at a time that's the performance bug, so bump it to a sensible size like 1MB and make sure the chunking logic still respects the requested `--chunk-size` argument correctly; and find the file-open/read section where no locking is applied, adding `fcntl.flock(fd, fcntl.LOCK_SH)` before reading and `fcntl.flock(fd, fcntl.LOCK_UN)` (or rely on the `with` block closing the file) afterward, to avoid capturing partially-written files while other processes append to them concurrently. After each fix, spot check with a small nested directory and a large file to confirm both correctness and speed improved noticeably, then run the archiver end-to-end against the real source/dest paths with the specified chunk size.
