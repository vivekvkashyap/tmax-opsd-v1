Build this as a straightforward recursive algorithm, not a numeric approximation. Parse the FASTA with a simple line reader (not biopython unless available) into 5 equal-length strings. Precompute a boolean "conserved" array of length 1000 by comparing all 5 characters at each column. Implement refinement as a recursive function on (start, end): compute ratio = sum(conserved[start:end])/(end-start); if ratio >= 0.70 and (end-start) > 25, recurse on the two halves; otherwise stop and record this as a final domain. Track domains that stopped specifically because they hit exactly 25 bp (not because ratio dropped below threshold) — only those are eligible "fully refined" domains for primer selection. Among fully refined 25bp domains with ratio >= 0.90, sort by start index to pick the lowest (forward) and highest (reverse) start. Extract the substring from Seq1 for each; reverse-complement the reverse primer using a simple A/T, C/G mapping and string reversal. Write num_final_domains as the count of ALL leaves in the final mesh (not just eligible ones). For the plot, matplotlib is sufficient — plot mesh domain conservation ratios as a step/bar plot, draw vertical lines at domain boundaries, and shade the two chosen primer regions. Double check the rounding: 200→100→50→25 bp halving is exact, so no off-by-one boundary issues should occur if you always split as [start, mid) and [mid, end).
