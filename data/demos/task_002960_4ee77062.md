Start by OCR'ing `/app/schema_rules.png` with `tesseract` to recover the exact `TABLE_NAME` and `AUTH_TOKEN` strings — do not guess these values, they must be read from the image. Then build the Rust pipeline: read `/app/dirty_data.jsonl` line by line, first do a raw string replace/regex fix converting the literal double-escaped sequence `\\uXXXX` into the real unicode escape `\uXXXX` before JSON-parsing each line (parsing the raw double-escaped text directly will leave a literal backslash-u in the string instead of the actual character). Deduplicate by `user_id`, keeping the record with the highest `timestamp` per id — do this with a HashMap<user_id, record> that you overwrite only when timestamp is strictly greater. Join with `/app/reference.csv` (csv crate) on `user_id`, bulk-insert into a new SQLite db at `/home/user/cleaned.db` using the OCR'd table name (rusqlite crate). Finally, build an HTTP server (e.g., using a lightweight framework or std TcpListener) bound to `127.0.0.1:8080` exposing `GET /user/<user_id>`, requiring header `Authorization: Bearer <extracted-token>` — return 401 if missing/wrong, otherwise query SQLite and return the joined JSON record. Keep the server running as a background/long-lived process so it can be verified live.