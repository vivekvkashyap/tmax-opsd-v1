Write a Go program that lists `manifest_0.json`..`manifest_999.json` in `/home/user/artifacts/`, spawns a goroutine per file (or a worker pool reading from a jobs channel) that decodes the JSON, computes `sum((size_bytes * (index+1)) % 9973)` over the `dependencies` array for that file using its own 0-based index within that file, and sends the per-file partial sum over a results channel to a single collector goroutine that accumulates `total_metric`; use a `sync.WaitGroup` (or a done-count) to know when to close the results channel safely, and avoid a shared mutable counter written from multiple goroutines without synchronization (that's the classic race an agent introduces here — either channel-based reduction or a mutex around the accumulator). Do not compute the modulo before multiplying by the wrong index base (index+1, not index). After computing, marshal `{"total_metric": <int>}` exactly and write it to `/home/user/build_report.json`, then actually build and run the program (`go run` or `go build && ./binary`) rather than hand-computing the number.