Treat `/app/log_analyzer` as a black-box oracle to reverse-engineer, not something to read the source of. Start by feeding it small, hand-crafted inputs via stdin (simple 3-column TSV rows, rows with wrong column counts, rows with `<<...>>` blocks containing embedded newlines, unicode metric keys with punctuation, malformed `key:value` pairs) and diff your own script's output against the binary's output for each case to pin down exact edge-case behavior — especially how it groups lines inside `<<`/`>>` even across `\n`, which SessionIDs it drops, and how it strips/normalizes metric keys.

Implement your `/home/user/solution` in Python for easy access to `re`, `unicodedata.normalize('NFKC', ...)`, and `json.dumps(..., separators=(',', ':'), sort_keys=True)`. Build the row tokenizer by scanning the raw input character-by-character, tracking an "inside a `<<...>>` block" flag so that `\n` only ends a row when not inside such a block; only split on `\t` and require exactly 3 columns, validate the SessionID regex, then split Metrics on `,` and each pair on the first `:`, parsing ints (skipping unparseable pairs) and normalizing keys (NFKC, lowercase, strip non-alphanumeric via regex) before summing. Make the file executable with a `#!/usr/bin/env python3` shebang, then fuzz it yourself against the oracle with randomized inputs (varying malformed rows, unicode, embedded newlines) before finishing, since a single-instance test easily misses corner cases the real fuzzer will hit.