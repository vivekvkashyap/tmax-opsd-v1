Parse `/home/user/target.fasta` by skipping the header line(s) starting with `>` and concatenating the remaining sequence lines, then compute GC fraction as (count of 'G'+'C') / total base count and derive r = 2.0 * gc_fraction — don't hardcode either value, compute them from the file. For the analytical curve, directly plug t=0..10 into the closed-form logistic formula given in the prompt. For the numerical curve, do NOT repeat the researcher's mistake of a large fixed Euler step — use `scipy.integrate.solve_ivp` (adaptive, e.g. RK45 with tight `rtol`/`atol`) or a manual RK4/Euler with dt ≤ 0.01, and request/interpolate values exactly at integer t via `t_eval=[0,...,10]` so the two series line up point-for-point. Verify numerically that `|y_num - y_exact|/y_exact < 0.01` at every t before writing outputs. Save `params.json` with exactly the two keys `gc_fraction` and `r`, and `results.csv` with header `t,y_num,y_exact`, one row per integer time point.
