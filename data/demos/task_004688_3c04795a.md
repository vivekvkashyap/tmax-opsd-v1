Reverse-engineer this by systematic black-box experimentation, not guessing. Start with `strings /app/legacy_web_scanner` and `strace -f -e trace=file,openat,readlink /app/legacy_web_scanner <testdir>` to see exactly which paths and symlinks it inspects and in what order. Build small synthetic directory trees yourself (using `mkdir`, `ln -s`) covering every combination: a user dir with only `public_html`, only a valid `tls/cert.pem` symlink, both, neither, `tls/cert.pem` as a broken symlink, `tls/cert.pem` as a real file (not a symlink), and mixed alphabetical ordering of usernames — run the legacy binary on each and record exact stdout byte-for-byte (watch for trailing newline, separator spacing, and sort order). Only once you've confirmed the precise output line format and the exact conditions that gate inclusion should you write the Rust program: walk depth-1 subdirectories, check `public_html` is a directory and `tls/cert.pem` is a symlink (use `symlink_metadata`/`read_link`, not something that follows and resolves it), print matching lines sorted by directory name, then compile with `cargo build --release` or `rustc` to `/home/user/new_web_scanner` and diff its output against the legacy binary on your test trees before finishing, since grading fuzzes many fresh directory structures for exact stdout equivalence.
