Implement this as clearly separated functions in C so each step is checkable: a CSV loader into a `double data[500][20]` array, a column-mean pass, an in-place mean-centering pass, a covariance-matrix builder (`C[i][j] = sum_n Xc[n][i]*Xc[n][j] / (N-1)`, computed with nested loops in a fixed, consistent order since floating-point summation order affects the last few digits), and a power-iteration loop. For power iteration, initialize `v` to all `1.0`, and each of the exact 100 iterations must (1) compute `w = C*v`, (2) compute the L2 norm of `w`, (3) divide every element of `w` by that norm to get the new `v` — don't normalize by anything else (e.g. max-abs) or the result won't match. After the loop, flip the sign of the whole vector if `v[0] < 0`. Use `double` throughout (not `float`) to keep precision consistent with a numpy reference, print with `%.6f\n` per line, and compile with `-lm` since you'll need `sqrt`. Test incrementally: print the covariance matrix and eigenvector norm after a few iterations to confirm convergence before trusting the final output.
