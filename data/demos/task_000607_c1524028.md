First detect each file's encoding robustly (try `chardet`/`charset_normalizer` in Python, or fall back to trying UTF-8, then UTF-16LE, then ISO-8859-1 in order and taking the first that decodes without error) — don't assume all files share one encoding. Normalize per the exact rules given (strip comment lines starting with `#`, drop blank lines, lowercase, strip whitespace, join with `\n`), and log a `[READ]` line to `/home/user/pipeline.log` per file as you go. For clustering, compute pairwise Levenshtein distance on the normalized strings (use a standard O(n*m) DP implementation or a library like `python-Levenshtein`/`rapidfuzz`), build a graph where an edge exists if distance <= 15, then find connected components (union-find or BFS/DFS) — this is the key step naive attempts get wrong by only checking exact matches or a single pivot file instead of transitive similarity. Within each cluster pick the representative by largest *original* file size in bytes (not normalized size), tie-broken alphabetically. Write the final JSON sorted by representative filename with members sorted alphabetically, and append the `[CLUSTER] Found <N> clusters` line last.