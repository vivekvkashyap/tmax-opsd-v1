Build this as a small user-space daemon, not a one-shot check. Create the directory tree and symlink exactly as specified (verify with `readlink` after creating it — a common mistake is linking to the wrong target or leaving a trailing/missing slash). When measuring directory size, always resolve through the symlink path (the rogue writer uses the symlink path too) and sum `os.path.getsize` over actual files, polling on a fixed interval rather than using a one-time check. To find the rogue PID reliably, use `pgrep -f rogue_service.py` (or scan `/proc` for the matching cmdline) rather than assuming a fixed PID. Kill it with `os.kill(pid, signal.SIGTERM)`, then write the alert line to `/home/user/alerts.log` matching the required format exactly (including punctuation), and exit the monitor process itself with code 0 right after — leaving it running after the kill will fail verification. Launch both scripts with `nohup ... &` or `subprocess.Popen`, and finally re-check that no `rogue_service.py` or `storage_monitor.py` process is still alive before declaring done.
