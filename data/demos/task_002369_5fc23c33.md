First, open `/app/legacy_spec.png` and read the 4-byte magic header value stated in the image text — you need its exact byte sequence to scan for. Then write the parser as a streaming byte scanner, not a naive split/regex: read all of stdin into a buffer, and iterate an index `i` looking for the magic header bytes. On each match, try to parse a chunk starting right after it: read 4 bytes little-endian length N, check enough bytes remain for payload+4-byte CRC (else invalid), zlib-decompress the payload (catch exceptions), and compare zlib.crc32 of the decompressed bytes against the stored little-endian CRC32. Only on full success do you emit output; split the decoded text on the first newline to get `Title: <Name>` vs the rest, and print `=== <Name>.txt ===\n<rest>\n---DOC---\n`. The critical correctness trap: on ANY failure at any stage, do not skip past the whole attempted chunk — resume scanning for the next magic header starting exactly one byte after where the failed magic header began (i.e. advance the search index by 1, not past the length/payload/crc region), so overlapping/false headers embedded in garbage are still found. Test against self-crafted valid+corrupted streams before finishing.
