Handle each log source's encoding conversion first, before any parsing, since garbled bytes will break your field extraction downstream: convert `syslog_eu.log` from ISO-8859-1 to UTF-8 with `iconv -f ISO-8859-1 -t UTF-8`, and `app_asia.log` from UTF-16LE with `iconv -f UTF-16LE -t UTF-8`. For the EU log, grep lines containing `ERROR:`, parse the bracketed `[DD/MMM/YYYY:HH:MM:SS +TZ]` timestamp with `date -d "..." +%s` (GNU date understands this format directly, or reformat it first) preserving the stated UTC offset so the epoch is correct, and capture the message text after `ERROR: `. For the Asia log, grep lines containing `- FAIL -`, parse `YYYY-MM-DD HH:MM:SS TZ` where TZ is an abbreviation like `JST` — convert that named zone to a UTC offset (e.g. a lookup table, since `date` doesn't always resolve arbitrary abbreviations reliably) before computing epoch seconds, and capture the message after the final ` - `. Append all `EPOCH|Message` lines to `/home/user/extracted_errors.log`, then `sort -t'|' -k1,1n -o` it in place. Make the script executable, run it once to produce the file, and install the cron job via `crontab -u user -l` piped with the new `*/5 * * * * /home/user/parser.sh` line through `crontab -u user -`. Double-check epoch values by manually computing one example rather than trusting a first `date` invocation.
