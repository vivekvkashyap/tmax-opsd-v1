Break this into independently testable pieces before wiring them together. First, the archiver: write a C function that recursively walks `/home/user/legacy_data/` (nftw or opendir/readdir with recursion), for each `.csv` file mmap()s it, parses `timestamp,sensor_id,value` lines, and appends JSON objects into one array; serialize with cJSON or jansson, write to `/home/user/www/historical.json`, then shell out to `tar czf` (or libtar/zlib) to produce `/home/user/www/historical.tar.gz` — verify these two files independently with `tar tzf` and a JSON validator before moving on. Second, the daemon: a TCP server on port 9000 that per-connection first requires the literal line `AUTH sensor_token_99X` before accepting further input, then parses newline-delimited JSON payloads (cJSON), maintains a running (count, sum) per sensor_id in memory to compute the average, writes averages to `/home/user/www/live_averages.json` after each update, and pushes the same values into a Redis hash `sensor_averages` via hiredis (`HSET sensor_averages <id> <avg>`). Test the daemon alone with `nc localhost 9000` before adding Nginx. Third, write `/home/user/nginx.conf` proxying/serving `/home/user/www/` on `127.0.0.1:8080`, start Nginx with `-c` pointing at it, and confirm both `historical.tar.gz` and `live_averages.json` are fetchable via curl. Compile the daemon to `/home/user/sensor_daemon` and background it (`&` or nohup) only after each piece works standalone.