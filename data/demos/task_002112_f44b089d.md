Extract the spec text with `pytesseract.image_to_string` on provision_specs.png and parse the Username/StartPort/RotateSize values out of the OCR'd lines yourself — don't hardcode any guessed values. Generate create_users.sh, service_logrotate.conf, and firewall.sh as pure string templates driven by those extracted values, looping IDs 0-9 for usernames and ports (StartPort+ID and 8000+ID). To automate the interactive installer, use subprocess.Popen with pipes: read/wait for each prompt's text on stdout, then write the corresponding answer plus newline to stdin (pexpect works too, but plain pipe I/O avoids the extra dependency). The critical requirement is concurrency: the installer sleeps ~2s per invocation, so running 10 of them sequentially blows the 4-second budget — launch them concurrently with a thread pool, multiprocessing pool, or asyncio subprocess tasks so their sleeps overlap. Create /home/user/logs before launching any installer, and make provision.py executable.
