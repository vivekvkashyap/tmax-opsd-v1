Treat this as a concurrency + shell-quoting debugging task, not a rewrite. First reproduce the bug by running the script several times and diffing /home/user/result.txt outputs — you should see it vary run to run, confirming a race condition, and notice one filename in /home/user/pcaps/ contains a space (list the directory with `ls -la` to see it). Read the script carefully: the `subprocess.check_output(cmd, shell=True, ...)` builds the tcpdump command with an f-string embedding the raw filepath — any filename with a space breaks shell tokenization and silently fails (caught by the bare `except: pass`), which explains files being ignored. Fix this by quoting the path in the shell string (e.g. wrap `filepath` in double quotes) or switching to `shell=False` with an argument list so no shell parsing occurs. Separately, the `total_bytes` global is updated by multiple threads with a read-sleep-write pattern with no synchronization — fix by adding a `threading.Lock()` and wrapping the read-modify-write of `total_bytes` in `with lock:` inside each thread. After both fixes, run the script multiple times to confirm the result is now stable and correct, then check /home/user/result.txt.