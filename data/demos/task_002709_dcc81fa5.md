In C, compute the SHA-256 of known_good.bin at runtime using OpenSSL's EVP digest API (EVP_DigestInit/Update/Final or the legacy SHA256() call), then convert the raw digest bytes to a lowercase hex string for comparison — don't hardcode the hash. Parse sec_events.log line by line, splitting each on the `|` delimiter into exactly four fields (use strtok or manual index scanning, being careful with trailing newlines). Load revoked_certs.txt into a list/set for fast fingerprint lookup. For each log line, apply all three criteria as a logical AND: fingerprint present in the revoked set (exact string match), CSP field does NOT contain the substring `default-src 'self'` (a strstr check), and the logged file hash differs from your freshly computed hash of known_good.bin (case-sensitive string compare, or normalize case if needed). Write only IPs meeting all three, one per line, to the output file — order doesn't matter but each condition must be checked independently, since a common bug is inverting the CSP or hash condition. Compile with -lcrypto and run before checking output.
