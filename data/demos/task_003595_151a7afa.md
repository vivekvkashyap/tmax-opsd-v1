Parse baseline.sha256 line by line into (hash, path) pairs, recompute each file's SHA-256 with hashlib, and compare — the single file whose computed hash differs from baseline is the compromised_file. Read that file's contents and search it with a regex that captures URLs following common XSS injection patterns (`fetch("...")`, `XMLHttpRequest`, `<script src="...">`) — extract just the URL up to (but not including) any query string, matching the example format shown in the description. Walk the webroot directory with os.walk and check each file's permission bits via `stat.S_IMODE(os.stat(path).st_mode)`, looking for the one file whose mode is exactly 0o777. Parse csp.txt for the `default-src` and `script-src` directive values (the allowed origins), extract the domain of the malicious URL, and set csp_bypassed to True only if that domain is not among the allowed origins in either directive. Assemble the four fields into the JSON structure exactly as specified and write it out.
