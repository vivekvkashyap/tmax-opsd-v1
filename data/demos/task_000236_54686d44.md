Work through the incident in dependency order. First extract frames from /app/incident_screen_record.mp4 (`ffmpeg -i ... frame_%d.png`) and inspect them for a QR code; decode it with a QR reader (e.g. `zbarimg` or a Python `pyzbar`/`opencv` script) rather than trying to read it visually, since the encoded passphrase must be exact. Use that recovered passphrase to decrypt vault.enc with `openssl enc -d -aes-256-cbc -pbkdf2 -in vault.enc -out vault.zip -pass pass:<recovered-passphrase>`, then unzip to /home/user/vault/. Generate the new password with a proper CSPRNG source (e.g. `openssl rand -base64` filtered to alphanumeric, or `/dev/urandom` via `tr`) at exactly 32 characters, write it to new_key.txt, and lock permissions with `chmod 400 new_key.txt`. Re-encrypt the vault contents with the new password using the same AES-256-CBC + pbkdf2 flags. For the payload, use `objdump`/`readelf`/`strings` to locate the `.rodata` section and find the 12-byte signature (4-byte magic `DE AD BE EF` followed by an 8-byte ID) — don't hardcode a signature you haven't actually verified from the binary. For the IDS scanner, since it must run on ~500k lines in well under a second, avoid per-line regex; strip spaces and do direct substring/byte search (e.g. precompute the hex signature once, and use fast string `in` checks or `bytes.find`) reading the file in bulk rather than line-by-line Python loops with regex compilation overhead, and write matching lines exactly as they appeared in the input to the output file.