Build and verify each service layer independently before wiring them together. In `api.py`, implement `GET /stats`: connect to Redis (`redis.Redis(host='127.0.0.1', port=6379)`), `LRANGE raw_telemetry 0 -1`, `json.loads` each entry, split scores by `group == "A"`/`"B"`, compute means, run `scipy.stats.ttest_ind(a, b, equal_var=False)` for Welch's t-test to get `p_value`, and compute the CI using sample variances with `ddof=1` (`numpy.var(x, ddof=1)`) per the exact formula given — don't accidentally use population variance (`ddof=0`), which will fail the verifier's independent recomputation. Enforce the bearer-token check as a FastAPI dependency that reads the `Authorization` header, compares against `Bearer secret-ml-token-99` exactly, and raises `HTTPException(401)` on mismatch/missing — test this with `curl` directly against port 8000 first. For nginx, add a `location /api/ { proxy_pass http://127.0.0.1:8000/stats; proxy_set_header Authorization $http_authorization; }` inside the port-8080 server block — the default proxy_pass does NOT forward the Authorization header automatically, so this explicit `proxy_set_header` line is required or the backend will always see missing auth. Run `populate_redis.py`, then `start_services.sh`, then confirm end-to-end with `curl -H "Authorization: Bearer secret-ml-token-99" http://127.0.0.1:8080/api/stats` before leaving services running in the background.