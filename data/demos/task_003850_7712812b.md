Since the reference is a black-box binary, characterize its exact join/missing-value semantics empirically before writing any C: run /app/data_oracle against several small hand-crafted CSV pairs — one with matching ids only, one with ids present in only one file, one with duplicate ids — and compare outputs to what pandas-style outer-join-then-pairwise-deletion would produce (i.e., only rows where the id exists in BOTH files with non-missing values contribute to the correlation; rows where one side is missing are excluded, not treated as zero). Once behavior is confirmed, implement server.c as a standard TCP server (socket/bind/listen/accept loop that keeps accepting after each client) on 127.0.0.1:9000: on each connection, read until you have two newline-separated paths, parse both CSVs (skip header, id + numeric value, watch for both int and float value formats), build a hash/array join by id keeping only ids present in both, then compute Pearson correlation with the standard formula (means, covariance, and both standard deviations over the joined pairs only). Format the result to exactly 4 decimal places with a trailing newline and write it back on the same socket, then close only that connection (not the listening socket). Test by piping the same CSV pairs you used against the oracle into your server with nc/printf and diff the outputs.