Build and test each of the three pieces independently before combining. For the Rust worker (`cargo new /home/user/user-mngr`), write straightforward logic: check `Path::new("/home/user/queue/request.txt").exists()`, if absent exit 0 immediately; if present, read it line by line, for each non-empty username create `/home/user/homes/<username>` (`fs::create_dir_all`) and write `readme.txt` with exactly `Welcome <username>` (no trailing newline mismatch — match the verifier's exact-string check), then delete the request file last so a crash mid-processing doesn't silently drop the request. For `run_service.sh`, keep it minimal: `while true; do /home/user/bin/user-mngr; sleep 1; done`, then `chmod +x`. For `deploy.sh`: `cd` into the project, run `cargo build --release`, check its exit status explicitly (`set -e` or manual `$?` check) and abort on failure; on success `mkdir -p /home/user/bin` and `cp target/release/user-mngr /home/user/bin/user-mngr`; find any running `run_service.sh` via `pgrep -f` and `kill` it (don't just skip this if none found); then launch the new supervisor detached (`nohup .../run_service.sh >/dev/null 2>&1 &` or `disown`) so `deploy.sh` can exit cleanly while it keeps running. Test end-to-end by running `deploy.sh`, dropping a sample `request.txt`, and confirming the home directories appear within a couple seconds.
