Build `pipeline.py` as three clearly separated stages and verify each against the raw CSV by eye before trusting downstream results. Cleaning: strip `$` from `price` and attempt a float conversion per row; any row that fails to convert (missing, non-numeric like "invalid") must be dropped entirely, not just have its price nulled — check your row count afterward matches what you'd expect from manually reading `products.csv`. Aggregation: group the cleaned data by `category`, average `price` per group, round to 2 decimals, and write `category_stats.csv` sorted alphabetically by category — recompute one category's average by hand to confirm your rounding matches. Recommendation: tokenize descriptions by lowercasing and splitting on whitespace only (explicitly do NOT strip punctuation, since trailing punctuation like "tools." is part of the token and affects set overlap), compute Jaccard similarity (intersection/union size) between P001's token set and every other cleaned product's token set, sort descending by similarity with ties broken by ascending product `id`, and take the top 3. Write `recommendations.txt` one id per line in similarity order. Finally, `run.sh` should invoke the Python script then pipe both output files through `sha256sum` in one command to `checksums.txt`, and be marked executable via `chmod +x`.