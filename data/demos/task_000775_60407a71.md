Start the services first (`/app/startup.sh`) so you can iterate against a live Redis and validation endpoint while fixing the C++ worker. Read `/home/user/cleaner.cpp` end to end before touching it: the "empty output" symptom points at the HTTP request to the validation service being malformed, so specifically inspect the cpp-httplib `Post(...)` call and check whether it's setting the `Content-Type: application/json` header (cpp-httplib silently defaults to something else if you pass a bare string body without specifying the content type param) — that's the root cause to fix first, then verify each item now round-trips as HTTP 200. Implement tokenization (lowercase, strip punctuation, split on whitespace) and Jaccard similarity (intersection/union of token sets) carefully, comparing each new item against ALL previously *accepted* items (not all seen items) and discarding on similarity >= 0.75 against any of them — order of processing from the Redis list matters for which duplicate is kept. Loop LPOP until Redis returns nil/empty. Build with the given g++ command, run it, and check `/home/user/cleaned_dataset.jsonl` for the exact `{"id":..,"tokens":[...]}` schema before considering it done.