Read the existing broken script first — its bug is `find -L` (follows symlinks) combined with an unguarded loop, causing infinite traversal through the symlink loop under `/home/user/monitored_data/`. Fix by dropping `-L` and using plain `find "$dir" -type f` (which by default does not descend into symlinked directories and treats symlinks as non-`-type f` unless they resolve — verify this excludes the loop), or explicitly add `-not -type l` for extra safety. For each real file found: get its size with `stat -c%s` (or `wc -c`), and branch — if <= 51200 bytes (50KB), `cp` it into the archive under its original basename; if larger, use `split -b 50k -d` or with `--additional-suffix`/custom suffix flags to produce `basename.part_aa`, `basename.part_ab`, etc. (match GNU split's default alphabetic suffix naming, verify with `split --help` that default suffix length/alphabet matches `aa`, `ab`...). After placing each file/chunk, compute `sha256sum` on the archived copy (not the original, since chunks differ) and append a `path | archived_name | checksum` line to a manifest array in memory. Finally sort all manifest lines by original path then chunk name (`sort` command or in-script sort) before writing `/home/user/archive/manifest.txt`, since verification checks the sorted result exactly.