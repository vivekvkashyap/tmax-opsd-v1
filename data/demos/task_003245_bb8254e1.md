This is black-box reverse engineering: probe first, model second, verify third. Write a script that calls `/app/oracle_scorer A B C D` across a structured grid/random sample of integer inputs in the -100..100 range (include cases that isolate each variable, e.g. vary only A while holding B,C,D fixed, to make the linear structure visible) and collect (A,B,C,D,Score,Category) into a table. Fit a linear regression of Score on A,B,C,D (e.g. `sklearn.linear_model.LinearRegression` or `numpy.linalg.lstsq`) — if the relationship is exactly linear the residuals will be ~0, confirming the formula; round the fitted coefficients and intercept to the nearest integers since these come from integer arithmetic, and re-verify predictions against a fresh batch of oracle calls to confirm bit-exactness. Separately study how Category depends on Score (or the raw inputs) by sorting rows by Score and looking for the exact threshold values where Category changes — don't assume it's a fixed split without checking boundary cases near the transition. Once confident, write `/home/user/solution.go` replicating the exact integer arithmetic and thresholds, compile to `/home/user/solution`, and re-fuzz it yourself against the oracle before considering the task done.