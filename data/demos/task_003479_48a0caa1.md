Process the archives in strict alphabetical filename order (sort the glob results), since later updates must overwrite earlier ones for the same key. For each archive, extract changes.wal (use tarfile in-memory or to a temp dir) and open it with `encoding='utf-16'` or `utf-16-le` explicitly — do not read it as UTF-8/ASCII or you'll get garbled/mis-split text. Parse the base system.conf into an ordered structure that preserves key order (e.g. a list of keys plus a dict, or Python 3.7+ dict which preserves insertion order) so later SET operations on existing keys update in place while new keys append at the end, and DELETE removes the key entirely if present (no-op if absent). Apply each WAL's lines in file order, across all three archives sequentially. After processing all three, write system.conf back as UTF-8 with the same KEY=value line format, then build final_backup.tar.gz containing just that file at the archive root (use `tar.add(path, arcname='system.conf')` so no leading directory components leak into the tarball).