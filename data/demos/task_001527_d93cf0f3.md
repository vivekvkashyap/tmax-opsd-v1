Start by compiling with verbose errors to see exactly what's broken: run `g++ -o /home/user/net_monitor /home/user/net_monitor.cpp` and read the compiler diagnostics rather than guessing. Expect two classes of problems — missing `#include` headers (the code uses `std::string`/iostream facilities without including `<iostream>`/`<cstdlib>`, and calls `getenv` without `<cstdlib>`) and a syntax error (a missing semicolon after a statement) — fix each based on what the compiler actually reports, then recompile until it succeeds and confirm with `./net_monitor` after exporting `TARGET_HOSTS`. For the environment variable, append (don't overwrite) a line to `/home/user/.bashrc` using the exact `export TARGET_HOSTS="127.0.0.1 1.1.1.1"` syntax so a grep for the variable and value succeeds. For log rotation, write a standard logrotate stanza targeting `/home/user/logs/net_monitor.log` inside braces, including the literal keywords `daily`, `rotate 3`, `compress`, and `missingok` on separate lines — logrotate config keywords are checked by substring, so keep them exact and unambiguous.
