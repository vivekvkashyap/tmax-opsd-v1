Parse both manifest JSONs (jq or Python's json module) and diff their `artifacts` lists by `name` to find the one entry present in v2 but absent from v1 — that's the suspicious binary. Rather than manually reading disassembly, the most exact way to recover the literal output string (including `\r\n` bytes) is to just run the binary directly and capture its raw stdout via command substitution or `subprocess.run(..., capture_output=True)` — this guarantees you get exactly what it prints, byte for byte, more reliably than eyeballing `strings`/`objdump` output. Write those captured bytes verbatim to the output file (careful: if you type `\r\n` as literal backslash-r-backslash-n characters instead of actual carriage-return/newline bytes, or vice versa, the comparison will fail — match what the binary actually emits). Then write `test_artifact.sh`: it should invoke the binary, compare its live output against the saved file's contents, and exit 0 on match / 1 otherwise. `chmod +x` it and actually run it to confirm it exits 0 before finishing.