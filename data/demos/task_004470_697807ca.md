Step 1: cd into /app/vendored/text_sanitizer-1.0.0, inspect setup.py and text_sanitizer/core.py directly to find the injected breakage — expect a straightforward syntax error (e.g. a missing colon on a function definition) in core.py and a setup.py whose `packages=[...]` list doesn't actually reference the real package directory name, causing pip to fail to discover it. Fix both minimally (correct the syntax, correct the package name string) and install with `pip install -e .`; verify with `python3 -c "from text_sanitizer.core import sanitize_text"`. Step 2: extract raw_docs.tar.gz, then for each nested archive test integrity before extracting (`unzip -t file.zip` or `tar -tzf file.tar.gz`) and skip any that fail rather than letting extraction errors abort your pipeline. Step 3: for each doc_N.json pull the `content` field with a JSON parser (jq or Python json), and for each doc_N.xml pull the text between `<Body>` tags (a real XML parser is safer than regex here, e.g. Python's xml.etree). Step 4: run each extracted string through sanitize_text, then do a literal string replace of "MacroHard" -> "NovaTech", sort all documents numerically by their N in the filename, and join them into final_documentation.md with exactly two blank lines between entries. Your output is graded by sequence-similarity against a hidden reference, so match formatting (trailing whitespace, blank-line count) precisely.