Read `logger.cpp` line by line and focus on any call where a function that interprets a format string (like `snprintf`/`printf`/`sprintf`) receives user-controlled input directly as its format argument instead of as a `"%s"`-style data argument — that pattern is a well-known CWE class (uncontrolled format string); identify the specific CWE ID by looking it up rather than guessing, and write only that ID to `cwe_report.txt`. Fix it by passing a fixed format string with a `%s` specifier and the user input as the corresponding argument, so no attacker-controlled data is ever interpreted as format directives. Compile with `g++`, run `./logger "USER_ACTION: Admin logged in"` exactly as specified, and confirm `raw_audit.log` contains that string unmodified. For encryption, compute `echo -n "COMPLIANCE_TOKEN_2024" | sha256sum` yourself to get the hex key (don't skip the `-n`, which matters for the hash), then use `openssl enc -aes-256-cbc -in raw_audit.log -out secure_audit.enc -K <hex_key> -iv <32-zero-hex> -nopad`-style invocation matching the stated 256-bit key and hardcoded all-zero IV; verify correctness by decrypting back and diffing against the original before finalizing. Finish with `chmod 400 secure_audit.enc` and `rm raw_audit.log`.
