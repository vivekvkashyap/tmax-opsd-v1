Chain the extraction with plain shell tools first, and confirm each stage's output before writing code: `tar -xzf /home/user/raw_docs.tar.gz -C /home/user/extracted/`, then `find /home/user/extracted -name '*.zip' -exec unzip -o {} -d /home/user/extracted_zips/ \;`. List the resulting `.txt` files and generate `doc_map.conf` with a loop over `find /home/user/extracted_zips -name '*.txt'`, emitting `INPUT_PATH=OUTPUT_PATH` lines where OUTPUT_PATH substitutes `/home/user/final_docs/<basename>`. For the C transformer: parse the conf file line by line (split on `=`), for each pair `open()`+`fstat()` the input to get its size, `mmap()` it read-only, scan for the literal `[DRAFT]` substring and overwrite the matched bytes in a copy of the buffer with `[FINAL]` (same length, so no resizing needed), write that buffer to `<OUTPUT_PATH>.tmp`, `fsync`+`close`, then call `rename()` to atomically replace the final path — this is the crux of the "atomic write" requirement, not `write()` directly to the target. Create `/home/user/final_docs/` before running the compiled binary, then diff the outputs against expectations.
