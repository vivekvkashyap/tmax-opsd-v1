First deduplicate `/home/user/system_metrics.log` while preserving first-occurrence order — use `awk '!seen[$0]++' /home/user/system_metrics.log > deduped.log` (do NOT use `sort -u`, which reorders lines) to keep only the first instance of each exact duplicate line in original order. Then inspect `/home/user/calculate_cooling.awk`: it currently divides `FAN_RPM` by `(CPU_TEMP - 20)` unconditionally, which explodes/divides-by-zero when `CPU_TEMP <= 20`. Edit the script to add a conditional: when column 2 (`CPU_TEMP`) is <= 20, print the cooling factor as `0` instead of doing the division; otherwise compute normally. Verify the awk script by hand-tracing a couple of edge-case rows before running it. Finally run the fixed awk script against the deduplicated log (not the original) and redirect the output to `/home/user/fixed_report.txt`, e.g. `awk -f calculate_cooling.awk deduped.log > /home/user/fixed_report.txt`. Double check formatting matches exactly what awk naturally prints (no extra decimals) since verification likely does an exact diff.
