Work this as a strict linear-algebra pipeline, not an approximate fit. Load the CSV with pandas, build the centered matrix X (subtract per-column means), and use `numpy.linalg.svd(X_centered, full_matrices=False)` to get U, S, Vt — the right singular vectors are rows of Vt, so v1=Vt[0], v2=Vt[1]. Project Z1 = X_centered @ v1, Z2 = X_centered @ v2, then apply the sign-flip rule exactly as specified (check sum(v1)<0, flip both v1 and Z1 together, same for v2/Z2) — getting this flip wrong silently flips the sign of every downstream residual. Fit the cubic with `numpy.polyfit(Z1, Z2, 3)`, which returns coefficients in [a,b,c,d] order matching the spec. Compute residuals per-row, then group by `id` and take the max with pandas groupby. For the KDE threshold, build `scipy.stats.gaussian_kde(ref_residuals, bw_method=0.2)` and solve `kde.integrate_box_1d(-inf, T) == 0.95` numerically with `scipy.optimize.brentq` over a reasonable bracket (e.g. min/max of the reference data extended a bit) rather than guessing T. Double check id/coefficient ordering and JSON types (floats, sorted int list) before writing output.
