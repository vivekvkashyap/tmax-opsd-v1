Work phase by phase and verify each with the actual check command before moving on. For TLS, generate cert+key in one step with `openssl req -x509 -newkey rsa:2048 -nodes -keyout server.key -out server.crt -days 365 -subj "/CN=staging.local"`, then `chmod 600` the key. For permissions, use `find /home/user/web_root -type d -exec chmod 755 {} +` and `find /home/user/web_root -type f -exec chmod 644 {} +` (separate find invocations, not a single chmod -R, since dirs and files need different modes). For the CSP tag, use `sed` to insert the exact meta line immediately after the literal `<head>` line in index.html (e.g. `sed -i '/<head>/a ...'`), matching the tag text exactly including the trailing space before `/>`. For redaction, write a `sed`/`awk`/regex in `redact.sh` that matches groups of 4-4-4-4 digits and rewrites the first three groups to `XXXX` while preserving the fourth exactly, then chmod +x and run it. For the audit summary, compute each line with the same tool the verifier will use (`md5sum`, `stat -c "%a"`, `grep -c`) so your output matches exactly, and end with the literal line `AUDIT_COMPLETE`.
