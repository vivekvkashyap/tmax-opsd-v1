Work the three sub-problems independently before running the script. First, extract the token from the capture: use `tshark -r /home/user/incident/traffic.pcap -Y http -T fields -e http.authorization` or fall back to `strings traffic.pcap | grep -i "Authorization: Bearer"` to pull the literal `Bearer <token>` value out of the raw HTTP payload — don't guess a token, read it out of the packet bytes. Write it to `/home/user/incident/.env` as `API_TOKEN=<token>` exactly. Second, do git forensics on `validate.sh`: run `git log -p validate.sh` (or `git log --oneline` then `git show <commit>`) to see the most recent diff, identify the specific line that changed an intermediate value used by an `if`/assertion check further down the script, and revert just that line so the computed value satisfies the assertion again (don't rewrite the whole script — find the minimal diff that broke it and undo it). Third, `chmod +x` if needed and run `bash /home/user/incident/validate.sh`, checking its exit code and confirming `/home/user/incident/success.log` is created with the token correctly interpolated; re-check the .env sourcing path if the script fails to find the variable.