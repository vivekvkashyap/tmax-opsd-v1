Write a proper RFC 4180-aware CSV parser in C++ that reads character by character (or field by field) tracking whether you're currently inside a quoted field, so an embedded newline within quotes doesn't get mistaken for a record boundary — a naive `std::getline` per-line loop will silently split records incorrectly here. For each fully-parsed row, filter to sensor_id == "TEMP_A", parse the ISO8601 timestamp (manual field extraction or sscanf on YYYY-MM-DDTHH:MM:SS), zero out the seconds field to align down to the minute, and apply the min-max normalization formula, clamping the result into [0.0, 1.0] with std::min/std::max in case any value falls outside the assumed theoretical range. Format the normalized value with std::fixed and setprecision(4). Compile exactly with `g++ -std=c++17 process_data.cpp -o process_data` and run the binary so processed_sensors.csv is actually generated, preserving the original row order in the output.
