Build and test this bottom-up. First get `/app/dist_calc` working standalone: pipe a hand-crafted three-line long-format string into it via stdin and confirm you understand its input/output contract before wiring it into the server — since it's stripped, treat it as a black box and only rely on the documented stdin format and single-float stdout. Write `/home/user/sensor_server.cpp` using POSIX sockets (`socket`/`bind`/`listen`/`accept`) bound to `127.0.0.1:7777`; for each connection, read a line, split on commas, and validate the CSV shape (exactly 4 fields) and that X/Y/Z each parse as floats in `[-100.0, 100.0]` — reject with `ERR: INVALID_DATA\n` on any parse failure or out-of-range value before touching the legacy binary. For valid input, reshape into the three-line long format and spawn `/app/dist_calc` with `fork`/`execve` (or `popen`), writing to its stdin and reading its stdout via pipes, then reap the child with `waitpid` (avoid zombies) and forward the parsed score back as `SCORE: <value>\n`. Compile, run it in the background, and leave it listening. Separately, write the cron line for "every 5 minutes" (`*/5 * * * * /home/user/batch_poll.sh`) to `/home/user/pipeline_cron`.
