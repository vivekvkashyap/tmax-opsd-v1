Fix correctness first, then performance, and validate each independently with `cargo test`/`cargo build --release` before moving on. In `decoder.rs`, replace the stub with real percent-decoding: scan the input, and whenever you hit `%` followed by two hex digits, parse them (`u8::from_str_radix(&s[i+1..i+3], 16)`) and push the resulting byte/char, otherwise pass characters through unchanged — run the existing `#[cfg(test)]` cases to confirm `%20`→space and `%2B`→`+` before moving on. In `graph.rs`, the recursion is exponential because `evaluate_node` re-visits shared ancestors on every path in a diamond DAG; fix it by threading a memo cache (e.g. change the signature to take `&mut HashMap<String, bool>`, or wrap it in `RefCell<HashMap<...>>` on the struct) and check/insert into the cache before recursing into dependencies, so each node is evaluated exactly once. After both fixes compile and `cargo test` passes, `cargo build --release`, copy the binary to `/home/user/auditor_bin`, and time it yourself against `/app/test_data/large_gateway.json` to confirm it's well under the 0.25s threshold before finishing.
