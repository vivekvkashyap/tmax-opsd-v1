Start by reading `setup.py` closely — it's broken in a subtle way (e.g. wrong Extension kwarg name or missing/incorrect `sources` list pointing at a nonexistent file), so fix it to properly declare `Extension('ceval', sources=['ceval.c'])`, then install with `pip install -e .` from `/home/user/pricing_engine` and confirm `python -c "import ceval"` works before moving on. For `engine.py`'s `get_and_eval`, note it does a semver comparison against "1.5.0" before ever calling `requests.get` — so your low-version test must assert the mock was NOT called (use `mock_get.assert_not_called()`), while the success test must patch `requests.get` so `.json()` returns a dict with an "expression" key, then assert the numeric result. Use `unittest.mock.patch("engine.requests.get")` (patch where it's looked up, i.e. in the `engine` module namespace, not `requests.get` directly) or it silently won't intercept the call. Write `run_tests.sh` with `pytest test_engine.py -v > test_results.log`, `chmod +x` it, and actually execute it — then open `test_results.log` and confirm it says "2 passed" before declaring success.