Start by reading `/home/user/health_aggregator.py` to find the arithmetic division inside the scoring function and identify every operand that feeds the divisor — the crash happens when those operands combine to make the divisor exactly zero. Cross-check this against `/home/user/logs/aggregator.log` for clues about which node/metric triggered it. To build the fuzzer, generate random JSON payloads matching the script's expected schema (a `nodes` array of `{id, metrics}` objects) with random numeric values for the metric fields, invoke the script as a subprocess on each candidate, and watch for a `ZeroDivisionError` in stderr/output; once caught, shrink the payload to the minimal fields needed and save it as `/home/user/bug_trigger.json`. For the fix, edit only the divisor-computing function: wrap the division so that when the computed divisor equals zero, the function returns 0 instead of raising — do not touch the function signature or other logic. Finally, re-run the script against `bug_trigger.json` to confirm it exits 0 and prints a score instead of crashing.
