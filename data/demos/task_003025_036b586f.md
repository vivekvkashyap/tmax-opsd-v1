Before touching code, characterize the bottleneck: open `/app/consumer.py` and see what WHERE clause it runs per popped user_id (likely something like `SELECT ... FROM user_logs WHERE user_id=? AND action='video_play'`), then run `sqlite3 /app/logs.db "EXPLAIN QUERY PLAN SELECT ... WHERE user_id=? AND action='video_play'"` to confirm it's doing a full table scan on the 500k-row table. Fix it with a composite index matching the query's filter columns in the order they're used: `sqlite3 /app/logs.db "CREATE INDEX idx_user_action ON user_logs(user_id, action);"` — putting `user_id` first lets SQLite seek directly, and including `action` in the same index lets it filter without a second lookup. Re-run `EXPLAIN QUERY PLAN` to confirm it now shows `SEARCH ... USING INDEX` instead of `SCAN`. Only touch `consumer.py`'s SQL if the query itself is structured badly (e.g. doing per-row Python aggregation instead of `SUM(duration)` in SQL) — otherwise the index alone should suffice. Finally run `python3 /app/consumer.py` end-to-end and confirm `/app/results.json` is produced with the right shape (user_id string keys, integer duration values) and that Redis's `job_queue` is fully drained.
