Treat `/app/provision_validator` as a black box first, since it is stripped and UPX-packed — plain `strings`/`objdump` on the packed image will show little useful text, so try `upx -d` to unpack it before static analysis (if UPX isn't available, black-box testing alone is enough). Run the binary against every file in both `configs/clean/` and `configs/evil/`, capturing the exit code and whether it segfaults (`echo $?`, check for signal 11/SIGSEGV) — a crash or unexpected accept on an "evil" file marks the vulnerable case. Then diff clean vs. evil files line-by-line to isolate exactly which fields/formats differ (e.g., how a quota value is written, and what a mount/path field contains) — don't guess, confirm each hypothesis by constructing a minimal test file and rerunning the binary against it to see if it still classifies as unsafe. Only once you've empirically nailed down the distinguishing rule(s) should you encode them in `/home/user/deploy_filter.py`, defaulting to REJECT on any parse error or ambiguity. For `apply_config.sh`, guard the config.ini append with a `grep -q` check before writing the `PreflightFilter=` line, and make the daemon start idempotent by checking `daemon.pid` for a live PID and killing/restarting it rather than spawning duplicates.
