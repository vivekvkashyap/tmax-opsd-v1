Start by OCR'ing `/app/lab_notes.png` (e.g. `tesseract /app/lab_notes.png -`) to recover the "Project Salt" string exactly as written — this is the value your signature check depends on, so verify it carefully before hardcoding it into the script. Build the verifier around Python's `zipfile` and `hashlib`: (1) try opening the zip and calling `testzip()`/read each member to catch corruption — REJECT on any exception; (2) read `manifest.txt`, parse `<filename> <sha256hex>` lines, and compare the resulting filename set against the zip's namelist minus `manifest.txt`/`signature.txt` — REJECT on any mismatch (extra or missing files) in either direction; (3) recompute SHA256 of each listed file's bytes and compare to the manifest's digest; (4) recompute `sha256(manifest_bytes + salt.encode())` and compare byte-for-byte against `signature.txt`'s contents (watch for trailing newlines when reading signature.txt — strip them before comparing). Iterate `sorted(glob('*.zip'))` for alphabetical order, and print exactly `filename: ACCEPT`/`REJECT` per file. Test against `/app/data/clean` and `/app/data/evil` before considering it done, since a single hidden edge case (extra newline, wrong salt encoding, wrong file ordering assumption) will fail the hidden corpora.