Run `cargo test` inside `/app/seq_utils` to see the failing assertion in `melting_temp` — compare its current formula against the standard Wallace rule `Tm = 2*(A+T) + 4*(G+C)`; the bug pairs the wrong bases with the wrong coefficients, so just correct the arithmetic, don't rewrite the counting logic. Scaffold `cargo new /home/user/analyzer` with a `path` dependency on `/app/seq_utils`. Parse stdin by skipping lines starting with `>` and concatenating the rest (trimmed) into one sequence. If it's under 20 bp, print `TOO_SHORT` and stop. Otherwise slide a 20-bp window with stride 1 across the sequence, compute each window's Tm via the fixed `melting_temp`, and bucket into 10 fixed-width bins spanning [40,80) with the last bin closed on both ends — compute the index as `(tm - 40) / 4` via integer division and clamp to 9 so `tm == 80` lands correctly. Print the 10 counts comma-separated, build with `cargo build --release`, and sanity-check against a few hand-built sequences before trusting it against the fuzz tester.
