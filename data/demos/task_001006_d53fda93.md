Recover the SALT string and WINDOW integer from `/app/specs/params.png` with `tesseract` (crop/upscale first if OCR misreads characters) — use these exact recovered values in your code, don't hardcode a guess. For `detect.sh`, validate each CSV row with a strict character-class check on `sensor_id` (only `[a-zA-Z0-9_]`, e.g. via a bash regex or `grep -qvE`) and a numeric range check on `value` (reject `< 0.0` or `> 1000.0`, e.g. via `awk`); print `CLEAN` only if every row in the file passes, otherwise `EVIL`, matching exactly against the provided clean/evil corpora until you get 100%. For `pipeline.cpp`, run each incoming line through the same validation logic first (reuse or reimplement it, don't skip filtering), then dedupe using a hash set keyed on the concatenated string `<timestamp_sec><sensor_id><SALT>` — construct the key with the OCR'd SALT, not a placeholder. Bucket by `timestamp_sec / 60 * 60`, and for the moving average maintain per-sensor bucket histories so each bucket's average covers itself plus the previous `WINDOW - 1` buckets (skip buckets with no prior data rather than treating missing buckets as zero). Print `bucket_time,sensor_id,moving_avg` per line, and test with a small handcrafted duplicate-containing input before trusting it.
