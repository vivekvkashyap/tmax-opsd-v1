Diagnose the bug by reading the SQL: `FROM nodes n, edges e1, edges e2` with only `n.id = e1.source` in WHERE is an implicit cross join — e2 is never linked to e1, so every row of e2 multiplies every (n, e1) pair. Rewrite using explicit `JOIN`s: join `nodes n` to `edges e1 ON n.id = e1.source` to get direct neighbors, then join `edges e2 ON e1.target = e2.source` to get the second hop, and `COUNT` e2 rows grouped by node — decide between `INNER JOIN` (only nodes with at least one length-2 path appear) and `LEFT JOIN` (all nodes appear, zero-count ones included) and be consistent, since the verifier accepts either as long as the math is correct for whichever set you emit. Test the query directly with `sqlite3 graph.db` before wiring it into Go. After fixing analyze.go, run it and validate the JSON output against `schema.json` (check required keys and types match) before redirecting to results.json. Finally, run `EXPLAIN QUERY PLAN <your exact fixed query>` via `sqlite3 graph.db "EXPLAIN QUERY PLAN ..."` and save the raw output to plan.txt — make sure the query used for the plan is character-identical to the one embedded in analyze.go.