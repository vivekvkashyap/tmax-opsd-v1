Build the ETL as an awk-centric pipeline rather than naive bash arithmetic. First read the CSV skipping the header, and for each row split `Tags` on `|` (e.g. with awk's `split()` or a `while IFS='|' read` loop) to emit one `(tag, value)` pair per tag. The critical trap: an empty `Value` field must be excluded entirely from both the sum and the count — never let it default to 0 via unquoted arithmetic (`$3+0` or similar silently turns blank into zero). Guard with an explicit emptiness check (e.g. `if (val != "")`) before accumulating into a per-tag sum/count map. After processing all rows, compute `mean = sum/count` for each tag and format it to exactly two decimals with correct rounding (awk's `printf "%.2f"` rounds correctly; avoid truncation via string slicing). Emit rows sorted alphabetically by tag (pipe through `sort` on the token column, keeping the header separate). Write the whole thing as a single `awk` script or an `awk` + `sort` pipeline invoked from `etl.sh`, taking input/output paths as `$1`/`$2`. For `test_etl.sh`, generate a small CSV with at least one blank value and one zero value, run `etl.sh`, and diff against a hand-computed expected CSV to print PASS/FAIL. Remember `chmod +x` both scripts.