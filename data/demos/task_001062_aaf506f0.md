Start by understanding exactly what's broken before touching anything: run `make` (or inspect the Makefile) to see the undefined-reference error, then use `nm vendor/auth.o` to list its undefined symbols and cross-reference with `nm` or `objdump -d malicious.S`-equivalent (or just read malicious.S) to find which symbol auth.o imports from it — that's the one function you must stub, not the whole file's behavior. Write clean_stub.c (simplest route) defining that exact function name with C linkage, doing nothing and returning immediately (matching the return type auth.o expects — check the calling code's use of the return value, if any, to decide int vs void). Edit the Makefile: replace the malicious.o compile rule and its entry in the OBJS list with a clean_stub.o rule/object, removing every reference to malicious.S so it's excised from the build entirely. Run `make` to produce server_secure, then verify with `nm server_secure | grep -i malicious` that the malicious symbol is truly gone while the expected symbol is still resolved. Finally write dep_graph.txt listing the actual linked .o files (server.o, http_parser.o, vendor/auth.o, clean_stub.o) comma-separated on one line.