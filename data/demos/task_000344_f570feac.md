Decode `/app/telemetry.mp4` yourself before writing config logic: extract frames (`ffmpeg -i /app/telemetry.mp4 frame_%03d.png` or per-frame mean brightness), classify each of the 120 frames as black (0) or white (1), group every 8 bits MSB-first into a byte, and convert to ASCII to recover the `KEY=VALUE` baseline string — derive it from the frame data, don't guess it. For the config manager, the core discipline is atomicity: every write to `objects/` or `keys/` must go through a temp file/symlink in `<workspace>/tmp/` followed by an atomic rename — never write or symlink directly to the final path. Implement WRITE (hex-decode -> decode via given encoding -> re-encode UTF-8 -> sha256 -> atomic object write -> atomic symlink update), ALIAS (copy the resolved symlink target verbatim, no-op if source key missing), and SNAPSHOT (hardlink the resolved object into `snapshots/` keyed by key and unix timestamp, disambiguating same-second collisions with a counter). Apply the decoded baseline as an implicit first WRITE before consuming stdin, then print all keys sorted alphabetically as `KEY:CONTENT` by resolving each symlink's target.
