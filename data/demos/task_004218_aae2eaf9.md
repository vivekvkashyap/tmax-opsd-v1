First triage every `.log.gz` file in `/home/user/backups` with `gzip -t <file>` (or check the first two bytes for the `1f 8b` magic via `xxd`/`file`) and delete any that fail — don't assume file extension implies validity. Decompress the survivors with `gzip -d` (or `gunzip -k` then remove originals) to get plain `.log` files. For the sanitization step, use a precise in-place replace such as `sed -i 's/\[OLD_STORAGE_NODE_55\]/[ARCHIVE_NODE_01]/g' *.log`, being careful to escape the brackets since they're regex metacharacters. Then compute manifest lines with `sha256sum *.log`, but note `sha256sum`'s default two-space separator and relative filenames — convert paths to absolute (e.g. prefix `/home/user/backups/`) and sort the lines alphabetically by path (`sort -k2`) before writing to `/home/user/valid_logs_manifest.txt`. Finally delete every remaining `.gz` file (both the ones you extracted and confirm no corrupt ones survive) so only the three sanitized `.log` files remain in the directory, and double check the manifest hashes actually match the post-replacement file contents by recomputing one by hand.