Chain the artifacts: OCR `/app/legacy_schema.png` with `tesseract` to recover the `MASTER_TOKEN` and `SERVICE_PORT` lines exactly as printed — don't transcribe from a visual guess. Extract `/app/backup_configs.zip` to `/home/user/configs/`, then check integrity with `unzip -t backup_configs.zip` (or `zipinfo`/per-file CRC check) to find which entries fail — the deliberately-corrupted file should error out or be excluded; only rename the entries that extracted and verified cleanly, using something like `find ... -name '*.conf.bak' -exec bash -c 'mv "$1" "${1%.conf.bak}.json"' _ {} \;`. Replace every `{{TOKEN_PLACEHOLDER}}` occurrence in the valid `.json` files with the OCR'd token using `sed -i "s/{{TOKEN_PLACEHOLDER}}/<token>/g"`. Then scaffold a minimal Rust HTTP server (axum/actix-web or even std `TcpListener` if crates are unavailable) bound to `127.0.0.1:<port>` from the image, implementing `GET /api/config/{filename}` that reads `configs/<filename>` and returns its raw contents as `application/json` with 200, or 404 if missing — build with `cargo build --release` and run the binary in the background so it stays up for verification.
