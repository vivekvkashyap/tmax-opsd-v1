Process files in a sorted loop: `for f in $(ls /home/user/archives/*.dat | sort)`. For each file, read the 8-byte header without a trailing newline using `dd if="$f" bs=1 count=8 2>/dev/null` (or `head -c 8`), and compare it exactly to the string `DATAv2.0`. For matching files, extract everything after the first 8 bytes with `dd if="$f" bs=1 skip=8 2>/dev/null` (or `tail -c +9`) and pipe that raw gzip stream through `gunzip -c` (or `zcat`) to get the decompressed text — do this in a subshell/pipeline so you don't need a temp file. Append the decompressed text to `/home/user/combined_v2.json`, but wrap the append step with `flock /home/user/combined.lock -c '...'` (or `exec 200>lock; flock 200; ...`) so it's safe under concurrent runs, per the constraint. Track a counter of successfully processed v2 files as you go. After the loop, count total lines in the combined file with `wc -l` and write both numbers to `/home/user/summary.txt` on separate lines. Run the script once and manually inspect both output files for correctness before finishing.
