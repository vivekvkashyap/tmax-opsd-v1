First recover the constraints by transcribing `/app/dba_notes.wav` (e.g. `whisper-cli /app/dba_notes.wav` or an ffmpeg+ASR pipeline) — listen for the exact traversal depth limit and the exact field name that must appear in every RETURN clause; use precisely what the transcript says rather than guessing typical values. Then write `/home/user/query_sanitizer.c` as a text parser, not a full query engine: locate the `MATCH` line and extract the traversal spec inside `*...->` (patterns like `*1..3`, `*4`, or unbounded `*1..` with no upper number) — reject unbounded patterns outright, and reject any bounded pattern whose upper number exceeds the limit from the audio. Separately, scan the `RETURN` line's field list for the required field name; reject if absent. Compile with `gcc -o /home/user/query_sanitizer query_sanitizer.c`, and iteratively test against every file in `/app/corpus/clean/` (must exit 0) and `/app/corpus/evil/` (must exit non-zero) with a small shell loop, fixing parsing edge cases (e.g. `*4` with no range, trailing whitespace, multiple RETURN fields) until 100% pass — string-match carefully against the exact corpus formats rather than over-engineering a generic query grammar.
