Two independent fixes are needed. First, open the step-size adaptation code inside `/app/adaptive_solve` (its integrator module) and find the line computing the new step size from tolerance and error — it currently uses `(err/tol)**0.5`, which is inverted; correct it to `(tol/err)**0.5` so that small error grows the step and large error shrinks it. Reinstall the package into your environment (e.g. `pip install -e /app/adaptive_solve` or equivalent) so your script picks up the fix rather than a stale copy. Second, in `/home/user/solve.py`, implement a standard O(m*n) dynamic-programming LCS length function for each pair of input sequences (including i==j, which gives the sequence's own length) to build the N×N matrix. Use `numpy.linalg.svd` to get singular values and take the largest (they're already sorted descending). Call the fixed integrator with that value as `r` and the exact keyword arguments given in the spec. Print in the exact required format — mismatched spacing or precision will fail equivalence checks against the oracle, so match `y_final: {y:.6f}, steps: {steps}` precisely and avoid any extra stdout output.
