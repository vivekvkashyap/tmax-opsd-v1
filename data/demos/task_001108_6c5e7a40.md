Load both CSVs into an in-memory SQLite DB with Python's `csv` + `sqlite3` (create `employees` and `departments` tables, `executemany` the rows in), then do all the real work in one SQL statement using `WITH RECURSIVE`: the base case selects the row where `id = ?` (the CLI arg, bound as a parameter, not string-interpolated), and the recursive case joins `employees e JOIN subtree s ON e.manager_id = s.id` to pull in direct reports layer by layer until no more match. Join that recursive CTE's employee rows to `departments` on `dept_id`, and in the same query compute `SUM(salary * budget_modifier)` over the whole joined subtree — this avoids doing the tree walk and aggregation in separate Python steps. Watch two subtleties: `manager_id` is stored as an empty string for the CEO, and `dept_id` type/join must match between the two tables (cast if needed since CSV values arrive as strings — either declare column types or cast explicitly in SQL, since comparing `TEXT` to `INTEGER` silently fails to match in SQLite). Print only the final sum, formatted with `"{:.2f}".format(total)`, nothing else on stdout. Test with a couple of different manager_ids to confirm the recursion and rounding both look right before considering it done.