This task has two unrelated halves — solve them independently. For the Makefile: a linking error when the source uses `math.h` functions (like `sqrt`) almost always means the math library isn't linked; add `-lm` to the final link recipe (it must come after the object files on the command line) and rerun `make` until `log_merger` builds cleanly, then invoke it on the two log files and redirect output to the target path. For the WAF binary: do NOT try to disassemble it exhaustively — instead treat it as a black box and probe it interactively with a wide range of crafted paths (encoded traversal like `%2e%2e%2f`, double slashes, `/./` segments, mixed-case and boundary substrings) to infer its exact algorithm empirically: URL-decode `%XX` sequences, normalize by collapsing repeated slashes and removing `/./` segments, then substring-match against a blocklist. Confirm ordering matters (decode/normalize BEFORE blocklist check, not after) by testing inputs where only the decoded form contains a blocked substring. Implement the same three-stage pipeline in pure Bash/coreutils (sed/printf for decoding, parameter expansion or sed for slash-collapsing, and case/grep for substring matching), and validate against your own test corpus of edge cases before trusting it against the fuzzer, matching both stdout ("ALLOW"/"BLOCK") and exit code exactly.