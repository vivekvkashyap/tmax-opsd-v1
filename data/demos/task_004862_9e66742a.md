Build this project bottom-up: data file, dependency, core logic, tests, then build/run script. First write `/home/user/release_manager/input.b64` exactly as given. Fetch `cJSON.h`/`cJSON.c` (a small standard single-file JSON library) into `lib/` — if network access to the canonical repo isn't available, vendoring a minimal compatible cJSON implementation yourself is acceptable as long as it exposes the parse API you use. In `validator.c`, write your own base64 decoder (standard 4-char-to-3-byte table lookup, handling `=` padding) rather than relying on an external base64 library, since none is specified as available. The core logic is a simple running-totals state machine: iterate lines in order, decode+parse each JSON object, and only commit (add to totals, append to accepted list) if CPU<=16, Mem<=32, and critical-tier count<=2 would still hold after inclusion — reject without mutating totals otherwise; this "check before commit, don't roll back" ordering is the easy place to get wrong. Write accepted service names, one per line, to `deploy_plan.txt`. Then write `test_validator.c` exercising the constraint logic directly (e.g. call your validation function with a pre-loaded critical count of 2 and assert rejection), log its output, and finally script the full build+test+run sequence in a Makefile or shell script and execute it end to end.