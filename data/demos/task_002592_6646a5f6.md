Split this into two independent sub-problems and solve them separately. For the frame analysis, extract frames from `/app/incident.mp4` with `ffmpeg -i /app/incident.mp4 frame_%04d.png` (or similar), then iterate frames in order checking pixel content (e.g. with Python/PIL: is every pixel's value at or near (0,0,0)?) to find the first 0-indexed frame that is fully black — write that integer to `/home/user/corrupted_frame.txt`, double-checking your indexing base (0 vs 1) against the frame numbering `ffmpeg` produces. For the classifier, don't rely on the visual black-frame cue at all — instead inspect `/app/incident.mp4` and the evil/clean corpora with `ffprobe -show_format -show_streams` (and/or raw `strings`/hex inspection) to find what metadata field distinguishes corrupted files from clean ones; cross-reference `/app/traffic.pcap` (e.g. via `tcpdump -A` or Wireshark/tshark) for a suspicious header value that hints at the exact malicious marker to search for. Implement `/home/user/sanitizer.py` to check for that specific marker in a video's metadata (via `ffprobe` JSON output or raw byte scanning) and exit 1 if present, 0 otherwise. Validate against every file in both `/app/corpus/clean/` and `/app/corpus/evil/` before considering it done.
