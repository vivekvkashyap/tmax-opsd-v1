Read the existing `build_model.sh` first to understand its exact loop structure, then modify it without changing the algorithm. For corrupted-line filtering, validate each line against a numeric regex (e.g. `[[ $line =~ ^-?[0-9]+([.][0-9]+)?$ ]]`) inside the read loop and `continue` past anything that doesn't match, rather than trying to catch arithmetic errors after the fact. Replace every `$(( ... ))` integer computation with `awk` or `bc -l`, carrying at least 4 decimal places throughout (including the average and the convergence-difference check) — mixing bash integer math back in anywhere will reintroduce truncation and break convergence. Append one line per loop iteration to the trace log in the exact `Iteration X: YYYYY` format (4 decimals) as you go, not after the loop ends, so it reflects the true iteration count. Keep the same `< 0.01` stopping condition and update formula, just computed with proper floating-point precision, and write only the final baseline (4 decimals) to the output file. Test end-to-end by running the script and inspecting both output files.
