Locate the deadlock first: search the vendored package (`/app/graph_auditor`) for wherever it acquires two locks for an edge insertion — likely a function like `insert_edge` that locks the source node then the destination node in whatever order the caller passed them. That's the classic lock-ordering deadlock: thread 1 inserts A→B (locks A then B) while thread 2 concurrently inserts B→A (locks B then A), and each waits on the other forever. The fix is to always acquire locks in a single global, deterministic order regardless of edge direction — e.g. sort the two node IDs and lock them in that sorted order — so no two threads can ever form a circular wait. Grep the codebase for `lock`/`acquire`/`Lock(` calls around edge/transaction insertion to find the exact spot. After fixing it, write `audit.py` to load the transactions via the package's API, build an index on account ID for fast lookup (check what indexing method the package exposes, e.g. a hash index or explicit `create_index` call), then use its cycle/aggregation query API to find cycles up to length 3 filtered by total transacted amount > $10,000, printing results as JSON. Since this will be fuzz-tested against a reference for exact equivalence, test with concurrent/threaded insertion locally to confirm the deadlock fix actually holds under load, and verify determinism (stable ordering) of your printed cycle list across repeated runs with identical input.