Treat `/app/legacy_backup_bin` purely as a black-box oracle for behavior, not something to copy or call from your solution: run it with several `<root> <max_depth> <page_size> <page_num>` combinations (varying depth 0/1/2/3, small and large page sizes, page_num beyond the last page, edge nodes with no outgoing edges) and diff outputs to pin down exact BFS semantics (undirected-vs-directed traversal, how ties/duplicates are handled, what happens when a page is out of range) and the exact JSON spacing/field order so your program's stdout is byte-identical for the same inputs. For the real fix: read `graph_data.bin` (8-byte LE uint32 pairs) into memory ONCE at startup, build an adjacency list (e.g. sorted arrays or a hash map from node id to a vector of neighbors) rather than re-scanning the file per BFS step — that in-memory adjacency lookup is what turns the O(V_reachable*E) file-scanning behavior of the legacy tool into O(V+E). Implement standard BFS with a visited set, collect reachable node ids, sort ascending, then slice `[page_num*page_size : page_num*page_size+page_size]` for the results array. Benchmark both binaries on the same large query and confirm both output equality and the required speedup margin before finishing.