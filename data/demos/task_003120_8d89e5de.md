Approach this as three separable checkpoints and verify each before moving on. First, diagnose the C source in `fast-udp-probe-1.0`: read `probe.c` and look specifically inside the packet-send loop for anything that artificially throttles throughput per iteration (e.g. a sleep/delay call) — that's the injected bottleneck, not an algorithmic inefficiency, so don't rewrite the whole loop, just remove the throttle and rebuild with the provided `make`. Second, before measuring throughput, start the router simulator script and confirm with a quick check (e.g. `ss`/`nc`) that something is actually bound to UDP 8080, then run the probe against `127.0.0.1 8080` and capture its real stdout (containing the `Throughput: <n> pps` line) into the results file — don't fabricate or hand-edit that number, let the fixed program produce it, and confirm it clears the required threshold. Third, write `monitor.sh` as a standalone rotation-then-log script: check the log file's size (e.g. via `stat`/`wc -c`) before each run, shift `probe.log.1`→`probe.log.2` and `probe.log`→`probe.log.1` only when the size threshold is exceeded, capping backups at 2, then append the probe's fresh output to `probe.log`.