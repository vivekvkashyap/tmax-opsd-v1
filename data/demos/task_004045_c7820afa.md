Start by localizing the real bug rather than rewriting the whole vendored library: read `/app/fast_ts_parser/parser.py` and look specifically for a naive `data.split('\n')` or similar line-splitting used to parse CSV — that's what breaks on embedded newlines inside quoted fields, since a real CSV record can legitimately span multiple physical lines. Fix it by replacing the manual split with Python's standard `csv.reader` (or `csv.DictReader`) operating on the raw file/stream, which correctly respects quoting and embedded newlines; re-run the package's own tests/feature-extraction and DTW-distance functions afterward to confirm rows are no longer silently dropped, since downstream feature extraction depends on getting every row. Then build `/home/user/sanitize_ts.py` with the exact `--input-dir/--output-dir/--reference` CLI contract, using the fixed library to load the reference series once and, for each input file, compute its features and DTW distance against the reference (reject if distance > 50.0) plus a numeric-column validity check (reject if any value in a numeric column fails to parse as a number). Files that pass both checks must be copied byte-for-byte unmodified (not re-serialized) into the output directory under their original filename. Validate against `/app/test_data/corpora/clean` and `/app/test_data/corpora/evil` yourself before finishing.
