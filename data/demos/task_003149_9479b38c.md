These two subtasks are independent — solve them separately and verify each before combining effort. For the video task, extract frames from `/app/dashboard_recording.mp4` (e.g. `ffmpeg -i ... frame_%04d.png`) and programmatically scan each frame for a contiguous pure-red (255,0,0) region of at least 50x50 pixels — use image analysis (e.g. PIL/numpy: build a boolean mask of exact-red pixels, then check for a large enough connected blob rather than just "any red pixel present", since noise/anti-aliasing near edges could produce false positives if you're not checking region size) and record the qualifying frame numbers into a comma-separated list. Recheck your frame numbering matches ffmpeg's actual output indexing (1-based vs 0-based, and video fps) before finalizing. For the filter task, write `cost_filter.py` to recursively walk the given directory (`os.walk`) checking, per file: size via `os.path.getsize` > 100000 bytes, extension in `{.dump,.iso,.mp4}` (case as given), and file contents (binary-safe read) containing the exact substring `[COST_ALERT: MASSIVE_LOG]` — reject on the first violation found, otherwise accept; print exactly `REJECT`/`ACCEPT` and use matching exit codes. Test both scripts against a few of your own constructed examples before relying on the hidden corpora.