Design `/home/user/process.sh` to never blindly `tar -xzf` an untrusted archive to disk. For each `.tar.gz` (processed in sorted/alphabetical order), first stream its listing with `tar -tzf <archive>` and classify each entry: a path is malicious if it starts with `/` or contains `../` anywhere; append any such raw paths (exactly as listed) to `/home/user/malicious_paths.log`. Build the complement list of safe paths and extract only those, e.g. by passing the filtered path list to `tar -xzf <archive> -C <output_dir> <safe_path_1> <safe_path_2> ...` (tar lets you name specific members to extract), so malicious entries are never written to the filesystem even transiently — do not extract everything first and then delete bad files, since that briefly creates a real traversal-write window. Since later archives should overwrite same-named files from earlier ones, simply extract archives in order into the same persistent output directory without clearing it between archives. After each archive's safe extraction completes, compute `du -sb <output_dir> | cut -f1` and append `<archive_filename>: <size>` to `/home/user/growth.log`, formatted exactly as specified. Test your script against both the given fixture (planted absolute-path and `../` payloads) and a legitimate nested-directory archive to confirm normal restores still work.