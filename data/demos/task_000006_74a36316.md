Start by reading `server.cpp` end to end and locating the three known fault classes before changing anything. First fix the variance math: a naive `sum_sq/n - (sum/n)^2` formula loses precision and can go negative under noise, producing NaN when sqrt'd — replace it with Welford's online algorithm (maintain running mean and M2, update per new sample, and derive variance as M2/n). Second, identify every place the video-processing thread writes centroid/stats and every place the HTTP and TCP handler threads read them; wrap all shared-state reads and writes with a single `std::mutex` (lock guard) so concurrent access can't produce torn/garbage doubles or crashes under load. Third, make sure JSON serialization always emits valid, well-formed numbers (guard against ever writing literal "nan"/"inf" tokens, and don't build JSON on top of possibly-corrupted memory). Also verify the ffmpeg frame-extraction path points at the real test video location the task specifies. After fixing, compile exactly as instructed, launch the binary in the background, and confirm both ports are listening before considering the task done — the verifier will hammer both endpoints concurrently and cross-check TCP bytes against the HTTP JSON.
