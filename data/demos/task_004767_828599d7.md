First recover the exact binary record schema from the image before writing any parsing code: run `tesseract /app/schema.png stdout` and read off each field's byte offset, size, and type carefully (there is a fixed record size with several typed fields including one field you must discard, plus a status-code field used for filtering) — get this precisely right, since a single wrong offset/size corrupts every downstream value. Prototype parsing on one file first with Python's `struct.unpack` against a small format string derived from the schema, and print a few decoded records to sanity-check they look plausible (e.g. status codes look like real HTTP-ish codes, timestamps are monotonically increasing) before scaling up.

For the pipeline itself: use `mmap.mmap` per file for fast reads, iterate record-by-record computing offsets, and only keep the non-discarded fields for rows whose status code is not in the excluded set. For concurrency, use `multiprocessing.Pool` where each worker processes one file and writes results to a small in-memory buffer, then acquires an `fcntl.flock(fd, fcntl.LOCK_EX)` on the shared output file, writes its buffer with `os.write`/`f.write` in one shot, flushes, and releases the lock — don't have workers interleave individual `write()` calls, since that risks corrupting records under concurrent access. Verify final record count and a handful of decoded values against your own manual computation before finishing.