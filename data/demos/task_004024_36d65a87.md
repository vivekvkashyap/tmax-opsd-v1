Treat `/app/detector_oracle` as a black-box classifier and probe it systematically rather than guessing: generate synthetic 5-column CSVs where you deliberately vary the pairwise correlation structure between different column pairs (holding others independent/random), run the oracle on each, and record which correlation strength/column-pair combination flips its exit code. Binary-search the threshold along a suspected axis once you find a sensitive column pair, and confirm the boundary is symmetric (check both strongly positive and strongly negative correlation) and that other column pairs don't matter. Once you've nailed down which columns and what correlation-coefficient threshold triggers rejection, implement the same statistic (e.g. Pearson correlation via a linear-algebra or manual crate) in Rust with matching strict/non-strict comparison at the boundary, matching the oracle's exact behavior at edge cases. Build in `--release`, and validate your binary against the same sample files and against freshly generated edge-case CSVs (correlation values very close to the boundary) before trusting it, since off-by-a-hair threshold mismatches are the likely failure mode.
