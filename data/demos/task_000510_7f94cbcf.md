For deduplication, write a script that walks `/app/storage_pool`, computes a SHA256 (or similar strong hash) of each file's full content, groups files by identical hash, picks one canonical path per group, and replaces every other file in that group with a hard link to the canonical file (`ln -f canonical duplicate`, or remove-then-`ln`) — verify afterward that duplicate wavs now share the same inode (`stat -c %i` or `ls -li`), since the checker specifically looks for any `.wav` with link count of 1. For the manifest, iterate unique inodes (not unique paths) in `/app/storage_pool`, hash each, and write one `<sha256>  <canonical_path>` line per unique file, sorted by path. For audio compression, use `ffmpeg` to encode `/app/voicemail.wav` to Ogg (Vorbis or Opus) at `/home/user/compressed_audio.ogg` — start at a moderate bitrate/quality setting and iterate: check the resulting file size stays under 150KB while keeping quality as high as the budget allows, since the verifier computes MSE between the decoded waveforms (via librosa) at the original sample rate. Test different bitrates if the first attempt is too large or too lossy, and confirm both the size and an approximate reconstruction error before finishing.