Build this as three independent, testable layers before wiring them together. First write a small ingestion routine: use httplib's Client to GET /metadata from 127.0.0.1:9001 and parse the JSON array with nlohmann::json into a map<int, string> of id->type; separately open a raw TCP socket (socket/connect/read loop) to 127.0.0.1:9002, read until EOF, and split each line on the comma to build an adjacency list (add both directions since edges are undirected). Run the ingestion standalone first and print counts to confirm you got real data before adding the HTTP server. Second, compute analytics once at startup (or lazily cache): degree = adjacency[node].size(); connected components via BFS/union-find over the whole graph, storing component id and precomputed component size per node so endpoint lookups are O(1). Third, implement the httplib::Server with the three routes; for /query, do not build a general parser — extract the two literals after "a.id = " and "b.type = '" with straightforward substring/regex parsing of that exact fixed template, then filter neighbors by stored type and return ascending-sorted ids. Bind and listen on 127.0.0.1:8080 in the background (e.g. nohup + &) so the process survives your session ending, and verify with curl before finishing.
