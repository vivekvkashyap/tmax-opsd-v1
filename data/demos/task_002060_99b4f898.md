Open `/app/proxy-resolver/router.py` and compare its actual logic line-by-line against the spec in the task description — don't just skim; the injected bug is subtle (likely a swapped string-concatenation order like `f"{dst}:{src}"` instead of `f"{src}:{dst}"`, and/or a wrong modulo base or wrong output-path template). Rewrite it to exactly: build `f"{src_ip}:{dst_domain}"`, MD5-hash it, take the last two hex characters, parse as base-16, mod 4, and format `/var/run/upstream_<result>.sock`. Test by hand-computing MD5 of a known string (`python3 -c "import hashlib; print(hashlib.md5(b'...').hexdigest())"`) and checking your script's output matches before trusting it broadly. Make `resolver.py` executable (`chmod +x`) and confirm it reads `<src_ip> <dst_domain>` lines from stdin, emitting exactly one socket path per line. For `/home/user/run_ci.sh`, pipe 3 sample input lines into the resolver and assert the outputs match expected values you compute independently, exiting nonzero on mismatch. For `/home/user/resolver.cron`, write a standard 5-field cron line (`*/5 * * * * /home/user/run_ci.sh`) — do not install it via `crontab`, just save the file.
