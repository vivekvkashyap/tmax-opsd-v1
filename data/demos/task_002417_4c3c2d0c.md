First fix the two pre-existing services before writing any new code: edit `/app/redis.conf` to bind `127.0.0.1` on `port 6379` and remove/replace the unixsocket-only config so `redis-server` actually listens on TCP; check `/app/api/app.py`'s Flask host binding and install `flask`/`redis` python packages if missing, then confirm `bash /app/start_services.sh` actually leaves both processes listening (verify with `curl`/`redis-cli ping` before moving on — don't assume the script works). For the pipeline script, replicate the bootstrap procedure exactly as specified: seed `random.Random(user_id)` once, then call `rng.choices(base_array, k=4)` 100 times in a fixed loop (order and seeding matter bit-for-bit for reproducibility against the grader), average each sample, then average the 100 means. Query Redis for `multiplier_<user_id>` in db 0, defaulting to 1.0 only if the key is absent (not if it's falsy/zero). POST the final feature to the Flask endpoint and print only the formatted 4-decimal prediction with no extra output — stray print/debug statements will break the exact-output check the grader uses across many randomized inputs.