Design the C program around two separate passes over the parsed edge list (citing_paper_id, cited_paper_id, year), since the two report sections are independent computations. For the longest chain: build an adjacency list keyed by paper ID (bounded array since max ID is 999) and run a DFS/topological longest-path computation over the DAG (memoize the longest path starting from each node to avoid exponential blowup) — track edge count, not node count. For the top-cited-paper-per-year: use a 2D count array (indexed by [year_offset][cited_paper_id] or a hash-like structure) incrementing per citation edge, then for each distinct year scan the counts to find the max, breaking ties by choosing the lowest paper ID (iterate IDs in ascending order and only replace the current best on strictly greater count). Read the CSV with simple `fscanf`/`sscanf` on the fixed comma-separated integer format, and be careful with off-by-one/whitespace when writing output lines. Format output exactly as specified (including "Longest Chain Length: <n>" and per-year lines sorted ascending by year), write to `/home/user/results.txt`, then compile with the exact `gcc -O2` command and run it before considering the task complete.
