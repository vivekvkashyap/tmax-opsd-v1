Reverse-engineer the obfuscation algorithm rather than shelling out to the binary per-record (50k records means subprocess-per-call will be too slow). Use tools like `file`, `strings /app/obfuscator_oracle`, and `objdump -d /app/obfuscator_oracle` (or `gdb`/`ghidra` if available) to inspect the disassembly around where it processes argv — look for a hex-decode step and a single-byte XOR loop constant. Confirm your hypothesis empirically: run the binary on a few known/crafted hex strings and check the decoded output matches what you'd expect from hex-decode + XOR with the byte you found, then port that exact transform into Python (bytes.fromhex, XOR each byte, decode utf-8) so decoding is done natively and fast. Build the HTTP server (stdlib `http.server` or a lightweight framework) exposing `POST /api/v1/ingest` on `127.0.0.1:8000`; parse the JSON body, decode each log, split on `|` into TIMESTAMP/IPV4/SEVERITY/MESSAGE, normalize IP octets by stripping leading zeros per-octet, uppercase SEVERITY, and bulk-insert all rows into `/home/user/logs.db`'s `system_logs` table in one `executemany` transaction for speed. Return the exact JSON response shape and leave the server running.
