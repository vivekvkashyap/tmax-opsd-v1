Start by inventorying the running services (check `/app/services/` and any compose file for how Postgres/Neo4j/the API are already provisioned) and confirm you can connect to each with a quick client command before writing code. Configure `/app/api/.env` with the Postgres DSN and Neo4j bolt URI/credentials the app expects, then get `/app/api/main.py` importing and starting cleanly. For the ETL script: write a recursive CTE against the `employees` table (`WITH RECURSIVE chain AS (... UNION ALL ...)`) to walk `manager_id` up to the root, then use the Neo4j Python driver with parameterized Cypher (`MERGE (e:Employee {id:$id}) ... MERGE (e)-[:REPORTS_TO]->(m)`) to load nodes/relationships — never string-format IDs into Cypher. For the two endpoints, implement Bearer-token auth as a dependency that checks the `Authorization` header and returns 401 on mismatch/missing before any DB work; the hierarchy endpoint should run a Cypher path query in Neo4j, and the cross-reference endpoint should join Postgres (department name via department_id, salary) with a Neo4j count of direct reports. Launch the API as a background process on `127.0.0.1:8080` and redirect its stdout/stderr to `/home/user/api_startup.log`, then manually curl both endpoints (with and without the token) to confirm behavior before finishing.