Write organize.py to process each `.dat` file with `mmap.mmap(f.fileno(), 0, access=mmap.ACCESS_READ)`, slicing just the first 24 bytes from the mapped object (this avoids reading the whole multi-hundred-MB file into RAM, which is the point of the requirement). Parse the header precisely per the given byte layout: bytes 0-7 are the magic string (validate it equals `SNSR_DAT`, skip/warn on files that don't match rather than crashing), bytes 8-15 are the sensor ID as a null-padded ASCII string (strip trailing `\x00` bytes after decoding, e.g. `header[8:16].rstrip(b'\x00').decode('ascii')`), and bytes 16-23 are a little-endian unsigned 64-bit integer timestamp (`struct.unpack('<Q', header[16:24])[0]`). For each file, `os.makedirs(f'/home/user/organized_dataset/{sensor_id}', exist_ok=True)` then create the symlink with `os.symlink(target, link_path)` — critically, the target must be the *absolute* path of the original raw file (use `os.path.abspath` on the source, not a relative path), since the verification does an exact string comparison against `readlink`. Close/release the mmap and file handle per iteration rather than holding them all open, and iterate files in any order since the verification checks each symlink independently by fixed name.