Write `clean_data.sh` as a single `awk`-driven pass over `/home/user/raw_data.csv` for correctness and simplicity: use `awk -F,` to (a) drop rows where the `age` field (2nd column) is empty, and (b) drop rows where `income` (3rd column) is `< 0` or `> 1000000`, always keeping the header row unconditionally (e.g. `NR==1 || (cond)`). Redirect the result to `/home/user/cleaned_data.csv`, preserving column order and original formatting. Count data rows with `wc -l` minus 1 (or `awk 'END{print NR-1}'`) on both the raw and cleaned files, and append a log line to `/home/user/experiment_log.txt` using `date '+%Y-%m-%d %H:%M:%S'` for the dynamic timestamp, matching the exact `[<timestamp>] Initial: <N> rows, Cleaned: <M> rows` format — watch for off-by-one errors from counting the header as a data row. Make the script executable with `chmod +x` and run it once so both output files exist before finishing.