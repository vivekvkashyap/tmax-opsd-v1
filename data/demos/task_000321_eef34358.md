Transcribe `/app/redteam_briefing.wav` with a local tool (e.g. `whisper` or another installed ASR) rather than guessing at content — run it and read the actual transcript text before designing the filter, since the specific evasion techniques named in the audio determine exactly what your detector must flag. Expect the techniques to be classic JWT bypasses (things like manipulating the `alg` header, and abusing the `kid` header for path/file tricks) but confirm the precise variants (e.g. exact casing rules, exact traversal pattern) from what the transcript actually says rather than assuming. Build the filter by decoding the JWT header segment (base64url-decode the first dot-separated part, ignore signature validity) and inspect the `alg` and `kid` fields with checks matching what the audio described — use case-insensitive comparison and substring/path checks as appropriate. Validate against the provided `/app/corpus/clean/` and `/app/corpus/evil/` samples first, iterating until you get 100% separation locally, before trusting it against the hidden grading corpus. Write results as filename->boolean JSON per the two-arg CLI spec.
