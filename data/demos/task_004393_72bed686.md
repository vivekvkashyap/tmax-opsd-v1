Work in two clearly separated phases. First, fix the vendored package: inspect `/app/watchdog-3.0.0/src/watchdog/events.py` for the broken import near the top of the file, correct it to the standard library module it should be, then `pip install -e /app/watchdog-3.0.0` and confirm `from watchdog.observers import Observer` imports cleanly before writing any of your own code. Second, build `dataset_server.py`: use a `watchdog` `Observer` with a handler that reacts to file-creation/close events under `/home/user/datasets/incoming/` (create the directory first), and on each `.zip` file, try opening it with Python's `zipfile` module and call `testzip()` (or catch `BadZipFile`) to validate integrity before computing anything — this is the key correctness gate for the 404 case. For valid archives, iterate every entry, read its bytes, and hash with `hashlib.sha256().hexdigest()`, storing results in an in-memory dict keyed by archive filename. Run a simple `http.server`-based handler (or similar) in a background thread on `127.0.0.1:8888` that parses the `archive` query param, looks up the manifest dict, and returns JSON 200 or 404 accordingly. Launch the whole script as a detached background process so it stays listening after your turn ends.