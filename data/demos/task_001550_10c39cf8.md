Fix correctness first, then performance. For the pathing bug, hardcode the absolute paths `/home/user/observability/logs/access.log` and `/home/user/observability/metrics/summary.json` directly in collector.sh (or derive them via `$(dirname "$(readlink -f "$0")")` if you want it portable) — do not rely on `$PWD`, `cd`, or any environment variable, since scheduler.sh strips env and chdirs to /tmp before invoking it. For performance, replace the `while read` loop entirely with a single-pass `awk` script: parse the combined log format by field position (status is typically field 9, bytes field 10 when split on whitespace, treating `"..."` and `[...]` as their own space-delimited tokens), accumulate counters for 2xx/3xx/4xx/5xx via integer division on the status code and sum bytes (skipping non-numeric `-` byte values), then print the JSON in one END block. Guard the empty/missing-file case with an explicit `[ -s "$LOG_FILE" ]` check that writes all-zero JSON before invoking awk. Benchmark with `time` against a large generated log to confirm you're well under 1.5s, not just correct.