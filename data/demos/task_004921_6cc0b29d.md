Build this as a straight ETL-then-SQL pipeline in Python: load both CSVs into pandas/sqlite3, create the `interaction_graph` table by grouping `interactions` on `(actor_email, target_email)` and summing `metric_value * {email:1, meeting:2, code_review:5}[interaction_type]` (compute the weight column first, then GROUP BY and SUM — don't try to do the type-weighting inside raw SQL CASE unless you're careful about exact multipliers). Add indexes on `interaction_graph(target_email)` and `employees(email)` so the join/group in step 4 is efficient. For the ranking, write one SQL query with a CTE that joins `interaction_graph` to `employees` on `target_email = email` to get department, GROUP BY target_email to get total incoming weight, then a second CTE applying `RANK() OVER (PARTITION BY department ORDER BY incoming_weight DESC, email ASC)`, filtering to rank <= 2. Dump the final rows to `/home/user/top_influencers.json` with the exact key names and types specified (incoming_weight as integer). Validate your query's output columns and ordering manually against the CSV data by hand-tracing a couple of departments before finalizing.
