Split this into two independent pieces. (1) For the detector: use a Rust JWT crate (e.g. `jsonwebtoken`) or hand-parse the three base64url segments yourself — decode header and payload JSON, check `alg` case-insensitively for "none", verify the signature with HMAC-SHA256 against the literal key, and scan the `user_id` claim string for the three injection substrings case-insensitively; treat any parse/decode/signature failure as EVIL, and only print EVIL/CLEAN with no other stdout. Build with `cargo build` so the debug binary lands at the required path, then sanity-test it against a few hand-crafted JWTs of each category before trusting it. (2) For remediation: change the literal bind address string in main.rs from `0.0.0.0` to `127.0.0.1`, then rewrite the token validation in auth.rs to construct a `Validation` object that pins the algorithm to HS256 explicitly (reject if the token's header alg differs) and verifies the signature with the shared secret — don't just switch on a claim without also checking the header algorithm, since that's the classic alg-confusion bypass. Restart the stack via start.sh and confirm with curl that a legitimate token gets 200 while alg=none is rejected, and that connecting directly to 8080 from outside fails.
