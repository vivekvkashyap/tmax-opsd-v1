Design the C++ parser around the record-boundary rule explicitly: a new record starts only when a line begins with a bracketed tag like `[INFO]`/`[WARN]`/`[CRITICAL]`, and every following line (including ones that don't start with a tag) belongs to the current record until the next tag line appears — so read files line-by-line, buffer lines into the "current record" only when its tag is `[CRITICAL]`, and flush/reset the buffer whenever a new tag line is seen. Iterate all `*.log` files in `/home/user/logs` (e.g. via `std::filesystem::directory_iterator`), collect matching records preserving exact internal newlines, and write them to `/home/user/critical_summary.tmp`. Critically, use the C `rename()` function (not `std::filesystem::rename` misuse or copy+delete) to atomically move `.tmp` to `.txt` only after all writes are flushed/closed — this is the detail a naive implementation gets wrong, leaving a `.tmp` behind or writing directly to the `.txt` path. Compile with `g++` and run it, confirm `critical_summary.txt` exists and `.tmp` does not, then as a separate shell step (not inside the C++ program) loop over the `.log` files and `mv` each into `/home/user/archive/` renaming the extension to `.archived`.
