In C, parse the CSV header first to learn the config column names dynamically (don't hardcode which configs exist, since the task says the count varies up to 10). For each subsequent row, split on commas, uppercase the `server_id` field in place, and for every config column compare its value to the previous row's value for the SAME server_id and SAME config key — you need a data structure keyed by (server_id, config_key) holding the last-seen value, e.g. parallel arrays or a small fixed-size table since rows are already time-sorted, so a single pass with "last value per (server,key)" lookup suffices (no need to buffer everything). Increment a change counter only when the current value differs from the stored last value AND a last value already exists for that pair (first sighting never counts as a change) — this "skip first occurrence" rule is the easy bug to introduce. After processing all rows, print `SERVER_ID,config_key,num_changes` (headerless) sorted first by server_id then by config_key alphabetically — use `qsort` with a comparator on both fields, or insertion-sort keys as you build the table by server_id order then column order. Compile to `/home/user/tracker` and run it against `/home/user/configs.csv` to produce `/home/user/changes.csv`; sanity-check a couple of rows by hand against the input before considering it done.