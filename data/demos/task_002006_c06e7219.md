Build the regular time grid explicitly first: 11 timestamps at 1-minute steps from `2023-10-01T10:00:00Z` to `2023-10-01T10:10:00Z` inclusive (e.g. via `datetime`/`pandas.date_range`). Parse both CSVs, convert timestamp strings to datetimes, and sort by time. For temp, do linear interpolation: for each grid point find the bracketing raw samples (or exact match) and interpolate `temp = t0 + (t1-t0) * (grid_t - t0_time)/(t1_time - t0_time)` — pandas' `set_index(...).reindex(grid).interpolate(method='time')` works well if you include the raw points in the index first, then reindex down to just the grid. For pressure, use forward-fill only (`.ffill()`), never interpolate — the most recent prior raw value carries forward, and note the raw pressure samples don't align with the grid either (e.g. one falls at :04:30, between two grid points), so ffill must operate on a combined index of raw+grid timestamps, not just the grid alone. Merge on timestamp, format timestamps back to strict `...Z` ISO8601 strings, validate the range/NaN constraints programmatically before writing, and output exactly the three columns in order to `/home/user/aligned_sensors.csv`.