Treat this as an adversarial-input validator, so validate defensively and reject anything ambiguous before touching the filesystem or spawning subprocesses. Parse the config with `mmap()` (map the file, then scan for the four expected `key: value` lines) rather than `fopen`/`ifstream`. Validate in order: all four keys present exactly once; `output_archive` matches a strict allowlist regex/character-by-character check (alnum and underscore only, must end in exactly `.tar`, and explicitly reject any `/`, `\`, or `..` substring — this blocks path traversal even disguised inside an otherwise valid-looking name); `start_frame`/`end_frame` are pure integers (reject trailing garbage); `start_frame >= 0`; `end_frame > start_frame`; and `end_frame` does not exceed the video's actual total frame count (probe this once with `ffprobe` against `/app/dataset_video.mp4`, don't hardcode it). Only after all checks pass should you create a securely-generated temp directory (`mkdtemp`), extract frames `start_frame..end_frame` with `ffmpeg -vf select=...` naming them `frame_0000.jpg` onward, `tar` them uncompressed, write the archive to a temp file in the working directory, then `rename()` it atomically to `output_archive`, and finally clean up the temp directory. Any validation failure must exit 1 with no side effects.
