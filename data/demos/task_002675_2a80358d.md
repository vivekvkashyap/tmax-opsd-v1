Split this into three independently testable pieces and get each working before wiring them together. First write the validation logic (hex msg_id length-16 check, positive-integer timestamp, alphanumeric+space-only user_text under 100 chars with valid UTF-8, and a float-parsable value) as a small reusable C function, since both the CLI filter and the daemon must apply identical rules — factor it into a shared .c/.h or duplicate carefully to avoid drift. Build and test `filter_cli` standalone on hand-crafted good/bad CSV rows before touching Redis, since a strict-rules bug will silently fail an adversarial corpus check. For the daemon, link against `hiredis`, loop on `BLPOP raw_logs 0`, dedupe with a simple hash set (e.g. a fixed-size array of strcmp'd strings or a real hash table) keyed on msg_id, lowercase user_text with `tolower`, and write `/home/user/www/stats.json` atomically (write to a temp file then rename) every 10th valid record so Nginx never serves a half-written file. Fix Nginx's `root`/`alias` directive to point at `/home/user/www` and reload it, and verify the end-to-end path yourself with `redis-cli rpush` and `curl` before considering it done.
