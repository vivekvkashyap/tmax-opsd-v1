Ignore the buggy `/app/legacy_auditor` cross-join behavior except as a cautionary example of what NOT to do — don't try to replicate its false positives. Inspect `/app/data/corp.db` schema (`entities`, `ownership`) and treat `ownership` edges as undirected for clustering. In `/home/user/compliance_filter.py`, load all ownership rows and build an undirected graph (plain Python union-find, or `networkx.Graph()` adding both `(parent,child)` edges) covering all entity ids, then find connected components; determine which component contains `sender_id` and which contains `receiver_id` (a single entity with no edges is its own singleton cluster). A transaction is a VIOLATION only if `amount > 50000.0` AND sender/receiver are in the *same* connected component — different clusters must never be flagged regardless of amount, and same-cluster transactions at or below 50000.0 must never be flagged either (exit 0). Parse the two CLI args `--db` and `--tx` with argparse, load the transaction JSON, run the check, and `sys.exit(1)` for violation or `sys.exit(0)` for compliant. Before finishing, hand-test against a same-cluster/high-amount case and a different-cluster/high-amount case to confirm you didn't accidentally reproduce the legacy binary's cross-join bug.