Load all four CSVs into SQLite (or duckdb/pandas) tables. Build a query that: joins authorships to itself on paper_id (self-join with r1.researcher_id < r2.researcher_id or similar to get unordered pairs without duplicates), joins to publications to filter year >= min_year, joins each researcher to institutions to get institution names, and filters WHERE inst_id_1 != inst_id_2 AND (interest_1 = ? OR interest_2 = ?) using bound parameters (sqlite3 `?` placeholders or duckdb parameter binding) — never f-string/concatenate the interest or min_year into the SQL text. After fetching pairs, in Python determine which of the two researcher names sorts first alphabetically and assign researcher_1/inst_1 vs researcher_2/inst_2 accordingly (do this in code, not in SQL, since the "first alphabetically" role can flip per row). Deduplicate identical (researcher_1, researcher_2, paper_title) rows, then sort by researcher_1, researcher_2, paper_title before writing the CSV with the exact header. Test by running the two example invocations and inspecting the output file.
