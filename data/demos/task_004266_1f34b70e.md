Design filter_docs.sh as a sequence of independent checks, any of which failing should exit non-zero immediately (fail fast/short-circuit). Extract the bundle to a temp directory (it's a tar archive, so plain `tar` works — you likely don't need /app/doc-extractor at all since the task gives you already-unpacked test bundles to validate against) and check in order: (1) exactly one metadata.json at the root and an assets/ directory exist — nothing missing, nothing unexpected; (2) metadata.json is valid JSON (use a real JSON parser, e.g. `python3 -c "import json,sys; json.load(open(...))"`, not grep) and contains a top-level "doc_id" string key; (3) walk assets/, and for any .zip or .tar.gz found, extract it in isolation and confirm it contains no further nested .zip/.tar.gz — reject if it does; (4) for every .png/.jpg anywhere in the unpacked tree, read its first few bytes and check they match the real magic number for that extension (PNG starts with \x89PNG, JPEG with \xFF\xD8\xFF) — a mismatched extension/content is a rejection. Test your script against every file in both test_data/clean and test_data/evil and confirm exit codes match expectations for each before considering it done; use temp dirs per invocation to avoid cross-contamination between checks.
