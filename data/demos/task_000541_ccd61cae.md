Read `/home/user/legacy_parser.py` to understand the DSL grammar (`job <name>:` followed by `requires: [...]`), then design a Rust line-based parser (split on newlines, trim, match on prefixes `job ` and `requires:`, parse the bracketed comma list) building an adjacency map of job -> dependencies. Implement Kahn's topological sort using a `BTreeSet`/sorted `Vec` of ready nodes so ties always resolve alphabetically — do not rely on `HashMap` iteration order. For cycle detection, when Kahn's algorithm stalls (nodes remain but none are ready), run a DFS from each node still in the graph tracking the recursion path to find the cycle, then rotate the reported cycle so it starts at its alphabetically smallest member and format as `A -> B -> C -> A`. For memory tracking, implement a wrapper struct implementing `GlobalAlloc` that delegates to `System` but increments/decrements an `AtomicUsize` on alloc/dealloc, tracking the max value seen, then register it via `#[global_allocator]` and print `PEAK_MEM: <bytes>` before exit. Set up the crate with `cargo new --bin pipeline-parser` at `/home/user/pipeline-parser`, add integration tests under `tests/` covering both a valid DAG and a circular one, then `cargo build --release` and run it against both fixture files to confirm exact stdout formats.