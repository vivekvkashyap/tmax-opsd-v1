Work in the given order since later steps depend on artifacts from earlier ones. First `mkdir -p /home/user/backup` and copy both the existing compiled binary and `/app/data/metrics.db` there before touching anything, so you have a rollback point. Then open `/app/rust_processor/src/main.rs` and find `compute_moving_average`'s O(N^2) pattern (`vec.insert(0, item)` repeatedly shifts the whole vector) — replace it with an O(N) approach, e.g. appending with `push` and reading from the back, or using a `VecDeque`, while keeping the exact same numeric output (same window size, same averaging math, just faster data structure operations). Rebuild with `cargo build --release` in `/app/rust_processor/`. For the interactive deployer, don't try to answer prompts manually — write an Expect script (`/home/user/deploy.exp`) that spawns `python3 /app/deploy_manager.py --binary <path>`, and for each of the four confirmation prompts uses `expect "...? [y/N]:"` followed by `send "y\r"`, ending with `expect eof`. Run the expect script to actually perform the staged rollout so the new optimized binary ends up live on port 8082, then manually hit the processor to sanity-check throughput before considering it done, since an automated benchmark will measure latency against a strict threshold.