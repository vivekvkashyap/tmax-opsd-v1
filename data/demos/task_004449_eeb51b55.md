Work each stage in isolation and record intermediate findings before moving on. Git forensics: run `git log --all --oneline -- secret_params.h` (or `git log -p --follow -- secret_params.h`) to find the commit that added the file before it was removed, then `git show <commit>:secret_params.h > secret_params.h` to restore it exactly as committed, and read the `SECRET_MULTIPLIER` #define value directly from the recovered file rather than guessing. Fuzzing: write a small libFuzzer harness with `extern "C" int LLVMFuzzerTestOneInput(...)` that interprets the input bytes as a 4-byte timestamp and calls `calculate_token`, compile with `clang++ -g -fsanitize=fuzzer,address fuzzer.cpp auth_math.cpp -o fuzzer`, then run `./fuzzer` until it finds and saves a crashing input in a `crash-*` file. Debugging: run `gdb ./fuzzer`, set a breakpoint inside `calculate_token` (e.g. at the line computing internal_hash or right before the null-pointer write), run with the crash file as argument (`run crash-<hash>`), and once stopped use `print internal_hash` (and `print/x` for hex) to read its actual value rather than computing it by hand from possibly-wrong assumptions. Write report.txt with the three requested lines using the values you actually recovered/observed from git and gdb, not derived independently.