Start by transcribing `/app/incident_report.wav` (e.g. with `whisper` or another available ASR tool) to extract the exclusion rules the audio specifies — do not assume rules; read them from the transcript output. Then inspect `/app/sample_backup_meta.db` with `sqlite3 .schema` to confirm the exact column names/types across `storage_nodes`, `backup_jobs`, and `job_chunks`, and how they join (node_id, job_id foreign keys). Write `reconcile.py` as a single SQL query using CTEs: first filter `backup_jobs` to only rows meeting the success criteria uncovered from the audio (status/hash conditions) and exclude nodes matching the region rule from the audio, then use `ROW_NUMBER() OVER (PARTITION BY node_id ORDER BY completed_at DESC)` to pick each node's latest qualifying job, join to `job_chunks` grouped by job_id summing `bytes_size` (using COALESCE/LEFT JOIN so nodes with zero chunks still appear with 0 bytes), and finally output node_id,node_name,latest_job_id,total_backup_bytes sorted by node_id ascending via `csv.writer` on stdout with zero debug prints. Because a fuzz harness compares byte-for-byte against a hidden reference across thousands of random DBs, test your script against several hand-built edge-case databases (nodes with no successful jobs, ties on completed_at, dangling chunks) before trusting it.