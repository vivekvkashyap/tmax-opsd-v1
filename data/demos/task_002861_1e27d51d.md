Design `/home/user/process_docs.sh` as a sequence of discrete, verifiable stages. (1) `mkdir -p /home/user/workspace && tar -xf /home/user/docs.tar -C /home/user/workspace`. (2) For symlink safety, iterate every symlink under workspace (`find /home/user/workspace -type l`), resolve its absolute target with `readlink -f`, and delete the link if that resolved path does not start with the workspace's own realpath — this correctly handles both absolute-path escapes (e.g. `/etc/passwd`) and relative `../../` traversal escapes, while leaving links that stay inside intact. (3) For every remaining regular file, use `file --mime-type -b` to classify; anything not `text/*` moves to `/home/user/assets/`, appending `.bin` only if the filename currently has no extension. (4) For files still classified as text, check `file --mime-encoding -b`; if it reports `iso-8859-1`, convert in place with `iconv -f ISO-8859-1 -t UTF-8` into a temp file then move it back. (5) Rename remaining text files' extensions to `.md` (strip any existing extension first). (6) Build the manifest by finding regular files (`-type f`, excluding symlinks) under both target directories, computing `sha256sum`, formatting as `path | hash`, and sorting by path before writing.