Write `/home/user/curate.sh` to recursively find `.tar.gz` and `.zip` files under `/home/user/incoming_artifacts` (`find ... \( -name '*.tar.gz' -o -name '*.zip' \)`). For each, check integrity first — `tar -tzf file >/dev/null 2>&1` or `unzip -t file >/dev/null 2>&1` — and if that fails, move it straight to `/home/user/quarantine/` and log a CORRUPT line, skipping further processing. For valid archives, extract to a scratch temp directory (`mktemp -d`), then look for either an executable/no-extension file that `readelf -h` succeeds on (parse the "Machine:" line, replace internal spaces with underscores) or a `.gcode` file (grep for the exact line pattern `; filament_type = <TYPE>` and capture `<TYPE>`). Use the extracted machine/filament value to build the destination directory path under `/home/user/curated_artifacts/ELF/<machine>/` or `.../GCode/<type>/`, `mkdir -p` it, then `mv` the original archive (not the extracted contents) there. Append one log line per archive to `/home/user/curation.log` in the exact `[<STATUS>] <filename> - <TYPE> - <METADATA>` format, then clean up the temp extraction directory each iteration. Finally, run the script once so the described final state actually exists on disk.