Fix the three layers bottom-up and test each before moving to the next. In `rust_engine/src/lib.rs`, the borrow-checker error comes from holding a mutable reference (`s_ptr = &mut seq`) while also indexing/reading `seq` directly in the same expression — resolve it by reading the two prior values into local variables (or using `seq[i-1]` and `seq[i-2]` directly without an extra `&mut` alias) before pushing, so there's no simultaneous mutable+immutable borrow; keep the actual recurrence math unchanged, then `cargo build` to confirm it compiles and locate the resulting `librust_engine.so` under `target/debug/`. In `server.py`, the version check `data.get("version") < "2.1.0"` does lexical string comparison, which misorders "10.0.0" before "2.1.0" — fix it by parsing both into comparable tuples of integers (split on `.`, cast each segment to `int`) or use the `packaging.version.Version` class, then compare properly; confirm the `ctypes.CDLL` path to `librust_engine.so` matches where cargo actually placed it. In `test_client.py`, use the `websockets` library to connect to `ws://localhost:8765`, send the exact JSON payload with `n=20` and version `"10.0.0"`, await the response, parse the `result` field, and write only that integer to `/home/user/result.txt`. Start the server in the background first and test the client against it interactively before finalizing.
