Work incrementally: first get the project building at all. Inspect the `Makefile` and the module's directory layout (`go.mod`, actual source paths) — a common misconfiguration is a `go build` invocation pointing at a `cmd/...` subpath that doesn't exist; fix the target path or simplify to `go build -o math-service .` matching the real source layout. Once it builds, profile for the leak: look for a long-lived global cache (e.g. a package-level `map[string][]int]` or similar) that stores computed results (such as prime-factorization arrays) keyed by input but is never evicted or bounded — this is the classic unbounded-cache memory leak in Go. Fix it by either bounding the cache with an LRU eviction policy, adding a TTL/clear step after each use, or removing the cache and recomputing when cheap. After fixing, rebuild to `/home/user/math-service-fixed`, then load-test it yourself (send many repeated and varied requests to its endpoint while sampling process RSS, e.g. via `/proc/<pid>/status` or `ps`) to confirm memory plateaus rather than grows linearly, before considering the task done.