In your C program, parse `config_log.txt` by scanning for `[UPDATE]` ... `[END]` blocks and keep only the last one encountered (don't stop at the first — the file order means later blocks appear further down, so track the most recent match as you scan to EOF rather than breaking on the first hit). Within that final block, collect the filenames listed after `Modified:` (lines starting with ` - `). For each filename, compute its SHA256 either by shelling out with `popen("sha256sum ...")` and parsing the output, or via a linked crypto library — output format must exactly match `sha256sum`'s `<hash>  <filename>` (two spaces, basename only). Critically, implement atomicity correctly: `fopen`/write to `/home/user/manifest.txt.tmp`, `fclose` it, then call `rename("/home/user/manifest.txt.tmp", "/home/user/manifest.txt")` — a naive approach that writes directly to the final path isn't atomic and won't satisfy the constraint check (`grep "rename(" archiver.c`). After the manifest is written successfully, build the tarball with the modified files at the archive root (not nested under `configs/`), e.g. `tar -czf /home/user/incremental.tar.gz -C /home/user/configs file1 file2`.