Use `ffmpeg -i /app/docs_walkthrough.mp4 -vf fps=1 -pix_fmt gray frame_%04d.pgm` (zero-padded so alphabetical order equals frame order) to get raw grayscale frames. In extractor.c, read each PGM's header to skip to pixel data, then for each pixel byte extract `byte & 1`, accumulate 8 such bits MSB-first into an output byte, and append to a growing buffer written to recovered.tar — do this across all frames in filename order without assuming a fixed image count, and don't worry about trailing bits/garbage past the tar's actual end since tar tolerates trailing zero padding. After `tar xf recovered.tar -C docs_raw`, walk the tree with something like `find docs_raw -type l` and for each symlink check with `readlink` whether it resolves to an ancestor directory (self-referential loop) — delete only those, not legitimate symlinks. For nested `*.tar.gz` files, extract each into its own containing directory (`tar xzf X.tar.gz -C $(dirname X)`) then remove the archive. Convert `.txt` files with `iconv -f ISO-8859-1 -t UTF-8` (find all `.txt`, iconv into a temp file, then overwrite). Finally `tar czf /home/user/clean_docs.tar.gz -C /home/user docs_raw`. Verify structurally against your own extracted copy before finalizing, since correctness is judged by matching file contents/paths to a hidden ground-truth directory.