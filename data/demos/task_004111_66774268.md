Separate the two deliverables and validate each independently before assuming the whole thing works end-to-end. For Part 1, edit `/home/user/nginx/nginx.conf` to add/fix a `location / { proxy_pass http://127.0.0.1:5000; ... }` block, then reload/restart nginx; edit `/home/user/app/run_flask.sh` to export `POSTGRES_URI` and `MONGO_URI` pointing at the given host/port/db/credentials before it launches `app.py`, then restart the Flask process so the env vars actually take effect (env vars set after process start won't apply). Test the chain incrementally: curl Flask directly on :5000 first, then curl through nginx on :8080, comparing responses, before considering it wired correctly. For Part 2, model `sql_relations` as graph nodes and each `join_conditions` entry as parsing out the two `table.column` sides via a regex/split on `=` and `.` to get an edge between the two tables named; build a union-find or simple BFS/DFS over these edges, and a query is ACCEPT only if starting from any one relation you can reach all others (or there's only one relation) — REJECT if any relation is unreachable from the rest, meaning multiple connected components. Run your script against every file in `/home/user/corpora/clean/` and `/home/user/corpora/evil/` yourself and confirm 100% correct ACCEPT/REJECT before finishing, rather than trusting the logic on inspection alone.