Tackle the four pieces in dependency order and verify each before moving on. First, the `wrk` build failure: inspect `/app/wrk/Makefile`'s `LIBS` line — a missing math-library linkage causes undefined-reference errors on functions like `exp`; add `-lm` back into `LIBS` and rerun `make` in `/app/wrk` until `wrk/wrk` links cleanly. Second, the Docker Compose 502: since `nginx` and `api` sit on separate `front-tier`/`back-tier` networks, either put both services on a shared network or add the `back-tier` network to the `nginx` service (bridging networks in `docker-compose.yml`) so Nginx's `proxy_pass` can resolve `api` by service name; bring the stack up with `docker compose up -d` and confirm `curl localhost:9090` returns content before continuing. Third, set up the bare repo (`git init --bare /home/user/git/api.git`) and a `post-receive` hook script that does `git --work-tree=/home/user/deploy --git-dir=/home/user/git/api.git checkout -f` followed by `cd /home/user/deploy && docker compose up -d --build`; commit and push the fixed compose file from a clone to trigger it. Finally, background an SSH tunnel (`ssh -f -N -L 8080:127.0.0.1:9090 user@localhost`) and confirm end-to-end traffic and `wrk` throughput on port 8080 before finishing.