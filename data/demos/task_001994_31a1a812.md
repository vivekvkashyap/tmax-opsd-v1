Work top-down through dependencies: first recover the equation's coefficients by running OCR (e.g. `tesseract /app/coefficients.png`) on the image and parsing out A, B, C from the recognized text — verify the OCR output looks sane (three numeric values) before hardcoding them into server.cpp. Second, fix the Makefile: with static libraries, link order matters — object/source files that call into a static archive must appear before the archive on the g++ command line, and httplib needs `-pthread` (and typically `-lssl -lcrypto` if built with SSL support) for its threading/socket symbols to resolve; reorder and add flags until `make` succeeds. Third, implement the `POST /compute` handler: parse the incoming JSON body for `x`, compute `f(x) = A*sin(x) + B*cos(x) + C*x^2` using the extracted coefficients, and return `{"result": <value>}` with correct JSON number formatting. Fourth, write a minimal self-contained nginx.conf (own `pid`, `error_log`, `access_log`, `events {}` block, and a `server` block proxying `/` to `127.0.0.1:9090`) so it can run without root. Finally launch both processes and leave them running; sanity-check by curling `/compute` locally through the proxy before considering it done.