Since the grader recompiles a canonical C implementation and diffs the output value, your program must match its arithmetic exactly, not just be "statistically correct" — implement it literally rather than optimizing or restructuring the math. Parse the CSV skipping the header, discard any row containing `?` in either field. Compute centroid as plain arithmetic mean of X and Y over valid rows. Compute each point's Euclidean distance to the centroid, then mean and population standard deviation (divide by N, not N-1) of those distances — use this exact divisor since it changes the outlier cutoff. Drop points where distance > mean + 2.0*std, keeping the rest in original order. Call `srand(42)` exactly once, then run exactly 10000 bootstrap iterations, each drawing N indices via `rand() % N` (in that order, one call per sampled index), building a resampled array, and computing Pearson correlation on it (0.0 if either variable's variance in the sample is zero). Average all 10000 correlations and write with `%.4f` to `/home/user/robust_corr.txt`. Compile with `gcc -lm`, run it, and inspect the output file before considering the task done.
