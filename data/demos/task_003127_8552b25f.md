Split this into two independent checks. For Phase 1, iterate every PEM file in `/home/user/certs/` and use `openssl x509 -enddate -noout -in <file>` (or `-checkend 0`) to determine which one is already past its expiration relative to the current system date; then pull that same file's Subject CN with `openssl x509 -subject -noout -in <file>` and write just the CN value to `/home/user/expired_cert.txt` — don't guess from filenames, actually check the date programmatically since certs can be misnamed. For Phase 2, write a bash script that greps `/home/user/access.log` for the token following `Bearer ` (careful with the regex so you capture the full three-dot-separated JWT and not trailing quotes/text), then for each token base64url-decodes the header segment (JWTs use base64url without padding, so translate `-_` to `+/` and pad with `=` before using `base64 -d`, or use `base64 -di` with appropriate flags) and checks if the decoded JSON contains `"alg":"none"` or `"alg": "none"` (allow for the optional space). For every match, base64url-decode the payload segment and extract the `sub` field value (e.g. with `jq -r .sub` or `grep -o`/`sed`), then output unique, alphabetically sorted subjects, one per line, to `/home/user/vulnerable_subjects.txt`. Make the script executable and actually run it to produce the final output file.
