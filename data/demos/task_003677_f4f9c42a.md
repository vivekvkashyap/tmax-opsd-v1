First get the vendored package installable: read `/app/ml_artifact_tracker-0.1.0/setup.py` for an overly strict/nonexistent pinned dependency version in `install_requires` and relax it (e.g. drop the exact version pin or use a real available version), then search the package source for Python-2-only imports (e.g. a `cPickleimport pickle` and confirm `pip install -e /app/ml_artifact_tracker-0.1.0` succeeds and the module imports cleanly. For the detector script, parse each JSON file defensively: wrap parsing in try/except so malformed JSON is treated as corrupted rather than crashing the whole run; check `metrics.loss`/`metrics.accuracy` for existence, numeric type, and NaN (use `math.isnan`); locate `model_size_mb` by checking both the root and an `artifacts` sub-object; verify it's an int or a float with no fractional part. Reject on ANY missing/invalid field rather than assuming defaults. Collect `artifact_id` only for fully-passing records, sort alphabetically, and write one per line to the `--output` path. Test against your own small clean/corrupted JSON samples before relying on the hidden corpora — build the script to be strict, since a single overly-lenient check will leak evil samples through.
