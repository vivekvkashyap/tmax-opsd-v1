Treat the Go program as a PTY-driven expect script, not a simple pipe: `os/exec` with plain `Cmd.StdoutPipe`/`StdinPipe` will deadlock or miss prompts because the mock's `read -r pass` line has no trailing newline flush behavior you can rely on via buffered readers — read the child's stdout incrementally (e.g. a `bufio.Reader` over the pipe, watching for substrings as bytes arrive) rather than waiting for a full line. Start the command, stream-scan stdout for the rejection string and, on match, immediately open (create if needed) `/home/user/alerts.log` and append the single required JSON line, then keep scanning for the `"password: "` prompt (note: no trailing newline is echoed after it, so line-based reading won't trigger) and write `probe_pass\n` to stdin at that point, then wait for the process to exit so it doesn't hang. For deploy.sh: use `go build -o /home/user/probe_ssh /home/user/probe_ssh.go`, then create the log file only if absent (`touch` guarded by `[ -f ]` or just `touch` since it's harmless), and `chmod 600` unconditionally — verify idempotency by mentally running the script twice and confirming no step errors or changes state differently the second time (exit 0 both times).
