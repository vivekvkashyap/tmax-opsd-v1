For `init_mount.sh`, use `mkdir -p` (naturally idempotent) and guard the file creation with a size check before writing, e.g. `if [ ! -f data.bin ] || [ "$(stat -c %s data.bin 2>/dev/null)" != "4096" ]; then dd if=/dev/zero of=data.bin bs=4096 count=1 2>/dev/null; fi` so re-running never errors or changes the end state; chmod the script `+x` and test by running it twice, checking exit code 0 both times. Write `custom_fstab` with exactly the one specified line, no trailing blank line surprises — verify with `cat -A` if unsure about hidden whitespace. For the Rust exporter, scaffold with `cargo new capacity_monitor`, use `std::net::TcpListener::bind("127.0.0.1:9090")` and loop over `listener.incoming()`, for each connection use `std::fs::read_dir` on `/home/user/mock_mount` summing `entry.metadata()?.len()` for direct children only (skip subdirectories), write `format!("BYTES:{}\n", total)` to the stream, then drop/close it; build with `cargo build --release` and confirm the binary path. Finally write the systemd unit with `[Unit] After=network-online.target mount-prep.service`, `[Service] ExecStart=<binary path>`, and `[Install] WantedBy=default.target` — do not attempt to actually start the service, just create the file.