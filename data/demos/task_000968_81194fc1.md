Work through the pipeline in dependency order and test each stage in isolation before wiring them together. First, compile `main.cpp` alone with `g++ -Wall` and run it under a debugger or with sanitizers (`-fsanitize=address`) to locate the crash — a common cause is an off-by-one loop bound that reads one past the string's end (e.g. `i <= s.length()` instead of `i < s.length()`), or a stray null-pointer dereference; fix the bounds/logic so exactly-one-`@`-emails pass and others are dropped, matching the input file's actual content. Second, write `backup.sh` standalone with `set -e`, accept `$1` as the source dir, `mkdir -p` the backup directory, and `tar -czf` the archive; test it manually and confirm `tar -tzf` succeeds. Third, open `ci_daemon.sh` and look for shell syntax errors (e.g. a missing `then` after an `if`) — fix with `bash -n` to check syntax, add `set -e` at the top so any failing step (compile, run, backup) aborts before the "success" email is written. Only after each piece works alone, run the full daemon end-to-end and verify all three output files.
