Treat this as three separable pieces built and tested independently before wiring together. First replace the buggy Levenshtein body with the standard textbook dynamic-programming table: a (len(a)+1) x (len(b)+1) matrix where dp[0][j]=j, dp[i][0]=i, and dp[i][j] = dp[i-1][j-1] if characters match, else 1+min(deletion, insertion, substitution); operate on runes, not bytes, to stay correct for non-ASCII input. Validate it against known distances (kitten/sitting=3, empty/abc=3, identical strings=0) in a real `_test.go` file before touching the server. For the HTTP handler, decode the JSON body with `json.NewDecoder`/`json.Unmarshal` into a struct with required string fields, and return 400 explicitly (via `http.Error` or `w.WriteHeader(400)`) whenever decoding fails or a field is empty — don't let a panic or default 200 slip through. For the e2e script, start the server with `go run . &`, poll (e.g. loop on curl or a short sleep) until it accepts connections rather than a fixed short sleep that might race, issue the POST with curl, write the raw response body exactly to the target JSON file, then kill the background PID and verify it's gone. Run `go test -v ./...` and redirect output to the required log so "PASS" is captured.