Do each cleanup phase separately and verify before moving to the next, since later steps depend on earlier ones being correct. Create both destination dirs with `mkdir -p` first. To find broken/looping symlinks, use `find /home/user/raw_dataset -xtype l` (this correctly flags both dangling links and symlink loops without infinite recursion) and `rm` only those, leaving other symlinks (like one pointing at a real file) untouched — test this on the loop pair before running it broadly. For archive verification, `find` all `*.zip` and `*.tar.gz` files, run `unzip -t <file>` / `tar -tzf <file>` and check the exit code; on failure `mv` the archive into corrupt_archives/, otherwise leave it in place. For JSON->CSV, use `jq -r` to emit a header line `id,value` plus one CSV row per array element (e.g. `jq -r '["id","value"], (.[] | [.id,.value]) | @csv'`), writing the .csv alongside each .json with the same basename. Finally, `find` every .csv (both original and newly converted) under raw_dataset, and for each one compute its containing directory's basename, then `ln <src> /home/user/clean_dataset/<dirname>_<filename>` to hard-link (not copy) it — double check the naming rule for files directly under raw_dataset itself (parent dir name is `raw_dataset`). Finish with the exact `ls -1 | sort >` manifest command.