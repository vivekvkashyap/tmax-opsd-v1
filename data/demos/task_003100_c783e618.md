Build the pipeline incrementally and inspect intermediate state at each stage rather than writing it all at once. First load `/home/user/products.csv` with pandas, then validate row-by-row (or via `pydantic`'s `parse_obj`/model) against a model requiring `product_id: int`, `text: str`, `label: str` — rows with a non-integer product_id or a null/None text field should raise a validation error and be dropped (catch the exception per-row and collect only the valid ones, rather than letting pydantic reject the whole batch). Fit `TfidfVectorizer(max_features=500, stop_words='english')` on the validated `text` column, then `TruncatedSVD(n_components=20, random_state=42)` on the resulting sparse matrix to get dense features. Wrap `LogisticRegression(random_state=42)` in `GridSearchCV(estimator, param_grid={'C':[0.1,1.0,10.0]}, cv=3)`, fit on the reduced features against `label`, and after fitting read `grid.best_params_['C']` and `grid.best_score_`. Write both to `/home/user/model_results.json` with `json.dump`, then actually execute the script (`python3 /home/user/pipeline.py`) to produce the file — don't just write code that would work in theory.