Follow the pipeline stages in order and validate each before moving on, since a mistake early (a bad sample or misaligned features) will silently tank the final R^2. Sample exactly 500 rows from `/app/data/unlabeled_features.csv` with a fixed seed, write them to a temp headerless CSV, and call `/app/bin/scorer <path>` via `subprocess`, capturing one float per line as target `y`. For each of the 20 features, bootstrap-resample the 500 (x_i, y) pairs 1000 times, compute Pearson correlation each time (`scipy.stats.pearsonr` or `np.corrcoef`), and take the 2.5th/97.5th percentiles as the CI — write these to `/home/user/feature_correlations.json` keyed by feature name. Select features whose CI excludes zero, order them by original numeric index (sort by the integer suffix, not selection order), and build the training matrix accordingly. Fit `sklearn.linear_model.LinearRegression()` on this reduced set, then `joblib.dump` to `/home/user/surrogate_model.pkl`. Trust the bootstrap test to reveal which features carry real signal rather than guessing which columns matter.
