Fix the leakage bug first: in `train.py`, locate where `PCA().fit(X)` (or fit_transform on the whole feature matrix) happens before `train_test_split`, and reorder so you split raw TF-IDF features into train/test, then `pca.fit(X_train)` only, then `pca.transform()` on both X_train and X_test — this is exactly what the grader checks for by inspecting the code, so keep the fit call visibly scoped to the training set. Save `vectorizer.pkl`, `pca_model.pkl`, `classifier.pkl` to `/home/user/workspace/models/` via pickle/joblib. For transcription, install `openai-whisper` and `ffmpeg`, run whisper on `/app/sample_audio.wav`, and feed the transcript through the same vectorizer→pca→classifier chain for `benchmark.py` (time 100 loop iterations, average, write the float to `benchmark.log`). Build `detector.py` as a CLI taking one wav path argument, doing transcribe→vectorize→pca-transform→predict, printing exactly `CLEAN` or `EVIL` (no extra text) based on class 0/1. Since a hidden corpus of evil (injection-style) and clean phrases will be tested, make sure your training data and TF-IDF vocabulary genuinely capture injection-language patterns rather than overfitting to one phrasing — test your detector locally against a few adversarial phrasings you construct yourself before finishing.
