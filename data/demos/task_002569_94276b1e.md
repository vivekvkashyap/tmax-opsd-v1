Start by inspecting the schema of `/home/user/ecommerce.db` (`.schema` in the `sqlite3` CLI or `PRAGMA table_info`) to see the actual foreign keys (`orders.user_id`, `order_items.order_id`, `order_items.product_id`) and filter columns (`products.category`, `orders.order_date`). Write `/home/user/optimize.sql` creating indexes on those join/filter columns (e.g. `orders(user_id)`, `order_items(order_id)`, `order_items(product_id)`, `products(category)`, and `orders(order_date)` — at least 4 total) and apply it with `sqlite3 ecommerce.db < optimize.sql`. For `/home/user/export.py`, read `category`, `start_date`, `end_date` from `sys.argv`, and build the query with `?` placeholders and a parameters tuple passed to `cursor.execute(query, params)` — never interpolate the arguments into the SQL string, since that's the injection vector being tested. Join `order_items` → `orders` → `users`, and `order_items` → `products`, filtering `products.category = ?` and `orders.order_date BETWEEN ? AND ?`. Build the result as a list of dicts with keys exactly `user_name, email, order_date, product_name, quantity`, sort by `(order_date, product_name, user_name)` ascending in Python or via `ORDER BY` in SQL, and `json.dump` to `/home/user/output.json`. Run it with the specified example arguments to confirm it produces sensible output before finishing.