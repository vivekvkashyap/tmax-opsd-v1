Treat this as two separate, independently-verifiable fixes plus a service. First, the build: inspect the Makefile's CFLAGS/LDFLAGS — if it uses OpenMP pragmas in the source but the compiler flags don't enable OpenMP, add the missing compiler+linker flag so `#pragma omp` directives actually parallelize instead of being silently ignored; rebuild and confirm `bin/nano_align` exists and threads are used (e.g. check with `OMP_NUM_THREADS` env var affecting behavior, or nm/ldd showing libgomp linked). Second, the solver bug: locate `adapt_step_size` in solver.c and reason from first principles about adaptive-step ODE integrators — when the local error EXCEEDS tolerance, the step size must be DECREASED (not increased) so the next attempt is more accurate; increasing it in that branch is what drives the integrator to diverge into NaNs. Fix the arithmetic so error > tol shrinks dt, then rebuild and sanity-test on a noisy signal to confirm no NaNs appear. Third, write the Python scoring script and HTTP service: call the compiled `nano_align` binary as a subprocess against ref_H0.fasta and ref_H1.fasta, parse its log-likelihood output, compute H1 - H0, and classify variant=true only if that margin is strictly greater than 5.0. Wire this into a backgrounded server on 127.0.0.1:9090 checking the `X-Nano-Auth` header before processing, and test it yourself with curl before finishing.