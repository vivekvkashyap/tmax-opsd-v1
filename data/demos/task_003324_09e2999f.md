Start by reading `/home/user/sensor_calc_old.c` to see the naive formula it uses (likely `variance = sum(x^2)/n - (sum(x)/n)^2`, which loses precision catastrophically when values share a large common base). Then probe the oracle directly: run `/app/sensor_calc_oracle` on both small/spread-out inputs and on large-base, low-variance inputs (e.g. `1e8+0.1 1e8+0.2 1e8+0.3`) and diff against your own naive C reimplementation to see exactly where and how they diverge — this confirms the instability and gives you real target strings to match. Capture the oracle's exact stdout for a few cases (`./sensor_calc_oracle 1 2 3` etc.) to nail the precise formatting (field labels, decimal places, newline placement) rather than guessing it. To fix the instability, replace the two-pass sum-of-squares formula with a single-pass, numerically stable running mean/variance update (updating mean and a second-moment accumulator incrementally per new sample, then dividing by count-1 for sample variance) — this is the standard fix for this exact catastrophic-cancellation failure mode. Match the oracle's Count/Mean/Variance formatting and edge-case behavior (n<2, n=0) exactly, with no extra output, then compile silently to `/home/user/sensor_calc` and diff outputs across many argv combinations before finishing.
