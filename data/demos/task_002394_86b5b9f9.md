Treat this as three independent phases and verify each before moving to the next. Phase 1: locate log files with `find /home/user/legacy_project -name '*.log'`, then edit each in place with a regex that matches standard email patterns (e.g. `[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}`) via `sed -E` or Python `re.sub`, replacing matches with the literal string `[REDACTED]` — test the regex against a sample line first so you don't over- or under-match. Phase 2: write a Python script that opens each file in `assets/` in binary mode, reads exactly the first 4 bytes, compares against the ELF (`\x7fELF`) and PNG (`\x89PNG`) magic byte sequences, creates the three destination directories with `os.makedirs(exist_ok=True)`, and moves (not copies) files accordingly so `assets/` ends up empty. Phase 3: for the tar snapshot workflow, run `tar --create --file=backup_full.tar --listed-incremental=project.snar legacy_project/` for the full backup, then after adding `update.txt`, rerun the identical command with `--file=backup_inc.tar` and the same `.snar` file — tar automatically detects it's an incremental run from the snapshot state.
