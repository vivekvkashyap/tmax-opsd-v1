Build this as a script first, verify it numerically in a plain .py file, then transplant the working code into notebook cells and execute the notebook — debugging inside a notebook via nbconvert is slow and error-prone. Load both latency files with numpy. For the bootstrap: call `np.random.seed(42)` immediately before the resampling loop, then for each of 10,000 iterations draw a resample (with replacement, same size as original) from each dataset, compute `median(B_resample) - median(A_resample)`, and take the 2.5th/97.5th percentiles of the resulting distribution as your CI bounds — get the seed placement and iteration order exactly right since order-of-random-calls affects reproducibility. For the MCMC step: implement Metropolis-Hastings from scratch (no external MCMC library) with an Exponential likelihood, a Normal(current, sigma=2.0) proposal, uniform prior on [0,200], starting mu=50.0, 10,000 total steps with the first 2,000 discarded as burn-in, seeding with 100 for config A and 200 for config B right before each respective run. Compute log-likelihood ratios and accept/reject with `log(uniform(0,1)) < ratio` to avoid overflow. Write results to `/home/user/results.json` with exactly the specified keys, save the two-panel plot to `/home/user/posterior_plot.png`, then run `jupyter nbconvert --execute --inplace` on the notebook so the output files actually get written to disk.