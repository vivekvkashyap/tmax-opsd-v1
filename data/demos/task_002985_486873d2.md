Recover the filtering rule from the audio before writing any Rust code, and check it against the provided corpora as ground truth. Transcribe `/app/dba_instructions.wav` (e.g. with `whisper-cli`/`openai-whisper`, or `ffmpeg` to extract audio plus a speech-to-text tool available in the environment) and read the resulting text carefully to extract the precise rejection rule described — cross-reference your interpretation against a couple of files in `/app/corpus/clean/` and `/app/corpus/evil/` to make sure your rule actually classifies the given examples correctly before generalizing. Then scaffold the Rust project with `cargo init /home/user/cypher_filter`, read the query file passed as the single CLI argument, and implement the check as a text/regex analysis of the query string (look specifically at how MATCH clauses list nodes — the distinguishing signal is structural, not about individual node syntax). Exit with the two required status codes based on the classification. Build with `cargo build --release`, then run your binary against every file in both corpus directories locally and confirm the accept/reject pattern matches expectations before considering the task done, since the hidden test set will apply the same rule.