Work the three recoveries independently, each verifiable on its own. To find `SECRET_COEFF`, don't try to parse the whole binary dump as text — grep for the literal marker string directly against the raw bytes: `grep -a -o 'SECRET_COEFF=[0-9]*' /home/user/memdump.dat` (the `-a` flag is essential since the file is otherwise binary/non-UTF8 and normal grep may treat it as binary and refuse to match). To build `clean.wal`, filter `data.wal` for lines matching only the strict `TX:<id> VAL:<number>` pattern using a tool that understands byte-level ASCII validity — e.g. `grep -a -P '^TX:\d+ VAL:\d+$' data.wal` or check each line with `LC_ALL=C grep -q '[^ -~]'` to detect any non-printable/non-ASCII byte and discard those lines; don't rely on eyeballing since garbage bytes may look like whitespace in a terminal. For the crash, compile and run the original C program under gdb or with valgrind against a single problematic value to see exactly where it segfaults, then reason about which values of `val` trigger it (test boundary/modulus-related values) and patch the C source defensively rather than just avoiding the trigger. Finally recompile, pipe `clean.wal` through your fixed binary with the recovered coefficient as argv[1], and redirect stdout to the results file — check the output format exactly matches what the program prints per line.
