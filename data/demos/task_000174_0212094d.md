This is a reverse-engineering-then-reimplementation task, not a from-scratch design. Start with `strings /app/traffic_oracle` and `file`/`ltrace`/`objdump -d` to see whether it's packed (check for UPX signatures — if so, `upx -d` it first so disassembly is meaningful) and to recover literal strings like usage/error messages, which usually reveal the exact control flow and exit codes. Trace behavior empirically: run the binary with varied argc, varied-length arguments, non-numeric prefixes, valid-looking 4-digit prefixes, and payloads containing likely XSS/SQLi markers, recording exact stdout, stderr, and exit code for each — this black-box behavior is your real spec, more reliable than reading disassembly alone for the exact digit-hash formula (step through the digit-processing loop in gdb/objdump to recover the precise arithmetic and modulus, since guessing at hash constants won't match by chance). Reproduce the observed branch order faithfully in C (argc check, length check, digit-format check, hash-based auth check, then substring checks for the injection signatures) and pin down the exact output strings and newline placement character-for-character. Compile exactly as instructed and re-test your binary against the same varied inputs you probed the oracle with before considering it done, since the fuzzer will compare byte-for-byte.
