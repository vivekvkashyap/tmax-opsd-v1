Build this as a two-step SQL-then-Python aggregation rather than trying to do everything in one query. First, use a parameterized query (`WHERE citations >= ?` with the CLI's `--min-citations` value bound as a parameter, not string-interpolated) to select only qualifying papers from `papers`. Then, for each qualifying paper, fetch its co-authors from `paper_authors`, and for every unordered pair of authors on that paper accumulate `weight += paper.citations` in a dict keyed by `(min(a,b), max(a,b))` — this naturally enforces the `source < target` ordering requirement and merges contributions across multiple shared papers. Collect the set of author ids that appear in any edge to determine the node list (only authors who co-authored on a qualifying paper, not all authors in the table), and look up their names via a join or a separate query. Finally sort nodes by id and edges by (source, target) before serializing to `/home/user/coauthorship_graph.json` in the exact `{"nodes":[...], "edges":[...]}` shape. Watch for the "strictly fewer than" wording — the citation filter must be `>=`, and pairs must exclude self-pairs and papers with a single author.
