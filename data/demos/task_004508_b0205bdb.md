Fix the circular include first: `parser.h` and `validator.h` include each other, so break the cycle with a forward declaration (`class Validator;` in `parser.h` instead of `#include "validator.h"`, keeping the real include only in the .cpp file that needs the full definition) rather than merging the headers or removing a class. Confirm `make` succeeds before touching logic. In `validator.cpp`, implement checksum validation by parsing `payload_hex` two hex characters at a time into bytes, XOR-folding them together, and comparing against the parsed `checksum_hex` byte. For rate limiting, keep a per-`user_id` history of timestamps (the existing `user_requests` map is set up for this); for each new request count how many prior stored timestamps fall in the half-open window `(timestamp - 1.0, timestamp]` — get the boundary inclusivity exactly right — and flag `RATE_LIMITED` if that count is >= 3, checking checksum failure first so it takes priority. Append every request's timestamp to history regardless of its status, then write the 5-column CSV via `writeFile`.
