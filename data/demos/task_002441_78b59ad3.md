First extract the 5 scene boundaries from `/app/loc_reference.mp4` empirically rather than assuming durations — use `ffmpeg`'s scene-change detection (e.g. `ffmpeg -i loc_reference.mp4 -vf "select='gt(scene,0.3)',showinfo" -f null -` and read the `pts_time` values from the showinfo log, or `ffprobe` frame analysis) to get precise cut timestamps in milliseconds, then convert to `HH:MM:SS,mmm` SRT format. For `/home/user/loc_render.go`, since output must be bit-for-bit identical to a hidden oracle across randomized fuzz tests, be exact about: JSONL decoding of one object per line via `bufio.Scanner` + `encoding/json`; grouping by `screen_id` into a map, then iterating screens 1..5 in fixed numeric order (not map iteration order, which is random in Go); sorting each screen's records by `key` using `sort.Slice` with plain byte/string comparison; injecting the literal `{"key":"DEFAULT","translation":"Missing"}` record only for screens with zero input records; and formatting each SRT block with index numbers starting at 1, the exact timestamp range for that screen, the literal header line `Screen <ID>:`, then `- <Key>: <Translation>` lines, using SRT's required blank-line separation between blocks. Test manually against a few hand-crafted JSONL inputs, including ones that omit screens entirely, before trusting the fuzz suite.