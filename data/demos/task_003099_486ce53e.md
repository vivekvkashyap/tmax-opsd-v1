This task has two independent deliverables graded separately — do both fully.

For Part 1 (video), use `ffmpeg -i /app/surveillance.mp4 -vf fps=1 -q:v 2 /home/user/frames/frame_%03d.jpg` (mkdir the output dir first), then count the resulting files with `ls /home/user/frames/*.jpg | wc -l` and write that integer (no extra whitespace) to `/home/user/frame_count.txt`. Don't hardcode a frame count — derive it from the actual extraction output since video length may vary.

For Part 2 (C sanitizer), design around the two corpora it will be tested against (clean files that must round-trip byte-for-byte, evil files that must be filtered and cause exit 1): use `zlib.h`'s `gzopen`/`gzgets` or `gzread` to decompress directly rather than shelling out to gunzip. Buffer each record between `BEGIN_RECORD` and `END_RECORD` lines (including those markers) as you scan; only after seeing `END_RECORD` decide whether the whole buffered record contains `../` or `MALICIOUS` anywhere across its lines, then either write it verbatim to the temp output or discard it. Track a "found malicious record" flag across the whole file for the exit code. Write to `OUTPUT_FILE.tmp`, `fflush`+`fsync` (or use file descriptors with `fsync`), then `rename()` atomically to `OUTPUT_FILE`. Compile and test against small manual gz fixtures before trusting it against the real corpus.