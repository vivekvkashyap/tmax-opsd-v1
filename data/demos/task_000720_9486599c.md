Treat the compose fix and the audio/sanitizer work as separate tasks. For the compose file, inspect the current networks section — the two services are likely on separate isolated networks, which is why `worker` can't reach `api`; add a single custom bridge network named `edge_net` under `networks:`, and attach both `api` and `worker` services to it (removing or supplementing any networks that isolate them), then bring the stack up with `docker compose up -d` and confirm connectivity with `docker exec` into worker and curling/pinging `api` by service name (Compose's embedded DNS resolves service names on shared networks). For the audio task, transcribe `/app/override_command.wav` with an STT tool to recover the exact secret phrase — read it carefully, don't guess a plausible-sounding phrase. Then write `/home/user/sanitize.py` as an executable script (`#!/usr/bin/env python3` shebang, `chmod +x`) that takes one file path argument, reads its contents, does a case-insensitive substring check for the recovered phrase, and calls `sys.exit(1)` if found or `sys.exit(0)` otherwise — keep the check on raw text content, not word-boundary matching, since the corpus embeds the phrase inside longer sentences.
