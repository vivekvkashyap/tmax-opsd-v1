Build the C++ normalizer first and test it standalone before wiring up services: read all of stdin into a byte buffer (don't assume text, use binary-safe reads e.g. `std::cin` with `std::noskipws` or `read()` in a loop until EOF), write the 4-byte magic `ARTI`, then the input length as a 4-byte little-endian unsigned int (construct it byte-by-byte to avoid endianness bugs, don't rely on struct packing), then stream the payload applying byte-stuffing: emit `0xFE 0x01` for each `0xFF` and `0xFE 0x00` for each `0xFE`, everything else passed through unchanged. Verify correctness with round-trip tests against random binary blobs of varying sizes including empty input and edge bytes 0xFF/0xFE. For the services half, start the stack script first and confirm each service (redis, flask, nginx) is actually listening before writing the watcher — use `inotifywait -m -r -e create` on the incoming directory, pipe each new file through the ingestor binary, and `curl --data-binary @- ` (not `-d`, which mangles binary) to the upload endpoint with the filename as a query param. Background the watcher with nohup/disown and confirm it survives.
