First OCR /app/transition_matrix.png (e.g. `tesseract transition_matrix.png stdout`) to recover the 4x4 transition probability table; inspect the raw OCR text carefully and correct any obvious misreads (confused digits/decimal points) by cross-checking that each state's outgoing row sums to 1.0 before trusting the numbers. Encode the recovered matrix as data inside simulate.sh (e.g. as arrays keyed by state). The core logic: given a current state and a random float r, walk the states in the fixed order S1,S2,S3,S4, accumulating cumulative probability, and transition to the first state where the running cumulative sum exceeds r (i.e. `[lower, upper)` semantics — use "cumulative <= r" to decide *not* yet transitioned, not "<"). Process argv[1] as the initial state and each subsequent argv entry as one random draw, updating current state after each and appending it to the output sequence. Print the full space-separated path (initial state plus one state per random number) as a single line, then `chmod +x` the script. Test it by hand against a couple of inputs where you compute the expected transition manually to confirm the boundary logic is right before considering it done.