Build this as a streaming pipeline, not a script that loads the whole file: first pass each line through `sed` (or `tr`/`perl`-free stream editor) with a regex like `s/\\x[0-9A-Fa-f]\{2\}//g` to strip the invalid hex escapes before any JSON parser sees them — do this on the raw text stream, not after `jq` has already choked. Pipe the cleaned stream into `jq -r '[.timestamp,.user_id,.response_time_ms]|@tsv'` to extract fields safely (handles `null` correctly as a token). Then process line-by-line in `awk`, keeping per-`user_id` state: a "last known valid response time" for imputation (default 0 if unseen), and a small rolling window (last up to 3 raw/imputed values) per user to compute the average — remember to imputed *before* updating the rolling window, and to average over however many of the last 3 values exist so far (1, 2, or 3), not always dividing by 3. Format `response_time_ms` as an integer and `rolling_avg_3` with `printf "%.2f"`, output exactly four comma-separated columns with no header, redirected to `/home/user/analyzed_logs.csv`.