Run `tesseract /app/network_params.png stdout` (try preprocessing with `convert` if OCR is noisy) to recover the edge list and the `Alpha` value — do not guess these. Build the adjacency matrix from the recovered edges, then normalize each row to sum to 1 (rows with zero out-degree get a self-loop probability of 1). Form the regularized transition matrix M = (1-Alpha)*P + Alpha*(1/N) for every entry, where N is the node count. To find the stationary distribution, either solve the linear system pi*M = pi with sum(pi)=1 via Gaussian elimination, or iterate pi_{t+1} = pi_t * M from a uniform start until convergence (power method) — power method is simpler to get right for small N. Then compute KL divergence D_KL(pi || U) = sum(pi_i * ln(pi_i / U_i)) using natural log, with U_i = 1/N. Print only that single float. Write this as a self-contained C++ program at `/home/user/resolve_network.cpp`, compile with `g++`, and run it, feeding in the OCR-derived edges/Alpha as constants or parsed input — verify by hand-checking row sums equal 1 before trusting the KL result.
