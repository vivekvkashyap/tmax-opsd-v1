Part 1: use `awk -F, '$5=="OK" {print $2","$3","$4}' /home/user/sensor_data.csv` (skip the header row) to extract only `val_x,val_y,val_z` for OK rows into `/home/user/clean_data.csv` with no header — double check you're not including the header line or non-OK rows (FAIL/ERROR) by mistake. Part 2: `cargo new data_analyzer` in `/home/user/data_analyzer`, add `ndarray` and `csv` as dependencies in `Cargo.toml`. In `main.rs`, read `clean_data.csv` row by row into a `Vec<f64>` and build an `Array2<f64>` of shape (n_rows, 3), then compute the Gram matrix `G = X^T · X` via `x.t().dot(&x)`. Note the useful shortcut: trace(X^T X) equals the sum of squares of every element of X, so you can sanity-check your matrix-multiply result against a simple element-wise sum-of-squares. Format the trace to exactly 2 decimal places (Rust's `{:.2}` format specifier) and write it to `/home/user/trace.txt`, then `cargo run` to actually generate the file.