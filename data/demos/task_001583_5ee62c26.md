Recursively enumerate archives with `find /home/user/legacy_backups -type f \( -name "*.zip" -o -name "*.tar.gz" \)`, being careful the `.tar.gz` match needs the compound suffix (a naive `-name "*.gz"` would also catch unrelated files). Test integrity per file using the exit status of `unzip -t <file> >/dev/null 2>&1` for zips and `tar -tzf <file> >/dev/null 2>&1` for tar.gz — a nonzero exit means corrupt. On corrupt: `rm` the file and record its basename. On valid: compute `sha256sum` of the file, build the new name `backup_<hash>.<ext>` (careful to strip the correct multi-part extension for `.tar.gz` vs the single `.zip`), `mkdir -p /home/user/clean_backups` once, then `mv` (not copy) the file there under the new name so nothing valid remains in the source tree. Track counts and the two filename lists as you go, then sort each list alphabetically before writing. Emit the final JSON report with a tool that guarantees valid JSON syntax (e.g. Python's `json.dump`, or careful manual construction with proper comma/bracket placement) matching the exact key names and structure specified. Run the whole script once and manually check the directories and JSON afterward to confirm no valid archives remain in the legacy tree and counts add up.