Build this pipeline stage by stage, checking output at each step rather than writing it all at once. First `mkdir -p /home/user/local_logs && cp /tmp/remote_logs/*.txt /home/user/local_logs/`. In the Python script, parse each line by splitting on ` | ` into timestamp/severity/message, and parse the timestamp with a format string matching `DD-MMM-YYYY HH:MM:SS ±HHMM` (Python's `datetime.strptime` with `%d-%b-%Y %H:%M:%S %z` handles the offset directly) — then convert to UTC with `.astimezone(timezone.utc)` and format with `strftime('%Y-%m-%dT%H:%M:%SZ')`. Group parsed records by severity, sort each group by the UTC datetime (not the raw string), and take `math.ceil(len(group) * 0.25)` earliest entries per group — this rounding detail is easy to get backwards (floor vs ceil), so double check on a group with a non-multiple-of-4 size. Merge all sampled records and do a final chronological sort by UTC time before writing the JSON array with the exact three keys specified, using `json.dump` with indentation. Strip whitespace from the message field. Finally write the single crontab line to `/home/user/cron_setup.txt` for minute 45 of every hour, with no extra comments or trailing content, and actually execute the script once to produce the JSON output.
