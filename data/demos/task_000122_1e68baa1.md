This task has three independent deliverables — fix the vendored build, write the validator, set up the git hook — verify each separately. For the vendored package: run `make build` in `/app/vm-user-sync-1.0.0` first to see it fail, inspect the Makefile with `cat -A` to spot spaces where a leading tab is required for the recipe line, fix it, and confirm `make build` succeeds; separately add `After=network.target` under `[Unit]` in `vm_user_sync/templates/user-service.jinja` (verify via the package's own lint entry point once installed in the venv). For the validator, handle symlink escape carefully: use `os.path.realpath()` on each link target and check with `os.path.commonpath` whether it stays under the resolved base directory — don't just eyeball the raw target string, since both relative traversal and absolute-path escapes must be caught. Scan `.ssh_key` files for the literal `command=` substring and `.cron` files for `curl`, `wget`, `nc`, or `bash`. Test against every subdirectory in `/app/corpus/clean/` and `/app/corpus/evil/`, checking exit codes match expectations. Finally, `git init --bare /home/user/provisioning.git` and add an executable `hooks/pre-receive` containing the required placeholder comment.
