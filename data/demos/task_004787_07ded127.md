In `/home/user/analyze.go`, first write a minimal FASTA parser (no third-party libs needed) that reads `/home/user/data/proteins.fasta`, collecting each `>ID` header and its following sequence line(s) concatenated into one string. For each sequence, compute the sum of `abs(int(seq[i]) - int(seq[j]))` over all `i,j` pairs in the full N×N grid (not just i<j — the task wants the complete matrix sum, which double-counts each unordered pair since abs is symmetric, so you can equivalently compute `2 * sum over i<j` if you want an O(N^2/2) shortcut, but computing the full N×N loop directly is simplest and safest to avoid an off-by-factor-of-2 bug). Dispatch one goroutine per sequence, each writing its `(ID, sum)` result into a channel or a mutex-protected map, and use a `sync.WaitGroup` to block until all goroutines finish before proceeding. Sort the collected results alphabetically by SequenceID, then write them to `/home/user/summary.csv` as `ID,Sum` lines with no header row, no trailing extra whitespace. Compile/run with `go run analyze.go` or build then execute, and verify the CSV content afterward.
