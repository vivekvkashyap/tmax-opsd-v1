This is a three-part infra task; solve each part independently and verify before moving on.

1. Video: run `mkdir -p /home/user/frames && ffmpeg -i /app/data_feed.mp4 -vf fps=1 /home/user/frames/out%03d.jpg`. Don't guess the frame count — after extraction, count the actual files written (`ls /home/user/frames | wc -l`) and write that integer to `/home/user/frame_count.txt`.

2. SQL index: the query filters `traffic_logs.timestamp` in a range and joins/groups by `nodes.id`→`region`. Design a composite index on `traffic_logs` covering the filter column first (`timestamp`) plus the join column (`source_node_id`) and the aggregated column (`bytes_transferred`) so the engine can use an index-only scan, plus an index on `nodes(id, region)` for the join/group. Write the exact `CREATE INDEX` statements to `/home/user/indexes.sql`.

3. Graph filter script: parse each `.json` file defensively (catch exceptions/malformed schema and reject rather than crash), build the graph with `networkx.Graph()`, add nodes/edges, then call `nx.degree_centrality(G)` and reject if any value >= 0.80. Only copy files that pass both checks. Test your script manually against a few hand-built clean/malicious JSON graphs before trusting it against the hidden corpora, since the "adversarial" cases include both malformed JSON and legitimate-looking but high-centrality star topologies.