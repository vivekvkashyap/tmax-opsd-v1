Tackle this in layers: get services up, then get the C tool correct against the adversarial corpora, then wire the daemon mode. For nginx, edit /home/user/nginx.conf changing `listen 80` to `listen 8080` and the document root to /home/user/reports (create that directory), then start via /app/start_services.sh and verify with `curl -I http://localhost:8080/`. For the C filter (Mode A), read the CSV line by line, split on commas (metadata may itself need careful handling if it can contain commas — check the corpus samples), and do a case-insensitive substring search (e.g. lowercase both the metadata field and each banned pattern before `strstr`) for `<script>`, `javascript:`, `UNION SELECT`, `onload=`; if any match, drop the whole row, otherwise copy it verbatim to the output. Validate against the clean and evil corpora under /app/corpora/ before moving on — 100% pass is required, so test both directions explicitly. For Mode B (daemon), use hiredis's `redisCommand` with BLPOP on `raw_logs`, apply the same filter function, bucket by `timestamp/60`, and when a new-minute log arrives, sort the just-finalized bucket's records by sensor_id (e.g. qsort/comparator), compute max/min temperature, and regenerate reports/index.html following the exact template given. Compile with `gcc -O2 processor.c -o log_tool -lhiredis` and leave the binary in /home/user/.