Work in three clean stages. First, fix the vendored package before touching Python: open `/app/fast-eval/c_src/Makefile` and check the `CFLAGS`/`gcc -shared` line — shared libraries on Linux require position-independent code, so a missing `-fPIC` will make the link fail with relocation errors; also diff the Makefile's build target filename against exactly what `setup.py` expects to import (`libfasteval.so`) since a subtle typo in the target name will make `pip install` silently produce the wrong artifact. Rebuild with `pip install -e /app/fast-eval` and confirm `python3 -c "import fast_eval; print(fast_eval.compute('ADD',2,3))"` works before writing any server code. Second, implement the chained-expression parser as an explicit left-to-right state machine (regex or split on `|`, extract op+args, feed the running accumulator as the second operand for single-arg ops like `MUL`/`SUB`). Third, build the Flask/FastAPI app separately, test the auth header and JSON body locally with `curl` before considering it "started in background" (use nohup/subprocess, verify with a curl smoke test that the process is actually bound to 127.0.0.1:8080). Keep the benchmark script's timer isolated from HTTP overhead — call the parser and `fast_eval.compute` directly in a tight loop.