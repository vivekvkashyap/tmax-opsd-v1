Model this as three separate joins, not one blended query. Parse each CSV with the `csv` crate into typed structs (City{id,name}, User{id,name,city_id}, Friendship{user1_id,user2_id}). Emit CREATE statements for cities and users sorted by id ascending — never rely on hash-map iteration order, collect into a Vec and sort by id first. For LIVES_IN, iterate users sorted by id and emit one MATCH/CREATE per user using their city_id — this is a direct field lookup, not a join on shared attributes, so do not fall into the original bug of joining on a shared column (city) which produces a cross join. For KNOWS, read friendships.csv directly (it's the ground truth for who actually knows whom) and emit both (u1,u2) and (u2,u1) directions since the file only stores each pair once; sort all KNOWS lines by (user1_id, user2_id) numerically, not lexicographically. Match the exact literal Cypher formatting (quotes, spacing, semicolons) given in the spec. For the friend-of-a-friend query, build an adjacency map from the same friendships data, compute direct friends of Alice, then friends-of-those excluding Alice herself and her direct friends, and cross-check city membership via the users/cities data before writing the single matching name to answer.txt.
