Build this as a pipeline of filters rather than one monolithic regex. First strip the header (tail -n +2 or equivalent). Split on tabs to get id/text_input/model_score, and drop rows where text_input is empty or exactly "NA"/"NaN" (case-sensitive, so don't lowercase before this check), and where model_score is empty — do these checks before any transformation. Validate model_score is numeric and within [0.0, 1.0] inclusive; use awk's arithmetic comparison or a regex that only matches valid decimal numbers so garbage strings like "abc" are rejected rather than silently coercing to 0. For tokenization, lowercase the text, strip everything except [a-z0-9 ] (this also strips punctuation like "!" and "..."), then squeeze repeated spaces to one and trim leading/trailing spaces — order matters: squeeze/trim after character stripping, since removed punctuation can leave extra spaces. Emit id|tokenized_text|model_score with pipe delimiters. A common mistake is trimming before squeezing internal multi-spaces, or applying the numeric-range check as a string comparison instead of a true numeric one. Diff your output against your own hand-derived expectation for each surviving row before finishing.
