Work in three passes. First, fix the crash: inspect `cli.py`, `registry.py`, `decoder.py` for a circular import (e.g. registry importing decoder while decoder imports registry at module load time) — break the cycle by deferring one import (move it inside a function, or restructure so the registry doesn't need the class at import time) and by using package-relative imports (`from .registry import ...`) so it works regardless of module caching order in a fresh container. Second, before touching the decoder, build a tiny local test harness (`test_mock.py`) that feeds a hand-crafted list of RGB tuples (including runs of duplicate frames) directly into the state machine so you can iterate without re-running ffmpeg each time; use it to find where bytes are dropped — a classic bug is failing to flush the last in-progress byte once the frame stream ends, so check `finalize()`/EOF handling emits any pending 8-bit buffer. Third, use `ffmpeg` to extract frames from `/app/signal.mp4`, sample pixel (x=5,y=5) per frame, collapse consecutive identical colors into single symbols, run them through your fixed state machine, and write the resulting ASCII string to `/home/user/decoded_output.txt`.