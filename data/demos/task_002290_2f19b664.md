This is a classic startup-race-condition plus missing-execute-bit problem. First inspect `/home/user/start.sh`: it currently launches `deploy_svc.py` before `state_svc.py`, and since `deploy_svc.py` performs a connectivity check to port 8081 at import/startup time, it dies if that port isn't bound yet — reorder so `state_svc.py` is started first, then insert `sleep 2`, then start `deploy_svc.py`, keeping both backgrounded (`&`) so the script itself can also be run with `&`. Second, check the hook's permission bits with `stat` or `ls -l` on `/home/user/registry.git/hooks/post-receive` — Git silently ignores non-executable hooks, so `chmod +x` it. Verify both services are actually listening (e.g. `curl` or `ss -ltnp`) before proceeding to the git steps. Then `git clone /home/user/registry.git /home/user/workspace`, add `trigger.txt` containing `deploy`, commit, and `git push origin master`. After the push, confirm the hook fired correctly by checking `/home/user/deploy.log` — if it's missing or empty, work backward: check hook execute bit, then check both services' logs/ports, rather than guessing.
