Work in three separate passes rather than trying to fix everything at once. First get it compiling: run `cargo build`, read the exact error (likely a version pin mismatch in Cargo.toml or a type error like using `i32` where a numeric literal needs `f64`), and fix the minimal thing each error points to — rerun `cargo build` after each fix rather than guessing ahead. Second, fix the math: the classic bug is accumulating `sum` and `sum_sq` as integers, causing overflow and a `negative` population variance from `E[x^2] - (E[x])^2` when sum_sq overflows; switch accumulators to `f64` (or use Welford's online algorithm, which avoids the sum-of-squares formulation entirely and is numerically stable) — implement `mean += delta/n` and `M2 += delta*(x-mean)` per element, then population variance is `M2/n`. Third, fix the memory issue: replace the `Vec<i32>` collect-then-loop pattern with a single pass that reads the CSV row by row via the `csv::Reader`'s streaming iterator, updating your running accumulators inline and never storing the full dataset. Finally run the binary against `/home/user/data.csv`, capture the variance, round to 2 decimals, and write it to result.txt.
