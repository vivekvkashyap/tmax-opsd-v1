Read the 50x20 matrix from `/home/user/genomic_signals.txt` with `fscanf` in a loop into a flat or 2D double array. Apply the 3-point moving average row-wise exactly per the given boundary formulas (note the boundaries reuse the edge value twice rather than reflecting or wrapping — implement precisely that, not a generic "same" padding convention you assume). For the SVD, use GSL's `gsl_linalg_SV_decomp` (note: GSL's SVD routine expects rows >= columns for the standard one-sided Jacobi routine — since you have 50 rows and 20 columns this is fine directly); the largest singular value is the first element of the returned S vector after decomposition, since GSL doesn't sort singular values for you in all paths — you may need to verify/sort. For bootstrapping, initialize `gsl_rng_mt19937` with seed 42 exactly once outside the loop, sample 50 row indices per iteration with `gsl_rng_uniform_int(r, 50)` (with replacement) from the *smoothed* matrix, recompute SVD each time, collect the 1000 sigma1 values, sort ascending with `qsort`, and read off indices 24 and 974 for the CI. Print with `%.4f` formatting matching the exact template.
