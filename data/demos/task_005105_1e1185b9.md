Split this into data prep and numerics, and validate each independently. For the CSV: parse the FASTA sequence into a plain string of one-letter codes (index i of the sequence corresponds to FASTA position i, 1-based); parse the PDB with fixed-column-based ATOM record parsing (columns for atom name, residue name, residue sequence number, B-factor per the PDB format spec — don't split on whitespace naively since fields can be flush against each other), keep only rows where the atom name is exactly `CA`, and for each CA row emit `index` (PDB residue seq number), `fasta_char` (the FASTA character at that same 1-based index), `pdb_res_name`, and `b_factor`. For the Rust fix: the gradient descent diverges because the learning rate (`let lr = 0.5;`) is far too large relative to the raw index scale (indices are O(1-5) but repeated multiplication compounds) — lower `lr` by an order of magnitude or two (e.g. 0.01 or smaller) so `d_alpha`/`d_beta` updates shrink the loss monotonically instead of oscillating to NaN/inf; you can verify convergence by printing intermediate loss and confirming it decreases steadily rather than blowing up. Since the B-factor vs index relationship here is exactly linear, a converged fit should show near-zero residual error. Rebuild with `cargo run` passing the CSV path, and write `alpha`/`beta` rounded to 4 decimals to `/home/user/output/model_results.txt`.