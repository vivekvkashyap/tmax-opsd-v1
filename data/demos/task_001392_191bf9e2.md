Split this into ETL/stats work and a deploy fix. For Step 1, load and concatenate all CSVs in `/home/user/data/raw/`, drop rows with missing `value`, then compute Q1/Q3 and IQR separately per `group` (A vs B) and filter outliers per-group using the strict `< Q1-1.5*IQR` / `> Q3+1.5*IQR` bounds — don't compute one global IQR across both groups. On the cleaned data, compute each group's mean, a 95% CI for the mean (use the standard formula `mean ± t_crit * (std/sqrt(n))`, being consistent about sample vs population std and which critical value table you use), and a Welch's two-tailed t-test (unequal variances — don't assume equal variance) comparing group A and B. Write the exact eight required keys to `/home/user/data/processed/results.json`. For Step 2, open `/app/statsserver/Makefile` and find the mismatch: the env var name it exports likely doesn't match what `app.py` reads via `os.environ.get(...)`, and the port passed to `flask run` likely isn't 8080 — fix both to point at your results.json and bind 127.0.0.1:8080. Start with `make serve` in the background, then curl `http://127.0.0.1:8080/api/stats` with the exact `Authorization: Bearer token-stats-2024` header to confirm it returns your computed JSON before finishing.