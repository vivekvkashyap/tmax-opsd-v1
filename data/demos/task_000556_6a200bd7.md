Build this as a straightforward Python pipeline over `/home/user/products.csv`, but get the ordering of operations right: first drop any row with a missing/NaN/empty value in any of the four columns (use pandas `dropna` plus an explicit empty-string check, since pandas won't treat `""` as NaN by default) — do this before computing embeddings so you don't waste compute on invalid rows. Install `sentence-transformers` and load `all-MiniLM-L6-v2`; for each surviving row, embed the reference string `f"{product_name} {category}"` and the `generated_description` separately, then compute cosine similarity between the two embeddings per row (use `sklearn.metrics.pairwise.cosine_similarity` or a manual dot-product/norm calculation — don't average vectors across rows). Independently check the word-count constraint by splitting `generated_description` on whitespace and counting tokens (5 to 30 inclusive). For each row, determine failure_reason from the two boolean checks: only-similarity-fails -> `"low_similarity"`, only-length-fails -> `"invalid_length"`, both-fail -> `"both"`, neither -> not flagged at all. Collect flagged rows into a list of `{"id": int, "failure_reason": str}`, sort ascending by id, and write compact/valid JSON to `/home/user/flagged_products.json`. Print your own computed similarity scores while developing so you can sanity-check which rows should flag before finalizing.
