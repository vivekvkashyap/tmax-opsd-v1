Read the existing script first to see exactly where the leak is (TF-IDF fit_transform runs on the whole corpus before the split). Fix it by calling `train_test_split(df['text'], df['label'], test_size=0.2, random_state=42)` on the raw text/labels directly, then build `sklearn.pipeline.Pipeline([('tfidf', TfidfVectorizer()), ('clf', LogisticRegression(random_state=42))])` and only ever call `.fit(X_train, y_train)` on it — never fit the vectorizer on the full dataset. This ordering matters because the checker recomputes the true (leak-free) accuracy independently and compares it to your `test_accuracy` with a tight tolerance, so any leak or seed mismatch will fail. For the timing benchmark, loop `pipeline.predict(X_test)` 100 times (e.g. with `time.perf_counter()`), sum durations, divide by 100, and convert seconds to milliseconds. Write `test_accuracy`, `avg_inference_ms`, and `pipeline_steps` (the list of step names from `pipeline.named_steps.keys()` or `pipeline.steps`) to `/home/user/metrics.json`, then actually run the script once to confirm the file is produced.
