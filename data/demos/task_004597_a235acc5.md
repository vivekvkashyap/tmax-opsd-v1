Structure the C++ program as parse-validate-aggregate-emit, testing each stage against the sample input before trusting the whole pipeline. Read translations.jsonl line by line; for each line, wrap `nlohmann::json::parse` in a try/catch since malformed unicode escapes will throw — catch the exception and skip that line rather than letting it crash the program. After successful parsing, validate `key` against an alphanumeric-plus-underscore check and `lang` against exactly two lowercase letters using simple character loops or `std::regex`; skip the line if either check fails, independent of whether JSON parsing succeeded. Maintain a `map<string, map<string, string>>` (lang -> key -> text) so that later duplicate (lang, key) pairs naturally overwrite earlier ones, giving you "last occurrence wins" for free, and so keys come out already sorted by using an ordered `std::map` for the inner container. After processing all lines, create the locales directory if it doesn't exist, and for each lang emit a header file following the exact template (namespace name, const std::string declarations one per line, closing brace) iterating the sorted inner map. Compile with the exact g++ command given and run it, then diff-check the generated headers against your own hand-traced expectations for a couple of entries before considering it correct.