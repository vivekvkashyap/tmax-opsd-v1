First recover the hardcoded secret from `/app/token_tool` before writing any validation code — since it's a stripped binary, use `strings /app/token_tool | grep -i -E 'key|secret'` (or similar) to pull printable string constants out of the binary's data section; a plausible HMAC key will look like a fixed-length random-ish string literal, not code. Confirm your candidate key by testing it: if you can find or construct a known-valid token in the clean corpus, recompute HMAC-SHA256 over `base64url(header).base64url(payload)` with your candidate key and check it matches the token's signature segment. Once confirmed, write `/home/user/secure_filter.cpp`: parse the `Authorization: Bearer <token>` header from the input file, split the token on `.` into exactly 3 non-empty parts (reject if signature part is missing/empty — this closes the alg=none/no-signature bypass), base64url-decode the header and check its `alg` field is `HS256` explicitly (reject `none` or anything else), recompute HMAC-SHA256 over the first two dot-joined segments using OpenSSL's `HMAC()`/EVP API with the recovered key, and do a constant-time-safe comparison against the decoded signature bytes. Print exactly `ACCEPT` or `REJECT` with a trailing newline. Compile with `g++ ... -lssl -lcrypto` to `/home/user/secure_filter`, then self-test against a handful of files from both corpora before finishing.