Treat `/app/custom_encoder` as a black-box oracle to recover the encoding scheme before writing C: feed it known plaintext strings (single characters first, e.g. "A", "B", spaces, digits) and observe the hex output — a single-byte XOR key applied uniformly will show up as a constant relationship between each input byte and its corresponding output hex pair (input_byte XOR output_byte = constant across all samples). Confirm the key holds across multiple varied inputs, including punctuation and special characters likely to appear in XSS/SQLi payloads, before trusting it. Also confirm the output is a plain uppercase hex string (pairs of hex digits per input byte, no separators) by checking output length is exactly double the input length. In your C decoder: read the file's hex text, convert each 2-char hex pair to a byte, XOR with the recovered key to get plaintext, then run `strstr`-based checks for known SQLi markers (`OR 1=1`, `UNION SELECT`, `--`, quotes-in-context) and XSS markers (`<script`, `onerror=`, `javascript:`) against the decoded plaintext. Print exactly `EVIL`/exit 1 or exactly `CLEAN`/exit 0 with no other stdout output. Validate against `/app/corpus/clean/` and `/app/corpus/evil/` for 100% accuracy before finishing.