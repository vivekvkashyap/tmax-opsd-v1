Treat this as black-box reverse engineering: since the source is gone, probe `/app/dataset_lineage_oracle` with small, hand-crafted JSON graphs piped to stdin and varying `argv` (source/target IDs) to learn its exact contract before writing any Rust. Start with a trivial 2-node graph, then a 3-node chain, then a graph with no path between source and target, and note precisely: the JSON key names for nodes/edges/weights, whether weights are ints or floats, and the exact stdout wording/formatting for both the success case (path listing + cost) and the unreachable case — capture raw bytes (`| xxd` or `| cat -A`) to catch trailing newlines or spacing you'd otherwise miss. Once the schema and output format are pinned down, scaffold `cargo new /home/user/lineage_solver`, parse stdin JSON (e.g. with `serde_json`) into an adjacency map, and implement standard Dijkstra (a `BinaryHeap` of `(cost, node)` with a `HashMap` for distances/predecessors) to get the shortest path and total cost, then reconstruct the path by walking predecessor links. Build with `cargo build --release` so the binary lands at `target/release/lineage_solver`, and diff your output against the oracle's output byte-for-byte on many more random graphs (including edge cases: disconnected nodes, self-loops, ties) before considering it done — exact string formatting is what the equivalence check enforces.