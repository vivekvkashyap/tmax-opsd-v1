Break this into independent, verifiable pieces. First, use `ffmpeg` to extract every frame of `/app/status_light.mp4` (e.g. `ffmpeg -i status_light.mp4 frame_%04d.png`), then classify each frame's dominant color programmatically (sample a pixel or average the image and check whether it's closer to pure green or pure red) rather than assuming a fixed frame count — count greens and reds yourself and compute `uptime = green/(green+red)*100` rounded to 2 decimals. Second, copy only the red-classified frames into a fresh directory and `tar -czf` just those into `/home/user/downtime_backup.tar.gz` — don't include green frames. Third, write `/home/user/alert.exp` using Tcl `expect`/`send` idioms: `spawn /app/trigger_alert.sh`, `expect "Enter server uptime percentage:"`, `send "<your computed value>\r"`, `expect "Confirm alert dispatch (y/n):"`, `send "y\r"`, then `expect eof` — match the prompt text exactly since expect is literal-string sensitive. Fourth, write one Python process (using `http.server`/Flask for port 8080 and raw `socket` for port 8081) that both serve the same computed uptime value, launch it detached/backgrounded (e.g. `nohup ... &`), and confirm both endpoints respond correctly with `curl`/`nc` before finishing.
