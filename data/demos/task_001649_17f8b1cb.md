Use `git bisect` to isolate the regression: `git bisect start`, mark current `HEAD` (or `main`) as `bad` and tag `v1.0` as `good`, then let git checkout candidate commits; at each step run `./test.sh` (it already exits non-zero via `timeout` on hang) and report `git bisect good`/`git bisect bad` accordingly until git identifies the first bad commit — capture its full hash with `git rev-parse` and write it to `/home/user/bug_commit.txt`, then `git bisect reset`. Once you have the culprit, `git show <hash>` to see exactly what changed — look specifically at loop bounds/modulo arithmetic around the queue's circular-buffer index inside the mutex-protected section, since an off-by-one there can make the traversal loop never terminate while the lock is held, starving every other thread (looks like a deadlock but is actually a livelock/infinite loop). Fix the same logic on the current `main` branch (not by reverting the whole commit, since later commits may depend on other changes in it), rebuild, and confirm `./test.sh` now exits 0 within its timeout.