Build the two profile-document lists exactly as specified (`name + " " + about` for ecommerce, `username + " " + signature` for forum), then fit ONE `TfidfVectorizer()` with default params on the combined corpus of both lists together (not two separate vectorizers) so the vocabulary and IDF weights are shared — fitting separately will give incomparable vectors and wrong similarities. Split the resulting TF-IDF matrix back into the ecommerce rows and forum rows by index, then compute `cosine_similarity(ecommerce_matrix, forum_matrix)` to get an (n_ecommerce x n_forum) similarity matrix. For each ecommerce row, find the argmax forum column and its score; only keep the match if that score is strictly greater than 0.45, otherwise skip that ecommerce user entirely. For kept matches, split the matched forum row's `topics` field on `;` into a list. Assemble the dict keyed by ecommerce `id` (as string) mapping to the topic list, and write it as JSON to `/home/user/output/recommendations.json`, creating the output directory first if needed.
