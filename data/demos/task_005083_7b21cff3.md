Build each stage independently and sanity-check it before chaining. Extract frames with `ffmpeg -i /app/data/dashboard.mp4 -vframes 30 frame_%03d.png` (native framerate, no `-r` override), then for each frame load it with `cv2`/`PIL`, convert to grayscale, and average pixel intensity — match `cv2.cvtColor(img, cv2.COLOR_BGR2GRAY).mean()` semantics since the verifier compares against that exact computation with ±1.0 tolerance. For gap-filling, reindex on the full 0..29 integer range and use pandas `interpolate(method='linear')` (or manual linear interpolation) only for missing frame_index rows, not for existing ones. For `validate.py`, treat it as a strict boolean classifier: read the CSV, check header names exactly, check frame_index is 0,1,2...N-1 with no gaps/reordering, check every intensity parses as float and lies in [0,255] inclusive — any parse failure or malformed row must also mean exit 1 (wrap parsing in try/except), and exit 0 only if every row passes every rule. For aggregation, import via `sqlite3 -csv` `.import`, then a single `GROUP BY (frame_index/10)*10` query into `summary.csv`. Write the literal cron string requested; don't install it.
