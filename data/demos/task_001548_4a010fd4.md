Build this in the order the pipeline needs it, not the order it's described: directories and permissions first (mkdir + chmod 700 on backups), then generate the self-signed cert with `openssl req -x509 -newkey rsa:2048 -days 365 -nodes -keyout server.key -out server.crt -subj ...` and immediately `chmod 600` the key. For the C tool, call `stat()` on the source directory and check the return value (non-zero means missing) before calling `system("tar -czf dest source")`; return the correct exit codes on each path. In pipeline.sh, compile with gcc, run the backup tool, then launch `openssl s_server -WWW -cert ... -key ... -port 8443` — crucially `cd` into `/home/user/backups` first (or use `-WWW` semantics that serve relative to cwd) and background it with `&` plus `disown`/redirected output so the script doesn't hang waiting on the server. Append the exact log line last. After running pipeline.sh, verify independently: check `ss -tlnp` for port 8443 and `curl -k https://127.0.0.1:8443/latest.tar.gz` to confirm the archive is actually servable, not just that the process started.