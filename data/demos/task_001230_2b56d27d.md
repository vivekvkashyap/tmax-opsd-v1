Build this as an exact-reproduction pipeline rather than a "reasonable NLP approach" — every step has a precise, checkable specification, so match it literally. Implement the tokenizer as its own function (lowercase, then regex-replace anything not `[a-z\s]` with a space, then `.split()` on whitespace) so you can pass it as a custom `tokenizer` to `sklearn.feature_extraction.text.CountVectorizer` with `token_pattern=None` — don't let sklearn's default tokenizer silently override your rules. Set `min_df=2` on the vectorizer to enforce the "appears in at least 2 documents" filter, and note that `CountVectorizer` already sorts vocabulary columns alphabetically by default, so no extra sorting is needed there. Fit `PCA(n_components=2, random_state=42, svd_solver='full')` on the resulting dense count matrix (call `.toarray()` first), transform, then round `pc1`/`pc2` to exactly 4 decimal places using standard rounding (watch for -0.0 vs 0.0 formatting differences). Finally sort the output rows by `ticket_id` ascending before writing to `/home/user/topics.csv` with exactly the three specified column names.
