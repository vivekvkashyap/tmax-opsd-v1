Write the extraction as two nested passes rather than trying to stream everything at once. First open `/home/user/incoming/payload.tar` with `tarfile` and extract its members (the `.zip` files) into a temporary working directory. Then, for each extracted `.zip`, open it with `zipfile.ZipFile` and iterate its namelist, extracting only entries whose name ends in `.bin` — write them directly into `/home/user/curated_binaries/` using just the base filename (`os.path.basename`) so nested paths inside the zip don't create subdirectories; create that output directory first with `os.makedirs(..., exist_ok=True)`. After all `.bin` files are collected, iterate the files actually present in `/home/user/curated_binaries/`, compute each one's SHA-256 by reading it in binary chunks through `hashlib.sha256()`, and build `hash  filename` lines (exactly two spaces, matching `sha256sum` format) — sort these lines alphabetically by filename before writing them to `/home/user/manifest.sha256`. Run the script at the end so the output files actually exist on disk.
