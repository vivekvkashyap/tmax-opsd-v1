First diagnose `/app/visualize.py`: check the `matplotlib.use(...)` call near the top — a non-rendering backend (e.g. one meant for testing, not actual image output) will produce broken/empty PNGs even though `savefig` doesn't error. Switch to a proper non-interactive backend such as `Agg` before importing `pyplot`, then rerun the script and confirm `/home/user/waveform.png` is a real, non-trivial image file. For the pipeline, load the WAV with `scipy.io.wavfile.read`, chop the signal into non-overlapping 1-second chunks using the sample rate (drop any incomplete trailing chunk so chunk count matches `targets.csv` row count), and per chunk compute RMS (`sqrt(mean(x**2))`) and zero-crossing rate (fraction of sign changes between consecutive samples) as your feature columns. Load targets in the same chronological order, print the Pearson correlation of each feature against target as a sanity check (should be clearly nonzero given the fixture), then do an 80/20 `train_test_split` with `shuffle=False, random_state=42` (note shuffle=False makes random_state a no-op, but include it per the spec), fit `LinearRegression`, compute test-set MSE, and dump `{"test_mse": <value>}` to `/home/user/metrics.json`. Threshold to beat is well below 2.0, so correct feature/target alignment matters more than model sophistication.