Treat this as three linked investigations, each feeding the next. First, extract audio/frames or subtitles from `/app/crash_demo.mp4` (e.g. `ffmpeg -i crash_demo.mp4 -vf fps=1 frames/%03d.png` plus reading any on-screen log text, or extracting audio if narrated) to find the log message or timestamp right before the crash — this tells you what kind of packet/field to look for. Then open `/app/traffic.pcap` with `tshark`/`scapy` and filter for packets matching that clue (e.g. a specific protocol field or TLV type byte) to isolate the exact malformed payload — compare candidate packets' structure (e.g. a length field vs actual remaining bytes) rather than guessing which one crashes. For the git bisection, use `git bisect start`, mark `master` bad and `v1.0` good, and at each step `git checkout`, rebuild, and feed the isolated payload to the daemon, checking for a segfault (exit code) to mark good/bad automatically — script this with `git bisect run <script>` so it's deterministic across ~200 commits. Write the resulting commit SHA verbatim (via `git rev-parse`) to `/home/user/bad_commit.txt`. For the sanitizer, reimplement bounds-checking logic (based on what the bisected diff removed) in C, reading stdin payload bytes and printing `1`/`0`; test it against both benign and malicious sample payloads before compiling to `/home/user/sanitizer`.