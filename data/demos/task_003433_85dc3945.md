Do this as a shell pipeline with clearly separated stages so you can inspect intermediate output. First clean: use `awk -F, 'NR>1 && $3!="" && $3!="NA" && $3+0>=0'` (careful: `$3+0>=0` on a non-numeric NA string would coerce to 0 and wrongly pass, so explicitly check the empty/"NA" cases before the numeric comparison, and don't let awk silently treat blank or NA fields as 0). Second, assign fold numbers by emitting cleaned rows with their line index modulo 3 (`NR % 3` after excluding header, adjusted so the 1st cleaned row is fold 0) — do this in the same awk pass or a second one, writing `value,label,fold` tuples to a temp file. Third, for each of the 10 candidate thresholds, loop over folds 0-2, and for each fold count correct predictions with awk/grep comparing `(value>=T)` against the `is_anomaly` label, sum correct/total per fold, then average the three fold accuracies (use `bc` or awk for floating math, careful with integer division). Track the best (T, accuracy) pair using strict `>` comparison when updating the best-so-far so that on ties the lowest T (checked first, ascending order) is kept. Finally print the winning `T,accuracy` with accuracy formatted to exactly 4 decimals (`printf "%d,%.4f\n"`) to `/home/user/best_model.csv`.