Run `tesseract /app/config.png stdout` first to OCR out the three key-value lines (`PRIOR_MEAN`, `PRIOR_VAR`, `PORT`) — read the actual recognized text rather than assuming values, and sanity-check the OCR output looks like plausible numbers/port before hardcoding it into your Go program. For data cleaning in `/home/user/sensor_data.csv`: parse with encoding/csv, treat both empty string and the literal "NaN" token as missing, compute the median of only the valid numeric values (sort and pick the middle, averaging the two middle values if the count is even) and impute that into missing rows, THEN apply the outlier filter (drop value < 0.0 or > 100.0) — order matters, since outliers should not affect the median used for imputation, and outlier filtering happens after imputation. For the Bayesian step, first compute sample variance (Bessel-corrected, N-1) of the cleaned data for `/stats`, then separately use the *population*-style formula given in the spec ($\sigma^2$ = "sample variance of your cleaned dataset" — read carefully whether the task wants Bessel-corrected value plugged into the posterior formula) to compute posterior mean/variance per the given conjugate-update equations for `/posterior`. Wire both as JSON handlers in a Go `net/http` server listening on `0.0.0.0:<recovered-port>`, and leave the process running (e.g. `go run server.go &`) so the verifier's curl requests succeed.