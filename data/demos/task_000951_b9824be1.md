Diagnose the two bugs separately before touching the code: reproduce the crash on space-containing filenames first (likely the C code builds a shell command string or uses `strtok`/`sscanf` on unquoted paths — switch to `fopen(filename, "r")` directly with the full path taken as a single string/argv element rather than any shell-based `popen`/`system` invocation, and use `readdir`+`opendir` rather than shell globbing to enumerate log files so spaces are never a delimiter). For the performance bug, find the existing O(n^2) sort (nested loops comparing/swapping) and replace it with `qsort()` using a comparator on the timestamp field — this alone should take a 100k-line merge from seconds to milliseconds. Check the Makefile's library flags: `-ljson-c` must appear, and if there's a competing system vs. local libjson-c, make sure `-I`/`-L` point at the correct include/lib paths so it compiles cleanly. After each fix, rebuild with `make` and test the aggregator directly (e.g., raw TCP request) before layering the Python API on top. Finally start all three services via `start_services.sh`, then measure end-to-end latency yourself with a timed HTTP GET to confirm you're comfortably under the 0.5s bar, not just barely passing.