Start by extracting the hidden parameters from the image with OCR — run `tesseract /app/cleaning_parameters.png stdout` (installing/using `pytesseract` if preferred) and parse the alpha, Z-score threshold, and minimum token frequency with a robust regex against the OCR text rather than assuming fixed values; print the parsed numbers to sanity-check them before hardcoding them into your script (OCR errors on decimals are common, so verify they look like reasonable statistical parameters). Then build `pipeline.py` to take exactly `sys.argv[1]` (input CSV) and `sys.argv[2]` (output JSON): compute mean/std of `numeric_val`, drop rows whose Z-score `(x-mean)/std` exceeds the extracted threshold in absolute value, run `scipy.stats.ttest_1samp(remaining_values, 0.0)` and set `is_significant` per-row based on whether the resulting p-value is strictly below alpha, then tokenize `text_data` by lowercasing and whitespace-splitting, count token frequency across the whole remaining dataset, drop tokens below the extracted minimum frequency, and rejoin surviving tokens per row. Since this will be checked against an oracle via randomized fuzzing, match the exact order of operations (filter outliers first, then compute significance and tokens on the filtered set) and write output as a JSON list of dicts.
