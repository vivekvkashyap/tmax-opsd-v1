Start by extracting the two hidden parameters (a calibration epsilon and a random seed) that are spoken in `/app/system_recording.wav` — install a speech-to-text tool (e.g. `openai-whisper`, `SpeechRecognition`+`pocketsphinx`, or similar) and transcribe the file rather than guessing; listen to/inspect the transcript output carefully since numbers may be spelled out as words and need careful parsing into the actual numeric values you'll hardcode into your script. Then implement `spectral_augment.py` as an exact filter: read exactly 4096 bytes (1024 float32 little-endian) from stdin via `sys.stdin.buffer.read(4096)`, compute `np.fft.rfft` then power spectrum `np.abs(X)**2` (513 bins for a 1024-point real input), seed `np.random.seed(<recovered-seed>)` immediately before drawing exactly 513 `Uniform(0,1)` samples via `np.random.uniform(0,1,size=513)` (order and dtype matter for bit-exactness — cast noise and the final result to float32), add noise to the power spectrum, clamp with `np.maximum(result, <recovered-epsilon>)`, cast to float32, and write raw bytes to stdout via `sys.stdout.buffer.write(...)`. Since the grader fuzzes against a hidden oracle with random inputs, test your own script against itself with several random inputs to confirm shapes/dtypes/byte-lengths are exactly right (4096 bytes in, 2052 bytes out), and double check you seed the RNG fresh per invocation, not once globally. Make it executable with a proper shebang.