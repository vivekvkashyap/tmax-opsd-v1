Read stdin line-by-line in Rust (`io::stdin().lines()`) until EOF, and apply the four rules in the exact given order per line — this ordering matters: format validation must happen before rate-limiting is incremented, meaning a malformed line should NOT count toward that IP's request tally (since the IP couldn't even be parsed). Split each line on `,` and require exactly one comma with both resulting parts non-empty (`splitn(2, ',')` then check both halves aren't empty) to decide `ERROR`. Maintain a `HashMap<String, u32>` counting valid (comma-parsed) requests per IP, incrementing before checking whether this is the 4th+ request from that IP (i.e. increment then test `count >= 4`) to trigger `REJECT: <IP>`. Only after passing both checks, verify the path starts with `/api/` using `.starts_with("/api/")` for `INVALID: <Path>`, otherwise print `ACCEPT: <IP>`. Compile exactly as specified: `rustc -C opt-level=s -C strip=symbols /home/user/limiter.rs -o /home/user/limiter`. Run it against `/home/user/requests.log` redirecting stdout to `/home/user/results.log`, and manually trace a few lines of the input by hand against your rule order to confirm you match the expected sequence before finishing.