Build the C load balancer with a simple accept-loop on port 8000 using standard BSD sockets: `socket`/`bind`/`listen`/`accept`, then for each accepted connection open a fresh outbound `connect()` to whichever backend (8001 or 8002) is next in a strictly alternating counter (a static/global variable toggled after each connection, not based on connection count parity computed incorrectly — verify alternation starts with 8001 on the very first connection). For the single-cycle proxy behavior, just `read()` from the client, `write()` that to the backend, `read()` the backend's reply, `write()` it back to the client, then `close()` both sockets — no need for full duplex threading. Compile with `gcc -o /home/user/lb lb.c`. For `health.sh`, detect whether port 8000 is bound using a portable check like `ss -ltn | grep -q ':8000 '` (avoid relying on tools that may not exist); on failure, append the exact crash string to `lb.log` with `>>` and relaunch `/home/user/lb &` disowned so it survives the script exiting. Install the cron entry with `(crontab -l 2>/dev/null; echo "* * * * * /home/user/health.sh") | crontab -`. Manually test round-robin behavior and the crash-recovery path (kill lb, run health.sh, check the log) before finishing.
