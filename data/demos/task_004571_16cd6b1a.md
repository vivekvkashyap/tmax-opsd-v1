Diagnose the C bug by reading the allocation carefully: each row buffer is allocated with only `w` bytes but `fread()` is told to read `w*3` bytes into it (RGB24 is 3 bytes per pixel) — that mismatch is the buffer overflow causing the segfault. Fix the allocation size to `w*3`, update the red-channel indexing to match, and free every allocated buffer plus the struct before returning to avoid leaks. Before touching the real video, exercise the fixed binary directly in your bash script: pipe `head -c $((1920*1080*3)) /dev/urandom` into it for 5 iterations and check the exit code each time, aborting the script with a nonzero exit if any invocation crashes. For the pipeline, extract raw frames with `ffmpeg -i /app/deploy_recording.mp4 -f rawvideo -pix_fmt rgb24 -`, then loop reading exactly `width*height*3` bytes per frame, feed each frame's bytes to the repaired analyze_frame binary, count how many frames report an average red intensity strictly greater than 200, and write that integer count to result.txt.
