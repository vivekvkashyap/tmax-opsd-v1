This task chains four distinct steps — validate each before moving to the next. First, transcribe the audio: use `ffmpeg` to convert `/app/incident_report.wav` to a format your speech-recognition tool prefers (e.g. 16kHz mono WAV), then run it through a local ASR tool (e.g. `whisper` if available, or Python's `speech_recognition`) to get the transcript text; read the transcript yourself to identify which shard IDs are named as corrupted rather than assuming a pattern — the numbers are spoken as words, so double check the parsed digits are correct. Second, connect to `/app/backup.db` with `sqlite3`/`python3 sqlite3` and write a query (or two-step Python filtering) that keeps only `entities` whose `shard_id` is not in the corrupted set, and only `relations` whose own `shard_id` is clean AND whose `src`/`dst` both reference valid (non-corrupted) entities — a plain shard filter on `relations` alone is insufficient since an edge can reference an entity from a bad shard. Third, build a `networkx.DiGraph()` from the cleaned rows, adding edges with `weight` attribute, then compute `networkx.pagerank(G, alpha=0.85, weight='weight')`. Finally, dump the resulting dict as JSON (id -> float) to `/home/user/pagerank.json`, keeping full float precision since the tolerance is tight (MAE <= 1e-4).