Split this into two independent workstreams. For Phase 1, diff the working config against what's broken: check `/app/config/` for a Redis URL/port mismatch (Flask likely points at the wrong Redis port) and check `/app/start.sh` for missing or mis-ordered service launches (Redis must be up before Flask/worker connect, and the worker's socket/queue name must match what Flask publishes to) — fix these config values in place rather than rewriting the architecture, then verify end-to-end with a manual `curl -X POST http://localhost:5000/process ...` and check the result lands in Redis. For Phase 2, treat this as an adversarial classification problem, not a hand-picked heuristic: study the provided `/app/corpus/clean/` and `/app/corpus/evil/` CSVs to characterize what makes data "near-singular" — e.g., compute the variance/range of the intensity column, check for a flat line (near-zero std dev), or estimate collinearity/rank via a simple linear regression residual or covariance determinant — and pick a numeric threshold that cleanly separates the two corpora rather than one that barely works on the samples you were given, since the hidden evaluation corpus will be stricter. Have `filter.c` read the CSV, compute your chosen statistic(s), and `exit(0)` for clean / `exit(1)` for evil. Compile exactly with `gcc -O3 /app/src/filter.c -o /app/bin/filter -lm` and re-test against both corpora before finishing.