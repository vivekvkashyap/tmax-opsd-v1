Break this into its five stages and validate each before moving to the next. (1) Extract frames from `/app/experiment_feed.mp4` with `ffmpeg` (or OpenCV), compute each frame's average red-channel value, then aggregate by grouping frames into 1-second buckets using the known frame rate to average within each second. (2) Read the timestamp offset from `/app/metadata.json`, apply it to `/app/sensor_logs.csv`'s timestamps (add or subtract as the offset semantics require), then join on matching per-second timestamps with the video features from step 1. (3) Compute a Pearson correlation matrix over the joined numeric columns; for any pair exceeding 0.90, drop whichever of the two appears later in column order — do this iteratively since dropping one column changes which pairs remain. (4) Fit `Ridge` with `GridSearchCV` (`alpha` in [0.1, 1.0, 10.0], `cv=5`) against `target_yield`, keep the best model in memory/on disk. (5) Stand up an HTTP server on `127.0.0.1:8080` exposing `POST /predict`, validate the `Authorization: Bearer secret-token-123` header, load features from the JSON body, run the trained model, and return `{"prediction": <value>}`; keep the process running so it can be probed.
