Treat this as three independent stages. (1) Fix the `Makefile`'s `LDFLAGS` to link against the correct/available audio library, confirm with a clean `make`. Then run `git bisect start` with `main` marked bad and an early commit good, and script `git bisect run make test` so it walks history to the commit that introduced the off-by-one loop bound in `parser.c` (a boundary comparison like `<=` that should be `<`, or vice versa, over the payload length) — fix it on `main` and rebuild. (2) Transcribe `/app/incident.wav` yourself rather than guessing the codeword, normalize it as needed, pass it as `--key <transcribed-word>` to `./parser` against the corrupted WAL, and redirect stdout exactly into the required JSON output file. (3) For the validator, read the file's raw bytes: check byte 0 against the magic value, treat byte 1 as payload length `N`, and require total size exactly `N+2` — reject on magic or size mismatch, accept only on exact match; test against well-formed and truncated/padded files you construct yourself first.
