Do the three layers independently and verify each before combining. For extraction, use a video library (e.g. `ffmpeg`/`opencv-python`/`PyAV`) to iterate every frame of `/app/deployment_signal.mp4` and test whether all pixels equal pure red (255,0,0) — don't sample only a few frames or assume the red run is contiguous without checking; count exactly how many consecutive/total frames pass the pure-red test, since that count becomes your DEPLOY_MAGIC_NUMBER. For the ASM/C piece, follow the System V AMD64 calling convention precisely: the single 32-bit arg arrives in `edi`, and `get_magic_offset` should move it into `eax`, add your discovered magic number, and `ret` with the result in `eax`; assemble with `nasm -f elf64 core.asm -o core.o`, then `gcc -shared -fPIC wrapper.c core.o -o /home/user/libdeploy.so`, where wrapper.c just declares `extern int get_magic_offset(int);` and forwards to it from `invoke_offset`. For the API layer, write a Python HTTP server bound exactly to 127.0.0.1:8000 with the two routes; load the .so via `ctypes.CDLL`, set `.argtypes`/`.restype` to `c_int` before calling, run it in the background (nohup/daemonize), and write its PID to /home/user/server.pid. Test both routes with curl yourself before finishing.
