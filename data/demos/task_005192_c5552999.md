Read the existing /home/user/gc_calc.go to see the current channel-based reduction, but don't just tweak the loop shape — change the data flow: instead of summing values as they arrive from the channel (`for w := range ch { totalWeight += w }`), collect every value into a `[]float64` slice first (drain the channel into the slice, still after `wg.Wait()`/close as today), then call `sort.Float64s()` (or equivalent) on that slice, and only then iterate the sorted slice in a simple sequential loop to accumulate the sum. This makes the float reduction order deterministic regardless of goroutine completion order. Keep `calcWeight` untouched, keep the `%.10f\n` print format, then `go run gc_calc.go > /home/user/fixed_result.txt` (or build+run) to produce the output file. Verify your output is stable by running it more than once and confirming the value doesn't change between runs.