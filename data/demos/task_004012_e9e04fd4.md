Work bottom-up: fix compilation, then linking, then the Python-facing bugs, and only then build the benchmark. In `rust_engine/src/lib.rs`, look for classic PyO3/borrow-checker mistakes — e.g. pushing references to locals that get dropped, or returning a reference (`&res`) where an owned value is expected — and correct them to move/own the data properly (push owned tuples, return `Ok(res)`). In `Cargo.toml`, check the `[lib]` section for a missing `crate-type = ["cdylib"]`, which is required for Python to load the compiled extension; add it, then build with `maturin develop` (or `pip install -e .`) and confirm the module imports cleanly in Python before touching anything else. In `server.py`, find where trajectory data is serialized before sending over the websocket and make sure it's valid `json.dumps(...)` of a JSON-serializable structure (not string-concatenated or sent as raw bytes). Use OCR (`tesseract`) on `/app/sys_config.png` to recover the exact ALPHA, BETA, and ITERATIONS values — read them precisely rather than guessing. In `run_benchmark.py`, call both `pure_python.py`'s function and the compiled Rust function with those parameters, time each with multiple runs for stability, compute MSE between the resulting coordinate arrays and the speedup ratio, and write both to `/home/user/benchmark_results.json` with keys `mse` and `speedup`.
