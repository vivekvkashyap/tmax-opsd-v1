Fix correctness before chasing speed, and verify each layer independently. Makefile: add `-fPIC` to the compile flags and `-shared` (plus `-o librms.so`) to the link step so it actually produces a loadable shared library. wrapper.py: inspect the ctypes call site and explicitly set `lib.compute_rms.argtypes = [ctypes.POINTER(ctypes.c_float), ctypes.c_int, ctypes.c_int, ctypes.POINTER(ctypes.c_float)]` (and `restype = None`) matching the C signature exactly — mismatched types (e.g. c_double where c_float is expected, or missing POINTER wrapping) are what cause segfaults/garbage. rms.c: replace the nested-loop moving RMS with a running-sum approach — maintain a running sum of squares over the current window, subtract the value leaving the window and add the value entering it as you slide by one sample each iteration, computing sqrt(sum_sq/window_size) per step in O(1) amortized instead of O(window_size) per step. After each fix, run `python3 benchmark.py /app/sample.wav` to confirm MSE < 1e-5 against the Python reference before worrying about timing; only once correctness holds, time the C path and confirm it comfortably beats the threshold. Save the final array with `numpy.save('/home/user/output.npy', ...)`.