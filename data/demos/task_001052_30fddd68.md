The key constraint is doing an in-place text substitution on the tar member content while streaming through gzip, without ever writing extracted files to disk. Because "AcmeCorp" and "NovaCorp" are the same length, you can pipe the decompressed tar stream through a byte-for-byte string replacer that doesn't change any offsets, so the tar headers (which encode file sizes) stay valid — this is the whole trick, and it breaks if you ever use a replacement of different length or let `sed`/`tr` add or drop bytes anywhere in the stream. Build the pipeline as `zcat legacy_docs.tar.gz | sed 's/AcmeCorp/NovaCorp/g' | gzip > updated_docs.tar.gz.tmp`, verifying that `sed`'s substitution operates on the raw byte stream correctly (test first on a small tar without gzip to confirm sed doesn't mangle binary tar header bytes elsewhere in the stream — restrict the substitution to only that literal string, don't use `-r`/extended patterns that might match unintended tar metadata bytes). After the pipeline succeeds and the temp file is fully written and flushed, atomically `mv updated_docs.tar.gz.tmp updated_docs.tar.gz` so a partial write is never visible at the final path. Validate at the end by extracting to a scratch directory and grepping for zero remaining "AcmeCorp" occurrences and the expected count of "NovaCorp".