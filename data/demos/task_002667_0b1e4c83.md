Do the audio transcription once, up front, to get real second-offsets, then focus most of your effort on the validator since that's what's graded. Transcribe `/app/localization/reference_sync.wav` (e.g. with `whisper` if available, or another installed ASR tool) and note the approximate timestamps where "Sync one", "Sync two", and "Sync three" occur — use these as your 0/1/2 -> seconds mapping constants in the script; exact precision isn't graded strictly, but the mapping must exist and be derived from the audio, not invented. For `sanitize_loc.py`: read the CSV with `csv.DictReader`, first run all validation checks (any of `en`/`es`/`fr`/`de` empty/missing on any row -> reject; any translation string containing `<script>`, `onload=`, or similar tag/attribute patterns -> reject; any raw `timestamp` value not in {0,1,2} -> reject) and exit 1 immediately on the first violation found — validate before transforming. Only if a file passes every check do you reshape wide-to-long (one row per key/timestamp per language), lowercase-and-underscore the `key` field, substitute the mapped audio timestamp, and print the resulting CSV to stdout before exiting 0. Test against both `/app/corpora/clean/` and `/app/corpora/evil/` files to confirm exit codes match expectations for every file.