Start by actually transcribing `/app/dictation.wav` (e.g. install/run `whisper` or another ASR tool via ffmpeg-decoded audio) rather than guessing the binary format — the exact field order, byte widths, and endianness are stated in the audio and must be followed precisely, since output is checked for bit-for-bit equivalence against a hidden oracle. Once you know the format, implement `doc_builder.py` to: walk the input directory recursively, track the `os.path.realpath()` of each visited directory in a `set` before recursing so cycles from adversarial symlinks (including ones pointing to ancestors) are skipped rather than followed infinitely; collect only `.md` files; sort the collected entries by their relative path (not filesystem order) for determinism; then stream each entry into the wire format your transcription revealed (length-prefixed path, length-prefixed content, using the specified integer widths/byte order) through a `gzip.open()` writer. For atomicity, write to a temp file in the same directory as the output (e.g. via `tempfile.mkstemp(dir=...)`) and `os.rename()` it into place only after the gzip stream is fully flushed and closed — a raw `write()` without close-then-rename risks partial/corrupt final files. Test against several hand-built symlink-loop directory trees before trusting it.
