Since this is graded by fuzz-equivalence against a hidden oracle across many random CSVs, prioritize matching the spec's exact semantics over "reasonable" interpretations. First extract the keyframe reference timeline once with ffprobe against packet-level info (select the video stream, show `pts_time`, `size`, and `flags`, filter for keyframes flagged `K`), and cache/hold that in the script since it doesn't change per invocation. For each CSV: pivot into a `timestamp x sensor_id` wide table, sort by timestamp, build a regular grid from `floor(min_ts)` to `ceil(max_ts)` in steps of 0.5, forward-fill missing sensor values across that grid (careful: a sensor with no prior observation yet must fill with 0.0, not NaN propagate), then compute the Euclidean norm across sensor columns per grid row. For each keyframe, round its `pts_time` to the nearest 0.5 using round-half-to-even vs round-half-up carefully (match `round(pts*2)/2` semantics), look up the distance at that rounded grid point (use -1.0 if out of range of the built grid, not just missing from data), then compute `abs(distance - pkt_size) / pkt_size` rounded to 4 decimals. Output must be a single JSON list printed to stdout ordered by original pts, with zero extra output — test your script directly against many synthetic CSVs including edge cases (single row, all timestamps in the past/future relative to grid bounds) before considering it done.