First understand the black-box binary empirically: run `/app/path_scorer` with a couple of sample node-ID arguments and observe that it just prints a single float to stdout, so your Go server only needs to shell out to it via `os/exec` with the path's node IDs as separate args and parse the printed value — don't try to reverse-engineer internal logic, just treat it as an external scoring call. For `GET /nodes`, use `database/sql` with a parameterized query (`WHERE label = ?`) against `nodes`, order results by `id ASC`, then apply pagination in SQL or in Go using `LIMIT`/`OFFSET` derived from `page` and `limit` (remember `page` is 1-indexed, so offset = `(page-1)*limit`), and return only `id`/`name` per the schema alongside the echoed `page`/`limit`. For `GET /shortest-path`, load `edges` into an adjacency list treating them as undirected, then run BFS from `start` to `end` tracking predecessors to reconstruct the path; if `end` is never reached, return 404 with the exact error JSON. On success, invoke `path_scorer` with the path's node IDs in order and include its parsed float as `score`. Compile and run the Go server bound to `127.0.0.1:9090`, and use `nohup ... &` (or similar) so it stays alive in the background before you finish, verifying it actually accepts connections first.