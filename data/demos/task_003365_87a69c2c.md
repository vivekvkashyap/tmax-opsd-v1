Edit `/app/nginx.conf` to add a `map` block on `$http_x_upload_filename` that flags traversal attempts, then reference that flag inside the `location /upload` block guarding the `proxy_pass` to the Flask backend. Use a single case-insensitive regex map value (nginx `map` supports `~*` regex matching) that catches literal `../` as well as URL-encoded variants like `%2e%2e%2f`, `..%2f`, and `%2e%2e/` — remember nginx does NOT auto-decode `%2e` etc. in headers, so you must match the encoded forms as literal substrings/regex alternatives, not rely on decoding. Inside `location /upload`, use `if ($bad_filename) { return 403; }` before the `proxy_pass http://localhost:5000;` line so benign requests still reach Flask. After editing, reload rather than fully restart to avoid downtime: `nginx -t` to validate syntax, then `nginx -s reload` (or `systemctl reload nginx` if managed that way). Test manually with a couple of `curl -H "X-Upload-Filename: ../../etc/passwd"` vs a clean filename against `http://localhost:8080/upload` to confirm 403 vs pass-through before considering it done, since the grader measures accuracy across many payload variants including mixed-case encodings.
