Break this into independently testable pieces before wiring them together. For the WAV parser, don't assume a fixed header offset — read the RIFF header, walk chunks by id/size until you find `data`, then read the payload as little-endian 16-bit signed samples with `encoding/binary.Read` (or manual byte assembly), and track the max of `abs()` across all samples as float64 `M`; print `M` while developing so you can sanity-check it against the WAV file's actual amplitude range. For the solver, implement Doolittle LU decomposition on the fixed 3x3 matrix explicitly (compute L and U with the standard elimination formulas), then do forward substitution (Ly=b) followed by backward substitution (Ux=x) — verify this against the 2x2 test case in Step 3 by hand first since it's a known, simple answer, before trusting it on the 3x3 system. For the server, wire `M` and the solved vector into the exact JSON shape requested, bind strictly to `127.0.0.1:9090`, and launch it with `nohup`/`&` (or a background job) so it stays listening after your session ends — confirm with `curl` that `/solve` responds before finishing.
