Use `os.walk('/home/user/configs')` but explicitly filter `dirnames` in-place each iteration to drop any entry where `os.path.islink(os.path.join(root, d))` is true — `os.walk`'s own `followlinks=False` default only stops it from descending into symlinked dirs but will still list them, and a naive script that doesn't filter can still loop or misbehave, so be deliberate about pruning before recursion continues. For each file ending in `.oldconf`, open with `encoding='ISO-8859-1'` (or `latin-1`) to read, since this is what preserves the non-ASCII bytes correctly, then do plain string `.replace()` calls for the two exact substrings (order doesn't matter since they don't overlap), write the result to a sibling path with `.newconf` extension using `encoding='utf-8'`, and only delete the original `.oldconf` file after the write succeeds (check for exceptions, or write-then-verify-then-delete, so a mid-run crash doesn't lose data). Leave any non-`.oldconf` files and the symlink itself completely untouched. Run the script once and confirm no `.oldconf` files remain, the symlink still resolves, and the untouched file is byte-identical.
