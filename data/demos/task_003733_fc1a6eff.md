Do not guess the algorithm parameters — recover them by transcribing `/app/meeting_recording.wav` first (e.g. with a local speech-to-text tool, or by listening and noting exact numbers), and extract every parameter mentioned: the initial score per node, the retention fraction kept by a node, the distribution fraction sent to neighbors, the fallback behavior when a node has no outgoing edges, the base addition per iteration, and the exact number of iterations. Only after you have the precise transcript should you implement the simulation: load `/app/edges.csv` into an adjacency structure (source -> list of targets), initialize every node's score to the stated initial value, then iterate the stated number of times, each iteration first computing each node's outgoing distribution based on its *current* score (compute all updates from the prior iteration's state before applying any, to avoid order-dependent bugs), then adding the base addition afterward. Pay attention to nodes with zero out-degree — the audio specifies special handling for them. Finally write `node,score` (node as string, score as float) to `/home/user/influence_scores.csv` for every node in the graph.
