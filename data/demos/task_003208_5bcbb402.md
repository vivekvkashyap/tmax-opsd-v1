Split the work into two clean stages inside `/home/user/pipeline.sh`. Stage 1: extract primer matches with `grep -oE` using a POSIX ERE like `GATA[ATCG]{5,10}CACA` against `/home/user/dna_sequences.txt` (note overlapping/adjacent matches on one line need care — `grep -oE` finds non-overlapping leftmost matches in order, which matches the spec). Stage 2: for each extracted sequence, compute its length L and feed L into a single `awk` invocation that implements the LCG exactly as specified — critically, seed must persist as ONE running value across the *entire* awk execution (initialize `seed=123` in a `BEGIN` block, not per input line/sequence), advancing it every iteration in the order sequences are processed, since resetting it per-sequence will give wrong numbers. Inside awk, for each L run exactly 1000 iterations updating `seed = (1103515245*seed + 12345) % 2147483648`, computing `r = seed/2147483648`, `x = r*L`, `y = x*x/L`, and accumulating the running sum; after 1000 iterations compute that sequence's integral as `(sum_y/1000)*L`, and keep a grand total across all sequences. Format the final grand total with `printf "%.2f\n"` to `/home/user/total_energy.txt`. Make the script executable and re-run it twice to confirm the LCG's determinism gives identical output both times.