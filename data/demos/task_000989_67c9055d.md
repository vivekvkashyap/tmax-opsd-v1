Install NetCDF headers first (`libnetcdf-dev`) and pick a Go NetCDF binding (e.g. `go-netcdf`) via `go mod init` + `go get`, then write a small probe program to confirm you can read the `temperature` variable's shape and values before building the solver — this avoids debugging I/O and math simultaneously. Build a boolean mask marking cells equal to `-999.0`, then reset those cells to `0.0` as the initial guess. Implement one Jacobi sweep as a pure function operating on a snapshot of the previous grid (read from old array, write to a new array) so that neighbor averages within an iteration never use values already updated in the same iteration — this is the classic bug that turns Jacobi into Gauss-Seidel. Parallelize by splitting row ranges across goroutines with a `sync.WaitGroup`, each goroutine only writing its own row slice of the new grid to avoid races. After each full sweep, compute the max absolute difference between old and new grids only over the masked (originally-missing) cells, and loop until it drops below `1e-4`. Only sum the full 50x50 grid after convergence, and format to exactly 4 decimal places when writing `/home/user/result.txt`.
