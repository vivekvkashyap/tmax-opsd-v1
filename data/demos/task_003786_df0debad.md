Treat this as a multi-format archaeology pipeline: unpack, identify, parse, then act, verifying each layer before trusting it. Extract `sensors.tar.gz` with `tarfile`, then inspect each nested zip's file listing (`zipfile.ZipFile(...).namelist()`) to find which one actually contains `telemetry.bin` — don't assume it's the first one, since the task deliberately includes a decoy/duplicate zip; read from whichever one(s) actually have it. When parsing `telemetry.bin`, confirm the first 12 bytes literally equal the ASCII header before trusting the file, then read the next 4 bytes as little-endian unsigned 32-bit (`struct.unpack('<I', ...)`) to get the offset — seek exactly to that byte offset (not offset+something) before reading the text tail. Use a precise regex like `r'\[CRITICAL\] EVENT_TRIGGERED at T=([\d.]+)s'` on the decoded text block to pull out timestamps; don't hand-split on whitespace, since debug lines are mixed in. For each timestamp, shell out to `ffmpeg -ss <t> -i experiment.mp4 -frames:v 1 -q:v 2 <path>` (using `-ss` before `-i` for fast+accurate seeking) and name files exactly `frame_<timestamp>.jpg` matching the timestamp's natural string form. Finally, iterate the extracted frames and `os.symlink` (not copy) any with timestamp > 10.0 into the curated directory, then touch `/app/DONE`.
