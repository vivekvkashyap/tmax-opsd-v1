Localize the bug before touching anything else: read `/home/user/sim_engine/src/integrator.rs`'s step-size adaptation carefully — an adaptive RK integrator should shrink `dt` when the local error estimate exceeds tolerance and grow it when error is comfortably below tolerance, so check the scale factor's ratio direction (it should behave like `tolerance / error`, not `error / tolerance`) and fix only that inverted relationship rather than rewriting the whole integrator. After fixing, `cargo build --release` and run the simulation, redirecting stdout to `/home/user/sim_output.csv`, and manually eyeball a few rows to make sure there are no `NaN`s (a divergent/unstable step-size policy is exactly what produces them). For the MSE comparison, join `sim_output.csv` and `/home/user/reference_data.csv` on exact-matching `time` values only (don't interpolate) and compute mean squared error of `node1_x`, writing `MSE: <value>` to `/home/user/evaluation.log`. Finally write a small `matplotlib` script plotting both trajectories' `node1_x` vs `time` and save as SVG to `/home/user/trajectory_plot.svg`.
