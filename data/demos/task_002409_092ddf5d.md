First locate the perturbation: grep the vendored package at `/app/vendor/tabular` for a hardcoded boolean/flag (likely in a `config.go`-style file) that disables imputation and causes rows with missing values to be dropped entirely — flip it to the non-destructive default so `JoinAndImpute`-style logic actually fills missing fields (e.g. with column mean) instead of discarding rows. Verify the fix by reading how the processing function branches on that flag. Then scaffold `/home/user/filter_data.go` as a small Go program with its own `go.mod`, adding a `replace github.com/data-prep/tabular => /app/vendor/tabular` directive so it builds against your locally-patched code (not any external copy). The program should: read the CSV path from argv, load and join it with `/app/data/metadata.csv` on `user_id` using the fixed library, then filter out rows with `risk_score > 100.0` or `risk_score < 0.0`, and print the sanitized CSV to stdout. Test against `/app/corpus/clean/` (must yield >0 data rows) and `/app/corpus/evil/` (must yield exactly 0 data rows) using `go run /home/user/filter_data.go <file>` for each corpus file before finishing.