Break this into three exact stages and keep the pixel-decoding precise. First, extract frames from `/app/graph_evolution.mp4` with `ffmpeg -i graph_evolution.mp4 frame_%02d.png` (1fps source, 10 frames) — verify you got exactly 10 frames and that frame ordering/frame_index numbering matches the video's temporal order (0-indexed vs 1-indexed matters for `frame_index` in the CSV, so pick one convention and be consistent). For each frame, read it as a 100x100 image (e.g. with PIL/OpenCV/numpy) and for each of the 10x10 blocks of 10x10 pixels, sample a pixel from the block interior (avoid edge/compression artifacts — sample near the block center) and classify it as an edge if the pixel is closer to black than white (use a threshold like `< 128` on all channels, not an exact `(0,0,0)` match, since video codecs introduce slight color drift) — row i, col j gives edge i→j. Write every detected edge as `frame_index,source,target` rows to `edges.csv`. Second, write `/home/user/aggregate.cpp` to read that CSV (validate the header exactly matches the schema, reject malformed rows), tally per-edge frame counts in a map, then compute per-node `total_out_weight` (sum of outgoing edge weights) and `total_in_weight` (sum of incoming edge weights) for nodes 0-9, and write `/home/user/node_stats.csv` sorted ascending by node with the exact header `node,total_out_weight,total_in_weight`. Since the verifier requires zero tolerance, get the video-to-adjacency-matrix decoding exactly right before worrying about the C++ aggregation logic.
