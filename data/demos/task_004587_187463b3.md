Work through the four phases independently and verify each output file precisely. Phase 1: `grep`/extract the HTML comment text after `<!-- PAYLOAD:` from `/home/user/evidence/index.html`, base64-decode it first (`base64 -d`), then hex-decode the result (`xxd -r -p` or `python3 -c "bytes.fromhex(...)"`) — the order matters, it's base64-of-hex, not hex-of-base64 — and write only the resulting IP string to `/home/user/c2_ip.txt` with no extra formatting. Phase 2: read `backup.sh` and spot the unsanitized shell interpolation of a user-controlled argument into a command executed via `eval` or similar — that's OS command injection, whose canonical CWE ID you should recall/look up and write as `CWE-XXX` to `vulnerability.txt`. Phase 3: copy `sshd_config_compromised` to `hardened_sshd_config` and edit only the three named directive lines in place (use `sed -i` with anchored patterns like `^PermitRootLogin` to avoid touching comments or spacing elsewhere), leaving every other byte identical. Phase 4: write the single literal CSP `<meta http-equiv="Content-Security-Policy" content="default-src 'self'">` tag to `csp_meta.txt`. Double check each file for trailing whitespace/newline issues since exact-match verification is likely.
