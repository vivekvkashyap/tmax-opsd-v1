For the secret recovery, don't just look at the current file contents — inspect the repository's history since the secret was removed in a later commit. Run `git -C /home/user/metrics_build log --oneline --all -- publish.sh` to find the commits touching that file, then `git -C /home/user/metrics_build log -p -- publish.sh` (or `git show <commit>:publish.sh` for the commit before the removal) to view the old file content and locate the token string starting with `SEC-`; write only that exact value (no quotes, no surrounding text) into `/home/user/recovered_token.txt`. For the precision issue, the problem is that `awk`'s default `print` uses `OFMT` (typically `%.6g`), which truncates the mean's decimal places — fix `compute_mean.sh` by formatting the final result explicitly with `printf "%.9f\n", sum/count` inside the `END` block instead of `print sum/count`, so it always emits exactly 9 digits after the decimal point regardless of the underlying value. After both fixes, run `bash /home/user/metrics_build/test_build.sh` and confirm it reports success and writes `/home/user/build_success.log`.
