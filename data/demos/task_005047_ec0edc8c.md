Start by transcribing /app/instructions.wav (e.g. with `openai-whisper` or another local ASR tool) and read the transcript carefully — it specifies the exact port, endpoint paths, query parameter names, and the algorithms required (cosine similarity for /similar, Ridge regression with cross-validated alpha for /predict); do not assume defaults, extract them from the audio. Load /app/customers.csv with pandas, and for /similar compute cosine similarity between the requested uid's feature vector and every other row's (numpy/sklearn `cosine_similarity`), sort descending, exclude the row matching the requested uid itself, and return the top 3 ids as a JSON array. For /predict, fit `RidgeCV` (or manual K-fold Ridge) with the exact alpha grid mentioned in the audio, cross-validated with the specified fold count, then refit on the full dataset with the selected alpha and return the prediction for the requested uid as a plain JSON float. Build this as a Flask/FastAPI app bound to the host/port from the audio, and launch it detached (e.g. `nohup ... &` or a background process) so it stays reachable after your turn ends — verify with a `curl` against both endpoints before finishing.