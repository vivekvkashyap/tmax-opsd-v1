Write the C pipeline as clearly separated stages so each is easy to verify independently. Use `regcomp`/`regexec` from `<regex.h>` with `REG_EXTENDED` to validate each `Email` field against the exact anchored pattern given; skip any row that fails to match or is malformed CSV. For masking, locate the `@` in the validated email, keep index 0, insert literal `***`, then append everything from `@` onward unchanged. Aggregate by masked email using a simple linked list or dynamically-grown array (no need for a hash table at this scale), summing `TransactionValue` as you parse each row with `atof`/`strtod`. Before writing output, sort the aggregate array with `qsort` using a comparator that orders by total value descending, breaking ties with `strcmp` on the masked email ascending. Print floats with `%.2f` exactly. After compiling with `gcc -o /home/user/process /home/user/process.c` and running it, create `/home/user/analytics.db` and import the CSV into `UserTotals` using sqlite3's CSV import mode (`.mode csv` + `.import`) after creating the table with matching column types — verify header row isn't imported as data.
