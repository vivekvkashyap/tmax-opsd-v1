Build this as a clean pipeline: (1) parse `/home/user/binding_data.txt` line by line, keep only records whose sequence starts with `ATGCGATCG`, strip the primer; (2) enumerate all 64 3-mers in alphabetical order (nested loop over "ACGT" three times, or generate and sort) to fix column order, then for each trimmed sequence slide a window of length 3 across it and increment the corresponding count column — this gives feature matrix X (rows=records, cols=64) and target vector y; (3) implement ridge regression using Eigen3: form X^T X, add lambda*I (lambda=2.0) on the diagonal, then solve for w via a stable Eigen solve (e.g. `.ldlt().solve(...)` or `.colPivHouseholderQr()`) rather than an explicit `.inverse()` call, since near-singular matrices make naive inversion numerically unstable; (4) multiply X^T y and solve for w; (5) write each of the 64 kmer,weight pairs to `/home/user/kmer_weights.csv`, rounding to exactly 4 decimal places with consistent formatting (e.g. `printf("%.4f")`). Double check the kmer-to-column mapping matches alphabetical order exactly, since a mismatched mapping is the most common source of a wrong answer even with correct math.
