First extract the initial edge table from `/app/init_topology.png`: use OCR (tesseract, possibly with `--psm 6` for tabular text, or crop/preprocess the image first for better accuracy) to read the Source/Target/Cost rows, sanity-check the extracted numbers look plausible (integer node ids, reasonable float costs), then write them to `/home/user/init_graph.csv` with no header. For the C query engine, since the verifier applies tens of thousands of updates and hundreds of thousands of queries under a tight 0.5s budget, avoid re-running Dijkstra from scratch per query naively with poor data structures — use an adjacency-list graph keyed by node id with a hash map or array indexed by node id (nodes go up to 100,000, so a flat array avoids hashing overhead), and a binary-heap-based Dijkstra (or even a simple bucket queue if costs are bounded) for shortest paths, applying ADD/DEL/UPD updates as direct adjacency mutations before running queries. Profile with a large synthetic dataset yourself (generate tens of thousands of random updates/queries) to confirm you're within the time budget before considering it done, and make sure output cost is `-1.0` for unreachable targets.
