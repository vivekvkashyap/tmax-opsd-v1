Bring the stack up bottom-to-top and test each layer before moving on. Start Redis and the Flask app, fixing the hardcoded wrong Redis port in `app.py` to `6379`, and add a `/health` route that pings Redis and only returns 200 with the ok/connected JSON if the ping succeeds — verify with a plain `curl localhost:5000/health` first, before TLS is in the picture. Generate a self-signed cert/key with `openssl req -x509 -newkey rsa:2048 -nodes -keyout server.key -out server.crt -days 365 -subj '/CN=localhost'`, then fix `nginx.conf` to `listen 8443 ssl;` with `ssl_certificate`/`ssl_certificate_key` pointing at those files and `proxy_pass http://127.0.0.1:5000;`; validate config syntax with `nginx -t` before starting it. For the validator, write `/home/user/validator.py`'s `validate_payload(filepath)` to: load and parse JSON (catch parse errors -> False), recursively walk all keys and string values checking for `../` or `..\\` substrings (-> False), and check `log_data` string length > 1024 (-> False); test it directly against every file in both `corpus/clean/` and `corpus/evil/` before wiring it into Flask, since the app integration only makes sense once the classifier is 100% correct standalone. Wire `validate_payload` into `/ingest` (write the POST body to a temp file first, then validate, returning 400 on failure). Finally write `test_stack.sh` to curl `/health` and loop-POST both corpora with `curl -k`, logging status codes as required.