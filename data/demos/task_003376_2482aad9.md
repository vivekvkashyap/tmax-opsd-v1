Build `/home/user/audit.sh` as a pipeline that joins three heterogeneous sources by user_id, not as a single monolithic script. Steps: (1) parse `users.json` with `jq` into a lookup of id→{dept,clearance}; (2) parse `org_chart.txt` into a manager lookup keyed by employee_id (each line is "manager_id employee_id"); (3) parse `systems.json` to get `req_dept` and `req_clearance` for the given `$1` system_id argument; (4) filter `access_logs.csv` rows where system_id matches the argument; (5) for each row, look up the user's clearance/dept, look up their manager's dept (null if no entry), and evaluate the two violation conditions independently so you can build the correct `violation_reason` string when one or both fail — check clearance first, dept-mismatch-with-manager-also-mismatched second, and combine text when both are true; (6) keep only violating rows; (7) sort by timestamp descending (numeric sort, not lexicographic) and take the top 3 with `sort -rn` and `head -n 3`; (8) emit strict JSON via `jq -n` with the exact key names and order shown in the spec. Test with `chmod +x audit.sh && ./audit.sh sysB` and inspect the output file before finishing.