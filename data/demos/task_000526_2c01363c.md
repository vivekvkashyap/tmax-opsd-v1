Tackle the five objectives as independent passes over the raw_data. For redaction, stream-parse auth_logs.jsonl line by line, and for any top-level key exactly named `password` or `secret_token` replace its value with the literal string `[REDACTED]`, writing each modified line back as JSON to redacted_logs.jsonl — while doing this pass, also tally failed_login counts per IP in a dict. For firewall rules, filter that tally to IPs with count strictly > 3, sort the IP strings lexicographically, and emit one `iptables -A INPUT -s <ip> -j DROP` line per IP. For SSH hardening, read sshd_config.insecure line by line and use regex/string matching to find any line (commented or not) starting with optional `#` then `PermitRootLogin` or `PasswordAuthentication`, rewriting it to the uncommented directive with value `no`, leaving all other lines untouched. For integrity, compute `sha256sum` of each file under raw_data/bin and compare against the recorded checksums.sha256 to find the one mismatching filename. Finally assemble audit_summary.json with the compromised filename and malicious IP count, and place every output under `/home/user/audit_task/results/` exactly as named.
