Extract the threshold from `/app/specs.png` with OCR (`tesseract /app/specs.png stdout`, preprocessing the image with e.g. `convert`/thresholding first if raw OCR is noisy) and parse the numeric value out of the recognized text programmatically rather than hardcoding a number you eyeball — read whatever the image actually contains. In the script, join `table1.csv` and `table2.csv` on `user_id` (e.g. via `join` after sorting both by key, or a short Python/pandas snippet) so mismatched or missing user_ids don't silently skew the correlation. Compute the Pearson correlation coefficient with a reliable method (e.g. `scipy.stats.pearsonr` or `numpy.corrcoef`) over the joined `metric_x`/`metric_y` pairs. Apply the pass/fail rule exactly as specified — strictly less than threshold means exit 1 (anomalous), and greater-than-or-equal means exit 0 (valid); get the boundary condition right since the grader tests many directories and boundary mistakes fail entire categories. Test your script against a couple of self-made directories with known correlations near the threshold to confirm the exit codes before considering it done.
