Work the three problems independently and verify each in isolation before wiring them together. (1) Linking: run `ldd` on the compiled daemon to confirm it can't find `libpayload.so`, then fix the `Makefile`'s link step by adding an rpath flag (e.g. `-Wl,-rpath,$(PWD)` or `-Wl,-rpath,'$$ORIGIN'`) rather than exporting `LD_LIBRARY_PATH` in the startup script — the task explicitly requires the binary to resolve the library at runtime without that env var. Rebuild and re-check with `ldd`/`readelf -d` that RPATH/RUNPATH is set. (2) Nginx 502: read `nginx.conf`'s `proxy_pass` target and compare it against the actual port the daemon binds to in `daemon.c`; correct the mismatch and restart both services. (3) Filter logic: implement `validate_payload` to parse the first 4 bytes as a big-endian magic number and the next 4 bytes as a big-endian length field, reading multi-byte integers manually (shift/OR bytes, don't assume host endianness or cast a `uint32_t*`), reject anything with wrong magic, wrong declared length vs actual remaining bytes, or a buffer shorter than 8 bytes. Compile, test against both corpora directly before trusting the end-to-end HTTP path, then produce `build.log` from the successful `make` run.
