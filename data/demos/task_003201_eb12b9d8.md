Work the three pieces independently, then wire them together at the end. For the video, use `ffmpeg` to extract frames (e.g. `ffmpeg -i ci_test_run.mp4 frame_%04d.png` or pipe raw RGB frames) and script a check per frame for "completely red" (red channel near 255, green/blue near 0 — allow small tolerance for compression artifacts) to get an exact frame count `N`; don't estimate visually. For `server.c`, first find and fix the buffer overflow in the HTTP header/request parser (a fixed `char buffer[N]; strcpy(...)` on unbounded input is the classic culprit — replace with bounds-checked copying such as `strncpy`/`snprintf` sized to the buffer, or dynamic allocation, and ensure the server doesn't die on a very long URL) and the memory leak (find any `malloc` in the per-request path missing a matching `free`). Then rewrite `calculate_optimal_score` as textbook 0/1 knapsack DP over the hardcoded `weights`/`values`/`capacity` arrays already in the file — a 2D or rolling 1D DP table indexed by weight capacity, not the existing greedy logic. Implement `/health` and an authenticated `/score` endpoint that checks the `Authorization: Bearer` header, parses `crash_count` from the JSON body, multiplies it by the DP result, and returns the JSON. Compile, launch the server in the background, and curl-test both endpoints (including an oversized-URL request and a missing-auth request) before leaving it running.
