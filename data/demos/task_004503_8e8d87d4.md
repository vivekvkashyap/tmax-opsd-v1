Start by recovering the PDE parameters from the audio: convert `/app/voicemail.wav` (e.g. `ffmpeg` to a clean WAV, then a local speech-to-text tool such as `whisper`) and transcribe it carefully — listen for the diffusivity, domain length, and end-time values spoken aloud, and double-check any decimals. Before coding a solver, derive the exact analytical solution by separation of variables for this IC/BC pair: it factors into a single spatial sine mode times an exponential decay in time — work this out on paper first so you have a ground truth to check against. Then implement a numerical solver (finite-difference in space, an implicit or Crank-Nicolson scheme in time is safer than explicit Euler, which can be unstable or too inaccurate at the required tolerance) with a fine enough grid and small enough timestep to make the max absolute error tiny. Generate exactly 101 evenly spaced x points over [0, L], evaluate u at t=T, and write the CSV with headers `x,u`. Validate by comparing your numerical output directly against your derived analytical formula before finalizing.
