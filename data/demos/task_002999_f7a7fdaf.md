Work through this as a deployment pipeline problem, not a one-off fix. First inspect `/home/user/nginx/nginx.conf` to read the `proxy_pass` target port — that's the port your backend must actually bind to; edit `app.py` in the workspace clone to listen there instead of guessing. Next build the `post-receive` hook: it runs with `GIT_DIR` set to the bare repo, so inside it explicitly `git --work-tree=/home/user/deployed_app checkout -f master` (or the correct branch) to materialize the code, then kill any prior `app.py` process (e.g. `pkill -f app.py` or grep `ps` output for the pid) before relaunching `python3 /home/user/deployed_app/app.py &` with output redirected so the hook doesn't hang waiting on the child. Make the hook executable (`chmod +x`). For the automation script, use Python's `subprocess` to run `git add`, `git commit -m "..."`, and `git push origin <branch>` from the workspace directory, checking return codes. After running it, give the new backend a brief moment to bind before curling — a push can return before the hook's background process is actually listening, so add a short retry/sleep loop around the `curl` in your verification.
