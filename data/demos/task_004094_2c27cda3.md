Treat `/app/integrator_oracle` as a black box: probe it with varied initial-guess/target-constant argument pairs and observe stdout format, precision, and timing (use `time` or shell timing) to learn the expected output convention and rough single-threaded runtime — do not try to disassemble or reverse-engineer its internals beyond this. For `/home/user/integrator.c`, replace the naive fixed-step/divergent scheme with a genuinely robust combination: use Newton-Raphson (or bisection fallback) for root-finding on the integral equation, and adaptive Simpson's rule (or RK4) for the integration itself so step size shrinks where the integrand curves sharply — a common failure is only fixing one of the two (root-finder or integrator) and leaving the other naive. Add `#pragma omp parallel for` with a reduction clause around the independent sub-interval integration work, and compile with `-O3 -fopenmp`. For `/home/user/test_regression.sh`, generate 100 random (guess, C) pairs, time both binaries per-input, then implement a real bootstrap: resample the 100 per-input speedup ratios with replacement many times, compute the mean each time, and report the 2.5th percentile as the CI lower bound. Verify accuracy (`|out_fast - out_oracle| <= 1e-6`) and speedup (>=2.0x) empirically before finalizing.