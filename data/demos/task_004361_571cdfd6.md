Fix and verify each layer independently before chaining them. (1) Dependency conflict: run `cargo build` in `/app/compute-engine` to see the exact tokio/axum version mismatch, then adjust `Cargo.toml` versions to a compatible pair (e.g. axum 0.6.x needs `tokio` with the `"full"` feature set and matching hyper version — check axum's own Cargo.toml for its expected tokio range) rather than blindly bumping to "latest". (2) Parsing panics: replace any `.unwrap()`/`.expect()` on `str::parse::<f32>()` with a pattern that parses to `f64`, checks `.is_finite()` (this naturally rejects NaN/Infinity), and simply skips (via `filter_map`) any value that fails to parse or isn't finite — so "NaN", "infinity", "N/A" text are silently dropped, not just NaN floats. (3) Variance: rewrite using `f64` and a numerically stable one-pass method (Welford's algorithm: maintain running mean and M2, update per sample, variance = M2/n) instead of the sum-of-squares formula, to avoid catastrophic cancellation on large-baseline data — verify manually against the large-number test case before moving on. (4) Nginx: add a `location /compute { proxy_pass http://127.0.0.1:9000; }` block listening on 8080. Start both services via `start.sh` and curl-test each of the three scenarios in the spec directly before considering the task done.
