First run `tesseract /app/target_config.png stdout` (or equivalent OCR) to recover the target IP and port range, then hardcode those recovered values as C++ literals in payload.cpp rather than parsing them at runtime — this saves both code and binary size. Structure the C++ program as: (1) a tight loop that attempts a raw TCP `connect()` to each port in the recovered range with a short timeout, treating the first successful connect as the open port; (2) an OpenSSL TLS handshake over that socket using `SSL_CTX_new`/`SSL_connect` (link with `-lssl -lcrypto`); (3) an `SSL_read` loop until EOF to capture the secret string sent by the server, then write it verbatim to /home/user/loot.txt. Test iteratively against `/app/start_mock_server.sh` before worrying about size. Only once functional correctness is confirmed, attack the size constraint: compile with `g++ -Os -s -static-libgcc -fno-exceptions -fno-rtti`, strip unused OpenSSL error-string tables, avoid `<iostream>` (use `printf`/raw syscalls instead) and avoid pulling in libstdc++ exception machinery, then verify with `stat -c %s /home/user/payload` against the ≤35000 byte threshold. Iterate flags if oversized rather than rewriting logic.
