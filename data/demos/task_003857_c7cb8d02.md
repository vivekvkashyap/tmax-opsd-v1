Work through this as a pipeline, testing each stage independently before wiring them together. First compile the C file into a position-independent shared object: `gcc -shared -fPIC -o /home/user/libmetrics.so /home/user/metrics.c`. Next, decode the binary: read `/home/user/data.bin` as bytes and decode with `.decode('utf-16le')` (or `iconv -f UTF-16LE -t UTF-8`), then split into individual expression lines, stripping any BOM or trailing whitespace/newlines. For evaluation, do not blindly call `eval()` on raw strings — write a small safe evaluator (or sanitize input to only digits/operators/parentheses/`**`/`/`/`*`/`+`/`-`) since the expressions may use Python-style `**` for exponentiation. Load the shared library with `ctypes.CDLL(...)`, set `record_metric.argtypes = [ctypes.c_double]`, and call it once per evaluated result, appending each formatted result to a list you then write one-per-line to `/home/user/eval_results.txt`. Finally, rerun the whole evaluation script (not just import it) under `valgrind --leak-check=full python3 your_script.py 2> /home/user/valgrind.log` so the leak inside `record_metric` gets exercised by every call and reported. Confirm `metrics.out` reflects the running total the C code accumulates across calls, in the same order the expressions appear in `data.bin`.
