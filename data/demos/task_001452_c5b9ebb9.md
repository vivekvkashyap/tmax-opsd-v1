This is a black-box reverse-engineering task: reimplement `/app/graph_materializer_bin`'s exact relational-to-graph mapping in Python, matching stdout byte-for-byte. Start by inspecting the schema of `/home/user/data.db` with `sqlite3 /home/user/data.db .schema` to see the actual tables/columns/foreign keys (Users, Departments, Roles, Projects, and likely a UserRoles link table). Then probe the binary with small hand-crafted JSON configs via `echo '{"source":..., "target":..., "link":...}' | /app/graph_materializer_bin` for each plausible table pair, and inspect the CSV it emits (`source_node,target_node,edge_type` order, header or no header, delimiter) as well as row counts — a naive join between two tables lacking a direct foreign key (e.g. Users→Projects) will explode into a cross join, so check whether the binary instead uses an explicit link table or a specific FK column (e.g. `owner_id`) to constrain that join. In your Python script, connect to the SQLite DB and build the equivalent explicit `INNER JOIN ... ON <fk column>` (or the link-table join) for each source/target/link combination you observe, avoiding any join condition that isn't backed by an actual key relationship. Verify your script's output against the binary's output for several different JSON configs before finishing, matching CSV formatting exactly.