Work this top to bottom since later phases depend on earlier recovered secrets. Phase 1: OCR `/app/legacy_key.png` (e.g. `tesseract`) to recover the base text of the master key; note the task tells you it's missing a 2-character lowercase suffix, so brute-force all 26x26 combinations by, for each candidate, computing `SHA256(base+suffix)` as the AES-256-GCM key, using the fixed all-zero 12-byte IV, and attempting to decrypt `/app/db_backup.enc` (GCM decryption fails/tag-mismatches on wrong keys, so loop until one succeeds and yields valid JSON) — write the full recovered key (base+correct suffix) to `/app/recovered_key.txt`. Phase 2: read `/app/old_service.sh` and notice it passes a secret via a command-line argument to a subprocess (`--password="$DB_PASS"`), which any local user can read via `/proc/<pid>/cmdline` — identify and write the matching CWE id to `/app/audit.txt`. Phase 3: build a Rust HTTP service (axum/actix-web) on `127.0.0.1:8080` exposing `POST /rotate`; validate the `Authorization: Bearer <key>` header against the phase-1 recovered key (401 if missing/wrong), then re-encrypt the same decrypted JSON credentials using AES-256-GCM with key `SHA256(new_key)` and the specified fixed all-ones IV, returning base64 of ciphertext+tag concatenated. Run it in the background and confirm it's actually listening before finishing.
