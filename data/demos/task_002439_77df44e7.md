Since no external libraries are available, implement your own simple hash table (array of buckets + linked list, e.g. djb2 hash) keyed on `server_id|config_key`, storing the config_value string; load `day1.csv` into one table and `day2.csv` into another with `fgets` + manual CSV splitting (fixed buffers sized for the 256-char max line). Iterate the union of keys from both tables to classify each as `ADDED` (only in day2), `DELETED` (only in day1), or `MODIFIED` (present in both with a differing value) — unchanged entries in both are simply skipped. Collect results into a dynamically-growing array of structs, then sort with `qsort` using a comparator that does `strcmp` on `server_id` first, and `config_key` as the tiebreaker. Write the CSV with `old_value`/`new_value` left empty exactly where the status implies it, keeping values as raw strings (don't reparse as numbers, or you risk changing their formatting). The verifier does an exact `diff`, so watch trailing newlines and field spacing closely — compile, run, and `diff` your own output against your expectations before finishing.