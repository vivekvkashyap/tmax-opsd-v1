Work through the four subtasks in order, testing each output before moving on. For the log extraction, recursively find `.log` files (`find /home/user/dataset_archive -name '*.log'`), grep for lines starting exactly with `ERROR: CORRUPT_RECORD ID=`, then use `awk`/`sed` to pull out the numeric ID and the bracketed trailing timestamp — a robust approach is a regex capturing `ID=([0-9]+)` and `\[([^\]]+)\]` and reformatting as `ID - TIMESTAMP`; sort the results numerically with `sort -n` (or `sort -t' ' -k1,1n`) before writing to `/home/user/corruption_report.txt`, watching for leading zeros in IDs (e.g. `002`) that must still sort correctly and print as given in output. For metadata repair, write `/home/user/generate_meta.py` to walk the tree for `.dat` files, check for a sibling `.meta` file with the same base name, and for any missing one, open the `.dat` file in binary mode, read exactly the first 8 bytes, and write their lowercase hex representation (`data.hex()`) with no spaces to the new `.meta` file — don't touch existing `.meta` files. For the summary script, count lines in the report and count newly-created `.meta` files (track this count during generation, don't just recount all `.meta` files, since one already existed) and emit exact-key JSON. Finally launch `python3 -m http.server 8080` from within the archive directory in the background, capture its PID with `$!` or `pgrep`, and write it to `/home/user/server.pid`.