Do the two parts independently and get each fully correct before combining. For the video: extract frames with `ffmpeg -i /app/optical_token.mp4 frame_%03d.png`, then for each frame in order (frame 0 first) determine if it's solid black or solid white (sample a pixel or average brightness — a threshold works since frames are solid colors), map black→0, white→1, and assemble the 32 bits MSB-first (frame 0 is bit 31, frame 31 is bit 0) into an unsigned 32-bit integer via bit-shifting — do not guess the value, derive it programmatically and print it to confirm before hardcoding it into your test invocations. For the parser: since you don't have the oracle's source, treat `/app/oracle_secure_parser` as a black box and probe it with varied stdin payloads (empty, too-short, exact match, wrong secret, huge/garbage input, embedded nulls) to infer its exact contract — the description tells you the shape is `AUTH-<secret>-<rest>` — but confirm exit codes and stdout/stderr text precisely via observation rather than assumption. Implement `/home/user/secure_parser.c` using safe bounded reads (`fread` into a fixed buffer with explicit size checks, `strncmp`/`snprintf` instead of unbounded `strcpy`/`sprintf`) so it never overflows regardless of input length, and mirror the oracle's observed output/exit-code behavior for every case you tested, including malformed/oversized/binary input. Compile with `gcc -O2 -Wall` and re-test against a wide range of fuzz-like inputs comparing your binary's output to the oracle's before considering it done.