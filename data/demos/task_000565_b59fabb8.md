Write a straightforward Go program using only `encoding/csv` and standard maps/slices — no external graph library needed. Parse `nodes.csv` into an `id -> name` map, and parse `edges.csv` into an adjacency structure `parent_id -> []child_id` representing "depends on" edges. Find the node id whose name is `Core_System`, then do a recursive (or iterative DFS/BFS with a visited set) traversal starting from that id, following child edges outward and collecting every reachable id into a set — using a visited/seen set is important both for correctness (avoiding infinite loops if the graph has cycles) and to naturally deduplicate reachable nodes if two ancestors depend on the same descendant. Exclude the starting `Core_System` id itself from the result set. Map the collected ids back to names via your id->name map, sort the resulting name list alphabetically with Go's `sort.Strings`, and write one name per line to the output file (ensure a trailing newline convention consistent with a straightforward `Fprintln` loop). Run it with `go run` and spot-check the output against your own manual trace of the dependency edges before finishing.
