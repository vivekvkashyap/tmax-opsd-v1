First inspect the table manually (`sqlite3 /home/user/etl_locks.db "SELECT * FROM lock_waits;"`) to see the shape of the wait graph before writing SQL blind. Build the query as a recursive CTE that starts from each row as a length-1 path `(start, current, path_list, depth)` and recursively joins `lock_waits` on `holder_tx = waiter_tx` of the next hop, extending the path each step; a cycle is detected when the new `current` equals the original `start` — filter the recursive results down to rows where that closing condition holds, then extract the distinct set of `tx_id`s that appear in that closed path. Once you have just the cycle's transaction rows (join back to the base table to get each one's `wait_start_ms`), apply a window function `RANK() OVER (ORDER BY wait_start_ms ASC)` (or `ROW_NUMBER()` if ties can't occur) to produce `cycle_rank`. Run this as one query via `sqlite3` or Python's `sqlite3` module inside `process_deadlock.py`, write results ordered by `cycle_rank` to CSV with the exact header `tx_id,wait_start_ms,cycle_rank`, and test against the live DB before considering it done — don't hand-derive the cycle from eyeballing rows.
