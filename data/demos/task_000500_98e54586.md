Build a robust reader, not a naive one — the whole point of this task is surviving a writer that mutates the file underneath you. In your HTTP handler, open `/home/user/app/logs/rotating.log.gz`, decompress it (e.g. Python's `gzip` module) and parse records by reading the fixed 4-byte magic (`EVT\x01`), then a 4-byte big-endian length (`struct.unpack('>I', ...)`), then exactly that many bytes as UTF-8 JSON (`json.loads`). Wrap every read/decompress step in try/except catching `EOFError`, `zlib.error`/`gzip.BadGzipFile`, `struct.error`, and short-read conditions (check you actually got the full number of bytes requested before parsing) — on any such failure, stop parsing and return whatever complete records you already collected rather than crashing or raising. Use Python's built-in `http.server` (or similar) so the process stays alive across repeated requests, set `Content-Type: application/json`, and return a JSON array of the decoded payload objects. Test by curling `/latest-events` multiple times in a row to confirm no crash even when a rotation happens mid-read.
