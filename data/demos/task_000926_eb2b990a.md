Transcribe `/app/incident_audio.wav` first (e.g. `whisper /app/incident_audio.wav --model base` or another STT tool available) and read the output text carefully to identify the two filtering criteria it states (an environment value and a retention-days value) — don't guess these, they must come from the actual transcript. Then inspect `/app/backups.db` schema and `/app/generate_report.sql` to see the implicit cross-join bug (likely a `FROM a, b` with a missing/wrong join predicate causing a cartesian blowup); rewrite as `/home/user/fixed_report.sql` with explicit `JOIN ... ON` clauses across `servers`, `backups`, `dependencies`, use a recursive CTE to compute servers within 2 hops of 'core-db' in the dependency graph, apply the transcript-derived environment and retention filters plus a "successful backups" status filter, `ORDER BY size DESC LIMIT 10`. For the classifier, inspect a few files in `/app/corpus/clean/` and `/app/corpus/evil/` to reverse-engineer what makes a record evil (e.g. a checksum field that should equal a hash of the data field, or a suspicious status masquerading via unexpected JSON structure) — implement that exact check in `/home/user/verify_backup_record.sh`, then run it against both corpora and confirm 100% correct classification before finishing.