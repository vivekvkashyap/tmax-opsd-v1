First recover the malicious-pattern spec from the audio: install/use a transcription tool (e.g. `whisper` CLI or another available speech-to-text) on `/app/backup_memo.wav`, read the transcript carefully to extract the exact literal substrings (or structural conditions) that mark a query as malicious versus safe — do not guess these from the task description, they only exist in the audio. Then design the sanitizer as substring/pattern matching in C: read each line from stdin, check it against the malicious patterns you transcribed (e.g. destructive graph deletes, specific suspicious window-function forms), and print "MALICIOUS" or "SAFE" per line accordingly, being careful that legitimate paginated/analytical queries with superficially similar syntax (ordinary `MATCH ... LIMIT`, normal `OVER (PARTITION BY col)`) are not falsely flagged — the distinguishing detail is in the transcript, not in general heuristics. Compile with `gcc`, test it manually against a few crafted safe/malicious lines before finishing, then write "READY" to `/home/user/done.txt`.