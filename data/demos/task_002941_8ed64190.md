Use real delta-debugging/bisection rather than manually scanning 10,000 lines: write a small script that runs the (unfixed) parser on progressively halved/binary-searched subsets of transactions.txt, comparing each subset's output sum against what a correct high-precision recomputation of that subset should be, to narrow down to the single line responsible for the discrepancy — a divide-and-conquer approach (split the file, test each half, recurse into whichever half still diverges) converges in log(N) steps instead of brute force. Once isolated, save that exact line verbatim to mre.txt. For the fix, read parser.cpp closely: look for any place a value is parsed or accumulated using a lower-precision type (e.g. `float` instead of `double`) or a format edge case (a trailing/stray character like `!` in one transaction type's amount field) that either gets mishandled or causes truncation/misparse — fix both the type used for accumulation/parsing and the string cleanup so all transaction types retain full double precision. Compile with `g++ -O2 parser.cpp -o parser_fixed`, run it on the original transactions.txt, and confirm stdout (formatted to 4 decimal places) matches expected_sum.txt before saving it to final_sum.txt.
