Write this as a straightforward line-by-line Python transform, but be exact about the regex and counting semantics since the spec is precise. Parse each line with a regex capturing the timestamp, USER_ID, and message (e.g. `r'^\[(.*?)\] (\S+): (.*)$'`). For masking, apply an email pattern like `\S+@\S+\.\S+` and a phone pattern `\d{3}-\d{3}-\d{4}` with `re.sub`, counting each match via `re.subn`'s returned count (summed across both patterns) to get `total_pii_masked` per message — apply email substitution before phone substitution (or vice versa) consistently, and reuse `re.sub` return values rather than re-scanning. For word count, call `.split()` on the *original* (or equivalently the anonymized, since masking doesn't change token count) message and take `len()` — do this per message, not per anonymized line reconstructed later. Accumulate per-`USER_ID` running totals of message count, word-count sum, and PII count as you iterate once through the file; on output compute `average_word_count = round(word_sum/count, 2)`. Write the anonymized lines preserving the exact `[timestamp] USER_ID: message` format, and dump the stats dict to JSON with `json.dump`.
