Work through this as a strict linear pipeline and validate each intermediate result before moving to the next stage, since a mistake early (e.g. wrong filtering order or wrong axis in SVD) silently corrupts everything downstream. Set up the venv first (`python3 -m venv /home/user/venv`, activate it, `pip install numpy scipy`) and run every script through that interpreter. Parse the FASTA manually (don't assume a library is available) by splitting on `>` headers, then filter to sequences containing the exact substring `"GATCA"` and sort by sequence ID string before computing anything — order matters because it determines row/column order in the distance matrix and thus which label lines up with which SVD row. Compute the Hamming distance matrix directly with nested loops or vectorized `!=` comparisons (all sequences share length, so no alignment needed). For SVD, use `numpy.linalg.svd(D)` and build `X = U @ diag(S)`, then take column 0 as your feature vector — print intermediate shapes to sanity-check. Join labels from the CSV by sequence ID (not by row index) before splitting into the two t-test groups, then run `scipy.stats.ttest_ind(group0, group1, equal_var=False)` and round each output value to exactly 4 decimals as specified.
