Treat this as a straightforward numeric pipeline in Python/numpy, but be careful about two easy mistakes. First, read both `x` and `raw_signal` datasets from the HDF5 file with `h5py` (index into the dataset object or use `[:]` to materialize a numpy array — don't leave it as a lazy h5py Dataset when passing to numpy functions). Second, compute `raw_integral = np.trapz(raw_signal, x)` exactly as specified — do not use `scipy.integrate.quad` or Simpson's rule, since the grader expects trapezoidal integration specifically. Third, normalize `p = raw_signal / raw_integral` so it integrates to 1, and construct `q` directly from the standard normal formula evaluated at the same `x` array (do not use `scipy.stats.norm.pdf`, though it would give the same numeric result — sticking to the given formula avoids subtle discrepancies). Fourth, compute the TVD as `0.5 * np.trapz(np.abs(p - q), x)`, again using trapz for consistency with how raw_integral was computed. Finally, round both scalars to exactly 4 decimal places with Python's `round()` before writing them to `/home/user/results.json` under the exact keys `raw_integral` and `tvd_distance` — verify the file is valid JSON by loading it back.