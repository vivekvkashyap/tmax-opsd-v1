Model this explicitly as a directed graph problem rather than ad-hoc string matching. Parse each JSONL line with `encoding/json` into a struct with `tx_id`, `action`, `resource`, `timestamp`. Build two maps: `resource -> holder tx_id` (from ACQUIRED events) and a list of (waiter, resource) pairs (from WAITING events). For each waiting pair, add a directed edge waiter -> holder-of-that-resource in a wait-for graph (`map[string][]string`). Then detect cycles: run DFS/coloring (white/gray/black) from every node, and whenever you find a back-edge into a node currently on the recursion stack, capture every node on that stack segment from the repeated node onward as part of a cycle — a transaction only counts as deadlocked if it's actually on a cycle, not merely reachable from one (don't just flag everyone downstream of a cycle). Collect the union of all such nodes across all detected cycles into a set, sort alphabetically, and marshal `{"deadlocked_transactions": [...]}` to `/home/user/deadlocks.json` with `encoding/json`. Run `go run detect.go` and inspect the output, tracing by hand through the resource-acquisition chain to confirm which transactions form a genuine cycle versus a one-way dependency chain.
