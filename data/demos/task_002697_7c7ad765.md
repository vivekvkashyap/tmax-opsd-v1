Design the C program around three clear stages: parse all `timestamp,paramA,paramB,paramC` lines from stdin into a sorted array (input may already be time-ordered but sort defensively); for each target `t` in `0,10,...,100`, locate the bracketing input samples (or handle before-first/after-last as flat extrapolation) and linearly interpolate each parameter independently: `value = v0 + (v1 - v0) * (t - t0) / (t1 - t0)`; then round to nearest integer using proper half-up rounding (`floor(x + 0.5)` for non-negative values — `round()` in C can behave differently on ties, so implement it explicitly to match spec). Compute Euclidean distance to the baseline `(50,100,150)` with `sqrt(pow(A-50,2)+pow(B-100,2)+pow(C-150,2))` and print with `%.2f` formatting for the drift column, comma-joining all five fields per line. In `pipeline.sh`, compile with `gcc process_drift.c -o process_drift -lm` (the `-lm` is required for `sqrt`), then `./process_drift < /home/user/raw_configs.txt > /home/user/drift_report.csv`. Test incrementally: verify interpolation and rounding on one or two synthetic timestamps by hand before trusting the full pipeline output.
