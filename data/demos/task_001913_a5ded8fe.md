Work the three layers in order and verify each before moving on. First, trace the data path Redis config: check `/app/api/.env` and `/app/worker/config.ini` (or equivalent) for the Redis host/port used by the FastAPI server and the C++ worker respectively — a mismatched port between the two config files is a classic reason services can't talk even though Redis itself is healthy; align them both to the actual `redis-server` port and restart the services via `start_services.sh`. Second, before touching optimization, stress-test the integrator directly: feed the RK45 step-size adaptation in `integrator.cpp` a stiff case and watch for NaN/divergence. Adaptive RK45 must shrink `dt` when local error exceeds tolerance and grow it when error is small — check the exponent/ratio direction in the `pow(...)` step-size update; an inverted ratio (error/tolerance instead of tolerance/error, or vice versa) causes the step to grow exactly when it should shrink. Recompile with `make` and confirm `POST /simulate` returns finite, non-diverging series for a stiff test payload before optimizing. Third, only once the pipeline returns clean simulations, write a small Python driver using `scipy.optimize.minimize` (or `curve_fit`) that calls the running API repeatedly, comparing simulated `P` to `/app/data/observed.csv`, and write the final `k1,k2,k3` to `/home/user/best_params.json`. Keep all services running afterward.
