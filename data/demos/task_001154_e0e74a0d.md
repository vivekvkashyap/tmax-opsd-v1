Split this into two independent deliverables and verify each in isolation before moving on. For the video phase: use ffmpeg to extract frames as grayscale (e.g. `ffmpeg -i experiment.mp4 -vf format=gray frame_%04d.png` or pipe raw frames), then compute the mean pixel intensity per frame (Python with Pillow/numpy, or ffmpeg's `signalstats`/`blackframe` filters give per-frame stats directly) and count frames whose mean strictly exceeds 100.0; write only that integer to the output file, no extra text. For the Rust phase: scaffold with `cargo new`, parse CSV manually or with the `csv` crate, split rows by `group`, and hand-roll the mean and population standard deviation (divide by N, not N-1) over group==0 only — do not import a stats crate for this since it's explicitly the naive-leak fix under test. Guard the zero-train-rows and zero-std edge cases explicitly (return 0.0 for every group==1 value rather than dividing by zero or panicking). Serialize the scaled group==1 values, in original order, as a JSON array to stdout with `serde_json`. Build with `cargo build --release` and confirm the binary lands exactly at target/release/scaler, then manually pipe a small sample CSV through it to sanity-check formatting before considering it done.