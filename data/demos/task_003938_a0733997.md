Start by reproducing: run `/home/user/run_pipeline.sh` (or the binary directly on each CSV) and observe which log file throws the `std::runtime_error`; inspect that file's values to confirm they are large-magnitude but low-variance, which is the classic catastrophic-cancellation signature (`sum_sq/n - mean^2` losing precision, occasionally going slightly negative). Open `/home/user/src/sensor_aggregator.cpp` and replace the naive "sum of squares minus square of mean" formula with a two-pass or Welford-style stable computation: first compute the mean, then accumulate `sum((x - mean)^2)` for the variance (or use running Welford updates), which avoids subtracting two large nearly-equal numbers. Also consider whether `float` precision is sufficient or whether accumulating in `double` internally (while keeping printed output format unchanged) further stabilizes the result — do not change the output format string. Recompile exactly as instructed (`g++ -O2 ... -o /home/user/bin/sensor_aggregator`), rerun `run_pipeline.sh`, and confirm all four log files process without a thrown exception, redirecting the successful run's stdout to `/home/user/pipeline_success.out`. Double check exit status is 0 and every batch file appears in the output.