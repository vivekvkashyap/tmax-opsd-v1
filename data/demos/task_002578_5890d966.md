Design the C++ program as a standard BFS from every Sensitive node simultaneously, not per-node separately, so the true global shortest path is found. Read `node_classifications.txt` into a map of node -> class, build an adjacency list from stdin edges (`SourceNode,TargetNode`), then run a multi-source BFS seeded from all Sensitive nodes, tracking the path taken to reach each node (e.g. store predecessor + path string per node). When a Public node is first reached at the current BFS depth, that's a shortest-length candidate; among all Public nodes reached at the same minimal depth, pick the lexicographically smallest full path string exactly as tie-break rule states — compare full "A -> B -> C" strings, not just individual node names. Emit `VIOLATION: <path>` joined by ` -> `, or `COMPLIANT: No paths found` if BFS never reaches any Public node. For the bash side, filter `/home/user/raw_schema_edges.csv` for rows whose Status column is exactly `Active` (e.g. `awk -F, '$3=="Active"{print $1","$2}'`, skipping any header row) and pipe that into `./audit_paths node_classifications.txt`, redirecting to `audit_report.txt`. Compile with `g++ -std=c++17`, make the shell script executable, and run it — double-check the header row of the CSV isn't accidentally treated as an edge.