Reproduce the crash first (`bash /home/user/service/start_service.sh`) to see which `echo ... > path` fails because its parent directory doesn't exist. Fix `start_service.sh` by adding `mkdir -p` for both the PID file's directory and the data directory before any write to them. Then replace the naive "PID file exists -> exit" check with a stale-lock check: if the PID file exists, read the PID and test liveness with `kill -0 "$pid" 2>/dev/null`; if the process is alive, print "Service already running" and `exit 1`; if not alive, `rm -f` the stale PID file and continue starting normally (write the new $$ to the PID file, write ACTIVE to the status file, then loop/sleep to stay resident). Launch it backgrounded (`.../start_service.sh &`) so it's actually running for the health check to observe. For `health_check.sh`: read the PID from `/home/user/service/run/service.pid`, verify it's alive with `kill -0`, read `/home/user/service/data/status.txt` and compare with `[ "$(cat ...)" = "ACTIVE" ]` — only print HEALTHY/exit 0 when both hold, else UNHEALTHY/exit 1. `chmod +x` the health check script.
