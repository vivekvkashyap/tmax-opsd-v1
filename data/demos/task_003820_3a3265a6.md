Build the traversal with explicit cycle detection: as you recurse/iterate through `/home/user/app_configs`, resolve each directory's canonical path (e.g. `realpath`) and track visited canonical paths in a set/array; before descending into a symlinked directory, check whether its resolved path was already visited and skip it if so (this is the key place naive scripts infinite-loop — `find -L` alone will not stop on the circular `app1<->app2` symlinks). For each regular file encountered (including files reached via non-circular/external symlinks, which should still be followed), check its size: use `split -b 1048576 -a 2 <file> <staging>/<basename>.part_` for files >=1MB, matching the `part_aa`, `part_ab` naming, otherwise copy it as-is into a flat staging directory (strip directory structure, just basenames). After staging everything, sort the list of staged basenames alphabetically and write it to `archive_log.txt`, then `tar -cf - -C <staging> . | gzip | base64 > /home/user/config_backup.tar.gz.b64`. Test by decoding/untarring your own output to confirm round-trip correctness before finishing.
