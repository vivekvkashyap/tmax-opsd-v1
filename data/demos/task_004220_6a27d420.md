Build the pipeline in two clean stages and verify each independently. For the bash stage, use something like `cut -d, -f3- /home/user/raw_logs.csv | tail -n +2 | tr '[:upper:]' '[:lower:]' | sed -E 's/[^a-z0-9]+/ /g'` (or `tr` for the non-alnum squeeze) to isolate the raw_message column, lowercase it, and collapse runs of non-alphanumeric characters into single spaces, writing one cleaned line per row to `/home/user/clean_logs.txt` — check the CSV doesn't have commas inside the message field that would break naive `cut`/`awk -F,` splitting. For the C stage, tokenize both the query argument (argv[1]) and each stdin line into unique-word sets (e.g. sort+dedupe an array of word pointers per line), compute |intersection|/|union| in straightforward integer arithmetic, track the running best score and its line text, and print only that one winning line at the end — keep the first-seen line on ties by using strict `>` when updating the best score, not `>=`. Compile with `gcc /home/user/jaccard.c -o /home/user/jaccard`, then run `/home/user/jaccard "critical server overload crash" < /home/user/clean_logs.txt > /home/user/best_match.txt` and inspect the output file to confirm it's exactly one clean line.