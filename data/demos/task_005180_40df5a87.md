Read `server.py` carefully to find each bug rather than rewriting from scratch. Fix #1: `await websocket.send(response)` sends a dict, which the `websockets` library can't serialize as a frame — wrap it as `await websocket.send(json.dumps(response))`. Fix #2: the matching loop currently breaks after finding *any* worker with the right skill without marking that worker unavailable, so the same worker can be reused across tasks — track assigned workers in a `set()` (or remove/pop the worker from the candidate list once used) so each worker is assigned at most once; if any task can't find an unused matching worker, the whole request is `unsatisfiable`. Fix #3: before (or after) sending each response, append `json.dumps(response) + "\n"` to `/home/user/scheduler_log.jsonl` (open in append mode) so every response — success or error — is logged exactly once per request. Install `websockets` via pip, then launch the fixed server in the background (e.g. `nohup python3 server.py &` or `setsid`) and confirm with `ss -ltnp` or a quick test client that it's listening on `127.0.0.1:8765` before finishing.
