First extract frame metadata with the given `ffprobe` invocation, redirecting stdout into `/home/user/extracted.csv` with the `timestamp,size` columns and no header. The real work is the Go filter: don't trust `strconv.ParseFloat` alone, because it happily accepts the literal strings "NaN", "Inf", "+Inf", "-Inf" and produces a float that IS one of those special values — after parsing each field, explicitly call `math.IsNaN(v)` and `math.IsInf(v, 0)` on the result and reject (exit 1) if either is true, regardless of what string produced it. Also reject if the parsed `size` is negative, and reject on any CSV read/parse error (malformed row, wrong column count, non-numeric garbage) rather than skipping it silently. Use `encoding/csv` to read rows, validate every row before writing anything, and only write the output file (echoing the exact same rows) if the entire file passes — read-then-decide, not write-as-you-go. Test iteratively against every file in both `/app/corpus/clean/` and `/app/corpus/evil/` via `go run filter.go <in> <out>` and check exit codes with `echo $?` until all pass, before running it on your own `extracted.csv`.
