Write the Python script to read `os.environ.get("TZ")` and `os.environ.get("LC_ALL")` and compare them with exact string equality against the two required values; only call Python's `input()` with the exact prompt text when both match, and `sys.exit(1)` immediately (no prompt) otherwise. Inside the interactive branch, compare the captured answer to exactly `"Y"` before writing the three-line command file with `\n` line endings — anything else (including lowercase `y` or empty input) must exit 1 without creating the file. In the bash wrapper, export both environment variables with the exact required values, then invoke the Python script feeding it the confirmation non-interactively via a pipe (e.g. `echo "Y" | python3 /home/user/deploy_update.py`) so `input()` reads it from stdin without needing a real TTY. Remember to `chmod +x` the wrapper. Test both paths yourself before finishing: run the wrapper and confirm the output file's exact contents, then run the script directly with mismatched/default env vars and confirm it exits 1 with no prompt and no file.
