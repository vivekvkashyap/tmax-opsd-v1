Structure this as three verifiable stages: ETL, closed-form OLS math, then benchmarking. Set up the Rust project with `cargo new` and add `csv`, `serde`, `serde_json`, `nalgebra` as dependencies. Read `raw_sensors.csv`, filter rows where `sensor_1 < 0.0 || sensor_2 < 0.0`, and derive `sensor_3 = sensor_1 * sensor_2` per kept row — get the filter condition and column order right, since a flipped inequality or wrong column order in the design matrix will silently corrupt every downstream number. Build the design matrix `X` (columns: intercept-1s, sensor_1, sensor_2, sensor_3) and target vector `y` as `nalgebra` matrices, then compute `beta = (X^T * X).try_inverse().unwrap() * X^T * y` — double check dimensions align (X is n×4, X^T*X is 4×4) before inverting. Sanity-check the resulting intercept/weights look like plausible regression coefficients (not wildly large, which would indicate a matrix-orientation bug) before moving on. For benchmarking, wrap the inference matmul in a loop of 1000 iterations timed with `std::time::Instant`, divide by 1000 and dataset size appropriately to get average microseconds per single inference, then serialize intercept, weights (3 floats), and avg_inference_micros to `/home/user/model_metrics.json` in the exact schema, and confirm with `cargo build --release && cargo run --release`.
