Create the venv (`python3 -m venv /home/user/venv`, then `source .../bin/activate && pip install numpy scipy`) and always activate it before running your analysis script. Follow the specified statistical recipe literally and in the exact order given, since the verifier recomputes ground truth the same way and compares with a tight tolerance: fit `scipy.stats.gaussian_kde(data)` with default bandwidth, find the mode via `scipy.optimize.minimize_scalar(lambda x: -kde.pdf(x), bounds=(0,10), method='bounded')` (minimizing the negative PDF, not maximizing directly), integrate `kde.pdf` over `[mode-1, mode+1]` with `scipy.integrate.quad`, fit `mu, std = scipy.stats.norm.fit(data)` on the *original* dataset, then draw exactly 10000 samples with `np.random.default_rng(42).normal(loc=mu, scale=std, size=10000)` (seed and order matter for reproducibility) before computing `scipy.stats.wasserstein_distance(data, samples)`. Round each of the three results to exactly 4 decimals with Python's `round()` and write them under the specified key names to `/home/user/results.json`.
