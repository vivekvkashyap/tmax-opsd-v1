Do not try to reverse-engineer the crash conditions by guessing blindly — since the binary is stripped and unstable, treat it as a black box you probe with small, controlled test FASTA files (a clean sequence, one containing 'N', one with a run of >20 identical bases) run through mpirun to confirm which inputs cause it to hang/crash versus succeed, rather than assuming. Write sanitize_reads.sh to parse FASTA records as header+sequence pairs (a record is exactly two lines: a '>' line and the following sequence line), check each sequence for any 'N'/'n' character and for any run of the same character longer than 20 in a row (use a loop or regex like matching 21+ repeated characters), and only when a record fails either check should you drop both its header and sequence lines from the output — otherwise copy the pair through unchanged. Test this against constructed edge cases (exactly 20 repeats = keep, 21 repeats = drop) since off-by-one errors here are the most common bug. For find_optimal_k.sh: sanitize the sample file first, then loop k=15,17,19,... calling mpirun with -np 4, capturing stdout as an integer, and stop as soon as that integer equals exactly 1, writing that k to optimal_k.txt — do not hardcode the k value, discover it by running the loop.
