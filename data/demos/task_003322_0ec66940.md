Run `tesseract /app/import_router.png stdout` (trying different `--psm` modes if the layout confuses it) and read the OCR output very carefully — it defines the exact input format regex, the accumulator/checksum recurrence, and the output URL template you must implement precisely, so don't paraphrase or guess at the algorithm from the task summary alone. Implement `/home/user/route.py` as an executable Python script (`#!/usr/bin/env python3`, `chmod +x`) that: validates the single CLI argument matches the required `/load/<module_name>` shape with `module_name` restricted to alphanumerics and underscores only (anchor the regex fully, e.g. `^/load/([A-Za-z0-9_]+)$`, so partial/extra-path matches are rejected); on mismatch prints exactly `INVALID`; on match runs the accumulator loop over the module name's characters exactly as OCR'd (initial value, multiplier, modulus, and per-character update order matter), then prints the resulting proxy URL using the exact template found in the image. Since this is checked by fuzzing many valid and invalid paths against a hidden oracle, test edge cases yourself: empty module name, hyphens, extra path segments, and boundary ASCII values.
