Treat this as three separable subproblems and solve them in order. First, load `routes` and `depends_on` from `/home/user/api_db.sqlite` and build a directed graph where an edge `route_id -> depends_on_id` means route_id must come after depends_on_id. Detect cycles (e.g. DFS with recursion-stack tracking); for the/each cycle found, among the edges that participate in it, remove the one whose `route_id` is numerically highest — do this minimally (only cycle-breaking edges, not unrelated ones) and re-check for remaining cycles until the graph is a DAG. Second, run a topological sort (Kahn's algorithm works well here since it naturally supports deterministic tie-breaking) where, whenever multiple nodes have zero remaining in-degree simultaneously, you pick them in ascending `id` order — use a min-heap or sort the ready set each iteration. Third, for each route's `condition` string, write a small parser/replacer that performs the substitutions in the exact order given (rewrite `param.X`/`header.X` prefixes to `req.param.X`/`req.header.X` first, or handle operators and field prefixes with careful regex/tokenization so `=` doesn't clobber inside `!=`, e.g. replace `!=` before `=`), wrap as `` req => <expr> ``, preserving parentheses and string literals verbatim. Assemble the sorted routes with `id`, `path`, `js_validator` keys into a JSON array and write it to `/home/user/migrated_api.json`.