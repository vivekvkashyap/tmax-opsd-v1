Handle the three pieces in dependency order since later steps need earlier outputs. First, run `protoc --python_out=/home/user /home/user/request.proto` to generate `request_pb2.py` — do this before touching the parser since it imports that module. Second, port `legacy_parser.py` to Python 3: replace `import urlparse` with `from urllib.parse import urlparse`, and replace `sys.stdout.write(req.SerializeToString())` with `sys.stdout.buffer.write(req.SerializeToString())` since protobuf serialization yields `bytes` and Python 3's text-mode `sys.stdout.write` will raise a TypeError on bytes. Third, write `/home/user/process_requests.sh`: read `/home/user/urls.txt` line by line, and before processing each URL call `sleep 1` (the rate limit — this is what makes the overall script take >=3 seconds for 3 URLs, so don't skip it or put it after processing only). For each URL, run `/usr/bin/time -v python3 legacy_parser.py "$url" 2> /tmp/time_out.txt` (time's report goes to stderr), extract the domain from the URL with a lightweight tool like `awk -F/ '{print $3}'` or `sed`, grep `/tmp/time_out.txt` for `Maximum resident set size` and pull the integer field, then append a line to `/home/user/processing.log` in the exact `Domain: ... | URL: ... | PeakMem: ... KB` format. Make the script executable and run it end-to-end to produce the log.