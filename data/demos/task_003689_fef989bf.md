In /home/user/process_sensors.py, use glob to read every CSV under /home/user/sensor_data/, concatenate into one DataFrame, and compute per-sensor_id mean and population std (pandas .std(ddof=0), not the default ddof=1 — this is the common mistake that will make anomaly thresholds and z-scores wrong). For anomaly detection, merge the per-sensor stats back onto each row, flag |temperature - mean| > 2*std, and compute z_score = (temperature - mean) / std only for flagged rows, rounding to 2 decimals before storage. Use sqlite3 to create /home/user/anomalies.db with a temperature_anomalies table (timestamp TEXT, sensor_id TEXT, temperature REAL, z_score REAL) and insert the anomaly rows via executemany, then commit/close the connection so the file is flushed to disk. For logging, use Python's logging module (or manual timestamp formatting) writing to /home/user/pipeline.log with lines matching `[YYYY-MM-DD HH:MM:SS] - [STEP] - [MESSAGE]` exactly, including the three required STEP/MESSAGE pairs with the actual counted totals (total rows ingested, total anomalies found) — compute these counts from your own DataFrame rather than hardcoding, then execute the script once and inspect both output files to confirm they match the described format.
