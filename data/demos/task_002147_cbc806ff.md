Write the pipeline as a single Python script rather than working interactively, so it is reproducible and easy to re-run. Load the CSV with pandas, call `dropna()` before any feature engineering (rows with NaNs must be gone before computing ratios, otherwise NaN propagates and skews correlations). Compute `cpu_mem_ratio` and `total_net` as new columns, then use `Series.corr()` (Pearson, the pandas default) against `status` for all seven numeric features — do not accidentally include `status` or `timestamp` in the candidate list. Rank features by the absolute value of the correlation coefficient, not the signed value, when picking the top 3 (a strong negative correlation matters as much as a strong positive one). For each selected feature, split it into two groups by `status` (0 vs 1) and run `scipy.stats.ttest_ind(group0, group1, equal_var=False)` for Welch's t-test — do not use the equal-variance default. Round both the correlation and p-value to 4 decimal places with Python's `round()` before serializing, and write the dict straight to JSON at the exact path requested. Double check key order/content matches only the top-3 features, not all seven.