For `sim_core.py`, follow the spec literally and match Python's own `random` module semantics exactly since the fuzz oracle is itself Python's `random.random()`-driven logic: seed once with `random.seed(seed)`, then loop up to `N_steps` times, drawing exactly one `random.random()` per step, incrementing a step counter every iteration (even the one that triggers absorption), and breaking immediately once `abs(pos) == L`. Print exactly `RESULT: <steps_taken>, <final_position>` with no extra whitespace or trailing text, since the equivalence check will likely do exact string/line comparison. For `worker.sh`, use a `while true` loop around `redis-cli blpop job_queue 0` (blocking pop, which returns two lines: the list name and the value — parse both), split the value on `|` into `job_id, N_steps, seed, p_right, L`, invoke `python3 /home/user/sim_core.py $N_steps $seed $p_right $L`, capture its stdout, build the JSON payload with `job_id` and `output` (use `jq -n` or careful string escaping to avoid malformed JSON), and `curl -s -X POST -H 'Content-Type: application/json' -d "$payload" http://localhost:5000/submit`. Make both files executable and test the worker manually against a running Redis/Flask before relying on the automated end-to-end check.
