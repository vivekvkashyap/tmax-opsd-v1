Write `deploy.py` to take `sys.argv[1]` as the version, then perform each step with absolute paths and atomicity in mind. (1) For the version-local config symlink: compute the target version directory absolute path and the global config's absolute path, then create/overwrite the symlink inside that directory — since a symlink may already exist there, remove it first (or use `os.symlink` after `os.remove`/a try-except, or the equivalent of `ln -sf`) rather than letting `os.symlink` fail on an existing file. (2) For the "atomic" global switch of `/home/user/app_current`: don't just delete-then-recreate the symlink (that leaves a window where the path doesn't exist) — instead create a new symlink at a temporary path pointing to the target version dir, then use `os.replace()`/`os.rename()` to atomically swap it onto `/home/user/app_current`, replacing whatever was there (existing symlink) in one step. (3) Append the log line to `/home/user/deploy.log` using the exact format string given, with all paths absolute. Finally run the script once with `v2` and leave the resulting state (symlinks + log) in place, don't clean up afterward.
