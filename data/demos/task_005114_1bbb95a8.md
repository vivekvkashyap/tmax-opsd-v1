Build the query in layers inside `sqlite3`, testing each layer's intermediate output before adding the next. First write a recursive CTE seeded with `source_node = 'db-main'` that unions in rows from `topology` where `source_node` matches a node already found, collecting reachable `target_node`s (remember to include `db-main` itself in the final node set, not just its targets) — run it alone and eyeball the node list before proceeding. Next, join `jobs` (status = 'SUCCESS') to that node set and compute the 3-row rolling average with a window function: `AVG(size_mb) OVER (PARTITION BY node ORDER BY run_date ROWS BETWEEN 2 PRECEDING AND CURRENT ROW)`. Then, for each node, isolate only the row with the max `run_date` — a `ROW_NUMBER() OVER (PARTITION BY node ORDER BY run_date DESC)` filtered to 1 is cleaner than a subquery MAX join. Filter that final set to `size_mb > 1.5 * rolling_avg`, round the rolling average to 2 decimals with `ROUND(...,2)`, and order by `size_mb DESC`. Run the whole query with `.mode list` and `.separator |` (or `-separator '|'` on the CLI) redirected into `/home/user/anomaly_report.csv`, and diff your output against your own manual trace of the seeded data before finishing.