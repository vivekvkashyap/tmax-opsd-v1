Use Go's standard `encoding/csv` reader (not manual line-splitting) to parse `/home/user/raw.csv` — it correctly handles embedded newlines inside quoted fields, which is exactly why naive line-by-line tools broke. For each row, extract `Temp` and `Hum` floats out of the multiline `Data` string with simple line-scanning or a regex per metric (e.g. match `Temp:\s*([\d.]+)` and `Hum:\s*([\d.]+)`, treating a metric as absent if its line isn't present). Build an in-memory structure keyed by (Station, Metric) holding a time-ordered list of raw values, then do a single pass over increasing Time per (Station, Metric): if the value is missing at Time=1, impute 0.0; if missing at a later Time, carry forward the last known imputed value. Immediately after imputation, compute the rolling average over the window of size 2 (current plus previous imputed value at the *same* Station+Metric; just the current value when there's no previous Time). Only after imputation is fully resolved should you compute rolling averages, since they depend on the imputed series, not raw values. Format every float with `%.1f`, sort output rows by Station, then Metric, then Time (all ascending, string comparison for Station/Metric), write the header, then run `go run process.go` (or build it) and inspect `/home/user/clean.csv` against the row-by-row logic by hand for a couple of rows before trusting it.