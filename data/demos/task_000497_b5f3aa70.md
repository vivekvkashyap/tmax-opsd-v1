This is a two-stage task: recover ground truth from video, then apply it to a config file. For the video analysis, write a Python/OpenCV script that reads `/app/dashboard_traffic.mp4` frame by frame, locates the 4x4 grid of status squares (divide the frame into a 4x4 region grid, or detect square contours), and for each cell samples its color — classify green vs. red using simple thresholds in HSV or RGB space (e.g. red channel dominant vs. green channel dominant) rather than exact pixel matching, since compression artifacts will cause noise. Track, across all frames, which of the 16 grid cells (indexed row-major, User00 top-left through User15 bottom-right) ever turn solid red for a sustained stretch (require the color to persist for multiple consecutive frames to avoid false positives from single-frame noise/flicker) — this gives you the actual set of affected UserIDs, do not hardcode a guess. Then write an idempotent script that loads `/home/user/network_configs.json`, sets `subnet` to `10.1.0.0/24` only for the affected users found in your detection step, leaves everyone else untouched, and writes the result to `/home/user/fixed_network.json`. Since grading uses an F1 threshold of 0.9 rather than exact match, prioritize correctly identifying most/all truly-affected users over guessing extras.
