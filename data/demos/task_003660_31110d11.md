Fix the Rust library before touching Go. In `/app/divsum-0.1.0/src/lib.rs`, the borrow-checker error comes from holding `&mut sum` alive while also writing to `sum` directly in the loop — remove the redundant mutable reference entirely and just accumulate into `sum` directly (the `*r += 0` line is dead weight from a bad refactor). In `Cargo.toml`, add `[lib]\ncrate-type = ["cdylib"]` so `cargo build --release` emits a `.so`. Verify the FFI boundary works with a tiny C or Rust test harness calling `sum_of_divisors(28)` and confirming it equals 56 (which is 1+2+4+7+14+28) before writing any Go code. For the Go side, initialize a module, write CGO bindings (`#cgo LDFLAGS: -L... -ldivsum`, matching header declarations) to call the exported symbol, read `/app/input.json` into a `[]uint64`, and process concurrently: spawn a goroutine per number but gate actual FFI calls through a buffered channel of capacity 4 (or a `sync.WaitGroup` + semaphore channel) so at most 4 calls are in flight — this is what the speedup benchmark measures against a serial (concurrency=1) baseline. Write results as a `map[string]uint64` keyed by the string form of each input number to `/app/output.json`. Finally write the Makefile's `build` target to run `cargo build --release` then `go build`, exporting `CGO_LDFLAGS`/`LD_LIBRARY_PATH` so the binary finds `libdivsum.so` at runtime.