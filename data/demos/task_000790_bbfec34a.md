Treat the two parts independently and validate each before moving on. Part 1: extract frames from `/app/incident.mp4` at its native 1fps rate with `ffmpeg -i /app/incident.mp4 frame_%04d.png` (this gives one PNG per second, 0- or 1-indexed depending on your naming — track the offset carefully), then for each frame check the pixel at (100,100) programmatically (e.g. with `python3 -c "from PIL import Image; ..."` or `ffmpeg`'s crop+signalstats filter) for exact RGB (255,255,255); find the first/only frame matching and write its 0-indexed frame number (adjust for ffmpeg's 1-based `%04d` naming when converting to a 0-indexed answer) to `/home/user/incident_frame.txt` with no extra whitespace. Part 2: write `filter_manifest.cpp` that opens the given file argument, finds the line starting with `TARGET_IP=`, extracts the IP, and checks it matches the literal prefix `10.0.0.` followed by a valid octet 0-255 (validate it's actually numeric and in range, and that the line/format isn't malformed) — exit 0 if so, exit 1 otherwise (including for any parse failure or missing field). Compile with `g++` to `/home/user/filter_manifest` and test it against every file in `/app/corpus/clean` and `/app/corpus/evil`, confirming exit codes with `echo $?` before finishing.
