Process each `.bin` file with a clear, testable per-file routine rather than one big script you can't debug. For each file: read the first 16 bytes, slice bytes 0-3 as the ASCII magic string, decode bytes 4-7 as a little-endian unsigned 32-bit int for version (`struct.unpack('<I', ...)` in Python), and discard bytes 8-15. Read everything after byte 16 as the payload, compute `hashlib.sha256(payload).hexdigest()` on exactly that slice (not the whole file — this is a common mistake). Split the payload into fixed 102400-byte chunks (the last chunk will be shorter — that's expected and fine, don't pad it), write each to `/home/user/repo/<basename>/chunk_NN` using zero-padded 2-digit indices starting at 00. Build the manifest dict incrementally as you process each file (basename without `.bin` as the key, containing magic/version/payload_sha256/chunks-list-in-order), then dump it once at the end as `/home/user/repo/manifest.json`. Before finishing, self-verify by reassembling one artifact's chunks in order and confirming the resulting hash matches the recorded `payload_sha256`.