Implement the DJB2 hash in C exactly as given (unsigned int accumulator, `hash*33+c` per character, printed with `%08x`), and write a line-by-line CSV transformer that splits each input line on commas, replaces only the 3rd field (UserID) with its hash, and reassembles the other fields untouched — use fgets with a buffer large enough for 1024-char lines and be careful to preserve exact formatting (no extra commas/newlines) on the untouched columns. For the Makefile, express the DAG declaratively: a `masker` target compiling with gcc -O3, per-file pattern or explicit rules for each `masked/raw_dataN.csv` that depend only on the corresponding raw file and the masker binary (so make -j4 can run them in parallel), each rule creating the masked/ directory (`mkdir -p masked`) and piping `./masker < raw/rawN.csv > masked/rawN.csv`. The `aggregate.csv` target must depend on all three masked files and concatenate them with `cat` in the fixed order 1,2,3 (not glob order, which isn't guaranteed to sort correctly). Make `all` the default target depending on aggregate.csv, then run `make -j4 all` and diff your output against a hand-computed DJB2 hash of one UserID to confirm correctness before trusting the pipeline.