Work the four subtasks in dependency order. First resolve `requirements.txt`: pin compatible versions yourself (e.g. bump the old numpy pin to satisfy both scipy and pandas' minimum numpy requirement) and install with `pip install --user` since you can't touch system paths — read the actual error message rather than guessing versions blindly. Second, read `/app/wrapper.py` carefully for exactly two bugs: an off-by-one slice that drops the last matrix column (look for something like `raw_data[:, :-1]` and remove the `:-1`), and an unbounded `while error > tolerance:` convergence loop that needs a `max_iter` cap added so it can't hang forever. Third, write a small MRE script that feeds sample/corpus matrices to `/app/matrix_eig_solver.bin` under a timeout and observe which inputs hang or crash — don't just read the corpora, actually exercise the binary to confirm your hypothesis. Fourth, compare the clean vs. evil corpus mathematically (don't eyeball JSON) using `numpy.linalg.det` and `numpy.linalg.cond` on each matrix to find the numeric threshold that cleanly separates them (e.g. near-zero determinant or very large condition number indicates singular/ill-conditioned = evil). Implement `/home/user/sanitizer.py` to load the JSON matrix, compute that same diagnostic, and exit 0 if safe / exit 1 if it should be rejected — validate against both provided corpora before considering it done.