First fix the vendored build: inspect the Makefile at /app/vendored/deptool/Makefile for the deliberately broken compiler flag, remove/correct it, and note it may also require setting an environment variable (check for `ifndef`/`ifeq` guards on env vars in the Makefile, or try `export BUILD_ENV=mobile` if the build references it) before `make` succeeds — confirm by running the compiled deptool binary directly against a sample file argument and checking its output makes sense. Second, build the HTTP server purely in bash/coreutils/nc: loop `nc -l -p 8080` (or similar), read the request line and headers, then read the Content-Length worth of POST body, parse the `"file": "..."` value out of the JSON body with a simple grep/sed pattern (no jq needed for a single-key extraction), call `./deptool <file>` and capture its stdout, then emit a well-formed HTTP/1.1 200 response with that output as the body. Third, implement rate limiting by tracking request timestamps (e.g. write epoch seconds to a small state file or array on each request), and before serving check how many requests fell within the current second — if this is the 3rd or later in the same second, respond 429 instead of invoking deptool. Test by sending 3 rapid POSTs with curl and confirming the third gets 429.
