Build the checker first, then the hook, and test both independently before wiring them together. For `check_tls.py`: read all of `sys.stdin`, then flag if `TLSv1.1` appears anywhere, OR if `TLSv1` appears followed by a non-digit/non-dot boundary (e.g. space, semicolon, end of string) — take care that `TLSv1.2`/`TLSv1.3` must NOT match, so check the substring immediately after `TLSv1` is not `.2`/`.3`; a regex like `TLSv1(?!\.[23])` combined with a check that it isn't part of `TLSv1.1` handled separately, or simpler: search for `TLSv1.1` OR (`TLSv1` present and not immediately followed by `.2`/`.3`). Print `WEAK TLS DETECTED` and exit 1 if flagged, else exit 0; chmod +x it. For the pre-receive hook: read `old new ref` line(s) from stdin, use `git diff-tree -r --name-status --diff-filter=AM <old> <new>` (handle old being all-zeros for new branch by diffing against the empty tree) to list added/modified files, filter for `*.conf` with awk/grep, then for each use `git show <new>:<path>` to get the incoming content and pipe into `check_tls.py`; if any exits 1, hook exits 1 immediately, otherwise exit 0. Test by pushing a good and a bad commit into a bare repo clone.
