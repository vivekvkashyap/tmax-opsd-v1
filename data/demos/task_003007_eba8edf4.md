In the C program, parse `data.csv` line by line (skip header), accumulate F1–F5 sums keyed by UserID (a small fixed-size array or simple hash map indexed by UserID since IDs are small integers works fine), then load the 5x2 `matrix.csv` into a small array. For each user, compute the two dot products of their 5-vector against the matrix's two columns exactly as matrix multiplication defines it (feature_i * matrix[i][0] summed for P1, feature_i * matrix[i][1] summed for P2) — do not swap rows/columns. Sort users by UserID ascending (simple insertion sort or qsort is fine given small N) before writing output with `%.2f` formatting and a header line. Compile with `gcc etl.c -o etl -lm` if needed and run it, then inspect `reduced_data.csv` by eye to sanity check the arithmetic. For `validate.sh`, use `awk -F,` to check every data row has exactly 3 fields and that fields 2 and 3 are non-empty and not the literal string "nan" (case-insensitive), printing VALID/INVALID and exiting 0/1 accordingly.