Work through this in stages rather than guessing at the filter math up front. First, extract the signal: open the video with OpenCV (`cv2.VideoCapture`), read each frame, and for each frame find the pixel location where the channel values equal (255,255,255) — e.g. `np.argwhere` on a mask, take its x-coordinate. Write these as one space-separated line to the required output path. Second, compile the given C source with gcc to the required binary path, then pipe your extracted signal into it via stdin and capture stdout as the reference. Third, read the C source itself (not just its behavior) to pin down the exact windowing logic — note it uses a fixed window of radius 2 (indices i-2..i+2) with clamped/clipped boundary indices (not wraparound, not zero-padding), computing max-min over that clamped window. Reimplement this in Python using vectorized/rolling approaches (e.g. sliding-window max/min via deque or numpy stride tricks) for O(N) or O(N log N) performance instead of the naive O(N^2) double loop, but replicate the exact boundary-clamping behavior — this is the most common place a naive reimplementation silently diverges from the C reference. Validate your Python script against the compiled C binary on several random inputs, including short arrays, before considering it done.
