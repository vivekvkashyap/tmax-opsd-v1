Build the toolchain bottom-up and verify each layer before moving to the next. Compile the given `system_solver.c`/`.h` into a shared object with position-independent code, e.g. `gcc -fPIC -shared -o libsystemsolver.so system_solver.c`, and confirm it's a valid shared library (`file libsystemsolver.so`). In `compute_boundaries.c`, read the three (x, y) points from the CSV, then set up the 3x3 linear system for a parabola y = ax^2 + bx + c: each row is [x_i^2, x_i, 1] with RHS y_i — get this system formulation right, since a transposed or mis-ordered matrix will silently give wrong coefficients even though `solve_3x3` runs without error. Link against the library with `-L/home/user/math_utils -lsystemsolver` and set `LD_LIBRARY_PATH` (or use `-Wl,-rpath`) when compiling and running so the dynamic linker finds it at runtime. After solving for a, b, c, find where y=0.5 by solving the quadratic a*x^2 + b*x + (c-0.5) = 0 via the standard quadratic formula, order the two roots smaller-first, and print both to exactly 6 decimal places, comma-separated, into `/home/user/results/boundaries.txt`. Run the binary yourself and sanity-check the two roots are symmetric around the parabola's vertex before finishing.
