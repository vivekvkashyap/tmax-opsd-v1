Write straightforward C: read each of the three CSV files line by line with `fscanf`/`fgets`+`sscanf` (no header to skip), and for every row compute `bucket = (timestamp / 60) * 60` using integer division (careful: this must be plain C integer division on `long`, not floating point). Use a data structure keyed by bucket timestamp — simplest robust approach is to collect all distinct buckets from all three sensors into one sorted set/array (e.g. read all buckets into a dynamic array, sort with `qsort`, dedupe), then for each unique bucket and each sensor separately sum values and counts in that bucket to compute the mean, defaulting to `-999.00` when a sensor has zero readings for that bucket. The two places naive attempts fail: (1) forgetting a bucket that only has data from one or two sensors — a bucket must appear in the output even if only one sensor contributed, so don't intersect, union the buckets; (2) formatting floats — use `printf("%.2f", ...)` consistently for both real averages and the `-999.00` placeholder so decimal precision matches exactly. After compiling and running, diff or manually check the output file's exact bytes/format (header line, comma-separated, no trailing whitespace surprises) since verification is an exact md5 match.
