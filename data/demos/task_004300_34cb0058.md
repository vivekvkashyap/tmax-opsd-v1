Work in three clean phases. First, extract video frames with `ffmpeg -i /app/audit_transmission.mp4 frame_%03d.png`, then decode each with `zbarimg --quiet --raw frame_*.png` and concatenate the fragments in frame order into one JSON string — verify it parses before trusting it, since a single misordered frame breaks the array. Second, write the C policy engine using a JSON library (cJSON or jansson) rather than hand-rolled string parsing, since edge cases like escaped quotes or numeric vs string fields will otherwise break parsing on the hidden test set. Implement each rule literally as specified: for Rule 1, check `file_owner == "root"` and parse the permission string as 4 octal digits, testing if the first digit is 4 or 6 (do not assume it's always 4 digits — validate string length); for Rule 2, do an exact string comparison of issuer vs subject plus the boolean trust flag; for Rule 3, do case-sensitive substring search for each of the four listed patterns. Test against your own extracted dataset first, then compile with `gcc -o /home/user/policy_engine /home/user/policy_engine.c -lcjson` (or similar), and confirm CSV output exactly matches `id,is_vulnerable` with no extra whitespace or trailing rows, since the grader diffs on merged `id`.