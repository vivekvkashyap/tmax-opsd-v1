Treat this as three independent, verifiable sub-problems rather than one blob of code. First, reshape the data: parse `rna_data.txt` where lines alternate between a `>Time_X` header and a `read_count:Y` value — read the file in pairs of lines, strip the `>Time_` and `read_count:` prefixes, and write a clean two-column CSV with a header row, preserving each pair's order. Second, derive the analytical solution symbolically: the linear ODE dC/dt = α − βC has closed form C(t) = α/β + (C(0) − α/β)·e^(−βt); plug in the given α, β, C(0), and t=1.0 directly with Python's `math.exp` — don't hardcode a memorized numeric answer, compute it from the formula so it's correct if constants ever differ. Third, implement forward Euler explicitly as a loop: `C += dt * (alpha - beta * C)` for `t/dt` steps starting from C(0), so you get the numerical trajectory value at t=1.0. Fourth, derive the stability bound analytically from the amplification factor: for dC/dt = α − βC, explicit Euler's growth factor is `|1 − β·dt|`; requiring this ≤ 1 gives `dt_max = 2/β` — compute this from β rather than stating a memorized number. Round every output value to 5 decimals before writing JSON.
