Build and validate each piece independently rather than chaining everything blind. For the C++ tool, parse the WAV header carefully (44-byte standard header for 16-bit PCM — read `data` chunk size to know how many samples follow) and implement a simple moving-average filter over the sample array using a window read from `/home/user/filter_config.ini` (parse the `window_size=X` line yourself, e.g. with a small string split, don't hardcode it); apply the filter symmetrically or with edge handling so you don't crash on boundary samples, then write out a valid WAV with the same header/format. Compile with `g++ /home/user/restore.cpp -o /home/user/restore_bin` and test it manually first: run it directly against `/app/noisy_backup.wav` and listen to or inspect the output waveform/amplitude statistics to judge whether the window size is too small (noise remains) or too large (audio gets muffled/lowpassed too aggressively) — iterate on the window value based on this before wiring up cron. Set up `socat TCP-LISTEN:9090,bind=127.0.0.1,fork TCP:127.0.0.1:8000 &` in the background. Write `run_restore.sh` to check port 9090 is listening (e.g. `nc -z 127.0.0.1 9090`) then invoke `restore_bin` with the exact input/output paths given. Install the cron entry with `crontab -l | { cat; echo "* * * * * /home/user/run_restore.sh"; } | crontab -`, then wait and confirm `/home/user/final_restored.wav` appears before considering the task done.
