Build this as a set of independent, testable pieces rather than one monolith. First create the five directories under `/home/user/`. Write `/home/user/nginx.conf` as a full, self-contained config (own `pid`, `error_log`/`access_log` paths under `nginx_logs/`, no `user` directive since it's unprivileged) listening on `127.0.0.1:8080`, with an `include /home/user/proxy_conf/*.conf;` directive inside the `server{}` block so per-backup snippets get picked up on reload; start it with `nginx -c /home/user/nginx.conf` (not the system config) and confirm it's running before moving on. Write `/home/user/restore_tester.py` to: glob `*.tar.gz` in `restore_inbox/`, extract each to `restore_active/<name>/` (strip `.tar.gz` for the dir name), walk the tree applying `os.chmod` (750 dirs, 640 files) and shell out to `setfacl -R -m u:nobody:rX <dir>` wrapped in a try/except so a missing ACL filesystem doesn't crash the script, then probe ports from 9000 upward with a bind-test until one is free, launch `python3 -m http.server <port> --directory <dir>` via `subprocess.Popen` in the background, write the matching `location /<name>/ { proxy_pass http://127.0.0.1:<port>/; }` snippet, move the tarball to `restore_processed/`, and finally call `nginx -c /home/user/nginx.conf -s reload` only if anything changed. Install the crontab with `crontab -l | { cat; echo "* * * * * python3 /home/user/restore_tester.py >> /home/user/cron_tester.log 2>&1"; } | crontab -` (preserve any existing entries). Run the script manually once yourself with a test tarball to confirm the whole chain (extract, permissions, proxy, reload, curl through 8080) actually works end to end before writing `READY` to `/home/user/setup_done.txt`.
