Design this as a true single-pass streaming pipeline in Rust: open /home/user/raw_etl_dump.txt with a `BufReader` and iterate `.lines()` — never call `read_to_string` on the whole file. For each line, apply normalization in this exact order: lowercase, trim leading/trailing whitespace, then collapse runs of multiple spaces to one (e.g. split on whitespace and rejoin with single spaces handles both trim and collapse at once), and skip if the result is empty. Maintain a `HashSet<String>` of normalized strings seen so far; skip (don't emit or count) any line already in the set, otherwise insert it. For new unique lines, count words by splitting on the single space you just normalized to, then maintain a `VecDeque` (or Vec capped at 3) of the last up-to-3 word counts, push the new count, evict the oldest if the window exceeds 3, and compute the rolling average as sum/len of whatever is currently in the window — round to exactly 2 decimal places (e.g. `(avg * 100.0).round() / 100.0`) before serializing. Append each result as one JSON object per line (text, words, rolling_avg) to /home/user/processed_dataset.jsonl, writing incrementally rather than buffering all records, then build and run the program to produce the final file.