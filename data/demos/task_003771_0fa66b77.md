Before writing any new code, open `/app/ts_processor/ts_processor/core.py` and locate the `process_and_validate` function's resampling/gap-fill line — look for a typo'd method name (something that would raise `AttributeError`) or a logically wrong aggregation/fill choice where the task says gaps should be filled via linear interpolation, not by an aggregation that leaves NaNs. Fix it to resample to the given frequency and then interpolate missing values linearly (pandas' `.interpolate(method='linear')`), and confirm with `assert df['value'].isna().sum() == 0` style sanity checks. Reinstall the package locally (`pip install -e /app/ts_processor`) so your fix is picked up. Then in `/home/user/clean_data.py`: read the raw CSV, first drop rows where `value` is outside [0, 100] (this must happen before calling the package, per the instructions — doing it after would let the package's own resample/interpolate see noisy values), then call the fixed `process_and_validate(df, 'D')`, and write the result with exactly `timestamp,value` columns to `/home/user/processed_sensor_data.csv`. Since grading is MSE against a hidden ground truth, be precise about interpolation being computed on the timestamp axis after resampling, and verify duplicate timestamps are also handled (dropped/aggregated) before resampling so they don't corrupt the interpolation.