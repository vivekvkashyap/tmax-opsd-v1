Use pandas for a clean melt/validate/rolling pipeline rather than hand-rolled loops. Steps: (1) read the CSV, then melt the wide columns `cpu_cores`, `memory_max`, `disk_gb` into long format with columns `parameter`/`value` keyed by `time`,`server`, dropping rows where the value is blank/NaN (empty strings must be treated as missing, not as the string "" or 0 — coerce to numeric with `errors='coerce'` or check for empty string explicitly before dropping). (2) Apply the inclusive bound check per parameter type using the ranges given in the task — a value is validated independently of what other columns in its original row were, since reshaping happens first. (3) Filter to just `memory_max` rows that passed validation, sort by `server` then `time`. (4) Group by `server` and compute a rolling window of size 3 with `min_periods=1` (not the default which would produce NaN early), take the mean, then round to exactly 2 decimals — watch for banker's rounding differences and use `round(x, 2)` consistently. (5) Write out only the four required columns in the exact order, formatted so the file matches expected content line-for-line — check whether memory_max should print as float (e.g. `1024.0`) to match. Verify by diffing your output against manually recomputed expected rows before finishing.