Parse both CSVs with the `csv` module, building an adjacency list from `edges.csv` respecting direction (src -> dst only, never traverse dst -> src). Implement Dijkstra's algorithm with `heapq` (a min-heap of (cost, node, path)) since edge costs are non-uniform — do not use plain BFS, which would only work if all costs were equal. Track the predecessor of each node (or accumulate the path in the heap tuples) so you can reconstruct the exact node sequence from "A" to "E" once you pop "E" off the heap, not just the total cost. After finding the path, look up each node's `base_value` from `nodes.csv` (cast the CSV string to `int`) and sum them for every node on the path, including both endpoints. Write the result as JSON to `/home/user/result.json` with keys `shortest_path_cost` (int), `total_node_value` (int), and `path` (list of node_id strings in traversal order). Only use standard library modules (`csv`, `json`, `heapq`) — no `networkx`/`pandas`.