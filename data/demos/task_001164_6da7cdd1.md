Translate the Cypher-style multi-hop pattern into a single SQL join over the edges/nodes tables rather than writing separate queries per hop. In C, use `sqlite3_open`, then build one parameterized statement that self-joins `edges` four times (or joins `edges` to `nodes` at each hop): Author node -> edges(relation='authored') -> Paper node -> edges(relation='uses') -> Dataset node -> edges(relation='tagged') -> Topic node, filtering the final Topic's name to the target topic via a bound parameter (`sqlite3_bind_text`) rather than string-interpolating it into the SQL. Add `ORDER BY a.name ASC, d.name ASC` in the SQL itself so you don't need to sort in C. Step through results with `sqlite3_step`/`sqlite3_column_text`, write each row as `name,name\n` directly with `fprintf`, and write the header line first — make sure there's no trailing blank line and no extra whitespace around the comma. Compile with `gcc query_graph.c -lsqlite3 -o query_graph` (link the sqlite3 library explicitly, a common build-error trap), run it, and diff the produced `/home/user/output.csv` against your own manual trace of the graph joins to sanity-check correctness before finishing.