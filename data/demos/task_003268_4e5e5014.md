Handle extraction format differences explicitly: use `tarfile` for the `.tar.gz` archives and `zipfile` for the `.zip`, extracting each into its own versioned directory. Write the INI-to-JSON conversion as a reusable function using `configparser` to read sections/keys (all values stay strings by default, which matches the requirement) and `json.dump` to write, mapping section name -> {key: value}; walk each version directory recursively (`os.walk` or `pathlib.rglob`) to find all `.ini` files, convert each to a sibling `.json` with the same basename, then delete the original `.ini` only after the JSON write succeeds. For the diff report, walk v1 and v3 by relative path (post-conversion, so compare `.json` files, not `.ini`), build sets of relative paths in each, and classify: paths in both with differing parsed-JSON content go into "changed" (compare loaded dict objects, not raw text, to avoid false positives from key ordering/whitespace), paths only in v3 go into "new" — sort both lists before writing. When building the final tar.gz, be careful to add the *contents* of `v3/` at the archive root, not the `v3` directory itself (use `arcname` in `tarfile.add` to strip the leading `v3/` prefix), and include `report.json` at the archive root alongside them.