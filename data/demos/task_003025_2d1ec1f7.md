Build the pipeline in the exact order specified since each step feeds the next. Load `refs.csv` and `mixture.csv` with pandas. Baseline-correct only the mixture: `np.polyfit(wavelength, intensity, 2)` then `np.polyval(coeffs, wavelength)` and subtract to get `Corrected_Intensity` — do not touch the reference spectra. Form `X` as the 4 reference columns (Comp1..Comp4, in that column order) and `y` as corrected intensity. Compute `U, S, Vt = np.linalg.svd(X, full_matrices=False)` — this `S` (untruncated) is what you print on the report's first line, formatted to 4 decimals, descending (SVD already returns descending order). For the pseudoinverse, manually build `S_inv` by inverting each singular value only if it's `>= 0.05`, else setting that slot to 0 (do not use `np.linalg.pinv`'s default `rcond`, since its truncation rule differs from a hard 0.05 threshold on raw singular values — build the diagonal matrix yourself), then `beta = Vt.T @ np.diag(S_inv) @ U.T @ y`. Compare each of the 4 betas against `historical_bounds.json`'s inclusive min/max per component, and write the report in the exact `Singular values: ...` / `CompN: value (STATUS)` format specified, matching precision and line order exactly.
