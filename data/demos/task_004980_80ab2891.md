Handle encoding detection defensively: for each CSV in `/home/user/data/reviews/`, don't assume UTF-8 — try `chardet.detect(raw_bytes)` (install via pip if missing) or attempt a fallback list of encodings (utf-8, latin-1, shift_jis, windows-1251, etc.) with try/except UnicodeDecodeError until one succeeds, since the regional files were written with different codecs and a naive `open(..., encoding='utf-8')` will crash on some of them. Parse each successfully-decoded file as CSV with a header, and on the `review_text` column run `re.findall(r'\b[A-Z]{2,3}-\d{4,5}\b', text)` to extract product codes exactly as specified (mind word boundaries so codes aren't over- or under-matched). Use `concurrent.futures.ProcessPoolExecutor` (or `multiprocessing.Pool`) to dispatch per-file extraction workers in parallel, then merge each worker's list of codes back in the main process and tally counts with a `Counter` or plain dict. Write the aggregated {code: count} mapping as JSON to `/home/user/product_frequencies.json`. Test your encoding-detection and regex logic on a couple of files manually first (print decoded text) to confirm you're not silently mis-decoding or missing matches before running the full parallel pipeline.
