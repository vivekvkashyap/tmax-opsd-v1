Scaffold with `cargo new graph_optimizer`, add `serde`, `serde_json`, and `csv` (plus `serde_derive`) to `Cargo.toml`. Define `#[derive(Deserialize)]` structs matching the exact JSON shapes for nodes, edges, and queries, and load all three files with `serde_json::from_reader`. Build an adjacency list (`HashMap<String, Vec<(String, u64)>>`) from the directed edges. For each query, implement Dijkstra's algorithm with a `BinaryHeap` (use `Reverse` for min-heap ordering) tracking both the running cost and a predecessor map so you can reconstruct the actual path, not just the distance — this predecessor-chain reconstruction is the part naive implementations skip. After finding the shortest path node sequence, sum the edge costs along it for `total_cost`, and separately sum every visited node's `delay` field (including both endpoints) for `total_delay`. Write results with the `csv` crate (or manual formatting) to `/home/user/results/optimized_paths.csv` with the exact header `query_id,path,total_cost,total_delay` and path nodes joined with `->`. Build with `cargo build --release` and run the resulting binary to produce the file; verify with `cat` that formatting (no extra quoting/whitespace) matches the required CSV structure exactly.
