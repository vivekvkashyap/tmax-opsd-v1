First explore the schema and data with `sqlite3 /home/user/infra_graph.db ".schema"` and a few `SELECT` probes so your script's queries are grounded in real column values, not assumptions. Write `/home/user/analyze_graph.sh` as `#!/bin/bash` taking `$1` as the DB path, then `chmod +x` it. Step 1: use `sqlite3 "$1"` to `DROP TABLE IF EXISTS ServiceDependencyGraph` then `CREATE TABLE ... AS SELECT e.source, e.target FROM Edges e JOIN Nodes ns ON e.source=ns.id JOIN Nodes nt ON e.target=nt.id WHERE e.type='DEPENDS_ON' AND ns.label='Service' AND nt.label='Service'`. Step 2: `CREATE INDEX idx_sdg_source ON ServiceDependencyGraph(source)` to speed forward traversal. Step 3: join Nodes/Edges to find Services that DEPENDS_ON a Database that is HOSTED_ON server "srv-core-01", dedupe with `DISTINCT`, `ORDER BY` name, and append under a `--- Pattern Match ---` header. Step 4: write a `WITH RECURSIVE` CTE over `ServiceDependencyGraph` tracking edge-count depth from "GatewayAPI", find the minimum depth reaching "PaymentProcessor", and append that integer under `--- Shortest Path ---`. Build the query logic dynamically (no hardcoded result values) and check `/home/user/output.log` formatting matches exactly after running the script.
