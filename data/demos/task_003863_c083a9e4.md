Recover the missing archival rule from the audio first: transcribe `/app/voicemail.wav` to get the exact MB size threshold and the specific file extensions to include — don't assume typical values, extract them from the transcript. Read `/etc/archival/legacy_policies.conf` for the directory-exclusion and age rules that must also apply. Then write `/home/user/archive_filter.sh` as a pure-bash/coreutils stream filter: read stdin line by line with `while IFS=' ' read -r path size ts; do ... done`, being careful that file paths may contain spaces — since the record format is `<path> <size> <timestamp>` with the path potentially containing spaces, you likely need to parse from the right (size and timestamp are the last two whitespace-delimited fields) rather than splitting naively from the left. Filter on size (convert MB threshold to bytes and compare with `-gt`), extension (check the path's suffix against the allowed list), and any excluded directory prefixes from the legacy config. For passing rows, base64-encode the path with `base64 -w0 <<< "$path"` (stripping the trailing newline `base64` adds) and floor-divide size by 1024 using integer arithmetic (`$((size / 1024))`), emitting `ARCHIVE_REQ|<b64path>|<size_kb>`. Test extensively against edge cases (spaces in paths, boundary sizes) since correctness is checked by fuzzing against a reference implementation.
