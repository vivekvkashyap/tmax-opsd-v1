Work through this as four independently-testable layers, verifying each with a direct probe before moving on. (1) In api.c, find the `strcpy`/`sprintf` writing the User-Agent header into `char user_agent[128]` and replace with a length-bounded call (`strncpy` with explicit null-termination, or `snprintf`) sized to the buffer, then `make` and confirm with a manual overlong-header curl that it no longer crashes before touching anything else. (2) Fix nginx.conf's empty server block: add a `location /api/ { proxy_pass http://127.0.0.1:8080/; }` inside the `server { listen 8000; ... }` block, then test with curl directly against 8000 before wiring in SSH. (3) Edit sshd_config: set `PasswordAuthentication no`, `PermitRootLogin no`, `AllowUsers devsecops`, and remove/comment RSA/ECDSA `HostKey` lines leaving only the Ed25519 one — test each with an actual ssh attempt (password login should fail, root should fail, devsecops with the provided key should succeed) rather than assuming the config file alone is sufficient. (4) Compute `sha256sum /app/bin/api` fresh AFTER your final recompile (order matters — hashing before the last rebuild will mismatch) and write just the hex digest to integrity.txt. Finish by running start.sh and re-verifying all three protocols end-to-end.
