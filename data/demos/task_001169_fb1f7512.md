Start by reading `/home/user/auth_service/src/main.rs` carefully to identify exactly which JWT header value and which payload claim(s) the verifier checks for, and in what literal JSON form (spacing matters for substring checks like `"alg":"none"`). This is a classic alg=none forgery: construct a JWT with three dot-separated parts — a base64url (no padding) encoded header JSON asserting the none algorithm, a base64url (no padding) encoded payload JSON containing whatever role/claim the source code checks for granting admin/superadmin access plus `"sub":"auditor"`, and an empty third segment (since there's no signature). Use Python's `base64.urlsafe_b64encode(...).rstrip(b'=')` or an equivalent no-padding base64url encoder — do not use the standard base64 alphabet, and confirm the decoded bytes exactly match what the Rust source string-matches against. Save the assembled token as a single line to `/home/user/forged_token.jwt`. Separately compute `sha256sum /home/user/auth_verifier` and write just the hex digest to `/home/user/binary_hash.txt`. Finally run `/home/user/auth_verifier /home/user/forged_token.jwt`, capture stdout to `/home/user/audit_trail.log`, and confirm it reports success at the elevated role.
