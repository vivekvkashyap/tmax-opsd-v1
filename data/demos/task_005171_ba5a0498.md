Split this into two independent, verifiable pieces rather than trying to do it all in one script. For the video: extract the frame at the 4-second mark precisely (e.g. `ffmpeg -ss 00:00:04 -i /app/graph_record.mp4 -frames:v 1 out.png`, adjusting slightly if the exact frame lands off by a fraction), then run `tesseract` on that frame (upscale/threshold the image first if OCR is noisy) to read the overlay text, and don't guess the value — confirm the OCR output matches the required `NODE_ID_XXXX` pattern before writing it into result.json. For the Cypher filter: parse each `MATCH` clause and look specifically for comma-separated top-level patterns — the key detection logic is that a `MATCH` clause is "evil" only if it contains multiple comma-separated node/pattern groups where at least one group has no relationship arrow/edge (`-->`, `<--`, `-[...]-`) connecting it to the rest; a single connected pattern like `(a)-[:KNOWS]->(b)` is safe even though it has no top-level comma, while `(a), (b)` or `(a)-[:KNOWS]->(b), (c)` (note `(c)` is disconnected) is evil. Test your regex/parsing logic against both a clearly safe and a clearly evil example you write yourself before trusting it, since the hidden corpora will include edge cases like the latter mixed pattern.