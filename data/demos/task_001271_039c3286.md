First extract the exact algorithm from the image rather than guessing at "adaptive step-size": OCR or visually read `/app/specification.png` carefully to get the precise initial conditions, loop condition, update order, and the exact formula for how `dt` changes each iteration, plus the analytical closed-form validation formula — small deviations (e.g. updating `dt` before vs after using it, or the wrong dependency of `dt` on `K`) will fail exact fuzz-equivalence against the oracle. Rewrite `/home/user/decay_simulator.py` to read FASTA from stdin (skip `>` header lines, concatenate sequence lines), compute `K` as GC content ratio exactly as the current buggy script already does (that part is likely already correct), then replace the fixed-`dt` Euler loop with the adaptive one from the image, and separately compute the analytical exact value with the image's formula. Print both values space-separated, each formatted to exactly 4 decimal places with `f"{x:.4f} {x_exact:.4f}"`. Before trusting your implementation, manually trace through a couple of small K values by hand or in a REPL to confirm the loop terminates and matches the spec's stated stopping condition, since off-by-one iteration counts are the most common cause of fuzz-test mismatches.
