Treat this as two independent forensic sub-tasks that both feed one output file. For the coefficient: open `/home/user/capture.pcap` with `tshark -r capture.pcap -Y udp -T fields -e data.text` (or `tcpdump -A -r capture.pcap`, or a Python `scapy` script reading `Raw` payloads) and scan the decoded payloads for a cleartext string of the form `COEFF=<value>`; extract exactly that numeric value, do not round or reformat it — write it verbatim to `config.txt`. For the regression: use `git log --oneline v1.0..main` to see the commit range, then either `git bisect start`, mark `main` bad and `v1.0` good, and let bisect walk the history running `make test` (or `./test_sim`) at each step, or manually checkout each commit between the tags in order and run the test to find the first one that fails/produces unstable output; once found, use `git show --stat <commit>` to see which `.cpp` file changed. Watch for numerical-instability bugs like a one-pass variance formula (sum of squares minus square of mean) replacing a numerically stable two-pass computation — that pattern causes catastrophic cancellation on large values. Get the full 40-char hash via `git rev-parse <commit>`, then write all three fields to `/home/user/solution.txt` in the exact specified format.