Build the validator as a script with a clear per-rule check function, since the grader runs it against many hidden clean and evil CSV files and needs a hard 0/1 exit code. Parse the CSV (skip header), and for each rule: (1) reshape conceptually by checking each of `cpu_limit`, `mem_limit`, `net_limit` per row against the "no value > 100" bound; (2) maintain a running rolling window of the last up-to-3 `cpu_limit` values as you read rows in order, compute the mean of whatever is currently in the window (1, 2, or 3 elements) at each row, and fail if that mean strictly exceeds 80.00 — don't require exactly 3 values before checking, the spec explicitly wants partial windows early on; (3) for the video rule, extract the reset timestamps ONCE up front (e.g. with `ffmpeg` extracting frames and checking which are solid red, or another frame-diffing approach against `/app/dashboard.mp4`), convert each qualifying frame index to `frame_index/10.0`, and then for any CSV row whose `timestamp` exactly equals one of those reset timestamps, require `mem_limit == 0` exactly. Use exact/tolerant float comparison for timestamp matching (careful with floating point equality — compare with a small epsilon). Test your script manually against a few synthetic clean/evil CSVs of your own before trusting it against the hidden corpora.
