Build this as a Go program with clearly separated stages, and get each one working before chaining to the next. Extract the tar (`os/exec` calling `tar -xf` or the `archive/tar` package) into a scratch directory, then use `filepath.Walk` to recursively find every `.txt` file regardless of nesting depth. For each file, compute the destination name by taking only the base filename and replacing spaces with underscores (`strings.ReplaceAll(base, " ", "_")`) — flatten means dropping all subdirectory structure, writing directly into `/home/user/processed/`; watch for filename collisions across different subdirectories if they occur. Read each file's bytes and convert ISO-8859-1 to UTF-8 explicitly — Latin-1 to UTF-8 conversion can be done byte-by-byte since every Latin-1 byte value maps directly to the Unicode code point of the same number (`golang.org/x/text/encoding/charmap` if available, or a manual rune conversion loop treating each byte as a `rune`) — don't just copy bytes and assume UTF-8, or accented characters will corrupt. Before appending each manifest line, `syscall.Flock` the open manifest file descriptor with an exclusive lock, write, then unlock immediately — do this per-file, not once globally, since the requirement is about safe concurrent appends. Finally shell out to `zip` or use `archive/zip` to compress the whole `/home/user/processed/` directory (including manifest.log) into `/home/user/final_docs.zip`.
