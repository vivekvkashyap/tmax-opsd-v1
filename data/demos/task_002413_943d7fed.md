Root-cause the ODE solver bug in `/home/user/simulation/integrator.go` before touching anything else: the step-size update multiplies `dt` by `(error/tolerance)`, which shrinks the step when error is high and grows it when error is low — backwards. Flip it to `dt *= (tolerance/error)`, and add explicit clamping so `dt` never leaves `[1e-6, 0.1]` (clamp after the multiply, each iteration). Next, change `main.go` so instead of emitting a flat interleaved `[]float64`, it groups the output into `[][]float64` of `[time, state]` pairs and writes that as a plain comma-separated `output.csv` (no header). For the distance metric, sort both the simulated `y` values and the values from `/home/user/simulation/observational_data.csv`, truncate the longer slice to match the shorter one's length, and compute the mean absolute difference between the two sorted sequences elementwise — this is the standard 1D Wasserstein-1 approximation; write the resulting float to `metric.log`. Finally write `run_pipeline.sh` that builds the binary, wraps execution with `runtime/pprof.StartCPUProfile`/`StopCPUProfile` writing to `cpu.prof` (you must add that import and calls into `main.go`), and exits 0 on success.