Implement PCA yourself as a scikit-learn-compatible Transformer: in `fit()`, compute the covariance matrix of the incoming (already-scaled) data, eigendecompose it with `np.linalg.eigh` (valid since covariance is symmetric), sort eigenvectors by descending eigenvalue, and keep the top 3 as `self.components_`; in `transform()`, project with `X @ self.components_`. Assemble `Pipeline([('scaler', StandardScaler()), ('pca', YourPCA(3)), ('ridge', Ridge())])` so that cross-validation refits the scaler and your PCA independently inside each training fold — this, not any manual leakage guard, is what prevents leakage; never call `.fit`/`.fit_transform` on the full dataset before the CV loop. Sweep `ridge__alpha` over `[0.1, 1.0, 10.0]` using `GridSearchCV` with `KFold(n_splits=5, shuffle=True, random_state=42)` and `scoring='neg_mean_squared_error'`, then take `best_params_` and `-best_score_` and write `{"best_alpha": ..., "cv_mse": ...}` to best_model.json.
