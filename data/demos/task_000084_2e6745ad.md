Work phase by phase and validate each before moving on. Phase 1: open `/app/vendored/fast_bayes/setup.py`, find where the C-extension's `include_dirs` is defined, add `import numpy as np` at the top and pass `include_dirs=[np.get_include()]` to the `Extension(...)` call so the NumPy C headers are found; then run `pip install -e /app/vendored/fast_bayes` and confirm `from fast_bayes import BayesianClassifier` imports cleanly. Phase 2: load `/home/user/data/metrics.csv` (first 4 columns = X, 5th = binary y), use scikit-learn's `StratifiedKFold`/`KFold` with `n_splits=5, random_state=42, shuffle=True` for reproducibility, loop over `alpha in [0.1, 1.0, 10.0]`, fit `BayesianClassifier(alpha=alpha)` per fold and average a scoring metric (e.g. accuracy or log-loss) to pick the best alpha, then write just that number to `/home/user/best_alpha.txt`. Phase 3: fit a final classifier on the full dataset with the chosen alpha, then stand up a minimal HTTP server (Flask/FastAPI/stdlib) bound to `127.0.0.1:8080` exposing `POST /evaluate` that checks the exact `Authorization: Bearer ds-research-2024` header (401 if missing/wrong), times the `.predict_proba(features)` call with a high-resolution clock (e.g. `time.perf_counter()`), converts to milliseconds, and returns the exact JSON shape requested. Leave the server running in the background (e.g. `nohup ... &`) and smoke-test with `curl` before finishing.