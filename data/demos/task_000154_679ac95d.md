Design `provision.py` around idempotent read-modify-write operations rather than blind appends. For the JSON config: load `/home/user/deployment.json`, check whether `"cache"` is already in `backend`'s `depends_on` list before adding it (create the key as `["cache"]` if absent, and skip appending if it's already present) so repeated runs never duplicate entries; similarly just set `frontend.restart_policy = "always"` unconditionally (assignment is naturally idempotent), then write the JSON back out preserving valid structure. For the mock group file: check if `/home/user/mock_group` exists and already contains the exact line `deploy_admins:x:1001:app_runner` before appending — only create/append when it's missing, so a second run doesn't duplicate the line (verify with `grep -Fxq` or equivalent, not just file existence). Finally, run `python3 /home/user/deploy_runner.py`, capturing its stdout to `/home/user/deploy_success.log` (e.g., via `subprocess.run(..., stdout=open(...))`), and run your whole script twice yourself to confirm no duplication and that the log still reports success both times before considering it done.