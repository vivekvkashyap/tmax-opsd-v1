Run `/app/legacy_pathfinder 99` and capture its CSV stdout (`source_node_id,target_node_id`) into a DataFrame — do not try to reverse-engineer or patch the binary itself, just treat it as a fixed data source. Open `/home/user/backup_meta.db` and load the `nodes` table, then filter edges by joining source and target node metadata: keep an edge only if both node_ids exist in the table AND (`source.region == target.region` OR `source.is_global == 1`) — implement this as a merge/join in pandas or a SQL join, not row-by-row Python loops, since there can be tens of thousands of edges. Build a `networkx.DiGraph` from the filtered edges only (unfiltered edges must never reach the graph), run `nx.pagerank(G, alpha=0.85)`, sort by pagerank descending, and slice the top 50. Serialize as a JSON list of `{"node_id": int, "pagerank": float}` objects to `/home/user/top_nodes.json`. Double-check the join direction (source's region determines validity based on source's own `is_global`, not the target's) since swapping it silently produces a different, wrong graph that still "runs".