First OCR `/app/cleaning_params.png` (e.g. with `tesseract`) to recover the WINDOW and ALPHA values printed on it — read them from the OCR output rather than assuming numbers, and sanity-check the parsed integer/float before hardcoding them into the C++ source. Design the pipeline in three clearly separate passes matching the spec: (1) build the regular 1-second grid by walking the sorted input timestamps with a pointer, carrying forward the last-seen value for each grid point (careful with the "use exact value at the very first timestamp" edge case); (2) compute the centered rolling mean by launching worker threads (`std::thread` or OpenMP `#pragma omp parallel for`) that each process a contiguous chunk of indices, clamping the `[i-W/2, i+W/2]` window to the array bounds per element rather than assuming a full window everywhere; (3) run the EMA as a strictly sequential single pass over the rolling-mean output, since EMA has a data dependency on the previous output and cannot be parallelized. Write the STARTED/COMPLETED log lines around each phase, format the CSV output with exactly 4 decimal places, and compile with `g++ -O3 -pthread` (or add `-fopenmp` if you used OpenMP).
