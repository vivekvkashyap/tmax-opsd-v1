Download and extract Fuseki 4.8.0 into `/home/user/fuseki` (no sudo needed since it's a self-contained tarball), then start it in-memory on the default dataset name with something like `./fuseki-server --mem /ds &`, and poll `http://localhost:3030/$/ping` or similar until it's up before proceeding. In `etl.go`, read the CSV with `encoding/csv`, build `INSERT DATA { <http://example.org/subj> <http://example.org/pred> <http://example.org/obj> . ... }` SPARQL Update bodies (URL-encode/escape as needed), and POST them via `net/http` with `Content-Type: application/sparql-update` to `/ds/update`; batching all triples in one request is simplest given the small CSV. For the transitive query, use SPARQL property paths — `SELECT DISTINCT ?x WHERE { <http://example.org/user1> <http://example.org/knows>+ ?x }` — issued as a GET/POST to `/ds/query` with `Accept: application/sparql-results+json`, which correctly captures depth >= 1 transitive closure without you manually recursing in Go. Parse the JSON results, strip each URI down to its local name (text after the last `/`), sort alphabetically, and write the exact `{"source": "user1", "transitive_connections": [...]}` schema to `/home/user/result.json`. Double check the property path direction matches "reachable from user1" (outgoing knows edges), not incoming.
