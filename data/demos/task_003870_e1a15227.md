Work in this order. First install `tesseract-ocr` (and `pytesseract` or shell out to the `tesseract` CLI) and run it on `/app/arch.png`; parse the recovered text to pull out the `MLPClassifier` keyword arguments (hidden_layer_sizes, activation, alpha) rather than guessing them. Load the three CSVs with pandas and inner-merge `features_A` and `features_B` and `labels` on `uuid` (chained `pd.merge(..., on='uuid', how='inner')`), since a naive concat would misalign rows or duplicate columns. The core trap is data leakage: never call `scaler.fit`/`fit_transform` on the full dataset before splitting — instead build `sklearn.pipeline.Pipeline([('scaler', StandardScaler()), ('mlp', MLPClassifier(**ocr_params, random_state=42, max_iter=500))])`, do `train_test_split(..., test_size=0.2, random_state=42)` first, then call `pipeline.fit` only on the training split so the scaler statistics never see test data. For the benchmark, loop `pipeline.predict(X_test)` 100 times with `time.perf_counter()` around each call and report the mean in milliseconds. Finally `joblib.dump(pipeline, '/home/user/solution_pipeline.joblib')`. Run the script end-to-end to confirm the file is produced before finishing.
