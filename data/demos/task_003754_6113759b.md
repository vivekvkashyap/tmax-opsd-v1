Split this into an offline feature-extraction stage and a serving stage. Use `cv2.VideoCapture`, read `fps = cap.get(cv2.CAP_PROP_FPS)` and total frame count to derive integer duration in seconds — don't hardcode a frame count; instead seek to each whole-second timestamp (`cap.set(cv2.CAP_PROP_POS_MSEC, sec*1000)` or compute the corresponding frame index from fps) for `sec = 0, 1, 2, ... floor(duration)-1`ish, being careful about the boundary (stop before you'd seek past the last available frame). For each grabbed frame, convert to grayscale (`cv2.cvtColor(..., COLOR_BGR2GRAY)`) and take `frame.mean()` as the intensity; threshold at 120.0 for `is_bright`. Fit `LogisticRegression(random_state=42)` on `frame_index` reshaped to `(n,1)` against the boolean labels. For the API, use FastAPI dependency injection or a manual header check to enforce the exact Bearer token on every route, returning 401/403 on mismatch — do this as a dependency so it applies uniformly. Define a strict Pydantic model for `/predict`'s body so unexpected/invalid types trigger FastAPI's automatic 422, and load or keep the extracted features/model in memory so `/features` and `/predict` can serve without recomputation. Run uvicorn programmatically or as a background process so it stays up for the automated checks.
