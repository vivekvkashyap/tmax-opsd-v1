Build this as a pipeline: discover, extract-selectively, classify by filename, then link. Use `find /home/user/raw_datasets -type f \( -name '*.tar.gz' -o -name '*.tar' -o -name '*.zip' \)` to enumerate archives regardless of nesting depth, then for each archive list its contents first and extract only entries ending in `.csv`/`.json` (e.g. `tar -tf`/`unzip -l` to filter names, then extract just those) into a scratch dir — never blanket-extract and prune afterward, since stray temp files are easy to leave behind. Parse each valid filename with a regex anchored on `^(\d{4})-(\d{2})-\d{2}_sensor([A-Za-z0-9]+)\.(csv|json)$` to get year, month, and sensor ID; `mkdir -p` and `mv` into `/home/user/clean_datasets/<YYYY>/<MM>/`. For each moved file, `mkdir -p` the matching `/home/user/sensor_views/sensor<ID>/` and `ln -s "$(realpath file)"` there — absolute-path symlinks, not relative. Keep a running counter of files successfully processed and write just that integer to `/home/user/summary.log`. Finally `tar -czf /home/user/final_dataset.tar.gz -C /home/user clean_datasets` so the archive root contains the `clean_datasets/` folder itself.
