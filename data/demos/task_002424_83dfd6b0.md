Treat this as a graph-filtering problem over two CSVs, not a single-pass scan. Load both CSVs (pandas or plain csv module is fine) and, for `relationships.csv`, group edges by `target` and `relation_type`. For each entity of type Company in `entities.csv`, check two conditions independently: (a) count of distinct `source` values where `relation_type == 'SUPPLIES'` and `target == this company` is >= 2; (b) at least one edge with `relation_type == 'LOAN_TO'`, `target == this company`, whose `source` entity has `type == 'Bank'` in entities.csv (join back to entities to check the source's type, don't assume). Among companies passing both filters, pick the one with the highest SUPPLIES in-degree count, breaking ties by alphabetically smallest `id`. Write the result as JSON to `/home/user/analysis_report.json` matching the exact key names and types specified (`target_company_id`, `target_company_name`, `incoming_supplies_count` as an integer). Double check your filtering logic against a couple of entities by hand before writing the final file.