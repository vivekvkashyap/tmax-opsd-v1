First transcribe `/app/analyst_notes.wav` locally (install a CLI STT tool such as `whisper`/`pocketsphinx`/`ffmpeg`+ASR, or use Python's `speech_recognition` package) to recover the four scoring rules and their point values and signs — read the transcript carefully since it defines both positive weights (missing CSP, missing HSTS, missing Secure/HttpOnly cookie flags) and a negative one (Server header leaking a version via a slash); do not assume values. Then write `/home/user/fast_audit.c` that streams `/app/headers_dataset.txt` (do not load it all into memory at once if avoidable, but at minimum avoid O(n^2) string operations), splitting on blank-line-separated blocks, and for each block does case-insensitive header-name checks (e.g. `strcasestr` or manual lowercasing) applying the exact weights you transcribed, accumulating one integer total, printing only that integer to stdout. Compile with `gcc -O3 -o /home/user/fast_audit /home/user/fast_audit.c`. Since the verifier requires ≥10x speedup over a Python baseline, avoid per-line regex/dynamic allocation in hot loops — use fixed buffers and simple substring scans. Sanity-check your score against `/app/naive_score.py`'s logic (with corrected weights) on a small sample before trusting the full run.