Structure the script as three clean stages: (1) build a directed weighted graph from `/home/user/interactions.jsonl` — for each JSON line, add `weight` to the edge `source_id -> target_id`, summing when the same pair appears multiple times (a `defaultdict` or `networkx.DiGraph` with edge-weight accumulation works well); (2) compute weighted in-degree per node as the sum of weights over all incoming edges (careful: this is in-degree, not out-degree — double-check direction against the JSON field names); (3) join node ids to `name`/`department` by querying `/home/user/users.db` with `sqlite3`. Sort candidates by weighted in-degree descending, breaking ties by `id` ascending, and take the top 3. Write `/home/user/top_influencers.json` as a JSON array of objects with keys in exactly this order and naming: `user_id`, `name`, `department`, `weighted_in_degree`. Run the script and re-open the output file to confirm it parses as valid JSON with the right shape before finishing — a common failure mode is silently swapping in-degree for out-degree or forgetting to sum weights across duplicate source/target pairs.
