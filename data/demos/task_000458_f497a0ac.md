This is the same data-leakage pattern as standardization done before the train/test split: in `/home/user/cleaner.c`, the mean and standard-deviation loops currently run over the full `count` of rows. Change both loops to iterate only over the training slice (indices `0` to `train_count-1`, where `train_count = count*0.8`), using population std (divide by `train_count`, not `train_count-1`), then apply that fixed mean/std to normalize every row in both the train-writing and test-writing loops — leave the 80/20 split index and `%.4f` formatting untouched. Compile with `gcc -o /home/user/cleaner cleaner.c -lm` and run `./cleaner /home/user/data.csv` (or however the program takes its input, matching its current argv/hardcoded-path convention) so `train_norm.csv` and `test_norm.csv` regenerate. For the benchmark, write `/home/user/benchmark.sh` to loop 20 invocations of `./cleaner`, capture each run's wall time (e.g. via `date +%s.%N` before/after or the shell's `time` builtin redirected to a var), sum and divide by 20, and write just that float to `/home/user/benchmark_avg.txt`.
