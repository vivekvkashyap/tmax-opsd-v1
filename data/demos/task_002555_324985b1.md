Extract the ground truth first: use `ffmpeg -i /app/incident_log.mp4 -vf fps=1 frame_%03d.png` to pull frames, then read (visually or via OCR) the on-screen text to find the exact required username and internal port — don't guess at plausible values. Then diagnose the actual failures rather than patching symptoms: create the discovered user with `useradd -r -s /usr/sbin/nologin -L <user>` (or equivalent locked/no-shell account), and update the `proxy_pass` line in nginx.conf to point at `127.0.0.1:<discovered_port>` instead of the current mismatched port. For reliability, understand why backend.sh is unstable (read its script — it likely exits/crashes deterministically after some condition) and write supervisor.sh as a tight `while true; do sudo -u <user> /home/user/backend.sh <port>; done` loop with no delay so restarts are effectively instant, since the 95% success threshold requires near-zero downtime between crash and restart. Launch the supervisor in the background first, confirm the backend port is actually listening, then start nginx with the local config, and finally hit the endpoint manually several times with curl to confirm 200s before trusting the automated 100-request check.
