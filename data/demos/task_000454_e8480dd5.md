Build this as a real, executed pipeline, not a manual calculation: write the notebook cells to load the CSV with pandas, identify the `wv_*` columns and their numeric wavelength values from the column names (don't hardcode a wavelength list — parse it from the header so it generalizes), then for each row compute `np.gradient(signal, 10)` (10nm spacing) and take its absolute value. For the four bands, build boolean masks on the wavelength array using inclusive bounds (e.g. `(wv>=400)&(wv<=490)`) and integrate the masked abs-derivative slice with `np.trapz(..., dx=10)` — get the band edges exactly as specified, since off-by-one wavelength inclusion will shift every integral. Write output with columns in the exact order `obs_id, band_1, band_2, band_3, band_4` to `/home/user/band_features.csv`. Critically, actually execute the notebook non-interactively (e.g. `jupyter nbconvert --to notebook --execute --inplace process_spectra.ipynb`) rather than just writing code that would work if run — the CSV must be produced by running the notebook, and you should install pandas/numpy/scipy/jupyter/nbconvert first if missing.
