Work in three independent passes and verify each before moving on. First, run `make` in the project directory to see the actual compiler error; C++ code missing a standard header (e.g. using fixed-width integer types) fails to compile until you add the right `#include`, so patch the source accordingly rather than rewriting logic. Second, OCR the debugger-dump image with `tesseract /app/debug_snapshot.png -` (or similar) to recover the exact numeric constants referenced in the code; read the OCR output carefully since digits are easy to misread, and cross-check by re-running tesseract with different page-segmentation modes if the first pass looks garbled. Third, locate where the current (wrong) constants are used in the transformation loop and replace them with the OCR-recovered values, preserving the surrounding algorithm exactly (order of multiply/mod, loop count) since equivalence is checked bit-exactly against a hidden reference via fuzzing many seeds. Finally rebuild with `make` and confirm the binary lands at `/home/user/project/engine`, and sanity-test it manually on a couple of seeds before considering it done.
