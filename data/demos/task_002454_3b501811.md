Build this orchestration script as a loop over the five noise levels, verifying each step manually once before wrapping it all in the loop. First confirm `papermill`, `jupyter`, `pandas`, `scipy`, and `jq` are installed (pip install as needed). For noise injection, write a small inline Python snippet (invoked from bash, e.g. `python3 -c "..."`) that reads `base_data.csv`, sets a fixed seed (e.g. `np.random.seed(42)`) before adding `np.random.normal(0, sigma, len(y))` to the y column for each sigma in turn — reseeding per sigma value keeps each run reproducible independently. Use `papermill mcmc_template.ipynb /home/user/mcmc_run_<sigma>.ipynb -p input_file /home/user/data_noise_<sigma>.csv -p output_json /home/user/result_<sigma>.json` to inject the two parameters the notebook expects and execute it, checking that the notebook actually completes (papermill will error loudly on cell failure — read that output if something breaks rather than guessing). Extract `estimated_mu` from each result JSON with `jq -r .estimated_mu`, and build `/home/user/summary.tsv` with a literal tab-separated header `NoiseLevel<TAB>EstimatedMu` followed by the 5 rows sorted by ascending noise level (loop in the natural 0.1..0.5 order). Make the script executable and run it end-to-end yourself to confirm `summary.tsv` has exactly 6 lines.
