Read aggregator.cpp's join loop before touching anything else: the bug is an implicit cross-join — a nested loop over neighbors and events that accumulates every event's value regardless of which node it belongs to. Fix it by adding the missing filter (`event.node_id == neighbor`), and critically, reset the rolling-sum accumulator per neighbor (window functions must partition by node_id) and sort each neighbor's events by timestamp before accumulating. Get the two backing services running first via start_services.sh (start the Flask graph API on :5000 and the TCP event stream on :5001, poll until both ports accept connections), then compile with `g++ -O2 -std=c++17 -lcurl -lpthread`. Because grading requires byte-for-byte identical output to /app/oracle_aggregator across many random stdin sequences, match the output format exactly as given (`Query: <node_id> | Rolling Sums: [v1, v2, ...]`) and diff your binary's output against the oracle's for the same varied stdin inputs before considering the fix complete. Do not modify the oracle.
