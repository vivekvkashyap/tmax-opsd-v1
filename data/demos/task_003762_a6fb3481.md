Implement this as a direct translation of the math into code, then verify numerically before trusting the output. Parse the FASTA file (simple header/sequence pairs) and for each sequence count occurrences of K, R, H, D, E, C, Y, plus fixed counts of 1 for N-terminus and C-terminus. Write `Q(pH, counts)` exactly as given: sum `N_i / (1 + 10**(pH - pKa_i))` over the basic groups minus sum `N_j / (1 + 10**(pKa_j - pH))` over the acidic groups, using the exact pKa table provided. Use `scipy.optimize.brentq(Q, 0, 14, args=(counts,))` per sequence to solve for the pI — first sanity-check that `Q(0)` and `Q(14)` have opposite signs (they should, given the pKa ranges) so brentq's bracket is valid. Read `/home/user/experimental_pi.csv` and match by Protein_ID; sort IDs consistently before building the two paired arrays so `scipy.stats.ttest_rel` compares the correct pairs. Round calculated pIs to 2 decimals and t_statistic/p_value to 4 decimals only in the final JSON write, not during intermediate calculations (rounding early would bias the t-test). Print your intermediate calculated pI values while developing to eyeball they're chemically sensible (acidic-heavy sequences should land low, basic-heavy ones high) before writing `/home/user/pi_results.json`.
