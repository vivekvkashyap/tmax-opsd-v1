Work in `/app/bayesian_tracker-1.0.0/`: first fix the `Makefile` by replacing the non-standard `$(CXX_COMPILER)` reference with the standard `$(CXX)` variable so `make` builds with `g++` out of the box. Then open `src/update_engine.cpp` and locate the `bootstrap_update` function — look for the cast of raw integer input to float followed by a sentinel check (`-9999`); the bug is that the cast happens before the sentinel check, letting -9999 corrupt the float math (and any NaN-producing branch) instead of being excluded. Fix it by comparing the raw integer value to -9999 first and `continue`-ing/skipping that element entirely (never casting it), then track a running sum and count of only the valid values for the mean. Rebuild the library with `make`. Next, write a small C++ HTTP server using the header-only `httplib.h` provided in `/app/`, link it against the fixed library, bind it to `127.0.0.1:8080`, and implement `POST /sample` to parse the CSV body into integers, call `Tracker::compute_mean`, and return the mean as plain text with status 200. Launch it in the background, redirecting/logging startup to `/home/user/server.log` with a line confirming the port. Test locally with `curl` sending a payload containing `-9999` before finishing.
