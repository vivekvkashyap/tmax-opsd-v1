Locate the bug precisely: in `/app/ridge_solver/src/solver.cpp`, the 2x2 normal-equations matrix is built as `a = sum_x2`, `b = sum_x`, `c = sum_x`, `d = n` and then inverted directly — the regularization parameter `lambda` (already parsed from `argv[1]`) is never added to the diagonal before computing `det`/the inverse, which is exactly why near-singular inputs blow up. Fix it by changing the diagonal assignments to `a = sum_x2 + lambda;` and `d = n + lambda;`, leaving the off-diagonal `b`/`c` and the rest of the closed-form ridge solution untouched. Recompile with OpenMP support explicitly enabled, e.g. `g++ -fopenmp -O2 -o solver src/solver.cpp` run from `/app/ridge_solver`, and confirm the binary name/location match what `server.py` invokes (`./solver` relative to `/app/ridge_solver`, per its `subprocess.run(..., cwd=...)`). Finally launch `python3 server.py 8080` in the background (e.g. with `nohup ... &` or equivalent) and leave it running, then sanity-test with a `curl -X POST` to `http://127.0.0.1:8080/fit` using near-collinear points to confirm you get finite, stable `w1`/`w0` instead of NaN/huge values.
