Build this as a pipeline of small, verifiable Bash stages inside one script rather than one giant awk one-liner. (1) Mask emails with something like `sed -E 's/^([0-9]+,)[^,]*,[^@,]+@/\1MASKED,***@/'` or an `awk -F,` rewrite of just the email field — test on the header-stripped body only. (2) Normalize timestamps: detect whether a field is all-digits (Unix epoch) vs an ISO8601 string, and convert both to `YYYY-MM-DD HH:MM:SS` UTC using `date -u -d @<epoch> +"%F %T"` for epochs and `date -u -d "<iso>" +"%F %T"` for ISO strings (GNU date parses `Z` fine). (3) Load into SQLite via `sqlite3 analytics.db` with a `CREATE TABLE` then `.import`/`INSERT` of the cleaned rows — watch out for the CSV header row leaking into the table. (4) Compute the three metrics with plain `SELECT COUNT(*)`, `SELECT COUNT(DISTINCT email)`, `SELECT SUM(amount)` queries, then substitute them into `template.html` with `sed` placeholder replacement, writing to `report.html`. Use a temp file only inside `/tmp` or delete any intermediate unmasked CSV with `rm` before the script exits — the final directory must contain no leftover unmasked data. Make the script executable and actually run it once to confirm all three output artifacts appear.
