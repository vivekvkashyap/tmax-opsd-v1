Write a Python ETL script that loops over the JSON files in `/home/user/artifacts/`, computing three independent statistics per experiment, then assembles them into a sorted CSV. For the Bayesian posterior mean, use the closed-form Beta-Binomial conjugate update: posterior mean = (successes + alpha) / (total_trials + alpha + beta) with alpha=2, beta=10 — no sampling needed, this is a direct formula. For mean latency, just average the `latencies` list. For the bootstrap CI, call `numpy.random.seed(42)` once before the bootstrap loop (do this per-experiment or once globally in a fixed processing order — reseed if you need each experiment's bootstrap to be independently reproducible), then resample each experiment's latencies with replacement exactly 10,000 times, compute the mean of each resample, and take the 2.5th/97.5th percentiles via `numpy.percentile`. Round every numeric output to exactly 4 decimal places, sort rows by `experiment_id` string order, write the exact header `experiment_id,posterior_prob,mean_latency,ci_lower,ci_upper`, and use Python's `csv` module (or manual formatting) to avoid stray whitespace or quoting issues. Sanity-check a couple of posterior values by hand against the simple formula before trusting the full pipeline.