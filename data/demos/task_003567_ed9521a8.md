Handle each objective independently and verify each in isolation before moving on. For integrity: compute the checksum yourself (e.g. `sha256sum /home/user/cert_bundle.pem`) and compare it against the value stored in `/home/user/cert_hash.sha256`, writing exactly `VERIFIED` or `FAILED` (no trailing content beyond what's asked) to the status file — don't just trust the hash file blindly, actually recompute and compare. For the credential leak: change `legacy_runner.sh` to export `DB_PASSWORD=<value>` (or set it inline before the python invocation) instead of passing `--db-pass <value>` on the command line, and update `processor.py`'s argparse usage to instead read `os.environ.get('DB_PASSWORD')` — remove the `--db-pass` argument entirely so it can't leak via `ps`/`/proc`. For log redaction: in the log-writing code, substitute the literal password value with the exact string `[REDACTED]` before writing to `/home/user/processing.log`, keeping the rest of the log line's wording intact. Finally re-run the runner script end-to-end and grep the log and a `ps aux` snapshot to confirm the real password never appears anywhere.
