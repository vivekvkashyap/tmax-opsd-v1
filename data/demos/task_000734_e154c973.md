First extract the ground-truth parameters by actually transcribing the audio — use a local speech-to-text tool (e.g. `whisper` if available, or another ASR you can run) on `/app/incident_dictation.wav`; don't guess the keyword or thresholds from the task description alone, since the description intentionally omits them. Listen for: the restricted keyword to compare messages against, the Levenshtein-distance threshold that counts as "too close" (note it operates on UTF-8 codepoints, not bytes — decode multi-byte UTF-8 sequences into codepoints before running Levenshtein, e.g. using `wchar_t`/`mbstowcs` or manual UTF-8 decoding in C), the time-bucket duration, and the event-count threshold per bucket. For the CSV parser, do not use a naive line-based reader — messages are double-quoted and may contain embedded raw newlines, so implement a proper quoted-field state machine that keeps consuming lines while inside an open quote. Parse ISO 8601 timestamps to epoch seconds (e.g. via `strptime` with `%Y-%m-%dT%H:%M:%SZ` and `timegm`), bucket by integer division of epoch by the bucket size, count events per bucket, and compute UTF-8-aware Levenshtein distance between each message and the keyword. Exit 1 if any rule is violated, else 0. Validate against the provided clean/evil corpora before finishing.