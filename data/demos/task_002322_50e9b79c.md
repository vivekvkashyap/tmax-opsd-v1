Work bug-by-bug and re-run after each fix rather than rewriting everything at once. First, run `./run.sh` to see the "data file not found" error, then inspect it and `process_org.py` for how the data directory is determined (likely an environment variable pointing to the wrong path) and correct the path so it points at `/home/user/ticket_8821/data/`. Second, open the org data and process_org.py's recursive traversal logic: since org data can contain cycles (an employee who is, transitively, their own report), add a `visited` set threaded through the recursive calls and skip any employee ID already in it to stop the `RecursionError`. Third, wrap the department lookup so records missing a "department" key are skipped (not silently treated as valid, and not crashing) — check for the key's presence before counting the employee. Fourth, find where multiple threads/workers mutate a shared department-count structure concurrently; protect the read-increment-write with a lock (e.g. `threading.Lock`) so counts aren't lost to race conditions, since the read-then-write pattern is the exact bug that causes indeterministic totals across runs. After all four fixes, rerun `./run.sh` several times and confirm `output/summary.json` is byte-for-byte identical across runs with plausible department counts.