Start by transcribing `/app/dictation.wav` (e.g. with `whisper` or another installed speech-to-text tool) and carefully parse the spoken numeric phrases ("zero point five", "one point two") into the alpha and beta weights the script needs — don't guess these values, extract them. Build `/home/user/profile_workflow.ipynb` as a minimal notebook that imports cProfile, constructs a synthetic JSON input with 50+ coordinates, and calls `cProfile.run(...)` against `/app/slow_metric.py`'s logic to demonstrate where the nested loops are slow (this is a profiling deliverable, not something the grader fuzzes). For `/home/user/fast_metric.py`, read JSON from stdin, convert `coords` to a NumPy array, and vectorize the O(n^2) computation: compute the full pairwise distance matrix via broadcasting (e.g. `np.linalg.norm(coords[:,None,:]-coords[None,:,:],axis=-1)`), build a same/mismatch boolean mask from the sequence characters, apply alpha where equal and beta where not (e.g. `np.where(mask, alpha, beta) * dist_matrix`), sum everything, and print with `f"{total:.4f}"`. Since the grader fuzzes against a hidden compiled oracle for bit-exact match, make sure your loop order and pair semantics (all i,j pairs including i==j, not just upper triangle) exactly mirror the given slow reference's semantics, not merely a mathematically-equivalent alternative.