Do the two independent sub-investigations before writing the shortest-path tool. First, transcribe `/app/incident_voicemail.wav` (e.g. with a local whisper model or available speech-to-text tool) and read the transcript carefully — it names the specific corrupted index you must avoid relying on. Second, confirm empirically that the named index causes wrong results: query `dependencies` both via a plan that would use that index and via one that forces a full table scan (`INDEXED BY` / `NOT INDEXED`, or simply `DROP INDEX` it in your own working copy) and compare row counts/content to see the discrepancy, rather than assuming which index is broken. Once confirmed, drop or bypass that index before ever reading `dependencies` in your script. Load `services`/`dependencies` into an in-memory graph (e.g. via `networkx`, using `latency` as edge weight) and implement Dijkstra between the two CLI-provided ids, printing only the integer total latency, or `-1` if disconnected — no extra text, since the fuzz-equivalence checker does exact stdout comparison against the oracle over many random id pairs.