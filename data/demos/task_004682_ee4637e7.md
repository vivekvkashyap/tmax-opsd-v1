Tackle the four stages independently and test each before integrating. Compile `fast_mse.c` as a shared library with `gcc -shared -fPIC -o fast_mse.so fast_mse.c` so `ctypes.CDLL` can load it and call `compute_mse` with `ctypes.POINTER(ctypes.c_double)` args. For the glitch filter, don't hand-tune thresholds against only the sample corpus — derive a physically-motivated rule from consecutive-frame displacement: for each CSV, compute per-step Euclidean distance between consecutive (x,y) rows and flag as EVIL if any single-frame jump is implausibly large relative to the rest of the trajectory (a fixed but reasoned pixel-distance threshold, since clean data has small smooth deltas and evil data has one or more large "teleport" deltas); validate against both provided corpora, aiming for a threshold that clearly separates them since the hidden eval corpus follows the same generation logic. For video tracking, use OpenCV: read frames, threshold/invert to isolate the darkest blob, and compute its centroid via `cv2.moments` or contour analysis per frame, writing `frame_index,x,y` in order. For model fitting, implement a real iterative optimizer (e.g., coordinate descent or gradient-free search) over `A, omega, phi, gamma, C` that calls into `fast_mse.so` each iteration and stops once relative MSE improvement drops below `1e-6`, then save the five fitted parameters as JSON.