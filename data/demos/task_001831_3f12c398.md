Use `git bisect` properly: mark `v1.1.0` as good and the current HEAD (or a known-bad recent commit) as bad, then at each bisect step build the tool and run it against log lines that include timestamps without fractional seconds (plain RFC3339, e.g. `2023-10-12T15:00:00Z`) to check whether entries are silently dropped — script this check so `git bisect run <script>` can automate it rather than testing by hand each step. Once bisect identifies the offending commit, inspect its diff to `parser.go`: a common regression here is hardcoding the timestamp layout to `time.RFC3339Nano`, which makes `time.Parse` fail (and thus drop) on timestamps lacking fractional seconds. Fix it by trying multiple layouts (RFC3339Nano, RFC3339, and any other format the tool supports) or using a more permissive parsing approach, so both fractional and non-fractional timestamps parse correctly. Rebuild with `go build` (vendor dir already populated, so use `-mod=vendor` if needed) and place the binary at `/home/user/fixed-stitcher`. Test it by piping varied timestamp-format JSON logs through stdin and confirming none are dropped and ordering stays correct.