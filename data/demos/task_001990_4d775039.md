Handle the mixed-encoding read first, since a naive `open(path)` with a fixed encoding will crash or mis-decode the UTF-16 files. Detect encoding per file (e.g. check for a UTF-16 BOM at the start of the raw bytes, or try `open(path, encoding='utf-8')` and fall back to `utf-16` on `UnicodeDecodeError`) before parsing. Parse `.ini` content manually or with `configparser` to extract just the key names under `[Security]` — remember the section ends at the next `[...]` header or EOF, so track section state line-by-line rather than assuming `[Security]` is always last. Group files by the `YYYY-MM` prefix parsed from the filename (`backup_YYYY-MM-DD.ini`), and for each month take the union of Security keys across all its files (a month can have multiple backup files, each contributing keys). Sort months chronologically, then for each month (from the second onward) compute Jaccard distance against the immediately preceding month's key-set: `1.0 - len(intersection)/len(union)`. Write `/home/user/drift_report.csv` with header `Month,Previous_Month,Distance`, rows in chronological order, distances formatted to exactly 3 decimals (e.g. via `f"{d:.3f}"`).