Work the two parts independently, then verify against edge cases. For the vendored package: inspect `/app/fisher-0.1.9/setup.py` for the C-extension build config — check `extra_compile_args` (or similar compiler-flag lists) for anything that isn't a real GCC/Clang flag (invalid `-O` levels are a common sabotage pattern); remove or replace the bogus flag with a valid one like `-O2`, then `pip install -e .` and confirm `import fisher; fisher.pvalue(...)` works from Python before writing the processing script. For the script: build the 2x2 contingency table exactly as specified (`[[c00,c01],[c10,c11]]` using cat1 as rows, cat2 as columns) and pass counts as plain ints to `fisher.pvalue(...).two_tail`. For PCA, mean-center each of the 3 continuous columns, run `numpy.linalg.svd` on the centered matrix, take `Vt[0]` as PC1, and apply the sign convention (flip whole vector if first element is negative) — sign ambiguity is the most common source of mismatches with a reference implementation. For the CI, use the sample stddev with `ddof=1` and the correct t-distribution critical value (e.g. `scipy.stats.t.interval` or manual `t.ppf`) rather than a normal-distribution approximation. Round all floats to exactly 4 decimals and print a single JSON line with `json.dumps`, matching key order/names exactly.
