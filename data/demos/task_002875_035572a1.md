Extract frames from the video with `ffmpeg -i /app/attack_session.mp4 -vf fps=<rate> frames/frame_%04d.png` (or step through frames around the 2-second mark at 30fps) and read the terminal text visible in those frames carefully — you need to recover the exact command and its parameters (the tool name, the flag used to set an XOR key, and the flag used to select hex output format) rather than assuming a generic scheme. Once you've read off the specific XOR key and output encoding from the frame(s) showing the command and its result, write `/home/user/detector.py` to: take a directory argument, read each file's contents as a hex string, decode the hex to raw bytes, XOR every byte with the key you recovered, and check the resulting plaintext for known injection signatures (script tags, SQL injection patterns like `' OR '1'='1`, event-handler payloads, etc.) using a reasonably broad but precise signature list so you don't false-positive on benign decoded text. Print only the basenames of files that decode to a match. Before finalizing, test against a few files you construct yourself (hex-encode an XSS string XORed with your recovered key) to confirm your decode-and-match logic actually flags it, and confirm it does not flag arbitrary hex-encoded benign text.
