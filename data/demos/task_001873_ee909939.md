Set up a real cargo project (`cargo new /home/user/extractor`) and implement the binary-format parser as a loop: read a `u16` little-endian length, read that many bytes as UTF-8 path, read a `u32` little-endian length, read that many raw data bytes, repeat until EOF — use `byteorder` or manual `u16::from_le_bytes`/`u32::from_le_bytes` on a buffered/streamed reader. The security fix is the crux: never join the raw path onto the output directory; instead take only the final path component (equivalent to `Path::new(path).file_name()`, or split on both `/` and manually strip any leading `..`/absolute markers) so `../../../etc/passwd` and `/root/secret.txt` both collapse to just their basename. Apply the `.md`→`.html` conversion by checking the case-insensitive extension of that basename, wrapping the raw bytes in the exact literal `<html><body><p>` / `</p></body></html>` markers, and renaming the extension. For every output file, lowercase the final name and prepend `doc_` before writing into `/home/user/extracted_docs/`. Build and run the binary against `/home/user/legacy_docs.bin`, then manually diff each produced file's content against what the archive entry should decode to.
