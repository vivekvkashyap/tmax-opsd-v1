Load both CSVs with pandas and do `primary.merge(secondary, on='link_code', how='left')`. The trap: after a left join, unmatched `extra_notes` become NaN, and if you build the document string with an f-string over the whole DataFrame before cleaning, `record_id` can get upcast to float (e.g. becomes 1001.0) because pandas sometimes coerces int columns when NaNs are introduced elsewhere, or because you cast the whole row to string. Explicitly force `record_id` to `int` (e.g. `df['record_id'].astype(int)`) before formatting into the document, and fill missing `extra_notes` with an empty string (`fillna('')`) rather than letting the literal "nan" string leak in. Build `document = f"{int(record_id)} {description} {extra_notes}"` per row. Fit `TfidfVectorizer()` with defaults on the `document` column, compute the full pairwise cosine similarity matrix, mask the diagonal (self-similarity) so you don't pick a record paired with itself, find the max off-diagonal entry, and write the two record_ids sorted ascending, comma-separated, to the output file.
