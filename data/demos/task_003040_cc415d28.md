Build the filter as a single C program with two entry paths sharing one core `is_clean(line)` function: (1) a `--test <in> <out>` mode that reads lines and writes only clean ones, for offline tuning against `/home/user/corpus/clean/` and `/home/user/corpus/evil/`; (2) a daemon mode that opens a UDP socket bound to `127.0.0.1:5000`, and for each clean received datagram opens (or keeps open) a TCP connection to `127.0.0.1:8080` and writes the line. Define "clean" precisely and conservatively: reject any byte < 0x20 except `\n`, reject any of `| > < & ; $` , and reject if any whitespace-delimited token exceeds 100 chars — apply the identical check function in both modes so tuning against the sample corpus actually reflects daemon behavior. Test against the given corpora first (adjust the rule set until 100% clean/evil separation), since the hidden validation corpus will exercise edge cases like embedded nulls, very long lines, and near-miss syslog formats. For the network leg, bring `tap0` up with `ip link set tap0 up` and assign `10.0.2.2/24` so the QEMU guest at `10.0.2.15` can reach the host's UDP 5000; then launch `start_services.sh` and your filter binary as background daemons before finishing.