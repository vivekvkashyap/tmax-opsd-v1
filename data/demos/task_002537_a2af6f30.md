Read `sync_log.txt` with `encoding='ISO-8859-1'`, then split it into records (e.g. on `RECORD_START`/`RECORD_END` or with a regex capturing `Target: (.+)`) preserving the order of appearance — do not use a set or dict that could reorder or dedupe. For each target path, open it in binary mode (`'rb'`) inside a try/except that catches `OSError`/`FileNotFoundError` — a circular symlink raises `OSError: Too many levels of symlinks`, and a missing file raises `FileNotFoundError`; catching broad `OSError` covers both cleanly. Only read `f.read(4)`; if fewer than 4 bytes are returned, skip that record too. Convert the bytes to hex with `.hex()` (already lowercase). Build the JSONL lines in original order, write them to a temp file in the same directory (e.g. via `tempfile.NamedTemporaryFile(dir=..., delete=False)` or a `.tmp` suffix), flush/close it, then call `os.rename()` to move it atomically to `/home/user/artifact_repo/magic_numbers.jsonl` — writing directly to the final path breaks the atomic-write requirement. Finally, actually execute the script with `python3`.
