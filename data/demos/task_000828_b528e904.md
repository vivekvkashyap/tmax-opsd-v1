This is a small, fully-determined numeric pipeline — work it out by hand first (or in a scratch calculation) so you know what correct output looks like, then implement the shell logic to reproduce it. In `process.sh`: create the artifacts dir/file with the given data, use `awk` to read the CSV (skip the header), compute each column's mean by accumulating sums across all data rows, then re-pass the data to subtract each column's mean (mean-centering) and accumulate the population variance (`sum(sq)/n`, not sample variance — no ddof correction). Compare the three variances to determine which single feature has the lowest variance and must be dropped (you must actually compute this, don't hardcode which columns survive without checking, since a naive attempt often assumes alphabetical/first-two-columns instead of comparing computed variances). For the two retained mean-centered features per row, compute the L2 norm as `sqrt(a^2+b^2)` and format to exactly 2 decimals — `awk`'s `sqrt()` plus `printf "%.2f"` handles this cleanly, or use `bc -l` with `scale`. Write header `id,l2_norm` then the four rows to `/home/user/processed_norms.csv`, and chmod +x the script.