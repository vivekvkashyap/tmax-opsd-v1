Work through this in dependency order. First compile the proto with `python3 -m grpc_tools.protoc -I. --python_out=. --grpc_python_out=. math_ops.proto` (installing `grpcio`/`grpcio-tools` via pip --user first) to produce `math_ops_pb2.py` and `math_ops_pb2_grpc.py` in the same directory as `server.py` expects to import them. For the memory leak, open `libmath.c` and look for a `malloc` whose buffer is never released before the function returns — add the matching `free()` call right before `return`, then rebuild with `gcc -shared -fPIC -o libmath.so libmath.c`. For the constraint problem, don't guess x and y — either install `z3-solver` and encode `x+y==120, x*y==3456, x>y` as constraints and solve, or brute-force iterate x from 119 down to 61 checking `x*(120-x)==3456` and `x>y`; use whichever integer solution the constraints uniquely determine. In `test_client.py`, start `server.py` in the background (e.g. `subprocess.Popen`), wait briefly for it to bind port 50051, then use the generated gRPC stub to call `ComputeMagic` with your solved x,y, and confirm `/home/user/result.json` gets written by the server as a result of that call.