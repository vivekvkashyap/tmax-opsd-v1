Write `pipeline.py` to read the CSV with the standard `csv` module (or `pandas`), strip HTML tags per row using a regex like `re.sub(r'<[^>]+>', '', text)`, then collapse runs of whitespace with `re.sub(r'\s+', ' ', text).strip()` — apply tag-stripping before whitespace-collapsing so tag boundaries don't leave stray spaces uncollapsed. Load the `cl100k_base` encoding via `tiktoken.get_encoding("cl100k_base")` and call `enc.encode(text)` per cleaned row to get an exact token count (don't approximate with word/char counts). Accumulate `total_rows` (row count), `total_tokens` (sum of per-row token counts), and `total_cost` as `total_tokens * 0.0002` rounded to 4 decimals with `round(...)` — compute the cost from the aggregate token sum, not by summing already-rounded per-row costs, to avoid rounding drift. Write these three keys to `/home/user/metrics.json` via `json.dump`. In `run_pipeline.sh`, `pip install tiktoken` (and pandas if used) before invoking `python3 pipeline.py`, and `chmod +x run_pipeline.sh`.
