Start by correlating the three log files in /home/user/logs/ chronologically (sort/merge by timestamp) to spot which payload shape precedes the memory spike in the aggregator — look specifically for JSON arrays with trailing commas or empty elements, since malformed-array error paths are the classic leak trigger in hand-rolled JSON parsers. Open /app/vendor/json11/json11.cpp and inspect the array-parsing function's error-handling branch: look for any `new`/heap allocation (e.g. building a detailed error message) on a path that returns early without a matching `delete` or without storing the pointer somewhere it gets freed — that's the leak. Fix it by either removing the unnecessary heap allocation, using a stack-local/static string, or ensuring it's deleted before every return path. Validate the fix by building a small standalone harness that links json11.cpp, feeding it a minimal reproducer with a trailing-comma array, and running it under valgrind to confirm zero leaked blocks. For the standalone filter, write /home/user/json_filter.cpp to detect the same structural anomaly (not full JSON parsing) directly in the raw text/bytes — a lightweight scan for the offending malformed pattern is more robust and faster than reusing the buggy parser — then compile it to /home/user/json_filter and return exit code 1 for evil payloads, 0 for clean ones.