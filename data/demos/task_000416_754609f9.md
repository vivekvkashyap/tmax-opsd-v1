First run `tesseract /app/release_spec.png` (try preprocessing with `convert`/`imagemagick` — grayscale, threshold, upscale — if raw OCR output is noisy) and locate the `MAX_AST_DEPTH=<N>` line in the extracted text; use that recovered integer as your depth limit constant, don't hardcode a guess. For the engine, read `main.cpp`/`parser.cpp` carefully for classic C++ pitfalls: unmatched `new` without `delete` (leaks, confirm/fix with `valgrind --leak-check=full`), missing `argv[1]` bounds checks, and unguarded division — add explicit zero-divisor checks before evaluating. Write a `CMakeLists.txt` targeting the `mathscript` executable and build into `/app/build/`. Implement `verify` mode as a separate code path from `eval`: parse the expression into an AST without evaluating, walk it to track nesting depth against your OCR'd limit, detect syntax errors during parsing, and detect division-by-zero by checking if the right operand of `/` is a literal (or constant-folds to) zero — print SAFE/exit 0 or UNSAFE/exit 1 accordingly. Before finishing, run your binary manually against a handful of files from both corpora to sanity-check behavior, since the grader requires 100% accuracy on both.
