Build the four pieces independently and verify each before moving on. For Step 1, use `ffmpeg -i /app/dashboard_capture.mp4 -vf fps=1 .../frame_%d.png` (or similar), then count the actual extracted files with `ls | wc -l` and write that real count to `frame_count.txt` — don't assume a duration, derive it from the extraction. For Step 2, the rules given are explicit: reject on `$(...)`, backtick command substitution, or the literal string `DROP TABLE`; implement this with `grep -E` patterns (e.g. matching `\$\(` and backtick-delimited spans and the literal string) rather than something narrower, since the script will be graded against a held-out eval corpus with different sample text — test against the provided clean/evil corpora and check exit codes for every file. For Step 3, write the two routing lines with the exact CIDR/gateway values and format given in the spec, verbatim, no extra whitespace. For Step 4, chain the pieces: extract frames, then for each `.eml` in `/app/incoming_alerts/`, invoke `alert_filter.sh` and copy only exit-0 files into `verified_maildir/new/` (create the directory first). `chmod +x` every script.