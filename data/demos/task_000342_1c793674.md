First deduplicate `/app/graph.db`'s `edges` table with plain SQL, e.g. `CREATE TABLE edges_dedup AS SELECT DISTINCT src, dst FROM edges; DROP TABLE edges; ALTER TABLE edges_dedup RENAME TO edges;` via `sqlite3 /app/graph.db "..."`. Then run `tesseract /app/task.png stdout` to OCR the query spec out of the image rather than guessing the analytics requirement — read the OCR text carefully since it defines the exact query semantics (hop count, direction, which degree to compute, sort order) your Go endpoint must implement; don't assume a generic "shortest path" or "degree" query without checking what the image actually says. Implement `/app/server.go` using `database/sql` with the `mattn/go-sqlite3` driver (or a pure-Go driver if cgo is unavailable) to open the cleaned DB, start `http.ListenAndServe("127.0.0.1:9000", ...)`, and in the `POST /analyze` handler run the two-step graph traversal (first-hop then second-hop via SQL joins on `edges`) followed by an in-degree count per candidate node computed over the whole deduplicated edge set. Marshal results into the JSON shape the OCR'd spec describes, sorted per its stated tie-break rule, and keep the server running in the background for the verifier's HTTP calls.
