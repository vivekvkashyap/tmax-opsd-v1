Use pandas groupby operations rather than manual iteration so the per-translator ordering/imputation stays correct. Merge telemetry with strings on `string_id` (left join keeping all telemetry rows). Sort by `translator_id` then `timestamp` before any fill so ffill/bfill respect chronological order within each group; impute with `df.groupby('translator_id')['edit_distance'].apply(lambda s: s.ffill().bfill())` (ffill first, then bfill only fills what ffill couldn't, matching "if the first record is missing use bfill after ffill"). Compute `normalized_edit_distance` using Python's `len()` on the `translated_text` string directly — this is correct for multi-byte Unicode since Python 3 strings are counted by codepoint, not raw bytes, so don't do any manual UTF-8 byte-length calculation. For the rolling average, group by translator again and apply `.rolling(window=3, min_periods=1).mean()` on the normalized column, keeping the per-group chronological order from the earlier sort. Filter rows where `rolling_avg > 0.50` strictly, round to 3 decimals, and emit only the required keys into a list of dicts written as JSON. Print/inspect intermediate DataFrames while developing to catch ordering bugs before trusting the final anomaly list.