Use Python's `tarfile` module to open `/home/user/artifacts.tar.gz` and iterate members in memory/streamed rather than extracting the whole archive to disk permanently — you can extract just each needed member to a temp location (or extract to a scratch dir and clean it up) to run `readelf` on it, since `readelf` needs a real file path. For each `build_*` directory found, run `readelf -h main.elf` and parse the "Entry point address:" line to get the hex value exactly as reported (don't hardcode or assume a value — different binaries will differ). For `trace.log`, count blocks by scanning lines and incrementing a counter each time you see the exact `--- BEGIN CRITICAL ---` marker (each BEGIN closed by a matching END counts as one block); don't just count either marker alone in case of malformed data. Build a list of `(build_id, entry_point, error_count)` tuples, sort by `build_id` alphabetically, and write the CSV with the exact header and no extra blank lines to `/home/user/report.csv`. Finally, use `split -b 40 -d /home/user/report.csv /home/user/chunks/part_` (or equivalent) so chunks are exactly 40 bytes except possibly the last, and sanity-check by concatenating the chunks back together and diffing against the original CSV.
