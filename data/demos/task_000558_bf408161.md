Work through this as a sequential pandas/sklearn pipeline on `/home/user/reviews.csv`, being careful about validation order: enforce schema first by attempting to coerce `review_id` to int (drop rows where it fails, e.g. `pd.to_numeric(..., errors='coerce')` then drop NaNs) and dropping rows where `text` is empty/NaN — do this before fitting anything so invalid rows never enter the vectorizer. Fit a single `TfidfVectorizer(stop_words='english', max_features=50)` on the *entire* cleaned `text` column (labeled and unlabeled rows together) and transform all of it — don't fit separately per subset, since vocabulary must match. Split the resulting feature matrix (keeping index alignment) into rows where `category` is `'electronics'`/`'books'` (labeled, for training) versus empty/missing (unlabeled, for prediction). Train `MultinomialNB()` on the labeled subset, then call `.predict_proba()` on the unlabeled subset. For benchmarking, isolate just that `predict_proba` call, run it in a tight loop 100 times, and average the wall-clock time (`time.perf_counter()` around each call) into `/home/user/benchmark.json` as `{"avg_inference_time_sec": <float>}`. Build `/home/user/predictions.csv` with columns `review_id, prob_books, prob_electronics` (map class probabilities to the right columns via the model's `classes_` order, not by assumed order), sorted ascending by `review_id`.
