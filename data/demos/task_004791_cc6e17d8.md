Design the C++ program around a per-MachineID sliding window (e.g. `std::deque<double>` capped at K=10, keyed in a `std::map<std::string, deque<double>>`), processing rows in file order (already chronological) and grouping computation by machine as you go — don't sort or batch by machine first, since mean/std must be the *rolling* state at the time each row arrives. Critically, compute the rolling mean/stddev from the window's *current* contents BEFORE inserting the new reading, and use the correct denominator: fewer than 2 elements means std=0 and mean=the reading itself (per spec), otherwise use the sample standard deviation (divide by N-1, not N) — mixing up population vs sample stddev is the most common bug. Standardize with z=(V-mean)/std unless std==0 then z=0, flag anomaly when |z|>3.0, then push V into the deque and evict the oldest if size exceeds 10. Format every float output with exactly 4 decimal places (`std::fixed << std::setprecision(4)`). Compile with `g++ -std=c++17 detector.cpp -o detector`, run it, and diff a few output rows by hand against a quick mental/python check before trusting the CSV.