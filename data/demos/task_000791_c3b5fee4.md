Build this as a clear script pipeline (Python + pandas/scikit-learn is the natural fit) and verify each stage's intermediate output before proceeding. First, load the CSV and compute the pairwise Pearson correlation matrix over just the sensor columns (`df[sensor_cols].corr()`); scan the upper triangle for any |correlation| > 0.90, and for each such pair drop the alphabetically-later sensor name into a set (use a set since one sensor could be flagged against multiple others) — do this iteratively/generally rather than hardcoding which columns you expect to be correlated, since your code should work if the underlying data differs. Second, split by row order (not `train_test_split` with shuffling) using `int(0.8*len(df))` as the cutoff, train `sklearn.linear_model.LinearRegression` on the retained-feature training rows, predict on the test rows, and compute RMSE via `sqrt(mean_squared_error(...))`. Third, write the JSON report with `dropped_sensors` and `retained_sensors` both sorted alphabetically and `rmse` rounded to exactly 2 decimals with `round(rmse, 2)`. Run the script and open `/home/user/report.json` to confirm the structure and value types match the spec exactly before finishing.
