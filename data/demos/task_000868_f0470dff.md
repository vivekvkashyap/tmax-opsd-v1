Split this into three independent, independently-testable pieces. First, get the frame count F from the reference video with ffmpeg/ffprobe (e.g. `ffprobe -v error -count_frames -select_streams v:0 -show_entries stream=nb_read_frames -of csv=p=0 /app/reference.mp4`, or decode with ffmpeg and count output frames) — do not hardcode a guessed frame count, derive it from the actual file since the fixture value isn't given to you. Second, scaffold `cargo new` at `/home/user/pipeline`, add the `csv` crate, and write the filter as a true streaming reader/writer (csv::Reader/Writer over buffered file handles, processing one record at a time) rather than loading the whole file into memory — this matters because verification uses large evil/clean corpora. Filtering logic: split description on whitespace, lowercase each token, and drop the row if any token exactly equals "eval" or "script" (not merely a substring match — a naive `contains("eval")` would over- or under-match legitimate text) or if the token count exceeds F. Preserve the original unmodified text/formatting for rows that pass. Build in release mode (`cargo build --release`) and confirm the binary path matches exactly. Finally write `setup_cron.sh` that appends/installs a crontab line via `(crontab -l 2>/dev/null; echo "0 * * * * /home/user/pipeline/target/release/filter /var/data/incoming.csv /var/data/clean.csv") | crontab -`, taking care not to duplicate or clobber any existing crontab entries.
