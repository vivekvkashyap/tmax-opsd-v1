Build this as a short pipeline of shell stages, verifying each stage's output before chaining the next. (1) Detect/convert encodings explicitly rather than assuming: use `iconv -f UTF-16LE -t UTF-8` on sensor_a.csv and `iconv -f ISO-8859-1 -t UTF-8` on sensor_b.csv (you can confirm with `file` beforehand), strip any BOM, and save both as clean UTF-8 temp files, dropping each file's header row before merging. (2) Merge and sort numerically/chronologically on the `time` field with `sort -t, -k1,1n` (not a plain lexical sort, since time values may not be zero-padded). (3) Deduplicate on `device,value,status` while keeping the first chronological occurrence — since the data is already time-sorted, a single-pass `awk` that tracks a seen-set keyed by those three fields (skip lines already seen) does this correctly; don't use `sort -u`, which doesn't preserve first-occurrence order reliably across all fields. (4) Compute the per-device rolling average over the last up-to-3 records using `awk` with a per-device array/queue (push new value, evict oldest beyond 3, average what remains), applied only across each device's own sequence, not globally. (5) Format `rolling_avg` to one decimal (`printf "%.1f"`), assemble the header plus rows into `/home/user/processed.csv` in UTF-8.
