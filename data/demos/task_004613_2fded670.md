First fix `/app/networkx-vendored/setup.py`: run it (`python setup.py --version` or `pip install -e .`) to surface the exact SyntaxError and line, then diff-review that region for a missing comma/paren in the `setup()` call arguments — fix minimally, don't rewrite the file. Install with `pip install -e /app/networkx-vendored` and confirm `import networkx` and `nx.node_link_graph()` work before writing detection logic. For the detector, load each JSON with `nx.node_link_graph(json.load(...))`, collect nodes with `status == "sanctioned"` and `status == "cleared"`, and for each sanctioned→cleared pair use `nx.shortest_path_length(G, source, target)` (catching `NetworkXNoPath`) to check if the shortest path has ≤3 edges — a violation exists if any such pair qualifies. Write matching basenames (not full paths) one per line to the output file, only for graphs with at least one violation. Before finishing, self-test by running your script against both `/app/corpus/evil/` and `/app/corpus/clean/` and confirming every evil file appears in the output and no clean file does — this catches off-by-one errors in the depth-3 boundary condition.