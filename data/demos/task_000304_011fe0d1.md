Read each line of `expressions.txt`, `eval()` it (or use a safe arithmetic parser) to get a numeric result, and immediately format that result as a string with exactly 4 decimal places (`f"{result:.4f}"`) before hashing with `hashlib.md5(...).hexdigest()` — hashing the raw float or a differently-rounded string is the main way naive attempts fail to match near-equal results like `50.0` vs `50.00000001`. Iterate lines in order, keep a `seen` set of hashes, and retain only the first expression per new hash (skip later duplicates) — track total line count and duplicate count as you go. Write `pipeline.log` with the literal `INFO: Processed <N> lines, found <D> duplicates.` line. Load `template.md`, and do simple string `.replace()` of `{{UNIQUE_COUNT}}` with the integer count and `{{EXPRESSIONS_LIST}}` with newline-joined `"<expr> = <result rounded to 1 decimal>"` lines in original first-appearance order — reformat to 1 decimal separately from the 4-decimal hash key. Save the substituted text to `/home/user/report.md`.
