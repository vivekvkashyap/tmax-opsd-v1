Work through the pipeline stages in dependency order, fixing the build first since everything downstream needs the compiled binary. Read the compiler error from `make build` — with `-Werror` active, missing `#include <stdio.h>` and `#include <string.h>` (needed for `printf`/`strlen`) triggers implicit-declaration errors; add just those includes, don't touch the Makefile's flags. For migrate.sh, use `sqlite3 /home/user/logs.db "ALTER TABLE access_logs ADD COLUMN threat_level INTEGER DEFAULT 0;"` guarded so it's safe to rerun (e.g. check `PRAGMA table_info` first, or ignore the "duplicate column" error). For fuzz.sh, generate each random string with a mix of alnum chars (e.g. via `tr -dc 'a-zA-Z0-9' </dev/urandom | head -c N` or `$RANDOM`-seeded loops), splice in one `<` or `>` at a random position, run `./sanitizer "$str"` and grep its output to confirm no `<`/`>` remains; only append FUZZING_PASSED if every one of the 50 trials holds, otherwise FUZZING_FAILED with exit 1. Chain everything in ci_pipeline.sh using `set -e` (or explicit `|| exit 1` after each step) so any failure stops execution before ci_success is created, then run it manually to confirm the artifact and log both appear.
