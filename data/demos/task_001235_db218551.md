Start by extracting the actual rules from `/app/dba_memo.wav` — don't assume generic backup-validation rules. Run an ASR tool (e.g. `whisper` if available, or another installed speech-to-text utility) on the audio and read the transcript carefully; the rules it states are the exact spec your script must enforce, so quote-check your transcript against what a validator would need (root node name, forbidden node name, cycle prohibition) rather than paraphrasing from memory. Then design `validate_manifest.sh` around three checks on the child→parent edge list: (1) build an adjacency map and, for every node, walk parent pointers with a visited-set to confirm the chain terminates at the required root — a visited-set is essential to avoid infinite looping on cycles; (2) reject if the forbidden node name appears anywhere in the file; (3) explicitly detect cycles (a node revisited before reaching the root, or self-loops) and treat that as failure even independent of the root-reachability check, since a cycle might coexist with other nodes reaching the root. Test your script by hand against a few manifests you construct yourself (one cyclic, one with the forbidden node, one broken-root, one clean) before trusting it on the hidden corpus.