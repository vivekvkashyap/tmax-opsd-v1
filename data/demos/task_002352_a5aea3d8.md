Work through the four stages in order, verifying each output before moving to the next. First, install `pandas`, `pyarrow`, `scipy`, `numpy`. Second, read `/home/user/artifacts/raw_logs.csv`, coerce `y_pred`/`y_true` to integers with error handling (e.g. `pandas.to_numeric(..., errors='coerce')`) and drop any row where casting fails or fields are missing/empty (including empty `model`), then write the cleaned frame to `/home/user/artifacts/cleaned_logs.parquet` via `pyarrow`/`pandas.to_parquet` — print row counts per model afterward as a sanity check against the expected split. Third, for the Bayesian step, treat each row's `y_pred == y_true` as a Bernoulli success/failure; starting from a Beta(2,2) prior, the conjugate posterior is simply `alpha = 2 + successes`, `beta = 2 + failures` per model — compute these directly from counts (no MCMC needed) and write them to `/home/user/artifacts/posteriors.json` in the exact given schema. Fourth, for the bootstrap CI: call `numpy.random.seed(123)` exactly once immediately before the resampling loop, then do 10,000 iterations each sampling the *entire* cleaned dataset (with replacement, same size) and recomputing `accuracy_beta - accuracy_alpha` on that resample; use `numpy.percentile` at 2.5 and 97.5 on the resulting distribution, and write `lower,upper` rounded to 4 decimals to `/home/user/artifacts/bootstrap_ci.txt`. Getting the seed placement and resampling unit (whole dataset, not per-model separately) exactly right matters since the check is an exact numeric match.