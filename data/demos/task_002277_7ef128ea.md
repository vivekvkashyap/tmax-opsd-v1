Write `/home/user/analyze.sh` as a pipeline of sort + join + awk. First sort both `temps.txt` and `loads.txt` on the composite key (timestamp, server_id) using `sort -t'|' -k1,1 -k2,2` since `join` requires sorted input on the join fields, then `join -t'|' -1 1 -2 1` won't directly work with a two-field composite key — instead build a combined key per line (e.g. with `awk -F'|' '{print $1"|"$2"|"$3}'` for each file) and join on that single combined field, or use `join -j 1` after prefixing each line with `timestamp_serverid` as field 1. After joining, use `awk` to filter: numeric check on cpu_temp/cpu_load (a regex like `^-?[0-9]+([.][0-9]+)?$`) and the `<= 100` bound, discarding ERR/blank/out-of-range rows. Feed the surviving (temp, load) pairs into a single `awk` pass accumulating N, sumX, sumY, sumXY, sumX2, sumY2, then compute slope/intercept/correlation with the standard least-squares formulas inside `awk` (or shell out to `bc -l` for the sqrt), rounding to 4 decimals. Emit valid JSON to `/home/user/results.json` and `chmod +x` the script. Trace through the filtering step manually against the sample data to confirm exactly which rows survive before trusting the final numbers.
