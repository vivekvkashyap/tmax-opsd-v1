Write the C program in a strict order because seccomp strict mode permanently restricts the process to `read`, `write`, `_exit`, and `sigreturn` — do all setup (socket creation, connect loop, DNS/addr resolution) before calling `prctl(PR_SET_SECCOMP, SECCOMP_MODE_STRICT, 0, 0, 0)`, and after that point use only raw `read()`/`write()`/`_exit()` — no `printf`, no `close()`, no libc buffered I/O, since any other syscall kills the process immediately. To find the live service, loop `connect()` over ports 8000-8100 on 127.0.0.1 (a plain `socket()`+`connect()` per port, checking the return value, closing failed attempts before seccomp is active) until one succeeds. Once connected and sandboxed, `write()` the literal bytes `AUTH: REDTEAM\n` to the socket, `read()` the response into a buffer, then `write(STDOUT_FILENO, ...)` it out and `_exit(0)`. Compile with `gcc /home/user/payload.c -o /home/user/payload`, run it, and redirect stdout to `/home/user/flag.txt` (e.g. `./payload > /home/user/flag.txt`).