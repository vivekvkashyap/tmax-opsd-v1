Layer the work: get the auth token first, then config, then the binary protocol parser, then the HTTP server. Transcribe `/app/directive.wav` with a local speech-to-text tool (e.g. `whisper`) and normalize the result to lowercase with no punctuation — that exact phrase is the bearer token you must check against the `Authorization: Bearer <phrase>` header. Parse `server.conf` as simple `key=value` lines to get `port` and `extract_dir`, creating `extract_dir` if missing. Implement the ARTF binary parser carefully with correct endianness: read the 4-byte magic, then a little-endian uint16 file count, then per file a little-endian uint16 name length, the raw (non-null-terminated) name bytes, a little-endian uint32 size, then that many data bytes — use explicit byte-shifting or `memcpy` into a `uint16_t`/`uint32_t` rather than assuming host struct layout matches the wire format. For the Zip Slip check, scan every filename in the archive for a leading `/` or a `../` substring before extracting anything; if any is unsafe, extract nothing and return 400, otherwise extract all and return 200. You can build the HTTP layer with raw sockets and minimal request-line/header parsing since only `POST /upload` matters. Run the compiled server in the background and manually curl it with valid/invalid tokens and safe/malicious archives to confirm each response code before leaving it running for the verifier.