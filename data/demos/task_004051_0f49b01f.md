Build the audit script as independent checks per file, then merge results, rather than one tangled pass. Walk the tree with `os.walk('/home/user/suspect_dir')`, and for each file use `os.stat` to get `st_mode`: test world-writable via `mode & stat.S_IWOTH`, and SUID/SGID via `mode & (stat.S_ISUID | stat.S_ISGID)`. Separately, compute each file's SHA-256 (read in binary chunks with `hashlib.sha256`) and compare against the set of hashes loaded from `/home/user/ioc_hashes.txt` (strip whitespace/newlines per line). For any file flagged by any of the three checks (world-writable, SUID/SGID, or IOC match) — note world-writable alone should NOT trigger flag extraction per the spec, only SUID/SGID or IOC match — read its contents as text (handle non-UTF8 gracefully, e.g. `errors='ignore'`) and apply `re.findall(r'EVIDENCE_FLAG_[A-Z0-9]{16}', content)`. Accumulate results into sets to dedupe, then sort each list with plain `sorted()` before writing. Serialize with `json.dumps(..., indent=2)` to `/home/user/forensics_report.json`. Double check by re-reading the report and cross-checking each list against a manual `ls -la` / `sha256sum` pass over the suspect directory.
