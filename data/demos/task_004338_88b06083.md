Work stage by stage and verify each before moving on. First scaffold the Rust crate with `cargo new --lib sanitizer_project`, set `crate-type = ["cdylib"]` in Cargo.toml, and implement `is_clean` with `#[no_mangle] pub extern "C" fn is_clean(input: *const c_char) -> bool`, converting the raw pointer safely with `CStr::from_ptr` (guard against null) and doing a case-insensitive substring check (lowercase the string, then check for each of the three patterns). Build with `cargo build --release` and locate the resulting `.so` under `target/release/`. Before wiring anything else, write a quick ctypes test script loading the `.so` directly and running it against every file in `/home/user/data/evil/` and `/home/user/data/clean/`, since those two corpora are your correctness gate — the evil/clean directories and their generator files don't exist yet, so create them yourself with varied casing/spacing around the three trigger patterns for evil, and unrelated benign text for clean. Only after the Rust logic is verified standalone should you write `build.sh` (compile + copy .so to `/home/user/app/libsans.so`), `wrapper.py` (ctypes loader exposing `check_text`), `app.py` (Flask `/submit` route using wrapper + Redis), and `config.env`. Start Redis and Flask, then re-run the same corpus through the live HTTP endpoint to confirm 200/403 behavior end-to-end, and leave both processes running.
