Structure this as a standard Go module build: `go mod init bioanalysis` in /home/user/analysis. Write a FASTA parser that reads sequence records (header lines starting with `>`, sequence lines following), then fan out GC-content computation across goroutines — e.g. a worker pool reading from a jobs channel and writing {sequence, gcContent, hasMotif} results to a results channel, synchronized with a `sync.WaitGroup`, rather than computing everything serially. Classify each sequence into Group A/B by checking `strings.Contains(seq, "ATGCATGC")`, accumulate per-group sums/counts, then compute means, absolute difference, and the >5.0 significance boolean — round all floats to 3 decimals only when marshaling to JSON (keep full precision during computation) and write to results.json matching the exact key names and structure shown in the task. For the plot, use gonum.org/v1/plot to build a simple two-bar chart of the group means and save as gc_plot.png. Write analyzer_test.go with table-driven cases (all-G, all-A, mixed 50/50, empty string) asserting your GC-percentage function's output, and confirm `go test` passes before finishing.