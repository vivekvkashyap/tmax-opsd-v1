Use Python's `os.kill(pid, 0)` inside a try/except `ProcessLookupError`/`PermissionError` to check liveness without actually killing anything — do not shell out to `ps` and grep unreliably. Read the PID from `/home/user/app.pid`, wrapping the read/int-parse in try/except so a missing or malformed file falls straight into the recovery branch. For recovery, do the three steps strictly in order: (1) build the tarball with `tarfile.open(path, "w:gz")` and `add()` the whole `/home/user/data` directory (or shell out to `tar czf`), writing exactly to `/home/user/backup/data_archive.tar.gz`; (2) open `/home/user/alerts.log` in append mode and write the exact alert string plus a newline; (3) launch the worker with `subprocess.Popen([...])` (not `subprocess.run`, which blocks) so it detaches and keeps running after your script exits — don't wait on it. Make sure `/home/user/backup` exists (create with `os.makedirs(exist_ok=True)`) before writing the tarball. After writing the script, actually execute it once so the recovery path fires against the currently-stale PID, and verify afterward that the new PID file was overwritten by the worker itself.