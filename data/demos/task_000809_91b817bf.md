Build this as a strict Unix pipeline, verifying each stage's output before moving to the next rather than writing one big script blind. Extract the tarball with `tar -xzf`, then check the raw file's actual byte encoding (e.g. `file raw_sensors.csv` or inspect for a BOM/null-byte pattern typical of UTF-16LE) before converting with `iconv -f UTF-16LE -t UTF-8`. For the wide-to-long reshape, parse the header row to split each non-Date column name on the last underscore into City and a language-tagged metric label, then translate only the known non-English metric tokens (Japanese/French/Russian words for temperature/humidity) to `Temperature`/`Humidity` via a small case/lookup table — leave already-English labels untouched. Derive Year and Month by splitting the Date string on `-` (it's already YYYY-MM-DD, no date parsing library needed). Emit one output row per (date, city, metric) combination. Finally sort the data rows lexicographically by Date, then City, then Metric (`sort -t, -k3,3 -k4,4 -k5,5` on the assembled long file, keeping the header separate), and ensure the file has Unix LF endings and valid UTF-8 (e.g. run `dos2unix` or strip `\r` explicitly, and validate with `iconv -f UTF-8 -t UTF-8`).
