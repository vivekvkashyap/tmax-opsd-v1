Split this into "make it correct" then "make it fast/orchestrated." First compile `/home/user/build_tools/checksum_engine.cpp` with `g++ -O2 -fsanitize=address -g` and run it against small then large/edge-case inputs — ASAN will pinpoint the exact line of the off-by-one overflow (likely a padding/chunk-boundary buffer sized one byte short) and the double-free (probably a pointer freed once in a normal path and again in an error/cleanup path); fix both so ASAN reports zero errors on repeated runs, not just once. Wrap the corrected logic in a small standalone CLI (`checksum_standalone`) that reads all of stdin as raw bytes and writes the hex checksum to stdout — you won't have the reference oracle to compare against directly, so instead reason from the algorithm's own spec/comments in the source to ensure your fixed version preserves the original intended output for well-formed inputs, and fuzz-test it yourself against edge sizes (0 bytes, 1 byte, multi-MB) for crashes/leaks before considering it done. Separately, fix `start_pipeline.sh` and `resolver.py` to point at the correct Redis/checksum-engine URLs and start all three services, then write `ci_verify.sh` to bring them up, POST to `/resolve`, and log the response to `/home/user/build_result.log`.