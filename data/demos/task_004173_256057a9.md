Break this into its three phases and validate each independently before moving to the next. Phase 1: run `/app/start_services.sh` and read the actual failure — generate a self-signed cert/key pair with `openssl req -x509 -newkey rsa:2048 -nodes -keyout /app/nginx/ssl/key.pem -out /app/nginx/ssl/cert.pem -days 365 -subj '/CN=localhost'`; then inspect the Flask startup (in the start script / app source) to find which port it actually binds, and edit the `proxy_pass` directive in `/app/nginx/nginx.conf` to point at that real port instead of whatever wrong port is configured; confirm with `curl -k https://127.0.0.1:8443/api/health`. Phase 2: parse `/app/logs/api_access.log`, extract the base64 JWT segments from each `Authorization: Bearer <token>` field, base64-decode just the JWT header (first dot-separated segment) for each, and find the one whose decoded header contains an `alg` of `none` (case-insensitive) — write that log line's source IP exactly to `/home/user/attacker_ip.txt`. Phase 3: write `/home/user/jwt_filter.py` using a JWT library configured to verify HMAC-SHA256 signatures with the given secret, explicitly restricting `algorithms=["HS256"]` (never trust the token's own `alg` header) and checking expiry, catching all decode/verification exceptions as invalid; iterate the input directory, and emit a JSON dict of filename→boolean to the output path.