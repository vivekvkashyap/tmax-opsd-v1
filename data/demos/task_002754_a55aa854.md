Work through this in dependency order: services must be reachable before the ETL can run, and the ETL must complete before the query can be validated. First, run `/app/start_services.sh` and inspect `/home/user/config.env` and `/home/user/etl.sh` to see the current (broken) values and connection logic. Fix `config.env` by setting the exact credentials given in the task (Postgres user `admin`/pass `adminpass`/db `network_db` on 5432; Neo4j user `neo4j`/pass `graphpass` on 7687) using the variable names the ETL script actually reads (e.g. `PGUSER`, `PGPASSWORD`, `PGDATABASE`, `NEO4J_USERNAME`, `NEO4J_PASSWORD`) — check both scripts to confirm the exact var names expected. For the ETL rewrite, replace the per-row `cypher-shell` loop with a bulk approach: use `psql ... -c "\copy (SELECT ...) TO 'routers.csv' CSV HEADER"` (or `psql -c "COPY ... TO STDOUT"`) to dump `routers` and `links` to CSV files Neo4j can reach, then issue a single `cypher-shell` session running `LOAD CSV WITH HEADERS FROM 'file:///...' AS row MERGE (:Router {...})` etc., wrapped in `CREATE CONSTRAINT`/index setup first for performance. This eliminates ~10,000 process spawns, which is the actual speed win. For the Cypher query, use a shortest-path/weighted-path construct (e.g. an APOC Dijkstra call or `shortestPath` with relationship weight) that sums `latency` along `CONNECTED_TO` edges between the two named routers, aliasing the result as `total_latency`. Time your own `etl.sh` run to sanity-check the speedup before finishing.