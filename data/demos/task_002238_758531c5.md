Reverse-engineer the oracle's cleaning transform empirically rather than guessing: pipe a range of known synthetic `f1,f2,f3` triples (including negatives, zeros, and simple round numbers) through `/app/clean_oracle` via stdin/stdout and compare outputs to inputs to infer the per-column transform (e.g. scaling, offset, clamping) — vary one column at a time to isolate each rule. Apply that inferred cleaning to all 150 rows of `train.csv` to build your Go training set. For Ridge Regression with gonum, implement closed-form ridge (`w = (XᵀX + λI)⁻¹Xᵀy`) or gradient descent, then do sequential (not shuffled) 3-fold CV exactly as specified — first third is fold 1 test, etc. — computing MSE per candidate λ in `{0.1, 1.0, 10.0}` and picking the lowest-average-MSE one; then refit on the full cleaned dataset with that λ for your deployed model. For serving, the prediction endpoint must apply the *same* cleaning transform to raw incoming features before scoring them (either shell out to `/app/clean_oracle` per request, or replicate the exact transform logic you derived in Go) — a mismatch here is the most likely silent bug. Run HTTP (`net/http`, port 8080, `/predict`) and gRPC (compiled from `/home/user/predictor.proto`, port 50051) servers concurrently in goroutines, keeping the process in the foreground.