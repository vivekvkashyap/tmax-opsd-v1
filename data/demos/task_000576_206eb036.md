Load /home/user/data.csv with pandas, then enforce the schema by coercing each of f1..f10 and target to numeric with `pd.to_numeric(..., errors='coerce')` and dropping any row where that coercion produced NaN (this correctly drops both non-numeric strings and already-missing values) — don't just use `dropna()` alone first, since string garbage like "corrupted_string" won't show as NaN until you attempt numeric coercion. On the cleaned f1..f10 matrix, fit `PCA(n_components=3, random_state=42)` and transform to get 3 components; feed those into `LogisticRegression(random_state=42)` fit on the same rows' target, then score accuracy by predicting on that identical training set (no holdout). Sum `pca.explained_variance_ratio_` across the 3 components for the variance metric. Round both the variance sum and the accuracy to exactly 4 decimal places with Python's `round()` before writing them to /home/user/run_metrics.json in the specified `{"variance_explained": ..., "accuracy": ...}` shape, and actually execute the script so the file is produced, not just written.