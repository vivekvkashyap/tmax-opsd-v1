Design the script around streaming and atomicity, not in-memory batch processing. Read `/home/user/server.log` line by line, use a regex to find the `[IP_ADDRESS]` bracketed token and replace its contents with the literal string `REDACTED` (keep the brackets), then write each redacted line to a temp file in the same directory (so `os.rename` is atomic across the same filesystem) before renaming it to `/home/user/sanitized.log`. While writing the redacted output, track the running byte offset yourself (sum of `len(line.encode())` for prior lines, not just character count) since that's the offset each 16-byte index record must reference — parse the leading `YYYY-MM-DD HH:MM:SS` with `datetime.strptime` and convert to UTC epoch via `datetime(..., tzinfo=timezone.utc).timestamp()`, being careful not to let local timezone leak in. Pack each record as `struct.pack('<QQ', epoch_ts, offset)` (unsigned little-endian 8+8 bytes) into a second temp file, then atomically rename it to `/home/user/index.bin`. Make the script executable and run it, then double check the sanitized file's line offsets by re-reading it in binary mode and comparing against what you computed, since off-by-one byte errors here are the most common failure mode.