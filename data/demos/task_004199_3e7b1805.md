Build this incrementally and check intermediate values as you go, since the final numbers must match an exact reference. Parse the FASTA preserving sequence order (split on `>` header lines, concatenate the following lines per record). Enumerate all 64 3-mers over {A,C,G,T} and sort them alphabetically to fix column order — print them out and eyeball the sort to be sure it's a plain string sort, not something else. For each sequence, slide a window of length 3 across every position from 0 to len-3 inclusive (this is the overlapping-kmer definition given in the prompt) and increment the matching column; separately tally G/C counts per sequence for the target vector Y. Use `numpy.linalg.svd(A, full_matrices=False)` and take the first 3 singular values (they come pre-sorted descending) — round to 4 decimals and comma-join. For ridge regression, solve `(A.T@A + lambda*I) w = A.T@Y` with `numpy.linalg.solve`, not matrix inversion, for numerical stability; sum the resulting weight vector and round to 4 decimals. Write each result to its own output file with no extra text, then re-run your script to confirm it's deterministic before finishing.