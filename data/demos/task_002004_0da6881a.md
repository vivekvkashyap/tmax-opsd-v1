Start by running `make` inside `/app/c-string-hasher` to see the actual compiler error, which will trace back to `hash.h` and `util.h` each `#include`-ing the other and each referencing a struct defined in the other file — the fix is to break the cycle with a forward declaration (e.g. `struct HashContext;` in `util.h`) instead of the full `#include "hash.h"`, keeping the real include only where the struct is actually used with full definition, not just pointer/reference. Rebuild until `libhasher.so` compiles cleanly. For `process.py`, use `ctypes.CDLL(path)`, set `.argtypes`/`.restype` explicitly on `compute_hash` (`c_char_p` in, `c_char_p` or `c_void_p` out) and on `free_hash_result`, call `compute_hash(arg.encode())`, decode and print the result, then call `free_hash_result` on the original pointer to avoid leaking the C-allocated buffer — do this cleanup even though it won't be directly observed, since correct memory handling is part of a correct FFI wrapper. For the workflow YAML, write valid GitHub Actions syntax with the exact job/step names and triggers specified, including an `actions/checkout@v3` step, a `make -C c-string-hasher` step, and a `python3 process.py "test_string"` step.
