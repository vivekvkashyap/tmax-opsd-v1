Fix the tooling first, then build the pipeline, then verify against the schema shape you produce. (1) Inspect `/app/shyaml-0.6.2/shyaml` and `setup.py` for the deliberate breakage — look for an unbalanced paren/quote or similar syntax error introduced by a "bad commit"; fix it minimally and confirm with `python3 -c "import shyaml"` or a quick `pip install -e /app/shyaml-0.6.2/` before relying on it downstream. (2) Read `/app/repo_config.ini` to build a mapping from old archive filenames to new ones. (3) For each `.tar.gz`: extract to a temp dir, read `metadata.txt` (decode as ISO-8859-1, e.g. `iconv -f ISO-8859-1 -t UTF-8` or Python's `open(..., encoding='latin-1')`), replace `__STATUS__` with `CURATED` (note: watch for other `__X__` macros mentioned generally — only replace what's specified), write it back with UTF-8 encoding, repack the tar.gz preserving the other files untouched, and rename per the config mapping. (4) Use shyaml (or a Python YAML/JSON parser once shyaml works) to extract metadata key-values into `/home/user/manifest.json` as a JSON array of {artifact_name, ...parsed metadata}. Test your JSON's structure and key names carefully since scoring compares (name, key, value) tuples exactly — mismatched key naming is the likely accuracy killer.
