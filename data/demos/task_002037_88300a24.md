Work in order since later steps depend on earlier ones. First compile both C files with `gcc -shared -fPIC -o /home/user/libs/libmathops.so.X.Y.Z file.c -lm` (the `.1.0.0`/`.2.1.0` suffixes are just filenames, not sonames — ctypes can load them via full path with `ctypes.CDLL(path)`). Set `calculate_trajectory.argtypes = [c_double, c_double]` and `.restype = c_double` before calling. For version selection, parse the manifest's constraint string (e.g. `>=2.0.0`) yourself with simple tuple comparison of `(major,minor,patch)` parsed from the `libmathops.so.X.Y.Z` filenames in `/home/user/libs/` — don't hardcode the winner, derive it from the actual files present and the actual constraint text. In the FastAPI app, load the shared library at module scope (or lazily) using a path built from the computed version, not a literal string, so mocking `ctypes.CDLL(...).calculate_trajectory` in tests is possible — patch the specific function/attribute your app calls, not the whole ctypes module. Start uvicorn as a background process (nohup/subprocess) bound to port 8080 before writing the deployment summary, and verify the port is actually listening before declaring done.
