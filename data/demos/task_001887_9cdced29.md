First inspect `config.ini` to learn which log files map to which character encodings — do not assume UTF-8 or guess encodings; read the config programmatically (e.g. Python's `configparser`) so the script generalizes to any station listed. Extract `raw_logs.tar.gz` and open each log file in binary mode, decoding with the exact encoding named in the config (Python's `bytes.decode(encoding)` or `codecs.open`), since decoding with the wrong codec silently produces mojibake instead of erroring. For parsing, don't rely on naive line-splitting — use a state machine or regex that scans for `===RECORD START===` ... `===RECORD END===` blocks and tolerates/skips garbage or blank lines between records, since the multi-line NOTES field and stray junk between records will break simple per-line parsers. Filter to only records with `STATUS: VALID`, extracting `station`, `timestamp`, `status`, `notes` fields with a per-field regex or line-prefix strip. Write each surviving record as one JSON line (ensuring `json.dumps(..., ensure_ascii=False)` or default UTF-8 output) to `normalized_logs.jsonl`, then zip it with `zipfile.ZipFile` using `arcname='normalized_logs.jsonl'` so no directory prefix is embedded.
