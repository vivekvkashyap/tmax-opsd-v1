Handle the two subtasks separately and verify each in isolation before combining. For Task 1, first run `/home/user/setup/legacy_config.sh` manually (or `cat` it) to see the exact order and wording of its `read -p` prompts, then automate it non-interactively — the simplest robust approach is piping the answers in order via `printf 'Etc/UTC\nen_US.UTF-8\ny\n' | /home/user/setup/legacy_config.sh` from within `/home/user/automate.sh` (or use `expect`/`pexpect` if the script buffers oddly); confirm afterward that the expected config file was actually written with the right timezone/locale values, don't just trust that the script exited 0. For Task 2, write `/home/user/check_storage.sh` to run `du -sk /home/user/secure_storage` and parse just the numeric KB field (first whitespace-separated column), compare it against 5000 with an integer comparison (`-ge`/`-lt` in bash, not string comparison), and append exactly `ALERT: secure_storage is <SIZE>KB` or `OK: secure_storage is <SIZE>KB` to `/home/user/storage_audit.log`, substituting the real integer for `<SIZE>` with no extra formatting. Make both scripts executable with `chmod +x` and run each once so their output files exist for grading.
