Split this into two independent workstreams and verify each in isolation before combining. For the service stack: read `/app/doc_stack/start.sh` to see how nginx, Flask, and Redis are launched, then trace the actual failure point rather than guessing — check nginx's `location /api/render` block for a missing or wrong `proxy_pass` target (it must point at the Flask port), and check `/app/doc_stack/.env` for a `REDIS_HOST` pointing at an unreachable remote name instead of `127.0.0.1`. After editing, restart the stack and confirm end-to-end with the exact curl command from the task before moving on. For the sanitizer script: build the nested-archive extraction (tar -> gunzip -> unzip) into a fresh temp directory per invocation, read every extracted `.md` file, and reject (temp files deleted, exit 1) if ANY file contains any of the three exact trigger strings — check this before doing any transformation. Only if all files are clean should you run the `[CONFIDENTIAL]` -> `[REDACTED]` substitution and print concatenated output, exit 0. Test against both `/home/user/corpora/clean/` and `/home/user/corpora/evil/` directories exhaustively, since the grader requires 100% correct classification on both — a single false negative or false positive fails the task.
