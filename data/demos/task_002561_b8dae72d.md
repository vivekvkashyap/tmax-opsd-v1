Build correctness first, then optimize for speed. (1) Get MongoDB running locally (Docker), load the CSV rows into `financial.transactions`. (2) Write the aggregation pipeline per spec: `$match` on `status == "completed"` and `amount >= 100`, then `$group` by `{src, dst}` summing `amount`. (3) Build the graph in memory and run Dijkstra's (not Bellman-Ford) per query with a proper priority queue (`container/heap`) — that's the main source of speedup over a naive reference; cap hops so paths beyond 6 edges are excluded, tracking hop count in the queue state. (4) Match the required JSON schema, field order/values, and tie-break rule (weight ascending, then src, then dst) exactly, then truncate to the first 50 — subtle sort/filtering mismatches fail exact-equality checks even with a correct algorithm. (5) Once correctness is solid, optimize: batch MongoDB reads, avoid redundant graph rebuilds per query, use efficient data structures — measure your own runtime with `time` before assuming you've hit the required speedup. Run as a non-root user throughout.