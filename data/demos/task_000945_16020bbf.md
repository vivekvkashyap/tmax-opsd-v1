Diagnose the port mismatch by reading the source rather than guessing: grep `collector.cpp` for the port it uses to talk to the logger (look at the `sendto`/connect call and the hardcoded port number in `send_to_logger`), then compare that to the `PORT=` value inside `/app/logger.sh` — they won't match, that's the "silently rejecting" bug; fix `logger.sh`'s port to equal the one the collector actually connects to, not the collector's listen port (8888) which is for inbound TCP clients. Compile the collector with `g++ /app/collector.cpp -o /home/user/bin/collector` after `mkdir -p /home/user/bin`. Start `logger.sh` in the background first (`bash /app/logger.sh &`), then launch the collector inside a user namespace as instructed: `unshare -U -r /home/user/bin/collector &` — verify with `ss -tlnp`/`ss -ulnp` that both ports are actually listening before moving on, since unshare can silently change effective networking/permissions. Test end-to-end by sending `CAPACITY_REPORT 500\n` via `nc 127.0.0.1 8888` or similar and confirming you get `OK_RECORDED\n` back and that the logger's file/socket actually received the forwarded UDP packet. Finally write the logrotate config for `/home/user/logs/capacity.log` with `daily` and `rotate 3` directives at `/home/user/config/logrotate.conf`.