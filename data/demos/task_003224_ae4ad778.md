Rebuild the evaluation pipeline in the order the task specifies rather than patching the leaky script piecemeal. First eliminate leakage by wrapping `StandardScaler` and `LogisticRegression(random_state=42)` in a single `sklearn.pipeline.make_pipeline`/`Pipeline`, then pass that pipeline (not pre-scaled data) into `cross_val_score` so scaling is refit per-fold. Second, loop `i` from 0 to 49, construct `StratifiedKFold(n_splits=5, shuffle=True, random_state=i)` fresh each iteration, run 5-fold CV with the pipeline, and accumulate all 250 scores into one array/list — do not reuse a single CV split or reseed incorrectly. Third, compute the plain mean of those 250 scores, then call `numpy.random.seed(42)` immediately before a loop that draws 1000 bootstrap resamples (each of size 250, sampled with replacement via `numpy.random.choice`) from the 250 scores, taking the mean of each resample; use `numpy.percentile` on the 1000 bootstrap means at 2.5 and 97.5 for the CI bounds — order of operations (seeding placement, sample size, percentile method) matters for exact reproducibility. Finally, `os.makedirs('/home/user/artifacts', exist_ok=True)` and write a JSON with exactly the keys `mean_accuracy`, `ci_lower`, `ci_upper`, then execute the script and confirm the file was produced.
