Start by inspecting the Makefile in `/app/libgraphbackup-1.2.0` to find the actual misconfiguration rather than guessing — check the compiler variable (a C++ library built with a C compiler front-end like `gcc` instead of `g++` will fail to link libstdc++/iostream symbols) and check the link flags for a missing `-shared` needed to produce a `.so`. Fix only what's broken, then run `make` and `make install` and confirm the `.so` and header actually land in `build/lib` and `build/include`. Write `filter_backup.cpp` to include the installed `graphbackup.h`, call `extract_queries(filepath)`, and for each returned query do a case-insensitive substring search for `DELETE`, `DROP`, `REMOVE`, and `CALL dbms.` (uppercase the query or use a case-insensitive find) — exit 1 as soon as any query matches any pattern, otherwise exit 0 after checking all queries. Compile with the correct include/lib paths and rpath (or set `LD_LIBRARY_PATH`) so the executable can find the shared library at runtime, then smoke-test against a handful of files from both `/app/corpora/clean/` and `/app/corpora/evil/` before assuming full corpus coverage.
