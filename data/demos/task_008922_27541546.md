The reproducibility requirement is the crux: your bootstrap resampling must use C++'s `<random>` in exactly the way specified, since any deviation (different RNG, different call order, different seeding) produces different numbers than the grader's reference implementation. First parse the CSV (skip header) into three double vectors, compute predicted_sales and squared error per row using the fixed formula, and get the plain mean for the original MSE. Then construct `std::mt19937 gen(42)` and `std::uniform_int_distribution<int> dist(0, N-1)` exactly once, before the 1000-iteration loop, and for each iteration draw N indices by calling `dist(gen)` in a straightforward inner loop over `j` from 0 to N-1 (drawing order matters for reproducibility) — sum the corresponding squared errors and divide by N to get that iteration's MSE. Collect all 1000 MSEs, `std::sort` ascending, take indices 25 and 975 (0-indexed) as the CI bounds, and average all 1000 for the bootstrap mean. Format all four output numbers with `std::fixed << std::setprecision(4)` writing to `/home/user/results.txt` in exactly the four-line label format shown. Compile with `g++ -O3 analyze.cpp -o analyze` and run it — optimization flags don't change RNG output, so don't worry about `-O3` affecting reproducibility.
