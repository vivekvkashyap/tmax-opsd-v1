Work top-down: apply the patch first (`patch -p0 < feature.patch` or `-p1`, checking which strip level matches the diff's path style, from inside the right directory) so the `compute_factors_adv` function exists behind `#ifdef ENABLE_ADV_MATH` before you touch the build. Then fix the Makefile in three distinct ways: add `-fPIC` to the shared-library compile flags (missing PIC is why `.so` build fails or the executable can't link), add `-lmathcore -L.` (or the correct path) plus an rpath linker flag (`-Wl,-rpath,'$ORIGIN'` or the absolute service path) to the `math_server` link line so it finds `libmathcore.so` at runtime without `LD_LIBRARY_PATH`, and add `-DENABLE_ADV_MATH` to the compile flags used for building objects that reference the new function. Add the negative-input check directly after `atoi()` parses `n` in `server.c`: if `n < 0`, `send()` the exact literal string `"ERR_NEGATIVE\n"` and `close()`/`continue` without calling either compute_factors variant. After `make` succeeds, start `math_server` in the background, use a simple TCP client (e.g. `nc` or a small script) to send `-42\n` then `15\n` on separate connections, and append the raw responses to `test_results.log` in order, verifying with `readelf -d math_server | grep -i rpath` that the rpath entry is actually present.