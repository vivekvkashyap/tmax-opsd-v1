Do not guess the spec — run `tesseract /app/schema_spec.png stdout` (or equivalent OCR) first and read its output carefully before writing any logic; the exact column list, the gap-fill rule, and the suspicious-description rules all live only in that image, so build nothing that hardcodes assumptions about them ahead of time. For the streaming pipeline, process `/app/transactions.csv` with a line-by-line reader (Python `csv.reader` over an open file handle, or `awk`) rather than `pandas.read_csv` on the whole file, keep only the OCR'd column subset, and apply the gap-filling rule you read from the image sequentially as you stream (carry forward the last-seen timestamp per the image's rule), emitting one JSON object per line to `/app/processed.jsonl`. For the filter, implement the suspicious-description rules exactly as OCR'd (likely simple substring/keyword and digit-run checks) as an executable script (`#!/usr/bin/env python3` or bash with a shebang, `chmod +x`) that reads the file path argument, inspects its single-line content, and exits 0 (clean) or 1 (suspicious) — test it against a few descriptions you construct yourself to sanity-check both directions before relying on the hidden corpus.