Use SQLite's recursive CTE support directly rather than doing the recursion in Python — write a `WITH RECURSIVE` query that starts from `parent_id = 1` with an initial multiplier of 1, and in the recursive step joins `bom` to the running result, propagating `quantity_so_far * bom.quantity` as the new multiplier for each `child_id`; this correctly handles multiple paths to the same component because CTE recursion naturally produces one row per path, which you then aggregate. After the recursive query returns all (component_id, contributed_quantity) path rows, `GROUP BY component_id` and `SUM()` the contributed quantities to combine multiple paths into one total per component — this is the step naive solutions skip, causing undercounted totals for shared sub-components. Join back to `components` for the name, exclude id=1, order by component_id ascending, and write the result list as JSON matching the exact key names and int types specified. Validate manually: trace one component's total by hand from the BOM data to sanity-check your CTE before trusting the full output.
