Use `awk` for the whole pipeline but structure it in two passes to avoid the precision trap. First pass: count out-degree per caller by extracting distinct (caller,callee) pairs (e.g. `awk -F, 'NR>1{print $1,$2}' file | sort -u | awk '{print $1}' | sort | uniq -c | sort -rn`) to find the caller with the most distinct callees — verify there are no duplicate caller-callee pairs inflating the count, since out-degree means *distinct* callees. Once you have the bottleneck caller ID, filter just its rows and compute population variance of `exec_time_ns` numerically stably: since values look like `9000000000.500`, squaring them in double precision loses the fractional part to rounding (catastrophic cancellation in the naive $E[X^2]-E[X]^2$ formula) — instead compute the mean first, then sum squared deviations from the mean, then divide by N (population, not N-1). Do this either in a single `awk` pass storing values in an array (two-pass: first compute mean, then loop again for sum of squared deviations) or pipe through `awk` twice. Format the final variance with `printf "%.4f"` and write the single line `caller_id,out_degree,variance` to `/home/user/bottleneck_analysis.txt`, matching the exact comma-separated format with no extra whitespace or trailing newline issues.