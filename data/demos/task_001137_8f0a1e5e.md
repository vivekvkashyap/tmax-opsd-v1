First recover the target mean/stddev from the image via OCR (`tesseract /app/target_distribution.png stdout`, preprocessing with ImageMagick if the text is unclear) rather than guessing values — read them directly off the printed text. Then process `/home/user/raw_sequences.fasta` with awk: for each record, concatenate sequence lines belonging to one header, compute the length, and keep only sequences whose length falls in `[mean-stddev, mean+stddev]` (a one-liner like `awk '/^>/{if(seq)print length(seq); seq=""} !/^>/{seq=seq$0} END{if(seq)print length(seq)}'` adapted to also filter and track headers works well — be careful that FASTA sequences can span multiple lines). Compute the empirical mean of the kept sequences' lengths with awk/bc, keeping to the count and sum. For the server, write a bash script using a `nc -l -p 8080` (or a loop wrapping `nc`) that reads the HTTP request line, and for `GET /stats` responds with a proper `HTTP/1.1 200 OK` status line, `Content-Type: application/json`, `Content-Length`, blank line, then the JSON body with the computed mean and count; run this in a loop in the background (`while true; do ...; done &`) so it keeps serving repeated requests.
