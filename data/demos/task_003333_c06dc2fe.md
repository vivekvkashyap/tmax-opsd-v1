Start by reading `/app/vendored/legacyhash/mixer.go` carefully — the inline comment says the intended operation is a 64-bit cyclic left rotate by 13 bits. A correct left-rotate-by-N on a 64-bit value is `(x << N) | (x >> (64-N))`; check whether the right-shift amount in the code matches `64-13=51` — if it uses a different shift amount, that's the bug to fix (Go's `math/bits.RotateLeft64(x, 13)` is a safe drop-in alternative if you want to avoid manual shift arithmetic). After fixing, set up `/home/user/go.mod` with a `replace legacyhash => /app/vendored/legacyhash` directive so `/home/user/hash_runner.go` can `import "legacyhash"`, call `legacyhash.Hash([]byte(input))`, and print the `uint64` result with `fmt.Printf("%016x\n", result)`. Compile with `go build -o /home/user/hash_runner /home/user/hash_runner.go`. Verify correctness yourself by running both `/opt/oracle/legacyhash_oracle "test"` and your compiled `/home/user/hash_runner "test"` on several different strings (including edge cases like empty string, long strings, unicode) and confirming the hex output matches exactly before finishing — the fuzzer will do this at scale.
