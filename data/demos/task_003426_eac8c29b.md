Build this as a small pipeline of scripts orchestrated by `run_etl.sh`, not one monolithic script. Extraction: use OpenCV (`cv2.VideoCapture`) to read every frame in order, convert to grayscale, and take the mean pixel value per frame, storing (frame_index, value) pairs — frame indices must start at 0 and be contiguous. Anomaly detection: flag any frame with value > 200 (flash) or < 10 (dropped) as invalid; do not clip or clamp these, discard them entirely as missing. Imputation: for each flagged index, find the nearest valid frame before and after it and linear-interpolate between their (index, value) pairs — this must work correctly for anomalies that occur back-to-back (e.g. two consecutive flash frames) and at signal edges, so implement it as "find the last valid point behind, the next valid point ahead, then lerp" rather than pairwise imputing before removing all anomalies. Round the final value to 2 decimals and write `frame,value` CSV rows (with header) to `/home/user/cleaned_timeseries.csv`. Since scoring is MSE against a smooth underlying signal, sanity-check your interpolated values visually (e.g. plot or eyeball print) around the anomaly indices before finalizing — a working `bash /home/user/run_etl.sh /app/sensor_feed.mp4` must actually produce the file.