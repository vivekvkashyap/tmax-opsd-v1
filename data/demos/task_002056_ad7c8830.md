Treat `/app/loc_encoder` as a black box first, then confirm with static analysis. Run it on a handful of crafted inputs — empty string, pure ASCII of known length, a string whose UTF-8 byte length lands exactly on a 16-byte boundary, one just past a boundary, and multi-byte inputs (accented Latin, Cyrillic, CJK, emoji) — and inspect the raw output with `xxd`/`od -A x -t x1z`. Look for: a fixed-size header before the string data (try to spot where the payload bytes begin and whether a length field precedes them, and in which byte order), how the total output length relates to the payload length (check whether it's always rounded up to some power-of-two boundary, and what byte value fills the gap), and whether composed vs. decomposed Unicode inputs (e.g. an accented character entered as a single codepoint vs. base+combining-mark) produce identical output — this reveals whether normalization happens before encoding. Cross-check byte-length assumptions against Python's `len(s.encode('utf-8'))`, not `len(s)`, since multi-byte characters differ between the two. Use `objdump -d`/`strings` on the binary only to corroborate hypotheses (e.g. confirm a modulo/alignment constant), not as a first resort. Implement your Python script to read `sys.argv[1]`, apply the normalization you inferred, build the header+payload+padding accordingly, and write raw bytes via `sys.stdout.buffer.write`, then validate by diffing your script's output against the binary's output for many varied test strings before trusting it.
