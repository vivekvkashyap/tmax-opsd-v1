Build the pandas pipeline in the exact order the spec implies, since each step's correctness depends on the previous: (1) `drop_duplicates()` on the raw dataframe first; (2) parse timestamps by trying ISO format then falling back to the US format per-row (e.g. `pd.to_datetime(col, errors='coerce', format=...)` combined across two passes, or a custom apply that tries both `strptime` patterns), then localize/convert to UTC-aware; (3) sort by timestamp; (4) group by `[device_id, sensor_type]` and use `.interpolate(method='linear')` on `value` within each group, then `.fillna(0.0)` to catch any leading/trailing NaNs interpolation can't fill (interpolate alone leaves edge NaNs — this is the detail naive solutions miss); (5) merge with metadata via `device_id`, using an inner join (or merge then dropna) so ghost devices absent from metadata.json are dropped; (6) floor timestamps to the hour (`dt.floor('h')`) and group by `[window_start, site, sensor_type]`, computing mean(value) rounded to 2 decimals and counting rows where the imputed value strictly exceeds that row's own `alert_threshold`. Sort the final frame by window_start, site, sensor_type, then write JSONL with ISO `Z`-suffixed timestamps to `/home/user/output/summary.jsonl`, creating the output directory first.
