Design `process_gcode.py` as a polling loop (simplest and most robust here, since watchdog can miss atomic-rename events depending on platform/filesystem): every ~0.5s, list `/home/user/incoming_gcode`, and for each file found, first check it's not still being written (the simulator does an atomic `shutil.move` from `/tmp`, so once it appears in the directory listing it's complete — but guard anyway, e.g. by trying to open+stat twice with a short gap and confirming size is stable) before processing. For each stable file: compute SHA256 by reading it in binary chunks through `hashlib.sha256()`, scan its lines for one starting exactly with `; filament used [mm] = ` and parse the trailing float, then append a single-line `json.dumps({...})` object with keys in the exact order/names specified to `gcode_manifest.jsonl` (open in append mode, one write per file, flush immediately so a later kill doesn't lose buffered lines), then `shutil.move` the file into `/home/user/archived_gcode/`. Log each step to stdout/stderr so `processing.log` shows activity. Launch it with `nohup python3 process_gcode.py >/home/user/processing.log 2>&1 & disown`, then run the simulator, wait for it to finish plus a short buffer for your poll interval to catch the last file, then send SIGTERM (or `kill`) to the background PID before checking outputs.