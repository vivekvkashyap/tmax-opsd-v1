Do not disassemble the binary deeply first — treat this as differential/structural analysis: compare byte layouts across `/app/corpus/clean/` and `/app/corpus/evil/` (`xxd`/`hexdump -C` on several of each) to spot the field that differs systematically, then check a size relationship against a declared length field rather than a content signature. Once you find a header with a magic value and a length field, verify empirically: for every evil file does the declared length imply a payload larger than what's present after the header, and does every clean file fit? Only trust the rule once it separates 100% of both corpora — never hardcode filenames. Implement `/home/user/validate_payload` to read the magic/length via a fixed-offset struct unpack (mind endianness) and compare against actual file size, exiting 0 or 1 accordingly. Separately, create the spool dirs with `mkdir -p` and `chmod 733` exactly on `incoming/`, and write `supervisor.sh` to loop over incoming files, validate each, then either run the binary (redirecting stdout) and delete the source, or move invalid files to quarantine untouched.
