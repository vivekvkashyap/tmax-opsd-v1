Do the backup first, before any modification: `tar -czf /home/user/services_backup.tar.gz -C /home/user services` (or equivalent) so it captures the pre-fix state. For fix_network.sh, don't naively overwrite whole files — use targeted in-place substitution (e.g. `sed -i` with a pattern that matches the `LISTEN_PORT=` and `UPSTREAM_URL=` key lines specifically, or add the line if missing) so any unrelated lines (TIMEOUT, LOG_LEVEL, THEME, comments) are preserved untouched. Design each edit to be idempotent: if you use sed substitution keyed on `^LISTEN_PORT=.*` -> `LISTEN_PORT=9001`, running it twice yields the same result, but if you instead append lines blindly each run will duplicate them — test by running the script twice and diffing the file before/after the second run. The db service has no UPSTREAM_URL — don't add one; the report script's "none" case corresponds only to db. Have generate_report.sh read the actual (now-corrected) service.conf files rather than hardcoding the values, so the report genuinely reflects file state, and verify the final network_report.log matches the required four lines exactly, in order.