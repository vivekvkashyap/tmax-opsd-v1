Separate the three sub-problems and build/test each independently before wiring them together. (1) OCR: run `tesseract /app/backup_policy.png stdout` (or similar OCR) to recover the literal `POLICY_ID: <PREFIX_TEXT>` string — do not guess the prefix, parse it out of the OCR output at runtime or read it once and hardcode the recovered value only after confirming it. (2) Zip-slip defense: for every ZipInfo/member in the uploaded archive, resolve its destination path with something like `os.path.realpath(os.path.join(vault_dir, member.filename))` and verify the result still starts with `os.path.realpath(vault_dir) + os.sep` before extracting; reject/skip anything that resolves outside, including via `../` sequences or absolute paths — don't just string-match "..". (3) Dedup via hard links: as you extract each renamed file, compute a content hash (e.g. sha256) and keep a dict from hash to first-seen path; for later files with the same hash, delete the just-written duplicate and call `os.link(first_path, dup_path)` instead of writing new bytes. Wire these into a minimal Flask/stdlib HTTP server bound to 127.0.0.1:9000 handling `POST /upload` with a multipart field named `archive`, and manually POST a crafted test zip (including a `../` entry and a duplicate-content file) with curl to confirm all three behaviors before considering it done.