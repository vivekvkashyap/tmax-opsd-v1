Build this as a bash script that delegates the relational logic to `sqlite3` or a `python3`/`pandas` one-liner, since window functions make ranking much easier than hand-rolled `awk`. Load both CSVs into a SQLite in-memory (or temp file) database, join `sales` to `products` on `product_id`, group by `product_id` to sum `quantity*price` as `total_revenue`, then use `RANK() OVER (PARTITION BY category ORDER BY total_revenue DESC)` to get standard competition ranking (ties share a rank, next rank skips accordingly) — this is exactly what SQL `RANK()` gives you for free, don't reimplement it manually unless you must. Filter to the requested `category` argument, `ORDER BY rank ASC, product_id ASC` for deterministic tie-breaking, then slice rows `(page-1)*2` through `page*2` for pagination. Emit the final two args (`category`, `page`) verbatim into the JSON envelope along with the sliced `results` array, formatting with `jq` or Python's `json.dumps` to guarantee valid, well-typed JSON (numbers not quoted). Make the script executable and test it against multiple category/page combinations, including a page beyond available results, before finishing.
