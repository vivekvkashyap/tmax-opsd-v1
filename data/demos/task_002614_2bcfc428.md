Model this as cycle detection over a wait-for graph built from the `locks` table: for each WAITING (tx_id, resource_id) row, find the tx_id that holds that same resource_id with status GRANTED — that GRANTED tx_id is the node the WAITING transaction is "waiting for". Build this tx-to-tx edge list (in SQL via a self-join of `locks` on `resource_id` where one side is WAITING and the other GRANTED, or in Python), then detect all transactions on any cycle — a recursive CTE that follows wait-for edges and flags any transaction reachable back to itself works well, or equivalently do DFS with a visited/recursion-stack per node in Python. Once you have the set of deadlocked tx_ids, join back to `transactions`, order by `start_time` ascending, and assign rank 1..N in that order (a SQL window function `ROW_NUMBER() OVER (ORDER BY start_time)` restricted to the deadlocked set works cleanly, or compute it in pandas). Write the three columns `tx_id,rank,query` to `/home/user/deadlocks.csv` with a plain comma-separated writer (Python's `csv` module), excluding every transaction not part of a cycle. Run the script so the CSV actually gets produced.