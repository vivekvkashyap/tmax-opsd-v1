Split this into two independent workstreams and don't let the Rust build issues distract from the reverse-engineering piece. For the Rust project, run `cargo build` (or `cargo check`) in `/home/user/workspace/migration-service`, read each borrow-checker error carefully (likely returning references to locals or missing/incorrect lifetime annotations on struct fields holding parsed AST data), and fix by adjusting lifetimes or ownership (e.g. returning owned data, or adding proper `<'a>` lifetime params) without touching the public function signatures. For the legacy resolver, black-box it first: pipe small hand-built graphs (a few nodes, a few edges) into `/app/legacy_graph_resolver` via stdin using the documented N-nodes/N-lines/E-edges/E-lines format, and vary which nodes have in-degree 0 simultaneously to detect the tie-breaking rule (test alphabetically-ordered vs insertion-ordered candidate sets) — this is exactly the kind of detail static disassembly alone easily gets wrong, so confirm it experimentally. Implement Kahn's algorithm with a min-heap over node names for ties in Rust, expose it as `agent_resolver` (stdin/stdout CLI) and via the `axum` `/api/v1/resolve` POST endpoint, then fuzz-test your implementation against the oracle on many random DAGs before considering it done.
