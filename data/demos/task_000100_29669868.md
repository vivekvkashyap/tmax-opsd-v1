Work this as three independent, verifiable stages so you can debug each before wiring them together. First, transcribe `/app/instruction.wav` — install and run a speech-to-text tool (e.g. `pip install openai-whisper` and run it on the file, or use any available offline ASR CLI) and read the transcript text yourself to identify the arithmetic operation it names (e.g. "add N" / "subtract N" / "multiply by N"); do not guess the operation, derive it from the actual transcription output. Second, parse `/app/math_data.csv`: for each Unicode character in an expression, use Python's `unicodedata.numeric(ch)` (or `unicodedata.decimal`/`digit` as fallback) to convert full-width digits, fractions, circled numbers, and Roman numerals into their numeric values, then evaluate the resulting simple binary expression (respect the operator between the two parsed numbers) — don't assume ASCII digits, `unicodedata.numeric` handles all these Unicode number forms directly. Apply the transcribed operation to each evaluated result. Third, log each pipeline stage (transcript text, record count, parse errors) to `/app/pipeline.log` in `YYYY-MM-DD HH:MM:SS - LEVEL - Message` format, then serve results via FastAPI/uvicorn (or `http.server`) on `0.0.0.0:8000` with `GET /result/{id}` returning `{"id": int, "result": float}` and 404 for unknown ids; keep the server process running (background/foreground) for testing.
