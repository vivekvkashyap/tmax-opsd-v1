Run `tesseract /app/system_params.png stdout` (or output to a text file then parse) to recover the ALPHA and BETA coefficients from the image text — don't hardcode guessed values; parse them out of the OCR'd `COEFFICIENTS: ALPHA=<val> BETA=<val>` line with grep/sed/awk. For `integrate.sh`, implement the trapezoidal rule directly in `awk` (loop `i=0..N`, step `h=X/N`, accumulate `h*(f(0)/2 + f(N*h)/2 + sum of f(i*h) for i=1..N-1)`) using the OCR'd ALPHA/BETA in `f(x)=ALPHA*x^2+BETA*x`, printing only the final float. For `density.sh`, generate 5000 uniform values in [0,100) (e.g. `awk 'BEGIN{srand(); for(i=0;i<5000;i++) print rand()*100}'`), bin them into the 10 fixed-width bins with awk counters, and print each line as `[bin_start]-[bin_end]: <count>` exactly matching the required format (e.g. `0-10: 512`). For `server.sh`, use a `while true; do ... | nc -l 127.0.0.1 8080; done` loop (or `nc -lk` if available, or `socat TCP-LISTEN:8080,fork,reuseaddr,bind=127.0.0.1 EXEC:handler.sh`) that reads one line, parses whether it's `INTEGRATE <X>` or `DENSITY`, dispatches to the matching script, and responds — test it manually with `echo "INTEGRATE 5.5" | nc 127.0.0.1 8080` before backgrounding it with `nohup ... &` and confirming it's still listening.