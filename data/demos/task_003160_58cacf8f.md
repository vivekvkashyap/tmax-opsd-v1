Start by diffing the clean vs evil corpora directly (`grep`, `jq`, or manual inspection) rather than guessing — look specifically for JSON-illegal-but-Python-json.loads-legal tokens like `NaN`, `Infinity`, `-Infinity`, and for unpaired UTF-16 surrogate escapes (e.g. `\ud800` with no matching low surrogate) that break strict UTF-8 encoding. Build `/home/user/detector.py` to reject a file (exit 1) if `json.loads` accepts a bare `NaN`/`Infinity`/`-Infinity` constant (Python's default parser allows these unless you override `parse_constant` to raise), or if re-encoding the parsed strings to UTF-8 raises `UnicodeEncodeError` due to lone surrogates — otherwise exit 0. Test this script against both local corpora directories first and confirm 100% separation before wiring it in. For the pipeline patch, edit only `/home/user/services/ingestion.py`: run your detector's validation logic (import the function, don't shell out) on the incoming JSON body before proxying, returning HTTP 400 immediately on failure and otherwise forwarding unchanged to `http://localhost:5001/process`; leave `backend.py` untouched. Validate end-to-end by starting all services via `start_services.sh` and curling `localhost:8080/ingest` with both known-clean and known-evil payloads, confirming the backend process stays alive throughout.