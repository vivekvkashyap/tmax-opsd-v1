Load the JSON list and iterate requests one at a time, checking both rules independently per request so a single request can emit two alert lines. For CWE-22, do a substring check on `path` for `../` OR the URL-encoded form `%2E%2E%2F` (match case-insensitively, since encodings can appear in mixed case). For CWE-269, pull the `Cookie` header, locate the `session=` segment (it may be followed by other cookie pairs separated by `;`, so isolate just the session value), base64-decode it, then `json.loads` the result and check whether `role` equals `"admin"` or `"administrator"`. Wrap the decode/parse step in a try/except so malformed base64 or invalid JSON is silently skipped rather than crashing the script — several records are designed to be garbage on purpose. When a request triggers both checks, write the CWE-22 line before the CWE-269 line, preserving the request's original order in the file. Append lines in the exact `IP - CWE_ID` format via a script you write and execute yourself, then confirm `/home/user/alerts.log` was created before finishing.
