Start with the SQL: use a window function like `ROW_NUMBER() OVER (PARTITION BY tx_id, resource_id ORDER BY event_time DESC)` to pick each `(tx_id, resource_id)` pair's most recent action, filter to `rn = 1`, and exclude pairs whose latest action is `RELEASE` — this correctly handles resources that were acquired-then-released or waited-then-acquired, which naive `MAX(event_time)` grouping can get wrong when ties or multiple state changes exist. Load that result into Python and build a directed graph (e.g. with `networkx.DiGraph`) where an edge goes from a transaction currently `WAIT`-ing on a resource to the transaction currently `HOLD`-ing that same resource — build a lookup of resource->holder first, then connect each waiter to that holder. Find deadlocks as simple cycles with `nx.simple_cycles()`, then deduplicate cycles that are rotations of each other (represent each cycle canonically, e.g. by its sorted tuple of members, since a simple cycle of the same node set should only be reported once). Sort the tx_ids within each cycle alphabetically, and sort the outer list of cycles by each cycle's first (smallest) element, then write the exact JSON structure to `/home/user/deadlocks.json`.
