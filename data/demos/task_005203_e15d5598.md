Fix the vendored crate first, since nothing else builds until it does: run `cargo build` inside `/app/outlier_detect` to see the actual compiler errors, then correct them precisely — check `Cargo.toml` for a misspelled table header (compare against a valid manifest's `[dependencies]` section) and check `src/lib.rs` for a plain syntax mistake like a missing semicolon between statements; fix only what's broken, don't rewrite the algorithm. Then scaffold the web service with `cargo new /home/user/data_service`, add it as a path dependency on the local `outlier_detect` crate plus an async HTTP framework (e.g. `axum`+`tokio`) and `serde`/`serde_json`, and implement `POST /process` to: reject requests missing the exact `Authorization: Bearer secret_token_999` header with 401; parse the JSON array, compute the mean of only the non-null `val`s, and impute nulls with that mean; pass the fully-populated vector into `outlier_detect::filter_outliers`; then bootstrap the cleaned values (1,000 resamples with replacement, same size as input, mean of each resample) and take the 5th/95th percentiles of those 1,000 means for the CI. Return `{"cleaned_count": ..., "mean_ci": [lower, upper]}` as JSON with status 200. Run the compiled server and smoke-test it yourself with `curl` before considering it done, checking auth rejection and a valid payload's shape.