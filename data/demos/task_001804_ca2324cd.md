Work in the stated order: fix the vendored dependency first, since everything downstream imports it. Inspect `/app/dictdiffer-0.9.0/dictdiffer/__init__.py` — a deliberately injected line (near the top) raises an ImportError unconditionally; remove that injected line only, leaving the real package logic untouched, then `pip install .` from that directory and confirm `import dictdiffer; list(dictdiffer.diff({"a":1},{"a":2}))` works in a fresh Python shell. Next build `/home/user/filter.py`: parse the JSON, and classify as evil if `pre_install`/`post_install` contain `/bin/sh` or `bash` as substrings, or `file_path` doesn't start with `/etc/app_config/`, or any `env` key starts with `LD_` — check every rule (any one violation is evil), and iterate against the given clean/evil corpora until every file classifies correctly, since partial rule coverage is the most common miss. Finally write `/home/user/pipeline.sh`: for each incoming JSON, invoke filter.py and branch on its exit code, logging `[WARN]` for evil; for clean ones, diff against baseline.json with the fixed dictdiffer, but first deduplicate by `server_id` keeping only the highest `timestamp` entry (do this dedup pass before diffing/logging, not per-file) before emitting `[INFO] Processed <server_id> with <N> changes`.
