Use `ffmpeg -i /app/microarray_signal.mp4 frame_%04d.png` to dump every frame, then for each frame use `convert frame.png -resize 2x2! -colorspace Gray txt:-` (or `-scale 2x2!` plus `-depth 8`) to force-downsample it to a 2x2 grid and dump raw grayscale intensities per pixel — the `!` forces exact dimensions rather than preserving aspect ratio, which matters since the true resolution is much higher. Parse the ImageMagick `txt:` output with `awk`/`grep`/`sed` to pull one intensity value per (x,y) coordinate per frame, appending each domain's series to its own file or array indexed by frame number (X starts at 1). For the regression, compute slope with the standard least-squares formula in awk: `m = (n*Σxy - Σx*Σy) / (n*Σx² - (Σx)²)`, done separately per domain, then round to 2 decimals with `printf "%.2f"`. For the server, write a loop using `socat TCP-LISTEN:9090,reuseaddr,fork EXEC:handler.sh` (or a `nc -l` loop) that parses the request line for the path and the `X-Bio-Auth` header, returns 401 if the header is missing/wrong, otherwise looks up the precomputed slope for that x,y and returns it as the plain-text body with a proper `HTTP/1.1 200 OK` status line and headers.
