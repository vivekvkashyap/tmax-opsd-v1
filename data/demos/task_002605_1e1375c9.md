First run `tesseract /app/alert_screenshot.png stdout` (or similar) to OCR the alert image and read off the exact HTTP header name it names as the exfiltration vector — use that literal string in your script rather than guessing a header name. Build `/home/user/traffic_sanitizer.py` to: load the input JSON, check if `headers` contains that exact key, and if present, try to base64-decode its value (wrap the decode in a try/except so malformed base64 doesn't crash the script — malformed content must be treated as benign/unchanged). If decoding succeeds, check whether the first 4 decoded bytes equal the ELF magic `b"\x7fELF"`; only then replace the header's value with the literal string `[REDACTED_ELF]`. In every other case (header absent, decode failure, valid base64 but no ELF magic), the output JSON must be byte-for-byte/structurally identical to the input — use `json.dump` with matching key order and don't touch unrelated fields. Test against `/app/traffic_dumps/` first, but remember the hidden grading corpora will include tricky benign cases (random noise, non-ELF binary data, PNG bytes) designed to catch overly aggressive redaction logic.