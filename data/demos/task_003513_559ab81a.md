Load `/app/signal.wav` and estimate the power spectral density (e.g. `scipy.signal.welch` or an FFT-based periodogram) over the full signal, then find the 3 highest peaks in the spectrum and round their frequencies to the nearest Hz — use enough frequency resolution (long enough window/nfft) to resolve closely-spaced tones distinctly, and sort the three ascending. For the graph optimization, don't try to solve for the Laplacian analytically — set up a numerical optimization (e.g. `scipy.optimize.minimize`) over the off-diagonal edge weights of a 4-node complete graph (6 free non-positive parameters), construct the resulting Laplacian by setting diagonal entries so each row sums to zero, compute its eigenvalues (`numpy.linalg.eigh`, guaranteed real since symmetric), and minimize squared error between the 3 non-zero eigenvalues (sorted) and the target frequencies divided by 100. Enforce off-diagonal ≤ 0 via bounds or a parameterization like negative squared/exp of free variables. Serve the result with a simple HTTP server (e.g. Python's `http.server` subclassed, or `flask`) on `127.0.0.1:8000` at `GET /model`, running it as a background process/thread so it stays alive for the verifier, returning frequencies and the 4x4 laplacian as JSON per the given schema.