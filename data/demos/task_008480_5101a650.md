Build server-side logic first, then test it independently before wiring the orchestration script. In `server.py`, add validation as an early guard in the `/process` handler: check `request.is_json` and that both `batch_id` and `records` keys exist, returning `400` immediately if not. For rate limiting, keep an in-memory dict keyed by client IP storing recent request timestamps; on each request prune entries older than 10 seconds, then reject with `429` if the remaining count is already >= 3, else append and proceed — a fixed-window counter that never expires old entries is the common bug, so verify pruning actually works. Test manually with `curl` (4 rapid POSTs) before writing the Node test. In the Node script, use `fetch`/`http` to hit `http://127.0.0.1:8080/process` for each case in order, writing results to `test_report.json`. In `run_pipeline.sh`, start the server backgrounded, poll until it responds (don't just `sleep`), run the Node test, kill the PID, and exit 0.
