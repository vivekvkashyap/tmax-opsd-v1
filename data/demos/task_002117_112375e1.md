Use pandas: load both CSVs, coerce `temperature`/`humidity` to numeric with `pd.to_numeric(..., errors="coerce")` so non-numeric/missing become NaN, then filter with the inclusive bounds (`temperature.between(-20.0, 50.0)` and `humidity.between(0.0, 100.0)`) and drop rows with any NaN across the four required columns — a naive filter that skips the NaN check will silently let the missing-value row through. Parse `timestamp`/`hour` with `pd.to_datetime(..., utc=True)`, floor the sensor timestamp to the hour with `.dt.floor("h")` so it aligns with the maintenance file's already-hour-aligned values, then `groupby(["hour","sensor_id"])[["temperature","humidity"]].mean().round(2)`. Merge with `pd.merge(agg, maintenance, on=["hour","sensor_id"], how="left")` and fill missing status with `.fillna("unknown")`. Before writing, sort with `.sort_values(["hour","sensor_id"])` and format the `hour` column back to the exact ISO8601-with-Z string form (e.g. `dt.strftime("%Y-%m-%dT%H:%M:%SZ")`) since `to_csv` would otherwise emit a different datetime string format than the original files. Finally write with the exact header `hour,sensor_id,avg_temperature,avg_humidity,status` and no index.