Install/verify `tesseract-ocr` plus `pytesseract`/`Pillow`, then OCR `/app/legacy_model_spec.png` (e.g. `pytesseract.image_to_string(Image.open(path))`) to recover the equation text — do not assume its coefficients or signs, read them from the OCR'd string. Write a small parser (regex over the recovered text, e.g. matching `<coef> \* feature_A`, `<sign> <coef> \* feature_B`, etc., plus a trailing constant) that turns the extracted equation into a reusable function of `feature_A/B/C`, being careful to preserve the exact sign of each term as printed (a common bug is dropping or flipping the minus sign on a subtracted term). Load `/app/data/batch_001.csv` with pandas, apply the parsed formula row-wise (vectorized arithmetic on the columns is fine and faster than `.apply`), and write `id,score` sorted ascending by `id` to `/home/user/scored_pipeline_output.csv`. Since grading is MSE against a ground-truth formula, sanity-check your OCR read by re-running tesseract with different `--psm` settings or manual inspection if the parsed numbers look implausible, since a single misread digit will fail the tight MSE threshold.