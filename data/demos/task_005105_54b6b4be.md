Handle the three parts in order; later parts depend on earlier fixes. First, diagnose vendored `dateparser` at `/app/vendor/dateparser`: run `python3 -c "import dateparser"` to see the real traceback — look for a typo'd import/module name in the package's config code, fix that one line, `pip install -e /app/vendor/dateparser`, and confirm `dateparser.parse('1 day ago')` works. Second, write `/home/user/setup_proxy.py` idempotently: check whether nginx is already running with the target config (pidfile or `pgrep -f` plus a diff against the desired conf) before regenerating anything; when it does act, render an nginx config listening on port 8080 with `location /` reverse-proxying to `proxy_pass http://unix:/tmp/backend_app.sock;`, write it to `/home/user/nginx/nginx.conf`, and start nginx with that config explicitly (use a writable prefix dir since it's unprivileged). Third, in `/home/user/log_converter.py`, parse `sys.argv[1]` with the fixed `dateparser`, convert to UTC, and print in a consistent format — since exact output format matters for the fuzz check, test several timestamp/locale combinations to settle on one stable format, and print `INVALID_DATE` with exit code 1 on parse failure.
