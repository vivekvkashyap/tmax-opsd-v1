Handle the three sub-problems independently before running the full pipeline. First, read the target SLA off the dashboard image with an OCR tool such as `tesseract /app/alert_dashboard.png stdout` (or open/inspect the PNG if OCR is unavailable) — this tells you the expected precision level you're aiming for, it doesn't hand you the final answer. Second, fix the build: inspect `CMakeLists.txt` in `/home/user/uptime_monitor` for a `libfmt` version conflict (likely two different `find_package`/`target_link_libraries` calls pulling in incompatible fmt versions, or a `FetchContent`/system mismatch) and consolidate it to link a single consistent version, then confirm `cmake . && make` succeeds cleanly. Third, look at `src/uptime_calculator.cpp` for a summation loop accumulating many small durations into a `float` — this is the precision-loss culprit; change the accumulator to `double`, or better, implement Kahan (compensated) summation so millions of small increments don't lose precision from repeated rounding. After both fixes, rebuild, run `./uptime_monitor data/ping_logs.csv`, and redirect its printed percentage (just the number) into `/home/user/calculated_sla.txt`. Sanity-check the value is stable/reproducible across repeated runs before finishing.