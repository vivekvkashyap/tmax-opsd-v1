Work through the diagnosis steps in order rather than jumping to build.sh. First run `strings /home/user/project/core.dump | grep FATAL_ERROR` to extract the corrupted file's absolute path from the dump. Second, clean that file by stripping non-printable bytes while keeping printable ASCII, e.g. `tr -cd '[:print:]\n' < path > /tmp/clean && mv /tmp/clean path` — do this in place on the exact path found, and inspect the result to confirm it looks like sane text (no stray control characters left). Third, open `calculate_threshold.sh` and find the `awk` sum that prints raw floating-point (`print sum`), which suffers from binary floating-point representation error; fix it by formatting the output explicitly with `printf "%.1f", sum` (or equivalent rounding) inside the awk program instead of relying on `print`'s default formatting. Finally run `bash build.sh > build_success.log` (or `./build.sh > ...` if executable) from the project directory and confirm the log contains the expected success message — re-run and check exit codes/log contents rather than assuming success.