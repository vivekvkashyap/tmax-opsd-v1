Work in three independent stages, and get each one verifiably correct before combining. (1) Salt extraction: use `ffmpeg -i /app/salt_video.mp4 -vf fps=1 frame_%d.png` to dump the 4 frames, then run `tesseract frame_N.png stdout -psm 10` (single-character page segmentation mode works best for one big glyph) on each frame, strip whitespace, and concatenate in frame order to build the 4-character Salt string — print/log what you extract so you can sanity-check it looks like a plausible alnum string before trusting it. (2) Patch parsing: for each `.patch` file, grep only for lines beginning with `+ROUTE` (unified diff added-lines format), and split each into the URL/params portion and the `CHECKSUM=<hex>` suffix using a regex — don't assume fixed whitespace. (3) Validation: for each route, recompute `hashlib.md5((url_and_params + salt).encode()).hexdigest()` and compare case-insensitively to the checksum; separately, case-insensitively search the URL/params string for the literal substring `%90%90%90`. A file is "evil" if any added route fails either check; a file with zero added routes is "clean". Write results as a flat JSON dict keyed by basename per the CLI's `--out` argument.
