Design the C pipeline around the fact that a CSV record can legitimately span multiple physical lines when a quoted field contains a newline — parse character-by-character tracking whether you're inside double quotes, and only treat a `\n` as a record separator when you're outside quotes; if a record (while still tracking quote state) is found to contain an embedded newline inside the quoted message, drop that whole record silently and don't emit anything for it. In `clean_extract.c`, for surviving rows extract the hour bucket by truncating the timestamp string to the `YYYY-MM-DD HH` prefix, compute message length as the character count between the message's opening and closing quotes (excluding the quotes themselves), and detect `"ERROR"` via a simple substring scan; emit one line per row to stdout in a simple delimited intermediate format. In `aggregate.c`, read from stdin (data will arrive pre-sorted by hour via `sort` in run.sh), group consecutive identical hour keys, accumulate counts and length sums, and print `avg_msg_len` with `printf("%.2f", ...)`. Write the Makefile to build both with `gcc -O2`, and have run.sh pipe `clean_extract < raw_logs.csv | sort | aggregate > aggregated_stats.csv`, writing the required header first. Test incrementally: verify clean_extract's output on the raw file alone before chaining into aggregate.
