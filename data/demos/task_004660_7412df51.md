Model the org chart as an adjacency list (map from manager_id to list of child emp_ids) built by parsing employees.csv, treating an empty string or the literal "NULL" as no manager. Load each employee's own sale_amount from sales.csv into a map (default 0 if an employee has no sales row). Compute each employee's subtree total with a post-order recursion/DFS: `total(node) = own_sales(node) + sum(total(child) for child in children(node))`, memoizing results to avoid recomputation, and being careful to actually recurse down before summing rather than only looking at direct reports (the requirement is direct + indirect reports). Write `/home/user/total_sales.csv` sorted by ascending numeric emp_id with header `emp_id,total_sales`. Parse `argv` for a `--target-id <ID>` flag; when present, after computing all totals, write just that employee's aggregated total (as a plain integer, no trailing content beyond what's expected) to `/home/user/query_result.txt`. Compile with `g++ -std=c++17 -O2 /home/user/aggregate.cpp -o /home/user/aggregate`, run once with no args to produce total_sales.csv, then run again as `./aggregate --target-id 2` to produce query_result.txt — verify both outputs by hand-tracing the small hierarchy before finishing.