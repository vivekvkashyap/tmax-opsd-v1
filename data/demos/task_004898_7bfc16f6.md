Build `/home/user/analyze.py` as a pipeline of independently-verifiable steps. First OCR `/app/dashboard.png` with `pytesseract` and locate the substring following "KEY: " in the recognized text to recover the AES key — pad/encode it to the required 32 bytes as needed for AES-256. Read `/app/payload.enc`, split off the first 16 bytes as the IV and treat the rest as ciphertext, decrypt with `cryptography`'s AES/CBC using the recovered key, and strip PKCS7 padding (the last byte's value tells you how many padding bytes to remove) to recover the raw ELF bytes. For redaction, work on the raw bytes directly: regex-match ASCII IP-address substrings that specifically fall in the `10.42.0.0/16` subnet (leave every other IP or string untouched), and replace each match in place with the same number of `*` bytes so total file length and all other offsets stay identical — this is critical since ELF structure must remain byte-exact. Write the result to `/home/user/sanitized_payload.elf`, then confirm it still starts with the ELF magic bytes and that only target-subnet IPs were altered, nothing else.
