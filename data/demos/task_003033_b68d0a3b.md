Fix the data-leak by restructuring the pipeline around a group-first split: read the CSV, filter rows into Control and Treatment DataFrames *before* any vectorizer touches them. For each group independently: instantiate a fresh `TfidfVectorizer()` with default params, call `fit_transform` only on that group's `text` column, then use the *same fitted* vectorizer's `.transform()` on both the group's texts and the single-element list containing the target query string — never call `fit`/`fit_transform` on combined data or on the query. Compute `cosine_similarity(doc_vectors, query_vector)` per group and flatten to get per-document similarity scores. Feed the two independent similarity arrays into `scipy.stats.ttest_ind(control_sims, treatment_sims, equal_var=False)` for Welch's t-test. Round each of the four output values to 4 decimal places with Python's `round()` before writing the JSON — do this at serialization time, not by truncating floats mid-computation, and use `float()` to avoid numpy dtype issues when dumping to JSON. Write the result to the exact path and key names specified, and verify by re-reading the JSON file to confirm valid syntax and matching keys.
