Read `libbackend.cpp` first and pinpoint exactly why it leaks: `new char[256]` is allocated per call and returned across the ctypes FFI boundary, but Python's `ctypes.c_char_p` never frees C-heap memory — so any fix must avoid returning newly-heap-allocated memory each call. Since you cannot touch `server.py`, use a fix that keeps the same function signature/ABI: replace the heap allocation with a `static` (or thread-local) fixed-size buffer that's reused across calls, so nothing new is ever leaked. Recompile with `g++ -fPIC -shared -o /home/user/app/libbackend.so libbackend.cpp`. Set up `socat TCP-LISTEN:8000,fork,reuseaddr TCP:localhost:9000 &` as a background reverse proxy, confirm it's listening (`ss -ltnp` or similar) before moving on. Launch the server under valgrind exactly as specified, backgrounded, redirecting stdout/stderr to the given log paths. Issue exactly 5 `curl` GETs to `localhost:8000/` through the proxy to exercise the leak path, then send `kill -TERM` to the Python process (not the proxy) so it exits gracefully and valgrind has a chance to flush its leak summary — abrupt kill -9 will prevent the summary from being written. Finally grep the valgrind log for `definitely lost: 0 bytes` to confirm your fix worked before finishing.
