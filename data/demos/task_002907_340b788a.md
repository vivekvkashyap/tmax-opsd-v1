Build the archiver in two passes: first compute compressed payloads for every file into a temp directory (gzip -9 for .log/.txt using `gzip -9 -c`; for .wav, transcode with `ffmpeg -i in.wav -ac 1 -b:a 32k out.mp3` and use the resulting mp3 bytes as the payload), recording each payload's byte size with `stat -c%s` or `wc -c`. Only after all payload sizes are known can you write the manifest header (since the manifest must precede the binary blob and needs exact sizes), so write MANIFEST, then each `<basename> <size>` line, then the `---DATA---` separator, then concatenate the raw compressed bytes for each file in the same order — use `cat` on the binary payload files directly onto the output (avoid piping through anything that could alter bytes, e.g. don't use echo or printf on binary data). Double check there's no extra trailing newline or padding between binary chunks. Iterate on the ffmpeg bitrate/channel flags and test actual output size against the 250000-byte threshold; if too large, confirm mono+32k settings were actually applied (check with ffprobe) rather than assuming.