First inspect the schema of `/home/user/access_graph.db` (`sqlite3 /home/user/access_graph.db .schema`) to confirm how `entities` and `relations` link (likely `relations(source_id, target_id)` referencing `entities(id)`), rather than assuming column names. In your Python script, create an index on `relations` (e.g. on `source_id`, or a composite `(source_id, target_id)`) via `CREATE INDEX IF NOT EXISTS ...` before running the query, since graph traversal joins benefit from it and the task explicitly checks for it. Write a recursive CTE that starts from the given username's entity id, recursively follows `relations` edges (via `UNION` to include the seed node and traverse indirect paths through intermediate Group entities), then filters the resulting reachable set to `type = 'System'`, dedupes, sorts alphabetically by name, and limits to 5. Pass the username as a bound parameter (`?`) into the query — never string-format it into SQL — to satisfy the injection-safety requirement. Write exactly the resulting names, one per line, to `/home/user/report.txt`, and run the script as `python3 /home/user/audit_query.py Alice` to produce the final output.