Use `std::filesystem` to iterate `/home/user/artifacts/raw_data/` and filter entries by extension (`path.extension() == ".txt"`) so non-txt files like any `.csv` are correctly excluded. For each matching file, open it with `std::ifstream`, and count tokens the simplest reliable way: stream extraction into a `std::string` via `while (file >> word) count++;`, which already splits on any whitespace (spaces, tabs, newlines) the same way the expected counts assume — avoid writing custom whitespace-splitting logic that might mis-handle multiple consecutive spaces or trailing newlines. Accumulate a running total token count and a running file count across all matched files. After processing, write the output file by hand-constructing the exact JSON string `{"total_tokens": <total>, "num_files": <count>}` (a plain `ofstream` write is fine here since the schema is fixed and simple — no library needed). Compile with `g++ -std=c++17 -o tokenizer_check tokenizer_check.cpp` and run the resulting binary so `/home/user/artifacts/token_summary.json` is actually produced; verify by cating the JSON file afterward rather than assuming the program worked.