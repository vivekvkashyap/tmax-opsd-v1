Build this as a real filesystem-watching daemon, not a polling script that races the test harness. Use Python's `watchdog` library (or a manual polling loop with a short interval as a fallback) to detect file-creation events in `/home/user/spool`, and crucially wait until each file is fully written before processing it (e.g. check the file size is stable across two checks, or watch for a rename/close event) to avoid reading a partially-written file. For `.gcode` files, read line by line, find the `; BEGIN CONFIG` / `; END CONFIG` markers, and within that block parse lines shaped `; KEY: VALUE` to pull out `MACHINE_TYPE` specifically — don't assume it's always the same line number. For `.elf` files, parse the ELF header's `e_machine` field directly with Python's `struct` module (offset 18, 2-byte little-endian value in a 64-byte ELF64 header) or use `pyelftools`, then map the numeric machine code to its symbolic name (`EM_ARM`, `EM_X86_64`, etc.) via the standard ELF constant table rather than hardcoding one value. After processing, archive with Python's `tarfile` module using `arcname=` set to just the basename so the tarball doesn't embed the absolute path, append a WAL line in the exact specified format with a real current timestamp, then delete the original from spool. Launch the script with `&`/`nohup` in the background, then create `ready.flag`, and keep the process alive — don't let the shell session exit or the daemon dies with it.
