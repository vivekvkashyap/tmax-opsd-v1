Parse the WAV file's header manually or with a small Go WAV-decoding routine to extract raw 16-bit PCM samples (assume standard `RIFF`/`WAVE`/`fmt `/`data` chunk layout, mono, 44100Hz as stated) — don't assume a library is available; reading the byte layout directly with `encoding/binary` is reliable. Slice the sample stream into consecutive, non-overlapping chunks of exactly 4410 samples, and for each chunk run an FFT (write a simple radix-2 FFT or DFT in Go, or use a small pure-Go FFT implementation) to get the magnitude spectrum; find the bin with the largest magnitude and convert its bin index to a frequency using `bin_index * sampleRate / windowSize`. Map the detected peak frequency to a base character only if it falls within ±50 Hz of one of the four defined bands, otherwise skip that window (don't force a nearest-match). Concatenate the resulting characters after the `>Extracted_Sequence` FASTA header line. For serving: run an `http.HandleFunc("/sequence", ...)` server checking the exact `Authorization: Bearer bio-perf-token` header before writing the FASTA body, and separately spin up a `net.Listen("tcp", ...)` accept-loop that reads until `PING\n` and writes back the FASTA string plus a trailing newline — launch both listeners as goroutines from one `main()` and keep the process alive (e.g., `select {}}` or `WaitGroup`) with the program left running in the background.
