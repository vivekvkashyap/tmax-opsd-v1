Treat this as recover-then-join, in that order. First, transcribe `/app/voip_audit.wav` (install `openai-whisper` and `ffmpeg`, run whisper on the file) and read the transcript carefully to extract the employee ID matching the `E-XXXX` pattern and the project name mentioned — don't assume a value, pull it from what whisper actually outputs (allow for minor transcription noise, but the ID format and a proper-noun project name should be recognizable). Second, inspect `/app/corp_audit.db`'s schema, then in Python find the compromised employee's `manager_id` and query all employees sharing that same `manager_id` (including the employee themself) to build the peer group — this is a single-level sibling lookup, not a multi-level recursive traversal. Third, join `access_logs` for every peer-group member, group by `project_name`, sum `access_count` per project, exclude the compromised project, and multiply each project's summed access_count by its `sensitivity` from the `projects` table to get that project's risk score. Finally write `/home/user/audit_report.json` with `compromised_employee`, `compromised_project`, and a `risk_scores` map of project→score, keeping the JSON keys/structure exactly as specified since the report is scored on both the extracted identifiers and score accuracy.