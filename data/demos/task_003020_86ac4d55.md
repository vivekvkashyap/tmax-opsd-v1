Work through this as three layered fixes, verifying each before moving on. (1) In `src/math.rs`, implement `power` with iterative binary exponentiation: initialize `res` as the identity matrix, then while `n > 0`, if `n` is odd multiply `res` by `base` (via your `multiply` function), always square `base` into itself, and right-shift `n` by 1 each iteration — this naturally returns identity for `n=0`. Fix any type mismatches/missing imports in `main.rs` so `cargo build` succeeds, then confirm with a few `cargo run` + local `curl -X POST http://127.0.0.1:8080/api/matrix_pow` calls before touching nginx. (2) Edit `nginx.conf`: keep `listen 9000;` and add a `location /api/ { proxy_pass http://127.0.0.1:8080/api/; }` block inside the server block — the trailing slash on both sides matters for path passthrough. (3) Launch the Rust binary in the background (e.g. `cargo run &` or run the built release binary with `nohup ... &`), start `nginx -c /home/user/app/nginx.conf`, confirm both ports are listening (`ss -ltnp` or a curl round-trip through 9000), then write `SERVICES_READY` to `/home/user/app/startup.log`. Keep both processes backgrounded/detached so they survive after your session ends.
