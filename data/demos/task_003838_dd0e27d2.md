Tackle the three components independently, then wire them together last. Write config_filter.go first and validate it purely against the local corpora before touching networking: parse the JSON `services` array, sum `allocated_ram_mb` and compare `<= 100000.0`, and compute the *population* standard deviation of `cpu_shares` (divide the sum of squared deviations by N, not N-1) comparing `< 10.0` — run it against every file in `/app/corpus/clean` and `/app/corpus/evil` in a loop, printing mismatches, and iterate until you hit 100% on both before moving on, since the hidden grading corpus tests the exact same invariants. Write server.go to listen on 8080, read the POST body for `/ingest`, write it to a temp file (`os.CreateTemp`), invoke the compiled filter binary via `exec.Command` and check its `ExitCode()`, then either `redis-cli` (via the `go-redis` client, `INCR valid_configs_received`) and 200, or 400 on rejection. Build/compile config_filter into a binary the server can exec rather than shelling out to `go run` each time (faster and more reliable). Last, configure nginx's `/home/user/nginx/nginx.conf` with a `location /ingest { proxy_pass http://127.0.0.1:8080; }` block and reload it, then confirm the full path end-to-end with curl through nginx's listening port, checking the Redis counter increments only on 200s.
