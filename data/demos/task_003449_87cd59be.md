Work incrementally rather than writing the whole pipeline blind. First set up the Go module: `cd /home/user && go mod init docparser`, then add a `replace github.com/russross/blackfriday/v2 => /app/blackfriday` directive so `go run docparser.go` resolves the local vendored copy. Before trusting the parser, write a throwaway test that parses a `# Heading` and prints it — this reveals the first character of every H1 is dropped. Locate the bug in `/app/blackfriday` (likely `block.go`'s ATX heading function) by finding the slice offset used to extract heading text, and fix the off-by-one so full text is captured; re-run your test to confirm. Only then build the full pipeline: walk `/home/user/docs` recursively, filter by size (>1KB), parse each with the fixed library to get the first H1's text, build the filename/heading JSON array, gzip it, and write it as `metadata.json.gz` inside a tar archive at the required output path. Check the tar entry name and gzip framing carefully since a wrapping mismatch silently breaks comparison.
