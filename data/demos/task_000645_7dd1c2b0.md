Do delta debugging on the log by bisecting: split `sensors.log` in half repeatedly, run `sensor_sync` (or a quick harness) against each half to see which half still crashes, and narrow down to the single offending line — or simply `grep` for a suspicious marker pattern in the log once you notice the crash correlates with a specific payload string, then confirm that exact single line reproduces the segfault alone before saving it to `/home/user/minimal_crash.log`. In `worker.c`, address the three bugs separately: (1) wrap increments to the shared `corrupted_packets` counter in a `pthread_mutex_t` lock/unlock (or use `__sync_fetch_and_add`/atomics) since multiple threads write it concurrently; (2) in `parse_timestamp`, zero-initialize the `struct tm` (e.g. `memset(&tm, 0, sizeof(tm))`) before `strptime`/`mktime`, and set `tm.tm_isdst = -1` so `mktime` doesn't read garbage stack fields for unset members, which is what causes `-1`/garbage epoch values; (3) find where the crashing payload triggers a bad pointer dereference or out-of-bounds access and add a guard/bounds-check so it's skipped rather than crashing. Rebuild with `make`, rerun on the full log, and redirect stdout to `/home/user/success.out`, confirming it exits cleanly and prints the expected summary.