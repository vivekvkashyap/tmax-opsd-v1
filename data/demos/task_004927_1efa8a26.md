First get the vendored package importable: open the source files under `/app/vendored/astro_core/astro_core/` and look for an obvious typo (e.g. a misspelled `import` keyword) causing a `SyntaxError` on load — fix that one token, then `pip install -e /app/vendored/astro_core` and confirm `from astro_core.calc import compute_derivative` works before writing classify.py. In `classify.py`, load `time`/`flux` from the HDF5 file with `h5py`, then for Condition A compute `compute_derivative(time, flux)` and check `max(abs(d)) < 50.0`. For Condition B, set up an `emcee.EnsembleSampler` with 32 walkers, a log-posterior combining uniform priors over [-100,100] for a,b,c and a Gaussian log-likelihood with fixed sigma=1.0 comparing `a*t**2+b*t+c` to flux; initialize walkers by jittering a random guess within the prior bounds, run `sampler.run_mcmc(p0, 500)`, then take `sampler.get_chain(discard=100, flat=True)` and compute the median of the `a` column, checking it's > 0. Exit 0 only if both conditions hold, else exit 1. Set a fixed random seed for the walker initialization and MCMC to keep results deterministic across runs, and test your script against files you place in a clean/evil test directory before trusting it on the real corpus.