Break this into independently testable pieces before combining them into one script. First, interact with /home/user/tools/fetch_backup using pexpect: spawn the process, use `child.expect("Operator PIN: ")` (matching exactly the prompt text, no trailing newline assumptions), then `child.sendline("8374")`, and wait for completion — verify /home/user/backup.tar appears afterward before moving on. Second, extract the tarball into /home/user/mnt_restore (create the dir first, use Python's tarfile module). Third, read the existing fstab file, append the exact specified line (matching whitespace/formatting precisely) without disturbing existing lines — open in append mode or read+rewrite preserving original content. Fourth, parse mnt_restore/metadata.json for the username field, then check membership by parsing /home/user/mock_root/etc/passwd (split each line on ':' and compare the first field) rather than substring-searching the raw file, and write the exact required log format. Fifth, implement the TCP proxy with Python's socket/threading (or asyncio): accept connections on 127.0.0.1:9999, inspect `client_address[0]` from accept(), only bidirectionally relay bytes to a new socket connected to 127.0.0.1:8080 if that IP is exactly '127.0.0.1', otherwise close immediately; run the listener loop in a daemon thread so the main script can finish setup and stay alive without blocking forever.
