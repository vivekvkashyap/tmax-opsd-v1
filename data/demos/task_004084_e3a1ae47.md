Use the SQLite C API's `.import`-equivalent programmatically: open an in-memory db (`sqlite3_open(":memory:", &db)`), create `accounts` and `transactions` tables with matching columns, then parse the two CSVs line-by-line in C++ (split on commas, skip the header row) and insert rows via prepared statements. Build the filtered join with a single SQL query joining transactions to accounts twice (once for source, once for target), filtering `amount > 500.0` and both accounts' `status = 'ACTIVE'` — this avoids reimplementing the join logic manually in C++. Iterate the result set to build an adjacency structure (e.g. `map<int,int> out_degree`) incrementing out_degree[source] for every row that survives the filter, and also insert both source and target into the node set even if a node ends up with out_degree 0. Sort candidate accounts by (out_degree descending, account_id ascending) and take the top 3, writing exactly 3 lines `account_id,out_degree` with no header to /home/user/top_accounts.csv. Compile with `g++ analyze.cpp -o analyze -lsqlite3` (install `libsqlite3-dev` first if the header/lib is missing) and run it to actually produce the output file — don't just leave a compiled binary unexecuted.