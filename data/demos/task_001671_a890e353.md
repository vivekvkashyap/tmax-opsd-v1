Diagnose the SQL bug precisely: a comma-separated `FROM users u, orders o, products p` with only a `users-orders` join condition and no `orders.product_id = products.id` condition is an implicit cross join against every product row, which inflates every sum — rewrite it as explicit `JOIN`s: `users JOIN orders ON u.id = o.user_id JOIN products ON o.product_id = p.id`, keeping the `WHERE u.status='active' AND p.category='electronics'` filters, then `GROUP BY` user name and `SUM(amount)`. Verify the corrected totals make sense by hand-checking one user's orders against the raw table data before moving on. Add indexes with `CREATE INDEX idx_users_status ON users(status);` and `CREATE INDEX idx_products_category ON products(category);` (confirm via `PRAGMA index_list`). Run the fixed script to produce `results.csv`, then separately run `EXPLAIN QUERY PLAN <your fixed query>;` via the sqlite3 CLI or cursor and save its raw output to `plan.txt` — don't hand-write a plan, capture the actual planner output.