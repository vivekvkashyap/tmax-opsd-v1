Create the venv with `python3 -m venv /home/user/mlops_env` and install pandas via `/home/user/mlops_env/bin/pip install pandas` (don't rely on the system Python). In the script, iterate the subdirectories of `/home/user/experiments`, and for each run: load `config.json` and pull `learning_rate`; load `metrics.csv`, find the row with the maximum `epoch` value (don't assume it's the last row — sort or use max explicitly) and take its `accuracy` as final_accuracy; use `os.path.getsize` on `model.pt` for model_size_bytes. Build a DataFrame with columns in the exact order `run_id, learning_rate, final_accuracy, model_size_bytes`, sort by `final_accuracy` descending and break ties with `run_id` ascending (pandas `sort_values(by=[...], ascending=[False, True])`), then write to CSV with `index=False` so no stray index column appears. Run the script with the venv's python interpreter to actually produce `/home/user/experiment_summary.csv` before finishing, and spot-check the CSV header and row order match the sorting rule.