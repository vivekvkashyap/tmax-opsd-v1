Fix the vendored library before touching the main program, since `manifest_filter.cpp` likely depends on it. Inspect `/app/vendored/csv_parser/include/csv.h` for missing standard-library includes (e.g. `std::vector` used without `#include <vector>`) and add what's needed; separately inspect the `Makefile` with `cat -A` around the failing build line to find where a literal tab is required before the compiler invocation but a run of spaces was substituted instead — fix the indentation and confirm `make -C /app/vendored/csv_parser` exits 0 and produces `libcsvparser.a`. For `manifest_filter.cpp`, implement a simple CSV row parser (split on commas, careful with exactly 3 fields per the spec) that filters rows where `file_type == "log"` and `size_bytes > 10485760` using strict integer comparison, then serialize the matching filenames as a JSON array (proper `"` escaping if needed, comma-separated, in encounter order) to stdout. Since the verifier fuzzes with edge cases — missing trailing newline, empty input, huge sizes, values exactly at the 10485760 boundary — write your own quick test harness feeding varied inputs (empty stdin, no trailing newline, boundary sizes) and inspect the output before considering the program done; boundary and empty-input handling are the most common places naive implementations diverge from a careful reference.
