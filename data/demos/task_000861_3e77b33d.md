Build `filter.py` purely from the physics spec, then validate against the two labeled corpora before trusting it — don't try to reverse-engineer the binary's internals. Steps: read the CSV's `time`/`x` columns; compute velocity via second-order central differences for interior points (`v[i] = (x[i+1]-x[i-1])/(t[i+1]-t[i-1])`) and one-sided (forward/backward) differences at the two endpoints — get this right since a naive first-difference everywhere biases energy estimates and will misclassify borderline files. Compute `E(t) = 0.5*m*v**2 + 0.5*k*x**2` with m=1.0, k=5.0 elementwise. Classify evil if `max(E) > 50.0` OR `max(abs(diff(E))) > 10.0`, else clean; exit(1) for evil, exit(0) for clean — get the exit codes and inequality direction exactly right (strict `>`, not `>=`, per spec). Iterate: run your script over every file in `/app/data/clean/` and `/app/data/evil/`, check the exit codes match expectations for all 200, and tighten CSV parsing (headers, non-uniform time spacing) if any misclassify.
