First recover the seed, do not assume it. Run `tesseract /app/build_ticket.png stdout` and parse the line matching `AUTH_SEED:\s*(\S+)` to extract `<seed_value>`; store it in the app. Ensure `nasm` and `tesseract-ocr` are installed (`sudo apt-get install -y nasm tesseract-ocr` if missing). Build the server in `/home/user/gateway.py` bound to `127.0.0.1:9090` with a `POST /build` route (Flask or `http.server`). Per request: read JSON, take `source`, write it to a unique temp path via `tempfile.NamedTemporaryFile`/`mkstemp` (unique per request to avoid races), and run `nasm -f elf64 <src.asm> -o <out.o>` via `subprocess`, checking the return code. On nonzero return, respond HTTP 400 with `{"error": "compilation failed"}`. On success, read the `.o` file's raw bytes and compute `hashlib.sha256(binary_bytes + seed.encode("utf-8")).hexdigest()` — concatenate the object bytes directly with the UTF-8 seed, in that order, with no separator. Respond HTTP 200 `{"hash": "<digest>"}`. Clean up temp files and launch the server in the background so it keeps listening.
