First inspect the SQLite schema (`sqlite3 /home/user/backups.db .schema`) to see how `target_dbs` and `backup_jobs` relate (a foreign key from jobs to databases, with `cluster_name` on the database side and `status`/`size_bytes`/`run_time` on the job side). In `/home/user/analyze.go`, open the DB with `database/sql` + the sqlite3 driver, and query using a placeholder `?` bound to `os.Args[1]` (never string-concatenate the cluster name into SQL) joined against `backup_jobs` filtered to `status = 'SUCCESS'`. Use a window-function query with `LAG(size_bytes) OVER (PARTITION BY db_id ORDER BY run_time)` computed over successful backups only, then pick, per database, the row with the maximum `run_time` — that row's `size_bytes` minus its `LAG` value is the size diff (treat `NULL` from `LAG` as `0` when no prior successful backup exists). Marshal the results into the exact JSON schema requested, sort by `database_name` alphabetically before serializing, and write to `/home/user/report.json`. Run `go run /home/user/analyze.go "eu-central-1"` to produce the file, and double check only databases in that cluster appear and failed backups never contribute to the "latest" or "previous" calculation.