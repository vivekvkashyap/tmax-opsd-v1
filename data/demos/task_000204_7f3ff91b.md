Write `/home/user/upgrade_pipeline.sh` as a sequence of automated transformation steps, each independently verifiable, rather than trying to do everything in one pass. Copy the tree first (`cp -r`). For the proto file, use `sed` to change the `syntax = "proto2";` line to `syntax = "proto3";` and strip every standalone `required ` field-label keyword (proto3 has no field labels for singular fields). Install `grpcio`/`grpcio-tools` via `pip3`, then invoke `python3 -m grpc_tools.protoc` with the right `--python_out`/`--grpc_python_out`/`--proto_path` flags to regenerate bindings inside `/home/user/modern_app`. For `service.py`, apply targeted `sed` substitutions: swap the Python 2 module imports for their `http.server`/`urllib.parse` equivalents, and convert `print "..."` statements to `print("...")` calls (watch for statements with string concatenation, which need careful regex handling, not a blind wrap). Launch the upgraded service in the background, give it a moment to bind both ports, `curl` the `/process` endpoint, capture the exact response body into `/home/user/migration_result.log`, then kill the background process before exiting.
