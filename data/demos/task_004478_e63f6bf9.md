Work through this in the stated order and verify each stage before moving on. First fix the C: read `libevaluator.c` carefully per operation — a common seeded bug is that the multiplication branch actually performs addition instead of `a * b`; compare each op_code branch against its intended arithmetic rather than assuming only one is wrong. Fix the Makefile so it compiles with `-fPIC` and links with `-shared` (a static/non-PIC object won't load correctly as a `.so` via ctypes) — run `make` and confirm `libevaluator.so` exists and `nm`/`ctypes.CDLL` can load it. For the parser, map textual ops (ADD/SUB/MUL/DIV) to their integer op_codes exactly as the wrapper expects, parse each line of `expressions.txt`, call `evaluate_op`, and write one result per line to `results.txt` in input order. For the property tests, use `hypothesis.given(st.integers(min_value=-1000, max_value=1000), ...)` for each op, comparing `evaluate_op` output against native Python arithmetic — for DIV, special-case `b == 0` to expect `0` (matching the C behavior) rather than raising or asserting a Python `ZeroDivisionError`. Run `pytest` and confirm all tests actually pass before finishing.
