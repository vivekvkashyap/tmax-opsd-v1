Gather evidence before writing any code. Run `strings /app/dump/core.oracle` and look for `[CONFIG]`-tagged lines that reveal the numeric constants the binary uses — don't invent constants, extract them from the dump. Separately, inspect the git history of `/app/legacy-port` with `git log -p` (and check out old commits/blobs if needed) to find the commit describing the linear-calibration formula shape (`y = (x * SCALE) + OFFSET`), which tells you the operation order even though the values there are stripped — cross-reference with the constants pulled from the core dump. Confirm the algorithm end-to-end by fuzz-testing your own draft implementation against `/app/bin/sensor_oracle` directly on a handful of hex inputs (pipe hex lines into the oracle via stdin and compare outputs) before finalizing. The critical correctness trap is precision: implement the arithmetic using true 32-bit float semantics matching the oracle's internal rounding (e.g. C `float`, or Python's `struct.pack('f', ...)` / `numpy.float32` after each intermediate operation) rather than default 64-bit doubles, and make sure any intermediate multiplication result is rounded to float32 before the addition, since chained double-precision math will diverge in the low bits. Wire the final approach into `/home/user/solution.sh`, reading/writing hex strings line by line via stdin/stdout, and validate against the oracle on many random inputs, not just a few, before considering it done.