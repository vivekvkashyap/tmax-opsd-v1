First fix the environment issue: check `ls -l /app/legacy_filter` — if it lacks the execute bit, `chmod +x` it before anything else; also confirm it's not a shell-format mismatch (check with `file`). To reverse-engineer the filtering logic, run the binary against small hand-crafted input files varying one column at a time and diff the printed lines against the input to infer the predicate (look for word-level properties like symmetry/palindrome and minimum length — test short words, 1-char words, mixed-case words, and non-alphanumeric words to nail the exact boundary conditions). Then fuzz it with progressively longer single lines (e.g. lines of length 50, 100, 127, 128, 200+) to find where it crashes — this reveals a fixed-size stack/heap buffer overflow in the C code around line-reading. Implement `/home/user/robust_filter.py` in pure Python (split on whitespace, apply the inferred per-word predicate to the 1-based column, print full matching lines) so it naturally has no buffer-size limit. Make it executable with `chmod +x`. Test edge cases: column index out of range for a given line, empty lines, and very long lines, ensuring your script never crashes and matches the binary's output exactly wherever it doesn't segfault.