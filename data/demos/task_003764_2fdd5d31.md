Implement the RLE encoder as a straightforward byte-stream state machine in C++: read stdin with `std::cin` set to binary-safe mode (or use `read()`/`fread` directly to avoid any newline translation), and process one byte at a time. Maintain a run counter for consecutive `0x00` bytes; when you hit a non-zero, non-0x00 byte, first flush any pending zero-run (emitting `0xFF <count>` chunks, capping each chunk at 255 and continuing to emit further `0xFF 0xFF`/`0xFF <remainder>` pairs for longer runs) then write the byte as-is; when you hit `0xFF` itself, flush any pending zero run first, then emit the literal `0xFF 0x00` escape sequence for it. Don't forget to flush a trailing zero-run at EOF. For atomicity: open `<output>.tmp` for writing, write everything, explicitly close/flush it, then use `rename()` (or `std::filesystem::rename`) to atomically move it to the final path — only after all stdin has been fully consumed (EOF), never partway through. Compile with `g++ -O2`, then test by piping `/home/user/raw_data.bin` through your compiled binary and inspecting the output bytes with `xxd` to manually trace through your encoding logic against small known runs before trusting the full file.
