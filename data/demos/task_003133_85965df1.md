Work through each deliverable independently and verify it in isolation before moving on:
1. For `diagnose.py`, parse `/home/user/connection.log` line by line, tracking the most recent IP seen from a "Connecting to <ip>" line, and when a line matching `type 51` is encountered, print that tracked IP (only the first occurrence, or whichever matches the spec) — do not hardcode any IP.
2. For `backup.sh`, use `tar -czf /home/user/manifests_backup.tar.gz -C /home/user manifests` (or equivalent) so paths inside the archive are relative.
3. For `new_fstab`, write a single line following standard `/etc/fstab` column order: `LABEL=<label> <mountpoint> <fstype> <options> <dump> <pass>`, matching the label, mount point, fs type, and options given in the task exactly.
4. For `provision_disk.sh`, use `truncate -s 50M` (or `dd`) to create the image file, then `mkfs.ext4 -F` on it — no root needed for a plain file-backed image.
5. For `launch_qemu.sh`, construct a `qemu-system-x86_64` command with `-nographic`/`-display none` combined with `-vnc :2`, a `-drive file=...,if=virtio`, run it backgrounded (`&`), and capture `$!` into `/home/user/qemu.pid`.
Make every script executable with `chmod +x`.
