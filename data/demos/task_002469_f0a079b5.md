Sequence this as: restructure -> compile -> fix imports -> test, verifying each stage works before the next. Create `src/c_lib/`, `src/pylib/`, `tests/` and `git mv`/`mv` the files into place, deleting root-level leftovers. Compile with `gcc -shared -fPIC -o src/c_lib/libmath.so src/c_lib/math_ops.c`. The circular import exists because `interpreter.py` imports `DEBUG_MODE` from `test_config.py`, which itself imports `evaluate` from `interpreter.py` — break the cycle by removing the interpreter's dependency on test_config entirely (a production module shouldn't import from a test file); if `DEBUG_MODE` is genuinely needed, define it locally or read it as an optional attribute. In interpreter.py's ctypes load call, replace the hardcoded/relative `./libmath.so` path with something anchored to the script's own location, e.g. `os.path.join(os.path.dirname(os.path.abspath(__file__)), '../c_lib/libmath.so')`, so it works regardless of the caller's cwd. Add `/home/user/project/tests/test_integration.py` asserting `evaluate("SUB 10 4") == 6`. Install pytest, then run `pytest /home/user/project/tests > /home/user/project/test_results.log 2>&1` and confirm the log shows all tests passing before finishing.