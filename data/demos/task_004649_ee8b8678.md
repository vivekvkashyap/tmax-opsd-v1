First fix the vendored tool before building on top of it: run `/app/libsh-data-1.2.0/bin/sh-hash-row` to see it fail, inspect the script for a hardcoded library path (likely a `LIB_DIR=` assignment pointing somewhere that doesn't exist) and correct it to resolve relative to the script's own real location (e.g. `LIB_DIR="$(cd "$(dirname "$0")/../lib" && pwd)"`), then verify the fix with a standalone manual call before wiring it into your pipeline. For `/home/user/detect.sh`, implement the two anomaly checks as separate stages: (1) group rows by `sensor_id` (e.g. `sort` by sensor then timestamp), then use `awk` to walk consecutive readings per sensor and flag if `current_value <= previous_value * 0.5` (careful with the strict "drops by 50% or more" boundary condition); (2) for each row, normalize `sensor_id,value` and pipe it through the fixed `sh-hash-row` utility, collect the hashes, and check for any duplicate with `sort | uniq -d`. If either stage finds a hit, `exit 1`; otherwise `exit 0` after both checks pass cleanly. Test against a handful of hand-crafted clean/anomalous CSVs of your own before running against the real evil/clean corpora, since silent awk field-splitting or CSV-header handling bugs are the most common source of false positives/negatives here.
