Launch `/home/user/log_server.py` in the background (e.g. `python3 log_server.py &`) and confirm `curl http://localhost:8123/raw_logs.csv` returns data before writing downstream code. In `/home/user/process_logs.cpp`, parse the CSV (skip header), drop rows with `value < 0`, then for each integer timestamp t=0..5 find the two surrounding valid (timestamp,value) points and do straight-line linear interpolation — handle the edge case where t exactly matches an existing timestamp (use that value directly, don't divide by zero). After computing all 6 resampled values, compute the mean and population standard deviation (divide by N, not N-1) across those 6 values, then Z-score each one and print `timestamp,normalized_value` rounded to exactly 4 decimal places (use consistent rounding, e.g. printf `%.4f`). Write `/home/user/pipeline.sh` to compile the C++ file with `g++`, `curl` the raw CSV, pipe/pass it into the compiled processor, and redirect the final output to `/home/user/processed_logs.csv`. Run the pipeline end-to-end and inspect the output file before finishing.