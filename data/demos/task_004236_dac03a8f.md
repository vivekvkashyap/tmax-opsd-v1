Write a Python script that iterates `.asset` files (e.g. `glob.glob('/home/user/legacy_docs/*.asset')`), opens each in binary mode, and parses the fixed header per the spec: bytes[0:4] must equal `b'DOCS'` (skip/verify otherwise), bytes[4:8] unpacked as big-endian uint32 via `struct.unpack('>I', data[4:8])[0]` gives the asset type, and bytes[16:] is the payload. Only when type == 1 write the payload bytes to `/home/user/extracted_docs/<basename>.txt` (swap the `.asset` extension for `.txt`, e.g. with `os.path.splitext`). Skip type-2 files entirely — do not emit any file for them.

For the transformation stage, loop over the extracted `.txt` files with a shell `for f in /home/user/extracted_docs/*.txt` and pipe each through `sed`: delete lines starting exactly with `DRAFT:` using `/^DRAFT:/d`, and substitute the literal bracketed placeholder with `Acme Corp` using `s/\[COMPANY_NAME\]/Acme Corp/g` (escape the brackets since they're regex metacharacters), writing output to `/home/user/final_docs/<same filename>`. Verify no trailing artifacts (e.g. blank lines from the deleted `DRAFT:` lines) remain that weren't in the original non-draft content.
