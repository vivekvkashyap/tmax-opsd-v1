Model this as a tree/DAG rollup rather than a join-based aggregation, since the previous analyst's cross-join bug came from trying to do it with flat SQL joins. Build an adjacency structure first: for each employee, a list of direct reports (children), derived from `manager_id` in employees.csv, plus a direct-project-budget total per employee from projects.csv (sum budgets grouped by emp_id, defaulting to 0 for employees with none). Then compute each employee's rolled-up total via a post-order traversal from the leaves up (or equivalently memoized recursion: `rollup(emp) = own_direct_budget + sum(rollup(child) for child in direct_reports)`), which correctly handles indirect reports at any depth without double-counting or explosive cross products. Watch for cycles/missing-parent edge cases only if the data suggests it (here it's a clean tree with one root whose manager_id is empty). After computing, sort the results by emp_id ascending and emit a JSON array with exactly the two required integer-valued keys per object. Sanity-check by manually tracing one mid-tree employee's expected rollup (sum of their own projects plus all descendants' projects) and comparing to your script's output before finalizing.