Treat this as a standard graph-construction-plus-BFS problem, done by hand with dicts (no networkx). Parse both `relational_deps.csv` (each row `db,depends_on` is an edge db -> depends_on) and `doc_deps.json` (each `"requires"` entry is also `db -> requirement`) into one combined adjacency map (dependent -> list of dependencies). Since you need "who depends on auth_db, directly or indirectly," build the REVERSE graph (dependency -> list of dependents) or just BFS/DFS by scanning for any node whose forward-dependency chain reaches `auth_db`; a plain BFS from `auth_db` over the reversed edges gives you both the affected set and shortest edge-distance in one pass — track distances in a dict as you visit (visited-set BFS, not DFS, so first-visit distance is guaranteed shortest). Exclude `auth_db` itself from output. Sort by the spec exactly: distance descending, then name ascending for ties — use a Python sort key like `sorted(items, key=lambda kv: (-kv[1], kv[0]))`. Write lines as `f"{dist}: {name}"` to `/home/user/restore_plan.log`, one per line, then run the script once and diff the output visually against your own hand-traced graph to catch missed transitive edges before finishing.
