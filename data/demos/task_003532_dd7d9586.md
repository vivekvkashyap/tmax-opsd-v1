Start the MLflow tracking server first (`mlflow server --host 127.0.0.1 --port 5000 &`) and poll it with `curl` until it responds before running any client code, since the client will fail if the server isn't up yet. In your pipeline script, load the CSV with pandas and the JSON with `json.load`/`pd.DataFrame`, then merge them with an inner join on both `machine_id` and `timestamp` (not just one key) so rows align exactly — verify the merged row count matches expectations before modeling. Use `sklearn.model_selection.GroupKFold(n_splits=3)` and pass `groups=df["machine_id"]` to `.split()` so no machine's rows leak across train/val within a fold. For each alpha in the grid, start an MLflow run (`mlflow.set_experiment("Wear_Prediction")`, `with mlflow.start_run():`), fit `Ridge(alpha=...)` per fold, collect per-fold `r2_score` on the held-out fold, average them, and log both the `alpha` param and `cv_r2` metric via `mlflow.log_param`/`mlflow.log_metric`. After the sweep, track the best (alpha, score) pair as you iterate rather than re-querying MLflow, then write `/home/user/best_run.json` with `best_alpha` and `best_cv_r2` rounded to 4 decimals using `round(x, 4)` and cast to float.
