Load the two JSON files, then implement the random walk as a straightforward loop rather than overengineering: start at node "A", tally a visit count per node (including the initial position), and for each of 1,000,000 steps pick a uniformly random neighbor from the current node's adjacency list (`rand.Intn(len(neighbors))`) and move there, incrementing that node's tally. After the loop, divide each tally by the total observation count (1,000,001) to get your empirical distribution, then compute TVD against `/home/user/experimental_dist.json` as `0.5 * sum(|P(x) - Q(x)|)` over all nodes appearing in either distribution. Common pitfalls: forgetting to count the starting node as step 0, using a biased or non-uniform neighbor pick, or mismatching node sets between the two distributions (make sure every key present in either map is included in the sum, treating a missing key's probability as 0). Round the final TVD to 4 decimal places and write it as plain text to `/home/user/tvd_result.txt`. Since it's Monte Carlo, running 1,000,000 steps should already converge close enough to the graph's true stationary behavior — no need to special-case or hardcode toward a target value.
