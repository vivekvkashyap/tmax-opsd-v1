Write `/home/user/bayes_check.go` to read `/home/user/dataset_stats.csv` with Go's `encoding/csv`, skip the header, and for each row compute the posterior via Bayes' theorem exactly as given, using `float64` throughout and `fmt.Sprintf("%s,%.6f\n", id, posterior)` (or equivalent) to guarantee exactly 6 decimal places — don't rely on default float printing which varies precision. In `/home/user/run_pipeline.sh`: `go build` the program, run the resulting binary twice into `results.txt` and `results2.txt`, then compare them with `diff -q` or `cmp` to decide `Reproducible: YES`/`NO` (a deterministic Go program run twice on the same static input should be identical — if it isn't, something like map iteration order or uninitialized state is leaking in). Use `awk -F, '{sum+=$2} END {printf "%.6f", sum}' results.txt` to compute the sum, and append both `Reproducible: ...` and `Sum: ...` lines to `/home/user/pipeline.log` in that order. Make the script executable with `chmod +x` and actually run it before finishing so `pipeline.log` exists with real output.