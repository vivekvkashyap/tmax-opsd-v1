Structure archive_config.py as: (1) load and json.parse /home/user/backup_rule.json to get `source` and `mask_key` dynamically — don't hardcode the path/key even though you can see them now, since the point is reading the rule file; (2) open the source file, call `fcntl.flock(f, fcntl.LOCK_SH)` immediately after opening and before reading, to correctly demonstrate the safe-read pattern the task asks for; (3) iterate lines, and for each line check if it starts with exactly `mask_key + "="` (use `line.startswith(mask_key + "=")`, not a plain substring check, so you don't also redact keys that merely contain mask_key as a prefix-with-suffix, e.g. leave `SECRET_TOKEN_EXTRA=skip` untouched since it doesn't start with `SECRET_TOKEN=`) — replace only the matching line's value with `***`, keep everything else byte-for-byte; (4) print transformed lines to stdout; (5) explicitly unlock (`fcntl.flock(f, fcntl.LOCK_UN)`) and close. Run the script and redirect stdout with `python3 archive_config.py > /home/user/archived_settings.conf` to produce the final archive.
