Stream the CSV with `getline`/`fgets` in a single pass, but because dedup requires "keep only the last row per ID" and the file is pre-sorted by ID, use a lookahead/buffering pattern: hold the previously parsed row, and each time you read a new row, if its ID differs from the buffered one, flush (process) the buffered row and replace the buffer with the new row; if it matches, just overwrite the buffer with the new row (since it's the more recent retry) — flush the final buffered row after the loop ends (EOF), which is the easy place to forget a row. Maintain a single `double last_valid_measurement` variable across the whole stream (not per-ID) that only updates when a row's Measurement field is present and non-"NA"; impute from it when the field is empty or "NA" — note that a row's *own* imputed value should still update this running last-valid tracker consistently with how you define "measurement seen up to that point" (test against the given example: ID 103's own NA row inherits 103's earlier valid 50.0, not the global default). For username, use manual `isspace()` trimming (leading/trailing spaces and tabs) plus `tolower()`. For word count, iterate raw bytes and only count contiguous runs of bytes matching `isalnum()` under the "C" locale, treating any byte with the top bit set (`(unsigned char)c >= 0x80`) as a non-word-character separator so multi-byte UTF-8 sequences don't get miscounted as extra words.
