Deduplicate with SQL window functions or correlated MAX(updated_at) subqueries: for papers, keep only the row per paper_id with the highest updated_at; for citations, keep only the row per (source_paper_id, target_paper_id) pair with the highest updated_at, then keep it only if that latest row's is_active = 1 — a stale-but-active earlier row must not leak in. After dedup, drop any citation whose source or target paper_id isn't in your deduped paper set. Build a networkx.DiGraph from the surviving edges and call networkx.pagerank with default parameters (don't override alpha/max_iter/tol). Rank nodes by pagerank descending, breaking exact ties by paper_id ascending, and take the top 3. For each, join through paper_authors to authors and sort names alphabetically. Round pagerank to exactly 4 decimal places and emit the JSON array in the specified key order — write a small standalone verification pass yourself (recompute independently) to sanity-check the pagerank numbers before finalizing.
