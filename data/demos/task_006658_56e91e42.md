Derive the divergence rule analytically before writing any shell code: for explicit Euler on dy/dt = -k*y with step dt, the update is y_{n+1} = y_n*(1 - k*dt), so the scheme diverges exactly when |1 - k*dt| > 1; work through the algebra for the given fixed dt to get a simple threshold on k alone (a single inequality on k), and double check the boundary direction (strict vs non-strict) against the "strictly exceeds 1" wording. Then compute k per sequence as GC-count divided by total sequence length using only Bash tools: parse the FASTA with awk, tracking the current header and accumulating the sequence body across possibly-wrapped lines until the next `>` or EOF (don't assume each sequence is a single line), count G/C occurrences (`grep -o '[GC]' | wc -l` or an awk character tally) and divide by the total base count for that record. Compare each k against your derived threshold, collect the headers (without the leading `>`) of records that exceed it, sort alphabetically, and write one per line to /home/user/diverging.txt. Validate your awk parsing against a couple of records by hand-counting bases before trusting the full script.
