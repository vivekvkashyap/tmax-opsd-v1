Approach this as three separate SQL tasks against `/home/user/backups.db`. First inspect indexes with `sqlite3 backups.db ".indexes deps"` or query `sqlite_master` — find the one indexing only `child_id` (bad for parent->child lookups since the query pattern filters by parent_id), write its name to bad_index.txt, DROP it, then CREATE the composite index `idx_deps_optimal` on `deps(parent_id, child_id)` (column order matters: filter column first). Second, write a bash script that runs a `sqlite3` query joining jobs/deps, GROUP BY parent to get child counts, wraps it with `RANK() OVER (ORDER BY child_count DESC)`, and pipes the top 3 rows (rank,job_name,child_count) as CSV to top_jobs.csv — make it executable and actually run it. Third, use a `WITH RECURSIVE` CTE seeded at the job named SystemDB that joins deps to itself, building a `path` column via string concatenation with `->` as you descend, then select all resulting paths (including the root alone) into backup_paths.csv. Test each SQL snippet standalone in the sqlite3 CLI before wiring it into the deliverable files.
