Load `/app/chime_recording.wav` with `scipy.io.wavfile` or `soundfile`, cast to float and divide by the max absolute sample value so it's in [-1, 1], and build the time vector from the sample rate. Define the 3-component decaying-sinusoid model `S(t) = Σ A_i·exp(-λ_i·t)·sin(2π f_i t + φ_i)` as a plain function of 12 parameters. Rather than guessing parameter values, first get a good initial estimate cheaply — e.g. an FFT/periodogram of the signal to spot the three dominant frequency peaks and rough amplitude/decay estimates from the envelope — then use that as the starting point for your MCMC sampler (`emcee` is a natural fit; run ≥4 walkers/chains in parallel via its built-in `multiprocessing` pool) with a Gaussian likelihood tied to MSE between model and data. After sampling and discarding burn-in, take the per-parameter posterior mean, plot histograms/KDEs of all 12 marginals to `/home/user/posteriors.png`, synthesize the reconstructed signal from those means, write it as 16-bit PCM WAV at the original sample rate to `/home/user/reconstructed.wav`, and save the means to `/home/user/parameters.csv`. Iterate on optimizer/MCMC settings until the reconstruction MSE against the normalized original is well below the required threshold.
