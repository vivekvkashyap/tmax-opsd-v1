Tackle this in the three stated phases, and don't hardcode assumptions where you can derive facts. Phase 1: black-box `/app/legacy_chksum` by feeding it several files of known content (including edge cases like an empty file, a single byte, and a large file) and comparing the printed hex checksum against checksums you compute with well-known algorithms (CRC32, Adler32, with/without various XOR masks or seeds) using a quick script — iterate hypotheses until every test file matches exactly, rather than guessing once. Phase 2: read `/home/user/ingester/src/main.rs`'s packet-reading loop for buffer allocations that are never freed or that leak via `Box::leak`/`mem::forget`/growing a `Vec` without bound reuse per connection — replace with properly scoped, reused, or dropped buffers, and implement the checksum-validation function using the exact algorithm you reverse-engineered in Phase 1, dropping the connection when it fails to match the payload. Phase 3: before starting the server, migrate `readings_v1` into a newly created `readings_v2` table (matching the renamed columns) via `sqlite3 /home/user/telemetry.db "..."` SQL, then have the Rust server insert newly validated packets into `readings_v2` too. Compile with `cargo build --release`, run the binary detached (`nohup ... &`) bound to `127.0.0.1:9999`, and manually test with a small TCP client script sending a valid and an invalid packet before declaring done.