Build the pipeline in clear stages and verify each stage's output shape/values before moving to the next. Parse the FASTA with a simple manual reader or `Bio.SeqIO` (install via `pip install --user`), map each base to its float value with a dict lookup, and apply `numpy.fft.fft` to get complex coefficients per sequence — compute power as `np.abs(fft_result)**2`. When finding the max-power frequency, explicitly slice out or mask index 0 before calling `argmax` (e.g. `np.argmax(power[1:]) + 1`), since forgetting to exclude the DC component is the most common mistake here. Parallelize across sequences with `multiprocessing.Pool.map` over a per-sequence worker function — make sure the worker function is defined at module level (not a lambda/closure) so it's picklable. Write results sorted alphabetically by SequenceID to CSV with the exact header given, rounding MaxPowerValue to 4 decimals via `round()` or a format string. For the plot, find the sequence with the globally largest MaxPowerValue, replot its full power spectrum (power vs. index) with matplotlib using a non-interactive backend (`Agg`) so it saves cleanly under `savefig`. Finally write a pytest test that imports your reshaping/FFT function directly and asserts `len(fft_result) == 4` for input "ACGT", then run both the pipeline and pytest to confirm everything executes cleanly end to end.