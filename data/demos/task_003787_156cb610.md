Split into two subproblems: (1) a brute-force search to forge the checksum-colliding log, (2) a careful C++ rewrite. For the forgery, write a short script that starts from the exact payload bytes, computes `compute_hash` incrementally (running sum of char codes mod 65535), and appends one uppercase 'A' (ASCII 65) at a time, recomputing the sum after each append, stopping the instant it equals exactly 25000 — don't solve it with plain division up front, since modular wraparound may require looping past 65535 more than once before landing exactly on target; a simulation loop guarantees an exact hit. Verify the forged file both starts with the exact payload string and hashes to 25000. For the fixed C++ program, read the file's raw bytes in binary mode, replicate `compute_hash` exactly, print "Invalid checksum" and exit(1) if it doesn't match, and otherwise replace every `<` with `&lt;` and `>` with `&gt;` character-by-character before wrapping the result in `<body><p>...</p></body>` and writing it to `audit_report.html` in the current working directory.
