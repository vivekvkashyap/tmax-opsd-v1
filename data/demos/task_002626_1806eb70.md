Treat this as five independent checkpoints, verify each before moving on. (1) Tar slip: never blind-extract; list entries first with `tar -tvf /app/docs_update.tar` and inspect for `../` sequences or absolute paths (also check for `--transform`-style rewritten headers), then extract only entries under `docs/` into `/home/user/docs_clean/`, stripping the `docs/` prefix, and confirm afterward that nothing landed outside that directory. (2) Use `find ... -name '*.md' -exec sed -i 's/DRAFT_STATUS: INCOMPLETE/DRAFT_STATUS: FINAL/g' {} +` for the bulk edit. (3) For the differential backup, compare each file in docs_clean against its same-named counterpart in docs_original byte-for-byte (`diff -q` or `cmp`); anything missing on the original side or differing goes into `patch.tar.gz` — remember the sed edit itself makes content differ even if only the draft marker changed. (4) Transcribe `/app/interview.wav` with the installed `whisper` CLI to recover the spoken code phrase — don't guess it. (5) Build `serve.sh` as a `while true; do ... | nc -l -p 8000; done &` loop that reads one line, compares it exactly against `PROJECT_CODE: <transcribed phrase>`, and emits `ACCESS_GRANTED\n` plus the file contents or `ACCESS_DENIED\n`; test it yourself with a manual `nc` client connection before declaring done.
