Treat this as three independent pieces that must be wired together in the correct order. First create `/home/user/.config/systemd/user/port-forward.service` with an `ExecStart` running `socat TCP-LISTEN:10025,fork,reuseaddr TCP:127.0.0.1:2525` and `Restart=always`. Second, inspect `mailer_app.cpp` for the hardcoded port constant and change it to connect to `10025` (the forwarder), not `2525` directly, then recompile with `g++`. Third, and this is the actual bug being tested: edit the existing `mailer_app.service` unit to add `Requires=port-forward.service` and `After=port-forward.service` in its `[Unit]` section so systemd guarantees ordering/dependency instead of racing. Run `systemctl --user daemon-reload`, then start `port-forward.service` before `mailer_app.service` (or rely on the Requires/After to enforce it), and check `journalctl --user -u mailer_app.service` plus `cat /home/user/mail_spool.log` to confirm the message actually arrived through the forward chain.
