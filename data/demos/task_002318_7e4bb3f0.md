Write the pipeline in `/home/user/prepare_data.py` following the six numbered steps as literal checkpoints, since the grader diffs exact JSON values. Load and inner-join `transactions.csv` and `queries.json` on `user_id`, and record `initial_rows` = row count right after this join, before any cleaning. Fill nulls in `user_query` with the literal string `"empty"` (not `NaN`, not empty string). Apply the outlier filter as `revenue >= 0` and `revenue <= 5000` (read the boundary wording carefully — "negative" and "excessively high" are inclusive/exclusive at the boundaries as stated), count removed rows as `outliers_removed`, and record `cleaned_rows` after filtering. Run `HashingVectorizer(n_features=10)` on the cleaned `user_query` column, sum across the 10 features per row for `query_embedding_sum`, and average that column for `embedding_sum_mean`. For the bootstrap, call `np.random.seed(42)` exactly once before the loop, then draw 1000 with-replacement samples of size `len(cleaned_df)` from the cleaned `revenue` array, take each sample's mean, and get the 2.5th/97.5th percentiles via `np.percentile` — seed placement and sampling order matter for reproducibility, so don't reseed inside the loop or shuffle in extra random calls before it. Round each numeric field as specified and write all six keys to `/home/user/experiment_summary.json`.
