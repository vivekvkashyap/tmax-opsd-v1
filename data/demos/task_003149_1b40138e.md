Write the Rust program so both branches (connection success and connection failure) are cleanly separated and each does exactly one thing, since the harness will run the compiled binary twice under different network conditions rather than reading your source. Recursively sum file sizes under `/home/user/user_data` using `std::fs::read_dir` combined with a recursive helper that checks `metadata().is_dir()` vs file, accumulating `metadata().len()` — don't forget to recurse into subdirectories (a common bug is only summing the top level). After computing the total, compare against the 5,000-byte threshold, then attempt `TcpStream::connect(\"127.0.0.1:5555\")`: on `Ok(stream)`, write the exact string `QUOTA_EXCEEDED:<TOTAL_BYTES>` (no extra whitespace/newline unless specified) to the stream and let it close; on `Err(_)` (any connection error, not just refused), write the exact literal SSH command string (verbatim, no trailing newline surprises) to `/home/user/tunnel_cmd.sh` using `std::fs::write`. Compile with `rustc /home/user/quota_monitor.rs -o /home/user/quota_monitor` and confirm it compiles cleanly, but do not execute it yourself — the verifier controls the network/disk state and will invoke it directly.