This is a black-box regression task: don't assume the relationship is nonlinear before checking. First generate a training CSV of several thousand random 5-column rows spanning a wide numeric range, run each through `/app/oracle input.csv > targets.txt`, and inspect the residuals of a plain least-squares fit — if a simple linear model already drives error near zero, don't overbuild. Implement Ridge Regression in C++ with Eigen: build the design matrix with an added bias (intercept) column of ones, solve `(X^T X + lambda*I) w = X^T y` via Eigen's solver (don't regularize the intercept term), and implement k-fold cross-validation manually (split indices into folds, hold one out, fit on the rest, measure validation MSE) over a small grid of lambda values (e.g. 0, 0.001, 0.01, 0.1, 1) to log to `/home/user/cv_results.log`. Pick the lambda with lowest mean validation MSE — for a truly linear target this should push toward lambda≈0. Keep `predictor.cpp` self-contained: read the same 5-column CSV format (no header), apply the learned weights, print one float per line matching the oracle's formatting. Compile with `-O3` and link Eigen headers from `/usr/include/eigen3`. Validate end-to-end by comparing your predictor's output against the oracle on a held-out sample before considering it done.
