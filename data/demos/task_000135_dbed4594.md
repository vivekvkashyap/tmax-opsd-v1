Apply the patch first (`patch < /home/user/ci_update.patch` or `git apply`, from the correct working directory so paths line up) and confirm `ci_config.json` changed as expected before writing any code against it. In `builder.py`: parse each service's `version` into a `(major, minor, patch)` int tuple by splitting on `.`, and implement `>=X.Y.Z` satisfaction as a straightforward tuple comparison (`installed_tuple >= required_tuple`) — don't compare version strings lexicographically. Represent the DAG as an adjacency structure keyed by service name; first prune: a service is invalid if any of its declared dependencies is missing from the config OR fails its version requirement, and this invalidity must propagate transitively (a service depending on an invalid service is also invalid) — compute this with a fixed-point/worklist pass before sorting, not just a single pass. For the topological sort with alphabetical tie-breaking among ready nodes, use Kahn's algorithm with the ready-set implemented as a sorted structure (e.g. re-sort the zero-indegree list, or use a min-heap keyed by name) so ties always resolve alphabetically. Write `build_order.log` one name per line in that order, and generate the Makefile with an `all:` target listing dependencies in that same order plus one target per valid service (any target ordering is fine, only `all`'s list order is checked).