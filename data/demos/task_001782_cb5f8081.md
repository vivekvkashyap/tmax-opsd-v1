Treat this as three independent fixes that must all be verified live. (1) Cron/environment: inspect `/home/user/scripts/health_check.sh` to see which binaries and env vars it needs, then edit the user's crontab (`crontab -e` or write a crontab file and `crontab` it) to export `PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin` and `LOG_DIR=/home/user/logs` before the job line, since cron runs with a minimal environment unlike an interactive shell — confirm it actually appends to `/home/user/logs/health.log` and stops writing to the fallback log. (2) Transcription: use a CLI speech-to-text tool (e.g. `whisper-cpp`, or `ffmpeg` to convert format first if needed) on `/app/admin_voicemail.wav` to recover the exact numeric sample-rate threshold and comparison direction (greater than vs. greater-or-equal) stated in the recording — do not guess the threshold, transcribe it. (3) Build a Rust CLI at `/home/user/audio_filter` that parses the WAV RIFF header to read the sample rate field directly (byte offset in the `fmt ` chunk) rather than shelling out, and exits 0/1 per the transcribed rule; compile release mode so the binary lands at `target/release/audio_filter`. Validate against every file in `/app/corpus/clean/` and `/app/corpus/evil/` before finishing, since 100% classification accuracy is required.
