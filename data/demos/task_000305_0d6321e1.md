Handle the three phases in order and verify each independently before moving on. For the fstab fix, rewrite the `/home/user/iot_storage` line to use fstab's six whitespace-separated fields with filesystem type `bind` and options `defaults,bind`, dump `0`, pass `0` — the bug is that the original entry used `ext4` as the type instead of `bind`. For `mail.conf`, just replace the two values: `SMTP_HOST=127.0.0.1` and `SMTP_PORT=2525`, keeping the `KEY=value` format the mock SMTP server's own parser expects. For `start_iot.sh`, start with `#!/bin/bash`, `set -e`, `set -u`; `grep -q bind` the fstab file and `exit 1` if absent; launch both background processes with `&` and capture their PIDs (`$!`) into variables; install a `trap 'kill $PID1 $PID2 2>/dev/null; echo CLEANUP_SUCCESS; exit 0' SIGINT SIGTERM` before the `wait` call so the trap fires while the script is blocked in `wait`, not after. Make the script executable with `chmod +x`.
