Treat `/app/legacy_packer` purely as a black-box baseline to sanity-check against, not something to reverse-engineer exactly — black-box test it with small crafted inputs to confirm your understanding of the capacity and category constraints, then focus your effort on your own solver. Implement a bin-packing heuristic stronger than naive first-fit: sort assets by size descending and use best-fit-decreasing (place each item in the feasible bin — respecting both the 10,000 capacity sum and no-duplicate-category rule — that leaves the least remaining capacity, opening a new bin only when none fits) since this reliably beats plain first-fit on bundle count; if time permits, a local-search/simulated-annealing refinement pass after initial packing can shave off further bundles. Validate your own output against both constraints programmatically before moving on (recompute each bundle's size sum and category set) so you don't fail the verifier's constraint check outright. Write `/home/user/optimized_bundles.json` with the required array-of-arrays-of-ids shape. Then set up a static file server for the JSON and a separate reverse proxy on port 8080 that forwards `GET /latest_build` to it (nginx `location /latest_build { proxy_pass ... }` or a small Go/Python proxy), and curl the proxy yourself to confirm it returns the file's exact contents before finishing.