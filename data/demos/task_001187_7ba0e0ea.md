Break this into independent, verifiable stages rather than guessing the rules. First, transcribe `/app/incident_094.wav` using whatever ASR tool is available (e.g. `whisper` CLI, or an ffmpeg+speech-to-text pipeline) and read the transcript carefully to extract the exact normalization rules per subsystem and the exact output format — do not assume rules, derive them from what was actually said. Write the C++ parser to read whitespace-separated SUBSYSTEM/VALUE pairs in a loop (`while (cin >> name >> value)`), apply the per-subsystem transformation exactly as dictated (branching on exact string match), and print one line per event in the format specified in the audio. Compile with `g++ -O2 -o /home/user/dashboard_parser parser.cpp`, and test it manually against a few hand-crafted inputs before wiring up deployment. For the deploy script, make every step idempotent: use `mkdir -p`, `install -m 0755 -d <dir>` for directories, `cp -f` (or a checksum-guarded copy) for binaries, and explicit `chmod 0755` on directories versus `chmod 0555` on the deployed binaries — apply chmod after copying so re-running the script doesn't fail when the binary is already read-only. Run `deploy.sh` twice yourself to confirm idempotency before finishing.
