Build the pipeline as three clearly separated stages and validate each on the raw log before moving to the next. First, read `/home/user/raw_sensors.log` line by line, and for deduplication compute `hashlib.md5(raw_line.encode()).hexdigest()` on the exact untouched line (including any trailing whitespace/newline as read) — keep a seen-hashes set and skip lines whose hash repeats, preserving first occurrence order. Then extract `timestamp`, `sensor_id`, and the raw `value` token (which may be the literal string `ERROR`) via a regex matching the `LOG_START [...] - {id} reported temp: {value}` pattern, and load into a pandas DataFrame, converting `ERROR` values to NaN. Group by `sensor_id`, sort each group by timestamp, and apply pandas' `interpolate(method='linear')` on the temperature column per group (not across sensors) to fill NaNs — verify manually that boundary/interior gaps interpolate as expected for this small dataset. Finally format temperature to 1 decimal (`round(...,1)` or string formatting), sort the combined frame by timestamp then sensor_id, write the three required columns as CSV, `mkdir -p /home/user/remote_archive`, and copy/write the file to `/home/user/remote_archive/cleaned_data.csv`, then run the script yourself to confirm the file is produced.
