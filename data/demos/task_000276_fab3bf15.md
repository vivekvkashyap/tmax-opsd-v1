Since the underlying model is described as a linear projection with probabilistic scaling, treat this as a regression-recovery problem: generate a large batch of random 50-dimensional input vectors (e.g. tens of thousands, drawn from a distribution similar to what the test set likely uses, such as standard normal), feed them through /app/feature_extractor via stdin (comma-separated per line) in efficient batches rather than one-at-a-time, and collect the 5-dimensional outputs. Then fit a linear model mapping the 50-dim inputs to each of the 5 output dimensions independently — ordinary least squares or ridge regression (`sklearn.linear_model.LinearRegression` or `BayesianRidge`) — recovering an estimated weight matrix W (50x5) and bias vector b (5,), since the binary's output is expected to equal `X @ W + b` plus small noise, and averaging over many samples cancels the noise. Use enough samples and check residuals/confidence intervals (e.g. bootstrap or repeated fits) to confirm your W, b estimates are stable and not overfit to noise before finalizing. Implement `FeatureExtractor.transform(X)` in reconstructed_pipeline.py as a simple deterministic `X @ W_hat + b_hat` using your fitted parameters — no per-call randomness, since the verifier compares against the noiseless expected value.