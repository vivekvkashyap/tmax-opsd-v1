This combines OCR recovery with a graph-query HTTP service. First run `tesseract` (via `convert`/`ffmpeg` preprocessing if needed) on `/app/whiteboard.png` to recover the scribbled Primary Author ID — inspect the raw OCR text carefully, it may include a label like "Target Author: <ID>" so parse out just the ID token rather than assuming a fixed format. Then implement the co-author lookup as a simple graph traversal over `/home/user/data/papers.jsonl`: for each JSON line, if the `authors` array contains the recovered primary author ID, collect every other author ID in that array into a set; after scanning all lines, the set (minus the primary author itself) is the answer — do this with `jq`, Python, or awk, whichever is simplest to keep deterministic. Serve the result behind a lightweight HTTP server (Flask/FastAPI/http.server) bound to `0.0.0.0:8080` with a `GET /api/graph/coauthors` route returning a JSON array of the string author IDs, and leave the process running in the background so the verifier can hit it. Avoid hardcoding the ID you read from the image — always derive it from the OCR step at runtime so the pipeline is reproducible.
