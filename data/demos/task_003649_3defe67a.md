Work through the pandas pipeline in the exact order specified, since each step's output feeds the next. Read the file with `pd.read_csv('/home/user/loc_telemetry.csv', encoding='utf-16')` (or `utf-16le`) — a plain default read will produce garbled/binary-looking text, which is your signal the encoding is wrong. Filter rows to `missing_key == 'error.network_timeout'` exactly (not a substring match). Parse the `date` column to datetime and set it as the index, then use `.resample('D').sum()` (after first ensuring there's a numeric `count` column) to gap-fill missing days across the min-to-max date range with 0 — resample naturally does this once the index is a proper DatetimeIndex covering only observed dates; don't manually build a date_range and reindex unless resample doesn't already cover it. Then compute a 3-day rolling sum with `.rolling(window=3, min_periods=1).sum()`. Cast the result to int (avoid `5.0` vs `5` mismatches), format the date index back to `YYYY-MM-DD` strings, and write out with exactly two columns `date,rolling_count` using `to_csv(..., encoding='utf-8', index=False)`. Double check the output file is valid UTF-8 (not UTF-16) by decoding the raw bytes.