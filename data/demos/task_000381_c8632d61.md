Build this as three verifiable layers, testing each before stacking the next. First, write `exporter.c` as a minimal blocking HTTP/1.1 server on `127.0.0.1:8000` (raw sockets: `socket`/`bind`/`listen`/`accept`) that, on any request, reads and returns the current contents of `/home/user/dashboard_data/metrics_source.txt` with a proper `HTTP/1.1 200 OK\r\nContent-Type: text/plain\r\nContent-Length: <n>\r\n\r\n<body>` response — compile with gcc, run it backgrounded, and confirm with `curl 127.0.0.1:8000` directly before touching SSH. Second, set up the tunnel with `ssh -f -N -i /home/user/.ssh/id_rsa -L 9090:127.0.0.1:8000 user@127.0.0.1`, using `-o StrictHostKeyChecking=no` if host-key prompts would otherwise block a non-interactive run; verify the tunnel process exists via `pgrep -f "ssh.*-L.*9090:127.0.0.1:8000"`. Third, once the tunnel is confirmed listening on 9090, use `curl -s http://127.0.0.1:9090/` (which strips headers automatically, unlike `curl -i`) and redirect the body to `/home/user/dashboard_test.log`. Verify both the exporter and the ssh process are still running as background jobs at the end.