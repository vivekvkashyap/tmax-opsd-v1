Split this into a one-time extraction step and a general-purpose runtime script, and don't hardcode assumptions about corpus size. First run `ffmpeg -i /app/schema_stream.mp4 -vf fps=1 frames_%03d.png` to get one frame per second, then decode each with `zbarimg` (e.g. `zbarimg --raw frames_*.png`) to pull out the embedded JSON snippets; parse and merge them into a single `{parent: [children...]}` dict, discarding any exact duplicate frames from consecutive seconds. Hardcode this extracted base graph as a Python dict literal at the top of `etl_graph.py` (recovering it is your job — the actual node names live in the video, not in the task text). For the runtime logic: read the stdin JSON array of `{"source":..., "target":...}` edges, merge each into the graph (`graph.setdefault(source, []).append(target)`), then do a BFS/DFS from `"ROOT"` tracking depth per node, and identify leaves as any node that never appears as a `source` key (or has an empty children list) — watch for cycles/duplicate edges introduced by fuzzed input and make traversal robust (e.g. track visited nodes) so it doesn't infinite-loop or double count. Output the leaf->depth mapping as JSON sorted alphabetically by key (`json.dumps(dict(sorted(...)))`). Make the script executable and test it manually with a few crafted stdin payloads before relying on it.
