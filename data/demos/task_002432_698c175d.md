Locate the bug by reading `create_index` in `/app/sqlite-utils/sqlite_utils/db.py` and comparing it against what it should do — look for a place where the SQL execution call has been replaced with a no-op so index creation silently does nothing. Restore the actual execute call, then re-run db_setup.py and confirm via `sqlite_master` that CREATE INDEX statements for the edges table's source/target columns actually took effect. For optimize_queries.py, don't do per-pair full-table-scan traversal; instead load the edges into an in-memory adjacency structure once (a single query, not one per pair) and run Dijkstra or BFS with a priority queue for each of the 50 pairs in Python, relying on the now-indexed table only for the initial bulk load, since 50 shortest-path computations over 50k edges must finish comfortably under 2 seconds. Serialize the pair-key strings and integer weights (or null when unreachable) exactly as specified, and time the whole script to confirm it's well under the 2.0s threshold.
