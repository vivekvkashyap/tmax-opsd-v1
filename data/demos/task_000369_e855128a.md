Work through the pandas pipeline in the exact order specified, since the bootstrap sampling is seed-sensitive and must be reproducible: load `/home/user/raw_cars.csv`, compute `vehicle_age = 2024 - year + 1` and `mileage_per_year = mileage / vehicle_age`, then subset columns to `make, vehicle_age, mileage_per_year, price` (drop `year`/`mileage`). Split into the `Standard` and `Luxury` subsets by `make`. To bring `Luxury` up to the same row count as `Standard`, use pandas' own sampling with replacement rather than a manual random loop — call `.sample(n=len(std_df), replace=True, random_state=42)` on the Luxury subset exactly, since any deviation in method or seed changes which rows get drawn and therefore the final mean price. Concatenate Standard (unmodified, all originals) with the bootstrapped Luxury rows, write the result to `/home/user/processed_cars.csv` with `index=False`, and compute the mean of the combined `price` column, rounding to exactly 2 decimal places before writing it as plain text to `/home/user/metrics.txt`. Double check the row count of the output equals twice the Standard subset's size.
