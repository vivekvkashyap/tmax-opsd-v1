Do this with two independent awk/join passes over the CSVs, no Python. For schema validation: build a set of valid account IDs from `accounts.csv` (e.g. `tail -n+2 accounts.csv | cut -d, -f1 | sort -u`), then for each transaction check both `from_account` and `to_account` against that set with `grep -Fxq` or an awk associative array loaded from the accounts file; collect failing `tx_id`s and pipe through `sort` before writing to `/home/user/invalid_tx.txt`. For deadlock detection: with awk, truncate each timestamp to the minute by taking the first 16 characters (`substr($5,1,16)`), and for every transaction record the triple (from, to, minute); a deadlock exists when both (A,B,minute) and (B,A,minute) appear — check this by building a lookup keyed on "from|to|minute" and testing for the reverse key. Normalize the pair so the JSON's `account1` is the lexicographically smaller ID (compare with awk string comparison), then emit with `jq -n --arg ...` to build valid JSON array entries, avoiding manual string concatenation which risks quoting bugs.
