Debug `poly.c` first: the current implementation mallocs a scratch buffer it never frees (fix by freeing it or removing the unnecessary allocation) and its Horner-style loop runs the index down to -1, causing an out-of-bounds coefficient read at `coeffs[degree+1]` — rewrite the loop so it iterates strictly from `degree` down to `0` inclusive, using only valid indices. Validate memory safety with `valgrind --leak-check=full` or compile with `-fsanitize=address` against varied degrees before moving on. Build `libpoly.so` with `gcc -shared -fPIC` and write a `Makefile` target that also builds the Go binary `poly-server`. In the Go package, use `cgo` (`#cgo LDFLAGS: -L. -lpoly` plus `#include "poly.h"`) to call `evaluate_polynomial`, and write `poly_test.go` that shells out to `/app/poly_oracle <x> <a0>...<an>` (note the oracle's coefficient order — check whether it's a0-first or matches your own convention) for a range of random/edge-case inputs, comparing its stdout float against your CGO wrapper's result with a small epsilon tolerance. Finally implement `main.go` with `net/http` only: check the `Authorization: Bearer SIGMA_7781` header on `POST /poly/eval`, unmarshal `{"coeffs":[...], "x":...}`, call your CGO function, and respond with `{"result": ...}`; build and launch it in the background on `127.0.0.1:8080` and confirm it's reachable before finishing.