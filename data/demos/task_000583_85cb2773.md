Diagnose each bug independently before patching. Assembly bug in `hash.c`: the inline `__asm__` block modifies `%eax` internally but never lists it in the clobber list, so under `-O3` the compiler may keep a live value in `eax` across the statement and get corrupted — add `"%eax"` (or `"%rax"`) to the clobber section (third colon-separated list) so GCC knows the register is destroyed. Leak/safety bug in `api.c`: the early-return branch rejecting malformed requests (missing/invalid `Authorization`) returns 400 without freeing the already-allocated `ctx->ip_addr`/`ctx` — add matching `free()` calls on that error path, and check for any field used before being assigned during IP/auth parsing. Build with `make` (enforces `-Wall -Werror -O3`); treat warnings as bugs, not noise to suppress. To verify the rate limiter, start `./server &`, issue three `curl -s -o /dev/null -w "%{http_code}\n"` requests with `-H "Authorization: Bearer token123"` from the same source, confirm you see 200, 200, then 429, and only then write those codes one per line to `/home/user/api_server/test_results.log`.
