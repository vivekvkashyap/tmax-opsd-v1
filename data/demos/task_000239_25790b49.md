First diagnose why the Flask app crashes: read `/app/api.py` and note it reads `REDIS_PORT` from the environment, defaulting to 6380, while Redis actually runs on the standard 6379. Fix this by exporting `REDIS_PORT=6379` in a wrapper script (or launching with the env var set) before starting `python3 /app/api.py`, then confirm the `/api/params` endpoint responds. For the sequence processor, fetch `alpha` and `N` from that endpoint at runtime rather than hardcoding them. Build the numeric signal by mapping each base to 1.0 (C/G) or 0.0 (A/T), then iterate the explicit finite-difference heat equation exactly as given — critically, update all interior points from the *previous* iteration's values simultaneously (use a separate output array per iteration, don't overwrite in place), and re-clamp both boundary points to 0.0 after every iteration. After N iterations compute the mean and print the 0-based indices strictly greater than the mean as a comma-separated list with no extra whitespace, since your script will be fuzz-tested byte-for-byte against a reference implementation across many random sequences and lengths (including short edge cases where the boundary/interior logic changes).
