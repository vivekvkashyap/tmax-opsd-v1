Work this like a real forensic incident: first isolate the malicious IP, then recover the deleted evidence, then correlate the two.

1. Analyze the pcap with `tshark` or `tcpdump`: filter on HTTP requests and grep for the target endpoint, e.g. `tshark -r /home/user/capture.pcap -Y 'http.request.uri == "/api/v1/explode"'` and extract `ip.src` from the matching packet.
2. You have no sudo/mount access, so use the sleuthkit suite directly on the raw image instead of mounting it: run `fls -r /home/user/storage.img` to list allocated and deleted inodes (deleted entries are marked, e.g. with `*`), find the entry for `gateway.log`, note its inode number, then run `icat /home/user/storage.img <inode>` to dump its recovered contents to a file.
3. Search the recovered log for the traceback block associated with the IP found in step 1 (the log line mentioning that source IP), and read the final exception line of that Python traceback to get its exact exception class name.
4. Write exactly two lines to `/home/user/incident_report.txt`: the IP on line 1, the exception type on line 2 — no extra text, labels, or trailing content, since the check does exact/substring matching on these two values.
