Process the JSONL file as a single streaming pass, keeping only small per-language state in memory (e.g. a dict from `lang` to a running top-3 heap/list plus a separate dedup set/dict keyed by normalized text). For each line: parse JSON, apply `unicodedata.normalize('NFKC', text).lower()`, skip if `len(normalized) < 15`. For deduplication, track the best (lexicographically smallest `id`) record seen so far per normalized-text key — since you're streaming, you must handle a later duplicate with a smaller id replacing an earlier one already provisionally kept, so don't discard candidates too early; keep a dict of normalized-text -> record until all input is read, since the "smallest id wins" rule requires seeing all duplicates first. Then group by language and pick the top 3 by normalized-text length, breaking ties by smallest id — a stable sort or explicit comparator on `(-length, id)` avoids off-by-one selection errors. Finally sort the output by `(lang, -length, id)` exactly as specified, and write each record's `text` field as the *normalized* (not original) text. Watch especially for full-width Unicode variants and case differences that only become identical after NFKC+lowercasing — verify your dedup catches those cases explicitly.