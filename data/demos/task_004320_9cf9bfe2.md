Treat this as three separate diagnostics before touching code: (1) run `git log -p` (or `git log --all -p`) in `/app/data-ingestor` and grep the full history for a removed constant/string that looks like an API key — a commit message mentioning removing a hardcoded secret is your signal to look at that commit's diff for the deleted line; export the recovered value as `UPSTREAM_API_KEY` when launching the service and wire the HTTP handler to check the `Authorization: Bearer <key>` header against it, rejecting mismatches. (2) Find the VWAP update/division logic in `src/main.rs` and add an explicit `qty == 0` guard that returns HTTP 400 before any division occurs, rather than trying to catch the panic. (3) Change every `f32` in the tick struct, VWAP accumulator state, and any related arithmetic to `f64` — search broadly, since a single missed `f32` field (state or serialization type) will silently reintroduce precision loss even if others are fixed. After fixing, run `cargo build --release`, verify it compiles cleanly, bind to `127.0.0.1:8080` exactly, and run it in the background (e.g. with `nohup ... &`) so it survives your session, then manually curl `/tick` and `/vwap` to confirm behavior before considering the task done.