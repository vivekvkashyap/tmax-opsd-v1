This is a two-part task: reverse-engineer a binary's transform, then wire up a broken multi-service pipeline.

For part 1, don't guess blindly — run `strings /app/bin/obfuscated_logger` to look for hash-related library symbols or format hints, and/or use `ltrace`/`strace` on a couple of sample invocations to see which crypto functions get called and in what order. Since the task says "standard cryptographic hashing," systematically test hypotheses by running the binary on a few known inputs and computing candidate hashes (md5sum, sha1sum, sha256sum, and combinations/chaining of them) on those same inputs with coreutils, comparing against the binary's actual output byte-for-byte until you find the exact construction (including any separator between the original message and the digest, and hex vs other encoding). Only write `/home/user/clean_logger.py` once you've confirmed the transform reproduces output exactly on several test strings, including edge cases (empty string, special characters).

For part 2, read the existing `nginx.conf` and `worker.py` carefully to find the specific broken values (wrong Redis host/port, misconfigured Lua block/location for `/log`) rather than rewriting from scratch, fix just those, then start Redis, the worker, and Nginx in that order, and validate the full path with a real `curl` POST before declaring success.
