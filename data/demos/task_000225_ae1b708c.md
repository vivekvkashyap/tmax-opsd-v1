First extract the two facts you need from the image with `tesseract /app/window_spec.png stdout` (or OCR equivalent): the exact windowing function formula and the exact name of the summation algorithm — transcribe both precisely, don't assume a textbook default for either. Then build the Go tool in clear stages: (1) stream stdin line by line, split on whitespace, keep only lines with exactly 5 tokens where token 0 is `ATOM` and tokens 1-4 parse as int/float/float/float respectively, silently skipping anything else; (2) collect the Z (4th float) values; (3) apply the windowing function read from the image elementwise (handle N=0 and N=1 as special cases per the spec); (4) implement the naive O(N^2) DFT directly from the formula — no FFT library; (5) compute squared magnitude per bin; (6) accumulate the final sum using the exact summation algorithm named in the image (research how that specific algorithm compensates for floating-point rounding error — it's not naive addition) so results are bit-reproducible across runs. Format the final print to exactly 6 decimals with a trailing newline. Test against edge cases (empty input, one ATOM line, malformed lines mixed in) before considering it done.
