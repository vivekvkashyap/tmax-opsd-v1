Fix the three layers in dependency order and verify each builds before moving on. In the C Makefile, add an archive step after compiling the object file — `ar rcs libfilter.a filter.o` — so `libfilter.a` actually gets produced; run `make` in `c_lib` and confirm the .a file exists and `nm libfilter.a` shows the symbols. In `build.rs`, uncomment/add the `cargo:rustc-link-search=native=<path-to-c_lib>` and `cargo:rustc-link-lib=static=filter` directives (use an absolute or `CARGO_MANIFEST_DIR`-relative path so it works regardless of cwd) — `cargo build` should then link cleanly. For the lifetime bug, the pattern `CString::new(x).unwrap().as_ptr()` is dangerous because the temporary CString is dropped at the end of the statement while the raw pointer is used after; fix by binding each CString to a named variable that outlives the FFI call (`let s = CString::new("apple").unwrap(); unsafe { filter_add(s.as_ptr()); }`), for both the seed strings and the per-line lookups. Once `cargo build --release` succeeds, write `run_pipeline.sh` to build both pieces, then wrap the release binary invocation with `/usr/bin/time -p ... 2> time.log`, redirect stdout to results.txt, and extract just the numeric "real" value from time.log into bench_log.txt with grep/awk.
