Start by inspecting the schema and indexes directly in sqlite3 (`.schema events`, `PRAGMA index_list(events);`) to confirm the events table columns and locate `idx_stale_data`, then drop it with `DROP INDEX idx_stale_data;` — do this as an explicit, separate step you can verify with another `PRAGMA index_list` call, not folded silently into the Python script. For the "latest successful event per directed pair" extraction, use a window function keyed on (src_node, dst_node) filtered to status='SUCCESS', e.g. `ROW_NUMBER() OVER (PARTITION BY src_node, dst_node ORDER BY event_time DESC)` in a CTE/subquery, then select where rn=1 — don't try to fake this with a GROUP BY MAX(event_time) join, which breaks ties differently and is exactly the kind of subtle bug to avoid here. Build a networkx DiGraph from those (src, dst) pairs (one edge per pair, since you already deduped to latest), compute in-degree with `G.in_degree()`, then write node,in_degree rows sorted alphabetically by node name to the CSV with a plain csv.writer, not printed dicts (to control the exact header and quoting). Run the script and re-open the CSV to confirm formatting before finishing.
