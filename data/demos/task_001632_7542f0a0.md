This is a streaming per-key state machine — solve it with a single sorted pass in awk, not a naive one-shot pipeline. First sort the log by CONFIG_KEY then TIMESTAMP (`sort -t'|' -k3,3 -k1,1`), computing NET_BYTES = BYTES_ADDED - BYTES_REMOVED per line. Feed the sorted stream into an awk script that keeps, per CONFIG_KEY, a small rolling buffer (e.g. an array of up to 3 recent NET_BYTES values, or three scalar variables shifted each iteration) — on each event, push the new value in, drop anything beyond the last 3, and compute the average with integer truncation-toward-zero division (careful: this differs from floor division for negative numbers, since NET_BYTES can go negative when bytes_removed > bytes_added — verify your awk integer-cast approach, e.g. via `int()`, matches C-style truncation, not floor). Emit the CSV row immediately per event to preserve the sorted order in `/home/user/rolling_averages.csv`. Separately track the maximum absolute rolling average seen across all keys/events, breaking ties by picking the alphabetically-first key, and write `/home/user/max_volatility.json` in the exact specified schema. No Python/Perl — awk, sort, sed only. Test with a small hand-crafted input where you can compute the expected rolling averages by hand before running on the full log.