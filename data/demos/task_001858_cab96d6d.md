Fix the build config first, then the Rust bug, then wire the server — verify each stage with a quick standalone test before moving on. In `/app/vendored/ci-log-decoder/pyproject.toml`, correct the `[build-system]` `build-backend` entry to point at maturin (e.g. `build-backend = "maturin"` with `requires = ["maturin>=1.0"]`) instead of the generic setuptools boilerplate. In `src/lib.rs`, run `cargo build` (or `maturin develop`) to see the borrow-checker error — it's likely a `&str`/slice reference taken from data inside a loop iteration being pushed into a `Vec` that must outlive the loop; fix it by collecting owned `String`s (e.g. `.to_string()`/`.to_owned()`) into the results vector instead of holding borrowed slices. Build and install with `maturin develop` (inside the venv) so `import ci_log_decoder` works from Python. Implement the state machine understanding: `0x02` start byte, next byte is length, then that many UTF-8 bytes, then `0x03` end byte — repeat until input exhausted. Write `/app/server.py` using Python's `http.server` (or similar) bound to `127.0.0.1:8181`, handling `POST /parse` by reading the raw request body bytes, calling `ci_log_decoder.decode_log(data)`, and returning the result JSON-encoded with `Content-Type: application/json`. Run it in the foreground so it stays alive for testing.