Set up the Go module first (`go mod init`, `go get github.com/mattn/go-sqlite3`, CGO enabled) and open `/home/user/company.db` read-only. Build the three analyses as separate passes: (1) for cross-department traffic, join `communications` to `employees`/`departments` on both sender and receiver, normalize each pair by sorting the two department names alphabetically before forming the `"DeptA-DeptB"` key (so A→B and B→A land in one bucket), and sum `bytes` per normalized pair, keeping only cross-department pairs. (2) Build an undirected adjacency structure from every communications row (edge both directions), compute each employee's degree as their unique-neighbor count, sort by degree descending with employee ID ascending as tiebreaker, take the top 3. (3) Reuse the adjacency graph and BFS from employee 1, tracking predecessors, to reconstruct the shortest path to employee 7. Marshal results into the exact JSON keys/structure shown, and hand-trace the small dataset (or print intermediate adjacency/degree info) to sanity-check before finalizing.