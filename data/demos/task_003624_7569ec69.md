First determine MAX_RECORDS by inspecting the video, don't hardcode a guess: use `ffprobe -v error -count_frames -select_streams v:0 -show_entries stream=nb_read_frames -of csv=p=0 /app/telemetry_feed.mp4` (or an equivalent frame-counting method) to get the exact frame count, then use that value as MAX_RECORDS in your C program. Design the CSV parser as a character-level state machine tracking an `in_quotes` flag: toggle it on unescaped `"`, and only treat a `\n` as a row terminator when *not* inside quotes — if a `\n` occurs while in_quotes, mark the row as having an embedded newline and drop the whole row once it's fully consumed (do not emit partial output). Parse the four comma-separated fields per row only when not inside quotes, mask the IP by finding the last `.` and replacing everything after it with `XXX` (leave IPs with no dot unchanged), track seen DeviceIDs in an array/set to skip duplicates, and stop emitting once MAX_RECORDS data rows have been printed or stdin is exhausted. Since correctness is verified via fuzz equivalence against a hidden oracle across many random inputs, test your compiled /home/user/cleaner locally against a range of hand-crafted CSVs (quoted newlines, duplicate IDs, IPs with 0-4 dots, edge cases at MAX_RECORDS) rather than a single happy-path example.
