Work phase by phase and verify each before moving on. Start the simulation in the background, then write a quick TCP port scanner over 10000-10050 that connects and reads whatever bytes arrive before the peer closes — most ports will refuse/drop immediately, exactly one will stream binary data; save that raw payload and note its port number. Separately, run `strings /home/user/auth_daemon | grep -E 'KEY_ID=|IV_ID='` to locate the two prefixed constants, then slice exactly the 16 characters following each prefix (watch for the strings tool showing extra characters/padding — verify the slice length is exactly 16 for both). Decrypt the captured payload with AES-128-CBC using those key/IV values and PKCS7 unpadding (Python `cryptography` library) to recover a JSON object with `salt` and `hashes`. For cracking, brute-force each 4-digit PIN 0000-9999, computing `SHA256(password + salt)` and comparing hex digests against each user's stored hash — do this for every user independently. Assemble the final JSON report exactly per the given schema, using the port you actually observed accepting the connection.
