Build this as three independently-testable stages before wiring the HTTP layer. First, load `openai/whisper-tiny` via `transformers` (pipeline or processor+model) and confirm it transcribes `/app/support_call.wav` to something reasonable on its own. Second, load `sentence-transformers/all-MiniLM-L6-v2`, embed each `description` from `/app/intents.json` once at startup (cache these embeddings, don't recompute per request), and write a cosine-similarity function (`dot / (norm*norm)`) that picks the argmax intent given a transcription embedding — test it manually against a couple of hand-typed sentences to confirm sane matches before hooking up audio. Third, expose a minimal Flask/FastAPI server bound to `127.0.0.1:5000` with a `POST /predict_intent` route that reads the raw request body bytes as WAV audio (use `io.BytesIO` + `soundfile`/`scipy.io.wavfile` to decode, resampling to 16kHz if needed since Whisper expects that), runs it through the transcription and embedding/matching pipeline, and returns exactly `{"transcription": ..., "intent": ...}` with no extra keys. Start the server as a background process and smoke-test it by POSTing `/app/support_call.wav`'s raw bytes with `Content-Type: audio/wav` and checking the JSON shape and a plausible intent match before finishing.