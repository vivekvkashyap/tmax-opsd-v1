Treat this as two independent bugs in `/home/user/analyze_motion.py`. First, the SQL query: read the current query against `calibration.db`'s `configs` table — if it lacks a `WHERE is_active = 1` clause (e.g. just grabs the first/any row), fix it to explicitly filter for the active row before extracting `offset_x`. Second, the variance instability: the current rolling-variance code likely computes variance via the naive sum-of-squares formula (`E[X^2] - E[X]^2`) in `float32`, which suffers catastrophic cancellation when the offset shifts all X values to a very large magnitude (small variance relative to a huge mean squared). Fix this by switching all coordinate/accumulator arithmetic to `float64`/double precision and computing variance via a numerically stable method — either two-pass (subtract the window mean from each value first, then average squared deviations) or an online method like Welford's algorithm — rather than the naive single-pass sum-of-squares formula. After fixing both, rerun the script end-to-end so it regenerates `/home/user/variance_output.csv` with `frame_idx,variance` columns, and sanity-check that no variance values are negative (a strong signal the cancellation bug is still present if you see any).