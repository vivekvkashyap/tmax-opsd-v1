Design the deployment logic as clearly separated steps and test each with your own manual multi-run scenario before trusting it, since the grader will call `deploy.py` repeatedly with different versions and inspect the resulting directory state. Step 1: copy `/home/user/app_source/` into `/home/user/releases/<version>/` using something like `shutil.copytree(..., dirs_exist_ok=True)` so re-deploying the same version overwrites rather than errors (idempotency). Step 2: atomically update the `current` symlink (remove existing symlink/file then `os.symlink`, or use a temp-name + `os.rename` swap) to point at the new release. Step 3: compute total bytes under `/home/user/releases/` by walking all files recursively and summing `os.path.getsize`. Step 4: if total exceeds 204800 bytes, repeatedly find the oldest release directory by modification time (`os.path.getmtime` on each release dir, or track deploy order) and delete it — but explicitly skip/never delete whichever directory `current` currently resolves to (`os.readlink`/`os.path.realpath`), even if that leaves the total over quota. Make the script executable with `chmod +x`. Manually simulate the deploy-quota scenario (deploy several versions of growing size) yourself to confirm oldest-first eviction and the current-protection rule both behave correctly before considering it done.
