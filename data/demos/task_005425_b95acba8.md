Write the script so it takes `$1` (tenant_id) and `$2` (backup_date) as positional args, then build the recursive CTE once and reuse its exact text for both the real query and the `EXPLAIN QUERY PLAN` variant (define it as a shell variable so you're not duplicating SQL). The CTE should anchor on `parent_id IS NULL AND tenant_id = $1`, selecting `name` as the initial path, and recursively join `filesystem` children on `parent_id = <anchor>.id`, concatenating `parent_path || '/' || name` — use SQLite's `||` operator for string concat. Pipe the final `SELECT path FROM cte ORDER BY path` (no header, so use `sqlite3 -noheader -csv` or `.mode list`) into `/home/user/paths.log`, and separately run `EXPLAIN QUERY PLAN <same CTE>` piped into `/home/user/plan.log` raw. For the Mongo pipeline, don't hand-write JSON strings with string interpolation that risks quoting bugs — build it with a heredoc or here-string where `$1` is substituted as a bare integer (no quotes) and `$2` as a quoted string, matching the two-stage `$match`/`$group` structure exactly, then validate it parses with `jq . /home/user/mongo_pipeline.json` before finishing. Remember to `chmod +x` the script.
