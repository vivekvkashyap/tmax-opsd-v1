Write the script to parse `RANKS=` and `THREADS=` out of the config file (e.g. with `grep`/`awk` or shell parameter expansion) before building the command — do not hardcode. Invoke exactly `timeout 2 mpirun -np <RANKS> -x OMP_NUM_THREADS=<THREADS> /app/mc_oracle <config>` and capture both its stdout and exit code (e.g. `output=$(timeout 2 mpirun ... 2>&1); rc=$?`). Classify as evil (exit 1) if: `timeout` killed it (rc 124), the command returned any non-zero exit code, or stdout contains the literal strings `Result: NaN` or `Result: 0.0000`; otherwise classify clean (exit 0). Test your script against a range of hand-crafted configs (varying SAMPLES divisibility by RANKS*THREADS, RANKS>4, THREADS=0, and normal cases) to make sure each classification path actually triggers, since you cannot inspect the binary's source. Pitfalls: forgetting `chmod +x` on the script, not capturing stdout separately from checking rc (both signals matter), and not using `timeout` correctly (must wrap the whole mpirun invocation, and its own exit code 124 signals a hang).
