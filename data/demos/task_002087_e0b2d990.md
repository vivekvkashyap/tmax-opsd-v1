Start by attempting `make` in `/app/fast-archiver-1.0/` and reading the failure/output size closely rather than assuming one bug — check the Makefile for a typo'd compiler variable, a missing optimization flag, and a missing library link flag, since all three commonly combine in vendored-package breakages (compiler name mismatch, no `-O3`, no `-lzstd` or similar). Fix each one, confirm `make` succeeds, then copy the resulting binary to `/home/user/bin/fast-archiver` and add that directory to PATH (`export PATH="/home/user/bin:$PATH"` in your shell/profile). Note that the optimization flag isn't just cosmetic here — it likely gates which compression code path the tool takes internally, so verify the binary's *actual* compressed output size on a test directory before wiring it into the daemon; if omitting `-O3` still "builds," that's a trap since the archive will then blow past the size threshold. For the daemon script, poll `/home/user/spool/` for `backup.conf` (a simple `while` loop with `sleep`), parse `TARGET_DIR=`/`EXCLUDE_EXT=` key-value lines, use `iconv -f ISO-8859-1 -t UTF-8` to convert every `.txt` file under `TARGET_DIR` in place, then invoke `fast-archiver <TARGET_DIR> /home/user/final_backup.fsa`. Run it in the background, drop the sample config, and check the resulting archive's byte size against the threshold with `stat -c%s`.
