Start by grepping `/app/vendored/pyjwt_custom-1.0.0/pyjwt_custom/decoder.py` for handling of the `alg` header field — the backdoor is almost certainly an early-return/bypass branch triggered when `alg` is `"none"` (case-insensitive), which skips signature verification entirely. Remove or invert that branch so a `none`-alg token instead raises/propagates an exception through the normal invalid-signature path, then reinstall with `pip install -e /app/vendored/pyjwt_custom-1.0.0/` so the patched code is actually what's imported (check with `python -c "import pyjwt_custom; print(pyjwt_custom.__file__)"`). Fix the key file permissions with `chmod 400 /home/user/keys/secret.key` — the server itself checks this via `os.stat(...).st_mode & 0o777`, so it must be exact, not just "restrictive". Read `/home/user/server.py` to confirm how it wires the JWT decode call into the `/data` route and how it maps exceptions to HTTP status codes (should be 401 on any decode failure, including missing/none-alg tokens). Finally launch it with `python3 /home/user/server.py &` (or nohup) so it persists in the background, and manually curl `/data` with no token, a none-alg token, and a validly-signed token to confirm 401/401/200 before finishing.
