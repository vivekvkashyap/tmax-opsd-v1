First extract the actual spec from `/app/spec.png` — open/OCR the image (or view it) to read the exact endpoint path, request payload shape, response JSON format, and the modularity score formula, since these details are given only in the image and must be read precisely rather than assumed. Implement the REST API (Flask or similar) exactly matching what the image specifies for the endpoint and payload/response schema, listening on port 8000. For the organization algorithm: parse each `.py` file in the target directory for `import <name>` lines to build a directed dependency graph among the files in that directory, then search for a partition into modules (each capped at 3 files) that maximizes (intra-module edges)/(total edges) — since the directory sizes are small, a greedy or brute-force/local-search grouping over connected components is tractable and more reliable than a heuristic that ignores the module-size cap. Write `test_api.py` with your own small fixture directory and known dependency graph so you can hand-verify the score formula before trusting it on the real target. Leave the API running in the background (e.g. via `nohup`/`&`) since the verifier will POST to it live and independently recompute the score against a hidden evaluation directory — don't hardcode or fudge the returned score.
