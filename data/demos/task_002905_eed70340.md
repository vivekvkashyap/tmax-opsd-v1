Write this as a straightforward multi-stage C++ program rather than trying to be clever with streaming. Parse `data.csv` carefully: the `text` field is quoted and may need trimming of the surrounding quotes (a simple split on the first/last comma plus quote-stripping is fine since fields don't contain embedded commas here). For each text, build a `double[26]` counting only alphabetic characters case-insensitively (`tolower`, then `c - 'a'` index), sum the counts, and divide each bin by the total to normalize — but guard the all-zero case (no alphabetic characters) so you don't divide by zero, leaving the vector as all zeros per the spec. Parse `weights.csv` into a `double[26]` indexed the same way. Compute the dot product of embedding and weights, classify with a strict `> 0.25` threshold, and write `id,score,prediction` with the score via `std::fixed << std::setprecision(4)`. Compile with `g++ -O3 -o /home/user/predictor /home/user/predictor.cpp` (or your chosen name) and actually run it against `/home/user/data.csv` and `/home/user/weights.csv` to produce `/home/user/predictions.csv` — don't just compile and stop.
