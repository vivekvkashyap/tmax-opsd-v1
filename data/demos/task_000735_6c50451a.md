Work through the four checks systematically rather than eyeballing files. Permissions: `find /home/user/webapp -type f -perm -o+w` to find world-writable files reliably (don't just `ls -l` by eye). Integrity: parse `/home/user/manifest.txt` line by line, recompute `sha256sum` for each corresponding file in `/home/user/webapp/`, and compare against the manifest's recorded hash — collect every mismatch. Vulnerability analysis must be scoped only to files that failed the checksum check (per the task's explicit instruction) — for each mismatched file, grep for injected XSS markers (e.g. `<script>` tags, inline event handlers, `fetch`/DOM sinks with unsanitized-looking content) in HTML/JS files, and for Python files grep for unsanitized use of `os.system`, `subprocess`, or `eval` combined with concatenated/formatted external input (not just any use of those functions — confirm the input is user-controlled and unsanitized). Build the JSON report exactly matching the required schema with absolute paths in each array, using `[]` for any category with no hits, and validate it parses with `python3 -m json.tool` before finishing.