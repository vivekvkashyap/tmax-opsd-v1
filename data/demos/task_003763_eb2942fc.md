Work through the build chain in order, testing each stage before moving to the next. First fix `Makefile`: it must use actual tab characters (not spaces) to indent recipe lines — check with `cat -A Makefile` to spot space-indented lines, then rewrite recipe lines with real tabs, add `-fPIC -shared` to the compile/link flags, and make sure the final `.so` is written to `lib/libfib.so` (create the `lib` dir if needed). Run `make` standalone and confirm `lib/libfib.so` exists before touching Go code. Then fix the `#cgo CFLAGS`/`#cgo LDFLAGS` lines in `main.go` to reference `${SRCDIR}/include` and `${SRCDIR}/lib` (matching the actual project layout) instead of the wrong placeholder directories. Build with `go build` (CGO enabled by default) and before running, export `LD_LIBRARY_PATH=$(pwd)/lib:$LD_LIBRARY_PATH` (or the absolute path to `lib/`) so the dynamic linker can find `libfib.so` at runtime — a missing LD_LIBRARY_PATH is the classic reason a CGO binary that compiled fine fails to start. Run the resulting binary and confirm `/home/user/result.txt` is created; if it's empty or the program panics, check the LD_LIBRARY_PATH and include/lib paths again rather than touching the already-correct Fibonacci logic.
