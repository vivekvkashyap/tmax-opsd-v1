Work through the four parts literally and verify each with a direct check rather than assuming it worked. (1) Write `/home/user/harden_net.sh` containing the exact `ip` invocations for a blackhole route on `203.0.113.0/24` and setting `eth0`'s MTU to `1400` (you won't execute it as non-root, just author it correctly), then `chmod +x` it. (2) `mkdir -p /home/user/secure_configs` and write the two conf files with `printf '%s' "PORT=8080" > app.conf` style (not `echo`, and no `-e`) so no trailing newline sneaks in — verify with `xxd` or `wc -c` that the byte count matches the string length exactly. (3) Scaffold a Rust binary (`cargo new /home/user/fs_monitor`), add a sha2 crate (or shell out to `sha256sum`) plus a JSON serializer (serde_json or manual string building), have it read every file in `secure_configs/`, hash each, and emit the `files` array explicitly sorted by `name` (sort in code, don't rely on directory read order) to `/home/user/audit_report.json`; `cargo build --release` then run the binary once by hand to produce the report. (4) Add a crontab line for `*/15 * * * *` invoking the release binary's full path with `2> /home/user/fs_monitor_error.log` and no stdout redirection, via `crontab -l | { cat; echo "..."; } | crontab -` (preserving any existing entries).