Treat `/app/linreg_ref` as a black-box oracle you can only observe via stdin/stdout — feed it small hand-crafted command sequences and diff its output against your evolving Python implementation rather than guessing its internals. Rewrite `/home/user/linreg.py` to maintain running sums `n, sum_x, sum_y, sum_xx, sum_xy` updated incrementally on `ADD`/`REMOVE_OLDEST` (O(1) per op, using a `collections.deque` to track insertion order for oldest-removal) instead of recomputing from a stored point list. For `PREDICT x`: guard `n < 2`, then compute `D = n*sum_xx - sum_x*sum_x`, guard `D == 0`, then compute the numerator/denominator pair for `y = sum_y/n + (n*sum_xy - sum_x*sum_y)*(n*x - sum_x) / (n*D)` — combine into a single `Fraction(num, den)` and let Python's `fractions.Fraction` auto-reduce to lowest terms; print as `numerator/denominator` unless denominator is 1 (then just numerator), keeping the sign on the numerator (`Fraction` already normalizes this). For `ci_test.sh`, generate a random command file (respecting valid-only inputs, e.g. always emitting REMOVE_OLDEST only when points exist to avoid ERROR-vs-ERROR ambiguity is fine either way since both should print ERROR), run both programs, and `diff` their stdout.
