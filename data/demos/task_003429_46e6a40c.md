Build this in three clear stages and test each before moving on. First write `/home/user/process.rs` using only `std`: parse the CSV manually (split on commas, `parse::<f64>()`), compute `x = v1+v2`, `y = v3+v4+v5` per row, then find the row with id `target_run`, compute Euclidean distance from every other row's (x,y) to its (x,y), skip comparing target_run to itself, and print only the id with the minimum distance (no extra whitespace/newlines beyond one). Compile with `rustc /home/user/process.rs -o /home/user/process` and verify it runs against the given CSV before writing `/home/user/closest.txt` with the raw output. For the reproducibility script, actually compile the binary fresh, run it on the original CSV, then use `shuf` to produce `/home/user/shuffled.csv`, run the binary again on that, and diff the trimmed outputs — print `PASS` only on an exact match, `FAIL` otherwise. A common mistake is using floating-point comparisons or sort order that isn't truly order-invariant (e.g. relying on first-row special-casing or `<=` vs `<` tie-breaks) — make sure ties are broken deterministically (e.g. by smallest distance strictly, first minimum found in original iteration order) so shuffling never changes the result.