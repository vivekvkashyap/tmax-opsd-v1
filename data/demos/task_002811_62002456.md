Reverse-engineer `/app/legacy_evaluator` by black-box testing before writing any server code: pipe in a range of expressions covering all four allowed operators, nested parentheses, negative results, and repeated variables (e.g. `echo '{"expr": "...", "data": {...}}' | /app/legacy_evaluator`) and compare against your own mental/hand computation to confirm it follows standard `+ - *` precedence with parentheses (no division, so it's simpler than full arithmetic) — also probe what it does with a missing variable, since the task says it likely defaults to 0. Define `evaluator.proto` with the exact message/service shapes specified (`EvalRequest{string expr, map<string,int32> data}`, `EvalResponse{int32 result}`, service `Evaluator` with rpc `Evaluate`), then generate Python stubs with `python -m grpc_tools.protoc --python_out=. --grpc_python_out=. -I. evaluator.proto`. Implement expression evaluation in `server.py` yourself with a small recursive-descent or shunting-yard parser respecting precedence (`*` before `+`/`-`, parentheses override) rather than relying on Python's `eval` on untrusted input in a way that changes precedence semantics — substituting variables from the `data` map (defaulting missing ones to 0) before or during evaluation. Start the gRPC server bound to `[::]:50051` and leave it running in the background. Write `test_pbt.py` using `hypothesis` to generate random small expressions/values, run both the legacy binary via `subprocess` and your gRPC stub, and assert equality, iterating on your parser until they always match.