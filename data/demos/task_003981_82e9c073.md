Start by OCR'ing `/app/config.png` (e.g. `tesseract /app/config.png -`) rather than assuming values — read out the exact TZ string and the MAGIC_BYTE integer as printed, double-checking the OCR output looks like a sane timezone name and a plausible byte value (0-255) since OCR can misread digits/letters. Write `env.sh` exporting `TZ` to the recovered value. For `processor.c`, implement the filter as a byte-at-a-time loop over stdin (`getchar`/`fread` in binary mode) that passes ASCII digit bytes through unchanged and XORs everything else with the recovered MAGIC_BYTE, flushing stdout at the end — build it with `gcc` and treat it as a raw byte stream (avoid text-mode line buffering assumptions, since the fuzz test uses arbitrary binary data including bytes that look like newlines/EOF markers). For `supervisor.sh`, source env.sh, `mkfifo` the pipe idempotently (check existence first), then loop `./processor < pipe >> processed.log; ` forever so any crash of processor triggers an immediate restart. Since the C program will be fuzz-tested bit-for-bit against a hidden oracle, self-test it against your own hand-built XOR/digit-passthrough logic on random binary inputs before finishing.
