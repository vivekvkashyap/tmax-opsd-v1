Split this into schema recovery and validator implementation, and get the schema right first since everything downstream depends on it. Extract frames from schema_recording.mp4 with ffmpeg (e.g. `ffmpeg -i schema_recording.mp4 -vf fps=1 frame_%03d.png`) and run OCR (`tesseract`) on the frames to recover the exact CREATE TABLE definitions shown — note down each table's column names/types and, critically, which column is a self-referencing foreign key (a column in a table that references that same table's primary key). Don't guess the schema from the task description; read it off the video. Then write the C program (`/home/user/filter_backup <file>`): parse each JSONL line (a C JSON library like cJSON, or a minimal hand-rolled parser since the objects are flat) into a table name plus key/value pairs, and reject immediately if the key set for that `_table` doesn't exactly match the recovered schema (extra or missing keys). For cycle detection, build a directed graph from the self-referencing FK column to the primary key within each table's rows and run a cycle check (DFS with a visiting/visited color marking, or simple iterative parent-chain walk with a seen-set) — any cycle found means REJECT. Print exactly `ACCEPT`/`REJECT` with matching exit codes 0/1, and test your binary against a few hand-built valid and cyclic JSONL files before trusting it on the hidden corpus.