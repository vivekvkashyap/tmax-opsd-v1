Build `/home/user/curator.py` as a directory watcher (either `watchdog`'s `Observer`/`FileSystemEventHandler` watching for `.zip` creation, or a simple polling loop scanning `/home/user/incoming/` every second or two) that, on detecting a new zip, does its work in a fresh temp directory (`tempfile.mkdtemp()`), NOT directly under `/home/user/published/`: extract the outer zip there, extract `inner.tar.gz` from it, parse `metadata.ini` with Python's `configparser` and dump it to `metadata.json` via `json.dump` preserving the section->key->value nesting exactly, then move/copy `binary.elf`, `signature.bin`, and `metadata.json` into a staging subdirectory named `<zipbase>_curated` still inside the temp area. Only then perform the atomic step: `os.rename(staging_dir, '/home/user/published/<zipbase>_curated')` (or `shutil.move` if temp and published share a filesystem) so the target directory appears fully-formed in one filesystem operation, never partially populated in place. After starting the script in the background, copy `/home/user/hold/release_1.zip` into `/home/user/incoming/`, poll/wait for the published directory to appear, then copy its `metadata.json` to `/home/user/success.log`.