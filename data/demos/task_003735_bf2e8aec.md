Split this into extraction and algorithm work, and get the extraction exactly right before writing the aggregator. Use `ffmpeg` to pull the subtitle stream out of `/app/incident_log.mp4` (e.g. `ffmpeg -i incident_log.mp4 -map 0:s:0 subs.srt`), then parse the `.srt` text, stripping timestamps/sequence numbers/blank lines, to recover the CSV lines that were flashed on screen — reassemble them in timestamp order and de-duplicate if lines repeat across overlapping subtitle cues. Write the reconstructed data with header `node_id,parent_id,metric_value` to `/home/user/extracted_graph.csv`, treating an empty or `0` parent_id as a root. For `/home/user/aggregate_graph.sh`, build a mapping of node -> children and node -> own metric_value, then compute each node's total via post-order/recursive summation over descendants (memoize or process leaves-to-roots to avoid recomputation, since depth can reach 50 and the graph can have hundreds of nodes) — a plain per-node walk that re-traverses the whole subtree every time will be correct but must still terminate promptly; an approach using associative arrays in awk or a small recursive function keyed by node_id both work. Output exactly `node_id,total_metric` header plus rows sorted numerically ascending by node_id. Test your script against several hand-built small CSVs with multi-level chains before trusting it on the extracted data, since the real evaluation will fuzz it against many generated graphs.
