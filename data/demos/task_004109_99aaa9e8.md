Take the backup first (`mkdir -p /home/user/app/backups && cp /home/user/app/start_services.sh /home/user/app/backups/start_services.sh.bak`) before editing anything, so the original is preserved regardless of what you do next. Fix the race by replacing the simultaneous background-launch with a polling wait: start `data_initializer.py` in the background, then loop `while [ ! -f /home/user/app/shared_data.json ]; do sleep 0.5; done` before starting `backend_api.py` — this is idempotent and works regardless of how long initialization actually takes, unlike a fixed `sleep N`. For the health check, write a Python script using `urllib.request` or `requests` wrapped in a broad `try/except` (covering connection refused, timeout, and non-200 status) so a failure never crashes the script itself — on success write `[OK] Service healthy\n`, on any failure write `[ERROR] Service unreachable or failing\n`, appending to `/home/user/app/logs/health.log` (create the `logs` dir first). Test both paths manually: run the fixed `start_services.sh`, wait, run `health_check.py` and check for `[OK]`; then kill the backend and rerun to confirm `[ERROR]` appears. Finally add the cron line with `(crontab -l 2>/dev/null; echo "* * * * * /usr/bin/python3 /home/user/app/health_check.py") | crontab -`.