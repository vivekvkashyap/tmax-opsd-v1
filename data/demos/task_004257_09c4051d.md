First transcribe `/app/data_request.mp3` — use a speech-to-text tool available in the environment (e.g. `openai-whisper` via pip, or another ASR library/CLI) rather than guessing constraints; play back or print the transcript and read it carefully to identify exactly which field(s) and value(s) it says to filter on (there may be more than one condition, e.g. a status field and a region field — capture both precisely as stated). Then load `/home/user/transactions.csv` into a local SQLite database (e.g. `sqlite3` via Python's `sqlite3` module, or pandas' `to_sql`), create an index on the filtered columns to keep the query plan efficient on the large file, and run a single SQL query that applies a `WHERE` clause matching the transcribed constraints exactly (mind case-sensitivity and exact string matching against the CSV's actual values) and a `GROUP BY product_category` with `SUM(amount) AS total_sales`. Write results to `/home/user/summary.json` matching the required schema exactly (`categories` list of `{category_name, total_sales}` objects), keeping full floating-point precision from the SQL sum since the grader checks numeric accuracy via MSE — avoid premature rounding in your aggregation code.