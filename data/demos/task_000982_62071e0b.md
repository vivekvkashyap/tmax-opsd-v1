Start by inspecting `/app/evidence.wav` with a spectrogram/tone-analysis tool (e.g. `sox`, `audacity`-style analysis, or a Python script using `scipy`/`numpy` FFT) to detect the DTMF/FSK tone sequence and decode it into the underlying hex/ASCII characters — do this programmatically rather than guessing, since the payload is a hex-encoded string followed by a SHA-256 checksum appended after it. Once you have the raw decoded bytes, split off the trailing 64 hex characters as the SHA-256 digest and treat the rest as the payload; write a small script (Python or the C++ program itself) to compute SHA-256 over the payload and compare it against the extracted digest before trusting the decoded string. In `/home/user/decoder.cpp`, implement (or link) a SHA-256 implementation, verify the checksum, and only then compute Shannon entropy over the decoded string using the standard formula `-sum(p_i * log2(p_i))` for each distinct character's probability — use double precision and avoid rounding intermediate values. Write the final entropy as a plain float to `/home/user/entropy.txt`; the grader compares it to the true entropy with tolerance 0.01, so get the exact decoded string right first since entropy is very sensitive to any character transcription error.