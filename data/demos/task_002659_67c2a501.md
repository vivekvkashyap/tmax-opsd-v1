This task has three independent phases — work them in order and verify each before moving on, since later phases depend on artifacts from earlier ones only loosely. Phase 1: extract frames from `/app/test_run.mp4` with `ffmpeg -i test_run.mp4 -vf fps=1 frame_%04d.png` (or similar), then open `/home/user/tools/analyzer/main.cpp` and look for a classic memory-safety bug — an off-by-one or unchecked buffer write when processing pixel data (e.g. writing past an array bound for certain image sizes); fix the bound check, recompile, and run it across all frames, capturing which frame number the tool reports `STATE=UI_ERROR_MODAL` for, then write that integer to `/home/user/error_frame.txt`. Phase 2: in `/home/user/dispatcher/main.go`, find the goroutine that appends results into a shared slice without synchronization — protect the shared slice with a `sync.Mutex` around the append (or route results through a channel collected by a single goroutine instead), then confirm with `go test -race`, since the race detector will fail silently-but-flakily on an unguarded append otherwise. Phase 3: build a Rust CLI in `/home/user/manifest_validator` that parses JSON, walks all `asset_path` fields checking for `../` sequences or paths under `/etc/` or `/var/`, and detects V1/V2 schema mixing (arrays vs. objects) — reject with nonzero exit on any violation. Compile with `cargo build --release` and self-test against a few hand-written good/bad manifests before trusting it against the hidden corpora.
