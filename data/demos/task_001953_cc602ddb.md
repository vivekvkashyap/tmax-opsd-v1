Design this as a clear three-stage C++ pipeline: parse -> reshape -> stratify+log. Use std::regex to split LogText on `|`, trimming whitespace around each segment, then match each segment against a pattern like `(\w+): \[(\w+)\] (\w+) - (\S+)` to pull Day/ProductID/Sentiment/ErrorCode; segments that don't match the pattern must be silently skipped (they still count toward "input rows" but not "long records"). Write the long-format rows to /home/user/output/long_format.csv with the exact header given. For stratification, group by (Region, Sentiment) using e.g. a map keyed on a pair/string, sort each group's records by UserID then Day lexicographically (plain std::string comparison, not numeric), and take the first 2 (or fewer) per stratum — do this as a distinct pass over the already-produced long records, not during initial parsing. Track three counters (raw input rows excluding header, total valid long records, total sample rows) and append exactly the three specified `[INFO] ...` log lines with the real counted integers. Use `std::filesystem::create_directories` before writing outputs, and compile with `g++ -std=c++17`.