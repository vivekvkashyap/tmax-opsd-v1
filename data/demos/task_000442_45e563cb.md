Build the two scripts as separate, testable pieces before wiring them together. For `operator.py`: parse `ip route` output looking for the line starting with `default via <ip>` (split on whitespace and take the token after "via"), falling back to `127.0.0.1` only if no such line exists — don't assume a fixed line format across systems. Ping with `subprocess.run(["ping","-c","1", ip], ...)` and check the returncode directly rather than parsing stdout text. Compute directory size in KB with `du -sk` (or sum `os.path.getsize` over `os.walk` and divide by 1024, rounding up with `math.ceil`) — be consistent about which KB convention you use since it must match what the manifest's `max_storage_kb` expects. Parse `manifest.json` with the `json` module. Build the exact log line with an f-string matching the required format precisely (spacing, `|` separators, labels), and always use append mode (`"a"`) when writing to `operator.log` so repeated runs accumulate rather than overwrite. For `supervisor.sh`, loop 3 times calling `python3 /home/user/operator.py`, with `sleep 1` between iterations only (not after the last), then chmod it executable and run it once.