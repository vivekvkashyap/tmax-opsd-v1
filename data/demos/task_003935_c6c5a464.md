Structure the C program in three clear passes over the same in-memory graph. Pass 1 (ingest): read `raw_routes.csv` line by line, split on commas, parse NodeA/NodeB/Latency as integers, and build an adjacency structure (arrays or adjacency list) while tracking the full set of distinct node IDs seen in either column — don't assume nodes are numbered 0..N-1 contiguously without gaps, discover the actual node set from the data since correctness must hold for arbitrary graphs. Pass 2 (shortest path): implement Dijkstra from node 0 over the undirected weighted graph (add edges both directions since it's undirected); any node not reached should get -1, not 0 or infinity, in the output. Pass 3 (degree): count each node's edge incidences (each CSV row increments degree for both endpoints by 1, so parallel edges between the same pair each count separately unless the data guarantees no duplicates). Write `processed_nodes.csv` with the header `NodeID,ShortestPathFrom0,Degree`, one row per distinct node, sorted ascending numerically by NodeID (not lexicographically as strings). Compile with `gcc`, run it, and diff your output file's exact bytes against what the logic should produce by tracing a couple of nodes by hand.
