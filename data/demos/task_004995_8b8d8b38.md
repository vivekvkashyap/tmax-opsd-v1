Treat the binary as a black box you characterize empirically, then confirm with static inspection. First, craft small hand-written WAL inputs covering each OP value (ARCHIVE, SKIP, DELETE, COMPRESS), records missing the BYTES line, records with BYTES=0, and multiple records in one stream, and pipe each through `/app/backup_wal_parser` via stdin to observe stdout precisely (watch for exact delimiter, presence/absence of trailing newline, and whether missing BYTES produces a line at all or defaults to something). Cross-check hypotheses with `strings /app/backup_wal_parser` for format strings (e.g. printf patterns) and `objdump -d` to see the arithmetic on the BYTES value (look for a shift/divide suggesting kilobyte conversion) and the string comparisons that decide which OP values are dropped vs kept. Once you're confident in the field-by-field mapping and the divide/rounding behavior (floor vs round), implement a script (bash awk/sed or Python) that parses BEGIN_WAL_RECORD...END_WAL_RECORD blocks line-by-line, tracks PATH/OP/BYTES per record, resets state at each BEGIN, and emits the CSV line only for the kept OP values. Test bit-exact equivalence by diffing your script's output against the binary's output on the same generated fuzz inputs, including edge cases like blank lines between records and records with fields in permuted order.
