Break this into independently testable pieces before combining into one C program. Traversal: walk the tree with `lstat`/`opendir`+`readdir` (or `nftw` with `FTW_PHYS`) so symlinks are never followed as directories — `FTW_PHYS`/checking `S_ISLNK` before recursing is what avoids the infinite loop, no cycle-detection bookkeeping needed. Decompression: for each `*.json.gz` match, stream through zlib's `gzopen`/`gzread` (or inflate) to get raw text, then split on newlines and do a light JSON scan for `"is_research_target":true` (a small hand-rolled JSON parser or just careful string/key matching is fine given the controlled input) to filter retained objects. OCR: extract the token from `/app/dataset_auth.png` using `tesseract dataset_auth.png stdout` (install if needed), trim whitespace from the result, and treat that recovered string — not any assumed value — as the bearer token to require. HTTP server: a minimal blocking accept-loop with `socket`/`bind`/`listen` on 127.0.0.1:8080 is sufficient; parse the request line and headers manually, check the `Authorization` header against the OCR'd token before returning 401 vs 200, and serialize the retained JSON objects into an array for the body. Test auth and traversal separately with `curl` and small crafted directories before trusting the whole pipeline.
