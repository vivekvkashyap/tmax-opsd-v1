Build the shared library first: `gcc -fPIC -shared -DCONDENSE_ZERO -o /home/user/libpoly.so math_utils.c`. Confirm the `-D` flag actually reaches the conditional `#ifdef CONDENSE_ZERO` block by inspecting the C source before compiling — a naive build without the macro will leave zero-coefficient terms in place after merging, silently breaking downstream diffs. In the Python wrapper, define the `ctypes.Structure` with fields in the exact order and types of the C struct (`coeff` then `exp`, both `c_int`) so the memory layout matches the ABI; mismatched field order or using the wrong ctypes width will corrupt data across the FFI boundary. Load terms into a `ctypes` array (e.g. `(PolyTerm * n)(*terms)`), pass a pointer plus a mutable `c_int` for `n_terms` (via `ctypes.byref`), and set `restype`/`argtypes` on the function. After calling `sort_and_merge` on both polynomials, build the subtraction by aligning matching exponents (default missing side to 0), drop zero-coefficient results, sort descending by exponent, then write `coefficient,exponent` lines to the result file. Test by running the script and inspecting the output file for reasonableness rather than assuming correctness.
