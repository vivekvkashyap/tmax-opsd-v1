Work through the pipeline stage by stage, verifying each before the next depends on it. First, extract `/app/shdoc-0.2.tar.gz`, inspect the `shdoc` script for the hardcoded `AWK=".../gawk"` path (or similar) and patch it in place with `sed -i` to point at the system's actual `awk`/`gawk` binary (find it with `which awk`) — confirm by running `shdoc` on a trivial script afterward instead of assuming the fix worked. Since you lack sudo, install with a user-local prefix (e.g. `make install PREFIX=$HOME/.local`) and add `$HOME/.local/bin` to `PATH`. Second, verify `legacy_docs.tar.gz` against `legacy_docs.md5` with `md5sum -c`, and only extract if it matches. Third, parse `mapping.conf` line-by-line splitting on the first `:` into old path and new path, creating parent directories (`mkdir -p`) before moving each file into `/home/user/organized_docs/`. Fourth, convert `[[Page Name]]` wiki links to `[Page Name](Page_Name.md)` with a regex-based `sed`/`awk`/Python pass over every `.md` file — this is the graded metric, so handle multi-word names, multiple links per line, and don't touch already-correct `[text](link)` markdown; test on a sample file and grep for leftover `[[` before trusting it at scale. Finally generate `deploy_api.md` with your fixed `shdoc`, then tar the whole `organized_docs/` tree to `/home/user/final_docs.tar.gz`.