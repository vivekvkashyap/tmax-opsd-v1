Build this as a pandas pipeline with explicit stages. Read each CSV with its correct `encoding=` (`utf-8`, `iso-8859-1`, `shift_jis`) so multibyte characters aren't mangled, then `pd.concat` them. Reshape wide-to-long with `pd.melt(df, id_vars=['user_id','timestamp'], value_vars=['q1_feedback','q2_feedback','q3_feedback'], var_name='question', value_name='feedback')` then strip the `_feedback` suffix off the `question` values; drop rows where `feedback.str.strip()` is empty. For dedup: strip whitespace from feedback, compute `hashlib.sha256(text.encode('utf-8')).hexdigest()` per row, then group by that hash and within each group sort by `(timestamp, user_id)` ascending and keep only the first row — a naive `drop_duplicates` keeps whichever pandas encounters first in current row order, not chronological order, so you must sort before deduping or use `groupby(...).apply` to pick the min explicitly. Finally sort the surviving rows by `(question, timestamp, user_id)`, write `clean_data.csv` with the exact column order specified and UTF-8 encoding, and write `summary.json` with per-question counts of surviving rows using `json.dump`.