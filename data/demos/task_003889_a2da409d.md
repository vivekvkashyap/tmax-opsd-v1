First recover the Risk Threshold T: extract the frame count from `/app/audit_logs.mp4` (e.g. `ffprobe -v error -count_frames -select_streams v:0 -show_entries stream=nb_read_frames -of csv=p=0 /app/audit_logs.mp4`, or decode all frames with `ffmpeg` to a temp dir and count the files) — that integer is T, use it as a literal in your script rather than hardcoding a guess. Then write the pure-Bash/awk pipeline carefully to avoid the Cartesian-product bug: read the edge file, first deduplicate exact `(SourceNode, DestinationNode)` pairs with `sort -u` (do NOT just pipe raw lines into an awk aggregation that joins on source alone, which silently multiplies counts), then use `awk` to count occurrences per SourceNode from the deduplicated pairs, filter rows where the count is strictly greater than T, and finally pipe through `sort -k2,2nr -k1,1` to get descending-by-count with ascending-alphabetical tie-breaking. Test your script by hand against a small crafted edge list with known duplicate edges to confirm counts don't inflate before relying on the fuzz suite. Keep everything in Bash/awk/sort/uniq — no Python or Perl.
