Work through the four phases in order since each depends on the last. Phase 1: `openssl req -x509 -newkey rsa:2048 -nodes -keyout server.key -out server.crt -days 365 -subj '/CN=localhost'` into the certs directory. Phase 2: `pip install -r requirements.txt`, then read `app.py` to see how it builds the upload save path from the client-supplied filename (vulnerable because it joins the upload dir with the raw filename without stripping `../`) — launch it in the background, and craft a curl exploit using `-k` (self-signed cert) with a multipart file field whose filename payload uses `../` traversal segments so the resolved path lands exactly on `/home/user/vuln_app/hacked.txt` (count directory levels precisely, one `../` per level to escape `uploads/`). Phase 3: after confirming the file was written, kill the Flask process, grep `access.log` for the line containing the traversal payload's filename, and extract just the bracketed timestamp substring into timestamp.txt. Phase 4: grep the sudoers.d files for the line granting `NOPASSWD` on `/usr/bin/find` and extract that line's leading username into privesc_user.txt.
