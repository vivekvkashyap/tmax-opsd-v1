Build this as a clear three-stage pipeline and keep the server running afterward. Stage 1: load `/app/data.wav` with librosa at its native sample rate, slice into consecutive non-overlapping 1-second chunks (drop or handle any short trailing chunk consistently), and for each chunk compute `librosa.feature.mfcc(..., n_mfcc=13)` then average across time frames to get one 13-D vector per chunk — this is your embedding table, keep it in memory alongside chunk indices. Stage 2: split chunks into first-half (Group A) and second-half (Group B, extra chunk goes to A if odd), take MFCC coefficient index 0 from each chunk in both groups, and run `scipy.stats.ttest_ind(A, B, equal_var=False)` for Welch's t-test; separately compute the 95% CI for the mean difference (A-B) using the standard two-sample CI formula with Welch-Satterthwaite degrees of freedom. Stage 3: stand up a minimal Flask/FastAPI app bound to `127.0.0.1:8080` exposing `GET /stats` (returns the precomputed t_stat/p_value/ci_lower/ci_upper) and `POST /retrieve` (computes Euclidean distance from the query chunk's embedding to all others, sorts ascending, excludes self, returns top_k ids). Launch it as a background process so it survives after your turn ends, and verify with a local curl before finishing.
