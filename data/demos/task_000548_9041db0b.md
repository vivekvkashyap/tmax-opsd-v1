First get the environment plumbing right: run `/app/start_services.sh`, then populate `/home/user/config.env` with the exact host/port/db/user/password/redis values given in the task description, and confirm the Python script can actually connect (e.g. a quick `psql`/`redis-cli` ping or a small connection test) before touching the SQL. Next, inspect the real schema — connect with `psql` and run `\d datasets` and `\d dataset_dependencies` (or query `information_schema`) rather than assuming column names; use the oracle only as a black-box reference for behavior, running it with a few different `--dataset-id`/`--max-depth` combos and diffing your script's output against it to check correctness, not for its internals. Rewrite the query in `dataset_query.py` as a recursive CTE: base case selects the starting dataset id at depth 0 (only if `is_active`), recursive case joins `dataset_dependencies` on `source_id` to reach `target_id` while incrementing depth and stopping at `--max-depth`, filtering `is_active = true` throughout. Deduplicate with `SELECT DISTINCT`, `ORDER BY id ASC`, then apply `LIMIT`/`OFFSET` in SQL (not Python) so pagination is exact, and output as a compact JSON array of integers. Iterate by diffing against the oracle across several random parameter sets until outputs match exactly.