Fix the vendored library before writing anything that depends on it. In the Makefile, swap `-std=c++11` for `-std=c++17` and rebuild `libtinyjson.a` to confirm it links cleanly. In `parser.cpp`, find where `\u` escapes are scanned — the bug is almost certainly treating the 4 hex digits as raw bytes or truncating instead of parsing them as a code point and re-encoding to UTF-8 (multi-byte sequences per the code point's range: 1 byte if <0x80, 2 bytes if <0x800, 3 bytes otherwise, watching for surrogate pairs if the fuzz corpus includes codepoints beyond the BMP); write a small standalone test with strings like `©` and `€` to confirm correct UTF-8 bytes come out before moving on. For `mask_loc.cpp`, apply the given LCG-style transform to `user_id` using unsigned 64-bit arithmetic exactly as specified (mod `4294967296ULL`), and truncate `confidence_score` toward zero to the nearest `0.1` using integer scaling (`floor(score*10)/10`, not naive float comparison) to avoid rounding surprises. Match the output key order, spacing, and no-trailing-newline requirement exactly, then wire the `build`/`run` Makefile targets to compile against the fixed static library.
