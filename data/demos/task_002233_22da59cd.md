In `/home/user/workspace/knn_pipeline.c`, locate `normalize_data(int train_size)` — the leak is that both the mean loop and the squared-diff loop iterate `for (int i = 0; i < N; i++)` when computing `sum`/`sq_diff_sum`, using test-set values to compute the normalization stats. Fix only those two accumulation loops to iterate `i < train_size` instead of `i < N` so mean/std are computed exclusively from training rows — but leave the final normalization-application loop (`data[i][j] = (data[i][j] - mean) / std`) iterating over all `N` rows, since the computed train-only stats must still be applied to test data too. Compile with `gcc -O3 -lm /home/user/workspace/knn_pipeline.c -o /home/user/workspace/knn_pipeline_fixed` and actually run it from the `workspace` directory (it opens `dataset.csv` relatively) to capture the real printed "Test Accuracy: X.XXXX" line. Write `/home/user/results/report.json` with `"leakage_fixed": true` and `"corrected_test_accuracy"` set to exactly the float your program printed — parse it programmatically from the run output rather than transcribing by eye, to avoid rounding mistakes.
