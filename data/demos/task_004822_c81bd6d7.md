First recover the redaction salt from `/app/policy_memo.png` using `pytesseract` inside your script (or a quick standalone check first) — search the OCR'd text for the line matching `MANDATORY REDACTION SALT: <value>` and extract everything after the colon, trimming whitespace; don't hardcode a guessed value. Then implement `/home/user/audit_filter.py` as a pure JSON transform, applying rules in this order to avoid conflicts: (1) recursively scan `payload`/`headers` string values for `<script>` or `${jndi:` (case-insensitive substring match) and replace matched values entirely with `[EXPLOIT_BLOCKED]`; (2) if `payload.password` exists and is a plaintext string (i.e., NOT matching a 64-hex-char lowercase pattern), replace it with `[REDACTED: <salt>]`; (3) check `headers` for a `Content-Security-Policy` key whose value contains `default-src 'self'` — if missing or absent, add a top-level `COMPLIANCE_WARNING: MISSING_CSP` key without altering anything else. Preserve key order/structure and untouched fields exactly for clean inputs — write output via `json.dump` from the same parsed dict, only mutating fields that trigger a rule, so genuinely clean logs round-trip byte-for-byte in structure. Test against both the clean and evil corpora before considering it done.