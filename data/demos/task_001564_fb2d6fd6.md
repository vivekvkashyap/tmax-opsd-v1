Read the filtering rules out of `/app/graph_rules.png` first — run `tesseract /app/graph_rules.png stdout` (or view it) to OCR the exact criteria, since guessing the thresholds will silently break correctness; apply whatever text you recover as filter predicates on `wait_time_ms`, `src_tx` prefix, and `tx_type` before building the graph. For correctness, build a directed adjacency list from the filtered edges in `/app/transactions.csv` and find all nodes on any cycle — the efficient way is Tarjan's (or Kosaraju's) strongly-connected-components algorithm in one linear pass, keeping any SCC of size > 1 (or a self-loop) as "in a deadlock"; this replaces the naive per-node DFS-with-reset approach that is O(V*(V+E)) and is why the reference implementation is slow. Write the deduplicated, alphabetically sorted transaction IDs to `/home/user/deadlocks.txt`. Compile with `-O2` at minimum for the C++17 build, and before worrying about speed, verify output correctness against your own understanding of the graph structure (small hand-checkable cases) since accuracy is checked first and the speedup only matters once results match exactly.