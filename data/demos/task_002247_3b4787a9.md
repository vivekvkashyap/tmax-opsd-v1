Work through this in the given order rather than jumping to the benchmarking script. First inspect the vendored package's Makefile literally byte-for-byte — build-system "typo" bugs (a variable name that almost matches another) are easy to miss by skimming; diff the `PREFIX` variable used in the target against the make invocation `make install PREFIX=...` to find the mismatch, then fix only that line. Next check the shipped shell script for non-executable permissions and Windows line endings — use `file` to confirm CRLF, then `dos2unix` (or `sed -i 's/\r$//'`) and `chmod +x` before relying on it; a shebang script with CRLF fails silently/oddly, so verify by actually running it after fixing. Only after `make install PREFIX=/home/user/.local` succeeds should you run the tool with `ENFORCE_STRICT_SCHEMA=1` to produce the CSV. For the Bayesian script, implement the counting and posterior-mean formula exactly as specified using `awk` (matching by exact artifact_id and `latency_ms <= threshold`), then sanity-check your script's output against a few manually computed cases before assuming correctness, since it will be fuzz-tested for bit-exact equivalence.