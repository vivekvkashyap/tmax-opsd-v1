This is a black-box reverse-engineering task graded by fuzz equivalence, so reason from observed behavior rather than assuming internals. First inspect what's available: check if `/app/priv_checker` is truly stripped-only or if you can still learn its structure via `strings`, `file`, `objdump -d`, or `ltrace`/`strace` on sample runs — but the fastest ground truth comes from black-box probing: run the binary directly with a range of crafted argv[1] values (missing arg, garbage bytes, valid-base64-but-bad-UTF8, valid UTF-8 with wrong pipe count, valid 3-field strings with varying CERT_SN/CSP_RULE/PRIV_LEVEL) and record exact stdout, stderr, and exit code for each, to infer the argument-count check, base64-decode-failure path, UTF-8-decode-failure path, the `CERT_SN|CSP_RULE|PRIV_LEVEL` pipe-delimited format, and how `/app/certs/root.pem` is consulted (e.g. count of "CERTIFICATE" lines used as a modulus). Reproduce each branch's exact message text and exit code faithfully in the Python port — mismatched exit codes or stderr wording on edge cases (bad padding, non-UTF8 bytes, wrong pipe count) are the most likely source of fuzz failures. Add a shebang and `chmod +x` the script, then self-fuzz it against the real binary on many random inputs before finishing.