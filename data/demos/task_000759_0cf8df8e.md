Read the existing etl.go and source.csv first to see the exact bug shapes before editing. Fix in this order: (1) wrap timestamp parsing in `time.Parse(time.RFC3339, ts)` and check the error — on failure, append the row's event_id to etl_errors.log (open with os.O_APPEND|os.O_CREATE|os.O_WRONLY) and `continue` to the next row instead of panicking; (2) apply the user_id sampling filter (`strings.HasSuffix(userID, "A") || strings.HasSuffix(userID, "B")`) before the timestamp check ordering matters only in that sampled-out rows must never touch the error log, so decide filter order carefully against the spec; (3) once parsed, derive hour via `t.Hour()` and is_weekend via `t.Weekday() == time.Saturday || t.Weekday() == time.Sunday`; (4) change the INSERT to `INSERT INTO events (...) VALUES (...) ON CONFLICT(event_id) DO UPDATE SET ...` (or `INSERT OR REPLACE INTO events ...` since this is sqlite3) so reruns are idempotent — the pre-seeded duplicate row for evt_001 in the fixture is there specifically to test this. After running the fixed script, export with `sqlite3 -header -csv /home/user/data.db "SELECT * FROM events;" > /home/user/final_export.csv`. Test by running the script twice to confirm no crash and no duplicate-key error.
