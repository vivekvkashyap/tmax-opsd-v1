Work through this as five independent subsystems and verify each before moving on. (1) `mkdir -p /home/user/frames` then `ln -s /app/camera1.mp4 /home/user/active_feed.mp4` — use a relative-target-correct symlink so `readlink` returns the exact source path. (2) Extract frames with `ffmpeg -i /home/user/active_feed.mp4 -vf fps=1 /home/user/frames/frame_%04d.jpg`; let the process exit naturally, don't background it, and check the resulting frame count matches the video's actual duration (probe with `ffprobe` if unsure) rather than assuming a fixed number. (3) Start `socat TCP-LISTEN:8080,fork,reuseaddr TCP:localhost:9090 &` (or `disown`) so it persists in the background. (4) For the validator, read JSON via `sys.stdin`, wrap parsing in try/except for invalid JSON, use a strict alphanumeric regex (`^[A-Za-z0-9]{1,20}$`) for device_id (this alone blocks shell metacharacters), and anchor the tz regex exactly as specified so `../` sequences fail the pattern naturally; append accepted/rejected raw JSON to the two log files (create `/home/user/logs/` first) and exit 0/1 accordingly — test against both benign and injection-style payloads yourself before considering it done. (5) Write a logrotate stanza for `/home/user/logs/*.log` with `daily`, `rotate 7`, `compress`, and `create 0644`.