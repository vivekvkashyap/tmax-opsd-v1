Append `export QEMU_MONITOR_SOCKET=/home/user/qemu-monitor.sock` to `/home/user/.bashrc` (exported, so it's inherited by child processes/scripts). For the Python script, use `pexpect.spawn(f"nc -U {socket_path}")` to connect, then drive the login sequence with `child.expect("login: ")`, `child.sendline("sre_admin")`, `child.expect("Password: ")`, `child.sendline("monitor123")`, and `child.expect(r"\(qemu\) ")` (escape the parens since they're regex metacharacters) to reach the monitor prompt. Send `info uptime`, then expect the prompt again and extract the uptime by parsing `child.before` — the text pexpect captured between the command and the next `(qemu) ` match — using a regex like `VM uptime: (.+)` rather than assuming a fixed substring position. Send `quit` to close cleanly. Wrap the whole interaction in try/except (catching `pexpect.exceptions.TIMEOUT`/`EOF` and general exceptions) so failures write `STATUS: ERROR` instead of crashing, and on success write `STATUS: OK - Uptime: <extracted>` to `/home/user/uptime_report.txt`. Read the socket path from `os.environ["QEMU_MONITOR_SOCKET"]` inside the script (not hardcoded) and run it once at the end to produce the report file.