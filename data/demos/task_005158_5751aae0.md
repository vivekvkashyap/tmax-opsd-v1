First recover the three parameters from the image: run OCR (`tesseract /app/priors.png stdout` or similar, installing tesseract if needed) and parse the "Prior Mean", "Prior Variance", and "Sensor Variance" lines — inspect the raw OCR text output before trusting it, since OCR can misread digits/decimals, and sanity-check the numbers look like plausible variance/mean values. Then implement `/home/user/simulator.cpp` in pure double-precision C++: parse `argv[2..argc-1]` as doubles, and sequentially apply the standard Gaussian conjugate-prior update for each observation — new posterior variance is the harmonic combination of prior variance and sensor variance, new posterior mean is the variance-weighted combination of prior mean and the observation, then treat that posterior as the prior for the next observation. After processing all observations, compute the 95% CI using the given Z=1.95996 constant applied to the sqrt of the final posterior variance. Print exactly four space-separated values with `printf("%.6f %.6f %.6f %.6f\n", ...)` — since verification is strict byte-for-byte output matching against a fuzzer with many random observation sequences, get the exact update formula and print formatting right, and test locally against several manual sequences before finishing. Compile with `g++` to produce `/home/user/simulator`.