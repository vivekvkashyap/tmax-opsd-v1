Write the server in C using standard POSIX sockets (`socket`/`bind`/`listen`/`accept`) bound to 127.0.0.1:9000, wrapped in an outer loop that re-accepts after each connection closes so it serves multiple sequential clients. Buffer incoming bytes and split on `\n` to get discrete commands (a client may send a command in more than one `recv` call, so don't assume one read equals one line). For `EMBED <text>`, validate length is 8-16 and every char is alphanumeric using `isalnum` before doing anything else; on failure write exactly `ERROR_SCHEMA\n`. On success, invoke the oracle via `popen("/app/embedding_oracle <text>", "r")` (or fork/exec + pipe) and parse the 16 whitespace-separated floats from its stdout into a growable array-of-vectors, appending and returning the new 0-based index. For `CORRELATE i j`, bounds-check both indices against the stored count first (respond `ERROR_NOT_FOUND\n` if either is out of range), then compute the standard Pearson correlation formula over the two 16-length vectors and print with `%.4f\n` — watch for negative-zero and rounding edge cases. Unknown commands get `ERROR_INVALID\n`, never a crash. Compile with `gcc -O2 -o /home/user/server server.c` and launch it backgrounded (e.g. `nohup ./server &`) before finishing.
