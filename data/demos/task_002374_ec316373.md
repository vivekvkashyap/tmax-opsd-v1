This is two coupled deliverables — get the detector right first using the labeled training data, then wire up the service pipeline. For the detector: load each CSV in `/home/user/train_data/clean/` and `/home/user/train_data/evil/`, compute the covariance matrix of its numeric feature columns, and compute its condition number (ratio of largest to smallest singular value, e.g. `numpy.linalg.cond` or via SVD) — do this for every training file and look at the distribution of condition numbers for clean vs evil groups, then pick/cross-validate a threshold that cleanly separates them (the task says a large gap should exist — clean datasets will cluster low, evil ones will be enormous due to injected collinearity). Bake that threshold into `/home/user/detector.py`: read the CSV path from argv[1], compute its condition number the same way, `sys.exit(0)` if below threshold else `sys.exit(1)`. For the pipeline: fix `/home/user/pipeline/start_worker.sh` to `export REDIS_URL="redis://localhost:6379/0"` before launching the worker, then actually start redis-server, the Flask API, and the worker (as background processes) so a POST to `/process` with a file_path flows through Redis to the worker, which invokes `detector.py` and moves the file to `/home/user/accepted/` or `/home/user/rejected/` based on its exit code — test this end-to-end with curl before finishing.
