Work this in order: first get the vendored `libb64-1.2` building, then use the crash log as evidence for what the sanitizer must reject, then write the filter. When `make` fails in `/app/libb64-1.2`, don't guess — read the actual compiler error. A `CC = gcc-9` (or similar) line in the Makefile pointing at a toolchain that isn't installed should be changed to the compiler that actually exists on the system (check with `which gcc` / `gcc --version`); a missing standard header (e.g. `string.h`) causing implicit-declaration warnings/errors for functions like `memset` should be added back to the relevant source file. Next, inspect `/var/log/telemetry_crash.log` to see which literal characters caused the downstream crash (shell metacharacters embedded in otherwise-base64-looking data) — this tells you exactly what your sanitizer must catch, not just "anything weird." Implement `/home/user/sanitizer.c` to read the whole input file and scan byte-by-byte against a strict allow-list (A-Z, a-z, 0-9, +, /, =, and whitespace/newline characters only); any other byte means exit(1), otherwise exit(0). Avoid partial reads or early-exit-on-first-line bugs — malicious bytes can appear anywhere, including after a seemingly clean first line. Test against both `/app/corpus/clean/` and `/app/corpus/evil/` before finishing.
