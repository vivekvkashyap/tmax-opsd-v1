Derive the validation rules empirically from the two corpora rather than guessing what "valid" means — read every file in `/home/user/app/corpora/clean/` and `/home/user/app/corpora/evil/` to see the exact shapes involved (missing required keys like `repository`/`commit_hash`, path-traversal strings such as `../` inside `build_target`, and pathologically deep nested JSON meant to blow the stack/memory). In `validate_build_request`, wrap `json.loads` in a try/except so malformed input returns False safely, check all mandatory fields are present and non-empty, reject any field value containing `../` or absolute-path traversal patterns, and guard against the deep-nesting bomb by using `json.loads(payload_str)` with a low recursion/depth check (e.g. iterative depth-counting on the decoded structure, or setting a max nesting limit) rather than a naive unguarded recursive parse. Test the function directly against both corpora until 100% match before touching services. Then edit `config.ini` to set the Redis host/port and dispatcher URL and flip `RATE_LIMIT_ENABLED=true`, start redis-server, the dispatcher, and the gateway in the background, and confirm the 6th rapid identical request from one IP gets 429 before writing the final "SYSTEM_READY" log.