CMake step: run `cmake -S builder -B builder/build && cmake --build builder/build` to see the link failure. Fix `builder/CMakeLists.txt` by adding `target_link_libraries(packager archiver)` so the executable resolves `compute_checksum`, and add `set_target_properties(packager PROPERTIES BUILD_RPATH "$ORIGIN")` (or install RPATH pointing at the lib's build output dir) so `packager` finds `libarchiver.so` at runtime without `LD_LIBRARY_PATH`. Rebuild and confirm `./builder/build/packager <file>` runs standalone.

Translation step: read `legacy_manifest.rb` carefully and port its exact JSON structure (top-level `version`, nested `artifacts` dict keyed by id, each with `file`/`category`/`checksum`) into `manifest.py`, but replace the hardcoded `"PENDING"` by shelling out to the built `packager` binary per artifact filepath (use `subprocess.run`, capture stdout, strip whitespace) to get the real checksum. Run `python3 manifest.py` so it reads `artifacts.json` and writes `final_manifest.json`. Validate the checksum logic independently (e.g., XOR every byte of the file) to sanity-check what `packager` reports before trusting the final output.