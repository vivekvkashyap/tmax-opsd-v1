Parse `/home/user/backup.log` as fixed multi-line records: split on `[RECORD START]`/`[RECORD END]` markers (or iterate lines and reset a small struct on each boundary), extracting `File:` and `Status:` fields, and only keep filenames whose `Status` is exactly `SUCCESS`. For each kept filename, join it under `/home/user/backups/` and check existence first (skip silently if missing, per the requirement) — use `std::path::Path::exists()` or handle the `Err` from `File::open` gracefully rather than panicking. Read the first 4 bytes and compare against the literal ASCII bytes `B`,`K`,`P`,`1` before proceeding; if the header doesn't match, skip that file too. For files that pass, hash the *entire* file (not just the remaining bytes) with a SHA-256 crate (e.g. `sha2`), streaming reads through the hasher rather than loading only the header. Write `"<filename> <hex_digest>\n"` lines to a temp file `/home/user/valid_backups.manifest.tmp`, flush/close it, then `std::fs::rename` it to the final `/home/user/valid_backups.manifest` path — rename on the same filesystem is atomic, so do the rename only after all writes succeed.
