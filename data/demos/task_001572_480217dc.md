Start by OCR'ing the alert image with `tesseract /app/graph_alert.png stdout` to recover the exact validation rules — don't guess at the precision-loss condition, read it off the image, since the specific wording (uptime < total but the computed percentage rounds to exactly 100.0) is the crux of the logic. Next fix the build: `sanitizer.c` is missing `#include <stdio.h>` and `#include <string.h>`, and since the Makefile compiles with `-Werror -Wimplicit-function-declaration`, any implicit declaration (of `printf`, `strlen`, `strspn`, etc.) will hard-fail the build — add both includes before writing more logic. Implement validation in this order: (1) reject if the filename contains any character outside `[A-Za-z0-9_-]` (use `strspn` against an allowed-charset string, or loop and check with `isalnum`/`==` comparisons — never allow spaces or shell metacharacters like `;`); (2) reject if `uptime > total` (logically invalid); (3) reject if `uptime < total` but `(uptime/total)*100.0 == 100.0` exactly (the floating-point precision-loss trap from the image) — compare as a double equality against `100.0` after the division. Build with `make`, then loop `./sanitizer < file` over every file in `/app/clean/` and `/app/evil/`, checking `$?` matches expectations for each, before declaring done.