Stay within coreutils/awk/sed/iconv/tar/grep only — no Python/Perl. First extract the tarball with `tar -xzf`. For each `.csv`, detect encoding (e.g. `file` reports UTF-16LE via a BOM or null-byte pattern) and convert non-UTF-8 files in place with `iconv -f UTF-16LE -t UTF-8`, being careful to only convert files that actually need it (converting a UTF-8 file as if it were UTF-16 will corrupt it). The key difficulty is multiline quoted `description` fields breaking naive line-based tools: use `awk` in proper CSV-record mode (tracking an odd/even count of `"` characters across lines to know when a record is still open) rather than `grep`/simple `awk -F,` per line, so embedded newlines inside quoted fields don't split a single logical record. For each file, join continuation lines back into one logical record, then filter where the `status` field is exactly `REJECTED`, emit `server_id:change_id`, and finally sort all results alphabetically across all files into `/home/user/rejected_changes.txt`.
