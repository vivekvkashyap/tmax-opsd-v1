First recover the secret token by transcribing `/app/admin_instructions.wav` (install a speech-to-text tool like `whisper`/`ffmpeg`+ASR) — listen closely for the exact token phrase, don't assume a format. Then write `/home/user/ingest_filter.py` using `argparse` for `--input-dir`/`--output-dir`, and for each `.zip` in the input dir run checks in order, discarding on first failure: (1) open with Python's `zipfile.ZipFile` and call `.testzip()` (or catch `BadZipFile`) to confirm integrity; (2) read `metadata.txt` from inside the archive and confirm its first line equals `Token: <recovered-token>` exactly; (3) find exactly one member ending in `.wav`, extract its bytes, and manually parse the RIFF/WAV header by reading the raw byte offsets (`RIFF` magic, `fmt ` chunk fields for format code, channels, sample rate, bits-per-sample) rather than any ffprobe/CLI tool — verify format=1 (PCM), channels=2, rate=44100, bits=16. Only if all checks pass, copy the original zip bytes to `<output-dir>/<name>.zip.tmp`, close the file handle fully, then `os.rename()` it to the final `<name>.zip` — this rename-after-close is what makes the write atomic and is specifically what the test observes via strace/inotify. Test against both the clean and evil corpora to confirm 100%/0% pass rates.