Approach this as two coupled subsystems: a corpus-driven classifier and a service integration, both testable independently. First inspect the provided `/app/corpus/clean/` and `/app/corpus/evil/` payload files directly (`grep`/`cat` every sample of each, diff the two sets) to characterize what distinguishes them — the task description already tells you the evasion involves manipulating the backend's execution arguments so `/proc` virtual files (which expose a process's environment, cmdline, etc.) leak into output somehow, so look specifically for any reference to `/proc` paths or argument-injection syntax that appears in evil samples but never in clean ones. Write `/home/user/detector.sh` to read the single file argument and grep/pattern-match for whatever markers you find that reliably separate the two corpora, exiting 1 on match and 0 otherwise; iterate against every file in both corpora in a loop until you hit 100% classification before moving on — don't just eyeball a few samples. Second, edit `/app/gateway/app.py` so that on each incoming request it writes the payload to a temp file, calls `subprocess.run(['/home/user/detector.sh', tmp_path])`, checks the return code, and only forwards to `localhost:9000` on success (else return 403 "MALICIOUS"). Third, harden `/app/backend/start_backend.sh` by wrapping the executor invocation with `unshare -p -f --mount-proc` or `bwrap` to isolate its `/proc` view. Start everything via `/app/start_all.sh` and confirm the flow end-to-end.