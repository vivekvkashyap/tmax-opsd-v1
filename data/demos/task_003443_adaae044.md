Start by inspecting the existing `/app/sqlite/Makefile` for anything that would silently disable authorization hooks or break archiving — search compile flags for `-DSQLITE_OMIT_*` (particularly the authorization one) and remove it, and check the `ar` invocation actually archives the compiled object file (`.o`), not the source (`.c`). Rebuild with `make` and confirm `libsqlite3.a` exists and is a valid archive (`ar t libsqlite3.a`). For the validator, use `sqlite3_open(":memory:")`, execute the three `CREATE TABLE` statements, then register a callback via `sqlite3_set_authorizer` that inspects the action code and table name on every access: deny anything targeting `restricted_metadata`, and deny any write-type action (INSERT/UPDATE/DELETE/DROP/ALTER/CREATE/PRAGMA/etc). Prepare the query with `sqlite3_prepare_v2` (a prepare failure/syntax error or reference to a nonexistent table should REJECT), and additionally check `sqlite3_stmt_readonly()` on the prepared statement as a second read-only guard. Compile and link against your fixed static library, then iterate every file in both corpora and confirm each classification matches expectations before considering the task done.
