Layer the fixes and build the worker as a genuine long-running consumer loop, not a batch script. First fix nginx.conf: add a `location /ingest { proxy_pass http://127.0.0.1:5000; }` block under the port-8080 server, and verify with `nginx -t` before starting it alongside Redis and flask_app.py. For worker.py: loop on `redis.blpop('raw_data', ...)`, and before calling `json.loads`, sanitize each line by stripping/fixing invalid `\u` escapes that aren't followed by exactly 4 hex digits (e.g. a regex replacing `\u(?![0-9a-fA-F]{4})` with something JSON-safe like an empty string or escaped backslash) — do this without discarding the rest of the line, so surrounding valid fields still parse. After parsing, validate: reject if `abs(sum(probabilities) - 1.0) > 1e-5`, or if `value < 0`; only push validated records to `clean_data`. For the stratified sample, keep a dict of group -> list of ids, capped at 2 per group in order of appearance, and regenerate report.html from scratch on every new sampled record using a string template that builds `<li>Group: id1, id2</li>` lines with groups sorted alphabetically. Run the worker as a background/daemon process so the verifier's POSTs and subsequent Redis checks can proceed asynchronously. Test against both clean.jsonl and evil.jsonl locally before relying on the automated corpus check.
