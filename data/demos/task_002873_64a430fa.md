Build this as a small, self-contained Go HTTP server rather than trying to shortcut the math. First get the audio duration reliably: shell out to `ffprobe -v error -show_entries format=duration -of default=noprint_wrappers=1:nokey=1 /app/dataset.wav` from Go (using `os/exec`), parse the float from stdout, and call it twice inside the handler to satisfy the reproducibility check — if the two calls disagree, return HTTP 500 before computing anything else. Then compute the two statistics directly from the formulas given in the task using the extracted duration `D`: z-score against the baseline N(mu0=10.0, sigma0=2.0), and the Bayesian posterior mean using the exact weighted-precision formula provided (don't approximate or round early — only round in the final JSON if needed). Serialize `{duration, z_score, posterior_mean}` as JSON with `encoding/json`, register the handler on `/metrics`, and start the server with `http.ListenAndServe("127.0.0.1:8080", nil)` in the background (e.g. `go run server.go &`), then verify with `curl` that it responds before considering the task done.
