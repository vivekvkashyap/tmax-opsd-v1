Build the orchestrator as a sequence of idempotent, checkable steps rather than one monolithic script. First create the directory tree and use `ln -s` for the two symlinks (verify with `ls -l` that they resolve to the right targets, not reversed). Launch `/home/user/cloud_emulator.sh &` and capture `$!` immediately. For the readiness check, avoid a blind `sleep`; instead loop polling the port with something like `while ! (exec 3<>/dev/tcp/localhost/8080) 2>/dev/null; do sleep 0.5; done` (or `nc -z`), since the emulator intentionally delays before opening the port — a premature Expect run will just get "not reachable" errors from the mock SSH tool. Write the Expect script with `spawn`, then `expect "Password: "` / `send "finops2024\r"`, `expect "Emulator> "` / `send "fetch_cost\r"`, `expect "Emulator> "` / `send "exit\r"` — make sure prompts match exactly including trailing space/no newline as printed by the mock. Redirect its stdout into `/home/user/finops/logs/cost.log` via the orchestrator. Finally, `kill` the captured emulator PID and confirm nothing still listens on 8080 (e.g. via `lsof` or another connectivity probe) before exiting.