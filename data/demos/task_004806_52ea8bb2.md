Work the three steps in order, confirming each before moving on. First, OCR `/app/sticky_note.png` (e.g. with `tesseract` or another OCR tool) to recover the base word printed on the note — don't assume a value, actually read the image — then the real passphrase is that word plus a single trailing digit, which you must determine by trial: attempt to decrypt/use `/app/admin_key.enc` (an encrypted SSH private key, e.g. via `ssh-keygen -y -f /app/admin_key.enc -P "<candidate>"` or `openssl`) trying digits 0-9 appended to the OCR'd word until decryption succeeds, then save the decrypted key to `/home/user/admin_key.pem`. Second, read `/app/sshd_config.target` line by line and compare each of `PermitRootLogin`, `PasswordAuthentication`, `X11Forwarding` against their secure values (`no` for all three) — only include directives in the audit JSON whose *current* value in the file is insecure (e.g. skip any that are already `no`). Third, implement the HTTP service: for `POST /log`, HTML-entity-encode `<>&"'` in the `entry` field (e.g. Python's `html.escape`) before appending to `/home/user/service_audit.log`; for `GET /report`, require `Authorization: Bearer <passphrase>` matching the exact cracked passphrase and return 401 otherwise, else return the audit JSON's contents. Test both endpoints yourself with curl, including negative auth cases, before leaving the service running in the background.
