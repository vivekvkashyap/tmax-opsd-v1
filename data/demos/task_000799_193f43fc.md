Fix the build before writing any downstream logic. Open `/app/vendored/seqstats-1.0/setup.py` and look at how the C extension declares its link libraries — a linker `libraries=[...]` entry naming a nonexistent library (rather than the correct short name for the math library, `'m'`, which is what `-lm` expects on Linux) will cause `pip install .` to fail at link time; correct that single entry, then `pip install .` (or `pip install -e .`) from that directory and confirm `import seqstats; seqstats.count_bases("ACGT")` works before moving on. For `process_seq.py`: parse only the first sequence in the FASTA (stop accumulating lines once a second `>` header appears), uppercase before counting, then build the constant 4x4 coefficient matrix and RHS vector from the four base counts exactly and solve with `numpy.linalg.solve` (not a hand-rolled solver, to match the oracle's floating-point behavior bit-for-bit). For the Monte Carlo step, seed Python's `random` module (not numpy's RNG) with the integer sum of the four counts, then draw exactly 10,000 `random.gauss(x, max(y, 1.0))` samples in a simple loop preserving call order, and average them — using numpy's vectorized normal sampler instead would break exact reproducibility against the reference. Format all five values to 4 decimals in the exact specified string layout.
