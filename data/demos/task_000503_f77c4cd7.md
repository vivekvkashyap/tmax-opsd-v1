Write the rotate/summarize script to match the verifier's exact expectations, then separately wire up cron. In `/home/user/rotate_and_summarize.py`: first check `os.path.exists` and `os.path.getsize(...) > 0` on `/home/user/app/data.log`, exiting immediately (no error) if either fails. Read the file, filter lines containing the literal substring "ERROR" (case-sensitive, exact match as given), and append them verbatim (including original line endings) to `/home/user/app/error_summary.log` — use append mode so repeated runs accumulate. Rename (not copy) the log to `/home/user/app/archive/data_<timestamp>.log` using `datetime.now().strftime("%Y%m%d_%H%M%S")` for the exact `YYYYMMDD_HHMMSS` pattern, ensuring the archive directory exists first. Then create a fresh empty `data.log` so the app can keep writing. `chmod +x` the script. Finally add a crontab entry with `crontab -e` or by writing to a crontab file and loading it with `crontab <file>`, using the line `0 0 * * * /usr/bin/python3 /home/user/rotate_and_summarize.py` — verify with `crontab -l` that the schedule fields and full python3/script path match exactly.
