Work top-to-bottom, verifying each stage before moving to the next. First, in `/home/user/edge_daemon.cpp`, find the hardcoded socket path constant and change it to `/home/user/run/edge.sock` (make sure the `run` directory exists first), then compile with `g++ /home/user/edge_daemon.cpp -o /home/user/edge_daemon`. Start the daemon in the background (e.g. `./edge_daemon &`), then run `python3 /home/user/client.py` and confirm it connects successfully rather than reporting a socket-not-found/connection error — check `/home/user/data/sensor.log` gets written before continuing. Only once that round-trip works should you write `/home/user/backup_job.sh`: it must run the three `tar -czf` compress-to-numbered-file steps in strict sequence with real `sleep 1` calls between them (not just a single tar duplicated three times — actually invoke tar three separate times so each archive is created at its own point in time), writing into `/home/user/backups/`. Run the script and check all three tarballs exist and contain `sensor.log`. Finally, kill the daemon process cleanly (e.g. `kill` its PID, not `kill -9` unless necessary) once backups are confirmed.
