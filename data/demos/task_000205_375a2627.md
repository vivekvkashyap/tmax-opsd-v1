Work in three independent phases and verify each before moving on. First, fix the Rust circular dependency: inspect src/lib.rs, parser.rs, and rules.rs to see which module imports the other's wildcard; break the cycle by having one module stop importing the other's `*` and instead reference only the specific items it needs (or move shared types into a third module both can depend on), then confirm with `cargo build --release` that librust_waf.so is produced. Second, determine the classification threshold empirically rather than guessing: run /app/legacy_scorer against a range of crafted payloads (varying which "dangerous" keywords like SELECT/UNION/<script> appear and how many times), read the printed "Score: N" alongside the exit code for each, and binary-search/bracket the boundary between exit 0 (clean) and exit 1 (evil) to pin down the exact numeric cutoff the binary uses — do not hardcode a guessed value. Third, write detector.py using ctypes.CDLL to load librust_waf.so, declare compute_hazard's argtypes/restype correctly (const char* -> uint32_t), read each file as bytes, call the function, compare against the threshold you discovered, and dump a path->"CLEAN"/"EVIL" JSON dict to results.json, overwriting on each run. Test against both /app/corpus/clean and /app/corpus/evil before considering it done.
