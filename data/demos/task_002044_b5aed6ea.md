Build this as a single Python script rather than an interactive exploration, since every step feeds the next. Read the WAV with `scipy.io.wavfile.read` or `soundfile`, reshape the sample array into 60 chunks of 16000 samples each (verify shape before proceeding). For each chunk compute `np.abs(np.fft.rfft(chunk))`, which gives 8001 bins; use `np.array_split(spectrum, 32)` and take the mean of each split to get a 32-dim vector, stacking all 60 into a (60,32) matrix. Center by subtracting the column means (do this yourself even though sklearn's PCA also centers internally, so you don't double up incorrectly — just let PCA handle centering, don't manually subtract and then also let PCA subtract). Fit `PCA(n_components=4)` on the (60,32) matrix and transform to get 60 4-dim vectors. Use `sklearn.metrics.pairwise.cosine_similarity` (or manual dot/norm) between chunk 0's vector and each of chunks 1-59. Sort descending by similarity, take the top 5, and write `chunk_id,similarity` rows (with header) to `/home/user/top5.csv`. Double check chunk indexing is 0-based and consistent between the chunking step and the CSV output.
