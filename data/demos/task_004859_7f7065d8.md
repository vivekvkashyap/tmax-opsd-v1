Fix the C side first, then the Rust side, then run — each stage should be independently verified before moving on. In `/home/user/pipeline/c_src`, inspect the Makefile's compile/link commands: building a proper shared library requires compiling the object with `-fPIC` and linking with `-shared` (a plain `gcc filter.o -o libfilter.so` produces a broken/non-PIC artifact); fix the flags and confirm `libfilter.so` is a real shared object with `file` or `objdump`. In `/home/user/pipeline/rust_src`, run `cargo build` to see the exact borrow-checker error: it will point at a struct field holding a `&str` reference to a local `String` (like `current_data`) that gets cleared/reused each loop iteration, so the reference outlives its data. Fix it by making the struct field an owned `String` (e.g. `data: String`) and cloning the local at the point of construction, adjusting the state machine accordingly — don't just add lifetimes to paper over it. Finally set `LD_LIBRARY_PATH=/home/user/pipeline/c_src` and run `cargo run -- /home/user/input.log /home/user/output.json`, then inspect the produced JSON to confirm only events passing `is_valid_event` were kept, in original order.