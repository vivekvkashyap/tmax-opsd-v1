Sequence this: recover the config values first, since everything else depends on them. Run `tesseract /app/config_spec.png stdout` (or equivalent OCR) to extract the proxy port and allowed plugin directory text from the image — inspect the raw OCR output carefully rather than assuming a value, since OCR can introduce stray characters you'll need to trim. Use the recovered port in the nginx config's `proxy_pass http://127.0.0.1:<port>;` under a `listen 80;` server block. For the Rust sanitiser, structure validation as two independent checks combined with OR-to-reject: (a) build a directed graph from `step_id -> depends_on` edges and detect cycles via DFS with a recursion-stack/visiting-state (not just a visited set, or you'll miss cycles); (b) for every step's `plugin_path`, check it starts with the exact allowed-directory string recovered from the image (use `str::starts_with`, being careful about trailing slash). Exit 0 only if both checks pass, exit 1 if either fails. Build with `cargo build` so the debug binary exists at `target/debug/<name>`, and test manually against a couple of clean/cyclic/bad-path JSON files before trusting the automated corpus run.