Before writing any C++, fix the vendored build: diff/inspect `/app/vendored/xxHash-0.8.2/Makefile` for anything that looks like a sabotaged tool variable (e.g. an `AR`, `CC`, or archive command reassigned to something broken like `/bin/false`), restore it to the standard tool (`ar`), then run `make` and confirm `libxxhash.a` actually contains object files (`ar t libxxhash.a`) rather than just trusting a zero exit code. Next, read `/home/user/legacy_hasher.py` closely and replicate its exact logic in C++ line-for-line: same line-stripping/whitespace handling, same seed value passed to XXH32, same even/odd filter condition, and same output formatting (zero-padded hex width, and the exact string reversal) — mismatches here are the most common source of "close but not bit-exact" failures. Compile with `g++ ... -I<xxhash include dir> -L<xxhash lib dir> -lxxhash` (static link), then self-test by piping a handful of tricky inputs (empty lines, unicode, very long lines) through both the Python oracle and your compiled binary and diffing stdout before assuming correctness.
