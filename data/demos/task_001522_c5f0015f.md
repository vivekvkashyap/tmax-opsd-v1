Fix the build before writing any harness logic, verifying each fix in isolation. Compile `encoder.c` directly with `gcc -Wall -Werror -o encoder encoder.c` to see the real compiler error — a missing header for functions like `malloc`/`free` (declared in `stdlib.h`) under `-Werror` is a classic cause of an implicit-declaration warning-as-error; add the missing `#include` rather than removing the strict flags. For the Makefile, inspect it with `cat -A Makefile` to check whether the recipe line under the `encoder:` target is indented with a tab (make requires a literal tab, not spaces) and fix accordingly, then confirm `make` produces `./encoder` cleanly. For the harness script: generate the payload with a tool like `printf` or `head -c` repeated writes (avoid loops of 100000 iterations — use something like `printf 'X%.0s' {1..100000}` or `yes X | head -c 100000` for speed), build the `SIZE:DATA` serialized file with `wc -c`/`stat` for the exact byte count, time the encoder invocation with `date +%s%3N` (or `$EPOCHREALTIME`) before/after, and independently compute the expected hex with `xxd -p` (concatenated, no newlines, lowercase) or `od -An -tx1` to diff against the encoder's actual output before writing PASS/FAIL to the log.