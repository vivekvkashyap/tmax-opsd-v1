First read `/home/user/service.sh` to confirm both bugs concretely: an ever-growing bash array (`history+=(...)`) that never gets trimmed, causing the memory leak, and a variance formula computed as `E[X^2] - E[X]^2`, which loses precision catastrophically when values are large in magnitude (as they are in this stream) because it subtracts two nearly-equal large numbers. Fix both in `/home/user/fixed_service.sh`: keep only the last 10 values in memory (e.g. a fixed-size sliding buffer, or re-slicing/trimming the array each iteration instead of unbounded append), and compute variance with the numerically stable two-pass method — first compute the window mean, then compute the mean of squared deviations from that mean (`sum((x-mean)^2)/N`), all via `awk` to avoid bash floating point issues. Keep the same per-window stdout output format. Run the fixed script against `/home/user/stream.txt` and redirect/extract only the very last printed line into `/home/user/final_variance.txt`.
