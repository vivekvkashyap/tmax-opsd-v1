Get the deduplication/tombstone logic exactly right in your C code before worrying about the shortest-path algorithm — that's where naive attempts fail. Parse `nodes.csv`, `edges.csv`, `tombstones.csv` with basic CSV line splitting (no external libs). Build a map from (src,dst) to the list of edge versions/costs seen; for each (src,dst) pair, first drop any edge record whose `version` is <= a tombstone's `version` for that same (src,dst) — note the rule is "version strictly greater than or equal to the edge's version" gets tombstoned, i.e. only edge versions strictly greater than every matching tombstone survive; among the surviving versions for a pair, keep only the highest-version one as the live edge. Only after computing this final live edge set should you build the adjacency structure and run Dijkstra (costs are non-negative integers) from node 10 to node 42, since a tombstoned or superseded edge must never appear in the graph. Write the path array and total cost to `/home/user/path.json` with strict double-quoted keys and no trailing commas — hand-format the JSON string rather than relying on formatting guesswork, and validate it parses with `python3 -c "import json; json.load(open('/home/user/path.json'))"` before finishing.