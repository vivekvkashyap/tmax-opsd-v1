Open `/app/libstatutils-1.2.0/Makefile` and look for aggressive optimization flags — specifically `-ffast-math` combined with `-O3` in `CFLAGS` — which is what breaks strict IEEE 754 semantics (fast-math lets the compiler assume no NaNs/infinities and reorder float reductions, hiding the catastrophic-cancellation behavior the naive variance formula is supposed to exhibit on large-mean data). Remove just that flag, keep the rest, then run `make install_python` to rebuild and install the Python extension via pip. Confirm the fix worked by importing `libstatutils` in a quick Python REPL check and calling `.variance()` on a small hand-crafted array with a very large mean and tiny spread — after the fix this should reliably go negative or invalid, exposing the cancellation instead of masking it. Then write `/home/user/detect_instability.py` to load the CSV column (e.g. with `csv`/`numpy`), call `libstatutils.variance(samples)`, and print `EVIL` if the result is `<= 0` or `NaN` (use `math.isnan`), else print `CLEAN`. Test against both `/app/corpora/clean/` and `/app/corpora/evil/` before finishing.
