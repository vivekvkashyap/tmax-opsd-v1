Build this as a straightforward scikit-learn pipeline, but be precise about the exact API calls since the verifier checks specific values. Load the CSV, split into `X` (all columns except `target`) and `y` (`target`). Instantiate `BayesianRidge()` with defaults, then run `GridSearchCV(estimator, param_grid={'alpha_1': [1e-6, 1e-5, 1e-4], 'lambda_1': [1e-6, 1e-5, 1e-4]}, cv=3)` and fit on the full dataset — do not add a separate train/test split since the task only asks for CV tuning. Pull `best_params_` and `best_score_` (which is the mean R² across folds for the default `BayesianRidge` scorer) into the required dict with the exact key names given. Before writing the artifact, validate the dict against `/home/user/schema.json` using `jsonschema.validate` and only write the file if that succeeds — this also catches type mismatches like numpy floats not being JSON-serializable (cast to native `float`/`int` before dumping). Run the script and inspect `artifact.json` afterward to confirm it parses as valid JSON and matches the schema's required keys with `additionalProperties: false`.