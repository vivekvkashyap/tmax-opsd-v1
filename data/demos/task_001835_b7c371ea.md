Work bottom-up through the pipeline. First enable/start the pre-supplied `app_mock` and `dashboard` user units with `systemctl --user enable --now`, and check their unit files use `Restart=on-failure` (add it if missing) so they auto-restart. Next build the C parser: read one line from stdin, split on `:` then `|` then `#`, and be strict — if any expected field or tag pair is malformed/missing, print exactly `INVALID_METRIC` and return; otherwise format the value with `%.4f` and rebuild the tag list preserving input order. Test it manually with a few valid and deliberately-broken input lines before moving on, since the verifier fuzzes this binary directly against a reference for bit-exact output. Then write `aggregator.sh`: use `nc -ul 8125` (or a socat/read loop) in an infinite loop to receive one UDP datagram at a time, pipe each through `./parser`, and if the result isn't `INVALID_METRIC`, `curl -s -X POST -H 'Content-Type: text/plain' --data "$parsed" http://localhost:9090/ingest`. Wrap failures so the loop never dies. Finally, create and start `aggregator.service` as a user unit, then send a real test UDP packet end-to-end to confirm the dashboard reflects it.
