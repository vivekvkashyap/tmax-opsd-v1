Load the `signal` dataset from `/home/user/nanopore_sim.h5` with `h5py`. For step 1, design a 4th-order Butterworth low-pass filter with normalized cutoff 0.1 via `scipy.signal.butter(4, 0.1)`, apply it zero-phase with `scipy.signal.filtfilt`, take `numpy.gradient` of the filtered signal, then call `scipy.signal.find_peaks` on the absolute value of that derivative with `height=0.05, distance=50` — use these exact parameters since the peak count is checked precisely. For step 2, integrate the RAW (unfiltered) signal slice `signal[1000:5000]` with `scipy.integrate.simpson` (fall back to `simps` if using an older scipy). For step 3, extract the raw (unfiltered) 500-point segment `signal[2000:2500]`, build an x-array `0..499`, and fit `A * exp(-((x-mu)**2)/(2*sigma**2))` via `scipy.optimize.curve_fit` with `p0=[4.0, 250.0, 50.0]` — take `abs(sigma)` from the fit since curve_fit can return a negative sign for a symmetric Gaussian. Write `num_events` (int), `integral_value` (rounded 2dp), `gaussian_A`, `gaussian_mu`, `gaussian_sigma` (each rounded 3dp) to `/home/user/analysis_results.json` with exactly those keys — mismatched rounding or using the filtered signal where the raw one is required will fail verification.
