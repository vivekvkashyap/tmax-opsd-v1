Build and test this in isolated stages before wiring them together as a daemon. First write and test `filter.sh` standalone against a sample log file to confirm it extracts exactly the lines containing `[CRITICAL]` via `grep`/`awk` and nothing else. Second, write the recursive traversal in C (`nftw()` or `opendir`/`readdir` recursion) over `/home/user/legacy_logs/`, filtering to `*.log` suffix, and invoke `filter.sh` per file with `popen()` in read mode, appending its stdout lines to `/home/user/archive/master_critical.log` while counting lines to maintain a running total. Third, implement the atomic summary update as its own small routine: format the JSON, `fopen`/`fwrite`/`fclose` (or `fsync`) to `summary.tmp`, then call `rename("summary.tmp", "summary.json")` — never write directly to the final path, since a crash mid-write must never leave a corrupt or partial `summary.json` visible. Fourth, add `inotify_init1`, `inotify_add_watch` on `/home/user/incoming/` with `IN_CLOSE_WRITE`, and reuse the exact same per-file processing routine for both phases so counts stay consistent. Test manually: process the seeded legacy logs, inspect `summary.json`/`master_critical.log`, then drop a new file into `incoming/` and confirm the numbers update within 2 seconds while the daemon runs detached in the background (`&`, and don't let it die when your shell session ends).
