Process the JSONL in the exact pipeline order the spec implies, since reordering changes results. (1) Stream-parse each line and deduplicate by `request_id`, keeping only the first occurrence seen in file order — use a `set()` of seen ids and skip subsequent duplicates before any other filtering, since a later duplicate of an already-counted unique payload must not be double counted. (2) After dedup, filter to `endpoint == "/login"` and `status_code == 401`. (3) Bucket by 5-minute tumbling windows: parse the ISO8601 timestamp, floor the minute to the nearest multiple of 5 (`minute - minute % 5`), zero out seconds, and re-render as an ISO8601 string with the same `Z` suffix format as the input. (4) Group by `(bucket, ip_address)` and count `len(set(payload_hashes))` per group — the unique-hash count, not raw request count, is what matters (watch for IPs that repeat the same payload many times, which should NOT trigger). (5) Flag groups where unique count is strictly greater than 5. (6) Sort results by `time_bucket` then `ip_address` ascending, and emit only the three required keys per object to `/home/user/suspicious_activity.json`.
