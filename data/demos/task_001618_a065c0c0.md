Build the sanitizer as three independent, composable checks combined with logical OR (reject if any triggers), and validate each check separately against the clean/evil corpora before combining. (1) For the audio rule, transcribe `/app/manager_notes.wav` with a local tool (`whisper`, `ffmpeg`+ASR, or similar) to recover the spoken codename, then grep the target file for that exact string. (2) For the leak check, extract the ID from the file's first line (format `DOC_ID: <ID>`) with `head -n1` + `sed`/`awk`, and test membership against `/app/train_master_ids.txt` with `grep -Fxq` (fixed-string, whole-line match to avoid partial-match false positives). (3) For the line-count anomaly check, precompute the mean and *sample* standard deviation (n-1 denominator) of `/app/baseline_line_counts.txt` once using `awk`/`bc -l`, derive the 95% CI as mean ± 1.96*stddev, then in the script compare `wc -l` of the target file against those bounds — reject if strictly outside `[lower, upper]`. Exit 1 as soon as any rule fires, else exit 0. Test every file in both `/app/corpus/clean/` and `/app/corpus/evil/` in a loop and confirm exit codes match expectations before considering the script done.