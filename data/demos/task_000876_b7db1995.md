First transcribe /app/voicemail.wav with an available speech-to-text tool to recover the exact target directory name and port number spoken — do not hardcode a guess, actually run the transcription and read the values from its output. Then process archives defensively: for each .tar.gz in /app/incoming, verify integrity before extracting (e.g. `gzip -t` or `tar -tzf` to detect corruption) and skip/discard any that fail rather than letting extraction crash your script; extract only the valid ones into a scratch directory. Rename extracted `*.dat` files to `*.bin` (a simple loop with `mv "$f" "${f%.dat}.bin"`) and move them into the release directory you created using the transcribed name. Compute SHA256 for each .bin file and write the manifest atomically: write to a temp file in the same directory first, then `mv` (rename, which is atomic on the same filesystem) it onto SHA256SUMS — never write directly to the final path. Determine the largest .bin file by size (ties broken alphabetically) and `ln -s` it as latest.bin using a relative or matching absolute path. Finally launch `python3 -m http.server <port> --bind 0.0.0.0 --directory <release_dir>` in the background (e.g. with `nohup ... &`) and confirm it's listening before finishing.