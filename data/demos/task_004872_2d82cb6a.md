Isolate the numerical bug before touching anything else. Open `dna_factorize/core.py` and find the NMF multiplicative-update line for `H` — it divides by a matrix product (`W.T @ W @ H` or similar reconstruction term) with no stabilizing term, so on near-singular/repetitive input that denominator can be exactly zero, producing NaNs. Add a small epsilon (`1e-9`) to that denominator only, mirroring how the `W`-update step is likely already stabilized elsewhere in the file — check for an existing epsilon pattern nearby as a template. Reinstall the package editable (`pip install -e /app/dna_factorize-1.2.0`) and sanity check by calling `embed_sequences` on the primers and confirming no NaNs appear in the output before moving on. Then treat step 3 as a constrained least-squares problem: minimize MSE between a convex combination ($w_i \ge 0$, $\sum w_i = 1$) of the 100 embedding rows and the target profile — use `scipy.optimize.minimize` with `SLSQP` and simplex constraints (or `scipy.optimize.nnls` plus renormalization) rather than hand-rolling gradient descent. Write all 100 weights, comma-separated in the primers' original order, to `weights.csv`, and check your own MSE against the threshold before finishing.
