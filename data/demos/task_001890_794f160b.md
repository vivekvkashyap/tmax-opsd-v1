Fix the numerical bug first: in `analyze_primers.py`, replace the `np.linalg.cholesky(C)` call with `np.linalg.svd(C, full_matrices=True)`, take the top-3 singular values/vectors (`U[:, :3]`, `S[:3]`, `Vt[:3, :]`), reconstruct `C_k = U[:, :3] @ np.diag(S[:3]) @ Vt[:3, :]`, and return `np.sum(C_k)` from `process_matrix` — test on one file first (`python3 analyze_primers.py /home/user/data/batch_1.txt`) to confirm it no longer raises `LinAlgError` even on the deliberately near-singular/rank-deficient inputs. Next, profile: loop over all 100 `batch_N.txt` files in a bash script, timing each invocation with something like `start=$(date +%s.%N); python3 analyze_primers.py "$f" >/dev/null; end=$(date +%s.%N); echo "$end - $start" | bc >> /home/user/times.txt` (or use `/usr/bin/time -f '%e'` and redirect stderr), producing exactly 100 lines. Finally write `fit_density.py` to load the 100 floats from `times.txt`, call `scipy.stats.norm.fit(data)` to get `(mean, std)`, and write them to `/home/user/profile_results.txt` in exactly the two-line `Mean: <val>`/`Std: <val>` format rounded to 4 decimals — match the format string precisely since verification is regex-based.
