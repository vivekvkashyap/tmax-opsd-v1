Parse the raw log with a strict regex matching the exact structure (timestamp in brackets, `INFO -` marker, endpoint after "accessed", `response_ms=` and `load=` fields) and skip any line that fails to match or lacks a `response_ms` value — don't try to salvage malformed lines. Deduplicate by comparing the full tuple (timestamp, endpoint, response_ms, load) as a set/map key, keeping lines with an empty `load=` as distinct from complete duplicates (a missing-load entry is not a duplicate of anything). Sort all remaining entries by timestamp. Then do a single forward pass to impute: track the last-seen valid load, and for each missing-load entry look ahead to find the next valid load, average the two, and format to exactly 2 decimal places — do this after sorting, since imputation depends on chronological neighbors, not file order. Finally write the CSV with the exact header row and comma-separated fields, matching order of columns precisely.
