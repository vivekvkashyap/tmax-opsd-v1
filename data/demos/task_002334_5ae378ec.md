Extract the tarball first (`tar xzf /app/ssh_log_analyzer-1.0.0.tar.gz`) and locate parser.py to inspect its regex patterns before touching anything else — the bug is narrow and localized to the IP and key-fingerprint regexes, not the overall architecture. The IP regex likely only matches IPv4 (four dot-separated groups); extend it (or add a second pattern) to also match IPv6 addresses (colon-separated hex groups, including `::` compression). The key-fingerprint regex likely only recognizes one format (e.g. RSA-style hex-colon strings); broaden it to also capture other key types/formats such as `SHA256:<base64>` and ED25519/ECDSA fingerprints as they actually appear in auth.log lines. After fixing, reinstall the package (`pip install .` in the extracted dir) and confirm `parse_log_line` returns non-null `ip`/`key_fingerprint` for every line in auth.log that should be parseable. Then write enforce_policy.py to cross-reference parsed entries against the weak-key criteria derived from cwe_rules.json, collect IPs tied to weak/compromised keys, and dedupe into the two output lists — check your F1 by eyeballing false negatives/positives against the log content before finalizing.