Implement the math in C using the standard closed-form OLS formulas, then wire it into a small shell pipeline. In `linear_regression.c`: parse the CSV skipping the header line (`fgets`/`sscanf` on `%lf,%lf`), accumulate `sum_x`, `sum_y`, `sum_x2`, `sum_xy`, and `n`, then compute slope `m = (n*sum_xy - sum_x*sum_y) / (n*sum_x2 - sum_x*sum_x)` and intercept `c = (sum_y - m*sum_x) / n`. Compute MSE by re-iterating the data (or reusing values while parsing) with `sum((y_i - (m*x_i+c))^2) / n`. Print the three lines with `printf("%.4f", ...)` for each value in exactly the `Slope:`/`Intercept:`/`MSE:` format required. In `evaluate.sh`, compile with `gcc -lm` (needed if you use `math.h` functions), run the binary, capture its stdout, and parse the three values out with `grep`/`sed`/`awk` (e.g. `grep 'Slope:' | awk '{print $2}'`) before building the JSON with `jq -n --arg ... ` or a heredoc, keeping all values formatted to 4 decimal places as strings/numbers matching the spec exactly. Make the script executable with `chmod +x evaluate.sh`, then actually run it end-to-end and inspect `model_metrics.json` with `jq` to confirm the structure and values look sane before finishing.