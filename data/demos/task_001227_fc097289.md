First get the vendored solver in `/app/pde_solver` compiling and numerically correct before touching the web service. Run `make` there and read the actual error/behavior: a missing `-lm` link flag in the Makefile will fail to link any math-library calls (e.g. `fabs`) — add `-lm` to the link line, don't remove the calls. Separately, check any array-indexing macro like `IDX(i, j, N)` used for 2D access into a 1D buffer — an incorrect definition (e.g. `i + j` instead of row-major `i * N + j`) causes memory corruption or wrong numeric output even after it compiles; fix the macro to proper row-major indexing and confirm `./solver 8` now produces a sane, non-garbage error value. Only then build the Python HTTP service: implement `POST /profile` with a lightweight framework (Flask/http.server), validate the token before doing any work (401 on mismatch), and implement the convergence loop by shelling out to `./solver <N>` via `subprocess`, timing each call with `time.perf_counter()`, doubling N while the parsed float output exceeds `target_error`. Plot execution time vs N with matplotlib (non-interactive `Agg` backend, since there's no display) and save to the exact path given. Keep the server running as a background/daemon process, not something that exits after one request.
