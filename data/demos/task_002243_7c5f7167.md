Start by running `tesseract /app/config.png -` (or convert the image and OCR it) to recover the four `KEY=VALUE` config lines — read them from the OCR output rather than assuming values, and sanity-check the OCR result since misreads (0/O, 1/l) are common. Then implement the C++ classifier precisely per spec: parse the headerless CSV into columns, compute the variance of each column and keep only the max-variance one, run `BOOTSTRAP_ITERS` resamples-with-replacement of size N from that column (use a seeded or any RNG — determinism isn't required since the decision only depends on the *average* posterior across iterations, which should be stable), and for each resample count values whose fractional part exceeds `1e-5` in magnitude, then apply the Beta-Binomial posterior-mean formula with the OCR'd `PRIOR_ALPHA`/`PRIOR_BETA`. Average the posterior means across all bootstrap iterations and compare strictly against the OCR'd `POSTERIOR_THRESHOLD` to decide CLEAN vs EVIL. Iterate against both `/app/corpus/clean/` and `/app/corpus/evil/` directories, running the compiled binary over every file, until every file classifies correctly — if borderline cases misclassify, double check your variance/fractional-part logic and OCR'd constants before assuming the algorithm itself is wrong.
