Treat this as reverse-engineering from a visual spec plus black-box fuzz verification — don't assume the algorithm, extract it. Run `tesseract /app/legacy_auth_spec.png` (or similar OCR/vision approach) to read the diagram's described encoding steps precisely, including the exact order of operations and any literal constant (like an XOR key) it specifies — transcribe it carefully since OCR can misread hex digits or punctuation, so sanity-check the extracted text for plausibility (e.g. does it read as a coherent numbered list of encoding steps). Implement encoder.py to read all of stdin as a string, then apply the stages in the exact order the document specifies (e.g. reverse the string, XOR each byte with the extracted key, base64-encode the result) and print with no trailing newline (use `sys.stdout.write`, not `print`, to avoid an extra `\n`). Before trusting your OCR read, test small hand-computed inputs (e.g. a short string where you can XOR and base64-encode by hand or with a one-liner) to confirm your understanding of stage ordering is exactly right, since fuzz-equivalence testing against many random inputs will expose any subtle ordering or byte-encoding mismatch.
