Build `/home/user/solve.sh` using `sqlite3` with a Recursive CTE over the `edges` table, joining `nodes` for names. The trap is the corrupted index on `edges(source_id)`: any plain `WHERE source_id = ?` or join predicate on `source_id` will use that stale index and return wrong rows. Defeat the query planner's index selection by wrapping the indexed column in a no-op expression (e.g. a unary `+` on the column, like `WHERE +source_id = ?`) in both the base case and the recursive join condition — this forces SQLite to fall back to a full table scan of the true data instead of the corrupted index, since indexed-column expressions can't use the index. Cap recursion at exactly 3 hops (depth check in the recursive term), select DISTINCT target node names reachable within 1-3 hops, sort alphabetically, and print one name per line. Test by piping several `$1` values through your script and diffing against expected reachability by manually tracing `edges` rows for small node IDs, since your own output must be self-consistent and index-independent — verify with `EXPLAIN QUERY PLAN` that no `USE INDEX` on the corrupted index appears.