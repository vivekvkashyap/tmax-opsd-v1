In the notebook, load both `.npy` arrays with numpy, then for each of the 5 datasets compute `U, S, Vt = np.linalg.svd(X[i], full_matrices=False)`. Build the truncated pseudoinverse manually rather than calling `np.linalg.pinv` with default tolerance: create `S_inv` as zeros, then set `S_inv[S >= 1e-4] = 1.0 / S[S >= 1e-4]` (the naive mistake is using `np.linalg.pinv(X[i])` with its default rcond, which uses a different truncation rule than a hard `1e-4` cutoff on singular values and will not match). Reconstruct the pseudoinverse as `Vt.T @ np.diag(S_inv) @ U.T`, then compute weights as `pinv @ y[i]`. Stack the 5 weight vectors into a `(5,10)` array in dataset order and save with `np.save('/home/user/weights.npy', weights)`. Finally, execute the notebook headlessly (e.g. `jupyter nbconvert --to notebook --execute --inplace fit_models.ipynb` or `papermill`) to actually produce the output file — writing correct code in the notebook without running it will not create `weights.npy`.