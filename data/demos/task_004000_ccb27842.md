Reassemble the split archive first: `cat legacy_backup.tar.gz.00 legacy_backup.tar.gz.01 legacy_backup.tar.gz.02 > legacy_backup.tar.gz`, extract it, then extract the inner `internal_configs.tar` into `/home/user/legacy_extracted/` — verify the resulting file tree with `find` before comparing. Build a file-path -> sha256 map for both trees: for the legacy tree, use `find ... -name '*.conf'` and hash each with `sha256sum`; for the current tree, use `find -L /home/user/current_configs -name '*.conf'` (the `-L` flag is the key detail — it makes `find` follow symlinks so you hash the target file's actual content, not the link) to compute relative paths and checksums. For each `.conf` file in current_configs, classify as NEW if its relative path doesn't exist in the legacy map, MODIFIED if it exists but the hash differs, and skip it otherwise (skip deleted files entirely — those only in legacy). Sort the NEW/MODIFIED set alphabetically by relative path and write `[STATUS] ./relative/path checksum` lines to the manifest. Build `update.tar.gz` from only those files using `tar -czhf update.tar.gz -C /home/user/current_configs file1 file2 ...` — the `-h` flag dereferences symlinks so the archive contains real file content, not links.