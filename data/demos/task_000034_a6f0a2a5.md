Build the script in clear stages and verify each independently before combining. First, decryption: read the base64 key from `/home/user/key.txt`, install `cryptography` if needed (`pip install cryptography`), and use `Fernet(key).decrypt(open('metadata.enc','rb').read())` then `json.loads(...)` on the result — print the decrypted dict first to confirm it worked before adding the policy checks. Second, the XSS check: recursively walk all string values in the parsed JSON (values can be nested) and do a case-insensitive substring search for `<script>` (e.g. `'<script>' in value.lower()`), not just top-level keys. Third, the ELF check: don't try to parse ELF structures by hand — shell out to a standard tool like `readelf -s /home/user/artifact.bin` or `nm -D`/`objdump -T` and grep the symbol table output for the literal `system` symbol name to detect dynamic imports. Finally assemble the four-key JSON report exactly as specified, computing `policy_passed` as the logical AND of the three conditions, and write it with `json.dump` to `/home/user/policy_report.json`. Run the script once at the end and inspect the output file to confirm the schema and boolean values are structurally correct.