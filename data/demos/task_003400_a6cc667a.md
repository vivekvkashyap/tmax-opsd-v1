Treat the legacy binary as a black box and reverse-engineer its scoring formula empirically before writing any Python: feed it small, controlled CSV rows via stdin (e.g. identical strings for a zero baseline, then vary only length, then only character values one at a time) and observe how the DIST number in its output changes for each isolated variable — this lets you separate a length-difference term from a character-value-difference term and infer how they combine (additive, weighted, etc.), rather than guessing a formula. Confirm your hypothesis on several more inputs, including multi-byte Unicode strings, before locking it in. Cross-check the output line format character-by-character (brackets, spacing, quoting, field order) against the binary's actual stdout, since the verifier compares bytes exactly. Implement the Python script to read stdin line-by-line (csv.reader over sys.stdin, not readlines() into memory) so it streams rather than buffering the whole file, split each row into its 6 fields, and for each of the 4 non-English columns compute your derived score and print the reshaped line in the exact matching format, flushing/streaming output incrementally.
