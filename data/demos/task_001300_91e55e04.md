Diagnose the SQL bug first: the buggy query in `/home/user/audit_tool.c` joins `employees e1, employees e2, messages m` with no linkage between `e1`/`e2` and `m` at all, producing a cartesian product. Fix it by explicitly joining `messages m` to `employees e1 ON m.sender_id = e1.id` and `employees e2 ON m.receiver_id = e2.id`, then filter `WHERE m.flagged = 1 AND e1.department = 'Finance' AND e2.department = 'Finance'`, selecting `e1.name AS sender_name, e2.name AS receiver_name`. For optimization, write `/home/user/optimize.sql` with indexes on the join/filter columns actually used by this query (e.g. `messages(sender_id)`, `messages(receiver_id)`, `messages(flagged)`, `employees(id)`, `employees(department)`), apply them with `sqlite3 /home/user/audit.db < optimize.sql`, then capture `EXPLAIN QUERY PLAN <query>;` output into `/home/user/query_plan.txt` and confirm it shows index usage rather than a full/cartesian scan. For centrality, extend the C code to tally, per employee name appearing in the filtered result set, how many rows they appear in as either sender or receiver, then write the top name (only) to `/home/user/highest_centrality.txt`. Compile with `-lsqlite3` and run it to produce the file.
