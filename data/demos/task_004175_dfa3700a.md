Build and test each layer of the pipeline independently before chaining them, and iterate against the two corpora (`/app/data/clean/`, `/app/data/evil/`) directly rather than only through the full HTTP path while developing. For `stability_guard.sh`: read one float per line from stdin into an array, then compute an FFT power spectrum — the simplest reliable approach is a short inline `python3 -c` heredoc using `numpy.fft.fft`/`numpy.abs(...)**2` on the signal, extracting the upper half of frequency bins; from those power values compute a bootstrap 95% CI for the mean (resample with replacement many times, e.g. 1000+ iterations, take the 2.5th/97.5th percentiles of the resampled means); reject (`exit 1`) if the CI lower bound exceeds `15.0`; otherwise `exit 0`. Tune your thresholding logic by running the script directly against every file in both corpora and counting misclassifications until you hit 100/100 before touching the web layer. For `cgi_proxy.sh`: read the CGI POST body (from stdin, respecting `CONTENT_LENGTH`), pipe it to `stability_guard.sh`, and on rejection print a raw `Status: 400` CGI header block with body `REJECTED`; on acceptance, `curl` the body to `http://127.0.0.1:8081/factorize` and relay its response verbatim. For `nginx.conf`: listen on 8000, proxy POST `/api/process` to the fcgiwrap unix socket, and include the standard `fastcgi_param`s (`SCRIPT_FILENAME`, `CONTENT_LENGTH`, etc.) so the body reaches your CGI script.