Work the two deliverables separately and verify each in isolation before wiring them together. First, fix evaluate.awk: since predict.sh emits comma-separated `id,prediction` and data.csv is comma-separated `id,score,label`, set `FS=","` (not the current `:`) for whatever joined-format you actually produce, and add an `NR==1{next}` (or similar) guard to skip the header row before it gets counted as a comparison — test it standalone with a small hand-built joined file and check the printed float against a known-correct fraction. Second, write run_pipeline.sh to loop over `10 20 30 40 50`, and for each threshold: time the predict.sh call specifically (e.g. capture `date +%s%N` immediately before/after the predict.sh invocation, not around the whole iteration, to isolate inference time in milliseconds), save its output, sort both the prediction output and data.csv by id (join requires sorted input), `join -t, -1 1 -2 1` them into the id,score,label,prediction order evaluate.awk expects, then run it through evaluate.awk. Append each threshold's `threshold,accuracy,time_ms` row to tuning_results.csv, track the max accuracy seen so far (favoring the lower threshold on ties by only updating on strictly-greater accuracy), and write that winning integer alone to best_threshold.txt.