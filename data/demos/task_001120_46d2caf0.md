Parse the binary format defensively rather than assuming every record is complete. After validating the 4-byte `WAL!` header, loop while there are at least 8 bytes remaining; unpack `TXID` and `LENGTH` as little-endian unsigned 32-bit ints (Python's `struct.unpack('<II', ...)`), then before reading the payload check that `LENGTH` bytes actually remain in the buffer — if not, stop immediately and discard that trailing partial record rather than raising an error or reading past the end. For each complete record, keep it only if `TXID % 3 == 0`. Implement your own run-length encoder for the payload (don't rely on a library): walk the string, count runs of identical consecutive characters, and emit `<count><char>` for each run in order — verify it manually on a case with runs greater than 9 to make sure multi-digit counts are handled correctly. Write one compressed line per surviving record, in the order they appeared in the file, to `/home/user/archive.rle`.
