Build this pipeline bottom-up so each piece is testable before wiring it together. First create all required directories and the static fixture files (`old_data/hello.txt`, `config/virtual_fstab`) exactly as specified. Next write the Rust `migrator` binary in a `cargo new` workspace: have it read and parse the single line of `virtual_fstab` (split on whitespace into source/destination), use `std::fs::create_dir_all` on the destination's parent directory, create a Unix symlink with `std::os::unix::fs::symlink(source, destination)`, append the exact `MOUNTED <source> TO <destination>` line to the log file, then loop forever with `std::thread::sleep`. Write `restart_service.sh` to read the PID file, check liveness with `kill -0 $PID` before killing, launch the release binary with `nohup ... &` and immediately capture `$!` as the new PID, writing it back to the PID file. Set up the bare repo's `post-receive` hook using `git --work-tree=... --git-dir=... checkout -f` (or `GIT_WORK_TREE`) to materialize pushed code into the worktree, then `cd` into the migrator crate, run `cargo build --release`, and finally invoke the restart script — remember to `chmod +x` the hook. Test the whole chain by cloning the bare repo, committing the Rust project, and pushing, then verify the symlink, log line, and running PID independently.