Tackle this in dependency order: (1) implement the Node `POST /calc` route in `node_api/server.js` with a hand-written recursive-descent (or shunting-yard) tokenizer/parser that handles `+ - * / ( )` with correct precedence — never call `eval()` or `new Function()`, since that is explicitly checked by grepping the file; (2) fix `python_client/setup.py` by subclassing setuptools' `build_py` (or `install`) command, overriding `run()` to first call `subprocess.run(["npm","install"], cwd="../node_api")` then `shutil.copytree` the whole `node_api` directory (code + `node_modules`) into the `poly_calc` package directory before calling the superclass method, and wire it in via `cmdclass={...}` plus `package_data`/`include_package_data=True` so the copied tree ships with the installed package; (3) run `pip install .` from `python_client/` and confirm `poly_calc/node_api/node_modules` exists in site-packages; (4) write `/home/user/test_run.py` to import `poly_calc`, call `start_server()`, `evaluate("14 + 2 * ( 18 - 6 ) / 4")`, write the exact JSON response to `/home/user/polymath/results.log`, then `stop_server()`. Test the parser standalone with curl before wiring the full pipeline together.