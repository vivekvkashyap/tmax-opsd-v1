Implement this as a straightforward Go program following the exact given OLS formulas — do not use a library, just accumulate sums of X, Y, X*Y, X^2 in a single pass over the CSV. Compute m and c from those sums, then compute residuals r_i = Y_i - (m*X_i + c) for every point, compute the population standard deviation (divide sum of squared residuals by N, not N-1 — this is the naive mistake to avoid), and filter to keep only points where |r_i| <= 2*sigma (strictly greater than 2*sigma is excluded). Write the kept rows to cleaned_data.csv with header "X,Y" and values formatted to exactly 1 decimal place (use fmt.Sprintf("%.1f,%.1f\n", x, y)), preserving original row order. For the benchmark, write a standard `func BenchmarkInference(b *testing.B) { for i := 0; i < b.N; i++ { _ = m*10.0 + c } }` in clean_test.go using the m/c learned from the full noisy dataset (not the cleaned one, per the spec), then run `go test -bench=. > /home/user/benchmark.txt` from within the module directory and confirm the file contains "BenchmarkInference" and "ns/op" before finishing.
