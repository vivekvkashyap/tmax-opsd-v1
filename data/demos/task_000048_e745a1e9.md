Bring services up first, since everything downstream depends on them: fix `/app/services/flask/.env` (`REDIS_HOST=127.0.0.1`, `REDIS_PORT=6379`), start `redis-server /app/services/redis/redis.conf &`, then `python3 /app/services/flask/app.py &`, and confirm both are listening (`redis-cli ping`, `curl 127.0.0.1:5000/api/data`) before writing the scripts. `etl.sh` should loop 20 times over `curl -s http://127.0.0.1:5000/api/data`, pull `.id`/`.text` with `jq -r`, and write each to `raw_data/<id>.txt`. For `filter.sh`, detect the zero-width Unicode characters by matching their raw UTF-8 byte sequences directly (e.g. `grep -qP '\x{200B}|\x{200C}|\x{200D}|\x{FEFF}'` under a UTF-8 locale, or `LC_ALL=C grep` against the exact byte patterns) rather than relying on locale-sensitive character classes. Compute the punctuation/alnum ratio by counting characters matching POSIX `[[:punct:]]` and `[[:alnum:]]` classes separately (e.g. `tr -d -c '[:punct:]' <file | wc -c`), then compare the ratio with `awk` (floating-point, not integer, division) against 0.5, and reject when alnum count is 0 but punct count is nonzero. Test both scripts against small clean/evil files you construct yourself before trusting them against the hidden corpus.