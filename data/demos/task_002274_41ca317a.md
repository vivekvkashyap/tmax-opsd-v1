Drive this with `git bisect start`, mark current `HEAD` bad and the first commit good, and let git checkout each candidate. When you land on the commit with the broken build (missing/misordered include), don't commit a fix into history — apply a minimal local edit to just that checked-out working tree (e.g. add the missing `#include`), verify `make` and `./run_test.sh` at that commit, then use `git bisect good`/`bad` based on the *runtime* result, and `git checkout -- .` (or reset the working tree) before bisect moves you to the next commit so you don't carry stray changes forward. Once bisect converges, record the exact 40-char culprit hash. Analyze the crash with `gdb ./monitor core` or `gdb --args ./monitor test_input.txt` plus `run`/`bt` to get the crashing function name — expect a division-by-zero in the weighted-average computation when a weight is 0. Fix it on current `master`/HEAD by skipping zero-weight entries in the loop (don't divide-then-multiply back through the weight at all), rebuild, confirm `./run_test.sh` prints `Success`, then write the three required lines to the report file.
