Diagnose the leak precisely before touching code: the bug is that `f1All`/`f2All` (and their mean/std) are built from every row regardless of `is_train`, then used to scale both train and test sets. The fix is to compute mean and *sample* standard deviation (Bessel's correction, i.e. gonum's `stat.StdDev` with nil weights already does N-1) using only rows where `is_train == 1`, then apply those same fixed parameters to scale every row — including test rows, which should NOT contribute to computing the mean/std but SHOULD be transformed by it. Concretely: change the SQL/loop so you accumulate into `f1Train`/`f2Train` slices conditioned on `r.IsTrain == 1`, compute `f1Mean/f1Std/f2Mean/f2Std` from those training-only slices, and then run the standardization formula over the *entire* `data` slice (both train and test) using those training-derived constants. Run `go mod init` and `go mod tidy` first so `github.com/mattn/go-sqlite3` and `gonum.org/v1/gonum/stat` resolve. After running, spot-check a couple of output rows by hand against the mean/std formula to confirm no leakage remains, since a single wrong slice reference silently reintroduces the bug.
