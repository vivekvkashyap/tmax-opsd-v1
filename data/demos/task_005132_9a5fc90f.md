Work in three independent, testable stages before wiring them into one Rust binary. (1) Video/graph extraction: use `ffmpeg` (or a Rust image/video crate) to dump all 30 frames of `/app/topology_scan.mp4` as images, read the pixel at each (x,y) for x,y in 0..20 in the top-left corner, and treat a pixel as black only if it's exactly (0,0,0) (allow a small tolerance for compression artifacts); union edge (x,y) across all frames to build one static 20-node adjacency matrix — verify by printing the accumulated edge list and sanity-checking symmetry. (2) Compute degree centrality (just node degree, since unweighted) for each of the 20 nodes, sort descending by degree with ties broken by lower index, and take the top 3 as High-Risk Hubs — do this as a standalone debug step first to see which nodes come out, rather than hardcoding indices. (3) Transaction filter: parse the JSON transactions, and for the sliding-window check, iterate over transactions on hub routers sorted by timestamp, and for each transaction sum bytes of all hub transactions with timestamp in [t, t+60) — a naive fixed-bucket approach (dividing time into non-overlapping 60s buckets) will misclassify some evil files, so implement a true sliding window per transaction start time. Wire these into `src/main.rs` reading `--video` and `--log` args, build with `cargo build --release`, and test against both `/app/corpus/clean/` and `/app/corpus/evil/` before trusting the result.