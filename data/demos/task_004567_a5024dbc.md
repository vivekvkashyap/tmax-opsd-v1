Start with static analysis: run `file`, `strings -n 6`, and `objdump -d -M intel /app/waf_auth_filter` to locate the validation logic in main — look for a base64 decode routine, comparisons against small immediate byte constants (the magic bytes), a running-XOR accumulator loop (the checksum), and a separate loop XOR-ing bytes with an index-derived value (the payload decryption). Use `gdb` to set breakpoints at the comparison instructions and step through with crafted argv values to confirm exact byte offsets, the checksum's seed constant, and the per-byte key formula tied to the loop index — derive these from the disassembly rather than guessing. Then write /home/user/token_decoder.py that: base64-decodes the input, validates minimum length and that the declared payload length matches the actual decoded length, verifies the checksum, XOR-decrypts the payload, and prints the plaintext with exit 0, or prints `INVALID` with exit 1 on any failure. Match behavior bit-for-bit, since a fuzz harness will diff your script's stdout/exit code against the real binary over thousands of malformed and well-formed base64 inputs.
