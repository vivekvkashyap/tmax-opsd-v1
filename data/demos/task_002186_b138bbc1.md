Fix nginx first: edit the server block in `/etc/nginx/sites-enabled/default` (or equivalent) to add a `location /api/data { proxy_pass http://127.0.0.1:5000/features; }` block, then `nginx -t` and reload/restart. For the bash pipeline, read exactly 100 lines from the sensor stream with `nc 127.0.0.1 9001 | head -n 100`, saving to a temp file so you can process it with `awk` without re-opening the socket. For each line, split on commas into an array in awk, apply the boundary-aware 3-point moving average (2-point average at index 1 and 100, 3-point elsewhere) and track the max of the smoothed values as that spectrum's peak — collect all 100 peaks into an array. For the bootstrap, use awk's `srand()`/`rand()` to resample 100 peaks with replacement B=500 times, compute each resample's mean, sort the 500 means, and take the values at the 2.5th/97.5th percentile ranks for the CI bounds. For the histogram, find min/max of the original 100 peaks, divide into 5 equal-width bins, and count membership with the boundary rule (equal-to-boundary goes to the lower bin except the true max, which goes to the top bin) — get this boundary logic exactly right since it must match precisely, not just approximately. Assemble the JSON string by hand (printf) and push it with `redis-cli SET latest_stats '<json>'`.