First fix `/home/user/qa_project/pyproject.toml`: run `python -m pip install .` to see the actual TOML/metadata error, then correct syntax (missing quotes, malformed tables, bad dependency specs) iteratively until install succeeds — don't guess the whole file blindly. For the resolver, treat `/app/legacy_resolver` as a black box: probe it systematically with a script that feeds many `(expression, version)` pairs covering each comparator (`<,>,<=,>=,=,!=`... check via probing which set it actually supports), `&&`/`||` combination, and nested parentheses, recording exit codes to infer precedence (does `&&` bind tighter than `||`? is grouping required or does it default to left-to-right?). Only after you've empirically confirmed the grammar, implement `/home/user/semver_match.sh`: parse the expression (recursive-descent or a simple tokenizer + shunting-yard is fine in bash+awk), and implement proper SemVer comparison (numeric field-by-field comparison of major.minor.patch, then handle any pre-release suffix ordering you observe from probing) rather than naive string comparison. Update `run_e2e_tests.sh` to call your script instead of the binary. Finally fuzz-test your own script against the real binary on many random cases locally before finishing, to catch precedence/comparison mismatches early.