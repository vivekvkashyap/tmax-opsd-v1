Scaffold the project with `cargo new ml_pipeline` in `/home/user`, add `csv` and `serde` to `Cargo.toml`. Read `/home/user/raw_data.csv` preserving original row order (don't shuffle or re-sort), and split strictly positionally: rows 1-80 are train, rows 81-100 are test — do this split before touching missing-value logic, since the bug to avoid is computing any imputation statistic from combined train+test data. Compute the mean of `feature_x` using only the parsed numeric (non-empty) values from the training rows, ignoring any test-set values entirely when computing that mean. Then apply that single training-derived mean to fill blanks in both the train output and the test output — the same imputed constant should appear in both files' rows that had empty `feature_x`. Write both CSVs manually (or via the `csv` writer) with the exact header `id,feature_x,label`, preserving row order within each split. Run the compiled binary to produce `/home/user/train_processed.csv` and `/home/user/test_processed.csv`, and sanity-check a couple of previously-empty rows in each file by hand before finishing.
