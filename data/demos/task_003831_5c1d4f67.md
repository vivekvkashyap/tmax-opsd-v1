Break this into sequential, independently-testable stages. First, parse `/var/log/auth_sys.log` and keep only lines indicating a *successful* login, extracting each user's MD5 hash; for each, crack it by hashing every word in `/opt/wordlist.txt` with `md5sum`/Python `hashlib.md5` and comparing to the logged hash, rather than assuming a mapping — this is a standard dictionary attack, so iterate and match. Write the recovered `{username: password}` pairs to `/app/recovered_creds.json`, then `chmod 0600` it. Second, treat `/app/legacy_token_gen` as an opaque binary — since it's stripped, don't try to reverse-engineer its internal hash algorithm; instead just shell out to it at runtime (`./legacy_token_gen <user> <password>`) from your new service whenever you need to validate a token, comparing its stdout output against the incoming `X-Legacy-Token` header. Third, build the HTTP service (Python's `http.server`/Flask, or similar) that loads `recovered_creds.json` into an in-memory dict at startup: `/rotate` must invoke the legacy binary with the user's *current in-memory* password to compute the expected token, compare it to the header, and only update the in-memory password on a match; `/verify` must check HTTP Basic Auth credentials against that same in-memory dict, which reflects rotations. Run it bound to `127.0.0.1:8000` and backgrounded, then manually exercise both endpoints with `curl` to confirm status codes before finishing.