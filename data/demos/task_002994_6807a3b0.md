Write a Python script that handles each log format with its own parsing logic rather than trying to force one generic parser: use `csv.DictReader` for web.csv and check `status_code == '403'` (or int comparison after casting), use `json.load` for api.json and check `'403' in entry['event']`, and read legacy.log line-by-line checking `'403' in line`. For legacy.log, extract the IP with a regex like `r'\d{1,3}(?:\.\d{1,3}){3}'` applied to matching lines — build and test this pattern against a sample line first rather than assuming a fixed column position, since the IP's position in the text varies. Collect all extracted IPv4 addresses that co-occur with a 403 signal into a Python `set` to dedupe before hashing (hash first then dedupe also works, but hashing raw strings is simpler to verify while debugging). For each unique IP compute `hashlib.sha256(ip.encode()).hexdigest()`, put all hashes into a `sorted()` list, and `json.dump` that list to `/home/user/anonymized_ips.json`. Double check you are not accidentally including IPs from non-403 lines (e.g. don't just regex-scan the whole file for IPs — only pull IPs from lines/entries that actually satisfy the 403 condition).