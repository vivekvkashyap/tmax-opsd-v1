First recover the 30 weights from the video, don't assume or hardcode plausible-looking numbers: extract each of the 30 frames from `/app/module_weights.mp4` (e.g. `ffmpeg -i module_weights.mp4 frame_%02d.png`, or read frames directly with `cv2`/`PIL` via a short Python snippet), and for each frame read the Red channel value of a pixel (frames are solid color, so any pixel works) to build a 30-element weight list indexed 0..29 in frame order — verify frame count is exactly 30 before trusting the mapping. Then write `/home/user/migrate_and_estimate.py`: parse `sys.argv[1]` as JSON, iterate `data['modules']` (already given as 30 entries with ids 0-29, but don't assume order — index into your weights list by each module's `id` field, not its position in the array), compute `sum(size[i] * weight[id_i])`, and print only `json.dumps({"schema_version": 2, "build_cost": cost})` to stdout with nothing else on stdout (route any debug prints to stderr). Since this is fuzz-tested against a hidden oracle across many random inputs, keep the arithmetic exactly integer (no float rounding drift) and make the script robust to any valid v1 JSON. `chmod +x` the script when done.