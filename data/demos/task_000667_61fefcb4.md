Treat this as a log-correlation forensics task: first `grep` `processor.log` for the panic marker to get the crash timestamps, then cross-reference each nearby `service_a.log` entry (by matching timestamp proximity or shared identifiers) to recover the exact `req_id`s involved — don't hand-pick req_ids, derive them from the logs. With those req_ids in hand, grep the JSONL files under `raw_data/` for each id and inspect the full payloads side by side with non-crashing payloads to spot the shared anomalous field values (an unusual `client_version` and a `duration` outside the normal range) — this is the "statistical anomaly," found by diffing crash vs. non-crash payloads, not assumed. Emit `/home/user/crash_timeline.csv` sorted chronologically with the exact header and one row per crashing req_id, pulling `timestamp`, `req_id`, `client_version`, and `duration` straight from the correlated data. For step 2, read `process.sh` to see exactly what condition it checks (grep for the panic-triggering logic) and build a JSON payload with `req_id":"req-test-999"` plus the anomalous client_version/duration values you identified, run `process.sh` with it via `$?` exit-code checking, and print the pass/fail message accordingly.