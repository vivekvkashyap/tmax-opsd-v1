Split this into a video-recovery step and a Go static-analysis step. For the secret: extract frames from `/app/incident_logs.mp4` around where scrollback text is visible using `ffmpeg -i /app/incident_logs.mp4 -vf fps=<N> frame_%04d.png` (try a few fps values / timestamps), then OCR each candidate frame (e.g. `tesseract frame.png stdout`) to recover the `JWT_SIGNING_SECRET` value — read it off the frames rather than assuming a value. Use that secret to HMAC-SHA256-sign a JWT for `sub=admin` with `exp` >1hr out (a small Python script with `PyJWT` or manual base64url+HMAC is fine), and save the raw token to `/home/user/admin_token.txt`. For the scanner: write `/home/user/policy_scanner.go` to iterate `.req` files in `/app/uploads/`, parsing the raw HTTP request text to pull the `Content-Disposition: filename=` value and reject traversal patterns (`../`, `..\`, leading `/`); locate and parse the embedded ELF body to check `e_ident[EI_CLASS] == ELFCLASS64` and to read the `.ssh_pubkey` section's contents by walking the ELF section header table, then validate the key type token is exactly `ssh-ed25519` (reject `ssh-rsa`, `ecdsa-*`, missing/malformed keys). Combine all three checks with AND logic, write `{filename: bool}` results to `/home/user/scan_results.json`, and iterate/test against multiple sample files since accuracy is graded across the full hidden set.