Split this into an offline extraction pass and a Go server that serves the precomputed data — don't try to do ffmpeg processing on every request. Use `ffmpeg -i /app/broadcast.mp4 -vf fps=1 frame_%03d.png` (or similar) to dump one frame per second, then compute each frame's average grayscale brightness (convert RGB via standard luminance weights, or load with a Go image library / a helper Python script and average pixel intensities) indexed by second T=0,1,2.... Extract subtitles with `ffmpeg -i /app/broadcast.mp4 -map 0:s:0 out.srt`, then parse the SRT timestamps and text with Go (or a helper script feeding into your Go program) — pay close attention to preserving UTF-8 correctly through file reads/writes/JSON encoding (Go's `encoding/json` handles UTF-8 natively, so avoid any lossy byte-level string manipulation). Match each second T to whichever subtitle cue's [start,end) timestamp range contains that second; parse `Payload: <text>` out of the `[MSGID: ...] Source: ... - Payload: ...` format. Build the Go template for `/report` and the two HTTP handlers (`/api/data`, `/report`) reading from your precomputed in-memory/second-indexed data structure, and keep the server running in the foreground/background bound to `127.0.0.1:8080` so the verifier's live HTTP requests succeed.