Work through this in dependency order since later stages need earlier ones working.

1. Fix `npm install` in `/home/user/api`: inspect `package.json` for the conflicting peer dependency versions (likely an old framework version pinned alongside a middleware/logging lib requiring a newer major version), bump the outdated pin to a compatible version (or use `--legacy-peer-deps` only as a last resort — prefer actually resolving the version conflict), then confirm `npm install` succeeds cleanly.

2. Write `frame_analyzer.c`: read stdin in a loop with `fread` into a buffer, process complete 3-byte groups only (discard trailing 1-2 leftover bytes), accumulate `total_score` as `int64_t`, applying `if (R > G+B) total_score += (R-G-B)` per pixel, then print `total_score % 1000003` to stdout. Write a Makefile compiling this with a standard C compiler.

3. In the Node API, implement `GET /analyze?frame=N` by spawning `ffmpeg` with `-vf "select=eq(n\,N)" -vframes 1 -f rawvideo -pix_fmt rgb24 -` piped into your compiled binary (spawn/pipe processes, don't shell out unsafely with untrusted input) and parse its stdout score into the JSON response.

4. Loop frames 0-100 calling your own running API, track the max score, and write the winning `{frame, score}` to `/home/user/highest_frame.json`.
