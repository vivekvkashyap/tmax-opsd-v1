Use `find /home/user/datasets_raw -type f \( -iname '*.csv' -o -iname '*.json' \)` to enumerate exactly the dataset files, ignoring everything else (.log, .tmp, .py, etc.) — don't hand-filter by directory, rely on the extension match so you don't miss nested subdirectories. For each match, compute its path relative to /home/user/datasets_raw (strip the prefix) and its SHA-256 via Python's `hashlib.sha256(open(path,'rb').read()).hexdigest()` (or `sha256sum`), and build a single JSON dict of relative-path -> hex-digest, writing it to /home/user/dataset_manifest.json. When building the tarball, use Python's `tarfile` (or `tar` with an explicit file list) and set each member's `arcname` to the same relative path used in the manifest so the archive's internal layout mirrors the dataset structure without the `datasets_raw` prefix; add the manifest file itself at the archive root as `dataset_manifest.json`. Before finishing, extract your own tarball to a scratch directory and independently re-hash each extracted file to confirm it matches the manifest, and confirm no junk files ended up inside the archive.