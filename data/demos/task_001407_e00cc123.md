Write the C++ program to link against the HDF5 C++ API and compile it explicitly, e.g. `g++ regularize.cpp -o regularize -lhdf5_cpp -lhdf5` (add `-I`/`-L` flags if HDF5 headers/libs aren't on the default path — check with `pkg-config --cflags --libs hdf5` or `h5c++ -show`). Use `H5::H5File`/`H5::DataSet::read` to load the 10x10 `cov_matrix` into a flat `double[100]` buffer, and the same API to write `reg_matrix` back with a matching `H5::DataSpace`. Seed `std::mt19937 gen(42);` once, outside the loop, and use `std::uniform_real_distribution<double>(0.0, 0.01)` sampled fresh for each of the 10 diagonal entries per iteration (so 100,000 total draws in a fixed, deterministic order) — reseeding per iteration or per element would break reproducibility. Accumulate the mean matrix by summing all 10000 regularized matrices element-wise then dividing by 10000, and separately record each iteration's trace (sum of the 10 diagonal entries) into an array. Sort a copy of the trace array and index into it at the 2.5th and 97.5th percentile positions (e.g. `floor(0.025*(N-1))` and `floor(0.975*(N-1))`) to get the bootstrap CI bounds, then write `lower,upper` to the text file. Double check off-diagonal entries of the output equal the original matrix exactly (only the diagonal changes).
