Build this as a clear pipeline of independently testable stages. First install pandas/pyarrow, then load `raw_sensor_data.csv` and apply the three schema checks as vectorized/row-wise validations: regex-match `sensor_id` against `^SENS-\d{4}$`, parse `timestamp` with a strict ISO-8601 parser (catching exceptions rather than silently coercing invalid strings to NaT), and range-check `raw_value` inclusively in [-10.0, 10.0] — drop any row failing any check, then write the survivors to Parquet. Filter that cleaned data to just `sensor_id == "SENS-0042"` and apply the standard conjugate Gaussian update: posterior variance = 1/(1/prior_var + n/meas_var), posterior mean = posterior_var * (prior_mean/prior_var + sum(x)/meas_var) — derive these from the values actually present in your cleaned data, don't hardcode them. Factor the posterior-computation math into its own function taking an array of values, then time 10,000 calls with `time.perf_counter()` around a loop, converting total elapsed seconds to average milliseconds per call. Round the two Bayesian outputs to 4 decimals and assemble the four required keys into `/home/user/results.json` exactly as named.