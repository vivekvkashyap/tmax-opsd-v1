Treat this as black-box reverse engineering, not guessing. First run `strings /app/graph_oracle` and `file`/`objdump` on it to spot clues (error string literals, format strings) even if stripped. Then systematically probe with crafted stdin: (1) empty input, (2) a single V line, (3) simple 2-3 node chains to learn the success output format precisely (spacing, arrow style, delimiter before latency), (4) a two-node cycle to see the exact cycle error text, (5) an edge referencing an undefined node, (6) malformed lines (wrong arg count, non-integer or negative latency, unknown directive) to find the exact error string and whether it checks malformed-before-undeclared-before-cycle (establish the precedence order of the three error checks by feeding inputs that trigger multiple conditions at once). For the core algorithm, test graphs with multiple equal-length paths to deduce the tie-break rule (compare candidate paths' concatenated node-ID strings). Build your Python implementation incrementally, verifying byte-for-byte against the binary after each discovery using a small harness that runs both programs on the same stdin and diffs stdout exactly (including trailing newline). Only after manual cases match should you layer on a hypothesis-based fuzzer to hunt for edge cases (self-loops, duplicate V definitions, whitespace variations).