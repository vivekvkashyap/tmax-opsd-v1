Use delta debugging methodically rather than guessing: split `/home/user/bad_input.csv` into individual lines and binary-search (or linearly test) which single line, when POSTed alone to the running service, causes the hang — a line containing an unbalanced quote character is the classic trigger for a quote-scanning parser, so look closely at lines with an odd number of `"` characters. Save that exact line (verbatim, no added newline) to `/home/user/poison_pill.txt`. For root-causing, run `git log -p` in `/app/fast-csv-api` to find the commit that changed the quote-handling logic in the CSV parser — compare the diff to see what changed in how an unclosed quote is handled (e.g. a loop that fails to advance its index or `break` when `str.find()` returns -1, causing it to spin forever). Patch the parsing loop so an unmatched quote is detected and the loop terminates or errors out instead of looping. Then start `python server.py` in the background from that directory and manually POST your poison pill with a short timeout to confirm it now returns within well under 1 second before finishing.
