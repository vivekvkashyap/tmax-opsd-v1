Tackle this in independently-verifiable pieces so you can test each before wiring them together. For the password: read `/app/auth_config.json`'s hash, identify it's MD5 (32 hex chars), and crack it with a fast dictionary/common-password wordlist attack (e.g. try common weak passwords directly, or use `john`/`hashcat` if available, or a small Python script hashing candidates from a common-passwords list) — write the recovered plaintext exactly to `/home/user/cracked_password.txt`, then replace the config's hash with `bcrypt.hashpw()` of a new >=16-char alphanumeric password you generate. For the redirect validator: don't write ad-hoc regex from memory — inspect `/app/corpus/clean/` and `/app/corpus/evil/` to see the actual URL shapes, then implement `/validate` logic that accepts only paths starting with a single `/` and not `//` (protocol-relative), rejects absolute URLs (`scheme://`), `//host` and non-http(s) schemes like `javascript:`; test against both corpora directly with curl before integrating. For NGINX: use the `auth_request` directive pointing at your validator's `/validate` endpoint on port 8080, passing through the `next` query param, so a 403 from the validator short-circuits before reaching Flask on 5000. Start all four processes and curl the end-to-end flow through port 8000 to confirm before finishing.