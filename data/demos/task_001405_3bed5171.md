Before touching anything else, read `/home/user/aggregate.c` closely and look for a type mismatch: accumulating floating-point values (embedding components typically in [-1,1]) into an integer accumulator will silently truncate every addition toward zero, producing sums (and thus means) of 0. The fix is simply to change the accumulator array's declared type from int to double (and its zero-initializer) so fractional contributions aren't discarded — don't rewrite the parsing/IO logic, the bug is isolated to that one declaration and the accumulation loop implicitly benefiting from it once the type is corrected. After fixing, compile with `gcc -o /home/user/aggregate /home/user/aggregate.c` (add `-lm` if math functions are used), run it against the existing `/home/user/embeddings.csv`, and confirm `/home/user/centroid.csv` has one line of 5 comma-separated values each formatted to exactly 4 decimal places (`%.4f`) with no trailing newline issues. Cross-check your result independently, e.g. with an awk one-liner computing column means from the CSV, to confirm the fix produces numerically correct centroids rather than just "not all zero."