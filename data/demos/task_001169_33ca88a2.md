Split this into "fix the vendored dependency" and "write the scheduler" and verify each in isolation. First, don't just assume the vendored `toposort` package is broken in some complex way — try importing it (`python3 -c "import sys; sys.path.insert(0,'/app/toposort-1.10'); import toposort"`) and read the traceback carefully; vendored-package perturbations in these tasks are usually a single deliberately introduced syntax/logic error at one specific line, so diff against your knowledge of what correct Python looks like (e.g. check for a missing colon, wrong operator, or off-by-one) and fix only that minimal defect in `/app/toposort-1.10/toposort.py` — do not rewrite the whole file. Second, for `scheduler.py`: parse the JSON manifest, compute `cumulative_cost` via a memoized recursive traversal (own cost plus cumulative cost of each immediate dependency), then use `toposort.toposort_flatten(deps_dict, sort=True)` — but since the built-in `sort=True` doesn't necessarily give you full deterministic tie-breaking control the spec demands, implement your own alphabetical-tie-break topological sort (Kahn's algorithm: repeatedly take the alphabetically-sorted list of currently-ready nodes) to guarantee reproducible ordering. Print strictly the JSON array to stdout with no other output.
