Start by running `tesseract /app/policy.png stdout` to OCR the policy image and pull the department code with a regex like `grep -oP 'DEPARTMENT_CODE:\s*\K\S+'` — don't hardcode any guessed code, always derive it fresh from OCR so the script is portable. For the archive script, list contents without extracting first via `tar -tf "$1"`, and reject (exit 1) if any listed path starts with `/` or contains a `../`/`..` traversal segment — use a regex that catches `../` anywhere in the path, at the start, in the middle, or as a full path component, since evil fixtures use absolute, purely-relative, and mixed traversal styles. Only after the path check passes, verify `settings.conf` exists at the archive root and extract it safely (`tar -xOf "$1" settings.conf`) to read the `DEPT=` value and compare it against the OCR'd code exactly. Test iteratively against both `/app/corpora/clean/` and `/app/corpora/evil/`, looping over each file and checking exit codes (`for f in /app/corpora/clean/*.tar; do bash validate_config.sh "$f"; echo $?; done`) until you hit 100% on both before finishing; remember `chmod +x` on the final script.
