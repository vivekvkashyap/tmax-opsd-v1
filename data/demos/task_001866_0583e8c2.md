Split this into the video task and the parser-recovery task, and treat the parser as an exact black-box equivalence problem, not a "roughly matches" one. For the video: extract frames with `ffmpeg -i /app/dashboard.mp4 frame_%04d.png` (or read via OpenCV) and scan sequentially for the first frame where the status indicator's text/color changes from healthy to critical — check neighboring frames around any suspected transition to pinpoint the exact frame number precisely, then write that single integer to `/home/user/crash_frame.txt`. For the parser: use `git log --all --diff-filter=D -- '*parser*'` (or similar) in `/home/user/uptime_repo` to find the commit that deleted the file, then `git show <commit>^:<path>` to recover its original content. Since you have `/app/oracle_parser` as a ground-truth reference, treat correctness as behavioral equivalence: write a fuzzing/diff harness that feeds many random and edge-case strings (empty, very long, binary garbage, malformed formats) to both the oracle and your fixed parser and diffs stdout byte-for-byte — iterate on your fix until outputs match on all generated cases, not just the one bug you initially spot in the recovered source. Compile/wrap the fixed version as an executable at exactly `/home/user/parser`, `chmod +x` it, and rerun your fuzz-diff harness one final time before finishing.