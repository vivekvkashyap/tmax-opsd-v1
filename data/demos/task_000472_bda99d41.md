Handle the two parts independently and test each in isolation. For the vendored package: open `/app/vendored/mail-sender-v2/setup.py` and look closely at the `install_requires` list — a missing comma between two string literals causes Python to silently concatenate them into one malformed requirement string, which breaks the install; add the comma. Then open `mail_sender/connection.py` and replace the reference to the nonexistent env var with the standard one, using `os.environ.get('SMTP_PORT', <sensible-default>)` instead of a hard `os.environ[...]` KeyError-prone lookup on the wrong variable name. Reinstall with `pip install -e /app/vendored/mail-sender-v2` and confirm `import mail_sender` and calling its connect function work without KeyError. For the path checker, implement `is_safe_backup_path` as a sequence of explicit, independent checks (prefix match with `str.startswith`, substring check for `..`, a whitespace check e.g. via regex or `any(c.isspace() for c in path)`, an explicit membership check against the exact metacharacter set given, and a length check) — don't rely on a single regex that might miss an edge case, and be precise about the length threshold being inclusive. Test your function manually against a few paths you construct yourself (both valid and clearly malicious) before trusting it against the hidden corpus.
