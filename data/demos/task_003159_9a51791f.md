The core difficulty is CSV parsing with embedded newlines inside quoted fields, which naive line-by-line reading (`fgets` per line) breaks on. In your C parser, read the file character-by-character (or buffer-by-buffer) and track a boolean "inside quotes" state that toggles on each unescaped `"`; only treat a `\n` as a record delimiter when that state is false, otherwise treat it as literal content within the current field. Split each completed record on commas only when outside quotes (commas inside quoted `notes` won't occur here per the header, but keep the quote-aware approach general). Track running sum and count of the `temp` field (column 3) as you parse each record, and count anomalies where `temp > 45.0` strictly. Compute the average as sum/count rounded to 2 decimal places using consistent rounding (not truncation) and append the exact `Records: <n>, AvgTemp: <x.xx>, Anomalies: <n>` line to `results.log`. In `run_job.sh`, compile with `gcc` before running, and make sure both files are executable. For the cron entry, use `(crontab -l 2>/dev/null; echo "15 3 * * * /home/user/pipeline/run_job.sh") | crontab -` to append without clobbering any existing crontab, and verify with `crontab -l` afterward.
