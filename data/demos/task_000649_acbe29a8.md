Work the pipeline in strict phase order and verify each artifact before moving on. First extract frames from the MP4 with `ffmpeg` (e.g. `ffmpeg -i /app/network_status.mp4 frame_%02d.png`) or read frames directly with OpenCV/PyAV in Python; for each frame, scan pixels for the one that is pure red (255,0,0), find its (x,y), and convert to a node id with `node_id = (x/10) + (y/10)*10` — be careful about integer division and confirm you're using 0-indexed frame numbers matching `frame_id,node_id` rows in `active_nodes.csv`. Second, in C, parse `/app/network_graph.csv` (skip the header) into an adjacency list (undirected: add edges both ways) and implement Dijkstra from node 0, computing costs on demand per query rather than precomputing once, since queries reference arbitrary frame_ids mapped through your CSV. Third, build a TCP server with `socket/bind/listen/accept` on 127.0.0.1:8888 that reads lines terminated by `\n`, parses `QUERY <frame_id>`, looks up the node for that frame, runs the shortest-path calc, and replies `PATH_COST: <cost>\n` (use -1 for unreachable). Handle multiple queries per connection in a loop until EOF/disconnect, and accept new connections in an outer loop. Compile, launch in background with `&`, and confirm with `ss`/`netstat` that the port is listening before finishing.