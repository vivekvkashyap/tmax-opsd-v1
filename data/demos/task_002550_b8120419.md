First view/OCR `/app/filter_spec.png` to read off the exact 5-point filter coefficients (indices -2..2) and the boundary rule (what to do when a tap falls outside the array) and the spectral energy formula — do not guess these values. Then write the Go program: parse the stdin line into a []float64, compute the smoothed signal y[i] by summing coefficient*x[i+offset] terms while skipping (treating as zero, per the spec) out-of-range indices, then compute energy by accumulating a single float64 in a plain sequential for-loop over i=0..N-1 adding one y[i]*y[i] term per iteration — never use math functions, parallel goroutines, sum-reduction helpers, or reordered/pairwise summation, since the grader requires bit-for-bit match with a reference that sums strictly in order. Print with exactly `fmt.Printf("%.8f\n", E)`. Compile with `go build -o /home/user/process_spectrum /home/user/process_spectrum.go`, then self-test against a few small hand-computed inputs (including edge cases near the boundaries) before trusting it against the fuzz harness.
