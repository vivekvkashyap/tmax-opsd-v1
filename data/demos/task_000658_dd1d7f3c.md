Use Python's `pathlib.Path.rglob("*.wal")` to recursively find all .wal files across subdirectories. Parse each file line-by-line with a state machine: watch for a line matching `BEGIN TX (\d+)` to start buffering a block (including that line), keep appending lines until you hit either `COMMIT TX <id>` or `ROLLBACK TX <id>` (also included in the buffer), then decide whether to keep the block based on which terminator matched — discard ROLLBACK blocks entirely. Extract the transaction id as an int (not string) so sorting is numeric, not lexicographic (`10` before `9` bug). Collect (id, block_text) tuples across all files, sort by id ascending, then join the blocks with exactly one blank line between them and write to commits.wal (mind trailing newline consistency with the given example). For chunking, read commits.wal as raw bytes and slice into consecutive 150-byte segments with plain Python slicing (`data[i:i+150]`), writing each to chunk_NN.dat with zero-padded two-digit indices — don't chunk by lines or characters, it must be exact byte counts. Create the chunks directory with `os.makedirs(..., exist_ok=True)` before writing.
