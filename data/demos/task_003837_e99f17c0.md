Diagnose the 502 by checking each hop in the request path independently: first confirm what port nginx's upstream/proxy_pass actually expects (read `/home/user/nginx/nginx.conf`), then check what port the Rust binary actually binds to when started plainly — since the task states it falls back to a wrong default port when its `.env` isn't found from the right working directory, try running the binary from different working directories (or inspect its startup code/how it loads `.env`) to see this fallback in action, rather than assuming env vars alone will fix it. Fix it robustly by explicitly setting/exporting the PORT env var (or correctly `cd`-ing into the directory containing `.env`, or passing `--env-file`) right before launching the server in your script, so it reliably binds to the port nginx expects. Build `/home/user/deploy.sh` to be idempotent and safe to re-run: kill any existing server process by name/pattern first, start the new one backgrounded with correct env, sleep briefly or poll until the port responds, then do a graceful `nginx -s reload` (not a full restart) using the existing config file. Run the script once and verify end-to-end with `curl` against nginx's port, not the Rust server's port directly.