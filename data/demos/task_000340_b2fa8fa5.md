Recover the obfuscation scheme empirically: run `/app/legacy_scanner <ip> <port>` for a couple of known ports, hex-decode the output, and look for a fixed single-byte XOR key — since decrypted JSON starts with `{"`, XOR-ing the first output byte against `'{'` gives you a key candidate, then confirm it holds across the whole string and other ports. Do not hardcode the scanner's mock output in your wrapper — always exec the real binary via `os/exec` with a 5-second `context.WithTimeout`, capture stdout, hex-decode, then XOR-decrypt with the discovered key. For redaction, parse into a `map[string]interface{}` and replace only the exact top-level keys `password`, `api_token`, `secret` with the literal string `[REDACTED]`, leaving everything else untouched, then re-marshal to JSON. Build the Go HTTP server bound to `127.0.0.1:9090` with a `POST /api/scan` handler parsing `{"target_ip":..., "target_port":...}`, returning 500 on scanner failure/timeout and 200 with `Content-Type: application/json` otherwise, and run it in the background so it stays up for the verifier's HTTP requests.
