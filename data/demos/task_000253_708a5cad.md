Do this in strict order. First, recover the magic marker string embedded as a text label in `/app/diagram.png` via OCR (`pytesseract`/`Pillow`) — crop/zoom if the first pass is noisy — confirm it's a `MARKER: <SOMETHING>` line, and use exactly that recovered ASCII string as your constant; don't guess it. Second, write `parse_single.py` as a streaming scanner: read stdin, repeatedly `find()` the marker bytes, read the next 4 bytes as little-endian length (`struct.unpack("<I", ...)`), slice that many payload bytes, and try UTF-8 decode — if the slice overruns the buffer or decoding fails, it's a false match, so advance the search by just 1 byte (not past the whole record) so a genuine following record isn't skipped. Apply DRAFT→FINAL and `rstrip()` before printing. Third, build `watcher.py` with polling/`inotify` for new `.bin` files, reuse the same parsing logic, write to a temp file then `os.rename()` into `/home/user/processed/<name>.txt` for atomicity, and update the `latest.txt` symlink. Test with a handcrafted binary file containing your recovered marker before trusting the watcher end-to-end.
