Start by locating the sabotage: grep the vendored library at `/app/networkx-3.1` for the raised exception string to find exactly which function throws it, rather than guessing. Fix it in place (remove the injected `raise` block) so the underlying algorithm behaves like stock networkx — do not rewrite the algorithm yourself, just excise the perturbation. Then write `/home/user/graph_query.py` so it inserts `/app/networkx-3.1` at the front of `sys.path` before importing networkx, opens the SQLite DB read-only, loads only rows with `is_active=1` into a directed or undirected graph matching the schema (edges also require both endpoints active), and for each stdin line calls the library's unweighted shortest-path function wrapped in a try/except that returns `-1,-1` on `NetworkXNoPath` or missing nodes. Sum node weights along the returned path for `total_weight`. Test with a handful of manual pairs against your own graph traversal logic before assuming correctness, since the verifier fuzzes hundreds of pairs and needs bit-exact match with a pristine-networkx oracle — so any behavioral divergence (e.g., wrong path length tie-breaking or inactive nodes leaking in) will be caught.