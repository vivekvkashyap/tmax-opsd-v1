First derive the analytical CDF yourself by integrating the given PDF f(x) = 3/4(1-x^2) from -1 to x, and simplify to a closed-form polynomial in x — you'll need this exact formula in your Go code for comparison. Implement rejection sampling: since f(x) is bounded on [-1,1] by a known maximum (evaluate f at its peak to find the envelope constant M), sample a candidate x uniformly in [-1,1] and a uniform u in [0,M], accept x if u <= f(x), else retry. For concurrency, spawn exactly 4 goroutines each with its own `rand.New(rand.NewSource(seed))` (a shared global rand.Source or a single lock-protected RNG will bottleneck/serialize and defeats the purpose — give each goroutine a distinct seed, e.g. derived from time plus worker index), collect their samples via channels or a sync.WaitGroup with a shared results slice guarded appropriately. To compute KS distance: sort the combined samples, evaluate the empirical CDF at the 201 evenly spaced points (0.01 step from -1.0 to 1.0) via a binary search or single pass over the sorted array, compute the analytical CDF at the same points, take the max absolute difference. Repeat for N=1e4, 1e5, 1e6 and write one CSV line per N in ascending order to the exact log path.