Write a recursive extractor that identifies archive type by sniffing the first few bytes of each file rather than trusting its extension: read the first 4 bytes and compare against `b"PK\x03\x04"` (zip) and `b"\x1f\x8b"` (gzip) signatures. For each discovered archive, extract it into a fresh temp directory (`tempfile.mkdtemp()` or similar) — use Python's `zipfile.ZipFile` for ZIP signatures and `tarfile.open(mode="r:gz")` for GZIP signatures (since these are gzipped tarballs, not bare gzip files) — then recurse into that temp directory applying the same signature-sniffing logic to every extracted entry, since archives can nest arbitrarily deep. For non-archive regular files, only if the filename ends in `.md` do you read and collect its contents, keyed by basename. After the full recursive walk completes, sort the collected `.md` entries alphabetically by basename, and for each print `### FILE: <basename>` then the exact raw file contents then a blank line, all to stdout — do not add extra formatting or trailing whitespace differences. Finally run `python3 /home/user/parse_docs.py > /home/user/compiled_docs.md` and diff the result against what you'd expect from tracing the nested archive structure yourself.